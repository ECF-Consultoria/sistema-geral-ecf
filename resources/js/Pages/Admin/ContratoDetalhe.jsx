import AppLayout from '@/Layouts/AppLayout';

/**
 * Admin/ContratoDetalhe.jsx — Fase 131 Plano 04 (D-01/ADM-01/ADM-02/UI-02).
 *
 * Placeholder MÍNIMO criado na Task 1 — sem ele o Inertia não resolve o
 * componente e o manifest do Vite não tem a entrada, e o teste da própria
 * Task 1 não passaria nem de 200 (mesmo padrão do 131-03, ver SUMMARY
 * daquele plano). A Task 2 substitui este conteúdo pela tela completa, no
 * MESMO arquivo.
 */
export default function ContratoDetalhe() {
    return (
        <AppLayout title="Adm · Contrato">
            <main className="p-6" />
        </AppLayout>
    );
}
