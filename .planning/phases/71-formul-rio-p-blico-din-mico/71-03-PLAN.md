---
phase: 71-formul-rio-p-blico-din-mico
milestone: v15.0
plan: 71-03
type: execute
wave: 3
depends_on: [71-01, 71-02]
files_modified:
  - tests/Feature/Phase71/NpsRespondRenderTest.php
  - tests/Feature/Phase71/NpsRespondSubmitFlowTest.php
autonomous: true
requirements: [NPS-D-01, NPS-D-02, NPS-D-03, NPS-D-04, NPS-D-05]
tags: [nps, tests, feature, phpunit, phase71, sc-verification, respond, submit, thankyou, alreadycompleted, expired]

must_haves:
  truths:
    - "Suite Feature Phase71 cobre SC1-SC5 do ROADMAP: (1) respond injeta template prop v15, (2) legacy sem template_id mantém props Phase 33, (3) obrigatórias enforced server-side (422), (4) ThankYou/AlreadyCompleted/Expired renderizados nos branches corretos, (5) fluxo submit v15 completo cria NpsResponse + N NpsResponseAnswer com snapshot congelado"
    - "Cada teste roda contra SQLite in-memory (RefreshDatabase)"
    - "Zero regressão em Phases 31/33/68/69/70 — rodar suite completa ao final e confirmar delta = 0"
    - "Não testa render pixel-a-pixel (Inertia assert prop presence + shape); interação de UI validada manualmente no Plan 71-02 T4"
    - "Guard de expiração testado: status='pending' + updated_at antigo → renders Expired e atualiza status='expired' no banco"
  artifacts:
    - path: tests/Feature/Phase71/NpsRespondRenderTest.php
      provides: "5 testes: (1) v15 injeta template com id/perguntas/opções, (2) legacy sem template_id retorna template=null e props legacy, (3) status='completed' → AlreadyCompleted, (4) expired → Expired + update status='expired', (5) survey inválido → 404"
      min_lines: 200
    - path: tests/Feature/Phase71/NpsRespondSubmitFlowTest.php
      provides: "5 testes: (1) submit v15 completo cria NpsResponse + N answers com snapshot, (2) obrigatória omitida → 422 answers.<qid> required, (3) option_id de outro template → 422 Rule::in, (4) dedup 23000 dispara AlreadyCompleted, (5) redirect final → Nps/ThankYou"
      min_lines: 230
  key_links:
    - from: tests/Feature/Phase71/*.php
      to: database/factories/NpsTemplateFactory.php
      via: "NpsTemplate::factory()->has(NpsTemplateQuestion::factory()->has(NpsTemplateOption::factory()->count(5))->count(3))->create()"
      pattern: "NpsTemplate::factory"
    - from: tests/Feature/Phase71/*.php
      to: app/Http/Controllers/NpsController.php
      via: "response = $this->get(route('nps.respond', $survey->token)) e response->assertInertia(fn (\$p) => \$p->component('Nps/Respond')->has('template'))"
      pattern: "assertInertia.*Nps/Respond"
---

<objective>
Entregar cobertura Feature completa para os 5 SC do ROADMAP Phase 71 — 2 arquivos de teste totalizando 10 testes que exercitam:
- Prop injection do template (Plan 71-01)
- Roteamento v15 vs legacy no Respond.jsx (Plan 71-02)
- Fluxo submit end-to-end reaproveitando contrato Phase 69-03

Purpose: Sem Feature tests, o backend/frontend Phase 71 pode ficar frágil frente a mudanças posteriores (Phase 72 dashboards, Phase 73 cleanup). Esta suite trava contrato:
1. Backend injeta shape correto do template payload
2. Dual-path funciona (v15 novo + Phase 33 legado)
3. Submit produz NpsResponseAnswer com snapshot congelado (invariante Phase 68)
4. Guards de expiração e completeness comportam-se corretamente
5. Server-side rejeita obrigatórias vazias mesmo se cliente contornar

Output:
- 2 arquivos em `tests/Feature/Phase71/`
- Cobertura de 100% dos SC do ROADMAP Phase 71
- Zero regressão em suites Phase 31/33/68/69/70
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/PHASE-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/PHASE-SUMMARY.md
@.planning/phases/70-ui-de-configuracao-admin/PHASE-SUMMARY.md
@.planning/phases/71-formul-rio-p-blico-din-mico/71-01-PLAN.md
@.planning/phases/71-formul-rio-p-blico-din-mico/71-02-PLAN.md
@app/Http/Controllers/NpsController.php
@app/Models/NpsSurvey.php
@app/Models/NpsTemplate.php
@app/Models/NpsTemplateQuestion.php
@app/Models/NpsTemplateOption.php
@app/Models/NpsResponse.php
@app/Models/NpsResponseAnswer.php
@database/factories/NpsTemplateFactory.php
@database/factories/NpsTemplateQuestionFactory.php
@database/factories/NpsTemplateOptionFactory.php
@database/factories/NpsSurveyFactory.php
@database/factories/NpsResponseFactory.php
@tests/Feature/Phase69/NpsSubmitDynamicValidationTest.php
@tests/Feature/Phase70/NpsTemplateCrudTest.php
@tests/TestCase.php

<interfaces>
Padrão de teste do projeto (herdar de Phase 69 e Phase 70):
- `use RefreshDatabase;`
- Sem `actingAs` — rotas `/nps/{token}` são públicas (guarded por token)
- Assertions: `$response->assertInertia(fn (Inertia\Testing\AssertableInertia $page) => $page->component('Nps/Respond')->has('template'))`
- Route helpers: `route('nps.respond', $survey->token)` — GET; `route('nps.submit', $survey->token)` — POST

From tests/Feature/Phase69/NpsSubmitDynamicValidationTest.php:
- Padrão de setup: fixtura de template com 3 perguntas escala + survey associado + submit via POST com answers.<qid>=<option_id>
- Reutilizar helpers privados se `fixtureTemplate*` estiver definido

From app/Http/Controllers/NpsController.php:
- Rota respond: `Route::get('/nps/{token}', ...)` — nome `nps.respond`
- Rota submit: `Route::post('/nps/{token}', ...)` — nome `nps.submit`
- submitResponseV15 renderiza `Nps/ThankYou` no sucesso, `Nps/AlreadyCompleted` em dedup 23000
</interfaces>

<research_reference>
ROADMAP Phase 71 SC → mapeamento de testes:
- **SC#1** (renderiza dinamicamente do template snapshot) → NpsRespondRenderTest T1 (v15 template injected)
- **SC#2** (radio cinza/amarelo mobile-friendly) → não testável em Feature test (validação manual T4 do Plan 71-02) — mas testamos que a prop tem shape suficiente para o front renderizar
- **SC#3** (obrigatórias marcadas + submit disabled client-side + server-side 422) → NpsRespondSubmitFlowTest T2 (obrigatória omitida retorna 422 com validation error em `answers.<qid>`)
- **SC#4** (ThankYou/AlreadyCompleted/Expired preservados) → NpsRespondRenderTest T3/T4 + NpsRespondSubmitFlowTest T4/T5
- **SC#5** (zero jargão técnico) → não testável em Feature test (validação manual + grep no Respond.jsx no Plan 71-02 T3 acceptance)

Convenções do projeto:
- Namespace `Tests\Feature\Phase71`
- Métodos `test_ok_...`, `test_valida_...`, `test_guard_...`
- Assertions em pt-BR quando checa mensagem visível (`assertSee('Já respondida')`)
</research_reference>
</context>

<tasks>
<task id="71-03-T1">
<title>NpsRespondRenderTest — 5 testes cobrindo Inertia render shape e guards</title>

<read_first>
- tests/Feature/Phase69/NpsSubmitDynamicValidationTest.php (padrão de setup fixture template + survey)
- tests/Feature/Phase70/NpsTemplateCrudTest.php (padrão RefreshDatabase + factory chains)
- app/Http/Controllers/NpsController.php linhas 307-380 (respond atualizado Plan 71-01)
- app/Models/NpsSurvey.php (fillable + isExpired + expires_at)
</read_first>

<action>
Criar `tests/Feature/Phase71/NpsRespondRenderTest.php`.

Estrutura:
```php
<?php
namespace Tests\Feature\Phase71;

use Tests\TestCase;
use Illuminate\Foundation\Testing\RefreshDatabase;
use App\Models\{Company, NpsSurvey, NpsTemplate, NpsTemplateQuestion, NpsTemplateOption};
use Carbon\Carbon;
use Inertia\Testing\AssertableInertia as Assert;

class NpsRespondRenderTest extends TestCase
{
    use RefreshDatabase;

    private function fixtureTemplateWithQuestions(): NpsTemplate {
        $t = NpsTemplate::factory()->create(['nome' => 'Template Teste', 'active' => true]);
        $q1 = NpsTemplateQuestion::factory()->create([
            'template_id' => $t->id, 'texto' => 'Como avalia o serviço?',
            'tipo' => 'escala', 'dimensao' => 'geral', 'obrigatoria' => true, 'ordem' => 1,
        ]);
        for ($i = 1; $i <= 5; $i++) {
            NpsTemplateOption::factory()->create([
                'question_id' => $q1->id, 'label' => (string)$i, 'peso' => $i, 'ordem' => $i,
            ]);
        }
        return $t->fresh(['questions.options']);
    }

    private function fixtureSurveyV15(NpsTemplate $template): NpsSurvey {
        $company = Company::factory()->create(['name' => 'Empresa Teste']);
        return NpsSurvey::factory()->create([
            'company_id'      => $company->id,
            'template_id'     => $template->id,
            'status'          => 'pending',
            'token'           => 'test-token-v15',
            'month_reference' => Carbon::now()->startOfMonth(),
            'expires_at'      => Carbon::now()->addDays(7),
        ]);
    }
}
```

Testes:

1. **`test_v15_survey_com_template_id_injeta_template_prop_com_shape_completo()`:**
   - Setup: template com 1 pergunta escala 5 options + survey v15
   - GET `/nps/{token}` → 200
   - `$response->assertInertia(fn (Assert $p) => $p
       ->component('Nps/Respond')
       ->has('template')
       ->has('template.id')
       ->where('template.nome', 'Template Teste')
       ->has('template.perguntas', 1)
       ->where('template.perguntas.0.texto', 'Como avalia o serviço?')
       ->where('template.perguntas.0.tipo', 'escala')
       ->where('template.perguntas.0.dimensao', 'geral')
       ->where('template.perguntas.0.obrigatoria', true)
       ->has('template.perguntas.0.options', 5)
       ->where('template.perguntas.0.options.0.peso', 1)
       ->where('template.perguntas.0.options.0.label', '1')
       ->has('template.perguntas.0.options.0.id')
     );`

2. **`test_legacy_survey_sem_template_id_recebe_template_null_e_props_legacy()`:**
   - Setup: survey com `template_id = null`
   - GET → 200
   - `$response->assertInertia(fn (Assert $p) => $p
       ->component('Nps/Respond')
       ->where('template', null)
       ->has('survey.estrategista_name')
       ->has('survey.tem_analista')
       ->has('survey.textos')
       ->has('perguntas_extras')
     );`

3. **`test_survey_completed_retorna_already_completed()`:**
   - Setup: survey v15 mas com `status='completed'`
   - GET → 200
   - `$response->assertInertia(fn (Assert $p) => $p->component('Nps/AlreadyCompleted'));`

4. **`test_survey_expired_renderiza_expired_e_atualiza_status()`:**
   - Setup: survey v15 com `expires_at = Carbon::now()->subDays(1)` (já expirou)
   - GET → 200
   - `$response->assertInertia(fn (Assert $p) => $p->component('Nps/Expired'));`
   - Assert `$this->assertDatabaseHas('nps_surveys', ['id' => $survey->id, 'status' => 'expired']);`

5. **`test_token_invalido_retorna_404()`:**
   - GET `/nps/token-inexistente` → 404
</action>

<acceptance_criteria>
- File `tests/Feature/Phase71/NpsRespondRenderTest.php` exists com 5 métodos `test_`
- Assert Inertia shape `template.perguntas.0.options.0.id` presente — assertar via `grep -c "template\\.perguntas\\.0\\.options\\.0\\.id" tests/Feature/Phase71/NpsRespondRenderTest.php` >= 1
- Assert legacy `template=null` + props legacy — assertar via `grep -n "'template', null\\|where..'template', null" tests/Feature/Phase71/NpsRespondRenderTest.php`
- Assert database status='expired' após acessar survey expirado — assertar via `grep -nE "assertDatabaseHas.*status.*expired" tests/Feature/Phase71/NpsRespondRenderTest.php`
- Rodar `php artisan test tests/Feature/Phase71/NpsRespondRenderTest.php` → 5/5 PASSED
</acceptance_criteria>
</task>

<task id="71-03-T2">
<title>NpsRespondSubmitFlowTest — 5 testes cobrindo submit v15 + guards server-side</title>

<read_first>
- tests/Feature/Phase69/NpsSubmitDynamicValidationTest.php (100% reference — a maioria dos casos aqui é subset validando integração Phase 71)
- app/Http/Controllers/NpsController.php linhas 420-620 (submitResponseV15 e submitResponseLegacy)
</read_first>

<action>
Criar `tests/Feature/Phase71/NpsRespondSubmitFlowTest.php` com namespace `Tests\Feature\Phase71`.

Reutilizar fixtures de setup do NpsRespondRenderTest — pode duplicar helpers privados OU criar uma parent class `Phase71TestCase extends TestCase` com `fixtureSurveyV15()`. Escolher pragmatismo: duplicar helpers (10 linhas cada) é mais simples que criar hierarquia.

Testes:

1. **`test_submit_v15_completo_cria_response_com_answers_snapshot()`:**
   - Setup: template com 2 perguntas escala 5 options cada
   - POST `/nps/{token}` com `answers: {[q1id]: option_peso_3_id, [q2id]: option_peso_5_id}, comment: 'ótimo', respondent_name: 'João'`
   - Assert redirect + Inertia render `Nps/ThankYou`
   - Assert `assertDatabaseHas('nps_responses', ['survey_id' => $survey->id, 'respondent_name' => 'João', 'comment' => 'ótimo'])`
   - Assert `assertDatabaseCount('nps_response_answers', 2)`
   - Assert `assertDatabaseHas('nps_response_answers', ['question_id' => q1->id, 'question_texto_snapshot' => 'Como avalia?', 'option_peso_snapshot' => 3])` (verifica snapshot congelado)
   - Assert `nps_surveys.status = 'completed'`

2. **`test_submit_v15_obrigatoria_omitida_retorna_422()`:**
   - Setup: template com 2 perguntas — q1 obrigatoria=true, q2 obrigatoria=false
   - POST omitindo `answers[q1id]`
   - `$response->assertStatus(422);`
   - `$response->assertJsonValidationErrors(['answers.' . q1->id]);`
   - Assert `assertDatabaseCount('nps_responses', 0);` (nada persistido)

3. **`test_submit_v15_option_id_de_outro_template_retorna_422()`:**
   - Setup: template A com 1 pergunta escala 5 options + template B com 1 pergunta escala 5 options
   - Survey associado a template A
   - POST com `answers: {[qA_id]: optionB_id}` (option pertence a template B)
   - `$response->assertStatus(422);` + validation error em `answers.<qA_id>` (Rule::in fails)

4. **`test_submit_v15_dedup_23000_retorna_already_completed()`:**
   - Setup: template + duas surveys com mesmo (company_id, month_reference, template_id) — primeiro completo, segundo pending
   - POST no pending
   - Assert Inertia render `Nps/AlreadyCompleted` (guard QueryException 23000 Phase 69-03)

5. **`test_submit_v15_redireciona_para_thankyou_no_sucesso()`:**
   - Setup: template válido + survey pending
   - POST answers válidas
   - `$response->assertInertia(fn (Assert $p) => $p->component('Nps/ThankYou'));`
   - Assert `nps_surveys.status = 'completed'` + `completed_at not null`

**Nota importante:** teste 4 pode duplicar teste da Phase 69-03 — se `NpsSubmitDynamicValidationTest::test_submit_v15_captura_query_exception_23000_e_renderiza_already_completed` já cobre, basta manter aqui um caso mínimo de sanity (sem re-validar todos os guards) para provar que o path Phase 71 (renderização + submit) ainda funciona end-to-end.
</action>

<acceptance_criteria>
- File exists com 5 métodos `test_`
- Snapshot testado — assertar via `grep -n "question_texto_snapshot\\|option_peso_snapshot" tests/Feature/Phase71/NpsRespondSubmitFlowTest.php`
- Rule::in cross-template testado — assertar via `grep -nE "template B|templateB|option_id.*outro" tests/Feature/Phase71/NpsRespondSubmitFlowTest.php`
- Dedup 23000 testado — assertar via `grep -nE "23000|AlreadyCompleted" tests/Feature/Phase71/NpsRespondSubmitFlowTest.php`
- Rodar `php artisan test tests/Feature/Phase71/NpsRespondSubmitFlowTest.php` → 5/5 PASSED
</acceptance_criteria>
</task>

<task id="71-03-T3">
<title>Rodar suite completa + baseline regressão</title>

<read_first>
- .planning/phases/70-ui-de-configuracao-admin/PHASE-SUMMARY.md (baseline: 108 tests NPS)
</read_first>

<action>
1. Baseline pré-plan:
   ```bash
   /c/xampp/php/php.exe artisan test tests/Feature/Phase31 tests/Feature/Phase33 tests/Feature/Phase68 tests/Feature/Phase69 tests/Feature/Phase70 --stop-on-failure 2>&1 | tail -10
   ```
   Registrar total tests (deve ser 108).

2. Rodar Phase 71 solo:
   ```bash
   /c/xampp/php/php.exe artisan test tests/Feature/Phase71 --stop-on-failure 2>&1 | tail -20
   ```
   Deve ser 10/10 PASSED.

3. Rodar suite completa (Phases 31/33/68/69/70/71):
   ```bash
   /c/xampp/php/php.exe artisan test tests/Feature/Phase31 tests/Feature/Phase33 tests/Feature/Phase68 tests/Feature/Phase69 tests/Feature/Phase70 tests/Feature/Phase71 2>&1 | tail -10
   ```
   Deve ser exatamente 118 tests PASSED (108 baseline + 10 novos). Zero regressão.

4. Se algum teste de Phase 31/33/68/69/70 quebrar → BLOQUEAR e reportar como regressão de Phase 71.
</action>

<acceptance_criteria>
- Phase 71 solo: 10/10 PASSED
- Suite NPS completa: 118 tests PASSED (delta = +10; zero regressão)
- Registrar assertions totais no SUMMARY (T4)
</acceptance_criteria>
</task>

<task id="71-03-T4">
<title>Escrever 71-03-SUMMARY.md e PHASE-SUMMARY.md</title>

<read_first>
- .planning/phases/70-ui-de-configuracao-admin/70-06-SUMMARY.md (padrão do Plan SUMMARY)
- .planning/phases/70-ui-de-configuracao-admin/PHASE-SUMMARY.md (padrão do PHASE-SUMMARY)
</read_first>

<action>
1. Escrever `.planning/phases/71-formul-rio-p-blico-din-mico/71-03-SUMMARY.md`:
   - Frontmatter YAML (phase/milestone/plan/tags/files_created/tests_created/assertions/regression_result/duration)
   - Corpo: objetivo do plan + mapeamento SC→testes + métricas medidas + regressão

2. Escrever `.planning/phases/71-formul-rio-p-blico-din-mico/PHASE-SUMMARY.md`:
   - Consolidação dos 3 plans da Phase 71
   - Requirements-completed: [NPS-D-01, NPS-D-02, NPS-D-03, NPS-D-04, NPS-D-05]
   - success-criteria-status: 5x PASSED
   - Métricas agregadas (files modified, LOC delta, tests, duration total)
   - Próximos passos: Phase 72 (Dashboards + pendências) desbloqueada
</action>

<acceptance_criteria>
- 71-03-SUMMARY.md exists com frontmatter válido
- PHASE-SUMMARY.md exists mapeando os 3 plans e 5 SCs
- Métricas com valores reais (não placeholder)
</acceptance_criteria>
</task>
</tasks>

<verification>
1. Phase 71 solo → 10/10 PASSED
2. Suite NPS completa → 118 PASSED (zero regressão em Phases 31/33/68/69/70)
3. Mapeamento SC→testes cobre 100% dos 5 SC do ROADMAP Phase 71
4. Snapshot congelado em nps_response_answers testado explicitamente
5. Dual-path (v15 novo + Phase 33 legado) exercitado em Feature tests
</verification>

<must_haves_summary>
- 5 REQs NPS-D-01 a NPS-D-05 fechados 100% (cobertura por Feature tests)
- 5 SC do ROADMAP Phase 71 → 100% mapeados
- Regressão preservada — Phases 31/33/68/69/70 continuam verdes
- PHASE-SUMMARY consolida entregas dos 3 plans
</must_haves_summary>
