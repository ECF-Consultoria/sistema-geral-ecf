---
phase: 71-formul-rio-p-blico-din-mico
milestone: v15.0
plan: 71-02
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - resources/js/Components/Nps/Config/PreviewFormulario.jsx
  - resources/js/Pages/Nps/Respond.jsx
  - resources/js/Pages/Nps/RespondLegado.jsx
autonomous: true
requirements: [NPS-D-01, NPS-D-02, NPS-D-03, NPS-D-04, NPS-D-05]
tags: [nps, frontend, react, jsx, inertia, respond, publico, template-dinamico, portabilidade, mobile-first, phase71]

must_haves:
  truths:
    - "PreviewFormulario aceita props opcionais 'value' + 'onChange' — quando ambos presentes usa modo controlled; quando ausentes preserva state interno (retrocompatível com Phase 70-05 Configuracao.jsx)"
    - "PreviewFormulario aceita 'errors' opcional (map {question_id: 'msg'}) para renderizar mensagem de erro por pergunta em mode=live"
    - "Nps/Respond.jsx recebe prop 'template' (do Plan 71-01) e roteia: se template presente → renderiza PreviewFormulario controlado + campos nome/comentário + submit; se template null → renderiza RespondLegado.jsx (fluxo Phase 33 preservado idêntico)"
    - "Nps/RespondLegado.jsx é o Respond.jsx atual (Phase 33) copiado integralmente antes da reescrita — zero mudança comportamental"
    - "submit v15 monta payload {respondent_name, comment, answers: {[question_id]: option_id}} — bate exatamente com o contrato do NpsController::submitResponseV15 (Phase 69-03)"
    - "Botão submit disabled até que todas as perguntas obrigatórias tenham resposta (client-side guard); server-side 422 se cliente contornar (já implementado no Phase 69-03)"
    - "Perguntas obrigatórias marcadas com asterisco vermelho + texto 'Obrigatória' via title (herdado do PreviewFormulario)"
    - "Layout mobile-first max-w-md (radio buttons cinza→amarelo já implementado no PreviewFormulario)"
    - "Zero jargão técnico visível ao cliente: nunca renderizar 'dimensao', 'tipo', 'peso', 'snapshot' — só texto da pergunta + labels das opções"
    - "npm run build verde sem regressão no bundle da Configuracao (Phase 70-05 continua funcionando)"
  artifacts:
    - path: resources/js/Components/Nps/Config/PreviewFormulario.jsx
      provides: "Componente refatorado para aceitar 'value/onChange/errors' opcionais + option identifier normalizado (id se presente, peso como fallback)"
      min_lines: 180
    - path: resources/js/Pages/Nps/Respond.jsx
      provides: "Reescrita: roteia por presença de template; v15 usa PreviewFormulario controlado + form fields nome/comentário + useForm submit"
      min_lines: 200
    - path: resources/js/Pages/Nps/RespondLegado.jsx
      provides: "Cópia integral do Respond.jsx atual antes da reescrita — preserva Phase 33 sem tocar 1 linha"
      min_lines_min: 300
  key_links:
    - from: resources/js/Pages/Nps/Respond.jsx
      to: resources/js/Components/Nps/Config/PreviewFormulario.jsx
      via: "import PreviewFormulario from '@/Components/Nps/Config/PreviewFormulario' + <PreviewFormulario template mode='live' value={answers} onChange={setAnswer} errors={errors} />"
      pattern: "PreviewFormulario.*mode=.live"
    - from: resources/js/Pages/Nps/Respond.jsx
      to: app/Http/Controllers/NpsController.php
      via: "useForm().post(route('nps.submit', token)) com payload {respondent_name, comment, answers: {qid: optid}}"
      pattern: "route..'nps.submit'"
---

<objective>
Entregar o **formulário público dinâmico** — reescrita de `Nps/Respond.jsx` que renderiza perguntas do template snapshot via `PreviewFormulario.jsx` (Phase 70-05, componente PURO) controlado + form fields nome/comentário + submit inertia. Cobre 100% dos REQs NPS-D-01 a NPS-D-05. Preserva Phase 33 integral via `RespondLegado.jsx` (roteamento por presença do prop `template`).

Purpose: NPS-D-01 exige "renderiza dinamicamente do template snapshot — nunca hardcoded". Sem este plan, mesmo com o backend Phase 71-01 injetando `template` no prop, `Nps/Respond.jsx` continuaria renderizando as 3 perguntas fixas de Phase 33. Este plan é a entrega visível do formulário público v15.0.

Design system already established (Phase 70-05):
- Tokens `ecf-*` (bg-ecf-bg, bg-ecf-card, bg-ecf-yellow, border-white/[0.08])
- Radio group cinza (`bg-white/[0.03]`) → amarelo (`bg-ecf-yellow text-[#050507]`) no selecionado
- Asterisco vermelho para obrigatórias
- Grid heurístico gridByCount (1-5 opções compacto; >5 stack)
- Layout mobile-first max-w-md (mesma largura útil do form real)

Output:
- Small refactor em `PreviewFormulario.jsx` (retrocompatibilidade)
- Cópia `Respond.jsx` → `RespondLegado.jsx` (backup do Phase 33)
- Reescrita completa de `Respond.jsx` (roteamento + form controlled + submit)
- Zero library nova
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/70-ui-de-configuracao-admin/PHASE-SUMMARY.md
@.planning/phases/70-ui-de-configuracao-admin/70-05-SUMMARY.md
@.planning/phases/71-formul-rio-p-blico-din-mico/71-01-PLAN.md
@app/Http/Controllers/NpsController.php
@resources/js/Pages/Nps/Respond.jsx
@resources/js/Pages/Nps/ThankYou.jsx
@resources/js/Pages/Nps/AlreadyCompleted.jsx
@resources/js/Pages/Nps/Expired.jsx
@resources/js/Components/Nps/Config/PreviewFormulario.jsx
@resources/js/Components/ui/button.jsx
@resources/js/Components/ui/input.jsx
@resources/js/Components/ui/textarea.jsx
@resources/js/lib/utils.js
@tailwind.config.js

<interfaces>
From resources/js/Components/Nps/Config/PreviewFormulario.jsx (Phase 70-05):
- Props atuais: `{ template, mode = 'preview' }`
- State interno: `respostas` (map perguntaOrdem → opcaoPeso) — usado só para feedback visual
- **Refactor necessário:** aceitar props opcionais `value` (map perguntaID → opcaoID) + `onChange` (setter) + `errors` (map perguntaID → mensagem). Quando `value/onChange` presentes: usa modo controlled; quando ausentes: fallback ao estado interno atual (Phase 70 continua funcionando)

From app/Http/Controllers/NpsController.php (Phase 71-01 T1):
- Prop `template` injetado em Nps/Respond quando survey.template_id existe
- Shape: `{id, nome, descricao, perguntas: [{id, ordem, texto, tipo, dimensao, obrigatoria, options: [{id, ordem, label, peso}]}]}`

From app/Http/Controllers/NpsController.php linhas 420-450 (Phase 69-03 submitResponseV15):
- Espera payload `{respondent_name, comment, answers: {[question_id]: option_id}}` — chave int, valor int
- Rules dinâmicas: `answers.{qid}` obrigatoriedade + `Rule::in(option_ids)` — Respond.jsx precisa mandar `option_id` (não peso)
- Route submit: `POST /nps/{token}` → `nps.submit`

From resources/js/Pages/Nps/Respond.jsx atual (Phase 33):
- 352 LOC — form hardcoded 3 perguntas escala 1-5 (estrategista/analista/empresa) + perguntas_extras + comentário + nome
- Consome props `survey.textos.*` + `perguntas_extras`
- useForm com chaves fixas `score_estrategista/score_analista/score_empresa/respostas_customizadas/comment/respondent_name`

From @inertiajs/react:
- `useForm(initial)` — form state + submit helpers
- Para submit v15 novo: `const { data, setData, post, processing, errors } = useForm({ respondent_name: '', comment: '', answers: {} })`
- `post(route('nps.submit', token), { onSuccess: ..., preserveScroll: true })`
</interfaces>

<research_reference>
ROADMAP Phase 71 SCs:
- #1 renderiza dinamicamente → PreviewFormulario controlled ✓
- #2 radio group cinza→amarelo + mobile-friendly → PreviewFormulario já implementa ✓
- #3 obrigatórias marcadas + submit disabled até tudo preenchido + server-side 422 → precisa validar client-side no Respond.jsx antes de habilitar botão
- #4 ThankYou/AlreadyCompleted/Expired preservados → **não tocar esses arquivos** (Phase 33 já entregou; ainda funcionam via `return Inertia::render('Nps/ThankYou')` no submitResponseV15)
- #5 zero jargão técnico → só renderizar `texto` da pergunta e `label` das opções; nunca renderizar `dimensao/tipo/peso/snapshot`

Design tokens travados (Phase 70 tailwind.config.js):
- Radio ativo: `bg-ecf-yellow text-[#050507] border-ecf-yellow`
- Radio inativo: `bg-white/[0.03] text-white/70 border-white/[0.08] hover:bg-white/[0.08]`
- Asterisco obrigatória: `text-red-400 ml-1`
- Erro por pergunta: `text-red-400 text-xs mt-1`

Memória do usuário (feedback_evitar_jargao_ui):
- Regra sistêmica: se termo não é auto-explicativo, simplificar/explicar/remover
- Aplicar rigorosamente aqui — cliente respondendo NPS não sabe o que é "dimensão"
</research_reference>
</context>

<tasks>
<task id="71-02-T1">
<title>Refatorar PreviewFormulario para aceitar controlled props (retrocompatível)</title>

<read_first>
- resources/js/Components/Nps/Config/PreviewFormulario.jsx (integral — 160 LOC)
- .planning/phases/70-ui-de-configuracao-admin/70-05-SUMMARY.md (contrato original)
</read_first>

<action>
Editar `resources/js/Components/Nps/Config/PreviewFormulario.jsx`:

**Mudança 1 — assinatura da função:**
```js
export default function PreviewFormulario({
    template,
    mode = 'preview',
    value,       // opcional — map { [pergunta_id]: option_id } quando controlled
    onChange,    // opcional — (perguntaId, optionId) => void quando controlled
    errors,      // opcional — map { [pergunta_id]: string } para erros server-side
}) {
```

**Mudança 2 — detectar modo controlled:**
```js
const isControlled = typeof value === 'object' && value !== null && typeof onChange === 'function';
const [internalRespostas, setInternalRespostas] = useState({});
const respostas = isControlled ? value : internalRespostas;
```

**Mudança 3 — normalizar identificador da opção (id se presente, senão peso):**
```js
const optionKey = (o) => o.id ?? o.peso;
const perguntaKey = (q) => q.id ?? q.ordem;
```

**Mudança 4 — handler `selecionar`:**
```js
const selecionar = (pergunta, opcao) => {
    const pk = perguntaKey(pergunta);
    const ok = optionKey(opcao);
    if (isControlled) {
        onChange(pk, ok);
    } else {
        setInternalRespostas((prev) => ({ ...prev, [pk]: ok }));
    }
};
```

**Mudança 5 — comparar seleção:**
```js
const selecionado = respostas[perguntaKey(q)];
// ...
const isActive = selecionado === optionKey(o);
```

**Mudança 6 — renderizar erro por pergunta (quando errors[qid] presente):**
Adicionar após o div de options, antes de fechar o `<div key={...}>`:
```jsx
{errors?.[perguntaKey(q)] && (
    <p className="text-red-400 text-xs mt-1">{errors[perguntaKey(q)]}</p>
)}
```

**Mudança 7 — footer em mode='live' NÃO renderiza botão (Phase 71 Respond envolve o componente com seu próprio botão):**
```jsx
<footer className="pt-4 border-t border-white/[0.06]">
    {mode === 'preview' && (
        <p className="text-white/40 text-[11.5px] text-center italic">
            Preview — as respostas não são salvas.
        </p>
    )}
    {/* mode='live' não renderiza nada aqui — Respond.jsx renderiza os campos nome/comentário/submit fora do componente */}
</footer>
```

**Retrocompatibilidade obrigatória:** o Configuracao.jsx (Phase 70-05) chama `<PreviewFormulario template={previewData} mode="preview" />` sem value/onChange/errors — precisa continuar funcionando idêntico (state interno + rodapé "as respostas não são salvas"). Testar visualmente após a mudança acessando `/nps/configuracao` como admin.
</action>

<acceptance_criteria>
- Assinatura da função aceita 5 props — assertar via `grep -A 6 "export default function PreviewFormulario" resources/js/Components/Nps/Config/PreviewFormulario.jsx | grep -c "value\\|onChange\\|errors"` >= 3
- Flag `isControlled` presente — assertar via `grep -n "isControlled" resources/js/Components/Nps/Config/PreviewFormulario.jsx`
- Helpers `optionKey` e `perguntaKey` presentes — assertar via `grep -c "optionKey\\|perguntaKey" resources/js/Components/Nps/Config/PreviewFormulario.jsx` >= 4
- Renderização condicional de erro presente — assertar via `grep -n "errors\\?\\.\\[perguntaKey" resources/js/Components/Nps/Config/PreviewFormulario.jsx`
- Footer em mode='live' NÃO renderiza botão desabilitado — assertar via `grep -A 5 "mode === 'preview'" resources/js/Components/Nps/Config/PreviewFormulario.jsx | grep -c "disabled\\|Enviar respostas"` == 0 OR pattern do footer refatorado presente
- Retrocompat: Phase 70 configuracao continua funcionando (npm run build + navegar `/nps/configuracao`)
</acceptance_criteria>
</task>

<task id="71-02-T2">
<title>Duplicar Respond.jsx como RespondLegado.jsx (backup preservação Phase 33)</title>

<read_first>
- resources/js/Pages/Nps/Respond.jsx (integral — 352 LOC, Phase 33)
</read_first>

<action>
Copiar integralmente `resources/js/Pages/Nps/Respond.jsx` para `resources/js/Pages/Nps/RespondLegado.jsx` **antes de reescrever** o Respond.jsx.

Comando (via ferramenta de write): ler todo o arquivo `Respond.jsx` e escrever como `RespondLegado.jsx` no mesmo diretório.

Ajustes mínimos no RespondLegado.jsx:
1. Trocar a linha `export default function Respond(...)` para `export default function RespondLegado(...)` (nome da função bate com o nome do arquivo)
2. Preservar TODO o resto — comentários, imports, useForm, submit, JSX — sem 1 mudança comportamental

Esse arquivo NÃO é renderizado diretamente pelo controller — Respond.jsx (T3) importa e renderiza condicionalmente quando `template === null`.
</action>

<acceptance_criteria>
- File `resources/js/Pages/Nps/RespondLegado.jsx` exists com pelo menos 300 LOC — assertar via `wc -l resources/js/Pages/Nps/RespondLegado.jsx` >= 300
- Função renomeada — assertar via `grep -n "export default function RespondLegado" resources/js/Pages/Nps/RespondLegado.jsx`
- Zero import da Phase 70 (mantém pattern original Phase 33) — assertar via `! grep -q "PreviewFormulario\\|Components/Nps/Config" resources/js/Pages/Nps/RespondLegado.jsx`
</acceptance_criteria>
</task>

<task id="71-02-T3">
<title>Reescrita completa de Nps/Respond.jsx — roteamento por template + PreviewFormulario controlado</title>

<read_first>
- resources/js/Pages/Nps/Respond.jsx (versão que vai ser substituída — para referência de padrão useForm e submit)
- resources/js/Pages/Nps/RespondLegado.jsx (que acabou de ser criado — para import)
- resources/js/Components/Nps/Config/PreviewFormulario.jsx (após refactor T1 — importa)
- resources/js/Layouts/AppLayout.jsx (Respond usa layout minimalista, sem AppLayout — cliente NÃO é usuário do sistema)
- app/Http/Controllers/NpsController.php linhas 420-450 (contrato submitResponseV15)
</read_first>

<action>
**Substituir integralmente** `resources/js/Pages/Nps/Respond.jsx`.

Nova estrutura:

```jsx
import { useState, useMemo } from 'react';
import { useForm, Head } from '@inertiajs/react';
import { Button } from '@/Components/ui/button';
import { Input } from '@/Components/ui/input';
import { Label } from '@/Components/ui/label';
import { Textarea } from '@/Components/ui/textarea';
import PreviewFormulario from '@/Components/Nps/Config/PreviewFormulario';
import RespondLegado from './RespondLegado';

/**
 * Phase 71 Plan 02 — Formulário público dinâmico v15.0.
 *
 * Roteamento: se prop `template` presente (survey.template_id !== null),
 * renderiza fluxo dinâmico via PreviewFormulario. Se `template` null,
 * delega ao RespondLegado (Phase 33 preservado integralmente).
 *
 * Contrato submit v15 (Phase 69-03):
 *   POST /nps/{token}
 *   { respondent_name, comment, answers: { [question_id]: option_id } }
 */
export default function Respond({ survey, perguntas_extras, template }) {
    // Rota legacy: template null → delega. Preserva 100% Phase 33.
    if (!template) {
        return <RespondLegado survey={survey} perguntas_extras={perguntas_extras} />;
    }

    // ─── Fluxo v15.0 dinâmico ───────────────────────────────────────────
    const { data, setData, post, processing, errors } = useForm({
        respondent_name: '',
        comment: '',
        answers: {},
    });

    // Handler passado ao PreviewFormulario controlado
    const setAnswer = (questionId, optionId) => {
        setData('answers', { ...data.answers, [questionId]: optionId });
    };

    // Client-side guard: submit disabled até que todas perguntas obrigatórias tenham resposta
    const podeEnviar = useMemo(() => {
        const obrigatorias = template.perguntas.filter(q => q.obrigatoria);
        return obrigatorias.every(q => data.answers[q.id] !== undefined && data.answers[q.id] !== null);
    }, [template, data.answers]);

    // Mapa de erros por pergunta — Laravel devolve como answers.<qid>
    const errorsByQuestion = useMemo(() => {
        const map = {};
        Object.entries(errors || {}).forEach(([key, msg]) => {
            const match = key.match(/^answers\.(\d+)$/);
            if (match) map[Number(match[1])] = msg;
        });
        return map;
    }, [errors]);

    const submit = (e) => {
        e.preventDefault();
        post(route('nps.submit', survey.token), { preserveScroll: true });
    };

    return (
        <>
            <Head title="Pesquisa de satisfação — ECF" />
            <div className="min-h-screen bg-ecf-bg text-white py-8 px-4">
                <div className="max-w-md mx-auto">
                    {/* Header do cliente — nome da empresa */}
                    <header className="mb-6 text-center">
                        <p className="text-white/60 text-sm">{survey.company_name}</p>
                    </header>

                    <form onSubmit={submit} className="space-y-5">
                        {/* PreviewFormulario controlado com value/onChange/errors */}
                        <PreviewFormulario
                            template={template}
                            mode="live"
                            value={data.answers}
                            onChange={setAnswer}
                            errors={errorsByQuestion}
                        />

                        {/* Campo comentário livre */}
                        <div className="max-w-md mx-auto bg-ecf-card border border-white/[0.08] rounded-2xl p-6 space-y-3">
                            <Label htmlFor="comment" className="text-white text-sm font-medium">
                                Comentário (opcional)
                            </Label>
                            <Textarea
                                id="comment"
                                value={data.comment}
                                onChange={(e) => setData('comment', e.target.value)}
                                placeholder="Escreva aqui, se quiser deixar uma observação."
                                rows={4}
                                maxLength={2000}
                                className="bg-white/[0.03] border-white/[0.08]"
                            />
                            {errors.comment && (
                                <p className="text-red-400 text-xs">{errors.comment}</p>
                            )}
                        </div>

                        {/* Campo nome opcional */}
                        <div className="max-w-md mx-auto bg-ecf-card border border-white/[0.08] rounded-2xl p-6 space-y-3">
                            <Label htmlFor="respondent_name" className="text-white text-sm font-medium">
                                Seu nome (opcional)
                            </Label>
                            <Input
                                id="respondent_name"
                                type="text"
                                value={data.respondent_name}
                                onChange={(e) => setData('respondent_name', e.target.value)}
                                placeholder="Como você prefere ser chamado(a)."
                                maxLength={255}
                                className="bg-white/[0.03] border-white/[0.08]"
                            />
                            {errors.respondent_name && (
                                <p className="text-red-400 text-xs">{errors.respondent_name}</p>
                            )}
                        </div>

                        {/* Submit — disabled até obrigatórias preenchidas */}
                        <div className="max-w-md mx-auto">
                            <Button
                                type="submit"
                                disabled={!podeEnviar || processing}
                                className="w-full bg-ecf-yellow text-[#050507] font-semibold hover:bg-ecf-yellow-hover disabled:opacity-50 disabled:cursor-not-allowed"
                            >
                                {processing ? 'Enviando...' : 'Enviar respostas'}
                            </Button>
                            {!podeEnviar && (
                                <p className="text-white/50 text-xs text-center mt-2">
                                    Responda todas as perguntas marcadas com <span className="text-red-400">*</span> para enviar.
                                </p>
                            )}
                        </div>
                    </form>

                    {/* Rodapé — apenas branding ECF, sem jargão técnico */}
                    <footer className="mt-8 text-center text-white/30 text-xs">
                        ECF Consultoria — pesquisa de satisfação
                    </footer>
                </div>
            </div>
        </>
    );
}
```

**Contract absoluto:**
- Zero jargão técnico visível (dimensao/tipo/peso/snapshot não aparecem em label nem placeholder)
- Botão submit disabled até obrigatórias preenchidas (client-side + server-side redundância)
- Erros server-side renderizados por pergunta via `errorsByQuestion` (regex match `answers.<qid>`)
- Layout mobile-first (max-w-md consistente com PreviewFormulario)
- Não usa AppLayout — cliente não é usuário logado; página standalone
</action>

<acceptance_criteria>
- File Respond.jsx reescrito com nova estrutura — assertar via `wc -l resources/js/Pages/Nps/Respond.jsx` >= 100 (era 352; nova versão ~150 LOC delegando ao PreviewFormulario)
- Import PreviewFormulario + RespondLegado — assertar via `grep -c "PreviewFormulario\\|RespondLegado" resources/js/Pages/Nps/Respond.jsx` >= 2
- Guard `!template` → delega ao RespondLegado — assertar via `grep -n "!template" resources/js/Pages/Nps/Respond.jsx`
- useForm com 3 campos (respondent_name, comment, answers) — assertar via `grep -A 5 "useForm" resources/js/Pages/Nps/Respond.jsx | grep -c "respondent_name\\|comment\\|answers"` >= 3
- `route('nps.submit', ...)` — assertar via `grep -n "route..'nps.submit'" resources/js/Pages/Nps/Respond.jsx`
- Botão submit tem `disabled={!podeEnviar || processing}` — assertar via `grep -n "!podeEnviar" resources/js/Pages/Nps/Respond.jsx`
- errorsByQuestion regex match `answers.<qid>` presente — assertar via `grep -n "answers\\\\\\\\\\.\\\\\\\\d\\|match.*answers" resources/js/Pages/Nps/Respond.jsx`
- Zero jargão técnico — assertar via `! grep -qiE "dimensao|snapshot|peso.*interno|option_id.*visivel" resources/js/Pages/Nps/Respond.jsx` (visível ao cliente — código pode usar, JSX texto não)
</acceptance_criteria>
</task>

<task id="71-02-T4">
<title>Build + smoke test manual dos 2 paths</title>

<read_first>
- resources/js/Pages/Nps/Respond.jsx (T3 concluído)
- resources/js/Components/Nps/Config/PreviewFormulario.jsx (T1 concluído)
</read_first>

<action>
1. `npm run build` — obrigatório por convenção do projeto
2. Verificar `public/build/manifest.json` inclui:
   - `resources/js/Pages/Nps/Respond.jsx` (asset novo)
   - `resources/js/Pages/Nps/RespondLegado.jsx` (asset novo)
3. Rodar `php artisan serve` OU usar servidor local; testar dois cenários:
   - **v15 path:** criar survey com template_id via tinker; acessar `/nps/{token}` — deve mostrar título do template + perguntas dinâmicas + campos nome/comentário/submit; submit deve funcionar e redirecionar para `/nps/{token}/thank-you` OU renderizar Nps/ThankYou
   - **Legacy path:** achar survey pre-v15.0 sem template_id (ou tinker forçar template_id=null); acessar `/nps/{token}` — deve mostrar form Phase 33 idêntico ao original
4. Verificar UX:
   - Perguntas obrigatórias mostram asterisco vermelho
   - Botão fica disabled com hint até preencher obrigatórias
   - Radio ativo em amarelo, inativo cinza (bg-white/[0.03])
   - Layout comprime bem em ≤400px width (Chrome DevTools mobile)

5. Verificar Phase 70 Configuracao ainda funciona (retrocompat PreviewFormulario):
   - Acessar `/nps/configuracao` como admin
   - Selecionar template → preview live deve renderizar sem erros
   - Console do navegador sem warnings de props faltando
</action>

<acceptance_criteria>
- `npm run build` exit 0
- Asset Respond-*.js presente em manifest.json
- Cenário v15: form dinâmico renderiza, botão disabled até preencher obrigatórias, submit vai pra ThankYou
- Cenário legacy: form Phase 33 idêntico ao original
- Phase 70 Configuracao continua funcionando (preview live sem regressão)
</acceptance_criteria>
</task>
</tasks>

<verification>
1. PreviewFormulario aceita controlled props sem quebrar Phase 70
2. Respond.jsx roteia por template presence
3. RespondLegado.jsx preserva Phase 33
4. Botão submit disabled até obrigatórias preenchidas
5. Zero jargão técnico visível ao cliente
6. npm run build verde
</verification>

<must_haves_summary>
- REQs NPS-D-01 a NPS-D-05 fechados no frontend
- PreviewFormulario segue puro (só ganha controlled interface opcional)
- Backward compat Phase 70 + Phase 33 preservados via delegação
- Guard de obrigatórias: client-side + server-side redundante
</must_haves_summary>
