---
phase: 122-persist-ncia-por-empresa-e-comandos-v21-0
plan: 06
type: execute
wave: 4
depends_on: [122-05]
files_modified:
  - .planning/phases/122-persist-ncia-por-empresa-e-comandos-v21-0/122-ROLLOUT.md
autonomous: false
requirements: [SNAP-06]

must_haves:
  truths:
    - "As competências fechadas foram reconsolidadas em produção e passaram a ter detalhe por empresa"
    - "A conferência de cada competência foi feita por reconsulta ao banco, com veredito por exit code"
    - "A cobertura de margem em pontos percentuais da carteira real está medida e registrada, antes de qualquer decisão sobre ligar a flag"
    - "A flag de nota por empresa continua desligada depois do rollout"
  artifacts:
    - path: ".planning/phases/122-persist-ncia-por-empresa-e-comandos-v21-0/122-ROLLOUT.md"
      provides: "Seção de evidência com o resultado real de cada competência e a cobertura pp medida"
      contains: "Evidência"
  key_links:
    - from: "produção"
      to: "desempenho:verificar-consolidacao"
      via: "exit code por competência registrado no runbook"
      pattern: "verificar-consolidacao"
---

<objective>
Executar o rollout da Fase 122 em produção seguindo o `122-ROLLOUT.md` e registrar a evidência reconsultada.

Purpose: sem a reconsolidação das competências fechadas, ranking, dashboard e Relatório de Bonificação continuam sem o detalhe por empresa, e o critério 5 do ROADMAP não fecha. A execução é humana porque toca produção e porque o veredito do gate FIXMARG-03 pode exigir decisão operacional (re-rodar mais tarde).
Output: `122-ROLLOUT.md` com a seção de evidência preenchida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-persist-ncia-por-empresa-e-comandos-v21-0/122-ROLLOUT.md
@.planning/phases/122-persist-ncia-por-empresa-e-comandos-v21-0/122-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rollout em produção — deploy, reconsolidação e conferência</name>
  <files>(nenhum arquivo do repositório é alterado nesta task — é execução em produção)</files>
  <action>
Apresentar ao usuário o roteiro de `<how-to-verify>` abaixo (seções 1-6 do `122-ROLLOUT.md`) e PAUSAR. O executor não roda deploy nem comando de produção por conta própria: apresenta, aguarda a execução/aprovação do usuário, e coleta de volta os 3 exit codes, os nomes de `SEM_SNAPSHOT` e o resultado da query de cobertura pp — que são o insumo da Task 2.
  </action>
  <verify>
    <automated>MISSING — verificação humana em produção; o veredito automatizável é o exit code de `desempenho:verificar-consolidacao --mes=` por competência, coletado pelo usuário</automated>
  </verify>
  <done>
Usuário reportou os 3 exit codes, os nomes de `SEM_SNAPSHOT` (ou a ausência deles) e o resultado da query de cobertura pp, e confirmou que a flag continua `false`.
  </done>
  <what-built>
Fase 122 completa e verde localmente: tabela `desempenho_company_score_snapshots`, escritor idempotente com trava de congelamento, os três comandos gravando as linhas por empresa, invalidação limpando a competência, `margem_amostra` em pontos percentuais com o legado preservado, gate FIXMARG-03 lendo a base que alimenta a nota de hoje, e o comando `desempenho:verificar-consolidacao`.

Nada disto muda nota, faixa ou pagamento: a flag `metrics.performance_company_first_score` continua `false` e o gate segue medindo a mesma amostra de sempre.
  </what-built>
  <how-to-verify>
Siga o `122-ROLLOUT.md`, nesta ordem:

1. **Árvore limpa** — commitar/aguardar; a árvore é compartilhada com outra sessão e outro dev. Nunca `git add -A`.
2. **Deploy** — deploy normal + `php artisan migrate --force` no VPS.
   **NÃO rodar `php artisan cache:clear`** (derrubou o site em 2026-07-30). O bump `desempenho.compute.v15 → v16` já invalida o cache antigo sozinho.
3. **Reconsolidar as competências fechadas**, uma por vez:
   - `php artisan desempenho:consolidar-mes --mes=2026-06`
   - `php artisan desempenho:consolidar-mes --mes=2026-05`
   - `php artisan desempenho:consolidar-mes --mes=2026-04`
   Ignore a linha final impressa — ela não é o critério.
4. **Conferir cada uma por reconsulta:**
   - `php artisan desempenho:verificar-consolidacao --mes=2026-06 --json`
   - idem para `2026-05` e `2026-04`
   Guarde a saída JSON e o **exit code** de cada uma (`echo $?`). Exit 0 = competência consistente.
5. **Medir a cobertura em pontos percentuais** com a query da seção 6 do runbook (lê as tabelas do comparador da Fase 121 — não toca a Adman).
6. **Reafirmar a flag:** `php artisan tinker --execute="var_dump(config('metrics.performance_company_first_score'));"` deve imprimir `false`.

**O que reportar de volta:** os 3 exit codes, quantos profissionais apareceram em `SEM_SNAPSHOT` (com nome), e o resultado da query de cobertura pp.

Se alguma competência der exit != 0 com `SEM_SNAPSHOT`, é o gate de margem recusando por amostra degradada (rate-limit da Adman) — a ação é re-rodar aquela competência mais tarde, não forçar. Descreva o caso e siga a seção 5 do runbook.
  </how-to-verify>
  <resume-signal>Responda com os 3 exit codes, os nomes de `SEM_SNAPSHOT` (se houver) e o resultado da query de cobertura pp — ou descreva o problema encontrado.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar a evidência no runbook</name>
  <files>.planning/phases/122-persist-ncia-por-empresa-e-comandos-v21-0/122-ROLLOUT.md</files>
  <action>
Acrescentar ao `122-ROLLOUT.md` uma seção `## Evidência da execução` em pt-BR, com data e hora BRT, contendo:
- Uma tabela por competência: `mes | exit_code | profissionais_com_snapshot | linhas_por_empresa | SEM_SNAPSHOT (nomes)`.
- O resultado da query de cobertura pp por profissional, e a leitura em uma frase: quantos ficariam abaixo de 0,7 se o gate FIXMARG-03 passasse a medir pp (o cenário de quando a flag ligar).
- Confirmação explícita de que `config('metrics.performance_company_first_score')` continua `false` após o rollout.
- Se houve `SEM_SNAPSHOT`: quais competências ficaram pendentes de re-execução e o que foi combinado.

Registrar os números EXATAMENTE como vieram da reconsulta, sem arredondar nem interpretar. Se algum passo não foi executado, escrever "não executado" — nunca inferir resultado.
  </action>
  <verify>
    <automated>grep -c "Evidência da execução" .planning/phases/122-persist-ncia-por-empresa-e-comandos-v21-0/122-ROLLOUT.md</automated>
  </verify>
  <done>
A seção existe com os 3 exit codes, a medição de cobertura pp e a confirmação do estado da flag.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador → produção (VPS) | Comandos que regravam a base do bônus de competências já pagas ou em vias de pagar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-23 | Denial of Service | `cache:clear` no VPS | mitigate | Proibição explícita no checkpoint e no runbook; bump de chave de cache torna o clear desnecessário |
| T-122-24 | Tampering | Reconsolidação alterando nota de competência fechada | accept | Reconsolidar já é caminho oficial e aconteceu duas vezes em 2026-06 (item 3 do CONTEXT); a flag desligada mantém a mesma fórmula, e a evidência reconsultada fica registrada |
| T-122-25 | Repudiation | Rollout dado como concluído sem prova | mitigate | Exit codes e nomes registrados no runbook; nenhum critério aceita stdout do `consolidar-mes` |
| T-122-26 | Elevation of Privilege | Flag ligada de carona no rollout | mitigate | Passo 6 do checkpoint reafirma `false` por consulta ao config |
| T-122-SC | Tampering | Instalação de pacotes | n/a | Nenhum pacote instalado neste plano |
</threat_model>

<verification>
1. Os 3 exit codes registrados no runbook
2. `config('metrics.performance_company_first_score')` = `false` confirmado em produção após o rollout
3. Nenhum `cache:clear` executado
</verification>

<success_criteria>
- Competências fechadas reconsolidadas em produção com detalhe por empresa (SNAP-06)
- Conferência feita por reconsulta, com veredito por exit code
- Cobertura de `margem_var_pp` da carteira real medida e registrada
- Flag continua desligada
</success_criteria>

<output>
Criar `.planning/phases/122-persist-ncia-por-empresa-e-comandos-v21-0/122-06-SUMMARY.md` ao terminar.
</output>
