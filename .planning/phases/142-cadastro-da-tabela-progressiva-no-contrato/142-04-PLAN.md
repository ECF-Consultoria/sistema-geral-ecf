---
phase: 142-cadastro-da-tabela-progressiva-no-contrato
plan: 04
type: execute
wave: 4
depends_on: ["142-03"]
files_modified:
  - resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx
  - resources/js/Pages/Admin/Financeiro.jsx
  - tests/Feature/Phase138/Phase138FaixasGrupoCrudTest.php
  - tests/Feature/Phase139/Phase139TabelaProgressivaFielTest.php
  - tests/Feature/Phase142/Phase142FechamentoSomenteLeituraTest.php
autonomous: false
requirements: [D-01, D-04]

must_haves:
  truths:
    - "O fechamento continua mostrando a tabela progressiva de cada empresa e a faixa em que ela está."
    - "A tabela própria de uma empresa agora aparece com as faixas, não mais como uma frase solta."
    - "Não há mais nenhum botão de cadastrar, editar ou apagar tabela dentro do fechamento."
    - "Quem quer cadastrar é levado para a página exclusiva no contrato, com um clique."
    - "O gate de testes continua sem falhas, com o filtro da fase nova incluído."
  artifacts:
    - path: "tests/Feature/Phase142/Phase142FechamentoSomenteLeituraTest.php"
      provides: "trava de que o fechamento mostra e não escreve"
  key_links:
    - from: "resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx"
      to: "rota admin.contratos.tabela.show"
      via: "link de cadastro"
      pattern: "admin.contratos.tabela.show"
    - from: "resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx"
      to: "prop tabela_faixas"
      via: "renderização da tabela própria"
      pattern: "tabela_faixas"
---

<objective>
Fechar os dois pedidos que sobraram, no mesmo arquivo:

**D-01 na tela de Fechamento** — a empresa com tabela própria passa a mostrar as faixas, usando a
prop `tabela_faixas` criada no plano 142-01. Acaba a frase solta "Tabela própria desta empresa".

**D-04** — nas palavras do usuário:
> "No fechamento deve continuar mostrando a tabela progressiva de cada empresa e a faixa que a
> empresa está, só não deve ser possível cadastrar ou editar as tabelas por ali."

⚠️ **Continuar mostrando é parte do pedido.** O que sai é a capacidade de escrever: os cinco
`FaixaFormDialog`, os `router.post`/`router.delete` e todos os botões de criar/substituir/apagar.
O que fica — e melhora — é a exibição: os quatro estados continuam existindo, cada um mostrando a
grade da tabela e a faixa em que a empresa caiu.

⚠️ **Este plano quebra assertions de duas suítes antigas de propósito**, porque elas travam
justamente o que D-04 manda remover. Estão nomeadas e resolvidas na Tarefa 2 — não são regressão,
são a mudança pedida.

Purpose: o fechamento vira o lugar de olhar a cobrança; o contrato vira o lugar de definir a
cobrança.
Output: `TabelaFaixasSection` só de leitura, testes retargetados e o gate fechado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-cadastro-da-tabela-progressiva-no-contrato/142-CONTEXT.md
@.planning/phases/142-cadastro-da-tabela-progressiva-no-contrato/142-01-PLAN.md
@.planning/phases/142-cadastro-da-tabela-progressiva-no-contrato/142-03-PLAN.md
@resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx
</context>

<interfaces>
Props por empresa/grupo, depois do plano 142-01:
- `tabela_faixas`: array de linhas quando `tabela_origem === 'propria'`; `null` nos demais
- `tabela_faixas_e_de_hoje`: `true` no mês fechado, `false` no mês corrente
- já existentes: `tabela_origem` (`'grupo'|'propria'|'servico'|null`), `tabela_servico_nome`,
  `tabela_grupo_nome`, `tabela_herdada_de_nome`, `procedencia_tabela`, `tabela_confirmada`,
  `faixa_ordem`, `valor_mensal`, `faixa_limite_superior`

`<TabelaProgressivaFaixas faixas={...} faixaOrdemAtual={...} notaRodape={...} />`
— componente compartilhado criado no plano 142-03 em `@/Components/Fechamento/TabelaProgressivaFaixas`.

Rota da ficha: `route('admin.contratos.tabela.show', empresa.id)`.

Assertions que este plano precisa alterar (medidas na leitura do código, não suposição):
- `tests/Feature/Phase138/Phase138FaixasGrupoCrudTest.php:194` — exige
  `admin.financeiro.faixas.grupo` dentro de `TabelaFaixasSection.jsx`
- `tests/Feature/Phase139/Phase139TabelaProgressivaFielTest.php:176` — mesma exigência
</interfaces>

<tasks>

<task type="auto">
  <name>Tarefa 1: TabelaFaixasSection só de leitura, mostrando a tabela própria</name>
  <files>resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx, resources/js/Pages/Admin/Financeiro.jsx</files>
  <action>
    **Remover de `TabelaFaixasSection.jsx`:** a subcomponente `FaixaFormDialog` inteira, o helper
    `linhaVaziaFaixa`, os estados `dialog`/`salvando`/`erro`, as funções `salvarEmpresa`,
    `salvarServico`, `salvarGrupo`, `voltarParaServico`, `voltarParaEmpresaDoGrupo`, `extrairErro`,
    `fecharDialog`, os imports de `router`, `Dialog*`, `Button`, `Input`, `Label`, `Plus`, `Trash2`,
    e todos os botões de ação dos quatro blocos. A prop `competenciaFechada` deixa de controlar
    botão nenhum — manter o aviso de cadeado só se ele ainda fizer sentido depois do corte; se não
    fizer, remover a prop e a passagem dela em `Financeiro.jsx`.

    **Manter e melhorar a exibição** (é metade do pedido):
    - bloco de GRUPO com tabela própria: selo "Tabela deste grupo" + grade — como hoje;
    - bloco de GRUPO sem tabela própria: as duas frases de herança — **intactas, palavra por
      palavra** ("Este grupo está usando a tabela da empresa …" e "Quem manda é a empresa do grupo
      que mais faturou no mês …"), que duas suítes travam;
    - estado `'servico'`: frase + grade vinda de `faixasPorServico` — como hoje;
    - **estado `'propria'`: passa a renderizar `<TabelaProgressivaFaixas faixas={empresa.tabela_faixas} …>`**
      no lugar da frase solta. Manter o selo "Tabela própria desta empresa" e a frase que nomeia o
      serviço substituído. Guarda defensiva: quando `tabela_faixas` vier nulo ou vazio (não deve
      acontecer depois do plano 01), cair de volta na frase, nunca renderizar grade vazia;
    - estado `null` ("A DEFINIR"): frase + link para a ficha.

    **`faixaOrdemAtual`** já é prop e continua destacando a faixa da empresa. ⚠️ No mês fechado
    (`tabela_faixas_e_de_hoje === true`) a tabela mostrada é a cadastrada HOJE, e a faixa destacada
    veio do mês congelado: só destacar quando a linha bater com o que foi congelado
    (`f.ordem === faixa_ordem` **e** `f.valor === valor_mensal` **e**
    `f.limite_superior === faixa_limite_superior`). Quando não bater, não destacar nada e passar
    `notaRodape="A tabela mudou depois deste mês. Esta é a que está cadastrada hoje."`. Quando bater,
    `notaRodape` fica nulo. Destacar uma linha errada num mês já cobrado é pior do que não destacar
    nenhuma.

    **Link único de saída**, presente nos quatro estados, com o rótulo variando:
    `<Link href={route('admin.contratos.tabela.show', empresa.id)}>` — "Cadastrar tabela de cobrança"
    quando não há tabela, "Ajustar tabela de cobrança" quando há. Texto de apoio: "O cadastro fica na
    página de contrato desta empresa." ⚠️ Sem jargão (as oito palavras da Fase 139 + "presumida").

    **`Financeiro.jsx`** — o botão "Cadastrar tabela de faixas" de `AusenciaTabelaPendencia`
    (variant="full") hoje é um `<a href="#tabela-faixas-{id}">` que pula para a seção do accordion.
    Trocar por `Link` para `route('admin.contratos.tabela.show', ...)`. Ajustar a origem do `href`
    passado ao componente. Não mexer em mais nada do arquivo — nem no `TabelaPresumidaAviso`, nem
    no chip de filtro, nem nos widgets.

    ⚠️ Escala do Tailwind: `px-4.5`, `gap-4.5`, `py-5.5` NÃO EXISTEM. Comentários em pt-BR.
    Atualizar o docblock do topo de `TabelaFaixasSection.jsx`: a limitação do 137-09 documentada ali
    **deixou de existir** — escrever que a Fase 142 expôs as linhas e moveu o cadastro para o
    contrato, para ninguém reintroduzir o formulário aqui por achar que a lacuna continua.

    `npm run build` ao final.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Nenhum `router.post`/`router.delete`/`FaixaFormDialog` sobra no arquivo; os quatro estados mostram a grade; build sem erro.</done>
</task>

<task type="auto">
  <name>Tarefa 2: retarget das travas antigas e a suíte da leitura</name>
  <files>tests/Feature/Phase138/Phase138FaixasGrupoCrudTest.php, tests/Feature/Phase139/Phase139TabelaProgressivaFielTest.php, tests/Feature/Phase142/Phase142FechamentoSomenteLeituraTest.php</files>
  <action>
    **Duas assertions antigas travam o que D-04 manda remover** — retargetar preservando a intenção,
    nunca apagando a trava:

    - `Phase138FaixasGrupoCrudTest::tabela_faixas_section_jsx_tem_a_frase_de_heranca_e_a_rota_nova`
      (linha ~194): trocar a exigência de `admin.financeiro.faixas.grupo` por
      `admin.contratos.tabela.show`, com mensagem em pt-BR dizendo que a Fase 142 (D-04) moveu o
      cadastro para o contrato e que o bloco de grupo agora **leva** para lá em vez de salvar ali.
      Renomear o método para `..._tem_a_frase_de_heranca_e_o_caminho_do_cadastro`. As três outras
      assertions do método (frase de herança, `tabela_herdada_de_nome`, banimento de "âncora")
      ficam intactas — a intenção original delas continua valendo inteira. O resto da suíte (CRUD
      HTTP em `admin.financeiro.faixas.grupo`) **não muda**: a rota continua viva, só sem UI.
    - `Phase139TabelaProgressivaFielTest::frase_de_heranca_da_tabela_do_grupo_continua_intacta`
      (linha ~176): mesma troca, mesma justificativa no docblock.

    **Suíte nova `Phase142FechamentoSomenteLeituraTest`** sobre
    `js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx`:
    (a) o arquivo **não contém** `router.post`, `router.delete`, `FaixaFormDialog`, `useForm`, nem
        nenhuma string `admin.financeiro.faixas.` — nenhuma escrita sai do fechamento;
    (b) o arquivo **contém** `admin.contratos.tabela.show` — existe caminho para quem quer cadastrar;
    (c) o arquivo **contém** `tabela_faixas` e usa `<TabelaProgressivaFaixas ` no bloco de tabela
        própria — a dívida do 137-09 está paga também aqui;
    (d) o arquivo **não contém** a frase "Substituir tabela própria" nem "Criar tabela própria" nem
        "Editar tabela do serviço" nem "Voltar a usar a tabela" — os rótulos de escrita sumiram;
    (e) as frases de herança do grupo continuam presentes (redundante com a Fase 138 de propósito:
        são as frases que mais vezes quase sumiram em refactor);
    (f) nenhuma das oito palavras de jargão como texto visível, reusando o helper da Fase 139;
    (g) a regex de escala quebrada do Tailwind não casa em `TabelaFaixasSection.jsx` nem em
        `Financeiro.jsx`.

    Docblock da suíte em pt-BR explicando D-04 com as palavras do usuário.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase142FechamentoSomenteLeituraTest|Phase138FaixasGrupoCrudTest|Phase139TabelaProgressivaFielTest"</automated>
  </verify>
  <done>As três suítes passam; nenhuma trava foi apagada, só reapontada com justificativa escrita.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 3: gate completo e conferência visual pelo usuário</name>
  <files>(nenhum arquivo alterado — tarefa de conferência)</files>
  <action>
    Rodar o gate ampliado, o build e o script Node de conferência do CSS compilado, registrar os
    números no SUMMARY e então apresentar ao usuário os sete pontos de conferência visual descritos
    em `<how-to-verify>`. Nada é editado nesta tarefa.
  </action>
  <what-built>
    Antes de chamar o usuário, rodar e registrar no SUMMARY:

    1. Gate ampliado:
       `C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase122|Phase136|Phase137|Phase138|Phase139|Phase140|Phase141|Phase142|Quick260909"`
       Referência antes desta fase: **553 testes / 2642 asserções / 0 falhas**. O número de testes
       sobe (as suítes da Fase 142); o número de falhas tem que continuar **0**.
    2. `npm run build` limpo.
    3. Script Node lendo `public/build/assets/*.css` e confirmando as classes com colchetes usadas
       nos arquivos novos e alterados. ⚠️ Node com `fs.readFileSync` + `includes`, **nunca `grep`
       do shell** — colchetes escapam mal e dão falso negativo.

    ⚠️ Falhas PRÉ-EXISTENTES, fora do gate, que **não são regressão desta fase** e não devem ser
    perseguidas: `Phase138AvisoMudancaFaixaTest::refazer_e_mudar_a_faixa...` (flaky),
    `Phase13ComercialTest` (10), `Phase14VerificarCobrancaTest`, `FechamentoMigrationTest`,
    `AdminFechamentoControllerTest` (4/16), `Phase14MigrationTest`,
    `Phase14MlbControllerFiltroTest`.

    ⚠️ O executor **não alcança produção** (`plink`/`pscp`/`deploy.sh` bloqueados em subagente) e o
    banco local está ~31 migrations atrás e vazio de dado real. Nada aqui é conferível contra as
    169 empresas reais — por isso este checkpoint existe. **Não deployar.**
  </what-built>
  <how-to-verify>
    1. Abrir `/administrativo/contratos/empresa/{id}` de uma empresa com tabela própria e conferir o
       bloco "Tabela de cobrança": ele diz quantas faixas e como a tabela chegou ali?
    2. Clicar no botão e conferir, na página exclusiva, se **as faixas que aparecem são as que estão
       gravadas** para aquela empresa (esta é a dívida da Fase 137 sendo paga — se abrir em branco
       ou com a tabela do serviço, o plano falhou).
    3. Digitar `1500000` no campo de mensalidade e conferir se aparece `1.500.000,00` enquanto se
       digita (D-02).
    4. Salvar uma alteração pequena numa empresa de teste e conferir se a mudança aparece no
       fechamento.
    5. Abrir `/administrativo/financeiro`, expandir uma empresa com tabela própria e conferir se
       **as faixas aparecem** (D-01) e se **não há mais nenhum botão de cadastrar/editar/apagar**
       (D-04), só o link para o contrato.
    6. Expandir um mês já fechado e conferir se a tabela aparece com a nota de rodapé quando ela
       mudou depois daquele mês.
    7. Conferir se algum texto da tela soa a jargão interno.
  </how-to-verify>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase122|Phase136|Phase137|Phase138|Phase139|Phase140|Phase141|Phase142|Quick260909"</automated>
    <human-check>Confirmação do usuário nos sete pontos acima.</human-check>
  </verify>
  <done>Gate sem falhas novas (referência: 553 testes antes da fase), build limpo, classes com colchetes confirmadas no CSS compilado, e aprovação do usuário registrada no SUMMARY.</done>
  <resume-signal>Responder "aprovado" ou descrever o que ficou errado.</resume-signal>
</task>

</tasks>

<threat_model>
## Fronteiras de confiança

| Fronteira | Descrição |
|-----------|-----------|
| tela de Fechamento → escrita | fronteira que este plano **fecha**: deixa de existir |
| mês congelado → exibição | tabela de hoje sendo mostrada ao lado de números de um mês já cobrado |

## Registro STRIDE

| ID | Categoria | Componente | Disposição | Mitigação |
|----|-----------|------------|------------|-----------|
| T-142-18 | Tampering | escrita a partir do fechamento | mitigate | superfície removida da UI; suíte (a) trava qualquer `router.post`/`admin.financeiro.faixas.` voltando ao arquivo |
| T-142-19 | Repudiation | rotas `admin.financeiro.faixas.*` continuam vivas sem UI | accept | seguem sob `role:admin`, com validação e (desde o plano 01) trilha de auditoria; são o caminho de rollback e o que as suítes das Fases 137/138 exercitam |
| T-142-20 | Information disclosure | tabela de hoje num mês fechado | mitigate | destaque só quando a linha bate com o congelado; nota de rodapé explícita quando não bate |
| T-142-21 | Denial of service | nenhum | accept | plano é só de UI e de teste; nenhuma consulta nova |
| T-142-SC | Tampering | instalação de pacotes | mitigate | nenhum pacote novo |
</threat_model>

<verification>
- Gate ampliado sem falhas novas, com o número de testes registrado no SUMMARY (antes: 553).
- Nenhuma escrita de tabela alcançável a partir de `/administrativo/financeiro`.
- Aprovação humana registrada.
</verification>

<success_criteria>
- O fechamento mostra tabela e faixa nos quatro estados, incluindo a tabela própria (D-01).
- O fechamento não cadastra nem edita (D-04).
- Gate verde com o filtro `Phase142` incluído.
</success_criteria>

<output>
Criar `.planning/phases/142-cadastro-da-tabela-progressiva-no-contrato/142-04-SUMMARY.md` ao terminar,
com os números do gate (antes × depois) e o resultado da conferência do CSS compilado.
Se algo aprendido nesta fase não for dedutível do código, acrescentar em
`.planning/learnings/fechamento-tabela-por-empresa.md` — o repositório atravessa as máquinas, a
memória de sessão não.
Commit em pt-BR, **apenas com os caminhos deste plano** (`git commit -- <arquivos>`) — árvore
compartilhada; nunca `git add -A`, `git add .`, `git commit -a` ou `git stash`.
⚠️ **Não deployar** sem autorização explícita do usuário nesta milestone.
</output>
