---
phase: 142-cadastro-da-tabela-progressiva-no-contrato
plan: 03
type: execute
wave: 3
depends_on: ["142-02"]
files_modified:
  - resources/js/lib/dinheiro.js
  - resources/js/Components/ui/campo-dinheiro.jsx
  - resources/js/Components/Fechamento/TabelaProgressivaFaixas.jsx
  - resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx
  - resources/js/Pages/Admin/TabelaEmpresa.jsx
  - tests/Feature/Phase139/Phase139TabelaProgressivaFielTest.php
  - tests/Feature/Phase142/Phase142FichaTabelaUiTest.php
autonomous: true
requirements: [D-01, D-02, D-03]

must_haves:
  truths:
    - "Abrir a tabela de uma empresa que já tem tabela mostra as faixas gravadas, e o formulário abre preenchido com elas."
    - "Digitar 1500000 num campo de valor mostra 1.500.000,00 enquanto se digita — o zero a mais fica visível."
    - "A tabela progressiva é renderizada por uma única definição de componente em todo o projeto."
    - "A ficha diz, sem jargão, se a tabela foi conferida pelo contrato, cadastrada à mão, ou copiada do serviço sem ninguém conferir."
    - "Quando existe leitura de contrato aguardando conferência daquela empresa, a ficha manda para a caixa de entrada da Fase 140."
  artifacts:
    - path: "resources/js/lib/dinheiro.js"
      provides: "opções de máscara e formatação de dinheiro, uma fonte só"
    - path: "resources/js/Components/ui/campo-dinheiro.jsx"
      provides: "input mascarado que devolve número, não string"
    - path: "resources/js/Components/Fechamento/TabelaProgressivaFaixas.jsx"
      provides: "a única definição da grade da tabela progressiva"
    - path: "resources/js/Pages/Admin/TabelaEmpresa.jsx"
      provides: "a página exclusiva do cadastro (D-03)"
      min_lines: 250
  key_links:
    - from: "resources/js/Pages/Admin/TabelaEmpresa.jsx"
      to: "rota admin.contratos.tabela.salvar"
      via: "router.post"
      pattern: "admin.contratos.tabela.salvar"
    - from: "resources/js/Pages/Admin/TabelaEmpresa.jsx"
      to: "CampoDinheiro"
      via: "campos de valor e de teto"
      pattern: "CampoDinheiro"
    - from: "resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx"
      to: "Components/Fechamento/TabelaProgressivaFaixas"
      via: "import"
      pattern: "Components/Fechamento/TabelaProgressivaFaixas"
---

<objective>
A página exclusiva pedida em D-03, com as faixas de verdade (D-01) e a máscara de dinheiro (D-02).

Três entregas encadeadas:

1. **Máscara de dinheiro** — `react-imask` (`^7.6.1`, já em `package.json`, já é o padrão de máscara
   do projeto desde a Fase 34). Nada de parser de string à mão.
2. **Uma definição só da grade** — `TabelaProgressivaFaixas` sai de dentro de
   `TabelaFaixasSection.jsx` e vira componente compartilhado, usado pela ficha nova e pelo
   fechamento. A Fase 139 já pagou o preço de ter três cópias divergentes dessa grade; criar a
   quarta dentro da página nova seria repetir o erro no mesmo mês.
3. **A ficha** — `Admin/TabelaEmpresa.jsx`, com o formulário abrindo **preenchido com o que está
   gravado**.

⚠️ **A dívida do 137-09 se paga expondo o dado, não adivinhando.** O aviso "os valores atuais não
são carregados aqui — preencha a tabela completa antes de salvar" existia porque o backend não
mandava as linhas. Agora manda (plano 142-01). Se o formulário abrir preenchido, é com
`tabela_empresa` vindo do banco — **nunca** com a tabela do serviço fazendo as vezes da tabela da
empresa. A tabela do serviço só aparece por escolha explícita ("começar a partir da tabela de X"),
com o resultado visível antes de salvar.

Purpose: transformar 169 tabelas ilegíveis em 169 tabelas conferíveis.
Output: dois utilitários de UI, um componente compartilhado e a página.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-cadastro-da-tabela-progressiva-no-contrato/142-CONTEXT.md
@.planning/phases/142-cadastro-da-tabela-progressiva-no-contrato/142-01-PLAN.md
@.planning/phases/142-cadastro-da-tabela-progressiva-no-contrato/142-02-PLAN.md
@resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx
@resources/js/Pages/Admin/TabelasContrato.jsx
</context>

<interfaces>
Props que `TabelaEmpresaContratoController::show()` entrega (plano 142-02):
- `company`: `{ id, name, company_group_id, grupo_nome }`
- `tabela_empresa`: `Array<{ ordem, limite_superior|null, valor, valor_e_piso }>` (vazio = sem tabela)
- `procedencia_empresa`: `'manual' | 'contrato' | 'presumida_servico' | null`
- `tabela_grupo`: mesmo formato, ou `null` quando a empresa não está em grupo
- `tabela_aplicada`: `{ origem, servico_nome, grupo_nome, herdada_de_company_name, faixas }` ou `null`
- `modelos_de_partida`: `Array<{ id, nome, faixas }>`
- `leitura_pendente`: `{ id, nome_envelope, tem_tabela }` ou `null`

Rotas Ziggy disponíveis: `admin.contratos.tabela.salvar` (POST, company), `admin.contratos.tabela.remover`
(DELETE, company), `admin.contratos.tabela.grupo.salvar` (POST, grupo), `admin.contratos.tabela.grupo.remover`
(DELETE, grupo), `admin.contratos.show` (GET, company), `admin.contratos.tabelas.index` (GET).

Padrão de máscara do projeto (`Comercial/NovaEmpresa.jsx`, `Admin/ContratoDetalhe.jsx`):
`import { IMaskInput } from 'react-imask';` com `value` + `onAccept`.

Componentes disponíveis: `@/Components/ui/{dialog,button,input,label,card}`, `@/lib/utils` (`cn`,
`formatCurrency`), `@/Layouts/AppLayout`, ícones `lucide-react`.

Erros do Inertia chegam em `onError(errors)`; as chaves do `SalvarFaixasFaturamentoRequest` são
`faixas`, `faixas.{idx}.limite_superior`, `faixas.{idx}.valor`, `faixas.{idx}.valor_e_piso` — o
front **exibe** a mensagem que veio do servidor, nunca reimplementa a regra de sobreposição.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarefa 1: máscara de dinheiro e a grade compartilhada</name>
  <files>resources/js/lib/dinheiro.js, resources/js/Components/ui/campo-dinheiro.jsx, resources/js/Components/Fechamento/TabelaProgressivaFaixas.jsx, resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx, tests/Feature/Phase139/Phase139TabelaProgressivaFielTest.php</files>
  <action>
    **`resources/js/lib/dinheiro.js`** — exporta `MASCARA_DINHEIRO` (objeto de opções do imask:
    `mask: Number`, `scale: 2`, `thousandsSeparator: '.'`, `radix: ','`, `mapToRadix: ['.']`,
    `padFractionalZeros: true`, `normalizeZeros: true`, `min: 0`), `formatarDinheiro(n)` (usa
    `toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' })`, devolve `'—'` para nulo) e
    `paraTextoDeCampo(n)` (número → string com vírgula decimal, para alimentar o `value` do input).
    Comentários em pt-BR explicando por que a máscara é do imask e não escrita à mão.

    **`resources/js/Components/ui/campo-dinheiro.jsx`** — componente `CampoDinheiro` (kebab-case no
    arquivo, PascalCase no export, como os outros primitivos de `ui/`). Envolve `IMaskInput` com
    `{...MASCARA_DINHEIRO}`, recebe `valor` (número ou null) e `onChange(numeroOuNull)`, e lê o
    número tipado do próprio imask: `onAccept={(_, mask) => onChange(mask.value === '' ? null : mask.typedValue)}`.
    ⚠️ Nunca fazer `parseFloat` de string mascarada no componente pai — quem sabe desmascarar é a
    máscara. Aceita `placeholder`, `disabled`, `aria-label` e `className`, com as mesmas classes do
    `Input` de `@/Components/ui/input` para não destoar.
    Prop opcional `avisoAcimaDe` (número): quando o valor passa desse teto, renderiza abaixo do campo
    uma linha âmbar "Confira: esse valor é maior que {formatado}." — é a defesa barata contra o zero
    a mais que o 142-CONTEXT chama pelo nome. Não bloqueia nada.

    **`resources/js/Components/Fechamento/TabelaProgressivaFaixas.jsx`** — mover a subcomponente
    `TabelaProgressivaFaixas` (e os helpers `fmtBRL`/`fmtValorFaixa` que ela usa) de dentro de
    `TabelaFaixasSection.jsx` para cá, como `export default`, **byte a byte no markup** — mesmas
    classes, mesma grade `grid-cols-[80px_1fr_160px]`, mesmo `px-[18px] py-3`, `text-[13px]`,
    `px-[18px] py-2.5`, `bg-ecf-card-2`, `rounded-xl border border-white/[0.06] overflow-hidden`,
    mesma ordem de cabeçalho. A Fase 139 mediu essa densidade contra o handoff do usuário; isto é
    mudança de endereço, não de desenho.
    Acrescentar uma prop nova `notaRodape` (string opcional) renderizada abaixo da grade em
    `text-white/30 text-[12px]` — é por onde o plano 04 vai dizer, num mês fechado, que a tabela
    mostrada é a cadastrada hoje.

    **`TabelaFaixasSection.jsx`** — apagar a definição local e importar de
    `@/Components/Fechamento/TabelaProgressivaFaixas`. Os dois usos existentes (bloco de grupo e
    bloco de serviço) continuam idênticos. Nada mais muda neste arquivo neste plano — o corte das
    ações de escrita é do plano 04.

    **Retarget de `Phase139TabelaProgressivaFielTest`** — a extração muda o endereço do que o teste
    mede, e o teste precisa continuar medindo a MESMA coisa, de forma mais forte:
    - `existe_exatamente_uma_definicao_da_subcomponente_de_tabela_progressiva`: varrer todos os
      `.jsx` sob `resource_path('js')` e afirmar que `function TabelaProgressivaFaixas(` aparece
      **exatamente uma vez no projeto inteiro** (era "uma vez no arquivo"). Mais forte, não mais fraco.
    - `a_subcomponente_e_reaproveitada_nos_dois_blocos_grupo_e_servico`: contar `<TabelaProgressivaFaixas `
      no projeto inteiro e exigir `>= 2`.
    - `faturamento_ate_aparece_so_duas_vezes...`: substituir por
      `cabecalho_da_grade_existe_uma_vez_so_no_projeto` — a regex do cabeçalho
      (`<span>Faixa</span> <span>Faturamento até</span> <span className="text-right">Mensalidade</span>`)
      aparece exatamente uma vez em todo `resources/js`. O risco que o teste original perseguia era
      **grade duplicada**, não a palavra "Faturamento até" (que é label legítimo de formulário em
      mais de um lugar); a nova forma persegue o risco de verdade.
    - assertions de densidade, `<table`, fonte e escala de Tailwind: apontar para o arquivo novo
      `js/Components/Fechamento/TabelaProgressivaFaixas.jsx`, mantendo `js/Pages/Admin/Financeiro.jsx`
      onde ele já era medido junto.
    - `frase_de_heranca_da_tabela_do_grupo_continua_intacta`: intacto neste plano.
    Registrar no docblock do teste, em pt-BR, que a Fase 142 moveu o componente e por quê.

    Rodar `npm run build` ao final.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase139TabelaProgressivaFielTest|Phase139FechamentoUiContratoTest"</automated>
  </verify>
  <done>Uma única definição da grade em todo o projeto, máscara e campo criados, suíte da Fase 139 verde, build sem erro.</done>
</task>

<task type="auto">
  <name>Tarefa 2: a página exclusiva Admin/TabelaEmpresa.jsx</name>
  <files>resources/js/Pages/Admin/TabelaEmpresa.jsx</files>
  <action>
    Página nova, dentro de `AppLayout`, título `Adm · Tabela de cobrança — {company.name}`, largura
    `max-w-4xl` e link "Voltar para o contrato" (`admin.contratos.show`) no topo — mesma anatomia de
    cabeçalho de `ContratoDetalhe.jsx`.

    **Bloco 1 — O que está cobrando esta empresa hoje.** Lê `tabela_aplicada`. Mostra a grade com
    `<TabelaProgressivaFaixas>` e uma frase acima dizendo de onde a tabela vem, em português comum:
    - `origem = 'grupo'`: "Quem manda aqui é a tabela do grupo {grupo_nome}."
    - `origem = 'propria'`: "Esta é a tabela desta empresa."
    - `origem = 'servico'`: "Esta empresa ainda está usando a tabela do serviço {servico_nome}."
    - `tabela_aplicada = null`: bloco âmbar "Esta empresa não tem tabela de cobrança — ela fica de
      fora do fechamento até alguém cadastrar uma."

    **Bloco 2 — Como esta tabela chegou aqui.** Lê `procedencia_empresa`. Copy **sem jargão**
    (proibidos: snapshot, competência, reconsolidação, rollup, âncora, origem, faixa piso,
    presumida):
    - `contrato`: selo verde discreto "Conferida pelo contrato assinado".
    - `manual`: selo neutro "Cadastrada à mão no sistema".
    - `presumida_servico`: selo neutro + frase "Esta tabela foi copiada do serviço contratado quando
      o sistema passou a cobrar por empresa. **Ninguém conferiu ela contra o contrato ainda.**"
    - `null`: nada.
    ⚠️ A palavra "presumida" não pode aparecer como rótulo na tela.

    **Bloco 3 — Leitura de contrato esperando conferência.** Só quando `leitura_pendente` não é nulo:
    caixa neutra com o nome do envelope e um `Link` para `admin.contratos.tabelas.index`, com a copy
    "Um contrato assinado que parece ser desta empresa foi lido e ainda não foi conferido. Conferir
    lá vale mais do que digitar aqui — o texto veio do contrato." ⚠️ Dizer **"parece ser desta
    empresa"**, nunca "é": o vínculo da leitura automática é palpite (D-05 da Fase 140).
    É o link cruzado da decisão de projeto do plano 142-01.

    **Bloco 4 — Editar a tabela desta empresa.** O formulário, inline na página (não em `Dialog` —
    a página inteira é dedicada a isso; o `Dialog` existia porque o cadastro morava espremido dentro
    de um accordion). Estado inicial das linhas: `tabela_empresa` quando existe; uma linha vazia
    quando não existe. **Nunca a tabela do serviço por padrão.**
    Cada linha: `Ordem` (number), `Faturamento até` (`CampoDinheiro`, vazio = sem teto),
    `Valor da mensalidade` (`CampoDinheiro` com `avisoAcimaDe={100000}`), botão de remover, e o
    checkbox "Valor é um piso (a partir de)" só quando o teto está vazio — mesma regra de hoje, que
    o backend recusa de outro jeito. Botões "Adicionar faixa", "Salvar tabela" e, quando já existe
    tabela, "Apagar a tabela desta empresa" (com `confirm()`, mandando para
    `admin.contratos.tabela.remover`).
    Acima do formulário, quando `modelos_de_partida` não está vazio: "Começar a partir da tabela de
    {serviço}" — preenche as linhas do formulário com aquele modelo, **sem salvar**, com aviso de
    que aquilo é um ponto de partida e precisa ser conferido contra o contrato antes de salvar.
    Quando `procedencia_empresa === 'contrato'`, o botão Salvar pede confirmação: "Esta tabela veio
    do contrato assinado. Salvar por cima substitui ela pelo que está na tela. Continuar?"

    Submissão: `router.post(route('admin.contratos.tabela.salvar', company.id), { faixas: [...] }, { preserveScroll: true })`,
    com `ordem` inteiro, `limite_superior` número ou `null`, `valor` número, `valor_e_piso` booleano.
    Erros: exibir a primeira mensagem que veio do servidor, sem reimplementar regra nenhuma no front.

    **Bloco 5 — Tabela do grupo.** Só quando `company.company_group_id` não é nulo. Mesma estrutura
    do bloco 4, apontando para `admin.contratos.tabela.grupo.salvar` / `...grupo.remover`, com a
    frase que já existe hoje em `TabelaFaixasSection`: "Quem manda é a empresa do grupo que mais
    faturou no mês — se outra empresa passar na frente, a tabela muda junto." Estado inicial:
    `tabela_grupo` quando tem linhas; uma linha vazia quando não tem. Hoje zero grupos têm tabela —
    o estado normal deste bloco é o vazio, e ele precisa dizer isso sem parecer defeito.
    ⚠️ Extrair o formulário como uma subcomponente local usada pelos blocos 4 e 5 — duas cópias do
    mesmo formulário nesta página é o mesmo erro da grade, em escala menor.

    ⚠️ Escala do Tailwind: `px-4.5`, `gap-4.5`, `py-5.5` NÃO EXISTEM. Só decimais 0.5/1.5/2.5/3.5.
    Comentários em pt-BR.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>A página existe, compila, e o formulário abre preenchido com `tabela_empresa` quando a empresa já tem tabela.</done>
</task>

<task type="auto">
  <name>Tarefa 3: travas de arquivo da ficha + conferência do CSS compilado</name>
  <files>tests/Feature/Phase142/Phase142FichaTabelaUiTest.php</files>
  <action>
    Suíte de travas de UI no padrão que o projeto já usa (leitura do `.jsx` como texto — mesmo molde
    de `Phase139TabelaProgressivaFielTest`). Sobre `js/Pages/Admin/TabelaEmpresa.jsx`:

    (a) usa `CampoDinheiro` e **não** contém `type="number"` nos campos de dinheiro — o campo cru é
        exatamente o que D-02 manda tirar;
    (b) usa `tabela_empresa` como estado inicial do formulário e **não** contém a frase do aviso
        antigo "Os valores atuais não são carregados aqui" — a dívida do 137-09 está paga;
    (c) importa a grade de `@/Components/Fechamento/TabelaProgressivaFaixas` e não define uma
        segunda `function TabelaProgressivaFaixas(`;
    (d) nenhuma das oito palavras de jargão aparece como texto visível: `snapshot`,
        `reconsolidação`, `rollup`, `âncora`/`ancora`, `competência`, `origem`, `faixa piso`,
        `presumida` (as sete primeiras são as travadas desde a Fase 139; "presumida" entra pela
        restrição do 142-CONTEXT). Reusar o helper de varredura de texto visível da suíte da Fase
        139, não escrever um segundo;
    (e) aponta para as quatro rotas da ficha (`admin.contratos.tabela.salvar`,
        `admin.contratos.tabela.remover`, `admin.contratos.tabela.grupo.salvar`,
        `admin.contratos.tabela.grupo.remover`) e **não** para nenhuma `admin.financeiro.faixas.*`;
    (f) tem o link cruzado para `admin.contratos.tabelas.index` e a frase que diz "parece ser desta
        empresa";
    (g) a regex de escala quebrada do Tailwind (`px-4.5`, `gap-4.5`, `py-5.5` e afins) não casa —
        copiar a assertion `nao_escala_valores_com_a_notacao_quebrada_de_decimais_do_tailwind` da
        Fase 139 apontando para os arquivos novos deste plano.

    **Conferência no CSS compilado.** Depois de `npm run build`, rodar um script Node curto no
    scratchpad que lê os `.css` de `public/build/assets/` e confirma que as classes com colchetes
    usadas nos arquivos novos foram efetivamente geradas (no mínimo `grid-cols-[80px_1fr_160px]`,
    `px-[18px]`, `text-[13px]`, `border-white/[0.06]`). ⚠️ Usar Node com `fs.readFileSync` e
    `String.prototype.includes`, **nunca `grep` do shell** — o shell escapa mal os colchetes e dá
    falso negativo. Registrar o resultado no SUMMARY.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter=Phase142FichaTabelaUiTest</automated>
  </verify>
  <done>Todas as travas passam e o script Node confirma as classes com colchetes presentes no CSS compilado.</done>
</task>

</tasks>

<threat_model>
## Fronteiras de confiança

| Fronteira | Descrição |
|-----------|-----------|
| digitação humana → payload de faixas | um zero a mais aqui vira fatura errada em cobrança viva |
| props do Inertia → renderização | dado de tabela de cobrança de uma empresa numa página pública para quem tem `admin.contratos` |

## Registro STRIDE

| ID | Categoria | Componente | Disposição | Mitigação |
|----|-----------|------------|------------|-----------|
| T-142-12 | Tampering | campos de valor | mitigate | máscara `react-imask` com separador de milhar torna o zero a mais visível; `avisoAcimaDe` alerta acima de R$ 100.000; a validação autoritativa segue no servidor |
| T-142-13 | Tampering | formulário pré-preenchido | mitigate | estado inicial vem de `tabela_empresa` (banco); a tabela do serviço só entra por clique explícito, com aviso, e sem salvar sozinha |
| T-142-14 | Tampering | substituição de tabela vinda do contrato | mitigate | `confirm()` nomeando a consequência antes do POST; o servidor devolve o `aviso` de que substituiu (plano 02) |
| T-142-15 | Repudiation | edição pela ficha | mitigate | toda escrita passa pelo `GravarTabelaEmpresaService` com `feito_de = 'contrato_ficha'` |
| T-142-16 | Information disclosure | bloco de leitura pendente | mitigate | só nome do envelope; sem texto do contrato e sem link da Clicksign |
| T-142-17 | Spoofing | vínculo palpitado da leitura | mitigate | copy obrigatória "parece ser desta empresa"; a ficha não confirma proposta nenhuma — só linka |
| T-142-SC | Tampering | instalação de pacotes | mitigate | nenhum pacote novo — `react-imask ^7.6.1` já está em `package.json` e já é usado em quatro páginas |
</threat_model>

<verification>
- `npm run build` sem erro e sem aviso novo.
- Script Node confirma as classes com colchetes no CSS compilado.
- `C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase139|Phase142"` sem falhas.
</verification>

<success_criteria>
- Formulário abre preenchido com o que está gravado, nunca com valores adivinhados.
- Campos de dinheiro mascarados; nenhum `type="number"` para valor.
- Uma definição de grade em todo o projeto.
- Nenhuma das oito palavras de jargão na tela.
</success_criteria>

<output>
Criar `.planning/phases/142-cadastro-da-tabela-progressiva-no-contrato/142-03-SUMMARY.md` ao terminar,
registrando o resultado da conferência do CSS compilado.
Commit em pt-BR, **apenas com os caminhos deste plano** (`git commit -- <arquivos>`) — árvore
compartilhada; nunca `git add -A`, `git add .`, `git commit -a` ou `git stash`.
</output>
