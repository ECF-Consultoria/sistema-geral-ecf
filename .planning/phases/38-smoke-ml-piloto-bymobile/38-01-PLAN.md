---
phase: 38-smoke-ml-piloto-bymobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Sugadores/MercadoLivreAdsService.php
  - storage/app/sugadores/ml-smoke/.gitkeep
  - tests/Feature/Phase38/MercadoLivreAdsServiceTest.php
autonomous: true
requirements:
  - REQ-38-02
  - REQ-38-03
  - REQ-38-04
  - REQ-38-07
user_setup: []

must_haves:
  truths:
    - "Novo service `App\\Services\\Sugadores\\MercadoLivreAdsService` existe e expõe métodos para: descoberta de advertiser, listagem de campanhas Product Ads, listagem de ads/items e tentativa de métricas de ads no período"
    - "Cada método do service retorna payload cru (array associativo + status HTTP + URL chamada) — não normaliza para o contrato §2.3 (normalização é responsabilidade do comando smoke no Plan 02)"
    - "Service reutiliza `App\\Services\\MercadoLivreService` (Phase 20) para autenticação/refresh do mlToken (não duplica lógica OAuth)"
    - "Service falha cedo com `\\RuntimeException` de mensagem clara em ausência de token, 401 persistente, 403, 5xx — sem catch silencioso"
    - "Diretório `storage/app/sugadores/ml-smoke/` existe versionado (via `.gitkeep`) — pronto para receber fixture do Plan 02"
    - "Nenhum arquivo de produção do módulo Sugadores foi tocado (SugadorAnalysisService/SugadorController/AnalyzeCompanySugadoresJob/FetchAdmanMlbsByCampaignJob intactos)"
    - "Suite `tests/Feature/Phase38/MercadoLivreAdsServiceTest.php` passa com Http::fake cobrindo: (a) advertiser descoberto OK, (b) advertiser ausente (lista vazia), (c) lista campanhas OK, (d) 401 persistente após refresh dispara exceção"
  artifacts:
    - path: "app/Services/Sugadores/MercadoLivreAdsService.php"
      provides: "Wrapper ML Mercado Ads stateless — chamadas crus para advertiser/campaigns/ads/metrics"
      contains: "namespace App\\Services\\Sugadores"
    - path: "storage/app/sugadores/ml-smoke/.gitkeep"
      provides: "Diretório versionado para fixture JSON do Plan 02"
    - path: "tests/Feature/Phase38/MercadoLivreAdsServiceTest.php"
      provides: "4 tests Http::fake cobrindo path feliz + erros"
      contains: "namespace Tests\\Feature\\Phase38"
  key_links:
    - from: "app/Services/Sugadores/MercadoLivreAdsService.php"
      to: "app/Services/MercadoLivreService.php"
      via: "constructor injection (DI Laravel) — chama ->get(\\$company, \\$endpoint, \\$query, \\$headers) já existente (linha 271)"
      pattern: "MercadoLivreService"
---

<objective>
Criar o service stateless `App\Services\Sugadores\MercadoLivreAdsService` que encapsula as chamadas Mercado Ads (Product Ads) necessárias para o smoke da Phase 38, **sem duplicar lógica OAuth** (delega para `MercadoLivreService` da Phase 20).

**Justificativa da divisão em 2 plans:** o Plan 01 entrega a camada HTTP testável via `Http::fake` (rápido, autônomo, sem dependência de token real). O Plan 02 entrega o comando Artisan + relatório CLI + smoke real contra a API ML (precisa de gate humano porque o executor não consegue rodar HTTP real contra ML com token do Bymobille).

Purpose: pavimentar o caminho para o comando `sugadores:ml-smoke` (Plan 02) com uma fachada limpa, testada e isolada. Service é stand-alone — não toca o path de produção do Sugadores.

Output: 1 service + 1 diretório versionado + 1 suite de testes Http::fake.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-smoke-ml-piloto-bymobile/38-CONTEXT.md
@plano-migracao-sugadores-ml-direto.md
@app/Services/MercadoLivreService.php
@app/Models/MlToken.php
@app/Models/Company.php
@CLAUDE.md

<interfaces>
<!-- Contratos extraídos da Phase 20 (MercadoLivreService) que este service reutiliza -->

From app/Services/MercadoLivreService.php:
```
class MercadoLivreService
{
    private const API_BASE = 'https://api.mercadolibre.com';

    // Garante token válido (renova se expirar em <5min). Retorna null se sem token ou revogado.
    public function ensureValidToken(Company $company): ?MlToken;

    // GET autenticado com retry após refresh em 401. Lança RuntimeException se 4xx/5xx persistente.
    public function get(Company $company, string $endpoint, array $query = [], array $headers = []): array;
}
```

From app/Models/MlToken.php:
```
class MlToken extends Model
{
    // Campos relevantes: company_id, access_token (encrypted), refresh_token (encrypted),
    // expires_at, status ('active'|'revoked'), ml_user_id (string — seller ID).
}
```

From app/Services/MercadoLivreService.php — endpoints Mercado Ads JÁ comprovados em prod (Phase 20):
```
// Resolução de advertiser (linha 438-459 do MercadoLivreService):
GET /advertising/advertisers?product_id=PADS
Headers: { 'Api-Version': '1' }
Resposta: { advertisers: [ { advertiser_id, site_id, ... } ] }

// Listagem de campanhas Product Ads (linha 548-590):
GET /marketplace/advertising/MLB/advertisers/{advertiser_id}/product_ads/campaigns/search
Query: date_from, date_to, aggregation_type=CAMPAIGN, metrics, limit, offset
Headers: { 'Api-Version': '1' }
Resposta: { results: [...], paging: { total } }

// Listagem de ads/items (linha 598-633 — NÃO comprovado em prod, candidato a validar):
GET /advertising/advertisers/{advertiser_id}/product_ads/items
Query: date_from, date_to, aggregation_type, metrics, limit, offset
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar diretório storage versionado + suite de testes Http::fake (RED)</name>
  <files>storage/app/sugadores/ml-smoke/.gitkeep, tests/Feature/Phase38/MercadoLivreAdsServiceTest.php</files>
  <read_first>
    - app/Services/MercadoLivreService.php — entender shape de payload `/advertising/advertisers` (linha 438-459) e `/product_ads/campaigns/search` (linha 548-590); o teste vai espelhar esses payloads via `Http::fake`
    - tests/Feature/Phase30/SyncAdgroupMlbsTest.php — padrão de Feature test que usa `Http::fake()` no projeto (referência de estilo)
    - app/Models/MlToken.php — entender shape para criar fixtures de MlToken
    - .planning/phases/38-smoke-ml-piloto-bymobile/38-CONTEXT.md — confirmar tratamento de erros decidido (401/403/429/5xx) e endpoints candidatos
    - CLAUDE.md — convenção pt-BR em comentários de teste; PSR-4 namespace `Tests\Feature\Phase38`
  </read_first>
  <behavior>
    Suite com 4 tests cobrindo o service que será criado na Tarefa 2:

    - **Test 1 — `discoverAdvertiser_returns_advertiser_data_when_account_has_pads`**:
      cria Company + MlToken ativo via factory/seeder direto;
      `Http::fake(['*/advertising/advertisers*' => Http::response(['advertisers' => [['advertiser_id' => 71098, 'site_id' => 'MLB', 'seller_id' => '465723451']]], 200)])`;
      chama `$service->discoverAdvertiser($company)`;
      assert: retorna array com keys `advertiser_id=71098`, `site_id='MLB'`, `seller_id='465723451'`, `raw` (payload cru completo), `url` (URL chamada), `status=200`.

    - **Test 2 — `discoverAdvertiser_returns_empty_when_account_has_no_pads`**:
      `Http::fake(['*/advertising/advertisers*' => Http::response(['advertisers' => []], 200)])`;
      chama `$service->discoverAdvertiser($company)`;
      assert: retorna array com `advertiser_id=null`, `raw=['advertisers' => []]`, `status=200` (NÃO lança exceção — conta sem Mercado Ads é estado válido).

    - **Test 3 — `listCampaigns_paginates_and_returns_all_results`**:
      `Http::fake` retorna 2 páginas (50 + 23 results, total=73);
      chama `$service->listCampaigns($company, 71098, '2026-05-26', '2026-06-25')`;
      assert: retorna array com `count=73`, `results` array com 73 itens, `raw_first_page` com keys `results`+`paging`, `endpoints_tried` array com 2 URLs chamadas.

    - **Test 4 — `discoverAdvertiser_throws_when_401_persists_after_refresh`**:
      `Http::fake(['*/advertising/advertisers*' => Http::response(['error' => 'invalid_token'], 401)])`;
      assert: chamar `$service->discoverAdvertiser($company)` lança `\RuntimeException` com mensagem contendo "Token" ou "401" (NÃO swallow).
      (Reusa o tratamento de 401-com-refresh já existente no `MercadoLivreService::get()` — o service novo só propaga.)

    Todos os 4 tests usam `RefreshDatabase` trait + factory inline para Company/MlToken (sem migration nova).
  </behavior>
  <action>
    Crie `storage/app/sugadores/ml-smoke/.gitkeep` (arquivo vazio, apenas para versionar diretório — sem ele, `git` não rastreia diretório vazio e o Plan 02 não consegue gravar fixture).

    Crie `tests/Feature/Phase38/MercadoLivreAdsServiceTest.php` com:
    - namespace `Tests\Feature\Phase38`;
    - uses `Tests\TestCase`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Illuminate\Support\Facades\Http`, `App\Models\Company`, `App\Models\MlToken`, `App\Services\Sugadores\MercadoLivreAdsService`;
    - método `setUp()` chama `parent::setUp()` + `Http::preventStrayRequests()` (garantia anti-rede);
    - método `private function makeCompanyWithMlToken(): Company` cria Company + MlToken (`access_token='fake-token-xyz'`, `refresh_token='fake-refresh'`, `expires_at=now()->addHour()`, `status='active'`, `ml_user_id='465723451'`);
    - 4 tests conforme `<behavior>` acima;
    - resolver service via `$this->app->make(MercadoLivreAdsService::class)` (Laravel DI resolve `MercadoLivreService` automaticamente).

    Comentários em pt-BR. Use `assertEquals`/`assertTrue`/`assertCount`/`expectException` (PHPUnit padrão do projeto).

    Rode `php artisan test --filter=MercadoLivreAdsServiceTest` — DEVE falhar com `Class App\Services\Sugadores\MercadoLivreAdsService not found` (RED esperado; Tarefa 2 cria a classe).
  </action>
  <verify>
    <automated>php artisan test --filter=MercadoLivreAdsServiceTest 2>&1 | grep -E "(Class.*MercadoLivreAdsService.*not found|Error|Errors)"</automated>
  </verify>
  <acceptance_criteria>
    - `storage/app/sugadores/ml-smoke/.gitkeep` existe (`test -f` retorna 0).
    - `tests/Feature/Phase38/MercadoLivreAdsServiceTest.php` existe.
    - `grep -c "namespace Tests.Feature.Phase38" tests/Feature/Phase38/MercadoLivreAdsServiceTest.php` ≥ 1.
    - `grep -c "function test_" tests/Feature/Phase38/MercadoLivreAdsServiceTest.php` ≥ 4 (4 métodos de teste).
    - `grep -c "Http::fake" tests/Feature/Phase38/MercadoLivreAdsServiceTest.php` ≥ 4 (cada teste configura seu fake).
    - `grep -c "Http::preventStrayRequests" tests/Feature/Phase38/MercadoLivreAdsServiceTest.php` ≥ 1 (guarda anti-rede).
    - `php artisan test --filter=MercadoLivreAdsServiceTest` retorna exit code != 0 com mensagem indicando classe inexistente (RED esperado).
  </acceptance_criteria>
  <done>
    Diretório storage versionado + 4 tests no RED apontando para classe inexistente (próxima tarefa cria a classe).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar MercadoLivreAdsService (GREEN)</name>
  <files>app/Services/Sugadores/MercadoLivreAdsService.php</files>
  <read_first>
    - app/Services/MercadoLivreService.php (linhas 271-317 do `get()`, 438-459 do `resolveAdvertiserId()`, 548-590 do `fetchCampaigns()`, 598-633 do `fetchAdsItems()`) — copiar shape de chamada, NÃO duplicar lógica (delegar via `->get()`)
    - tests/Feature/Phase38/MercadoLivreAdsServiceTest.php (criado na Tarefa 1) — usar como spec do que precisa retornar
    - .planning/phases/38-smoke-ml-piloto-bymobile/38-CONTEXT.md (seção "Endpoints candidatos") — endpoints HTTP exatos a chamar
    - plano-migracao-sugadores-ml-direto.md §2.1, §2.2, §2.3 — confirmar fluxo advertiser → campanhas → ads → métricas
    - CLAUDE.md — convenção pt-BR em comentários, naming PascalCase
  </read_first>
  <action>
    Crie `app/Services/Sugadores/MercadoLivreAdsService.php` com:

    1. **Namespace:** `App\Services\Sugadores` (NOVO namespace — primeiro service nesse path).

    2. **Imports:** `Illuminate\Support\Facades\Log`, `App\Models\Company`, `App\Services\MercadoLivreService`.

    3. **Constantes privadas (URLs candidatas — comentário pt-BR explicando que são "candidatas a validar contra doc oficial https://developers.mercadolivre.com.br/"):**
       - `private const ENDPOINT_ADVERTISERS = '/advertising/advertisers';`
       - `private const ENDPOINT_CAMPAIGNS_SEARCH = '/marketplace/advertising/MLB/advertisers/%d/product_ads/campaigns/search';` (template com `%d` para `sprintf`)
       - `private const ENDPOINT_ADS_ITEMS = '/advertising/advertisers/%d/product_ads/items';`
       - `private const API_VERSION_HEADER = ['Api-Version' => '1'];`
       - `private const DEFAULT_METRICS = 'cost,clicks,prints,total_amount,direct_amount,indirect_amount,acos,roas,cpc,units_quantity,ctr';`

    4. **Constructor injection:** `public function __construct(private MercadoLivreService $ml) {}`

    5. **Método `discoverAdvertiser(Company $company): array`:**
       - Monta `$endpoint = self::ENDPOINT_ADVERTISERS`, `$query = ['product_id' => 'PADS']`, `$headers = self::API_VERSION_HEADER`;
       - Chama `$payload = $this->ml->get($company, $endpoint, $query, $headers);` (delega auth/refresh para Phase 20);
       - Extrai `$advertisers = $payload['advertisers'] ?? []`;
       - Retorna `[
           'advertiser_id' => $advertisers[0]['advertiser_id'] ?? null,
           'site_id' => $advertisers[0]['site_id'] ?? null,
           'seller_id' => $advertisers[0]['seller_id'] ?? null,
           'raw' => $payload,
           'url' => $endpoint . '?product_id=PADS',
           'status' => 200,
         ];`
       - Se `MercadoLivreService::get()` lançar `\RuntimeException` (401 persistente, 5xx, etc), **propaga** sem catch (teste 4 valida).

    6. **Método `listCampaigns(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array`:**
       - Loop com `$offset=0`, `$limit=50`, `$results=[]`, `$endpointsTried=[]`, `$firstPagePayload=null`;
       - Em cada iteração: monta `$endpoint = sprintf(self::ENDPOINT_CAMPAIGNS_SEARCH, $advertiserId)`; chama `$page = $this->ml->get($company, $endpoint, ['date_from' => $dateFrom, 'date_to' => $dateTo, 'aggregation_type' => 'CAMPAIGN', 'metrics' => self::DEFAULT_METRICS, 'limit' => $limit, 'offset' => $offset], self::API_VERSION_HEADER);`
       - Acumula `$results = array_merge($results, $page['results'] ?? []);` e `$endpointsTried[] = $endpoint . '?offset=' . $offset;`
       - Salva `$firstPagePayload = $firstPagePayload ?? $page;` (preserva shape do payload cru para inspeção no relatório)
       - `$total = $page['paging']['total'] ?? count($results);` `$offset += $limit;` quebra loop quando `$offset >= $total` OU `$offset >= 500` (teto de segurança igual ao MercadoLivreService::fetchCampaigns linha 587)
       - Retorna `['count' => count($results), 'results' => $results, 'raw_first_page' => $firstPagePayload, 'endpoints_tried' => $endpointsTried];`

    7. **Método `listAds(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array`:**
       - Mesma estrutura de `listCampaigns()` mas com `$endpoint = sprintf(self::ENDPOINT_ADS_ITEMS, $advertiserId)` e `aggregation_type='total'` no query.
       - Teto de segurança: `$offset >= 2000` (igual `fetchAdsItems` linha 630).
       - **Importante**: endpoint NÃO foi validado em prod (Phase 20 nunca chamou). Comentário pt-BR no método: `// Endpoint candidato a validar — Phase 38 smoke é justamente para confirmar shape do payload`.
       - Retorna mesma estrutura: `count`, `results`, `raw_first_page`, `endpoints_tried`.

    8. **Método `tryFetchAdsMetrics(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array`:**
       - Wrapper try/catch sobre `listAds()` que captura `\RuntimeException`;
       - Em sucesso retorna `['ok' => true, 'data' => $listAdsResult, 'error' => null];`
       - Em erro retorna `['ok' => false, 'data' => null, 'error' => ['message' => $e->getMessage(), 'class' => get_class($e)]];` (relatório do Plan 02 precisa diferenciar "endpoint funcionou mas vazio" de "endpoint 404")

    9. **Comentários PHPDoc em pt-BR** em cada método público com:
       - Descrição curta
       - `@param`/`@return` documentando shape do array retornado
       - Para `listAds`/`tryFetchAdsMetrics`: nota "ENDPOINT CANDIDATO — validar contra doc oficial; smoke da Phase 38 imprime URL+status para correção rápida".

    Não logar `access_token` em lugar nenhum (segurança §security_minimum do contexto). `Log::warning` apenas para diagnóstico de chamadas falhadas com `$company->id` (não com token).

    Rode `php artisan test --filter=MercadoLivreAdsServiceTest` — DEVE passar 4/4 (GREEN).
  </action>
  <verify>
    <automated>php artisan test --filter=MercadoLivreAdsServiceTest 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `app/Services/Sugadores/MercadoLivreAdsService.php` existe.
    - `grep -c "^namespace App.Services.Sugadores;" app/Services/Sugadores/MercadoLivreAdsService.php` == 1.
    - `grep -c "class MercadoLivreAdsService" app/Services/Sugadores/MercadoLivreAdsService.php` == 1.
    - `grep -c "private MercadoLivreService" app/Services/Sugadores/MercadoLivreAdsService.php` == 1 (DI do service Phase 20).
    - Métodos públicos presentes: `grep -v '^[[:space:]]*//' app/Services/Sugadores/MercadoLivreAdsService.php | grep -cE "public function (discoverAdvertiser|listCampaigns|listAds|tryFetchAdsMetrics)"` == 4.
    - Não duplica OAuth: `grep -c "TOKEN_URL\|exchangeCode\|refreshToken\|saveToken" app/Services/Sugadores/MercadoLivreAdsService.php` == 0.
    - Não loga token: `grep -ciE "access_token|refresh_token" app/Services/Sugadores/MercadoLivreAdsService.php` == 0 (service nunca toca o token diretamente — `MercadoLivreService::get()` cuida disso internamente).
    - `php artisan test --filter=MercadoLivreAdsServiceTest` retorna exit code 0 com "OK (4 tests" ou "Tests: 4" no output.
    - Nenhum arquivo de prod do Sugadores tocado: `git diff --name-only HEAD app/Services/SugadorAnalysisService.php app/Http/Controllers/SugadorController.php app/Jobs/AnalyzeCompanySugadoresJob.php app/Jobs/FetchAdmanMlbsByCampaignJob.php 2>&1` retorna vazio.
  </acceptance_criteria>
  <done>
    Service criado, 4/4 tests verdes, zero toque em arquivos de produção do Sugadores, contrato pronto para o comando smoke do Plan 02 consumir.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → API ML | Service envia `access_token` (criptografado em rest no MlToken) via header Bearer — chamada externa para `api.mercadolibre.com` |
| Service ↔ MercadoLivreService (Phase 20) | Trust interno; Plan 01 não toca OAuth/refresh diretamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure | `MercadoLivreAdsService` logging | mitigate | Service NÃO loga `access_token`/`refresh_token`; só usa `$company->id` em mensagens. Acceptance criteria valida via grep. |
| T-38-02 | Spoofing | Token expirado/inválido em chamada ao ML | mitigate | Delega para `MercadoLivreService::ensureValidToken()` (Phase 20) que faz refresh single-use com `Cache::lock` — pattern já em prod. |
| T-38-03 | Denial of Service | Loop de paginação infinita | mitigate | Tetos de segurança em `listCampaigns` (offset ≥ 500) e `listAds` (offset ≥ 2000) — espelha `MercadoLivreService::fetchCampaigns/fetchAdsItems`. |
| T-38-04 | Tampering | Testes que fazem chamadas reais ao ML por engano | mitigate | `Http::preventStrayRequests()` no `setUp()` força todas as chamadas a passarem pelo `Http::fake()`. |

**Severidade global:** LOW (service stand-alone sem rota HTTP, sem persistência, consumido apenas pelo comando Artisan do Plan 02 que roda manualmente).
</threat_model>

<verification>
- Suite `tests/Feature/Phase38/MercadoLivreAdsServiceTest.php` 4/4 verde.
- Suite Phase 20 (regressão MercadoLivreService) continua verde: `php artisan test --filter=MercadoLivre` (excluindo o Phase38) sem novos vermelhos.
- Grep final: nenhum arquivo de prod do Sugadores foi modificado.
</verification>

<success_criteria>
- `app/Services/Sugadores/MercadoLivreAdsService.php` criado, 4 métodos públicos, namespace correto.
- 4 tests Http::fake verdes (RED→GREEN claro entre Tarefa 1 e Tarefa 2).
- `storage/app/sugadores/ml-smoke/.gitkeep` versionado.
- Plan 02 pode injetar `MercadoLivreAdsService` no comando Artisan sem refactor adicional.
</success_criteria>

<output>
Criar `.planning/phases/38-smoke-ml-piloto-bymobile/38-01-SUMMARY.md` ao concluir, registrando:
- Decisões tomadas (signatures finais dos métodos públicos do service)
- Endpoints implementados (com nota sobre quais foram comprovados em Phase 20 e quais são "candidatos a validar" — `listAds`)
- 4 tests adicionados + comando de regressão Phase 20
- Confirmação de zero toque em arquivos de prod do Sugadores
- Próximo passo: Plan 02 (comando Artisan + checkpoint humano para smoke real)
</output>
