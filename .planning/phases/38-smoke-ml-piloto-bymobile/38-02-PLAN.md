---
phase: 38-smoke-ml-piloto-bymobile
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - app/Console/Commands/SugadoresMlSmoke.php
  - tests/Feature/Phase38/MlSmokeCommandTest.php
autonomous: false
requirements:
  - REQ-38-01
  - REQ-38-05
  - REQ-38-06
  - REQ-38-07

user_setup:
  - service: mercadolivre
    why: "Smoke real precisa de empresa com mlToken ativo (Bymobille - Teste é a única hoje); operador roda manualmente no host de dev XAMPP e revisa o relatório"
    env_vars: []
    dashboard_config:
      - task: "Confirmar company_id do Bymobille via tinker antes de rodar smoke real"
        location: "php artisan tinker --execute='dump(\\App\\Models\\Company::where(\"name\",\"like\",\"%Bymobille%\")->whereHas(\"mlToken\")->get([\"id\",\"name\"])->toArray());'"

must_haves:
  truths:
    - "Comando artisan `sugadores:ml-smoke --company={id} --days=30` está registrado e roda sem fatal error (`php artisan list | grep sugadores:ml-smoke` retorna a linha)"
    - "Comando falha com exit code != 0 e mensagem clara quando empresa não existe, não tem mlToken, ou token é inválido após tentativa de refresh"
    - "Comando grava arquivo JSON em `storage/app/sugadores/ml-smoke/{company_id}-{YYYY-MM-DD}.json` com chaves `company_id`, `company_name`, `run_at`, `days_window`, `advertiser`, `campaigns`, `ads`, `metrics`, `report`"
    - "Comando imprime no console relatório com seções obrigatórias: `endpoints_ok`, `endpoints_failed`, `contract_fields_present`, `contract_fields_missing`, `blockers`"
    - "Comando NÃO loga `access_token` nem `refresh_token` no console nem na fixture (segurança §security_minimum)"
    - "Nenhuma alteração em arquivos de produção do módulo Sugadores: `SugadorAnalysisService`, `SugadorController`, `AnalyzeCompanySugadoresJob`, `FetchAdmanMlbsByCampaignJob`, migrations, schema, `routes/console.php` (smoke é manual, não agendado)"
    - "Suite `tests/Feature/Phase38/MlSmokeCommandTest.php` passa com 4 tests Http::fake cobrindo: (1) empresa inexistente, (2) empresa sem mlToken, (3) sucesso completo gera fixture com shape correto, (4) relatório CLI contém seções obrigatórias"
    - "Smoke real (Bymobille, host de dev) executado pelo operador com sucesso OU com erro claro de permissão/scope/token — checkpoint humano valida saída"
  artifacts:
    - path: "app/Console/Commands/SugadoresMlSmoke.php"
      provides: "Comando Artisan smoke ML — orquestra MercadoLivreAdsService + grava fixture + imprime relatório"
      contains: "protected $signature = 'sugadores:ml-smoke"
    - path: "tests/Feature/Phase38/MlSmokeCommandTest.php"
      provides: "4 tests Feature do comando smoke"
      contains: "namespace Tests\\Feature\\Phase38"
    - path: "storage/app/sugadores/ml-smoke/{company_id}-{YYYY-MM-DD}.json"
      provides: "Fixture JSON anonimizável gerada pelo smoke real (NÃO versionada — gerada em runtime; `.gitkeep` do Plan 01 garante diretório)"
  key_links:
    - from: "app/Console/Commands/SugadoresMlSmoke.php"
      to: "app/Services/Sugadores/MercadoLivreAdsService.php"
      via: "constructor injection (Laravel DI)"
      pattern: "MercadoLivreAdsService"
    - from: "app/Console/Commands/SugadoresMlSmoke.php"
      to: "storage/app/sugadores/ml-smoke/"
      via: "Storage::disk('local')->put() ou file_put_contents(storage_path(...))"
      pattern: "storage_path.*sugadores.*ml-smoke"
---

<objective>
Entregar o comando Artisan `sugadores:ml-smoke --company={id} --days=30` (Fase 0 do plano de migração v11.0) que orquestra o `MercadoLivreAdsService` (Plan 01), grava fixture JSON e imprime relatório CLI estruturado para o operador decidir se a Phase 39 pode começar.

Purpose: gate de entrada da Milestone v11.0. Sem este smoke verde com dados reais do Bymobille, a Phase 39 (provider pattern) não tem fixtures nem confirmação dos endpoints ML.

Output: 1 comando Artisan + 4 tests Feature + checkpoint humano que valida o smoke real contra a API ML.

**autonomous: false** — última tarefa é checkpoint `human-verify` (executor não consegue rodar HTTP real contra ML; operador precisa rodar `php artisan sugadores:ml-smoke --company={id_bymobille}` no XAMPP e revisar o relatório).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-smoke-ml-piloto-bymobile/38-CONTEXT.md
@.planning/phases/38-smoke-ml-piloto-bymobile/38-01-PLAN.md
@plano-migracao-sugadores-ml-direto.md
@app/Services/MercadoLivreService.php
@app/Console/Commands/SyncAdmanData.php
@app/Models/Company.php
@CLAUDE.md

<interfaces>
<!-- Contratos da Tarefa 2 do Plan 01 que este comando consome -->

From app/Services/Sugadores/MercadoLivreAdsService.php (criado no Plan 01):
```
namespace App\Services\Sugadores;

class MercadoLivreAdsService
{
    public function __construct(private \App\Services\MercadoLivreService $ml) {}

    // Retorna ['advertiser_id', 'site_id', 'seller_id', 'raw', 'url', 'status']
    public function discoverAdvertiser(\App\Models\Company $company): array;

    // Retorna ['count', 'results', 'raw_first_page', 'endpoints_tried']
    public function listCampaigns(\App\Models\Company $company, int $advertiserId, string $dateFrom, string $dateTo): array;

    // Retorna ['count', 'results', 'raw_first_page', 'endpoints_tried']
    public function listAds(\App\Models\Company $company, int $advertiserId, string $dateFrom, string $dateTo): array;

    // Retorna ['ok' => bool, 'data' => array|null, 'error' => ['message','class']|null]
    public function tryFetchAdsMetrics(\App\Models\Company $company, int $advertiserId, string $dateFrom, string $dateTo): array;
}
```

<!-- Contrato normalizado §2.3 do plano-migracao-sugadores-ml-direto.md (campos a validar no relatório) -->
Campos do contrato normalizado para comparar com payload ML real:
- adgroup_id, adgroup_name, campaign_id, thumbnail, adgroup_type, catalog_listing
- mlb_id, mlb_titulo
- investment (= cost), revenue (= total_amount), sold_quantity (= units_quantity)
- clicks, impressions (= prints), cpc, ctr, acos, roas
- organic_amount, organic_units (podem não existir — null aceitável)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Testes Feature do comando smoke (RED)</name>
  <files>tests/Feature/Phase38/MlSmokeCommandTest.php</files>
  <read_first>
    - .planning/phases/38-smoke-ml-piloto-bymobile/38-CONTEXT.md (seção "Testes" + "Fixture JSON" + "Relatório CLI") — assertions exigidas
    - tests/Feature/Phase38/MercadoLivreAdsServiceTest.php (criado no Plan 01) — padrão de Http::fake já estabelecido nesta phase
    - app/Services/Sugadores/MercadoLivreAdsService.php (criado no Plan 01) — interfaces a invocar indiretamente via comando
    - app/Console/Commands/SyncAdmanData.php — padrão de comando Artisan no projeto (estilo de output, exit codes, options parsing)
    - app/Services/MercadoLivreService.php (linhas 271-317 do `get()`) — entender que `Http::fake` no nível raiz captura TODAS as chamadas downstream (fake mock no comando captura também as chamadas que o service do Plan 01 dispara)
  </read_first>
  <behavior>
    Suite com 4 tests cobrindo o comando que será criado na Tarefa 2:

    - **Test 1 — `command_fails_when_company_id_does_not_exist`**:
      `$this->artisan('sugadores:ml-smoke', ['--company' => 999999, '--days' => 30])`
        ->expectsOutputToContain('Empresa não encontrada')
        ->assertExitCode(1);

    - **Test 2 — `command_fails_when_company_has_no_ml_token`**:
      Cria Company sem MlToken;
      `$this->artisan('sugadores:ml-smoke', ['--company' => $company->id])`
        ->expectsOutputToContain('sem token ML') ou similar
        ->assertExitCode(1);

    - **Test 3 — `command_writes_fixture_with_expected_shape_when_all_endpoints_succeed`**:
      Cria Company + MlToken ativo;
      `Http::fake` para os 3+ endpoints: `/advertising/advertisers` (advertiser_id=71098), `/marketplace/advertising/MLB/advertisers/71098/product_ads/campaigns/search` (2 campanhas), `/advertising/advertisers/71098/product_ads/items` (3 ads com métricas);
      Roda comando; `assertExitCode(0)`;
      Lê arquivo `storage/app/sugadores/ml-smoke/{id}-{date}.json`;
      Assert: arquivo existe (`Storage::disk('local')->exists(...)`) E contém chaves obrigatórias: `company_id`, `company_name`, `run_at`, `days_window`, `advertiser`, `campaigns`, `ads`, `metrics`, `report`;
      Assert: `report.endpoints_ok` é array não-vazio;
      Assert: `report.contract_fields_present` contém pelo menos `clicks`, `impressions`, `cost`, `total_amount`;
      Assert: `advertiser.advertiser_id === 71098`;
      Assert: `campaigns.count === 2`;
      Assert: fixture **NÃO contém** o access_token (`assertStringNotContainsString('fake-token', $fixtureContent)`).

    - **Test 4 — `command_report_prints_required_sections_when_endpoint_fails`**:
      `Http::fake` configurado para `/advertising/advertisers` retornar OK mas `/product_ads/items` retornar 404;
      Roda comando;
      `expectsOutputToContain('endpoints_failed')` (ou seção equivalente);
      `expectsOutputToContain('blockers')` ou `expectsOutputToContain('Blockers para Phase 39')`;
      Comando termina com exit code 0 (smoke é diagnóstico — endpoint falhado é DADO, não erro de execução; só erros fatais terminam != 0).

    Setup: `Http::preventStrayRequests()` + `Storage::fake('local')` no `setUp()` para isolar I/O e rede.
  </behavior>
  <action>
    Crie `tests/Feature/Phase38/MlSmokeCommandTest.php` com:
    - namespace `Tests\Feature\Phase38`;
    - uses `Tests\TestCase`, `RefreshDatabase`, `Http`, `Storage`, `App\Models\Company`, `App\Models\MlToken`;
    - `setUp()`: chama `parent::setUp()`, `Http::preventStrayRequests()`, `Storage::fake('local')` (isola gravação da fixture);
    - 4 testes conforme `<behavior>` acima;
    - Helper `private function makeBymobileLike(): Company` cria Company `name='ByMobille - Teste'` + MlToken ativo (`access_token='fake-token-xyz'`, `refresh_token='fake-refresh'`, `expires_at=now()->addHour()`, `status='active'`, `ml_user_id='465723451'`);
    - Helper `private function fakeHappyPath(int $advertiserId = 71098): void` configura `Http::fake([...])` com payloads realistas (campos: `id`, `name`, `status`, `metrics` → `cost`, `clicks`, `prints`, `total_amount`, `acos`, `roas`, `cpc`, `units_quantity`).

    Comentários em pt-BR.

    Rode `php artisan test --filter=MlSmokeCommandTest` — DEVE falhar com `Command "sugadores:ml-smoke" is not defined` (RED esperado).
  </action>
  <verify>
    <automated>php artisan test --filter=MlSmokeCommandTest 2>&1 | grep -E "(is not defined|Error|Errors|Command.*sugadores:ml-smoke)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Phase38/MlSmokeCommandTest.php` existe.
    - `grep -c "function test_" tests/Feature/Phase38/MlSmokeCommandTest.php` == 4.
    - `grep -c "sugadores:ml-smoke" tests/Feature/Phase38/MlSmokeCommandTest.php` ≥ 4 (cada test invoca o comando).
    - `grep -c "Storage::fake" tests/Feature/Phase38/MlSmokeCommandTest.php` ≥ 1.
    - `grep -c "Http::preventStrayRequests" tests/Feature/Phase38/MlSmokeCommandTest.php` ≥ 1.
    - `grep -c "assertStringNotContainsString.*fake-token" tests/Feature/Phase38/MlSmokeCommandTest.php` ≥ 1 (assert anti-token-leak).
    - `php artisan test --filter=MlSmokeCommandTest` exit code != 0 com mensagem "is not defined" (RED esperado).
  </acceptance_criteria>
  <done>
    4 tests Feature no RED apontando para comando inexistente.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar comando SugadoresMlSmoke (GREEN)</name>
  <files>app/Console/Commands/SugadoresMlSmoke.php</files>
  <read_first>
    - app/Console/Commands/SyncAdmanData.php — copiar estilo de signature, options parsing, exit codes, output do projeto
    - app/Services/Sugadores/MercadoLivreAdsService.php (criado no Plan 01) — interfaces a invocar
    - tests/Feature/Phase38/MlSmokeCommandTest.php (criado na Tarefa 1) — spec de output e fixture
    - .planning/phases/38-smoke-ml-piloto-bymobile/38-CONTEXT.md (seção "Fixture JSON" + "Relatório CLI" + "Tratamento de erros") — formato exato
    - plano-migracao-sugadores-ml-direto.md §2.3 — lista de campos do contrato normalizado para comparar
    - CLAUDE.md — convenções pt-BR + PascalCase verb-noun
  </read_first>
  <action>
    Crie `app/Console/Commands/SugadoresMlSmoke.php` com:

    1. **Namespace e imports:**
       - `namespace App\Console\Commands;`
       - `use App\Models\Company; use App\Services\Sugadores\MercadoLivreAdsService; use Carbon\Carbon; use Illuminate\Console\Command; use Illuminate\Support\Facades\Log;`

    2. **Signature exato:**
       ```
       protected $signature = 'sugadores:ml-smoke
           {--company= : ID numerico da empresa (default: Bymobille - Teste)}
           {--days=30 : Janela em dias para metricas (clamp 1-90)}';
       ```

    3. **Description:**
       ```
       protected $description = 'Smoke test da API oficial ML Mercado Ads para validar shape de payload antes da migracao Sugadores Adman->ML (Fase 0 da Milestone v11.0)';
       ```

    4. **handle(MercadoLivreAdsService $ads): int** — fluxo:

       **a) Resolver empresa:**
       - `$companyId = (int) $this->option('company');`
       - Se `$companyId === 0`: tentar resolver Bymobille via `Company::whereHas('mlToken', fn($q) => $q->where('status','active'))->where('name','like','%Bymobille%')->orWhere('name','like','%ByMobille%')->first();` — se NULL ou múltiplos, falhar com `$this->error('Empresa nao encontrada — passe --company={id} explicitamente.')` e `return self::FAILURE;`
       - Senão: `$company = Company::with('mlToken')->find($companyId);` — se NULL, `$this->error("Empresa nao encontrada (id={$companyId}).")` e `return self::FAILURE;`
       - Se `!$company->mlToken || $company->mlToken->status !== 'active'`: `$this->error("Empresa {$company->id} ({$company->name}) sem token ML ativo — execute OAuth ML primeiro.")` e `return self::FAILURE;`

       **b) Clamp --days em [1, 90]:**
       - `$days = max(1, min(90, (int) $this->option('days')));`
       - `$dateTo = now()->toDateString(); $dateFrom = now()->subDays($days)->toDateString();`

       **c) Inicializar estruturas de relatório:**
       ```
       $endpointsOk = [];
       $endpointsFailed = [];
       $contractFields = [
           // chave_no_contrato => nome_no_ml (referência §2.3 do plano)
           'investment' => 'cost', 'revenue' => 'total_amount', 'sold_quantity' => 'units_quantity',
           'clicks' => 'clicks', 'impressions' => 'prints', 'cpc' => 'cpc', 'ctr' => 'ctr',
           'acos' => 'acos', 'roas' => 'roas', 'mlb_id' => 'item_id', 'mlb_titulo' => 'title',
           'thumbnail' => 'thumbnail', 'adgroup_type' => 'type', 'catalog_listing' => 'catalog_listing',
           'organic_amount' => 'organic_amount', 'organic_units' => 'organic_units',
       ];
       $present = []; $missing = []; $blockers = [];
       ```

       **d) Etapa 1: Descobrir advertiser** (com try/catch para `\RuntimeException`):
       - Chamar `$advertiser = $ads->discoverAdvertiser($company);`
       - Em sucesso: `$endpointsOk[] = $advertiser['url'];` log no console `$this->info("OK Advertiser: id={$advertiser['advertiser_id']}, site={$advertiser['site_id']}, seller={$advertiser['seller_id']}");`
       - Se `$advertiser['advertiser_id'] === null`: `$blockers[] = 'Conta sem Mercado Ads (advertisers vazio) — Phase 39 nao pode prosseguir';` `$this->warn('Advertiser vazio — gravando fixture e abortando');` segue para gravar fixture parcial; retorna `self::SUCCESS` no fim (smoke é diagnóstico).
       - Em `\RuntimeException`: `$endpointsFailed[] = ['endpoint' => '/advertising/advertisers', 'error' => $e->getMessage()];` `$blockers[] = 'Discovery falhou: ' . $e->getMessage();` $this->error(...); $advertiser = null; segue para fixture parcial.

       **e) Etapa 2: Listar campanhas** (só se advertiser_id != null):
       - Try/catch igual etapa 1.
       - Em sucesso: registra endpoints_tried do retorno em `$endpointsOk`; coleta `$campaigns` (estrutura completa); inspeciona `$campaigns['raw_first_page']['results'][0]['metrics'] ?? []` para checar campos presentes do contrato.

       **f) Etapa 3: Listar ads/items** + **Etapa 4: Métricas** (via `$ads->tryFetchAdsMetrics()` que já encapsula try/catch):
       - Se `$result['ok'] === true`: registra ads em `$endpointsOk`; inspeciona shape do primeiro item para `$present`/`$missing`.
       - Se `$result['ok'] === false`: `$endpointsFailed[] = ['endpoint' => 'product_ads/items', 'error' => $result['error']['message']]; $blockers[] = 'Endpoint ads/items falhou — confirmar nome correto via doc oficial: ' . $result['error']['message'];`

       **g) Calcular `$present`/`$missing`:**
       - Coleta um sample shape — preferir primeiro item de ads, fallback campanhas (métricas no nível item são mais ricas).
       - `foreach ($contractFields as $contractKey => $mlKey)`: se `array_key_exists($mlKey, $sampleMetricsArray) || array_key_exists($mlKey, $sampleTopLevelArray)`: `$present[] = $contractKey;` senão `$missing[] = $contractKey;`

       **h) Montar fixture array:**
       ```
       $fixture = [
           'company_id' => $company->id,
           'company_name' => $company->name,
           'run_at' => now()->toIso8601String(),
           'days_window' => $days,
           'date_from' => $dateFrom,
           'date_to' => $dateTo,
           'advertiser' => $advertiser,  // pode ser null
           'campaigns' => $campaigns ?? null,
           'ads' => $ads_result ?? null,
           'metrics' => $metrics_result ?? null,
           'report' => [
               'endpoints_ok' => $endpointsOk,
               'endpoints_failed' => $endpointsFailed,
               'contract_fields_present' => $present,
               'contract_fields_missing' => $missing,
               'blockers' => $blockers,
           ],
       ];
       ```

       **i) Gravar fixture:**
       - `$filename = "{$company->id}-" . now()->toDateString() . '.json';`
       - `$relativePath = 'sugadores/ml-smoke/' . $filename;`
       - `\Storage::disk('local')->put($relativePath, json_encode($fixture, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES));`
       - `$this->info("Fixture gravada em storage/app/{$relativePath}");`

       **j) Imprimir relatório CLI** (formato Markdown legível conforme CONTEXT.md "Relatório CLI"):
       ```
       $this->newLine();
       $this->line('## Smoke ML — ' . $company->name . ' (company_id=' . $company->id . ', ' . $days . 'd)');
       $this->newLine();
       $this->line('### endpoints_ok (' . count($endpointsOk) . ')');
       foreach ($endpointsOk as $url) { $this->line('  - ' . $url); }
       $this->newLine();
       $this->line('### endpoints_failed (' . count($endpointsFailed) . ')');
       foreach ($endpointsFailed as $f) { $this->line('  - ' . $f['endpoint'] . ': ' . $f['error']); }
       $this->newLine();
       $this->line('### contract_fields_present (' . count($present) . ')');
       $this->line('  ' . implode(', ', $present));
       $this->newLine();
       $this->line('### contract_fields_missing (' . count($missing) . ')');
       $this->line('  ' . implode(', ', $missing));
       $this->newLine();
       $this->line('### blockers (' . count($blockers) . ')');
       if (empty($blockers)) {
           $this->info('  (vazio) — pronto para Phase 39');
       } else {
           foreach ($blockers as $b) { $this->error('  - ' . $b); }
       }
       $this->newLine();
       $this->warn('Fixture pode conter dados reais de cliente — revisar antes de compartilhar fora da equipe.');
       ```

       **k) Return:** `return self::SUCCESS;` (smoke nunca termina != 0 a menos que falha fatal de resolução de empresa/token — etapas individuais falhadas viram blockers, NÃO exit code).

    5. **Sem logar token:** o código NUNCA acessa `$company->mlToken->access_token` diretamente — toda a interação HTTP passa pelo `MercadoLivreAdsService` que por sua vez delega ao `MercadoLivreService` (Phase 20). Acceptance criteria valida via grep.

    6. **Comentários PHPDoc em pt-BR** no `handle()` explicando o fluxo das 4 etapas + nota de "smoke é diagnóstico, exit code só falha em problemas de resolução de empresa/token".

    Rode `php artisan test --filter=MlSmokeCommandTest` — DEVE passar 4/4 (GREEN).
    Rode `php artisan list | grep sugadores:ml-smoke` — DEVE retornar a linha do comando.
  </action>
  <verify>
    <automated>php artisan test --filter=MlSmokeCommandTest 2>&1 | tail -3 && php artisan list 2>&1 | grep -c "sugadores:ml-smoke"</automated>
  </verify>
  <acceptance_criteria>
    - `app/Console/Commands/SugadoresMlSmoke.php` existe.
    - `grep -c "class SugadoresMlSmoke extends Command" app/Console/Commands/SugadoresMlSmoke.php` == 1.
    - Signature exato: `grep -c "sugadores:ml-smoke" app/Console/Commands/SugadoresMlSmoke.php` ≥ 1; `grep -c "company=" app/Console/Commands/SugadoresMlSmoke.php` ≥ 1; `grep -c "days=30" app/Console/Commands/SugadoresMlSmoke.php` ≥ 1.
    - Construtor injection: `grep -c "MercadoLivreAdsService" app/Console/Commands/SugadoresMlSmoke.php` ≥ 2 (use + parâmetro).
    - Grava fixture: `grep -c "sugadores/ml-smoke" app/Console/Commands/SugadoresMlSmoke.php` ≥ 1.
    - Não loga token: `grep -ciE "access_token|refresh_token" app/Console/Commands/SugadoresMlSmoke.php` == 0.
    - Não toca `routes/console.php`: `git diff --name-only HEAD routes/console.php 2>&1` retorna vazio.
    - Comando registrado: `php artisan list 2>&1 | grep -c "sugadores:ml-smoke"` ≥ 1.
    - `php artisan test --filter=MlSmokeCommandTest` exit code 0 com "OK (4 tests" ou "Tests: 4 passed".
    - Regressão Phase 38: `php artisan test --filter=Phase38` exit code 0 com pelo menos 8 tests verdes (4 do Plan 01 + 4 do Plan 02).
    - Zero toque em arquivos de prod do Sugadores: `git diff --name-only HEAD app/Services/SugadorAnalysisService.php app/Http/Controllers/SugadorController.php app/Jobs/AnalyzeCompanySugadoresJob.php app/Jobs/FetchAdmanMlbsByCampaignJob.php database/migrations/ 2>&1 | grep -v '^$'` retorna vazio.
  </acceptance_criteria>
  <done>
    Comando funcional + 4/4 tests verdes + zero regressão Phase 20/30 + zero toque em prod do Sugadores. Pronto para smoke real (Tarefa 3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 3: Smoke real Bymobille + revisão do relatório (CHECKPOINT)</name>
  <what-built>
    Comando `sugadores:ml-smoke --company={id_bymobille} --days=30` que:
    - Resolve mlToken ativo do Bymobille
    - Chama API ML real (advertiser → campanhas → ads → métricas)
    - Grava fixture em `storage/app/sugadores/ml-smoke/{id}-{YYYY-MM-DD}.json`
    - Imprime relatório CLI com `endpoints_ok`, `endpoints_failed`, `contract_fields_present`, `contract_fields_missing`, `blockers`
  </what-built>
  <how-to-verify>
    **Passo 0 — Confirmar o `company_id` do Bymobille (uma vez):**
    ```bash
    php artisan tinker --execute='dump(\App\Models\Company::where("name","like","%Bymobille%")->whereHas("mlToken", fn($q) => $q->where("status","active"))->get(["id","name"])->toArray());'
    ```
    Anote o `id` retornado (o usuário sabe qual empresa é o piloto).

    **Passo 1 — Rodar smoke real no XAMPP local (não no VPS):**
    ```bash
    php artisan sugadores:ml-smoke --company={id_anotado} --days=30
    ```

    **Passo 2 — Verificar o output do console contém:**
    - [ ] Linha `OK Advertiser: id=..., site=MLB, seller=...` (ou mensagem clara de "advertiser vazio"/"401"/"403"/"403 scope ausente")
    - [ ] Seção `### endpoints_ok (N)` com pelo menos 1 URL
    - [ ] Seção `### endpoints_failed (N)` — pode estar vazia OU com URLs+erros explicados
    - [ ] Seção `### contract_fields_present (N)` listando campos (ex: `clicks, impressions, cost, total_amount, acos, roas, cpc`)
    - [ ] Seção `### contract_fields_missing (N)` listando ausentes (ex: `organic_amount, organic_units` — esperado serem `null` segundo §2.3 do plano)
    - [ ] Seção `### blockers (N)` — vazia (pronto p/ Phase 39) OU com lista clara de issues

    **Passo 3 — Verificar fixture gravada:**
    ```bash
    ls -la storage/app/sugadores/ml-smoke/
    cat storage/app/sugadores/ml-smoke/{id}-$(date +%Y-%m-%d).json | head -50
    ```
    - [ ] Arquivo existe com nome `{company_id}-YYYY-MM-DD.json`
    - [ ] JSON válido (pode testar com `php -r "json_decode(file_get_contents('storage/app/sugadores/ml-smoke/{id}-...json')); var_dump(json_last_error_msg());"` — deve imprimir `'No error'`)
    - [ ] Contém as chaves: `company_id`, `company_name`, `run_at`, `days_window`, `advertiser`, `campaigns`, `ads`, `metrics`, `report`
    - [ ] **NÃO contém** o `access_token` em texto puro (sanity check: `grep -ic "access_token" storage/app/sugadores/ml-smoke/*.json` deve ser 0)

    **Passo 4 — Decisão do operador:**
    Com base no relatório, responder ao agente:
    - **APROVADO PARA PHASE 39** — campos suficientes do contrato §2.3 estão presentes; blockers vazios ou triviais
    - **AJUSTAR ENDPOINTS** — algum endpoint candidato tem nome errado na doc oficial; informar URL correta e re-rodar
    - **PROBLEMA DE PERMISSÃO** — token Bymobille sem scope ML Ads; precisa reautenticar OAuth antes de prosseguir
    - **OUTRA QUESTÃO** — descrever
  </how-to-verify>
  <resume-signal>
    Digite uma das opções:
    - "aprovado" — Phase 39 pode começar; SUMMARY.md vai registrar smoke verde + caminho da fixture
    - "ajustar: {detalhe}" — vou ajustar URLs/endpoints no MercadoLivreAdsService e pedir re-execução
    - "permissão" — vou registrar como blocker no SUMMARY e pausar v11.0 até OAuth reautenticado
    - "outra: {descrição}" — vou avaliar e propor próximo passo
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Console operator → CLI | Operador local do XAMPP roda comando Artisan — trust alto (mesmo host) |
| CLI → API ML | Comando dispara chamadas HTTPS para `api.mercadolibre.com` com `access_token` do Bymobille via Bearer header (delegado ao MercadoLivreService Phase 20) |
| CLI → filesystem local | Comando grava fixture JSON em `storage/app/sugadores/ml-smoke/` — pode conter PII (nomes de cliente final, títulos de produto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Information Disclosure | Fixture JSON gravada em disco | mitigate | Comando avisa explicitamente no console "Fixture pode conter dados reais de cliente — revisar antes de compartilhar fora da equipe". Diretório `storage/app/sugadores/ml-smoke/` fica fora do `public/` (não acessível via HTTP). Acceptance criteria valida que `access_token` nunca aparece na fixture. |
| T-38-06 | Information Disclosure | `access_token` em log/console | mitigate | Comando NUNCA acessa `$company->mlToken->access_token` diretamente — só via `MercadoLivreAdsService → MercadoLivreService::get()` que mantém o token no header HTTP, fora de logs. Acceptance criteria valida via `grep -ciE "access_token|refresh_token" == 0`. |
| T-38-07 | Tampering | Input `--company` malicioso (SQL injection) | mitigate | Eloquent `Company::find((int) $companyId)` faz cast explícito para int + parametrized query. Sem string concat em SQL. |
| T-38-08 | Denial of Service | `--days` muito alto causa loop de paginação infinita | mitigate | Clamp `max(1, min(90, $days))`. Service do Plan 01 já tem teto de offset 500/2000. |
| T-38-09 | Spoofing | Operador roda contra empresa errada | mitigate | Comando exige `--company={id}` explícito ou resolve Bymobille por nome ESTRITO; se ambíguo, falha e pede ID explícito. Output sempre imprime `company_id` e `company_name` antes de chamar API. |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Nenhum package install nesta phase — só código PHP nativo + Laravel facades já em uso. |

**Severidade global:** LOW (comando dev-only, roda manual no XAMPP local, não expõe rota HTTP, não persiste em DB de produção do Sugadores). Maior risco real é vazamento da fixture — mitigado por aviso no console + diretório fora de `public/`.
</threat_model>

<verification>
- Suite `tests/Feature/Phase38/MlSmokeCommandTest.php` 4/4 verde (automatizado).
- Regressão: `php artisan test --filter=Phase38` 8/8 verde (combinado com Plan 01).
- Regressão Phase 20: `php artisan test --filter=MercadoLivre` sem novos vermelhos.
- `php artisan list | grep sugadores:ml-smoke` retorna a linha do comando.
- Smoke real (Tarefa 3): operador valida output e fixture, responde via resume-signal.
- Grep de zero-toque em arquivos de prod Sugadores: vazio.
- `routes/console.php` intocado (smoke é manual, não agendado).
</verification>

<success_criteria>
- Comando `sugadores:ml-smoke` registrado, testado e operacional contra `Http::fake`.
- Fixture JSON gravada com shape correto, sem leak de token.
- Smoke real contra API ML executado pelo operador com relatório legível.
- Decisão humana registrada: aprovado para Phase 39 OU ajustes pendentes.
- Zero toque em qualquer arquivo de produção do módulo Sugadores.
</success_criteria>

<output>
Criar `.planning/phases/38-smoke-ml-piloto-bymobile/38-02-SUMMARY.md` ao concluir, registrando:
- Signature final do comando + opções
- Resultados do smoke real: lista de `endpoints_ok` / `endpoints_failed` reportada pelo operador
- Caminho da fixture gerada (operador anota para reuso na Phase 39 como fixture de teste)
- Decisão do operador (aprovado / ajustar / pausar) e justificativa
- 4 tests Feature adicionados + comando regressão Phase 20
- Blockers para Phase 39 (se houver)
- Confirmação de zero toque em arquivos de prod do Sugadores
</output>
