---
phase: 124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst
plan: 05
type: execute
wave: 3
depends_on: ["124-03", "124-04"]
files_modified:
  - app/Http/Controllers/ComercialController.php
  - app/Http/Controllers/Api/HubspotWebhookController.php
  - tests/Feature/Phase124KillSwitchTest.php
  - .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-depois-05.txt
  - .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/rede-ampla-05.txt
autonomous: true
requirements: [FLUXO-04, FLUXO-05, FLUXO-06, FLUXO-07, REDE-01]

must_haves:
  truths:
    - "Nao existe mais codigo de criacao de MlbEmpresa dentro de ComercialController nem de HubspotWebhookController"
    - "Uma empresa criada pelo HubSpot ou pelo Comercial continua sendo roteada ao operacional na mesma hora"
    - "O cadastro manual continua gravando gmail_colaborador na implementacao de Polos"
    - "Empresa que ja tem MlbEmpresa continua protegida, inclusive no replay de evento antigo"
    - "Com o interruptor ligado, nenhum dos dois caminhos de entrada cria ficha de operacao — a fiacao esta provada ponta a ponta"
    - "O diff nominal de testes entre antes e depois da refatoracao e vazio"
  artifacts:
    - path: "app/Http/Controllers/ComercialController.php"
      provides: "store() delegando o roteamento ao router; wrapper criarImplementacaoPolo removido (D-03)"
    - path: "app/Http/Controllers/Api/HubspotWebhookController.php"
      provides: "criarEmpresa() delegando ao router; rotearImplementacao() removido"
    - path: ".planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-depois-05.txt"
      provides: "Lista nominal de testes DEPOIS da refatoracao completa"
      min_lines: 30
  key_links:
    - from: "app/Http/Controllers/ComercialController.php"
      to: "App\\Services\\Operacional\\EmpresaOperacionalRouter"
      via: "rotearCadastro() dentro da DB::transaction do store()"
      pattern: "rotearCadastro"
    - from: "app/Http/Controllers/Api/HubspotWebhookController.php"
      to: "App\\Services\\Operacional\\EmpresaOperacionalRouter"
      via: "rotearServico() no laco por servico criado"
      pattern: "rotearServico"
    - from: "tests/Feature/Phase124KillSwitchTest.php"
      to: "POST /comercial/empresas e POST /api/webhooks/hubspot"
      via: "prova ponta a ponta de que o interruptor ligado para os dois caminhos"
      pattern: "administrativo_bloqueio_ativo"
---

<objective>
Religar os dois caminhos de entrada ao `EmpresaOperacionalRouter` e apagar o código duplicado que
ficou para trás — fechando FLUXO-04 de verdade.

Propósito: enquanto o código inline continuar existindo nos controllers, a duplicação continua
existindo e as Fases 128/133 teriam dois lugares para tocar. Este é o plano onde o caminho de
produção realmente muda de forma — e por isso é o mais delicado da fase.

Saída: `ComercialController::store()` e `HubspotWebhookController::criarEmpresa()` delegando ao
router; `ComercialController::criarImplementacaoPolo()` e
`HubspotWebhookController::rotearImplementacao()` removidos; a fiação do interruptor provada ponta a
ponta pelos dois caminhos; diff nominal vazio contra `baseline-antes.txt`.

**Refatoração pura.** Se qualquer teste do gate mudar de lado, o plano PARA — não se "atualiza o
teste".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/124-CONTEXT.md
@.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/124-RESEARCH.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real — não explorar o codebase. -->

API do router criado no plano 124-04 (`App\Services\Operacional\EmpresaOperacionalRouter`):
- `public const CHAVE_BLOQUEIO = 'administrativo_bloqueio_ativo';`
- `bloqueioAtivo(): bool`
- `rotearServico(Company $company, string $nomeServico, array $handoff = []): void`
  → mecânica do HubSpot: um nome por chamada, COM guard `MlbEmpresa::exists()`
- `rotearCadastro(Company $company, iterable $nomesServicos, array $handoff = []): void`
  → mecânica do Comercial: lista de nomes, dedup por tipo, SEM guard entre iterações (D-08)

Substituição no caminho Comercial (`ComercialController::store()`, dentro da `DB::transaction`
aberta na linha 643) — o bloco das linhas 683-715 vira:
```
$router->rotearCadastro($company, $servicosCriados->pluck('nome'), $validated);
```
(`$servicosCriados` é uma `Collection` de models `Servico`; o router recebe NOMES.)

Substituição no caminho HubSpot (`HubspotWebhookController::criarEmpresa()`, dentro da
`DB::transaction` aberta na linha 513) — as linhas 649-651 viram:
```
foreach ($servicosCriados as $nomeServico) {
    $router->rotearServico($company, $nomeServico);
}
```
O laço permanece: `$servicosCriados` já é uma lista de nomes, e chamar uma vez por nome é justamente
o que preserva o guard entre iterações.

Padrão de resolução do service já usado neste arquivo (linha 635):
`app(HubspotDealHandoffService::class)` → usar `app(EmpresaOperacionalRouter::class)` nos dois
controllers, por consistência.

**FORA DE ESCOPO, não tocar:**
- `MlbImplementacaoController::criarImplementacaoPolo()` (linha ~1092) — homônimo, terceira cópia
  independente usada pelo Onboarding manual. Uma busca textual por `criarImplementacaoPolo` vai
  encontrá-lo. É dívida técnica pré-existente, não é desta fase.
- `HubspotWebhookController::calcularPendencias()` (linhas 975-997) — outro conceito, fica intacto.
- Qualquer chamada à Clicksign, qualquer desligamento real do roteamento, qualquer regra de isenção
  de Polos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Religar o cadastro manual e remover o wrapper criarImplementacaoPolo</name>

  <read_first>
    - app/Http/Controllers/ComercialController.php — `store()` (linhas 593-748) inteiro, com atenção
      ao bloco 683-715 e ao wrapper `criarImplementacaoPolo()` (linhas 970-981); e o cabeçalho de
      imports (linhas 1-20)
    - app/Services/Operacional/EmpresaOperacionalRouter.php — o service criado no plano 124-04
    - tests/Feature/Phase124RegressaoComercialTest.php — os 5 testes que este plano NÃO pode quebrar
    - tests/Feature/Phase14ComercialTest.php — a baseline viva do caminho manual
  </read_first>

  <files>app/Http/Controllers/ComercialController.php</files>

  <action>
1. Em `store()`, dentro da `DB::transaction` (linha 643), substituir o bloco inteiro de roteamento
   (linhas 683-715, do comentário "(c) Roteamento Phase 13 PRESERVADO" até o fecho do `foreach`) por
   uma única chamada ao router:
   `$router->rotearCadastro($company, $servicosCriados->pluck('nome'), $validated);`

   Resolver o router com `app(EmpresaOperacionalRouter::class)` e incluí-lo no `use (...)` da closure
   da transaction, ou resolvê-lo dentro da própria closure — o que ficar mais legível. Não mudar a
   assinatura pública de `store()`.

   Manter o comentário pt-BR explicando que o roteamento passou a viver em
   `EmpresaOperacionalRouter` (Fase 124, FLUXO-04) e que o `$validated` é o pacote opcional do wizard
   (D-02) de onde a factory lê o `gmail_colaborador`.

2. Apagar o método privado `criarImplementacaoPolo()` (linhas 970-981) — D-03. Ele era um wrapper de
   uma linha sobre `MlbImplementacaoFactory::criarParaPolo()`, e o router já chama a factory direto.

3. Limpar imports que ficaram órfãos. Rodar `grep -n "MlbImplementacao\b" app/Http/Controllers/ComercialController.php`
   e `grep -n "MlbEmpresa" app/Http/Controllers/ComercialController.php` ANTES de remover qualquer
   `use`: `MlbEmpresa` continua sendo usado no guard de duplicata da linha 631, então o import dele
   FICA. Só remover o que o grep provar que não tem mais nenhum uso.

4. Adicionar `use App\Services\Operacional\EmpresaOperacionalRouter;` na ordem dos imports existentes.

5. Não tocar em `servicoDisparaImplementacao()` — continua público e estático, é a fonte única da
   regra e o router o consome.
  </action>

  <acceptance_criteria>
    - `grep -c "MlbEmpresa::create" app/Http/Controllers/ComercialController.php` = 0 (não existe mais
      criação de ficha dentro do controller).
    - `grep -c "criarImplementacaoPolo" app/Http/Controllers/ComercialController.php` = 0 (D-03).
    - `grep -c "rotearCadastro" app/Http/Controllers/ComercialController.php` = 1.
    - `grep -c "public static function servicoDisparaImplementacao" app/Http/Controllers/ComercialController.php` = 1
      (o helper continua existindo e público).
    - `"/c/xampp/php/php.exe" -l app/Http/Controllers/ComercialController.php` → `No syntax errors detected`.
    - `"/c/xampp/php/php.exe" vendor/bin/phpunit tests/Feature/Phase124RegressaoComercialTest.php tests/Feature/Phase14ComercialTest.php`
      → mesmos nomes verdes/vermelhos de `baseline-antes.txt` para esses dois arquivos (a única falha
      admitida continua sendo `test_update_ignora_campos_legacy`), e NENHUM teste foi editado.
    - `git diff --stat tests/` retorna vazio nesta task — nenhum arquivo de teste é alterado nas
      Tasks 1 e 2 (a única extensão de teste da fase acontece na Task 3, em arquivo fora do gate).
  </acceptance_criteria>

  <verify>
    <automated>"/c/xampp/php/php.exe" vendor/bin/phpunit --testdox --do-not-cache-result tests/Feature/Phase124RegressaoComercialTest.php tests/Feature/Phase14ComercialTest.php</automated>
  </verify>

  <done>O cadastro manual roteia via `EmpresaOperacionalRouter`; o wrapper foi removido; os testes de caracterização do caminho Comercial continuam passando sem edição.</done>
</task>

<task type="auto">
  <name>Task 2: Religar o webhook HubSpot e remover rotearImplementacao</name>

  <read_first>
    - app/Http/Controllers/Api/HubspotWebhookController.php — `criarEmpresa()` (linhas 498-717,
      atenção às linhas 644-651), `rotearImplementacao()` (linhas 929-963), `calcularPendencias()`
      (linhas 975-997, que NÃO se mexe) e o cabeçalho de imports
    - app/Services/Operacional/EmpresaOperacionalRouter.php — o service criado no plano 124-04
    - tests/Feature/Phase124RegressaoHubspotTest.php — os 4 testes que este plano NÃO pode quebrar
    - tests/Feature/Phase35HubspotV2Test.php — a baseline viva do caminho webhook
    - .planning/quick/260805-eqk-handoff-hubspot-notes-origem-lead/260805-eqk-SUMMARY.md e
      .planning/quick/260805-ohs-notes-sem-poluicao-hubspot/260805-ohs-SUMMARY.md — os bugs
      corrigidos em 05-06/08/2026 NESTE mesmo caminho; ler antes de tocar no arquivo
  </read_first>

  <files>app/Http/Controllers/Api/HubspotWebhookController.php</files>

  <action>
1. Em `criarEmpresa()`, dentro da `DB::transaction` (linha 513), manter o laço das linhas 649-651 com
   a mesma forma, trocando só a chamada:
   `$router->rotearServico($company, $nomeServico);`
   Resolver o router com `app(EmpresaOperacionalRouter::class)` — mesmo padrão da linha 635.

   Preservar o laço é o ponto crítico: é ele que faz o guard rodar ENTRE serviços, que é o
   comportamento de hoje deste caminho. Não converter para uma chamada única com a lista inteira.

   Manter/adaptar o comentário pt-BR das linhas 646-648 explicando que a mecânica migrou para
   `EmpresaOperacionalRouter` (Fase 124, FLUXO-04) e que o guard anti-duplicidade continua sendo o
   backstop (D-06/D-07, FLUXO-05).

2. Apagar o método privado `rotearImplementacao()` (linhas 929-963) inteiro, docblock incluído — o
   conteúdo dele agora vive no router.

3. Limpar imports órfãos, sempre conferindo por grep antes: rodar
   `grep -n "MlbImplementacaoFactory" app/Http/Controllers/Api/HubspotWebhookController.php`,
   `grep -n "MlbEmpresa" ...` e `grep -n "ComercialController" ...`. Remover apenas os `use` que o
   grep provar sem nenhum uso restante.

4. Adicionar `use App\Services\Operacional\EmpresaOperacionalRouter;` na ordem dos imports.

5. NÃO tocar em `calcularPendencias()` (linhas 975-997), nem em `persistirContratos()`, nem em
   `reprocessarEvento()`, nem no bloco de validação HMAC.
  </action>

  <acceptance_criteria>
    - `grep -c "private function rotearImplementacao" app/Http/Controllers/Api/HubspotWebhookController.php` = 0.
    - `grep -c "MlbEmpresa::create" app/Http/Controllers/Api/HubspotWebhookController.php` = 0.
    - `grep -c "rotearServico" app/Http/Controllers/Api/HubspotWebhookController.php` = 1 e a chamada
      está dentro de um `foreach` sobre `$servicosCriados`.
    - `grep -c "private function calcularPendencias" app/Http/Controllers/Api/HubspotWebhookController.php` = 1
      (o homônimo continua intacto).
    - `"/c/xampp/php/php.exe" -l app/Http/Controllers/Api/HubspotWebhookController.php` → `No syntax errors detected`.
    - `"/c/xampp/php/php.exe" vendor/bin/phpunit tests/Feature/Phase124RegressaoHubspotTest.php tests/Feature/Phase35HubspotV2Test.php`
      → todos verdes, sem nenhuma edição em arquivo de teste.
    - `git diff --stat tests/` retorna vazio nesta task.
    - `git diff --name-only` NÃO contém `app/Http/Controllers/MlbImplementacaoController.php`.
  </acceptance_criteria>

  <verify>
    <automated>"/c/xampp/php/php.exe" vendor/bin/phpunit --testdox --do-not-cache-result tests/Feature/Phase124RegressaoHubspotTest.php tests/Feature/Phase35HubspotV2Test.php</automated>
  </verify>

  <done>O webhook roteia via `EmpresaOperacionalRouter` mantendo o laço por serviço; `rotearImplementacao()` deixou de existir; os testes de caracterização do caminho HubSpot continuam passando sem edição.</done>
</task>

<task type="auto">
  <name>Task 3: Provar a fiação do interruptor ponta a ponta e fechar o gate</name>

  <read_first>
    - .planning/phases/124-.../baseline-antes.txt (gerado no plano 124-03) e o comando exato
      registrado em 124-03-SUMMARY.md
    - .planning/phases/124-.../124-RESEARCH.md § "Q3" — bloco "Rede mais ampla" (os 7 arquivos) e a
      tabela de falhas pré-existentes com os nomes exatos
    - tests/Feature/Phase124KillSwitchTest.php — os 5 testes isolados criados no plano 124-04 (este
      arquivo será estendido; ele NÃO faz parte do gate de 6 arquivos)
    - tests/Feature/Phase124RegressaoComercialTest.php e tests/Feature/Phase124RegressaoHubspotTest.php
      — de onde copiar o POST autenticado e o disparo de webhook com HMAC
    - app/Http/Controllers/ComercialController.php e
      app/Http/Controllers/Api/HubspotWebhookController.php (os arquivos alterados nas Tasks 1 e 2)
  </read_first>

  <files>tests/Feature/Phase124KillSwitchTest.php, .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-depois-05.txt, .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/rede-ampla-05.txt</files>

  <action>
0. Estender `tests/Feature/Phase124KillSwitchTest.php` (criado no plano 124-04) com 2 testes que
   provam a FIAÇÃO — ou seja, que o roteador realmente está no caminho dos dois controllers e que o
   interruptor os alcança:

   - `test_interruptor_ligado_impede_o_cadastro_manual_de_criar_ficha`: `Configuracao::set('administrativo_bloqueio_ativo', '1')`;
     POST `/comercial/empresas` com Polos (copiar o setup de `Phase124RegressaoComercialTest`).
     Asserções: a `Company` É criada normalmente e o contrato também, mas `MlbEmpresa::count() === 0`
     e `MlbImplementacao::count() === 0`. O cadastro comercial não quebra — só o roteamento para.
   - `test_interruptor_ligado_impede_o_webhook_de_criar_ficha`: mesma chave ligada; disparar o webhook
     com um serviço Polos (copiar os helpers de `Phase124RegressaoHubspotTest`). Asserções: resposta
     200, `Company` criada, `MlbEmpresa::count() === 0`, `MlbImplementacao::count() === 0`.

   Estes 2 testes só passam DEPOIS das Tasks 1 e 2 — é comportamento novo e por isso vivem fora do
   gate congelado. Docblock em pt-BR: em produção a chave está `'0'` e nada disso acontece; o valor
   deste teste é ter o mecanismo de emergência provado ANTES de a Fase 133 depender dele.

1. Rodar EXATAMENTE o mesmo comando de gate do plano 124-03 (os 6 arquivos, `--testdox`,
   `--do-not-cache-result`, mesmo filtro de linhas voláteis), gravando em `baseline-depois-05.txt`, e
   comparar:
   `diff .planning/phases/124-.../baseline-antes.txt .planning/phases/124-.../baseline-depois-05.txt`

   Esperado: **diff vazio**. Se algum nome mudar de lado, PARAR e reportar qual teste e qual das duas
   Tasks anteriores é a causa provável — não editar o teste.

2. Rodar UMA VEZ a rede mais ampla (7 arquivos que compartilham a transaction de `criarEmpresa` /
   `persistirContratos` / dedup / replay), gravando em `rede-ampla-05.txt`:
   `tests/Feature/Phase113HubspotDedupTest.php`, `Phase113HubspotEnrichmentTest.php`,
   `Phase114HubspotReplayTest.php`, `Phase114ComercialListagemEnrichmentTest.php`,
   `Phase115HubspotInvariantesTest.php`, `Phase116HubspotReenriquecerHandoffTest.php`,
   `Phase33OnboardingFichaTest.php`.

   Não é exigido 100% verde: a falha `Phase33OnboardingFichaTest::test_padroes_expoem_mensagem_e_grants_padrao`
   (chave `'Serra Gaúcha'` ausente na config de padrões) é pré-existente e não relacionada. O critério
   é: **nenhum teste que passava antes pode ter virado falha**. Se aparecer qualquer outra falha além
   dessa, PARAR e reportar.

3. Registrar no SUMMARY: o comando de gate na íntegra, o resultado do diff, a lista nominal de falhas
   remanescentes e a confirmação de que **nenhum dos 6 arquivos do gate** foi editado durante a
   refatoração (o único arquivo de teste tocado por este plano é `Phase124KillSwitchTest.php`, que
   está fora do gate e ganhou 2 testes NOVOS, sem alterar os 5 existentes).

**Não fazer deploy.** Não rodar `deploy.sh`, `deploy_parcial.sh` nem nenhum comando no VPS.
  </action>

  <acceptance_criteria>
    - `diff baseline-antes.txt baseline-depois-05.txt` retorna saída VAZIA (exit code 0) — é a prova
      formal de "zero mudança de comportamento" desta fase.
    - `rede-ampla-05.txt` existe e a única falha listada é
      `test_padroes_expoem_mensagem_e_grants_padrao`.
    - Os 6 arquivos do gate ficaram intocados:
      `git diff --stat tests/Feature/Phase35HubspotV2Test.php tests/Feature/Phase14ComercialTest.php tests/Feature/Phase37ComercialListagemTest.php tests/Unit/ComercialControllerHelperTest.php tests/Feature/Phase124RegressaoComercialTest.php tests/Feature/Phase124RegressaoHubspotTest.php`
      retorna vazio.
    - `tests/Feature/Phase124KillSwitchTest.php` tem 7 testes (5 do plano 124-04 + 2 de fiação) e
      todos passam; os 5 originais não foram alterados (`git diff` mostra apenas linhas adicionadas).
    - `git status --porcelain` não lista nenhum arquivo fora de: os dois controllers,
      `tests/Feature/Phase124KillSwitchTest.php`, os dois `.txt` do diretório da fase, e o SUMMARY.
    - Nenhum comando de deploy foi executado.
  </acceptance_criteria>

  <verify>
    <automated>diff .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-antes.txt .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-depois-05.txt</automated>
  </verify>

  <done>Interruptor provado ponta a ponta nos dois caminhos, diff nominal vazio contra o baseline capturado antes da refatoração, e rede ampla sem regressão nova — a fase provou que o comportamento observável não mudou e que o mecanismo de emergência responde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provedor externo → `POST /api/webhooks/hubspot` | payload não confiável; validação HMAC v3 + janela de replay ficam INTOCADAS nesta fase |
| navegador → `POST /comercial/empresas` | entrada do formulário; `abort_unless(hasPermission)` + `validate()` ficam INTOCADOS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-13 | Spoofing | validação HMAC do webhook HubSpot | mitigate | o bloco de validação de assinatura não é tocado; `git diff` do controller não pode conter alteração em `hash_equals`, `REPLAY_WINDOW_MS` ou leitura do raw body |
| T-124-14 | Elevation of Privilege | `ComercialController::store()` | mitigate | o `abort_unless(hasPermission('comercial.cadastrar_empresa') \|\| isAdmin())` e o `validate()` permanecem literalmente iguais; a mudança se restringe ao bloco de roteamento |
| T-124-15 | Tampering | duplicação de ficha de operação | mitigate | o guard `MlbEmpresa::where('company_id')->exists()` continua sendo o primeiro passo do caminho HubSpot dentro do router; coberto por `test_hubspot_empresa_que_ja_tem_ficha_nao_ganha_segunda` e `test_reprocess_event_em_empresa_com_ficha_nao_duplica_nem_recria` |
| T-124-16 | Denial of Service | roteamento operacional parar em silêncio | mitigate | a chave `administrativo_bloqueio_ativo` permanece `'0'` em produção e o gate prova por diff nominal que toda empresa continua sendo roteada na hora; quando ligada, o bloqueio é deliberado, registrado em `Log::warning` e comprovado nos dois caminhos por `Phase124KillSwitchTest` |
| T-124-17 | Tampering | nenhum install de pacote | accept | este plano não instala nem atualiza dependência alguma — `composer.json`/`package.json` não são tocados |
</threat_model>

<verification>
- `diff baseline-antes.txt baseline-depois-05.txt` → vazio
- `grep -c "MlbEmpresa::create" app/Http/Controllers/ComercialController.php app/Http/Controllers/Api/HubspotWebhookController.php` → 0 nos dois
- `grep -c "criarImplementacaoPolo" app/Http/Controllers/ComercialController.php` → 0
- `git diff --stat` dos 6 arquivos do gate → vazio (o único teste tocado é `Phase124KillSwitchTest.php`, fora do gate)
- `"/c/xampp/php/php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase124KillSwitchTest.php` → 7 verdes
- `git diff --name-only` NÃO contém `app/Http/Controllers/MlbImplementacaoController.php`
- `git status --porcelain composer.json composer.lock package.json package-lock.json` → vazio
</verification>

<success_criteria>
- FLUXO-04 fechado: existe um único lugar que roteia empresa para a operação, consumido pelos dois caminhos
- FLUXO-05, FLUXO-06 e FLUXO-07 comprovados por teste que passou ANTES e DEPOIS sem edição
- REDE-01 instalado, funcional e provado nos dois caminhos de entrada — desligado por padrão, sem efeito em produção
- Diff nominal vazio contra `baseline-antes.txt` — a prova de refatoração pura
- Nenhum deploy executado
</success_criteria>

<output>
Criar `.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/124-05-SUMMARY.md` ao terminar,
com o comando de gate na íntegra, o resultado do diff e a lista nominal de falhas pré-existentes remanescentes.

Commit (apenas os próprios paths, árvore compartilhada — nunca `git add -A`, nunca `git stash`):
`git commit -- app/Http/Controllers/ComercialController.php app/Http/Controllers/Api/HubspotWebhookController.php tests/Feature/Phase124KillSwitchTest.php .planning/phases/124-*/baseline-depois-05.txt .planning/phases/124-*/rede-ampla-05.txt`
com mensagem em pt-BR
`refactor(124-05): roteamento operacional unificado no EmpresaOperacionalRouter`
</output>
