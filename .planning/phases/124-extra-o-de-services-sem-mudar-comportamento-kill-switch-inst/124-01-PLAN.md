---
phase: 124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Phase124RegressaoComercialTest.php
autonomous: true
requirements: [FLUXO-04, FLUXO-07, REDE-01]

must_haves:
  truths:
    - "Um cadastro manual de Polos com gmail_colaborador preenchido grava esse e-mail em dados.links_admin.gmail_colaborador da MlbImplementacao"
    - "Um cadastro manual de Incubadora cria MlbEmpresa tipo INCUBADORA e nenhuma MlbImplementacao"
    - "Um cadastro manual com Polos + Assessoria na MESMA submissao cria DUAS MlbEmpresa (divergencia D-08, preservada de proposito)"
    - "Com administrativo_bloqueio_ativo desligado ('0', o estado de producao) o cadastro manual roteia exatamente como sempre roteou"
  artifacts:
    - path: "tests/Feature/Phase124RegressaoComercialTest.php"
      provides: "Caracterizacao do caminho Comercial antes da extracao"
      contains: "class Phase124RegressaoComercialTest"
      min_lines: 120
  key_links:
    - from: "tests/Feature/Phase124RegressaoComercialTest.php"
      to: "POST /comercial/empresas"
      via: "actingAs(admin)->post"
      pattern: "post\\('/comercial/empresas'"
    - from: "tests/Feature/Phase124RegressaoComercialTest.php"
      to: "MlbImplementacao.dados.links_admin.gmail_colaborador"
      via: "assercao sobre o array dados"
      pattern: "links_admin"
---

<objective>
Congelar, em teste automatizado, o comportamento observável de HOJE do caminho de cadastro manual
(`ComercialController::store()`) — antes de qualquer refatoração acontecer.

Propósito: a Fase 124 é refatoração pura. A única forma de provar "zero mudança de comportamento" é
ter testes escritos ANTES, que passem SEM ALTERAÇÃO depois. Se um teste deste arquivo precisar mudar
para continuar verde, o comportamento mudou — e isso reprova a fase.

Saída: `tests/Feature/Phase124RegressaoComercialTest.php` cobrindo os 3 gaps confirmados pela pesquisa
no caminho Comercial (`gmail_colaborador` na criação, Incubadora ponta a ponta, multi-tipo na mesma
submissão) mais a prova de que, com o interruptor DESLIGADO (o estado de produção), o roteamento
é exatamente o de hoje.

**Este plano NÃO toca em nenhum arquivo de produção.** Nada em `app/` é criado ou modificado aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/124-CONTEXT.md
@.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/124-RESEARCH.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real — não explorar o codebase. -->

`ComercialController::servicoDisparaImplementacao(string $nome): ?string` (estático, público)
- `str_contains($nome, 'Polos')` → `'polos'`
- `str_contains($nome, 'Assessoria')` → `'assessoria'`
- `str_contains($nome, 'Incubadora')` → `'incubadora'`
- qualquer outro → `null` (case-sensitive de propósito)

`ComercialController::store()` — bloco de roteamento de HOJE (linhas 683-715), dentro da
`DB::transaction` aberta na linha 643:
- deriva `$tiposImplementacao` = `$servicosCriados->map(servicoDisparaImplementacao)->filter()->unique()->values()`
- percorre a lista **sem nenhum guard entre iterações**
- `polos` → `MlbEmpresa::create(nome, tipo='POLO', projeto='POLOS', company_id)` + `criarImplementacaoPolo($mlbEmp, $validated)`
- `assessoria` → `MlbEmpresa::create(nome, tipo='ASSESSORIA', company_id)` (sem `projeto`)
- `incubadora` → `MlbEmpresa::create(nome, tipo='INCUBADORA', company_id)` (sem `projeto`)
- nenhum dos ramos seta `fase`, `estagio` ou `criado_por`

`MlbImplementacaoFactory::criarParaPolo(MlbEmpresa $empresa, array $handoff = []): MlbImplementacao`
- lê `$handoff['gmail_colaborador']` e, **só se não-vazio**, sobrescreve
  `$dados['links_admin']['gmail_colaborador']`
- persiste `MlbImplementacao::create(['empresa_id', 'token' => Str::random(48), 'dados'])`

`App\Models\Configuracao` (chave/valor):
- `Configuracao::get(string $chave, $default = null): mixed`
- `Configuracao::set(string $chave, $valor): void`
- convenção de booleano do projeto: string `'1'` / `'0'` (nunca `true`/`false` nativo)
- a chave desta milestone é `administrativo_bloqueio_ativo`, default desligado
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Caracterizar gmail_colaborador e Incubadora no cadastro manual</name>

  <read_first>
    - tests/Feature/Phase14ComercialTest.php — LEITURA COMPLETA. É o molde: `setUp()` com
      `Servico::firstOrCreate` dos 6 serviços canônicos, helpers `userAdmin()` e `servico()`,
      e o formato exato do POST para `/comercial/empresas`. Copiar essa estrutura.
    - app/Http/Controllers/ComercialController.php — método `store()` (linhas 593-748), com atenção
      às regras de validação (linhas 600-627, onde `gmail_colaborador` é aceito) e ao bloco de
      roteamento (683-715)
    - app/Services/MlbImplementacaoFactory.php — leitura completa (61 linhas)
    - app/Models/MlbImplementacao.php — o método `dadosPadrao()`, para saber qual é o valor DEFAULT
      de `links_admin.gmail_colaborador` quando o wizard não manda nada
    - database/migrations/2026_04_30_000003_create_mlb_empresas_table.php — os defaults das colunas
      `fase` e `estagio` (a caracterização precisa travá-los explicitamente)
    - .planning/phases/124-.../124-RESEARCH.md § "Q1 — Desenho dos testes de caracterização"
  </read_first>

  <files>tests/Feature/Phase124RegressaoComercialTest.php</files>

  <action>
Criar `tests/Feature/Phase124RegressaoComercialTest.php`, classe `Phase124RegressaoComercialTest`,
namespace `Tests\Feature`, com `use RefreshDatabase`.

Copiar de `Phase14ComercialTest` o `setUp()` (que garante os 6 serviços canônicos via
`Servico::firstOrCreate`) e os helpers privados `userAdmin()` e `servico(string $nome)`. NÃO extrair
trait compartilhada — a convenção do projeto é cada arquivo de teste ter sua própria cópia dos
helpers (confirmado na pesquisa).

Docblock de classe em pt-BR explicando: este arquivo caracteriza o comportamento de HOJE do caminho
`ComercialController::store()` para a Fase 124 (refatoração pura). Se algum destes testes precisar
ser alterado, o comportamento mudou — não "atualizar o teste", investigar a regressão.

Escrever 3 testes:

1. `test_comercial_polos_grava_gmail_colaborador_em_links_admin` (FLUXO-07)
   POST `/comercial/empresas` com `nome`, `servicos` = [Polos] e `gmail_colaborador` =
   `'colaborador@empresateste.com'`. Asserções: sem erros de sessão; existe `MlbEmpresa` tipo `POLO`
   para a company; a `MlbImplementacao` dessa `MlbEmpresa` tem
   `dados['links_admin']['gmail_colaborador'] === 'colaborador@empresateste.com'`.
   Comentário em pt-BR marcando que este comportamento é TRANSITÓRIO (D8 da milestone: o campo sai
   do formulário do Comercial na Fase 131) — este teste morre lá de propósito e isso não é regressão.

2. `test_comercial_polos_sem_gmail_colaborador_mantem_o_padrao`
   Mesmo POST, SEM a chave `gmail_colaborador`. Asserção: o valor de
   `dados['links_admin']['gmail_colaborador']` é exatamente
   `MlbImplementacao::dadosPadrao()['links_admin']['gmail_colaborador']` — ler o default do próprio
   método, nunca hardcodar string literal.

3. `test_comercial_incubadora_cria_mlb_empresa_sem_implementacao`
   POST com `servicos` = [Incubadora]. Asserções: `MlbEmpresa` criada com `tipo === 'INCUBADORA'`;
   `projeto` é `null` (só o ramo Polos seta `projeto`); `fase` é `null`; `estagio` igual ao default
   declarado na migration `2026_04_30_000003_create_mlb_empresas_table.php` (confirmar o literal na
   leitura, não presumir); `MlbImplementacao::count() === 0`.
  </action>

  <acceptance_criteria>
    - O arquivo `tests/Feature/Phase124RegressaoComercialTest.php` existe e declara
      `class Phase124RegressaoComercialTest extends TestCase` com `use RefreshDatabase`.
    - Os 3 testes acima existem com exatamente esses nomes e PASSAM contra o código de produção
      ATUAL, sem nenhuma alteração em `app/`.
    - `git status --porcelain app/` não lista nenhum arquivo (nada de produção foi tocado).
    - A asserção do teste 2 referencia `MlbImplementacao::dadosPadrao()`; `grep -n "dadosPadrao" tests/Feature/Phase124RegressaoComercialTest.php` retorna pelo menos 1 linha.
    - A asserção de `estagio` no teste 3 usa o mesmo literal que a migration declara como default.
  </acceptance_criteria>

  <verify>
    <automated>"/c/xampp/php/php.exe" vendor/bin/phpunit --testdox --do-not-cache-result tests/Feature/Phase124RegressaoComercialTest.php</automated>
  </verify>

  <done>Os 3 testes passam contra o código de produção intocado; nenhum arquivo em `app/` foi modificado.</done>
</task>

<task type="auto">
  <name>Task 2: Caracterizar a divergência D-08 (duas fichas) e o roteamento com o interruptor desligado</name>

  <read_first>
    - tests/Feature/Phase124RegressaoComercialTest.php — o arquivo criado na Task 1 (será estendido)
    - app/Http/Controllers/ComercialController.php — bloco de roteamento linhas 683-715 (o
      `->unique()` na derivação de `$tiposImplementacao` e a AUSÊNCIA de guard no `foreach`)
    - app/Http/Controllers/Api/HubspotWebhookController.php — `rotearImplementacao()` linhas 929-963
      (o guard `MlbEmpresa::where('company_id', $company->id)->exists()` na linha 938, que é
      justamente o que o caminho Comercial NÃO tem)
    - app/Models/Configuracao.php — leitura completa (35 linhas)
    - .planning/REQUIREMENTS-v22.md § "D10 — A divergência de duas fichas de operação é preservada"
    - .planning/phases/124-.../124-RESEARCH.md § "Q4 — Riscos da extração além do CONTEXT"
  </read_first>

  <files>tests/Feature/Phase124RegressaoComercialTest.php</files>

  <action>
Acrescentar 2 testes ao arquivo criado na Task 1.

4. `test_comercial_dois_tipos_no_mesmo_request_cria_duas_mlb_empresa_hoje`
   POST `/comercial/empresas` com `servicos` = [Polos, Assessoria] na MESMA submissão.
   Asserções: `MlbEmpresa::where('company_id', $company->id)->count() === 2`; os `tipo` presentes são
   exatamente `POLO` e `ASSESSORIA`; a `MlbEmpresa` de tipo `POLO` tem `MlbImplementacao` associada e
   a de tipo `ASSESSORIA` não tem.

   Docblock em pt-BR OBRIGATÓRIO, com este teor: este é o comportamento vigente do caminho Comercial,
   registrado de propósito (D-08 do CONTEXT / D10 do REQUIREMENTS-v22). O caminho HubSpot, para o
   mesmo cenário, criaria UMA ficha só, por causa do guard da linha ~938. Aplicar aquele guard
   literalmente ao laço do Comercial faria este teste passar a exigir `count() === 1` — o que É uma
   mudança de comportamento observável e está PROIBIDO na Fase 124. Se alguém precisar mudar esta
   asserção, é decisão consciente de outra fase, não conserto de refactor.

5. `test_roteamento_do_comercial_com_interruptor_desligado_permanece_igual` (REDE-01)
   `Configuracao::set('administrativo_bloqueio_ativo', '0')` ANTES do POST — `'0'` é o estado de
   produção e continuará sendo até a Fase 133. Depois, POST com Polos.
   Asserções idênticas ao caminho feliz: `MlbEmpresa` tipo `POLO` criada E `MlbImplementacao`
   correspondente criada.

   Docblock em pt-BR: este é o teste que prova o Phase Boundary da fase — com o interruptor
   DESLIGADO (o único estado que existe em produção), o roteamento é exatamente o de hoje. Ele passa
   antes da refatoração (nada lê a chave) e precisa continuar passando depois (a chave é lida e está
   desligada). O outro lado do interruptor — chave LIGADA interrompendo o roteamento de verdade — é
   provado por teste isolado no plano 124-04 e não entra neste arquivo, porque é comportamento NOVO
   e este arquivo é o baseline congelado.
  </action>

  <acceptance_criteria>
    - Os 5 testes do arquivo passam contra o código de produção ATUAL (nenhuma linha em `app/` alterada).
    - O teste 4 afirma `count() === 2` — não 1.
    - O docblock do teste 4 cita explicitamente D-08 e a proibição de "consertar" a divergência;
      `grep -c "D-08" tests/Feature/Phase124RegressaoComercialTest.php` retorna ≥ 1.
    - O teste 5 usa a string literal `administrativo_bloqueio_ativo` e o valor `'0'` (string, nunca
      `false`), seguindo a convenção do projeto — e assere que a ficha É criada.
    - `git status --porcelain app/` continua vazio.
  </acceptance_criteria>

  <verify>
    <automated>"/c/xampp/php/php.exe" vendor/bin/phpunit --testdox --do-not-cache-result tests/Feature/Phase124RegressaoComercialTest.php</automated>
  </verify>

  <done>Os 5 testes do arquivo passam; a divergência D-08 e o comportamento com o interruptor desligado estão documentados em teste; nada de produção foi tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `POST /comercial/empresas` | entrada não confiável do formulário do Comercial, já protegida por `abort_unless(hasPermission)` + `validate()` — esta fase não altera nenhum dos dois |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Information Disclosure | `Phase124RegressaoComercialTest` | mitigate | fixtures usam e-mails sintéticos (`colaborador@empresateste.com`); nenhum dado real de cliente entra no repositório |
| T-124-02 | Tampering | nenhum install de pacote | accept | este plano não instala nem atualiza dependência alguma — `composer.json`/`package.json` não são tocados |
</threat_model>

<verification>
- `"/c/xampp/php/php.exe" vendor/bin/phpunit --testdox --do-not-cache-result tests/Feature/Phase124RegressaoComercialTest.php` → 5 testes verdes
- `git status --porcelain app/` → saída vazia
- `git status --porcelain composer.json composer.lock package.json package-lock.json` → saída vazia
</verification>

<success_criteria>
- 5 testes de caracterização do caminho Comercial passando contra o código de produção INTOCADO
- `gmail_colaborador`, Incubadora, divergência D-08 e roteamento com interruptor desligado cobertos
- Zero arquivos de produção modificados
</success_criteria>

<output>
Criar `.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/124-01-SUMMARY.md` ao terminar.

Commit (apenas o próprio path, árvore compartilhada — nunca `git add -A`, nunca `git stash`):
`git commit -- tests/Feature/Phase124RegressaoComercialTest.php` com mensagem em pt-BR
`test(124-01): caracteriza roteamento do cadastro manual antes da extracao`
</output>
