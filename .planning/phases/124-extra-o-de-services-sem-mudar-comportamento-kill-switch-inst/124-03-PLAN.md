---
phase: 124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst
plan: 03
type: execute
wave: 2
depends_on: ["124-01", "124-02"]
files_modified:
  - .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-antes.txt
  - .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-depois-03.txt
  - app/Services/Comercial/PendenciasComerciaisService.php
  - app/Http/Controllers/ComercialController.php
autonomous: true
requirements: [FLUXO-03]

must_haves:
  truths:
    - "A regra das 7 pendencias comerciais vive num unico arquivo, fora do controller"
    - "A listagem do Comercial mostra exatamente as mesmas pendencias que mostrava antes da extracao"
    - "Existe um baseline por NOME de teste, capturado antes de qualquer refatoracao, para comparar depois"
  artifacts:
    - path: ".planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-antes.txt"
      provides: "Lista nominal de testes verdes/vermelhos ANTES da refatoracao"
      min_lines: 30
    - path: "app/Services/Comercial/PendenciasComerciaisService.php"
      provides: "Calculo unico das pendencias comerciais (FLUXO-03)"
      contains: "class PendenciasComerciaisService"
      min_lines: 100
  key_links:
    - from: "app/Http/Controllers/ComercialController.php"
      to: "App\\Services\\Comercial\\PendenciasComerciaisService"
      via: "injecao no metodo listagem()"
      pattern: "PendenciasComerciaisService"
    - from: "app/Services/Comercial/PendenciasComerciaisService.php"
      to: "HubspotLineItemMapping::paraNome"
      via: "cache estatica de resolucao de nome"
      pattern: "paraNome"
---

<objective>
Congelar o baseline de regressão da fase e extrair a primeira regra duplicada:
`ComercialController::calcularPendenciasComerciais()` vira
`App\Services\Comercial\PendenciasComerciaisService::calcular()` (FLUXO-03).

Propósito: as 7 pendências comerciais são a fonte que a tela do Administrativo (Fase 131) e o gate de
contrato (Fases 128/133) vão consumir. Tirar a regra de dentro de um método privado de controller é
pré-condição para isso — e é a metade mais barata e menos arriscada da extração desta fase.

Saída: `baseline-antes.txt` (a referência imutável do resto da fase), o service novo, e o controller
consumindo o service.

**Refatoração pura.** O corpo do método é copiado LITERALMENTE, incluindo o early-return
`if (!$c->is_origem_hubspot) return [];` e a `static $matchCache`. Mudar o early-return é a decisão
A4, atribuída à **Fase 128** — não acontece aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/124-CONTEXT.md
@.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/124-RESEARCH.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real — não explorar o codebase. -->

Assinatura de HOJE (a preservar):
`ComercialController::calcularPendenciasComerciais(Company $c): array` — privado, linhas 467-578.
Retorna uma lista de slugs, na ordem em que são empilhados:
`sem_servico`, `sem_valor`, `servico_nao_reconhecido`, `sem_setor`, `sem_contato`, `valor_revisar`,
`possivel_duplicidade`.

Único call site de HOJE (linha 242, dentro de `listagem()`):
```
$todasEmpresas->each(function (Company $c) {
    $c->is_origem_hubspot     = (bool) ($c->hubspot_evento_origem_exists ?? false);
    $c->pendencias_comerciais = $this->calcularPendenciasComerciais($c);
});
```
As relações de que o método depende já vêm eager-loaded pela query da linha 205
(`contratosServico` filtrado por `ativo`, com `servico`; `hubspotEventos` limitado a 3) e
`is_origem_hubspot` é anotado NA LINHA ANTERIOR à chamada. O service NÃO deve fazer `refresh()` nem
`load()` próprio nesta fase — isso mudaria o número de queries e o dado lido.

Assinatura NOVA (alvo):
`App\Services\Comercial\PendenciasComerciaisService::calcular(Company $c): array` — pública, mesmo
corpo, mesmo retorno.

**Homônimo que NÃO é este e NÃO deve ser tocado:**
`HubspotWebhookController::calcularPendencias()` (linhas 975-997) — outro conceito, 4 slugs
diferentes (`sem_responsavel`, `sem_cust_id`, `sem_email_colaborador`, `sem_servico`), usado só para
decidir se notifica o Comercial. Fica exatamente como está.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturar o baseline nominal ANTES de qualquer refatoração</name>

  <read_first>
    - .planning/phases/124-.../124-RESEARCH.md § "Q3 — Estratégia de baseline num projeto que não roda
      a suíte inteira" (o subconjunto escolhido, o motivo de cada arquivo, e a lista nominal das
      falhas pré-existentes)
    - tests/Feature/Phase124RegressaoComercialTest.php (criado no plano 124-01)
    - tests/Feature/Phase124RegressaoHubspotTest.php (criado no plano 124-02)
  </read_first>

  <files>.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-antes.txt</files>

  <action>
Confirmar primeiro que os dois arquivos de teste dos planos 124-01 e 124-02 existem — se não
existirem, PARAR: o baseline seria capturado incompleto e perderia o valor.

Rodar o gate de 6 arquivos, num processo só, com o PHP do XAMPP (fora do PATH), e gravar a saída
filtrada em `baseline-antes.txt`:

```
"/c/xampp/php/php.exe" vendor/bin/phpunit --testdox --do-not-cache-result \
  tests/Feature/Phase35HubspotV2Test.php \
  tests/Feature/Phase14ComercialTest.php \
  tests/Feature/Phase37ComercialListagemTest.php \
  tests/Unit/ComercialControllerHelperTest.php \
  tests/Feature/Phase124RegressaoComercialTest.php \
  tests/Feature/Phase124RegressaoHubspotTest.php \
  2>&1 | grep -v -E '^(Time:|Memory:|Runtime:|Configuration:|PHPUnit |OK|Tests:|WARNINGS?|$)' \
  > .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-antes.txt
```

O filtro remove só as linhas voláteis (tempo, memória, versão) — o que fica é o NOME de cada teste
com ✔ ou ✘. É por NOME que o resto da fase compara, nunca por contagem: contagem igual com nomes
trocados é exatamente o cenário perigoso que a lição do projeto manda evitar.

**Este comando é o gate da fase.** Anotar no SUMMARY que ele deve ser reexecutado, textualmente
igual, ao fim de cada plano de refatoração.

Conferir que as únicas linhas com ✘ são as falhas pré-existentes já catalogadas na pesquisa:
`Phase14ComercialTest::test_update_ignora_campos_legacy` (coluna legacy `service_type`, não toca
roteamento). Se aparecer qualquer outra falha, PARAR e reportar — significa que a árvore mudou por
trabalho de outra sessão/outro dev e o baseline precisa ser reavaliado antes de refatorar.

Não rodar `Phase13ComercialTest` — é obsoleto (envia `service_type`, campo extinto), está em
deferred, e não entra no gate.
  </action>

  <acceptance_criteria>
    - `baseline-antes.txt` existe, tem mais de 30 linhas e contém ao menos uma ocorrência de cada um
      destes nomes de classe: `Phase35HubspotV2`, `Phase14Comercial`, `Phase37ComercialListagem`,
      `Phase124RegressaoComercial`, `Phase124RegressaoHubspot`.
    - O arquivo NÃO contém as linhas `Time:`, `Memory:` nem `PHPUnit ` (o diff futuro não pode acusar
      diferença por tempo de execução): `grep -c -E '^(Time:|Memory:|PHPUnit )' baseline-antes.txt`
      retorna 0.
    - A única linha com ✘ referente ao roteamento é `test_update_ignora_campos_legacy`. Qualquer outra
      falha interrompe o plano.
    - `git status --porcelain app/` retorna vazio (nenhum código de produção foi tocado ainda).
  </acceptance_criteria>

  <verify>
    <automated>grep -c "Phase124RegressaoHubspot" .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-antes.txt</automated>
  </verify>

  <done>`baseline-antes.txt` gravado com a lista nominal de testes, sem linhas voláteis, com as falhas pré-existentes conferidas contra a lista da pesquisa.</done>
</task>

<task type="auto">
  <name>Task 2: Extrair PendenciasComerciaisService com o corpo copiado literalmente</name>

  <read_first>
    - app/Http/Controllers/ComercialController.php — método `calcularPendenciasComerciais()`
      (linhas 467-578) LINHA A LINHA, e o call site em `listagem()` (linhas 236-243) com a query
      que eager-loada as relações (linhas 205-213)
    - app/Models/HubspotLineItemMapping.php — o método estático `paraNome()`, usado pela
      `static $matchCache`
    - app/Services/Hubspot/HubspotDealHandoffService.php — só para conferir a convenção de
      namespace/docblock de service do projeto (`App\Services\<Dominio>\<Nome>Service`, comentários pt-BR)
    - .planning/research/ARCHITECTURE.md § "PendenciasComerciaisService" (o alerta sobre a
      `static $matchCache` e sobre o homônimo `calcularPendencias()` do webhook)
    - .planning/phases/124-.../124-CONTEXT.md § "Reusable Assets" (o early-return de `is_origem_hubspot`
      sai INTACTO — mudá-lo é a decisão A4, da Fase 128)
  </read_first>

  <files>app/Services/Comercial/PendenciasComerciaisService.php, app/Http/Controllers/ComercialController.php</files>

  <action>
1. Criar `app/Services/Comercial/PendenciasComerciaisService.php`, namespace
   `App\Services\Comercial`, classe `PendenciasComerciaisService`, com um único método público
   `calcular(Company $c): array`.

   O corpo é **cópia literal** das linhas 467-578 do controller, incluindo:
   - o early-return `if (!$c->is_origem_hubspot) { return []; }` — INTACTO;
   - a `static $matchCache = [];` e a closure `$resolverNome` **exatamente como estão** (manter
     `static` dentro do método; virar propriedade de instância mudaria o escopo do cache conforme a
     forma de resolução do service, e esta fase não pode ter efeito colateral de resolução);
   - todos os comentários pt-BR existentes, inclusive os que datam hotfixes (2026-06-19) e quick
     tasks (260805-eqk) — eles são histórico de decisão, não ruído;
   - a ordem em que os slugs são empilhados.

   Nenhum `refresh()`, nenhum `load()`, nenhuma query nova: o método continua lendo apenas relações
   já carregadas pelo chamador.

   Acrescentar docblock de classe em pt-BR dizendo: fonte única das 7 pendências COMERCIAIS
   (FLUXO-03), extraída de `ComercialController` na Fase 124 sem alteração de comportamento; e o
   aviso de que `HubspotWebhookController::calcularPendencias()` é outro conceito (4 slugs, uso de
   notificação) e não deve ser confundido com este.

2. Em `ComercialController`: apagar o método privado `calcularPendenciasComerciais()` e passar a
   consumir o service em `listagem()`.

   Injetar por assinatura de método na action: `public function listagem(Request $request,
   PendenciasComerciaisService $pendencias)`, e trocar a linha 242 por
   `$c->pendencias_comerciais = $pendencias->calcular($c);`. Resolver o service UMA vez (fora do
   `each()`) preserva o comportamento da cache dentro do request.

   Adicionar o `use App\Services\Comercial\PendenciasComerciaisService;` na ordem dos imports
   existentes. Conferir com grep se `HubspotLineItemMapping` ainda é usado em outro ponto do
   controller antes de remover o import — se não for, remover; se for, manter.

3. Não tocar em `HubspotWebhookController` neste plano.
  </action>

  <acceptance_criteria>
    - `app/Services/Comercial/PendenciasComerciaisService.php` existe com
      `namespace App\Services\Comercial;` e `public function calcular(Company $c): array`.
    - O early-return de origem HubSpot sobreviveu:
      `grep -c "is_origem_hubspot" app/Services/Comercial/PendenciasComerciaisService.php` ≥ 1.
    - A cache estática sobreviveu:
      `grep -c "static \$matchCache" app/Services/Comercial/PendenciasComerciaisService.php` = 1.
    - Os 7 slugs aparecem no service: `sem_servico`, `sem_valor`, `servico_nao_reconhecido`,
      `sem_setor`, `sem_contato`, `valor_revisar`, `possivel_duplicidade`.
    - O método sumiu do controller:
      `grep -c "private function calcularPendenciasComerciais" app/Http/Controllers/ComercialController.php` = 0.
    - `HubspotWebhookController::calcularPendencias()` continua intacto:
      `git diff --name-only` NÃO lista `app/Http/Controllers/Api/HubspotWebhookController.php`.
    - `"/c/xampp/php/php.exe" vendor/bin/phpunit tests/Feature/Phase37ComercialListagemTest.php` →
      16/16 verdes (é o teste que caracteriza justamente este cálculo).
  </acceptance_criteria>

  <verify>
    <automated>"/c/xampp/php/php.exe" vendor/bin/phpunit --testdox --do-not-cache-result tests/Feature/Phase37ComercialListagemTest.php tests/Feature/Phase114ComercialListagemEnrichmentTest.php</automated>
  </verify>

  <done>O cálculo das 7 pendências vive em `PendenciasComerciaisService`; o controller consome o service; `Phase37ComercialListagemTest` continua 16/16.</done>
</task>

<task type="auto">
  <name>Task 3: Provar zero regressão contra o baseline nominal</name>

  <read_first>
    - .planning/phases/124-.../baseline-antes.txt (gerado na Task 1)
    - .planning/phases/124-.../124-RESEARCH.md § "Pitfall A: confiar na contagem X/Y em vez do nome"
    - app/Services/Comercial/PendenciasComerciaisService.php e
      app/Http/Controllers/ComercialController.php (os arquivos alterados na Task 2)
  </read_first>

  <files>.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-depois-03.txt</files>

  <action>
Rodar EXATAMENTE o mesmo comando da Task 1, gravando em `baseline-depois-03.txt`, e comparar por
diff com `baseline-antes.txt`:

```
diff .planning/phases/124-.../baseline-antes.txt \
     .planning/phases/124-.../baseline-depois-03.txt
```

O resultado esperado é **diff vazio**. Qualquer nome que mude de ✔ para ✘ ou de ✘ para ✔ é regressão
desta extração e precisa ser investigado antes de seguir — não "atualizar o teste".

Se o diff acusar diferença, NÃO prosseguir: reportar o nome exato do teste que mudou de lado e o que
na extração pode tê-lo causado (candidatos: cache `static` perdida, relação não carregada, ordem dos
slugs alterada).
  </action>

  <acceptance_criteria>
    - `baseline-depois-03.txt` existe.
    - `diff baseline-antes.txt baseline-depois-03.txt` retorna saída VAZIA (exit code 0).
    - Nenhum arquivo além de `app/Services/Comercial/PendenciasComerciaisService.php`,
      `app/Http/Controllers/ComercialController.php` e os dois `.txt` do diretório da fase aparece em
      `git status --porcelain` como alterado por este plano.
  </acceptance_criteria>

  <verify>
    <automated>diff .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-antes.txt .planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/baseline-depois-03.txt</automated>
  </verify>

  <done>Diff vazio entre o baseline de antes e o de depois — a extração das pendências não mudou nenhum comportamento observável.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `GET /comercial/empresas` (listagem) | dado de empresa exibido a usuário autenticado com permissão do Comercial; a extração não altera nenhuma checagem de acesso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-06 | Elevation of Privilege | `ComercialController::listagem()` | mitigate | a extração move só o CÁLCULO; o `abort_unless(hasPermission)` e os filtros de query ficam onde estão — `git diff` do controller não pode conter mudança em linha de autorização |
| T-124-07 | Information Disclosure | `PendenciasComerciaisService` | accept | o service lê apenas relações já carregadas pelo chamador; não amplia o conjunto de empresas visível (o escopo continua sendo definido pela query da `listagem()`) |
| T-124-08 | Tampering | nenhum install de pacote | accept | este plano não instala nem atualiza dependência alguma |
</threat_model>

<verification>
- `diff baseline-antes.txt baseline-depois-03.txt` → vazio
- `grep -c "private function calcularPendenciasComerciais" app/Http/Controllers/ComercialController.php` → 0
- `git diff --name-only` NÃO contém `app/Http/Controllers/Api/HubspotWebhookController.php`
- `git status --porcelain composer.json composer.lock package.json package-lock.json` → vazio
</verification>

<success_criteria>
- `baseline-antes.txt` existe e é a referência nominal do resto da fase
- FLUXO-03 satisfeito: as 7 pendências comerciais vivem num único arquivo consumível por Comercial, HubSpot e Administrativo
- Zero mudança de comportamento comprovada por diff de nomes de teste
</success_criteria>

<output>
Criar `.planning/phases/124-extra-o-de-services-sem-mudar-comportamento-kill-switch-inst/124-03-SUMMARY.md` ao terminar,
registrando o comando de gate na íntegra (é reusado pelo plano 124-05).

Commit (apenas os próprios paths, árvore compartilhada — nunca `git add -A`, nunca `git stash`):
`git commit -- app/Services/Comercial/PendenciasComerciaisService.php app/Http/Controllers/ComercialController.php .planning/phases/124-*/baseline-*.txt`
com mensagem em pt-BR
`refactor(124-03): extrai PendenciasComerciaisService sem mudar comportamento`
</output>
