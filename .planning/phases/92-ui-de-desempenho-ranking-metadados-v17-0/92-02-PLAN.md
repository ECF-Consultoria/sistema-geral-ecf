---
phase: 92-ui-de-desempenho-ranking-metadados-v17-0
plan: 02
type: execute
wave: 2
depends_on: ["92-01"]
files_modified:
  - resources/js/Pages/Performance/Index.jsx
  - resources/js/Pages/Performance/Show.jsx
  - resources/js/Pages/Portfolio/Show.jsx
autonomous: false
requirements: [DESEMP-08]

must_haves:
  truths:
    - "Cada linha do ranking /performance mostra o status da nota: badge 'Aguarda régua Shopee' quando score_status='blocked', 'Parcial' quando 'partial', sem badge (ou 'Oficial' discreto) quando 'official' — nunca o slug cru"
    - "Cada linha exibe os metadados por profissional: empresas únicas, vínculos de serviço, vínculos sem fonte financeira (tooltip na célula Empresas ou equivalente), além do status da nota"
    - "O select de contexto (Todos/Performance/Shopee) filtra a visualização do ranking — muda quais linhas/números aparecem — sem recalcular nota (usa os metadados já no payload)"
    - "Performance/Show.jsx (drill-down) mostra o badge de status ao lado da nota + os 4 metadados"
    - "Profissional blocked vendo a própria carteira (Portfolio/Show.jsx) vê 'sua nota ainda não é oficial (aguarda régua Shopee)' no lugar do card de comparação de pares"
  artifacts:
    - path: "resources/js/Pages/Performance/Index.jsx"
      provides: "SCORE_STATUS_LABEL/BADGE + badge na linha + metadados + select de contexto view-only"
      contains: "Aguarda régua Shopee"
    - path: "resources/js/Pages/Performance/Show.jsx"
      provides: "Badge de status + metadados no FaixaBonusCard"
      contains: "score_status"
    - path: "resources/js/Pages/Portfolio/Show.jsx"
      provides: "Mensagem self-view do blocked no lugar da comparação"
      contains: "não é oficial"
  key_links:
    - from: "Performance/Index.jsx RankingConsultoria"
      to: "u.score_status"
      via: "SCORE_STATUS_LABEL lookup + badge"
      pattern: "score_status"
    - from: "Performance/Index.jsx select contexto"
      to: "rankingFiltrado (useMemo)"
      via: "filtro client-side por vinculos_sem_fonte_financeira"
      pattern: "contexto"
    - from: "Portfolio/Show.jsx"
      to: "performance_profissional.score_status"
      via: "render condicional da mensagem"
      pattern: "blocked"
---

<objective>
Frontend da Fase 92: exibir na UI de Desempenho os metadados por profissional e o status da nota (com labels sem jargão), adicionar o select de auditoria por contexto (view-only), e mostrar a mensagem de "nota não oficial" na self-view do profissional blocked.

Purpose: fechar SC2 (metadados + status por linha) e SC3 (filtro de auditoria) do ROADMAP, e completar a UX da correção da pendência (blocked não vira 0.0 e o próprio profissional entende por quê).

Output: `Performance/Index.jsx`, `Performance/Show.jsx` e o bloco de comparação de `Portfolio/Show.jsx` atualizados + `npm run build` exit 0 + checkpoint visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-ui-de-desempenho-ranking-metadados-v17-0/92-RESEARCH.md
@.planning/phases/92-ui-de-desempenho-ranking-metadados-v17-0/92-01-SUMMARY.md
@resources/js/Pages/Performance/Index.jsx
@resources/js/Pages/Performance/Show.jsx

<interfaces>
<!-- Props novas que o Plan 92-01 passou ao Inertia (Performance/Index):
     - ranking[].score_status: 'official' | 'partial' | 'blocked'
     - ranking[].empresas_unicas, .vinculos_servico, .vinculos_financeiros, .vinculos_sem_fonte_financeira: int
     - ranking[].componentes_disponiveis: ?array
     - contexto: 'todos' | 'performance' | 'shopee'   (valor validado no backend)
   Performance/Show recebe `resultado` inteiro → resultado.score_status + os 4 metadados já chegam.
   Portfolio/Show recebe `performance_profissional` (com .score_status) — já passado hoje. -->

<!-- Padrão de badge JÁ existente a reusar (Performance/Index.jsx:42-53): -->
const FAIXA_LABEL = { sem_bonus: 'Sem bônus', basico: 'Básico', intermediario: 'Intermediário', maximo: 'Máximo' };
const FAIXA_BADGE_CLS = { sem_bonus: 'bg-white/[0.04] text-white/60 border-white/[0.08]', /* ... */ };
// badge: <span className={cn('inline-flex items-center text-[10px] font-semibold px-2 py-0.5 rounded-full border', cls)}>{lbl}</span>

<!-- Toolbar de filtros (Index.jsx ~209-263): applyFilter({ setor, cargo, mes }) faz router.get preservando URL.
     O select de mês (~211-224) e os botões de cargo (~236-262) são o padrão a espelhar. -->

<!-- Labels de status TRAVADOS pelo usuário (não usar slug cru): -->
//   blocked → 'Aguarda régua Shopee'   (auto-explicativo, não punitivo)
//   partial → 'Parcial'
//   official → sem badge (ou 'Oficial' discreto)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge de status + metadados + select de contexto no ranking (Performance/Index.jsx)</name>
  <files>resources/js/Pages/Performance/Index.jsx</files>
  <action>
    Criar `SCORE_STATUS_LABEL` e `SCORE_STATUS_BADGE_CLS` no mesmo padrão de `FAIXA_LABEL`/`FAIXA_BADGE_CLS` (ver `<interfaces>`), com os labels TRAVADOS: blocked → 'Aguarda régua Shopee' (tom âmbar/alerta suave), partial → 'Parcial' (âmbar claro), official → sem badge. NUNCA renderizar o slug cru.
    Em `RankingConsultoria` (linha ~369-486): renderizar o badge de status na célula "Nota" (ao lado do `—` quando blocked, ou discreto quando partial). Não adicionar coluna nova ao grid (as 10 colunas já estão apertadas — ver research §"Onde a linha do ranking já teria tratamento parcial"). Nos metadados por linha (SC2): usar a célula "Empresas" existente (linha ~471-473) com um `title` (tooltip) mostrando empresas_unicas, vinculos_servico e vinculos_sem_fonte_financeira; o status já vem pelo badge. Textos do tooltip em pt-BR, sem jargão.
    Select de contexto (SC3): adicionar um `<select>` na toolbar (bloco ~209-263), no mesmo estilo do select de mês, com opções Todos/Performance/Shopee. Ele é VIEW-ONLY. Duas opções de implementação — escolher a mais simples que persista o estado:
      (a) round-trip via `applyFilter({ setor, cargo, mes: mes_selecionado, contexto: valor })` (consistente com mês/cargo, persiste na URL) — o backend já ecoa a prop `contexto`; então
      (b) filtrar client-side `rankingFiltrado` num `useMemo` por `contexto`: 'shopee' → linhas com `vinculos_sem_fonte_financeira > 0`; 'performance' → linhas com `vinculos_sem_fonte_financeira === 0`; 'todos' → sem filtro. Ler o valor atual da prop `contexto` (default 'todos').
    IMPORTANTE: o filtro só muda quais linhas aparecem — NUNCA altera nota_final nem chama backend de score. Ao filtrar, reaproveitar/preservar o bloco de transparência `sem_carteira` existente sem alteração de comportamento.
    Manter tudo pt-BR; usar `cn()` e tokens `ecf-*`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Em /performance: (1) profissional só-Shopee mostra badge "Aguarda régua Shopee" e nota "—"; (2) tooltip da célula Empresas mostra empresas únicas / vínculos / vínculos sem fonte; (3) select Todos/Performance/Shopee muda as linhas exibidas sem mudar nenhuma nota.</human-check>
  </verify>
  <done>Build exit 0; badge de status por linha com labels travados; metadados visíveis; select de contexto filtra a visualização sem recalcular.</done>
</task>

<task type="auto">
  <name>Task 2: Badge de status + metadados no drill-down (Performance/Show.jsx) + self-view blocked (Portfolio/Show.jsx)</name>
  <files>resources/js/Pages/Performance/Show.jsx, resources/js/Pages/Portfolio/Show.jsx</files>
  <action>
    Performance/Show.jsx — `FaixaBonusCard` (linha ~129-218): já recebe `resultado` inteiro, logo `resultado.score_status` e os 4 metadados já chegam. Adicionar o badge de status (reusar SCORE_STATUS_LABEL/BADGE — pode extrair para @/lib ou duplicar o mapa local, à discrição do executor) ao lado da nota (linha ~171-192). Quando `resultado.nota_final == null` (blocked), complementar o texto existente "Sem dados suficientes..." (linha ~195-199) com a explicação pt-BR do status (ex.: blocked → "Carteira sem vínculo com fonte financeira ainda — aguardando régua de bônus da diretoria."). Exibir os 4 metadados (empresas_unicas, vinculos_servico, vinculos_financeiros, vinculos_sem_fonte_financeira) num bloco discreto do card (há espaço no drill-down). Sem jargão técnico.
    Portfolio/Show.jsx — self-view do blocked: o componente já recebe `performance_profissional` (com `.score_status`). No bloco que renderiza a comparação de pares (`comparacao_contextual && performance_profissional`, linha ~1146), adicionar um ramo: quando `performance_profissional?.score_status === 'blocked'` (e/ou `comparacao_contextual == null`), renderizar uma mensagem no lugar do card — "Sua nota ainda não é oficial — carteira sem fonte financeira (aguarda régua Shopee)." Edição cirúrgica: NÃO reescrever o mapeamento `comparacao_contextual` (useMemo linha ~500-519) nem outros blocos da página. Reusar tokens `ecf-*`.
    ATENÇÃO (sessão paralela NPS ativa): tocar SÓ o bloco de comparação de pares de Portfolio/Show.jsx. NÃO tocar nada de Nps/*.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Em /performance/{blocked} o card mostra badge de status + explicação pt-BR + metadados. Em /portfolio/{blocked} (self-view do profissional só-Shopee) aparece "sua nota ainda não é oficial (aguarda régua Shopee)" no lugar da comparação de pares — sem 0.0 fantasma.</human-check>
  </verify>
  <done>Build exit 0; drill-down mostra status + metadados; self-view do blocked mostra a mensagem em vez da comparação distorcida.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Ranking /performance com badge de status (Aguarda régua Shopee/Parcial/Oficial) + metadados por linha + select de contexto view-only; drill-down /performance/{user} com status+metadados; self-view do blocked em /portfolio/{user} com mensagem de nota não oficial. Correção da comparação de pares (blocked fora, tamanho_amostra correto) validada no Plan 92-01 por teste.</what-built>
  <how-to-verify>
    1. Abrir /performance. Conferir: profissional só-Shopee (ex.: Matheus/Gustavo/Felipe conforme dados) aparece no ranking com badge "Aguarda régua Shopee" e nota "—" (não some, não vira 0).
    2. Passar o mouse na célula "Empresas" de qualquer linha → tooltip com empresas únicas / vínculos de serviço / vínculos sem fonte financeira.
    3. Trocar o select de contexto para "Shopee" → só linhas com vínculo sem fonte financeira; "Performance" → só as elegíveis; "Todos" → tudo. Confirmar que NENHUMA nota muda ao trocar (é só visualização).
    4. Abrir /performance/{profissional blocked} → card mostra badge de status + explicação pt-BR + os 4 metadados.
    5. Abrir /portfolio/{profissional blocked} logado como ele (self-view) → mensagem "sua nota ainda não é oficial (aguarda régua Shopee)" no lugar da comparação de pares.
    6. Abrir /portfolio/{profissional official} com pares → comparação de pares aparece normal e o "vs N analistas" NÃO conta o blocked.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva ajustes visuais/labels.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (select contexto) → estado do React / URL | valor do filtro; a whitelist real é imposta no backend (Plan 92-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03 | Tampering | select de contexto (client-side) | mitigate | Filtro é puramente visual sobre dados já autorizados no payload; o backend (92-01) já validou a whitelist e nunca recalcula score — um valor client-side arbitrário no máximo não filtra nada |
| T-92-SC | Tampering | npm installs | accept | Nenhuma dependência nova instalada (stack herdada); `npm run build` apenas recompila o bundle existente |
</threat_model>

<verification>
- `npm run build` exit 0 (jsx mudou — obrigatório por convenção do projeto).
- Checkpoint visual humano aprovado cobrindo badge de status + metadados + select de contexto + self-view do blocked.
- Sem regressão visual no ranking dos profissionais official/partial (nota, faixa, deltas inalterados).
</verification>

<success_criteria>
- SC2: cada linha do ranking exibe metadados + status da nota com labels sem jargão (labels travados pelo usuário).
- SC3: select Todos/Performance/Shopee muda só a visualização.
- UX da pendência: self-view do blocked explica a ausência de nota oficial em vez de mostrar comparação distorcida.
</success_criteria>

<output>
Criar `.planning/phases/92-ui-de-desempenho-ranking-metadados-v17-0/92-02-SUMMARY.md` ao finalizar.
</output>
