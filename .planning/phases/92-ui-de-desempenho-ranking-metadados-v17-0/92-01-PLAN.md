---
phase: 92-ui-de-desempenho-ranking-metadados-v17-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/PerformanceController.php
  - app/Http/Controllers/PortfolioController.php
  - tests/Feature/V16/PerformanceIndexMetadadosTest.php
  - tests/Feature/V16/ComparacaoContextualBlockedTest.php
autonomous: true
requirements: [DESEMP-08]

must_haves:
  truths:
    - "O payload de /performance (Inertia prop `ranking`) inclui, por linha, os 6 metadados de elegibilidade (empresas_unicas, vinculos_servico, vinculos_financeiros, vinculos_sem_fonte_financeira, score_status, componentes_disponiveis) — vindos do compute cacheado, sem recomputar"
    - "?contexto= aceita só todos|performance|shopee (whitelist); valor inválido cai em 'todos'; o valor NUNCA chega ao DesempenhoScoreService nem altera nota_final/score_status de nenhuma linha"
    - "No comparacaoContextual, profissional com score_status==='blocked' é excluído de $scoresPares (mesmo continue do sem_carteira) — não vira 0.0 na comparação de pares"
    - "tamanho_amostra passa a contar só pares com nota calculável (blocked fora), batendo com a base da mediana"
    - "Profissional blocked vendo a própria tela recebe sinal (score_status via performance_profissional) para exibir 'nota ainda não oficial' em vez de comparação — sem 0.0 fantasma"
  artifacts:
    - path: "app/Http/Controllers/PerformanceController.php"
      provides: "Passthrough dos 6 metadados no array do ranking + helper contextoFiltro() view-only + prop contexto"
      contains: "vinculos_sem_fonte_financeira"
    - path: "app/Http/Controllers/PortfolioController.php"
      provides: "Exclusão de blocked em $scoresPares + tamanho_amostra corrigido"
      contains: "score_status"
    - path: "tests/Feature/V16/PerformanceIndexMetadadosTest.php"
      provides: "Cobre SC2 (metadados no payload) + SC3 (contexto view-only) + SC1 (gate ausência de score separado)"
    - path: "tests/Feature/V16/ComparacaoContextualBlockedTest.php"
      provides: "Cobre a correção da Distorção A/B (blocked fora dos pares, tamanho_amostra correto, self-view blocked)"
  key_links:
    - from: "PerformanceController::index()"
      to: "resultado (compute cacheado)"
      via: "map dos 6 metadados no array do ranking"
      pattern: "score_status.*=>.*resultado\\['score_status'\\]"
    - from: "PortfolioController::show() comparacaoContextual"
      to: "$scoresPares"
      via: "continue quando score_status==='blocked'"
      pattern: "score_status.*blocked"
---

<objective>
Backend da Fase 92: fazer o passthrough dos 6 metadados de elegibilidade (já calculados na Fase 91) para o payload do ranking `/performance`, adicionar o filtro de auditoria `?contexto=` view-only (whitelist da Fase 90), e corrigir a pendência crítica herdada da Fase 91 — a distorção A/B do bloco `comparacaoContextual` em `PortfolioController::show()`.

Purpose: A UI (Plan 92-02) só consegue exibir status/metadados por profissional se o controller repassar as chaves; e o bônus/comparação de pares precisa parar de tratar um profissional `blocked` como nota 0.0. Nenhuma dessas mudanças recalcula score — é passthrough + uma exclusão cirúrgica na amostra de pares.

Output: `PerformanceController` e `PortfolioController` atualizados + 2 arquivos de teste Feature verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-ui-de-desempenho-ranking-metadados-v17-0/92-RESEARCH.md
@.planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-02-SUMMARY.md
@app/Http/Controllers/PerformanceController.php
@app/Http/Controllers/PortfolioController.php

<interfaces>
<!-- Shape retornado por DesempenhoScoreService::compute()/computeCached() (Fase 91, NÃO tocar).
     Estas 6 chaves JÁ ESTÃO no array retornado e no cache v4 e no breakdown_json do snapshot mensal. -->
$resultado = [
    // ... campos já repassados hoje (nota_final, faixa_bonus, componentes, empresas_carteira, empresas_com_baseline, sem_carteira, motivo, ...)
    'empresas_unicas'               => int,      // empresas distintas na carteira do profissional
    'vinculos_servico'              => int,      // total de vínculos de serviço (company_users com servico_id)
    'vinculos_financeiros'          => int,      // vínculos com fonte financeira elegível (ML/Adman)
    'vinculos_sem_fonte_financeira' => int,      // vínculos sem financeiro (ex.: Shopee) — > 0 sinaliza contexto Shopee
    'score_status'                  => string,   // 'official' | 'partial' | 'blocked'
    'componentes_disponiveis'       => ?array,   // quais componentes entraram na média
];

<!-- Fonte de referência do filtro (Fase 90), a espelhar em PerformanceController.
     app/Http/Controllers/PortfolioController.php:78-85 -->
private function contextoFiltro(Request $request): array {
    return match ($request->query('contexto')) {
        'performance' => ['param' => 'performance', 'setor' => Servico::SETOR_PERFORMANCE],
        'shopee'      => ['param' => 'shopee',      'setor' => Servico::SETOR_SHOPEE],
        default       => ['param' => 'todos',       'setor' => null],
    };
}

<!-- comparacaoContextual (PortfolioController::show ~1484-1547): loop que monta $scoresPares.
     Hoje o único filtro é sem_carteira (linha 1489). -->
foreach (User::whereIn('id', $paresIds)->get() as $par) {
    $resultadoPar = $this->scoreService->computeCached($par, $mesReferencia);
    if (($resultadoPar['sem_carteira'] ?? false) === true) { continue; }
    $scoresPares->put($par->id, $resultadoPar);
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Passthrough dos 6 metadados + filtro ?contexto= view-only no PerformanceController::index()</name>
  <files>app/Http/Controllers/PerformanceController.php, tests/Feature/V16/PerformanceIndexMetadadosTest.php</files>
  <behavior>
    - Teste 1 (SC2): GET /performance como admin → cada item da prop Inertia `ranking` contém as 6 chaves novas (empresas_unicas, vinculos_servico, vinculos_financeiros, vinculos_sem_fonte_financeira, score_status, componentes_disponiveis).
    - Teste 2 (SC2 blocked): fixture de profissional só-Shopee (score_status='blocked', nota_final=null) NÃO some do ranking (sem_carteira=false) e vem com score_status==='blocked' + vinculos_sem_fonte_financeira > 0.
    - Teste 3 (SC3): GET /performance?contexto=shopee e ?contexto=performance retornam, para cada profissional, o MESMO nota_final e o MESMO score_status que ?contexto=todos (o filtro nunca recalcula; só a prop `contexto` muda). Valor inválido (?contexto=xpto) resolve para 'todos'.
    - Teste 4 (SC1, gate de ausência): `grep -rin "score_shopee\|score_ml\|ranking_shopee\|ranking_ml" app/ resources/js/` continua retornando 0 — assert via shell no teste OU teste dedicado de string. (Reusar o padrão do 91-02; pode ser um assertion simples lendo os arquivos do controller.)
  </behavior>
  <action>
    Adicionar as 6 chaves ao array retornado dentro de `$rankingRaw = $users->map(...)` (por volta da linha 119-140), lendo direto de `$resultado` com `?? default` — mesmo padrão das chaves já presentes (ver `<interfaces>` para nomes e tipos). NÃO chamar o service de novo: `$resultado` já é o compute cacheado (ou o breakdown_json do snapshot mensal, que também contém as 6 chaves — sem fallback especial). Castar int nos contadores e string em score_status (default 'blocked').
    Criar o método privado `contextoFiltro(Request $request): array` espelhando o da Fase 90 (ver `<interfaces>`) — whitelist explícita via `match()`, nunca repassar o valor cru. IMPORTANTE: o resultado do filtro é APENAS repassado à view como prop `contexto` (o `param` resolvido: 'todos'|'performance'|'shopee'); NÃO passar `setor` para `computeCached()`/`compute()` nem para nenhuma query do ranking (DESEMP-02 proíbe segundo score). A filtragem de quais linhas aparecem é feita client-side no Plan 92-02 usando os metadados já presentes no payload — este controller só valida a whitelist (defesa contra Tampering) e ecoa o valor seguro.
    No `Inertia::render('Performance/Index', [...])` (linha ~231) adicionar `'contexto' => $contextoFiltro['param']`.
    NÃO tocar `dashboardCarteira()` nem `show()` — fora de escopo (decisão do usuário: só o ranking /performance). `show()` já passa `$resultado` inteiro, então os metadados já chegam ao Performance/Show.jsx sem mudança de backend.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit --filter=PerformanceIndexMetadadosTest</automated>
  </verify>
  <done>Os 3 testes de payload passam; ?contexto= inválido cai em 'todos'; nota_final/score_status idênticos entre contextos; gate de ausência (SC1) verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir comparacaoContextual — excluir blocked dos pares + tamanho_amostra + self-view (PortfolioController::show)</name>
  <files>app/Http/Controllers/PortfolioController.php, tests/Feature/V16/ComparacaoContextualBlockedTest.php</files>
  <behavior>
    - Teste 1 (Distorção A): cenário com 1 profissional `official` (nota calculável) + 1 `blocked` (só-Shopee) + pelo menos 2 outros `official` no mesmo cargo. GET portfolio.show do par `official` → `comparacao_contextual.tamanho_amostra` NÃO conta o `blocked`; o `blocked` não influencia percentil/medianas.
    - Teste 2 (Distorção B): `tamanho_amostra` === número de pares com nota calculável (sem_carteira=false E score_status!=='blocked'), batendo com a contagem usada na mediana de nota_final.
    - Teste 3 (self-view blocked): GET portfolio.show do PRÓPRIO profissional `blocked` → `comparacao_contextual` é null (não há 0.0 fantasma) E a prop `performance_profissional.score_status` === 'blocked' está disponível para o front exibir a mensagem "nota ainda não oficial". Confirmar que $minhaNota nunca vira 0.0 a partir de um null real.
  </behavior>
  <action>
    No loop que monta `$scoresPares` (por volta da linha 1485-1493), adicionar, logo após o `continue` do `sem_carteira`, um segundo guard: se `($resultadoPar['score_status'] ?? null) === 'blocked'` → `continue` (blocked não tem nota calculável, não entra na amostra de pares). Ver `<interfaces>` para o trecho exato. Isso resolve a Distorção A (o `null → 0.0` da linha 1497 vira defensivo puro, nunca acionado por um blocked real) e a Distorção B (`tamanho_amostra = $scoresPares->count()` na linha 1547 passa a refletir só pares com nota calculável).
    Self-view do profissional blocked: garantir que, quando o PRÓPRIO `$user` está blocked (`$performanceProfissional['score_status'] === 'blocked'`), o bloco de comparação não produza `comparacaoContextual` distorcido. Com o guard acima, o próprio user (se blocked) não entra em `$scoresPares`, então `$meuResultado` cairia no fallback `$performanceProfissional` e `$minhaNota` viraria 0.0 SE o bloco `>= 2` rodasse. Para evitar isso: envolver a construção de `$comparacaoContextual` com uma checagem — se `$performanceProfissional['score_status'] === 'blocked'`, deixar `$comparacaoContextual = null` (não construir comparação). O front (Plan 92-02) usa `performance_profissional.score_status` (já passado à view hoje) para renderizar a mensagem "sua nota ainda não é oficial (aguarda régua Shopee)".
    NÃO reescrever `medianaPares`/`percentil`/`relativo` — correção é só o predicado de inclusão + o guard de self-view. NÃO tocar nenhum outro método do PortfolioController.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit --filter=ComparacaoContextualBlockedTest</automated>
  </verify>
  <done>Blocked fica fora de $scoresPares; tamanho_amostra bate com a base da mediana; self-view do blocked não gera 0.0 fantasma (comparacao_contextual null + score_status disponível).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PerformanceController::index | query param `?contexto=` (input não confiável) cruza aqui |
| par ↔ profissional (comparacaoContextual) | notas de pares do mesmo cargo agregadas para exibição |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | Tampering | `?contexto=` em PerformanceController::index | mitigate | Resolver via `match()` whitelist (todos/performance/shopee), default seguro 'todos'; nunca usar `$request->query('contexto')` cru em query builder, log ou chamada ao service |
| T-92-02 | Information Disclosure | comparacaoContextual (notas de pares) | accept | Nenhuma mudança de escopo de autorização; `$paresIds` já filtrado por cargo + active=true; a correção só muda QUEM entra por elegibilidade de dado (blocked), não por permissão |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote nesta fase (stack 100% herdada) — sem superfície de supply chain |
</threat_model>

<verification>
- `C:\xampp\php\php.exe vendor/bin/phpunit --filter="PerformanceIndexMetadados|ComparacaoContextualBlocked"` verde.
- Regressão de domínio: `C:\xampp\php\php.exe vendor/bin/phpunit --filter="Desempenho|Bonus|Portfolio"` — baseline 75/76 (1 falha pré-existente `PublicacaoDesempenhoRouteTest`, ver 91-02 §Pitfall 3); nenhuma regressão nova.
- Gate SC1 (ausência de score separado): `grep -rin "score_shopee\|score_ml\|ranking_shopee\|ranking_ml" app/ resources/js/` → 0 (excetuando o texto de UI "Score geral de qualidade" já auditado no 91-02).
</verification>

<success_criteria>
- Ranking `/performance` entrega os 6 metadados por linha (SC2 — dado disponível pro front).
- `?contexto=` validado por whitelist e view-only, sem tocar o score service (SC3, SC1).
- comparacaoContextual não trata blocked como 0.0; tamanho_amostra correto; self-view do blocked coerente (pendência da Fase 91 fechada).
</success_criteria>

<output>
Criar `.planning/phases/92-ui-de-desempenho-ranking-metadados-v17-0/92-01-SUMMARY.md` ao finalizar.
</output>
