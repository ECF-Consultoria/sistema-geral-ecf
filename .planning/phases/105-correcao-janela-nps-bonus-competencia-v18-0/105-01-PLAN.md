---
phase: 105-correcao-janela-nps-bonus-competencia-v18-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/DesempenhoScoreService.php
  - tests/Feature/V18/JanelaNpsBonusTest.php
autonomous: true
requirements: [NPSWIN-01, NPSWIN-02]
must_haves:
  truths:
    - "Bônus de competência FECHADA M lê o NPS coletado em M+1 (não em M)"
    - "Felipe competência junho (fechada) reflete o NPS de julho (~4.97) → nota ~3.5, não 1.5"
    - "No mês EM CURSO o componente NPS é EXCLUÍDO (null), não 0.0 — não tanka a nota do preview operacional"
    - "Competência fechada com janela M+1 JÁ encerrada e 0 respostas reais → nps 0.0 (penaliza, decisão da diretoria)"
    - "Competência fechada com janela M+1 ainda EM COLETA e 0 respostas → NPS excluído (null), não 0.0"
    - "O financeiro (faturamento/margem) permanece na competência M — só o NPS desloca +1"
  artifacts:
    - path: "app/Services/DesempenhoScoreService.php"
      provides: "Deslocamento +1 da janela de NPS no caminho fechado + mecânica exclui/0.0 + cache bump v5→v6"
      contains: "addMonthNoOverflow"
    - path: "tests/Feature/V18/JanelaNpsBonusTest.php"
      provides: "Testes da janela de NPS deslocada (Felipe, em-curso, closed-0-fechado, closed-0-coletando)"
      contains: "class JanelaNpsBonusTest"
  key_links:
    - from: "DesempenhoScoreService::compute"
      to: "computeNpsMedio"
      via: "mês deslocado (\$mes+1) quando \$periodo['is_closed']"
      pattern: "addMonthNoOverflow"
    - from: "DesempenhoScoreService::cacheKey"
      to: "Redis"
      via: "chave versionada v6"
      pattern: "desempenho\\.compute\\.v6"
---

<objective>
Deslocar +1 mês a janela de leitura do componente NPS no motor de bônus,
APENAS no caminho oficial/mês fechado (D1). O bônus de competência M passa a
ler o NPS coletado em M+1 (mês de pagamento), mantendo o financeiro em M.
No mês em curso o componente NPS passa a ser EXCLUÍDO (null), nunca 0.0 —
porque o NPS de M só é coletado em M+1, que ainda não existe.

Purpose: Corrige o bug de prod (Felipe competência junho → 1.5 em vez de ~3.5)
causado por ler NPS do próprio mês M (0 respostas → 0.0 → nota tankada), quando
o NPS de M só chega em M+1. MEXE NA RÉGUA DO BÔNUS — rigor máximo.
Output: `compute()` deslocado, mecânica exclui/0.0 definida, cache bump v5→v6,
suíte V18/JanelaNpsBonusTest cobrindo os 4 cenários obrigatórios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-correcao-janela-nps-bonus-competencia-v18-0/105-CONTEXT.md
@.planning/phases/105-correcao-janela-nps-bonus-competencia-v18-0/105-RESEARCH.md
@app/Services/DesempenhoScoreService.php
@tests/Feature/V18/DesempenhoPeriodoOficialTest.php

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código — não explorar de novo. -->

DesempenhoScoreService::compute(User $user, Carbon $mesReferencia, ?array $periodoOverride = null): array
  - linha 314: $periodo = $periodoOverride ?? $this->resolvePeriodo($mes);
    O $periodo (shape do MetricPeriodResolver) EXPÕE a chave booleana 'is_closed'.
    'is_closed' = true para last_closed_month e para YYYY-MM de mês passado;
    false para current_month. É o SINAL CANÔNICO de "mês fechado" e já está
    disponível ANTES da chamada a computeNpsMedio (linha 332).
  - linha 332 (PONTO DE INJEÇÃO): $nps = $this->computeNpsMedio($user, $mes);

DesempenhoScoreService::computeNpsMedio(User $user, Carbon $mes): float
  - filtra nps_surveys.completed_at BETWEEN startOfMonth($mes) e endOfMonth($mes)
    (fonte canônica DEC-80-B0 — NÃO mexer na query, já é a coluna certa).
  - retorna round($notas->avg(), 2); vazio → 0.0 (DESEMP-03).
  - IMPORTANTE (sentinela): as notas são 1..5 (escala do formulário, clamp),
    logo qualquer média NÃO-vazia é >= 1.0. O valor exatamente 0.0 SÓ ocorre no
    ramo vazio → 0.0 é um sentinela confiável de "nenhuma resposta na janela".
  - ⚠ NÃO alterar a assinatura (User, Carbon): float — testes a invocam por
    reflection com assertSame, que recusa mudança de tipo de retorno.

DesempenhoScoreService::cacheKey(int $userId, Carbon $mes): string
  - linha 265: sprintf('desempenho.compute.v5.%d.%s', $userId, $periodKey)

MetricPeriodResolver::resolve(['period_key' => ...]) retorna array com:
  current_start, current_end, baseline_start, baseline_end, mode,
  comparison_mode, bonus_payment_month, bonus_competence_month,
  is_current_month, is_closed  ← usar is_closed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Testes da janela de NPS deslocada — 4 cenários obrigatórios</name>
  <files>tests/Feature/V18/JanelaNpsBonusTest.php</files>
  <behavior>
    Criar suíte em namespace Tests\Feature\V18 com Http::preventStrayRequests()
    + Http::fake() determinístico no setUp (o compute() alcança
    AdmanMetricDiffService/providers ML — ISOLAMENTO HTTP TOTAL obrigatório em
    TODO teste que chama compute() com empresa elegível). Semear AdmanMetric
    local pro financeiro ser determinístico sem rede.

    - Teste 1 (Felipe / competência fechada lê M+1): setTestNow num mês em que a
      competência M é FECHADA e a janela M+1 tem respostas. Semear respostas NPS
      com completed_at em M+1 (ex.: notas → média 4.97) e ZERO respostas em M.
      Chamar compute($user, $mesM). Asserir componentes.nps_medio ~= 4.97
      (assertEqualsWithDelta), e que a nota_final NÃO é a tankada (~3.5 com margem
      negativa), provando que lê M+1 e não M (que daria 0.0 → ~1.5).
    - Teste 2 (mês em curso exclui NPS): setTestNow dentro do mês corrente.
      Chamar compute($user, $mesCorrente). Asserir componentes.nps_medio === null
      (EXCLUÍDO, não 0.0) e componentes_disponiveis.nps_medio === false, e que a
      nota_final foi calculada só com os componentes financeiros disponíveis
      (não tankada por 0.0).
    - Teste 3 (fechada, janela M+1 JÁ encerrada, 0 respostas reais → 0.0):
      setTestNow tal que TANTO M quanto M+1 estejam totalmente no passado.
      ZERO respostas NPS em M+1. Asserir componentes.nps_medio === 0.0 (penaliza)
      e componentes_disponiveis.nps_medio === true.
    - Teste 4 (fechada, janela M+1 ainda EM COLETA, 0 respostas → null):
      setTestNow tal que M seja fechada mas M+1 seja o mês corrente (em coleta).
      ZERO respostas em M+1. Asserir componentes.nps_medio === null (EXCLUÍDO,
      não 0.0) e componentes_disponiveis.nps_medio === false.
  </behavior>
  <action>Escrever os 4 testes acima em RED (a implementação ainda lê a janela M). Espelhar o setUp de isolamento de tests/Feature/V18/DesempenhoPeriodoOficialTest.php (Http::preventStrayRequests + Http::fake no setUp; Carbon::setTestNow no início de cada teste e limpo no tearDown). Reusar helpers de fixture de usuário elegível/empresa/company_users desses testes existentes quando fizer sentido. NÃO instanciar providers reais. Cada teste que alcança compute() com empresa elegível DEVE ter Http::fake ativo. Rodar a suíte e confirmar que os 4 falham pelo motivo certo (lê janela M, retorna 0.0 onde deveria ler M+1/excluir).</action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test tests/Feature/V18/JanelaNpsBonusTest.php 2>&1 | grep -E "FAIL|Tests:" </automated>
  </verify>
  <done>Os 4 testes existem e falham (RED) por lerem a janela M / retornarem 0.0 onde a regra nova exige M+1 ou exclusão. Nenhum stray HTTP request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Deslocamento +1 no caminho fechado + mecânica exclui/0.0 + cache bump v5→v6</name>
  <files>app/Services/DesempenhoScoreService.php</files>
  <behavior>
    - compute() decide a janela de NPS por $periodo['is_closed'] (sinal canônico,
      disponível na linha 314 antes da linha 332):
        · Mês FECHADO (is_closed=true): janela de NPS = $mes+1
          ($mes->copy()->addMonthNoOverflow()). Chama computeNpsMedio($user, $mesNps).
          O financeiro (computeVarFaturamento/computeVarMargem) CONTINUA usando $mes.
        · Mês EM CURSO (is_closed=false): NPS EXCLUÍDO → $nps = null (D1).
          NÃO deslocar, NÃO 0.0.
    - Mecânica exclui-vs-penaliza no ramo fechado (usa a sentinela 0.0 de
      computeNpsMedio, já que média não-vazia é sempre >= 1.0):
        · Se computeNpsMedio($mesNps) == 0.0 (vazio) E a janela M+1 AINDA está em
          coleta (endOfMonth($mesNps) >= now(), i.e. M+1 não fechou) → $nps = null
          (EXCLUÍDO — não tanka; a competência ainda vai receber NPS).
        · Se computeNpsMedio($mesNps) == 0.0 E M+1 JÁ FECHOU (endOfMonth($mesNps) <
          now()) → mantém 0.0 (penaliza — houve 0 respostas de verdade).
        · Qualquer valor >= 1.0 → usa como está.
    - computeNpsMedio permanece INTOCADO (assinatura e corpo).
    - componentes_disponiveis['nps_medio'] deixa de ser hardcoded true e passa a
      refletir ($nps !== null) — a UI "Em curso"/janela-em-coleta pode esconder o
      card de NPS sem tankar a nota.
    - Cache bump: cacheKey() muda o prefixo de v5 para v6 com comentário histórico
      no mesmo padrão dos bumps anteriores (v1→v5), explicando que a REGRA de
      janela do componente NPS mudou e invalida todos os valores v5.
  </behavior>
  <action>Implementar a lógica do <behavior> em compute() (a partir da linha ~332), respeitando D1 (SÓ o caminho fechado desloca). Usar $periodo['is_closed'] como o sinal — NÃO recalcular "fechado" via now()/between. Deslocar com addMonthNoOverflow (evita o edge do dia 31). Manter o financeiro em $mes. Ajustar componentes_disponiveis['nps_medio'] para ($nps !== null) e atualizar o comentário adjacente (hoje diz "DESEMP-03 força 0.0 — nunca indisponível"). Bump v5→v6 em cacheKey() com bloco de comentário histórico explicando a mudança de régua da janela de NPS (v18.0 · Fase 105). NÃO tocar computeNpsMedio, resolvePeriodo, computeOficial, os componentes financeiros, nem o dual-path. Rodar a suíte do Task 1 até GREEN.</action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test tests/Feature/V18/JanelaNpsBonusTest.php 2>&1 | grep -E "OK|PASS|Tests:|FAIL"</automated>
  </verify>
  <done>Os 4 testes passam (GREEN). compute() desloca +1 só no caminho fechado, exclui no em-curso, distingue 0.0-real de exclusão-por-coleta. cacheKey usa v6. computeNpsMedio e os componentes financeiros inalterados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| régua do bônus (compute) | mudança altera valores pagos a pessoas reais — erro numérico tem impacto financeiro |
| cache Redis | valor errado servido por até 7 dias (mês fechado) se o bump for esquecido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Tampering | compute() janela de NPS | mitigate | 4 testes determinísticos travam os cenários; Http isolado; golden Felipe ~3.5 |
| T-105-02 | Information Disclosure | cache v5 órfão | mitigate | bump v5→v6; chaves v5 expiram por TTL (sem cache:clear) |
| T-105-03 | Repudiation | 0.0 vs null ambíguo | mitigate | sentinela 0.0 documentada; distinção coleta-vs-fechado por endOfMonth < now() |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test tests/Feature/V18/JanelaNpsBonusTest.php` — 4/4 verdes, zero stray HTTP.
- Regressões de âncora (Phase74/V16) NÃO são responsabilidade deste plano — ficam RED até o Plan 105-03 ajustar as fixtures (documentado como dependência).
</verification>

<success_criteria>
- Caminho fechado lê NPS de M+1; financeiro segue em M (NPSWIN-01).
- Mês em curso exclui NPS (null); exclui-vs-0.0 resolvido por estado da janela M+1 (NPSWIN-02).
- cacheKey em v6; componentes_disponiveis.nps_medio dinâmico.
- computeNpsMedio e componentes financeiros intocados.
</success_criteria>

<output>
Create `.planning/phases/105-correcao-janela-nps-bonus-competencia-v18-0/105-01-SUMMARY.md` when done.
</output>
