---
phase: 105-correcao-janela-nps-bonus-competencia-v18-0
plan: 03
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - app/Http/Controllers/NpsController.php
  - tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php
  - tests/Feature/Phase74/DesempenhoScoreServiceTest.php
  - tests/Feature/V16/BonusAtribuicoesNpsTest.php
  - tests/Feature/V16/BonusDualPathRegressaoTest.php
  - tests/Feature/V16/DesempenhoElegibilidadeTest.php
  - tests/Feature/V18/DesempenhoMetadadosCacheTest.php
autonomous: true
requirements: [NPSWIN-03, NPSWIN-04]
must_haves:
  truths:
    - "Invalidar/revalidar uma resposta NPS coletada em X busta o cache da competência X−1 (a que lê X como janela de NPS)"
    - "A âncora Carlos (Phase74) volta a verde com as fixtures de NPS na janela M+1 e golden recalculado/documentado"
    - "O dual-path do NPS (BonusDualPathRegressaoTest / BonusAtribuicoesNpsTest) preserva a atribuição — só a JANELA de leitura mudou"
    - "A suíte completa fica verde (nenhuma regressão remanescente do deslocamento +1)"
  artifacts:
    - path: "app/Http/Controllers/NpsController.php"
      provides: "bustarCacheDoBonus busta a competência = completed_month − 1"
      contains: "subMonthNoOverflow"
    - path: "tests/Feature/Phase74/DesempenhoScoreServiceTest.php"
      provides: "Âncora Carlos com NPS na janela M+1 + golden documentado"
      contains: "compute"
  key_links:
    - from: "NpsController::bustarCacheDoBonus"
      to: "DesempenhoScoreService::cacheKey"
      via: "competência = mês de completed_at − 1"
      pattern: "subMonthNoOverflow"
---

<objective>
Fechar as duas pontas restantes do deslocamento +1:
(1) O cache-bust do bônus (`NpsController::bustarCacheDoBonus`) deve invalidar a
competência CERTA — uma resposta coletada em X alimenta o bônus da competência
X−1 (que lê X como janela de NPS), então bustar a chave de X−1, não a de X.
(2) Recolocar as fixtures de NPS dos testes-âncora existentes (Phase74/V16) na
janela M+1 e recalcular/documentar o golden, provando que o dual-path e a régua
seguem íntegros — só a JANELA mudou.

Purpose: Sem (1), invalidar uma resposta NPS "não faz nada" no /performance por
até 7 dias na competência real. Sem (2), a suíte fica RED após a 105-01. MEXE NA
RÉGUA — rigor máximo, golden documentado.
Output: bust por competência, âncoras verdes, suíte completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-correcao-janela-nps-bonus-competencia-v18-0/105-CONTEXT.md
@.planning/phases/105-correcao-janela-nps-bonus-competencia-v18-0/105-RESEARCH.md
@app/Http/Controllers/NpsController.php
@app/Services/DesempenhoScoreService.php

<interfaces>
<!-- Achados do código — não reexplorar. -->

NpsController::bustarCacheDoBonus(NpsResponse $response, NpsSurvey $survey) (linha 1776):
  $mesCompletado = $survey->completed_at?->copy()->startOfMonth();
  foreach ($userIds as $userId) {
      Cache::forget($scoreService->cacheKey($userId, $mesCompletado));  ← chave errada
  }
  → PROBLEMA (descoberto nesta fase): pós-105-01 a competência M lê o NPS de M+1.
    Uma resposta com completed_at em X (=julho) alimenta a competência X−1 (=junho).
    Logo a chave a bustar é cacheKey($userId, $mesCompletado->subMonthNoOverflow()),
    NÃO a de $mesCompletado. O cacheKey já embute v6 (herda o bump da 105-01) —
    o helper continua sendo a fonte única, nada de string hardcoded aqui.
    Chamado por invalidar() (~1723) e revalidar() (~1754).

  Teste real do bust (Blocker 2 do plan-checker — NpsInvalidacaoCallSitesTest
  NÃO exercita bustarCacheDoBonus, chama update direto no model):
    tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php
      - test_invalidar_resposta_de_mes_fechado_com_atribuicao_busta_o_cache_do_bonus
        (linha ~232): completed_at = 2026-06-15 (X = junho). Hoje semeia
        Cache::put(sprintf('desempenho.compute.v5.%d.%s', id, '2026-06')) e assere
        forget dessa chave. Pós-fix a competência afetada é X-1 = MAIO e o prefixo
        é v6 → o Cache::put e o assert passam a usar
        sprintf('desempenho.compute.v6.%d.%s', id, '2026-05').
      - test_admin_revalida_restaura_flag_activity_e_cache_forget (linha ~331):
        idem — chave semeada/asserida vira v6 + 2026-05.

DesempenhoScoreService::computeNpsMedio filtra por nps_surveys.completed_at.
  ⚠ Sessão paralela (NPS 94-96) mexeu em NPS — o filtro invalidated_at já está
    em notasPorAtribuicao; completed_at segue a fonte (validar antes de codar).

Testes-âncora que quebram após a 105-01 (fixam completed_at NO MESMO mês da
competência testada — RESEARCH §6):
  - tests/Feature/Phase74/DesempenhoScoreServiceTest.php (âncora Carlos):
      setTestNow 2026-08-01 (linha 94); compute(carlos, 2026-07-01) (linha 550).
      Golden atual: nps_medio 4.25 / var_fat 3.00 / var_margem 4.50 / nota 4.42 /
      faixa 'basico'. Fixtures NPS em julho (linha 398, $mesYm.'-10').
      Também: test_nps_medio_e_zero... (linha 621) e test_nps_medio_e_media...
      (linha 640) fixam NPS no próprio mês da competência.
  - tests/Feature/V16/BonusAtribuicoesNpsTest.php e BonusDualPathRegressaoTest.php:
      idem — respostas no mesmo mês da competência.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: bustarCacheDoBonus busta a competência X−1 (não X)</name>
  <files>app/Http/Controllers/NpsController.php</files>
  <action>Em bustarCacheDoBonus (linha ~1776), a competência afetada por uma resposta coletada em $mesCompletado (=X) é X−1 (a competência que lê X como janela de NPS pós-105-01). Ajustar para bustar `$scoreService->cacheKey($userId, $mesCompletado->copy()->subMonthNoOverflow()->startOfMonth())`. Manter o uso do helper cacheKey() (nunca montar a chave à mão; herda v6). Atualizar o docblock do método explicando o motivo do −1 (regra da janela de NPS, Fase 105 · v18.0). Atualizar os DOIS testes de tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php (test_invalidar...linha ~232 e test_admin_revalida...linha ~331): a chave semeada via Cache::put e asserida via Cache::has passa de v5+2026-06 (X) para v6+2026-05 (X-1) — usar subMonthNoOverflow sobre o mês de completed_at e o prefixo v6. Este é um toque cirúrgico num arquivo COMPARTILHADO com a sessão paralela de NPS — commit atômico imediato só desta mudança; `git status` antes; `git add` só dos caminhos explícitos desta mudança (app/Http/Controllers/NpsController.php + tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php); se algum arquivo da sessão paralela aparecer staged, `git restore --staged` nele. Validar antes de codar que completed_at ainda é a coluna-fonte (grep rápido em notasPorAtribuicao/scopeValida).</action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php 2>&1 | grep -E "OK|PASS|FAIL|Tests:"</automated>
  </verify>
  <done>bustarCacheDoBonus busta a competência X−1 via helper cacheKey(); docblock explica o −1; commit atômico só do NpsController; NpsInvalidacaoRespostaTest verde com a chave v6 + competência X-1 (2026-05).</done>
</task>

<task type="auto">
  <name>Task 2: Recolocar fixtures de NPS na janela M+1 e recalcular/documentar o golden âncora</name>
  <files>tests/Feature/Phase74/DesempenhoScoreServiceTest.php, tests/Feature/V16/BonusAtribuicoesNpsTest.php, tests/Feature/V16/BonusDualPathRegressaoTest.php</files>
  <action>Para cada teste que exercita compute() com competência FECHADA e fixa respostas NPS no MESMO mês da competência, mover o completed_at das respostas para a janela M+1 (competência + 1 mês) — a régua e as notas NÃO mudam, só o mês do fixture, então o golden numérico deve permanecer o mesmo quando a janela M+1 estiver FECHADA. Escolher o setTestNow de cada teste de modo que a competência M seja fechada E a janela M+1 também esteja fechada (totalmente no passado) — assim o valor de NPS é determinístico e o comportamento 0.0-real vale (evita cair na regra de exclusão-por-coleta da 105-01). Na âncora Carlos: manter compute(carlos, 2026-07-01); ajustar setTestNow para depois do fim de agosto/2026 (ex.: 2026-09-01) e mover as respostas NPS de julho→agosto; confirmar que o golden (nps 4.25 / nota 4.42 / faixa 'basico') se mantém e DOCUMENTAR a conta num comentário no teste ("NPS agora lido de agosto = M+1 de julho; fixtures +1 mês; régua inalterada → golden idêntico"). Tratar os testes test_nps_medio_e_zero / test_nps_medio_e_media (Phase74) e os equivalentes V16 do mesmo jeito. Onde a semântica exigir (competência fechada mas M+1 ainda em coleta), ajustar a expectativa para null (exclusão) conforme a regra da 105-01, documentando. NÃO tocar código de produção neste task. Preservar as asserções de dual-path (atribuição/isolamento) — só a JANELA (mês do completed_at) muda.</action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test tests/Feature/Phase74/DesempenhoScoreServiceTest.php tests/Feature/V16/BonusAtribuicoesNpsTest.php tests/Feature/V16/BonusDualPathRegressaoTest.php 2>&1 | grep -E "OK|PASS|FAIL|Tests:"</automated>
  </verify>
  <done>Âncoras e dual-path verdes com fixtures na janela M+1; golden recalculado e a conta documentada em comentário; nenhuma mudança de código de produção.</done>
</task>

<task type="auto">
  <name>Task 3: Fallout ESPERADO da fase — remediar em 2 categorias (janela M+1 + versão de cache v6)</name>
  <files>tests/Feature/V16/DesempenhoElegibilidadeTest.php, tests/Feature/V18/DesempenhoMetadadosCacheTest.php, tests/Feature/Phase74/DesempenhoScoreServiceTest.php</files>
  <action>O plan-checker enumerou os testes que quebram como FALLOUT ESPERADO desta fase (NÃO são sinal a investigar). Remediar em DUAS categorias distintas, deixando claro em comentário que ambas são consequência planejada do deslocamento +1 e do bump v5→v6:

  CATEGORIA (a) — janela do fixture (mover completed_at → M+1): qualquer teste que fixe respostas NPS no MESMO mês da competência fechada. Aplicar o mesmo ajuste do Task 2 (mover completed_at +1 mês; escolher setTestNow onde M e M+1 estejam fechados; ajustar para null quando a semântica for exclusão-por-coleta). Documentar a conta.

  CATEGORIA (b) — literal de versão de cache (v5 → v6): renomear os literais 'desempenho.compute.v5' hardcoded nos asserts. Locais conhecidos (o checker achou todos):
    · tests/Feature/V16/DesempenhoElegibilidadeTest.php — test_cache_bumpado_para_v5 (~L362/392): renomear o método para ...v6, trocar o literal e a mensagem de assert para v6.
    · tests/Feature/V18/DesempenhoMetadadosCacheTest.php — ~L230, L256, L258, L275: trocar 'desempenho.compute.v5' → 'v6' (chaves fechada/operacional/current_month).
    · tests/Feature/V16/BonusDualPathRegressaoTest.php — ~L560 (já em files_modified): idem, literal + mensagem v6. (também é remediado no Task 2 pela janela; aqui só o literal de versão.)

  DEPOIS das duas categorias, rodar a suíte Feature inteira. Se sobrar QUALQUER falha que NÃO seja (a) janela de NPS nem (b) versão de cache — ex.: algo financeiro/elegibilidade/dual-path de atribuição — isso É sinal e deve ser investigado, NÃO silenciado. Editar os demais testes sob tests/Feature/ conforme necessário para (a)/(b), sempre com Http isolado.</action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --testsuite=Feature 2>&1 | grep -E "FAIL|Tests:|Failures:|Errors:"</automated>
  </verify>
  <done>`artisan test --testsuite=Feature` sem falhas; os literais v5→v6 enumerados foram renomeados (categoria b) e as fixtures de janela movidas (categoria a); ambas documentadas como fallout esperado; nenhuma falha não-(a)/(b) mascarada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache-bust do bônus | invalidação de resposta precisa atingir a competência real (X−1) |
| NpsController compartilhado | sessão paralela de NPS ativa no mesmo arquivo |
| golden dos testes-âncora | régua paga — golden errado esconde bug financeiro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-06 | Repudiation | bust na chave errada | mitigate | bustar X−1 via helper; NpsInvalidacaoRespostaTest (chave v6 + 2026-05) prova o forget |
| T-105-07 | Tampering | golden mascarando regressão | mitigate | mover só o mês do fixture; documentar que o número não muda; não silenciar quebra financeira |
| T-105-08 | Tampering | conflito com sessão paralela NPS | mitigate | git add explícito; git status antes; restore --staged de arquivo alheio; commit atômico |
</threat_model>

<verification>
- `artisan test --testsuite=Feature` sem falhas.
- Diff do NpsController toca só bustarCacheDoBonus (+docblock).
- Cada golden alterado tem comentário com a conta.
</verification>

<success_criteria>
- Cache-bust atinge a competência X−1 (NPSWIN-03).
- Âncora Carlos e dual-path verdes com janela M+1 e golden documentado (NPSWIN-04).
- Suíte Feature completa verde; nenhuma regressão financeira mascarada.
</success_criteria>

<output>
Create `.planning/phases/105-correcao-janela-nps-bonus-competencia-v18-0/105-03-SUMMARY.md` when done.
</output>
