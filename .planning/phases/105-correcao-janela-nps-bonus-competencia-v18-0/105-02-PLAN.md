---
phase: 105-correcao-janela-nps-bonus-competencia-v18-0
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - routes/console.php
  - app/Console/Commands/ConsolidarMesDesempenho.php
  - tests/Feature/V18/ConsolidarMesJanelaNpsTest.php
autonomous: true
requirements: [NPSWIN-03]
must_haves:
  truths:
    - "O cron desempenho:consolidar-mes congela no FIM do mês de coleta, não mais no dia 1"
    - "Ao rodar no fim de M+1, o cron consolida a competência M (= mês corrente − 1) cujo NPS acabou de ser coletado"
    - "O snapshot mensal gravado captura o NPS de M+1 (via compute deslocado da 105-01), não 0 respostas"
  artifacts:
    - path: "routes/console.php"
      provides: "Agendamento de consolidar-mes no último dia do mês (fim da coleta)"
      contains: "lastDayOfMonth"
    - path: "tests/Feature/V18/ConsolidarMesJanelaNpsTest.php"
      provides: "Teste do congelamento no fim do mês de coleta consolidando a competência certa com NPS de M+1"
      contains: "class ConsolidarMesJanelaNpsTest"
  key_links:
    - from: "routes/console.php"
      to: "desempenho:consolidar-mes"
      via: "lastDayOfMonth('14:00')"
      pattern: "lastDayOfMonth"
    - from: "ConsolidarMesDesempenho::handle"
      to: "DesempenhoScoreService::compute"
      via: "competência = today->subMonthNoOverflow (mês que acabou de coletar NPS)"
      pattern: "subMonthNoOverflow"
---

<objective>
Ajustar o timing do cron `desempenho:consolidar-mes` para congelar o snapshot
mensal no FIM do mês de coleta (D2), consolidando a competência = (mês corrente
− 1) cujo NPS acabou de ser coletado. Sem isso, aplicar o deslocamento +1 da
105-01 sem mudar o cron congelaria 0 NPS todo mês (Pitfall 3 do RESEARCH — pior
que o bug original).

Purpose: A janela de NPS de M só FECHA no fim de M+1 (junho fecha ~31/07). O cron
atual roda dia 1 de M+1 (14:00), quando quase nenhuma resposta de M+1 existe.
Mover pro último dia do mês faz o congelamento capturar a coleta completa.
Consequência aceita (D2): pagamento de junho desliza pro fim de julho/início de
agosto. SEM snapshot provisório + refechamento.
Output: cron reagendado, semântica de competência documentada, teste de fim-de-mês.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105-correcao-janela-nps-bonus-competencia-v18-0/105-CONTEXT.md
@.planning/phases/105-correcao-janela-nps-bonus-competencia-v18-0/105-RESEARCH.md
@app/Console/Commands/ConsolidarMesDesempenho.php
@routes/console.php

<interfaces>
<!-- Achados do código — não reexplorar. -->

routes/console.php (linhas 193-202): agendamento atual
  Schedule::command('desempenho:consolidar-mes')
    ->monthlyOn(1, '14:00')   ← MUDAR para ->lastDayOfMonth('14:00')
    ->timezone('America/Sao_Paulo')->name('desempenho-consolidar-mes')
    ->onOneServer()->withoutOverlapping();

ConsolidarMesDesempenho::handle() (linha 71): competência DEFAULT já é
  $mes = Carbon::today()->subMonthNoOverflow()->startOfMonth();
  → Rodando no ÚLTIMO DIA de julho, today.subMonth = junho ⇒ competência junho.
    A LÓGICA DE COMPETÊNCIA JÁ ESTÁ CORRETA para o novo timing — NÃO precisa
    mudar o cálculo de $mes. Só a DOCUMENTAÇÃO (docblock/description) descreve
    o timing antigo "dia 1 14:00" e precisa refletir "último dia do mês".
  - linha 97: $this->scoreService->compute($user, $mes)  ← já usa compute()
    (não computeCached), então herda o deslocamento +1 da 105-01 direto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Teste do congelamento no fim do mês de coleta consolidando a competência certa</name>
  <files>tests/Feature/V18/ConsolidarMesJanelaNpsTest.php</files>
  <behavior>
    Suíte em Tests\Feature\V18 com Http::preventStrayRequests() + Http::fake() no
    setUp (o command chama compute() → alcança AdmanMetricDiffService/providers).
    - Teste A (competência + captura de NPS de M+1): Carbon::setTestNow no ÚLTIMO
      dia de um mês M+1 (ex.: 31/07 14:05). Semear usuário elegível + empresa +
      financeiro (AdmanMetric) de competência M (junho). Semear respostas NPS com
      completed_at em M+1 (julho, dentro do mês que está fechando) → média
      determinística. Rodar `artisan desempenho:consolidar-mes` (sem --mes).
      Asserir: gravou DesempenhoScoreSnapshot com mes_referencia = M (junho, =
      today->subMonth), e que breakdown_json.componentes.nps_medio reflete o NPS
      de M+1 (não 0.0), provando que o congelamento no fim do mês captura a coleta.
    - Teste B (override --mes continua funcionando): rodar com --mes=YYYY-MM
      explícito e asserir idempotência (updateOrCreate não duplica em rerun).
  </behavior>
  <action>Escrever os testes acima. Espelhar o isolamento HTTP de tests/Feature/V18/DesempenhoPeriodoOficialTest.php. Usar $this->artisan('desempenho:consolidar-mes') e assertExitCode(0). Como o command usa compute() (não cache), o deslocamento da 105-01 se aplica — o teste depende da 105-01 já estar mesclada (depends_on). Rodar; em RED o Teste A ainda pode passar parcialmente (a competência já é today->subMonth), mas a asserção-chave é que nps_medio vem de M+1 — garantir que ela é significativa (semear 0 respostas em M e >0 em M+1 para que ler M daria 0.0 e ler M+1 dá o valor esperado).</action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test tests/Feature/V18/ConsolidarMesJanelaNpsTest.php 2>&1 | grep -E "FAIL|PASS|Tests:"</automated>
  </verify>
  <done>Testes existem e exercitam o congelamento no fim do mês; a asserção de NPS-de-M+1 é significativa (falharia se lesse M).</done>
</task>

<task type="auto">
  <name>Task 2: Reagendar cron para o fim do mês de coleta + documentar semântica nova</name>
  <files>routes/console.php, app/Console/Commands/ConsolidarMesDesempenho.php</files>
  <action>Em routes/console.php, trocar `->monthlyOn(1, '14:00')` por `->lastDayOfMonth('14:00')` no bloco `desempenho-consolidar-mes` (manter timezone/onOneServer/withoutOverlapping). Atualizar o comentário do bloco (linhas ~193-196) explicando a semântica nova v18.0 · Fase 105: "congela no ÚLTIMO DIA do mês de coleta (fim de M+1), consolidando a competência M cujo NPS acabou de ser coletado; pagamento desliza pro fim de M+1 — decisão D2, sem snapshot provisório". Em app/Console/Commands/ConsolidarMesDesempenho.php, atualizar SOMENTE o docblock de classe e o `$description` para refletir o novo timing (de "dia 1 14:00" para "último dia do mês, após a coleta de NPS de M+1 fechar"). NÃO alterar o cálculo de $mes (linha 71 já dá a competência certa: today->subMonthNoOverflow). NÃO tocar a lógica de snapshot/ranking. Confirmar os testes do Task 1 verdes.</action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test tests/Feature/V18/ConsolidarMesJanelaNpsTest.php 2>&1 | grep -E "OK|PASS|Tests:|FAIL"</automated>
  </verify>
  <done>routes/console.php usa lastDayOfMonth('14:00'); docblocks refletem o timing novo; cálculo de competência inalterado; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron de congelamento | grava o snapshot que vira a régua paga do bônus mensal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-04 | Tampering | timing do consolidar-mes | mitigate | teste com setTestNow no último dia prova competência+captura de NPS de M+1 |
| T-105-05 | Denial of Service | congelar 0 NPS todo mês (Pitfall 3) | mitigate | lastDayOfMonth garante coleta completa antes de congelar |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test tests/Feature/V18/ConsolidarMesJanelaNpsTest.php` verde.
- `C:\xampp\php\php.exe artisan schedule:list 2>&1 | grep consolidar-mes` mostra o novo agendamento (último dia do mês).
</verification>

<success_criteria>
- Cron congela no fim do mês de coleta (NPSWIN-03).
- Competência consolidada = mês corrente − 1, capturando o NPS de M+1.
- Override --mes e idempotência preservados.
</success_criteria>

<output>
Create `.planning/phases/105-correcao-janela-nps-bonus-competencia-v18-0/105-02-SUMMARY.md` when done.
</output>
