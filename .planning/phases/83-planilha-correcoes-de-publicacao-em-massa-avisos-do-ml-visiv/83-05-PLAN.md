---
phase: 83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv
plan: 05
type: execute
wave: 4
depends_on: ["83-03", "83-04"]
files_modified: []
autonomous: false
requirements: [FIX-83-1, FIX-83-2, FIX-83-3, FIX-83-4, FIX-83-5, FIX-83-6]

must_haves:
  truths:
    - "Os gates da fase estão verdes: npm run build, npm run test:js e a suíte PHPUnit"
    - "Nenhum arquivo fora do escopo da fase foi tocado (nenhum .php, nenhum AnunciarML.jsx)"
    - "O publicador confirmou em produção que os 6 requisitos funcionam"
  artifacts:
    - path: ".planning/phases/83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv/83-05-SUMMARY.md"
      provides: "Varredura final + roteiro de verificação em produção + resultado do checkpoint"
  key_links:
    - from: "83-05-SUMMARY.md"
      to: "os 6 requisitos FIX-83"
      via: "tabela de rastreabilidade requisito → onde foi entregue"
      pattern: "FIX-83"
---

<objective>
Fechar a fase: varredura de consistência, gates verdes e o checkpoint visual — que, como na Phase 82,
**só existe em produção**.

Purpose: os planos 01-04 mexeram em estado assíncrono, canvas e coerção de entrada. Os testes provam
as funções puras; os gates estruturais provam a fiação. O que nenhum comando prova é a experiência:
publicar 4 anúncios de verdade e ver a fila drenar linha a linha. Essa prova depende de uma conta ML
conectada, e não existe nenhuma no ambiente local.

Output: `83-05-SUMMARY.md` com a varredura, o bundle final e o roteiro de verificação; e o resultado
do checkpoint depois que o usuário rodar o roteiro em produção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv/83-01-SUMMARY.md
@.planning/phases/83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv/83-02-SUMMARY.md
@.planning/phases/83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv/83-03-SUMMARY.md
@.planning/phases/83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv/83-04-SUMMARY.md
@.planning/phases/82-planilha-excel-like-na-grade-de-anuncio-em-massa-glide-data-/82-07-SUMMARY.md
</context>

<contexto_verificado>
Fatos confirmados — não re-investigar:

1. **O checkpoint NÃO é verificável em localhost.** O banco local tem **zero** empresas com
   `ml_token` (medido na Phase 82: `Company::whereHas('mlToken')->count()` = 0) e não há OAuth do ML
   aqui — o painel vem vazio e a grade nem abre. **Não instrua ninguém a rodar `artisan serve` para
   ver a grade**: é um beco sem saída conhecido. A verificação só existe em produção.
2. **Deploy é decisão e ação do usuário** (CLAUDE.md: "Não executar deploy sem autorização explícita").
   Não rode `deploy.sh`, `deploy_parcial.sh` nem `deploy_run.sh`. Não faça push para a VPS.
3. **3 falhas em `Phase75` são pré-existentes e de ambiente**, não da fase: `cURL error 60: SSL
   certificate` batendo em `https://api.mercadolibre.com/oauth/token` a partir desta máquina Windows
   (medido e documentado no `82-07-SUMMARY.md`). Se aparecerem, cite o precedente; não "conserte".
4. **Esta fase é 100% frontend.** Nenhuma migration, nenhuma rota, nenhum controller, nenhum job,
   nenhum pacote. `git diff --name-only` não deve listar **nenhum** `.php` de aplicação.
</contexto_verificado>

<tasks>

<task type="auto">
  <name>Task 1: Varredura final e gates da fase</name>
  <files>nenhum arquivo de aplicação — só leitura e o SUMMARY</files>
  <action>
    Rode os gates e registre a saída real de cada um (número, não adjetivo):

    - `npm run build` — verde. Registre o tempo e o tamanho dos chunks `AnunciarMassa-*.js` e
      `GradeAnuncioGlide-*.js`, comparando com a linha de base do `82-07-SUMMARY.md`
      (`AnunciarMassa` 17.57 kB, `GradeAnuncioGlide` 392.67 kB / 134.33 gzip). Um crescimento
      pequeno em `AnunciarMassa` é esperado (o painel do plano 04). Um salto grande em
      `GradeAnuncioGlide` seria sinal de que algo entrou no bundle sem querer — investigue antes de
      fechar.
    - `npm run test:js` — verde, `# fail 0`. Registre a contagem de testes.
    - `C:\php\php.exe artisan test --filter=Phase82` — deve seguir **9/9**. Use o PHP 8.5 de
      `C:\php\php.exe` (o do XAMPP é 8.2 e o projeto exige ≥8.3).
    - `C:\php\php.exe artisan test` — suíte completa. As 3 falhas de `Phase75` por SSL são
      pré-existentes (item 3 do `<contexto_verificado>`); qualquer OUTRA falha é regressão e bloqueia
      a fase.

    Varredura de consistência (leia, não presuma):
    - `git diff --name-only` — confirme que só aparecem: `gradeMassaUtils.js`, `AnunciarMassa.jsx`,
      `GradeAnuncioGlide.jsx`, `package.json`, `tests/js/*` e os artefatos de `.planning/`. **Nenhum
      `.php`. Nenhum `AnunciarML.jsx`** (o wizard estava fora do escopo de propósito — ver plano 04,
      `<contexto_verificado>` item 5).
    - Confirme que nenhuma dependência entrou: `git diff package.json` deve mostrar só a linha do
      script `test:js`; `package-lock.json` não deve aparecer no diff.
    - Restos de migração: nenhum `console.log` esquecido; nenhum `TODO`/`FIXME` novo; nenhum
      `dropdownComDelete` (o wrapper que o research sugeriu e que o plano 03 provou ser código morto);
      nenhuma prop `onDelete` no `<DataEditor>`.
    - Comentários: os cabeçalhos de `AnunciarMassa.jsx` e `GradeAnuncioGlide.jsx` continuam
      descrevendo a divisão de responsabilidade (página dona do estado × grade que só desenha) e
      agora precisam mencionar o merge/polling. Se o comportamento mudou e o comentário não, corrija
      o comentário — em pt-BR.

    Monte no SUMMARY a tabela de rastreabilidade requisito → onde foi entregue, com os 6 IDs
    (FIX-83-1 … FIX-83-6), marcando cada um como "✅ código + gates" ou "⏳ aguarda checkpoint".
  </action>
  <verify>
    <automated>npm run build && npm run test:js</automated>
  </verify>
  <done>Os 4 gates rodados com saída registrada; `git diff --name-only` sem `.php` e sem `AnunciarML.jsx`; tabela de rastreabilidade dos 6 requisitos no SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual em produção — os 6 requisitos (FIX-83-1 a FIX-83-6)</name>
  <files>nenhum arquivo — verificação humana em produção</files>
  <action>Pare a execução e apresente ao usuário o roteiro de verificação descrito abaixo, na íntegra. NÃO deployar — o deploy é decisão e ação dele (CLAUDE.md). NÃO tentar verificar em localhost: o banco local tem zero empresas com `ml_token` e a grade nem abre. Aguarde o sinal de retomada antes de fechar a fase.</action>
  <what-built>
    Os planos 01-04 entregaram, todos em frontend (nenhuma rota, migration ou pacote novo):

    - **FIX-83-1** — `publicarLote` destrava o botão em `finally` (o caminho de sucesso nunca
      destravava: o `setPublicandoLote(false)` só existia dentro do `catch`).
    - **FIX-83-2** — a causa raiz: `AnunciarMassa.jsx` lia a prop `rascunhos` só no mount. Agora um
      `useEffect([rascunhos])` faz merge por `id` de volta para `abas[].linhas[]` (só campos de
      servidor — nunca o que você está digitando), alimentado por polling de 3s idêntico ao que já
      roda no wizard, com teto de segurança. Cada linha mostra o resultado: ✓ verde publicado, ✕
      vermelho erro, ↑ publicando.
    - **FIX-83-3** — painel abaixo da grade com o erro completo do ML expansível (`<details>`) e
      botão "Copiar erro".
    - **FIX-83-4** — segunda seção do painel, âmbar, listando quais linhas têm avisos do ML e qual é
      cada aviso.
    - **FIX-83-5** — preço aceita `129,99` (e `1.234,56`), normalizado no ponto único de escrita da
      grade.
    - **FIX-83-6** — (a) Delete limpa as células selecionadas em todas as colunas; (b) "Remover
      categoria" move as linhas para "Sem categoria" — nada é apagado.

    Gates verdes: `npm run build`, `npm run test:js`, `php artisan test --filter=Phase82` 9/9.
    **Nada foi deployado** — o deploy é decisão e ação sua.
  </what-built>
  <how-to-verify>
    **Este roteiro só roda em produção** (`https://admin.ecfconsultoria.com.br`). O ambiente local
    tem zero empresas com conta ML conectada — a grade não abre lá. Depois do SEU deploy, em
    `/mlb/anuncios` → aba "Em massa", numa empresa com conta ML:

    **O bug principal (FIX-83-1 + FIX-83-2) — comece por aqui:**
    1. Monte **4 linhas publicáveis** (título, preço, estoque e os obrigatórios da categoria).
    2. Clique em **"Publicar 4 em lote"** e **não saia da tela**.
    3. Esperado: o botão **destrava** (não fica girando para sempre — este era o FIX-83-1); aparece
       "4 anúncio(s) enfileirado(s)"; as 4 linhas mostram **↑ amarelo**.
    4. Em ~3 a 15s, sem você tocar em nada e **sem recarregar a página**: cada linha vira **✓ verde
       (publicado)** ou **✕ vermelho (erro)**, uma a uma, conforme a fila drena. O contador
       "Publicar N em lote" cai até 0. **Este é o coração da fase** — se as linhas ficarem em ↑ para
       sempre, o bug não foi consertado.
    5. Recarregue a página: as linhas **publicadas somem** da grade. **Isso é correto, não é bug** —
       elas não são mais rascunhos abertos (o backend só devolve rascunho em aberto). O ✓ verde é
       uma confirmação da sessão.

    **FIX-83-3 — erro do ML legível:**
    6. Numa linha que deu **✕**, olhe o painel **abaixo da grade**: "N linha(s) falharam ao publicar".
       Clique na seta para expandir → o retorno **íntegro** do ML aparece, sem cortar.
    7. Clique em **"Copiar erro"** e cole no bloco de notas: o texto completo tem que estar lá.

    **FIX-83-4 — avisos do ML:**
    8. Clique em **"Validar tudo"**. Se a `PublishBar` disser "N com avisos do ML", o painel abaixo
       da grade agora tem uma seção **âmbar** listando **quais** linhas e **quais** avisos ("Linha 4
       · {categoria} — 2 aviso(s)"; ao expandir: campo → mensagem).
    9. Confirme a distinção: **vermelho = falhou de verdade**, **âmbar = aviso que não bloqueia**. As
       duas seções não podem se parecer.

    **FIX-83-5 — preço com vírgula:**
    10. Digite `129,99` na coluna **Preço** e saia da célula. Esperado: vira `129.99`, a linha **não**
        fica vermelha reclamando de preço, e o contador de publicáveis a considera.
    11. Cole uma coluna de preços do Excel no formato BR (ex.: `1.234,56`). Esperado: entram como
        números válidos.

    **FIX-83-6a — Delete:**
    12. Selecione um bloco de células em **Título, Preço, Estoque, SKU, Peso** (as que **não**
        funcionavam) e aperte **Delete**. Esperado: limpam. Antes desta fase, não acontecia nada.
    13. Delete em **GTIN** e **Descrição** (que já funcionavam) — continuam limpando.
    14. Delete na coluna **Tipo**: volta para **Clássico** (o padrão).
    15. Espere ~1s e recarregue: o que você apagou continua apagado (o autosave pegou).

    **FIX-83-6b — remover categoria:**
    16. Numa aba de categoria **com linhas**, clique em **"Remover categoria"**. Esperado: pergunta
        confirmando que as N linhas vão para "Sem categoria" e que **nada é apagado**.
    17. Confirme. Esperado: a aba some, as linhas aparecem na aba **"Sem categoria"** com os dados
        intactos, e a `PublishBar` passa a mostrar "N sem categoria" (elas saem do lote — o ML
        recusaria anúncio sem categoria).
    18. Recarregue a página: as linhas **continuam** em "Sem categoria" (o `category_id: null`
        persistiu). Nenhum rascunho sumiu.
    19. Numa aba **vazia**, clique em "Remover categoria": some direto, sem perguntar.

    **Zero regressão (o que a Phase 82 entregou e não pode ter quebrado):**
    20. Colar do Excel, alça de preenchimento (fill handle), seleção multi-retângulo, Tab/Enter/setas,
        dropdown de Tipo e de Gênero, bolinha de origem (violeta → âmbar ao editar), autosave,
        troca de aba de categoria, "Gerar EAN-13" e "Remover" na toolbar — tudo como antes.
  </how-to-verify>
  <resume-signal>
    Responda "aprovado" ou descreva o que divergiu — de preferência apontando o número do passo.
    Se o passo 4 falhar (linhas presas em ↑), diga se o painel de erro mostrou algo e se a fila do
    servidor drenou (`supervisorctl status ecf-worker:*`) — é a informação que separa "o polling não
    liga" de "o job não roda".
  </resume-signal>
  <verify>
    <human-check>O usuário roda o roteiro em produção e responde "aprovado" ou lista as divergências por número de passo. Não há alternativa automatizada: canvas não expõe DOM inspecionável e não existe conta ML conectada fora de produção.</human-check>
  </verify>
  <done>O usuário aprovou os 6 requisitos em produção, ou as divergências foram registradas no SUMMARY para virarem gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Código verificado localmente → produção | O único ambiente onde esta fase pode ser provada é o de produção, com conta ML real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-14 | Tampering | Deploy não autorizado para validar a fase | mitigate | O plano proíbe explicitamente rodar `deploy*.sh` e dar push para a VPS; o deploy é ação do usuário (CLAUDE.md). O checkpoint é `gate="blocking"` e não pode ser auto-aprovado |
| T-83-15 | Repudiation | Fase declarada pronta sem prova real | mitigate | O checkpoint humano é bloqueante e enumera os 6 requisitos passo a passo; os gates automatizados provam as funções puras e a fiação, mas o SUMMARY registra explicitamente o que só o olho humano em produção pode fechar |
| T-83-16 | Denial of Service | Publicação de teste real criando anúncios indesejados no ML do cliente | accept | O roteiro usa o fluxo normal de publicação da ferramenta, na empresa que o usuário escolher; apagar rascunho não remove anúncio publicado no ML (ressalva já documentada em `excluirRascunho`, controller L331). O usuário decide em qual empresa testar |
| T-83-SC | Tampering | npm/composer install | N/A | Nenhum pacote instalado em toda a fase |
</threat_model>

<verification>
- `npm run build` — verde, com os tamanhos de chunk registrados e comparados com a Phase 82
- `npm run test:js` — verde, `# fail 0`
- `C:\php\php.exe artisan test --filter=Phase82` — 9/9
- `C:\php\php.exe artisan test` — só as 3 falhas pré-existentes de SSL em `Phase75`
- `git diff --name-only` — sem `.php`, sem `AnunciarML.jsx`, sem `package-lock.json`
- Checkpoint humano em produção — aprovado pelo usuário
</verification>

<success_criteria>
- Todos os gates verdes e registrados com número no SUMMARY
- Tabela de rastreabilidade cobrindo FIX-83-1 … FIX-83-6
- Nada deployado por agente; nenhuma dependência nova; nenhum `.php` tocado
- Checkpoint humano em produção aprovado (ou as divergências registradas para virarem gaps)
</success_criteria>

<output>
Crie `.planning/phases/83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv/83-05-SUMMARY.md` ao terminar.
Registre: saída real de cada gate, delta de bundle vs. Phase 82, tabela de rastreabilidade dos 6
requisitos, o roteiro de verificação e o resultado do checkpoint.
</output>
</content>
