---
phase: 83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv
plan: 03
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - resources/js/Pages/Mlb/GradeAnuncioGlide.jsx
  - tests/js/estrutura-grade-glide.test.js
autonomous: true
requirements: [FIX-83-2, FIX-83-5, FIX-83-6]

must_haves:
  truths:
    - "Cada linha mostra o resultado real: publicado, erro de publicação, publicando, aviso do ML ou erro local"
    - "A tecla Delete limpa o conteúdo das células selecionadas em TODAS as colunas, não só nas de texto puro"
    - "Delete passa pelos callbacks da página (o autosave dispara sozinho), nunca escreve direto no estado"
    - "Digitar ou colar 129,99 na coluna Preço grava 129.99 no estado da linha"
  artifacts:
    - path: "resources/js/Pages/Mlb/GradeAnuncioGlide.jsx"
      provides: "Glifos de status do servidor, onDelete no origemCellRenderer, normalização de preço no onCellsEdited"
    - path: "tests/js/estrutura-grade-glide.test.js"
      provides: "Gates estruturais da grade (fonte sem comentários)"
  key_links:
    - from: "resources/js/Pages/Mlb/GradeAnuncioGlide.jsx"
      to: "gradeMassaUtils.normalizarPreco"
      via: "chamada dentro do onCellsEdited"
      pattern: "normalizarPreco"
    - from: "origemCellRenderer"
      to: "deleteRange da lib"
      via: "propriedade onDelete do custom cell renderer"
      pattern: "onDelete"
---

<objective>
A grade passa a **mostrar o que aconteceu** e a **se comportar como planilha** em dois pontos que
faltaram na Phase 82.

Três entregas:
1. **Status por linha (FIX-83-2)** — o glifo da coluna de status ganha os dois estados terminais que
   não existiam: publicado (✓ verde) e erro real de publicação (✕ vermelho). Sem eles, o merge do
   plano 02 chega ao estado e não aparece na tela.
2. **Delete em todas as colunas (FIX-83-6a, per D-06)** — hoje Delete não faz nada em 8 das 10
   colunas base.
3. **Preço com vírgula (FIX-83-5, per D-05)** — plugar o `normalizarPreco` do plano 01 no ponto único
   de escrita da grade.

Purpose: este plano é o "olho" da fase — o plano 02 conserta o estado, este mostra o estado. Os dois
juntos fecham FIX-83-2.

Output: `GradeAnuncioGlide.jsx` com a cascata de glifos ampliada, `onDelete` no renderer de origem, e
a coerção de preço no `onCellsEdited` — mais os gates estruturais da grade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv/83-RESEARCH.md
@.planning/phases/83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv/83-01-SUMMARY.md
@resources/js/Pages/Mlb/GradeAnuncioGlide.jsx
@resources/js/Pages/Mlb/gradeMassaUtils.js
</context>

<decisoes_travadas>
Decisões do usuário — NÃO reabrir. Citadas por ID nas tarefas:

- **D-05** — Preço com vírgula: coerir em `onCellsEdited` (ponto único de escrita da grade), NÃO em
  `montarPayloadLinha` — o autosave já chama `montarPayloadLinha` e persistiria `price: null`
  silenciosamente. Aceitar `129,99` e `129.99`.
- **D-06** — Delete: fazer funcionar em **TODAS** as colunas, **passando pelos callbacks da página**
  (`onEditarCelula`), nunca escrevendo direto no estado.
</decisoes_travadas>

<contexto_verificado>
Lido no código-fonte real da lib instalada nesta sessão. **O research acertou o diagnóstico mas
errou a extensão do conserto — o conserto é menor do que ele propôs.** Não re-investigue; não siga
a "Opção A" do research ao pé da letra:

1. **`deleteRange` (`glide-data-grid/dist/esm/data-editor/data-editor.js:1766-1797`)**, célula a célula:
   - L1772: `if (!cellValue.allowOverlay && cellValue.kind !== Boolean) continue;` → **a coluna de
     status (`_st`, `allowOverlay: false`) é PULADA**. Não há risco de o Delete escrever um campo
     `st` fantasma na linha. Não precisa de guarda para isso.
   - L1778: se o custom renderer tem **`onDelete`**, usa ele.
   - L1781: **senão**, se o `provideEditor` devolve um objeto (`has(obj,'editor')` → sim), usa
     **`editor.deletedValue?.(cell)`**.
   - L1790: só entra no `editList` se `newVal !== undefined`. Custom conta como editável
     (`isEditableGridCell` só exclui Loading/Bubble/RowID/Protected/Drilldown).
   - L1795: o `editList` vai para `mangledOnCellsEdited` → **cai no `onCellsEdited` desta grade** →
     `onEditarCelula` → estado da página → autosave. É exatamente o que D-06 pede: nada de escrita
     direta no estado, nada de `onDelete` no nível do `<DataEditor>`.
2. **`onDelete` no nível do `<DataEditor>` NÃO é necessário** (L1836: `onDelete?.(gridSelection) ?? true`
   — sem a prop, o default já é deletar). Não adicione a prop; seria a Opção B do research, mais
   código para o mesmo efeito.
3. **`DropdownCell` JÁ suporta Delete.** `glide-data-grid-cells/dist/esm/cells/dropdown-cell.js:183-192`:
   o `provideEditor` devolve `deletedValue: v => ({...v, copyData: '', data: {...v.data, value: ''}})`.
   **Não crie o wrapper `dropdownComDelete`** que o research sugeriu — é código morto: a lib já cai no
   `deletedValue` no L1781. `RENDERERS` fica como está.
4. **`origemCellRenderer` (`GradeAnuncioGlide.jsx:146-149`)** devolve `{editor: EditorOrigem,
   disablePadding: true}` — **sem `deletedValue` e sem `onDelete`** → `newVal` fica `undefined` → a
   célula nunca entra no `editList`. **Esta é a lacuna real**, e cobre as 8 colunas de
   `COLS_COM_ORIGEM` (title, sku, price, estoque, pesoG, alturaCm, larguraCm, comprimentoCm).
5. **`GridCellKind.Text` já funciona** (`text-cell.js:45-48`: `onDelete: c => ({...c, data: ''})`) —
   é por isso que Delete já limpa `gtin`, `descricao` e atributos de texto livre hoje.
6. **A segunda lacuna, que o research não viu:** com o `deletedValue` do DropdownCell, a coluna
   **Tipo** já emite hoje uma edição com `data.value = ''` — mas o `onCellsEdited` desta grade a
   descarta: `normalizarTipoAnuncio('')` devolve `null` e o `if (cod)` (L423) engole a edição. Ou
   seja, Delete no Tipo não faz nada **por causa do nosso código**, não da lib. Atributos `list`
   (Gênero etc.) já funcionam: caem em `onEditarCelula(uid, attrId, '', {attr:true})`.

Resumo do que FIX-83-6a exige de verdade: **`onDelete` no `origemCellRenderer` + tratar o vazio na
coluna Tipo.** Mais nada.

7. **A prop `rascunhos` / o estado da linha:** o plano 01 acrescentou `statusServidor`, `erroResumo`,
   `erroCompleto` e `publicando` ao `linhaVazia()`. O plano 02 os preenche. Esta grade só os LÊ.
</contexto_verificado>

<tasks>

<task type="auto">
  <name>Task 1: Glifo e realce dos estados terminais publicado/erro (FIX-83-2)</name>
  <files>resources/js/Pages/Mlb/GradeAnuncioGlide.jsx, tests/js/estrutura-grade-glide.test.js</files>
  <action>
    Duas mudanças, ambas lendo campos que o plano 02 já preenche (`linha.statusServidor`).

    (a) **Cascata de glifos** — no `getCellContent`, bloco `if (coluna._st)` (L255-274). A cascata de
    hoje (`publicando ↑` → `salvando/validando ⋯` → `erro local !` → `aviso ML ◐` → `válido ✓` →
    `salvo ✓`) ganha **dois ramos novos no topo**, sem alterar nenhum dos existentes (SHEET2-07):
    - `linha.statusServidor === 'publicado'` → glifo `✓`, cor `#22c55e` (green-500 — tom mais forte
      que o `#34d399` do "válido no ML", que continua onde está)
    - `linha.statusServidor === 'erro'` → glifo `✕`, cor `#ef4444`
    Ordem final: publicado → erro (servidor) → publicando → salvando/validando → erro local → aviso
    ML → válido → salvo. Estados terminais primeiro: são a verdade final sobre a linha; um erro local
    detectado depois de publicada não desfaz a publicação.

    Por que `✕` e não `!`: per Pitfall 3 do research, "erro real de publicação" (o POST /items
    falhou de fato) e "erro local" (campo obrigatório vazio, ainda nem tentou) são coisas diferentes
    e não podem dividir o mesmo símbolo. `!` continua sendo erro local; `✕` é falha real. O motivo
    legível de cada `✕` vem no painel do plano 04 — canvas não hospeda tooltip.

    (b) **Realce da linha** — no `getRowThemeOverride` (L348-358), mesma lógica de precedência,
    também só acrescentando ramos no topo:
    - `statusServidor === 'publicado'` → `{ bgCell: 'rgba(34,197,94,0.06)', borderColor: 'rgba(34,197,94,0.35)' }`
    - `statusServidor === 'erro'` → `{ bgCell: 'rgba(239,68,68,0.10)', borderColor: 'rgba(239,68,68,0.5)' }`
      (mais forte que o vermelho de erro local, que fica intacto logo abaixo)
    Os dois ramos existentes (erro local vermelho, aviso ML âmbar) não mudam.

    Cuidado com dependências: `getCellContent` tem deps `[colunas, aba.linhas, aba]` e
    `getRowThemeOverride` tem `[aba]` — não acrescente dependência nova, os campos novos vêm dentro
    de `aba.linhas`. O comentário do arquivo explica por que as deps são mínimas de propósito (a lib
    chama `getCellContent` centenas de vezes por segundo no scroll); não afrouxe isso.

    Crie `tests/js/estrutura-grade-glide.test.js` importando `lerSemComentarios` de `./_fonte.js`
    (plano 01) e asserte sobre a fonte **sem comentários** de `GradeAnuncioGlide.jsx`: contém
    `statusServidor`, contém `'publicado'`, contém `'erro'`, contém `#22c55e`. Um `test()` por
    grupo, mensagens em pt-BR. Lê sem comentários de propósito — a prosa em pt-BR deste arquivo cita
    os identificadores e um `grep -c` cru passaria pelo motivo errado.
  </action>
  <verify>
    <automated>npm run test:js</automated>
  </verify>
  <done>`npm run test:js` verde. `npm run build` verde. Os 6 ramos antigos da cascata de glifos e os 2 ramos antigos do realce de linha estão idênticos; só houve acréscimo no topo de cada um.</done>
</task>

<task type="auto">
  <name>Task 2: Delete funcional em todas as colunas (FIX-83-6a, D-06)</name>
  <files>resources/js/Pages/Mlb/GradeAnuncioGlide.jsx, tests/js/estrutura-grade-glide.test.js</files>
  <action>
    Duas mudanças cirúrgicas — leia `<contexto_verificado>` antes: o conserto é **menor** do que o
    research propôs, e fazer a mais é criar código morto.

    (a) **`onDelete` no `origemCellRenderer`** (o objeto que começa em L112). Acrescente a
    propriedade `onDelete: (cell) => ({ ...cell, copyData: '', data: { ...cell.data, texto: '' } })`.
    Isso cobre as 8 colunas de `COLS_COM_ORIGEM` (Título, SKU, Preço, Estoque, Peso, Altura, Largura,
    Comprimento) — as que hoje não respondem ao Delete. `copyData: ''` acompanha o padrão da própria
    lib (é o que o `deletedValue` do DropdownCell faz). Preserve `origem` e `alinharDireita` e `max`
    dentro de `data` — só o `texto` é apagado; a bolinha de origem não é conteúdo digitado.

    **Não** adicione a prop `onDelete` ao `<DataEditor>` e **não** crie wrapper para o `DropdownCell`
    — a lib já cobre os dois casos (L1836 e o `deletedValue` do dropdown). `RENDERERS` não muda.

    Por que isto satisfaz D-06 sem escrever no estado: o `onDelete` do renderer só devolve a célula
    vazia; quem monta o `editList` e chama `onCellsEdited` é a própria lib (`deleteRange` L1790-1795),
    e o `onCellsEdited` desta grade já delega tudo para `onEditarCelula` da página — o autosave com
    debounce dispara sozinho, sem nenhuma mudança extra.

    (b) **Tipo (coluna `tier`) aceita o vazio.** No `onCellsEdited`, no ramo de `dropdown-cell` para
    `coluna.id === 'tier'` (L420-424): hoje `normalizarTipoAnuncio(escolhido)` devolve `null` para
    string vazia e o `if (cod)` engole a edição — Delete no Tipo não faz nada. Trate o vazio antes:
    se `escolhido` for string vazia (após trim), chame
    `onEditarCelula(linha.uid, 'tier', 'gold_special', { chaveOrigem: chaveOrigem('tier') })` e siga
    (`continue`). `gold_special` (Clássico) é o mesmo default de `linhaVazia()` e o mesmo fallback
    que `montarPayloadLinha` já aplica (`listing_type_id: l.tier || 'gold_special'`) — apagar o Tipo
    volta ao padrão, não deixa a linha sem tipo.

    **Não** mexa no comportamento de texto não reconhecido: `normalizarTipoAnuncio('bla')` continua
    devolvendo `null` e a edição continua sendo ignorada (tolerância deliberada de hoje, SHEET2-07).
    A diferença é só o vazio, que agora tem significado ("limpar").

    Acrescente ao gate estrutural: a fonte sem comentários de `GradeAnuncioGlide.jsx` contém
    `onDelete`; **não** contém `dropdownComDelete` (gate anti-código-morto); e o `RENDERERS` segue
    sendo `[DropdownCell, origemCellRenderer]`.
  </action>
  <verify>
    <automated>npm run test:js</automated>
  </verify>
  <done>`npm run test:js` verde. `npm run build` verde. `origemCellRenderer` tem `onDelete`; o ramo `tier` do `onCellsEdited` trata string vazia; nenhum wrapper de DropdownCell foi criado; `<DataEditor>` não ganhou prop `onDelete`.</done>
</task>

<task type="auto">
  <name>Task 3: Preço aceita vírgula no ponto único de escrita (FIX-83-5, D-05)</name>
  <files>resources/js/Pages/Mlb/GradeAnuncioGlide.jsx, tests/js/estrutura-grade-glide.test.js</files>
  <action>
    Importe `normalizarPreco` de `@/Pages/Mlb/gradeMassaUtils` (junto dos helpers que já vêm de lá) e
    aplique-o **dentro do `onCellsEdited`** (per D-05).

    Local exato: logo depois da linha que calcula `const valor = (cel?.kind === GridCellKind.Custom
    && cel.data?.kind === 'origem-cell') ? String(cel.data.texto ?? '') : String(cel?.data ?? '')`
    (L435-437), defina `const valorNormalizado = coluna.id === 'price' ? normalizarPreco(valor) : valor;`
    e use `valorNormalizado` na chamada final `onEditarCelula(linha.uid, coluna.id, valorNormalizado,
    { chaveOrigem: chaveOrigem(coluna.id) })` (L459).

    Os outros ramos não precisam do valor normalizado e não devem ser mexidos — vale conferir e
    deixar registrado no comentário: o ramo de atributo (`coluna._attr`) tem `coluna.id` no formato
    `attr:XXX`, nunca `price`; o ramo de `alturaCm` idem. `price` só chega na chamada final.

    Por que aqui e não no `montarPayloadLinha` (D-05, com o motivo por extenso no comentário em
    pt-BR): o autosave chama `montarPayloadLinha` a cada 600ms de digitação — com `l.price = '129,99'`,
    `Number('129,99')` é `NaN`, `JSON.stringify(NaN)` vira `null`, e o banco recebe `price: null`
    **antes de o publicador sequer tentar publicar**. Pior: `errosLocaisLinha` faz `Number(l.price) > 0`,
    então a linha ficaria marcada em vermelho com "falta preço" e o campo visivelmente preenchido com
    `129,99`. Normalizar aqui mantém `l.price` sempre no formato que `Number()` entende, e nenhuma
    outra função precisa de defesa redundante.

    Cobre digitação **e** paste sem duplicar lógica: `coercePasteValue` roda antes, célula a célula,
    e só faz trim/limite — o paste desemboca no mesmo `onCellsEdited` como um lote de edições. Não
    acrescente coerção de vírgula no `coercePasteValue`; seria a segunda implementação da mesma regra.

    Acrescente ao gate estrutural: a fonte sem comentários de `GradeAnuncioGlide.jsx` contém
    `normalizarPreco` e contém `valorNormalizado`; e a fonte sem comentários de
    `resources/js/Pages/Mlb/AnunciarMassa.jsx` **não** contém `normalizarPreco` dentro de
    `montarPayloadLinha` (gate de D-05 — a coerção não pode migrar para a borda de saída; asserte de
    forma simples, ex.: o trecho da fonte a partir de `function montarPayloadLinha` não inclui
    `normalizarPreco`).
  </action>
  <verify>
    <automated>npm run test:js</automated>
  </verify>
  <done>`npm run test:js` verde. `npm run build` verde. `normalizarPreco` é chamado só no `onCellsEdited`, só para `coluna.id === 'price'`, e `montarPayloadLinha` continua intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Célula do canvas → `onCellsEdited` → callbacks da página | Todo texto digitado, colado, preenchido por fill handle ou apagado por Delete atravessa aqui |
| Renderer customizado → `deleteRange` da lib | O `onDelete` do renderer decide o que é "vazio" para aquela célula |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-02 | Tampering | `normalizarPreco` no `onCellsEdited` corrompendo valor | mitigate | Função pura testada no plano 01 (não numérico passa cru); aplicada só quando `coluna.id === 'price'`; `errosLocaisLinha` segue sendo a barreira que reprova a linha |
| T-83-07 | Tampering | Delete apagando célula fora do que o usuário selecionou | mitigate | Não implementamos seleção nem varredura: quem percorre o range é o `deleteRange` da lib (L1769-1771), já em produção; o `onDelete` do renderer só descreve o "vazio" de UMA célula. A coluna de status é imune por `allowOverlay: false` (L1772, verificado) |
| T-83-08 | Tampering | Delete escrevendo direto no estado, fora do fluxo de autosave (viola D-06) | mitigate | O caminho é `deleteRange` → `editList` → `mangledOnCellsEdited` → `onCellsEdited` → `onEditarCelula` (verificado em L1790-1795). Gate estrutural garante que não existe prop `onDelete` no `<DataEditor>` nem escrita direta em `aba.linhas` |
| T-83-09 | Denial of Service | `getCellContent` instável causando redraw a cada 3s de polling | mitigate | Nenhuma dependência nova nos `useCallback`; a estabilidade da referência de `abas` é garantida pelo merge do plano 01 (devolve a mesma referência quando nada muda) |
| T-83-SC | Tampering | npm install | N/A | Nenhum pacote instalado — a lib e o pacote de cells já estavam auditados na Phase 82 |
</threat_model>

<verification>
- `npm run test:js` — verde (plano 01 + gates estruturais desta grade)
- `npm run build` — verde (gate obrigatório do CLAUDE.md)
- `C:\php\php.exe artisan test --filter=Phase82` — segue 9/9 (nenhum `.php` tocado)
- `git diff --name-only` não lista nenhum `.php` nem `AnunciarMassa.jsx`
- Zero regressão SHEET2-07: `rangeSelect`, `fillHandle`, `allowedFillDirections`, `getCellsForSelection`,
  `onPaste`, `coercePasteValue`, `rowMarkers`, `freezeColumns`, `onRowAppended` e o `temaEcf` seguem
  intactos; `RENDERERS` segue `[DropdownCell, origemCellRenderer]`
</verification>

<success_criteria>
- FIX-83-2: a coluna de status distingue publicado (✓ verde), erro real (✕ vermelho), publicando (↑), erro local (!), aviso do ML (◐)
- FIX-83-6a: `origemCellRenderer.onDelete` existe (8 colunas destravadas) e o ramo `tier` trata vazio (volta ao padrão Clássico); nenhum wrapper de DropdownCell, nenhuma prop `onDelete` no `<DataEditor>`
- FIX-83-5: `normalizarPreco` plugado no `onCellsEdited`, só na coluna `price`, cobrindo digitação, paste e fill handle
- Nenhuma dependência nova; nenhuma regra de coerção duplicada
</success_criteria>

<output>
Crie `.planning/phases/83-planilha-correcoes-de-publicacao-em-massa-avisos-do-ml-visiv/83-03-SUMMARY.md` ao terminar.
Registre no SUMMARY: a cascata final de glifos (ordem e cor) e a confirmação de que DropdownCell não precisou de wrapper.
</output>
</content>
