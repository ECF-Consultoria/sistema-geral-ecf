---
phase: 80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s
plan: 03
type: execute
wave: 3
depends_on: ["80-02"]
files_modified:
  - app/Http/Controllers/PerformanceController.php
  - resources/js/Pages/Performance/Dashboard.jsx
  - tests/Feature/V16/WidgetNpsAtribuicoesTest.php
autonomous: false
requirements: [DEC-80-E]

must_haves:
  truths:
    - "A resposta do NPS Shopee aparece na lista de últimas respostas do widget do responsável Shopee"
    - "A coluna NPS da tabela de empresas mostra a nota do serviço pelo qual a pessoa responde"
    - "O heatmap empresa×mês inclui as notas vindas de atribuições (não só do modelo principal)"
    - "Cada resposta listada identifica a área em linguagem clara (Mercado Livre / Shopee)"
    - "Nenhum widget mostra a nota de um serviço que não é da pessoa logada"
  artifacts:
    - path: "app/Http/Controllers/PerformanceController.php"
      provides: "Helper dual-path por resposta alimentando coluna NPS, últimas respostas e heatmap"
      contains: "nps_score_assignments"
    - path: "resources/js/Pages/Performance/Dashboard.jsx"
      provides: "Rótulo de área nas últimas respostas"
    - path: "tests/Feature/V16/WidgetNpsAtribuicoesTest.php"
      provides: "Prova de que o payload do dashboardCarteira reflete as atribuições"
      min_lines: 60
  key_links:
    - from: "app/Http/Controllers/PerformanceController.php"
      to: "nps_score_assignments"
      via: "helper notasNpsDoUsuarioPorResposta"
      pattern: "nps_score_assignments"
    - from: "resources/js/Pages/Performance/Dashboard.jsx"
      to: "nps.respostas[].area"
      via: "prop do Inertia"
      pattern: "area"
---

<objective>
Alinhar os três leitores de apresentação do `PerformanceController::dashboardCarteira` (coluna NPS por empresa, últimas respostas e heatmap) para refletirem as atribuições congeladas — hoje eles filtram por `->principal()` e a resposta do NPS Shopee **nunca** aparece, mesmo com o bônus já correto.

Purpose: os planos 80-01/80-02 consertam o ranking e o headline `nps.media` (vêm do service). Estes três widgets fazem query própria com `->principal()` e continuariam mentindo: a pessoa veria a média subir sem enxergar de onde veio. Este plano é deliberadamente separado para manter a cirurgia do bônus isolada e de baixo risco.

Output: um helper dual-path no controller alimentando os 3 widgets + rótulo de área + `npm run build` + checkpoint visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s/80-CONTEXT.md
@.planning/phases/80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s/80-RESEARCH.md
@.planning/phases/80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s/80-01-SUMMARY.md
@.planning/phases/80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s/80-02-SUMMARY.md
@app/Http/Controllers/PerformanceController.php
@app/Services/DesempenhoScoreService.php
@resources/js/Pages/Performance/Dashboard.jsx

<interfaces>
<!-- Contratos que o executor precisa. -->

Payload atual do `dashboardCarteira` (`PerformanceController.php:576-580`) — SHAPE A PRESERVAR:
```php
'nps' => [
    'media'     => $npsMedio,      // vem do service (já corrigido nos planos 01/02) — NÃO mexer
    'respostas' => $npsRespostas,  // [{empresa, nota, quando}] — ganha 'area'
    'heatmap'   => $heatmap,       // {meses:[{chave,label}], empresas:[{id,name,notas:{'Y-m':nota}}]}
],
```

Consumo no frontend (`resources/js/Pages/Performance/Dashboard.jsx`):
```jsx
function NpsHeatmapWidget({ nps }) { ... }   // :214 — heatmap + media
export default function DashboardCarteira({ pessoa, periodo, kpis, nps, metas, empresas, nps_pendentes = [] })  // :331
// tabela de empresas: coluna 'nps' :613/:647/:694 — lê e.nps
```

Os 3 pontos com `->principal()` a alinhar (todos em `dashboardCarteira`):
```php
:300-307  $npsByCompany   — última survey principal por empresa, últimos 60d
:380-386  $recentSurveys  — últimas 4 surveys principal
:424-430  $surveysHeatmap — surveys principal dos últimos 6 meses
```

`nps_score_assignments` (Fase 79): nps_response_id, company_id, servico_id, service_setor
('performance' | 'shopee'), role ('consultor' | 'estrategista'), user_id, average_score.
Mês/data da resposta: JOIN nps_responses → nps_surveys.completed_at (NUNCA assigned_at).

App\Models\Servico: SETOR_PERFORMANCE = 'performance', SETOR_SHOPEE = 'shopee'.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Helper dual-path por resposta no PerformanceController + teste</name>
  <files>app/Http/Controllers/PerformanceController.php, tests/Feature/V16/WidgetNpsAtribuicoesTest.php</files>
  <read_first>
    - `app/Http/Controllers/PerformanceController.php:292-446` — os 3 blocos com `->principal()`
    - `app/Services/DesempenhoScoreService.php` — os helpers `notasPorAtribuicao`/`notasLegado` do plano 80-01 (espelhar a semântica do dual-path; NÃO reimplementar a regra de bônus)
    - `.planning/phases/80-.../80-01-PLAN.md` § `<decisao_critica_do_plano>` — o predicado do skip
    - `tests/Feature/V16/BonusAtribuicoesNpsTest.php` — helpers de template/survey/submit já prontos
  </read_first>
  <behavior>
    Novo helper privado `notasNpsDoUsuarioPorResposta(User $user, Collection $companyIds, Carbon $desde): Collection` devolvendo linhas com `company_id`, `completed_at` (Carbon), `nota` (float) e `area` (string 'performance' | 'shopee' | null no legado), aplicando o MESMO dual-path do service:
    - (A) atribuições do user (`user_id = $user->id`) com JOIN até `nps_surveys.completed_at >= $desde`, status `completed`, dedup 1× por (`nps_response_id`, `role`);
    - (B) legado: surveys `->principal()` das empresas de `$companyIds`, `completed_at >= $desde`, nota via `NpsScoreCalculator::compute($response, $dim)` + fallback `score_*`, **pulando** as respostas que já têm atribuição no papel correspondente à dimensão do cargo do user;
    - `area` de (A) sai de `service_setor`; de (B) fica null (legado não tem serviço).

    `test_widget_lista_resposta_shopee_do_responsavel`: responsável Shopee acessa `/performance/carteira` (ou a rota do `dashboardCarteira`) e o payload Inertia `nps.respostas` contém a empresa da resposta do NPS Shopee com a nota da atribuição e `area` de Shopee; e **não** contém a nota do NPS ML da mesma empresa. Usar `$response->assertInertia(...)` ou inspeção do prop `nps`.
  </behavior>
  <action>
    Criar o helper privado `notasNpsDoUsuarioPorResposta` no `PerformanceController`, seguindo a convenção do arquivo (método privado `camelCase`, comentários pt-BR, seções `// ─── ... ───`).

    Reusar a mesma decisão de predicado do plano 80-01 (skip por resposta+papel) — **este helper é apresentação, não régua de bônus**: ele NÃO pode virar segunda fonte de verdade da nota da pessoa. Documentar no docblock que o número oficial do bônus é `desempenho.componentes.nps_medio` (do `DesempenhoScoreService`) e que este helper existe só para detalhar de onde a média veio. Se os dois divergirem, o service ganha.

    Eager-load para evitar N+1 (o loop legado usa `NpsScoreCalculator` sobre `response.answers` — manter o `with(['response.answers', 'response.survey'])` já presente).

    Criar `tests/Feature/V16/WidgetNpsAtribuicoesTest.php` com o cenário acima. Comentários em pt-BR; suite em `tests/Feature/V16/` (nunca `tests/Feature/Phase80/` — colide com o dev em paralelo).
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=WidgetNpsAtribuicoesTest</automated>
  </verify>
  <acceptance_criteria>
    Helper existe e o teste passa. `vendor/bin/phpunit --filter=Desempenho` continua verde (o helper não tocou o service). `git diff app/Services/DesempenhoScoreService.php` vazio neste plano.
  </acceptance_criteria>
  <done>Helper dual-path de apresentação criado, testado, sem tocar a régua de bônus.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Ligar coluna NPS, últimas respostas e heatmap ao helper + rótulo de área</name>
  <files>app/Http/Controllers/PerformanceController.php, resources/js/Pages/Performance/Dashboard.jsx</files>
  <read_first>
    - `app/Http/Controllers/PerformanceController.php:292-446` e :551-584 (montagem do payload)
    - `resources/js/Pages/Performance/Dashboard.jsx:214-260` (NpsHeatmapWidget) e :331, :694 (coluna NPS)
  </read_first>
  <behavior>
    Os 3 widgets passam a derivar do helper da Tarefa 1, com uma única passagem de dados:
    - **Coluna NPS por empresa** (janela 60d): nota da resposta mais recente por empresa.
    - **Últimas respostas** (4 mais recentes, sem janela): cada item ganha `area` — rótulo **em linguagem clara**: `'performance'` → "Mercado Livre", `'shopee'` → "Shopee", null → sem rótulo. **Nunca** exibir o slug cru `performance` na UI (regra do projeto: evitar jargão sem explicação).
    - **Heatmap** (6 meses): matriz empresa×mês com média das notas do range.
    Shape do payload preservado (`nps.media`/`nps.respostas`/`nps.heatmap`); a única adição é `area` em `respostas[]`.
  </behavior>
  <action>
    Substituir as 3 queries com `->principal()` (:300-307, :380-386, :424-430) por derivações do helper da Tarefa 1: uma chamada com `$desde = now()->subMonths(5)->startOfMonth()` (a janela mais larga, do heatmap) e recortes em memória para as outras duas (60d para a coluna; 4 mais recentes para a lista). Evita 3 queries redundantes e garante que os 3 widgets contem a mesma história.

    Remover o `$npsDim`/`$npsCalculator`/`$dimensao`/`$npsField` do escopo do `dashboardCarteira` **apenas** se ficarem sem uso após o refactor (o helper os encapsula) — não deixar variável órfã.

    **NÃO** tocar em `$npsMedio` (:491): ele vem de `$data['componentes']['nps_medio']` (o service). O headline continua sendo a fonte oficial.

    No `Dashboard.jsx`: exibir o rótulo de área ao lado de cada item de `nps.respostas` (badge discreto com tokens `ecf-*` e `cn()`, coerente com o dark theme). Mapear os slugs para rótulos legíveis no componente. Renderizar sem quebrar quando `area` vier null (respostas legadas).

    **Cuidado com o bundle** (memória `feedback_rollup_map_scope_bug`): computar qualquer flag booleana **dentro** do callback do `.map()`, nunca em variável de escopo do componente usada dentro do map — o Rollup elimina a var e gera `ReferenceError` só no build de produção.

    Rodar `npm run build` ao final (convenção do projeto após mudança de frontend).
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=WidgetNpsAtribuicoesTest && vendor/bin/phpunit --filter=Performance && npm run build</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "principal()" app/Http/Controllers/PerformanceController.php` não retorna nenhuma ocorrência dentro de `dashboardCarteira` (:265-584). `npm run build` conclui sem erro. `--filter=Performance` e `--filter=Desempenho` verdes. Payload mantém as chaves `media`/`respostas`/`heatmap`.
  </acceptance_criteria>
  <done>Os 3 widgets leem o dual-path; rótulo de área legível; bundle buildado; testes verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 3: Checkpoint visual — NPS Shopee visivel no widget, isolamento preservado</name>
  <action>Pausar para verificacao humana. Nao prosseguir sem o sinal de retomada. Se o usuario reportar problema, corrigir no PerformanceController/Dashboard.jsx e rodar npm run build novamente antes de repetir o checkpoint.</action>
  <what-built>
    O widget de NPS do `/performance` (carteira) passou a refletir as atribuições congeladas: a coluna NPS por empresa, as últimas 4 respostas (agora com o rótulo da área — "Mercado Livre" / "Shopee") e o heatmap empresa×mês. O headline da média e o ranking já vinham corrigidos dos planos 80-01/80-02.
  </what-built>
  <how-to-verify>
    1. Suba o ambiente local e faça login com um usuário responsável por Shopee (ou o seed equivalente ao Gustavo: analista de ML **e** de Shopee).
    2. Acesse o dashboard de carteira (`/performance/carteira`) e confira:
       - a nota média de NPS do topo do widget bate com a do ranking em `/performance`;
       - na lista de últimas respostas aparece a resposta do NPS Shopee, com o rótulo "Shopee";
       - as respostas de ML aparecem com o rótulo "Mercado Livre" (nunca o slug cru "performance");
       - a coluna NPS da tabela de empresas mostra nota para a empresa que respondeu Shopee.
    3. Faça login como um analista de **ML** de uma empresa que também tem Shopee: a resposta do NPS Shopee **não** pode aparecer para ele, nem influenciar a nota dele.
    4. Confira que o heatmap continua renderizando (6 colunas de mês) e não quebrou com as notas novas.
    5. Confira o console do navegador: sem `ReferenceError` (bundle de produção — ver `npm run build`).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que está errado (print ajuda)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dashboardCarteira` → usuário logado | Widget escopado a `$request->user()`; não pode alargar para notas de terceiros |
| helper de apresentação → régua de bônus | Segunda agregação da mesma nota pode divergir do número oficial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-10 | Information Disclosure | helper `notasNpsDoUsuarioPorResposta` | mitigate | Query sempre filtrada por `user_id = $request->user()->id` e `company_ids` da carteira dele; teste prova que o analista de ML não vê a resposta Shopee |
| T-80-11 | Tampering (divergência de fonte) | helper vs `DesempenhoScoreService` | mitigate | Helper é só apresentação; `nps.media` continua vindo do service (`$data['componentes']['nps_medio']`); docblock declara que o service é a fonte oficial |
| T-80-12 | Denial of Service (perf) | loop do `NpsScoreCalculator` no ramo legado | mitigate | Eager-load `with(['response.answers','response.survey'])` preservado + uma única query para os 3 widgets (evita N+1 e 3 varreduras) |
| T-80-13 | Tampering | bundle de produção | mitigate | `npm run build` obrigatório + flags booleanas computadas dentro do `.map()` (memória `feedback_rollup_map_scope_bug`) + checkpoint confere o console |
| T-80-SC | Tampering | instalação de pacotes | accept | Nenhuma dependência nova (npm/composer) — só rebuild do bundle existente |
</threat_model>

<verification>
- `vendor/bin/phpunit --filter=WidgetNpsAtribuicoesTest` — verde
- `vendor/bin/phpunit --filter=Performance && vendor/bin/phpunit --filter=Desempenho` — verdes
- `npm run build` — sem erro
- `grep -n "principal()" app/Http/Controllers/PerformanceController.php` — sem ocorrência em `dashboardCarteira`
- `git diff app/Services/DesempenhoScoreService.php` — vazio (a régua de bônus não foi tocada neste plano)
- Checkpoint visual aprovado
</verification>

<success_criteria>
- Coluna NPS, últimas respostas e heatmap refletem as atribuições (Shopee visível para quem é do Shopee)
- Rótulo de área em linguagem clara ("Mercado Livre" / "Shopee"), sem slug cru
- Analista de ML não vê nem recebe a nota do NPS Shopee da mesma empresa
- `nps.media` continua vindo do `DesempenhoScoreService` (fonte única do bônus)
- Bundle buildado; sem regressão em `--filter=Performance`
</success_criteria>

<output>
Criar `.planning/phases/80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s/80-03-SUMMARY.md` ao terminar. Registrar os itens do checkpoint visual e se o `PortfolioController:1383` (mesmo padrão `->principal()` + dimensão por cargo) deve virar um follow-up — ele NÃO está no escopo desta fase.
</output>
