---
phase: 80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - app/Services/DesempenhoScoreService.php
  - tests/Feature/V16/BonusDualPathRegressaoTest.php
autonomous: true
requirements: [DEC-80-B, DEC-80-C, DEC-80-E]

must_haves:
  truths:
    - "Um mês SEM atribuições produz nota IDÊNTICA à do cálculo legado atual (bônus histórico intacto)"
    - "Um mês MISTO soma os dois caminhos sem contar nenhuma resposta 2×"
    - "Usuário sem respostas no mês continua recebendo 0.0"
    - "O ranking /performance e o widget não servem a nota antiga do cache v2"
    - "Meses já consolidados em desempenho_score_snapshots não são reescritos"
  artifacts:
    - path: "tests/Feature/V16/BonusDualPathRegressaoTest.php"
      provides: "Regressão histórica, mês misto e prova do bump de cache"
      min_lines: 100
    - path: "app/Services/DesempenhoScoreService.php"
      provides: "Chave de cache v3"
      contains: "desempenho.compute.v3"
  key_links:
    - from: "app/Services/DesempenhoScoreService.php"
      to: "Cache::remember"
      via: "chave versionada v3"
      pattern: "desempenho\\.compute\\.v3"
---

<objective>
Provar que a cirurgia do plano 80-01 **não alterou bônus de nenhum mês sem atribuição**, que o mês de transição (misto) soma corretamente, e bumpar a chave de cache v2→v3 para que ranking e widgets parem de servir a nota antiga.

Purpose: o plano 80-01 mudou a régua que paga pessoas reais. Sem estas provas não há como afirmar que o histórico ficou idêntico. O bump de cache é o que faz a correção aparecer — sem ele o `/performance` serve valores v2 por até 7 dias.

Output: suite de regressão do dual-path + cache v3 + varredura dos consumidores + suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s/80-CONTEXT.md
@.planning/phases/80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s/80-RESEARCH.md
@.planning/phases/80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s/80-01-SUMMARY.md
@app/Services/DesempenhoScoreService.php
@tests/Feature/Phase74/DesempenhoScoreServiceTest.php
@tests/Feature/V16/BonusAtribuicoesNpsTest.php

<interfaces>
<!-- Contratos que o executor precisa. -->

Cache atual (`DesempenhoScoreService.php:135-156`) — bumpar SÓ a versão da string:
```php
$cacheKey = sprintf('desempenho.compute.v2.%d.%s', $user->id, $mes->format('Y-m'));
$ttl = $mes->lt($mesCorrente) ? now()->addDays(7) : now()->addMinutes(10);
return Cache::remember($cacheKey, $ttl, fn () => $this->compute($user, $mesReferencia));
```

Consumidores de `computeCached` (varredura completa do RESEARCH — conferir todos):
```
PerformanceController::index          :108, :114   (ranking /performance)
PerformanceController::dashboardCarteira :272      (widget analista/estrategista)
PerformanceController::show           :907         (view individual)
DashboardController                   :797         (widget "Desempenho da equipe")
PortfolioController                   :1251, :1277 (NÃO estava no CONTEXT — RESEARCH C2)
WarmDesempenhoCache (cron 8min)       :71          (repopula a v3 sozinho)
```

Consumidores de `compute()` DIRETO (sem cache, por design — não mexer):
```
SnapshotDesempenhoScores :89   ConsolidarMesDesempenho :97
```

phpunit.xml: `DB_CONNECTION=sqlite` `:memory:`, `CACHE_STORE=array`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Regressão do dual-path — mês sem atribuição, mês misto e 0.0</name>
  <files>tests/Feature/V16/BonusDualPathRegressaoTest.php</files>
  <read_first>
    - `tests/Feature/V16/BonusAtribuicoesNpsTest.php` (plano 80-01) — helpers de template/survey/submit já montados; reusar o padrão
    - `tests/Feature/Phase74/DesempenhoScoreServiceTest.php:234-283` (`mockNpsRespostaPrincipal`, `month_reference` zerado de propósito) e :388-425 (âncora Carlos)
    - `.planning/phases/80-.../80-RESEARCH.md` Pitfalls 3, 4 e 5
  </read_first>
  <behavior>
    Classe `Tests\Feature\V16\BonusDualPathRegressaoTest` (`RefreshDatabase` + `CriaCenarioResponsaveis`, `Carbon::setTestNow` fixo). `computeNpsMedio` por reflection.

    - `test_mes_sem_atribuicoes_mantem_nota_legada`: mês com respostas criadas via factory **sem** passar pelo `NpsSnapshotService` (zero linhas em `nps_score_assignments`) → a média sai 100% do ramo legado e bate o valor exato esperado (ex.: 2 respostas do modelo principal, pesos 4 e 2 → `3.0`). Asserção extra: `assertSame(0, NpsScoreAssignment::count())` — se houver atribuição, o teste não está provando o que diz.
    - `test_mes_misto_soma_os_dois_caminhos_sem_duplicar`: no MESMO mês e para o MESMO user, uma resposta antiga sem atribuição (factory direta, modelo principal) e uma resposta nova com atribuição (submit real via `$this->post`). Esperado: média = média das DUAS notas, cada uma contando 1×. Escolher pesos que tornem a duplicação detectável (se a resposta com atribuição contar 2×, o valor muda) e assertar com `assertEqualsWithDelta(..., 0.01)` (Pitfall 4: `decimal(5,2)` vs float sem round).
    - `test_nps_medio_e_zero_sem_respostas_no_mes_com_carteira`: user com carteira ativa e nenhuma resposta no mês → `assertSame(0.0, ...)` (float estrito — DESEMP-03 penaliza).
    - `test_nps_medio_usa_atribuicao_mesmo_sem_carteira_ativa`: user SEM empresa ativa na carteira mas COM atribuição no mês → recebe a média da atribuição (prova que o guard de carteira vazia desceu para o ramo legado e não zera mais o resultado inteiro).
  </behavior>
  <action>
    Criar `tests/Feature/V16/BonusDualPathRegressaoTest.php` com os 4 cenários. Marcar o template principal e chamar `NpsTemplate::resetPrincipalCache()` (Pitfall 5 — sem isso o ramo legado devolve conjunto vazio e o teste "passa" pelo motivo errado). Comentários em pt-BR.

    Para o mês misto, garantir que ambas as respostas tenham `completed_at` dentro do mesmo mês computado — o submit real seta `completed_at = now()`; a resposta antiga precisa de `completed_at` explícito no mesmo mês. Não usar `month_reference` para nada (é o mês do disparo e é NULL em muitas linhas — DEC-80-B0).
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=BonusDualPathRegressaoTest</automated>
  </verify>
  <acceptance_criteria>
    Os 4 testes passam contra o código do plano 80-01. Se `test_mes_misto_soma_os_dois_caminhos_sem_duplicar` falhar para MAIS que o esperado, a união não está disjunta (Pitfall 3) — corrigir o service, não o teste.
  </acceptance_criteria>
  <done>Arquivo criado; 4 testes verdes; regressão histórica e mês misto provados.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Bump da chave de cache v2 → v3 + prova</name>
  <files>app/Services/DesempenhoScoreService.php, tests/Feature/V16/BonusDualPathRegressaoTest.php</files>
  <read_first>
    - `app/Services/DesempenhoScoreService.php:135-156` — `computeCached` (única ocorrência da string da chave)
    - `.planning/phases/80-.../80-RESEARCH.md` § "Cache: onde bumpar e quem consome"
  </read_first>
  <behavior>
    - `test_cache_bumpado_para_v3`: semear `Cache::put('desempenho.compute.v2.{userId}.{Y-m}', ['nota_final' => 99.9], 600)` (lixo reconhecível) e provar que `computeCached` **ignora** o valor v2 e devolve o shape recalculado. Assertar também que a chave `desempenho.compute.v3.{userId}.{Y-m}` passou a existir (`Cache::has`).
  </behavior>
  <action>
    Em `computeCached`, trocar `desempenho.compute.v2.` por `desempenho.compute.v3.` no `sprintf` da linha :141 — **só a versão**, sem mexer no TTL adaptativo (7 dias mês fechado / 10 min mês corrente) nem no `Cache::remember`. Acrescentar comentário pt-BR acima da linha, no formato do bump anterior: registrar que o bump de 2026-07-15 é porque `computeNpsMedio` passou a somar as atribuições congeladas (`nps_score_assignments`) e os valores v2 têm a nota antiga (sem Shopee). Preservar o comentário do bump v1→v2 (histórico).

    Adicionar `test_cache_bumpado_para_v3` ao `BonusDualPathRegressaoTest`.

    Varrer os consumidores e confirmar que **nenhum** monta a chave por conta própria (todos passam por `computeCached`): rodar `grep -rn "desempenho.compute" app/` e conferir que a única ocorrência é a do service. Conferir na lista do `<interfaces>` que `PortfolioController` (:1251, :1277) e `DashboardController` (:797) estão cobertos — o `PortfolioController` não estava no CONTEXT e é o consumidor mais fácil de esquecer. **Não** adicionar `cache:clear` a nenhum script: as chaves v2 viram órfãs e expiram por TTL, e o `WarmDesempenhoCache` (cron 8min) repopula a v3 sozinho.

    **Não** rodar `desempenho:consolidar-mes` com `--mes` passado em momento nenhum (DEC-80-E: reescreveria bônus já fechado em `desempenho_score_snapshots`).
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=BonusDualPathRegressaoTest && grep -rn "desempenho\.compute" app/ | grep -v '^\s*//'</automated>
  </verify>
  <acceptance_criteria>
    `grep -rn "desempenho.compute.v3" app/Services/DesempenhoScoreService.php` retorna 1 linha de código; `grep -rn "desempenho.compute.v2" app/` não retorna nenhuma linha de **código** (só comentário histórico, se mantido). `test_cache_bumpado_para_v3` verde. Nenhum outro arquivo monta a chave.
  </acceptance_criteria>
  <done>Chave v3 ativa, TTL intacto, teste do bump verde, consumidores conferidos.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Regressão ampla + âncora Carlos + snapshots preservados</name>
  <files>tests/Feature/V16/BonusDualPathRegressaoTest.php</files>
  <read_first>
    - `tests/Feature/Phase74/DesempenhoScoreServiceTest.php:388-425` — `test_fixture_carlos_retorna_nota_4_08_basico`
    - `.planning/phases/80-.../80-RESEARCH.md` § "Blast radius / regressão"
  </read_first>
  <action>
    Rodar a regressão completa e registrar os resultados no SUMMARY:
    1. `vendor/bin/phpunit --filter=Desempenho` — inclui a âncora `test_fixture_carlos_retorna_nota_4_08_basico`. O valor travado é **`nota_final = 4.08` + `faixa_bonus = 'basico'`** — **NÃO** 3.35/`sem_bonus` (o 3.35 do brief foi superado em 2026-07-09 pelas réguas 1-5). O fixture cria surveys por factory sem passar pelo `NpsSnapshotService` → zero atribuições → é a prova de regressão do caminho legado embutida na suite. **Se quebrar: o dual-path vazou — investigar o service, NUNCA ajustar o teste.**
    2. `vendor/bin/phpunit --filter=Performance` — `PerformanceCargoFilterTest` e afins.
    3. `vendor/bin/phpunit --filter=Nps` — inclui a suite de escrita da Fase 79 (`SubmitSnapshotTest`, `AtribuicaoPorServicoNpsTest`, `AtribuicaoPorServicoIsolamentoTest`, `CarteiraBonusNaoDobraTest`). **Se algum destes mudar de comportamento, a Fase 80 invadiu a escrita da Fase 79** — reverter a invasão.
    4. `vendor/bin/phpunit` — suite completa (phase gate).

    Confirmar por leitura (sem executar) que nada nesta fase reescreve o passado: `SnapshotDesempenhoScores` e `ConsolidarMesDesempenho` seguem chamando `compute()` direto (não `computeCached`) e nenhum comando de consolidação foi alterado. Registrar no SUMMARY o aviso operacional para o time: no dia do deploy o `delta_vs_ontem` do ranking vai registrar um degrau — é o efeito real da correção (a nota Shopee entrando), não bug.

    Se algum teste fora de `tests/Feature/V16/` mudar de valor esperado, PARAR e reportar — é sinal de que o bônus histórico mudou.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=Desempenho && vendor/bin/phpunit --filter=Performance && vendor/bin/phpunit --filter=Nps && vendor/bin/phpunit</automated>
  </verify>
  <acceptance_criteria>
    Suite completa verde. `test_fixture_carlos_retorna_nota_4_08_basico` verde com 4.08/basico **sem nenhuma edição no arquivo de teste**. `git diff --stat tests/Feature/Phase74/` vazio. Nenhum teste da Fase 79 alterado.
  </acceptance_criteria>
  <done>Regressão ampla verde; âncora Carlos intacta sem edição; snapshots consolidados não reescritos; aviso do degrau registrado no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache (Redis prod) → tela de bônus | Valor v2 servido após o deploy mostra nota antiga como se fosse a corrigida |
| `desempenho_score_snapshots` → bônus pago | Mês fechado já virou dinheiro; reprocessar reescreve o passado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-06 | Spoofing (dado velho como novo) | `computeCached` | mitigate | Bump v2→v3 na chave + `test_cache_bumpado_para_v3` semeando lixo na v2; varredura confirma que nenhum consumidor monta a chave por fora |
| T-80-07 | Tampering | `ConsolidarMesDesempenho` / `desempenho_score_snapshots` | mitigate | DEC-80-E: proibido rodar `consolidar-mes --mes` passado; mês fechado é lido do `breakdown_json`; nenhum comando de consolidação alterado nesta fase |
| T-80-08 | Repudiation (mudança silenciosa de bônus) | régua histórica | mitigate | `test_mes_sem_atribuicoes_mantem_nota_legada` + âncora Carlos 4.08/basico sem edição do arquivo de teste |
| T-80-09 | Tampering | invasão da escrita da Fase 79 | mitigate | `--filter=Nps` verde e `git diff` vazio nos testes da Fase 79 |
| T-80-SC | Tampering | instalação de pacotes | accept | Fase não instala dependência alguma — sem superfície de supply chain |
</threat_model>

<verification>
- `vendor/bin/phpunit --filter=BonusDualPathRegressaoTest` — 5/5 verdes (4 da Tarefa 1 + cache v3)
- `vendor/bin/phpunit` — suite completa verde
- `grep -rn "desempenho.compute.v2" app/` — sem ocorrência em código
- `git diff --stat tests/Feature/Phase74/ tests/Feature/V16/SubmitSnapshotTest.php` — vazio
</verification>

<success_criteria>
- Mês sem atribuições → nota idêntica ao legado (bônus histórico preservado)
- Mês misto → soma dos dois caminhos, cada resposta 1×
- `assertSame(0.0, ...)` preservado para user sem respostas
- Chave de cache v3 ativa, TTL adaptativo intacto, consumidores todos cobertos
- Âncora Carlos 4.08/basico verde sem edição do teste
- Nenhum snapshot consolidado reescrito
</success_criteria>

<output>
Criar `.planning/phases/80-b-nus-e-relat-rios-desempenhoscoreservice-l-atribui-es-por-s/80-02-SUMMARY.md` ao terminar. Registrar: resultado da suite completa, o aviso do degrau no `delta_vs_ontem` do dia do deploy, e a sugestão operacional de rodar `grep '[NPS Snapshot] responsável faltante'` nos logs do VPS pós-deploy para dimensionar quantas empresas ficam permanentemente no ramo legado (RESEARCH OQ3).
</output>
