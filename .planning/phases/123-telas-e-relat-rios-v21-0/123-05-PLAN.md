---
phase: 123-telas-e-relat-rios-v21-0
plan: 05
type: execute
wave: 4
depends_on: ["123-01", "123-04"]
files_modified:
  - app/Http/Controllers/RelatorioBonificacaoController.php
  - resources/js/Pages/Desempenho/RelatorioBonificacao.jsx
  - tests/Feature/Phase123/RelatorioBonificacaoEmpresasTest.php
  - tests/js/estrutura-relatorio-bonificacao.test.js
autonomous: true
requirements: [UIEM-04]

must_haves:
  truths:
    - "A tabela continua uma linha por profissional — a leitura de 'quem bateu o bônus' não muda"
    - "A justificativa empresa a empresa fica a um clique, na linha expansível"
    - "As empresas exibidas vêm da mesma tabela que o detalhe do profissional lê, não do breakdown_json"
    - "O PDF continua resumo, uma linha por profissional, sem template novo"
    - "Profissional sem detalhe gravado expande para um aviso, não para uma tabela vazia"
  artifacts:
    - path: "app/Http/Controllers/RelatorioBonificacaoController.php"
      provides: "empresas_score + empresas_score_resumo + tem_detalhe_empresas por profissional"
      contains: "companyScoreReader"
    - path: "resources/js/Pages/Desempenho/RelatorioBonificacao.jsx"
      provides: "linha expansível reusando EmpresasScoreTabela"
    - path: "tests/Feature/Phase123/RelatorioBonificacaoEmpresasTest.php"
      provides: "Prova de UIEM-04/D-08 (mesma fonte) e de D-09 (PDF intocado)"
  key_links:
    - from: "app/Http/Controllers/RelatorioBonificacaoController.php"
      to: "App\\Services\\Desempenho\\CompanyScoreSnapshotReader"
      via: "paraUsuarios() UMA vez, fora do map de profissionais em montarLinhas()"
      pattern: "paraUsuarios"
    - from: "resources/js/Pages/Desempenho/RelatorioBonificacao.jsx"
      to: "resources/js/Components/Desempenho/EmpresasScoreTabela.jsx"
      via: "import + render dentro da <tr> de expansão"
      pattern: "EmpresasScoreTabela"
---

<objective>
Fechar UIEM-04: o Relatório de Bonificação — o documento que decide pagamento — passa a mostrar, a um clique, sobre quais empresas cada nota foi feita, lendo exatamente a mesma tabela que a tela individual lê.

Purpose: hoje o admin vê "Rubens, nota 4,10, faixa intermediário" e não tem como auditar de onde veio esse 4,10 sem sair da tela. A D-08 põe a justificativa na própria linha sem quebrar a leitura de "quem bateu o bônus".
Output: `montarLinhas()` entregando o detalhe por profissional, a linha expansível no JSX reusando `EmpresasScoreTabela` do Plano 04, e a suíte que prova a fonte única e que o PDF não mudou.

**Esta fase não calcula nada.** E o `pdf()` **não muda** (D-09): o documento que circula para gestão/RH ganha em ser curto; a auditoria empresa a empresa acontece na tela. Sem segundo template dompdf nesta fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-RESEARCH.md
@app/Http/Controllers/RelatorioBonificacaoController.php
@resources/js/Pages/Desempenho/RelatorioBonificacao.jsx
</context>

<interfaces>
<!-- Contratos prontos dos Planos 01 e 04. Não explorar o codebase para achá-los. -->

`App\Services\Desempenho\CompanyScoreSnapshotReader` (Plano 01):
- `paraUsuarios(array $userIds, Carbon $mes): Collection` — `Collection<int userId, Collection<int, array>>`
- `resumo(Collection $linhas): array` — `['entraram' => int, 'nao_entraram' => int]`

`resources/js/Components/Desempenho/EmpresasScoreTabela.jsx` (Plano 04):
props `{ linhas = [], resumo = { entraram: 0, nao_entraram: 0 }, className = '' }`.
Renderiza as duas seções ("entraram na conta (N)" / "não entraram (M)"), o formato
`14,1% → 12,0%  −2,1`, os motivos traduzidos e o selo de Shopee. É componente puro —
sem `router`, sem `usePage`.

`resources/js/lib/desempenhoLabels.js` (Plano 01) exporta, entre outros:
`AVISO_SEM_DETALHE_TITULO`, `avisoSemDetalheFechado`.

Estado ATUAL de `RelatorioBonificacaoController` (já lido, 187 linhas):
- `index()` chama `montarLinhas($competencia, $cargo)` e renderiza `Desempenho/RelatorioBonificacao`
  com `competencia`, `competencia_label`, `competencias_disponiveis`, `cargo`, `profissionais`,
  `total_contemplados`.
- `pdf()` chama o MESMO `montarLinhas()` e carrega a view `pdf.relatorio-bonificacao`.
- `montarLinhas()` monta `$cargosPorUser`, `$users`, `$snapshots` (snapshot-first) e devolve,
  por profissional que atingiu o bônus, as chaves `id`, `name`, `cargo_label`, `nps_medio`,
  `var_faturamento_pct`, `var_margem_pct`, `nota_final`, `faixa_slug`, `faixa_label`,
  `faixa_promovida`. Filtra fora quem tem `faixa === 'sem_bonus'` e ordena por `nota_final` desc.

Estado ATUAL de `resources/js/Pages/Desempenho/RelatorioBonificacao.jsx` (171 linhas, já lido):
tabela de 8 colunas (`#`, `Profissional`, `Cargo`, `NPS médio`, `Var. faturamento`,
`Var. margem %`, `Nota final`, `Faixa`), com `fmtNota`/`fmtNps`/`fmtPct`/`corPct` locais e
`FaixaBadge`. Sem estado local hoje (componente sem `useState`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: montarLinhas() entrega o detalhe por profissional (D-08) e o PDF fica intocado (D-09)</name>

  <files>
app/Http/Controllers/RelatorioBonificacaoController.php
tests/Feature/Phase123/RelatorioBonificacaoEmpresasTest.php
  </files>

  <read_first>
app/Http/Controllers/RelatorioBonificacaoController.php
resources/views/pdf/relatorio-bonificacao.blade.php
app/Services/Desempenho/CompanyScoreSnapshotReader.php
tests/Feature/Phase123/Phase123TestCase.php
  </read_first>

  <action>
Injetar `App\Services\Desempenho\CompanyScoreSnapshotReader` no construtor de `RelatorioBonificacaoController` como propriedade promovida privada `$companyScoreReader`, ao lado de `$scoreService` e `$periodResolver`. Adicionar o `use`.

Em `montarLinhas()`, logo depois da query de `$snapshots` e **antes** do `$users->map(...)`, fazer UMA única leitura do detalhe por empresa:
`$detalhePorUser = $this->companyScoreReader->paraUsuarios($users->pluck('id')->all(), $competencia);`

Dentro do `map()`, para cada profissional que passa no filtro de bônus, acrescentar três chaves ao array retornado, preservando todas as atuais:
- `'empresas_score'` → `($detalhePorUser->get($u->id) ?? collect())->values()->all()`
- `'empresas_score_resumo'` → `$this->companyScoreReader->resumo($detalhePorUser->get($u->id) ?? collect())`
- `'tem_detalhe_empresas'` → a Collection do profissional não está vazia

A fonte é a tabela `desempenho_company_score_snapshots`, **nunca** `$resultado['empresas_score']` vindo do `breakdown_json`. UIEM-04 exige literalmente "a mesma fonte que o ranking" — parsear o blob JSON aqui criaria um terceiro ponto de leitura que pode divergir dos outros dois num commit futuro. A linha existente `$resultado = ($snap && isset($snap->breakdown_json['componentes'])) ? $snap->breakdown_json : $this->scoreService->computeCached($u, $competencia);` fica exatamente como está — ela resolve `nota_final` e os componentes agregados, que continuam sendo o conteúdo da linha resumo.

**`pdf()` não recebe nenhuma edição (D-09).** Ele chama o mesmo `montarLinhas()`, então o array passado à view passa a carregar `empresas_score` — mas `resources/views/pdf/relatorio-bonificacao.blade.php` não itera essa chave, e não deve passar a iterar. O PDF continua resumo, uma linha por profissional. Não criar segundo template dompdf, não adicionar parâmetro de "modo detalhado", não tocar na blade.

Criar `tests/Feature/Phase123/RelatorioBonificacaoEmpresasTest.php`, namespace `Tests\Feature\Phase123`, estendendo `Phase123TestCase`, estilo `#[Test]`, `Http::fake()` no `setUp` e `adminLogado()` (a rota é `role:admin`). Casos:
- **Caminho comum:** profissional contemplado (semear `seedMensal()` com `breakdown_json` de `nota_final` alta e `faixa_bonus` diferente de `sem_bonus`) e 3 linhas em `desempenho_company_score_snapshots` para `2026-06`. `GET route('desempenho.relatorio-bonificacao') . '?mes=2026-06'` devolve `profissionais.0.empresas_score` com 3 itens, `profissionais.0.tem_detalhe_empresas === true` e `profissionais.0.empresas_score_resumo.entraram` batendo com a contagem de `status === 'complete'`.
- **Prova de fonte única (o teste que mais importa aqui):** gravar `breakdown_json['empresas_score'] = []` no snapshot mensal e mesmo assim as 3 linhas da tabela dedicada aparecem na prop. Se alguém trocar a fonte para o blob JSON no futuro, este teste acende.
- **Ausência (D-03):** profissional contemplado sem nenhuma linha na competência devolve `empresas_score === []`, `tem_detalhe_empresas === false`, `empresas_score_resumo.entraram === 0` — e status 200.
- **D-09, regressão do PDF:** `GET route('desempenho.relatorio-bonificacao.pdf') . '?mes=2026-06'` devolve 200 com header `Content-Type` contendo `application/pdf`. E, no mesmo teste, ler `resources/views/pdf/relatorio-bonificacao.blade.php` com `file_get_contents()` e assertar, com `assertStringNotContainsString`, que a blade não contém `empresas_score` nem `nota_empresa`. É a prova de que o documento que circula para gestão/RH continua resumo.
- **Uma query só:** com 3 profissionais contemplados em cena, `DB::enableQueryLog()` e assertar exatamente 1 query contra `desempenho_company_score_snapshots`.
- **Não-contemplado não vaza:** profissional com `faixa_bonus === 'sem_bonus'` continua fora de `profissionais`, mesmo tendo linhas de detalhe gravadas.
  </action>

  <verify>
    <automated>php artisan test --filter=RelatorioBonificacaoEmpresasTest</automated>
  </verify>

  <acceptance_criteria>
- `php artisan test --filter=RelatorioBonificacaoEmpresasTest` sai com código 0, com ao menos 6 testes
- `grep -c 'paraUsuarios' app/Http/Controllers/RelatorioBonificacaoController.php` retorna exatamente `1`
- `git diff resources/views/pdf/relatorio-bonificacao.blade.php` não retorna nenhuma linha (blade intocada)
- `git diff app/Http/Controllers/RelatorioBonificacaoController.php` não altera nenhuma linha dentro do método `pdf()`
- `grep -c 'empresas_score' resources/views/pdf/relatorio-bonificacao.blade.php` retorna `0`
- `grep -v '^\s*[/*]' app/Http/Controllers/RelatorioBonificacaoController.php | grep -c "breakdown_json\['empresas_score'\]"` retorna `0`
- `php artisan test --filter=RelatorioBonificacaoTest` (suíte preexistente) continua verde
  </acceptance_criteria>

  <done>
Cada profissional contemplado carrega o próprio detalhe por empresa, lido da tabela dedicada numa única query por página, e o PDF continua sendo o mesmo resumo de uma linha por profissional.
  </done>
</task>

<task type="auto">
  <name>Task 2: linha expansível no Relatório de Bonificação reusando EmpresasScoreTabela</name>

  <files>
resources/js/Pages/Desempenho/RelatorioBonificacao.jsx
tests/js/estrutura-relatorio-bonificacao.test.js
  </files>

  <read_first>
resources/js/Pages/Desempenho/RelatorioBonificacao.jsx
resources/js/Components/Desempenho/EmpresasScoreTabela.jsx
resources/js/lib/desempenhoLabels.js
tests/js/_fonte.js
  </read_first>

  <action>
Editar `resources/js/Pages/Desempenho/RelatorioBonificacao.jsx`. Importar `useState` de `react`, `ChevronRight` e `ChevronDown` de `lucide-react` (somar ao import existente de `lucide-react`), `EmpresasScoreTabela` de `@/Components/Desempenho/EmpresasScoreTabela`, e `AVISO_SEM_DETALHE_TITULO` + `avisoSemDetalheFechado` de `@/lib/desempenhoLabels`.

**1. Estado de expansão.** No componente `RelatorioBonificacao`, criar `const [abertos, setAbertos] = useState(() => new Set());` guardando os `p.id` expandidos, com uma função `alternar(id)` que clona o Set, adiciona/remove e chama `setAbertos`. Nasce tudo fechado — a leitura primária da tela é "quem bateu o bônus", e a D-08 põe a justificativa a um clique, não na cara.

**2. Coluna de expansão.** A primeira coluna (hoje o índice `#`) vira o gatilho: manter o número, e antes dele um `<button type="button">` com `ChevronDown` quando aberto e `ChevronRight` quando fechado, `className="text-white/40 hover:text-white transition-colors"`, `aria-expanded` refletindo o estado e `aria-label` dizendo `Ver empresas de {p.name}`. O cabeçalho `#` ganha `colSpan` nenhum — a estrutura de 8 colunas fica igual, só o conteúdo da primeira célula muda. A linha inteira (`<tr>`) também pode chamar `alternar(p.id)` no `onClick`, desde que os elementos interativos existentes não sejam prejudicados (não há nenhum nesta tabela hoje).

**3. Linha de expansão.** Para cada profissional, quando `abertos.has(p.id)`, renderizar uma `<tr>` adicional logo abaixo da linha dele, com uma única `<td colSpan={8} className="px-4 py-4 bg-black/20 border-b border-white/[0.05]">` contendo:
- se `p.tem_detalhe_empresas` → `<EmpresasScoreTabela linhas={p.empresas_score} resumo={p.empresas_score_resumo} />`
- caso contrário → a caixa de aviso da D-03: `rounded-xl border border-white/[0.1] bg-white/[0.03] px-4 py-3` com título `AVISO_SEM_DETALHE_TITULO` em `text-white/80 text-sm font-semibold` e corpo `avisoSemDetalheFechado(competencia_label)` em `text-white/50 text-xs mt-1 leading-relaxed`. Expandir para um aviso é melhor que expandir para uma tabela vazia.

Usar `React.Fragment` com `key={p.id}` envolvendo as duas `<tr>` (a linha do profissional e a de expansão), já que `map` precisa de uma única raiz por item.

**4. Defaults defensivos.** No `map`, ler `p.empresas_score ?? []`, `p.empresas_score_resumo ?? { entraram: 0, nao_entraram: 0 }` e `p.tem_detalhe_empresas === true`. A tela não pode quebrar se receber um payload de antes desta fase.

**5. Rodapé.** Acrescentar ao parágrafo final da tela (hoje começa com `"Atingiu o bônus" = nota final ≥ 4,00`) uma frase explicando a expansão: `Clique na seta ao lado do nome para ver as empresas que formaram a nota daquele profissional.` Manter o resto do parágrafo como está.

Não mudar as 8 colunas existentes, nem os formatadores locais (`fmtNota`, `fmtNps`, `fmtPct`, `corPct`), nem o `FaixaBadge`, nem o botão de exportar PDF. A tabela continua uma linha por profissional (D-08).

Criar `tests/js/estrutura-relatorio-bonificacao.test.js` usando `lerSemComentarios` de `tests/js/_fonte.js`. Assertar sobre `resources/js/Pages/Desempenho/RelatorioBonificacao.jsx` que a fonte contém `EmpresasScoreTabela`, `useState`, `tem_detalhe_empresas`, `empresas_score_resumo` e `avisoSemDetalheFechado`; que NÃO contém a string `'Ainda não há detalhe'` (texto vive no módulo de labels); e que continua contendo `route('desempenho.relatorio-bonificacao.pdf'` — prova de que o botão de PDF não foi removido nem alterado de rota.

Rodar `npm run build` ao final — obrigatório por convenção do projeto após qualquer edição de frontend.
  </action>

  <verify>
    <automated>npm run test:js &amp;&amp; npm run build &amp;&amp; php artisan test --filter=Phase123</automated>
  </verify>

  <acceptance_criteria>
- `npm run test:js` sai com código 0 incluindo `estrutura-relatorio-bonificacao.test.js`
- `npm run build` completa sem erro
- `grep -c 'EmpresasScoreTabela' resources/js/Pages/Desempenho/RelatorioBonificacao.jsx` retorna ao menos `2` (import + uso)
- `grep -c 'colSpan={8}' resources/js/Pages/Desempenho/RelatorioBonificacao.jsx` retorna ao menos `2` (o vazio já existente + a linha de expansão)
- `grep -c 'Ainda não há detalhe' resources/js/Pages/Desempenho/RelatorioBonificacao.jsx` retorna `0`
- O `useState` de expansão inicia com um Set vazio (nenhuma linha nasce aberta)
- `grep -c "relatorio-bonificacao.pdf" resources/js/Pages/Desempenho/RelatorioBonificacao.jsx` retorna ao menos `1`
- `php artisan test --filter=Phase123` continua verde
  </acceptance_criteria>

  <done>
A tabela do Relatório continua uma linha por profissional e ganha a justificativa empresa a empresa a um clique, reusando o mesmo componente da tela individual — o mesmo formato de margem, os mesmos motivos traduzidos, o mesmo selo de Shopee.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /desempenho/relatorio-bonificacao` | Rota já gated por `role:admin` (routes/web.php:553-555); esta fase não altera o middleware |
| browser → `GET /desempenho/relatorio-bonificacao/pdf` | Mesma rota gated; o método `pdf()` não recebe nenhuma edição nesta fase |
| `?mes=YYYY-MM` e `?cargo=` → controller | Já validados por `preg_match` e por `in_array(['analista','estrategista'])`; nenhum input novo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-02 | Information Disclosure | props `profissionais[].empresas_score` | accept | A tela já é `role:admin` e já mostra nota, faixa e componentes agregados de todo mundo; o detalhe por empresa é do mesmo nível de sensibilidade e do mesmo público. Nenhum controle novo é necessário |
| T-123-10 | Information Disclosure | PDF ganhando dado por empresa sem querer | mitigate | `pdf()` intocado e blade intocada; teste assere com `assertStringNotContainsString` que a view não cita `empresas_score` nem `nota_empresa`; gate `git diff` vazio na blade. O PDF circula fora do sistema (gestão/RH) — é a superfície de vazamento mais larga do módulo |
| T-123-04 | Denial of Service | N+1 dentro do `map()` de `montarLinhas()` | mitigate | `paraUsuarios()` chamado UMA vez fora do `map()`; teste com `DB::enableQueryLog()` assere exatamente 1 query com 3 profissionais em cena |
| T-123-03 | Tampering | `?mes=`/`?cargo=` manipulados | accept | Mitigação preexistente (regex + allowlist + Eloquent parametrizado); o reader recebe `Carbon` tipado |
| T-123-SC | Tampering | npm/composer installs | accept | Nenhum pacote novo; `react` e `lucide-react` já instalados |
</threat_model>

<verification>
- `php artisan test --filter=RelatorioBonificacaoEmpresasTest` verde
- `php artisan test --filter=RelatorioBonificacaoTest` (preexistente) verde
- `php artisan test --filter=Phase123` verde
- `npm run test:js` verde
- `npm run build` sem erro
- `git diff resources/views/pdf/relatorio-bonificacao.blade.php` vazio
- `git diff --stat app/Services/` vazio
</verification>

<success_criteria>
- Relatório e Auditoria exibem `nota_empresa` lendo a MESMA tabela que a tela individual (UIEM-04)
- A leitura é 1 query por página, não 1 por profissional
- A tabela continua uma linha por profissional, com a justificativa a um clique (D-08)
- O PDF continua resumo, sem segundo template (D-09), provado por teste
- Profissional sem detalhe expande para aviso, não para tabela vazia (D-03)
</success_criteria>

<output>
Criar `.planning/phases/123-telas-e-relat-rios-v21-0/123-05-SUMMARY.md` ao terminar.
</output>
