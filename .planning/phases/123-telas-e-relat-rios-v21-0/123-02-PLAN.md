---
phase: 123-telas-e-relat-rios-v21-0
plan: 02
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - app/Http/Controllers/PerformanceController.php
  - tests/Feature/Phase123/PerformanceShowMesesDisponiveisTest.php
  - tests/Feature/Phase123/PerformanceShowEmpresasScoreTest.php
  - tests/Feature/Phase123/PerformanceShowSemDetalheTest.php
autonomous: true
requirements: [UIEM-02, UIEM-03]

must_haves:
  truths:
    - "O botão 'Mês fechado' de /performance/{id} fica habilitado em produção hoje, com 2026-06 selecionável"
    - "Em competência fechada com detalhe gravado, a tela recebe uma linha por empresa da carteira com nota e os três componentes"
    - "D-01: a lista por empresa aparece somente em competência fechada, lendo desempenho_company_score_snapshots por SELECT puro — o modo 'Em curso' continua exatamente como está, sem lista por empresa e sem nenhuma chamada de API nova"
    - "Competência fechada sem detalhe gravado devolve lista vazia e um sinal explícito de ausência, nunca erro"
    - "Renderizar a tela com detalhe por empresa não dispara nenhuma requisição HTTP"
  artifacts:
    - path: "app/Http/Controllers/PerformanceController.php"
      provides: "props empresas_score / tem_detalhe_empresas / empresas_score_resumo e meses_disponiveis derivado dos dados"
      contains: "companyScoreReader"
    - path: "tests/Feature/Phase123/PerformanceShowEmpresasScoreTest.php"
      provides: "Prova de UIEM-02, D-06 (denominador) e D-07 (Shopee dentro da conta) + regressão de fan-out"
    - path: "tests/Feature/Phase123/PerformanceShowMesesDisponiveisTest.php"
      provides: "Prova da D-02 — competências derivadas dos dados, sem corte fixo"
    - path: "tests/Feature/Phase123/PerformanceShowSemDetalheTest.php"
      provides: "Prova da D-03 — ausência vira aviso, não lista vazia silenciosa nem 500"
  key_links:
    - from: "app/Http/Controllers/PerformanceController.php"
      to: "App\\Services\\Desempenho\\CompanyScoreSnapshotReader"
      via: "injeção no construtor + paraUsuario() dentro de show()"
      pattern: "CompanyScoreSnapshotReader"
    - from: "PerformanceController::show()"
      to: "prop Inertia empresas_score"
      via: "Inertia::render('Performance/Show', [...])"
      pattern: "'empresas_score'"
---

<objective>
Fazer o backend de `PerformanceController::show()` entregar o detalhe por empresa da competência fechada, e **desbloquear a fase** corrigindo o filtro fixo que hoje deixa o dropdown "Mês fechado" vazio em produção.

Purpose: sem a correção da D-02 nenhum usuário consegue abrir uma competência fechada — o critério 2 do ROADMAP não é alcançável e o checkpoint visual da fase teria que ser feito sem dado real. Sem as props novas, as três telas não têm o que exibir.
Output: `PerformanceController::show()` com `meses_disponiveis` derivado dos dados e três props novas, mais as 3 primeiras suítes `Phase123`.

**Esta fase não calcula nada.** A leitura é `SELECT` puro via `CompanyScoreSnapshotReader` (Plano 01). É PROIBIDO chamar `CompanyScoreService::computeEmpresasScore()` ou passar `incluirEmpresasScore: true` daqui — isso reabriria o fan-out de HTTP por empresa que já produziu página de 70s neste módulo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-RESEARCH.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-VALIDATION.md
@.planning/learnings/desempenho-bonificacao.md
@app/Http/Controllers/PerformanceController.php
@tests/Feature/PerformanceShowPeriodoTest.php
</context>

<interfaces>
<!-- Contratos prontos, entregues pelo Plano 01. Não explorar o codebase para achá-los. -->

`App\Services\Desempenho\CompanyScoreSnapshotReader` (criado no Plano 01):
- `paraUsuario(int $userId, Carbon $mes): Collection` — Collection de arrays com 17 chaves
  (`company_id`, `company_name`, `fonte_financeira`, `status`, `nps_pontos`, `faturamento_atual`,
  `faturamento_anterior`, `faturamento_var_pct`, `faturamento_pontos`, `margem_pct_atual`,
  `margem_pct_anterior`, `margem_var_pp`, `margem_pontos`, `componentes_presentes`,
  `nota_empresa`, `nota_empresa_parcial`, `quality`), já ordenada (nota desc, null por último,
  desempate por nome).
- `paraUsuarios(array $userIds, Carbon $mes): Collection` — agrupada por `user_id`.
- `resumo(Collection $linhas): array` — `['entraram' => int, 'nao_entraram' => int]`,
  onde `entraram` = contagem de `status === 'complete'`.

`Tests\Feature\Phase123\Phase123TestCase` (criado no Plano 01) — base abstract com
`criarUserElegivel()`, `adminLogado()`, `darCarteira()`, `seedMensal()`, `seedLinha()`,
`seedLinhaShopee()` e `Carbon::setTestNow('2026-08-15 10:00:00')`.

Estado ATUAL de `PerformanceController::show()` (já lido, linhas 1261-1327):
- `$ctx = $this->resolveContextoPeriodo($request)` devolve `periodo.is_closed`.
- `$resultado` vem de `breakdown_json` quando existe snapshot mensal, senão de `computeCached()`.
- `$mesesFechados` é montado nas linhas 1294-1301 com o corte fixo a remover.
- O `Inertia::render('Performance/Show', [...])` já entrega `user`, `resultado`,
  `mes_selecionado`, `modo`, `meses_disponiveis`, `periodo`, `bonus`, `nps_window`,
  `empresas_invalidadas`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: D-02 — competências do dropdown derivadas dos dados (remove o corte fixo 2026-08-01)</name>

  <files>
app/Http/Controllers/PerformanceController.php
tests/Feature/Phase123/PerformanceShowMesesDisponiveisTest.php
  </files>

  <read_first>
app/Http/Controllers/PerformanceController.php
tests/Feature/PerformanceShowPeriodoTest.php
tests/Feature/Phase123/Phase123TestCase.php
.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
  </read_first>

  <action>
Em `app/Http/Controllers/PerformanceController.php`, na montagem de `$mesesFechados` dentro de `show()` (hoje nas linhas 1294-1301), remover a cláusula `->whereDate('mes_referencia', '>=', '2026-08-01')`. O resto da query fica igual: `DesempenhoScoreSnapshot::mensal()->where('user_id', $user->id)->orderByDesc('mes_referencia')->pluck('mes_referencia')` mapeado para `Y-m`, `->unique()->values()`.

A existência da linha já é o sinal correto e não há risco de o mês corrente entrar na lista: `DesempenhoScoreSnapshot::mensal()` filtra `whereNotNull('mes_referencia')`, e só `ConsolidarMesDesempenho` escreve linha mensal — ela só existe para competência efetivamente fechada e consolidada.

Atualizar os DOIS comentários que hoje descrevem o corte: o comentário inline imediatamente acima da query (hoje linha 1292, texto `Meses fechados disponíveis para o dropdown (DESEMP-14 · >= 2026-08-01)`) e o docblock de `show()` (hoje linha 1198, texto `não-null + >= 2026-08-01 (DESEMP-14)`). O texto novo, em pt-BR, deve dizer que as competências são derivadas dos dados — existe linha mensal, logo o mês está fechado e consolidado — e citar a D-02 da Fase 123 com o motivo real: o corte fixo deixava o dropdown vazio em produção porque `desempenho:consolidar-mes` congela o mês ANTERIOR no último dia do mês, de modo que a primeira competência a passar em `>= 2026-08-01` só existiria em 30/09/2026.

**Não** remover a menção a DESEMP-14 de `resources/js/Pages/Performance/Index.jsx` (linha 27) — aquele comentário fala do RANKING, que esta fase não altera. Nenhum outro arquivo é tocado nesta task.

Criar `tests/Feature/Phase123/PerformanceShowMesesDisponiveisTest.php`, namespace `Tests\Feature\Phase123`, estendendo `Phase123TestCase`, estilo `#[Test]`. Usar um profissional SEM carteira como alvo (o mesmo truque de `tests/Feature/PerformanceShowPeriodoTest.php`: `compute()` retorna `sem_carteira` cedo, sem HTTP) e um admin autenticado via `adminLogado()`. Casos:
- Com `seedMensal()` para `2026-06-01` e `2026-05-01`, `GET route('performance.show', $alvo)` devolve a prop `meses_disponiveis` igual a `['2026-06', '2026-05']` — nesta ordem (decrescente). Este é o caso que hoje falharia: com o corte fixo a prop viria vazia. Assertar por `->where('meses_disponiveis', ['2026-06', '2026-05'])`, não por contagem.
- Um snapshot DIÁRIO (`mes_referencia => null`) não aparece na lista.
- Um snapshot mensal de OUTRO usuário não aparece na lista do alvo.
- Sem nenhum snapshot mensal, `meses_disponiveis` é `[]` e a resposta é 200.
- `Http::fake()` no início e `Http::assertNothingSent()` no fim de ao menos um dos casos.
  </action>

  <verify>
    <automated>php artisan test --filter=PerformanceShowMesesDisponiveisTest</automated>
  </verify>

  <acceptance_criteria>
- `grep -c '2026-08-01' app/Http/Controllers/PerformanceController.php` retorna `0` (código E comentários)
- `php artisan test --filter=PerformanceShowMesesDisponiveisTest` sai com código 0, com ao menos 4 testes
- O teste assere `meses_disponiveis === ['2026-06', '2026-05']` literalmente
- `php artisan test --filter=PerformanceShowPeriodoTest` continua verde (3 testes)
- `grep -c 'DESEMP-14' resources/js/Pages/Performance/Index.jsx` continua `1` (o ranking não foi tocado)
- `git diff --name-only` nesta task lista apenas `app/Http/Controllers/PerformanceController.php` e o novo arquivo de teste
  </acceptance_criteria>

  <done>
O dropdown "Mês fechado" passa a oferecer as competências que realmente têm snapshot mensal do profissional — 2026-06 fica selecionável em produção hoje — e o corte fixo `2026-08-01` não existe mais em lugar nenhum do controller.
  </done>
</task>

<task type="auto">
  <name>Task 2: props do detalhe por empresa em show() (D-01, D-03, D-06)</name>

  <files>
app/Http/Controllers/PerformanceController.php
  </files>

  <read_first>
app/Http/Controllers/PerformanceController.php
app/Services/Desempenho/CompanyScoreSnapshotReader.php
.planning/phases/123-telas-e-relat-rios-v21-0/123-RESEARCH.md
.planning/learnings/desempenho-bonificacao.md
  </read_first>

  <action>
Injetar `App\Services\Desempenho\CompanyScoreSnapshotReader` no construtor de `PerformanceController` como propriedade promovida privada `$companyScoreReader`, ao lado das dependências que já estão lá (`$scoreService`, `$periodResolver`). Adicionar o `use` correspondente.

Dentro de `show()`, depois do cálculo de `$invalidadasDoUser` e antes do `return Inertia::render(...)`, montar o detalhe por empresa:
- Inicializar `$empresasScore` como Collection vazia.
- Preencher com `$this->companyScoreReader->paraUsuario($user->id, $mesReferencia)` **somente quando** `$ctx['periodo']['is_closed']` é `true`. Este guard é a D-01 literal: no modo "Em curso" a seção não existe, e o custo é zero de API nos dois ramos.
- Derivar `$temDetalheEmpresas = $empresasScore->isNotEmpty()`. **Não** derivar isso de `is_closed` sozinho — 2026-05 e 2026-04 são meses fechados que nunca tiveram consolidação nenhuma, e um snapshot congelado anterior à Fase 122 também não tem linhas. `is_closed` é propriedade de calendário, não de existência de dado.
- Derivar `$resumoEmpresas = $this->companyScoreReader->resumo($empresasScore)`.

Acrescentar três props ao array do `Inertia::render('Performance/Show', [...])`, mantendo TODAS as props atuais intactas (`user`, `resultado`, `mes_selecionado`, `modo`, `meses_disponiveis`, `periodo`, `bonus`, `nps_window`, `empresas_invalidadas`):
- `'empresas_score' => $empresasScore->values()->all()`
- `'tem_detalhe_empresas' => $temDetalheEmpresas`
- `'empresas_score_resumo' => $resumoEmpresas`

O denominador da D-06 é pré-calculado aqui no backend, não recontado no front: o critério de "entrou na conta" é `status === 'complete'`, o MESMO que decide o denominador da nota em `DesempenhoScoreService::computeNotaFinalPorEmpresa()`. Reinventar a contagem no JSX arriscaria divergir do critério real quando a flag for ligada no futuro.

Comentário em pt-BR acima do bloco, no estilo do arquivo, citando D-01/D-03/D-06 da Fase 123 e registrando por que a leitura é do reader e nunca do serviço de cálculo.

**Proibições desta task, sem exceção:**
- Não chamar `CompanyScoreService::computeEmpresasScore()`.
- Não passar `incluirEmpresasScore: true` para `DesempenhoScoreService::compute()` nem para `computeCached()`.
- Não ler `breakdown_json['empresas_score']` — a fonte é a tabela dedicada, é isso que garante "a mesma fonte" de UIEM-04 nos três controllers.
- Não editar `app/Services/DesempenhoScoreService.php` nem `app/Services/Desempenho/CompanyScoreService.php`. Qualquer edição neles arrisca quebrar `PayloadBaselineFlagOffTest` (Phase120), o teste dourado que trava o shape do payload com a flag desligada.
- Não mexer em `config/metrics.php` — `performance_company_first_score` continua `false`.
  </action>

  <verify>
    <automated>php artisan test --filter=PerformanceShowPeriodoTest && php artisan test --filter=Phase120</automated>
  </verify>

  <acceptance_criteria>
- `grep -v '^\s*[/*]' app/Http/Controllers/PerformanceController.php | grep -c 'computeEmpresasScore\|incluirEmpresasScore'` retorna `0`
- `grep -c "'empresas_score'" app/Http/Controllers/PerformanceController.php` retorna ao menos `1`
- `grep -c "'tem_detalhe_empresas'" app/Http/Controllers/PerformanceController.php` retorna ao menos `1`
- `grep -c "'empresas_score_resumo'" app/Http/Controllers/PerformanceController.php` retorna ao menos `1`
- `git diff --stat app/Services/` não lista nenhum arquivo modificado
- `php artisan test --filter=PerformanceShowPeriodoTest` verde (3 testes)
- `php artisan test --filter=Phase120` verde (18 testes) — `PayloadBaselineFlagOffTest` intocado
- `php artisan test --filter=Phase122` no mesmo resultado de antes da task
  </acceptance_criteria>

  <done>
`Performance/Show` recebe `empresas_score`, `tem_detalhe_empresas` e `empresas_score_resumo` em competência fechada com detalhe gravado; recebe lista vazia e `tem_detalhe_empresas = false` em qualquer outro caso; e o modo "Em curso" ficou byte-idêntico em comportamento.
  </done>
</task>

<task type="auto">
  <name>Task 3: suítes do detalhe por empresa — denominador, Shopee, fan-out e ausência</name>

  <files>
tests/Feature/Phase123/PerformanceShowEmpresasScoreTest.php
tests/Feature/Phase123/PerformanceShowSemDetalheTest.php
  </files>

  <read_first>
app/Http/Controllers/PerformanceController.php
tests/Feature/Phase123/Phase123TestCase.php
tests/Feature/PerformanceShowPeriodoTest.php
.planning/phases/123-telas-e-relat-rios-v21-0/123-VALIDATION.md
  </read_first>

  <action>
Criar as duas suítes em `tests/Feature/Phase123/`, namespace `Tests\Feature\Phase123`, ambas estendendo `Phase123TestCase`, estilo `#[Test]`, todas as asserções de prop via `assertInertia` sobre o componente `Performance/Show`.

**Fixture hermética obrigatória em todos os casos:** semear `seedMensal($alvo->id, '2026-06-01')` ANTES do `GET`. Com o snapshot mensal presente, `show()` usa `breakdown_json` e não cai no `computeCached()` — é isso que torna o teste determinístico e sem rede. Chamar `Http::fake()` no `setUp` da suíte.

`PerformanceShowEmpresasScoreTest` — casos:
- **Caminho comum:** carteira com 3 linhas em `2026-06` (2 com `status='complete'`, 1 com `status='partial'`). `GET route('performance.show', $alvo) . '?mes=2026-06'` devolve `tem_detalhe_empresas === true`, `empresas_score` com 3 itens, `empresas_score_resumo.entraram === 2` e `empresas_score_resumo.nao_entraram === 1`.
- **Três componentes visíveis (UIEM-02):** cada item de `empresas_score` traz as chaves `nota_empresa`, `nps_pontos`, `faturamento_pontos`, `margem_pontos`, `margem_pct_atual`, `margem_pct_anterior`, `margem_var_pp` e `quality.motivos` (array). Assertar também que NÃO traz `origem` nem `gerado_em`.
- **Regressão de fan-out (o mais importante da suíte):** `Http::fake()` e, depois do `GET` com detalhe presente, `Http::assertNothingSent()`. Um teste desta fase que demora mais de ~1s ou que precisa de resposta fake real é sinal de que algo voltou a chamar o dispatcher ao vivo.
- **D-07 — Shopee dentro da conta:** usar `seedLinhaShopee()` para uma empresa (`status='complete'`, `margem_pontos = 1.0`, `quality.margin_source = 'placeholder_shopee'`, `margem_var_pp = null`) e assertar que ela aparece em `empresas_score` E é contada em `entraram`. Ela **nunca** pode ser excluída do denominador.
- **Fixture Matheus Estrela (carteira só-Shopee):** 2 linhas, ambas `seedLinhaShopee()`. Resultado esperado: `entraram === 2`, `nao_entraram === 0`, e as duas com `quality.margin_source === 'placeholder_shopee'`. Nomear o teste de forma que o caso real fique explícito.
- **Fixture Felipe (denominador pequeno):** 3 linhas `complete` + 27 linhas `partial` na mesma competência. Resultado esperado: `entraram === 3`, `nao_entraram === 27`, `empresas_score` com 30 itens. Este é o caso que a D-06 existe para tornar impossível de ignorar.
- **D-01 — "Em curso" não lista:** semear linhas por empresa para a competência CORRENTE (2026-08) e fazer `GET route('performance.show', $alvo)` sem `?mes` (modo em curso, `periodo.is_closed === false`). Esperado: `empresas_score === []` e `tem_detalhe_empresas === false`, mesmo existindo linhas gravadas.
- **Ordenação:** com notas 3.5, 4.8 e uma `null`, a ordem de `empresas_score` é 4.8, 3.5, null.

`PerformanceShowSemDetalheTest` — casos (D-03/UIEM-03):
- Competência fechada COM snapshot mensal mas SEM nenhuma linha em `desempenho_company_score_snapshots`: resposta 200, `tem_detalhe_empresas === false`, `empresas_score === []`, `empresas_score_resumo.entraram === 0`, `empresas_score_resumo.nao_entraram === 0`. Nada de 500, nada de prop ausente.
- Competência fechada sem nenhum snapshot (usar `?mes=2026-05`): resposta 200 e as mesmas props presentes com os mesmos valores. Este é o caso "mês fechado no calendário que nunca teve consolidação".
- Isolamento: linhas de OUTRO profissional na mesma competência não fazem `tem_detalhe_empresas` virar `true` para o alvo.
- Borda "sem carteira" (caso Débora Lima): profissional elegível pelo cargo e sem nenhuma empresa vinculada não pode gerar erro — a resposta é 200 com `empresas_score === []`. Não aparecer não é bug; quebrar é.
  </action>

  <verify>
    <automated>php artisan test --filter=Phase123</automated>
  </verify>

  <acceptance_criteria>
- `php artisan test --filter=Phase123` sai com código 0
- `PerformanceShowEmpresasScoreTest` tem um caso que chama `Http::assertNothingSent()`
- Existe um teste cujo nome cita `felipe` e assere `entraram === 3` e `nao_entraram === 27`
- Existe um teste cujo nome cita `shopee` e assere que a linha `placeholder_shopee` está DENTRO de `entraram`
- Existe um teste que assere `empresas_score === []` no modo em curso mesmo com linhas gravadas na competência corrente
- `PerformanceShowSemDetalheTest` tem 4 testes e nenhum deles espera status diferente de 200
- `php artisan test --filter=Phase122` sem regressão
  </acceptance_criteria>

  <done>
As três props novas estão travadas por teste nos casos reais de produção (Felipe, Matheus Estrela, Débora Lima), o denominador da D-06 é verificável, a Shopee está provadamente dentro da conta, e a regressão de fan-out de API tem um teste que acende se alguém voltar a chamar o dispatcher na tela.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /performance/{user}` | Rota já gated por `permission:core.performance` (routes/web.php:521-522); esta fase não altera o middleware |
| `?mes=YYYY-MM` → `Carbon` | Input já validado por `preg_match('/^\d{4}-\d{2}$/')` em `resolveContextoPeriodo()`; nenhum input novo é criado |
| prop Inertia `empresas_score` → browser | Margem e faturamento por empresa passam a trafegar para quem já podia ver a nota agregada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Information Disclosure | prop `empresas_score` de `PerformanceController::show()` | mitigate | O conteúdo novo vive dentro de rota já gated por `permission:core.performance`; a fase não adiciona rota, middleware nem endpoint. O shape enxuto do reader (Plano 01, T-123-05) impede vazar `origem`/`gerado_em`/timestamps. Escopo é sempre `doUsuario($user->id)` — nunca a competência inteira |
| T-123-03 | Tampering | `?mes=` manipulado para forçar competência arbitrária | accept | Mitigação preexistente: regex + `Carbon::createFromFormat` + Eloquent parametrizado; o scope `daCompetencia()` recebe `Carbon`, nunca string crua. Nenhuma competência fora do escopo do usuário é alcançável porque `doUsuario()` sempre é aplicado |
| T-123-04 | Denial of Service | `show()` reabrindo o fan-out de HTTP por empresa | mitigate | Guard `is_closed` + leitura exclusiva pelo reader; gate de `grep` proibindo `computeEmpresasScore`/`incluirEmpresasScore` no controller; teste com `Http::assertNothingSent()` |
| T-123-06 | Tampering | Edição acidental de `DesempenhoScoreService`/`CompanyScoreService` mudando o shape do payload | mitigate | Proibição explícita na task 2 + gate `git diff --stat app/Services/` vazio + `php artisan test --filter=Phase120` verde (`PayloadBaselineFlagOffTest`) |
| T-123-SC | Tampering | npm/composer installs | accept | Nenhum pacote novo nesta fase; `composer.json`/`package.json` intocados |
</threat_model>

<verification>
- `php artisan test --filter=Phase123` verde
- `php artisan test --filter=PerformanceShowPeriodoTest` verde
- `php artisan test --filter=Phase120` verde (teste dourado do payload)
- `php artisan test --filter=Phase122` sem regressão
- `grep -c '2026-08-01' app/Http/Controllers/PerformanceController.php` retorna 0
- `git diff --stat app/Services/` vazio
</verification>

<success_criteria>
- O dropdown "Mês fechado" oferece 2026-06 em produção (D-02, bug bloqueante resolvido)
- `Performance/Show` recebe uma linha por empresa com nota e os três componentes em competência fechada com detalhe (UIEM-02)
- Ausência de detalhe vira sinal explícito, nunca lista vazia silenciosa nem 500 (UIEM-03/D-03)
- Nenhuma chamada HTTP é feita ao renderizar a tela com detalhe (regressão de fan-out travada por teste)
- Shopee está dentro do denominador, provado por teste (D-07)
</success_criteria>

<output>
Criar `.planning/phases/123-telas-e-relat-rios-v21-0/123-02-SUMMARY.md` ao terminar.
</output>
