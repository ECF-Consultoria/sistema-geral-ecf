---
phase: 123-telas-e-relat-rios-v21-0
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/BonusAuditoriaController.php
  - tests/Feature/Phase123/AuditoriaBonusNotaEmpresaTest.php
  - resources/js/Pages/Desempenho/Auditoria.jsx
  - resources/js/lib/desempenhoLabels.js
  - tests/js/estrutura-auditoria-desempenho.test.js
autonomous: true
gap_closure: true
requirements: [UIEM-04]

must_haves:
  truths:
    - "BonusAuditoriaController::index() só chama computeCached() ao vivo quando a competência não tem snapshot mensal utilizável (breakdown_json com a chave componentes) para aquele profissional"
    - "Quando nota_final vem do fechamento congelado, a linha do profissional expõe nota_congelada=true"
    - "Quando nota_final vem de recomputo ao vivo, a tela mostra um selo explícito ao lado da nota, distinguindo essa safra da nota por empresa (sempre congelada) — nunca lado a lado sem sinal"
  artifacts:
    - path: "app/Http/Controllers/BonusAuditoriaController.php"
      provides: "index() snapshot-first para nota_final (mesmo padrão de RelatorioBonificacaoController::montarLinhas()), flag nota_congelada por profissional"
    - path: "resources/js/Pages/Desempenho/Auditoria.jsx"
      provides: "NotaBadge com selo de safra recalculada quando nota_congelada===false"
    - path: "resources/js/lib/desempenhoLabels.js"
      provides: "NOTA_RECALCULADA_TEXTO / NOTA_RECALCULADA_TITULO"
  key_links:
    - from: "BonusAuditoriaController::index()"
      to: "DesempenhoScoreSnapshot::mensal()"
      via: "snapshot-first com fallback computeCached(), mesmo padrão de RelatorioBonificacaoController::montarLinhas() linhas 100-125"
      pattern: "DesempenhoScoreSnapshot::mensal\\(\\)"
    - from: "Auditoria.jsx NotaBadge"
      to: "prof.nota_congelada"
      via: "prop congelada renderiza selo NOTA_RECALCULADA_TEXTO"
      pattern: "nota_congelada"
---

<objective>
Fechar o Gap 2 do `123-VERIFICATION.md` (CR-02 do `123-REVIEW.md`): a Auditoria de Bônus — tela usada para decidir invalidação de empresa para pagamento — exibe `nota_final` do profissional recomputada AO VIVO (`computeCached()`) ao lado de `nota_empresa` por empresa lida do fechamento CONGELADO, sem nenhum sinal distinguindo as duas safras.

Purpose: `.planning/learnings/desempenho-bonificacao.md` §2 documenta caso real — a releitura da MESMA competência fechada, 14 horas depois, mudou a margem de 4,24% para 2,52% e derrubou a faixa de bônus de alguém sem nenhuma mudança de código. Nesta tela, um admin vendo `nota_final` não bater com as notas por empresa abaixo vai concluir erro de cálculo e agir sobre isso — exatamente o tipo de decisão errada que este plano existe para prevenir.

Output: `BonusAuditoriaController::index()` passa a ser snapshot-first (Opção A do REVIEW), lendo `breakdown_json` do fechamento quando existe e caindo em `computeCached()` só como fallback — mesmo padrão que `RelatorioBonificacaoController::montarLinhas()` já usa. A tela ganha um selo visível sempre que a nota exibida não é a congelada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-VERIFICATION.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-REVIEW.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
@.planning/learnings/desempenho-bonificacao.md
@app/Http/Controllers/BonusAuditoriaController.php
@app/Http/Controllers/RelatorioBonificacaoController.php
@resources/js/Pages/Desempenho/Auditoria.jsx
@resources/js/lib/desempenhoLabels.js
@tests/Feature/Phase123/Phase123TestCase.php
@tests/Feature/Phase123/AuditoriaBonusNotaEmpresaTest.php
</context>

<interfaces>
<!-- Padrão snapshot-first JÁ EM PRODUÇÃO para o MESMO problema — replicar,
     não inventar novo. app/Http/Controllers/RelatorioBonificacaoController.php
     linhas 100-125 (montarLinhas()): -->

```php
// Snapshot mensal fechado da competência (fonte autoritativa; 1 query).
$snapshots = DesempenhoScoreSnapshot::mensal()
    ->whereIn('user_id', $users->pluck('id'))
    ->whereDate('mes_referencia', $competencia->toDateString())
    ->get()
    ->keyBy('user_id');

// ...dentro do map() de profissionais:
// Snapshot-first: breakdown_json do fechamento; fallback computeCached
// quando a competência não foi consolidada para este profissional.
$snap = $snapshots->get($u->id);
$resultado = ($snap && isset($snap->breakdown_json['componentes']))
    ? $snap->breakdown_json
    : $this->scoreService->computeCached($u, $competencia);
```

From app/Http/Controllers/BonusAuditoriaController.php (estado atual — o que
muda): linha 77 `$detalhePorUser = $this->companyScoreReader->paraUsuarios(...)`
já lê a nota POR EMPRESA da fonte congelada corretamente; linha 87
`$resultado = $this->scoreService->computeCached($u, $competencia);` é o que
precisa virar snapshot-first. A closure do `map()` (linha 85) já recebe
`$cargosPorUser, $competencia, $invalidadas, $detalhePorUser` via `use` —
acrescentar `$snapshotsMensais`.

From resources/js/Pages/Desempenho/Auditoria.jsx (o que muda):
```jsx
function NotaBadge({ nota }) {                    // linha 32 — ganha 2º prop
    if (nota === null || nota === undefined) { ... }
    // ...
}
// ...
<NotaBadge nota={prof.nota_final} />               // linha 186, dentro de ProfissionalCard
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BonusAuditoriaController snapshot-first para nota_final (CR-02, Opção A)</name>
  <files>app/Http/Controllers/BonusAuditoriaController.php, tests/Feature/Phase123/AuditoriaBonusNotaEmpresaTest.php</files>
  <behavior>
    - Profissional com snapshot mensal gravado (`seedMensal()`) para a competência: `profissionais.N.nota_final` vem EXATAMENTE do `breakdown_json['nota_final']` do snapshot (usar um valor fracionário distinguível, ex. 4.97, que não coincide com o que `computeCached()` produziria a partir da fixture de teste) e `profissionais.N.nota_congelada === true`.
    - Profissional SEM snapshot mensal (só `seedLinha()` por empresa, sem `seedMensal()`): `profissionais.N.nota_congelada === false` — cai no fallback `computeCached()`.
    - Profissional com snapshot mensal gravado mas cujo `breakdown_json` NÃO tem a chave `componentes` (simulando snapshot truncado ou de safra anterior): também cai no fallback e `nota_congelada === false` — mesma guarda que `RelatorioBonificacaoController::montarLinhas()` já usa (`isset($snap->breakdown_json['componentes'])`).
    - `--filter=AuditoriaBonusNotaEmpresaTest` continua 100% verde — nenhum teste existente muda de comportamento (a leitura por empresa em `desempenho_company_score_snapshots`, linha 77, não é tocada por esta task).
  </behavior>
  <action>
    Em `BonusAuditoriaController::index()`, logo após `$detalhePorUser = $this->companyScoreReader->paraUsuarios(...)` (linha 77), buscar os snapshots mensais dos profissionais candidatos numa única query: `DesempenhoScoreSnapshot::mensal()->whereIn('user_id', $users->pluck('id'))->whereDate('mes_referencia', $competencia->toDateString())->get()->keyBy('user_id')` — MESMO padrão de `RelatorioBonificacaoController::montarLinhas()` (linhas 100-105), inclusive o uso de `whereDate()` em vez de comparar `mes_referencia` cru (evita a armadilha de SQLite documentada em `.planning/learnings/desempenho-bonificacao.md` §6/§3).

    Dentro do `$users->map(function ($u) use (...))` (linha 85), acrescentar `$snapshotsMensais` ao `use`. Substituir a chamada incondicional `$resultado = $this->scoreService->computeCached($u, $competencia);` (linha 87) por: `$snap = $snapshotsMensais->get($u->id);` seguido de `$notaCongelada = $snap !== null && is_array($snap->breakdown_json) && isset($snap->breakdown_json['componentes']);` e `$resultado = $notaCongelada ? $snap->breakdown_json : $this->scoreService->computeCached($u, $competencia);` — MESMO padrão de `RelatorioBonificacaoController::montarLinhas()` (linhas 122-125).

    Acrescentar `'nota_congelada' => $notaCongelada,` ao array devolvido por profissional (linha ~124-132, ao lado de `'nota_final'`), para o front poder rotular qual safra está sendo mostrada.

    NÃO tocar `RelatorioBonificacaoController.php` (já é snapshot-first — fora de escopo), nem a leitura por empresa em `desempenho_company_score_snapshots` (linha 77-83, já correta), nem `bustarCacheDaEmpresa()` (linhas 216-239, invalidação — comportamento inalterado).

    Escrever os 3 cenários do `<behavior>` em `AuditoriaBonusNotaEmpresaTest.php`, seguindo o estilo dos testes já existentes na suíte (`seedMensal()`/`seedLinha()`/`darCarteira()`/`criarUserElegivel()` de `Phase123TestCase`).
  </action>
  <verify>
    <automated>php artisan test --filter=AuditoriaBonusNotaEmpresaTest</automated>
  </verify>
  <done>`BonusAuditoriaController::index()` só chama `computeCached()` quando a competência não tem snapshot mensal utilizável para aquele profissional; toda linha de `profissionais` expõe `nota_congelada`.</done>
</task>

<task type="auto">
  <name>Task 2: Selo de safra na Auditoria.jsx (CR-02, complemento visual)</name>
  <files>resources/js/Pages/Desempenho/Auditoria.jsx, resources/js/lib/desempenhoLabels.js, tests/js/estrutura-auditoria-desempenho.test.js</files>
  <action>
    Em `resources/js/lib/desempenhoLabels.js`, na seção "Constantes de texto (pt-BR, sem jargão — texto final)", acrescentar duas constantes novas: `NOTA_RECALCULADA_TEXTO` (selo curto, ex. `'recalculada agora'`) e `NOTA_RECALCULADA_TITULO` (tooltip explicando, em pt-BR e sem jargão, que a nota do profissional foi recalculada ao vivo nesta consulta e pode divergir das notas por empresa abaixo, que são as congeladas no fechamento — sem implicar erro de cálculo). Seguir o mesmo padrão de nomenclatura e formato já usado por `SELO_SHOPEE_TEXTO`/`SELO_SHOPEE_TITULO` no mesmo arquivo.

    Em `Auditoria.jsx`, importar as duas constantes novas de `@/lib/desempenhoLabels` (linha 9-13, mesmo bloco de import já existente). Alterar `NotaBadge` (linha 32) para aceitar uma segunda prop `congelada` e renderizar, ao lado da nota, um selo pequeno quando `congelada === false` EXPLICITAMENTE (nunca quando `undefined`/`null` — payload sem o campo não deve gerar alarme falso). Usar o mesmo padrão visual de selo `rounded-full border px-2 py-0.5 text-[10px]` já usado em `NotaEmpresaCell` (linhas 82-87) para o selo Shopee, mas com uma paleta distinta (ex. `border-sky-500/25 bg-sky-500/10 text-sky-200` — mesmo tom já usado em `CARGO_COR.analista` neste arquivo, linha 27) para não ser confundido com o aviso amber de placeholder Shopee. `title={NOTA_RECALCULADA_TITULO}`, texto `{NOTA_RECALCULADA_TEXTO}`.

    Em `ProfissionalCard` (linha 186), passar `<NotaBadge nota={prof.nota_final} congelada={prof.nota_congelada} />`.

    Acrescentar um teste a `tests/js/estrutura-auditoria-desempenho.test.js` (mesmo padrão `lerSemComentarios()` já usado no arquivo) provando: (a) `NotaBadge` referencia `congelada`/`nota_congelada`; (b) a fonte usa `NOTA_RECALCULADA_TEXTO`/`NOTA_RECALCULADA_TITULO` importados de `desempenhoLabels`, nunca o texto do selo escrito à mão no JSX (mesmo padrão anti-hardcode do teste já existente para o aviso de ausência, linhas 37-40).

    Rodar `npm run build` ao final (convenção do projeto).
  </action>
  <verify>
    <automated>node --test tests/js/estrutura-auditoria-desempenho.test.js</automated>
  </verify>
  <done>Um admin abrindo a Auditoria de Bônus vê, ao lado de qualquer nota agregada que não veio do fechamento congelado, um selo "recalculada agora" com tooltip explicando a divergência de safra — nunca lado a lado sem sinal. `npm run build` roda sem erro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin autenticado → `BonusAuditoriaController::index()` | decisão de invalidar empresa para pagamento de bônus, baseada em números que podem vir de duas safras (congelada × ao vivo) sem sinal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-123-13 | Tampering (integridade de decisão de negócio) | `BonusAuditoriaController::index()` | mitigate | snapshot-first para `nota_final` (mesma safra da `nota_empresa` já congelada) + flag `nota_congelada` explícita, selo visível na tela quando a nota veio de recomputo ao vivo — evita decisão de invalidação de bônus baseada em comparação de safras diferentes (precedente real em `.planning/learnings/desempenho-bonificacao.md` §2) |
| T-123-02 (herdado, reafirmado) | Information Disclosure | props de `BonusAuditoriaController::index()` | accept | A tela inteira já é `role:admin`; este plano não adiciona campo sensível novo, só corrige a fonte de um campo já exposto |
| T-123-SC | Tampering | npm/composer installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
```bash
php artisan test --filter=AuditoriaBonusNotaEmpresaTest
php artisan test --filter=Phase123
php artisan test --filter=Phase120
php artisan test --filter=Desempenho
npm run test:js
npm run build
```
Baseline conhecida: `--filter=Desempenho` 14 failed/101 passed (pré-existente, não é regressão). `--filter=Phase123` deve continuar 41/41 (ou 41 + os novos testes deste plano, todos verdes). `npm run test:js` deve continuar na mesma baseline (1 falha pré-existente e não relacionada em `estrutura-grade-glide.test.js`).
</verification>

<success_criteria>
- `AuditoriaBonusNotaEmpresaTest` prova os 3 cenários de safra (congelada / sem snapshot / snapshot truncado), todos verdes.
- `Auditoria.jsx` renderiza o selo de safra quando `nota_congelada === false`, nunca quando o campo está ausente.
- `--filter=Phase123` e `--filter=Phase120` continuam verdes; `--filter=Desempenho` na baseline conhecida (14 failed/101 passed), sem regressão nova.
- `npm run build` roda sem erro.
</success_criteria>

<output>
Create `.planning/phases/123-telas-e-relat-rios-v21-0/123-08-SUMMARY.md` when done
</output>
