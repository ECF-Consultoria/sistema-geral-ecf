---
phase: 123-telas-e-relat-rios-v21-0
plan: 06
type: execute
wave: 5
depends_on: ["123-02", "123-03", "123-04", "123-05"]
files_modified:
  - .planning/phases/123-telas-e-relat-rios-v21-0/123-CHECKPOINT-VISUAL.md
autonomous: false
requirements: [UIEM-01, UIEM-02, UIEM-03, UIEM-04]

must_haves:
  truths:
    - "A suíte completa está no mesmo estado da baseline documentada — nenhuma regressão nova"
    - "npm run build roda sem erro (convenção obrigatória do projeto)"
    - "As três telas foram vistas com dado real de 2026-06 e aprovadas pelo usuário"
    - "Os três casos-limite reais (Felipe, Matheus Estrela, Débora Lima) foram conferidos na tela"
    - "Nenhuma das três telas dá erro por causa de profissional ausente da consolidação"
  artifacts:
    - path: ".planning/phases/123-telas-e-relat-rios-v21-0/123-CHECKPOINT-VISUAL.md"
      provides: "Registro do que foi conferido, com o resultado observado por item"
  key_links:
    - from: "checkpoint visual"
      to: "competência 2026-06"
      via: "única competência com detalhe por empresa em produção (286 linhas, 11 profissionais)"
      pattern: "2026-06"
---

<objective>
Fechar o critério 5 do ROADMAP: `npm run build` rodado e checkpoint visual aprovado. É um gate humano — nenhuma asserção automatizada substitui olhar as três telas com dado real.

Purpose: os casos que mais importam nesta fase não são reproduzíveis por fixture sintética. Felipe (margem sobre 3 de 30 empresas), Matheus Estrela (carteira só-Shopee, nota puxada por placeholder) e Débora Lima (ausente da consolidação) existem no banco de produção e são exatamente onde as decisões D-06, D-07 e D-03 são postas à prova.
Output: a suíte completa comparada com a baseline conhecida, o build de produção, e um registro do que foi visto em cada item.

**Competência obrigatória: 2026-06.** É a ÚNICA com detalhe por empresa em produção hoje (286 linhas, 11 profissionais, rollout da Fase 122 em 2026-08-03). 2026-07 só existe a partir de 31/08 e **não** pode ser antecipada por reconsolidação manual — recompute de competência fechada pode mexer em pagamento de quem está perto de uma fronteira de régua (learnings §2: Danilo perdeu o bônus de junho/2026 sem nenhuma mudança de código, a 0,24 pp da fronteira).

**Nenhum deploy nesta fase sem autorização explícita do usuário.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-telas-e-relat-rios-v21-0/123-VALIDATION.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
@.planning/learnings/desempenho-bonificacao.md
@.planning/phases/122-persist-ncia-por-empresa-e-comandos-v21-0/122-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suíte completa contra a baseline conhecida + build de produção</name>

  <files>
.planning/phases/123-telas-e-relat-rios-v21-0/123-CHECKPOINT-VISUAL.md
  </files>

  <read_first>
.planning/phases/123-telas-e-relat-rios-v21-0/123-VALIDATION.md
.planning/phases/122-persist-ncia-por-empresa-e-comandos-v21-0/122-VERIFICATION.md
  </read_first>

  <action>
Rodar, nesta ordem, capturando a saída de cada um em arquivo (nunca conferir por pipe: `comando | tail -20; echo $?` devolve o exit code do `tail`, não do comando — armadilha de shell que já enganou nesta casa, learnings §4):

1. `php artisan test --filter=Phase123` — precisa estar 100% verde. É a suíte nova da fase inteira: `CompanyScoreSnapshotReaderTest`, `PerformanceShowMesesDisponiveisTest`, `PerformanceShowEmpresasScoreTest`, `PerformanceShowSemDetalheTest`, `RetrocompatSnapshotAntigoTest`, `RelatorioBonificacaoEmpresasTest`, `AuditoriaBonusNotaEmpresaTest`.
2. `php artisan test --filter=Phase122` — a fonte de dados que esta fase apenas lê continua estável.
3. `php artisan test --filter=Phase120` — o teste dourado `PayloadBaselineFlagOffTest` continua verde; a baseline registrada é 18 passed.
4. `npm run test:js` — inclui `desempenhoLabels.test.js` e os três testes estruturais de JSX.
5. `php artisan test --filter=Phase110` — **baseline conhecida: 2 failed / 3 passed**, documentada em `122-VERIFICATION.md`. Não é regressão desta fase.
6. `php artisan test --filter=Desempenho` — **baseline conhecida: 14 failed / 101 passed**, documentada em `122-VERIFICATION.md`. Não é regressão desta fase.
7. `php artisan test` (suíte completa) — registrar o total. Qualquer falha precisa ser classificada como baseline preexistente (itens 5 e 6) ou como regressão nova. Regressão nova bloqueia o checkpoint.
8. `npm run build` — obrigatório pela convenção do projeto ao final de qualquer edição de frontend.

Confirmar também, por `grep`, os três invariantes de escopo desta fase:
- `git diff --stat app/Services/DesempenhoScoreService.php app/Services/Desempenho/CompanyScoreService.php` sai vazio — as réguas e a agregação ficaram intocadas; `computeVarMargem()` continua em `avg()` e `computeVarFaturamento()` continua em `median()`, que divergem de propósito.
- `git diff --stat config/metrics.php` sai vazio e `performance_company_first_score` continua `false`.
- `git diff resources/views/pdf/relatorio-bonificacao.blade.php` sai vazio — o PDF continua resumo (D-09).

Criar `.planning/phases/123-telas-e-relat-rios-v21-0/123-CHECKPOINT-VISUAL.md` com a tabela de resultados dos 8 comandos (comando, exit code, resultado observado, veredito) e a seção de invariantes de escopo. Deixar preparada a segunda parte do documento — o roteiro visual da task 2 — com os itens em branco para preenchimento durante o checkpoint. Documento em pt-BR.
  </action>

  <verify>
    <automated>php artisan test --filter=Phase123 &amp;&amp; npm run test:js &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
- `php artisan test --filter=Phase123` sai com código 0
- `php artisan test --filter=Phase120` sai com código 0 (18 testes)
- `npm run test:js` sai com código 0
- `npm run build` sai com código 0
- `php artisan test --filter=Phase110` no mesmo 2 failed / 3 passed da baseline de `122-VERIFICATION.md`
- `php artisan test --filter=Desempenho` no mesmo 14 failed / 101 passed da baseline de `122-VERIFICATION.md`
- `git diff --stat app/Services/DesempenhoScoreService.php app/Services/Desempenho/CompanyScoreService.php config/metrics.php resources/views/pdf/relatorio-bonificacao.blade.php` não lista nenhum arquivo
- `123-CHECKPOINT-VISUAL.md` existe com os 8 comandos, seus exit codes e o veredito de cada um
  </acceptance_criteria>

  <done>
A suíte completa está classificada contra a baseline documentada, o build de produção passa, e está provado por diff que nenhuma régua, nenhuma agregação, nenhuma flag e nenhum template de PDF foi tocado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual em 2026-06 com os casos-limite reais</name>

  <files>
.planning/phases/123-telas-e-relat-rios-v21-0/123-CHECKPOINT-VISUAL.md
  </files>

  <read_first>
.planning/phases/123-telas-e-relat-rios-v21-0/123-VALIDATION.md
.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
  </read_first>

  <what-built>
Três telas passaram a explicar a régua e a mostrar a nota de cada empresa, lendo o detalhe já persistido pela Fase 122:

- `/performance/{id}` — card de margem reescrito sem jargão de campo de API, e uma seção nova "Empresas da carteira" em competência fechada, dividida em "entraram na conta (N)" / "não entraram (M)", com a margem no formato `14,1% → 12,0%  −2,1` e os três componentes por empresa.
- `/desempenho/relatorio-bonificacao` — tabela continua uma linha por profissional, com as empresas a um clique na linha expansível. O PDF **não** mudou.
- `/desempenho/auditoria-bonus` — cada empresa já listada ganhou a nota que teve na competência.

Além disso, o bug bloqueante foi corrigido: o dropdown "Mês fechado" de `/performance/{id}` estava **vazio em produção** por causa de um filtro fixo `>= 2026-08-01`. Agora as competências são derivadas dos dados — 2026-06 é selecionável.
  </what-built>

  <how-to-verify>
Rodar a aplicação localmente (`php artisan serve` + assets já buildados pela Task 1) e conferir os itens abaixo **na competência 2026-06**, que é a única com detalhe por empresa. Anotar o resultado observado de cada item em `123-CHECKPOINT-VISUAL.md`.

**A. Desbloqueio do seletor (D-02)**
1. Abrir `/performance/{id}` de qualquer profissional. O botão "Mês fechado" deve estar **habilitado** (antes desta fase estava desabilitado com o tooltip "Nenhum mês fechado disponível ainda").
2. Clicar nele e conferir que o `<select>` oferece **junho de 2026**.

**B. Card de margem (UIEM-01)**
3. No card de margem, o título deve ser "Margem" e o texto abaixo **não pode** citar `percentageMargin` nem nenhum outro nome de campo de API. Deve explicar que é variação relativa, com o exemplo "de 10% para 11% aparece como +10%".
4. Quando a competência tiver `var_margem_pp`, deve aparecer também a frase em linguagem simples do tipo "A margem subiu 3,2 pontos percentuais na média da carteira."
5. O número grande do card deve continuar sendo a variação relativa — é ele que produz a nota exibida ao lado, e nenhum número em destaque pode contradizer a nota.

**C. Felipe — o denominador (UIEM-02 / D-06)**
6. Abrir `/performance/{id}` do **Felipe** em 2026-06. A seção "Entraram na conta (N)" deve mostrar um N **pequeno** — não 30. A seção "Não entraram (M)" deve listar as ~27 restantes, **com o motivo visível em cada linha** (frases em português, nunca `snake_case`).
7. Conferir que a segunda seção **nasce colapsada** (são mais de 8 empresas) e que o botão diz quantas são.
8. Conferir que os dois números somados batem com o total de empresas da carteira dele.

**D. Matheus Estrela — Shopee (UIEM-02 / D-07)**
9. Abrir `/performance/{id}` do **Matheus Estrela** em 2026-06. **Todas** as empresas dele devem aparecer em "Entraram na conta" — nunca excluídas do denominador.
10. Como a carteira inteira é Shopee, deve aparecer **um aviso único** no topo da seção explicando que a Shopee não fornece margem e que todas entram com 1,00 ponto fixo — e **não** um selo repetido em cada uma das linhas. Confirmar que a leitura resultante é "falta o dado", nunca "a margem foi ruim".

**E. Caso comum (UIEM-02)**
11. Abrir um profissional com maioria de empresas completas (ex.: Ana Julia ou Luiz Henrique, da tabela do rollout da Fase 122). A tabela deve renderizar limpa, com a margem legível como `14,1% → 12,0%  −2,1` e os três componentes visíveis por empresa.
12. Conferir a ressalva de que a nota do topo vem do cálculo por carteira — ela precisa estar visível, porque com a flag desligada a nota do topo **não** é a média das notas listadas abaixo.

**F. Ausência não é silêncio (UIEM-03 / D-03)**
13. Trocar para o modo "Em curso": a seção deve aparecer com o aviso de que a lista só existe depois do fechamento — não sumir.
14. Selecionar uma competência fechada sem detalhe (ex.: 2026-05, se aparecer no seletor): deve aparecer o aviso "Detalhe por empresa indisponível" com a explicação — nunca uma tabela vazia nem erro.
15. Conferir que o card de margem no modo "Em curso" continua mostrando o número normalmente.

**G. Débora Lima — ausência não pode quebrar (UIEM-03)**
16. Conferir que **Débora Lima** não aparece no ranking, no Relatório de Bonificação nem na Auditoria de Bônus — ela cai em "sem carteira" na consolidação, que é problema de OUTRA fase. Não aparecer **não** é bug desta fase.
17. Conferir que a ausência dela **não** gera erro 500 em nenhuma das três telas.

**H. Relatório de Bonificação (UIEM-04 / D-08 / D-09)**
18. Abrir `/desempenho/relatorio-bonificacao?mes=2026-06`. A tabela deve continuar com **uma linha por profissional**.
19. Expandir a linha de ao menos um contemplado (ex.: Rubens, único com faixa `intermediario` em 2026-06 pela tabela do rollout) e conferir empresas + nota de cada uma + os três componentes.
20. Clicar em "Exportar PDF" e confirmar que o PDF continua **resumo**, uma linha por profissional, sem a lista por empresa.

**I. Auditoria de Bônus (UIEM-04 / D-10)**
21. Abrir `/desempenho/auditoria-bonus?mes=2026-06`, expandir um profissional e conferir a nota por empresa em cada linha.
22. Trocar para uma competência sem detalhe e conferir que aparece o banner no topo da página, e não colunas vazias sem explicação.
23. Conferir que os números batem com o que a tela individual do mesmo profissional mostra na mesma competência — é literalmente a mesma fonte.

**Deploy:** não executar. A autorização de deploy é uma decisão separada do usuário.
  </how-to-verify>

  <acceptance_criteria>
- Os 23 itens têm resultado anotado em `123-CHECKPOINT-VISUAL.md` (aprovado / ajuste pedido, com o que foi observado)
- Os itens 6, 9 e 16-17 (Felipe, Matheus Estrela, Débora Lima) estão explicitamente respondidos — são os casos-limite reais desta fase
- O item 20 confirma que o PDF continua resumo (D-09)
- O item 23 confirma que Auditoria e tela individual mostram o mesmo número para o mesmo profissional/competência (UIEM-04)
- O usuário deu aprovação explícita, ou registrou os ajustes pedidos
  </acceptance_criteria>

  <resume-signal>Responder "aprovado" para fechar a fase, ou descrever os ajustes necessários item por item.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dado de produção → tela local | O checkpoint é feito contra dado real de 2026-06; nenhuma escrita é feita, só leitura |
| repositório → VPS | Fora de escopo desta fase — deploy requer autorização explícita do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-11 | Tampering | Reconsolidação de competência fechada para "ter mais dado no checkpoint" | mitigate | Explicitamente proibido: o checkpoint usa 2026-06 e só. Recompute de mês fechado pode mexer em pagamento de quem está perto de uma fronteira de régua (learnings §2, caso Danilo). `desempenho:consolidar-mes` não pode ser rodado nesta fase |
| T-123-12 | Elevation of Privilege | Deploy não autorizado durante o checkpoint | mitigate | `deploy.sh` publica exatamente `origin/main` — quem deploya publica o trabalho de todos que já estiverem lá (learnings §10). Nenhum comando de deploy é executado neste plano; a autorização é decisão separada do usuário |
| T-123-13 | Repudiation | Fechar a fase sem evidência do que foi conferido | mitigate | `123-CHECKPOINT-VISUAL.md` registra os 8 comandos com exit code e os 23 itens visuais com resultado observado |
| T-123-SC | Tampering | npm/composer installs | accept | Nenhum pacote novo em toda a fase |
</threat_model>

<verification>
- `php artisan test --filter=Phase123` verde
- `php artisan test --filter=Phase120` verde
- `php artisan test --filter=Phase122` sem regressão
- `php artisan test --filter=Phase110` e `--filter=Desempenho` idênticos à baseline de `122-VERIFICATION.md`
- `npm run test:js` verde
- `npm run build` sem erro
- `123-CHECKPOINT-VISUAL.md` completo e aprovado pelo usuário
</verification>

<success_criteria>
- Critério 5 do ROADMAP satisfeito: `npm run build` rodado e checkpoint visual aprovado
- Os três casos-limite reais foram conferidos com dado de produção de 2026-06
- Nenhuma regressão nova contra a baseline documentada
- Réguas, agregações, flag e template de PDF provadamente intocados
- Nenhum deploy executado
</success_criteria>

<output>
Criar `.planning/phases/123-telas-e-relat-rios-v21-0/123-06-SUMMARY.md` ao terminar.
</output>
