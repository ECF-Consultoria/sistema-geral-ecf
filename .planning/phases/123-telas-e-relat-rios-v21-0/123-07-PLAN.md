---
phase: 123-telas-e-relat-rios-v21-0
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/PerformanceController.php
  - tests/Feature/Phase123/PerformanceAutorizacaoTest.php
  - app/Services/Desempenho/CompanyScoreSnapshotReader.php
  - tests/Feature/Phase123/CompanyScoreSnapshotReaderTest.php
autonomous: true
gap_closure: true
requirements: [UIEM-02, UIEM-03]

must_haves:
  truths:
    - "Não-admin com core.performance vê a própria página de desempenho (200) e o próprio JSON de evolução (200)"
    - "Não-admin com core.performance NÃO consegue ver a página nem o JSON de evolução de outro profissional trocando o {user} na URL (403)"
    - "Líder do setor Performance continua vendo o desempenho de quem está na própria equipe (200) — visão consolidada preservada, D-232/240 de app/Models/User.php não quebra"
    - "Líder de um setor NÃO consegue ver desempenho de quem não está sob sua liderança (403)"
    - "?mes= fora do intervalo 01-12 nunca produz 500 em /performance nem em /performance/{user} — cai no mês corrente por fallback"
    - "O payload de empresas_score não carrega mais faturamento_atual, faturamento_anterior nem componentes_presentes — chaves sem consumidor JSX"
  artifacts:
    - path: "app/Http/Controllers/PerformanceController.php"
      provides: "autorizadoParaVerDesempenhoDe() reusada em show()/evolucao(); resolveContextoPeriodo() com regex de mês corrigida"
    - path: "tests/Feature/Phase123/PerformanceAutorizacaoTest.php"
      provides: "prova dos dois lados (dono/outro) em show() e evolucao(), e do caso líder-vê-equipe/líder-não-vê-fora-da-equipe"
    - path: "app/Services/Desempenho/CompanyScoreSnapshotReader.php"
      provides: "mapear() com shape de 14 chaves, sem faturamento absoluto nem contagem interna"
  key_links:
    - from: "PerformanceController::show()"
      to: "autorizadoParaVerDesempenhoDe()"
      via: "abort_unless no topo do método"
      pattern: "abort_unless\\(\\s*\\$this->autorizadoParaVerDesempenhoDe"
    - from: "PerformanceController::evolucao()"
      to: "autorizadoParaVerDesempenhoDe()"
      via: "abort_unless no topo do método"
      pattern: "abort_unless\\(\\s*\\$this->autorizadoParaVerDesempenhoDe"
    - from: "autorizadoParaVerDesempenhoDe()"
      to: "user_setores"
      via: "mesma regra de PortfolioController::transparencia() (líder + membro do setor liderado)"
      pattern: "setoresLiderados\\(\\)->pluck\\('setores.id'\\)"
---

<objective>
Fechar os dois achados Critical/Warning do `123-REVIEW.md` e `123-VERIFICATION.md` que dizem respeito ao endpoint `/performance/{user}` (e seu par JSON `/api/performance/{user}/evolucao`): CR-01 (ausência de checagem de autorização por usuário-alvo), o complemento WR-03 (payload carrega faturamento absoluto sem consumidor) e WR-01 (regex de `?mes=` aceita mês inválido e produz 500).

Purpose: a Fase 123 acrescentou a este endpoint nome de empresa cliente, faturamento em R$, margem e nota por empresa — dado financeiro de terceiro e de compensação. Sem checagem de autorização, qualquer não-admin com a permission `core.performance` (que NÃO é admin-only — líder do setor Performance a recebe via `Permissions::AUTO_LIDERANCA_PERFORMANCE`, e a permission pode ser concedida a um setor inteiro via `setor_permissoes`) pode trocar o `{user}` na URL e ler o desempenho financeiro de qualquer outro profissional.

Output: `PerformanceController::show()` e `::evolucao()` passam a exigir admin, o próprio usuário, ou líder de um setor do qual o usuário-alvo é membro — mesma regra já em produção em `PortfolioController::transparencia()` para o mesmo tipo de dado. `CompanyScoreSnapshotReader::mapear()` para de expor 3 campos sem consumidor. `?mes=` inválido nunca mais derruba a rota.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-VERIFICATION.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-REVIEW.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
@.planning/learnings/desempenho-bonificacao.md
@app/Http/Controllers/PerformanceController.php
@app/Http/Controllers/PortfolioController.php
@app/Services/Desempenho/CompanyScoreSnapshotReader.php
@app/Models/User.php
@app/Support/Permissions.php
@tests/Feature/Phase123/Phase123TestCase.php
@tests/Feature/Phase123/CompanyScoreSnapshotReaderTest.php
</context>

<interfaces>
<!-- Regra de autorização JÁ APROVADA e em produção, para replicar literalmente
     — não inventar critério novo. app/Http/Controllers/PortfolioController.php
     linhas 214-226 (transparencia()): -->

```php
public function transparencia(Request $request, User $user): \Inertia\Response
{
    $atual = $request->user();
    $autorizado = $atual->isAdmin()
        || $atual->id === $user->id
        || (
            $atual->isLider()
            && DB::table('user_setores as us')
                ->whereIn('us.setor_id', $atual->setoresLiderados()->pluck('setores.id'))
                ->where('us.user_id', $user->id)
                ->exists()
        );
    abort_unless($autorizado, 403);
    // ...
}
```

<!-- Por que esta regra e não "admin || dono" cru: Portfolio/Transparencia.jsx
     (linha 51) já navega de volta para route('performance.show', profissional.id)
     a partir da tela de transparência de um líder — se show() usar uma regra
     mais estreita que transparencia(), o botão "Desempenho" quebra para
     qualquer líder que hoje acessa a transparência de um membro da equipe.
     E app/Models/User.php linhas 232-240 documenta que o líder do setor
     Performance ganha core.performance via AUTO_LIDERANCA_PERFORMANCE
     precisamente para "visão consolidada da equipe" — abort_unless(admin||dono)
     quebraria esse caso de uso legítimo. -->

From app/Models/User.php (métodos usados pela regra, já existentes):
```php
public function isAdmin(): bool { return $this->role === 'admin'; }
public function isLider(): bool { return $this->setoresLiderados()->exists(); }
public function setoresLiderados(): BelongsToMany; // pivot setor_lideres, líder não precisa ser membro
```

From app/Http/Controllers/PerformanceController.php (assinaturas a alterar):
```php
public function show(Request $request, User $user): \Inertia\Response   // linha 1268
public function evolucao(Request $request, User $user): JsonResponse    // linha 962
private function resolveContextoPeriodo(Request $request): array       // linha 1221, regex na linha 1234
```

From app/Http/Controllers/PerformanceController.php::indexPolos() (linha 994,
padrão de regex correto já usado no MESMO arquivo — só não foi reaproveitado
em resolveContextoPeriodo()):
```php
if (!preg_match('/^\d{4}-(0[1-9]|1[0-2])$/', $mesRef)) {
    $mesRef = now()->format('Y-m');
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Autorização por usuário-alvo em show()/evolucao() (CR-01) + regex de mês correta (WR-01)</name>
  <files>app/Http/Controllers/PerformanceController.php, tests/Feature/Phase123/PerformanceAutorizacaoTest.php</files>
  <behavior>
    - Não-admin com `core.performance` (via `setor_permissoes`, sem ser líder) faz GET em `route('performance.show', $ele_mesmo)` → 200.
    - O mesmo user faz GET em `route('performance.show', $outro)` (outro profissional, nenhuma relação de liderança) → 403.
    - Os dois cenários acima repetidos para `route('performance.evolucao', ...)` (GET JSON) → 200 / 403.
    - Líder de um setor com slug EXATO `'performance'` (para exercitar `Permissions::AUTO_LIDERANCA_PERFORMANCE` de verdade, não uma permission manual) faz GET em `route('performance.show', $membro_da_equipe)` (membro via `user_setores` no mesmo setor) → 200. Sanity check prévio no teste: `$lider->fresh()->hasPermission('core.performance')` é `true` antes de exercitar a rota.
    - O mesmo líder faz GET em `route('performance.show', $fora_da_equipe)` (profissional que não pertence a nenhum setor liderado por ele) → 403.
    - Admin autenticado faz GET em `/performance?mes=2026-13` e em `route('performance.show', $qualquer)` . `'?mes=9999-99'` → ambos 200 (nunca 500), caindo no mês corrente por fallback (mesmo comportamento de `indexPolos()` para mês inválido).
  </behavior>
  <action>
    Investigar e decidir a regra correta ANTES de codificar (não presumir `admin || dono`): `PortfolioController::transparencia()` (linhas 214-226) já implementa, em produção, exatamente o mesmo tipo de checagem para o mesmo tipo de dado (carteira/financeiro de terceiro) — admin, OU o próprio usuário, OU líder de um setor do qual o usuário-alvo é MEMBRO via `user_setores`. Replicar essa regra literalmente, não inventar uma nova: um `abort_unless(admin || dono)` cru quebraria o caso legítimo do líder do setor Performance (`Permissions::AUTO_LIDERANCA_PERFORMANCE`, concedida precisamente para "visão consolidada da equipe" — ver `app/Models/User.php:232-240`) e o botão "Desempenho" de `Portfolio/Transparencia.jsx:51`, que hoje qualquer líder acessando a transparência de um membro da equipe usa para voltar a `performance.show` daquele mesmo membro.

    Extrair a checagem para um método privado novo `autorizadoParaVerDesempenhoDe(Request $request, User $user): bool` em `PerformanceController` — evita duplicar a query em `show()` e `evolucao()`. Corpo idêntico ao de `transparencia()`, devolvendo `bool` em vez de chamar `abort_unless` diretamente (quem chama `abort_unless` é cada método público).

    Chamar `abort_unless($this->autorizadoParaVerDesempenhoDe($request, $user), 403, 'Você só pode ver o seu próprio desempenho ou o de quem está sob sua liderança.')` como a PRIMEIRA linha de `show()` (linha 1268) e de `evolucao()` (linha 962), antes de qualquer leitura de dado — a mesma disciplina de `PortfolioController::transparencia()`.

    WR-01, mesmo arquivo: trocar a regex de `resolveContextoPeriodo()` (linha 1234, hoje `/^\d{4}-\d{2}$/`) para `/^\d{4}-(0[1-9]|1[0-2])$/` — a mesma forma que `indexPolos()` (linha 994) já usa, com o mesmo comentário sobre o overflow silencioso do Carbon (`2026-13` viraria `2027-01`; `9999-99` viraria um ano de 5 dígitos que quebra `MetricPeriodResolver::resolve()`). `resolveContextoPeriodo()` é a fonte única de período tanto de `index()` (linha 111, rota `/performance`) quanto de `show()` (linha 1273, rota `/performance/{user}`) — uma única correção cobre as duas rotas citadas no WR-01.

    Escrever `tests/Feature/Phase123/PerformanceAutorizacaoTest.php` (extends `Phase123TestCase`, reusa `criarUserElegivel()`/`darCarteira()`) cobrindo o `<behavior>` acima. Para o cenário de líder, criar um setor NOVO com slug EXATAMENTE `'performance'` (não reusar `$this->setorId`, que é `'performance-123'` — `Permissions::AUTO_LIDERANCA_PERFORMANCE` em `app/Models/User.php:236` checa o slug literal `'performance'`), inserir o líder via `DB::table('setor_lideres')->insert([...])` (sem `assigned_by`, mesmo padrão de `tests/Feature/Phase72/DashboardPendencyPropsTest.php:157-163`) e o membro da equipe via `DB::table('user_setores')->insert([...])`. Para o cenário "não-admin com permission mas sem liderança", conceder `core.performance` via `DB::table('setor_permissoes')->insert(['setor_id' => ..., 'permission_key' => 'core.performance', ...])` no setor de `criarUserElegivel()` (mesmo padrão de `tests/Feature/PublicacaoDesempenhoRouteTest.php:81-86`).
  </action>
  <verify>
    <automated>php artisan test --filter=PerformanceAutorizacaoTest</automated>
  </verify>
  <done>`show()` e `evolucao()` rejeitam com 403 qualquer não-admin/não-dono/não-líder-da-equipe-do-alvo; líder do setor Performance continua enxergando quem está sob sua liderança; `?mes=` fora de 01-12 nunca produz 500 em nenhuma das duas rotas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Payload enxuto do CompanyScoreSnapshotReader (WR-03)</name>
  <files>app/Services/Desempenho/CompanyScoreSnapshotReader.php, tests/Feature/Phase123/CompanyScoreSnapshotReaderTest.php</files>
  <behavior>
    - `CompanyScoreSnapshotReader::paraUsuario()`/`paraUsuarios()` devolvem linhas com EXATAMENTE 14 chaves (hoje 17).
    - `faturamento_atual`, `faturamento_anterior` e `componentes_presentes` NUNCA aparecem no array devolvido (`assertArrayNotHasKey` para as 3, ao lado das 6 chaves internas já proibidas — `origem`, `gerado_em`, `created_at`, `updated_at`, `id`, `user_id`).
    - As 14 chaves restantes (`company_id`, `company_name`, `fonte_financeira`, `status`, `nps_pontos`, `faturamento_var_pct`, `faturamento_pontos`, `margem_pct_atual`, `margem_pct_anterior`, `margem_var_pp`, `margem_pontos`, `nota_empresa`, `nota_empresa_parcial`, `quality`) continuam presentes, na mesma ordem.
  </behavior>
  <action>
    Antes de editar, reconfirmar por grep que nenhum consumidor JSX lê as 3 chaves (`grep -rn "faturamento_atual\|faturamento_anterior\|componentes_presentes" resources/js` — esperado zero ocorrências; já confirmado no planejamento, mas o código pode ter mudado entre o planejamento e a execução).

    Remover `faturamento_atual`, `faturamento_anterior` e `componentes_presentes` do array devolvido por `CompanyScoreSnapshotReader::mapear()` (linhas 143-144 e 151). NÃO tocar no model `DesempenhoCompanyScoreSnapshot`, na migration `2026_08_03_120000_create_desempenho_company_score_snapshots_table.php`, nem em `CompanyScoreSnapshotWriter` — esses 3 campos continuam existindo na tabela e sendo gravados no fechamento; só param de trafegar para o browser através deste reader especificamente. Atualizar o docblock de `mapear()` de "exatamente 17 chaves" para "exatamente 14 chaves".

    Atualizar `tests/Feature/Phase123/CompanyScoreSnapshotReaderTest.php::shape_tem_exatamente_as_17_chaves_esperadas_sem_campos_internos` — renomear o método para `shape_tem_exatamente_as_14_chaves_esperadas_sem_campos_internos`, remover as 3 chaves de `$esperado`, e acrescentar as 3 removidas ao loop de `assertArrayNotHasKey` que já verifica ausência de chaves internas (o teste passa a provar a AUSÊNCIA, não só a presença de um shape antigo).
  </action>
  <verify>
    <automated>php artisan test --filter=CompanyScoreSnapshotReaderTest</automated>
  </verify>
  <done>O reader devolve shape de 14 chaves sem faturamento absoluto em R$ nem contagem interna; nenhum consumidor quebra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Não-admin autenticado → `PerformanceController::show()`/`::evolucao()` | usuário troca `{user}` na URL/rota para tentar ler dado financeiro/de compensação de outro profissional |
| Query string `?mes=` → `resolveContextoPeriodo()` | input não confiável vira `Carbon::createFromFormat()` sem guarda de intervalo válido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-123-10 | Information Disclosure | `PerformanceController::show()`/`::evolucao()` | mitigate | `abort_unless($this->autorizadoParaVerDesempenhoDe(...))` — admin \|\| dono \|\| líder-da-equipe-do-alvo, mesma regra já em produção em `PortfolioController::transparencia()` |
| T-123-11 | Information Disclosure | `CompanyScoreSnapshotReader::mapear()` | mitigate | remove `faturamento_atual`/`faturamento_anterior`/`componentes_presentes` do shape (WR-03) — reduz dado financeiro de terceiro que trafega para o browser sem consumidor |
| T-123-12 | Tampering (input malformado) | `resolveContextoPeriodo()` | mitigate | regex `\d{4}-(0[1-9]\|1[0-2])$` evita overflow silencioso do `Carbon` que hoje produz 500 (WR-01) |
| T-123-SC | Tampering | npm/composer installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
```bash
php artisan test --filter=PerformanceAutorizacaoTest
php artisan test --filter=CompanyScoreSnapshotReaderTest
php artisan test --filter=Phase123
php artisan test --filter=Phase120
php artisan test --filter=Desempenho
```
Baseline conhecida: `--filter=Desempenho` 14 failed/101 passed (pré-existente, não é regressão). `--filter=Phase123` deve continuar 41/41 (ou 41 + os novos testes deste plano, todos verdes) — nenhuma asserção existente deve precisar mudar além da renomeação do teste de shape na Task 2.
</verification>

<success_criteria>
- `PerformanceAutorizacaoTest` prova os dois lados (dono/outro) em `show()` e `evolucao()`, e o caso líder-vê-equipe / líder-não-vê-fora-da-equipe, todos verdes.
- `CompanyScoreSnapshotReaderTest` prova shape de 14 chaves, sem as 3 removidas.
- `?mes=2026-13` e `?mes=9999-99` devolvem 200 (nunca 500) em `/performance` e `/performance/{user}`.
- `--filter=Phase123` e `--filter=Phase120` continuam verdes; `--filter=Desempenho` na baseline conhecida (14 failed/101 passed), sem regressão nova.
</success_criteria>

<output>
Create `.planning/phases/123-telas-e-relat-rios-v21-0/123-07-SUMMARY.md` when done
</output>
