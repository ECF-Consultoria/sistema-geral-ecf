---
phase: 123-telas-e-relat-rios-v21-0
plan: 04
type: execute
wave: 3
depends_on: ["123-01", "123-02"]
files_modified:
  - resources/js/Pages/Performance/Show.jsx
  - resources/js/Components/Desempenho/EmpresasScoreTabela.jsx
  - tests/Feature/Phase123/RetrocompatSnapshotAntigoTest.php
  - tests/js/estrutura-performance-show.test.js
autonomous: true
requirements: [UIEM-01, UIEM-02, UIEM-03]

must_haves:
  truths:
    - "O card de margem explica a variação em linguagem simples e não cita nenhum nome de campo de API"
    - "D-04: o card agregado do topo continua exibindo o número legado (var_margem_pct) enquanto a flag estiver desligada — pontos percentuais aparecem só na lista por empresa, e nenhum número em destaque contradiz a nota ao lado dele"
    - "O detalhe do profissional lista as empresas da carteira com a nota de cada uma e seus três componentes"
    - "É impossível ler a nota sem ver sobre quantas empresas ela foi feita"
    - "D-05: a margem por empresa é apresentada como antes → depois + variação (14,1% → 12,0%  −2,1), usando margem_pct_anterior e margem_pct_atual — o ponto percentual fica auto-evidente pela leitura, sem depender do termo"
    - "Empresa Shopee aparece dentro da conta com ressalva de valor provisório, nunca como nota baixa sem explicação"
    - "Snapshot antigo sem empresas_score renderiza no visual anterior, com aviso, sem erro de render"
  artifacts:
    - path: "resources/js/Components/Desempenho/EmpresasScoreTabela.jsx"
      provides: "Tabela de duas seções com denominador explícito, formato antes→depois e selo Shopee"
      min_lines: 120
    - path: "resources/js/Pages/Performance/Show.jsx"
      provides: "Card de margem sem jargão + seção 'Empresas da carteira' + aviso de ausência"
    - path: "tests/Feature/Phase123/RetrocompatSnapshotAntigoTest.php"
      provides: "Prova de UIEM-03/D-11 — payload legado não quebra e não ganha chave inventada"
  key_links:
    - from: "resources/js/Pages/Performance/Show.jsx"
      to: "resources/js/Components/Desempenho/EmpresasScoreTabela.jsx"
      via: "import + render abaixo do bloco 'Info carteira'"
      pattern: "EmpresasScoreTabela"
    - from: "resources/js/Components/Desempenho/EmpresasScoreTabela.jsx"
      to: "resources/js/lib/desempenhoLabels.js"
      via: "import dos formatadores e textos"
      pattern: "@/lib/desempenhoLabels"
    - from: "resources/js/Pages/Performance/Show.jsx"
      to: "prop empresas_score"
      via: "desestruturação com default []"
      pattern: "empresas_score"
---

<objective>
Entregar a tela que é o coração da fase: `/performance/{id}` passa a explicar a margem sem jargão (UIEM-01) e a mostrar, em competência fechada, a nota de cada empresa da carteira com seus três componentes (UIEM-02) — dividida em "entraram na conta (N)" e "não entraram (M)", de modo que seja impossível ler a nota sem ver o denominador.

Purpose: o número que hoje aparece no card de margem cita `percentageMargin`, que é nome de campo de API. E a nota do profissional aparece sem nenhuma pista de sobre quantas empresas ela foi feita — Felipe tem margem sobre 3 de 30 empresas e nada na tela diz isso.
Output: um componente de tabela reutilizável (o Relatório de Bonificação vai reusá-lo no Plano 05), a seção nova em `Show.jsx`, o card de margem reescrito e a suíte de retrocompatibilidade.

**Esta fase não calcula nada.** O front só formata o que veio pronto nas props `empresas_score`, `empresas_score_resumo` e `tem_detalhe_empresas` (Plano 02). Nada é recontado no JSX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
@.planning/phases/123-telas-e-relat-rios-v21-0/123-RESEARCH.md
@.planning/learnings/desempenho-bonificacao.md
@resources/js/Pages/Performance/Show.jsx
@resources/js/lib/desempenhoLabels.js
</context>

<interfaces>
<!-- Contratos prontos dos Planos 01 e 02. Não explorar o codebase para achá-los. -->

Props que `Performance/Show.jsx` passa a receber (Plano 02):
- `empresas_score`: array de objetos com 17 chaves — `company_id`, `company_name`,
  `fonte_financeira`, `status`, `nps_pontos`, `faturamento_atual`, `faturamento_anterior`,
  `faturamento_var_pct`, `faturamento_pontos`, `margem_pct_atual`, `margem_pct_anterior`,
  `margem_var_pp`, `margem_pontos`, `componentes_presentes`, `nota_empresa`,
  `nota_empresa_parcial`, `quality`. Já vem ordenado: nota desc, `null` por último,
  desempate por nome.
- `empresas_score_resumo`: `{ entraram: number, nao_entraram: number }`.
- `tem_detalhe_empresas`: boolean.
`quality` sempre é objeto com `revenue_diff_source`, `margin_diff_source`, `margin_source`
e `motivos` (array de 0 a N slugs). `status` é string, nunca enum.

`resources/js/lib/desempenhoLabels.js` (Plano 01) exporta:
`MOTIVO_LABEL`, `motivoLabel`, `STATUS_EMPRESA_LABEL`, `statusEmpresaLabel`, `fmtPctAbs`,
`fmtPp`, `fmtVarPct`, `fmtNotaEmpresa`, `fmtMargemAntesDepois`, `fraseVarMargemPp`,
`ehPlaceholderShopee`, `MARGEM_CARD_TITULO`, `MARGEM_CARD_SUBLABEL`, `SELO_SHOPEE_TEXTO`,
`SELO_SHOPEE_TITULO`, `AVISO_CARTEIRA_SO_SHOPEE`, `MARGEM_SEM_DADO_TEXTO`,
`AVISO_SEM_DETALHE_TITULO`, `AVISO_SEM_DETALHE_EM_CURSO`, `avisoSemDetalheFechado`,
`tituloEntraram`, `tituloNaoEntraram`, `SECAO_ENTRARAM_SUBTITULO`,
`SECAO_NAO_ENTRARAM_SUBTITULO`, `NOTA_TOPO_RESSALVA`, `LIMIAR_COLAPSO_NAO_ENTRARAM`.

Estado ATUAL de `resources/js/Pages/Performance/Show.jsx` (562 linhas, já lido):
- Helpers de módulo `formatContaNota`, `mesExtenso`, `formatPercent`, `fmtDia`.
- Componentes locais `ParametroCard` (props `icone`, `titulo`, `valor`, `sublabel`,
  `accentColor`, `emBreve`, `trendDir`) e `FaixaBonusCard`.
- Card de margem nas linhas 499-506: `titulo="Variação da margem %"`,
  `valor={formatPercent(c.var_margem_pct)}`,
  `sublabel="Variação da margem % (percentageMargin) vs mês anterior · fonte Adman"`.
- Bloco "Info carteira" nas linhas 521-539, dentro do ramo `!semCarteira`.
- Bloco "Como interpretar" nas linhas 543-557.
- Tokens em uso: `bg-ecf-card`, `border-white/[0.08]`, `bg-white/[0.02]`,
  `text-[10px] uppercase tracking-wider`, `tabular-nums`, `cn()` de `@/lib/utils`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Card de margem sem jargão (UIEM-01) + retrocompatibilidade do payload (UIEM-03/D-11)</name>

  <files>
resources/js/Pages/Performance/Show.jsx
tests/Feature/Phase123/RetrocompatSnapshotAntigoTest.php
  </files>

  <read_first>
resources/js/Pages/Performance/Show.jsx
resources/js/lib/desempenhoLabels.js
tests/Feature/Phase123/Phase123TestCase.php
.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
  </read_first>

  <action>
Em `resources/js/Pages/Performance/Show.jsx`, importar de `@/lib/desempenhoLabels` os símbolos `MARGEM_CARD_TITULO`, `MARGEM_CARD_SUBLABEL` e `fraseVarMargemPp`.

**1. Card de margem (linhas 499-506).** Trocar `titulo="Variação da margem %"` por `titulo={MARGEM_CARD_TITULO}` (valor: `'Margem'`) e o `sublabel` atual — a string que contém `percentageMargin`, que é nome de campo de API e é exatamente o jargão que UIEM-01 manda eliminar — por uma composição de duas frases:
- sempre: `MARGEM_CARD_SUBLABEL`, que é `'Variação relativa da margem contra a janela anterior, na média das empresas da carteira. Ex.: de 10% para 11% aparece como +10%.'`
- e, **somente quando** `c.var_margem_pp` não for nulo nem indefinido, concatenar (com um espaço) o retorno de `fraseVarMargemPp(c.var_margem_pp)`, que produz a frase de UIEM-01 em linguagem simples: `'A margem subiu 3,2 pontos percentuais na média da carteira.'` ou `'A margem caiu 1,0 ponto percentual na média da carteira.'`.

O `valor` do card **continua** `formatPercent(c.var_margem_pct)` — não trocar. É a D-04: enquanto `metrics.performance_company_first_score` estiver `false`, quem produz a `nota_final` exibida ao lado é o número relativo, e nenhum número em destaque pode contradizer a nota. Os pontos percentuais entram como explicação no sublabel e como unidade real na lista por empresa, nunca como o número grande do card. O `trendDir` também continua governado por `c.var_margem_pct`.

Isso já satisfaz o "rótulo legado" da D-11 sem branch adicional: quando `var_margem_pp` está ausente ou nulo (payload de snapshot antigo, ou modo "Em curso" com `computeCached()` sem shadow), o card exibe `var_margem_pct` com a explicação legada e nada mais. Usar optional chaining (`c?.var_margem_pp`) e tratar `undefined` e `null` do mesmo jeito — o payload evoluiu por adição em várias fases e a chave pode não existir.

**2. Bloco "Como interpretar" (linhas 549-556).** Trocar a enumeração `NPS · Var. Faturamento · Var. Margem` por `NPS · variação do faturamento · variação da margem`. Abreviação de nome de coluna também é jargão não auto-explicativo.

**3. Defaults defensivos.** Na desestruturação de props do componente `PerformanceShow`, acrescentar `empresas_score = []`, `empresas_score_resumo = { entraram: 0, nao_entraram: 0 }` e `tem_detalhe_empresas = false`. Elas ainda não são usadas nesta task (a seção entra na task 3), mas o default precisa existir desde já: um snapshot antigo ou um deploy parcial nunca pode fazer a tela receber `undefined` e quebrar no render.

Criar `tests/Feature/Phase123/RetrocompatSnapshotAntigoTest.php`, namespace `Tests\Feature\Phase123`, estendendo `Phase123TestCase`, estilo `#[Test]`, `Http::fake()` no `setUp`. Casos (D-11/UIEM-03):
- Semear um snapshot mensal para `2026-06-01` cujo `breakdown_json` tem `componentes` **sem** a chave `var_margem_pp` e **sem** a chave `empresas_score` no nível raiz — simulando snapshot gravado antes da Fase 120. `GET route('performance.show', $alvo) . '?mes=2026-06'` devolve 200, a prop `resultado.componentes.var_margem_pct` presente com o valor semeado, `tem_detalhe_empresas === false` e `empresas_score === []`.
- Assertar que o backend **não inventa** a chave: `resultado.componentes` continua sem `var_margem_pp` (usar `assertInertia` com `missing` ou verificar o array de chaves). O controller é passthrough do `breakdown_json` — se ele começar a preencher chave que o snapshot não tinha, o teste dourado da Fase 120 é o próximo a quebrar.
- Semear `breakdown_json['empresas_score'] = []` (chave presente e vazia, caso pós-Fase 120 sem shadow) e provar que o comportamento é idêntico: 200, `tem_detalhe_empresas === false`.
- Modo "Em curso" (sem `?mes`) com profissional sem carteira: 200, `resultado.sem_carteira === true`, `empresas_score === []`, sem erro.
  </action>

  <verify>
    <automated>php artisan test --filter=RetrocompatSnapshotAntigoTest &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
- `grep -c 'percentageMargin' resources/js/Pages/Performance/Show.jsx` retorna `0`
- `grep -c 'Var. Margem' resources/js/Pages/Performance/Show.jsx` retorna `0`
- `grep -c 'MARGEM_CARD_SUBLABEL' resources/js/Pages/Performance/Show.jsx` retorna ao menos `1`
- `grep -c 'fraseVarMargemPp' resources/js/Pages/Performance/Show.jsx` retorna ao menos `1`
- A desestruturação de props define `empresas_score = []`, `empresas_score_resumo = { entraram: 0, nao_entraram: 0 }` e `tem_detalhe_empresas = false`
- O `valor` do card de margem continua sendo `formatPercent(c.var_margem_pct)` (D-04 preservada)
- `php artisan test --filter=RetrocompatSnapshotAntigoTest` sai com código 0, com ao menos 4 testes
- `php artisan test --filter=Phase120` continua verde
- `npm run build` completa sem erro
  </acceptance_criteria>

  <done>
O card de margem explica a variação em português simples, sem citar nome de campo de API, sem trocar o número que produz a nota, e a tela sobrevive a payload de snapshot antigo sem `undefined` nem 500.
  </done>
</task>

<task type="auto">
  <name>Task 2: Componente EmpresasScoreTabela — duas seções, denominador explícito, formato antes→depois e selo Shopee</name>

  <files>
resources/js/Components/Desempenho/EmpresasScoreTabela.jsx
  </files>

  <read_first>
resources/js/lib/desempenhoLabels.js
tests/js/desempenhoLabels.test.js
resources/js/Pages/Desempenho/RelatorioBonificacao.jsx
resources/js/Pages/Performance/Show.jsx
.planning/phases/123-telas-e-relat-rios-v21-0/123-CONTEXT.md
  </read_first>

  <action>
Criar `resources/js/Components/Desempenho/EmpresasScoreTabela.jsx` — componente de apresentação puro, sem `router`, sem `usePage`, sem fetch. Vai ser consumido por `Performance/Show.jsx` (task 3) e por `Desempenho/RelatorioBonificacao.jsx` (Plano 05), então nada nele pode assumir uma tela específica. Docblock de topo em pt-BR citando D-05, D-06 e D-07.

**Props:** `{ linhas = [], resumo = { entraram: 0, nao_entraram: 0 }, className = '' }`. Todos os defaults obrigatórios — o componente nunca pode quebrar com prop ausente.

**Divisão em duas seções (D-06).** Separar `linhas` chamando `dividirPorEntrada(linhas)` de `@/lib/desempenhoLabels`, que devolve `{ entraram, naoEntraram }`. **Não reimplementar o filtro aqui** — a regra é função pura testada no Plano 01 (casos Felipe e Matheus Estrela travados em `tests/js/desempenhoLabels.test.js`); duplicá-la dentro do JSX a tornaria verificável só por olho humano na wave 5. A ordem interna de cada grupo é a que veio do backend (nota desc, `null` por último, desempate por nome) e `dividirPorEntrada` preserva — **não reordenar no front**. Os títulos vêm de `tituloEntraram(resumo.entraram)` e `tituloNaoEntraram(resumo.nao_entraram)`, ou seja `Entraram na conta (3)` e `Não entraram (27)`. O denominador vive no próprio título: é isso que torna impossível ler a nota sem ver sobre quantas empresas ela foi feita. Usar sempre os números de `resumo`, que vêm pré-calculados do backend pelo mesmo critério que decide o denominador da nota — nunca recontar a partir de `linhas.length`.

Abaixo do título da primeira seção, duas linhas de texto em `text-white/50 text-xs`: `SECAO_ENTRARAM_SUBTITULO` e, logo em seguida, `NOTA_TOPO_RESSALVA`. A segunda é obrigatória e não é decoração: com a flag `metrics.performance_company_first_score` em `false`, a `nota_final` do topo **não** é a média das notas por empresa listadas abaixo (learnings §9 — pelo método antigo 8 de 11 profissionais recebiam bônus em 2026-06; pelo novo, 1 de 11). Sem essa ressalva a tela põe dois números contraditórios lado a lado.

Abaixo do título da segunda seção: `SECAO_NAO_ENTRARAM_SUBTITULO`.

**Colapso da segunda seção.** Estado local com `useState`, inicializado por `deveColapsarNaoEntraram(resumo.nao_entraram)` de `@/lib/desempenhoLabels` — a seção nasce colapsada quando a função devolve `true`. Não comparar com `LIMIAR_COLAPSO_NAO_ENTRARAM` à mão aqui: o limiar e a direção da comparação estão travados por teste de fronteira no Plano 01 (`8` → aberta, `9` → colapsada). No caso Felipe são 27 empresas — a segunda seção é maior que a primeira e nasce colapsada, senão engole a tela. O botão de alternância mostra `` `Ver as ${resumo.nao_entraram} empresas que não entraram` `` quando fechado e `` `Ocultar as ${resumo.nao_entraram} empresas que não entraram` `` quando aberto, com o ícone `ChevronRight`/`ChevronDown` de `lucide-react`, estilo `inline-flex items-center gap-1.5 text-[13px] text-white/60 hover:text-white transition-colors`. Quando `resumo.nao_entraram === 0`, a segunda seção inteira (título, subtítulo e botão) não é renderizada.

**Tabela.** Uma `<table>` por seção dentro de `<div className="overflow-x-auto">`, com `min-w-[720px]`, na mesma densidade de `Desempenho/RelatorioBonificacao.jsx`: cabeçalho `border-b border-white/[0.08] text-left text-[10px] uppercase tracking-wider text-white/40` com células `px-3 py-3 font-semibold`; linhas `border-b border-white/[0.05] hover:bg-white/[0.02]` com células `px-3 py-3`; números em `tabular-nums`. Container externo `rounded-2xl bg-ecf-card border border-white/[0.08]`, composto com `cn()` de `@/lib/utils` junto do `className` recebido.

Colunas da PRIMEIRA seção: `Empresa`, `NPS`, `Faturamento`, `Margem`, `Nota`.
Colunas da SEGUNDA seção: `Empresa`, `NPS`, `Faturamento`, `Margem`, `Nota parcial`, `Por que não entrou`.

**Conteúdo das células (UIEM-02 — os três componentes precisam estar visíveis):**
- `Empresa`: `linha.company_name` em `text-white/80`, truncado; abaixo, em `text-[10px] uppercase tracking-wider text-white/30`, a `fonte_financeira` (`adman` / `shopee`) quando não for nula.
- `NPS`: `fmtNotaEmpresa(linha.nps_pontos)` e, embaixo, `pontos` em `text-[10px] text-white/30`. Quando nulo, `—`.
- `Faturamento`: `fmtVarPct(linha.faturamento_var_pct)` colorido (`>= 0` → `text-emerald-300`, `< 0` → `text-rose-300`, nulo → `text-white/40`) e, embaixo, `` `${fmtNotaEmpresa(linha.faturamento_pontos)} pontos` `` em `text-[10px] text-white/30`.
- `Margem`: este é o formato travado pela D-05. Renderizar três elementos em linha com `gap-1.5`: `fmtPctAbs(linha.margem_pct_anterior)`, o caractere `→` em `text-white/30`, e `fmtPctAbs(linha.margem_pct_atual)`, seguidos de `fmtPp(linha.margem_var_pp)` destacado (`font-semibold`, cor pelo sinal). Resultado lido na tela: `14,1% → 12,0%  −2,1`. O número da variação **não leva `%`** — é ponto percentual, e somar `%` reintroduziria exatamente a confusão que a D-05 existe para evitar. Pôr `title={fmtMargemAntesDepois(linha.margem_pct_anterior, linha.margem_pct_atual, linha.margem_var_pp)}` na célula. Embaixo, `` `${fmtNotaEmpresa(linha.margem_pontos)} pontos` ``.
- `Nota` (1ª seção): `fmtNotaEmpresa(linha.nota_empresa)` em `font-bold`, escala de cor `>= 4.5` → `text-emerald-400`, `>= 4.0` → `text-ecf-yellow`, senão `text-white/70`.
- `Nota parcial` (2ª seção): `fmtNotaEmpresa(linha.nota_empresa_parcial)` em `text-white/50`, com `title` explicando que é a média só dos componentes presentes. Nunca colorir como se fosse nota oficial.
- `Por que não entrou` (2ª seção): `linha.quality?.motivos` mapeado por `motivoLabel()` e renderizado como selos empilhados (`inline-flex rounded-full bg-white/[0.04] border border-white/[0.08] px-2 py-0.5 text-[10px] text-white/60`). **Sempre iterar** — `motivos` é array de 0 a N entradas, nunca assumir string única. Quando o array estiver vazio ou ausente, mostrar `—`.

**Selo Shopee (D-07) — com o tratamento do caso "carteira inteira".** Decidir com `carteiraTodaShopeeNaEntrada(entraram)` de `@/lib/desempenhoLabels`, passando as linhas da PRIMEIRA seção. **Não escrever o `every()` aqui** — a função já trata o array vazio (devolve `false`) e está travada por teste no Plano 01 com o caso Matheus Estrela; um `every()` escrito à mão em array vazio devolveria `true` e mostraria o aviso de carteira só-Shopee numa carteira sem nenhuma empresa:
- **Se `true`** (caso Matheus Estrela, carteira só-Shopee): renderizar UMA vez, logo abaixo do subtítulo da primeira seção, uma faixa `rounded-lg border border-amber-500/25 bg-amber-500/10 px-3 py-2 text-xs text-amber-200` com o texto `AVISO_CARTEIRA_SO_SHOPEE`. Nas células de margem dessas linhas, mostrar apenas `MARGEM_SEM_DADO_TEXTO` (`'sem dado de margem'`) em `text-white/40 text-xs`, **sem** selo por linha. Um selo desenhado como exceção vira ruído quando é toda a carteira.
- **Caso contrário:** para cada linha em que `ehPlaceholderShopee(linha)` for verdadeiro, a célula de margem mostra o selo `SELO_SHOPEE_TEXTO` com `title={SELO_SHOPEE_TITULO}`, estilo `inline-flex items-center gap-1 rounded-full border border-amber-500/25 bg-amber-500/10 px-2 py-0.5 text-[10px] font-semibold text-amber-200`, no lugar do formato antes→depois. Nunca mostrar apenas `—` para esse caso: perderia a explicação de que é limitação da fonte, não desempenho ruim.

Em nenhuma hipótese a linha Shopee sai da primeira seção. Ela é `status='complete'` com `margem_pontos = 1.0` fixo (placeholder da Fase 109) e conta no denominador — tratar como "dentro da conta, com ressalva visual".

Nenhum texto literal em pt-BR pode ser escrito direto neste arquivo além dos cabeçalhos de coluna e das palavras `pontos`/`—`: todo o resto vem de `@/lib/desempenhoLabels`.
  </action>

  <verify>
    <automated>npm run test:js &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` completa sem erro
- `grep -c '@/lib/desempenhoLabels' resources/js/Components/Desempenho/EmpresasScoreTabela.jsx` retorna `1`
- O arquivo contém `tituloEntraram`, `tituloNaoEntraram`, `NOTA_TOPO_RESSALVA`, `AVISO_CARTEIRA_SO_SHOPEE`, `SELO_SHOPEE_TEXTO`, `dividirPorEntrada`, `carteiraTodaShopeeNaEntrada`, `deveColapsarNaoEntraram` e `ehPlaceholderShopee`
- `grep -c 'linhas.length' resources/js/Components/Desempenho/EmpresasScoreTabela.jsx` retorna `0` no cálculo dos títulos (o denominador vem de `resumo`)
- `grep -c 'fmtPp' resources/js/Components/Desempenho/EmpresasScoreTabela.jsx` retorna ao menos `1` e a célula de margem não concatena `'%'` ao resultado de `fmtPp`
- **As três ramificações vêm das funções puras, não reimplementadas no JSX:** `grep -cE "\.filter\(.*status|status === 'complete'" resources/js/Components/Desempenho/EmpresasScoreTabela.jsx` retorna `0`; `grep -c '.every(' resources/js/Components/Desempenho/EmpresasScoreTabela.jsx` retorna `0`; `grep -c 'LIMIAR_COLAPSO_NAO_ENTRARAM' resources/js/Components/Desempenho/EmpresasScoreTabela.jsx` retorna `0` (o limiar é aplicado dentro de `deveColapsarNaoEntraram`, não comparado aqui)
- O `useState` de colapso é inicializado por `deveColapsarNaoEntraram(resumo.nao_entraram)`
- `npm run test:js` continua verde (as regras que o componente consome estão travadas lá)
- O componente aceita ser renderizado com zero props sem lançar (defaults presentes em `linhas`, `resumo` e `className`)
- `quality?.motivos` é iterado com `map`, nunca tratado como string
  </acceptance_criteria>

  <done>
Existe um componente reutilizável que mostra a carteira dividida em "entraram na conta (N)" e "não entraram (M)", com a margem no formato `14,1% → 12,0%  −2,1`, os três componentes visíveis por empresa, os motivos traduzidos e o selo de Shopee que degrada para aviso único quando a carteira inteira é Shopee.
  </done>
</task>

<task type="auto">
  <name>Task 3: Encaixe da seção "Empresas da carteira" em Show.jsx + aviso de ausência (D-03)</name>

  <files>
resources/js/Pages/Performance/Show.jsx
tests/js/estrutura-performance-show.test.js
  </files>

  <read_first>
resources/js/Pages/Performance/Show.jsx
resources/js/Components/Desempenho/EmpresasScoreTabela.jsx
resources/js/lib/desempenhoLabels.js
tests/js/_fonte.js
  </read_first>

  <action>
Em `resources/js/Pages/Performance/Show.jsx`, importar `EmpresasScoreTabela` de `@/Components/Desempenho/EmpresasScoreTabela` e, de `@/lib/desempenhoLabels`, os símbolos `AVISO_SEM_DETALHE_TITULO`, `AVISO_SEM_DETALHE_EM_CURSO` e `avisoSemDetalheFechado`.

Inserir a seção nova **dentro do ramo `!semCarteira`**, imediatamente APÓS o bloco "Info carteira" (hoje linhas 521-539) e ANTES do fechamento do fragmento `</>`. O bloco "SEM CARTEIRA" (DESEMP-10) continua sendo um ramo alternativo — quando o profissional não tem carteira, nada disso é renderizado.

A seção tem um cabeçalho próprio `Empresas da carteira` em `text-white text-lg font-display font-bold` com o ícone `Briefcase` (já importado no arquivo), e abaixo dele um de dois conteúdos:

**Quando `tem_detalhe_empresas` é `true`:** renderizar `<EmpresasScoreTabela linhas={empresas_score} resumo={empresas_score_resumo} />`.

**Quando `tem_detalhe_empresas` é `false` (D-03):** renderizar uma caixa de aviso — a seção **aparece com explicação**, nunca some silenciosamente. Quem viu a lista em 2026-06 e não a vê em 2026-07 precisa entender que não quebrou. A caixa usa `rounded-2xl border border-white/[0.08] bg-white/[0.02] p-5 flex items-start gap-3` com o ícone `Info` (já importado), título `AVISO_SEM_DETALHE_TITULO` em `text-white/80 text-sm font-semibold`, e corpo em `text-white/50 text-xs mt-1 leading-relaxed` escolhido assim:
- se `modoAtivo === 'em_curso'` → `AVISO_SEM_DETALHE_EM_CURSO`
- caso contrário → `avisoSemDetalheFechado(mesExtenso(String(mes_selecionado ?? '').slice(0, 7)))`, que produz por exemplo `Ainda não há detalhe por empresa para junho de 2026. O detalhe passou a ser gravado no fechamento a partir de agosto/2026 — competências consolidadas antes disso não têm esse registro.`

A variável `modoAtivo` já existe no componente (linha 310) e o helper `mesExtenso` já existe no módulo — reusar, não recriar.

Não usar `periodo?.is_closed` sozinho para decidir entre lista e aviso: a decisão é sempre `tem_detalhe_empresas`, que o backend derivou da existência real de linhas. 2026-05 e 2026-04 são meses fechados no calendário que nunca tiveram consolidação nenhuma.

Criar `tests/js/estrutura-performance-show.test.js` usando `lerSemComentarios` de `tests/js/_fonte.js` (a fonte sem comentários — os comentários deste projeto citam os próprios identificadores e um `grep` cru passaria pelo motivo errado). Assertar sobre `resources/js/Pages/Performance/Show.jsx`:
- a fonte NÃO contém `percentageMargin` (gate de UIEM-01 que sobrevive a refactor)
- a fonte contém `EmpresasScoreTabela`, `tem_detalhe_empresas`, `empresas_score_resumo`, `AVISO_SEM_DETALHE_EM_CURSO` e `avisoSemDetalheFechado`
- a fonte NÃO contém a string `'Ainda não há detalhe'` (o texto vive só no módulo de labels)
- a fonte contém `formatPercent(c.var_margem_pct)` — prova de que a D-04 não foi violada trocando o número do card
E assertar sobre `resources/js/Components/Desempenho/EmpresasScoreTabela.jsx` que a fonte contém `tituloEntraram`, `tituloNaoEntraram` e `NOTA_TOPO_RESSALVA`.

Rodar `npm run build` ao final — obrigatório por convenção do projeto após qualquer edição de frontend.
  </action>

  <verify>
    <automated>npm run test:js &amp;&amp; npm run build &amp;&amp; php artisan test --filter=Phase123</automated>
  </verify>

  <acceptance_criteria>
- `npm run test:js` sai com código 0 incluindo `estrutura-performance-show.test.js`
- `npm run build` completa sem erro
- `grep -c 'EmpresasScoreTabela' resources/js/Pages/Performance/Show.jsx` retorna ao menos `2` (import + uso)
- `grep -c 'Ainda não há detalhe' resources/js/Pages/Performance/Show.jsx` retorna `0`
- A escolha entre lista e aviso é feita por `tem_detalhe_empresas`, não por `periodo.is_closed` — `grep` da condição do bloco novo não contém `is_closed`
- A seção nova está dentro do ramo `!semCarteira`, depois do bloco "Info carteira"
- `php artisan test --filter=Phase123` continua verde
  </acceptance_criteria>

  <done>
`/performance/{id}` em competência fechada com detalhe mostra a carteira empresa a empresa com nota, três componentes e denominador explícito; sem detalhe, mostra por que a lista não está ali; e nada disso é renderizado para profissional sem carteira.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prop Inertia → render React | Único ponto novo desta camada; todo dado já foi filtrado por usuário e competência no backend (Plano 02) |
| browser → `GET /performance/{user}` | Rota já gated por `permission:core.performance`; esta fase não altera o middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Information Disclosure | Seção "Empresas da carteira" em `Performance/Show.jsx` | mitigate | O front não busca dado — só renderiza a prop, que o backend já escopou por `doUsuario($user->id)` e por competência. Nenhum fetch, nenhum endpoint novo, nenhum id de outra carteira alcançável a partir do JSX |
| T-123-08 | Information Disclosure | Contradição entre a nota do topo e as notas por empresa | mitigate | `NOTA_TOPO_RESSALVA` renderizada obrigatoriamente na primeira seção — com a flag `false`, `nota_final` não é a média das notas listadas (learnings §9: 8/11 contra 1/11 em 2026-06). Sem a ressalva, a tela induz leitura errada sobre pagamento de bônus |
| T-123-09 | Denial of Service (render) | Payload sem `empresas_score`/`var_margem_pp` | mitigate | Defaults na desestruturação de props + optional chaining em `quality?.motivos` e `c?.var_margem_pp`; `RetrocompatSnapshotAntigoTest` prova 200 e ausência de chave inventada |
| T-123-SC | Tampering | npm/composer installs | accept | Nenhum pacote novo; `lucide-react` e `cn()` já são dependências instaladas |
</threat_model>

<verification>
- `php artisan test --filter=RetrocompatSnapshotAntigoTest` verde
- `php artisan test --filter=Phase123` verde
- `php artisan test --filter=Phase120` verde (teste dourado do payload intocado)
- `npm run test:js` verde
- `npm run build` sem erro
- `grep -c 'percentageMargin' resources/js/Pages/Performance/Show.jsx` retorna 0
- `git diff --stat app/` vazio (esta é uma wave de front; o backend já foi entregue no Plano 02)
</verification>

<success_criteria>
- Card de margem em linguagem simples, sem nome de campo de API, sem trocar o número que produz a nota (UIEM-01 + D-04)
- Lista por empresa com nota e três componentes, dividida em "entraram (N)" / "não entraram (M)" (UIEM-02 + D-06)
- Margem lida como `14,1% → 12,0%  −2,1` (D-05)
- Shopee dentro da conta com ressalva, e sem virar ruído quando é a carteira inteira (D-07)
- Ausência de detalhe explicada, nunca silenciosa (D-03)
- Payload antigo renderiza sem erro e sem chave inventada (UIEM-03 + D-11)
</success_criteria>

<output>
Criar `.planning/phases/123-telas-e-relat-rios-v21-0/123-04-SUMMARY.md` ao terminar.
</output>
