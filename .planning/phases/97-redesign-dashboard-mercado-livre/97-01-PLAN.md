---
phase: 97-redesign-dashboard-mercado-livre
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/DashboardController.php
  - tests/Feature/Dashboard/DashboardMetricasPeriodoTest.php
autonomous: true
requirements: [DASH-97-2, DASH-97-3, DASH-97-4]
must_haves:
  truths:
    - "Cada KPI financeiro carrega variação vs. o período imediatamente anterior (mesma quantidade de dias)"
    - "A margem de contribuição média é ponderada pelo faturamento, não uma média simples"
    - "O gráfico tem uma série diária de margem além da de faturamento, ambas sobre o recorte"
    - "Filtrar em /dashboard/mercadolivre preserva o recorte marketplace='meli' (payload carrega marketplace + rota de navegação)"
    - "A /dashboard genérica continua funcionando (marketplace ausente = todo o setor)"
  artifacts:
    - path: "app/Http/Controllers/DashboardController.php"
      provides: "adminDashboard com janelas atual/anterior, margem ponderada, margin_chart, filters.marketplace, dashboard_route_name, stats.novas_empresas_count"
      contains: "novas_empresas_count"
    - path: "tests/Feature/Dashboard/DashboardMetricasPeriodoTest.php"
      provides: "Cobertura das janelas período-anterior, margem ponderada e preservação do marketplace"
      contains: "class DashboardMetricasPeriodoTest"
  key_links:
    - from: "DashboardController::adminDashboard"
      to: "adman_metrics"
      via: "SUM(revenue) da janela atual e da janela anterior"
      pattern: "whereBetween.*reference_date"
    - from: "Inertia payload stats"
      to: "Admin.jsx KPIs"
      via: "chaves *_delta_pct / *_delta_pp / novas_empresas_count"
      pattern: "delta"
---

<objective>
Preparar o BACKEND dos KPIs e do gráfico do redesign: calcular a janela do período atual vs. a janela anterior (mesmo nº de dias, a partir de `adman_metrics`), a margem de contribuição PONDERADA por faturamento com sua variação em pontos percentuais (pp), uma série diária de margem para o gráfico, a contagem de "empresas novas no mês" para o subtítulo do KPI, e corrigir a base do bug do marketplace no payload (incluir `marketplace` em `filters` + expor a rota de navegação correta).

Purpose: O JSX do redesign (planos 97-03/97-04) consome esses props novos. Sem eles o frontend não tem de onde ler delta, margem ponderada nem a série de margem.
Output: `DashboardController::adminDashboard` enriquecido + suíte de teste da matemática de janelas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md
@app/Http/Controllers/DashboardController.php
@app/Services/AdmanService.php

<interfaces>
<!-- Contrato atual do payload (adminDashboard ~843): stats{...}, revenue_chart[], tacos_chart[],
     filters{company_id, consultor_id, estrategista_id, group_id}, period. -->
<!-- filters HOJE NÃO tem marketplace → causa o bug do recorte ML sumir (ver Riscos §1 do CONTEXT). -->
<!-- getPeriodRange(period) → ['from'=>Y-m-d, 'to'=>Y-m-d]; N dias = 1/7/30/180 (default 30). -->
<!-- adman_metrics: colunas usadas → company_id, reference_date, revenue, ad_spend, contribution_margin_pct, tacos. -->
<!-- $companies = universo já filtrado (Performance, sem MlbEmpresa, + filtros company/consultor/estrategista/grupo/marketplace). -->
<!-- contratos_servico: ativo (bool), servicos.setor='performance'; possui data de início do atendimento (D3 "empresa nova"). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Janelas período-anterior, margem ponderada e série diária de margem</name>
  <files>app/Http/Controllers/DashboardController.php, tests/Feature/Dashboard/DashboardMetricasPeriodoTest.php</files>
  <read_first>
    - app/Http/Controllers/DashboardController.php (adminDashboard ~233-927: getPeriodRange ~217, blocos revenue/margin ~384-590, montagem do Inertia::render ~843-926)
    - app/Services/AdmanService.php (fallback SUM em adman_metrics)
    - .planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md (KPIs §"4 KPIs" e Claude's Discretion sobre janelas)
  </read_first>
  <behavior>
    - Dado adman_metrics com histórico >= 2N dias, SUM(revenue) da janela atual [from,to] e da janela anterior [from-Ndias, from-1] são calculados e o delta % = (atual-anterior)/anterior*100 é exposto em stats.total_revenue_delta_pct (null quando anterior=0).
    - avg_margin passa a ser PONDERADO por faturamento: SUM(revenue*contribution_margin_pct)/SUM(revenue) do recorte na janela atual; delta em pontos percentuais (pp) vs. mesma conta na janela anterior em stats.avg_margin_delta_pp.
    - margin_chart[] tem 1 ponto por dia do período (mesmo eixo de datas de revenue_chart), value = margem ponderada do dia sobre o recorte (0 quando sem dados no dia).
    - Janela atual N=30 comparada a N=30 anteriores (nunca N dias atuais vs mês inteiro anterior).
  </behavior>
  <action>
    Adicionar ao adminDashboard o cálculo das duas janelas a partir de `adman_metrics` (reusar o padrão de SUM agregado já presente nos blocos de revenue). Derivar `getPeriodRange` para também devolver a janela anterior (mesmo nº de dias imediatamente antes de `from`). Calcular: (a) `total_revenue` da janela anterior e expor `stats.total_revenue_delta_pct` (delta %, `null` se anterior=0 para o front tratar como "sem base"); (b) trocar `avg_margin` de média simples de `contribution_margin_pct` para média PONDERADA por `revenue` (SUM(revenue*margin)/SUM(revenue)) na janela atual, e expor `stats.avg_margin_delta_pp` (diferença em pontos percentuais vs. a mesma conta na janela anterior); (c) montar `margin_chart` como a série DIÁRIA da margem ponderada sobre o recorte, com o mesmo eixo de datas (`d/m`) e política de preenchimento com 0 usada por `revenueChart`. Todos os cálculos restritos a `$companies->pluck('id')` (o recorte). Comentários em pt-BR. NÃO alterar `revenue_chart`/`tacos_chart` existentes (não regredir). Criar a suíte de teste com fixtures de `adman_metrics` cobrindo >= 2N dias para 2+ empresas do universo Performance, asserindo os deltas e a margem ponderada via `assertInertia` (chaves de `stats` e presença de `margin_chart`).
  </action>
  <verify>
    <automated>php artisan test --filter=DashboardMetricasPeriodoTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=DashboardMetricasPeriodoTest` verde.
    - Payload Inertia de `Dashboard/Admin` contém `stats.total_revenue_delta_pct`, `stats.avg_margin_delta_pp`, `avg_margin` ponderado e `margin_chart` com nº de pontos = nº de dias do período.
    - `revenue_chart` e `tacos_chart` inalterados na forma.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contagem de empresas novas no mês + correção da base do bug do marketplace</name>
  <files>app/Http/Controllers/DashboardController.php, tests/Feature/Dashboard/DashboardMetricasPeriodoTest.php</files>
  <read_first>
    - app/Http/Controllers/DashboardController.php (montagem do array `filters` ~878-883; método `mercadolivre()` ~149 e `index()` ~68 que definem a rota/marketplace)
    - .planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md (D3 "empresa nova"; Riscos §1 bug do marketplace)
  </read_first>
  <behavior>
    - stats.novas_empresas_count = nº de empresas do recorte cujo contrato ativo em serviço de setor 'performance' iniciou no mês corrente (D3: data de início do contrato, NÃO companies.created_at).
    - filters inclui a chave 'marketplace' com o valor do recorte atual (ex.: 'meli' em /dashboard/mercadolivre, null em /dashboard).
    - O payload expõe dashboard_route_name = nome da rota atual ('mercadolivre.dashboard' ou 'dashboard') para o front navegar preservando o contexto.
  </behavior>
  <action>
    (a) Calcular `stats.novas_empresas_count`: contar, dentro de `$companies` (recorte), as empresas com `contratos_servico.ativo=true` em `servico.setor='performance'` cuja data de início cai no mês corrente (`Carbon::now()->startOfMonth()..now()`), conforme D3 — usar a coluna de início do `contratos_servico` (confirmar o nome real da coluna ao ler o model/migration; NÃO usar `companies.created_at`). (b) Adicionar `'marketplace' => $marketplaceFilter` ao array `filters` do `Inertia::render` (corrige a base do Risco §1 — o front hoje não recebe marketplace de volta). (c) Expor `'dashboard_route_name' => $request->route()?->getName()` no payload, para o applyFilter do front (97-03) navegar pela rota corrente (que re-aplica `meli` server-side em `mercadolivre()`), preservando recorte E o active-state do menu. Estender a suíte de teste: (1) requisição a `mercadolivre.dashboard` → `filters.marketplace === 'meli'` e `dashboard_route_name === 'mercadolivre.dashboard'`; (2) empresa com contrato iniciado neste mês conta em `novas_empresas_count`, empresa com contrato antigo não conta. Comentários pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=DashboardMetricasPeriodoTest</automated>
  </verify>
  <acceptance_criteria>
    - Teste verde cobrindo `filters.marketplace`, `dashboard_route_name` e `novas_empresas_count`.
    - Em `/dashboard/mercadolivre` o payload devolve `filters.marketplace='meli'`; em `/dashboard` devolve `null`.
    - `novas_empresas_count` usa início de contrato (D3), verificado por fixture de contrato antigo vs. do mês.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador admin → adminDashboard | query params (period/company_id/group_id/consultor_id/estrategista_id/marketplace) cruzam aqui |
| adman_metrics/contratos_servico → payload | dados financeiros por empresa expostos ao front |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01-01 | Tampering | query param `marketplace` | mitigate | whitelist `in:meli,shopee,amazon` já aplicada em `mercadolivre()`/`ecf()` antes de chegar ao método — manter |
| T-97-01-02 | Information Disclosure | janelas/margem por empresa | accept | universo permanece `$companiesQuery` (Performance, sem MlbEmpresa); filtros só restringem, nunca ampliam o escopo já autorizado |
| T-97-01-03 | Tampering | company_id/group_id | mitigate | usados em `where()` com binding Eloquent (sem concatenação SQL) — sem injeção |
| T-97-01-SC | Tampering | dependências | accept | nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- `php artisan test --filter=DashboardMetricasPeriodoTest` verde.
- Suítes que já tocam DashboardController continuam verdes (regressão): `php artisan test --filter=Dashboard`.
- Fixtures usam >= 2N dias de `adman_metrics` para a janela anterior existir.
</verification>

<success_criteria>
- KPIs recebem delta (faturamento %, margem pp); margem é ponderada por faturamento.
- Série diária de margem disponível (`margin_chart`).
- `filters.marketplace` + `dashboard_route_name` no payload (base do fix do bug).
- `novas_empresas_count` por início de contrato (D3).
</success_criteria>

<output>
Criar `.planning/phases/97-redesign-dashboard-mercado-livre/97-01-SUMMARY.md` ao concluir.
</output>
