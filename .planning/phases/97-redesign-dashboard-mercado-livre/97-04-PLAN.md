---
phase: 97-redesign-dashboard-mercado-livre
plan: 04
type: execute
wave: 3
depends_on: ["97-02", "97-03"]
files_modified:
  - resources/js/Pages/Dashboard/Admin.jsx
  - resources/js/Components/Dashboard/ChartEvolucao.jsx
  - resources/js/Components/Dashboard/NpsRuimCarrossel.jsx
  - resources/js/Components/Dashboard/ScoreEquipe.jsx
  - resources/js/Components/Dashboard/NovasEmpresas.jsx
autonomous: false
requirements: [DASH-97-4, DASH-97-5, DASH-97-6, DASH-97-7]
must_haves:
  truths:
    - "O gráfico 'Evolução no período' tem abas Faturamento e Margem, série diária do recorte e hover interativo (tooltip + Pico/Menor)"
    - "O card 'NPS ruim' lista as respostas de nota baixa do recorte com link para o NPS completo"
    - "O 'Score da equipe' mostra nota 0–5 com barra + breakdown, ordenado do pior para o melhor, com link para a área da equipe"
    - "As 'Novas empresas no mês' aparecem em cards quando houver, cada card linkando para a empresa"
    - "A tela tem estados de carregando/vazio/erro reais (sem toggle de demo)"
  artifacts:
    - path: "resources/js/Components/Dashboard/ChartEvolucao.jsx"
      provides: "Gráfico abas Faturamento/Margem com hover e Pico/Menor"
      min_lines: 40
    - path: "resources/js/Components/Dashboard/NpsRuimCarrossel.jsx"
      provides: "Carrossel de respostas ruins com link"
      min_lines: 30
    - path: "resources/js/Components/Dashboard/ScoreEquipe.jsx"
      provides: "Lista de score 0-5 com breakdown, pior→melhor"
      min_lines: 30
    - path: "resources/js/Components/Dashboard/NovasEmpresas.jsx"
      provides: "Cards de empresas novas do mês"
      min_lines: 20
  key_links:
    - from: "ChartEvolucao"
      to: "revenue_chart / margin_chart"
      via: "abas Faturamento/Margem"
      pattern: "margin_chart"
    - from: "NpsRuimCarrossel"
      to: "route('nps.index')"
      via: "link Abrir NPS completo"
      pattern: "nps.index"
    - from: "ScoreEquipe"
      to: "route('performance.index')"
      via: "link Área da equipe"
      pattern: "performance.index"
---

<objective>
Fechar o frontend do redesign: gráfico "Evolução no período" com abas Faturamento/Margem (série diária do recorte, hover com tooltip e marcações Pico/Menor), card "NPS ruim" (carrossel com link ao NPS completo), "Score da equipe" (nota 0–5 oficial + barra + breakdown carteira·NPS·Margem·TACoS, ordenado pior→melhor, link para a área da equipe), "Novas empresas no mês" (cards condicionais → `companies.show`) e ESTADOS reais (carregando/vazio/erro). Encerra com checkpoint visual humano (dark theme) das duas rotas, sem bloquear a automação (o build roda antes).

Purpose: Entrega DASH-97-4 (gráfico), DASH-97-5 (NPS ruim UI), DASH-97-6 (Score UI) e DASH-97-7 (novas empresas + estados) no frontend.
Output: 4 componentes novos + `Admin.jsx` montando-os com estados reais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md
@resources/js/Pages/Dashboard/Admin.jsx

<interfaces>
<!-- Props do 97-01: revenue_chart[{date,revenue}], margin_chart[{date,value}], stats. -->
<!-- Props do 97-02: performance_equipe[{id,name,nota_final,faixa_bonus, carteira,nps,margem,tacos ...}] (ordenar pior→melhor),
     nps_ruins[{company_id,company_name,data,nota,comment,analista,estrategista}],
     novas_empresas[{id,name,grupo,status,faturamento_parcial,tacos,analista,estrategista}]. -->
<!-- recharts já usado no arquivo (AreaChart/BarChart/Tooltip/ResponsiveContainer). Preferir recharts;
     SVG manual só se recharts não der hover/Pico/Menor equivalente (Claude's Discretion do CONTEXT). -->
<!-- Estados: loading via Inertia (router processing) ; empty quando recorte sem dados (stats.total_companies===0 ou lista vazia) ; error se prop de erro/base falhar. SEM toggle de demo. -->
<!-- Rotas Ziggy: nps.index, performance.index, companies.show. -->
<!-- Pitfall Rollup: cores/flags/labels de status computados DENTRO do callback do .map(). Tokens ecf-*, cn(). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gráfico "Evolução no período" com abas Faturamento/Margem e hover</name>
  <files>resources/js/Components/Dashboard/ChartEvolucao.jsx, resources/js/Pages/Dashboard/Admin.jsx</files>
  <read_first>
    - resources/js/Pages/Dashboard/Admin.jsx (uso de recharts existente: AreaChart/Tooltip/ResponsiveContainer ~230-245; chartStyle ~84-88)
    - .planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md (§Gráfico "Evolução no período"; D4 abas Fat+Margem)
  </read_first>
  <action>
    Criar `ChartEvolucao.jsx`: abas "Faturamento" e "Margem" (D4 — só essas duas); a aba Faturamento plota `revenue_chart` (R$), a aba Margem plota `margin_chart` (%). Usar recharts (padrão do projeto) com hover: tooltip mostrando data, valor e "+X% vs média" (média da série da aba); marcar visualmente o Pico e o Menor ponto (ex.: ReferenceDot/label). Subtítulo dinâmico: "{métrica} {unidade} · {período} · {N empresas | nome da empresa}" — derivar de `period`/`filters.company_id`/`companies_list`. Se recharts não permitir Pico/Menor + o hover pedido, SVG manual é aceitável (Claude's Discretion) desde que preserve o comportamento. Montar `<ChartEvolucao .../>` em `Admin.jsx` no lugar do bloco de gráfico atual do modo normal. Cores/flags dentro dos callbacks (pitfall Rollup); tokens `ecf-*`; comentários pt-BR.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0.
    - `ChartEvolucao.jsx` tem abas Faturamento/Margem consumindo `revenue_chart`/`margin_chart`, com tooltip e marcação Pico/Menor.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Cards NPS ruim / Score da equipe / Novas empresas + estados reais</name>
  <files>resources/js/Components/Dashboard/NpsRuimCarrossel.jsx, resources/js/Components/Dashboard/ScoreEquipe.jsx, resources/js/Components/Dashboard/NovasEmpresas.jsx, resources/js/Pages/Dashboard/Admin.jsx</files>
  <read_first>
    - resources/js/Pages/Dashboard/Admin.jsx (widget performance_equipe atual ~408-509; NpsPendingWidget import ~15 como molde de "resumo + link")
    - .planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md (§Linha 2.1fr/1fr NPS ruim + Score; §Novas empresas; §Estados reais)
  </read_first>
  <action>
    Criar 3 componentes: (a) `NpsRuimCarrossel.jsx` — carrossel horizontal (setas ‹ ›, badge com contagem) das respostas `nps_ruins`: empresa, data, nota (cor por faixa computada no callback), comentário, analista, estrategista, link "Abrir NPS completo →" (`route('nps.index')`); empty-state neutro "Nenhuma nota baixa no período" (sem o jargão "6 ou acima"); rodapé "Ver respostas completas →". (b) `ScoreEquipe.jsx` — lista compacta por pessoa de `performance_equipe`: nome, papel, nota 0–5 (`nota_final`, D1) com barra, e linha "carteira · NPS · Margem · TACoS" (breakdown), ORDENADO pior→melhor (surfacing de quem precisa de atenção); link "Área da equipe →" (`route('performance.index')`). (c) `NovasEmpresas.jsx` — largura total CONDICIONAL (só quando `novas_empresas.length>0`): cards com nome, grupo, status (Atenção/Ramp-up/Saudável, cor no callback), faturamento parcial, TACoS, responsáveis; card → `route('companies.show', id)`; link "Listagem / cadastro →" (`companies.index`). Montar os três em `Admin.jsx` (linha 2.1fr/1fr para NPS ruim + Score; Novas empresas full-width abaixo). Implementar ESTADOS reais no `Admin.jsx`: carregando (skeletons via estado de processing do Inertia), vazio (recorte sem dados → mensagens por widget), erro (prop/base falhando → bloco "Tentar novamente"); REMOVER qualquer toggle de estados de demo herdado. Reavaliar o `tvMode` existente: mantê-lo funcional (não quebrar) — não é escopo redesenhá-lo. Rodar `npm run build`. Pitfall Rollup, tokens `ecf-*`, `cn()`, comentários pt-BR, zero jargão sem explicação.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0.
    - NPS ruim linka `nps.index`; Score linka `performance.index` e ordena pior→melhor com nota 0–5 + breakdown; Novas empresas condicional linka `companies.show`.
    - Estados carregando/vazio/erro reais presentes; sem toggle de demo; `tvMode` ainda monta.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual das duas rotas (dark theme)</name>
  <what-built>
    Redesign completo do Dashboard/Admin.jsx (filtros rascunho→aplicar + chips, KPIs com delta/link, gráfico Faturamento/Margem interativo, NPS ruim, Score da equipe, Novas empresas, estados reais) servindo /dashboard e /dashboard/mercadolivre.
  </what-built>
  <how-to-verify>
    1. `npm run build` já rodou verde na Task 2. Subir o ambiente (`php artisan serve` + `npm run dev` se preferir hot reload).
    2. Acessar `/dashboard/mercadolivre` como admin. Conferir dark theme (tokens ecf-*), filtros colapsáveis, mudar um filtro → botão "Aplicar" fica amarelo → aplicar → chips aparecem e TODOS os widgets mudam (KPIs, gráfico, NPS ruim, Score, Novas empresas).
    3. Confirmar que ao aplicar o filtro a URL/recorte permanece em Mercado Livre (não cai na /dashboard genérica) e o menu lateral segue em ML.
    4. Alternar abas do gráfico Faturamento/Margem; passar o mouse (tooltip + Pico/Menor).
    5. Acessar `/dashboard` genérica: carrega sem marketplace, todo o setor, sem quebrar (não regrediu).
    6. Conferir estados: filtrar por uma empresa sem dados no período → estado vazio adequado.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os ajustes visuais/comportamentais necessários.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin.jsx → render de dados sensíveis | comentários de NPS e financeiro por empresa exibidos no dark theme admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04-01 | Information Disclosure | comentários de clientes no card NPS ruim | mitigate | página só acessível via role admin / líder com core.dashboard (middleware EnsureUserHasRole); front apenas renderiza o que o backend já autorizou |
| T-97-04-02 | Tampering | ids de link (companies.show/nps.index) | mitigate | rotas destino aplicam suas próprias policies/escopo; o link não concede acesso extra |
| T-97-04-SC | Tampering | dependências npm | accept | nenhum pacote novo — recharts/lucide/@radix já presentes |
</threat_model>

<verification>
- `npm run build` exit 0 (Task 1 e Task 2).
- Checkpoint visual humano aprovado nas duas rotas.
- Regressão backend acumulada: `php artisan test --filter=Dashboard` verde.
</verification>

<success_criteria>
- Gráfico abas Fat/Margem interativo (DASH-97-4).
- NPS ruim com link (DASH-97-5).
- Score 0–5 + breakdown pior→melhor + link (DASH-97-6).
- Novas empresas + estados reais + build (DASH-97-7).
</success_criteria>

<output>
Criar `.planning/phases/97-redesign-dashboard-mercado-livre/97-04-SUMMARY.md` ao concluir.
</output>
