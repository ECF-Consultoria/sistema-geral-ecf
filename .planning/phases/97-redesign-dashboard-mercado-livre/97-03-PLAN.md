---
phase: 97-redesign-dashboard-mercado-livre
plan: 03
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - resources/js/Pages/Dashboard/Admin.jsx
  - resources/js/Components/Dashboard/FiltrosDashboard.jsx
autonomous: true
requirements: [DASH-97-1, DASH-97-2, DASH-97-3]
must_haves:
  truths:
    - "Os filtros usam rascunho→Aplicar: mudar um select não recarrega; só o botão Aplicar aplica o recorte"
    - "O botão Aplicar destaca (amarelo) quando há mudança pendente vs. o recorte ativo"
    - "Chips dos filtros ativos aparecem abaixo, removíveis individualmente, com Limpar tudo"
    - "Filtrar em /dashboard/mercadolivre preserva marketplace='meli' (não cai na /dashboard genérica)"
    - "Os 4 KPIs mostram valor + variação vs. período anterior + link para a área completa"
  artifacts:
    - path: "resources/js/Components/Dashboard/FiltrosDashboard.jsx"
      provides: "Painel colapsável rascunho→aplicar + chips"
      min_lines: 40
    - path: "resources/js/Pages/Dashboard/Admin.jsx"
      provides: "applyFilter corrigido (rota corrente + marketplace) e KPIs com delta/link"
      contains: "dashboard_route_name"
  key_links:
    - from: "Admin.jsx applyFilter"
      to: "Ziggy route(dashboard_route_name)"
      via: "navegação preservando marketplace"
      pattern: "route\\(.*route"
    - from: "KPIs"
      to: "áreas completas"
      via: "Link Ziggy (companies.index, nps.index, performance.index)"
      pattern: "route\\("
---

<objective>
Refatorar a barra de filtros do `Dashboard/Admin.jsx` para o padrão do mockup — painel COLAPSÁVEL, RASCUNHO→APLICAR, CHIPS removíveis, "Limpar tudo", estado vazio — CORRIGIR o bug do marketplace (o `applyFilter` hoje usa `route('dashboard')` hardcoded e perde o recorte ML), e transformar os 4 KPIs em cartões com valor + variação vs. período anterior + link para a área completa. Sem regredir a `/dashboard` genérica (mesmo componente, D2).

Purpose: Resolve a reclamação central (filtros que não propagam / recorte que some) e entrega DASH-97-1 (filtros), DASH-97-2 (fix marketplace + não regressão) e DASH-97-3 (KPIs com delta/link) no frontend.
Output: `FiltrosDashboard.jsx` novo + `Admin.jsx` com `applyFilter` corrigido e KPIs enriquecidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md
@resources/js/Pages/Dashboard/Admin.jsx

<interfaces>
<!-- Props novas vindas do 97-01: stats.total_revenue_delta_pct, stats.avg_margin_delta_pp,
     stats.novas_empresas_count, avg_margin (ponderado), margin_chart[], filters.marketplace,
     dashboard_route_name ('mercadolivre.dashboard' | 'dashboard'). -->
<!-- Props existentes: filters{company_id,consultor_id,estrategista_id,group_id,marketplace},
     period, companies_list[], grupos_list[], analistas[], estrategistas[], combinacoes[],
     stats{avg_nps, total_companies, total_revenue, ...}, adman_last_sync{iso,label}. -->
<!-- applyFilter atual (~124) é o BUG: router.get(route('dashboard'), {...filters, period, [key]:value}). -->
<!-- ECFSelect local (~25), KpiCard local (~56), roleLabel/cross-filter analistasFiltrados/estrategistasFiltrados (~167-179). -->
<!-- Rotas Ziggy p/ links: companies.index, nps.index, performance.index. -->
<!-- Pitfall Rollup: computar cores/flags DENTRO do callback de .map() (nunca var de escopo externo). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FiltrosDashboard (rascunho→aplicar, chips, colapsável) + fix da navegação marketplace</name>
  <files>resources/js/Components/Dashboard/FiltrosDashboard.jsx, resources/js/Pages/Dashboard/Admin.jsx</files>
  <read_first>
    - resources/js/Pages/Dashboard/Admin.jsx (applyFilter ~124-130; barra de filtros ~293-325; ECFSelect ~25; cross-filter ~167-179)
    - .planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md (§Filtros; Riscos §1 e §3)
  </read_first>
  <action>
    Criar `FiltrosDashboard.jsx` (novo componente) implementando o padrão do mockup: painel COLAPSÁVEL (botão "Filtros" com contador de chips ativos); estado local `draft` (Período 7/30/60/90 default 30, Empresa, Grupo, Estrategista, Analista) que só é promovido a `applied` ao clicar "Aplicar"; botão "Aplicar" com destaque amarelo (`ecf-yellow`) quando `draft` diverge do recorte ativo; CHIPS abaixo dos filtros ativos, cada um removível (×) + "Limpar tudo"; estado vazio "Nenhum — mostrando todo o setor (últimos 30 dias)". Preservar o cross-filter analista↔estrategista via `combinacoes` (mover a lógica `analistasFiltrados`/`estrategistasFiltrados` para o componente ou receber por prop). Reusar `ECFSelect` e tokens `ecf-*`/`cn()`; comentários pt-BR; sem jargão sem explicação. **CORRIGIR o applyFilter em Admin.jsx**: ao aplicar, navegar via `router.get(route(dashboard_route_name || 'dashboard'), { ...appliedFilters, period, marketplace: filters.marketplace || undefined }, { preserveState:true, preserveScroll:true })` — NUNCA `route('dashboard')` fixo. Isso preserva o recorte `meli` e o active-state do menu em `/dashboard/mercadolivre` e mantém a `/dashboard` genérica funcionando (marketplace undefined). Remover a barra de filtros antiga (~293-325) substituindo por `<FiltrosDashboard .../>`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` sai com código 0.
    - `grep -n "route('dashboard')" resources/js/Pages/Dashboard/Admin.jsx` NÃO retorna o applyFilter hardcoded (a navegação usa `dashboard_route_name` + `marketplace`).
    - `FiltrosDashboard.jsx` existe e implementa draft→apply + chips + colapsável.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 4 KPIs com valor + delta vs. período anterior + link para a área completa</name>
  <files>resources/js/Pages/Dashboard/Admin.jsx</files>
  <read_first>
    - resources/js/Pages/Dashboard/Admin.jsx (KpiCard ~56-82; grid de KPIs modo normal; formatCurrency/formatPercent/cn de @/lib/utils)
    - .planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md (§"4 KPIs")
  </read_first>
  <action>
    Estender `KpiCard` (ou compor) para exibir: label, `link↗` (ícone) para a área completa, valor, delta e sub. Renderizar os 4 KPIs do CONTEXT: (1) Faturamento total → `stats.total_revenue` + delta `stats.total_revenue_delta_pct` (verde/vermelho por sinal; "sem base" quando null) + link Ziggy para Performance por empresa (`companies.index` ou seção da própria dash); (2) Margem contrib. média → `stats.avg_margin` (já ponderado) + delta em pp `stats.avg_margin_delta_pp` + link relatório de margem; (3) NPS médio → `stats.avg_nps` + sub "N respostas · M ruins" (M = `nps_ruins.length` quando disponível, senão `nps_distribution.negativas`) + link `nps.index`; (4) Empresas ativas → `stats.total_companies` + sub "+{stats.novas_empresas_count} novas no mês" + link `companies.index`. Cores do delta e sinais computados DENTRO do render de cada card / callback do `.map()` (pitfall Rollup). Tokens `ecf-*`, `cn()`, comentários pt-BR, sem jargão. Não remover o modo TV existente (deve continuar montando).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` sai com código 0.
    - Os 4 KPIs renderizam valor + delta (faturamento %, margem pp) + link Ziggy + sub (respostas/ruins, novas no mês).
    - Deltas com cor por sinal e tratamento de "sem base" (null).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin.jsx → navegação Inertia | params de filtro (incl. marketplace) enviados de volta ao controller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03-01 | Tampering | marketplace na navegação | mitigate | valor vem do payload (`filters.marketplace`) e é re-validado server-side (whitelist) — o front não inventa marketplace |
| T-97-03-02 | Information Disclosure | render dos KPIs | accept | dados já filtrados/autorizados pelo backend; front só formata |
| T-97-03-SC | Tampering | dependências npm | accept | nenhum pacote novo — reusa recharts/lucide/@radix já presentes |
</threat_model>

<verification>
- `npm run build` exit 0.
- `grep -n "route('dashboard')" resources/js/Pages/Dashboard/Admin.jsx` sem o applyFilter hardcoded.
- Regressão manual leve: `/dashboard` genérica ainda carrega (marketplace undefined) — validado no checkpoint visual do 97-04.
</verification>

<success_criteria>
- Filtros rascunho→aplicar + chips + colapsável (DASH-97-1).
- Bug do marketplace corrigido + /dashboard não regride (DASH-97-2).
- 4 KPIs com delta + link (DASH-97-3).
</success_criteria>

<output>
Criar `.planning/phases/97-redesign-dashboard-mercado-livre/97-03-SUMMARY.md` ao concluir.
</output>
