---
phase: 97-redesign-dashboard-mercado-livre
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
files_modified:
  - app/Http/Controllers/DashboardController.php
  - tests/Feature/Dashboard/DashboardWidgetsRecorteTest.php
autonomous: true
requirements: [DASH-97-1, DASH-97-5, DASH-97-6, DASH-97-7]
must_haves:
  truths:
    - "O Score da equipe passa a listar apenas os analistas/estrategistas das empresas do recorte (antes ignorava os filtros)"
    - "As empresas pendentes de NPS refletem o recorte (via NpsPendingService::forCompanies), não mais todas as empresas"
    - "A lista de NPS ruim traz respostas com nota baixa do recorte, excluindo as invalidadas (Fase 96)"
    - "As empresas novas do mês vêm do início do contrato ativo (D3), com dados para os cards"
  artifacts:
    - path: "app/Http/Controllers/DashboardController.php"
      provides: "performance_equipe filtrado por recorte, nps_pendentes via forCompanies, nps_ruins com scopeValida, novas_empresas detalhado"
      contains: "nps_ruins"
    - path: "tests/Feature/Dashboard/DashboardWidgetsRecorteTest.php"
      provides: "Cobertura de recorte nos widgets antes cegos + exclusão de invalidadas"
      contains: "class DashboardWidgetsRecorteTest"
  key_links:
    - from: "adminDashboard"
      to: "NpsPendingService::forCompanies"
      via: "recorte $companies"
      pattern: "forCompanies\\("
    - from: "adminDashboard"
      to: "NpsResponse scopeValida (Fase 96)"
      via: "query de respostas ruins"
      pattern: "valida\\(\\)"
    - from: "adminDashboard"
      to: "DesempenhoScoreService::computeCached"
      via: "membros do recorte"
      pattern: "computeCached"
---

<objective>
Fazer os widgets do redesign RESPEITAREM o recorte aplicado (reclamação central do usuário) e alimentar os cards novos: (1) "Score da equipe" listando só os analistas/estrategistas das empresas filtradas, usando a nota oficial 0–5 do `DesempenhoScoreService` (D1) com breakdown; (2) `nps_pendentes` via `NpsPendingService::forCompanies($companies)` (recorte) em vez de `forCarteira` (todas); (3) "NPS ruim" — respostas de nota baixa do recorte, EXCLUINDO invalidadas (`scopeValida`, Fase 96); (4) "Novas empresas no mês" — empresas com contrato ativo iniciado no mês (D3), com os campos dos cards.

Purpose: Fecha DASH-97-1 (todos os widgets no recorte, incl. os 2 hoje cegos), DASH-97-5 (NPS ruim), DASH-97-6 (Score oficial) e DASH-97-7 (novas empresas) no lado backend.
Output: props `performance_equipe` (recortado), `nps_pendentes` (recortado), `nps_ruins`, `novas_empresas`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md
@app/Http/Controllers/DashboardController.php
@app/Services/DesempenhoScoreService.php
@app/Services/Nps/NpsPendingService.php
@app/Models/NpsResponse.php

<interfaces>
<!-- DesempenhoScoreService::computeCached($user, $mes) → array com:
     nota_final (?float 0-5), faixa_bonus (?string), sem_carteira (bool),
     componentes{nps_medio, var_faturamento_pct, var_margem_pct, absenteismo_pct},
     pontos_componentes{nps, faturamento, margem}. ATENCAO: cache do DesempenhoScoreService (chave versionada, hoje v5 — isolar via Cache::flush no setUp, nao hardcodar versao)
     → nos testes usar compute() puro OU limpar cache. -->
<!-- NpsPendingService::forCompanies($companies, ?Carbon) → array [{company_id,name,template_id,template_nome,month_reference,dias_atraso}] JÁ recebe o recorte. -->
<!-- NpsPendingService::forCarteira($user) → HOJE usado (~841): admin=TODAS as empresas → precisa trocar por forCompanies($companies). -->
<!-- NpsResponse: scopeValida() = whereNull('invalidated_at') (Fase 96); campos score_empresa (int 1-5), comment, invalidated_at. -->
<!-- NpsSurvey: ->principal(), ->response (hasOne), company_id, status, completed_at. -->
<!-- $companies (recorte) e $companies->pluck('id') já montados no início do adminDashboard. -->
<!-- company_users pivot: role 'consultor' = Analista, role 'estrategista' = Estrategista (vínculo empresa↔profissional). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Score da equipe e NPS pendentes passam a respeitar o recorte</name>
  <files>app/Http/Controllers/DashboardController.php, tests/Feature/Dashboard/DashboardWidgetsRecorteTest.php</files>
  <read_first>
    - app/Http/Controllers/DashboardController.php (bloco `$perfMembros` ~759-817 e `$npsPentendes = ...forCarteira` ~838-841)
    - app/Services/DesempenhoScoreService.php (retorno de compute ~373-401: nota_final, componentes, pontos_componentes)
    - app/Services/Nps/NpsPendingService.php (forCompanies ~180 vs forCarteira ~155)
    - .planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md (D1 Score; Riscos §2 widgets cegos)
  </read_first>
  <behavior>
    - Com filtro company_id=X aplicado, performance_equipe contém APENAS os profissionais vinculados às empresas do recorte (analista/estrategista de X), não o setor inteiro.
    - nps_pendentes com filtro aplicado retorna só pendências das empresas do recorte (via forCompanies($companies)).
    - Sem filtro, performance_equipe e nps_pendentes cobrem todo o universo Performance (não regride o comportamento amplo).
    - Cada item de performance_equipe expõe nota_final (0-5) + breakdown (carteira/nps/margem/tacos) para o card.
  </behavior>
  <action>
    Restringir `$perfMembrosQuery` aos profissionais vinculados a `$companies` (recorte): derivar os `user_id` de `company_users` cujo `company_id` está em `$companies->pluck('id')` e cujo `role` ∈ {'consultor','estrategista'}, e usar esse conjunto como filtro adicional do query de membros (mantendo o recorte de líder de setor já existente ~776-783). Assim os filtros company/grupo/analista/estrategista/marketplace propagam para o Score da equipe. Manter `computeCached` (não regredir a performance — audit performance-lentidao). Além dos campos legados já enviados, incluir no item o breakdown para o card: `componentes` (ou `pontos_componentes`) mapeado em `carteira` (empresas_carteira), `nps`, `margem`, `tacos` — nomear as chaves que o card (97-04) vai ler; documentar as chaves escolhidas em comentário pt-BR. Trocar `$this->npsPending->forCarteira($request->user())` por `$this->npsPending->forCompanies($companies)` (Risco §2). Criar a suíte asserindo: (1) com company_id filtrado, `performance_equipe` só tem os responsáveis daquela empresa; (2) `nps_pendentes` idem. **Nos testes, isolar o cache do DesempenhoScoreService** com `Cache::flush()` no setUp (agnóstico de versão — NÃO hardcodar a string da chave; a versão já mudou para `v5` e há `DesempenhoScoreService::cacheKey($userId, $mes)` público se precisar de forget dirigido) para não ler nota de outra rodada.
  </action>
  <verify>
    <automated>php artisan test --filter=DashboardWidgetsRecorteTest</automated>
  </verify>
  <acceptance_criteria>
    - Teste verde provando que Score da equipe e NPS pendentes seguem o recorte.
    - `grep -n "forCompanies" app/Http/Controllers/DashboardController.php` retorna o novo call-site e não resta `forCarteira` no adminDashboard.
    - Item de `performance_equipe` inclui `nota_final` + chaves de breakdown (carteira/nps/margem/tacos).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lista de NPS ruim (scopeValida) e cards de novas empresas do mês</name>
  <files>app/Http/Controllers/DashboardController.php, tests/Feature/Dashboard/DashboardWidgetsRecorteTest.php</files>
  <read_first>
    - app/Http/Controllers/DashboardController.php (bloco NPS ~539-611: $npsResponses com ->principal()->valida(), $notaDe dual-path)
    - app/Models/NpsResponse.php (scopeValida ~118; score_empresa, comment, invalidated_at)
    - .planning/phases/97-redesign-dashboard-mercado-livre/97-CONTEXT.md (widget "NPS ruim"; D3 novas empresas; Riscos §4 Fase 96)
  </read_first>
  <behavior>
    - nps_ruins lista respostas do recorte com nota da dimensão empresa BAIXA, excluindo invalidated_at != null (scopeValida).
    - Cada item traz: company_id + nome, data da resposta, nota, comentário, analista, estrategista, e ids para o link "Abrir NPS completo".
    - Uma resposta invalidada pelo admin NÃO aparece em nps_ruins nem infla o avg_nps.
    - novas_empresas traz cards das empresas com contrato ativo iniciado no mês (D3): nome, grupo, status, faturamento parcial, tacos, responsáveis, id para companies.show.
  </behavior>
  <action>
    Montar `nps_ruins` a partir de `NpsSurvey`/`NpsResponse` do recorte (`whereIn company_id` em `$companies->pluck('id')`), reusando o mesmo padrão `->principal()` + relação `response` com `scopeValida()` (`->valida()`) já presente no bloco NPS, e o `$notaDe` (calculator dimensão 'empresa' para surveys v15, fallback `score_empresa`). **Definição de "nota ruim":** usar a MESMA régua do bucket `negativas` já existente — nota da empresa ≤ 3 na escala 1–5 do sistema. O "≤5 / ≥6" do mockup é herança da escala 0–10 e NÃO se aplica aqui (usar ≤3 evita marcar todas as respostas como ruins); documentar isso em comentário pt-BR e deixar o empty-state neutro ("Nenhuma nota baixa no período") sem jargão. Cada item: `company_id`, `company_name`, `data` (completed_at formatado), `nota`, `comment`, `analista` (consultor->first()?->name), `estrategista` (estrategista->first()?->name). Montar `novas_empresas`: as empresas do recorte cujo contrato ativo de setor Performance iniciou no mês corrente (mesma regra D3 do 97-01, agora com detalhe) — campos: `id`, `name`, `grupo` (company group name), `status` (derivar rótulo Atenção/Ramp-up/Saudável a partir de sinal já disponível, ex.: tacos/margem — decisão do executor, documentar), `faturamento_parcial` (revenueByCompany do recorte), `tacos`, `analista`, `estrategista`. Adicionar ambas as chaves ao `Inertia::render`. Estender a suíte: (1) resposta com `invalidated_at` NÃO aparece em `nps_ruins`; (2) resposta nota ≤3 válida aparece; (3) empresa com contrato do mês entra em `novas_empresas`, empresa antiga não. Comentários pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=DashboardWidgetsRecorteTest</automated>
  </verify>
  <acceptance_criteria>
    - Teste verde: invalidada excluída de `nps_ruins`; nota ≤3 válida incluída; `novas_empresas` por início de contrato.
    - `nps_ruins` traz comentário + analista + estrategista + ids de link.
    - `grep -n "valida()" app/Http/Controllers/DashboardController.php` cobre a query de nps_ruins (Fase 96).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador admin/líder → adminDashboard | recorte de filtros define quais empresas/pessoas são expostas |
| NpsResponse (comentários de cliente) → payload | dado sensível (texto livre do cliente) renderizado no card NPS ruim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02-01 | Information Disclosure | comentários de clientes (nps_ruins) | mitigate | só montados/servidos dentro de adminDashboard, protegido por role admin / líder com `core.dashboard`; nunca em rota pública |
| T-97-02-02 | Information Disclosure | respostas invalidadas | mitigate | `scopeValida()`/`->valida()` (Fase 96) exclui `invalidated_at` de nps_ruins e do avg — teste garante |
| T-97-02-03 | Information Disclosure | pessoas fora do recorte no Score | mitigate | membros derivados de `company_users` do recorte + restrição de líder por setor preservada |
| T-97-02-SC | Tampering | dependências | accept | nenhum pacote novo instalado |
</threat_model>

<verification>
- `php artisan test --filter=DashboardWidgetsRecorteTest` verde.
- `php artisan test --filter=Dashboard` sem regressão nas suítes existentes.
- Cache do DesempenhoScoreService isolado nos testes via Cache::flush() no setUp (agnostico de versao — chave ja e v5) para determinismo.
</verification>

<success_criteria>
- Score da equipe e NPS pendentes respeitam o recorte (DASH-97-1).
- NPS ruim exclui invalidadas (DASH-97-5 backend, Fase 96).
- Score usa nota_final 0–5 com breakdown (DASH-97-6 backend).
- Novas empresas por início de contrato (DASH-97-7 backend).
</success_criteria>

<output>
Criar `.planning/phases/97-redesign-dashboard-mercado-livre/97-02-SUMMARY.md` ao concluir.
</output>
