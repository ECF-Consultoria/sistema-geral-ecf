---
phase: 82-planilha-excel-like-na-grade-de-anuncio-em-massa-glide-data-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - resources/views/app.blade.php
  - resources/js/Pages/Mlb/gradeMassaUtils.js
  - resources/js/Pages/Mlb/AnunciarMassa.jsx
autonomous: true
requirements: [SHEET2-06, SHEET2-07]

must_haves:
  truths:
    - "As 5 dependências do glide-data-grid estão instaladas e `npm run build` continua verde"
    - "Existe um `<div id=\"portal\">` como ÚLTIMO filho de `<body>` — sem ele o overlay editor da lib falha em silêncio"
    - "As funções puras de validação/coerção vivem num módulo importável por página E por componente de grade, sem ciclo de import"
    - "Nenhuma regra de validação mudou: `errosLocaisLinha` produz exatamente a mesma saída de antes do move"
  artifacts:
    - path: "resources/js/Pages/Mlb/gradeMassaUtils.js"
      provides: "Helpers puros compartilhados entre AnunciarMassa.jsx e a futura GradeAnuncioGlide.jsx"
      exports: ["gerarEan13", "nomeCurto", "semAcento", "normalizarTipoAnuncio", "parseDimensoes", "casarValueList", "errosLocaisLinha", "linhaPublicavel", "linhaVazia", "ATTR_MARCA", "ATTR_MODELO"]
      min_lines: 90
    - path: "resources/views/app.blade.php"
      provides: "Nó DOM do portal exigido pelo overlay editor do glide-data-grid"
      contains: "id=\"portal\""
    - path: "package.json"
      provides: "Dependências @glideapps/glide-data-grid + peers"
      contains: "@glideapps/glide-data-grid"
  key_links:
    - from: "resources/js/Pages/Mlb/AnunciarMassa.jsx"
      to: "resources/js/Pages/Mlb/gradeMassaUtils.js"
      via: "import ESM dos helpers puros"
      pattern: "from '@/Pages/Mlb/gradeMassaUtils'"
---

<objective>
Preparar o terreno para a grade em canvas: instalar as dependências verificadas pelo research,
criar o nó DOM do portal (sem o qual NENHUM editor de célula abre) e extrair as funções puras de
validação/coerção de `AnunciarMassa.jsx` para um módulo próprio — para que a nova grade possa
importá-las sem ciclo de import e sem duplicar regra de negócio.

Este plano NÃO renderiza grade nenhuma. É fundação. A grade nasce no Plan 02.

Purpose: o portal é o gotcha nº1 verificado no código-fonte da lib (falha SILENCIOSA); e a extração
de helpers é a única forma de a nova grade reusar `errosLocaisLinha`/`casarValueList` sem que
`GradeAnuncioGlide.jsx` importe de `AnunciarMassa.jsx` que importa de `GradeAnuncioGlide.jsx`
(ciclo). Fazer isso ANTES de escrever a grade evita retrabalho.
Output: 5 pacotes instalados, `<div id="portal">` no layout Blade, `gradeMassaUtils.js` novo,
`AnunciarMassa.jsx` importando dele em vez de declarar as funções localmente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/82-planilha-excel-like-na-grade-de-anuncio-em-massa-glide-data-/82-RESEARCH.md

@resources/js/Pages/Mlb/AnunciarMassa.jsx
@resources/views/app.blade.php
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar as dependências do glide-data-grid e medir o impacto no bundle</name>
  <files>package.json, package-lock.json</files>
  <action>
    ANTES de instalar qualquer coisa: rodar `npm run build` e anotar o tamanho reportado pelo Vite
    para o chunk `AnunciarMassa-*.js` (baseline). Registrar o número no SUMMARY.

    Instalar como `dependencies` (não devDependencies), exatamente os 5 pacotes aprovados na
    §Package Legitimacy Audit do 82-RESEARCH.md — todos com veredicto slopcheck `[OK]`, nenhum
    `[ASSUMED]`/`[SUS]`, portanto SEM checkpoint de legitimidade:
    `@glideapps/glide-data-grid`, `@glideapps/glide-data-grid-cells`, `lodash`, `marked`,
    `react-responsive-carousel`.

    Fixar a versão estável `6.0.3` para os dois pacotes `@glideapps/*` — NÃO usar nenhum
    `6.0.4-alphaXX` (pre-release, explicitamente vetado pelo research). Os 3 peers (`lodash`,
    `marked`, `react-responsive-carousel`) são exigências de peer dependency da lib e entram
    mesmo sem uso direto no nosso código — não removê-los por "parecerem não usados".

    Depois de instalar: rodar `npm run build` de novo e anotar o novo tamanho do chunk
    `AnunciarMassa-*.js`. Registrar baseline → depois → delta no SUMMARY (isso fecha a incógnita
    A3 do Assumptions Log do research). A página é lazy-loaded via `import.meta.glob` sem `eager`,
    então o custo afeta só quem abre esta página — se o delta for grande, isso é informação para o
    checkpoint final, NÃO motivo para reverter a decisão travada da lib.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -30 && node -e "const p=require('./package.json'); const d=p.dependencies||{}; const req=['@glideapps/glide-data-grid','@glideapps/glide-data-grid-cells','lodash','marked','react-responsive-carousel']; const falta=req.filter(k=>!d[k]); if(falta.length){console.error('FALTA em dependencies:',falta.join(', ')); process.exit(1);} if(/alpha/.test(d['@glideapps/glide-data-grid'])){console.error('pre-release proibido'); process.exit(1);} console.log('deps OK', JSON.stringify(req.map(k=>k+'@'+d[k])));"</automated>
  </verify>
  <done>Os 5 pacotes estão em `dependencies` do package.json em versão estável, `package-lock.json` atualizado, `npm run build` verde, e o delta de tamanho do chunk AnunciarMassa está registrado no SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar o nó DOM do portal exigido pelo overlay editor</name>
  <files>resources/views/app.blade.php</files>
  <action>
    Adicionar `<div id="portal"></div>` como ÚLTIMO filho de `<body>`, imediatamente depois de
    `@inertia`. Esta é a mitigação do Pitfall 1 do 82-RESEARCH.md, verificado lendo o
    código-fonte da lib (`data-grid-overlay-editor.tsx` faz `document.getElementById("portal")` e,
    se não achar, loga erro no console e retorna SEM renderizar — nenhuma edição de célula abre,
    nenhum dropdown abre, e a UI não dá nenhum sinal do problema).

    É a única mudança de Blade da fase inteira. `app.blade.php` é o layout único de todas as
    páginas Inertia deste projeto — a div fica presente em toda página, o que é inofensivo (é um
    container vazio) e é exatamente o que a lib documenta.

    Não mexer em mais nada do arquivo: não tocar no bloco de tema claro/escuro, nas fontes, no
    `@routes`/`@viteReactRefresh`/`@vite`, nem no `@inertiaHead`.
  </action>
  <verify>
    <automated>grep -c 'id="portal"' resources/views/app.blade.php | grep -qx '1' && node -e "const s=require('fs').readFileSync('resources/views/app.blade.php','utf8'); const m=s.match(/<body[^>]*>([\s\S]*?)<\/body>/); if(!m){console.error('body nao encontrado');process.exit(1);} const corpo=m[1].trim(); if(!/id=\"portal\"[^>]*>\s*<\/div>$/.test(corpo)){console.error('portal nao e o ultimo filho de <body>:', JSON.stringify(corpo.slice(-80))); process.exit(1);} console.log('portal e o ultimo filho de <body> OK');"</automated>
  </verify>
  <done>`<div id="portal"></div>` é o último elemento dentro de `<body>` em `resources/views/app.blade.php` e o restante do arquivo está inalterado.</done>
</task>

<task type="auto">
  <name>Task 3: Extrair os helpers puros para gradeMassaUtils.js (move puro, zero mudança de comportamento)</name>
  <files>resources/js/Pages/Mlb/gradeMassaUtils.js, resources/js/Pages/Mlb/AnunciarMassa.jsx</files>
  <action>
    Criar `resources/js/Pages/Mlb/gradeMassaUtils.js` e MOVER para lá, byte a byte (mesma
    implementação, mesmos comentários pt-BR), estes símbolos hoje declarados em
    `AnunciarMassa.jsx`, todos com `export`:
    `gerarEan13` (L18), `nomeCurto` (L49), `ATTR_MARCA`/`ATTR_MODELO` (L59-60),
    `errosLocaisLinha` (L69), `linhaPublicavel` (L84), `semAcento` (L119),
    `normalizarTipoAnuncio` (L123), `parseDimensoes` (L132), `casarValueList` (L145),
    `linhaVazia` (L1273).

    REGRA DURA (SHEET2-07): é um MOVE, não um refactor. Não renomear, não "melhorar", não trocar
    assinatura, não mudar ordem de checagem, não alterar os textos de comentário. Qualquer
    alteração de comportamento aqui é uma regressão de validação silenciosa que só apareceria em
    produção, com anúncio inválido indo para o Mercado Livre.

    Em `AnunciarMassa.jsx`: apagar as declarações locais movidas e importar do módulo novo usando o
    alias `@` (`import { ... } from '@/Pages/Mlb/gradeMassaUtils';`), na posição de import que a
    convenção do arquivo já usa (framework → Inertia → lucide → locais). Conferir que TODOS os
    pontos de uso continuam resolvendo: `errosLocaisLinha` é usada em `LinhaGrade` e em
    `resumoLote`; `linhaPublicavel` em `resumoLote`; `casarValueList`/`normalizarTipoAnuncio`/
    `parseDimensoes` dentro de `colarNaGrade`; `gerarEan13` na célula GTIN; `linhaVazia` em
    `linhaDeRascunho`, `adicionarLinha`, `puxarProduto`, `colarNaGrade` e no efeito de init;
    `nomeCurto` nas cápsulas de aba e em `Grade`.

    O que NÃO move (continua em AnunciarMassa.jsx): `iniciais`, `SEM_CATEGORIA`,
    `montarPayloadLinha`, `linhaDeRascunho`, `colunasColaveis`, `colarNaGrade`, e todos os
    componentes JSX. `colunasColaveis`/`colarNaGrade` só serão removidos no Plan 03, quando o
    paste nativo da lib estiver no lugar — não antecipar.

    Motivo do arquivo separado (e não manter tudo em `AnunciarMassa.jsx` como a convenção do
    CLAUDE.md sugere para sub-componentes de página): o `GradeAnuncioGlide.jsx` do Plan 02 precisa
    dessas funções e vai ser importado por `AnunciarMassa.jsx` — declarar os helpers na página
    criaria um ciclo de import página↔grade. `Pages/Mlb/ModoAnuncioTabs.jsx` já é o precedente de
    módulo não-página vivendo em `Pages/Mlb/`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && node -e "const u=require('fs').readFileSync('resources/js/Pages/Mlb/gradeMassaUtils.js','utf8').split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n'); const req=['gerarEan13','nomeCurto','semAcento','normalizarTipoAnuncio','parseDimensoes','casarValueList','errosLocaisLinha','linhaPublicavel','linhaVazia','ATTR_MARCA','ATTR_MODELO']; const falta=req.filter(n=>!new RegExp('export\\\\s+(const|function)\\\\s+'+n+'\\\\b').test(u)); if(falta.length){console.error('nao exportado:',falta.join(', '));process.exit(1);} const a=require('fs').readFileSync('resources/js/Pages/Mlb/AnunciarMassa.jsx','utf8').split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n'); if(!/from '@\/Pages\/Mlb\/gradeMassaUtils'/.test(a)){console.error('AnunciarMassa nao importa de gradeMassaUtils');process.exit(1);} const dup=req.filter(n=>new RegExp('^(const|function)\\\\s+'+n+'\\\\b','m').test(a)); if(dup.length){console.error('declaracao local duplicada em AnunciarMassa:',dup.join(', '));process.exit(1);} console.log('extracao OK');"</automated>
    <automated>C:/php/php.exe artisan test --filter=Phase82</automated>
  </verify>
  <done>`gradeMassaUtils.js` exporta os 11 símbolos com implementação idêntica à anterior; `AnunciarMassa.jsx` não declara mais nenhum deles e importa todos do módulo novo; `npm run build` verde; `php artisan test --filter=Phase82` verde (9 testes).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry npm → node_modules do projeto | Código de terceiro entra na árvore de build e roda no browser do publicador |
| navegador → `<div id="portal">` | Nó DOM global compartilhado por todas as páginas Inertia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-SC | Tampering | `npm install` dos 5 pacotes | mitigate | slopcheck rodado no research com veredicto `[OK]` para os 5 (§Package Legitimacy Audit do 82-RESEARCH.md); nenhum `[ASSUMED]`/`[SUS]` → sem checkpoint bloqueante; versão estável fixada em 6.0.3, pre-releases `6.0.4-alphaXX` vetadas; nenhum `postinstall` detectado |
| T-82-01 | Tampering | `<div id="portal">` em `app.blade.php` | accept | Container vazio, sem conteúdo controlado por usuário, sem `innerHTML`; só recebe React portals da própria lib. Presente em todas as páginas, mas inerte quando a grade não está montada |
| T-82-02 | Tampering | move de `errosLocaisLinha` para outro módulo | mitigate | Move literal (sem refactor) + `php artisan test --filter=Phase82` como gate; a validação bloqueante local é a barreira antes de qualquer POST de rascunho |
</threat_model>

<verification>
- `npm run build` verde antes e depois da instalação; delta de chunk registrado.
- `php artisan test --filter=Phase82` verde (backend intocado — 9 testes de `tests/Feature/Phase82/GradeMassaTest.php`).
- `<div id="portal"></div>` é comprovadamente o último filho de `<body>`.
- A grade atual (`<table>` HTML) continua funcionando exatamente como antes — este plano não a toca.
</verification>

<success_criteria>
- 5 pacotes instalados em `dependencies`, versão estável, lockfile atualizado.
- Portal DOM presente e posicionado corretamente.
- `gradeMassaUtils.js` existe, exporta os 11 símbolos, e `AnunciarMassa.jsx` consome dele sem duplicar declaração.
- Zero mudança de comportamento observável na página (a grade antiga segue no ar, idêntica).
</success_criteria>

<output>
Create `.planning/phases/82-planilha-excel-like-na-grade-de-anuncio-em-massa-glide-data-/82-01-SUMMARY.md` when done
</output>
