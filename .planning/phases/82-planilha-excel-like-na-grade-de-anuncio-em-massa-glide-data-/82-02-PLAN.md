---
phase: 82-planilha-excel-like-na-grade-de-anuncio-em-massa-glide-data-
plan: 02
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - resources/js/Pages/Mlb/GradeAnuncioGlide.jsx
  - resources/js/Pages/Mlb/AnunciarMassa.jsx
autonomous: true
requirements: [SHEET2-07, SHEET2-08]

must_haves:
  truths:
    - "A aba ativa renderiza num canvas com as MESMAS colunas de hoje: 10 base + só os obrigatórios da categoria ativa"
    - "As cores do canvas são as do sistema (ecf-bg/ecf-card/ecf-yellow) — a grade não parece um corpo estranho na página"
    - "Editar uma célula grava no estado da aba e dispara o autosave com debounce exatamente como antes"
    - "Editar uma célula que veio do cliente muda a origem daquele campo para 'publicador'"
    - "Colar '10x20x30' em Altura preenche Altura, Largura e Comprimento (paridade com a coluna A×L×C de hoje)"
    - "A última linha da grade oferece '+ Adicionar linha' e criar linha continua funcionando"
  artifacts:
    - path: "resources/js/Pages/Mlb/GradeAnuncioGlide.jsx"
      provides: "DataEditor em canvas: tema ecf-*, colunas dinâmicas, getCellContent, onCellsEdited"
      exports: ["default"]
      min_lines: 200
  key_links:
    - from: "resources/js/Pages/Mlb/AnunciarMassa.jsx"
      to: "resources/js/Pages/Mlb/GradeAnuncioGlide.jsx"
      via: "render do componente no lugar do antigo <Grade>"
      pattern: "<GradeAnuncioGlide"
    - from: "resources/js/Pages/Mlb/GradeAnuncioGlide.jsx"
      to: "@glideapps/glide-data-grid"
      via: "import do DataEditor e do CSS obrigatório"
      pattern: "glide-data-grid/dist/index.css"
    - from: "resources/js/Pages/Mlb/GradeAnuncioGlide.jsx"
      to: "onEditarCelula (AnunciarMassa)"
      via: "onCellsEdited chama o callback de edição+autosave da página"
      pattern: "onCellsEdited"
---

<objective>
Trocar a `<table>` HTML por um `<DataEditor>` em canvas, preservando o modelo de dados e o ciclo de
vida da página. Este plano entrega a grade EDITÁVEL com o tema do sistema (SHEET2-08) e a mesma
estrutura de colunas de hoje — as capacidades de planilha (range, fill, paste, dropdown) chegam nos
Plans 03 e 04.

Purpose: é a virada de paradigma da fase. De "um `<input>` JSX por célula" para "a grade pergunta o
conteúdo célula a célula via `getCellContent` e devolve edições em lote via `onCellsEdited`". Tudo
o mais da página (abas, autosave, PublishBar, puxar produtos) fica INTOCADO — a migração é
cirúrgica por decisão travada.
Output: `GradeAnuncioGlide.jsx` novo, renderizado por `AnunciarMassa.jsx`; `Grade`/`LinhaGrade`/
`Cell`/`CellInput`/`Th`/`OrigemBadge` removidos.

## Estado intermediário conhecido (aceito)

Ao fim deste plano, comparado com hoje, faltam: paste do Excel, dropdown dos atributos `list`,
seleção de range/fill handle, badge de origem, contador do título, realce de erro local, botão de
gerar EAN-13 e remover linha. Todos entram nos Plans 03-06. NADA é publicado/deployado no meio da
fase — o gate de zero regressão (SHEET2-07) é medido no checkpoint do Plan 07, não por wave.
Por isso NÃO tente construir pontes temporárias (ex.: manter `onPaste={colarNaGrade}` num wrapper
`<div>` em volta do canvas) — é esforço jogado fora e comportamento não provado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/82-planilha-excel-like-na-grade-de-anuncio-em-massa-glide-data-/82-RESEARCH.md
@.planning/phases/82-planilha-excel-like-na-grade-de-anuncio-em-massa-glide-data-/82-01-SUMMARY.md

@resources/js/Pages/Mlb/AnunciarMassa.jsx
@resources/js/Pages/Mlb/gradeMassaUtils.js
@tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar GradeAnuncioGlide.jsx com o tema ecf-*, as colunas dinâmicas e getCellContent</name>
  <files>resources/js/Pages/Mlb/GradeAnuncioGlide.jsx</files>
  <action>
    Criar o componente `GradeAnuncioGlide` (export default) recebendo as props que o `<Grade>` atual
    recebe: `aba`, `empresa`, `onEditarCelula`, `onAdicionarLinha`, `onRemoverLinha`. Manter o padrão
    de comentários pt-BR do arquivo original (blocos `// ═══...═══` e `// ─── ... ───`).

    Imports obrigatórios: `DataEditor` e `GridCellKind` de `@glideapps/glide-data-grid`, e o CSS
    `@glideapps/glide-data-grid/dist/index.css` (sem ele o canvas renderiza quebrado — §Standard
    Stack do research). O CSS fica só neste componente, não em `app.jsx`.

    TEMA (SHEET2-08): objeto `temaEcf` no escopo do módulo (constante, fora do componente — não
    precisa de memo se não depende de nada). Usar o mapeamento da §Pattern 6 do 82-RESEARCH.md, que
    já traduz os hex reais de `tailwind.config.js` (`ecf.card` #0f1116 → `bgCell`, `ecf.card-2`
    #14161d → `bgHeader`/`bgCellMedium`, `ecf.yellow` #ffe600 → `accentColor`, `ecf.line`
    rgba(255,255,255,0.08) → `borderColor`, fonte Inter). Classes Tailwind NÃO alcançam o canvas —
    toda cor de célula passa por este objeto. Os valores exatos de opacidade/contraste são uma
    escolha de design a validar no checkpoint visual do Plan 07 (Assumption A4 do research).

    COLUNAS: constante `COLS_BASE` no escopo do módulo com os 10 campos base, nesta ordem exata
    (é a ordem visual de hoje e também a ordem de mapeamento do paste do Plan 03):
    `title` (Título, 240), `tier` (Tipo, 100), `price` (Preço, 100), `estoque` (Estoque, 90),
    `sku` (SKU, 120), `gtin` (GTIN, 140), `pesoG` (Peso g, 90), `alturaCm` (Altura cm, 90),
    `larguraCm` (Largura cm, 90), `comprimentoCm` (Comprim. cm, 100).

    Duas mudanças deliberadas de estrutura de coluna vs. hoje, ambas vindas do research — registrar
    as duas no SUMMARY para o checkpoint:
    (a) a coluna única "A×L×C cm" (que hoje tem 3 `<input>` dentro de um `<td>`) vira 3 colunas
        independentes (Altura/Largura/Comprimento) — canvas não aninha inputs numa célula, e 3
        colunas é o que torna fill handle e paste utilizáveis nesses campos;
    (b) a coluna "Foto" SAI. Hoje ela é um placeholder decorativo (um quadrado com "+" que não faz
        nada; o próprio comentário do código diz "upload real fora do escopo desta grade"). Não é
        uma capacidade perdida — é um enfeite que não sobrevive ao canvas.

    Colunas dinâmicas: `useMemo` sobre `aba.obrigatorios`, uma coluna por atributo, com
    `id: 'attr:' + o.id`, `title: o.name`, `width: 150`, e o objeto do atributo pendurado em `_attr`
    (o `getCellContent` usa `_attr.value_type`/`_attr.values` para decidir o tipo de célula). A lista
    final de colunas = `COLS_BASE` + dinâmicas, memoizada com dependência em `aba.obrigatorios`
    (SHEET-02: só os obrigatórios da categoria ATIVA, nunca a união de todas as abas).

    Cabeçalho em dois níveis: usar a prop `group` de `GridColumn` para reproduzir os grupos de hoje
    — `'Campos base'` nas 10 base e `'Ficha técnica · ' + nomeCurto(aba.caminho, aba.category_id)`
    nas dinâmicas. Marcar obrigatório no próprio `title` da coluna com sufixo ` *` em Título, Preço,
    Estoque e em todas as dinâmicas (hoje isso é o `<Th req>`). O Título mostra o limite:
    `Título (${aba.max_title_length}) *`.

    getCellContent (§Pattern 2 do research): `useCallback` com dependências MÍNIMAS —
    `[colunas, aba.linhas]`, NUNCA o objeto `aba` inteiro nem `abas` (Pitfall 3: referência instável
    faz a lib, que chama isso centenas de vezes por segundo no scroll, redesenhar tudo).
    Comportamento por coluna:
    - fora do range (linha/coluna inexistente) → `GridCellKind.Loading` com `allowOverlay: false`;
    - `tier` → `GridCellKind.Text`, `allowOverlay: false` (SOMENTE LEITURA neste plano), exibindo
      `Clássico` para `gold_special` e `Premium` para `gold_pro`. Vira dropdown editável no Plan 03;
      deixar como texto livre editável aqui permitiria gravar lixo em `listing_type_id`;
    - coluna de atributo com `_attr.value_type === 'list'` e `_attr.values` não vazio →
      `GridCellKind.Text` com `allowOverlay: false` (mesma razão; vira `DropdownCell` no Plan 03);
    - `price`, `estoque`, `pesoG`, `alturaCm`, `larguraCm`, `comprimentoCm`, `gtin` →
      `GridCellKind.Text` editável com `contentAlign: 'right'` (hoje esses inputs são
      `text-right font-mono tabular-nums`);
    - demais (`title`, `sku`, atributos texto) → `GridCellKind.Text` editável.
    Manter os valores como STRING, como o estado da linha já faz hoje (`linhaVazia` cria tudo `''`)
    — NÃO usar `GridCellKind.Number`. `montarPayloadLinha` já faz `Number(l.price)` na saída;
    trocar o tipo aqui mudaria o payload e é regressão (SHEET2-07).
    Ler o valor via `linha.attrs[coluna._attr.id]` quando o id começa com `attr:`, senão
    `linha[coluna.id]`; sempre `String(valor ?? '')` em `data` e `displayData`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && node -e "const s=require('fs').readFileSync('resources/js/Pages/Mlb/GradeAnuncioGlide.jsx','utf8').split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n'); const req=[['import do CSS obrigatorio',/glide-data-grid\/dist\/index\.css/],['DataEditor importado',/DataEditor/],['tema ecf mapeado',/accentColor:\s*'#ffe600'/],['bgCell = ecf.card',/bgCell:\s*'#0f1116'/],['COLS_BASE',/COLS_BASE/],['colunas dinamicas por atributo',/attr:/],['grupo Ficha tecnica',/Ficha t/],['getCellContent memoizado',/getCellContent\s*=\s*useCallback/],['deps minimas de getCellContent',/\[colunas,\s*aba\.linhas\]/]]; const falta=req.filter(([n,r])=>!r.test(s)).map(([n])=>n); if(falta.length){console.error('FALTA:',falta.join(' | '));process.exit(1);} if(/aba\.obrigatorios/.test(s)===false){console.error('colunas nao derivam de aba.obrigatorios');process.exit(1);} console.log('grade: tema+colunas+getCellContent OK');"</automated>
  </verify>
  <done>`GradeAnuncioGlide.jsx` exporta o componente, importa o CSS da lib, define `temaEcf` com os hex reais dos tokens ecf-*, deriva colunas de `COLS_BASE` + `aba.obrigatorios` com grupos e asteriscos, e tem `getCellContent` memoizado com dependências `[colunas, aba.linhas]`. `npm run build` verde.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever no estado via onCellsEdited (origem, A×L×C, autosave) e adicionar linha via trailing row</name>
  <files>resources/js/Pages/Mlb/GradeAnuncioGlide.jsx</files>
  <action>
    Implementar `onCellsEdited` como o ÚNICO ponto de entrada de escrita da grade (§Pattern 3 do
    research: verificado no código-fonte que a lib chama `onCellsEdited` primeiro para TUDO —
    digitação, fill handle e paste — e só cai no `onCellEdited` singular se este handler retornar
    falsy). Implementar SÓ o plural e retornar `true`. NÃO implementar `onCellEdited`.

    O handler recebe uma lista de `{ location: [col, row], value }`. Para cada edição, resolver a
    coluna por `colunas[col]` e delegar à página chamando `onEditarCelula(uid, campo, valor, opts)`
    — que é o `editarComSalvar` já existente em `AnunciarMassa.jsx` e que JÁ faz as três coisas que
    não podem regredir: grava no estado da aba, marca `salvo: false`, e agenda o autosave com
    debounce de 600ms por linha. Para colunas de atributo, passar `{ attr: true }` e o `_attr.id`
    como campo; para colunas base, o `coluna.id`. NÃO reimplementar `setAbas`/`agendarSalvar` aqui
    dentro — a página continua dona do estado (migração cirúrgica).

    ORIGEM DO CAMPO (SHEET-04, não pode regredir): `editarCelula` promove `origem[campo]` de
    'cliente' para 'publicador'. Mas as chaves de origem NÃO batem 1:1 com os nomes de campo da
    linha: `puxarProduto` grava `origem.available_quantity` (não `origem.estoque`) e
    `origem.description` (não `origem.descricao`) — e é por essas chaves que a UI de hoje lê o badge
    (`l.origem['available_quantity']` na célula de Estoque). Declarar no módulo um mapa
    `CHAVE_ORIGEM = { estoque: 'available_quantity', descricao: 'description' }` e um helper
    `chaveOrigem(campo)` que devolve o mapeado ou o próprio campo, e usá-lo ao repassar o campo
    para `onEditarCelula`. Sem isso, editar Estoque nunca apaga a bolinha violeta. Registrar no
    SUMMARY que isso corrige uma inconsistência pré-existente (hoje `set('estoque', ...)` grava em
    `origem.estoque`, chave que ninguém lê).

    A×L×C (paridade com a coluna única de hoje): quando o valor escrito na coluna `alturaCm`
    contiver um separador de dimensão (`x`, `×` ou `*`), passar o texto por `parseDimensoes` de
    `gradeMassaUtils` e, se ele devolver um objeto, emitir TRÊS edições (alturaCm, larguraCm,
    comprimentoCm) em vez de uma. Isso preserva a capacidade "colar/digitar 10x20x30" que a coluna
    `dim3` de `colunasColaveis` dá hoje, agora funcionando também na digitação direta, não só no
    paste. Se `parseDimensoes` devolver null, gravar o texto cru em `alturaCm` (mesmo fallback
    tolerante de hoje).

    ADICIONAR LINHA: usar o mecanismo nativo — `rows={aba.linhas.length}`, `onRowAppended` chamando
    `onAdicionarLinha`, e `trailingRowOptions` com `hint: '+ Adicionar linha'`, `sticky: true`,
    `tint: true`. É a tradução direta da última `<tr>` da tabela de hoje.

    Congelar a primeira coluna com `freezeColumns={1}` (Título sempre visível no scroll horizontal —
    equivalente ao `sticky left-0` da coluna "#" de hoje). O Plan 05 vai prepender a coluna de
    status e passar isso para 2.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && node -e "const s=require('fs').readFileSync('resources/js/Pages/Mlb/GradeAnuncioGlide.jsx','utf8').split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n'); const req=[['onCellsEdited',/onCellsEdited/],['delega para onEditarCelula',/onEditarCelula\(/],['mapa de chave de origem',/CHAVE_ORIGEM/],['available_quantity mapeado',/available_quantity/],['parseDimensoes reusado',/parseDimensoes/],['import dos utils',/from '@\/Pages\/Mlb\/gradeMassaUtils'/],['onRowAppended',/onRowAppended/],['trailingRowOptions',/trailingRowOptions/],['freezeColumns',/freezeColumns/]]; const falta=req.filter(([n,r])=>!r.test(s)).map(([n])=>n); if(falta.length){console.error('FALTA:',falta.join(' | '));process.exit(1);} if(/onCellEdited(?!s)/.test(s)){console.error('onCellEdited singular nao deve ser implementado');process.exit(1);} console.log('escrita+autosave+trailing row OK');"</automated>
  </verify>
  <done>`onCellsEdited` existe, é o único handler de escrita (não há `onCellEdited` singular), delega para `onEditarCelula` da página preservando o autosave com debounce, traduz o campo pela `CHAVE_ORIGEM` e expande A×L×C via `parseDimensoes`. `onRowAppended` + `trailingRowOptions` reproduzem "+ Adicionar linha". `npm run build` verde.</done>
</task>

<task type="auto">
  <name>Task 3: Trocar a tabela HTML pelo canvas em AnunciarMassa.jsx e remover os componentes de célula mortos</name>
  <files>resources/js/Pages/Mlb/AnunciarMassa.jsx</files>
  <action>
    Importar `GradeAnuncioGlide` de `@/Pages/Mlb/GradeAnuncioGlide` e substituir o render de
    `<Grade ... />` por `<GradeAnuncioGlide ... />`, passando as MESMAS props que `<Grade>` recebe
    hoje (`aba`, `empresa`, `onEditarCelula={editarComSalvar}`, `onAdicionarLinha`,
    `onRemoverLinha`) MENOS `onColar` — o paste passa a ser responsabilidade da lib no Plan 03.

    Remover de `AnunciarMassa.jsx` os componentes que o canvas substitui e que ficam sem nenhum
    consumidor: `Grade`, `LinhaGrade`, `Th`, `Cell`, `CellInput`, `OrigemBadge`. Não há canvas que
    renderize JSX — não existe caminho de volta para esses componentes, então eles não são "código
    morto que talvez volte", são lixo.

    NÃO remover ainda: `colunasColaveis`, `colarNaGrade`, o estado `avisoColagem` e o bloco JSX que
    exibe o aviso de colagem. Eles ficam órfãos (sem consumidor) até o Plan 03, que wira o paste
    nativo e só então os apaga — é a regra explícita da fase: código de paste manual só sai quando
    o paste nativo estiver provado no lugar. Adicionar um comentário pt-BR curto marcando que são
    remanescentes a remover no Plan 03, para ninguém achar que é esquecimento.

    NÃO tocar em NADA mais do arquivo: estado das abas e `abaAtiva`, efeitos de init e de
    carregamento de colunas, `preverCategoria`/`adicionarCategoria`, `patchAba`, `editarCelula`,
    `adicionarLinha`, refs de autosave, `patchLinha`, `salvarLinha`, `agendarSalvar`,
    `editarComSalvar`, `removerLinha`, `puxarProduto`, `validarTudo`, `resumoLote`, `publicarLote`,
    `PublishBar`, `montarPayloadLinha`, `linhaDeRascunho`, o cabeçalho, o `ModoAnuncioTabs`, as
    cápsulas de aba por categoria, a barra de breadcrumb e o painel de puxar produtos do cliente.
    Se o diff deste arquivo tocar em qualquer um desses, a migração deixou de ser cirúrgica.

    Depois de importar `lucide-react`: conferir se algum ícone (`Trash2`, `Check`, `AlertTriangle`,
    `Loader2`) ficou sem uso após a remoção dos componentes e limpar só os imports que sobraram sem
    consumidor — sem mexer nos que a `PublishBar` e o resto da página ainda usam.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15 && node -e "const a=require('fs').readFileSync('resources/js/Pages/Mlb/AnunciarMassa.jsx','utf8'); const c=a.split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n'); if(!/<GradeAnuncioGlide/.test(c)){console.error('GradeAnuncioGlide nao renderizado');process.exit(1);} if(!/from '@\/Pages\/Mlb\/GradeAnuncioGlide'/.test(c)){console.error('import ausente');process.exit(1);} const mortos=['Grade','LinhaGrade','Th','Cell','CellInput','OrigemBadge']; const viv=mortos.filter(n=>new RegExp('^function\\\\s+'+n+'\\\\s*\\\\(','m').test(c)); if(viv.length){console.error('componentes de celula ainda declarados:',viv.join(', '));process.exit(1);} const preservar=['patchAba','editarCelula','salvarLinha','agendarSalvar','editarComSalvar','removerLinha','puxarProduto','validarTudo','publicarLote','montarPayloadLinha','linhaDeRascunho','PublishBar','colunasColaveis','colarNaGrade']; const sumiu=preservar.filter(n=>!new RegExp('\\\\b'+n+'\\\\b').test(c)); if(sumiu.length){console.error('REGRESSAO - simbolo preservado sumiu:',sumiu.join(', '));process.exit(1);} console.log('swap cirurgico OK');"</automated>
    <automated>C:/php/php.exe artisan test --filter=Phase82</automated>
  </verify>
  <done>A página renderiza `<GradeAnuncioGlide>`; `Grade`/`LinhaGrade`/`Th`/`Cell`/`CellInput`/`OrigemBadge` não existem mais; todo o resto da página (autosave, PublishBar, puxar produtos, abas) continua declarado e intocado; `colunasColaveis`/`colarNaGrade` seguem no arquivo aguardando o Plan 03; `npm run build` e `php artisan test --filter=Phase82` verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publicador → célula do canvas | Texto arbitrário digitado vira `payload` de rascunho e depois item do Mercado Livre |
| GradeAnuncioGlide → AnunciarMassa | O componente de grade escreve no estado da página via callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-03 | Tampering | valor digitado numa célula (`getCellContent`/`onCellsEdited`) | accept | `GridCellKind.Text` desenha com `ctx.fillText` no canvas, nunca `innerHTML` — XSS de DOM não se aplica ao canvas; a página já renderiza os demais campos com JSX padrão (escape automático do React), sem `dangerouslySetInnerHTML` |
| T-82-04 | Tampering | `tier` (`listing_type_id`) e atributos `value_type=list` | mitigate | Ambos ficam `allowOverlay: false` (somente leitura) neste plano; só passam a ser editáveis no Plan 03 como `DropdownCell` com `allowedValues` fechado — não existe janela em que texto livre entre nesses campos |
| T-82-05 | Denial of Service | `getCellContent` com referência instável | mitigate | `useCallback` com dependências mínimas `[colunas, aba.linhas]` (Pitfall 3 do research); gate automatizado verifica o array de dependências |
| T-82-06 | Tampering | payload de publicação em lote | mitigate | Inalterado: `errosLocaisLinha` (barreira local) e a validação server-side de `MlbAnuncioController` continuam idênticas; backend não muda nesta fase |
</threat_model>

<verification>
- `npm run build` verde (prova import/CSS/JSX corretos — é o gate de frontend do projeto).
- `php artisan test --filter=Phase82` verde (9 testes; prova que o backend segue intocado).
- Gate estrutural: os símbolos de estado/autosave/publicação da página continuam todos declarados.
- Comportamento de canvas (render, edição, overlay) NÃO é verificável automaticamente nem em
  localhost (nenhuma empresa com `ml_token` no banco local, sem OAuth ML) — fica para o checkpoint
  visual do Plan 07.
</verification>

<success_criteria>
- A aba ativa renderiza via `<DataEditor>` com tema ecf-*, 10 colunas base + obrigatórios da categoria.
- Editar célula → estado atualizado + autosave agendado, via o mesmo `editarComSalvar` de hoje.
- Origem de campo promovida de 'cliente' para 'publicador' com as chaves corretas.
- "10x20x30" em Altura preenche as 3 dimensões.
- "+ Adicionar linha" funciona pela trailing row nativa.
- Diff de `AnunciarMassa.jsx` limitado a: imports, render da grade, remoção dos componentes de célula.
</success_criteria>

<output>
Create `.planning/phases/82-planilha-excel-like-na-grade-de-anuncio-em-massa-glide-data-/82-02-SUMMARY.md` when done
</output>
