---
phase: 13-reestruturacao-cadastro-empresas
plan: 03
type: execute
wave: 3
depends_on:
  - 13-02
files_modified:
  - app/Http/Controllers/ComercialController.php
  - app/Http/Controllers/MlbImplementacaoController.php
  - app/Notifications/EmpresaCadastradaNotification.php
  - routes/web.php
  - tests/Feature/Phase13ComercialTest.php
autonomous: true
requirements:
  - COM-02
  - COM-03
  - COM-04
  - COM-05
  - COM-06
  - COM-07
  - COM-08
must_haves:
  truths:
    - "GET /comercial/empresas/novo retorna 200 para user com permissão e 403 sem"
    - "POST /comercial/empresas com POLOS cria company + mlb_empresa + mlb_implementacao atomicamente"
    - "POST /comercial/empresas com Assessoria cria company + mlb_empresa sem mlb_implementacao"
    - "POST /comercial/empresas com publicidade ou gestao cria apenas company"
    - "Guard de duplicata bloqueia nome igual em companies OU mlb_empresas (case-insensitive)"
    - "Notificação disparada para líderes do setor de destino após criação (fora da transaction)"
    - "Activity log gerado para cada criação"
    - "Company criada pelo Comercial aparece em AdminController::fechamento()"
  artifacts:
    - path: "app/Http/Controllers/ComercialController.php"
      provides: "index() + store() com DB::transaction, guard duplicata, activity log"
    - path: "app/Http/Controllers/MlbImplementacaoController.php"
      provides: "método privado criarImplementacaoPolo() extraído das linhas 176-213"
    - path: "app/Notifications/EmpresaCadastradaNotification.php"
      provides: "extends BaseNotification com titulo, mensagem, categoria=MANUAL, autorUserId"
    - path: "routes/web.php"
      provides: "rotas /comercial/* com middleware permission:comercial.cadastrar_empresa"
  key_links:
    - from: "ComercialController::store()"
      to: "DB::transaction()"
      via: "criação atômica de company + mlb_empresa + mlb_implementacao"
      pattern: "DB::transaction"
    - from: "ComercialController::store()"
      to: "EmpresaCadastradaNotification"
      via: "Notification::send() fora da transaction"
      pattern: "Notification::send"
    - from: "routes/web.php"
      to: "ComercialController"
      via: "middleware permission:comercial.cadastrar_empresa"
      pattern: "permission:comercial\\.cadastrar_empresa"
---

<objective>
Controller, rotas, notificação e suíte de testes: implementar ComercialController com lógica de criação por service_type, extrair a lógica POLO de MlbImplementacaoController para reutilização, criar EmpresaCadastradaNotification e registrar as rotas /comercial/* com middleware de permissão.

Purpose: É o núcleo de negócio da Phase 13 — sem este plano, o fluxo de cadastro centralizado não existe. A extração de criarImplementacaoPolo() evita duplicação de lógica (D-20 do CONTEXT.md).

Output: ComercialController funcional, EmpresaCadastradaNotification criada, rotas registradas, suíte Phase13ComercialTest GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-reestruturacao-cadastro-empresas/13-CONTEXT.md
@.planning/phases/13-reestruturacao-cadastro-empresas/13-RESEARCH.md
@.planning/phases/13-reestruturacao-cadastro-empresas/13-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que o executor precisa conhecer -->

De app/Notifications/BaseNotification.php:
  abstract class BaseNotification extends Notification {
    public function __construct(
      public string $titulo,
      public string $mensagem,
      public Categoria $categoria,
      public ?int $autorUserId = null,
      public ?string $url = null,
      public array $meta = [],
    ) {}
    public function via(object $notifiable): array { return ['database']; }
    public function toArray(object $notifiable): array { /* 6 chaves canônicas */ }
  }

De app/Notifications/Categoria.php:
  enum Categoria: string {
    case META_ATRIBUIDA = 'meta_atribuida';
    case META_ATINGIDA  = 'meta_atingida';
    case MANUAL         = 'manual'; // usar MANUAL para EmpresaCadastradaNotification
  }

De app/Models/Setor.php:
  public function lideres(): BelongsToMany  // via setor_lideres pivot

De app/Http/Controllers/MlbImplementacaoController.php linhas 176-213 (lógica canônica a extrair):
  // Criação de MlbEmpresa POLO + MlbImplementacao dentro de DB::transaction
  $empresa = MlbEmpresa::create(['nome', 'tipo'='POLO', 'projeto'='POLOS', 'fase'='M0', 'estagio'='Não Listado', 'criado_por'])
  $dados = MlbImplementacao::dadosPadrao();
  $p = MlbConfiguracao::implementacaoPadroes();
  if ($p['tutorial_intro']) $dados['tutorial_intro'] = $p['tutorial_intro'];
  if (!empty($p['tutoriais'])) $dados['tutoriais'] = array_merge($dados['tutoriais'], $p['tutoriais']);
  if (!empty($p['links_admin_extra'])) { $dados['links_admin']['programa_decola'] = ...; }
  MlbImplementacao::create(['empresa_id', 'token' => Str::random(48), 'dados'])

Padrão de rota com middleware permission (de routes/web.php — Phase 12):
  Route::middleware('permission:notificacoes.criar')->group(function () {
    Route::get('/notificacoes/nova',  [..., 'nova'])->name('notificacoes.nova');
    Route::post('/notificacoes/nova', [..., 'criar'])->name('notificacoes.criar');
  });

Padrão de activity log (de MlbImplementacaoController linhas 209-212):
  activity('implementacao')
    ->causedBy($request->user())
    ->withProperties(['empresa' => $empresa->nome, 'vinculada' => (bool) $existing])
    ->log('Implementação MLB criada para "' . $empresa->nome . '"');

Mapeamento service_type → slug do setor de destino (para buscar líderes):
  'polos'      → 'publicacao'   (setor Publicação gerencia implementação POLO)
  'assessoria' → 'publicacao'   (setor Publicação gerencia Assessoria também)
  'publicidade'→ 'publicidade'  (pode não existir ainda no banco — verificar isNotEmpty())
  'gestao'     → 'gestao'       (pode não existir ainda no banco — verificar isNotEmpty())
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Suíte Phase13ComercialTest + EmpresaCadastradaNotification</name>
  <files>
    tests/Feature/Phase13ComercialTest.php
    app/Notifications/EmpresaCadastradaNotification.php
  </files>
  <read_first>
    - app/Notifications/BaseNotification.php (construtor 6 params, canal database)
    - app/Notifications/Categoria.php (enum com MANUAL)
    - app/Notifications/ManualNotification.php (exemplo de subclasse concreta — padrão a seguir)
    - tests/Feature/Phase13MigrationTest.php (padrão do projeto para testes desta phase)
  </read_first>
  <behavior>
    Testes RED antes de criar o controller; GREEN após:
    - test_sem_permissao_recebe_403: user sem 'comercial.cadastrar_empresa' recebe 403 em GET /comercial/empresas/novo
    - test_admin_acessa_sem_permissao_explicita: admin recebe 200 em GET /comercial/empresas/novo (short-circuit isAdmin)
    - test_validacao_campos_obrigatorios: POST sem 'nome' ou 'service_type' retorna erro de validação (422 ou redirect com errors)
    - test_guard_duplicata_companies: POST com nome igual a company existente retorna erro 'nome' com mensagem de duplicata
    - test_guard_duplicata_mlb_empresas: POST com nome igual a mlb_empresa existente retorna erro 'nome' com mensagem de duplicata
    - test_cria_empresa_polos_atomico: POST com service_type='polos' cria 1 company + 1 mlb_empresa + 1 mlb_implementacao; rollback se qualquer create falhar
    - test_cria_empresa_assessoria: POST com service_type='assessoria' cria 1 company + 1 mlb_empresa; sem mlb_implementacao
    - test_cria_empresa_publicidade: POST com service_type='publicidade' cria apenas 1 company; sem mlb_empresa
    - test_cria_empresa_gestao: POST com service_type='gestao' cria apenas 1 company; sem mlb_empresa
    - test_empresa_visivel_no_financeiro: company criada pelo Comercial aparece em AdminController::fechamento() props
    - test_notificacao_lideres: Notification::fake() + criação de setor 'publicacao' com líder → assert notificação EmpresaCadastradaNotification enviada para o líder
    - test_sem_lideres_nao_falha: setor de destino sem líderes → criação bem-sucedida sem exceção
  </behavior>
  <action>
    Criar tests/Feature/Phase13ComercialTest.php com classe Phase13ComercialTest extends TestCase.
    Usar traits: RefreshDatabase, WithFaker (ou factory helpers).
    Marcar métodos com @group phase13.
    Usar actingAs() para simular usuário com/sem permissão.
    Para testar permissão, criar usuário e setor com permission_key='comercial.cadastrar_empresa' via DB helpers.

    Criar app/Notifications/EmpresaCadastradaNotification.php:
    Namespace: App\Notifications.
    Classe extends BaseNotification.
    Construtor: (string $nomeEmpresa, string $serviceType, ?int $autorUserId).
    Chamar parent::__construct com:
      titulo: 'Nova empresa cadastrada: ' . $nomeEmpresa
      mensagem: 'O setor Comercial cadastrou a empresa "' . $nomeEmpresa . '" (tipo: ' . $serviceType . '). Verifique os pendentes.'
      categoria: Categoria::MANUAL
      autorUserId: $autorUserId
      url: route('notificacoes.index')
      meta: ['empresa' => $nomeEmpresa, 'service_type' => $serviceType]
    Sem sobrescrever via() ou toArray() — herda da BaseNotification.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase13ComercialTest</automated>
  </verify>
  <acceptance_criteria>
    - app/Notifications/EmpresaCadastradaNotification.php existe e estende BaseNotification
    - EmpresaCadastradaNotification não sobrescreve via() nem toArray()
    - tests/Feature/Phase13ComercialTest.php existe com os 12 métodos listados no behavior
    - Testes Phase13ComercialTest falham (RED) porque ComercialController ainda não existe — verificar que os erros são de "class not found" ou "404 route not found", não de sintaxe
  </acceptance_criteria>
  <done>
    EmpresaCadastradaNotification.php criada e sintaticamente válida.
    Phase13ComercialTest.php criada com 12+ métodos de teste.
    Testes estão RED por ausência do controller — esperado neste momento.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: ComercialController, extração de criarImplementacaoPolo() e rotas</name>
  <files>
    app/Http/Controllers/ComercialController.php
    app/Http/Controllers/MlbImplementacaoController.php
    routes/web.php
  </files>
  <read_first>
    - app/Http/Controllers/MlbImplementacaoController.php (linhas 176-213 — lógica canônica a extrair)
    - app/Http/Controllers/AdminController.php (fechamento() — confirmar que lista todas companies sem filtro de status)
    - app/Models/MlbImplementacao.php (dadosPadrao(), CHECKLIST, assinaturas)
    - app/Models/MlbConfiguracao.php (implementacaoPadroes())
    - app/Models/Company.php (fillable atual após Wave 1 — inclui 'status')
    - app/Models/MlbEmpresa.php (fillable atual após Wave 1 — inclui 'company_id')
    - app/Models/Setor.php (lideres() relationship)
    - routes/web.php (estrutura do arquivo, onde inserir as rotas /comercial/*)
  </read_first>
  <action>
    PASSO 1 — Extrair método privado em MlbImplementacaoController:
    Criar método privado criarImplementacaoPolo(MlbEmpresa $empresa): MlbImplementacao.
    Mover a lógica das linhas 176-213 (apenas a parte de MlbImplementacao::create) para este método:
      $dados = MlbImplementacao::dadosPadrao();
      $p = MlbConfiguracao::implementacaoPadroes();
      if ($p['tutorial_intro']) $dados['tutorial_intro'] = $p['tutorial_intro'];
      if (!empty($p['tutoriais'])) $dados['tutoriais'] = array_merge($dados['tutoriais'], $p['tutoriais']);
      if (!empty($p['links_admin_extra'])) { $dados['links_admin']['programa_decola'] = $p['links_admin_extra']['programa_decola'] ?? ''; }
      return MlbImplementacao::create(['empresa_id' => $empresa->id, 'token' => Str::random(48), 'dados' => $dados]);
    Atualizar MlbImplementacaoController::criar() para chamar $this->criarImplementacaoPolo($empresa) no lugar da lógica inline.
    ATENÇÃO: não quebrar o comportamento existente do método criar() — apenas mover a lógica de criação de implementação.

    PASSO 2 — Criar ComercialController:
    Namespace: App\Http\Controllers.
    Usar: Company, MlbEmpresa, MlbImplementacao, MlbConfiguracao, Setor, EmpresaCadastradaNotification, DB, Str, Inertia, Notification, Request, ValidationException.
    Seguir o padrão inline do CompanyController (sem service layer separado, conforme discrição do Claude em CONTEXT.md).

    Método index(): abort_unless(auth()->user()->hasPermission('comercial.cadastrar_empresa') || auth()->user()->isAdmin(), 403). Retornar Inertia::render('Comercial/NovaEmpresa', []).

    Método store(Request $request):
      (1) Validação via $request->validate():
          'nome'         => 'required|string|max:255'
          'cnpj'         => 'nullable|string|max:20|unique:companies,cnpj'
          'service_type' => 'required|in:polos,assessoria,publicidade,gestao'
          'subtipo'      => 'nullable|in:polos,assessoria' (condicional — validar se service_type='publicacao' mas aceitar sempre nullable)
      (2) Guard de duplicata (per D-07 e D-21):
          $existeEmCompanies  = Company::whereRaw('LOWER(name) = LOWER(?)', [$validated['nome']])->exists();
          $existeEmMlbEmpresa = MlbEmpresa::whereRaw('LOWER(nome) = LOWER(?)', [$validated['nome']])->exists();
          Se qualquer exists() for true: throw ValidationException::withMessages(['nome' => 'Já existe uma empresa com o nome "' . $validated['nome'] . '" no sistema.']);
      (3) DB::transaction() com variável $company para capturar fora:
          - service_type='polos': Company::create(nome, cnpj, service_type='polos', status='pendente', active=true) + MlbEmpresa::create(nome, tipo='POLO', projeto='POLOS', fase='M0', estagio='Não Listado', company_id=$company->id, criado_por=user->id) + $this->criarImplementacaoPolo($empresa)
          - service_type='assessoria': Company::create(nome, cnpj, service_type='assessoria', status='pendente', active=true) + MlbEmpresa::create(nome, tipo='ASSESSORIA', company_id=$company->id, criado_por=user->id)
          - service_type='publicidade' ou 'gestao': apenas Company::create(nome, cnpj, service_type, status='pendente', active=true)
      (4) FORA da transaction — activity log:
          activity('comercial')->causedBy($request->user())->withProperties(['empresa' => $company->name, 'service_type' => $company->service_type])->log('Empresa cadastrada pelo Comercial: "' . $company->name . '"');
      (5) FORA da transaction — notificação para líderes (per Armadilha 4 do RESEARCH.md — não disparar dentro da transaction):
          $slugSetor = $this->resolverSlugSetor($company->service_type);
          $setor = Setor::where('slug', $slugSetor)->first();
          if ($setor) { $lideres = $setor->lideres; if ($lideres->isNotEmpty()) { Notification::send($lideres, new EmpresaCadastradaNotification($company->name, $company->service_type, $request->user()->id)); } }
      (6) return back()->with('success', 'Empresa "' . $company->name . '" cadastrada com sucesso.');

    Método privado resolverSlugSetor(string $serviceType): string:
      'polos' → 'publicacao', 'assessoria' → 'publicacao', default → Str::slug($serviceType)

    PASSO 3 — Adicionar método privado criarImplementacaoPolo ao ComercialController:
    Mesmo conteúdo que o extraído em MlbImplementacaoController, para que ComercialController seja autocontido.
    (Ambos os controllers têm o método privado — sem trait compartilhado para manter simplicidade, per decisão de discrição do Claude.)

    PASSO 4 — Registrar rotas em routes/web.php:
    Dentro do grupo middleware(['auth', 'verified']), adicionar bloco separado para o módulo Comercial:
      // Módulo Comercial — cadastro centralizado de empresas (Phase 13)
      Route::middleware('permission:comercial.cadastrar_empresa')
           ->prefix('comercial')
           ->name('comercial.')
           ->group(function () {
               Route::get('/empresas/novo',  [ComercialController::class, 'index'])->name('empresas.novo');
               Route::post('/empresas',      [ComercialController::class, 'store'])->name('empresas.store');
           });
    Adicionar use App\Http\Controllers\ComercialController; no bloco de imports.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase13ComercialTest</automated>
  </verify>
  <acceptance_criteria>
    - app/Http/Controllers/ComercialController.php existe com classe ComercialController
    - ComercialController contém métodos index(), store(), resolverSlugSetor(), criarImplementacaoPolo()
    - routes/web.php contém 'permission:comercial.cadastrar_empresa' como middleware de rota
    - routes/web.php contém rota nomeada 'comercial.empresas.novo'
    - routes/web.php contém rota nomeada 'comercial.empresas.store'
    - Phase13ComercialTest: todos os 12+ testes GREEN
    - C:\xampp\php\php.exe artisan route:list mostra /comercial/empresas/novo como rota existente
    - MlbImplementacaoController ainda funciona (método criar() não quebrou após extração)
  </acceptance_criteria>
  <done>
    Phase13ComercialTest suite: todos os testes GREEN.
    ComercialController.php existe e implementa os 4 service_types.
    EmpresaCadastradaNotification disparada para líderes de setor após criação.
    Rota 'comercial.empresas.novo' registrada com middleware permission.
    MlbImplementacaoController não quebrado (criar() ainda funciona).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→ComercialController | Input de formulário não autenticado pode tentar criar companies |
| ComercialController→DB::transaction | Criação atômica de múltiplos registros |
| ComercialController→Notification::send | Dispatch para líderes pode falhar silenciosamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Spoofing / EoP | GET/POST /comercial/empresas sem permissão | mitigate | middleware 'permission:comercial.cadastrar_empresa' retorna 403 antes de chegar ao controller |
| T-13-08 | Tampering | Injeção SQL via campo 'nome' no guard de duplicata | mitigate | whereRaw() com binding parametrizado — não interpolação de string |
| T-13-09 | Tampering | CNPJ duplicado via criação Comercial | mitigate | 'unique:companies,cnpj' na validação do campo cnpj |
| T-13-10 | Tampering | Mass assignment em Company::create sem 'status' no fillable | mitigate | 'status' foi adicionado ao Company.$fillable na Wave 1 |
| T-13-11 | Denial of Service | Notification::send() dentro da transaction poderia causar rollback por falha de notificação | mitigate | Notification::send() disparado FORA da transaction (per Armadilha 4 do RESEARCH.md) |
</threat_model>

<verification>
Após executar este plano, verificar:
1. C:\xampp\php\php.exe artisan test --filter=Phase13ComercialTest — todos GREEN
2. C:\xampp\php\php.exe artisan route:list --name=comercial — mostrar as 2 rotas
3. C:\xampp\php\php.exe artisan test --filter=MlbImplementacaoTest — sem regressões (se existir)
4. C:\xampp\php\php.exe artisan test — suíte completa sem regressões
</verification>

<success_criteria>
- Phase13ComercialTest: todos os testes GREEN (12+ métodos)
- ComercialController.php implementa os 4 service_types com DB::transaction
- Guard de duplicata verifica companies.name E mlb_empresas.nome
- Notificação disparada fora da transaction, com verificação isNotEmpty()
- Activity log gerado para cada criação
- Rotas /comercial/empresas registradas com middleware de permissão
</success_criteria>

<output>
Criar `.planning/phases/13-reestruturacao-cadastro-empresas/13-03-SUMMARY.md` ao concluir.
</output>
