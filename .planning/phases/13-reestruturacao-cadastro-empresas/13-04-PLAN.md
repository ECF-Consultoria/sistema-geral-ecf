---
phase: 13-reestruturacao-cadastro-empresas
plan: 04
type: execute
wave: 4
depends_on:
  - 13-03
files_modified:
  - resources/js/Layouts/AppLayout.jsx
  - resources/js/Pages/Comercial/NovaEmpresa.jsx
  - resources/js/Pages/Mlb/Empresas.jsx
  - resources/js/Pages/Companies/Index.jsx
  - app/Http/Controllers/MlbController.php
  - app/Http/Controllers/CompanyController.php
autonomous: false
requirements:
  - COM-01
  - COM-02
  - COM-03
  - COM-09
must_haves:
  truths:
    - "Sidebar exibe item 'Cadastro de Empresas' para usuário com 'comercial.cadastrar_empresa'"
    - "Formulário /comercial/empresas/novo exibe campos Nome, CNPJ, service_type"
    - "Campo subtipo aparece dinamicamente quando service_type='publicacao'"
    - "Guard de duplicata exibe mensagem de erro clara no formulário"
    - "Página /mlb/empresas exibe seção 'Pendentes' com badge para companies status='pendente'"
    - "Página /companies exibe pendentes com badge visual para service_type publicidade/gestao"
  artifacts:
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "item Comercial no sidebar com separador de seção + gating por comercial.cadastrar_empresa"
    - path: "resources/js/Pages/Comercial/NovaEmpresa.jsx"
      provides: "formulário de cadastro com campos dinâmicos e feedback de erros"
    - path: "resources/js/Pages/Mlb/Empresas.jsx"
      provides: "seção Pendentes no topo com badge visual"
    - path: "resources/js/Pages/Companies/Index.jsx"
      provides: "seção Pendentes para publicidade/gestao com badge visual"
  key_links:
    - from: "AppLayout.jsx NAV_ITEMS"
      to: "rota 'comercial.empresas.novo'"
      via: "routeName: 'comercial.empresas.novo'"
      pattern: "comercial.empresas.novo"
    - from: "NovaEmpresa.jsx useForm()"
      to: "rota 'comercial.empresas.store'"
      via: "post(route('comercial.empresas.store'))"
      pattern: "comercial\\.empresas\\.store"
    - from: "Mlb/Empresas.jsx"
      to: "companies pendentes"
      via: "prop pendentes filtrada no MlbController"
      pattern: "pendentes"
---

<objective>
UI completa: item Comercial no sidebar, formulário NovaEmpresa.jsx com campos dinâmicos, seção "Pendentes" nas páginas existentes /mlb/empresas e /companies. Checkpoint humano obrigatório para verificação visual.

Purpose: Sem a UI, o fluxo de cadastro não é acessível para o time Comercial. Sem as seções de pendentes, os setores de destino não conseguem ver empresas recém-cadastradas aguardando complemento de dados.

Output: Frontend completo com npm run build sem erros + checkpoint humano verificando o formulário e o badge de pendentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-reestruturacao-cadastro-empresas/13-CONTEXT.md
@.planning/phases/13-reestruturacao-cadastro-empresas/13-RESEARCH.md
@.planning/phases/13-reestruturacao-cadastro-empresas/13-03-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que o executor precisa conhecer -->

De resources/js/Layouts/AppLayout.jsx — estrutura do NAV_ITEMS (padrão de separadores de seção):
  // Separador de seção Liderança (padrão a seguir para Comercial):
  { label: 'Meu Setor', routeName: 'lideranca.index', ..., leadSeparatorBefore: true, excludeRoles: ['admin'] }
  // Separador de seção MLB (padrão a seguir para Comercial):
  { label: 'Pub · Dashboard', routeName: 'mlb.dashboard', ..., mlbSeparatorBefore: true }
  // Separador de seção Adm:
  { label: 'Empresas', routeName: 'admin.empresas', ..., adminSeparatorBefore: true }

De resources/js/Layouts/AppLayout.jsx — SidebarInner renderiza separadores:
  {item.leadSeparatorBefore && ...}
  {item.mlbSeparatorBefore && ...}
  {item.adminSeparatorBefore && ...}
  // ATENÇÃO: adicionar handler para item.comercialSeparatorBefore no mesmo padrão

De resources/js/Layouts/AppLayout.jsx — check de permissão no filtro userNav:
  NAV_ITEMS.filter(n => {
    if (n.excludeRoles?.includes(mainRole)) return false;
    return n.permission ? permissions.includes(n.permission) : true;
  })

De resources/js/Pages/Mlb/Empresas.jsx — props recebidas do MlbController:
  // Verificar o que MlbController::empresas() retorna para saber como adicionar 'pendentes'

De app/Http/Controllers/MlbController.php — método empresas():
  // Lê mlb_empresas e retorna array; precisa incluir companies.status='pendente' no join/query

De app/Http/Controllers/CompanyController.php — método index():
  // Lê todas companies via Company::with(['consultor', 'estrategista', 'latestMetrics'])
  // Precisa incluir 'status' na serialização do map()

Padrão de badge visual do projeto (de AppLayout):
  // ecf-yellow como cor primária; bg-white/[0.03] para cards; border-white/[0.08] para bordas
  // Pendentes usam: badge amarelo bg-ecf-yellow/10 text-ecf-yellow border-ecf-yellow/20
</interfaces>

<tasks>

<task type="auto">
  <name>Tarefa 1: AppLayout — item Comercial no sidebar e backend com prop pendentes</name>
  <files>
    resources/js/Layouts/AppLayout.jsx
    app/Http/Controllers/MlbController.php
    app/Http/Controllers/CompanyController.php
  </files>
  <read_first>
    - resources/js/Layouts/AppLayout.jsx (inteiro — NAV_ITEMS, SidebarInner, lógica de separadores)
    - app/Http/Controllers/MlbController.php (método empresas() — estrutura de props retornadas)
    - app/Http/Controllers/CompanyController.php (método index() — map() de companies)
  </read_first>
  <action>
    Em AppLayout.jsx — NAV_ITEMS array:
    Adicionar antes dos itens MLB (mlbSeparatorBefore) um novo item:
      { label: 'Cadastro de Empresas', routeName: 'comercial.empresas.novo', page: 'Comercial/NovaEmpresa',
        icon: PlusCircle, permission: 'comercial.cadastrar_empresa', comercialSeparatorBefore: true }
    Verificar que PlusCircle já está importado (já está na linha 7 do arquivo original).

    Em AppLayout.jsx — SidebarInner component:
    Adicionar handler para o novo flag comercialSeparatorBefore no mesmo padrão dos outros separadores.
    Texto do separador: "Comercial".
    Usar o mesmo markup dos separadores existentes:
      {item.comercialSeparatorBefore && (!collapsed || mobile) && (
        <div className="flex items-center gap-2 px-3 pt-4 pb-1.5">
          <div className="h-px flex-1 bg-white/[0.06]" />
          <span className="text-white/20 text-[10px] font-semibold uppercase tracking-wider">Comercial</span>
          <div className="h-px flex-1 bg-white/[0.06]" />
        </div>
      )}

    Em MlbController.php — método empresas():
    A página /mlb/empresas precisa exibir "Pendentes". Adicionar à query (ou como prop separada) as companies com status='pendente' E service_type IN ('polos', 'assessoria') que ainda não têm mlb_empresa vinculada, OU as que já têm mlb_empresa mas têm company.status='pendente'.
    Estratégia: adicionar prop 'empresas_pendentes' ao retorno de Inertia::render() com array de companies com status='pendente' que sejam de publicação (service_type IN polos/assessoria).
    Query: Company::whereIn('service_type', ['polos', 'assessoria'])->where('status', 'pendente')->orderBy('created_at', 'desc')->get(['id', 'name', 'service_type', 'created_at'])

    Em CompanyController.php — método index():
    Adicionar 'status' no map() de companies para que Companies/Index.jsx possa exibir badge de pendente.
    Adicionar prop 'empresas_pendentes' com companies status='pendente' e service_type IN ('publicidade', 'gestao'):
    Company::whereIn('service_type', ['publicidade', 'gestao'])->where('status', 'pendente')->orderBy('created_at', 'desc')->get(['id', 'name', 'service_type', 'created_at'])
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase13</automated>
  </verify>
  <acceptance_criteria>
    - AppLayout.jsx contém 'comercialSeparatorBefore: true' no NAV_ITEMS
    - AppLayout.jsx contém handler para item.comercialSeparatorBefore no SidebarInner
    - AppLayout.jsx contém routeName: 'comercial.empresas.novo' no item Comercial
    - MlbController.php método empresas() passa 'empresas_pendentes' como prop para Inertia::render()
    - CompanyController.php método index() inclui 'status' na serialização do map() e passa 'empresas_pendentes'
    - C:\xampp\php\php.exe artisan test --filter=Phase13 — sem regressões
  </acceptance_criteria>
  <done>
    AppLayout.jsx atualizado com separador "Comercial" e item de menu.
    MlbController e CompanyController passam prop 'empresas_pendentes' para os respectivos renders.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: NovaEmpresa.jsx + seções Pendentes + npm run build</name>
  <files>
    resources/js/Pages/Comercial/NovaEmpresa.jsx
    resources/js/Pages/Mlb/Empresas.jsx
    resources/js/Pages/Companies/Index.jsx
  </files>
  <read_first>
    - resources/js/Pages/Notificacoes/Nova.jsx (padrão de formulário useForm() com submit e erros — referência de estilo)
    - resources/js/Pages/Mlb/Empresas.jsx (inteiro — onde inserir seção Pendentes, estrutura dos cards)
    - resources/js/Pages/Companies/Index.jsx (inteiro — onde inserir seção Pendentes, componentes UI usados)
    - resources/js/Components/ui/select.jsx (componente Select do shadcn para service_type)
  </read_first>
  <action>
    Criar resources/js/Pages/Comercial/NovaEmpresa.jsx:
    Componente default NovaEmpresa() com AppLayout wrapping.
    Estado de formulário via useForm() com campos: { nome: '', cnpj: '', service_type: '', subtipo: '' }.
    Campos do formulário:
      - Nome: input text obrigatório; erro inline se errors.nome
      - CNPJ: input text opcional com máscara visual (apenas texto livre — sem formatação JS automática)
      - Tipo de Serviço: Select (componente ui/select) com opções: POLOS, Assessoria, Publicidade, Gestão
        Mapear valores: POLOS→'polos', Assessoria→'assessoria', Publicidade→'publicidade', Gestão→'gestao'
      - Subtipo (condicional): exibir apenas quando service_type === 'polos' ou service_type === 'assessoria'.
        ATENÇÃO: O formulário usa service_type com valores 'polos' e 'assessoria', não 'publicacao'.
        O subtipo não é necessário para o backend atual — mas o campo deve existir no formulário
        para o caso de Publicação no futuro. Por enquanto: se service_type for 'polos' ou 'assessoria',
        o backend já sabe o que criar sem precisar do subtipo. O campo subtipo pode ser omitido
        do formulário nesta fase, pois D-06 só menciona subtipo POLOS/Assessoria quando service_type='publicacao',
        mas o fluxo atual usa 'polos' e 'assessoria' diretamente como service_types distintos.
    Submit: post(route('comercial.empresas.store'), { preserveScroll: true }).
    Feedback de sucesso: verificar flash.success via usePage().props e exibir mensagem.
    Feedback de erro: exibir errors.nome abaixo do campo Nome.
    Estilo: dark theme consistente com o resto do projeto — bg cards ecf-card, bordas border-white/[0.08], botão submit com bg-ecf-yellow text-black.

    Em resources/js/Pages/Mlb/Empresas.jsx:
    Receber nova prop 'empresas_pendentes' (array de companies, pode ser vazio).
    Se empresas_pendentes.length > 0: exibir seção no TOPO da página (antes da lista principal).
    Seção: título "Empresas Pendentes" com badge de contagem (badge amarelo ecf-yellow).
    Cada item: nome da empresa, service_type, data de cadastro formatada.
    Estilo: card com border-ecf-yellow/20 bg-ecf-yellow/[0.03] para destacar dos itens normais.

    Em resources/js/Pages/Companies/Index.jsx:
    Receber nova prop 'empresas_pendentes' (array de companies pendentes de publicidade/gestao).
    Se empresas_pendentes.length > 0: exibir seção "Pendentes" no topo da lista existente.
    Mesmo estilo de badge e card que Mlb/Empresas.jsx para consistência visual.

    Executar npm run build ao final (OBRIGATÓRIO após edição JSX).
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - resources/js/Pages/Comercial/NovaEmpresa.jsx existe com componente default NovaEmpresa
    - NovaEmpresa.jsx usa useForm() e post(route('comercial.empresas.store'))
    - NovaEmpresa.jsx não contém nenhum campo 'subtipo' obrigatório (subtipo omitido nesta fase)
    - Mlb/Empresas.jsx aceita prop 'empresas_pendentes' e renderiza seção quando não vazia
    - Companies/Index.jsx aceita prop 'empresas_pendentes' e renderiza seção quando não vazia
    - npm run build: 0 erros, 0 warnings críticos
  </acceptance_criteria>
  <done>
    NovaEmpresa.jsx criada e funcional.
    Mlb/Empresas.jsx e Companies/Index.jsx com seção Pendentes.
    npm run build: sucesso sem erros.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Fluxo completo do Comercial:
    - Item "Cadastro de Empresas" no sidebar (com separador "Comercial") visível para admin
    - Formulário /comercial/empresas/novo com campos Nome, CNPJ, Tipo de Serviço
    - Criação bem-sucedida redireciona com flash de sucesso
    - Páginas /mlb/empresas e /companies exibem seção "Pendentes" quando há empresas com status='pendente'
  </what-built>
  <how-to-verify>
    1. Abrir http://localhost/ecf_admin/public (ou URL local configurada) e fazer login como admin

    2. Verificar sidebar: deve aparecer separador "COMERCIAL" com sub-item "Cadastro de Empresas"
       antes dos itens MLB. Clicar no item deve navegar para /comercial/empresas/novo.

    3. No formulário /comercial/empresas/novo:
       a. Tentar submeter sem preencher Nome → deve aparecer mensagem de erro "campo obrigatório"
       b. Preencher Nome="Empresa Teste Pendente", selecionar Tipo="Publicidade", clicar Cadastrar
       c. Deve aparecer flash verde "Empresa 'Empresa Teste Pendente' cadastrada com sucesso."

    4. Acessar /companies → verificar que aparece seção "Pendentes" no topo com a empresa recém-criada
       com badge amarelo (service_type='publicidade', status='pendente')

    5. Criar outra empresa: Nome="Empresa MLB Pendente", Tipo="POLOS"
       a. Acessar /mlb/empresas → verificar seção "Pendentes" no topo com esta empresa

    6. Tentar criar empresa com nome duplicado (ex: "Empresa Teste Pendente" novamente)
       → deve aparecer mensagem de erro no campo Nome sobre duplicata

    7. Acessar /administrativo/financeiro → verificar que as duas empresas criadas aparecem na lista
       (COM-07: empresa criada pelo Comercial aparece no Fechamento)
  </how-to-verify>
  <resume-signal>Digite "aprovado" se tudo funcionou corretamente, ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→NovaEmpresa.jsx | Input de formulário do usuário Comercial |
| NovaEmpresa.jsx→ComercialController | POST com dados do formulário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Spoofing | Item Comercial no sidebar acessível sem permissão | mitigate | gating por permissions.includes('comercial.cadastrar_empresa') no frontend + middleware no backend |
| T-13-13 | Tampering | Submissão direta de POST /comercial/empresas sem CSRF | mitigate | CSRF middleware padrão do Laravel protege todas as rotas auth; frontend usa useForm() do Inertia que inclui o token automaticamente |
| T-13-14 | Information Disclosure | Seção Pendentes expõe dados de empresas para usuários não autorizados | accept | Seções Pendentes aparecem em páginas já gated por permissão (mlb.empresas, core.empresas) |
</threat_model>

<verification>
Após executar este plano (antes do checkpoint humano), verificar:
1. npm run build — sem erros
2. C:\xampp\php\php.exe artisan test --filter=Phase13 — todos GREEN
3. C:\xampp\php\php.exe artisan route:list --name=comercial — mostrar as 2 rotas
4. Verificar que NovaEmpresa.jsx não tem import de módulos inexistentes
5. Checkpoint humano: usuário verifica sidebar, formulário, criação, seções pendentes e duplicata
</verification>

<success_criteria>
- npm run build: 0 erros
- Phase13ComercialTest: todos os testes GREEN
- Sidebar exibe "Cadastro de Empresas" com separador "COMERCIAL" para admin
- Formulário /comercial/empresas/novo funcional com validação e feedback
- Seções "Pendentes" visíveis em /mlb/empresas e /companies
- Checkpoint humano aprovado
</success_criteria>

<output>
Criar `.planning/phases/13-reestruturacao-cadastro-empresas/13-04-SUMMARY.md` ao concluir.
</output>
