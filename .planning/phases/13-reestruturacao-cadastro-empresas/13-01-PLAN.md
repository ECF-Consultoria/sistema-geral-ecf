---
phase: 13-reestruturacao-cadastro-empresas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_05_25_100001_add_status_to_companies.php
  - database/migrations/2026_05_25_100002_add_company_id_to_mlb_empresas.php
  - app/Models/Company.php
  - app/Models/MlbEmpresa.php
  - app/Http/Controllers/AdminController.php
  - resources/js/Pages/Admin/Financeiro.jsx
autonomous: true
requirements:
  - COM-10
  - COM-11
must_haves:
  truths:
    - "companies.status coluna existe com valores 'pendente' e 'ativo'"
    - "Todos os registros existentes em companies têm status='ativo'"
    - "mlb_empresas.company_id coluna FK nullable existe"
    - "AdminController::updateEmpresa() aceita service_type 'polos' (não quebra com polo→polos rename)"
    - "Financeiro.jsx exibe badge correto para empresas com service_type='polos'"
  artifacts:
    - path: "database/migrations/2026_05_25_100001_add_status_to_companies.php"
      provides: "Adiciona companies.status + renomeia polo→polos + status='ativo' em existentes"
    - path: "database/migrations/2026_05_25_100002_add_company_id_to_mlb_empresas.php"
      provides: "Adiciona mlb_empresas.company_id FK nullable nullOnDelete"
    - path: "app/Models/Company.php"
      provides: "status em $fillable e $casts"
    - path: "app/Models/MlbEmpresa.php"
      provides: "company_id em $fillable"
    - path: "app/Http/Controllers/AdminController.php"
      provides: "validação service_type atualizada para novos valores"
    - path: "resources/js/Pages/Admin/Financeiro.jsx"
      provides: "SERVICE_LABELS/SERVICE_COLORS com chaves 'polos', 'publicidade', 'gestao'"
  key_links:
    - from: "AdminController::updateEmpresa()"
      to: "companies.service_type"
      via: "validação 'nullable|in:polos,assessoria,incubadora,publicidade,gestao'"
      pattern: "in:polos,assessoria,incubadora,publicidade,gestao"
    - from: "Financeiro.jsx SERVICE_LABELS"
      to: "company.service_type"
      via: "objeto JavaScript indexado por string"
      pattern: "polos.*POLO"
---

<objective>
Fundação de banco de dados: adicionar as duas colunas que a Phase 13 precisa (companies.status e mlb_empresas.company_id), renomear service_type='polo'→'polos' nos dados existentes, e atualizar simultaneamente o código que valida/exibe service_type para não quebrar o sistema após o rename.

Purpose: Sem estas migrations e correções simultâneas, qualquer tentativa de usar o novo fluxo falharia; a validação antiga rejeitaria 'polos' como valor inválido; o frontend exibiria badge vazio onde antes aparecia 'POLO'.

Output: Duas migrations prontas para rodar, models atualizados com novos fillables, AdminController corrigido, Financeiro.jsx com labels novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-reestruturacao-cadastro-empresas/13-CONTEXT.md
@.planning/phases/13-reestruturacao-cadastro-empresas/13-RESEARCH.md
</context>

<interfaces>
<!-- Contratos existentes que o executor precisa conhecer -->

De app/Models/Company.php ($fillable atual — NÃO inclui 'status'):
  protected $fillable = [
    'name', 'cnpj', 'adman_account_id', 'adman_store_id', 'ml_store_id',
    'segment', 'active', 'notes',
    'service_type', 'contract_type', 'contract_start', 'contract_end',
    'additional_service', 'additional_service_price',
    'parent_company_id',
  ];

De app/Models/MlbEmpresa.php ($fillable atual — NÃO inclui 'company_id'):
  protected $fillable = [
    'nome', 'tipo', 'cust_id', 'polo', 'gmail', 'estagio', 'fase', 'projeto', 'prioridade',
    'responsavel_id', 'contexto',
    'skus_estagio1', 'skus_estagio2', 'skus_estagio3',
    'prazo_estagio1', 'prazo_estagio2', 'prazo_estagio3',
    'encerramento', 'criado_por',
    'problema', 'problema_nota', 'problema_em',
  ];

De app/Http/Controllers/AdminController.php linha 53 (PROBLEMÁTICA — 'polo' hardcoded):
  'service_type' => 'nullable|in:polo,assessoria,incubadora',

De resources/js/Pages/Admin/Financeiro.jsx linhas 9-19 (PROBLEMÁTICAS — 'polo' hardcoded):
  const SERVICE_LABELS = { polo: 'POLO', assessoria: 'Assessoria', incubadora: 'Incubadora' };
  const SERVICE_COLORS = { polo: 'bg-blue-500/10 text-blue-300 border-blue-500/20', ... };

Padrão de FK nullOnDelete (de 2026_05_20_100003_add_parent_company_id_to_companies.php):
  $table->unsignedBigInteger('company_id')->nullable()->after('id');
  $table->foreign('company_id')->references('id')->on('companies')->nullOnDelete();
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Migrations de schema — companies.status e mlb_empresas.company_id</name>
  <files>
    database/migrations/2026_05_25_100001_add_status_to_companies.php
    database/migrations/2026_05_25_100002_add_company_id_to_mlb_empresas.php
    tests/Feature/Phase13MigrationTest.php
  </files>
  <read_first>
    - database/migrations/2026_05_20_100003_add_parent_company_id_to_companies.php (padrão FK nullOnDelete)
    - database/migrations/2026_05_19_100001_add_service_fields_to_companies.php (padrão de addColumn em companies)
    - database/migrations/2026_05_07_000011_fix_fase_projeto_mlb_empresas.php (padrão idempotente)
    - app/Models/Company.php (fillable e casts atuais)
    - app/Models/MlbEmpresa.php (fillable atual)
  </read_first>
  <behavior>
    - Teste COM-10 estrutural: após rodar migrations, Schema::hasColumn('companies', 'status') === true
    - Teste COM-11 estrutural: após rodar migrations, Schema::hasColumn('mlb_empresas', 'company_id') === true
    - Teste polo→polos: companies com service_type='polo' têm service_type='polos' após migration
    - Teste status existentes: todas as companies com status IS NULL antes recebem status='ativo'
    - Teste idempotência partial: re-rodar migration 100001 não cria coluna duplicada (usa ifNotExists ou verifica existência)
  </behavior>
  <action>
    Criar migration 2026_05_25_100001_add_status_to_companies.php com método up() que:
    (1) Adiciona coluna 'status' (string, nullable, after 'active') em companies via Schema::table.
    (2) Atualiza companies existentes: UPDATE companies SET status='ativo' WHERE status IS NULL.
    (3) Renomeia service_type: UPDATE companies SET service_type='polos' WHERE service_type='polo'.
    Método down() reverte: UPDATE SET service_type='polo' WHERE service_type='polos' + dropColumn('status').

    Criar migration 2026_05_25_100002_add_company_id_to_mlb_empresas.php com método up() que:
    (1) Adiciona coluna 'company_id' (unsignedBigInteger, nullable, after 'id') em mlb_empresas.
    (2) Adiciona foreign key: references 'id' on 'companies', nullOnDelete (seguindo padrão de 2026_05_20_100003).
    Método down() reverte: dropForeign + dropColumn.

    Criar tests/Feature/Phase13MigrationTest.php com classe Phase13MigrationTest extends TestCase.
    Marcar com @group phase13 e garantir que os testes listados no behavior passem (RED antes de criar as migrations, GREEN depois).
    Os testes usam RefreshDatabase e verificam estado real do schema após rodar as migrations.

    Após as migrations existirem e os testes passarem, atualizar Company.$fillable adicionando 'status' e Company.$casts adicionando 'status' => 'string'.
    Atualizar MlbEmpresa.$fillable adicionando 'company_id'.
    Atualizar Company.getActivitylogOptions() para incluir 'status' no logOnly array.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase13MigrationTest</automated>
  </verify>
  <done>
    Phase13MigrationTest suite: todos os testes GREEN.
    Schema::hasColumn('companies', 'status') retorna true após php artisan migrate.
    Schema::hasColumn('mlb_empresas', 'company_id') retorna true após php artisan migrate.
    'status' presente em Company.$fillable; 'company_id' presente em MlbEmpresa.$fillable.
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Corrigir AdminController validação e Financeiro.jsx labels (polo→polos simultâneo)</name>
  <files>
    app/Http/Controllers/AdminController.php
    resources/js/Pages/Admin/Financeiro.jsx
  </files>
  <read_first>
    - app/Http/Controllers/AdminController.php (inteiro — ver updateEmpresa() linha 53 e fechamento())
    - resources/js/Pages/Admin/Financeiro.jsx (linhas 1-120 — ver SERVICE_LABELS, SERVICE_COLORS, GraficoServico)
  </read_first>
  <action>
    Em AdminController.php método updateEmpresa() linha 53:
    Trocar 'nullable|in:polo,assessoria,incubadora' por 'nullable|in:polos,assessoria,incubadora,publicidade,gestao'.
    Isso resolve a Armadilha 1 do RESEARCH.md: sem esta correção na mesma wave da migration, qualquer
    edição de empresa via Fechamento retornaria 422 com service_type='polos'.

    Em resources/js/Pages/Admin/Financeiro.jsx:
    (a) Renomear chave 'polo' → 'polos' em SERVICE_LABELS e SERVICE_COLORS.
    (b) Adicionar entradas 'publicidade' e 'gestao' em SERVICE_LABELS:
        publicidade: 'Publicidade', gestao: 'Gestão'
    (c) Adicionar entradas 'publicidade' e 'gestao' em SERVICE_COLORS com cores distintas:
        publicidade: 'bg-orange-500/10 text-orange-300 border-orange-500/20'
        gestao: 'bg-cyan-500/10 text-cyan-300 border-cyan-500/20'
    (d) Em GraficoServico (função inline), atualizar o array de dados:
        trocar key 'polo' → 'polos', nome 'POLO' → 'POLO', e adicionar entradas para 'publicidade' e 'gestao'.

    Executar npm run build ao final desta tarefa (OBRIGATÓRIO após edição JSX — conforme CLAUDE.md).
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=AdminControllerTest</automated>
  </verify>
  <done>
    AdminController.php linha com validação service_type contém 'in:polos,assessoria,incubadora,publicidade,gestao' (não contém 'in:polo,').
    Financeiro.jsx não contém a string 'polo:' como chave de objeto (SERVICE_LABELS e SERVICE_COLORS atualizados).
    Financeiro.jsx contém as strings 'polos:' e 'publicidade:' e 'gestao:' como chaves.
    npm run build executado sem erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration→banco | Script de migration escreve diretamente no banco sem validação extra |
| controller→banco | AdminController::updateEmpresa() aceita dados do formulário frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | AdminController::updateEmpresa() service_type | mitigate | Validação `nullable|in:polos,...` via Validator::make — lista fechada de valores permitidos |
| T-13-02 | Tampering | Migration de dados (UPDATE polo→polos) | accept | Operação interna de rename sem input externo; risco mínimo |
| T-13-03 | Tampering | mlb_empresas.company_id FK mass assignment | mitigate | 'company_id' adicionado ao $fillable de MlbEmpresa explicitamente |
</threat_model>

<verification>
Após executar este plano, verificar:
1. C:\xampp\php\php.exe artisan migrate:status — mostrar as duas novas migrations como "Ran"
2. C:\xampp\php\php.exe artisan test --filter=Phase13MigrationTest — todos GREEN
3. Grep: AdminController.php não deve conter 'in:polo,' (chave sem 's' no final)
4. Grep: Financeiro.jsx não deve conter 'polo:' como chave de objeto de labels
5. npm run build sem erros
</verification>

<success_criteria>
- Migration 2026_05_25_100001 executada: coluna status existe em companies, service_type='polo' inexistente no banco
- Migration 2026_05_25_100002 executada: coluna company_id existe em mlb_empresas com FK para companies
- AdminController.php: validação service_type aceita 'polos', 'publicidade', 'gestao'
- Financeiro.jsx: SERVICE_LABELS['polos'] == 'POLO', SERVICE_LABELS['publicidade'] == 'Publicidade'
- Phase13MigrationTest: todos os testes GREEN
- npm run build: 0 erros
</success_criteria>

<output>
Criar `.planning/phases/13-reestruturacao-cadastro-empresas/13-01-SUMMARY.md` ao concluir.
</output>
