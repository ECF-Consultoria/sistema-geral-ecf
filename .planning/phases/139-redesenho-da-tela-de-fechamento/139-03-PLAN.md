---
phase: 139-redesenho-da-tela-de-fechamento
plan: 03
type: execute
wave: 3
depends_on: ["139-02"]
files_modified:
  - resources/js/Pages/Admin/Financeiro.jsx
autonomous: true
requirements: [D-01, D-02, D-05]
must_haves:
  truths:
    - "O topo da tela responde 'quanto vamos receber' num número só, grande e legível"
    - "O topo da tela responde 'quem subiu de faixa' com contagem, ganho em R$/mês e atalho por empresa"
    - "Serviços contratados vira barra empilhada com legenda, ocupando a largura toda"
    - "Tipo de cobrança e Distribuição de faixas somem da tela"
    - "O estado da competência, fechar/refazer com confirmação visível e o Sincronizar atenuado continuam funcionando"
  artifacts:
    - path: "resources/js/Pages/Admin/Financeiro.jsx"
      provides: "Cabeçalho redesenhado + os três widgets novos"
      contains: "function TotalAReceberCard"
  key_links:
    - from: "resources/js/Pages/Admin/Financeiro.jsx"
      to: "prop `totais` do backend"
      via: "props do componente Financeiro"
      pattern: "totais"
    - from: "SubiramDeFaixaCard"
      to: "lista de empresas"
      via: "callback onFocarEmpresa (id) elevado ao componente de página"
      pattern: "onFocarEmpresa"
---

<objective>
Refazer o topo da tela: cabeçalho com título do mês + pill de estado + ações, e o grid
de três widgets — "Total a receber", "Subiram de faixa este mês" (em destaque) e
"Serviços contratados" (barra empilhada, largura total). Remover "Tipo de cobrança",
"Distribuição de faixas" e o "Total consolidado" de três colunas.

Purpose: D-01. Hoje um terço da tela mostra Recebido/Inadimplente/A receber os três
vazios — o sistema não sabe se o cliente pagou. E o donut e o gráfico de barras não
ajudavam ninguém a decidir nada.

Output: `Financeiro.jsx` com cabeçalho e widgets novos, sem recharts, com todo o
controle de competência das Fases 137/138 intacto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-redesenho-da-tela-de-fechamento/139-CONTEXT.md
@.planning/phases/139-redesenho-da-tela-de-fechamento/139-02-SUMMARY.md
@design_handoff_fechamento/README.md
@resources/js/Pages/Admin/Financeiro.jsx

<interfaces>
<!-- Props que a página recebe depois dos planos 01 e 02. -->

```
Financeiro({
  companies,              // linhas; cada uma com conta_no_total, estado, cobranca_mensal,
                          // faturamento, servicos_contratados, faixa_ordem,
                          // faixa_ordem_anterior, valor_faixa_anterior,
                          // subiu_de_faixa, ganho_faixa, valor_faixa_e_piso, ...
  mes_selecionado,        // 'YYYY-MM'
  servicos_disponiveis,
  faixas_por_servico,
  faixas_por_grupo,
  competencia_fechada,
  competencia_fechada_em,
  totais                  // NOVO (plano 02)
})
```

`totais`: `total_a_receber` (float), `total_e_piso` (bool), `empresas_com_cobranca`
(int), `empresas_sem_valor_definido` (int), `faturamento_gerado` (float|null),
`mes_anterior_fechado` (bool), `mes_anterior_total` (float|null), `variacao`
(float|null), `upgrades_quantidade` (int), `upgrades_ganho_total` (float),
`upgrades_ganho_parcial` (bool).

Helpers já existentes no arquivo, para reusar sem recriar: `fmtBRL`, `fmtValorFaixa`
(prefixa "a partir de" quando é piso), `faixaNome`, `cn`, `mesExtensoAno`.
</interfaces>

<traducao_de_tokens>
D-02 manda seguir estrutura/hierarquia/espaçamento do handoff e manter a paleta e a
tipografia do ECF Admin. Tabela de conversão, use sempre a coluna da direita:

| handoff | ECF Admin |
|---|---|
| `#0B0B0C` fundo | fundo do `AppLayout` (não redefinir) |
| `#131315` card | `bg-white/[0.02]` com `border-white/[0.08]` |
| `#0F0F11` superfície interna | `bg-black/30` |
| `#232327` / `#2A2A2E` borda | `border-white/[0.08]` |
| `#33333A` borda de card aberto | `border-white/20` |
| `#E5DE3F` acento | `text-ecf-yellow` / `bg-ecf-yellow` (`#ffe600`) |
| `#F2EE8A` acento hover | `hover:bg-ecf-yellow/80` |
| `#14140A` texto sobre acento | `text-black` |
| `#5BD98A` dinheiro/positivo | `text-emerald-400` (já é a cor de dinheiro no arquivo) |
| `#F2F2F0` texto primário | `text-white` |
| `#C9C9C4` secundário | `text-white/75` |
| `#A8A8A2` terciário | `text-white/60` |
| `#8A8A85` muted | `text-white/40` |
| `#6C6C68` sutil/placeholder | `text-white/30` |
| `#FF8A8A` alerta | `text-amber-300` sobre `bg-amber-500/10` (é o que `IntegrationBadge` já usa) |
| Instrument Sans | fonte do projeto (`font-sans`) — título grande usa `font-display` |
| JetBrains Mono | `font-mono` do Tailwind + `tabular-nums` |

⚠️ **Proibido** adicionar `@import`/`<link>` de Google Fonts, `Instrument Sans` ou
`JetBrains Mono` — a tipografia do handoff foi oferecida ao usuário e recusada.

Tudo que o handoff especifica em **proporção, espaçamento, hierarquia de tamanho,
raio e hover** é para seguir com fidelidade — é ali que mora a melhoria de UX. Escala:
52px/bold para a métrica herói, 30px/600 para o título, 13px para labels de widget,
12px para metadados; padding de card `24px 26px`; gap do grid 16px.
</traducao_de_tokens>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Remover os widgets aposentados e o recharts</name>
  <files>resources/js/Pages/Admin/Financeiro.jsx</files>
  <action>
Apagar do arquivo: `GraficoCobranca`, `GraficoFaixas`, `MiniPie`, `ChartCard`,
`TOOLTIP_STYLE`, `TotalConsolidado`, e a linha de import do `recharts` (linha 5) —
depois dessas remoções nenhum componente do arquivo usa recharts (o `BarChart2` do
`ProgressaoModal` é ícone do lucide, não confundir).

Manter `GraficoServico` **apenas como referência de agregação enquanto você escreve a
Tarefa 3** e removê-lo no fim dela (o widget novo substitui, mas a lógica de contagem
por `servico_nome` com o balde `Sem contratos` é para reaproveitar). Manter
`SERVICO_PALETTE` e `corPorNome` — a cor determinística por nome do serviço continua
sendo o mapeamento do projeto (não adotar as cores do handoff).

Remover também do JSX da página o `<div className="grid grid-cols-3 gap-4">` com os três
gráficos e a chamada `<TotalConsolidado empresas={companies} />`.

⚠️ Não tocar em: `StatusCompetenciaBadge`, `FecharCompetenciaButton`,
`RefazerFechamentoDialog`, `useConfirmacaoTemporaria`, `ConfirmacaoInline`,
`SyncFaturamentoBtn`, `GerarRelatoriosBtn`, `MesSeletor`, `EvolucaoBadge`,
`ProgressaoModal`, `FechamentoRow`, `AusenciaTabelaPendencia`,
`AusenciaFaturamentoBadge`, `FaturamentoCombinadoBreakdown`,
`GrupoServicosDivergentesBanner`, `FaixaProgresso`, `ContratosSection`,
`TabelaFaixasSection`. Há suítes que leem este `.jsx` como texto e exigem que várias
dessas funções continuem existindo com o mesmo nome, neste mesmo arquivo.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase137|Phase138"</automated>
  </verify>
  <done>`grep -c recharts resources/js/Pages/Admin/Financeiro.jsx` retorna 0; `npx vite build` roda sem erro de import; gate 137/138 verde.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Cabeçalho + widget "Total a receber"</name>
  <files>resources/js/Pages/Admin/Financeiro.jsx</files>
  <action>
**Cabeçalho.** Reescrever o bloco de topo seguindo a estrutura do handoff (§1):
`flex flex-wrap gap-5 items-end justify-between mb-7`.

- Esquerda: título `Fechamento de {mês por extenso}` em 30px/600 com
  `tracking-[-0.02em]`, usando o helper `mesExtensoAno(mes_selecionado)` que já existe
  (só o mês, sem o ano, quando o ano é o corrente — se ficar ambíguo, manter mês e ano).
  Ao lado, o `StatusCompetenciaBadge` **já existente** — só reestilizar para o formato
  pill do handoff (padding `5px 11px`, `rounded-full`, pontinho de 6px antes do texto;
  âmbar/amarelo em "Em aberto", esmeralda em "Fechado"). As palavras **"Em aberto"** e
  a data de fechamento não podem mudar.
- Subtítulo 14px `text-white/40`: "Faturamento do mês, faixa aplicada e mensalidade a
  cobrar de cada empresa." Manter, ao lado ou logo abaixo, o aviso "Dados D-1 da Adman"
  com o mesmo `title` explicativo que já existe (é disclaimer de precisão de dado, não
  decoração).
- Direita, `gap-2.5`, nesta ordem: `MesSeletor`, `SyncFaturamentoBtn`,
  `GerarRelatoriosBtn`, e por último a ação primária —
  `FecharCompetenciaButton` ou `RefazerFechamentoDialog` conforme `competencia_fechada`,
  exatamente como está hoje. Os secundários ganham o formato do handoff
  (`px-3.5 py-2.5 rounded-[10px] border-white/[0.08] bg-white/[0.03] text-[14px]
  text-white/75`, hover `bg-white/[0.06] text-white`); o primário fica
  `bg-ecf-yellow text-black font-semibold rounded-[10px] px-4.5 py-2.5`.

**Widget Total a receber.** Criar `function TotalAReceberCard({ totais })`:
card `rounded-2xl border border-white/[0.08] bg-white/[0.02] px-6 py-6 flex flex-col
gap-4.5`.

- Linha de topo: label "Total a receber neste fechamento" (13px, `text-white/40`) e,
  à direita, "{empresas_com_cobranca} empresas com cobrança" (12px, `text-white/30`).
  Quando `empresas_sem_valor_definido > 0`, acrescentar embaixo, em `text-amber-300`
  12px: "{N} sem valor definido — não entram nesta soma". **Não** usar a palavra
  "tabela" aqui; o detalhe de tabela fica na linha da empresa.
- Métrica herói: "R$" 22px + o número em 52px/700 `tracking-[-0.03em] leading-none
  font-mono tabular-nums text-emerald-400` + "/mês" 15px `text-white/30`. Formatar o
  número sem o prefixo "R$" (o prefixo é elemento separado). Se `total_e_piso` for
  `true`, escrever "a partir de" antes do "R$", em 15px `text-white/40` — nunca o valor
  seco (mesma disciplina de `fmtValorFaixa`).
- Rodapé com `border-t border-white/[0.06] pt-4 flex gap-7`, três pares
  label(12px `text-white/30`)/valor(16px `font-mono tabular-nums`):
  - "Mês passado" → `fmtBRL(mes_anterior_total)` em `text-white/75`. Quando
    `mes_anterior_fechado` é `false`: escrever **"mês passado ainda não foi fechado"**
    em `text-white/30`, nunca R$ 0.
  - "Variação" → `+`/`−` + `fmtBRL(Math.abs(variacao))`, verde quando positiva,
    `text-white/60` quando zero, `text-red-400` quando negativa. `variacao === null` →
    traço com o mesmo texto do item anterior, sem inventar número.
  - "Faturamento gerado" → `fmtBRL(faturamento_gerado)` em `text-white/75`;
    `null` → "sem faturamento apurado neste mês".

Ligar `<TotalAReceberCard totais={totais} />` no JSX da página, dentro de um grid
`grid grid-cols-1 xl:grid-cols-[1.15fr_1fr] gap-4` (abaixo de ~1100px vira uma coluna,
conforme o handoff).

Copy sem jargão: proibidos "snapshot", "competência", "reconsolidação", "rollup",
"âncora", "origem", "faixa piso" em qualquer texto visível desta tela.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase137CompetenciaUiTest"</automated>
  </verify>
  <done>O cabeçalho traz título do mês, pill "Em aberto"/"Fechado", as quatro ações e o disclaimer D-1. `TotalAReceberCard` mostra o total, a contagem e os três pares do rodapé, com os três estados de ausência escritos por extenso.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Widgets "Subiram de faixa este mês" e "Serviços contratados"</name>
  <files>resources/js/Pages/Admin/Financeiro.jsx</files>
  <action>
**`function SubiramDeFaixaCard({ totais, companies, onFocarEmpresa })`** — card em
destaque: `rounded-2xl border border-ecf-yellow/35 px-6 py-6 flex flex-col gap-4` com
fundo `bg-gradient-to-b from-ecf-yellow/10 to-ecf-yellow/[0.02]`.

- Topo: label "Subiram de faixa este mês" (13px/600 `text-ecf-yellow`) e, à direita,
  "+{fmtBRL(upgrades_ganho_total)}/mês" (12px `font-mono` `text-white/40`). Quando
  `upgrades_ganho_parcial` é `true`, prefixar "no mínimo " — parte do ganho não é
  conhecida e o número não pode se apresentar como completo.
- Número herói: `upgrades_quantidade` em 52px/700 `font-mono tabular-nums text-white` +
  "empresas" 15px `text-white/40` (singular quando 1).
- Lista de atalhos, `gap-2`, derivada de `companies.filter(c => c.subiu_de_faixa)`,
  ordenada por `ganho_faixa` desc (as sem ganho conhecido por último). Um botão por
  empresa: `w-full flex items-center justify-between gap-3 px-3 py-2.5 rounded-[9px]
  border border-ecf-yellow/20 bg-black/40 text-left hover:border-ecf-yellow/50
  hover:bg-black/70 transition-colors`. Esquerda: `c.name` (13px/500 `text-white`).
  Direita: "Faixa {faixa_ordem_anterior} → {faixa_ordem}" (12px `font-mono`
  `text-ecf-yellow`) — quando `faixa_ordem_anterior` é null, escrever só
  "subiu para a faixa {faixa_ordem}".
- `onClick` chama `onFocarEmpresa(c.id)`.
- Se `upgrades_quantidade === 0`: manter o card, com o número 0 e a frase
  "Nenhuma empresa mudou de faixa neste mês." em 13px `text-white/40`, sem lista.
  Nunca esconder o card — a ausência de upgrade é resposta, não vazio.

`onFocarEmpresa` é uma prop de callback; o componente de página passa uma função que
(a) zera a busca, (b) liga o filtro "Subiram de faixa" e (c) marca a empresa como a
linha aberta. **Neste plano** basta elevar o estado ao componente de página e passar o
callback — o filtro em chips e a abertura da linha são implementados no plano 04;
deixe o handler chamando dois `setState` que já existirão lá (`setFiltroChip('subiu')`
e `setEmpresaFocada(id)`), declarando os dois estados agora com
`useState('todos')` / `useState(null)`.

**`function ServicosContratadosBar({ companies })`** — card largura total
(`xl:col-span-2`), `rounded-2xl border border-white/[0.08] bg-white/[0.02] px-6 py-5.5
flex flex-col gap-4`.

- Contagem: reaproveitar a agregação de `GraficoServico` (um voto por contrato ativo;
  empresa sem nenhum contrato cai no balde "Sem contratos"), ordenada desc por
  quantidade, cor via `corPorNome` e cinza `#374151` para "Sem contratos".
- Topo: label "Serviços contratados" (13px `text-white/40`) e, à direita,
  "{total} contratos ativos" (12px `text-white/30`).
- Barra: `flex h-2.5 rounded-full overflow-hidden gap-[2px]`; cada segmento com
  `style={{ width: pct + '%', background: cor }}` e `title` com nome + quantidade.
- Legenda: `flex flex-wrap gap-x-6 gap-y-2.5`; por item, quadrado de 8px
  (`rounded-[2px]`) na cor, nome (13px `text-white/75`) e quantidade (13px/600
  `font-mono text-white`).
- Zero contratos no dataset inteiro: uma linha "Nenhum contrato ativo." em
  `text-white/30`, sem barra.

Depois de escrever este componente, **remover `GraficoServico`** do arquivo.

Ligar os dois no grid criado na Tarefa 2, na ordem do handoff: Total a receber →
Subiram de faixa → Serviços contratados (esta ocupando a linha inteira).
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase122|Phase136|Phase137|Phase138|Phase139"</automated>
  </verify>
  <done>Os três widgets renderizam a partir de `totais` e `companies`. `grep -c "GraficoCobranca\|GraficoFaixas\|GraficoServico\|MiniPie\|TotalConsolidado" resources/js/Pages/Admin/Financeiro.jsx` retorna 0. Gate completo verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props do Inertia → DOM | valores financeiros de toda a carteira renderizados no navegador do usuário admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-07 | Tampering | totais derivados no front | mitigate | os totais vêm prontos de `totais`; proibido recalcular no JSX (foi somando no front, por chave inexistente, que o widget antigo passou a mostrar zero em produção) |
| T-139-08 | Information Disclosure | XSS via nome de empresa | accept | React escapa o conteúdo por padrão e nenhum componente novo usa `dangerouslySetInnerHTML` |
| T-139-09 | Repudiation | ação primária trocada por engano | mitigate | `FecharCompetenciaButton`/`RefazerFechamentoDialog` são reusados sem alteração de comportamento — a confirmação visível de sucesso do incidente 260903-la4 não pode ser perdida no restyle |
| T-139-SC | Tampering | instalação de pacote | accept | não se aplica — nenhum pacote instalado; a remoção do recharts deste arquivo não desinstala a dependência |
</threat_model>

<verification>
- `grep -c "recharts" resources/js/Pages/Admin/Financeiro.jsx` → 0
- `grep -c "cobranca_mensal_grupo" resources/js/Pages/Admin/Financeiro.jsx` → 0 (a chave
  morta some junto com o `TotalConsolidado`)
- `grep -c "Instrument Sans\|JetBrains" resources/js/Pages/Admin/Financeiro.jsx` → 0
- `C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase122|Phase136|Phase137|Phase138|Phase139"` — 0 falhas
</verification>

<success_criteria>
- Topo da tela com cabeçalho + três widgets; Tipo de cobrança, Distribuição de faixas e
  Total consolidado removidos.
- Controle de competência (badge, fechar, refazer com motivo e confirmação, Sincronizar
  atenuado quando fechada) funcionando igual a antes.
- Nenhuma fonte externa introduzida; paleta `ecf-*` mantida.
</success_criteria>

<output>
Criar `.planning/phases/139-redesenho-da-tela-de-fechamento/139-03-SUMMARY.md` ao terminar.
</output>
