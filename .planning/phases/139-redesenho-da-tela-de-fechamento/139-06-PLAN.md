---
phase: 139-redesenho-da-tela-de-fechamento
plan: 06
type: execute
wave: 6
depends_on: ["139-05"]
files_modified:
  - tests/Feature/Phase139/Phase139FechamentoUiContratoTest.php
  - public/build/
autonomous: false
requirements: [D-01, D-02, D-04, D-05]
must_haves:
  truths:
    - "Existe teste que reprova se qualquer um dos quatro dados novos deixar de ser consumido pela tela"
    - "Existe teste que reprova se um estado de ausência das Fases 137/138 sumir do redesenho"
    - "Existe teste que reprova a entrada de Instrument Sans ou JetBrains Mono no projeto"
    - "O bundle é reconstruído e o gate de testes fecha em 0 falhas"
    - "O usuário confere a tela redesenhada em produção e aprova"
  artifacts:
    - path: "tests/Feature/Phase139/Phase139FechamentoUiContratoTest.php"
      provides: "Trava de contrato da UI redesenhada (props consumidas + copy + tipografia)"
  key_links:
    - from: "tests/Feature/Phase139/Phase139FechamentoUiContratoTest.php"
      to: "resources/js/Pages/Admin/Financeiro.jsx"
      via: "leitura do arquivo como texto (o projeto não tem test runner de JS)"
      pattern: "resource_path"
---

<objective>
Fechar a fase: a trava de contrato da UI redesenhada, o rebuild do bundle, o gate
completo de testes e a conferência humana em produção.

Purpose: os quatro dados novos podem chegar às props e ainda assim não aparecer na
tela — foi exatamente isso que aconteceu três vezes nesta linha de trabalho. O projeto
não roda test runner de JS, então a única trava barata é ler o `.jsx` como texto, do
mesmo jeito que `Phase137FinanceiroUiContratoTest` e `Phase137CompetenciaUiTest` já
fazem.

Output: suíte `Phase139FechamentoUiContratoTest`, bundle reconstruído, gate verde e
aprovação do usuário.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-redesenho-da-tela-de-fechamento/139-CONTEXT.md
@.planning/phases/139-redesenho-da-tela-de-fechamento/139-05-SUMMARY.md
@tests/Feature/Phase137/Phase137FinanceiroUiContratoTest.php
@resources/js/Pages/Admin/Financeiro.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Trava de contrato da UI redesenhada</name>
  <files>tests/Feature/Phase139/Phase139FechamentoUiContratoTest.php</files>
  <behavior>
    Frente PROPS (via `GET /administrativo/financeiro` como admin):
    - a resposta tem `totais` com as 11 chaves do plano 02;
    - toda linha de `companies` tem `faixa_ordem_anterior`, `valor_faixa_anterior`,
      `subiu_de_faixa` e `ganho_faixa`.

    Frente ARQUIVO (lendo `resources/js/Pages/Admin/Financeiro.jsx` como texto):
    - contém `totais`, `faixa_ordem_anterior`, `valor_faixa_anterior`,
      `subiu_de_faixa`, `ganho_faixa`, `upgrades_ganho_total`, `mes_anterior_total` —
      **é esta a asserção que pega o dado morrendo no último trecho**: sem ela, o
      backend pode estar perfeito e a tela continuar sem o widget;
    - contém `function TotalAReceberCard`, `function SubiramDeFaixaCard`,
      `function ServicosContratadosBar`;
    - **não** contém `GraficoCobranca`, `GraficoFaixas`, `MiniPie`, `TotalConsolidado`,
      `recharts`, `cobranca_mensal_grupo`;
    - continua contendo `function FechamentoRow`, `function ProgressaoModal`,
      `function EvolucaoBadge`, `function FechamentoAccordion`,
      `function RefazerFechamentoDialog`, `function FecharCompetenciaButton`;
    - continua contendo os literais `A DEFINIR`, `Sem faturamento neste mês`,
      `Faturamento do mês`, `Em aberto`, `Refazer fechamento`,
      `Motivo do reprocessamento`, `Mercado Livre`, `Shopee`, `a partir de`;
    - **não** contém, em nenhuma capitalização, `acumulado`, nem `Fat. do mês`;
    - **não** contém `Instrument Sans`, `JetBrains`, `fonts.googleapis`.

    Frente COPY SEM JARGÃO (mesmo arquivo, e também `TabelaFaixasSection.jsx`):
    - nenhuma das palavras `snapshot`, `reconsolidação`, `rollup`, `âncora` aparece em
      string de texto visível. Para evitar falso positivo com nome de variável e
      comentário, comparar apenas as linhas que não começam com `//`, `*` ou `/*`
      (filtrar antes de contar — comentário em pt-BR é a convenção do projeto e não
      pode invalidar a asserção).
  </behavior>
  <action>
Criar `tests/Feature/Phase139/Phase139FechamentoUiContratoTest.php`, namespace
`Tests\Feature\Phase139`, no molde exato de
`tests/Feature/Phase137/Phase137FinanceiroUiContratoTest.php` (docblock em pt-BR
explicando por que a frente de ARQUIVO existe: não há `vitest`/`jest` no
`package.json`).

Um método `#[Test]` por bloco de `<behavior>`, com mensagem de asserção em pt-BR
dizendo a consequência concreta na tela.

Para a frente COPY SEM JARGÃO, montar o texto filtrado com algo equivalente a
`collect(explode("\n", $conteudo))->reject(fn ($l) => preg_match('/^\s*(\/\/|\*|\/\*)/', $l))
->implode("\n")` antes de aplicar `assertStringNotContainsStringIgnoringCase` — contar
sobre o arquivo cru transformaria os próprios comentários do código numa asserção
auto-invalidante.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter=Phase139FechamentoUiContratoTest</automated>
  </verify>
  <done>A suíte existe e passa. Removendo à mão qualquer uma das quatro chaves novas do JSX, ela falha (verificar isso ao menos uma vez antes de dar a tarefa por concluída).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Build do frontend e gate completo</name>
  <files>public/build/</files>
  <action>
Rodar `npm run build` (obrigatório após mexer em JSX, convenção do projeto) e conferir
que termina sem erro — atenção especial a import morto do `recharts` e a variável de
escopo do componente usada dentro de `.map()`, que o Rollup elimina e vira
`ReferenceError` só no bundle (armadilha já registrada neste projeto).

Rodar o gate:
`C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase122|Phase136|Phase137|Phase138|Phase139"`
e registrar no SUMMARY o total de testes/asserções. O ponto de partida era
**276 testes / 1452 asserções / 0 falhas**; o número final deve ser MAIOR em testes e
igual em falhas (zero).

Falhas fora desse filtro que já eram pré-existentes e **não** são regressão desta fase:
`FechamentoMigrationTest`, `AdminFechamentoControllerTest` (4 de 16), `Phase14MigrationTest`,
`Phase14MlbControllerFiltroTest`, `Phase14VerificarCobrancaTest`.

⚠️ Commitar apenas os caminhos desta fase, um a um
(`git commit -- <arquivo>`). A árvore é compartilhada com outro desenvolvedor e com
outras sessões: **nunca** `git add -A`, `git add .`, `git commit -a` ou `git stash`.
Mensagem de commit em pt-BR.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase122|Phase136|Phase137|Phase138|Phase139"</automated>
  </verify>
  <done>`npm run build` termina sem erro e o gate fecha com 0 falhas, com mais testes que os 276 iniciais.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 3: Conferência visual da tela redesenhada em produção</name>
  <what-built>
Tela de Fechamento redesenhada: cabeçalho com o mês e o estado, widget "Total a receber",
widget em destaque "Subiram de faixa este mês", "Serviços contratados" em barra
empilhada, lista em quatro colunas com barra de progresso, filtros em chips, e a área
expandida com a conta em três passos + tabela progressiva com a faixa atual destacada.

⚠️ **A conferência tem que ser em produção.** O banco local está ~31 migrations atrasado
e sem dado real (0 grupos, 0 métricas) — abrir a tela local não prova nada. O executor
não tem acesso à VPS (`plink`/`pscp`/`deploy.sh` são bloqueados dentro de subagente):
o deploy é feito pelo orquestrador ou pelo usuário, com autorização explícita, como foi
na Fase 138.
  </what-built>
  <action>
Pausar a execução e devolver o controle ao orquestrador. O executor NÃO roda `deploy.sh`,
`plink` nem `pscp` — esses comandos são bloqueados dentro de subagente. Apresentar o
roteiro de `<how-to-verify>` e aguardar o sinal de retomada; só depois da aprovação
escrever o SUMMARY e dar a fase por concluída.
  </action>
  <how-to-verify>
Depois do deploy, abrir `https://admin.ecfconsultoria.com.br/administrativo/financeiro`
e conferir, na ordem:

1. **Total a receber** mostra um número diferente de zero e "N empresas com cobrança"
   coerente com a carteira. (Antes do redesenho este widget mostrava "0 pagadores com
   dados" e três valores vazios — se voltar a mostrar zero, o total está lendo a chave
   errada.)
2. O rodapé traz **Mês passado**, **Variação** e **Faturamento gerado**. Se agosto não
   estiver fechado, o campo diz isso por extenso — nunca R$ 0.
3. **Subiram de faixa este mês** mostra a contagem, o "+R$ X/mês" e um atalho por
   empresa com "Faixa 2 → 3". Clicar num atalho filtra e abre a linha daquela empresa.
4. Trocar para um mês **já fechado** no seletor: o widget de upgrades continua
   preenchido (não pode zerar ao fechar o mês) e a pill fica verde "Fechado".
5. **Serviços contratados** aparece como barra empilhada com legenda e quantidades.
6. Na lista: quatro colunas com cabeçalho, barra de progresso na coluna da faixa e a
   mensalidade alinhada à direita.
7. Achar uma empresa **sem tabela**: precisa dizer "Tabela de faixas: A DEFINIR" — nunca
   R$ 0. E uma empresa **sem faturamento no mês**: "Sem faturamento neste mês". São
   mensagens diferentes.
8. Abrir uma empresa que vende no Mercado Livre **e** na Shopee: a composição
   "Mercado Livre X + Shopee Y = Z" continua visível.
9. Abrir uma linha de **grupo**: a composição do grupo e a frase que nomeia de qual
   empresa a tabela foi herdada continuam lá.
10. Uma empresa em **faixa sem teto**: a mensalidade aparece como "a partir de R$ X",
    nunca o valor seco.
11. Clicar em **Refazer fechamento** (mês fechado) e conferir que aparece a confirmação
    de sucesso na tela — foi um sucesso silencioso que fez o usuário clicar três vezes.
12. Com o mês fechado, **Sincronizar faturamento** fica atenuado e desabilitado.
  </how-to-verify>
  <resume-signal>Responder "aprovado" ou descrever o que ficou errado, item por item.</resume-signal>
  <done>Usuário respondeu "aprovado" depois de conferir os 12 itens na tela de produção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build local → bundle servido em produção | o artefato de `public/build/` é o que o navegador executa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-16 | Tampering | commit arrastando trabalho de outra sessão | mitigate | commitar caminho por caminho com `git commit -- <arquivo>`; `git add -A`/`git stash` proibidos — a árvore é compartilhada |
| T-139-17 | Denial of Service | bundle quebrado em produção | mitigate | `npm run build` obrigatório antes do deploy + conferência visual bloqueante depois dele |
| T-139-18 | Repudiation | aprovar sem conferir em produção | mitigate | checkpoint bloqueante com roteiro item a item; o ambiente local não tem dado real e não serve de prova |
| T-139-SC | Tampering | instalação de pacote | accept | não se aplica — nenhum pacote npm/composer instalado nesta fase |
</threat_model>

<verification>
- `C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase122|Phase136|Phase137|Phase138|Phase139"` — 0 falhas, total > 276 testes.
- `npm run build` sem erro.
- Aprovação explícita do usuário no checkpoint.
</verification>

<success_criteria>
- Suíte `Phase139FechamentoUiContratoTest` trava os quatro dados novos e os estados de
  ausência das Fases 137/138.
- Gate completo verde, bundle reconstruído.
- Usuário aprovou a tela em produção.
</success_criteria>

<output>
Criar `.planning/phases/139-redesenho-da-tela-de-fechamento/139-06-SUMMARY.md` ao terminar.
</output>
