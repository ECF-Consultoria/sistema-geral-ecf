---
phase: 88-camada-de-contexto-carteiracontextservice-v17-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Portfolio/CarteiraContextService.php
  - tests/Feature/V16/CarteiraContextServiceTest.php
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-05]

must_haves:
  truths:
    - "CarteiraContextService::forUser($user, $filters) retorna os vínculos de serviço ativos com company_id, company_name, servico_id, servico_nome, setor, role, role_label nos 4 cenários canônicos (só Performance / só Shopee / Performance+Shopee com pessoas diferentes / mesmo profissional nos dois serviços da mesma empresa)"
    - "Vínculo de setor performance marca has_financial_source=true, financial_source='adman', financial_metrics_eligible=true; vínculo shopee marca false/null/false — resolvido por servicos.setor, sem hardcode de servico_id (Mentoria resolve igual à Gestão)"
    - "Mesma empresa com 2 vínculos do mesmo profissional conta 1 empresa única e 2 vínculos de serviço"
    - "servico_id preenchido tem prioridade; servico_id null + contrato performance ativo resolve como Performance legado; servico_id null + só contrato Shopee ativo NÃO vira responsável Shopee"
    - "Vínculo de empresa inativa não entra no retorno default"
  artifacts:
    - path: "app/Services/Portfolio/CarteiraContextService.php"
      provides: "forUser() + contadores() — fonte única de vínculos carteira×serviço"
      exports: ["forUser", "contadores"]
      min_lines: 120
    - path: "tests/Feature/V16/CarteiraContextServiceTest.php"
      provides: "Suite Feature cobrindo CTX-01..05 (4 cenários canônicos + ramos legado + edge cases)"
      min_lines: 150
  key_links:
    - from: "tests/Feature/V16/CarteiraContextServiceTest.php"
      to: "app/Services/Portfolio/CarteiraContextService.php"
      via: "app(CarteiraContextService::class)->forUser(...)"
      pattern: "CarteiraContextService"
    - from: "app/Services/Portfolio/CarteiraContextService.php"
      to: "company_users / contratos_servico / servicos"
      via: "query builder com join + whereExists"
      pattern: "company_users"
    - from: "tests/Feature/V16/CarteiraContextServiceTest.php"
      to: "tests/Feature/V16/CriaCenarioResponsaveis.php"
      via: "use CriaCenarioResponsaveis (trait reutilizada, não recriada)"
      pattern: "CriaCenarioResponsaveis"
---

<objective>
Criar o `App\Services\Portfolio\CarteiraContextService` — a fonte única e confiável de vínculos de carteira por serviço da milestone v17.0 — com suite Feature completa provando CTX-01..05.

Propósito: hoje `User::companies()` consolida por empresa e faz o responsável Shopee "herdar" faturamento/margem de ML. Este service resolve setor, papel e elegibilidade financeira POR VÍNCULO, sem tocar em nenhum consumidor (reapontamentos são Fases 89-92). Zero mudança de comportamento visível ao usuário nesta fase.

Output: 1 service novo + 1 arquivo de teste novo. Nenhum arquivo existente modificado. Sem deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (seção Phase 88 — success criteria SC1-SC4)
@.planning/phases/88-camada-de-contexto-carteiracontextservice-v17-0/88-RESEARCH.md (queries de referência prontas em §Código de referência + adendo com números reais de prod)
@plano-carteira-desempenho-multi-servico.md (§Camada tecnica proposta = shape; §Regras de compatibilidade = CTX-05)
@app/Models/Servico.php (constantes SETOR_* e SETORES)
@app/Models/Company.php (linhas 191-209 — consultorDoServico/estrategistaDoServico, precedente de wherePivot servico_id)
@app/Services/DesempenhoScoreService.php (linhas 19-116 — convenção de docblock rico + shape array documentado, sem DTO)
@tests/Feature/V16/CriaCenarioResponsaveis.php (trait de fixture a REUSAR — criarServico/criarContrato/inserirPivot/criarCenarioMlComResponsaveis/inserirLinhaShopee)
</context>

<interfaces>
<!-- Contratos que o executor usa diretamente — sem exploração de codebase. -->

De app/Models/Servico.php:
```php
public const SETOR_PERFORMANCE = 'performance';
public const SETOR_PUBLICACAO  = 'publicacao';
public const SETOR_POLOS       = 'polos';
public const SETOR_SHOPEE      = 'shopee';
public const SETOR_OUTROS      = 'outros';
public const SETORES = [...]; // lista plana dos 5
```

De tests/Feature/V16/CriaCenarioResponsaveis.php (namespace Tests\Feature\V16):
```php
protected function criarServico(string $setor, bool $ativo = true): int;
protected function criarContrato(int $companyId, int $servicoId, bool $ativo = true): int;
protected function inserirPivot(int $companyId, int $userId, string $role, ?int $servicoId): int;
/** @return array{company: Company, servicoPerf: int, analista: User, estrategista: User} */
protected function criarCenarioMlComResponsaveis(): array;
/** @return array{servicoShopee: int, row: int} */
protected function inserirLinhaShopee(int $companyId, int $userId, string $role): array;
```

Mapa role→label já consagrado no codebase (CompanyController.php:756, ShopeeEmpresasController.php:250):
```php
'consultor'    => 'Analista',
'estrategista' => 'Estrategista',
```

Assinatura travada do service (research §Shape e assinatura):
```php
/**
 * @param  array{setor?: string|null, role?: string|null, active?: bool}  $filters
 * @return \Illuminate\Support\Collection<int, array{
 *   user_id: int, company_id: int, company_name: string,
 *   servico_id: ?int, servico_nome: ?string, setor: string,
 *   role: string, role_label: string,
 *   has_financial_source: bool, financial_source: ?string, financial_metrics_eligible: bool,
 * }>
 */
public function forUser(User $user, array $filters = []): Collection;

/**
 * Contadores de dedup (CTX-04) sobre uma Collection já resolvida por forUser().
 * @return array{empresas_unicas: int, vinculos_servico: int, vinculos_financeiros: int, vinculos_sem_fonte_financeira: int}
 */
public function contadores(Collection $vinculos): array;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar CarteiraContextService + testes dos 4 cenários canônicos (CTX-01..04)</name>
  <files>app/Services/Portfolio/CarteiraContextService.php, tests/Feature/V16/CarteiraContextServiceTest.php</files>
  <behavior>
    Testes em `tests/Feature/V16/CarteiraContextServiceTest.php` (namespace `Tests\Feature\V16`, `use RefreshDatabase; use CriaCenarioResponsaveis;` — SQLite :memory: já configurado no phpunit.xml; MySQL local fora do ar não bloqueia). Cenários obrigatórios do plano canônico:
    - Cenário 1 (só Performance): `criarCenarioMlComResponsaveis()` → `forUser($analista)` retorna 1 vínculo com shape completo (company_id, company_name, servico_id, servico_nome, setor='performance', role='consultor', role_label='Analista') e flags `has_financial_source=true`, `financial_source='adman'`, `financial_metrics_eligible=true` (CTX-01+CTX-02).
    - Cenário 2 (só Shopee): empresa nova via `Company::factory()` + `inserirLinhaShopee()` (sem contrato performance) → `forUser($gustavo)` retorna 1 vínculo setor='shopee' com `has_financial_source=false`, `financial_source=null`, `financial_metrics_eligible=false` (CTX-02).
    - Cenário 3 (Performance+Shopee na MESMA empresa, pessoas DIFERENTES): `criarCenarioMlComResponsaveis()` + `inserirLinhaShopee($company->id, $outroUser->id, 'consultor')` → `forUser($analistaMl)` retorna SÓ o vínculo performance; `forUser($outroUser)` retorna SÓ o vínculo shopee — atribuição Shopee não vaza pro responsável ML e vice-versa.
    - Cenário 4 (MESMO profissional nos dois serviços da mesma empresa — cenário do dedup CTX-04 e do CART-05 futuro): `inserirPivot()` 2x pro mesmo user/company com servico_id performance e servico_id shopee → `forUser()` retorna 2 vínculos (NÃO colapsa — anti-pattern do distinct de User::companies()); `contadores($vinculos)` retorna `empresas_unicas=1`, `vinculos_servico=2`, `vinculos_financeiros=1`, `vinculos_sem_fonte_financeira=1`.
  </behavior>
  <action>
    Criar `app/Services/Portfolio/CarteiraContextService.php` (namespace `App\Services\Portfolio` — diretório novo, nome do plano canônico §Camada tecnica proposta). Service de LEITURA puro, stateless, sem construtor com dependências, seguindo a convenção do `DesempenhoScoreService` (docblock rico, shape em array documentado via @return, NUNCA DTO — decisão travada).

    `forUser(User $user, array $filters = []): Collection` monta os vínculos em DUAS fontes (queries de referência prontas no 88-RESEARCH.md §Código de referência — usar `DB::table` com joins, como lá):

    1. **Fonte 1 — servico_id PREENCHIDO (prioridade, CTX-05):** `company_users cu` JOIN `companies c` JOIN `servicos s ON s.id = cu.servico_id`, filtrando `cu.user_id`, `whereNotNull('cu.servico_id')`, `whereIn('cu.role', ['consultor','estrategista'])` (Pitfall 1 do research: `company_users.role` NUNCA é 'mentor' — não confundir com `users.role`), e `c.active = true` quando filtro `active` (default `true`).
    2. **Fonte 2 — servico_id NULL, ramo legado (CTX-05):** mesma base SEM join em servicos, `whereNull('cu.servico_id')` + `whereExists` de `contratos_servico ct JOIN servicos s` com `ct.company_id = cu.company_id`, `ct.ativo = true`, `s.setor = Servico::SETOR_PERFORMANCE`. Esse ramo resolve como Performance legado. Regra de prioridade: EXCLUIR do ramo legado linhas cujo (user_id, company_id, role) já tenha linha com `servico_id` preenchido (o preenchido vence — nunca duplicar o vínculo). O vínculo legado sai com `servico_id = null` e `servico_nome = null` (é a verdade da pivot — não inventar o id do contrato) e `setor = 'performance'`. Linha NULL SEM contrato performance ativo (incluindo empresa só-Shopee) NÃO entra no retorno — nunca vira responsável Shopee automaticamente (regra explícita do plano canônico).

    Normalizar as duas fontes no MESMO shape (ver `<interfaces>`): `role_label` via match `'consultor' => 'Analista', 'estrategista' => 'Estrategista'`; flags financeiras derivadas de `setor` via match centralizado em método privado (ex.: `flagsFinanceirasPorSetor(string $setor): array`):
    - `Servico::SETOR_PERFORMANCE` → `['has_financial_source' => true, 'financial_source' => 'adman', 'financial_metrics_eligible' => true]` — cobre Gestão E Mentoria automaticamente porque ambos têm `setor='performance'` no catálogo (CTX-03, sem hardcode de servico_id, confirmado pela migration seed_servicos_setor);
    - `Servico::SETOR_SHOPEE` → `[false, null, false]`;
    - `default` (polos/publicacao/outros) → `[false, null, false]` — sem fonte financeira até segunda ordem; branch defensivo, 0 linhas reais hoje (adendo do research).

    Filtros (`$filters`): `setor` aplicado EM MEMÓRIA na Collection final, DEPOIS da resolução (Pitfall 2: o setor de linha NULL só existe após resolver via contrato); `role` pode ir no SQL; `active` default `true` controla o `where('c.active', true)`.

    `contadores(Collection $vinculos): array` computa `empresas_unicas` (`pluck('company_id')->unique()->count()`), `vinculos_servico` (`count()`), `vinculos_financeiros` (`where('financial_metrics_eligible', true)->count()`) e `vinculos_sem_fonte_financeira` (complemento) — metadados de dedup CTX-04 SEM colapsar vínculos (Pitfall 3: jamais copiar o `distinct('companies.id')` de `User::companies()`).

    Docblock da classe DEVE registrar as decisões travadas (comentários em pt-BR):
    - por que SEM cache (queries locais baratas, 268 linhas de pivot em prod, zero HTTP — diferente do `DesempenhoScoreService::computeCached()` que existe por causa de HTTP síncrono à API ML);
    - por que NÃO chama `MetricsProviderFactory` (vocabulários distintos: `financial_source` aqui = setor→fonte constante 'adman'|null; o factory decide provider técnico 'ambos'|'so-ml'|... — consumidores das Fases 89+ combinam os dois DEPOIS);
    - CTX-05 é DEFESA contra escrita futura sem servico_id, não migração (adendo do research: 0 linhas reais nos ramos legados em prod — por isso NÃO existe comando de backfill);
    - setores `polos`/`publicacao`/`outros` caem no default sem fonte financeira, documentado para o próximo dev que ligar um serviço desses;
    - `User::companies()` permanece intocado como carteira consolidada legada (critério global v17.0).

    Escrever também `tests/Feature/V16/CarteiraContextServiceTest.php` com os 4 cenários do `<behavior>` + o teste de `contadores()`. Reusar a trait `CriaCenarioResponsaveis` (mesmo namespace) — NÃO recriar helpers de fixture. Testes em `tests/Feature/V16/` conforme decisão do orquestrador (dev paralelo usa `tests/Feature/Phase77..82` e o módulo MLB/Anúncios — não tocar). Nomes de testes e comentários em pt-BR.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16/CarteiraContextServiceTest.php</automated>
  </verify>
  <done>Service criado em app/Services/Portfolio/ com forUser() + contadores() e docblock registrando as 5 decisões travadas; os 4 cenários canônicos + contadores passam verdes em SQLite :memory: (mínimo 5 testes).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes dos ramos CTX-05 (legado/prioridade) + Mentoria sem hardcode + empresa inativa + filtros</name>
  <files>tests/Feature/V16/CarteiraContextServiceTest.php</files>
  <behavior>
    Estender o MESMO arquivo de teste com os casos de borda (cada item = 1 teste nomeado em pt-BR):
    - CTX-05a (legado performance): `inserirPivot($company, $user, 'consultor', null)` em empresa COM contrato performance ativo (criar via `criarServico(SETOR_PERFORMANCE)` + `criarContrato`) → vínculo aparece com `setor='performance'`, `servico_id=null`, `servico_nome=null`, `financial_metrics_eligible=true`.
    - CTX-05b (não assumir Shopee): `inserirPivot($company, $user, 'consultor', null)` em empresa SÓ com contrato Shopee ativo (sem performance) → `forUser($user)` NÃO retorna esse vínculo (count 0) — prova que null jamais vira `setor='shopee'`.
    - CTX-05c (prioridade do preenchido): mesmo user/company/role com UMA linha `servico_id` preenchido (performance) E UMA linha null → `forUser` retorna exatamente 1 vínculo (o preenchido), não 2.
    - CTX-03 (Mentoria sem hardcode): criar um SEGUNDO serviço de setor performance com nome 'Mentoria' (via `DB::table('servicos')->insertGetId([... 'nome' => 'Mentoria', 'setor' => Servico::SETOR_PERFORMANCE ...])` ou `criarServico` ajustando o nome) — o id gerado pelo autoincrement do SQLite será qualquer um, ≠ 6/7, o que prova ausência de hardcode → vínculo com esse servico_id marca `financial_metrics_eligible=true`.
    - Empresa inativa: cenário performance com `Company::factory()->create(['active' => false])` → `forUser` default não retorna o vínculo; `forUser($user, ['active' => false])` retorna (parâmetro exposto pra Fase 90).
    - Filtro `setor`: user com vínculo performance + vínculo shopee → `['setor' => 'shopee']` retorna só o shopee; `['setor' => 'performance']` retorna só o performance (inclui o vínculo legado resolvido do CTX-05a se combinado no mesmo cenário — filtro aplicado pós-resolução).
    - Filtro `role`: user consultor numa empresa e estrategista noutra → `['role' => 'estrategista']` retorna só o vínculo de estrategista.
  </behavior>
  <action>
    Adicionar os 7 testes acima ao `CarteiraContextServiceTest.php`. Usar exclusivamente os helpers da trait `CriaCenarioResponsaveis` + `Company::factory()` / `User::factory()`. Onde a trait não cobrir (serviço performance com nome custom para o caso Mentoria), inserir via `DB::table` inline no teste — NÃO adicionar métodos novos à trait (ela é da v16.0 e o dev paralelo pode tocá-la; arquivo compartilhado = risco de conflito).

    Se durante a implementação o comportamento do service divergir do esperado nos ramos CTX-05, corrigir o SERVICE (a regra do plano canônico §Regras de compatibilidade é a fonte de verdade), nunca afrouxar o teste.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16/CarteiraContextServiceTest.php</automated>
  </verify>
  <done>Suite completa com ≥12 testes verdes cobrindo CTX-01..05: 4 cenários canônicos + contadores + CTX-05a/b/c + Mentoria + inativa + filtros setor/role.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de regressão — suite completa verde, zero consumidor tocado</name>
  <files>(nenhum arquivo novo — gate de verificação)</files>
  <action>
    Rodar a suite Feature completa e confirmar delta zero vs baseline (nenhum teste que passava antes quebra — a fase não modifica NENHUM arquivo existente, então qualquer regressão indica efeito colateral inesperado e deve ser investigada antes de fechar).

    Confirmar por grep que nenhum consumidor foi reapontado nesta fase (isso é trabalho das Fases 89-92): `grep -rn "CarteiraContextService" app/Http app/Console resources/js` deve retornar 0 resultados — o service só é referenciado por ele mesmo e pelo teste. Confirmar também que `User::companies()` não foi alterado: `git diff --stat app/Models/User.php` vazio.

    NÃO rodar `npm run build` (nenhum .jsx tocado; dev paralelo com trabalho não commitado no módulo MLB — Pitfall 5 do research). NÃO fazer deploy.
  </action>
  <verify>
    <automated>php artisan test && bash -c 'test -z "$(grep -rln CarteiraContextService app/Http app/Console resources/js 2>/dev/null)" && git diff --quiet app/Models/User.php && echo GATE_OK'</automated>
  </verify>
  <done>`php artisan test` 100% verde (delta zero vs baseline); zero referências a CarteiraContextService fora de app/Services/Portfolio e tests/; User.php sem diff; nenhum arquivo do módulo MLB/Anúncios tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Nenhuma nova | Service de leitura interno, sem rota HTTP, sem input externo nesta fase; `$filters` chega só de código PHP interno (Fases 89-92 farão a validação de request quando expuserem UI) |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-88-01 | Information Disclosure | `forUser()` retorna vínculos de outro user se o caller passar `$user` errado | accept | O escopo por `$user` é parâmetro explícito (mesmo modelo do `DesempenhoScoreService`); a autorização de QUEM pode consultar QUEM é responsabilidade do controller consumidor (Fase 89/90) — documentado no docblock |
| T-88-02 | Tampering | Filtro `setor` com valor fora de `Servico::SETORES` | mitigate | Valor desconhecido simplesmente não casa com nenhum vínculo (filtro em memória por igualdade) — retorno vazio, sem SQL injetável (nenhum filtro vai cru pra SQL; `role` usa whereIn com binding) |
| T-88-SC | Tampering | Supply chain npm/pip/composer | accept | Zero dependência nova instalada nesta fase (service + teste puros sobre stack existente) |
</threat_model>

<verification>
- `php artisan test tests/Feature/V16/CarteiraContextServiceTest.php` — ≥12 testes verdes cobrindo os 4 SC do ROADMAP.
- `php artisan test` — suite completa verde, delta zero (fase não altera arquivo existente).
- `grep -rn "MetricsProviderFactory" app/Services/Portfolio/` → 0 resultados (pitfall de vocabulário respeitado).
- `git diff app/Models/User.php` → vazio (`User::companies()` intocado, critério global v17.0).
</verification>

<success_criteria>
Mapeiam 1:1 com os Success Criteria da Phase 88 no ROADMAP:
1. `forUser` retorna vínculos com shape completo, testado nos 4 cenários canônicos (SC1 / CTX-01).
2. Flags financeiras corretas por setor, Gestão E Mentoria via `servicos.setor` sem hardcode (SC2 / CTX-02+CTX-03).
3. Mesma empresa + 2 vínculos do mesmo profissional = 1 empresa única + 2 vínculos em `contadores()` (SC3 / CTX-04).
4. Prioridade do `servico_id` preenchido; null+contrato performance = legado; null+só Shopee não assume (SC4 / CTX-05, implementado como defesa — sem comando de backfill, 0 linhas reais em prod).
</success_criteria>

<output>
Criar `.planning/phases/88-camada-de-contexto-carteiracontextservice-v17-0/88-01-SUMMARY.md` ao concluir (em pt-BR).
</output>
