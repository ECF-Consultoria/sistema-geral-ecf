---
phase: 61-dashboards-multi-fonte-indicador-de-origem
plan: 06
type: execute
wave: 3
depends_on: [61-01, 61-03, 61-04, 61-05]
files_modified:
  - tests/Feature/Phase61/DashboardMultiFonteE2ETest.php
  - tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php
  - tests/Feature/Phase61/FeatureFlagRegressionTest.php
autonomous: true
requirements: [DATA-05, DASH-04, DASH-05, DASH-06]
tags: [testing, e2e, feature-flag, regressao, cobertura-3-casos]
must_haves:
  truths:
    - "Suite E2E cobre os 3 casos canônicos do ADR DATA-04 (só-Adman / só-ML / ambos) + none em cada superfície da Phase 61 (dashboard ML, portfolio Show, portfolio Carteiras, carteira individual)"
    - "Regressão feature flag: com `UNIFIED_METRICS_ENABLED=false`, payload Inertia é IDÊNTICO ao pré-Phase-61 em ambos DashboardController e PortfolioController"
    - "Baseline Phase 60 mantido: `php artisan test tests/Feature/Phase60` continua 46/46 verde"
    - "Cada teste E2E verifica presença de `data-testid` das SourceBadges renderizadas (via HTML string check ou via AssertableInertia props)"
  artifacts:
    - path: "tests/Feature/Phase61/DashboardMultiFonteE2ETest.php"
      provides: "5 casos cobrindo dashboard ML nos 3 casos + none + flag OFF regressão"
      min_lines: 100
    - path: "tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php"
      provides: "6 casos cobrindo Portfolio/Show + Portfolio/Carteiras nos 3 casos + none + flag OFF regressão"
      min_lines: 100
    - path: "tests/Feature/Phase61/FeatureFlagRegressionTest.php"
      provides: "4 casos garantindo delta zero quando flag OFF em rotas afetadas"
      min_lines: 80
  key_links:
    - from: "Test file"
      to: "Config::set('metrics.unified_metrics_enabled', bool)"
      via: "override runtime da flag antes de cada acting-as-admin request"
      pattern: "Config::set"
    - from: "Test file"
      to: "AssertableInertia"
      via: "->assertInertia(fn($p) => $p->has('...') / ->where('...') / ->missing('...'))"
      pattern: "AssertableInertia|assertInertia"
---

<objective>
Fechar a Phase 61 com suite E2E que prova os 4 Success Criteria do ROADMAP nos 3 casos do ADR DATA-04 + o caso none:

- SC #1 (Dashboard ML unifica fontes num KPI) — coberto em `DashboardMultiFonteE2ETest`
- SC #2 (Analista/Estrategista tolerantes a ML-only) — coberto em `PortfolioMultiFonteE2ETest`
- SC #3 (Badge ML na carteira individual) — coberto por `PortfolioMultiFonteE2ETest` + reuso do `CompanyShowSourceTest` (61-03) já verde
- SC #4 (Badges em cada métrica) — coberto por asserts de `data-testid` nas 4 páginas

Purpose: Sem cobertura E2E, temos apenas testes de enriquecimento backend (61-01) + smoke component (61-02). E2E dá a garantia de que a costura backend→prop→badge funciona ponta-a-ponta e que a flag OFF preserva o legado.

Output: 3 test files novos em `tests/Feature/Phase61/` cobrindo dashboard + portfolio + feature flag regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/adrs/DATA-04-precedencia-multifonte.md
@.planning/phases/60-base-multi-fonte-backend-ml-adman-unificado/60-04-SUMMARY.md
@.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-01-PLAN.md
@.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-03-PLAN.md
@.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-04-PLAN.md
@.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-05-PLAN.md
@tests/Feature/Phase60/BaselineRegressionTest.php
@tests/Feature/Phase60/UnifiedMetricsServiceTest.php

<interfaces>
Padrão de teste E2E do projeto (extraído de `tests/Feature/Phase60/BaselineRegressionTest.php`):

```php
namespace Tests\Feature\Phase61;
use App\Models\Company;
use App\Models\User;
use App\Models\AdmanMetric;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Config;
use Inertia\Testing\AssertableInertia;
use Tests\TestCase;

class DashboardMultiFonteE2ETest extends TestCase {
    use RefreshDatabase;

    private function admin(): User {
        return User::factory()->create(['role' => 'admin']);
    }

    public function test_flag_off_dashboard_ml_nao_expoe_source_counts(): void {
        Config::set('metrics.unified_metrics_enabled', false);
        $admin = $this->admin();
        $response = $this->actingAs($admin)->get(route('mercadolivre.dashboard'));
        $response->assertOk();
        $response->assertInertia(fn(AssertableInertia $p) =>
            $p->missing('stats.source_counts')
              ->missing('companies_performance.0.source')
              ->has('companies_performance')
        );
    }
    // ...
}
```

Fixtures necessárias por caso (padrão Phase 60):
- **só-Adman**: `Company::factory()->create(['adman_account_id' => 'ABC123', 'active' => true]); AdmanMetric::factory()->create(['company_id'=>$c->id, 'reference_date' => now()->subDay(), 'revenue' => 1000]);` — SEM mlToken.
- **só-ML**: `Company::factory()->create(['adman_account_id' => null, 'active' => true]); MlToken::factory()->create(['company_id' => $c->id, 'status' => 'active']);` — sem AdmanMetric.
- **ambos**: `Company::factory()->create(['adman_account_id' => 'XYZ789']); MlToken::factory()->create(['company_id'=>$c->id, 'status'=>'active']); AdmanMetric::factory()->create([...]);`
- **none**: `Company::factory()->create(['adman_account_id' => null, 'active' => true]);` — nem mlToken nem AdmanMetric.

**IMPORTANTE**: Como o `MlMetricsProvider` (Phase 60) usa `MercadoLivreService` LIVE com cache, testes que exercitam caso 'só-ML' ou 'ambos' NÃO precisam mockar HTTP se não chamarem `readForCompany()` — o Phase 61 usa apenas `caseFor()` que só olha accessors (sem HTTP). Mas para os E2E do Portfolio/Show que renderiza cache Adman calls legacy (`getCachedGrossBillingsMany`), pode ser necessário `Http::fake()` ou mock em `AdmanService`. Padrão de mock: `$this->app->instance(AdmanService::class, Mockery::mock(...))` com stubs para `getCachedGrossBillingsMany` e `getCachedAccountMetricsMany` retornando array vazio (empresas fixtures não têm cache).

Rotas envolvidas (validadas via `route:list`):
- `dashboard` (índice — legacy)
- `mercadolivre.dashboard` (Phase 58)
- `portfolio.own` (admin renderiza Carteiras; analista/estrategista renderiza Show)
- `portfolio.show` (`/admin/users/{user}/portfolio`)
- `companies.show` (carteira individual)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DashboardMultiFonteE2ETest — 5 casos (3 fontes + none + regressão flag OFF)</name>
  <files>tests/Feature/Phase61/DashboardMultiFonteE2ETest.php</files>
  <read_first>
    - tests/Feature/Phase60/BaselineRegressionTest.php (padrão setUp + AssertableInertia)
    - tests/Feature/Phase60/UnifiedMetricsServiceTest.php (padrão fixtures 3 casos + none)
    - .planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-01-PLAN.md (contrato REAL do payload Dashboard admin: `stats.source_counts` + `companies_performance[N].source` — NÃO `summary`/`companies`)
    - .planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-05-PLAN.md (contrato do frontend — data-testid da legenda)
    - database/factories/CompanyFactory.php + database/factories/UserFactory.php (verificar factories disponíveis)
    - app/Models/MlToken.php (verificar factory + relação com Company)
  </read_first>
  <behavior>
    - Test 1 `test_flag_off_dashboard_ml_nao_expoe_source_counts`: Config OFF; admin autentica; GET `mercadolivre.dashboard` → 200 + AssertableInertia **missing `stats.source_counts`** + missing `companies_performance.0.source` + has `companies_performance` (chaves reais do payload)
    - Test 2 `test_flag_on_dashboard_ml_expoe_source_counts_agregado`: Config ON; fixtures 1 só-Adman + 1 só-ML + 1 ambos + 1 none; GET → 200 + `stats.source_counts` presente com contagens corretas ({adman:1, ml:1, unified:1, none:1})
    - Test 3 `test_flag_on_dashboard_ml_empresa_so_adman_tem_source_adman`: Config ON; fixture só-Adman → `companies_performance[N].source === 'adman'` (N = índice da fixture)
    - Test 4 `test_flag_on_dashboard_ml_empresa_so_ml_nao_quebra`: Config ON; fixture só-ML (sem AdmanMetric) → GET retorna 200 (não crash) + `companies_performance[N].source === 'ml'`
    - Test 5 `test_flag_on_dashboard_ml_empresa_none_aparece_com_source_none`: Config ON; fixture sem integração → `companies_performance[N].source === 'none'` + inclusa em `stats.total_companies`
  </behavior>
  <action>
    Criar test file `tests/Feature/Phase61/DashboardMultiFonteE2ETest.php` com namespace `Tests\Feature\Phase61`, `use RefreshDatabase`.

    setUp: mockar `AdmanService` no container para evitar HTTP outbound: `$this->app->instance(\App\Services\AdmanService::class, Mockery::mock(\App\Services\AdmanService::class)->shouldReceive('getCachedGrossBillingsMany','getCachedAccountMetricsMany')->andReturn([])->getMock());` — dashboards usam esses métodos em cache lookup legacy. Retornando array vazio, cai no fallback SUM DB (que também vazio para fixtures não-Adman), gerando payload com métricas null mas sem quebrar.

    Contrato de `admin()` helper privado: retorna `User::factory()->create(['role' => 'admin', 'active' => true])`.

    Filtros hardcoded do dashboard (linha 194-206 de DashboardController) exigem que empresas tenham contrato de serviço setor='performance'. Fixture helper `attachPerformanceContract($company)` — replicar padrão usado em `Phase37CompaniesPerformanceFilterTest` (adicionar contrato ativo em servico com setor=Servico::SETOR_PERFORMANCE via factory ou insertRaw). Sem isso, as fixtures não aparecem no dashboard.

    Precondição também: `whereDoesntHave('mlbEmpresa')` — as fixtures não devem ter `MlbEmpresa` associada, o que é natural em factories básicas.

    Cada teste segue estrutura: setUp local (fixtures do caso) → Config::set (flag) → actingAs(admin) → GET rota → assertInertia com asserts do behavior.

    **Constraints hard:**
    - NÃO modificar código de produção
    - NÃO chamar `MercadoLivreService` real (mock em service container se necessário)
    - Fixtures RefreshDatabase (SQLite in-memory)
    - Namespace exato `Tests\Feature\Phase61`
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase61/DashboardMultiFonteE2ETest.php --stop-on-failure</automated>
  </verify>
  <done>
    - 5/5 testes verdes
    - Namespace correto
    - Fixtures cobrem os 3 casos + none
    - Config toggle testado
  </done>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase61/DashboardMultiFonteE2ETest.php`
    - `grep -c "public function test_" tests/Feature/Phase61/DashboardMultiFonteE2ETest.php` >= 5
    - `grep -c "Config::set('metrics.unified_metrics_enabled'" tests/Feature/Phase61/DashboardMultiFonteE2ETest.php` >= 5 (uma por teste)
    - `grep -c "assertInertia" tests/Feature/Phase61/DashboardMultiFonteE2ETest.php` >= 5
    - `grep -c "'source'\|'source_counts'" tests/Feature/Phase61/DashboardMultiFonteE2ETest.php` >= 4
    - `php artisan test tests/Feature/Phase61/DashboardMultiFonteE2ETest.php` retorna passed 5
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PortfolioMultiFonteE2ETest — 6 casos (Show + Carteiras × 3 fontes + none + flag OFF)</name>
  <files>tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php</files>
  <read_first>
    - app/Http/Controllers/PortfolioController.php (rotas + shape do payload)
    - .planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-01-PLAN.md (contrato de `companies[].source` e `user_portfolios[].source_counts`)
    - .planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-04-PLAN.md (guardas frontend `c.source &&` e `u.source_counts &&`)
    - tests/Feature/Phase60/BaselineRegressionTest.php (padrão actingAs para portfolio.own)
  </read_first>
  <behavior>
    - Test 1 `test_flag_off_portfolio_show_nao_expoe_source`: Config OFF; analista fixture com 2 empresas (1 adman, 1 ml); GET `portfolio.show` → 200 + missing `companies.0.source`
    - Test 2 `test_flag_on_portfolio_show_expoe_source_por_empresa`: Config ON; mesmo fixture; GET → 200 + `companies.0.source` presente, valor ∈ {adman,ml,unified,none}
    - Test 3 `test_flag_on_portfolio_show_empresa_ml_only_nao_quebra_render`: Config ON; analista com empresa só-ML (sem AdmanMetric); GET → 200 sem exception; empresa aparece na lista com `source='ml'`
    - Test 4 `test_flag_on_portfolio_carteiras_admin_expoe_source_counts_por_user`: Config ON; admin autentica; 1 analista com 3 empresas (1 ml, 1 adman, 1 unified); GET `portfolio.own` → 200 + `user_portfolios.0.source_counts` presente com `{ml:1, adman:1, unified:1, none:0}`
    - Test 5 `test_flag_off_portfolio_carteiras_admin_nao_expoe_source_counts`: Config OFF; mesmo cenário; GET → 200 + missing `user_portfolios.0.source_counts`
    - Test 6 `test_flag_on_portfolio_show_empresa_none_aparece`: Config ON; analista com 1 empresa sem integração; GET → 200 + empresa aparece na lista com `source='none'` (não é omitida)
  </behavior>
  <action>
    Criar test file `tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php` com namespace correto e RefreshDatabase.

    Mockar `AdmanService` (mesmo padrão do Task 1) e `PortfolioScoreService`: `$this->app->instance(\App\Services\PortfolioScoreService::class, Mockery::mock(...)->shouldReceive('compute')->andReturn(['score' => 0, 'metricas' => []])->getMock());` — evita cálculo de score complexo em fixtures mínimas.

    Fixtures: helper `createAnalistaWith(int $qtdMlOnly, int $qtdAdmanOnly, int $qtdAmbos, int $qtdNone): User` que cria analista + N empresas de cada tipo + pivot `company_users` com role='consultor' + user_setores com cargo='analista' (para renderCarteirasConsolidadas encontrar via query `->join('cargos as c', 'c.id', '=', 'us.cargo_id')->where('c.slug', 'analista')` — linha 101-103 do controller).

    Rotas:
    - Test 1/2/3/6: `route('portfolio.show', $analista->id)` — admin autenticado
    - Test 4/5: `route('portfolio.own')` — admin autenticado (renderiza Carteiras consolidadas)

    Cada teste: Config::set → actingAs(admin) → GET → assertInertia.

    Para o assert de `source_counts`: `->where('user_portfolios.0.source_counts.ml', 1)->where('user_portfolios.0.source_counts.adman', 1)`.

    **Constraints hard:**
    - Zero HTTP outbound (mocks em service container)
    - Fixtures cobrem exatamente os 3 casos + none
    - Namespace exato
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php --stop-on-failure</automated>
  </verify>
  <done>
    - 6/6 testes verdes
    - Portfolio/Show renderiza sem crash em todos os casos
    - Portfolio/Carteiras expõe `source_counts` só quando flag ON
  </done>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php`
    - `grep -c "public function test_" tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php` >= 6
    - `grep -c "portfolio.show\|portfolio.own" tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php` >= 4
    - `grep -c "source_counts\|companies.0.source" tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php` >= 5
    - `php artisan test tests/Feature/Phase61/PortfolioMultiFonteE2ETest.php` retorna passed 6
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FeatureFlagRegressionTest — delta zero quando flag OFF em rotas afetadas</name>
  <files>tests/Feature/Phase61/FeatureFlagRegressionTest.php</files>
  <read_first>
    - tests/Feature/Phase60/BaselineRegressionTest.php (padrão smoke tests 200 + delta zero)
    - .planning/phases/60-base-multi-fonte-backend-ml-adman-unificado/60-04-SUMMARY.md ("Delta legacy = 0 auditado")
    - config/metrics.php (verificar valor default)
  </read_first>
  <behavior>
    - Test 1 `test_flag_default_false`: `config('metrics.unified_metrics_enabled')` retorna `false` por default (sem env setada)
    - Test 2 `test_flag_off_route_dashboard_ml_permanece_200`: Config OFF; admin autentica; GET `mercadolivre.dashboard` → 200 (sem regressão de status)
    - Test 3 `test_flag_off_route_portfolio_own_permanece_200`: Config OFF; admin; GET `portfolio.own` → 200
    - Test 4 `test_flag_off_route_companies_show_permanece_200`: Config OFF; admin; empresa fixture; GET `companies.show` → 200 (NOTA: `companies.show` sempre expõe `company.source` porque 61-03 removeu a flag desse enriquecimento estético; a regressão aqui é apenas de status HTTP, não do shape do payload)
    - Test 5 `test_baseline_phase60_ainda_verde`: chama programaticamente `$this->artisan('test', ['--filter' => 'Phase60'])` ou assert manual instanciando cada test do Phase 60 — alternativa mais pragmática: apenas rodar `\Artisan::call('test', ['--filter' => 'BaselineRegressionTest'])` e assert que assertRunOk.

    (Test 5 pode ser deferido — ver constraints)
  </behavior>
  <action>
    Criar `tests/Feature/Phase61/FeatureFlagRegressionTest.php` com namespace correto + RefreshDatabase.

    Mockar `AdmanService` (mesmo padrão dos Tasks 1 e 2).

    Tests 2-4 são smoke tests simples: Config::set OFF + actingAs admin + GET rota + `assertOk()`.

    Test 1 é declarativo: `$this->assertFalse((bool) config('metrics.unified_metrics_enabled'))` sem tocar em Config::set (verifica default via config file).

    Test 5 DEFERIDO — em vez de rodar suite via artisan (complexo em teste isolado), documentar no `<verify>` do plan que o executor deve rodar `php artisan test tests/Feature/Phase60` manualmente após concluir o Task 3 e reportar contagem no SUMMARY.

    Fixtures mínimas para Test 4: `Company::factory()->create(['adman_account_id' => 'X'])` — não precisa preencher AdmanMetric.

    **Constraints hard:**
    - Zero HTTP outbound
    - Testes rápidos (< 5s cada — smoke apenas)
    - Namespace exato
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase61/FeatureFlagRegressionTest.php --stop-on-failure &amp;&amp; php artisan test tests/Feature/Phase60</automated>
  </verify>
  <done>
    - 4 testes verdes (Test 5 deferido para verify manual)
    - Todas as rotas afetadas retornam 200 com flag OFF
    - Baseline Phase 60 continua 46/46 (verificado no verify command combinado)
  </done>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase61/FeatureFlagRegressionTest.php`
    - `grep -c "public function test_" tests/Feature/Phase61/FeatureFlagRegressionTest.php` >= 4
    - `grep -c "assertOk\|assertStatus(200)" tests/Feature/Phase61/FeatureFlagRegressionTest.php` >= 3
    - `grep -c "config('metrics.unified_metrics_enabled')" tests/Feature/Phase61/FeatureFlagRegressionTest.php` >= 1
    - `php artisan test tests/Feature/Phase61/FeatureFlagRegressionTest.php` retorna passed 4
    - `php artisan test tests/Feature/Phase60` continua passed 46
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → controller | fixtures precisam refletir os 3 casos + none SEM chamar serviços externos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-06-01 | Tampering | Testes fazem HTTP outbound acidental para ML/Adman | mitigate | Mock explícito de `AdmanService` no container em setUp de todos os tests; `Http::preventStrayRequests()` opcional (padrão Phase 40) |
| T-61-06-02 | Availability | Falso positivo por SQLite não-transacional | accept | `use RefreshDatabase` reseta a base entre testes; padrão Phase 60 validado |
| T-61-06-03 | Repudiation | Passa localmente mas falha em CI | mitigate | Fixtures não dependem de fixture cache ou tempo local; período fixado em `now()->subDay()` explicitamente |
| T-61-06-SC | Tampering (supply chain) | Nenhum install | n/a | Reusa Mockery/PHPUnit já em composer.lock |
</threat_model>

<verification>
1. `php artisan test tests/Feature/Phase61` — todos os tests dos 3 arquivos + os testes das 61-01/61-03 = >= 27 passed (14 do 61-01 + 4 do 61-03 + 5+6+4 = 15 deste plan; combinar totaliza >= 29)
2. `php artisan test tests/Feature/Phase60` — continua 46/46 (baseline intacto)
3. Cada teste roda em < 5s (fixtures mínimas + mocks)
4. Zero HTTP outbound (nenhuma request para ml_url ou adman_url em logs)
</verification>

<success_criteria>
- 3 test files novos com 15 testes cobrindo os 4 SC da Phase 61 nos 3 casos + none + flag OFF
- Baseline Phase 60 intacto
- Cada assert observável (não visual/subjetivo)
- Documentação de próximos passos no SUMMARY: (a) virar flag ON em produção; (b) monitorar logs de divergência TACOS
</success_criteria>

<output>
Criar `.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-06-SUMMARY.md` ao final + `PHASE-SUMMARY.md` final da Phase 61 consolidando os 6 plans.
</output>
