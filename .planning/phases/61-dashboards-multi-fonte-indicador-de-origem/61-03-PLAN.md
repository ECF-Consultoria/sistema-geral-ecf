---
phase: 61-dashboards-multi-fonte-indicador-de-origem
plan: 03
type: execute
wave: 2
depends_on: [61-02]
files_modified:
  - resources/js/Pages/Companies/Show.jsx
autonomous: true
requirements: [DASH-06, DATA-05]
tags: [frontend, badge, carteira-individual, working-tree-safe]
must_haves:
  truths:
    - "Carteira individual `/companies/{id}` exibe `<SourceBadge>` no header ao lado do nome da empresa"
    - "Empresa com `mlToken` ativo renderiza badge `ML` (variant `ml`) — cobrindo o SC #3 do ROADMAP para DASH-06"
    - "Empresa sem `mlToken` ativo mas com `adman_account_id` renderiza badge `Adman`"
    - "Empresa com AMBOS renderiza badge `Agregado`"
    - "Empresa sem integração renderiza badge `Sem integração`"
    - "Working tree pré-existente em `Companies/Show.jsx` NÃO é sobrescrito — plan altera APENAS a região do header entre linhas ~532-570"
  artifacts:
    - path: "resources/js/Pages/Companies/Show.jsx"
      provides: "Header enriquecido com SourceBadge da fonte de origem da empresa"
      contains: "SourceBadge"
  key_links:
    - from: "Companies/Show.jsx header"
      to: "@/Components/ui/source-badge"
      via: "import SourceBadge"
      pattern: "import.*SourceBadge"
    - from: "CompanyController::show"
      to: "Companies/Show.jsx props"
      via: "prop `company.source` (adman|ml|unified|none) — derivada via `MetricsProviderFactory::caseFor`"
      pattern: "company.source"
---

<objective>
Cumprir DASH-06 do ROADMAP: exibir badge visual "ML" (e variantes correlatas) ao lado do nome da empresa no header da carteira individual `/companies/{id}`. Costurado sem tocar o resto do arquivo — o executor deve limitar edições à região do header (linhas ~532-570) e ao top do arquivo (imports).

Purpose: SC #3 do ROADMAP Phase 61 ("Carteira individual exibe badge visual 'ML' ao lado do nome de cada empresa conectada ao Mercado Livre"). O ADR DATA-04 já formaliza o vocabulário; o Phase 60 já entrega o `caseFor()`; falta apenas o pipeline backend→prop→badge.

Output: 
1. `CompanyController::show` retorna prop `company.source` derivada de `MetricsProviderFactory::caseFor()` (SEM feature flag — este é um badge estético que ROADMAP define como SC obrigatório da Phase 61, INDEPENDENTE do flag). 
2. `Companies/Show.jsx` importa `SourceBadge` e renderiza no header ao lado do `<h1>` da empresa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/adrs/DATA-04-precedencia-multifonte.md
@.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-02-PLAN.md
@app/Http/Controllers/CompanyController.php
@app/Services/Metrics/MetricsProviderFactory.php
@resources/js/Pages/Companies/Show.jsx
@resources/js/Components/ui/source-badge.jsx

<interfaces>
Contrato `<SourceBadge>` (entregue em 61-02):
```jsx
import { SourceBadge } from '@/Components/ui/source-badge';
<SourceBadge variant="ml" />               // "ML" fundo amarelo ML
<SourceBadge variant="adman" />            // "Adman" cinza discreto
<SourceBadge variant="unified" />          // "Agregado" transparente com borda amarela
<SourceBadge variant="none" />             // "Sem integração" outline vazio
```

Header atual de `Companies/Show.jsx` (linhas 532-570 — REGIÃO ÚNICA a editar):
```jsx
<div className="card-ecf rounded-2xl p-6 flex items-start justify-between gap-4">
    <div className="flex items-center gap-4">
        <div className="w-12 h-12 ...">
            <Building2 size={20} className="text-ecf-yellow/70" />
        </div>
        <div>
            <h1 className="...">{company.name}</h1>
            <div className="flex items-center gap-2 mt-1 flex-wrap">
                {company.segment && ...}
                {company.cnpj && ...}
                {company.adman_account_id && (
                    <span className="... font-mono ...">cust {company.adman_account_id}</span>
                )}
            </div>
        </div>
    </div>
    <div className="flex items-center gap-2 shrink-0">
        <Link href={route('sugadores.config.show', company.id)} ... />
        <Badge variant={company.active ? 'success' : 'destructive'}>...</Badge>
    </div>
</div>
```

Ponto de inserção do `<SourceBadge>`: dentro do `<div className="flex items-center gap-2 shrink-0">` no final do header (junto com o badge de Ativa/Inativa) — mantém a consistência visual do agrupamento de badges no lado direito. Alternativa: adicionar ao lado do `<h1>` diretamente. Escolher a alternativa que MENOS altera linhas do working tree existente.

Working tree caveat: `git status` mostra `Companies/Show.jsx` já modificado (untracked-from-scope). O executor NÃO deve fazer commits que sobrescrevam outras seções; APENAS a região do header + imports podem receber edit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — CompanyController::show expõe `company.source` derivado do factory</name>
  <files>app/Http/Controllers/CompanyController.php, tests/Feature/Phase61/CompanyShowSourceTest.php</files>
  <read_first>
    - app/Http/Controllers/CompanyController.php (localizar método `show` e o `Inertia::render('Companies/Show', ...)` correspondente; procurar pela prop `company` sendo passada)
    - app/Services/Metrics/MetricsProviderFactory.php (método `caseFor` — mapeamento 4 casos)
    - app/Models/Company.php linhas 74-93 (accessors `cust_id` e `is_ml_driven`)
    - resources/js/Pages/Companies/Show.jsx linhas 365-425 (função `CompanyShow` — verificar como `company` é desestruturado nas props)
  </read_first>
  <action>
    Localizar em `CompanyController.php` o método `show(Company $company)` (ou equivalente que renderiza `Companies/Show`). Injetar `MetricsProviderFactory $factory` via DI: adicionar como parâmetro do método (Laravel resolve automaticamente) OU adicionar no constructor se já existir constructor no controller.

    Antes do `Inertia::render('Companies/Show', [...])`, calcular `$source = match($factory->caseFor($company)) { 'ambos'=>'unified', 'so-ml'=>'ml', 'so-adman'=>'adman', 'none'=>'none' };`.

    Adicionar `'source' => $source` ao array serializado da prop `company` — como o payload atual passa `$company->toArray()` ou similar, o mais seguro é fazer merge no array final antes do render, na chave `company.source`. Verificar exatamente como as props são montadas para não quebrar contratos existentes.

    Este enriquecimento é UNCONDITIONAL (sem feature flag) — porque DASH-06 é SC obrigatório da Phase 61 no ROADMAP e não faz sentido esconder um badge estético atrás de flag. Justificativa: `caseFor()` é I/O-free (só accessor read), não polui logs, não incrementa custo de rota — safe by default.

    Criar test `tests/Feature/Phase61/CompanyShowSourceTest.php` com 4 casos:
    - Test 1: Empresa fixture só-Adman → GET `/companies/{id}` retorna 200 + payload contém `company.source === 'adman'`
    - Test 2: Empresa fixture só-ML (mlToken active + adman_account_id NULL) → `company.source === 'ml'`
    - Test 3: Empresa fixture ambos → `company.source === 'unified'`
    - Test 4: Empresa fixture nenhum → `company.source === 'none'`

    Usar `AssertableInertia` (`$response->assertInertia(fn($p) => $p->where('company.source', 'ml'))`).

    **Constraints hard:**
    - NÃO alterar outros métodos de `CompanyController.php` (`index`, `store`, `update`, `destroy` etc.)
    - NÃO modificar contract de `company` (chaves existentes preservadas — só adição de nova chave `source`)
    - NÃO chamar `readForCompany()` — só `caseFor()`
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase61/CompanyShowSourceTest.php --stop-on-failure</automated>
  </verify>
  <done>
    - 4/4 testes verdes
    - `CompanyController::show` retorna `company.source` nos 4 casos
    - Zero modificação em outros métodos do controller
    - Working tree de outras seções do controller preservado
  </done>
  <acceptance_criteria>
    - `grep -c "MetricsProviderFactory" app/Http/Controllers/CompanyController.php` >= 1
    - `grep -c "'source' =>" app/Http/Controllers/CompanyController.php` >= 1
    - `grep -c "caseFor" app/Http/Controllers/CompanyController.php` >= 1
    - Test file existe com >= 4 métodos `test_`
    - `git diff app/Http/Controllers/CompanyController.php | grep -c "^+"` <= 30 linhas (edit mínimo)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Frontend — Companies/Show.jsx renderiza `<SourceBadge>` no header (edição cirúrgica)</name>
  <files>resources/js/Pages/Companies/Show.jsx</files>
  <read_first>
    - resources/js/Pages/Companies/Show.jsx linhas 1-20 (imports atuais)
    - resources/js/Pages/Companies/Show.jsx linhas 522-570 (região do header — a ÚNICA região que pode receber edit)
    - resources/js/Components/ui/source-badge.jsx (contrato do componente entregue em 61-02)
    - CLAUDE.md seção "Design system" (tokens ecf-*, dark theme first)
  </read_first>
  <action>
    Edição CIRÚRGICA. Working tree do arquivo tem alterações untracked de outro escopo — respeitar.

    Passo 1: No topo do arquivo (bloco de imports linhas 1-17), adicionar UMA linha nova: `import { SourceBadge } from '@/Components/ui/source-badge';`. Colocar logo depois do import do `Badge` existente (linha 3) para preservar agrupamento.

    Passo 2: Na região do header (linhas 532-570), no `<div className="flex items-center gap-2 shrink-0">` (linha ~557) que contém o Link Sugadores + Badge Ativa/Inativa, INSERIR o `<SourceBadge>` como PRIMEIRO filho desse div (antes do `<Link>`). Renderização:
    ```jsx
    <SourceBadge variant={company.source ?? 'none'} />
    ```

    Fallback defensivo `?? 'none'`: se por qualquer motivo `company.source` vier undefined (backward compat com caches ou payloads antigos), o componente cai em variant 'none' em vez de quebrar.

    Passo 3: NÃO alterar `<h1>{company.name}</h1>`. NÃO alterar o cust_id badge amarelo existente (linha 544-553). NÃO alterar Badge de Ativa/Inativa. NÃO alterar botão Sugadores. NENHUMA edit fora do header + imports.

    Passo 4: Rodar `npm run build` para confirmar compile verde.

    **Constraints hard:**
    - Máximo 2 linhas adicionadas (1 import + 1 render)
    - Zero linha removida
    - `git diff resources/js/Pages/Companies/Show.jsx` deve mostrar APENAS insertions no bloco de imports e no header — NENHUM diff em outras seções do arquivo (KPIs, tabs, cards, modais)
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -c "SourceBadge" resources/js/Pages/Companies/Show.jsx</automated>
  </verify>
  <done>
    - `npm run build` compila sem warning
    - `<SourceBadge>` presente no header
    - Diff mínimo (2 adições, 0 remoções)
    - Working tree existente de outras seções não afetado
  </done>
  <acceptance_criteria>
    - `grep -c "import { SourceBadge }" resources/js/Pages/Companies/Show.jsx` == 1
    - `grep -c "SourceBadge variant" resources/js/Pages/Companies/Show.jsx` == 1
    - `git diff resources/js/Pages/Companies/Show.jsx | grep -c "^+"` <= 5 (adições mínimas)
    - `git diff resources/js/Pages/Companies/Show.jsx | grep -c "^-"` == 0 (zero remoção)
    - `npm run build` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → view | `company.source` untrusted enum string chega ao componente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03-01 | Tampering | Prop `company.source` undefined ou lixo | mitigate | `variant={company.source ?? 'none'}` — fallback defensivo; cva default em SourceBadge também vale como segunda linha |
| T-61-03-02 | Information Disclosure | Badge no header | accept | Só string enum pública ('ml','adman','unified','none'); sem token/cust_id/PII |
| T-61-03-SC | Tampering (supply chain) | Nenhum install | n/a | Reusa componente 61-02; nenhum `npm install`/`composer require` |
</threat_model>

<verification>
1. `npm run build` verde
2. `php artisan test tests/Feature/Phase61/CompanyShowSourceTest.php` — 4 passed
3. Manual (documentar no SUMMARY): abrir `/companies/{id_de_empresa_ml_only}` em prod-like → badge "ML" visível no header
4. Working tree de outras seções de `Companies/Show.jsx` intacto (`git diff --stat` mostra apenas edit no header)
5. `git diff CompanyController.php` mostra APENAS enriquecimento no método `show`
</verification>

<success_criteria>
- DASH-06 fechado: badge visual de fonte no header da carteira individual
- Working tree existente preservado
- Sem regressão em outras seções da tela
- Backend enriquecimento em 1 método pontual
</success_criteria>

<output>
Criar `.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-03-SUMMARY.md` ao final.
</output>
