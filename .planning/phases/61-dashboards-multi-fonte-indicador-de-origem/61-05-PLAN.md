---
phase: 61-dashboards-multi-fonte-indicador-de-origem
plan: 05
type: execute
wave: 2
depends_on: [61-01, 61-02]
files_modified:
  - resources/js/Pages/Dashboard/Admin.jsx
autonomous: true
requirements: [DASH-04, DATA-05]
tags: [frontend, dashboard-ml, kpi, badge, agregacao-multifonte]
must_haves:
  truths:
    - "Dashboard `/dashboard/mercadolivre` (renderiza `Dashboard/Admin.jsx` com marketplace=meli) exibe KPIs de topo com `<SourceBadge variant='unified'>` (label 'Agregado') quando o payload inclui `source_counts` mixando fontes"
    - "Cada linha da tabela de empresas no dashboard admin exibe `<SourceBadge>` ao lado do nome (variant vem de `c.source` já enriquecido em 61-01)"
    - "Legenda mini no topo mostra a distribuição: 'N ML · M Adman · K Agregado · L Sem integração' usando `stats.source_counts` (guarda defensiva quando undefined)"
    - "Empresas ML-only ou source='none' aparecem na tabela sem crash; métricas ausentes viram '—' (padrão CustIdInvalido existente)"
    - "Sem flag ON, dashboard renderiza IDÊNTICO ao pré-Phase-61 (guarda `c.source && `)"
  artifacts:
    - path: "resources/js/Pages/Dashboard/Admin.jsx"
      provides: "KPIs com badge Agregado + tabela de empresas com badge por linha + legenda de fontes no topo"
      contains: "SourceBadge"
  key_links:
    - from: "Dashboard/Admin.jsx"
      to: "@/Components/ui/source-badge"
      via: "import SourceBadge"
      pattern: "SourceBadge"
    - from: "Dashboard/Admin.jsx"
      to: "backend payload stats.source_counts + companies_performance[].source (enriquecidos em 61-01)"
      via: "renderização condicional"
      pattern: "source_counts"
---

<objective>
Cumprir DASH-04 do ROADMAP: "Dashboard Mercado Livre (`/dashboard/mercadolivre`) exibe KPI unificado que soma empresas fonte-ML + fonte-Adman num único número, sem duplicar nem ignorar". Complementarmente, aplicar `<SourceBadge>` em cada linha da tabela + legenda agregada no topo (DATA-05).

Purpose: Hoje o dashboard já soma todas empresas em um único KPI (via SUM de `AdmanMetric` + híbrido cache Adman) mas SEM COMUNICAR isso ao operador. O usuário vê "Faturamento total 30d" e não sabe se aquilo é só-Adman (que exclui ML-only) ou realmente agregado. A Phase 61 costura a comunicação visual sem alterar o cálculo — os KPIs já são unificados por natureza (soma sobre todas empresas do escopo), o badge apenas EXPLICITA que é agregado quando existem múltiplas fontes.

Output: 1 arquivo frontend modificado. Header do dashboard ganha legenda agregada; tabela de empresas ganha badge por linha.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/adrs/DATA-04-precedencia-multifonte.md
@.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-01-PLAN.md
@.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-02-PLAN.md
@resources/js/Pages/Dashboard/Admin.jsx
@resources/js/Components/ui/source-badge.jsx

<interfaces>
Payload backend enriquecido em 61-01 (`DashboardController::adminDashboard` com `UNIFIED_METRICS_ENABLED=true`):

- Prop `companies_performance[]` recebe: `{ id, name, cust_id_status, revenue, ad_spend, tacos, margin, ..., source?: 'adman'|'ml'|'unified'|'none' }` (nome real da chave no `Inertia::render` de `DashboardController::adminDashboard`)
- Prop `stats` recebe: `{ total_companies, avg_tacos, total_revenue, ..., source_counts?: {adman: int, ml: int, unified: int, none: int} }` (chave real do payload; NÃO chamar de `summary`)

**Tolerância obrigatória:** Sem flag ON, `source` e `source_counts` são undefined → UI degrada sem badges (comportamento anterior).

Estrutura atual de `Dashboard/Admin.jsx`:
- Linhas 1-95: imports + helpers + `CustIdInvalidoBadge` local
- Linha 94: `export default function AdminDashboard({ stats, companies_performance, period, ... })` (nomes REAIS das props — NÃO usar `companies`/`summary`)
- Linhas 319-325: badge D-1 Adman (Sync info)
- Linhas 512-515: renderização de `CustIdInvalidoBadge` inline ao lado de `c.name` na tabela — MODELO EXATO para inserir `SourceBadge`

Contrato `<SourceBadge>` (61-02):
```jsx
<SourceBadge variant="ml|adman|unified|none" />
```

Regra de UX (feedback do usuário `feedback_evitar_jargao_ui`): NUNCA mostrar "unified" cru. `<SourceBadge variant="unified" />` já resolve isso (label "Agregado"). Legenda inline do topo deve seguir mesmo vocabulário — nunca escrever "unified" no JSX.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard/Admin.jsx — SourceBadge por linha + legenda `source_counts` no topo</name>
  <files>resources/js/Pages/Dashboard/Admin.jsx</files>
  <read_first>
    - resources/js/Pages/Dashboard/Admin.jsx linhas 1-30 (imports)
    - resources/js/Pages/Dashboard/Admin.jsx linhas 94-100 (signature + desestruturação de props)
    - resources/js/Pages/Dashboard/Admin.jsx linhas 300-330 (região de header com badges Sync D-1 — modelo de "chip" existente)
    - resources/js/Pages/Dashboard/Admin.jsx linhas 505-520 (renderização de linha da tabela com `c.name` + `CustIdInvalidoBadge` — modelo para inserir SourceBadge)
    - resources/js/Components/ui/source-badge.jsx (contrato 61-02)
  </read_first>
  <action>
    Passo 1: Adicionar import no bloco existente (após linha ~11): `import { SourceBadge } from '@/Components/ui/source-badge';`.

    Passo 2: Verificar que `stats` já está no bloco de props (linhas 94-100 destruturam `stats` e `companies_performance` — nomes REAIS). Ler `source_counts` de `stats.source_counts` com fallback `null`. Padrão sugerido no início do component body:
    ```jsx
    const sourceCounts = stats?.source_counts ?? null;
    ```

    Passo 3: Adicionar legenda agregada no header (procurar região próxima do badge Sync D-1, linhas ~319-325). Inserir bloco condicional discreto:
    ```jsx
    {sourceCounts && (
        <div className="flex items-center gap-2 text-[11px] text-white/50 flex-wrap" data-testid="dashboard-source-legend">
            <span className="uppercase tracking-wider">Fontes:</span>
            {sourceCounts.ml > 0 && <span className="inline-flex items-center gap-1"><SourceBadge variant="ml" />{sourceCounts.ml}</span>}
            {sourceCounts.unified > 0 && <span className="inline-flex items-center gap-1"><SourceBadge variant="unified" />{sourceCounts.unified}</span>}
            {sourceCounts.adman > 0 && <span className="inline-flex items-center gap-1"><SourceBadge variant="adman" />{sourceCounts.adman}</span>}
            {sourceCounts.none > 0 && <span className="inline-flex items-center gap-1"><SourceBadge variant="none" />{sourceCounts.none}</span>}
        </div>
    )}
    ```
    Ordem canônica: ML → Agregado → Adman → Sem integração (prioriza fontes ricas primeiro).

    Passo 4: Adicionar `<SourceBadge>` na linha da tabela ao lado de `c.name`. Localizar o padrão `<CustIdInvalidoBadge status={c.cust_id_status} />` (linha ~515) e adicionar IMEDIATAMENTE APÓS ele (mesma célula do nome):
    ```jsx
    {c.source && <SourceBadge variant={c.source} />}
    ```
    A guarda `c.source &&` protege contra flag OFF.

    Passo 5: `npm run build` verde.

    **Constraints hard:**
    - Máximo 20 linhas adicionadas ao arquivo inteiro
    - Zero linha removida
    - NÃO alterar KPIs de topo (Faturamento, Invest. Ads, TACOS médio — o cálculo permanece; adiciona apenas legenda ABAIXO/AO LADO do bloco)
    - NÃO alterar filtros (period, marketplace, company_id, consultor_id, estrategista_id, group_id)
    - NÃO tocar em `CustIdInvalidoBadge` local (linhas 38-90 aproximadamente)
    - NÃO alterar renderização da tabela (colunas, sort, empty state)
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -c "SourceBadge" resources/js/Pages/Dashboard/Admin.jsx</automated>
  </verify>
  <done>
    - `npm run build` verde
    - Import de `SourceBadge` presente
    - Legenda `source_counts` no header (guarda defensiva presente)
    - Badge por linha na tabela (ao lado do `CustIdInvalidoBadge`)
    - `data-testid="dashboard-source-legend"` presente (para asserts do 61-06)
    - Diff mínimo (<= 20 linhas +)
  </done>
  <acceptance_criteria>
    - `grep -c "import { SourceBadge }" resources/js/Pages/Dashboard/Admin.jsx` == 1
    - `grep -c "SourceBadge variant" resources/js/Pages/Dashboard/Admin.jsx` >= 5 (1 tabela + 4 legenda variantes)
    - `grep -c "source_counts" resources/js/Pages/Dashboard/Admin.jsx` >= 1
    - `grep -c "sourceCounts" resources/js/Pages/Dashboard/Admin.jsx` >= 4 (declaração + 4 checks)
    - `grep -c "dashboard-source-legend" resources/js/Pages/Dashboard/Admin.jsx` == 1
    - `git diff resources/js/Pages/Dashboard/Admin.jsx | grep -c "^-"` == 0
    - `git diff resources/js/Pages/Dashboard/Admin.jsx | grep -c "^+"` <= 25
    - `npm run build` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → view | `source` / `source_counts` opcionais no payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05-01 | Tampering | `stats.source_counts` sem chaves esperadas | mitigate | Guards `sourceCounts?.ml > 0` por variante — chave faltando cai como falsy safe |
| T-61-05-02 | Availability | Ordem visual das fontes confusa | mitigate | Ordem canônica declarada explicitamente (ML → Agregado → Adman → Sem integração) — evita reshuffle por object key order do JS |
| T-61-05-03 | Information Disclosure | Legenda expõe distribuição da base | accept | Contagens agregadas por variante — nenhum cust_id, empresa individual ou PII exposto |
| T-61-05-SC | Tampering (supply chain) | Nenhum install | n/a | Reusa 61-02; sem `npm install` |
</threat_model>

<verification>
1. `npm run build` verde
2. Flag OFF: dashboard renderiza sem legenda de fontes nem badges na tabela (backward compat)
3. Flag ON: legenda aparece no header + cada linha tem badge
4. Empresa fixture com `source='ml'` na tabela → badge amarelo ML visível
5. Empresa fixture com `source='none'` → badge cinza "Sem integração" visível
6. Diff mínimo — nenhuma alteração fora do header/tabela
</verification>

<success_criteria>
- DASH-04 fechado no aspecto UX: KPIs comunicam que são agregação multi-fonte (legenda visível)
- DATA-05 avançado: badges de fonte por empresa na tabela
- Backend do KPI não alterado (soma continua sendo hybrid cache Adman + SUM DB — Phase 61 não muda cálculo)
- Backward compat via guardas `sourceCounts &&` + `c.source &&`
</success_criteria>

<output>
Criar `.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-05-SUMMARY.md` ao final.
</output>
