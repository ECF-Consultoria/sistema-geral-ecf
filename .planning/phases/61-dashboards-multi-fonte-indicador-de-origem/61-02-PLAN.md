---
phase: 61-dashboards-multi-fonte-indicador-de-origem
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/Components/ui/source-badge.jsx
autonomous: true
requirements: [DATA-05]
tags: [frontend, ui-primitive, shadcn, cva, badge]
must_haves:
  truths:
    - "Componente `<SourceBadge>` reusável importável via `@/Components/ui/source-badge` em qualquer página React"
    - "Prop `variant` obrigatória, aceita EXATAMENTE 4 valores: `ml`, `adman`, `unified`, `none` — enum sincronizado com ADR DATA-04"
    - "Variant `ml` renderiza rótulo `ML` com token amarelo Mercado Livre (`#FFE600`)"
    - "Variant `adman` renderiza rótulo `Adman` com token cinza neutro (`white/[0.6]`)"
    - "Variant `unified` renderiza rótulo `Agregado` (NÃO usar jargão 'unified') com token amarelo ECF"
    - "Variant `none` renderiza rótulo `Sem integração` com outline cinza fraco"
    - "Componente tem `title` HTML nativo com tooltip pt-BR explicativo por variant (não requer Radix Tooltip que não está no projeto)"
    - "Componente aceita `data-testid` via prop `data-testid` (default `source-badge-{variant}`) para uso em testes E2E"
  artifacts:
    - path: "resources/js/Components/ui/source-badge.jsx"
      provides: "Componente `<SourceBadge variant='ml|adman|unified|none' />` reusável"
      min_lines: 40
      contains: "export function SourceBadge"
  key_links:
    - from: "SourceBadge"
      to: "class-variance-authority + tailwind-merge (`cn` de @/lib/utils)"
      via: "cva pattern - alinhado com badge.jsx existente"
      pattern: "cva\\("
---

<objective>
Entregar o componente React reusável `<SourceBadge>` que renderiza a origem de uma métrica (`ml|adman|unified|none`) como badge visual com rótulo pt-BR, tooltip explicativo (via `title` nativo) e tokens de cor consistentes com o design system ECF. Este é o único primitivo visual consumido pelos plans 61-03, 61-04 e 61-05 — sem ele, cada consumidor inventaria variante local.

Purpose: Fixar vocabulário visual e evitar drift ("unified" na UI vira "Agregado" — regra `feedback_evitar_jargao_ui`; ADR DATA-04 declara 4 valores canônicos e devemos travar a tradução UI em um único lugar).

Output: 1 arquivo novo em `resources/js/Components/ui/source-badge.jsx` seguindo padrão shadcn/cva já estabelecido em `badge.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/adrs/DATA-04-precedencia-multifonte.md
@resources/js/Components/ui/badge.jsx
@resources/js/lib/utils.js
@tailwind.config.js

<interfaces>
Padrão cva + cn já estabelecido no projeto (extraído de `resources/js/Components/ui/badge.jsx`):

```jsx
import * as React from 'react';
import { cva } from 'class-variance-authority';
import { cn } from '@/lib/utils';

const badgeVariants = cva('inline-flex items-center rounded-full border px-2.5 py-0.5 text-xs font-semibold', {
    variants: {
        variant: {
            default: '...',
            success: 'border-transparent bg-green-600 text-white ...',
        },
    },
    defaultVariants: { variant: 'default' },
});
function Badge({ className, variant, ...props }) {
    return <div className={cn(badgeVariants({ variant }), className)} {...props} />;
}
export { Badge, badgeVariants };
```

Tokens ECF disponíveis em `tailwind.config.js` (`theme.extend.colors.ecf`):
- `ecf-yellow` = `#ffe600` — brand ECF (usar para `unified` = agregado ECF)
- `ecf-bg` = `#050507`, `ecf-card` = `#0f1116`
- Padrão de opacity: `bg-white/[0.03]`, `border-white/[0.08]`

Cor canônica Mercado Livre: `#FFE600` (amarelo ML) — MESMO tom do `ecf-yellow` por coincidência. Para diferenciar visualmente ML do agregado, usar shade diferente ou glyph. Solução simples: `ml` usa fundo sólido `bg-[#FFE600] text-black` (contraste forte); `unified` usa `bg-ecf-yellow/15 border-ecf-yellow/40 text-ecf-yellow` (transparente com borda).

Radix Tooltip NÃO está no package.json. Radix Popover está (@radix-ui/react-popover ^1.1.15) mas exige clique. Solução: usar atributo HTML nativo `title=""` para tooltip on-hover — funciona em todos os browsers, zero dependência nova.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar `<SourceBadge>` com 4 variantes + tooltip nativo + testid</name>
  <files>resources/js/Components/ui/source-badge.jsx</files>
  <read_first>
    - resources/js/Components/ui/badge.jsx (padrão cva + cn exato — copiar estrutura)
    - resources/js/lib/utils.js (verificar `cn` export)
    - tailwind.config.js (tokens `ecf.*` disponíveis)
    - .planning/adrs/DATA-04-precedencia-multifonte.md linhas 147-172 (vocabulário exato dos 4 valores de `source`)
  </read_first>
  <action>
    Criar arquivo novo `resources/js/Components/ui/source-badge.jsx` seguindo padrão cva de `badge.jsx`. Imports: `React`, `cva` de `class-variance-authority`, `cn` de `@/lib/utils`.

    Definir `sourceBadgeVariants = cva(baseClasses, { variants: { variant: { ml, adman, unified, none } }, defaultVariants: { variant: 'none' } })`. Base classes: `inline-flex items-center gap-1 rounded-full border px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wide`.

    Classes por variant (concreto):
    - `ml`:      `bg-[#FFE600] text-black border-transparent` (amarelo Mercado Livre sólido)
    - `adman`:   `bg-white/[0.06] text-white/70 border-white/[0.12]` (cinza neutro discreto)
    - `unified`: `bg-ecf-yellow/15 text-ecf-yellow border-ecf-yellow/40` (agregado ECF - transparente com borda amarela)
    - `none`:    `bg-transparent text-white/40 border-white/[0.15]` (outline vazio - sem integração)

    Definir mapa `LABEL` module-scope: `{ ml: 'ML', adman: 'Adman', unified: 'Agregado', none: 'Sem integração' }`.

    Definir mapa `TOOLTIP` module-scope (pt-BR):
    - `ml`: `'Métrica calculada a partir da API do Mercado Livre (fonte primária ADR DATA-04)'`
    - `adman`: `'Métrica calculada a partir do sync Adman (last-sync D-1)'`
    - `unified`: `'Métrica agregada: ML dita valores operacionais, Adman enriquece campos exclusivos (net_billing, taxes, etc)'`
    - `none`: `'Empresa sem integração ativa em nenhuma fonte'`

    Component signature: `function SourceBadge({ variant = 'none', className, showLabel = true, 'data-testid': testId, ...props })`. Renderizar `<span>` (NÃO `<div>` para permitir inline em headers). Composição:
    ```jsx
    <span
        className={cn(sourceBadgeVariants({ variant }), className)}
        title={TOOLTIP[variant]}
        data-testid={testId ?? `source-badge-${variant}`}
        {...props}
    >
        {showLabel ? LABEL[variant] : null}
    </span>
    ```

    Export: `export { SourceBadge, sourceBadgeVariants }` — mesmo padrão de `badge.jsx`.

    Adicionar comentário docblock pt-BR no topo do arquivo referenciando o ADR DATA-04 e explicando as 4 variantes. Uma linha sobre por que NÃO usar Radix Tooltip (não está no projeto; `title` nativo é suficiente e evita dependência nova).

    Rodar `npm run build` ao final para confirmar zero warning.

    **Constraints hard:**
    - NÃO modificar `badge.jsx` existente
    - NÃO adicionar dependência nova ao `package.json`
    - NÃO usar Radix Tooltip (não está no projeto)
    - Componente NÃO deve importar `Badge` de `badge.jsx` — é primitivo próprio para permitir composição em qualquer lugar (inclusive dentro de outras Badges)
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20 &amp;&amp; grep -c "export function SourceBadge\|export { SourceBadge" resources/js/Components/ui/source-badge.jsx</automated>
  </verify>
  <done>
    - Arquivo `resources/js/Components/ui/source-badge.jsx` existe com >= 40 linhas
    - `npm run build` compila sem warning/error
    - Componente `SourceBadge` exportado (nomeado, não default)
    - 4 variantes cobertas (ml/adman/unified/none)
    - Tooltip `title` nativo presente em cada variant
    - `data-testid` prop suportado (default `source-badge-{variant}`)
  </done>
  <acceptance_criteria>
    - `test -f resources/js/Components/ui/source-badge.jsx`
    - `grep -c "variant.*ml\|variant.*adman\|variant.*unified\|variant.*none" resources/js/Components/ui/source-badge.jsx` >= 4
    - `grep -c "Agregado\|Sem integração\|Adman\|ML" resources/js/Components/ui/source-badge.jsx` >= 4
    - `grep -c "title=" resources/js/Components/ui/source-badge.jsx` >= 1
    - `grep -c "data-testid" resources/js/Components/ui/source-badge.jsx` >= 1
    - `grep -c "@radix-ui/react-tooltip\|from '@radix-ui/react-popover'" resources/js/Components/ui/source-badge.jsx` == 0 (nenhuma dep nova)
    - `git diff package.json` retorna 0 linhas (nenhuma dependência adicionada)
    - `npm run build` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → view props | O valor de `variant` vem do payload Inertia (enriquecido no 61-01) — untrusted string do backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02-01 | Tampering | Prop `variant` inválida | mitigate | `defaultVariants: { variant: 'none' }` no cva — variant desconhecido cai em 'none' (fail-safe visual) em vez de quebrar render |
| T-61-02-02 | Information Disclosure | Tooltip contém dados sensíveis | mitigate | Tooltips estáticos pt-BR sem `company_id`/`cust_id`/token; só texto explicativo da fonte |
| T-61-02-SC | Tampering (supply chain) | Nenhum install | n/a | Reusa `class-variance-authority` e `clsx`/`tailwind-merge` já em package.json; nenhum `npm install` |
</threat_model>

<verification>
1. `npm run build` compila sem warning
2. `grep -c "SourceBadge" resources/js/Components/ui/source-badge.jsx` >= 2 (definição + export)
3. Todos os 4 variants ml/adman/unified/none definidos no cva
4. Nenhuma dependência nova em package.json
5. Padrão consistente com `badge.jsx` existente (cva + cn + export nomeado)
</verification>

<success_criteria>
- 1 arquivo novo, sem outras modificações
- Componente reusável importável via `import { SourceBadge } from '@/Components/ui/source-badge'`
- Vocabulário travado: 4 variantes com labels e tooltips pt-BR fixos
- Sem "unified" jargão na UI (mostra "Agregado")
- Sem dependência nova
</success_criteria>

<output>
Criar `.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-02-SUMMARY.md` ao final.
</output>
