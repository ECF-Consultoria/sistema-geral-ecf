---
phase: 61-dashboards-multi-fonte-indicador-de-origem
plan: 04
type: execute
wave: 2
depends_on: [61-01, 61-02]
files_modified:
  - resources/js/Pages/Portfolio/Show.jsx
  - resources/js/Pages/Portfolio/Carteiras.jsx
autonomous: true
requirements: [DASH-05, DATA-05]
tags: [frontend, portfolio, analista, estrategista, badge, tolerancia-fonte]
must_haves:
  truths:
    - "Dashboard do Analista/Estrategista (`/portfolio` para admin agregado, `/admin/users/{u}/portfolio` para carteira individual) renderiza SEM lançar erro quando empresa é ML-only ou sem integração"
    - "Cada linha de empresa em `Portfolio/Show.jsx` exibe `<SourceBadge>` ao lado do nome da empresa (variant vem de `company.source` do payload)"
    - "Empresas com `source === 'none'` aparecem visualmente cinza + CTA discreto (não são silenciosamente omitidas nem quebram a linha)"
    - "`Portfolio/Carteiras.jsx` (view consolidada admin) exibe distribuição de fontes por profissional (mini-legenda com contadores usando `source_counts`)"
    - "Quando flag `UNIFIED_METRICS_ENABLED=false` (default), Portfolio renderiza SEM badges/counts — payload sem `source` degrada para comportamento pré-Phase-61 (backward compat)"
  artifacts:
    - path: "resources/js/Pages/Portfolio/Show.jsx"
      provides: "Linhas de empresa com SourceBadge + tolerância a source='none'"
      contains: "SourceBadge"
    - path: "resources/js/Pages/Portfolio/Carteiras.jsx"
      provides: "Cards de profissional com contadores de fonte + tolerância a source_counts undefined"
      contains: "source_counts"
  key_links:
    - from: "Portfolio/Show.jsx"
      to: "@/Components/ui/source-badge"
      via: "import SourceBadge"
      pattern: "SourceBadge"
    - from: "Portfolio/Carteiras.jsx"
      to: "backend payload user.source_counts (enriquecido em 61-01 quando flag ON)"
      via: "renderização condicional"
      pattern: "source_counts"
---

<objective>
Cumprir DASH-05 do ROADMAP: "Dashboards de Analista e Estrategista renderizam a carteira sem lançar erro quando uma empresa é ML-only (sem Adman) — empresas Adman-only continuam aparecendo normalmente". Complementarmente, aplicar `<SourceBadge>` em cada linha/card para satisfazer DATA-05 na superfície Portfolio.

Purpose: Hoje, carteiras contendo empresas ML-only exibem métricas parciais/zeradas porque toda a leitura vem de `AdmanMetric::whereIn` (linha 260 de PortfolioController). Não quebra runtime, mas quebra a leitura mental do profissional (empresa some ou vira "empresa sem dados"). Com `source` no payload (61-01 backend + flag ON), a UI pode distinguir "empresa ML-only" (badge ML + explicação) de "empresa sem integração" (badge none + CTA).

Output: 2 arquivos frontend modificados. Show renderiza badge por empresa na tabela; Carteiras renderiza legenda de fontes no card do profissional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/adrs/DATA-04-precedencia-multifonte.md
@.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-01-PLAN.md
@.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-02-PLAN.md
@resources/js/Pages/Portfolio/Show.jsx
@resources/js/Pages/Portfolio/Carteiras.jsx
@resources/js/Components/ui/source-badge.jsx

<interfaces>
Payload backend enriquecido em 61-01 (só quando `UNIFIED_METRICS_ENABLED=true`):

- `Portfolio/Show.jsx` prop `companies[]` recebe: `{ id, name, role, tacos, revenue, ad_spend, ..., source?: 'adman'|'ml'|'unified'|'none' }`
- `Portfolio/Carteiras.jsx` prop `user_portfolios[]` recebe: `{ id, name, tipo, companies_count, avg_tacos, ..., source_counts?: {adman: int, ml: int, unified: int, none: int} }`

**Tolerância obrigatória:** Quando flag OFF, `source` e `source_counts` são undefined no payload. A UI DEVE renderizar sem quebrar — sem badge (comportamento anterior) em vez de crash.

Contrato `<SourceBadge>` (61-02):
```jsx
<SourceBadge variant="ml|adman|unified|none" />
```

Estrutura atual de `Portfolio/Show.jsx` (linha ~457-475): tabela de empresas via `empresasView` (result de useMemo). Cada linha da tabela renderiza colunas: name, tacos, revenue, ad_spend, etc. Ponto de inserção do badge: na célula do NOME da empresa (mesmo padrão do 61-03 header — badge lado direito do nome).

Estrutura atual de `Portfolio/Carteiras.jsx` (linhas 68-119): grid de cards, cada card mostra `u.name`, `tipoLabel[u.tipo]`, `u.companies_count` e 4 mini-cards de métricas (TACOS/Faturamento/Margem/AdSpend). Ponto de inserção do `source_counts`: adicionar uma linha discreta abaixo do "N empresa(s)" com breakdown pequeno "3 ML · 2 Adman · 1 Agregado".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portfolio/Show.jsx — SourceBadge por linha + tolerância a source='none'/undefined</name>
  <files>resources/js/Pages/Portfolio/Show.jsx</files>
  <read_first>
    - resources/js/Pages/Portfolio/Show.jsx linhas 1-30 (imports)
    - resources/js/Pages/Portfolio/Show.jsx linhas 415-490 (assinatura do componente + props)
    - resources/js/Pages/Portfolio/Show.jsx linhas 456-475 (useMemo empresasView — filtro/sort)
    - Buscar por grep no arquivo pela renderização da tabela/lista de empresas (procurar por `.map` sobre `empresasView`) para localizar a célula do nome da empresa
    - resources/js/Components/ui/source-badge.jsx (contrato 61-02)
  </read_first>
  <action>
    Passo 1: Adicionar import no bloco existente (linha ~1-30): `import { SourceBadge } from '@/Components/ui/source-badge';`.

    Passo 2: Localizar o loop `.map` que renderiza cada linha da lista/tabela de empresas (usar Grep dentro do arquivo por `empresasView.map` ou por identificadores de linha como `c.name` dentro de `<td>` ou `<tr>`). Na célula/coluna do NOME da empresa, adicionar `<SourceBadge>` inline logo APÓS o texto do nome:
    ```jsx
    <span className="inline-flex items-center gap-2">
      {c.name}
      {c.source && <SourceBadge variant={c.source} />}
    </span>
    ```

    A guarda `c.source &&` é crítica — quando flag OFF, `c.source` é undefined e o badge NÃO renderiza (backward compat total).

    Passo 3: Tolerância a `source='none'`. Verificar se há alguma condição que hoje filtra empresas por presença de `revenue > 0` ou similar — se sim, garantir que empresas com `source='none'` NÃO são silenciosamente omitidas (aparecem cinza com "—" nas métricas). NÃO remover filtros existentes; apenas confirmar que a lista `companies` já vem completa do backend e que empresas com todos os campos numéricos null são renderizadas com placeholder "—" já existente (linhas ~586-590 em Companies/Show usam `? formatCurrency() : '—'` — mesmo padrão em Portfolio).

    Passo 4: Se `empresasView` tem sort por `revenue` (linha ~463), verificar que empresas com `revenue=null` não estouram: o sort `av - bv` com `av=0` (`?? 0` na linha 464) já protege. Nenhuma mudança necessária.

    Passo 5: `npm run build` para confirmar compile verde.

    **Constraints hard:**
    - Máximo 3-5 linhas adicionadas (1 import + guarda condicional + wrapper span)
    - NÃO remover filtros ou lógica existente
    - NÃO alterar assinatura de props do componente
    - NÃO tocar props `alertas`, `revenue_timeseries`, `meta_carteira_calculada`, `performance_profissional`, `comparacao_contextual`, `nps_history`, `sugador_counters`, `ppa_counters`
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -c "SourceBadge" resources/js/Pages/Portfolio/Show.jsx</automated>
  </verify>
  <done>
    - `npm run build` verde
    - `<SourceBadge>` aparece em cada linha de empresa quando `c.source` está presente
    - Guarda defensiva `c.source &&` presente (backward compat com flag OFF)
    - Empresas `source='none'` aparecem cinza mas NÃO omitidas
  </done>
  <acceptance_criteria>
    - `grep -c "import { SourceBadge }" resources/js/Pages/Portfolio/Show.jsx` == 1
    - `grep -c "c.source &&" resources/js/Pages/Portfolio/Show.jsx` >= 1
    - `grep -c "SourceBadge variant" resources/js/Pages/Portfolio/Show.jsx` >= 1
    - `git diff resources/js/Pages/Portfolio/Show.jsx | grep -c "^-"` == 0 (zero remoção de código existente)
    - `git diff resources/js/Pages/Portfolio/Show.jsx | grep -c "^+"` <= 10
    - `npm run build` exit code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Portfolio/Carteiras.jsx — breakdown de source_counts por profissional</name>
  <files>resources/js/Pages/Portfolio/Carteiras.jsx</files>
  <read_first>
    - resources/js/Pages/Portfolio/Carteiras.jsx linhas 1-30 (imports + labels)
    - resources/js/Pages/Portfolio/Carteiras.jsx linhas 68-119 (grid de cards de profissional — bloco a editar)
    - resources/js/Components/ui/source-badge.jsx (contrato 61-02 — pode ser usado inline como legenda)
  </read_first>
  <action>
    Passo 1: Adicionar import no topo: `import { SourceBadge } from '@/Components/ui/source-badge';` (após imports Lucide, linha ~3-4).

    Passo 2: Localizar o card do profissional (linhas ~77-90), especificamente o bloco `<p className="text-white/30 text-[11px] mt-0.5">{tipoLabel[u.tipo] ?? u.tipo} · {u.companies_count} empresa{...}</p>`. LOGO ABAIXO desse `<p>`, adicionar um bloco condicional que renderiza mini-legenda de fontes SE `u.source_counts` existir (guarda defensiva idêntica ao Task 1):

    ```jsx
    {u.source_counts && (
        <div className="flex items-center gap-1.5 mt-1 flex-wrap">
            {u.source_counts.ml > 0 && <span className="text-[10px] text-white/50 inline-flex items-center gap-1"><SourceBadge variant="ml" />{u.source_counts.ml}</span>}
            {u.source_counts.adman > 0 && <span className="text-[10px] text-white/50 inline-flex items-center gap-1"><SourceBadge variant="adman" />{u.source_counts.adman}</span>}
            {u.source_counts.unified > 0 && <span className="text-[10px] text-white/50 inline-flex items-center gap-1"><SourceBadge variant="unified" />{u.source_counts.unified}</span>}
            {u.source_counts.none > 0 && <span className="text-[10px] text-white/50 inline-flex items-center gap-1"><SourceBadge variant="none" />{u.source_counts.none}</span>}
        </div>
    )}
    ```

    Cada mini-item mostra o badge + contagem numérica. Só renderiza variantes com contagem > 0 (evita poluir card com "0 ML · 0 Adman"). Layout `flex-wrap` para caber em cards estreitos.

    Passo 3: Guardas defensivas confirmadas — quando flag OFF, `u.source_counts` é undefined → bloco inteiro pula.

    Passo 4: `npm run build` verde.

    **Constraints hard:**
    - Máximo 15 linhas adicionadas
    - Zero linha removida
    - NÃO tocar as 4 mini-cards de métricas (TACOS/Faturamento/Margem/AdSpend) — linhas 91-116
    - NÃO alterar filtro de período (linhas 46-54)
    - NÃO alterar assinatura de props
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -c "source_counts" resources/js/Pages/Portfolio/Carteiras.jsx</automated>
  </verify>
  <done>
    - `npm run build` verde
    - Bloco `u.source_counts && (...)` presente
    - 4 variantes cobertas (ml/adman/unified/none)
    - Renderização condicional (só variants com count > 0)
    - Backward compat quando flag OFF (bloco não renderiza)
  </done>
  <acceptance_criteria>
    - `grep -c "import { SourceBadge }" resources/js/Pages/Portfolio/Carteiras.jsx` == 1
    - `grep -c "u.source_counts" resources/js/Pages/Portfolio/Carteiras.jsx` >= 5 (1 guard + 4 variantes)
    - `grep -c "SourceBadge variant=" resources/js/Pages/Portfolio/Carteiras.jsx` >= 4 (ml, adman, unified, none)
    - `git diff resources/js/Pages/Portfolio/Carteiras.jsx | grep -c "^-"` == 0
    - `git diff resources/js/Pages/Portfolio/Carteiras.jsx | grep -c "^+"` <= 20
    - `npm run build` exit code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → view props | `source` / `source_counts` opcionalmente presentes no payload Inertia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04-01 | Tampering | `source_counts` sem chaves esperadas | mitigate | Guardas `> 0` por chave individual (`u.source_counts.ml > 0`) evitam render quando chave missing |
| T-61-04-02 | Denial of Service | Carteira grande (200+ empresas) | accept | Sem N+1 client-side; SourceBadge é componente puro sem side-effects; render O(N) linear |
| T-61-04-03 | Availability | Empresa ML-only quebra render (regressão SC #2) | mitigate | Guarda `c.source &&`; tolerância a null nas métricas via `?? '—'` já em vigor no arquivo (linhas 586+ pattern) |
| T-61-04-SC | Tampering (supply chain) | Nenhum install | n/a | Reusa 61-02; sem `npm install` |
</threat_model>

<verification>
1. `npm run build` verde nos 2 arquivos
2. Com `UNIFIED_METRICS_ENABLED=false`: renderização idêntica ao estado atual (badges/counts ausentes, mesma tabela e cards)
3. Com `UNIFIED_METRICS_ENABLED=true`: badges aparecem por empresa + contadores por card de profissional
4. Empresa ML-only em uma carteira renderiza a linha sem quebrar (SC #2 do ROADMAP fechado)
5. Diff mínimo em ambos os arquivos (sem alteração em outras seções)
</verification>

<success_criteria>
- DASH-05 fechado: carteiras Analista/Estrategista tolerantes a empresas ML-only e sem integração
- DATA-05 avançado: badges de fonte visíveis em cada linha da tabela e mini-legenda por profissional
- Backward compat total quando flag OFF (sem badges, sem crash)
- Zero regressão visual em cards de profissional (TACOS/Faturamento/Margem/AdSpend intactos)
</success_criteria>

<output>
Criar `.planning/phases/61-dashboards-multi-fonte-indicador-de-origem/61-04-SUMMARY.md` ao final.
</output>
