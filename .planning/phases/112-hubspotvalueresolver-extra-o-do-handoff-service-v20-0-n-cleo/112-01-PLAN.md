---
phase: 112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/Services/Hubspot/HubspotValueResolver.php
  - tests/Unit/HubspotValueResolverTest.php
autonomous: true
requirements: [HUB-VAL-01]
tags: [hubspot, value-resolver, tdd, comercial]

must_haves:
  truths:
    - "resolve(Servico, lineItem, dealProps) devolve o valor OPERACIONAL (mensal quando o serviço é mensal) sem tocar em banco"
    - "line item monthly + price numérico devolve price*quantity com confidence=high (invariante de não-regressão)"
    - "line item annually price 36000 + P1Y devolve valor_operacional=3000"
    - "serviço único com amount 36000 NÃO é dividido por 12"
    - "sem line item, deal.amount 36000 e valor_padrao 3000 (tolerância 5%) devolve 3000 com warning de inferência"
    - "sem line item, deal.amount 35000 sem valor_padrao compatível marca valor_revisar (confidence=low)"
  artifacts:
    - path: "app/Services/Hubspot/HubspotValueResolver.php"
      provides: "Regra de valor mensal×anual isolada e testável"
      contains: "function resolve"
    - path: "tests/Unit/HubspotValueResolverTest.php"
      provides: "6 casos-âncora do prompt Fase 10 + paridade de não-regressão"
      contains: "class HubspotValueResolverTest"
  key_links:
    - from: "HubspotValueResolver::resolve"
      to: "Servico::TIPO_MENSAL / TIPO_UNICA / valor_padrao"
      via: "leitura de atributos do Servico recebido (sem query)"
      pattern: "TIPO_MENSAL|valor_padrao"
---

<objective>
Criar `App\Services\Hubspot\HubspotValueResolver` — classe PURA (sem I/O, sem banco) que decide o valor OPERACIONAL de um contrato a partir de `(Servico $servico, array $lineItem, array $dealProps)`, distinguindo cobrança mensal × anual e devolvendo a proveniência completa (valor original, tipo, normalizado mensal, frequência, período, confidence, warning).

Purpose: É o núcleo que resolve o bug real R$ 36.000 (anual) × R$ 3.000 (mensal). Isolar a regra numa classe testável impede que ela fique espalhada dentro do controller e permite provar cada caso com teste unitário rápido (TDD-first).

Output: `HubspotValueResolver.php` + `HubspotValueResolverTest.php` com os 6 casos-âncora do prompt (Fase 10) e testes de paridade que blindam o INVARIANTE DE NÃO-REGRESSÃO.

Escopo desta fase (LOCKED CONTEXT): SÓ valor/contratos. A resolução do mapping line_item→Servico, a orquestração multi-item e a persistência ficam nos planos 112-02 e 112-03. Aqui o resolver recebe o `Servico` já resolvido e um único `lineItem` (ou `[]` no fluxo legado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-CONTEXT.md
@.planning/STATE.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real — não explorar o codebase. -->

Servico (app/Models/Servico.php):
- const TIPO_MENSAL = 'mensal'; const TIPO_UNICA = 'unica';
- atributo tipo_cobranca (string 'mensal'|'unica')
- atributo valor_padrao (cast decimal:2 — ACESSO devolve string tipo "3000.00"; sempre castar com (float))
- Pode ser instanciado em memória sem banco: new Servico(['tipo_cobranca'=>'mensal','valor_padrao'=>3000])

Shape do $lineItem (como HubspotApiClient::fetchDealLineItems normaliza — app/Services/HubspotApiClient.php ~254-288):
- id (string|null), name (string|null)
- price (float|null — já is_numeric), quantity (int|null), amount (float|null)
- hs_product_id (string|null)
- recurringbillingfrequency (string|null — ex.: 'monthly','annually','quarterly')
- hs_recurring_billing_period (string|null — ISO-8601, ex.: 'P1Y')
- hs_line_item_currency_code (string|null)
- hs_mrr (float|null), hs_arr (float|null), hs_tcv (float|null), hs_acv (float|null)

Shape do $dealProps (deal['properties'] do HubSpot): chave 'amount' (string numérica) é o valor nativo do deal usado no fluxo legado.

Comportamento ATUAL a preservar (HubspotWebhookController::processarLineItems ~412-417):
- qty = quantity numérico e >0 senão 1
- valor = price numérico ? price*qty : (float) servico->valor_padrao
- INVARIANTE: monthly + price numérico → resolver DEVE devolver EXATAMENTE price*qty, confidence=high.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Tarefa 1 (RED): esqueleto do HubspotValueResolver + suite unitária dos 6 casos-âncora</name>
  <files>app/Services/Hubspot/HubspotValueResolver.php, tests/Unit/HubspotValueResolverTest.php</files>
  <read_first>
    - .planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-CONTEXT.md (bloco &lt;decisions&gt; — regras 1..6 e casos-âncora)
    - app/Models/Servico.php (linhas 30-58: fillable, casts decimal:2, TIPO_MENSAL/TIPO_UNICA)
    - tests/Feature/Phase37WebhookLineItemsTest.php (helper criarServico + asserts de valor 500 — fonte da paridade de não-regressão)
  </read_first>
  <behavior>
    - Caso 1 (paridade/não-regressão): Servico mensal, lineItem {price:3000, quantity:1, recurringbillingfrequency:'monthly'}, deal amount 36000 → valor_operacional=3000.0, valor_original=3000.0, valor_original_tipo='unit_price', billing_frequency='monthly', confidence='high', warning=null.
    - Caso 1b (paridade qty>1): mensal, {price:500, quantity:2, recurringbillingfrequency:'monthly'} → valor_operacional=1000.0, confidence='high'.
    - Caso 2: mensal, {price:36000, quantity:1, recurringbillingfrequency:'annually', hs_recurring_billing_period:'P1Y'} → valor_operacional=3000.0, valor_original=36000.0, normalizado_mensal=3000.0, billing_frequency='annually', billing_period='P1Y', confidence='high'.
    - Caso 3: mensal, {price:36000, hs_mrr:3000, hs_arr:36000, recurringbillingfrequency:'annually'} → valor_operacional=3000.0 (usa hs_mrr), valor_original_tipo='mrr', confidence='high'.
    - Caso 4: Servico único (TIPO_UNICA), {amount:36000, price:36000, quantity:1} → valor_operacional=36000.0 (NÃO divide por 12), confidence='high'.
    - Caso 5: SEM line item ([] ), dealProps {amount:'36000'}, Servico mensal valor_padrao 3000 → valor_operacional=3000.0 (36000/12 dentro de 5% de 3000), valor_original=36000.0, valor_original_tipo='deal_amount_annual', warning != null (inferência), confidence='medium'.
    - Caso 6: SEM line item, dealProps {amount:'35000'}, Servico mensal valor_padrao 0/null (sem compatível) → warning contém 'valor_revisar', confidence='low'.
  </behavior>
  <action>
    Criar `app/Services/Hubspot/HubspotValueResolver.php` no namespace `App\Services\Hubspot` com APENAS a assinatura pública `resolve(\App\Models\Servico $servico, array $lineItem, array $dealProps): array` retornando um array-stub fixo (ex.: todas as 8 chaves com null/0 e confidence='low') — o suficiente para a suite carregar e FALHAR nas asserções (RED genuíno, não fatal por classe inexistente). Documentar o shape de retorno em PHPDoc pt-BR listando as 8 chaves (valor_operacional, valor_original, valor_original_tipo, normalizado_mensal, billing_frequency, billing_period, confidence, warning).

    Criar `tests/Unit/HubspotValueResolverTest.php` (extends Tests\TestCase, SEM RefreshDatabase — teste puro; instanciar Servico em memória com `new Servico([...])`). Implementar um método de teste por caso do bloco &lt;behavior&gt; (nomes descritivos em pt: test_line_item_mensal_monthly_usa_price_vezes_quantity_high, test_line_item_mensal_annually_36000_p1y_vira_3000, test_line_item_mensal_hs_mrr_prevalece_sobre_arr, test_servico_unica_amount_36000_nao_divide_por_12, test_sem_line_item_deal_amount_36000_infere_3000_com_warning, test_sem_line_item_deal_amount_35000_marca_valor_revisar, mais test_paridade_monthly_price_500_qty_2). Usar assertEqualsWithDelta(…, 0.01) para floats. Comentários em pt-BR. NÃO implementar a lógica do resolver ainda.

    Rodar a suite e CONFIRMAR RED (asserções falham, sem erro fatal de classe/método ausente). Commit: `test(112-01): RED — HubspotValueResolver 6 casos-âncora + paridade`.
  </action>
  <verify>
    <automated>php artisan test --filter=HubspotValueResolverTest 2>&1 | grep -Eq "FAIL|failed|Failed asserting" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/Services/Hubspot/HubspotValueResolver.php` existe com método público `resolve` e PHPDoc das 8 chaves.
    - `tests/Unit/HubspotValueResolverTest.php` existe com 7 métodos de teste (6 âncora + 1 paridade qty>1).
    - `php artisan test --filter=HubspotValueResolverTest` FALHA por asserção (não por erro fatal): saída contém "Failed asserting" ou "FAIL".
    - Nenhuma query de banco no teste (sem RefreshDatabase; Servico instanciado com `new`).
  </acceptance_criteria>
  <done>Suite RED verde-como-vermelho: falha nas asserções dos 7 métodos, classe e método existem, commit de teste criado.</done>
</task>

<task type="tdd" tdd="true">
  <name>Tarefa 2 (GREEN): implementar resolve() com as regras 1..6 + helper de tolerância</name>
  <files>app/Services/Hubspot/HubspotValueResolver.php</files>
  <read_first>
    - app/Services/Hubspot/HubspotValueResolver.php (esqueleto da Tarefa 1)
    - tests/Unit/HubspotValueResolverTest.php (contrato de saída dos 7 métodos)
    - .planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-CONTEXT.md (regras 1..6 do &lt;decisions&gt;)
  </read_first>
  <action>
    Implementar `resolve()` cobrindo, na ordem: (1) COM line item + Servico mensal — recurringbillingfrequency='monthly' + price numérico → valor_operacional = price*quantity, valor_original_tipo='unit_price', confidence='high'; recurringbillingfrequency='annually' + price numérico → se hs_mrr numérico usar hs_mrr (tipo='mrr') senão price*quantity/12 (tipo='deal_amount_annual' ou 'net_price'), confidence='high' quando hs_recurring_billing_period indica 12 meses (billing_period começa com 'P1Y'), senão 'medium'; hs_mrr numérico presente → fonte forte para mensal (tipo='mrr'); só amount do line item parecendo anual → se amount/12 ≈ valor_padrao ou ≈ price (helper de tolerância) usar amount/12 + warning de inferência. (2) COM line item + Servico único (TIPO_UNICA) → usar amount se numérico senão price*quantity; NUNCA dividir por 12; confidence='high'. (3) SEM line item (lineItem vazio, usa dealProps['amount']): Servico mensal e amount/12 ≈ valor_padrao (tolerância) → amount/12, valor_original_tipo='deal_amount_annual', confidence='medium', warning de inferência; Servico mensal e amount ≈ valor_padrao → amount (tipo='deal_amount', confidence='high'); indecidível → valor mais conservador (amount/12 quando mensal), confidence='low', warning contendo 'valor_revisar'. (4) sempre preencher normalizado_mensal quando o serviço é mensal; billing_frequency/billing_period a partir do line item; currency de hs_line_item_currency_code quando útil. (5) helper privado `aproximadamente(float $a, float $b, float $margem = 0.05): bool` comparando |a-b|/max(|b|,epsilon) ≤ margem; tolerância default 5%, deixar como constante `TOLERANCIA_PADRAO = 0.05`. (6) NUNCA sobrescrever valor de baixa confiança sem warning. Cast todo acesso a valor_padrao/price com (float). Comentários pt-BR nas regras de negócio não óbvias.

    Rodar a suite até 7/7 verde. Commit: `feat(112-01): GREEN — regra de valor mensal×anual (HubspotValueResolver)`.
  </action>
  <verify>
    <automated>php artisan test --filter=HubspotValueResolverTest 2>&1 | grep -Eq "OK|PASS|Tests:.*[1-9].*(passed|OK)" && echo GREEN-OK</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=HubspotValueResolverTest` → 7/7 verde (0 falhas).
    - Caso monthly price 3000 qty 1 → valor_operacional exatamente 3000.0, confidence='high' (INVARIANTE preservado).
    - Caso annually 36000 + P1Y → 3000.0; caso hs_mrr → usa 3000; caso único 36000 → 36000.0 (não /12).
    - Caso deal.amount 36000 + valor_padrao 3000 → 3000.0 com warning de inferência; caso 35000 sem compatível → warning contém 'valor_revisar' e confidence='low'.
    - Existe helper privado de tolerância com margem default 5% (constante TOLERANCIA_PADRAO).
    - `resolve()` não faz nenhuma chamada a banco/HTTP (grep: sem `::where`, sem `Http::`, sem `DB::` no arquivo).
  </acceptance_criteria>
  <done>Resolver implementado, 7/7 testes verdes, invariante de não-regressão provado por teste de paridade, commit GREEN criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados HubSpot (lineItem/dealProps) → resolver | valores numéricos não confiáveis (podem vir string vazia, null, não-numérico) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01-01 | Tampering | resolve() com input numérico inválido | mitigate | guardas is_numeric + cast (float) antes de qualquer cálculo; ausência → cai no ramo conservador com warning, nunca lança |
| T-112-01-02 | Tampering | regressão de valor (invariante) | mitigate | teste de paridade monthly→price*qty confidence=high trava o valor exato; suite unitária roda no gate |
| T-112-01-03 | Denial of Service | divisão por zero em amount/12 ou tolerância | accept/mitigate | 12 é constante; helper de tolerância usa epsilon no denominador para evitar /0 |
</threat_model>

<verification>
- `php artisan test --filter=HubspotValueResolverTest` → 7/7 verde.
- Gate de não-regressão (as suites de webhook não devem ter mudado, pois este plano não toca o controller): `php artisan test --filter=Phase37WebhookLineItemsTest` continua 10/10 verde.
- `HubspotValueResolver.php` não contém `DB::`, `Http::` nem `::where(` (lógica pura).
</verification>

<success_criteria>
- Resolver isolado e testável entregue; 6 casos-âncora do prompt (Fase 10) + paridade de não-regressão todos verdes.
- Invariante monthly→price*quantity/high preservado por teste.
- HUB-VAL-01 satisfeito.
</success_criteria>

<output>
Criar `.planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-01-SUMMARY.md` ao concluir.
</output>
