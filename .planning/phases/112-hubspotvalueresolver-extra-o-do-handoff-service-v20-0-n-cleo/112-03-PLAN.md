---
phase: 112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo
plan: 03
type: execute
wave: 3
depends_on: ["112-02"]
files_modified:
  - app/Http/Controllers/Api/HubspotWebhookController.php
  - tests/Feature/Phase112HubspotHandoffWebhookTest.php
autonomous: true
requirements: [HUB-VAL-02, HUB-VAL-03, HUB-VAL-05]
tags: [hubspot, webhook, refactor, persistencia, tdd, comercial]

must_haves:
  truths:
    - "o webhook, ao fechar um deal com line item mensal R$3.000 + amount/ARR R$36.000, grava valor_contratado=3000"
    - "os R$36.000 e a proveniência ficam gravados nas colunas hubspot_valor_* do ContratoServico"
    - "multi-line-item cria contratos separados por hubspot_line_item_id distinto, cada um com seus campos de auditoria"
    - "o controller delega a montagem de valor/contratos ao HubspotDealHandoffService (não calcula valor inline)"
    - "HMAC, filtro de stage, idempotência, DB::transaction, atualização de hubspot_eventos e notificação Comercial seguem preservados"
    - "a linha legada em observacoes (tipo_cobranca / servico HubSpot) continua sendo gravada"
  artifacts:
    - path: "app/Http/Controllers/Api/HubspotWebhookController.php"
      provides: "Controller fino orquestrando o handoff + persistindo colunas de auditoria"
      contains: "HubspotDealHandoffService"
    - path: "tests/Feature/Phase112HubspotHandoffWebhookTest.php"
      provides: "E2E do webhook provando 36k→3k + auditoria + multi-item + regressão"
      contains: "class Phase112HubspotHandoffWebhookTest"
  key_links:
    - from: "HubspotWebhookController::criarEmpresa"
      to: "HubspotDealHandoffService::build"
      via: "delegação da montagem de contratos"
      pattern: "->build\\("
    - from: "criação do ContratoServico"
      to: "colunas hubspot_valor_* / hubspot_line_item_id"
      via: "contract_to_create do DTO"
      pattern: "hubspot_valor_original"
---

<objective>
Deixar o `HubspotWebhookController` FINO: a montagem de valor/contratos sai de `processarLineItems`/`processarServicoLegado` e passa a delegar ao `HubspotDealHandoffService` (plano 112-02). O controller consome o DTO e PERSISTE cada `ContratoServico` com `valor_contratado` = valor operacional + as 11 colunas de auditoria da Fase 111, mantendo a linha legada em `observacoes` e todo o resto do fluxo (HMAC, filtro de stage, idempotência, DB::transaction, `hubspot_eventos`, notificação Comercial, roteamento MlbEmpresa) intacto.

Purpose: HUB-VAL-05 (controller fino, comportamento preservado) + HUB-VAL-03 (proveniência/confidence/warning gravados) + HUB-VAL-02 (contratos multi-item persistidos separados). Fecha o critério de aceite âncora da milestone: deal com line item mensal R$3.000 + amount/ARR R$36.000 → `valor_contratado=3.000`, com R$36.000 em `hubspot_valor_original`.

Output: controller refatorado + `Phase112HubspotHandoffWebhookTest` (E2E, Http::fake) provando 36k→3k, auditoria preenchida, multi-item separado e regressão verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-CONTEXT.md
@.planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-02-PLAN.md

<interfaces>
<!-- Contratos consumidos. Não explorar o codebase. -->

HubspotDealHandoffService (plano 112-02):
- __construct(HubspotValueResolver $resolver)  // resolvido via container
- build(array $deal, array $lineItems, array $propsDeal): HubspotHandoffData

HubspotHandoffData: ->deal_data, ->line_items, ->contracts_to_create (array de arrays com servico_id, servico_nome, valor_contratado + 11 chaves hubspot_*), ->warnings, ->confidence.

Pontos do controller a preservar (app/Http/Controllers/Api/HubspotWebhookController.php):
- receive() ~63-119: HMAC v3 (timestamp replay 5min + hash_equals), decode, 1 HubspotEvento por evento.
- processar() ~126-238: filtro subscription_type='deal.propertyChange' + property_name='dealstage' + property_value ∈ stagesGatilho; idempotência (object_id + company_id_criada NOT NULL); fetchDeal/fetchAssociatedCompanyId/fetchCompany/fetchAssociatedContactId/fetchContact/fetchDealLineItems; update status processado/erro; notificarComercialSePendente.
- criarEmpresa() ~260-340: DB::transaction — cria Company (name/cnpj/email/phone/nicho/dor/vende_ml/faturamento) + notes contato + branch line items×legado + rotearImplementacao por servico. ESTE é o ponto de delegação.
- processarLineItems() ~364-472 e processarServicoLegado() ~486-518: lógica de valor/contrato JÁ EXTRAÍDA para o handoff (112-02) — substituir seu miolo por consumo do DTO; a parte de warnings no payload e a gravação em notes (servico não encontrado) permanecem responsabilidade do controller.

ContratoServico (app/Models/ContratoServico.php): $fillable já inclui as 11 colunas hubspot_* + observacoes; casts decimal:2/array. Manter a linha `observacoes = "tipo_cobranca: {mensal|unica} (HubSpot line_item: {nome})"` do fluxo Phase 37.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): E2E do webhook — 36k→3k + auditoria + multi-item</name>
  <files>tests/Feature/Phase112HubspotHandoffWebhookTest.php</files>
  <read_first>
    - tests/Feature/Phase37WebhookLineItemsTest.php (INTEIRO — reaproveitar mockHubspot/dispararWebhook/assinatura/servidor e o setUp de config)
    - app/Models/ContratoServico.php (colunas hubspot_* + casts)
    - .planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-CONTEXT.md (§specifics — critério de aceite âncora)
  </read_first>
  <behavior>
    - Deal fechado ganho com 1 line item mensal (name mapeado, price 3000, recurringbillingfrequency 'monthly') → ContratoServico com valor_contratado=3000.0, hubspot_valor_confidence='high'.
    - Deal com 1 line item ANNUALLY (price 36000, hs_recurring_billing_period 'P1Y', hs_arr 36000) mapeado a Servico mensal → valor_contratado=3000.0, hubspot_valor_original=36000.0, hubspot_valor_original_tipo preenchido, hubspot_billing_frequency='annually', hubspot_billing_period='P1Y'.
    - Deal com 2 line items mapeados (ids distintos) → 2 ContratoServico com hubspot_line_item_id distintos e cada um com seus hubspot_valor_*.
    - observacoes do contrato continua contendo 'mensal' (linha legada Phase 37 preservada).
  </behavior>
  <action>
    Criar `tests/Feature/Phase112HubspotHandoffWebhookTest.php` (RefreshDatabase). Copiar/reusar os helpers de `Phase37WebhookLineItemsTest` (setUp de config services.hubspot.*, assinatura HMAC, servidor(), criarServico, criarMapping, eventoPadrao, mockHubspot, dispararWebhook) — se necessário, estender o mockHubspot para injetar props extras do line item (hs_recurring_billing_period, hs_arr, hs_mrr, hs_line_item_currency_code) nas properties do fake, pois o mock atual do Phase37 só manda name/price/quantity/hs_product_id/recurringbillingfrequency.

    Implementar os 4 cenários do &lt;behavior&gt; como métodos de teste distintos, afirmando os campos gravados em ContratoServico (valor_contratado, hubspot_valor_original, hubspot_valor_confidence, hubspot_billing_frequency, hubspot_billing_period, hubspot_line_item_id) via assertEqualsWithDelta/assertSame. NÃO implementar o refactor do controller ainda — a suite deve FALHAR porque o controller atual grava 36000 (não 3000) e não popula as colunas hubspot_*.

    Rodar e confirmar RED. Commit: `test(112-03): RED — E2E webhook 36k→3k + colunas de auditoria`.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase112HubspotHandoffWebhookTest 2>&1 | grep -Eq "FAIL|failed|Failed asserting" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Phase112HubspotHandoffWebhookTest.php` existe com ≥4 métodos cobrindo o &lt;behavior&gt;.
    - A suite FALHA (RED) contra o controller atual: pelo menos o caso annually falha por valor_contratado=36000 (esperado 3000) e/ou colunas hubspot_* nulas.
    - Http::fake presente em todos os cenários (nenhuma chamada real a api.hubapi.com).
  </acceptance_criteria>
  <done>Suite E2E RED criada e vermelha por comportamento (valor não normalizado + auditoria não persistida), commit de teste criado.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): controller fino delega ao handoff + persiste auditoria; gate de regressão</name>
  <files>app/Http/Controllers/Api/HubspotWebhookController.php</files>
  <read_first>
    - app/Http/Controllers/Api/HubspotWebhookController.php (INTEIRO — refatorar criarEmpresa + processarLineItems + processarServicoLegado)
    - app/Services/Hubspot/HubspotDealHandoffService.php + HubspotHandoffData.php (plano 112-02)
    - tests/Feature/Phase112HubspotHandoffWebhookTest.php (Tarefa 1 — alvo GREEN)
    - tests/Feature/Phase37WebhookLineItemsTest.php (regressão que NÃO pode mudar de valor)
  </read_first>
  <action>
    Refatorar `HubspotWebhookController::criarEmpresa` para delegar valor/contratos ao handoff: dentro da DB::transaction, após criar a Company + notes de contato, chamar `app(HubspotDealHandoffService::class)->build($deal, $lineItems, $propsDeal)` (injetar o service via container ou por parâmetro; DI no construtor é aceitável). Para cada `contract_to_create` do DTO, criar o `ContratoServico` preenchendo: company_id, servico_id, valor_contratado (= valor operacional do DTO), data_contratacao=now()->toDateString(), ativo=true, as 11 colunas de auditoria (`hubspot_line_item_id`, `hubspot_product_id`, `hubspot_billing_frequency`, `hubspot_billing_period`, `hubspot_currency`, `hubspot_valor_original`, `hubspot_valor_original_tipo`, `hubspot_valor_normalizado_mensal`, `hubspot_valor_confidence`, `hubspot_valor_warning`, `hubspot_snapshot`) a partir do DTO, e MANTER a linha legada `observacoes = "tipo_cobranca: {mensal|unica} (HubSpot line_item: {nome})"` (derivar tipo_cobranca da billing_frequency/servico como hoje). Alimentar `rotearImplementacao` com os `servico_nome` do DTO. Persistir os `warnings` do DTO no payload do evento (`line_items_nao_mapeados` para itens sem mapping — mesma chave/shape de hoje) e, no fluxo legado, manter a gravação do nome do serviço em `company->notes` quando o handoff sinalizar 'servico_nao_encontrado'.

    Reduzir `processarLineItems`/`processarServicoLegado` ao que sobrou de responsabilidade do controller (persistência a partir do DTO + warnings/notes), OU removê-los e concentrar a persistência num único método privado `persistirContratos(Company, HubspotHandoffData, HubspotEvento)`. NÃO alterar receive()/processar() (HMAC, filtro, idempotência, fetches, update de status, notificarComercialSePendente permanecem idênticos). Preservar os logs [HubSpot Webhook] no canal ecf-webhooks; NUNCA logar token/secret. Comentários pt-BR.

    Rodar a suite nova até GREEN e o gate de regressão. Commit: `refactor(112-03): controller fino delega ao handoff + persiste auditoria HubSpot`.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase112HubspotHandoffWebhookTest 2>&1 | grep -Eq "OK|PASS|Tests:.*(passed|OK)" && echo GREEN-OK</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Phase112HubspotHandoffWebhookTest` → todos verdes; caso annually grava valor_contratado=3000.0 e hubspot_valor_original=36000.0.
    - GATE DE REGRESSÃO verde (valores inalterados): `php artisan test --filter="Phase34HubspotWebhookTest|Phase35HubspotV2Test|Phase37WebhookLineItemsTest|Phase37LineItemsFetchTest"` — 0 falhas; o caso monthly price 500 do Phase37 continua gravando 500.0.
    - `grep -n "HubspotDealHandoffService\|->build(" app/Http/Controllers/Api/HubspotWebhookController.php` mostra a delegação; o controller NÃO calcula mais `price * $qty` inline para valor_contratado (grep do trecho antigo `(float) \$item\['price'\] \* \$qty` não aparece mais no cálculo de contrato).
    - ContratoServico criado tem `hubspot_valor_original` preenchido no caso annually e `observacoes` contendo 'mensal' (linha legada preservada).
    - receive()/processar() inalterados no que toca HMAC/idempotência (grep: `hash_equals`, `REPLAY_WINDOW_MS`, `whereNotNull('company_id_criada')` ainda presentes).
  </acceptance_criteria>
  <done>Controller fino delega ao handoff, persiste as 11 colunas de auditoria, mantém observacoes legada; E2E verde e regressão (Phase34/35/37) sem mudança de valor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot → webhook público POST /api/webhooks/hubspot | payload externo assinado; único ponto de entrada não autenticado por sessão |
| DTO handoff → persistência ContratoServico | valores/auditoria gravados no banco de negócio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-03-01 | Tampering | refactor quebra validação HMAC/idempotência | mitigate | receive()/processar() intocados; gate de regressão Phase34/35 + grep de hash_equals/company_id_criada |
| T-112-03-02 | Tampering | regressão de valor (invariante monthly→price*qty) | mitigate | gate Phase37WebhookLineItemsTest verde sem alterar asserções de valor (500 continua 500) |
| T-112-03-03 | Information Disclosure | token/secret em log durante refactor | mitigate | logs só com evento_id/deal_id/company_id; nunca token; revisão do grep dos Log::channel |
| T-112-03-04 | Repudiation | perda de proveniência do valor no banco | mitigate | 11 colunas hubspot_* + hubspot_snapshot gravadas por contrato; teste E2E afirma persistência |
</threat_model>

<verification>
- `php artisan test --filter=Phase112HubspotHandoffWebhookTest` → verde (36k→3k + auditoria + multi-item).
- `php artisan test --filter="Phase34HubspotWebhookTest|Phase35HubspotV2Test|Phase37WebhookLineItemsTest|Phase37LineItemsFetchTest"` → 0 falhas (invariante de não-regressão).
- `php artisan test --filter="HubspotValueResolverTest|Phase112HandoffServiceTest"` → verdes (integração dos planos 01/02 preservada).
- Controller delega ao handoff (grep `->build(`) e não calcula valor de contrato inline.
</verification>

<success_criteria>
- HUB-VAL-05: controller fino orquestrando (validar → idempotência → handoff → persistir → atualizar evento → notificar), comportamento legado preservado.
- HUB-VAL-03: valor_original/tipo/normalizado_mensal/confidence/warning + billing/currency/line_item_id/product_id/snapshot gravados por contrato.
- HUB-VAL-02: multi-line-item persistido em contratos separados por hubspot_line_item_id.
- Critério de aceite âncora da milestone atingido: line item mensal R$3.000 + amount/ARR R$36.000 → valor_contratado=3.000, R$36.000 em hubspot_valor_original.
- Regressão zero (Phase34/35/37 verdes, valores inalterados).
</success_criteria>

<output>
Criar `.planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-03-SUMMARY.md` ao concluir.
</output>
