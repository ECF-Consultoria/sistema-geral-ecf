---
phase: 40-shadow-mode-tabelas-de-compara-o
plan: 02
type: tdd
wave: 2
depends_on: ["40-01"]
files_modified:
  - app/Services/Sugadores/ShadowRunService.php
  - tests/Feature/Phase40/ShadowRunServiceTest.php
autonomous: true
requirements: [REQ-40-02]
tags: [phase-40, sugadores, shadow-mode, service, ml-migration, tdd, mockery]

must_haves:
  truths:
    - "ShadowRunService roda 2 vezes `SugadorAnalysisService::analyzeCompany` por empresa+data — uma com `forceProvider='adman'`, outra com `forceProvider='ml'`, AMBAS com `dryRun=true`"
    - "Para cada execução, persiste 1 row em `sugador_provider_runs` (status `running` → `completed` ou `failed`) e N rows em `sugador_provider_items` (um por motivo detectado)"
    - "ZERO gravação em `sugadores` — `assertDatabaseCount('sugadores', $initial)` igual antes e depois (gate crítico)"
    - "Falha do provider Adman NÃO interrompe execução do provider ML (e vice-versa) — registra `status=failed` + `error=msg` na row correspondente"
    - "Retorna array `['adman_run_id'=>int|null, 'ml_run_id'=>int|null, 'adman_status'=>string, 'ml_status'=>string]`"
    - "`raw_hash` de cada item = `sha256` do `metrics_json` + `motivos` ordenados (determinístico)"
    - "Consome `SugadorAnalysisService` e `SugadoresAdsProviderFactory` via DI no constructor (Laravel resolve)"
  artifacts:
    - path: app/Services/Sugadores/ShadowRunService.php
      provides: "Orquestrador shadow: 2 runs Adman+ML por (empresa+data); grava em runs+items; nunca em sugadores"
    - path: tests/Feature/Phase40/ShadowRunServiceTest.php
      provides: "Suite Feature com Mockery validando: 2 runs criadas, items persistidos, zero gravação sugadores, falha isolada por provider"
  key_links:
    - from: app/Services/Sugadores/ShadowRunService.php
      to: app/Services/SugadorAnalysisService.php
      via: "$this->analysis->analyzeCompany(\\$company, \\$ref, dryRun=true, forceProvider='adman'|'ml')"
      pattern: "analyzeCompany.*true.*('adman'|'ml')"
    - from: app/Services/Sugadores/ShadowRunService.php
      to: sugador_provider_runs + sugador_provider_items
      via: "SugadorProviderRun::create + SugadorProviderItem::create dentro de DB::transaction"
      pattern: "SugadorProviderRun::create|SugadorProviderItem::create"
---

<objective>
Plan 40-02 entrega o orquestrador `ShadowRunService` (REQ-40-02). Para cada empresa+data:
1. Cria row Adman em `sugador_provider_runs` (`status=running`)
2. Chama `SugadorAnalysisService::analyzeCompany($company, $ref, dryRun=true, forceProvider='adman')`
3. Persiste cada motivo do `detalhes[]` retornado como row em `sugador_provider_items`
4. Atualiza row Adman: `status=completed`, `finished_at=now()`, `summary={campanhas, adgroups}`
5. Repete tudo pro provider ML (independente — falha de um não interrompe o outro)
6. NUNCA grava em `sugadores` — `dryRun=true` no service já garante isso (Phase 39 entregou)

Purpose: produzir registros paralelos comparáveis (Plan 40-03 consome).
Output: 1 service + 1 test Feature com Mockery (zero gravação em sugadores assertada).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-CONTEXT.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-01-PLAN.md
@.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-04-SUMMARY.md
@app/Services/SugadorAnalysisService.php
@app/Services/Sugadores/SugadoresAdsProviderFactory.php

<interfaces>
<!-- Contratos consumidos por ShadowRunService -->

SugadorAnalysisService (Phase 39 Plan 39-04):
```php
public function analyzeCompany(
    Company $company,
    ?Carbon $referenceDate = null,
    bool $dryRun = false,
    ?string $forceProvider = null  // 'adman'|'ml'|null
): array
```
Retorno:
```php
[
    'skipped'   => bool,
    'reason'    => string|null,
    'campanhas' => int,
    'adgroups'  => int,
    'detalhes'  => [
        ['tipo'=>'campanha'|'adgroup', 'campaign_id'=>string, 'adgroup_id'=>string|null,
         'mlb_id'=>string|null, 'motivos'=>string[], 'metrics'=>array, 'novo'=>bool],
        ...
    ],
]
```
Importante: com `dryRun=true` o service NÃO grava em `sugadores` (Phase 39 garantiu via `$existingMap = $dryRun ? collect() : ...` e bypass do upsert).

SugadoresAdsProviderFactory:
```php
public function for(Company $company, ?string $forceName = null): SugadoresAdsProvider
```
Lança `\RuntimeException` quando empresa não tem provider compatível — `analyzeCompany` já converte em `skipped=true`.

Models Plan 40-01:
- SugadorProviderRun::create(['company_id','provider','reference_date','periodo_inicio','periodo_fim','status','started_at','finished_at','error','summary'])
- SugadorProviderItem::create(['run_id','tipo','campaign_id','adgroup_id','mlb_id','motivos','metrics_json','raw_hash'])
- Cast `summary` array, `motivos` array, `metrics_json` array; `started_at`/`finished_at` immutable_datetime

Periodo da janela analisada (espelhar SugadorAnalysisService linhas 136-137):
- $periodoFim = $referenceDate->copy()->subDay()
- $periodoInicio = $periodoFim->copy()->subDays($config->dias_analise - 1)
Porém ShadowRunService NÃO acessa SugadorConfig — pede os 2 dates via detalhes do service (NÃO disponível) OU calcula default `dias_analise = 7` se não tiver acesso. Decisão: passar `$periodoInicio` e `$periodoFim` derivados da config DENTRO do ShadowRunService chamando `SugadorConfig::forCompany($company)` (mesma API que SugadorAnalysisService usa internamente). Cast já existe.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): suite Feature ShadowRunService com Mockery</name>
  <files>tests/Feature/Phase40/ShadowRunServiceTest.php</files>
  <read_first>
    - .planning/phases/40-shadow-mode-tabelas-de-compara-o/40-CONTEXT.md (§decisions ShadowRunService responsabilidades)
    - app/Services/SugadorAnalysisService.php (linhas 1-160 — assinatura analyzeCompany + retorno detalhes)
    - app/Services/Sugadores/SugadoresAdsProviderFactory.php (entender que forceProvider='adman'|'ml')
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-04-SUMMARY.md (decisões Mockery + bind container)
    - app/Models/Sugador.php (model que NÃO pode ganhar rows novas — gate crítico)
  </read_first>
  <behavior>
    - Test 1: run_cria_duas_rows_em_sugador_provider_runs — Mock `SugadorAnalysisService` retornando `detalhes=[]` pra ambos providers; chamar `ShadowRunService::run($company, $today)`; `assertDatabaseCount('sugador_provider_runs', 2)`; uma com `provider='adman'`, outra `provider='ml'`
    - Test 2: run_persiste_items_por_motivo_detectado — Mock retorna 2 itens Adman + 3 itens ML (cada qual com motivos populados); `assertDatabaseCount('sugador_provider_items', 5)`; cada item tem `run_id` apontando pro run correto
    - Test 3: run_NAO_grava_em_sugadores_GATE_CRITICO — `$initial = Sugador::count()`; rodar `ShadowRunService::run($company, $today)` com mock retornando 10 itens; `assertEquals($initial, Sugador::count())`; também `assertDatabaseCount('sugadores', $initial)` antes E depois; assertion sobre ZERO insert nessa tabela é o gate principal da Phase 40
    - Test 4: run_marca_status_completed_quando_provider_executa_ok — Após run sucesso, row tem `status='completed'`, `finished_at IS NOT NULL`, `summary` JSON com chaves `campanhas` e `adgroups`
    - Test 5: run_marca_status_failed_quando_provider_lanca_excecao — Mock `analyzeCompany` lança `\RuntimeException('adman down')` quando `forceProvider='adman'`; mas retorna OK quando `forceProvider='ml'`; após run: row Adman tem `status='failed'`, `error` contém 'adman down'; row ML tem `status='completed'`. Validar resultado retornado `['adman_status'=>'failed','ml_status'=>'completed']`
    - Test 6: run_marca_status_failed_em_ml_nao_interrompe_adman — espelho do Test 5 mas com falha em ML
    - Test 7: run_calcula_raw_hash_deterministico — Mock retorna 2 itens com os MESMOS motivos+metrics; assertar que ambos têm o mesmo `raw_hash` (SHA256 de 64 chars hex)
    - Test 8: run_retorna_array_com_run_ids_e_status — `$result` keys = `['adman_run_id','ml_run_id','adman_status','ml_status']`; `adman_run_id` é int positivo; `ml_run_id` é int positivo
    - Test 9: run_usa_dryRun_true_em_ambas_chamadas_analyzeCompany — Mockery `->with(Mockery::any(), Mockery::any(), true, 'adman')->once()` e `->with(Mockery::any(), Mockery::any(), true, 'ml')->once()` (gate: garante que NÃO existe caminho onde `dryRun=false` seja passado)
  </behavior>
  <action>
    Criar `tests/Feature/Phase40/ShadowRunServiceTest.php` usando `RefreshDatabase` + Mockery. Setup: cria Company minimal + SugadorConfig minimal (dias_analise=7). Bind mock `SugadorAnalysisService` no container via `$this->app->instance(SugadorAnalysisService::class, $mock)` — segue mesmo pattern do 39-04 (validado funcional). Helper privado `mockAnalysisReturning(array $detalhesAdman, array $detalhesMl)` que configura o mock pra retornar `['skipped'=>false,'campanhas'=>X,'adgroups'=>Y,'detalhes'=>$detalhesAdman]` quando 4º arg é 'adman' e similar pra 'ml'. Helper `detalhesFixture(int $n, string $prefix)` gera N detalhes sintéticos com `tipo='adgroup'`, `campaign_id="{$prefix}-cmp-{$i}"`, `adgroup_id="{$prefix}-adg-{$i}"`, `mlb_id="MLB{$i}"`, `motivos=['cpc_alto']`, `metrics=['investment'=>10.0+$i,'revenue'=>0.0]`. Tests usam `assertDatabaseCount`, `assertDatabaseHas` com colunas chave; Test 5/6 usam Mockery throw via `andThrow`. 9 tests; rodar para confirmar 9/9 FAIL (`ShadowRunService` ainda não existe — `Class not found`).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase40\\\\ShadowRunService 2>&1 | tee /tmp/40-02-red.log; grep -E "FAIL|Errors|failures|not found" /tmp/40-02-red.log | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/Feature/Phase40/ShadowRunServiceTest.php` existe com 9 tests
    - Saída PHPUnit mostra `Tests: 9 failed` OU `Errors:` com `Class App\Services\Sugadores\ShadowRunService not found`
    - Suite RODA sem fatal/parse error (Mockery + bind container compila)
    - Commit message: `test(40-02): adiciona suite ShadowRunService (RED)`
  </acceptance_criteria>
  <done>Suite Feature commitada vermelha; gate RED atendido.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): implementa ShadowRunService</name>
  <files>app/Services/Sugadores/ShadowRunService.php</files>
  <read_first>
    - tests/Feature/Phase40/ShadowRunServiceTest.php (criada na Tarefa 1 — define expectativas)
    - app/Services/SugadorAnalysisService.php (linhas 1-160 — analyzeCompany retorna array com `detalhes`)
    - app/Models/SugadorProviderRun.php (Plan 40-01 — casts e fillable)
    - app/Models/SugadorProviderItem.php (Plan 40-01)
    - app/Models/SugadorConfig.php (método `forCompany` retorna config ou default)
    - .planning/phases/40-shadow-mode-tabelas-de-compara-o/40-CONTEXT.md (§decisions ShadowRunService)
  </read_first>
  <action>
    Criar `app/Services/Sugadores/ShadowRunService.php`. Namespace `App\Services\Sugadores`. Constructor: `public function __construct(private \App\Services\SugadorAnalysisService $analysis) {}`. Comentário de cabeçalho PHPDoc pt-BR: "Phase 40 Plan 40-02 — orquestra shadow runs paralelas (Adman+ML) por empresa+data. NÃO grava em `sugadores` (delegação ao SugadorAnalysisService com dryRun=true)."

    Método público:
    ```php
    public function run(\App\Models\Company $company, ?\Carbon\Carbon $referenceDate = null): array
    ```
    - `$referenceDate = $referenceDate ?? now()->startOfDay()`
    - Calcular `$periodoFim = $referenceDate->copy()->subDay()` e `$periodoInicio = $periodoFim->copy()->subDays(($config->dias_analise ?? 7) - 1)` lendo `\App\Models\SugadorConfig::forCompany($company)`
    - Loop sobre `['adman', 'ml']`. Pra cada provider:
      - Chamar método privado `executeForProvider($company, $referenceDate, $periodoInicio, $periodoFim, $providerName)` que retorna `['run_id'=>int, 'status'=>'completed'|'failed']`
    - Retornar `['adman_run_id'=>$results['adman']['run_id'], 'ml_run_id'=>$results['ml']['run_id'], 'adman_status'=>$results['adman']['status'], 'ml_status'=>$results['ml']['status']]`

    Método privado `executeForProvider(Company, Carbon, Carbon, Carbon, string $providerName): array`:
    1. Criar `$run = \App\Models\SugadorProviderRun::create([...])` com `provider=$providerName`, `status='running'`, `started_at=now()`, demais campos derivados
    2. Try:
       - `$result = $this->analysis->analyzeCompany($company, $referenceDate, true, $providerName)` (4º arg `dryRun=true` SEMPRE, 5º arg `$providerName`)
       - Para cada `$d` em `$result['detalhes']`, criar `SugadorProviderItem::create([...])` com `raw_hash = sha256` (ver abaixo)
       - `$run->update(['status'=>'completed','finished_at'=>now(),'summary'=>['campanhas'=>$result['campanhas']??0,'adgroups'=>$result['adgroups']??0,'items'=>count($result['detalhes']??[])]])`
       - Return `['run_id'=>$run->id, 'status'=>'completed']`
    3. Catch `\Throwable $e`:
       - `\Illuminate\Support\Facades\Log::error("[Sugadores Shadow] Empresa {$company->id} provider {$providerName}: " . $e->getMessage())`
       - `$run->update(['status'=>'failed','finished_at'=>now(),'error'=>substr($e->getMessage(), 0, 65000)])`
       - Return `['run_id'=>$run->id, 'status'=>'failed']`

    Método privado `computeRawHash(array $motivos, array $metrics): string`:
    - `$motivosSorted = $motivos; sort($motivosSorted);`
    - `$payload = json_encode(['motivos'=>$motivosSorted, 'metrics'=>$metrics], JSON_UNESCAPED_UNICODE | JSON_THROW_ON_ERROR | JSON_SORT_KEYS_NOT_AVAILABLE)` — usar `ksort($metrics)` antes do encode (JSON_SORT_KEYS_NOT_AVAILABLE não existe; recursivamente ordenar chaves antes)
    - Retornar `hash('sha256', $payload)` (string 64 chars hex)
    - Helper recursivo `sortRecursive(&$arr)` se metrics é array aninhado

    Mapeamento `$d` → SugadorProviderItem.create:
    - `tipo` = `$d['tipo']`
    - `campaign_id` = `(string)($d['campaign_id'] ?? '')`
    - `adgroup_id` = `$d['adgroup_id'] ?? null`
    - `mlb_id` = `$d['mlb_id'] ?? null`
    - `motivos` = `$d['motivos'] ?? []`
    - `metrics_json` = `$d['metrics'] ?? []`
    - `raw_hash` = `computeRawHash($d['motivos']??[], $d['metrics']??[])`

    Comentário inline pt-BR explicando que `dryRun=true` HARDCODED é o gate de segurança Phase 40 — NUNCA passar false aqui (Phase 42 vai introduzir caminho de gravação separado).

    NÃO modificar: SugadorAnalysisService, AdmanService, MercadoLivreService, factory, providers, Models de Plan 40-01, Sugador, SugadorConfig, qualquer Job ou Command. Validar via `git diff --name-only HEAD` após implementação = apenas 1 arquivo novo + 1 test.

    Rodar `php artisan test --filter=Phase40\\\\ShadowRunService` — 9/9 PASS.
    Rodar `php artisan test --filter=Sugador` — count >= 73 (65 Sugador baseline + 8 do Plan 40-01) verdes, zero regressão.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase40\\\\ShadowRunService 2>&1 | tee /tmp/40-02-green.log; grep -E "OK|PASS|Tests: 9 passed" /tmp/40-02-green.log | head -3 && php artisan test --filter=Sugador 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `app/Services/Sugadores/ShadowRunService.php` existe (1 arquivo novo)
    - 9/9 tests `Phase40\ShadowRunServiceTest` verdes
    - **GATE CRÍTICO REQ-40-02:** Test 3 (`run_NAO_grava_em_sugadores_GATE_CRITICO`) verde — assertion `Sugador::count()` antes == depois
    - `grep -c "dryRun=true\|true,.*'adman'\|true,.*'ml'\|true,.*\\\$providerName" app/Services/Sugadores/ShadowRunService.php` retorna >= 1 (gate hardcoded `true`)
    - `grep -c "Sugador::create\|Sugador::insert\|->sugadores->\|sugadores'.*create" app/Services/Sugadores/ShadowRunService.php` retorna 0 (zero gravação direta em sugadores)
    - `git diff --name-only HEAD~2 HEAD app/` mostra apenas `app/Services/Sugadores/ShadowRunService.php`
    - Suite Sugador completa: `php artisan test --filter=Sugador` continua com count >= 73 verdes (gate zero regressão)
    - Commit message: `feat(40-02): implementa ShadowRunService — 2 runs paralelas Adman+ML sem gravar em sugadores (GREEN)`
  </acceptance_criteria>
  <done>ShadowRunService entregue; 9/9 tests verdes; ZERO gravação em sugadores confirmada; suite Sugador sem regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ShadowRunService → SugadorAnalysisService | Composição via DI; passa `dryRun=true` hardcoded como gate de segurança |
| ShadowRunService → DB (sugador_provider_*) | Escrita em tabelas auxiliares Plan 40-01; ZERO escrita em `sugadores` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02-01 | Elevation of Privilege | ShadowRunService grava em `sugadores` | mitigate | `dryRun=true` HARDCODED na chamada a `analyzeCompany`; Test 3 (gate crítico) valida `Sugador::count()` antes==depois |
| T-40-02-02 | Denial of Service | Provider Adman down derruba run ML | mitigate | try/catch \Throwable por provider em métodos isolados; falha de um marca status=failed mas não propaga |
| T-40-02-03 | Information Disclosure | error column logando dados sensíveis | mitigate | `substr($e->getMessage(), 0, 65000)` evita overflow; mensagem técnica do provider; sem dump de payload completo |
| T-40-02-04 | Tampering | duplicação de runs no mesmo dia | accept | Plan 40-02 NÃO adiciona constraint unique (multiple runs/day é feature pra debugging); duplicação visível via `idx_company_ref_provider` no Plan 40-03 |
| T-40-02-SC | Tampering | composer/npm installs | n/a | Plan 40-02 não instala pacote — usa só Laravel core + Mockery (já no composer.json) |

Severity geral: NONE — service backend isolado, sem rotas HTTP, sem mudança em fluxo de produção.
</threat_model>

<verification>
- Tarefa 1: 9/9 tests RED
- Tarefa 2: 9/9 tests GREEN; Test 3 (gate crítico zero gravação) verde; suite Sugador sem regressão
- Grep gate: `dryRun=true` ou equivalente hardcoded confirmado; zero `Sugador::create` no service
- `git diff --name-only` confirma apenas 2 arquivos modificados (service + test)
</verification>

<success_criteria>
- 1 service novo (`ShadowRunService`) entregue
- 9/9 tests Feature verdes (incluindo gate crítico zero gravação)
- Falha isolada por provider (Adman down não impacta ML)
- Suite Sugador continua >= 73 verdes (zero regressão)
- 2 commits atômicos (RED + GREEN)
</success_criteria>

<output>
Criar `.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-02-SUMMARY.md` quando done.
</output>
</content>
</invoke>