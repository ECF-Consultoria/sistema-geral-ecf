---
phase: 40-shadow-mode-tabelas-de-compara-o
plan: 04
type: tdd
wave: 3
depends_on: ["40-02", "40-03"]
files_modified:
  - app/Console/Commands/SugadoresShadowMl.php
  - app/Console/Commands/SugadoresCompareProviders.php
  - config/sugadores.php
  - .env.example
  - routes/console.php
  - tests/Feature/Phase40/SugadoresShadowMlCommandTest.php
  - tests/Feature/Phase40/SugadoresCompareProvidersCommandTest.php
autonomous: true
requirements: [REQ-40-04, REQ-40-05, REQ-40-06, REQ-40-07, REQ-40-08]
tags: [phase-40, sugadores, shadow-mode, command, scheduler, ml-migration, tdd]

must_haves:
  truths:
    - "Comando `php artisan sugadores:shadow-ml --company={id|all} [--days=N]` registrado e descoberto pelo Artisan"
    - "Comando `php artisan sugadores:compare-providers --company={id} --from=YYYY-MM-DD --to=YYYY-MM-DD [--format=table|json]` registrado"
    - "Quando `--company=all`, shadow-ml lê `config('sugadores.ml_shadow_companies')` (CSV via env `SUGADORES_ML_SHADOW_COMPANIES`); env vazia + `--company=all` aborta com exit 1 e mensagem clara pt-BR"
    - "Quando `--company={id}` numérico, processa só aquela empresa (ignora env)"
    - "shadow-ml itera `--days` (default 1) dias para trás chamando `ShadowRunService::run($company, $date)` por dia"
    - "compare-providers `--format=json` produz JSON parseável com chaves matched, metrics_diff, motivo_diff, apenas_adman, apenas_ml, quarentena_diff, paridade_motivos_pct"
    - "compare-providers exit code 0 se `paridade_motivos_pct >= 95.0`, exit 1 caso contrário"
    - "compare-providers `--format=table` imprime tabela pt-BR com counts + paridade %"
    - "Scheduler em `routes/console.php` adiciona entrada `Schedule::command('sugadores:shadow-ml --company=all --days=1')->dailyAt('13:00')->timezone('America/Sao_Paulo')->onOneServer()->withoutOverlapping()` SEM modificar entradas existentes"
    - "`config/sugadores.php` retorna array com chave `ml_shadow_companies` => array de int (filtra vazios via `array_filter`)"
    - "`.env.example` ganha bloco `SUGADORES_ML_SHADOW_COMPANIES=` documentado em pt-BR"
  artifacts:
    - path: app/Console/Commands/SugadoresShadowMl.php
      provides: "Comando Artisan dispara ShadowRunService por empresa+dia"
    - path: app/Console/Commands/SugadoresCompareProviders.php
      provides: "Comando Artisan imprime relatório paridade (table|json); exit 1 se <95%"
    - path: config/sugadores.php
      provides: "Config Laravel expondo lista CSV de empresas elegíveis pra shadow"
    - path: .env.example
      provides: "Env SUGADORES_ML_SHADOW_COMPANIES documentada"
    - path: routes/console.php
      provides: "Scheduler diário 13h BRT pra shadow-ml --company=all"
    - path: tests/Feature/Phase40/SugadoresShadowMlCommandTest.php
      provides: "Suite cobrindo: --company={id} dispara N runs, --company=all respeita env, abort se env vazia, --days propaga"
    - path: tests/Feature/Phase40/SugadoresCompareProvidersCommandTest.php
      provides: "Suite cobrindo: exit 0 se paridade ≥95, exit 1 se <95, --format=json parseável, --format=table imprime relatório pt-BR"
  key_links:
    - from: app/Console/Commands/SugadoresShadowMl.php
      to: app/Services/Sugadores/ShadowRunService.php
      via: "DI no handle() → $shadow->run($company, $date)"
      pattern: "ShadowRunService.*run\\("
    - from: app/Console/Commands/SugadoresCompareProviders.php
      to: app/Services/Sugadores/ProviderComparisonService.php
      via: "DI no handle() → $comparison->compareWindow($companyId, $from, $to)"
      pattern: "compareWindow\\("
    - from: routes/console.php
      to: app/Console/Commands/SugadoresShadowMl.php
      via: "Schedule::command('sugadores:shadow-ml --company=all --days=1')"
      pattern: "sugadores:shadow-ml.*dailyAt"
---

<objective>
Plan 40-04 fecha a Phase 40 entregando os 2 comandos CLI (shadow-ml + compare-providers), o scheduler diário, a config + env. Wave 3 — consome ShadowRunService (Plan 40-02) e ProviderComparisonService (Plan 40-03). Cobre 5 requirements (REQ-40-04..08).

Purpose: tornar shadow mode operacional via cron diário + permitir verificação de paridade via CLI/CI (exit code automatável).

Output: 2 commands + 1 config + 1 edit em .env.example + 1 edit em routes/console.php + 2 suites Feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-CONTEXT.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-01-PLAN.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-02-PLAN.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-03-PLAN.md
@.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-05-SUMMARY.md
@app/Console/Commands/AnalyzeSugadores.php
@routes/console.php

<interfaces>
<!-- Pattern de comando consumido pelo Plan 40-04 (referência: AnalyzeSugadores Phase 39 Plan 39-05) -->

Padrão Command Laravel 12:
- $signature: 'sugadores:shadow-ml {--company= : ID ou "all"} {--days=1 : Dias para trás (1-90)}'
- $description: em pt-BR, descrevendo o que o comando faz
- handle(): retorna `Command::SUCCESS` (0) ou `Command::FAILURE` (1)
- DI via constructor — Laravel resolve services automaticamente

Services Plan 40-02 e 40-03:
- ShadowRunService::run(Company $c, ?Carbon $date = null): array{adman_run_id,ml_run_id,adman_status,ml_status}
- ProviderComparisonService::compareWindow(int $companyId, Carbon $from, Carbon $to): array (7 chaves)

Config pattern (config/sugadores.php — NOVO arquivo):
```php
return [
    'ml_shadow_companies' => array_map('intval', array_filter(
        array_map('trim', explode(',', env('SUGADORES_ML_SHADOW_COMPANIES', '')))
    )),
];
```

Scheduler atual em routes/console.php (linha ~44): `sugadores:analyze` roda dailyAt('12:00'). Plan 40-04 adiciona ENTRADA NOVA sem modificar a existente; horário 13:00 (1h depois).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): suites Feature para os 2 comandos</name>
  <files>
    tests/Feature/Phase40/SugadoresShadowMlCommandTest.php
    tests/Feature/Phase40/SugadoresCompareProvidersCommandTest.php
  </files>
  <read_first>
    - .planning/phases/40-shadow-mode-tabelas-de-compara-o/40-CONTEXT.md (§decisions 2 comandos)
    - app/Console/Commands/AnalyzeSugadores.php (pattern de signature, exit code, output pt-BR)
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-05-SUMMARY.md (decisões assertion command — `expectsOutputToContain` quirk; usar BufferedOutput via Kernel quando substring conflitar)
    - routes/console.php (não tocar nesta tarefa — só ler)
  </read_first>
  <behavior>
    **`SugadoresShadowMlCommandTest`:**
    - Test 1: comando_registrado_no_artisan — `Artisan::all()` contém chave `sugadores:shadow-ml`
    - Test 2: company_id_numerico_dispara_run_para_empresa — Mock `ShadowRunService` esperando `->run(Mockery::on(fn($c) => $c->id === $companyId), Mockery::any())->once()`; chamar `Artisan::call('sugadores:shadow-ml', ['--company' => $companyId])`; exit 0
    - Test 3: company_all_respeita_env_csv — Setar `config(['sugadores.ml_shadow_companies' => [10, 20]])`; mock esperando 2 chamadas (uma com company id 10, outra id 20); chamar `--company=all`; exit 0
    - Test 4: company_all_sem_env_aborta_exit_1 — `config(['sugadores.ml_shadow_companies' => []])`; chamar `--company=all`; assertar exit 1 + output contém pt-BR "Nenhuma empresa elegível" ou "SUGADORES_ML_SHADOW_COMPANIES vazia"
    - Test 5: days_param_itera_N_dias — `--company={id} --days=3`; mock esperando 3 chamadas (3 datas distintas)
    - Test 6: days_clamp_invalido_aborta_ou_clamp — `--days=0` ou `--days=999`: aborta com exit 1 OR clampa para [1,90]. Decisão Plan 40-04: clampa silenciosamente para max(1, min(90, $days)) — testar que com `--days=999` é tratado como 90 (3 mocks * 30 dias seria absurdo; vamos testar apenas que NÃO crasha e que clamp acontece via spy contando chamadas <= 90)
    - Test 7: empresa_inexistente_aborta — `--company=99999` (id inexistente); exit 1 com mensagem pt-BR "Empresa 99999 não encontrada"
    - Test 8: output_pt_br_progresso_e_summary — mock retornando `['adman_run_id'=>1,'ml_run_id'=>2,'adman_status'=>'completed','ml_status'=>'completed']`; output contém algo como "Empresa X" + summary final tipo "X runs Adman ok, Y runs ML ok, Z falhas"

    **`SugadoresCompareProvidersCommandTest`:**
    - Test 1: comando_registrado_no_artisan — `Artisan::all()` contém `sugadores:compare-providers`
    - Test 2: from_to_required — chamar sem `--from`: exit 1 + mensagem em pt-BR ou validação Laravel; chamar sem `--to`: idem
    - Test 3: company_required — chamar sem `--company`: exit 1
    - Test 4: format_table_imprime_relatorio_pt_br — Mock `ProviderComparisonService::compareWindow` retornando `['matched'=>8,'metrics_diff'=>1,'motivo_diff'=>1,'apenas_adman'=>2,'apenas_ml'=>1,'quarentena_diff'=>0,'paridade_motivos_pct'=>61.54]`; output contém "matched" ou "Coincidências" e "61,54%" ou "61.54%" (aceitar ambos formatos pt-BR)
    - Test 5: format_json_imprime_json_parseavel — `--format=json`; capturar output via BufferedOutput; `json_decode(...)` retorna array com as 7 chaves esperadas; chave `paridade_motivos_pct` é float
    - Test 6: exit_code_0_se_paridade_95 — Mock retorna `paridade_motivos_pct=95.0`; assertExitCode(0)
    - Test 7: exit_code_0_se_paridade_99 — Mock retorna 99.5; exit 0
    - Test 8: exit_code_1_se_paridade_abaixo_95 — Mock retorna 94.99; exit 1
    - Test 9: exit_code_1_se_paridade_zero — Mock retorna 0.0; exit 1

    Setup ambos: `RefreshDatabase`, bind mock no container via `$this->app->instance(ShadowRunService::class, $mock)` ou `$this->app->instance(ProviderComparisonService::class, $mock)`. Helper privado `makeCompany(int $id)` cria Company minimal com ID explícito. Comentários pt-BR.

    Total: 8 + 9 = 17 tests. Rodar para confirmar 17/17 FAIL (comandos não existem ainda).
  </behavior>
  <action>
    Criar `tests/Feature/Phase40/SugadoresShadowMlCommandTest.php` (8 tests) e `tests/Feature/Phase40/SugadoresCompareProvidersCommandTest.php` (9 tests). Namespace `Tests\Feature\Phase40`. Usar `RefreshDatabase`. Helper estático ou trait minimal `makeCompany` em cada arquivo. Tests 5 e 6 do segundo arquivo usam `Artisan::call` + capturando exit code retornado pra checar.

    Para `--format=json` (Test 5 compare-providers): usar `Artisan::call(...)` que retorna exit code; capturar output via `Artisan::output()` que devolve a string completa; `json_decode($output, true)` deve produzir array (NÃO null). Validar `is_float($decoded['paridade_motivos_pct'])`.

    Para tests com validação de argumento ausente (Test 2/3 compare-providers): Laravel marca options como required via `?` notation no signature ('{--from= : descrição}' não é required automaticamente — tem que ser validado dentro de `handle()`). Tests assumem que o handle valida e retorna exit 1 com mensagem.

    Rodar para 17/17 FAIL.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase40.*Command 2>&1 | tee /tmp/40-04-red.log; grep -E "FAIL|Errors|not found" /tmp/40-04-red.log | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Ambos arquivos de teste criados (`SugadoresShadowMlCommandTest.php` + `SugadoresCompareProvidersCommandTest.php`)
    - Saída PHPUnit mostra 17 testes falhando (Class not found OR Artisan command not found)
    - Suite RODA sem fatal/parse error
    - Commit message: `test(40-04): adiciona suites shadow-ml + compare-providers (RED)`
  </acceptance_criteria>
  <done>17 tests Feature vermelhos commitados; gate RED atendido.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): implementa 2 commands + config + env + scheduler</name>
  <files>
    app/Console/Commands/SugadoresShadowMl.php
    app/Console/Commands/SugadoresCompareProviders.php
    config/sugadores.php
    .env.example
    routes/console.php
  </files>
  <read_first>
    - tests/Feature/Phase40/SugadoresShadowMlCommandTest.php (criada na Tarefa 1)
    - tests/Feature/Phase40/SugadoresCompareProvidersCommandTest.php (criada na Tarefa 1)
    - app/Services/Sugadores/ShadowRunService.php (Plan 40-02)
    - app/Services/Sugadores/ProviderComparisonService.php (Plan 40-03)
    - app/Console/Commands/AnalyzeSugadores.php (pattern Phase 39 Plan 39-05)
    - routes/console.php (entradas Schedule existentes — NÃO MODIFICAR)
    - .env.example (encontrar bloco de envs ML ou Sugadores para colocar próximo)
  </read_first>
  <action>
    **1) `config/sugadores.php`** (arquivo NOVO):
    ```php
    <?php
    return [
        // Phase 40 — CSV de company_ids elegíveis para shadow mode ML.
        // Vazio = ninguém. Setar via env SUGADORES_ML_SHADOW_COMPANIES.
        'ml_shadow_companies' => array_map('intval', array_filter(array_map('trim',
            explode(',', (string) env('SUGADORES_ML_SHADOW_COMPANIES', ''))
        ))),
    ];
    ```

    **2) `.env.example`** — adicionar bloco no fim do arquivo (ou próximo a outras envs ML/Adman se houver):
    ```
    # Phase 40 — CSV de company_ids elegíveis para shadow mode ML (Sugadores).
    # Vazio = scheduler diário não roda shadow pra ninguém (use --company={id} manual).
    # Ex.: SUGADORES_ML_SHADOW_COMPANIES=10,20,42
    SUGADORES_ML_SHADOW_COMPANIES=
    ```

    **3) `app/Console/Commands/SugadoresShadowMl.php`** (arquivo NOVO):
    - Namespace `App\Console\Commands`; extends `Illuminate\Console\Command`
    - `protected $signature = 'sugadores:shadow-ml {--company= : ID ou \"all\"} {--days=1 : Dias para trás (clamp 1..90)}';`
    - `protected $description = 'Phase 40 — roda shadow mode (Adman + ML em paralelo) gravando em sugador_provider_runs/items sem alterar sugadores';`
    - Constructor: `public function __construct(private \App\Services\Sugadores\ShadowRunService $shadow) { parent::__construct(); }`
    - `handle(): int`:
      - `$companyOpt = $this->option('company');`
      - Validar: se vazio → erro pt-BR "Parâmetro --company obrigatório (id numérico ou 'all')" + return Command::FAILURE
      - Resolver lista de empresas:
        - Se `--company=all`: `$ids = config('sugadores.ml_shadow_companies', [])`. Se vazia → erro pt-BR "Nenhuma empresa elegível — defina SUGADORES_ML_SHADOW_COMPANIES" + return Command::FAILURE
        - Senão: `$ids = [(int)$companyOpt]`
      - `$days = max(1, min(90, (int) $this->option('days')))` (clamp silencioso contra T-40-04-01)
      - `$companies = \App\Models\Company::whereIn('id', $ids)->get()`. Pra cada id em $ids não encontrado: erro pt-BR "Empresa {$id} não encontrada" + return Command::FAILURE
      - Contadores: `$admanOk=0; $mlOk=0; $falhas=0;`
      - Loop empresas × loop dias (0..$days-1):
        - `$date = now()->startOfDay()->subDays($i)`
        - `$this->line("Empresa {$company->id} ({$company->name}) — {$date->toDateString()}")`
        - `try { $r = $this->shadow->run($company, $date); }` catch: `$falhas++; $this->error(...); continue`
        - `if ($r['adman_status']==='completed') $admanOk++; else $falhas++;`
        - `if ($r['ml_status']==='completed') $mlOk++; else $falhas++;`
      - `$this->info("Concluído: {$admanOk} runs Adman ok, {$mlOk} runs ML ok, {$falhas} falhas")`
      - Return Command::SUCCESS

    **4) `app/Console/Commands/SugadoresCompareProviders.php`** (arquivo NOVO):
    - Signature: `'sugadores:compare-providers {--company= : ID empresa (obrigatório)} {--from= : YYYY-MM-DD (obrigatório)} {--to= : YYYY-MM-DD (obrigatório)} {--format=table : table|json}'`
    - Description pt-BR
    - Constructor: `public function __construct(private \App\Services\Sugadores\ProviderComparisonService $comparison) { parent::__construct(); }`
    - `handle(): int`:
      - Validar `--company`, `--from`, `--to` não vazios → erro pt-BR + Command::FAILURE
      - Parsear datas com try/catch (`Carbon::parse`); inválido → erro pt-BR + Command::FAILURE
      - `$format = $this->option('format');` validar in ['table','json'] → erro se diferente
      - `$report = $this->comparison->compareWindow((int)$this->option('company'), $from, $to)`
      - Se `format='json'`: `$this->line(json_encode($report, JSON_UNESCAPED_UNICODE | JSON_PRETTY_PRINT))`
      - Se `format='table'`:
        - `$this->table(['Bucket','Count'], [['Coincidências (matched)', $report['matched']], ['Métricas divergentes', $report['metrics_diff']], ['Motivos divergentes', $report['motivo_diff']], ['Apenas Adman', $report['apenas_adman']], ['Apenas ML', $report['apenas_ml']], ['Quarentena divergente', $report['quarentena_diff']]])`
        - `$this->line(sprintf('Paridade de motivos: %.2f%%', $report['paridade_motivos_pct']))`
      - Return `$report['paridade_motivos_pct'] >= 95.0 ? Command::SUCCESS : Command::FAILURE`

    **5) `routes/console.php`** — adicionar APÓS o bloco `Schedule::command('sugadores:cleanup-quarentena')` (linha ~50 atual), SEM tocar nas entradas existentes:
    ```php
    // Phase 40 — Shadow mode ML diário às 13h BRT (1h depois do sugadores:analyze 12h).
    // Só dispara pras empresas em SUGADORES_ML_SHADOW_COMPANIES. NÃO escreve em `sugadores`.
    Schedule::command('sugadores:shadow-ml --company=all --days=1')
        ->dailyAt('13:00')
        ->timezone('America/Sao_Paulo')
        ->name('sugadores-shadow-ml-daily')
        ->onOneServer()
        ->withoutOverlapping();
    ```

    Rodar `php artisan test --filter=Phase40` — TODOS os tests da Phase 40 verdes (8 do Plan 40-01 + 9 do Plan 40-02 + 15 do Plan 40-03 + 17 desta Tarefa 2 = 49 total). Suite Sugador `php artisan test --filter=Sugador` continua sem regressão.

    Não modificar `SugadorAnalysisService.php`, providers, factory, models, ShadowRunService, ProviderComparisonService (criados em plans anteriores), `AnalyzeSugadores.php`, `CleanupSugadoresQuarentena.php`, ou qualquer Schedule existente em routes/console.php.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase40 2>&1 | tee /tmp/40-04-green.log; grep -E "OK|PASS|Tests:" /tmp/40-04-green.log | head -5 && php artisan list 2>&1 | grep -E "sugadores:shadow-ml|sugadores:compare-providers" && php artisan schedule:list 2>&1 | grep -E "sugadores:shadow-ml|sugadores-shadow-ml" && php artisan test --filter=Sugador 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 2 comandos visíveis em `php artisan list`: `sugadores:shadow-ml` E `sugadores:compare-providers`
    - `php artisan schedule:list` mostra entrada `sugadores-shadow-ml-daily` agendada para `13:00` timezone `America/Sao_Paulo`
    - `grep -c "dailyAt('13:00')" routes/console.php` retorna >= 1 (entrada NOVA)
    - `grep -c "timezone('America/Sao_Paulo')" routes/console.php` retorna >= 2 (Plan 40-04 adiciona 1; outra já existe do polos)
    - `grep -c "onOneServer\|withoutOverlapping" routes/console.php` mostra ambos no bloco novo (verificar manualmente OR `grep -A 5 "sugadores-shadow-ml-daily" routes/console.php` mostra ambos)
    - `php artisan tinker --execute="echo json_encode(config('sugadores.ml_shadow_companies'));"` retorna `[]` quando env vazia, `[10,20]` quando `SUGADORES_ML_SHADOW_COMPANIES=10,20`
    - `grep -c "SUGADORES_ML_SHADOW_COMPANIES" .env.example` retorna >= 1
    - Todos os 17 tests do Plan 40-04 verdes (`Tests: 17 passed` no filtro `Phase40\\\\Sugadores.*Command`)
    - Total Phase 40 acumulado: `php artisan test --filter=Phase40` mostra >= 49 tests passados
    - Suite Sugador: `php artisan test --filter=Sugador` continua com count >= baseline + tests novos da Phase 40 (zero regressão)
    - Validação gate exit codes:
      - `php artisan sugadores:compare-providers --company=1 --from=2026-06-01 --to=2026-06-25 --format=json` em empresa vazia (sem runs) retorna paridade 100 OR aborta limpo
      - Smoke manual: comando registrado e responde sem fatal
    - `git diff --name-only HEAD` mostra exatamente os 7 arquivos do `files_modified` (2 commands + config + .env.example + routes/console.php + 2 tests)
    - NÃO existem modificações em `routes/console.php` linhas existentes — apenas linhas adicionadas no fim (validar via `git diff routes/console.php` mostrando apenas `+` lines no bloco novo)
    - Commit message: `feat(40-04): comandos sugadores:shadow-ml + sugadores:compare-providers + scheduler 13h BRT + config/env (GREEN)`
  </acceptance_criteria>
  <done>2 comandos registrados; scheduler adicionado sem tocar existentes; config + env documentadas; 17/17 tests verdes; Phase 40 inteira pronta para SUMMARY + deploy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → ShadowRunService | Comando Artisan invocável via cron; sem exposição HTTP |
| CLI → ProviderComparisonService | Leitura de tabelas auxiliares; sem efeito colateral em DB |
| Scheduler (cron) → CLI | Roda como user `www-data` no VPS; mesma escala que outros schedules |
| env SUGADORES_ML_SHADOW_COMPANIES | Lista de IDs; sem secret material |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04-01 | DoS | `--days=999999` em loop | mitigate | clamp `max(1, min(90, $days))` no handle do shadow-ml |
| T-40-04-02 | DoS | `--company=all` com env populada com IDs inválidos | mitigate | validar existência via `Company::whereIn(...)->get()` antes do loop; abort exit 1 com mensagem pt-BR específica |
| T-40-04-03 | Information Disclosure | `--format=json` em pipe pra log | accept | output já é destinado a CI/cron; sem PII além de IDs/métricas que já estão em `sugadores` (não é dado novo) |
| T-40-04-04 | Tampering | Edit incorreto em `routes/console.php` derruba schedule existente | mitigate | acceptance_criteria valida via `git diff routes/console.php` que apenas linhas foram ADICIONADAS (não modificadas) |
| T-40-04-05 | Spoofing | env CSV manipulada por terceiro injetando ID arbitrário | accept | env é gerenciada por sysadmin no VPS; mesmo modelo de trust que `ADMAN_API_KEY` e demais envs |
| T-40-04-06 | Elevation of Privilege | comando expõe rota HTTP indiretamente | n/a | Plan 40-04 NÃO cria rota HTTP; só Artisan commands |
| T-40-04-SC | Tampering | composer/npm installs | n/a | Plan 40-04 não instala pacote — Laravel core + Carbon (já no composer.json) |

Severity geral: NONE — comandos CLI sem efeito em produção (`dryRun=true` garantido pelo Plan 40-02 que esses comandos consomem); scheduler isolado (`onOneServer` + `withoutOverlapping`); env é hint pra escopo, não credencial.
</threat_model>

<verification>
- Tarefa 1: 17 tests RED (8 shadow-ml + 9 compare-providers)
- Tarefa 2: 17/17 GREEN; comandos visíveis em `artisan list`; scheduler em `schedule:list`; tinker confirma config; .env.example documenta env; git diff confirma apenas adições em routes/console.php
- Phase 40 acumulado: >= 49 tests verdes (8 + 9 + 15 + 17)
- Suite Sugador continua sem regressão
- Smoke manual real (rodar contra MariaDB em prod) DEFERIDO até MariaDB local voltar — não bloqueia Phase 40 (tests SQLite em-memory cobrem)
</verification>

<success_criteria>
- 2 commands Artisan funcionais com exit codes corretos (REQ-40-04, REQ-40-05)
- Scheduler adicionado sem tocar entradas existentes (REQ-40-06)
- Config + env documentadas (REQ-40-07)
- 17 testes Feature verdes cobrindo signature, exit codes, formato output, abort cases (REQ-40-08 parte)
- 2 commits atômicos (RED + GREEN)
- Phase 40 INTEIRA pronta para deploy
</success_criteria>

<output>
Criar `.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-04-SUMMARY.md` quando done.
</output>
</content>
</invoke>