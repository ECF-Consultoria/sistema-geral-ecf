---
phase: 40-shadow-mode-tabelas-de-compara-o
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php
  - app/Models/SugadorProviderRun.php
  - app/Models/SugadorProviderItem.php
  - tests/Feature/Phase40/CreateSugadorProviderTablesTest.php
autonomous: true
requirements: [REQ-40-01]
tags: [phase-40, sugadores, migration, shadow-mode, ml-migration, tdd]

must_haves:
  truths:
    - "Após `php artisan migrate`, as tabelas `sugador_provider_runs` e `sugador_provider_items` existem com schema exato do §decisions do CONTEXT.md"
    - "`sugador_provider_runs` possui foreign key `company_id → companies.id` com cascadeOnDelete"
    - "`sugador_provider_items` possui foreign key `run_id → sugador_provider_runs.id` com cascadeOnDelete"
    - "Índice composto `idx_company_ref_provider (company_id, reference_date, provider)` presente em `sugador_provider_runs`"
    - "Índice composto `idx_run_tipo (run_id, tipo)` presente em `sugador_provider_items`"
    - "Models Eloquent `SugadorProviderRun` e `SugadorProviderItem` declaram `$fillable` e `$casts` corretos (json arrays, datetime tz)"
    - "Relação `SugadorProviderRun::items()` retorna `HasMany(SugadorProviderItem::class)`; `SugadorProviderItem::run()` retorna `BelongsTo(SugadorProviderRun::class)`"
    - "Migration é idempotente — guard `Schema::hasTable` impede re-criação"
    - "Não toca em `sugadores`, `sugador_configs`, `sugador_acoes`"
  artifacts:
    - path: database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php
      provides: "Migration cria 2 tabelas + índices compostos + FKs com cascade"
    - path: app/Models/SugadorProviderRun.php
      provides: "Model Eloquent com casts (summary array, started_at/finished_at immutable_datetime) + relação items()"
    - path: app/Models/SugadorProviderItem.php
      provides: "Model Eloquent com casts (motivos array, metrics_json array) + relação run()"
    - path: tests/Feature/Phase40/CreateSugadorProviderTablesTest.php
      provides: "Suite Feature valida schema (colunas, índices, FKs) + relação Eloquent + cast comportamento"
  key_links:
    - from: app/Models/SugadorProviderItem.php
      to: sugador_provider_runs
      via: foreignId('run_id')
      pattern: "BelongsTo.*SugadorProviderRun"
    - from: tests/Feature/Phase40/CreateSugadorProviderTablesTest.php
      to: database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php
      via: Schema::hasTable + Schema::hasColumn assertions
      pattern: "assertTrue\\(Schema::hasTable"
---

<objective>
Plan 40-01 entrega a fundação de dados da Phase 40: 1 migration que cria as 2 tabelas auxiliares (`sugador_provider_runs` 10 colunas + `sugador_provider_items` 8 colunas) e 2 Models Eloquent espelhando o schema. Sem isso, Plans 40-02 (ShadowRunService) e 40-03 (ProviderComparisonService) não têm onde gravar/ler.

Purpose: registrar runs paralelas Adman+ML por empresa+data com motivos detectados, mantendo `sugadores` intacta (escrita só por path Adman atual).
Output: 1 migration + 2 Models + 1 test Feature validando schema + relações.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-CONTEXT.md
@database/migrations/2026_06_22_000000_create_polos_faturamento_snapshots_table.php
@app/Models/Sugador.php

<interfaces>
<!-- Sugador (referência de casts/fillable). Phase 40 NÃO modifica este model. -->

Sugador:
- $table = 'sugadores'
- $casts inclui: reference_date date, motivos array, raw_data array, decimal:2 para valores monetários
- Trait LogsActivity (NÃO replicar em SugadorProviderRun/Item — tabelas auxiliares não precisam de audit log)

Padrão de migration do projeto (do polos_faturamento_snapshots):
- Guard idempotência: `if (Schema::hasTable('nome')) return;` no início de up()
- Down: `Schema::dropIfExists('nome')` — mas com 2 tabelas com FK, dropar FILHA primeiro
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): suite Feature valida schema das 2 tabelas + relações Eloquent</name>
  <files>tests/Feature/Phase40/CreateSugadorProviderTablesTest.php</files>
  <read_first>
    - .planning/phases/40-shadow-mode-tabelas-de-compara-o/40-CONTEXT.md (§decisions schema completo)
    - app/Models/Sugador.php (referência de casts e estilo)
    - database/migrations/2026_06_22_000000_create_polos_faturamento_snapshots_table.php (padrão de migration idempotente)
  </read_first>
  <behavior>
    - Test 1: schema_sugador_provider_runs_tem_colunas_corretas — `Schema::hasTable('sugador_provider_runs')` true; `Schema::hasColumns('sugador_provider_runs', ['id','company_id','provider','reference_date','periodo_inicio','periodo_fim','status','started_at','finished_at','error','summary','created_at','updated_at'])` true
    - Test 2: schema_sugador_provider_items_tem_colunas_corretas — `Schema::hasColumns('sugador_provider_items', ['id','run_id','tipo','campaign_id','adgroup_id','mlb_id','motivos','metrics_json','raw_hash','created_at','updated_at'])` true
    - Test 3: indice_composto_runs_company_ref_provider_existe — consultar `DB::select` em `sqlite_master` (ou `SHOW INDEX` quando MySQL) filtrando `name = 'idx_company_ref_provider'`; assertCount >= 1
    - Test 4: indice_composto_items_run_tipo_existe — análogo, `name = 'idx_run_tipo'`
    - Test 5: foreign_key_company_id_cascade_on_delete — criar 1 Company, 1 SugadorProviderRun apontando pra ela; deletar Company; assertDatabaseMissing('sugador_provider_runs', ['company_id' => $companyId])
    - Test 6: foreign_key_run_id_cascade_on_delete — criar Run + 2 Items; deletar Run; assertDatabaseCount('sugador_provider_items', 0)
    - Test 7: model_sugador_provider_run_casts_e_relacao — instanciar SugadorProviderRun com summary=['k'=>1] e started_at=now(); save+refresh; `is_array($run->summary)` true; `$run->started_at instanceof CarbonInterface` true; criar item belongsTo run; `$run->items()->first()->id === $item->id`
    - Test 8: model_sugador_provider_item_casts_e_relacao — instanciar SugadorProviderItem com motivos=['cpc_alto'] e metrics_json=['investment'=>100.5]; save+refresh; `is_array($item->motivos)` true; `is_array($item->metrics_json)` true; `$item->run->id === $run->id`
  </behavior>
  <action>
    Criar `tests/Feature/Phase40/CreateSugadorProviderTablesTest.php` usando `RefreshDatabase`. Namespace `Tests\Feature\Phase40`. Helper privado `makeCompany()` que cria minimal Company via `Company::factory()` ou `Company::create(['name'=>'Test','active'=>true])` se factory não existir. Helper `makeRun(Company $c)` retorna SugadorProviderRun preenchido com provider='adman', reference_date=today, periodo_inicio=today subDays 7, periodo_fim=today subDay, status='running', started_at=now(). Tests usam `Schema::hasTable`, `Schema::hasColumns`, `DB::select` direto contra `sqlite_master` (Test 3/4 — SQLite em-memory é o driver de teste default) para verificar índices nomeados. Comentários pt-BR. Suite DEVE rodar 8/8 FAIL antes da Tarefa 2 (migration e models não existem ainda).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase40\\\\CreateSugadorProviderTables 2>&1 | tee /tmp/40-01-red.log; grep -E "FAIL|Errors|failures" /tmp/40-01-red.log | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/Feature/Phase40/CreateSugadorProviderTablesTest.php` existe
    - Suite RODA (sem fatal/parse error)
    - 8/8 tests FALHAM com mensagens esperadas (tabela não existe / model não existe)
    - Saída contém "Tests: 8 failed" OR cada test com "Failed asserting" / "Class ... not found"
    - Commit message: `test(40-01): adiciona suite schema sugador_provider_runs+items (RED)`
  </acceptance_criteria>
  <done>Suite Feature commitada vermelha; gate RED do TDD atendido.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): migration cria 2 tabelas + 2 Models Eloquent</name>
  <files>
    database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php
    app/Models/SugadorProviderRun.php
    app/Models/SugadorProviderItem.php
  </files>
  <read_first>
    - tests/Feature/Phase40/CreateSugadorProviderTablesTest.php (criada na Tarefa 1)
    - .planning/phases/40-shadow-mode-tabelas-de-compara-o/40-CONTEXT.md (§decisions schema)
    - database/migrations/2026_06_22_000000_create_polos_faturamento_snapshots_table.php (padrão idempotência + down())
    - app/Models/Sugador.php (estilo de casts/fillable em pt-BR)
  </read_first>
  <action>
    1) **Migration** `database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php` — `return new class extends Migration`. `up()`:
       - Guard `if (Schema::hasTable('sugador_provider_runs')) { return; }`
       - `Schema::create('sugador_provider_runs', function (Blueprint $table) {` com EXATAMENTE: id; foreignId('company_id')->constrained('companies')->cascadeOnDelete(); string('provider', 32)->index(); date('reference_date')->index(); date('periodo_inicio'); date('periodo_fim'); string('status', 32)->index(); timestampTz('started_at'); timestampTz('finished_at')->nullable(); text('error')->nullable(); json('summary')->nullable(); timestamps(); index(['company_id','reference_date','provider'], 'idx_company_ref_provider')
       - Guard separado `if (Schema::hasTable('sugador_provider_items')) { return; }`
       - `Schema::create('sugador_provider_items', function (Blueprint $table) {` com: id; foreignId('run_id')->constrained('sugador_provider_runs')->cascadeOnDelete(); string('tipo', 32); string('campaign_id', 64)->index(); string('adgroup_id', 64)->nullable()->index(); string('mlb_id', 32)->nullable()->index(); json('motivos'); json('metrics_json'); string('raw_hash', 64)->index(); timestamps(); index(['run_id','tipo'], 'idx_run_tipo')
       - `down()`: dropar FILHA primeiro — `Schema::dropIfExists('sugador_provider_items')` então `Schema::dropIfExists('sugador_provider_runs')` (ordem importa por FK)
       - Comentário de cabeçalho pt-BR explicando: Phase 40 Plan 40-01; tabelas auxiliares pra shadow mode Sugadores; NÃO substitui `sugadores`
    2) **Model** `app/Models/SugadorProviderRun.php` — namespace `App\Models`; extends `Model`; `protected $table = 'sugador_provider_runs'`; `$fillable = ['company_id','provider','reference_date','periodo_inicio','periodo_fim','status','started_at','finished_at','error','summary']`; `$casts = ['reference_date' => 'date', 'periodo_inicio' => 'date', 'periodo_fim' => 'date', 'started_at' => 'immutable_datetime', 'finished_at' => 'immutable_datetime', 'summary' => 'array']`; método `items(): HasMany { return $this->hasMany(SugadorProviderItem::class, 'run_id'); }`; método `company(): BelongsTo { return $this->belongsTo(Company::class); }`. SEM trait LogsActivity (tabela auxiliar — sem audit log). Comentário PHPDoc pt-BR no topo: "Run de shadow mode Phase 40. Uma row por execução (empresa+data+provider). Detalhes em SugadorProviderItem."
    3) **Model** `app/Models/SugadorProviderItem.php` — namespace `App\Models`; extends `Model`; `protected $table = 'sugador_provider_items'`; `$fillable = ['run_id','tipo','campaign_id','adgroup_id','mlb_id','motivos','metrics_json','raw_hash']`; `$casts = ['motivos' => 'array', 'metrics_json' => 'array']`; método `run(): BelongsTo { return $this->belongsTo(SugadorProviderRun::class, 'run_id'); }`. SEM trait LogsActivity. Comentário PHPDoc pt-BR no topo: "Item detectado em uma run de shadow mode Phase 40. raw_hash = SHA256 de motivos+metrics_json normalizado para detecção de duplicatas."
    4) Rodar `php artisan migrate --pretend` (sanity) e depois `php artisan test --filter=Phase40\\\\CreateSugadorProviderTables` — 8/8 PASS.
    NÃO modificar nenhum arquivo fora dos 3 listados em `<files>`. NÃO criar config/sugadores.php (Plan 40-04).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase40\\\\CreateSugadorProviderTables 2>&1 | tee /tmp/40-01-green.log; grep -E "OK|PASS|Tests: 8 passed" /tmp/40-01-green.log | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php` existe e roda sem erro (`php artisan migrate --pretend` mostra `CREATE TABLE sugador_provider_runs` E `CREATE TABLE sugador_provider_items`)
    - Arquivos `app/Models/SugadorProviderRun.php` e `app/Models/SugadorProviderItem.php` existem
    - `grep -c "idx_company_ref_provider" database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php` retorna 1
    - `grep -c "idx_run_tipo" database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php` retorna 1
    - `grep -c "cascadeOnDelete" database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php` retorna >= 2
    - `php artisan test --filter=Phase40\\\\CreateSugadorProviderTables` mostra `Tests: 8 passed`
    - Suite Sugador completa zero regressão: `php artisan test --filter=Sugador` mostra count >= 65 verdes (baseline pré-Phase 40 — Phase 39 fechou em 65/65)
    - Commit message: `feat(40-01): migration sugador_provider_runs+items + Models Eloquent (GREEN)`
  </acceptance_criteria>
  <done>Migration + 2 Models commitados; 8/8 tests Phase 40 verdes; suite Sugador continua verde sem regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 40 → DB | Migration cria tabelas novas; FKs com cascadeOnDelete contidas no schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Tampering | Migration order (FK cascade) | mitigate | down() dropa filha (`sugador_provider_items`) antes da pai (`sugador_provider_runs`) — evita erro de FK no rollback |
| T-40-01-02 | DoS | Re-run de migration em prod | mitigate | guards `Schema::hasTable` idempotentes em up() — re-run não recria nem dropa |
| T-40-01-03 | Information Disclosure | error column (text) | accept | mensagens de erro de provider podem conter cust_id; texto livre é necessário pra debug; sem PII real (cust_id já está em `companies` e `mlb_empresas`) |
| T-40-01-SC | Tampering | composer/npm installs | n/a | Plan 40-01 não instala nenhum pacote novo (apenas Eloquent/Schema já presentes em Laravel 12) |

Severity geral: NONE — schema novo isolado, sem mudança em `sugadores`, sem novas dependências, sem rotas HTTP.
</threat_model>

<verification>
- Tarefa 1: 8/8 tests RED (saída PHPUnit mostra falhas esperadas — tabela/model não existe)
- Tarefa 2: 8/8 tests GREEN; `php artisan migrate --pretend` mostra ambas as tabelas; `php artisan test --filter=Sugador` continua em 65+ verdes (zero regressão)
- Grep: `grep -rn "use App\\\\Models\\\\SugadorProviderRun\\|use App\\\\Models\\\\SugadorProviderItem" app/` deve retornar 0 ocorrências fora dos 2 Models (Plan 40-01 não consome models em outros lugares — Plans 40-02/03 farão isso)
</verification>

<success_criteria>
- 2 tabelas criadas com schema exato do CONTEXT §decisions
- 2 Models Eloquent com casts/fillable/relações corretas
- 1 test Feature 8/8 verde validando schema + relações + cascade
- Suite Sugador acumulada continua sem regressão
- 2 commits atômicos (RED + GREEN)
</success_criteria>

<output>
Criar `.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-01-SUMMARY.md` quando done (formato padrão do template).
</output>
</content>
</invoke>