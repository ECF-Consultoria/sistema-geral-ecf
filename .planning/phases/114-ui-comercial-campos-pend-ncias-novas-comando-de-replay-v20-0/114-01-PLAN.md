---
phase: 114-ui-comercial-campos-pend-ncias-novas-comando-de-replay-v20-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/ComercialController.php
  - tests/Feature/Phase114ComercialListagemEnrichmentTest.php
autonomous: true
requirements: [HUB-UI-01, HUB-UI-02]

must_haves:
  truths:
    - "A listagem Comercial expõe nome_contato/cargo_contato/hubspot_observacao/hubspot_deal_id/hubspot_company_id por empresa de origem HubSpot"
    - "Cada contrato do payload traz hubspot_valor_original/hubspot_valor_normalizado_mensal/hubspot_valor_confidence/hubspot_valor_warning/hubspot_billing_frequency (best-effort/nullable)"
    - "A pendência valor_revisar aparece só para empresa origem HubSpot com contrato ativo confidence='low' OU warning não-nulo"
    - "A pendência sem_contato aparece quando nome_contato é null numa empresa origem HubSpot"
    - "A pendência possivel_duplicidade aparece quando o evento/snapshot registrou dedup fraco"
    - "Empresa legada não-HubSpot continua sem NENHUMA pendência (as 3 novas inclusive)"
  artifacts:
    - path: "app/Http/Controllers/ComercialController.php"
      provides: "payload enriquecido + 3 pendências novas + counts + whitelist"
      contains: "valor_revisar"
    - path: "tests/Feature/Phase114ComercialListagemEnrichmentTest.php"
      provides: "cobertura das 3 pendências novas + campos novos + gate de não-regressão origem"
  key_links:
    - from: "ComercialController::listagem"
      to: "pendencia_counts"
      via: "3 chaves novas somadas"
      pattern: "sem_contato|valor_revisar|possivel_duplicidade"
    - from: "ComercialController::listagem"
      to: "whitelist do filtro pendencia"
      via: "in_array com as 8 chaves"
      pattern: "possivel_duplicidade"
---

<objective>
Estender o backend da listagem Comercial (`ComercialController::listagem()` + `calcularPendenciasComerciais()`) para expor os dados enriquecidos capturados nas Fases 111–113 e calcular 3 pendências novas — SÓ para empresas de origem HubSpot, de forma ADITIVA (as 5 pendências e os contadores atuais não mudam).

Purpose: HUB-UI-01 (novos campos no payload) e HUB-UI-02 (pendências novas origem-HubSpot). É a fundação de payload que o plano 114-02 (frontend) consome.
Output: payload da listagem com campos de contato/observação/IDs por empresa + bloco de valor por contrato; 3 pendências novas (`sem_contato`, `valor_revisar`, `possivel_duplicidade`) somadas em `pendencia_counts` e adicionadas à whitelist do filtro; suite de teste nova provando enriquecimento + isolamento origem-HubSpot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-ui-comercial-campos-pend-ncias-novas-comando-de-replay-v20-0/114-CONTEXT.md

<interfaces>
<!-- Colunas já existentes (fillable/cast confirmados) — usar direto, sem exploração. -->

Company (app/Models/Company.php) — fillable inclui:
  nome_contato, cargo_contato, hubspot_deal_id, hubspot_company_id,
  hubspot_contact_id, hubspot_domain, hubspot_observacao, hubspot_snapshot
  cast: hubspot_snapshot => 'array'

ContratoServico (app/Models/ContratoServico.php) — fillable inclui:
  hubspot_billing_frequency, hubspot_valor_original,
  hubspot_valor_normalizado_mensal, hubspot_valor_confidence, hubspot_valor_warning
  cast: hubspot_valor_original => 'decimal:2', hubspot_valor_normalizado_mensal => 'decimal:2'

HubspotEvento (app/Models/HubspotEvento.php):
  payload => 'array'; o dedup fraco (113-03) grava payload['possivel_duplicidade']
  = { candidate_company_id, via, nome_normalizado }.
  hubspot_snapshot['warnings'] da Company também carrega uma entrada
  { tipo: 'possivel_duplicidade', candidate_company_id, via, nome_normalizado }.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender calcularPendenciasComerciais com sem_contato/valor_revisar/possivel_duplicidade</name>
  <files>app/Http/Controllers/ComercialController.php, tests/Feature/Phase114ComercialListagemEnrichmentTest.php</files>
  <read_first>
    - app/Http/Controllers/ComercialController.php (método calcularPendenciasComerciais ~404-484 e a guarda `if (!$c->is_origem_hubspot) return [];`)
    - .planning/phases/114-ui-comercial-campos-pend-ncias-novas-comando-de-replay-v20-0/114-CONTEXT.md (seção "Backend — pendências novas (HUB-UI-02)")
    - app/Http/Controllers/Api/HubspotWebhookController.php (linhas ~441-485 — onde o dedup fraco grava payload['possivel_duplicidade'] e hubspot_snapshot['warnings'])
  </read_first>
  <behavior>
    - sem_contato: empresa origem HubSpot com nome_contato null/'' → pendência presente; com nome_contato preenchido → ausente.
    - valor_revisar: empresa origem HubSpot com ALGUM contrato ativo de hubspot_valor_confidence='low' OU hubspot_valor_warning não-nulo → presente; contratos todos 'high'/'medium' e sem warning → ausente.
    - possivel_duplicidade: empresa origem HubSpot cujo hubspot_snapshot['warnings'] tem entrada tipo='possivel_duplicidade' OU algum hubspotEventos carregado tem payload['possivel_duplicidade'] → presente; sem marcação → ausente.
    - Isolamento (gate crítico): empresa legada (não origem HubSpot) retorna [] mesmo com nome_contato null, contrato low e sem snapshot — a guarda de origem no topo do método já cobre; nenhuma das 3 novas escapa dela.
    - As 5 pendências atuais permanecem idênticas (mesma ordem e condições).
  </behavior>
  <action>
    Escreva primeiro os testes (RED) no arquivo novo tests/Feature/Phase114ComercialListagemEnrichmentTest.php cobrindo os 5 casos do bloco behavior (reaproveite os helpers actingAsAdmin/criarEmpresa/marcarOrigemHubspot/criarContrato do Phase37ComercialListagemTest — pode copiá-los para a nova suite). Depois estenda o método privado calcularPendenciasComerciais em ComercialController, APÓS as 5 pendências existentes e ANTES do return, mantendo intacta a guarda de origem no topo. Para sem_contato, cheque nome_contato null ou string vazia. Para valor_revisar, itere os contratos ativos já carregados ($c->contratosServico->where('ativo', true)) e marque a pendência se algum tiver hubspot_valor_confidence==='low' OU hubspot_valor_warning !== null. Para possivel_duplicidade, cheque primeiro $c->hubspot_snapshot['warnings'] (array cast) por uma entrada com tipo==='possivel_duplicidade'; como fallback, cheque a relação $c->hubspotEventos já carregada por payload['possivel_duplicidade'] presente. Não faça query nova dentro do método — use só o que a listagem já eager-loada (hubspotEventos limit 3 + colunas da company). Comentários em pt-BR explicando a regra de negócio de cada nova pendência.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase114ComercialListagemEnrichmentTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Phase114ComercialListagemEnrichmentTest` verde nos 5 casos de pendência
    - pendência valor_revisar NÃO aparece para empresa legada (não-HubSpot) mesmo com contrato low
    - sem_contato aparece quando nome_contato é null numa empresa origem HubSpot
  </acceptance_criteria>
  <done>calcularPendenciasComerciais retorna as 3 pendências novas só para origem HubSpot; testes verdes; guarda de origem intacta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enriquecer payload da listagem (campos de contato/IDs + bloco de valor) + counts + whitelist + detalhes</name>
  <files>app/Http/Controllers/ComercialController.php, tests/Feature/Phase114ComercialListagemEnrichmentTest.php</files>
  <read_first>
    - app/Http/Controllers/ComercialController.php (bloco de payload por empresa ~307-343; $pendenciaCounts ~244-257; whitelist do filtro pendencia ~194-196; $detalhes ~286-305)
    - tests/Feature/Phase114ComercialListagemEnrichmentTest.php (suite criada na Task 1 — adicionar asserções de payload/counts)
    - tests/Feature/Phase37ComercialListagemTest.php (contrato de payload atual — NÃO pode regredir)
  </read_first>
  <behavior>
    - Payload por empresa origem HubSpot inclui as chaves nome_contato, cargo_contato, hubspot_observacao, hubspot_deal_id, hubspot_company_id (valores vindos das colunas; null quando ausentes).
    - Cada item de contratos_servico inclui hubspot_valor_original, hubspot_valor_normalizado_mensal, hubspot_valor_confidence, hubspot_valor_warning, hubspot_billing_frequency (best-effort/nullable) sem remover nenhuma chave atual (valor_contratado continua sendo o valor operacional).
    - pendencia_counts passa a ter as 8 chaves (5 atuais + sem_contato/valor_revisar/possivel_duplicidade), todas somando as empresas certas.
    - A whitelist do filtro pendencia aceita as 3 novas chaves (filtro por card funciona).
    - pendencias_detalhes é SEMPRE um array de strings por chave (mesmo padrão de dados_close_incompletos/servico_nao_reconhecido — o componente PendenciaBadges faz detalhes[chave].join(', ')): valor_revisar → lista de nomes de serviço dos contratos afetados; possivel_duplicidade → EXATAMENTE [$nomeCandidata] (array de 1 elemento com o NOME de exibição real da empresa candidata).
  </behavior>
  <action>
    Adicione asserções à suite Phase114ComercialListagemEnrichmentTest para: (a) presença das 5 chaves de company novas numa empresa origem HubSpot com dados preenchidos; (b) presença das 5 chaves hubspot_* em cada contrato do payload; (c) pendencia_counts contém as 8 chaves e conta corretamente valor_revisar; (d) whitelist — GET com ?pendencia=valor_revisar filtra a lista; (e) para uma empresa com match fraco (candidata real existente), is_array($row['pendencias_detalhes']['possivel_duplicidade']) é true E o elemento é o NOME de exibição da candidata (não o id nem nome_normalizado). Depois, em ComercialController::listagem: adicione as 5 chaves de company ao array de retorno do map (~307-343) lendo direto das colunas ($c->nome_contato etc.); adicione as 5 chaves hubspot_* dentro do map de contratos_servico (~330-340) lendo de $ct->hubspot_valor_original etc. (cast decimal já devolve string/num — normalize valor_original/normalizado_mensal para float quando não-null, espelhando o padrão de valor_contratado); adicione 'sem_contato', 'valor_revisar', 'possivel_duplicidade' com valor 0 ao array $pendenciaCounts (~244-249); adicione as 3 chaves à whitelist in_array do filtro pendencia (~194-196). Estenda o bloco $detalhes (~286-305) para popular valor_revisar como ARRAY de nomes dos serviços dos contratos com low/warning, e possivel_duplicidade como ARRAY de UM elemento contendo o NOME REAL de exibição da empresa candidata — resolva o candidate_company_id do warning (hubspot_snapshot['warnings'] ou payload do evento) via Company::find($candidateId)?->name (lookup barato, mesmo racional de baixo volume do HubspotCompanyMatcher), com fallback para nome_normalizado do warning SOMENTE se a candidata não existir mais. Nunca grave o id cru nem string sem acento/case como detalhe exibido. IMPORTANTE: todo valor de $detalhes[chave] deve ser array de strings (o componente PendenciaBadges reusado no 114-02 chama .join(', ') sempre que o array não é vazio — um valor escalar quebraria o render). Best-effort: campos ausentes viram null nas colunas, nunca quebrar empresa legada. Comentários pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase114ComercialListagemEnrichmentTest</automated>
  </verify>
  <acceptance_criteria>
    - payload da listagem inclui nome_contato/cargo_contato/hubspot_observacao/hubspot_deal_id/hubspot_company_id por empresa origem HubSpot
    - cada contrato do payload inclui as 5 chaves hubspot_* (nullable)
    - pendencia_counts tem 8 chaves; ?pendencia=valor_revisar filtra corretamente
    - pendencias_detalhes traz array de nomes de serviço para valor_revisar
    - is_array($row['pendencias_detalhes']['possivel_duplicidade']) é true e contém o NOME real da empresa candidata (resolvido via Company::find(...)->name; fallback nome_normalizado só se sumiu)
  </acceptance_criteria>
  <done>Payload enriquecido + counts/whitelist com 8 chaves + detalhes; suite Phase114 verde.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de não-regressão — Phase37ComercialListagemTest verde sem alterar asserções</name>
  <files>app/Http/Controllers/ComercialController.php</files>
  <read_first>
    - tests/Feature/Phase37ComercialListagemTest.php (as 5 pendências atuais + contrato de payload — NÃO editar asserções)
  </read_first>
  <action>
    Rode a suite Phase37 SEM modificar suas asserções. Se algo quebrar, o defeito está nas Tasks 1–2 (as mudanças deveriam ser puramente aditivas): corrija o controller, nunca o teste Phase37. Confirme que as 5 pendências atuais, os 5 contadores originais e as chaves de payload preexistentes permanecem idênticos. Este gate materializa a INVARIANTE DE NÃO-REGRESSÃO do CONTEXT.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase37ComercialListagemTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Phase37ComercialListagemTest` sai com 0 falhas
    - nenhuma asserção do Phase37 foi alterada (git diff limpo nesse arquivo)
  </acceptance_criteria>
  <done>Phase37ComercialListagemTest verde sem edição; invariante preservada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → /comercial/empresas/listagem | payload novo exposto a usuários com comercial.cadastrar_empresa ou admin (guarda já existente) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01-01 | Information Disclosure | payload com hubspot_observacao/IDs | accept | Rota já é admin/comercial-only via abort_unless; dados internos, sem PII de terceiros além do já exposto (email/telefone) |
| T-114-01-02 | Tampering | filtro pendencia (query string) | mitigate | whitelist in_array mantida — as 3 novas chaves entram na whitelist, valores fora dela caem para null |
</threat_model>

<verification>
- `php artisan test --filter=Phase114ComercialListagemEnrichmentTest` verde
- `php artisan test --filter=Phase37ComercialListagemTest` verde (0 falhas, sem editar o teste)
- Empresa legada não-HubSpot continua com pendencias_comerciais === []
</verification>

<success_criteria>
- HUB-UI-01: campos de contato/IDs + bloco de valor no payload da listagem
- HUB-UI-02: 3 pendências novas só para origem HubSpot, somadas em counts + whitelist
- Invariante de não-regressão preservada (Phase37 verde)
</success_criteria>

<output>
Create `.planning/phases/114-ui-comercial-campos-pend-ncias-novas-comando-de-replay-v20-0/114-01-SUMMARY.md` when done
</output>
