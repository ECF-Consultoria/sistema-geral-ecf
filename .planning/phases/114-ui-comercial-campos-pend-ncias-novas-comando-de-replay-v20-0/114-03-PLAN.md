---
phase: 114-ui-comercial-campos-pend-ncias-novas-comando-de-replay-v20-0
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Console/Commands/ReprocessHubspotEvent.php
  - app/Http/Controllers/Api/HubspotWebhookController.php
  - tests/Feature/Phase114HubspotReplayTest.php
autonomous: true
requirements: [HUB-REPLAY-01]

must_haves:
  truths:
    - "php artisan hubspot:reprocess-event {id} reprocessa um HubspotEvento e cria o contrato que faltava depois que o admin cadastra o mapping ausente"
    - "O replay é idempotente: rodar 2× não cria company nova (match forte por hubspot_company_id) nem duplica contrato (guard hubspot_line_item_id)"
    - "O comando loga resumo estruturado (evento id, deal id, company id, contratos criados/ignorados, warnings) no canal ecf-webhooks, sem token"
    - "Os testes usam Http::fake sempre — nenhuma chamada real ao HubSpot"
  artifacts:
    - path: "app/Console/Commands/ReprocessHubspotEvent.php"
      provides: "comando Artisan hubspot:reprocess-event {id}"
      contains: "hubspot:reprocess-event"
    - path: "app/Http/Controllers/Api/HubspotWebhookController.php"
      provides: "método público reusável reprocessarEvento() que o comando invoca"
      contains: "public function reprocessarEvento"
    - path: "tests/Feature/Phase114HubspotReplayTest.php"
      provides: "prova do efeito prático (line item sem mapping → mapping → replay cria) + idempotência"
  key_links:
    - from: "ReprocessHubspotEvent::handle"
      to: "HubspotWebhookController::reprocessarEvento"
      via: "resolução via container"
      pattern: "reprocessarEvento"
    - from: "reprocessarEvento"
      to: "HubspotCompanyMatcher + persistirContratos guard"
      via: "reuso do fluxo criarEmpresa (dedup + guard de contrato)"
      pattern: "criarEmpresa"
---

<objective>
Criar o comando de replay `php artisan hubspot:reprocess-event {id}` que reprocessa um `HubspotEvento` já recebido — recriando/atualizando contratos que ficaram faltando (tipicamente um line item que estava sem mapping e o admin acabou de cadastrar) — reusando a lógica de handoff/dedup já existente no webhook, de forma idempotente.

Purpose: HUB-REPLAY-01. Fecha o loop operacional: quando um deal cai com serviço não reconhecido, o admin cadastra o `HubspotLineItemMapping` e roda o replay para materializar o contrato, sem esperar um novo webhook.
Output: comando Artisan (naming verbo-substantivo, classe `ReprocessHubspotEvent`, signature `hubspot:reprocess-event {id}`); método público reusável `HubspotWebhookController::reprocessarEvento()` que executa fetch + criarEmpresa sem o filtro de dealstage e sem o early-return de idempotência de ingestão; suite de teste com `Http::fake` provando o efeito prático + idempotência.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-ui-comercial-campos-pend-ncias-novas-comando-de-replay-v20-0/114-CONTEXT.md

<interfaces>
<!-- Reuso direto — não reimplementar dedup/handoff. -->
HubspotWebhookController (private hoje):
  - criarEmpresa(array $deal, ?array $hubCompany, ?array $contatoPrincipal, array $contatos,
      ?string $companyId, array $lineItems, ?HubspotEvento $evento): Company
    → já resolve dedup via HubspotCompanyMatcher (match forte por hubspot_company_id/cnpj →
      enriquece, NÃO cria company) e persiste contratos via persistirContratos()
      (guard por hubspot_line_item_id → NÃO duplica contrato). Idempotência vem daqui.
  - o bloco de fetch dentro de processar() (linhas ~165-224) mostra o padrão:
    fetchDeal + fetchAssociatedCompanyId + fetchCompany + fetchAssociatedContactIds +
    fetchContacts + HubspotContactSelector::selecionar + fetchDealLineItems.

HubspotApiClient (app/Services/HubspotApiClient.php) — métodos públicos:
  fetchDeal, fetchAssociatedCompanyId, fetchCompany, fetchAssociatedContactIds,
  fetchContacts, fetchDealLineItems (todos usam Http:: — testáveis com Http::fake).

HubspotEvento: object_id (deal id), payload (array), company_id_criada.

Config: config('services.hubspot.props.deal'|'company'|'contact').

Comando de referência de estilo (signature/handle/Log): app/Console/Commands/NpsBackfillAssignmentsConsolidado.php
Log estruturado: Log::channel('ecf-webhooks')->info('[HubSpot ...]', [...]) — NUNCA logar token/secret.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Método público reprocessarEvento() no HubspotWebhookController (reuso do fetch + criarEmpresa)</name>
  <files>app/Http/Controllers/Api/HubspotWebhookController.php, tests/Feature/Phase114HubspotReplayTest.php</files>
  <read_first>
    - app/Http/Controllers/Api/HubspotWebhookController.php (processar() ~130-258: bloco de fetch + criarEmpresa; note o filtro dealstage ~140-148 e o early-return jaProcessado ~150-163 que o replay NÃO deve aplicar)
    - app/Services/HubspotApiClient.php (assinaturas dos fetch* — ~50-407)
    - .planning/phases/114-ui-comercial-campos-pend-ncias-novas-comando-de-replay-v20-0/114-CONTEXT.md (seção "Comando de replay (HUB-REPLAY-01)")
  </read_first>
  <behavior>
    - reprocessarEvento(HubspotEvento $evento, HubspotApiClient $api): array busca deal/company/contatos/line_items (mesmo padrão do bloco de fetch de processar()) e chama criarEmpresa(), sem aplicar o filtro de dealstage nem o early-return jaProcessado (essas são regras de ingestão de webhook, não de replay).
    - Idempotência herdada: com uma empresa já existente por hubspot_company_id, criarEmpresa cai no match forte (enriquece, não cria company); contrato já existente por hubspot_line_item_id é pulado pelo guard de persistirContratos. Segundo run não duplica nada.
    - Retorna um resumo array com: evento_id, deal_id, company_id, contratos_criados (contagem), contratos_ignorados (contagem), warnings (lista) — medindo criados/ignorados via ContratoServico contados por company antes/depois (não altere a assinatura de persistirContratos).
    - Loga o resumo em Log::channel('ecf-webhooks') com tag [HubSpot Replay]; token/secret NUNCA aparece no log.
    - Atualiza o evento (status/company_id_criada/processado_em) coerente com o resultado, sem apagar histórico do payload.
  </behavior>
  <action>
    Escreva primeiro os testes (RED) — ver Task 2 (a suite é a mesma; se preferir, crie a suite na Task 2 e itere aqui). Extraia o bloco de fetch de processar() (deal, companyId, hubCompany, contatos batch + HubspotContactSelector, lineItems) reaproveitando exatamente as mesmas chamadas e config props. Torne reprocessarEvento público: ele NÃO chama o filtro dealstage nem a guarda jaProcessado; captura a contagem de ContratoServico da company (quando já houver company_id_criada) antes de chamar criarEmpresa e depois, para derivar contratos_criados; envolve em try/catch \Throwable gravando status='erro'+erro_msg no evento em falha (espelhando processar). Ao final, atualiza o evento (status='processado', company_id_criada=$company->id, processado_em=now()) e loga o resumo estruturado no canal ecf-webhooks com [HubSpot Replay]. Opcional (sem obrigatoriedade): processar() pode delegar ao novo método, mas só se a regressão Hubspot permanecer verde — priorize regressão zero sobre DRY. Comentários pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase114HubspotReplayTest</automated>
  </verify>
  <acceptance_criteria>
    - existe `public function reprocessarEvento(HubspotEvento $evento, HubspotApiClient $api): array` no controller
    - o método reusa criarEmpresa (dedup + guard) e NÃO reaplica filtro dealstage / early-return jaProcessado
    - resumo retornado inclui company_id e contratos_criados
  </acceptance_criteria>
  <done>reprocessarEvento público, reusando o handoff/dedup; testado pela suite Phase114HubspotReplayTest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comando hubspot:reprocess-event + suite Phase114HubspotReplayTest (efeito prático + idempotência, Http::fake)</name>
  <files>app/Console/Commands/ReprocessHubspotEvent.php, tests/Feature/Phase114HubspotReplayTest.php</files>
  <read_first>
    - app/Console/Commands/NpsBackfillAssignmentsConsolidado.php (padrão de signature/description/handle/Log em pt-BR)
    - app/Http/Controllers/Api/HubspotWebhookController.php (reprocessarEvento — método da Task 1)
    - app/Models/HubspotLineItemMapping.php (paraNome — para o teste cadastrar o mapping ausente)
    - tests/Feature/Phase113HubspotDedupTest.php (padrão de Http::fake + montagem de payload de deal/company/contact/line items para o webhook)
  </read_first>
  <behavior>
    - Comando classe ReprocessHubspotEvent, signature 'hubspot:reprocess-event {id : ID do HubspotEvento}', description pt-BR. handle() carrega o HubspotEvento pelo id (erro amigável + exit FAILURE se não existir), resolve HubspotApiClient do container, chama controller->reprocessarEvento(), imprime o resumo (evento/deal/company/criados/ignorados/warnings) e retorna SUCCESS.
    - Teste efeito prático: monta um evento cujo line item ficou sem mapping (empresa criada sem esse contrato) via Http::fake; assere que o contrato do serviço NÃO existe; cadastra HubspotLineItemMapping para o nome do line item; roda `artisan('hubspot:reprocess-event', ['id' => $evento->id])`; assere que o ContratoServico do serviço passou a existir (efeito prático — a pendência valor/serviço some).
    - Teste idempotência: com Http::fake estável, roda o comando 2×; assere Company::count() estável e ContratoServico::count() estável entre o 1º e o 2º run (nem company nem contrato duplicados).
    - Nenhuma chamada real ao HubSpot: Http::fake em todos os cenários; assere Http::assertSentCount ou equivalente / ausência de rede.
  </behavior>
  <action>
    Escreva a suite Phase114HubspotReplayTest primeiro (RED): reuse o padrão de Http::fake do Phase113HubspotDedupTest para simular fetchDeal/fetchCompany/fetchAssociatedContactIds/fetchContacts/fetchDealLineItems. Cenário 1 (efeito prático): crie um HubspotEvento de deal fechado com um line item "Serviço X" sem HubspotLineItemMapping — pode originar a empresa via webhook real (POST assinado como no Phase113) OU criar a Company + evento diretamente com hubspot_company_id setado; garanta que a empresa existe e o contrato de "Serviço X" NÃO existe; crie o Servico "Serviço X" ativo + HubspotLineItemMapping::paraNome correspondente; rode o comando; assere que o ContratoServico foi criado com o servico_id certo. Cenário 2 (idempotência): rode o comando novamente e assere contagens estáveis. Depois implemente o comando ReprocessHubspotEvent em app/Console/Commands (Laravel 12 auto-registra) delegando a HubspotWebhookController::reprocessarEvento. Garanta que o token nunca é impresso/logado. Comentários pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase114HubspotReplayTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan hubspot:reprocess-event {id}` cria o contrato faltante depois do mapping cadastrado (ContratoServico passa a existir)
    - 2º run mantém Company::count() e ContratoServico::count() (idempotente)
    - todos os cenários usam Http::fake; nenhum HubSpot real
  </acceptance_criteria>
  <done>Comando funcional + suite Phase114HubspotReplayTest verde provando efeito prático e idempotência.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de regressão do webhook HubSpot</name>
  <files>app/Http/Controllers/Api/HubspotWebhookController.php</files>
  <read_first>
    - tests/Feature/Phase113HubspotDedupTest.php e demais suites Hubspot (Phase34/35/112/113)
  </read_first>
  <action>
    Rode a bateria completa de testes HubSpot para garantir que a extração de reprocessarEvento + a tornar o método público não regrediram o fluxo de ingestão do webhook. Se algo quebrar, corrija o controller (a mudança deve ser aditiva/extração pura), nunca relaxe asserções existentes.
  </action>
  <verify>
    <automated>php artisan test --filter=Hubspot</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Hubspot` sai com 0 falhas (mantém a baseline 84/84 + os novos do replay)
  </acceptance_criteria>
  <done>Suite Hubspot completa verde; ingestão do webhook intacta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot API → comando de replay | refetch de deal/company/contacts/line_items (dados externos) |
| CLI admin → reprocessamento | comando roda no servidor (Artisan), reprocessa evento por id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-03-01 | Information Disclosure | log do replay | mitigate | logar só ids/contagens/warnings no canal ecf-webhooks; token/secret NUNCA logado (padrão já vigente no controller) |
| T-114-03-02 | Tampering | duplicação de company/contrato no re-run | mitigate | idempotência via HubspotCompanyMatcher (match forte) + guard hubspot_line_item_id em persistirContratos; teste de idempotência trava contagens |
| T-114-03-03 | Repudiation | quem/quando reprocessou | mitigate | evento registra processado_em/status; log estruturado com evento_id/deal_id/company_id |
| T-114-03-SC | Tampering | dependências (nenhum install novo) | accept | fase não instala pacotes npm/pip/cargo; sem superfície de supply-chain nova |
</threat_model>

<verification>
- `php artisan test --filter=Phase114HubspotReplayTest` verde (efeito prático + idempotência)
- `php artisan test --filter=Hubspot` verde (regressão zero na ingestão)
- Nenhum teste chama HubSpot real (Http::fake em todos)
</verification>

<success_criteria>
- HUB-REPLAY-01: comando idempotente que recria contrato faltante pós-mapping, reusando dedup/handoff, com log estruturado sem token
</success_criteria>

<output>
Create `.planning/phases/114-ui-comercial-campos-pend-ncias-novas-comando-de-replay-v20-0/114-03-SUMMARY.md` when done
</output>
