---
phase: 138-rea-comercial-conectada-etapa-v23-0
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-BASELINE-TESTES.md
autonomous: true
requirements: []
# Vazio de propósito: este plano não entrega nenhum COMERC-*. Ele é o gate de
# processo do CLAUDE.md (baseline antes de migration em tabela com dado de
# produção) que o plano 138-03 precisa ter cumprido antes de criar as 3 colunas.
must_haves:
  truths:
    - "Existe um registro escrito do estado da suíte ANTES de qualquer migration desta fase"
    - "As ~10 falhas pré-existentes de Polos estão nomeadas no registro, para não serem confundidas com regressão da 138"
    - "As suítes que tocam `companies`, o módulo Contrato e a máquina de estados da 137 estão medidas com contagem literal"
    - "Nenhum arquivo de `app/`, `database/` ou `resources/` é modificado por este plano"
    - "Decisões do CONTEXT implementadas por este plano: D-09 (a migration aditiva de 3 colunas em `companies` é exatamente o que dispara o gate do CLAUDE.md e o que esta baseline protege), D-14 (a baseline não carimba etapa em ninguém — ela só mede; legado com `etapa` NULL continua NULL)"
  artifacts:
    - path: ".planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-BASELINE-TESTES.md"
      provides: "Baseline verde registrada antes da migration (exigência do CLAUDE.md § GSD obrigatório)"
      contains: "PolosControllerTest"
  key_links:
    - from: "138-BASELINE-TESTES.md"
      to: "138-03-PLAN.md (migration das 3 colunas)"
      via: "regra de comparação de regressão — todo plano posterior compara contra estes números, nunca contra zero"
      pattern: "Regra de compara"
---

<objective>
Registrar por escrito o estado da suíte de testes ANTES de a Fase 138 criar qualquer coluna
em `companies`.

Propósito: o `CLAUDE.md` classifica "migration que altere tabela existente com dado em
produção" como fase GSD obrigatória, exigindo baseline de testes antes e `VERIFICATION.md`
ao final. A D-09 cria três colunas aditivas (`hubspot_owner_id`, `hubspot_owner_nome`,
`data_venda`) em `companies`, que tem ~500 registros reais em produção. Sem esta baseline, a
primeira suíte vermelha da fase vira caça a fantasma — exatamente o que aconteceria com as
~10 falhas antigas de Polos, que não são regressão de ninguém.

Este plano NÃO toca código de produção. Nenhum arquivo em `app/`, `database/` ou
`resources/` é modificado aqui.

Saída: `138-BASELINE-TESTES.md` com os números medidos e a regra de comparação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-VALIDATION.md
@.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-BASELINE-TESTES.md
@.planning/learnings/painel-polos-status-e-meta.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medir as suítes que a Fase 138 encosta</name>
  <files>.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-BASELINE-TESTES.md</files>
  <read_first>
    - `.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-VALIDATION.md` § "Baseline exigida pelo `CLAUDE.md`" — o comando literal e por que são exatamente estas suítes
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-BASELINE-TESTES.md` — o formato a reproduzir e o achado do timeout de rede da suíte Feature completa
    - `.planning/learnings/painel-polos-status-e-meta.md` §2 — as ~10 falhas antigas de Polos e a causa raiz delas
  </read_first>
  <action>
    Rode cada comando abaixo separadamente e capture a linha literal de resumo de cada um
    (`Tests: N, Assertions: N, Failures: N`), incluindo o exit code.

    1. A baseline específica da fase, num único processo:
       `vendor/bin/phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php tests/Feature/Phase131/ContratoAdminPermissaoTest.php tests/Unit/Phase137 tests/Feature/Phase137 --colors=never`
    2. `vendor/bin/phpunit tests/Feature/Phase38/PolosControllerTest.php --colors=never`
    3. `vendor/bin/phpunit tests/Feature/Polos/PolosFaturamentoSnapshotTest.php --colors=never`
    4. `vendor/bin/phpunit --testsuite=Unit --colors=never`

    Use sempre o binário `C:\xampp\php\php.exe` (o `php` não está no PATH do Bash) e sempre
    por diretório/arquivo. **Não** rode `php artisan test` nem `--testsuite=Feature` inteiro:
    o `137-BASELINE-TESTES.md` mediu que os dois travam numa cascata de timeout de rede
    (~300 s) e morrem sem imprimir o resumo, queimando o orçamento de tempo da task sem
    produzir número nenhum.

    Antes de rodar, confirme que `node_modules/` e `public/build/manifest.json` existem neste
    worktree — o `138-VALIDATION.md` mediu que sim, e é isso que evita o erro
    `Vite manifest not found` nas Feature tests que renderizam Inertia. Se `public/build/`
    tiver sumido, rode `npm run build` (com timeout de 600000 ms) antes de medir e registre
    isso no arquivo. Não rode `npm install` sem necessidade e não deixe `package.json` nem
    `package-lock.json` modificados.

    Se `tests/Feature/Phase37CompaniesPerformanceFilterTest.php` não devolver 24 testes /
    44 assertions verdes (o número que a Fase 137 registrou), **não conserte nada** — registre
    a divergência. Ela vira parte da baseline, não tarefa desta fase.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase131/ContratoAdminPermissaoTest.php tests/Unit/Phase137 --colors=never</automated>
  </verify>
  <acceptance_criteria>
    - O comando de baseline específica foi executado e o resumo literal (`Tests:`/`Assertions:`/`Failures:`) foi capturado com o exit code
    - `tests/Feature/Phase131/ContratoAdminPermissaoTest.php` sai com exit code 0 (é a regressão de D-15 que a fase inteira precisa manter verde)
    - `tests/Unit/Phase137` e `tests/Feature/Phase137` saem com exit code 0
    - Existe `public/build/manifest.json` no worktree
    - Nenhuma linha da saída das Feature tests contém `Vite manifest not found`
    - `git status --porcelain package.json package-lock.json` não devolve nenhuma linha
    - `git status --porcelain app/ database/ resources/` não devolve nenhuma linha
  </acceptance_criteria>
  <done>Todos os números da baseline estão medidos, com comando e exit code, prontos para serem escritos.</done>
</task>

<task type="auto">
  <name>Task 2: Escrever 138-BASELINE-TESTES.md com a regra de comparação</name>
  <files>.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-BASELINE-TESTES.md</files>
  <read_first>
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-BASELINE-TESTES.md` — o molde de estrutura: frontmatter com `sha_head`, seção por comando, seção "Falhas pré-existentes", tabela "Regra de comparação"
    - `.planning/learnings/painel-polos-status-e-meta.md` §2 — texto que explica por que as falhas de Polos não são regressão
    - `CLAUDE.md` § "GSD obrigatório" — por que a baseline precisa ser anterior à migration
  </read_first>
  <action>
    Crie `138-BASELINE-TESTES.md` no diretório da fase, com frontmatter contendo `phase: 138`,
    `plano: 138-01`, `registrado_em` (data de hoje) e `sha_head` (saída de `git rev-parse HEAD`).

    Corpo, nesta ordem:

    1. Um parágrafo dizendo que a baseline foi coletada **antes** de a migration do plano
       138-03 existir, e nomeando as três colunas que ela vai criar
       (`hubspot_owner_id`, `hubspot_owner_nome`, `data_venda`) e a tabela (`companies`,
       ~500 registros em produção). Registre que este plano não tocou `app/`, `database/`
       nem `resources/`.
    2. Uma seção por comando executado na Task 1, com o comando literal e o resumo literal.
    3. Uma seção "Falhas pré-existentes de Polos — NÃO são regressão da 138" citando
       nominalmente `tests/Feature/Phase38/PolosControllerTest.php` e
       `tests/Feature/Polos/PolosFaturamentoSnapshotTest.php`, com a contagem observada de
       cada uma e o ponteiro para `.planning/learnings/painel-polos-status-e-meta.md` §2.
    4. Uma seção "Aviso de execução" repetindo, com o comando, que `php artisan test` e
       `--testsuite=Feature` não terminam neste ambiente, e que todo plano da fase roda por
       diretório/arquivo com `C:\xampp\php\php.exe vendor/bin/phpunit`.
    5. Uma tabela final "Regra de comparação" com uma linha por suíte medida e o resultado
       baseline. Escreva na letra: comparação de regressão nos planos 138-02 em diante é
       sempre contra estes números, nunca contra zero.

    Se qualquer falha apareceu fora dos dois arquivos de Polos, liste-a nominalmente na seção
    3 com o nome do teste — ela passa a fazer parte da baseline e **não** vira tarefa de
    correção desta fase.

    O arquivo é o "antes": não editá-lo depois de criado.
  </action>
  <verify>
    <automated>grep -c "PolosControllerTest\|PolosFaturamentoSnapshotTest\|Regra de compara" .planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-BASELINE-TESTES.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-BASELINE-TESTES.md` existe
    - O arquivo contém as strings `hubspot_owner_id`, `hubspot_owner_nome` e `data_venda`
    - O arquivo cita nominalmente `tests/Feature/Phase38/PolosControllerTest.php` e `tests/Feature/Polos/PolosFaturamentoSnapshotTest.php`, cada um com contagem de falhas observada
    - O arquivo contém a string `Regra de compara` e uma tabela com uma linha por suíte medida
    - O frontmatter contém `sha_head` com o SHA real de `git rev-parse HEAD`
    - `ls database/migrations | grep -c "hubspot_owner"` devolve `0` (a baseline é anterior à migration — o plano 138-03 ainda não rodou)
    - `git status --porcelain app/ database/ resources/` não devolve nenhuma linha
  </acceptance_criteria>
  <done>Baseline escrita, datada, anterior à migration, com as falhas pré-existentes nomeadas e a regra de comparação explícita.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree ↔ árvore compartilhada | mais de uma sessão de Claude Code edita este repositório em máquinas diferentes; um commit largo arrasta trabalho alheio |
| suíte de testes ↔ rede externa | a suíte Feature completa dispara chamadas HTTP reais não mockadas e trava o processo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-01 | Tampering | commit da baseline | mitigate | Commit exclusivamente por caminho (`git commit -- .planning/phases/138-.../138-BASELINE-TESTES.md`), nunca `git add -A`/`git add .` — a árvore é compartilhada (CLAUDE.md; `feedback_commit_por_caminho_arrasta_outra_sessao.md`). Critério de aceite exige `git status --porcelain app/ database/ resources/` vazio |
| T-138-02 | Denial of Service | `php artisan test` / `--testsuite=Feature` | mitigate | Proibido nos dois `<action>`: trava ~300 s numa cascata de timeout de rede e morre sem resumo (`137-BASELINE-TESTES.md`). Todo comando roda por diretório/arquivo |
| T-138-SC | Tampering | instalação de pacote | accept | Esta fase não instala nenhum pacote Composer/npm novo (`138-RESEARCH.md` § "Package Legitimacy Audit"). `npm install` só é permitido para restaurar o `package-lock.json` já versionado, e `package.json`/`package-lock.json` inalterados é critério de aceite |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Feature/Phase131/ContratoAdminPermissaoTest.php tests/Unit/Phase137 tests/Feature/Phase137` sai com exit code 0
- `138-BASELINE-TESTES.md` existe, nomeia as três colunas futuras e as duas suítes de Polos
- `ls database/migrations | grep -c "hubspot_owner"` devolve `0`
- `git status --porcelain app/ database/ resources/` vazio
</verification>

<success_criteria>
1. Baseline registrada por escrito ANTES de qualquer migration da fase, cumprindo o gate do `CLAUDE.md`
2. As ~10 falhas antigas de Polos estão nomeadas e não podem ser confundidas com regressão da 138
3. Nenhuma linha de código de produção foi tocada
</success_criteria>

<output>
Create `.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-01-SUMMARY.md` when done
</output>
