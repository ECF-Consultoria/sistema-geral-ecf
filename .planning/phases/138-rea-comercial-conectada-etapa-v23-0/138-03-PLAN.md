---
phase: 138-rea-comercial-conectada-etapa-v23-0
plan: 03
type: execute
wave: 1
depends_on: [138-01, 138-02]
files_modified:
  - database/migrations/2026_09_02_120000_add_hubspot_owner_data_venda_to_companies_table.php
  - app/Models/Company.php
  - config/services.php
  - app/Services/HubspotApiClient.php
  - app/Services/Hubspot/HubspotOwnerResolver.php
  - .env.example
  - tests/Feature/Phase138/CompaniesColunasOwnerVendaTest.php
  - tests/Unit/Phase138/HubspotOwnerResolverTest.php
autonomous: true
requirements: [COMERC-02]
must_haves:
  truths:
    - "`companies` tem as colunas `hubspot_owner_id`, `hubspot_owner_nome` e `data_venda`, todas nullable e sem default"
    - "A property de owner do deal é buscada junto com as demais numa única chamada, por chave de config, sem segundo caminho de fetch"
    - "`HubspotApiClient::fetchOwner()` devolve `null` em qualquer erro HTTP e nunca lança — owner arquivado/removido não derruba fluxo nenhum"
    - "O nome do responsável comercial é resolvido com cache, sem uma chamada HTTP por empresa listada"
    - "Nenhuma coluna existente de `companies` muda de tipo, default ou significado"
    - "Decisões do CONTEXT implementadas por este plano: D-08 (responsável comercial vem do HubSpot — property no deal + `GET /crm/v3/owners/{id}` com cache), D-09 (grava em colunas aditivas nullable de `companies`, nunca extraindo JSON dentro de WHERE/ORDER), D-12 ('setor ou segmento' é o setor ECF de `servicos.setor`; o `industry` do HubSpot permanece dentro de `hubspot_snapshot.company` e não vira coluna nesta fase)"
  artifacts:
    - path: "database/migrations/2026_09_02_120000_add_hubspot_owner_data_venda_to_companies_table.php"
      provides: "As 3 colunas aditivas nullable da D-09 em companies"
      contains: "hasColumn"
    - path: "app/Services/HubspotApiClient.php"
      provides: "fetchOwner(string $ownerId): ?array — GET resiliente de /crm/v3/owners/{id}"
      contains: "fetchOwner"
    - path: "app/Services/Hubspot/HubspotOwnerResolver.php"
      provides: "Resolução cacheada do nome do responsável comercial a partir do owner id"
      contains: "Cache::remember"
  key_links:
    - from: "config/services.php"
      to: "app/Http/Controllers/Api/HubspotWebhookController.php"
      via: "props.deal.owner_id entra no array de properties do fetchDeal já existente"
      pattern: "HUBSPOT_PROP_DEAL_OWNER_ID"
    - from: "app/Services/Hubspot/HubspotOwnerResolver.php"
      to: "app/Services/HubspotApiClient.php"
      via: "resolverNome() chama fetchOwner() dentro do Cache::remember"
      pattern: "fetchOwner"
---

<objective>
Criar as três colunas da D-09 em `companies` e a fonte do campo "responsável comercial" do §2,
que hoje **não existe** no sistema.

Propósito: o `138-CONTEXT.md` mediu em 2026-09-02 que a string `owner` não aparece em
`config/services.php`, `HubspotApiClient` nem `HubspotWebhookController` — o campo do §2 nunca
teve fonte. `closedate` já é buscado hoje, mas cai só dentro de `hubspot_snapshot.deal` (JSON),
e a D-09 recusa depender disso: extrair JSON dentro de `WHERE`/`ORDER` no MariaDB é armadilha
que o SQLite dos testes não pega (`.planning/learnings/desempenho-bonificacao.md` §6), e a
Fase 143 vai querer a data da venda como evento datado.

**Gate do `CLAUDE.md` cumprido:** a baseline pré-migration foi registrada pelo plano 138-01.
Não rode a migration deste plano antes de `138-BASELINE-TESTES.md` existir.

Este plano cria a capacidade. Quem **persiste** o dado é o plano 138-04; quem **exibe** são os
planos 138-05 e 138-06.

Saída: migration aditiva, `$fillable`/`$casts` no `Company`, a chave de property no config,
`fetchOwner()` no client e o `HubspotOwnerResolver`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-CONTEXT.md
@.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-PATTERNS.md
@.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-HUBSPOT-MEDICOES.md
@.planning/learnings/desempenho-bonificacao.md
@app/Models/Company.php
@app/Services/HubspotApiClient.php
@config/services.php
</context>

<interfaces>
<!-- Contratos fixados aqui para os planos 138-04, 138-05 e 138-06 não divergirem. -->

Colunas novas em `companies` (todas nullable, sem default):
- `hubspot_owner_id`  — string(255). Id numérico do owner no HubSpot, como string.
- `hubspot_owner_nome` — string(255). Nome de exibição resolvido pelo `HubspotOwnerResolver`.
- `data_venda`        — date. Formato `'Y-m-d'`, o mesmo em que `closedate` chega hoje
  (medido em `HubspotDealHandoffService::parseDataHubspot()`).

Chave de config nova: `services.hubspot.props.deal.owner_id`
  → `env('HUBSPOT_PROP_DEAL_OWNER_ID', '<valor confirmado em 138-HUBSPOT-MEDICOES.md>')`

Assinaturas novas:
- `App\Services\HubspotApiClient::fetchOwner(string $ownerId): ?array`
- `App\Services\Hubspot\HubspotOwnerResolver::resolverNome(?string $ownerId): ?string`

`resolverNome(null)` devolve `null` sem tocar em cache nem em rede.
`hubspot_owner_nome === null` é estado NORMAL, não erro — empresa de cadastro manual nunca
tem deal HubSpot e por isso nunca tem owner (`138-RESEARCH.md`, Pitfall 2).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migration aditiva das 3 colunas e o Company model</name>
  <files>database/migrations/2026_09_02_120000_add_hubspot_owner_data_venda_to_companies_table.php, app/Models/Company.php, tests/Feature/Phase138/CompaniesColunasOwnerVendaTest.php</files>
  <read_first>
    - `database/migrations/2026_07_24_111001_add_hubspot_fields_to_companies_table.php` — o molde exato: um bloco `if (! Schema::hasColumn(...))` por coluna no `up()`, `foreach` com `hasColumn` no `down()`, nada além de `companies` tocado
    - `database/migrations/2026_09_01_110000_add_etapa_to_companies_table.php` — o docblock da Fase 137 que cita o anti-padrão do próprio repositório (`2026_05_25_100001_add_status_to_companies.php`, que mistura criação de coluna com rename no mesmo `up()`)
    - `app/Models/Company.php`, blocos `$fillable` (linhas ~170-222) e `$casts` (linhas ~224-245) — os comentários que já documentam por que cada grupo de colunas está lá
    - `.planning/learnings/desempenho-bonificacao.md` §6 — as armadilhas de MariaDB que o SQLite dos testes não pega
    - `.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-BASELINE-TESTES.md` — confirmar que a baseline existe antes de rodar a migration
  </read_first>
  <action>
    Crie a migration `database/migrations/2026_09_02_120000_add_hubspot_owner_data_venda_to_companies_table.php`
    com `Schema::table('companies', ...)` e exatamente três colunas, cada uma dentro do seu
    próprio `if (! Schema::hasColumn('companies', <col>))`:
    `hubspot_owner_id` como `string(255)->nullable()`, `hubspot_owner_nome` como
    `string(255)->nullable()`, e `data_venda` como `date()->nullable()`.

    Use `date`, não `dateTime`: `closedate` chega do HubSpot como string `'Y-m-d'` (medido no
    deal `64133858455` em 2026-08-20, registrado no comentário de `config/services.php`), e a
    Fase 143 vai ler isto como data de evento, não como carimbo de hora.

    Nenhuma coluna ganha `default()`, nenhum índice é criado, nenhuma outra coluna é tocada e
    nenhum backfill roda dentro desta migration — o retroativo é comando manual do plano
    138-04 (D-10). O `down()` percorre as três colunas num `foreach` com `Schema::hasColumn`
    antes de `dropColumn`, espelhando o molde da Fase 111.

    Em `app/Models/Company.php`, acrescente `hubspot_owner_id`, `hubspot_owner_nome` e
    `data_venda` ao `$fillable`, num bloco próprio com comentário pt-BR citando Fase 138 /
    COMERC-02 / D-09 e dizendo em uma linha que `hubspot_owner_*` é espelho do HubSpot
    (reescrito a cada processamento, como `hubspot_notas`/`hubspot_observacao`) e que
    `data_venda` é a data de fechamento do deal. Acrescente `'data_venda' => 'date'` ao
    `$casts`. `hubspot_owner_id` e `hubspot_owner_nome` são string e **não** ganham cast.

    Rode a migration com `C:\xampp\php\php.exe artisan migrate` e confira o resultado por
    reconsulta ao banco (`SHOW COLUMNS FROM companies LIKE 'hubspot_owner%'` e
    `LIKE 'data_venda'`), nunca só pela mensagem de sucesso do artisan — disciplina do
    `.planning/learnings/desempenho-bonificacao.md` §6.

    Crie `tests/Feature/Phase138/CompaniesColunasOwnerVendaTest.php` provando: as três colunas
    existem via `Schema::hasColumn`; uma `Company` criada por factory sem passar nenhuma delas
    nasce com as três `null`; `data_venda` atribuída como string `'2026-08-24'` volta como
    instância de data e formata `'Y-m-d'` igual; e que nenhuma delas é obrigatória (criar
    empresa sem elas não lança).
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase138/CompaniesColunasOwnerVendaTest.php --colors=never</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Phase138/CompaniesColunasOwnerVendaTest.php` sai com exit code 0
    - A migration contém exatamente 3 ocorrências de `Schema::hasColumn` no `up()` e nenhuma chamada a `default(`
    - `grep -c "dropColumn" database/migrations/2026_09_02_120000_add_hubspot_owner_data_venda_to_companies_table.php` devolve pelo menos 1 (o `down()` é reversível)
    - A migration não contém as strings `Company::` nem `DB::table` (nenhum backfill dentro da migration)
    - `grep -c "hubspot_owner_id\|hubspot_owner_nome\|data_venda" app/Models/Company.php` devolve pelo menos 4 (3 no `$fillable` + 1 no `$casts`)
    - A reconsulta ao banco (`SHOW COLUMNS FROM companies`) lista as três colunas como `YES` em `Null` e `NULL` em `Default`
    - `tests/Feature/Phase131/ContratoAdminPermissaoTest.php` e `tests/Unit/Phase137` continuam saindo com exit code 0 (regressão contra a baseline do 138-01)
  </acceptance_criteria>
  <done>As três colunas existem em `companies`, são nullable, são atribuíveis por Eloquent e nenhuma coluna existente mudou.</done>
</task>

<task type="auto">
  <name>Task 2: Property de owner no config, fetchOwner() no client e o HubspotOwnerResolver</name>
  <files>config/services.php, app/Services/HubspotApiClient.php, app/Services/Hubspot/HubspotOwnerResolver.php, tests/Unit/Phase138/HubspotOwnerResolverTest.php</files>
  <read_first>
    - `.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-HUBSPOT-MEDICOES.md` — o valor confirmado de `property_owner_nome_interno:` (é ele, não o default sugerido pela pesquisa, que entra no config)
    - `config/services.php`, bloco `hubspot.props.deal` (linhas ~134-182) — uma linha por property, `env()` com default = nome interno medido; e o comentário de topo que registra a regra "propriedade ausente na conta HubSpot = null no payload, nunca quebra o fluxo"
    - `app/Services/HubspotApiClient.php` — `fetchCompany()` (linhas ~94-102) para o shape do GET, e `fetchAssociatedCompanyId()` (linhas ~67-83) para o padrão resiliente que devolve `null` em vez de lançar. `self::BASE` e `$this->token` já existem na classe
    - `app/Services/MercadoLivreService.php` linhas 693-710 (`resolveAdvertiserId`) — o molde de `Cache::remember` com TTL longo e sentinela cacheável para o caso "não existe"
    - `app/Services/Digisac/NpsDigisacDispatchService.php` linhas 154-168 — o precedente de resolução por config no projeto
    - `app/Services/Hubspot/HubspotCompanyMatcher.php` ou `HubspotContactSelector.php` — o tamanho e a forma de classe pequena de um propósito só nesta pasta
  </read_first>
  <action>
    **config/services.php** — acrescente ao array `hubspot.props.deal` uma única chave nova,
    `'owner_id' => env('HUBSPOT_PROP_DEAL_OWNER_ID', '<valor confirmado>')`, usando o valor de
    `property_owner_nome_interno:` do `138-HUBSPOT-MEDICOES.md`. Comentário pt-BR acima dela
    citando Fase 138 / COMERC-02 / D-08, dizendo que é property PADRÃO do HubSpot (não
    customizada) e que mesmo assim foi medida, com o ponteiro para o arquivo de medições.
    Não mexa em nenhuma outra chave do bloco. Acrescente `HUBSPOT_PROP_DEAL_OWNER_ID` ao
    `.env.example` (comentado como opcional — o default cobre o caso comum).

    Nenhum segundo caminho de fetch é criado: `HubspotWebhookController::processar()` já faz
    `array_values($propsDeal)` dentro do `fetchDeal()`, então acrescentar a chave ao config
    basta para o valor cair em `hubspot_snapshot.deal` — é o padrão config-driven que o
    `138-RESEARCH.md` § "Don't Hand-Roll" manda reusar.

    **app/Services/HubspotApiClient.php** — acrescente `fetchOwner(string $ownerId): ?array`
    seguindo o padrão de `fetchAssociatedCompanyId()`, não o de `fetchCompany()`: faça
    `Http::withToken($this->token)->get(self::BASE . "/crm/v3/owners/{$ownerId}")`, e se
    `!$res->ok()` logue por `Log::channel('ecf-webhooks')->warning(...)` com **apenas**
    `owner_id` e `status` HTTP no contexto, e devolva `null`. Nunca `$res->throw()`, nunca
    logue o token nem a mensagem crua da exceção (disciplina T-111-03, já seguida em todo o
    arquivo). Em sucesso devolve `$res->json()`, cujo shape oficial traz `id`, `email`,
    `firstName`, `lastName`, `userId` e `archived`. Atualize o docblock de topo da classe
    acrescentando o método à lista numerada que já existe lá.

    **app/Services/Hubspot/HubspotOwnerResolver.php** — classe nova, pequena, um propósito só,
    na mesma pasta de `HubspotCompanyMatcher`/`HubspotContactSelector`. Construtor com
    promoted property recebendo `HubspotApiClient`. Método público
    `resolverNome(?string $ownerId): ?string`:
    - `$ownerId` nulo ou string vazia devolve `null` imediatamente, sem tocar em cache nem em
      rede;
    - caso contrário, `Cache::remember("hubspot_owner_nome_{$ownerId}", now()->addDays(7), ...)`
      chamando `fetchOwner()` e compondo o nome a partir de `firstName` e `lastName` (trim,
      juntando com espaço); se os dois vierem vazios, caia para `email`; se o payload for
      `null` ou não tiver nada disso, guarde a **sentinela string vazia** `''` no cache (nunca
      `null`, que o `Cache::remember` reconsultaria toda vez — é o mesmo motivo do `0` como
      sentinela em `MercadoLivreService::resolveAdvertiserId()`);
    - na volta, converta a sentinela `''` de novo para `null` antes de devolver ao chamador.
    TTL de 7 dias é deliberado: a ECF tem poucos vendedores (D-08) e o nome muda raramente.
    Comentários em pt-BR, com divisor `// ─── Seção ───` se a classe passar de um método.

    **tests/Unit/Phase138/HubspotOwnerResolverTest.php** — use `Http::fake()` com a rota
    `api.hubapi.com/crm/v3/owners/*` (o mesmo padrão de `tests/Feature/Phase34HubspotWebhookTest.php`;
    nenhum teste desta fase pode chamar a API real). Prove: nome composto de
    `firstName`+`lastName`; fallback para `email` quando os dois vierem vazios; `null` quando
    o id é `null`; `null` quando a API devolve 403 (o caso do escopo faltando, Pitfall 3) e
    404 (owner arquivado); e que uma segunda chamada com o mesmo id **não** dispara nova
    requisição HTTP (`Http::assertSentCount(1)`), provando o cache. Limpe o cache no `setUp`
    para os casos não vazarem entre si.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase138/HubspotOwnerResolverTest.php --colors=never</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Unit/Phase138/HubspotOwnerResolverTest.php` sai com exit code 0 e cobre os 6 casos nomeados no action (nome composto, fallback e-mail, id nulo, 403, 404, cache com `assertSentCount(1)`)
    - `grep -c "HUBSPOT_PROP_DEAL_OWNER_ID" config/services.php` devolve `1`
    - `grep -c "owner_id" config/services.php` devolve `1` (a chave entrou uma vez só, dentro de `props.deal`)
    - `grep -c "fetchOwner" app/Services/HubspotApiClient.php` devolve pelo menos 2 (assinatura + docblock de topo)
    - `grep -c "throw()" app/Services/HubspotApiClient.php` devolve o mesmo valor de antes desta task (o método novo não adicionou nenhum `$res->throw()`)
    - `app/Services/HubspotApiClient.php` não contém as strings `$this->token` dentro de nenhuma chamada a `Log::` (token nunca vai para log)
    - `app/Services/Hubspot/HubspotOwnerResolver.php` contém `Cache::remember` e trata `?string $ownerId` nulo antes de qualquer acesso a rede
    - Nenhum teste desta fase contém a string `->get('https://api.hubapi.com` sem um `Http::fake` no mesmo arquivo
    - `git diff --stat config/services.php` mostra alteração de no máximo 6 linhas (só a chave nova e seu comentário)
  </acceptance_criteria>
  <done>A property de owner entra no fetch já existente por config, o client sabe buscar o owner sem nunca lançar, e o nome é resolvido com cache de 7 dias.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API do HubSpot → `companies` | payload de terceiro passa a ser persistido em colunas novas de uma tabela com ~500 registros de produção |
| ECF Admin → api.hubapi.com | o `HUBSPOT_ACCESS_TOKEN` atravessa aqui em toda chamada de `fetchOwner()` |
| cache compartilhado (driver `database`) → payload de listagem | o nome resolvido do owner fica cacheado por 7 dias e é lido por toda listagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-138-06 | Information Disclosure | `HubspotApiClient::fetchOwner()` | mitigate | Log só com `owner_id` e `status` HTTP, nunca o token nem a mensagem crua da exceção — disciplina T-111-03 já seguida em todo o arquivo. Critério de aceite proíbe `$this->token` dentro de chamada a `Log::` |
| T-138-07 | Tampering | migration em `companies` | mitigate | Migration estritamente aditiva: 3 colunas nullable, sem `default()`, sem índice, sem backfill, com `Schema::hasColumn` defensivo e `down()` reversível. Nenhuma coluna existente é tocada. Baseline pré-migration registrada no plano 138-01 (gate do `CLAUDE.md`), e o resultado é conferido por reconsulta ao banco, nunca por stdout do artisan (`.planning/learnings/desempenho-bonificacao.md` §6) |
| T-138-08 | Denial of Service | `HubspotOwnerResolver` sem cache | mitigate | `Cache::remember` com TTL de 7 dias + sentinela `''` para o caso "não resolvido". Sem a sentinela, todo owner irresolúvel dispararia uma requisição HTTP por empresa em cada render de listagem — o mesmo motivo do sentinela `0` em `MercadoLivreService::resolveAdvertiserId()` |
| T-138-09 | Information Disclosure | dado do HubSpot persistido | accept | `hubspot_owner_id`/`hubspot_owner_nome` são nome e id de vendedor **da própria ECF**, não dado de cliente; já circulam internamente. `data_venda` é data de fechamento comercial. Nenhum PII de terceiro entra nas colunas novas. As duas listagens que os exibem já exigem permission (`admin.contratos` e `comercial.entrada`) |
| T-138-SC | Tampering | dependências | accept | Nenhum pacote Composer ou npm novo (`138-RESEARCH.md` § "Package Legitimacy Audit"). `composer.json` e `package.json` não mudam neste plano |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Feature/Phase138/CompaniesColunasOwnerVendaTest.php tests/Unit/Phase138 --colors=never` sai 0
- `vendor/bin/phpunit tests/Feature/Phase131/ContratoAdminPermissaoTest.php tests/Unit/Phase137 tests/Feature/Phase137 --colors=never` sai 0 (regressão contra a baseline do 138-01)
- `SHOW COLUMNS FROM companies` lista as três colunas novas como nullable sem default
- `git diff --stat` não mostra alteração em `composer.json`, `package.json` nem em nenhuma migration anterior
</verification>

<success_criteria>
1. As três colunas da D-09 existem, são nullable e não alteram nenhuma coluna existente
2. A property de owner entra no fetch de deal já existente por uma única chave de config, com o nome medido
3. O nome do responsável comercial é resolvido a partir do HubSpot com cache, e a ausência dele é um `null` normal, nunca um erro
</success_criteria>

<output>
Create `.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-03-SUMMARY.md` when done
</output>
