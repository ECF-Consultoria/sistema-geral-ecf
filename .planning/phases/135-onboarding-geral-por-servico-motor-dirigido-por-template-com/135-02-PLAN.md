---
phase: 135-onboarding-geral-por-servico-motor-dirigido-por-template-com
plan: 02
type: execute
wave: 2
depends_on: ["135-01"]
files_modified:
  - database/migrations/2026_08_11_120000_create_onboarding_templates_tables.php
  - database/migrations/2026_08_11_120100_create_onboardings_tables.php
  - app/Models/OnboardingTemplate.php
  - app/Models/TemplatePasso.php
  - app/Models/Onboarding.php
  - app/Models/OnboardingPasso.php
  - app/Models/OnboardingLink.php
  - tests/Feature/Phase135/OnboardingSchemaTest.php
autonomous: true
requirements: [SC-01, SC-05, SC-09, SC-10, SC-11, D-01, D-06, D-07, D-09, D-10, D-11, D-12, D-14, D-19]

must_haves:
  truths:
    - "O onboarding é ancorado em Company × Servico, com um onboarding por contrato (D-01/SC-01)"
    - "Só existe uma versão ativa de template por serviço, garantido pelo banco em SQLite E MariaDB (D-07/SC-09)"
    - "Um passo sabe declarar dono, setor, dependências, SLA, condição e fonte automática (D-14/D-12/D-09/SC-05)"
    - "O estado de um passo consegue representar 'aguardando coleta' e 'indeterminado' — nunca só feito/não-feito (D-11/SC-07)"
    - "Existe o timestamp de quando o passo ficou acionável, para o painel responder 'há quantos dias' (SC-11)"
    - "O token do cliente vive na empresa, não no onboarding (D-06/SC-10)"
    - "dono e auto_fonte são colunas independentes — um passo pode ter dono=cliente e ainda assim se resolver sozinho (D-19)"
  artifacts:
    - path: "database/migrations/2026_08_11_120000_create_onboarding_templates_tables.php"
      provides: "Tabelas onboarding_templates + template_passos com versionamento imutável"
      contains: "onboarding_templates"
    - path: "database/migrations/2026_08_11_120100_create_onboardings_tables.php"
      provides: "Tabelas onboardings + onboarding_passos + onboarding_links"
      contains: "disponivel_em"
    - path: "app/Models/TemplatePasso.php"
      provides: "Catálogo fechado de dono, auto_fonte e condicao como constantes PHP (D-09/D-14)"
      contains: "AUTO_FONTES"
    - path: "app/Models/OnboardingPasso.php"
      provides: "Estados de passo incluindo aguardando_coleta e indeterminado (D-11)"
      contains: "STATUS_AGUARDANDO_COLETA"
  key_links:
    - from: "app/Models/Onboarding.php"
      to: "onboarding_templates"
      via: "template_id — versão congelada em que o onboarding nasceu"
      pattern: "template_id"
    - from: "app/Models/OnboardingPasso.php"
      to: "template_passos"
      via: "template_passo_id + chave denormalizada"
      pattern: "template_passo_id"
---

<objective>
Criar o schema do motor de onboarding: 5 tabelas novas e seus 5 models, **ao lado** do onboarding
de Polos, sem tocar em nada dele.

Purpose: tudo nesta fase pendura aqui. Três decisões de schema são as que separam este motor de
"mais um checklist": (a) o versionamento é **imutável por linha nova** — publicar não faz `UPDATE`
em `template_passos` de uma versão publicada (D-07/SC-09); (b) o estado de um passo tem **seis**
valores, não dois, porque "tabela vazia" nunca pode virar "zero" (D-11/SC-07); (c) existe
`disponivel_em` — sem ele o painel não sabe responder "há quantos dias" para nenhum passo
dependente (SC-11), lacuna estrutural levantada pelo `135-UI-SPEC.md`.

Output: 2 migrations (5 tabelas), 5 models com casts e constantes de catálogo fechado, suíte de
schema verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-CONTEXT.md
@.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-PATTERNS.md
@.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Contratos e decisões de modelagem já tomadas. O executor NÃO decide isto. -->

**Molde de migration (ler antes de escrever):**
`database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php`
- `Schema::hasTable(...)` como guarda de idempotência antes de cada `Schema::create`
- `down()` simétrico, em ordem REVERSA de dependência de FK
- índice único parcial multi-driver nas linhas 87-99 (SQLite `WHERE` vs. MariaDB coluna gerada)

`database/migrations/2026_05_11_000001_create_mlb_implementacoes_table.php`
- `$table->json('dados')->nullable()` + `$table->string('token', 64)->unique()`

**Molde de model:** `app/Models/MlbImplementacao.php` (`$fillable` + `$casts` com `'dados' => 'array'`)

**Schema-alvo desta fase (decidido — não reabrir):**

`onboarding_templates`: id, servico_id (FK servicos, cascadeOnDelete), versao (unsignedInteger),
ativo (boolean default false), publicado_em (timestamp nullable), publicado_por (FK users
nullOnDelete, nullable), timestamps.
- `unique(['servico_id','versao'])`
- índice único parcial **composto**: só 1 linha `ativo = 1` por `servico_id`

`template_passos`: id, template_id (FK onboarding_templates, cascadeOnDelete), ordem
(unsignedSmallInteger), chave (string 60), titulo (string 160), descricao (text nullable),
dono (string 20), setor_id (FK setores nullOnDelete, nullable), depende_de (json nullable —
**array de `chave`, não de id**), sla_dias (unsignedSmallInteger nullable), auto_fonte (string 60
nullable), condicao (json nullable), obrigatorio (boolean default true), timestamps.
- `unique(['template_id','chave'])`

`onboardings`: id, company_id (FK companies cascadeOnDelete), servico_id (FK servicos
cascadeOnDelete), contrato_servico_id (FK contratos_servico nullOnDelete, nullable),
template_id (FK onboarding_templates restrictOnDelete), status (string 20 default 'rascunho'),
responsavel_id (FK users nullOnDelete, nullable), iniciado_em (timestamp nullable — vira
`andamento`), concluido_em (timestamp nullable), timestamps.
- `unique('contrato_servico_id')` — um onboarding por contrato (SC-01)
- índice `['company_id','servico_id','status']`

`onboarding_passos`: id, onboarding_id (FK cascadeOnDelete), template_passo_id (FK
template_passos restrictOnDelete), chave (string 60 — **denormalizada** do template, é a chave de
agregação de D-10/SC-10), status (string 24 default 'bloqueado'), valor (json nullable),
feito_por (FK users nullOnDelete, nullable), feito_em (timestamp nullable), auto_em (timestamp
nullable), disponivel_em (timestamp nullable), coleta_iniciada_em (timestamp nullable),
tentativas (unsignedSmallInteger default 0), ultimo_erro (string 255 nullable), timestamps.
- `unique(['onboarding_id','template_passo_id'])`
- índice `['status','disponivel_em']` (o comando de reavaliação varre por aqui)
- índice `['chave']`

`onboarding_links`: id, company_id (FK companies, `->unique()`, cascadeOnDelete), token
(string 64 unique), ultimo_acesso (timestamp nullable), timestamps.

**Catálogos fechados (constantes PHP, nunca texto livre vindo de request — D-09/D-14):**

`TemplatePasso`:
- `DONO_CLIENTE='cliente'`, `DONO_INTERNO='interno'`, `DONO_SISTEMA='sistema'`, `DONOS=[...]` (D-14: três, não quatro)
- `AUTO_FONTE_ADMAN_ACCOUNT_ID='adman_account_id_preenchido'`
- `AUTO_FONTE_ADMAN_GRANT='adman_grant_ativo'`
- `AUTO_FONTE_ML_TOKEN='ml_token_ativo'`
- `AUTO_FONTE_ACERVO='acervo_coletado'`
- `AUTO_FONTE_METRICAS='metricas_conta'`
- `AUTO_FONTES = [as 5 acima]`
- `CONDICAO_ANUNCIOS_INATIVOS='anuncios_inativos_maior_que_zero'`, `CONDICOES=[...]` (D-12)

`Onboarding`: `STATUS_RASCUNHO`, `STATUS_ANDAMENTO`, `STATUS_CONCLUIDO`, `STATUSES=[...]`

`OnboardingPasso` (D-11 — seis estados, nunca dois):
- `STATUS_BLOQUEADO='bloqueado'` (depende de passo não concluído)
- `STATUS_ABERTO='aberto'` (acionável, esperando o dono)
- `STATUS_AGUARDANDO_COLETA='aguardando_coleta'` (resolver disparou coleta assíncrona)
- `STATUS_INDETERMINADO='indeterminado'` (sonda devolveu 429/timeout — retenta)
- `STATUS_CONCLUIDO='concluido'`
- `STATUS_NAO_APLICAVEL='nao_aplicavel'` (condição de D-12 avaliada falsa)

**Armadilhas ativas:**
- `UPDATE <tabela> <alias> SET` (JOIN-update, MySQL-only) **derruba a suíte SQLite** — se
  precisar de backfill em migration, usar `DB::table(...)->update([...])`
- MariaDB não tem índice único parcial nativo — usar a bifurcação por driver do molde NPS
- Testes rodam em SQLite `:memory:` (`phpunit.xml:27-28`); produção é MariaDB
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration das tabelas de template (versionamento imutável + 1 ativa por serviço)</name>

  <files>database/migrations/2026_08_11_120000_create_onboarding_templates_tables.php</files>

  <read_first>
    - `database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` (molde — guarda `hasTable`, `down()` reverso e, nas linhas 87-99, o índice único parcial multi-driver que este plano reaproveita)
    - `app/Models/Servico.php` (FK alvo — tabela `servicos`, constantes de setor)
    - `app/Models/Setor.php` (FK alvo de `template_passos.setor_id`)
  </read_first>

  <action>
Criar `database/migrations/2026_08_11_120000_create_onboarding_templates_tables.php` com as tabelas
`onboarding_templates` e `template_passos` exatamente na forma descrita no bloco `<interfaces>`.

O ponto caro é o índice único parcial **composto** que garante "só uma versão `ativo=true` por
`servico_id`" (D-07/SC-09). Reaproveitar a técnica das linhas 90-99 do molde NPS, adaptando o
escopo: no SQLite, `CREATE UNIQUE INDEX onboarding_templates_ativo_uniq ON onboarding_templates(servico_id) WHERE ativo = 1`;
no MySQL/MariaDB, coluna virtual gerada cuja expressão devolve `servico_id` quando `ativo = 1` e
`NULL` caso contrário (`NULL` não colide em unique), mais `UNIQUE INDEX` sobre ela. Bifurcar por
`DB::connection()->getDriverName()`, como o molde faz.

`depende_de` guarda **array de `chave`** (string), não de `id` — chaves sobrevivem ao
versionamento (cada versão nova cria linhas novas com ids novos, mas as mesmas chaves), e a
guarda de ciclo do Plano 07 opera sobre chaves.

Comentários de coluna em pt-BR via `->comment('...')` nas colunas não-óbvias (`versao`,
`publicado_em`, `depende_de`, `auto_fonte`, `condicao`), seguindo a convenção do projeto.

`down()` derruba `template_passos` antes de `onboarding_templates` e remove explicitamente o
índice/coluna gerada no ramo MySQL antes do `dropIfExists`. Nenhum `UPDATE ... alias SET` em
lugar nenhum.
  </action>

  <verify>
    <automated>C:/xampp/php/php.exe artisan migrate --env=testing --database=sqlite 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `C:/xampp/php/php.exe artisan migrate:fresh --env=testing` conclui sem erro de SQL
    - Inserir dois `onboarding_templates` com o MESMO `servico_id` e `ativo = true` lança `QueryException` (unicidade garantida pelo banco, não pela aplicação)
    - Inserir dois `onboarding_templates` com o mesmo `servico_id`, um `ativo = true` e outro `ativo = false`, funciona
    - Inserir dois `template_passos` com o mesmo `template_id` e a mesma `chave` lança `QueryException`
    - `grep -v '^\s*//' database/migrations/2026_08_11_120000_create_onboarding_templates_tables.php | grep -c 'getDriverName'` retorna ≥ 1
    - Nenhuma ocorrência de JOIN-update MySQL-only: `grep -Ec "UPDATE +[a-z_]+ +[a-z_]+ +SET" database/migrations/2026_08_11_120000_create_onboarding_templates_tables.php` retorna 0
  </acceptance_criteria>

  <done>As duas tabelas de template existem, versionadas, com a unicidade de "1 versão ativa por serviço" imposta pelo banco nos dois drivers.</done>
</task>

<task type="auto">
  <name>Task 2: Migration das tabelas de onboarding (com disponivel_em) e do link por empresa</name>

  <files>database/migrations/2026_08_11_120100_create_onboardings_tables.php</files>

  <read_first>
    - `database/migrations/2026_05_11_000001_create_mlb_implementacoes_table.php` (molde de "1 token por dono": `foreignId(...)->unique()->constrained()` + `string('token',64)->unique()`)
    - `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-UI-SPEC.md` (seção "Gap de dado que esta tela expõe" — por que `disponivel_em` precisa existir)
    - `app/Models/ContratoServico.php` (FK alvo — tabela `contratos_servico`)
  </read_first>

  <action>
Criar `database/migrations/2026_08_11_120100_create_onboardings_tables.php` com `onboardings`,
`onboarding_passos` e `onboarding_links`, exatamente na forma do bloco `<interfaces>`.

Três colunas precisam de comentário explícito em pt-BR porque a razão de existirem não é dedutível:
- `onboarding_passos.disponivel_em` — "momento em que o passo ficou acionável (onboarding saiu de
  rascunho, para passos sem dependência; ou todas as `depende_de` concluíram). É a origem da
  contagem de 'há quantos dias' do painel — nunca contar do created_at do onboarding."
- `onboarding_passos.coleta_iniciada_em` — "quando o resolver disparou uma coleta assíncrona;
  alimenta o watchdog visual de 'coleta demorando mais que o esperado'. É timestamp de **disparo**,
  carimbado uma única vez: reavaliação que apenas reconfirma a coleta em curso não reescreve o
  valor, senão o relógio nunca envelhece e nem o redisparo nem o watchdog de 30 min chegam a
  disparar."
- `onboarding_passos.chave` — "denormalizada de template_passos.chave; é a chave de agregação do
  link único por empresa (D-06/D-10) e precisa sobreviver à troca de versão do template."

`onboardings.template_id` usa `restrictOnDelete()` (a versão em que o onboarding nasceu não pode
sumir debaixo dele — é o que sustenta SC-09), enquanto `contrato_servico_id` usa `nullOnDelete()`
(o histórico do onboarding sobrevive à exclusão do contrato).

`onboardings` recebe `unique('contrato_servico_id')` — é a tradução literal de "um onboarding por
contrato" (SC-01) para uma constraint de banco.

`down()` em ordem reversa: `onboarding_links`, `onboarding_passos`, `onboardings`.
Guarda `Schema::hasTable(...)` antes de cada `create`, como o molde NPS.
  </action>

  <verify>
    <automated>C:/xampp/php/php.exe artisan test --filter=OnboardingSchemaTest</automated>
  </verify>

  <acceptance_criteria>
    - `C:/xampp/php/php.exe artisan migrate:fresh --env=testing` conclui sem erro
    - `Schema::hasColumn('onboarding_passos', 'disponivel_em')` é `true`
    - `Schema::hasColumn('onboarding_passos', 'coleta_iniciada_em')` é `true`
    - `Schema::hasColumn('onboarding_passos', 'chave')` é `true`
    - Dois `onboardings` com o mesmo `contrato_servico_id` não-nulo lançam `QueryException`
    - Dois `onboarding_links` com o mesmo `company_id` lançam `QueryException`
    - Dois `onboarding_passos` com o mesmo par `onboarding_id` + `template_passo_id` lançam `QueryException`
    - `grep -Ec "UPDATE +[a-z_]+ +[a-z_]+ +SET" database/migrations/2026_08_11_120100_create_onboardings_tables.php` retorna 0
  </acceptance_criteria>

  <done>As três tabelas de instância existem, com `disponivel_em` no schema e "um onboarding por contrato" imposto pelo banco.</done>
</task>

<task type="auto">
  <name>Task 3: Os 5 models com casts, relações e catálogos fechados</name>

  <files>app/Models/OnboardingTemplate.php, app/Models/TemplatePasso.php, app/Models/Onboarding.php, app/Models/OnboardingPasso.php, app/Models/OnboardingLink.php, tests/Feature/Phase135/OnboardingSchemaTest.php</files>

  <read_first>
    - `app/Models/MlbImplementacao.php` (molde de `$fillable` + `$casts` com `'dados' => 'array'`, linhas 13-52)
    - `app/Models/Servico.php` (molde de constantes de catálogo: `SETOR_*` + array `SETORES`)
    - `app/Models/ContratoServico.php` (molde de model-pivot com `LogsActivity` e `getActivitylogOptions()`)
    - `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-CONTEXT.md` (D-11, D-14, D-19 — a semântica de cada constante)
  </read_first>

  <action>
Criar os 5 models em `app/Models/`: `OnboardingTemplate`, `TemplatePasso`, `Onboarding`,
`OnboardingPasso`, `OnboardingLink`.

Casts obrigatórios: `TemplatePasso::$casts` com `'depende_de' => 'array'`, `'condicao' => 'array'`,
`'obrigatorio' => 'boolean'`; `OnboardingPasso::$casts` com `'valor' => 'array'`,
`'feito_em'|'auto_em'|'disponivel_em'|'coleta_iniciada_em' => 'datetime'`;
`OnboardingTemplate::$casts` com `'ativo' => 'boolean'`, `'publicado_em' => 'datetime'`;
`Onboarding::$casts` com `'iniciado_em'|'concluido_em' => 'datetime'`.

Constantes de catálogo fechado exatamente como listadas no bloco `<interfaces>` — os arrays
`TemplatePasso::DONOS`, `TemplatePasso::AUTO_FONTES`, `TemplatePasso::CONDICOES`,
`Onboarding::STATUSES` e `OnboardingPasso::STATUSES` são a fonte de verdade que o FormRequest do
Plano 07 e o registry de resolvers do Plano 03 vão validar contra. Nenhum valor de enum pode
existir só como string solta no código.

No docblock de `TemplatePasso`, registrar em pt-BR a regra da D-19: `dono` responde "de quem é a
bola?" e `auto_fonte` responde "como o sistema sabe que aconteceu?" — eixos independentes; um
passo com `dono = cliente` pode ter `auto_fonte` preenchido (é o passo 5 do template de Gestão) e
nesse caso **nunca** aceita conclusão manual como verdade.

No docblock de `OnboardingPasso`, registrar em pt-BR a regra da D-11: `aguardando_coleta` e
`indeterminado` existem para que "tabela vazia" nunca seja lida como "zero real"; um resolver que
devolva booleano está errado por construção.

Relações: `OnboardingTemplate` hasMany `TemplatePasso` (ordenado por `ordem`) e belongsTo `Servico`;
`Onboarding` belongsTo `Company`/`Servico`/`OnboardingTemplate`/`ContratoServico`, belongsTo `User`
como `responsavel`, hasMany `OnboardingPasso`; `OnboardingPasso` belongsTo `Onboarding`/`TemplatePasso`
e belongsTo `User` como `feitoPor`; `OnboardingLink` belongsTo `Company`.

`Onboarding` usa `LogsActivity` com `getActivitylogOptions()` logando `status` e `responsavel_id`
(transições são a parte auditável), no mesmo formato de `ContratoServico`. Os demais models não
precisam de activitylog.

Scopes úteis: `Onboarding::scopeEmAndamento()`, `Onboarding::scopeNaoConcluido()`,
`OnboardingPasso::scopePendentesDeReavaliacao()` (status em `aguardando_coleta` ou `indeterminado`),
`OnboardingTemplate::scopeAtivo()`.

Criar `tests/Feature/Phase135/OnboardingSchemaTest.php` cobrindo: os casts de array e datetime
devolvem os tipos certos após round-trip no banco; `AUTO_FONTES` tem exatamente 5 entradas e
`DONOS` exatamente 3; `OnboardingPasso::STATUSES` contém `aguardando_coleta` e `indeterminado`;
e as 4 constraints de unicidade da Task 1 e 2 lançam `QueryException`. Comentários em pt-BR.
  </action>

  <verify>
    <automated>C:/xampp/php/php.exe artisan test --filter=OnboardingSchemaTest</automated>
  </verify>

  <acceptance_criteria>
    - `C:/xampp/php/php.exe artisan test --filter=OnboardingSchemaTest` termina com 0 failures
    - `count(TemplatePasso::AUTO_FONTES) === 5` e os 5 valores são exatamente `adman_account_id_preenchido`, `adman_grant_ativo`, `ml_token_ativo`, `acervo_coletado`, `metricas_conta`
    - `count(TemplatePasso::DONOS) === 3` e contém `cliente`, `interno`, `sistema` (D-14 — nenhum quarto dono)
    - `count(OnboardingPasso::STATUSES) === 6` e contém `aguardando_coleta`, `indeterminado` e `nao_aplicavel`
    - Um `TemplatePasso` salvo com `depende_de = ['acesso_colaborador_ml']` volta do banco como `array`, não como string JSON
    - Um `OnboardingPasso` salvo com `disponivel_em = now()` volta como instância de `Carbon`
    - `grep -rn "mlb_implementacoes\|MlbImplementacao" app/Models/Onboarding*.php app/Models/TemplatePasso.php` não retorna nenhuma linha (D-02: motor novo não referencia Polos)
  </acceptance_criteria>

  <done>Os 5 models existem com casts corretos e os catálogos fechados de `dono`, `auto_fonte`, `condicao` e status disponíveis como constantes PHP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aplicação → banco | Valores de `dono`, `auto_fonte`, `condicao` e `status` chegam do CRUD de template (Plano 07) e precisam ser restringidos por catálogo, não por confiança |
| token público → banco | `onboarding_links.token` é capacidade por posse — quem tem a string acessa os dados de onboarding da empresa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-02-01 | Tampering | `template_passos.auto_fonte` / `dono` / `condicao` | mitigate | Catálogos fechados como constantes PHP (`TemplatePasso::AUTO_FONTES/DONOS/CONDICOES`) — o FormRequest do Plano 07 valida com `Rule::in()` contra elas; nenhuma string livre do request chega ao banco (D-09) |
| T-135-02-02 | Information Disclosure | `onboarding_links.token` | mitigate | `string(64)` com `unique()` no banco; a geração usa `Str::random(48)` (~285 bits) no Plano 10 — mesma classe de risco já aceita para Polos, sem índice sequencial |
| T-135-02-03 | Tampering | Versão publicada de template alterada retroativamente | mitigate | `onboardings.template_id` com `restrictOnDelete()` + índice único parcial de "1 ativa por serviço"; o Plano 07 publica versão N+1 por INSERT, nunca UPDATE em `template_passos` publicado |
| T-135-02-04 | Denial of Service | Ciclo em `depende_de` travando a avaliação de passos | transfer | Schema guarda `depende_de` como array de chaves; a detecção de ciclo é obrigatória no save do template (Plano 07, SC-08) — este plano só garante que a estrutura permita o algoritmo |
| T-135-02-SC | Tampering | instalação de pacote npm/pip/cargo | accept | Nenhum pacote novo (`135-RESEARCH.md` §Package Legitimacy Audit: "Não aplicável") |
</threat_model>

<verification>
1. `C:/xampp/php/php.exe artisan migrate:fresh --env=testing` conclui sem erro (as duas migrations rodam em SQLite)
2. `C:/xampp/php/php.exe artisan test --filter=OnboardingSchemaTest` → 0 failures
3. `C:/xampp/php/php.exe artisan test --filter=OnboardingWave0Test` → continua 0 failures
4. Nenhum arquivo de Polos em `git status --porcelain` (`mlb_implementacoes`, `MlbImplementacao*`, `Pages/MlbImplementacao/`)
</verification>

<success_criteria>
- 5 tabelas criadas, migrations idempotentes e portáveis SQLite↔MariaDB
- `disponivel_em` e `coleta_iniciada_em` no schema (SC-11 e watchdog viáveis)
- Catálogos fechados como constantes PHP em `TemplatePasso`, `Onboarding` e `OnboardingPasso`
- 4 constraints de unicidade impostas pelo banco (versão ativa por serviço, chave por template, contrato por onboarding, link por empresa)
</success_criteria>

<output>
Criar `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-02-SUMMARY.md` ao terminar.
</output>
