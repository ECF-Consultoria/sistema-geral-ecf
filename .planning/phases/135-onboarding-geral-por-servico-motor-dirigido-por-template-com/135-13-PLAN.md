---
phase: 135-onboarding-geral-por-servico-motor-dirigido-por-template-com
plan: 13
type: execute
wave: 7
depends_on: ["135-07", "135-10", "135-11", "135-12"]
files_modified:
  - .planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-BASELINE-TESTES.md
  - .planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-GATE-FINAL.md
autonomous: false
requirements: [SC-02, SC-06, SC-07, SC-11, D-02, D-11, D-18]

must_haves:
  truths:
    - "O onboarding de Polos continua byte-a-byte intocado, provado por diff de arquivos e por contagem de falhas contra a baseline (SC-02/D-02)"
    - "Os 11 critérios de sucesso e as 19 decisões têm evidência apontada, não afirmação"
    - "A sonda de grant Adman foi conferida contra a API real, incluindo o comportamento de 429 (D-18/D-11)"
    - "A coleta assíncrona do acervo foi conferida com empresa nunca sincronizada (SC-07)"
    - "As 3 telas novas renderizam no navegador e estão no manifest do Vite"
  artifacts:
    - path: ".planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-GATE-FINAL.md"
      provides: "Evidência por SC e por D, com comando/arquivo:linha, e o veredito do gate de Polos"
      contains: "SC-02"
  key_links:
    - from: ".planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-GATE-FINAL.md"
      to: ".planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-BASELINE-TESTES.md"
      via: "comparação antes × depois das suítes de Polos"
      pattern: "baseline"
---

<objective>
Fechar a fase provando as duas coisas que ninguém consegue provar olhando para um plano isolado: que
o Polos não regrediu, e que os 11 critérios de sucesso têm evidência.

Purpose: SC-02 é uma comparação, não uma opinião. O `135-BASELINE-TESTES.md` guardou o "antes" no
Plano 01; aqui se mede o "depois" e se compara. E as quatro verificações que nenhum teste
automatizado alcança — a semântica real da sonda Adman (incluindo 429), a coleta assíncrona de uma
empresa nunca sincronizada, e o render das 3 telas — precisam de um humano com acesso ao ambiente.

Este plano **não é autônomo**: termina num checkpoint que só o usuário fecha.

Output: `135-GATE-FINAL.md` com evidência por SC e por D, baseline atualizada e o checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-VALIDATION.md
@.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-BASELINE-TESTES.md
@.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-CONTEXT.md
@.planning/learnings/painel-polos-status-e-meta.md

<interfaces>
<!-- O escopo intocável de Polos e o mapa de evidência. -->

**Arquivos que NÃO podem ter sido modificados por nenhum plano desta fase (D-02/SC-02):**
- `app/Models/MlbImplementacao.php`
- `app/Http/Controllers/MlbImplementacaoController.php`
- `app/Models/MlbEmpresa.php`, `app/Observers/MlbEmpresaObserver.php`
- `resources/js/Pages/Mlb/ImplementacaoPublica.jsx`
- `resources/js/Pages/MlbImplementacao/` (todo o diretório)
- `resources/js/Pages/Mlb/OnboardingFicha.jsx`
- Qualquer migration de `mlb_implementacoes`
- As rotas `/implementacao/*` e `/mlb/implementacao*` em `routes/web.php`
- A entrada `'implementacao/*'` em `bootstrap/app.php`

**Suítes do gate de regressão:** `PolosControllerTest`, `PolosFaturamentoSnapshotTest`.
Regra: falha já registrada na baseline **não é** regressão desta fase; falha **nova** viola D-02 e
trava a fase.

**Suítes de risco do Observer:** `Phase112HubspotHandoffWebhookTest`, `Phase113HubspotDedupTest`,
`Phase37ComercialListagemTest`, `Phase37CompaniesPerformanceFilterTest`.

**Verificações que nenhum teste alcança (`135-VALIDATION.md` §Verificações só-manuais):**
| Comportamento | SC | Por que manual |
|---|---|---|
| Sonda de grant Adman contra a API real | SC-06/D-18 | Testes usam `Http::fake()`; a semântica real (sucesso = grant ativo · 400/404/500 = sem grant · **429 = indeterminado**) só se confirma contra `api.adman.com.br`, respeitando `ADMAN_RATE_LIMIT_RPM = 10` |
| Coleta assíncrona do acervo | SC-07 | `mlb:sync-acervo` **enfileira** (job de até 1800 s); nenhum teste espera isso |
| Render das 3 telas novas | SC-11 | Teste de Inertia cobre props, não pixels |
| Página React some do manifest | SC-11 | Armadilha registrada: re-export puro não entra no build; o erro só aparece em runtime |

**`artisan test` sem `--filter` não conclui neste projeto.** Todo comando deste plano usa filtro,
e o interpretador é `C:/xampp/php/php.exe`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de regressão do Polos (SC-02) — diff de arquivos + contagem contra a baseline</name>

  <files>.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-BASELINE-TESTES.md, .planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-GATE-FINAL.md</files>

  <read_first>
    - `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-BASELINE-TESTES.md` (arquivo sendo modificado — o "antes" e o SHA em que foi coletado)
    - `.planning/learnings/painel-polos-status-e-meta.md` (§2 — por que as falhas antigas dessas suítes não são de ninguém)
    - `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-VALIDATION.md` (a regra do gate)
  </read_first>

  <action>
Rodar o gate em duas frentes.

**Frente 1 — diff de arquivos.** Comparar o SHA registrado no frontmatter do
`135-BASELINE-TESTES.md` com o `HEAD` atual (`git diff --name-only <sha_baseline>..HEAD`) e
verificar que **nenhum** dos arquivos/diretórios da lista do bloco `<interfaces>` aparece. Este é o
sentido literal de "byte-a-byte intocado": não é "não quebrou", é "não foi tocado".

**Frente 2 — contagem de falhas.** Rodar filtrado, uma a uma:
`PolosControllerTest`, `PolosFaturamentoSnapshotTest`, `Phase112HubspotHandoffWebhookTest`,
`Phase113HubspotDedupTest`, `Phase37ComercialListagemTest`, `Phase37CompaniesPerformanceFilterTest`,
e `Phase135`.

Atualizar `135-BASELINE-TESTES.md` com a coluna "depois da fase" e o SHA do `HEAD` atual.

Se qualquer suíte de Polos tiver falha **nova** em relação à baseline: **parar** e registrar em
`135-GATE-FINAL.md` a falha, o teste e a causa provável — o gate de D-02 é bloqueante e não se
resolve ajustando a expectativa.

Criar `135-GATE-FINAL.md` com frontmatter (`phase: 135`, `executado_em`, `sha`) e duas seções:
"Gate de Polos (SC-02/D-02)" com o resultado das duas frentes, e "Suítes de risco do Observer" com
a comparação por suíte.
  </action>

  <verify>
    <automated>C:/xampp/php/php.exe artisan test --filter=PolosControllerTest</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --name-only <sha_baseline>..HEAD` não lista nenhum dos arquivos/diretórios de Polos do bloco `<interfaces>`
    - `git diff <sha_baseline>..HEAD -- routes/web.php | grep -c '^-.*implementacao'` retorna 0 (nenhuma rota de Polos removida ou alterada)
    - `git diff <sha_baseline>..HEAD -- bootstrap/app.php | grep -c "^-.*'implementacao/\*'"` retorna 0
    - `PolosControllerTest` e `PolosFaturamentoSnapshotTest` têm contagem de failures **igual ou menor** à da baseline
    - As 4 suítes de risco do Observer têm contagem de failures **igual ou menor** à da baseline
    - `C:/xampp/php/php.exe artisan test --filter=Phase135` termina com 0 failures
    - `135-GATE-FINAL.md` existe, com o SHA do HEAD e o veredito explícito `APROVADO` ou `REPROVADO` na seção do gate de Polos
    - `135-BASELINE-TESTES.md` passa a ter as contagens "depois da fase" para as 6 suítes
  </acceptance_criteria>

  <done>Está provado, por diff e por contagem, que o onboarding de Polos não foi tocado nem regrediu.</done>
</task>

<task type="auto">
  <name>Task 2: Mapa de evidência SC-01..SC-11 e D-01..D-19</name>

  <files>.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-GATE-FINAL.md</files>

  <read_first>
    - `.planning/ROADMAP.md` (seção Phase 135 — os 11 critérios de sucesso, na íntegra)
    - `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-CONTEXT.md` (as decisões D-01..D-19)
    - `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-GATE-FINAL.md` (arquivo sendo modificado — o que a Task 1 deixou)
    - Os 12 `135-NN-SUMMARY.md` produzidos pelos planos anteriores
  </read_first>

  <action>
Acrescentar a `135-GATE-FINAL.md` duas tabelas de evidência.

**Tabela 1 — Critérios de sucesso.** Uma linha por `SC-01..SC-11`, com colunas
`SC | Comportamento | Evidência | Status`. A coluna Evidência precisa ser **verificável por outra
pessoa**: nome de teste + comando com `--filter`, ou `arquivo:linha`, ou saída de CLI. Frases como
"implementado no Plano X" não contam como evidência.

**Tabela 2 — Decisões.** Uma linha por `D-01..D-19`, com colunas
`D | Decisão | Onde vive no código | Status`. Para decisões que não viram código diretamente
(D-02 é "não fazer", D-08 é restrição de escopo), a evidência é a **ausência** provada: o comando
de diff/grep que demonstra que nada foi tocado ou que nada além de Gestão foi criado.

Rodar, e registrar a saída, as verificações estruturais de fecho:
- `C:/xampp/php/php.exe artisan test --filter=Phase135` (suíte inteira da fase)
- `C:/xampp/php/php.exe artisan route:list --path=onboarding` (as rotas internas)
- `C:/xampp/php/php.exe artisan route:list --path=onboarding-cliente` (as 3 públicas)
- `C:/xampp/php/php.exe artisan schedule:list` filtrado por `onboarding`
- `npm run build` e a presença das 3 páginas no `public/build/manifest.json`
- `OnboardingResolverFactory::catalogo()` devolvendo as 5 chaves (via `artisan tinker` ou um teste)

Qualquer SC ou D sem evidência verificável entra como `PENDENTE` com o que falta — nunca como
`OK` por presunção.
  </action>

  <verify>
    <automated>C:/xampp/php/php.exe artisan test --filter=Phase135</automated>
  </verify>

  <acceptance_criteria>
    - `135-GATE-FINAL.md` contém uma tabela com as 11 linhas `SC-01`..`SC-11`, cada uma com evidência que é comando, teste ou `arquivo:linha`
    - `135-GATE-FINAL.md` contém uma tabela com as 19 linhas `D-01`..`D-19`
    - Nenhuma célula de Evidência contém apenas "implementado no Plano NN" sem comando/arquivo
    - `C:/xampp/php/php.exe artisan test --filter=Phase135` termina com 0 failures
    - `C:/xampp/php/php.exe artisan route:list --path=onboarding` lista `onboarding.templates.index`, `onboarding.templates.store`, `onboarding.templates.migrar`, `onboarding.painel.index`, `onboarding.painel.show`, `onboarding.responsavel.confirmar`, `onboarding.passos.concluir` e `onboarding.link.gerar`
    - `C:/xampp/php/php.exe artisan route:list --path=onboarding-cliente` lista as 3 rotas públicas
    - `C:/xampp/php/php.exe artisan schedule:list` contém `onboarding:reavaliar-passos`
    - `npm run build` conclui e `public/build/manifest.json` contém `Onboarding/Painel`, `Onboarding/Templates/Index` e `Onboarding/Publico`
  </acceptance_criteria>

  <done>Cada critério e cada decisão tem uma evidência que outra pessoa consegue reexecutar.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificações que nenhum teste alcança</name>

  <files>.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-GATE-FINAL.md</files>

  <read_first>
    - `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-VALIDATION.md` (seção "Verificações só-manuais")
    - `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-GATE-FINAL.md` (o que as Tasks 1 e 2 deixaram)
  </read_first>

  <action>
Preparar o ambiente para a conferência humana e então **pausar**, apresentando o roteiro de
`<how-to-verify>` ao usuário.

Antes de pausar: garantir que o worker de fila está descrito no roteiro (a coleta de acervo depende
dele), gerar um link público de teste para uma empresa com onboarding em `andamento`, e deixar em
`135-GATE-FINAL.md` uma seção "Verificações manuais" com as 4 linhas em branco, prontas para
receber o veredito de cada item.

Ao receber a resposta do usuário, preencher a seção com o resultado item a item e a data.
Divergência relatada em qualquer item vira registro explícito no documento — nunca "aprovado com
ressalva" implícito.
  </action>

  <verify>
    <automated>npm run build</automated>
    <human-check>Roteiro de 4 itens em `<how-to-verify>`, confirmado pelo usuário</human-check>
  </verify>

  <what-built>
    O motor de onboarding por serviço completo: 5 tabelas, 5 resolvers automáticos, Observer nos 4
    call-sites de contrato, CRUD de template versionado com guarda de ciclo, painel operacional,
    portal do cliente por token, e o comando de reavaliação agendado a cada 10 minutos.
    Todos os gates automatizados estão verdes e registrados em `135-GATE-FINAL.md`.
  </what-built>

  <how-to-verify>
1. **Sonda de grant Adman contra a API real (SC-06/D-18/D-11)**
   - Rodar o resolver do passo 4 para **uma** empresa com grant Adman comprovadamente ativo e
     **uma** sem grant. Sugestão: `C:/xampp/php/php.exe artisan onboarding:reavaliar-passos --onboarding=<id>`
     com o worker rodando, e conferir `onboarding_passos.status` e `valor` no banco por reconsulta
     (nunca por stdout).
   - Esperado: grant ativo → `concluido`; sem grant → `aberto`/`nao_coletado` com motivo.
   - **O ponto crítico:** provocar (ou aguardar) um `429` e confirmar que o passo vai para
     `indeterminado`, **nunca** para "sem grant". Respeitar `ADMAN_RATE_LIMIT_RPM = 10` — não
     disparar em lote.

2. **Coleta assíncrona do acervo (SC-07)**
   - Escolher uma empresa com `ml_tokens.status = active` que **nunca** sincronizou acervo
     (`ml_acervo_itens` sem linhas para ela) e rodar a reavaliação.
   - Esperado: o passo 8 fica em `aguardando_coleta` e a Tela 1 mostra "Coletando dados
     automaticamente…". **Nunca** "0 anúncios".
   - Depois que o worker terminar o `SyncMlAcervoCompanyJob`, rodar a reavaliação de novo e
     conferir que o passo conclui com os números certos.

3. **Render das 3 telas (SC-11)**
   - `npm run build`, hard reload, e abrir: `/onboarding`, `/onboarding/templates` e
     `/onboarding-cliente/{token}` (link gerado por uma empresa com onboarding em `andamento`).
   - Conferir que nenhuma das três dá "Unable to locate file in Vite manifest" e que o painel
     **não** mostra barra de porcentagem como resposta principal.
   - Na Tela 2, abrir o Select de `auto_fonte` e confirmar que aparecem 5 opções com rótulo legível
     e linha de ajuda — e que não há nenhum campo de texto livre ao lado.

4. **Conferência de escopo (D-02)**
   - Abrir `/mlb/implementacao` e um `/implementacao/{token}` existente do Polos e confirmar que
     continuam funcionando exatamente como antes.
  </how-to-verify>

  <acceptance_criteria>
    - O usuário confirma que a sonda Adman classificou corretamente os 3 casos, com atenção explícita ao 429 → `indeterminado`
    - O usuário confirma que empresa sem acervo mostrou "Coletando dados automaticamente…" e nunca "0 anúncios"
    - O usuário confirma que as 3 telas renderizam sem erro de manifest
    - O usuário confirma que `/mlb/implementacao` e `/implementacao/{token}` do Polos seguem funcionando
    - O resultado de cada item fica registrado em `135-GATE-FINAL.md`, seção "Verificações manuais", com data
  </acceptance_criteria>

  <resume-signal>Responder "aprovado" ou descrever o que divergiu, item a item</resume-signal>

  <done>As quatro verificações que nenhum teste alcança foram feitas por um humano com acesso ao ambiente, e o resultado está registrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambiente real (Adman, API do ML, worker) → verificação | A checagem manual toca APIs de produção com dados de clientes reais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-13-01 | Denial of Service | Verificação manual disparando sondas em lote e estourando o rate limit da Adman | mitigate | Instrução explícita de rodar com `--onboarding=<id>` (uma empresa por vez) e respeitar `ADMAN_RATE_LIMIT_RPM = 10` |
| T-135-13-02 | Repudiation | Gate de Polos aprovado por impressão em vez de por medição | mitigate | Duas frentes obrigatórias — `git diff --name-only` contra o SHA da baseline **e** contagem de falhas por suíte; veredito explícito `APROVADO`/`REPROVADO` gravado no documento |
| T-135-13-03 | Tampering | Falha nova em Polos "resolvida" ajustando a expectativa do teste | mitigate | O plano determina parar e registrar causa; o gate de D-02 é bloqueante por desenho |
| T-135-13-04 | Information Disclosure | Verificação manual do portal expondo link de cliente real | accept | O link já é capacidade por posse e a verificação é feita pelo próprio time que administra o sistema; o token não é registrado no documento de gate |
| T-135-13-SC | Tampering | instalação de pacote npm/pip/cargo | accept | Nenhum pacote foi instalado em nenhum plano da fase (`composer.json`/`package.json` inalterados) — verificar no diff final |
</threat_model>

<verification>
1. `git diff --name-only <sha_baseline>..HEAD` sem nenhum arquivo de Polos
2. `C:/xampp/php/php.exe artisan test --filter=Phase135` → 0 failures
3. Suítes de Polos e as 4 de risco com contagem ≤ baseline
4. `git diff <sha_baseline>..HEAD -- composer.json package.json` vazio (nenhum pacote novo na fase)
5. `135-GATE-FINAL.md` com as duas tabelas de evidência e a seção de verificações manuais preenchida
6. Checkpoint aprovado pelo usuário
</verification>

<success_criteria>
- Gate de Polos APROVADO por diff de arquivos e por contagem de falhas
- SC-01..SC-11 e D-01..D-19 com evidência reexecutável
- 3 telas no manifest e renderizando
- Sonda Adman conferida contra a API real, incluindo o comportamento de 429
- Coleta assíncrona conferida com empresa nunca sincronizada
- Nenhum pacote novo instalado na fase
</success_criteria>

<output>
Criar `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-13-SUMMARY.md` ao terminar.
</output>
