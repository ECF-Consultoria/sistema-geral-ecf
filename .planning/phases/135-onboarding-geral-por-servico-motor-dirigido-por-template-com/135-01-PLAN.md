---
phase: 135-onboarding-geral-por-servico-motor-dirigido-por-template-com
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-BASELINE-TESTES.md
  - database/factories/ContratoServicoFactory.php
  - app/Models/ContratoServico.php
  - tests/Feature/Phase135/OnboardingWave0Test.php
autonomous: true
requirements: [SC-02, D-02, D-13]

must_haves:
  truths:
    - "Existe um registro versionado da contagem de falhas das suítes de Polos ANTES de qualquer alteração desta fase (denominador do gate SC-02 / D-02)"
    - "Existe um registro da contagem de falhas dos 4 arquivos de teste que passarão a disparar o Observer de ContratoServico (D-13)"
    - "Um teste consegue criar ContratoServico via factory, sem montar array manualmente"
  artifacts:
    - path: ".planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-BASELINE-TESTES.md"
      provides: "Baseline numérica de falhas pré-existentes por suíte (Polos + arquivos de risco do Observer)"
      contains: "PolosControllerTest"
    - path: "database/factories/ContratoServicoFactory.php"
      provides: "Fixture de contrato de serviço para os testes do Observer"
      contains: "class ContratoServicoFactory"
    - path: "tests/Feature/Phase135/OnboardingWave0Test.php"
      provides: "Primeira suíte da fase — prova que o diretório e a factory funcionam"
  key_links:
    - from: "app/Models/ContratoServico.php"
      to: "database/factories/ContratoServicoFactory.php"
      via: "trait HasFactory"
      pattern: "use HasFactory"
---

<objective>
Fechar os gaps de Wave 0 do `135-VALIDATION.md` **antes** de qualquer linha de código do motor
existir: registrar a baseline de falhas pré-existentes das suítes que esta fase precisa vigiar,
e criar a `ContratoServicoFactory` que os testes do Observer vão precisar.

Purpose: o gate SC-02 ("onboarding de Polos byte-a-byte intocado") é uma comparação
**antes × depois**. Sem o "antes" registrado agora, ele vira julgamento por impressão — e o
`.planning/learnings/painel-polos-status-e-meta.md` §2 documenta que essas suítes já têm falhas
antigas que **não** são regressão de ninguém. O mesmo vale para os 4 arquivos que hoje criam
contrato de "Gestão" sem nenhum efeito colateral e que, a partir do Plano 04, passarão a
disparar o Observer.

Output: `135-BASELINE-TESTES.md` com contagens numéricas por suíte, `ContratoServicoFactory`,
e o diretório `tests/Feature/Phase135/` com a primeira suíte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-VALIDATION.md
@.planning/learnings/painel-polos-status-e-meta.md
@CLAUDE.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real — não explorar o codebase. -->

`app/Models/ContratoServico.php` (estado atual, resumido):
- `protected $table = 'contratos_servico'`
- `use LogsActivity;` (SEM `HasFactory` hoje — este plano adiciona)
- `$fillable`: company_id, servico_id, valor_contratado, data_contratacao, data_vencimento,
  ativo, observacoes, + 11 colunas `hubspot_*`
- `$casts`: valor_contratado decimal:2, data_contratacao/data_vencimento date:Y-m-d, ativo boolean

`database/factories/CompanyFactory.php` (molde de formato — 31 linhas):
- `protected $model = Company::class;`
- `definition(): array` devolvendo array plano com `fake()->...` e defaults literais
  (`active => true`, `marketplace => 'meli'`, `adman_account_id => null`)

Ambiente:
- `php` NÃO está no PATH do Bash tool → usar `C:/xampp/php/php.exe` (barra normal)
- `artisan test` **sem** `--filter` não conclui neste projeto (timeout em `MercadoLivreAdsService`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar a baseline de falhas pré-existentes (denominador do gate SC-02)</name>

  <read_first>
    - `.planning/learnings/painel-polos-status-e-meta.md` (§2 — as 10 falhas antigas de Polos e por que não são regressão)
    - `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-VALIDATION.md` (seção "Gate de regressão do Polos" e "Arquivos de risco do Observer")
    - `phpunit.xml` (confirmar `DB_CONNECTION=sqlite` / `:memory:` e `QUEUE_CONNECTION=sync`)
  </read_first>

  <action>
Rodar, uma a uma e **sempre com `--filter`**, as 6 suítes que esta fase precisa vigiar, e gravar
o resultado numérico em `.planning/phases/135-.../135-BASELINE-TESTES.md`.

Comandos exatos (executar cada um separadamente, `C:/xampp/php/php.exe artisan test --filter=<Classe>`):
- `PolosControllerTest` — gate de regressão do Polos (D-02/SC-02)
- `PolosFaturamentoSnapshotTest` — gate de regressão do Polos
- `Phase112HubspotHandoffWebhookTest` — call-site 1 do Observer (HubSpot webhook)
- `Phase113HubspotDedupTest` — call-site 1 do Observer (dedup)
- `Phase37ComercialListagemTest` — call-site 2 do Observer (Comercial)
- `Phase37CompaniesPerformanceFilterTest` — call-site 3 do Observer (Company/Show)

**Atenção aos caminhos:** o `135-VALIDATION.md` cita esses 4 arquivos como
`tests/Feature/Phase112/...`; os caminhos reais são planos —
`tests/Feature/Phase112HubspotHandoffWebhookTest.php`, `tests/Feature/Phase113HubspotDedupTest.php`,
`tests/Feature/Phase37ComercialListagemTest.php`, `tests/Feature/Phase37CompaniesPerformanceFilterTest.php`.
Usar o filtro por **nome de classe**, que independe do caminho.

O documento gerado deve ter: frontmatter com `phase: 135`, `registrado_em` (data ISO) e o SHA do
`HEAD` no momento da coleta; uma tabela com colunas `Suíte | Passed | Failed | Errors | Comando`;
e um parágrafo final, em pt-BR, dizendo explicitamente que falha listada aqui **não é** regressão
desta fase e que qualquer falha **nova** nessas mesmas suítes viola D-02/SC-02 e trava a fase.

Se alguma suíte não concluir em tempo hábil, registrar `TIMEOUT` na linha em vez de inventar
número — nunca deixar a célula vazia nem estimar.

Nenhum arquivo de aplicação pode ser alterado nesta task. É coleta e registro, só.
  </action>

  <verify>
    <automated>C:/xampp/php/php.exe artisan test --filter=PolosControllerTest</automated>
  </verify>

  <acceptance_criteria>
    - O arquivo `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-BASELINE-TESTES.md` existe
    - `grep -c 'PolosControllerTest\|PolosFaturamentoSnapshotTest\|Phase112HubspotHandoffWebhookTest\|Phase113HubspotDedupTest\|Phase37ComercialListagemTest\|Phase37CompaniesPerformanceFilterTest' 135-BASELINE-TESTES.md` retorna ≥ 6
    - Cada uma das 6 linhas da tabela tem valores numéricos em `Passed` e `Failed` (ou a string literal `TIMEOUT`), nunca célula vazia
    - O frontmatter contém `registrado_em` e o SHA do HEAD (`git rev-parse HEAD` no momento da coleta)
    - `git status --porcelain app/ database/ resources/ routes/` não retorna nenhuma linha (nenhum arquivo de aplicação tocado nesta task)
  </acceptance_criteria>

  <done>Existe um registro numérico, datado e amarrado a um SHA, das falhas pré-existentes das 6 suítes — o denominador contra o qual o Plano 12 vai medir regressão.</done>
</task>

<task type="auto">
  <name>Task 2: ContratoServicoFactory + diretório da suíte Phase135</name>

  <read_first>
    - `database/factories/CompanyFactory.php` (molde exato de formato, 31 linhas)
    - `app/Models/ContratoServico.php` (arquivo sendo modificado — ver `$fillable`/`$casts` reais)
    - `app/Models/Servico.php` (constantes `SETOR_PERFORMANCE`, `TIPO_MENSAL` — a factory precisa de um `servico_id` coerente)
  </read_first>

  <action>
Criar `database/factories/ContratoServicoFactory.php` no formato minimalista de `CompanyFactory`:
`protected $model = ContratoServico::class;` e `definition()` cobrindo só os campos obrigatórios —
`company_id` (via `Company::factory()`), `servico_id` (via `Servico::factory()` se existir, senão
criando o `Servico` inline com `setor => Servico::SETOR_PERFORMANCE` e
`tipo_cobranca => Servico::TIPO_MENSAL`), `valor_contratado`, `data_contratacao` (`now()->toDateString()`),
`data_vencimento => null` e `ativo => true`. Nenhuma coluna `hubspot_*` no default.

Acrescentar um state nomeado `paraServico(Servico|int $servico)` que fixa o `servico_id` — os testes
do Observer (Plano 04) precisam criar contrato do serviço "Gestão" especificamente.

Em `app/Models/ContratoServico.php`, adicionar `use Illuminate\Database\Eloquent\Factories\HasFactory;`
no bloco de imports e incluir `HasFactory` na lista de traits da classe (hoje é só `use LogsActivity;`).
Não alterar `$fillable`, `$casts`, `getActivitylogOptions()` nem os scopes existentes.

Criar `tests/Feature/Phase135/OnboardingWave0Test.php` com dois testes: um provando que
`ContratoServico::factory()->create()` persiste uma linha em `contratos_servico` com `ativo = true`,
e outro provando que o state `paraServico()` grava o `servico_id` pedido. Usar `RefreshDatabase`
(padrão da suíte). Comentários em pt-BR.
  </action>

  <verify>
    <automated>C:/xampp/php/php.exe artisan test --filter=OnboardingWave0Test</automated>
  </verify>

  <acceptance_criteria>
    - `C:/xampp/php/php.exe artisan test --filter=OnboardingWave0Test` termina com 0 failures e ≥ 2 testes executados
    - `grep -v '^\s*\*' app/Models/ContratoServico.php | grep -c 'HasFactory'` retorna ≥ 2 (import + uso na lista de traits)
    - `ContratoServico::factory()->create()` persiste uma linha com `ativo = true` e `data_vencimento = null`
    - `ContratoServico::factory()->paraServico($servico)->create()->servico_id` é igual ao id passado
    - O diretório `tests/Feature/Phase135/` existe e contém ao menos 1 arquivo
    - `git diff --stat app/Models/ContratoServico.php` mostra apenas linhas adicionadas (nenhuma remoção): `git diff -U0 app/Models/ContratoServico.php | grep -c '^-[^-]'` retorna 0
  </acceptance_criteria>

  <done>Testes de qualquer plano desta fase conseguem gerar contratos de serviço por factory, e a suíte `Phase135` tem casa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Este plano não cria rota, endpoint nem entrada de dado externo — é infraestrutura de teste e documentação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01-01 | Tampering | `app/Models/ContratoServico.php` (model de produção tocado por motivo de teste) | mitigate | Alteração restrita a adicionar o trait `HasFactory`; critério de aceite exige diff sem linhas removidas (`grep -c '^-[^-]'` = 0), provando que `$fillable`/`$casts`/activitylog ficaram intactos |
| T-135-01-02 | Repudiation | Baseline de falhas sem procedência | mitigate | `135-BASELINE-TESTES.md` grava o SHA do HEAD e a data da coleta — a baseline é auditável e não pode ser "lembrada" depois |
| T-135-01-SC | Tampering | instalação de pacote npm/pip/cargo | accept | Nenhum pacote novo é instalado nesta fase (`135-RESEARCH.md` §Package Legitimacy Audit: "Não aplicável"); `composer.json`/`package.json` não mudam |
</threat_model>

<verification>
1. `C:/xampp/php/php.exe artisan test --filter=OnboardingWave0Test` → 0 failures
2. `135-BASELINE-TESTES.md` existe, com 6 linhas de suíte preenchidas e SHA do HEAD
3. `git status --porcelain` mostra apenas: o baseline, a factory, `ContratoServico.php` e a suíte nova
4. Nenhum arquivo sob `app/Http/Controllers/MlbImplementacaoController.php`, `app/Models/MlbImplementacao.php`, `resources/js/Pages/MlbImplementacao/` ou `resources/js/Pages/Mlb/ImplementacaoPublica.jsx` foi tocado (D-02)
</verification>

<success_criteria>
- Baseline numérica das 6 suítes registrada, datada e amarrada a um SHA
- `ContratoServicoFactory` funcional, com state `paraServico()`
- `tests/Feature/Phase135/` criado com suíte verde
- Zero arquivos de Polos modificados
</success_criteria>

<output>
Criar `.planning/phases/135-onboarding-geral-por-servico-motor-dirigido-por-template-com/135-01-SUMMARY.md` ao terminar.
</output>
