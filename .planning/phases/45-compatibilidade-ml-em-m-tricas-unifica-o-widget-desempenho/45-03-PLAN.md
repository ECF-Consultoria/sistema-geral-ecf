---
phase: 45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho
plan: 03
type: execute
status: DEFERRED
wave: 3
depends_on:
  - 45-01
  - 45-02
deferred_at: "2026-06-29"
deferred_reason: "Smoke 45-01 retornou NÃO NECESSÁRIAS — Plan 45-02 deferred → refactor de PortfolioScoreService delegando ao factory não tem factory pra delegar. Ver 45-01-BYMOBILLE-SMOKE.md."
conditional: "Executar APENAS se 45-01-BYMOBILLE-SMOKE.md contiver 'NECESSÁRIAS'. Confirmado 'NÃO NECESSÁRIAS' em 2026-06-29 → DEFERRED."
files_modified:
  - app/Services/PortfolioScoreService.php
  - tests/Feature/Phase45/PortfolioScoreServiceMlCompatTest.php
autonomous: true
requirements:
  - REQ-45-06
  - REQ-45-07
must_haves:
  truths:
    - "PortfolioScoreService::compute() delega a busca de rev/rev_prev/ads para CompanyMetricsProviderFactory::for($company)->fetchScoreMetrics() em vez de usar AdmanMetric::whereIn() e AdmanService::getCachedGrossBillingsMany() diretamente"
    - "A lógica downstream (cálculo de crescimento, half1/half2, eligíveis, scoreFinal, classificação) permanece inalterada bit-a-bit"
    - "Empresa Adman-only continua produzindo EXATAMENTE o mesmo score que antes (anti-regressão)"
    - "Empresa ML-only (Bymobille #298) agora produz score != 0 quando tem dados em adman_metrics"
    - "Categoria execucao_ads continua null (descontinuada — pitfall §6.6 do RESEARCH)"
    - "PortfolioScoreService continua recebendo AdmanService via constructor (para usos legados fora do score principal) e agora também recebe CompanyMetricsProviderFactory"
  artifacts:
    - path: "app/Services/PortfolioScoreService.php"
      provides: "Service refatorado: usa factory por empresa para rev/rev_prev/ads"
      contains: "CompanyMetricsProviderFactory"
      min_lines: 250
    - path: "tests/Feature/Phase45/PortfolioScoreServiceMlCompatTest.php"
      provides: "Suite TDD: anti-regressão Adman + empresa ML-only gera score != 0"
      min_lines: 100
  key_links:
    - from: "app/Services/PortfolioScoreService.php"
      to: "app/Services/Performance/CompanyMetricsProviderFactory.php"
      via: "constructor injection + chamada por empresa no loop"
    - from: "app/Services/PortfolioScoreService.php"
      to: "app/Services/Sugadores/SugadoresAdsProviderFactory.php"
      via: "espelha padrão de refactor (Phase 39-04 — SugadorAnalysisService)"
---

<objective>
Wave 3 (condicional — executar apenas se Plans 45-01+02 confirmaram necessidade):

Refatorar `PortfolioScoreService::compute()` para substituir as chamadas diretas a `AdmanMetric::whereIn()` (para `sumAtual`) e `AdmanService::getCachedGrossBillingsMany()` por um loop empresa-a-empresa usando `CompanyMetricsProviderFactory::for($company)->fetchScoreMetrics($company, $from, $to)`.

**Analogia exata:** este plan espelha o que Plan 39-04 fez ao refatorar `SugadorAnalysisService` — o service central parou de chamar `AdmanService` diretamente e passou a delegar ao factory. A lógica de detecção (análogo: a lógica de score) ficou inalterada.

**Critério de sucesso principal:** empresa Adman-only → score idêntico ao anterior (zero regressão); empresa ML-only → score != 0 quando tem dados em adman_metrics.

**Nota sobre sumHalf1 / sumHalf2:** as queries das metades (linhas 206-217 do PortfolioScoreService) também leem `AdmanMetric` diretamente. Este plan TAMBÉM migra essas queries para o factory — mas com uma abordagem diferente: ao invés de chamar `fetchScoreMetrics` 3x por empresa (atual, half1, half2), o provider recebe os mesmos parâmetros `$from/$to` e é chamado uma vez por janela. Verificar se o contrato `fetchScoreMetrics(Company, string $from, string $to)` cobre essas janelas ou se um método auxiliar é necessário — decidir na implementação com base no código atual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-CONTEXT.md
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-RESEARCH.md
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md

<interfaces>
<!-- Estado atual de PortfolioScoreService::compute() (linhas 88-112 — O QUE VAI MUDAR): -->
```php
// ANTES (Phase 45 substitui isso):
$sumAtual = AdmanMetric::whereIn('company_id', $companyIds)
    ->whereBetween('reference_date', [$atualFrom, $atualTo])
    ->selectRaw('company_id, SUM(revenue) as rev, SUM(revenue_prev_period) as rev_prev, SUM(ad_spend) as ads')
    ->groupBy('company_id')
    ->get()
    ->keyBy('company_id');

$grossCache = $this->adman->getCachedGrossBillingsMany($custIds, $atualFrom, $atualTo);

$revAtual = [];
$revAnterior = [];
foreach ($companies as $c) {
    $row = $sumAtual->get($c->id);
    $cacheRev = null;
    if ($c->cust_id && isset($grossCache[$c->cust_id]['value']) && $grossCache[$c->cust_id]['value'] !== null) {
        $cacheRev = (float) $grossCache[$c->cust_id]['value'];
    }
    $revAtual[$c->id]    = $cacheRev ?? (float) ($row->rev ?? 0);
    $revAnterior[$c->id] = (float) ($row->rev_prev ?? 0);
}
```

<!-- DEPOIS (Phase 45 introduz): -->
```php
// Delega ao factory por empresa — provider correto escolhido automaticamente
// (Adman com cache gross para empresas Adman; ML sem cache para ML-only).
$revAtual   = [];
$revAnterior = [];
$adsAtual   = [];
foreach ($companies as $c) {
    try {
        $metrics = $this->metricsFactory->for($c)->fetchScoreMetrics($c, $atualFrom, $atualTo);
    } catch (\RuntimeException $e) {
        // Empresa sem provider (sem adman_account_id e sem mlToken) — score zerado, log warning.
        Log::warning("[Performance] PortfolioScoreService: empresa {$c->id} ({$c->name}) sem provider — {$e->getMessage()}");
        $metrics = ['rev' => 0.0, 'rev_prev' => 0.0, 'ads' => 0.0];
    }
    $revAtual[$c->id]    = $metrics['rev'];
    $revAnterior[$c->id] = $metrics['rev_prev'];
    $adsAtual[$c->id]    = $metrics['ads'];
}
```

<!-- Mesmo padrão para sumHalf1/sumHalf2 (linhas 206-217): -->
```php
// ANTES: AdmanMetric::whereIn(...)->whereBetween('reference_date', [$half1From, $atualTo])...
// DEPOIS: loop por empresa chamando factory->for($c)->fetchScoreMetrics($c, $half1From, $atualTo)
// (idem para half2)
```

<!-- Constructor a modificar (linha 43): -->
```php
// ANTES:
public function __construct(private AdmanService $adman) {}

// DEPOIS (injetar factory adicional):
public function __construct(
    private AdmanService $adman,
    private \App\Services\Performance\CompanyMetricsProviderFactory $metricsFactory,
) {}
```

<!-- Pattern de referência (SugadorAnalysisService refactor Plan 39-04): -->
// Plan 39-04 substituiu $this->adman->fetchAdsMetrics() por $provider->fetchAdgroupsMetrics()
// Preservou toda a lógica de detecção (evaluateMetrics, buildRow, STATUS_TRAVADOS)
// Aqui: preservamos scoreFinal(), mediana(), crescimentoAjustadoPct, recuperacaoPct etc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (BASELINE + RED): Capturar baseline + criar tests anti-regressão failing</name>
  <files>
    tests/Feature/Phase45/PortfolioScoreServiceMlCompatTest.php
  </files>
  <read_first>
    - app/Services/PortfolioScoreService.php (COMPLETO — ler antes de qualquer edit; entender todas as dependências: AdmanMetric, AdmanService::getCachedGrossBillingsMany, PortfolioGoal, Goal, Meeting, NpsSurvey, as 3 janelas temporal, e o shape de retorno de compute())
    - app/Services/Performance/CompanyMetricsProviderFactory.php (criado em 45-02 — o que vamos injetar)
    - app/Services/Performance/AdmanMetricsProvider.php (criado em 45-02)
    - app/Services/Performance/MlMetricsProvider.php (criado em 45-02)
    - app/Contracts/CompanyMetricsProvider.php (criado em 45-02)
    - tests/Feature/Phase45/CompanyMetricsProviderTest.php (criado em 45-02 — referência de estilo)
    - app/Models/AdmanMetric.php (campos para criar fixtures inline nos testes)
    - app/Models/Company.php (fields: adman_account_id, ml_store_id; relations: mlToken, companies())
  </read_first>
  <behavior>
    **Commit BASELINE (chore):** antes de qualquer modificação, rodar a suite atual e registrar count.
    `php artisan test --filter=PortfolioScore 2>&1` — se nenhum teste existir, registrar "0 tests" como baseline.

    **Tests Feature a criar (SQLite in-memory, RefreshDatabase):**

    1. `test_compute_empresa_adman_only_score_nao_zerado_quando_tem_dados_adman_metrics`
       - Setup: User + Company com adman_account_id='TEST123' + AdmanMetric com revenue=5000, revenue_prev_period=4000, ad_spend=500 para os últimos 30d
       - Act: `PortfolioScoreService::compute($user)` (mockar AdmanService::getCachedGrossBillingsMany para retornar null/empty — testar path DB puro)
       - Assert: `$resultado['score'] > 0` E `$resultado['empresas_eligiveis'] >= 1`

    2. `test_compute_empresa_ml_only_score_nao_zerado_quando_tem_dados_adman_metrics`
       - Setup: User + Company SEM adman_account_id COM mlToken(status='active') + AdmanMetric com revenue=3000, revenue_prev_period=2500 para os últimos 30d
       - Act: `PortfolioScoreService::compute($user)`
       - Assert: `$resultado['score'] > 0` E `$resultado['empresas_eligiveis'] >= 1`
       (Este teste vai FALHAR no RED pois o PortfolioScoreService ainda não usa o factory)

    3. `test_compute_empresa_sem_provider_nao_quebra_compute`
       - Setup: User + Company SEM adman_account_id E SEM mlToken
       - Act: `PortfolioScoreService::compute($user)`
       - Assert: não lança exceção; `$resultado['empresas_eligiveis'] = 0` (empresa zerada, não elegível); `$resultado['score']` é float

    4. `test_compute_empresa_hibrida_usa_ml_provider`
       - Setup: User + Company com adman_account_id='TEST' E mlToken(status='active') + AdmanMetric revenue=4000
       - Act: `PortfolioScoreService::compute($user)` com spy/mock no factory para confirmar que MlMetricsProvider é chamado
       - Assert: `$resultado['empresas_eligiveis'] >= 1`

    5. `test_compute_shape_retorno_preservado`
       - Assert que todas as chaves do shape de retorno estão presentes: `tem_base_comparativa`, `empresas_eligiveis`, `empresas_carteira`, `metricas`, `score`, `classificacao`, `periodo`
       - E que `metricas` tem chave `execucao_ads` com valor null (não regredir §6.6 RESEARCH)

    Nota: Tests 1+3+5 usam AdmanMetricsProvider (Adman-only setup); Test 2 usa MlMetricsProvider; Test 4 usa factory com empresa híbrida.
    Para isolar AdmanService (que faz chamadas HTTP), usar `$this->mock(AdmanService::class)` retornando arrays vazios para getCachedGrossBillingsMany (testar path DB, não cache HTTP).
  </behavior>
  <action>
    1. Rodar `php artisan test --filter=PortfolioScore` e registrar baseline (commit chore se ainda não há testes).
    2. Criar `tests/Feature/Phase45/PortfolioScoreServiceMlCompatTest.php` com os 5 tests.
    3. Rode `php artisan test --filter=PortfolioScoreServiceMlCompatTest` → DEVE FALHAR NO MÍNIMO no Test 2 (empresa ML-only score=0 com PortfolioScoreService atual) — RED state.
    4. Commitar: `test(45-03): suite anti-regressão PortfolioScoreService ML compat (RED)`
  </action>
  <verify>
    <automated>php artisan test --filter=PortfolioScoreServiceMlCompatTest 2>&amp;1 | grep -E "FAILED|Tests:"</automated>
  </verify>
  <done>
    - `test -f tests/Feature/Phase45/PortfolioScoreServiceMlCompatTest.php` exit 0
    - `grep -c "public function test_" tests/Feature/Phase45/PortfolioScoreServiceMlCompatTest.php` >= 5
    - `php artisan test --filter=PortfolioScoreServiceMlCompatTest` exit code != 0 (RED — pelo menos Test 2 falha)
    - Commit RED presente em git log
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): Refatorar PortfolioScoreService para usar CompanyMetricsProviderFactory</name>
  <files>app/Services/PortfolioScoreService.php</files>
  <read_first>
    - app/Services/PortfolioScoreService.php (COMPLETO — ler AGORA antes de editar; confirmar exatamente quais linhas substituir)
    - tests/Feature/Phase45/PortfolioScoreServiceMlCompatTest.php (criado Tarefa 1 — o que deve passar)
    - app/Services/Performance/CompanyMetricsProviderFactory.php (criado 45-02 — o que injetamos)
    - app/Services/Sugadores/SugadoresAdsProviderFactory.php (padrão de refactor: constructor injection + loop por empresa)
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-01-PLAN.md (Task 2 desse plan — como o factory foi injetado no SugadorAnalysisService)
  </read_first>
  <behavior>
    **Modificações em PortfolioScoreService.php:**

    1. **Adicionar use statement:**
       `use App\Services\Performance\CompanyMetricsProviderFactory;`
       `use Illuminate\Support\Facades\Log;` (se não existir)

    2. **Modificar constructor (linha 43):**
    ```php
    public function __construct(
        private AdmanService $adman,
        private CompanyMetricsProviderFactory $metricsFactory,
    ) {}
    ```
    IMPORTANTE: manter `private AdmanService $adman` — ainda é usado em `getCachedGrossBillingsMany` dentro do `AdmanMetricsProvider` (injetado lá via DI separado) e pode existir outros usos no service (verificar).

    3. **Substituir bloco sumAtual + grossCache + loop (linhas ~88-112):**
    Remover: `$sumAtual = AdmanMetric::whereIn(...)...`, `$grossCache = $this->adman->getCachedGrossBillingsMany(...)`, e o `foreach` que popula `$revAtual`/`$revAnterior`.
    Substituir por loop factory conforme o snippet em `<interfaces>` desta plan.

    4. **Substituir sumHalf1 e sumHalf2 (linhas ~206-217):**
    Cada `AdmanMetric::whereIn('company_id', $companyIds)->whereBetween('reference_date', [$halfXFrom, $halfXTo])...` vira loop empresa-a-empresa chamando `$this->metricsFactory->for($c)->fetchScoreMetrics($c, $halfXFrom, $halfXTo)`. Agregar em Collection `->keyBy('company_id')` equivalente usando array acumulador com mesma shape (`rev`, `rev_prev`, `ads`).

    5. **Preservar tudo mais inalterado:**
       - `$eligiveis` filter, `$crescimentoAjustadoPct`, `$emQuedaIds`, `$recuperadasCount`, `scoreFinal()`, `classificar()`, `mediana()`, atingimento meta, qualidade NPS/meetings — NENHUMA mudança.
       - Categoria `execucao_ads` continua null (linha ~249) — não tocar.
       - Shape de retorno de `compute()` idêntico.

    **Decisão sobre sumHalf1/sumHalf2:**
    As queries das metades retornam a mesma shape (`rev`, `rev_prev`, `ads`) que o factory já fornece via `fetchScoreMetrics`. Criar `$sumHalf1Result[$c->id]` e `$sumHalf2Result[$c->id]` como arrays acumuladores, depois adaptar as linhas que leem `$sumHalf1->get($c->id)` para ler `$sumHalf1Result[$c->id]` com um objeto anônimo compatível OU refatorar os acessos para usar os arrays diretamente. Escolher a abordagem mais segura com menos mudança de linhas de lógica downstream.

    **Verificação de zero regressão:**
    `php artisan test --filter=Phase45` E `php artisan test --filter=Sugador` devem passar.
    `php artisan test --filter=Performance` (se existente) deve passar.

    **Registro de desvios (Rule 1):** se durante a implementação qualquer downstream do PortfolioScoreService aceder ao `$adman` diretamente (além do constructor), documentar em SUMMARY.md mas NÃO remover o campo `$adman` do constructor — preservar compat.
  </behavior>
  <action>
    1. Ler `app/Services/PortfolioScoreService.php` completo para confirmar linhas exatas.
    2. Adicionar `use` statements necessários.
    3. Modificar constructor para injetar `CompanyMetricsProviderFactory`.
    4. Substituir o bloco sumAtual+grossCache+loop (Tarefa identificada) pelo loop factory.
    5. Substituir sumHalf1+sumHalf2 da mesma forma.
    6. Confirmar via `git diff` que APENAS essas linhas mudaram e que `scoreFinal()`, `mediana()`, `classificar()`, e lógica de meta/NPS/meetings estão inalterados.
    7. Rode `php artisan test --filter=PortfolioScoreServiceMlCompatTest` → DEVE PASSAR (GREEN, todos os 5 tests).
    8. Rode regressão: `php artisan test --filter="Phase45|Sugador"` → todos verdes.
    9. Commitar: `refactor(45-03): PortfolioScoreService delega métricas por empresa ao CompanyMetricsProviderFactory (GREEN)`
  </action>
  <verify>
    <automated>php artisan test --filter=PortfolioScoreServiceMlCompatTest && php artisan test --filter=Phase45 && php artisan test --filter=Sugador 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `grep -c "CompanyMetricsProviderFactory" app/Services/PortfolioScoreService.php` >= 2 (use + property)
    - `grep "AdmanMetric::whereIn" app/Services/PortfolioScoreService.php | grep -v "#"` retorna vazio (queries diretas removidas; comentários antigos preservados se houver)
    - `grep "getCachedGrossBillingsMany" app/Services/PortfolioScoreService.php` retorna vazio (delegado ao AdmanMetricsProvider)
    - `grep "execucao_ads" app/Services/PortfolioScoreService.php` ainda retorna null (não regrediu §6.6)
    - `php artisan test --filter=PortfolioScoreServiceMlCompatTest` exit 0 (GREEN — 5 tests verdes)
    - `php artisan test --filter=Phase45` exit 0 (todos os tests Phase 45 verdes)
    - `php artisan test --filter=Sugador` exit 0 (zero regressão)
    - Commit GREEN presente em git log
  </done>
</task>

</tasks>

<verification>
- `php artisan test --filter="Phase45|Sugador"` exit 0 (sem regressão)
- `git diff --name-only HEAD~4 HEAD | grep -v "test\|planning\|SUMMARY"` mostra apenas: PerformanceController.php, PortfolioScoreService.php, Contracts/CompanyMetricsProvider.php, Services/Performance/*.php (nada mais)
- `grep "AdmanMetric::whereIn" app/Services/PortfolioScoreService.php | wc -l` = 0 (queries diretas removidas)
- `grep -c "metricsFactory" app/Services/PortfolioScoreService.php` >= 3 (constructor + chamadas)
- `grep "execucao_ads.*null" app/Services/PortfolioScoreService.php` retorna linha (categoria descontinuada preservada)
- `git log --oneline -4` mostra os 2 commits desta plan: BASELINE chore (ou skip) + RED test + GREEN refactor
</verification>

<success_criteria>
- `PortfolioScoreService::compute()` usa `CompanyMetricsProviderFactory` por empresa — sem chamadas diretas a `AdmanMetric::whereIn()` ou `getCachedGrossBillingsMany()`
- Empresa Adman-only → score idêntico (anti-regressão confirmado por Test 1)
- Empresa ML-only → score != 0 quando tem dados em `adman_metrics` (Test 2 verde)
- Empresa sem provider → compute não lança exceção; empresa excluída das elegíveis (Test 3 verde)
- Shape de retorno de `compute()` idêntico — sem breaking changes para DashboardController e PerformanceController
- 5 tests Feature Phase 45 PortfolioScore verdes
- Zero regressão na suite Sugador
- 2-3 commits com prefixo `(45-03)`: BASELINE (chore) + RED test + GREEN refactor
</success_criteria>

<output>
Criar `.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-03-SUMMARY.md` ao finalizar.
Próximo: Plan 45-04 (Wave 4 — UAT humano via gsd-verify-work na dashboard + página /performance).
</output>
