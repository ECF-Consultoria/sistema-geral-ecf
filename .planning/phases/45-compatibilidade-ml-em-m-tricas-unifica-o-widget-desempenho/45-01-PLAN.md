---
phase: 45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/PerformanceController.php
autonomous: true
requirements:
  - REQ-45-01
  - REQ-45-02
must_haves:
  truths:
    - "O filtro de users do PerformanceController::index() usa user_setores→cargos (slug ∈ ['analista','estrategista']), idêntico ao DashboardController::adminDashboard() (linhas 588-594)"
    - "O smoke via Tinker/Artisan contra produção confirma se company_id=298 (Bymobille) tem registros em adman_metrics — resultado documentado no SUMMARY.md desta plan"
    - "Nenhuma outra lógica do PerformanceController é alterada neste plan"
    - "Suite Performance (se existente) e suite Sugador continuam verdes — zero regressão"
  artifacts:
    - path: "app/Http/Controllers/PerformanceController.php"
      provides: "Filtro de users alinhado ao widget — cargo via user_setores, não role legacy"
      contains: "user_setores as us"
      min_lines: 100
    - path: ".planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md"
      provides: "Resultado do smoke Bymobille #298 — confirma presença/ausência em adman_metrics e destrava ou encerra plans 45-02/03"
  key_links:
    - from: "app/Http/Controllers/PerformanceController.php (linha ~39)"
      to: "app/Http/Controllers/DashboardController.php (linhas 588-594)"
      via: "filtro idêntico via user_setores→cargos"
      pattern: "whereExists → user_setores → cargos → slug ∈ ['analista','estrategista']"
---

<objective>
Wave 1: duas entregas atômicas de baixo risco.

**Tarefa 1 — Fix filtro users no PerformanceController:** o `index()` filtra hoje por `role ∈ ['consultor','mentor'] AND publication_role IS NULL`, enquanto o widget (`DashboardController`) usa `user_setores → cargos` com `slug ∈ ['analista','estrategista']`. Isso causa divergência de ranking entre a página e o widget. Corrigir `PerformanceController::index()` para usar o mesmo filtro canônico do widget.

**Tarefa 2 — Smoke Bymobille #298 em adman_metrics:** a causa raiz do bug ML é que `PortfolioScoreService` lê exclusivamente `adman_metrics`. Se Bymobille não tem dados lá, a empresa fica zerada. Confirmar via Tinker/Artisan em produção: `SELECT COUNT(*) FROM adman_metrics WHERE company_id = 298`. O resultado destrava (ou encerra prematuramente) as plans 45-02 e 45-03.

Se o smoke mostrar que Bymobille #298 JÁ tem dados em `adman_metrics` → Plans 45-02/03 viram "não necessárias" e Phase 45 fecha aqui com atualização de memória. Se NÃO tem dados → Plans 45-02/03 são necessárias (provider ML separado).

Wave 1 é autônoma — não depende de nenhuma plan anterior de Phase 45.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-CONTEXT.md
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-RESEARCH.md

<interfaces>
<!-- Filtro canônico do widget (DashboardController.php linhas 588-594): -->
```php
$perfMembrosQuery = User::where('active', true)
    ->whereExists(function ($q) {
        $q->from('user_setores as us')
          ->join('cargos as c', 'c.id', '=', 'us.cargo_id')
          ->whereColumn('us.user_id', 'users.id')
          ->whereIn('c.slug', ['analista', 'estrategista']);
    });
```

<!-- Filtro ATUAL (legado) do PerformanceController.php linhas 39-43: -->
```php
$users = User::where('active', true)
    ->whereIn('role', ['consultor', 'mentor'])
    ->whereNull('publication_role')
    ->get();
```

<!-- Lógica de cargo em PerformanceController (linhas 51-60) — JÁ CORRETA, não tocar: -->
```php
$cargosPorUser = DB::table('user_setores as us')
    ->join('cargos as c', 'c.id', '=', 'us.cargo_id')
    ->whereIn('c.slug', ['analista', 'estrategista'])
    ->select('us.user_id', 'c.slug')
    ->get()
    ->keyBy('user_id');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Corrigir filtro de users no PerformanceController::index()</name>
  <files>app/Http/Controllers/PerformanceController.php</files>
  <read_first>
    - app/Http/Controllers/PerformanceController.php (completo — ler ANTES de editar para entender o shape atual de $users e como é consumido)
    - app/Http/Controllers/DashboardController.php linhas 583-615 (filtro canônico que vamos espelhar)
  </read_first>
  <behavior>
    Substituir a query de $users (linhas 39-43 do PerformanceController::index()) pelo filtro via user_setores→cargos, idêntico ao widget do DashboardController.

    ANTES (legado):
    ```php
    $users = User::where('active', true)
        ->whereIn('role', ['consultor', 'mentor'])
        ->whereNull('publication_role')
        ->get();
    ```

    DEPOIS (canônico — espelha DashboardController:588-594):
    ```php
    // Fonte canônica: cargo via user_setores → cargos (desde quick 260610-f69).
    // Alinhado ao widget "Desempenho da equipe" do DashboardController (Phase 45 fix).
    $users = User::where('active', true)
        ->whereExists(function ($q) {
            $q->from('user_setores as us')
              ->join('cargos as c', 'c.id', '=', 'us.cargo_id')
              ->whereColumn('us.user_id', 'users.id')
              ->whereIn('c.slug', ['analista', 'estrategista']);
        })
        ->get(['id', 'name', 'role']);
    ```

    A lógica de $cargosPorUser (já presente nas linhas 51-60) permanece inalterada — ela continua sendo a fonte de cargo_slug para enriquecer o ranking.

    Nenhuma outra linha do método index() é alterada.
    O método indexPolos() (linha 111+) e o método show() (linha 232+) NÃO são tocados.
  </behavior>
  <action>
    1. Ler `app/Http/Controllers/PerformanceController.php` completo para localizar as linhas exatas.
    2. Substituir o bloco `$users = User::where('active', true)` (o query legado com `whereIn('role', ...)` + `whereNull('publication_role')`) pelo novo bloco usando `whereExists` com join user_setores→cargos, conforme especificado em <behavior>.
    3. Adicionar comentário pt-BR explicando a mudança: `// Fonte canônica: cargo via user_setores → cargos (alinhado ao widget Dashboard — Phase 45 fix).`
    4. Confirmar que nada além dessas linhas foi alterado via `git diff app/Http/Controllers/PerformanceController.php`.
    5. Commitar: `fix(45-01): alinha filtro de users /performance ao widget Dashboard via user_setores→cargos`
  </action>
  <verify>
    <automated>git diff HEAD app/Http/Controllers/PerformanceController.php | grep "whereExists\|user_setores\|whereIn.*slug"</automated>
    <automated>php artisan test --filter=Performance 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `grep -c "user_setores as us" app/Http/Controllers/PerformanceController.php` retorna >= 1
    - `grep "whereIn.*role.*consultor" app/Http/Controllers/PerformanceController.php` retorna vazio (legado removido do index())
    - Commit `fix(45-01): ...` presente em `git log --oneline -3`
    - php artisan test --filter=Performance exits 0 (ou não tem testes e exit 0 vazio)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Smoke Bymobille #298 em adman_metrics (decisão-gate para plans 45-02/03)</name>
  <files>.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md</files>
  <read_first>
    - .planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-RESEARCH.md (§4 — chain User→companies→métricas, gap concreto Bymobille)
    - app/Models/Company.php (accessor cust_id, isMlDriven, mlToken relation — verificar se company_id=298 tem adman_account_id ou apenas ml_store_id)
  </read_first>
  <behavior>
    Executar consultas de diagnóstico em produção via `php artisan tinker` (ou via `php artisan db:query` se disponível):

    **Consulta A — Dados em adman_metrics para Bymobille:**
    ```sql
    SELECT COUNT(*) as total, MIN(reference_date) as desde, MAX(reference_date) as ate
    FROM adman_metrics
    WHERE company_id = 298;
    ```

    **Consulta B — company_id=298 identifiers:**
    ```sql
    SELECT id, name, adman_account_id, ml_store_id
    FROM companies
    WHERE id = 298;
    ```

    **Consulta C — mlToken status:**
    ```sql
    SELECT status, expires_at
    FROM ml_tokens
    WHERE company_id = 298
    ORDER BY id DESC
    LIMIT 1;
    ```

    **Interpretação dos resultados:**
    - SE `total > 0` em adman_metrics AND dados são recentes (últimos 30d) → Bymobille JÁ tem dados; Plans 45-02/03 NÃO são necessárias. Problema de score pode ser outro (ex: cust_id mismatch no cache Adman).
    - SE `total = 0` OU dados muito antigos (> 60d) → Bymobille sem cobertura em adman_metrics; Plans 45-02/03 NECESSÁRIAS (MlMetricsProvider).
    - SE company_id=298 não existe → documentar e reavaliar.

    Documentar resultado em `.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md`.
  </behavior>
  <action>
    1. Via Tinker ou DB query, rodar as 3 consultas acima em produção (ou em dev conectado ao DB de prod se disponível).
       Alternativa para ambiente dev: `php artisan tinker --execute "DB::select('SELECT COUNT(*) as total, MIN(reference_date) as desde, MAX(reference_date) as ate FROM adman_metrics WHERE company_id = 298')"`
       E: `php artisan tinker --execute "DB::select('SELECT id, name, adman_account_id, ml_store_id FROM companies WHERE id = 298')"`
       E: `php artisan tinker --execute "DB::select('SELECT status FROM ml_tokens WHERE company_id = 298 ORDER BY id DESC LIMIT 1')"`

    2. Criar `.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md` com estrutura:

    ```markdown
    # Smoke Bymobille #298 — adman_metrics (Phase 45-01)

    **Data:** YYYY-MM-DD
    **Objetivo:** Confirmar se company_id=298 tem dados em adman_metrics para determinar se Plans 45-02/03 são necessárias.

    ## Resultados

    ### A — adman_metrics WHERE company_id=298
    | total | desde | até |
    |-------|-------|-----|
    | XXX   | YYYY-MM-DD | YYYY-MM-DD |

    ### B — companies WHERE id=298
    | id | name | adman_account_id | ml_store_id |
    |----|------|-----------------|-------------|
    | 298 | Bymobille | NULL / VALOR | 298 / VALOR |

    ### C — ml_tokens WHERE company_id=298
    | status | expires_at |
    |--------|-----------|
    | active | YYYY-MM-DD |

    ## Decisão

    **Plans 45-02/03:** [NECESSÁRIAS / NÃO NECESSÁRIAS]

    **Justificativa:** [adman_metrics total=0 → provider ML necessário] OU [adman_metrics total=N com dados recentes → bug pode ser no cust_id do cache Adman, não em ausência de dados]

    ## Próximo passo
    - SE necessárias: executar `gsd-execute-phase 45` para Plans 45-02 e 45-03
    - SE não necessárias: Phase 45 fecha aqui; atualizar memory project_adman_data_sources.md
    ```

    3. Commitar o arquivo de smoke: `docs(45-01): smoke Bymobille #298 em adman_metrics — [RESULTADO: X registros / DECISÃO: plans 45-02/03 necessárias|não necessárias]`
  </action>
  <verify>
    <automated>test -f .planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md</automated>
    <automated>grep -c "Decisão\|Plans 45-02/03" .planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md</automated>
  </verify>
  <done>
    - Arquivo 45-01-BYMOBILLE-SMOKE.md existe com resultados das 3 consultas preenchidos
    - Campo "Decisão" diz explicitamente "NECESSÁRIAS" ou "NÃO NECESSÁRIAS"
    - Commit `docs(45-01): smoke Bymobille...` presente em git log
  </done>
</task>

</tasks>

<verification>
- `grep -c "user_setores as us" app/Http/Controllers/PerformanceController.php` retorna >= 1 (novo filtro canônico presente)
- `grep "whereIn.*role.*consultor" app/Http/Controllers/PerformanceController.php` retorna vazio (filtro legado removido do method index())
- `test -f .planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md` exit 0
- `grep "NECESSÁRIAS\|NÃO NECESSÁRIAS" .planning/phases/45-*/45-01-BYMOBILLE-SMOKE.md` retorna 1 linha (decisão documentada)
- `php artisan test --filter=Performance` exit 0 (ou vazio)
- `git log --oneline -5` mostra 2 commits novos: fix filtro + docs smoke
</verification>

<success_criteria>
- Divergência de "quem aparece no ranking" entre widget e página `/performance` resolvida: ambos usam mesmo filtro `user_setores → cargos`.
- Estado real de Bymobille #298 em `adman_metrics` documentado com decisão explícita sobre plans 45-02/03.
- Zero regressão em testes existentes.
- 2 commits atômicos com prefixo `(45-01)`.
</success_criteria>

<output>
Criar `.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-SUMMARY.md` ao finalizar.
Próximo: se smoke Tarefa 2 → "NECESSÁRIAS" → executar Plan 45-02; se "NÃO NECESSÁRIAS" → Phase 45 encerra aqui, atualizar STATE.md e memory.
</output>
