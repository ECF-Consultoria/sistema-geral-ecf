---
phase: 45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
conditional: "Executar APENAS se 45-01-BYMOBILLE-SMOKE.md contiver 'NECESSÁRIAS' na seção Decisão. Se 'NÃO NECESSÁRIAS', esta plan é DEFERRED."
files_modified:
  - app/Contracts/CompanyMetricsProvider.php
  - app/Services/Performance/AdmanMetricsProvider.php
  - app/Services/Performance/MlMetricsProvider.php
  - app/Services/Performance/CompanyMetricsProviderFactory.php
  - tests/Feature/Phase45/CompanyMetricsProviderTest.php
autonomous: true
requirements:
  - REQ-45-03
  - REQ-45-04
  - REQ-45-05
must_haves:
  truths:
    - "Interface CompanyMetricsProvider existe em app/Contracts/ com 3 métodos: supports(), name(), fetchScoreMetrics()"
    - "AdmanMetricsProvider::supports() → !empty(adman_account_id); MlMetricsProvider::supports() → optional(mlToken)->status === 'active'"
    - "Factory prefere ML sobre Adman quando ambos suportam — idêntico à regra do SugadoresAdsProviderFactory pós Phase 42"
    - "Factory lança RuntimeException quando nenhum provider suporta a empresa — com mensagem clara em pt-BR"
    - "Tests Feature Phase 45 via SQLite in-memory: empresa Adman-only → AdmanMetricsProvider; empresa ML-only → MlMetricsProvider; empresa híbrida → ML preferido; empresa sem nenhum → RuntimeException"
    - "Zero modificação em PortfolioScoreService, AdmanService, Company, AdmanMetric neste plan"
  artifacts:
    - path: "app/Contracts/CompanyMetricsProvider.php"
      provides: "Interface PHP com supports(), name(), fetchScoreMetrics() — análogo a SugadoresAdsProvider"
      contains: "interface CompanyMetricsProvider"
      min_lines: 40
    - path: "app/Services/Performance/AdmanMetricsProvider.php"
      provides: "Implementação Adman: lê adman_metrics + getCachedGrossBillingsMany"
      contains: "implements CompanyMetricsProvider"
      min_lines: 60
    - path: "app/Services/Performance/MlMetricsProvider.php"
      provides: "Implementação ML: lê adman_metrics sem cache Adman (empresa ML-only)"
      contains: "implements CompanyMetricsProvider"
      min_lines: 50
    - path: "app/Services/Performance/CompanyMetricsProviderFactory.php"
      provides: "Factory: ML preferido → Adman fallback → RuntimeException"
      contains: "class CompanyMetricsProviderFactory"
      min_lines: 40
    - path: "tests/Feature/Phase45/CompanyMetricsProviderTest.php"
      provides: "Suite TDD: 8+ tests cobrindo supports(), factory resolution, fetchScoreMetrics() retorno"
      min_lines: 120
  key_links:
    - from: "app/Services/Performance/AdmanMetricsProvider.php"
      to: "app/Services/AdmanService.php"
      via: "constructor injection (composição — getCachedGrossBillingsMany)"
    - from: "app/Services/Performance/MlMetricsProvider.php"
      to: "app/Models/AdmanMetric.php"
      via: "query direta (lê adman_metrics sem cache Adman)"
    - from: "app/Services/Performance/CompanyMetricsProviderFactory.php"
      to: "app/Contracts/CompanyMetricsProvider.php"
      via: "return type do método for()"
    - from: "app/Services/Performance/CompanyMetricsProviderFactory.php"
      to: "app/Services/Sugadores/SugadoresAdsProviderFactory.php"
      via: "espelha lógica de resolução (ML preferido)"
---

<objective>
Wave 2 (condicional — executar apenas se smoke 45-01 confirmar "NECESSÁRIAS"):

Criar o contract `CompanyMetricsProvider` e suas 2 implementações (`AdmanMetricsProvider`, `MlMetricsProvider`) + factory de resolução (`CompanyMetricsProviderFactory`), espelhando o pattern v11.0 Sugadores (Phase 39).

O objective desta plan é construir a camada de abstração de métricas isolada e testada. A integração com `PortfolioScoreService` (que substitui as chamadas diretas a `AdmanMetric::whereIn()`) é a Plan 45-03 — propositalmente separada para que o contract seja estável e testado antes do refactor do service central.

Pattern a espelhar: `app/Services/Sugadores/SugadoresAdsProviderFactory.php` (já existe — lógica de resolução idêntica com ML preferido). Interface análoga a `app/Contracts/SugadoresAdsProvider.php`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-CONTEXT.md
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-RESEARCH.md
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md

<interfaces>
<!-- Contract proposto (§4 RESEARCH): -->
```php
interface CompanyMetricsProvider {
    public function supports(Company $company): bool;
    public function name(): string;
    /**
     * Retorna revenue atual, revenue anterior e ad_spend para a empresa
     * no período informado.
     * @return array{rev: float, rev_prev: float, ads: float}
     */
    public function fetchScoreMetrics(Company $company, string $from, string $to): array;
}
```

<!-- Shape que PortfolioScoreService espera (linhas 88-111 de PortfolioScoreService.php): -->
```php
// Para cada empresa, o service precisa de:
$revAtual[$c->id]    = $cacheRev ?? (float) ($row->rev ?? 0);
$revAnterior[$c->id] = (float) ($row->rev_prev ?? 0);
// Além de rev/rev_prev, sumHalf1 e sumHalf2 também leem ads (ad_spend) das metades
// Logo o contrato fetchScoreMetrics precisa incluir ads também:
// @return array{rev: float, rev_prev: float, ads: float}
```

<!-- Pattern molde (SugadoresAdsProviderFactory.php linha 59-71): -->
```php
// Regra pós Phase 42: ML preferido quando empresa tem mlToken active
if ($this->mlProvider->supports($company)) {
    return $this->mlProvider;
}
if ($this->admanProvider->supports($company)) {
    return $this->admanProvider;
}
throw new \RuntimeException("Empresa {$company->id} sem provider compatível ...");
```

<!-- AdmanMetric query atual no PortfolioScoreService (linhas 88-93): -->
```php
$sumAtual = AdmanMetric::whereIn('company_id', $companyIds)
    ->whereBetween('reference_date', [$atualFrom, $atualTo])
    ->selectRaw('company_id, SUM(revenue) as rev, SUM(revenue_prev_period) as rev_prev, SUM(ad_spend) as ads')
    ->groupBy('company_id')
    ->get()
    ->keyBy('company_id');
```

<!-- MlToken check (espelha MercadoLivreSugadoresProvider::supports()): -->
```php
// MercadoLivreSugadoresProvider::supports() Phase 39-02:
return optional($company->mlToken)->status === 'active';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): Criar contract + tests Feature failing</name>
  <files>
    app/Contracts/CompanyMetricsProvider.php,
    tests/Feature/Phase45/CompanyMetricsProviderTest.php
  </files>
  <read_first>
    - app/Contracts/SugadoresAdsProvider.php (molde — estrutura de interface a espelhar)
    - app/Services/Sugadores/SugadoresAdsProviderFactory.php (lógica de resolução ML→Adman a copiar)
    - app/Services/Sugadores/AdmanSugadoresProvider.php (padrão de implements + supports())
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php (padrão de supports() ML: `optional($company->mlToken)->status === 'active'`)
    - app/Services/PortfolioScoreService.php linhas 88-111 (shape de dados que o refactor vai consumir — já contextualizado acima)
    - app/Models/Company.php (accessors: adman_account_id, ml_store_id, cust_id, isMlDriven, mlToken relation)
    - app/Models/MlToken.php (fields: company_id, status, expires_at — confirmar estrutura)
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-01-PLAN.md (referência de estilo dos tests Unit Phase 39)
  </read_first>
  <behavior>
    **Contract `CompanyMetricsProvider` (interface PHP):**
    - Namespace: `App\Contracts`
    - Métodos:
      - `public function supports(Company $company): bool;`
      - `public function name(): string;`
      - `public function fetchScoreMetrics(Company $company, string $from, string $to): array;`
        - `$from` e `$to` em formato `YYYY-MM-DD`
        - Retorna `array{rev: float, rev_prev: float, ads: float}`
        - PHPDoc descreve cada chave: `rev` = revenue atual no período, `rev_prev` = revenue do período anterior (via `revenue_prev_period` no DB ou cálculo alternativo), `ads` = investimento em anúncios
    - PHPDoc da classe em pt-BR: propósito, Phase 45, espelha SugadoresAdsProvider, 2 implementações atuais

    **Tests Feature Phase 45 (SQLite in-memory, RefreshDatabase):**
    Cobrir (8 tests mínimo):
    1. `test_adman_provider_supports_company_with_adman_account_id` — company com adman_account_id='123' → true
    2. `test_adman_provider_does_not_support_company_without_adman_account_id` — company sem adman_account_id → false
    3. `test_ml_provider_supports_company_with_active_ml_token` — company com mlToken status='active' → true
    4. `test_ml_provider_does_not_support_company_without_active_ml_token` — company sem mlToken ou status='expired' → false
    5. `test_factory_returns_ml_provider_for_ml_only_company` — company com mlToken active e sem adman_account_id → factory retorna instância MlMetricsProvider
    6. `test_factory_returns_adman_provider_for_adman_only_company` — company com adman_account_id e sem mlToken → AdmanMetricsProvider
    7. `test_factory_prefers_ml_when_company_has_both` — company com adman_account_id AND mlToken active → MlMetricsProvider (ML preferido)
    8. `test_factory_throws_when_no_provider_supports` — company sem adman_account_id e sem mlToken → RuntimeException
    9. `test_adman_provider_fetch_score_metrics_returns_correct_shape` — com dados em adman_metrics: retorna array com chaves rev, rev_prev, ads como floats
    10. `test_ml_provider_fetch_score_metrics_returns_correct_shape` — empresa ML-only com dados em adman_metrics: retorna array com chaves rev, rev_prev, ads (ads=0.0 pois ML-only não tem ad_spend via Adman)

    Usar SQLite in-memory (RefreshDatabase). NÃO usar Mockery pois os providers fazem queries DB — criar fixtures via factories/seeds inline.
    Nota: AdmanMetricsProvider também chama `$this->adman->getCachedGrossBillingsMany(...)` — no teste mockar via `$this->mock(AdmanService::class)` ou, se complicado, testar apenas o path DB (sem cache Adman) — documentar no SUMMARY.md.
  </behavior>
  <action>
    1. Criar `app/Contracts/CompanyMetricsProvider.php` com interface conforme especificado.
    2. Criar `tests/Feature/Phase45/CompanyMetricsProviderTest.php` com os 10 tests (classes providers/factory ainda não existem — tests devem falhar com "Class not found" ou similar).
    3. Rode `php artisan test --filter=CompanyMetricsProviderTest` → DEVE FALHAR (RED).
    4. Commitar: `test(45-02): suite CompanyMetricsProvider — contract + factory resolution + fetch shape (RED)`
  </action>
  <verify>
    <automated>php artisan test --filter=CompanyMetricsProviderTest 2>&amp;1 | grep -E "FAILED|Error|not found"</automated>
  </verify>
  <done>
    - `test -f app/Contracts/CompanyMetricsProvider.php` exit 0
    - `grep -c "interface CompanyMetricsProvider" app/Contracts/CompanyMetricsProvider.php` = 1
    - `grep -c "function supports\|function name\|function fetchScoreMetrics" app/Contracts/CompanyMetricsProvider.php` = 3
    - `test -f tests/Feature/Phase45/CompanyMetricsProviderTest.php` exit 0
    - `php artisan test --filter=CompanyMetricsProviderTest` exit code != 0 (RED — implementações ainda não existem)
    - Commit RED presente em git log
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): Implementar AdmanMetricsProvider + MlMetricsProvider + Factory</name>
  <files>
    app/Services/Performance/AdmanMetricsProvider.php,
    app/Services/Performance/MlMetricsProvider.php,
    app/Services/Performance/CompanyMetricsProviderFactory.php
  </files>
  <read_first>
    - app/Contracts/CompanyMetricsProvider.php (criado na Tarefa 1 — interface a implementar)
    - tests/Feature/Phase45/CompanyMetricsProviderTest.php (criado na Tarefa 1 — tests a satisfazer)
    - app/Services/Sugadores/AdmanSugadoresProvider.php (molde de implementação Adman)
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php (molde de suports() ML)
    - app/Services/Sugadores/SugadoresAdsProviderFactory.php (molde de factory)
    - app/Services/PortfolioScoreService.php linhas 88-112 (lógica atual que vamos encapsular — ler ANTES para entender exatamente as queries + grossCache)
    - app/Services/AdmanService.php (confirmar assinatura de getCachedGrossBillingsMany)
    - app/Models/AdmanMetric.php (campos: company_id, reference_date, revenue, revenue_prev_period, ad_spend)
    - app/Models/MlToken.php (campos: company_id, status)
  </read_first>
  <behavior>
    **AdmanMetricsProvider** (`app/Services/Performance/AdmanMetricsProvider.php`):
    - Namespace: `App\Services\Performance`
    - `implements App\Contracts\CompanyMetricsProvider`
    - Constructor: `public function __construct(private AdmanService $adman) {}`
    - `supports(Company $c): bool` → `return !empty($c->adman_account_id);`
    - `name(): string` → `return 'adman';`
    - `fetchScoreMetrics(Company $c, string $from, string $to): array`:
      - Query `AdmanMetric::where('company_id', $c->id)->whereBetween('reference_date', [$from, $to])->selectRaw('SUM(revenue) as rev, SUM(revenue_prev_period) as rev_prev, SUM(ad_spend) as ads')->first()`
      - Tenta cache Adman: `$this->adman->getCachedGrossBillingsMany([$c->cust_id], $from, $to)` → se retornar valor não-null para `$c->cust_id`, usa como `rev` (mesma lógica do PortfolioScoreService linhas 107-110)
      - Retorna `['rev' => float, 'rev_prev' => float, 'ads' => float]` — zeros quando sem dados
    - PHPDoc pt-BR explicando que encapsula a lógica de PortfolioScoreService::linhas 88-111 para a empresa individual

    **MlMetricsProvider** (`app/Services/Performance/MlMetricsProvider.php`):
    - Namespace: `App\Services\Performance`
    - `implements App\Contracts\CompanyMetricsProvider`
    - Constructor: `public function __construct() {}` (sem AdmanService — não usa cache Adman para empresas ML-only)
    - `supports(Company $c): bool` → `return optional($c->mlToken)->status === 'active';`
    - `name(): string` → `return 'ml';`
    - `fetchScoreMetrics(Company $c, string $from, string $to): array`:
      - Mesma query DB: `AdmanMetric::where('company_id', $c->id)->whereBetween('reference_date', [$from, $to])->selectRaw('SUM(revenue) as rev, SUM(revenue_prev_period) as rev_prev, SUM(ad_spend) as ads')->first()`
      - SEM cache Adman (empresa ML-only não tem adman_account_id válido; evita chamada de API que retornaria erro/422)
      - `ads` pode ser 0.0 se empresa ML-only não tem ad_spend via Adman
      - Se `rev = 0` E `rev_prev = 0`: não indica erro — empresa pode simplesmente não ter dados históricos ainda (logar warning estruturado: `Log::warning('[Performance] MlMetricsProvider: empresa {id} sem dados em adman_metrics', [...])`)
      - Retorna `['rev' => float, 'rev_prev' => float, 'ads' => float]` — zeros quando sem dados
    - PHPDoc pt-BR: empresa ML-only (sem adman_account_id); NÃO chama AdmanService (evita 422)

    **CompanyMetricsProviderFactory** (`app/Services/Performance/CompanyMetricsProviderFactory.php`):
    - Namespace: `App\Services\Performance`
    - `use App\Contracts\CompanyMetricsProvider; use App\Models\Company;`
    - Constructor: `public function __construct(private AdmanMetricsProvider $admanProvider, private MlMetricsProvider $mlProvider) {}`
    - `public function for(Company $company): CompanyMetricsProvider`:
      - Primeiro tenta ML: `if ($this->mlProvider->supports($company)) return $this->mlProvider;`
      - Depois Adman: `if ($this->admanProvider->supports($company)) return $this->admanProvider;`
      - Fallback: `throw new \RuntimeException("Empresa {$company->id} ({$company->name}) sem provider de métricas compatível (sem adman_account_id e sem mlToken ativo).");`
    - PHPDoc pt-BR: espelha SugadoresAdsProviderFactory; ML preferido (alinhado ao cut-over Phase 42); Adman é fallback

    **IMPORTANTE — Pitfall §6.5 do RESEARCH:**
    AdmanMetricsProvider NUNCA deve chamar `getCachedGrossBillingsMany` para empresas que têm `adman_account_id = null`. O `supports()` já garante isso (retorna false quando sem adman_account_id), então este case nunca chega ao AdmanMetricsProvider.
  </behavior>
  <action>
    1. Criar diretório `app/Services/Performance/` (se não existir).
    2. Criar `app/Services/Performance/AdmanMetricsProvider.php` conforme <behavior>.
    3. Criar `app/Services/Performance/MlMetricsProvider.php` conforme <behavior>.
    4. Criar `app/Services/Performance/CompanyMetricsProviderFactory.php` conforme <behavior>.
    5. Rode `php artisan test --filter=CompanyMetricsProviderTest` → DEVE PASSAR (GREEN, todos os 10 tests).
    6. Rode regressão: `php artisan test --filter=Sugador` → deve continuar verde (zero regressão).
    7. Commitar: `feat(45-02): CompanyMetricsProvider contract + AdmanMetricsProvider + MlMetricsProvider + factory (GREEN)`
  </action>
  <verify>
    <automated>php artisan test --filter=CompanyMetricsProviderTest && php artisan test --filter=Sugador 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `test -f app/Services/Performance/AdmanMetricsProvider.php` exit 0
    - `test -f app/Services/Performance/MlMetricsProvider.php` exit 0
    - `test -f app/Services/Performance/CompanyMetricsProviderFactory.php` exit 0
    - `grep -c "implements CompanyMetricsProvider" app/Services/Performance/AdmanMetricsProvider.php` = 1
    - `grep -c "implements CompanyMetricsProvider" app/Services/Performance/MlMetricsProvider.php` = 1
    - `grep "getCachedGrossBillingsMany" app/Services/Performance/MlMetricsProvider.php` retorna vazio (ML provider NÃO usa cache Adman)
    - `php artisan test --filter=CompanyMetricsProviderTest` exit 0 (GREEN — 10+ tests verdes)
    - `php artisan test --filter=Sugador` exit 0 (zero regressão)
    - Commit GREEN presente em git log
  </done>
</task>

</tasks>

<verification>
- `php artisan test --filter=Phase45` exit 0 (suite completa verde)
- `php artisan test --filter=Sugador` exit 0 (zero regressão no módulo vizinho)
- `ls app/Services/Performance/` lista 3 arquivos: AdmanMetricsProvider.php, MlMetricsProvider.php, CompanyMetricsProviderFactory.php
- `ls app/Contracts/CompanyMetricsProvider.php` existe
- `grep "getCachedGrossBillingsMany" app/Services/Performance/MlMetricsProvider.php` vazio (pitfall §6.5 garantido)
- `git diff --name-only HEAD~2 app/Services/PortfolioScoreService.php` vazio (service NÃO modificado nesta plan)
- `git log --oneline -5` mostra 2 commits: RED test + GREEN feat
</verification>

<success_criteria>
- Contract `CompanyMetricsProvider` declarado com 3 métodos + PHPDoc em pt-BR
- `AdmanMetricsProvider` e `MlMetricsProvider` implementam o contract sem modificar AdmanService, AdmanMetric, ou PortfolioScoreService
- Factory resolve ML→Adman→exception seguindo o mesmo padrão do SugadoresAdsProviderFactory
- 10+ tests Feature Phase 45 verdes via SQLite in-memory
- Zero regressão na suite Sugador
- 2 commits: RED + GREEN com prefixo `(45-02)`
</success_criteria>

<output>
Criar `.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-02-SUMMARY.md` ao finalizar.
Próximo: Plan 45-03 (Wave 3 — refactor PortfolioScoreService para usar o factory por empresa, substituindo AdmanMetric::whereIn() e getCachedGrossBillingsMany() pelos providers).
</output>
