---
phase: 45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho
plan: 04
type: execute
wave: 4
depends_on:
  - 45-01
autonomous: false
requirements:
  - REQ-45-08
must_haves:
  truths:
    - "UAT humano confirma que widget Dashboard e página /performance mostram o mesmo ranking (mesmos usuários na mesma ordem)"
    - "Se smoke 45-01 retornou 'NECESSÁRIAS' e Plans 45-02/03 foram executadas: Bymobille #298 aparece no score de quem a gerencia"
    - "Se smoke 45-01 retornou 'NÃO NECESSÁRIAS': UAT confirma que o bug de ranking era apenas de filtro de users (resolvido pela Tarefa 1 do Plan 45-01)"
  artifacts:
    - path: ".planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-04-UAT.md"
      provides: "Checklist de UAT preenchido com resultados reais do operador"
---

<objective>
Wave 4 — UAT humano (não autônomo): checkpoint de validação visual na aplicação real em produção.

Este plan SEMPRE executa, independente do caminho tomado pelo smoke da Plan 45-01:
- **Caminho A (smoke: "NECESSÁRIAS" + Plans 45-02/03 executadas):** confirmar que Bymobille #298 aparece no score de quem a gerencia E que o ranking é idêntico entre widget e página.
- **Caminho B (smoke: "NÃO NECESSÁRIAS"):** confirmar apenas que o fix de filtro de users (Plan 45-01 Tarefa 1) alinhou os dois rankings.

Após deploy para produção (por `/gsd-execute-phase 45` + deploy autorizado), o operador navega para Dashboard admin e para `/performance` e confirma os itens abaixo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-CONTEXT.md
@.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md
</context>

<tasks>

<task type="human" tdd="false">
  <name>Tarefa 1: Deploy + checklist UAT humano na aplicação em produção</name>
  <files>.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-04-UAT.md</files>
  <read_first>
    - .planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-01-BYMOBILLE-SMOKE.md (decisão do smoke — determina Caminho A vs B)
    - app/Http/Controllers/PerformanceController.php (confirmar que o fix de filtro está em prod)
  </read_first>
  <behavior>
    **PASSO 0 — Deploy (se não feito ainda):**
    - Rodar `npm run build` local (sempre necessário para Inertia/React)
    - Deployar para VPS (conforme autorização permanente de deploy — memory `feedback_autorizacao_permanente_deploy`)
    - Confirmar que `php artisan config:cache && php artisan route:cache` foram rodados no VPS

    **PASSO 1 — Checklist UAT (operador humano):**

    O operador acessa `https://admin.ecfconsultoria.com.br` com conta admin e percorre:

    **A. Convergência widget ↔ página:**
    - [ ] Abrir Dashboard admin → anotar os 3-5 primeiros nomes no widget "Desempenho da equipe"
    - [ ] Navegar para `/performance` → verificar que os mesmos nomes aparecem no ranking (pode haver diferença de posição por ordenação, mas as PESSOAS devem ser as mesmas)
    - [ ] Se algum nome aparece em um e não no outro → documentar como bug residual

    **B. Ordenação (pitfall §3 RESEARCH):**
    - [ ] Widget ordena por `score DESC` (simples)
    - [ ] Página `/performance` ordena por `tem_base_comparativa DESC * 1000 + score` (usuários com base comparativa insuficiente vão pro fim)
    - [ ] Confirmar que a divergência de ordenação (quando existe) é apenas para usuários com `<5 empresas elegíveis` — comportamento esperado, não é bug

    **C. Bymobille (se Caminho A — smoke "NECESSÁRIAS"):**
    - [ ] Identificar qual analista/estrategista gerencia Bymobille #298 via `/companies/298` → aba de atribuição
    - [ ] Abrir `/performance/{user_id}` desse profissional → confirmar que Bymobille aparece na seção de empresas da carteira COM faturamento > 0
    - [ ] Score do profissional deve ser maior que 0 (anteriormente ficava zerado pela Bymobille sem dados Adman)

    **D. Sem regressão visual:**
    - [ ] Clicar em pelo menos 2 profissionais via `/performance/{id}` → confirmar que o drilldown carrega sem erro 500
    - [ ] Confirmar que a categoria "Execução de Ads" aparece vazia/null (não algum valor incorreto — categoria descontinuada, deve continuar oculta)

    **PASSO 2 — Documentar resultado:**
    Criar `.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-04-UAT.md` com checklist preenchido + prints/observações.

    **PASSO 3 — Commitar:**
    `docs(45-04): UAT Phase 45 — widget/performance convergem [APROVADO/REPROVADO]`
  </behavior>
  <action>
    Execute o UAT acima em produção (ou staging conectado ao DB de prod).
    Se algum item do checklist falhar → abrir bug como TODO em `.planning/todos/pending/` e NÃO marcar UAT como APROVADO.
    Se todos os itens passarem → marcar como APROVADO e commitar o UAT.md.
  </action>
  <verify>
    <manual>Checklist UAT com todos os itens [x] (marcados como OK) ou [!] (com explicação de divergência esperada)</manual>
  </verify>
  <done>
    - `test -f .planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-04-UAT.md` exit 0
    - O arquivo contém `APROVADO` ou `REPROVADO` com justificativas
    - Commit `docs(45-04): ...` presente em git log
  </done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/45-*/45-04-UAT.md` exit 0
- Arquivo UAT contém decisão explícita (APROVADO/REPROVADO)
- Se APROVADO: Phase 45 pode ser marcada como completa via `/gsd-progress`
- Se REPROVADO: itens de reprovação viram TODOs pendentes antes de fechar a phase
</verification>

<success_criteria>
- UAT humano confirma convergência widget ↔ página `/performance` (mesmos usuários)
- Se Caminho A: Bymobille aparece com faturamento > 0 no drilldown do seu analista
- Nenhum erro 500 nos drilldowns de `/performance/{id}`
- Phase 45 aprovada para fechar
</success_criteria>

<output>
Criar `.planning/phases/45-compatibilidade-ml-em-m-tricas-unifica-o-widget-desempenho/45-04-SUMMARY.md` ao finalizar.
Após UAT aprovado: rodar `/gsd-progress` para fechar Phase 45 e avançar para Phase 46 (histórico longitudinal de scores).
</output>
