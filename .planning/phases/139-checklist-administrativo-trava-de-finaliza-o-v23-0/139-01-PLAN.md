---
phase: 139-checklist-administrativo-trava-de-finaliza-o-v23-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-BASELINE-TESTES.md
  - config/services.php
  - .env.example
  - tests/Unit/Phase139/AdmanRegisterUrlConfigTest.php
  - .planning/REQUIREMENTS-v23.md
autonomous: true
requirements: [ADMIN-02, ADMIN-03]
user_setup: []

must_haves:
  truths:
    - "D-04: `config('services.adman.register_url')` devolve o link fixo de cadastro do Adman (`https://app.ad-man.io/register?ref=588D0DD78C4F`) sem nenhum parâmetro por empresa, e trocar o `ref` em produção é mudança de `.env` — nunca mudança de código"
    - "D-04: a grafia correta é **Adman** (nunca ADMA) em toda chave, comentário e rótulo criado nesta fase"
    - "D-19: `.planning/REQUIREMENTS-v23.md` registra, junto ao ADMIN-02, as duas exceções explícitas: D-06 (\"Contrato revisado\" é marcação manual com autoria) e D-16 (\"Contrato assinado\" também fecha por `ContratoLiberacao`)"
    - "D-06: a exceção de marcação manual do item 1 está escrita no REQUIREMENTS-v23.md, não implícita no código"
    - "D-16: a segunda fonte de fechamento do item 3 está escrita no REQUIREMENTS-v23.md, não implícita no código"
    - "D-04: `.planning/REQUIREMENTS-v23.md` registra, junto ao ADMIN-03, que o nome é **Adman** (não ADMA) e que o item do link Adman é **manual** — link fixo de cadastro, não gerado por empresa. Só os itens 7 (OAuth ML, D-05) e 8 (conexão ECF, D-14) são gerados/auto-marcados pelo checklist"
    - "Existe `139-BASELINE-TESTES.md` com a saída real da suíte 137/138 capturada ANTES da primeira linha de código desta fase"
  artifacts:
    - path: ".planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-BASELINE-TESTES.md"
      provides: "Baseline verde/vermelho de Phase137+Phase138 antes do código da 139"
      contains: "tests/Unit/Phase137"
    - path: "config/services.php"
      provides: "Chave adman.register_url"
      contains: "register_url"
    - path: ".env.example"
      provides: "Documentação das 3 chaves ADMAN_*"
      contains: "ADMAN_REGISTER_URL"
    - path: "tests/Unit/Phase139/AdmanRegisterUrlConfigTest.php"
      provides: "Prova automatizada de D-04"
    - path: ".planning/REQUIREMENTS-v23.md"
      provides: "Registro das exceções D-06/D-16 ao ADMIN-02 e da correção D-04 ao ADMIN-03"
      contains: "D-16"
  key_links:
    - from: "config/services.php"
      to: ".env (ADMAN_REGISTER_URL)"
      via: "env() com default seguro"
      pattern: "env\\('ADMAN_REGISTER_URL'"
---

<objective>
Fundação sem comportamento observável: capturar a baseline de testes das Fases 137/138 antes de
qualquer código novo, publicar o link fixo de cadastro do Adman como chave de configuração (D-04) e
registrar por escrito, no `REQUIREMENTS-v23.md`, as duas exceções ao ADMIN-02 (D-19) e a correção de
premissa do ADMIN-03 (D-04/D-05/D-14).

Purpose: sem a baseline não há como separar falha nova de falha herdada quando os planos seguintes
começarem a acrescentar chamadores de `EtapaTransicaoService`. Sem os registros no
`REQUIREMENTS-v23.md`, dois requisitos ficariam contradizendo decisões travadas — o ADMIN-02 diz que
o grupo Contrato não tem marcação manual (e o item 1 tem, D-06) e o ADMIN-03 diz que o link do Adman
é gerado pelo checklist (e ele é fixo e manual, D-04). Quem verificar a fase mediria pelo texto
errado.
Output: `139-BASELINE-TESTES.md`, `config/services.php` com `adman.register_url`, `.env.example`
com o bloco `ADMAN_*`, um teste unitário e o `REQUIREMENTS-v23.md` atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-CONTEXT.md
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-VALIDATION.md
@.planning/REQUIREMENTS-v23.md
@CLAUDE.md

**Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada` (branch `feat/fluxo-entrada-empresas`).
Todo caminho abaixo é relativo a essa raiz.

**Comandos de teste que funcionam neste ambiente (Bash):** o binário PHP **não** está no PATH e a
forma `C:\xampp\php\php.exe` **quebra no Bash** (as barras invertidas viram escape). Usar
`/c/xampp/php/php.exe`. `php artisan test` e `--testsuite=Feature` **não terminam** — nunca usar.

<interfaces>
<!-- Estado atual de config/services.php:38-41 — o array 'adman' JÁ EXISTE, com duas chaves. -->
<!-- A tarefa acrescenta uma TERCEIRA chave nesse mesmo array, não cria array novo. -->

config/services.php (linhas 40-43, estado atual):
    'adman' => [
        'base_url' => env('ADMAN_BASE_URL', 'https://api.adman.com.br/v1'),
        'api_key'  => env('ADMAN_API_KEY', ''),
    ],

.env.example: nenhuma linha `ADMAN_*` existe hoje (lacuna PRÉ-EXISTENTE, confirmada por
`grep -in adman .env.example` = nenhum resultado).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturar a baseline de testes das Fases 137/138</name>
  <files>.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-BASELINE-TESTES.md</files>
  <read_first>
    - `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-VALIDATION.md` (seção "Baseline obrigatória ANTES de qualquer código novo" e "Restrições duras deste ambiente")
    - `.planning/phases/138-rea-comercial-conectada-etapa-v23-0/138-VALIDATION.md` (o formato do arquivo de baseline da fase anterior — copiar a estrutura)
    - `.planning/learnings/painel-polos-status-e-meta.md` §2 (as ~10 falhas de Polos que NÃO são regressão — não incluir essas suítes na baseline)
  </read_first>
  <action>
Rodar, na raiz do worktree, exatamente:
`/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase137 tests/Feature/Phase137 tests/Feature/Phase138 --colors=never`

Gravar `139-BASELINE-TESTES.md` com: o comando exato executado (nas duas grafias — Bash e cmd),
a data/hora, a saída literal do resumo final (contagem de tests/assertions/failures/errors), e a
lista nominal de cada teste que falhar, se houver. Se a suíte fechar 100% verde, registrar isso
explicitamente com a frase "baseline verde" — a ausência de falhas é informação, não silêncio.

Incluir uma seção "Suítes deliberadamente FORA da baseline" nomeando
`tests/Feature/Phase38/PolosControllerTest.php` e `tests/Feature/Polos/PolosFaturamentoSnapshotTest.php`
com a razão (falhas antigas documentadas em `.planning/learnings/painel-polos-status-e-meta.md` §2,
não são regressão desta fase e não são gate).

Incluir a advertência de que `php artisan test` e `php artisan test --testsuite=Feature` não
terminam neste ambiente (travam ~300s sem imprimir resumo) e que `C:\xampp\php\php.exe` não
funciona no Bash — a forma que funciona é `/c/xampp/php/php.exe`.

Nenhum arquivo de código é tocado nesta task. Commit com `git commit -- <caminhos>` — nunca
`git add -A` nem `git add .` (a árvore é compartilhada por mais de uma sessão).
  </action>
  <acceptance_criteria>
    - O arquivo `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-BASELINE-TESTES.md` existe
    - O arquivo contém a string literal `tests/Unit/Phase137`, `tests/Feature/Phase137` e `tests/Feature/Phase138`
    - O arquivo contém a string literal `/c/xampp/php/php.exe`
    - O arquivo contém uma linha com o resumo numérico do PHPUnit no formato `Tests: <n>, Assertions: <n>` (ou `OK (<n> tests, <n> assertions)`)
    - O arquivo contém a string `PolosControllerTest` na seção de suítes fora da baseline
    - Nenhum arquivo sob `app/`, `database/`, `routes/`, `resources/` ou `config/` foi alterado nesta task
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-BASELINE-TESTES.md &amp;&amp; grep -q 'tests/Feature/Phase138' .planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-BASELINE-TESTES.md &amp;&amp; grep -q '/c/xampp/php/php.exe' .planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-BASELINE-TESTES.md &amp;&amp; echo BASELINE_OK</automated>
  </verify>
  <done>Existe um arquivo de baseline com a saída real da suíte 137/138, capturada antes de qualquer código desta fase, e as suítes de Polos estão explicitamente fora do gate.</done>
</task>

<task type="auto">
  <name>Task 2: Publicar o link fixo do Adman como configuração (D-04)</name>
  <files>config/services.php, .env.example, tests/Unit/Phase139/AdmanRegisterUrlConfigTest.php</files>
  <read_first>
    - `config/services.php` linhas 36-50 (o array `'adman'` existente, que ganha a terceira chave — não criar array novo)
    - `config/services.php` linhas 115-140 (o bloco `'hubspot'` — o padrão de chave configurável com default seguro que a Fase 138 usou, citado pelo D-04 como precedente)
    - `.env.example` (arquivo inteiro; hoje **não** há nenhuma linha `ADMAN_*` — a lacuna é pré-existente)
    - `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-PATTERNS.md` seção `config/services.php (modificado)` e seção `.env.example (modificado)`
  </read_first>
  <action>
Em `config/services.php`, acrescentar ao array `'adman'` **já existente** a chave `register_url`,
com `env('ADMAN_REGISTER_URL', 'https://app.ad-man.io/register?ref=588D0DD78C4F')`. Não criar um
array novo, não renomear as chaves existentes `base_url`/`api_key`. Comentário em pt-BR explicando,
em duas linhas: (a) é link de cadastro/indicação **fixo, idêntico para todas as empresas** — não há
geração por empresa e o `AdmanService` é somente leitura; (b) trocar o código `ref` em produção é
edição de `.env` na VPS, sem deploy (D-04).

Em `.env.example`, criar um bloco `ADMAN_*` com as **três** chaves — `ADMAN_BASE_URL`,
`ADMAN_API_KEY` e `ADMAN_REGISTER_URL` — com os mesmos defaults do `config/services.php`.
Decisão registrada aqui, e não assumida em silêncio: as duas primeiras já eram lidas por
`config/services.php` desde antes desta fase e nunca estiveram documentadas; documentá-las junto
custa uma linha cada e evita deixar o bloco novo mentindo por omissão sobre o que o Adman precisa.

Criar `tests/Unit/Phase139/AdmanRegisterUrlConfigTest.php` (criar o diretório `tests/Unit/Phase139`)
com um caso que afirma que `config('services.adman.register_url')` devolve exatamente
`https://app.ad-man.io/register?ref=588D0DD78C4F` quando `ADMAN_REGISTER_URL` não está definida, e um
segundo caso que afirma que a URL **não** contém nenhum placeholder de empresa (nem `{company}`, nem
`company_id`, nem `:id`) — a prova de que o link é fixo, não gerado. Classe estende
`Tests\TestCase`; nomes de método em `snake_case` prefixado por `test_`, como o resto do projeto.

Grafia: **Adman** em todo comentário e rótulo. Nunca "ADMA".
  </action>
  <acceptance_criteria>
    - `config/services.php` contém a string literal `'register_url' => env('ADMAN_REGISTER_URL'`
    - `config/services.php` contém a string literal `https://app.ad-man.io/register?ref=588D0DD78C4F`
    - A chave `register_url` está dentro do array `'adman'` já existente (o arquivo continua tendo exatamente uma ocorrência de `'adman' => [`)
    - `.env.example` contém as três linhas `ADMAN_BASE_URL=`, `ADMAN_API_KEY=` e `ADMAN_REGISTER_URL=`
    - `tests/Unit/Phase139/AdmanRegisterUrlConfigTest.php` existe e contém `services.adman.register_url`
    - Nenhum arquivo do projeto contém a grafia `ADMA ` ou `ADMA_` introduzida por esta task
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase139/AdmanRegisterUrlConfigTest.php --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase139/AdmanRegisterUrlConfigTest.php --colors=never &amp;&amp; grep -c "'adman' => \[" config/services.php &amp;&amp; grep -q 'ADMAN_REGISTER_URL' .env.example &amp;&amp; echo CONFIG_OK</automated>
  </verify>
  <done>`config('services.adman.register_url')` devolve o link fixo, a chave está documentada em `.env.example` junto das outras duas do Adman, e um teste unitário prova que a URL não tem nada por empresa.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar as exceções ao ADMIN-02 (D-19) e a correção do ADMIN-03 (D-04) no REQUIREMENTS-v23.md</name>
  <files>.planning/REQUIREMENTS-v23.md</files>
  <read_first>
    - `.planning/REQUIREMENTS-v23.md` linhas 70-95 (o bloco ADMIN-01..ADMIN-06; a linha 80 é o ADMIN-02 e a linha 81 é o ADMIN-03 — as duas notas entram coladas a cada um deles)
    - `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-CONTEXT.md` D-06, D-16 e D-19 (o texto exato das duas exceções ao ADMIN-02 e a instrução de editar à mão)
    - `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-CONTEXT.md` D-04, D-05 e D-14 (a correção do ADMIN-03: link Adman fixo e manual; OAuth ML e conexão ECF como os únicos itens gerados/auto-marcados)
  </read_first>
  <action>
Editar `.planning/REQUIREMENTS-v23.md` **à mão** (nunca via `gsd-sdk query requirements.*` — esses
verbos escrevem em `.planning/REQUIREMENTS.md` sem sufixo, que é da v17 e está stale).

Logo abaixo da linha do **ADMIN-02**, acrescentar uma nota recuada com o título
"Duas exceções explícitas (Fase 139, D-19)" contendo os dois itens:

1. **D-06 — "Contrato revisado" é marcação MANUAL com autoria.** Justificativa a escrever: revisar
   um contrato é ato humano e não deixa rastro digital; nenhum dos 7 estados do envelope
   (`rascunho`, `aguardando_assinaturas`, `assinado`, `recusado`, `expirado`, `cancelado`, `erro`)
   representa "revisado", e as duas alternativas avaliadas foram recusadas —
   `ContratoDadosMinimosService::estaPronta()` mediria completude de cadastro, e `status !== rascunho`
   seria a mesma condição de "enviado", tornando um dos dois itens decorativo.
2. **D-16 — "Contrato assinado" também fecha por `ContratoLiberacao`.** Justificativa a escrever: das
   3 vias de liberação (`ContratoLiberacao::VIA_TODAS` — `webhook`, `manual`, `reconciliacao`), a via
   `manual` (`ContratoAdminController::liberarManual()` → `EmpresaOperacionalRouter::liberarEmpresa()`)
   nunca escreve `contrato_assinaturas.status`/`assinado_em`; ler só essas colunas deixaria a empresa
   liberada por essa via permanentemente impedida de finalizar a entrada administrativa.

Fechar a nota com a frase de escopo: ambas continuam sendo **leitura pura** do estado entregue pela
v22.0 — nenhuma delas reintroduz lógica de assinatura, e a D5 da milestone segue intacta.

Logo abaixo da linha do **ADMIN-03**, acrescentar uma segunda nota, no **mesmo padrão** da anterior,
com o título "Correção de premissa (Fase 139, D-04/D-05/D-14)". O texto original do ADMIN-03 diz que
"Link ADMA ... são gerados pelo próprio checklist, que marca o item ao gerar" — isso está errado em
dois pontos, e o requisito **não deve ser reescrito**: o original fica, a correção vai anotada
embaixo, exatamente como foi feito com o ADMIN-02. Escrever:

1. **O nome é Adman, não ADMA** (correção do usuário, 2026-09-09).
2. **O link do Adman NÃO é gerado e NÃO é por empresa** — é um link fixo de cadastro/indicação,
   idêntico para todas, que passa a morar em `config('services.adman.register_url')` (D-04).
   Consequência direta: o item 6 ("Link Adman entregue") é **manual**, porque não existe estado
   observável para auto-marcar. Fabricar um sinal aqui seria marcar "concluído" sem evidência.
3. **Só dois itens são de fato gerados e auto-marcados pelo checklist:** o item 7, "Grant da
   consultoria", que é o **OAuth do Mercado Livre** e fecha somente quando o cliente **conectou**
   (`ml_tokens.status = active`), nunca quando o link foi gerado (D-05); e o item 8, "Conexão com o
   sistema ECF", que fecha por **existência** da linha em `onboarding_links` (D-14).
   A leitura original de que o item usaria `SyncGrantsFromSftp` está descartada — aquele comando é
   **global** (baixa um XLSX inteiro e faz upsert para todas as empresas) e nunca poderia ser
   acionado como "gerar para esta empresa".

Não alterar o texto do ADMIN-02 nem do ADMIN-03 em si, nem de nenhum outro requisito — as duas notas
são acréscimos abaixo das linhas existentes. Não tocar em `.planning/REQUIREMENTS.md` (sem sufixo).
  </action>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS-v23.md` contém a string literal `D-06` e a string literal `D-16`
    - `.planning/REQUIREMENTS-v23.md` contém a string `ContratoLiberacao`
    - `.planning/REQUIREMENTS-v23.md` contém a string `Contrato revisado`
    - `.planning/REQUIREMENTS-v23.md` contém a string `D-04` e a string `ml_tokens.status`
    - `.planning/REQUIREMENTS-v23.md` contém a string `config('services.adman.register_url')`
    - As linhas dos requisitos `**ADMIN-02**` e `**ADMIN-03**` continuam com o texto original inalterado (`git diff` não mostra alteração dentro das linhas que começam com `- [ ] **ADMIN-02**` e `- [ ] **ADMIN-03**` — as notas são linhas NOVAS abaixo delas)
    - `.planning/REQUIREMENTS.md` (sem sufixo) **não** aparece no `git status` desta task
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'D-16' .planning/REQUIREMENTS-v23.md &amp;&amp; grep -q 'ContratoLiberacao' .planning/REQUIREMENTS-v23.md &amp;&amp; grep -q 'ml_tokens.status' .planning/REQUIREMENTS-v23.md &amp;&amp; grep -q 'adman.register_url' .planning/REQUIREMENTS-v23.md &amp;&amp; test 0 -eq $(git diff --name-only -- .planning/REQUIREMENTS.md | wc -l) &amp;&amp; echo REQ_OK</automated>
  </verify>
  <done>As duas exceções ao ADMIN-02 e a correção do ADMIN-03 estão escritas no `REQUIREMENTS-v23.md`, com justificativa e sem reescrever o texto original dos requisitos, e o `REQUIREMENTS.md` da v17 não foi tocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` da VPS → `config/services.php` | valor de configuração operacional atravessa aqui; é a superfície que o D-04 abre de propósito |
| repositório compartilhado → árvore de trabalho | mais de uma sessão de dev edita o mesmo worktree (learnings §10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-01-01 | Tampering | `ADMAN_REGISTER_URL` no `.env` da VPS | accept | O valor é um link público de cadastro/indicação, sem segredo. Quem edita o `.env` da VPS já tem acesso de root ao servidor — o risco marginal é zero, e o default seguro em código garante que `.env` ausente não quebra nada |
| T-139-01-02 | Information Disclosure | `.env.example` versionado | mitigate | `.env.example` recebe **apenas** o default público de `ADMAN_REGISTER_URL`; `ADMAN_API_KEY=` fica com valor **vazio**, nunca com a chave real. Verificado no acceptance criteria pela ausência de valor após `ADMAN_API_KEY=` |
| T-139-01-03 | Tampering | commit por `git add -A` arrastando trabalho de outra sessão | mitigate | Commit exclusivamente por `git commit -- <caminhos>` com os 5 arquivos nominais deste plano (learnings §10; incidente `feedback_commit_por_caminho_arrasta_outra_sessao`) |
| T-139-01-SC | Tampering | npm/composer installs | mitigate | **Nenhum pacote é instalado nesta fase** — `139-RESEARCH.md` §Package Legitimacy Audit registra "não aplicável", e não há `composer require`/`npm install` em nenhuma task deste plano. Se algum plano posterior precisar de pacote, a gate de legitimidade tem de ser reexecutada naquele momento |
</threat_model>

<verification>
1. `139-BASELINE-TESTES.md` existe e traz o resumo numérico real da suíte 137/138.
2. `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase139 --colors=never` sai 0.
3. `grep -q 'register_url' config/services.php` e `grep -q 'ADMAN_REGISTER_URL' .env.example` passam.
4. `grep -q 'D-16' .planning/REQUIREMENTS-v23.md` passa.
5. `git status --short` não mostra nenhum arquivo fora dos 5 declarados em `files_modified`.
</verification>

<success_criteria>
- Baseline capturada antes de qualquer código da fase, com as suítes de Polos explicitamente fora do gate
- `config('services.adman.register_url')` resolve para o link fixo, provado por teste unitário
- As três chaves `ADMAN_*` documentadas em `.env.example`, com `ADMAN_API_KEY` vazia
- As duas exceções ao ADMIN-02 (D-06 e D-16) escritas em `.planning/REQUIREMENTS-v23.md`
- A correção do ADMIN-03 (Adman em vez de ADMA; link fixo e item manual; só itens 7 e 8 auto-marcam) escrita logo abaixo do requisito, sem reescrever o texto original
- Nenhuma linha de `app/`, `routes/`, `database/` ou `resources/` alterada
</success_criteria>

<output>
Criar `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-01-SUMMARY.md` ao terminar.
</output>
