---
phase: 139-checklist-administrativo-trava-de-finaliza-o-v23-0
plan: 07
type: execute
wave: 6
depends_on: ["139-05", "139-06"]
files_modified:
  - app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php
  - app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php
  - tests/Feature/Phase139/ChecklistDirigeEtapaTest.php
autonomous: true
requirements: [ADMIN-01, ADMIN-05, ADMIN-06]
user_setup: []

must_haves:
  truths:
    - "D-15: o progresso do checklist dirige as etapas 2, 3 e 4 — 1º item concluído leva a `administrativo_andamento`; envelope enviado leva a `aguardando_assinatura`; tudo concluído + contrato assinado leva a `administrativo_concluido`. Sem isso o FINALIZAR nasceria morto, porque a etapa 5 só é alcançável a partir da 4"
    - "D-15: toda transição passa por `EtapaTransicaoService::transicionar()`, com o usuário da sessão como ator — nunca `update()` direto em `companies.etapa`"
    - "D-15: o salto 2→4 acontece para empresa isenta de contrato (D-07) — sem envelope não existe etapa 3, e a tabela `TRANSICOES_PERMITIDAS` já prevê esse salto"
    - "D-07: empresa isenta nunca passa por `aguardando_assinatura`"
    - "O sincronizador só AVANÇA. Desmarcar um item depois de a empresa ter chegado à etapa 4 não a devolve para a 3 — retrocesso é Deferred no CONTEXT.md e nunca é automático na máquina de estados"
    - "Empresa com `etapa` NULL (legado) nunca é carimbada pelo sincronizador — mesma disciplina da D-14 da Fase 138 e D-05 da Fase 137"
  artifacts:
    - path: "app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php"
      provides: "Tradução do progresso do checklist em transições de etapa 1→2→3→4"
      contains: "ETAPA_ADMINISTRATIVO_CONCLUIDO"
    - path: "tests/Feature/Phase139/ChecklistDirigeEtapaTest.php"
      provides: "D-15 — as três transições, o salto 2→4 e a não-regressão de etapa"
  key_links:
    - from: "app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php"
      to: "app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php"
      via: "chamada ao fim de cada mutação e da montagem com ator conhecido"
      pattern: "ChecklistEtapaSincronizadorService"
    - from: "app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php"
      to: "app/Services/FluxoEntrada/EtapaTransicaoService.php"
      via: "transicionar() — única porta de escrita de companies.etapa"
      pattern: "->transicionar\\("
---

<objective>
Fazer o progresso do checklist dirigir a etapa da empresa (D-15): criar o
`ChecklistEtapaSincronizadorService`, que traduz o estado do checklist nas transições 1→2, 2→3 e
2/3→4, e ligá-lo ao `ChecklistAdministrativoService` para que rode a cada montagem com ator conhecido
e a cada marcação/desmarcação.

Purpose: hoje **nenhum chamador de produção** escreve as etapas 2, 3 ou 4 — só a etapa 1. A tabela
`TRANSICOES_PERMITIDAS` só permite chegar em `aguardando_distribuicao` a partir de
`administrativo_concluido`. Sem este plano, o FINALIZAR do plano 139-06 nunca teria de onde partir.
Output: 1 service novo, o wiring no service existente e 1 arquivo de teste Feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-CONTEXT.md
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-RESEARCH.md
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-PATTERNS.md

**Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada`. No Bash, o PHP é
`/c/xampp/php/php.exe`. Nunca `php artisan test`.

<interfaces>
<!-- Já existentes / criados nos planos anteriores — consumir exatamente. -->
App\Models\Company
  ETAPA_AGUARDANDO_ADMINISTRATIVO='aguardando_administrativo'  // 1
  ETAPA_ADMINISTRATIVO_ANDAMENTO='administrativo_andamento'    // 2
  ETAPA_AGUARDANDO_ASSINATURA='aguardando_assinatura'          // 3
  ETAPA_ADMINISTRATIVO_CONCLUIDO='administrativo_concluido'    // 4
  ETAPA_AGUARDANDO_DISTRIBUICAO='aguardando_distribuicao'      // 5
  // companies.etapa pode ser NULL (legado da Fase 137, D-05 — nunca carimbar)

App\Services\FluxoEntrada\EtapaTransicaoService::TRANSICOES_PERMITIDAS (privada, medida no código)
  ''                              => [1]
  aguardando_administrativo (1)   => [administrativo_andamento (2)]
  administrativo_andamento (2)    => [aguardando_assinatura (3), administrativo_concluido (4)]  // salto 2→4
  aguardando_assinatura (3)       => [administrativo_concluido (4)]
  administrativo_concluido (4)    => [aguardando_distribuicao (5)]

App\Services\ChecklistAdministrativo\ChecklistAdministrativoService  (plano 139-05)
  paraEmpresa(Company): array      // ganha 2º parâmetro opcional neste plano
  progresso(Company): array
  concluirManualmente(Company, string $chave, User, bool $forcar = false)
  reabrirItem(Company, string $chave, User)
  gerarConexaoEcf(Company, User)

App\Services\ChecklistAdministrativo\FinalizarEntradaAdministrativaService  (plano 139-06)
  podeFinalizar(Company): array    // ['permitido' => bool, 'requisito_faltante' => ?string]

App\Services\ChecklistAdministrativo\ChecklistAdministrativoDefinicao
  chave do item 2 = 'contrato_enviado'
</interfaces>

<!-- Contrato de SAÍDA definido aqui e consumido pelo plano 139-08. -->
<interfaces>
ChecklistEtapaSincronizadorService
  sincronizar(Company $company, User $por): array
      // => ['transicoes' => array<int, string>, 'etapa_final' => ?string]
      // 'transicoes' lista, em ordem, as etapas efetivamente aplicadas nesta chamada (pode ser vazia)

ChecklistAdministrativoService::paraEmpresa(Company $company, ?User $ator = null): array
      // quando $ator vem preenchido, sincroniza a etapa DEPOIS de persistir o resultado dos resolvers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChecklistEtapaSincronizadorService — a régua da D-15</name>
  <files>app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php</files>
  <read_first>
    - `app/Services/FluxoEntrada/EtapaTransicaoService.php` (arquivo inteiro — a tabela `TRANSICOES_PERMITIDAS` nas linhas 58-75, o docblock da classe nas linhas 16-40 sobre por que a etapa nunca é derivada, e `transicionar()` a partir da linha 212)
    - `app/Http/Controllers/ComercialController.php` linhas 680-695 (o chamador de produção — a forma de invocar e de tratar `status !== 'transicionado'`)
    - `app/Services/ChecklistAdministrativo/FinalizarEntradaAdministrativaService.php` (plano 139-06 — `podeFinalizar()` é a condição da etapa 4)
    - `app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php` (plano 139-05 — `paraEmpresa()` é a fonte do estado dos itens)
    - `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-CONTEXT.md` D-15 (a régua na letra) e a seção Deferred Ideas (a reversibilidade que **não** é escopo)
  </read_first>
  <action>
Criar `app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php`, com construtor
promovido recebendo `ChecklistAdministrativoService`, `FinalizarEntradaAdministrativaService` e
`EtapaTransicaoService`.

Docblock de classe em pt-BR registrando a razão de existir: até esta fase, nenhum chamador de
produção escrevia as etapas 2, 3 ou 4 — só a etapa 1 (`HubspotWebhookController` e
`ComercialController`). Como a etapa 5 só é alcançável a partir da 4, o FINALIZAR nasceria morto.
Esta classe é o **terceiro** chamador de produção de `EtapaTransicaoService`, e é a única a escrever
as etapas intermediárias.

`sincronizar(Company $company, User $por): array` — comportamento:

- **Guarda de legado:** se `$company->etapa` é `null`, retornar `['transicoes' => [], 'etapa_final'
  => null]` **sem tocar em nada**. Comentário citando D-14 da Fase 138 e D-05 da Fase 137: empresa
  legada nunca é carimbada, e derivar etapa de estado externo é exatamente o que a máquina de
  estados existe para impedir.
- **Guarda de escopo:** se a etapa atual **não** está no conjunto {1, 2, 3}, retornar sem
  transicionar. Empresa já em `administrativo_concluido` (4) ou adiante não é responsabilidade
  deste service — a 4→5 é do `FinalizarEntradaAdministrativaService` e as posteriores são das
  Fases 141/142.
- **Avanço em cadeia**, avaliado em laço com no máximo 3 iterações (uma por degrau possível), na
  ordem abaixo. A cada degrau aplicado, reler `$company->refresh()` e reavaliar — uma empresa que
  fecha tudo de uma vez pode subir 1→2→4 numa única chamada.
  1. **etapa 1 → 2** quando `progresso()['feitos'] >= 1`. Ou seja: o primeiro item concluído tira a
     empresa de "aguardando administrativo".
  2. **etapa 2 → 3** quando a empresa **exige contrato** e o item de chave `contrato_enviado` está
     `concluido` na montagem. Para empresa isenta este degrau **nunca** dispara — sem envelope não
     existe etapa 3 (D-07).
  3. **etapa 2 ou 3 → 4** quando `FinalizarEntradaAdministrativaService::podeFinalizar($company)`
     devolver `permitido = true`. O caso 2→4 é o **salto** previsto na tabela de transições para
     empresa isenta (D-15/D-07) e não precisa de tratamento especial: a tabela já o permite.
- Toda transição por `$this->etapas->transicionar($company, $destino, $por)`. Quando o retorno vier
  com `status !== 'transicionado'`, emitir `Log::warning('[Checklist] transição de etapa recusada',
  ['company_id' => ..., 'destino' => ..., 'resultado' => ...])` e **interromper a cadeia** — nunca
  tentar o degrau seguinte por cima de uma recusa.
- Devolver a lista das etapas efetivamente aplicadas, em ordem, e a etapa final.

**O sincronizador só AVANÇA — nunca retrocede.** Comentário pt-BR obrigatório: desmarcar um item
depois de a empresa ter chegado à etapa 4 **não** a devolve para a 3. A reversibilidade está
registrada como Deferred no CONTEXT.md (não discutida com o usuário), e
`EtapaTransicaoService::podeTransicionar()` recusa retrocesso sem motivo explícito por desenho —
inventar aqui um motivo automático seria escrever no histórico uma decisão que ninguém tomou.

Proibido neste arquivo: qualquer escrita direta em `companies.etapa`, qualquer `DB::transaction()`
envolvendo `transicionar()` (o service chamado já é transacional com `lockForUpdate()`).
  </action>
  <acceptance_criteria>
    - `ChecklistEtapaSincronizadorService.php` contém `public function sincronizar(Company $company, User $por): array`
    - Contém as quatro constantes `ETAPA_AGUARDANDO_ADMINISTRATIVO`, `ETAPA_ADMINISTRATIVO_ANDAMENTO`, `ETAPA_AGUARDANDO_ASSINATURA`, `ETAPA_ADMINISTRATIVO_CONCLUIDO`
    - Contém a guarda de `etapa === null` antes de qualquer chamada a `transicionar(`
    - **Não** contém `update(['etapa'`, `->etapa =`, nem `DB::transaction(` (verificar ignorando linhas de comentário)
    - Contém `Log::warning(` no tratamento de `status !== 'transicionado'`
    - `/c/xampp/php/php.exe -l app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php` sai sem erro
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe -l app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php &amp;&amp; test 0 -eq $(grep -v '^[[:space:]]*[/*]' app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php | grep -c -e "update(\['etapa'" -e 'DB::transaction(') &amp;&amp; grep -q 'public function sincronizar' app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php &amp;&amp; echo SINCRONIZADOR_OK</automated>
  </verify>
  <done>Existe um service que traduz o estado do checklist nas transições 1→2, 2→3 e 2/3→4, sempre por `EtapaTransicaoService`, que ignora empresa legada e nunca retrocede.</done>
</task>

<task type="auto">
  <name>Task 2: Ligar o sincronizador ao ChecklistAdministrativoService e provar a régua</name>
  <files>app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php, tests/Feature/Phase139/ChecklistDirigeEtapaTest.php</files>
  <read_first>
    - `app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php` (arquivo inteiro, como ficou no plano 139-05 — os pontos de mutação a instrumentar)
    - `app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php` (criado na Task 1)
    - `tests/Feature/Phase138/ComercEtapaNascimentoCadastroManualTest.php` linhas 60-95 (asserção de etapa e de histórico por reconsulta ao banco — o molde deste teste)
    - `tests/Unit/Phase137/EtapaTransicaoServiceTest.php` linhas 20-40 (`empresaNaEtapa()` — o helper de setup de etapa a copiar)
  </read_first>
  <action>
Alterar `ChecklistAdministrativoService` para receber o `ChecklistEtapaSincronizadorService` no
construtor e chamá-lo em quatro pontos, sempre **depois** de a persistência do checklist terminar:

1. `paraEmpresa(Company $company, ?User $ator = null)` — parâmetro novo, **opcional**, no fim da
   assinatura (mudança aditiva; nenhum chamador existente quebra). Quando `$ator` vier preenchido,
   sincronizar depois de persistir o resultado dos resolvers e **antes** de montar o array de
   retorno, para que o payload já reflita a etapa nova. Comentário pt-BR explicando por que a
   montagem também sincroniza e não só as mutações: o degrau 2→3 depende de um evento **externo**
   (o webhook do Clicksign gravando `enviado_em`), que não passa por nenhuma ação do checklist — o
   carregamento da ficha é o único momento em que o sistema observa esse fato.
2. `concluirManualmente()` — sincronizar ao final, passando `$usuario` como ator.
3. `reabrirItem()` — sincronizar ao final. Não vai retroceder (o sincronizador só avança), mas a
   chamada fica para que a régua tenha um ponto único.
4. `gerarConexaoEcf()` — sincronizar ao final (fechar o item 8 pode ser o 1º item concluído).

Não introduzir `EtapaTransicaoService` diretamente neste arquivo — a única dependência nova é o
sincronizador. Comentar em pt-BR: ponto único de tradução progresso→etapa.

Criar `tests/Feature/Phase139/ChecklistDirigeEtapaTest.php` com `RefreshDatabase` e o `setUp()`
blindado (`Http::fake()`, `Queue::fake()`, `config(['services.clicksign.signatarios_ecf' => []])`).
Todas as asserções de etapa por **reconsulta ao banco** (`$company->refresh()` ou `Company::find()`),
nunca pela resposta em memória. Casos:
1. empresa na etapa 1, marcar o primeiro item manual: etapa vira `administrativo_andamento` e existe
   linha em `company_etapa_transicoes` com `user_id` do usuário que marcou;
2. empresa na etapa 2, com serviço que exige contrato, envelope ganha `enviado_em` (gravar
   `ContratoAssinatura` direto) e a ficha é montada com ator: etapa vira `aguardando_assinatura`;
3. empresa na etapa 3, todos os 9 itens concluídos e contrato assinado: etapa vira
   `administrativo_concluido`;
4. **salto 2→4 (D-15/D-07)** — empresa **isenta** na etapa 2 com os 6 itens concluídos: etapa vira
   `administrativo_concluido` **sem nunca** ter passado por `aguardando_assinatura`; afirmar por
   `company_etapa_transicoes` que **não existe** nenhuma linha com `etapa_nova = aguardando_assinatura`;
5. **cadeia numa chamada só** — empresa na etapa 1 com tudo já pronto (itens automáticos fechados
   pelo estado real, manuais marcados em sequência): a última marcação leva a empresa até
   `administrativo_concluido`, e o histórico registra **cada** degrau como linha própria, em ordem;
6. **não retrocede** — empresa na etapa 4, desmarcar um item: `permitido` do `podeFinalizar` volta a
   `false`, mas `companies.etapa` **continua** `administrativo_concluido` por reconsulta;
7. **legado** — empresa com `etapa = null`, marcar item: `etapa` continua `null` e **nenhuma** linha
   é criada em `company_etapa_transicoes`;
8. **sem ator** — `paraEmpresa($company)` sem o segundo parâmetro não transiciona nada, mesmo com
   todos os itens fechados (prova de que a sincronização exige ator conhecido).
  </action>
  <acceptance_criteria>
    - `ChecklistAdministrativoService.php` contém `ChecklistEtapaSincronizadorService` no construtor
    - A assinatura passou a ser `public function paraEmpresa(Company $company, ?User $ator = null): array`
    - O arquivo contém `->sincronizar(` em pelo menos 4 pontos
    - `ChecklistAdministrativoService.php` continua **sem** referência direta a `EtapaTransicaoService` (verificar ignorando comentários)
    - `tests/Feature/Phase139/ChecklistDirigeEtapaTest.php` contém pelo menos 8 métodos `public function test_`
    - O teste contém `assertDatabaseMissing('company_etapa_transicoes'` no caso do salto 2→4 e no caso do legado
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ChecklistDirigeEtapaTest.php --colors=never` sai com código 0
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139 tests/Unit/Phase139 --colors=never` sai com código 0 (nenhum teste anterior da fase quebrou com a mudança de assinatura)
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139 tests/Unit/Phase139 --colors=never &amp;&amp; test 4 -le $(grep -c -- '->sincronizar(' app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php) &amp;&amp; echo WIRING_OK</automated>
  </verify>
  <done>Marcar item, gerar conexão ou apenas abrir a ficha avança a etapa conforme a D-15, incluindo o salto 2→4 da empresa isenta, sem nunca retroceder e sem tocar em empresa legada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evento externo (webhook Clicksign) → etapa da empresa | o degrau 2→3 é a única leitura de estado externo que provoca escrita de etapa nesta fase |
| carregamento de ficha (GET) → escrita de `companies.etapa` | um GET passa a ter efeito colateral de escrita; a fronteira precisa de ator autenticado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-07-01 | Spoofing | etapa avançada sem ator identificável (GET anônimo, job, comando) | mitigate | `sincronizar()` exige `User $por` tipado e `paraEmpresa()` só sincroniza quando `$ator` vem preenchido — teste 8 prova que sem ator nada transiciona. O controller (139-08) passa `$request->user()`, sempre autenticado pelo middleware da rota |
| T-139-07-02 | Tampering | histórico falso: etapas carimbadas em empresa legada que nunca as viveu | mitigate | Guarda de `etapa === null` antes de qualquer transição, com teste 7 afirmando zero linha em `company_etapa_transicoes`. Mesma disciplina da D-14 da Fase 138 |
| T-139-07-03 | Elevation of Privilege | pular a etapa 3 e chegar à 4 sem contrato enviado numa empresa que exige contrato | mitigate | O degrau 2→4 só passa quando `podeFinalizar()` permite, e isso exige o item `contrato_assinado` concluído para empresa que exige contrato — a régua do 139-06 é a mesma; o salto só é inócuo porque quem exige contrato não o alcança sem assinar |
| T-139-07-04 | Repudiation | retrocesso automático apagando o instante real de uma etapa que a Fase 143 vai medir | mitigate | O sincronizador só avança, por desenho; teste 6 afirma que desmarcar não muda a etapa. `EtapaTransicaoService` já recusa retrocesso sem motivo |
| T-139-07-05 | Denial of Service | laço de transições disparando em cascata a cada request | mitigate | Laço com no máximo 3 iterações (uma por degrau possível) e interrupção imediata em qualquer recusa; `transicionar()` já é idempotente para destino igual à etapa atual |
| T-139-07-SC | Tampering | npm/composer installs | mitigate | Nenhum pacote instalado — `139-RESEARCH.md` §Package Legitimacy Audit: "não aplicável" |
</threat_model>

<verification>
1. `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139 tests/Unit/Phase139 --colors=never` sai 0.
2. `grep -v '^[[:space:]]*[/*]' app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php | grep -c "update(\['etapa'"` retorna 0.
3. Regressão obrigatória: `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase137 tests/Feature/Phase137 tests/Feature/Phase138 --colors=never` igual à `139-BASELINE-TESTES.md` — este plano acrescenta o **terceiro** chamador de produção de `EtapaTransicaoService` e é o de maior risco de regressão da fase.
</verification>

<success_criteria>
- As três transições da D-15 acontecem, cada uma com linha própria de histórico
- Salto 2→4 comprovado para empresa isenta, sem passar por `aguardando_assinatura`
- Empresa legada (`etapa` NULL) nunca é carimbada
- Nenhuma escrita direta em `companies.etapa` em lugar nenhum da fase
- Suíte 137/138 sem regressão contra a baseline
</success_criteria>

<output>
Criar `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-07-SUMMARY.md` ao terminar.
</output>
