---
phase: 139-checklist-administrativo-trava-de-finaliza-o-v23-0
plan: 05
type: execute
wave: 4
depends_on: ["139-02", "139-03", "139-04"]
files_modified:
  - app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php
  - tests/Feature/Phase139/ChecklistContagemItensTest.php
  - tests/Unit/Phase139/ChecklistProgressoTest.php
  - tests/Feature/Phase139/ChecklistMarcacaoManualAutoriaTest.php
autonomous: true
requirements: [ADMIN-01, ADMIN-03, ADMIN-04]
user_setup: []

must_haves:
  truths:
    - "D-07: `paraEmpresa()` monta 9 itens para empresa com serviço que exige contrato e 6 para empresa isenta — o grupo Contrato NÃO aparece no payload da isenta, e o denominador do progresso acompanha"
    - "D-02: nenhum item é marcado \"não aplicável\" para resolver isenção — o item simplesmente não é instanciado"
    - "D-10: o denominador do progresso vem do CATÁLOGO em código, nunca da contagem de linhas em `checklist_administrativo_itens` — linha com `chave` órfã não entra no denominador e a ficha ainda fecha 100%"
    - "D-11: marcar manualmente grava `feito_por` e `feito_em` juntos; desmarcar limpa OS DOIS juntos; a leitura da tela vem sempre das colunas, nunca do activity log"
    - "D-13: `concluirManualmente()` recusa item com `auto_fonte` declarado no catálogo, lançando `DomainException` — os 4 itens automáticos não podem ser marcados à mão pela tela"
    - "D-14: o item 8 continua fechando por existência do `onboarding_links`, e `gerarConexaoEcf()` é a ação idempotente que cria a linha quando o usuário pede"
    - "Este service não conhece `EtapaTransicaoService` nem `ChecklistEtapaSincronizadorService` — a sincronização de etapa é orquestrada pela camada HTTP (plano 139-08), e é essa direção one-way que mantém o grafo de injeção acíclico"
  artifacts:
    - path: "app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php"
      provides: "Montagem condicional, execução dos resolvers, progresso e marcação manual com autoria"
      contains: "public function paraEmpresa"
      min_lines: 120
    - path: "tests/Feature/Phase139/ChecklistContagemItensTest.php"
      provides: "ADMIN-01 + D-07 — 9 itens com contrato, 6 sem"
    - path: "tests/Unit/Phase139/ChecklistProgressoTest.php"
      provides: "D-10 — chave órfã fora do denominador"
    - path: "tests/Feature/Phase139/ChecklistMarcacaoManualAutoriaTest.php"
      provides: "ADMIN-04 + D-11 — autoria gravada e limpa em par"
  key_links:
    - from: "app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php"
      to: "app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php"
      via: "catálogo fechado é a fonte do denominador"
      pattern: "ChecklistAdministrativoDefinicao::itens"
    - from: "app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php"
      to: "app/Services/ChecklistAdministrativo/Resolvers/"
      via: "mapa chave() => resolver, injetado no construtor"
      pattern: "ChecklistResolver"
---

<objective>
Escrever o `ChecklistAdministrativoService`: monta o checklist de uma empresa conforme ela exija ou
não contrato (D-07), roda os 4 resolvers automáticos e persiste o resultado, calcula o progresso a
partir do catálogo (D-10) e permite marcar/desmarcar manualmente com autoria (D-11), recusando
marcação manual em item automático (D-13).

Purpose: é o coração de leitura e escrita do checklist. Tudo o que a tela mostra e tudo o que o
FINALIZAR consulta sai daqui.
Output: 1 service + 3 arquivos de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-CONTEXT.md
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-PATTERNS.md

**Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada`. No Bash, o PHP é
`/c/xampp/php/php.exe`. Nunca `php artisan test`.

⚠️ **Este plano NÃO transiciona `companies.etapa`, e este arquivo NUNCA vai transicionar.** A régua
da D-15 (progresso do checklist dirigindo as etapas 2/3/4) mora no
`ChecklistEtapaSincronizadorService` (plano 139-07) e é disparada pela **camada HTTP** (plano
139-08), nunca por dentro deste service. O motivo é estrutural, não estilístico: o sincronizador
precisa **ler** este service, então se este service recebesse o sincronizador no construtor o grafo
de injeção fecharia ciclo (`ChecklistAdministrativoService` → `ChecklistEtapaSincronizadorService` →
`ChecklistAdministrativoService`) e o container do Laravel lançaria `CircularDependencyException`,
derrubando com 500 toda rota que type-hinte qualquer um dos dois.
Consequência prática para a execução deste plano: **nenhuma** chamada a `EtapaTransicaoService` nem
ao sincronizador nasce aqui, e `paraEmpresa()` fica com **um** argumento (`Company $company`) — não
receber ator é o que impede a tentação de sincronizar por dentro.

<interfaces>
<!-- Já criado pelos planos 139-02/139-03/139-04 — consumir exatamente estes nomes. -->
App\Models\ChecklistAdministrativoItem
  $table = 'checklist_administrativo_itens'
  STATUS_ABERTO='aberto' STATUS_CONCLUIDO='concluido'
  fillable: company_id, chave, status, valor, feito_por, feito_em, auto_em
  company(): BelongsTo   feitoPor(): BelongsTo (withTrashed)

App\Services\ChecklistAdministrativo\ChecklistAdministrativoDefinicao
  GRUPO_CONTRATO='contrato' GRUPO_ENTRADA='entrada' NATUREZA_MANUAL='manual' NATUREZA_AUTO='auto'
  static itens(bool $exigeContrato): array   static item(string $chave): ?array
  static chaves(bool $exigeContrato): array
  item shape: ['ordem','chave','titulo','grupo','natureza','auto_fonte','ajuda']

App\Contracts\ChecklistResolver
  chave(): string; label(): string; ajuda(): string; resolver(Company $company): ChecklistResolverResultado

App\Services\ChecklistAdministrativo\Resolvers\{ContratoEnviadoResolver, ContratoAssinadoResolver,
  MlOAuthConectadoResolver, ConexaoEcfResolver}

App\Services\Onboarding\OnboardingLinkService
  public function paraEmpresa(Company $company): OnboardingLink   // firstOrCreate, idempotente
  // construtor: __construct(private OnboardingEngineService $engine) — resolver pelo container

App\Models\Servico::exigeContrato(): bool
App\Models\Company::contratosServico(): HasMany
</interfaces>

<!-- Contrato de SAÍDA que este plano define e que os planos 139-06/139-08/139-09 consomem. -->
<interfaces>
ChecklistAdministrativoService::paraEmpresa(Company $company): array
[
  'exige_contrato' => bool,
  'grupos' => [
      'contrato' => ['chave' => 'contrato', 'titulo' => 'Contrato', 'itens' => [ ...item... ]],  // ausente se isenta
      'entrada'  => ['chave' => 'entrada',  'titulo' => 'Entrada',  'itens' => [ ...item... ]],
  ],
  'progresso' => ['feitos' => int, 'total' => int, 'percentual' => int],
]
item (achatado para o payload Inertia, nunca o model inteiro):
[
  'chave' => string, 'titulo' => string, 'grupo' => string, 'natureza' => 'manual'|'auto',
  'status' => 'aberto'|'concluido', 'ajuda' => string,
  'motivo' => ?string,            // motivo do resolver quando aberto
  'feito_por_nome' => ?string, 'feito_em' => ?string, 'auto_em' => ?string,  // ISO-8601
]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: paraEmpresa() — montagem condicional e execução dos resolvers</name>
  <files>app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php, tests/Feature/Phase139/ChecklistContagemItensTest.php</files>
  <read_first>
    - `app/Services/Onboarding/OnboardingEngineService.php` linhas 500-545 (`aplicarResultado()` — como o motor traduz o resultado do resolver em status e `auto_em`; copiar a forma, ignorando os ramos de coleta assíncrona)
    - `app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php` (criado no plano 139-03 — a fonte do que existe)
    - `app/Services/ChecklistAdministrativo/Resolvers/ContratoAssinadoResolver.php` e `.../ConexaoEcfResolver.php` (criados no plano 139-04 — as assinaturas a injetar)
    - `app/Http/Controllers/ContratoAdminController.php` linhas 507-530 e 555-575 (como o controller já carrega `contratosServico` ativos — a mesma leitura que decide `exigeContrato`)
    - `tests/Feature/Phase131/ContratoAdminDetalheTest.php` linhas 30-140 (o `setUp()` blindado e os três helpers de setup a copiar)
  </read_first>
  <action>
Criar `app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php`.

Construtor com propriedades promovidas recebendo os quatro resolvers
(`ContratoEnviadoResolver`, `ContratoAssinadoResolver`, `MlOAuthConectadoResolver`,
`ConexaoEcfResolver`) e o `OnboardingLinkService`. Método privado `mapaDeResolvers(): array`
montando `[$resolver->chave() => $resolver]` — nenhuma factory nova, o container do Laravel já
resolve as dependências. Comentário pt-BR: não se cria `ChecklistResolverFactory` porque são quatro
resolvers fixos de um catálogo fechado; uma factory só se justifica quando o conjunto é aberto.

Método privado `exigeContrato(Company $company): bool` — `true` quando existe ao menos um
`contratosServico` com `ativo === true` cujo `servico->exigeContrato()` é `true`. Usar
`loadMissing('contratosServico.servico')` antes, para não fazer N+1.

`paraEmpresa(Company $company): array` — o contrato de saída está no bloco `<interfaces>` acima e
deve ser respeitado na letra (os planos 139-08 e 139-09 já consomem essas chaves).
Passos: (1) resolver `exigeContrato`; (2) pegar `ChecklistAdministrativoDefinicao::itens($exige)`;
(3) carregar de uma vez as linhas existentes de `ChecklistAdministrativoItem` da empresa, indexadas
por `chave`, com `feitoPor` eager-loaded; (4) para cada item de natureza `auto`, rodar o resolver
correspondente e **persistir** o resultado com `updateOrCreate(['company_id','chave'], [...])`:
`concluido` grava `status = STATUS_CONCLUIDO`, `valor` = valor do resultado e `auto_em = now()`;
qualquer outro estado grava `status = STATUS_ABERTO` e **não** mexe em `auto_em` já existente; (5)
para itens `manual`, ler a linha (ausente = `aberto`, e nunca criar linha só para representar
"aberto" — persistência **lazy**, a linha nasce quando alguém marca); (6) agrupar por `grupo`,
preservando a ordem do catálogo; (7) omitir a chave `'contrato'` de `grupos` inteira quando
`exigeContrato` for `false` (D-07 — o grupo **não existe**, não vem vazio nem marcado).

Regra explícita a comentar em pt-BR: um item `auto` nunca carrega `feito_por_nome` (ninguém o
marcou); um item `manual` nunca carrega `auto_em`. Se as duas colunas aparecerem preenchidas na
mesma linha, é sintoma de override forçado e a tela deve poder mostrar as duas — não normalizar
apagando uma.

Rodar os 4 resolvers a cada chamada é aceitável e deliberado: são leituras de coluna local, sem
rede — diferente do `AdmanGrantResolver` do motor de Onboarding, que sonda a API e por isso é
assíncrono lá. Registrar essa decisão em comentário.

Criar `tests/Feature/Phase139/ChecklistContagemItensTest.php` com `RefreshDatabase` e o `setUp()`
blindado (`Http::fake()`, `Queue::fake()`, `config(['services.clicksign.signatarios_ecf' => []])`),
mais os helpers `servicoComContrato()`, `empresaCompleta()` e `vincularServico()` com
`ContratoServico::withoutEvents(...)`. Casos:
1. empresa com serviço que exige contrato: `paraEmpresa()` devolve `exige_contrato = true`,
   `grupos` com as duas chaves, e a soma dos itens dos dois grupos é **9**;
2. empresa com serviço isento (`Servico::create([... 'exige_contrato' => false ...])`, o caso Polos):
   `exige_contrato = false`, `grupos` **não tem** a chave `'contrato'`
   (`assertArrayNotHasKey('contrato', ...)`), e a contagem é **6**;
3. empresa com os dois tipos de serviço ativos ao mesmo tempo: conta como "exige contrato" (9 itens);
4. a ordem dos itens dentro de cada grupo segue a `ordem` do catálogo;
5. nenhuma linha é criada em `checklist_administrativo_itens` para item `manual` só por chamar
   `paraEmpresa()` — `assertDatabaseCount` das linhas manuais é 0 (persistência lazy).
  </action>
  <acceptance_criteria>
    - `ChecklistAdministrativoService.php` contém `public function paraEmpresa(Company $company): array`
    - Contém `ChecklistAdministrativoDefinicao::itens(`
    - Contém `updateOrCreate(` e **não** contém `EtapaTransicaoService` nem `ChecklistEtapaSincronizadorService` (a régua de etapa é do plano 139-07 e o disparo é do 139-08 — este arquivo não pode depender de nenhum dos dois, sob pena de ciclo de injeção)
    - `tests/Feature/Phase139/ChecklistContagemItensTest.php` contém `assertArrayNotHasKey('contrato'`
    - O teste contém as asserções de contagem `9` e `6` sobre os itens montados
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ChecklistContagemItensTest.php --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ChecklistContagemItensTest.php --colors=never &amp;&amp; test 0 -eq $(grep -v '^[[:space:]]*[/*]' app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php | grep -c -e 'EtapaTransicaoService' -e 'ChecklistEtapaSincronizadorService') &amp;&amp; echo MONTAGEM_OK</automated>
  </verify>
  <done>`paraEmpresa()` devolve 9 itens com contrato e 6 sem, com o grupo Contrato ausente do payload da isenta, e nenhuma linha é criada para item manual só por ler.</done>
</task>

<task type="auto">
  <name>Task 2: progresso() com denominador vindo do catálogo (D-10)</name>
  <files>app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php, tests/Unit/Phase139/ChecklistProgressoTest.php</files>
  <read_first>
    - `app/Services/Onboarding/OnboardingSituacaoService.php` linhas 218-267 (`contadores()` e `progresso()` — a fórmula análoga, que subtrai `nao_aplicaveis` do total; aqui não há o que subtrair, e a razão está no PATTERNS)
    - `resources/js/Components/Onboarding/Painel/ProgressoBarra.jsx` linhas 20-67 (o contrato de props `{feitos, total, percentual}` que este método precisa devolver para o componente ser reusado por import direto no plano 139-09)
    - `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-PATTERNS.md` seção `ChecklistAdministrativoService.php`, subseção "CÁLCULO DO DENOMINADOR DO PROGRESSO" e a armadilha da chave órfã
  </read_first>
  <action>
Acrescentar ao `ChecklistAdministrativoService` o método
`progresso(Company $company): array` devolvendo exatamente `['feitos' => int, 'total' => int,
'percentual' => int]` — mesmo contrato de props do `ProgressoBarra.jsx`, para que o componente seja
reusado por import direto no plano 139-09 em vez de duplicado.

**O denominador vem do catálogo, nunca da tabela.** `total` = número de itens que
`ChecklistAdministrativoDefinicao::itens($exigeContrato)` declara para aquela empresa (6 ou 9).
`feitos` = quantos desses itens estão `concluido` na montagem. `percentual` =
`(int) round($feitos / $total * 100)` com guarda para `$total === 0`.

Comentário pt-BR obrigatório registrando por que a fórmula é mais simples que a do Onboarding:
lá o denominador é `count($passos) - $naoAplicaveis`, porque a régua nasce copiada para linhas e a
isenção é marcada. Aqui a D-02 proíbe `nao_aplicavel` e a D-07 resolve a isenção **no nascimento**
(o item nem é instanciado), então não há o que subtrair. E registrar a consequência prática: uma
linha gravada em `checklist_administrativo_itens` com `chave` que saiu do catálogo (renomeação em
deploy) é lixo órfão — não aparece na tela e **não entra no denominador**; a ficha continua podendo
fechar 100%. É a família de bug já registrada em `project_onboarding_observacoes_publicacao_260902`
("renomear item do checklist não pode trocar o `id`"), resolvida aqui por construção. Reforçar:
renomear o **rótulo** de um item nunca pode trocar a `chave`.

Reaproveitar o resultado de `paraEmpresa()` internamente para não montar o checklist duas vezes
quando o chamador precisar dos dois (expor `progresso()` público, mas o `paraEmpresa()` já devolve
`progresso` no payload — a duplicação de trabalho é o que se quer evitar, não a duplicação de API).

Criar `tests/Unit/Phase139/ChecklistProgressoTest.php` com `RefreshDatabase` (o caso da chave órfã
exige linha em banco). Casos:
1. empresa isenta com 0 itens concluídos: `total = 6`, `feitos = 0`, `percentual = 0`;
2. empresa isenta com todos os 6 concluídos: `percentual = 100`;
3. empresa com contrato, 0 concluídos: `total = 9`;
4. **chave órfã** — inserir diretamente em `checklist_administrativo_itens` uma linha com
   `chave = 'item_que_saiu_do_catalogo'` e `status = 'concluido'`, e afirmar que `total` continua
   sendo 6 (ou 9) e que `feitos` **não** subiu por causa dela; e que com os itens reais todos
   concluídos o `percentual` fecha exatamente `100`, não `86` (D-10);
5. `total === 0` nunca produz divisão por zero.
  </action>
  <acceptance_criteria>
    - `ChecklistAdministrativoService.php` contém `public function progresso(Company $company): array`
    - O método devolve as três chaves literais `'feitos'`, `'total'`, `'percentual'`
    - O cálculo do `total` referencia `ChecklistAdministrativoDefinicao` e **não** usa `ChecklistAdministrativoItem::where(...)->count()` como denominador
    - `tests/Unit/Phase139/ChecklistProgressoTest.php` contém a string `item_que_saiu_do_catalogo`
    - O teste contém a asserção de `percentual` igual a `100` no cenário com chave órfã presente
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase139/ChecklistProgressoTest.php --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase139/ChecklistProgressoTest.php --colors=never</automated>
  </verify>
  <done>O progresso devolve `{feitos,total,percentual}` com denominador vindo do catálogo, e uma linha com chave órfã comprovadamente não impede a ficha de fechar 100%.</done>
</task>

<task type="auto">
  <name>Task 3: Marcação manual com autoria, desmarcação e geração da conexão ECF</name>
  <files>app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php, tests/Feature/Phase139/ChecklistMarcacaoManualAutoriaTest.php</files>
  <read_first>
    - `app/Services/Onboarding/OnboardingEngineService.php` linhas 585-665 (`concluirManualmente()` e `reabrirPasso()` — a recusa por `auto_fonte`, o par `feito_por`/`feito_em`, a limpeza dos dois juntos e o `activity()` log)
    - `app/Models/Pendencia.php` linhas 84-97 (o `->withTrashed()` que o model já tem — o teste depende dele)
    - `app/Services/Onboarding/OnboardingLinkService.php` linhas 28-50 (`paraEmpresa()`, `firstOrCreate` idempotente — a ação que gera a conexão ECF)
    - `app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php` (a consulta de `natureza`/`auto_fonte` que decide se a marcação manual é permitida)
  </read_first>
  <action>
Acrescentar ao `ChecklistAdministrativoService`:

`concluirManualmente(Company $company, string $chave, User $usuario, bool $forcar = false): ChecklistAdministrativoItem`
- Buscar o item no catálogo com `ChecklistAdministrativoDefinicao::item($chave)`; `null` →
  `DomainException` "item desconhecido" (defesa de chave órfã e de string livre).
- Se o item pertence ao grupo `contrato` e a empresa **não** exige contrato (D-07) →
  `DomainException` "este item não existe para esta empresa".
- Se o item tem `auto_fonte` e `$forcar === false` → `DomainException` com mensagem em pt-BR
  nomeando o item e dizendo que ele tem verificação automática. O parâmetro `$forcar` existe por
  paridade com o molde do Onboarding, mas **não é exposto por HTTP nesta fase** — comentar isso.
- Caso permitido: `updateOrCreate(['company_id','chave'])` com `status = STATUS_CONCLUIDO`,
  `feito_por = $usuario->id`, `feito_em = now()`. Os dois campos **sempre juntos** (D-11).
- Registrar `activity('checklist-administrativo')->performedOn($company)->withProperties([...])`
  como trilha **secundária** — comentar em pt-BR que a leitura da tela vem sempre das colunas
  `feito_por`/`feito_em`, nunca do activity log (D-11, ponto 3).

`reabrirItem(Company $company, string $chave, User $usuario): ChecklistAdministrativoItem`
- Recusa com `DomainException` se o item não está `concluido` (nada a desmarcar).
- Grava `status = STATUS_ABERTO`, `feito_por = null`, `feito_em = null` — **os três no mesmo
  `save()`**, nunca limpar um sem o outro (D-11, ponto 2). Activity log equivalente.

`gerarConexaoEcf(Company $company, User $usuario): OnboardingLink`
- Chama `OnboardingLinkService::paraEmpresa($company)` (idempotente, `firstOrCreate`) e devolve o
  link. **Não** marca o item 8 à mão: o `ConexaoEcfResolver` já vai ler a existência da linha na
  próxima montagem (D-14). Comentar isso — marcar à mão criaria uma segunda fonte de verdade para o
  mesmo fato.

Nenhum destes métodos aceita `user_id` por parâmetro cru vindo de requisição: a assinatura recebe
`User $usuario` tipado, e o controller (plano 139-08) passará `$request->user()`. Comentar citando
a mesma disciplina T-137-02 já documentada em `EtapaTransicaoService::transicionar()`.

Criar `tests/Feature/Phase139/ChecklistMarcacaoManualAutoriaTest.php` com `RefreshDatabase` e o
`setUp()` blindado. Casos:
1. marcar `grupo_whatsapp_criado`: por reconsulta ao banco, `status = concluido`,
   `feito_por = $user->id` e `feito_em` não-nulo;
2. desmarcar o mesmo item: `status = aberto` e **os dois** campos nulos na mesma linha;
3. marcar `contrato_enviado` (item `auto`) lança `DomainException` e **nenhuma linha** é criada —
   `assertDatabaseMissing`;
4. marcar um item do grupo Contrato numa empresa isenta lança `DomainException` (D-07);
5. marcar `chave_inexistente` lança `DomainException` e nada é gravado;
6. **autoria sobrevive ao desligamento**: marcar com um `User`, fazer soft delete do usuário, e
   afirmar que o payload de `paraEmpresa()` ainda traz `feito_por_nome` preenchido (D-11, ponto 1);
7. `gerarConexaoEcf()` chamado duas vezes cria **uma** linha em `onboarding_links`
   (`assertDatabaseCount('onboarding_links', 1)`), e depois da primeira chamada o item 8 aparece
   `concluido` na montagem (D-14).
  </action>
  <acceptance_criteria>
    - `ChecklistAdministrativoService.php` contém `public function concluirManualmente(`, `public function reabrirItem(` e `public function gerarConexaoEcf(`
    - `concluirManualmente` recebe `User $usuario` tipado e **não** tem nenhum parâmetro chamado `$userId` ou `$user_id`
    - O arquivo contém `throw new \DomainException` em pelo menos 3 pontos
    - `reabrirItem` grava `feito_por` e `feito_em` como `null` no mesmo bloco (nenhum `save()` intermediário entre as duas atribuições)
    - `gerarConexaoEcf` contém `paraEmpresa(` e **não** marca o item por `updateOrCreate`
    - `tests/Feature/Phase139/ChecklistMarcacaoManualAutoriaTest.php` contém `expectException(\DomainException::class)` e `assertDatabaseCount('onboarding_links', 1)`
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ChecklistMarcacaoManualAutoriaTest.php --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ChecklistMarcacaoManualAutoriaTest.php --colors=never &amp;&amp; grep -q 'public function gerarConexaoEcf' app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php &amp;&amp; echo MARCACAO_OK</automated>
  </verify>
  <done>Marcar grava autoria, desmarcar limpa os dois campos juntos, item automático recusa marcação manual, e a conexão ECF é gerada de forma idempotente sem marcar o item à mão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller (139-08) → `ChecklistAdministrativoService` | `chave` e ator atravessam aqui; o service é a última linha de defesa antes da persistência |
| `checklist_administrativo_itens` → payload Inertia | dado persistido atravessa para o navegador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-05-01 | Spoofing | autoria falsificada por `user_id` vindo do corpo da requisição | mitigate | `concluirManualmente()`/`reabrirItem()` recebem `User $usuario` **tipado**, nunca `int`. Asserção de código-fonte de que não existe parâmetro `$userId`/`$user_id`. Mesma disciplina T-137-02 de `EtapaTransicaoService::transicionar()` |
| T-139-05-02 | Tampering | `chave` livre vinda de requisição criando linha arbitrária | mitigate | `ChecklistAdministrativoDefinicao::item($chave)` devolve `null` para chave fora do catálogo e o service lança `DomainException` antes de qualquer escrita — teste dedicado afirma que nada é gravado |
| T-139-05-03 | Elevation of Privilege | item do grupo Contrato marcado em empresa isenta | mitigate | `concluirManualmente()` recusa item de grupo `contrato` quando `exigeContrato()` é `false` (D-07), com teste |
| T-139-05-04 | Repudiation | item automático marcado à mão sem rastro de override | mitigate | Recusa por `DomainException` quando o catálogo declara `auto_fonte`; o parâmetro `$forcar` existe mas **não é exposto por HTTP** nesta fase (o plano 139-08 não o encaminha) |
| T-139-05-05 | Repudiation | autoria perdida quando o colaborador é desligado | mitigate | `feitoPor()->withTrashed()` (plano 139-02) + caso de teste que faz soft delete do autor e verifica que o nome continua no payload |
| T-139-05-06 | Information Disclosure | payload achatado vazando model inteiro | mitigate | O contrato de saída no bloco `<interfaces>` lista as chaves permitidas; nunca serializar o model — mesma disciplina T-131-04-04 já aplicada no `ContratoAdminController::show()` |
| T-139-05-SC | Tampering | npm/composer installs | mitigate | Nenhum pacote instalado — `139-RESEARCH.md` §Package Legitimacy Audit: "não aplicável" |
</threat_model>

<verification>
1. `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase139 tests/Feature/Phase139 --colors=never` sai 0.
2. `grep -v '^[[:space:]]*[/*]' app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php | grep -c -e 'EtapaTransicaoService' -e 'ChecklistEtapaSincronizadorService'` retorna 0 — este service não conhece a máquina de estados nem o sincronizador, e é isso que mantém o grafo de injeção acíclico.
3. Regressão: `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase137 tests/Feature/Phase137 tests/Feature/Phase138 --colors=never` igual à `139-BASELINE-TESTES.md`.
</verification>

<success_criteria>
- 9 itens com contrato, 6 sem, com o grupo Contrato ausente do payload da isenta
- Denominador do progresso vindo do catálogo, imune a chave órfã
- Marcação manual com autoria em par, desmarcação limpando os dois juntos
- Item automático recusa marcação manual; chave desconhecida recusa antes de escrever
- Conexão ECF gerada de forma idempotente, sem marcar o item por segunda fonte de verdade
</success_criteria>

<output>
Criar `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-05-SUMMARY.md` ao terminar.
</output>
