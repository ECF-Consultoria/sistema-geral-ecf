---
phase: 139-checklist-administrativo-trava-de-finaliza-o-v23-0
plan: 04
type: execute
wave: 3
depends_on: ["139-02", "139-03"]
files_modified:
  - app/Services/ChecklistAdministrativo/Resolvers/ContratoEnviadoResolver.php
  - app/Services/ChecklistAdministrativo/Resolvers/ContratoAssinadoResolver.php
  - app/Services/ChecklistAdministrativo/Resolvers/MlOAuthConectadoResolver.php
  - app/Services/ChecklistAdministrativo/Resolvers/ConexaoEcfResolver.php
  - tests/Feature/Phase139/ChecklistGrupoContratoAutoTest.php
  - tests/Feature/Phase139/ContratoAssinadoPorLiberacaoTest.php
  - tests/Feature/Phase139/MultiplosEnvelopesTest.php
  - tests/Feature/Phase139/ChecklistGrupoEntradaAutoTest.php
autonomous: true
requirements: [ADMIN-02, ADMIN-03]
user_setup: []

must_haves:
  truths:
    - "D-16: o item 3 (`contrato_assinado`) fecha por `contrato_assinaturas.assinado_em`/`status === assinado` **OU** por `ContratoLiberacao` existente para aquele serviço — empresa liberada pela via `manual`, que nunca escreve `contrato_assinaturas`, consegue fechar o item"
    - "D-18: com 2+ serviços que exigem contrato, os itens 2 e 3 só fecham quando TODOS estão no estado pedido — um assinado + um pendente deixa o item pendente (manda o mais atrasado)"
    - "D-05: o item 7 (`grant_consultoria_ml`) fecha somente quando o cliente CONECTOU (`ml_tokens.status === 'active'`), nunca quando o link OAuth foi gerado — link gerado expira em 7 dias e mentiria o tempo todo"
    - "D-14: o item 8 (`conexao_ecf_gerada`) fecha por EXISTÊNCIA da linha em `onboarding_links` — o resolver apenas LÊ, nunca chama `OnboardingLinkService::paraEmpresa()` (que é `firstOrCreate` e fecharia o item sozinho na primeira renderização da ficha)"
    - "D5 da milestone: nenhum resolver desta fase escreve em `contrato_assinaturas`, cria cliente HTTP de assinatura ou toca `contrato_liberacoes` — provado por reconsulta ao banco e por `Http::assertNothingSent()`"
  artifacts:
    - path: "app/Services/ChecklistAdministrativo/Resolvers/ContratoEnviadoResolver.php"
      provides: "Item 2 — leitura agregada de enviado_em por serviço que exige contrato"
    - path: "app/Services/ChecklistAdministrativo/Resolvers/ContratoAssinadoResolver.php"
      provides: "Item 3 — OR entre ContratoAssinatura e ContratoLiberacao, agregado por serviço"
      contains: "existeParaServico"
    - path: "app/Services/ChecklistAdministrativo/Resolvers/MlOAuthConectadoResolver.php"
      provides: "Item 7 — leitura de ml_tokens.status"
      contains: "mlToken"
    - path: "app/Services/ChecklistAdministrativo/Resolvers/ConexaoEcfResolver.php"
      provides: "Item 8 — leitura de existência de onboarding_links"
    - path: "tests/Feature/Phase139/ChecklistGrupoContratoAutoTest.php"
      provides: "ADMIN-02 + asserção da D5 (nada é escrito em contrato_assinaturas)"
  key_links:
    - from: "app/Services/ChecklistAdministrativo/Resolvers/ContratoAssinadoResolver.php"
      to: "app/Models/ContratoLiberacao.php"
      via: "ContratoLiberacao::existeParaServico()"
      pattern: "ContratoLiberacao::existeParaServico"
    - from: "app/Services/ChecklistAdministrativo/Resolvers/MlOAuthConectadoResolver.php"
      to: "app/Models/MlToken.php"
      via: "Company::mlToken relação hasOne"
      pattern: "\\$company->mlToken"
---

<objective>
Escrever os 4 resolvers automáticos do checklist — itens 2, 3, 7 e 8 — cada um lendo estado que já
existe em outra tabela, sem nunca escrever nada. Inclui a segunda fonte do item 3 (D-16) e a
agregação "manda o mais atrasado" quando a empresa tem 2+ serviços com contrato (D-18).

Purpose: são a metade automática do checklist. Cada um deles é leitura pura de um estado que outro
subsistema já mantém — nenhum deles duplica a fonte, nenhum deles reconstrói assinatura (D5).
Output: 4 resolvers + 4 arquivos de teste Feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-CONTEXT.md
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-RESEARCH.md
@.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-PATTERNS.md

**Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada`. No Bash, o PHP é
`/c/xampp/php/php.exe`. Nunca `php artisan test`.

<interfaces>
<!-- Criado pelo plano 139-03 — consumir exatamente estes nomes. -->
App\Contracts\ChecklistResolver
  chave(): string; label(): string; ajuda(): string;
  resolver(App\Models\Company $company): App\Services\ChecklistAdministrativo\ChecklistResolverResultado;

App\Services\ChecklistAdministrativo\ChecklistResolverResultado
  ::concluido(array $valor = []) | ::naoColetado(?string $motivo, array $valor = []) | ::indeterminado(string $motivo)

App\Services\ChecklistAdministrativo\ChecklistAdministrativoDefinicao
  AUTO_FONTE_CONTRATO_ENVIADO='contrato_enviado'  AUTO_FONTE_CONTRATO_ASSINADO='contrato_assinado'
  AUTO_FONTE_ML_OAUTH='ml_oauth_conectado'        AUTO_FONTE_CONEXAO_ECF='conexao_ecf'

<!-- Já existentes no código — assinaturas medidas, não supostas. -->
App\Models\Company
  contratosServico()                       // HasMany ContratoServico (Company.php:580)
  mlToken()                                // HasOne MlToken (Company.php:597)
App\Models\Servico
  exigeContrato(): bool                    // Servico.php:294
App\Models\ContratoAssinatura
  STATUS_RASCUNHO | STATUS_AGUARDANDO_ASSINATURAS | STATUS_ASSINADO | STATUS_RECUSADO
  | STATUS_EXPIRADO | STATUS_CANCELADO | STATUS_ERRO      // ContratoAssinatura.php:113-119
  colunas lidas: company_id, servico_id, status, enviado_em, assinado_em
App\Models\ContratoLiberacao
  static existeParaServico(int $companyId, int $servicoId): ?self    // ContratoLiberacao.php:129
  VIA_WEBHOOK='webhook' VIA_MANUAL='manual' VIA_RECONCILIACAO='reconciliacao'
App\Models\MlToken
  colunas: status ('active'|'expired'|'revoked'), ml_user_id, connected_at
App\Models\OnboardingLink
  coluna: company_id (unique)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContratoEnviadoResolver e ContratoAssinadoResolver (itens 2 e 3)</name>
  <files>app/Services/ChecklistAdministrativo/Resolvers/ContratoEnviadoResolver.php, app/Services/ChecklistAdministrativo/Resolvers/ContratoAssinadoResolver.php, tests/Feature/Phase139/ChecklistGrupoContratoAutoTest.php</files>
  <read_first>
    - `app/Services/Onboarding/Resolvers/MlTokenAtivoResolver.php` (arquivo inteiro, 69 linhas — a FORMA de um resolver: chave/label/ajuda/resolver, e o uso dos 3 estados do value object)
    - `app/Models/ContratoAssinatura.php` linhas 100-200 (as 7 constantes de status, `STATUS_EM_ANDAMENTO`, e os hooks — para saber o que NÃO tocar)
    - `app/Models/ContratoLiberacao.php` (arquivo inteiro, em especial `existeParaServico()` na linha 129 e as 3 vias nas linhas 61-70)
    - `app/Http/Controllers/ContratoAdminController.php` linhas 555-575 (como o controller já lê `contratosServico` ativos e os `ContratoAssinatura` da empresa — a mesma leitura, sem escrita)
    - `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-RESEARCH.md` Pitfall 2 e Pitfall 3 (a razão de D-16 e D-18)
  </read_first>
  <action>
Criar os dois resolvers em `app/Services/ChecklistAdministrativo/Resolvers/`, ambos implementando
`App\Contracts\ChecklistResolver`.

**Universo comum aos dois** (extrair num método privado em cada classe, ou num trait pequeno se
preferir — não criar service novo): os serviços da empresa que exigem contrato =
`$company->contratosServico` filtrados por `ativo === true` e `servico->exigeContrato() === true`.
Se essa lista vier **vazia**, devolver `ChecklistResolverResultado::indeterminado()` com motivo
"empresa sem serviço que exija contrato" — nunca `concluido`. Na prática o grupo Contrato nem é
montado nesse caso (D-07, plano 139-05), mas o resolver não pode responder "concluído" para um
universo vazio: é exatamente o modo de falha "vazio lido como zero" que o value object de 3 estados
existe para evitar.

**Envelope vigente por serviço** (decisão do planejador, registrar em comentário pt-BR): para cada
serviço que exige contrato, o envelope vigente é `ContratoAssinatura::where('company_id', ...)
->where('servico_id', ...)->orderByDesc('id')->first()`. Não é "todos os envelopes já criados": o
projeto cria uma **linha nova** a cada nova tentativa (`GatilhoContratoAdministrativoService` não
reaproveita a linha `erro` antiga), então agregar sobre linhas mortas de `erro`/`cancelado` travaria
o item para sempre. A regra "manda o mais atrasado" da D-18 é aplicada **entre serviços**, que é
onde os envelopes concorrentes de fato aparecem (`ContratoClicksignService::iniciarParaEmpresa()`
itera por grupo de serviço e pode criar um envelope por grupo).

`ContratoEnviadoResolver` — `chave()` devolve `ChecklistAdministrativoDefinicao::AUTO_FONTE_CONTRATO_ENVIADO`;
`label()` "Contrato enviado"; `ajuda()` em pt-BR explicando que lê `contrato_assinaturas.enviado_em`
e nunca escreve. `resolver()` devolve `concluido` somente quando **todos** os serviços que exigem
contrato têm envelope vigente com `enviado_em !== null`; caso contrário `naoColetado` com motivo
nomeando quantos serviços ainda faltam (ex.: "1 de 2 contratos ainda não foi enviado"). No `valor`
do `concluido`, gravar o mapa `servico_id => enviado_em` em ISO-8601.

`ContratoAssinadoResolver` — `chave()` devolve `AUTO_FONTE_CONTRATO_ASSINADO`; `label()`
"Contrato assinado". A condição por serviço é um **OR de duas fontes** (D-16): (a) envelope vigente
com `assinado_em !== null` **ou** `status === ContratoAssinatura::STATUS_ASSINADO`; **ou**
(b) `ContratoLiberacao::existeParaServico($company->id, $servicoId) !== null`. `concluido` só quando
**todos** os serviços satisfazem pelo menos uma das duas. No `valor`, registrar por serviço qual das
duas fontes fechou (`'assinatura'` ou `'liberacao'`) — é o que dá rastro na tela de por que o item
fechou sem envelope assinado. Comentário pt-BR obrigatório citando D-16 e a razão: das 3 vias de
liberação, a via `manual` (`ContratoAdminController::liberarManual()` →
`EmpresaOperacionalRouter::liberarEmpresa()`) **nunca** toca `status`/`assinado_em`, e existe
exatamente para "Clicksign fora do ar, cliente assinou fora do sistema".

**Proibido nos dois arquivos** (D5 da milestone): qualquer `save()`, `create()`, `update()`,
`updateOrCreate()` ou `firstOrCreate()` sobre `ContratoAssinatura` ou `ContratoLiberacao`; qualquer
uso de `Http::`; qualquer instanciação de `ContratoClicksignService`. Só `where`/`first`/`get`.

Criar `tests/Feature/Phase139/ChecklistGrupoContratoAutoTest.php` (criar o diretório) usando
`RefreshDatabase`, com o setup copiado de `ContratoAdminDetalheTest` (`servicoComContrato()`,
`empresaCompleta()`, `vincularServico()` com `ContratoServico::withoutEvents(...)`, mais
`Http::fake()` e `Queue::fake()` no `setUp()`). Casos:
1. empresa com 1 serviço que exige contrato e **sem** `ContratoAssinatura`: itens 2 e 3 `naoColetado`;
2. envelope com `enviado_em` preenchido e `assinado_em` nulo: item 2 `concluido`, item 3 `naoColetado`;
3. envelope com `status = STATUS_ASSINADO` e `assinado_em` preenchido: itens 2 e 3 `concluido`;
4. **D5** — depois de rodar os dois resolvers, reconsultar a linha de `contrato_assinaturas` no banco
   e afirmar que `status`, `enviado_em`, `assinado_em` e `updated_at` estão idênticos aos gravados no
   setup, e `Http::assertNothingSent()`. Comentário no teste dizendo que esta é a asserção da D5 da
   milestone: o grupo Contrato **lê** o envelope da v22.0, nunca o reconstrói.

Criar as linhas de `ContratoAssinatura` diretamente por `ContratoAssinatura::create([...])` no
teste — nunca disparando o job real.
  </action>
  <acceptance_criteria>
    - Os dois arquivos de resolver existem e contêm `implements ChecklistResolver`
    - `ContratoAssinadoResolver.php` contém a string literal `ContratoLiberacao::existeParaServico`
    - Nenhum dos dois arquivos contém `->save()`, `::create(`, `->update(`, `firstOrCreate` ou `Http::` (verificar ignorando linhas de comentário: `grep -v '^\s*[/*]' <arquivo> | grep -c '\->save()'` retorna 0)
    - `tests/Feature/Phase139/ChecklistGrupoContratoAutoTest.php` contém `Http::assertNothingSent()`
    - `tests/Feature/Phase139/ChecklistGrupoContratoAutoTest.php` contém `withoutEvents`
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ChecklistGrupoContratoAutoTest.php --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ChecklistGrupoContratoAutoTest.php --colors=never &amp;&amp; test 0 -eq $(grep -v '^[[:space:]]*[/*]' app/Services/ChecklistAdministrativo/Resolvers/ContratoEnviadoResolver.php app/Services/ChecklistAdministrativo/Resolvers/ContratoAssinadoResolver.php | grep -c -e '\->save()' -e '::create(' -e 'Http::') &amp;&amp; echo SEM_ESCRITA_OK</automated>
  </verify>
  <done>Itens 2 e 3 leem o estado do envelope (e, no item 3, também a liberação registrada), agregando por serviço, e um teste prova que nada foi escrito em `contrato_assinaturas`.</done>
</task>

<task type="auto">
  <name>Task 2: Testes dedicados da liberação manual (D-16) e dos múltiplos envelopes (D-18)</name>
  <files>tests/Feature/Phase139/ContratoAssinadoPorLiberacaoTest.php, tests/Feature/Phase139/MultiplosEnvelopesTest.php</files>
  <read_first>
    - `app/Services/Operacional/EmpresaOperacionalRouter.php` linhas 284-361 (`liberarEmpresa()` — o caminho da via `manual`, que grava `ContratoLiberacao` sem tocar `contrato_assinaturas`; chamar este método direto no teste, não o controller)
    - `app/Http/Controllers/ContratoAdminController.php` linhas 1195-1244 (`liberarManual()` — a validação e os argumentos que `liberarEmpresa()` espera, incluindo `contrato_assinatura_id` nullable)
    - `app/Services/ChecklistAdministrativo/Resolvers/ContratoAssinadoResolver.php` (criado na Task 1 — a régua sob teste)
    - `tests/Feature/Phase131/ContratoAdminDetalheTest.php` linhas 30-140 (o `setUp()` com `Http::fake()`/`Queue::fake()`/`config(['services.clicksign.signatarios_ecf' => []])` e os três helpers de setup)
  </read_first>
  <action>
Criar `tests/Feature/Phase139/ContratoAssinadoPorLiberacaoTest.php` com `RefreshDatabase` e o mesmo
`setUp()` blindado. Cenário central: empresa com 1 serviço que exige contrato, **sem nenhum**
`ContratoAssinatura` com `assinado_em`/`status = assinado`, mas com uma `ContratoLiberacao` criada
pela via `ContratoLiberacao::VIA_MANUAL` (chamar `EmpresaOperacionalRouter::liberarEmpresa()`
diretamente, com `$contrato = null`, `liberadoPorUserId` de um admin criado no teste, e um dos
motivos de `ContratoLiberacao::MOTIVOS_MANUAIS`). Afirmar:
1. `ContratoAssinadoResolver::resolver($company)->ehConcluido()` é `true`;
2. por reconsulta ao banco, que **nenhuma** linha de `contrato_assinaturas` ganhou `assinado_em` ou
   `status = assinado` como efeito da liberação manual — é a prova de que a segunda fonte era mesmo
   necessária, não um atalho;
3. o `valor` do resultado registra a fonte `'liberacao'` para aquele serviço.
Comentário no cabeçalho do arquivo em pt-BR: sem a D-16, esta empresa ficaria permanentemente
impedida de finalizar a entrada administrativa, sem saída pela tela.

Criar `tests/Feature/Phase139/MultiplosEnvelopesTest.php` com o mesmo setup. Cenário: uma empresa
com **dois** serviços distintos que exigem contrato (dois `Servico::create` com
`exige_contrato = true`, dois `ContratoServico::withoutEvents(...)`), e dois `ContratoAssinatura`,
um por serviço. Casos:
1. serviço A com `enviado_em` preenchido e serviço B sem: item 2 **pendente** (D-18);
2. serviço A `assinado` e serviço B `aguardando_assinaturas`: item 3 **pendente** — um assinado + um
   pendente não fecha;
3. os dois enviados: item 2 `concluido`; os dois assinados: item 3 `concluido`;
4. serviço A assinado por envelope e serviço B fechado por `ContratoLiberacao` manual: item 3
   `concluido` (cruzamento D-16 × D-18);
5. defesa da linha morta: serviço A com um envelope antigo em `STATUS_ERRO` **e** um envelope mais
   novo `assinado` (dois `ContratoAssinatura` para o MESMO serviço) — o item 3 fecha, porque o
   vigente é o mais recente por `orderByDesc('id')`, não a linha morta.

Comentário no cabeçalho em pt-BR: o Pitfall 3 do RESEARCH mediu que o schema permite 2+ envelopes
simultâneos e que isso nunca foi observado em produção — este teste é a única evidência que existe
do comportamento, e por isso as cinco combinações são escritas por extenso.
  </action>
  <acceptance_criteria>
    - Os dois arquivos de teste existem em `tests/Feature/Phase139/`
    - `ContratoAssinadoPorLiberacaoTest.php` contém as strings `VIA_MANUAL` e `liberarEmpresa`
    - `MultiplosEnvelopesTest.php` contém pelo menos 5 métodos `public function test_`
    - `MultiplosEnvelopesTest.php` contém a string `STATUS_ERRO`
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ContratoAssinadoPorLiberacaoTest.php tests/Feature/Phase139/MultiplosEnvelopesTest.php --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ContratoAssinadoPorLiberacaoTest.php tests/Feature/Phase139/MultiplosEnvelopesTest.php --colors=never</automated>
  </verify>
  <done>Existe prova automatizada de que empresa liberada pela via manual fecha o item 3, e de que 2+ serviços com contrato só fecham quando todos chegam ao estado.</done>
</task>

<task type="auto">
  <name>Task 3: MlOAuthConectadoResolver e ConexaoEcfResolver (itens 7 e 8)</name>
  <files>app/Services/ChecklistAdministrativo/Resolvers/MlOAuthConectadoResolver.php, app/Services/ChecklistAdministrativo/Resolvers/ConexaoEcfResolver.php, tests/Feature/Phase139/ChecklistGrupoEntradaAutoTest.php</files>
  <read_first>
    - `app/Services/Onboarding/Resolvers/MlTokenAtivoResolver.php` (arquivo inteiro — o RESEARCH recomenda cópia quase literal para o item 7; copiar a distinção entre `$token !== null` (revogado) e `$token === null` (nunca conectou))
    - `app/Models/MlToken.php` (arquivo inteiro — `$fillable`, `$casts` e os valores de `status`)
    - `app/Services/Onboarding/OnboardingLinkService.php` linhas 28-50 (`paraEmpresa()` — repare que é método de **instância** com `OnboardingEngineService` injetado no construtor, e que é `firstOrCreate`)
    - `app/Models/OnboardingLink.php` (a tabela e o unique em `company_id`)
    - `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-RESEARCH.md` Pitfall 1 (autorização OAuth falsa por clique interno)
  </read_first>
  <action>
Criar `MlOAuthConectadoResolver` implementando `ChecklistResolver`. `chave()` devolve
`ChecklistAdministrativoDefinicao::AUTO_FONTE_ML_OAUTH`; `label()` "Grant da consultoria (OAuth
Mercado Livre)"; `ajuda()` em pt-BR dizendo que confere `ml_tokens.status = active` para a empresa,
de forma síncrona e sem reautenticação. `resolver(Company $company)` lê `$company->mlToken` e:
`status === 'active'` → `concluido` com `valor` contendo `ml_user_id` e `conectado_em` (ISO-8601 de
`connected_at`); token existente com outro status → `naoColetado('Autorização do cliente foi
revogada')`; token ausente → `naoColetado('Cliente ainda não autorizou o acesso')`.

Comentário pt-BR obrigatório no docblock, citando D-05: o item fecha quando o **cliente conectou**,
nunca quando o link foi gerado — `companies.ml_link_url` tem TTL de 7 dias e o checklist estaria
mentindo o tempo todo. E citando o Pitfall 1: este resolver **não gera link** nem chama
`MercadoLivreService::buildAuthUrl()`; a geração é ação separada
(`POST /companies/{company}/ml/initiate`, que já existe) e a UI dela é "copiar link", nunca abrir —
clique interno de usuário ECF logado autoriza a PRÓPRIA conta do Mercado Livre, e o callback de
`Company` sobrescreve o token incondicionalmente, sem trava de divergência.

Criar `ConexaoEcfResolver` implementando `ChecklistResolver`. `chave()` devolve
`AUTO_FONTE_CONEXAO_ECF`; `label()` "Conexão com o sistema ECF gerada". `resolver()` faz **leitura
pura de existência**: `OnboardingLink::where('company_id', $company->id)->exists()` →
`concluido(['token_existe' => true])`, senão `naoColetado('Conexão com o sistema ECF ainda não foi
gerada')`.

⚠️ **Decisão explícita do planejador, a registrar em comentário pt-BR no arquivo:** este resolver
**não** chama `OnboardingLinkService::paraEmpresa()`, embora o `139-PATTERNS.md` cite esse método
como análogo. `paraEmpresa()` é `firstOrCreate` — chamá-lo de dentro de um resolver que roda a cada
carregamento da ficha criaria o link e fecharia o item 8 sozinho na primeira renderização, para toda
empresa, tornando o item decorativo e violando o ADMIN-03 ("gerados pelo próprio checklist, que
marca o item ao gerar"). A D-14 continua valendo na letra — "fecha por existência, não por clique" —
mas quem **cria** a linha é a ação explícita do usuário (endpoint do plano 139-08, que chama
`OnboardingLinkService::paraEmpresa()`); o resolver só observa. A idempotência medida pela D-14
segue sendo o que permite o botão ser acionado quantas vezes for.

Criar `tests/Feature/Phase139/ChecklistGrupoEntradaAutoTest.php` com `RefreshDatabase`. Casos:
1. empresa sem `MlToken`: item 7 `naoColetado` com motivo de "ainda não autorizou";
2. `MlToken` com `status = 'revoked'`: item 7 `naoColetado` com motivo de "revogada";
3. `MlToken` com `status = 'active'`: item 7 `concluido`, e o `valor` traz `ml_user_id`;
4. empresa sem `OnboardingLink`: item 8 `naoColetado`;
5. `OnboardingLink::create(['company_id' => ..., 'token' => ...])`: item 8 `concluido`;
6. **defesa do efeito colateral** — rodar `ConexaoEcfResolver::resolver()` numa empresa **sem**
   `OnboardingLink` e afirmar, por reconsulta ao banco, que continua existindo **zero** linha em
   `onboarding_links` para aquela empresa. É a prova de que o resolver não cria a linha.
  </action>
  <acceptance_criteria>
    - `MlOAuthConectadoResolver.php` contém `$company->mlToken` e a comparação com `'active'`
    - `MlOAuthConectadoResolver.php` **não** contém `buildAuthUrl` nem `MercadoLivreService`
    - `ConexaoEcfResolver.php` contém `OnboardingLink::where('company_id'` e `->exists()`
    - `ConexaoEcfResolver.php` **não** contém `paraEmpresa` fora de linha de comentário (`grep -v '^[[:space:]]*[/*]' ConexaoEcfResolver.php | grep -c 'paraEmpresa'` retorna 0)
    - `tests/Feature/Phase139/ChecklistGrupoEntradaAutoTest.php` contém `assertDatabaseCount('onboarding_links'` ou `assertDatabaseMissing('onboarding_links'`
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ChecklistGrupoEntradaAutoTest.php --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139/ChecklistGrupoEntradaAutoTest.php --colors=never &amp;&amp; test 0 -eq $(grep -v '^[[:space:]]*[/*]' app/Services/ChecklistAdministrativo/Resolvers/ConexaoEcfResolver.php | grep -c 'paraEmpresa') &amp;&amp; echo RESOLVER_LEITURA_OK</automated>
  </verify>
  <done>Item 7 fecha só com `ml_tokens.status = active`; item 8 fecha por existência de `onboarding_links` e o resolver comprovadamente não cria a linha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `contrato_assinaturas` (v22.0) → checklist | estado de outro subsistema atravessa aqui em modo somente-leitura; escrever seria reconstruir assinatura (D5) |
| conta Mercado Livre do cliente → `ml_tokens` | o OAuth do cliente é a evidência do item 7; a evidência pode ser falsificada por clique interno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-04-01 | Spoofing | autorização OAuth falsa (usuário ECF logado autoriza a própria conta ML) | mitigate | O resolver **não gera link**; o docblock registra que a geração é ação separada e que a UI é "copiar link", nunca `<a href>` que abre direto (Pitfall 1, `project_polos_oauth_link_boas_vindas_260827`). A trava de divergência existe só em `callbackPolos()`, nunca no fluxo de `Company` — não há defesa de servidor a copiar, a defesa é de desenho de UI (plano 139-09) |
| T-139-04-02 | Tampering | escrita acidental em `contrato_assinaturas` a partir do checklist | mitigate | Proibição explícita de `save/create/update/firstOrCreate/Http::` nos dois resolvers de contrato, verificada por asserção de código-fonte com filtro de comentários, mais reconsulta ao banco em `ChecklistGrupoContratoAutoTest` e `Http::assertNothingSent()` |
| T-139-04-03 | Repudiation | item 8 fechando sozinho sem ninguém ter gerado a conexão | mitigate | `ConexaoEcfResolver` faz `exists()`, nunca `firstOrCreate()`; teste dedicado afirma zero linha em `onboarding_links` depois de resolver uma empresa sem conexão |
| T-139-04-04 | Elevation of Privilege | empresa liberada pela via `manual` travada para sempre no ADMIN-05 (negação de serviço de negócio) | mitigate | Segunda fonte `ContratoLiberacao::existeParaServico()` no item 3 (D-16), com teste dedicado |
| T-139-04-05 | Information Disclosure | `valor` do resolver vazando dado sensível para o payload da tela | mitigate | O `valor` guarda apenas `servico_id`, timestamps ISO-8601, `ml_user_id` e a fonte de fechamento — nunca token, nunca chave de API, nunca dado de signatário |
| T-139-04-SC | Tampering | npm/composer installs | mitigate | Nenhum pacote instalado — `139-RESEARCH.md` §Package Legitimacy Audit: "não aplicável" |
</threat_model>

<verification>
1. `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase139 --colors=never` sai 0.
2. Nenhum arquivo em `app/Services/ChecklistAdministrativo/Resolvers/` contém `->save()`, `::create(`, `->update(`, `firstOrCreate` ou `Http::` fora de comentário.
3. `grep -q 'ContratoLiberacao::existeParaServico' app/Services/ChecklistAdministrativo/Resolvers/ContratoAssinadoResolver.php` passa.
4. Regressão: a suíte 137/138 continua igual à `139-BASELINE-TESTES.md`.
</verification>

<success_criteria>
- Os 4 resolvers existem, implementam `ChecklistResolver` e devolvem os 3 estados do value object
- Item 3 fecha pelas duas fontes (D-16); itens 2 e 3 agregam por serviço (D-18)
- Item 7 fecha só com token ativo (D-05); item 8 fecha por existência sem criar nada (D-14)
- Prova automatizada de que nada é escrito em `contrato_assinaturas` e nenhum HTTP é disparado (D5)
</success_criteria>

<output>
Criar `.planning/phases/139-checklist-administrativo-trava-de-finaliza-o-v23-0/139-04-SUMMARY.md` ao terminar.
</output>
