---
phase: 133-liga-o-bloqueio-ativa-o-real-v22-0
plan: 05
type: execute
wave: 3
depends_on: [133-04]
files_modified:
  - .planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md
autonomous: false
requirements: [FLUXO-01, FLUXO-02, FLUXO-09]

must_haves:
  truths:
    - "Com a chave ligada em produção, a faixa aparece na tela de Contratos do Administrativo — e some quando ela é desligada"
    - "O próximo cadastro real de Polos gerou ficha operacional com a chave ligada, conferido por reconsulta ao banco e não pela tela"
    - "Se a ficha não tiver nascido, a chave foi desligada na hora e o fato está registrado"
    - "Nenhuma ativação manual legítima foi recusada por engano nas primeiras 48 horas — conferido no log"
    - "O 133-ROLLOUT.md está preenchido: o que era campo em branco virou resultado datado"
  artifacts:
    - path: ".planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md"
      provides: "Campos de resultado preenchidos: faixa conferida, primeiro cadastro real de Polos, varredura de recusas no log"
      contains: "Campos de resultado"
  key_links:
    - from: ".planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md"
      to: "mlb_empresas (produção)"
      via: "reconsulta por company_id após o primeiro cadastro real de Polos"
      pattern: "MlbEmpresa"
---

<objective>
Provar em produção que **nada quebrou** (D-01/D-05).

Com a chave ligada (plano 133-04), a exceção por serviço passa a ser exercitada de verdade. A prova
combinada pelo usuário é o **próximo cadastro real de Polos**: se a ficha nascer, a exceção está
provada; se não nascer, **desligar na hora** — a própria chave é a saída, nunca rollback de código.

⛔ **Nada de empresa de teste.** O usuário recusou explicitamente criar empresa fictícia para
antecipar a prova (diferente do que aceitou na Fase 132). A espera é deliberada.

⚠️ Este plano pode ficar **aberto por dias** — depende de um cadastro real acontecer. Isso é
esperado e não é bloqueio da fase; o que não pode acontecer é a chave ficar ligada sem ninguém
conferir o primeiro caso.

Propósito: sem esta observação, a fase entrega uma trava que nunca foi exercitada contra o mundo
real. Saída: o `133-ROLLOUT.md` com os campos de resultado preenchidos — ou a chave desligada e o
motivo registrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-CONTEXT.md
@.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md
@.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-VALIDATION.md
@CLAUDE.md

<interfaces>
**Reconsulta da ficha de uma empresa específica** (a prova da D-05 — nunca pela tela):

`php artisan tinker --execute="\$e=App\Models\MlbEmpresa::where('company_id', ID)->first(); echo \$e ? \$e->id . '|' . \$e->tipo . '|' . \$e->projeto . '|' . \$e->created_at : 'SEM FICHA';"`

**Contagem geral, para comparar com a baseline do `133-ROLLOUT.md`:**

`php artisan tinker --execute="echo App\Models\MlbEmpresa::count(), '|', App\Models\MlbEmpresa::where('tipo','POLO')->count();"`

**Estado da chave:**

`php artisan tinker --execute="echo app(App\Services\Operacional\EmpresaOperacionalRouter::class)->bloqueioAtivo() ? 'ligado' : 'desligado';"`

**Desligar (saída de emergência, D-05):**

`php artisan tinker --execute="App\Models\Configuracao::set(App\Services\Operacional\EmpresaOperacionalRouter::CHAVE_BLOQUEIO, '0');"`

**Rastros deixados pelo código dos planos 133-01 e 133-02** (é por eles que se procura efeito
indevido no log do VPS, `storage/logs/laravel*.log`):
- retenção no roteamento: `[Administrativo] Roteamento operacional retido pelo gate administrativo.`
  com `company_id` e `servicos_retidos`
- recusa de ativação manual: `[Administrativo] Ativação manual retida pelo gate administrativo.`
  com `company_id`, `user_id` e `tipo_pedido`

**Fato de produção que orienta a leitura do resultado** (medido em 2026-08-18): 486 fichas, todas
`POLO`; zero de Assessoria/Incubadora. Portanto, **retenção esperada nas primeiras semanas: zero**.
Uma linha de "Roteamento operacional retido" com serviço isento, ou qualquer "Ativação manual
retida" de empresa legítima de Polos, é sinal de que a exceção falhou para o lado errado — critério
de desligar.

⚠️ **A leitura errada mais provável desta fase:** procurar retenção, não achar, e concluir que a
trava não funciona. Não funcionar seria o contrário: uma ficha de Assessoria nascendo com a chave
ligada, ou uma ficha de Polos deixando de nascer.
</interfaces>
</context>

<aviso_de_ambiente>
⚠️ **Nenhuma mudança de código neste plano.** Só conferência em produção e preenchimento do roteiro.

⚠️ **Proibido `php artisan cache:clear` no VPS** (incidente de 2026-07-30 — derruba o site).

⚠️ **Conferência sempre por reconsulta ao banco**, nunca por stdout de comando de escrita e nunca
pela tela (disciplina das Fases 130/132).

⚠️ **Nada de empresa de teste em produção.**
</aviso_de_ambiente>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Conferir a faixa na tela, com a chave ligada e com ela desligada</name>
  <files>.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md</files>
  <read_first>
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md`, seção "Conferir depois de ligar"
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-03-SUMMARY.md` — o texto exato da faixa entregue
  </read_first>
  <what-built>
    O plano 133-03 acrescentou uma faixa no topo de `/administrativo/contratos` que aparece quando o
    bloqueio está ligado e explica, sem jargão, que as empresas da lista estão aguardando a
    assinatura do contrato para entrar na operação. A prop é calculada no servidor; o teste
    automatizado prova a prop nos três estados da chave, mas não prova o pixel.
  </what-built>
  <how-to-verify>
    1. Com a chave **ligada** (estado deixado pelo plano 133-04), abrir
       `https://admin.ecfconsultoria.com.br/administrativo/contratos` com um usuário que tenha
       `admin.contratos`.
    2. Conferir: a faixa âmbar aparece **entre** o título "Contratos" e o grid de situações; o grid
       de resumo continua sendo a primeira coisa que se lê depois dela; a lista de empresas continua
       exatamente como antes (nenhuma linha sumiu).
    3. Ler o texto em voz alta e responder: **dá para entender sem saber como o sistema funciona
       por dentro?** Se aparecer qualquer palavra de bastidor ("flag", "roteamento", "ficha
       operacional", "interruptor"), reprovar.
    4. Pedir para desligar a chave por um instante (comando no `<interfaces>`), recarregar a tela e
       confirmar que a faixa **some** e a tela volta a ser a da Fase 131.
    5. **Ligar a chave de novo** e confirmar por reconsulta que voltou a `ligado` — este passo não é
       opcional.
    6. Registrar o resultado (aprovado / ajustes) no `133-ROLLOUT.md`, seção "Campos de resultado".
  </how-to-verify>
  <acceptance_criteria>
    - A faixa aparece com a chave ligada e some com ela desligada — os dois estados conferidos na mesma sessão
    - A lista e o grid de resumo estão intactos; nenhuma linha de empresa desapareceu por causa da chave
    - Nenhuma palavra de bastidor no texto visível (UI-06)
    - Ao final, `bloqueioAtivo()` volta a imprimir `ligado` — conferido por reconsulta, não pela tela
    - O resultado está escrito no `133-ROLLOUT.md` com data/hora e quem conferiu
  </acceptance_criteria>
  <action>
    Conduzir a conferencia visual com o usuario seguindo `<how-to-verify>`, e registrar o resultado
    no `133-ROLLOUT.md` (secao "Campos de resultado") com data/hora e quem conferiu. Executar os
    comandos de desligar e religar a chave apenas com o usuario acompanhando, e confirmar o estado
    final por reconsulta. Nenhuma alteracao de codigo nesta task: se o texto da faixa precisar mudar,
    registrar o ajuste pedido e abrir a correcao no plano 133-03, nao editar o `.jsx` aqui.
  </action>
  <verify>
    <human-check>Faixa visivel com a chave ligada, ausente com ela desligada, e `bloqueioAtivo()` imprimindo `ligado` ao final</human-check>
  </verify>
  <done>A faixa foi conferida nos dois estados da chave, o texto foi aprovado sem jargao, a chave voltou a ficar ligada e o resultado esta escrito no `133-ROLLOUT.md`.</done>
  <resume-signal>Responder "aprovado" ou descrever o que precisa mudar no texto/posição da faixa</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: O primeiro cadastro real de Polos com a chave ligada (D-05)</name>
  <files>.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md</files>
  <read_first>
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md`, seções "Critério de parar" e "Campos de resultado"
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-CONTEXT.md`, decisões D-01 e D-05
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-01-SUMMARY.md` e `133-02-SUMMARY.md` — as mensagens de log que o código passou a emitir
  </read_first>
  <what-built>
    Com a chave ligada, o roteamento passou a decidir **por serviço**: quem exige contrato aguarda a
    etapa administrativa; Polos, que é isento, continua entrando direto. A porta dos fundos do time
    de Publicação também passou a respeitar a mesma regra, decidindo pelos serviços realmente
    contratados. Nada disso foi exercitado ainda contra um caso real.
  </what-built>
  <how-to-verify>
    1. Esperar o **próximo cadastro real de uma empresa de Polos** — pelo Comercial ou pelo webhook
       do HubSpot. ⛔ Não criar empresa de teste.
    2. Anotar o `company_id` da empresa recém-cadastrada.
    3. Reconsultar a ficha no VPS com o comando do `<interfaces>`. Esperado: uma linha com
       `tipo = POLO`, `projeto = POLOS` e `created_at` no momento do cadastro.
       - **Nasceu ficha** → a exceção está provada. Registrar no `133-ROLLOUT.md` e seguir para o
         passo 4.
       - **Saiu `SEM FICHA`** → **desligar a chave imediatamente** com o comando do `<interfaces>`,
         registrar o `company_id`, a hora e a saída do comando no `133-ROLLOUT.md`, e abrir a
         correção. A chave é a saída — não tentar corrigir com deploy antes de desligar.
    4. Comparar a contagem geral de `mlb_empresas` com a baseline registrada no `133-ROLLOUT.md`:
       deve ter subido exatamente pelas fichas legítimas do período, nunca ter caído.
    5. Varrer o log do VPS das primeiras 48 horas procurando as duas mensagens do `<interfaces>`.
       Esperado, dado o estado da base: **nenhuma** ou apenas retenções de serviço que de fato exige
       contrato. Qualquer "Ativação manual retida" de empresa legítima de Polos é sinal de recusa
       indevida — registrar e avaliar desligar.
    6. Preencher os campos de resultado do `133-ROLLOUT.md` e registrar a conclusão em uma frase:
       *a trava está ligada e nada quebrou* — ou o que quebrou e o que foi feito.
  </how-to-verify>
  <acceptance_criteria>
    - A reconsulta da ficha foi feita no banco de produção, por `company_id`, e a saída está copiada no `133-ROLLOUT.md` (não parafraseada)
    - A contagem de `mlb_empresas` não caiu em relação à baseline
    - A varredura do log foi feita e o resultado registrado — inclusive quando o resultado é "nenhuma ocorrência", que é o esperado
    - Se a ficha não nasceu: a chave foi desligada **antes** de qualquer tentativa de correção, com hora registrada
    - Nenhum `cache:clear` executado; nenhuma empresa de teste criada em produção
    - O `133-ROLLOUT.md` não tem mais campo de resultado em branco
  </acceptance_criteria>
  <action>
    Aguardar o proximo cadastro real de Polos e conduzir a conferencia de `<how-to-verify>` com o
    usuario. Copiar para o `133-ROLLOUT.md` a SAIDA LITERAL dos comandos de reconsulta (ficha por
    `company_id`, contagem geral, estado da chave) — nunca a parafrase. Registrar `company_id`, e
    nao nome de empresa, e-mail ou telefone, porque o arquivo e versionado.
    Se a reconsulta devolver `SEM FICHA`: desligar a chave IMEDIATAMENTE com o comando do
    `<interfaces>`, registrar a hora e a saida, e parar — sem tentar corrigir por deploy antes de
    desligar. Ao final, varrer o log das primeiras 48h atras das duas mensagens `[Administrativo]` e
    registrar o resultado inclusive quando for "nenhuma ocorrencia".
  </action>
  <verify>
    <human-check>Saida literal da reconsulta por `company_id` colada no `133-ROLLOUT.md`, contagem de `mlb_empresas` nao inferior a baseline, e varredura de log registrada</human-check>
  </verify>
  <done>O primeiro cadastro real de Polos com a chave ligada foi conferido por reconsulta ao banco; o resultado (ficha nasceu / chave desligada) esta registrado no `133-ROLLOUT.md` sem campo em branco.</done>
  <resume-signal>Responder "ficha nasceu" (com o `company_id` e a saída da reconsulta), "sem ficha — chave desligada" (com a hora), ou "ainda aguardando cadastro real"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Produção → conferência humana | A única prova de que a exceção funciona no mundo real passa por alguém olhar o banco no momento certo. Se ninguém olhar, a trava fica ligada sem evidência. |
| Log do VPS → decisão de manter ou desligar | As duas mensagens de `Log::warning` são o único rastro de retenção. Se o log rotacionar antes da varredura, a evidência some. |
| Tela → julgamento do observador | A tela mostra o estado do cadastro, não o estado da ficha operacional. Concluir pela tela é a forma mais fácil de aprovar um resultado errado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-22 | Denial of Service | Polos deixando de ser roteado com a chave ligada | mitigate | Reconsulta por `company_id` no primeiro cadastro real, com critério binário (`SEM FICHA` = desligar na hora, antes de qualquer correção). O comando de desligar está escrito por extenso no roteiro e neste plano. |
| T-133-23 | Denial of Service | Ativação manual legítima recusada por engano (fail-safe da D-07 pegando empresa sem `ContratoServico`) | mitigate | Varredura do log das primeiras 48h procurando `[Administrativo] Ativação manual retida` — é justamente por isso que o plano 133-02 exigiu `company_id`, `user_id` e `tipo_pedido` na mensagem. Sem esse rastro, uma recusa indevida seria silenciosa. |
| T-133-24 | Repudiation | Conferência declarada mas não feita | mitigate | O `133-ROLLOUT.md` exige a **saída copiada** do comando, não uma paráfrase, com data/hora e quem conferiu. "Conferi e estava certo" não é registro verificável. |
| T-133-25 | Spoofing | Conferir pela tela em vez do banco | mitigate | Proibição explícita no aviso de ambiente e nos dois checkpoints; o comando de reconsulta está escrito por extenso para não haver desculpa de "não sabia como olhar". A tela mostra cadastro, não ficha. |
| T-133-26 | Information Disclosure | Nome/dado de cliente real no roteiro versionado | mitigate | Registrar `company_id` e a saída técnica do comando; **não** copiar nome de empresa, e-mail ou telefone para o arquivo versionado. |
| T-133-SC | Tampering (supply chain) | npm/composer install | accept | **Nenhuma instalação de pacote nesta fase.** Este plano não altera código nem dependências; só executa consultas de leitura e, no pior caso, a escrita de um valor `'0'` na tabela `configuracoes`. |
</threat_model>

<verification>
- A faixa foi vista na tela com a chave ligada e desaparece com ela desligada; a chave voltou a `ligado` ao final
- A ficha do primeiro cadastro real de Polos existe, conferida por `company_id` no banco de produção
- A contagem de `mlb_empresas` não caiu em relação à baseline do `133-ROLLOUT.md`
- A varredura do log das primeiras 48h está registrada, mesmo quando o resultado é "nenhuma ocorrência"
- `133-ROLLOUT.md` sem campo de resultado em branco
</verification>

<success_criteria>
- SC 2b conferido em produção no dia do rollout: Polos continua indo direto para a operação com o bloqueio ligado
- SC 4 exercitado de verdade: desligar a chave devolveu a tela e o comportamento de antes, na hora, sem deploy
- Nenhuma retenção indevida registrada no log
- A conclusão da fase está escrita em uma frase honesta: a trava está ligada e nada quebrou — ou o que quebrou e o que foi feito
</success_criteria>

<output>
Criar `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-05-SUMMARY.md` ao terminar.
</output>
