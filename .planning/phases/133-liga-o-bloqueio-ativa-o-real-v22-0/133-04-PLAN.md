---
phase: 133-liga-o-bloqueio-ativa-o-real-v22-0
plan: 04
type: execute
wave: 2
depends_on: [133-01, 133-02, 133-03]
files_modified:
  - .planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md
autonomous: false
requirements: [FLUXO-01, FLUXO-02, FLUXO-09]

must_haves:
  truths:
    - "Existe um roteiro escrito e versionado com o passo a passo de ligar, o comando de conferir e o de desligar — e o critério de parar"
    - "As quatro pré-condições do checkpoint humano do ROADMAP foram confirmadas com o usuário, uma a uma, antes de qualquer ativação"
    - "A chave administrativo_bloqueio_ativo está ligada em produção, conferida por reconsulta ao banco e não pela tela"
    - "Desligar a chave devolve o sistema ao comportamento de antes na hora, sem deploy e sem rollback de código"
    - "Fica registrado quem ligou, quando, e qual foi o commit implantado — a chave não tem trilha própria de autoria"
  artifacts:
    - path: ".planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md"
      provides: "Roteiro da ativação: pré-condições, comandos de ligar/conferir/desligar, critério de parar e campos de resultado"
      min_lines: 70
  key_links:
    - from: ".planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md"
      to: "App\\Services\\Operacional\\EmpresaOperacionalRouter::CHAVE_BLOQUEIO"
      via: "comandos de tinker escritos por extenso, com o nome da constante"
      pattern: "administrativo_bloqueio_ativo"
---

<objective>
Ligar a chave em produção — com autorização explícita, roteiro escrito e conferência por reconsulta
ao banco.

Os três planos anteriores tornaram a chave **segura de ligar**: a exceção por serviço existe, a
porta dos fundos está fechada e a tela conta a consequência. Este plano faz a virada.

⚠️ O ROADMAP condiciona a ativação a **quatro pré-condições** que só o usuário pode confirmar:
(a) o webhook chegou de forma confiável durante o período de observação (Fases 128/129 em produção);
(b) o alerta de contrato preso já disparou ao menos uma vez em sandbox (Fase 130);
(c) a liberação manual já foi testada em produção ao menos uma vez (Fase 130);
(d) o cutover para a Clicksign de produção foi concluído e aprovado (Fase 132).

⚠️ **A saída de emergência é a própria chave, nunca rollback de código** (D-05). Se algo der errado,
`Configuracao::set(..., '0')` devolve o comportamento de antes no mesmo segundo — sem deploy, sem
migration, sem espera.

⚠️ **Não existe UI de toggle** para esta chave (nem para a irmã da Fase 132). O padrão do projeto é
`php artisan tinker` no VPS. Isso é deliberado: um botão que para o operacional inteiro não deve
existir sem fricção.

Propósito: sem a ativação, os três planos anteriores são código que ninguém exercita.
Saída: o `133-ROLLOUT.md` preenchido e a chave ligada em produção, conferida por reconsulta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-CONTEXT.md
@.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-RESEARCH.md
@.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md
@CLAUDE.md

<interfaces>
**A chave e como se lê** (`app/Services/Operacional/EmpresaOperacionalRouter.php:44-63`):

```php
public const CHAVE_BLOQUEIO = 'administrativo_bloqueio_ativo';
public function bloqueioAtivo(): bool { return Configuracao::get(self::CHAVE_BLOQUEIO, '0') === '1'; }
```

Convenção: booleano persistido como string `'1'`/`'0'`; **ausência da linha significa desligado** —
não existe migration de seed gravando valor nenhum.

**Comandos, para o VPS (padrão idêntico ao usado nos planos 132-02/03):**

- ligar: `php artisan tinker --execute="App\Models\Configuracao::set(App\Services\Operacional\EmpresaOperacionalRouter::CHAVE_BLOQUEIO, '1');"`
- conferir: `php artisan tinker --execute="echo app(App\Services\Operacional\EmpresaOperacionalRouter::class)->bloqueioAtivo() ? 'ligado' : 'desligado';"`
- desligar: `php artisan tinker --execute="App\Models\Configuracao::set(App\Services\Operacional\EmpresaOperacionalRouter::CHAVE_BLOQUEIO, '0');"`

Localmente (se for preciso ensaiar): `C:\xampp\php\php.exe artisan tinker --execute="..."`.

**Reconsulta de evidência — quantas fichas existem antes e depois** (usar sempre este formato, e
nunca a tela):

`php artisan tinker --execute="echo App\Models\MlbEmpresa::count(), '|', App\Models\MlbEmpresa::where('tipo','POLO')->count();"`

**Estado medido em produção em 2026-08-18** (baseline para comparar): 486 fichas em `mlb_empresas`,
todas `POLO`; zero de Assessoria ou Incubadora; 190 empresas em `companies`; 4 com ficha vinculada.

**Deploy:** `deploy.sh` na raiz (VPS `177.7.53.164`, `plink`/`pscp`). Exige árvore de trabalho limpa
e **autorização explícita do usuário** (CLAUDE.md). ⚠️ A árvore é compartilhada com outro
desenvolvedor e com outras sessões do Claude Code: o deploy publica o trabalho de todas as sessões
juntas — confirmar com o usuário antes.

⚠️ **Nunca rodar `php artisan cache:clear` no VPS.** Incidente de 2026-07-30: apaga o cache aquecido
da Adman, o dashboard passa a esperar a API, os workers do php-fpm ficam ocupados e até o login
para. Se for necessário limpar algo, `systemctl reload php8.2-fpm`.

**Precedente do roteiro:** `.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md`
— seções, campos de resultado em branco, procedimento numerado de voltar atrás e critério de parar.
</interfaces>
</context>

<aviso_de_ambiente>
⚠️ **Este plano é o único que toca produção.** Nenhuma linha de código de aplicação muda aqui.

⚠️ **Deploy exige autorização explícita** e árvore de trabalho limpa (compartilhada com outra
sessão/dev — commitar só os caminhos deste plano).

⚠️ **Proibido `cache:clear` no VPS** (ver `<interfaces>`).

⚠️ **Nada de empresa de teste.** O usuário recusou criar empresa fictícia para provar a exceção
(diferente da Fase 132): a prova vem do próximo cadastro real de Polos, no plano 133-05.
</aviso_de_ambiente>

<tasks>

<task type="auto">
  <name>Task 1: `133-ROLLOUT.md` — o roteiro escrito da virada</name>
  <files>.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md</files>
  <read_first>
    - `.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md` inteiro — o molde de roteiro desta milestone
    - `.planning/ROADMAP.md`, seção "Phase 133" — os Success Criteria 1, 2, 2b, 3, 4, 5 e o texto integral do checkpoint humano (as quatro pré-condições)
    - `app/Services/Operacional/EmpresaOperacionalRouter.php` linhas 44-70
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-CONTEXT.md`, decisões D-01 e D-05
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-01-SUMMARY.md`, `133-02-SUMMARY.md`, `133-03-SUMMARY.md` — o que de fato foi entregue nos três planos anteriores
  </read_first>
  <action>
    Criar `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md`, em pt-BR, com
    estas seções (nomes exatos, porque a verificação da fase confere por elas):

    **`## O que muda quando a chave liga`** — em linguagem simples: empresa cujo serviço exige
    contrato passa a aguardar a etapa administrativa; Polos continua entrando direto. E o aviso
    honesto da D-01: **hoje o efeito prático é nulo** — não existe nenhuma ficha de Assessoria ou
    Incubadora na base, então nada será retido de imediato. A prova de sucesso é "nada quebrou".

    **`## Pré-condições (checkpoint humano do ROADMAP)`** — as quatro, (a) a (d), cada uma com uma
    caixa `- [ ]` e espaço para quem confirmou e quando. Copiar o texto do ROADMAP, não parafrasear.

    **`## Antes de ligar`** — checklist: os três planos anteriores commitados; a suíte da fase verde
    (`tests/Feature/Phase133`, `Phase124KillSwitchTest`, `Phase128`); `npm run build` rodado; deploy
    autorizado e concluído; e a contagem-baseline de `mlb_empresas` registrada (comando do
    `<interfaces>`).

    **`## Ligar`** — o comando de tinker por extenso, seguido do comando de conferência. Registrar
    que a conferência é por **reconsulta** e nunca pela tela.

    **`## Conferir depois de ligar`** — três conferências imediatas: (1) `bloqueioAtivo()` devolve
    `ligado`; (2) a contagem de `mlb_empresas` não mudou no ato de ligar; (3) a tela
    `/administrativo/contratos` mostra a faixa.

    **`## Critério de parar (e desligar na hora)`** — três gatilhos concretos, cada um com o comando
    de desligar logo abaixo: (1) um cadastro real de Polos **não** gerou ficha; (2) apareceu recusa
    de ativação manual que não deveria existir (`Log::warning` com `[Administrativo] Ativação manual
    retida`); (3) qualquer erro 500 nos caminhos de cadastro. Registrar em negrito que **rollback de
    código nunca é o plano de saída** — a chave é.

    **`## Como voltar atrás`** — o comando de desligar, a afirmação de que o efeito é imediato e não
    exige deploy nem migration, e o lembrete de que empresas retidas enquanto a chave esteve ligada
    **não** são roteadas retroativamente ao desligar: quem ficou de fora entra pela liberação manual
    (Fase 130) ou pelo webhook de assinatura.

    **`## Campos de resultado`** — em branco, para o plano 133-05 preencher: data/hora da ativação,
    quem autorizou, quem executou, commit implantado (`git rev-parse --short HEAD`), contagem de
    `mlb_empresas` antes e depois, e o resultado do primeiro cadastro real de Polos.

    ⛔ Nenhum segredo, token, senha de VPS ou dado de cliente real no arquivo — ele é versionado.
  </action>
  <verify>
    <automated>test -f .planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md && grep -c "^## " .planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe e o `grep -c "^## "` retorna 7 ou mais (as sete seções obrigatórias)
    - `grep -c "administrativo_bloqueio_ativo\|CHAVE_BLOQUEIO" <arquivo>` retorna 3 ou mais — ligar, conferir e desligar estão escritos por extenso
    - `grep -c "^- \[ \]" <arquivo>` retorna 4 ou mais — as quatro pré-condições têm caixa própria
    - `grep -c "reconsulta" <arquivo>` retorna 1 ou mais — a disciplina de conferir pelo banco está escrita
    - `grep -ci "rollback de código" <arquivo>` retorna 1 ou mais — a saída declarada é a chave, não o deploy
    - `grep -ci "cache:clear" <arquivo>` retorna 1 ou mais e a menção é uma **proibição** (conferir por leitura)
    - `grep -nE "[A-Za-z0-9_\-]{30,}" <arquivo>` não retorna nada que pareça token, senha ou hostkey
    - O arquivo tem 70 linhas ou mais (`wc -l`)
  </acceptance_criteria>
  <done>O roteiro existe, versionado, com pré-condições, comandos por extenso, critério de parar, procedimento de voltar atrás e campos de resultado em branco.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirmar as quatro pré-condições e autorizar a virada</name>
  <files>.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md</files>
  <read_first>
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md` (criado na Task 1)
    - `.planning/ROADMAP.md`, o checkpoint humano da Phase 133
    - `.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md` — o gate (d), já aprovado
  </read_first>
  <decision>Ligar `administrativo_bloqueio_ativo` em produção agora?</decision>
  <context>
    Apresentar ao usuário, antes de perguntar:
    - O resultado da suíte da fase (`Tests: N` de `tests/Feature/Phase133`, `Phase124KillSwitchTest`, `Phase128`)
    - A contagem-baseline de `mlb_empresas` (total e `POLO`)
    - As quatro pré-condições do ROADMAP, uma a uma, pedindo confirmação individual: (a) webhook confiável no período de observação; (b) alerta de contrato preso disparou em sandbox; (c) liberação manual testada em produção; (d) cutover Clicksign concluído e aprovado
    - O aviso honesto da D-01: **com a chave ligada, nada será retido hoje** — não existe ficha de Assessoria ou Incubadora na base. A prova de sucesso é "nada quebrou", não "algo foi bloqueado". Quem esperar ver retenção vai concluir, errado, que a fase falhou
    - Que o deploy publica o trabalho de **todas** as sessões e do outro desenvolvedor que compartilham a árvore
  </context>
  <options>
    <option id="ligar-agora">
      <name>Ligar agora</name>
      <pros>Fecha a milestone; a trava passa a existir de verdade para quando a primeira Assessoria/Incubadora entrar</pros>
      <cons>Exige deploy (publica o trabalho de todas as sessões); qualquer erro na exceção por serviço para o único fluxo vivo até alguém desligar</cons>
    </option>
    <option id="adiar">
      <name>Adiar a ativação</name>
      <pros>Código entregue e provado por teste, sem tocar produção; pode ligar em janela mais tranquila</pros>
      <cons>Os SC 1/2/2b/3/5 não são exercitados em produção; a fase fica sem a prova empírica</cons>
    </option>
    <option id="parar">
      <name>Parar — alguma pré-condição não está atendida</name>
      <pros>Respeita o checkpoint do ROADMAP; evita ligar com a rede de segurança incompleta</pros>
      <cons>Exige voltar à fase correspondente antes de retomar</cons>
    </option>
  </options>
  <acceptance_criteria>
    - As quatro pré-condições foram apresentadas **individualmente** e a resposta de cada uma foi registrada no `133-ROLLOUT.md` (caixa marcada, com quem confirmou e quando)
    - A autorização de deploy é explícita, nesta conversa — autorização genérica anterior não vale para este passo, porque a árvore é compartilhada
    - Se a escolha for `adiar` ou `parar`, a Task 3 **não** é executada e o motivo fica registrado no `133-ROLLOUT.md` e no SUMMARY
  </acceptance_criteria>
  <action>
    Apresentar ao usuario, NESTA ordem e sem resumir: (1) a saida real da suite da fase
    (`Tests: N` de `tests/Feature/Phase133`, `tests/Feature/Phase124KillSwitchTest.php` e
    `tests/Feature/Phase128`); (2) a contagem-baseline de `mlb_empresas` (total e `POLO`) obtida por
    reconsulta no VPS; (3) as quatro pre-condicoes (a) a (d) do checkpoint humano do ROADMAP, UMA A
    UMA, pedindo confirmacao individual de cada; (4) o aviso da D-01 de que nada sera retido hoje; e
    (5) o fato de o deploy publicar o trabalho de todas as sessoes que compartilham a arvore.
    Marcar cada pre-condicao confirmada no `133-ROLLOUT.md`, com quem confirmou e quando, ANTES de
    aceitar a resposta final. Se qualquer pre-condicao ficar sem confirmacao, a opcao valida e
    `parar` — nunca prosseguir por inferencia.
  </action>
  <verify>
    <human-check>As quatro caixas de pre-condicao do `133-ROLLOUT.md` estao marcadas com autor e data, e a resposta escolhida esta registrada no arquivo</human-check>
  </verify>
  <done>O usuario respondeu `ligar-agora`, `adiar` ou `parar`; as quatro pre-condicoes estao registradas no `133-ROLLOUT.md`; e, se `ligar-agora`, a autorizacao de deploy foi dada explicitamente nesta conversa.</done>
  <resume-signal>Responder: `ligar-agora`, `adiar` ou `parar` — e, se `ligar-agora`, autorizar o deploy explicitamente</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy, ligar a chave e conferir por reconsulta</name>
  <files>.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md</files>
  <read_first>
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md` — as seções "Antes de ligar", "Ligar" e "Conferir depois de ligar"
    - `deploy.sh` — o que o script faz depois do envio (`composer install --no-dev`, `migrate --force`, caches, `supervisorctl restart`)
    - `.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-03-PLAN.md` linhas 120-140 — o formato de comando de tinker no VPS já usado nesta milestone
  </read_first>
  <action>
    Executar **somente** se a Task 2 devolveu `ligar-agora` com autorização explícita de deploy.

    1. Conferir que a árvore está limpa (`git status --porcelain` vazio para os caminhos rastreados)
       e que a suíte da fase está verde. Registrar o commit a implantar
       (`git rev-parse --short HEAD`) no campo correspondente do `133-ROLLOUT.md`.
    2. Registrar a **contagem-baseline** de `mlb_empresas` (total e `POLO`) por reconsulta no VPS,
       com o comando do `<interfaces>`, no `133-ROLLOUT.md`. Sem baseline, "nada quebrou" não é
       verificável depois.
    3. Rodar o deploy (`deploy.sh`).
    4. Ligar a chave no VPS com o comando de tinker do `<interfaces>` — `Configuracao::set(...)` com
       a constante `EmpresaOperacionalRouter::CHAVE_BLOQUEIO` e o valor string `'1'`.
    5. Conferir por **reconsulta**: `bloqueioAtivo()` deve imprimir `ligado`. ⛔ Não conferir pela
       tela.
    6. Conferir que a contagem de `mlb_empresas` **não mudou** com o ato de ligar (ligar não cria
       nem apaga ficha).
    7. Preencher no `133-ROLLOUT.md`: data/hora da ativação (com fuso), quem autorizou, quem
       executou, o commit implantado e as contagens antes/depois.

    ⛔ **Não rodar `php artisan cache:clear` no VPS** — ver o aviso de ambiente. Se algo parecer
    preso em cache, `systemctl reload php8.2-fpm`.
    ⛔ Não criar empresa de teste em produção para "provar" a exceção — o usuário recusou; a prova é
    o próximo cadastro real (plano 133-05).
    ⛔ Se qualquer conferência falhar, **desligar imediatamente** com o comando da seção "Como voltar
    atrás", registrar o ocorrido no `133-ROLLOUT.md` e parar.
  </action>
  <verify>
    <automated>No VPS: `php artisan tinker --execute="echo app(App\Services\Operacional\EmpresaOperacionalRouter::class)->bloqueioAtivo() ? 'ligado' : 'desligado', '|', App\Models\MlbEmpresa::count();"` → imprime `ligado|<mesma contagem do baseline>`</automated>
  </verify>
  <acceptance_criteria>
    - A saída do comando acima começa com `ligado` — conferido por reconsulta ao banco, não pela tela
    - A contagem de `mlb_empresas` depois de ligar é **idêntica** à baseline registrada antes
    - `grep -c "ligado" .planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-ROLLOUT.md` retorna 1 ou mais e os campos de data/hora, autorizador, executor e commit estão preenchidos (nenhum placeholder do tipo `TBD` restante nesses campos)
    - Nenhum `cache:clear` foi executado no VPS (conferir o histórico de comandos da sessão)
    - Nenhuma empresa nova foi criada em produção por esta task
  </acceptance_criteria>
  <done>A chave está ligada em produção, conferida por reconsulta, sem alteração na contagem de fichas, e o `133-ROLLOUT.md` registra quem autorizou, quem executou, quando e com qual commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sessão local → VPS de produção (`deploy.sh` / `plink`) | Comandos executados aqui alteram o sistema que a operação usa agora. Não há ambiente intermediário entre esta sessão e o cliente final. |
| `configuracoes.administrativo_bloqueio_ativo` → operação inteira | Uma linha de tabela liga ou desliga o roteamento automático de todas as empresas. É o maior raio de efeito por menor esforço em todo o projeto. |
| Árvore de trabalho compartilhada → deploy | O deploy publica o trabalho de todas as sessões do Claude Code e do outro desenvolvedor que editam a mesma árvore, não apenas o desta fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-16 | Repudiation | A chave não registra quem a ligou nem quando | mitigate | `Configuracao::set()` grava apenas valor — não há autor, não há histórico. Mitigação por processo: o `133-ROLLOUT.md` versionado exige data/hora, quem autorizou, quem executou e o commit implantado, e o commit do arquivo dá o carimbo de tempo em git. Não há mitigação de código nesta fase (dar trilha à chave seria capacidade nova). |
| T-133-17 | Denial of Service | Chave ligada com a exceção quebrada para de rotear Polos, o único fluxo vivo | mitigate | Ativação só depois dos planos 133-01/02/03 verdes; conferência imediata de que a contagem de fichas não mudou; critério de parar escrito com o comando de desligar logo abaixo; e o plano 133-05 confere o primeiro cadastro real. A chave é a saída, em segundos, sem deploy. |
| T-133-18 | Elevation of Privilege | Ativação sem passar pelo checkpoint humano do ROADMAP | mitigate | Task 2 é `checkpoint:decision` bloqueante, com as quatro pré-condições apresentadas **individualmente** e registradas no roteiro. `workflow.auto_advance` está desligado; mesmo se estivesse ligado, este gate não é auto-aprovável. |
| T-133-19 | Denial of Service | `cache:clear` no VPS derruba o site | mitigate | Proibição explícita em três lugares deste plano (aviso de ambiente, ação da Task 3, roteiro), com a alternativa correta (`systemctl reload php8.2-fpm`). Incidente real de 2026-07-30. |
| T-133-20 | Information Disclosure | Segredos ou dados de cliente no roteiro versionado | mitigate | `<acceptance_criteria>` da Task 1 reprova cadeias longas que pareçam token/senha/hostkey; o roteiro registra resultados (`confere` / `não confere`), nunca valores. |
| T-133-21 | Tampering | Deploy publica trabalho não revisado de outra sessão | accept | Natureza da árvore compartilhada, conhecida e registrada na memória do projeto. Mitigação: autorização explícita nesta conversa (Task 2), árvore limpa antes do deploy e o commit implantado registrado no roteiro para permitir reconstruir depois o que foi publicado. |
| T-133-SC | Tampering (supply chain) | composer/npm install durante o deploy | accept | O `deploy.sh` roda `composer install --no-dev` a partir do `composer.lock` já versionado — instala versões travadas, não resolve dependência nova. Esta fase não adiciona nenhum pacote; se o deploy acusar mudança de lockfile, é sinal de desvio de escopo e deve parar para revisão. |
</threat_model>

<verification>
- No VPS, `bloqueioAtivo()` imprime `ligado` (reconsulta ao banco, nunca a tela)
- A contagem de `mlb_empresas` é idêntica antes e depois de ligar
- `133-ROLLOUT.md` existe, tem as 7 seções e os campos de autoria/data/commit preenchidos
- A tela `/administrativo/contratos` mostra a faixa (conferência visual formal fica no plano 133-05)
- Nenhum `cache:clear` executado; nenhuma empresa de teste criada em produção
</verification>

<success_criteria>
- As quatro pré-condições do checkpoint humano foram confirmadas uma a uma e registradas
- A chave está ligada em produção, com autorização explícita e prova por reconsulta
- Existe roteiro escrito de como desligar, com critério de parar — e a saída declarada é a chave, nunca rollback de código (SC 4)
- Ligar não alterou nenhuma ficha existente
</success_criteria>

<output>
Criar `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-04-SUMMARY.md` ao terminar.
</output>
