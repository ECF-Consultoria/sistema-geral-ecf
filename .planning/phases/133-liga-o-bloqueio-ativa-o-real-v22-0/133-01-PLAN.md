---
phase: 133-liga-o-bloqueio-ativa-o-real-v22-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php
  - app/Services/Operacional/EmpresaOperacionalRouter.php
  - tests/Feature/Phase124KillSwitchTest.php
autonomous: true
requirements: [FLUXO-01, FLUXO-02]

must_haves:
  truths:
    - "Com o interruptor ligado, uma empresa cujo serviço exige contrato NÃO ganha ficha de operação — nem pelo webhook do HubSpot, nem pelo cadastro do Comercial"
    - "Com o interruptor ligado, uma empresa de Polos continua ganhando ficha normalmente, exatamente como hoje (SC 2b)"
    - "Empresa com Polos E Assessoria na mesma submissão: só o Polos entra na operação; a Assessoria fica esperando (D-02)"
    - "Um nome de serviço que não existe no catálogo é tratado como se exigisse contrato — nunca como isento (fail-safe)"
    - "Com o interruptor desligado, nada muda em relação ao comportamento de hoje"
    - "O teste declara explicitamente que o Polos do cenário é isento, e falha alto se não for — a suíte não pode provar o contrário do que promete"
  artifacts:
    - path: "app/Services/Operacional/EmpresaOperacionalRouter.php"
      provides: "Exceção por serviço dentro de rotear() — o ponto de extensão da Fase 128 preenchido"
      contains: "exige_contrato"
    - path: "tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php"
      provides: "Prova da exceção: Polos passa com a chave ligada, Assessoria é retida, mista separa, nome fora do catálogo é retido"
      min_lines: 120
    - path: "tests/Feature/Phase124KillSwitchTest.php"
      provides: "Os 4 testes do bloqueio total reescritos para serviço que EXIGE contrato + 2 testes novos que provam Polos passando pelos dois caminhos HTTP"
  key_links:
    - from: "app/Services/Operacional/EmpresaOperacionalRouter.php::rotear()"
      to: "servicos.exige_contrato"
      via: "consulta em lote whereIn(nome)->where('exige_contrato', false)"
      pattern: "exige_contrato"
    - from: "app/Services/Operacional/EmpresaOperacionalRouter.php::rotear()"
      to: "aplicarRoteamento()"
      via: "chamada apenas com os serviços liberados quando o bloqueio está ligado"
      pattern: "aplicarRoteamento"
---

<objective>
Preencher o ponto de extensão que hoje é **comentário** dentro de
`EmpresaOperacionalRouter::rotear()`.

Hoje, ligado, o interruptor bloqueia **tudo** — inclusive Polos, que é o único fluxo que de fato
cria ficha em produção (486 fichas, todas `POLO`). Ligar a chave sem esta task **quebraria a
operação viva** e não bloquearia nada de verdade (não existe nenhuma ficha de Assessoria ou
Incubadora na base — D-01).

A decisão passa a ser tomada **por serviço, dentro do laço** (D-02): cada nome de serviço é
confrontado com `servicos.exige_contrato`. Quem é isento passa; quem exige contrato fica esperando
a etapa administrativa. Uma empresa com Polos + Assessoria tem o Polos roteado e a Assessoria
retida — a alternativa (prender a empresa inteira) foi explicitamente rejeitada pelo usuário.

Propósito: sem esta exceção, o SC 2b é impossível e a fase inteira não pode ser ligada em produção.
Saída: `rotear()` com a exceção, o arquivo de teste novo que a prova, e os 4 testes existentes do
`Phase124KillSwitchTest` trocados para um cenário que continue fazendo sentido.

⚠️ **A prova de sucesso desta fase é "nada quebrou", não "algo foi bloqueado"** (D-01). Quem
executar não deve procurar retenção em produção: hoje não há o que reter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-CONTEXT.md
@.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-RESEARCH.md
@.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-PATTERNS.md
@.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-VALIDATION.md
@CLAUDE.md

<interfaces>
**Estado atual de `app/Services/Operacional/EmpresaOperacionalRouter.php::rotear()` (linhas 106-127):**

```php
private function rotear(Company $company, iterable $nomesServicos, array $handoff, bool $guardPorEmpresa): void
{
    if ($this->bloqueioAtivo()) {
        // Interruptor de emergência (REDE-01). Ligado, o roteamento automático PARA:
        // ... (comentário longo)
        // PONTO DE EXTENSÃO da Fase 128 (FLUXO-08/D-09): aqui vai entrar a consulta
        // "este serviço exige contrato?", que isenta Polos do bloqueio.
        Log::warning('[Administrativo] Roteamento operacional bloqueado pelo interruptor de emergência (' . self::CHAVE_BLOQUEIO . ').', [
            'company_id' => $company->id,
        ]);

        return;
    }

    $this->aplicarRoteamento($company, $nomesServicos, $handoff, $guardPorEmpresa);
}
```

**Forma-alvo já validada pela pesquisa (filtro em lote, uma única query, sem N+1):**

```php
$nomes = collect($nomesServicos)->values();

$isentos = Servico::whereIn('nome', $nomes)
    ->where('exige_contrato', false)
    ->pluck('nome');

$liberados = $nomes->intersect($isentos)->values();
$retidos   = $nomes->diff($liberados)->values();
```

`Servico` **já está importado** no topo do arquivo (linha 11) — não adicionar import novo.

**A consulta autorizada (`app/Models/Servico.php:181-189, 244-257`) — não hand-rollar:**

```php
public function scopeExigeContrato($query) { return $query->where('exige_contrato', true); }
public function exigeContrato(): bool     { return (bool) $this->exige_contrato; }
```

⛔ **Proibido** qualquer `if ($nome === 'Polos')` novo em código de produção. Quem decide é o dado
(`servicos.exige_contrato`), não o nome — é o defeito que a Fase 128 corrigiu.

**`aplicarRoteamento()`** (mesmo arquivo, logo abaixo) já recebe `iterable $nomesServicos` — uma
`Collection` de nomes é aceita como está. A mecânica de guard/lock/dedup **não muda**.

**Os 4 testes que ficam obsoletos de propósito** (`tests/Feature/Phase124KillSwitchTest.php`):

| Método | Linhas | Cenário atual |
|---|---|---|
| `test_interruptor_ligado_impede_o_roteamento_do_cadastro` | 186-196 | `rotearCadastro($company, ['Polos'])`, chave ligada, espera 0 fichas |
| `test_interruptor_ligado_impede_o_roteamento_por_servico` | 204-214 | `rotearServico($company, 'Polos')`, idem |
| `test_interruptor_ligado_impede_o_cadastro_manual_de_criar_ficha` | 253-275 | `POST /comercial/empresas` com o `Servico` Polos, idem |
| `test_interruptor_ligado_impede_o_webhook_de_criar_ficha` | 286-299 | Webhook HubSpot com `servico_ecf: 'Polos'`, idem |

**Fixture do `setUp()` atual do mesmo arquivo (linhas 44-53)** — cria Polos/Assessoria/Incubadora
por `firstOrCreate` **sem** declarar `exige_contrato`, herdando o valor do seed real:

```php
foreach (['Polos', 'Assessoria', 'Incubadora'] as $nome) {
    Servico::firstOrCreate(
        ['nome' => $nome],
        ['valor_padrao' => 0, 'tipo_cobranca' => 'mensal', 'ativo' => true],
    );
}
```

⚠️ **A armadilha central da fase:** o DEFAULT de schema de `servicos.exige_contrato` é **`true`**
(migration `2026_08_13_100001_add_exige_contrato_to_servicos_table.php`). Além disso,
`firstOrCreate` **ignora o array de atributos quando a linha já existe** — o valor efetivo vem do
seed. Fixture que não **afirma** o valor pode criar um "Polos" não-isento, e aí o teste do SC 2b
passa a provar exatamente o contrário do que promete.

**Molde de fixture com `exige_contrato` explícito** (`tests/Feature/Phase128/InvarianteRoteamentoTest.php:45-58`):

```php
Servico::firstOrCreate(
    ['nome' => 'Assessoria'],
    [
        'valor_padrao'   => 500,
        'tipo_cobranca'  => Servico::TIPO_MENSAL,
        'ativo'          => true,
        'setor'          => Servico::SETOR_PUBLICACAO,
        'exige_contrato' => true,
    ],
);
```

**Helpers reaproveitáveis do `Phase124KillSwitchTest`** (linhas 79-92): `criarEmpresa()`,
`router()`, `userAdmin()`. Os mocks do webhook HubSpot (`mockaHubSpot()`,
`disparaWebhookHubspot()`, `assinaturaHubspot()`) só existem nesse arquivo — é por isso que os
testes HTTP de Polos-passando nascem **lá**, e não no arquivo novo.

**`ComercialController::servicoDisparaImplementacao()`** (linhas 58-66) mapeia nome → tipo por
`str_contains`: `'Polos'` → `polos`, `'Assessoria'` → `assessoria`, `'Incubadora'` → `incubadora`.
Um nome livre contendo "Polos" (ex.: `'Polos Fantasma Não Catalogado'`) **casa** nesse mapa — é
exatamente por isso que o fail-safe precisa de teste próprio.
</interfaces>
</context>

<aviso_de_ambiente>
⚠️ **PHP não está no PATH** — usar sempre `C:\xampp\php\php.exe`.

⚠️ **Nunca rodar `artisan test` sem filtro** — há um ponto pré-existente que estoura o timeout no
`MercadoLivreAdsService`, sem relação com esta fase.

⚠️ **`--filter` que não casa com nada SAI 0** e ainda varre a suíte inteira. `No tests found` com
exit 0 é **FALHA**. Conferir sempre que a saída traz `Tests: N` com N > 0.

⚠️ **Árvore de trabalho compartilhada** com outro desenvolvedor e com outras sessões do Claude
Code: commitar só os caminhos deste plano (`git commit -- <arquivos>`), nunca `git add -A`.

⚠️ **Nenhum deploy neste plano.** O deploy e a ativação da chave são o plano 133-04.
</aviso_de_ambiente>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — fixture com `exige_contrato` explícito e os testes que provam a exceção (RED)</name>
  <files>tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php</files>
  <read_first>
    - `tests/Feature/Phase124KillSwitchTest.php` linhas 1-100 (namespace, imports, `setUp()`, helpers `criarEmpresa`/`router`/`userAdmin`) e 180-235 (os testes de interruptor ligado/desligado, que são o molde de asserção)
    - `tests/Feature/Phase128/InvarianteRoteamentoTest.php` linhas 30-70 — fixture com `exige_contrato` explícito
    - `tests/Feature/Phase128/ExigeContratoTest.php` (74 linhas, inteiro) — o teste que prova que Polos nasce isento pelo seed; é a origem da asserção-sentinela pedida abaixo
    - `app/Models/Servico.php` linhas 25-60 (`$fillable`, `$casts`) e 175-260 (`scopeExigeContrato`, `exigeContrato`)
    - `app/Services/Operacional/EmpresaOperacionalRouter.php` inteiro — em especial `rotearServico()`, `rotearCadastro()`, `rotear()` e o docblock de `rotearCadastro()` que explica por que o caminho Comercial NÃO tem guard por empresa
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-VALIDATION.md`, seção "Wave 0 Requirements"
  </read_first>
  <behavior>
    - **Sentinela de fixture:** no cenário do teste, `Servico` "Polos" tem `exigeContrato() === false` e "Assessoria" tem `exigeContrato() === true`. Se não tiver, o arquivo inteiro falha aqui, com mensagem dizendo que a fixture está errada — nunca "passa" provando o contrário
    - Chave ligada + `rotearCadastro($company, ['Polos'])` → nasce 1 `MlbEmpresa` tipo `POLO` com `projeto = 'POLOS'` e 1 `MlbImplementacao` vinculada
    - Chave ligada + `rotearServico($company, 'Polos')` → mesmo resultado pelo segundo ponto de entrada
    - Chave ligada + `rotearCadastro($company, ['Assessoria'])` → nenhuma `MlbEmpresa`, nenhuma `MlbImplementacao`
    - Chave ligada + `rotearCadastro($company, ['Polos', 'Assessoria'])` → exatamente 1 `MlbEmpresa`, tipo `POLO`; nenhuma linha com tipo `ASSESSORIA`
    - Chave ligada + `rotearCadastro($company, ['Polos Fantasma Não Catalogado'])` (nome que casa no mapa `servicoDisparaImplementacao` mas não existe em `servicos`) → nenhuma ficha: nome fora do catálogo é tratado como "exige contrato"
    - Chave desligada + `rotearCadastro($company, ['Assessoria'])` → nasce a ficha `ASSESSORIA` normalmente (o filtro não vazou para o caminho desligado)
  </behavior>
  <action>
    Criar o diretório `tests/Feature/Phase133/` e o arquivo
    `tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php`, namespace `Tests\Feature\Phase133`,
    classe `RoteamentoExcecaoServicoTest extends TestCase` com `use RefreshDatabase`.

    Docblock de classe em pt-BR explicando: que este arquivo prova a exceção por serviço da D-02;
    que a fase inteira depende de o "Polos" do cenário ser **realmente** isento; e que o default de
    schema de `servicos.exige_contrato` é `true`, motivo da asserção-sentinela.

    No `setUp()`, depois do `parent::setUp()`: garantir os serviços do cenário com
    `Servico::firstOrCreate(['nome' => ...], [...])` para `Polos` e `Assessoria` e, **logo em
    seguida**, forçar o valor de forma explícita com `Servico::where('nome', 'Polos')->update(['exige_contrato' => false])`
    e `Servico::where('nome', 'Assessoria')->update(['exige_contrato' => true])`. O `update` depois
    do `firstOrCreate` é deliberado e precisa de comentário dizendo o porquê: `firstOrCreate`
    ignora o array de atributos quando a linha já existe (o seed do catálogo roda em todo
    `RefreshDatabase`), então declarar o valor no array **não** garante nada.

    Escrever os helpers privados `criarEmpresa(string $nome = 'Empresa Teste Exceção')` (usando
    `Company::create(['name' => $nome])`) e `router()` (`app(EmpresaOperacionalRouter::class)`),
    copiados do `Phase124KillSwitchTest` — o projeto não usa trait compartilhada entre arquivos de
    teste, cada arquivo tem sua cópia.

    Escrever um método de teste por item do bloco `<behavior>`, com nomes em pt-BR no padrão do
    projeto (`test_...`). Nomes obrigatórios (o plano 133-04 e a verificação da fase citam estes
    literais):
    - `test_fixture_declara_polos_isento_e_assessoria_exigindo_contrato` (a sentinela)
    - `test_interruptor_ligado_nao_impede_o_roteamento_do_cadastro_de_polos`
    - `test_interruptor_ligado_nao_impede_o_roteamento_por_servico_de_polos`
    - `test_interruptor_ligado_retem_servico_que_exige_contrato`
    - `test_com_polos_e_assessoria_apenas_polos_e_roteado`
    - `test_nome_de_servico_fora_do_catalogo_e_retido_mesmo_parecendo_polos`
    - `test_interruptor_desligado_roteia_assessoria_como_sempre`

    Ligar/desligar a chave com `Configuracao::set(EmpresaOperacionalRouter::CHAVE_BLOQUEIO, '1')` /
    `'0'` — convenção do projeto é string `'1'`/`'0'`, nunca booleano nativo.

    Toda asserção deve ser por **reconsulta ao banco** (`MlbEmpresa::where(...)->first()`,
    `MlbImplementacao::where('empresa_id', ...)`), com mensagem explicativa no terceiro argumento —
    nunca `assertTrue` mudo.

    ⛔ **Não tocar em `EmpresaOperacionalRouter.php` nesta task.** Esta task termina VERMELHA nos
    casos de isenção, de propósito: é o RED do ciclo. A implementação é a Task 2.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo `tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php` existe e a saída do comando traz `Tests: 7` (nunca `No tests found`)
    - `test_fixture_declara_polos_isento_e_assessoria_exigindo_contrato` **passa** já nesta task — se falhar, a fixture está errada e nada mais do plano é confiável
    - `test_interruptor_ligado_retem_servico_que_exige_contrato` e `test_interruptor_desligado_roteia_assessoria_como_sempre` **passam** já nesta task (comportamento atual)
    - Falham exatamente 4 testes: os dois de Polos com a chave ligada, o misto e o de nome fora do catálogo — este é o RED esperado e deve ser registrado no SUMMARY
    - `grep -c "exige_contrato" tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php` retorna 2 ou mais (o valor é declarado, não herdado)
    - `grep -vE '^\s*(\*|//|/\*)' tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php | grep -c "update("` retorna 2 ou mais (as duas declarações explícitas de `exige_contrato` existem em código, não só em comentário)
    - `git status --porcelain` lista apenas `tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php` entre os arquivos deste plano
  </acceptance_criteria>
  <done>O arquivo de teste existe, a sentinela de fixture passa, e os 4 casos da exceção falham por ausência da implementação — não por fixture errada.</done>
</task>

<task type="auto">
  <name>Task 2: A exceção por serviço dentro de `rotear()` (D-02) + os 4 testes do Phase124 trocados de cenário</name>
  <files>app/Services/Operacional/EmpresaOperacionalRouter.php, tests/Feature/Phase124KillSwitchTest.php</files>
  <read_first>
    - `app/Services/Operacional/EmpresaOperacionalRouter.php` inteiro — `CHAVE_BLOQUEIO`, `bloqueioAtivo()`, `rotearServico()`, `rotearCadastro()`, `rotear()`, `aplicarRoteamento()`, `criarFicha()`, `liberarEmpresa()`; em especial o docblock de `liberarEmpresa()`, que explica por que a liberação manual passa por FORA do interruptor (não mexer nisso)
    - `app/Models/Servico.php` linhas 175-260
    - `tests/Feature/Phase124KillSwitchTest.php` inteiro (300 linhas)
    - `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-RESEARCH.md`, seções "Recomendação de implementação — filtro em lote, sem N+1", "Pitfall 1" e "Pitfall 4"
    - `tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php` (criado na Task 1)
  </read_first>
  <action>
    **(a) `EmpresaOperacionalRouter::rotear()`** — substituir o `return` incondicional do bloco
    `if ($this->bloqueioAtivo())` pelo filtro em lote descrito no `<interfaces>`:

    - Normalizar `$nomesServicos` para uma `Collection` com `collect(...)->values()`.
    - **Uma única consulta**, nunca dentro de laço: `Servico::whereIn('nome', $nomes)->where('exige_contrato', false)->pluck('nome')` — os isentos.
    - `$liberados = $nomes->intersect($isentos)->values()` e `$retidos = $nomes->diff($liberados)->values()`.
    - Quando houver retidos, manter o `Log::warning` com o prefixo `[Administrativo]`, agora com a
      chave `servicos_retidos` além de `company_id` — array associativo, nunca string interpolada.
      Trocar o texto de "bloqueado pelo interruptor de emergência" para algo que descreva a retenção
      seletiva (ex.: "Roteamento operacional retido pelo gate administrativo"), porque a mensagem
      atual passou a ser falsa: nem tudo é bloqueado.
    - Quando `$liberados` estiver vazio, `return` (nada muda em relação a hoje).
    - Quando houver liberados, chamar `aplicarRoteamento($company, $liberados, $handoff, $guardPorEmpresa)`
      e retornar. Os parâmetros `$handoff` e `$guardPorEmpresa` passam **inalterados** — a mecânica
      de guard/lock/dedup da Fase 129 não muda.

    Substituir o comentário "PONTO DE EXTENSÃO da Fase 128" por um comentário em pt-BR que registre:
    que a decisão é **por serviço** (D-02) e não por empresa, com o exemplo Polos + Assessoria; que
    nome fora do catálogo cai fora de `$isentos` e portanto é **retido** (fail-safe, mesmo espírito
    do `default(true)` da migration `2026_08_13_100001`); e que a consulta é em lote de propósito,
    para não virar N+1 dentro do laço.

    ⛔ Não introduzir nenhuma comparação por nome literal (`'Polos'`) em código de produção.
    ⛔ Não alterar `aplicarRoteamento()`, `criarFicha()`, `liberarEmpresa()` nem `bloqueioAtivo()`.

    **(b) `tests/Feature/Phase124KillSwitchTest.php`** — na MESMA task e no MESMO commit da
    mudança acima, para a suíte nunca ficar vermelha entre commits:

    1. No `setUp()`, depois do `firstOrCreate` já existente, acrescentar as duas declarações
       explícitas de `exige_contrato` (`update` para Polos = `false`, para Assessoria e Incubadora =
       `true`), com o mesmo comentário de justificativa da Task 1.
    2. Trocar o cenário dos 4 testes listados no `<interfaces>` de `Polos` para `Assessoria` — o
       serviço passa a ser um que **exige** contrato, e a asserção original (`count() === 0`) volta
       a ser verdadeira pelo motivo certo. Renomear os 4 métodos acrescentando o sufixo
       `_quando_o_servico_exige_contrato` (nos dois primeiros) e `_para_servico_que_exige_contrato`
       (nos dois de HTTP), preservando os prefixos `test_interruptor_ligado_` e as palavras
       `cadastro`/`webhook`/`servico` — há comandos de verificação que filtram por esses literais.
    3. Reescrever o docblock de cada um dos 4: hoje eles afirmam "não cria NADA"; passam a afirmar
       que o interruptor retém **o que exige contrato**, e apontam para os testes de isenção.
    4. **Acrescentar** dois testes novos, que provam o SC 2b nos dois caminhos HTTP (é neste arquivo
       que vivem os mocks do webhook, por isso eles nascem aqui e não no arquivo da Task 1):
       - `test_interruptor_ligado_nao_impede_o_cadastro_manual_de_criar_ficha_para_polos` — `POST /comercial/empresas`
         com o `Servico` Polos e a chave ligada; asserção positiva: `MlbEmpresa` tipo `POLO` existe
         para a `Company` criada, e a `MlbImplementacao` correspondente também
       - `test_interruptor_ligado_nao_impede_o_webhook_de_criar_ficha_para_polos` — webhook HubSpot
         com `servico_ecf: 'Polos'` e a chave ligada; mesma asserção positiva
    5. Atualizar o docblock de topo da classe: ele hoje diz que ligado "o roteamento automático
       PARA"; passa a dizer que ligado o roteamento **retém o que exige contrato e libera o que é
       isento**, citando a Fase 133 e a D-02.

    ⛔ **Proibido apenas relaxar asserção** (trocar `assertSame(0, ...)` por algo permissivo ou
    apagar o teste). A intenção original continua válida — o que mudou é que a retenção passou a ser
    seletiva.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php tests/Feature/Phase124KillSwitchTest.php tests/Feature/Phase128</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima sai verde com `Tests: N` e N ≥ 16 (7 do arquivo novo + 9 do Phase124 + os do Phase128); `No tests found` é falha
    - `tests/Feature/Phase124KillSwitchTest.php` tem 9 métodos de teste: `grep -c "public function test_" tests/Feature/Phase124KillSwitchTest.php` retorna 9
    - Os 4 métodos antigos não existem mais com o nome antigo: `grep -c "function test_interruptor_ligado_impede_o_roteamento_do_cadastro()" tests/Feature/Phase124KillSwitchTest.php` retorna 0
    - Os 4 cenários trocaram de serviço: `grep -c "Assessoria" tests/Feature/Phase124KillSwitchTest.php` retorna 4 ou mais
    - `grep -vE '^\s*(\*|//|/\*)' app/Services/Operacional/EmpresaOperacionalRouter.php | grep -c "exige_contrato"` retorna 1 ou mais — a consulta existe em código, não só em comentário
    - `grep -vE '^\s*(\*|//|/\*)' app/Services/Operacional/EmpresaOperacionalRouter.php | grep -c "'Polos'"` retorna 0 — nenhuma decisão por nome literal em produção
    - `grep -c "servicos_retidos" app/Services/Operacional/EmpresaOperacionalRouter.php` retorna 1 ou mais — o log diz QUAIS serviços ficaram retidos
    - Nenhuma consulta dentro de laço: `aplicarRoteamento` continua sendo chamado no máximo uma vez por execução de `rotear()` (conferir por leitura; o `Servico::whereIn` está FORA de qualquer `foreach`)
    - Regressão: `C:\xampp\php\php.exe artisan test tests/Feature/Phase129 tests/Feature/Phase130` continua verde com `Tests: N`, N > 0 (a liberação manual passa por fora do interruptor e não pode ter sido afetada)
    - `git status --porcelain` lista apenas os arquivos deste plano
  </acceptance_criteria>
  <done>Com a chave ligada, Polos é roteado e Assessoria é retida — provado nos dois pontos de entrada do router e nos dois caminhos HTTP; com a chave desligada nada mudou; e nenhum teste foi apenas relaxado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catálogo `servicos` (dado editável pela tela da Fase 131) → decisão de roteamento | O valor de `exige_contrato` de uma linha decide se uma empresa entra ou não na operação. Quem edita o catálogo altera, indiretamente, quem passa pelo gate. |
| Nome de serviço vindo do handoff HubSpot → `rotear()` | O nome chega como string livre do CRM. Um nome não catalogado é uma entrada não confiável que precisa cair para o lado seguro. |
| Interruptor `administrativo_bloqueio_ativo` (linha em `configuracoes`) → operação viva | Ligado e errado, o interruptor para o único fluxo que cria ficha em produção (486 fichas Polos). É negação de serviço ao fluxo vivo, não só uma trava. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-01 | Tampering | `rotear()` — nome de serviço fora do catálogo | mitigate | Fail-safe explícito: só entra em `$liberados` quem **casa** com uma linha de `servicos` com `exige_contrato = false`. Nome desconhecido fica em `$retidos`. Teste dedicado `test_nome_de_servico_fora_do_catalogo_e_retido_mesmo_parecendo_polos` prova que um nome livre contendo "Polos" não vira passe livre. |
| T-133-02 | Denial of Service | `rotear()` bloqueando Polos por engano | mitigate | A exceção é provada por 3 testes com a chave LIGADA (cadastro, por serviço, e os dois caminhos HTTP no Phase124). Sem eles, ligar a chave em produção pararia o único fluxo vivo. O plano 133-05 confere por reconsulta ao banco no primeiro cadastro real e a chave é a saída imediata (D-05). |
| T-133-03 | Tampering | Fixture de teste com `exige_contrato` herdado | mitigate | Asserção-sentinela obrigatória no `setUp`/primeiro teste dos dois arquivos: se Polos não for isento no cenário, a suíte falha alto em vez de "passar" provando o contrário. O default de schema é `true` — herdar valor é o modo mais provável de a fase se auto-enganar. |
| T-133-04 | Repudiation | Retenção sem rastro de qual serviço foi retido | mitigate | `Log::warning('[Administrativo] ...')` com `company_id` **e** `servicos_retidos`. Sem a segunda chave, "por que essa empresa não entrou?" só se responde reconstruindo o catálogo da época. |
| T-133-05 | Elevation of Privilege | Decisão por nome literal reintroduzida em código | mitigate | Item de `<acceptance_criteria>` reprova qualquer `'Polos'` em linha de código (comentários filtrados). A decisão vive no dado (`servicos.exige_contrato`), editável e auditável pela tela da Fase 131. |
| T-133-SC | Tampering (supply chain) | npm/composer install | accept | **Nenhuma instalação de pacote nesta fase.** Nenhum `composer require` / `npm install` aparece em task alguma dos 5 planos; a mudança usa apenas `Servico`, `Collection` e `Log`, já presentes. Se alguma task de execução precisar instalar algo, é sinal de desvio de escopo e deve parar para revisão. |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test tests/Feature/Phase133/RoteamentoExcecaoServicoTest.php tests/Feature/Phase124KillSwitchTest.php tests/Feature/Phase128` verde, com `Tests: N` e N ≥ 16
- `C:\xampp\php\php.exe artisan test tests/Feature/Phase129 tests/Feature/Phase130` continua verde (nenhuma regressão em `liberarEmpresa()`)
- Reconsulta ao código: `rotear()` continua sendo o **único** lugar do service que lê `bloqueioAtivo()`
- `git status --porcelain` mostra apenas os 3 arquivos de `files_modified`
</verification>

<success_criteria>
- Com o interruptor ligado, serviço que exige contrato não gera ficha (SC 1 e SC 2) e Polos gera normalmente (SC 2b)
- Empresa com Polos + Assessoria tem só o Polos roteado (D-02)
- Nome de serviço fora do catálogo é retido, nunca liberado
- Com o interruptor desligado, o comportamento é bit a bit o de hoje (SC 4)
- Os 4 testes do `Phase124KillSwitchTest` foram trocados de cenário e receberam companhia — nenhuma asserção foi apenas relaxada
</success_criteria>

<output>
Criar `.planning/phases/133-liga-o-bloqueio-ativa-o-real-v22-0/133-01-SUMMARY.md` ao terminar.
</output>
