---
phase: 81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi
plan: 02
type: execute
wave: 2
depends_on: ["81-01"]
files_modified:
  - app/Http/Controllers/NpsTemplateController.php
  - routes/web.php
  - tests/Feature/V16/EmpresasElegiveisTest.php
autonomous: true
requirements: [DEC-81-3]

must_haves:
  truths:
    - "Dado um modelo com serviços cobertos, o endpoint retorna só empresas ativas com contrato ativo de algum serviço coberto"
    - "Modelo sem serviços cobertos retorna TODAS as empresas ativas (fallback)"
    - "Usuário não-admin recebe apenas empresas da própria carteira"
  artifacts:
    - path: "app/Http/Controllers/NpsTemplateController.php"
      provides: "método empresasElegiveis() (JSON)"
      contains: "public function empresasElegiveis"
    - path: "routes/web.php"
      provides: "rota GET empresas-elegiveis no grupo role:admin"
      contains: "empresas-elegiveis"
    - path: "tests/Feature/V16/EmpresasElegiveisTest.php"
      provides: "cobertura DEC-81-3 (com scopes, sem scopes, carteira)"
  key_links:
    - from: "empresasElegiveis()"
      to: "Company.contratosServico (ativo) ∩ template.servicos"
      via: "whereHas + whereIn servico_id"
      pattern: "whereHas\\('contratosServico'"
    - from: "empresasElegiveis()"
      to: "carteira do usuário não-admin"
      via: "whereIn companies do user"
      pattern: "isAdmin"
---

<objective>
Criar o endpoint JSON dedicado `GET .../templates/{template}/empresas-elegiveis`
que, dado um modelo, retorna as empresas elegíveis para gerar link — inversão da
lógica de "serviços cobertos ∩ contratos ativos" do `NpsTemplateService`.

Purpose: alimentar o modal gerar-link modelo-first (81-04) sem inflar o payload
inicial de `/nps`; garantir que não-admin só veja empresas da própria carteira.
Output: `empresasElegiveis()` no controller, 1 rota nomeada, EmpresasElegiveisTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-CONTEXT.md
@.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-RESEARCH.md
@app/Http/Controllers/NpsTemplateController.php
@tests/Feature/V16/CriaCenarioResponsaveis.php

<interfaces>
<!-- Molde: empresasAfetadas() (L285-331) já é um endpoint JSON deste controller. -->

Company: relation contratosServico(); ContratoServico::scopeActive() = where('ativo', true)
Company: relation companies() no User → $user->companies()->pluck('companies.id')
User::isAdmin(): bool
NpsTemplate::servicos()->pluck('servicos.id') → coleção de servico_id cobertos

Padrão de escopo por carteira (não-admin): whereIn('id', $user->companies()->pluck('companies.id'))
Padrão do endpoint JSON: return response()->json([...]) — NÃO Inertia.

Rota a adicionar: GET no grupo role:admin, junto ao bloco empresas-afetadas (~L242).
Nome: nps.configuracao.templates.empresas-elegiveis
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: empresasElegiveis() — inversão do resolveForCompany</name>
  <files>app/Http/Controllers/NpsTemplateController.php, routes/web.php, tests/Feature/V16/EmpresasElegiveisTest.php</files>
  <behavior>
    - Modelo com serviços cobertos {S}: retorna só empresas active=true com contrato ativo (ativo=true) de algum servico em {S}, ordenadas por name.
    - Modelo SEM serviços cobertos (pivot vazio): retorna TODAS as empresas active=true (fallback — cobre o is_default/NPS Padrão), ordenadas por name.
    - Usuário não-admin: resultado restrito à carteira (whereIn companies do user), em ambos os casos.
    - Response JSON: { template_id, empresas: [{id, name}] }.
  </behavior>
  <action>
    Criar `public function empresasElegiveis(Request $request, NpsTemplate $template)` per DEC-81-3.
    Obter `$servicoIds = $template->servicos()->pluck('servicos.id')`. Base:
    `Company::query()->where('active', true)->orderBy('name')`. Se `$servicoIds` NÃO vazio, aplicar
    `->whereHas('contratosServico', fn($q) => $q->active()->whereIn('servico_id', $servicoIds))`;
    se vazio, NÃO aplicar filtro de serviço (fallback → todas as empresas ativas — decisão travada
    "modelo sem scopes → todas"). Escopar carteira: `if (! $request->user()->isAdmin()) $query->whereIn('id', $request->user()->companies()->pluck('companies.id'))`.
    Retornar `response()->json(['template_id' => $template->id, 'empresas' => $query->get(['id','name'])])`.
    Registrar a rota GET `nps.configuracao.templates.empresas-elegiveis` junto ao bloco empresas-afetadas
    (~L242), no grupo role:admin.
    Escrever EmpresasElegiveisTest (namespace V16, RefreshDatabase) reusando `CriaCenarioResponsaveis`:
    cenário (a) template com scope Shopee → só empresa com contrato Shopee ativo aparece; empresa ML não;
    (b) template sem scopes → todas as empresas ativas aparecem; (c) usuário não-admin (com carteira de 1
    empresa) → só a empresa da carteira, mesmo havendo outras elegíveis. Assertar via JSON (assertJsonPath /
    assertJsonCount em `empresas`).
  </action>
  <verify>
    <automated>php artisan test --filter=EmpresasElegiveisTest</automated>
  </verify>
  <done>EmpresasElegiveisTest verde nos 3 cenários; endpoint JSON registrado no grupo role:admin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (usuário) → endpoint JSON | template_id via rota; resultado pode vazar empresas fora da carteira se não escopado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-05 | Information Disclosure | empresasElegiveis() para não-admin | mitigate | `whereIn('id', carteira)` quando `! isAdmin()` — não vaza empresas de fora |
| T-81-06 | Spoofing | rota sem gate | mitigate | rota dentro do grupo `role:admin` (EnsureUserHasRole) |
| T-81-07 | Denial of Service | fallback "todas" sem filtro | accept | admin-only + lista de empresas é limitada; sem paginação necessária nesta fase |
</threat_model>

<verification>
- `php artisan test --filter=EmpresasElegiveisTest` verde.
- `php artisan test --filter=Nps` sem regressões.
</verification>

<success_criteria>
empresasElegiveis() implementado com fallback (sem scopes → todas) e escopo de carteira;
rota GET registrada; EmpresasElegiveisTest verde.
</success_criteria>

<output>
Criar `.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-02-SUMMARY.md` ao concluir.
</output>
