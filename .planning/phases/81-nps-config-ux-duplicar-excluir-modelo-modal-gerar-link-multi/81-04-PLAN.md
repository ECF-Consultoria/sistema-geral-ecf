---
phase: 81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi
plan: 04
type: execute
wave: 3
depends_on: ["81-02", "81-03"]
files_modified:
  - resources/js/Pages/Nps/Index.jsx
autonomous: false
requirements: [DEC-81-3]

must_haves:
  truths:
    - "No modal Gerar Link, o admin escolhe primeiro o MODELO (obrigatório) e depois a EMPRESA"
    - "Ao escolher o modelo, o select de empresa lista só as empresas elegíveis retornadas pelo endpoint"
    - "Escolher modelo Shopee mostra só empresas com Gestão de ADS Shopee; escolher outro modelo troca a lista"
    - "O botão Gerar Link só habilita com modelo E empresa selecionados; o submit continua em nps.generate"
    - "npm run build fica verde e a suíte de testes da fase passa (zero regressão)"
  artifacts:
    - path: "resources/js/Pages/Nps/Index.jsx"
      provides: "modal gerar-link modelo-first com filtro reativo de empresas"
      contains: "empresas-elegiveis"
  key_links:
    - from: "onSelectModelo (Nps/Index)"
      to: "route nps.configuracao.templates.empresas-elegiveis"
      via: "axios.get + setEmpresasElegiveis"
      pattern: "empresas-elegiveis"
    - from: "modal submit"
      to: "route nps.generate"
      via: "post {company_id, template_id}"
      pattern: "nps.generate"
---

<objective>
Reordenar o modal "Gerar Link NPS" (`Nps/Index.jsx`) para **modelo-first**:
passo 1 escolhe o MODELO (obrigatório, sem `__auto__`); passo 2 escolhe a
EMPRESA, cuja lista é carregada do endpoint `empresas-elegiveis` (81-02).
Fechar a fase com `npm run build` verde, regressão da suíte e checkpoint visual.

Purpose: evitar gerar link com empresa fora da cobertura do modelo; entregar a UX
final da fase 81.
Output: modal modelo-first com filtro reativo; build verde; checkpoint humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-CONTEXT.md
@.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-RESEARCH.md
@resources/js/Pages/Nps/Index.jsx

<interfaces>
<!-- Estado atual do modal (Nps/Index.jsx). -->

useForm inicial (L864): { company_id: '', template_id: '' }
submit (L916): post(route('nps.generate'), { onSuccess: () => { reset(); setOpen(false); } })
Modal atual (L1105-1157): select EMPRESA primeiro (companies.map), depois select MODELO opcional
  gated por `isAdmin && templates.length > 1`, com item "__auto__" (Automático).
Props da página: companies, templates, isAdmin (auth.user.role === 'admin').
HTTP no browser: window.axios (bootstrap.js) — usar window.axios.get.
Endpoint (81-02): GET route('nps.configuracao.templates.empresas-elegiveis', templateId)
  → { template_id, empresas: [{id, name}] }
UI: shadcn Select/SelectTrigger/SelectContent/SelectItem, Dialog, Button; tokens ecf-*, cn().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: modal modelo-first com filtro reativo de empresas</name>
  <files>resources/js/Pages/Nps/Index.jsx</files>
  <action>
    Reordenar o modal (L1105-1157) per DEC-81-3: passo 1 = select MODELO (obrigatório, remover o item
    "__auto__" e a exigência de vir vazio); passo 2 = select EMPRESA populado pelas empresas elegíveis.
    Adicionar estado `const [empresasElegiveis, setEmpresasElegiveis] = useState([])` e
    `const [carregandoEmpresas, setCarregandoEmpresas] = useState(false)`. Handler
    `onSelectModelo(id)`: `setData(d => ({ ...d, template_id: id, company_id: '' }))`, então
    `setCarregandoEmpresas(true)`, `const { data: res } = await window.axios.get(route('nps.configuracao.templates.empresas-elegiveis', id))`,
    `setEmpresasElegiveis(res.empresas)`, `setCarregandoEmpresas(false)` (tratar erro com finally). O select
    de empresa itera `empresasElegiveis` (não mais `companies`), fica desabilitado enquanto `!data.template_id`
    ou `carregandoEmpresas`, e mostra estado vazio ("Nenhuma empresa elegível para este modelo") quando a
    lista voltar vazia. O botão Gerar habilita só com `data.template_id && data.company_id`. O submit continua
    `post(route('nps.generate'))` (backend já aceita template_id). Ao fechar/reset do modal, limpar
    `empresasElegiveis`. Atualizar DialogDescription para refletir o fluxo (escolher modelo → empresa).
    ATENÇÃO Pitfall 4 (Rollup): derivar quaisquer flags por-item DENTRO do `.map()` do select de empresas
    (ex.: label/value por empresa), não no escopo do componente. Rodar `npm run build` ao final da tarefa
    para pegar o gotcha do `.map()`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Modal modelo-first: modelo obrigatório, empresa carregada do endpoint e filtrada; sem "__auto__"; build verde.</done>
</task>

<task type="auto">
  <name>Tarefa 2: regressão da fase (suíte V16 + Nps)</name>
  <files>tests/Feature/V16/DuplicarModeloTest.php, tests/Feature/V16/ExcluirModeloTest.php, tests/Feature/V16/EmpresasElegiveisTest.php</files>
  <action>
    Rodar a suíte da fase e a regressão do módulo NPS para confirmar zero quebra no CRUD de templates
    (store/update/toggleActive/setPrincipal) e no fluxo gerar-link. Não alterar código de teste aqui —
    é gate de fase; se algo falhar, corrigir o código-fonte referenciado pelo teste que quebrou.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16 && php artisan test --filter=Nps</automated>
  </verify>
  <done>Suíte V16 verde e regressão Nps sem falhas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Backend duplicate/destroy/empresas-elegiveis (81-01/81-02), botões Duplicar/Excluir na config (81-03)
    e o modal gerar-link modelo-first com filtro reativo (esta tarefa).
  </what-built>
  <how-to-verify>
    1. `npm run build` já verde; subir o app local.
    2. Ir em `/nps/configuracao`, abrir um modelo → clicar **Duplicar**: a lista recarrega com um "{nome} (cópia)"; o novo clone NÃO é principal.
    3. No clone, clicar **Excluir** → confirmar: some da lista. Abrir o modelo **principal**: botão Excluir aparece desabilitado.
    4. Tentar excluir um modelo que já tem respostas (se houver) → aparece flash de erro sugerindo arquivar.
    5. Ir em `/nps` → **Gerar Link**: escolher primeiro o MODELO. Ao escolher o modelo Shopee, o select de empresa mostra SÓ empresas com Gestão de ADS Shopee. Trocar para o modelo Performance → a lista de empresas troca (empresas ML). Modelo principal/sem escopo → todas.
    6. Selecionar empresa e gerar o link → link é gerado normalmente (nps.generate).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas observados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (usuário) → endpoint empresas-elegiveis | lista de empresas do modal vem do server; escopo por carteira garantido no 81-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-08 | Information Disclosure | modal exibe empresas de outro consultor | mitigate | fonte é o endpoint 81-02, que escopa por carteira p/ não-admin (frontend só renderiza o retornado) |
| T-81-09 | Tampering | submit gerar-link com company_id fora da cobertura | accept | UX filtra; nps.generate já valida company_id existente (fora de escopo desta fase reforçar) |
</threat_model>

<verification>
- `npm run build` verde.
- `php artisan test tests/Feature/V16` e `--filter=Nps` verdes.
- Checkpoint humano aprovado (fluxo duplicar/excluir + modal modelo-first).
</verification>

<success_criteria>
Modal modelo-first funcional com filtro reativo; build verde; suíte da fase e regressão Nps verdes;
checkpoint visual aprovado.
</success_criteria>

<output>
Criar `.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-04-SUMMARY.md` ao concluir.
</output>
