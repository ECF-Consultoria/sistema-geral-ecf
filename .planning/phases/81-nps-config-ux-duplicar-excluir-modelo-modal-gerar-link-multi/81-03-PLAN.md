---
phase: 81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi
plan: 03
type: execute
wave: 2
depends_on: ["81-01"]
files_modified:
  - resources/js/Components/Nps/Config/TemplateEditForm.jsx
  - resources/js/Pages/Nps/Configuracao.jsx
autonomous: true
requirements: [DEC-81-1, DEC-81-2]

must_haves:
  truths:
    - "O admin vê um botão Duplicar no editor de modelo e, ao clicar, a lista recarrega com o novo clone"
    - "O admin vê um botão Excluir no editor; ele fica desabilitado quando o modelo é o principal"
    - "Excluir pede confirmação; em bloqueio 422 o flash de erro aparece; em sucesso a lista recarrega"
  artifacts:
    - path: "resources/js/Components/Nps/Config/TemplateEditForm.jsx"
      provides: "handlers duplicar()/excluir() + botões no header do editor"
      contains: "templates.duplicate"
  key_links:
    - from: "TemplateEditForm duplicar()"
      to: "route nps.configuracao.templates.duplicate"
      via: "router.post + onSaved (refresh lista)"
      pattern: "templates.duplicate"
    - from: "TemplateEditForm excluir()"
      to: "route nps.configuracao.templates.destroy"
      via: "router.delete + confirm"
      pattern: "templates.destroy"
---

<objective>
Adicionar os botões **Duplicar** e **Excluir** no editor de modelo NPS
(`TemplateEditForm`), espelhando o padrão de `alternarAtivo`/`definirPrincipal`,
e garantir que a `Configuracao` recarregue a lista após a ação.

Purpose: expor as ações backend do 81-01 na UX da config.
Output: handlers `duplicar()`/`excluir()` + botões no header do editor;
recarga da lista via `onSaved`/`refresh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-CONTEXT.md
@.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-RESEARCH.md
@resources/js/Components/Nps/Config/TemplateEditForm.jsx
@resources/js/Pages/Nps/Configuracao.jsx

<interfaces>
<!-- Padrão já existente em TemplateEditForm.jsx (L218-254). -->

alternarAtivo(): router.patch(route('...toggle-active', template.id), {}, { preserveScroll, onSuccess: () => { mostrarToast?.(); onSaved?.(template.id); } })
definirPrincipal(): router.patch(route('...set-principal', template.id), ...) com confirm()
bloquearDesativacao = template.is_default && template.active  // padrão de guarda UI

Configuracao.jsx: `const refresh = () => router.reload({ only: ['templates'] })` (L201)
  passa `onSaved={refresh}` ao editor (L218) e `onSaved={(savedId)=>{...}}` no bloco de edição (L282-284).
Ícones lucide já usados no projeto: Copy, Trash2 (importar de 'lucide-react').
Tokens ecf-*, cn() de '@/lib/utils'. Rotas via Ziggy route().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: handlers e botões Duplicar/Excluir no TemplateEditForm</name>
  <files>resources/js/Components/Nps/Config/TemplateEditForm.jsx</files>
  <action>
    Adicionar `duplicar()`: `router.post(route('nps.configuracao.templates.duplicate', template.id), {}, { preserveScroll: true, onSuccess: () => { mostrarToast?.(); onSaved?.(template.id); } })`.
    Após duplicar, a lista recarrega (DEC-81-1 revisado: recarregar a lista, NÃO abrir o editor do clone).
    Adicionar `excluir()`: guarda UI `if (template.is_default) return;`; `if (! confirm('Excluir o modelo "'+ (template.nome || 'Modelo #'+template.id) +'"? Esta ação não pode ser desfeita.')) return;`
    então `router.delete(route('nps.configuracao.templates.destroy', template.id), { preserveScroll: true, onSuccess: () => { mostrarToast?.(); onSaved?.(template.id); } })`.
    O erro 422 (is_default / tem respostas) já é surfaced pelo flash global de erro (HandleInertiaRequests) — não tratar manualmente.
    Renderizar dois botões no header do editor, junto aos existentes (ativar/principal): "Duplicar"
    (ícone Copy) e "Excluir" (ícone Trash2). Desabilitar o "Excluir" quando `template.is_default`
    (mesma lógica de guarda visual de `bloquearDesativacao`), com title/hint explicando "defina outro
    modelo como principal antes". Usar tokens ecf-* e `cn()` consistentes com os botões vizinhos.
    ATENÇÃO ao gotcha Rollup (Pitfall 4): não computar flag no escopo do componente e usá-la dentro de
    um `.map()` — aqui não há map, então derivar `podeExcluir` no escopo do componente é seguro.
  </action>
  <verify>
    <automated>npx eslint resources/js/Components/Nps/Config/TemplateEditForm.jsx || echo "eslint ausente — validar no build do plano 81-04"</automated>
  </verify>
  <done>Botões Duplicar/Excluir renderizados no header; Excluir disabled quando is_default; handlers usam as rotas do 81-01 e recarregam a lista via onSaved.</done>
</task>

</tasks>

<verification>
- Build verde é validado no plano 81-04 (checkpoint visual + `npm run build`).
- Handlers referenciam `nps.configuracao.templates.duplicate` e `.destroy`.
</verification>

<success_criteria>
Editor de modelo exibe Duplicar e Excluir; Excluir bloqueado no principal; ações recarregam a lista.
</success_criteria>

<output>
Criar `.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-03-SUMMARY.md` ao concluir.
</output>
