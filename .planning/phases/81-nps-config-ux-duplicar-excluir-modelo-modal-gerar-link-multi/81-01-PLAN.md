---
phase: 81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/NpsTemplateController.php
  - routes/web.php
  - tests/Feature/V16/DuplicarModeloTest.php
  - tests/Feature/V16/ExcluirModeloTest.php
autonomous: true
requirements: [DEC-81-1, DEC-81-2]

must_haves:
  truths:
    - "Admin duplica um modelo e obtém um clone completo (perguntas+opções+serviços) com is_default=false"
    - "Duplicar o modelo principal NÃO gera um segundo is_default (sem QueryException 23000)"
    - "O modelo original permanece intocado após a duplicação"
    - "Excluir um modelo não-principal SEM respostas funciona e limpa a config (perguntas/opções/scopes)"
    - "Excluir o modelo principal é bloqueado com 422 e mensagem clara"
    - "Excluir um modelo COM respostas é bloqueado com 422 sugerindo arquivar (toggleActive)"
  artifacts:
    - path: "app/Http/Controllers/NpsTemplateController.php"
      provides: "métodos duplicate() e destroy()"
      contains: "public function duplicate"
    - path: "routes/web.php"
      provides: "rotas duplicate + destroy no grupo role:admin"
      contains: "templates.duplicate"
    - path: "tests/Feature/V16/DuplicarModeloTest.php"
      provides: "cobertura DEC-81-1"
    - path: "tests/Feature/V16/ExcluirModeloTest.php"
      provides: "cobertura DEC-81-2 (3 cenários)"
  key_links:
    - from: "NpsTemplateController@duplicate"
      to: "NpsTemplate::replicate + servicos()->sync"
      via: "DB::transaction"
      pattern: "replicate\\(|servicos\\(\\)->sync"
    - from: "NpsTemplateController@destroy"
      to: "surveys()->whereHas('response')"
      via: "guarda de negócio antes do delete"
      pattern: "whereHas\\('response'\\)"
---

<objective>
Implementar o backend das ações **Duplicar** e **Excluir** modelo NPS no
`NpsTemplateController`, com rotas no grupo `role:admin` e testes Feature em
`tests/Feature/V16/`.

Purpose: dar ao admin controle direto sobre modelos (clonar um modelo existente
para variar o serviço coberto; excluir modelos descartáveis) SEM destruir
histórico nem quebrar o dashboard NPS.
Output: `duplicate()` + `destroy()` no controller, 2 rotas nomeadas, 2 arquivos
de teste (DuplicarModeloTest, ExcluirModeloTest).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-CONTEXT.md
@.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-RESEARCH.md
@app/Http/Controllers/NpsTemplateController.php
@app/Models/NpsTemplate.php
@tests/Feature/V16/CriaCenarioResponsaveis.php

<interfaces>
<!-- Contratos que o executor usa direto — sem explorar o codebase. -->

NpsTemplate (fillable): nome, descricao, active, is_default, priority, envio_automatico_mensal, mensagem_whatsapp
NpsTemplate relations:
  - questions(): HasMany NpsTemplateQuestion (ordenada por ordem, id)
  - servicos(): BelongsToMany Servico via nps_template_service_scopes (template_id, servico_id)
  - surveys(): HasMany NpsSurvey via template_id (nullOnDelete)
NpsTemplateQuestion (fillable): template_id, texto, tipo, dimensao, obrigatoria, ordem
  - options(): HasMany NpsTemplateOption (question_id)
NpsTemplateOption (fillable): question_id, label, peso, ordem
NpsSurvey: relation response() (uma resposta por survey)

Molde de transação já no controller: setPrincipal() (L188-210) usa DB::transaction.
Guarda is_default já usada em update()/toggleActive() (abort 422).

Rotas existentes (routes/web.php, grupo ['auth','verified','role:admin']):
  - L161 PUT   .../templates/{template}          → update
  - L163 PATCH .../templates/{template}/toggle-active → toggleActive
  - L168 PATCH .../templates/{template}/set-principal → setPrincipal
  Adicionar as novas rotas logo após a L169 (set-principal), dentro do MESMO grupo.

FK verificadas nas migrations:
  - nps_template_questions.template_id → cascadeOnDelete
  - nps_template_options.question_id   → cascadeOnDelete (via pergunta)
  - nps_template_service_scopes.template_id → cascadeOnDelete
  - nps_surveys.template_id → nullOnDelete
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: duplicate() — clonagem atômica do modelo</name>
  <files>app/Http/Controllers/NpsTemplateController.php, routes/web.php, tests/Feature/V16/DuplicarModeloTest.php</files>
  <behavior>
    - POST duplicate cria um novo NpsTemplate com nome "{nome} (cópia)" e is_default=false.
    - O clone recebe TODAS as perguntas do original (texto/tipo/dimensao/obrigatoria/ordem) e, para cada pergunta, TODAS as opções (label/peso/ordem).
    - O pivot nps_template_service_scopes do clone = mesmos servico_id do original.
    - Duplicar o modelo PRINCIPAL (is_default=true) produz clone is_default=false — sem QueryException 23000 (não colide no unique parcial).
    - O modelo original permanece com o mesmo nome, mesmas perguntas/opções/scopes (intocado).
  </behavior>
  <action>
    Criar `public function duplicate(NpsTemplate $template)` per DEC-81-1. Antes do loop,
    fazer `$template->load('questions.options')` para evitar N+1 (Pitfall 5). Dentro de
    `DB::transaction`: usar `$template->replicate()`, então setar explicitamente
    `is_default=false` (INVARIANTE — Pitfall 2) e `nome = $template->nome.' (cópia)'`; salvar.
    Herdar active/priority/envio_automatico_mensal/mensagem_whatsapp do replicate (não sobrescrever).
    Para cada pergunta: `$q->replicate()`, setar `template_id` do clone, salvar; para cada opção:
    `$o->replicate()`, setar `question_id` do clone da pergunta, salvar. Depois
    `$clone->servicos()->sync($template->servicos()->pluck('servicos.id')->all())`.
    Retornar `back()->with('success', ...)`. Registrar a rota POST
    `nps.configuracao.templates.duplicate` logo após a rota set-principal (L169), no grupo role:admin.
    Escrever DuplicarModeloTest com namespace `Tests\Feature\V16`, `use RefreshDatabase`, autenticado
    como admin; montar template com 2 perguntas (uma com opções) + 1 servico no pivot; assertar clone
    (contagem de perguntas/opções/scopes iguais, is_default=false, nome com "(cópia)") e original intocado;
    incluir cenário duplicando o principal sem exceção.
  </action>
  <verify>
    <automated>php artisan test --filter=DuplicarModeloTest</automated>
  </verify>
  <done>DuplicarModeloTest verde; clone completo com is_default=false; original intocado; rota registrada.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: destroy() — exclusão com guardas de negócio</name>
  <files>app/Http/Controllers/NpsTemplateController.php, routes/web.php, tests/Feature/V16/ExcluirModeloTest.php</files>
  <behavior>
    - destroy de modelo não-principal e SEM respostas: apaga o template; cascade remove perguntas/opções/scopes; retorna success.
    - destroy do modelo is_default=true: abort(422) "modelo principal não pode ser excluído" — template permanece no banco.
    - destroy de modelo com pelo menos 1 survey respondido: abort(422) sugerindo arquivar (toggleActive) — template permanece.
  </behavior>
  <action>
    Criar `public function destroy(NpsTemplate $template)` per DEC-81-2. Guarda 1: se
    `$template->is_default` → `abort(422, 'O modelo principal não pode ser excluído. Defina outro modelo como principal antes.')`
    (espelha update/toggleActive). Guarda 2: se `$template->surveys()->whereHas('response')->exists()` →
    `abort(422, 'Este modelo tem respostas associadas. Desative-o (arquivar) em vez de excluir — assim o histórico e as métricas continuam corretos.')`
    — evita o Pitfall 1 (nullOnDelete zeraria as notas no dashboard). Caso contrário, `$template->delete()`
    (cascade limpa a config) e `back()->with('success', ...)`. Registrar a rota DELETE
    `nps.configuracao.templates.destroy` logo após a rota duplicate, no grupo role:admin.
    Escrever ExcluirModeloTest (namespace V16, RefreshDatabase, admin) com 3 cenários: (a) modelo
    não-principal sem respostas → 200/redirect + assertDatabaseMissing do template e das perguntas;
    (b) modelo is_default → assertStatus 422 + template ainda existe; (c) modelo com NpsSurvey+resposta →
    422 + template ainda existe. Reusar fábricas/DB::table conforme padrão dos testes V16 existentes.
  </action>
  <verify>
    <automated>php artisan test --filter=ExcluirModeloTest</automated>
  </verify>
  <done>ExcluirModeloTest verde nos 3 cenários; guardas is_default e "tem respostas" retornam 422; histórico preservado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (admin) → controller | payload de duplicar/excluir cruza aqui; is_default nunca vem do request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01 | Tampering | destroy() em modelo com histórico | mitigate | Guarda `surveys()->whereHas('response')->exists()` → 422 (Pitfall 1: nullOnDelete zeraria notas no dashboard) |
| T-81-02 | Denial of Service | destroy() do modelo principal | mitigate | Guarda `is_default` → 422 (quebraria resolveForCompany/disparo mensal) |
| T-81-03 | Elevation of Privilege | duplicate() promovendo clone a principal via payload | mitigate | `is_default=false` forçado no clone; não vem do request (Pitfall 2) |
| T-81-04 | Spoofing | rotas fora do gate | mitigate | rotas dentro do grupo `role:admin` (EnsureUserHasRole) |
</threat_model>

<verification>
- `php artisan test --filter=DuplicarModeloTest` e `--filter=ExcluirModeloTest` verdes.
- `php artisan test --filter=Nps` (regressão CRUD templates) sem quebras.
</verification>

<success_criteria>
duplicate() e destroy() implementados com guardas; 2 rotas no grupo role:admin;
DuplicarModeloTest + ExcluirModeloTest verdes; nenhum modelo com is_default duplicado ou excluído.
</success_criteria>

<output>
Criar `.planning/phases/81-nps-config-ux-duplicar-excluir-modelo-modal-gerar-link-multi/81-01-SUMMARY.md` ao concluir.
</output>
