---
phase: 36-comercial-uxe-atribuir-servico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/ComercialController.php
  - resources/js/Pages/Comercial/Empresas.jsx
  - routes/web.php
  - resources/js/Layouts/AppLayout.jsx
autonomous: true
requirements:
  - REQ-36-01
  - REQ-36-02

must_haves:
  truths:
    - "Rota /comercial/empresas vira redirect 302 para /comercial/empresas/novo (preserva nome comercial.empresas para compat — sidebar links nao quebram)"
    - "ComercialController::empresas() removido (logica obsoleta de listagem + edit)"
    - "resources/js/Pages/Comercial/Empresas.jsx APAGADO (arquivo nao mais usado)"
    - "ComercialController::update() pode permanecer aceitando filha_ids OU simplificar removendo — manter por compat com possiveis outros consumers (ex: /companies modal admin)"
    - "AppLayout.jsx — item Empresas do Comercial atualizado: aponta direto para route('comercial.empresas.novo') OU mantem comercial.empresas (vai cair no redirect, funciona igual)"
    - "Build verde + suite Phase 31+33+34+35 verde (zero regressao)"
  artifacts:
    - path: "routes/web.php"
      provides: "Redirect rota comercial.empresas"
      contains: "comercial.empresas.novo"
    - path: "app/Http/Controllers/ComercialController.php"
      provides: "empresas() removido"
      contains: "ComercialController"
---

# Plan 36-01: Simplifica /comercial/empresas

Wave 1. Independente.

## Escopo

1. **`routes/web.php`**: substituir definição da rota `comercial.empresas`:
   ```php
   // ANTES (definicao via metodo):
   Route::get('/comercial/empresas', [ComercialController::class, 'empresas'])->name('comercial.empresas');

   // DEPOIS (redirect direto):
   Route::get('/comercial/empresas', function () {
       return redirect()->route('comercial.empresas.novo');
   })->name('comercial.empresas');
   ```

2. **`app/Http/Controllers/ComercialController.php`**:
   - Apagar método `empresas()` inteiro (linhas 69-138 aprox)
   - Remover imports não usados após remoção (verificar Inertia, Setor — se for único user dessa import, remover)

3. **`resources/js/Pages/Comercial/Empresas.jsx`** — APAGAR arquivo inteiro.

4. **`resources/js/Layouts/AppLayout.jsx`** — verificar item de menu "Empresas" do Comercial. Se aponta para `route('comercial.empresas')`, ajustar pra `route('comercial.empresas.novo')` E renomear label de "Empresas" para "Cadastrar empresa" (clareza). Se já aponta pra novo, manter.

## Constraints

- pt-BR comentários
- Não fazer deploy
- Manter os 70 testes anteriores verdes (zero regressão)
- Não tocar em `ComercialController::create` (rota `comercial.empresas.novo`), `store`, `update`, helper `servicoDisparaImplementacao`, `criarImplementacaoPolo`
- Não tocar em `Comercial/NovaEmpresa.jsx`

## Verificação

- `php artisan route:list | grep "comercial.empresas"` mostra rota redirecting + a nova rota
- Acesso `/comercial/empresas` → 302 → `/comercial/empresas/novo`
- Sidebar item "Cadastrar empresa" (ou "Empresas") aponta para rota correta
- Build verde
- Suite verde

## Commits

Atômicos:
- `feat(36-01): /comercial/empresas vira redirect para /novo`
- `feat(36-01): remove Comercial/Empresas.jsx + AppLayout aponta para /novo`

Final: `36-01-SUMMARY.md`.
