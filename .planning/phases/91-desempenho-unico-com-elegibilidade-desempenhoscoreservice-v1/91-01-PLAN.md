---
phase: 91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/Services/DesempenhoScoreService.php
  - tests/Feature/V16/DesempenhoElegibilidadeTest.php
  - tests/Feature/Phase74/DesempenhoScoreServiceTest.php
  - tests/Feature/V16/BonusDualPathRegressaoTest.php
autonomous: true
requirements: [DESEMP-01, DESEMP-03, DESEMP-04, DESEMP-05, DESEMP-06, DESEMP-07]

must_haves:
  truths:
    - "Profissional só-Shopee permanece no ranking (sem_carteira=false) com score_status='blocked', nota_final=null e faixa_bonus=null"
    - "Profissional misto (Performance+Shopee) recebe score_status='official' com financeiro calculado APENAS das empresas de vínculo elegível"
    - "Profissional só-Performance mantém nota byte-a-byte idêntica à régua anterior (âncora Carlos 4.08/basico verde SEM editar a expectativa)"
    - "compute() expõe empresas_unicas, vinculos_servico, vinculos_financeiros, vinculos_sem_fonte_financeira, score_status e componentes_disponiveis"
    - "computeCached grava sob desempenho.compute.v4 e nunca devolve valores gravados sob v3"
    - "computeNpsMedio, notasPorAtribuicao e notasLegado permanecem intocados (testes 1-4 do BonusDualPathRegressaoTest sem nenhuma alteração)"
    - "sem_carteira=true só quando o profissional tem ZERO vínculos ativos de qualquer setor"
  artifacts:
    - path: "app/Services/DesempenhoScoreService.php"
      provides: "computeUniverso via CarteiraContextService + score_status + metadados + cache v4"
      contains: "CarteiraContextService"
    - path: "tests/Feature/V16/DesempenhoElegibilidadeTest.php"
      provides: "Suite TDD dos cenários blocked/official/partial/dedup/sem_carteira/cache v4"
      min_lines: 200
    - path: "tests/Feature/Phase74/DesempenhoScoreServiceTest.php"
      provides: "Fixture criarEmpresaNaCarteira forward-compat (contratos_servico + servico_id)"
      contains: "contratos_servico"
  key_links:
    - from: "app/Services/DesempenhoScoreService.php"
      to: "app/Services/Portfolio/CarteiraContextService.php"
      via: "forUser() + contadores() dentro de computeUniverso"
      pattern: "forUser\\("
    - from: "computeUniverso"
      to: "computeVarFaturamento/computeVarMargem"
      via: "EloquentCollection<Company> filtrada por financial_metrics_eligible e deduplicada"
      pattern: "financial_metrics_eligible"
    - from: "computeCached"
      to: "Cache Redis"
      via: "chave versionada bumpada"
      pattern: "desempenho\\.compute\\.v4"
---

<objective>
Refatorar `DesempenhoScoreService::computeUniverso` para derivar o universo de empresas dos VÍNCULOS DE SERVIÇO ativos (`CarteiraContextService::forUser`) em vez da carteira consolidada (`$user->companies()`), com TDD rigoroso: os componentes financeiros passam a usar só vínculos `financial_metrics_eligible=true`, a nota ganha `score_status` (`official`/`partial`/`blocked`), o retorno expõe os 6 metadados de auditoria e a chave de cache é bumpada v3→v4.

Purpose: eliminar o bug medido em prod onde responsável só-Shopee "herda" faturamento/margem ML de empresas que não gerencia (Felipe: avaliado sobre 29 empresas gerenciando 4; Matheus: 15 gerenciando 0), sem nunca criar um segundo score por marketplace.

Output: `DesempenhoScoreService.php` refatorado + suite `tests/Feature/V16/DesempenhoElegibilidadeTest.php` nova + fixture Phase74 forward-compat, tudo verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-RESEARCH.md
@.planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-CONTEXT.md
@.planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-VALIDATION.md
@app/Services/DesempenhoScoreService.php
@app/Services/Portfolio/CarteiraContextService.php
@tests/Feature/Phase74/DesempenhoScoreServiceTest.php
@tests/Feature/V16/BonusDualPathRegressaoTest.php
@tests/Feature/V16/CriaCenarioResponsaveis.php

## Decisões travadas (NÃO reabrir — proveniência registrada em 91-CONTEXT.md)

- **`official`** = todos os componentes disponíveis. Profissional MISTO (ML+Shopee) é OFFICIAL — o financeiro vem do subconjunto elegível dele, todos os componentes existem. (Isto CORRIGE a proposta do research, que sugeria `partial` pra misto — D-91-02 no 91-CONTEXT.md.)
- **`partial`** = tem vínculo financeiro elegível (`vinculos_financeiros > 0`), mas algum componente indisponível no período (var_faturamento_pct ou var_margem_pct = null). Mecanismo pronto pra futuro — NPS nunca é indisponível hoje (força 0.0 por DESEMP-03). Absenteísmo NÃO conta como componente (placeholder por spec).
- **`blocked`** = ZERO vínculos financeiros elegíveis (só-Shopee). Decisão do usuário 2026-07-16 via AskUserQuestion, com derivação `nota_final=null`/`faixa_bonus=null` comunicada e aceita na mesma conversa (D-91-01 no 91-CONTEXT.md): **nota_final=null, faixa_bonus=null** — sem nota oficial até a diretoria aprovar régua. Com null, a ordenação atual (`sortByDesc(nota ?? -1)`) já manda pro fim do ranking — NÃO criar tratamento especial de sort.
- `sem_carteira=true` SÓ com zero vínculos ativos de qualquer setor. Matheus (só-Shopee) permanece no ranking com `blocked`.
- Snapshot: compat de campos — `empresas_carteira` FICA (agora preenchido com o valor de `empresas_unicas`); campos novos são ADICIONADOS ao shape (absorvidos pelo `breakdown_json` JSON-cast, sem migração).
- A MATEMÁTICA de `computeVarFaturamento`/`computeVarMargem`/`computeNotaFinal` NÃO muda; muda só o conjunto de empresas de entrada. `computeNpsMedio` INTOCADO (dual-path da Fase 80).
- Bump da chave de cache **v3→v4 OBRIGATÓRIO** (padrão dos 2 bumps anteriores documentados no próprio código, linhas 140-150).
- Meses fechados NÃO são reprocessados (snapshots ficam com a régua da época).

## FRONTEIRA (sessão paralela ativa — Fase 94/NPS nesta mesma árvore)

- Tocar SOMENTE: `DesempenhoScoreService.php` + os 3 arquivos de teste listados em `files_modified`.
- NÃO tocar: PerformanceController (Fase 92), PortfolioController, User.php, Company*, qualquer código NPS (`computeNpsMedio`/`notasPorAtribuicao`/`notasLegado` — nem uma linha), models/services de NPS.
- No `BonusDualPathRegressaoTest`: alteração MÍNIMA e cirúrgica apenas no teste 5 (`test_cache_bumpado_para_v3`) — ver Task 2. Testes 1-4 são a prova formal do DESEMP-03 e ficam intactos.
- `git add` SEMPRE com caminhos explícitos (nunca `git add -A`/`git add .`).
- NÃO usar `gsd-sdk state.advance-plan`/`update-progress` (mutaria o estado da outra sessão).
- NÃO fazer deploy.
- php local: `C:\xampp\php\php.exe`. MySQL local fora — a suite roda em SQLite `:memory:` (forçado pelo phpunit.xml).

<interfaces>
<!-- Contratos que o executor usa diretamente — sem exploração de codebase. -->

De app/Services/Portfolio/CarteiraContextService.php (Fase 88, testado, NÃO alterar):
```php
// forUser: um vínculo por (company_id, servico_id). Misto = 2 linhas, não 1.
public function forUser(User $user, array $filters = []): Collection
// shape de cada linha:
// ['user_id','company_id','company_name','servico_id'(?int),'servico_nome',
//  'setor','role','role_label','has_financial_source','financial_source',
//  'financial_metrics_eligible'(bool)]

public function contadores(Collection $vinculos): array
// ['empresas_unicas'=>int,'vinculos_servico'=>int,
//  'vinculos_financeiros'=>int,'vinculos_sem_fonte_financeira'=>int]
```

Assinaturas que NÃO mudam em DesempenhoScoreService.php:
```php
private function computeVarFaturamento(User $user, Carbon $mes, EloquentCollection $companies): array
private function computeVarMargem(User $user, Carbon $mes, EloquentCollection $companies): ?float
private function computeNpsMedio(User $user, Carbon $mes): float   // INTOCADO
private function computeNotaFinal(?float $nps, ?float $varFat, ?float $varMargem): ?float  // INTOCADO
```
Ambos os métodos financeiros já retornam null/0 para Collection vazia — passar
Collection vazia no caso blocked NÃO exige guard novo.

Novo método privado (semântica travada):
```php
private function computeScoreStatus(array $contadores, ?float $varFat, ?float $varMargem): string
{
    if ($contadores['vinculos_financeiros'] === 0) {
        return 'blocked';   // só-Shopee / zero elegíveis — DESEMP-06
    }
    if ($varFat === null || $varMargem === null) {
        return 'partial';   // componente financeiro indisponível no período
    }
    return 'official';      // todos os componentes disponíveis (inclui MISTO)
}
```

Chaves NOVAS no shape de retorno de compute() (ADITIVAS — nada é removido):
```php
'empresas_unicas'               => int,
'vinculos_servico'              => int,
'vinculos_financeiros'          => int,
'vinculos_sem_fonte_financeira' => int,
'score_status'                  => 'official'|'partial'|'blocked',
'componentes_disponiveis'       => [
    'nps_medio'           => true,             // sempre true (DESEMP-03 força 0.0)
    'var_faturamento_pct' => $varFat !== null,
    'var_margem_pct'      => $varMargem !== null,
],
// compat: 'empresas_carteira' passa a receber o valor de empresas_unicas
```

Trait de fixture pronta (tests/Feature/V16/CriaCenarioResponsaveis.php — reutilizar, NÃO alterar):
```php
protected function criarServico(string $setor, bool $ativo = true): int
protected function criarContrato(int $companyId, int $servicoId, bool $ativo = true): int
protected function inserirPivot(int $companyId, int $userId, string $role, ?int $servicoId): int
```

Seed de bonus_faixas (migration 2026_07_09_140003): sem_bonus [0.00,3.99] ·
basico [4.00,4.49] · intermediario [4.50,4.99] · maximo [5.00,5.00].
</interfaces>

## Cenários da suite nova (valores exatos — determinismo obrigatório)

Setup comum do arquivo `tests/Feature/V16/DesempenhoElegibilidadeTest.php`:
- `RefreshDatabase` + `use CriaCenarioResponsaveis` + `DB::statement('PRAGMA foreign_keys = ON')`.
- `Carbon::setTestNow('2026-08-15 10:00:00')` — agosto/2026 é o mês EM CURSO (janela dia 1-15 vs dia 1-15 de julho); `tearDown` reseta.
- Setor/cargos: replicar o padrão do `BonusDualPathRegressaoTest::setUp` (setores slug `performance-91` + cargo `analista`); helper `criarUserComCargo` local.
- Stub LOCAL de `MetricsProviderFactory` bindado no container: subclasse mínima no fim do próprio arquivo (constructor vazio sem parent, `caseFor()` retorna `'so-adman'` sempre, `forCompany()` retorna `[]`) — força o path Adman em ambas as janelas. NÃO importar o stub do arquivo Phase74 (classe secundária de outro arquivo de teste não é autoload-confiável com `--filter`).
- Helper local de empresa: `Company::factory()` + forçar `created_at` = -3 meses (padrão Phase74, `timestamps=false` + `forceFill`) — fora do filtro "empresa nova".
- Helper local de Adman: 1 row `AdmanMetric` no dia 10 do mês (`revenue`, `contribution_margin`) — dia 10 existe nas duas janelas (dias comuns OK).
- `compute()` chamado com `Carbon::parse('2026-08-01')`.

| # | Teste | Cenário | Asserts exatos |
|---|-------|---------|----------------|
| 1 | `test_so_shopee_recebe_blocked_com_nota_null_e_permanece_no_ranking` | 1 empresa, serviço SHOPEE (`criarServico(Servico::SETOR_SHOPEE)` + `inserirPivot(..., 'consultor', $servicoShopee)`); AdmanMetric da empresa: ago rev 11000/margem 10500, jul rev 10000/margem 10000 (dados que NÃO podem vazar) | `sem_carteira===false`; `score_status==='blocked'`; `nota_final===null`; `faixa_bonus===null`; `faixa_promovida===false`; `componentes['var_faturamento_pct']===null`; `componentes['var_margem_pct']===null`; `componentes['nps_medio']===0.0`; `empresas_unicas===1`; `vinculos_servico===1`; `vinculos_financeiros===0`; `vinculos_sem_fonte_financeira===1`; `empresas_com_baseline===0` |
| 2 | `test_misto_e_official_com_financeiro_so_do_vinculo_elegivel` | Empresa A: serviço PERFORMANCE preenchido (`criarServico(SETOR_PERFORMANCE)` + `criarContrato` + `inserirPivot`), Adman ago 10300/10280 · jul 10000/10000 (rev/margem → +3.00%/+2.80%). Empresa B: serviço SHOPEE, Adman ago rev 99999 · jul rev 1 (absurdo detectável se vazar) | `score_status==='official'`; `var_faturamento_pct≈3.00` (delta 0.001); `var_margem_pct≈2.80`; `empresas_com_baseline===1`; `empresas_unicas===2`; `vinculos_servico===2`; `vinculos_financeiros===1`; `vinculos_sem_fonte_financeira===1`; `nota_final≈3.00` (nps 0.0→1pt clamp; réguas 4+4; (1+4+4)/3); `faixa_bonus==='sem_bonus'`; `empresas_carteira===2` |
| 3 | `test_dois_vinculos_na_mesma_empresa_nao_duplicam_financeiro` | Empresa X única: pivot performance (servico perf) E pivot shopee (servico shopee), mesmo user, role consultor nas duas; Adman ago rev 10500 · jul rev 10000 (+5.00%) | `empresas_unicas===1`; `vinculos_servico===2`; `vinculos_financeiros===1`; `empresas_com_baseline===1` (dedup — se duplicasse seria 2); `var_faturamento_pct≈5.00` |
| 4 | `test_partial_quando_vinculo_elegivel_sem_dados_financeiros_no_periodo` | 1 empresa performance elegível, ZERO rows AdmanMetric | `score_status==='partial'`; `var_faturamento_pct===null`; `var_margem_pct===null`; `nota_final≈1.00` (só NPS 0.0→1pt); `faixa_bonus==='sem_bonus'` (nota calculada normalmente — partial NÃO zera nota) |
| 5 | `test_sem_carteira_so_quando_zero_vinculos_ativos` | User elegível sem NENHUMA linha em company_users | `sem_carteira===true`; motivo contém "Sem carteira em"; `nota_final===null`; shape TAMBÉM expõe os 6 metadados novos zerados (`empresas_unicas===0`, `vinculos_financeiros===0`, `score_status==='blocked'`, `componentes_disponiveis` todos false) |
| 6 | `test_compute_expoe_os_6_metadados_de_elegibilidade` | Reusar cenário do teste 2 (ou fixture mínima performance) | `assertArrayHasKey` para as 6 chaves + subchaves `nps_medio`/`var_faturamento_pct`/`var_margem_pct` de `componentes_disponiveis`; `empresas_carteira === empresas_unicas` (compat DESEMP-05 — Fase 92 consome sem recomputar) |
| 7 | `test_cache_bumpado_para_v4` | User elegível SEM carteira (zero HTTP); `Cache::put` de lixo reconhecível (`['nota_final'=>99.9]`) sob a chave `desempenho.compute.v3.{id}.{Y-m}` | `computeCached` NÃO devolve o lixo; retorno é o shape recalculado (`sem_carteira===true`, `nota_final===null`); `Cache::has('desempenho.compute.v4.{id}.{Y-m}')===true`; chave v3 fica intacta (órfã, expira por TTL) |
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Fixture Phase74 forward-compat + suite nova de elegibilidade falhando</name>
  <files>tests/Feature/Phase74/DesempenhoScoreServiceTest.php, tests/Feature/V16/DesempenhoElegibilidadeTest.php</files>
  <behavior>
    - Os 7 cenários da tabela acima escritos em tests/Feature/V16/DesempenhoElegibilidadeTest.php DEVEM FALHAR contra o código atual (computeUniverso ainda usa $user->companies(); shape ainda não tem os metadados; cache ainda é v3).
    - A suite Phase74 inteira (12 testes) DEVE CONTINUAR VERDE com o fixture atualizado — a mudança de fixture é backward-compatible porque o código atual ignora contratos_servico/servico_id.
  </behavior>
  <action>
    Parte A — fixture Phase74 (Pitfall 1 do research: sem isso a suite inteira quebraria em massa na Task 2). Em `DesempenhoScoreServiceTest.php`, atualizar SOMENTE o helper `criarEmpresaNaCarteira()` (per decisão travada do orquestrador): (1) criar UMA vez por teste (property lazy `private ?int $servicoPerfId = null`) um serviço de setor performance via `DB::table('servicos')->insertGetId(...)` no padrão do trait `CriaCenarioResponsaveis::criarServico`; (2) para cada empresa criada, inserir `contratos_servico` ativo apontando pra esse serviço (padrão `criarContrato` do trait); (3) adicionar `'servico_id' => $this->servicoPerfId` no insert da pivot `company_users`. PRESERVAR: a API do helper (parâmetros `$pivotCreatedAt`/`$role`), os timestamps forçados, e TODOS os valores esperados dos 12 testes — a âncora Carlos 4.08/basico deve seguir passando SEM editar expectativa (se o valor mudar depois da Task 2, é bug da refatoração, não do teste). NÃO alterar o teste `test_nps_dimensao_por_cargo_estrategista_e_analista_diferem` (Douglas/Stefani insere pivot inline sem servico_id de propósito — só invoca computeNpsMedio por reflection, que não usa computeUniverso).

    Parte B — criar `tests/Feature/V16/DesempenhoElegibilidadeTest.php` com os 7 testes da tabela de cenários (valores exatos travados lá), usando a trait `CriaCenarioResponsaveis`, o setup comum descrito no contexto (Carbon congelado 2026-08-15, stub local de MetricsProviderFactory retornando 'so-adman'/[], helpers locais de empresa e AdmanMetric). Comentários e nomes em pt-BR, docblock de classe referenciando DESEMP-01/04/05/06/07 e a decisão D-91-01 do 91-CONTEXT.md (blocked → nota null, decisão do usuário 2026-07-16).

    Rodar as duas suites e confirmar: Phase74 verde, nova suite VERMELHA (falhas esperadas: chaves ausentes no shape, score_status inexistente, cache v3). Commit: `test(91-01): suite de elegibilidade por vínculo (RED) + fixture Phase74 forward-compat` — git add apenas dos 2 arquivos.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit --filter=DesempenhoScoreServiceTest (12/12 verde) && C:\xampp\php\php.exe vendor/bin/phpunit --filter=DesempenhoElegibilidadeTest (falha — RED confirmado)</automated>
  </verify>
  <done>Fixture Phase74 cria contratos_servico + servico_id sem alterar nenhum valor esperado; suite nova existe com os 7 cenários e falha pelo motivo certo (shape/status/cache ainda antigos); commit de teste feito com caminhos explícitos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Refatorar computeUniverso + score_status + metadados + bump cache v4</name>
  <files>app/Services/DesempenhoScoreService.php, tests/Feature/V16/BonusDualPathRegressaoTest.php</files>
  <behavior>
    - Os 7 testes da DesempenhoElegibilidadeTest passam.
    - Phase74 12/12 verde (âncora Carlos 4.08/basico intacta).
    - BonusDualPathRegressaoTest 5/5 verde (testes 1-4 sem NENHUMA alteração; teste 5 com edit mínimo: lixo plantado migra pra v3 e alvo de gravação migra pra v4).
  </behavior>
  <action>
    Em `DesempenhoScoreService.php` (implementa DESEMP-01/04/05/06/07 conforme decisões travadas):

    1. Imports: adicionar `use App\Models\Company;` e `use App\Services\Portfolio\CarteiraContextService;`. Constructor: adicionar `private CarteiraContextService $carteiraContext` via promoted property (DI padrão do projeto).

    2. `computeUniverso(User $user, Carbon $mes)`: substituir `$user->companies()->where('active', true)->get()` por `$this->carteiraContext->forUser($user, ['active' => true])`. Se `$vinculos->isEmpty()` → `sem_carteira=true` com o MESMO motivo pt-BR atual ("Sem carteira em {mês}") — DESEMP-07: só zero vínculos dispara sem_carteira. Caso contrário retornar `['sem_carteira'=>false, 'contadores'=>$this->carteiraContext->contadores($vinculos), 'companies_elegiveis'=>...]` onde companies_elegiveis é `Company::whereIn('id', $vinculos->where('financial_metrics_eligible', true)->pluck('company_id')->unique())->get()` — o `unique()` é o dedup defensivo do Pitfall 4 (empresa com 2 vínculos elegíveis nunca entra 2× no SUM), e devolver `EloquentCollection<Company>` preserva as assinaturas de computeVarFaturamento/computeVarMargem (que usam `$company->created_at`/`caseFor`).

    3. `compute()`: passar `companies_elegiveis` para os DOIS métodos financeiros (Collection vazia é válida — os métodos já retornam null/0). computeNpsMedio segue chamado igual (INTOCADO — nem parâmetro muda). Montar `componentes_disponiveis` (nps sempre true; var_fat/var_margem = not-null). Criar `computeScoreStatus(array $contadores, ?float $varFat, ?float $varMargem): string` com a semântica EXATA do bloco interfaces (blocked se vinculos_financeiros===0; partial se algum var null; senão official — misto é official, corrigindo a proposta do research por decisão do orquestrador, D-91-02). SE status==='blocked': forçar `$nota = null` ANTES de classificarFaixa (decisão do usuário 2026-07-16, D-91-01 do 91-CONTEXT.md — sem nota oficial até a diretoria aprovar régua); o fluxo existente então produz faixa null + promovida false sozinho, sem código especial. Adicionar ao shape de retorno as 6 chaves novas (ADITIVAS) e trocar `'empresas_carteira' => $companies->count()` por `'empresas_carteira' => $contadores['empresas_unicas']` (compat — mesmo valor prático pra só-performance). `pontos_componentes` e `periodo_meta` intocados.

    4. `shapeSemCarteira()`: adicionar as 6 chaves novas zeradas — contadores 0, `score_status => 'blocked'` (coerente com a fórmula: zero vínculos financeiros), `componentes_disponiveis` todos false.

    5. Bump da chave de cache (linha ~150): `desempenho.compute.v3` → `desempenho.compute.v4`, adicionando comentário pt-BR no padrão dos 2 bumps anteriores (v1→v2, v2→v3, já documentados nas linhas 140-149): "Bump v3→v4 em 2026-07-16 (Fase 91 · DESEMP-01): computeUniverso passou a derivar o universo dos vínculos de serviço (CarteiraContextService) — os valores v3 têm a nota da carteira consolidada por company_id (Shopee herdando financeiro ML). Chaves v3 viram órfãs e expiram por TTL."

    6. Atualizar docblocks afetados (classe: acrescentar nota da Fase 91; `compute()`: shape novo; `computeUniverso`: nova fonte) em pt-BR. NÃO tocar em `computeNpsMedio`, `notasPorAtribuicao`, `notasLegado`, `computeVarFaturamento`, `computeVarMargem` (corpo), `computeNotaFinal`, réguas, `classificarFaixa`, `promoverPor2MesesConsecutivos`.

    Em `BonusDualPathRegressaoTest.php` — edit MÍNIMO e cirúrgico APENAS no teste 5. A semântica do teste ("o cache não serve a chave da versão anterior e grava na versão nova") é preservada; apenas o PAR de versões avança um passo, acompanhando o bump desta fase: renomear `test_cache_bumpado_para_v3` → `test_cache_bumpado_para_v4`; o lixo reconhecível (`['nota_final'=>99.9]`) passa a ser plantado sob `desempenho.compute.v3` (a chave que fica órfã AGORA — é a v3 que representa o valor stale de prod após este deploy, não mais a v2) e a asserção de gravação passa a mirar `desempenho.compute.v4`. Manter as asserções de que o lixo nunca é servido, de que o retorno é o shape recalculado (`sem_carteira===true`/`nota_final===null`) e de que a chave antiga (v3) fica intacta/órfã expirando por TTL. Atualizar variáveis (`$chaveV2`→`$chaveV3`, `$chaveV3`→`$chaveV4`), mensagens e comentários coerentemente. Testes 1-4 (computeNpsMedio): ZERO alteração — são a prova formal do DESEMP-03. Nota: este teste fica intencionalmente equivalente ao `test_cache_bumpado_para_v4` da suite nova — redundância aceita (um vive na suite de regressão do dual-path, outro na suite da Fase 91).

    Rodar as 3 suites. Commit: `feat(91-01): universo do Desempenho por vínculo de serviço com elegibilidade financeira (DESEMP-01/04/05/06/07) + cache v4` — git add apenas dos 2 arquivos.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit --filter="DesempenhoScoreServiceTest|DesempenhoElegibilidadeTest|BonusDualPathRegressaoTest"</automated>
  </verify>
  <done>7/7 elegibilidade verde; Phase74 12/12 verde com âncora Carlos 4.08/basico SEM edição de expectativa; BonusDualPath 5/5 verde com testes 1-4 intocados; grep confirma "desempenho.compute.v4" no service e ausência de "$user->companies()" dentro de computeUniverso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cálculo → bônus pago | O output do service alimenta pagamento real de bônus — integridade do dado financeiro é o ativo |
| Redis/cache → UI | Cache versionado pode servir nota velha silenciosamente após deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01 | Tampering (integridade) | computeUniverso/compute | mitigate | Âncora Carlos 4.08/basico + BonusDualPath 5/5 como regressão bloqueante; dedup unique() no filtro elegível |
| T-91-02 | Information/Integrity | computeCached (chave v3 stale) | mitigate | Bump v4 + testes test_cache_bumpado_para_v4 (suite nova e regressão dual-path) |
| T-91-03 | Tampering | Snapshots de meses fechados | accept | Comportamento correto por design — meses fechados NÃO são reprocessados (documentado no Plano 91-02); nenhum comando de reprocessamento é adicionado |
</threat_model>

<verification>
- `C:\xampp\php\php.exe vendor/bin/phpunit --filter="DesempenhoScoreServiceTest|DesempenhoElegibilidadeTest|BonusDualPathRegressaoTest"` — tudo verde.
- `git diff HEAD~2 -- app/Services/DesempenhoScoreService.php` NÃO contém alterações em computeNpsMedio/notasPorAtribuicao/notasLegado (Success Criterion 3).
- `git status` limpo de arquivos fora da fronteira (nenhum arquivo NPS/controller alterado).
</verification>

<success_criteria>
- computeUniverso deriva de CarteiraContextService::forUser — `$user->companies()` não aparece mais em computeUniverso (Criterion 1).
- Só-Shopee: blocked + nota null + permanece no ranking; misto: official com financeiro só do elegível; só-performance: nota byte-a-byte igual (Criteria 4, 6, 7).
- Retorno expõe os 6 metadados (Criterion 5).
- Cache v4 gravando, v3 ignorado.
</success_criteria>

<output>
Criar `.planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-01-SUMMARY.md` ao concluir (pt-BR).
</output>
