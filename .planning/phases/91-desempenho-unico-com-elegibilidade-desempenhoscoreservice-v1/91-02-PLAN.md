---
phase: 91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1
plan: 02
type: execute
wave: 2
depends_on: [91-01]
files_modified: []
autonomous: true
requirements: [DESEMP-02]

must_haves:
  truths:
    - "Não existe nenhum score separado por marketplace no código (sem Score ML / Score Shopee / Score Geral)"
    - "Todos os consumidores de compute()/computeCached() seguem compatíveis com o shape novo sem edição (chaves aditivas)"
    - "As decisões de escopo estão declaradas por escrito: meses fechados não reprocessados; Matheus passa a gerar snapshot mensal; roteiro tinker pós-deploy pronto"
  artifacts:
    - path: ".planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-02-SUMMARY.md"
      provides: "Auditoria DESEMP-02 + declarações de escopo + roteiro de validação numérica pós-deploy"
      contains: "consolidar-mes"
  key_links:
    - from: "PerformanceController / ConsolidarMesDesempenho / SnapshotDesempenhoScores / WarmDesempenhoCache"
      to: "DesempenhoScoreService::compute/computeCached"
      via: "shape aditivo — nenhum consumidor editado nesta fase"
      pattern: "computeCached|compute\\("
---

<objective>
Fechar a Fase 91 com o gate do DESEMP-02 (prova de AUSÊNCIA de score separado por marketplace), auditoria de compatibilidade dos 8 consumidores do service com o shape novo, e documentação obrigatória das decisões de escopo + roteiro de validação numérica pós-deploy via tinker.

Purpose: DESEMP-02 é um requisito de ausência — nenhum teste unitário prova sozinho que algo NÃO existe; precisa de auditoria explícita. E dois efeitos colaterais em produção (Matheus ganhando snapshot mensal; snapshots fechados congelados na régua antiga) precisam estar declarados ANTES do deploy pra não parecerem bug.

Output: `91-02-SUMMARY.md` com a auditoria, as declarações de escopo e o roteiro tinker. Nenhum arquivo de código é modificado neste plano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-01-SUMMARY.md
@app/Console/Commands/ConsolidarMesDesempenho.php
@app/Console/Commands/SnapshotDesempenhoScores.php
@app/Console/Commands/WarmDesempenhoCache.php

## FRONTEIRA (sessão paralela ativa — Fase 94/NPS nesta mesma árvore)

- Este plano NÃO modifica código. Se a auditoria encontrar incompatibilidade real de consumidor, DOCUMENTAR no SUMMARY como pendência da Fase 92 — NÃO corrigir aqui (PerformanceController é fronteira da Fase 92).
- `git add` sempre com caminhos explícitos; não usar `gsd-sdk state.advance-plan`/`update-progress`.
- NÃO fazer deploy. NÃO rodar `desempenho:consolidar-mes` para nenhum mês (reescreveria bônus histórico — Pitfall 3 do research).
- Gate de regressão limitado ao domínio Desempenho/Bônus (a sessão paralela pode ter testes NPS em WIP — falhas fora do domínio são registradas como fora de escopo, não bloqueiam).

## Fatos já confirmados na pesquisa (validar, não redescobrir)

- `ConsolidarMesDesempenho` e `SnapshotDesempenhoScores` chamam `compute()` DIRETO (não cacheado) — confirmado por leitura; o docblock de `computeCached` exige isso.
- `breakdown_json` é cast JSON — absorve as 6 chaves novas sem migração.
- `PerformanceController::index()` linha ~144 rejeita `sem_carteira===true` antes do sort — com a regra nova, só-Shopee (Matheus) deixa de ser rejeitado e entra com `nota_final=null`, que o `sortByDesc(nota ?? -1)` manda pro fim naturalmente (decisão: sem tratamento especial de sort).
- Consumidor blocked no `ConsolidarMesDesempenho`: `score = round((null ?? 0.0)*20) = 0`, `classificacao = ''`, `tem_base_comparativa = false` — efeito colateral aceitável a declarar.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate DESEMP-02 (ausência de score separado) + auditoria dos consumidores + regressão de domínio</name>
  <files>(nenhum — verificação e coleta de evidência)</files>
  <action>
    1. Gate DESEMP-02 — provar a AUSÊNCIA: rodar grep case-insensitive em `app/`, `routes/` e `resources/js/` por padrões de score separado (`score_shopee`, `score_ml`, `ScoreShopee`, `ScoreMl`, `scoreGeral`, `Score Geral`, `ranking_shopee`) e por rotas/métodos novos que sugiram segundo ranking. Resultado esperado: ZERO ocorrências de implementação (ocorrências em comentários/planning não contam). Registrar o comando e o resultado como evidência.
    2. Confirmar que o único lugar onde "setor" aparece no service é nos metadados de auditoria (vinculos_financeiros/vinculos_sem_fonte_financeira via CarteiraContextService) — nunca como segunda nota: grep por `setor` em `app/Services/DesempenhoScoreService.php` deve retornar no máximo comentários/uso indireto do CarteiraContextService.
    3. Auditoria dos 8 consumidores (tabela do 91-RESEARCH.md): confirmar por leitura pontual (Grep, não releitura integral) que `PerformanceController` (index/show/dashboardCarteira), `WarmDesempenhoCache`, `ConsolidarMesDesempenho`, `SnapshotDesempenhoScores` continuam lendo apenas chaves que EXISTEM no shape novo (todas as antigas foram preservadas; `empresas_carteira` agora vale empresas_unicas — semanticamente equivalente). Qualquer leitura de chave removida (não deve haver nenhuma) → registrar como bloqueio.
    4. Regressão de domínio: rodar `C:\xampp\php\php.exe vendor/bin/phpunit --filter="Desempenho|Bonus"` — tudo verde é gate. Rodar também a suite Feature completa como INFORMATIVO: falhas fora do domínio Desempenho/Bônus/NPS-regressão são anotadas como pré-existentes/sessão paralela, sem bloquear.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit --filter="Desempenho|Bonus" (0 falhas) && grep -rin "score_shopee\|score_ml\|ScoreShopee\|ScoreMl" app/ routes/ resources/js/ | grep -v "^Binary" | wc -l (resultado 0)</automated>
  </verify>
  <done>Evidência coletada: zero score separado (DESEMP-02); 8 consumidores compatíveis com shape aditivo; suíte de domínio verde; eventuais falhas externas classificadas como fora de escopo.</done>
</task>

<task type="auto">
  <name>Task 2: SUMMARY com declarações de escopo + roteiro tinker de validação pós-deploy</name>
  <files>.planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-02-SUMMARY.md</files>
  <action>
    Escrever o `91-02-SUMMARY.md` (pt-BR) contendo obrigatoriamente, além do padrão do template:

    1. **Declaração de escopo — meses fechados NÃO reprocessados**: snapshots com `breakdown_json` já gravado permanecem com a régua da época (universo por company_id consolidado). Reprocessar um mês passado (`desempenho:consolidar-mes --mes=YYYY-MM`) reescreveria bônus potencialmente já pago — é decisão de negócio separada da diretoria, NUNCA passo técnico desta fase. Só o mês corrente em diante reflete a régua nova automaticamente.

    2. **Efeito colateral declarado — Matheus (só-Shopee)**: com `sem_carteira=false` sob a regra nova, ele PASSA a receber row de snapshot mensal do `ConsolidarMesDesempenho` (antes era pulado). A row dele sai com `score=0`, `classificacao=''`, `tem_base_comparativa=false` e `score_status='blocked'` dentro do breakdown_json — comportamento esperado, não bug.

    3. **Efeito colateral declarado — ranking**: blocked entra no ranking com `nota_final=null` e cai pro fim via `sortByDesc(nota ?? -1)` existente; a exibição do status é trabalho da Fase 92.

    4. **Roteiro de validação numérica pós-deploy (tinker no VPS — executar QUANDO o deploy for autorizado, não agora)**: script pronto que, para os users Felipe, Gustavo, Matheus, Luiz e Ana (busca por nome via User::where('name','like',...)), chama `app(DesempenhoScoreService::class)->compute($user, now()->startOfMonth())` e imprime nome, nota_final, score_status, empresas_unicas, vinculos_servico, vinculos_financeiros e empresas_com_baseline — comparando com a nota antiga ainda visível no cache v3 (`Cache::get(sprintf('desempenho.compute.v3.%d.%s', $id, now()->format('Y-m')))`) antes que expire. Expectativas a validar: Matheus → blocked/nota null (antes tinha nota com financeiro ML indevido); Felipe/Gustavo → contadores refletindo só os vínculos reais; Luiz/Ana → nota estável (carteiras 100% performance devem manter valor próximo ao anterior). Incluir o lembrete: NÃO rodar cache:clear (chaves v3 expiram por TTL) e NÃO rodar consolidar-mes retroativo.

    5. Evidências da Task 1 (comandos + resultados do gate DESEMP-02 e da auditoria de consumidores).

    Commit: `docs(91-02): fecha Fase 91 — gate DESEMP-02, escopo de snapshots e roteiro de validação pós-deploy` — git add apenas do SUMMARY (e do ROADMAP se o executor marcar os plans como concluídos).
  </action>
  <verify>
    <automated>grep -c "consolidar-mes" ".planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-02-SUMMARY.md" (>= 1) && grep -ci "matheus" ".planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-02-SUMMARY.md" (>= 1)</automated>
  </verify>
  <done>SUMMARY existe com as 3 declarações de escopo, o roteiro tinker pós-deploy com os 5 nomes e as evidências do gate DESEMP-02; commit feito com caminhos explícitos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot histórico → bônus já pago | Reprocessamento acidental de mês fechado reescreve valor financeiro histórico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-04 | Repudiation/Tampering | desempenho:consolidar-mes retroativo | mitigate | Declaração explícita no SUMMARY + nenhum passo de reprocessamento no plano; validação pós-deploy usa compute() isolado em tinker, nunca o command |
| T-91-05 | Elevation (escopo) | Segundo score por marketplace | mitigate | Gate de ausência (grep) documentado como evidência do DESEMP-02 |
</threat_model>

<verification>
- Gate DESEMP-02 com evidência gravada no SUMMARY.
- `phpunit --filter="Desempenho|Bonus"` verde.
- SUMMARY contém as declarações de escopo e o roteiro tinker.
</verification>

<success_criteria>
- Criterion 2 do ROADMAP provado por auditoria de ausência (score único por profissional).
- Fase 91 fechada com efeitos colaterais de produção declarados por escrito antes de qualquer deploy.
</success_criteria>

<output>
Criar `.planning/phases/91-desempenho-unico-com-elegibilidade-desempenhoscoreservice-v1/91-02-SUMMARY.md` (o próprio artefato principal deste plano).
</output>
