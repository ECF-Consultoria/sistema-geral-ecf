---
phase: 49-rankings-de-desempenho-por-fun-o-ranking-separado-de-publica
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-49-01
must_haves:
  truths:
    - "cargo_slug já existe em cada item de $ranking (PerformanceController::index() linhas 72-73) — filtrar pós-cálculo, não no service"
    - "PortfolioScoreService::compute(User $user) não precisa ser alterado — recebe user individual"
    - "Tabs só aparecem no setor consultoria (!isPolos) — proteger no frontend"
    - "URL pattern: /performance?setor=consultoria&cargo=analista — preserveState: true"
    - "Inertia já propaga 'setor' como 'consultoria' (linha 113 do controller) — adicionar 'cargo' no mesmo render()"
  artifacts:
    - path: "app/Http/Controllers/PerformanceController.php"
      provides: "filtro ?cargo pós-cálculo no método index()"
    - path: "resources/js/Pages/Performance/Index.jsx"
      provides: "grupo de 3 botões toggle Geral|Analistas|Estrategistas no header"
    - path: "tests/Feature/PerformanceCargoFilterTest.php"
      provides: "testes PHPUnit RED→GREEN para os 3 filtros de cargo"
  key_links:
    - from: "PerformanceController::index()"
      to: "Performance/Index.jsx"
      via: "Inertia::render('Performance/Index', ['cargo' => $cargo])"
      pattern: "prop 'cargo' propagada; componente usa para destacar botão ativo"
    - from: "Performance/Index.jsx botão Analistas"
      to: "PerformanceController::index()"
      via: "router.get(route('performance.index'), { setor:'consultoria', period, cargo:'analista' }, { preserveState: true })"
      pattern: "mesmo pattern dos seletores existentes (linhas 77-78 do Index.jsx)"
---

<objective>
Adicionar 3 tabs de filtro por cargo (Geral / Analistas / Estrategistas) na página `/performance`, exclusivamente no modo consultoria. No backend, `PerformanceController::index()` ganha o parâmetro `?cargo` que filtra o `$ranking` pós-cálculo via `cargo_slug` (já presente em cada item). No frontend, `Performance/Index.jsx` exibe um grupo de botões toggle inline (mesmo padrão visual do seletor Consultoria|Publicações existente) visível apenas quando `!isPolos`.

Esta wave é independente e de baixo risco: zero mudança no `PortfolioScoreService`, filtro de 3 linhas no controller, e adição de botões no header do componente. Inclui TDD com testes PHPUnit Feature (RED → GREEN) usando SQLite in-memory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-rankings-de-desempenho-por-fun-o-ranking-separado-de-publica/49-CONTEXT.md
@.planning/phases/49-rankings-de-desempenho-por-fun-o-ranking-separado-de-publica/49-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Testes PHPUnit Feature — RED (falham antes da implementação)</name>
  <files>tests/Feature/PerformanceCargoFilterTest.php</files>
  <read_first>
    - app/Http/Controllers/PerformanceController.php (linhas 22-114) — assinatura do index()
    - routes/web.php (linhas 242-247) — rota performance.index com middleware permission:core.performance
    - tests/Feature/ — ver exemplo de Feature test existente para padrão de setUp/RefreshDatabase
  </read_first>
  <behavior>
    Criar arquivo de teste que AINDA vai falhar (RED) porque o parâmetro ?cargo ainda não existe no controller.
    Os testes cobrem:
    1. Sem cargo: retorna todos os users do setor consultoria (analistas + estrategistas)
    2. cargo=analista: retorna somente items onde cargo_slug === 'analista'
    3. cargo=estrategista: retorna somente items onde cargo_slug === 'estrategista'
    4. cargo inválido (ex: 'gestor'): ignorado, retorna todos (geral)
    5. Prop 'cargo' é propagada no response Inertia
  </behavior>
  <action>
    1. Criar `tests/Feature/PerformanceCargoFilterTest.php`
    2. Usar trait `RefreshDatabase` com SQLite in-memory (phpunit.xml já configura isso)
    3. Criar user admin com factory + assignar permission 'core.performance' via setor/permissão
       — OU usar user com role 'admin' que tem acesso total (User::isAdmin() short-circuit em hasPermission())
    4. Criar 2 users consultor com cargo 'analista' e 1 user mentor com cargo 'estrategista'
       via `user_setores` e `cargos` (tabelas relacionadas) ou mockar via factory
    5. Testar:
       - GET /performance → response 200, prop ranking contém os 3 users, prop cargo === null
       - GET /performance?cargo=analista → prop ranking contém só os 2 analistas
       - GET /performance?cargo=estrategista → prop ranking contém só o 1 estrategista
       - GET /performance?cargo=invalido → prop ranking contém os 3 users (filtro ignorado)
    6. Rodar `php artisan test tests/Feature/PerformanceCargoFilterTest.php` — confirmar RED
    7. Commitar: `test(49-01): testes RED para filtro ?cargo em /performance`
  </action>
  <verify>
    `php artisan test tests/Feature/PerformanceCargoFilterTest.php` retorna FAILURES (RED esperado neste ponto)
  </verify>
  <done>Arquivo de teste criado, commit feito, testes FALHAM (RED confirmado)</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Backend — adicionar parâmetro ?cargo ao PerformanceController::index()</name>
  <files>app/Http/Controllers/PerformanceController.php</files>
  <read_first>
    - app/Http/Controllers/PerformanceController.php (linhas 22-114) — método index() completo
    - §2 do RESEARCH.md — Opção A recomendada: filtrar pós-cálculo no array PHP
  </read_first>
  <behavior>
    Adicionar leitura de `$cargo = $request->get('cargo')` no início do método index() (linha 30, depois do desvio para indexPolos).
    Após o cálculo completo de `$ranking` (após a ordenação e map de posicao, linha 108),
    aplicar filtro pós-cálculo se $cargo for 'analista' ou 'estrategista'.
    Propagar `cargo` no Inertia::render() junto com ranking, period, setor.
    Zero mudança no PortfolioScoreService. Zero mudança na query de $users.
  </behavior>
  <action>
    1. Ler `app/Http/Controllers/PerformanceController.php`
    2. Após a linha `$period = $request->get('period', '30');` (linha 30), adicionar:
       ```php
       // Filtro opcional por cargo (analista/estrategista); null = Geral (todos)
       $cargo = $request->get('cargo');
       if (!in_array($cargo, ['analista', 'estrategista'], true)) {
           $cargo = null; // ignora valores inválidos
       }
       ```
    3. Após `->values()` e `->map(function ($r, $idx) { ... })` (após linha 108), adicionar:
       ```php
       // Filtra por cargo pós-cálculo (cargo_slug já presente em cada item do ranking)
       if ($cargo !== null) {
           $ranking = $ranking->filter(fn ($r) => $r['cargo_slug'] === $cargo)->values();
       }
       ```
    4. Na linha `Inertia::render('Performance/Index', [...])` (linhas 110-114), adicionar `'cargo' => $cargo` no array de props
    5. Commitar: `feat(49-01): adiciona filtro ?cargo ao PerformanceController::index()`
  </action>
  <verify>
    `php artisan test tests/Feature/PerformanceCargoFilterTest.php` retorna GREEN (todos passam)
  </verify>
  <done>Testes GREEN; cargo filtrado corretamente; prop 'cargo' propagada no Inertia</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 3: Frontend — 3 botões toggle Geral|Analistas|Estrategistas em Performance/Index.jsx</name>
  <files>resources/js/Pages/Performance/Index.jsx</files>
  <read_first>
    - resources/js/Pages/Performance/Index.jsx (linhas 75-131) — assinatura do componente e bloco de filtros do header
    - §1 RESEARCH.md (linhas 62-78) — onde inserir: após seletor de setor, antes de period; mesmo padrão visual de toggle pill
  </read_first>
  <behavior>
    Adicionar prop `cargo = null` à assinatura do componente `PerformanceIndex`.
    No bloco de filtros do header (div com className "flex items-center gap-2", linha 92),
    inserir APÓS o grupo de botões Consultoria|Publicações e ANTES do SelectBox de period,
    um novo grupo de 3 botões `Geral | Analistas | Estrategistas`.
    O grupo só é renderizado quando `!isPolos`.
    Botão ativo usa classes `bg-ecf-yellow/[0.12] text-ecf-yellow` (mesmo pattern dos botões existentes).
    Click chama `applyFilter({ setor: 'consultoria', period, cargo: 'analista' })` etc.
    Botão "Geral" chama `applyFilter({ setor: 'consultoria', period })` (sem cargo — prop undefined remove da query string).
  </behavior>
  <action>
    1. Ler `resources/js/Pages/Performance/Index.jsx`
    2. Atualizar assinatura do componente (linha 75):
       ```jsx
       export default function PerformanceIndex({ ranking = [], period = '30', setor = 'consultoria', cargo = null, mes, meses = [] }) {
       ```
    3. Após o bloco de botões Consultoria|Publicações (após a div fechada na linha ~119),
       inserir (ANTES do `{/* Period / Month selector */}`):
       ```jsx
       {/* Filtro por cargo — só no setor consultoria */}
       {!isPolos && (
           <div className="flex rounded-xl border border-white/[0.08] overflow-hidden">
               {[
                   { label: 'Geral',        value: null },
                   { label: 'Analistas',    value: 'analista' },
                   { label: 'Estrategistas', value: 'estrategista' },
               ].map((opt, i, arr) => (
                   <div key={opt.value ?? 'geral'} className="flex">
                       <button
                           onClick={() => applyFilter(
                               opt.value
                                   ? { setor: 'consultoria', period, cargo: opt.value }
                                   : { setor: 'consultoria', period }
                           )}
                           className={cn(
                               'px-3 h-9 text-[13px] font-medium transition-colors',
                               cargo === opt.value
                                   ? 'bg-ecf-yellow/[0.12] text-ecf-yellow'
                                   : 'text-white/50 hover:text-white/80 hover:bg-white/[0.04]'
                           )}
                       >
                           {opt.label}
                       </button>
                       {i < arr.length - 1 && <div className="w-px bg-white/[0.08]" />}
                   </div>
               ))}
           </div>
       )}
       ```
    4. Verificar que `cn` já está importado (linha 4) — sim, está
    5. Commitar: `feat(49-01): adiciona tabs Geral/Analistas/Estrategistas em /performance`
  </action>
  <verify>
    - Inspecionar JSX gerado visualmente: 3 botões aparecem no header quando setor=consultoria
    - Botão "Analistas" ativo quando URL tem cargo=analista
    - Botão não aparece no setor Publicações (isPolos=true)
  </verify>
  <done>Botões visíveis no modo consultoria; click muda URL corretamente; modo polos não exibe botões</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 4: Build frontend + smoke check</name>
  <files>— (build)</files>
  <read_first>— nenhum arquivo adicional necessário</read_first>
  <behavior>
    Rodar `npm run build` para garantir que o JSX compila sem erros.
    Confirmar que os testes PHP continuam GREEN após a adição do frontend.
  </behavior>
  <action>
    1. `npm run build` — verificar saída sem erros
    2. `php artisan test tests/Feature/PerformanceCargoFilterTest.php` — confirmar GREEN
    3. Commitar se houver arquivos gerados pelo build que precisem de commit (normalmente não — public/build é gitignored)
  </action>
  <verify>
    `npm run build` termina sem erros; testes PHPUnit GREEN
  </verify>
  <done>Build limpo; testes GREEN; Wave 1 completa</done>
</task>

</tasks>

<verification>
1. `php artisan test tests/Feature/PerformanceCargoFilterTest.php` → todos os testes GREEN
2. `npm run build` → sem erros de compilação JSX
3. Abrir `/performance` no browser → 3 botões aparecem no header (Geral | Analistas | Estrategistas)
4. Click "Analistas" → URL muda para `/performance?setor=consultoria&cargo=analista`, ranking mostra só analistas
5. Click "Estrategistas" → URL muda para `/performance?setor=consultoria&cargo=estrategista`, ranking mostra só estrategistas
6. Click "Geral" → URL remove `cargo`, ranking mostra todos
7. Click "Publicações" (setor toggle) → botões de cargo DESAPARECEM
8. Botão ativo destaca com fundo amarelo (`ecf-yellow`)
</verification>

<success_criteria>
- [ ] Testes PHPUnit Feature passam (cargo=analista filtra, cargo=estrategista filtra, sem cargo retorna todos, cargo inválido ignorado)
- [ ] Prop `cargo` propagada no Inertia::render() de PerformanceController::index()
- [ ] 3 botões toggle renderizados no frontend apenas no modo consultoria
- [ ] Botão ativo destacado visualmente com ecf-yellow
- [ ] Click em botão navega via router.get com preserveState: true
- [ ] npm run build sem erros
- [ ] PerformanceController::indexPolos() não alterado
- [ ] PortfolioScoreService não alterado
</success_criteria>

<output>
Criar `.planning/phases/49-rankings-de-desempenho-por-fun-o-ranking-separado-de-publica/49-01-SUMMARY.md` ao final com: tarefas executadas, commits, resultado dos testes, desvios (se houver).
Próximo: Wave 2 → `49-02-PLAN.md` (rota /publicacao/desempenho + sidebar entry)
</output>
