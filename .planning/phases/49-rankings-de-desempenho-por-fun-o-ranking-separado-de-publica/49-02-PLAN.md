---
phase: 49-rankings-de-desempenho-por-fun-o-ranking-separado-de-publica
plan: "02"
type: execute
wave: 2
depends_on: ["49-01"]
autonomous: true
requirements:
  - REQ-49-02
must_haves:
  truths:
    - "VERIFICAR SCHEMA ANTES DE TUDO: 'publication_role' (sem _legacy) pode não existir (Phase 7 renomeou) — linha 125 de indexPolos() usa ->whereIn('publication_role', ...) que pode falhar"
    - "Se publication_role não existe: substituir filtro por user_setores → cargos.slug IN ('publicador', 'lider-de-publicacao', 'gestor-de-publicacao')"
    - "Ranking de publicação JÁ EXISTE como indexPolos() (linhas 117-210 do PerformanceController) — não recriar lógica"
    - "Permission a reusar: 'mlb.dashboard' — já existe, já configura os setores corretos, admin sempre vê"
    - "Trophy já importado em AppLayout.jsx linha 7 — não reimportar"
    - "Sidebar entry usa page: 'Performance' para isActive() funcionar (page renderizada é Performance/Index)"
    - "indexPolos() é método PRIVADO — deve ser tornado público ou extraído para método público novo"
  artifacts:
    - path: "app/Http/Controllers/PerformanceController.php"
      provides: "método público indexPublicacao() + método indexPolos() mantido como alias ou renomeado"
    - path: "routes/web.php"
      provides: "GET /publicacao/desempenho → PerformanceController@indexPublicacao, name: publicacao.desempenho.index"
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "entry 'Desempenho' no grupo Publicações após 'Pub · Dashboard'"
    - path: "tests/Feature/PublicacaoDesempenhoRouteTest.php"
      provides: "testes PHPUnit: 200 com permission, 403 sem, renderiza Performance/Index com setor=polos"
  key_links:
    - from: "routes/web.php GET /publicacao/desempenho"
      to: "PerformanceController::indexPublicacao()"
      via: "middleware('permission:mlb.dashboard')"
      pattern: "mesmo pattern das rotas mlb existentes"
    - from: "AppLayout.jsx NAV_TREE grupo Publicações"
      to: "publicacao.desempenho.index"
      via: "{ label: 'Desempenho', routeName: 'publicacao.desempenho.index', page: 'Performance', icon: Trophy, permission: 'mlb.dashboard' }"
      pattern: "mesmo pattern de entry do grupo Publicações; page: 'Performance' ativa isActive() quando em Performance/Index"
---

<objective>
Criar rota separada `/publicacao/desempenho` que expõe o ranking de publicação (atualmente acessível apenas como tab "Publicações" dentro de `/performance`) como destino próprio no menu lateral, dentro do dropdown "Publicações". A lógica já existe em `PerformanceController::indexPolos()` (linhas 117-210) — esta wave extrai/torna público esse método e cria a rota nova com permissão `mlb.dashboard`. Inclui verificação obrigatória do schema (`publication_role` vs coluna legada), correção do filtro de users se necessário, sidebar entry, e testes PHPUnit Feature para cobertura de acesso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-rankings-de-desempenho-por-fun-o-ranking-separado-de-publica/49-CONTEXT.md
@.planning/phases/49-rankings-de-desempenho-por-fun-o-ranking-separado-de-publica/49-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Verificar schema — coluna publication_role existe? (BLOCKER)</name>
  <files>— (verificação de schema)</files>
  <read_first>
    - §5 RESEARCH.md (linhas 253-292) — alerta sobre publication_role vs publication_role_legacy
    - §6 RESEARCH.md Pitfall #1 — indexPolos() linha 125 usa ->whereIn('publication_role', ...)
    - app/Http/Controllers/PerformanceController.php (linhas 124-127) — query atual
    - database/migrations/ — buscar migration de Phase 7 que renomeou a coluna
  </read_first>
  <behavior>
    Verificar se a coluna `publication_role` (SEM sufixo `_legacy`) existe na tabela `users`.
    Isso determina se `indexPolos()` funciona como está ou precisa de correção.

    Método de verificação (usar o que for disponível):
    - Inspecionar migrations em database/migrations/ buscando por 'publication_role' para ver se foi renomeada
    - OU verificar app/Models/User.php para ver se $fillable inclui 'publication_role' (sem _legacy)
    - OU verificar se existe coluna no schema SQLite de testes via tinker
  </behavior>
  <action>
    1. Buscar em database/migrations/ por migration que mencione 'publication_role' com renameColumn ou dropColumn
    2. Ler app/Models/User.php — verificar $fillable e $casts para presença de 'publication_role' vs 'publication_role_legacy'
    3. Verificar se User.php tem propriedades/helpers que lêem publication_role (ex: hasPublicationRole() em linhas 210-241 do §4 RESEARCH.md)
    4. Decisão:
       - Se coluna EXISTE → indexPolos() funciona; prosseguir sem mudança na query
       - Se coluna NÃO EXISTE → substituir filtro em indexPolos() linha 125 por:
         ```php
         ->whereExists(function ($q) {
             $q->from('user_setores as us')
               ->join('cargos as c', 'c.id', '=', 'us.cargo_id')
               ->whereColumn('us.user_id', 'users.id')
               ->whereIn('c.slug', ['publicador', 'lider-de-publicacao', 'gestor-de-publicacao']);
         })
         ```
       - Também substituir `->get(['id', 'name', 'publication_role', 'publication_meta'])` retirando `publication_role` da select se coluna não existe
    5. Documentar decisão no SUMMARY ao final
    6. Commitar se houve correção: `fix(49-02): corrige filtro de users em indexPolos() — usa user_setores em vez de publication_role_legacy`
  </action>
  <verify>
    - Se coluna existe: verificação passou, nada a commitar
    - Se coluna não existe e foi corrigida: `php artisan test` ainda verde, sem erros de schema
  </verify>
  <done>Schema verificado; decisão documentada; indexPolos() funcional (com ou sem correção)</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Backend — extrair indexPolos() para método público indexPublicacao()</name>
  <files>app/Http/Controllers/PerformanceController.php</files>
  <read_first>
    - app/Http/Controllers/PerformanceController.php (linhas 22-28) — index() chama indexPolos() via desvio
    - app/Http/Controllers/PerformanceController.php (linhas 117-210) — indexPolos() completo
  </read_first>
  <behavior>
    Tornar `indexPolos()` acessível como rota nova sem duplicar lógica.
    Estratégia: criar método público `indexPublicacao(Request $request)` que chama `$this->indexPolos($request)`.
    Manter `indexPolos()` como privado (para não quebrar o desvio em index() linha 26-28).
    Isso garante zero duplicação e permite routing independente.

    Alternativa (se preferir): mudar indexPolos() para público diretamente — mas isso expõe o método no router
    sem nome semântico claro. Preferir método wrapper público com nome explícito.
  </behavior>
  <action>
    1. Ler `app/Http/Controllers/PerformanceController.php`
    2. Após o método `index()` (linha 115), adicionar novo método público antes de `private function indexPolos()`:
       ```php
       /**
        * Ranking de desempenho do setor de publicação.
        * Acessível via GET /publicacao/desempenho (permission: mlb.dashboard).
        * Reutiliza a lógica de indexPolos() — mesma page, setor='polos'.
        */
       public function indexPublicacao(Request $request): \Inertia\Response
       {
           return $this->indexPolos($request);
       }
       ```
    3. Confirmar que `indexPolos()` permanece `private`
    4. Commitar: `feat(49-02): adiciona método público indexPublicacao() em PerformanceController`
  </action>
  <verify>
    `php artisan route:list | grep publicacao` — ainda sem rota (rota é adicionada na Tarefa 3)
    `php artisan test` — sem erros de compilação PHP
  </verify>
  <done>Método indexPublicacao() público criado; indexPolos() privado mantido; sem duplicação de lógica</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 3: Rota — GET /publicacao/desempenho em routes/web.php</name>
  <files>routes/web.php</files>
  <read_first>
    - routes/web.php (linhas 242-247) — rotas /performance existentes como referência de padrão
    - routes/web.php — buscar grupo de rotas mlb (middleware auth + role:admin ou permission:mlb.*) para inserir na posição correta
  </read_first>
  <behavior>
    Adicionar rota nomeada `publicacao.desempenho.index` no grupo de rotas autenticadas,
    com middleware `permission:mlb.dashboard` (reutiliza permission existente — sem nova key).
    Posição no arquivo: próxima ao grupo de rotas mlb existentes (para consistência de leitura).
  </behavior>
  <action>
    1. Ler routes/web.php para encontrar o bloco de rotas mlb
    2. Localizar onde rotas com middleware('permission:mlb.dashboard') são declaradas
    3. Adicionar após as rotas de performance (linhas 242-247) OU junto ao bloco mlb:
       ```php
       // Phase 49 Wave 2 — Ranking de desempenho do setor Publicações (rota própria no menu)
       Route::get('/publicacao/desempenho', [PerformanceController::class, 'indexPublicacao'])
           ->middleware('permission:mlb.dashboard')
           ->name('publicacao.desempenho.index');
       ```
    4. Verificar que `PerformanceController` já está no use statement do arquivo (está, pois performance.index usa-o)
    5. Commitar: `feat(49-02): adiciona rota GET /publicacao/desempenho → publicacao.desempenho.index`
  </action>
  <verify>
    `php artisan route:list --name=publicacao.desempenho.index` retorna a rota com método GET e middleware correto
  </verify>
  <done>Rota registrada; `php artisan route:list` confirma middleware permission:mlb.dashboard e name correto</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 4: Sidebar — entry "Desempenho" no grupo Publicações em AppLayout.jsx</name>
  <files>resources/js/Layouts/AppLayout.jsx</files>
  <read_first>
    - resources/js/Layouts/AppLayout.jsx (linhas 128-143) — grupo 'Publicações' completo
    - resources/js/Layouts/AppLayout.jsx (linhas 1-10) — imports (Trophy já importado linha 7)
    - §3 RESEARCH.md (linhas 128-185) — estrutura de entry e campo 'page' para isActive()
    - §7 RESEARCH.md item 5 — decisão: usar page: 'Performance' pois a rota renderiza Performance/Index
  </read_first>
  <behavior>
    Inserir nova entry no grupo 'Publicações' (NAV_TREE, linhas 128-143) logo após a entry 'Pub · Dashboard' (linha 133).
    A entry usa `page: 'Performance'` para que `isActive()` acenda quando o usuário está em `Performance/Index`
    (mesmo page component que `/performance` — comportamento intencional: o menu Publicações acende quando
    o user acessa /publicacao/desempenho, assim como o menu Performance acende para /performance).
    Permission: `mlb.dashboard` — mesmo gating do 'Pub · Dashboard'.
    Icon: `Trophy` — já importado na linha 7.
  </behavior>
  <action>
    1. Ler `resources/js/Layouts/AppLayout.jsx` (linhas 128-143)
    2. Após a linha da entry `{ label: 'Pub · Dashboard', routeName: 'mlb.dashboard', ... }` (linha 133), inserir:
       ```js
       { label: 'Desempenho', routeName: 'publicacao.desempenho.index', page: 'Performance', icon: Trophy, permission: 'mlb.dashboard' },
       ```
    3. Confirmar que `Trophy` está no import da linha 7 (está — confirmado no RESEARCH §3)
    4. Commitar: `feat(49-02): adiciona entry Desempenho no grupo Publicações do sidebar`
  </action>
  <verify>
    - `npm run build` sem erros
    - No browser: grupo "Publicações" mostra entry "Desempenho" para users com permission mlb.dashboard
    - Entry não aparece para users sem mlb.dashboard
  </verify>
  <done>Entry visível no sidebar para usuários com mlb.dashboard; Trophy icon exibido; link navega para /publicacao/desempenho</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 5: Testes PHPUnit Feature — cobertura de acesso à rota</name>
  <files>tests/Feature/PublicacaoDesempenhoRouteTest.php</files>
  <read_first>
    - tests/Feature/PerformanceCargoFilterTest.php (criado em 49-01) — padrão de setup com user admin
    - routes/web.php — confirmar middleware da rota
    - app/Models/User.php (linhas 104-109 e 210-241) — isAdmin() e hasPermission()
  </read_first>
  <behavior>
    Criar testes Feature para a nova rota `/publicacao/desempenho`:
    1. User admin → GET /publicacao/desempenho retorna 200 e renderiza component 'Performance/Index' com setor='polos'
    2. User sem permission mlb.dashboard → retorna 403
    3. User autenticado com permission mlb.dashboard → retorna 200
    Usar SQLite in-memory (RefreshDatabase). Não precisa de dados reais de publicacao — apenas verificar status HTTP e props Inertia.
  </behavior>
  <action>
    1. Criar `tests/Feature/PublicacaoDesempenhoRouteTest.php`
    2. Test 1: actingAs(admin) → GET('/publicacao/desempenho') → assertStatus(200) → assertInertia com component 'Performance/Index' e prop setor = 'polos'
    3. Test 2: actingAs(user sem permission) → GET('/publicacao/desempenho') → assertStatus(403)
    4. Test 3: actingAs(user com permission mlb.dashboard) → GET('/publicacao/desempenho') → assertStatus(200)
    5. Rodar: `php artisan test tests/Feature/PublicacaoDesempenhoRouteTest.php`
    6. Commitar: `test(49-02): testes de acesso para rota /publicacao/desempenho`
  </action>
  <verify>
    `php artisan test tests/Feature/PublicacaoDesempenhoRouteTest.php` → todos GREEN
  </verify>
  <done>Testes GREEN; cobertura: admin 200, sem-permission 403, com-permission 200</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 6: Build frontend final + smoke check geral</name>
  <files>— (build)</files>
  <read_first>— nenhum arquivo adicional</read_first>
  <behavior>
    Rodar `npm run build` para bundlar as mudanças do AppLayout.jsx.
    Rodar suite de testes completa das waves 01 e 02.
  </behavior>
  <action>
    1. `npm run build` — verificar sem erros
    2. `php artisan test tests/Feature/PerformanceCargoFilterTest.php tests/Feature/PublicacaoDesempenhoRouteTest.php`
    3. `php artisan route:list --name=publicacao.desempenho.index` — confirmar rota
  </action>
  <verify>
    Build limpo; todos os testes das waves 01 e 02 GREEN; rota listada corretamente
  </verify>
  <done>Wave 2 completa; build limpo; testes GREEN; rota e sidebar funcionais</done>
</task>

</tasks>

<verification>
1. `php artisan route:list --name=publicacao.desempenho.index` → rota GET /publicacao/desempenho com middleware permission:mlb.dashboard
2. `php artisan test tests/Feature/PublicacaoDesempenhoRouteTest.php` → todos GREEN
3. `npm run build` → sem erros
4. No browser como admin: sidebar grupo "Publicações" exibe entry "Desempenho" com ícone Trophy
5. Click em "Desempenho" navega para /publicacao/desempenho e exibe ranking de publicação
6. Como user sem permission mlb.dashboard: entry não aparece no sidebar; acesso direto retorna 403
7. indexPolos() privado — verificar que `php artisan route:list` NÃO lista `indexPolos` como rota
8. Schema verification documentado no SUMMARY: publication_role existe/não existe + ação tomada
</verification>

<success_criteria>
- [ ] Schema verificado e decisão documentada (publication_role existe ou filtro corrigido)
- [ ] Método público indexPublicacao() criado em PerformanceController, sem duplicação de lógica
- [ ] Rota GET /publicacao/desempenho registrada com name publicacao.desempenho.index e middleware permission:mlb.dashboard
- [ ] Entry "Desempenho" no grupo Publicações do AppLayout.jsx com icon Trophy e permission mlb.dashboard
- [ ] Testes Feature GREEN: 200 para admin e user com permission; 403 sem permission
- [ ] npm run build sem erros
- [ ] Testes da Wave 1 (49-01) continuam GREEN
</success_criteria>

<output>
Criar `.planning/phases/49-rankings-de-desempenho-por-fun-o-ranking-separado-de-publica/49-02-SUMMARY.md` ao final com: resultado da verificação de schema, commits realizados, resultado dos testes, desvios (se houver).
Próximo: Wave 3 → `49-03-PLAN.md` (UAT humano em produção)
</output>
