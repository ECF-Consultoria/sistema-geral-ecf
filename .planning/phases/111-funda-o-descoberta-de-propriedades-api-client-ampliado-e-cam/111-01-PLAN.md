---
phase: 111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/services.php
  - app/Console/Commands/HubspotInspectProperties.php
  - tests/Feature/Phase111HubspotConfigPropsTest.php
  - tests/Feature/Phase111InspectPropertiesTest.php
autonomous: true
requirements: [HUB-API-01, HUB-API-02]
user_setup: []

must_haves:
  truths:
    - "config('services.hubspot.props.deal') expõe as novas chaves (observacao/description/closed_won_reason/closedate/pipeline/hs_mrr/hs_arr/hs_tcv/hs_acv/hs_currency) mantendo as antigas"
    - "config('services.hubspot.props.company') expõe domain/industry/annualrevenue/city/state/country mantendo name/cnpj/email/phone"
    - "config('services.hubspot.props.contact') expõe mobilephone/jobtitle/additional_emails mantendo firstname/lastname/email/phone"
    - "php artisan hubspot:inspect-properties imprime nome interno + label + type + fieldType por objeto"
    - "O comando nunca imprime nem loga o access token"
    - "Falha (rede/403) em um objeto não crasha o comando — reporta erro e segue os demais"
  artifacts:
    - path: "config/services.php"
      provides: "Mapeamento estendido services.hubspot.props via env com defaults seguros"
      contains: "hs_mrr"
    - path: "app/Console/Commands/HubspotInspectProperties.php"
      provides: "Comando de diagnóstico Properties API hubspot:inspect-properties"
      contains: "hubspot:inspect-properties"
    - path: "tests/Feature/Phase111HubspotConfigPropsTest.php"
      provides: "Prova das chaves de config novas + preservação das antigas"
    - path: "tests/Feature/Phase111InspectPropertiesTest.php"
      provides: "Prova de saída do comando, resiliência e não-vazamento de token (Http::fake)"
  key_links:
    - from: "app/Console/Commands/HubspotInspectProperties.php"
      to: "https://api.hubapi.com/crm/v3/properties/{objectType}"
      via: "Http::withToken GET"
      pattern: "crm/v3/properties"
    - from: "app/Console/Commands/HubspotInspectProperties.php"
      to: "config('services.hubspot.access_token')"
      via: "reuso de credencial existente"
      pattern: "services\\.hubspot"
---

<objective>
Ampliar `config/services.php` (bloco `services.hubspot.props`) com as novas propriedades de deal/company/contact mapeáveis por env, e criar o comando Artisan `hubspot:inspect-properties` que valida via Properties API os nomes internos reais da conta ECF — sem jamais vazar o access token.

Purpose: Estabelecer a camada de configuração e o diagnóstico que a milestone v20.0 usa antes de depender de qualquer custom property (hs_mrr etc.). Aqui NADA muda de comportamento no webhook — só se ADICIONAM chaves de config e um comando de leitura.
Output: `config/services.php` estendido + `app/Console/Commands/HubspotInspectProperties.php` + 2 suites Feature (config + comando).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md

# Fonte de verdade (props exatas + assinatura do comando)
@prompt-claude-otimizacao-comercial-hubspot.md

# Código a ESTENDER (não substituir)
@config/services.php

<interfaces>
<!-- Estado atual de config/services.php → services.hubspot (a ESTENDER, preservar chaves) -->
services.hubspot:
  client_secret, access_token, stage_fechado_ganho_id
  props.deal:    nicho, dor, vende_ml, faturamento_mensal, servico   (mapeados por env)
  props.company: name, cnpj, email, phone
  props.contact: firstname, lastname, email, phone

<!-- Consumidor atual (NÃO alterar) — lê o array inteiro por chave nomeada -->
app/Http/Controllers/Api/HubspotWebhookController.php:
  config('services.hubspot.props.deal')     // usa $propsDeal['servico'], etc.
  config('services.hubspot.props.company')
  config('services.hubspot.props.contact')
<!-- Adicionar chaves NÃO quebra esse consumidor (acesso por chave nomeada, não iteração estrita). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender services.hubspot.props com deal/company/contact ampliados</name>
  <files>config/services.php, tests/Feature/Phase111HubspotConfigPropsTest.php</files>
  <read_first>
    - config/services.php (bloco services.hubspot atual — linhas ~118-148)
    - prompt-claude-otimizacao-comercial-hubspot.md (§Fase 1, snippet PHP das props)
    - .planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md (§Config props HUB-API-01)
  </read_first>
  <behavior>
    - Test: config('services.hubspot.props.deal') contém as 5 chaves antigas (nicho/dor/vende_ml/faturamento_mensal/servico) E as 10 novas (observacao/description/closed_won_reason/closedate/pipeline/hs_mrr/hs_arr/hs_tcv/hs_acv/hs_currency)
    - Test: config('services.hubspot.props.company') contém name/cnpj/email/phone + domain/industry/annualrevenue/city/state/country
    - Test: config('services.hubspot.props.contact') contém firstname/lastname/email/phone + mobilephone/jobtitle/additional_emails
    - Test: default de props.contact.additional_emails == 'hs_additional_emails'
    - Test: default de props.deal.hs_mrr == 'hs_mrr' e props.company.domain == 'domain'
  </behavior>
  <action>
    Em `config/services.php`, dentro de `services.hubspot.props`, ADICIONAR (nunca remover/substituir as chaves atuais) as sub-chaves conforme §Fase 1 do prompt canônico, cada valor via `env(...)` com default = nome interno padrão HubSpot:
    - `props.deal`: `observacao` (env HUBSPOT_PROP_DEAL_OBSERVACAO default 'observacao'), `description`, `closed_won_reason`, `closedate`, `pipeline`, `hs_mrr` (env HUBSPOT_PROP_DEAL_MRR), `hs_arr`, `hs_tcv`, `hs_acv`, `hs_currency` — usar os nomes de env exatos do prompt (HUBSPOT_PROP_DEAL_ARR/TCV/ACV/CURRENCY, HUBSPOT_PROP_DEAL_DESCRIPTION/CLOSED_WON_REASON/CLOSEDATE/PIPELINE).
    - `props.company`: `domain`, `industry`, `annualrevenue` (env HUBSPOT_PROP_COMPANY_ANNUAL_REVENUE default 'annualrevenue'), `city`, `state`, `country`.
    - `props.contact`: `mobilephone`, `jobtitle`, `additional_emails` (env HUBSPOT_PROP_CONTACT_ADDITIONAL_EMAILS default 'hs_additional_emails').
    Comentário pt-BR curto acima do bloco novo marcando Phase 111 / HUB-API-01 e a regra "propriedade ausente na conta = null, nunca quebra". NÃO tocar em client_secret/access_token/stage_fechado_ganho_id.
    Criar a suite `Phase111HubspotConfigPropsTest` (sem RefreshDatabase — só lê config) provando o behavior acima.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase111HubspotConfigPropsTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Phase111HubspotConfigPropsTest` sai com código 0.
    - config('services.hubspot.props.deal.hs_mrr') !== null e as 5 chaves antigas continuam presentes.
    - `grep -v '^\s*//' config/services.php | grep -c "hs_mrr\|annualrevenue\|additional_emails"` >= 3.
    - Regressão: `php artisan test --filter=Phase34HubspotWebhookTest` e `--filter=Phase37WebhookLineItemsTest` continuam verdes (consumidor de props intacto).
  </acceptance_criteria>
  <done>As 3 sub-chaves (deal/company/contact) expõem as novas props via env com default, preservando as antigas, e o consumidor legado do webhook segue verde.</done>
</task>

<task type="auto">
  <name>Task 2: Comando hubspot:inspect-properties (Properties API, sem vazar token)</name>
  <files>app/Console/Commands/HubspotInspectProperties.php, tests/Feature/Phase111InspectPropertiesTest.php</files>
  <read_first>
    - app/Services/HubspotApiClient.php (padrão Http::withToken + base https://api.hubapi.com + reuso de config('services.hubspot.access_token'))
    - prompt-claude-otimizacao-comercial-hubspot.md (§Fase 1: assinatura --objects=deals,companies,contacts,line_items; imprime nome interno/label/type/fieldType; nunca loga token)
    - .planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md (§Comando inspect-properties HUB-API-02)
    - tests/Feature/Phase37LineItemsFetchTest.php (padrão de Http::fake com URL patterns para HubSpot)
  </read_first>
  <action>
    Criar comando Artisan `HubspotInspectProperties` com signature `hubspot:inspect-properties {--objects=deals,companies,contacts,line_items}` (aceita CSV; default os 4 objetos). Para cada objeto, GET `https://api.hubapi.com/crm/v3/properties/{objectType}` com `Http::withToken(config('services.hubspot.access_token'))`. Iterar `results` e imprimir por propriedade: `name` (nome interno), `label`, `type`, `fieldType` (linha tabular via `$this->line`/`$this->table`). Cabeçalho por objeto com o nome do objeto.
    Resiliência: se a chamada de um objeto falhar (não-2xx ou exceção de rede), reportar erro amigável em pt-BR via `$this->warn`/`$this->error` (ex.: "Falha ao consultar propriedades de {obj}: HTTP {status}") e CONTINUAR os demais objetos — nunca lançar exceção que aborte o comando. Exit code 0 mesmo com falhas parciais.
    Segurança (T-111-01): NUNCA imprimir nem logar o access token nem a string 'Bearer'; a mensagem de erro contém só objeto + status HTTP. Comentários pt-BR.
    Criar suite `Phase111InspectPropertiesTest` com `Http::fake` mockando `/crm/v3/properties/deals` (200 com results contendo name/label/type/fieldType) e `/crm/v3/properties/companies` (403). Nenhuma chamada real.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase111InspectPropertiesTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Phase111InspectPropertiesTest` sai com código 0.
    - Teste assertivo: saída de `--objects=deals` contém o nome interno, label, type e fieldType do fake.
    - Teste de resiliência: com `--objects=deals,companies` e companies=403, o comando termina exit 0 e ainda imprime as props de deals.
    - Teste de segurança: a saída/`Artisan::output()` NÃO contém o token de teste nem 'Bearer'.
    - `php artisan hubspot:inspect-properties --help` lista a opção `--objects`.
  </acceptance_criteria>
  <done>Comando lista name/label/type/fieldType por objeto via Properties API, resiliente a falha por objeto, sem vazar token, coberto por teste com Http::fake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ECF app → HubSpot Properties API | Requisição autenticada por Bearer (Private App token) sai do app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Information Disclosure | HubspotInspectProperties (stdout/log) | mitigate | Nunca imprimir/logar access_token nem 'Bearer'; erro só com objeto+status. Teste assertivo cobre não-vazamento. |
| T-111-02 | Denial of Service | Properties API indisponível/403 | mitigate | Falha por objeto é capturada e reportada; comando segue os demais e sai 0 (nunca aborta). |
</threat_model>

<verification>
- `php artisan test --filter=Phase111HubspotConfigPropsTest` e `--filter=Phase111InspectPropertiesTest` verdes.
- Regressão HubSpot: `php artisan test --filter=Phase34HubspotWebhookTest --filter=Phase37WebhookLineItemsTest` (ou rodar as suites Phase34/35/37 HubSpot) verdes.
- `grep` confirma novas chaves em config/services.php e signature do comando.
</verification>

<success_criteria>
- HUB-API-01: `config('services.hubspot.props')` expõe deal/company/contact ampliados por env com default, sem quebrar as chaves atuais nem o consumidor do webhook.
- HUB-API-02: `hubspot:inspect-properties --objects=...` imprime nome interno/label/type/fieldType por objeto, resiliente e sem vazar token.
</success_criteria>

<output>
Criar `.planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-01-SUMMARY.md` ao concluir.
</output>
