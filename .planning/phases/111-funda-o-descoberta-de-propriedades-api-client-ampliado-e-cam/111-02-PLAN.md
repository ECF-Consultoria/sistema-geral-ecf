---
phase: 111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/HubspotApiClient.php
  - tests/Feature/Phase37LineItemsFetchTest.php
  - tests/Feature/Phase111HubspotApiClientTest.php
autonomous: true
requirements: [HUB-API-03]
user_setup: []

must_haves:
  truths:
    - "fetchDealLineItems retorna, além dos campos atuais, description/amount/hs_sku/hs_recurring_billing_period/hs_recurring_billing_start_date/hs_recurring_billing_end_date/hs_line_item_currency_code/hs_mrr/hs_arr/hs_tcv/hs_acv"
    - "Os valores e a resiliência dos campos atuais (name/price/quantity/hs_product_id/recurringbillingfrequency) permanecem idênticos"
    - "Existem fetchAssociatedCompanyIds/fetchAssociatedContactIds/fetchAssociations/fetchCompanies/fetchContacts coexistindo com os métodos atuais sem quebrá-los"
    - "A base permanece https://api.hubapi.com/crm/v3/objects/... (não migra para /crm/objects/2026-03)"
    - "Nenhum teste faz chamada real ao HubSpot (Http::fake) e nenhum log vaza o Bearer token"
  artifacts:
    - path: "app/Services/HubspotApiClient.php"
      provides: "Client ampliado: line items com props completas + novos métodos de associação/batch"
      contains: "fetchAssociatedCompanyIds"
    - path: "tests/Feature/Phase111HubspotApiClientTest.php"
      provides: "Cobertura dos 5 novos métodos com Http::fake"
    - path: "tests/Feature/Phase37LineItemsFetchTest.php"
      provides: "Contrato de fetchDealLineItems atualizado para o shape ampliado (demais cenários intactos)"
  key_links:
    - from: "app/Services/HubspotApiClient.php::fetchDealLineItems"
      to: "line_items properties ampliadas"
      via: "string $properties com os 16 nomes"
      pattern: "hs_recurring_billing_period"
    - from: "app/Services/HubspotApiClient.php::fetchCompanies"
      to: "/crm/v3/objects/companies/batch/read ou N GETs"
      via: "padrão já usado no client"
      pattern: "objects/companies"
---

<objective>
Ampliar `HubspotApiClient` com (a) o conjunto completo de propriedades em `fetchDealLineItems` e (b) novos métodos de associações/batch (`fetchAssociatedCompanyIds`, `fetchAssociatedContactIds`, `fetchAssociations`, `fetchCompanies`, `fetchContacts`), sem quebrar os métodos atuais nem o comportamento observável do webhook. Mantém a base v3 (decisão travada: NÃO migrar para `/crm/objects/2026-03`).

Purpose: Dar ao handoff da Fase 112+ acesso a MRR/ARR/amount/período de billing e a todos os contatos/empresas associados — mas só a capacidade; o consumidor atual (webhook) segue lendo os mesmos campos por chave, sem mudança de comportamento.
Output: `HubspotApiClient` ampliado + contrato do `Phase37LineItemsFetchTest` atualizado para o shape novo + suite `Phase111HubspotApiClientTest` para os métodos novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md

# Fonte de verdade (lista de props mínimas + nomes de método)
@prompt-claude-otimizacao-comercial-hubspot.md

# Código a ESTENDER + teste de contrato a ampliar
@app/Services/HubspotApiClient.php
@tests/Feature/Phase37LineItemsFetchTest.php

<interfaces>
<!-- Métodos ATUAIS de HubspotApiClient (NÃO quebrar assinatura/comportamento) -->
fetchDeal(string $id, array $properties): array
fetchAssociatedCompanyId(string $dealId): ?string      // primeiro company; 404 => null
fetchCompany(string $id, array $properties): array
fetchAssociatedContactId(string $dealId): ?string      // primeiro contato; 404 => null
fetchContact(string $id, array $properties): array
fetchDealLineItems(string $dealId): array              // hoje: 6 chaves por item

<!-- Shape ATUAL do item de fetchDealLineItems (será AMPLIADO) -->
{ id, name, price:?float, quantity:?int, hs_product_id:?string, recurringbillingfrequency:?string }

<!-- Consumidor (NÃO alterar): HubspotWebhookController lê $item['price'] por chave -->
<!-- Adicionar chaves ao item NÃO quebra o controller (acesso por chave). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ampliar fetchDealLineItems para o conjunto completo de props</name>
  <files>app/Services/HubspotApiClient.php, tests/Feature/Phase37LineItemsFetchTest.php</files>
  <read_first>
    - app/Services/HubspotApiClient.php (método fetchDealLineItems — linhas ~158-241, string $properties e o mapeamento de saída)
    - tests/Feature/Phase37LineItemsFetchTest.php (assert de array_keys em test_deal_com_2_line_items — linhas ~81-84; demais cenários T2-T9)
    - prompt-claude-otimizacao-comercial-hubspot.md (§Fase 3, array $properties de 16 nomes)
    - .planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md (§HubspotApiClient HUB-API-03)
  </read_first>
  <behavior>
    - Test: item retornado inclui as chaves novas description/amount/hs_sku/hs_recurring_billing_period/hs_recurring_billing_start_date/hs_recurring_billing_end_date/hs_line_item_currency_code/hs_mrr/hs_arr/hs_tcv/hs_acv além das 6 atuais
    - Test: amount/hs_mrr/hs_arr/hs_tcv/hs_acv viram float quando numéricos, null quando ausentes
    - Test: description/hs_sku/hs_recurring_billing_period/datas/currency_code passam como string ou null (sem cast numérico)
    - Test: price continua float, quantity continua int, recurringbillingfrequency continua string/null (valores idênticos ao contrato atual)
    - Test (regressão preservada): T2 (results vazio→[]), T3 (404→[]), T4 (500→[]), T5 (item 500 pula+warning), T9 (token não vaza) permanecem verdes sem edição de lógica
  </behavior>
  <action>
    Em `fetchDealLineItems`, trocar a string `$properties` (hoje `name,price,quantity,hs_product_id,recurringbillingfrequency`) pela lista completa da §Fase 3 do prompt: `name,description,price,amount,quantity,hs_product_id,hs_sku,recurringbillingfrequency,hs_recurring_billing_period,hs_recurring_billing_start_date,hs_recurring_billing_end_date,hs_line_item_currency_code,hs_mrr,hs_arr,hs_tcv,hs_acv`. Ampliar o array `$out[]` de saída incluindo essas chaves na mesma ordem: campos monetários (`amount`,`hs_mrr`,`hs_arr`,`hs_tcv`,`hs_acv`) com o mesmo padrão de `price` (cast `(float)` só quando `is_numeric`, senão `null`); campos textuais (`description`,`hs_sku`,`hs_recurring_billing_period`,`hs_recurring_billing_start_date`,`hs_recurring_billing_end_date`,`hs_line_item_currency_code`) como `?? null` (passthrough string). Preservar `id/name/price/quantity/hs_product_id/recurringbillingfrequency` exatamente como hoje. Manter os logs `info`/`warning` existentes intactos (não vazam token). Atualizar o PHPDoc do @return.
    Em `Phase37LineItemsFetchTest`: atualizar SOMENTE a asserção `array_keys($out[0])` de `test_deal_com_2_line_items_retorna_lista_completa` para a nova ordem completa de chaves, e enriquecer os `Http::response` do fake + adicionar asserções de valor para as chaves novas (ex.: amount float, hs_mrr float, description string). NÃO alterar os demais métodos de teste (T2..T9) — eles são o guard de regressão de comportamento.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase37LineItemsFetchTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Phase37LineItemsFetchTest` sai com código 0 (todos os 9 cenários verdes, incl. o de array_keys atualizado).
    - `array_keys($out[0])` inclui as 16+1(id) chaves; `grep -c "hs_recurring_billing_period" app/Services/HubspotApiClient.php` >= 1.
    - Regressão: `php artisan test --filter=Phase37WebhookLineItemsTest` continua verde (controller consome $item['price'] sem quebrar).
    - Base inalterada: `grep -c "crm/objects/2026-03" app/Services/HubspotApiClient.php` == 0.
  </acceptance_criteria>
  <done>fetchDealLineItems retorna o shape ampliado com casts corretos; contrato do Phase37LineItemsFetchTest atualizado só na asserção de shape; comportamento dos campos atuais e do webhook preservado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Novos métodos de associações e batch read</name>
  <files>app/Services/HubspotApiClient.php, tests/Feature/Phase111HubspotApiClientTest.php</files>
  <read_first>
    - app/Services/HubspotApiClient.php (padrões atuais: fetchAssociatedCompanyId retorna results.0.toObjectId; fetchCompany/fetchContact GET por id com properties; resiliência 404→null/[])
    - prompt-claude-otimizacao-comercial-hubspot.md (§Fase 3: assinaturas fetchAssociatedCompanyIds/fetchAssociatedContactIds/fetchAssociations/fetchContacts/fetchCompanies)
    - .planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md (§HubspotApiClient HUB-API-03 + Claude's Discretion sobre batch read vs N GETs)
    - tests/Feature/Phase37LineItemsFetchTest.php (padrão Http::fake + token 'fake-token' + URL patterns)
  </read_first>
  <behavior>
    - Test: fetchAssociatedCompanyIds($dealId) retorna array de TODOS os toObjectId (não só o primeiro); 404/500 → []
    - Test: fetchAssociatedContactIds($dealId) idem para contacts; 404/500 → []
    - Test: fetchAssociations($fromObject,$fromId,$toObject) retorna a lista de results (ou [] em falha), montando a URL /crm/v3/objects/{fromObject}/{fromId}/associations/{toObject}
    - Test: fetchCompanies([ids], [properties]) retorna array indexado por id (ou lista) com as properties pedidas; ids vazio → []
    - Test: fetchContacts([ids], [properties]) idem
    - Test: nenhuma chamada real (Http::fake); métodos atuais (fetchAssociatedCompanyId singular) continuam existindo e verdes
  </behavior>
  <action>
    Adicionar ao `HubspotApiClient` (mantendo a base `self::BASE . "/crm/v3/objects/..."` e o padrão `Http::withToken($this->token)`):
    - `fetchAssociatedCompanyIds(string $dealId): array` — GET associations/companies, coletar TODOS `results.*.toObjectId` como strings; não-2xx → [].
    - `fetchAssociatedContactIds(string $dealId): array` — idem para associations/contacts.
    - `fetchAssociations(string $fromObject, string $fromId, string $toObject): array` — GET `/crm/v3/objects/{fromObject}/{fromId}/associations/{toObject}`, retorna `results` (array) ou [] em falha. Reusar internamente nos dois métodos acima se conveniente.
    - `fetchCompanies(array $ids, array $properties): array` — buscar múltiplas companies. Escolher o padrão já usado no client (preferir N GETs a `fetchCompany` OU batch read `/crm/v3/objects/companies/batch/read` POST — decisão do executor conforme Discretion). Retornar mapa id→properties (ou lista de arrays com id+properties). `$ids` vazio → [].
    - `fetchContacts(array $ids, array $properties): array` — idem para contacts.
    Resiliência consistente com os métodos atuais (falha não propaga exceção nesses métodos de associação/batch; loga warning no canal `ecf-webhooks` sem token, se logar). Comentários pt-BR + PHPDoc de @return. NÃO alterar os métodos singulares existentes.
    Criar suite `Phase111HubspotApiClientTest` (Http::fake, token 'fake-token') cobrindo o behavior acima.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase111HubspotApiClientTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Phase111HubspotApiClientTest` sai com código 0.
    - `grep -c "function fetchAssociatedCompanyIds\|function fetchAssociatedContactIds\|function fetchAssociations\|function fetchCompanies\|function fetchContacts" app/Services/HubspotApiClient.php` == 5.
    - Métodos singulares preservados: `grep -c "function fetchAssociatedCompanyId(" app/Services/HubspotApiClient.php` == 1 (sem 's').
    - Regressão: `php artisan test --filter=Phase34HubspotWebhookTest --filter=Phase35HubspotV2Test` verdes.
  </acceptance_criteria>
  <done>Os 5 novos métodos existem, cobertos por teste com Http::fake, coexistindo com os singulares atuais; base v3 preservada; nenhum token em log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ECF app → HubSpot CRM v3 API | GET/POST autenticados por Bearer para deals/line_items/companies/contacts/associations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-03 | Information Disclosure | Logs de HubspotApiClient | mitigate | Logs (warning/info) só com deal_id/line_item_id/status; nunca token nem 'Bearer'. Cenário T9 do Phase37 e teste novo cobrem. |
| T-111-04 | Tampering/Compat | Migração de base v3→2026-03 | accept | Decisão travada: manter /crm/v3/objects. Não migrar nesta fase (risco de contrato). Documentado. |
</threat_model>

<verification>
- `php artisan test --filter=Phase37LineItemsFetchTest` e `--filter=Phase111HubspotApiClientTest` verdes.
- Regressão HubSpot: Phase34/Phase35/Phase37WebhookLineItems verdes.
- `grep` confirma 5 métodos novos + base v3 intacta + `hs_recurring_billing_period` presente.
</verification>

<success_criteria>
- HUB-API-03: fetchDealLineItems retorna as props mínimas do prompt e os 5 novos métodos coexistem com os atuais sem quebrá-los; base v3 mantida; tudo sob Http::fake sem vazar token.
</success_criteria>

<output>
Criar `.planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-02-SUMMARY.md` ao concluir.
</output>
