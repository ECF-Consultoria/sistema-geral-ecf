---
phase: 111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_24_111001_add_hubspot_fields_to_companies_table.php
  - database/migrations/2026_07_24_111002_add_hubspot_fields_to_contratos_servico_table.php
  - app/Models/Company.php
  - app/Models/ContratoServico.php
  - tests/Feature/Phase111HubspotSchemaTest.php
autonomous: true
requirements: [HUB-SCHEMA-01, HUB-SCHEMA-02]
user_setup: []

must_haves:
  truths:
    - "Após migrate, companies tem hubspot_deal_id/hubspot_company_id/hubspot_contact_id (indexadas), nome_contato, cargo_contato, hubspot_domain, hubspot_observacao, hubspot_snapshot(json), todas nullable"
    - "Após migrate, contratos_servico tem hubspot_line_item_id (index), hubspot_product_id, hubspot_billing_frequency, hubspot_billing_period, hubspot_currency, hubspot_valor_original(decimal 12,2), hubspot_valor_original_tipo, hubspot_valor_normalizado_mensal(decimal 12,2), hubspot_valor_confidence, hubspot_valor_warning, hubspot_snapshot(json), todas nullable"
    - "As migrations são defensivas (Schema::hasColumn) e reversíveis (down remove só as colunas adicionadas)"
    - "Company e ContratoServico têm as novas colunas em $fillable e casts json/decimal apropriados"
    - "As colunas nascem sem uso: nenhum comportamento do weblook/legado muda"
    - "Funciona em SQLite (ambiente de testes) e MySQL (produção)"
  artifacts:
    - path: "database/migrations/2026_07_24_111001_add_hubspot_fields_to_companies_table.php"
      provides: "8 colunas de origem HubSpot em companies (defensiva + rollback)"
      contains: "hubspot_snapshot"
    - path: "database/migrations/2026_07_24_111002_add_hubspot_fields_to_contratos_servico_table.php"
      provides: "11 colunas de origem/valor HubSpot em contratos_servico (defensiva + rollback)"
      contains: "hubspot_valor_normalizado_mensal"
    - path: "app/Models/Company.php"
      provides: "$fillable + cast hubspot_snapshot=array"
      contains: "hubspot_deal_id"
    - path: "app/Models/ContratoServico.php"
      provides: "$fillable + casts decimal/json das novas colunas"
      contains: "hubspot_valor_normalizado_mensal"
    - path: "tests/Feature/Phase111HubspotSchemaTest.php"
      provides: "Prova de colunas + fillable + casts pós-migrate"
  key_links:
    - from: "app/Models/Company.php"
      to: "companies.hubspot_snapshot"
      via: "cast array + fillable"
      pattern: "hubspot_snapshot"
    - from: "app/Models/ContratoServico.php"
      to: "contratos_servico.hubspot_valor_normalizado_mensal"
      via: "cast decimal:2 + fillable"
      pattern: "hubspot_valor_normalizado_mensal"
---

<objective>
Criar as colunas estruturadas de origem HubSpot em `companies` (8 colunas) e `contratos_servico` (11 colunas) via migrations defensivas e reversíveis, e refletir as colunas em `$fillable`/`casts` de `Company` e `ContratoServico`. As colunas nascem nullable e NÃO são usadas ainda — são a base de auditoria/proveniência que a Fase 112+ vai popular. Zero mudança de comportamento no webhook/legado.

Purpose: Persistir de forma estruturada os IDs HubSpot, contato principal, observação, valor original×normalizado e proveniência — hoje espalhados em `notes`/`observacoes` ou inexistentes. Aqui só se cria o schema; o preenchimento vem depois.
Output: 2 migrations + Company/ContratoServico atualizados + suite `Phase111HubspotSchemaTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md

# Fonte de verdade (lista exata de colunas + tipos)
@prompt-claude-otimizacao-comercial-hubspot.md

# Padrão de migration defensiva do projeto
@database/migrations/2026_06_12_300001_add_close_fields_to_companies_table.php

# Models a ESTENDER
@app/Models/Company.php
@app/Models/ContratoServico.php

<interfaces>
<!-- Company: $fillable/$casts atuais já contêm json (marketplaces_extras=>array) e decimal (faturamento_mensal=>decimal:2) — precedente cross-driver (SQLite+MySQL) -->
<!-- ContratoServico: $fillable = [company_id, servico_id, valor_contratado, data_contratacao, data_vencimento, ativo, observacoes]; casts valor_contratado=>decimal:2 -->
<!-- Precedente defensivo: cada add-column dentro de if(! Schema::hasColumn(...)); down() dropa só as colunas próprias; ordem via ->after(...) -->
</interfaces>
</context>

<tasks>

<!-- Ordem TDD (RED→GREEN): Task 1 cria a suite que FALHA (colunas ainda não existem);
     Tasks 2 e 3 criam as migrations + models que fazem a suite passar. Isso evita o
     falso-verde de `php artisan test --filter=<classe-inexistente>` (que imprime
     "No tests found." e retorna exit 0). -->

<task type="auto" tdd="true">
  <name>Task 1: (RED) Suite Phase111HubspotSchemaTest — schema + casts + zero-impacto no legado</name>
  <files>tests/Feature/Phase111HubspotSchemaTest.php</files>
  <read_first>
    - tests/Feature/Phase37WebhookLineItemsTest.php (padrão de teste do webhook legado — para confirmar zero-impacto)
    - app/Models/Company.php ($fillable ~29-44, $casts ~46-62 — estado ATUAL, ainda sem colunas HubSpot)
    - app/Models/ContratoServico.php ($fillable ~23-31, $casts ~33-38 — estado ATUAL)
    - prompt-claude-otimizacao-comercial-hubspot.md (§Fase 4 → listas exatas de colunas companies/contratos_servico)
    - .planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md (Success Criteria 4 e 5)
  </read_first>
  <behavior>
    - test_companies_tem_colunas_hubspot: Schema::hasColumn('companies', X) true para as 8 colunas (hubspot_deal_id/hubspot_company_id/hubspot_contact_id/nome_contato/cargo_contato/hubspot_domain/hubspot_observacao/hubspot_snapshot); cria Company setando hubspot_deal_id + hubspot_snapshot(array), recarrega e prova persistência + cast array
    - test_contratos_tem_colunas_hubspot: Schema::hasColumn('contratos_servico', X) true para as 11 colunas; cria ContratoServico setando hubspot_valor_original/hubspot_valor_normalizado_mensal/hubspot_snapshot e prova casts decimal:2/array
    - test_colunas_nascem_nullable_e_sem_uso: cria Company e ContratoServico SEM nenhum campo hubspot e confirma que salvam (colunas nullable) — prova zero-impacto no fluxo legado
    - Estado RED esperado: sem as migrations das Tasks 2/3, os 3 métodos FALHAM (colunas não existem) — NÃO "No tests found"
  </behavior>
  <action>
    Criar `Phase111HubspotSchemaTest` (namespace Tests\Feature, `use RefreshDatabase`, roda em SQLite) com exatamente os 3 métodos do bloco behavior. Nomear os métodos de modo que `--filter=Phase111HubspotSchemaTest::test_companies` e `::test_contratos` casem por prefixo (ex.: `test_companies_tem_colunas_hubspot`, `test_contratos_tem_colunas_hubspot`). Usar as 8 e 11 colunas EXATAS do prompt canônico §Fase 4. Comentário pt-BR de cabeçalho marcando Phase 111 / HUB-SCHEMA-01/02 e a estratégia TDD (RED antes das migrations).
    IMPORTANTE: este é o passo RED — ao rodar agora (antes das Tasks 2/3) a suite DEVE falhar com erro de coluna/tabela inexistente, provando que os testes existem e são executados (guard contra falso-verde de classe inexistente).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase111HubspotSchemaTest 2>&1 | grep -Eq "FAIL|Tests:.*failed|error" &amp;&amp; echo RED-OK || (echo "ESPERADO FALHAR (RED) mas nao falhou — verificar se a classe/metodos existem" &amp;&amp; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - A suite EXISTE e é executada: `php artisan test --filter=Phase111HubspotSchemaTest` NÃO imprime "No tests found." (guard anti-falso-verde).
    - No estado RED (antes das migrations) a suite FALHA nos 3 métodos por coluna inexistente — não passa nem reporta zero testes.
    - `grep -c "function test_" tests/Feature/Phase111HubspotSchemaTest.php` == 3.
    - `grep -c "RefreshDatabase" tests/Feature/Phase111HubspotSchemaTest.php` >= 1.
  </acceptance_criteria>
  <done>Suite de contrato de schema criada com 3 métodos executáveis; falha no estado RED (colunas ainda não existem), garantindo que as Tasks 2/3 têm um verde real a atingir.</done>
</task>

<task type="auto">
  <name>Task 2: (GREEN) Migration companies (8 colunas HubSpot) + Company model</name>
  <files>database/migrations/2026_07_24_111001_add_hubspot_fields_to_companies_table.php, app/Models/Company.php</files>
  <read_first>
    - database/migrations/2026_06_12_300001_add_close_fields_to_companies_table.php (padrão defensivo up/down + ->after)
    - app/Models/Company.php ($fillable linhas ~29-44, $casts ~46-62)
    - tests/Feature/Phase111HubspotSchemaTest.php (contrato da Task 1 — colunas exatas exigidas)
    - prompt-claude-otimizacao-comercial-hubspot.md (§Fase 4 → Companies: lista das 8 colunas)
    - .planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md (§Migrations companies HUB-SCHEMA-01)
  </read_first>
  <action>
    Criar migration defensiva `add_hubspot_fields_to_companies_table` (timestamp ~2026_07_24_111001, ajuste se colidir). No `up()`, para cada coluna dentro de `if (! Schema::hasColumn('companies', ...))`: `hubspot_deal_id` (string nullable + `->index()`), `hubspot_company_id` (string nullable + index), `hubspot_contact_id` (string nullable + index), `nome_contato` (string nullable), `cargo_contato` (string nullable), `hubspot_domain` (string nullable), `hubspot_observacao` (`text` nullable), `hubspot_snapshot` (`json` nullable). Todas nullable, sem default. `down()` defensivo dropando só essas 8 colunas (dropar índices antes se necessário via `Schema::hasColumn`). Comentário pt-BR de cabeçalho marcando Phase 111 / HUB-SCHEMA-01 e "colunas nascem sem uso (base de auditoria)".
    Em `Company.php`: adicionar as 8 colunas ao `$fillable` (comentário pt-BR "Phase 111 — origem HubSpot (HUB-SCHEMA-01)") e cast `hubspot_snapshot => 'array'` em `$casts`. Não remover nada existente.
    Cross-driver: `json` + `string`/`text` funcionam em SQLite e MySQL (precedente: marketplaces_extras=>array). Sem branch SQLite necessária aqui (não há enum/CHECK).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase111HubspotSchemaTest::test_companies</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Phase111HubspotSchemaTest::test_companies` sai com código 0 (o método existe desde a Task 1 e agora passa — não é "No tests found").
    - Após `migrate`, `Schema::hasColumn('companies', X)` true para as 8 colunas.
    - `(new Company)->getFillable()` contém as 8 novas colunas; `hubspot_snapshot` casta para array.
    - `grep -c "Schema::hasColumn('companies'" database/migrations/2026_07_24_111001_*.php` >= 8 (defensivo por coluna).
  </acceptance_criteria>
  <done>companies ganha as 8 colunas HubSpot nullable/indexadas via migration defensiva reversível; Company reflete fillable + cast json; o método test_companies da suite passa (GREEN real).</done>
</task>

<task type="auto">
  <name>Task 3: (GREEN) Migration contratos_servico (11 colunas HubSpot) + ContratoServico model</name>
  <files>database/migrations/2026_07_24_111002_add_hubspot_fields_to_contratos_servico_table.php, app/Models/ContratoServico.php</files>
  <read_first>
    - database/migrations/2026_06_12_300001_add_close_fields_to_companies_table.php (padrão defensivo up/down)
    - app/Models/ContratoServico.php ($fillable linhas ~23-31, $casts ~33-38)
    - tests/Feature/Phase111HubspotSchemaTest.php (contrato da Task 1 — colunas exatas exigidas + método test_colunas_nascem_nullable_e_sem_uso)
    - prompt-claude-otimizacao-comercial-hubspot.md (§Fase 4 → ContratosServico: lista das 11 colunas + descrição dos tipos de valor)
    - .planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-CONTEXT.md (§Migrations contratos_servico HUB-SCHEMA-02)
  </read_first>
  <action>
    Criar migration defensiva `add_hubspot_fields_to_contratos_servico_table` (timestamp ~2026_07_24_111002). No `up()`, cada coluna sob `if (! Schema::hasColumn('contratos_servico', ...))`: `hubspot_line_item_id` (string nullable + `->index()`), `hubspot_product_id` (string nullable), `hubspot_billing_frequency` (string nullable), `hubspot_billing_period` (string nullable), `hubspot_currency` (string nullable), `hubspot_valor_original` (`decimal(12,2)` nullable), `hubspot_valor_original_tipo` (string nullable — ex.: unit_price/net_price/mrr/arr/deal_amount, comentar em pt-BR), `hubspot_valor_normalizado_mensal` (`decimal(12,2)` nullable), `hubspot_valor_confidence` (string nullable — ex.: high/medium/low), `hubspot_valor_warning` (`text` nullable), `hubspot_snapshot` (`json` nullable). Todas nullable. `down()` defensivo removendo só essas 11 colunas. Cabeçalho pt-BR Phase 111 / HUB-SCHEMA-02 + "colunas de proveniência, sem uso ainda; valor_contratado segue como valor operacional (não mexer)".
    Em `ContratoServico.php`: adicionar as 11 colunas ao `$fillable`; casts `hubspot_valor_original => 'decimal:2'`, `hubspot_valor_normalizado_mensal => 'decimal:2'`, `hubspot_snapshot => 'array'`. Não alterar `valor_contratado` nem o comportamento existente.
    Cross-driver: decimal(12,2) e json funcionam em SQLite e MySQL (precedente: valor_contratado=>decimal:2, faturamento_mensal). Sem enum/CHECK → sem branch SQLite.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase111HubspotSchemaTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Phase111HubspotSchemaTest` sai com código 0 — os 3 métodos passam (GREEN completo: companies + contratos + nullable/zero-impacto).
    - Após `migrate`, `Schema::hasColumn('contratos_servico', X)` true para as 11 colunas.
    - `(new ContratoServico)->getFillable()` contém as 11 colunas; `hubspot_valor_original`/`hubspot_valor_normalizado_mensal` castam decimal:2; `hubspot_snapshot` casta array.
    - `grep -c "Schema::hasColumn('contratos_servico'" database/migrations/2026_07_24_111002_*.php` >= 11.
    - `valor_contratado` e casts existentes intactos (`grep -c "valor_contratado" app/Models/ContratoServico.php` >= 2).
    - Regressão zero: `php artisan test --filter=Phase37WebhookLineItemsTest --filter=Phase34HubspotWebhookTest` verdes.
  </acceptance_criteria>
  <done>contratos_servico ganha as 11 colunas HubSpot nullable via migration defensiva reversível; ContratoServico reflete fillable + casts decimal/json; valor_contratado inalterado; a suite Phase111HubspotSchemaTest fica 100% verde e a suíte HubSpot legada permanece verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → schema de produção (MariaDB) | Alterações de schema aplicadas via deploy; risco de incompatibilidade cross-driver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-05 | Denial of Service | Migration não-idempotente em re-run/deploy | mitigate | Cada coluna sob Schema::hasColumn; down() defensivo; testado em SQLite (ambiente de teste do projeto). |
| T-111-06 | Tampering | Cross-driver json/decimal (SQLite vs MariaDB) | mitigate | Usar só json/string/text/decimal(12,2) — todos com precedente funcional no projeto; sem enum/CHECK (evita armadilha SQLite conhecida na MEMORY). |
</threat_model>

<verification>
- `php artisan test --filter=Phase111HubspotSchemaTest` verde.
- Regressão: Phase34/Phase37 HubSpot verdes.
- `grep` confirma 8 colunas defensivas em companies e 11 em contratos_servico + fillable/casts nos models.
</verification>

<success_criteria>
- HUB-SCHEMA-01: companies ganha as 8 colunas nullable (3 indexadas) via migration defensiva reversível, refletidas em Company.
- HUB-SCHEMA-02: contratos_servico ganha as 11 colunas nullable (line_item_id indexado, 2 decimais, 1 json) via migration defensiva reversível, refletidas em ContratoServico; valor_contratado inalterado.
</success_criteria>

<output>
Criar `.planning/phases/111-funda-o-descoberta-de-propriedades-api-client-ampliado-e-cam/111-03-SUMMARY.md` ao concluir.
</output>
