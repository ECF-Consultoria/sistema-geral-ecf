---
phase: 35-fix-cadastro-hubspot-v2
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - app/Http/Controllers/ComercialController.php
  - app/Http/Controllers/Api/HubspotWebhookController.php
  - app/Services/HubspotApiClient.php
  - app/Services/MlbImplementacaoFactory.php
  - config/services.php
  - .env.example
  - tests/Feature/Phase35HubspotV2Test.php
autonomous: true
requirements:
  - REQ-35-06
  - REQ-35-07

must_haves:
  truths:
    - "HubspotApiClient ganha 2 metodos: fetchAssociatedContactId(dealId) e fetchContact(id, properties) — Http::withToken padrao Plan 34-04"
    - "config/services.php ganha bloco hubspot.props.contact (firstname, lastname, email, phone) com env() override"
    - ".env.example documenta HUBSPOT_PROP_CONTACT_FIRSTNAME etc"
    - "ComercialController::criarImplementacaoPolo extraido para classe estatica reutilizavel App\\Services\\MlbImplementacaoFactory::criarParaPolo(MlbEmpresa \$emp, array \$handoff = []). Metodo private original em ComercialController vira proxy para a factory (preserva API existente)"
    - "HubspotWebhookController::criarEmpresa apos criar Company + ContratoServico: chama ComercialController::servicoDisparaImplementacao(servicoNome) e roteia: polos => MlbEmpresa POLO + factory; assessoria => MlbEmpresa ASSESSORIA; incubadora => MlbEmpresa INCUBADORA. Publicidade/Gestão/Publicação retornam null e nao criam mlb_empresas (preserva semantica atual)"
    - "HubspotWebhookController::criarEmpresa fetcha contato vinculado (se houver) via novo HubspotApiClient::fetchAssociatedContactId + fetchContact. Falla silenciosa se nao houver contato"
    - "Fallback do contato: se contato.email existe E company.email_cliente vazio, usa contato.email. Idem para telefone (contato.phone -> company.telefone se vazio). Prioridade: company > contato"
    - "Nome do contato (firstname + lastname concatenados, trim) salvo em company.notes como 'Contato (HubSpot): {nome}' se houver. Gancho pra coluna futura"
    - "Suite Phase35HubspotV2Test com 4-6 cases: contato_e_buscado_e_email_fallback, contato_telefone_fallback, polos_cria_mlb_empresa, assessoria_cria_mlb_empresa, publicidade_nao_cria_mlb_empresa, sem_contato_fluxo_segue_sem_erro"
  artifacts:
    - path: "app/Services/MlbImplementacaoFactory.php"
      provides: "Factory estatica reutilizavel para criarParaPolo"
      contains: "criarParaPolo"
    - path: "app/Services/HubspotApiClient.php"
      provides: "Metodos contact"
      contains: "fetchContact"
    - path: "app/Http/Controllers/Api/HubspotWebhookController.php"
      provides: "Cria MlbEmpresa quando servico dispara + fetcha contato"
      contains: "servicoDisparaImplementacao"
---

# Plan 35-02: HubSpot v2 — contato vinculado + MlbEmpresa + factory

Wave 2. Depende W1 (mais por coexistencia segura no controller que por dependencia tecnica).

## Escopo

1. **Extrair `ComercialController::criarImplementacaoPolo` para `App\Services\MlbImplementacaoFactory`**:
   ```php
   namespace App\Services;
   use App\Models\MlbEmpresa;
   use App\Models\MlbImplementacao;
   use App\Models\MlbConfiguracao;
   use Illuminate\Support\Str;
   class MlbImplementacaoFactory {
       public static function criarParaPolo(MlbEmpresa $empresa, array $handoff = []): MlbImplementacao {
           // copiar logica atual do ComercialController::criarImplementacaoPolo
           // (mantida sem alteracao funcional)
       }
   }
   ```
   `ComercialController::criarImplementacaoPolo` vira proxy:
   ```php
   private function criarImplementacaoPolo(MlbEmpresa $empresa, array $handoff = []): MlbImplementacao {
       return MlbImplementacaoFactory::criarParaPolo($empresa, $handoff);
   }
   ```

2. **`HubspotApiClient`** — adicionar 2 métodos:
   ```php
   public function fetchAssociatedContactId(string $dealId): ?string {
       $res = Http::withToken($this->token)
           ->get(self::BASE . "/crm/v3/objects/deals/{$dealId}/associations/contacts");
       if (!$res->ok()) return null;
       return $res->json('results.0.toObjectId');
   }
   public function fetchContact(string $id, array $properties): array {
       $res = Http::withToken($this->token)
           ->get(self::BASE . "/crm/v3/objects/contacts/{$id}", ['properties' => implode(',', $properties)]);
       $res->throw();
       return $res->json();
   }
   ```

3. **`config/services.php`** — adicionar:
   ```php
   'contact' => [
       'firstname' => env('HUBSPOT_PROP_CONTACT_FIRSTNAME', 'firstname'),
       'lastname'  => env('HUBSPOT_PROP_CONTACT_LASTNAME', 'lastname'),
       'email'     => env('HUBSPOT_PROP_CONTACT_EMAIL', 'email'),
       'phone'     => env('HUBSPOT_PROP_CONTACT_PHONE', 'phone'),
   ],
   ```
   dentro do `props` existente.

4. **`.env.example`** — documentar 4 vars HUBSPOT_PROP_CONTACT_*.

5. **`HubspotWebhookController::criarEmpresa`** — depois da chamada atual ao API client (fetchDeal + fetchCompany), adicionar fetch de contato:
   ```php
   $contactId = $api->fetchAssociatedContactId($dealId);
   $hubContact = null;
   if ($contactId) {
       try {
           $hubContact = $api->fetchContact($contactId, array_values($contactPropsConfig));
       } catch (\Throwable $e) {
           // log warning, segue sem contato
       }
   }
   $cprops = $hubContact['properties'] ?? [];
   ```
   No mapeamento da company:
   - `email_cliente` = company.email ?? contato.email
   - `telefone` = company.phone ?? contato.phone
   - Concatenar `firstname + ' ' + lastname` → adiciona em notes "Contato (HubSpot): {nome}"

6. **Apos criar Company + ContratoServico no `DB::transaction`**, adicionar:
   ```php
   $tipoImpl = $servicoNome ? \App\Http\Controllers\ComercialController::servicoDisparaImplementacao($servicoNome) : null;
   if ($tipoImpl === 'polos') {
       $mlbEmp = MlbEmpresa::create(['nome' => $company->name, 'tipo' => 'POLO', 'projeto' => 'POLOS', 'company_id' => $company->id]);
       MlbImplementacaoFactory::criarParaPolo($mlbEmp);
   } elseif ($tipoImpl === 'assessoria') {
       MlbEmpresa::create(['nome' => $company->name, 'tipo' => 'ASSESSORIA', 'company_id' => $company->id]);
   } elseif ($tipoImpl === 'incubadora') {
       MlbEmpresa::create(['nome' => $company->name, 'tipo' => 'INCUBADORA', 'company_id' => $company->id]);
   }
   ```

7. **Testes** (`tests/Feature/Phase35HubspotV2Test.php`) — 4-6 cases:
   - `test_polos_cria_mlb_empresa_e_implementacao` — payload com servico=Polos → cria Company + MlbEmpresa POLO + MlbImplementacao
   - `test_assessoria_cria_mlb_empresa` — idem ASSESSORIA
   - `test_publicidade_nao_cria_mlb_empresa` — servico=Publicidade → so Company
   - `test_contato_email_fallback` — company sem email + contato com email → email_cliente = contato.email
   - `test_contato_telefone_fallback` — idem telefone
   - `test_sem_contato_fluxo_completa` — fetchAssociatedContactId retorna null → sem erro, sem fallback
   - Use Http::fake para mockar API HubSpot

## Constraints

- pt-BR comentários
- Não fazer deploy
- Suite Phase 34 HubSpot existente continua verde (zero regressão na assinatura HMAC v3)
- Factory `MlbImplementacaoFactory` é pura — não acoplada a HTTP

## Verificação

- `php artisan test --filter="Phase34HubspotWebhook|Phase35HubspotV2"` verde
- Smoke local com cURL: simular webhook com deal.servico=Polos → confirma Company + MlbEmpresa + MlbImplementacao no DB
- Build verde
