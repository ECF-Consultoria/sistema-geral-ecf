---
phase: 35-fix-cadastro-hubspot-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_13_400001_backfill_empresa_nova_existentes.php
  - app/Http/Controllers/CompanyController.php
  - app/Models/Company.php
  - resources/js/Pages/Companies/Index.jsx
  - resources/js/Pages/Comercial/NovaEmpresa.jsx
  - resources/js/Pages/Comercial/Empresas.jsx
  - resources/js/Pages/Companies/Show.jsx
autonomous: true
requirements:
  - REQ-35-01
  - REQ-35-02
  - REQ-35-03
  - REQ-35-04
  - REQ-35-05

must_haves:
  truths:
    - "Migration de backfill (defensiva — só zera registros antigos) atualiza empresas com created_at < 2026-06-13 setando empresa_nova=false + empresa_nova_visto_em=now() + empresa_nova_visto_por=null"
    - "Migration idempotente (re-run = no-op em registros já zerados)"
    - "Company model ganha relacionamento mlbEmpresa() hasOne (ou hasMany — verificar realidade — se hasMany, usar mlbEmpresas())"
    - "CompanyController::index aplica whereDoesntHave('mlbEmpresa') (ou whereDoesntHave('mlbEmpresas') se hasMany) na query base — empresas com MlbEmpresa NÃO aparecem em /companies"
    - "Contadores no payload (total empresas + tabs) também excluem empresas com MlbEmpresa — coerencia"
    - "CompanyController::index aceita query param ?sort=nova_recente|nova_antiga; quando filter tabs=pendencias e item=empresa_nova, ordena por created_at desc (recente) ou asc (antiga); default mantem orderBy('name')"
    - "Companies/Index.jsx mostra dropdown 'Ordenar' quando aba Pendencias ativa E filtro especifico empresa_nova selecionado (2 opcoes: Mais recente / Mais antiga). Outras combinacoes mostram default."
    - "Label 'Vende em outras marketplaces?' substituido por 'Em quais outros marketplaces o cliente já vende?' + helper text/tooltip 'Marketplaces que o cliente já opera por conta própria — não serviços que vamos prestar'. Aplicado em 3 sites: Comercial/NovaEmpresa.jsx, Comercial/Empresas.jsx, Companies/Index.jsx modal"
    - "Auditar e padronizar formatacao BRL em todos os sites com valor de servico/contrato/faturamento — formatCurrency de @/lib/utils OU helper local fmtBRL consistente. Sites alvos: Companies/Show.jsx, Companies/Index.jsx, Comercial/Empresas.jsx, Comercial/NovaEmpresa.jsx summary"
  artifacts:
    - path: "database/migrations/2026_06_13_400001_backfill_empresa_nova_existentes.php"
      provides: "Migration backfill idempotente"
      contains: "where('created_at'"
    - path: "app/Models/Company.php"
      provides: "Relacionamento mlbEmpresa"
      contains: "MlbEmpresa"
    - path: "app/Http/Controllers/CompanyController.php"
      provides: "Filtra empresas com MlbEmpresa + sort opcional"
      contains: "whereDoesntHave"
---

# Plan 35-01: Fixes UX/backfill + filtro /companies sem MlbEmpresa

Wave 1. Sem deps. Bloqueia W2 só se W2 precisar dos novos relacionamentos (não estritamente — pode rodar paralelo, mas vou marcar como bloqueador pra evitar conflitos de merge no CompanyController).

## Escopo

1. **Migration `2026_06_13_400001_backfill_empresa_nova_existentes.php`**:
   ```php
   public function up(): void {
       DB::table('companies')
           ->where('created_at', '<', '2026-06-13 00:00:00')
           ->where('empresa_nova', true)  // idempotência: só zera quem ainda está true
           ->update(['empresa_nova' => false, 'empresa_nova_visto_em' => now(), 'empresa_nova_visto_por' => null]);
   }
   public function down(): void {
       // No-op intencional — não dá pra distinguir backfill de marcação humana após o fato.
   }
   ```

2. **`app/Models/Company.php`** — verificar se já tem `mlbEmpresa()` ou `mlbEmpresas()`. Se faltar, adicionar:
   ```php
   public function mlbEmpresa() { return $this->hasOne(MlbEmpresa::class); }
   // OU hasMany se 1 empresa pode ter múltiplos registros mlb (improvável)
   ```

3. **`CompanyController::index`**:
   - Adicionar `whereDoesntHave('mlbEmpresa')` (ou plural) na query base de companies
   - Garantir que `pendCounts` e `total` no payload também excluem MLB (mesma query base)
   - Adicionar parse de `?sort=nova_recente|nova_antiga` + lógica condicional:
     ```php
     $sort = $request->input('sort');
     if (in_array($sort, ['nova_recente', 'nova_antiga'], true)) {
         $base->orderBy('created_at', $sort === 'nova_recente' ? 'desc' : 'asc');
     } else {
         $base->orderBy('name');
     }
     ```

4. **`Companies/Index.jsx`**:
   - Quando aba `pendencias` ativa E filter `empresa_nova` selecionado (pendenciaFilter === 'empresa_nova'), exibir um Select shadcn com 2 opções (Mais recente, Mais antiga) que chama `router.get` com `?sort=...`
   - Mudar label das marketplaces (atual: "Vende em outras marketplaces?") por:
     ```
     "Em quais outros marketplaces o cliente já vende?"
     <p className="text-xs text-white/40 mt-1">
       Marketplaces que o cliente já opera por conta própria. Não confundir com serviços que vamos prestar.
     </p>
     ```

5. **`Comercial/NovaEmpresa.jsx`** + **`Comercial/Empresas.jsx`**:
   - Aplicar mesma mudança de label das marketplaces (consistência)

6. **Audit + format BRL** (D-07) em:
   - `Companies/Show.jsx` — campo Faturamento mensal (verificar se usa formatCurrency)
   - `Comercial/Empresas.jsx` — cards de contrato (já usa fmtBRL local; checar se BRL completo ou abreviado)
   - `Comercial/NovaEmpresa.jsx` summary de serviços + faturamento
   - `Companies/Index.jsx` modal admin (faturamento mensal display)

   Padrão: `Number(n||0).toLocaleString('pt-BR', { style:'currency', currency:'BRL' })`. Se sites já usam padrão diferente (ex: sem casas decimais), unificar com casas decimais visíveis ou pelo menos consistência.

## Constraints

- Comentários pt-BR
- Não fazer deploy
- Manter os 83 testes anteriores verdes (zero regressão)
- Migration idempotente — re-run = no-op
- `whereDoesntHave` deve usar nome correto da relação (verificar Company.php)

## Verificação

- `php artisan migrate` roda local sem erro
- Tinker: criar 2 Company (1 com created_at='2026-06-12', 1 com created_at='2026-06-14') + rodar migration → primeira fica empresa_nova=false, segunda permanece true
- Tinker: criar Company + MlbEmpresa associada + chamar Company::query()->whereDoesntHave('mlbEmpresa')->count() → não inclui essa
- Acesso /companies não mostra empresa que tem mlb_empresa associada
- Build verde
- Suite Phase 31+33+34+35 verde
