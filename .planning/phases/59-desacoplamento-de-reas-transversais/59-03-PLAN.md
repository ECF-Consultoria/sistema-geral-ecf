---
phase: 59-desacoplamento-de-reas-transversais
plan: 03
type: execute
wave: 3
depends_on: [59-01, 59-02]
files_modified:
  - .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md
autonomous: true
requirements: [CROSS-02, CROSS-03]
tags: [regression, verification, multi-marketplace, cross-cutting]

must_haves:
  truths:
    - "`php artisan test` completo pós-fix passa com número de verdes >= baseline registrado em 59-AUDIT.md §Baseline pré-fix (Plan 01)"
    - "Phase 57 continua 20/20 e Phase 58 continua 16/16 pós-fix (baselines conhecidos)"
    - "Se algum teste ficou vermelho por causa dos fixes, foi corrigido no MESMO commit (não deferido)"
    - "Seção '## Publicação — CONFIRMED transversal' do AUDIT confirmada com evidência de execução da suite (não apenas grep estático)"
    - "AUDIT.md tem seção final '## Plan 03 — Regressão + confirmação Publicação' com contagem pós-fix e delta vs. baseline"
  artifacts:
    - path: ".planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md"
      provides: "Regressão registrada + Publicação confirmada + Phase 59 fechada"
      contains: "Plan 03 — Regressão"
  key_links:
    - from: "AUDIT.md §Plan 03 — Regressão"
      to: "AUDIT.md §Baseline pré-fix (Plan 01)"
      via: "delta numérico (pós - pré) >= 0"
      pattern: "Delta.*[0-9]+"
    - from: "AUDIT.md §Publicação — CONFIRMED transversal"
      to: "output real de `php artisan test` (não apenas grep estático)"
      via: "referência a suite executada"
      pattern: "php artisan test"
---

<objective>
Fechar Phase 59 com gate de regressão rigoroso. Rodar suite completa pós-fix, comparar com baseline registrado no Plan 01, corrigir qualquer teste que quebrou por causa dos fixes do Plan 02, e reforçar a seção "Publicação — CONFIRMED transversal" com evidência de suite verde (não só grep estático). Deliverable central: CROSS-02 (Publicação transversal confirmada) + CROSS-03 (zero regressão).

Purpose: A suite verde é o cinto de segurança realista do escopo cirúrgico do CONTEXT §5. Sem gate rigoroso, um fix mal-calibrado pode passar despercebido; sem confirmação Publicação via suite, CROSS-02 fica meia-boca (grep-only vs. execução real).

Output: `59-AUDIT.md` fechado com seção "Plan 03 — Regressão" (contagem pós-fix, delta vs. baseline, testes vermelhos resolvidos se houve) + confirmação Publicação com referência a execução real da suite. Phase 59 pronta para `/gsd:complete-phase 59`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/59-desacoplamento-de-reas-transversais/59-CONTEXT.md
@.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md
@.planning/phases/59-desacoplamento-de-reas-transversais/59-01-SUMMARY.md
@.planning/phases/59-desacoplamento-de-reas-transversais/59-02-SUMMARY.md

<interfaces>
Baselines conhecidos (de 58-VERIFICATION.md linhas 49-50):
- Phase 57: 20 passed (26 assertions)
- Phase 58: 16 passed (62 assertions)

Baseline completo `php artisan test` gravado no `59-AUDIT.md §Baseline pré-fix (Plan 01)` — recuperar valores literais dessa seção.

Regra do gate:
- passed_pos_fix >= passed_baseline (nunca menos)
- Phase 57 e Phase 58 devem permanecer 20 e 16 exatos (baselines fixos)
- Se teste vermelho aparecer, deve ser resolvido nesta plan (não deferido)

Como resolver teste vermelho legítimo pós-fix:
- Se teste dependia de naming antigo (ex: assertEquals('contarMlb', ...)): atualizar teste para novo naming — commit `test(59-03): ajusta assertion para novo naming pós-fix Plan 02`
- Se teste dependia de comportamento antigo (ex: dependia de filtro removido): reverter fix específico no controller e reclassificar item no AUDIT como "AUDIT errou classificação — reverter" — commit `revert(59-03): reverte fix XYZ; AUDIT reclassificado`
- Regra dura: nenhum teste fica vermelho no final desta plan
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rodar suite completa pós-fix + comparar com baseline</name>
  <files>.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</files>
  <read_first>
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md` (§Baseline pré-fix + §Plan 02 — Execução)
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-02-SUMMARY.md` (fixes aplicados — contexto para diagnóstico se houver vermelho)
  </read_first>
  <action>
    (1) Recuperar baseline exato do AUDIT.md §Baseline pré-fix (Plan 01): número de tests passed, assertions, tempo. Registrar como B_passed, B_assertions.

    (2) Rodar `php artisan test` completo (sem filtro). Capturar do output final: P_passed, P_assertions, P_time.

    (3) Rodar suites-alvo do baseline conhecido:
    - `php artisan test --filter=Phase57` → esperado 20/20
    - `php artisan test --filter=Phase58` → esperado 16/16

    (4) Calcular deltas:
    - Delta_passed = P_passed - B_passed (esperado >= 0)
    - Delta_assertions = P_assertions - B_assertions (informativo)

    (5) Se P_passed < B_passed OU Phase 57 != 20 OU Phase 58 != 16:
    - IDENTIFICAR quais testes ficaram vermelhos: `php artisan test 2>&1 | grep -E "FAIL|Failing"`
    - CLASSIFICAR cada vermelho como (a) naming quebrou assertion — corrigir teste; (b) lógica quebrou comportamento — reverter fix; (c) pré-existente não relacionado — documentar como fora do escopo
    - Ir para Task 2 (fix dos vermelhos) — não avançar para documentação até resolver

    (6) Se P_passed >= B_passed E Phase 57 == 20 E Phase 58 == 16:
    - Prosseguir direto para Task 3 (documentação de fechamento) — Task 2 pula

    Comentários pt-BR em qualquer nota inline.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase57 && php artisan test --filter=Phase58</automated>
  </verify>
  <done>
    Suite executada; P_passed, P_assertions e deltas calculados; Phase 57 e Phase 58 baselines verificados; decisão registrada: (a) prosseguir para Task 3 diretamente, ou (b) resolver vermelhos em Task 2 primeiro.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolver vermelhos pós-fix (skip se suite já verde)</name>
  <files>
    tests/**/*.php,
    app/Http/Controllers/ComercialController.php,
    app/Http/Controllers/CompanyController.php,
    app/Http/Controllers/AdminController.php
  </files>
  <read_first>
    - Cada arquivo de teste vermelho identificado em Task 1
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-02-SUMMARY.md` (contexto de qual fix Plan 02 potencialmente causou o vermelho)
  </read_first>
  <action>
    Apenas executar se Task 1 identificou vermelhos causados pelos fixes do Plan 02. Se suite já está verde, PULAR esta task.

    Para cada teste vermelho:

    (1) Ler arquivo de teste e trecho da falha. Identificar causa raiz.

    (2) Classificar:
    - **Caso A — naming quebrou assertion** (teste referenciava método/variável renomeada): atualizar teste com novo naming. Rodar `php artisan test --filter={NomeDoTeste}` para confirmar verde. Commit atômico `test(59-03): ajusta {ArquivoTeste} para novo naming pós-fix Plan 02`.
    - **Caso B — lógica quebrou comportamento intencional** (ex: fix removeu filtro que era essencial ao caso de teste): REVERTER o commit específico do Plan 02 que causou o vermelho (`git revert <sha>` OU Edit reverse na mão). Reabrir 59-AUDIT.md linha correspondente, mover de "APLICADO em <sha>" para "REVERTIDO — reclassificado como no-op (AUDIT errou)". Commit `revert(59-03): reverte fix XYZ — reclassificado no AUDIT`.
    - **Caso C — vermelho pré-existente não relacionado aos fixes**: documentar em AUDIT.md §Plan 03 seção "Vermelhos pré-existentes fora do escopo" e não tocar (não é regressão nossa).

    (3) Após cada resolução, rodar suite filtrada afetada. Após TODAS resoluções, rodar `php artisan test` completo novamente para confirmar zero vermelhos novos.

    (4) Regra dura: se caso B (reverter) for aplicado, atualizar `59-AUDIT.md` na linha correspondente ANTES de commitar — rastreabilidade CROSS-01.

    Comentários pt-BR.
  </action>
  <verify>
    <automated>php artisan test 2>&1 | tail -5</automated>
  </verify>
  <done>
    Suite completa 100% verde. Cada vermelho identificado em Task 1 foi resolvido via Caso A, B ou C, com commit atômico e nota no AUDIT.md quando aplicável.
  </done>
</task>

<task type="auto">
  <name>Task 3: Documentar fechamento — seção 'Plan 03 — Regressão + Publicação' no AUDIT + reforço Publicação transversal</name>
  <files>.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</files>
  <read_first>
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md` (estado atual, especialmente seção Publicação e §Baseline)
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-CONTEXT.md` (§Success Criteria items 4/5/6)
  </read_first>
  <action>
    Adicionar seção final no `59-AUDIT.md`: "## Plan 03 — Regressão + confirmação Publicação"

    Conteúdo obrigatório:

    (1) **Regressão (CROSS-03):**
    - Data ISO de execução
    - `Baseline (Plan 01): B_passed tests, B_assertions assertions`
    - `Pós-fix (Plan 03): P_passed tests, P_assertions assertions`
    - `Delta: P_passed - B_passed = X (esperado >= 0)`
    - `Phase 57: 20/20 ✓ (baseline preservado)`
    - `Phase 58: 16/16 ✓ (baseline preservado)`
    - Se Task 2 rodou: lista dos commits `test(59-03)` / `revert(59-03)` gerados
    - Afirmativa final: "Zero regressão confirmada." OU se algum vermelho pré-existente foi documentado como fora de escopo, listar aqui.

    (2) **Publicação transversal reforçada (CROSS-02):**
    - Reabrir a seção "## Publicação — CONFIRMED transversal" (criada em Plan 01) e adicionar evidência DINÂMICA:
      - "Suite completa executada em <data> — 0 testes de módulos Publicação/pub.* ficaram vermelhos após os fixes cirúrgicos em áreas transversais"
      - Grep adicional se algum teste específico exercita hasPubPermission: `grep -rn "hasPubPermission\|pub\." tests/ | head -10` (amostrar)
    - Afirmativa final: "Publicação confirmada transversal — permissões pub.* funcionam sem amarração implícita a marketplace; grep + suite verde comprovam."

    (3) **Encerramento Phase 59:**
    - Cross-ref CROSS-01/02/03 com plan de origem e status
    - Nota: "Phase 59 pronta para `/gsd:complete-phase 59`"
    - Aviso: se AUDIT flagou itens "deferred v14+", listar como TODOs para próxima milestone (não bloqueiam fechamento desta phase)

    Comentários pt-BR.
  </action>
  <verify>
    <automated>grep -c "Plan 03 — Regressão" .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</automated>
  </verify>
  <done>
    AUDIT.md fechado com seção "Plan 03 — Regressão + confirmação Publicação" contendo: deltas de suite (>=0), Phase 57/58 baselines preservados, Publicação transversal com evidência dinâmica de suite verde, cross-ref CROSS-01/02/03 → planos, aviso de itens deferred v14+ para próxima milestone. Phase 59 pronta para complete-phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| execução de suite completa | roda contra SQLite in-memory; sem side-effect em prod |
| edição de arquivo de teste | pode escamotear regressão real se ajuste for muito laxo (ex: comentar assertion em vez de corrigir) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03-01 | Tampering | teste ajustado com assertion frouxa para "passar" sem validar comportamento real | mitigate | Task 2 §(2) Caso A obriga que teste seja ajustado apenas para novo naming (não relaxar assertion); Caso B obriga reverter fix se comportamento essencial foi quebrado — nunca "comentar" assertion |
| T-59-03-02 | Repudiation | AUDIT.md fecha sem evidência numérica de baseline vs. pós-fix | mitigate | Task 3 §(1) exige delta numérico literal (não texto vago tipo "tudo verde"); Plan 01 já registrou baseline numérico |
| T-59-03-03 | Information Disclosure | grep incluir dados sensíveis no AUDIT | mitigate | Grep de Task 3 §(2) limita a `hasPubPermission|pub\.` — só naming/refs, sem valores de token/secret |
| T-59-03-04 | Denial of Service | fix não revertido causa regressão em prod pós-deploy | mitigate | Task 2 obriga revert quando teste vermelho for causado por lógica quebrada; Task 3 exige delta >= 0 explicitamente; sem gate positivo, plan não fecha |
</threat_model>

<verification>
- `php artisan test` completo passa (P_passed >= B_passed do baseline Plan 01)
- `php artisan test --filter=Phase57` → 20/20 exatos
- `php artisan test --filter=Phase58` → 16/16 exatos
- `grep -c "Plan 03 — Regressão" 59-AUDIT.md` == 1
- `grep -c "Delta:" 59-AUDIT.md` >= 1 (delta numérico registrado)
- `grep -c "Zero regressão confirmada" 59-AUDIT.md` == 1 (ou justificativa registrada se algum vermelho pré-existente ficou fora de escopo)
- `grep -c "Publicação confirmada transversal" 59-AUDIT.md` >= 1
- `git diff --name-only HEAD~N HEAD` mostra apenas arquivos em `files_modified` desta plan + eventuais tests/*.php que Task 2 ajustou (caso A)
</verification>

<success_criteria>
- CROSS-03 fechado: zero regressão confirmada com número exato de delta
- CROSS-02 fechado: Publicação transversal confirmada por grep estático + suite dinâmica
- 59-AUDIT.md é o arquivo canônico de fechamento da Phase 59 (rastreia tudo: baseline, fixes, regressão, Publicação)
- Phase 57 e Phase 58 baselines preservados (20 e 16 respectivamente)
- Se algum vermelho pré-existente ficou fora de escopo, ficou explícito no AUDIT (não silenciado)
- Phase 59 pronta para `/gsd:complete-phase 59`
</success_criteria>

<output>
Criar `.planning/phases/59-desacoplamento-de-reas-transversais/59-03-SUMMARY.md` ao concluir, contendo:
- Deltas numéricos da suite (baseline vs. pós-fix)
- Phase 57 (20/20) e Phase 58 (16/16) confirmados
- Lista de commits `test(59-03)` / `revert(59-03)` se Task 2 rodou
- Cross-ref: CROSS-01 fechado por Plan 01+02, CROSS-02 fechado por Plan 01+03, CROSS-03 fechado por Plan 03
- Itens deferred v14+ listados como TODOs para próxima milestone
- Nota final: "Phase 59 pronta para /gsd:complete-phase 59"

Criar também `.planning/phases/59-desacoplamento-de-reas-transversais/PHASE-SUMMARY.md` (fechamento da phase) resumindo os 3 plans em ~30 linhas com deliverables e evidências.
</output>
