---
phase: 59-desacoplamento-de-reas-transversais
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - app/Http/Controllers/ComercialController.php
  - app/Http/Controllers/CompanyController.php
  - app/Http/Controllers/AdminController.php
  - .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md
autonomous: true
requirements: [CROSS-01]
tags: [refactor, multi-marketplace, cross-cutting, fixes]

must_haves:
  truths:
    - "Todos os itens classificados 'fix Phase 59' na lista '## Itens a corrigir no Plan 02' do 59-AUDIT.md estão resolvidos no código"
    - "Nenhum item classificado 'deferred v14+' ou 'no-op (legítimo)' foi tocado (respeitar escopo cirúrgico D-4)"
    - "Cada fix tem 1 commit atômico com mensagem no formato `refactor(59-02): {arquivo}:{linha} — {tipo}`"
    - "Cada linha corrigida em AUDIT.md tem status atualizado (fix Phase 59 → APLICADO em <commit-sha>)"
    - "Se 'Itens a corrigir' estava vazia (todos INFO/legítimo), documentar Plan 02 como no-op consciente e explícito"
  artifacts:
    - path: "app/Http/Controllers/ComercialController.php"
      provides: "Fixes cirúrgicos onde AUDIT classificou 'fix Phase 59'"
      contains: "class ComercialController"
    - path: "app/Http/Controllers/CompanyController.php"
      provides: "Fixes cirúrgicos onde AUDIT classificou 'fix Phase 59'"
      contains: "class CompanyController"
    - path: "app/Http/Controllers/AdminController.php"
      provides: "Fixes cirúrgicos onde AUDIT classificou 'fix Phase 59'"
      contains: "class AdminController"
    - path: ".planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md"
      provides: "Atualizado — cada fix marcado APLICADO"
      contains: "APLICADO"
  key_links:
    - from: "59-AUDIT.md §Itens a corrigir"
      to: "commits refactor(59-02) no working tree"
      via: "1 commit por item corrigido"
      pattern: "refactor\\(59-02\\)"
---

<objective>
Aplicar EXATAMENTE os fixes classificados como `fix Phase 59` na seção "## Itens a corrigir no Plan 02" do `59-AUDIT.md` (produzido no Plan 01). Escopo estritamente cirúrgico — não generalizar além do que o AUDIT classificou, não migrar para pivot N:N (deferred v14+), não tocar código ML-dedicado.

Purpose: Fechar CROSS-01 na parte executiva. AUDIT.md classificou; agora corrige o que foi classificado — sem improvisar escopo. Se AUDIT determinou lista vazia, Plan 02 vira no-op consciente e documentado (comportamento válido — o mapa por si só já entrega CROSS-01).

Output: 3 controllers com fixes aplicados (arquivo:linha exatamente conforme AUDIT.md); AUDIT.md atualizado com "APLICADO em <sha>"; commits atômicos rastreáveis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/59-desacoplamento-de-reas-transversais/59-CONTEXT.md
@.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md
@.planning/phases/59-desacoplamento-de-reas-transversais/59-01-SUMMARY.md
@app/Http/Controllers/ComercialController.php
@app/Http/Controllers/CompanyController.php
@app/Http/Controllers/AdminController.php

<interfaces>
Escopo desta plan é 100% derivado de `59-AUDIT.md §Itens a corrigir no Plan 02`. NÃO é permitido:
- Adicionar itens não listados no AUDIT (mesmo se parecerem "obviamente erradas" durante a leitura)
- Remover itens listados no AUDIT sem justificativa registrada (bloqueia CROSS-01 tracking)
- Fazer refactor de escopo maior (rename em cascata que passa por outros arquivos além dos 3 controllers)
- Migrar queries para pivot N:N `whereHas('marketplaces', ...)` — deferred v14+ conforme CONTEXT §4 e Deferred Ideas

Regra de escopo: cada item da lista "Itens a corrigir" produz EXATAMENTE 1 commit. Se um item exigir mudança em >1 arquivo, dividir em 2 commits atômicos (um por arquivo) e citar o item original nos dois.

Padrões de fix conhecidos (calibração — validar contra AUDIT antes de aplicar):
- **Naming rename** (ex: método `contarMlb()` → `contarEmpresasAtivas()` em contexto transversal): grep completo dos callers ANTES; rename com Edit; grep após para confirmar 0 refs órfãs; se caller JSX/teste/doc, incluir no mesmo commit
- **Remoção de filtro `where('marketplace', 'meli')` em query transversal**: comentar linha original com 1-liner pt-BR explicando "removido — query é transversal"; Edit substitui; grep após para verificar
- **Default UI ML em dropdown/select**: mudar valor default null ou dinâmico (a partir de `$company->primaryMarketplace()->marketplace` se disponível); nunca hardcodar shopee ou outro marketplace
- **Comentário/docblock desatualizado**: reescrever para refletir realidade multi-marketplace atual

Se AUDIT.md `Itens a corrigir` estiver VAZIA:
- Plan 02 vira no-op consciente e documentado
- Adicionar 1 commit `docs(59-02): Plan 02 fecha como no-op — AUDIT classificou 0 fixes cirúrgicos` que atualiza APENAS `59-AUDIT.md` com nota "Plan 02 no-op — AUDIT.md §Itens a corrigir vazia"
- Pular Task 2 e ir direto para verificação/summary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ler lista '## Itens a corrigir no Plan 02' + validar cada item contra código atual</name>
  <files>.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</files>
  <read_first>
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md` (§Sumário + §Itens a corrigir no Plan 02)
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-01-SUMMARY.md` (contagem baseline + sumário Plan 01)
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-CONTEXT.md` (§4 profundidade dos fixes — regra do escopo cirúrgico)
  </read_first>
  <action>
    Ler a lista "## Itens a corrigir no Plan 02" do `59-AUDIT.md`. Para cada item:

    (1) Registrar arquivo:linha, trecho, tipo, plano de fix descrito no AUDIT.
    (2) Ler o range de código real (arquivo alvo, linha ± 5) para confirmar que o trecho ainda existe idêntico ao que AUDIT capturou (Plan 01 pode ter sido rodado em commit anterior — validar drift).
    (3) Se o trecho DRIFTOU (arquivo mudou desde AUDIT), NÃO aplicar fix cego — reabrir com nota no summary "Item X driftou — revalidar AUDIT antes de Plan 02".
    (4) Se o trecho ESTÁ IDÊNTICO, marcar como "pronto para aplicar em Task 2".

    Produzir uma lista interna de trabalho no formato:
    - Item 1: ComercialController.php:LINHA — trecho `...` — plano `substituir por ...` — status: [pronto | driftou | ambiguidade]
    - (etc)

    Se a lista do AUDIT tem 0 itens (todos INFO/legítimo/deferred): PULAR Task 2 e ir direto para Task 3 (documentação no-op).

    Comentários pt-BR em qualquer nota inline.
  </action>
  <verify>
    <automated>grep -c "Itens a corrigir no Plan 02" .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</automated>
  </verify>
  <done>
    Lista de trabalho interna gerada com N itens (N pode ser 0). Para N > 0: cada item validado contra código atual (nenhum drift silencioso). Para N = 0: pular Task 2 e ir para Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Aplicar fixes item a item — 1 commit atômico por item</name>
  <files>
    app/Http/Controllers/ComercialController.php,
    app/Http/Controllers/CompanyController.php,
    app/Http/Controllers/AdminController.php
  </files>
  <read_first>
    - Cada arquivo alvo listado nos itens (só o range afetado — usar offset/limit no Read; não re-ler o arquivo inteiro)
    - `app/Models/Company.php` (linhas 1-40 + accessors `getAdmanAccountIdAttribute`/`getMlStoreIdAttribute`) — necessário se algum fix envolver leitura de coluna que agora tem accessor Phase 57
  </read_first>
  <action>
    Para cada item da lista de trabalho gerada em Task 1 (loop item a item):

    (1) Aplicar o fix EXATO conforme classificado no AUDIT (não expandir escopo). Usar Edit para trocar o trecho original pelo trecho corrigido.
    (2) Se o fix envolver rename de método/variável: grep no repo inteiro (`grep -rn "nome_antigo" app/ resources/js/ tests/ database/`) antes do rename para listar todos os callers. Se aparecerem callers em arquivo fora dos 3 controllers listados em `files_modified`, PARAR e reabrir — expandir escopo requer revisão do usuário (potencial merge conflict com outra frente conforme Risk Summary CONTEXT §Risk).
    (3) Adicionar comentário pt-BR inline explicando o "porquê" da mudança em uma linha (formato: `// Phase 59 §fix — {motivo curto}`).
    (4) Rodar `php artisan test` filtrando o(s) módulo(s) afetado(s) primeiro (mais rápido; ex: `--filter=Comercial` se o fix foi em ComercialController). Se algum teste ficar vermelho, ANALISAR: era teste que dependia do naming antigo? Atualizar teste no MESMO commit. Se era teste que dependia da lógica antiga (ex: filtro `where('marketplace','meli')` era intencional para o teste), REVERTER fix — o AUDIT classificou errado, marcar item como "reclassificar" e seguir.
    (5) Commit atômico com mensagem no formato: `refactor(59-02): {arquivo}:{linha} — {tipo} — {resumo curto} pt-BR`.
    (6) Atualizar `59-AUDIT.md` na linha correspondente: mudar coluna Plano de `fix Phase 59` para `APLICADO em <sha_curto>`.

    Após todos os itens processados:
    - Rodar `php artisan test` completo (sem filtro) UMA vez para amostrar zero regressão (o gate rigoroso fica no Plan 03, mas fail cedo economiza tempo).
    - Se full suite tiver algum vermelho novo que não estava no baseline: reabrir com nota antes do commit final.

    Regra dura: `git diff --name-only HEAD~N HEAD` (onde N = quantidade de commits desta plan) deve mostrar APENAS arquivos em `files_modified` do frontmatter. Se algum arquivo fora dessa lista aparecer, foi expansão de escopo — reverter.

    Comentários pt-BR em todo código novo/reescrito conforme CLAUDE.md.
  </action>
  <verify>
    <automated>git log --oneline --grep "refactor(59-02)" | wc -l</automated>
  </verify>
  <done>
    Para lista N > 0: N commits `refactor(59-02): ...` no histórico; cada arquivo em `files_modified` foi editado; AUDIT.md tem cada item movido para "APLICADO em <sha>"; `git diff --name-only HEAD~N HEAD` mostra APENAS arquivos em `files_modified` (nada fora dessa whitelist).
  </done>
</task>

<task type="auto">
  <name>Task 3: Documentar Plan 02 no AUDIT + prep gate para Plan 03</name>
  <files>.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</files>
  <read_first>
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md` (verificar estado atual das linhas atualizadas em Task 2)
  </read_first>
  <action>
    Adicionar seção final no `59-AUDIT.md`: "## Plan 02 — Execução (status)"

    Conteúdo:
    - Data ISO de execução
    - Contagem literal: `N itens da lista "Itens a corrigir" foram processados; M aplicados; K reclassificados; L pulados por drift`
    - Se N == 0 (no-op consciente): registrar explícito "Plan 02 fechou como no-op — AUDIT §Itens a corrigir vazia. CROSS-01 fecha só com o mapeamento (Plan 01). Zero risco de regressão."
    - Lista literal dos commits gerados: `- refactor(59-02): ... — <sha>`
    - Nota final: "Gate de regressão delegado ao Plan 03 (roda `php artisan test` completo e compara com baseline em §Baseline pré-fix)"

    Se Task 2 foi pulada (N == 0), atualizar AUDIT com esta seção e commitar `docs(59-02): Plan 02 fecha como no-op — AUDIT classificou 0 fixes cirúrgicos`.

    Comentários pt-BR.
  </action>
  <verify>
    <automated>grep -c "Plan 02 — Execução" .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</automated>
  </verify>
  <done>
    AUDIT.md ganhou seção "## Plan 02 — Execução (status)" com contagens literais e lista de commits. Plan 03 tem input claro (número de fixes aplicados + link para commits) para o gate de regressão.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edição de controller transversal | mudança em ComercialController/CompanyController/AdminController impacta rotas de cadastro, fechamento e admin — surface amplo |
| rename de método público | pode quebrar callers em JSX/tests/docs se grep for insuficiente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-02-01 | Elevation of Privilege | fix incorreto que remove `where('marketplace','meli')` que era gate de dados | mitigate | CONTEXT §Security explicitou o risco "Data exposure via generalização de filter"; AUDIT.md em Plan 01 classifica cada linha com IMPACTO antes de plano; Task 2 §(4) exige rodar suite filtrada e analisar teste vermelho — se filtro era intencional, reverter |
| T-59-02-02 | Tampering | rename que quebra caller externo (JSX/teste/doc) silenciosamente | mitigate | Task 2 §(2) exige grep completo do repo ANTES do rename; se caller aparecer fora da whitelist `files_modified`, PARAR e reabrir com usuário — nunca expandir escopo cego |
| T-59-02-03 | Denial of Service | fix quebra rota /companies ou /comercial em prod | mitigate | Task 2 §(4) roda testes por módulo antes de commitar; Plan 03 roda suite completa como gate final antes do usuário decidir deploy; se algum teste ficar vermelho no full run, Plan 02 NÃO é considerado fechado até resolvido |
| T-59-02-04 | Tampering | AUDIT.md desatualizado após fixes (perda de rastreabilidade CROSS-01) | mitigate | Task 2 §(6) atualiza AUDIT linha a linha; Task 3 adiciona seção de execução com contagens e commits — histórico completo do que foi feito fica no MD |
</threat_model>

<verification>
- `git log --oneline --grep "refactor(59-02)" | wc -l` >= N (onde N = itens da lista "Itens a corrigir" — pode ser 0 se AUDIT no-op)
- `git diff --name-only HEAD~N HEAD` mostra APENAS arquivos em `files_modified` deste frontmatter (nunca fora)
- `grep -c "APLICADO em" .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md` == N (cada item aplicado teve linha atualizada)
- `grep -c "Plan 02 — Execução" .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md` == 1
- `php artisan test` completo ainda verde (rigor final fica no Plan 03; aqui é smoke)
- `php artisan test --filter=Phase57` continua 20/20
- `php artisan test --filter=Phase58` continua 16/16
</verification>

<success_criteria>
- Todos os itens `fix Phase 59` do AUDIT foram processados (aplicados, reclassificados ou pulados por drift — cada um com resolução explícita)
- AUDIT.md atualizado como fonte única de verdade sobre o que Plan 02 fez
- Zero item fora da whitelist tocado (sem expansão de escopo)
- Se AUDIT era vazia: Plan 02 fecha como no-op explícito e documentado (resultado válido)
- Plan 03 tem gate de regressão claro (baseline Plan 01 vs. estado atual)
</success_criteria>

<output>
Criar `.planning/phases/59-desacoplamento-de-reas-transversais/59-02-SUMMARY.md` ao concluir, contendo:
- Contagem literal: N itens processados, M aplicados, K reclassificados, L skipped
- Lista de commits: `refactor(59-02): ... — <sha>` (ou `docs(59-02)` se no-op)
- Arquivos tocados exatos (git diff --stat)
- Amostra de suite verde (contagem numérica pós-fix)
- Cross-ref: linha do AUDIT correspondente a cada commit
</output>
