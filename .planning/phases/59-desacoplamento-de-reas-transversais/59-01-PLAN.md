---
phase: 59-desacoplamento-de-reas-transversais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md
autonomous: true
requirements: [CROSS-01, CROSS-02]
tags: [audit, refactor, multi-marketplace, cross-cutting]

must_haves:
  truths:
    - "Baseline de testes verdes registrado antes de qualquer mudança (contagem numérica exata para comparar em Plan 03)"
    - "AUDIT.md existe e cobre os 3 controllers hotspot (ComercialController, CompanyController, AdminController)"
    - "AUDIT.md tem seção dedicada 'Publicação — CONFIRMED transversal' com evidências grep (arquivo:linha)"
    - "Cada linha da tabela AUDIT tem 5 colunas obrigatórias preenchidas (Linha, Trecho, Tipo, Severidade, Plano)"
    - "Sumário final classifica cada item em: fix Phase 59 vs. deferred v14+"
  artifacts:
    - path: ".planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md"
      provides: "Deliverable CROSS-01 — mapa exaustivo de acoplamento ML nas áreas transversais"
      contains: "## Comercial (ComercialController.php)"
      min_lines: 80
  key_links:
    - from: "59-AUDIT.md"
      to: "app/Http/Controllers/{Comercial,Company,Admin}Controller.php"
      via: "referência arquivo:linha em cada linha da tabela"
      pattern: "\\d+.*(marketplace|meli|mlb|Mlb|ml_store)"
    - from: "59-AUDIT.md §Publicação"
      to: "app/Models/User.php (hasPubPermission), app/Http/Middleware/EnsurePermission.php"
      via: "grep-based evidence (arquivo:linha) de que pub.* é transversal"
      pattern: "pub\\.|hasPubPermission|EnsurePermission"
---

<objective>
Produzir `59-AUDIT.md` — deliverable central do CROSS-01. Scout completo dos 3 controllers hotspot (Comercial, Company, Admin) + confirmação Publicação transversal via grep. Nenhuma mudança em código de produção. Captura o baseline `php artisan test` para gate de regressão do Plan 03.

Purpose: Toda a Phase 59 se apoia neste documento. Sem AUDIT.md classificado por linha (`fix Phase 59` vs. `deferred v14+`), Plan 02 (fixes) não tem lista concreta de trabalho e Plan 03 (regressão) não tem baseline pra comparar.

Output: Arquivo único `.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md` com 4 tabelas (Comercial/Company/Admin/Publicação) + metadata (baseline test count, data, comando grep usado) + sumário classificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/59-desacoplamento-de-reas-transversais/59-CONTEXT.md
@app/Http/Controllers/ComercialController.php
@app/Http/Controllers/CompanyController.php
@app/Http/Controllers/AdminController.php
@app/Models/User.php
@app/Http/Middleware/EnsurePermission.php

<interfaces>
Contexto de código já mapeado no scout inicial (2026-07-06) — NÃO precisa re-explorar codebase.

Refs conhecidas (grep `-E "marketplace|meli|mlb|Mlb|ml_store"`):

ComercialController.php (29 refs) — arquivo:linha:
- 10: use App\Models\MlbEmpresa
- 11: use App\Models\MlbImplementacao
- 42: docblock "mlb_empresas/mlb_implementacao"
- 342: 'ml_store_id' => $c->ml_store_id
- 491,495: docblock "mlb_empresas + mlb_implementacao"
- 528-529: validação marketplaces_extras (whitelist shopee/amazon/magalu/temu/tiktok)
- 540,542,544: guard duplicata em companies + mlb_empresas
- 566,572: serialização marketplaces_extras JSON
- 611,617,619,625,632: MlbEmpresa::create + criarImplementacaoPolo + branch por service_type
- 706-707: validação update marketplaces_extras
- 733: docblock "preserva mlb_empresas"
- 882-902: criarImplementacaoPolo (proxy para MlbImplementacaoFactory)

CompanyController.php (17 refs):
- 71,85,88: whereDoesntHave('mlbEmpresa') — filtro Performance-only Phase 37
- 125,398: marketplaces_extras exposto ao Inertia
- 129,131,165,268-269,402,494,509,545,547,660,711: adman_account_id / ml_store_id em payloads/lookups
- 522-523: validação marketplaces_extras
- 586,630: docblock referências a mlb_empresas nullOnDelete

AdminController.php (10 refs):
- 55,199,202-203,399,494,545,547,660,711: sempre em contexto de payload cust_id resolution (adman_account_id ?: ml_store_id) — RESOLUÇÃO LEGÍTIMA de accessor Company::cust_id

Publicação helper (transversal segundo CONTEXT §2, mas naming amarra a ML):
- app/Models/User.php:216-218 — `hasPubPermission(string $perm)` retorna `hasPermission("mlb.{$perm}")`
- Chave de permissão usa prefixo `mlb.` — candidato à classificação `Publicação — CONFIRMED transversal com naming amarrado a ML` (deferred v14+ documentação)
- EnsurePermission.php:14 — docblock exemplo `permission:mlb.vendas` — só docblock, não filtra por marketplace
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturar baseline de testes verdes antes de qualquer mudança</name>
  <files>.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</files>
  <read_first>
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-CONTEXT.md` (§Success Criteria — item 4/6 exigem contagem baseline)
    - `.planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-VERIFICATION.md` (score Phase 58 = 16/16; Phase 57 = 20/20)
  </read_first>
  <action>
    Rodar `php artisan test` completo (sem filtro). Capturar 3 valores exatos do output final: total de testes passados, total de assertions, tempo total. Comparar Phase 57 = 20 verdes e Phase 58 = 16 verdes (declarados em 58-VERIFICATION.md linha 49-50) e confirmar que ambas continuam verdes.

    Criar o arquivo `59-AUDIT.md` com header + seção "## Baseline pré-fix (Plan 01)" preenchida com data ISO, comando executado, total de tests passed, assertions, tempo, e destaque dos subtotais Phase57/Phase58 (números literais). Manter espaço para as demais seções que Task 2 vai preencher — não deixar o arquivo vazio abaixo do baseline.

    Se algum teste estiver vermelho no baseline, PARAR — não iniciar Task 2. Reportar quantos vermelhos e reabrir com o usuário (baseline vermelho invalida o gate de "zero regressão" no Plan 03). Comentários pt-BR em qualquer texto explicativo.
  </action>
  <verify>
    <automated>test -f .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md && grep -c "Baseline pré-fix" .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</automated>
  </verify>
  <done>
    Arquivo `59-AUDIT.md` criado com seção "## Baseline pré-fix (Plan 01)" contendo: data ISO 2026-07-XX, comando `php artisan test`, número total de tests passed (numeric), número de assertions, tempo em segundos. Phase 57 e Phase 58 confirmadas verdes (>=20 e >=16 respectivamente).
  </done>
</task>

<task type="auto">
  <name>Task 2: Scout completo + classificação por linha nos 3 controllers + seção Publicação transversal</name>
  <files>.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</files>
  <read_first>
    - `.planning/phases/59-desacoplamento-de-reas-transversais/59-CONTEXT.md` (§3 template AUDIT.md; §4 profundidade dos fixes; §Success Criteria)
    - `app/Http/Controllers/ComercialController.php` (29 refs — arquivo grande, ~900 linhas; ler alvo por range após grep)
    - `app/Http/Controllers/CompanyController.php` (17 refs — ~700 linhas)
    - `app/Http/Controllers/AdminController.php` (10 refs — ~800 linhas)
    - `app/Models/User.php` (linhas 210-230 — hasPubPermission + prefixo mlb.)
    - `app/Http/Middleware/EnsurePermission.php` (arquivo curto, ler inteiro)
  </read_first>
  <action>
    Preencher `59-AUDIT.md` com 4 seções (Comercial, Admin, Company, Publicação) + Sumário final, seguindo EXATAMENTE o template de CONTEXT §3.

    Estrutura obrigatória:

    (1) Antes das seções, adicionar "## Metodologia" com:
    - Comando grep exato usado: `grep -nE "marketplace|meli|mlb|Mlb|ml_store" app/Http/Controllers/{Comercial,Company,Admin}Controller.php`
    - Critério de classificação Tipo: "filtro hardcoded" | "default UI ML" | "naming" | "payload/accessor legítimo" | "import/use" | "docblock" | "validação whitelist"
    - Critério de Severidade: HIGH (bloqueia empresa não-ML), MEDIUM (naming ou default que confunde), LOW (docblock ou naming cosmético), INFO (código legítimo, sem ação)
    - Critério de Plano: `fix Phase 59` | `deferred v14+` | `no-op (legítimo)` | `docs only`

    (2) Uma tabela por controller com 5 colunas: Linha | Trecho (código curto sem code fence, entre backticks inline) | Tipo | Severidade | Plano. Ler cada linha listada no scout (refs conhecidas em <interfaces>) e classificar. Exemplos concretos esperados (para calibração — validar caso a caso lendo o código real):
    - ComercialController.php:342 `'ml_store_id' => $c->ml_store_id` → tipo="payload/accessor legítimo", severidade=INFO, plano="no-op (legítimo)" (é apenas exposição de coluna existente)
    - ComercialController.php:528-529 `Rule::in(['shopee','amazon',...])` → tipo="validação whitelist", severidade=INFO, plano="no-op (legítimo)" (já é generalizado)
    - CompanyController.php:71-88 `whereDoesntHave('mlbEmpresa')` → tipo="filtro hardcoded", severidade=depende — se filtro é ML-specific em query transversal, HIGH; se é filtro Performance-only Phase 37 conforme docblock, INFO+"no-op (legítimo)"
    - AdminController.php:55 `'ml_store_id' => $c->ml_store_id` → tipo="payload legítimo", severidade=INFO
    - AdminController.php:199-203 comentário sobre cache key = cust_id → tipo="docblock", severidade=INFO

    Ler o range de código real de cada linha antes de classificar — não classificar de cabeça. Docstrings/comentários pt-BR nas anotações do próprio AUDIT quando fizer sentido explicar o "porquê" da classificação.

    (3) Seção "## Publicação — CONFIRMED transversal" com evidências grep:
    - `app/Models/User.php:216-218` — hasPubPermission delega para hasPermission("mlb.{$perm}") — flag como "naming amarrado a ML apesar do domínio ser transversal"; classificar como severidade=LOW plano="deferred v14+" (renomear prefixo `mlb.` → `pub.` afeta permission_key gravadas em banco, é migração; fora do escopo cirúrgico Phase 59)
    - `app/Http/Middleware/EnsurePermission.php` — validar via leitura completa que o middleware NÃO tem `where('marketplace', ...)` nem filtra por ML; se confirmado, documentar como "middleware é agnóstico de marketplace"
    - Executar grep adicional `grep -rn "pub\." app/Http/Controllers/ | head -30` para amostrar refs a permissões pub.*; anotar 3-5 exemplos representativos com arquivo:linha
    - Concluir a seção com afirmativa: "Permissões `pub.*` (via `hasPubPermission`) são checadas sem contexto de marketplace; middleware `EnsurePermission` valida chave literal contra `user_setores → cargos → permissoes`; nenhuma tela de Publicação exige `adman_account_id`."

    (4) Sumário final:
    - Totais por Severidade (HIGH: X, MEDIUM: X, LOW: X, INFO: X) somando os 3 controllers
    - Totais por Plano (fix Phase 59: X, deferred v14+: X, no-op legítimo: X, docs only: X)
    - Lista explícita "## Itens a corrigir no Plan 02" com bullet por item (formato `- ComercialController.php:LINHA — descrição — plano fix`) — esta lista é a INPUT direta do Plan 02.
    - Se lista "Itens a corrigir" estiver vazia (todos os 56 refs foram classificados INFO/legítimo/deferred), documentar isso explicitamente: "Nenhum fix cirúrgico necessário — CROSS-01 fecha só com o mapa; Plan 02 vira no-op documentado; Plan 03 confirma regressão baseline." Este é um resultado válido dado que o escopo do CONTEXT §4 é conservador.

    NÃO tocar em código de produção nesta task. Se durante a leitura for tentador editar algo, resistir — pertence ao Plan 02.

    Comentários em pt-BR conforme CLAUDE.md.
  </action>
  <verify>
    <automated>grep -c "^| " .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md</automated>
  </verify>
  <done>
    `59-AUDIT.md` contém: seção Metodologia + 3 tabelas de controllers (Comercial/Company/Admin) com >=5 colunas por linha da tabela + seção "Publicação — CONFIRMED transversal" com >=3 evidências grep + Sumário com totais por Severidade e Plano + lista "Itens a corrigir no Plan 02" (mesmo que vazia, deve estar explícita). Zero mudança em código de produção (git diff --stat mostra apenas 59-AUDIT.md).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leitura de código-fonte | scout via Read/Grep, sem execução — sem risco de injeção |
| execução de `php artisan test` | teste roda contra SQLite in-memory (RefreshDatabase); sem side-effect em prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01-01 | Information Disclosure | `59-AUDIT.md` | mitigate | AUDIT.md fica em `.planning/` (repo interno); trechos citados são arquivo:linha públicos (não incluem credenciais, tokens ou dados de cliente). Validar em Task 2 que nenhum trecho colado no MD contém string com `_KEY`, `_TOKEN`, `password`, `secret`. |
| T-59-01-02 | Tampering | classificação errada de "no-op legítimo" em ref que era acoplamento real | mitigate | Task 2 obriga leitura do código real antes de classificar; CONTEXT §Risk `Média` já registra este risco; Plan 02 usa a lista "Itens a corrigir" como input rígido — se AUDIT errar pra menos, o gap volta na próxima verify |
| T-59-01-03 | Repudiation | AUDIT sem baseline de testes | mitigate | Task 1 obriga baseline com contagem literal antes de Task 2 escrever qualquer classificação — sem baseline, Plan 03 não tem gate de regressão |
</threat_model>

<verification>
- `test -f .planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md` retorna 0 (arquivo existe)
- `grep -c "Baseline pré-fix" 59-AUDIT.md` >= 1
- `grep -c "^## Comercial\|^## Company\|^## Admin\|^## Publicação" 59-AUDIT.md` >= 4 (4 seções)
- `grep -cE "^\| [0-9]+ \|" 59-AUDIT.md` >= 20 (ao menos 20 linhas de tabela cobrindo os 56 refs conhecidos após consolidação — número exato depende da classificação)
- `grep -c "Itens a corrigir no Plan 02" 59-AUDIT.md` == 1
- `git diff --name-only` mostra APENAS `.planning/phases/59-desacoplamento-de-reas-transversais/59-AUDIT.md` como modificado (zero controllers/services/views tocados)
- `php artisan test --filter=Phase57` continua 20/20 (Task 1 já provou; grep aqui é redundância defensiva)
- `php artisan test --filter=Phase58` continua 16/16
</verification>

<success_criteria>
- `59-AUDIT.md` criado, populado e commitado (via `/gsd:complete-plan`)
- CROSS-01 tem deliverable concreto (mapeamento por arquivo:linha classificado)
- CROSS-02 tem seção dedicada com evidências grep (fecha metade — confirmação via regressão em Plan 03)
- Baseline de testes registrado como número exato para comparação no Plan 03
- Lista "Itens a corrigir no Plan 02" é a input rígida do próximo plano — Plan 02 não vai improvisar escopo
- Zero mudança em código de produção nesta plan
</success_criteria>

<output>
Criar `.planning/phases/59-desacoplamento-de-reas-transversais/59-01-SUMMARY.md` ao concluir, incluindo:
- Contagens finais do AUDIT (total refs mapeados, distribuição por Severidade, distribuição por Plano)
- Baseline de testes exato (número de passes, assertions, tempo)
- Lista literal "Itens a corrigir no Plan 02" (pode ser vazia — documentar como tal)
- Timestamp de finalização
</output>
