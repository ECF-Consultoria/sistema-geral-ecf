---
phase: 53-inteligencia-detector-sugadores
plan: "03"
type: execute
wave: 3
depends_on: ["53-01", "53-02"]
autonomous: false
requirements:
  - REQ-53-03
files_modified:
  - .planning/phases/53-inteligencia-detector-sugadores/53-03-UAT.md
  - .planning/phases/53-inteligencia-detector-sugadores/PHASE-SUMMARY.md
must_haves:
  truths:
    - "Deploy da Phase 53 (Waves 1+2) em prod concluído com estratégia stash → deploy.sh → pop (outro dev em paralelo — memory `feedback_perguntar_antes_deploy_v9`)"
    - "Cache limpo em prod após deploy (`php artisan cache:clear` — memory `project_ecf_drive_cache_stale`) para forçar novo cálculo com filtros ativos"
    - "Dry-run em CAMILLO PARTS (company_id=1) NÃO gera sugador para adgroup 1902557017 (MLB6258261358 pausado) — fix B1 confirmado"
    - "Dry-run em BARAOSHOP VARIEDADES (company_id=229) NÃO gera sugador para adgroup 496843010 (MLB3759543315 com 781 vendas globais FULL) — fix B2 confirmado"
    - "Dry-run em DINMAP (company_id=319) NÃO gera sugador para adgroup 1784220962 (MLB4359551777 com 6 vendas globais + 4 orders) — fix B3 confirmado"
    - "Análise diária pós-deploy em ByMobille (empresa de referência histórica) segue detectando sugadores legítimos (adgroups sem venda global e sem sinais adicionais) — regressão negativa"
    - "Contagem de campaign_name populados sobe de 0 (baseline pré-fix) para >0 no lote de sugadores criados na análise diária pós-deploy (efeito colateral do cache listCampaigns)"
    - "Log de prod NÃO mostra novos erros de expose de token/access_token nos avisos de fail-open (T-39-02-01)"
    - "PHASE-SUMMARY.md consolidado documenta os 3 fixes + descoberta sistêmica + métricas antes/depois"
  artifacts:
    - path: ".planning/phases/53-inteligencia-detector-sugadores/53-03-UAT.md"
      provides: "checklist UAT rodado em prod com evidências (queries tinker, logs, contagens)"
      contains: "B1 CAMILLO"
    - path: ".planning/phases/53-inteligencia-detector-sugadores/PHASE-SUMMARY.md"
      provides: "consolidação da phase: fixes aplicados, testes verdes, métricas prod antes/depois, débitos técnicos abertos"
      contains: "Phase 53 — UAT aprovado"
  key_links:
    - from: "deploy prod"
      to: "cache clear"
      via: "SSH root@177.7.53.164 cd /var/www/ecf_admin && php artisan cache:clear"
      pattern: "cache:clear"
    - from: "dry-run em prod"
      to: "evidência de skip"
      via: "php artisan sugadores:analyze --company={id} --dry-run + inspecionar detalhes"
      pattern: "sugadores:analyze --company="
    - from: "baseline"
      to: "comparação"
      via: "COUNT sugadores pendentes empresa X antes vs depois; COUNT campaign_name IS NOT NULL"
      pattern: "SELECT COUNT"
---

<objective>
Fechar a Phase 53 com UAT em prod dos 3 casos B1/B2/B3 e da descoberta sistêmica (cache listCampaigns). Deploy exige coordenação (outro dev em paralelo nesta milestone — memory `feedback_perguntar_antes_deploy_v9`), então esta wave é **explícitamente não-autônoma**: pergunta antes de rodar `deploy.sh`, valida com queries em tinker e fecha com PHASE-SUMMARY.

**Objetivos operacionais:**

1. Deploy Waves 1+2 em prod sem quebrar trabalho do outro dev (stash → deploy → pop).
2. Cache clear em prod para forçar recomputo com cache novo de listCampaigns + fetchItemStatus.
3. Rodar dry-run em CAMILLO/BARAOSHOP/DINMAP e coletar evidência de que os 3 adgroups reais NÃO viram sugador.
4. Regressão em ByMobille (histórico) — detector segue funcional para casos legítimos.
5. Coletar métrica antes/depois de `campaign_name` populados (efeito colateral do cache).
6. PHASE-SUMMARY consolidado + fecha o TODO bloco B do briefing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-inteligencia-detector-sugadores/53-CONTEXT.md
@.planning/phases/53-inteligencia-detector-sugadores/53-RESEARCH.md
@.planning/phases/53-inteligencia-detector-sugadores/53-01-PLAN.md
@.planning/phases/53-inteligencia-detector-sugadores/53-02-PLAN.md
@.planning/todos/pending/270701-melhorias-sugadores-ui-ux-e-detector.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 1: Autorização + deploy Waves 1+2 em prod</name>
  <what-built>
    - Wave 1 (53-01): cache listCampaigns TTL 10min + fetchItemStatus + filtro MLB paused/closed/under_review
    - Wave 2 (53-02): chave `sold_global` propagada + filtro `gasto_sem_venda` removido quando venda orgânica >=10
    - 17 testes verdes na suite `MercadoLivreSugadoresProviderFilterTest`
    - Regressão Phase 41/42 verde
  </what-built>
  <how-to-verify>
    Contexto crítico (memory `feedback_perguntar_antes_deploy_v9`): outro dev está em paralelo nesta milestone. NÃO rodar `deploy.sh` sem confirmar caso-a-caso.

    Passos (usuário confirma cada bloco):

    1. Confirmar que branch local tem SOMENTE os commits das Waves 1+2 (`git log --oneline -10` — checar que aparecem `test(53-01)`, `feat(53-01)`, `test(53-02)`, `feat(53-02)` e nada não relacionado como topo do branch).
    2. Confirmar que WIP não relacionado ao Phase 53 está em stash ou em outra branch:
       ```bash
       git status
       git stash list
       ```
    3. **Perguntar ao usuário:** "Confirma deploy Phase 53 waves 1+2 agora? Outro dev pode ter WIP em prod — precisa alinhamento?"
    4. Se ok:
       ```bash
       # No local (VS Code / bash)
       ./deploy.sh
       ```
       (Ou `./deploy_parcial.sh` se o usuário preferir — perguntar.)
    5. Após deploy, SSH em prod e limpar cache (memory `project_ecf_drive_cache_stale` — cache mascara fix de dados):
       ```bash
       ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan cache:clear && php artisan config:clear"
       ```
    6. Verificar supervisor:
       ```bash
       ssh root@177.7.53.164 "supervisorctl status ecf-worker:*"
       ```
       Todos os workers devem estar RUNNING.
    7. Smoke rápido: `ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan tinker --execute='echo App\Models\Sugador::count();'"` — retorna número; app não crashou.
  </how-to-verify>
  <resume-signal>Type "deployed" quando deploy + cache clear + workers OK. Ou "aborted" com motivo.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 2: UAT B1 CAMILLO PARTS (adgroup 1902557017 pausado no ML)</name>
  <what-built>
    Filtro Wave 1 — `fetchItemStatus` consulta status do MLB e skipa `paused/closed/under_review`.
  </what-built>
  <how-to-verify>
    SSH em prod:
    ```bash
    ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan tinker"
    ```

    Dentro do tinker:
    ```php
    // 1) Confirmar que o MLB continua paused no ML (baseline)
    $c = App\Models\Company::find(1); // CAMILLO PARTS MATRIZ
    $ml = app(App\Services\MercadoLivreService::class);
    $status = $ml->fetchItemStatus($c, 'MLB6258261358');
    echo json_encode($status);
    // Esperado: {"status":"paused","sub_status":["paused_by_seller"],...}

    // 2) Rodar dry-run pra hoje
    exit;
    ```

    Fora do tinker:
    ```bash
    ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan sugadores:analyze --company=1 --dry-run 2>&1 | tee /tmp/uat-b1.log"
    ```

    Verificar no output:
    - Empresa 1 analisada
    - `adgroups_flagados` — comparar com contagem baseline pré-fix
    - Adgroup 1902557017 NÃO aparece na lista de detalhes

    Query de confirmação:
    ```bash
    ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan tinker --execute='
    \$hoje = now()->toDateString();
    \$s = App\Models\Sugador::where(\"company_id\", 1)
        ->where(\"reference_date\", \$hoje)
        ->where(\"adgroup_id\", \"1902557017\")
        ->first();
    echo \$s ? \"AINDA VIRA SUGADOR — fix nao funcionou\" : \"OK — nao vira sugador\";
    '"
    ```

    (Se dry-run não persiste, verificar após próxima análise agendada — cron 3h da manhã. Alternativa: rodar sem `--dry-run` mas em empresa de menor risco. CAMILLO tem alto volume, dry-run é seguro.)
  </how-to-verify>
  <resume-signal>Type "b1-ok" se o adgroup 1902557017 NÃO aparece mais como sugador. Ou "b1-fail" com output do dry-run.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 3: UAT B2 BARAOSHOP (adgroup 496843010 com 781 vendas globais FULL)</name>
  <what-built>
    Filtro Wave 2 — `sold_global >= 10` remove motivo `gasto_sem_venda` (produto vende organicamente).
  </what-built>
  <how-to-verify>
    SSH em prod, mesmo pattern:
    ```bash
    ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan tinker --execute='
    \$c = App\Models\Company::find(229); // BARAOSHOP VARIEDADES
    \$ml = app(App\Services\MercadoLivreService::class);
    \$status = \$ml->fetchItemStatus(\$c, \"MLB3759543315\");
    echo json_encode(\$status);
    '"
    ```
    Esperado: `sold_quantity` >= 10 (pelo research, 781 em prod).

    Dry-run:
    ```bash
    ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan sugadores:analyze --company=229 --dry-run 2>&1 | tee /tmp/uat-b2.log"
    ```

    Verificar:
    - Adgroup 496843010 NÃO aparece nos detalhes flagados
    - Se aparece em `detalhes[]` mas com motivos que NÃO incluem `gasto_sem_venda`, aceitar (fix é seletivo — só remove aquele motivo)

    Query:
    ```bash
    ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan tinker --execute='
    \$hoje = now()->toDateString();
    \$s = App\Models\Sugador::where(\"company_id\", 229)
        ->where(\"reference_date\", \$hoje)
        ->where(\"adgroup_id\", \"496843010\")
        ->first();
    if (!\$s) { echo \"OK — nao vira sugador\"; }
    else { echo \"AINDA VIRA — motivos: \" . json_encode(\$s->motivos); }
    '"
    ```
  </how-to-verify>
  <resume-signal>Type "b2-ok" se o adgroup 496843010 saiu como sugador (ou perdeu o motivo `gasto_sem_venda`). Ou "b2-fail" com output.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 4: UAT B3 DINMAP (adgroup 1784220962 com 4 orders + mismatch mlb_id)</name>
  <what-built>
    Filtro Wave 2 (`sold_global >= 10` — vale para B3 também se sold_quantity global >= 10; se for menor, provavelmente permanece flagged mas com motivo legítimo). Comentário do mismatch B3 documentado.
  </what-built>
  <how-to-verify>
    ```bash
    ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan tinker --execute='
    \$c = App\Models\Company::find(319); // DINMAP
    \$ml = app(App\Services\MercadoLivreService::class);
    // Research diz que o mismatch é MLB4359551779 (sugador) vs MLB4359551777 (raw_data). Testamos ambos.
    echo \"MLB4359551779 (sugador.mlb_id): \" . json_encode(\$ml->fetchItemStatus(\$c, \"MLB4359551779\")) . PHP_EOL;
    echo \"MLB4359551777 (raw_data.item_id — fonte-verdade): \" . json_encode(\$ml->fetchItemStatus(\$c, \"MLB4359551777\"));
    '"
    ```

    Esperado (research): MLB4359551777 → sold_quantity=6, MLB4359551779 → possivelmente 404 ou dados divergentes.

    ```bash
    ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan sugadores:analyze --company=319 --dry-run 2>&1 | tee /tmp/uat-b3.log"
    ```

    **Nota:** research §Caso B3 diz que sold_global=6 < 10 → **B3 pode continuar flagged** por gasto_sem_venda. Isso é ESPERADO com threshold LOCKED em 10. Se o operador não concordar, é oportunidade de discutir se threshold=5 ou threshold configurável entra em phase futura (research §Assumption A1). NÃO fazer hotfix nesta phase — documentar como observação no UAT.md.

    Query:
    ```bash
    ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan tinker --execute='
    \$hoje = now()->toDateString();
    \$s = App\Models\Sugador::where(\"company_id\", 319)
        ->where(\"reference_date\", \$hoje)
        ->where(\"adgroup_id\", \"1784220962\")
        ->first();
    if (!\$s) { echo \"OK — nao vira sugador\"; }
    else { echo \"AINDA VIRA — motivos: \" . json_encode(\$s->motivos) . \" | mlb_id: \" . \$s->mlb_id . \" | raw item_id: \" . (\$s->raw_data[\"item_id\"] ?? \"null\"); }
    '"
    ```
  </how-to-verify>
  <resume-signal>Type "b3-ok" (ou "b3-partial" se sold_global<10 e operador aceita threshold=10) ou "b3-fail" com output. Se b3-partial, incluir nota no UAT.md sobre threshold configurável.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 5: Regressão ByMobille + métricas sistêmicas (cache listCampaigns)</name>
  <what-built>
    Regressão negativa: fix não pode quebrar detector legítimo.
    Efeito sistêmico esperado: `campaign_name` populado > 0 (era 0/183 antes do cache).
  </what-built>
  <how-to-verify>
    1. **Regressão ByMobille** (empresa histórica sem falso-positivo conhecido):
       ```bash
       ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan tinker --execute='
       \$hojeCount = App\Models\Sugador::whereDate(\"reference_date\", now()->toDateString())
           ->whereHas(\"company\", fn(\$q) => \$q->where(\"name\", \"like\", \"%Bymobille%\"))
           ->count();
       echo \"ByMobille sugadores hoje: \" . \$hojeCount;
       '"
       ```
       Comparar com contagem histórica (perguntar ao usuário se número é razoável — nada de "0 sugadores" repentino ou "10x mais").

    2. **Métrica cache listCampaigns** — validar que campaign_name está sendo populado:
       ```bash
       ssh root@177.7.53.164 "cd /var/www/ecf_admin && php artisan tinker --execute='
       \$hoje = now()->toDateString();
       \$total = App\Models\Sugador::where(\"reference_date\", \$hoje)->count();
       \$comNome = App\Models\Sugador::where(\"reference_date\", \$hoje)->whereNotNull(\"campaign_name\")->count();
       echo \"total: \$total | com campaign_name: \$comNome | pct: \" . round(\$comNome/max(\$total,1)*100) . \"%\";
       '"
       ```
       **Baseline pré-fix (research):** 0/183 = 0%. **Esperado pós-fix:** >50% (só cai a 0% se listCampaigns falhar em TODAS as empresas, indicando cache não funcional).

    3. **Log de segredos** (T-39-02-01 anti-leak):
       ```bash
       ssh root@177.7.53.164 "grep -i 'access_token\\|refresh_token\\|Bearer ' /var/www/ecf_admin/storage/logs/laravel.log | tail -10"
       ```
       Deve retornar **vazio** (ou apenas ocorrências antigas). Nenhuma linha nova com token exposto.

    4. **Rate-limit ML residual**:
       ```bash
       ssh root@177.7.53.164 "grep -c 'Rate limit ML' /var/www/ecf_admin/storage/logs/laravel.log"
       ```
       Comparar com contagem de ontem — deve reduzir dramaticamente (cache 10min elimina duplicatas).
  </how-to-verify>
  <resume-signal>Type "regressao-ok" com números (ByMobille count, %campaign_name, rate-limit count antes/depois). Ou "regressao-fail" com o que quebrou.</resume-signal>
</task>

<task type="auto">
  <name>Tarefa 6: Escrever 53-03-UAT.md + PHASE-SUMMARY.md + commit final</name>
  <files>
    - .planning/phases/53-inteligencia-detector-sugadores/53-03-UAT.md
    - .planning/phases/53-inteligencia-detector-sugadores/PHASE-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/54-04-UAT.md — template UAT recente
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/PHASE-SUMMARY.md — template consolidado (se existir; senão espelhar phases 52/46)
  </read_first>
  <action>
    1. **Escrever 53-03-UAT.md** consolidando as evidências das Tarefas 2-5:
       - Data + hora do deploy
       - Baseline pré-fix (0/183 campaign_name populado; 3 casos flagged)
       - B1 CAMILLO: status MLB retornado (`paused`), adgroup 1902557017 skipado (ou motivos)
       - B2 BARAOSHOP: sold_global=781, motivo `gasto_sem_venda` removido, adgroup 496843010 não persiste (ou não tem outros motivos)
       - B3 DINMAP: sold_global=6, resultado (skipado se ≥10 = FALSE, então mantém — flag como observação de threshold)
       - Regressão ByMobille: N sugadores hoje (valor esperado)
       - Métrica sistêmica: `campaign_name` populado passa de 0% para X%
       - Rate-limit ML residual: Y ocorrências (comparado a Z de ontem)
       - Segurança: nenhum token exposto em logs
    2. **Escrever PHASE-SUMMARY.md**:
       - Objetivo Phase 53 (reduzir falso-positivo em 3 casos + descoberta sistêmica)
       - Waves entregues (1, 2, 3) com REQ-IDs (REQ-53-01, REQ-53-02, REQ-53-03)
       - Testes: 17 verdes na suite `MercadoLivreSugadoresProviderFilterTest`; regressão Phase 41/42 verde
       - Arquivos modificados: `MercadoLivreAdsService.php`, `MercadoLivreService.php`, `MercadoLivreSugadoresProvider.php`, `SugadorAnalysisService.php`, arquivo de teste
       - Débitos técnicos abertos:
         - Threshold `sold_global >= 10` LOCKED — configurável via `SugadorConfig` é feature futura (research §Assumption A1)
         - Sync legacy Adman com `adgroup_id=NULL` em BARAOSHOP (1600 rows) — abrir quick task separada (research §Open Question 2)
         - Bulk-update retroativo de sugadores herdados com mismatch mlb_id — DEFERRED (research §Open Question 1)
         - Alerta operacional de fail-open `campaign_name = NULL` — Phase 53 mitiga com cache; se recorrer, considerar warn→error no MercadoLivreSugadoresProvider (research §Descoberta sistêmica)
       - Fecha o TODO bloco B do briefing `.planning/todos/pending/270701-melhorias-sugadores-ui-ux-e-detector.md` — mover para `.planning/todos/done/`
    3. **Fechar TODO:** `mv .planning/todos/pending/270701-melhorias-sugadores-ui-ux-e-detector.md .planning/todos/done/`
    4. **Atualizar ROADMAP.md:** marcar Phase 53 como `[x]` (checked) na linha 136.
    5. **Commit final:**
       ```bash
       git add .planning/phases/53-inteligencia-detector-sugadores/ .planning/ROADMAP.md .planning/todos/
       git commit -m "docs(53): fecha Phase 53 — UAT aprovado em prod, 3 casos B1/B2/B3 resolvidos + cache listCampaigns"
       ```
    6. **NÃO push** — usuário decide quando fazer `git push` (memory `feedback_perguntar_antes_deploy_v9`).
  </action>
  <verify>
    <automated>test -f .planning/phases/53-inteligencia-detector-sugadores/53-03-UAT.md && test -f .planning/phases/53-inteligencia-detector-sugadores/PHASE-SUMMARY.md && grep -q "Phase 53 — UAT aprovado" .planning/phases/53-inteligencia-detector-sugadores/PHASE-SUMMARY.md</automated>
  </verify>
  <done>UAT.md e PHASE-SUMMARY.md gravados com evidências reais coletadas nas tarefas 2-5. ROADMAP marcado. TODO movido para done. Commit registrado (não pushed).</done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/53-inteligencia-detector-sugadores/53-03-UAT.md` — arquivo existe
2. `test -f .planning/phases/53-inteligencia-detector-sugadores/PHASE-SUMMARY.md` — arquivo existe
3. `grep -q "\[x\].*Phase 53" .planning/ROADMAP.md` — phase marcada como checked
4. `test -f .planning/todos/done/270701-melhorias-sugadores-ui-ux-e-detector.md` — TODO movido
5. `git log --oneline -5 | head -1 | grep -q "docs(53)"` — commit final registrado
</verification>

<success_criteria>
- [ ] Deploy autorizado explicitamente pelo usuário (memory `feedback_perguntar_antes_deploy_v9` respeitada)
- [ ] Cache clear em prod executado após deploy
- [ ] Workers supervisor RUNNING pós-deploy
- [ ] B1 CAMILLO: adgroup 1902557017 confirmado skipado (ou motivos justificam)
- [ ] B2 BARAOSHOP: adgroup 496843010 confirmado sem motivo `gasto_sem_venda`
- [ ] B3 DINMAP: comportamento documentado (skipado se sold_global≥10 OU flagged com observação de threshold)
- [ ] ByMobille segue detectando sugadores (regressão negativa)
- [ ] Métrica `campaign_name` populado > 0% (baseline: 0/183 = 0%)
- [ ] Nenhum token/access_token exposto em logs novos
- [ ] 53-03-UAT.md + PHASE-SUMMARY.md gravados com evidências
- [ ] ROADMAP marcado + TODO movido para done
- [ ] Commit final registrado (não pushed)
</success_criteria>

<output>
Após tudo verde, reportar ao usuário:
- Phase 53 fechada
- 3 casos B1/B2/B3 resolvidos (ou B3 com nota de threshold)
- Métrica sistêmica: campaign_name populado passa de 0% para X%
- Débitos técnicos abertos listados no PHASE-SUMMARY
- Sugerir próximos passos:
  - `git push` quando alinhado com outro dev
  - Nova quick task para sync legacy Adman `adgroup_id=NULL` (BARAOSHOP)
  - Consideração futura: `sold_global` threshold configurável no `SugadorConfig`
</output>
