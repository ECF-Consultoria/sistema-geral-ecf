---
phase: 53-inteligencia-detector-sugadores
plan: "02"
type: execute
wave: 2
depends_on: ["53-01"]
autonomous: true
requirements:
  - REQ-53-02
files_modified:
  - app/Services/Sugadores/MercadoLivreSugadoresProvider.php
  - app/Services/SugadorAnalysisService.php
  - tests/Unit/Services/Sugadores/MercadoLivreSugadoresProviderFilterTest.php
must_haves:
  truths:
    - "Provider ML propaga a chave `sold_global` (int|null) em cada adgroup do payload — igual a `mlb_sold_quantity_global` já exposta na Wave 1 (Wave 2 apenas garante nome canônico usado pelo service)"
    - "`SugadorAnalysisService::evaluateMetrics` recebe um novo sinal composto: se motivo `gasto_sem_venda` bate MAS `sold_global >= 10`, o motivo é REMOVIDO da lista final de motivos"
    - "Se remover `gasto_sem_venda` deixa a lista de motivos vazia → o adgroup NÃO é sugador (não persiste)"
    - "Se o adgroup também bate outros motivos (ex: `cpc_alto`, `cliques_sem_conversao`, `acos_alto`), esses são preservados — só o motivo `gasto_sem_venda` é filtrado"
    - "Threshold `10` LOCKED (research §Assumptions A1) — hardcoded no evaluateMetrics, sem passar por `SugadorConfig`"
    - "Se o mismatch B3 acontecer (sugador.mlb_id ≠ raw_data.item_id), o provider prioriza `raw_data.item_id` como fonte-verdade para `fetchItemStatus` (documentado em comentário — evita persistência de status de MLB errado)"
    - "Adgroups com `sold_global = null` (fail-open da Wave 1) NÃO ativam o filtro — comportamento atual preservado (evita falso-negativo em caso de instabilidade ML)"
    - "Cada teste RED (failing) tem commit separado do GREEN (implementação) — padrão TDD Phase 52/54"
  artifacts:
    - path: "app/Services/Sugadores/MercadoLivreSugadoresProvider.php"
      provides: "chave `sold_global` propagada no payload de cada adgroup + comentário do mismatch mlb_id/item_id (B3)"
      contains: "'sold_global'"
    - path: "app/Services/SugadorAnalysisService.php"
      provides: "evaluateMetrics remove motivo gasto_sem_venda quando sold_global>=10 (venda orgânica)"
      contains: "sold_global"
    - path: "tests/Unit/Services/Sugadores/MercadoLivreSugadoresProviderFilterTest.php"
      provides: "5 testes novos adicionados: sold_global=0 mantém hit, sold_global>=10 skipa gasto_sem_venda, sold_global<10 mantém hit, outros motivos preservados, mismatch mlb_id usa raw_data.item_id"
  key_links:
    - from: "MercadoLivreSugadoresProvider::fetchAdgroupsMetrics"
      to: "chave `sold_global` no payload"
      via: "renomeio/alias de `mlb_sold_quantity_global` da Wave 1 para `sold_global` (nome canônico que evaluateMetrics vai ler)"
      pattern: "'sold_global' => \\$mlbStatusInfo\\['sold_quantity'\\]"
    - from: "SugadorAnalysisService::evaluateMetrics"
      to: "filtro composto sold_global>=10"
      via: "após computar `$motivos`, remover 'gasto_sem_venda' se sold_global >= 10"
      pattern: "SugadorAnalysisService.php:543-628 (evaluateMetrics)"
    - from: "raw_data.item_id (mismatch B3)"
      to: "fetchItemStatus"
      via: "priorizar raw_data.item_id ao invés de sugador.mlb_id herdado de sync legacy"
      pattern: "\\$mlbId = \\$r\\['item_id'\\] ?? null (já é assim na Wave 1)"
---

<objective>
Fixar 2 vetores de FALSO-POSITIVO por **venda orgânica** confirmados em prod:

1. **B2 BARAOSHOP VARIEDADES (research §Caso B2 confirmado):** MLB3759543315 tem **781 vendas globais** e é FULL. Payload ads reporta 0 vendas atribuídas porque Ads API só conta atribuição direta ao ads patrocinado — cliente vende via busca orgânica no FULL. Detector flagged como sugador ("gastou R$ 12,22 sem vender"), mas o produto vende MUITO organicamente.
2. **B3 DINMAP (research §Caso B3 confirmado):** MLB4359551777 tem 6 vendas globais + 4 orders confirmados nos últimos 30d. Mesmo padrão — atribuição ads=0, produto vende orgânico. Também confirmada descoberta colateral: `sugador.mlb_id` diverge de `raw_data.item_id` em 1 caractere (herança de sync antigo).

**Fix unificado B2+B3:** filtro composto no `evaluateMetrics` — se motivo `gasto_sem_venda` bate MAS `sold_global >= 10`, remover o motivo. Não é config: research §Assumption A1 tornou o limiar LOCKED (10 vendas globais em 30d). Wave 1 já expõe `mlb_sold_quantity_global` no payload — Wave 2 apenas conecta ao service.

**Wave 2 é TDD obrigatório** — filtro no `evaluateMetrics` é O critério canônico do módulo; regressão perigosa para 40+ empresas rodando análise diária.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-inteligencia-detector-sugadores/53-CONTEXT.md
@.planning/phases/53-inteligencia-detector-sugadores/53-RESEARCH.md
@.planning/phases/53-inteligencia-detector-sugadores/53-01-PLAN.md
@app/Services/SugadorAnalysisService.php
@app/Services/Sugadores/MercadoLivreSugadoresProvider.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Testes RED — filtro sold_global no evaluateMetrics + regressões</name>
  <files>
    - tests/Unit/Services/Sugadores/MercadoLivreSugadoresProviderFilterTest.php
  </files>
  <read_first>
    - tests/Unit/Services/Sugadores/MercadoLivreSugadoresProviderFilterTest.php — arquivo criado na Wave 1 (adicionar novos testes ao final)
    - app/Services/SugadorAnalysisService.php:543-628 — evaluateMetrics inteiro (assinatura atual + critérios)
    - app/Models/SugadorConfig.php — defaults de threshold pra construir config no teste
    - .planning/phases/53-inteligencia-detector-sugadores/53-RESEARCH.md §Caso B2 e §Caso B3 (evidência prod)
    - CLAUDE.md memory `project_mariadb_local_corrompido` — SQLite in-memory
  </read_first>
  <behavior>
    - `test_evaluate_metrics_removes_gasto_sem_venda_when_sold_global_gte_10`: chamar `$service->evaluateMetrics(['investment'=>12.22, 'sold_quantity'=>0, 'clicks'=>4, 'cpc'=>3.05, 'acos'=>null, 'sold_global'=>781], $configDefault)` → array de motivos NÃO contém `gasto_sem_venda`. Se era o único motivo, retorna `[]` (adgroup não é sugador).
    - `test_evaluate_metrics_keeps_gasto_sem_venda_when_sold_global_lt_10`: mesma métrica, `sold_global=5` → motivos CONTÉM `gasto_sem_venda` (comportamento preservado — baixo volume orgânico não invalida hit).
    - `test_evaluate_metrics_keeps_gasto_sem_venda_when_sold_global_null`: mesma métrica, `sold_global=null` (fail-open da Wave 1) → motivos CONTÉM `gasto_sem_venda` (comportamento preservado — sem sinal ML, hit legítimo).
    - `test_evaluate_metrics_preserves_other_motives_when_removing_gasto_sem_venda`: métricas `['investment'=>50, 'sold_quantity'=>0, 'clicks'=>30, 'cpc'=>2, 'acos'=>null, 'sold_global'=>100]` com config default (cliques_minimos_sem_venda=15) → resultado deve conter `cliques_sem_conversao` (bate por clicks>=15) mas NÃO `gasto_sem_venda` (filtrado).
    - `test_evaluate_metrics_preserves_acos_alto_regardless_of_sold_global`: métricas `['investment'=>50, 'sold_quantity'=>3, 'clicks'=>20, 'cpc'=>2, 'acos'=>85, 'sold_global'=>150]` → motivos CONTÉM `acos_alto` (venda no ads > 0, filtro sold_global não se aplica).
    - `test_provider_propagates_sold_global_in_payload`: reusa setup dos testes Wave 1 (Http::fake `/items/MLB111` → `sold_quantity=781`). Chama `$provider->fetchAdgroupsMetrics(...)` → adgroup retornado tem chave `sold_global` === 781 (nome canônico usado por evaluateMetrics).
    - `test_provider_uses_raw_data_item_id_when_populated_for_status_check` (mismatch B3): não precisa alterar código na Wave 2 se Wave 1 já usa `$r['item_id']` como fonte — o teste apenas afirma que o comportamento é esse (assertion: quando payload ads traz `item_id=MLB999`, a Http::fake é chamada com `/items/MLB999`, não com nenhum outro MLB derivado).
  </behavior>
  <action>
    1. Abrir `tests/Unit/Services/Sugadores/MercadoLivreSugadoresProviderFilterTest.php` (criado na Wave 1).
    2. Adicionar helper `defaultConfig()`: cria `SugadorConfig` novo com defaults (gasto_minimo_sem_venda=10, cliques_minimos_sem_venda=15, cpc_maximo=null, acos_maximo_pct=80, todos com logic OPTIONAL). Confere valores em `app/Models/SugadorConfig.php`.
    3. Injetar `SugadorAnalysisService` via `$this->app->make(\App\Services\SugadorAnalysisService::class)`.
    4. Implementar os 5 testes de `evaluateMetrics` + 2 testes de provider descritos em `<behavior>`.
    5. Rodar `vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest` → **os 10 testes da Wave 1 continuam verdes**, os 7 novos DEVEM falhar (RED — sold_global ainda não é lido pelo evaluateMetrics; provider não expõe chave com esse nome).
    6. Commit RED: `test(53-02): failing tests para filtro sold_global no evaluateMetrics + regressão de motivos preservados`
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest --stop-on-failure</automated>
  </verify>
  <done>7 novos testes escritos, todos falhando pelo motivo correto. Testes Wave 1 (10) continuam verdes. Commit RED registrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: GREEN — expor sold_global no provider + filtro no evaluateMetrics</name>
  <files>
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php
    - app/Services/SugadorAnalysisService.php
  </files>
  <read_first>
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php:190-247 — foreach dos adgroups (Wave 1 já adicionou mlb_sold_quantity_global)
    - app/Services/SugadorAnalysisService.php:543-628 — evaluateMetrics inteiro
    - app/Services/SugadorAnalysisService.php:604-628 — bloco final de montagem de $motivos (onde vai entrar o filtro)
    - .planning/phases/53-inteligencia-detector-sugadores/53-RESEARCH.md §Recomendação T-53-04 e §Assumptions A1
  </read_first>
  <action>
    1. **Provider — chave `sold_global` canônica:** em `MercadoLivreSugadoresProvider::fetchAdgroupsMetrics`, dentro do `$out[] = [...]` (linhas 208-244), adicionar (mantendo `mlb_sold_quantity_global` como alias interno mas expondo o nome usado pelo service):
       ```php
       // Phase 53-02: nome canônico consumido por SugadorAnalysisService::evaluateMetrics.
       // Representa vendas GLOBAIS do MLB no ML (não só as atribuídas ao ads).
       'sold_global' => $mlbStatusInfo['sold_quantity'] ?? null,
       ```
       (Se Wave 1 já expôs como `mlb_sold_quantity_global`, manter ambas — `sold_global` é o nome público usado no service; `mlb_sold_quantity_global` é redundante mas não regride quem já lia.)
       Comentário do mismatch B3:
       ```php
       // Phase 53-02 nota B3 (research §Caso B3): observado em prod que sugador.mlb_id
       // pode divergir do raw_data.item_id (herança de sync antigo — DINMAP MLB4359551779
       // vs raw item MLB4359551777). Aqui usamos $r['item_id'] como fonte-verdade
       // (item_id vem do payload atual do Ads, não do banco). Se o upsert em Sugador
       // persistir esse mesmo valor via 'mlb_id' => $ad['mlb_id'], os registros novos
       // ficam sincronizados. Registros antigos com mismatch permanecem — não bloqueia
       // esta phase (fix retroativo é decisão de bulk-update, deferred).
       ```
    2. **SugadorAnalysisService::evaluateMetrics** — filtro composto:
       Localizar o bloco final que monta `$motivos` (linhas ~624-628):
       ```php
       $motivos = [];
       foreach ($criteria as $c) {
           if ($c['hit']) $motivos[] = $c['key'];
       }
       return $motivos;
       ```
       Substituir por:
       ```php
       $motivos = [];
       foreach ($criteria as $c) {
           if ($c['hit']) $motivos[] = $c['key'];
       }

       // Phase 53-02: filtro venda orgânica global (fix B2 BARAOSHOP + B3 DINMAP).
       // Se motivo 'gasto_sem_venda' bate mas o MLB vende globalmente >= 10 unidades
       // (FULL orgânico, busca direta, etc), o adgroup NÃO é sugador — cliente escolheu
       // pausar o ads porque produto vende sem ele. Threshold 10 LOCKED (research
       // §Assumptions A1 — configurable é feature futura).
       // sold_global vem do provider ML (Wave 1 -> Wave 2). Path Adman NÃO expõe
       // essa chave — sold_global=null preserva comportamento legacy (fail-open).
       $soldGlobal = $metrics['sold_global'] ?? null;
       if ($soldGlobal !== null && (int) $soldGlobal >= 10) {
           $motivos = array_values(array_filter($motivos, fn($m) => $m !== 'gasto_sem_venda'));
       }

       return $motivos;
       ```
       **CUIDADO:** o filtro roda DEPOIS que required/optional já validaram — se `gasto_sem_venda` era o único motivo, `$motivos` fica `[]` e o adgroup não é persistido (o caller já valida `if (empty($motivos)) continue;` na linha 198). Se havia motivos adicionais (`cliques_sem_conversao`, `acos_alto`), eles preservam-se.
       **NÃO** alterar a lógica de required/optional acima — o filtro é aplicado só na projeção final de motivos.
    3. Rodar suite completa: `vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest` → 17/17 verdes (10 Wave 1 + 7 Wave 2).
    4. Rodar regressão: `vendor/bin/phpunit --filter=Phase42` (path ML) + `vendor/bin/phpunit --filter=Phase30` ou equivalente Adman se existir. Path Adman não expõe `sold_global` → `null` → nenhuma mudança de motivos → sem regressão.
    5. Commit GREEN: `feat(53-02): filtro sold_global>=10 remove gasto_sem_venda (fix B2 BARAOSHOP + B3 DINMAP)`
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest --stop-on-failure</automated>
  </verify>
  <done>17/17 testes verdes na suite Phase 53. Regressão Phase 42 verde. Commit GREEN registrado. Path Adman intocado (sold_global=null, comportamento legacy preservado).</done>
</task>

</tasks>

<verification>
1. `vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest` → 17/17 verdes
2. `vendor/bin/phpunit --filter=Phase42` → regressão path ML verde
3. `vendor/bin/phpunit --filter=Phase41` → regressão MercadoLivreAdsService verde
4. Grep: `grep -n "sold_global" app/Services/SugadorAnalysisService.php app/Services/Sugadores/MercadoLivreSugadoresProvider.php` deve mostrar ≥2 ocorrências (1 no provider, 1 no service)
5. Grep: comentário do mismatch B3 presente no provider (`grep -n "MLB4359551779" app/Services/Sugadores/MercadoLivreSugadoresProvider.php`)
</verification>

<success_criteria>
- [ ] 7 testes novos escritos, todos verdes
- [ ] Total 17 testes verdes na suite Phase 53
- [ ] `MercadoLivreSugadoresProvider` expõe chave canônica `sold_global` no payload de cada adgroup
- [ ] `SugadorAnalysisService::evaluateMetrics` remove motivo `gasto_sem_venda` quando `sold_global >= 10`
- [ ] Motivos adicionais (`cliques_sem_conversao`, `acos_alto`, `cpc_alto`) são PRESERVADOS quando o filtro remove só `gasto_sem_venda`
- [ ] `sold_global = null` (fail-open) NÃO ativa o filtro → path Adman e cenários de instabilidade ML mantêm comportamento legacy
- [ ] Threshold `10` hardcoded (não passa por SugadorConfig)
- [ ] Comentário do mismatch B3 documentado no provider
- [ ] Regressão Phase 41 + Phase 42 verde
- [ ] Commits RED e GREEN separados (mínimo 2 commits nesta wave)
</success_criteria>

<output>
Criar `.planning/phases/53-inteligencia-detector-sugadores/53-02-SUMMARY.md` documentando:
- Threshold 10 LOCKED (research §Assumption A1) — feature de tornar configurável é DEFERRED
- Comportamento fail-open confirmado (sold_global=null preserva comportamento legacy — path Adman intocado)
- Como o filtro se compõe com required/optional (roda DEPOIS da projeção final — só remove `gasto_sem_venda`, não altera semântica de required)
- Nota do mismatch B3: registros novos ficam sincronizados via `raw_data.item_id`; registros antigos permanecem (bulk-update DEFERRED)
- Contagem de testes verdes acumulados (17) + commits RED/GREEN
- Preparação da Wave 3 (UAT): pontos a validar em prod (CAMILLO, BARAOSHOP, DINMAP)
</output>
