---
phase: 53-inteligencia-detector-sugadores
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-53-01
files_modified:
  - app/Services/Sugadores/MercadoLivreAdsService.php
  - app/Services/MercadoLivreService.php
  - app/Services/Sugadores/MercadoLivreSugadoresProvider.php
  - tests/Unit/Services/Sugadores/MercadoLivreSugadoresProviderFilterTest.php
must_haves:
  truths:
    - "`MercadoLivreAdsService::listCampaigns` retorna do cache no 2º call dentro de 10min (mesmo advertiser + mesma janela from/to) — sem re-executar HTTP"
    - "Cache key normalizada como `ml:campaigns:{advertiser_id}:{from}:{to}` com TTL 600s — miss → HTTP + set; hit → cache direto"
    - "`MercadoLivreService::fetchItemStatus(Company, string $mlbId): array` existe, chama `/items/{mlbId}?attributes=id,status,sub_status,available_quantity,sold_quantity,shipping` e retorna array com chaves `status`, `sub_status`, `available_quantity`, `sold_quantity`, `logistic_type`"
    - "`fetchItemStatus` cacheia em `ml:item-status:{mlbId}` com TTL 3600s"
    - "`fetchItemStatus` fail-open: se a chamada ML lança (rate-limit, 404, timeout) retorna `['status' => null, 'sub_status' => [], 'available_quantity' => null, 'sold_quantity' => null, 'logistic_type' => null]` + log warning — NUNCA propaga exception"
    - "`MercadoLivreSugadoresProvider::fetchAdgroupsMetrics` chama `fetchItemStatus` para cada adgroup com `mlb_id` não-nulo e propaga chave `mlb_status` (string|null) e `mlb_sold_quantity_global` (int|null) no payload retornado"
    - "Loop de adgroups do provider skipa adgroups cujo `mlb_status IN ['paused', 'closed', 'under_review']` — o adgroup NÃO entra no array `$out` retornado"
    - "Adgroups com `mlb_status = null` (fail-open) OU `mlb_status = 'active'` continuam sendo retornados (comportamento preservado — não regride Bymobille/casos legítimos)"
    - "Cada teste RED (failing) tem commit separado do GREEN (implementação) — padrão TDD Phase 52/54"
  artifacts:
    - path: "app/Services/Sugadores/MercadoLivreAdsService.php"
      provides: "listCampaigns envolvido em Cache::remember com key ml:campaigns:{advertiser}:{from}:{to} TTL 600s"
      contains: "Cache::remember\\('ml:campaigns:"
    - path: "app/Services/MercadoLivreService.php"
      provides: "método público fetchItemStatus(Company, string $mlbId): array com cache TTL 3600 e fail-open"
      contains: "public function fetchItemStatus"
    - path: "app/Services/Sugadores/MercadoLivreSugadoresProvider.php"
      provides: "fetchAdgroupsMetrics enriquece cada adgroup com mlb_status + mlb_sold_quantity_global e skipa quando status em quarantine list"
      contains: "fetchItemStatus"
    - path: "tests/Unit/Services/Sugadores/MercadoLivreSugadoresProviderFilterTest.php"
      provides: "5 testes: cache listCampaigns, MLB paused → skip, MLB under_review → skip, MLB closed → skip, MLB active → mantém, MLB status=null (fail-open) → mantém"
  key_links:
    - from: "MercadoLivreAdsService::listCampaigns"
      to: "Cache::remember"
      via: "chave ml:campaigns:{advertiser}:{from}:{to} TTL 600s"
      pattern: "Cache::remember\\(['\"]ml:campaigns:"
    - from: "MercadoLivreService::fetchItemStatus"
      to: "self::get(Company, '/items/{mlbId}', [attributes=...])"
      via: "helper HTTP existente linha 271 + Cache::remember TTL 3600"
      pattern: "MercadoLivreService.php:271 (helper get)"
    - from: "MercadoLivreSugadoresProvider::fetchAdgroupsMetrics"
      to: "MercadoLivreService::fetchItemStatus"
      via: "chamada por adgroup + skip se status IN paused/closed/under_review"
      pattern: "['paused', 'closed', 'under_review']"
---

<objective>
Fixar 2 vetores CRÍTICOS do detector de sugadores no path ML:

1. **Descoberta sistêmica (research §Descoberta sistêmica):** `MercadoLivreAdsService::listCampaigns` **NÃO tem cache** — 639 incidents rate-limit no log de prod, `campaign_name = NULL` em 100% dos 183 sugadores criados hoje. Sem esse fix, filtro de quarentena SGI/paused **nunca dispara pelo path ML** e os demais fixes ficam parciais.
2. **B1 CAMILLO PARTS (research §Caso B1):** Adgroup 1902557017 com MLB6258261358 pausado no ML foi flagged como sugador. Fix confirmado com evidência real de prod (API ML retornou `status=paused`, `sub_status=["paused_by_seller"]`). Contra-hipótese lean (`investment=0 AND clicks=0`) **REFUTADA** — 0 de 183 sugadores hoje se encaixam.

**Wave 1 é TDD obrigatório** (backend, regressão perigosa afetando análise diária de 40+ empresas). Cada mudança tem teste RED antes do GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-inteligencia-detector-sugadores/53-CONTEXT.md
@.planning/phases/53-inteligencia-detector-sugadores/53-RESEARCH.md
@app/Services/Sugadores/MercadoLivreAdsService.php
@app/Services/Sugadores/MercadoLivreSugadoresProvider.php
@app/Services/MercadoLivreService.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Testes RED — cache listCampaigns + fetchItemStatus + filtro por mlb_status</name>
  <files>
    - tests/Unit/Services/Sugadores/MercadoLivreSugadoresProviderFilterTest.php
  </files>
  <read_first>
    - tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php:1-80 — padrão Http::preventStrayRequests + Http::fake + Cache::flush + Carbon::setTestNow
    - app/Services/Sugadores/MercadoLivreAdsService.php:353-399 — listCampaigns atual (SEM cache; envelope da resposta)
    - app/Services/MercadoLivreService.php:271-320 — helper get() (o fetchItemStatus vai usar)
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php:136-247 — fetchAdgroupsMetrics atual (onde vai entrar o filtro)
    - .planning/phases/53-inteligencia-detector-sugadores/53-RESEARCH.md §Caso B1 (evidência prod) e §Descoberta sistêmica
    - CLAUDE.md memory `project_mariadb_local_corrompido` — usar SQLite in-memory + Http::fake (nunca DB local nem rede real)
  </read_first>
  <behavior>
    - `test_list_campaigns_hits_cache_on_second_call`: cria Company + MlToken active + MlAdvertiser cacheado. `Http::fake(['*/campaigns/search*' => Http::response(['results'=>[], 'paging'=>['total'=>0]], 200)])`. Chama `listCampaigns($company, 620095, '2026-06-01', '2026-06-30')` 2x com mesmos args → `Http::assertSentCount(1)` (2º call vem do cache).
    - `test_list_campaigns_cache_key_varies_by_date_range`: mesma setup. Chama listCampaigns 2x com `date_to` diferente → `Http::assertSentCount(2)` (chaves distintas, sem colisão).
    - `test_fetch_item_status_returns_normalized_array`: Http::fake `*/items/MLB123*` → `Http::response(['id'=>'MLB123','status'=>'paused','sub_status'=>['paused_by_seller'],'available_quantity'=>1,'sold_quantity'=>4,'shipping'=>['logistic_type'=>'fulfillment']], 200)`. Chama `fetchItemStatus($company, 'MLB123')` → assertArraySubset `['status'=>'paused','available_quantity'=>1,'sold_quantity'=>4,'logistic_type'=>'fulfillment']` e `sub_status` contém `paused_by_seller`.
    - `test_fetch_item_status_hits_cache_on_second_call`: mesma setup. Chama `fetchItemStatus($company, 'MLB123')` 2x → `Http::assertSentCount(1)` (só o call de token check + 1 item — ou zero items no 2º).
    - `test_fetch_item_status_fail_open_on_exception`: Http::fake `*/items/*` → `Http::response('rate limit', 429)`. Chama `fetchItemStatus($company, 'MLB999')` → NÃO lança, retorna `['status'=>null,'sub_status'=>[],'available_quantity'=>null,'sold_quantity'=>null,'logistic_type'=>null]`.
    - `test_provider_skips_adgroup_when_mlb_paused`: setup completo — Http::fake listCampaigns (results normais) + fake `/1/ads/product_ads/items` (1 adgroup com item_id=MLB111) + fake `/items/MLB111` retornando `status=paused`. Chama `provider->fetchAdgroupsMetrics(company, from, to)` → array retornado NÃO contém o adgroup (foi skipado).
    - `test_provider_skips_adgroup_when_mlb_under_review`: mesma setup, `status=under_review` → adgroup NÃO retornado.
    - `test_provider_skips_adgroup_when_mlb_closed`: mesma setup, `status=closed` → adgroup NÃO retornado.
    - `test_provider_keeps_adgroup_when_mlb_active`: mesma setup, `status=active` → adgroup RETORNADO (regressão — comportamento atual preservado).
    - `test_provider_keeps_adgroup_when_mlb_status_null_fail_open`: Http::fake `/items/*` → 429 (rate-limit). Provider NÃO deve travar; adgroup deve ser retornado (fail-open documentado). Preserva volume de análise em caso de instabilidade ML.
  </behavior>
  <action>
    1. Criar diretório `tests/Unit/Services/Sugadores/` se não existir.
    2. Escrever `MercadoLivreSugadoresProviderFilterTest.php` extends `Tests\TestCase` com `use RefreshDatabase;`.
    3. Namespace `Tests\Unit\Services\Sugadores`.
    4. `setUp()`: `parent::setUp()` + `Http::preventStrayRequests()` + `Cache::flush()` + `Carbon::setTestNow(Carbon::create(2026,7,2,12,0,0))`.
    5. Helper `makeCompanyWithMlToken()`: copiar padrão de `tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php:66-95` (Company + MlToken active + MlAdvertiser cacheado com advertiser_id=620095).
    6. Implementar os 10 testes do bloco `<behavior>` acima usando `Http::fake([...])` com wildcards por endpoint.
    7. Para os testes de provider (últimos 5): resolver `MercadoLivreAdsService` + `MercadoLivreSugadoresProvider` via `$this->app->make(...)`.
    8. Rodar `vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest` → todos DEVEM falhar (RED — cache ainda não existe, fetchItemStatus não existe, filtro não existe).
    9. Commit RED: `test(53-01): failing tests para cache listCampaigns + fetchItemStatus + filtro mlb_status`
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest --stop-on-failure</automated>
  </verify>
  <done>10 testes escritos, todos falhando pelo motivo correto (método/cache/filtro inexistente — NÃO por setup quebrado). Commit RED registrado. Arquivo namespace + use RefreshDatabase configurados.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: GREEN — cache listCampaigns + método fetchItemStatus</name>
  <files>
    - app/Services/Sugadores/MercadoLivreAdsService.php
    - app/Services/MercadoLivreService.php
  </files>
  <read_first>
    - app/Services/Sugadores/MercadoLivreAdsService.php:353-399 — listCampaigns inteiro (envolver o corpo em Cache::remember)
    - app/Services/MercadoLivreService.php:264-320 — helper `get()` já existente (o novo método vai reusar)
    - app/Services/MlColetaService.php:196 — padrão de uso `/items/{itemId}` já em prod (referência)
    - .planning/phases/53-inteligencia-detector-sugadores/53-RESEARCH.md §Overview de helpers existentes
  </read_first>
  <action>
    1. **MercadoLivreAdsService::listCampaigns** — envolver o corpo (linhas 355-398) em `Cache::remember`:
       ```php
       public function listCampaigns(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array
       {
           $cacheKey = "ml:campaigns:{$advertiserId}:{$dateFrom}:{$dateTo}";
           return Cache::remember($cacheKey, 600, function () use ($company, $advertiserId, $dateFrom, $dateTo) {
               // ... corpo atual (linhas 355-398) intacto ...
           });
       }
       ```
       Adicionar `use Illuminate\Support\Facades\Cache;` no topo se ainda não estiver.
       Comentário em pt-BR: `// Phase 53-01: cache 10min contra rate-limit ML (639 incidents em prod) — sem ele campaign_name fica NULL e filtro SGI nunca dispara`.
       **CUIDADO:** `resetMetrics()` roda ANTES do cache — no cache hit não deve gerar métricas de "pages_read". Mover a chamada `$this->resetMetrics()` pra DENTRO do closure (só executa no miss). Métricas ficam zeradas no hit (comportamento intencional — não houve request nova).
    2. **MercadoLivreService::fetchItemStatus** — adicionar método público após `get()` (após linha ~320):
       ```php
       /**
        * Phase 53-01: consulta status atual do MLB (ativo/pausado/under_review/closed) para
        * o detector de sugadores excluir anúncios inativos (fix B1 CAMILLO PARTS).
        *
        * Cache 1h — status de MLB muda raramente; assumption A2 documentada no research.
        * Fail-open: em caso de rate-limit/timeout/404, retorna shape completo com nulls
        * + log warning. Provider tratará null como "não sei, deixa entrar" (comportamento
        * atual preservado — nunca regride caso legítimo por falha ML).
        *
        * @return array{status:?string, sub_status:array<string>, available_quantity:?int, sold_quantity:?int, logistic_type:?string}
        */
       public function fetchItemStatus(Company $company, string $mlbId): array
       {
           $cacheKey = "ml:item-status:{$mlbId}";
           return Cache::remember($cacheKey, 3600, function () use ($company, $mlbId) {
               try {
                   $body = $this->get($company, "/items/{$mlbId}", [
                       'attributes' => 'id,status,sub_status,available_quantity,sold_quantity,shipping',
                   ]);
                   return [
                       'status'             => $body['status'] ?? null,
                       'sub_status'         => is_array($body['sub_status'] ?? null) ? $body['sub_status'] : [],
                       'available_quantity' => isset($body['available_quantity']) ? (int) $body['available_quantity'] : null,
                       'sold_quantity'      => isset($body['sold_quantity']) ? (int) $body['sold_quantity'] : null,
                       'logistic_type'      => $body['shipping']['logistic_type'] ?? null,
                   ];
               } catch (\Throwable $e) {
                   Log::warning("[MercadoLivre] fetchItemStatus falhou para MLB {$mlbId} empresa {$company->id}: " . $e->getMessage());
                   return [
                       'status'             => null,
                       'sub_status'         => [],
                       'available_quantity' => null,
                       'sold_quantity'      => null,
                       'logistic_type'      => null,
                   ];
               }
           });
       }
       ```
       Adicionar `use Illuminate\Support\Facades\Cache;` no topo se ainda não estiver.
    3. Rodar testes das duas primeiras seções: `vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest --stop-on-failure` — os 5 primeiros testes (cache + fetchItemStatus) DEVEM passar; os 5 finais (provider) ainda falham (Tarefa 3).
    4. Commit GREEN parcial: `feat(53-01): cache 10min em listCampaigns + método fetchItemStatus com fail-open`
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest --filter="test_list_campaigns|test_fetch_item_status" --stop-on-failure</automated>
  </verify>
  <done>5 testes iniciais verdes (cache + fetchItemStatus). Testes de provider ainda vermelhos (esperado — Tarefa 3 fecha). Commit GREEN registrado. Fail-open confirmado: teste `test_fetch_item_status_fail_open_on_exception` passa retornando array com nulls.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: GREEN — filtro mlb_status no MercadoLivreSugadoresProvider</name>
  <files>
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php
  </files>
  <read_first>
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php:136-247 — fetchAdgroupsMetrics inteiro
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php:47 — construtor atual (só recebe MercadoLivreAdsService — adicionar MercadoLivreService)
    - app/Services/MercadoLivreService.php — método fetchItemStatus da Tarefa 2
    - .planning/phases/53-inteligencia-detector-sugadores/53-RESEARCH.md §Caso B1 e §Recomendação T-53-03
  </read_first>
  <action>
    1. **Injeção MercadoLivreService** no construtor:
       ```php
       public function __construct(
           private MercadoLivreAdsService $ads,
           private \App\Services\MercadoLivreService $ml,
       ) {}
       ```
       Laravel auto-injeta via service container (ambos são singleton-safe).
    2. **fetchAdgroupsMetrics** — dentro do foreach dos `$results` (linhas 190-244), após montar as métricas mas ANTES do `$out[] = [...]`:
       ```php
       // Phase 53-01: fix B1 (CAMILLO PARTS). Detector NAO enxergava status do MLB
       // individual — adgroup ativo com MLB pausado era flagged falsamente. Consulta
       // /items/{mlb_id} + skip se paused/closed/under_review. Fail-open: se mlb_status
       // for null (rate-limit, sem mlb_id, timeout), NAO skipa — preserva volume.
       $mlbId = $r['item_id'] ?? null;
       $mlbStatusInfo = $mlbId
           ? $this->ml->fetchItemStatus($company, (string) $mlbId)
           : ['status' => null, 'sub_status' => [], 'available_quantity' => null, 'sold_quantity' => null, 'logistic_type' => null];

       $mlbStatus = $mlbStatusInfo['status'] ?? null;
       if ($mlbStatus !== null && in_array($mlbStatus, ['paused', 'closed', 'under_review'], true)) {
           continue; // adgroup skipado — MLB inativo no ML
       }
       ```
    3. **Propagar `mlb_status` e `mlb_sold_quantity_global`** no payload retornado (adicionar dentro do array `$out[] = [...]`):
       ```php
       // Phase 53-01: expostos pra debug e pra Wave 2 (filtro vendas orgânicas) consumir.
       'mlb_status'                => $mlbStatus,
       'mlb_sold_quantity_global'  => $mlbStatusInfo['sold_quantity'] ?? null,
       'mlb_logistic_type'         => $mlbStatusInfo['logistic_type'] ?? null,
       ```
       (Não substitui `raw_data.status` que é do adgroup — apenas adiciona chaves novas de topo.)
    4. Rodar suite completa: `vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest` → 10/10 verdes.
    5. Rodar regressão Phase 42 (é a suite que valida provider ML + quarentena): `vendor/bin/phpunit --filter=Phase42` → **deve continuar verde**. Se falhar, causa provável: testes Phase 42 não injetaram `Http::fake('/items/*')` — nesse caso `fetchItemStatus` cai no fail-open e retorna null (adgroup passa). Se ainda regredir, garantir que Phase 42 mocka `MercadoLivreService::fetchItemStatus` no container (`$this->app->instance(MercadoLivreService::class, $mock)`) OU adiciona `Http::fake(['*/items/*' => Http::response(['status'=>'active'], 200)])`.
    6. Commit GREEN final: `feat(53-01): provider ML skipa adgroup quando MLB pausado/closed/under_review (fix B1 CAMILLO PARTS)`
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest --stop-on-failure</automated>
  </verify>
  <done>10/10 testes da suite Phase 53-01 verdes. Regressão Phase 42 verde (ou ajustada com Http::fake extra pra `/items/*`). Commit GREEN registrado. Provider expõe 3 chaves novas no payload (`mlb_status`, `mlb_sold_quantity_global`, `mlb_logistic_type`) que Wave 2 vai consumir.</done>
</task>

</tasks>

<verification>
1. `vendor/bin/phpunit --filter=MercadoLivreSugadoresProviderFilterTest` → 10/10 verdes
2. `vendor/bin/phpunit --filter=Phase42` → regressão verde (path ML sugadores intacto)
3. `vendor/bin/phpunit --filter=Phase41` → regressão MercadoLivreAdsService (cache não quebra métricas nem pagination)
4. Grep no código: `grep -n "Cache::remember" app/Services/Sugadores/MercadoLivreAdsService.php` deve mostrar 1 nova ocorrência com key `ml:campaigns:`
5. Grep: `grep -n "fetchItemStatus" app/Services/MercadoLivreService.php app/Services/Sugadores/MercadoLivreSugadoresProvider.php` deve mostrar ≥3 ocorrências
</verification>

<success_criteria>
- [ ] 10 testes escritos (5 cache + fetchItemStatus, 5 filtro mlb_status)
- [ ] 10/10 testes verdes
- [ ] `MercadoLivreAdsService::listCampaigns` cacheia em `ml:campaigns:{advertiser}:{from}:{to}` com TTL 600s
- [ ] `MercadoLivreService::fetchItemStatus` existe, retorna shape completo, cacheia 3600s, fail-open documentado
- [ ] `MercadoLivreSugadoresProvider::fetchAdgroupsMetrics` skipa adgroups com `mlb_status IN [paused, closed, under_review]`
- [ ] Adgroups com `mlb_status=null` (fail-open) OU `active` continuam sendo retornados
- [ ] Regressão Phase 41 + Phase 42 verde
- [ ] Commits RED e GREEN separados (mínimo 3 commits: 1 RED + 2 GREEN parciais/finais)
- [ ] Nenhum log de token/access_token expõe segredo no fail-open (T-39-02-01 anti-leak)
</success_criteria>

<output>
Criar `.planning/phases/53-inteligencia-detector-sugadores/53-01-SUMMARY.md` documentando:
- Decisão de TTL: 600s (listCampaigns) e 3600s (fetchItemStatus) — justificativa research §Overview
- Fail-open documentado com shape esperado (null em todas as chaves de status)
- Padrão de mock nos testes (Http::fake wildcards + Cache::flush no setUp)
- Ajuste eventual na regressão Phase 42 (Http::fake `/items/*` genérico se necessário)
- Contagem de testes verdes + commits RED/GREEN
- Chaves novas expostas no payload do provider que Wave 2 vai consumir
</output>
