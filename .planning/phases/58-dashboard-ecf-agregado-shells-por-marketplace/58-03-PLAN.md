---
phase: 58
plan: 58-03
type: execute
wave: 2
depends_on: [58-01, 58-02]
files_modified:
  - resources/js/Layouts/AppLayout.jsx
  - tests/Feature/Phase58/DashboardNavigationSmokeTest.php
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "Grupo 'Mercado Livre' do NAV_TREE tem 'ECF Consolidado' como PRIMEIRO item, apontando para route('ecf.dashboard')"
    - "Item 'Dashboard' original do grupo Mercado Livre (routeName='dashboard') e renomeado para 'Mercado Livre' e aponta para route('mercadolivre.dashboard')"
    - "Nenhum item novo do sidebar referencia route('dashboard') (canonical fallback SAI do menu — CONTEXT §5)"
    - "Rota canonical 'dashboard' continua ativa em routes/web.php (compat de deep links preservada)"
    - "Smoke test valida navegacao E2E: 4 rotas retornam 200 renderizando componente Inertia correto"
    - "npm run build compila o AppLayout.jsx sem erros de JSX"
  artifacts:
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "NAV_TREE atualizado: 2 items novos no grupo Mercado Livre (ECF Consolidado + Mercado Livre) substituindo o item Dashboard antigo"
      contains: "ecf.dashboard"
    - path: "tests/Feature/Phase58/DashboardNavigationSmokeTest.php"
      provides: "Smoke E2E: as 4 rotas do modelo multi-marketplace + legacy renderizam componentes Inertia corretos"
  key_links:
    - from: "AppLayout.jsx NAV_TREE grupo 'Mercado Livre'"
      to: "route('ecf.dashboard')"
      via: "Novo item { label: 'ECF Consolidado', routeName: 'ecf.dashboard', ... }"
      pattern: "ecf\\.dashboard"
    - from: "AppLayout.jsx NAV_TREE grupo 'Mercado Livre'"
      to: "route('mercadolivre.dashboard')"
      via: "Renomeacao do item Dashboard existente"
      pattern: "mercadolivre\\.dashboard"
    - from: "Smoke test"
      to: "Componentes Inertia Dashboard/Admin (ecf+mercadolivre), Dashboard/ShopeeShell, Dashboard/AmazonShell"
      via: "assertInertia(fn => $page->component(...))"
      pattern: "assertInertia"
---

<objective>
Fechar Phase 58 costurando o menu (NAV_TREE em AppLayout.jsx) com as rotas criadas no Plan 58-01 e os shells JSX do Plan 58-02. Adicionar smoke test de navegacao E2E que valida os 4 caminhos completos (route → controller → componente Inertia). Rota canonical `dashboard` sai do menu mas permanece registrada.

Purpose: Fechar DASH-01/02/03 do ponto de vista do usuario final. Sem mudanca no NAV_TREE, o backend do Plan 58-01 fica invisivel. Sem smoke E2E, ninguem valida que Plan 01 + Plan 02 realmente conectam.

Output: 1 arquivo modificado (AppLayout.jsx) + 1 arquivo novo (smoke test). Build Vite verde. Depende de 58-01 (nomes de rota) e 58-02 (componentes JSX).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-CONTEXT.md
@.planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-01-PLAN.md
@.planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-02-PLAN.md
@resources/js/Layouts/AppLayout.jsx
@tests/Feature/Phase57/DashboardSmokeTest.php

<interfaces>
<!-- Contratos-chave para o executor. Extraidos do codebase. -->

Formato de item do NAV_TREE (padrao existente em AppLayout.jsx linha 44):
{
    label: 'Dashboard',
    routeName: 'dashboard',
    page: 'Dashboard',
    icon: LayoutDashboard,
    permission: 'core.dashboard'
}

Regras dos campos:
- label: string exibida no sidebar
- routeName: nome da rota Laravel (usado por route(routeName) no JSX de navegacao)
- page: string usada por isActive('<page>') pra destaque do item ativo — Dashboard/Admin e o page real renderizado pelo controller
- icon: componente lucide-react
- permission: string da permissao core.* que ja existe (Phase 8/12) — 'core.dashboard' cobre ambos items novos (mesma permissao usada pelo antigo Dashboard)

Grupo Mercado Livre atual (AppLayout.jsx linhas 37-74):
{
    group: 'Mercado Livre',
    icon: Store,
    iconSrc: '/images/mercado-livre-87.svg',
    defaultOpen: true,
    children: [
        // Secao Performance
        { label: 'Dashboard',   routeName: 'dashboard',           page: 'Dashboard',    icon: LayoutDashboard, permission: 'core.dashboard' },
        { label: 'Desempenho',  ... },
        ...
    ],
}

Icone recomendado para "ECF Consolidado" (CONTEXT §4):
- PieChart (ja importado em AppLayout.jsx linha 9)

Icone do "Mercado Livre" apos renomeacao:
- LayoutDashboard (ja importado, mantem o mesmo do antigo Dashboard)

Rotas nomeadas criadas em Plan 58-01 (existem apos merge de Plan 58-01):
- ecf.dashboard
- mercadolivre.dashboard
- shopee.dashboard
- amazon.dashboard

Componentes Inertia criados em Plan 58-02:
- Dashboard/ShopeeShell (page = 'Dashboard/ShopeeShell')
- Dashboard/AmazonShell (page = 'Dashboard/AmazonShell')

Padrao Assertable Inertia (Feature test):
- use Inertia\Testing\AssertableInertia as Assert;
- $response->assertInertia(fn (Assert $page) => $page->component('Dashboard/Admin'));

Ziggy: route() helper JS resolve routeName dinamicamente — nao alterar Ziggy config; o novo nome ja e serializado apos Plan 58-01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar NAV_TREE em AppLayout.jsx</name>
  <read_first>
    - resources/js/Layouts/AppLayout.jsx linhas 22-98 (NAV_TREE completa; foco no grupo Mercado Livre linhas 37-74)
    - .planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-CONTEXT.md secoes 4 e 5
  </read_first>
  <files>resources/js/Layouts/AppLayout.jsx</files>
  <action>
Aplicar 2 mudancas EXATAS no NAV_TREE dentro do grupo Mercado Livre (linhas ~42-50):

Mudanca A — ADICIONAR item ECF Consolidado como PRIMEIRO item da secao Performance (imediatamente apos o comentario `// ── Secao Performance ─────────────────────────`, ANTES do item Dashboard antigo):

Formato do novo item (mesma estrutura dos demais items):
- label: 'ECF Consolidado'
- routeName: 'ecf.dashboard'
- page: 'Dashboard/Admin' (mesmo componente renderizado pelo pipeline agregado — CONTEXT §4 explicita "highlight funciona via page compartilhado")
- icon: PieChart (importado ja em linha 9)
- permission: 'core.dashboard' (permissao existente — mesma do Dashboard antigo)

Adicionar comentario pt-BR na linha acima: `// NOVO Phase 58 DASH-01 — agregado atraves de marketplaces`

Mudanca B — RENOMEAR o item Dashboard existente (linha 44) para "Mercado Livre" apontando para `mercadolivre.dashboard`:

Alterar o item existente:
- label: 'Dashboard' → 'Mercado Livre'
- routeName: 'dashboard' → 'mercadolivre.dashboard'
- page permanece 'Dashboard/Admin' (mesmo pipeline reutilizado — CONTEXT §4)
- icon permanece LayoutDashboard
- permission permanece 'core.dashboard'

Adicionar comentario pt-BR: `// MUDADO Phase 58 DASH-02 — antes era { label: 'Dashboard', routeName: 'dashboard' }; rota legacy segue registrada para deep links (CONTEXT §5).`

Depois das duas mudancas, o topo do children do grupo Mercado Livre fica:

// ── Secao Performance ─────────────────────────
// NOVO Phase 58 DASH-01 — agregado atraves de marketplaces
{ label: 'ECF Consolidado', routeName: 'ecf.dashboard',           page: 'Dashboard/Admin', icon: PieChart,        permission: 'core.dashboard' },
// MUDADO Phase 58 DASH-02 — antes era { label: 'Dashboard', routeName: 'dashboard' }; rota legacy segue registrada para deep links (CONTEXT §5).
{ label: 'Mercado Livre',   routeName: 'mercadolivre.dashboard',  page: 'Dashboard/Admin', icon: LayoutDashboard, permission: 'core.dashboard' },
{ label: 'Desempenho',  ...

NAO alterar demais items do grupo (Desempenho, Empresas, Carteira, Sugadores, Metas, PPA, dividers, sub-secoes Dados Estrategicos e Polos). NAO alterar imports (PieChart e LayoutDashboard ja estao importados na linha 9 e 4 respectivamente).

Sanity: rodar `npm run build` — precisa completar 0 erros.
  </action>
  <verify>
    <automated>grep -c "ecf.dashboard" resources/js/Layouts/AppLayout.jsx</automated>
    <automated>grep -c "mercadolivre.dashboard" resources/js/Layouts/AppLayout.jsx</automated>
    <automated>grep -v '^\s*//' resources/js/Layouts/AppLayout.jsx | grep -c "routeName: 'dashboard'"</automated>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Grep `ecf.dashboard` retorna >= 1 em AppLayout.jsx (item ECF Consolidado)
    - Grep `mercadolivre.dashboard` retorna >= 1 (item Mercado Livre renomeado)
    - Grep filtrado de comentario `routeName: 'dashboard'` retorna 0 em codigo funcional (canonical fallback saiu do menu)
    - Comentarios pt-BR nos 2 pontos alterados citam Phase 58 DASH-01/02
    - `npm run build` completa sem erros
    - Zero mudanca fora do grupo Mercado Livre — `git diff --stat resources/js/Layouts/AppLayout.jsx` mostra alteracoes concentradas em ~5 linhas
  </acceptance_criteria>
  <done>Commit `feat(58-03): DASH-01/02 — NAV_TREE ECF Consolidado + rename Dashboard→Mercado Livre` isolado.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke test E2E de navegacao</name>
  <read_first>
    - tests/Feature/Phase57/DashboardSmokeTest.php (padrao factory + actingAs)
    - tests/Feature/Phase58/DashboardShellsBackendTest.php (padrao AssertableInertia — criado no Plan 58-01)
  </read_first>
  <files>tests/Feature/Phase58/DashboardNavigationSmokeTest.php</files>
  <action>
Criar `tests/Feature/Phase58/DashboardNavigationSmokeTest.php` — namespace `Tests\Feature\Phase58`, extende TestCase, usa RefreshDatabase.

Docblock pt-BR no topo da classe:
"Phase 58 v13.0 — Smoke test E2E de navegacao multi-marketplace. Valida os 4 caminhos completos (route → controller → componente Inertia) apos NAV_TREE atualizado. Pega regressao GROSSEIRA de wiring — nao valida valores exatos de KPI (deferido para UAT em prod)."

Tests obrigatorios (5 tests):

Test 1 — test_ecf_dashboard_renderiza_componente_admin
- User factory role=admin + Company factory com marketplace=meli + CompanyMarketplace factory primary
- actingAs admin, GET route('ecf.dashboard')
- assertOk
- assertInertia(fn (Assert $page) => $page->component('Dashboard/Admin'))
- Comentario pt-BR: "ECF Consolidado reutiliza o mesmo componente Dashboard/Admin do pipeline agregado (CONTEXT §2)"

Test 2 — test_mercadolivre_dashboard_renderiza_componente_admin
- Analogo ao Test 1, GET route('mercadolivre.dashboard'), mesmo assertInertia component 'Dashboard/Admin'
- Comentario pt-BR: "Dashboard ML forca filter marketplace=meli via $request->merge (Plan 58-01) mas reutiliza Dashboard/Admin"

Test 3 — test_shopee_dashboard_renderiza_shell
- User admin + Company meli
- actingAs, GET route('shopee.dashboard')
- assertOk
- assertInertia(fn (Assert $page) => $page->component('Dashboard/ShopeeShell')->where('marketplace', 'shopee')->where('label', 'Shopee'))

Test 4 — test_amazon_dashboard_renderiza_shell
- Analogo, GET route('amazon.dashboard'), component 'Dashboard/AmazonShell', props marketplace=amazon label=Amazon

Test 5 — test_legacy_dashboard_ainda_navegavel
- User admin + Company meli
- GET '/dashboard' (URL literal — nao pelo nome; garante fallback funciona mesmo se alguem digitar direto)
- assertOk
- Comentario pt-BR: "Rota canonical /dashboard permanece registrada (CONTEXT §5) para deep links e bookmarks legados"

Imports obrigatorios no topo:
- App\Models\Company
- App\Models\CompanyMarketplace
- App\Models\User
- Illuminate\Foundation\Testing\RefreshDatabase
- Inertia\Testing\AssertableInertia as Assert
- Tests\TestCase

Todos os tests seguem shape do Phase 57 DashboardSmokeTest (factory admin + factory company + actingAs + get + assertions).

Rodar `php artisan test --filter=DashboardNavigationSmokeTest` — precisa retornar 5/5 verdes.
  </action>
  <verify>
    <automated>test -f tests/Feature/Phase58/DashboardNavigationSmokeTest.php &amp;&amp; grep -c "public function test_" tests/Feature/Phase58/DashboardNavigationSmokeTest.php</automated>
    <automated>php artisan test --filter=DashboardNavigationSmokeTest 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em tests/Feature/Phase58/DashboardNavigationSmokeTest.php
    - Contem 5 metodos public function test_
    - Todos os 5 tests passam (GREEN)
    - Usa AssertableInertia para checar component names + props
    - Comentarios em pt-BR
  </acceptance_criteria>
  <done>Commit `test(58-03): smoke E2E navegacao multi-marketplace (5 tests)` isolado.</done>
</task>

<task type="auto">
  <name>Task 3: Gate final de zero regressao + resumo Phase 58</name>
  <read_first>
    - .planning/phases/57-modelo-de-dados-multi-marketplace/PHASE-SUMMARY.md
    - .planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-01-SUMMARY.md
    - .planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-02-SUMMARY.md
  </read_first>
  <files>(sem modificacao — apenas validacao final)</files>
  <action>
Rodar as 3 suites e confirmar zero regressao:

1. `php artisan test --filter=Phase57` — deve manter 20 tests verdes (baseline modelo N:N intacto)
2. `php artisan test --filter=Phase58` — deve mostrar 16 tests verdes:
   - 5 rotas (Plan 58-01)
   - 4 filter (Plan 58-01)
   - 2 shells backend (Plan 58-01)
   - 5 navegacao E2E (Plan 58-03 Task 2)
3. `php artisan test tests/Feature/DashboardTest.php` (se existir) — nao regride.

Rodar sanity de rotas final:
- `php artisan route:list --path=dashboard` deve mostrar as 5 rotas nomeadas ativas (dashboard, ecf.dashboard, mercadolivre.dashboard, shopee.dashboard, amazon.dashboard).

Se todas passarem: registrar contagem final na Phase 58 SUMMARY. Se alguma falhar: NAO forcar GREEN — retornar diagnostico (deviation policy Rule 1).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase57 2>&amp;1 | tail -3</automated>
    <automated>php artisan test --filter=Phase58 2>&amp;1 | tail -3</automated>
    <automated>php artisan route:list --path=dashboard 2>&amp;1 | grep -c "Dashboard"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 57: 20/20 verdes
    - Phase 58: 16/16 verdes (5+4+2+5)
    - route:list mostra 5 rotas nomeadas com metodos DashboardController
    - Zero mudanca de arquivo nesta task (git diff --name-only HEAD retorna vazio)
  </acceptance_criteria>
  <done>Nenhum commit adicional. Contagem final 36 tests verdes (20 Phase57 + 16 Phase58) documentada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NAV_TREE (client JSON) → route() Ziggy | routeName gerado server-side pelo Ziggy; sidebar renderiza links so pra usuarios autenticados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-06 | Broken access control | Item ECF Consolidado no NAV_TREE | mitigate | Campo `permission: 'core.dashboard'` reusa gate ja existente. Filtragem em `filteredNav` (AppLayout.jsx) respeita `hasPermission`. |
| T-58-07 | Availability regression | Rename Dashboard → Mercado Livre confunde users | accept | Rota canonical `dashboard` permanece registrada (Plan 58-01 preserva). Deep links + bookmarks funcionam. Comunicacao pos-deploy em release note. |
| T-58-SC | Supply Chain | Nenhum pacote novo | N/A | Zero install. Icones `PieChart` e `LayoutDashboard` ja importados em AppLayout.jsx (lucide-react ja em package.json). |
</threat_model>

<verification>
- `grep -c "ecf.dashboard" resources/js/Layouts/AppLayout.jsx` >= 1
- `grep -c "mercadolivre.dashboard" resources/js/Layouts/AppLayout.jsx` >= 1
- Grep filtrado de comentario: `grep -v '^\s*//' resources/js/Layouts/AppLayout.jsx | grep -c "routeName: 'dashboard'"` == 0
- `npm run build` compila sem erros
- Suite Phase 58: 16 verdes; suite Phase 57: 20 verdes (baseline preservado)
- `php artisan route:list --path=dashboard` lista as 5 rotas
</verification>

<success_criteria>
1. DASH-01 (E2E): sidebar mostra "ECF Consolidado" no topo do grupo Mercado Livre, clique navega para `/dashboard/ecf` e renderiza componente Dashboard/Admin
2. DASH-02 (E2E): sidebar mostra "Mercado Livre" no lugar do antigo Dashboard, clique navega para `/dashboard/mercadolivre`
3. DASH-03 (E2E): items estaticos Shopee/Amazon do sidebar (ja existentes — nao alterados nesta plan) apontam para `/em-desenvolvimento`; MAS as novas rotas `/dashboard/shopee` e `/dashboard/amazon` renderizam os shells JSX do Plan 58-02 quando acessadas diretamente
4. Rota legacy `/dashboard` continua ativa (compat)
5. Zero regressao Phase 57 (20 tests baseline)
6. Suite Phase 58 fecha em 16 tests verdes
</success_criteria>

<output>
Criar `.planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-03-SUMMARY.md` documentando:
- Contagem final: 36 tests verdes (20 Phase57 + 16 Phase58)
- Estado do NAV_TREE apos mudanca (before/after visual)
- Confirmacao de zero regressao
- Nota sobre items Shopee/Amazon estaticos existentes: NAO tocados (continuam apontando para /em-desenvolvimento — decisao consciente; adicionar novos items para /dashboard/shopee e /dashboard/amazon fica pra decisao futura no proximo touchpoint UAT)
- Preparo para UAT em prod: 4 URLs para o usuario validar visualmente
</output>
