---
phase: 58
plan: 58-02
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/Pages/Dashboard/ShopeeShell.jsx
  - resources/js/Pages/Dashboard/AmazonShell.jsx
autonomous: true
requirements: [DASH-03]

must_haves:
  truths:
    - "resources/js/Pages/Dashboard/ShopeeShell.jsx existe e exporta default um componente React que recebe props marketplace e label"
    - "resources/js/Pages/Dashboard/AmazonShell.jsx existe e exporta default um componente React que recebe props marketplace e label"
    - "Ambos shells usam AppLayout como wrapper e Head para o titulo"
    - "Layout tem 4 KPI cards placeholder (GMV, Vendas, ROAS, Sellers) com valor '—' e label 'Aguardando integracao'"
    - "Card explicativo com icone Construction (lucide-react) + CTA Link href={route('ecf.dashboard')} + label 'Ver Dashboard ECF Consolidado'"
    - "Design usa tokens ecf-* (bg-ecf-card, ecf-yellow, border white/[0.06|0.08]), classe cn() do @/lib/utils, dark theme consistente com EmDesenvolvimento.jsx"
    - "Icones de marca via <img src=\"/images/shopee-icon.svg\"> e <img src=\"/images/icons8-amazon.svg\"> (arquivos ja existem Phase 56)"
    - "Build Vite (npm run build) roda sem erros nem warnings de JSX invalido"
  artifacts:
    - path: "resources/js/Pages/Dashboard/ShopeeShell.jsx"
      provides: "Componente Inertia Dashboard/ShopeeShell renderizado pelo controller Plan 58-01"
      contains: "export default function ShopeeShell"
      min_lines: 60
    - path: "resources/js/Pages/Dashboard/AmazonShell.jsx"
      provides: "Componente Inertia Dashboard/AmazonShell renderizado pelo controller Plan 58-01"
      contains: "export default function AmazonShell"
      min_lines: 60
  key_links:
    - from: "resources/js/Pages/Dashboard/ShopeeShell.jsx"
      to: "route('ecf.dashboard')"
      via: "Link href={route('ecf.dashboard')} — CTA para o dashboard consolidado"
      pattern: "route\\('ecf\\.dashboard'\\)"
    - from: "resources/js/Pages/Dashboard/AmazonShell.jsx"
      to: "route('ecf.dashboard')"
      via: "Link href={route('ecf.dashboard')} — CTA para o dashboard consolidado"
      pattern: "route\\('ecf\\.dashboard'\\)"
    - from: "Ambos shells"
      to: "@/Layouts/AppLayout + @/lib/utils cn"
      via: "wrapper padrao do sistema; import padronizado do projeto"
      pattern: "@/Layouts/AppLayout"
---

<objective>
Criar os 2 componentes React shell (`ShopeeShell.jsx` + `AmazonShell.jsx`) que o `DashboardController::shopee` e `::amazon` (Plan 58-01) renderizam via Inertia. Layout consistente com design system ECF: header com marca, 4 KPI cards placeholder mockup e card explicativo com CTA para `route('ecf.dashboard')`.

Purpose: Cobrir DASH-03 (UI). Comunicar visualmente ao usuario que ha marketplaces em roadmap sem enganar com KPIs zerados. Reusar padroes visuais ja estabelecidos (EmDesenvolvimento.jsx da Phase 56).

Output: 2 componentes JSX novos, build Vite sem erros. Paralelo com Plan 58-01 (zero file overlap — este toca so JSX; Plan 01 so PHP).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-CONTEXT.md
@resources/js/Pages/EmDesenvolvimento.jsx
@resources/js/Layouts/AppLayout.jsx

<interfaces>
<!-- Contratos-chave para o executor. Extraidos do codebase. -->

Path alias Vite (vite.config.js): `@` -> `resources/js/`.

Wrapper padrao (resources/js/Layouts/AppLayout.jsx):
- Import: `import AppLayout from '@/Layouts/AppLayout';`
- Uso: <AppLayout title="Dashboard Shopee">...</AppLayout>

Utility de classes (resources/js/lib/utils.js):
- Import: `import { cn } from '@/lib/utils';`

Inertia:
- Import: `import { Head, Link } from '@inertiajs/react';`
- Uso Link: <Link href={route('ecf.dashboard')} className="...">

Icones (lucide-react):
- Import: `import { Construction, BarChart3 } from 'lucide-react';`

Icones de marca ja publicados (Phase 56):
- Shopee: /images/shopee-icon.svg
- Amazon: /images/icons8-amazon.svg

Design tokens (tailwind.config.js theme.extend.colors.ecf):
- bg-ecf-bg (#050507), bg-ecf-card (#0f1116), text-ecf-yellow (#ffe600)
- Bordas: border-white/[0.06], border-white/[0.08]
- Backgrounds sutis: bg-white/[0.03], bg-white/[0.08]

Contract das props recebidas do controller (Plan 58-01):
- ShopeeShell recebe: { marketplace: 'shopee', label: 'Shopee' }
- AmazonShell recebe: { marketplace: 'amazon', label: 'Amazon' }

Padrao existente comparavel (resources/js/Pages/EmDesenvolvimento.jsx):
- 78 linhas, export default function EmDesenvolvimento({ marketplace })
- AppLayout wrapper + Head + card centralizado com Construction icon
- Usa Link href={route('dashboard')} — nosso CTA vai apontar para route('ecf.dashboard')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar Dashboard/ShopeeShell.jsx</name>
  <read_first>
    - resources/js/Pages/EmDesenvolvimento.jsx (padrao visual comparavel)
    - resources/js/Layouts/AppLayout.jsx linhas 1-100 (imports canonicos usados no projeto)
    - .planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-CONTEXT.md secao 3 (design proposto — spec do layout)
  </read_first>
  <files>resources/js/Pages/Dashboard/ShopeeShell.jsx</files>
  <action>
Criar novo componente React em `resources/js/Pages/Dashboard/ShopeeShell.jsx` seguindo EXATAMENTE o design proposto no CONTEXT §3, com comentarios pt-BR.

Estrutura obrigatoria do componente:

1. Imports (topo do arquivo, uma linha cada bloco):
   - `import { Head, Link } from '@inertiajs/react';`
   - `import { Construction, BarChart3 } from 'lucide-react';`
   - `import AppLayout from '@/Layouts/AppLayout';`
   - `import { cn } from '@/lib/utils';`

2. Docblock JSX em pt-BR no topo (antes do export) descrevendo:
   - "Phase 58 v13.0 DASH-03 — Shell 'Dashboard Shopee'. Renderizado pelo DashboardController::shopee (Plan 58-01). Mockup com KPI cards placeholder + CTA para Dashboard ECF Consolidado. Design consistente com EmDesenvolvimento.jsx (Phase 56)."

3. Export default: `export default function ShopeeShell({ marketplace, label })` — desestruturar as 2 props que o controller passa.

4. Body do componente com esta hierarquia visual (usar cn() para composicao):
   - <AppLayout title={`Dashboard ${label}`}>
     - <Head title={`${label} em desenvolvimento`} />
     - <div className="p-6 space-y-6"> (container geral, spacing consistente com EmDesenvolvimento)
       - Header block: flex items-center gap-3
         - <img src="/images/shopee-icon.svg" className="w-8 h-8" alt={label} />
         - <h1 className="text-white text-2xl font-display font-bold">Dashboard {label}</h1>
         - Badge "Em desenvolvimento" com ml-auto text-xs bg-white/[0.08] text-white/60 px-2 py-1 rounded-full
       - Grid de 4 KPI cards placeholder: div grid grid-cols-2 md:grid-cols-4 gap-4 (usar breakpoint mobile-friendly)
         - Array literal `['GMV', 'Vendas', 'ROAS', 'Sellers'].map(kpi => ...)` renderiza cada card com:
           - Container div: cn('bg-ecf-card border border-white/[0.06] rounded-xl p-4')
           - Label KPI: text-white/40 text-xs {kpi}
           - Valor mockup: text-white/20 text-2xl font-bold mt-2 "—"
           - Sublabel: text-white/30 text-[10px] mt-1 "Aguardando integracao"
           - key={kpi} obrigatorio (React key warning)
       - Card explicativo: bg-ecf-card border border-white/[0.06] rounded-xl p-8 text-center
         - Icone: <Construction size={48} className="mx-auto text-ecf-yellow mb-4" />
         - Titulo: <h2 className="text-white text-lg font-semibold mb-2">Integracao {label} em desenvolvimento</h2>
         - Paragrafo pt-BR: "Estamos preparando o pipeline de dados {label} — API, sync, metricas agregadas. Enquanto isso, o Dashboard ECF Consolidado ja mostra os resultados totais das empresas."
         - CTA <Link href={route('ecf.dashboard')} className={cn('inline-flex items-center gap-2 rounded-lg px-4 py-2.5 bg-ecf-yellow text-[#252525] text-[13px] font-semibold hover:bg-ecf-yellow/90 transition-colors')}>
           - <BarChart3 size={16} />
           - Ver Dashboard ECF Consolidado

Total esperado: ~65-75 linhas. Sem lint warnings de key/prop.

NAO adicionar import de `useState`/`useEffect` (componente puramente presentational). NAO passar `title` na prop de AppLayout se AppLayout nao aceita (checar quick — se AppLayout aceita `title` seguir; caso contrario passar via <Head title>).
  </action>
  <verify>
    <automated>test -f resources/js/Pages/Dashboard/ShopeeShell.jsx &amp;&amp; grep -c "export default function ShopeeShell" resources/js/Pages/Dashboard/ShopeeShell.jsx</automated>
    <automated>grep -c "route('ecf.dashboard')" resources/js/Pages/Dashboard/ShopeeShell.jsx</automated>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em resources/js/Pages/Dashboard/ShopeeShell.jsx
    - Exporta default `ShopeeShell` (1 ocorrencia via grep)
    - Contem `route('ecf.dashboard')` (1 ocorrencia via grep)
    - Contem `/images/shopee-icon.svg` (icone marca)
    - Contem os 4 KPI labels: GMV, Vendas, ROAS, Sellers
    - `npm run build` completa sem erros nem warnings de JSX (vite build passa 0 errors)
    - Comentarios/textos em pt-BR
  </acceptance_criteria>
  <done>Commit `feat(58-02): DASH-03 — Dashboard/ShopeeShell.jsx mockup com KPI cards + CTA` isolado desta task.</done>
</task>

<task type="auto">
  <name>Task 2: Criar Dashboard/AmazonShell.jsx</name>
  <read_first>
    - resources/js/Pages/Dashboard/ShopeeShell.jsx (recem criado — usar como template base)
    - .planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-CONTEXT.md secao 3 (mesmo layout, so muda icone/label)
  </read_first>
  <files>resources/js/Pages/Dashboard/AmazonShell.jsx</files>
  <action>
Criar `resources/js/Pages/Dashboard/AmazonShell.jsx` com estrutura IDENTICA ao ShopeeShell.jsx (mesmos imports, mesma hierarquia, mesmas classes), trocando apenas:

1. Nome do componente: `export default function AmazonShell({ marketplace, label })`

2. Docblock: "Phase 58 v13.0 DASH-03 — Shell 'Dashboard Amazon'. Renderizado pelo DashboardController::amazon (Plan 58-01). Layout espelha ShopeeShell.jsx — muda apenas icone de marca e label."

3. Icone: `<img src="/images/icons8-amazon.svg" className="w-8 h-8" alt={label} />` (imagem ja publicada Phase 56)

4. Textos permanecem parametrizados via prop `{label}` — codigo agnostico do marketplace concreto.

5. CTA identico — Link href={route('ecf.dashboard')} + BarChart3 + "Ver Dashboard ECF Consolidado".

Justificativa da duplicacao (nao extrair pra componente comum agora): o CONTEXT §3 explicitamente escolhe duplicar 2 shells simples ao inves de abstrair pra 1 componente parametrico + 2 wrappers — v14+ pode divergir (Amazon tem SP-API, Shopee tem Open Platform, KPIs vao diferir). Manter simples e duplicado ate os shells virarem dashboards reais.
  </action>
  <verify>
    <automated>test -f resources/js/Pages/Dashboard/AmazonShell.jsx &amp;&amp; grep -c "export default function AmazonShell" resources/js/Pages/Dashboard/AmazonShell.jsx</automated>
    <automated>grep -c "route('ecf.dashboard')" resources/js/Pages/Dashboard/AmazonShell.jsx</automated>
    <automated>grep -c "/images/icons8-amazon.svg" resources/js/Pages/Dashboard/AmazonShell.jsx</automated>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe em resources/js/Pages/Dashboard/AmazonShell.jsx
    - Exporta default `AmazonShell` (1 ocorrencia via grep)
    - Contem `route('ecf.dashboard')` (1 ocorrencia)
    - Contem `/images/icons8-amazon.svg` (icone Amazon)
    - Contem os 4 KPI labels: GMV, Vendas, ROAS, Sellers
    - `npm run build` completa sem erros nem warnings
    - Zero mudanca fora de resources/js/Pages/Dashboard/ (`git diff --name-only HEAD` limitado a Dashboard/ShopeeShell.jsx e Dashboard/AmazonShell.jsx)
  </acceptance_criteria>
  <done>Commit `feat(58-02): DASH-03 — Dashboard/AmazonShell.jsx mockup espelhando Shopee` isolado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller (server) → props (JSON serializado) | Props `marketplace` e `label` sao constantes hardcoded no controller (nao vem de request) — confiavel |
| JSX render → DOM | Todos os textos passam via React (auto-escape); imagens sao SVGs estaticos publicos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | XSS (Injection) | Reflexao das props `marketplace` e `label` no JSX | accept | Props vem de valores hardcoded server-side (`'shopee'`, `'Shopee'`, `'amazon'`, `'Amazon'` em DashboardController Plan 58-01). React auto-escapa. Sem `dangerouslySetInnerHTML`. |
| T-58-05 | Broken auth | Rota renderiza shell publicamente | mitigate | Rotas ficam DENTRO do grupo `auth+verified` (Plan 58-01). Componentes JSX so sao acessiveis apos login. |
| T-58-SC | Tampering (Supply Chain) | npm installs | N/A | Zero pacote novo. Todos os imports (react, @inertiajs/react, lucide-react) ja existem em package.json. |
</threat_model>

<verification>
- Ambos os arquivos existem em resources/js/Pages/Dashboard/
- `grep -c "export default function" resources/js/Pages/Dashboard/{Shopee,Amazon}Shell.jsx` retorna 1 em cada
- `grep -rn "route('ecf.dashboard')" resources/js/Pages/Dashboard/` >= 2 (1 por shell)
- `grep -c "cn(" resources/js/Pages/Dashboard/{Shopee,Amazon}Shell.jsx` >= 1 em cada (uso do utility)
- `npm run build` completa sem erros (o build da Phase 58 vai reusar bundle Vite existente)
- Zero warning de "unused import" ou "missing key" no build
</verification>

<success_criteria>
1. DASH-03 (UI): shells ShopeeShell.jsx e AmazonShell.jsx existem e sao componentes React validos
2. Layout consistente: header com marca, 4 KPI cards placeholder ("—"), card CTA para ecf.dashboard
3. Design system respeitado: tokens ecf-* (ecf-card, ecf-yellow), cn() utility, dark theme
4. Build Vite verde (0 erro, 0 warning JSX)
5. Comentarios em pt-BR conforme convencao do projeto
</success_criteria>

<output>
Criar `.planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-02-SUMMARY.md` documentando: os 2 componentes criados, decisao de duplicacao ao inves de extracao (justificada em Task 2), status do build Vite (esperado 0 erros), e handoff pra Plan 03 (NAV_TREE ja pode referenciar Dashboard/ShopeeShell e Dashboard/AmazonShell via campo `page`).
</output>
