---
phase: 58
plan: 58-01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/web.php
  - app/Http/Controllers/DashboardController.php
  - tests/Feature/Phase58/DashboardRoutesTest.php
  - tests/Feature/Phase58/DashboardFilterTest.php
  - tests/Feature/Phase58/DashboardShellsBackendTest.php
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "GET /dashboard/ecf retorna 200 (autenticado admin) e renderiza o mesmo componente Inertia Dashboard/Admin do /dashboard atual"
    - "GET /dashboard/mercadolivre retorna 200 e delega ao mesmo pipeline com marketplace=meli forcado"
    - "GET /dashboard/shopee retorna 200 e responde componente Inertia Dashboard/ShopeeShell com props marketplace=shopee e label=Shopee"
    - "GET /dashboard/amazon retorna 200 e responde componente Inertia Dashboard/AmazonShell com props marketplace=amazon e label=Amazon"
    - "GET /dashboard (legacy) continua retornando 200 para admin (sem regressao no fallback canonical)"
    - "Query param ?marketplace=shopee|amazon|meli e validado contra whitelist; valores fora do whitelist recebem 422"
    - "Zero regressao nos 20 testes Phase 57 (DashboardSmokeTest, SugadoresSmokeTest, PerformanceSmokeTest, helpers e accessors)"
  artifacts:
    - path: "routes/web.php"
      provides: "4 rotas GET nomeadas (ecf.dashboard, mercadolivre.dashboard, shopee.dashboard, amazon.dashboard) dentro do grupo auth+verified"
      contains: "ecf.dashboard"
    - path: "app/Http/Controllers/DashboardController.php"
      provides: "4 metodos publicos novos (ecf, mercadolivre, shopee, amazon) + filter opcional ?marketplace= no companiesQuery"
      contains: "public function ecf"
    - path: "tests/Feature/Phase58/DashboardRoutesTest.php"
      provides: "4 tests que exercitam as 4 rotas novas + 1 test que garante /dashboard legacy continua ativo"
    - path: "tests/Feature/Phase58/DashboardFilterTest.php"
      provides: "Tests do whitelist do ?marketplace= (aceita meli/shopee/amazon, rejeita valores fora do whitelist)"
    - path: "tests/Feature/Phase58/DashboardShellsBackendTest.php"
      provides: "Tests de assinatura Inertia dos shells (component name + props marketplace/label)"
  key_links:
    - from: "routes/web.php"
      to: "DashboardController::ecf|mercadolivre|shopee|amazon"
      via: "Route::get('/dashboard/{slug}', [DashboardController::class, method])"
      pattern: "ecf\\.dashboard|mercadolivre\\.dashboard|shopee\\.dashboard|amazon\\.dashboard"
    - from: "DashboardController::mercadolivre"
      to: "DashboardController::index (via $request->merge(['marketplace' => 'meli']))"
      via: "delegacao interna que preserva o pipeline existente"
      pattern: "\\$request->merge"
    - from: "DashboardController::adminDashboard"
      to: "Company::query com when(marketplace, ...)"
      via: "filtro opcional que so aplica quando o request valida o whitelist"
      pattern: "->when\\(.*marketplace"
---

<objective>
Adicionar as 4 rotas novas do modelo multi-marketplace + 4 metodos correspondentes no `DashboardController` + filtro opcional `?marketplace=` validado por whitelist, cobrindo DASH-01 (rota ECF), DASH-02 (rota ML) e DASH-03 (rotas shell Shopee/Amazon — backend).

Purpose: Preparar o pipeline para renderizar dashboards por marketplace SEM tocar na logica de agregacao existente (adminDashboard/userDashboard preservados; o filtro e OPCIONAL via `when()`). Os shells renderizam direto pra evitar KPIs zerados enganosos.

Output: 5 arquivos modificados/criados. Rotas nomeadas prontas para o Plan 03 (NAV_TREE) referenciar. Testes Feature verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-CONTEXT.md
@.planning/phases/57-modelo-de-dados-multi-marketplace/PHASE-SUMMARY.md
@routes/web.php
@app/Http/Controllers/DashboardController.php
@tests/Feature/Phase57/DashboardSmokeTest.php

<interfaces>
<!-- Contratos-chave para o executor. Extraidos do codebase — nao explorar. -->

Rota canonical existente (routes/web.php linha 138, grupo auth+verified):
Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');

Assinatura publica do controller (app/Http/Controllers/DashboardController.php):
- public function __construct(private AdmanService $adman) {}
- public function index(Request $request) — decide entre adminDashboard, userDashboard ou redirect Publicacao
- private function adminDashboard(Request $request, Carbon $since, string $period) — linha 104-731
- private function userDashboard(User $user, Carbon $since, string $period) — linha 732+
- private function getSince(string $period): Carbon
- private function getPeriodRange(string $period): array

Ponto onde o filter marketplace entra em adminDashboard (linhas 106-134):
- $companyFilter/$consultorFilter/$estrategistaFilter/$grupoFilter ja sao lidos via $request->get
- $companiesQuery = Company::with([...])->where('active', true)->whereDoesntHave('mlbEmpresa')->whereHas('contratosServico', ...)
- Depois vem: if ($companyFilter) $companiesQuery->where('id', $companyFilter); ... encadeados

Ponto onde o filter marketplace entra em userDashboard (linha 734):
- $companies = $user->companies()->with(['latestMetrics', 'goals'])->get();
- Coluna `companies.marketplace` existe (Phase 18.5) e tem indice; usar `->where('marketplace', $mp)` e o caminho recomendado no CONTEXT §2.

Model N:N Phase 57 (nao usar aqui — CONTEXT §2 explicita filter por coluna flat por performance):
- CompanyMarketplace tem colunas marketplace ENUM('meli','shopee','amazon','magalu')

Shells devem renderizar diretamente via Inertia::render sem passar pelo pipeline (CONTEXT §2 racional):
- Inertia::render('Dashboard/ShopeeShell', ['marketplace' => 'shopee', 'label' => 'Shopee'])
- Inertia::render('Dashboard/AmazonShell', ['marketplace' => 'amazon', 'label' => 'Amazon'])

Whitelist obrigatorio para o filter (threat model T-58-01):
- Aceita: 'meli', 'shopee', 'amazon'
- Rejeita: qualquer outro valor (retornar 422 via $request->validate)

Padrao de test Phase 57 (tests/Feature/Phase57/DashboardSmokeTest.php):
- Cria User factory role=admin + Company factory + CompanyMarketplace factory
- $this->actingAs($admin)->get('/dashboard')
- $this->assertLessThan(500, $response->status())
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature tests RED (rotas + filter + shells backend)</name>
  <read_first>
    - tests/Feature/Phase57/DashboardSmokeTest.php (padrao factory + actingAs + assertLessThan 500)
    - app/Models/User.php (User::factory ja usado em Phase 57)
    - app/Models/Company.php (Company::factory ja usado em Phase 57)
    - app/Models/CompanyMarketplace.php (CompanyMarketplace::factory ja usado em Phase 57)
    - .planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-CONTEXT.md secoes 1, 2 e 3
  </read_first>
  <files>tests/Feature/Phase58/DashboardRoutesTest.php, tests/Feature/Phase58/DashboardFilterTest.php, tests/Feature/Phase58/DashboardShellsBackendTest.php</files>
  <action>
Criar 3 arquivos de teste PHP Feature namespace `Tests\Feature\Phase58`, todos usando `Illuminate\Foundation\Testing\RefreshDatabase` (mesmo padrao do Phase 57 DashboardSmokeTest.php).

Arquivo 1 — DashboardRoutesTest.php (5 tests obrigatorios):
- test_dashboard_ecf_route_retorna_200: cria User admin + Company meli, actingAs, GET route('ecf.dashboard'), assertOk (nao 5xx).
- test_dashboard_mercadolivre_route_retorna_200: identico com route('mercadolivre.dashboard').
- test_dashboard_shopee_route_retorna_200: identico com route('shopee.dashboard').
- test_dashboard_amazon_route_retorna_200: identico com route('amazon.dashboard').
- test_dashboard_legacy_route_continua_ativa: GET '/dashboard' com admin autenticado, assertOk — garante rota canonical fallback preservada (per CONTEXT §5).

Arquivo 2 — DashboardFilterTest.php (4 tests obrigatorios):
- test_filter_marketplace_meli_aceito: GET route('ecf.dashboard', ['marketplace' => 'meli']), assertOk.
- test_filter_marketplace_shopee_aceito: GET route('ecf.dashboard', ['marketplace' => 'shopee']), assertOk.
- test_filter_marketplace_amazon_aceito: GET route('ecf.dashboard', ['marketplace' => 'amazon']), assertOk.
- test_filter_marketplace_invalido_rejeitado: GET route('ecf.dashboard', ['marketplace' => 'evil<script>']), assertStatus(422) — threat model T-58-01 (injection whitelist gate).

Arquivo 3 — DashboardShellsBackendTest.php (2 tests obrigatorios):
- test_shopee_shell_renderiza_componente_e_props: GET route('shopee.dashboard'), usar `assertInertia(fn (\Inertia\Testing\AssertableInertia $page) => $page->component('Dashboard/ShopeeShell')->where('marketplace', 'shopee')->where('label', 'Shopee'))`. Import `Inertia\Testing\AssertableInertia`.
- test_amazon_shell_renderiza_componente_e_props: analogo para 'Dashboard/AmazonShell', marketplace=amazon, label=Amazon.

Todos os tests usam User::factory()->create(['role' => 'admin']) + Company::factory()->create(['marketplace' => 'meli']) (mesmo pattern Phase 57). Rodar RED antes de commit — precisa falhar (rotas nao existem ainda).

Comentarios em pt-BR no topo da classe explicando: "Phase 58 — Feature tests das rotas dashboard multi-marketplace. RED antes das rotas serem implementadas na Task 2."
  </action>
  <verify>
    <automated>php artisan test --filter=Phase58 2>&amp;1 | tail -20 (deve mostrar 11 tests falhando com "Route [ecf.dashboard] not defined" ou similar — RED esperado)</automated>
  </verify>
  <acceptance_criteria>
    - Arquivos existem em tests/Feature/Phase58/
    - Cada arquivo tem namespace `Tests\Feature\Phase58`, usa RefreshDatabase
    - 11 tests declarados no total (5 + 4 + 2)
    - Rodando os testes retorna FALHA (RED — rotas nao existem)
    - Zero test do Phase 57 fica quebrado por esta task (0 diff em tests/Feature/Phase57/*)
  </acceptance_criteria>
  <done>Commit `test(58-01): Wave 1 RED — feature tests rotas + filter + shells backend (11 tests)` com 3 arquivos novos, nenhum arquivo modificado fora de tests/Feature/Phase58/.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — 4 rotas nomeadas + 4 metodos controller + filter whitelist</name>
  <read_first>
    - routes/web.php linhas 135-148 (grupo auth+verified onde entra a rota atual /dashboard)
    - app/Http/Controllers/DashboardController.php linhas 1-140 (imports + index + adminDashboard header) e linhas 730-740 (userDashboard header)
    - .planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-CONTEXT.md secoes 1, 2 e 5
  </read_first>
  <files>routes/web.php, app/Http/Controllers/DashboardController.php</files>
  <action>
Passo A — routes/web.php (dentro do grupo `Route::middleware(['auth', 'verified'])` que ja envolve linha 138):
Adicionar EXATAMENTE apos a linha `Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');` (nao remover essa linha — CONTEXT §5 exige preservar) as 4 rotas nomeadas:
- HTTP verb GET, path `/dashboard/ecf`, controller `DashboardController::class`, method `ecf`, route name `ecf.dashboard`
- HTTP verb GET, path `/dashboard/mercadolivre`, controller `DashboardController::class`, method `mercadolivre`, route name `mercadolivre.dashboard`
- HTTP verb GET, path `/dashboard/shopee`, controller `DashboardController::class`, method `shopee`, route name `shopee.dashboard`
- HTTP verb GET, path `/dashboard/amazon`, controller `DashboardController::class`, method `amazon`, route name `amazon.dashboard`

Bloco deve levar comentario pt-BR no topo: "Phase 58 v13.0 — Rotas Dashboard multi-marketplace. `/dashboard` legacy preservada como canonical fallback (CONTEXT §5)."

Passo B — DashboardController.php (adicionar 4 metodos publicos novos DEPOIS de `index()` e ANTES de `getSince()`; nao tocar em `index`, `adminDashboard`, `userDashboard`, `getSince`, `getPeriodRange`):

Metodo `public function ecf(Request $request)`:
- Comentario pt-BR: "Phase 58 DASH-01 — Dashboard ECF agregado atraves de marketplaces. Hoje delega ao pipeline existente sem filter (100% empresas meli); v14+ vai agregar via CompanyMarketplace pivot."
- Valida o whitelist do request via $request->validate (Passo D).
- return $this->index($request);

Metodo `public function mercadolivre(Request $request)`:
- Comentario pt-BR: "Phase 58 DASH-02 — Dashboard Mercado Livre. Forca filter marketplace=meli via merge no request; delega ao pipeline existente."
- $request->merge(['marketplace' => 'meli']);
- Valida whitelist depois do merge.
- return $this->index($request);

Metodo `public function shopee(Request $request)`:
- Comentario pt-BR: "Phase 58 DASH-03 — Shell Shopee. Renderiza direto (bypass pipeline) para evitar KPIs zerados enganosos (CONTEXT §2)."
- return Inertia::render('Dashboard/ShopeeShell', ['marketplace' => 'shopee', 'label' => 'Shopee']);

Metodo `public function amazon(Request $request)`:
- Comentario pt-BR: "Phase 58 DASH-03 — Shell Amazon. Renderiza direto (bypass pipeline) para evitar KPIs zerados enganosos (CONTEXT §2)."
- return Inertia::render('Dashboard/AmazonShell', ['marketplace' => 'amazon', 'label' => 'Amazon']);

Passo C — DashboardController::adminDashboard() (imediatamente apos leitura dos filtros existentes em linha ~110, ANTES de `$companyFilter = ...`):
Adicionar leitura do filter marketplace: `$marketplaceFilter = $request->get('marketplace');`
Depois do bloco de encadeamento de filtros (apos `if ($grupoFilter) { $companiesQuery->where('company_group_id', $grupoFilter); }` em linha ~134), adicionar:
- Filtro opcional aplicado no companiesQuery: `if ($marketplaceFilter) { $companiesQuery->where('marketplace', $marketplaceFilter); }`
- Filtro opcional aplicado tambem em `$allCompanies` (linha ~450) para manter o dropdown consistente com o universo filtrado.
- Comentario pt-BR: "Phase 58 DASH-01/02 — Filter marketplace opcional. Usa coluna flat companies.marketplace (indice existente Phase 18.5) por performance; migracao pra pivot CompanyMarketplace fica pra v14+ (CONTEXT §2)."

Passo D — Validacao whitelist (threat model T-58-01, ASVS L1):
Em `ecf()` e `mercadolivre()`, antes do `return $this->index($request)`:
`$request->validate(['marketplace' => 'nullable|string|in:meli,shopee,amazon']);`
Isso garante que query param evil como `?marketplace=evil<script>` retorna 422 automatico via ValidationException do Laravel (sem precisar de try/catch — comportamento default).

Passo E — userDashboard: MUDANCA MINIMA. Como usuarios comuns ja tem carteira restrita, o filter tem baixo impacto pratico. Adicionar apenas apos linha 734 (`$companies = $user->companies()->with([...])->get();`) ANTES do pluck:
Se `request()->marketplace` presente e no whitelist, filtrar `$companies = $companies->where('marketplace', request()->marketplace);` (filter em Collection, ja em memoria — sem re-query). Comentario pt-BR curto explicando.

Rodar `php artisan route:list --name=dashboard` para conferir os 5 nomes (`dashboard`, `ecf.dashboard`, `mercadolivre.dashboard`, `shopee.dashboard`, `amazon.dashboard`).
  </action>
  <verify>
    <automated>php artisan route:list --path=dashboard 2>&amp;1 | grep -E "ecf.dashboard|mercadolivre.dashboard|shopee.dashboard|amazon.dashboard" | grep -v '^#' | wc -l</automated>
    <automated>php artisan test --filter=Phase58 --stop-on-failure 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan route:list --path=dashboard` lista as 5 rotas (dashboard, ecf.dashboard, mercadolivre.dashboard, shopee.dashboard, amazon.dashboard) — grep sem `#` retorna 4 para as 4 novas
    - Os 11 tests da Task 1 passam (GREEN)
    - Grep em DashboardController.php: `grep -c "public function ecf" app/Http/Controllers/DashboardController.php` == 1; idem para `mercadolivre`, `shopee`, `amazon`
    - Grep whitelist: `grep -c "in:meli,shopee,amazon" app/Http/Controllers/DashboardController.php` >= 2 (usado em ecf + mercadolivre)
    - Grep filter aplicado no companiesQuery: `grep -c "marketplace.*\$marketplaceFilter" app/Http/Controllers/DashboardController.php` >= 1
    - Zero mudanca fora dos 2 arquivos declarados: `git diff --name-only HEAD` deve conter apenas routes/web.php + DashboardController.php (alem dos 3 test files da Task 1 se ainda nao commitados)
  </acceptance_criteria>
  <done>Commit `feat(58-01): Wave 1 GREEN — 4 rotas + 4 metodos + filter marketplace whitelist` com 11 tests Phase58 verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Gate de zero regressao Phase 57 + suite Sugadores/Performance</name>
  <read_first>
    - .planning/phases/57-modelo-de-dados-multi-marketplace/PHASE-SUMMARY.md (Wave 2 lista os 20 tests baseline)
  </read_first>
  <files>(nenhum arquivo modificado — apenas rodar suites)</files>
  <action>
Executar as suites baseline para provar zero regressao introduzida pelas mudancas em Task 2:

1. `php artisan test --filter=Phase57` — deve manter os 20 tests verdes (helpers + accessors + backfill + DashboardSmoke + SugadoresSmoke + PerformanceSmoke). Se algum falhar, INVESTIGAR (nao suprimir): provavelmente o filter opcional foi mal aplicado e quebrou pipeline default.

2. `php artisan test --filter=Phase58` — deve manter os 11 tests verdes (Task 2 GREEN).

3. `php artisan test tests/Feature/DashboardTest.php` (se existir) — outros tests de dashboard nao Phase57/58; deve continuar verde.

4. Grep sanity: garantir que o metodo `index()` original nao foi modificado alem da aceitacao passiva de `?marketplace=` no request:
   - `git diff HEAD~1 app/Http/Controllers/DashboardController.php | grep -E "^-.*public function index"` deve ser vazio (assinatura de index intacta).
   - `git diff HEAD~1 app/Http/Controllers/DashboardController.php | grep -E "^-.*private function (getSince|getPeriodRange|adminDashboard|userDashboard)"` deve ser vazio (assinaturas privadas intactas).

Se todos os passos verdes → registrar contagem final em summary. Se algum falhar → NAO avancar; retornar diagnostico ao orquestrador (deviation policy Rule 1 — reportar antes de "consertar").
  </action>
  <verify>
    <automated>php artisan test --filter=Phase57 2>&amp;1 | tail -5</automated>
    <automated>php artisan test --filter=Phase58 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Phase 57 suite: 20/20 verdes (baseline preservado)
    - Phase 58 suite: 11/11 verdes (todos os tests GREEN)
    - Assinaturas de `index`, `adminDashboard`, `userDashboard`, `getSince`, `getPeriodRange` intactas (grep sanity)
  </acceptance_criteria>
  <done>Nenhum commit adicional (apenas validacao). Registrar em SUMMARY.md contagem 20+11=31 tests verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Route/Controller | Query string `?marketplace=<slug>` cruza a boundary — input nao confiavel |
| Sidebar/link JSX → route helper | Route params gerados server-side via Ziggy (confiavel — mesma boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Tampering (Injection) | `?marketplace=` no ecf/mercadolivre/adminDashboard | mitigate | `$request->validate(['marketplace' => 'nullable\|string\|in:meli,shopee,amazon'])` em `ecf()` e `mercadolivre()`; controller so aplica `where('marketplace', $mp)` apos validacao. Test `test_filter_marketplace_invalido_rejeitado` cobre. |
| T-58-02 | Elevation of Privilege | Rotas novas `/dashboard/{ecf,mercadolivre,shopee,amazon}` | mitigate | Rotas adicionadas DENTRO do grupo `Route::middleware(['auth', 'verified'])` existente (linha 135 web.php). Mesma protecao da rota canonical `/dashboard`. Sem mudanca no `EnsureUserHasRole`. |
| T-58-03 | Information Disclosure | Shell props `label` reflete valor server-side ('Shopee'/'Amazon') no JSX | accept | Label vem do controller (constante hardcoded), nao do request. React auto-escapa via JSX render. Sem risco de XSS. |
| T-58-SC | Tampering (Supply Chain) | npm/composer installs | N/A | Zero pacotes novos nesta plan (backend puro Laravel + tests PHPUnit ja existentes). |
</threat_model>

<verification>
- `php artisan route:list --path=dashboard` mostra as 5 rotas nomeadas (dashboard, ecf.dashboard, mercadolivre.dashboard, shopee.dashboard, amazon.dashboard)
- 11 tests em `tests/Feature/Phase58/` verdes (5 rotas + 4 filter + 2 shells backend)
- 20 tests baseline Phase 57 continuam verdes
- Grep `grep -c "in:meli,shopee,amazon" app/Http/Controllers/DashboardController.php` >= 2 (whitelist ativo em ecf + mercadolivre)
- Grep `grep -c "->where('marketplace'" app/Http/Controllers/DashboardController.php` >= 1 (filter aplicado no companiesQuery)
</verification>

<success_criteria>
1. DASH-01 (backend): rota `ecf.dashboard` retorna 200 renderizando o mesmo componente Dashboard/Admin do pipeline atual
2. DASH-02 (backend): rota `mercadolivre.dashboard` retorna 200 com marketplace=meli forcado — hoje output identico ao ECF por 100% empresas serem meli
3. DASH-03 (backend): rotas `shopee.dashboard` e `amazon.dashboard` renderizam componente Inertia Dashboard/ShopeeShell e Dashboard/AmazonShell com props corretas
4. Rota legacy `/dashboard` continua funcionando (compat de deep links per CONTEXT §5)
5. Whitelist do filter marketplace rejeita valores fora de {meli, shopee, amazon} com 422
6. Zero regressao no baseline Phase 57 (20 tests verdes preservados)
</success_criteria>

<output>
Criar `.planning/phases/58-dashboard-ecf-agregado-shells-por-marketplace/58-01-SUMMARY.md` documentando: contagem final de tests (31 verdes: 20 Phase57 + 11 Phase58), lista de rotas registradas, arquivos modificados, notas de handoff pra Plan 02 (shells JSX ainda nao existem — testes de shell backend assertam so componente name via Inertia contract, nao renderizacao real) e Plan 03 (NAV_TREE pode referenciar `route('ecf.dashboard')` e `route('mercadolivre.dashboard')`).
</output>
