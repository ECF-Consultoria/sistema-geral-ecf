---
phase: 103-carteira-por-periodo-v18-0
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/PortfolioController.php
  - tests/Feature/V18/CarteiraPeriodoDiffTest.php
  - tests/Feature/V16/CarteiraFinanceiroElegibilidadeTest.php
  - tests/Feature/V16/CarteiraIndividualContextoTest.php
  - tests/Feature/V16/CarteirasConsolidadasContextoTest.php
autonomous: true
requirements: [CAR-01, CAR-02, CAR-03]

must_haves:
  truths:
    - "renderCarteiraProfissional resolve período via MetricPeriodResolver — nenhum now()/subMonth()/endOfMonth() inline para montar janela"
    - "?mes=YYYY-MM é traduzido para period_key do resolver (current_month quando é o mês corrente, YYYY-MM caso contrário)"
    - "margem_variacao_pct por empresa vem de AdmanMetricDiffService.contribution_margin_value.diff_pct (NÃO contribution_margin_pct)"
    - "Mês em curso permanece byte-idêntico ao comportamento anterior (variação e totais de faturamento/ad_spend)"
    - "Mês fechado usa baseline janela-de-mesmo-tamanho (não mês calendário anterior inteiro)"
    - "Elegibilidade financeira v17 preservada: Shopee sem fonte não entra; dedup por company_id único"
    - "payload.periodo expõe current_start/current_end/baseline_start/baseline_end do resolver"
  artifacts:
    - path: "app/Http/Controllers/PortfolioController.php"
      provides: "renderCarteiraProfissional consumindo periodResolver + admanDiffService"
      contains: "periodResolver->resolve"
    - path: "tests/Feature/V18/CarteiraPeriodoDiffTest.php"
      provides: "Golden mês fechado + gate adman_diff + byte-idêntico mês em curso + Shopee sem fonte"
      exports: []
  key_links:
    - from: "app/Http/Controllers/PortfolioController.php::renderCarteiraProfissional"
      to: "MetricPeriodResolver::resolve"
      via: "resolução única de período"
      pattern: "periodResolver->resolve"
    - from: "app/Http/Controllers/PortfolioController.php::renderCarteiraProfissional"
      to: "AdmanMetricDiffService::compute"
      via: "variação de margem via contribution_margin_value"
      pattern: "admanDiffService->compute"
---

<objective>
Migrar `renderCarteiraProfissional()` (carteira individual do profissional) para
resolver o período via `MetricPeriodResolver` (Fase 100) e derivar a variação de
margem por empresa via `AdmanMetricDiffService` (Fase 101), espelhando o transplante
que a Fase 102 já fez no `DesempenhoScoreService` — com UM desvio crítico: a carteira
consome `contribution_margin_value.diff_pct` (variação da margem R$), NÃO
`contribution_margin_pct` que a 102 usou pro score. Confundir os dois muda
silenciosamente um número já auditado 3× (Tomelin/Gabriela/LOJASINVAL).

Purpose: CAR-01/CAR-02/CAR-03 na carteira individual — período resolvido por regra
única, soma nas janelas do resolver, variação de margem via diff Adman quando
disponível, elegibilidade v17 intacta, coerência de janela entre todos os blocos.
Output: `renderCarteiraProfissional` refatorado + suíte V18 nova + isolamento HTTP
nos testes V16 existentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-carteira-por-periodo-v18-0/103-RESEARCH.md
@.planning/REQUIREMENTS-v18.md

<interfaces>
<!-- Contratos que o executor precisa — extraídos do código-fonte. Usar direto,
     sem explorar o codebase. -->

MetricPeriodResolver::resolve(array $filtros): array
  - App\Services\Metrics\MetricPeriodResolver
  - $filtros['period_key'] ∈ {'current_month','last_closed_month','YYYY-MM','custom'}
  - Retorna (14 chaves, strings Y-m-d inclusivas):
      mode, period_key, current_start, current_end, baseline_start, baseline_end,
      days_count, comparison_mode, timezone, data_fresh_until,
      bonus_payment_month, bonus_competence_month, is_current_month, is_closed
  - current_month → comparison_mode='same_interval_previous_month' (baseline
    alinhada por dia no mês anterior — IDÊNTICA ao bloco atual da carteira em curso)
  - YYYY-MM → comparison_mode='previous_equal_length_window' (baseline = N dias
    imediatamente anteriores a current_start — MUDA vs o mês-calendário-anterior atual)

AdmanMetricDiffService::compute(Company $company, array $periodo): array
  - App\Services\Metrics\AdmanMetricDiffService (fail-open, cacheado 24h)
  - $periodo = objeto INTEIRO do resolver (precisa comparison_mode + as 4 datas)
  - Retorno['metrics'] tem 3 chaves:
      'revenue', 'contribution_margin_value', 'contribution_margin_pct'
    cada uma = ['value'=>?float, 'diff_pct'=>?float, 'diff_source'=>?string]
  - Gate ADM-02: comparison_mode='previous_equal_length_window' E .diff nativo
    presente ⇒ diff_source='adman_diff'; caso contrário 'calculated_fallback'
    (SUM local com guards de dias-comuns — mesma matemática do bloco atual da carteira)
  - ⚠ USAR 'contribution_margin_value' (mapeado de profitMargin.diff — variação %
    do valor R$) — sucessor do margem_variacao_pct atual. NUNCA 'contribution_margin_pct'
    (percentageMargin.diff — métrica diferente, é a que a 102 usou pro score).

Anatomia ATUAL de renderCarteiraProfissional (PortfolioController.php):
  - Constructor injeta: AdmanService, DesempenhoScoreService, MetricsProviderFactory,
    CarteiraContextService (linhas 27-32)
  - Bloco de período inline a SUBSTITUIR: linhas 148-176 (if($ehMesEmCurso){...}else{...})
  - Bloco de dias-comuns + variação a SUBSTITUIR pelo diff service: linhas 355-416
  - CarteiraContextService::forUser()/contadores() — elegibilidade v17 — INTOCADO
  - $companyIdsElegiveis (dedup por company_id único elegível) — INTOCADO
  - payload.periodo atual (display) — linhas 549-558: em_curso/mes_selecionado/
    meses_disponiveis/dia_atual/dias_no_mes/mes_label/range_atual/range_anterior
</interfaces>

@app/Http/Controllers/PortfolioController.php
@tests/Feature/V18/DesempenhoPeriodoOficialTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold da suíte V18 (RED) — golden mês fechado + gate adman_diff + byte-idêntico + Shopee sem fonte</name>
  <files>tests/Feature/V18/CarteiraPeriodoDiffTest.php</files>
  <behavior>
    ISOLAMENTO HTTP OBRIGATÓRIO (decisão travada 6): setUp com
    `Http::preventStrayRequests()` + fakes determinísticos. tearDown reseta
    `Carbon::setTestNow()`. Espelhar o padrão de DesempenhoPeriodoOficialTest
    (helpers semearDiario / fakeAdmanSemDiff / fakeAdmanComDiff). Todos os testes
    fixam `Carbon::setTestNow('2026-07-20 14:00:00')`. Empresas elegíveis via
    contrato Performance + pivot company_users (padrão criarEmpresaElegivel).

    - Test A (CAR-01/CAR-03): GET portfolio.show com ?mes ausente → payload.periodo
      contém current_start/current_end/baseline_start/baseline_end; em 20/07 o modo
      é current_month (current_start=2026-07-01, current_end=2026-07-20,
      baseline 2026-06-01..2026-06-20).
    - Test B (CAR-01, golden mês fechado): GET ?mes=2026-05 → payload.periodo.
      current_start=2026-05-01, current_end=2026-05-31, baseline_start=2026-03-31,
      baseline_end=2026-04-30 (janela-de-mesmo-tamanho — 31 dias imediatamente
      anteriores a 01/05). Trava o Pitfall 2: baseline NÃO é 01-30/04.
    - Test C (CAR-02, Pitfall 1): mês fechado ?mes=2026-05, fakeAdmanComDiff onde
      profitMargin.diff=15.0 E percentageMargin.diff=99.0 (valores distintos).
      Assert empresas[0].margem_variacao_pct === 15.0 (lê contribution_margin_value,
      NÃO os 99.0 de contribution_margin_pct). Prova a escolha de campo.
    - Test D (CAR-02, fallback): mês fechado, fakeAdmanSemDiff, fixture DENSA
      (semearDiario) current 01-31/05 margem 300/rev 1000 e baseline 31/03-30/04
      margem 250/rev 1000 → margem_variacao_pct = (300-250)/250*100 = +20.00
      (calculated_fallback sobre SUM local).
    - Test E (byte-idêntico mês em curso): ?mes ausente (current_month),
      fixture DENSA SEM gaps cobrindo 01-20/07 (rev 1000/margem 300/dia) e
      01-20/06 (rev 1000/margem 240/dia) → margem_variacao_pct:
      atual=SUM margem 20 dias=6000, anterior=4800 → (6000-4800)/4800 = +25.00; total_faturamento e
      variacao_margem_pct do resumo idênticos ao cálculo manual (prova operacional
      = fallback = comportamento anterior).
    - Test F (elegibilidade v17): empresa com vínculo SÓ Shopee (sem contrato
      Performance) → aparece em empresas[] com margem_contribuicao=null e
      margem_variacao_pct=null; resumo.total_faturamento=0.0. Preserva
      financial_metrics_eligible=false; diff service NÃO é chamado pra ela.

    Os testes DEVEM falhar (RED) antes da Task 2 — o payload.periodo ainda não
    expõe current_start/baseline_start e a variação ainda vem do bloco inline.
  </behavior>
  <action>Criar tests/Feature/V18/CarteiraPeriodoDiffTest.php cobrindo Tests A-F acima. Reaproveitar os helpers de fixture do DesempenhoPeriodoOficialTest (criarUserAnalista via user_setores+cargo, criarEmpresaElegivel com contrato_servico+company_users, semearDiario denso, fakeAdmanSemDiff, e um fakeAdmanComDiff que devolve profitMargin.diff em */performance/* summarizedData e percentageMargin.diff em */accounts/*/metrics*). Empresa Shopee via inserirLinhaShopee/CriaCenarioResponsaveis (padrão V16) SEM contrato Performance. Fixar Carbon::setTestNow em todos. Assert sobre viewData('page')['props'].</action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CarteiraPeriodoDiffTest</automated>
  </verify>
  <done>Arquivo existe; suíte roda e FALHA nos asserts de payload.periodo.current_start/baseline_start e margem_variacao_pct (RED esperado antes da implementação).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar renderCarteiraProfissional com resolver + diff service (GREEN)</name>
  <files>app/Http/Controllers/PortfolioController.php</files>
  <action>
No constructor (linhas 27-32), adicionar injeção de `MetricPeriodResolver $periodResolver` e `AdmanMetricDiffService $admanDiffService` (namespaces App\Services\Metrics\*). Adicionar os `use` correspondentes. NÃO tocar em nenhuma outra função do arquivo (sessão paralela ativa — só renderCarteiraProfissional).

Em renderCarteiraProfissional: manter o parse de `?mes=` (regex whitelist `/^\d{4}-\d{2}$/`) e `$mesSelecionado`/`$mesCorrente`/`$ehMesEmCurso` (linhas 148-157). SUBSTITUIR o bloco if/else de datas (159-176) por UMA resolução: `$periodo = $ehMesEmCurso ? periodResolver->resolve(['period_key'=>'current_month']) : periodResolver->resolve(['period_key'=>$mesSelecionado->format('Y-m')])`. Derivar `$dateFrom=$periodo['current_start']`, `$dateTo=$periodo['current_end']`, `$dateFromPrev=$periodo['baseline_start']`, `$dateToPrev=$periodo['baseline_end']`.

Variação de margem por empresa: REMOVER o bloco de dias-comuns + fallback manual (355-416) que recalcula margemAtual/margemAnterior e $margemVarPct. No lugar, para cada empresa ELEGÍVEL, chamar `admanDiffService->compute($c, $periodo)` e ler `$margemVarPct = $resultado['metrics']['contribution_margin_value']['diff_pct'] ?? null`. Usar SEMPRE `contribution_margin_value` (per D-Pitfall1) — NUNCA `contribution_margin_pct`. O diff service já encapsula os guards de dias-comuns e o gate adman_diff/fallback. Comentário pt-BR explicando a escolha de campo. O display absoluto (`margem_contribuicao`/`margem_contribuicao_anterior`) permanece lendo os SUMs de janela `$atualPorEmpresa`/`$anteriorPorEmpresa` (info factual de volume) — só a VARIAÇÃO troca de fonte.

Passar o objeto Company ($c/$rawCompanies) ao diff service (ele precisa de adman_account_id/marketplace). $atualPorEmpresa/$anteriorPorEmpresa continuam consultados nas janelas do resolver ($dateFrom..$dateTo / $dateFromPrev..$dateToPrev). motivo_sem_margem/ad_spend/tacos/revenue permanecem como hoje (cache Adman batch + fallback SUM).

payload.periodo (549-558): PRESERVAR os campos de display existentes; ADICIONAR as chaves do resolver: current_start, current_end, baseline_start, baseline_end, mode, comparison_mode, is_current_month, is_closed (CAR-03 — cards/séries leem a janela resolvida). Derivar range_atual/range_anterior a partir das datas do resolver. NÃO construir seletor/toggle novo (isso é Fase 104).
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CarteiraPeriodoDiffTest</automated>
  </verify>
  <done>Suíte V18 verde (Tests A-F). payload.periodo expõe as 4 datas do resolver; margem_variacao_pct vem de contribution_margin_value; mês fechado usa baseline janela-de-mesmo-tamanho; mês em curso byte-idêntico; Shopee sem fonte não entra.</done>
</task>

<task type="auto">
  <name>Task 3: Isolamento HTTP nos testes V16 que alcançam a carteira individual (regressão verde)</name>
  <files>tests/Feature/V16/CarteiraFinanceiroElegibilidadeTest.php, tests/Feature/V16/CarteiraIndividualContextoTest.php, tests/Feature/V16/CarteirasConsolidadasContextoTest.php</files>
  <action>
Após a Task 2, renderCarteiraProfissional passa a chamar `admanDiffService->compute()` por empresa elegível (que faz leitura ao vivo à Adman, fail-open). Sem fake, os testes V16 que atingem `portfolio.show` com empresa elegível disparam requisição REAL (decisão travada 6). Adicionar no setUp() dos 3 arquivos: `Http::preventStrayRequests()` + `Http::fake(['*/performance/*'=>Http::response([],404), '*/accounts/*/metrics*'=>Http::response([],404)])` (força calculated_fallback determinístico, sem rede). Adicionar `use Illuminate\Support\Facades\Http;`. Os 3 arquivos seedam SEMPRE no mês corrente (`now()->startOfMonth()->addDays(2)`) e NENHUM asserta margem_variacao_pct/variacao_margem_pct/total_margem_anterior — logo os NÚMEROS não mudam (operacional = fallback = comportamento anterior); o ajuste é PURO isolamento HTTP, não recálculo de golden. Se algum número mudar, PARAR e reportar (indica regressão real, não ajuste esperado). NÃO alterar asserts de valor.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Carteira</automated>
  </verify>
  <done>Todos os testes V16 (Carteira*) + V18 verdes; nenhuma requisição real à Adman; nenhum assert de valor V16 alterado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query string (`?mes=`, `?contexto=`) → renderCarteiraProfissional | input não confiável do cliente cruza aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Tampering | `?mes=` repassado ao MetricPeriodResolver | mitigate | whitelist regex `/^\d{4}-\d{2}$/` já existente (linha 152) ANTES de montar period_key; string fora do formato cai em current_month, nunca vira period_key cru (resolver lança InvalidArgumentException → evitar 500) |
| T-103-02 | Information Disclosure | enumeração de carteira alheia via `?mes=` | accept | autorização em `show()` (isAdmin/próprio/líder do setor) é resolvida ANTES de renderCarteiraProfissional; nenhum vetor novo introduzido pela troca de período |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=CarteiraPeriodoDiffTest` verde (A-F)
- `C:\xampp\php\php.exe artisan test --filter=Carteira` verde (V16 + V18)
- Grep de sanidade: `renderCarteiraProfissional` não contém mais `subMonth()`/`endOfMonth()`/`->setDay(min(` para montar janela (só o resolver resolve período)
</verification>

<success_criteria>
- CAR-01: renderCarteiraProfissional resolve período via MetricPeriodResolver; mês fechado não usa now()/mês em curso
- CAR-02: variação de margem vem de contribution_margin_value.diff_pct (diff Adman quando disponível, fallback marcado); elegibilidade v17 preservada (Shopee sem fonte não entra)
- CAR-03: payload.periodo expõe current_start/current_end/baseline_start/baseline_end; todos os blocos leem a janela resolvida
</success_criteria>

<output>
Criar `.planning/phases/103-carteira-por-periodo-v18-0/103-01-SUMMARY.md` ao concluir.
Documentar no SUMMARY: (a) baseline do modo fechado MUDOU (janela-de-mesmo-tamanho) — consequência conhecida, igual à Fase 102; (b) validação numérica em produção pendente (Tomelin/Gabriela/LOJASINVAL) já que não há UI/checkpoint visual nesta fase (é a Fase 104); (c) renderPortfolio permanece com período inline (known-gap fora de CAR-01, não tocado); (d) **N+1 HTTP aceito como dívida técnica** — a carteira passa a chamar `AdmanMetricDiffService::compute()` por empresa e o PortfolioController NÃO tem cache próprio (diferente do DesempenhoScoreService::computeCached); cold-cache pode disparar dezenas de chamadas síncronas. Mitigado pelo cache 24h interno do diff service (amortiza cargas subsequentes). Follow-up de batching fica pra depois se medição em prod acusar lentidão.
</output>
