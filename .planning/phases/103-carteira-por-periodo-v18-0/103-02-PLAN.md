---
phase: 103-carteira-por-periodo-v18-0
plan: 02
type: tdd
wave: 2
depends_on: ["103-01"]
files_modified:
  - app/Http/Controllers/PortfolioController.php
  - tests/Feature/V18/CarteiraConsolidadaPeriodoTest.php
autonomous: true
requirements: [CAR-01, CAR-03]

must_haves:
  truths:
    - "renderCarteirasConsolidadas resolve período via MetricPeriodResolver (janela atual do resolver para as somas)"
    - "As somas financeiras (revenue/ad_spend/margem) usam a janela current_start..current_end do resolver, não now()->subDays() rolante"
    - "payload da consolidada expõe periodo (shape do resolver: current_start/current_end/baseline_start/baseline_end)"
    - "Mês em curso: as somas permanecem byte-idênticas (mesmo total_revenue) ao comportamento anterior no dia da execução"
    - "Nenhuma variação/baseline nova adicionada à consolidada (avg_margin inalterado; escopo mínimo)"
    - "Elegibilidade financeira v17 e dedup por company_id preservados (Shopee sem fonte não herda financeiro)"
  artifacts:
    - path: "app/Http/Controllers/PortfolioController.php"
      provides: "renderCarteirasConsolidadas com janela do resolver + payload periodo"
      contains: "periodResolver->resolve"
    - path: "tests/Feature/V18/CarteiraConsolidadaPeriodoTest.php"
      provides: "Janela do resolver + payload periodo + somas byte-idênticas mês em curso"
      exports: []
  key_links:
    - from: "app/Http/Controllers/PortfolioController.php::renderCarteirasConsolidadas"
      to: "MetricPeriodResolver::resolve"
      via: "resolução única de período para as somas"
      pattern: "periodResolver->resolve"
---

<objective>
Migrar `renderCarteirasConsolidadas()` (visão admin/líder — cards por profissional)
para resolver o período via `MetricPeriodResolver` e somar as métricas na janela
atual do resolver, expondo `periodo` no payload. Escopo MÍNIMO (decisão travada 3):
a consolidada hoje usa `?period=1/7/30/180` (janela rolante em DIAS, SEM baseline);
esta fase troca a FONTE do período para o resolver e expõe `periodo` — NÃO adiciona
chip de variação nem baseline por card (isso é UI/Fase 104). `avg_margin` permanece
inalterado.

Purpose: CAR-01 (renderCarteirasConsolidadas resolve período via resolver; mês
fechado não usa now()) + CAR-03 (payload carrega periodo; coerência de janela).
Output: `renderCarteirasConsolidadas` refatorado + suíte V18 nova.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-carteira-por-periodo-v18-0/103-RESEARCH.md
@.planning/REQUIREMENTS-v18.md

<interfaces>
<!-- Contratos que o executor precisa. Usar direto, sem explorar o codebase. -->

MetricPeriodResolver::resolve(array $filtros): array
  - App\Services\Metrics\MetricPeriodResolver (já injetado no constructor pela Task 2 do 103-01)
  - period_key='current_month' → current_start=01/mês, current_end=hoje (data_fresh_until)
  - period_key='YYYY-MM' → mês calendário completo, baseline janela-de-mesmo-tamanho
  - Datas retornadas como strings Y-m-d inclusivas.

Anatomia ATUAL de renderCarteirasConsolidadas (PortfolioController.php:595-841):
  - Período inline a SUBSTITUIR: linhas 597-604 ($period=?period; $days=match; $since=now()->subDays)
    e 646-647 ($dateFrom=$since->toDateString(); $dateTo=now()->toDateString())
  - Somas por card (linhas 745-750): AdmanMetric SUM(revenue)/SUM(ad_spend)/AVG(contribution_margin_pct)
    filtrado por `->where('reference_date','>=',$dateFrom)` (SÓ limite inferior hoje)
  - CarteiraContextService::forUser()/contadores() — elegibilidade/dedup v17 — INTOCADO
  - $companyIdsElegiveis (dedup por company_id único elegível) — INTOCADO
  - payload atual (835-840): user_portfolios, period, contexto, totais
  - NÃO chama AdmanMetricDiffService (sem variação por card nesta fase)

Rota: portfolio.own (admin → consolidada; líder → consolidada filtrada por setor).
</interfaces>

@app/Http/Controllers/PortfolioController.php
@tests/Feature/V16/CarteirasConsolidadasContextoTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold V18 consolidada (RED) — janela do resolver + payload periodo + somas byte-idênticas</name>
  <files>tests/Feature/V18/CarteiraConsolidadaPeriodoTest.php</files>
  <behavior>
    Fixar `Carbon::setTestNow('2026-07-20 14:00:00')` (determinismo de janela).
    tearDown reseta setTestNow. Não precisa de Http::fake (a consolidada não chama
    o diff service) — mas incluir `Http::preventStrayRequests()` no setUp por higiene.
    Cenário: admin + 1 analista (via user_setores/cargo) com empresa elegível
    (contrato Performance + pivot company_users) e AdmanMetric semeado.

    - Test A (CAR-03): GET portfolio.own → payload contém `periodo` com
      current_start/current_end/baseline_start/baseline_end; sem ?mes → current_month
      (current_start=2026-07-01, current_end=2026-07-20).
    - Test B (CAR-01, byte-idêntico mês em curso): AdmanMetric semeado em 2026-07-10
      (dentro da janela current_month) revenue=1000 → card.total_revenue=1000.0
      (a janela current_start..current_end soma esse registro; mesmo número que a
      lógica rolante daria no dia 20).
    - Test C (escopo mínimo): assert que o card NÃO ganhou chave de variação/baseline
      nova (ex.: assertArrayNotHasKey de 'margem_variacao_pct'/'avg_margin_anterior'
      no card) e que `avg_margin` continua presente com a semântica atual
      (AVG(contribution_margin_pct)).
    - Test D (mês fechado, CAR-01): GET portfolio.own?mes=2026-05 → payload.periodo
      resolve closed_period (current_start=2026-05-01, current_end=2026-05-31); somas
      restritas a maio (registro de julho NÃO entra).

    RED esperado: o payload ainda não expõe `periodo` e a soma usa now()->subDays().
  </behavior>
  <action>Criar tests/Feature/V18/CarteiraConsolidadaPeriodoTest.php. Reaproveitar helpers do CarteirasConsolidadasContextoTest (vincularCargo via user_setores, criarContrato/inserirPivot, seedAdmanMetric com reference_date explícito). Fixar Carbon::setTestNow. Assert sobre viewData('page')['props'] (user_portfolios, periodo). Localizar card por id.</action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CarteiraConsolidadaPeriodoTest</automated>
  </verify>
  <done>Arquivo existe; suíte roda e FALHA nos asserts de payload.periodo e da janela fechada (RED antes da implementação).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar renderCarteirasConsolidadas com janela do resolver (GREEN)</name>
  <files>app/Http/Controllers/PortfolioController.php</files>
  <action>
Em renderCarteirasConsolidadas (só esta função — sessão paralela ativa): SUBSTITUIR o bloco de período rolante (597-604 e 646-647) pela resolução via `$this->periodResolver` (já injetado no constructor pela Task 2 do 103-01). Regra: se `?mes=` presente e casa `/^\d{4}-\d{2}$/` E é passado, resolver `period_key=$mes` (closed_period); caso contrário `current_month` (operacional, default preserva comportamento). Derivar `$dateFrom=$periodo['current_start']`, `$dateTo=$periodo['current_end']`.

Ajustar a query de somas (linha 745-750): trocar `->where('reference_date','>=',$dateFrom)` por `->whereBetween('reference_date',[$dateFrom,$dateTo])` (janela fechada dos dois lados — coerência CAR-03). Manter SUM(revenue)/SUM(ad_spend)/AVG(contribution_margin_pct) e todo o resto (cache gross/account, dedup $companyIdsElegiveis, contadores, source_counts) INALTERADO. NÃO adicionar baseline/variação por card (decisão travada 3 — escopo mínimo).

payload (835-840): MANTER `period` (echo legado — a Carteiras.jsx ainda lê até a Fase 104) e ADICIONAR `periodo` = resultado do resolver (current_start/current_end/baseline_start/baseline_end/mode/comparison_mode/is_current_month/is_closed). Comentário pt-BR marcando que o seletor visual rolante (1/7/30/180) da .jsx é substituído pela janela mensal do resolver — mismatch UI intencional, resolvido na Fase 104 (known-gap 103↔104).
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CarteiraConsolidadaPeriodoTest</automated>
  </verify>
  <done>Suíte V18 consolidada verde (A-D); somas na janela do resolver; payload expõe periodo; avg_margin inalterado; sem variação nova por card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query string (`?mes=`, `?period=`, `?contexto=`) → renderCarteirasConsolidadas | input não confiável do cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-03 | Tampering | `?mes=` repassado ao MetricPeriodResolver na consolidada | mitigate | whitelist regex `/^\d{4}-\d{2}$/` antes de montar period_key; fora do formato cai em current_month, nunca string crua ao resolver (evita InvalidArgumentException→500) |
| T-103-04 | Information Disclosure | consolidada de setor alheio via portfolio.own | accept | autorização em `own()` (isAdmin / líder do próprio setor via setoresLiderados) resolvida ANTES do render; nenhum vetor novo |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=CarteiraConsolidadaPeriodoTest` verde (A-D)
- `C:\xampp\php\php.exe artisan test --filter=Carteira` verde (V16 + V18 individual + V18 consolidada)
- Grep de sanidade: `renderCarteirasConsolidadas` não contém mais `now()->subDays(` para montar janela
</verification>

<success_criteria>
- CAR-01: renderCarteirasConsolidadas resolve período via MetricPeriodResolver; mês fechado (?mes=) não usa now()/janela rolante
- CAR-03: payload da consolidada carrega periodo (janelas); somas coerentes na janela current_start..current_end
- Escopo mínimo preservado: sem variação/baseline nova por card; avg_margin inalterado; elegibilidade v17 e dedup intactos
</success_criteria>

<output>
Criar `.planning/phases/103-carteira-por-periodo-v18-0/103-02-SUMMARY.md` ao concluir.
Documentar: (a) known-gap 103↔104 — o seletor rolante 1/7/30/180 da Carteiras.jsx
continua exibido mas o backend agora usa janela mensal do resolver; a UI é rewired na
Fase 104; (b) consolidada permanece SEM variação de margem por card (decisão de escopo
mínimo — adição de baseline por card é candidata a fase futura, não CAR-01..03).
</output>
