---
phase: 89-carteira-individual-rendercarteiraprofissional-por-contexto-
plan: 02
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - app/Models/Company.php
  - app/Http/Controllers/CompanyController.php
  - tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php
autonomous: false
requirements: [CART-08]
must_haves:
  truths:
    - "/companies mostra na coluna Analista/Estrategista o responsável do SERVIÇO DE PERFORMANCE — nunca o responsável Shopee, mesmo quando a linha Shopee da pivot é mais ANTIGA (ordem que derrubava o ->first() ingênuo)"
    - "/companies/{id} idem, com shape array preservado"
    - "A pendência 'sem_responsavel' acusa empresa que tem estrategista-Shopee mas NÃO tem analista de Performance (regra OR sobre as relações novas — a lista de pendências CRESCE, comportamento confirmado pelo usuário)"
    - "Empresa cujo único serviço de performance é Mentoria (id ≠ Gestão) resolve responsável normalmente — filtro por servicos.setor, sem hardcode de id"
    - "Os 9 call-sites de Company::consultor()/estrategista() (DashboardController, GoalController, Goal, CalculateGoalResults, MeetingController, HubspotWebhookController, NpsDispararMensal, ComercialController) permanecem intocados"
  artifacts:
    - path: "app/Models/Company.php"
      provides: "Relações novas analistaPerformance()/estrategistaPerformance() (setor performance via subquery + ramo legado servico_id null)"
      contains: "analistaPerformance"
    - path: "app/Http/Controllers/CompanyController.php"
      provides: "index()/show() reapontados para as relações novas, chaves de payload consultor/estrategista preservadas, pendência OR"
      contains: "analistaPerformance"
    - path: "tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php"
      provides: "Testes CART-08 (index + show + pendência OR + Mentoria + legado)"
      min_lines: 80
  key_links:
    - from: "app/Http/Controllers/CompanyController.php"
      to: "Company::analistaPerformance()"
      via: "with()/load() + ->first()/->map() no payload"
      pattern: "analistaPerformance"
    - from: "app/Http/Controllers/CompanyController.php"
      to: "pendência sem_responsavel"
      via: "OR sobre analistaPerformance/estrategistaPerformance"
      pattern: "analistaPerformance->isEmpty\\(\\) \\|\\|"
    - from: "app/Models/Company.php"
      to: "servicos.setor"
      via: "whereIn subquery + whereExists contratos_servico (ramo legado)"
      pattern: "SETOR_PERFORMANCE"
---

<objective>
CART-08: criar as relações `analistaPerformance()`/`estrategistaPerformance()` no model Company (filtro por `servicos.setor='performance'` via subquery, SEM hardcode de id, cobrindo também o ramo legado `servico_id NULL` + contrato performance ativo — espelhando as 2 fontes do CarteiraContextService em granularidade de EMPRESA) e reapontar SÓ `CompanyController::index()` e `::show()` para elas, trocando a pendência `sem_responsavel` de AND para OR. Fecha com o checkpoint visual da fase (as duas telas).

Purpose: hoje `Company::consultor()` mistura responsável ML e Shopee na mesma Collection — o `->first()` pode mostrar o responsável Shopee na coluna Analista de um painel 100% Performance, e a pendência AND deixou ~7 empresas sem analista invisíveis.
Output: leitura de responsáveis correta em /companies, relações antigas preservadas para os 9 call-sites consolidados, fase verificada visualmente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-carteira-individual-rendercarteiraprofissional-por-contexto-/89-RESEARCH.md
@app/Models/Company.php
@app/Http/Controllers/CompanyController.php
@tests/Feature/V16/CriaCenarioResponsaveis.php
@tests/Feature/V16/LeitoresConsolidadoRegressaoTest.php

<interfaces>
<!-- Contratos que o executor usa diretamente. -->

Precedente no próprio Company.php (linhas 197-209) — NÃO serve direto (servico_id fixo), mas dá o padrão:
```php
public function consultorDoServico(int $servicoId)
{
    return $this->belongsToMany(User::class, 'company_users')
        ->wherePivot('role', 'consultor')
        ->wherePivot('servico_id', $servicoId);
}
```

Relação nova (esqueleto do RESEARCH §CART-08, validado contra o schema real):
```php
public function analistaPerformance()
{
    return $this->belongsToMany(User::class, 'company_users')
        ->wherePivot('role', 'consultor')
        ->where(function ($q) {
            $q->whereIn('company_users.servico_id', function ($sub) {
                $sub->select('id')->from('servicos')->where('setor', Servico::SETOR_PERFORMANCE);
            })->orWhere(function ($q2) {
                $q2->whereNull('company_users.servico_id')
                   ->whereExists(function ($sub2) {
                       $sub2->select(DB::raw(1))
                            ->from('contratos_servico as ct')
                            ->join('servicos as s2', 's2.id', '=', 'ct.servico_id')
                            ->whereColumn('ct.company_id', 'company_users.company_id')
                            ->where('ct.ativo', true)
                            ->where('s2.setor', Servico::SETOR_PERFORMANCE);
                   });
            });
        })
        ->distinct('users.id');
}
// estrategistaPerformance(): idêntico com wherePivot('role', 'estrategista')
```
Company.php NÃO importa DB hoje — adicionar `use Illuminate\Support\Facades\DB;` (Servico é do mesmo namespace App\Models, sem import).
`->distinct('users.id')` (com argumento, nunca boolean) — garante COUNT(DISTINCT users.id), mesmo racional do comentário em consultor() (linhas 164-176).

Pontos exatos de reapontamento em CompanyController:
- index(): with(['consultor','estrategista', ...]) linhas 98-100 → relações novas; linhas 158-159 `$c->consultor->first()?->only([...])` → `$c->analistaPerformance->first()?->only([...])` (CHAVE do payload continua 'consultor'/'estrategista' — Companies/Index.jsx INTOCADO);
- index() linha 190 pendência: `($c->consultor->isEmpty() && $c->estrategista->isEmpty())` → `($c->analistaPerformance->isEmpty() || $c->estrategistaPerformance->isEmpty())`;
- show(): load(['consultor','estrategista', ...]) linhas 278-279 → relações novas; linhas 453-454 `$company->consultor->map->only([...])->values()` → `$company->analistaPerformance->map->only([...])->values()` (chaves preservadas, shape array preservado).
- update()/bulkAssign()/servicoPerformanceAtivoId() JÁ CORRETOS (Fase 76) — NÃO tocar.

Filtro de escopo do index(): a empresa só aparece em /companies se tiver contrato performance ativo
(whereHas contratosServico, linhas 113-118) — fixtures precisam de criarContrato(performance, ativo) sempre.
Rota de teste: GET /companies e GET /companies/{id} com actingAs(User::factory(['role' => 'admin'])).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relações analistaPerformance/estrategistaPerformance + suíte CART-08 (RED→GREEN)</name>
  <files>app/Models/Company.php, tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php</files>
  <behavior>
    CompanyControllerResponsavelPerformanceTest (RefreshDatabase + CriaCenarioResponsaveis, asserts via AssertableInertia em Companies/Index e Companies/Show):
    - Teste index anti-->first()-ingênuo: cenário ML (criarCenarioMlComResponsaveis) + analista Shopee DIFERENTE via inserirLinhaShopee, com a linha Shopee tendo created_at/assigned_at mais ANTIGO (update direto em company_users, padrão de LeitoresConsolidadoRegressaoTest:91-95 — ordem que faria um first() sem filtro devolver o Shopee) → GET /companies: companies.N.consultor.id === analista PERFORMANCE.
    - Teste show: mesma montagem → GET /companies/{id}: 'consultor' é ARRAY (shape preservado) contendo SÓ o analista de performance.
    - Teste pendência OR (comportamento novo, explícito): empresa com contrato performance ativo + estrategista SHOPEE apenas (nenhum vínculo performance) → pendencias contém 'sem_responsavel'. Contraprova: empresa com analista E estrategista de performance → pendencias NÃO contém 'sem_responsavel'. Segunda contraprova da mudança AND→OR: empresa com analista performance mas SEM estrategista performance → CONTÉM 'sem_responsavel' (antes, com AND, não continha).
    - Teste Mentoria: empresa cujo vínculo usa um SEGUNDO serviço de setor performance (criarServico('performance') extra — simula Mentoria, id ≠ Gestão) → analistaPerformance()->first() resolve o analista (sem hardcode de servico_id).
    - Teste legado CTX-05: inserirPivot(servico_id NULL) em empresa com contrato performance ativo → analistaPerformance() encontra o responsável; e em empresa SEM contrato performance ativo (só shopee) → NÃO encontra.
    - Teste invariante das relações antigas: após inserir linha Shopee, Company::consultor()->count() continua 1 e devolve o mesmo user (as relações consolidadas seguem existindo para os 9 call-sites).
  </behavior>
  <action>
    1. Escrever o teste primeiro e confirmar RED (relações não existem → BadMethodCallException/asserts falham).
    2. Adicionar em `app/Models/Company.php` (logo após estrategistaDoServico(), linha ~209) as 2 relações do esqueleto em interfaces, com PHPDoc pt-BR explicando: espelha as 2 fontes do CarteiraContextService (servico_id preenchido com setor performance OU servico_id NULL + contrato performance ativo) em granularidade de EMPRESA; nunca retorna responsável Shopee; consumidor exclusivo desta fase = CompanyController::index()/show(). Adicionar `use Illuminate\Support\Facades\DB;`.
    3. Rodar a parte da suíte que exercita as relações diretamente (Mentoria/legado/invariante) até GREEN; os testes de index/show/pendência continuam RED até a Task 2 — isso é esperado, documentar no commit.
  </action>
  <verify>
    <automated>php artisan test --filter=CompanyControllerResponsavelPerformance (testes de relação verdes; testes de controller ainda RED até Task 2) && php artisan test --filter=LeitoresConsolidadoRegressao</automated>
  </verify>
  <done>Relações novas existem com filtro por setor + ramo legado; testes de relação verdes; LeitoresConsolidadoRegressaoTest verde (relações antigas intactas). Commits `test(89-02)`/`feat(89-02)` em pt-BR.</done>
</task>

<task type="auto">
  <name>Task 2: Reapontar CompanyController::index()/show() + pendência OR + gate de fronteira</name>
  <files>app/Http/Controllers/CompanyController.php</files>
  <action>
    Aplicar EXATAMENTE os 4 reapontamentos listados em interfaces (with/first no index, pendência OR, load/map no show), preservando bit-a-bit as CHAVES 'consultor'/'estrategista' do payload — `Companies/Index.jsx` e `Companies/Show.jsx` NÃO podem mudar. Na pendência, comentário pt-BR registrando a decisão: "Fase 89 (CART-08) — AND→OR sobre as relações de Performance; a lista de pendências CRESCE (~7 empresas reais sem analista passam a aparecer) — comportamento desejado: essas empresas acumularam invisíveis justamente pelo AND". NÃO tocar em `calcularPendencias()` do HubspotWebhookController (espelha o AND antigo deliberadamente — divergência documentada, decisão própria dele em fase futura), nem em update()/bulkAssign() (escrita já correta, Fase 76). Rodar o gate de fronteira: `git diff --name-only main@{upstream}..HEAD` (ou desde o 1º commit da fase) não pode conter User.php, DesempenhoScoreService.php, DashboardController.php, GoalController.php, Goal.php, CalculateGoalResults.php, MeetingController.php, HubspotWebhookController.php, NpsDispararMensal.php, ComercialController.php, Companies/Index.jsx, Companies/Show.jsx.
  </action>
  <verify>
    <automated>php artisan test --filter=CompanyControllerResponsavelPerformance && php artisan test --filter=V16 && php artisan test --filter=Nps && git diff --name-only HEAD~3..HEAD | grep -vE "PortfolioController|AdminCarteira|Company\.php|CompanyController|tests/Feature/V16|\.planning" | wc -l (esperado: 0 ou só artefatos de build)</automated>
  </verify>
  <done>Suíte CART-08 100% verde; V16 completa verde; suíte Nps verde (protege dual-path do bônus/fallback NpsDispararMensal); fronteira limpa. Commit `feat(89-02): ...`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual da fase — carteira individual + /companies</name>
  <action>Apresentar ao usuário o roteiro de verificação abaixo e aguardar aprovação. Nenhum código novo — checkpoint verifica o que os planos 89-01 e 89-02 automatizaram.</action>
  <what-built>Fase 89 completa: carteira individual por contexto de serviço (badges de vínculo, "sem fonte financeira", financeiro deduplicado por elegibilidade) e /companies exibindo o responsável de Performance com pendência OR.</what-built>
  <how-to-verify>
    Pré-requisito: `npm run build` já rodado (plano 89-01). Nota: o MariaDB local está indisponível desde 2026-06-25 — se não houver ambiente local com dados, a verificação pode ser feita pós-deploy no VPS (deploy SÓ com autorização explícita, fora deste plano; confirmar caso-a-caso conforme regra da milestone com dev paralelo).

    1. Como admin, abrir a carteira de um profissional que tenha empresa com vínculo Performance + Shopee (`/admin/users/{id}/portfolio`):
       - A empresa aparece UMA vez, com 2 badges sob o nome ("Mercado Livre · Analista" e "Shopee · Analista — sem fonte financeira")
       - Faturamento total NÃO dobrou vs. antes (empresa conta 1x)
    2. Abrir a carteira de um profissional que só cuida de Shopee em alguma empresa que também tem ML:
       - A empresa aparece com badge Shopee e células "—" (tooltip "Sem fonte financeira conectada"), sem faturamento ML
    3. Abrir `/companies`:
       - Coluna Analista/Estrategista mostra o responsável de PERFORMANCE (conferir 1 empresa que sabidamente tem responsável Shopee diferente)
       - Aba Pendências: empresas sem analista de performance agora aparecem como "sem responsável" (lista cresce ~7 empresas — esperado e desejado)
    4. Abrir o detalhe de uma dessas empresas (`/companies/{id}`) e conferir os mesmos responsáveis
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o problema encontrado</resume-signal>
  <verify>Usuário confirma visualmente os 4 passos do roteiro (carteira com 2 badges, "sem fonte financeira", /companies com responsável de Performance, pendências OR)</verify>
  <done>Usuário respondeu "aprovado" — ou os problemas relatados foram registrados para correção</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /companies, /companies/{id} | admin/consultor consultando responsáveis e pendências de empresas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03 | Information Disclosure | coluna Analista/Estrategista exibindo responsável errado (Shopee em painel Performance) | mitigate | relações novas filtradas por servicos.setor via subquery (Task 1), provado por teste com pivot Shopee mais antigo |
| T-89-04 | Tampering | regressão nos 9 call-sites consolidados (bônus/notificação/NPS) | mitigate | relações antigas intocadas + LeitoresConsolidadoRegressaoTest + suíte Nps no gate + git diff de fronteira (Task 2) |
| T-89-SC | Tampering | supply chain | accept | zero pacotes novos nesta fase — sem gate de legitimidade necessário |
</threat_model>

<verification>
- `php artisan test` completo verde antes do verify-work da fase
- `git diff` da fase restrito a: PortfolioController.php, AdminCarteira.jsx, Company.php, CompanyController.php, tests/Feature/V16/*, .planning/*
- Checkpoint visual aprovado pelo usuário
</verification>

<success_criteria>
- CART-08: /companies (index + show) mostra responsável de Performance, nunca Shopee, mesmo com pivot Shopee mais antigo (teste dedicado)
- Pendência `sem_responsavel` em OR sobre as relações novas — teste explícito do comportamento novo (empresa com só 1 dos 2 papéis de performance acusa)
- Empresa só-Mentoria e ramo legado servico_id NULL resolvem (testes dedicados, sem hardcode de id)
- 9 call-sites e Companies/*.jsx intocados (gate de fronteira + regressão consolidada verde)
- SEM deploy nesta fase
</success_criteria>

<output>
Criar `.planning/phases/89-carteira-individual-rendercarteiraprofissional-por-contexto-/89-02-SUMMARY.md` ao concluir (pt-BR).
Registrar no SUMMARY: (1) a pendência OR fez a lista de pendências crescer (~7 empresas) — comportamento confirmado pelo usuário; (2) `calcularPendencias()` do HubspotWebhookController mantém o AND antigo deliberadamente (divergência documentada, decisão futura própria); (3) `renderPortfolio()` segue como known-gap (ver SUMMARY 89-01).
</output>
