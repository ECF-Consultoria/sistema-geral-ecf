---
phase: 89-carteira-individual-rendercarteiraprofissional-por-contexto-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/PortfolioController.php
  - resources/js/Pages/Portfolio/AdminCarteira.jsx
  - tests/Feature/V16/CarteiraFinanceiroElegibilidadeTest.php
  - tests/Feature/V16/CarteiraIndividualContextoTest.php
autonomous: true
requirements: [CART-01, CART-02, CART-03, CART-04, CART-05]
must_haves:
  truths:
    - "Admin abrindo a carteira de um profissional com Performance+Shopee na mesma empresa vê a empresa UMA vez, com os 2 vínculos exibidos separadamente"
    - "Vínculo Shopee aparece na carteira com estado explícito 'sem fonte financeira' — a linha da empresa não exibe faturamento/margem de ML quando o profissional não tem vínculo elegível nela"
    - "Analista responsável APENAS por Shopee de uma empresa que também tem ML não recebe revenue/margem/ad_spend/tacos dessa empresa no total da carteira"
    - "Profissional responsável por ML E Shopee da mesma empresa tem o financeiro ML contado UMA única vez"
    - "ad_spend e tacos existem no payload da carteira individual, já nascendo filtrados por elegibilidade financeira"
  artifacts:
    - path: "app/Http/Controllers/PortfolioController.php"
      provides: "renderCarteiraProfissional consumindo CarteiraContextService com dedup financeiro por company_id único elegível"
      contains: "CarteiraContextService"
    - path: "resources/js/Pages/Portfolio/AdminCarteira.jsx"
      provides: "Badge de vínculo por serviço sob o nome da empresa + estado 'sem fonte financeira'"
      contains: "servicos"
    - path: "tests/Feature/V16/CarteiraFinanceiroElegibilidadeTest.php"
      provides: "Testes CART-03/04/05 (elegibilidade + dedup)"
      min_lines: 80
    - path: "tests/Feature/V16/CarteiraIndividualContextoTest.php"
      provides: "Testes CART-01/02 (agrupamento por empresa + vínculos no payload)"
      min_lines: 60
  key_links:
    - from: "app/Http/Controllers/PortfolioController.php"
      to: "app/Services/Portfolio/CarteiraContextService.php"
      via: "forUser($user, ['active' => true]) substituindo $user->companies()"
      pattern: "forUser\\("
    - from: "app/Http/Controllers/PortfolioController.php"
      to: "AdmanMetric"
      via: "whereIn('company_id', $companyIdsElegiveis) — nunca por vínculo"
      pattern: "financial_metrics_eligible"
    - from: "resources/js/Pages/Portfolio/AdminCarteira.jsx"
      to: "payload empresas[].servicos"
      via: "map de badges por vínculo"
      pattern: "financial_metrics_eligible"
---

<objective>
Refatorar `renderCarteiraProfissional()` (PortfolioController:114-423) para consumir `CarteiraContextService::forUser()` (Fase 88, verificada) em vez de `$user->companies()`, agrupando vínculos por empresa (CART-01), expondo o vínculo Shopee com "sem fonte financeira" (CART-02), e filtrando TODA a soma financeira (revenue, margem, ad_spend, tacos — os 2 últimos ADICIONADOS nesta fase) pelos `company_id` únicos com vínculo `financial_metrics_eligible=true` (CART-03/04/05).

Purpose: hoje o responsável Shopee "herda" faturamento/margem ML de empresas que ele não gere em ML (medido em prod: Felipe avaliado sobre 29 empresas gerenciando 4). Esta é a Fase 2 do plano canônico multi-serviço.
Output: carteira individual correta por contexto de serviço + 2 suítes de teste Feature V16 + UI com badges de vínculo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/89-carteira-individual-rendercarteiraprofissional-por-contexto-/89-RESEARCH.md
@app/Services/Portfolio/CarteiraContextService.php
@app/Http/Controllers/PortfolioController.php
@resources/js/Pages/Portfolio/AdminCarteira.jsx
@tests/Feature/V16/CriaCenarioResponsaveis.php
@tests/Feature/V16/LeitoresConsolidadoRegressaoTest.php

<interfaces>
<!-- Contratos que o executor usa diretamente — sem exploração de codebase. -->

De app/Services/Portfolio/CarteiraContextService.php (Fase 88, PRONTO — nunca reimplementar):
```php
// forUser(User $user, array $filters = []): Collection de arrays:
// { user_id, company_id, company_name, servico_id (?int), servico_nome (?string),
//   setor, role ('consultor'|'estrategista'), role_label ('Analista'|'Estrategista'),
//   has_financial_source, financial_source, financial_metrics_eligible }
public function forUser(User $user, array $filters = []): Collection;
public function contadores(Collection $vinculos): array; // NÃO usar nesta fase (contadores de topo = Fase 90)
```

Padrão ad_spend/tacos já existente no MESMO arquivo (renderCarteirasConsolidadas, linhas 526-572):
```php
$sumDb = AdmanMetric::whereIn('company_id', $companyIds)
    ->selectRaw('company_id, SUM(revenue) as rev, SUM(ad_spend) as ads, ...')
    ->groupBy('company_id')->get()->keyBy('company_id');
$account = $this->adman->getCachedAccountMetricsMany($custIds, $dateFrom, $dateTo);
// ads: cache $account[$custId]['value']['investment'] com fallback $row->ads
// tacos: cache $account[$custId]['value']['tacos'] com fallback ($ads / $rev) * 100 quando $rev > 0
```

Cache Adman em teste: `getCachedGrossBillingsMany`/`getCachedAccountMetricsMany` são CACHE-ONLY
(Cache::many; miss retorna value=null, ZERO HTTP) → em teste o fallback SUM DB local sempre vence.

Trait de fixtures (tests/Feature/V16/CriaCenarioResponsaveis.php):
```php
protected function criarServico(string $setor, bool $ativo = true): int;
protected function criarContrato(int $companyId, int $servicoId, bool $ativo = true): int;
protected function inserirPivot(int $companyId, int $userId, string $role, ?int $servicoId): int;
protected function criarCenarioMlComResponsaveis(): array; // {company, servicoPerf, analista, estrategista}
protected function inserirLinhaShopee(int $companyId, int $userId, string $role): array; // {servicoShopee, row}
```

Rota alvo: GET `/admin/users/{user}/portfolio` → `portfolio.show` (admin abrindo OUTRO profissional
cai em renderCarteiraProfissional). Teste: `$this->actingAs($admin)->get(route('portfolio.show', $user))`
com `Inertia\Testing\AssertableInertia` (padrão de tests/Feature/Portfolio/RenderPortfolioTest.php).
`AdmanMetric` fillable: company_id, reference_date, revenue, contribution_margin, ad_spend, tacos, ...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escrever testes RED — elegibilidade financeira (CART-03/04/05) e contexto por vínculo (CART-01/02)</name>
  <files>tests/Feature/V16/CarteiraFinanceiroElegibilidadeTest.php, tests/Feature/V16/CarteiraIndividualContextoTest.php</files>
  <behavior>
    CarteiraFinanceiroElegibilidadeTest (RefreshDatabase + CriaCenarioResponsaveis; admin via User::factory(['role' => 'admin']); GET route('portfolio.show', $profissional) e asserts no payload Inertia 'resumo'/'empresas'):
    - Teste CART-04: empresa com contrato performance E shopee ativos + AdmanMetric no mês corrente (revenue=1000, contribution_margin=200, ad_spend=100); profissional com vínculo SÓ shopee (inserirPivot com servico_id do shopee). Payload: empresa APARECE em empresas[] (1 linha, vínculo shopee), mas faturamento/margem_contribuicao/ad_spend/tacos da linha são null E resumo.total_faturamento == 0.0, resumo.total_ad_spend == 0.0.
    - Teste CART-04 (papel estrategista): mesmo cenário com role 'estrategista' — mesmo resultado.
    - Teste CART-05 (o mais importante da fase): profissional com vínculo performance E shopee da MESMA empresa (criarCenarioMlComResponsaveis + inserirLinhaShopee no MESMO analista) + AdmanMetric revenue=1000 no mês corrente. resumo.total_faturamento == 1000.0 (nunca 2000.0) e empresas[] tem exatamente 1 item.
    - Teste regressão (analista ML comum): criarCenarioMlComResponsaveis + AdmanMetric revenue=1000, ad_spend=100 → linha da empresa tem faturamento 1000.0, ad_spend 100.0 e tacos 10.0 (fallback ads/rev*100 — cache vazio em teste).
    - Teste CART-03 (empresas mistas): analista com empresa A elegível (performance, revenue=1000, ad_spend=100) + empresa B só-shopee (revenue=500, ad_spend=50 em AdmanMetric): resumo.total_faturamento == 1000.0 e resumo.total_ad_spend == 100.0 — B não contamina.
    CarteiraIndividualContextoTest (mesma base):
    - Teste CART-01: profissional Performance+Shopee da mesma empresa → empresas[] tem 1 item com empresas.0.servicos contendo 2 entradas (setores 'performance' e 'shopee', role_label 'Analista').
    - Teste CART-02: a entrada shopee de servicos tem financial_metrics_eligible === false (a performance, true).
    - Teste Mentoria: vínculo apontando pra um SEGUNDO serviço de setor performance (criarServico('performance') extra, nome livre) → entrada em servicos com financial_metrics_eligible === true e a empresa entra no financeiro (sem hardcode de servico_id).
    - Teste legado: inserirPivot com servico_id NULL em empresa com contrato performance ativo → vínculo aparece em servicos com setor 'performance' e elegível (ramo CTX-05 atravessando o controller).
  </behavior>
  <action>
    Criar os 2 arquivos em `tests/Feature/V16/` (namespace `Tests\Feature\V16`), reusando o trait `CriaCenarioResponsaveis` — decisão: manter V16 por consistência com o que já está commitado (nota do RESEARCH §Fixtures). Seed de AdmanMetric via `AdmanMetric::create()` com `reference_date` dentro do mês corrente (usar `now()->startOfMonth()->addDays(2)` p/ não depender do dia). Empresa precisa de `adman_account_id` (a factory já preenche via cenário do RenderPortfolioTest — se a CompanyFactory não preencher, setar manualmente). Comentários pt-BR referenciando CART-XX em cada teste. NÃO criar factories novas (padrão do trait é DB::table puro). Rodar a suíte e CONFIRMAR que os testes novos FALHAM pelo motivo certo (payload atual não tem `servicos` nem `ad_spend`, e total soma tudo) — RED legítimo, não erro de sintaxe/fixture.
  </action>
  <verify>
    <automated>php artisan test --filter="CarteiraFinanceiroElegibilidade|CarteiraIndividualContexto" (DEVE FALHAR — asserts de comportamento novo; falha por assert, não por exception de setup)</automated>
  </verify>
  <done>2 arquivos de teste existem, rodam, e falham exclusivamente nos asserts de comportamento novo. Commit `test(89-01): ...` em pt-BR.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar renderCarteiraProfissional — service como origem, dedup por company_id elegível, ad_spend/tacos (GREEN)</name>
  <files>app/Http/Controllers/PortfolioController.php</files>
  <action>
    Implementar em `renderCarteiraProfissional()` (PortfolioController:114-423), mantendo INTACTA a lógica de janela de datas (linhas 125-153) e o recorte de dias comuns (fix LOJASINVAL 2026-07-13):

    1. Injetar `CarteiraContextService` no construtor (promoted property, padrão dos services já injetados). Import `App\Services\Portfolio\CarteiraContextService`.
    2. Substituir a origem (linha 156-161): `$vinculos = $this->carteiraContext->forUser($user, ['active' => true]);` e `$porEmpresa = $vinculos->groupBy('company_id');`. Carregar os models pra exibição: `$rawCompanies = Company::whereIn('id', $porEmpresa->keys())->with('mlToken')->orderBy('name')->get();` (o service não expõe cust_id/mlToken). Referência: decisão travada — NUNCA reimplementar resolução de vínculo no controller; `forUser()` é a única porta (o join direto em company_users.servico_id perde o ramo legado CTX-05).
    3. Dedup financeiro (CART-04/05): `$companyIdsElegiveis = $vinculos->where('financial_metrics_eligible', true)->pluck('company_id')->unique()->values();`. TODAS as queries de AdmanMetric (as 2 de SUM, linhas 172-183, e as 2 de dias-com-margem, linhas 218-230) passam a usar `whereIn('company_id', $companyIdsElegiveis)`. PITFALL (RESEARCH §Algoritmo de dedup): AdmanMetric é POR EMPRESA — nunca iterar vínculos somando; a unidade de consulta é company_id único.
    4. CART-03 — adicionar ad_spend/tacos: incluir `SUM(ad_spend) as ads` no selectRaw da query da janela ATUAL (linha 172-177); `$custIdsElegiveis` = cust_id só das empresas em `$companyIdsElegiveis` (filtrar `$rawCompanies`); chamar `$account = $this->adman->getCachedAccountMetricsMany($custIdsElegiveis, $dateFrom, $dateTo);` além do `getCachedGrossBillingsMany` existente (que também passa a receber só `$custIdsElegiveis`). No map por empresa: `ad_spend` = cache `investment` com fallback `$rowAtual->ads`; `tacos` = cache `tacos` com fallback `($ads / $revenue) * 100` quando revenue > 0 — mesmo padrão de renderCarteirasConsolidadas:552-571 no MESMO arquivo.
    5. Map por empresa (linhas 242-351): iterar `$rawCompanies` como hoje, mas: (a) `$ehElegivel = $companyIdsElegiveis->contains($c->id)`; se NÃO elegível → faturamento/margem/ad_spend/tacos/margem_variacao_pct = null E `motivo_sem_margem` = null (não é problema de sync — a UI deriva "sem fonte financeira" dos vínculos; não poluir o banner rosa de qualidade de dados); (b) anexar `'servicos' => $porEmpresa->get($c->id)->map(fn ($v) => [servico_id, servico_nome, setor, role, role_label, financial_metrics_eligible])->values()` (shape do RESEARCH §Desenho recomendado do payload).
    6. Resumo (linhas 402-410): manter chaves atuais; `empresas_sem_margem` conta SÓ empresas elegíveis com margem null (excluir as sem fonte, per item 5a); adicionar `total_ad_spend` (soma dos ad_spend não-null) e `tacos_medio` (média SIMPLES dos tacos per-empresa não-null — mesmo racional do hotfix 2026-06-23 de alinhar com Dashboard). NÃO adicionar contadores de vínculos/empresas-únicas ao resumo (é CART-07/Fase 90).
    7. Atualizar comentários pt-BR do método referenciando Fase 89 + CART-IDs. NÃO tocar em `renderPortfolio()` nem `renderCarteirasConsolidadas()` (Fase 90 / known-gap).
  </action>
  <verify>
    <automated>php artisan test --filter="CarteiraFinanceiroElegibilidade|CarteiraIndividualContexto" && php artisan test --filter=V16 && php artisan test --filter=RenderPortfolio</automated>
  </verify>
  <done>Testes das 2 suítes novas verdes (GREEN); suíte V16 completa verde (baseline 117/117 + novos); RenderPortfolioTest verde (prova que renderPortfolio ficou intocado). Commit `feat(89-01): ...`.</done>
</task>

<task type="auto">
  <name>Task 3: AdminCarteira.jsx — badges de vínculo por serviço + estado "sem fonte financeira" + npm run build</name>
  <files>resources/js/Pages/Portfolio/AdminCarteira.jsx</files>
  <action>
    Evoluir a listagem de empresas (tbody, linhas ~350-391) SEM redesenhar a página (filtro Todos/Performance/Shopee é CART-07/Fase 90 — NÃO implementar):

    1. Constante local `SETOR_LABELS = { performance: 'Mercado Livre', shopee: 'Shopee' }` — regra do projeto: nunca slug cru na tela; fallback `servico_nome ?? 'Outro serviço'` para setores futuros.
    2. Sob o nome da empresa (dentro do map de `empresasView`), renderizar 1 badge por item de `c.servicos ?? []`: `{SETOR_LABELS[s.setor]} · {s.role_label}` — Performance em estilo destaque (ex.: `bg-ecf-yellow/10 text-ecf-yellow border-ecf-yellow/25`), Shopee em neutro/âmbar com sufixo `— sem fonte financeira` quando `!s.financial_metrics_eligible`. Usar `cn()` e tokens ecf-* existentes. Empresa com 2 vínculos aparece 1x com 2 badges (CART-01 visível).
    3. Estado explícito por linha: computar `const temFonteFinanceira = (c.servicos ?? []).some(s => s.financial_metrics_eligible)` DENTRO do callback do map (pitfall conhecido do Rollup: flag booleana computada fora do map some do bundle — memória `feedback_rollup_map_scope_bug`). Quando `!temFonteFinanceira`, células de Faturamento/Margem/Var. mostram "—" com `title="Sem fonte financeira conectada — vínculo Shopee ainda não tem faturamento no painel"` (sem o "?" rosa de erro de sync, que continua reservado a `motivo_sem_margem`).
    4. Atualizar o docblock de contrato de props do topo do arquivo (empresas[] agora tem `servicos[]`, `ad_spend`, `tacos`; resumo tem `total_ad_spend`, `tacos_medio`). NÃO adicionar colunas de ad_spend/tacos à tabela nesta fase (payload-only; UI completa é Fase 90).
    5. Defensivo: `c.servicos` pode vir undefined em cache de página antiga — todos os acessos com `?? []`.
    6. Rodar `npm run build` — exit 0 obrigatório (convenção do projeto; atenção ao dev paralelo do módulo MLB: não commitar artefatos de build de arquivos alheios se o repo não versiona `public/build`, seguir o padrão dos commits recentes).
  </action>
  <verify>
    <automated>npm run build && php artisan test --filter="CarteiraIndividualContexto|CarteiraFinanceiroElegibilidade"</automated>
  </verify>
  <done>Build exit 0; testes seguem verdes; badges e estado "sem fonte financeira" implementados sem filtro de contexto (Fase 90). Commit `feat(89-01): ...`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /admin/users/{user}/portfolio | admin/líder/próprio user consultando dados financeiros de carteira |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Information Disclosure | renderCarteiraProfissional (financeiro entre setores) | mitigate | é o bug que a fase corrige: gating por `financial_metrics_eligible` na lista de company_id consultada em AdmanMetric (Task 2 item 3), provado por CarteiraFinanceiroElegibilidadeTest |
| T-89-02 | Broken Access Control (IDOR) | rota /admin/users/{user}/portfolio | accept | `abort_unless` existente (PortfolioController:79-88) inalterado; fase não muda autorização, só fonte de dados |
| T-89-SC | Tampering | supply chain | accept | zero pacotes novos nesta fase (npm/composer intocados) — sem gate de legitimidade necessário |
</threat_model>

<verification>
- `php artisan test --filter=V16` — suíte inteira verde (baseline 117/117 + testes novos)
- `php artisan test --filter=RenderPortfolio` — renderPortfolio() intocado
- `npm run build` exit 0
- Fronteira: `git diff --name-only` da fase NÃO contém `app/Models/User.php`, `app/Services/DesempenhoScoreService.php`, `resources/js/Pages/Companies/Index.jsx`, `resources/js/Pages/Companies/Show.jsx`
</verification>

<success_criteria>
- CART-01: payload agrupa por empresa com `servicos[]` (teste + badge na UI)
- CART-02: vínculo Shopee com `financial_metrics_eligible=false` e estado "sem fonte financeira" na tela
- CART-03: ad_spend/tacos adicionados e filtrados por elegibilidade (teste dedicado)
- CART-04: analista só-Shopee com total_faturamento 0 em empresa que tem ML (teste dedicado, ambos os papéis)
- CART-05: ML+Shopee mesma empresa conta financeiro 1x (teste dedicado)
</success_criteria>

<output>
Criar `.planning/phases/89-carteira-individual-rendercarteiraprofissional-por-contexto-/89-01-SUMMARY.md` ao concluir (pt-BR).
OBRIGATÓRIO registrar no SUMMARY os known-gaps decididos: (1) `renderPortfolio()` (auto-visualização via /admin/users/{seu_id}/portfolio) fica com o comportamento antigo — candidato à Fase 90/follow-up (Pitfall 2/6 do RESEARCH); (2) filtro Todos/Performance/Shopee e contadores de topo são CART-07/Fase 90.
</output>
