---
phase: 106-fix-timeout-mes-fechado-warm-cache-v18-0
plan: 02
type: tdd
wave: 2
depends_on: ["106-01"]
files_modified:
  - app/Http/Controllers/PerformanceController.php
  - tests/Feature/Phase106/PerformanceControllerWarmDegradationTest.php
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "No modo FECHADO, profissional FRIO não é computado ao vivo na requisição — vira linha placeholder calculando:true"
    - "Havendo ≥1 frio, o warm sob-demanda é enfileirado uma única vez por mês (lock impede empilhar a cada poll)"
    - "No modo EM CURSO o gate não atua — o ranking segue computando normal (comportamento intocado)"
    - "Um profissional já QUENTE no modo fechado mostra a nota real (número idêntico ao baseline)"
  artifacts:
    - path: "app/Http/Controllers/PerformanceController.php"
      provides: "Gate quente/frio no index() + dispatch com lock + prop aquecendo"
      contains: "isCached"
    - path: "tests/Feature/Phase106/PerformanceControllerWarmDegradationTest.php"
      provides: "Prova SC2/SC3 — frio→placeholder+dispatch, lock, em-curso intocado, quente→número"
      exists: true
  key_links:
    - from: "app/Http/Controllers/PerformanceController.php::index"
      to: "app/Services/DesempenhoScoreService::isCached"
      via: "gate antes de computeCached quando periodo.is_closed"
      pattern: "->isCached\\("
    - from: "app/Http/Controllers/PerformanceController.php::index"
      to: "desempenho:warm-cache"
      via: "Artisan::queue com lock Cache::add"
      pattern: "Artisan::queue\\('desempenho:warm-cache'"
---

<objective>
Ligar a degradação graciosa (SC2/SC3) no `PerformanceController::index`, reusando o
`isCached` (Plan 106-01) e o command estendido (`--mes`/`--user`, Plan 106-01).

Regra: quando `$periodoResolvido['is_closed'] === true`, ANTES de chamar
`computeCached()` para um profissional (caminho sem snapshot mensal válido), checar
`isCached($u, $mesReferencia)`:
- **QUENTE** → chama computeCached (cache hit, <1s) → linha completa como hoje.
- **FRIO** → NÃO computa; devolve linha placeholder `calculando: true` (sem número) e
  coleta o `$u->id` em `$usuariosFrios`.

Se `$usuariosFrios` não estiver vazio: adquirir lock curto `Cache::add("desempenho.warm.lock.{YYYY-MM}", true, 3min)` e, só se adquirido, `Artisan::queue('desempenho:warm-cache', ['--mes' => YYYY-MM, '--user' => $usuariosFrios])`. Expor prop `aquecendo` (bool) = houve ≥1 frio.

O modo EM CURSO e o caminho de SNAPSHOT mensal ficam INTOCADOS — o gate só envolve as
chamadas de `computeCached()` em período fechado.

Purpose: elimina o cálculo síncrono de ~14 profissionais (N+1 Adman → >300s → tela
branca). Pior caso vira "calculando…" com warm em background.
Output: `PerformanceController.php` estendido + teste feature (Queue::fake) verde.

**FRONTEIRA:** só `index()` (o gate/dispatch/payload). NÃO tocar cálculo da nota,
resolver, diff-service, `dashboardCarteira`, `show`, `indexPolos`. NÃO tocar o
caminho de snapshot (linha ~150 `if (! $ehMesEmCurso && $snap)`) — snapshot já é rápido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/106-fix-timeout-mes-fechado-warm-cache-v18-0/106-RESEARCH.md
@app/Http/Controllers/PerformanceController.php
@tests/Feature/PerformanceCargoFilterTest.php

<interfaces>
<!-- Contratos entregues pelo Plan 106-01 (Wave 1) — usar direto. -->

DesempenhoScoreService:
```php
public function isCached(User $user, Carbon $mes): bool; // Cache::has(cacheKey) — não computa
public function computeCached(User $user, Carbon $mes): array; // inalterado
```

WarmDesempenhoCache (signature estendida no 106-01):
```
desempenho:warm-cache {--mes= : YYYY-MM} {--user=* : IDs}
// Artisan::queue('desempenho:warm-cache', ['--mes' => 'YYYY-MM', '--user' => [1,2,3]])
```

PerformanceController::index — pontos de ancoragem já existentes:
- $periodoResolvido (MetricPeriodResolver, 1×/página) — LER $periodoResolvido['is_closed'].
- $ehMesEmCurso (bool) — mês corrente.
- Loop $users->map(...) ~linha 143: monta cada linha do ranking. As chamadas a
  computeCached estão em 2 ramos: (a) fallback quando snapshot mensal não tem
  'componentes'; (b) else (mês em curso OU sem snapshot). O gate envolve SÓ essas
  chamadas quando is_closed.
- Shape da linha do ranking: array com id/name/nota_final/faixa_bonus/componentes/etc.
- return Inertia::render('Performance/Index', [ 'ranking'=>..., 'periodo'=>..., ... ]).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Gate quente/frio + placeholder calculando + prop aquecendo</name>
  <files>app/Http/Controllers/PerformanceController.php, tests/Feature/Phase106/PerformanceControllerWarmDegradationTest.php</files>
  <behavior>
    - Modo FECHADO (?mes=YYYY-MM de mês passado, sem snapshot mensal), users frios: cada linha do ranking do user frio tem calculando===true e nota_final===null; prop aquecendo===true; e NENHUM compute ao vivo ocorre (Http::preventStrayRequests() SEM fake não estoura).
    - Modo FECHADO, user já QUENTE (cache pré-populado com computeCached): a linha traz calculando ausente/false e nota_final numérica igual ao computado; esse user NÃO entra em usuariosFrios.
    - Modo EM CURSO (sem ?mes/?modo): o gate não atua — linhas computadas normalmente (com Http::fake determinístico), prop aquecendo===false, nenhuma linha calculando.
  </behavior>
  <action>
    RED: criar tests/Feature/Phase106/PerformanceControllerWarmDegradationTest.php (namespace Tests\Feature\Phase106, use RefreshDatabase). Setup espelhando PerformanceCargoFilterTest (admin autenticado; users analista/estrategista com empresa ativa -3 meses via company_users). Carbon::setTestNow numa data de meados de mês; usar ?mes= de um mês passado para forçar is_closed. Helper para ler props: $response->viewData('page')['props']. Casos frio/quente/em-curso do <behavior>. Para pré-aquecer um user: Cache::put(app(DesempenhoScoreService::class)->cacheKey($u->id,$mes), ['componentes'=>[...],'nota_final'=>3.5, ...shape mínimo...], 300). Para o caso frio: Http::preventStrayRequests() e assert status 200 + calculando true.
    GREEN: em index(), inicializar `$usuariosFrios = [];` antes do map. Passar `$periodoResolvido`, `&$usuariosFrios` e `$this->scoreService` ao closure do map (use). Dentro do map, no ponto ANTES de cada chamada a `computeCached($u, $mesReferencia)` (nos 2 ramos que computam), inserir: se `$periodoResolvido['is_closed'] && ! $this->scoreService->isCached($u, $mesReferencia)` → `$usuariosFrios[] = $u->id;` e retornar uma linha placeholder que preserva id/name/role/cargo_slug/cargo_label mas com `'calculando' => true`, `'nota_final' => null`, `'faixa_bonus' => null`, `'sem_carteira' => false`, `'score_status' => 'calculando'`, componentes null — de forma que NÃO seja rejeitada pelo reject(sem_carteira) e NÃO computada. Fora do map, expor `$aquecendo = ! empty($usuariosFrios);`. Adicionar `'aquecendo' => $aquecendo` e garantir que toda linha tenha a chave `'calculando'` (default false nas linhas normais). Comentários pt-BR referenciando SC2/SC3. NÃO reordenar de forma a criar salto: manter o sortByDesc atual (linhas calculando com nota_final null caem ao fim — o front trata a ordenação transitória; documentar no comentário que a posição estabiliza quando o mês fica 100% quente).
  </action>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=PerformanceControllerWarmDegradationTest</automated>
  </verify>
  <done>Modo fechado frio → linhas calculando:true + aquecendo:true + zero compute ao vivo; quente → número real; em curso → intocado (aquecendo:false). Teste verde.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Dispatch do warm sob-demanda com lock anti-duplicação</name>
  <files>app/Http/Controllers/PerformanceController.php, tests/Feature/Phase106/PerformanceControllerWarmDegradationTest.php</files>
  <behavior>
    - Modo fechado com ≥1 frio: exatamente 1 push de `desempenho:warm-cache` na fila, com --mes = competência selecionada e --user contendo os IDs frios (Queue::fake / Bus assertion do QueuedCommand).
    - Segundo request idêntico dentro da janela do lock (mesmo mês) NÃO enfileira 2º job (lock Cache::add impede) — total continua 1.
    - Modo fechado com TODOS quentes (nenhum frio): NENHUM job enfileirado; aquecendo===false.
  </behavior>
  <action>
    RED: estender PerformanceControllerWarmDegradationTest com os 3 casos. Usar Queue::fake() e afirmar o enfileiramento do Artisan queued command (assertar via Queue::assertPushed(\Illuminate\Foundation\Console\ ... ou o wrapper Illuminate\Console\Scheduling — usar a classe que Laravel 12 enfileira para Artisan::queue: `Illuminate\Foundation\Console\QueuedCommand` se disponível; senão asseverar contagem via Queue::assertPushed callback filtrando pelo command). Se a asserção direta do QueuedCommand for frágil no ambiente, fallback: spy no Cache do lock — 1º request cria a lock key, 2º não re-cria — e afirmar que a lock existe. Priorizar a asserção de fila; documentar o fallback no teste.
    GREEN: após o map e o reject/sort (ou logo após montar $usuariosFrios, antes do return), inserir: `if (! empty($usuariosFrios)) { $lockKey = 'desempenho.warm.lock.'.$mesReferencia->format('Y-m'); if (Cache::add($lockKey, true, now()->addMinutes(3))) { Artisan::queue('desempenho:warm-cache', ['--mes' => $mesReferencia->format('Y-m'), '--user' => array_values(array_unique($usuariosFrios))]); } }`. Importar Illuminate\Support\Facades\Cache e Illuminate\Support\Facades\Artisan no topo do controller. Comentário pt-BR: lock de 3min evita empilhar job a cada poll de 6s do front (RESEARCH Pitfall 3). NÃO passar valor de request cru — $usuariosFrios vem da query interna já filtrada; $mesReferencia já validado por preg_match no bloco de resolução de mês.
  </action>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=PerformanceControllerWarmDegradationTest</automated>
  </verify>
  <done>1 job enfileirado com --mes + --user corretos; 2º request não duplica (lock); zero frio → zero job. Teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /performance | `?mes`/`?modo` já validados por preg_match/whitelist no index (código existente) |
| controller → queue | `--user` = IDs de query interna (active + cargo), nunca de request; `--mes` = string já validada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-03 | Denial of Service (auto-infligido) | dispatch de warm a cada poll | mitigate | Lock `Cache::add(...,3min)` por mês — só 1 dispatch por janela mesmo com N polls de 6s |
| T-106-04 | Input | IDs/mes passados ao `Artisan::queue` | mitigate | IDs vêm de query filtrada (não de request); `$mesReferencia->format('Y-m')` já normalizado; command revalida (T-106-01) |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | Sem instalação de pacote nesta fase |
</threat_model>

<verification>
- `"C:/xampp/php/php.exe" artisan test --filter=PerformanceControllerWarmDegradationTest` verde
- `"C:/xampp/php/php.exe" artisan test --filter=PerformanceCargoFilterTest` segue verde (não regride o /performance)
- Diff de `PerformanceController.php` toca APENAS index() — `dashboardCarteira`/`show`/`indexPolos` intocados (`git diff app/Http/Controllers/PerformanceController.php`)
</verification>

<success_criteria>
- SC2: modo fechado frio degrada (calculando + warm em background), sem compute síncrono.
- SC3: modo em curso e caminho de snapshot intocados; nenhuma tela branca (200 sempre).
- SC4: users quentes retornam número idêntico ao baseline (sem regressão).
</success_criteria>

<output>
Criar `.planning/phases/106-fix-timeout-mes-fechado-warm-cache-v18-0/106-02-SUMMARY.md` ao concluir.
</output>
