---
phase: 106-fix-timeout-mes-fechado-warm-cache-v18-0
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/Console/Commands/WarmDesempenhoCache.php
  - app/Services/DesempenhoScoreService.php
  - tests/Unit/DesempenhoScoreServiceCacheTest.php
  - tests/Feature/Phase106/WarmDesempenhoCacheTest.php
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "O warm agendado aquece o mês corrente E a competência do último mês fechado numa única execução"
    - "É possível perguntar 'este user/mês está em cache?' sem disparar o compute() caro (sem chamada Adman)"
    - "O warm sob-demanda aceita alvo pontual (--mes + --user) para catch-up de um mês frio específico"
  artifacts:
    - path: "app/Services/DesempenhoScoreService.php"
      provides: "Wrapper público isCached(User, Carbon): bool sobre Cache::has(cacheKey)"
      contains: "public function isCached"
    - path: "app/Console/Commands/WarmDesempenhoCache.php"
      provides: "Warm de 2 alvos (corrente + last_closed_month) + options --mes e --user=*"
      contains: "last_closed_month"
    - path: "tests/Feature/Phase106/WarmDesempenhoCacheTest.php"
      provides: "Prova SC1 — 2 competências aquecidas, chaves distintas"
      exists: true
    - path: "tests/Unit/DesempenhoScoreServiceCacheTest.php"
      provides: "Prova isCached true/false sem tocar Adman"
      exists: true
  key_links:
    - from: "app/Services/DesempenhoScoreService.php::isCached"
      to: "app/Services/DesempenhoScoreService.php::cacheKey"
      via: "Cache::has da mesma chave que computeCached escreve"
      pattern: "Cache::has\\(\\$this->cacheKey"
    - from: "app/Console/Commands/WarmDesempenhoCache.php"
      to: "app/Services/Metrics/MetricPeriodResolver::resolve"
      via: "resolve(['period_key' => 'last_closed_month'])"
      pattern: "last_closed_month"
---

<objective>
Entregar os DOIS blocos de backend puros da Fase 106, ambos com regra clara de I/O
(portanto TDD):

1. **SC1 — warm de 2 alvos:** `WarmDesempenhoCache` passa a aquecer, na mesma execução,
   o mês corrente (comportamento atual) MAIS a competência do último mês fechado
   (via `MetricPeriodResolver::resolve(['period_key' => 'last_closed_month'])`). Ganha
   também as options `--mes=YYYY-MM` (aquece só aquela competência) e `--user=*`
   (aquece só estes IDs) — insumo do dispatch sob-demanda que o Plan 106-02 vai chamar.

2. **Gate isCached (metade do SC2):** wrapper público
   `DesempenhoScoreService::isCached(User, Carbon): bool` = `Cache::has($this->cacheKey(...))`.
   Responde "está pronto?" SEM computar (sem bater na Adman). É a peça que o
   Plan 106-02 usa para separar quente/frio no controller.

Purpose: sem o warm do fechado, "Bônus atual" cai sempre frio; sem o `isCached`, o
controller não tem como decidir "computo ou degrado" sem pagar o custo do compute().
Output: 2 arquivos de produção estendidos + 2 arquivos de teste novos, suíte verde.

**FRONTEIRA RÍGIDA:** NÃO tocar `compute()`, `computeCached()`, `cacheKey()` nem
qualquer lógica de CÁLCULO da nota/régua (v18/Fase 105 intocado). `isCached` é um
wrapper novo de 3 linhas; o command só ganha um 2º alvo de mês e duas options.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/106-fix-timeout-mes-fechado-warm-cache-v18-0/106-RESEARCH.md
@app/Console/Commands/WarmDesempenhoCache.php
@app/Services/Metrics/MetricPeriodResolver.php
@tests/Feature/PerformanceCargoFilterTest.php

<interfaces>
<!-- Contratos já existentes que os executores usam direto — sem explorar o código. -->

DesempenhoScoreService (app/Services/DesempenhoScoreService.php):
```php
// chave pública, determinística, versionada (v6). NÃO alterar.
public function cacheKey(int $userId, Carbon $mes): string;
// 'desempenho.compute.v6.{userId}.{current_month|YYYY-MM}'

// computeCached faz Cache::remember(cacheKey, ttl, fn()=>compute()). NÃO alterar.
public function computeCached(User $user, Carbon $mes): array;
```

MetricPeriodResolver (app/Services/Metrics/MetricPeriodResolver.php):
```php
public function resolve(array $filtros): array; // 14 chaves
// resolve(['period_key' => 'last_closed_month']) →
//   ['bonus_competence_month' => 'YYYY-MM', 'current_start' => 'YYYY-MM-01', 'is_closed' => true, ...]
// Service PURO (só Carbon) — determinístico via Carbon::setTestNow().
```

WarmDesempenhoCache HOJE (app/Console/Commands/WarmDesempenhoCache.php):
- signature: `desempenho:warm-cache {--user= : ...}`
- handle(): `$mesReferencia = Carbon::now()->startOfMonth();` → 1 loop de $users →
  `computeCached($user, $mesReferencia)`. Filtro canônico de $users:
  user_setores → cargos.slug IN ['analista','estrategista'] + active=true.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Wrapper público isCached() no DesempenhoScoreService</name>
  <files>app/Services/DesempenhoScoreService.php, tests/Unit/DesempenhoScoreServiceCacheTest.php</files>
  <behavior>
    - Cache vazio para (user, mês) → isCached(user, mês) retorna false.
    - Após popular a MESMA chave que computeCached usa (Cache::put($service->cacheKey($u->id,$mes), [...], 60)) → isCached(user, mês) retorna true.
    - isCached NÃO computa: com Http::preventStrayRequests() ativo e SEM Http::fake, chamar isCached num user frio NÃO lança (prova zero chamada Adman/ML). Mês corrente e mês fechado ambos cobertos (chaves current_month vs YYYY-MM).
  </behavior>
  <action>
    RED: criar tests/Unit/DesempenhoScoreServiceCacheTest.php (namespace Tests\Unit, use RefreshDatabase, resolve o service via app(DesempenhoScoreService::class)). Cobrir os 3 casos do <behavior>. Fixar o relógio com Carbon::setTestNow para distinguir mês corrente de mês fechado. Para o caso "não computa": Http::preventStrayRequests() no início do teste (sem Http::fake) e assert que isCached(userFrio, mesFechado) === false sem exceção.
    GREEN: adicionar em DesempenhoScoreService o método `public function isCached(User $user, Carbon $mes): bool { return Cache::has($this->cacheKey($user->id, $mes)); }`, logo após cacheKey(). Comentário pt-BR: wrapper fino da Fase 106 (SC2) — reusa a MESMA chave de computeCached, NUNCA computa. Confirmar que Illuminate\Support\Facades\Cache já está importado no arquivo (está — computeCached usa Cache::remember). NÃO tocar cacheKey/compute/computeCached.
  </action>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=DesempenhoScoreServiceCacheTest</automated>
  </verify>
  <done>isCached retorna true/false correto para a chave de computeCached; prova de que user frio não dispara HTTP externo (preventStrayRequests não estoura). Teste verde.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: WarmDesempenhoCache aquece 2 alvos + options --mes / --user=*</name>
  <files>app/Console/Commands/WarmDesempenhoCache.php, tests/Feature/Phase106/WarmDesempenhoCacheTest.php</files>
  <behavior>
    - Sem options, com Carbon fixo em meados de um mês: após rodar `desempenho:warm-cache`, Cache::has do mês corrente E Cache::has da competência last_closed_month estão AMBOS true, para cada user elegível — 2 chaves distintas por user (cacheKey deriva current_month vs YYYY-MM).
    - Com `--mes=YYYY-MM` (competência fechada específica): aquece SÓ aquela competência (Cache::has dela = true), NÃO aquece o mês corrente automaticamente.
    - Com `--user=<id>` (um só): aquece só aquele user; outro user elegível continua frio.
    - --mes inválido (ex.: 'abc' ou '2026-13') → command falha graciosamente (retorna FAILURE / não cria chave), sem 500.
  </behavior>
  <action>
    RED: criar tests/Feature/Phase106/WarmDesempenhoCacheTest.php (namespace Tests\Feature\Phase106, use RefreshDatabase). Setup espelhando PerformanceCargoFilterTest: setor+cargos (analista/estrategista), 2 users elegíveis com empresa ativa via company_users (-3 meses) para não caírem em sem_carteira. Http::fake(['*' => Http::response([], 200)]) determinístico (o computeCached bate na Adman/ML — precisa de fake). Carbon::setTestNow numa data de meados de mês. Rodar via $this->artisan('desempenho:warm-cache')->assertExitCode(0) e afirmar via app(DesempenhoScoreService::class)->isCached($u, $mesCorrente) e ->isCached($u, $mesFechado). Repetir os casos de --mes e --user.
    GREEN: no WarmDesempenhoCache — (1) injetar `private MetricPeriodResolver $periodResolver` no construtor (import App\Services\Metrics\MetricPeriodResolver); (2) trocar signature para `desempenho:warm-cache {--mes= : Aquece só esta competência YYYY-MM (catch-up/sob-demanda)} {--user=* : Aquecer só estes user IDs (debug ou dispatch sob-demanda)}`; (3) no handle(): montar $mesesAlvo — se --mes presente e casa /^\d{4}-\d{2}$/ → [Carbon::createFromFormat('Y-m', $mes)->startOfMonth()]; se --mes presente e inválido → this->error + return FAILURE; senão → [ mês corrente, Carbon::parse(periodResolver->resolve(['period_key'=>'last_closed_month'])['bonus_competence_month'].'-01')->startOfMonth() ]; (4) aninhar o loop de $users existente dentro de `foreach ($mesesAlvo as $mesReferencia)`, mantendo o try/catch, o Log::warning e os contadores; (5) trocar o filtro `->when($this->option('user'), fn($q,$id)=>$q->where('id',(int)$id))` por `->when($this->option('user'), fn($q,$ids)=>$q->whereIn('id', array_map('intval',(array)$ids)))` — array vazio é falsy, então sem --user o filtro não aplica (comportamento agendado preservado). Comentários pt-BR explicando o 2º alvo (SC1) e as options novas. NÃO mexer no filtro canônico de cargos nem no shape do compute.
  </action>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=WarmDesempenhoCacheTest</automated>
  </verify>
  <done>Warm sem options popula as 2 chaves (corrente + fechado) por user; --mes aquece só a competência dada; --user restringe aos IDs; --mes inválido retorna FAILURE sem 500. Teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/queue → command options | `--mes` / `--user` chegam de agendamento (confiável) OU do dispatch do controller (Plan 106-02, IDs de query interna já filtrada) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering/Input | `WarmDesempenhoCache` option `--mes` | mitigate | Validar `preg_match('/^\d{4}-\d{2}$/', $mes)` antes de Carbon; inválido → FAILURE, nunca query com valor cru (V5 ASVS) |
| T-106-02 | Input | `WarmDesempenhoCache` option `--user=*` | mitigate | `array_map('intval', (array)$ids)` — IDs sempre coeridos a int antes do whereIn |
| T-106-SC | Tampering | npm/pip/cargo installs | accept | Fase 106 não instala nenhum pacote novo (RESEARCH §Standard Stack) — gate de legitimidade não se aplica |
</threat_model>

<verification>
- `"C:/xampp/php/php.exe" artisan test --filter=DesempenhoScoreServiceCacheTest` verde
- `"C:/xampp/php/php.exe" artisan test --filter=WarmDesempenhoCacheTest` verde
- Diff de `DesempenhoScoreService.php` mostra APENAS o método isCached adicionado (compute/cacheKey/computeCached intocados) — `git diff app/Services/DesempenhoScoreService.php`
- Suíte de nota existente segue verde (não regride v18/105): `"C:/xampp/php/php.exe" artisan test --filter=DesempenhoScoreSnapshotTest`
</verification>

<success_criteria>
- SC1 provado por WarmDesempenhoCacheTest: 2 competências aquecidas (chaves distintas via isCached).
- Gate isCached provado por DesempenhoScoreServiceCacheTest, incluindo prova de zero-compute (preventStrayRequests).
- Nenhuma mudança em compute/cacheKey/computeCached (SC4 — não regride números).
</success_criteria>

<output>
Criar `.planning/phases/106-fix-timeout-mes-fechado-warm-cache-v18-0/106-01-SUMMARY.md` ao concluir.
</output>
