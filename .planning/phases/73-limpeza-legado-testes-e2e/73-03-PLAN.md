---
phase: 73-limpeza-legado-testes-e2e
milestone: v15.0
plan: 73-03
type: execute
wave: 2
depends_on: [73-01]
files_modified:
  - resources/js/Pages/Performance/Dashboard.jsx
  - resources/js/Pages/Companies/Show.jsx
autonomous: true
requirements: [NPS-F-01, NPS-F-02]
tags: [nps, frontend, cleanup, react, jsx, promotor-neutro-detrator, score-legacy, phase73]

must_haves:
  truths:
    - "grep -rn 'Promotor\\|Neutro\\|Detrator' resources/js/ retorna ZERO — SC#1 completo (combinado com Plan 73-01 backend)"
    - "grep -rn 'score_overall\\|score_consultant\\|score_mentor' resources/js/ retorna ZERO — SC#2"
    - "Performance/Dashboard.jsx substitui LABEL_CLASSE {Promotor: 'Positivo', ...} + BADGE_CLASSE e condicionais r.classe === 'Promotor'|'Neutro'|'Detrator' por cores por threshold direto (score >= 4 verde; <= 3 vermelho)"
    - "Payload consumido: nota (número 1-5, sem classe) — Performance/Dashboard.jsx classifica cor localmente no JSX; sem string 'classe'"
    - "Dashboard/Admin.jsx e Dashboard/User.jsx (se consumirem promotores/neutros/detratores como props) recebem novas chaves 'positivas'/'negativas' do Plan 73-01"
    - "Companies/Show.jsx remove comentário obsoleto linha 1001 sobre score_overall + qualquer ref a score_consultant/score_mentor mesmo em comentários"
    - "npm run build verde"
  artifacts:
    - path: resources/js/Pages/Performance/Dashboard.jsx
      provides: "Sem Promotor/Neutro/Detrator; cor por threshold direto"
      min_lines_delta: 30
    - path: resources/js/Pages/Companies/Show.jsx
      provides: "Sem refs a score_overall/consultant/mentor mesmo em comentários"
      min_lines_delta: 5
  key_links: []
---

<objective>
Concluir a limpeza dos rastros legados no frontend — Performance/Dashboard.jsx (Promotor/Neutro/Detrator + cores) e Companies/Show.jsx (comentário obsoleto score_overall). Fecha SC#1 e SC#2 do ROADMAP Phase 73.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/73-limpeza-legado-testes-e2e/73-01-PLAN.md
@resources/js/Pages/Performance/Dashboard.jsx
@resources/js/Pages/Companies/Show.jsx
@resources/js/lib/utils.js
</context>

<tasks>
<task id="73-03-T1">
<title>Performance/Dashboard.jsx — remover Promotor/Neutro/Detrator; cor por threshold direto</title>

<read_first>
- resources/js/Pages/Performance/Dashboard.jsx linhas 45-70 (LABEL_CLASSE + BADGE_CLASSE)
- resources/js/Pages/Performance/Dashboard.jsx linhas 355-375 (renderização badge com r.classe)
</read_first>

<action>
Em `resources/js/Pages/Performance/Dashboard.jsx`:

1. REMOVER blocos:
```jsx
// Ex. linha 52-58 e 60-64 — LABEL_CLASSE + BADGE_CLASSE
const LABEL_CLASSE = { Promotor: 'Positivo', Neutro: 'Neutro', Detrator: 'Insatisfeito' };
const BADGE_CLASSE = { Promotor: 'bg-emerald-500/15...', ... };
```

2. SUBSTITUIR por helper simples baseado em nota numérica:
```jsx
// Phase 73 Plan 03 — sem classificação Promotor/Neutro/Detrator (v15 escala 1-5).
// Cor por threshold direto na nota: >=4 positiva (verde), <=3 negativa (vermelho).
function corPorNota(nota) {
    if (nota == null) return 'bg-white/[0.03] border-white/[0.08] text-white/60';
    return nota >= 4
        ? 'bg-emerald-500/15 border-emerald-500/30 text-emerald-300'
        : 'bg-rose-500/15 border-rose-500/30 text-rose-300';
}
```

3. LOCALIZAR o bloco de renderização (~linha 355-375) que usa `r.classe === 'Promotor' && '...'` — substituir por `corPorNota(r.nota)`:

Antes:
```jsx
className={cn(
    r.classe === 'Promotor' && 'bg-emerald-500/15 border-emerald-500/30 text-emerald-300',
    r.classe === 'Neutro'   && 'bg-amber-500/15 border-amber-500/30 text-amber-300',
    r.classe === 'Detrator' && 'bg-rose-500/15 border-rose-500/30 text-rose-300',
)}
```

Depois:
```jsx
className={cn(corPorNota(r.nota))}
```

4. Remover qualquer OUTRO local que referencie `LABEL_CLASSE`, `BADGE_CLASSE`, `r.classe` ou strings `Promotor|Neutro|Detrator` — se sobrar em outro map/JSX, substituir por `nota`/`corPorNota`.

Comentário pt-BR obrigatório explicando a mudança na primeira ocorrência.
</action>

<acceptance_criteria>
- `grep -c "Promotor\\|Neutro\\|Detrator" resources/js/Pages/Performance/Dashboard.jsx` === 0
- Presença da helper corPorNota — assertar via `grep -n "corPorNota" resources/js/Pages/Performance/Dashboard.jsx`
- `grep -c "r\\.classe" resources/js/Pages/Performance/Dashboard.jsx` === 0
- Ambas cores emerald e rose ainda usadas para threshold — assertar via `grep -c "emerald-500\\|rose-500" resources/js/Pages/Performance/Dashboard.jsx` >= 2
- `npm run build` verde (execução na Task T3)
</acceptance_criteria>
</task>

<task id="73-03-T2">
<title>Companies/Show.jsx — remover comentário obsoleto score_overall e refs legacy</title>

<read_first>
- resources/js/Pages/Companies/Show.jsx linha 995-1010 (comentário e contexto)
</read_first>

<action>
1. Localizar linha 1001 comentário: `// Phase 31 (Plan 05) — score_empresa (1-5) substitui score_overall (0-10).`
2. REMOVER esse comentário — obsoleto pós-Phase 68/69/72
3. Fazer grep no arquivo por `score_overall`, `score_consultant`, `score_mentor` — se sobrar QUALQUER ocorrência (em código ou comentário), REMOVER
4. Não alterar código ativo (score_empresa/score_analista/score_estrategista continuam servindo via dual-path já implementado no Plan 72-02)
</action>

<acceptance_criteria>
- `grep -c "score_overall\\|score_consultant\\|score_mentor" resources/js/Pages/Companies/Show.jsx` === 0
- Nenhuma modificação em código ativo (só remove comentários obsoletos)
- `npm run build` verde (execução na Task T3)
</acceptance_criteria>
</task>

<task id="73-03-T3">
<title>Build + verificação final SC#1 e SC#2</title>

<action>
1. `npm run build` — verde
2. `grep -rn "Promotor\|Neutro\|Detrator" c:/xampp/htdocs/ecf_admin/ecf_admin/app c:/xampp/htdocs/ecf_admin/ecf_admin/resources/js 2>&1 | grep -v -i test | wc -l` — deve ser 0 (SC#1 completo)
3. `grep -rn "score_overall\|score_consultant\|score_mentor" c:/xampp/htdocs/ecf_admin/ecf_admin/app c:/xampp/htdocs/ecf_admin/ecf_admin/resources/js 2>&1 | grep -v -i test | wc -l` — deve ser 0 (SC#2 completo)
4. Suite baseline: `/c/xampp/php/php.exe artisan test tests/Feature/Phase31 tests/Feature/Phase33 tests/Feature/Phase68 tests/Feature/Phase69 tests/Feature/Phase70 tests/Feature/Phase71 tests/Feature/Phase72 --stop-on-failure 2>&1 | tail -5` — 146 verdes + 1 pré-existente
</action>

<acceptance_criteria>
- npm run build verde
- 2 greps retornam 0 (excluindo pastas test)
- Suite baseline verde
</acceptance_criteria>
</task>
</tasks>

<must_haves_summary>
- REQ NPS-F-01 100% coberto (frontend + backend combinados com Plan 73-01)
- REQ NPS-F-02 100% coberto
- SC#1 e SC#2 do ROADMAP fechados
</must_haves_summary>
