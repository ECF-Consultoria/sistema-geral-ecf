---
phase: 73-limpeza-legado-testes-e2e
milestone: v15.0
plan: 73-01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/PerformanceController.php
  - app/Http/Controllers/DashboardController.php
autonomous: true
requirements: [NPS-F-01]
tags: [nps, backend, cleanup, refactor, legacy-removal, promotor-neutro-detrator, dashboard, score-calculator, phase73]

must_haves:
  truths:
    - "grep -rn 'Promotor\\|Neutro\\|Detrator' app/ retorna ZERO — SC#1 do ROADMAP"
    - "PerformanceController.php:301 (classificação nota >=9/>=7/else) é REMOVIDA — payload de recentSurveys segue com 'nota' bruta + campo 'quando'; sem 'classe'"
    - "DashboardController linhas 530-575 (promotores/neutros/detratores counters via score_empresa) removidas — payload da view NPS agrega em 'positivas' (score >= 4) e 'negativas' (score <= 3) ao invés de 3 buckets"
    - "DashboardController linhas 885-888 e 1019-1020 ($scoreField = score_estrategista|score_analista) refatoradas — usa NpsScoreCalculator::compute($survey->response, 'estrategista'|'analista') via app() helper"
    - "Chaves das props Inertia mantêm mesmos nomes ('promotores'→'positivas', 'detratores'→'negativas'; 'neutros' removido) — frontend Performance/Dashboard.jsx (Plan 73-03) e Dashboard/Admin.jsx (Plan 73-03 se necessário) recebem os novos nomes"
    - "Zero mudança em NpsScoreCalculator, NpsPendingService, NpsTemplateService — reusa Phase 69/72"
    - "Suite baseline (Phases 31/33/68/69/70/71/72) permanece VERDE (delta = 0)"
  artifacts:
    - path: app/Http/Controllers/PerformanceController.php
      provides: "Método index/show refatorado — classificação Promotor/Neutro/Detrator REMOVIDA de linha ~301"
      min_lines_delta: 20
    - path: app/Http/Controllers/DashboardController.php
      provides: "Métodos adminDashboard/userDashboard refatorados — buckets promotores/neutros/detratores substituídos por positivas/negativas; scoreField legacy substituído por NpsScoreCalculator"
      min_lines_delta: 60
  key_links:
    - from: app/Http/Controllers/PerformanceController.php
      to: (removal)
      via: "Remove classificação Promotor/Neutro/Detrator; retorna nota bruta"
      pattern: "!Promotor|Neutro|Detrator"
    - from: app/Http/Controllers/DashboardController.php
      to: app/Services/Nps/NpsScoreCalculator.php
      via: "app(NpsScoreCalculator::class)->compute($survey->response, 'empresa'|'estrategista'|'analista')"
      pattern: "NpsScoreCalculator::class\\)\\)->compute"
---

<objective>
Executar a **limpeza backend** dos rastros legados de "Promotor/Neutro/Detrator" (herdado do NPS clássico escala 0-10 pré-v15.0) e do padrão `$scoreField = 'score_estrategista'|'score_analista'` (lê `NpsResponse->score_*` legado direto). Substitui pelo `NpsScoreCalculator` (Phase 69-02) que respeita `template_snapshot`. Cobre 100% do REQ NPS-F-01 no backend.

Purpose: SC#1 do ROADMAP exige `grep -rn "Promotor\|Neutro\|Detrator" app/` retornar zero. Hoje há 1 ocorrência (`PerformanceController.php:301`) + 4 buckets em `DashboardController.php` (linhas 530-575) que usam a lógica implícita da classificação (promotores=score 5, neutros=4, detratores=1-3). Este plan remove ambos e migra a fonte de leitura das médias por dimensão pro `NpsScoreCalculator`.

Output:
- 2 controllers modificados (blocos cirúrgicos, ~80 LOC no total)
- Zero rota nova, zero service novo, zero migration
- Zero mudança em NpsScoreCalculator/NpsPendingService/NpsTemplateService
- Chaves de props renomeadas para o frontend consumir no Plan 73-03
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/PHASE-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/PHASE-SUMMARY.md
@.planning/phases/70-ui-de-configuracao-admin/PHASE-SUMMARY.md
@.planning/phases/71-formul-rio-p-blico-din-mico/PHASE-SUMMARY.md
@.planning/phases/72-dashboards-pendencias-dia-cobranca/PHASE-SUMMARY.md
@app/Http/Controllers/PerformanceController.php
@app/Http/Controllers/DashboardController.php
@app/Services/Nps/NpsScoreCalculator.php
@app/Models/NpsResponse.php
@app/Models/NpsSurvey.php

<interfaces>
From app/Services/Nps/NpsScoreCalculator.php (Phase 69-02):
- `compute(NpsResponse $response, string $dimensao): ?float` — AVG option_peso_snapshot filtrado por dimensao ∈ {estrategista, analista, empresa, geral}
- Retorna null quando não há answers na dimensão

From app/Http/Controllers/PerformanceController.php linha ~290-310 (bloco `$npsRespostas = $recentSurveys->map(...)`):
- `$npsField` deve ser dimension-aware — no legacy vem como 'score_empresa' (default do usuário) ou 'score_estrategista'/'score_analista' quando o user é mentor/analista
- **Trade-off:** legacy usa escala 0-10 no threshold >=9/>=7 mas o CODE lê nps_responses.score_* que é escala 1-5 na v15 — inconsistência histórica que este plan resolve tornando o payload puramente numérico (frontend Plan 73-03 decide como exibir)

From app/Http/Controllers/DashboardController.php:
- Linhas 528-534: `$avgNps` (score_empresa via avg) — MANTER como está (só troca chave se necessário — aqui é média direta, não classificação)
- Linhas 564-575: buckets `promotores/neutros/detratores` — REMOVER estes buckets; substituir por `positivas` (score >= 4) e `negativas` (score <= 3). Neutro (4) some — passa a integrar 'positivas' porque a v15 tem escala 1-5 onde 4 é "bom"
- Linhas 885-888 e 1019-1020: `$scoreField = $u->isMentor() ? 'score_estrategista' : 'score_analista'` — substituir por chamada ao NpsScoreCalculator via reduce das surveys da carteira

Chaves alteradas (frontend Plan 73-03 vai consumir):
- Removidas: `promotores`, `neutros`, `detratores`
- Adicionadas: `positivas` (int), `negativas` (int)
- Mantidas: `avgNps` (float)
</interfaces>

<research_reference>
ROADMAP Phase 73 SCs:
- **SC#1** (grep zero occurrences) — ESTE PLAN elimina 5+ ocorrências em app/
- **SC#5** (delta = 0 na suite completa) — critério de aceite deste plan também; se algo quebrar aqui, rollback

Decisão de mapeamento 1-5 → semântica:
- Escala v15 é 1..5 SEMPRE (memória: "Escala 1-5 sempre — nunca 0-10 clássico NPS")
- Classificação limpa: `>=4 → positiva; <=3 → negativa` (só 2 buckets — v13 tinha 3 mas era artefato do modelo Promotor/Neutro/Detrator)
- Alternativa rejeitada: 3 buckets Excelente(5) / Bom(4) / Precisa melhorar(1-3) — mais complexo sem ganho analítico

**IMPORTANTE — Dual-path para surveys v15 vs legacy:**
- Surveys v15 (template_id != null): score vem de NpsScoreCalculator::compute($response, 'empresa')
- Surveys legacy (template_id === null): score vem de $response->score_empresa direto (Phase 31 preservado)
- Este plan preserva o dual-path — só REMOVE a classificação Promotor/Neutro/Detrator e reorganiza os buckets
</research_reference>
</context>

<tasks>
<task id="73-01-T1">
<title>Remover classificação Promotor/Neutro/Detrator em PerformanceController.php:301</title>

<read_first>
- app/Http/Controllers/PerformanceController.php linhas 290-320 (bloco recentSurveys.map)
- app/Services/Nps/NpsScoreCalculator.php (para uso opcional)
</read_first>

<action>
Em `app/Http/Controllers/PerformanceController.php`, no bloco de `$npsRespostas = $recentSurveys->map(...)` (linha ~294-310):

**Antes:**
```php
$npsRespostas = $recentSurveys->map(function ($s) use ($npsField) {
    $nota = $s->response?->$npsField;
    if ($nota === null) return null;
    $classe = $nota >= 9 ? 'Promotor' : ($nota >= 7 ? 'Neutro' : 'Detrator');
    return [
        'empresa' => $s->company?->name ?? '—',
        'nota'    => (int) $nota,
        'classe'  => $classe,
        'quando'  => optional($s->completed_at)->diffForHumans(),
    ];
})->filter()->values();
```

**Depois:**
```php
// Phase 73 Plan 01 (SC#1) — classificação Promotor/Neutro/Detrator (herança
// NPS 0-10 clássico) REMOVIDA. Payload segue com nota bruta 1-5 (v15) ou
// 1-5 legacy; frontend (Plan 73-03) exibe cor por threshold direto.
// Para surveys v15 (template_id != null), lê média por dimensão via
// NpsScoreCalculator respeitando template_snapshot.
$calculator = app(\App\Services\Nps\NpsScoreCalculator::class);
$npsRespostas = $recentSurveys->map(function ($s) use ($calculator) {
    $isV15 = $s->template_id !== null && $s->response;
    $nota  = $isV15
        ? $calculator->compute($s->response, 'empresa')
        : $s->response?->score_empresa;
    if ($nota === null) return null;
    return [
        'empresa' => $s->company?->name ?? '—',
        'nota'    => round((float) $nota, 2),  // pode ser float via avg
        'quando'  => optional($s->completed_at)->diffForHumans(),
    ];
})->filter()->values();
```

**Ajuste opcional:** o `$npsField` do escopo pai não é mais usado dentro desse map (só usávamos para pegar `$s->response?->$npsField`). Se o `$npsField` só existir para esse map, considere REMOVER a variável também — verificar contexto.
</action>

<acceptance_criteria>
- `grep -c "Promotor\\|Neutro\\|Detrator" app/Http/Controllers/PerformanceController.php` === 0
- Bloco novo com NpsScoreCalculator presente — assertar via `grep -n "NpsScoreCalculator" app/Http/Controllers/PerformanceController.php`
- Chave 'classe' NÃO aparece no payload de npsRespostas — assertar via `sed -n '/npsRespostas = /,/->filter/p' app/Http/Controllers/PerformanceController.php | ! grep -q "'classe'"`
- Comentário pt-BR explicativo presente
- `php -l app/Http/Controllers/PerformanceController.php` verde
</acceptance_criteria>
</task>

<task id="73-01-T2">
<title>Refatorar DashboardController.php linhas 528-575 — buckets promotores/neutros/detratores → positivas/negativas</title>

<read_first>
- app/Http/Controllers/DashboardController.php linhas 520-580 (bloco NPS Médio + buckets)
- app/Services/Nps/NpsScoreCalculator.php
- app/Models/NpsSurvey.php (relação com response + template_id)
</read_first>

<action>
Em `app/Http/Controllers/DashboardController.php` no bloco NPS (linhas ~520-580):

**Antes:**
```php
$avgNps = $npsResponses->avg(fn($s) => $s->response?->score_empresa) ?? 0;

// ... comentário legacy sobre classificação ...

$npsClassificacao = [
    'promotores' => $npsResponses->filter(fn($s) => (int) ($s->response?->score_empresa ?? 0) === 5)->count(),
    'neutros'    => $npsResponses->filter(fn($s) => (int) ($s->response?->score_empresa ?? 0) === 4)->count(),
    'detratores' => $npsResponses->filter(fn($s) => (int) ($s->response?->score_empresa ?? 0) >= 1
                                                && (int) ($s->response?->score_empresa ?? 0) <= 3)->count(),
];
```

**Depois:**
```php
// Phase 73 Plan 01 (SC#1) — média NPS dimension 'empresa' via NpsScoreCalculator
// para surveys v15; fallback score_empresa legado para pre-v15. Dual-path
// preservado (memória: "não quebrar histórico").
$calculator = app(\App\Services\Nps\NpsScoreCalculator::class);
$notaDe = function ($s) use ($calculator) {
    if ($s->template_id !== null && $s->response) {
        return $calculator->compute($s->response, 'empresa');
    }
    return $s->response?->score_empresa;
};

$avgNps = round(
    (float) $npsResponses->filter(fn($s) => $notaDe($s) !== null)
                         ->avg($notaDe),
    2
) ?: 0.0;

// Phase 73 Plan 01 (SC#1) — buckets simplificados escala 1-5.
// Promotor/Neutro/Detrator (herança 0-10) REMOVIDOS. v15 opera:
//   nota >= 4 → resposta positiva
//   nota <= 3 → resposta negativa
$npsClassificacao = [
    'positivas' => $npsResponses->filter(fn($s) => ($n = $notaDe($s)) !== null && $n >= 4)->count(),
    'negativas' => $npsResponses->filter(fn($s) => ($n = $notaDe($s)) !== null && $n <= 3)->count(),
];
```

**IMPORTANTE:** verificar em outros pontos do controller se `$npsClassificacao['promotores']`, `['neutros']` ou `['detratores']` são usados downstream — se sim, atualizar OU aceitar que o frontend consume os novos nomes.
</action>

<acceptance_criteria>
- `grep -c "promotores\\|neutros\\|detratores" app/Http/Controllers/DashboardController.php` === 0 (ou apenas comentários)
- `grep -c "'positivas'\\|'negativas'" app/Http/Controllers/DashboardController.php` >= 2
- Bloco novo com NpsScoreCalculator presente — assertar via `grep -c "NpsScoreCalculator" app/Http/Controllers/DashboardController.php` >= 1
- Comentário pt-BR explicativo presente
- `php -l app/Http/Controllers/DashboardController.php` verde
</acceptance_criteria>
</task>

<task id="73-01-T3">
<title>Refatorar $scoreField legacy em DashboardController.php linhas 885/1019 — usar NpsScoreCalculator</title>

<read_first>
- app/Http/Controllers/DashboardController.php linhas 880-900 e 1010-1035 (blocos $scoreField)
- app/Services/Nps/NpsScoreCalculator.php
</read_first>

<action>
Em `app/Http/Controllers/DashboardController.php`, localizar os 2 blocos `$scoreField = $user->isMentor() ? 'score_estrategista' : 'score_analista';` (linhas ~885-888 e ~1019-1020).

**Padrão de substituição:**

Antes:
```php
// exemplo do bloco linha 1019-1020:
$scoreField = $user->isMentor() ? 'score_estrategista' : 'score_analista';
$avgUser = $mySurveys->avg(fn($s) => $s->response?->$scoreField) ?? 0;
```

Depois:
```php
// Phase 73 Plan 01 (SC#1) — média por dimensão via NpsScoreCalculator para
// surveys v15; fallback direto na coluna legada para surveys pre-v15.
$dimensao   = $user->isMentor() ? 'estrategista' : 'analista';
$scoreField = $user->isMentor() ? 'score_estrategista' : 'score_analista';  // fallback legacy
$calculator = app(\App\Services\Nps\NpsScoreCalculator::class);
$avgUser = round(
    (float) $mySurveys->filter(function ($s) use ($calculator, $dimensao, $scoreField) {
        return $s->response !== null && (
            ($s->template_id !== null && $calculator->compute($s->response, $dimensao) !== null)
            || $s->response->$scoreField !== null
        );
    })->avg(function ($s) use ($calculator, $dimensao, $scoreField) {
        return $s->template_id !== null && $s->response
            ? $calculator->compute($s->response, $dimensao)
            : $s->response?->$scoreField;
    }),
    2
) ?: 0.0;
```

Repetir o padrão nos 2 blocos (~linha 885-888 e ~1019-1020). Se o padrão inicial for muito específico, extrair para closure privada `notaDimensaoUser` para reuso.

**Simplificação aceita:** se a lógica ficar muito verbosa, extrair helper privado no controller:
```php
private function avgNotaDimensao(iterable $surveys, string $dimensao, string $scoreFieldLegacy): float
{
    $calculator = app(\App\Services\Nps\NpsScoreCalculator::class);
    return round((float) collect($surveys)->avg(function ($s) use ($calculator, $dimensao, $scoreFieldLegacy) {
        if ($s->template_id !== null && $s->response) {
            return $calculator->compute($s->response, $dimensao);
        }
        return $s->response?->$scoreFieldLegacy;
    }), 2) ?: 0.0;
}
```

E chamar `$avgUser = $this->avgNotaDimensao($mySurveys, $dimensao, $scoreField);` nos 2 blocos.
</action>

<acceptance_criteria>
- `grep -c "\\$scoreField = " app/Http/Controllers/DashboardController.php` <= 2 (linhas legadas viraram fallback OU foram removidas)
- Presença de NpsScoreCalculator em pelo menos 3 pontos (2 novos blocos + T2) — assertar via `grep -c "NpsScoreCalculator" app/Http/Controllers/DashboardController.php` >= 3
- Se helper `avgNotaDimensao` foi criado, presente — assertar via `grep -n "avgNotaDimensao" app/Http/Controllers/DashboardController.php`
- Comentário pt-BR explicativo em cada bloco novo
- `php -l app/Http/Controllers/DashboardController.php` verde
</acceptance_criteria>
</task>

<task id="73-01-T4">
<title>Verificar suite baseline + smoke sanity</title>

<read_first>
- Todos os arquivos modificados nas T1-T3
</read_first>

<action>
1. `/c/xampp/php/php.exe -l app/Http/Controllers/PerformanceController.php`
2. `/c/xampp/php/php.exe -l app/Http/Controllers/DashboardController.php`
3. `grep -rn "Promotor\|Neutro\|Detrator" c:/xampp/htdocs/ecf_admin/ecf_admin/app 2>&1 | wc -l` — deve retornar 0 (SC#1 backend)
4. Rodar baseline suite:
   ```bash
   /c/xampp/php/php.exe artisan test tests/Feature/Phase31 tests/Feature/Phase33 tests/Feature/Phase68 tests/Feature/Phase69 tests/Feature/Phase70 tests/Feature/Phase71 tests/Feature/Phase72 --stop-on-failure 2>&1 | tail -5
   ```
   Deve continuar 146 verdes + 1 pré-existente Phase33 documentada (delta zero)

5. Se algum teste quebrar → BLOQUEAR e investigar. Erros esperados a monitorar:
   - Assertion Inertia sobre `nps_classificacao.promotores` — se algum teste checa isso, precisa ser atualizado para `.positivas`
   - Assertion sobre `classe` no npsRespostas — atualizar assertion
</action>

<acceptance_criteria>
- 2 arquivos `php -l` verdes
- `grep Promotor|Neutro|Detrator app/` retorna 0
- Suite baseline (Phases 31-72) continua verde (delta zero)
</acceptance_criteria>
</task>
</tasks>

<verification>
1. PerformanceController.php:301 classificação REMOVIDA
2. DashboardController promotores/neutros/detratores → positivas/negativas
3. DashboardController $scoreField → NpsScoreCalculator (dual-path preservado)
4. Backend grep Promotor|Neutro|Detrator retorna 0
5. Suite baseline permanece verde
</verification>

<must_haves_summary>
- REQ NPS-F-01 backend 100% coberto
- SC#1 (grep zero) parcialmente atendido no backend — completa no Plan 73-03 (frontend)
- Zero regressão na suite existente
</must_haves_summary>
