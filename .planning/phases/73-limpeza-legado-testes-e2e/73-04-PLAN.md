---
phase: 73-limpeza-legado-testes-e2e
milestone: v15.0
plan: 73-04
type: execute
wave: 3
depends_on: [73-01, 73-02, 73-03]
files_modified:
  - tests/Feature/Phase73/NpsV15E2ETest.php
  - tests/Feature/Phase73/NpsGoalMetricNpsTest.php
autonomous: true
requirements: [NPS-F-04]
tags: [nps, tests, feature, e2e, phase73, sc-verification, calculate-goal-results, integration]

must_haves:
  truths:
    - "NpsV15E2ETest cobre fluxo completo v15.0: (a) admin cria template com 2 perguntas + 5 options auto-escala, (b) associa serviço via sync, (c) dispatch mensal via NpsDispararMensal cria surveys com template_id, (d) cliente responde /nps/{token} com PreviewFormulario controlled, (e) NpsScoreCalculator retorna média correta por dimensão, (f) segundo submit no mesmo mês retorna AlreadyCompleted (dedup 23000), (g) NpsPendingService::forCarteira lista empresas pendentes após dia_cobranca — todos os fluxos em UM único teste linear"
    - "NpsGoalMetricNpsTest cobre Plan 73-02: (a) Goal metric='nps' com 3 respostas no período retorna média correta, (b) sem respostas → null, (c) dual-path v15+legacy: 2 responses v15 + 1 legacy → média correta"
    - "Suite Phase 73 solo: 5-7 tests passed (E2E é longo, ~3-5 sub-cenários no mesmo teste)"
    - "Suite completa NPS (Phases 31/33/68/69/70/71/72/73) verde — delta = tests do Phase 73"
    - "Zero regressão em Phases 31/33/68/69/70/71/72"
  artifacts:
    - path: tests/Feature/Phase73/NpsV15E2ETest.php
      provides: "E2E linear cobrindo 6+ fluxos da migração v15.0 num único teste"
      min_lines: 300
    - path: tests/Feature/Phase73/NpsGoalMetricNpsTest.php
      provides: "3-4 testes de metric='nps' no CalculateGoalResults"
      min_lines: 180
  key_links: []
---

<objective>
Entregar suite Feature Phase 73: 1 arquivo E2E linear cobrindo TODO o fluxo v15.0 (admin cria template → cliente responde → cálculo por dimensão → pendências) + 1 arquivo focado em `metric='nps'` do CalculateGoalResults (Plan 73-02). Fecha SC#4 e SC#5 do ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/PHASE-SUMMARY.md
@.planning/phases/70-ui-de-configuracao-admin/PHASE-SUMMARY.md
@.planning/phases/71-formul-rio-p-blico-din-mico/PHASE-SUMMARY.md
@.planning/phases/72-dashboards-pendencias-dia-cobranca/PHASE-SUMMARY.md
@.planning/phases/73-limpeza-legado-testes-e2e/73-01-PLAN.md
@.planning/phases/73-limpeza-legado-testes-e2e/73-02-PLAN.md
@app/Services/Nps/NpsTemplateService.php
@app/Services/Nps/NpsScoreCalculator.php
@app/Services/Nps/NpsPendingService.php
@app/Http/Controllers/NpsController.php
@app/Http/Controllers/NpsTemplateController.php
@app/Http/Controllers/NpsTemplateQuestionController.php
@app/Http/Controllers/NpsTemplateOptionController.php
@app/Jobs/CalculateGoalResults.php
@app/Console/Commands/NpsDispararMensal.php
@app/Models/Goal.php
@tests/Feature/Phase69/NpsSubmitDynamicValidationTest.php
@tests/Feature/Phase70/NpsTemplateCrudTest.php
@tests/Feature/Phase71/NpsRespondRenderTest.php
@tests/Feature/Phase72/NpsPendingServiceTest.php
</context>

<tasks>
<task id="73-04-T1">
<title>NpsV15E2ETest — 1 teste linear cobrindo 7 fluxos verticais + 3-5 testes focados em edge cases</title>

<read_first>
- tests/Feature/Phase69/NpsSubmitDynamicValidationTest.php (padrão fixture template + submit)
- tests/Feature/Phase70/NpsTemplateCrudTest.php (padrão actingAs admin + PATCH/POST)
- tests/Feature/Phase72/NpsPendingServiceTest.php (padrão setTestNow + Configuracao::set)
- app/Console/Commands/NpsDispararMensal.php (contrato de dispatch)
</read_first>

<action>
Criar `tests/Feature/Phase73/NpsV15E2ETest.php` no namespace `Tests\Feature\Phase73`.

Estrutura mínima:
```php
<?php
namespace Tests\Feature\Phase73;

use Tests\TestCase;
use Illuminate\Foundation\Testing\RefreshDatabase;
use App\Models\{User, Company, Servico, NpsTemplate, NpsTemplateQuestion, NpsTemplateOption, NpsSurvey, NpsResponse, Configuracao};
use Carbon\Carbon;
use Inertia\Testing\AssertableInertia as Assert;

class NpsV15E2ETest extends TestCase
{
    use RefreshDatabase;

    protected function tearDown(): void { Carbon::setTestNow(); parent::tearDown(); }
}
```

Testes:

1. **`test_e2e_v15_fluxo_completo_admin_cria_template_cliente_responde_calcula_e_detecta_pendente()`** (teste principal linear ~150 LOC):
   - **Setup:** admin, consultor, 1 servico, 1 company (com contrato ativo do servico), consultor no pivot da company
   - **Fluxo 1 (admin cria template):** actingAs admin → POST /nps/configuracao/templates com nome/priority=10 → assertDatabaseHas
   - **Fluxo 2 (admin adiciona pergunta escala):** POST /nps/configuracao/templates/{id}/perguntas com tipo=escala, dimensao='empresa', obrigatoria=true → assertDatabaseCount options=5
   - **Fluxo 3 (associa serviço):** PUT /nps/configuracao/templates/{id}/servicos com servicos=[servicoId] → assertDatabaseHas nps_template_service_scopes
   - **Fluxo 4 (dispatch mensal):** artisan `nps:disparar-mensal --now` → assertDatabaseHas nps_surveys com template_id=template.id
   - **Fluxo 5 (cliente responde):** GET /nps/{token} → assertInertia Nps/Respond has('template'); POST /nps/{token} com answers → assertInertia Nps/ThankYou; assertDatabaseCount nps_response_answers=1
   - **Fluxo 6 (calculator retorna média correta):** buscar $response criado; assertEquals(peso escolhido, calculator->compute($response, 'empresa'))
   - **Fluxo 7 (dedup + pendente):** POST /nps/{token} de novo → assertInertia Nps/AlreadyCompleted (23000); Carbon::setTestNow após dia_cobranca; assertar company NÃO está pendente (já respondeu)

2. **`test_e2e_v15_template_sem_analista_funciona_sem_quebrar()`:**
   - Setup: template com só 1 pergunta dimensao='empresa' (nenhuma de 'analista' ou 'estrategista')
   - Cliente responde; calculator->compute($response, 'analista') retorna null (não crasha)
   - Dashboard renderiza sem erro

3. **`test_e2e_dispatch_idempotente_nao_duplica_surveys()`:**
   - Setup: template default + company + 1 survey completed já existe no mês corrente
   - artisan nps:disparar-mensal → assertDatabaseCount nps_surveys === 1 (não cria segunda; guard 23000 do dedup)

4. **`test_e2e_pendencia_aparece_apos_dia_cobranca()`:**
   - Setup: Configuracao::set('nps_dia_cobranca', 15); template default + company sem survey
   - Carbon::setTestNow('2026-07-14'); assert forCarteira retorna vazio
   - Carbon::setTestNow('2026-07-16'); assert forCarteira retorna 1 empresa

5. **`test_e2e_snapshot_congelado_preserva_valor_apos_editar_template()`:**
   - Setup: template + option label='ótimo' peso=5; cliente responde; option gets snapshot
   - Admin edita template: option label passa a 'Excelente'
   - Verificar via NpsResponseAnswer que option_label_snapshot === 'ótimo' (congelado) e peso_snapshot === 5
</action>

<acceptance_criteria>
- File exists com 5 métodos test_
- Teste 1 (E2E linear) cobre 7 fluxos — assertar via `grep -c "assertInertia\\|assertDatabaseHas\\|assertDatabaseCount" tests/Feature/Phase73/NpsV15E2ETest.php` >= 15
- Uso de Carbon::setTestNow para guard temporal — assertar via `grep -c "setTestNow" tests/Feature/Phase73/NpsV15E2ETest.php` >= 3
- Teste snapshot congelado — assertar via `grep -n "option_label_snapshot" tests/Feature/Phase73/NpsV15E2ETest.php`
- `php artisan test tests/Feature/Phase73/NpsV15E2ETest.php` → 5/5 PASSED
</acceptance_criteria>
</task>

<task id="73-04-T2">
<title>NpsGoalMetricNpsTest — 3 testes cobrindo computeNps do Plan 73-02</title>

<read_first>
- app/Jobs/CalculateGoalResults.php (após T1 do Plan 73-02)
- app/Models/Goal.php + GoalResult.php
</read_first>

<action>
Criar `tests/Feature/Phase73/NpsGoalMetricNpsTest.php`.

Testes:

1. **`test_computeNps_com_3_responses_v15_retorna_media_correta()`:**
   - Setup: 1 company, 1 template com 1 pergunta dimensao='empresa', 3 surveys completed no período com answers pesos 5, 3, 4
   - Setup: Goal com metric='nps', company_id=company.id, período contém as 3 completed_at
   - Executar job via reflexão OU dispatchSync
   - Assert GoalResult.actual_value === 4.0 ((5+3+4)/3 = 4.0)

2. **`test_computeNps_sem_responses_no_periodo_retorna_null()`:**
   - Setup: Goal metric='nps' com período em mês futuro/passado sem responses
   - Assert GoalResult.actual_value === null (semântica "sem dado")

3. **`test_computeNps_dual_path_mistura_v15_e_legacy()`:**
   - Setup: 2 responses v15 (via template + answers peso=4 dimensao='empresa') + 1 response legacy (score_empresa=2)
   - Assert média === (4+4+2)/3 = 3.33 (arredondado)

**Nota:** se não for possível chamar `computeNps` privado, testar via job completo: instanciar `CalculateGoalResults` → dispatch com goal → verificar GoalResult persistido. Se model Goal exigir muitos campos obrigatórios, factory pode facilitar.
</action>

<acceptance_criteria>
- File exists com 3 métodos test_
- Assertar valor da média correta — assertar via `grep -c "assertEquals.*4\\|assertSame.*4" tests/Feature/Phase73/NpsGoalMetricNpsTest.php` >= 1
- Teste null semântico — assertar via `grep -n "assertNull\\|=== null" tests/Feature/Phase73/NpsGoalMetricNpsTest.php`
- `php artisan test tests/Feature/Phase73/NpsGoalMetricNpsTest.php` → 3/3 PASSED
</acceptance_criteria>
</task>

<task id="73-04-T3">
<title>Suite completa + SUMMARY + PHASE-SUMMARY</title>

<action>
1. Phase 73 solo: `/c/xampp/php/php.exe artisan test tests/Feature/Phase73 --stop-on-failure 2>&1 | tail -10` → 8 PASSED
2. Suite completa (Phases 31/33/68/69/70/71/72/73): `/c/xampp/php/php.exe artisan test tests/Feature/Phase31 tests/Feature/Phase33 tests/Feature/Phase68 tests/Feature/Phase69 tests/Feature/Phase70 tests/Feature/Phase71 tests/Feature/Phase72 tests/Feature/Phase73 2>&1 | tail -10` → (146 + 8) = 154 PASSED + 1 pré-existente Phase33
3. Escrever `.planning/phases/73-limpeza-legado-testes-e2e/73-04-SUMMARY.md`
4. Escrever `.planning/phases/73-limpeza-legado-testes-e2e/PHASE-SUMMARY.md` — consolidar 4 plans + REQs NPS-F-01 a F-04 fechados + SC1-SC5 PASSED + **Milestone v15.0 CONCLUÍDA (Phase 68-05 remanescente é o único item aberto)**
</action>

<acceptance_criteria>
- Phase 73 solo: 8 PASSED
- Suite completa: 154 PASSED (+8 delta)
- 2 SUMMARY files com métricas medidas
</acceptance_criteria>
</task>
</tasks>

<must_haves_summary>
- REQ NPS-F-04 100% coberto (E2E completa)
- SC#4 e SC#5 do ROADMAP fechados
- Milestone v15.0 finalizada (só resta 68-05)
</must_haves_summary>
