---
phase: 08-funda-o-de-notifica-es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_05_21_100001_create_notifications_table.php
  - tests/Feature/Notifications/Phase8FoundationTest.php
autonomous: true
requirements:
  - (infra) Slice 1 — Storage: tabela `notifications` (pré-requisito declarado pelo Goal da Phase 8; não tem REQ-ID próprio, mas é Success Criterion #1 da fase)

must_haves:
  truths:
    - "Migration `2026_05_21_100001_create_notifications_table.php` existe e roda sem erro em SQLite in-memory (testes) e em MySQL local (após `php artisan migrate`)."
    - "Após a migration, a tabela `notifications` existe com as 9 colunas canônicas do Laravel 12 conforme schema nativo (D-05): `id` (uuid PK), `type`, `notifiable_id`, `notifiable_type`, `data` (text com cast `array`, NÃO json), `read_at`, `created_at`, `updated_at` — sem coluna `categoria` dedicada."
    - "Phase 9 pode escrever em `notifications` via `Notification::send` sem mudança adicional de schema."
    - "Esqueleto de `Phase8FoundationTest.php` cria a suíte canônica que vai conter os 7 testes do escopo da fase (D-11): Test 1 schema; Tests 2/3/4 catálogo + AUTO_LIDERANCA; Tests 5/6 admin + líder via `hasPermission`; Test 7 smoke `Notification::send` com payload de 6 chaves."
  artifacts:
    - path: "database/migrations/2026_05_21_100001_create_notifications_table.php"
      provides: "Migration anônima Laravel 11+ que cria tabela `notifications` no schema canônico"
      contains: "Schema::create('notifications'"
    - path: "tests/Feature/Notifications/Phase8FoundationTest.php"
      provides: "Esqueleto da suíte Phase 8 com Test 1 (schema da tabela) GREEN"
      contains: "class Phase8FoundationTest extends TestCase"
  key_links:
    - from: "database/migrations/2026_05_21_100001_create_notifications_table.php"
      to: "tabela `notifications` (SQLite in-memory em testes; MySQL local em XAMPP em prod)"
      via: "Schema::create"
      pattern: "uuid\\('id'\\)->primary\\(\\)"
    - from: "tests/Feature/Notifications/Phase8FoundationTest.php"
      to: "Schema::hasTable / Schema::hasColumn"
      via: "RefreshDatabase migra tudo in-memory antes do teste"
      pattern: "Schema::hasTable\\('notifications'\\)"
---

## Phase Goal

**As a** desenvolvedor implementando o sistema de Notificações v3.0, **I want to** ter a tabela `notifications` migrada com o schema canônico Laravel 12, **so that** qualquer subsystem das Phases 9–12 pode persistir notificações via `Notification::send` sem mudança de schema.

<objective>
Slice 1 da fundação (MVP vertical) — entrega a camada de **storage**. Cria a migration `2026_05_21_100001_create_notifications_table.php` com o schema literal do stub canônico Laravel 12 (uuid PK, morphs polimórficos, text data, read_at nullable, timestamps) e ancora a suíte de testes da fase com `Phase8FoundationTest::test_migration_cria_tabela_notifications` GREEN.

**Purpose:** Sem esta tabela, nenhuma outra slice da Phase 8 nem das Phases 9–12 funciona — é o piso. Slice 2 (BaseNotification + enum + smoke test) depende desta migration estar no lugar para que `Notification::send` consiga gravar a linha em `notifications`.

**Output:**
- 1 migration nova (`2026_05_21_100001_create_notifications_table.php`)
- 1 arquivo de teste novo (`Phase8FoundationTest.php`) com Test 1 implementado (schema da tabela) — os outros 6 testes ficam como métodos vazios (`markTestIncomplete` ou stubs) que slices 2–4 vão preencher (compondo a suíte canônica de 7 testes — D-11)

**Sequência TDD intra-plan:** Tarefa 1 cria o esqueleto do teste (Test 1 fica RED — esperado, pois a migration ainda não existe). Tarefa 2 cria a migration e Test 1 vira GREEN. Esta ordem é proposital — segue Red→Green→Refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-funda-o-de-notifica-es/08-CONTEXT.md
@.planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md
@.planning/phases/08-funda-o-de-notifica-es/08-PATTERNS.md
@.planning/phases/08-funda-o-de-notifica-es/08-VALIDATION.md
@./CLAUDE.md
@database/migrations/2026_05_20_200005_create_setor_lideres_table.php
@tests/Feature/FechamentoMigrationTest.php

<interfaces>
<!-- Schema canônico Laravel 12 que esta migration DEVE replicar literalmente. -->
<!-- Source: vendor/laravel/framework/src/Illuminate/Notifications/Console/stubs/notifications.stub linhas 14-21 -->

Schema esperado da tabela `notifications`:
- `id` UUID char(36) PRIMARY KEY (gerado pelo dispatcher via Str::uuid())
- `type` string (FQCN da subclasse concreta de BaseNotification)
- `notifiable_id` unsignedBigInteger (gerado por `morphs('notifiable')`)
- `notifiable_type` string (gerado por `morphs('notifiable')`)
- índice composto em (`notifiable_type`, `notifiable_id`) (gerado por `morphs`)
- `data` TEXT (NÃO json — cast `array` no DatabaseNotification trata round-trip JSON)
- `read_at` timestamp nullable
- `created_at` + `updated_at` (timestamps padrão)

Padrão estrutural do projeto (envelope da migration):
- `return new class extends Migration { ... };` (Laravel 11+ anônima)
- PHPDoc pt-BR no topo explicando propósito de negócio
- Métodos `up()` e `down()` com PHPDoc pt-BR de 1 linha
- `down()` usa `Schema::dropIfExists('notifications')`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar diretório e esqueleto do teste (Wave 0 — pré-requisito do Test 1)</name>
  <files>tests/Feature/Notifications/Phase8FoundationTest.php</files>
  <read_first>
    - tests/Feature/FechamentoMigrationTest.php (template literal para o esqueleto: namespace, imports, RefreshDatabase, classe extends TestCase)
    - .planning/phases/08-funda-o-de-notifica-es/08-PATTERNS.md (§5 Test file — Analog A linhas 430–463 dá o boilerplate exato)
    - .planning/phases/08-funda-o-de-notifica-es/08-VALIDATION.md (§Per-Task Verification Map confirma o nome do filter `Phase8FoundationTest`)
    - .planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md (§Validation Architecture Test 1 linhas 580–589 para os 9 asserts)
    - CLAUDE.md (§Conventions §Comments — comentários em pt-BR; §Naming — método `test_` + snake_case)
  </read_first>
  <behavior>
    - Antes do Test 1 rodar, a tabela `notifications` ainda não existe no codebase — então sem a Tarefa 2 (migration) o Test 1 falha. Ordem TDD: criar teste primeiro (RED), depois criar migration (GREEN).
    - Test 1 (`test_migration_cria_tabela_notifications`) deve fazer `Schema::hasTable('notifications')` (1 assert) + 8 `Schema::hasColumn(...)` para: `id`, `type`, `notifiable_id`, `notifiable_type`, `data`, `read_at`, `created_at`, `updated_at`.
    - Os outros 6 testes da fase devem existir como métodos públicos no arquivo (assinatura `public function test_...(): void {}`) **com corpo `$this->markTestIncomplete('Implementado em slice X / Plan 0Y');`** — slices 2–4 vão preencher (compondo a suíte canônica de 7 testes — D-11).
    - O filter `php artisan test --filter=Phase8FoundationTest::test_migration_cria_tabela_notifications` precisa rodar isoladamente.
    - **Estado RED esperado ao final desta tarefa:** o arquivo de teste existe e está sintaticamente válido (`php -l` passa), mas Test 1 vai falhar quando executado (porque a tabela `notifications` ainda não foi criada). Isso é proposital e prova a sequência TDD — Tarefa 2 vai virar GREEN.
  </behavior>
  <action>
    Criar `tests/Feature/Notifications/Phase8FoundationTest.php` (subdiretório novo — autoload PSR-4 `Tests\\` → `tests/` já cobre via `composer.json` linha 39). Namespace: `Tests\Feature\Notifications`. Classe: `class Phase8FoundationTest extends TestCase` usando `use RefreshDatabase;`. Imports completos: `Illuminate\Foundation\Testing\RefreshDatabase`, `Illuminate\Support\Facades\Schema`, `Illuminate\Support\Facades\Notification`, `Illuminate\Notifications\DatabaseNotification`, `Tests\TestCase`, `App\Models\User`, `App\Models\Setor`, `App\Notifications\BaseNotification`, `App\Notifications\Categoria`, `App\Support\Permissions`. Implementar Test 1 (`test_migration_cria_tabela_notifications`) com 9 asserts (1 `hasTable` + 8 `hasColumn`). Declarar os outros 6 testes como métodos públicos vazios com `$this->markTestIncomplete('Implementado em slice futura');`: `test_base_notification_persiste_payload_canonico`, `test_permissions_all_inclui_notificacoes_criar`, `test_catalog_inclui_grupo_notificacoes`, `test_auto_lideranca_inclui_notificacoes_criar`, `test_admin_tem_permissao_via_short_circuit`, `test_lider_tem_permissao_via_auto_lideranca`. Comentários e docblocks em pt-BR conforme CLAUDE.md. Nada de `Notification::fake()` em nenhum lugar (Pitfall 1 do RESEARCH).
  </action>
  <verify>
    <automated>test -f tests/Feature/Notifications/Phase8FoundationTest.php && php -l tests/Feature/Notifications/Phase8FoundationTest.php</automated>
    <note>NÃO executar PHPUnit nesta tarefa — Test 1 vai estar RED de propósito (migration ainda não existe). O verify GREEN do Test 1 fica para Tarefa 2 (que cria a migration). Aqui verificamos apenas: (a) o arquivo existe no caminho correto, (b) `php -l` aceita a sintaxe.</note>
  </verify>
  <acceptance_criteria>
    - Source: arquivo `tests/Feature/Notifications/Phase8FoundationTest.php` existe.
    - Source: arquivo contém literalmente `namespace Tests\Feature\Notifications;`.
    - Source: arquivo contém literalmente `class Phase8FoundationTest extends TestCase`.
    - Source: arquivo contém literalmente `use RefreshDatabase;` dentro da classe.
    - Source: arquivo contém o método `public function test_migration_cria_tabela_notifications(): void` com pelo menos 9 chamadas a `$this->assertTrue(Schema::has...)` (1 `hasTable` + 8 `hasColumn`).
    - Source: arquivo contém os 6 métodos `test_*` adicionais com `markTestIncomplete` no corpo.
    - Source: arquivo NÃO contém a string `Notification::fake` em lugar nenhum (Pitfall 1).
    - CLI: `php -l tests/Feature/Notifications/Phase8FoundationTest.php` sai 0 (sintaxe válida).
    - **Estado RED esperado e aceito:** Test 1 vai falhar se executado neste momento (`Schema::hasTable('notifications')` retorna false porque a migration ainda não foi criada). Isto é o estado RED da sequência TDD e NÃO deve ser interpretado como falha de tarefa. O verify desta tarefa NÃO chama PHPUnit justamente para evitar essa ambiguidade. Após Tarefa 2 criar a migration, Test 1 vira GREEN.
    - **NÃO verificar:** "PHPUnit retorna exit code 0 ao filtrar Test 1" — isso é critério de aceitação da Tarefa 2, não desta.
  </acceptance_criteria>
  <done>
    Arquivo de teste criado no diretório novo `tests/Feature/Notifications/`, Test 1 implementado completo (9 asserts), demais 6 testes esqueletados com `markTestIncomplete`. Comentários em pt-BR. Sintaxe PHP válida. Test 1 vai RED neste momento (esperado e proposital — vira GREEN na Tarefa 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Criar migration `2026_05_21_100001_create_notifications_table.php` (faz Test 1 ficar GREEN)</name>
  <files>database/migrations/2026_05_21_100001_create_notifications_table.php</files>
  <read_first>
    - vendor/laravel/framework/src/Illuminate/Notifications/Console/stubs/notifications.stub (schema canônico — copy literal do corpo do `Schema::create`)
    - database/migrations/2026_05_20_200005_create_setor_lideres_table.php (estilo do projeto: `return new class extends Migration`, PHPDoc pt-BR, imports padrão)
    - database/migrations/2026_05_18_100001_create_adman_sync_logs_table.php (padrão de PHPDocs nos métodos `up()`/`down()`)
    - .planning/phases/08-funda-o-de-notifica-es/08-PATTERNS.md (§1 Migration — Composição final esperada linhas 96–134 dá o arquivo completo)
    - .planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md (§Common Pitfalls 3 e 4 — texto sobre `text` vs `json`, uuid PK)
    - .planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md (§Common Pitfalls 6 — timestamp posterior a `2026_05_20_200008`)
    - .planning/phases/08-funda-o-de-notifica-es/08-CONTEXT.md (D-05 — referência da decisão sobre formato da coluna `data`)
  </read_first>
  <behavior>
    - Após esta tarefa, `php artisan test --filter=Phase8FoundationTest::test_migration_cria_tabela_notifications` sai 0 (GREEN).
    - `Schema::hasTable('notifications')` retorna `true` após `RefreshDatabase` rodar todas as migrations em SQLite in-memory.
    - `Schema::hasColumn` retorna `true` para cada uma das 8 colunas: `id`, `type`, `notifiable_id`, `notifiable_type`, `data`, `read_at`, `created_at`, `updated_at`.
    - `php artisan migrate:status` lista a nova migration `2026_05_21_100001_create_notifications_table` como "Pending" antes de rodar e "Ran" depois.
    - `down()` permite rollback limpo: `Schema::dropIfExists('notifications')`.
  </behavior>
  <action>
    **Reconciliação D-05 vs stub canônico (W2 do revision):** D-05 do CONTEXT.md menciona `data` como tipo `json` em forma descritiva simplificada, mas o stub canônico Laravel 12 (`vendor/laravel/framework/src/Illuminate/Notifications/Console/stubs/notifications.stub` linha 18) usa `$table->text('data')` combinado com cast `array` no `DatabaseNotification` (vendor/.../DatabaseNotification.php linhas 47–50). Implementar com `text('data')` conforme **Pitfall 3 do RESEARCH** — esta é a forma canônica do framework e a única que mantém compatibilidade com o cast `array` nativo. Usar `json('data')` quebraria SQLite in-memory (driver) e contradiz o stub. Esta divergência é descritiva, não conflito de decisão.

    Criar `database/migrations/2026_05_21_100001_create_notifications_table.php`. Usar literalmente o padrão `return new class extends Migration` (Laravel 11+ anônimo), imports `Illuminate\Database\Migrations\Migration`, `Illuminate\Database\Schema\Blueprint`, `Illuminate\Support\Facades\Schema`. PHPDoc de classe em pt-BR explicando que é a tabela nativa do Laravel para o canal `database`, polimórfica via `notifiable_id`/`notifiable_type`, parte da fundação v3.0. Adicionar 1 linha de comentário em pt-BR na PHPDoc reconciliando D-05: "A coluna `data` é `text` (não `json`) — forma canônica Laravel 12; o cast `array` do `DatabaseNotification` (vendor/.../DatabaseNotification.php linhas 47-50) cuida do round-trip JSON automaticamente. D-05 do CONTEXT.md descreve o conteúdo como JSON (que é o efeito final), mas a coluna física é `text`." Método `up()` com PHPDoc pt-BR "Executa a migration: cria tabela `notifications` no schema canônico Laravel 12." e corpo: `Schema::create('notifications', function (Blueprint $table) { $table->uuid('id')->primary(); $table->string('type'); $table->morphs('notifiable'); $table->text('data'); $table->timestamp('read_at')->nullable(); $table->timestamps(); });`. Método `down()` com PHPDoc pt-BR "Reverte a migration: dropa a tabela." e corpo `Schema::dropIfExists('notifications');`. Não usar `json('data')` (Pitfall 3 — quebraria cast `array` do DatabaseNotification). Não tocar no PK (Pitfall 4 — UUID gerado pelo dispatcher via `Str::uuid()`). Timestamp do arquivo é `2026_05_21_100001` (estritamente posterior a `2026_05_20_200008` — Pitfall 6). Comentários inline em pt-BR explicando `morphs` (gera notifiable_id+type+índice composto) e `text` (cast array no model faz round-trip JSON).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase8FoundationTest::test_migration_cria_tabela_notifications</automated>
  </verify>
  <acceptance_criteria>
    - Source: arquivo `database/migrations/2026_05_21_100001_create_notifications_table.php` existe.
    - Source: arquivo contém literalmente `return new class extends Migration`.
    - Source: arquivo contém literalmente `Schema::create('notifications'`.
    - Source: arquivo contém literalmente `$table->uuid('id')->primary();`.
    - Source: arquivo contém literalmente `$table->morphs('notifiable');`.
    - Source: arquivo contém literalmente `$table->text('data');` (NÃO `$table->json('data');`).
    - Source: arquivo contém literalmente `$table->timestamp('read_at')->nullable();`.
    - Source: arquivo contém literalmente `Schema::dropIfExists('notifications');` no método `down()`.
    - Source: PHPDoc da classe e dos métodos `up()/down()` estão em pt-BR.
    - Source: PHPDoc da classe contém nota reconciliando D-05 (`text` vs `json`) conforme W2.
    - Behavior: `php artisan test --filter=Phase8FoundationTest::test_migration_cria_tabela_notifications` sai 0 (Test 1 GREEN).
    - CLI: `php artisan migrate:status` lista `2026_05_21_100001_create_notifications_table` na lista de migrations conhecidas.
  </acceptance_criteria>
  <done>
    Migration criada, Test 1 verde, schema canônico Laravel 12 fielmente replicado. Reconciliação D-05 documentada na PHPDoc. Pronto para Slice 2 (BaseNotification + enum + smoke test) escrever na tabela.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ SQLite in-memory | Slice 1 opera puramente em ambiente de teste / migration runner; sem interação user/HTTP. |
| Migration ↔ Production database | `php artisan migrate` em XAMPP local cria a tabela em MySQL — fora do agente, executado pelo dev. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Schema `data` JSON | accept | RESEARCH §Common Pitfall 3: `text('data')` + cast `array` no `DatabaseNotification` (vendor lines 47–50) é o padrão Laravel 12. Não usar `json('data')` introduziria dependência de versão SQLite/MySQL JSON e contradiz o stub canônico. Severity: **low** — payload em `data` é controlado pela aplicação, não por input não-confiável (categoria já é validada por enum tipado em Slice 2). |
| T-08-02 | Denial of Service | Crescimento ilimitado da tabela `notifications` | transfer | Out of scope desta fase. Cleanup diário (descartar lidas com >30 dias) é REQ POLL-04 entregue na Phase 12. Severity: **low** — sem tráfego ainda; tabela vazia durante toda a Phase 8. |
| T-08-03 | Tampering | Ordem de execução de migrations | mitigate | Timestamp `2026_05_21_100001` é estritamente posterior à última migration existente (`2026_05_20_200008`). Pitfall 6 do RESEARCH. Acceptance criteria checa via `migrate:status`. Severity: **low**. |

**Não aplicável nesta slice:** V4 Access Control (sem rota/middleware), V2 Authentication (sem login), V6 Cryptography (sem cripto).
</threat_model>

<verification>
**Após Tarefa 1:** Arquivo de teste existe e tem sintaxe PHP válida. Test 1 está RED (esperado — migration ainda não existe). NÃO executar PHPUnit nesta etapa.
**Após Tarefa 2:** Test 1 vira GREEN via `php artisan test --filter=Phase8FoundationTest::test_migration_cria_tabela_notifications`.
**Manual (dev host XAMPP, fora do agente):** `php artisan migrate` cria a tabela `notifications` em MySQL real. Confirmar via Tinker: `\Illuminate\Support\Facades\Schema::hasTable('notifications')` retorna `true`.
</verification>

<success_criteria>
- Tabela `notifications` existe no schema com 9 colunas canônicas em SQLite in-memory (testes) e em MySQL local (após `php artisan migrate` manual).
- Test 1 `Phase8FoundationTest::test_migration_cria_tabela_notifications` está GREEN (após Tarefa 2).
- Os 6 outros testes estão declarados como esqueletos (`markTestIncomplete`) prontos para slices 2–4.
- Cobre 1/3 de Success Criterion #1 do ROADMAP (tabela migrada). Os outros 2/3 (BaseNotification persiste + integração ponta-a-ponta) ficam com Slice 2.
</success_criteria>

<output>
Criar `.planning/phases/08-funda-o-de-notifica-es/08-01-SUMMARY.md` ao final, registrando:
- Arquivos criados (caminhos absolutos)
- Test 1 status (GREEN após Tarefa 2)
- Próxima slice (Plan 02 — Slice 2 Storage→Domain types)
- Confirmação de pt-BR em comentários e PHPDocs
- Nota: estado RED de Test 1 ao final de Tarefa 1 foi intencional (sequência TDD)
</output>
