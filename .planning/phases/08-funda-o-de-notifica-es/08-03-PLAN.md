---
phase: 08-funda-o-de-notifica-es
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - app/Support/Permissions.php
  - tests/Feature/Notifications/Phase8FoundationTest.php
autonomous: true
requirements:
  - PERM-01
  - PERM-03

must_haves:
  truths:
    - "`App\\Support\\Permissions::NOTIFICACOES_CRIAR === 'notificacoes.criar'` (constante pública exposta — D-07)."
    - "`App\\Support\\Permissions::all()` retorna array que contém `'notificacoes.criar'`."
    - "`App\\Support\\Permissions::isValid('notificacoes.criar')` retorna `true`."
    - "`App\\Support\\Permissions::catalog()` retorna array com a chave `'Notificações'`."
    - "`App\\Support\\Permissions::catalog()['Notificações']` é um array que contém uma entry com `'key' => 'notificacoes.criar'`, `'label' => 'Criar notificações'`, `'description'` em pt-BR mencionando \"manuais\" e \"usuários\" / \"setores\" / \"líderes\" / \"todos\" (D-08)."
    - "`App\\Support\\Permissions::AUTO_LIDERANCA` contém `'notificacoes.criar'` (D-09) (junto com as 3 keys de liderança originais) — isso é o que faz líderes herdarem a permissão automaticamente via merge em `User::effectivePermissions()`."
    - "O grupo `'Notificações'` aparece em `catalog()` POSICIONADO entre o grupo `'Sistema'` e o grupo `'Liderança (automático para líderes)'` (D-08)."
  artifacts:
    - path: "app/Support/Permissions.php"
      provides: "Catálogo de permissions com a nova chave `notificacoes.criar` registrada em 3 lugares (constante + catalog group + AUTO_LIDERANCA)"
      contains: "NOTIFICACOES_CRIAR"
    - path: "tests/Feature/Notifications/Phase8FoundationTest.php"
      provides: "Tests 2, 3 e 4 GREEN (cobertura PERM-01 + PERM-03 no nível de catálogo)"
      contains: "test_permissions_all_inclui_notificacoes_criar"
  key_links:
    - from: "app/Support/Permissions.php (constante NOTIFICACOES_CRIAR)"
      to: "app/Support/Permissions.php (entry no catalog group `'Notificações'` + entrada em AUTO_LIDERANCA)"
      via: "self::NOTIFICACOES_CRIAR referenciado dentro do mesmo arquivo"
      pattern: "self::NOTIFICACOES_CRIAR"
    - from: "app/Support/Permissions.php (catalog)"
      to: "app/Support/Permissions.php (método all() que varre o catalog)"
      via: "all() faz foreach em catalog() e coleta keys — chave nova aparece automaticamente"
      pattern: "foreach \\(self::catalog\\(\\) as \\$group\\)"
---

## Phase Goal

**As a** desenvolvedor implementando o sistema de Notificações v3.0, **I want to** ter `notificacoes.criar` registrada no catálogo `App\Support\Permissions` em 3 lugares (constante + grupo `'Notificações'` em `catalog()` + entrada em `AUTO_LIDERANCA`), **so that** a UI de setores (Phase 12) consegue listar e atribuir essa permission, líderes ganham automaticamente via short-circuit já existente, e `Permissions::all()` / `isValid()` reconhecem a chave nova.

<objective>
Slice 3 da fundação (MVP vertical) — entrega o **permission catalog**. Faz 3 edições cirúrgicas em `app/Support/Permissions.php`: (1) adiciona constante pública `NOTIFICACOES_CRIAR = 'notificacoes.criar'` com PHPDoc pt-BR, (2) insere novo grupo `'Notificações'` no array `catalog()` entre `'Sistema'` e `'Liderança (automático para líderes)'`, (3) adiciona `self::NOTIFICACOES_CRIAR` ao final do array `AUTO_LIDERANCA`. Preenche Tests 2, 3 e 4 da suíte (`test_permissions_all_inclui_notificacoes_criar`, `test_catalog_inclui_grupo_notificacoes`, `test_auto_lideranca_inclui_notificacoes_criar`).

**Purpose:** Sem esta edição, Phase 12 (UI de setores) não consegue mostrar/atribuir `notificacoes.criar`; líderes não ganham automaticamente (PERM-03 falha); admin via short-circuit funcionaria mas a chave não seria reconhecida por `isValid()` em FormRequests. Esta slice é o "registro oficial" da nova permission no catálogo canônico do projeto. PERM-02 (admin short-circuit) **não exige código novo** (D-10) — fica para Plan 04 fazer verificação ponta-a-ponta junto com Test 5.

**Output:**
- 1 arquivo editado (`app/Support/Permissions.php` — 3 edições cirúrgicas)
- 3 testes preenchidos no arquivo de teste (Tests 2, 3, 4)

**Cobre:** PERM-01 (registro completo no catálogo com label/descrição pt-BR) + PERM-03 (registro em AUTO_LIDERANCA — a verificação end-to-end via `hasPermission` fica no Plan 04). D-07, D-08, D-09 implementados.

**Dependência de wave (revision B1):** Este plan está em wave 3 com `depends_on: [08-02]` porque escreve em `tests/Feature/Notifications/Phase8FoundationTest.php` — o mesmo arquivo editado por Plans 02 e 04. Para evitar clobber de arquivo, os 3 plans (02, 03, 04) são serializados sequencialmente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-funda-o-de-notifica-es/08-CONTEXT.md
@.planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md
@.planning/phases/08-funda-o-de-notifica-es/08-PATTERNS.md
@.planning/phases/08-funda-o-de-notifica-es/08-VALIDATION.md
@./CLAUDE.md
@app/Support/Permissions.php
@tests/Feature/Notifications/Phase8FoundationTest.php

<interfaces>
<!-- Estado atual de Permissions.php (verificado linha a linha): -->
<!-- - Linha 63: const SISTEMA_ACTIVITY_LOG = 'sistema.activity_log'; -->
<!-- - Linha 64: const SISTEMA_DESENVOLVIMENTO = 'sistema.desenvolvimento'; -->
<!-- - Linha 65: const SISTEMA_SETORES = 'sistema.setores'; -->
<!-- - Linhas 67-72: constantes LIDERANCA_* -->
<!-- - Linhas 78-82: array AUTO_LIDERANCA com 3 entries (LIDERANCA_DASHBOARD_SETOR, LIDERANCA_DEFINIR_METAS, LIDERANCA_VER_MEMBROS) -->
<!-- - Linhas 126-130: grupo 'Sistema' em catalog() com 3 entries -->
<!-- - Linhas 131-135: grupo 'Liderança (automático para líderes)' em catalog() -->

3 edições cirúrgicas (D-07, D-08, D-09):

1. CONSTANTE NOVA — inserir após linha 65 (`SISTEMA_SETORES`), antes da linha 67 (`LIDERANCA_DASHBOARD_SETOR`):
   ```
   /** Criar notificações manuais (admin sempre tem; líderes ganham via AUTO_LIDERANCA). */
   public const NOTIFICACOES_CRIAR        = 'notificacoes.criar';
   ```
   Alinhamento de `=` com espaços (CLAUDE.md §Code Style alignment-style).

2. AUTO_LIDERANCA — adicionar nova linha ao final do array (após `LIDERANCA_VER_MEMBROS`, antes do `]`):
   ```
   self::NOTIFICACOES_CRIAR,
   ```
   Trailing comma já é o padrão (linha 81).

3. CATALOG — inserir novo grupo entre o grupo `'Sistema'` (linha 126-130) e `'Liderança (automático para líderes)'` (linha 131):
   ```
   'Notificações' => [
       ['key' => self::NOTIFICACOES_CRIAR, 'label' => 'Criar notificações', 'description' => 'Envia notificações manuais para usuários, setores, líderes ou todos'],
   ],
   ```

Os métodos `all()` e `isValid()` NÃO precisam de mudança — eles varrem `catalog()` dinamicamente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: 3 edições cirúrgicas em `app/Support/Permissions.php`</name>
  <files>app/Support/Permissions.php</files>
  <read_first>
    - app/Support/Permissions.php (estado atual COMPLETO — verificar linhas 5–18 docblock de classe, 63–65 constantes SISTEMA_*, 67–72 constantes LIDERANCA_*, 78–82 AUTO_LIDERANCA, 126–130 grupo 'Sistema' em catalog(), 131–135 grupo 'Liderança')
    - .planning/phases/08-funda-o-de-notifica-es/08-CONTEXT.md (D-07, D-08, D-09 — valores exatos da constante, label, descrição, posicionamento)
    - .planning/phases/08-funda-o-de-notifica-es/08-PATTERNS.md (§4 Permissions edit linhas 342–423 — 3 edições com source lines exatas e formato a replicar)
    - .planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md (§Common Pitfalls 5 — AUTO_LIDERANCA é const, não pode ser mutado em runtime; deve ser editado no arquivo)
    - CLAUDE.md (§Code Style — alignment-style formatting; §Comments — pt-BR)
  </read_first>
  <behavior>
    - Após esta edição, `Permissions::NOTIFICACOES_CRIAR` resolve para `'notificacoes.criar'` (constante pública).
    - `Permissions::all()` retorna lista contendo `'notificacoes.criar'` automaticamente (sem mudança em `all()` — o método varre `catalog()` dinamicamente).
    - `Permissions::isValid('notificacoes.criar')` retorna `true`.
    - `Permissions::catalog()` tem chave `'Notificações'` posicionada entre `'Sistema'` e `'Liderança (automático para líderes)'`.
    - `Permissions::catalog()['Notificações']` é array com 1 entry: `['key' => 'notificacoes.criar', 'label' => 'Criar notificações', 'description' => 'Envia notificações manuais para usuários, setores, líderes ou todos']`.
    - `Permissions::AUTO_LIDERANCA` contém 4 entries (3 originais + `'notificacoes.criar'`).
    - Os métodos `all()` (linhas 144-153) e `isValid()` (linhas 156-159) NÃO são alterados.
    - Os 3 grupos pré-existentes (`'Core (ECF Consultoria)'`, `'Publicações (MLB)'`, `'Administrativo'`, `'Sistema'`, `'Liderança ...'`) permanecem inalterados em conteúdo e ordem (exceto que `'Notificações'` agora aparece entre `'Sistema'` e `'Liderança'`).
  </behavior>
  <action>
    Editar `app/Support/Permissions.php` em 3 pontos cirúrgicos:

    (A) Adicionar nova constante pública após `SISTEMA_SETORES` (atualmente linha 65) e antes da seção `LIDERANCA_*` que começa na linha 67. Inserir 2 linhas: PHPDoc de 1 linha em pt-BR `/** Criar notificações manuais (admin sempre tem; líderes ganham via AUTO_LIDERANCA). */` + declaração `public const NOTIFICACOES_CRIAR        = 'notificacoes.criar';`. Manter alinhamento de `=` consistente com as linhas vizinhas (espaços, não tabs — observar o padrão das linhas 63–65 que tem `=` na mesma coluna).

    (B) Editar o array `AUTO_LIDERANCA` (atualmente linhas 78–82) para adicionar uma 4ª entry `self::NOTIFICACOES_CRIAR,` (com trailing comma — padrão já presente na linha 81) antes do `]` final. Pode opcionalmente adicionar comentário inline `// notificações: líderes ganham automaticamente (PERM-03)`.

    (C) Inserir um novo grupo `'Notificações'` no array retornado por `catalog()` (atualmente linhas 92–136) entre o fechamento do grupo `'Sistema'` (linha 130 termina com `],`) e a abertura do grupo `'Liderança (automático para líderes)'` (linha 131). O grupo novo tem exatamente 1 entry com 3 chaves: `'key' => self::NOTIFICACOES_CRIAR`, `'label' => 'Criar notificações'`, `'description' => 'Envia notificações manuais para usuários, setores, líderes ou todos'`. Trailing comma após o `]` final do grupo (padrão observado nas linhas 106, 119, 125, 130).

    NÃO editar `all()`, NÃO editar `isValid()`, NÃO editar `catalog()` em nenhum grupo pré-existente, NÃO mudar a ordem dos grupos `Core`/`MLB`/`Administrativo`/`Sistema` (apenas inserir `'Notificações'` no ponto exato), NÃO tocar `app/Models/User.php` (D-10 — short-circuit + AUTO_LIDERANCA merge existentes resolvem PERM-02/03 sem código novo no model). Comentários em pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase8FoundationTest</automated>
  </verify>
  <acceptance_criteria>
    - Source: `app/Support/Permissions.php` contém literalmente `public const NOTIFICACOES_CRIAR` (com `=` e `'notificacoes.criar'`).
    - Source: a constante `NOTIFICACOES_CRIAR` está declarada DEPOIS de `SISTEMA_SETORES` e ANTES de `LIDERANCA_DASHBOARD_SETOR` (posicionamento entre as seções `SISTEMA_*` e `LIDERANCA_*`).
    - Source: a constante tem PHPDoc de 1 linha em pt-BR imediatamente acima dela.
    - Source: o array `AUTO_LIDERANCA` contém literalmente `self::NOTIFICACOES_CRIAR,` como nova entry após `self::LIDERANCA_VER_MEMBROS,`.
    - Source: o array `AUTO_LIDERANCA` agora tem exatamente 4 entries (era 3).
    - Source: `catalog()` contém literalmente a string `'Notificações' =>` (com cedilha) como chave de grupo.
    - Source: o grupo `'Notificações'` aparece DEPOIS do grupo `'Sistema'` e ANTES do grupo `'Liderança (automático para líderes)'` na ordem do array retornado por `catalog()`.
    - Source: o grupo `'Notificações'` contém uma entry com `'label' => 'Criar notificações'` (string exata, com cedilha em "Notificações" não aparece aqui mas em "Notificações" do grupo sim).
    - Source: a entry do grupo `'Notificações'` contém `'description' => 'Envia notificações manuais para usuários, setores, líderes ou todos'` (string exata em pt-BR).
    - Source: os 3 grupos pré-existentes (`'Sistema'`, `'Liderança (automático para líderes)'`, `'Administrativo'`) NÃO tiveram entries adicionadas, removidas ou alteradas.
    - Source: `all()` e `isValid()` permanecem com o corpo exato (não foram editados).
    - Source: `app/Models/User.php` NÃO foi modificado neste plan (D-10).
    - Behavior: após esta tarefa, Tarefa 2 (Tests 2/3/4) deve passar GREEN sem mais nada.
  </acceptance_criteria>
  <done>
    `Permissions.php` estendido com 3 edições homogêneas espelhando padrões internos. `NOTIFICACOES_CRIAR` registrado em constante + catalog group `'Notificações'` + `AUTO_LIDERANCA`. Pronto para Tarefa 2 preencher os 3 testes de validação.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Preencher Tests 2, 3 e 4 (`permissions_all`, `catalog`, `auto_lideranca`)</name>
  <files>tests/Feature/Notifications/Phase8FoundationTest.php</files>
  <read_first>
    - .planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md (§Validation Architecture Tests 2/3/4 linhas 592–615 — corpos literais de cada teste com asserts exatos)
    - .planning/phases/08-funda-o-de-notifica-es/08-PATTERNS.md (§5 Test file — usar `\App\Support\Permissions` via import já adicionado em Plan 01)
    - .planning/phases/08-funda-o-de-notifica-es/08-VALIDATION.md (§Per-Task Verification Map linhas 44–46 — confirma os 3 IDs de teste e seus REQ links)
    - app/Support/Permissions.php (estado atual + edições de Tarefa 1 — verificar nomes finais)
    - tests/Feature/Notifications/Phase8FoundationTest.php (esqueleto criado em Plan 01 — os 3 métodos já existem em `markTestIncomplete`)
  </read_first>
  <behavior>
    - Após esta tarefa:
      - `php artisan test --filter=Phase8FoundationTest::test_permissions_all_inclui_notificacoes_criar` sai 0 (GREEN).
      - `php artisan test --filter=Phase8FoundationTest::test_catalog_inclui_grupo_notificacoes` sai 0 (GREEN).
      - `php artisan test --filter=Phase8FoundationTest::test_auto_lideranca_inclui_notificacoes_criar` sai 0 (GREEN).
    - Total de tests GREEN ao final: 5/7 (Tests 1, 2, 3, 4, 7). Tests 5 e 6 ainda em `markTestIncomplete` (preenchidos em Plan 04).
    - Os 3 testes operam sobre o catálogo estático (sem `RefreshDatabase` strict, mas o trait está na classe — não conflita).
  </behavior>
  <action>
    Editar `tests/Feature/Notifications/Phase8FoundationTest.php`. Substituir os corpos dos 3 métodos que estão em `markTestIncomplete` desde Plan 01:

    **Test 2 (`test_permissions_all_inclui_notificacoes_criar`)** — corpo:
    `$this->assertContains('notificacoes.criar', \App\Support\Permissions::all());`
    `$this->assertSame('notificacoes.criar', \App\Support\Permissions::NOTIFICACOES_CRIAR);`
    `$this->assertTrue(\App\Support\Permissions::isValid('notificacoes.criar'));`

    **Test 3 (`test_catalog_inclui_grupo_notificacoes`)** — corpo:
    `$catalog = \App\Support\Permissions::catalog();`
    `$this->assertArrayHasKey('Notificações', $catalog);`
    `$entry = collect($catalog['Notificações'])->firstWhere('key', 'notificacoes.criar');`
    `$this->assertNotNull($entry);`
    `$this->assertSame('Criar notificações', $entry['label']);`
    `$this->assertStringContainsString('manuais', $entry['description']);`

    **Test 4 (`test_auto_lideranca_inclui_notificacoes_criar`)** — corpo:
    `$this->assertContains('notificacoes.criar', \App\Support\Permissions::AUTO_LIDERANCA);`

    Pode usar imports já presentes (`use App\Support\Permissions;` foi adicionado em Plan 01) — referência curta `Permissions::all()` é equivalente. Comentários inline em pt-BR opcionais explicando o que cada teste prova (qual Success Criterion / REQ-ID). NÃO tocar nos métodos `test_admin_tem_permissao_via_short_circuit` nem `test_lider_tem_permissao_via_auto_lideranca` (Plan 04). NÃO tocar no método `test_base_notification_persiste_payload_canonico` (já foi preenchido no Plan 02).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase8FoundationTest</automated>
  </verify>
  <acceptance_criteria>
    - Source: método `test_permissions_all_inclui_notificacoes_criar` contém os 3 asserts (`assertContains`, `assertSame` com constante, `assertTrue` com `isValid`).
    - Source: método `test_catalog_inclui_grupo_notificacoes` contém os 5 asserts (`assertArrayHasKey`, `firstWhere`, `assertNotNull`, `assertSame` no label, `assertStringContainsString` em 'manuais').
    - Source: método `test_auto_lideranca_inclui_notificacoes_criar` contém o assert `assertContains` em `AUTO_LIDERANCA`.
    - Source: NENHUM dos 3 métodos contém mais `markTestIncomplete`.
    - Behavior: `php artisan test --filter=Phase8FoundationTest::test_permissions_all_inclui_notificacoes_criar` sai 0.
    - Behavior: `php artisan test --filter=Phase8FoundationTest::test_catalog_inclui_grupo_notificacoes` sai 0.
    - Behavior: `php artisan test --filter=Phase8FoundationTest::test_auto_lideranca_inclui_notificacoes_criar` sai 0.
    - Behavior: `php artisan test --filter=Phase8FoundationTest` mostra 5/7 GREEN (1, 2, 3, 4, 7) + 2/7 SKIPPED (5, 6 ainda em `markTestIncomplete`). Zero RED.
  </acceptance_criteria>
  <done>
    Tests 2, 3 e 4 preenchidos e GREEN. PERM-01 (registro do catálogo) e PERM-03 (presença em AUTO_LIDERANCA) verificados no nível de catálogo. A verificação E2E via `hasPermission()` (Test 5 e Test 6) fica para Plan 04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code edit ↔ Runtime catalog | Edição estática em `Permissions.php` define o catálogo canônico. Não há input de usuário; mudança só via commit + deploy. |
| Catalog ↔ Authorization layer (User::hasPermission) | Plan 03 estabelece a chave no catálogo; Plan 04 prova que a resolução via `hasPermission()` continua correta com a nova chave. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Elevation of Privilege | Mutação de `AUTO_LIDERANCA` em runtime para adicionar permission arbitrária | mitigate | `AUTO_LIDERANCA` é `public const` PHP — imutável em runtime. PHP rejeita `Permissions::AUTO_LIDERANCA[] = ...;`. Mudança requer commit + deploy + code review. RESEARCH §Security Domain + §Common Pitfall 5 + Acceptance Criteria checa via source assertion. Severity: **low** — mitigação estrutural por design da linguagem. |
| T-08-08 | Tampering | Chave de catalog não-listada em `all()` (chave fantasma) | mitigate | `Permissions::all()` (linhas 144–153) varre `catalog()` dinamicamente — não é possível adicionar entry ao catalog sem que apareça em `all()`. Test 2 prova esta propriedade. Severity: **low**. |
| T-08-09 | Information Disclosure | Descrição do catalog vazada para frontend (não-autorizados veem nomes de permissions) | accept | Out of scope desta fase. `catalog()` é exposto apenas em `/sistema/setores` (admin via short-circuit + `EnsureUserHasRole` middleware), entregue na Phase 12. Phase 8 só registra a chave; não há rota nova. Severity: **low**. |

**Mapeamento ASVS V4 Access Control:** PERM-01 (registro) é o **enabler** do controle de acesso para Phases 11/12. O gate efetivo só é aplicado quando uma rota verifica `hasPermission('notificacoes.criar')` — isso ainda não existe.
</threat_model>

<verification>
**Após Tarefa 1:** 3 edições em `Permissions.php` verificáveis via source assertions. Nenhum teste mudou de status ainda (mas Tarefa 2 vai aproveitar as edições).
**Após Tarefa 2:** Tests 2, 3, 4 GREEN. Suíte da fase mostra 5/7 GREEN + 2/7 SKIPPED (markTestIncomplete) + 0 RED.
</verification>

<success_criteria>
- PERM-01 (registro completo no catálogo com label/descrição pt-BR) coberto por Tests 2 + 3 GREEN.
- PERM-03 (presença em AUTO_LIDERANCA) coberto por Test 4 GREEN (nível de catálogo); verificação end-to-end via `hasPermission` fica para Plan 04 (Test 6).
- Success Criterion #2 do ROADMAP (`Permissions::all()` inclui `notificacoes.criar` no grupo "Notificações") coberto.
- Success Criterion #5 do ROADMAP (`Permissions::AUTO_LIDERANCA` contém `notificacoes.criar`) coberto.
- D-07, D-08, D-09 implementados.
- Zero mudança em `app/Models/User.php` (D-10).
</success_criteria>

<output>
Criar `.planning/phases/08-funda-o-de-notifica-es/08-03-SUMMARY.md` ao final, registrando:
- Arquivo editado: `app/Support/Permissions.php` (+3 edições cirúrgicas)
- Testes verdes na suíte ao final do plano: 5/7 (Tests 1, 2, 3, 4, 7)
- REQ-IDs cobertos: PERM-01 (completo), PERM-03 (registro — verificação E2E em Plan 04)
- Decisões implementadas: D-07, D-08, D-09
- Próxima slice: Plan 04 (Authorization resolution — PERM-02 admin + PERM-03 líder verificados via `hasPermission` end-to-end)
</output>
