---
phase: 08-funda-o-de-notifica-es
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - tests/Feature/Notifications/Phase8FoundationTest.php
autonomous: true
requirements:
  - PERM-02
  - PERM-03

must_haves:
  truths:
    - "Admin (`role = 'admin'`) retorna `true` em `$admin->hasPermission('notificacoes.criar')` sem nenhuma atribuição de setor nem permission manual — short-circuit `if ($this->isAdmin()) return true;` (User.php linha 106) resolve. Esta cobertura é exatamente o que D-10 promete: admin não precisa de mudança alguma."
    - "User não-admin (`role = 'consultor'`) cadastrado em `setor_lideres` (via `setoresLiderados()->attach`) retorna `true` em `$user->hasPermission('notificacoes.criar')` sem permission manual no setor — merge de `AUTO_LIDERANCA` em `effectivePermissions()` (User.php linhas 135–137) resolve (D-09 entregue em Plan 03 + D-10 prova que não exige código novo)."
    - "Em ambos os casos, ZERO mudança em `app/Models/User.php` (D-10) — Plan 04 só prova end-to-end que o registro feito em Plan 03 funciona via os mecanismos pré-existentes."
    - "Suíte completa `Phase8FoundationTest` (7 testes) está 100% GREEN — zero `markTestIncomplete`, zero falha."
  artifacts:
    - path: "tests/Feature/Notifications/Phase8FoundationTest.php"
      provides: "Tests 5 e 6 (`test_admin_tem_permissao_via_short_circuit`, `test_lider_tem_permissao_via_auto_lideranca`) preenchidos e GREEN — fechando a suíte da fase"
      contains: "test_admin_tem_permissao_via_short_circuit"
  key_links:
    - from: "tests/Feature/Notifications/Phase8FoundationTest.php (Test 5)"
      to: "app/Models/User.php linha 106 (short-circuit `if ($this->isAdmin()) return true;`)"
      via: "$admin->hasPermission('notificacoes.criar')"
      pattern: "hasPermission\\('notificacoes\\.criar'\\)"
    - from: "tests/Feature/Notifications/Phase8FoundationTest.php (Test 6)"
      to: "app/Models/User.php linhas 135–137 (merge de Permissions::AUTO_LIDERANCA quando isLider())"
      via: "$user->setoresLiderados()->attach + $user->hasPermission"
      pattern: "setoresLiderados\\(\\)->attach"
---

## Phase Goal

**As a** desenvolvedor implementando o sistema de Notificações v3.0, **I want to** ter prova end-to-end de que admin (via short-circuit) e líder (via AUTO_LIDERANCA merge) ganham `notificacoes.criar` automaticamente sem atribuição manual, **so that** Phases 11/12 podem confiar em `$user->hasPermission('notificacoes.criar')` como gate único de autorização para qualquer fluxo de criação de notificação manual.

<objective>
Slice 4 da fundação (MVP vertical) — entrega a **authorization resolution** verificada end-to-end. Esta slice **não cria código novo de produção** (D-10 trava: `User::hasPermission` e `User::effectivePermissions` já cobrem PERM-02 e PERM-03 pelos mecanismos existentes). O trabalho aqui é puramente de teste: preencher os 2 últimos métodos da suíte `Phase8FoundationTest` (Tests 5 e 6) com setups exatos e asserts ponta-a-ponta, fechando 100% da suíte.

**Purpose:** Sem estes testes, PERM-02 e PERM-03 ficam "comprovados em catálogo" (Plan 03) mas não comprovados "em uso real" (`hasPermission`). Plan 04 fecha essa lacuna — após esta slice, a equipe pode confiar que admin e líder ganham `notificacoes.criar` automaticamente em qualquer ponto do sistema, sem precisar atribuir setor nem permission manual.

**Output:**
- 2 testes preenchidos no arquivo de teste (Tests 5 e 6)
- Suíte `Phase8FoundationTest` 7/7 GREEN
- Phase 8 completa — `must_haves` da fase satisfeitos

**Cobre:** PERM-02 (admin via short-circuit) + PERM-03 (líder via AUTO_LIDERANCA — end-to-end via `hasPermission`). Success Criteria #3 e #4 do ROADMAP §Phase 8.

**Dependência de wave (revision B1):** Este plan está em wave 4 com `depends_on: [08-03]` porque escreve em `tests/Feature/Notifications/Phase8FoundationTest.php` — o mesmo arquivo editado por Plans 02 e 03. Para evitar clobber de arquivo, os 3 plans (02, 03, 04) são serializados sequencialmente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-funda-o-de-notifica-es/08-CONTEXT.md
@.planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md
@.planning/phases/08-funda-o-de-notifica-es/08-PATTERNS.md
@.planning/phases/08-funda-o-de-notifica-es/08-VALIDATION.md
@./CLAUDE.md
@app/Models/User.php
@app/Models/Setor.php
@app/Support/Permissions.php
@tests/Feature/Notifications/Phase8FoundationTest.php
@database/factories/UserFactory.php
@database/migrations/2026_05_20_200005_create_setor_lideres_table.php
@tests/Feature/AdminFechamentoControllerTest.php

<interfaces>
<!-- Mecanismos já implementados em User.php que Plan 04 NÃO toca: -->

User::hasPermission (User.php linhas 104-110, NÃO MODIFICAR):
  public function hasPermission(string $key): bool {
      if ($this->isAdmin()) return true;             // ← cobre PERM-02
      return \in_array($key, $this->effectivePermissions(), true);
  }

User::effectivePermissions (User.php linhas 113-140, NÃO MODIFICAR):
  - Se isAdmin() → retorna Permissions::all() (caminho admin)
  - Senão coleta keys via setor_permissoes (do user nos setores)
  - Se isLider() → array_merge com Permissions::AUTO_LIDERANCA
  - Cache via $effectivePermissionsCache (limpo por $user->refresh())

User::isLider (User.php linhas 88-91, NÃO MODIFICAR):
  return $this->setoresLiderados()->exists();

User::setoresLiderados (User.php linhas 75-79, NÃO MODIFICAR):
  belongsToMany(Setor) via pivot setor_lideres com withPivot('assigned_by', 'assigned_at')->withTimestamps()

Pivot setor_lideres (migration 2026_05_20_200005, colunas relevantes):
  - setor_id (FK setores)
  - user_id (FK users)
  - assigned_by (FK users nullable)
  - assigned_at (timestamp)
  - timestamps padrão

Setor (app/Models/Setor.php) — campos fillable observados:
  - nome (string, required)
  - slug (string — gerado automaticamente em booted() se vazio)
  - active (bool, default true)

<!-- Templates LITERAIS dos Tests 5 e 6 (RESEARCH §Validation Architecture): -->

Test 5 (linhas 619-622):
  $admin = User::factory()->create(['role' => 'admin']);
  $this->assertTrue($admin->hasPermission('notificacoes.criar'));
  // Não atribui setor nem permissão manual — prova short-circuit

Test 6 (linhas 626-633):
  $setor = \App\Models\Setor::create(['nome' => 'Setor Teste', 'active' => true]);
  $user  = User::factory()->create(['role' => 'consultor']);   // explicitamente NÃO admin
  $user->setoresLiderados()->attach($setor->id, ['assigned_by' => null, 'assigned_at' => now()]);
  $user->refresh();   // invalida o cache effectivePermissionsCache
  $this->assertTrue($user->isLider());
  $this->assertTrue($user->hasPermission('notificacoes.criar'));
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Preencher Test 5 — admin via short-circuit (PERM-02)</name>
  <files>tests/Feature/Notifications/Phase8FoundationTest.php</files>
  <read_first>
    - .planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md (§Validation Architecture Test 5 linhas 618–623 — template literal)
    - .planning/phases/08-funda-o-de-notifica-es/08-PATTERNS.md (§5 Test file — Analog B linhas 470–491; padrão de criar admin via UserFactory)
    - .planning/phases/08-funda-o-de-notifica-es/08-CONTEXT.md (D-10 — admin NÃO precisa de mudança em User.php; short-circuit existente já cobre PERM-02)
    - app/Models/User.php linhas 100–110 (hasPermission + short-circuit — não modificar, apenas confirmar comportamento)
    - tests/Feature/AdminFechamentoControllerTest.php linhas 16–19 (padrão `User::factory()->create(['role' => 'admin'])` já usado no projeto)
    - tests/Feature/Notifications/Phase8FoundationTest.php (esqueleto criado em Plan 01 — método existe em `markTestIncomplete`)
  </read_first>
  <behavior>
    - Após esta tarefa, `php artisan test --filter=Phase8FoundationTest::test_admin_tem_permissao_via_short_circuit` sai 0 (GREEN).
    - O teste cria um admin via `User::factory()->create(['role' => 'admin'])`, NÃO atribui setor, NÃO atribui permission manual, e ainda assim `$admin->hasPermission('notificacoes.criar')` retorna `true`.
    - Isto prova que o short-circuit `if ($this->isAdmin()) return true;` (User.php linha 106) cobre a nova permission `notificacoes.criar` automaticamente — D-10 confirmado em runtime.
    - O teste roda em <100ms (sem queries de catálogo nem joins — short-circuit retorna antes).
  </behavior>
  <action>
    Editar `tests/Feature/Notifications/Phase8FoundationTest.php`. Substituir o corpo do método `test_admin_tem_permissao_via_short_circuit` (atualmente em `markTestIncomplete` desde Plan 01) pelo seguinte conteúdo: criar admin via `$admin = User::factory()->create(['role' => 'admin']);`. Fazer 1 assert principal: `$this->assertTrue($admin->hasPermission('notificacoes.criar'));`. Pode opcionalmente adicionar 1 assert defensivo `$this->assertTrue($admin->isAdmin());` para deixar claro que o short-circuit é o caminho ativo. NÃO chamar `$admin->setores()->attach`, NÃO chamar `$admin->setoresLiderados()->attach`, NÃO criar `SetorPermissao` — o teste prova exatamente que admin NÃO precisa de nenhuma atribuição. Comentário inline em pt-BR explicando que o assert prova PERM-02 + Success Criterion #3 via short-circuit (User.php linha 106). NÃO tocar no método `test_lider_tem_permissao_via_auto_lideranca` (Tarefa 2). NÃO tocar no método `test_base_notification_persiste_payload_canonico` (já foi preenchido em Plan 02). NÃO tocar nos 3 métodos `test_permissions_all_*` / `test_catalog_*` / `test_auto_lideranca_*` (já foram preenchidos em Plan 03).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase8FoundationTest::test_admin_tem_permissao_via_short_circuit</automated>
  </verify>
  <acceptance_criteria>
    - Source: método `test_admin_tem_permissao_via_short_circuit` contém literalmente `User::factory()->create(['role' => 'admin'])`.
    - Source: método contém literalmente `$admin->hasPermission('notificacoes.criar')` envolto em `assertTrue`.
    - Source: método NÃO contém mais `markTestIncomplete`.
    - Source: método NÃO contém `setores()->attach` nem `setoresLiderados()->attach` nem criação de `SetorPermissao` (prova que admin não precisa de atribuição).
    - Source: `app/Models/User.php` NÃO foi modificado neste plano (D-10).
    - Behavior: `php artisan test --filter=Phase8FoundationTest::test_admin_tem_permissao_via_short_circuit` sai 0.
  </acceptance_criteria>
  <done>
    Test 5 GREEN. PERM-02 verificado end-to-end via `hasPermission()`. 6/7 testes da suíte GREEN ao final desta tarefa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Preencher Test 6 — líder via AUTO_LIDERANCA merge (PERM-03 end-to-end)</name>
  <files>tests/Feature/Notifications/Phase8FoundationTest.php</files>
  <read_first>
    - .planning/phases/08-funda-o-de-notifica-es/08-RESEARCH.md (§Validation Architecture Test 6 linhas 626–634 — template literal com setup exato)
    - .planning/phases/08-funda-o-de-notifica-es/08-PATTERNS.md (§5 Test file — Padrão de criar Setor + attach como líder linhas 530–549)
    - .planning/phases/08-funda-o-de-notifica-es/08-CONTEXT.md (D-09 — `NOTIFICACOES_CRIAR` adicionado em AUTO_LIDERANCA; merge automático em `effectivePermissions()`)
    - app/Models/User.php linhas 75–80 (relação setoresLiderados com withPivot)
    - app/Models/User.php linhas 88–91 (isLider)
    - app/Models/User.php linhas 113–140 (effectivePermissions com merge AUTO_LIDERANCA — não modificar)
    - app/Models/Setor.php (campos fillable; `booted()` gera slug automático se vazio)
    - database/migrations/2026_05_20_200005_create_setor_lideres_table.php (colunas do pivot: setor_id, user_id, assigned_by nullable, assigned_at timestamp)
    - tests/Feature/Notifications/Phase8FoundationTest.php (esqueleto criado em Plan 01)
  </read_first>
  <behavior>
    - Após esta tarefa, `php artisan test --filter=Phase8FoundationTest::test_lider_tem_permissao_via_auto_lideranca` sai 0 (GREEN).
    - O teste cria um `Setor` ativo, cria um `User` não-admin (`role: 'consultor'`), faz attach via `setoresLiderados()->attach($setor->id, ['assigned_by' => null, 'assigned_at' => now()])`, chama `$user->refresh()` para invalidar o cache `effectivePermissionsCache` (User.php linha 57), assert `$user->isLider() === true` e assert `$user->hasPermission('notificacoes.criar') === true`.
    - O teste NÃO atribui setor membro via `setores()->attach`, NÃO cria entrada em `setor_permissoes` — prova que apenas estar em `setor_lideres` (i.e., `isLider() === true`) basta para herdar `AUTO_LIDERANCA`.
    - Após esta tarefa, a suíte completa `Phase8FoundationTest` mostra 7/7 GREEN — zero `markTestIncomplete`, zero falha. Phase 8 está completa.
  </behavior>
  <action>
    Editar `tests/Feature/Notifications/Phase8FoundationTest.php`. Substituir o corpo do método `test_lider_tem_permissao_via_auto_lideranca` (atualmente em `markTestIncomplete` desde Plan 01) pelo template do RESEARCH §Validation Architecture Test 6. Setup: criar Setor com `\App\Models\Setor::create(['nome' => 'Setor Teste', 'active' => true])` — o `booted()` do model gera `slug` automático (`'setor-teste'`) se vazio (verificável no model). Criar user não-admin com `User::factory()->create(['role' => 'consultor'])` — `role` deve ser `'consultor'` (ou qualquer não-`'admin'`) para forçar o caminho não-short-circuit. Attach via `$user->setoresLiderados()->attach($setor->id, ['assigned_by' => null, 'assigned_at' => now()])` — passar EXATAMENTE essas 2 chaves de pivot porque a migration declara `assigned_at` timestamp NOT NULL e `assigned_by` nullable. Chamar `$user->refresh();` IMEDIATAMENTE após o attach — sem isso, `effectivePermissionsCache` mantém o resultado anterior (User.php linha 57) e o assert falha. Asserts: `$this->assertTrue($user->isLider());` (1) + `$this->assertTrue($user->hasPermission('notificacoes.criar'));` (2). Pode adicionar 1 assert defensivo `$this->assertFalse($user->isAdmin());` para deixar claro que não está no caminho short-circuit. Comentário inline em pt-BR explicando que o teste prova PERM-03 + Success Criterion #4 via merge automático de `AUTO_LIDERANCA` em `effectivePermissions()`. NÃO atribuir setor membro nem permission manual. NÃO modificar `app/Models/User.php` (D-10). NÃO tocar nos outros métodos da suíte (já preenchidos em Plans 01/02/03 + Tarefa 1 deste plano).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase8FoundationTest</automated>
  </verify>
  <acceptance_criteria>
    - Source: método `test_lider_tem_permissao_via_auto_lideranca` contém literalmente `Setor::create(` (ou `\App\Models\Setor::create(` — equivalente com import).
    - Source: método contém literalmente `'role' => 'consultor'` (ou outro valor não-`'admin'`) ao criar o user.
    - Source: método contém literalmente `setoresLiderados()->attach` com argumentos `$setor->id` e array com chaves `assigned_by` e `assigned_at`.
    - Source: método contém literalmente `$user->refresh();` IMEDIATAMENTE ou logo após o `attach()`.
    - Source: método contém literalmente `assertTrue($user->isLider())`.
    - Source: método contém literalmente `$user->hasPermission('notificacoes.criar')` dentro de `assertTrue`.
    - Source: método NÃO contém `'role' => 'admin'` (não criar admin neste teste — isto é o Test 5).
    - Source: método NÃO contém `setores()->attach` (sem `Liderados` no nome) nem criação de `SetorPermissao` (prova que líder não precisa de membro+permission).
    - Source: método NÃO contém mais `markTestIncomplete`.
    - Source: `app/Models/User.php` NÃO foi modificado neste plano (D-10).
    - Behavior: `php artisan test --filter=Phase8FoundationTest::test_lider_tem_permissao_via_auto_lideranca` sai 0.
    - Behavior: `php artisan test --filter=Phase8FoundationTest` mostra 7/7 GREEN (todos os 7 testes da fase: 1, 2, 3, 4, 5, 6, 7). Zero RED, zero SKIPPED.
    - CLI: `php artisan test` (suíte completa Unit + Feature) sai 0 — nenhum teste pré-existente foi quebrado pela Phase 8.
  </acceptance_criteria>
  <done>
    Test 6 GREEN. PERM-03 verificado end-to-end via `hasPermission()`. Suíte `Phase8FoundationTest` está 7/7 GREEN. Phase 8 — Fundação de Notificações — está completa. Próxima fase do milestone v3.0 é Phase 9 (Backend de Leitura, Contador e Polling).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User identity claim ↔ Authorization decision | `User::hasPermission` é o gate único. Plan 04 verifica end-to-end que admin (short-circuit) e líder (AUTO_LIDERANCA) ganham `notificacoes.criar` sem caminho lateral. |
| Pivot `setor_lideres` write ↔ `isLider()` query | Test 6 escreve em `setor_lideres` via factory de relação; `isLider()` lê via `setoresLiderados()->exists()`. Quebra de consistência aqui seria detectada como assertion failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Elevation of Privilege | User não-admin sem `setoresLiderados()` ganharia `notificacoes.criar` indevidamente | mitigate | `hasPermission` (User.php linhas 104–110) usa `effectivePermissions()` que SÓ merge `AUTO_LIDERANCA` quando `isLider() === true`. RESEARCH §Existing Patterns to Reuse + Test 6 verifica o caso positivo. Caso negativo (consultor SEM setoresLiderados → deve retornar `false`) é coberto implicitamente pela lógica testada — se o caso negativo fosse `true`, Test 6 não distinguiria entre os caminhos. Severity: **medium** — mitigação por código existente verificado, sem rota nova. |
| T-08-11 | Spoofing | User com `role` arbitrário escalando para admin | accept (out of scope) | `role` é editado apenas via UI admin de usuários (`/sistema/setores` em Phase 12). Sem self-service para mudar role; mudança requer outro admin. Phase 8 não introduz novo vetor. Severity: **low**. |
| T-08-12 | Tampering | Cache stale `effectivePermissionsCache` retornando resultado antigo após attach de setor | mitigate | `$user->refresh()` chamado IMEDIATAMENTE após `setoresLiderados()->attach()` (Test 6 — verificado em acceptance criteria). User.php linha 57 declara `$effectivePermissionsCache = null;` por instância — `refresh()` recarrega a instância e zera o cache. RESEARCH §Validation Architecture Test 6 destaca isso explicitamente. Severity: **medium** (causaria falso-negativo do teste se omitido); mitigação explícita no source assertion. |

**Mapeamento ASVS V4 Access Control:** PERM-02 (admin) e PERM-03 (líder) são o **gate final** que será usado pelas Phases 11/12 para autorizar criação manual de notificações. Plan 04 entrega a evidência end-to-end de que `hasPermission('notificacoes.criar')` é confiável.
</threat_model>

<verification>
**Após Tarefa 1:** Test 5 GREEN; 6/7 testes da suíte verdes.
**Após Tarefa 2:** Test 6 GREEN; 7/7 testes da suíte verdes; Phase 8 completa.

**Gate da fase:**
- `php artisan test --filter=Phase8FoundationTest` sai 0 com 7 testes passados.
- `php artisan test --testsuite=Feature` sai 0 (nenhum teste pré-existente quebrado).
- `php artisan test` (Unit + Feature) sai 0.

**Manual (dev host XAMPP, fora do agente):**
- `php artisan migrate` cria tabela `notifications` em MySQL local sem erro.
- Tinker: `App\Models\User::find(<id_admin>)->hasPermission('notificacoes.criar')` retorna `true`.
- Tinker: `App\Models\User::find(<id_lider>)->hasPermission('notificacoes.criar')` retorna `true` (user que está em `setor_lideres`).
- Tinker: `App\Models\User::find(<id_consultor_sem_lideranca>)->hasPermission('notificacoes.criar')` retorna `false`.
</verification>

<success_criteria>
- 7/7 testes da suíte `Phase8FoundationTest` GREEN.
- PERM-02 verificado end-to-end (admin via short-circuit, sem atribuição).
- PERM-03 verificado end-to-end (líder via AUTO_LIDERANCA merge, sem permission manual).
- Success Criterion #3 do ROADMAP (admin `true` automaticamente) coberto.
- Success Criterion #4 do ROADMAP (líder `true` automaticamente) coberto.
- D-10 honrado — zero linhas modificadas em `app/Models/User.php`.
- Phase 8 — Fundação de Notificações — está completa e pronta para `/gsd:verify-work`. Phases 9–12 podem agora confiar em `$user->hasPermission('notificacoes.criar')` como gate único.
</success_criteria>

<output>
Criar `.planning/phases/08-funda-o-de-notifica-es/08-04-SUMMARY.md` ao final, registrando:
- Arquivo editado: `tests/Feature/Notifications/Phase8FoundationTest.php` (+2 testes preenchidos)
- Testes verdes na suíte: 7/7 (suíte completa)
- REQ-IDs cobertos: PERM-02 (end-to-end), PERM-03 (end-to-end)
- Decisões implementadas: D-10 (confirmação)
- Status da Phase 8: COMPLETA — pronta para `/gsd:verify-work` e para iniciar planejamento da Phase 9
- Confirmação de pt-BR em comentários e mensagens de teste
</output>
