---
phase: 62-metas-apresenta-o-clara-edi-o-r-pida
plan: 05
type: execute
wave: 3
depends_on:
  - 62-02
files_modified:
  - app/Http/Controllers/CompanyController.php
  - resources/js/Pages/Companies/Show.jsx
  - tests/Feature/Phase62/CompanyShowGoalsPayloadTest.php
autonomous: true
requirements:
  - META-01
must_haves:
  truths:
    - "Payload de CompanyController::show inclui `goals[].results` com até 12 GoalResult (period ASC) por meta"
    - "Companies/Show.jsx renderiza <GoalProgressPanel /> para cada meta ativa da empresa dentro da seção 'Metas Ativas'"
    - "Cada meta exibe chart + percentual + valor absoluto na mesma tela (SC #1 do phase)"
    - "Empresas sem metas seguem mostrando o empty state existente (regressão zero)"
    - "Seção 'Metas Ativas' preserva o botão 'Nova meta' com auth `can_create_goals`"
  artifacts:
    - path: "app/Http/Controllers/CompanyController.php"
      provides: "Método show retorna goals com results[] embutido"
      contains: "'results'"
    - path: "resources/js/Pages/Companies/Show.jsx"
      provides: "Seção Metas Ativas com <GoalProgressPanel /> por meta"
      contains: "GoalProgressPanel"
    - path: "tests/Feature/Phase62/CompanyShowGoalsPayloadTest.php"
      provides: "Testes AssertableInertia validando shape do payload goals com results"
  key_links:
    - from: "Companies/Show.jsx"
      to: "resources/js/Components/goals/GoalProgressPanel.jsx"
      via: "import default"
      pattern: "GoalProgressPanel"
    - from: "CompanyController::show"
      to: "goals.results"
      via: "->load(['goals' => fn... => $q->with('results')])"
      pattern: "results"
---

<objective>
Elevar a seção "Metas Ativas" da carteira individual (`/companies/{id}`) de uma lista pobre (`metric_label + target_value` bruto) para apresentação clara com chart + percentual + valor absoluto (META-01) usando o componente `<GoalProgressPanel />` entregue no Plan 62-02.

Purpose: Success Criteria #1 do phase — "Usuário abre uma empresa e visualiza meta atribuída + progresso mensal em apresentação única contendo chart + percentual + valor absoluto". Hoje essa tela é onde o consultor/estrategista naturalmente aterrissa quando quer analisar UMA empresa, então é o lugar principal para METAS visíveis.

Output: `CompanyController::show` enriquecido com results por meta; JSX de Companies/Show.jsx renderiza `<GoalProgressPanel />` em vez de linha simples; testes Feature validam shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-02-PLAN.md

@app/Http/Controllers/CompanyController.php
@resources/js/Pages/Companies/Show.jsx
@app/Models/Goal.php
@app/Models/GoalResult.php

<interfaces>
<!-- Contratos definidos. NÃO explorar codebase. -->

## Payload atual de CompanyController::show (linhas 447-450)

```
'goals' => $company->goals->map(fn($g) => [
    'id' => $g->id, 'metric' => $g->metric, 'metric_label' => $g->metric_label,
    'target_value' => $g->target_value, 'active' => $g->active,
])->values(),
```

**Objetivo:** adicionar `value_type`, `period_type`, `results[]` em cada goal, mantendo o resto:

```
'goals' => $company->goals->map(fn($g) => [
    'id'           => $g->id,
    'metric'       => $g->metric,
    'metric_label' => $g->metric_label,
    'target_value' => $g->target_value,
    'value_type'   => $g->value_type,
    'period_type'  => $g->period_type,
    'active'       => $g->active,
    'results'      => $g->results
        ->sortBy('period')
        ->values()
        ->map(fn($r) => [
            'id'             => $r->id,
            'period'         => $r->period,
            'realized_value' => (float) $r->realized_value,
            'target_value'   => (float) $r->target_value,
            'achieved'       => (bool) $r->achieved,
        ]),
])->values(),
```

Eager load (linha 279):
```
'goals' => fn($q) => $q->where('active', true)->with(['results' => fn($rq) => $rq->orderBy('period', 'desc')->limit(12)]),
```

## Seção UI atual (linhas 817-838)

Section "Metas Ativas" com:
- Botão "Nova meta" (`canCreateGoals`)
- Lista de goals: apenas `{g.metric_label ?? g.metric}` + `{g.target_value}` bruto
- Empty state: "Nenhuma meta cadastrada"

## Componente <GoalProgressPanel /> (do Plan 62-02) — assinatura

```
<GoalProgressPanel
  goal={{ id, metric, metric_label, target_value, value_type, period_type }}
  results={[{ id, period, realized_value, target_value, achieved }]}
  compact={true}    // usar modo compact aqui pra não ocupar espaço demais
/>
```

## Ziggy route + Inertia AssertableInertia
Testes usam `->assertInertia(fn (\Inertia\Testing\AssertableInertia $page) => $page->component('Companies/Show')->has('company.goals')->has('company.goals.0.results'))`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enriquecer CompanyController::show payload com goals.results + testes de shape</name>
  <files>
    - app/Http/Controllers/CompanyController.php
    - tests/Feature/Phase62/CompanyShowGoalsPayloadTest.php
  </files>
  <read_first>
    - app/Http/Controllers/CompanyController.php (linhas 271-527 — método show completo, focar em linha 279 eager load e 447-450 map)
    - app/Models/Goal.php linhas 97-102 (relação results)
    - app/Models/GoalResult.php (fields exatos)
    - .planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-02-PLAN.md § prop signature
  </read_first>
  <behavior>
    - Test 1 (shape base): admin visita `/companies/{id}` de company com 2 goals ativas + N GoalResults → payload `company.goals` array com 2 elementos, cada um com chaves `id, metric, metric_label, target_value, value_type, period_type, active, results`
    - Test 2 (results ordenados ASC por period): 3 GoalResults com periods '2026-05', '2026-06', '2026-07' → `results[0].period === '2026-05' && results[2].period === '2026-07'`
    - Test 3 (limit 12): goal com 15 GoalResults → `results.length === 12`
    - Test 4 (goal sem results): goal sem nenhum GoalResult → `results === []` (array vazio, não null)
    - Test 5 (só goals ativas): goal com `active=false` NÃO aparece no payload (comportamento preservado da eager load)
    - Test 6 (types corretos): `realized_value` e `target_value` são números (float), não strings; `achieved` é boolean
  </behavior>
  <action>
    1. Em `CompanyController::show`, alterar a eager load da linha 279 de `'goals' => fn($q) => $q->where('active', true),` para:
       ```
       // Phase 62 (META-01): results últimos 12 períodos ASC alimenta chart do <GoalProgressPanel>.
       'goals' => fn($q) => $q->where('active', true)
           ->with(['results' => fn($rq) => $rq->orderBy('period', 'desc')->limit(12)]),
       ```
    2. Alterar o mapper (linhas 447-450) para incluir `value_type, period_type, results`:
       ```php
       'goals' => $company->goals->map(fn($g) => [
           'id'           => $g->id,
           'metric'       => $g->metric,
           'metric_label' => $g->metric_label,
           'target_value' => $g->target_value,
           'value_type'   => $g->value_type,
           'period_type'  => $g->period_type,
           'active'       => $g->active,
           'results'      => $g->results
               ->sortBy('period')
               ->values()
               ->map(fn($r) => [
                   'id'             => $r->id,
                   'period'         => $r->period,
                   'realized_value' => (float) $r->realized_value,
                   'target_value'   => (float) $r->target_value,
                   'achieved'       => (bool) $r->achieved,
               ]),
       ])->values(),
       ```
    3. Comentário pt-BR no bloco: `// Phase 62 (META-01): results ordenados ASC pra chart; results[] pode ser vazio.`
    4. NÃO tocar em outras chaves do payload (revenue_30d, ecf_drive, contratos_servico, etc.).
    5. **Testes**: criar `tests/Feature/Phase62/CompanyShowGoalsPayloadTest.php` com `use RefreshDatabase;`. Helpers: `createAdminUser()`, `createCompanyWithGoals(int $goalCount, int $resultsPerGoal)`. Cobrir os 6 tests em `<behavior>` usando `AssertableInertia` (`use Inertia\\Testing\\AssertableInertia as Assert;`). Estrutura:
       ```php
       $this->actingAs($admin)
            ->get(route('companies.show', $company->id))
            ->assertOk()
            ->assertInertia(fn (Assert $page) => $page
                ->component('Companies/Show')
                ->has('company.goals', 2)
                ->has('company.goals.0', fn (Assert $goal) => $goal
                    ->hasAll(['id', 'metric', 'metric_label', 'target_value', 'value_type', 'period_type', 'active', 'results'])
                    ->has('results', 3)
                    ->where('results.0.period', '2026-05')
                    ->where('results.2.period', '2026-07')
                    ->etc()
                )
                ->etc()
            );
       ```
    6. GoalResult factory: se `database/factories/GoalResultFactory.php` NÃO existe, criar em linha usando `GoalResult::create([...])` explícito. Se existe, usar `GoalResult::factory()`.
    7. Company + Goal factories já existem no projeto — usar `Company::factory()`, `Goal::factory()`.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase62/CompanyShowGoalsPayloadTest.php</automated>
  </verify>
  <acceptance_criteria>
    - 6/6 tests verdes
    - `grep -c "'results'" app/Http/Controllers/CompanyController.php` >= 2 (uma na eager load, uma no mapper)
    - `grep -c "sortBy('period')" app/Http/Controllers/CompanyController.php` == 1
    - `grep -c "Phase 62" app/Http/Controllers/CompanyController.php` >= 1 (comentário breadcrumb)
    - Regressão zero: `php artisan test --filter=CompanyController` (se existir suite) continua verde; `php artisan test --filter=Phase18` continua verde
    - Payload preserva `revenue_30d, ecf_drive, contratos_servico, adman_metrics, ml_metrics` (sem tocar)
    - Tests não fake nenhum dado que não seja fixture SQLite
  </acceptance_criteria>
  <done>
    Payload de CompanyController::show enriquecido com goals.results (últimos 12 ASC). 6 testes de shape verdes. Regressão zero nas suites adjacentes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Substituir Section "Metas Ativas" por grid de <GoalProgressPanel /> em Companies/Show.jsx</name>
  <files>
    - resources/js/Pages/Companies/Show.jsx
  </files>
  <read_first>
    - resources/js/Pages/Companies/Show.jsx (linhas 800-840 — Section Metas Ativas + Section Dados da Empresa que fica no mesmo grid)
    - resources/js/Components/goals/GoalProgressPanel.jsx (deve existir após Plan 62-02; confirmar assinatura)
  </read_first>
  <action>
    1. Adicionar import no topo:
       ```jsx
       import GoalProgressPanel from '@/Components/goals/GoalProgressPanel';
       ```
    2. Localizar o grid `<div className="grid grid-cols-1 md:grid-cols-2 gap-5">` (linha 799) que contém `<Section icon={Building2} title="Dados da Empresa">` + `<Section icon={Target} title="Metas Ativas">`.
    3. QUEBRAR esse grid: manter `<Section Dados da Empresa>` sozinho em `<div className="grid grid-cols-1 md:grid-cols-2 gap-5">` com placeholder direito (ou apenas full width). Movimentar `<Section Metas Ativas>` para FORA desse grid, em bloco full-width abaixo, para dar espaço aos charts.
    4. Reescrever a `<Section icon={Target} title="Metas Ativas">` (linhas 818-838). Novo layout:
       ```jsx
       <Section icon={Target} title="Metas Ativas">
         {canCreateGoals && (
           <div className="flex justify-end mb-3">
             <Button size="sm" onClick={abrirNovaMeta}>
               <Plus className="h-3.5 w-3.5 mr-1" /> Nova meta
             </Button>
           </div>
         )}
         {(company.goals || []).filter(g => g.active).length === 0 ? (
           <p className="text-white/25 text-sm text-center py-8" data-testid="company-goals-empty">
             Nenhuma meta cadastrada
           </p>
         ) : (
           <div
             data-testid="company-goals-grid"
             className="grid grid-cols-1 lg:grid-cols-2 gap-4"
           >
             {company.goals.filter(g => g.active).map(g => (
               <GoalProgressPanel
                 key={g.id}
                 goal={g}
                 results={g.results || []}
                 compact
               />
             ))}
           </div>
         )}
       </Section>
       ```
    5. PRESERVAR intocado: Section "Dados da Empresa" (linhas 801-815), Dialog Nova Meta (linhas ~1155), handler `abrirNovaMeta`, todo o resto do arquivo (contratos, ECF Drive, PPAs, NPS, etc.).
    6. Comentário pt-BR na Section: `// Phase 62 (META-01): metas exibidas via <GoalProgressPanel> — chart + % + valor absoluto por meta.`
    7. Se algum teste E2E existente (Phase18, Phase25, etc.) checar existência de `<Section icon={Target}` com children específicos, ajustar via helper `Rule 1` documentando em comentário do teste.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|Error" || echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completa sem erros
    - `grep -c "GoalProgressPanel" resources/js/Pages/Companies/Show.jsx` >= 2 (import + JSX)
    - `grep -c "data-testid=\"company-goals-grid\"" resources/js/Pages/Companies/Show.jsx` == 1
    - `grep -c "data-testid=\"company-goals-empty\"" resources/js/Pages/Companies/Show.jsx` == 1
    - Preserva `abrirNovaMeta`, `canCreateGoals`, Dialog Nova Meta (Task 1 do plan 62-04 já preservou pra Goals/Index; aqui preservar pra Companies/Show)
    - Preserva `<Section icon={Building2} title="Dados da Empresa">` intocada
    - Preserva Section "Serviços contratados" abaixo (linhas ~842)
    - Zero mudança em CompanyController.php EXCETO o que foi alterado na Task 1
  </acceptance_criteria>
  <done>
    Companies/Show.jsx renderiza `<GoalProgressPanel />` por meta ativa no modo compact. Empty state mantido. Botão Nova meta preservado. Layout responsivo (1 col mobile, 2 col desktop). Build Vite verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → GET /companies/{id} | Auth já validado em `userCanViewCompany` — sem mudança. Payload novo (results) segue a mesma regra de autorização por empresa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-05-01 | Information Disclosure | Results de outra empresa vazam via eager load errado | mitigate | Eager load parte de `$company->goals` — Eloquent constrainge pelo company_id no relationship; `->where('active', true)` restringe ainda mais; Test 5 do Task 1 valida isso |
| T-62-05-02 | Denial of Service | Empresa com muitas metas + muitos results consome memória | mitigate | `->limit(12)` no results eager load; `->where('active', true)` restringe goals; goals raramente > 10 por empresa (histórico do projeto) |
| T-62-05-03 | Tampering | Payload result values chegando como string decimal quebra chart JS | mitigate | Cast explícito `(float)` no map — Test 6 do Task 1 valida |
| T-62-05-SC | Tampering | npm installs | accept | Zero pacotes novos — <GoalProgressPanel /> já entregue no Plan 62-02 |
</threat_model>

<verification>
- `php artisan test tests/Feature/Phase62/CompanyShowGoalsPayloadTest.php` → 6/6 verdes
- `npm run build` verde
- grep gates passam
- Manual UAT (checkpoint next phase): abrir `/companies/{id}` de empresa com metas → ver charts renderizados
</verification>

<success_criteria>
- Payload enriquecido, testes de shape verdes
- Companies/Show.jsx renderiza chart + % + valor absoluto por meta (SC #1 do phase)
- Zero regressão em outras seções da página
</success_criteria>

<output>
Criar `.planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-05-SUMMARY.md` com: (a) diff resumido do payload de show (chaves adicionadas), (b) diff resumido do JSX (Section antes/depois), (c) testes verdes, (d) build status, (e) confirmação de zero regressão.
</output>
