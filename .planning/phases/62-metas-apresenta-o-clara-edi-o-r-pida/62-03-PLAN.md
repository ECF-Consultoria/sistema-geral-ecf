---
phase: 62-metas-apresenta-o-clara-edi-o-r-pida
plan: 03
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - resources/js/Components/goals/GoalHistoryDrawer.jsx
autonomous: true
requirements:
  - META-04
must_haves:
  truths:
    - "Componente <GoalHistoryDrawer /> abre em <Dialog /> (Radix) ao clicar em ícone Clock ao lado da meta"
    - "Ao abrir, faz fetch em GET /goals/{goalId}/history via axios (endpoint criado no Plan 62-01) — não pre-fetch"
    - "Renderiza lista das últimas 10 alterações com autor, data (formato pt-BR), descrição pt-BR e diff (valor anterior → valor novo)"
    - "Inclui link 'Ver log completo' para /activity-log?subject_type=App\\Models\\Goal&subject_id={id} (rota existente)"
    - "Componente é 100% controlado por props (open, onOpenChange, goalId) — sem estado global"
  artifacts:
    - path: "resources/js/Components/goals/GoalHistoryDrawer.jsx"
      provides: "Componente <GoalHistoryDrawer open onOpenChange goalId />"
      exports: ["default GoalHistoryDrawer"]
      min_lines: 90
  key_links:
    - from: "GoalHistoryDrawer"
      to: "GET /goals/{goalId}/history"
      via: "axios.get via useEffect quando open=true"
      pattern: "axios\\.get.*goals.*history"
    - from: "GoalHistoryDrawer"
      to: "route('activity-log.index')"
      via: "<Link href={route('activity-log.index', { subject_type: ..., subject_id: ... })}>"
      pattern: "activity-log\\.index"
---

<objective>
Entregar componente React `<GoalHistoryDrawer />` que exibe o histórico de alterações de UMA meta específica em um Dialog (Radix), consumindo o endpoint `/goals/{id}/history` criado no Plan 62-01.

Purpose: META-04 exige "histórico visível na tela de gestão (últimas N alterações + link pra log completo)". Componente encapsula o fetch + renderização + navegação para tela full de activity-log, é reusável entre Goals/Index.jsx e futuras telas.

Output: Componente self-contained com estado local para loading/error/data. Dependência de rede: `axios.get(route('goals.history', goalId))` — endpoint fornecido pelo Plan 62-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-01-PLAN.md

@resources/js/Components/ui/dialog.jsx
@resources/js/Pages/Goals/Index.jsx
@resources/js/bootstrap.js

<interfaces>
<!-- Contratos definidos. NÃO explorar codebase. -->

## Prop signature

```
GoalHistoryDrawer({
  open: boolean,                          // controlado pelo parent (Goals/Index)
  onOpenChange: (open: boolean) => void,  // padrão Radix Dialog
  goalId: number | null,                  // null quando fechado — não faz fetch
  metricLabel?: string,                   // 'Faturamento' — mostrado no header pra contexto
})
```

## Contrato de resposta do endpoint (definido no Plan 62-01)

```
GET /goals/{goalId}/history → 200
{
  "entries": [
    {
      "id": 123,
      "description": "Meta atualizada",       // pt-BR from Goal::getActivitylogOptions
      "causer_name": "Fulano de Tal" | null,
      "created_at": "2026-07-07T10:15:30.000000Z",
      "changes": {
        "old":        { "target_value": "10000.00" },
        "attributes": { "target_value": "12000.00" }
      }
    }
  ]
}
```

Empty response: `{ "entries": [] }` — status 200.

## axios já bootstrap-ado (resources/js/bootstrap.js)
```
window.axios = axios
axios.defaults.headers.common['X-Requested-With'] = 'XMLHttpRequest';
```
Importar direto: `import axios from 'axios';`

## Radix Dialog padrão (@/Components/ui/dialog)
```
<Dialog open={open} onOpenChange={onOpenChange}>
  <DialogContent className="max-w-lg max-h-[80vh] overflow-y-auto">
    <DialogHeader><DialogTitle>...</DialogTitle></DialogHeader>
    ...
  </DialogContent>
</Dialog>
```

## Ziggy route helper
`route('goals.history', goalId)` retorna URL absoluta.
`route('activity-log.index', { subject_type: 'App\\Models\\Goal', subject_id: goalId })` retorna URL com query string.

## Formatação de datas
Usar `date-fns` (já em package.json): `format(parseISO(created_at), 'dd/MM/yyyy HH:mm')` para exibir "07/07/2026 10:15".

## Labels pt-BR para chaves de mudança
```
const FIELD_LABELS = {
  target_value: 'Valor alvo',
  metric: 'Métrica',
  active: 'Ativa',
  company_id: 'Empresa',
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar <GoalHistoryDrawer /> com fetch on-open + render de entries</name>
  <files>
    - resources/js/Components/goals/GoalHistoryDrawer.jsx
  </files>
  <read_first>
    - resources/js/Components/ui/dialog.jsx (assinatura Dialog/DialogContent/DialogHeader/DialogTitle)
    - resources/js/Pages/Goals/Index.jsx (linhas 1-15 imports — pegar padrão de import Radix Dialog + Ziggy `route()` global)
    - resources/js/bootstrap.js (confirmar que axios está global — pode ser importado direto)
    - .planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-01-PLAN.md (§ contrato do endpoint)
    - app/Http/Controllers/ActivityLogController.php linha 21 (confirmar filtro por `subject_type` e `subject_id` query params)
  </read_first>
  <behavior>
    Componente controlado pelo parent:

    - Test 1 (não fetch quando fechado): open=false → nenhum axios call, `[data-testid="goal-history-drawer"]` NÃO no DOM
    - Test 2 (fetch quando abre): open transita false→true e goalId=42 → axios.get chamado com URL de `route('goals.history', 42)`
    - Test 3 (loading state): durante fetch pendente → `[data-testid="goal-history-loading"]` visível
    - Test 4 (render entries): resposta com 3 entries → 3 elementos `[data-testid="goal-history-entry"]` no DOM, cada um com autor + data formatada dd/MM/yyyy HH:mm + descrição + diff old→new
    - Test 5 (empty state): resposta `{"entries":[]}` → `[data-testid="goal-history-empty"]` com texto "Nenhuma alteração registrada"
    - Test 6 (error state): axios call falha (500) → `[data-testid="goal-history-error"]` com CTA de retry
    - Test 7 (link log completo): sempre presente `[data-testid="goal-history-full-link"]` com href para `/activity-log?subject_type=App\\Models\\Goal&subject_id={goalId}`
    - Test 8 (re-fetch quando goalId muda): open=true + goalId muda de 42 → 43 → novo axios call com URL de 43
  </behavior>
  <action>
    1. Criar `resources/js/Components/goals/GoalHistoryDrawer.jsx`. Exportar `default function GoalHistoryDrawer({ open, onOpenChange, goalId, metricLabel })`.
    2. Estado local: `[entries, setEntries] = useState([])`, `[loading, setLoading] = useState(false)`, `[error, setError] = useState(null)`.
    3. `useEffect` dependente de `[open, goalId]`: quando `open === true && goalId != null`, chamar `axios.get(route('goals.history', goalId))` — em `.then` setar entries; em `.catch` setar error; sempre setar loading false no finally. Guardar cancelamento com `AbortController` ou flag `alive` local pra evitar setState em unmount.
    4. Wrap tudo em `<Dialog open={open} onOpenChange={onOpenChange}>` com `<DialogContent className="max-w-lg max-h-[80vh] overflow-y-auto card-ecf">` — `data-testid="goal-history-drawer"`.
    5. Header: `<DialogHeader><DialogTitle>Histórico de alterações — {metricLabel ?? 'Meta'}</DialogTitle></DialogHeader>`.
    6. Bloco de estado:
       - loading → `<div data-testid="goal-history-loading">Carregando histórico...</div>` (ícone spinner opcional via `Loader2` de lucide-react).
       - error → `<div data-testid="goal-history-error">` com mensagem "Não foi possível carregar o histórico" + `<Button onClick={retryFn}>Tentar novamente</Button>` que reseta error e re-dispara o fetch.
       - entries.length === 0 (após loading) → `<div data-testid="goal-history-empty">Nenhuma alteração registrada</div>`.
       - Senão: `entries.map(entry => <div data-testid="goal-history-entry" key={entry.id}>...</div>)`.
    7. Renderização de cada entry:
       - Linha 1: `{entry.causer_name ?? 'Sistema'}` + `<span className="text-white/40">·</span>` + `{format(parseISO(entry.created_at), 'dd/MM/yyyy HH:mm')}`.
       - Linha 2: `{entry.description}` (label pt-BR: 'Meta criada' / 'Meta atualizada' / 'Meta excluída').
       - Linha 3: diff formatado para cada key em `entry.changes.attributes` — `{FIELD_LABELS[key] ?? key}: {oldValue} → {newValue}` onde `oldValue = entry.changes.old?.[key] ?? '—'` e `newValue = entry.changes.attributes?.[key]`.
       - `active` deve ser mapeado para "Sim/Não" antes de renderizar.
    8. Footer com link full log:
       ```
       <a data-testid="goal-history-full-link"
          href={route('activity-log.index', { subject_type: 'App\\Models\\Goal', subject_id: goalId })}
          className="text-ecf-yellow text-[12px] font-semibold hover:underline">
         Ver log completo →
       </a>
       ```
    9. Imports necessários: `useState, useEffect` (react), `axios`, `format, parseISO` (date-fns), `Loader2, Clock, AlertTriangle` (lucide-react), Dialog primitives (@/Components/ui/dialog), `Button` (@/Components/ui/button).
    10. Comentários pt-BR nas seções não-óbvias (useEffect cleanup, mapeamento de FIELD_LABELS).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|Error" || echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completa sem erros
    - `grep -c "data-testid=\"goal-history-drawer\"" resources/js/Components/goals/GoalHistoryDrawer.jsx` == 1
    - `grep -c "data-testid=\"goal-history-loading\"" resources/js/Components/goals/GoalHistoryDrawer.jsx` == 1
    - `grep -c "data-testid=\"goal-history-error\"" resources/js/Components/goals/GoalHistoryDrawer.jsx` == 1
    - `grep -c "data-testid=\"goal-history-empty\"" resources/js/Components/goals/GoalHistoryDrawer.jsx` == 1
    - `grep -c "data-testid=\"goal-history-entry\"" resources/js/Components/goals/GoalHistoryDrawer.jsx` == 1
    - `grep -c "data-testid=\"goal-history-full-link\"" resources/js/Components/goals/GoalHistoryDrawer.jsx` == 1
    - `grep -c "route('goals.history'" resources/js/Components/goals/GoalHistoryDrawer.jsx` == 1
    - `grep -c "route('activity-log.index'" resources/js/Components/goals/GoalHistoryDrawer.jsx` == 1
    - `grep -c "from 'axios'" resources/js/Components/goals/GoalHistoryDrawer.jsx` == 1
    - Arquivo tem >= 90 linhas úteis
    - Cleanup do useEffect implementado (guard contra setState pós-unmount)
  </acceptance_criteria>
  <done>
    Componente `<GoalHistoryDrawer />` implementado. Fetch on-open, cleanup no unmount, estados loading/error/empty/data cobertos, link para log completo presente. Build Vite passa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /goals/{id}/history | Já protegido no Plan 62-01 (auth admin OR user vinculado à empresa). Componente só orquestra. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03-01 | Information Disclosure | Renderizar `causer_name` sem escape pode injetar HTML | mitigate | React escapa por padrão em `{expressao}` no JSX — não usar `dangerouslySetInnerHTML` em nenhum lugar |
| T-62-03-02 | Denial of Service | Fetch repetido em loop se prop `goalId` fica trocando | mitigate | useEffect com deps `[open, goalId]` só refaz quando UMA das duas muda; AbortController cancela request pendente ao trocar |
| T-62-03-03 | Repudiation | Endpoint retorna 403 e user não sabe motivo | mitigate | Estado `error` mostra mensagem genérica pt-BR + retry; sem detalhes técnicos que ajudem enumeration |
| T-62-03-SC | Tampering | npm installs | accept | Zero pacotes novos — axios/date-fns/lucide-react/Radix já em package.json |
</threat_model>

<verification>
- `npm run build` verde
- grep gates passam
- Inspeção manual: JSX contém Dialog + useEffect com deps `[open, goalId]` + AbortController ou flag alive
</verification>

<success_criteria>
- Componente drawer entregue com 6 data-testids
- Fetch on-open (não pre-fetch)
- Cleanup no unmount
- Link para log completo apontando para rota Ziggy correta
</success_criteria>

<output>
Criar `.planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-03-SUMMARY.md` com: (a) prop signature, (b) estrutura de estados, (c) resultado dos grep gates, (d) build status.
</output>
