---
phase: 62-metas-apresenta-o-clara-edi-o-r-pida
plan: 04
type: execute
wave: 3
depends_on:
  - 62-01
  - 62-03
files_modified:
  - resources/js/Pages/Goals/Index.jsx
  - app/Http/Controllers/GoalController.php
autonomous: true
requirements:
  - META-04
must_haves:
  truths:
    - "Na aba 'Por Empresa' do /goals, click no valor da meta transforma o texto em input controlled sem sair da página"
    - "Ao pressionar Enter ou perder foco, o payload `target_value` é enviado via PUT /goals/{goal} com preserveScroll (padrão Inertia)"
    - "Alteração otimista: valor exibido atualiza imediatamente; em erro, valor volta ao original e mensagem de erro aparece"
    - "Ícone Clock (lucide-react) ao lado de cada meta abre <GoalHistoryDrawer /> com o goalId correspondente"
    - "Botão Editar Dialog completo (para editar período/tipo/descrição) PRESERVADO — inline edit é atalho para target_value apenas"
  artifacts:
    - path: "resources/js/Pages/Goals/Index.jsx"
      provides: "Aba 'Por Empresa' com inline edit + ícone Clock por meta + integração <GoalHistoryDrawer />"
      contains: "GoalHistoryDrawer"
    - path: "app/Http/Controllers/GoalController.php"
      provides: "GoalController::index inclui `results` (últimos até 12) em cada goal do payload das companies"
      contains: "results"
  key_links:
    - from: "Goals/Index.jsx EmpresasSection"
      to: "route('goals.update', goal.id)"
      via: "editForm.put + preserveScroll"
      pattern: "put\\(route\\('goals\\.update'"
    - from: "Goals/Index.jsx"
      to: "GoalHistoryDrawer component"
      via: "import + <GoalHistoryDrawer />"
      pattern: "GoalHistoryDrawer"
    - from: "GoalController::index"
      to: "goal.results relation"
      via: "$c->goals->load('results') ou eager"
      pattern: "results"
---

<objective>
Costurar inline edit + histórico drawer na aba "Por Empresa" da tela `/goals`, atendendo META-04 sem quebrar o comportamento existente (Dialog de edição completa, Nova Meta, exibição de period/value_type).

Purpose: Hoje a edição exige clicar em ícone Pencil → abrir Dialog → editar → submeter. Isso é 4 cliques + navegação para editar um número. Inline edit no valor da meta transforma em 2 cliques + digitar + Enter — atendendo "edição rápida sem sair da listagem".

Output: Página Goals/Index.jsx com nova UX de inline edit e drawer de histórico; GoalController::index enriquecido com `results` para permitir mini-progresso opcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-01-PLAN.md
@.planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-03-PLAN.md

@resources/js/Pages/Goals/Index.jsx
@app/Http/Controllers/GoalController.php
@app/Models/Goal.php
@app/Models/GoalResult.php

<interfaces>
<!-- Contratos definidos. NÃO explorar codebase. -->

## GoalController::index shape atual (linhas 27-41)

Cada company mapeada retorna:
```
{
  id, name, consultor, estrategista,
  goals: [
    { id, metric, metric_label, target_value, value_type, period_type, description }
  ]
}
```

**Precisa adicionar** `results` na goal (últimos até 12 GoalResult ordenados por period ASC):
```
goals: [
  {
    ...campos atuais,
    results: [
      { id, period, realized_value, target_value, achieved }
    ]
  }
]
```

Query eager: `Company::with(['goals' => fn($q) => $q->where('active', true)->with(['results' => fn($rq) => $rq->orderBy('period', 'desc')->limit(12)])])`.
Depois no map, inverter order para ASC pro chart: `->results->reverse()->values()->map(...)`.

## GoalHistoryDrawer prop signature (do Plan 62-03)
```
<GoalHistoryDrawer
  open={historyOpen}
  onOpenChange={setHistoryOpen}
  goalId={historyGoalId}
  metricLabel={historyGoal?.metric_label}
/>
```

## Endpoint PUT /goals/{goal} (do Plan 62-01)
Aceita mesmas keys de antes: `target_value` (obrigatório), `period_type` (obrigatório), `value_type` (nullable), `description` (nullable), `active` (boolean). Inline edit envia apenas `target_value` + `period_type` (mantém period_type atual).

## useForm padrão Inertia (já usado no arquivo)
```
const inlineForm = useForm({ target_value: '', period_type: 'monthly' });
inlineForm.put(route('goals.update', goalId), {
  preserveScroll: true,
  onError: () => rollback(),
  onSuccess: () => setEditingGoalId(null),
});
```

## Ícones lucide-react já disponíveis
`Clock`, `Check`, `X` (já usados no projeto).

## Layout atual (Goals/Index.jsx linhas 162-198)
Cada goal é renderizada em um card com:
- `metric_label` (uppercase)
- `formatGoalValue(goal)` — grande, font-display
- badges de period_type e value_type
- description
- botões Pencil (edit dialog) + Trash (delete) — só admin

Novo: adicionar botão Clock ao lado do Pencil (sempre visível, não só admin), e envolver o `<p>` do `formatGoalValue` com componente `<InlineTargetEdit>` que alterna entre display e edit mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enriquecer GoalController::index com results (últimos 12 períodos por goal)</name>
  <files>
    - app/Http/Controllers/GoalController.php
  </files>
  <read_first>
    - app/Http/Controllers/GoalController.php (linhas 14-97 — método index inteiro)
    - app/Models/Goal.php linha 99-102 (relação `results()` já existe: `hasMany(GoalResult::class)->orderBy('period', 'desc')`)
    - app/Models/GoalResult.php inteiro (fields: period, realized_value, target_value, achieved)
  </read_first>
  <action>
    1. Em `GoalController::index`, na eager load das companies (`->with(['goals', 'consultor', 'estrategista'])`), incluir results: `->with(['goals' => fn($q) => $q->where('active', true)->with(['results' => fn($rq) => $rq->orderBy('period', 'desc')->limit(12)]), 'consultor', 'estrategista'])`.
       - IMPORTANTE: o `->where('active', true)` que hoje está no `->goals->where('active', true)->map(...)` da linha 32 (mapper) pode ser movido para o closure de eager load ou permanecer no map — escolher UMA das opções e ficar consistente. Preferir mover para eager load (economiza query).
    2. Ajustar o mapper da linha 32 pra também passar `results` (em ordem ASC pra chart):
       ```
       'results' => $g->results
           ->sortBy('period')
           ->values()
           ->map(fn($r) => [
               'id'             => $r->id,
               'period'         => $r->period,
               'realized_value' => (float) $r->realized_value,
               'target_value'   => (float) $r->target_value,
               'achieved'       => (bool) $r->achieved,
           ])
       ```
    3. NÃO tocar em outras props (users, my_portfolio_goals, metrics, etc.).
    4. Comentário pt-BR no closure: `// Phase 62 (META-04): results últimos 12 períodos alimenta chart do <GoalProgressPanel> quando exibido inline.`
    5. Verificar que `$user->isAdmin()` filter continua funcionando (linhas 22-25).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase62 tests/Feature/Phase62/ 2>&1 | tail -20 && echo "---" && php -r "require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$app->make(Illuminate\\Contracts\\Console\\Kernel::class)->bootstrap(); echo 'Bootstrap OK';"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'results'" app/Http/Controllers/GoalController.php` >= 1
    - `grep -c "sortBy('period')" app/Http/Controllers/GoalController.php` == 1
    - Suite Phase62 do plan 62-01 continua verde
    - `php artisan route:list --path=goals` não muda
    - `php artisan tinker --execute "var_dump(App\Http\Controllers\GoalController::class);"` retorna string OK (parse do arquivo sem erro sintático)
  </acceptance_criteria>
  <done>
    GoalController::index expõe `goals[].results` no payload. Formato ASC por period. Zero mudança no shape de outras chaves. Testes Phase 62 do 62-01 seguem verdes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inline edit de target_value + integração <GoalHistoryDrawer /> na aba Por Empresa</name>
  <files>
    - resources/js/Pages/Goals/Index.jsx
  </files>
  <read_first>
    - resources/js/Pages/Goals/Index.jsx (LEITURA COMPLETA — 724 linhas, focar em EmpresasSection linhas 59-607)
    - resources/js/Components/goals/GoalHistoryDrawer.jsx (deve existir após 62-03; ler o export default para confirmar prop signature)
    - resources/js/Components/ui/input.jsx (padrão do input)
    - .planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-01-PLAN.md § endpoint PUT
  </read_first>
  <action>
    1. Adicionar imports no topo:
       ```
       import { Clock, Check, X as XIcon } from 'lucide-react';  // Clock/Check/X (X renomeado pra XIcon)
       import GoalHistoryDrawer from '@/Components/goals/GoalHistoryDrawer';
       ```
    2. Dentro de `EmpresasSection`, adicionar estados novos:
       ```
       const [editingGoalId, setEditingGoalId] = useState(null);
       const [editingValue, setEditingValue] = useState('');
       const [historyOpen, setHistoryOpen] = useState(false);
       const [historyGoal, setHistoryGoal] = useState(null);
       const inlineForm = useForm({ target_value: '', period_type: 'monthly' });
       ```
    3. Handlers:
       - `startInlineEdit(goal)`: `setEditingGoalId(goal.id); setEditingValue(String(goal.target_value)); inlineForm.setData({ target_value: String(goal.target_value), period_type: goal.period_type });`
       - `cancelInlineEdit()`: `setEditingGoalId(null); setEditingValue('');`
       - `commitInlineEdit(goal)`: se `editingValue === String(goal.target_value)` → `cancelInlineEdit()` (no-op). Senão `inlineForm.setData({ target_value: editingValue, period_type: goal.period_type }); inlineForm.put(route('goals.update', goal.id), { preserveScroll: true, onSuccess: () => setEditingGoalId(null), onError: () => { /* mantém aberto pra correção */ } });`
       - `openHistory(goal)`: `setHistoryGoal(goal); setHistoryOpen(true);`
    4. Substituir o `<p className="font-display font-extrabold text-2xl leading-none">{formatGoalValue(goal)}</p>` (linhas 169 e 630) por um bloco condicional:
       ```jsx
       {editingGoalId === goal.id ? (
         <form onSubmit={e => { e.preventDefault(); commitInlineEdit(goal); }}
               className="flex items-center gap-1"
               data-testid={`goal-inline-edit-form-${goal.id}`}>
           <Input
             type="number" step="0.01" min="0" autoFocus
             value={editingValue}
             onChange={e => setEditingValue(e.target.value)}
             onBlur={() => commitInlineEdit(goal)}
             onKeyDown={e => e.key === 'Escape' && cancelInlineEdit()}
             disabled={inlineForm.processing}
             data-testid={`goal-inline-edit-input-${goal.id}`}
             className="h-9 w-32 text-lg font-bold"
           />
           <button type="submit" className="p-1 rounded hover:bg-white/10" title="Salvar"><Check size={14} /></button>
           <button type="button" onClick={cancelInlineEdit} className="p-1 rounded hover:bg-white/10" title="Cancelar"><XIcon size={14} /></button>
         </form>
       ) : (
         <button
           type="button"
           onClick={() => can_manage && startInlineEdit(goal)}
           disabled={!can_manage}
           data-testid={`goal-inline-edit-trigger-${goal.id}`}
           className={cn(
             "font-display font-extrabold text-2xl leading-none text-left",
             can_manage && "hover:underline decoration-dashed underline-offset-4 cursor-text",
           )}
           title={can_manage ? 'Clique para editar' : undefined}
         >
           {formatGoalValue(goal)}
         </button>
       )}
       ```
    5. Ao lado do botão Pencil (linhas 184-195), adicionar novo botão Clock (visível pra TODOS — leitura de histórico não requer manage):
       ```jsx
       <button
         type="button"
         onClick={() => openHistory(goal)}
         data-testid={`goal-history-open-${goal.id}`}
         className="p-1.5 rounded-lg hover:bg-white/10 transition-colors opacity-50 hover:opacity-100"
         title="Ver histórico de alterações"
       >
         <Clock size={13} />
       </button>
       ```
       Reordenar para: Clock → Pencil → Trash. Remover o wrapper `{can_manage && ...}` que envolvia apenas Pencil+Trash — envolver APENAS esses dois botões, deixar Clock fora do gate.
    6. Renderizar `<GoalHistoryDrawer />` no final do fragment do return (mesmo nível dos outros Dialogs):
       ```jsx
       <GoalHistoryDrawer
         open={historyOpen}
         onOpenChange={setHistoryOpen}
         goalId={historyGoal?.id ?? null}
         metricLabel={historyGoal?.metric_label}
       />
       ```
    7. PRESERVAR sem tocar: Dialog "Nova Meta", Dialog "Editar Meta" (funciona pra editar tipo/período/descrição), botão Pencil, botão Trash, empty state, badges de period/value_type.
    8. Comentário pt-BR no topo dos handlers: `// Phase 62 — META-04: inline edit envia só target_value+period_type; para editar tipo/período completo, usar botão Pencil.`
    9. NÃO tocar em `CarteirasSection` nem `MinhasMetasCarteira` — inline edit só na aba Por Empresa (SC #2 do phase).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error|Error" || echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completa sem erros
    - `grep -c "GoalHistoryDrawer" resources/js/Pages/Goals/Index.jsx` >= 2 (import + JSX)
    - `grep -c "goal-inline-edit-trigger" resources/js/Pages/Goals/Index.jsx` == 1
    - `grep -c "goal-inline-edit-input" resources/js/Pages/Goals/Index.jsx` == 1
    - `grep -c "goal-history-open" resources/js/Pages/Goals/Index.jsx` == 1
    - `grep -c "inlineForm" resources/js/Pages/Goals/Index.jsx` >= 3
    - `grep -c "Nova Meta" resources/js/Pages/Goals/Index.jsx` >= 1 (Dialog original preservado)
    - `grep -c "Editar Meta" resources/js/Pages/Goals/Index.jsx` >= 1 (Dialog original preservado)
    - Preserva `formatGoalValue(goal)` em ambos os locais (aba empresa + aba carteira)
    - Botão Clock renderizado FORA do bloco `{can_manage && ...}` (histórico livre para leitura)
  </acceptance_criteria>
  <done>
    Aba Por Empresa suporta inline edit do target_value com Enter/Blur (submit) e Escape (cancel). Ícone Clock por meta abre GoalHistoryDrawer. Dialogs originais Nova Meta e Editar Meta preservados. Build Vite verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click inline → PUT /goals/{id} | Auth já validado no Plan 62-01 (admin OR estrategista). Client não pode bypassar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04-01 | Elevation of Privilege | Consultor / analista vê botão inline edit mas backend nega | mitigate | UI gate `disabled={!can_manage}` já no trigger; backend 403 (Plan 62-01) é linha de defesa final; Inertia surface o erro flash pra usuário |
| T-62-04-02 | Tampering | Payload inline pode conter chaves extras (active, description) | mitigate | inlineForm só carrega `target_value` + `period_type`; validate no Controller (Plan 62-01) rejeita chaves não listadas via `$request->validate()` |
| T-62-04-03 | Denial of Service | Ficar clicando no display pode gerar N chamadas concorrentes | mitigate | `disabled={inlineForm.processing}` no Input; `onSuccess` fecha o modo edit antes de reabrir |
| T-62-04-SC | Tampering | npm installs | accept | Zero pacotes novos |
</threat_model>

<verification>
- `npm run build` verde
- grep gates passam
- Regressão zero: `/goals` continua renderizando aba Por Empresa + aba Por Carteira (admin) + tab switch funciona
- Zero mudança na aba Por Carteira (só a aba Por Empresa recebe inline edit)
</verification>

<success_criteria>
- Inline edit de target_value entregue com Enter/Blur/Escape
- Ícone Clock abre drawer de histórico
- Dialogs originais preservados (não regressão)
- GoalController::index enriquecido com results
</success_criteria>

<output>
Criar `.planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-04-SUMMARY.md` com: (a) diff resumido da aba Por Empresa (screenshot mental do UI antes/depois), (b) confirmação dos data-testids adicionados, (c) build status, (d) confirmação de zero regressão na aba Por Carteira.
</output>
