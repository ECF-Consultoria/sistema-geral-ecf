---
phase: 90-carteiras-consolidadas-rendercarteirasconsolidadas-v17-0
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/Feature/V16/CarteirasConsolidadasContextoTest.php
  - app/Http/Controllers/PortfolioController.php
autonomous: true
requirements: [CART-06, CART-07]
tags: [laravel, controller-refactor, carteira, multi-servico, tdd]

must_haves:
  truths:
    - "Card de profissional só-Shopee numa empresa que também tem ML NÃO mostra total_revenue/avg_margin/avg_tacos de ML dessa empresa"
    - "Card de profissional com ML+Shopee na MESMA empresa expõe empresas_unicas=1 e vinculos_servico=2 sem duplicar total_revenue"
    - "?contexto=performance|shopee filtra vínculos nas DUAS telas (consolidada e individual); valor inválido cai em 'todos'"
    - "Totais do topo da consolidada usam UNIÃO de company_id de todos os vínculos exibidos — empresa compartilhada entre 2 profissionais conta 1 vez"
    - "source_counts (flag unified_metrics_enabled) não conta empresa Shopee-only como fonte do profissional"
  artifacts:
    - path: "tests/Feature/V16/CarteirasConsolidadasContextoTest.php"
      provides: "Cobertura Feature de CART-06 + CART-07 backend (10+ testes)"
      min_lines: 200
    - path: "app/Http/Controllers/PortfolioController.php"
      provides: "renderCarteirasConsolidadas via CarteiraContextService + filtro ?contexto= nas duas funções"
      contains: "contextoFiltro"
  key_links:
    - from: "app/Http/Controllers/PortfolioController.php"
      to: "app/Services/Portfolio/CarteiraContextService.php"
      via: "carteiraContext->forUser dentro do map() de renderCarteirasConsolidadas"
      pattern: "carteiraContext->forUser"
    - from: "app/Http/Controllers/PortfolioController.php"
      to: "CarteiraContextService::contadores"
      via: "spread dos contadores no payload do card"
      pattern: "carteiraContext->contadores"
    - from: "renderCarteirasConsolidadas"
      to: "AdmanMetric"
      via: "whereIn com companyIdsElegiveis (unique)"
      pattern: "companyIdsElegiveis"
---

<objective>
Refatorar `renderCarteirasConsolidadas()` para resolver vínculos via `CarteiraContextService::forUser()` (CART-06): financeiro gated por `financial_metrics_eligible`, contadores por card via `contadores()`, `source_counts` corrigido na MESMA fonte, e filtro de contexto `?contexto=` (todos/performance/shopee) funcional nas DUAS funções de carteira (consolidada E individual — CART-07 backend). TDD: testes primeiro (RED), implementação depois (GREEN).

Purpose: fechar o mesmo bug de herança financeira que a Fase 89 corrigiu na carteira individual, agora na visão de cards por profissional — Felipe (estrategista Shopee) parava de "herdar" o faturamento ML de empresas geridas por outro profissional.
Output: `PortfolioController.php` refatorado + `tests/Feature/V16/CarteirasConsolidadasContextoTest.php` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-carteiras-consolidadas-rendercarteirasconsolidadas-v17-0/90-RESEARCH.md
@app/Http/Controllers/PortfolioController.php
@app/Services/Portfolio/CarteiraContextService.php
@tests/Feature/V16/CarteiraFinanceiroElegibilidadeTest.php
@tests/Feature/V16/CriaCenarioResponsaveis.php
</context>

<interfaces>
<!-- Contratos já existentes que o executor usa direto — sem exploração de codebase. -->

De `app/Services/Portfolio/CarteiraContextService.php` (Fase 88, pronto e testado):

```php
// forUser: resolve vínculos por serviço (fonte 1: servico_id preenchido; fonte 2: legado NULL→performance)
public function forUser(User $user, array $filters = []): Collection
// $filters aceitos: ['setor' => ?string, 'role' => ?string, 'active' => bool]
// shape de cada item: { user_id, company_id, company_name, servico_id, servico_nome,
//   setor, role, role_label, has_financial_source, financial_source, financial_metrics_eligible }

// contadores: metadados de dedup sobre Collection já resolvida por forUser()
public function contadores(Collection $vinculos): array
// retorna: { empresas_unicas, vinculos_servico, vinculos_financeiros, vinculos_sem_fonte_financeira }
```

De `app/Models/Servico.php`: constantes `Servico::SETOR_PERFORMANCE` (= 'performance') e `Servico::SETOR_SHOPEE` (= 'shopee').

De `tests/Feature/V16/CriaCenarioResponsaveis.php` (trait):
```php
protected function criarServico(string $setor, bool $ativo = true): int
protected function criarContrato(int $companyId, int $servicoId, bool $ativo = true): int
protected function inserirPivot(int $companyId, int $userId, string $role, ?int $servicoId): int
protected function criarCenarioMlComResponsaveis(): array // {company, servicoPerf, analista, estrategista}
protected function inserirLinhaShopee(int $companyId, int $userId, string $role): array // {servicoShopee, row}
```

Precedente de acesso a props Inertia em teste (CarteiraFinanceiroElegibilidadeTest):
`$props = $response->viewData('page')['props'];`
</interfaces>

<feature>
  <name>Carteiras consolidadas via CarteiraContextService + filtro de contexto</name>
  <files>app/Http/Controllers/PortfolioController.php, tests/Feature/V16/CarteirasConsolidadasContextoTest.php</files>
  <behavior>
    Cenário-chave (CART-06, travado pelo usuário): empresa X com contrato performance ativo + AdmanMetric (revenue 1000). Luiz = estrategista de PERFORMANCE de X; Felipe = estrategista de SHOPEE de X. Resultado esperado:
    - Card do Luiz: total_revenue = 1000.0, empresas_unicas = 1, vinculos_financeiros = 1
    - Card do Felipe: total_revenue = 0.0, avg_tacos/avg_margin = null, empresas_unicas = 1, vinculos_sem_fonte_financeira = 1 (card EXISTE, não some)
    - Totais do topo: empresas_unicas = 1 (X conta UMA vez — união, nunca soma de cards), vinculos_servico = 2
  </behavior>
  <implementation>
    Ver Task 2. Mesma receita provada na Fase 89 (`renderCarteiraProfissional`), aplicada dentro do `map()` que monta cada card.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): suite CarteirasConsolidadasContextoTest com testes falhando</name>
  <files>tests/Feature/V16/CarteirasConsolidadasContextoTest.php</files>
  <behavior>
    Criar `tests/Feature/V16/CarteirasConsolidadasContextoTest.php` (`use RefreshDatabase; use CriaCenarioResponsaveis;`). Como a consolidada só lista profissionais presentes em `user_setores` com cargo analista/estrategista, adicionar helpers locais no teste (mesmo pattern de `tests/Feature/DesempenhoScoreSnapshotTest.php::setUp` + `criarUserComCargo`): `setUp()` insere via `DB::table` 1 setor + cargos `analista` e `estrategista`; helper `vincularCargo(User $u, string $cargoSlug)` insere a linha `user_setores`. Reutilizar também os helpers `criarAdmin()`, `dataNoMesCorrente()` e `seedAdmanMetric()` no shape do `CarteiraFinanceiroElegibilidadeTest` (copiar localmente — são privados lá). Rota da consolidada: `actingAs($admin)->get(route('portfolio.own'))` (admin cai em renderCarteirasConsolidadas); props em `$response->viewData('page')['props']` — cards em `user_portfolios`, localizar card por `id` do user.

    Testes obrigatórios (nomes em pt-BR, snake_case):
    - Test 1 `test_shopee_only_nao_herda_financeiro_ml_na_consolidada` — cenário-chave Luiz/Felipe do bloco `<behavior>`: os 2 asserts de card + os 2 asserts de totais do topo (`props['totais']['empresas_unicas'] === 1`, `props['totais']['vinculos_servico'] === 2`).
    - Test 2 `test_card_ml_e_shopee_mesma_empresa_nao_duplica_financeiro` — profissional com vínculo performance E shopee na MESMA empresa (usar `criarCenarioMlComResponsaveis()` + `inserirLinhaShopee()` no analista + `vincularCargo`): card com `total_revenue === 1000.0` (2000.0 seria o bug), `empresas_unicas === 1`, `vinculos_servico === 2`.
    - Test 3 `test_regressao_card_analista_ml_comum` — analista só-ML: `total_revenue === 1000.0`, `total_ad_spend === 100.0`, `avg_tacos === 10.0` (fallback ads/rev*100 — cache Adman é miss em teste), `empresas_unicas === 1`, `vinculos_financeiros === 1`.
    - Test 4 `test_filtro_contexto_shopee_esconde_card_sem_vinculo_shopee` — cenário Luiz/Felipe + `get(route('portfolio.own', ['contexto' => 'shopee']))`: card do Luiz DESAPARECE (não zerado — decisão travada, consistente com o `->filter()` atual), card do Felipe permanece com `total_revenue === 0.0`; nenhum card do payload tem `total_revenue > 0` (Shopee NUNCA mostra R$ de ML).
    - Test 5 `test_filtro_contexto_performance_esconde_card_so_shopee` — inverso: Felipe some, Luiz permanece com `total_revenue === 1000.0`.
    - Test 6 `test_contexto_invalido_cai_em_todos` — `['contexto' => 'hacker\'--']`: mesmos cards do default (whitelist — os 2 cards aparecem), e prop `contexto === 'todos'`.
    - Test 7 `test_contador_agregado_usa_uniao_de_company_id` — empresa X compartilhada: Luiz estrategista performance + Maria estrategista performance da MESMA empresa X (2 cards, mesma empresa): `totais.empresas_unicas === 1`, `totais.vinculos_servico === 2` (soma ingênua daria 2 empresas — o bug que a decisão travada proíbe).
    - Test 8 `test_source_counts_nao_conta_empresa_shopee_only` — `config(['metrics.unified_metrics_enabled' => true])`; cenário Luiz/Felipe (empresa X TEM adman_account_id): card do Felipe tem `source_counts` com soma 0 (empresa não é fonte dele); card do Luiz tem `adman === 1` (sem mlToken → so-adman).
    - Test 9 `test_filtro_contexto_funciona_na_carteira_individual` — profissional com performance+shopee na mesma empresa; `get(route('portfolio.show', [$prof, 'contexto' => 'shopee']))`: `empresas` tem 1 linha cujo array `servicos` contém SÓ o vínculo shopee (count 1, `setor === 'shopee'`), `resumo.total_faturamento === 0.0`.
    - Test 10 `test_resumo_individual_expoe_contadores_de_vinculos` — mesmo profissional SEM filtro: `resumo.vinculos_servico === 2`, `resumo.vinculos_sem_fonte_financeira === 1`, prop `contexto === 'todos'`.

    Rodar `php artisan test --filter=CarteirasConsolidadasContextoTest` — TODOS devem FALHAR (payload atual não tem empresas_unicas/totais/contexto e não filtra). Tests 1-3 podem falhar por chave ausente (ok, é RED). Commit: `test(90-01): suite RED consolidada contexto + dedup (CART-06/07)`.
  </behavior>
  <action>
    Escrever o arquivo de teste completo conforme `<behavior>`. NÃO tocar no controller nesta task. Atenção: `inserirLinhaShopee` já cria contrato shopee ativo pra empresa — não duplicar. `AdmanMetric` semeado com `dataNoMesCorrente()` cai na janela default `period=30` da consolidada. Não usar factories de Servico/ContratoServico (não existem — trait usa DB::table).
  </action>
  <verify>
    <automated>php artisan test --filter=CarteirasConsolidadasContextoTest 2>&1 | grep -E "FAIL|Tests:" — suite executa (sem erro de sintaxe/fixture) e falha por asserção</automated>
  </verify>
  <done>Arquivo criado com 10 testes, todos executam e falham por asserção (não por erro de setup), commit RED feito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): refatorar renderCarteirasConsolidadas + ?contexto= nas duas funções</name>
  <files>app/Http/Controllers/PortfolioController.php</files>
  <behavior>Todos os 10 testes da Task 1 passam; suites CarteiraFinanceiroElegibilidadeTest e V16 continuam verdes (regressão zero).</behavior>
  <action>
    Tudo em `app/Http/Controllers/PortfolioController.php`, comentários pt-BR. Adicionar `use App\Models\Servico;` nos imports.

    **1. Helper privado `contextoFiltro(Request $request): array`** — parse com whitelist (ASVS V5, Pitfall 3 do research): lê `$request->query('contexto')`; retorna `['param' => 'todos'|'performance'|'shopee', 'setor' => null|Servico::SETOR_PERFORMANCE|Servico::SETOR_SHOPEE]`. Valor fora da whitelist → `['param' => 'todos', 'setor' => null]`. Nomear `?contexto=` e variável `$contextoFiltro` — NUNCA `?setor=` (colide com `$setoresFiltro` organizacional já existente no mesmo método — decisão travada).

    **2. `renderCarteirasConsolidadas()` (linhas ~561-734)** — manter período, listas de analistas/estrategistas e `$setoresFiltro` intocados. Dentro do `map()` por profissional:
    - Trocar a origem (linha ~619-621): de `$u->estrategistaCompanies()/consultorCompanies()` para `$vinculos = $this->carteiraContext->forUser($u, ['role' => $tipo === 'estrategista' ? 'estrategista' : 'consultor', 'active' => true, 'setor' => $contextoFiltro['setor']])`. As relações antigas permanecem INTOCADAS em `User.php` (outros call-sites) — só o controller reaponta.
    - `if ($vinculos->isEmpty()) return null;` — preserva o `->filter()` atual; card 100% fora do contexto DESAPARECE (decisão travada).
    - `$contadores = $this->carteiraContext->contadores($vinculos);` — spread no payload do card (`empresas_unicas`, `vinculos_servico`, `vinculos_financeiros`, `vinculos_sem_fonte_financeira`). Não reinventar contagem.
    - Dedup financeiro (MESMA receita da Fase 89, linhas 177-181 do arquivo): `$companyIdsElegiveis = $vinculos->where('financial_metrics_eligible', true)->pluck('company_id')->unique()->values();`. AdmanMetric é por-EMPRESA — nunca iterar vínculos na soma (Pitfall 1).
    - Se `$companyIdsElegiveis` vazio (profissional só-Shopee ou filtro shopee ativo): card existe com contadores + `avg_tacos => null, total_revenue => 0.0, avg_margin => null, total_ad_spend => 0.0` (pseudocódigo do research §Algoritmo de dedup).
    - Senão: `$companies = Company::whereIn('id', $companyIdsElegiveis)->get(['id', 'adman_account_id', 'ml_store_id']);` e o bloco de soma existente (SUM DB + cache gross/account, linhas ~628-689) fica IGUAL, só trocando a fonte de `$companies`/`$companyIds`.
    - Manter no payload: `id, name, tipo, role, avg_tacos, total_revenue, avg_margin, total_ad_spend` + `'companies_count' => $contadores['empresas_unicas']` como ALIAS TEMPORÁRIO (Pitfall 4 — `Carteiras.jsx:82` consome a chave hoje; o Plan 90-02 troca o .jsx e remove o alias no mesmo commit).
    - **`source_counts` corrigido JUNTO (Pitfall 2, decisão travada):** mover o cálculo pra DENTRO do map principal (elimina o segundo `map()` das linhas ~709-728 que recomputava as relações antigas). Quando `$this->unifiedMetricsEnabled()`: `$companiesFonte = Company::whereIn('id', $companyIdsElegiveis)->with('mlToken')->get();` e contar `{adman, ml, unified, none}` via `factoryToSource()`. Card sem elegíveis → todos zero. Deletar o bloco condicional antigo por completo.
    - **Totais agregados do topo (SC4 + decisão travada):** acumular fora do map (via `use (&$...)` ou coletando os `$vinculos` de cada card num Collection): `totais.empresas_unicas` = UNIÃO (`->unique()`) de TODOS os `company_id` de TODOS os vínculos exibidos — NUNCA soma de `empresas_unicas` entre cards (empresa compartilhada contaria 2x); `totais.vinculos_servico` = soma dos vínculos exibidos (aditivamente correta). Cards que retornam null não contribuem.
    - Props novas no `Inertia::render('Portfolio/Carteiras', ...)`: `'contexto' => $contextoFiltro['param']` e `'totais' => ['empresas_unicas' => ..., 'vinculos_servico' => ...]`.

    **3. `renderCarteiraProfissional()` (SC3 — o filtro vale pras DUAS telas):**
    - Linha ~162: `$vinculos = $this->carteiraContext->forUser($user, ['active' => true, 'setor' => $contextoFiltro['setor']]);` (única mudança na resolução — default 'todos' preserva comportamento atual, regressão zero na suite da Fase 89).
    - No `resumo`: adicionar `'vinculos_servico'` e `'vinculos_sem_fonte_financeira'` vindos de `$this->carteiraContext->contadores($vinculos)` (`total_empresas` já cobre empresas únicas).
    - Prop nova: `'contexto' => $contextoFiltro['param']`.

    Rodar `php artisan test --filter=CarteirasConsolidadasContextoTest` até verde. Commit: `feat(90-01): consolidada via CarteiraContextService + filtro ?contexto= (CART-06/07)`.
  </action>
  <verify>
    <automated>php artisan test --filter=CarteirasConsolidadasContextoTest && php artisan test --filter=CarteiraFinanceiroElegibilidadeTest && php artisan test --filter=V16</automated>
  </verify>
  <done>10/10 testes novos verdes; CarteiraFinanceiroElegibilidadeTest (5 testes da Fase 89) e suite V16 completas sem regressão; nenhum call-site de `estrategistaCompanies()`/`consultorCompanies()` restante em renderCarteirasConsolidadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→controller | query param `?contexto=` (input não confiável) cruza aqui |
| carteira→carteira | dados financeiros de empresa gerida por outro profissional não podem vazar pro card errado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Information Disclosure | renderCarteirasConsolidadas (soma AdmanMetric) | mitigate | gate por `financial_metrics_eligible=true` + `->unique()` de company_id — é exatamente o que CART-06 corrige (Test 1/2) |
| T-90-02 | Tampering (input) | `?contexto=` query param | mitigate | whitelist explícita todos/performance/shopee no helper `contextoFiltro()`; valor arbitrário cai em 'todos' (Test 6) |
| T-90-03 | Elevation of Privilege | acesso à consolidada | accept | gate existente em `own()` (isAdmin/isLider + $setoresFiltro) permanece intocado — fora do escopo da fase |
| T-90-SC | Tampering | npm/composer installs | accept | nenhum pacote novo instalado nesta fase (audit do research: N/A) |
</threat_model>

<verification>
- `php artisan test --filter=CarteirasConsolidadasContextoTest` — 10/10 verde
- `php artisan test --filter=V16` — regressão zero (baseline Fases 88/89)
- `php artisan test --filter=Portfolio` — RenderPortfolioTest intocado (renderPortfolio é known-gap documentado, NÃO tocar — decisão travada)
- `git status` antes de commitar — confirmar que NENHUM arquivo de NPS (`app/Services/Nps/`, `NpsController`, `resources/js/Pages/Nps/*`), `User.php`, `DesempenhoScoreService`, `CompanyController` ou `Company.php` foi tocado (fronteira do dev paralelo)
</verification>

<success_criteria>
- SC1 (backend): cards expõem `empresas_unicas` ≠ `vinculos_servico` (Test 2)
- SC2: profissional só-Shopee não puxa faturamento/margem ML (Test 1)
- SC3 (backend): `?contexto=` funcional nas duas funções de render (Tests 4/5/9)
- Totais por união (Test 7) e `source_counts` gated (Test 8) — decisões travadas atendidas
- `renderPortfolio()` NÃO tocado (débito técnico documentado — reafirmar 1 linha no SUMMARY)
</success_criteria>

<output>
Criar `.planning/phases/90-carteiras-consolidadas-rendercarteirasconsolidadas-v17-0/90-01-SUMMARY.md` ao concluir (pt-BR). Incluir 1 linha reafirmando que o gap de `renderPortfolio()` persiste por decisão (89-RESEARCH Pitfall 2 / 90-RESEARCH §known-gap).
</output>
