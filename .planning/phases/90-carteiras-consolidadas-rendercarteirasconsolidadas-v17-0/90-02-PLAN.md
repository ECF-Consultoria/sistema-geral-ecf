---
phase: 90-carteiras-consolidadas-rendercarteirasconsolidadas-v17-0
plan: 02
type: execute
wave: 2
depends_on: [90-01]
files_modified:
  - tests/Feature/V16/CarteirasConsolidadasContextoTest.php
  - resources/js/Pages/Portfolio/Carteiras.jsx
  - resources/js/Pages/Portfolio/AdminCarteira.jsx
  - app/Http/Controllers/PortfolioController.php
autonomous: false
requirements: [CART-07]
tags: [react, inertia, carteira, ui, filtro-contexto]

must_haves:
  truths:
    - "Select de contexto (Todos / Mercado Livre / Shopee) visível nas DUAS telas e dispara reload com ?contexto="
    - "Card da consolidada mostra 'X empresas · Y vínculos' (empresas_unicas/vinculos_servico) e chip âmbar quando vinculos_sem_fonte_financeira > 0"
    - "Topo da consolidada mostra os contadores agregados (união de empresas + total de vínculos exibidos)"
    - "AdminCarteira preserva ?mes= ao trocar o contexto e mostra contadores no topo"
    - "npm run build sai com exit 0"
  artifacts:
    - path: "resources/js/Pages/Portfolio/Carteiras.jsx"
      provides: "Select contexto + contadores por card + banner agregado"
      contains: "empresas_unicas"
    - path: "resources/js/Pages/Portfolio/AdminCarteira.jsx"
      provides: "Select contexto (preservando ?mes=) + contadores de topo"
      contains: "contexto"
    - path: "app/Http/Controllers/PortfolioController.php"
      provides: "Alias companies_count removido (payload limpo)"
  key_links:
    - from: "resources/js/Pages/Portfolio/Carteiras.jsx"
      to: "route portfolio.own"
      via: "router.get com { period, contexto }"
      pattern: "contexto"
    - from: "resources/js/Pages/Portfolio/AdminCarteira.jsx"
      to: "renderCarteiraProfissional"
      via: "router.visit com ?contexto= + ?mes="
      pattern: "contexto"
    - from: "resources/js/Pages/Portfolio/Carteiras.jsx"
      to: "payload user_portfolios"
      via: "lê empresas_unicas/vinculos_servico (não mais companies_count)"
      pattern: "vinculos_servico"
---

<objective>
Entregar a UI do CART-07: seletor de contexto (Todos / Mercado Livre / Shopee) nas duas telas de carteira, contadores empresas únicas × vínculos de serviço por card e no topo, chip "sem fonte financeira" nos cards da consolidada, e troca da chave legada `companies_count` pelos contadores novos (removendo o alias no controller no MESMO commit — Pitfall 4).

Purpose: fechar o SC3/SC4 da fase — o backend do 90-01 já entrega os dados; falta a camada apresentacional nas duas telas.
Output: `Carteiras.jsx` + `AdminCarteira.jsx` evoluídos, bundle rebuildado, checkpoint visual aprovado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/90-carteiras-consolidadas-rendercarteirasconsolidadas-v17-0/90-RESEARCH.md
@.planning/phases/90-carteiras-consolidadas-rendercarteirasconsolidadas-v17-0/90-01-SUMMARY.md
@resources/js/Pages/Portfolio/Carteiras.jsx
@resources/js/Pages/Portfolio/AdminCarteira.jsx
@app/Http/Controllers/PortfolioController.php
</context>

<interfaces>
<!-- Props entregues pelo backend após o Plan 90-01 (contrato pronto — não explorar). -->

`Portfolio/Carteiras` (consolidada):
```js
// user_portfolios: [{ id, name, tipo, role,
//   empresas_unicas, vinculos_servico, vinculos_financeiros, vinculos_sem_fonte_financeira,
//   companies_count,            // ALIAS legado — REMOVER neste plan (backend + frontend juntos)
//   avg_tacos, total_revenue, avg_margin, total_ad_spend, source_counts? }]
// totais: { empresas_unicas, vinculos_servico }   // união — nunca soma de cards
// contexto: 'todos' | 'performance' | 'shopee'
// period: '1'|'7'|'30'|'180'
```

`Portfolio/AdminCarteira` (individual):
```js
// resumo: { total_empresas, ..., vinculos_servico, vinculos_sem_fonte_financeira }
// contexto: 'todos' | 'performance' | 'shopee'
// periodo: { mes_selecionado, meses_disponiveis, em_curso, ... }   // já existente
```

Padrões visuais a reaproveitar (já no codebase):
- `SETOR_LABELS = { performance: 'Mercado Livre', shopee: 'Shopee' }` (AdminCarteira.jsx:53 — rótulos sem jargão, nunca slug cru)
- Chip âmbar sem-fonte: `bg-amber-500/10 text-amber-300/80 border-amber-500/20` (AdminCarteira.jsx:406)
- Select estilizado: classes do select de período (Carteiras.jsx:50) / select de mês (AdminCarteira.jsx:170)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carteiras.jsx — select contexto + contadores por card + banner agregado (e remoção do alias companies_count no controller)</name>
  <files>resources/js/Pages/Portfolio/Carteiras.jsx, app/Http/Controllers/PortfolioController.php, tests/Feature/V16/CarteirasConsolidadasContextoTest.php</files>
  <behavior>
    - Teste (ajuste na suite existente `CarteirasConsolidadasContextoTest`): adicionar 1 teste `test_payload_consolidada_nao_emite_companies_count` — card NÃO contém a chave `companies_count` (`assertArrayNotHasKey`), contém `empresas_unicas`. RED antes da remoção do alias, GREEN depois.
  </behavior>
  <action>
    **Frontend (`Carteiras.jsx`):**
    - Aceitar props novas `contexto = 'todos'` e `totais = null`.
    - Constante `CONTEXTO_OPTIONS = [{ value: 'todos', label: 'Todos' }, { value: 'performance', label: 'Mercado Livre' }, { value: 'shopee', label: 'Shopee' }]` — rótulos sem jargão (decisão travada), nunca slug cru.
    - Segundo `<select>` ao lado do de período (mesmas classes Tailwind), disparando `router.get(route('portfolio.own'), { period, contexto: novoValor }, { preserveState: true, preserveScroll: true })`. O handler do período passa a preservar o contexto atual também (`{ period: novoValor, contexto }`).
    - Banner agregado no topo (entre o header e o grid, ou inline no header): "**{totais.empresas_unicas}** empresa(s) única(s) · **{totais.vinculos_servico}** vínculo(s) de serviço" — estilo discreto (`text-white/50 text-xs` ou card fino `card-ecf`), renderizado só quando `totais` presente (defensivo). NÃO computar união no frontend — o backend já entrega correta.
    - Linha do card (linha 82): trocar `{u.companies_count} empresa{...}` por `{u.empresas_unicas} empresa{u.empresas_unicas !== 1 ? 's' : ''} · {u.vinculos_servico} vínculo{u.vinculos_servico !== 1 ? 's' : ''}`. Pluralização pt-BR correta.
    - Chip âmbar no card quando `u.vinculos_sem_fonte_financeira > 0`: `{u.vinculos_sem_fonte_financeira} vínculo(s) sem fonte financeira` — mesmas classes do chip âmbar do AdminCarteira (`bg-amber-500/10 text-amber-300/80 border-amber-500/20`, rounded-full, text-[10px]). Texto auto-explicativo, sem jargão.
    - PITFALL Rollup (memória do projeto): qualquer flag booleana usada dentro do `.map()` dos cards deve ser computada DENTRO do callback, nunca em variável de escopo do componente.
    - Empty state sob filtro: se `user_portfolios.length === 0` e `contexto !== 'todos'`, texto do empty state menciona o filtro ("Nenhum profissional com vínculos neste contexto. Troque o filtro para Todos.") — evita o bug reportado "cadê o card da Ana quando filtro Shopee?" (Open Question 1 do research).

    **Backend (mesmo commit — Pitfall 4):** remover o alias `'companies_count' => ...` do payload do card em `renderCarteirasConsolidadas()` (PortfolioController.php). Frontend e backend trocam a chave juntos — nenhum commit intermediário com card quebrado.
  </action>
  <verify>
    <automated>php artisan test --filter=CarteirasConsolidadasContextoTest && npm run build</automated>
  </verify>
  <done>Select de contexto e contadores renderizam a partir das props; `companies_count` não existe mais nem no payload nem no .jsx (grep zero em ambos); teste novo verde; build exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: AdminCarteira.jsx — select contexto no header + contadores no topo</name>
  <files>resources/js/Pages/Portfolio/AdminCarteira.jsx</files>
  <action>
    - Aceitar prop nova `contexto = 'todos'`.
    - Select de contexto no header, ao lado do select de mês existente (linhas 159-188), mesmas classes e mesmo padrão de label ("Contexto" em `text-white/40 text-[11px] uppercase`). Opções: Todos / Mercado Livre / Shopee (mesma `CONTEXTO_OPTIONS` — duplicar a constante local, sem criar módulo compartilhado pra 2 usos).
    - onChange: `router.visit(window.location.pathname + '?contexto=' + valor + '&mes=' + (periodo?.mes_selecionado ?? ''), { preserveScroll: false })` — PRESERVAR o `?mes=` ativo (decisão travada; mesmo pattern do select de mês, linha 167). Simetricamente, o select de MÊS existente passa a preservar `?contexto=` (incluir `&contexto=` na URL que ele monta).
    - Contadores no topo: na linha de subtítulo do header (linha 152, hoje "{cargo_label} · N empresas em carteira") evoluir para "{cargo_label} · {resumo.total_empresas} empresa(s) única(s) · {resumo.vinculos_servico} vínculo(s) de serviço"; quando `resumo.vinculos_sem_fonte_financeira > 0`, chip âmbar pequeno ao lado ("{n} sem fonte financeira") — mesmo tom do chip da tabela (linha 406). Renderização defensiva (`?? 0` / render condicional) pois props podem faltar em cache de página antiga.
    - Atualizar o docblock do topo do arquivo (linhas 46-48): a nota "filtro completo ... é Fase 90" sai — a fase chegou.
    - Comentários pt-BR.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Select de contexto funcional preservando ?mes=; contadores visíveis no topo; build exit 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual — as duas telas com filtro e contadores</name>
  <files>nenhum — verificação humana, sem mudança de código</files>
  <action>Apresentar ao usuário o roteiro de verificação abaixo (what-built + how-to-verify) e aguardar a aprovação explícita antes de encerrar a fase. Se o usuário relatar problema, corrigir e re-apresentar. NÃO fazer deploy sem confirmação (dev paralelo na milestone).</action>
  <what-built>Filtro de contexto (Todos / Mercado Livre / Shopee) + contadores empresas×vínculos nas telas de Carteiras (consolidada, admin) e AdminCarteira (individual). Backend refatorado no Plan 90-01 (cards sem herança de financeiro ML pra responsável só-Shopee).</what-built>
  <how-to-verify>
    Pré-requisito: `npm run build` já rodado (Tasks 1-2). Sem MariaDB local funcional (incidente conhecido) — validar no VPS após o deploy do pacote da fase, OU localmente com SQLite/seed se disponível.

    1. Acessar `/portfolio` logado como admin (aba Carteira) — visão consolidada:
       a. Topo mostra "N empresas únicas · M vínculos de serviço" (agregado).
       b. Cada card mostra "X empresas · Y vínculos" (não mais só "X empresas").
       c. Card de profissional com vínculo Shopee (ex.: Felipe) mostra chip âmbar "sem fonte financeira" e NÃO mostra faturamento ML de empresa gerida por outro.
       d. Select "Contexto" ao lado do de período; escolher "Shopee": cards sem vínculo Shopee DESAPARECEM (comportamento esperado, não é bug); nenhum card mostra R$ >0.
       e. Escolher "Mercado Livre": só cards com vínculo de Performance; valores financeiros voltam.
       f. Voltar para "Todos" — grid completa.
    2. Clicar "Ver" num profissional (AdminCarteira, individual):
       a. Header mostra select "Contexto" ao lado do select de mês + contadores "X empresas únicas · Y vínculos".
       b. Trocar contexto para "Shopee" e verificar que o `?mes=` selecionado se mantém na URL (e vice-versa: trocar o mês mantém o contexto).
       c. Com "Shopee" ativo: linhas só com badge Shopee, colunas financeiras em "—".
    3. Conferir que a legenda de fontes (badges ML/Adman) dos cards, se visível (flag unified_metrics_enabled), não conta empresas Shopee-only.
  </how-to-verify>
  <verify>
    <human-check>Usuário percorre os passos 1-3 do roteiro e confirma o comportamento nas duas telas</human-check>
  </verify>
  <done>Usuário respondeu "aprovado" (ou os problemas relatados foram corrigidos e a re-verificação foi aprovada).</done>
  <resume-signal>Digite "aprovado" ou descreva o problema encontrado</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→controller | `?contexto=` montado pela UI — validação já no backend (Plan 90-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-04 | Tampering (input) | selects de contexto (2 telas) | transfer | whitelist server-side do Plan 90-01 cobre qualquer valor manipulado no client |
| T-90-05 | Information Disclosure | chips/contadores nos cards | accept | dados já visíveis ao admin/líder no gate existente de `own()`; UI não expõe nada novo além do payload |
| T-90-SC | Tampering | npm/composer installs | accept | nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- `npm run build` — exit 0 (gate obrigatório, 2 .jsx mudam — convenção do CLAUDE.md)
- `php artisan test --filter=CarteirasConsolidadasContextoTest && php artisan test --filter=V16` — regressão zero
- `grep -rn "companies_count" resources/js/Pages/Portfolio/ app/Http/Controllers/PortfolioController.php` — zero matches (chave legada removida por completo)
- `git status` — nenhum arquivo fora da fronteira (NPS, User.php, DesempenhoScoreService, CompanyController, Company.php) tocado
- Checkpoint visual aprovado pelo usuário
</verification>

<success_criteria>
- SC3 completo: filtro de contexto funcional (UI + backend) nas DUAS telas
- SC4 completo: badges por linha (já existentes na individual) + contadores visíveis no topo das duas telas + contadores por card na consolidada
- Rótulos "Mercado Livre"/"Shopee"/"Todos" — zero jargão/slug cru na UI (regra sistêmica do projeto)
- NÃO deployar sem confirmação do usuário (dev paralelo)
</success_criteria>

<output>
Criar `.planning/phases/90-carteiras-consolidadas-rendercarteirasconsolidadas-v17-0/90-02-SUMMARY.md` ao concluir (pt-BR).
</output>
