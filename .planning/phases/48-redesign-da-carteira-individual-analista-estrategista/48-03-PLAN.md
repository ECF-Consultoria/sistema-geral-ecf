---
phase: 48-redesign-da-carteira-individual-analista-estrategista
plan: "03"
type: execute
wave: 3
depends_on: ["48-01", "48-02"]
autonomous: true
requirements:
  - REQ-48-02
must_haves:
  truths:
    - "status e acao_recomendada JA existem no payload de cada empresa (c.status, c.acao_recomendada) — dados chegam no frontend, foram removidos da tabela UI em hotfix 2026-06-19 (comentario linha 778 Show.jsx)"
    - "queda_mom_pct ja existe no payload de cada empresa (c.queda_mom_pct, linha 354 controller) — usar como base para cor do sparkline"
    - "Recharts ja esta no projeto — usar LineChart mini sem eixos para sparkline (nao adicionar react-sparklines)"
    - "Tabela atual (linhas 797-869 Show.jsx) tem 5 colunas: Empresa | Faturamento | Meta | Margem | Ads"
    - "Adicionar colunas: Status (badges) + Acao (texto) + Crescimento (sparkline) — totalizando 8 colunas"
    - "No mobile a tabela VIRA CARDS (responsividade obrigatoria)"
  artifacts:
    - path: "resources/js/Components/Carteira/SparklineCrescimento.jsx"
      provides: "Componente mini LineChart Recharts sem eixos, verde/vermelho/cinza por threshold"
    - path: "resources/js/Pages/Portfolio/Show.jsx"
      provides: "Tabela com colunas Status + Acao + coluna Crescimento (sparkline) + cards mobile"
  key_links:
    - from: "Show.jsx tabela linha 797-869"
      to: "coluna Status"
      via: "c.status + STATUS_LABEL + STATUS_CLS (ja existem no arquivo linha 33-43)"
      pattern: "Reintroduzir badge inline usando classes ja definidas"
    - from: "Show.jsx tabela"
      to: "coluna Crescimento"
      via: "SparklineCrescimento component com c.queda_mom_pct"
      pattern: "threshold ±2%: verde se >+2%, vermelho se <-2%, cinza entre"
---

<objective>
Adicionar tres colunas a tabela de empresas do Show.jsx: Status (badge colorida), Acao recomendada (texto), e Crescimento (mini sparkline Recharts verde/vermelho/cinza). Criar o componente externo SparklineCrescimento.jsx para o mini-grafico. Tambem tornar a tabela responsiva no mobile convertendo para cards em telas pequenas.

Este plan roda em PARALELO ao 48-02 (mesma Wave 2) — ambos partem do estado entregue pelo 48-01. O 48-02 foca em hero/KPIs; este foca na tabela e no novo componente de sparkline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-CONTEXT.md
@.planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-UI-BRIEF.md
@.planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-RESEARCH.md
@resources/js/Pages/Portfolio/Show.jsx
@resources/js/lib/utils.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Criar componente SparklineCrescimento.jsx</name>
  <files>resources/js/Components/Carteira/SparklineCrescimento.jsx</files>
  <read_first>
    resources/js/Pages/Portfolio/Show.jsx linhas 718-770 (pattern Recharts ja em uso no projeto)
    .planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-RESEARCH.md §8 item 6 (criterio de cor e queda_mom_pct)
  </read_first>
  <behavior>
    Criar componente funcional SparklineCrescimento que recebe:
    - queda_mom_pct: float|null — percentual de crescimento/queda vs periodo anterior
    - data: array opcional de pontos historicos [{date, value}] — se nao fornecido, renderiza apenas indicador de cor
    
    Logica de cor (threshold ±2%):
    - queda_mom_pct > +2%: verde (emerald-400, stroke="#34d399")
    - queda_mom_pct < -2%: vermelho (red-400, stroke="#f87171")
    - entre -2% e +2% (inclusive): cinza (white/40, stroke="rgba(255,255,255,0.4)")
    - null: cinza (sem dado)
    
    Quando data array fornecido (serie historica): renderizar mini LineChart Recharts (60x28px) sem eixos, sem grid, sem tooltip, sem legenda. Apenas a linha colorida.
    
    Quando sem data array: renderizar apenas um indicador textual compacto com a cor (ex: "+18.7%" em verde ou "-5.2%" em vermelho).
    
    A prop queda_mom_pct ja chega no payload de cada empresa (controller linha 354: "queda_mom_pct" => $quedaMomPct).
  </behavior>
  <action>
    1. Criar diretorio resources/js/Components/Carteira/ se nao existir
    2. Criar SparklineCrescimento.jsx com:
       - import { LineChart, Line, ResponsiveContainer } from "recharts"
       - funcao getColor(pct): retorna { stroke, textCls } por threshold
       - se data?.length > 1: renderizar mini LineChart 60x28 sem eixos
       - caso contrario: renderizar badge texto "pct >= 0 ? + : -" formatado em 1 decimal com cor correta
       - null: "-" em cinza
    3. Export default SparklineCrescimento
  </action>
  <verify>
    O componente importa sem erro no Show.jsx. Renderiza visualmente no browser.
  </verify>
  <done>SparklineCrescimento.jsx criado e exportando componente funcional. Sem erros de import.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Adicionar colunas Status, Acao e Crescimento na tabela desktop</name>
  <files>resources/js/Pages/Portfolio/Show.jsx</files>
  <read_first>
    resources/js/Pages/Portfolio/Show.jsx linhas 796-869 (tabela atual com 5 colunas)
    resources/js/Pages/Portfolio/Show.jsx linhas 33-43 (STATUS_LABEL e STATUS_CLS ja definidos)
  </read_first>
  <behavior>
    Na tabela existente (linhas 796-869):
    1. Adicionar 3 headers: "Status" (apos Empresa), "Acao" (apos Ads), "Crescimento" (ultima coluna).
    2. Adicionar celulas correspondentes em cada linha (tbody):
       - Status: badge com c.status usando STATUS_LABEL[c.status] e STATUS_CLS[c.status] (ja existem no arquivo) — mostrar so se c.status nao for null
       - Acao: texto compacto c.acao_recomendada, truncado em 120px, cor white/60 — mostrar "-" se null
       - Crescimento: <SparklineCrescimento queda_mom_pct={c.queda_mom_pct} />
    3. Ajustar colSpan do "Nenhuma empresa encontrada" para 8 (era 5).
    4. Importar SparklineCrescimento no topo do arquivo.
    
    Layout das colunas (8 total):
    Empresa | Status | Faturamento | Meta | Margem | Ads | Acao | Crescimento
  </behavior>
  <action>
    1. Ler Show.jsx linhas 798-870
    2. Adicionar import: import SparklineCrescimento from "@/Components/Carteira/SparklineCrescimento"
    3. No thead: inserir <th> para Status (apos Empresa), para Acao (apos Ads), para Crescimento (ultima)
    4. Em cada linha do tbody: inserir <td> de Status, Acao e Crescimento nas posicoes corretas
    5. Atualizar colSpan de 5 para 8 no empty state
    6. Verificar que STATUS_LABEL e STATUS_CLS estao disponiveis (linhas 33-43 — sim)
  </action>
  <verify>
    /portfolio no browser: tabela com colunas Status (badges coloridas), Acao (texto), Crescimento (indicador colorido). Sem erros JS.
  </verify>
  <done>Tabela com 8 colunas. Status e Acao exibidos. Sparkline/indicador de crescimento visivel por linha.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 3: Responsividade mobile — tabela vira cards</name>
  <files>resources/js/Pages/Portfolio/Show.jsx</files>
  <read_first>
    resources/js/Pages/Portfolio/Show.jsx linhas 773-870 (container da tabela)
    .planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-UI-BRIEF.md secao "Responsividade"
  </read_first>
  <behavior>
    Adicionar visualizacao alternativa em mobile (< md) onde cada empresa e um card empilhado:
    
    Card mobile por empresa:
    - Linha 1: nome da empresa (link) + badge Status + indicador Crescimento
    - Linha 2: Faturamento em destaque + Meta % 
    - Linha 3: Margem | Ads
    - Linha 4: Acao recomendada (texto completo)
    
    Implementacao: usar "hidden md:block" na tabela desktop e "block md:hidden" no container de cards mobile. Os cards usam o mesmo array empresasView.
    
    Nao pode haver overflow horizontal em mobile.
  </behavior>
  <action>
    1. Adicionar classe "hidden md:table" na <table> (ou wrapper "hidden md:block")
    2. Antes da tabela, adicionar bloco "md:hidden" com div.space-y-2 onde cada empresa e um Card mini:
       - Cada card: p-3, border border-white/[0.06], rounded-lg
       - Linha topo: Link nome + badge status + SparklineCrescimento
       - Linha meio: faturamento em destaque (formatCurrencyCompact) + meta%
       - Linha inferior: margem% | ads compact | acao (texto truncado)
    3. Nao repetir logica — usar o mesmo empresasView do estado
  </action>
  <verify>
    Em viewport 375px (mobile): tabela sumiu, cards aparecem empilhados. Sem scroll horizontal.
    Em viewport 1024px (desktop): tabela aparece, cards sumidos.
  </verify>
  <done>Tabela desktop "hidden md:table". Cards mobile "md:hidden". Responsividade sem overflow.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 4: Build e verificacao final</name>
  <files>resources/js/Pages/Portfolio/Show.jsx, resources/js/Components/Carteira/SparklineCrescimento.jsx</files>
  <read_first>N/A</read_first>
  <behavior>
    Rodar npm run build e verificar que o componente SparklineCrescimento importa corretamente, a tabela renderiza com as 3 novas colunas, e a responsividade mobile esta funcionando.
  </behavior>
  <action>
    1. npm run build
    2. Verificar build sem erros
    3. Abrir /portfolio no browser desktop: tabela com 8 colunas
    4. Simular mobile no DevTools: cards empilhados, sem overflow
  </action>
  <verify>
    Build OK. Desktop: tabela 8 colunas. Mobile: cards.
  </verify>
  <done>Build passa. Tabela responsiva funcional.</done>
</task>

</tasks>

<verification>
- [ ] SparklineCrescimento.jsx criado em resources/js/Components/Carteira/
- [ ] Componente usa threshold ±2% para verde/vermelho/cinza
- [ ] Tabela tem 8 colunas: Empresa | Status | Faturamento | Meta | Margem | Ads | Acao | Crescimento
- [ ] Status e Acao reintroduzidos (dados ja existiam no payload, apenas exibicao removida)
- [ ] Indicador de crescimento usa queda_mom_pct (ja no payload do controller)
- [ ] Mobile: tabela vira cards (hidden md:table / md:hidden)
- [ ] colSpan do empty state atualizado para 8
- [ ] npm run build sem erros
</verification>

<success_criteria>
- Tabela orientada a acao: profissional ve Status + Acao recomendada + indicador de crescimento em cada linha
- SparklineCrescimento.jsx reutilizavel e independente do Show.jsx
- Sem overflow horizontal no mobile
- Recharts reutilizado (sem adicionar nova biblioteca)
</success_criteria>

<output>
Criar `.planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-03-SUMMARY.md` ao final.
Plans 48-04 e 48-05 sao a proxima wave.
</output>