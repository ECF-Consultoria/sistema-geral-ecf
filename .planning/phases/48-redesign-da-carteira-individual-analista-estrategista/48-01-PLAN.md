---
phase: 48-redesign-da-carteira-individual-analista-estrategista
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-48-01
  - REQ-48-04
  - REQ-48-05
must_haves:
  truths:
    - "PortfolioController::renderPortfolio() linha 223 — alvo único de mudança backend"
    - "PortfolioScoreService Caminho A (linhas 165-178) removido; Caminho B (linhas 182-194) vira único caminho"
    - "Props meta_carteira e portfolio_goals SAEM do payload Inertia (linhas 820-826 e 809)"
    - "Tres novas props ENTRAM: nps_history, sugador_counters (so analista), ppa_counters (so estrategista)"
    - "revenue_timeseries.meta_acumulada passa a usar soma Goal.metric=revenue em vez de PortfolioGoal"
    - "MariaDB local corrompido — usar SQLite in-memory em todos os testes PHPUnit"
  artifacts:
    - path: "app/Services/PortfolioScoreService.php"
      provides: "Caminho B exclusivo na categoria atingimento_meta (linhas 165-195 refatoradas)"
    - path: "app/Http/Controllers/PortfolioController.php"
      provides: "renderPortfolio() com 3 novas props + meta_carteira_calculada + remocao de meta_carteira e portfolio_goals"
    - path: "tests/Feature/Portfolio/RenderPortfolioTest.php"
      provides: "Cobertura PHPUnit das mudancas backend"
  key_links:
    - from: "PortfolioController::renderPortfolio()"
      to: "Inertia prop nps_history"
      via: "NpsSurvey::whereIn(company_id, companyIds)->groupBy(month_reference)"
      pattern: "Reusar scoreField = isMentor() ? score_estrategista : score_analista (linha 258 PortfolioScoreService)"
    - from: "PortfolioController::renderPortfolio()"
      to: "Inertia prop sugador_counters"
      via: "Sugador::whereIn(company_id, consultorCompanyIds)->groupBy(status)"
      pattern: "Condicional: so quando cargoSlug === analista"
    - from: "PortfolioController::renderPortfolio()"
      to: "Inertia prop ppa_counters"
      via: "Ppa::where(mentor_id, user->id)"
      pattern: "Condicional: so quando cargoSlug === estrategista"
---

<objective>
Refatorar o backend da carteira individual para eliminar a dependencia de PortfolioGoal (meta agregada) e injetar tres novas props ao payload do Inertia: nps_history (historico mensal de NPS do profissional), sugador_counters (counters de sugadores da carteira — apenas para analistas) e ppa_counters (PPAs por mentor — apenas para estrategistas). Tambem remover Caminho A do PortfolioScoreService e recalcular meta_carteira_calculada via soma das metas individuais por empresa.

Esta e a Wave 1 — base que as Waves 2 e 3 consumirao. Nenhuma mudanca visual e feita aqui. Ao final, o frontend continua renderizando normalmente (props removidas sao opcionais no Show.jsx e serao limpas nas waves seguintes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-CONTEXT.md
@.planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-RESEARCH.md
@app/Http/Controllers/PortfolioController.php
@app/Services/PortfolioScoreService.php
@app/Models/Sugador.php
@app/Models/Ppa.php
@app/Models/NpsSurvey.php
@app/Models/NpsResponse.php
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Remover Caminho A do PortfolioScoreService</name>
  <files>app/Services/PortfolioScoreService.php</files>
  <read_first>app/Services/PortfolioScoreService.php linhas 158-199 (bloco "atingimento da meta da carteira")</read_first>
  <behavior>
    Deletar o bloco if ($metaModel?->target_value !== null) (linhas 176-180) inteiro, mantendo apenas o conteudo do else (linhas 181-194) como caminho unico. Tambem remover a query $metaModel (linhas 165-169). Atualizar comentario da secao para refletir que Caminho B e o unico.
  </behavior>
  <action>
    1. Ler PortfolioScoreService.php linhas 155-200
    2. Deletar linhas 165-180 (query PortfolioGoal + Caminho A)
    3. Remover o else { e } de fechamento — deixar apenas o corpo inline
    4. Atualizar comentario: "Phase 48 — soma das metas individuais ativas (Goal.metric=revenue). PortfolioGoal revenue descontinuado."
    5. Garantir que $goalsIndividuais, $metaTarget, $metaRealizado, $metaOrigem continuam definidos
  </action>
  <verify>
    grep "metaOrigem.*portfolio" app/Services/PortfolioScoreService.php — deve retornar vazio
    php artisan test --filter PortfolioScore (se existir suite)
  </verify>
  <done>PortfolioScoreService.php nao contem mais referencia a PortfolioGoal na funcao compute(). metaOrigem so retorna "empresas:N" ou null.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Recalcular meta_carteira_calculada via Goals individuais no Controller</name>
  <files>app/Http/Controllers/PortfolioController.php</files>
  <read_first>
    app/Http/Controllers/PortfolioController.php linhas 596-637 (bloco meta_carteira + revenue_timeseries)
    app/Http/Controllers/PortfolioController.php linhas 806-834 (payload Inertia completo)
  </read_first>
  <behavior>
    Substituir consulta PortfolioGoal do calculo de $metaCarteiraTarget (linhas 597-611) por: Goal::where("active", true)->where("metric", "revenue")->whereIn("company_id", $companyIds)->sum("target_value"). Remover do payload: meta_carteira e portfolio_goals. Adicionar: meta_carteira_calculada com {target_value, realized_value, achieved_pct, restante, has_goal}. Remover tambem a query de $portfolioGoals (linhas 368-391).
  </behavior>
  <action>
    1. Ler linhas 596-640 e 806-834 do controller
    2. Substituir bloco PortfolioGoal revenue (linhas 597-611) por query Goal::sum
    3. No payload Inertia: remover "meta_carteira" e "portfolio_goals"
    4. No payload Inertia: adicionar "meta_carteira_calculada" => [target_value, realized_value, achieved_pct, restante, has_goal]
    5. Remover query $portfolioGoals (linhas 368-391) — nao e mais necessaria no payload individual
    6. Verificar que $companyIds esta disponivel no ponto da nova query (linha 394 — sim)
  </action>
  <verify>
    /portfolio carrega sem erro 500. DevTools Network: pageProps contem "meta_carteira_calculada" e NAO contem "meta_carteira" nem "portfolio_goals".
  </verify>
  <done>Payload Inertia contem meta_carteira_calculada e NAO contem meta_carteira nem portfolio_goals.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 3: Adicionar prop nps_history ao payload</name>
  <files>app/Http/Controllers/PortfolioController.php</files>
  <read_first>
    app/Services/PortfolioScoreService.php linhas 253-261 (pattern de query NPS por User)
    .planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-RESEARCH.md §3 (pipeline proposto)
    app/Models/NpsSurvey.php (relacoes disponiveis)
  </read_first>
  <behavior>
    Adicionar query de historico NPS agrupado por mes ao renderPortfolio(), apos $comparacaoContextual. Usar isMentor() ? "score_estrategista" : "score_analista" (mesmo criterio do PortfolioScoreService linha 258). Formato: [{month: "YYYY-MM", avg: float|null, count: int, ultima_nota: int|null}] ordenado por mes crescente.
  </behavior>
  <action>
    1. Inserir bloco apos calculo de $comparacaoContextual e antes do return Inertia::render:
       $npsScoreField = $user->isMentor() ? "score_estrategista" : "score_analista";
       Query NpsSurvey::with("response")->whereIn("company_id", $companyIdsAll)->where("status", "completed")->whereNotNull("month_reference")->orderBy("month_reference")->get()
       groupBy por mes, map para {month, avg, count, ultima_nota}
    2. Verificar import NpsSurvey no controller (adicionar use App\Models\NpsSurvey; se ausente)
    3. Adicionar "nps_history" => $npsHistory no payload Inertia
  </action>
  <verify>
    DevTools: pageProps.nps_history e um array (pode ser vazio). Sem erros 500.
  </verify>
  <done>Prop nps_history presente no payload com estrutura [{month, avg, count, ultima_nota}]. Array vazio e valido.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 4: Adicionar props sugador_counters e ppa_counters ao payload</name>
  <files>app/Http/Controllers/PortfolioController.php</files>
  <read_first>
    .planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-RESEARCH.md §4 e §5
    app/Models/Sugador.php linhas 98-113 (constantes de status)
    app/Models/Ppa.php (campos mentor_id, completed_at)
    app/Http/Controllers/PortfolioController.php linhas 708-712 (cargoSlug ja derivado)
  </read_first>
  <behavior>
    Usando $cargoSlug ja derivado (linha 708-712):
    - Se "analista": calcular $sugadorCounters via Sugador queries em consultorCompanies do user. Mapeamento: resolvidos=[resolvido,movido,auto_resolvido]; pendentes=[pendente,em_acao]; nao_resolvidos=[ignorado].
    - Se "estrategista": calcular $ppaCounters via Ppa::where("mentor_id", $user->id). Campos: concluidos_mes, em_andamento, total.
    - Outros: ambos null.
    Injetar no payload: sugador_counters, ppa_counters, cargo_slug.
  </behavior>
  <action>
    1. Inserir bloco apos nps_history (Tarefa 3) e antes do return
    2. Verificar que User::consultorCompanies() existe (linha 166-170 User.php)
    3. Adicionar use App\Models\Sugador; e use App\Models\Ppa; se ausentes
    4. Adicionar "sugador_counters", "ppa_counters", "cargo_slug" no payload Inertia
  </action>
  <verify>
    Para user analista: sugador_counters nao null, ppa_counters null.
    Para user estrategista: ppa_counters nao null, sugador_counters null.
    Para admin: ambos null, cargo_slug null.
  </verify>
  <done>Payload contem sugador_counters (array|null), ppa_counters (array|null), cargo_slug (string|null).</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 5: Testes PHPUnit Feature — validar mudancas backend</name>
  <files>tests/Feature/Portfolio/RenderPortfolioTest.php</files>
  <read_first>
    tests/ (verificar se existe tests/Feature/Portfolio/)
    phpunit.xml (confirmar config SQLite in-memory)
  </read_first>
  <behavior>
    Criar teste PHPUnit Feature com SQLite in-memory. Cobrir: (a) meta_carteira ausente do payload; (b) meta_carteira_calculada presente com has_goal=false sem Goals; (c) achieved_pct correto com Goals; (d) sugador_counters para analista; (e) ppa_counters para estrategista; (f) nps_history presente; (g) PortfolioScoreService nao retorna metaOrigem=portfolio.
  </behavior>
  <action>
    1. Criar tests/Feature/Portfolio/ se nao existir
    2. Criar RenderPortfolioTest.php com RefreshDatabase + SQLite
    3. setUp: User analista + User estrategista + Company + company_users + Goals de revenue + Sugadores + Ppas
    4. Testes: actingAs($user)->get(route("portfolio.own"))->assertInertia(...)
    5. Rodar: php artisan test tests/Feature/Portfolio/RenderPortfolioTest.php
  </action>
  <verify>
    php artisan test tests/Feature/Portfolio/RenderPortfolioTest.php — todos verde
  </verify>
  <done>Todos os testes passando. Nenhuma regressao: php artisan test --filter Portfolio</done>
</task>

</tasks>

<verification>
- [ ] PortfolioScoreService.php nao contem mais bloco Caminho A (metaOrigem = "portfolio")
- [ ] renderPortfolio() payload NAO contem meta_carteira nem portfolio_goals
- [ ] renderPortfolio() payload CONTEM meta_carteira_calculada, nps_history, sugador_counters, ppa_counters, cargo_slug
- [ ] revenue_timeseries[*].meta_acumulada calculada via Goals individuais somados (nao via PortfolioGoal)
- [ ] Testes PHPUnit passando (SQLite in-memory)
- [ ] /portfolio carrega sem erro 500 no browser
- [ ] Nenhum import ausente no controller (NpsSurvey, Sugador, Ppa, Goal)
</verification>

<success_criteria>
- Backend refatorado: meta agregada (PortfolioGoal) removida do fluxo de pontuacao e do payload
- 3 novas props injetadas e tipadas corretamente no payload Inertia
- Testes cobrindo casos analista, estrategista, e ausencia de PortfolioGoal
- Pagina /portfolio nao quebra — frontend renderiza com degradacao graciosamente (props novas ignoradas ate Wave 2/3)
</success_criteria>

<output>
Criar `.planning/phases/48-redesign-da-carteira-individual-analista-estrategista/48-01-SUMMARY.md` ao final.
Apos este plan: executar 48-02 e 48-03 em paralelo.
</output>