---
phase: 132-cutover-sandbox-produ-o-checkpoint-humano-v22-0
plan: 03
type: execute
wave: 3
depends_on: ["132-02"]
files_modified:
  - .planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md
autonomous: false
# Fase SEM REQ-ID (assim está no ROADMAP) — rastreio pelos Success Criteria da fase.
requirements: [SC-132-2, SC-132-3, D-03, D-05, D-06]

must_haves:
  truths:
    - "A URL do aviso automático está cadastrada na conta de PRODUÇÃO da Clicksign — visto na tela, não deduzido"
    - "O primeiro contrato de produção foi emitido contra uma empresa fictícia claramente marcada, nunca contra um cliente real"
    - "O aviso automático de produção chegou e foi processado — provado por linhas na tabela `contrato_assinatura_eventos`, com assinatura conferida"
    - "Se nenhum evento aparecer no banco depois de ativar o contrato, a virada é revertida — e isso está registrado como decisão, não como esquecimento"
  artifacts:
    - path: ".planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md"
      provides: "SC2 e SC3 preenchidos, com os ids da empresa/contrato/envelope e o que o banco devolveu"
      contains: "SC3"
  key_links:
    - from: "painel de produção da Clicksign"
      to: "https://admin.ecfconsultoria.com.br/api/webhooks/clicksign"
      via: "cadastro manual do webhook com o mesmo segredo do `.env`"
      pattern: "api/webhooks/clicksign"
    - from: "ClicksignWebhookController::receive()"
      to: "contrato_assinatura_eventos"
      via: "gravação do evento com signature_valid + enfileiramento do processamento"
      pattern: "contrato_assinatura_eventos"
---

<objective>
Provar que a virada funciona de ponta a ponta usando um contrato **de mentira**: cadastrar o aviso
automático na conta de produção (SC2), criar uma empresa fictícia claramente marcada, emitir o
primeiro envelope de produção, ativá-lo, e conferir **no banco** que o aviso chegou e foi
processado (SC3).

Propósito: até aqui o sistema só está *configurado* para produção. Configuração certa e fluxo
funcionando são coisas diferentes — a Fase 130 provou isso da pior maneira, ficando bloqueada
porque o ambiente de teste não conseguia concluir uma assinatura.

Saída: seções SC2 e SC3 do `132-GATE.md` preenchidas, com os ids da empresa, do contrato e do
envelope, e com o que a tabela `contrato_assinatura_eventos` devolveu em cada passo.

⚠️ **A partir deste plano as ações têm consequência real:** e-mail de verdade chega em caixas de
entrada de verdade, e **a API da Clicksign não cancela envelope em andamento** (medido — §15.2).
Cada task diz explicitamente o que é irreversível.

⚠️ **Nenhuma task deste plano escreve código.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-CONTEXT.md
@.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md
@.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-GATE.md
@.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md
@CLAUDE.md

<interfaces>
**A URL que precisa ser cadastrada no painel de produção** (`routes/web.php:94`):
`POST https://admin.ecfconsultoria.com.br/api/webhooks/clicksign` → `ClicksignWebhookController@receive`

**O que o receiver faz** (`app/Http/Controllers/Api/ClicksignWebhookController.php`), na ordem:
1. valida o header `Content-Hmac` contra `CLICKSIGN_WEBHOOK_SECRET`; se não confere → grava mesmo
   assim com `signature_valid = 0` e responde **401**;
2. grava `ContratoAssinaturaEvento` **sempre**, com `payload_hash` único (evento repetido não
   duplica);
3. enfileira `ProcessarEventoClicksignJob` e responde 200.
Consequência prática: se o segredo cadastrado no painel for diferente do que está no `.env`, os
eventos **aparecem no banco com `signature_valid = 0` e nunca são processados**. Por isso a
conferência do SC3 lê essa coluna, e não só a contagem de linhas.

**Colunas de `contrato_assinatura_eventos`** que a conferência usa: `contrato_assinatura_id`,
`clicksign_envelope_id`, `name` (o nome do evento), `signature_valid`, `status`
(`recebido`/`processado`/`ignorado`/`erro`), `processado_em`, `erro_msg`.

**Como o contrato nasce e por que ele NÃO sai sozinho** (`app/Jobs/GerarContratoAssinaturaJob.php`,
docblock, item 7): o sistema monta o envelope e **para no rascunho** (`ativar: false`, D-02 da
Fase 127). Rascunho é inerte: não dispara aviso, não é assinável (medido — §12.3). **Ativar é um
passo à parte.**

⚠️ **Não existe caminho no app para ATIVAR o envelope** — nenhuma tela, nenhum comando. Só
`ClicksignClient::ativarEnvelope()`. E a rodada da Fase 130 mediu que **a tela da Clicksign não
consegue ativar envelope gerado por modelo** (fica preso em "Enviando documento…" e o botão Enviar
nunca sai do cinza); pela API funcionou na hora. Logo: ativar aqui é chamada de API via `tinker`,
não clique no painel.

**Limite de 1 envelope por minuto, global** (§16): depois de clicar em "Gerar contrato", o envelope
pode demorar até cerca de um minuto para existir — e a tela mostra sucesso antes disso. Conferir
por reconsulta ao campo `clicksign_envelope_id`, nunca pela mensagem da tela.

**Rascunho expira em 7 dias** (§11.2): entre gerar e ativar não pode passar uma semana.

**Prazo do envelope:** 30 dias por padrão (`CLICKSIGN_PRAZO_DIAS`), com lembrete a cada 3 dias
enviado pela própria Clicksign.

**A tela do Administrativo** (Fase 131): `/administrativo/contratos` lista as empresas;
`/administrativo/contratos/empresa/{id}` é onde se completa o cadastro e se clica em gerar. Acesso
por permissão `admin.contratos`.

**Serviço recomendado para a empresa fictícia: Gestão** (setor `performance`). Motivo medido:
`EmpresaOperacionalRouter::criarFicha()` só cria ficha operacional para `polos`, `assessoria` e
`incubadora` — com Gestão, liberar a empresa **não cria lixo adicional** em `mlb_empresas`
(observado no SC3 da Fase 130). Polos, além disso, é isento de contrato (D9) e não serviria.
</interfaces>
</context>

<disciplina_desta_fase>
1. **Conferir por reconsulta ao banco depois de cada passo (D-05)** — nunca pela tela, nunca pela
   mensagem de sucesso. A tela do Administrativo diz "Contrato gerado" antes de o envelope existir.
2. **Critério de parar (D-06):** se, depois de ativar o contrato, **nenhum evento aparecer em
   `contrato_assinatura_eventos`**, executar o procedimento de voltar atrás da seção 5 do
   `132-GATE.md` e registrar. É o único item que sozinho invalida a virada.
3. **Nada de cliente real.** Empresa fictícia, marcada no nome, com e-mails da própria casa.
4. **Nenhum valor de credencial** em arquivo, log ou chat.
</disciplina_desta_fase>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: cadastrar o aviso automático na conta de PRODUÇÃO (SC2)</name>
  <files>.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md</files>
  <read_first>
    - `app/Http/Controllers/Api/ClicksignWebhookController.php`, docblock e método `receive()` — em especial a matriz de status: assinatura inválida grava o evento e responde 401
    - `routes/web.php` linhas 85-95 — a rota e o comentário com a URL de produção
    - `.planning/phases/129-webhook-clicksign-v22-0/129-GATE.md` — como o receiver foi provado ao vivo pela internet na Fase 129 (recusa, aceita e não duplica)
    - `.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md`, seção 3 — por que o cadastro vem ANTES do envelope
  </read_first>
  <action>
Preencher a seção "SC2" do `132-GATE.md` e PAUSAR. Nenhum arquivo de código é editado.

⚠️ **A ordem importa:** o aviso automático precisa estar cadastrado **antes** do primeiro envelope.
Se o envelope for criado primeiro, os eventos da ativação não têm para onde ir e não dá para saber
se chegariam — a medição do SC3 fica sem valor.
  </action>
  <what-built>
A rota `POST /api/webhooks/clicksign` existe desde a Fase 129 e já foi provada ao vivo pela
internet: recusa quem manda assinatura errada (401), aceita quem manda certo (200) e não duplica
evento repetido. O que falta é a Clicksign de **produção** saber que essa URL existe — hoje ela
está cadastrada apenas na conta de teste.
  </what-built>
  <how-to-verify>
1. Entrar no painel da Clicksign de **produção** (`https://app.clicksign.com`) — conferir na barra
   de endereço que **não** é `sandbox.clicksign.com`.
2. Ir na configuração de webhooks da conta e cadastrar a URL exata:
   `https://admin.ecfconsultoria.com.br/api/webhooks/clicksign`
3. No campo de segredo/chave do webhook, usar **o mesmo valor** que está em
   `CLICKSIGN_WEBHOOK_SECRET` no `.env` de produção. ⚠️ Se os dois forem diferentes, os avisos
   chegam mas são **recusados**: aparecem no banco com `signature_valid = 0` e nunca são
   processados — o contrato ficaria preso sem ninguém entender por quê.
4. Salvar e **conferir com os próprios olhos** que a URL aparece cadastrada e ativa na lista de
   webhooks da conta. Este é o "confirmado, não assumido" do SC2.
5. Registrar no `132-GATE.md`: que a URL está cadastrada na conta de produção (dizer explicitamente
   "conta de produção", com a data/hora e quem cadastrou), quais eventos foram marcados (se o
   painel permitir escolher, marcar todos os relacionados a assinatura e a fechamento do documento)
   e que o segredo usado é o mesmo do `.env` (registrar `confere`, **nunca o valor**).
  </how-to-verify>
  <verify>
    <human-check>O usuário confirma, olhando a tela de webhooks da conta de PRODUÇÃO, que a URL está lá e ativa.</human-check>
    <automated>MISSING — não existe conferência automatizada do painel de um terceiro. A prova empírica vem na Task 3, quando os eventos reais aparecerem em `contrato_assinatura_eventos` com `signature_valid = 1`.</automated>
  </verify>
  <acceptance_criteria>
    - `132-GATE.md` seção SC2 afirma, com data/hora e autor, que a URL foi vista cadastrada e ativa na conta de **produção** (não na de teste)
    - A URL registrada é exatamente `https://admin.ecfconsultoria.com.br/api/webhooks/clicksign`
    - O registro diz que o segredo cadastrado é o mesmo do `.env` (`confere`), sem citar o valor
    - Se o painel permitir escolher eventos, o registro lista quais foram marcados
  </acceptance_criteria>
  <done>O aviso automático está cadastrado na conta de produção, conferido visualmente e registrado — e a prova empírica virá na Task 3.</done>
  <resume-signal>Responder "webhook cadastrado em produção" ou descrever o que a tela não permitiu fazer.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: empresa fictícia e primeiro envelope de produção — ainda sem enviar nada (D-03)</name>
  <files>.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md</files>
  <read_first>
    - `app/Jobs/GerarContratoAssinaturaJob.php`, docblock inteiro — em especial os itens 1 (guard de reentrega), 2 (guard de modelo) e 7 (`ativar: false`: o envelope PARA no rascunho)
    - `app/Services/Clicksign/ContratoDadosMinimosService.php` — `faltantes()` e `faltantesDaConfiguracaoEcf()`, os dois conjuntos de pendência que bloqueiam a geração
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` §16 — o limite de 1 envelope/min e a armadilha do job liberado de volta para a fila
    - `.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-GATE.md`, seção "Fixtures de teste criadas" — como a Fase 130 marcou dados fictícios para nunca serem confundidos com reais
  </read_first>
  <action>
Preencher a seção "SC3 — parte 1 (envelope criado)" do `132-GATE.md` e PAUSAR. Registrar também, na
seção 7 ("Lixo que fica para limpar depois"), o id da empresa e o id do contrato criados.

⚠️ **Nada é enviado nesta task.** O envelope nasce em rascunho e rascunho é inerte: não dispara
aviso, não manda e-mail, não é assinável. O envio acontece na Task 3, e é lá que a ação passa a ser
irreversível.
  </action>
  <what-built>
O fluxo das Fases 127 e 131: o Administrativo abre a empresa, completa o cadastro, clica em
"Gerar contrato", e o sistema monta o envelope na Clicksign a partir de um modelo `.docx`,
**parando no rascunho** de propósito. Quem envia de fato é um segundo passo, humano.
  </what-built>
  <how-to-verify>
1. **Criar a empresa fictícia em produção**, com nome que ninguém confunda com cliente —
   sugestão: `TESTE CUTOVER 132 — NAO E CLIENTE`. CNPJ fictício. Nome de contato **sem dígitos e
   sem parênteses** (medido: a Clicksign recusa com 400 no ponteiro do nome).
2. **E-mail do "cliente" da empresa fictícia:** usar um endereço real da casa que alguém consiga
   abrir, e que seja **DIFERENTE** dos três signatários da ECF (`thiago@`, `emerson@`,
   `comercial@`). ⚠️ Na rodada da Fase 130, apontar todos os signatários para o mesmo e-mail
   travou a tela de assinatura ("Estamos processando o(s) documento(s)" para sempre) — quatro
   endereços distintos evitam repetir isso.
3. **Avisar as quatro pessoas antes**: a partir da Task 3 elas recebem e-mail de verdade da
   Clicksign pedindo assinatura. Deixar combinado quem assina primeiro (a Task 3 depende disso).
4. **Atribuir à empresa um contrato de serviço de Gestão** (setor `performance`) — ver o motivo em
   `<interfaces>`: minimiza o lixo que fica em produção.
5. Abrir `/administrativo/contratos/empresa/{id}` e completar o cadastro até **não sobrar
   pendência**. Conferir por reconsulta, não pela tela:
   `php artisan tinker --execute="\$c=App\Models\Company::find(ID); \$d=app(App\Services\Clicksign\ContratoDadosMinimosService::class); print_r(\$d->faltantes(\$c)); print_r(\$d->faltantesDaConfiguracaoEcf());"`
   Esperado: os dois arrays **vazios**.
6. Clicar em **Gerar contrato**.
7. **Esperar até um minuto e conferir por reconsulta** (o limite é de 1 envelope por minuto,
   global, e a tela diz sucesso antes de o envelope existir):
   `php artisan tinker --execute="\$c=App\Models\ContratoAssinatura::where('company_id',ID)->latest()->first(); echo 'contrato=', \$c->id, ' status=', \$c->status, ' envelope=', var_export(filled(\$c->clicksign_envelope_id), true), ' criado=', \$c->created_at, ' atualizado=', \$c->updated_at;"`
   Esperado: status `rascunho` e envelope **preenchido**.
   ⚠️ Se `created_at` e `updated_at` forem idênticos e o envelope estiver vazio, o job foi barrado
   pelo limite — esperar mais um minuto e reconsultar. Se continuar vazio, conferir `failed_jobs` e
   o log `ecf-webhooks` antes de tentar de novo (gerar duas vezes não duplica: existe guard de
   reentrega, mas o diagnóstico vem primeiro).
8. Registrar no `132-GATE.md`: id da empresa, id do contrato, se o envelope foi criado, quanto
   tempo levou, e o id do envelope **truncado aos 8 primeiros caracteres** (é rastreabilidade, não
   credencial — o valor inteiro fica só no banco).
  </how-to-verify>
  <verify>
    <automated>No VPS: `php artisan tinker --execute="\$c=App\Models\ContratoAssinatura::where('company_id',ID)->latest()->first(); echo \$c->status, '|', var_export(filled(\$c->clicksign_envelope_id), true);"` → imprime `rascunho|true`</automated>
    <human-check>O usuário confirma que a empresa está claramente marcada como fictícia e que as quatro pessoas foram avisadas.</human-check>
  </verify>
  <acceptance_criteria>
    - `ContratoDadosMinimosService::faltantes()` e `faltantesDaConfiguracaoEcf()` reconsultados devolvem arrays vazios ANTES da geração
    - Reconsulta ao banco mostra o contrato em `rascunho` com `clicksign_envelope_id` preenchido
    - Nenhum e-mail foi enviado ainda (rascunho é inerte) — e o `132-GATE.md` diz isso explicitamente
    - O nome da empresa contém a marcação de teste e o CNPJ é fictício
    - Os quatro e-mails de signatário são **distintos** entre si
    - `132-GATE.md` seção 7 registra o id da empresa e o id do contrato para limpeza futura
  </acceptance_criteria>
  <done>Existe um envelope de produção em rascunho, para uma empresa fictícia marcada, e nada foi enviado a ninguém ainda.</done>
  <resume-signal>Responder "envelope criado" com o id do contrato e o id da empresa, ou descrever o que travou.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: enviar de verdade e provar que o aviso automático chegou (SC3 + critério de parar da D-06)</name>
  <files>.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md</files>
  <read_first>
    - `app/Services/Clicksign/ClicksignClient.php`, método `ativarEnvelope()` (~linha 303) — a assinatura do método e o que ele manda (prazo e lembrete)
    - `app/Http/Controllers/Api/ClicksignWebhookController.php` — a matriz de status e o que significa `signature_valid = 0`
    - `app/Models/ContratoAssinaturaEvento.php` — as colunas e os quatro valores de `status`
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` §12.6 — a ativação dispara uma **rajada de eventos retroativos** (os `add_signer` e o `update_deadline` só aparecem no instante em que o envelope é ativado)
    - `.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-GATE.md`, achados 1 e 2 — por que ativar pela tela não funciona para envelope gerado por modelo, e por que o ambiente de teste não mandava e-mail
  </read_first>
  <action>
Preencher a seção "SC3 — parte 2 (o aviso chegou)" do `132-GATE.md` e PAUSAR.

⚠️ **A partir do passo 1 abaixo a ação é IRREVERSÍVEL**: envelope ativado dispara e-mail real e
**a API não cancela envelope em andamento** (medido — §15.2). Cancelar, se for preciso, é operação
de painel, registrada pela tela do Administrativo.

⚠️ **Ainda NÃO concluir todas as assinaturas.** Esta task quer só a primeira. A conclusão acontece
no plano 132-04, com o aviso automático propositalmente desligado, para provar a rede de segurança.
  </action>
  <what-built>
O envelope existe em rascunho. Ativar é o que o transforma em documento vivo: a Clicksign passa a
mandar e-mail para cada signatário e a avisar o nosso sistema a cada acontecimento. Do nosso lado,
o receiver da Fase 129 confere a assinatura do aviso, grava o evento e enfileira o processamento.
  </what-built>
  <how-to-verify>
1. **Ativar o envelope pela API** (não pela tela — a tela não ativa envelope gerado por modelo,
   medido na Fase 130), no VPS:
   `php artisan tinker --execute="\$c=App\Models\ContratoAssinatura::find(ID_DO_CONTRATO); \$r=app(App\Services\Clicksign\ClicksignClient::class)->ativarEnvelope(\$c->clicksign_envelope_id); echo \$r['status'] ?? '?';"`
   Esperado: o envelope sai de rascunho e passa a estar em andamento.
2. **Esperar de 1 a 2 minutos** (a rajada de eventos retroativos chega junto com a ativação) e
   **conferir por reconsulta ao banco** — este é o passo que decide se a virada continua:
   `php artisan tinker --execute="\$e=App\Models\ContratoAssinaturaEvento::where('contrato_assinatura_id',ID_DO_CONTRATO)->orderBy('id')->get(['id','name','signature_valid','status','processado_em','erro_msg']); echo 'total=', \$e->count(), PHP_EOL; foreach(\$e as \$x){ echo \$x->id,' ',\$x->name,' valid=',(int)\$x->signature_valid,' ',\$x->status,' ',\$x->processado_em,' ',\$x->erro_msg,PHP_EOL; }"`
   Esperado: **pelo menos uma linha**, com `signature_valid = 1` e `status` = `processado` (ou
   `ignorado`, para eventos que o sistema legitimamente não usa — `ignorado` também conta como
   processado corretamente; `erro` não conta).
3. 🚦 **PONTO DE PARADA (D-06).** Se `total = 0`:
   - **parar aqui**;
   - executar o procedimento de voltar atrás da seção 5 do `132-GATE.md` (restaurar as linhas do
     `.env`, `config:clear && config:cache`, reiniciar os workers, **e abrir o painel da Clicksign
     para conferir se algum envelope saiu depois da reversão**);
   - registrar no `132-GATE.md` que a virada foi revertida e por quê.
   Se `total > 0` mas todas as linhas tiverem `signature_valid = 0`: o segredo cadastrado no painel
   **não** é o mesmo do `.env`. Corrigir no painel (Task 1) e repetir o passo 2 — não é caso de
   reverter, é caso de corrigir.
4. **Primeira assinatura.** Pedir a UMA das quatro pessoas que abra o e-mail da Clicksign e assine.
   ⚠️ Em produção o e-mail chega de verdade — foi justamente isso que faltou no ambiente de teste e
   bloqueou a Fase 130.
5. **Conferir de novo por reconsulta** (mesmo comando do passo 2): deve haver linha(s) nova(s),
   com `signature_valid = 1` e `status` processado. É a prova de que o canal funciona de forma
   contínua, e não só na rajada de ativação.
6. Conferir também que o contrato mudou de estado como esperado:
   `php artisan tinker --execute="\$c=App\Models\ContratoAssinatura::find(ID_DO_CONTRATO)->fresh(); echo \$c->status,' enviado_em=',var_export(\$c->enviado_em,true),' liberado_em=',var_export(\$c->liberado_em,true);"`
   Esperado: `aguardando_assinaturas`, `enviado_em` preenchido, `liberado_em` ainda **vazio**
   (só a assinatura de todos libera).
7. Registrar no `132-GATE.md`: total de eventos, os nomes dos eventos recebidos, quantos com
   `signature_valid = 1`, quantos `processado`, quantos `erro` (com a mensagem, se houver), e o
   estado do contrato depois da primeira assinatura. Registrar também **quem assinou primeiro** e a
   que horas — o plano 132-04 depende de saber quem ainda falta.
  </how-to-verify>
  <verify>
    <automated>No VPS: `php artisan tinker --execute="echo App\Models\ContratoAssinaturaEvento::where('contrato_assinatura_id',ID)->where('signature_valid',1)->count();"` → imprime número maior que 0</automated>
    <human-check>O usuário confirma que recebeu o e-mail da Clicksign de PRODUÇÃO e conseguiu assinar.</human-check>
  </verify>
  <acceptance_criteria>
    - Reconsulta a `contrato_assinatura_eventos` devolve pelo menos 1 linha para o contrato, com `signature_valid = 1` — o aviso automático de produção **chegou**
    - Pelo menos uma dessas linhas está com `status` = `processado` (ou `ignorado`), e **nenhuma** com `status` = `erro` sem explicação registrada
    - Depois da primeira assinatura, surgiram linhas novas — o canal funciona continuamente, não só na ativação
    - Reconsulta ao contrato mostra `aguardando_assinaturas` com `enviado_em` preenchido e `liberado_em` vazio
    - O e-mail da Clicksign chegou numa caixa de entrada real (confirmado por quem assinou)
    - Se `total = 0`, o `132-GATE.md` registra a reversão executada e a conferência do painel após reverter — e a fase para aqui
    - `132-GATE.md` registra quem assinou primeiro e quem ainda falta
  </acceptance_criteria>
  <done>O primeiro envelope de produção foi enviado a uma empresa fictícia, o aviso automático chegou e foi processado, e isso está provado por reconsulta ao banco — não pela tela.</done>
  <resume-signal>Responder "aviso chegou" com o total de eventos e quantos com assinatura válida; ou "aviso NÃO chegou", que aciona a reversão da D-02.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clicksign de produção → `POST /api/webhooks/clicksign` | Entrada pública, não autenticada por sessão. Quem sabe a URL pode tentar forjar uma liberação de empresa. |
| Sistema → caixas de entrada reais | A partir da ativação, pessoas de verdade recebem pedido de assinatura de um documento com valor jurídico. |
| Operador → API da Clicksign | Ativar envelope é irreversível: a API não cancela envelope em andamento. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-20 | Spoofing | `ClicksignWebhookController::receive()` | mitigate | Já mitigado na Fase 129 (validação de `Content-Hmac` com fórmula medida). Esta fase **confere o efeito**: a conferência do SC3 lê `signature_valid`, não só a contagem de linhas. Evento com `signature_valid = 0` nunca é processado. |
| T-132-21 | Tampering | Ação irreversível (envelope ativado) | mitigate | A ativação só acontece contra empresa **fictícia** claramente marcada (D-03), com serviço escolhido para minimizar efeito colateral. O plano diz em que passo exato a irreversibilidade começa. Saída de emergência: registrar cancelamento pela tela (Fase 131, D-13) e concluir no painel. |
| T-132-22 | Information Disclosure | Contrato de teste em produção | mitigate | Nenhum dado de cliente real entra na empresa fictícia: CNPJ fictício, nome marcado como teste, e-mails só da própria casa. Ids ficam registrados para limpeza futura. |
| T-132-23 | Information Disclosure | `132-GATE.md` versionado | mitigate | Id de envelope registrado truncado; nenhum token, secret ou e-mail de cliente real. |
| T-132-24 | Denial of Service | Segredo do webhook divergente entre painel e `.env` | mitigate | Detectado explicitamente pela leitura de `signature_valid` no passo 3, com ação distinta da reversão (corrigir no painel, não reverter a virada). |
| T-132-25 | Repudiation | "O aviso não chegou" sem evidência | mitigate | A conferência é sempre por reconsulta ao banco, com os valores transcritos no `132-GATE.md` com data/hora e autor — nunca por captura de tela ou mensagem de sucesso. |
| T-132-26 | Elevation of Privilege | Empresa fictícia liberada ao operacional | accept | O bloqueio de roteamento ainda está desligado (Fase 133) e o serviço escolhido (Gestão/`performance`) não cria ficha operacional. O resíduo é uma empresa e um contrato, registrados para limpeza. |
| T-132-SC | Tampering (supply chain) | npm/composer install | accept | Nenhuma instalação de pacote neste plano. |
</threat_model>

<verification>
- A URL de webhook está cadastrada e ativa na conta de produção, vista na tela (SC2)
- `contrato_assinatura_eventos` tem linhas para o contrato de teste, com `signature_valid = 1` e `status` processado (SC3)
- O contrato está em `aguardando_assinaturas`, com `enviado_em` preenchido e `liberado_em` vazio
- `132-GATE.md` tem SC2 e SC3 preenchidos e a seção 7 com os ids do que precisa ser limpo depois
</verification>

<success_criteria>
- **SC2:** a URL do aviso automático está cadastrada na conta de **produção** — confirmado olhando a tela, não deduzido
- **SC3:** o primeiro envelope de produção saiu contra empresa de teste controlada, e o aviso de produção chegou e foi processado — provado por reconsulta ao banco
- O critério de parar da D-06 foi aplicado conscientemente: ou houve evento, ou a virada foi revertida e registrada
</success_criteria>

<output>
Criar `.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-03-SUMMARY.md` ao terminar.
</output>
