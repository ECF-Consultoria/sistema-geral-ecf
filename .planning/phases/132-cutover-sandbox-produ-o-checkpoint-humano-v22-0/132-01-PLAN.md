---
phase: 132-cutover-sandbox-produ-o-checkpoint-humano-v22-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Support/Clicksign/ClicksignAmbiente.php
  - config/services.php
  - tests/Feature/Phase132/ClicksignAmbienteTest.php
  - .env.example
  - .planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md
autonomous: true
# Fase SEM REQ-ID (assim está no ROADMAP). O rastreio é pelos Success Criteria
# da própria fase (SC1..SC5) e pelos gates empíricos #3 e #10.
requirements: [SC-132-1, SC-132-4, GATE-EMP-03, D-01, D-02, D-06]

must_haves:
  truths:
    - "Escrever `production`, `producao`, `produção` ou `prod` na variável de ambiente leva o botão da tela do Administrativo para o painel de PRODUÇÃO da Clicksign — nenhuma das quatro grafias cai no painel de teste"
    - "Uma grafia desconhecida (erro de digitação) continua caindo no painel de TESTE, nunca no de produção"
    - "O `.env.example` diz quais grafias valem e onde guardar as credenciais de teste para poder voltar atrás"
    - "Existe um roteiro escrito, em linguagem simples, com o passo a passo da virada, o procedimento de voltar atrás e o critério de parar"
  artifacts:
    - path: "app/Support/Clicksign/ClicksignAmbiente.php"
      provides: "Normalização única da grafia do ambiente Clicksign (ehProducao / painelUrl)"
      exports: ["ehProducao", "painelUrl"]
    - path: "tests/Feature/Phase132/ClicksignAmbienteTest.php"
      provides: "Prova das 4 grafias aceitas, do default seguro e da precedência da URL explícita"
    - path: ".planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md"
      provides: "Roteiro da virada + procedimento de voltar atrás (D-02) + critério de parar (D-06) + campos de resultado"
      min_lines: 80
  key_links:
    - from: "config/services.php"
      to: "App\\Support\\Clicksign\\ClicksignAmbiente::painelUrl()"
      via: "resolução da chave painel_url"
      pattern: "ClicksignAmbiente::painelUrl"
    - from: "app/Http/Controllers/ContratoAdminController.php"
      to: "config('services.clicksign.painel_url')"
      via: "prop painel_clicksign_url já existente (Fase 131)"
      pattern: "painel_clicksign_url"
---

<objective>
Fechar, ANTES de qualquer virada, a armadilha de grafia encontrada no discuss: o código compara
`CLICKSIGN_ENV === 'producao'` (português) e o ROADMAP manda escrever `production` (inglês).
Quem seguir o ROADMAP à risca faz o botão *"Registrar e ir para a Clicksign"* (Fase 131) apontar
para o painel de TESTE enquanto o sistema já opera em produção — e ninguém é avisado.

Propósito: é uma falha **silenciosa**, que só apareceria quando alguém do Administrativo fosse
cancelar um contrato real e não o encontrasse no painel (ou, pior, mexesse no envelope errado).
Fechar a classe do problema (D-01) custa uma classe de 20 linhas; depender de memória na hora da
virada custa um contrato.

Saída: o normalizador de ambiente + a fiação em `config/services.php` + testes; o `.env.example`
documentando as grafias aceitas e o novo estacionamento das credenciais de teste; e o
`132-GATE.md` — o roteiro escrito que os planos 132-02, 132-03 e 132-04 vão preencher.

**Este é o único plano da fase que mexe em código.** Os três seguintes são checklist humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-CONTEXT.md
@.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-GATE.md
@.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md
@CLAUDE.md

<interfaces>
`config/services.php`, bloco `'clicksign'` (a partir da linha ~205) — o estado ATUAL:
- `'env' => env('CLICKSIGN_ENV', 'sandbox')` — só armazena, ninguém normaliza
- `'base_url' => env('CLICKSIGN_BASE_URL', 'https://sandbox.clicksign.com/api/v3')` — variável
  PRÓPRIA, **não é derivada** de `env`
- `'painel_url' => env('CLICKSIGN_PAINEL_URL') ?: (env('CLICKSIGN_ENV','sandbox') === 'producao' ? 'https://app.clicksign.com' : 'https://sandbox.clicksign.com')`
  — ⚠️ **é esta linha que tem a armadilha**
- `'template_id' => env('CLICKSIGN_TEMPLATE_ID')` — ⚠️ **sandbox e produção têm UUIDs DIFERENTES**
  (dito no próprio comentário do arquivo). Não é tocado por este plano, mas é o motivo de o
  plano 132-02 ter um passo extra que o ROADMAP esqueceu

Quem mais lê `CLICKSIGN_ENV`:
- `app/Console/Commands/ClicksignSondarModelo.php:~118` — `garantirAmbienteSeguro()`:
  `if ($env === 'sandbox') return true;` e, fora disso, exige `--producao` + confirmação
  interativa. ⛔ **NÃO ALTERAR.** Essa guarda falha para o lado SEGURO (grafia desconhecida =
  não é sandbox = exige `--producao`). Trocá-la por `!ehProducao()` faria um erro de digitação
  virar "sandbox" e liberar a sondagem sem a flag — seria afrouxar uma trava a pretexto de
  "uniformizar". A assimetria é de propósito e o teste desta task a documenta.
- `app/Http/Controllers/ContratoAdminController.php:~285` — expõe `config('services.clicksign.painel_url')`
  como prop `painel_clicksign_url`. Já está certo; nada a mudar lá.

`app/Support/Clicksign/ClicksignHmacVarredura.php` — molde de classe de apoio desta família
(estática, sem estado, docblock em pt-BR). A classe nova mora ao lado dela.

`Illuminate\Support\Str::ascii()` — converte `produção` em `producao` (ç e til). É o que resolve
a grafia com acento sem tabela manual de substituição.
</interfaces>
</context>

<aviso_de_ambiente>
⚠️ **PHP não está no PATH** — usar sempre `C:\xampp\php\php.exe`.

⚠️ **Filtro do PHPUnit que não casa com nada SAI 0 e ainda varre a suíte inteira** (medido neste
repositório). `INFO No tests found.` com EXIT_CODE 0 é **FALHA**, não sucesso — e a varredura sem
filtro estoura o timeout num ponto pré-existente do `MercadoLivreAdsService`. Ao rodar qualquer
`--filter=`, conferir que a saída traz `Tests: N` com N > 0.

⚠️ **Nenhum deploy neste plano.** O deploy é a Task 1 do plano 132-02 e exige autorização
explícita do usuário (a árvore de trabalho é compartilhada com outro desenvolvedor).
</aviso_de_ambiente>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `ClicksignAmbiente` — uma grafia normalizada, um default seguro (D-01)</name>
  <files>app/Support/Clicksign/ClicksignAmbiente.php, config/services.php, tests/Feature/Phase132/ClicksignAmbienteTest.php</files>
  <read_first>
    - `config/services.php`, bloco `'clicksign'` inteiro (~linhas 205-260) — em especial o comentário acima de `painel_url`, que explica por que o default aponta para o sandbox
    - `app/Support/Clicksign/ClicksignHmacVarredura.php` — molde de classe de apoio estática desta família
    - `app/Console/Commands/ClicksignSondarModelo.php`, método `garantirAmbienteSeguro()` — a guarda que NÃO pode ser alterada, e o motivo (está no `<interfaces>` acima)
    - `tests/Feature/Phase126/ClicksignConfigEFixturesTest.php` linhas 18-40 — molde de teste que assere `config('services.clicksign.*')`
  </read_first>
  <behavior>
    - `ehProducao('production')`, `ehProducao('producao')`, `ehProducao('produção')` e `ehProducao('prod')` são todos `true`
    - Grafia com espaço em volta e com maiúsculas também vale: `'  PRODUÇÃO  '` é `true`
    - `ehProducao('sandbox')`, `ehProducao('')`, `ehProducao(null)` e `ehProducao('producaozinho')` são `false`
    - `painelUrl(null, 'production')` devolve a URL do painel de produção
    - `painelUrl(null, 'sandbox')` e `painelUrl(null, 'ambiente-que-nao-existe')` devolvem a URL do painel de teste (default seguro: erro de digitação nunca leva ninguém ao painel real)
    - `painelUrl('https://exemplo.invalido', 'production')` devolve `https://exemplo.invalido` — a URL explícita vence
    - `config('services.clicksign.painel_url')` continua resolvendo para o painel de TESTE no ambiente de testes (nenhuma regressão do comportamento default)
  </behavior>
  <action>
    Criar `app/Support/Clicksign/ClicksignAmbiente.php` (namespace `App\Support\Clicksign`), classe
    final com métodos estáticos e docblock em pt-BR explicando a armadilha que ela fecha (citar a
    D-01 e o fato de o ROADMAP e o código divergirem na grafia).

    **`public static function ehProducao(?string $env): bool`** — normaliza o valor recebido:
    `trim`, `Str::ascii` (é o que transforma `produção` em `producao`) e `mb_strtolower`, nesta
    ordem; devolve `true` apenas quando o resultado é exatamente um destes três: `producao`,
    `production`, `prod`. Valor nulo, vazio ou qualquer outra coisa devolve `false`. Declarar o
    conjunto aceito como constante pública da classe (`GRAFIAS_PRODUCAO`) para que o teste use a
    mesma fonte e ninguém precise repetir a lista.

    **`public static function painelUrl(?string $painelUrlExplicita, ?string $env): string`** —
    se `$painelUrlExplicita` vier preenchida (`filled()`), devolve ela; senão devolve a URL do
    painel de produção quando `ehProducao($env)` for verdadeiro, e a URL do painel de teste em
    qualquer outro caso. Declarar as duas URLs como constantes públicas da classe
    (`PAINEL_PRODUCAO`, `PAINEL_SANDBOX`) e usar essas constantes também nas asserções do teste —
    URL literal repetida em dois lugares é convite a divergência.

    Motivo de a resolução do painel morar na classe, e não continuar inline no config: config é
    avaliado no boot e fica em cache, o que torna a regra impossível de testar sem recarregar a
    aplicação inteira. Com a regra na classe, o teste é direto e o config vira uma linha de fiação.

    Em `config/services.php`, trocar SOMENTE a expressão de `'painel_url'` por uma chamada a
    `ClicksignAmbiente::painelUrl(env('CLICKSIGN_PAINEL_URL'), env('CLICKSIGN_ENV', 'sandbox'))`,
    com o `use App\Support\Clicksign\ClicksignAmbiente;` no topo do arquivo. Preservar o comentário
    que já existe acima da chave e ACRESCENTAR a ele: as quatro grafias aceitas, o motivo (a D-01,
    o conflito entre a grafia do código e a do ROADMAP) e a afirmação de que grafia desconhecida
    continua caindo no painel de teste de propósito.
    ⛔ **Não mexer em `'env'`, `'base_url'`, `'template_id'` nem em nenhuma outra chave do bloco.**
    ⛔ **Não alterar `ClicksignSondarModelo::garantirAmbienteSeguro()`** — ver `<interfaces>`.

    Criar `tests/Feature/Phase132/ClicksignAmbienteTest.php` cobrindo o bloco `<behavior>` inteiro,
    com nomes de teste em pt-BR (padrão do projeto) e um teste dedicado, com comentário explicando
    o porquê, para o caso `grafia desconhecida cai no painel de teste` — é ele que documenta a
    escolha de default seguro.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test tests/Feature/Phase132/ClicksignAmbienteTest.php</automated>
  </verify>
  <acceptance_criteria>
    - A saída traz `Tests:` com N > 0 e todos verdes; `No tests found` é FALHA mesmo saindo 0
    - `grep -c "ClicksignAmbiente::painelUrl" config/services.php` retorna 1
    - `grep -c "=== 'producao'" config/services.php` retorna 0 (a comparação frágil deixou de existir)
    - `git diff config/services.php` mostra alteração APENAS na chave `painel_url` (mais o `use` no topo) — nenhuma outra chave do bloco `clicksign` tocada
    - `git diff --stat app/Console/Commands/ClicksignSondarModelo.php` está vazio (a guarda de sondagem não foi tocada)
    - `C:\xampp\php\php.exe artisan tinker --execute="echo App\Support\Clicksign\ClicksignAmbiente::ehProducao('produção') ? 'sim' : 'nao';"` imprime `sim`
  </acceptance_criteria>
  <done>Teste verde e todos os itens de <acceptance_criteria> conferidos, inclusive os dois `grep` e o diff limitado.</done>
</task>

<task type="auto">
  <name>Task 2: `.env.example` — as grafias que valem e onde guardar a chave de volta</name>
  <files>.env.example</files>
  <read_first>
    - `.env.example` linhas 180-230 — o bloco Clicksign inteiro, para escrever no mesmo tom e não duplicar chave
    - `.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-CONTEXT.md`, seção `<code_context>` — o achado de que `CLICKSIGN_PROD_TOKEN` e `CLICKSIGN_PROD_WEBHOOK_SECRET` são variáveis MORTAS (nada em `config/services.php` as lê)
  </read_first>
  <action>
    Ajustar o comentário da linha `CLICKSIGN_ENV` para dizer, em linguagem simples, que valem as
    quatro grafias — `sandbox` para teste; `producao`, `produção`, `production` ou `prod` para
    valer de verdade — e que qualquer outra coisa é tratada como teste de propósito.

    Acrescentar, logo abaixo do bloco atual, um sub-bloco **comentado** (chaves sem valor, todas
    prefixadas por `#`) chamando-se explicitamente de estacionamento:

    - `CLICKSIGN_SANDBOX_BASE_URL`
    - `CLICKSIGN_SANDBOX_ACCESS_TOKEN`
    - `CLICKSIGN_SANDBOX_WEBHOOK_SECRET`
    - `CLICKSIGN_SANDBOX_TEMPLATE_ID`

    O texto do comentário precisa dizer três coisas, nesta ordem:
    1. **O app NÃO lê estas chaves.** Elas existem só para guardar as credenciais de TESTE depois
       que o sistema passar a operar em produção — é o que torna "voltar atrás" (D-02) um
       copia-e-cola em vez de uma caça ao token antigo.
    2. Quem opera de verdade são sempre `CLICKSIGN_BASE_URL`, `CLICKSIGN_ACCESS_TOKEN`,
       `CLICKSIGN_WEBHOOK_SECRET` e `CLICKSIGN_TEMPLATE_ID` — virar a chave é **substituir o valor
       delas**, não trocar de nome de variável.
    3. Se o `.env` de alguma máquina ainda tiver `CLICKSIGN_PROD_TOKEN` ou
       `CLICKSIGN_PROD_WEBHOOK_SECRET`, essas linhas são resto de um estacionamento antigo, **não
       são lidas por nada**, e devem ser removidas na virada — dois pares de credenciais no mesmo
       arquivo é o tipo de coisa que confunde exatamente quem está sob pressão.

    Acrescentar também, no comentário do `CLICKSIGN_TEMPLATE_ID` já existente, uma linha em negrito
    lembrando que **o UUID do modelo é diferente entre teste e produção** e que trocá-lo faz parte
    da virada.

    ⛔ **Nenhum valor real** entra neste arquivo: só nomes de chave, sempre vazios ou comentados.
  </action>
  <verify>
    <automated>grep -c "CLICKSIGN_SANDBOX_ACCESS_TOKEN\|CLICKSIGN_SANDBOX_WEBHOOK_SECRET\|CLICKSIGN_SANDBOX_BASE_URL\|CLICKSIGN_SANDBOX_TEMPLATE_ID" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - O `grep` acima retorna 4 (as quatro chaves de estacionamento documentadas — são comentários DE PROPÓSITO, o app não as lê)
    - `grep -c "production" .env.example` retorna 1 ou mais (as grafias aceitas estão escritas)
    - `grep -nE "^(CLICKSIGN_SANDBOX_|CLICKSIGN_PROD_)" .env.example` não retorna nada — nenhuma dessas chaves está ativa (sem `#`)
    - Nenhuma linha do arquivo tem valor de token, secret ou UUID preenchido: `grep -nE "^CLICKSIGN_[A-Z_]+=.+" .env.example` só retorna as linhas já existentes de valores públicos (`sandbox`, a URL do sandbox, `30`, `3`, `20971520`)
    - `git diff .env.example` é só adição de comentário e ajuste de texto — nenhuma chave existente foi renomeada nem removida
  </acceptance_criteria>
  <done>`.env.example` documenta as grafias aceitas e o estacionamento de rollback, sem nenhum valor real.</done>
</task>

<task type="auto">
  <name>Task 3: escrever o roteiro da virada — `132-GATE.md` (com o voltar-atrás e o critério de parar)</name>
  <files>.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md</files>
  <read_first>
    - `.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-GATE.md` — o molde de gate desta milestone: ambiente preparado, roteiro, resultado, resumo para o usuário. **Copiar a estrutura, não o conteúdo**
    - `.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-CONTEXT.md` — as decisões D-01 a D-06 na íntegra
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` §15 (o que a API NÃO faz: não cancela envelope em andamento, não corrige e-mail de signatário) e §16 (limite de 1 envelope/min do próprio app)
    - `app/Console/Commands/ClicksignReconciliar.php` — o que a varredura faz e o carimbo que ela grava
  </read_first>
  <action>
    Criar `132-GATE.md` — o documento que as pessoas vão ler **no dia da virada**. Escrever em
    linguagem simples: quem executar pode não ser quem planejou. Preferir "virar a chave para a
    Clicksign de verdade" a "cutover"; explicar qualquer termo técnico na primeira vez que aparecer.

    Estrutura obrigatória (seções nomeadas, nesta ordem):

    **1. O que está acontecendo aqui, em uma frase.** Hoje o sistema conversa com a Clicksign de
    TESTE; depois desta virada ele conversa com a de verdade, os contratos valem juridicamente e
    os e-mails chegam em caixas de entrada reais.

    **2. Antes de começar — o que precisa estar de pé.** Lista de conferência: a correção do plano
    132-01 publicada em produção; as 4 pessoas que vão assinar avisadas (⚠️ desde 2026-08-17 os
    e-mails dos três signatários da ECF são REAIS — `thiago@`, `emerson@`, `comercial@`); as
    credenciais de produção em mãos; e uma janela de horário que **não** seja perto das 07:00
    (a varredura `clicksign:reconciliar` roda sozinha nesse horário e pode corrigir o contrato
    antes de alguém medir o "antes").

    **3. Ordem dos passos, e por que essa ordem.** Correção da grafia → conferir as variáveis →
    cadastrar o aviso automático (webhook) no painel de produção → primeiro contrato de teste →
    provar a rede de segurança → aprovação final. Deixar explícito que **pular a ordem quebra a
    medição**: sem o webhook cadastrado antes do envelope, não dá para saber se ele chegaria.

    **4. Campos de resultado em branco**, um por Success Criteria (SC1 a SC5), mais um para o gate
    empírico #3 (URL de produção) e outro para o gate empírico #10 (a rede de segurança / SC1 da
    Fase 130). Cada campo com: o que foi feito, o que o BANCO devolveu (não a tela, não o console),
    data/hora e quem executou.

    **5. Como voltar atrás (D-02) — procedimento numerado, não parágrafo.** Passos:
    1. Restaurar no `.env` de produção as 4 linhas de teste (`CLICKSIGN_ENV`, `CLICKSIGN_BASE_URL`,
       `CLICKSIGN_ACCESS_TOKEN`, `CLICKSIGN_WEBHOOK_SECRET`) a partir do estacionamento
       `CLICKSIGN_SANDBOX_*`, mais o `CLICKSIGN_TEMPLATE_ID` de teste.
    2. Rodar `php artisan config:clear && php artisan config:cache`. ⚠️ Explicar em uma frase:
       **enquanto a configuração está em cache, editar o `.env` não muda absolutamente nada.**
    3. Reiniciar os workers da fila (`supervisorctl restart ecf-worker:*`). ⚠️ Explicar:
       **o worker guarda a configuração antiga na memória; sem reiniciar, ele continua falando com
       a Clicksign de produção mesmo com o `.env` já revertido.**
    4. ⚠️ **Restaurar o `.env` NÃO impede um contrato já enfileirado de sair.** A janela é curta
       (segundos até cerca de um minuto, por causa do limite de 1 envelope por minuto), mas existe.
       **Abrir o painel da Clicksign de produção e conferir com os próprios olhos** se algum
       envelope saiu depois da reversão — nunca assumir que parou na hora.
    5. Se saiu algo: **a API não cancela envelope em andamento** (medido — §15.2 do empírico).
       Cancelar é operação de painel. Registrar o cancelamento pela tela do Administrativo
       (`/administrativo/contratos`, ação de registrar cancelamento — Fase 131, D-13), que grava
       quem pediu e por quê, e **concluir o cancelamento no painel da Clicksign**.

    **6. Quando PARAR (D-06).** Um único critério manda parar e voltar atrás: **depois de ativar o
    contrato, nenhum evento aparecer na tabela `contrato_assinatura_eventos`.** Explicar por quê:
    sem o aviso automático, nenhuma empresa é liberada sozinha, e a Fase 133 estaria ligando um
    bloqueio em cima de um mecanismo quebrado. Diferença cosmética (um campo com nome estranho, um
    horário em fuso diferente) **não** manda parar — anota-se e segue.

    **7. Lixo que fica para limpar depois.** A empresa fictícia e o contrato criados no plano
    132-03 ficam na base de produção (custo aceito na D-03). Registrar aqui o id da empresa e o id
    do contrato assim que existirem, para que a limpeza futura saiba o que apagar.

    ⛔ **Nenhum token, secret, UUID de credencial ou e-mail de cliente real entra neste arquivo** —
    ele é versionado. Quando for preciso provar que uma credencial é a certa, registrar o
    RESULTADO da conferência (`confere` / `não confere`), nunca o valor.
  </action>
  <verify>
    <automated>grep -c "^## " .planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - O `grep` retorna 7 ou mais (as sete seções obrigatórias existem)
    - `grep -c "SC1\|SC2\|SC3\|SC4\|SC5" 132-GATE.md` retorna 5 ou mais — há campo de resultado para cada Success Criteria
    - O procedimento de voltar atrás está numerado (`grep -cE "^[0-9]\." 132-GATE.md` retorna 5 ou mais) e cita, com estas palavras, `config:cache`, `ecf-worker` e "painel da Clicksign"
    - O arquivo contém a frase que explica que restaurar o `.env` não impede um contrato já enfileirado de sair
    - `grep -nE "[A-Za-z0-9_\-]{30,}" 132-GATE.md` não retorna nenhuma linha que pareça token/secret (só URLs e nomes de arquivo)
    - Nenhum e-mail de cliente real no arquivo: `grep -c "@" 132-GATE.md` só casa com os endereços institucionais já citados no CONTEXT, se citados
  </acceptance_criteria>
  <done>`132-GATE.md` existe, tem as 7 seções, os campos de resultado em branco, o procedimento numerado de voltar atrás e o critério de parar — e nenhum segredo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` → `config()` → tela do Administrativo | O valor de uma variável de ambiente decide para QUAL painel da Clicksign um humano é levado. Um humano no painel errado age no ambiente errado. |
| Repositório versionado (`.env.example`, `132-GATE.md`) | Tudo que entra aqui é público para quem tem acesso ao repo, e fica no histórico do git para sempre. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-132-01 | Spoofing | `config/services.php` → `painel_url` | mitigate | O ambiente é normalizado por `ClicksignAmbiente::ehProducao()` (4 grafias); grafia desconhecida continua resolvendo para o painel de TESTE. Teste dedicado prova o default seguro. |
| T-132-02 | Elevation of Privilege | `ClicksignSondarModelo::garantirAmbienteSeguro()` | mitigate | Proibido alterar a comparação `=== 'sandbox'` desta guarda. Ela falha para o lado seguro; "uniformizar" com `!ehProducao()` faria um erro de digitação liberar sondagem contra produção sem a flag `--producao`. Coberto por item de `<acceptance_criteria>` (diff vazio). |
| T-132-03 | Information Disclosure | `.env.example` | mitigate | Só nomes de chave, sempre vazios ou comentados; `<acceptance_criteria>` tem grep que reprova qualquer valor preenchido. |
| T-132-04 | Information Disclosure | `132-GATE.md` (versionado) | mitigate | Proibido registrar token, secret, UUID de credencial ou e-mail de cliente; registra-se o RESULTADO da conferência (`confere`/`não confere`), nunca o valor. Grep de cadeias longas no `<acceptance_criteria>`. |
| T-132-05 | Repudiation | Roteiro sem autor/data | mitigate | Cada campo de resultado do `132-GATE.md` exige data/hora e quem executou — sem isso, "foi conferido" não é verificável depois. |
| T-132-SC | Tampering (supply chain) | npm/composer install | accept | **Nenhuma instalação de pacote nesta fase.** Nenhum `composer require`, `npm install` ou equivalente aparece em qualquer task dos 4 planos; a classe nova usa só `Illuminate\Support\Str`, já presente. Se alguma task de execução precisar instalar algo, isso é sinal de desvio de escopo e deve parar para revisão. |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test tests/Feature/Phase132/ClicksignAmbienteTest.php` verde, com `Tests: N` e N > 0
- Nenhuma regressão no bloco Clicksign do config: `C:\xampp\php\php.exe artisan test tests/Feature/Phase126/ClicksignConfigEFixturesTest.php` continua verde
- `git status --porcelain` mostra apenas os 5 arquivos de `files_modified` (a árvore é compartilhada — commitar só estes caminhos, nunca `git add -A`)
</verification>

<success_criteria>
- As quatro grafias de produção levam ao painel de produção; qualquer outra continua levando ao de teste
- O `.env.example` diz quais grafias valem, onde guardar as credenciais de teste para voltar atrás, e que as chaves `CLICKSIGN_PROD_*` são resto morto a ser removido
- Existe um roteiro escrito e versionado (`132-GATE.md`) com o passo a passo, o procedimento numerado de voltar atrás e o critério de parar
- Nenhum segredo entrou em arquivo versionado
</success_criteria>

<output>
Criar `.planning/phases/132-cutover-sandbox-produ-o-checkpoint-humano-v22-0/132-01-SUMMARY.md` ao terminar.
</output>
