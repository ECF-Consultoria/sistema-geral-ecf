---
plan: 56-03
phase: 56
wave: 3
depends_on: [56-01, 56-02]
files_modified:
  - .planning/phases/56-menu-lateral-multi-marketplace-stubs-em-desenvolvimento/56-UAT.md
  - .planning/phases/56-menu-lateral-multi-marketplace-stubs-em-desenvolvimento/PHASE-SUMMARY.md
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]
---

# Wave 3 — UAT humano + deploy + PHASE-SUMMARY

## Objective

Fechar Phase 56 com validação humana (UAT), deploy autorizado e resumo canônico. Sem essa wave, a phase fica "código pronto" mas não formalmente entregue e não visível em prod.

`autonomous: false` porque UAT precisa de humano validando cada checkpoint visual + confirmação explícita de deploy (conforme [[feedback_perguntar_antes_deploy_v9]] — checa caso-a-caso enquanto v11.0+ tem outro dev em paralelo; v13.0 herda o mesmo cuidado até ser confirmado).

## Tasks

### Task 1 — Checklist UAT humano (56-UAT.md)

<action>
Criar arquivo `56-UAT.md` no diretório da phase com o checklist abaixo. Cada item precisa ser explicitamente validado por humano navegando no sistema local (ou em prod após deploy). Não fazer isso via automação.

O executor deve APRESENTAR o checklist ao usuário e coletar cada resposta antes de marcar `status: aprovado` ou `status: gaps_found` no frontmatter final.

Estrutura sugerida do arquivo:

```markdown
---
phase: 56
status: pendente  # pendente | aprovado | gaps_found
tested_at: null
tested_by: null
environment: local  # local | prod
---

# Phase 56 — UAT Checklist

**Milestone:** v13.0 Reorganização Multi-Marketplace
**Requirements cobertos:** NAV-01, NAV-02, NAV-03, NAV-04

## Preparação

1. `git status` deve estar limpo (todos os commits de Waves 1 e 2 já feitos)
2. `npm run build` deve rodar sem erro
3. Servidor local iniciado (via `php artisan serve` ou XAMPP)
4. Login como usuário com role `admin`

## Checkpoints visuais (todos [ ] devem virar [x])

### C1 — Grupo Mercado Livre (NAV-01)
- [ ] Sidebar mostra grupo `Mercado Livre` no TOPO (primeiro item)
- [ ] Grupo aparece ABERTO por padrão (chevron para cima; children visíveis)
- [ ] Ordem dos children ANTES do divider: Dashboard, Desempenho, Empresas, Carteira, Reuniões, Sugadores, Metas, PPA (nessa ordem)
- [ ] Divider mostra label "POLOS" pequeno + hr sutil (não é um item clicável)
- [ ] Após divider: Onboarding, Empresas Polos, Faturamento Polos
- [ ] Cada item tem ícone correspondente + badge (só Sugadores tem badge vermelho de contador)
- [ ] Clicar em Dashboard → carrega `/dashboard` sem erro
- [ ] Clicar em Sugadores → carrega `/sugadores` sem erro
- [ ] Clicar em Onboarding → carrega `/mlb/implementacao` sem erro
- [ ] Fechar o grupo ML → grupo colapsa; abrir novamente → volta aberto
- [ ] Recarregar página (F5) → grupo permanece aberto (defaultOpen respeitou preferência de sessão)

### C2 — Publicação transversal (NAV-02)
- [ ] Grupo `Publicação` (singular, com cedilha e acento) aparece FORA da pasta Mercado Livre
- [ ] Grupo `Publicações` (plural antigo) NÃO aparece em lugar algum
- [ ] Sub-items do grupo Publicação continuam funcionando (Pub · Dashboard, Desempenho, Treinamentos, etc)
- [ ] Grupo `Polos` velho (separado) NÃO aparece — foi absorvido pelo grupo Mercado Livre

### C3 — Stubs Shopee/Amazon com badge (NAV-03)
- [ ] Items `Shopee` e `Amazon` aparecem na sidebar (posição: logo após o grupo Mercado Livre)
- [ ] Cada um tem ícone (ShoppingCart para Shopee, Package2 para Amazon)
- [ ] Cada um tem badge cinza discreto com texto "Em breve" à direita do nome
- [ ] Badge NÃO é vermelho (não é contador — é label estático)

### C4 — Placeholder /em-desenvolvimento (NAV-04)
- [ ] Clicar em `Shopee` na sidebar → navega para `/em-desenvolvimento?marketplace=shopee`
- [ ] Página renderiza com título "Shopee em desenvolvimento" no card central
- [ ] Ícone de obra (Construction) em círculo amarelo destacado
- [ ] Texto explicativo em pt-BR menciona "Shopee" no corpo
- [ ] Botão amarelo "Voltar ao Dashboard" visível
- [ ] Clicar no botão → navega para `/dashboard`
- [ ] Repetir para `Amazon` — título "Amazon em desenvolvimento"
- [ ] Sidebar permanece visível durante a navegação (AppLayout wraps)

### C5 — Permissões continuam gatando (regressão)
- [ ] Fazer logout, logar como usuário NÃO-admin (analista ou publicador se disponível)
- [ ] Grupo `Mercado Livre` mostra APENAS os items que o usuário tem permissão (ex: analista sem `core.metas` não vê Metas dentro do grupo)
- [ ] Se usuário sem permissão em NENHUM item do grupo ML → grupo inteiro some (comportamento do `filteredTree`)
- [ ] Items `Shopee` e `Amazon` VISÍVEIS para todos autenticados (sem permission gating)
- [ ] Grupo `Publicação` continua respeitando as permissions dos sub-items mlb.* (mesma lógica de antes)

### C6 — Zero regressão em rotas existentes
- [ ] Navegar por 5 rotas aleatórias do sistema (ex: Comercial > Empresas, Admin > Fechamento, NPS > Pesquisas) — todas carregam sem erro
- [ ] Nenhum warning novo no console do navegador
- [ ] `php artisan route:list | wc -l` — número de rotas ≥ ao anterior (pra confirmar que só adicionamos)

## Fecha

- [ ] Todos os C1-C6 marcados [x]
- [ ] Sem regressões observadas
- [ ] Aprovar → editar frontmatter: `status: aprovado`, `tested_at: <data>`, `tested_by: <nome>`

Se algum C falhar → `status: gaps_found`, listar os itens abaixo do checklist e escalar pra hotfix na mesma wave ou nova phase.
```
</action>

<read_first>
- .planning/phases/56-menu-lateral-multi-marketplace-stubs-em-desenvolvimento/56-CONTEXT.md (§ Success criteria)
- .planning/phases/56-menu-lateral-multi-marketplace-stubs-em-desenvolvimento/56-01-PLAN.md (verification criteria da Wave 1)
- .planning/phases/56-menu-lateral-multi-marketplace-stubs-em-desenvolvimento/56-02-PLAN.md (verification criteria da Wave 2)
</read_first>

<acceptance_criteria>
1. Arquivo `.planning/phases/56-.../56-UAT.md` existe
2. Frontmatter tem chaves `phase`, `status`, `tested_at`, `tested_by`, `environment`
3. Arquivo lista 6 checkpoints (C1..C6) cobrindo NAV-01..04 + regressão
4. Após UAT humano: `status` no frontmatter é `aprovado` OU `gaps_found` (não pendente)
</acceptance_criteria>

### Task 2 — Deploy autorizado (após UAT aprovado local)

<action>
**BLOQUEIO ATIVO:** Só rodar esta task se Task 1 estiver com `status: aprovado`.

Confirmar com o usuário se quer fazer deploy AGORA:

> UAT local aprovado ✓. Posso rodar `.\deploy_parcial.sh` para subir Phase 56 pra prod?
> Arquivos modificados: `resources/js/Layouts/AppLayout.jsx`, `resources/js/Pages/EmDesenvolvimento.jsx`, `routes/web.php`.

**Se SIM:**
1. Rodar `.\deploy_parcial.sh` (ou `.\deploy.sh` conforme convenção do projeto — verificar qual está sendo usado atualmente)
2. Após deploy, monitorar output do script (particularmente erro em `composer install --no-dev`, `php artisan migrate --force`, cache commands)
3. Acessar `https://admin.ecfconsultoria.com.br` em modo anônimo (evita cache local)
4. Repetir smoke visual básico: sidebar renderiza, ML aberto, Shopee/Amazon com badge, placeholder funciona
5. Se OK → registrar em PHASE-SUMMARY que deploy foi feito em `<data>`
6. Se algo quebrar → hotfix rápido (ex: cache clear via `php artisan cache:clear` no VPS) ou rollback via git revert

**Se NÃO (usuário quer deployar depois):**
1. Marcar em PHASE-SUMMARY que deploy está pendente
2. Não bloqueia fechamento da phase — deploy pode ser feito depois manualmente

**Se ainda houver dúvida sobre deploy_parcial vs deploy_full:**
- `deploy_parcial.sh` — SÓ arquivos frontend + rotas + config (não faz migrations)
- `deploy.sh` — deploy completo (mexe em backend/DB/queue restart)
- Para Phase 56 (frontend + 1 rota nova sem controller), `deploy_parcial.sh` é suficiente
</action>

<read_first>
- deploy.sh (verificar comandos executados)
- deploy_parcial.sh (verificar se existe e o que faz)
- CLAUDE.md § "Platform Requirements" (VPS Hostinger, supervisorctl restart pattern)
</read_first>

<acceptance_criteria>
1. Se deploy foi feito: `curl -sI https://admin.ecfconsultoria.com.br/em-desenvolvimento -H "Accept: text/html"` retorna 302 (redirect para login) OU 200 se logado — indica rota registrada em prod
2. Se deploy foi feito: HTML da resposta em prod contém `Mercado Livre` (visível pelo grepper depois de autenticado)
3. Se deploy NÃO foi feito: PHASE-SUMMARY.md tem seção `## Deploy` com status `pendente` + arquivos a subir
</acceptance_criteria>

### Task 3 — PHASE-SUMMARY.md canônico

<action>
Criar `.planning/phases/56-menu-lateral-multi-marketplace-stubs-em-desenvolvimento/PHASE-SUMMARY.md` no formato canônico do projeto (frontmatter YAML + seções em pt-BR).

Estrutura:

```markdown
---
phase: 56
name: Menu lateral multi-marketplace + stubs "em desenvolvimento"
milestone: v13.0
status: complete  # complete | partially_complete | gaps_found
completed_at: <data>
requirements_completed: [NAV-01, NAV-02, NAV-03, NAV-04]
plans_delivered: [56-01, 56-02, 56-03]
uat_status: aprovado  # aprovado | gaps_found
deployed_to_prod: true  # true | false | pending
---

# Phase 56 — PHASE-SUMMARY

**Milestone:** v13.0 Reorganização Multi-Marketplace
**Escopo:** Reorganizar sidebar da app ECF Admin para refletir estrutura multi-marketplace da ECF Consultoria. Mudança visual imediata, zero risco de dados.

## O que foi entregue

### Wave 1 — Refactor NAV_TREE + extensão renderSidebar
- Grupo `Mercado Livre` (aberto por padrão via nova flag `defaultOpen: true`) contendo:
  - 8 items Performance (Dashboard, Desempenho, Empresas, Carteira, Reuniões, Sugadores, Metas, PPA)
  - Divider "POLOS" (novo tipo de entry em NAV_TREE)
  - 3 items Polos (Onboarding, Empresas Polos, Faturamento Polos)
- Items topo `Shopee` e `Amazon` com badge cinza estático "Em breve" (novo campo `badgeText` distinto do `showBadge` dinâmico)
- Grupo renomeado `Publicações` → `Publicação` (singular)
- Grupo `Polos` velho removido (absorvido pelo grupo Mercado Livre)
- Extensão de `renderSidebar` + `itemVisivel` + inicialização de `openGroups` para suportar as 3 novas features

### Wave 2 — Rota + página placeholder
- Rota GET `/em-desenvolvimento` registrada em `routes/web.php` (nome: `em-desenvolvimento`)
- Componente `resources/js/Pages/EmDesenvolvimento.jsx` — placeholder consistente com design system ECF (dark theme, tokens `ecf-*`, ícone Construction, botão CTA "Voltar ao Dashboard")
- Query param `?marketplace=<slug>` alimenta sub-título dinâmico ("Shopee em desenvolvimento" / "Amazon em desenvolvimento" / genérico)

### Wave 3 — UAT humano + deploy
- 6 checkpoints C1-C6 validados manualmente
- Deploy via `deploy_parcial.sh` (se autorizado)

## Requirements cobertos

| REQ-ID | Descrição | Como validado |
|--------|-----------|---------------|
| NAV-01 | Sidebar mostra pasta "Mercado Livre" aberta com Performance + Polos | UAT C1 |
| NAV-02 | Sidebar mostra "Publicação" transversal fora do ML | UAT C2 |
| NAV-03 | Sidebar mostra "Shopee" e "Amazon" com stub em desenvolvimento | UAT C3 |
| NAV-04 | Rota `/em-desenvolvimento` renderiza placeholder consistente com DS | UAT C4 |

## Arquivos modificados

- `resources/js/Layouts/AppLayout.jsx` — refactor NAV_TREE + extensão renderSidebar
- `resources/js/Pages/EmDesenvolvimento.jsx` — NOVO componente placeholder
- `routes/web.php` — NOVA rota `em-desenvolvimento`

## Decisões implementadas (locked no CONTEXT.md)

1. Achatar em 1 grupo Mercado Livre com separator visual (não implementar sub-grupos aninhados)
2. Dashboard aponta pra rota `dashboard` atual (Phase 58 renomeia)
3. Rename `Publicações → Publicação` mantendo sub-items mlb.* intactos (Phase 59 audita)
4. Stubs com badge "Em breve" + placeholder na página

## Habilita phases

- **Phase 57** (Modelo de dados multi-marketplace): estrutura visual estabelecida — decisões de dados agora podem seguir o contrato visual do menu
- **Phase 58** (Dashboard ECF agregado): shell `/dashboard/{marketplace}` fica trivial de encaixar
- **Phase 59** (Desacoplamento áreas transversais): Publicação transversal já sinalizada visualmente

## Notas para consulta futura

- Nova convenção NAV_TREE: entry pode ter `divider: 'label'` OU `badgeText: 'label'` (estático) OU `showBadge: 'counter_key'` (dinâmico) OU `defaultOpen: true` (grupo). Documentado inline nos comentários de `AppLayout.jsx`.
- Placeholder de marketplace usa `MARKETPLACE_LABEL` object — extensível para próximos marketplaces sem tocar em rota/backend.
```

Data de completed_at = data do fechamento da UAT.
</action>

<read_first>
- .planning/phases/56-menu-lateral-multi-marketplace-stubs-em-desenvolvimento/56-CONTEXT.md
- .planning/phases/56-menu-lateral-multi-marketplace-stubs-em-desenvolvimento/56-UAT.md (para saber status e data)
</read_first>

<acceptance_criteria>
1. Arquivo `.planning/phases/56-.../PHASE-SUMMARY.md` existe
2. Frontmatter tem: `phase: 56`, `milestone: v13.0`, `status`, `requirements_completed: [NAV-01, NAV-02, NAV-03, NAV-04]`, `plans_delivered: [56-01, 56-02, 56-03]`
3. Corpo lista as 3 waves entregues + requirements table + arquivos modificados
4. Corpo referencia as 4 decisões locked no CONTEXT.md como implementadas
5. `uat_status` casa com o `status` do 56-UAT.md
6. Se deployado: `deployed_to_prod: true`; senão `pending` ou `false`
</acceptance_criteria>

### Task 4 — Atualizar ROADMAP.md e REQUIREMENTS.md

<action>
1. Em `.planning/ROADMAP.md`, marcar Phase 56 como completa:
   - Linha da entrada resumida `- [ ] **Phase 56:** ... ` → `- [x] **Phase 56:** ... *(completed <data>)*`
   - No detalhe `### Phase 56:` → alterar `**Status:** Pending` → `**Status:** Complete`

2. Em `.planning/REQUIREMENTS.md`, na tabela `## Traceability v13.0`, marcar cada REQ NAV-* como `Complete` (era `Pending`).

3. Em `.planning/STATE.md`, registrar sessão de fechamento via SDK:
   ```bash
   gsd-sdk query state.record-session --stopped-at "Phase 56 fechada (aprovado UAT)" --resume-file ".planning/phases/56-.../PHASE-SUMMARY.md"
   ```
</action>

<read_first>
- .planning/ROADMAP.md (linhas ~153 sumário Phase 56 + linha ~1315 detail section)
- .planning/REQUIREMENTS.md (seção Traceability v13.0)
</read_first>

<acceptance_criteria>
1. Grep `- [x] **Phase 56:` em `.planning/ROADMAP.md` retorna 1 match
2. Grep `- [ ] **Phase 56:` em `.planning/ROADMAP.md` retorna 0 matches
3. Em `.planning/REQUIREMENTS.md`, cada NAV-01..04 na tabela Traceability aparece com status `Complete` (não Pending)
4. `.planning/STATE.md` foi atualizado — `last activity` menciona Phase 56 e a data
</acceptance_criteria>

### Task 5 — Commit final

<action>
Um único commit fechando Phase 56 com mensagem canônica pt-BR:

```bash
git add .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/phases/56-menu-lateral-multi-marketplace-stubs-em-desenvolvimento/
git commit -m "$(cat <<'EOF'
docs(56): fecha Phase 56 — menu lateral multi-marketplace (v13.0)

- Grupo Mercado Livre aberto por padrão consolidando Performance + Polos
- Items Shopee/Amazon com badge Em breve + placeholder /em-desenvolvimento
- Renomeacao Publicacoes → Publicacao (singular, transversal)
- Zero regressao em rotas existentes; UAT humano aprovado
- Requirements: NAV-01, NAV-02, NAV-03, NAV-04

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```
</action>

<read_first>
- (nenhum — commit final)
</read_first>

<acceptance_criteria>
1. `git log -1 --oneline` retorna o commit com mensagem começando `docs(56): fecha Phase 56`
2. `git status` está limpo após o commit
</acceptance_criteria>

## Must haves

- UAT humano validado — 6 checkpoints (C1-C6) todos [x]
- Zero regressão em rotas existentes
- PHASE-SUMMARY.md canônico com frontmatter completo
- ROADMAP + REQUIREMENTS + STATE atualizados
- Commit final documenta fechamento

## Deferred (NÃO fazer nesta wave)

- Feature tests PHP formalizados — smoke funcional já cobre
- Rollout gradual/feature flag — mudança de menu é visual e reversível via git revert; não vale complexidade
- Comunicação ao time sobre menu novo — fora do escopo GSD; usuário decide como comunicar
