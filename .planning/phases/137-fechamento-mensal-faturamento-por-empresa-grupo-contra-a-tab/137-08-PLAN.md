---
phase: 137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab
plan: 08
type: execute
wave: 5
depends_on: ["137-03", "137-05", "137-07"]
files_modified:
  - app/Http/Controllers/AdminController.php
  - app/Jobs/EnviarRelatorioFechamentoJob.php
  - resources/views/admin/relatorio-fechamento.blade.php
  - resources/views/admin/relatorio-geral.blade.php
  - resources/views/admin/relatorio-geral-pdf.blade.php
  - tests/Feature/Phase137/Phase137RelatoriosFechamentoTest.php
autonomous: true
requirements: [D-02, D-05, D-06, D-08, D-10, D-11]

must_haves:
  truths:
    - "A constante FAIXAS não existe mais em lugar nenhum do código"
    - "PDF por empresa, relatório geral e email mensal mostram o mesmo número da tela para a mesma competência"
    - "O email mensal passa a incluir o faturamento de Shopee, que nunca incluiu"
    - "Nenhum dos três consumidores agrupa por parent_company_id"
  artifacts:
    - path: "app/Jobs/EnviarRelatorioFechamentoJob.php"
      provides: "Email mensal lendo o snapshot/rollup central, sem tabela de faixas própria"
      contains: "FechamentoRollupService"
  key_links:
    - from: "app/Jobs/EnviarRelatorioFechamentoJob.php"
      to: "app/Services/Fechamento/FechamentoRollupService.php"
      via: "faturamento da competência, incluindo Shopee"
      pattern: "FechamentoRollupService"
    - from: "app/Http/Controllers/AdminController.php"
      to: "app/Services/Fechamento/FechamentoFaixaResolver.php"
      via: "faixa dos relatórios PDF"
      pattern: "FechamentoFaixaResolver"
---

<objective>
Migrar os três call-sites restantes — `gerarRelatorio()`, `gerarRelatorioGeral()` e
`EnviarRelatorioFechamentoJob` — e apagar a constante `FAIXAS` das duas cópias.

Purpose: a pesquisa mediu que a tabela progressiva está DUPLICADA em `AdminController::FAIXAS` e
`EnviarRelatorioFechamentoJob::FAIXAS`, e que a lógica de `parent_company_id` aparece em quatro
lugares. Deixar um para trás significa dois números diferentes para a mesma empresa no mesmo mês — e
isso é dinheiro. O job ainda tem um segundo furo pré-existente: soma só `adman_metrics` e NUNCA
incluiu Shopee, então D-05 também precisa ser fechado aqui, não só na tela.

Output: 3 consumidores migrados, 2 constantes apagadas, 3 blades com um fallback de título,
1 arquivo de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-RESEARCH.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-UI-SPEC.md

<interfaces>
Contratos que o executor precisa e NÃO deve ir procurar no código:

O que os três consumidores fazem hoje (medido pela pesquisa):
- `AdminController::gerarRelatorio()` (linhas 453-587): mesma janela móvel de 30 dias no mês
  corrente, `$company->filhas` como grupo (linha ~488), `calcularFaixa()`/`faixaLabel()`, e para o
  mês atual chama `AdmanService::fetchGrossBillingsBatch()` pela rede.
- `AdminController::gerarRelatorioGeral()` (linhas 590-716): mesma janela, `whereNull('parent_company_id')`
  como filtro de raiz (linha ~625), `$company->filhas` como grupo, e o mesmo cache da Adman.
- `EnviarRelatorioFechamentoJob` (242 linhas): tem a própria cópia da constante `FAIXAS`
  (linhas 33-40), o próprio `calcularFaixa()` (linha ~220), usa `whereNull('parent_company_id')` +
  `filhas` (linhas 84-96), e soma SÓ `AdmanMetric` (linha ~100) — nunca incluiu Shopee.
  É disparado por `AdminController::enviarRelatorioGeral()` via `dispatchSync()` e pelo
  `Schedule::call()` de `routes/console.php` linhas 260-278 (dia e hora configuráveis por
  `Configuracao::get('email_envio_auto_dia','5')` e `'email_envio_auto_hora','09:00'`).
  ⚠️ A pesquisa registra que o valor REALMENTE configurado em produção não foi verificado — não
  assumir "dia 1". Este plano NÃO altera o agendamento.

Serviços centrais (planos 03 e 05), com as mesmas assinaturas já usadas no plano 07:
`FechamentoRollupService::janela()` e `::porEmpresa()`, `FechamentoFaixaResolver::paraEmpresa()` e
`::classificar()`, `FechamentoSnapshot::ORIGEM_CONSOLIDAR_MES`.

Contrato das views (preservar as chaves para não reescrever Blade):
- `admin.relatorio-fechamento` consome `$company`, `$mes_label`, `$faturamento`, `$faixa_label`,
  `$valor_mensal`, `$cobranca_mensal`, `$vinculadas` (array com `name`, `faixa_label`,
  `faturamento`, `valor_mensal`, `cobranca_mensal`), `$total_faturamento`, `$total_mensalidade`,
  `$recebido`, `$gerado_em`, `$servicos_contratados_pai`.
- `admin.relatorio-geral` e `admin.relatorio-geral-pdf` consomem `$relatorios` (cada item com
  `company`, `recebido`, `faturamento`, `faixa_label`, `valor_mensal`, `cobranca_mensal`,
  `vinculadas`, `total_mensalidade`), `$mes_label`, `$mes_selecionado`, `$gerado_em`,
  `$filtro_recebido`.
- As três views imprimem o nome da entidade com `{{ $company->name }}` (linhas 254 e 308 em
  `relatorio-fechamento`, 354 e 404 em `relatorio-geral`).

`tests/Feature/Phase14BladeRefactorTest.php` cobre essas views e precisa continuar verde.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: gerarRelatorio() e gerarRelatorioGeral() lendo a fonte central</name>
  <files>app/Http/Controllers/AdminController.php, resources/views/admin/relatorio-fechamento.blade.php, resources/views/admin/relatorio-geral.blade.php, resources/views/admin/relatorio-geral-pdf.blade.php</files>
  <read_first>
    - app/Http/Controllers/AdminController.php linhas 453-760 (os dois métodos inteiros)
    - app/Http/Controllers/AdminController.php a versão já migrada de `fechamento()` (plano 07) — é o molde de como resolver janela, faixa e snapshot
    - resources/views/admin/relatorio-fechamento.blade.php linhas 250-260 e 300-315 (onde o nome da entidade é impresso)
    - resources/views/admin/relatorio-geral.blade.php linhas 350-360 e 400-410 (idem)
  </read_first>
  <action>
Em `gerarRelatorio(Request $request, Company $company)`: trocar a derivação de janela pela
`FechamentoRollupService::janela($mesSelecionado)`, eliminando o ramo
`Carbon::now()->subDays(30)` e a chamada de rede `AdmanService::fetchGrossBillingsBatch()`. O
faturamento vem de `FechamentoRollupService::porEmpresa()` (ML + Shopee, D-05) para o conjunto
"empresa + irmãs de grupo"; a faixa vem de `FechamentoFaixaResolver`; quando a competência já está
fechada, os números vêm de `fechamento_snapshots`, igual à tela.

O "grupo" do relatório deixa de ser `$company->filhas` e passa a ser as empresas-irmãs do mesmo
`company_group_id` (excluindo a própria). Empresa sem grupo gera relatório sem vinculadas. `$titulo`
novo: o nome do `CompanyGroup` quando a empresa pertence a um, senão o nome da própria empresa.

Em `gerarRelatorioGeral(Request $request)`: trocar o filtro `whereNull('parent_company_id')` por
"uma linha por grupo (representada pela empresa-âncora de maior faturamento, empate pelo menor id) +
uma linha por empresa sem grupo". `$r['vinculadas']` passa a ser as demais empresas do grupo.
`$r['titulo']` segue a mesma regra do parágrafo anterior. Manter os filtros existentes de
`servico_nome` e `recebido` funcionando.

`faixa_label` continua sendo string legível, agora montada a partir do retorno de
`classificar()` (`Faixa {ordem}` com o intervalo em reais, ou `Faixa máxima (acima de R$ X)` quando
`limite_superior` é nulo). Empresa sem tabela recebe `faixa_label = null` e a view já trata isso com
o travessão — não inventar `R$ 0`.

Nas três views, trocar APENAS as ocorrências que imprimem o nome da entidade no cabeçalho e na
primeira linha da tabela por `{{ $titulo ?? $company->name }}` (em `relatorio-geral` e
`relatorio-geral-pdf`, `{{ $r['titulo'] ?? $company->name }}` no contexto do laço). Nenhuma outra
linha de Blade muda — nem estilo, nem estrutura, nem as demais chaves.

Depois de os dois métodos pararem de usá-los, APAGAR de `AdminController` a constante privada
`FAIXAS` e os helpers privados `calcularFaixa()`, `faixaNumero()` e `faixaLabel()`. Se algum outro
método do arquivo ainda os referenciar, migrá-lo também — nenhum pode ficar para trás.
  </action>
  <acceptance_criteria>
    - `grep -c "FAIXAS" app/Http/Controllers/AdminController.php` retorna 0.
    - `grep -c "calcularFaixa\|faixaNumero\|faixaLabel" app/Http/Controllers/AdminController.php` retorna 0.
    - `grep -c "subDays(30)" app/Http/Controllers/AdminController.php` retorna 0.
    - `grep -c "parent_company_id" app/Http/Controllers/AdminController.php` — as ocorrências restantes só podem estar em `empresas()`/`updateEmpresa()` (cadastro), nunca em `fechamento()`, `gerarRelatorio()` ou `gerarRelatorioGeral()`.
    - `git diff --stat` nas três views mostra no máximo 2 linhas alteradas por arquivo.
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase14BladeRefactorTest` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter="Phase137RelatoriosFechamentoTest|Phase14BladeRefactorTest"</automated>
  </verify>
  <done>Os dois relatórios leem a fonte central, agrupam por `CompanyGroup` e a constante `FAIXAS` sumiu do controller.</done>
</task>

<task type="auto">
  <name>Tarefa 2: EnviarRelatorioFechamentoJob sem tabela própria e com Shopee</name>
  <files>app/Jobs/EnviarRelatorioFechamentoJob.php</files>
  <read_first>
    - app/Jobs/EnviarRelatorioFechamentoJob.php (arquivo inteiro, 242 linhas — a constante `FAIXAS` nas linhas 33-40, o `calcularFaixa()` na ~220, o `whereNull('parent_company_id')` na 85 e a query só de `AdmanMetric` na 100)
    - app/Http/Controllers/AdminController.php já migrado (a versão de `gerarRelatorioGeral()` da Tarefa 1 é o molde exato do payload que este job monta)
    - app/Mail/RelatorioFechamentoMail.php (o que o email consome do payload)
  </read_first>
  <action>
Reescrever a montagem de dados do `handle()` para usar `FechamentoRollupService` e
`FechamentoFaixaResolver` resolvidos do container (`app(...)`) — o job continua `ShouldQueue` e
serializável, então não injetar serviços no construtor.

Remover a constante privada `FAIXAS` (linhas 33-40) e o método privado `calcularFaixa()` — o job
para de ter tabela própria. Remover o docblock que descreve o job como "espelho intencional" de
`gerarRelatorioGeral()` e substituí-lo por um que diga que ele lê a MESMA fonte central, citando a
razão: a pesquisa mediu a duplicação e ela produzia dois números para a mesma empresa.

Trocar `whereNull('parent_company_id')` + `filhas` pela mesma regra de grupo da Tarefa 1 (uma linha
por `CompanyGroup`, representada pela empresa-âncora, mais uma linha por empresa sem grupo), e
acrescentar `titulo` a cada item de `$relatorios`, exatamente como `gerarRelatorioGeral()` faz — as
views que o email renderiza são as mesmas.

Trocar a query que soma só `AdmanMetric` por `FechamentoRollupService::porEmpresa()`, o que fecha o
gap pré-existente de D-05: o email nunca incluiu Shopee. Quando a competência já está fechada, ler
`fechamento_snapshots`/`fechamento_grupo_snapshots` em vez de recalcular.

O agendamento em `routes/console.php` NÃO é alterado neste plano — a pesquisa registra que o valor
realmente configurado em produção (`email_envio_auto_dia` / `email_envio_auto_hora`) não foi
verificado, e mexer nele sem confirmar mudaria a rotina do Administrativo sem pedido.

Preservar `timeout = 120`, `tries = 2` e o comportamento de sair cedo com `Log::warning` quando não
há destinatários configurados.
  </action>
  <acceptance_criteria>
    - `grep -c "FAIXAS" app/Jobs/EnviarRelatorioFechamentoJob.php` retorna 0.
    - `grep -c "calcularFaixa" app/Jobs/EnviarRelatorioFechamentoJob.php` retorna 0.
    - `grep -c "parent_company_id" app/Jobs/EnviarRelatorioFechamentoJob.php` retorna 0.
    - Empresa com receita Shopee no mês aparece no payload do job com `faturamento` maior que zero (hoje apareceria como nulo).
    - Para a mesma competência e a mesma empresa, o `faturamento` e o `cobranca_mensal` do payload do job são idênticos aos da resposta de `/financeiro` (asserção de igualdade entre os dois no teste).
    - `routes/console.php` não aparece no diff deste plano.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137RelatoriosFechamentoTest</automated>
  </verify>
  <done>O email mensal lê a fonte central, inclui Shopee e não tem mais tabela de faixas própria.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Teste de convergência dos quatro consumidores</name>
  <files>tests/Feature/Phase137/Phase137RelatoriosFechamentoTest.php</files>
  <read_first>
    - tests/Feature/Phase137/Phase137FinanceiroPropsTest.php (plano 07 — os cenários de fixture a reutilizar)
    - tests/Feature/Phase14BladeRefactorTest.php (como o projeto testa as views de relatório)
    - app/Mail/RelatorioFechamentoMail.php (o shape do payload do email)
  </read_first>
  <action>
Criar `tests/Feature/Phase137/Phase137RelatoriosFechamentoTest.php` (namespace
`Tests\Feature\Phase137`, `use RefreshDatabase`) com um cenário-base compartilhado: 1 grupo com 2
empresas (uma só ML, uma com ML e Shopee), 1 empresa avulsa com `parent_company_id` apontando para
outra empresa e SEM grupo, faixas de Gestão semeadas, e métricas de um mês fechado.

Testes obrigatórios:
(a) para a mesma competência e a mesma entidade, o `faturamento` que sai de `/financeiro`, o que sai
de `gerarRelatorio()`, o que sai de `gerarRelatorioGeral()` e o que sai do payload de
`EnviarRelatorioFechamentoJob` são IGUAIS — é o teste que impede a duplicação de tabela de voltar;
(b) o mesmo para `cobranca_mensal`;
(c) a empresa com `parent_company_id` e sem grupo aparece como entidade própria nos três
consumidores (D-08/D-09);
(d) a empresa com Shopee tem `faturamento` maior que o valor só-ML no payload do email (o gap que
o job tinha);
(e) com a competência fechada, os quatro consumidores devolvem o valor congelado mesmo depois de
alterar `adman_metrics`;
(f) uma asserção de que nem `AdminController` nem `EnviarRelatorioFechamentoJob` contêm a string
`FAIXAS` (leitura do arquivo com `file_get_contents` e `assertStringNotContainsString`) — é a trava
de regressão contra a duplicação voltar.

Nenhuma asserção pode depender de texto impresso por comando.
  </action>
  <acceptance_criteria>
    - O teste (a) compara quatro valores e falha se qualquer um divergir por mais de 0.01.
    - O teste (f) passa com `assertStringNotContainsString('FAIXAS', ...)` nos dois arquivos.
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137RelatoriosFechamentoTest` termina com exit code 0.
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase14` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter="Phase137RelatoriosFechamentoTest|Phase14"</automated>
  </verify>
  <done>Existe uma trava automatizada provando que os quatro consumidores dão o mesmo número e que a constante duplicada não volta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job de email → destinatários externos | o PDF anexado sai da rede interna com faturamento de todos os clientes |
| PDF por empresa → cliente | número errado no PDF é cobrança errada documentada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-29 | Tampering | duas tabelas de faixas divergindo entre tela e email | mitigate | constante `FAIXAS` apagada dos dois arquivos e teste (f) com `assertStringNotContainsString` como trava de regressão; teste (a) compara os quatro consumidores |
| T-137-30 | Information Disclosure | relatório geral enviado por email para destinatário indevido | accept | comportamento pré-existente: a lista vem de `Configuracao::get('email_destinatarios_fechamento')`, editável só pela tela `role:admin`; esta fase não muda a lista nem o agendamento |
| T-137-31 | Repudiation | PDF gerado a partir de recálculo ao vivo de mês fechado | mitigate | os dois relatórios e o job leem `fechamento_snapshots` quando a competência está fechada; teste (e) prova que alterar `adman_metrics` não muda o PDF |
| T-137-32 | Denial of Service | job chamando a Adman por rede para ~190 empresas | mitigate | a chamada `fetchGrossBillingsBatch` sai do caminho; o rollup é 100% local sobre `adman_metrics` e `shopee_metrics` |
</threat_model>

<verification>
1. `"C:/xampp/php/php.exe" artisan test --filter="Phase137|Phase14|AdminFechamentoController"` — exit code 0.
2. `grep -rc "FAIXAS" app/Http/Controllers/AdminController.php app/Jobs/EnviarRelatorioFechamentoJob.php` — 0 nos dois.
3. `git diff --stat resources/views/admin/` — no máximo 2 linhas alteradas por view.
</verification>

<success_criteria>
- Os quatro consumidores do fechamento leem a mesma fonte e devolvem o mesmo número.
- A constante duplicada não existe mais e há teste que impede o retorno.
- O email mensal passa a incluir Shopee.
- Agendamento e destinatários do email permanecem exatamente como estavam.
</success_criteria>

<output>
Criar `.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-08-SUMMARY.md` ao concluir.
</output>
