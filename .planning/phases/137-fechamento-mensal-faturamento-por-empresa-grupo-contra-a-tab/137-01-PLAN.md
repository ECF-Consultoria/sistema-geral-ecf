---
phase: 137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_09_02_100001_create_servico_faixas_faturamento_table.php
  - database/migrations/2026_09_02_100002_create_empresa_faixas_faturamento_table.php
  - database/migrations/2026_09_02_100003_seed_faixas_fechamento_gestao.php
  - app/Models/ServicoFaixaFaturamento.php
  - app/Models/EmpresaFaixaFaturamento.php
  - app/Models/Servico.php
  - app/Models/Company.php
  - tests/Feature/Phase137/Phase137FaixasSchemaTest.php
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-13]

must_haves:
  truths:
    - "As faixas da tabela progressiva existem como dado no banco, não como constante PHP"
    - "Uma empresa pode ter faixas próprias que substituem inteiramente as do serviço"
    - "A tabela padrão de Gestão que o sistema aplica hoje (primeira faixa R$ 3.000) está semeada e é reconsultável no banco"
    - "Rodar as migrations duas vezes não duplica nem sobrescreve faixa editada à mão"
  artifacts:
    - path: "database/migrations/2026_09_02_100001_create_servico_faixas_faturamento_table.php"
      provides: "Tabela de faixas por serviço, com discriminador de contexto (fechamento | contrato)"
      contains: "sff_servico_contexto_ordem_unq"
    - path: "database/migrations/2026_09_02_100002_create_empresa_faixas_faturamento_table.php"
      provides: "Tabela de exceção de faixas por empresa (D-13, all-or-nothing)"
      contains: "eff_empresa_ordem_unq"
    - path: "database/migrations/2026_09_02_100003_seed_faixas_fechamento_gestao.php"
      provides: "Seed idempotente da tabela de Gestão vigente (D-02)"
      contains: "updateOrInsert"
    - path: "app/Models/ServicoFaixaFaturamento.php"
      provides: "Model auditável das faixas por serviço"
      contains: "LogsActivity"
    - path: "app/Models/EmpresaFaixaFaturamento.php"
      provides: "Model auditável das faixas por empresa"
      contains: "LogsActivity"
  key_links:
    - from: "app/Models/Company.php"
      to: "app/Models/EmpresaFaixaFaturamento.php"
      via: "relação hasMany faixasFaturamento()"
      pattern: "faixasFaturamento"
    - from: "app/Models/Servico.php"
      to: "app/Models/ServicoFaixaFaturamento.php"
      via: "relação hasMany faixasFaturamento()"
      pattern: "faixasFaturamento"
---

<objective>
Transformar a tabela progressiva em DADO estruturado: duas tabelas de faixas (por serviço e por
empresa), seus models auditáveis e o seed idempotente da tabela de Gestão vigente.

Purpose: sem as faixas como dado não existe fechamento nenhum — hoje elas só vivem como a constante
`FAIXAS`, duplicada em dois arquivos PHP, e como texto dentro dos `.docx` da Clicksign. É a fundação
de D-01, e é o que permite D-04 (cadastro manual) e D-13 (exceção all-or-nothing).

Output: 3 migrations, 2 models novos, 2 relações em models existentes, 1 teste de schema+seed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-PATTERNS.md
@.planning/learnings/desempenho-bonificacao.md

<interfaces>
Contratos que o executor precisa e NÃO deve ir procurar no código:

A tabela vigente (constante `AdminController::FAIXAS` + o ramo `default` de `calcularFaixa()`),
que é literalmente o que D-02 manda semear para o serviço `Gestão`:

  ordem 1 · limite_superior 499999.99   · valor 3000.00
  ordem 2 · limite_superior 999999.99   · valor 4500.00
  ordem 3 · limite_superior 1999999.99  · valor 6000.00
  ordem 4 · limite_superior 2999999.99  · valor 7500.00
  ordem 5 · limite_superior 3999999.99  · valor 9000.00
  ordem 6 · limite_superior 4999999.99  · valor 10500.00
  ordem 7 · limite_superior NULL        · valor 12000.00   (faixa máxima, sem teto)

`App\Models\Servico`: `$fillable` inclui `nome, valor_padrao, tipo_cobranca, ativo, setor,
plataforma, contrato_junto_com_servico_id`. Constantes `SETOR_PERFORMANCE = 'performance'`,
`SETOR_SHOPEE = 'shopee'`, `SETORES_FINANCEIROS = [performance, shopee]`. Relação já existente
`servicoDono(): BelongsTo` para `contrato_junto_com_servico_id`.

`App\Models\Company`: já tem `filhas()`, `pai()`, `grupo()` (belongsTo `CompanyGroup` por
`company_group_id`), `contratosServico()` (hasMany), e o accessor `getCustIdAttribute()`
(`adman_account_id ?: ml_store_id`).

⚠️ **Serviços que existem, MEDIDOS NO BANCO DE PRODUÇÃO em 2026-09-02 — migration NÃO é fonte
confiável para isto.** Neste projeto a configuração por serviço (`clicksign_template_id`,
`plataforma`, `clicksign_assinatura_posicionada`, `contrato_junto_com_servico_id`) é preenchida por
passo de PRODUÇÃO pós-deploy, direto no banco. Ler migration para saber quais serviços existem
produz conclusão errada.

Estado real dos serviços com tabela progressiva própria e plataforma definida:

  id 6   `Gestão`                ativo · exige_contrato · plataforma Mercado Livre · junto_com —
  id 9   `Gestão de ADS Shopee`  ativo · exige_contrato · plataforma Shopee        · junto_com 6
  id 10  `Brigada`               ativo · exige_contrato · plataforma Mercado Livre · junto_com —

`Brigada` É um serviço próprio (criado em 2026-08-26, com modelo de contrato próprio), e o
`HubspotLineItemMapping::paraNome('Brigada')` aponta para o serviço 10 — a migration
`2026_06_18_100004_seed_hubspot_line_item_mapping.php`, que diz "Brigada → Gestão", está OBSOLETA.
O serviço de Shopee chama-se `Gestão de ADS Shopee`, NÃO `Shopee` (o nome da migration
`2026_07_14_100002_seed_servico_shopee.php` também não reflete o estado atual). Serviços que exigem
contrato hoje são 8 no total.

Setores, também MEDIDOS EM PRODUÇÃO em 2026-09-02 (valores literais): `Gestão` (6) = `performance`,
`Gestão de ADS Shopee` (9) = `shopee`, `Brigada` (10) = `performance`.

O seed desta fase toca APENAS o serviço de nome `Gestão`. Tudo o que este plano precisa saber sobre
o catálogo já está medido acima — NÃO consultar o banco de produção nem o VPS para decidir nada
aqui. O executor não tem esse acesso (comandos de produção são bloqueados dentro de subagentes), e
a decisão já está tomada com os números deste bloco.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Migrations das duas tabelas de faixas</name>
  <files>database/migrations/2026_09_02_100001_create_servico_faixas_faturamento_table.php, database/migrations/2026_09_02_100002_create_empresa_faixas_faturamento_table.php</files>
  <read_first>
    - database/migrations/2026_07_09_140002_create_bonus_faixas_table.php (molde exato: guard de idempotência com `Schema::hasTable`, índice nomeado à mão, comentários em pt-BR)
    - database/migrations/2026_08_03_120000_create_desempenho_company_score_snapshots_table.php (convenção de nome curto de índice `dcss_...` e o comentário de "STRING sempre, nunca enum")
    - database/migrations/2026_06_11_120000_create_company_groups_table.php (ordem `nullable()` ANTES de `constrained()->nullOnDelete()`)
    - .planning/learnings/desempenho-bonificacao.md §6 e §10.1 (as três armadilhas de MariaDB que o SQLite dos testes não pega)
  </read_first>
  <action>
Criar `servico_faixas_faturamento` com: `id`; `servico_id` FK para `servicos` com
`constrained()->cascadeOnDelete()` e NOT NULL; `contexto` do tipo `string(20)` com default
`'fechamento'` — valores aceitos `'fechamento'` (a tabela que o fechamento aplica, D-02) e
`'contrato'` (a tabela que sai no contrato novo gerado pelo sistema, usada por D-03 no plano 04);
`ordem` `unsignedSmallInteger` NOT NULL; `limite_superior` `decimal(16,2)` `nullable()`, onde NULL
significa "sem teto" (última faixa) — NÃO usar 0 nem 999999999 como sentinela; `valor`
`decimal(10,2)` NOT NULL; `timestamps()`.

Criar `empresa_faixas_faturamento` com: `id`; `company_id` FK para `companies` com
`constrained()->cascadeOnDelete()` e NOT NULL (linha de exceção de empresa apagada não tem valor de
auditoria — mesma decisão de design de `desempenho_company_score_snapshots.company_id`); `ordem`
`unsignedSmallInteger`; `limite_superior` `decimal(16,2)` `nullable()`; `valor` `decimal(10,2)`;
`timestamps()`.

Índices — SEMPRE com nome explícito passado como segundo argumento (MariaDB recusa nome acima de 64
caracteres com erro 1059, o SQLite dos testes não pega, e a migration fica marcada como rodada com
o índice faltando):
- em `servico_faixas_faturamento`: `unique(['servico_id','contexto','ordem'], 'sff_servico_contexto_ordem_unq')`
  e `index(['servico_id','contexto'], 'sff_servico_contexto_idx')`.
- em `empresa_faixas_faturamento`: `unique(['company_id','ordem'], 'eff_empresa_ordem_unq')`.

`contexto` é `string()`, NUNCA `enum()` — o CHECK do enum é enforçado no SQLite dos testes e quebra
ao surgir valor novo (armadilha já registrada no projeto).

As duas migrations abrem com o guard `if (Schema::hasTable('<tabela>')) { return; }`, igual ao de
`bonus_faixas`. `down()` usa `Schema::dropIfExists`. Comentários em pt-BR, incluindo a nota de por
que o índice é nomeado à mão e por que `contexto` é string.
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*//' database/migrations/2026_09_02_100001_create_servico_faixas_faturamento_table.php | grep -c "sff_servico_contexto_ordem_unq"` retorna 1 ou mais.
    - `grep -c "enum(" database/migrations/2026_09_02_1000*.php` retorna 0 para as duas migrations.
    - Nenhum nome de índice declarado nas duas migrations excede 64 caracteres (verificável com `awk`/`wc -c` sobre as strings citadas).
    - Após `php artisan migrate`, `Schema::hasTable('servico_faixas_faturamento')` e `Schema::hasTable('empresa_faixas_faturamento')` são verdadeiros.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest</automated>
  </verify>
  <done>As duas tabelas existem após `migrate`, com índices nomeados curtos e sem coluna `enum`.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Models auditáveis e relações</name>
  <files>app/Models/ServicoFaixaFaturamento.php, app/Models/EmpresaFaixaFaturamento.php, app/Models/Servico.php, app/Models/Company.php</files>
  <read_first>
    - app/Models/BonusFaixa.php (molde: `LogsActivity`, `getActivitylogOptions()` com `logOnlyDirty()`, scope `ordenadas()`, método `classificar()`)
    - app/Models/Servico.php (onde encaixar a relação — logo abaixo de `servicoDono()`)
    - app/Models/Company.php (onde encaixar a relação — próximo de `contratosServico()`, linha ~422)
  </read_first>
  <action>
Criar `App\Models\ServicoFaixaFaturamento` com `HasFactory` e `LogsActivity`;
`$fillable = ['servico_id','contexto','ordem','limite_superior','valor']`; casts `ordem` para `int`,
`limite_superior` e `valor` para `decimal:2`. Constantes públicas `CONTEXTO_FECHAMENTO =
'fechamento'` e `CONTEXTO_CONTRATO = 'contrato'`. Relação `servico(): BelongsTo`. Scope
`ordenadas($query)` que faz `orderBy('ordem')`. Scope `deFechamento($query)` que filtra
`contexto = CONTEXTO_FECHAMENTO`. `getActivitylogOptions()` com `logOnlyDirty()`,
`dontSubmitEmptyLogs()`, `useLogName('faixa_faturamento')` e descrição em pt-BR citando serviço,
contexto e ordem — D-01 registra que o valor da faixa entra em contrato e precisa ser auditável.

Criar `App\Models\EmpresaFaixaFaturamento` com a mesma estrutura, `$fillable =
['company_id','ordem','limite_superior','valor']`, relação `company(): BelongsTo`, scope
`ordenadas()`, `useLogName('faixa_faturamento')`.

Em `App\Models\Servico`, adicionar `faixasFaturamento(): HasMany` para `ServicoFaixaFaturamento`.
Em `App\Models\Company`, adicionar `faixasFaturamento(): HasMany` para `EmpresaFaixaFaturamento`.
Ambas com docblock em pt-BR explicitando D-13: a existência de QUALQUER linha em
`empresa_faixas_faturamento` substitui a tabela inteira do serviço, nunca linha a linha.

Não colocar lógica de classificação de faixa nos models — ela mora no resolver do plano 03, para
existir num lugar só.
  </action>
  <acceptance_criteria>
    - `ServicoFaixaFaturamento::CONTEXTO_FECHAMENTO` e `::CONTEXTO_CONTRATO` existem e valem `'fechamento'` e `'contrato'`.
    - `(new Company)->faixasFaturamento()` e `(new Servico)->faixasFaturamento()` retornam instância de `Illuminate\Database\Eloquent\Relations\HasMany`.
    - Criar e depois alterar uma linha de `ServicoFaixaFaturamento` grava linha em `activity_log` com `log_name = 'faixa_faturamento'` (conferido por reconsulta à tabela `activity_log`, não por saída de comando).
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest</automated>
  </verify>
  <done>Models criados com auditoria ativa e as duas relações resolvem sem erro.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Seed idempotente da tabela de Gestão vigente + teste</name>
  <files>database/migrations/2026_09_02_100003_seed_faixas_fechamento_gestao.php, tests/Feature/Phase137/Phase137FaixasSchemaTest.php</files>
  <read_first>
    - database/migrations/2026_07_09_140003_seed_bonus_faixas_iniciais.php (molde de seed idempotente por `updateOrInsert`)
    - database/migrations/2026_05_27_100001_seed_servicos_catalog.php (nomes canônicos já semeados, e por que usa `firstOrCreate` e não `updateOrCreate`)
    - app/Http/Controllers/AdminController.php linhas 23-29 e 837-845 (a constante `FAIXAS` e o ramo `maxima` de `calcularFaixa()` — a fonte dos 7 valores)
  </read_first>
  <action>
Criar migration de seed que localiza o `Servico` de `nome` exatamente `'Gestão'` e, se ele existir,
insere as 7 linhas de `servico_faixas_faturamento` com `contexto = 'fechamento'` e exatamente os
valores listados no bloco `<interfaces>` deste plano (ordem 1 a 7; ordem 7 com `limite_superior`
NULL e `valor` 12000.00). Se o serviço `Gestão` não existir, a migration é no-op silencioso — nunca
cria serviço.

Idempotência por `DB::table('servico_faixas_faturamento')->updateOrInsert(['servico_id' => $id,
'contexto' => 'fechamento', 'ordem' => $n], [...])`, gravando `created_at`/`updated_at` com `now()`.
Rodar duas vezes não pode duplicar linha nem alterar contagem.

Não semear as faixas de `Gestão de ADS Shopee` (id 9) nem de `Brigada` (id 10): os valores dessas
duas tabelas só existem como texto dentro dos `.docx` (`modelo-contrato-shopee-COM-VARIAVEIS.docx`,
`modelo-contrato-brigada-v3.docx`) e não estão disponíveis como dado — inventá-los gravaria número
errado numa tabela que decide cobrança. As duas são tabelas **DE SERVIÇO**, iguais em natureza à de
Gestão, e entram pelo cadastro de D-04 na rota `admin.financeiro.faixas.servico` (planos 06 e 09) —
NÃO são exceção por empresa. São, portanto, TRÊS tabelas de serviço no total: Gestão (semeada aqui),
Shopee e Brigada (cadastradas pela tela, no checkpoint do plano 10). Até serem cadastradas, as
empresas desses dois serviços aparecem como `A DEFINIR` na tela, que é o comportamento desenhado no
UI-SPEC. Registrar isso como comentário em pt-BR no topo da migration.

`down()` não apaga nada (mesma disciplina de `seed_servicos_catalog`): comentário explicando que
remover faixas quebraria fechamento já congelado.

Criar `tests/Feature/Phase137/Phase137FaixasSchemaTest.php` (namespace `Tests\Feature\Phase137`,
`use RefreshDatabase`) cobrindo: (a) as duas tabelas existem e têm as colunas declaradas;
(b) `contexto` aceita `'contrato'` sem violar CHECK — provando que não virou `enum`;
(c) as 7 faixas de Gestão existem com `contexto='fechamento'`, a ordem 1 vale 3000.00 e a ordem 7
tem `limite_superior` nulo e valor 12000.00 — conferido por `DB::table(...)` (reconsulta), não pelo
retorno do seed; (d) inserir duas linhas com o mesmo `(servico_id, contexto, ordem)` estoura
`QueryException` (unique ativo); (e) `Company::faixasFaturamento()` e `Servico::faixasFaturamento()`
devolvem as linhas criadas.
  </action>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM servico_faixas_faturamento WHERE contexto='fechamento'` retorna 7 após uma migração limpa com o serviço `Gestão` presente.
    - O valor da `ordem = 1` é exatamente `3000.00` e o da `ordem = 7` é `12000.00` com `limite_superior IS NULL`.
    - O teste `Phase137FaixasSchemaTest` passa e nenhuma de suas asserções lê stdout de comando.
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest</automated>
  </verify>
  <done>Seed grava 7 faixas de Gestão, roda duas vezes sem duplicar, e o teste de schema está verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → banco de produção | DDL executado com privilégio total no MariaDB; erro de nome de índice deixa tabela sem constraint |
| seed → valores de cobrança | número semeado errado vira mensalidade cobrada errada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Tampering | `servico_faixas_faturamento` / `empresa_faixas_faturamento` | mitigate | `LogsActivity` nos dois models com `useLogName('faixa_faturamento')`; toda alteração de valor de faixa fica em `activity_log` |
| T-137-02 | Denial of Service | migration em MariaDB | mitigate | índices nomeados à mão e curtos + guard `Schema::hasTable` idempotente; evita o erro 1059 que já deixou tabela sem índice em produção |
| T-137-03 | Repudiation | seed de valores de cobrança | mitigate | valores semeados vêm literalmente de `AdminController::FAIXAS` (D-02) e são conferidos por reconsulta ao banco no teste, não por saída do comando |
| T-137-SC | Tampering | npm/pip/composer installs | accept | esta fase não instala nenhum pacote — RESEARCH.md registra "Package Legitimacy Audit não se aplica"; nada a verificar |
</threat_model>

<verification>
1. `"C:/xampp/php/php.exe" artisan migrate:fresh --env=testing` roda sem erro.
2. `"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest` — exit code 0.
3. `"C:/xampp/php/php.exe" artisan test --filter=AdminFechamentoControllerTest` — exit code 0 (nada foi tocado no comportamento atual da tela ainda).
</verification>

<success_criteria>
- Duas tabelas de faixas existem com índices nomeados curtos e sem coluna `enum`.
- Os 7 valores da tabela vigente de Gestão estão no banco e batem com `AdminController::FAIXAS`.
- Migrations rodam duas vezes sem duplicar linha.
- Nenhum comportamento observável da tela `/financeiro` mudou neste plano.
</success_criteria>

<output>
Criar `.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-01-SUMMARY.md` ao concluir.
</output>
