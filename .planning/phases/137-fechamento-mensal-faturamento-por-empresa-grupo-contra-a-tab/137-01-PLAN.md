---
phase: 137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_09_02_100001_create_servico_faixas_faturamento_table.php
  - database/migrations/2026_09_02_100002_create_empresa_faixas_faturamento_table.php
  - database/migrations/2026_09_02_100003_seed_faixas_faturamento_iniciais.php
  - app/Models/ServicoFaixaFaturamento.php
  - app/Models/EmpresaFaixaFaturamento.php
  - app/Models/Servico.php
  - app/Models/Company.php
  - tests/Feature/Phase137/Phase137FaixasSchemaTest.php
autonomous: true
requirements: [D-01, D-02, D-02b, D-04, D-13]

must_haves:
  truths:
    - "As faixas da tabela progressiva existem como dado no banco, não como constante PHP"
    - "As três tabelas medidas nos modelos publicados (Gestão, Brigada e Shopee) estão no banco e são reconsultáveis"
    - "Uma empresa pode ter faixas próprias que substituem inteiramente as do serviço"
    - "A última faixa de Gestão/Brigada é reconhecível como PISO (a partir de R$ 12.000), não como valor fechado"
    - "Rodar as migrations duas vezes não duplica nem sobrescreve faixa editada à mão"
  artifacts:
    - path: "database/migrations/2026_09_02_100001_create_servico_faixas_faturamento_table.php"
      provides: "Tabela de faixas por serviço, com a marcação de valor-piso"
      contains: "sff_servico_ordem_unq"
    - path: "database/migrations/2026_09_02_100002_create_empresa_faixas_faturamento_table.php"
      provides: "Tabela de exceção de faixas por empresa (D-13, all-or-nothing)"
      contains: "eff_empresa_ordem_unq"
    - path: "database/migrations/2026_09_02_100003_seed_faixas_faturamento_iniciais.php"
      provides: "Seed idempotente das três tabelas medidas (D-02b)"
      contains: "updateOrInsert"
    - path: "app/Models/ServicoFaixaFaturamento.php"
      provides: "Model auditável das faixas por serviço"
      contains: "LogsActivity"
    - path: "app/Models/EmpresaFaixaFaturamento.php"
      provides: "Model auditável das faixas por empresa"
      contains: "LogsActivity"
  key_links:
    - from: "app/Models/Company.php"
      to: "app/Models/EmpresaFaixaFaturamento.php"
      via: "relação hasMany faixasFaturamento()"
      pattern: "faixasFaturamento"
    - from: "app/Models/Servico.php"
      to: "app/Models/ServicoFaixaFaturamento.php"
      via: "relação hasMany faixasFaturamento()"
      pattern: "faixasFaturamento"
---

<objective>
Transformar a tabela progressiva em DADO estruturado: duas tabelas de faixas (por serviço e por
empresa), seus models auditáveis e o seed idempotente das TRÊS tabelas medidas nos modelos
publicados na Clicksign.

Purpose: sem as faixas como dado não existe fechamento nenhum — hoje elas só vivem como a constante
`FAIXAS`, duplicada em dois arquivos PHP, e como texto dentro dos modelos da Clicksign. É a
fundação de D-01, o dado de D-02b, e é o que permite D-04 (cadastro manual) e D-13 (exceção
all-or-nothing).

Output: 3 migrations, 2 models novos, 2 relações em models existentes, 1 teste de schema e seed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-PATTERNS.md
@.planning/learnings/desempenho-bonificacao.md

<interfaces>
Contratos que o executor precisa e NÃO deve ir procurar no código.

⚠️ **Serviços MEDIDOS NO BANCO DE PRODUÇÃO em 2026-09-02 — migration NÃO é fonte confiável para
saber quais serviços existem.** Neste projeto a configuração por serviço (`clicksign_template_id`,
`plataforma`, `clicksign_assinatura_posicionada`, `contrato_junto_com_servico_id`) é preenchida por
passo de produção pós-deploy, direto no banco. Valores literais:

  id 6   `Gestão`                setor `performance` · plataforma Mercado Livre · junto_com —
  id 9   `Gestão de ADS Shopee`  setor `shopee`      · plataforma Shopee        · junto_com 6
  id 10  `Brigada`               setor `performance` · plataforma Mercado Livre · junto_com —

`Brigada` É um serviço próprio (criado em 2026-08-26, com modelo de contrato próprio) — a migration
`2026_06_18_100004` que diz "Brigada → Gestão" está OBSOLETA. O serviço de Shopee chama-se
`Gestão de ADS Shopee`, NÃO `Shopee`. São 8 serviços exigindo contrato no total.

⚠️ **AS TRÊS TABELAS DE FAIXAS, MEDIDAS NOS MODELOS PUBLICADOS NA CLICKSIGN em 2026-09-02 (D-02b).**
Fonte autoritativa: o modelo publicado, NÃO os `.docx` locais da raiz do repositório, que estão
desatualizados (dizem R$ 3.500 na primeira faixa de Gestão; o modelo publicado diz R$ 3.000, e o
usuário confirmou em 2026-09-02 que R$ 3.000 é o valor comercial correto — D-02). Usar os números
abaixo, literalmente; não abrir nem reinterpretar os `.docx`.

`Gestão` (id 6) e `Brigada` (id 10) — 7 faixas, tabelas IDÊNTICAS entre si e idênticas à constante
`AdminController::FAIXAS` já vigente:

  ordem 1 · limite_superior 499999.99   · valor 3000.00  · valor_e_piso false
  ordem 2 · limite_superior 999999.99   · valor 4500.00  · valor_e_piso false
  ordem 3 · limite_superior 1999999.99  · valor 6000.00  · valor_e_piso false
  ordem 4 · limite_superior 2999999.99  · valor 7500.00  · valor_e_piso false
  ordem 5 · limite_superior 3999999.99  · valor 9000.00  · valor_e_piso false
  ordem 6 · limite_superior 4999999.99  · valor 10500.00 · valor_e_piso false
  ordem 7 · limite_superior NULL        · valor 12000.00 · valor_e_piso TRUE

`Gestão de ADS Shopee` (id 9) — 8 faixas, todas em "Até", nenhuma "A partir de":

  ordem 1 · limite_superior 50000.00    · valor 1500.00  · valor_e_piso false
  ordem 2 · limite_superior 150000.00   · valor 2000.00  · valor_e_piso false
  ordem 3 · limite_superior 250000.00   · valor 2500.00  · valor_e_piso false
  ordem 4 · limite_superior 500000.00   · valor 3000.00  · valor_e_piso false
  ordem 5 · limite_superior 1000000.00  · valor 3500.00  · valor_e_piso false
  ordem 6 · limite_superior 1500000.00  · valor 4000.00  · valor_e_piso false
  ordem 7 · limite_superior 2000000.00  · valor 4500.00  · valor_e_piso false
  ordem 8 · limite_superior 3000000.00  · valor 5000.00  · valor_e_piso false

⚠️ **Duas particularidades do dado medido, ambas deliberadas e NÃO devem ser "corrigidas":**
1. A última faixa de Gestão/Brigada é **"a partir de R$ 12.000"** — piso, não preço fechado. Daí a
   coluna `valor_e_piso`. Sem ela o sistema cobraria exatamente R$ 12.000 de quem deveria pagar mais.
2. **Shopee NÃO tem faixa aberta:** a última band tem teto (R$ 3.000.000). Empresa Shopee acima
   disso não casa em faixa nenhuma. Isso é o dado real do modelo publicado — não inventar uma oitava
   faixa sem teto. O tratamento é ausência visível (plano 03 devolve `null`, a tela mostra
   `A DEFINIR`), e o ponto está na lista de conferência do checkpoint do plano 10.
3. Os tetos de Gestão/Brigada usam a convenção já vigente da constante `FAIXAS` (`499999.99` para a
   band "Até R$ 500.000"), preservando exatamente o comportamento de cobrança atual; os de Shopee
   usam o valor citado como teto inclusivo (`50000.00` para "Até R$ 50.000"). A divergência de um
   centavo entre as duas convenções é conhecida e está na conferência do plano 10 — NÃO uniformizar
   por conta própria: mudar os tetos de Gestão moveria empresas de faixa e alteraria cobrança.


`App\Models\Servico`: `$fillable` inclui `nome, valor_padrao, tipo_cobranca, ativo, setor,
plataforma, contrato_junto_com_servico_id`. Constantes `SETOR_PERFORMANCE = 'performance'`,
`SETOR_SHOPEE = 'shopee'`, `SETORES_FINANCEIROS = [performance, shopee]`. Relação já existente
`servicoDono(): BelongsTo` para `contrato_junto_com_servico_id`.

`App\Models\Company`: já tem `filhas()`, `pai()`, `grupo()` (belongsTo `CompanyGroup` por
`company_group_id`), `contratosServico()` (hasMany), e o accessor `getCustIdAttribute()`
(`adman_account_id ?: ml_store_id`).

Tudo o que este plano precisa saber já está medido acima — NÃO consultar o banco de produção nem o
VPS para decidir nada aqui. O executor não tem esse acesso (comandos de produção são bloqueados
dentro de subagentes) e a decisão já está tomada com os números deste bloco.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Migrations das duas tabelas de faixas</name>
  <files>database/migrations/2026_09_02_100001_create_servico_faixas_faturamento_table.php, database/migrations/2026_09_02_100002_create_empresa_faixas_faturamento_table.php</files>
  <read_first>
    - database/migrations/2026_07_09_140002_create_bonus_faixas_table.php (molde exato: guard de idempotência com `Schema::hasTable`, índice nomeado à mão, comentários em pt-BR)
    - database/migrations/2026_08_03_120000_create_desempenho_company_score_snapshots_table.php (convenção de nome curto de índice `dcss_...` e o comentário de "STRING sempre, nunca enum")
    - database/migrations/2026_06_11_120000_create_company_groups_table.php (ordem `nullable()` ANTES de `constrained()->nullOnDelete()`)
    - .planning/learnings/desempenho-bonificacao.md §6 e §10.1 (as três armadilhas de MariaDB que o SQLite dos testes não pega)
  </read_first>
  <action>
Criar `servico_faixas_faturamento` com: `id`; `servico_id` FK para `servicos` com
`constrained()->cascadeOnDelete()` e NOT NULL; `ordem` `unsignedSmallInteger` NOT NULL;
`limite_superior` `decimal(16,2)` `nullable()`, onde NULL significa "sem teto" (faixa aberta) — NÃO
usar 0 nem 999999999 como sentinela; `valor` `decimal(10,2)` NOT NULL; `valor_e_piso` `boolean`
com default `false`; `timestamps()`.

`valor_e_piso` marca a faixa cujo valor no contrato é "a partir de R$ X", e não um preço fechado —
é o caso da última faixa de Gestão e de Brigada ("acima de R$ 5.000.000 → a partir de R$ 12.000").
Sem essa coluna o sistema cobraria exatamente R$ 12.000 de quem deveria pagar mais. Registrar essa
razão como comentário em pt-BR na migration.

Criar `empresa_faixas_faturamento` com: `id`; `company_id` FK para `companies` com
`constrained()->cascadeOnDelete()` e NOT NULL (linha de exceção de empresa apagada não tem valor de
auditoria — mesma decisão de design de `desempenho_company_score_snapshots.company_id`); `ordem`
`unsignedSmallInteger`; `limite_superior` `decimal(16,2)` `nullable()`; `valor` `decimal(10,2)`;
`valor_e_piso` `boolean` default `false`; `timestamps()`.

Índices — SEMPRE com nome explícito passado como segundo argumento (MariaDB recusa nome acima de 64
caracteres com erro 1059, o SQLite dos testes não pega, e a migration fica marcada como rodada com
o índice faltando):
- em `servico_faixas_faturamento`: `unique(['servico_id','ordem'], 'sff_servico_ordem_unq')`.
- em `empresa_faixas_faturamento`: `unique(['company_id','ordem'], 'eff_empresa_ordem_unq')`.

Nenhuma coluna de lista fixa é `enum()` — não há nenhuma nestas duas tabelas, e caso venha a surgir,
usar `string()`: o CHECK do enum é enforçado no SQLite dos testes e quebra ao surgir valor novo.

As duas migrations abrem com o guard `if (Schema::hasTable('<tabela>')) { return; }`, igual ao de
`bonus_faixas`. `down()` usa `Schema::dropIfExists`. Comentários em pt-BR, incluindo a nota de por
que o índice é nomeado à mão e o que `valor_e_piso` significa.
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*//' database/migrations/2026_09_02_100001_create_servico_faixas_faturamento_table.php | grep -c "sff_servico_ordem_unq"` retorna 1 ou mais.
    - `grep -c "enum(" database/migrations/2026_09_02_1000*.php` retorna 0 para as duas migrations.
    - Nenhum nome de índice declarado nas duas migrations excede 64 caracteres.
    - As duas tabelas têm a coluna `valor_e_piso` do tipo boolean com default false.
    - Após `php artisan migrate`, `Schema::hasTable('servico_faixas_faturamento')` e `Schema::hasTable('empresa_faixas_faturamento')` são verdadeiros.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest</automated>
  </verify>
  <done>As duas tabelas existem após `migrate`, com índices nomeados curtos, sem coluna `enum` e com a marcação de valor-piso.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Models auditáveis e relações</name>
  <files>app/Models/ServicoFaixaFaturamento.php, app/Models/EmpresaFaixaFaturamento.php, app/Models/Servico.php, app/Models/Company.php</files>
  <read_first>
    - app/Models/BonusFaixa.php (molde: `LogsActivity`, `getActivitylogOptions()` com `logOnlyDirty()`, scope `ordenadas()`, método `classificar()`)
    - app/Models/Servico.php (onde encaixar a relação — logo abaixo de `servicoDono()`)
    - app/Models/Company.php (onde encaixar a relação — próximo de `contratosServico()`, linha ~422)
  </read_first>
  <action>
Criar `App\Models\ServicoFaixaFaturamento` com `HasFactory` e `LogsActivity`;
`$fillable = ['servico_id','ordem','limite_superior','valor','valor_e_piso']`; casts `ordem` para
`int`, `limite_superior` e `valor` para `decimal:2`, `valor_e_piso` para `bool`. Relação
`servico(): BelongsTo`. Scope `ordenadas($query)` que faz `orderBy('ordem')`.
`getActivitylogOptions()` com `logOnlyDirty()`, `dontSubmitEmptyLogs()`,
`useLogName('faixa_faturamento')` e descrição em pt-BR citando serviço e ordem — D-01 registra que
o valor da faixa entra em contrato e precisa ser auditável.

Criar `App\Models\EmpresaFaixaFaturamento` com a mesma estrutura, `$fillable =
['company_id','ordem','limite_superior','valor','valor_e_piso']`, relação `company(): BelongsTo`,
scope `ordenadas()`, `useLogName('faixa_faturamento')`.

Em `App\Models\Servico`, adicionar `faixasFaturamento(): HasMany` para `ServicoFaixaFaturamento`.
Em `App\Models\Company`, adicionar `faixasFaturamento(): HasMany` para `EmpresaFaixaFaturamento`.
Ambas com docblock em pt-BR explicitando D-13: a existência de QUALQUER linha em
`empresa_faixas_faturamento` substitui a tabela inteira do serviço, nunca linha a linha.

Não colocar lógica de classificação de faixa nos models — ela mora no resolver do plano 03, para
existir num lugar só.
  </action>
  <acceptance_criteria>
    - `(new Company)->faixasFaturamento()` e `(new Servico)->faixasFaturamento()` retornam instância de `Illuminate\Database\Eloquent\Relations\HasMany`.
    - `valor_e_piso` está em `$fillable` e no `$casts` como `bool` nos dois models.
    - Criar e depois alterar uma linha de `ServicoFaixaFaturamento` grava linha em `activity_log` com `log_name = 'faixa_faturamento'` (conferido por reconsulta à tabela `activity_log`, não por saída de comando).
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest</automated>
  </verify>
  <done>Models criados com auditoria ativa e as duas relações resolvem sem erro.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Seed idempotente das três tabelas medidas + teste</name>
  <files>database/migrations/2026_09_02_100003_seed_faixas_faturamento_iniciais.php, tests/Feature/Phase137/Phase137FaixasSchemaTest.php</files>
  <read_first>
    - database/migrations/2026_07_09_140003_seed_bonus_faixas_iniciais.php (molde de seed idempotente por `updateOrInsert`)
    - database/migrations/2026_05_27_100001_seed_servicos_catalog.php (por que usa `firstOrCreate` e não `updateOrCreate`, e por que `down()` não apaga)
    - app/Http/Controllers/AdminController.php linhas 23-29 e 837-845 (a constante `FAIXAS` — confirma que a tabela de Gestão medida é idêntica à vigente)
  </read_first>
  <action>
Criar a migration de seed que popula as TRÊS tabelas de serviço com os valores literais do bloco
`<interfaces>` deste plano (D-02b, medidos nos modelos publicados na Clicksign em 2026-09-02).

Resolução dos serviços por NOME exato, nunca por id hardcoded (o id é dado de produção e pode
divergir em outros ambientes): `Gestão`, `Brigada` e `Gestão de ADS Shopee`. Se algum deles não
existir no ambiente, a migration pula APENAS esse serviço, em silêncio — nunca cria serviço.

`Gestão` e `Brigada` recebem a MESMA tabela de 7 faixas (elas são idênticas nos modelos publicados):
montar o array uma vez e aplicá-lo aos dois serviços, com um comentário em pt-BR registrando que a
identidade é fato medido, não coincidência de digitação. `Gestão de ADS Shopee` recebe as 8 faixas
próprias. A ordem 7 de Gestão/Brigada é a única linha com `limite_superior` nulo e
`valor_e_piso = true`.

Idempotência por `DB::table('servico_faixas_faturamento')->updateOrInsert(['servico_id' => $id,
'ordem' => $n], [...])`, gravando `created_at`/`updated_at` com `now()`. Rodar duas vezes não pode
duplicar linha nem alterar contagem.

Não semear nada em `empresa_faixas_faturamento`: exceção por empresa é cadastro manual (D-04), para
tabelas antigas ou fora do padrão, e inventar exceção mudaria cobrança de empresa real.

`down()` não apaga nada (mesma disciplina de `seed_servicos_catalog`): comentário explicando que
remover faixas quebraria fechamento já congelado.

Criar `tests/Feature/Phase137/Phase137FaixasSchemaTest.php` (namespace `Tests\Feature\Phase137`,
`use RefreshDatabase`) cobrindo: (a) as duas tabelas existem e têm as colunas declaradas, incluindo
`valor_e_piso`; (b) as 7 faixas de `Gestão` existem, a ordem 1 vale 3000.00 e a ordem 7 tem
`limite_superior` nulo, valor 12000.00 e `valor_e_piso` verdadeiro — conferido por `DB::table(...)`
(reconsulta), não pelo retorno do seed; (c) as faixas de `Brigada` são IDÊNTICAS às de `Gestão`
(comparação linha a linha de ordem, limite e valor); (d) `Gestão de ADS Shopee` tem 8 faixas, a
ordem 1 vale 1500.00, a ordem 8 vale 5000.00 e NENHUMA delas tem `limite_superior` nulo nem
`valor_e_piso` verdadeiro; (e) inserir duas linhas com o mesmo `(servico_id, ordem)` estoura
`QueryException`; (f) `Company::faixasFaturamento()` e `Servico::faixasFaturamento()` devolvem as
linhas criadas; (g) rodar o seed duas vezes mantém as contagens em 7, 7 e 8.
  </action>
  <acceptance_criteria>
    - `SELECT COUNT(*) FROM servico_faixas_faturamento` retorna 22 após migração limpa com os três serviços presentes (7 + 7 + 8).
    - A `ordem = 7` de Gestão e de Brigada tem `limite_superior IS NULL`, `valor = 12000.00` e `valor_e_piso` verdadeiro.
    - Nenhuma faixa de `Gestão de ADS Shopee` tem `limite_superior IS NULL` (asserção explícita — a tabela de Shopee não tem faixa aberta, e isso é o dado real).
    - As 7 linhas de Brigada batem uma a uma com as 7 de Gestão em `ordem`, `limite_superior` e `valor`.
    - Rodar o seed duas vezes não altera as contagens.
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest</automated>
  </verify>
  <done>As três tabelas medidas estão no banco, o seed roda duas vezes sem duplicar, e o teste de schema está verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → banco de produção | DDL executado com privilégio total no MariaDB; erro de nome de índice deixa tabela sem constraint |
| seed → valores de cobrança | número semeado errado vira mensalidade cobrada errada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-01 | Tampering | `servico_faixas_faturamento` / `empresa_faixas_faturamento` | mitigate | `LogsActivity` nos dois models com `useLogName('faixa_faturamento')`; toda alteração de valor de faixa fica em `activity_log` |
| T-137-02 | Denial of Service | migration em MariaDB | mitigate | índices nomeados à mão e curtos + guard `Schema::hasTable` idempotente; evita o erro 1059 que já deixou tabela sem índice em produção |
| T-137-03 | Repudiation | seed de valores de cobrança | mitigate | valores vêm dos modelos PUBLICADOS na Clicksign, medidos em 2026-09-02 (D-02b), e são conferidos por reconsulta ao banco no teste, não por saída do comando; os `.docx` locais, desatualizados, não são fonte |
| T-137-41 | Tampering | faixa-piso tratada como preço fechado | mitigate | coluna `valor_e_piso` marca "a partir de R$ 12.000"; teste afirma que a ordem 7 de Gestão e Brigada tem a marcação, impedindo cobrança a menor |
| T-137-SC | Tampering | npm/pip/composer installs | accept | esta fase não instala nenhum pacote — RESEARCH.md registra "Package Legitimacy Audit não se aplica"; nada a verificar |
</threat_model>

<verification>
1. `"C:/xampp/php/php.exe" artisan migrate:fresh --env=testing` roda sem erro.
2. `"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasSchemaTest` — exit code 0.
3. `"C:/xampp/php/php.exe" artisan test --filter=AdminFechamentoControllerTest` — exit code 0 (nada foi tocado no comportamento atual da tela ainda).
</verification>

<success_criteria>
- Duas tabelas de faixas existem com índices nomeados curtos e com a marcação de valor-piso.
- As três tabelas medidas (Gestão, Brigada e Shopee) estão no banco e batem com os modelos publicados.
- Migrations rodam duas vezes sem duplicar linha.
- Nenhum comportamento observável da tela `/financeiro` mudou neste plano.
</success_criteria>

<output>
Criar `.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-01-SUMMARY.md` ao concluir.
</output>
