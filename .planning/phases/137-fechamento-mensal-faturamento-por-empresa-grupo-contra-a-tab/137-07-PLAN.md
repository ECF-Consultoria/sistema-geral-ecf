---
phase: 137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab
plan: 07
type: execute
wave: 4
depends_on: ["137-03", "137-05"]
files_modified:
  - app/Http/Controllers/AdminController.php
  - tests/Feature/AdminFechamentoControllerTest.php
  - tests/Feature/Phase14FechamentoUiTest.php
  - tests/Feature/Phase137/Phase137FinanceiroPropsTest.php
autonomous: true
requirements: [D-02, D-06, D-08, D-09, D-10, D-11]

must_haves:
  truths:
    - "A tela /financeiro mostra faturamento de mês-calendário, nunca de janela móvel de 30 dias"
    - "As linhas de grupo da tela vêm dos grupos do Comercial, não de parent_company_id"
    - "Mês já fechado exibe o valor congelado, não um recálculo ao vivo"
    - "A faixa exibida vem da tabela do banco, não da constante FAIXAS"
    - "A progressão mensal não tem coluna acumulada"
  artifacts:
    - path: "app/Http/Controllers/AdminController.php"
      provides: "fechamento() lendo rollup+resolver+snapshot e agregando por CompanyGroup"
      contains: "FechamentoRollupService"
    - path: "tests/Feature/Phase137/Phase137FinanceiroPropsTest.php"
      provides: "Contrato das props novas da tela"
  key_links:
    - from: "app/Http/Controllers/AdminController.php"
      to: "app/Services/Fechamento/FechamentoRollupService.php"
      via: "injeção no construtor e porEmpresa() na competência selecionada"
      pattern: "FechamentoRollupService"
    - from: "app/Http/Controllers/AdminController.php"
      to: "app/Models/FechamentoSnapshot.php"
      via: "leitura da competência congelada quando o mês já foi fechado"
      pattern: "FechamentoSnapshot"
    - from: "app/Http/Controllers/AdminController.php"
      to: "app/Models/CompanyGroup.php"
      via: "agregação de grupo por company_group_id"
      pattern: "CompanyGroup"
---

<objective>
Migrar o primeiro e mais visível dos quatro call-sites: `AdminController::fechamento()`, a rota
`/financeiro`.

Purpose: é onde as quatro mudanças pedidas pelo usuário se tornam visíveis — acaba o acumulativo e a
janela móvel (D-06), os grupos passam a ser os do Comercial (D-08/D-10), a faixa passa a vir do
banco (D-02 via plano 01) e o mês fechado passa a ler o congelado (D-11).

Output: `fechamento()` reescrito, 2 suítes existentes atualizadas, 1 suíte nova de contrato de props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-RESEARCH.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-UI-SPEC.md
@.planning/learnings/desempenho-bonificacao.md

<interfaces>
Contratos que o executor precisa e NÃO deve ir procurar no código:

Dos planos 03 e 05 (assinaturas já fixadas):
- `FechamentoRollupService::janela(string $mes): array` → `['inicio' => Carbon, 'fim' => Carbon]`.
- `FechamentoRollupService::porEmpresa(string $mes, ?Collection $companies = null): array` → por `company_id`, com `faturamento_ml`, `faturamento_shopee`, `faturamento_total`.
- `FechamentoFaixaResolver::paraEmpresa(Company $c): ?array` → `origem`, `servico_id`, `servico_nome`, `faixas`.
- `FechamentoFaixaResolver::classificar(float $fat, Collection $faixas): ?array` → `ordem`, `label`, `valor`, `limite_inferior`, `limite_superior`.
- `FechamentoSnapshot` / `FechamentoGrupoSnapshot` com as colunas do plano 02 e `ORIGEM_CONSOLIDAR_MES`.

Estado atual de `fechamento()` (`AdminController.php` linhas 126-390), que é o que muda:
- linhas 141-155: o `if ($isMesAtual)` com `Carbon::now()->subDays(30)` — é a janela móvel que D-06 elimina.
- linhas 158-165: leitura de `CompanyMonthlyRevenue` para a progressão — sai (a pesquisa mediu que a tabela fica com valor rolling obsoleto quando o mês vira).
- linhas 266-292: o laço que acumula `$acumProg` — sai inteiro (D-06, "não deve ter acumulativo").
- linhas 340-372: a agregação por `filhaIds`/`$dadosPorId[$filhaId]` — substituída por `CompanyGroup`.
- linha 374: `$dados['conta_no_total'] = $dados['parent_company_id'] === null;` — substituída pela regra de grupo.
- linhas 205-233: cache `getCachedGrossBillingsMany` da Adman para o mês atual — sai; o rollup é 100% local (`adman_metrics` é alimentado diariamente por `adman:sync`), o que também elimina o `RefreshGrossBillingCacheJob::dispatchIfQueued()` deste método.

A constante `AdminController::FAIXAS` e os helpers privados `calcularFaixa()`, `faixaNumero()` e
`faixaLabel()` NÃO são removidos neste plano — `gerarRelatorio()` e `gerarRelatorioGeral()` ainda os
usam e só migram no plano 08, que é quem os apaga. Depois deste plano, `fechamento()` não pode mais
referenciá-los.

Copy obrigatória do UI-SPEC (as strings vão nas props, não no JSX): `A DEFINIR` para empresa sem
tabela (o mesmo literal de `ContratoPdfService::PLACEHOLDER`), `Sem faturamento neste mês` para
empresa sem faturamento.

Testes que vão precisar de ajuste, e por quê:
- `tests/Feature/AdminFechamentoControllerTest.php` linhas 201-266: os 4 testes de faixa afirmam os valores da constante `FAIXAS`. Os VALORES continuam os mesmos (D-02), mas a fonte passa a ser `servico_faixas_faturamento` — os testes precisam semear serviço + contrato + faixas.
- `tests/Feature/Phase14FechamentoUiTest.php`: afirma que TODA empresa do payload tem `servicos_contratados`; as linhas de grupo também precisam ter essa chave.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: fechamento() — janela de mês-calendário, faixa do banco e leitura do congelado</name>
  <files>app/Http/Controllers/AdminController.php</files>
  <read_first>
    - app/Http/Controllers/AdminController.php linhas 126-300 (o método inteiro que está sendo reescrito)
    - app/Services/Fechamento/FechamentoRollupService.php e app/Services/Fechamento/FechamentoFaixaResolver.php (plano 03)
    - app/Models/FechamentoSnapshot.php (plano 02)
    - .planning/learnings/desempenho-bonificacao.md §3 (mês fechado lê snapshot congelado)
  </read_first>
  <action>
Injetar `FechamentoRollupService` e `FechamentoFaixaResolver` no construtor de `AdminController`,
ao lado do `AdmanService` já existente (propriedades promovidas).

Em `fechamento()`, substituir o bloco de janela pelas chamadas ao rollup: derivar `$mesSelecionado`
do request (mesma validação atual, recusando mês futuro), e obter a janela por
`FechamentoRollupService::janela($mesSelecionado)`. O ramo `if ($isMesAtual)` com
`Carbon::now()->subDays(30)` desaparece — mês corrente passa a ser dia 1 até hoje, mês passado é o
mês-calendário completo (D-06).

Introduzir a bifurcação de fonte: `$competenciaFechada` é verdadeiro quando existe qualquer linha em
`fechamento_snapshots` com `whereDate('mes_referencia', primeiro dia do mês selecionado)` e
`origem = FechamentoSnapshot::ORIGEM_CONSOLIDAR_MES`.
- Se fechada: os números por empresa vêm das linhas de `fechamento_snapshots` (e as de grupo, de
  `fechamento_grupo_snapshots`) — NÃO recalcular nada ao vivo. É a mesma disciplina do módulo
  Desempenho: corrigir código não muda competência já fechada; quem regrava é
  `fechamento:consolidar-mes`.
- Se aberta: os números vêm de `FechamentoRollupService::porEmpresa()` e de
  `FechamentoFaixaResolver` aplicados ao vivo.

Remover deste método a leitura de `CompanyMonthlyRevenue`, o batch de cache
`getCachedGrossBillingsMany`, a variável `$missingCache` e o `RefreshGrossBillingCacheJob::
dispatchIfQueued()` — o rollup é totalmente local e não depende de aquecimento de cache. Manter os
imports usados por outros métodos do controller.

Estado por empresa com a mesma precedência do comando do plano 05: `sem_integracao`,
`sem_faturamento`, `sem_tabela`, `ok`. Emitir nas props, além do que já existe: `faturamento_ml`,
`faturamento_shopee`, `faixa_ordem`, `faixa_label`, `faixa_limite_inferior`,
`faixa_limite_superior`, `tabela_origem` (`'propria'` | `'servico'` | null), `tabela_servico_nome`, `valor_faixa_e_piso` (bool — quando verdadeiro, o valor da faixa é "a partir de R$ X" e a tela precisa dizer isso; é a última faixa de Gestão e de Brigada).
Preservar as chaves existentes `faixa`, `valor_mensal`, `cobranca_mensal`, `evolucao`, `estado`,
`has_adman`, `recebido`, `servicos_contratados`, `periodo_inicio`, `periodo_fim` — a tela e as
suítes atuais dependem delas.

Acrescentar às props de página: `competencia_fechada` (bool), `competencia_fechada_em` (data ISO da
maior `gerado_em` da competência, ou null) e `faixas_por_servico` (lista dos serviços de setor
financeiro com suas faixas, para a seção de tabela do plano 09).

`fechamento()` não pode mais referenciar `self::FAIXAS`, `calcularFaixa()`, `faixaNumero()` nem
`faixaLabel()` — esses continuam no arquivo apenas para `gerarRelatorio()`/`gerarRelatorioGeral()`,
que migram no plano 08 e os apagam lá.
  </action>
  <acceptance_criteria>
    - `grep -c "subDays(30)" ` no corpo de `fechamento()` retorna 0.
    - `grep -c "CompanyMonthlyRevenue" ` no corpo de `fechamento()` retorna 0.
    - `grep -c "calcularFaixa\|self::FAIXAS" ` no corpo de `fechamento()` retorna 0.
    - Com o mês corrente selecionado, `periodo_inicio` da resposta é o dia 1 do mês, não uma data 30 dias atrás.
    - Com uma competência fechada, alterar `adman_metrics` do mês NÃO muda o `faturamento` retornado pela tela (asserção comparando com o valor gravado em `fechamento_snapshots`).
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FinanceiroPropsTest</automated>
  </verify>
  <done>A tela lê mês-calendário, faixa do banco e valor congelado quando a competência está fechada.</done>
</task>

<task type="auto">
  <name>Tarefa 2: fechamento() — agregação por CompanyGroup e progressão sem acumulado</name>
  <files>app/Http/Controllers/AdminController.php</files>
  <read_first>
    - app/Http/Controllers/AdminController.php linhas 266-380 (o laço de progressão com `$acumProg` e a agregação por `filhaIds`, que são os dois trechos substituídos)
    - app/Models/CompanyGroup.php (relação `companies()`)
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md (D-08, D-09, D-10)
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-UI-SPEC.md seções 1 e 2
  </read_first>
  <action>
Substituir a agregação por `filhaIds`/`parent_company_id` por agregação via `company_group_id`:
empresas com `company_group_id` não nulo somam numa linha de grupo; empresas sem grupo continuam
como linha individual. `parent_company_id` deixa de participar de qualquer soma — D-09 registra a
consequência aceita (`MOVELOVEOFICIAL` deixa de somar com as três filhas, e o conserto, se for o
caso, é feito no módulo Comercial, não aqui). As chaves `parent_company_id`, `nome_pai`, `filha_ids`
e `is_filha` podem continuar sendo emitidas como informação, mas nenhuma delas pode influenciar
soma, faixa ou `conta_no_total`.

Cada linha de grupo carrega: `tipo = 'grupo'`, `id` = id da empresa-âncora (a de maior
`faturamento_total`, empate pelo menor id — mantém funcionando o link de relatório por empresa que a
tela já tem), `name` = nome do `CompanyGroup`, `grupo` com `id`/`name`/`color`, `filhas` = as
empresas-membro com seus próprios números, `servicos_contratados` = união dos serviços das
empresas-membro (a suíte `Phase14FechamentoUiTest` exige essa chave em TODA linha),
`faturamento`/`faturamento_ml`/`faturamento_shopee` = SOMA das membros (D-10), faixa classificada
sobre a SOMA usando a tabela da empresa-âncora, e `tabelas_divergentes` true quando as membros
resolvem tabelas diferentes (alimenta o banner âmbar do UI-SPEC). Linhas de empresa avulsa levam
`tipo = 'empresa'`.

`conta_no_total` passa a ser: true para linha de grupo e para empresa sem grupo; false para empresa
que é membro de um grupo (ela já está contada dentro da linha do grupo). Quando a competência está
fechada, os números de grupo vêm de `fechamento_grupo_snapshots`, não de uma soma recalculada.

Substituir a progressão: em vez de acumular `company_monthly_revenues`, montar `progressao` a partir
das linhas já congeladas em `fechamento_snapshots` (ou `fechamento_grupo_snapshots`, para linha de
grupo) da mesma entidade, ordenadas por `mes_referencia` crescente, cada item com `mes` (`'YYYY-MM'`),
`mensal` (o `faturamento_total`), `faixa`, `faixa_label`, `valor_faixa` e `evolucao`. A chave
`acumulado` deixa de ser emitida — D-06 é explícito ("não deve ter acumulativo") e o UI-SPEC manda
remover a coluna e a palavra da UI inteira.
  </action>
  <acceptance_criteria>
    - `grep -c "'acumulado'" app/Http/Controllers/AdminController.php` retorna 0.
    - `grep -c "acumProg" app/Http/Controllers/AdminController.php` retorna 0.
    - Com 2 empresas no mesmo `CompanyGroup`, a resposta traz 1 linha `tipo='grupo'` cujo `faturamento` é a soma das duas, e as 2 empresas aparecem em `filhas` com `conta_no_total` false.
    - Com uma empresa que tem `parent_company_id` preenchido e nenhum grupo, a resposta traz essa empresa como linha própria com `conta_no_total` true.
    - Toda linha do array `companies` tem a chave `servicos_contratados`.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FinanceiroPropsTest</automated>
  </verify>
  <done>Grupos vêm do Comercial, a soma define a faixa e a progressão não tem coluna acumulada.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Atualizar as suítes existentes e criar o contrato de props</name>
  <files>tests/Feature/AdminFechamentoControllerTest.php, tests/Feature/Phase14FechamentoUiTest.php, tests/Feature/Phase137/Phase137FinanceiroPropsTest.php</files>
  <read_first>
    - tests/Feature/AdminFechamentoControllerTest.php (arquivo inteiro — os 16 testes, especialmente os 4 de faixa nas linhas 201-266 e `test_metrica_fora_do_mes_nao_conta`)
    - tests/Feature/Phase14FechamentoUiTest.php (os 4 testes, especialmente o de `servicos_contratados` em todas as empresas)
    - .planning/learnings/desempenho-bonificacao.md §0.02 (medir a baseline com as mudanças em stash antes de chamar qualquer falha de "regressão sua")
  </read_first>
  <action>
Antes de alterar qualquer teste, rodar `"C:/xampp/php/php.exe" artisan test
--filter="AdminFechamentoControllerTest|Phase14FechamentoUiTest"` com as mudanças em stash e anotar
quais testes já falhavam — o projeto tem histórico de falhas pré-existentes sendo perseguidas como
regressão. Registrar a baseline no SUMMARY.

Atualizar `AdminFechamentoControllerTest`: os 4 testes de faixa precisam semear o serviço `Gestão`,
um `ContratoServico` ativo ligando a empresa a ele, e as faixas de `servico_faixas_faturamento` com
— os VALORES esperados continuam os mesmos (3.000 até 499.999,99; 4.500 na
faixa 2; 12.000 na máxima), porque D-02 manda manter a tabela vigente; o que muda é a fonte. O teste
`test_metrica_fora_do_mes_nao_conta` continua válido e vira também a prova de que a janela é
mês-calendário. Não apagar nenhum teste — se algum deixar de fazer sentido, converter em asserção do
comportamento novo e explicar no comentário, em pt-BR.

Atualizar `Phase14FechamentoUiTest`: garantir que os cenários incluam pelo menos uma empresa em
grupo, para provar que a linha de grupo também traz `servicos_contratados`.

Criar `tests/Feature/Phase137/Phase137FinanceiroPropsTest.php` (namespace `Tests\Feature\Phase137`)
cobrindo: (a) mês corrente devolve `periodo_inicio` no dia 1 do mês; (b) grupo de 2 empresas gera 1
linha `tipo='grupo'` com soma e faixa da soma; (c) empresa com `parent_company_id` e sem grupo é
linha própria; (d) competência fechada devolve o valor do snapshot mesmo depois de alterar
`adman_metrics`; (e) empresa sem tabela devolve `estado='sem_tabela'` e `valor_mensal` nulo;
(f) empresa com ML e Shopee no mês devolve `faturamento_ml`, `faturamento_shopee` e o `faturamento`
igual à soma; (g) nenhum item de `progressao` tem a chave `acumulado`; (h) props de página trazem
`competencia_fechada`.
  </action>
  <acceptance_criteria>
    - `"C:/xampp/php/php.exe" artisan test --filter=AdminFechamentoControllerTest` termina com exit code 0.
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase14FechamentoUiTest` termina com exit code 0.
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137FinanceiroPropsTest` termina com exit code 0.
    - Nenhum método de teste foi deletado das duas suítes existentes (contagem de `public function test` igual ou maior à original).
    - A baseline de falhas pré-existentes está registrada no SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter="AdminFechamentoControllerTest|Phase14FechamentoUiTest|Phase137FinanceiroPropsTest"</automated>
  </verify>
  <done>As três suítes verdes, sem teste deletado, com as props novas sob contrato.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/financeiro` | tela de admin que expõe faturamento de todas as empresas |
| snapshot congelado → exibição | exibir recálculo ao vivo em mês fechado contradiz o que foi cobrado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-25 | Information Disclosure | props de `/financeiro` com faturamento de toda a base | mitigate | rota permanece no grupo `['auth','verified','role:admin']`; nenhuma props nova é exposta fora dele; teste de 403 para não-admin já existe em `AdminFechamentoControllerTest` e é preservado |
| T-137-26 | Tampering | tela recalculando competência fechada | mitigate | bifurcação explícita: com `competencia_fechada` verdadeira, os números vêm de `fechamento_snapshots`/`fechamento_grupo_snapshots`; teste (d) prova que alterar `adman_metrics` não muda o exibido |
| T-137-27 | Repudiation | número exibido sem dizer de qual tabela veio | mitigate | props `tabela_origem` e `tabela_servico_nome` acompanham cada linha, e `tabelas_divergentes` sinaliza grupo com réguas diferentes |
| T-137-28 | Denial of Service | N+1 em ~190 empresas ao resolver faixa por empresa | mitigate | rollup faz 2 queries agregadas totais; `contratosServico.servico` e `grupo` continuam eager-loaded; as faixas por serviço são carregadas uma vez e reusadas |
</threat_model>

<verification>
1. `"C:/xampp/php/php.exe" artisan test --filter="Phase137|AdminFechamentoController|Phase14Fechamento"` — exit code 0.
2. Abrir `/financeiro` com mês corrente e conferir que `periodo_inicio` é o dia 1 (via props do Inertia).
3. `grep -c "subDays(30)" app/Http/Controllers/AdminController.php` — o resultado só pode vir de `gerarRelatorio()`/`gerarRelatorioGeral()`, que migram no plano 08.
</verification>

<success_criteria>
- `fechamento()` não usa janela móvel, `company_monthly_revenues`, `parent_company_id` para somar, nem a constante `FAIXAS`.
- Mês fechado exibe o congelado.
- Grupo do Comercial é a única fonte de agrupamento da tela.
- Nenhuma coluna acumulada sobrevive nas props.
</success_criteria>

<output>
Criar `.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-07-SUMMARY.md` ao concluir.
</output>
