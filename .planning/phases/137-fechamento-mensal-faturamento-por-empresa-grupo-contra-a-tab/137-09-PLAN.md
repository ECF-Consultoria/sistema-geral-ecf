---
phase: 137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab
plan: 09
type: execute
wave: 5
depends_on: ["137-06", "137-07"]
files_modified:
  - resources/js/Pages/Admin/Financeiro.jsx
  - resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx
  - tests/Feature/Phase137/Phase137FinanceiroUiContratoTest.php
autonomous: true
requirements: [D-04, D-05, D-13]

must_haves:
  truths:
    - "Empresa sem tabela aparece como A DEFINIR com um caminho para cadastrar, nunca como R$ 0"
    - "Empresa sem faturamento no mês diz isso em palavras, nunca um traço mudo"
    - "Faturamento combinado abre a composição Mercado Livre + Shopee, nunca soma silenciosa"
    - "A tabela de faixas de uma empresa pode ser cadastrada e removida pela tela"
    - "A tabela de faixas de um serviço pode ser editada pela tela — é por aí que as tabelas de Gestão de ADS Shopee (id 9) e Brigada (id 10) entram no sistema"
    - "A barra de progresso de faixa deixa de depender de limites hardcoded no JSX"
  artifacts:
    - path: "resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx"
      provides: "Bloco de tabela de faixas dentro do accordion, com CRUD all-or-nothing"
      contains: "Tabela própria desta empresa"
    - path: "resources/js/Pages/Admin/Financeiro.jsx"
      provides: "Estados de ausência, breakdown ML+Shopee e banner de grupo divergente"
      contains: "A DEFINIR"
  key_links:
    - from: "resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx"
      to: "admin.financeiro.faixas.empresa"
      via: "router.post/router.delete com o nome de rota do plano 06"
      pattern: "financeiro.faixas.empresa"
    - from: "resources/js/Pages/Admin/Financeiro.jsx"
      to: "props faixa_limite_inferior/faixa_limite_superior"
      via: "FaixaProgresso consumindo os limites vindos do backend"
      pattern: "faixa_limite_superior"
---

<objective>
A parte da tela que responde "qual tabela vale para esta empresa e o que fazer quando não vale
nenhuma": estados de ausência visível, composição ML + Shopee e o cadastro manual da tabela de
faixas dentro do accordion.

Purpose: D-04 é requisito explícito do usuário e o UI-SPEC já contratou a copy e os estados. D-05
exige que a soma das plataformas nunca seja silenciosa. E a barra de faixa precisa parar de ler a
tabela hardcoded no JSX, que era o espelho da constante `FAIXAS` agora apagada.

Output: 1 componente novo em arquivo próprio, alterações pontuais em `Financeiro.jsx`, 1 teste de
contrato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-UI-SPEC.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-PATTERNS.md

<interfaces>
Contratos que o executor precisa e NÃO deve ir procurar no código:

`resources/js/Pages/Admin/Financeiro.jsx` tem 1255 linhas e já exporta, como componentes locais:
`ServiceBadge` (39), `IntegrationBadge` (62), `GraficoFaixas` (169), `EvolucaoBadge` (200),
`RecebidoToggle` (212), `TotalConsolidado` (245), `FaixaProgresso` (296), `ProgressaoModal` (336),
`FechamentoRow` (435), `ContratosSection` (492), `FechamentoAccordion` (590), `FechamentoList` (676),
`MesSeletor` (719), `GerarRelatoriosBtn` (749), `SyncFaturamentoBtn` (867), `FiltroBarra` (898) e o
default `Financeiro` (945). NENHUM deles é recriado do zero — o UI-SPEC marca `FechamentoRow`,
`FechamentoAccordion`, `EvolucaoBadge`, `FaixaProgresso`, `ProgressaoModal`, `TotalConsolidado`,
`GraficoFaixas`, `MesSeletor`, `ServiceBadge`, `IntegrationBadge` e `ContratosSection` como
"reaproveitar".

Imports já disponíveis no arquivo (linhas 1-12): `Dialog`, `DialogContent`, `DialogHeader`,
`DialogTitle`, `DialogFooter`, `Button`, `Input`, `Label`, `Textarea`, `cn`, `formatDate`,
`formatCurrency`, `axios`, `router`/`Link` do Inertia, ícones de `lucide-react`. Nenhum primitivo
novo de `Components/ui/` é necessário.

As constantes `FAIXAS_LIMITES` (linha 14) e `FAIXA_NOMES` (linha 23) são o espelho JS da constante
`FAIXAS` do PHP, que o plano 08 apagou — elas saem neste plano.

Props novas vindas do plano 07, por linha de `companies`: `tipo` (`'empresa'`|`'grupo'`), `grupo`,
`faturamento_ml`, `faturamento_shopee`, `faixa_ordem`, `faixa_label`, `faixa_limite_inferior`,
`faixa_limite_superior`, `tabela_origem` (`'propria'`|`'servico'`|null), `tabela_servico_nome`,
`tabelas_divergentes`, `valor_faixa_e_piso`, `estado` (`'ok'`|`'sem_faturamento'`|`'sem_tabela'`|`'sem_integracao'`).
Props de página: `competencia_fechada`, `competencia_fechada_em`, `faixas_por_servico`.

Rotas do plano 06 (usar via helper `route()` do Ziggy): `admin.financeiro.faixas.empresa` (POST),
`admin.financeiro.faixas.empresa.remover` (DELETE), `admin.financeiro.faixas.servico` (POST).

Copy obrigatória, literal, do UI-SPEC:
- `Tabela de faixas: A DEFINIR` + subtexto `Cadastre a tabela de faturamento desta empresa para ela entrar no fechamento.` + botão `Cadastrar tabela de faixas`
- `Sem faturamento neste mês`
- `Tabela do serviço {nome do serviço}`
- `Tabela própria desta empresa` + subtexto `Substitui completamente a tabela do serviço "{nome}".`
- `Este grupo tem empresas com tabelas diferentes`
- `Mercado Livre {valor} + Shopee {valor} = {total}`
- `Criar tabela própria`, `Adicionar faixa`, `Voltar a usar a tabela do serviço`
- `Sem limite superior` como rótulo do campo de teto vazio
- `Valor é um piso ("a partir de")` como rótulo do checkbox de `valor_e_piso` no form de faixa
- `a partir de {valor}` sempre que a faixa aplicada tiver `valor_faixa_e_piso` verdadeiro — na linha da empresa, no accordion e na listagem da tabela. Nunca exibir o valor cru como se fosse preço fechado: é a última faixa de Gestão e de Brigada, e mostrar "R$ 12.000" seco faria o Administrativo cobrar a menos.
- `Remover a faixa "{ordem}ª faixa" desta tabela?` no `confirm()` nativo
- `Essa faixa se sobrepõe à faixa {ordem}. Ajuste o limite antes de salvar.`

Regras visuais fechadas do UI-SPEC: só os 4 tamanhos (11/13/15/20px) e 2 pesos (400/600); accent
`ecf-yellow` só para o botão de cadastrar/editar tabela (item 5 da lista fechada); ausência de dado
usa paleta neutra (`white/40`, `white/25`) ou `amber-400` para pendência que exige ação — NUNCA
destructive; `Tabela própria desta empresa` é badge NEUTRO, não accent.

Convenção do projeto: rodar `npm run build` depois de toda mudança de frontend.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Estados de ausência, breakdown ML+Shopee e FaixaProgresso sem tabela hardcoded</name>
  <files>resources/js/Pages/Admin/Financeiro.jsx</files>
  <read_first>
    - resources/js/Pages/Admin/Financeiro.jsx linhas 1-40 (imports e as constantes `FAIXAS_LIMITES`/`FAIXA_NOMES` que saem)
    - resources/js/Pages/Admin/Financeiro.jsx linhas 296-336 (`FaixaProgresso`) e 435-500 (`FechamentoRow`)
    - resources/js/Pages/Admin/Financeiro.jsx linhas 590-680 (`FechamentoAccordion`, onde a seção "Composição do grupo" já existe)
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-UI-SPEC.md seções "Copywriting Contract" e "Telas e Estados"
  </read_first>
  <action>
Remover as constantes `FAIXAS_LIMITES` e `FAIXA_NOMES` do topo do arquivo. `FaixaProgresso` passa a
receber `limiteInferior` e `limiteSuperior` por prop (vindos de `faixa_limite_inferior` e
`faixa_limite_superior`) e o rótulo da faixa por `faixa_label`; quando `limiteSuperior` é nulo, a
barra mostra o estado de faixa máxima que já existe hoje, sem calcular percentual.

Criar quatro componentes locais no mesmo arquivo, no estilo dos badges já existentes (função local,
objeto de config por estado, `cn()` e ícones `lucide-react`):
- `AusenciaTabelaPendencia` — usado quando `estado === 'sem_tabela'`: substitui a mensalidade
  calculada pelo texto `Tabela de faixas: A DEFINIR` em `amber-400`, com o subtexto e o botão
  `Cadastrar tabela de faixas` que expande o accordion na seção de faixas. Nunca oculta a linha e
  nunca mostra R$ 0.
- `AusenciaFaturamentoBadge` — usado quando `estado === 'sem_faturamento'`: substitui o valor
  monetário por `Sem faturamento neste mês` em `white/40`, e a linha NÃO desenha `FaixaProgresso`.
- `FaturamentoCombinadoBreakdown` — quando `faturamento_ml` e `faturamento_shopee` são ambos não
  nulos, renderiza, abaixo do total, a linha `Mercado Livre {valor} + Shopee {valor} = {total}`
  usando o `fmtBRL` já existente no arquivo.
- `GrupoServicosDivergentesBanner` — quando `tabelas_divergentes` é verdadeiro numa linha de grupo,
  renderiza dentro do accordion expandido o banner âmbar `Este grupo tem empresas com tabelas
  diferentes` seguido da lista "empresa → tabela aplicada" por linha, usando `tabela_origem` e
  `tabela_servico_nome` de cada membro. É aviso, não erro — cor `amber-400`, nunca destructive.

Onde a mensalidade da faixa é impressa (linha e accordion), prefixar com `a partir de ` quando `valor_faixa_e_piso` for verdadeiro — é a última faixa de Gestão e de Brigada, cujo contrato diz "a partir de R$ 12.000"; exibir o número seco faria o Administrativo cobrar a menos.

Ligar os quatro em `FechamentoRow` e `FechamentoAccordion` sem reescrever esses componentes: os
pontos de inserção são o slot onde hoje se imprime `fmtBRL(empresa.faturamento)` e a mensalidade, e
a seção "Composição do grupo" já existente do accordion.

Não introduzir um terceiro peso de fonte nem tamanho fora dos 4 declarados; não usar `ecf-yellow` em
nenhum desses quatro componentes (accent é reservado a ação e status, e ausência de dado não é
nenhum dos dois).

Rodar `npm run build` ao final.
  </action>
  <acceptance_criteria>
    - `grep -c "FAIXAS_LIMITES\|FAIXA_NOMES" resources/js/Pages/Admin/Financeiro.jsx` retorna 0.
    - `grep -c "A DEFINIR" resources/js/Pages/Admin/Financeiro.jsx` retorna 1 ou mais.
    - `grep -c "Sem faturamento neste mês" resources/js/Pages/Admin/Financeiro.jsx` retorna 1 ou mais.
    - `grep -c "a partir de" resources/js/Pages/Admin/Financeiro.jsx` retorna 1 ou mais (exibição da faixa-piso).
    - `grep -c "ecf-yellow" ` dentro dos quatro componentes novos retorna 0.
    - `npm run build` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Ausência de tabela e de faturamento são visíveis e nomeadas, a soma ML+Shopee é aberta e a barra de faixa lê os limites do backend.</done>
</task>

<task type="auto">
  <name>Tarefa 2: TabelaFaixasSection e FaixaFormDialog — cadastro manual (D-04)</name>
  <files>resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx, resources/js/Pages/Admin/Financeiro.jsx</files>
  <read_first>
    - resources/js/Pages/Admin/Financeiro.jsx linhas 492-590 (`ContratosSection` — molde estrutural de bloco CRUD dentro do accordion, incluindo o `confirm()` nativo para ação destrutiva)
    - resources/js/Pages/Admin/Financeiro.jsx linhas 590-680 (`FechamentoAccordion`, onde a seção nova é encaixada)
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-UI-SPEC.md seção 4 (os três estados da seção e o form de faixa)
  </read_first>
  <action>
Criar `resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx` exportando por default o
componente `TabelaFaixasSection`, que recebe `empresa`, `faixasPorServico` e
`competenciaFechada`. O arquivo é separado porque `Financeiro.jsx` já tem 1255 linhas — o UI-SPEC
deixa essa decisão para o executor e o tamanho justifica.

Três estados da empresa, conforme o UI-SPEC, mais o caminho de edição da tabela do serviço:
1. `tabela_origem === 'servico'` — lista somente leitura das faixas do serviço (obtidas de
   `faixasPorServico` pelo `servico_id` da empresa), rótulo `Tabela do serviço {nome}` e um botão
   secundário `Criar tabela própria` que abre o form pré-preenchido com as faixas do serviço.
2. `tabela_origem === 'propria'` — lista editável (colunas ordem, limite superior, valor), rótulo
   `Tabela própria desta empresa`, o aviso FIXO e sempre visível
   `Substitui completamente a tabela do serviço "{nome}".` (D-13 — não mostrar só na hora de salvar),
   botões `Adicionar faixa` e, por linha, editar e remover; e o botão
   `Voltar a usar a tabela do serviço`, que dispara `confirm()` nativo antes do DELETE.
3. `tabela_origem` nulo — estado `A DEFINIR` com o CTA único `Cadastrar tabela de faixas`.
4. **Editar a tabela DO SERVIÇO** — no estado 1, além de `Criar tabela própria`, um botão
   `Editar tabela do serviço` abre o mesmo `FaixaFormDialog` operando sobre as faixas do serviço e
   salva por `router.post(route('admin.financeiro.faixas.servico', servicoId), { faixas })`. As três
   tabelas de serviço (`Gestão`, `Brigada` e `Gestão de ADS Shopee`) já nascem semeadas pelo plano
   01 com os valores medidos nos modelos publicados (D-02b), então este caminho é para CORRIGIR uma
   régua, não para cadastrá-la do zero. Editar tabela de serviço afeta TODAS as empresas que a
   herdam: exibir a contagem de empresas afetadas ao lado do botão e pedir `confirm()` nativo antes
   de salvar.

`FaixaFormDialog` (componente local do mesmo arquivo) usa `Dialog`/`Input`/`Label` já importados,
com os campos `Ordem` (número), `Faturamento até` (moeda; vazio significa sem teto e o campo precisa
exibir o rótulo `Sem limite superior`, nunca ficar mudo) e `Valor da mensalidade` (moeda). Remover
uma linha usa `confirm()` nativo com a copy `Remover a faixa "{ordem}ª faixa" desta tabela?`. Quando o campo de teto fica vazio (faixa aberta), o dialog oferece o checkbox `Valor é um piso ("a partir de")`, que grava `valor_e_piso` — só habilitado nessa condição, porque o backend recusa piso em faixa com teto.

Salvar envia a TABELA INTEIRA (array `faixas`) por `router.post(route('admin.financeiro.faixas.empresa',
empresa.id), { faixas })` — nunca linha a linha, porque a exceção é all-or-nothing (D-13) e o
backend do plano 06 substitui o conjunto em transação. Erros de validação chegam pelo mecanismo de
`errors` do Inertia e a mensagem de sobreposição é exibida como veio do servidor, sem reescrita no
front.

Quando `competenciaFechada` é verdadeiro, os controles de edição ficam desabilitados com explicação
curta — mudar a tabela não altera um mês já congelado, e deixar o botão ativo prometeria algo falso.

Em `Financeiro.jsx`, importar o componente e encaixá-lo dentro de `FechamentoAccordion`, na seção
nova "Tabela de faixas aplicada" prevista pelo UI-SPEC, sem alterar as seções já existentes do
accordion.

Rodar `npm run build` ao final.
  </action>
  <acceptance_criteria>
    - `grep -c "Substitui completamente a tabela do serviço" resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx` retorna 1 ou mais.
    - `grep -c "Sem limite superior" resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx` retorna 1 ou mais.
    - `grep -cE "router\.(post|delete)" resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx` retorna 3 ou mais (salvar tabela da empresa, remover tabela da empresa, salvar tabela do serviço).
    - `grep -c "financeiro.faixas.servico" resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx` retorna 1 ou mais — sem isso o checkpoint do plano 10 não é executável.
    - `grep -c "faixas:" resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx` — o payload enviado é a lista inteira, não um item.
    - `grep -c "function FechamentoRow\|function ProgressaoModal\|function EvolucaoBadge" resources/js/Pages/Admin/Financeiro.jsx` retorna 3 (os componentes reaproveitados continuam existindo).
    - `npm run build` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>A tabela de faixas de uma empresa pode ser criada, editada, substituída inteira e removida pela tela.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Teste de contrato da tela</name>
  <files>tests/Feature/Phase137/Phase137FinanceiroUiContratoTest.php</files>
  <read_first>
    - tests/Feature/Phase14FechamentoUiTest.php (como o projeto testa props do Inertia e o nome do componente)
    - tests/Feature/Phase137/Phase137FinanceiroPropsTest.php (plano 07)
  </read_first>
  <action>
Criar `tests/Feature/Phase137/Phase137FinanceiroUiContratoTest.php` (namespace
`Tests\Feature\Phase137`) com duas frentes.

Frente de props: a resposta de `/administrativo/financeiro` traz, em toda linha de `companies`, as
chaves `tipo`, `estado`, `faturamento_ml`, `faturamento_shopee`, `faixa_label`,
`faixa_limite_inferior`, `faixa_limite_superior`, `tabela_origem`, `tabela_servico_nome`, `valor_faixa_e_piso`; e, nas
props de página, `faixas_por_servico` e `competencia_fechada`. Sem essas chaves os componentes da
Tarefa 1 e 2 renderizariam vazio em silêncio.

Frente de arquivo (trava de regressão barata, sem framework de teste JS no projeto): lê
`resources/js/Pages/Admin/Financeiro.jsx` e `resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx`
com `file_get_contents` e afirma: (a) `Financeiro.jsx` NÃO contém `FAIXAS_LIMITES` nem `FAIXA_NOMES`;
(b) `Financeiro.jsx` contém `A DEFINIR` e `Sem faturamento neste mês`; (c) `Financeiro.jsx` ainda
contém `function FechamentoRow`, `function ProgressaoModal` e `function EvolucaoBadge` — a trava
contra recriar do zero o que o UI-SPEC mandou reaproveitar; (d) `TabelaFaixasSection.jsx` contém
`Substitui completamente a tabela do serviço`.

Comentários do teste em pt-BR explicando por que a frente de arquivo existe.
  </action>
  <acceptance_criteria>
    - Todas as chaves listadas na frente de props têm asserção própria.
    - As 4 asserções da frente de arquivo existem e passam.
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137FinanceiroUiContratoTest` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FinanceiroUiContratoTest</automated>
  </verify>
  <done>Props e componentes reaproveitados estão sob trava automatizada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formulário do browser → tabela de cobrança | o form de faixas envia valores que definem quanto o cliente paga |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-33 | Tampering | payload de faixas manipulado no browser | mitigate | validação autoritativa é do servidor (`SalvarFaixasFaturamentoRequest`, plano 06); o front só exibe a mensagem que veio do servidor e não reimplementa a regra |
| T-137-34 | Elevation of Privilege | ações de faixas chamadas por usuário sem role | mitigate | as rotas ficam no grupo `role:admin` e o FormRequest reforça com `authorize()`; a tela só é alcançável por admin |
| T-137-35 | Information Disclosure | valores de mensalidade de todas as empresas no bundle do browser | accept | os dados vêm por props de uma página já restrita a `role:admin`; nenhum dado novo de cliente é exposto além do que a tela já mostra |
| T-137-36 | Repudiation | usuário achar que editou a tabela e o mês fechado ter mudado | mitigate | com `competencia_fechada` verdadeiro, os controles de edição ficam desabilitados com explicação — a tela não promete efeito que não existe |
</threat_model>

<verification>
1. `npm run build` — exit code 0.
2. `"C:/xampp/php/php.exe" artisan test --filter=Phase137` — exit code 0.
3. Abrir `/financeiro`, expandir uma empresa sem tabela e confirmar `Tabela de faixas: A DEFINIR` com o CTA.
</verification>

<success_criteria>
- Nenhum estado de ausência aparece como R$ 0 ou traço mudo.
- Soma ML + Shopee sempre exibe a composição.
- Cadastro manual da tabela funciona pela tela, all-or-nothing.
- Componentes que o UI-SPEC mandou reaproveitar continuam existindo.
</success_criteria>

<output>
Criar `.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-09-SUMMARY.md` ao concluir.
</output>
