---
phase: 137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab
plan: 03
type: execute
wave: 2
depends_on: ["137-01"]
files_modified:
  - app/Services/Fechamento/FechamentoFaixaResolver.php
  - app/Services/Fechamento/FechamentoRollupService.php
  - tests/Feature/Phase137/Phase137FaixaResolverTest.php
  - tests/Feature/Phase137/Phase137RollupTest.php
autonomous: true
requirements: [D-01, D-05, D-06, D-07, D-13]

must_haves:
  truths:
    - "Dada uma empresa, o sistema sabe qual tabela de faixas se aplica a ela e por quê (própria ou do serviço)"
    - "Empresa com faixas próprias ignora inteiramente a tabela do serviço"
    - "O faturamento de uma competência é a soma de mês-calendário fechado, nunca janela móvel de 30 dias"
    - "Empresa que vende nas duas plataformas tem ML e Shopee somados numa faixa única"
    - "Empresa sem tabela e empresa sem faturamento são estados distintos e nomeados, nunca R$ 0 silencioso"
  artifacts:
    - path: "app/Services/Fechamento/FechamentoFaixaResolver.php"
      provides: "Resolução da tabela aplicável (D-01/D-13) e classificação de um faturamento em faixa"
      exports: ["paraEmpresa", "classificar"]
    - path: "app/Services/Fechamento/FechamentoRollupService.php"
      provides: "Faturamento ML+Shopee por empresa numa janela de mês-calendário"
      exports: ["porEmpresa", "janela"]
  key_links:
    - from: "app/Services/Fechamento/FechamentoRollupService.php"
      to: "app/Services/Metrics/MetricPeriodResolver.php"
      via: "resolve(['period_key' => 'YYYY-MM'])"
      pattern: "MetricPeriodResolver"
    - from: "app/Services/Fechamento/FechamentoRollupService.php"
      to: "shopee_metrics"
      via: "SUM(revenue) com whereDate na janela"
      pattern: "ShopeeMetric"
    - from: "app/Services/Fechamento/FechamentoFaixaResolver.php"
      to: "app/Models/EmpresaFaixaFaturamento.php"
      via: "consulta de exceção por empresa antes de cair no serviço"
      pattern: "EmpresaFaixaFaturamento"
---

<objective>
Os dois serviços que respondem as perguntas centrais da fase: "qual tabela de faixas vale para esta
empresa?" e "quanto ela faturou nesta competência?".

Purpose: hoje as duas respostas estão hardcoded e espalhadas — a tabela é a constante `FAIXAS`
duplicada em dois arquivos, e o faturamento é uma janela MÓVEL de 30 dias no mês corrente. D-06 mata
a janela móvel, D-05 manda somar ML+Shopee, D-07 exige o lado Shopee que hoje não existe no
fechamento, e D-01/D-13 exigem herança serviço→empresa com exceção all-or-nothing. Centralizar aqui
é o que permite os 4 call-sites do plano 07 e 08 lerem de uma fonte só.

Output: 2 serviços novos em `app/Services/Fechamento/`, 2 arquivos de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-RESEARCH.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-PATTERNS.md
@.planning/learnings/desempenho-bonificacao.md

<interfaces>
Contratos que o executor precisa e NÃO deve ir procurar no código:

`App\Services\Metrics\MetricPeriodResolver::resolve(array $filtros): array` — com
`['period_key' => 'YYYY-MM']` devolve as chaves `current_start` e `current_end` já como
mês-calendário fechado (método interno `resolveSpecificMonth`, linha ~225: ancora em
`Carbon::parse($periodKey . '-01', TIMEZONE)->startOfMonth()->startOfDay()`). Com
`['period_key' => 'current_month']` devolve dia 1 do mês corrente até hoje. É a implementação
oficial de D-06 — NÃO reescrever Carbon à mão.

`App\Models\AdmanMetric` — tabela `adman_metrics`, colunas `company_id`, `reference_date`,
`revenue`. O padrão de rollup ML já usado em `AdminController::fechamento()` (linhas 179-191) é
`whereBetween('reference_date', [$inicio, $fim])->whereNotNull('revenue')->selectRaw('company_id,
SUM(revenue) as faturamento')->groupBy('company_id')`.

`App\Models\ShopeeMetric` — tabela `shopee_metrics`, colunas `company_id`, `reference_date`
(cast `date`, persistido como datetime `Y-m-d 00:00:00`), `revenue`. O padrão de janela em
`ShopeeMetricDiffService::naJanela()` (linhas 159-167) usa `whereDate('reference_date','>=',$ini)` e
`whereDate('reference_date','<=',$fim)` — `whereDate`, NÃO `whereBetween`: comparação de string
excluiria o último dia da janela.

`App\Models\Servico` — `SETOR_PERFORMANCE = 'performance'`, `SETOR_SHOPEE = 'shopee'`,
`SETORES_FINANCEIROS = ['performance','shopee']`, colunas `plataforma` e
`contrato_junto_com_servico_id`, relação `servicoDono()`.

⚠️ **Serviços MEDIDOS NO BANCO DE PRODUÇÃO em 2026-09-02 — migration NÃO é fonte confiável para
saber quais serviços existem.** Neste projeto a configuração por serviço (`clicksign_template_id`,
`plataforma`, `clicksign_assinatura_posicionada`, `contrato_junto_com_servico_id`) é preenchida por
passo de produção pós-deploy, direto no banco. Os três serviços com tabela progressiva própria e
plataforma definida são `Gestão` (id 6, Mercado Livre, sem dono), `Gestão de ADS Shopee` (id 9,
Shopee, `contrato_junto_com_servico_id = 6`) e `Brigada` (id 10, Mercado Livre, sem dono). O serviço
de Shopee NÃO se chama `Shopee`, e `Brigada` É um serviço próprio, criado em 2026-08-26 com modelo
de contrato próprio — a migration `2026_06_18_100004` que diz "Brigada → Gestão" está OBSOLETA. O
par 9 → 6 é o contrato combinado que motiva D-05; Brigada, sem dono, resolve para a própria tabela.

`App\Models\Company` — `contratosServico()` (hasMany `ContratoServico`, filtrar `ativo = true`,
relação `servico`), accessor `cust_id` (`adman_account_id ?: ml_store_id`), `faixasFaturamento()`
(criada no plano 01).

`App\Models\ServicoFaixaFaturamento` (plano 01) — `CONTEXTO_FECHAMENTO = 'fechamento'`, scopes
`ordenadas()` e `deFechamento()`. `App\Models\EmpresaFaixaFaturamento` (plano 01) — scope
`ordenadas()`.

NÃO usar `AdmanMetricDiffService` (é HTTP-first, orientado a variação percentual de bônus — faria
chamada de rede por empresa todo dia 1). NÃO usar `CompanyMonthlyRevenue`/`company_monthly_revenues`
como fonte (a pesquisa mediu: `adman:sync-faturamento` roda diário com `--mes=` = mês corrente e
ninguém re-sincroniza o mês depois que ele fecha; o valor gravado é o rolling de 30 dias do momento
da virada). NÃO reusar `computeVarFaturamento()`/`median()` do `DesempenhoScoreService` — é outro
problema (variação percentual de carteira), e aqui a regra de grupo é SOMA por D-10.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: FechamentoFaixaResolver — herança serviço→empresa e classificação</name>
  <files>app/Services/Fechamento/FechamentoFaixaResolver.php, tests/Feature/Phase137/Phase137FaixaResolverTest.php</files>
  <read_first>
    - app/Models/BonusFaixa.php (o método `classificar()`, linhas 96-107 — molde direto, trocando a comparação de `nota_min/nota_max` por `limite_superior`)
    - app/Support/CobrancaCalculator.php (como o projeto já faz herança "padrão do serviço com override do contrato")
    - app/Models/Servico.php (constantes de setor, `SETORES_FINANCEIROS`, coluna `plataforma`, relação `servicoDono()`) — e confirmar o estado real dos serviços por reconsulta ao banco, nunca por migration
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md (D-01, D-05, D-13)
  </read_first>
  <behavior>
    - Empresa com 3 linhas em `empresa_faixas_faturamento` e serviço com 7 linhas → resolve `origem = 'propria'` e devolve as 3 linhas da empresa, `servico_id` nulo (D-13 all-or-nothing).
    - Empresa sem exceção, com contrato ativo de `Gestão` (id 6) → resolve `origem = 'servico'`, `servico_id` = id de Gestão, e as 7 faixas de contexto `fechamento`.
    - Empresa sem exceção, com contratos ativos de `Gestão de ADS Shopee` (id 9) e `Gestão` (id 6), onde o de Shopee tem `contrato_junto_com_servico_id = 6` → resolve para `Gestão` (contrato combinado, D-05).
    - Empresa sem exceção, com contrato ativo só de `Gestão de ADS Shopee` (dono ausente na empresa) e sem faixas cadastradas para esse serviço → resolve `null` (é o estado `A DEFINIR` até o cadastro do checkpoint do plano 10).
    - Empresa sem exceção, com contrato ativo de `Brigada` (id 10, sem `contrato_junto_com_servico_id`) e com faixas de Brigada cadastradas → resolve `origem = 'servico'` e `servico_id` = 10. Brigada tem tabela DE SERVIÇO própria — não é caso de exceção por empresa.
    - Empresa sem nenhum contrato de serviço candidato → resolve `null`.
    - `classificar(450000.00, faixas de Gestão)` devolve ordem 1, valor 3000.00, limite_inferior 0.0, limite_superior 499999.99.
    - `classificar(9000000.00, faixas de Gestão)` devolve ordem 7, valor 12000.00, `limite_superior` nulo e `label` `'maxima'`.
    - `classificar(500000.00, ...)` devolve ordem 2 (limite da faixa 1 é 499999.99, exclusivo por cima).
  </behavior>
  <action>
Criar `App\Services\Fechamento\FechamentoFaixaResolver` com dois métodos públicos.

`paraEmpresa(Company $company): ?array` devolve `null` quando não há tabela aplicável, ou um array
com as chaves `origem` (`'propria'` ou `'servico'`), `servico_id` (`int|null`), `servico_nome`
(`string|null`) e `faixas` (`Illuminate\Support\Collection` de linhas com as chaves `ordem`,
`limite_superior`, `valor`, ordenadas por `ordem` crescente). A ordem de resolução é:
1. Se `EmpresaFaixaFaturamento::where('company_id', ...)->ordenadas()->get()` tiver QUALQUER linha,
   devolve `origem = 'propria'` com essas linhas e `servico_id = null`. Isto é D-13: a exceção
   substitui a tabela INTEIRA, nunca linha a linha — não completar faixa faltante com a do serviço.
2. Senão, escolhe o serviço aplicável entre os `contratosServico` ativos da empresa. É **candidato**
   o serviço ativo que tenha `plataforma` preenchida OU `setor` em `Servico::SETORES_FINANCEIROS` —
   os dois critérios em OU, não só o setor. Razão medida no banco de produção em 2026-09-02: os três
   serviços com tabela progressiva própria são `Gestão` (id 6, plataforma Mercado Livre, sem dono),
   `Gestão de ADS Shopee` (id 9, plataforma Shopee, `contrato_junto_com_servico_id = 6`) e `Brigada`
   (id 10, plataforma Mercado Livre, sem dono). ⚠️ ANTES de implementar, confirmar por RECONSULTA AO
   BANCO (`SELECT id, nome, setor, plataforma, contrato_junto_com_servico_id FROM servicos WHERE
   ativo = 1`) qual `setor` o serviço `Brigada` tem: filtrar só por `SETORES_FINANCEIROS` deixaria
   Brigada de fora caso o setor dele não seja `performance` nem `shopee`, e Brigada
   demonstravelmente tem tabela progressiva e plataforma. Registrar o `setor` medido no SUMMARY.
   Se algum candidato tiver `contrato_junto_com_servico_id` apontando para outro serviço que TAMBÉM
   é candidato ativo da mesma empresa, o dono vence (regra do contrato combinado já aplicada em
   `ContratoClicksignService::iniciarParaEmpresa()`, e é o que faz D-05 ter "uma tabela só para as
   duas plataformas" — em produção, o par é 9 → 6). `Brigada` NÃO tem dono: empresa de Brigada
   resolve para a tabela do próprio serviço `Brigada`. Desempate determinístico quando não há dono:
   plataforma `Mercado Livre` antes de `Shopee`; permanecendo empate, o menor `servicos.id`.
3. Carrega `ServicoFaixaFaturamento` do serviço escolhido com os scopes `deFechamento()` e
   `ordenadas()`. Se vier vazio, devolve `null`.

`classificar(float $faturamento, Collection $faixas): ?array` devolve `null` se `$faixas` estiver
vazia; senão percorre em ordem crescente de `ordem` e devolve a PRIMEIRA cujo `limite_superior` seja
nulo ou maior-ou-igual ao faturamento. O retorno tem `ordem` (int), `label` (string:
`'faixa_' . ordem` quando há teto, `'maxima'` quando `limite_superior` é nulo — mantém compatível o
vocabulário que a tela e os testes atuais já usam), `valor` (float), `limite_inferior` (float: o
`limite_superior` da faixa anterior, ou 0.0 na primeira) e `limite_superior` (float|null). O
`limite_inferior` existe para a barra `FaixaProgresso` da tela parar de depender da tabela hardcoded
no JSX.

O serviço não faz nenhuma query de faturamento e não decide nada sobre grupo — só tabela e
classificação. Comentários em pt-BR.

Escrever o teste `Phase137FaixaResolverTest` ANTES da implementação, cobrindo cada item do bloco
`<behavior>`, com `use RefreshDatabase` e namespace `Tests\Feature\Phase137`. Cada asserção confere o
retorno do método, nunca texto impresso.
  </action>
  <acceptance_criteria>
    - Todos os casos do bloco `<behavior>` têm um método de teste correspondente em `Phase137FaixaResolverTest`.
    - Empresa com exceção própria de 3 faixas nunca recebe uma 4ª faixa vinda do serviço (asserção explícita de `count() === 3`).
    - `classificar()` com faturamento acima do último teto devolve `label = 'maxima'`.
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixaResolverTest` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixaResolverTest</automated>
  </verify>
  <done>Resolver devolve tabela e faixa corretas para os 9 cenários, com D-13 provado por teste.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: FechamentoRollupService — ML + Shopee em mês-calendário</name>
  <files>app/Services/Fechamento/FechamentoRollupService.php, tests/Feature/Phase137/Phase137RollupTest.php</files>
  <read_first>
    - app/Services/Metrics/MetricPeriodResolver.php (métodos `resolve`, `resolveSpecificMonth` linha ~225 e `resolveCurrentMonth` linha ~110 — as chaves `current_start`/`current_end`)
    - app/Services/Metrics/ShopeeMetricDiffService.php linhas 104-194 (`naJanela()` e o comentário de por que `whereDate` e não `whereBetween`)
    - app/Http/Controllers/AdminController.php linhas 141-200 (a janela móvel de 30 dias que D-06 elimina e o rollup ML por `AdmanMetric` que se preserva)
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-RESEARCH.md seção 1 (por que `company_monthly_revenues` não pode ser a fonte)
  </read_first>
  <behavior>
    - `janela('2026-08')` devolve início 2026-08-01 e fim 2026-08-31, independentemente da data de hoje.
    - `janela('2026-09')` quando hoje é 2026-09-02 devolve início 2026-09-01 e fim 2026-09-02 (mês corrente vai até hoje, nunca 30 dias para trás).
    - Empresa com `adman_metrics` de 3 dias dentro de agosto e 1 dia em julho: o faturamento de `2026-08` soma só os 3 dias de agosto.
    - Empresa com `adman_metrics` e `shopee_metrics` no mesmo mês devolve `faturamento_ml`, `faturamento_shopee` e `faturamento_total` = soma dos dois (D-05).
    - Empresa só com `shopee_metrics` devolve `faturamento_ml` nulo, `faturamento_shopee` preenchido e `faturamento_total` igual ao Shopee (D-07).
    - Empresa sem nenhuma métrica no mês devolve `faturamento_total` NULO — nunca 0.0.
    - Métrica de Shopee no último dia do mês (31/08) entra na janela de `2026-08` (regressão do `whereDate`).
  </behavior>
  <action>
Criar `App\Services\Fechamento\FechamentoRollupService` com injeção de `MetricPeriodResolver` via
construtor promovido.

`janela(string $mes): array` — recebe `'YYYY-MM'` e devolve `['inicio' => Carbon, 'fim' => Carbon]`.
Delegar a `MetricPeriodResolver::resolve()` usando `period_key = 'current_month'` quando `$mes` é o
mês corrente e `period_key = $mes` caso contrário, e converter `current_start`/`current_end` em
Carbon. NUNCA usar `Carbon::createFromFormat('Y-m', ...)` sem o dia: sem o dia explícito o PHP
preenche com o dia de hoje e ESTOURA para o mês seguinte quando o mês alvo tem menos dias (armadilha
já documentada em `ConsolidarMesDesempenho.php` linhas 118-127). Quando precisar ancorar à mão, usar
`Carbon::createFromFormat('Y-m-d', $mes . '-01')->startOfMonth()`.

`porEmpresa(string $mes, ?Collection $companies = null): array` — devolve um array indexado por
`company_id`, cada item com as chaves `faturamento_ml` (`float|null`), `faturamento_shopee`
(`float|null`) e `faturamento_total` (`float|null`). Duas queries agregadas, nunca N+1:
- ML: `AdmanMetric` com `whereBetween('reference_date', [inicio, fim])`, `whereNotNull('revenue')`,
  `selectRaw('company_id, SUM(revenue) as faturamento')`, `groupBy('company_id')` — o mesmo padrão
  que `AdminController::fechamento()` já usa para mês passado, sem chamada HTTP.
- Shopee: `ShopeeMetric` com `whereDate('reference_date','>=',$inicio->toDateString())` e
  `whereDate('reference_date','<=',$fim->toDateString())`, `selectRaw('company_id, SUM(revenue) as
  faturamento')`, `groupBy('company_id')`. `whereDate` é obrigatório: `reference_date` é persistido
  como datetime e `whereBetween` puro compararia string, excluindo o último dia do mês.
Quando `$companies` é passado, restringir as duas queries com `whereIn('company_id', ...)`.

`faturamento_total` é a soma dos dois lados quando pelo menos um existe, e NULO quando nenhum
existe. Nunca devolver 0.0 para representar ausência — "sem faturamento" e "faturou zero" são
estados diferentes, e o UI-SPEC reserva copy distinta para o primeiro.

Escrever `Phase137RollupTest` ANTES da implementação, cobrindo cada item do bloco `<behavior>`,
com `Carbon::setTestNow()` para travar "hoje" nos casos de mês corrente.
  </action>
  <acceptance_criteria>
    - `grep -c "subDays(30)" app/Services/Fechamento/FechamentoRollupService.php` retorna 0.
    - `grep -c "CompanyMonthlyRevenue\|company_monthly_revenues" app/Services/Fechamento/FechamentoRollupService.php` retorna 0.
    - `grep -c "AdmanMetricDiffService" app/Services/Fechamento/FechamentoRollupService.php` retorna 0.
    - A query de Shopee usa `whereDate` (asserção coberta pelo teste do dia 31 entrando na janela).
    - Empresa sem métrica devolve `faturamento_total === null` (asserção `assertNull`, não `assertEquals(0)`).
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137RollupTest` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137RollupTest</automated>
  </verify>
  <done>Rollup devolve ML, Shopee e total por empresa em janela de mês-calendário, com ausência distinguível de zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tabelas de métrica → valor de cobrança | soma errada de janela vira faixa errada e mensalidade errada |
| resolução de tabela → contrato do cliente | aplicar a tabela do serviço errado cobra valor que o cliente não assinou |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-08 | Tampering | `FechamentoRollupService::janela()` | mitigate | janela vem de `MetricPeriodResolver`, implementação única já testada; teste explícito prova que `2026-09` não retrocede 30 dias e que o dia 31 entra na janela |
| T-137-09 | Repudiation | `FechamentoFaixaResolver::paraEmpresa()` | mitigate | o retorno carrega `origem` e `servico_id`, gravados no snapshot pelo plano 05 — sempre dá para dizer por que aquela faixa foi aplicada |
| T-137-10 | Information Disclosure | serviços de domínio sem checagem de permissão | accept | são serviços internos sem rota própria; a autorização vive no grupo `role:admin` das rotas que os consomem (planos 06 e 07) |
| T-137-11 | Elevation of Privilege | exceção por empresa mascarando a tabela do serviço | mitigate | D-13 all-or-nothing implementado explicitamente e coberto por teste (`count() === 3`); a origem fica registrada no snapshot para auditoria |
</threat_model>

<verification>
1. `"C:/xampp/php/php.exe" artisan test --filter=Phase137` — exit code 0.
2. `"C:/xampp/php/php.exe" artisan test --filter=AdminFechamentoControllerTest` — exit code 0 (nenhum call-site foi tocado ainda).
</verification>

<success_criteria>
- Existe um único lugar que responde "qual tabela vale para esta empresa" e um único lugar que responde "quanto ela faturou neste mês".
- Nenhum dos dois usa janela móvel de 30 dias, `company_monthly_revenues` ou chamada HTTP.
- D-13 e D-05 provados por teste automatizado.
</success_criteria>

<output>
Criar `.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-03-SUMMARY.md` ao concluir.
</output>
