---
phase: 137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab
plan: 06
type: execute
wave: 4
depends_on: ["137-01", "137-03", "137-05"]
files_modified:
  - app/Http/Requests/SalvarFaixasFaturamentoRequest.php
  - app/Http/Controllers/FechamentoController.php
  - routes/web.php
  - tests/Feature/Phase137/Phase137FaixasCrudTest.php
  - tests/Feature/Phase137/Phase137CompetenciaEndpointTest.php
autonomous: true
requirements: [D-04, D-11, D-12, D-13]

must_haves:
  truths:
    - "O Administrativo consegue cadastrar a tabela de faixas de uma empresa pelo sistema, sem passar por contrato"
    - "Cadastrar faixas com limites sobrepostos é recusado com mensagem que diz qual faixa conflita"
    - "Voltar a usar a tabela do serviço apaga a exceção da empresa"
    - "Fechar a competência pela tela grava o snapshot; refazer exige motivo"
    - "Usuário sem role admin recebe 403 em todos os endpoints novos"
  artifacts:
    - path: "app/Http/Requests/SalvarFaixasFaturamentoRequest.php"
      provides: "Validação da tabela inteira de faixas, incluindo sobreposição e faixa sem teto única"
      contains: "withValidator"
    - path: "app/Http/Controllers/FechamentoController.php"
      provides: "CRUD das faixas (serviço e empresa) e as ações de fechar/refazer competência"
      exports: ["salvarFaixasServico", "salvarFaixasEmpresa", "removerFaixasEmpresa", "fecharCompetencia", "refazerCompetencia"]
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Controllers/FechamentoController.php"
      via: "rotas dentro do grupo auth+verified+role:admin com prefixo administrativo"
      pattern: "FechamentoController"
    - from: "app/Http/Controllers/FechamentoController.php"
      to: "app/Console/Commands/ConsolidarMesFechamento.php"
      via: "Artisan::call do comando de consolidação"
      pattern: "fechamento:consolidar-mes"
---

<objective>
A superfície HTTP da fase: cadastro manual da tabela de faixas (D-04) e as ações de fechar e refazer
uma competência (D-11/D-12), num controller novo e dedicado.

Purpose: D-04 é requisito explícito do usuário — "um jeito de cadastrar a tabela progressiva pelo
sistema, como se estivesse fazendo contrato, mas só para o sistema saber as faixas", cobrindo
tabelas antigas e fora do padrão. E o fechamento precisa ser um ato do Administrativo pela tela, não
um cron que decide sozinho quando o número congela.

Output: 1 FormRequest, 1 controller novo, rotas dentro do grupo admin existente, 2 arquivos de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-PATTERNS.md
@.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-UI-SPEC.md

<interfaces>
Contratos que o executor precisa e NÃO deve ir procurar no código:

Grupo de rotas já existente em `routes/web.php` linha 1393:
`Route::middleware(['auth','verified','role:admin'])->prefix('administrativo')->name('admin.')->group(...)`.
As rotas novas entram DENTRO desse grupo — guard e prefixo já resolvidos, não criar grupo novo. O
próprio arquivo tem o comentário: rotas específicas devem vir ANTES de `/financeiro/{company}` para
não colidir com o parâmetro dinâmico. Rotas de `/financeiro` já existentes:
`financeiro`, `financeiro.relatorio.geral`, `financeiro.relatorio.enviar`, `financeiro.sync`,
`financeiro.update`, `financeiro.recebido`, `financeiro.relatorio`.

Do plano 01: `App\Models\ServicoFaixaFaturamento` (`CONTEXTO_FECHAMENTO`, `CONTEXTO_CONTRATO`,
colunas `servico_id`, `contexto`, `ordem`, `limite_superior`, `valor`) e
`App\Models\EmpresaFaixaFaturamento` (colunas `company_id`, `ordem`, `limite_superior`, `valor`).

Do plano 03: `App\Services\Fechamento\FechamentoFaixaResolver::paraEmpresa()`.

Do plano 05: comando `fechamento:consolidar-mes` com as opções `--mes=`, `--motivo=`, `--por=` e
`--se-ausente`; retorna exit code 0 em sucesso e 1 em falha (inclusive quando a competência já está
fechada e falta `--motivo`).

Molde de validação de sobreposição: `app/Http/Requests/UpdateBonusFaixaRequest.php` — `authorize()`
devolvendo `$this->user()?->isAdmin() === true`, e `withValidator()` com `$validator->after(...)`
adicionando UMA mensagem e parando (não iterar o resto depois de achar o primeiro conflito).

Molde de controller CRUD simples: `app/Http/Controllers/DesempenhoConfigController.php`.

Copy obrigatória (UI-SPEC, usar literalmente): erro de sobreposição
`Essa faixa se sobrepõe à faixa {ordem}. Ajuste o limite antes de salvar.`; erro ao fechar
`Não foi possível fechar {mês}/{ano}. Nada foi alterado — tente novamente ou avise o time técnico.`;
erro ao refazer `Não foi possível refazer o fechamento de {mês}/{ano}. O registro anterior continua
valendo.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: FormRequest de validação da tabela inteira de faixas</name>
  <files>app/Http/Requests/SalvarFaixasFaturamentoRequest.php, tests/Feature/Phase137/Phase137FaixasCrudTest.php</files>
  <read_first>
    - app/Http/Requests/UpdateBonusFaixaRequest.php (arquivo inteiro: `authorize()`, `rules()`, `withValidator()` com a regra composta e a mensagem única)
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-UI-SPEC.md (a copy exata da mensagem de erro de sobreposição)
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md (D-13 — a tabela é salva inteira, nunca linha a linha)
  </read_first>
  <behavior>
    - Payload com 3 faixas de `limite_superior` 100000, 200000 e null passa na validação.
    - Payload com duas faixas de `limite_superior` nulo é recusado (só a última faixa pode ser sem teto).
    - Payload cuja faixa de maior `ordem` tem `limite_superior` preenchido enquanto uma anterior tem nulo é recusado.
    - Payload com `limite_superior` não crescente na ordem (ex.: ordem 1 = 200000, ordem 2 = 100000) é recusado com a mensagem exata de sobreposição citando a ordem conflitante.
    - Payload com `ordem` duplicada é recusado.
    - Payload com `valor` negativo é recusado.
    - Payload com array `faixas` vazio é recusado (para "não ter tabela" existe a ação de remover, não salvar vazio).
    - Usuário não-admin recebe 403 antes de qualquer validação.
  </behavior>
  <action>
Criar `App\Http\Requests\SalvarFaixasFaturamentoRequest`.

`authorize()` devolve `$this->user()?->isAdmin() === true` — guard duplo junto com o middleware
`role:admin` do grupo de rotas, exatamente como `UpdateBonusFaixaRequest` já faz.

`rules()` valida: `faixas` obrigatório, array, mínimo 1 item; `faixas.*.ordem` obrigatório, inteiro,
mínimo 1; `faixas.*.limite_superior` nullable, numérico, mínimo 0; `faixas.*.valor` obrigatório,
numérico, mínimo 0.

`withValidator()` com `$validator->after()` aplicando, sobre o array já ordenado por `ordem`:
(a) `ordem` não pode repetir; (b) exatamente uma faixa pode ter `limite_superior` nulo, e ela precisa
ser a de MAIOR `ordem` — é a faixa "sem teto"; (c) os `limite_superior` preenchidos precisam ser
estritamente crescentes na ordem; quando a regra (c) falhar, adicionar a mensagem literal
`Essa faixa se sobrepõe à faixa {ordem}. Ajuste o limite antes de salvar.` com `{ordem}` substituído
pela ordem da faixa anterior, e RETORNAR — uma sobreposição basta, mesma disciplina de
`UpdateBonusFaixaRequest`.

Mensagens em pt-BR. Sem `messages()` genérico que sobrescreva a mensagem específica de sobreposição.

Criar `tests/Feature/Phase137/Phase137FaixasCrudTest.php` com os casos de validação do bloco
`<behavior>` (os casos de persistência entram na Tarefa 2, no mesmo arquivo).
  </action>
  <acceptance_criteria>
    - Payload com duas faixas sem teto retorna 422 com erro apontando o campo de `limite_superior`.
    - Payload não crescente retorna 422 e a mensagem contém literalmente `se sobrepõe à faixa`.
    - Payload válido de 3 faixas passa da validação (o teste da Tarefa 2 confirma a persistência).
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasCrudTest` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasCrudTest</automated>
  </verify>
  <done>Tabela de faixas só é aceita quando é uma régua coerente: ordens únicas, limites crescentes e uma única faixa sem teto no fim.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: FechamentoController — CRUD das faixas por serviço e por empresa</name>
  <files>app/Http/Controllers/FechamentoController.php, tests/Feature/Phase137/Phase137FaixasCrudTest.php</files>
  <read_first>
    - app/Http/Controllers/DesempenhoConfigController.php (molde de controller CRUD simples, `back()->with('success', ...)`)
    - app/Models/ServicoFaixaFaturamento.php e app/Models/EmpresaFaixaFaturamento.php (plano 01)
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-CONTEXT.md (D-04 e D-13)
  </read_first>
  <behavior>
    - POST das faixas de uma empresa substitui a tabela INTEIRA dela: 5 faixas antigas + payload de 3 → sobram exatamente 3 linhas.
    - POST das faixas de um serviço com `contexto = 'fechamento'` não toca nas linhas de `contexto = 'contrato'` do mesmo serviço, e vice-versa.
    - DELETE das faixas de uma empresa apaga todas as linhas dela e faz o resolver voltar a devolver `origem = 'servico'`.
    - Uma falha no meio do salvamento não deixa a empresa com meia tabela (transação).
    - Não-admin recebe 403 nos três endpoints.
  </behavior>
  <action>
Criar `App\Http\Controllers\FechamentoController` com três métodos de faixas.

`salvarFaixasServico(SalvarFaixasFaturamentoRequest $request, Servico $servico)` — lê o `contexto`
do payload validado (`'fechamento'` por padrão; aceita `'contrato'` para alimentar D-03) e, dentro
de `DB::transaction`, apaga todas as linhas de `servico_faixas_faturamento` daquele `servico_id` E
daquele `contexto` (nunca dos dois contextos de uma vez) e recria as faixas recebidas. Retorna
`back()->with('success', 'Tabela de faixas salva.')`.

`salvarFaixasEmpresa(SalvarFaixasFaturamentoRequest $request, Company $company)` — dentro de
`DB::transaction`, apaga todas as linhas de `empresa_faixas_faturamento` daquela empresa e recria as
recebidas. É D-13 na prática: a exceção é a tabela inteira, substituída de uma vez, nunca editada
linha a linha. Retorna `back()->with('success', 'Tabela própria da empresa salva.')`.

`removerFaixasEmpresa(Request $request, Company $company)` — apaga todas as linhas de
`empresa_faixas_faturamento` da empresa (a ação "Voltar a usar a tabela do serviço" do UI-SPEC) e
retorna `back()->with('success', 'Empresa voltou a usar a tabela do serviço.')`.

O `contexto` aceito precisa ser validado contra as duas constantes de `ServicoFaixaFaturamento` —
qualquer outro valor é 422.

Os três métodos não fazem checagem de permissão própria além do `authorize()` do FormRequest e do
middleware do grupo; `removerFaixasEmpresa` usa `abort_unless($request->user()?->isAdmin(), 403)`
porque não passa por FormRequest.

Acrescentar os casos de persistência do bloco `<behavior>` ao `Phase137FaixasCrudTest`.
  </action>
  <acceptance_criteria>
    - Após POST com 3 faixas numa empresa que tinha 5, `DB::table('empresa_faixas_faturamento')->where('company_id', $id)->count()` é exatamente 3.
    - Após POST no contexto `fechamento` de um serviço, a contagem de linhas de `contexto = 'contrato'` daquele serviço permanece inalterada.
    - Após DELETE, `FechamentoFaixaResolver::paraEmpresa($company)['origem']` é `'servico'`.
    - Usuário com `role` diferente de `admin` recebe status 403 nos três endpoints (3 asserções distintas).
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasCrudTest` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137FaixasCrudTest</automated>
  </verify>
  <done>Cadastro manual da tabela por empresa e por serviço funciona, é all-or-nothing e é fechado para não-admin.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: Endpoints de fechar/refazer competência e registro das rotas</name>
  <files>app/Http/Controllers/FechamentoController.php, routes/web.php, tests/Feature/Phase137/Phase137CompetenciaEndpointTest.php</files>
  <read_first>
    - routes/web.php linhas 1393-1412 (o grupo admin e o comentário sobre ordem das rotas antes de `/financeiro/{company}`)
    - app/Http/Controllers/AdminController.php linhas 805-825 (`enviarRelatorioGeral`, molde de endpoint que valida `mes` com regex e devolve JSON)
    - app/Console/Commands/ConsolidarMesFechamento.php (plano 05 — opções e exit codes)
    - .planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-UI-SPEC.md seção 5 e a tabela de Copywriting
  </read_first>
  <behavior>
    - POST de fechar com `mes=2026-08` grava linhas em `fechamento_snapshots` e devolve 200.
    - POST de fechar numa competência já fechada devolve 409 com a mensagem de erro do UI-SPEC, e o snapshot permanece o mesmo.
    - POST de refazer sem `motivo` devolve 422.
    - POST de refazer com `motivo` grava linha em `fechamento_reconsolidacoes` com `reconsolidado_por` igual ao id do usuário autenticado, e devolve 200.
    - POST de fechar com `mes` no futuro devolve 422.
    - POST de fechar com `mes` fora do formato `YYYY-MM` devolve 422.
    - Não-admin recebe 403 nos dois endpoints.
  </behavior>
  <action>
Acrescentar ao `FechamentoController` dois métodos.

`fecharCompetencia(Request $request)` — valida `mes` obrigatório com
`regex:/^\d{4}-\d{2}$/` e recusa mês futuro (422 com mensagem em pt-BR). Chama
`Artisan::call('fechamento:consolidar-mes', ['--mes' => $mes, '--por' => $request->user()->id])` e
inspeciona o EXIT CODE retornado por `Artisan::call` — nunca a saída de texto. Exit 0 devolve JSON
200 com `message` confirmando o fechamento. Exit diferente de 0 devolve JSON 409 com a copy literal
do UI-SPEC `Não foi possível fechar {mês}/{ano}. Nada foi alterado — tente novamente ou avise o time
técnico.`, com `{mês}/{ano}` substituídos pelo mês por extenso em pt-BR
(`translatedFormat('F Y')`), e registra `Log::error` prefixado `"[Fechamento]"` com a saída do
comando (`Artisan::output()`).

`refazerCompetencia(Request $request)` — valida `mes` com o mesmo regex e `motivo` obrigatório,
string, mínimo 10 caracteres, máximo 1000. Chama o mesmo comando acrescentando
`'--motivo' => $motivo`. Mesma inspeção por exit code. Falha devolve 409 com a copy literal
`Não foi possível refazer o fechamento de {mês}/{ano}. O registro anterior continua valendo.`

Os dois métodos usam `set_time_limit(0)` antes de chamar o comando (a consolidação percorre ~190
empresas; o precedente é `AdminController::syncFaturamento`), e `abort_unless($request->user()
?->isAdmin(), 403)` no topo.

Em `routes/web.php`, DENTRO do grupo `['auth','verified','role:admin']` com prefixo
`administrativo` e nome `admin.`, e ANTES da rota `Route::patch('/financeiro/{company}', ...)`
(o próprio arquivo documenta a razão), registrar:
- `POST /financeiro/competencia/fechar` → `FechamentoController@fecharCompetencia`, nome `financeiro.competencia.fechar`
- `POST /financeiro/competencia/refazer` → `FechamentoController@refazerCompetencia`, nome `financeiro.competencia.refazer`
- `POST /financeiro/faixas/servico/{servico}` → `salvarFaixasServico`, nome `financeiro.faixas.servico`
- `POST /financeiro/faixas/empresa/{company}` → `salvarFaixasEmpresa`, nome `financeiro.faixas.empresa`
- `DELETE /financeiro/faixas/empresa/{company}` → `removerFaixasEmpresa`, nome `financeiro.faixas.empresa.remover`
Com o `use App\Http\Controllers\FechamentoController;` correspondente no topo do arquivo. Nenhuma
rota existente é alterada ou removida.

Escrever `Phase137CompetenciaEndpointTest` cobrindo cada item do bloco `<behavior>`, com todas as
asserções de efeito por reconsulta às tabelas.
  </action>
  <acceptance_criteria>
    - `"C:/xampp/php/php.exe" artisan route:list --name=financeiro` lista as 5 rotas novas além das 7 já existentes.
    - Nenhuma das 7 rotas de `/financeiro` já existentes teve método, URI ou nome alterado (conferível no diff de `routes/web.php`).
    - POST de refazer com motivo válido cria exatamente 1 linha em `fechamento_reconsolidacoes` com `reconsolidado_por` igual ao id do usuário logado.
    - Fechar competência já fechada devolve status 409 e o `faturamento_total` gravado permanece igual (asserção por reconsulta antes/depois).
    - `"C:/xampp/php/php.exe" artisan test --filter=Phase137CompetenciaEndpointTest` termina com exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>"C:/xampp/php/php.exe" artisan test --filter=Phase137CompetenciaEndpointTest</automated>
  </verify>
  <done>Fechar e refazer competência funcionam pela HTTP, o veredito vem do exit code do comando e o motivo é obrigatório para refazer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → endpoints de faixas | entrada não confiável define quanto o cliente paga |
| browser → endpoint de fechar/refazer | ação que congela (ou reabre) um número de cobrança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-20 | Elevation of Privilege | rotas novas de `/financeiro/...` | mitigate | rotas registradas DENTRO do grupo `['auth','verified','role:admin']` já existente + `authorize()` no FormRequest + `abort_unless(isAdmin)` nos métodos sem FormRequest; teste explícito de 403 para não-admin em cada endpoint |
| T-137-21 | Tampering | payload de faixas com valores incoerentes | mitigate | `SalvarFaixasFaturamentoRequest` valida tipos, ordens únicas, limites estritamente crescentes e faixa sem teto única; salvamento em transação (nunca meia tabela) |
| T-137-22 | Repudiation | quem refez o fechamento | mitigate | `refazerCompetencia` repassa `--por` com o id do usuário autenticado e `--motivo` obrigatório; o writer grava os dois em `fechamento_reconsolidacoes` |
| T-137-23 | Spoofing | CSRF nas ações POST/DELETE | mitigate | rotas dentro do grupo `web` padrão, com proteção CSRF ativa; `/financeiro` não está na lista de exceção de CSRF (só `/implementacao/*` está) |
| T-137-24 | Denial of Service | consolidação síncrona travando o php-fpm | mitigate | `set_time_limit(0)` no endpoint e uso do comando existente que faz 2 queries agregadas; nenhuma chamada HTTP externa no caminho |
</threat_model>

<verification>
1. `"C:/xampp/php/php.exe" artisan test --filter=Phase137` — exit code 0.
2. `"C:/xampp/php/php.exe" artisan route:list --name=financeiro` mostra 12 rotas (7 antigas + 5 novas).
3. `"C:/xampp/php/php.exe" artisan test --filter=AdminFechamentoControllerTest` — exit code 0.
</verification>

<success_criteria>
- Cadastro manual de tabela de faixas existe, valida a régua e é all-or-nothing.
- Fechar e refazer competência existem como ações HTTP, com motivo obrigatório para refazer.
- Nenhuma rota existente de `/financeiro` mudou.
- Todos os endpoints novos devolvem 403 para não-admin.
</success_criteria>

<output>
Criar `.planning/phases/137-fechamento-mensal-faturamento-por-empresa-grupo-contra-a-tab/137-06-SUMMARY.md` ao concluir.
</output>
