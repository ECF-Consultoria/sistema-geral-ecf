---
phase: 38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r
plan: 02
type: execute
wave: 1
depends_on: ["38-01"]
files_modified:
  - app/Services/PublicadorScoreService.php
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Publicador com dados completos recebe um score float entre 0 e 100 com classificação coerente"
    - "Eixo sem dado retorna valor null e o peso é redistribuído automaticamente (score nunca NaN)"
    - "Pontualidade usa empresas com responsavel_id=userId; os outros 4 eixos usam publicações com user_id=userId"
  artifacts:
    - path: "app/Services/PublicadorScoreService.php"
      provides: "Cálculo dos 5 eixos + score 0-100 + classificação para um publicador"
      contains: "class PublicadorScoreService"
      min_lines: 120
  key_links:
    - from: "app/Services/PublicadorScoreService.php"
      to: "App\\Models\\MlbEmpresa"
      via: "where('responsavel_id', $userId) para Pontualidade"
      pattern: "responsavel_id"
    - from: "app/Services/PublicadorScoreService.php"
      to: "scoreFinal()"
      via: "redistribuição de pesos nulos copiada do PortfolioScoreService"
      pattern: "function scoreFinal"
---

<objective>
Criar `app/Services/PublicadorScoreService.php`, espelhando rigorosamente o
`PortfolioScoreService` (copiar `scoreFinal()` e `classificar()` VERBATIM). O método público
`compute(int $userId, string $mesRef): array` calcula os 5 eixos do publicador
(Meta · Produtividade · Pontualidade · Conversão · Qualidade), normaliza cada um para 0-100,
monta o array `$pontos` com pesos e retorna score, classificação, `pontos_categoria` (radar)
e `metricas` (MiniMetrics). Esta Wave leva a suíte unitária da Wave 0 para GREEN.

Os valores `feito`, `vendas` e `meta` vêm de fora (do controller, via `calcularKpis()`) — o
Service NÃO recalcula a query de KPI. Apenas Pontualidade e Qualidade fazem queries próprias
(SKUs por empresa responsável; problemas/feedbacks por publicação).

Purpose: Centralizar a lógica de score do publicador num Service testável, análogo direto da
Carteira dos analistas de ADS, sem migration e sem dependência de API externa.
Output: 1 Service novo; suíte `PublicadorScoreServiceTest` em GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/CONTEXT.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-RESEARCH.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-PATTERNS.md

<interfaces>
<!-- Contrato de assinatura e retorno (do RESEARCH §"PublicadorScoreService — Assinatura e Retorno"). -->

public function compute(int $userId, string $mesRef): array

Retorno:
- 'score' => float (0-100)
- 'classificacao' => string ('excelente'|'bom'|'atencao'|'critico')
- 'pontos_categoria' => [
     'meta'          => ['valor' => float|null, 'peso' => 35],
     'produtividade' => ['valor' => float|null, 'peso' => 25],
     'pontualidade'  => ['valor' => float|null, 'peso' => 20],
     'conversao'     => ['valor' => float|null, 'peso' => 10],
     'qualidade'     => ['valor' => float|null, 'peso' => 10],
   ]
- 'metricas' => [
     'meta'          => ['feito' => int, 'alvo' => int, 'pct' => float|null],
     'produtividade' => ['feito' => int, 'meta' => int, 'pct' => float|null],
     'pontualidade'  => ['total_skus' => int, 'atrasados' => int, 'pct_no_prazo' => float|null],
     'conversao'     => ['vendidos' => int, 'feito' => int, 'pct' => float|null],
     'qualidade'     => ['pct_sem_problema' => float|null, 'pct_feedbacks_resolvidos' => float|null],
   ]

Métodos a copiar VERBATIM de app/Services/PortfolioScoreService.php:
- scoreFinal(array $pontos): float  (linhas 379-390 — redistribuição de pesos nulos)
- classificar(float $score): string (linhas 392-398 — limiares 85/70/55)

Helpers do controller que o Service NÃO chama (são private):
- calcularKpis() devolve ['feito' => int, 'vendas' => int, ...] — o controller passa esses valores ao Service na Wave 2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar PublicadorScoreService com compute() + scoreFinal()/classificar() verbatim</name>
  <read_first>
    - app/Services/PortfolioScoreService.php (MOLDE — ler scoreFinal() linhas 379-390, classificar() linhas 392-398, scoreQualidade() linhas 363-374, estrutura do array $pontos e do retorno de compute; copiar scoreFinal e classificar verbatim)
    - app/Http/Controllers/MlbController.php (linhas 461-481 do dashboard — loop de atrasos sobre skus_estagio1/2/3 + prazo_estagio*; linhas 210-253 metaParaMes/metaCadastrada para entender a meta)
    - app/Models/MlbEmpresa.php (casts skus_estagio* => 'array', normalizaSkus(), prazo_estagio*, responsavel_id)
    - app/Models/Publicacao.php (casts de data, problema, comentario_resolvido, net_billing)
    - .planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-RESEARCH.md (seção "Fórmula de Normalização 0-100 para os 5 Eixos" — fórmulas exatas dos 5 eixos)
    - .planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-PATTERNS.md (seção do Service — imports, docblock, padrão de retorno)
  </read_first>
  <behavior>
    - Test 1 (PUB-01): publicador com dados completos → score float em [0,100], pontos_categoria com 5 chaves, classificacao em {excelente,bom,atencao,critico}.
    - Test 2 (PUB-02): publicador sem publicações no mês → conversao.valor e qualidade.valor são null; score calculado só com eixos que têm dado (sem NaN/exception).
    - Test 3 (PUB-03): publicador sem MlbEmpresa com responsavel_id=ele → pontualidade.valor null e metricas.pontualidade.total_skus = 0.
  </behavior>
  <action>
    Criar `app/Services/PublicadorScoreService.php`, namespace `App\Services`, imports `App\Models\MlbEmpresa`, `App\Models\Publicacao`, `Carbon\Carbon`. Sem injeção de dependências no construtor (diferente do PortfolioScoreService — não há AdmanService). Comentários e docblock em pt-BR.

    Assinatura do método principal: `public function compute(int $userId, string $mesRef, int $feito, int $vendas, int $meta): array`. Receber `feito`, `vendas` e `meta` como parâmetros (vindos de `calcularKpis()` no controller — NÃO recalcular essa query; ver Armadilha 3/6 do RESEARCH). Derivar `$primeiro`/`$ultimo` do mês via `Carbon::createFromFormat('Y-m', $mesRef)->startOfMonth()`/`->endOfMonth()`.

    Definir os pesos como constantes de classe ajustáveis (ex.: `private const PESO_META = 35; PESO_PRODUTIVIDADE = 25; PESO_PONTUALIDADE = 20; PESO_CONVERSAO = 10; PESO_QUALIDADE = 10;`).

    Calcular cada eixo conforme as fórmulas exatas do RESEARCH §"Fórmula de Normalização":
    - Meta: `$meta > 0 ? min(100.0, round(($feito/$meta)*100, 1)) : null`.
    - Produtividade: dois segmentos lineares (0%→0pts, 100%→80pts, ≥130%→100pts), null quando `$meta == 0`.
    - Pontualidade: iterar `MlbEmpresa::where('responsavel_id', $userId)->get([...skus/prazo...])`, contar `$totalSkus` e `$atrasadosCnt` (SKU com `atrasado=true` OU pendente `ok=false` com prazo vencido), normalizar `(($totalSkus-$atrasadosCnt)/$totalSkus)*100`; null quando `$totalSkus == 0`. Usar `$e->{"skus_estagio{$stage}"} ?? []` (Armadilha 7).
    - Conversão: `$feito > 0 ? round(($vendas/$feito)*100, 1) : null`.
    - Qualidade: componente A = % publicações sem problema (peso 0.60), componente B = % feedbacks resolvidos (publicações com comentário não vazio; peso 0.40), combinar tratando null em cada componente (espelha `scoreQualidade()` do molde); null quando ambos null.

    Montar `$pontos` com a estrutura idêntica do molde, chamar `$score = $this->scoreFinal($pontos)` e `$classif = $this->classificar($score)`. Copiar `scoreFinal()` e `classificar()` VERBATIM do PortfolioScoreService (não reimplementar). Retornar o array completo com `score`, `classificacao`, `pontos_categoria` (= `$pontos`) e `metricas` conforme o contrato em <interfaces>.

    Proteger toda divisão com checagem `> 0` (Armadilha 4). Não logar (sem API externa). Após codar, ajustar a suíte unitária da Wave 0 se a assinatura de `compute()` divergir (os testes passam `feito`/`vendas`/`meta` — alinhar).
  </action>
  <verify>
    <automated>php artisan test --filter=PublicadorScoreServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `app/Services/PublicadorScoreService.php` existe e contém `class PublicadorScoreService`
    - Contém `function compute(`, `function scoreFinal(` e `function classificar(`
    - `scoreFinal()` e `classificar()` são idênticos aos do PortfolioScoreService (mesmos limiares 85/70/55, mesma redistribuição de peso nulo)
    - Pontualidade referencia `responsavel_id` e itera `skus_estagio` com fallback `?? []`
    - `php artisan test --filter=PublicadorScoreServiceTest` sai com código 0 (3 testes GREEN: PUB-01, PUB-02, PUB-03)
  </acceptance_criteria>
  <done>Service criado; suíte unitária da Wave 0 verde (RED → GREEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → Service | `$userId`/`$mesRef`/`$feito`/`$vendas`/`$meta` vêm do controller já autenticado; `$mesRef` validado por regex na Wave 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02 | Denial of Service | divisão por zero / SUM null | mitigate | Toda divisão protegida por `> 0`; eixos sem dado retornam null (Armadilhas 4 e 7 do RESEARCH) |
| T-38-03 | Information Disclosure | Service consulta dados de outro publicador | mitigate | `compute()` filtra estritamente por `$userId` (responsavel_id e user_id); controller já garante que `$userId` é o do usuário autenticado |
| T-38-SC | Tampering | npm/composer installs | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `php artisan test --filter=PublicadorScoreServiceTest` verde (3/3)
- `php artisan test --filter=Phase38` não regride (Polos intactos; MeuPainelControllerTest ainda RED até Wave 2 — esperado)
- Grep confirma `scoreFinal` e `classificar` no novo Service
</verification>

<success_criteria>
- Service implementa os 5 eixos com as fórmulas exatas do RESEARCH
- Pesos como constantes ajustáveis
- Redistribuição de pesos nulos via scoreFinal() copiado verbatim
- Suíte unitária da Wave 0 em GREEN
</success_criteria>

<output>
Create `.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-02-SUMMARY.md` when done
</output>
