---
phase: 38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r
plan: 03
type: execute
wave: 2
depends_on: ["38-02"]
files_modified:
  - app/Http/Controllers/MlbController.php
autonomous: true
requirements: [PUB-04, PUB-05, PUB-06]
nyquist_compliant: true

must_haves:
  truths:
    - "meuPainel() passa as props novas score_publicador, faturamento_mes e net_billing_timeseries"
    - "Todas as props existentes (kpis, meta, mesRef, meses, evolucaoDiaria, problemas...) continuam presentes"
    - "Publicador sem publicações ou com net_billing null não causa exceção (faturamento_mes = 0)"
    - "$mesRef inválido cai no fallback now() via regex YYYY-MM"
  artifacts:
    - path: "app/Http/Controllers/MlbController.php"
      provides: "meuPainel() evoluído com PublicadorScoreService + faturamento + timeseries + validação de mesRef"
      contains: "PublicadorScoreService"
  key_links:
    - from: "app/Http/Controllers/MlbController.php"
      to: "App\\Services\\PublicadorScoreService"
      via: "compute(user->id, mesRef, feito, vendas, meta)"
      pattern: "PublicadorScoreService"
    - from: "app/Http/Controllers/MlbController.php"
      to: "Inertia 'Mlb/MeuPainel'"
      via: "props score_publicador/faturamento_mes/net_billing_timeseries"
      pattern: "net_billing_timeseries"
---

<objective>
Evoluir `MlbController@meuPainel()` (linhas 531-641) para ADICIONAR — sem remover nada — as
props que alimentam o novo Painel do Publicador: `score_publicador` (do
`PublicadorScoreService`), `faturamento_mes` (SUM net_billing do mês), `anuncios_feitos` e
`vendas_mes` (reuso de `$kpis`), e `net_billing_timeseries` (evolução acumulada do
faturamento por dia). Adicionar a validação de `$mesRef` (regex YYYY-MM com fallback `now()`).
Esta Wave leva a suíte de feature da Wave 0 para GREEN.

Reaproveitar os helpers existentes: `calcularKpis()` já entrega `feito` e `vendas` — passar
ao Service em vez de recalcular (Armadilhas 3 e 6 do RESEARCH).

Purpose: Conectar o cálculo do Service à página Inertia, mantendo backward compatibility total
das props atuais.
Output: `meuPainel()` evoluído; suíte `MeuPainelControllerTest` em GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/CONTEXT.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-RESEARCH.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-PATTERNS.md

<interfaces>
<!-- Estado atual do meuPainel() e contrato do Service (já implementado na Wave 1). -->

Service (Wave 1):
- App\Services\PublicadorScoreService::compute(int $userId, string $mesRef, int $feito, int $vendas, int $meta): array

meuPainel() hoje (linhas 531-641) já tem em escopo:
- $user = $request->user()
- $mesRef = $request->get('mes', now()->format('Y-m'))
- $meta = $this->metaParaMes($user->id, $mesRef)        // int — nunca null (fallback 220)
- $ref = Carbon::createFromFormat('Y-m', $mesRef)->startOfMonth()
- $kpis = $this->calcularKpis($user->id, $ref, $meta)   // $kpis['feito'] int, $kpis['vendas'] int
- $primeiro = $ref->copy()->startOfMonth()->toDateString()
- $ultimo   = $ref->copy()->endOfMonth()->toDateString()
- Inertia::render('Mlb/MeuPainel', [...]) com props: kpis, evolucaoDiaria, topEmpresas,
  feedbacks, meta, mesRef, meses, problemas, ticketEvolucao, ticketAtual

Props novas a adicionar (RESEARCH §"Props Expandidas"):
- 'score_publicador'        => array do Service
- 'faturamento_mes'         => float (SUM net_billing do mês, COALESCE/whereNotNull)
- 'anuncios_feitos'         => $kpis['feito']
- 'vendas_mes'              => $kpis['vendas']
- 'net_billing_timeseries'  => [['date' => 'Y-m-d', 'realizado' => float], ...] (acumulado)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Validar $mesRef, calcular faturamento + timeseries e chamar o Service</name>
  <read_first>
    - app/Http/Controllers/MlbController.php (método meuPainel linhas 531-641 — pontos de inserção; bloco use no topo do arquivo para adicionar import; query de $evolucaoDiaria linhas 545-552 como molde do timeseries)
    - app/Services/PublicadorScoreService.php (assinatura de compute() — confirmar ordem dos parâmetros feito/vendas/meta entregue na Wave 1)
    - .planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-RESEARCH.md (seção "net_billing_timeseries — query proposta" + §Segurança regex de $mesRef + Armadilhas 1 e 5)
    - .planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-PATTERNS.md (seção do controller — import, validação de $mesRef, Inertia::render expandido, query de timeseries)
  </read_first>
  <behavior>
    - Test PUB-04 (test_meu_painel_passa_props_novas): GET mlb.meu-painel → 200, component 'Mlb/MeuPainel', has score_publicador + faturamento_mes + net_billing_timeseries, e kpis + meta + mesRef continuam presentes.
    - Test PUB-05 (test_sem_publicacoes): publicador sem publicações → faturamento_mes = 0.0 e score_publicador.score = 0.0, sem exceção.
    - Test PUB-06 (test_net_billing_null): todas as publicações com net_billing null → faturamento_mes = 0.0 (SUM null tratado com COALESCE/cast).
  </behavior>
  <action>
    No bloco `use` do topo de `MlbController.php`, adicionar `use App\Services\PublicadorScoreService;`.

    Em `meuPainel()`, logo após `$mesRef = $request->get('mes', now()->format('Y-m'));` (linha 536), inserir a validação: se `$mesRef` não casar com `^\d{4}-\d{2}$`, reatribuir `$mesRef = now()->format('Y-m');` ANTES do `Carbon::createFromFormat('Y-m', $mesRef)` da linha 538 (evita exceção — §Segurança do RESEARCH).

    Após `$kpis` (linha 540), instanciar o Service e chamar compute passando os valores já calculados: `$scoreData = (new PublicadorScoreService())->compute($user->id, $mesRef, (int) $kpis['feito'], (int) $kpis['vendas'], (int) $meta);` (NÃO recalcular feito/vendas — Armadilhas 3 e 6).

    Calcular `$faturamentoMes` como SUM de `net_billing` das publicações do mês do publicador (tipo != variacao), protegendo null: usar `(float) (Publicacao::where('user_id', $user->id)->whereBetween('data', [$primeiro, $ultimo])->where('tipo', '!=', 'variacao')->sum('net_billing') ?? 0)` — `sum()` já retorna 0/null seguro; garantir cast `(float)` (Armadilha 1). Os `$primeiro`/`$ultimo` já existem no escopo (linhas 542-543).

    Calcular `$netBillingTimeseries` conforme RESEARCH §"net_billing_timeseries — query proposta": `Publicacao::where('user_id', $user->id)->whereBetween('data', [$primeiro, $ultimo])->where('tipo', '!=', 'variacao')->whereNotNull('net_billing')->selectRaw('data, SUM(net_billing) as billing_dia')->groupBy('data')->orderBy('data')->get()`, depois acumular dia a dia com `&$acumulado` e mapear para `['date' => Carbon::parse($r->data)->format('Y-m-d'), 'realizado' => round($acumulado, 2)]` (formato Y-m-d para o XAxis; Armadilha 5).

    No `Inertia::render('Mlb/MeuPainel', [...])` (linhas 626-640), MANTER todas as 10 chaves existentes e ADICIONAR: `'score_publicador' => $scoreData`, `'faturamento_mes' => $faturamentoMes`, `'anuncios_feitos' => $kpis['feito']`, `'vendas_mes' => $kpis['vendas']`, `'net_billing_timeseries' => $netBillingTimeseries`. Não remover/renomear nenhuma prop existente.

    Comentários em pt-BR. Não criar rota nova, não tocar em `checkPubAccess`, não alterar permissão.
  </action>
  <verify>
    <automated>php artisan test --filter=MeuPainelControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - `MlbController.php` contém `use App\Services\PublicadorScoreService;` e a chamada `(new PublicadorScoreService())->compute(`
    - `meuPainel()` contém o guard `preg_match('/^\d{4}-\d{2}$/', $mesRef)` com fallback `now()->format('Y-m')` ANTES do `Carbon::createFromFormat`
    - `Inertia::render('Mlb/MeuPainel', ...)` inclui `score_publicador`, `faturamento_mes`, `net_billing_timeseries` E mantém `kpis`, `meta`, `mesRef`, `meses`, `problemas`, `ticketEvolucao`, `ticketAtual`
    - `net_billing_timeseries` usa formato de data `Y-m-d`
    - `php artisan test --filter=MeuPainelControllerTest` sai com código 0 (3 testes GREEN: PUB-04, PUB-05, PUB-06)
  </acceptance_criteria>
  <done>Controller evoluído; suíte de feature da Wave 0 verde (RED → GREEN); props existentes preservadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query string → controller | `?mes=YYYY-MM` é entrada do usuário e alimenta `Carbon::createFromFormat` (lança exceção em valor inválido) |
| controller → Service | passa `$user->id` autenticado; nunca id arbitrário do request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `$mesRef` query param | mitigate | Guard `preg_match('/^\d{4}-\d{2}$/', $mesRef)` com fallback `now()` antes de `Carbon::createFromFormat` (§Segurança do RESEARCH) |
| T-38-04 | Information Disclosure | acesso à rota meu-painel | accept | `checkPubAccess('meu_painel')` já existente cobre V4 (controle de acesso) — não alterado nesta fase |
| T-38-02 | Denial of Service | SUM(net_billing) null / divisão | mitigate | `(float)(... ->sum('net_billing') ?? 0)` e `whereNotNull` no timeseries (Armadilha 1) |
| T-38-SC | Tampering | npm/composer installs | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `php artisan test --filter=MeuPainelControllerTest` verde (3/3)
- `php artisan test --filter=Phase38` verde combinado (Polos + Painel do Publicador; sem regressão)
- `php artisan test --filter=PublicadorScoreServiceTest` continua verde
- Grep confirma as 5 props novas e o guard de regex no controller
</verification>

<success_criteria>
- meuPainel() emite as props novas sem remover nenhuma existente
- Validação de $mesRef ativa
- faturamento_mes e timeseries robustos a net_billing null
- Suíte de feature da Wave 0 em GREEN
</success_criteria>

<output>
Create `.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-03-SUMMARY.md` when done
</output>
