---
phase: 38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r
plan: 04
type: execute
wave: 3
depends_on: ["38-03"]
files_modified:
  - resources/js/Pages/Mlb/MeuPainel.jsx
autonomous: false
requirements: [PUB-04, PUB-05]
nyquist_compliant: true

must_haves:
  truths:
    - "O publicador vê título 'Painel do Publicador', score 0-100 (RadialBar) e radar de 5 eixos"
    - "Vê KPIs grandes: Faturamento, Anúncios Feitos, Vendas no Mês"
    - "Vê o gráfico de evolução acumulada do faturamento (LineChart)"
    - "Publicador sem dados vê estado vazio limpo, sem erro de render"
    - "npm run build conclui sem erro"
  artifacts:
    - path: "resources/js/Pages/Mlb/MeuPainel.jsx"
      provides: "Layout score + radar + KPIs + evolução do faturamento espelhando Portfolio/Show.jsx"
      contains: "Painel do Publicador"
  key_links:
    - from: "resources/js/Pages/Mlb/MeuPainel.jsx"
      to: "score_publicador.pontos_categoria"
      via: "montagem das dimensões do RadarChart"
      pattern: "pontos_categoria"
    - from: "resources/js/Pages/Mlb/MeuPainel.jsx"
      to: "net_billing_timeseries"
      via: "LineChart dataKey=realizado"
      pattern: "net_billing_timeseries"
---

<objective>
Reformular `resources/js/Pages/Mlb/MeuPainel.jsx` para o layout score + radar + KPIs + evolução
do faturamento, espelhando `resources/js/Pages/Portfolio/Show.jsx`. Copiar (não importar) os
componentes `KpiCard`, `Chip`, `MiniMetric`, `ChartTooltip`, `PerformanceSection`
(RadialBarChart + RadarChart) e o `LineChart` de evolução, adaptando as 5 dimensões para os
eixos do publicador. Consumir as props novas emitidas na Wave 2. Rodar `npm run build` ao final.

MANTER: `ProblemasSection`, feedbacks do líder, seletor de mês nativo (NÃO trocar para Radix —
risco de tela preta) e `TicketIndividualChart` como seção secundária.

Purpose: Entregar ao publicador o painel rico (igual ao dos analistas de ADS) no lugar da lista
de KPIs simples atual.
Output: MeuPainel.jsx reformulado; bundle Vite atualizado; verificação visual humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/CONTEXT.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-RESEARCH.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-PATTERNS.md

<interfaces>
<!-- Props recebidas pela página (emitidas pelo controller na Wave 2) + componentes-molde de Show.jsx. -->

Props da página MeuPainel:
- Existentes (manter): kpis, evolucaoDiaria, topEmpresas, feedbacks, meta, mesRef, meses, problemas, ticketEvolucao, ticketAtual
- Novas (Fase 38): score_publicador, faturamento_mes, anuncios_feitos, vendas_mes, net_billing_timeseries

score_publicador (do PublicadorScoreService):
- { score: float, classificacao: 'excelente'|'bom'|'atencao'|'critico',
    pontos_categoria: { meta|produtividade|pontualidade|conversao|qualidade: {valor: float|null, peso: int} },
    metricas: { meta:{feito,alvo,pct}, produtividade:{feito,meta,pct}, pontualidade:{total_skus,atrasados,pct_no_prazo}, conversao:{vendidos,feito,pct}, qualidade:{pct_sem_problema,pct_feedbacks_resolvidos} } }

net_billing_timeseries: [{ date: 'YYYY-MM-DD', realizado: float }]

Componentes-molde em resources/js/Pages/Portfolio/Show.jsx (copiar, não importar):
- KpiCard (linhas 91-114), Chip (118-130), MiniMetric (315-326), ChartTooltip (369-411)
- PerformanceSection com RadialBarChart + RadarChart (141-306) — radial em 158-159, dimensões em 149-155
- LineChart de evolução (719-771)
- Lookup tables CLASSIF_LABEL/CLASSIF_CLS/CLASSIF_BG/SCORE_COLOR (53-72)

Componentes a PRESERVAR no MeuPainel.jsx atual:
- import { TicketIndividualChart } from '@/Components/TicketMedioChart' (linha 3)
- ProblemasSection (linha 60) e seu uso (linha 231)
- Seletor de mês nativo <select> (NÃO Radix)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Reformular MeuPainel.jsx (score + radar + KPIs + evolução) e rodar build</name>
  <files>resources/js/Pages/Mlb/MeuPainel.jsx</files>
  <read_first>
    - resources/js/Pages/Mlb/MeuPainel.jsx (estado atual integral — assinatura linha 152, ProblemasSection linha 60, uso linha 231, TicketIndividualChart linhas 3 e 281, seletor de mês nativo)
    - resources/js/Pages/Portfolio/Show.jsx (MOLDE — KpiCard 91-114, Chip 118-130, MiniMetric 315-326, ChartTooltip 369-411, PerformanceSection 141-306, RadialBar 158-159, LineChart 719-771, lookup tables 53-72)
    - resources/js/lib/utils.js (cn, formatCurrencyCompact, formatPercent — usar estes, não funções locais)
    - .planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-PATTERNS.md (seção do JSX — imports, montagem das dimensões do radar, ChartTooltip adaptado, LineChart simplificado)
    - .planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/CONTEXT.md (decisões: nome "Painel do Publicador", 5 eixos, só individual)
  </read_first>
  <action>
    Reescrever `resources/js/Pages/Mlb/MeuPainel.jsx`. Adicionar à assinatura do componente default as props novas: `score_publicador`, `faturamento_mes`, `anuncios_feitos`, `vendas_mes`, `net_billing_timeseries` (com defaults seguros: `net_billing_timeseries = []`, `faturamento_mes = 0`). Manter as props existentes.

    Adicionar imports do recharts para radar/radial/linha (`RadialBarChart, RadialBar, PolarAngleAxis, RadarChart, Radar, PolarGrid, PolarRadiusAxis, LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer`) e de `cn, formatCurrencyCompact, formatPercent` de `@/lib/utils`. Manter o import de `TicketIndividualChart`.

    Copiar verbatim de Show.jsx (não importar): as lookup tables `CLASSIF_LABEL/CLASSIF_CLS/CLASSIF_BG/SCORE_COLOR` (53-72), e os componentes `KpiCard` (91-114), `Chip` (118-130), `MiniMetric` (315-326). Copiar `ChartTooltip` (369-411) adaptando para mostrar só "Faturamento acumulado" via `formatCurrencyCompact(realizado)` (sem meta_acumulada/projecao). Copiar `PerformanceSection` (141-306) adaptando: título "Desempenho do Publicador", remover a prop/uso de `comparacao`, e substituir o array `dimensoes` pelas 5 dimensões do publicador montadas a partir de `score_publicador.pontos_categoria` e `score_publicador.metricas` (Meta, Produtividade, Pontualidade, Conversão, Qualidade — usar `?.valor ?? 0` para o eixo e `metricas.*.pct`/`pct_no_prazo`/`pct_sem_problema` para o tooltip bruto, conforme PATTERNS).

    Montar o layout:
    1. Header: título "Painel do Publicador" + o seletor de mês nativo existente (manter como está; usar `meses`/`mesRef` + `router.get` por query `?mes=`).
    2. KPIs grandes (KpiCard): Faturamento = `formatCurrencyCompact(faturamento_mes)`; Anúncios Feitos = `anuncios_feitos` (ou `kpis.feito`); Vendas no Mês = `vendas_mes` (ou `kpis.vendas`) + taxa de conversão de `kpis.conversao_vendas`.
    3. `<PerformanceSection data={score_publicador} />` (renderizar só quando `score_publicador` existir) — RadialBar do score + RadarChart dos 5 eixos + MiniMetrics de detalhe (Atingimento da meta, Vendas/conversão, Entregas com atraso, Qualidade, Produtividade).
    4. LineChart de evolução do faturamento alimentado por `net_billing_timeseries` (dataKey `realizado`, stroke verde `#10b981`, XAxis `tickFormatter` fatiando `date` em dd/mm, YAxis `formatCurrencyCompact`, Tooltip `<ChartTooltip />`). Copiar de Show.jsx 719-771 simplificado (só a linha "realizado").
    5. `<ProblemasSection ... />` mantido. Lista de feedbacks do líder mantida.
    6. `<TicketIndividualChart evolucao={ticketEvolucao} ticketAtual={ticketAtual} />` como seção secundária mantida.

    Estados vazios (publicador sem dados — Armadilhas/PUB-05): se `!score_publicador` ou `score_publicador.score === 0`, exibir placeholders limpos (ex.: "Sem dados no período") em vez de quebrar; `net_billing_timeseries` vazio → LineChart não renderiza ou mostra estado vazio; `faturamento_mes === 0` → exibir "R$ 0,00" / "—". Nada de exceção de render.

    Usar `cn()` para classes condicionais e tokens `ecf-*` (dark theme). Comentários em pt-BR. Ao final, rodar `npm run build` e confirmar sucesso.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Abrir /mlb/meu-painel logado como publicador: conferir título "Painel do Publicador", score (RadialBar) + radar de 5 eixos, 3 KPIs grandes (Faturamento/Anúncios/Vendas), LineChart de faturamento, e ProblemasSection/feedbacks/Ticket mantidos. Conferir estado vazio (publicador sem publicações) sem erro.</human-check>
  </verify>
  <acceptance_criteria>
    - `resources/js/Pages/Mlb/MeuPainel.jsx` contém a string "Painel do Publicador"
    - Referencia `score_publicador.pontos_categoria` (montagem do radar) e `net_billing_timeseries` (LineChart)
    - Mantém `ProblemasSection`, `TicketIndividualChart` e o seletor de mês nativo (sem Radix Select)
    - Usa `formatCurrencyCompact` e `cn` de `@/lib/utils`
    - `npm run build` conclui sem erro (exit 0)
  </acceptance_criteria>
  <done>Página reformulada com score+radar+KPIs+evolução; build verde; pronta para checkpoint visual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 2: Checkpoint visual — verificar Painel do Publicador no navegador</name>
  <files>resources/js/Pages/Mlb/MeuPainel.jsx</files>
  <action>
    PAUSA para verificação humana. A Tarefa 1 já automatizou a reformulação do MeuPainel.jsx e
    rodou `npm run build`. O usuário valida visualmente o render no navegador (Recharts/dark theme
    não é coberto por teste de unidade PHP — ver 38-VALIDATION §Manual-Only Verifications).
  </action>
  <what-built>
    MeuPainel.jsx reformulado para o formato "Painel do Publicador": score 0-100 (RadialBar),
    radar de 5 eixos (Meta · Produtividade · Pontualidade · Conversão · Qualidade), KPIs grandes
    (Faturamento · Anúncios Feitos · Vendas no Mês), LineChart de evolução do faturamento, mais
    ProblemasSection, feedbacks e TicketIndividualChart mantidos. `npm run build` executado.
  </what-built>
  <how-to-verify>
    1. Acesse `/mlb/meu-painel` logado como um publicador com dados no mês.
    2. Confirme o título "Painel do Publicador" e o seletor de mês funcionando (troca o mês e recarrega os dados).
    3. Confirme o card de score com RadialBar (0-100) e a classificação colorida coerente.
    4. Confirme o radar com exatamente 5 eixos rotulados Meta, Produtividade, Pontualidade, Conversão, Qualidade.
    5. Confirme os 3 KPIs grandes: Faturamento (R$), Anúncios Feitos (nº), Vendas no Mês (nº + % conversão).
    6. Confirme o gráfico de evolução do faturamento (linha verde acumulada) com tooltip em R$.
    7. Confirme que ProblemasSection, lista de feedbacks e o gráfico de Ticket continuam presentes.
    8. Troque para um publicador SEM publicações no mês: a tela deve mostrar estado vazio limpo (sem erro de render, sem tela branca).
    9. Confirme layout consistente em dark theme (tokens ecf-*), sem quebras.
  </how-to-verify>
  <verify>
    <human-check>Render visual aprovado pelo usuário conforme os 9 itens de how-to-verify.</human-check>
  </verify>
  <done>Usuário aprovou o render visual do Painel do Publicador (ou ajustes aplicados e reaprovados).</done>
  <resume-signal>Digite "aprovado" ou descreva os ajustes visuais necessários.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props Inertia → React | dados já sanitizados pelo controller; render-only, sem entrada de usuário nova na página |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Denial of Service | render com props nulas/vazias | mitigate | Defaults seguros (`net_billing_timeseries = []`, `faturamento_mes = 0`) e guards de estado vazio antes de renderizar charts (PUB-05) |
| T-38-06 | Tampering | Radix Select com value="" → tela preta | mitigate | Manter `<select>` nativo do seletor de mês (decisão registrada em MEMORY: Radix Select value vazio derruba render) |
| T-38-SC | Tampering | npm installs | accept | Nenhum pacote novo (recharts/lucide-react já no projeto — RESEARCH §Auditoria de Pacotes) |
</threat_model>

<verification>
- `npm run build` exit 0
- `php artisan test --filter=Phase38` continua verde (backend intacto)
- Checkpoint visual humano aprovado
- Grep confirma "Painel do Publicador", `pontos_categoria`, `net_billing_timeseries` e ausência de Radix Select no seletor de mês
</verification>

<success_criteria>
- Layout score + radar + KPIs + evolução espelhando Show.jsx
- 5 eixos do publicador no radar
- Elementos mantidos (Problemas, feedbacks, Ticket, seletor nativo)
- Estado vazio sem erro
- Build verde + verificação visual aprovada
</success_criteria>

<output>
Create `.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-04-SUMMARY.md` when done
</output>
