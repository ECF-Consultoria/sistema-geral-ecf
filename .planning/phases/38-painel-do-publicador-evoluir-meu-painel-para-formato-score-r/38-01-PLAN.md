---
phase: 38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Unit/PublicadorScoreServiceTest.php
  - tests/Feature/Phase38Publicador/MeuPainelControllerTest.php
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03, PUB-04, PUB-05, PUB-06]
nyquist_compliant: true

must_haves:
  truths:
    - "Existe suíte unitária que falha (RED) cobrindo score 0-100, eixo null e Pontualidade sem empresas"
    - "Existe suíte de feature que falha (RED) cobrindo as 3 props novas, estado vazio e net_billing null"
    - "Os comandos --filter de cada teste retornam testes executáveis (não 'No tests found')"
  artifacts:
    - path: "tests/Unit/PublicadorScoreServiceTest.php"
      provides: "Stubs RED para PUB-01, PUB-02, PUB-03"
      contains: "class PublicadorScoreServiceTest"
    - path: "tests/Feature/Phase38Publicador/MeuPainelControllerTest.php"
      provides: "Stubs RED para PUB-04, PUB-05, PUB-06"
      contains: "namespace Tests\\Feature\\Phase38Publicador"
  key_links:
    - from: "tests/Unit/PublicadorScoreServiceTest.php"
      to: "App\\Services\\PublicadorScoreService"
      via: "new PublicadorScoreService()->compute()"
      pattern: "new PublicadorScoreService"
    - from: "tests/Feature/Phase38Publicador/MeuPainelControllerTest.php"
      to: "route('mlb.meu-painel')"
      via: "actingAs + get + assertInertia"
      pattern: "route\\('mlb.meu-painel'\\)"
---

<objective>
Criar os stubs de teste (Nyquist Wave 0) ANTES de qualquer implementação, no estado RED.
Esta fase é a base de validação: define o contrato observável do `PublicadorScoreService`
(suíte unitária) e do `MlbController@meuPainel` evoluído (suíte de feature). As suítes
falham porque `PublicadorScoreService` ainda não existe e as props novas ainda não são
emitidas — exatamente o esperado em Wave 0.

ATENÇÃO À COLISÃO: já existe `tests/Feature/Phase38/` (módulo Polos). A suíte de feature
desta fase usa pasta e namespace **Phase38Publicador** para não colidir.

Purpose: Travar o comportamento esperado em testes automatizados antes de codar, garantindo
que as Waves 1-3 sejam guiadas por testes (RED → GREEN).
Output: 2 arquivos de teste em estado RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/CONTEXT.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-RESEARCH.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-PATTERNS.md
@.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-VALIDATION.md

<interfaces>
<!-- Contrato que as suítes assumem (será implementado nas Waves 1-2). Extraído do RESEARCH/PATTERNS. -->

Service a testar (Wave 1):
- App\Services\PublicadorScoreService::compute(int $userId, string $mesRef): array
- Retorno: ['score' => float, 'classificacao' => string, 'pontos_categoria' => array, 'metricas' => array]
- pontos_categoria: chaves meta|produtividade|pontualidade|conversao|qualidade, cada uma ['valor' => float|null, 'peso' => int]
- Pesos: meta=35, produtividade=25, pontualidade=20, conversao=10, qualidade=10

Controller a testar (Wave 2):
- Rota nomeada: mlb.meu-painel (GET /mlb/meu-painel) → componente Inertia 'Mlb/MeuPainel'
- Props novas esperadas: score_publicador, faturamento_mes, net_billing_timeseries
- Props existentes que devem CONTINUAR presentes: kpis, meta, mesRef, meses, evolucaoDiaria

Models para dados sintéticos (sem factories — insert direto, padrão do projeto):
- App\Models\Publicacao::create([...]) — campos: user_id, data (Y-m-d), empresa, tipo, vendido (bool), vendas_qty (int), net_billing (decimal nullable), problema (bool), comentario, comentario_resolvido (bool)
- App\Models\MlbEmpresa::create([...]) — campos: nome, responsavel_id, skus_estagio1/2/3 (array), prazo_estagio1/2/3 (date)
- App\Models\User::factory()->create(['role' => 'admin', 'email_verified_at' => now()]) — admin bypassa checkPubAccess
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Stub unitário PublicadorScoreServiceTest (RED) — PUB-01, PUB-02, PUB-03</name>
  <read_first>
    - tests/Unit/CalcularFaixaTest.php (análogo de teste unitário — estrutura namespace Tests\Unit, RefreshDatabase, asserts de float)
    - .planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-RESEARCH.md (seção "Validação Architecture" e "Fórmula de Normalização" — comportamento esperado de cada eixo)
    - .planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-PATTERNS.md (seção "tests/Unit/PublicadorScoreServiceTest.php" — padrão de insert direto com Publicacao::create/MlbEmpresa::create)
    - app/Models/Publicacao.php (fillable e casts — confirmar campos aceitos no create: user_id, data, tipo, vendido, vendas_qty, net_billing, problema, comentario, comentario_resolvido)
    - app/Models/MlbEmpresa.php (fillable e casts — confirmar skus_estagio1/2/3 como array e responsavel_id)
  </read_first>
  <action>
    Criar `tests/Unit/PublicadorScoreServiceTest.php` no namespace `Tests\Unit`, estendendo `Tests\TestCase`, usando o trait `RefreshDatabase`. Importar `App\Services\PublicadorScoreService`. Instanciar via `new PublicadorScoreService()` (sem DI — o Service não tem dependências externas). Criar três métodos de teste:
    (1) `test_score_completo_retorna_float_entre_0_e_100` (PUB-01): criar um `User`, várias `Publicacao::create` do mês corrente com mix de vendido/problema/net_billing e uma `MlbEmpresa::create` com `responsavel_id` = user e `skus_estagio1` contendo SKUs ok/atrasados; chamar `compute($user->id, now()->format('Y-m'))`; assertar `score` é float com `assertGreaterThanOrEqual(0.0, ...)` e `assertLessThanOrEqual(100.0, ...)`, e que `pontos_categoria` tem as 5 chaves esperadas.
    (2) `test_eixo_null_redistribui_peso` (PUB-02): cenário onde um eixo fica sem dado (ex.: sem publicações no mês → conversao/qualidade null); assertar que o eixo correspondente em `pontos_categoria` tem `valor === null` e que o `score` ainda é calculado a partir dos pesos restantes (não NaN, não exception). Espelha a redistribuição do `scoreFinal()`.
    (3) `test_pontualidade_sem_empresas` (PUB-03): user sem nenhuma `MlbEmpresa` com `responsavel_id` = ele; assertar `pontos_categoria['pontualidade']['valor'] === null` e `metricas['pontualidade']['total_skus'] === 0`.
    Os testes DEVEM falhar agora (classe `PublicadorScoreService` ainda não existe) — estado RED esperado. NÃO criar o Service neste plano.
  </action>
  <verify>
    <automated>php artisan test --filter=PublicadorScoreServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Unit/PublicadorScoreServiceTest.php` existe e contém `class PublicadorScoreServiceTest`
    - Contém os três métodos: `test_score_completo_retorna_float_entre_0_e_100`, `test_eixo_null_redistribui_peso`, `test_pontualidade_sem_empresas`
    - Referencia `new PublicadorScoreService` e `App\Services\PublicadorScoreService`
    - `php artisan test --filter=PublicadorScoreServiceTest` EXECUTA os 3 testes (não retorna "No tests found") e eles falham em RED (Service inexistente)
  </acceptance_criteria>
  <done>Suíte unitária criada, executável e em estado RED (falha por Service inexistente).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Stub de feature MeuPainelControllerTest (RED) — PUB-04, PUB-05, PUB-06</name>
  <read_first>
    - tests/Feature/Phase38/PolosControllerTest.php (análogo exato — namespace, RefreshDatabase, helper de usuário, actingAs + assertInertia com has()/where(), padrão de insert direto)
    - app/Http/Controllers/MlbController.php (método meuPainel linhas 531-641 — props atuais e checkPubAccess('meu_painel'))
    - routes/web.php (linha 461 — rota nomeada `meu-painel` sob prefixo que gera `mlb.meu-painel`)
    - .planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-PATTERNS.md (seção "tests/Feature/Phase38Publicador/MeuPainelControllerTest.php" — namespace Phase38Publicador, helper publicador() com role admin)
  </read_first>
  <action>
    Criar a pasta `tests/Feature/Phase38Publicador/` e o arquivo `MeuPainelControllerTest.php` no namespace `Tests\Feature\Phase38Publicador` (NÃO `Phase38` — evita colisão com a suíte dos Polos), estendendo `Tests\TestCase`, com `RefreshDatabase`. Importar `App\Models\MlbEmpresa`, `App\Models\Publicacao`, `App\Models\User`, `Inertia\Testing\AssertableInertia as Assert`. Criar helper privado `publicador(): User` que retorna `User::factory()->create(['role' => 'admin', 'email_verified_at' => now()])` (admin bypassa `checkPubAccess`). Criar três métodos:
    (1) `test_meu_painel_passa_props_novas` (PUB-04): criar publicador + algumas `Publicacao::create` do mês com `net_billing`; `actingAs($this->publicador())->get(route('mlb.meu-painel'))->assertStatus(200)->assertInertia(...)` verificando `->component('Mlb/MeuPainel')`, `->has('score_publicador')`, `->has('faturamento_mes')`, `->has('net_billing_timeseries')` E que props existentes continuam: `->has('kpis')`, `->has('meta')`, `->has('mesRef')`.
    (2) `test_sem_publicacoes` (PUB-05): publicador sem nenhuma publicação; assertar status 200 e `->where('faturamento_mes', 0.0)` e `->where('score_publicador.score', 0.0)` — sem exceção, props vazias válidas.
    (3) `test_net_billing_null` (PUB-06): publicador com `Publicacao::create` que têm `net_billing` = null em todas; assertar status 200 e `->where('faturamento_mes', 0.0)` (sem divisão por zero, sem SUM null quebrando).
    Os testes DEVEM falhar agora (props novas ainda não emitidas pelo controller) — estado RED. NÃO alterar o controller neste plano.
  </action>
  <verify>
    <automated>php artisan test --filter=MeuPainelControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Phase38Publicador/MeuPainelControllerTest.php` existe com `namespace Tests\Feature\Phase38Publicador`
    - Contém os métodos `test_meu_painel_passa_props_novas`, `test_sem_publicacoes`, `test_net_billing_null`
    - Usa `route('mlb.meu-painel')` e `assertInertia` com `has('score_publicador')`, `has('faturamento_mes')`, `has('net_billing_timeseries')`
    - `php artisan test --filter=MeuPainelControllerTest` EXECUTA os 3 testes (não "No tests found") e eles falham em RED (props ausentes)
    - Não há colisão de nome de classe com `tests/Feature/Phase38/` — namespace distinto confirmado
  </acceptance_criteria>
  <done>Suíte de feature criada na pasta Phase38Publicador, executável e em RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query string → controller | `?mes=YYYY-MM` é entrada do usuário; validação coberta na Wave 2 (regex). Aqui apenas testamos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `$mesRef` query param (validado na Wave 2) | accept | Fora do escopo deste plano (só testes RED); regex `^\d{4}-\d{2}$` será adicionada no controller na Wave 2 |
| T-38-SC | Tampering | npm/composer installs | accept | Nenhum pacote instalado nesta fase (RESEARCH §Auditoria de Pacotes: zero instalações) |
</threat_model>

<verification>
- `php artisan test --filter=PublicadorScoreServiceTest` executa 3 testes (RED esperado)
- `php artisan test --filter=MeuPainelControllerTest` executa 3 testes (RED esperado)
- Nenhum teste retorna "No tests found"
- Suítes existentes não quebram: `php artisan test --filter=Phase38` continua passando (Polos intactos)
</verification>

<success_criteria>
- 2 arquivos de teste criados, ambos executáveis e em RED
- Namespace `Phase38Publicador` confirmado (sem colisão com Polos)
- Contrato observável dos 5 eixos e das 3 props novas travado em testes
</success_criteria>

<output>
Create `.planning/phases/38-painel-do-publicador-evoluir-meu-painel-para-formato-score-r/38-01-SUMMARY.md` when done
</output>
