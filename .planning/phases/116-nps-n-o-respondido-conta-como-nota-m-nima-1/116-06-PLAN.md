---
phase: 116-nps-n-o-respondido-conta-como-nota-m-nima-1
plan: 06
type: execute
wave: 3
depends_on: ["116-01", "116-02"]
files_modified:
  - app/Console/Commands/NpsMaterializarNaoRespondidos.php
  - app/Console/Commands/NpsDispararMensal.php
  - app/Http/Controllers/NpsController.php
  - routes/console.php
  - tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php
autonomous: true
requirements: [NPSFLOOR-08, NPSFLOOR-08b, NPSFLOOR-08c, NPSFLOOR-07, NPSFLOOR-11]

must_haves:
  truths:
    - "Existe um comando que materializa os NPS não respondidos e roda idempotente"
    - "O comando em --dry-run mostra, por pessoa e competência, a nota de NPS e a faixa de bônus antes e depois, sem gravar nada"
    - "Sem --dry-run e sem --force o comando exige confirmação interativa antes de gravar"
    - "Existe caminho de rollback do que o comando gravou"
    - "Um NPS recém-disparado (automático ou manual) já nasce contando como 1, sem esperar o cron"
    - "O cache do bônus das pessoas afetadas é invalidado quando o comando grava"
    - "O snapshot mensal do bônus (DesempenhoScoreSnapshot) das competências efetivamente backfilladas é reconsolidado — o retroativo chega ao ranking do /performance, ao Relatório de Bonificação e à auditoria de bônus, não só ao cálculo ao vivo"
    - "O --dry-run diz quais competências terão o snapshot reconsolidado e quantas pessoas mudam de faixa NO SNAPSHOT"
    - "O --desfazer também reconsolida, para o rollback não deixar o snapshot inconsistente com as linhas removidas"
    - "Depois de reconsolidar, o comando CONFERE o snapshot gravado pessoa a pessoa e NOMEIA (nome + competência) quem foi aprovado no relatório mas NÃO teve o snapshot atualizado — nunca uma contagem agregada"
  artifacts:
    - path: "app/Console/Commands/NpsMaterializarNaoRespondidos.php"
      provides: "Comando idempotente com --dry-run/--force/--mes/--desfazer, relatório antes/depois, reconsolidação do snapshot mensal e conferência nominal do resultado"
      contains: "desempenho:consolidar-mes"
    - path: "routes/console.php"
      provides: "Agendamento diário da materialização"
      contains: "nps:materializar-nao-respondidos"
    - path: "tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php"
      provides: "Prova de idempotência, dry-run read-only, relatório antes/depois, reconsolidação do snapshot e aviso nominal quando a reconsolidação é recusada pelo gate de margem"
      contains: "DesempenhoScoreSnapshot"
  key_links:
    - from: "app/Console/Commands/NpsMaterializarNaoRespondidos.php"
      to: "app/Services/Nps/NpsImputationService.php"
      via: "materializarLote(dryRun)"
      pattern: "materializarLote"
    - from: "app/Console/Commands/NpsMaterializarNaoRespondidos.php"
      to: "app/Services/DesempenhoScoreService.php"
      via: "setIncluirImputadas(false) para a coluna ANTES + cacheKey() para o cache-bust"
      pattern: "setIncluirImputadas"
    - from: "app/Console/Commands/NpsMaterializarNaoRespondidos.php"
      to: "app/Console/Commands/ConsolidarMesDesempenho.php"
      via: "Artisan::call('desempenho:consolidar-mes', ['--mes' => YYYY-MM]) por competência afetada, após a escrita real"
      pattern: "desempenho:consolidar-mes"
    - from: "app/Console/Commands/NpsMaterializarNaoRespondidos.php"
      to: "app/Models/DesempenhoScoreSnapshot.php"
      via: "re-consulta do snapshot mensal por (user_id, mes_referencia) DEPOIS da reconsolidação, para nomear quem não foi atualizado"
      pattern: "conferirSnapshotsReconsolidados"
    - from: "app/Console/Commands/NpsDispararMensal.php"
      to: "app/Services/Nps/NpsImputationService.php"
      via: "materializar() logo após criar o survey"
      pattern: "materializar"
---

<objective>
Entregar a operação da regra: o comando `nps:materializar-nao-respondidos` (idempotente, com `--dry-run`
que produz o **relatório de impacto antes/depois por pessoa e competência** exigido por D1), a
**reconsolidação do snapshot mensal do bônus** das competências backfilladas, a **conferência nominal do
que de fato foi congelado**, o agendamento diário, o gancho no momento do disparo (para o 1 valer desde o
disparo, sem esperar o cron) e o caminho de rollback.

Purpose: sem este plano a regra existe no cálculo mas ninguém materializa as linhas — e o backfill
retroativo (D1), que pode mudar quem bateu o bônus em meses fechados, não teria como ser revisado antes
de aplicar **nem chegaria às telas oficiais**.
Output: 1 comando novo, 2 ganchos de disparo, 1 entrada de schedule, 1 suíte de Console.

## Regras

1. **Nada é aplicado sem revisão humana.** Sem `--dry-run` e sem `--force`, o comando SEMPRE imprime o
   diff + o relatório antes/depois e pede `confirm(..., false)` antes de gravar. É o molde de
   `NpsBackfillAssignmentsConsolidado`, endurecido pelo requisito D1.
2. **O relatório antes/depois é o entregável mais forte deste plano** (vai além do analog): por
   (pessoa × competência) mostrar `nps_antes`, `nps_depois`, `nota_final_antes`, `nota_final_depois`,
   `faixa_antes`, `faixa_depois` e uma marca visível quando a faixa muda. É por essa tabela que o
   usuário vai decidir aplicar ou não o retroativo.
3. **`ANTES` é calculado com a regra desligada**, via `DesempenhoScoreService::setIncluirImputadas(false)`
   (toggle entregue no plano 116-02) — nunca por heurística ou por leitura de snapshot.
4. **Rollback existe e é explícito** (`--desfazer --mes=YYYY-MM`), porque o comando altera dado que
   alimenta bonificação. Rollback também confirma antes de apagar, busta cache **e reconsolida**.
5. **Idempotência é do serviço**, não do comando — o comando só orquestra, imprime, confirma e reconsolida.
6. **Backfill sem reconsolidação não existe (regra estrutural desta fase).** O bump de cacheKey (116-02)
   e o cache-bust só afetam o **cálculo ao vivo**, que é o que meses ABERTOS usam. Competência **fechada**
   é servida pelo registro congelado `DesempenhoScoreSnapshot` mensal — `RelatorioBonificacaoController`
   o declara como *"fonte autoritativa"*, e `PerformanceController` (ranking mensal),
   `PortfolioController` (heatmap de meses fechados) e `BonusAuditoriaController` leem dele, **não** de
   `computeCached()`. Como D1 mira exatamente as competências já fechadas, materializar as linhas sem
   reconsolidar o snapshot faria o usuário aprovar um relatório antes/depois que **não mudaria nada** nas
   telas oficiais. Portanto: **reconsolidar faz parte do backfill, não é passo opcional**, e é
   **reuso** de `ConsolidarMesDesempenho` (`--mes=YYYY-MM`, já suportado para reprocessamento manual
   pós-incidente / DESEMP-09) — nunca reimplementação do `updateOrCreate`.
7. **Reconsolidar não é o mesmo que ter reconsolidado — conferir é obrigatório (NPSFLOOR-08c).**
   O relatório antes/depois é calculado com `compute()` direto, **sem passar pelo gate de margem
   FIXMARG-03** do `ConsolidarMesDesempenho`. Ou seja: ele mostra o que *deveria* acontecer. Se, no
   momento da reconsolidação, a amostra de margem de alguém estiver degradada (Adman em rate-limit),
   a persistência do snapshot **daquela pessoa é recusada** e o congelado permanece com o número
   pré-backfill — enquanto o operador acha que aprovou a mudança para todo mundo. O comando NÃO pode
   confiar na saída do comando chamado (ela só carrega a contagem agregada `Degradados: N`, sem nomes):
   ele tem que **re-consultar o `DesempenhoScoreSnapshot`** das pessoas/competências que estavam no
   relatório e **nomear as divergências**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-CONTEXT.md
@.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-PATTERNS.md
@.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-01-SUMMARY.md
@.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-02-SUMMARY.md
</context>

<interfaces>
- `NpsImputationService::materializarLote(?Carbon $mes = null, bool $dryRun = false): array`
  → `['criados','pulos_ja_existentes','pulos_sem_responsavel','promovidos_definitivo','removidos','detalhe']`
- `NpsImputationService::materializar(NpsSurvey $survey, bool $dryRun = false): array`
- `DesempenhoScoreService::setIncluirImputadas(bool $incluir): static` (toggle do relatório, plano 116-02)
- `DesempenhoScoreService::compute(User $user, Carbon $mesReferencia): array` (não cacheado)
- `DesempenhoScoreService::cacheKey(int $userId, Carbon $mes): string` (agora `desempenho.compute.v12.*`)
- `BonusFaixa::classificar(float $nota): ?self`
- **Reconsolidação (reuso, não reimplementação):** `desempenho:consolidar-mes {--mes=YYYY-MM}` —
  `App\Console\Commands\ConsolidarMesDesempenho`, itera os users elegíveis (`active` + cargo
  `analista`/`estrategista`), roda `compute($user, $mes)` e grava
  `DesempenhoScoreSnapshot::updateOrCreate(['user_id','mes_referencia'], [...])`, depois repopula
  `ranking_pos` do mês. É idempotente por construção.
- **Gate FIXMARG-03 dentro do `ConsolidarMesDesempenho` — e por que a saída dele NÃO serve de aviso:**
  se `compute()['margem_amostra']['cobertura']` ficar abaixo de `0.7` com `n_elegivel > 0`, o comando
  **RECUSA** a persistência daquele user (preserva o snapshot anterior) e `continue`. O detalhe por
  pessoa (`user_id`, `user_name`, `mes_referencia`, `cobertura`) vai **exclusivamente para `Log::error`**.
  A única coisa que `Artisan::output()` captura é a linha de resumo final:
  `"[Desempenho Mensal] Mes {YYYY-MM} — OK: {n} · Falhas: {n} · Sem carteira: {n} · Degradados: {n}"`
  — **contagem agregada, sem nomes**. Portanto parsear `Degradados` só diz *quantos*, nunca *quem*, e
  ainda quebraria se a formatação da linha mudasse (arquivo compartilhado com outras sessões).
  **Consequência de projeto:** a verificação é feita por **re-consulta ao banco**, não por parsing.
- **Fonte da conferência (leitura, não escrita):**
  `DesempenhoScoreSnapshot::mensal()->where('user_id', $id)->whereDate('mes_referencia', 'YYYY-MM-01')->first()`
  → colunas `score` (int = `nota_final * 20`) e `classificacao` (string = `faixa_bonus`).
  É a MESMA linha que `PerformanceController`, `RelatorioBonificacaoController` e `BonusAuditoriaController`
  leem — conferir aqui é conferir a tela.
- Régua de competência: competência financeira = mês do survey (`competencia_nps`) **menos 1 mês**.
  É essa competência financeira que vai em `--mes` do `desempenho:consolidar-mes`.
</interfaces>

<notas_de_execucao>
- **Árvore compartilhada:** `NpsController.php` e `NpsDispararMensal.php` são editados por outras sessões.
  Commitar só os próprios paths (`git commit -- <arquivos>`), nunca `git add -A`.
- **`ConsolidarMesDesempenho.php` NÃO é tocado por este plano (decisão explícita).** Seria possível fazê-lo
  imprimir os degradados nominalmente no stdout, mas: (a) é o comando que **paga o bônus** e carrega o gate
  FIXMARG-03 da Fase 110, (b) está na árvore compartilhada com outras sessões, e (c) qualquer aviso baseado
  em stdout fica refém do formato da saída. A conferência por re-consulta ao `DesempenhoScoreSnapshot`
  entrega a mesma informação (com nomes) sem tocar em arquivo compartilhado e sem depender de formatação.
- **Localizar por nome:** `generate()` em `NpsController`, o `NpsSurvey::create` dentro do loop
  `foreach ($modelosAplicaveis ...)` em `NpsDispararMensal`.
- Comentários, descrição do comando e saída de console em pt-BR.
- **GATE DE DEPLOY (obrigatório, não é sugestão):** este plano é quem **liga a escrita real** — o gancho
  no disparo e o cron diário. Ele NÃO pode ir para produção antes de **toda a Wave 2** (planos 116-03
  área NPS, 116-04 carteira do profissional, 116-05 dashboards/UI) estar mesclada e implantada junto.
  Se o 116-06 subir sozinho, o Desempenho já refletiria o piso enquanto área NPS, carteira e dashboards
  continuariam mostrando a média antiga — telas oficiais discordando entre si em produção, exatamente o
  problema de confiança que a fase existe para evitar. Este projeto tem histórico de deploy publicando o
  trabalho de várias sessões junto (memória do projeto: árvore compartilhada), então a verificação é
  explícita antes de qualquer `deploy.sh`: confirmar que os SUMMARYs de 116-03, 116-04 e 116-05 existem e
  que os arquivos deles estão commitados na `main`. Enquanto isso não for verdade, **não habilitar o
  schedule nem o gancho em produção** (não deployar este plano isolado).
</notas_de_execucao>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): suíte de Console do comando de materialização</name>
  <files>tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php</files>
  <read_first>
    - app/Console/Commands/NpsBackfillAssignmentsConsolidado.php (molde do comando: --dry-run/--force, tabela de diff, confirm, chunkById, cache-bust, Log::info)
    - app/Console/Commands/ConsolidarMesDesempenho.php (o comando reusado na reconsolidação: --mes, users elegíveis, updateOrCreate em DesempenhoScoreSnapshot, gate de margem FIXMARG-03 e a linha de resumo agregada)
    - tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php (molde de fixture que faz o consolidar-mes gravar snapshot em SQLite, incluindo o provider stub e o vínculo de carteira exigido)
    - tests/Feature/Phase110/ConsolidarMesMargemResilienteTest.php (molde EXATO de como forçar `cobertura < 0.7`: provider stub instanciado via `$this->app->instance(MetricsProviderFactory::class, ...)`, 1 empresa com margem real + 3 sem dado → cobertura 0,25; e como assertar o snapshot preservado)
    - app/Services/Nps/NpsImputationService.php (assinatura de materializarLote e shape de stats)
    - app/Services/DesempenhoScoreService.php (setIncluirImputadas, compute, cacheKey)
    - app/Models/BonusFaixa.php (classificar)
    - tests/Feature/V16/CriaCenarioResponsaveis.php
  </read_first>
  <behavior>
    - `--dry-run` com surveys não respondidos: exit code 0, NENHUMA linha gravada em
      `nps_imputed_assignments`, e a saída contém as colunas do relatório (`Pessoa`, `Competência`,
      `NPS antes`, `NPS depois`, `Faixa antes`, `Faixa depois`).
    - `--dry-run` também lista as competências que **serão reconsolidadas** e quantas pessoas mudam de
      faixa **no snapshot**; e NENHUMA linha de `desempenho_score_snapshots` é alterada (comparar
      `score`/`classificacao` antes e depois da execução).
    - `--force` grava: as linhas passam a existir; rodar de novo com `--force` não cria nenhuma linha
      adicional (idempotência via contagem antes/depois).
    - **Reconsolidação (prova do retroativo chegar ao registro autoritativo):** com uma competência
      FECHADA que já tem `DesempenhoScoreSnapshot` mensal gravado (com a regra desligada) e um survey não
      respondido naquela competência, rodar com `--force` faz o `score`/`classificacao` daquela linha de
      `DesempenhoScoreSnapshot` MUDAR — asserção sobre a linha do snapshot, não sobre `compute()`.
    - Reconsolidação restrita: uma competência SEM nenhuma linha backfillada não tem o snapshot tocado
      (o `updated_at`/`score` da linha daquela outra competência permanece igual).
    - **Reconsolidação recusada pelo gate de margem → aviso NOMINAL (NPSFLOOR-08c):** montar o mesmo
      cenário de reconsolidação, mas com a carteira da pessoa forçando `cobertura < 0.7`
      (molde do `ConsolidarMesMargemResilienteTest`: empresas Adman elegíveis sem margem real no
      provider stub). Rodar com `--force`. Asserções obrigatórias, todas juntas:
      (a) o `score` do `DesempenhoScoreSnapshot` daquela pessoa **permanece** o valor pré-backfill
      (o gate recusou); (b) a saída do comando contém o **nome da pessoa** E a **competência
      `YYYY-MM`** numa mensagem de aviso; (c) a saída NÃO se limita a uma contagem — assertar
      explicitamente `expectsOutputToContain($user->name)`; (d) o exit code sinaliza o problema
      (`assertExitCode` != 0 **ou** a saída contém o marcador de atenção definido no comando — fixar
      a escolha no teste e no comando, não deixar ambíguo); (e) as linhas de
      `nps_imputed_assignments` continuam gravadas (a recusa é do congelamento, não do backfill).
    - Contraste do caso acima: com cobertura saudável, a MESMA execução NÃO imprime nenhum aviso de
      snapshot não atualizado (evita aviso falso-positivo permanente).
    - Sem `--dry-run` e sem `--force`, respondendo "não" na confirmação → nada é gravado e nenhum snapshot
      é reconsolidado.
    - `--mes=2026-06` restringe a materialização à competência informada (survey de outro mês não vira linha).
    - `--mes` com formato inválido (ex.: `junho`) → exit code != 0 e nenhuma escrita.
    - Cenário sem nada a fazer → exit code 0, mensagem de "nada a materializar" e **nenhuma**
      reconsolidação disparada (snapshot intocado).
    - Cache do bônus: com uma chave de cache pré-populada para a pessoa afetada na competência do
      survey menos 1 mês, a execução com `--force` remove essa chave.
    - `--desfazer --mes=YYYY-MM --force` apaga as linhas daquela competência (inclusive `definitivo`),
      busta o cache das pessoas afetadas **e reconsolida** — o `score` do `DesempenhoScoreSnapshot` volta
      ao valor de antes do backfill (rollback completo, sem snapshot órfão).
    - Relatório: pessoa cuja faixa de bônus muda por causa da imputação aparece marcada na tabela.
  </behavior>
  <action>
    Criar `tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php` (namespace
    `Tests\Feature\Phase116`, `RefreshDatabase`), 1 método por item do `<behavior>`, usando
    `$this->artisan('nps:materializar-nao-respondidos', [...])` com `->expectsOutputToContain(...)`,
    `->expectsConfirmation(...)` e `->assertExitCode(...)`, mais asserções de banco
    (`assertDatabaseCount('nps_imputed_assignments', N)`) e de cache (`Cache::has(...)`).
    Para os casos de reconsolidação, montar a fixture no molde de
    `tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php` (é a única fixture do repositório que
    faz `desempenho:consolidar-mes` gravar snapshot em SQLite: user `active` com cargo elegível +
    carteira com contrato de serviço ativo, senão o user cai em `sem_carteira` e o comando pula a
    gravação). Gravar o snapshot "antes" rodando `desempenho:consolidar-mes --mes=...` com a regra ainda
    sem linhas imputadas, guardar `score`/`classificacao`, e assertar a mudança depois do `--force`.
    Para o caso de margem degradada, reaproveitar o setup do
    `tests/Feature/Phase110/ConsolidarMesMargemResilienteTest.php` (provider stub via
    `$this->app->instance(MetricsProviderFactory::class, ...)` + `Http::fake` obrigatório) para levar a
    cobertura abaixo de 0,7 — extrair o setup para um helper privado do próprio arquivo
    (`montarCarteiraComMargemDegradada()`), não duplicar em cada método.
    Não existe teste de Console para os comandos de backfill NPS hoje — este arquivo estabelece o padrão.
    `Carbon::setTestNow` + `tearDown`.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsMaterializarNaoRespondidosCommandTest 2>&1 | grep -qi "FAILURES\|Failed\|not found" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com `namespace Tests\Feature\Phase116;`
    - `grep -c "public function test_" tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php` retorna >= 14
    - Contém `expectsConfirmation`, `assertDatabaseCount`, `--desfazer`, `Faixa antes` e `DesempenhoScoreSnapshot`
    - Existe pelo menos um método cujo nome contém `snapshot` e que assere mudança de `score` ou `classificacao` em `DesempenhoScoreSnapshot`
    - Existe pelo menos um método cujo nome contém `degradad` e que (i) assere o `score` do snapshot INALTERADO e (ii) chame `expectsOutputToContain` com o **nome do user** e com a competência `YYYY-MM`
    - `grep -c "MetricsProviderFactory" tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php` retorna >= 1
    - `php artisan test --filter=NpsMaterializarNaoRespondidosCommandTest` sai != 0 (RED)
    - Commit isolado `test(116-06): suíte RED do comando de materialização de NPS não respondido`
  </acceptance_criteria>
  <done>Suíte RED commitada, incluindo a prova de reconsolidação do snapshot e a prova do aviso nominal quando o gate de margem recusa o congelamento.</done>
</task>

<task type="auto">
  <name>Tarefa 2 (GREEN): comando nps:materializar-nao-respondidos com relatório antes/depois, reconsolidação verificada do snapshot e rollback</name>
  <files>app/Console/Commands/NpsMaterializarNaoRespondidos.php</files>
  <read_first>
    - app/Console/Commands/NpsBackfillAssignmentsConsolidado.php (molde integral: signature, handle em 2 passos, exibirDiff, exibirStats, confirm, bustarCacheDoBonus, Log::info)
    - app/Console/Commands/ConsolidarMesDesempenho.php (comando REUSADO na reconsolidação: signature --mes, gate FIXMARG-03 que pode recusar a persistência, e a linha de resumo — que só traz CONTAGEM, sem nomes)
    - app/Services/Nps/NpsImputationService.php (materializarLote/materializar e o shape de $stats/detalhe)
    - app/Services/DesempenhoScoreService.php (setIncluirImputadas, compute, cacheKey)
    - app/Models/BonusFaixa.php (classificar)
    - app/Models/DesempenhoScoreSnapshot.php (scope mensal(), colunas score/classificacao/mes_referencia)
    - app/Http/Controllers/NpsController.php (método bustarCacheDoBonus — a régua de competência a duplicar, não a chamar)
  </read_first>
  <action>
    Criar `app/Console/Commands/NpsMaterializarNaoRespondidos.php`, signature
    `nps:materializar-nao-respondidos {--dry-run : Só mostra o impacto, sem gravar} {--force : Pula a
    confirmação interativa} {--mes= : Restringe a uma competência YYYY-MM} {--desfazer : Apaga as linhas
    materializadas da competência informada}`, descrição em pt-BR explicando a regra da fase.
    Construtor com injeção de `NpsImputationService` e `DesempenhoScoreService`.

    **Validação de `--mes`:** regex `^\d{4}-\d{2}$` antes de qualquer uso (mesmo molde do `$mesFiltro` de
    `NpsController::index`), depois `Carbon::parse($mes.'-01')->startOfMonth()`. Formato inválido →
    `$this->error(...)` + `return self::FAILURE`. Nunca interpolar a string em query.
    `--desfazer` sem `--mes` → erro (evita apagar o histórico inteiro por engano).

    **Fluxo padrão (sem `--desfazer`):**
    1. Passo 1 read-only: `materializarLote($mes, dryRun: true)` → stats + detalhe.
    2. `exibirDiff()`: tabela com `survey_id`, `company_id`, `competencia_nps`, `dimensao`, `role`,
       `user_id`, `status_alvo`.
    3. **`exibirImpacto()` (requisito D1):** a partir do detalhe, agrupar por (`user_id`,
       competência financeira = `competencia_nps` menos 1 mês) e, para cada par, calcular
       `nps_antes`/`nota_final_antes`/`faixa_antes` com `setIncluirImputadas(false)` e
       `nps_depois`/`nota_final_depois`/`faixa_depois` com `setIncluirImputadas(true)`, via
       `compute($user, $competencia)` (não cacheado). Imprimir `$this->table()` com as colunas
       `Pessoa`, `Competência`, `NPS antes`, `NPS depois`, `Nota antes`, `Nota depois`, `Faixa antes`,
       `Faixa depois`, `Muda faixa?`. Ao final, um resumo com quantas pessoas mudam de faixa.
       Como o comando roda em modo dry-run com as linhas AINDA não gravadas, calcular o "depois"
       materializando dentro de uma transação com rollback (`DB::transaction` + exception de controle)
       ou gravando e desfazendo na mesma transação — documentar a escolha em comentário pt-BR.
       **Guardar esse resultado em memória** (`array` indexado por `user_id` + competência, com
       `user_name`, `nota_final_depois` e `faixa_depois`): é o **contrato esperado** que a conferência
       do passo 9 vai cobrar do snapshot.
    4. **`exibirPlanoDeReconsolidacao()` (requisito do gate humano):** listar as competências financeiras
       distintas derivadas do detalhe — são exatamente as que terão `desempenho:consolidar-mes --mes=`
       rodado depois da escrita — e, para cada uma, quantas pessoas mudam de faixa **no snapshot**:
       comparar a `classificacao` da linha `DesempenhoScoreSnapshot` mensal já gravada (o valor que as
       telas mostram HOJE) com a `faixa_depois` calculada no passo 3. Deixar explícito na saída que é
       isso que o usuário está aprovando: "estas N competências terão o registro congelado do bônus
       reescrito". Competência ainda ABERTA (sem snapshot mensal) aparece marcada como "mês aberto —
       sem snapshot a reconsolidar".
    5. Se `criados + promovidos_definitivo + removidos === 0` → mensagem "nada a materializar",
       **nenhuma reconsolidação** e `self::SUCCESS`.
    6. `--dry-run` → mensagem "DRY-RUN — nada foi gravado e nenhum snapshot foi reconsolidado" e
       `self::SUCCESS`.
    7. Sem `--force` → `$this->confirm('Aplicar a materialização e reconsolidar o snapshot das
       competências afetadas?', false)`; recusa → aviso e saída sem gravar e sem reconsolidar.
    8. Passo 2: `materializarLote($mes, dryRun: false)`, `exibirStats()`, cache-bust,
       **`reconsolidarSnapshots()`**, **`conferirSnapshotsReconsolidados()`** e
       `Log::info('[NPS Imputação] execução concluída', [...stats, 'mes' => ..., 'competencias_reconsolidadas' => [...], 'nao_materializados' => [...], 'usuario_so' => get_current_user(), 'dry_run' => false])`.

    **`reconsolidarSnapshots(array $competenciasFinanceiras): void` — REUSO, não reimplementação:**
    para cada competência financeira afetada (derivada de `competencia_nps` menos 1 mês, distinct, só as
    efetivamente backfilladas — nunca "todas"), chamar
    `Artisan::call('desempenho:consolidar-mes', ['--mes' => $competencia->format('Y-m')])` e ecoar a saída
    (`Artisan::output()`) para o operador. Ordem obrigatória: **primeiro** as linhas imputadas são
    gravadas, **depois** a reconsolidação — senão o `compute()` do consolidar-mes ainda não enxerga o piso.
    Comentar em pt-BR: reusar o comando garante que ranking, Relatório de Bonificação e auditoria de bônus
    passem a bater com o cálculo ao vivo, e evita duplicar o `updateOrCreate` + o repopulamento de
    `ranking_pos` + o gate de margem FIXMARG-03.
    Envolver cada competência em `try/catch (\Throwable $e)` com `Log::error` — falha de reconsolidação
    NÃO desfaz as linhas já gravadas, mas tem que ser gritada na saída com a instrução de re-rodar.

    **`conferirSnapshotsReconsolidados(array $esperado): array` — VERIFICAR, não confiar (NPSFLOOR-08c):**
    **não parsear a saída do `desempenho:consolidar-mes`.** Ela só carrega a contagem agregada
    (`Degradados: N`), sem nomes, e o detalhe por pessoa fica só no `Log::error` daquele comando — além de
    ser um arquivo compartilhado cuja formatação pode mudar. Em vez disso, **depois** da reconsolidação,
    para CADA par (`user_id`, competência) do `$esperado` montado no passo 3:
      1. Re-consultar `DesempenhoScoreSnapshot::mensal()->where('user_id', $userId)
         ->whereDate('mes_referencia', $competencia->toDateString())->first()`.
      2. Comparar a `classificacao` gravada com a `faixa_depois` esperada (e o `score` com
         `(int) round($notaFinalDepois * 20)`, com tolerância de 1 ponto para arredondamento).
      3. Divergência (ou linha ausente) → acumular `['user_id','user_name','competencia','faixa_esperada','faixa_no_snapshot']`.
    Se o acumulador estiver vazio: `$this->info('Conferência OK — todas as N pessoas do relatório tiveram
    o snapshot atualizado.')`. Se não estiver: `$this->warn()` + `$this->table()` **nomeando cada pessoa e
    competência**, com o texto: *"estas N pessoas foram aprovadas no relatório mas o snapshot NÃO foi
    atualizado — motivo provável: cobertura de margem < 0,7 (gate FIXMARG-03 do
    `desempenho:consolidar-mes`, ver `Log::error` de `[Desempenho Mensal]`). Ação: re-rodar
    `php artisan desempenho:consolidar-mes --mes=YYYY-MM` quando o rate-limit da Adman passar."*
    Nesse caso o comando **não pode sair silenciosamente com sucesso**: retornar `self::FAILURE` (as
    linhas imputadas ficam gravadas — a falha é do congelamento, não do backfill; dizer isso na saída) e
    logar `Log::error('[NPS Imputação] snapshot não materializado para parte das pessoas', [...])`.
    Fixar essa escolha (exit code) igual à do teste da Tarefa 1.

    **Cache-bust:** para cada `user_id` afetado, `Cache::forget($scoreService->cacheKey($userId, $competencia))`
    onde `$competencia` = `competencia_nps` menos 1 mês (régua NPSWIN-03). Duplicar a régua aqui em vez
    de chamar `NpsController` (mesmo racional documentado no comando analog).

    **`--desfazer`:** exige `--mes`; mostra quantas linhas serão apagadas (por competência e por pessoa),
    pede confirmação (a menos de `--force`), apaga TODAS as linhas daquela competência (inclusive
    `definitivo` — é o caminho de rollback), busta o cache das pessoas afetadas, **chama o mesmo
    `reconsolidarSnapshots()` seguido do mesmo `conferirSnapshotsReconsolidados()`** (sem isso o snapshot
    fica inconsistente com as linhas removidas: o congelado continuaria com a nota rebaixada que já não
    tem lastro — e sem a conferência o operador não saberia que o rollback não chegou ao congelado) e loga
    `Log::warning('[NPS Imputação] rollback executado', [...])`. No rollback o `$esperado` é o valor
    **pré-backfill** (calculado com `setIncluirImputadas(false)`).
  </action>
  <verify>
    <automated>php artisan test --filter=NpsMaterializarNaoRespondidosCommandTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan list | grep -c "nps:materializar-nao-respondidos"` retorna 1
    - O arquivo contém `--dry-run`, `--force`, `--mes`, `--desfazer`, `setIncluirImputadas`, `classificar`, `Cache::forget`, `Log::warning` e `desempenho:consolidar-mes`
    - `grep -c "reconsolidarSnapshots" app/Console/Commands/NpsMaterializarNaoRespondidos.php` retorna >= 3 (definição + chamada no fluxo padrão + chamada no `--desfazer`)
    - `grep -c "conferirSnapshotsReconsolidados" app/Console/Commands/NpsMaterializarNaoRespondidos.php` retorna >= 3 (definição + chamada no fluxo padrão + chamada no `--desfazer`)
    - `grep -v '^\s*//' app/Console/Commands/NpsMaterializarNaoRespondidos.php | grep -c "DesempenhoScoreSnapshot::updateOrCreate"` retorna 0 (é reuso do comando, não reimplementação)
    - `grep -v '^\s*//' app/Console/Commands/NpsMaterializarNaoRespondidos.php | grep -c "Degradados"` retorna 0 (a conferência é por re-consulta ao banco, não por parsing da saída do comando compartilhado)
    - `grep -c "DesempenhoScoreSnapshot::mensal" app/Console/Commands/NpsMaterializarNaoRespondidos.php` retorna >= 1
    - `php artisan nps:materializar-nao-respondidos --mes=junho` sai != 0 e não grava nada
    - `php artisan test --filter=NpsMaterializarNaoRespondidosCommandTest` sai 0
    - Commit isolado `feat(116-06): comando nps:materializar-nao-respondidos com relatório antes/depois, reconsolidação verificada do snapshot e rollback`
  </acceptance_criteria>
  <done>Comando completo, com relatório de impacto, reconsolidação do registro autoritativo, conferência nominal do que foi congelado e rollback, suíte de Console verde.</done>
</task>

<task type="auto">
  <name>Tarefa 3: ganchos no disparo (automático e manual) + agendamento diário</name>
  <files>app/Console/Commands/NpsDispararMensal.php, app/Http/Controllers/NpsController.php, routes/console.php</files>
  <read_first>
    - app/Console/Commands/NpsDispararMensal.php (arquivo alvo — o NpsSurvey::create dentro do loop de modelos aplicáveis, incluindo o ramo dry-run que NÃO pode gravar nada)
    - app/Http/Controllers/NpsController.php (arquivo alvo — método generate(), onde o survey manual é criado com month_reference NULL)
    - routes/console.php (padrão das entradas de schedule: dailyAt + timezone + name + withoutOverlapping)
    - app/Services/Nps/NpsImputationService.php (materializar)
  </read_first>
  <action>
    1. **`NpsDispararMensal`** — logo após cada `NpsSurvey::create(...)` bem-sucedido, chamar
       `app(NpsImputationService::class)->materializar($survey)`. Isso faz a nota 1 valer **desde o
       disparo** (D2) sem esperar o cron do dia seguinte. Envolver em `try/catch (\Throwable $e)` com
       `Log::warning('[NPS Imputação] falha ao materializar no disparo', [...])` — falha aqui NUNCA pode
       abortar o disparo do NPS (o cron diário corrige depois). **Não chamar nada no ramo `--dry-run`**
       do comando de disparo.
    2. **`NpsController::generate()`** — mesma chamada após a criação do survey manual, mesmo
       `try/catch`. Comentar que surveys manuais têm `month_reference` NULL e por isso caem no fallback
       `created_at` do serviço (D6).
    3. **`routes/console.php`** — agendar
       `Schedule::command('nps:materializar-nao-respondidos --force')->dailyAt('09:30')->timezone('America/Sao_Paulo')->name('nps-materializar-nao-respondidos')->onOneServer()->withoutOverlapping();`
       com bloco de comentário em pt-BR explicando: roda 30min depois de `nps:disparar-mensal` (09:00) e
       bem antes de `desempenho:consolidar-mes` (último dia do mês, 14:00), de forma que o snapshot do
       bônus sempre enxergue os não respondidos da competência; `--force` porque é execução não
       interativa; é idempotente por construção; e é ele que promove as linhas para `definitivo` quando
       a competência vira. Comentar também que a reconsolidação disparada por esta execução diária é
       naturalmente inócua no regime permanente (o `desempenho:consolidar-mes` é idempotente e a
       competência corrente ainda não tem snapshot mensal) — ela existe para o backfill retroativo — e
       que, por consequência, a conferência de snapshot também não tem o que cobrar no regime permanente
       (competência aberta = sem snapshot esperado, não entra no `$esperado`).
  </action>
  <verify>
    <automated>php artisan test --filter=NpsMaterializarNaoRespondidosCommandTest && php artisan test --filter=NpsDisparar</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "materializar" app/Console/Commands/NpsDispararMensal.php` retorna >= 1
    - `grep -c "materializar" app/Http/Controllers/NpsController.php` retorna >= 1
    - `routes/console.php` contém `nps:materializar-nao-respondidos --force` e `dailyAt('09:30')`
    - `php artisan schedule:list | grep -c "nps:materializar-nao-respondidos"` retorna 1
    - `php artisan test --filter=NpsDisparar` sai 0
    - `php artisan test --filter=Nps` sai 0
    - Commit isolado `feat(116-06): materializa o NPS não respondido no disparo e agenda a rotina diária`
  </acceptance_criteria>
  <done>Nota 1 passa a valer desde o disparo; rotina diária agendada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador com acesso a shell/CI → dado de bonificação | O comando escreve retroativamente dados que decidem quem recebe bônus |
| Flag `--mes` → query | Único input textual do comando |
| Cron (não interativo) → escrita automática | Execução diária sem humano no loop |
| Comando → `DesempenhoScoreSnapshot` (registro congelado do bônus) | A reconsolidação reescreve o registro autoritativo de competências fechadas |
| Relatório aprovado pelo humano → snapshot efetivamente gravado | O relatório usa `compute()` direto; a gravação passa pelo gate FIXMARG-03 — os dois podem divergir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-06-01 | Tampering | escrita retroativa em massa sem revisão | mitigate | Sem `--dry-run` e sem `--force` o comando SEMPRE imprime diff + relatório antes/depois + plano de reconsolidação e exige `confirm(default false)`; o backfill histórico é operação humana (checkpoint no plano 116-08) |
| T-116-06-02 | Tampering | `--mes` malicioso/malformado | mitigate | Regex `^\d{4}-\d{2}$` + `Carbon::parse` antes de qualquer query; zero interpolação em `whereRaw` |
| T-116-06-03 | Repudiation | não saber quem rodou o backfill e quando | mitigate | `Log::info`/`Log::warning` estruturado com stats, `--mes`, competências reconsolidadas, pessoas não materializadas, usuário do SO e timestamp, em toda execução que grava ou apaga |
| T-116-06-04 | Denial of Service | execução concorrente do cron com o comando manual | mitigate | `withoutOverlapping()` + `onOneServer()` no schedule; a escrita é idempotente por grão único; a reconsolidação também é idempotente (`updateOrCreate`) |
| T-116-06-05 | Tampering | escrita irreversível | mitigate | `--desfazer --mes=YYYY-MM` apaga as linhas da competência (rollback real), busta o cache, **reconsolida** o snapshot e **confere** o resultado, devolvendo o congelado ao valor pré-backfill; a migration `down()` derruba a tabela inteira como último recurso |
| T-116-06-06 | Elevation of Privilege | disparar a materialização pela web | mitigate | Nenhum endpoint HTTP é criado — a materialização só existe via Artisan (acesso a servidor/CI). Se algum dia virar botão, terá que passar por `EnsureUserHasRole` `role:admin` |
| T-116-06-07 | Denial of Service | falha da materialização abortar o disparo do NPS | mitigate | Gancho no disparo em `try/catch (\Throwable)` com log — nunca propaga |
| T-116-06-08 | Repudiation | reconsolidação recusada pelo gate FIXMARG-03 deixando o snapshot de ALGUMAS pessoas com o número pré-backfill, enquanto o operador acredita ter aprovado a mudança para todas | mitigate | `conferirSnapshotsReconsolidados()` **re-consulta o `DesempenhoScoreSnapshot`** de cada par (pessoa, competência) do relatório aprovado e compara `classificacao`/`score` com o esperado. Divergência → `$this->warn` + tabela **nomeando pessoa e competência**, instrução de re-rodar `desempenho:consolidar-mes --mes=`, `Log::error` estruturado e exit code de falha. **Não depende de parsear a saída do comando compartilhado** (que só traz a contagem agregada `Degradados: N`, sem nomes, e cuja formatação pode mudar). Coberto por caso de teste dedicado que força `cobertura < 0.7` e assere o nome da pessoa na saída |
| T-116-06-09 | Tampering | reconsolidar competências que não foram backfilladas | mitigate | `reconsolidarSnapshots()` só recebe as competências financeiras distintas derivadas do detalhe efetivamente gravado — nunca um range aberto; coberto por caso de teste dedicado |
| T-116-06-10 | Tampering | alterar `ConsolidarMesDesempenho` (comando que paga o bônus, árvore compartilhada) para obter o aviso | accept | Decisão explícita de NÃO tocar no arquivo: a conferência por re-consulta entrega a mesma informação com nomes, sem risco de regressão no gate FIXMARG-03 nem conflito com outras sessões (ver `<notas_de_execucao>`) |
| T-116-06-SC | Tampering | instalação de pacotes | accept | Nenhum pacote novo nesta fase |
</threat_model>

<verification>
1. `php artisan test --filter=NpsMaterializarNaoRespondidosCommandTest` verde.
2. `php artisan test --filter=Nps` verde.
3. `php artisan schedule:list` mostra a entrada `nps-materializar-nao-respondidos`.
4. `php artisan nps:materializar-nao-respondidos --dry-run` roda sem gravar e sem reconsolidar (ambiente local com banco de pé).
5. Teste dedicado prova que uma competência fechada backfillada tem o `DesempenhoScoreSnapshot` mensal alterado (não só o `compute()`).
6. Teste dedicado prova que, com `cobertura < 0.7`, a saída **nomeia** a pessoa e a competência cujo snapshot não foi atualizado (não apenas uma contagem).
</verification>

<success_criteria>
- Comando idempotente, com relatório antes/depois por pessoa e competência, confirmação obrigatória e rollback.
- O backfill retroativo chega ao registro autoritativo do bônus (`DesempenhoScoreSnapshot`) das competências afetadas — ranking, Relatório de Bonificação e auditoria mostram o número novo.
- Quando NÃO chega para alguém, o comando diz **quem** e **em qual competência**, com a ação de correção — nunca uma contagem agregada nem sucesso silencioso.
- `--desfazer` devolve linhas E snapshot ao estado anterior, com a mesma conferência.
- NPS recém-disparado já conta como 1 sem esperar o cron.
- Rotina diária agendada antes do congelamento do bônus.
</success_criteria>

<output>
Criar `.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-06-SUMMARY.md` ao concluir.
</output>

