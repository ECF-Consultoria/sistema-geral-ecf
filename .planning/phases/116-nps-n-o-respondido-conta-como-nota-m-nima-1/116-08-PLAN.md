---
phase: 116-nps-n-o-respondido-conta-como-nota-m-nima-1
plan: 08
type: execute
wave: 4
depends_on: ["116-02", "116-03", "116-04", "116-05", "116-06", "116-07"]
files_modified:
  - docs/nps-nao-respondido-nota-1.md
  - tests/Feature/Phase116/NpsFloorRegressaoTest.php
autonomous: false
requirements: [NPSFLOOR-08, NPSFLOOR-10]

must_haves:
  truths:
    - "A suíte completa do projeto está verde com a regra ligada"
    - "Todos os consumidores de média de NPS mostram o mesmo número para o mesmo cenário"
    - "Existe documento explicando a regra, a janela, o rollback e como rodar o backfill"
    - "O backfill retroativo em produção só acontece depois de o usuário revisar o relatório antes/depois"
  artifacts:
    - path: "docs/nps-nao-respondido-nota-1.md"
      provides: "Documentação operacional da regra e do backfill"
      contains: "nps:materializar-nao-respondidos"
    - path: "tests/Feature/Phase116/NpsFloorRegressaoTest.php"
      provides: "Teste de coerência entre call-sites (mesmo cenário, mesmo número)"
      contains: "NpsFloorRegressaoTest"
  key_links:
    - from: "docs/nps-nao-respondido-nota-1.md"
      to: "app/Console/Commands/NpsMaterializarNaoRespondidos.php"
      via: "procedimento operacional de dry-run, aplicação e rollback"
      pattern: "nps:materializar-nao-respondidos"
---

<objective>
Fechar a fase: suíte completa verde, um teste de coerência entre os call-sites, documentação operacional
e o gate humano do backfill retroativo em produção (D1).

Purpose: a fase muda dinheiro em competências fechadas. O último passo NÃO é código — é o usuário
olhar o relatório antes/depois e decidir aplicar.
Output: 1 suíte de coerência, 1 doc, 2 checkpoints humanos.

## O que este plano NÃO faz

Não roda o backfill de produção automaticamente. A aplicação retroativa é **operação manual do usuário**,
depois de revisar o `--dry-run`. O plano fornece o procedimento e o gate, não a execução.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-CONTEXT.md
@.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-01-SUMMARY.md
@.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-06-SUMMARY.md
</context>

<notas_de_execucao>
- **Árvore compartilhada:** commitar só os próprios paths, nunca `git add -A`.
- **Deploy:** não executar `deploy.sh` sem autorização explícita do usuário neste plano.
- Documento em pt-BR.
</notas_de_execucao>

<tasks>

<task type="auto">
  <name>Tarefa 1: teste de coerência entre call-sites + suíte completa verde</name>
  <files>tests/Feature/Phase116/NpsFloorRegressaoTest.php</files>
  <read_first>
    - tests/Feature/Phase96/NpsInvalidacaoCallSitesTest.php (molde do teste de coerência entre telas)
    - tests/Feature/Phase116/NpsFloorAreaNpsTest.php (cenário base já montado)
    - tests/Feature/Phase116/NpsFloorDesempenhoTest.php
    - tests/Feature/Phase116/NpsFloorCarteiraTest.php
    - tests/Feature/Phase116/NpsFloorDashboardsTest.php
  </read_first>
  <behavior>
    - Cenário único (1 empresa, 1 responsável de cada papel, 1 resposta real nota 5 e 1 survey não
      respondido no mesmo mês) verificado em TODOS os consumidores: card da área NPS, componente de NPS
      do bônus, coluna NPS da carteira, ranking do dashboard, média da página da empresa e meta de NPS —
      todos com o valor esperado pela regra, sem nenhum deles ignorando o não respondido.
    - Cenário-espelho sem nenhum survey: todos os consumidores mantêm o comportamento anterior à fase
      (invariante D3 verificado de ponta a ponta).
  </behavior>
  <action>
    Criar `tests/Feature/Phase116/NpsFloorRegressaoTest.php` (namespace `Tests\Feature\Phase116`,
    `RefreshDatabase`) com um método por consumidor sobre a MESMA fixture (extraída para um helper
    `montarCenarioBase()` no próprio arquivo), no molde de `NpsInvalidacaoCallSitesTest`. É o checklist
    executável contra o Pitfall 4 da Fase 96: se algum call-site futuro esquecer a regra, este arquivo
    fica vermelho.
    Depois, rodar a suíte completa (`php artisan test`) e tratar qualquer falha nova pela mesma régua dos
    planos anteriores: fixture com survey pendente "de passagem" → ajustar o valor esperado com
    comentário `// Fase 116`; regra errada → corrigir o código. Falhas pré-existentes não relacionadas
    (documentadas em `.planning/deferred-items.md`) devem ser listadas no SUMMARY como pré-existentes,
    com a prova de que já falhavam antes (rodar o mesmo filtro no commit anterior à fase, se necessário).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase116 && php artisan test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Phase116/NpsFloorRegressaoTest.php` existe e contém `montarCenarioBase`
    - `grep -c "public function test_" tests/Feature/Phase116/NpsFloorRegressaoTest.php` retorna >= 7
    - `php artisan test --filter=Phase116` sai 0
    - `php artisan test` sai 0, OU o SUMMARY lista nominalmente cada falha remanescente com prova de que é pré-existente
    - `git diff --unified=0 -- tests/ | grep -c "^+.*markTestSkipped"` retorna 0
    - Commit isolado `test(116-08): coerência entre call-sites do piso de NPS + suíte completa`
  </acceptance_criteria>
  <done>Suíte completa verde (ou falhas pré-existentes provadas) e checklist de call-sites executável no repositório.</done>
</task>

<task type="auto">
  <name>Tarefa 2: documentação operacional da regra e do backfill</name>
  <files>docs/nps-nao-respondido-nota-1.md</files>
  <read_first>
    - docs/hubspot-regra-de-valor.md (molde de doc técnico curto do projeto, entregue na Fase 115)
    - app/Console/Commands/NpsMaterializarNaoRespondidos.php (flags reais do comando)
    - app/Services/Nps/NpsImputationService.php (regra de elegibilidade e de fechamento)
    - .planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-CONTEXT.md (decisões D1–D7)
  </read_first>
  <action>
    Criar `docs/nps-nao-respondido-nota-1.md` (pt-BR, curto e operacional) com as seções:
    **1. A regra** — NPS efetivamente disparado e não respondido vale nota 1 (piso da escala 1–5) em
    todos os consumidores da média; empresa sem disparo nunca entra; empresa invalidada na competência
    não entra.
    **2. Quando vira definitivo** — vale desde o disparo; quando o mês do disparo termina sem resposta,
    a nota 1 fica congelada e uma resposta posterior não reescreve aquele mês. Explicar por que o gate é
    "o mês virou" e não "é o último dia" (o link vale até 23:59:59 do último dia).
    **3. Onde a regra aparece** — lista dos consumidores tocados (área NPS, bônus/Desempenho, carteira
    do profissional, dashboards, página da empresa, meta de NPS), com arquivo e método de cada um. Esta
    lista é o checklist para quem for mexer em agregação de NPS no futuro.
    **4. Operação** — como rodar `nps:materializar-nao-respondidos` (`--dry-run`, `--mes`, `--force`),
    o que o relatório antes/depois significa, que ele roda diariamente às 09:30 BRT, e que o comando é
    idempotente.
    **5. Rollback** — `--desfazer --mes=YYYY-MM` (com confirmação), o que ele apaga, e o fato de que a
    tabela `nps_imputed_assignments` é isolada: nenhuma resposta real é alterada pela fase.
    **6. Cache** — a chave do bônus está em `desempenho.compute.v12`; depois de aplicar backfill em
    produção, rodar `php artisan cache:clear` no VPS.
    Sem números de linha (envelhecem); referenciar sempre por nome de classe/método.
  </action>
  <verify>
    <automated>test -f docs/nps-nao-respondido-nota-1.md && grep -c "nps:materializar-nao-respondidos\|--desfazer\|desempenho.compute.v12" docs/nps-nao-respondido-nota-1.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/nps-nao-respondido-nota-1.md` existe e contém as strings `--dry-run`, `--desfazer`, `desempenho.compute.v12` e `cache:clear`
    - O documento lista nominalmente os 6 grupos de consumidores com classe e método
    - `grep -c "linha [0-9]" docs/nps-nao-respondido-nota-1.md` retorna 0 (sem referência a número de linha)
    - Commit isolado `docs(116-08): regra do NPS não respondido, operação do backfill e rollback`
  </acceptance_criteria>
  <done>Documento operacional commitado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 3: revisão do relatório antes/depois e decisão sobre o backfill retroativo</name>
  <what-built>
    A regra está implementada e testada em todos os consumidores; o comando
    `nps:materializar-nao-respondidos` está pronto, agendado (09:30 BRT) e com rollback
    (`--desfazer --mes=YYYY-MM`). O histórico ainda NÃO foi materializado em produção — é decisão do
    usuário (D1), porque pode mudar quem bateu o bônus em competências já fechadas.
  </what-built>
  <how-to-verify>
    1. Com o banco de pé (local com dados reais ou VPS em leitura), quantificar o volume:
       contar `nps_surveys` com `status != 'completed'` agrupado por `month_reference` — é o tamanho do
       backfill.
    2. Rodar `php artisan nps:materializar-nao-respondidos --dry-run` e revisar a tabela de impacto:
       por pessoa e competência, `NPS antes` × `NPS depois`, `Nota antes` × `Nota depois` e
       `Faixa antes` × `Faixa depois`.
    3. Conferir especialmente as linhas marcadas como "muda faixa" — são as pessoas cujo resultado de
       bônus em mês fechado muda.
    4. Se o impacto estiver de acordo: aplicar por competência (`--mes=YYYY-MM`, uma de cada vez,
       confirmando a cada uma) ou de uma vez, e em seguida rodar `php artisan cache:clear` no VPS.
    5. Se algo parecer errado: NÃO aplicar; reportar quais linhas destoam. Se já tiver aplicado,
       `php artisan nps:materializar-nao-respondidos --desfazer --mes=YYYY-MM` reverte.
    6. Conferir depois, na tela `/nps` e no Desempenho, que os números batem entre si.
  </how-to-verify>
  <read_first>
    - docs/nps-nao-respondido-nota-1.md (procedimento escrito na tarefa 2)
    - app/Console/Commands/NpsMaterializarNaoRespondidos.php
  </read_first>
  <action>
    Pausar a execução e apresentar ao usuário o procedimento de how-to-verify. Nenhum arquivo é alterado
    nesta tarefa e NENHUM comando de escrita é executado pelo agente: a materialização retroativa em
    produção é operação do usuário (D1). Registrar no SUMMARY a decisão tomada e as competências
    efetivamente aplicadas.
  </action>
  <verify>
    <human-check>Usuário confirma revisão do relatório antes/depois e informa a decisão (aplicar / adiar)</human-check>
  </verify>
  <acceptance_criteria>
    - Usuário confirma que revisou o relatório antes/depois e que o impacto por pessoa/competência está de acordo
    - Usuário informa se aplicou o backfill (e em quais competências) ou se optou por adiar
    - Se aplicado: `php artisan cache:clear` executado no ambiente onde a escrita ocorreu
  </acceptance_criteria>
  <resume-signal>Responder "aprovado" (com as competências aplicadas) ou descrever o que destoou no relatório</resume-signal>
  <done>Decisão do usuário registrada no SUMMARY, com as competências efetivamente aplicadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador → produção | A materialização retroativa altera a base do bônus de competências fechadas |
| Cache de produção → valor pago | Cache não limpo depois do backfill serve número antigo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-08-01 | Tampering | backfill retroativo em produção | mitigate | Checkpoint humano bloqueante: só aplica depois que o usuário revisa o relatório antes/depois; aplicação recomendada competência a competência |
| T-116-08-02 | Repudiation | não saber o que foi aplicado | mitigate | O SUMMARY registra as competências aplicadas; o comando loga cada execução com stats e `--mes` |
| T-116-08-03 | Tampering | cache servindo bônus antigo depois do backfill | mitigate | `cache:clear` no procedimento e no doc; a chave já foi bumpada para `v12` no plano 116-02 |
| T-116-08-04 | Denial of Service | deploy sem autorização | mitigate | Nota explícita de execução: não rodar `deploy.sh` sem autorização do usuário |
| T-116-08-SC | Tampering | instalação de pacotes | accept | Nenhum pacote novo nesta fase |
</threat_model>

<verification>
1. `php artisan test --filter=Phase116` verde.
2. `php artisan test` verde (ou falhas pré-existentes nominalmente provadas no SUMMARY).
3. `docs/nps-nao-respondido-nota-1.md` existe com o procedimento completo.
4. Checkpoint do backfill revisado e decidido pelo usuário.
</verification>

<success_criteria>
- Um mesmo cenário produz o mesmo número em todos os consumidores de média de NPS.
- Documento operacional permite rodar, revisar e reverter o backfill sem reler o código.
- Nenhuma escrita retroativa em produção aconteceu sem revisão humana do impacto no bônus.
</success_criteria>

<output>
Criar `.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-08-SUMMARY.md` ao concluir.
</output>
