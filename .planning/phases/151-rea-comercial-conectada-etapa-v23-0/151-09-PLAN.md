---
phase: 151-rea-comercial-conectada-etapa-v23-0
plan: 09
type: execute
wave: 4
depends_on: [151-07, 151-08]
files_modified:
  - .planning/phases/151-rea-comercial-conectada-etapa-v23-0/138-CONTA-SISTEMA-HUBSPOT.md
autonomous: false
requirements: [COMERC-01, COMERC-02, COMERC-03]
must_haves:
  truths:
    - "A conta 'Sistema HubSpot' existe em local e na VPS, é comprovadamente não-logável e está registrada onde não vire login esquecido"
    - "As listagens Contrato e Entrada renderizam de verdade no navegador, com os 8 campos do §2 e as duas pendências em colunas separadas"
    - "A suíte da fase e a baseline pré-migration estão verdes, com as ~10 falhas antigas de Polos separadas de qualquer falha nova"
    - "Nenhum deploy sai desta fase sem autorização explícita do usuário"
    - "Decisões do CONTEXT implementadas por este plano: D-17 (checkpoint humano antes do deploy conferindo que a conta de sistema não é logável, criada nos dois ambientes e registrada — o precedente direto é o usuário de review da Shopee, `users.id=30`, ativo em produção desde 2026-07-16 porque ninguém anotou que precisava sair), D-15 (conferência visual de que Contrato dentro do Comercial continua acessível por quem tem só `admin.contratos`), D-16 (conferência de que `Administrativo › Empresas` sumiu do menu e continua respondendo por URL direta), D-01 (conferência de que a fase entregou a casca — nenhum item de checklist aparece nas telas, o que é escopo da Fase 152)"
  artifacts:
    - path: ".planning/phases/151-rea-comercial-conectada-etapa-v23-0/138-CONTA-SISTEMA-HUBSPOT.md"
      provides: "Registro durável da conta de sistema nos dois ambientes, para não virar login esquecido"
      contains: "id_vps"
  key_links:
    - from: "138-CONTA-SISTEMA-HUBSPOT.md"
      to: ".env local e .env da VPS"
      via: "HUBSPOT_WEBHOOK_USER_ID precisa apontar para o id de cada ambiente, e os ids são diferentes"
      pattern: "HUBSPOT_WEBHOOK_USER_ID"
---

<objective>
Fechar a fase com as três verificações que nenhum teste automatizado consegue fazer, e com a
comparação de regressão contra a baseline registrada no plano 151-01.

Propósito: três coisas desta fase vivem fora do alcance do PHPUnit — uma conta de usuário
criada em dois ambientes (um deles produção), o render real de duas telas Inertia, e o estado
do escopo OAuth contra a conta HubSpot real. As três precisam de olho humano.

O precedente que torna o checkpoint da conta de sistema obrigatório está registrado no
`MEMORY.md` do projeto: o usuário de review da Shopee (`users.id=30`) continua ativo em
produção desde 2026-07-16 porque ninguém anotou que precisava sair. Uma conta de sistema criada
e não registrada vira exatamente isso.

**Nenhum deploy sai desta fase.** O `CLAUDE.md` proíbe deploy sem autorização explícita, e este
plano não pede autorização — ele prepara o terreno e registra o que precisa acontecer nos dois
ambientes quando o deploy for autorizado, em sessão própria.

Saída: suíte comparada com a baseline, conta de sistema conferida e registrada nos dois
ambientes, e as duas telas verificadas no navegador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/151-rea-comercial-conectada-etapa-v23-0/151-CONTEXT.md
@.planning/phases/151-rea-comercial-conectada-etapa-v23-0/151-VALIDATION.md
@.planning/phases/151-rea-comercial-conectada-etapa-v23-0/151-BASELINE-TESTES.md
@.planning/phases/151-rea-comercial-conectada-etapa-v23-0/138-HUBSPOT-MEDICOES.md
@.planning/learnings/painel-polos-status-e-meta.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suíte da fase comparada com a baseline pré-migration</name>
  <files>.planning/phases/151-rea-comercial-conectada-etapa-v23-0/138-CONTA-SISTEMA-HUBSPOT.md</files>
  <read_first>
    - `.planning/phases/151-rea-comercial-conectada-etapa-v23-0/151-BASELINE-TESTES.md` — os números do "antes" e a tabela "Regra de comparação"
    - `.planning/phases/151-rea-comercial-conectada-etapa-v23-0/151-VALIDATION.md` § "Amostragem" — o comando de suíte por wave
    - `.planning/learnings/painel-polos-status-e-meta.md` §2 — as ~10 falhas antigas de Polos, que não são regressão
  </read_first>
  <action>
    Rode, cada um separadamente e com `C:\xampp\php\php.exe`:

    1. A suíte da fase: `vendor/bin/phpunit tests/Unit/Phase151 tests/Feature/Phase151 --colors=never`
    2. A suíte por wave, com as regressões:
       `vendor/bin/phpunit tests/Feature/Phase131 tests/Unit/Phase150 tests/Feature/Phase150 tests/Feature/Phase34HubspotWebhookTest.php --colors=never`
    3. A baseline do plano 151-01:
       `vendor/bin/phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php --colors=never`
    4. As duas suítes de Polos, isoladas:
       `vendor/bin/phpunit tests/Feature/Phase38/PolosControllerTest.php --colors=never` e
       `vendor/bin/phpunit tests/Feature/Polos/PolosFaturamentoSnapshotTest.php --colors=never`

    Não rode `php artisan test` nem `--testsuite=Feature` inteiro — travam neste ambiente.

    Compare item a item com a tabela "Regra de comparação" do `151-BASELINE-TESTES.md`.
    Qualquer falha **fora** das duas suítes de Polos é regressão desta fase e precisa ser
    corrigida antes de acionar os checkpoints — não leve regressão para o humano verificar.

    Rode também `npm run build` (timeout de 600000 ms) e confirme que
    `public/build/manifest.json` contém as entradas de `Comercial/Entrada.jsx`,
    `Admin/Contratos.jsx` e `Layouts/AppLayout.jsx`. Confirme que não há `public/hot` órfão no
    worktree — um `public/hot` esquecido derruba o carregamento de JSX no `artisan serve` sem
    erro visível.

    Suba o app local (`php artisan serve` ou XAMPP) apontando para `/comercial/entrada` e
    `/administrativo/contratos`, deixando as duas telas prontas para o humano abrir.

    Atualize `138-CONTA-SISTEMA-HUBSPOT.md` (criado pelo plano 151-07) com o resultado da
    comparação: a tabela de suíte × baseline, e o `id` local da conta de sistema confirmado por
    reconsulta SQL (`SELECT id, name, email, role, active FROM users WHERE email='sistema.hubspot@ecfconsultoria.com.br'`),
    nunca pelo que o comando imprimiu.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase151 tests/Feature/Phase151 tests/Feature/Phase131 tests/Unit/Phase150 tests/Feature/Phase150 --colors=never</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Unit/Phase151`, `tests/Feature/Phase151`, `tests/Feature/Phase131`, `tests/Unit/Phase150` e `tests/Feature/Phase150` saem todos com exit code 0
    - `tests/Feature/Phase34HubspotWebhookTest.php` sai com exit code 0
    - A baseline (`Phase37CompaniesPerformanceFilterTest` + `CompanyControllerResponsavelPerformanceTest`) devolve exatamente a mesma contagem registrada em `151-BASELINE-TESTES.md`
    - As duas suítes de Polos devolvem a mesma contagem de falhas registrada na baseline — nem uma a mais
    - `npm run build` sai com exit code 0 e `public/build/manifest.json` contém as três entradas nomeadas no action
    - `ls public/hot` não encontra o arquivo
    - `138-CONTA-SISTEMA-HUBSPOT.md` contém a tabela suíte × baseline e o `id` local da conta obtido por `SELECT` direto
  </acceptance_criteria>
  <done>A fase está verde contra a baseline, o front está buildado e as duas telas estão no ar para verificação humana.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Conta "Sistema HubSpot" não-logável em local e na VPS</name>
  <files>.planning/phases/151-rea-comercial-conectada-etapa-v23-0/138-CONTA-SISTEMA-HUBSPOT.md</files>
  <read_first>
    - `.planning/phases/151-rea-comercial-conectada-etapa-v23-0/151-CONTEXT.md` D-17 — as quatro obrigações que a decisão cria
    - `.planning/phases/151-rea-comercial-conectada-etapa-v23-0/138-CONTA-SISTEMA-HUBSPOT.md` — o registro criado pelo plano 151-07, com `id_vps:` em branco
    - `app/Console/Commands/HubspotCriarUsuarioSistema.php` — o comando, sua idempotência, e o comentário sobre o login não checar `users.active`
    - `.planning/phases/151-rea-comercial-conectada-etapa-v23-0/138-HUBSPOT-MEDICOES.md` — o campo `escopo_owners_read:`, que pode ter ficado `NÃO CONFIRMADO` no plano 151-02
  </read_first>
  <action>
    Antes de acionar o checkpoint, cole no corpo dele: o `id` local da conta obtido por `SELECT`
    direto na Task 1, o e-mail canônico, e o resultado do teste automatizado de
    não-logabilidade (`ComercAtorSistemaTest`, caso 4 — `Auth::attempt` falha para as 4 senhas
    óbvias). Cole também o valor atual de `escopo_owners_read:` do `138-HUBSPOT-MEDICOES.md`.
    Então pare e aguarde a verificação humana.
  </action>
  <what-built>
    O comando `hubspot:criar-usuario-sistema` cria, de forma idempotente, a conta
    `Sistema HubSpot` (`sistema.hubspot@ecfconsultoria.com.br`) com `role = 'consultor'`,
    `active = false`, senha `Hash::make(Str::random(64))` descartada na mesma expressão, sem
    linha em `user_setores` (logo sem cargo e sem permissão por setor) e sem linha em
    `company_users`. `config('services.hubspot.webhook_user_id')` aponta para ela, e é ela que
    consta como ator em `company_etapa_transicoes` de toda empresa nascida pelo webhook.

    **Achado que muda o que precisa ser verificado:** o fluxo de login deste projeto **não**
    checa `users.active` — não há verificação em `LoginRequest` nem nos controllers de `Auth/`.
    `active = false` é defesa em profundidade; a senha aleatória de 64 caracteres é o gate real.
  </what-built>
  <how-to-verify>
    1. **Local.** Confirme por consulta ao banco que existe exatamente **uma** linha em `users`
       com o e-mail `sistema.hubspot@ecfconsultoria.com.br`, com `active = 0` e
       `role = 'consultor'`. Anote o `id`.
    2. Confirme que `HUBSPOT_WEBHOOK_USER_ID` no `.env` local aponta para esse `id`.
    3. Tente logar na tela de login local com esse e-mail e as senhas `password`, vazio, e o
       próprio e-mail. As três precisam falhar.
    4. Confirme que a conta não aparece com nenhum cargo nem permissão na tela de setores.
    5. **VPS.** A conta ainda **não** existe lá — este plano não faz deploy. Confirme e registre
       o roteiro para quando o deploy for autorizado, em sessão própria: rodar
       `php artisan hubspot:criar-usuario-sistema --apply` na VPS, anotar o `id` **daquele**
       ambiente (será diferente do local) e apontar `HUBSPOT_WEBHOOK_USER_ID` do `.env` da VPS
       para ele. Sem isso, o webhook em produção cria a empresa mas ela nunca ganha etapa —
       degrada em silêncio, com log, exatamente como desenhado.
    6. **Registro.** Confirme que `138-CONTA-SISTEMA-HUBSPOT.md` está preenchido com: id local,
       e-mail, data, o campo `id_vps:` (em branco até o deploy) e uma linha dizendo
       explicitamente que esta é uma conta de sistema que deve ser auditada em toda revisão de
       usuários. Precedente direto: o usuário de review da Shopee (`users.id=30`) segue ativo em
       produção desde 2026-07-16 porque ninguém anotou que precisava sair.
    7. Se `escopo_owners_read:` ficou `NÃO CONFIRMADO` no plano 151-02, diga agora se você
       consegue confirmar no painel do HubSpot ou se isso fica como pendência declarada para a
       primeira chamada real de `fetchOwner()` em produção.
    8. Responda "aprovado" ou descreva o que precisa mudar.
  </how-to-verify>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase151/ComercAtorSistemaTest.php --colors=never</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Phase151/ComercAtorSistemaTest.php` sai com exit code 0
    - `SELECT COUNT(*) FROM users WHERE email='sistema.hubspot@ecfconsultoria.com.br'` devolve exatamente `1` no ambiente local
    - Essa linha tem `active = 0` e `role = 'consultor'`, e não tem nenhuma linha correspondente em `user_setores` nem em `company_users`
    - As três tentativas de login manuais do passo 3 falharam
    - `138-CONTA-SISTEMA-HUBSPOT.md` contém `id_vps:` e a linha que declara a conta como auditável em toda revisão de usuários
    - O arquivo registra o roteiro de criação na VPS e a consequência de não fazê-la (empresa nasce sem etapa, com log, sem erro visível)
    - Nenhum comando de deploy foi executado
  </acceptance_criteria>
  <resume-signal>Responda "aprovado" ou descreva o que precisa mudar na conta de sistema</resume-signal>
  <done>A conta de sistema é comprovadamente não-logável no local, está registrada por escrito, e o roteiro para a VPS está declarado sem que nenhum deploy tenha acontecido.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Render das listagens Contrato e Entrada e da navegação reorganizada</name>
  <files>resources/js/Pages/Comercial/Entrada.jsx, resources/js/Pages/Admin/Contratos.jsx, resources/js/Layouts/AppLayout.jsx</files>
  <read_first>
    - `.planning/phases/151-rea-comercial-conectada-etapa-v23-0/151-VALIDATION.md` § "Verificações só manuais", última linha — grep no bundle prova o deploy, nunca que a mudança funciona
    - `.planning/phases/151-rea-comercial-conectada-etapa-v23-0/151-CONTEXT.md` D-01, D-06, D-11, D-15 e D-16
    - `.planning/ROADMAP.md`, bloco "Phase 151" — os 5 Success Criteria que esta verificação fecha
  </read_first>
  <action>
    Antes de acionar o checkpoint, confirme que o app local está no ar, que `npm run build`
    rodou nesta sessão e que existe ao menos uma empresa em cada uma das etapas 1 a 5 no banco
    local para a verificação de fronteira fazer sentido (crie-as pelo `EtapaTransicaoService`,
    nunca por `update()` direto). Cole no corpo do checkpoint as URLs e os ids das empresas de
    teste. Então pare e aguarde a verificação humana.
  </action>
  <what-built>
    A Área Comercial passou a ter quatro módulos: Cadastro de Empresas, Grupos, **Contrato**
    (que é a tela `admin.contratos` da Fase 131, com o item de menu movido de grupo — rota,
    controller e página não mudaram de lugar) e **Entrada** (módulo novo, casca). As duas
    listagens ganharam os 8 campos mínimos do §2, com pendência do fluxo e pendências do
    cadastro em colunas separadas. `Administrativo › Empresas` saiu do menu, com rota e tela
    vivas por URL direta.
  </what-built>
  <how-to-verify>
    1. Abra o app local e olhe a sidebar. O grupo **Comercial** precisa mostrar, nesta ordem:
       Cadastro de Empresas, Grupos, Contrato, Entrada — e depois os itens que já existiam.
    2. Olhe o grupo **Administrativo**: **Empresas** e **Contratos** não podem mais aparecer
       lá. Relatório, Fechamento e Inventário continuam.
    3. Abra `/administrativo/empresas` digitando a URL direta. A tela precisa carregar
       normalmente (D-16 — sai do menu agora, é apagada depois, em trabalho próprio).
    4. Clique em **Comercial › Contrato**. Confirme que a tela de contratos abre igual a antes,
       com o grid de resumo por situação no topo, e que a tabela agora mostra CNPJ, Setor,
       Responsável comercial, Data da venda, Etapa e Pendências além das colunas antigas.
    5. Clique em **Comercial › Entrada**. Confirme que a tabela lista empresas em fluxo de
       entrada com os mesmos campos.
    6. **A verificação que só o olho pega (D-11):** na coluna Pendências das **duas** telas,
       confirme que a pendência do fluxo (a declarada, com motivo) e as pendências do cadastro
       aparecem como coisas visualmente distintas — nunca somadas num número só. Se você vir
       um único contador, é regressão da D-11.
    7. Confirme que uma empresa sem responsável comercial mostra travessão (`—`), não uma
       mensagem de erro nem um alerta. Empresa cadastrada à mão nunca teve deal no HubSpot e
       por isso nunca tem owner — isso é correto e esperado.
    8. **Fronteira da D-07:** confirme que a empresa que está em "Administrativo Concluído"
       aparece na Entrada, e que a que está em "Aguardando Distribuição" não aparece.
    9. **A casca é casca (D-01/D-06):** confirme que nenhuma das duas telas mostra item de
       checklist, botão FINALIZAR ou controle que sugira que os 8 itens do módulo Entrada já
       existem. Eles são a Fase 152.
    10. Faça login com um usuário que tenha **só** `admin.contratos` (sem `role:admin`) e
        confirme que ele vê e abre o módulo Contrato dentro do Comercial. É a D-15 na prática —
        o motivo pelo qual essas rotas foram deliberadamente deixadas fora do `role:admin`.
    11. Responda "aprovado" ou descreva o que está errado.
  </how-to-verify>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase151 tests/Feature/Phase131 --colors=never</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Phase151` e `tests/Feature/Phase131` saem com exit code 0
    - Os 11 passos da verificação foram percorridos e respondidos pelo usuário
    - A verificação do passo 6 (duas pendências visualmente distintas) foi feita nas **duas** telas
    - A verificação do passo 10 (usuário só com `admin.contratos`) foi feita e resultou em acesso concedido
    - Nenhum item de checklist, botão FINALIZAR ou placeholder da Fase 152 aparece em qualquer das duas telas
    - `git status --porcelain` não lista nenhuma alteração pendente em `resources/js/` ao fim do checkpoint
    - Nenhum comando de deploy foi executado
  </acceptance_criteria>
  <resume-signal>Responda "aprovado" ou descreva o que está errado nas telas</resume-signal>
  <done>As duas listagens e a navegação reorganizada foram verificadas no navegador por um humano, incluindo a separação visual das duas pendências e o acesso por permissão própria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worktree local → VPS de produção | um deploy levaria migration, rotas novas e uma conta de usuário para produção |
| conta de sistema → superfície de autenticação de produção | uma linha em `users` que ninguém audita é uma porta esquecida |
| artefato de planejamento → memória institucional | o que não for escrito aqui não atravessa para a próxima sessão nem para a outra máquina |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-151-34 | Elevation of Privilege | conta de sistema esquecida em produção | mitigate | Checkpoint humano **bloqueante** e não auto-aprovável antes de qualquer deploy, conferindo não-logabilidade e ausência de cargo/permissão. Registro durável em `138-CONTA-SISTEMA-HUBSPOT.md` com a linha explícita de "auditar em toda revisão de usuários" — o precedente é `users.id=30` (review da Shopee), ativo desde 2026-07-16 por falta exatamente desse registro |
| T-151-35 | Tampering | deploy não autorizado | mitigate | Nenhuma task deste plano executa deploy, `git push`, `pscp` ou `deploy.sh`. O `CLAUDE.md` proíbe deploy sem autorização explícita; o roteiro para a VPS é **registrado**, não executado, e critério de aceite de duas tasks exige que nenhum comando de deploy tenha rodado |
| T-151-36 | Repudiation | verificação visual substituída por grep | mitigate | O `151-VALIDATION.md` registra na letra que grep no bundle prova o deploy, nunca que a mudança funciona. Por isso o render das duas telas é `checkpoint:human-verify` com 11 passos nomeados, não um teste de string no `manifest.json` |
| T-151-37 | Information Disclosure | escopo OAuth ainda não confirmado | accept | Se `escopo_owners_read:` ficou `NÃO CONFIRMADO` no plano 151-02, o efeito é degradação silenciosa: `fetchOwner()` devolve `null` em 403 e `hubspot_owner_nome` fica sempre vazio. Não é falha de segurança — é campo em branco. O passo 7 do checkpoint da Task 2 cobra a decisão explícita de confirmar agora ou declarar como pendência para a primeira chamada real em produção |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Unit/Phase151 tests/Feature/Phase151 tests/Feature/Phase131 tests/Unit/Phase150 tests/Feature/Phase150 --colors=never` sai 0
- A baseline e as duas suítes de Polos devolvem exatamente as contagens de `151-BASELINE-TESTES.md`
- `138-CONTA-SISTEMA-HUBSPOT.md` tem id local, `id_vps:` e a linha de auditoria
- Os dois checkpoints humanos foram aprovados
- Nenhum comando de deploy foi executado
</verification>

<success_criteria>
1. A conta de sistema é comprovadamente não-logável, está registrada e o roteiro para a VPS está escrito — sem deploy
2. As duas listagens renderizam no navegador com os 8 campos do §2 e as duas pendências visualmente separadas
3. `Administrativo › Empresas` sumiu do menu e continua acessível por URL direta; o módulo Contrato abre para quem tem só `admin.contratos`
4. A suíte da fase está verde e as ~10 falhas antigas de Polos continuam sendo exatamente as mesmas da baseline
</success_criteria>

<output>
Create `.planning/phases/151-rea-comercial-conectada-etapa-v23-0/151-09-SUMMARY.md` when done
</output>
