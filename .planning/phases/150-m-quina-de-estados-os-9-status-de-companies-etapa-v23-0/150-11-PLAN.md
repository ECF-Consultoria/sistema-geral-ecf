---
phase: 150-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0
plan: 11
type: execute
gap_closure: true
wave: 6
depends_on: ["150-08", "150-09"]
files_modified:
  - resources/js/Pages/Companies/Index.jsx
  - tests/Feature/Phase150/EtapaFiltroListagemTest.php
autonomous: false
requirements: [ETAPA-05]

must_haves:
  truths:
    - "Filtrar por etapa não apaga o filtro de Cust ID já ativo, e vice-versa"
    - "Filtrar por pendência não apaga nem o de etapa nem o de Cust ID"
    - "Trocar a ordenação da aba Pendências não apaga nenhum dos filtros ativos"
    - "A visão padrão de `/companies` continua sem filtro nenhum"
    - "Existe um único ponto no arquivo que monta a query de `companies.index` — nenhum handler monta params por conta própria"
  artifacts:
    - path: "resources/js/Pages/Companies/Index.jsx"
      provides: "Handlers de filtro centralizados num único montador de params"
      contains: "aplicarFiltros"
    - path: "tests/Feature/Phase150/EtapaFiltroListagemTest.php"
      provides: "Prova de que os três filtros combinam no backend + gate estático dos handlers"
  key_links:
    - from: "resources/js/Pages/Companies/Index.jsx"
      to: "CompanyController::index()"
      via: "router.get com cust_id_status + etapa + com_pendencia + sort na mesma query"
      pattern: "aplicarFiltros"
---

<objective>
Fechar o **G3 (WARNING)** do `150-REVIEW.md` (WR-03): os handlers de filtro de
`/companies` montam a query do zero e cada um esquece os outros. Escolher uma etapa apaga em
silêncio o filtro de Cust ID que o admin acabou de aplicar; escolher Cust ID apaga a etapa e a
pendência; trocar a ordenação da aba Pendências apaga etapa e pendência.

O backend **já suporta os três combinados** — `CompanyController::index()` encadeia
`cust_id_status`, `etapa`/`sem_etapa` e `com_pendencia` em `when()` sem conflito. A limitação é
inteiramente do cliente. Pior: contradiz o comentário que a própria Fase 150 escreveu logo
acima de `aplicarEtapaFilter` ("Cada handler PRESERVA o outro filtro já ativo — um não pode
apagar o outro (D-23)") — verdadeiro só para o par etapa/pendência, falso para a interação com
`cust_id_status` e com `sort`.

Purpose: filtro que apaga outro filtro em silêncio é o modo de falha que faz o operador
desconfiar do painel inteiro — e a D-23 exige combinabilidade real, não parcial.
Output: um único montador de query, os quatro handlers como invólucros finos, e a combinação
provada no backend e na tela.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/150-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/150-REVIEW.md
@.planning/phases/150-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/150-07-SUMMARY.md

> ⚠️ **Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada`. Conferir com `pwd`.
> ⚠️ **PHP:** `C:\xampp\php\php.exe`. **Nunca** `php artisan test` nem `--testsuite=Feature`.
> ⚠️ **`npm run build` ao fim de toda alteração de frontend** (`CLAUDE.md`). `node_modules/` e
> `public/build/` já existem neste worktree (criados em 150-01) e são gitignored — **não**
> rodar `npm install`, e **não** commitar artefato de build.
</context>

<measured_facts>
| Fato | Valor medido |
|---|---|
| `router.get(` em `resources/js/Pages/Companies/Index.jsx` | **4 ocorrências**, linhas 199, 208, 220, 227 — todas dentro dos quatro handlers de filtro, nenhuma em outro lugar do arquivo |
| `aplicarCustIdFilter` (linha 198) | envia **só** `cust_id_status`. Descarta `etapa`, `com_pendencia`, `sort`, `tab` |
| `aplicarSort` (linha 203) | envia `cust_id_status` + `sort` + `tab: 'pendencias'` fixo. Descarta `etapa` e `com_pendencia` |
| `aplicarEtapaFilter` (linha 216) | envia `etapa` + `com_pendencia`. Descarta `cust_id_status`, `sort`, `tab` |
| `aplicarComPendenciaFilter` (linha 223) | envia `etapa` + `com_pendencia`. Descarta `cust_id_status`, `sort`, `tab` |
| Onde cada controle renderiza | `aplicarCustIdFilter` linha 451, `aplicarEtapaFilter` 464, `aplicarComPendenciaFilter` 478 (aba **Empresas**); `aplicarSort` linha 613 (aba **Pendências**) |
| Suporte do backend | `CompanyController::index()` linhas 174-182 encadeia os três `when()` sem conflito; `filters` (linha 392) devolve os quatro |
| Cobertura de teste hoje | `EtapaFiltroListagemTest` tem `test_filtros_combinados_intersectam_etapa_e_pendencia`, mas **nenhum** teste combina `cust_id_status` com os novos |

**Sobre `tab`:** o `aplicarSort` é o único que hoje envia `tab`, e o faz com o valor fixo
`'pendencias'`. O montador único precisa preservar `tab` — omitir quebraria `aplicarSort`. Como
o controle de ordenação só renderiza dentro da aba Pendências, preservar a aba **corrente** em
vez do literal produz o mesmo resultado ali, e passa a manter a aba num refresh de página vindo
dos outros três controles. É melhoria deliberada, não efeito colateral — registrar como tal.
</measured_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralizar a montagem da query num único ponto (WR-03)</name>

  <read_first>
    - `resources/js/Pages/Companies/Index.jsx` linhas 186-230 — os estados de filtro
      (`custIdStatusFilter`, `sortFilter`, `etapaFilter`, `comPendenciaFilter`, `tab`) e os
      quatro handlers, inclusive o comentário da Fase 150 nas linhas 211-215 que faz a
      afirmação hoje falsa e precisa ser corrigido, não só ampliado.
    - `resources/js/Pages/Companies/Index.jsx` linhas 445-485 e 605-620 — os quatro pontos de
      chamada (`onChange`/`onClick`), para confirmar a assinatura que cada invólucro precisa
      continuar aceitando (`valor` string nos selects, `ativo` booleano no toggle).
    - `app/Http/Controllers/CompanyController.php` linhas 86-130 e 174-182 e 390-400 — o
      contrato do servidor: quais params são aceitos, a allow-list de `etapa` com o sentinela
      `sem_etapa` (D-22), e o formato da prop `filters` que o componente lê de volta.
    - `150-REVIEW.md` seção WR-03 — o achado e a forma de correção sugerida pelo revisor.
    - `.planning/learnings/painel-polos-status-e-meta.md` — a página React que some do manifest
      do Vite; contexto útil se o build se comportar de forma estranha.
  </read_first>

  <files>resources/js/Pages/Companies/Index.jsx</files>

  <behavior>
    - Com `cust_id_status` ativo, escolher uma etapa → a query enviada contém **os dois**
    - Com `etapa` ativa, escolher um `cust_id_status` → a query contém **os dois**
    - Com `etapa` e `cust_id_status` ativos, ligar o toggle de pendência → a query contém **os três**
    - Na aba Pendências, com `cust_id_status` ativo, trocar a ordenação → a query contém
      `cust_id_status`, `sort` e `tab`, além de `etapa`/`com_pendencia` se estiverem ativos
    - Limpar um filtro (valor vazio / toggle desligado) remove **só** aquele param, mantendo os demais
    - Sem nenhum filtro, a query é vazia — a visão padrão continua sem filtro nenhum
  </behavior>

  <action>
**1. Criar um único montador de query** — uma função `aplicarFiltros(overrides)` que monta os
params a partir dos filtros **atualmente ativos** (`cust_id_status`, `etapa`, `com_pendencia`,
`sort`, `tab`), aplica por cima os `overrides` recebidos, remove as chaves cujo valor for vazio
/ desligado, e faz o único `router.get(route('companies.index'), ...)` do arquivo, mantendo
`preserveState: true` e `preserveScroll: true` como hoje.

**2. Reescrever os quatro handlers como invólucros finos** que só chamam `aplicarFiltros` com o
override do seu próprio campo. As assinaturas públicas não mudam — os quatro pontos de chamada
no JSX continuam idênticos. `aplicarSort` deixa de fixar `tab: 'pendencias'` e passa a herdar a
aba corrente do montador (mesmo resultado, já que o controle só existe naquela aba).

**3. Corrigir o comentário das linhas 211-215.** A afirmação atual ("Cada handler PRESERVA o
outro filtro já ativo — D-23") era verdadeira só para o par etapa/pendência e por isso induziu
ao erro. O comentário novo deve dizer que **todos** os filtros ativos são reenviados a cada
mudança, por um montador único, e nomear o achado WR-03 do `150-REVIEW.md` como o motivo da
centralização. Manter a nota já existente de que o filtro é server-side e nunca `Array.filter`
de cliente (D-21), e o contra-exemplo `em_operacao`, que continua derivado no cliente de
propósito até a Fase 155.

**4. Não alterar mais nada do arquivo** — nem o JSX dos controles, nem a lógica das abas, nem o
`em_operacao` derivado, nem qualquer outra parte da página.

**5. `npm run build`** ao final (exigência do `CLAUDE.md`). Não commitar `public/build/` — é
gitignored e a VPS builda no servidor.
  </action>

  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/Phase150 --colors=never</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "router\.get(route('companies\.index')" resources/js/Pages/Companies/Index.jsx` = **1**
      (hoje são 4)
    - `grep -c "aplicarFiltros" resources/js/Pages/Companies/Index.jsx` ≥ 5 (a definição + os
      quatro invólucros)
    - `grep -n "aplicarCustIdFilter\|aplicarSort\|aplicarEtapaFilter\|aplicarComPendenciaFilter" resources/js/Pages/Companies/Index.jsx`
      segue mostrando os quatro pontos de chamada no JSX inalterados (linhas ~451, ~464, ~478, ~613)
    - `npm run build` termina com exit code 0 e regenera `public/build/manifest.json`
    - `phpunit tests/Feature/Phase150 --colors=never` verde (o backend não foi tocado, mas a
      suíte precisa continuar verde após o rebuild — as Feature tests dependem do manifest)
    - `git status --short` não lista nada dentro de `public/build/` como pendente de commit
  </acceptance_criteria>

  <done>Existe um único ponto no arquivo que monta a query de `companies.index`, e os quatro handlers passaram a delegar a ele sem mudar as próprias assinaturas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provar a combinação no backend e travar a centralização por gate estático</name>

  <read_first>
    - `tests/Feature/Phase150/EtapaFiltroListagemTest.php` — INTEIRO. Os 9 testes existentes,
      especialmente `test_filtros_combinados_intersectam_etapa_e_pendencia` (o molde do teste
      novo) e `test_prop_filters_reflete_etapa_e_com_pendencia_aplicados` (o molde da asserção
      sobre a prop `filters`). Os testes existentes não mudam.
    - `tests/Feature/Phase150/EtapaPendenciaParaleloTest.php` linhas 150-185 — o molde de gate
      estático desta fase (varredura de arquivo + `assertEmpty` com mensagem que nomeia o
      requisito violado). O gate desta task é sobre um arquivo `.jsx`, mas a forma é a mesma.
    - `app/Http/Controllers/CompanyController.php` linhas 86-130 — os valores aceitos de
      `cust_id_status`, para o teste usar um valor real e não um inventado.
    - `app/Models/Company.php` — `scopeComPendenciaAberta()` e `declararPendencia()`, usados
      pelos testes existentes para montar cenário.
  </read_first>

  <files>tests/Feature/Phase150/EtapaFiltroListagemTest.php</files>

  <behavior>
    - `GET /companies?cust_id_status=<válido>&etapa=<válida>&com_pendencia=1` devolve a
      INTERSEÇÃO dos três, não a de dois
    - A prop `filters` da mesma resposta ecoa os três valores aplicados simultaneamente
    - Gate estático: `Index.jsx` faz no máximo **um** `router.get(route('companies.index')`, e
      cada um dos quatro handlers de filtro delega ao montador único — nenhum monta params por
      conta própria
  </behavior>

  <action>
**1. Acrescentar os dois testes de combinação tripla** a `EtapaFiltroListagemTest.php`, seguindo
o molde do teste de combinação já existente: montar empresas que satisfaçam subconjuntos
diferentes dos três critérios e afirmar que só a que satisfaz **os três** aparece; e afirmar que
a prop `filters` devolve os três valores. É a prova de que o contrato que o cliente passou a
depender existe de verdade no servidor.

**2. Acrescentar o gate estático** ao mesmo arquivo: ler
`resources/js/Pages/Companies/Index.jsx` e afirmar (a) que a contagem de
`router.get(route('companies.index')` é no máximo 1, e (b) que cada um dos quatro nomes de
handler aparece delegando ao montador único. A mensagem de falha deve nomear WR-03 e explicar o
dano concreto ("um handler voltou a montar params por conta própria — escolher um filtro vai
apagar os outros em silêncio"), nunca "assertion failed".

**3. Docblock do gate:** registrar por que ele existe (o comentário do código-fonte já tinha
afirmado a preservação e estava errado — o gate é a versão executável dessa afirmação) e que ele
é estático por natureza: prova a FORMA do arquivo, não o comportamento no navegador, que é o que
o checkpoint da Task 3 cobre.

**Não fazer:** não alterar os 9 testes existentes; não tocar `CompanyController`; não usar
`git add -A`.
  </action>

  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Unit/Phase150 tests/Feature/Phase150 --colors=never</automated>
  </verify>

  <acceptance_criteria>
    - Os dois testes novos de combinação tripla passam
    - O gate estático passa contra o `Index.jsx` já centralizado e, provado dirigido, **FALHA**
      quando um dos handlers é revertido temporariamente para montar params por conta própria —
      evidência colada no SUMMARY, reversão desfeita com `git status --short` limpo
    - Os 9 testes pré-existentes de `EtapaFiltroListagemTest` seguem verdes (o `git diff` do
      arquivo mostra apenas linhas ADICIONADAS)
    - Suíte da fase verde: `phpunit tests/Unit/Phase150 tests/Feature/Phase150`
    - Baseline `/companies` intacta: `phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php` = 24/24
  </acceptance_criteria>

  <done>A combinação dos três filtros está provada no servidor e a centralização do cliente está travada por gate que já foi visto falhando.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana — combinar filtros na tela real</name>

  <read_first>
    - `.planning/phases/150-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/150-07-SUMMARY.md`
      § dos dois achados da verificação — inclusive o `.env` do worktree que apontava para o
      build do OUTRO checkout e produzia tela branca. Se a tela vier em branco, é o primeiro
      lugar a olhar: `APP_URL`/`ASSET_URL` precisam apontar para `/ecf_fluxo_entrada/public`.
    - `resources/js/Pages/Companies/Index.jsx` — os quatro handlers já centralizados pela Task 1,
      para saber exatamente qual controle da tela corresponde a qual handler.
    - `./CLAUDE.md` — a régua que este passo existe para honrar: `npm run build` já rodou na
      Task 1, e grep no bundle prova o DEPLOY, nunca que a MUDANÇA funciona.
  </read_first>

  <files>.planning/phases/150-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/150-11-SUMMARY.md</files>

  <action>
Apresentar ao usuário o roteiro de `<how-to-verify>` abaixo, na ordem, e aguardar o veredito.
Antes de apresentar, confirmar que o servidor local está servindo ESTE worktree (não o
`ecf_admin`) e que `public/build/manifest.json` é o gerado pela Task 1 — o achado de 150-07 foi
exatamente um `.env` apontando para o build do outro checkout.

Nenhuma correção é aplicada dentro deste passo. Se o usuário reportar divergência, registrar
como deviation no `150-11-SUMMARY.md` — com o passo, o esperado e o observado — e só então
decidir a correção, nunca ajustando em silêncio durante a verificação.
  </action>

  <what-built>
Os quatro handlers de filtro de `/companies` passaram a delegar a um montador único que reenvia
todos os filtros ativos (`cust_id_status`, `etapa`, `com_pendencia`, `sort`, `tab`) a cada
mudança. O backend já suportava a combinação; a prova automatizada cobre o servidor
(interseção tripla) e a forma do arquivo (gate estático). O que nenhuma das duas prova é o
round-trip real no navegador — que é exatamente onde a Fase 150 já encontrou um bug de ambiente
antes.
  </what-built>

  <how-to-verify>
1. Abrir `/companies` no navegador. **Esperado:** a visão padrão está inalterada — nenhum filtro
   pré-aplicado, a contagem de empresas é a mesma de sempre.
2. Na aba **Empresas**, aplicar o filtro de **Cust ID** (qualquer valor que devolva resultado).
   **Esperado:** a lista estreita e a URL passa a conter `cust_id_status=`.
3. **Sem limpar o anterior**, escolher **"Sem etapa (legado)"** no seletor de etapa.
   **Esperado:** a URL contém **os dois** params (`cust_id_status=` E `etapa=sem_etapa`), e o
   seletor de Cust ID continua mostrando o valor escolhido no passo 2. É este o passo que
   falhava antes.
4. **Sem limpar nada**, ligar o **toggle de pendência**. **Esperado:** a URL contém **os três**
   params, e os dois controles anteriores seguem preenchidos.
5. Limpar **só** o filtro de etapa (voltar o seletor para a opção vazia). **Esperado:** `etapa=`
   some da URL, `cust_id_status=` e `com_pendencia=` permanecem.
6. Ir para a aba **Pendências** e trocar a **ordenação**. **Esperado:** a URL mantém
   `cust_id_status=`/`com_pendencia=` além de `sort=` e `tab=pendencias`, e a aba não pula de
   volta para Empresas.
7. Dar **F5** com filtros aplicados. **Esperado:** a tela volta com os mesmos filtros e na mesma
   aba.
  </how-to-verify>

  <acceptance_criteria>
    - Passos 3 e 4 confirmados pelo usuário: combinar filtros NÃO apaga os anteriores, e a URL
      carrega os params combinados
    - Passo 1 confirmado: a visão padrão de `/companies` está inalterada
    - Passo 5 confirmado: limpar um filtro remove só aquele
    - Passo 6 confirmado: ordenar na aba Pendências não apaga os filtros nem troca de aba
    - Qualquer divergência é descrita pelo usuário e vira deviation registrada no SUMMARY, nunca
      resolvida em silêncio
  </acceptance_criteria>

  <resume-signal>Responder "aprovado" ou descrever exatamente qual passo divergiu e o que apareceu na tela</resume-signal>

  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/Phase150/EtapaFiltroListagemTest.php --colors=never</automated>
    <human-check>Passos 1 a 7 de `<how-to-verify>` conferidos no navegador e aprovados pelo usuário</human-check>
  </verify>

  <done>O usuário confirmou no navegador que combinar filtro de etapa, pendência e Cust ID preserva os três, que a visão padrão segue inalterada e que limpar um filtro remove só aquele.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| navegador → `GET /companies` | Query params de filtro são entrada não confiável e já passam por allow-list no servidor |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-150-36 | Information Disclosure (por omissão) | Filtro que apaga outro em silêncio faz o operador ler uma lista mais ampla do que pensa estar lendo, e agir sobre ela | mitigate | Montador único reenvia todos os filtros ativos; gate estático impede reintrodução |
| T-150-37 | Tampering | Param de filtro forjado na URL (`?etapa=<arbitrário>`) | accept (já mitigado) | `CompanyController::index()` valida contra `Company::ETAPAS` + sentinela `sem_etapa`; valor fora do domínio vira `null`. Coberto por `test_filtro_etapa_invalida_devolve_mesmo_payload_que_sem_filtro` |
| T-150-38 | Elevation of Privilege | Filtro não altera escopo de visibilidade — o recorte de `/companies` é definido antes dos `when()` de filtro | accept | Nenhuma mudança de escopo neste plano; baseline `/companies` (24/24) é o guarda |
| T-150-39 | Denial of Service | Combinação dos três filtros gera query sem índice útil | accept | `etapa` é indexada (150-02) e `pendencia_aberta` também (150-04); base de ~500 empresas |

Sem instalação de pacote npm/composer neste plano — `npm run build` usa o `node_modules/` já
existente, então `T-150-SC` (legitimidade de pacote) não se aplica e nenhum checkpoint de
instalação é criado. **Se por qualquer motivo for necessário `npm install`, PARAR** e reportar:
instalação de pacote exige o protocolo de legitimidade, que este plano não carrega.
</threat_model>

<verification>
- `phpunit tests/Unit/Phase150 tests/Feature/Phase150 --colors=never` verde
- `phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php --colors=never` = 24/24
- `npm run build` exit 0
- Checkpoint humano aprovado (Task 3)
- ⚠️ NÃO rodar `php artisan test` nem `--testsuite=Feature`; NÃO rodar `npm install`
</verification>

<success_criteria>
Combinar filtro de etapa, de pendência e de Cust ID em `/companies` preserva os três — provado
no servidor por teste de interseção tripla, travado no cliente por gate estático, e confirmado
por um humano no round-trip real do navegador.
</success_criteria>

<output>
Criar `.planning/phases/150-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/150-11-SUMMARY.md`.

Commit por caminho explícito (nunca `git add -A` / `git add .`; **não** commitar `public/build/`),
mensagem em pt-BR:
`fix(150-11): filtros de /companies deixam de se apagar entre si (WR-03)`
</output>
