---
phase: 102-desempenho-oficial-por-competencia-v18-0
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - app/Services/DesempenhoScoreService.php
  - app/Http/Controllers/NpsController.php
  - tests/Feature/V18/DesempenhoMetadadosCacheTest.php
  - tests/Feature/V16/BonusDualPathRegressaoTest.php
  - tests/Feature/V16/DesempenhoElegibilidadeTest.php
  - tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php
autonomous: true
requirements: [BON-02, BON-04, BON-05]
must_haves:
  truths:
    - "O retorno de compute() inclui periodo (current/baseline/mode/comparison_mode) e bonus (competence_month, payment_month) — competência/pagamento derivados de $mes para qualquer mês fechado"
    - "score único preservado — nenhum score por marketplace; score_status/financial_metrics_eligible/empresas_unicas intactos (invariante v17)"
    - "chave de cache bumpada v4->v5 e inclui o period_key — operacional (current_month) e oficial (mês fechado) do mesmo mês não colidem"
    - "a chave é construída por um helper público único; NpsController::bustarCacheDoBonus usa o helper (não hardcode) e continua bustando a chave certa"
    - "computeNpsMedio permanece zero-diff (dual-path Fase 80 intacto); BonusDualPathRegressaoTest e DesempenhoElegibilidadeTest verdes com a chave v5"
  artifacts:
    - path: "app/Services/DesempenhoScoreService.php"
      provides: "metadados periodo/bonus + helper cacheKey v5 + computeCached com period_key"
      contains: "desempenho.compute.v5"
    - path: "tests/Feature/V18/DesempenhoMetadadosCacheTest.php"
      provides: "prova de shape periodo/bonus + separação de chave operacional vs oficial"
  key_links:
    - from: "app/Http/Controllers/NpsController.php"
      to: "app/Services/DesempenhoScoreService.php"
      via: "bustarCacheDoBonus -> cacheKey(userId, mes)"
      pattern: "cacheKey\\("
    - from: "app/Services/DesempenhoScoreService.php"
      to: "compute() retorno"
      via: "bloco periodo/bonus"
      pattern: "'competence_month'"
---

<objective>
Fechar a camada de contrato e invalidação em cima do núcleo migrado no 102-01: expor `periodo`/`bonus` no retorno (BON-04), bumpar a chave de cache v4→v5 com `period_key` (evitando colisão operacional×oficial e servir número velho), preservar as invariantes de elegibilidade da v17 (BON-05) e validar a competência de bônus (BON-02) com a regressão do dual-path intacta.

Purpose: garantir que o número novo APAREÇA em prod (bump de cache) sem quebrar a invalidação de bônus da sessão NPS, e que nada da régua de elegibilidade v17/dual-path v16 regrida.
Output: metadados no shape + helper de cache único + NpsController alinhado + suítes de regressão verdes + guia de deploy/validação no SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-desempenho-oficial-por-competencia-v18-0/102-RESEARCH.md
@.planning/phases/102-desempenho-oficial-por-competencia-v18-0/102-01-SUMMARY.md

@app/Services/DesempenhoScoreService.php
@tests/Feature/V16/BonusDualPathRegressaoTest.php

<interfaces>
<!-- Já existente após 102-01. -->

Chave de cache atual (a bumpar): sprintf('desempenho.compute.v4.%d.%s', $user->id, $mes->format('Y-m'))
  em DesempenhoScoreService::computeCached() (comentários de bump v1..v4 no arquivo).

Consumidores que hardcodam a chave (precisam do bump coordenado):
  - app/Http/Controllers/NpsController.php ~L1662 (bustarCacheDoBonus) — Cache::forget da chave;
    docblock diz "NUNCA hardcode a versão às cegas" -> trocar por helper.
  - tests/Feature/V16/BonusDualPathRegressaoTest.php ~L532-554 (assert grava v4)
  - tests/Feature/V16/DesempenhoElegibilidadeTest.php ~L352-367 (assert grava v4)
  - tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php ~L241,L340 (seed da chave v4) — SESSÃO PARALELA

periodo/bonus alvo (BON-04) — do resolver + derivado de $mes (Assumption A2 do RESEARCH):
  'periodo' => [ current_start, current_end, baseline_start, baseline_end, mode, comparison_mode ]
  'bonus'   => [ 'competence_month' => $mes->format('Y-m') (quando mês fechado),
                 'payment_month'    => $mes->copy()->addMonthNoOverflow()->format('Y-m') ]
  Manter periodo_meta existente lado a lado (transição — remoção fica pra Fase 104).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Metadados periodo/bonus + cache v5 com period_key + helper público + invalidação NPS</name>
  <files>app/Services/DesempenhoScoreService.php, app/Http/Controllers/NpsController.php, tests/Feature/V18/DesempenhoMetadadosCacheTest.php, tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php</files>
  <behavior>
    - compute() retorna 'periodo' (current/baseline/mode/comparison_mode do $periodo) e 'bonus'
      (competence_month/payment_month derivados de $mes) — presentes para qualquer mês fechado; periodo_meta preservado (BON-04).
    - computeCached grava sob 'desempenho.compute.v5.{id}.{period_key}': mês corrente -> 'current_month',
      mês fechado -> 'Y-m'. Operacional e oficial do mesmo mês NÃO colidem (chaves distintas).
    - Helper público cacheKey(int $userId, Carbon $mes): string devolve a mesma chave usada por computeCached.
    - NpsController::bustarCacheDoBonus usa o helper e, ao invalidar resposta de mês fechado, remove a
      chave certa (o teste Phase96 de mês fechado continua verde após o bump).
  </behavior>
  <action>
    Em compute(): acrescentar ao array de retorno os blocos 'periodo' (mapear de `$periodo`:
    current_start/current_end/baseline_start/baseline_end/mode/comparison_mode) e 'bonus'
    (competence_month = `$mes->format('Y-m')` e payment_month = `$mes->copy()->addMonthNoOverflow()->format('Y-m')`
    quando NÃO for mês em curso; quando em curso, manter null nos dois — coerente com o resolver). Manter
    `periodo_meta` como está (transição — não remover). NÃO alterar chaves existentes (BON-04/BON-05).

    Adicionar helper público `cacheKey(int $userId, Carbon $mes): string` que devolve
    `sprintf('desempenho.compute.v5.%d.%s', $userId, $periodKey)` onde `$periodKey` = 'current_month' se
    `$mes` for o mês corrente, senão `$mes->format('Y-m')` (mesma regra de resolvePeriodo). computeCached
    passa a usar o helper e aceitar `?array $periodoOverride = null` (repassando a compute()); o TTL segue
    a lógica atual (mês fechado 7d / corrente 10min). Substituir o comentário de bump: acrescentar o bloco
    v4→v5 explicando que a régua de baseline mudou (calendário → janela-de-mesmo-tamanho) e a fonte da
    margem mudou (AdmanMetricDiffService), invalidando as chaves v4; incluir period_key evita colisão
    operacional×oficial (D-02).

    NpsController::bustarCacheDoBonus (~L1649): trocar o `sprintf('desempenho.compute.v4...')` por
    `app(DesempenhoScoreService::class)->cacheKey($userId, $mesCompletado)` (ou injeção existente). Mínimo
    e cirúrgico — sinalizar no SUMMARY que é toque em arquivo da sessão paralela NPS (commit por caminho
    explícito). Atualizar o docblock que menciona "v4".

    Phase96/NpsInvalidacaoRespostaTest.php (~L241,L340): trocar o seed hardcoded `desempenho.compute.v4...`
    por `desempenho.compute.v5...` (mês fechado 2026-06 → period_key '2026-06', a chave continua batendo).

    Novo teste tests/Feature/V18/DesempenhoMetadadosCacheTest.php: (a) shape periodo/bonus correto para
    mês fechado (competence/payment) e nulls no mês corrente; (b) computeCached grava v5 e a chave inclui
    period_key; (c) chave operacional (current_month) ≠ chave do mesmo mês fechado (sem colisão);
    (d) cacheKey() == chave gravada. User sem carteira para cortar cedo (zero HTTP), como nos testes de cache existentes.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit --filter DesempenhoMetadadosCacheTest tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php</automated>
  </verify>
  <done>periodo/bonus no shape; chave v5 com period_key via helper público; NpsController e Phase96 alinhados ao v5; testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invariantes v17 + zero-diff do NPS + regressão v16 + competência de bônus</name>
  <files>tests/Feature/V18/DesempenhoMetadadosCacheTest.php, tests/Feature/V16/BonusDualPathRegressaoTest.php, tests/Feature/V16/DesempenhoElegibilidadeTest.php</files>
  <behavior>
    - score_status/financial_metrics_eligible/empresas_unicas/vinculos_* permanecem calculados como na v17;
      só-Shopee continua blocked com nota_final=null; score único (sem score por marketplace) (BON-05).
    - computeNpsMedio devolve exatamente o mesmo valor de antes (dual-path Fase 80 intacto) — provado por
      reflection nos cenários do BonusDualPathRegressaoTest.
    - computeOficial em jul/2026 rotula bonus.competence_month='2026-06'/payment_month='2026-07' e o
      resultado é o número da competência fechada (BON-02).
    - BonusDualPathRegressaoTest e DesempenhoElegibilidadeTest verdes com a chave v5 (ajuste cirúrgico v4→v5).
  </behavior>
  <action>
    BonusDualPathRegressaoTest.php e DesempenhoElegibilidadeTest.php: trocar as asserções de chave
    `desempenho.compute.v4` por `desempenho.compute.v5` (ajuste cirúrgico, como na Fase 91 v3→v4). Os
    testes de computeNpsMedio por reflection NÃO mudam — se algum falhar, é regressão real do dual-path,
    investigar (não relaxar a asserção).

    Acrescentar ao DesempenhoMetadadosCacheTest asserções de invariante (BON-05): user só-Shopee →
    score_status='blocked', nota_final=null; user misto → score único, sem chave de score por marketplace;
    empresas_unicas/vinculos_* presentes e coerentes. E uma asserção de zero-diff do NPS: computar
    computeNpsMedio (reflection) num cenário simples e comparar com o valor esperado da régua NPS (mesma
    fórmula da v17), provando que a migração de período não tocou o NPS.

    Adicionar teste de competência (BON-02): computeOficial(User) com now=2026-07-20 → bonus.competence_month
    '2026-06', payment_month '2026-07', e nota_final = a da competência fechada (mesmo número de
    compute($u, Carbon('2026-06-01'))).

    NÃO alterar o command ConsolidarMesDesempenho: ele chama compute($user, $mes) que resolve period_key
    'Y-m' automaticamente (janela-de-mesmo-tamanho) — a competência fechada já sai pela régua nova sem
    mudança de código (validar via ConsolidarMesDesempenhoCommandTest recalibrado no 102-01).

    **ISOLAMENTO HTTP OBRIGATÓRIO (fix plan-checker — BLOCKER, mesma regra do 102-01):** qualquer teste
    deste plano que alcance compute()/computeCached() com empresa financeiramente elegível (BonusDualPath,
    DesempenhoElegibilidade, DesempenhoMetadadosCache, o teste de competência BON-02) DEVE ter
    `Http::preventStrayRequests()` no setUp e `Http::fake([...])` vazio/404-style forçando calculated_fallback
    determinístico — pois computeVarMargem agora chama a API real da Adman via AdmanMetricDiffService.
    Nenhuma requisição real à Adman em teste; o número do bônus vem do fixture/régua, nunca de prod.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/V16/BonusDualPathRegressaoTest.php tests/Feature/V16/DesempenhoElegibilidadeTest.php --filter . && C:\xampp\php\php.exe vendor/bin/phpunit --filter DesempenhoMetadadosCacheTest</automated>
  </verify>
  <done>Invariantes v17 provadas; NPS zero-diff; competência jun/2026 rotulada; regressão v16 verde com chave v5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Toque mínimo no NpsController (sessão paralela) apenas troca a construção da chave de cache por um helper — sem novo input/endpoint/auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-03 | Tampering/Information disclosure | Redis serve bônus velho (chave v4) após o deploy da régua nova | mitigate | Bump v4→v5; as chaves v4 viram órfãs e expiram por TTL (sem cache:clear) |
| T-102-04 | Tampering | Colisão de chave operacional×oficial do mesmo mês servindo metadados errados | mitigate | period_key na chave separa current_month de Y-m |
| T-102-05 | Repudiation/Integrity | Invalidação de NPS deixa de bustar o bônus (chave defasada) → número stale sem sinal | mitigate | NpsController usa o helper público único; teste Phase96 de mês fechado cobre o bust |
| T-102-SC | Tampering | Instalação de pacotes | accept | Nenhuma instalação nesta fase |
</threat_model>

<verification>
- `C:\xampp\php\php.exe vendor/bin/phpunit --filter DesempenhoMetadadosCacheTest` verde.
- `C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/V16/BonusDualPathRegressaoTest.php tests/Feature/V16/DesempenhoElegibilidadeTest.php` verde.
- `C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php` verde.
- `git status` antes do commit; commitar apenas os caminhos explícitos deste plano (sessão paralela NPS ativa).
</verification>

<success_criteria>
- BON-04: retorno inclui periodo + bonus; score único preservado.
- BON-05: score_status/financial_metrics_eligible/blocked intactos; computeNpsMedio zero-diff.
- BON-02: competência jun/2026 rotulada e paga em jul/2026 (computeOficial + command sem mudança de código).
- Cache v5 com period_key; NpsController alinhado; regressão v16 verde.
</success_criteria>

<output>
Create `.planning/phases/102-desempenho-oficial-por-competencia-v18-0/102-02-SUMMARY.md` when done.
Documentar no SUMMARY (sem criar task de código):
  - Junho/2026 NÃO tem snapshot mensal (mes_referencia NULL nos 226 diários) → cai no COMPUTE LIVE do PerformanceController; após o deploy v18 junho computa ao vivo pela régua nova, sem número velho preso.
  - Congelar junho é escolha de deploy-time: `php artisan desempenho:consolidar-mes --mes=2026-06` (idempotente) — fora do escopo de código desta fase.
  - Wiring do PerformanceController para chamar computeOficial no contexto "Bônus atual" é Fase 104 (UI); o backend já sustenta.
  - Follow-up recomendado (não obrigatório): estender WarmDesempenhoCache para aquecer o período oficial (Pitfall 3 do RESEARCH) e medir tempo do ConsolidarMesDesempenho sob o AdmanMetricDiffService live-read (Pitfall 2).
  - Validação prod pré-planejada: comparar bônus antigo (v17) vs novo (v18) por profissional no modo oficial antes de comunicar.
</output>
