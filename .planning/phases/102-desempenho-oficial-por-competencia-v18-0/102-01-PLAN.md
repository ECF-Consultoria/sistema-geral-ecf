---
phase: 102-desempenho-oficial-por-competencia-v18-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/DesempenhoScoreService.php
  - tests/Feature/V18/DesempenhoPeriodoOficialTest.php
  - tests/Feature/Phase74/DesempenhoScoreServiceTest.php
  - tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php
autonomous: true
requirements: [BON-01, BON-02, BON-03]
must_haves:
  truths:
    - "computeOficial(User) resolve o último mês fechado: em jul/2026 a competência é jun/2026, janela atual 01/06..30/06 e baseline 02/05..31/05"
    - "computeVarFaturamento e computeVarMargem usam as janelas do MetricPeriodResolver — zero now()/startOfMonth/subMonth inline para definir período de comparação"
    - "var_margem_pct vem de AdmanMetricDiffService::compute(company,periodo)->metrics['contribution_margin_pct']['diff_pct'] quando o gate previous_equal_length_window bate; fallback calculado marcado quando o diff nativo está ausente"
    - "os ~140 linhas de guards duplicados (margem_dias + dias-comuns por dia-do-mês) saem do DesempenhoScoreService — a fonte única passa a ser AdmanMetricDiffService"
    - "a âncora Carlos e a suíte Phase74 passam com fixtures densos e golden number derivado da régua nova (não preservado da v17)"
  artifacts:
    - path: "app/Services/DesempenhoScoreService.php"
      provides: "computeOficial + resolvePeriodo + janelas via resolver + margem via diff service"
      contains: "computeOficial"
    - path: "tests/Feature/V18/DesempenhoPeriodoOficialTest.php"
      provides: "prova de competência fechada + gate adman_diff + faturamento por janela"
  key_links:
    - from: "app/Services/DesempenhoScoreService.php"
      to: "app/Services/Metrics/MetricPeriodResolver.php"
      via: "resolvePeriodo() -> resolve(['period_key' => ...])"
      pattern: "periodResolver->resolve"
    - from: "app/Services/DesempenhoScoreService.php"
      to: "app/Services/Metrics/AdmanMetricDiffService.php"
      via: "computeVarMargem -> compute(company, periodo)"
      pattern: "admanDiffService->compute"
---

<objective>
Migrar o núcleo de cálculo do `DesempenhoScoreService` para consumir o `MetricPeriodResolver` (janelas) e o `AdmanMetricDiffService` (variação de margem pronta da Adman), introduzindo o modo oficial de bônus por competência fechada. Esta é a mudança que ALTERA o número pago — rigor máximo, TDD, golden numbers derivados da régua (nunca escolhidos para passar).

Purpose: fechar BON-01 (janelas via resolver), BON-02 (competência do mês fechado — julho paga junho) e BON-03 (margem via `percentageMargin.diff` da Adman com fallback marcado), eliminando a duplicação intencional de guards deixada pela Fase 101.
Output: `DesempenhoScoreService` reescrito no cálculo de faturamento/margem + suíte V18 nova + suíte Phase74 recalibrada com fixtures densos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-desempenho-oficial-por-competencia-v18-0/102-RESEARCH.md

@app/Services/DesempenhoScoreService.php
@app/Services/Metrics/MetricPeriodResolver.php
@app/Services/Metrics/AdmanMetricDiffService.php
@tests/Feature/Phase74/DesempenhoScoreServiceTest.php

<interfaces>
<!-- Contratos que o executor consome — não explorar o codebase, usar direto. -->

MetricPeriodResolver::resolve(array $filtros): array
  period_key aceito: 'current_month' | 'last_closed_month' | 'YYYY-MM' | 'custom'
  Retorno (14 chaves, sempre strings Y-m-d nas datas):
    mode: 'operational'|'official_bonus'|'closed_period'
    period_key, current_start, current_end, baseline_start, baseline_end,
    days_count, comparison_mode ('same_interval_previous_month'|'previous_equal_length_window'),
    timezone, data_fresh_until, bonus_payment_month (?string 'Y-m'), bonus_competence_month (?string 'Y-m'),
    is_current_month (bool), is_closed (bool)
  - 'last_closed_month' -> mode='official_bonus', comparison_mode='previous_equal_length_window',
    bonus_competence_month/bonus_payment_month populados.
  - 'YYYY-MM' (mês específico) -> mode='closed_period', comparison_mode='previous_equal_length_window',
    bonus_*=null (bônus oficial é SÓ last_closed_month).
  - 'current_month' -> mode='operational', comparison_mode='same_interval_previous_month'.

AdmanMetricDiffService::compute(Company $company, array $periodo): array
  $periodo = objeto INTEIRO do resolver (precisa de comparison_mode/current_*/baseline_*).
  Retorno:
    metrics: [
      'revenue'                   => ['value'=>?float,'diff_pct'=>?float,'diff_source'=>?string],
      'contribution_margin_value' => [...],
      'contribution_margin_pct'   => ['value'=>?float,'diff_pct'=>?float,'diff_source'=>?string],
    ]
    quality: ['status'=>'complete'|'partial'|'missing', ...]
  diff_source ∈ {'adman_diff','calculated_fallback',null}. Gate: usa 'adman_diff' só quando
  comparison_mode==='previous_equal_length_window' E .diff nativo != null; senão fallback calculado
  sobre AdmanMetric local (guard offset-dias-comuns interno). É LIVE-READ (HTTP fetchPerformance) —
  fail-open (nunca lança).

DesempenhoScoreService::compute() shape atual (PRESERVAR chaves existentes; ver arquivo).
  componentes.var_margem_pct / var_faturamento_pct são a saída dos métodos privados alvo.
  score_status/empresas_unicas/vinculos_*/componentes_disponiveis PERMANECEM (invariante v17).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolver + modo oficial + faturamento por janela + margem via diff service</name>
  <files>app/Services/DesempenhoScoreService.php, tests/Feature/V18/DesempenhoPeriodoOficialTest.php</files>
  <behavior>
    - computeOficial(User) com now congelado 2026-07-20 14:00 → resolve last_closed_month:
      competência jun/2026, current_start=2026-06-01, current_end=2026-06-30,
      baseline_start=2026-05-02, baseline_end=2026-05-31 (BON-02).
    - compute($u, Carbon('2026-06-01')) (now=2026-07-20) sem override → resolvePeriodo devolve
      period_key='2026-06' (closed_period), MESMAS janelas de current/baseline que computeOficial;
      compute($u, Carbon('2026-07-01')) (mês corrente) → current_month/operational.
    - var_margem_pct: com Http::fake devolvendo payload Adman com `.diff` E comparison_mode
      previous_equal_length_window → diff_source='adman_diff' e usa o diff nativo (BON-03).
    - var_margem_pct: com Http::fake vazio (sem `.diff`) → cai em calculated_fallback lendo
      AdmanMetric local (fixtures densos diários) — média das % por empresa.
    - var_faturamento_pct: média das variações por empresa calculada sobre as janelas do $periodo
      (fixtures densos cobrindo current e baseline), empresa nova/`caseFor none` excluídas (BON-01).
  </behavior>
  <action>
    Injetar `MetricPeriodResolver` e `AdmanMetricDiffService` no construtor (manter as 3 deps atuais).

    Assinatura (decisão travada, Opção B): `compute(User $user, Carbon $mesReferencia, ?array $periodoOverride = null)`.
    Adicionar privado `resolvePeriodo(Carbon $mes): array` — se `$mes` == mês corrente devolve
    `resolve(['period_key'=>'current_month'])`, senão `resolve(['period_key'=>$mes->format('Y-m')])`.
    Adicionar público `computeOficial(User $user): array` — resolve `last_closed_month`, deriva
    `$mes = Carbon::parse($periodo['current_start'])->startOfMonth()` e chama
    `compute($user, $mes, $periodo)`. Em `compute()`, `$periodo = $periodoOverride ?? $this->resolvePeriodo($mes)`.

    computeVarFaturamento: receber `array $periodo` e SUBSTITUIR o bloco de janelas inline
    (linhas ~705-720: `$hoje`/`$ehMesEmCurso`/`$inicioMes`/`$fimMes`/`$inicioAnter`/`$fimAnter`) por
    `Carbon::parse($periodo['current_start'])` etc. O ramo ML-first (`readForCompany`) e o fallback
    Adman passam a usar essas janelas. Preservar o filtro "empresa nova" (companies.created_at) e o
    filtro `caseFor !== 'none'`. Manter `empresas_com_baseline`. (BON-01, per D-01/D-05.)

    computeVarMargem: receber `array $periodo` e SUBSTITUIR o corpo inteiro (linhas ~819-971) por
    delegação por empresa a `$this->admanDiffService->compute($company, $periodo)`, coletando
    `metrics['contribution_margin_pct']['diff_pct']` (ignora null) e retornando `round(avg,2)` ou null.
    REMOVER integralmente: bloco de janelas inline (~819-838), queries `margemAtual`/`margemAnterior`
    + guard margem_dias (~845-859), e o loop com guard dias-comuns por dia-do-mês (~874-971). A fonte
    única de guards passa a ser AdmanMetricDiffService (BON-03, per D-04). Comentar em pt-BR que
    `contribution_margin_pct` (chave do diff service) = `percentageMargin` da Adman (≠ AdmanMetric.contribution_margin R$).

    NÃO alterar: computeNpsMedio/notasPorAtribuicao/notasLegado (zero-diff), computeScoreStatus,
    computeUniverso, réguas, classificarFaixa, promoção DESEMP-08, shapeSemCarteira.

    Testes (tests/Feature/V18/DesempenhoPeriodoOficialTest.php): usar RefreshDatabase + PRAGMA
    foreign_keys + Carbon::setTestNow('2026-07-20 14:00') + `Http::fake()` para controlar o path do
    diff service (payload com `.diff` para provar o gate adman_diff; fake vazio para provar o
    calculated_fallback sobre AdmanMetric local). Fixtures DENSOS: uma linha AdmanMetric por dia
    cobrindo current e baseline (imunes ao desalinhamento de offset — Pitfall 1 do RESEARCH).
    Reutilizar o padrão de stub `MetricsProviderFactory` do Phase74 quando precisar do ramo ML.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit --filter DesempenhoPeriodoOficialTest</automated>
  </verify>
  <done>Suíte V18 verde: computeOficial devolve competência jun/2026 com janelas 01/06..30/06 e 02/05..31/05; margem usa adman_diff sob o gate e calculated_fallback quando ausente; faturamento média por janela; guards inline removidos (git diff prova a remoção de ~140 linhas).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recalibrar âncora Carlos + suíte Phase74 (fixtures densos, golden derivado da régua)</name>
  <files>tests/Feature/Phase74/DesempenhoScoreServiceTest.php, tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php</files>
  <behavior>
    - A âncora Carlos passa em modo mês-fechado (now=2026-08-01, compute(2026-07-01) → period_key '2026-07')
      com var_margem_pct/var_faturamento_pct/nota_final derivados da baseline janela-de-mesmo-tamanho
      (não mais 4.08 preservado da v17) — a aritmética explícita fica no comentário do teste.
    - Todos os testes que hoje usam o padrão "1 linha no dia 15" (mockAdmanRevenueMargem) passam com
      fixtures diários densos cobrindo as janelas current e baseline sob a régua nova.
  </behavior>
  <action>
    Reescrever `mockAdmanRevenueMargem` (ou adicionar helper `mockAdmanDiario`) para inserir uma linha
    AdmanMetric por DIA nas janelas current e baseline resolvidas — não mais uma única linha no dia 15.
    Motivo (Pitfall 1 do RESEARCH): a baseline janela-de-mesmo-tamanho não começa no dia 1, então
    fixtures esparsos caem na interseção vazia de dias-comuns do AdmanMetricDiffService → diff_pct=null.

    Recalcular os golden numbers de CADA teste a partir da régua nova (soma da janela current vs soma
    da baseline janela-de-mesmo-tamanho), documentando a conta no comentário. Para a âncora Carlos:
    derivar var_faturamento_pct e var_margem_pct dos totais densos e recomputar nota_final/faixa pela
    fórmula existente (NPS + reguaFaturamento + reguaMargem)/3. NUNCA ajustar o esperado só pra passar —
    o valor vem da régua. Atualizar o nome do método de teste da âncora se o número mudar de faixa.

    Ajustar os testes que dependem do stub ML (`test_var_margem_usa_adman_como_fonte_canonica`,
    `test_provider_ml_first_com_adman_fallback`): como as janelas agora vêm do resolver e a margem
    passa pelo AdmanMetricDiffService, garantir `Http::fake()` para forçar o calculated_fallback sobre
    AdmanMetric local, e configurar revenue do stub para as janelas efetivas (baseline pode cruzar o
    mês anterior). O teste do lag de margem (`..._dias_finais_sem_margem`) deve continuar provando que
    o guard offset-dias-comuns do diff service recorta simetricamente (comportamento equivalente ao fix
    Tomelin, agora no diff service).

    Aplicar o mesmo tratamento de fixtures densos em ConsolidarMesDesempenhoCommandTest (mesmo padrão
    esparso). Rodar também ComparacaoContextualBlockedTest para confirmar (oráculo recomputa; deve
    passar sem edição — se falhar por mudança de valor, ajustar só o setup de fixture, não a asserção
    de blocked).
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/Phase74 tests/Feature/V16/ComparacaoContextualBlockedTest.php</automated>
  </verify>
  <done>Phase74 inteira verde com fixtures densos; âncora Carlos com golden derivado da régua nova e aritmética documentada; ComparacaoContextualBlockedTest verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Refatoração interna de cálculo consumido por rotas já protegidas por `permission:core.performance`. Sem input de usuário novo, endpoint novo ou mudança de auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering/Information disclosure | Baseline errada (janela de tamanho diferente) produz bônus incorreto | mitigate | Fonte única de janela = MetricPeriodResolver (Fase 100, testado); testes V18 fixam as janelas de competência exatas |
| T-102-02 | Tampering | Guards de dias-comuns divergentes entre services reintroduzem bug já cicatrizado | mitigate | Remover os guards duplicados do DesempenhoScoreService; fonte única = AdmanMetricDiffService (fallback marcado) |
| T-102-SC | Tampering | Instalação de pacotes | accept | Nenhuma instalação npm/pip/composer nesta fase — sem superfície de supply-chain nova |
</threat_model>

<verification>
- `C:\xampp\php\php.exe vendor/bin/phpunit --filter DesempenhoPeriodoOficialTest` verde.
- `C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/Phase74` verde.
- `git diff` prova a remoção dos ~140 linhas de guards de margem e das janelas inline de faturamento.
</verification>

<success_criteria>
- BON-01: faturamento e margem usam `period.current_*`/`period.baseline_*` (zero now()/startOfMonth inline para período).
- BON-02: computeOficial em jul/2026 usa competência jun/2026 (01/06..30/06 vs 02/05..31/05).
- BON-03: var_margem_pct via AdmanMetricDiffService com gate adman_diff + fallback marcado.
- Âncora Carlos recalibrada com golden derivado da régua (não preservado).
</success_criteria>

<output>
Create `.planning/phases/102-desempenho-oficial-por-competencia-v18-0/102-01-SUMMARY.md` when done.
Sinalizar explicitamente no SUMMARY: novo golden number da âncora Carlos + aritmética; que a matemática de faturamento operacional (mês em curso) permanece equivalente à v17 (resolver 1:1 no ramo current_month).
</output>
