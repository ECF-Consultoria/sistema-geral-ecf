---
phase: 74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_09_140002_create_bonus_faixas_table.php
  - database/migrations/2026_07_09_140003_seed_bonus_faixas_iniciais.php
  - app/Models/BonusFaixa.php
  - database/factories/BonusFaixaFactory.php
autonomous: true
requirements: [DESEMP-07]

must_haves:
  truths:
    - "Tabela bonus_faixas existe com colunas id, slug, nome, descricao, nota_min, nota_max, ordem, ativo, timestamps"
    - "4 rows seed criadas: sem_bonus [0.00, 3.99] ordem=1, basico [4.00, 4.49] ordem=2, intermediario [4.50, 4.99] ordem=3, maximo [5.00, 5.00] ordem=4"
    - "Model BonusFaixa usa LogsActivity trait com getActivitylogOptions() em pt-BR"
    - "Casts nota_min => decimal:2, nota_max => decimal:2, ativo => bool aplicados"
    - "Factory permite criar BonusFaixa em testes com sobreescrita de qualquer campo"
  artifacts:
    - path: "database/migrations/2026_07_09_140002_create_bonus_faixas_table.php"
      provides: "Schema bonus_faixas"
      contains: "bonus_faixas"
    - path: "database/migrations/2026_07_09_140003_seed_bonus_faixas_iniciais.php"
      provides: "Seed inicial das 4 faixas via migration transacional"
      contains: "sem_bonus"
    - path: "app/Models/BonusFaixa.php"
      provides: "Model Eloquent com LogsActivity"
      contains: "class BonusFaixa"
    - path: "database/factories/BonusFaixaFactory.php"
      provides: "Factory para testes"
      contains: "BonusFaixaFactory"
  key_links:
    - from: "app/Models/BonusFaixa.php"
      to: "bonus_faixas"
      via: "table binding padrão Eloquent + LogsActivity trait"
      pattern: "getActivitylogOptions"
---

<objective>
Criar tabela dedicada `bonus_faixas` + Model `BonusFaixa` + seed de 4 faixas iniciais (D-14, D-15, D-16), servindo como fonte de verdade da régua de bonificação editável via UI admin (Plans 74-05/74-07) e renderizada no artigo dinâmico do Manual (Plan 74-08).

Purpose: destrava o método `classificarFaixa` do `DesempenhoScoreService` (Plan 74-03) e a UI de configuração (Plan 74-05), fechando REQ DESEMP-07.

Output: 2 migrations + 1 Model + 1 Factory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md
@database/migrations/2026_06_30_000001_create_desempenho_score_snapshots_table.php

<interfaces>
Schema alvo `bonus_faixas`:

| Coluna       | Tipo                    | Constraint          | Semântica                                     |
|--------------|-------------------------|---------------------|-----------------------------------------------|
| id           | bigInteger              | PK auto-increment   | ID interno                                    |
| slug         | varchar(50)             | UNIQUE, NOT NULL    | Chave estável de código (`sem_bonus`, etc)    |
| nome         | varchar(100)            | NOT NULL            | Label visível UI (editável)                   |
| descricao    | text                    | NULLABLE            | Texto explicativo (aparece no Manual)         |
| nota_min     | DECIMAL(3,2)            | NOT NULL            | Piso inclusivo, [0.00 – 5.00]                 |
| nota_max     | DECIMAL(3,2)            | NOT NULL            | Teto inclusivo, [0.00 – 5.00]                 |
| ordem        | unsignedSmallInteger    | NOT NULL, default 0 | Ordenação ascendente na UI                    |
| ativo        | boolean                 | NOT NULL, default 1 | Faixa participa da classificação              |
| created_at   | timestamp               |                     |                                               |
| updated_at   | timestamp               |                     |                                               |

Seeds (rows) — via migration transacional (idempotente por slug):

| slug           | nome            | nota_min | nota_max | ordem | ativo | descricao (pt-BR resumida)                                                                                     |
|----------------|-----------------|----------|----------|-------|-------|----------------------------------------------------------------------------------------------------------------|
| sem_bonus      | Sem bônus       | 0.00     | 3.99     | 1     | 1     | Nota final abaixo do piso da régua. Sem bônus mensal.                                                          |
| basico         | Básico          | 4.00     | 4.49     | 2     | 1     | Bônus básico da régua.                                                                                         |
| intermediario  | Intermediário   | 4.50     | 4.99     | 3     | 1     | Bônus intermediário. 2 meses consecutivos nesta faixa promovem para máximo (regra DESEMP-08).                  |
| maximo         | Máximo          | 5.00     | 5.00     | 4     | 1     | Nota final = 5.00 OU 2 meses consecutivos intermediário. Bônus máximo.                                        |

Model:
- Trait `LogsActivity` (Spatie) — padrão do projeto (`User`, `Company`, `NpsTemplate`).
- `getActivitylogOptions()` retorna `LogOptions::defaults()->logOnlyDirty()->useLogName('bonus_faixa')`.
- `$fillable = ['slug', 'nome', 'descricao', 'nota_min', 'nota_max', 'ordem', 'ativo']`.
- `$casts = ['nota_min' => 'decimal:2', 'nota_max' => 'decimal:2', 'ativo' => 'bool', 'ordem' => 'int']`.

Factory: default state cria faixa aleatória com `nota_min` < `nota_max`, `ativo=true`, `ordem` sequencial. Estados `basico()`, `intermediario()`, `maximo()`, `semBonus()` para replicar as 4 rows canônicas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration da tabela bonus_faixas</name>
  <files>database/migrations/2026_07_09_140002_create_bonus_faixas_table.php</files>
  <read_first>
    - database/migrations/2026_06_30_000001_create_desempenho_score_snapshots_table.php (estilo Schema::hasTable guard idempotente + comentários pt-BR)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md (DESEMP-07)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-14, D-15)
  </read_first>
  <action>
    Criar `2026_07_09_140002_create_bonus_faixas_table.php` idempotente. `up()`:

    1. Guard `if (Schema::hasTable('bonus_faixas')) return;`.
    2. `Schema::create('bonus_faixas', function (Blueprint $table) {...})` com colunas conforme a tabela em `<interfaces>`: `id`, `string('slug', 50)->unique()`, `string('nome', 100)`, `text('descricao')->nullable()`, `decimal('nota_min', 3, 2)`, `decimal('nota_max', 3, 2)`, `unsignedSmallInteger('ordem')->default(0)`, `boolean('ativo')->default(true)`, `timestamps()`.
    3. Índice composto `(ativo, ordem)` para acelerar `where('ativo', true)->orderBy('ordem')` (query padrão da UI e do artigo dinâmico do Manual, Plan 74-08).

    `down()`: `Schema::dropIfExists('bonus_faixas')`.

    Cabeçalho pt-BR: cita Phase 74 D-14 + DESEMP-07 + explica que a tabela é a fonte da régua de bônus editável pelo admin, sem hardcode.
  </action>
  <verify>
    <automated>php artisan migrate --pretend 2>&1 | grep -i "bonus_faixas" | head -10</automated>
    Ver output SQL contendo `CREATE TABLE "bonus_faixas"` e as colunas.
  </verify>
  <acceptance_criteria>
    - Arquivo `database/migrations/2026_07_09_140002_create_bonus_faixas_table.php` existe.
    - `php artisan migrate --pretend` inclui `CREATE TABLE "bonus_faixas"` na saída.
    - Todas as 9 colunas do schema alvo presentes (incluindo `descricao TEXT NULL`).
    - `slug` tem UNIQUE index.
    - Cabeçalho da migration cita Phase 74 + D-14 + DESEMP-07.
    - Migration idempotente (guard `Schema::hasTable`).
  </acceptance_criteria>
  <done>Migration da tabela criada, idempotente, aplicável e reversível.</done>
</task>

<task type="auto">
  <name>Task 2: Criar migration de seed transacional para as 4 faixas iniciais</name>
  <files>database/migrations/2026_07_09_140003_seed_bonus_faixas_iniciais.php</files>
  <read_first>
    - database/migrations/2026_07_09_140002_create_bonus_faixas_table.php (Task 1 desta wave — schema alvo)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md (DESEMP-07)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-16)
  </read_first>
  <action>
    Criar `2026_07_09_140003_seed_bonus_faixas_iniciais.php` que popula as 4 faixas canônicas do D-16 via `DB::table` (não usar Model — modelos podem não estar auto-loaded em migrations puras).

    Em `up()`:
    1. Abrir `DB::transaction(function () { ... })`.
    2. Para cada faixa listada em `<interfaces>` (sem_bonus, basico, intermediario, maximo), usar `DB::table('bonus_faixas')->updateOrInsert(['slug' => $slug], [nome, descricao, nota_min, nota_max, ordem, ativo => true, created_at => now(), updated_at => now()])`. Isto garante idempotência (rerun não duplica).
    3. Textos em pt-BR conforme coluna "descricao" da tabela em `<interfaces>`.

    Em `down()`: `DB::table('bonus_faixas')->whereIn('slug', ['sem_bonus', 'basico', 'intermediario', 'maximo'])->delete()` — reversível para desenvolvimento.

    Cabeçalho pt-BR cita Phase 74 D-16 + DESEMP-07 + explica que os valores da diretoria/gestão (2026-07-09) podem ser alterados pelo admin via UI (Plan 74-05) — este seed é apenas o ponto de partida.
  </action>
  <verify>
    <automated>php artisan migrate 2>&1 | tail -20 && php artisan tinker --execute="echo \DB::table('bonus_faixas')->orderBy('ordem')->pluck('slug')->join(',');"</automated>
    Saída esperada: `sem_bonus,basico,intermediario,maximo`.
  </verify>
  <acceptance_criteria>
    - Após `php artisan migrate`, `DB::table('bonus_faixas')->count() === 4`.
    - Slugs presentes na ordem: sem_bonus, basico, intermediario, maximo.
    - `nota_min`/`nota_max` casam os valores do D-16: [0.00,3.99], [4.00,4.49], [4.50,4.99], [5.00,5.00].
    - Todos com `ativo = 1`.
    - Rerun `php artisan migrate:refresh` seguido de aplicar novamente NÃO duplica rows (updateOrInsert por slug).
  </acceptance_criteria>
  <done>Seed idempotente aplicado; 4 faixas canônicas persistidas com valores corretos.</done>
</task>

<task type="auto">
  <name>Task 3: Criar Model BonusFaixa com LogsActivity + Factory</name>
  <files>app/Models/BonusFaixa.php, database/factories/BonusFaixaFactory.php</files>
  <read_first>
    - app/Models/DesempenhoScoreSnapshot.php (padrão de Model minimalista com casts/fillable)
    - app/Models/NpsTemplate.php (padrão LogsActivity + getActivitylogOptions em pt-BR — reuso do padrão)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-15)
  </read_first>
  <action>
    Criar `app/Models/BonusFaixa.php`:

    1. `namespace App\Models; use Illuminate\Database\Eloquent\Model; use Illuminate\Database\Eloquent\Factories\HasFactory; use Spatie\Activitylog\LogOptions; use Spatie\Activitylog\Traits\LogsActivity;`.
    2. `class BonusFaixa extends Model` com `use HasFactory, LogsActivity;`.
    3. `protected $table = 'bonus_faixas';`.
    4. `$fillable` = ['slug','nome','descricao','nota_min','nota_max','ordem','ativo'] conforme `<interfaces>`.
    5. `$casts` = ['nota_min'=>'decimal:2','nota_max'=>'decimal:2','ativo'=>'bool','ordem'=>'int'].
    6. Método `public function getActivitylogOptions(): LogOptions` retornando `LogOptions::defaults()->logOnlyDirty()->useLogName('bonus_faixa')->setDescriptionForEvent(fn(string $event) => "Faixa de bônus {$this->slug} foi {$event}")`.
    7. Scopes: `scopeAtivas($q) => $q->where('ativo', true)`; `scopeOrdenadas($q) => $q->orderBy('ordem')`.
    8. Método estático `public static function classificar(float $nota): ?self` que retorna a primeira `BonusFaixa` ativa com `nota_min <= $nota AND $nota <= nota_max` (ordenada por `ordem` ASC). Retorna `null` se nenhuma faixa cobrir. Este método é reutilizado pelo `DesempenhoScoreService::classificarFaixa` no Plan 74-03.
    9. Docblock pt-BR cita Phase 74 D-15/D-16, aponta que `classificar` NÃO aplica a regra DESEMP-08 "2 meses consecutivos" (essa fica no Service pra manter Model puro).

    Criar `database/factories/BonusFaixaFactory.php`:

    1. `namespace Database\Factories; use App\Models\BonusFaixa; use Illuminate\Database\Eloquent\Factories\Factory;`.
    2. `class BonusFaixaFactory extends Factory { protected $model = BonusFaixa::class; public function definition(): array { return [ 'slug' => $this->faker->unique()->slug(1), 'nome' => $this->faker->words(2, true), 'descricao' => $this->faker->sentence(), 'nota_min' => 0.00, 'nota_max' => 5.00, 'ordem' => $this->faker->numberBetween(1, 10), 'ativo' => true ]; } }`.
    3. Estados nomeados: `public function semBonus(): static => $this->state(['slug'=>'sem_bonus','nome'=>'Sem bônus','nota_min'=>0.00,'nota_max'=>3.99,'ordem'=>1])`. Similar para `basico`, `intermediario`, `maximo` conforme D-16.
    4. Estado `inativa()` que seta `ativo => false`.

    Não adicionar relacionamento com nenhum outro Model (BonusFaixa é standalone — não FK para User/Company).
  </action>
  <verify>
    <automated>php artisan tinker --execute="\$f = App\Models\BonusFaixa::classificar(4.35); echo \$f?->slug ?? 'null'; echo PHP_EOL; \$f2 = App\Models\BonusFaixa::classificar(3.35); echo \$f2?->slug ?? 'null'; echo PHP_EOL; \$f3 = App\Models\BonusFaixa::classificar(5.00); echo \$f3?->slug ?? 'null';"</automated>
    Saída esperada: `basico`, depois `sem_bonus`, depois `maximo`.
  </verify>
  <acceptance_criteria>
    - `BonusFaixa::classificar(4.35)` retorna Model com `slug === 'basico'`.
    - `BonusFaixa::classificar(3.35)` retorna Model com `slug === 'sem_bonus'`.
    - `BonusFaixa::classificar(5.00)` retorna Model com `slug === 'maximo'`.
    - `BonusFaixa::classificar(-1.0)` retorna `null` (nenhuma faixa cobre).
    - `BonusFaixaFactory::new()->intermediario()->create()` cria row com slug intermediario.
    - `grep -q "LogsActivity" app/Models/BonusFaixa.php` retorna 0.
    - `grep -q "logOnlyDirty" app/Models/BonusFaixa.php` retorna 0.
  </acceptance_criteria>
  <done>Model BonusFaixa + Factory criados, `classificar()` funcional, LogsActivity ativado.</done>
</task>

</tasks>

<verification>
Rodar `php artisan migrate` → tabela criada + 4 rows seed. Rodar tinker snippet para validar `BonusFaixa::classificar()`. Nenhum erro de fillable/cast.
</verification>

<success_criteria>
- Tabela `bonus_faixas` existe com 9 colunas + unique(slug) + índice (ativo, ordem).
- 4 rows seed persistidas conforme D-16.
- Model `BonusFaixa` com LogsActivity + método estático `classificar(float)`.
- Factory permite criação em testes com estados nomeados.
- Rerun das migrations é idempotente.
</success_criteria>

<output>
Criar `.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-02-SUMMARY.md` ao concluir, listando arquivos criados, output do tinker snippet e link para DESEMP-07/D-14/D-15/D-16.
</output>
