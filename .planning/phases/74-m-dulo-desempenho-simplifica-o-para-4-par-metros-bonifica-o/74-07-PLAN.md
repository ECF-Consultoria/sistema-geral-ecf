---
phase: 74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o
plan: 07
type: execute
wave: 4
depends_on: [74-05]
files_modified:
  - resources/js/Pages/Desempenho/Configuracao.jsx
autonomous: true
requirements: [DESEMP-12]

must_haves:
  truths:
    - "Página /desempenho/configuracao renderiza lista das faixas de bônus"
    - "Cada faixa é editável inline (nota_min, nota_max, nome, descricao, ordem)"
    - "Botão toggle ativa/desativa faixa preserva histórico sem apagar row"
    - "Erros de validação (sobreposição, range, gte) exibidos em pt-BR ao lado do campo"
    - "Feedback de sucesso via toast/alert 'Faixa atualizada com sucesso'"
    - "Faixas inativas aparecem com opacity reduzida e badge 'Inativa'"
  artifacts:
    - path: "resources/js/Pages/Desempenho/Configuracao.jsx"
      provides: "UI React admin para CRUD das faixas de bônus"
      contains: "faixas"
  key_links:
    - from: "resources/js/Pages/Desempenho/Configuracao.jsx"
      to: "route('desempenho.configuracao.faixas.update', faixa.id)"
      via: "useForm().patch"
      pattern: "desempenho.configuracao.faixas.update"
    - from: "resources/js/Pages/Desempenho/Configuracao.jsx"
      to: "route('desempenho.configuracao.faixas.toggle', faixa.id)"
      via: "router.patch"
      pattern: "desempenho.configuracao.faixas.toggle"
---

<objective>
Criar a página Inertia `Desempenho/Configuracao.jsx` — UI admin para editar as 4 (ou mais) faixas de `bonus_faixas` inline. Fecha o lado frontend do REQ DESEMP-12 (D-12).

Purpose: sem essa página, a admin precisa mudar seed no DB para ajustar régua. Após esta phase, o ajuste vira 30 segundos + 1 clique.

Output: 1 arquivo JSX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md
@app/Http/Controllers/DesempenhoConfigController.php
@app/Http/Requests/UpdateBonusFaixaRequest.php
@app/Models/BonusFaixa.php
@resources/js/Pages/Nps/Configuracao.jsx
@resources/js/Pages/Nps/Index.jsx

<interfaces>
Props recebidas do `DesempenhoConfigController::index` (Plan 74-05):

```
faixas: Array<{
  id: number,
  slug: 'sem_bonus' | 'basico' | 'intermediario' | 'maximo' | string,
  nome: string,
  descricao: string | null,
  nota_min: string,          // decimal:2 → string "0.00" no JSON
  nota_max: string,          // decimal:2 → string "3.99"
  ordem: number,
  ativo: boolean,
  created_at: string,
  updated_at: string,
}>
flash?: { success?: string, error?: string }
```

Rotas alvo (registradas Plan 74-05):
- PATCH `route('desempenho.configuracao.faixas.update', faixa.id)` — payload {nome, descricao, nota_min, nota_max, ordem}.
- PATCH `route('desempenho.configuracao.faixas.toggle', faixa.id)` — sem body.

Design tokens (dark/glass — padrão Nps/Configuracao.jsx quando disponível, senão Nps/Index.jsx):
- Container: `<AppLayout title="Configuração — Régua de Bônus">` com `<div className="max-w-5xl mx-auto space-y-6">`.
- Cabeçalho: `<h1 className="text-white text-2xl font-display font-bold">Régua de Bonificação — Desempenho</h1>` + subtitulo pt-BR "Configure as faixas de bônus do módulo Performance. As mudanças refletem imediatamente no ranking e no artigo do Manual."
- Alerta info: bloco `bg-ecf-yellow/5 border border-ecf-yellow/20 rounded-2xl p-4` com texto "Regras: (1) nota_min < nota_max (exceto faixa 'maximo' [5.00,5.00]); (2) nenhuma sobreposição entre faixas ativas; (3) intervalo válido [0,5]. Regra especial: 2 meses consecutivos em 'Intermediário' promovem automaticamente para 'Máximo' — configurada no service."
- Lista de cards, um por faixa:
  - Card faixa ATIVA: `bg-ecf-card border border-white/[0.08] rounded-2xl p-6`.
  - Card faixa INATIVA: mesma classe + `opacity-50` + badge cinza "Inativa".
  - Header do card: nome (editável inline) + slug (chip cinza, read-only) + botão toggle ativo/inativo à direita.
  - Body do card: 4 inputs em grid 2×2:
    - `nota_min` (type=number step=0.01 min=0 max=5).
    - `nota_max` (type=number step=0.01 min=0 max=5).
    - `ordem` (type=number step=1 min=0).
    - `descricao` (textarea rows=3).
  - Botão "Salvar alterações" à direita do card (disabled quando form limpo).
  - Preview inline: "Faixa cobre notas de X.XX a Y.YY" — atualiza on-change para dar feedback imediato.
- Toast/alert de sucesso via `flash.success` (top-right, auto-dismiss 3s).
- Erros de validação: renderizar `errors[field]` do useForm ao lado do campo (`text-red-400 text-xs mt-1`).

Componente estrutural:

```
export default function Configuracao({ faixas, flash }) {
    // useState para tabs / toasts se necessário
    return (
        <AppLayout title="Configuração — Régua de Bônus">
            <div className="max-w-5xl mx-auto space-y-6">
                <Cabecalho />
                <AlertaRegras />
                {faixas.map(f => <FaixaCard key={f.id} faixa={f} />)}
            </div>
        </AppLayout>
    );
}

function FaixaCard({ faixa }) {
    const { data, setData, patch, processing, errors, isDirty } = useForm({
        nome: faixa.nome,
        descricao: faixa.descricao ?? '',
        nota_min: faixa.nota_min,
        nota_max: faixa.nota_max,
        ordem: faixa.ordem,
    });
    const handleSalvar = (e) => {
        e.preventDefault();
        patch(route('desempenho.configuracao.faixas.update', faixa.id), {
            preserveScroll: true,
        });
    };
    const handleToggle = () => {
        router.patch(route('desempenho.configuracao.faixas.toggle', faixa.id), {}, {
            preserveScroll: true,
        });
    };
    // ...JSX do card
}
```

Toast helper: se `flash?.success` presente, renderizar `<div className="fixed top-4 right-4 bg-emerald-500/10 border border-emerald-500/30 text-emerald-300 rounded-2xl px-4 py-3 shadow-lg">{flash.success}</div>` com `useEffect` que auto-esconde após 3s (setState).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar página Desempenho/Configuracao.jsx</name>
  <files>resources/js/Pages/Desempenho/Configuracao.jsx</files>
  <read_first>
    - resources/js/Pages/Nps/Configuracao.jsx (padrão de página de configuração admin com useForm + patch + errors — se existir; caso contrário usar Nps/Index.jsx como referência dark/glass)
    - app/Http/Requests/UpdateBonusFaixaRequest.php (Plan 74-05) para saber quais chaves de erro esperar (`nota_min`, `nota_max`, `nome`, `descricao`, `ordem`)
    - app/Http/Controllers/DesempenhoConfigController.php (Plan 74-05) para confirmar rotas nomeadas
    - resources/js/Layouts/AppLayout.jsx (uso do wrapper)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-12, D-13, D-21)
  </read_first>
  <action>
    Criar `resources/js/Pages/Desempenho/Configuracao.jsx` implementando a estrutura descrita em `<interfaces>`. Regras:

    1. Imports:
       - `import { useForm, usePage, router } from '@inertiajs/react';`
       - `import { useState, useEffect } from 'react';`
       - `import { Save, Power, PowerOff, AlertCircle, Info, X } from 'lucide-react';`
       - `import AppLayout from '@/Layouts/AppLayout';`
       - `import { cn } from '@/lib/utils';`

    2. Componente `Configuracao({ faixas })`:
       - Ler `flash` via `usePage().props.flash`.
       - Componente interno `Toast` que renderiza `flash.success` no canto superior direito com auto-dismiss (`useEffect + setTimeout 3000`).
       - Bloco cabeçalho + subtitulo pt-BR.
       - Bloco AlertaRegras com texto pt-BR explicando validação (sem sobreposição, range 0-5, min<max).
       - Loop `faixas.map(f => <FaixaCard key={f.id} faixa={f} />)`.

    3. Componente `FaixaCard({ faixa })`:
       - `useForm` com estado inicial dos campos editáveis.
       - `handleSalvar(e)`: preventDefault + `patch(route('desempenho.configuracao.faixas.update', faixa.id), {preserveScroll: true})`.
       - `handleToggle()`: `router.patch(route('desempenho.configuracao.faixas.toggle', faixa.id), {}, {preserveScroll: true})`.
       - Layout:
         - Div externo: `<div className={cn('bg-ecf-card border border-white/[0.08] rounded-2xl p-6', !faixa.ativo && 'opacity-50')}>`.
         - Header: nome editável em input (bordless quando não focado) + slug em chip cinza `<span className="text-white/40 text-xs uppercase font-mono">{faixa.slug}</span>` + badge "Inativa" quando `!faixa.ativo` + botão toggle à direita.
         - Botão toggle: `<button onClick={handleToggle} className={cn('...', faixa.ativo ? 'bg-emerald-500/10 text-emerald-300' : 'bg-white/[0.03] text-white/40')}>{faixa.ativo ? <Power /> : <PowerOff />}</button>`.
         - Form: `<form onSubmit={handleSalvar} className="mt-4 space-y-4">`.
           - Grid 2x2 dos inputs (nota_min, nota_max, ordem, descricao).
           - Cada input com label pt-BR + campo + `{errors.field && <p className="text-red-400 text-xs mt-1">{errors.field}</p>}`.
           - Preview `<p className="text-white/40 text-xs">Faixa cobre notas de {data.nota_min} a {data.nota_max}</p>`.
           - Botão "Salvar alterações" à direita: `<button type="submit" disabled={processing || !isDirty} className="bg-ecf-yellow text-black rounded-2xl px-4 py-2 font-semibold disabled:opacity-40">Salvar alterações</button>`.

    4. Descrição: textarea `rows={3}` com placeholder "Texto que aparece no artigo do Manual explicando esta faixa" (dica pt-BR conforme D-13).

    5. Comentários pt-BR citando Phase 74 D-12/D-13/D-21 no topo.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('resources/js/Pages/Desempenho/Configuracao.jsx','utf8'); const t=['faixa.slug','nota_min','nota_max','desempenho.configuracao.faixas.update','desempenho.configuracao.faixas.toggle','useForm','Inativa','Sobreposição\|Regras','ativo']; const m=t.filter(x=>!s.includes(x.split('\\\\')[0]) && !new RegExp(x.replace(/\\\\/g,'')).test(s)); if(m.length){console.error('MISSING',m);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `resources/js/Pages/Desempenho/Configuracao.jsx` existe.
    - Referencia `route('desempenho.configuracao.faixas.update'` E `route('desempenho.configuracao.faixas.toggle'`.
    - useForm com campos `nome`, `descricao`, `nota_min`, `nota_max`, `ordem`.
    - Renderiza badge "Inativa" quando `!faixa.ativo`.
    - Erros do backend (`errors.nota_min` etc) renderizados no lado do campo.
    - Toast de sucesso com auto-dismiss.
    - Design dark/glass (referências `bg-ecf-card`, `border-white/[0.08]`).
    - Comentários pt-BR citam D-12/D-13.
  </acceptance_criteria>
  <done>Página React da configuração pronta para render em /desempenho/configuracao.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar build do frontend</name>
  <files>public/build/manifest.json</files>
  <read_first>
    - package.json (script build)
    - CLAUDE.md (convenção "Run npm run build after every frontend change")
  </read_first>
  <action>
    Executar `npm run build`. Corrigir erros de import/sintaxe caso apareçam.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Build exit 0.
    - Nenhum ERROR/SyntaxError na saída.
    - `public/build/manifest.json` inclui `resources/js/Pages/Desempenho/Configuracao.jsx` (grep no manifest).
  </acceptance_criteria>
  <done>Build compilando com o novo arquivo.</done>
</task>

</tasks>

<verification>
Login como admin, navegar em `/desempenho/configuracao` — deve mostrar 4 faixas seed com valores editáveis. Editar `intermediario.nota_min` de 4.50 para 4.60 → click Salvar → toast de sucesso + valor persistido. Tentar setar `basico.nota_max = 4.70` → validação de sobreposição rejeita em pt-BR.
</verification>

<success_criteria>
- Página React funcional em /desempenho/configuracao.
- Admin edita faixas inline (nota_min, nota_max, nome, descricao, ordem).
- Toggle ativo/inativo funciona preservando histórico.
- Erros de validação exibidos em pt-BR no formulário.
- Build compila sem erro.
- Design consistente com padrão dark/glass do projeto.
</success_criteria>

<output>
Criar `.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-07-SUMMARY.md` ao concluir, listando arquivo, output do build, decisão D-12/D-13, REQ DESEMP-12.
</output>
