---
phase: 74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o
plan: 10
type: execute
wave: 5
depends_on: [74-04, 74-05]
files_modified:
  - tests/Feature/Phase74/DesempenhoConfigControllerTest.php
  - tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php
autonomous: true
requirements: [DESEMP-09, DESEMP-12]

must_haves:
  truths:
    - "GET /desempenho/configuracao com user não-admin retorna 403"
    - "GET /desempenho/configuracao com admin retorna 200 e as 4 faixas seed nas props"
    - "PATCH /desempenho/configuracao/faixas/{faixa} rejeita nota_min >= nota_max (exceto slug maximo)"
    - "PATCH /desempenho/configuracao/faixas/{faixa} rejeita sobreposição entre faixas ativas em pt-BR"
    - "PATCH toggle-active alterna ativo=true→false→true sem apagar row"
    - "Comando desempenho:consolidar-mes --mes=2026-07 grava snapshot com mes_referencia=2026-07-01"
    - "Rerun do comando no mesmo mês é IDEMPOTENTE (updateOrCreate)"
    - "User sem carteira NÃO recebe row no snapshot"
  artifacts:
    - path: "tests/Feature/Phase74/DesempenhoConfigControllerTest.php"
      provides: "Suite Feature das rotas admin de configuração"
      contains: "test_validacao_sobreposicao_de_faixas_ativas"
    - path: "tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php"
      provides: "Suite Feature do comando mensal + idempotência"
      contains: "test_idempotencia_do_command_consolidar_mes"
  key_links:
    - from: "tests/Feature/Phase74/DesempenhoConfigControllerTest.php"
      to: "App\\Http\\Controllers\\DesempenhoConfigController"
      via: "$this->actingAs($admin)->patch(route(...))"
      pattern: "desempenho.configuracao"
    - from: "tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php"
      to: "App\\Console\\Commands\\ConsolidarMesDesempenho"
      via: "$this->artisan('desempenho:consolidar-mes')"
      pattern: "desempenho:consolidar-mes"
---

<objective>
Suite Feature de dois arquivos cobrindo (a) o `DesempenhoConfigController` + `UpdateBonusFaixaRequest` — DESEMP-12; (b) o novo comando `ConsolidarMesDesempenho` + idempotência + tratamento de sem_carteira — DESEMP-09.

Purpose: fecha as REQs de configuração admin e frequência mensal com contra regressão de fluxo real (rota + DB).

Output: 2 arquivos de teste Feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md
@app/Http/Controllers/DesempenhoConfigController.php
@app/Http/Requests/UpdateBonusFaixaRequest.php
@app/Console/Commands/ConsolidarMesDesempenho.php
@app/Console/Commands/SnapshotDesempenhoScores.php
@app/Models/BonusFaixa.php
@app/Models/DesempenhoScoreSnapshot.php
@tests/Feature/Phase74/DesempenhoScoreServiceTest.php

<interfaces>
Namespace: `Tests\Feature\Phase74`. RefreshDatabase + PRAGMA foreign_keys ON no setUp.

Fixtures:
- `criarAdmin(): User` — cria user com `role='admin'` (User::isAdmin() true).
- `criarAnalista(): User` — user com cargo `analista` via user_setores/cargos.
- `criarEstrategista(): User` — user com cargo `estrategista`.
- `criarSnapshotMensal(User $u, string $mesReferencia, string $classificacao): DesempenhoScoreSnapshot` — para testar promoção 2 meses.

**Arquivo A — DesempenhoConfigControllerTest.php** — testes obrigatórios:

1. `test_get_configuracao_como_analista_retorna_403` — actingAs analista, GET `/desempenho/configuracao` → 403.
2. `test_get_configuracao_como_estrategista_retorna_403` — mesmo com estrategista.
3. `test_get_configuracao_como_admin_retorna_200_com_faixas_seed` — actingAs admin, GET → 200. Assertar `Inertia::hasProp('faixas')` (via `Inertia::assertHas('faixas', fn ($f) => count($f) >= 4)`). Verificar 4 slugs seed presentes.
4. `test_patch_faixa_atualiza_nota_min_e_nota_max_com_sucesso` — admin edita `intermediario` de `[4.50,4.99]` para `[4.60,4.99]` → 302 redirect back + flash success + DB reflete valor novo.
5. `test_patch_faixa_rejeita_nota_min_maior_que_nota_max` — admin envia `nota_min=4.99, nota_max=4.50` → 302 com session errors `nota_max` (regra `gte`).
6. `test_patch_faixa_permite_igualdade_para_slug_maximo` — admin edita `maximo` com `[5.00, 5.00]` → sucesso.
7. `test_patch_faixa_rejeita_range_fora_de_0_5` — admin envia `nota_min=-1` → error. `nota_max=6` → error.
8. `test_validacao_sobreposicao_de_faixas_ativas` — admin edita `basico` de `[4.00, 4.49]` para `[4.00, 4.60]` (invade intermediario 4.50-4.99) → 302 com error `nota_min` contendo "Sobreposição com a faixa \"Intermediário\"".
9. `test_toggle_active_alterna_ativo_true_false` — admin patch toggle → row.ativo = false. Nova patch → row.ativo = true.
10. `test_toggle_active_como_analista_retorna_403` — proteção middleware admin.
11. `test_faixa_desativada_nao_participa_de_validacao_sobreposicao` — desativar `intermediario`, depois expandir `basico` para `[4.00, 4.60]` → agora aceita (não há sobreposição com faixa ativa).

**Arquivo B — ConsolidarMesDesempenhoCommandTest.php** — testes obrigatórios:

Setup: reuso do provider stub do Plan 74-09 (extrair para helper trait `Tests\Feature\Phase74\Concerns\StubProvidersTrait` OU duplicar bind no setUp). Carbon::setTestNow('2026-08-01 14:05:00') congela.

1. `test_comando_grava_snapshot_com_mes_referencia_do_mes_anterior_quando_sem_flag` — sem `--mes`, roda em 2026-08-01. Cria 1 analista com carteira ativa julho/2026. Assertar `DesempenhoScoreSnapshot::where('user_id', $u->id)->where('mes_referencia', '2026-07-01')->exists()`.
2. `test_comando_aceita_mes_flag_yyyy_mm` — `artisan('desempenho:consolidar-mes', ['--mes' => '2026-06'])` → snapshot com `mes_referencia = 2026-06-01`.
3. `test_idempotencia_do_command_consolidar_mes` — rodar 2× → `DesempenhoScoreSnapshot::mensal()->count()` == número de users (não duplica). O snapshot updateOrCreate garante isso.
4. `test_comando_pula_user_sem_carteira_no_mes` — user sem `company_users` no mês → NÃO grava row. Assertar `DesempenhoScoreSnapshot::mensal()->where('user_id', $userSem->id)->doesntExist()`.
5. `test_comando_popular_ranking_pos_por_mes_referencia` — 3 users com nota_final diferentes → após rodar, `ranking_pos` populado 1..3 filtrando por mes_referencia.
6. `test_command_snapshot_diario_preservado_grava_mes_referencia_null` — rodar `desempenho:snapshot-scores` (comando diário do Plan 74-04 reescrito) → row com `mes_referencia=null`, `ref_date=hoje`. NÃO conflita com row mensal do mesmo user.
7. `test_command_snapshot_diario_pula_user_sem_carteira` — mesmo comportamento no diário.

Padrões:
- Attribute `#[Test]` do PHPUnit 11.
- Nomenclatura pt-BR nos snake_case dos test methods.
- Fixtures reutilizando helpers do Plan 74-09 quando possível (podem viver em trait `Concerns/BuildsCarteira.php` compartilhada).

Cache/limpeza: cada teste rodar `RefreshDatabase` limpa. Nenhum teste depende de outro.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar DesempenhoConfigControllerTest.php</name>
  <files>tests/Feature/Phase74/DesempenhoConfigControllerTest.php</files>
  <read_first>
    - app/Http/Controllers/DesempenhoConfigController.php (Plan 74-05)
    - app/Http/Requests/UpdateBonusFaixaRequest.php (Plan 74-05)
    - app/Models/BonusFaixa.php (Plan 74-02)
    - tests/Feature/Phase74/DesempenhoScoreServiceTest.php (Plan 74-09 — reusar helpers criarAdmin/criarAnalista)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md (DESEMP-12)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-11, D-13)
  </read_first>
  <action>
    Criar `tests/Feature/Phase74/DesempenhoConfigControllerTest.php`:

    1. Namespace `Tests\Feature\Phase74`.
    2. Class extends `Tests\TestCase` com `use RefreshDatabase;` e `use PHPUnit\Framework\Attributes\Test;`.
    3. `setUp()`: parent + PRAGMA foreign_keys ON.
    4. Helpers:
       - `criarAdmin(): User` — `User::factory()->create(['role' => 'admin', 'active' => true])`.
       - `criarAnalista(): User` — User + user_setores/cargos.slug='analista' (via DB direto ou reusar helper do Plan 74-09 se compartilhado).
       - `criarEstrategista(): User`.
    5. Implementar os 11 testes conforme lista do `<interfaces>` (arquivo A).
    6. Para cada patch, usar `$this->actingAs($admin)->patch(route('desempenho.configuracao.faixas.update', $faixa->id), $payload)`.
    7. Para assertar `Inertia::hasProp`, importar `use Inertia\Testing\AssertableInertia;` e usar `$response->assertInertia(fn ($page) => $page->component('Desempenho/Configuracao')->has('faixas', 4))`.
    8. Cada teste com comentário pt-BR citando o REQ e o número do teste da lista `<interfaces>`.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Feature/Phase74/DesempenhoConfigControllerTest.php --stop-on-failure 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0 do phpunit.
    - Pelo menos 11 testes (grep `#[Test]` >= 11).
    - Nenhum `markTestSkipped/Incomplete`.
    - Cada teste asserta payload/DB/session errors específico.
  </acceptance_criteria>
  <done>Suite do controller admin verde.</done>
</task>

<task type="auto">
  <name>Task 2: Criar ConsolidarMesDesempenhoCommandTest.php</name>
  <files>tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php</files>
  <read_first>
    - app/Console/Commands/ConsolidarMesDesempenho.php (Plan 74-04)
    - app/Console/Commands/SnapshotDesempenhoScores.php (Plan 74-04)
    - app/Services/DesempenhoScoreService.php (Plan 74-03)
    - app/Models/DesempenhoScoreSnapshot.php (Plan 74-01)
    - tests/Feature/Phase74/DesempenhoScoreServiceTest.php (Plan 74-09 — reusar provider stub bind + helpers de fixture)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md (DESEMP-09, DESEMP-10)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-08, D-09)
  </read_first>
  <action>
    Criar `tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php`:

    1. Namespace/class/traits idem Plan 74-09/Plan Task 1.
    2. `setUp()`: parent + PRAGMA + bind stub de `MetricsProviderFactory` (copiar do Plan 74-09 setUp) + `Carbon::setTestNow('2026-08-01 14:05:00')`.
    3. Helpers reutilizados: `criarAnalista`, `criarEmpresaNaCarteira`, `mockRevenueMes`, `mockContribMargemMes`, `mockNpsRespostaMes` — se possível extrair para trait `Tests\Feature\Phase74\Concerns\BuildsCarteira` reutilizando entre os 3 arquivos de teste da phase (Plan 74-09, Task 1 desta wave, e Task 2 desta wave). Se preferir simplicidade, duplicar helpers.
    4. Implementar os 7 testes listados no `<interfaces>` arquivo B.
    5. Para invocar comando: `$this->artisan('desempenho:consolidar-mes')->assertSuccessful()` e `$this->artisan('desempenho:consolidar-mes', ['--mes' => '2026-06'])`.
    6. Cada teste comenta o REQ/decisão que endereça (DESEMP-09, DESEMP-10, D-08, D-09).
    7. Assertar `ranking_pos` populado corretamente no teste 5 — usar `assertEquals` no rank filtrando por `mes_referencia`.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Feature/Phase74/ConsolidarMesDesempenhoCommandTest.php --stop-on-failure 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0.
    - Pelo menos 7 testes.
    - Testes assertam `mes_referencia` correta no snapshot.
    - Teste de idempotência confirma `count` estável após 2 execuções.
    - Teste de sem_carteira confirma `doesntExist` para user sem carteira.
    - Teste do snapshot diário (SnapshotDesempenhoScores) confirma `mes_referencia=null`.
  </acceptance_criteria>
  <done>Suite do comando mensal + preservação do diário verde.</done>
</task>

<task type="auto">
  <name>Task 3: Rodar a suite Phase74 completa + regressão</name>
  <files>tests/Feature/Phase74/*</files>
  <read_first>
    - phpunit.xml (config)
  </read_first>
  <action>
    Executar as suites das 3 phases (Plan 74-09 + Task 1 + Task 2 desta wave) num único run + verificar zero regressão nas suites existentes que consumem o novo shape:

    ```
    vendor/bin/phpunit tests/Feature/Phase74/ --stop-on-failure
    ```

    Depois rodar:

    ```
    vendor/bin/phpunit --filter="DesempenhoScoreSnapshotTest|DesempenhoEvolucaoTest|PerformanceCargoFilterTest|Portfolio/RenderPortfolioTest" 2>&1 | tail -30
    ```

    Se qualquer suite existente falhar, investigar e corrigir NO PRÓPRIO controller/service (nunca degradar assertions). Se falha for de shape (v1 → v2 mudou), adaptar apenas os assertions dos testes existentes MINIMAMENTE — não deletar testes.

    Registrar output no SUMMARY.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Feature/Phase74/ 2>&1 | tail -10 && vendor/bin/phpunit --filter="DesempenhoScoreSnapshotTest|DesempenhoEvolucaoTest|PerformanceCargoFilterTest" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit tests/Feature/Phase74/` exit code 0.
    - Regressão: suites existentes `DesempenhoScoreSnapshotTest`, `DesempenhoEvolucaoTest`, `PerformanceCargoFilterTest`, `Portfolio/RenderPortfolioTest` — exit code 0.
    - Nenhuma degradação (deletar assert só permitido se REQ SPEC boundaries removeu a funcionalidade e for justificado no SUMMARY).
  </acceptance_criteria>
  <done>Todas suites verdes; zero regressão introduzida.</done>
</task>

</tasks>

<verification>
`vendor/bin/phpunit tests/Feature/Phase74/` → verde. Além disso, uma bateria de smoke: `vendor/bin/phpunit --filter="Desempenho|Performance"` deve continuar verde após a phase.
</verification>

<success_criteria>
- 2 novos arquivos de teste criados.
- 11 testes de config controller + 7 testes do comando mensal.
- Idempotência do comando comprovada por teste.
- Validação de sobreposição comprovada por teste.
- Zero regressão em suites existentes.
</success_criteria>

<output>
Criar `.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-10-SUMMARY.md` ao concluir, listando arquivos, output do phpunit (contagem testes), REQ DESEMP-09/DESEMP-12 endereçados, comprovação de zero regressão via output do filter no console.
</output>
