---
phase: 74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o
plan: 09
type: execute
wave: 5
depends_on: [74-04]
files_modified:
  - tests/Feature/Phase74/DesempenhoScoreServiceTest.php
autonomous: true
requirements: [DESEMP-01, DESEMP-02, DESEMP-03, DESEMP-04, DESEMP-05, DESEMP-06, DESEMP-08, DESEMP-10, DESEMP-11]

must_haves:
  truths:
    - "Fixture Carlos (NPS 4.25 + var_fat 3% + var_margem 2.8%) retorna nota_final=3.35 e faixa_bonus='sem_bonus'"
    - "User sem NPS no mês recebe nps_medio=0.0 (não null, não excluído)"
    - "Empresa nova na carteira (menos de 2 meses) NÃO entra em computeVarFaturamento"
    - "User sem carteira retorna sem_carteira=true + motivo pt-BR"
    - "2 meses consecutivos intermediario → promove para maximo"
    - "MetricsProviderFactory ML-first + Adman-fallback exercitado end-to-end via stub"
    - "Absenteísmo sempre retorna null nesta phase"
  artifacts:
    - path: "tests/Feature/Phase74/DesempenhoScoreServiceTest.php"
      provides: "Suite completa cobrindo REQ-01..08, 10, 11"
      min_lines: 300
      contains: "test_fixture_carlos_retorna_nota_3_35_sem_bonus"
  key_links:
    - from: "tests/Feature/Phase74/DesempenhoScoreServiceTest.php"
      to: "App\\Services\\DesempenhoScoreService"
      via: "app(DesempenhoScoreService::class)"
      pattern: "DesempenhoScoreService"
    - from: "tests/Feature/Phase74/DesempenhoScoreServiceTest.php"
      to: "App\\Services\\Metrics\\MetricsProviderFactory"
      via: "instance() swap para stub controlado"
      pattern: "MetricsProviderFactory"
---

<objective>
Criar a suite de testes Feature bloqueante `DesempenhoScoreServiceTest` cobrindo os requisitos DESEMP-01 a DESEMP-11 (exceto DESEMP-07 já validado em Plan 74-02 e DESEMP-09 validado em Plan 74-10). A âncora é a **fixture Carlos**: se este teste passa, a matemática está fiel à decisão da diretoria.

Purpose: contra regressão silenciosa. Sem esta suite, uma mudança errada nos cálculos passaria despercebida — o exemplo Carlos garante que a nota 3.35 continua saindo.

Output: 1 arquivo de testes Feature em `tests/Feature/Phase74/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md
@app/Services/DesempenhoScoreService.php
@app/Services/Metrics/MetricsProviderFactory.php
@app/Services/Metrics/UnifiedMetricsDto.php
@app/Models/BonusFaixa.php
@app/Models/DesempenhoScoreSnapshot.php
@app/Models/AdmanMetric.php
@app/Models/NpsSurvey.php
@app/Models/NpsResponse.php
@app/Models/User.php
@app/Models/Company.php

<interfaces>
Namespace: `Tests\Feature\Phase74`.
Base class: `Tests\TestCase`.
Traits: `RefreshDatabase`. No `setUp()`: `DB::statement('PRAGMA foreign_keys = ON')` (padrão do projeto para SQLite tests).

Provider stub: durante `setUp()`, instanciar um double do `MetricsProviderFactory` que retorna um `MlMetricsProvider` stub controlável por-company. Estratégia mínima:

```
$this->app->bind(MetricsProviderFactory::class, function () {
    return new class extends MetricsProviderFactory {
        public array $revenueByCompany = [];
        public array $caseByCompany = [];
        public function __construct() {}
        public function caseFor(Company $c): string {
            return $this->caseByCompany[$c->id] ?? 'so-adman';
        }
        public function forCompany(Company $c): array {
            // Retorna um provider stub que responde revenue conhecido
            return [ new class($this->revenueByCompany[$c->id] ?? []) implements \App\Contracts\MetricsProvider {
                public function __construct(private array $revenuePerMonth) {}
                public function supports(\App\Models\Company $c): bool { return true; }
                public function name(): string { return 'stub'; }
                public function readForCompany(\App\Models\Company $c, \Carbon\Carbon $from, \Carbon\Carbon $to): \App\Services\Metrics\UnifiedMetricsDto {
                    $key = $from->format('Y-m');
                    return new \App\Services\Metrics\UnifiedMetricsDto(
                        company_id: $c->id, source: 'stub', period_from: $from, period_to: $to,
                        revenue: $this->revenuePerMonth[$key] ?? null,
                        ad_spend: null, sold_quantity: null, tacos: null,
                        net_billing: null, sales_fee: null, taxes: null, shipping_cost: null,
                        product_cost: null, contribution_margin: null, acos: null, roas: null,
                        clicks: null, impressions: null, orders_count: null,
                    );
                }
            }];
        }
    };
});
```

Fixture Carlos (helper `criarCarlosCompleto()`):
- User `role=consultor` (analista) com `cargo` na taxonomia via `user_setores` + `cargos.slug='analista'`.
- 3 empresas (Company::factory) associadas via `company_users` pivot como `role=consultor` com `created_at = now()->subMonths(3)` (baseline suficiente).
- Para cada empresa: `AdmanMetric` rows preenchidas para:
  - Mês julho 2026: revenue distribuído para a média de var_faturamento = 3%.
  - Mês junho 2026: revenue baseline distribuído para var média 3%.
  - `contribution_margin` para a média de var_margem = 2.8%.
- NpsSurvey completed para carteira em julho/2026, com `NpsResponse.score_analista` produzindo média 4.25 (legacy path — sem template v15).
- Sem snapshot mês anterior (junho) — regra "2 meses consecutivos" NÃO se aplica.

Fixture de empresa NOVA (helper `criarEmpresaNova(User $u, Carbon $mes): Company`):
- Company::factory associada ao user via `company_users` com pivot `created_at = $mes->copy()->subDays(15)` (menos de 2 meses na carteira).

Fixture "2 meses consecutivos intermediario":
- Snapshot mensal junho/2026 → `DesempenhoScoreSnapshot::factory` (ou create) com `classificacao='intermediario'`, `mes_referencia='2026-06-01'`, `user_id=$carlos->id`.
- Julho/2026 novo compute com nota 4.75 (natural intermediario) → deve promover para `maximo` + `faixa_promovida=true`.

Configuração NPS legacy fallback: garantir que o `NpsScoreCalculator::compute` fallbacks para `$response->score_analista` mesmo sem template v15 configurado — checar comportamento e usar fallback direto se calculator retorna null.

Bonus faixas: as 4 rows do seed (Plan 74-02) devem existir. Como `RefreshDatabase` roda todas migrations (incluindo seed migration Plan 74-02), a tabela já vem populada com as 4 faixas.

Testes obrigatórios (mínimo 9 métodos):

1. `test_fixture_carlos_retorna_nota_3_35_sem_bonus` — âncora bloqueante (DESEMP-01).
2. `test_nota_final_e_media_direta_em_escalas_naturais` — chamada isolada de `computeNotaFinal(4.25, 3.0, 2.8)` retorna 3.35 (DESEMP-02).
3. `test_nps_medio_e_zero_quando_user_sem_respostas_no_mes` (DESEMP-03).
4. `test_nps_medio_e_media_das_notas_recebidas_no_mes` — user com 3 respostas 5/4/3 → nps_medio = 4.0 (DESEMP-03).
5. `test_var_faturamento_media_das_variacoes_por_empresa` — carteira `[-2%, +7%, +4%]` → 3.00% (DESEMP-04).
6. `test_var_faturamento_exclui_empresa_nova_da_media` — empresa recém-adicionada não entra (DESEMP-04).
7. `test_var_margem_usa_adman_como_fonte_canonica` — empresa com `contribution_margin` do Adman, provider ML no `so-ml` → margem ainda vem do Adman (DESEMP-05).
8. `test_absenteismo_retorna_null_sempre` — para qualquer user (DESEMP-06).
9. `test_2_meses_consecutivos_intermediario_promove_para_maximo` — snapshot junho + cálculo julho ambos intermediario → julho retorna `maximo` + `promovida=true` (DESEMP-08).
10. `test_user_sem_carteira_retorna_sem_carteira_true_com_motivo_pt_br` — user sem `company_users` no mês → shape `sem_carteira=true` + motivo com "Sem carteira em" e mês pt-BR (DESEMP-10).
11. `test_provider_ml_first_com_adman_fallback` — empresa com `caseFor='so-ml'` recebe revenue via provider ML stub; empresa com `caseFor='so-adman'` recebe via AdmanMetric direto; empresa com `caseFor='none'` é excluída (DESEMP-11).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar tests/Feature/Phase74/DesempenhoScoreServiceTest.php</name>
  <files>tests/Feature/Phase74/DesempenhoScoreServiceTest.php</files>
  <read_first>
    - app/Services/DesempenhoScoreService.php (Plan 74-03 — assinatura completa)
    - app/Services/Metrics/MetricsProviderFactory.php + AdmanMetricsProvider.php (para stub das assinaturas)
    - app/Services/Metrics/UnifiedMetricsDto.php (shape do DTO retornado por provider)
    - app/Models/BonusFaixa.php (seed disponível via migration Plan 74-02)
    - app/Models/DesempenhoScoreSnapshot.php (fillable atualizado com mes_referencia — Plan 74-01)
    - app/Models/User.php (isMentor, companies, setores)
    - app/Models/Company.php (relacionamento company_users, factory)
    - app/Models/AdmanMetric.php (colunas)
    - app/Models/NpsSurvey.php + NpsResponse.php (relacionamento + score_analista/score_estrategista)
    - database/factories/*Factory.php (verificar factories existentes de User, Company, AdmanMetric, NpsSurvey, NpsResponse)
    - tests/Feature/DesempenhoScoreSnapshotTest.php (padrão de setUp com PRAGMA + factories)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md (DESEMP-01..11)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-27, D-28, D-29)
  </read_first>
  <action>
    Criar `tests/Feature/Phase74/DesempenhoScoreServiceTest.php`:

    1. Namespace `Tests\Feature\Phase74`.
    2. `class DesempenhoScoreServiceTest extends TestCase` com `use RefreshDatabase;`.
    3. `setUp()`:
       - `parent::setUp();`
       - `DB::statement('PRAGMA foreign_keys = ON');` (SQLite).
       - Bind stub de `MetricsProviderFactory` conforme código do `<interfaces>`. Guardar referência ao stub em `$this->providerStub` para configurar por teste.
       - `Carbon::setTestNow(Carbon::parse('2026-08-01 14:05:00'))` — congela para os cálculos referirem julho como mês fechado.

    4. Métodos helper privados:
       - `criarUserAnalista(): User` — cria User + user_setores/cargos.slug='analista' via factory ou DB inserts diretos.
       - `criarEmpresaNaCarteira(User $u, string $pivotCreatedAt = '-3 months'): Company` — Company::factory + attach com `company_users.created_at` controlado.
       - `criarEmpresaNova(User $u): Company` — atalho para `criarEmpresaNaCarteira($u, '-15 days')`.
       - `mockRevenueMes(Company $c, string $mesYearMonth, float $revenue): void` — cria AdmanMetric row com `reference_date` no mês + `revenue`.
       - `mockContribMargemMes(Company $c, string $mesYearMonth, float $margem): void` — cria AdmanMetric row com `contribution_margin`.
       - `mockNpsRespostaMes(Company $c, string $mesYearMonth, int $scoreAnalista): void` — cria NpsSurvey completed no mês + NpsResponse.
       - `criarSnapshotMensalAnterior(User $u, string $mesReferencia, string $classificacao): DesempenhoScoreSnapshot`.

    5. Método fixture: `criarCarlosCompleto(): User` — implementa a fixture spec DESEMP-01:
       - `$carlos = $this->criarUserAnalista(['name' => 'Carlos']);`
       - 3 empresas com pivot `-3 months`.
       - Configurar `AdmanMetric` para gerar variação 3% (média das 3 empresas) para julho vs junho.
       - Configurar `contribution_margin` para gerar variação 2.8%.
       - Configurar NpsSurvey/Response com nota 4.25 (2 respostas: 4.5 e 4.0 → média 4.25).
       - Return $carlos.

    6. Testes (usar `#[Test]` attribute do PHPUnit 11 conforme padrão do projeto):

    - **test_fixture_carlos_retorna_nota_3_35_sem_bonus**:
      ```
      $carlos = $this->criarCarlosCompleto();
      $service = app(DesempenhoScoreService::class);
      $result = $service->compute($carlos, Carbon::parse('2026-07-01'));
      $this->assertEquals(4.25, $result['componentes']['nps_medio']);
      $this->assertEquals(3.00, $result['componentes']['var_faturamento_pct']);
      $this->assertEquals(2.80, $result['componentes']['var_margem_pct']);
      $this->assertNull($result['componentes']['absenteismo_pct']);
      $this->assertEquals(3.35, $result['nota_final']);
      $this->assertEquals('sem_bonus', $result['faixa_bonus']);
      $this->assertFalse($result['sem_carteira']);
      $this->assertFalse($result['faixa_promovida']);
      ```

    - **test_nota_final_e_media_direta_em_escalas_naturais** — usar reflection ou método público equivalente para chamar `computeNotaFinal(4.25, 3.0, 2.8)` → 3.35. Se `computeNotaFinal` for privado, invocar via ReflectionMethod. Também testar `(null, 3.0, 2.8)` → 2.90; `(null, null, null)` → null.

    - **test_nps_medio_e_zero_quando_user_sem_respostas_no_mes** — user com carteira mas sem NpsSurvey no mês → compute retorna `componentes.nps_medio === 0.0` (não null).

    - **test_nps_medio_e_media_das_notas_recebidas_no_mes** — user com 3 respostas [5, 4, 3] → media 4.0.

    - **test_var_faturamento_media_das_variacoes_por_empresa** — carteira 3 empresas com variações -2%/+7%/+4% → var_faturamento_pct = 3.00.

    - **test_var_faturamento_exclui_empresa_nova_da_media** — 3 empresas onde 1 é nova (pivot criado há 15 dias). Só as 2 antigas contam. Verificar `empresas_com_baseline == 2`.

    - **test_var_margem_usa_adman_como_fonte_canonica** — mockar caseFor='so-ml' para uma empresa, mas contribution_margin no Adman. Compute deve pegar var_margem via AdmanMetric mesmo com case ML.

    - **test_absenteismo_retorna_null_sempre** — mesmo com carteira, `componentes.absenteismo_pct === null`.

    - **test_2_meses_consecutivos_intermediario_promove_para_maximo** — criar snapshot junho com classificacao=intermediario + fixture julho compute que naturalmente cai em intermediario (via reengenharia dos revenues NPS/faturamento/margem para produzir nota_final entre 4.50 e 4.99). Assertar `faixa_bonus === 'maximo'` + `faixa_promovida === true`.

    - **test_user_sem_carteira_retorna_sem_carteira_true_com_motivo_pt_br** — user sem `company_users` no mês → `sem_carteira === true`, `motivo` contém "Sem carteira em" e nome do mês em pt-BR ("julho" ou "agosto").

    - **test_provider_ml_first_com_adman_fallback** — 3 empresas:
      - Empresa A: caseFor=so-ml, provider stub retorna revenue 10000 mês atual, 9000 mês anterior → +11.11%.
      - Empresa B: caseFor=so-adman, AdmanMetric revenue 5000/4500 → +11.11%.
      - Empresa C: caseFor=none → EXCLUÍDA (empresas_com_baseline não conta).
      Assertar que `empresas_com_baseline === 2` (só A e B) e `var_faturamento_pct` reflete média de A+B.

    7. Reference PHPUnit 11 attributes: `use PHPUnit\Framework\Attributes\Test;` e `#[Test]` acima de cada método.

    8. Comentários pt-BR em cada teste citando o REQ endereçado.

    9. Nomenclatura de arquivo/classe segue padrão `tests/Feature/Phase74/DesempenhoScoreServiceTest.php` (D-26).

    **Ordem sugerida de escrita**: helpers → fixture Carlos → âncora → demais → provider swap.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter="DesempenhoScoreServiceTest" --stop-on-failure 2>&1 | tail -40</automated>
    Todos os testes DEVEM passar. Se algum falha, corrigir no service (Plan 74-03) ou no teste.
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --filter=DesempenhoScoreServiceTest` retorna exit code 0.
    - Pelo menos 11 testes definidos (grep `#[Test]` >= 11).
    - Teste `test_fixture_carlos_retorna_nota_3_35_sem_bonus` presente e passa.
    - Arquivo tem >= 300 linhas.
    - Nenhum teste marcado como `$this->markTestSkipped` ou `$this->markTestIncomplete`.
    - `use RefreshDatabase;` no topo da classe.
    - `PRAGMA foreign_keys = ON` no setUp().
    - Bind de MetricsProviderFactory stub no setUp().
  </acceptance_criteria>
  <done>Suite verde; fixture Carlos passa como âncora.</done>
</task>

</tasks>

<verification>
Rodar `vendor/bin/phpunit tests/Feature/Phase74/DesempenhoScoreServiceTest.php` — 11+ testes passando, 0 falhas. Se o teste Carlos falha, é sinal de bug na engine v2 (Plan 74-03) — reportar via SUMMARY.
</verification>

<success_criteria>
- Todos os 11+ testes verdes.
- Fixture Carlos assertando o valor exato 3.35 + sem_bonus.
- Provider stub isolado (não chama HTTP real).
- Zero flakiness (Carbon::setTestNow congelado).
- Reflete decisões D-27/D-28/D-29 do CONTEXT.
</success_criteria>

<output>
Criar `.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-09-SUMMARY.md` ao concluir, listando arquivo criado, output do phpunit filter, número de testes verdes, REQs DESEMP-01..11 endereçados.
</output>
