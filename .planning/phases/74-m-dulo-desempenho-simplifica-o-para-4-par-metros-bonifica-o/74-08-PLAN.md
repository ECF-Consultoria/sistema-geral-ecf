---
phase: 74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o
plan: 08
type: execute
wave: 4
depends_on: [74-02]
files_modified:
  - app/Http/Controllers/ManualController.php
  - resources/js/Pages/Manual/Show.jsx
  - resources/js/Pages/Manual/artigos.js
  - resources/js/Pages/Manual/Artigos/DesempenhoBonificacao.jsx
autonomous: true
requirements: [DESEMP-13]

must_haves:
  truths:
    - "Rota /manual/desempenho-bonificacao retorna 200 e renderiza o artigo"
    - "Artigo lista as faixas de bonus_faixas ATIVAS em ordem crescente"
    - "Admin edita nota_min/nota_max/descricao em /desempenho/configuracao → reload em /manual/desempenho-bonificacao reflete a mudança"
    - "Manual/artigos.js expõe entry 'desempenho-bonificacao' com título 'Régua de Bonificação — Desempenho'"
    - "ManualController::show passa prop bonus_faixas quando slug = 'desempenho-bonificacao'"
  artifacts:
    - path: "resources/js/Pages/Manual/Artigos/DesempenhoBonificacao.jsx"
      provides: "Artigo dinâmico do Manual com tabela de faixas"
      contains: "bonus_faixas"
    - path: "resources/js/Pages/Manual/artigos.js"
      provides: "Entry desempenho-bonificacao no catálogo"
      contains: "desempenho-bonificacao"
    - path: "app/Http/Controllers/ManualController.php"
      provides: "show() com lookup condicional de props por slug"
      contains: "bonus_faixas"
  key_links:
    - from: "app/Http/Controllers/ManualController.php"
      to: "App\\Models\\BonusFaixa"
      via: "ativas + ordenadas + get"
      pattern: "BonusFaixa::ativas"
    - from: "resources/js/Pages/Manual/Show.jsx"
      to: "resources/js/Pages/Manual/Artigos/DesempenhoBonificacao.jsx"
      via: "artigos.js Component + props spread"
      pattern: "props"
---

<objective>
Criar o artigo dinâmico `/manual/desempenho-bonificacao` sincronizado em tempo real com a config `bonus_faixas`. Fecha REQ DESEMP-13 (D-22, D-23, D-24, D-25).

Purpose: o time Performance abre um link e vê a régua atualizada — sem staleness, sem cache, sem precisar de deploy quando a admin ajusta as faixas.

Output: 1 controller atualizado + 1 wrapper Show.jsx atualizado + 1 catálogo artigos.js atualizado + 1 componente novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md
@app/Http/Controllers/ManualController.php
@app/Models/BonusFaixa.php
@resources/js/Pages/Manual/Show.jsx
@resources/js/Pages/Manual/artigos.js
@resources/js/Pages/Manual/Artigos/Cronograma.jsx

<interfaces>
Fluxo esperado após esta phase:

```
Browser → /manual/desempenho-bonificacao
       → ManualController::show('desempenho-bonificacao')
       → if slug === 'desempenho-bonificacao': $faixas = BonusFaixa::where('ativo', true)->orderBy('ordem')->get()
       → Inertia::render('Manual/Show', ['slug' => $slug, 'artigoProps' => ['bonus_faixas' => $faixas, 'metodologia_texto' => '...']])
       → Manual/Show.jsx: buscarArtigo(slug) → artigo.Component
       → <Component {...artigoProps} />
       → DesempenhoBonificacao.jsx renderiza tabela de $bonus_faixas
```

Estrutura das faixas passadas para o componente:

```
bonus_faixas: Array<{
  id, slug, nome, descricao, nota_min, nota_max, ordem, ativo
}>
metodologia_texto: string   // parágrafo pt-BR explicativo dos 4 parâmetros — hardcoded no controller
```

Alterações mínimas necessárias:

**A) app/Http/Controllers/ManualController.php**:
- Método `show(string $slug)` recebe slug (já existe).
- Adicionar switch/if para carregar props por slug:
  ```
  $artigoProps = [];
  if ($slug === 'desempenho-bonificacao') {
      $artigoProps = [
          'bonus_faixas' => \App\Models\BonusFaixa::where('ativo', true)->orderBy('ordem')->get(),
          'metodologia_texto' => '<parágrafo pt-BR fixo>',
      ];
  }
  return Inertia::render('Manual/Show', ['slug' => $slug, 'artigoProps' => $artigoProps]);
  ```
- Cabeçalho pt-BR cita Phase 74 D-24/DESEMP-13.

**B) resources/js/Pages/Manual/Show.jsx**:
- Atualmente recebe apenas `{ slug }`. Passar a receber `{ slug, artigoProps }`.
- Passar `artigoProps` como spread para `<Component {...artigoProps} />` (linha final onde renderiza Component).
- Se `artigoProps` ausente/vazio, renderizar `<Component />` sem props (backwards-compat com Cronograma.jsx que não usa props).

**C) resources/js/Pages/Manual/artigos.js**:
- Adicionar import: `import DesempenhoBonificacao from './Artigos/DesempenhoBonificacao';`.
- Adicionar entry ao objeto `ARTIGOS`:
  ```
  'desempenho-bonificacao': {
      slug: 'desempenho-bonificacao',
      titulo: 'Régua de Bonificação — Desempenho',
      categoria: 'Módulo Desempenho',
      descricao: 'Como calculamos a nota final e a faixa de bônus mensal do time Performance.',
      Component: DesempenhoBonificacao,
  },
  ```

**D) resources/js/Pages/Manual/Artigos/DesempenhoBonificacao.jsx** (novo):
- Recebe props `{ bonus_faixas, metodologia_texto }`.
- Renderiza:
  1. Bloco introdutório: título + parágrafo `metodologia_texto` (recebido do backend — pode ser hardcoded no controller).
  2. Seção "Os 4 parâmetros da nota final" — texto explicativo pt-BR curto para cada:
     - NPS médio (0-5): "Média das notas NPS que o analista/estrategista recebeu no mês. Sem respostas → nota 0 (penaliza)."
     - Var. Faturamento (%): "Média das variações percentuais de faturamento vs mês anterior por empresa da carteira. Empresas novas (menos de 2 meses) não contam. Fonte: ML OAuth primeiro, Adman fallback."
     - Var. Margem (%): "Análogo do faturamento, para margem de contribuição. Fonte: Adman canônico."
     - Absenteísmo: "Em standby nesta versão. Fonte de dados em definição pela diretoria."
  3. Bloco "Fórmula da nota final": `nota_final = média(NPS, Var. Faturamento, Var. Margem)` — LaTeX/monospace formatado em bloco destacado.
  4. Tabela DINÂMICA das faixas de bônus (renderizada a partir de `bonus_faixas`):
     - Colunas: Faixa (nome + slug) | Nota mínima | Nota máxima | Descrição.
     - Iterar `bonus_faixas.map((faixa) => <tr>...</tr>)`.
  5. Bloco "Regra especial": pt-BR "2 meses consecutivos em Intermediário promovem automaticamente para Máximo."
  6. Rodapé pequeno: "Faixas configuráveis por admins em /desempenho/configuracao."
- Estilo: dark/glass consistente com Cronograma.jsx (`bg-ecf-card`, `border-white/[0.08]`, `rounded-2xl`, `p-6`).

Cache: NENHUM — nada de Cache::remember (D-25). BonusFaixa::where('ativo', true)->orderBy('ordem')->get() na render — permite reload imediato pós-edição.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar ManualController::show para passar artigoProps</name>
  <files>app/Http/Controllers/ManualController.php</files>
  <read_first>
    - app/Http/Controllers/ManualController.php (atual — 26 linhas)
    - app/Models/BonusFaixa.php (Plan 74-02) — scopes ativas/ordenadas + método classificar
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-24)
  </read_first>
  <action>
    Editar `app/Http/Controllers/ManualController.php`:

    1. Adicionar `use App\Models\BonusFaixa;` no topo.
    2. Atualizar `show(string $slug)`:
       ```
       $artigoProps = [];
       if ($slug === 'desempenho-bonificacao') {
           $artigoProps = [
               'bonus_faixas' => BonusFaixa::where('ativo', true)->orderBy('ordem')->get(['id','slug','nome','descricao','nota_min','nota_max','ordem','ativo']),
               'metodologia_texto' => 'O score de desempenho da equipe Performance é calculado mensalmente a partir de 4 parâmetros — NPS médio, variação de faturamento, variação de margem de contribuição e absenteísmo (em standby). A nota final é a média direta dos 3 primeiros; a faixa de bônus é determinada pela régua abaixo, configurável pela administração.',
           ];
       }
       return Inertia::render('Manual/Show', [
           'slug' => $slug,
           'artigoProps' => $artigoProps,
       ]);
       ```
    3. Docblock class-level: adicionar linha "Phase 74 D-24/DESEMP-13 — slug 'desempenho-bonificacao' recebe prop `bonus_faixas` (rows ativas ordenadas) para render dinâmico no artigo."
    4. Não alterar `index()` — permanece igual.
  </action>
  <verify>
    <automated>php artisan route:list --name=manual.show 2>&1 | head -5 && curl -s -o /dev/null -w "%{http_code}" "http://localhost/manual/desempenho-bonificacao" 2>&1 || true</automated>
    Se o server local não estiver rodando, verificação via grep:
    `grep -q "bonus_faixas" app/Http/Controllers/ManualController.php`
  </verify>
  <acceptance_criteria>
    - `grep -q "BonusFaixa" app/Http/Controllers/ManualController.php` retorna 0.
    - `grep -q "desempenho-bonificacao" app/Http/Controllers/ManualController.php` retorna 0.
    - `grep -q "artigoProps" app/Http/Controllers/ManualController.php` retorna 0.
    - `php -l app/Http/Controllers/ManualController.php` retorna "No syntax errors".
    - Rota `manual.show` continua funcional (não quebrou `Cronograma`).
  </acceptance_criteria>
  <done>ManualController passa artigoProps por slug + query BonusFaixa quando aplicável.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar Manual/Show.jsx para spread de artigoProps + adicionar entry em artigos.js</name>
  <files>resources/js/Pages/Manual/Show.jsx, resources/js/Pages/Manual/artigos.js</files>
  <read_first>
    - resources/js/Pages/Manual/Show.jsx (atual — 55 linhas)
    - resources/js/Pages/Manual/artigos.js (catálogo atual)
    - resources/js/Pages/Manual/Artigos/Cronograma.jsx (padrão de artigo — para consistência estilística)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-22, D-23)
  </read_first>
  <action>
    **Sub-tarefa 2A — resources/js/Pages/Manual/Show.jsx**:
    - Assinatura: `export default function Show({ slug, artigoProps = {} }) { ... }`.
    - No JSX onde renderiza `<Component />`, trocar para `<Component {...artigoProps} />`.
    - Preservar branch "Artigo não encontrado" quando `!artigo`.
    - Não alterar breadcrumb nem AppLayout wrapper.

    **Sub-tarefa 2B — resources/js/Pages/Manual/artigos.js**:
    - Adicionar linha de import: `import DesempenhoBonificacao from './Artigos/DesempenhoBonificacao';` abaixo do import Cronograma.
    - Adicionar entry ao objeto ARTIGOS:
      ```
      'desempenho-bonificacao': {
          slug: 'desempenho-bonificacao',
          titulo: 'Régua de Bonificação — Desempenho',
          categoria: 'Módulo Desempenho',
          descricao: 'Como calculamos a nota final e a faixa de bônus mensal do time Performance.',
          Component: DesempenhoBonificacao,
      },
      ```
    - Manter `cronograma` entry inalterado. Preservar `listarArtigos`/`buscarArtigo`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('resources/js/Pages/Manual/artigos.js','utf8'); if(!s.includes('desempenho-bonificacao')) throw new Error('missing entry'); if(!s.includes('DesempenhoBonificacao')) throw new Error('missing import'); const s2=require('fs').readFileSync('resources/js/Pages/Manual/Show.jsx','utf8'); if(!/artigoProps/.test(s2)) throw new Error('Show missing artigoProps spread'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `resources/js/Pages/Manual/artigos.js` contém entry `'desempenho-bonificacao'` com Component: DesempenhoBonificacao.
    - Import de `./Artigos/DesempenhoBonificacao` presente.
    - `resources/js/Pages/Manual/Show.jsx` faz spread `{...artigoProps}` no `<Component />`.
    - Assinatura de Show inclui `artigoProps = {}` como prop default.
  </acceptance_criteria>
  <done>Wrapper e catálogo atualizados para artigo dinâmico.</done>
</task>

<task type="auto">
  <name>Task 3: Criar componente Manual/Artigos/DesempenhoBonificacao.jsx</name>
  <files>resources/js/Pages/Manual/Artigos/DesempenhoBonificacao.jsx</files>
  <read_first>
    - resources/js/Pages/Manual/Artigos/Cronograma.jsx (padrão visual + estrutura de artigo — dark/glass tokens, tipografia)
    - app/Models/BonusFaixa.php (colunas expostas pelo controller)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md (DESEMP-13)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-25)
  </read_first>
  <action>
    Criar `resources/js/Pages/Manual/Artigos/DesempenhoBonificacao.jsx`:

    1. Assinatura: `export default function DesempenhoBonificacao({ bonus_faixas = [], metodologia_texto = '' }) { ... }`.
    2. Imports mínimos: `import { cn } from '@/lib/utils';` (opcional se usar `cn`).
    3. Layout dentro de wrapper padrão `<article className="prose prose-invert max-w-none space-y-6">`.

    4. Seções:
       - `<header>`: título grande "Régua de Bonificação — Desempenho" (`text-white text-2xl font-display font-bold`) + `<p className="text-white/60">{metodologia_texto}</p>`.
       - `<section>` "Os 4 parâmetros da nota final" — cabeçalho `text-ecf-yellow text-sm uppercase tracking-wider` + 4 sub-blocos (`<div>` cada) descrevendo NPS/Faturamento/Margem/Absenteísmo conforme texto pt-BR do `<interfaces>`.
       - `<section>` "Fórmula da nota final" — box destacado `bg-ecf-yellow/5 border border-ecf-yellow/20 rounded-2xl p-4` contendo `<code>nota_final = média(NPS, Var. Faturamento, Var. Margem)</code>`. Subtítulo: "Absenteísmo NÃO participa nesta versão."
       - `<section>` "Faixas de bonificação" — tabela dinâmica:
         ```
         <table className="w-full text-left text-sm">
           <thead className="text-ecf-yellow uppercase text-xs">
             <tr><th className="p-3">Faixa</th><th className="p-3">Nota mín.</th><th className="p-3">Nota máx.</th><th className="p-3">Descrição</th></tr>
           </thead>
           <tbody>
             {bonus_faixas.map(f => (
               <tr key={f.id} className="border-t border-white/[0.06]">
                 <td className="p-3"><span className="text-white font-semibold">{f.nome}</span><span className="text-white/40 text-xs block font-mono">{f.slug}</span></td>
                 <td className="p-3 text-white/70">{f.nota_min}</td>
                 <td className="p-3 text-white/70">{f.nota_max}</td>
                 <td className="p-3 text-white/50 text-xs">{f.descricao ?? '—'}</td>
               </tr>
             ))}
           </tbody>
         </table>
         ```
         Envolver em `<div className="bg-ecf-card border border-white/[0.08] rounded-2xl overflow-hidden">`.
       - `<section>` "Regra especial: promoção por 2 meses consecutivos" — texto pt-BR curto explicando DESEMP-08.
       - `<footer className="text-white/40 text-xs">`: "Régua editada pela administração em /desempenho/configuracao. Mudanças refletem imediatamente aqui — sem cache."

    5. Comentários pt-BR citando Phase 74 D-22/D-25/DESEMP-13 no topo.

    6. Se `bonus_faixas.length === 0`, renderizar placeholder cinza "Nenhuma faixa ativa configurada."
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('resources/js/Pages/Manual/Artigos/DesempenhoBonificacao.jsx','utf8'); const t=['bonus_faixas','metodologia_texto','2 meses consecutivos','nota_final','Absenteísmo']; const m=t.filter(x=>!s.includes(x)); if(m.length){console.error('MISSING',m);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe e passa validação de tokens acima.
    - Tabela renderiza dinamicamente `bonus_faixas.map`.
    - Texto pt-BR explicando os 4 parâmetros.
    - Regra especial "2 meses consecutivos" mencionada.
    - Placeholder para lista vazia.
    - Design dark/glass consistente com Cronograma.
  </acceptance_criteria>
  <done>Artigo dinâmico pronto, consumindo props do backend.</done>
</task>

<task type="auto">
  <name>Task 4: Rodar build do frontend</name>
  <files>public/build/manifest.json</files>
  <read_first>
    - package.json
  </read_first>
  <action>
    Executar `npm run build`. Se erro de import (path `./Artigos/DesempenhoBonificacao`), verificar case-sensitivity do filename.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Build exit 0.
    - Nenhum erro na saída.
    - `public/build/manifest.json` inclui referência ao novo artigo (grep `DesempenhoBonificacao`).
  </acceptance_criteria>
  <done>Build limpo; artigo pronto para servir.</done>
</task>

</tasks>

<verification>
Login como admin, acessar `/manual/desempenho-bonificacao` → deve retornar 200 + renderizar tabela com as 4 faixas seed. Editar em `/desempenho/configuracao` alterando `intermediario.nota_min` para 4.60 → recarregar `/manual/desempenho-bonificacao` → tabela mostra 4.60 sem precisar de deploy.
</verification>

<success_criteria>
- Rota /manual/desempenho-bonificacao funcional (200).
- Tabela reflete faixas ATIVAS em tempo real.
- Admin edita → reload no manual mostra novo valor.
- Zero cache/staleness.
- Build compilando.
</success_criteria>

<output>
Criar `.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-08-SUMMARY.md` ao concluir, listando 4 arquivos, output do build, teste manual do fluxo edição→reload, decisões D-22..25, REQ DESEMP-13.
</output>
