---
phase: 74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_09_140001_alter_desempenho_score_snapshots_add_mes_referencia.php
  - app/Models/DesempenhoScoreSnapshot.php
autonomous: true
requirements: [DESEMP-09]

must_haves:
  truths:
    - "Tabela desempenho_score_snapshots tem coluna mes_referencia DATE NULL"
    - "Unique key antigo (user_id, ref_date) foi substituído por (user_id, ref_date, mes_referencia)"
    - "Snapshots diários existentes continuam válidos (mes_referencia = NULL)"
    - "Model DesempenhoScoreSnapshot expõe cast 'mes_referencia' => 'date'"
  artifacts:
    - path: "database/migrations/2026_07_09_140001_alter_desempenho_score_snapshots_add_mes_referencia.php"
      provides: "Alter migration para coluna mes_referencia + rewire unique"
      contains: "mes_referencia"
    - path: "app/Models/DesempenhoScoreSnapshot.php"
      provides: "Model com fillable/cast atualizados"
      contains: "mes_referencia"
  key_links:
    - from: "app/Models/DesempenhoScoreSnapshot.php"
      to: "desempenho_score_snapshots"
      via: "cast + fillable"
      pattern: "mes_referencia"
---

<objective>
Evoluir a tabela `desempenho_score_snapshots` para suportar as duas modalidades (diário + mensal fechado) coexistindo no mesmo storage (D-01, D-02, D-03).

Purpose: fundação de schema que destrava o novo `DesempenhoScoreService` (Plan 74-03) e o novo comando `desempenho:consolidar-mes` (Plan 74-04) sem quebrar o histórico v1 nem o snapshot diário existente.

Output: 1 migration alter idempotente + Model atualizado com cast/fillable de `mes_referencia`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md
@database/migrations/2026_06_30_000001_create_desempenho_score_snapshots_table.php
@app/Models/DesempenhoScoreSnapshot.php

<interfaces>
Schema alvo (após migration):

Colunas atuais preservadas: id, user_id (FK cascade), ref_date (DATE), score (unsignedTinyInteger), classificacao (string 16), ranking_pos (unsignedSmallInteger NULL), tem_base_comparativa (boolean default false), empresas_carteira (unsignedSmallInteger default 0), empresas_eligiveis (unsignedSmallInteger default 0), breakdown_json (json), timestamps.

Coluna nova: `mes_referencia` DATE NULL — semântica:
- NULL → row é snapshot diário legado (D-02).
- YYYY-MM-01 → row é snapshot mensal fechado.

Índices/uniques:
- DROP unique legado `desempenho_score_snapshots_user_id_ref_date_unique` (`user_id`, `ref_date`).
- CREATE unique novo `(user_id, ref_date, mes_referencia)`.
- Preservar índice `(ref_date, score)` existente.
- CREATE índice `(mes_referencia, score)` para consultas do ranking mensal (`SnapshotDesempenhoScores`/`ConsolidarMesDesempenho` + `PerformanceController::index`).

Regras de segurança:
- Migration idempotente: `Schema::hasColumn` guard antes de adicionar; `hasIndex`/try-catch para unique legado.
- Não backfillar `mes_referencia` em rows históricas — permanece NULL (D-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration alter para mes_referencia + reorganizar unique</name>
  <files>database/migrations/2026_07_09_140001_alter_desempenho_score_snapshots_add_mes_referencia.php</files>
  <read_first>
    - database/migrations/2026_06_30_000001_create_desempenho_score_snapshots_table.php (schema atual + estilo de migration idempotente Schema::hasTable)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md (requisito DESEMP-09)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (decisões D-01, D-02, D-03)
  </read_first>
  <action>
    Criar nova migration idempotente `2026_07_09_140001_alter_desempenho_score_snapshots_add_mes_referencia.php` que, em `up()`:

    1. Se `Schema::hasColumn('desempenho_score_snapshots', 'mes_referencia')` === false, adicionar coluna `$table->date('mes_referencia')->nullable()->after('ref_date')`. Não backfillar valor — snapshots históricos v1 permanecem com `mes_referencia = NULL` (D-01, spec DESEMP-14).
    2. Dropar unique legado `(user_id, ref_date)`. Nome canônico Laravel: `desempenho_score_snapshots_user_id_ref_date_unique`. Envolver em `try/catch \Throwable` para tolerar rerun em ambiente sem o índice (idempotente). Preferir `Schema::table` + `dropUnique(['user_id', 'ref_date'])`.
    3. Criar unique novo `(user_id, ref_date, mes_referencia)` com nome custom `desempenho_score_snapshots_user_ref_mes_unique` (encurta pra <64 chars — MariaDB limit). Usar guard `try/catch` para idempotência (rerun não deve crashar).
    4. Criar índice não-unique `(mes_referencia, score)` com nome `desempenho_score_snapshots_mes_score_idx` para acelerar ranking mensal. Envolver em `try/catch`.

    Em `down()`: dropar índice/unique novos e recriar unique antigo `(user_id, ref_date)` no rollback. Também remover coluna `mes_referencia`.

    Comentar em pt-BR no cabeçalho o motivo (Phase 74 D-01/D-02/D-03), a semântica dos valores (NULL = diário legado, YYYY-MM-01 = mensal fechado) e a estratégia idempotente. Sem heredoc — usar closure padrão Laravel `return new class extends Migration { public function up(): void { ... } public function down(): void { ... } };`.
  </action>
  <verify>
    <automated>php artisan migrate --pretend 2>&1 | tail -30</automated>
    Ver a saída SQL da nova migration incluir `ALTER TABLE ... ADD COLUMN mes_referencia`, DROP unique legado e CREATE unique/index novos.
  </verify>
  <acceptance_criteria>
    - Arquivo `database/migrations/2026_07_09_140001_alter_desempenho_score_snapshots_add_mes_referencia.php` existe.
    - `php artisan migrate --pretend` inclui `ADD COLUMN "mes_referencia" DATE` e `CREATE UNIQUE INDEX ... user_ref_mes_unique` na saída.
    - Método `down()` presente e reverte a operação (drop coluna + drop novo unique/index + recriar unique antigo).
    - Cabeçalho da migration cita Phase 74 + D-01/D-02/D-03 em pt-BR.
    - Rerun `php artisan migrate` (após aplicada) NÃO gera erro de "column already exists" (guard `Schema::hasColumn`).
  </acceptance_criteria>
  <done>Migration criada, idempotente, aplicável e reversível.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar Model DesempenhoScoreSnapshot com fillable + cast de mes_referencia</name>
  <files>app/Models/DesempenhoScoreSnapshot.php</files>
  <read_first>
    - app/Models/DesempenhoScoreSnapshot.php (Model atual, fillable e casts)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (decisões D-01, D-02, D-03)
  </read_first>
  <action>
    Editar `app/Models/DesempenhoScoreSnapshot.php`:

    1. Adicionar `'mes_referencia'` ao array `$fillable` (após `'ref_date'`, mantendo ordem semântica).
    2. Adicionar cast `'mes_referencia' => 'date'` ao array `$casts` (para `Carbon` na hidratação — mesmo padrão de `ref_date`).
    3. Atualizar docblock do class-level para documentar que agora existem 2 modalidades de row:
       - `ref_date` populada, `mes_referencia = NULL` → snapshot diário (Phase 46, preservado).
       - `ref_date = mes_referencia = YYYY-MM-01` → snapshot mensal fechado (Phase 74 D-02).
       Citar D-02 do 74-CONTEXT.md como fonte de decisão.
    4. Adicionar scope de conveniência `scopeMensal($query)` que filtra `whereNotNull('mes_referencia')` e scope `scopeDiario($query)` que filtra `whereNull('mes_referencia')`. Ambos com PHPDoc pt-BR.
    5. NÃO adicionar relacionamento novo (`user()` continua único). NÃO adicionar mais colunas ao `$fillable` além de `mes_referencia`.
  </action>
  <verify>
    <automated>php artisan tinker --execute="print_r((new App\Models\DesempenhoScoreSnapshot)->getFillable()); print_r((new App\Models\DesempenhoScoreSnapshot)->getCasts());"</automated>
    Verificar que a saída contém `mes_referencia` no fillable e `mes_referencia => date` nos casts.
  </verify>
  <acceptance_criteria>
    - `grep -q "'mes_referencia'" app/Models/DesempenhoScoreSnapshot.php` retorna 0.
    - `grep -q "mes_referencia' => 'date'" app/Models/DesempenhoScoreSnapshot.php` retorna 0.
    - `grep -q "scopeMensal\|scopeDiario" app/Models/DesempenhoScoreSnapshot.php` retorna 0.
    - Docblock cita Phase 74 D-02.
    - Nenhuma outra coluna adicionada ao fillable ou casts.
  </acceptance_criteria>
  <done>Model atualizado com fillable/cast de mes_referencia + 2 scopes de conveniência + docblock atualizado.</done>
</task>

</tasks>

<verification>
Executar `php artisan migrate` em banco sqlite de teste e confirmar que a coluna aparece via `PRAGMA table_info(desempenho_score_snapshots)` ou similar (MariaDB: `DESCRIBE desempenho_score_snapshots`). Rerun da migration não deve gerar erro.
</verification>

<success_criteria>
- Coluna `mes_referencia DATE NULL` presente em `desempenho_score_snapshots`.
- Unique legado (user_id, ref_date) foi substituído por unique (user_id, ref_date, mes_referencia).
- Índice (mes_referencia, score) criado.
- Model expõe cast/fillable + scopes `mensal`/`diario`.
- Migration idempotente (rerun sem erro).
- Zero perda de dados históricos v1.
</success_criteria>

<output>
Criar `.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-01-SUMMARY.md` ao concluir, listando arquivos criados/alterados, comando de migrate rodado, decisões implementadas (D-01/D-02/D-03) e link para o SPEC DESEMP-09.
</output>
