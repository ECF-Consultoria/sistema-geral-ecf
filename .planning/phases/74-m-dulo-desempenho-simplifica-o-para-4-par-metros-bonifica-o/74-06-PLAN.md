---
phase: 74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o
plan: 06
type: execute
wave: 4
depends_on: [74-04]
files_modified:
  - resources/js/Pages/Performance/Dashboard.jsx
  - resources/js/Pages/Performance/Index.jsx
  - resources/js/Pages/Performance/Show.jsx
autonomous: true
requirements: [DESEMP-04, DESEMP-05, DESEMP-06, DESEMP-10, DESEMP-14]

must_haves:
  truths:
    - "Performance/Dashboard.jsx renderiza cards de 4 parâmetros (NPS/Faturamento/Margem/Absenteísmo)"
    - "Card Absenteísmo exibe placeholder '—' + badge amarelo 'Em breve'"
    - "Dashboard abre em VIEW MENSAL por default (mês fechado mais recente); toggle 'Ver diário' alterna para série rolling 30d"
    - "Performance/Index.jsx (ranking) filtra usuários sem_carteira=true"
    - "Performance/Show.jsx mostra card por parâmetro + toggle de mês"
    - "Dashboard filtra snapshots por mes_referencia >= '2026-08-01' (não mistura scores v1)"
    - "Faixa promovida via regra '2 meses consecutivos' recebe badge visual de destaque"
  artifacts:
    - path: "resources/js/Pages/Performance/Dashboard.jsx"
      provides: "Dashboard consolidado v2 com 4 cards de parâmetros"
      contains: "faixa_bonus"
    - path: "resources/js/Pages/Performance/Index.jsx"
      provides: "Ranking filtrando sem_carteira"
      contains: "sem_carteira"
    - path: "resources/js/Pages/Performance/Show.jsx"
      provides: "View individual do analista/estrategista"
      contains: "faixa_bonus"
  key_links:
    - from: "resources/js/Pages/Performance/Dashboard.jsx"
      to: "props Inertia do PerformanceController::dashboardCarteira"
      via: "usePage props com shape novo (nota_final, faixa_bonus, componentes)"
      pattern: "nota_final|faixa_bonus"
    - from: "resources/js/Pages/Performance/Show.jsx"
      to: "componentes.absenteismo_pct"
      via: "renderização placeholder"
      pattern: "Em breve"
---

<objective>
Reescrever os 3 componentes React do módulo Performance para consumir o novo shape de props do `DesempenhoScoreService`: 4 cards de parâmetros (NPS/Faturamento/Margem/Absenteísmo), view mensal default com toggle diário, filtro `sem_carteira` no ranking, badge "Em breve" no Absenteísmo, filtro por `mes_referencia >= 2026-08-01`. Fecha REQs DESEMP-04, DESEMP-05, DESEMP-06, DESEMP-10 e DESEMP-14 no lado UI.

Purpose: entrega a UI que o time Performance vê no dia 1 do mês seguinte pra saber sua faixa de bônus.

Output: 3 arquivos JSX reescritos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-SPEC.md
@.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md
@resources/js/Pages/Performance/Dashboard.jsx
@resources/js/Pages/Performance/Index.jsx
@resources/js/Pages/Performance/Show.jsx
@resources/js/Pages/Nps/Index.jsx
@app/Services/DesempenhoScoreService.php
@app/Http/Controllers/PerformanceController.php

<interfaces>
Shape de props recebido do PerformanceController::index (Plan 74-04):

```
ranking: Array<{
  id, name, role, cargo_slug, cargo_label,
  empresas_carteira, empresas_com_baseline,
  nota_final: number | null,
  faixa_bonus: 'sem_bonus'|'basico'|'intermediario'|'maximo'|null,
  faixa_promovida: boolean,
  componentes: {
    nps_medio: number|null,
    var_faturamento_pct: number|null,
    var_margem_pct: number|null,
    absenteismo_pct: null,          // sempre null nesta phase (DESEMP-06)
  },
  sem_carteira: boolean,
  motivo: string|null,
  delta_vs_mes_passado: number|null,
  delta_vs_ontem: number|null,
  delta_vs_semana_passada: number|null,
  posicao: number,
}>
period: string
cargo: 'analista'|'estrategista'|null
mes_fechado: 'YYYY-MM-01'
```

Shape do dashboardCarteira (Performance/Dashboard.jsx):

```
resultado: { user_id, user_name, mes_referencia, sem_carteira, motivo,
             empresas_carteira, empresas_com_baseline,
             componentes: {...}, nota_final, faixa_bonus, faixa_promovida }
mes_atual: 'YYYY-MM-01'         // mês em curso parcial
mes_fechado: 'YYYY-MM-01'|null  // último mês fechado
resultado_mes_fechado: {...}|null
```

Design tokens (dark/glass — padrão Nps/Index.jsx redesign 2026-07-08):
- Card: `bg-ecf-card`, `border border-white/[0.08]`, `rounded-2xl`, `p-6`.
- Título card: `text-ecf-yellow`, `text-xs uppercase tracking-wider`.
- Valor destaque: `text-white text-3xl font-display font-bold`.
- Label secundário: `text-white/50 text-xs`.
- Badge "Em breve" (amarelo): `bg-ecf-yellow/10 text-ecf-yellow border border-ecf-yellow/30 rounded-full text-[10px] font-semibold uppercase px-2 py-0.5`.
- Badge promovida (D-08): `bg-emerald-500/10 text-emerald-300 border border-emerald-500/30`.

Filtros:
- Dashboard.jsx snapshots históricos: filtrar `mes_referencia >= '2026-08-01'` (DESEMP-14).
- Index.jsx ranking: `.filter(r => !r.sem_carteira)` antes de ranquear (DESEMP-10).
- Show.jsx: `resultado.sem_carteira` → renderizar badge amarelo grande "Sem carteira em {mes_extenso}" no lugar dos cards.

Labels pt-BR:
- Card 1: "NPS médio" · nota 0-5.
- Card 2: "Faturamento" · "% vs mês anterior" · `+3.0%` / `-2.5%` / `—`.
- Card 3: "Margem de contribuição" · "% vs mês anterior · fonte Adman".
- Card 4: "Absenteísmo" · "—" + badge "Em breve" + subtítulo "Fonte de dados em definição".
- Card faixa: label pt-BR (Sem bônus, Básico, Intermediário, Máximo) + nota_final.
- Badge promovida: "Promovida (2 meses consecutivos)" quando `faixa_promovida=true`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever Performance/Dashboard.jsx</name>
  <files>resources/js/Pages/Performance/Dashboard.jsx</files>
  <read_first>
    - resources/js/Pages/Performance/Dashboard.jsx (existente — estrutura AppLayout + charts a substituir)
    - resources/js/Pages/Nps/Index.jsx (padrão dark/glass 2026-07-08 — cores, radius, tipografia)
    - app/Http/Controllers/PerformanceController.php método dashboardCarteira (Plan 74-04) — shape enviado
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-18, D-19)
  </read_first>
  <action>
    Reescrever `resources/js/Pages/Performance/Dashboard.jsx`:

    1. Imports: `usePage` do `@inertiajs/react`, `useState, useMemo` do react, ícones Lucide `Star, TrendingUp, TrendingDown, Coins, UserX, Trophy, Calendar`, `AppLayout` de `@/Layouts/AppLayout`, `cn` de `@/lib/utils`.
    2. Componente default `Dashboard()`:
       - `const { resultado, mes_atual, mes_fechado, resultado_mes_fechado, serie_diaria } = usePage().props;`.
       - Estado `const [view, setView] = useState('mes_fechado');` — valores 'mes_fechado' | 'mes_atual' | 'diario'.
       - `const activeData = view === 'mes_fechado' ? resultado_mes_fechado : resultado;`.
       - Se `activeData?.sem_carteira`, renderizar bloco pt-BR com badge amarelo "Sem carteira no período" + motivo — NÃO renderizar cards.
    3. Cabeçalho: título "Desempenho — Carteira" + subtitulo com `mesExtenso(view === 'mes_fechado' ? mes_fechado : mes_atual)` marcando "(parcial)" quando em `mes_atual` (D-18).
    4. Toggle 3 opções (`mes_fechado`/`mes_atual`/`diario`) em `<div className="inline-flex bg-ecf-card border border-white/[0.08] rounded-2xl p-1">`.
    5. Grid `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-4 gap-4` com 4 cards:
       - Card 1 NPS: valor `activeData.componentes.nps_medio?.toFixed(2) ?? '—'`; escala max 5.0; label "Nota média das respostas NPS do mês".
       - Card 2 Faturamento: valor `formatPercent(activeData.componentes.var_faturamento_pct)`; ícone TrendingUp verde ou TrendingDown vermelho conforme sinal; label "% variação vs mês anterior".
       - Card 3 Margem: valor `formatPercent(activeData.componentes.var_margem_pct)`; ícone Coins; label "% variação vs mês anterior · fonte Adman".
       - Card 4 Absenteísmo: valor "—" + badge amarelo "Em breve" + label "Fonte de dados em definição" — SPEC DESEMP-06.
    6. Card destaque "Faixa de bônus" abaixo do grid (span full-width em `md:col-span-2 xl:col-span-4`):
       - Valor grande: `faixaLabel(activeData.faixa_bonus)` (helper: sem_bonus→"Sem bônus", basico→"Básico", intermediario→"Intermediário", maximo→"Máximo").
       - Nota final: `activeData.nota_final?.toFixed(2)` ou "—".
       - Se `activeData.faixa_promovida === true`, badge emerald "Promovida (2 meses consecutivos)".
       - Se `activeData.nota_final === null`, placeholder "Sem dados suficientes para classificação".
    7. Bloco "Empresas na carteira": "N empresas com baseline, M total" via `empresas_com_baseline`/`empresas_carteira`.
    8. View 'diario': se `serie_diaria` presente, renderizar Recharts LineChart consumindo `serie_diaria.filter(p => p.mes_referencia === null)`. Se ausente, esconder toggle 'diario'.
    9. Helpers embutidos:
       - `formatPercent(v)` → `v == null ? '—' : (v >= 0 ? '+' : '') + v.toFixed(1) + '%'`.
       - `faixaLabel(slug)` → mapa pt-BR.
       - `mesExtenso(iso)` → `new Date(iso).toLocaleDateString('pt-BR', {month:'long', year:'numeric'})`.
    10. Envolver tudo em `<AppLayout title="Desempenho — Carteira"><div className="max-w-7xl mx-auto space-y-6">...</div></AppLayout>`.
    11. Comentários pt-BR citando SPEC DESEMP-04/DESEMP-05/DESEMP-06/DESEMP-10/DESEMP-14 e D-18.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('resources/js/Pages/Performance/Dashboard.jsx','utf8'); const t=['faixa_bonus','faixa_promovida','sem_carteira','Em breve','var_faturamento_pct','var_margem_pct','nps_medio']; const m=t.filter(x=>!s.includes(x)); if(m.length){console.error('MISSING',m);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Todos os tokens do shape novo presentes: faixa_bonus, faixa_promovida, sem_carteira, Em breve, var_faturamento_pct, var_margem_pct, nps_medio.
    - Grid de 4 cards presente (grep por `md:grid-cols-2` ou `xl:grid-cols-4`).
    - Card Absenteísmo renderiza `Em breve` como badge.
    - Toggle 3 views: mes_fechado / mes_atual / diario.
    - Nenhuma referência a v1 removida: `crescimento_ajustado`, `execucao_ads`, `recuperacao`, `atingimento_meta`, `absenteeism_rate`.
  </acceptance_criteria>
  <done>Dashboard.jsx reescrito com 4 cards + faixa + toggle mês/diário + placeholder Absenteísmo.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever Performance/Index.jsx (ranking)</name>
  <files>resources/js/Pages/Performance/Index.jsx</files>
  <read_first>
    - resources/js/Pages/Performance/Index.jsx (existente — filtros cargo, ordenação, delta_vs_ontem)
    - app/Http/Controllers/PerformanceController.php::index (Plan 74-04) — shape novo
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-18, D-20)
  </read_first>
  <action>
    Reescrever `resources/js/Pages/Performance/Index.jsx`:

    1. Imports padrão do projeto (usePage, router, useState, useMemo; ícones Lucide; AppLayout; cn).
    2. `Index({ ranking, period, setor, cargo, mes_fechado })`:
       - `const rankingFiltrado = useMemo(() => ranking.filter(r => !r.sem_carteira), [ranking]);` — DESEMP-10.
       - `const semCarteira = useMemo(() => ranking.filter(r => r.sem_carteira), [ranking]);` — usado no bloco de exclusão transparente.
    3. Cabeçalho: "Ranking Performance — {mesExtenso(mes_fechado)}" + subtítulo "Ranking do mês fechado mais recente" (D-20). Se `mes_fechado` for null, subtítulo "Ranking do mês em curso (parcial)".
    4. Toggle cargo (Geral | Analista | Estrategista) navegando via `router.get(route('performance.index'), {cargo: X})`.
    5. Tabela colunas:
       - Posição.
       - Nome + chip `cargo_label`.
       - Nota final (número + tendência ▲/▼/─ por `delta_vs_mes_passado`).
       - Faixa de bônus (badge colorido por slug: sem_bonus cinza, basico azul, intermediario roxo, maximo dourado). Se `faixa_promovida`, badge emerald pequeno "PROMOVIDA".
       - Delta vs mês passado (`+0.15` / `-0.30` / `—`).
       - NPS médio.
       - Var Faturamento.
       - Var Margem.
       - Empresas (`empresas_com_baseline` / `empresas_carteira`).
       - Row inteira clicável, `router.get(route('performance.show', r.id))`.
    6. Bloco dropdown "Como calculamos?" (fechado por default): descrição pt-BR "Nota final = média(NPS, Var. Faturamento, Var. Margem). Absenteísmo em standby. Faixas configuráveis pelo admin em /desempenho/configuracao." + link `/manual/desempenho-bonificacao` (Plan 74-08).
    7. Bloco "Excluídos do ranking (sem carteira no período)" listando `semCarteira.map(r => r.name)` como chips cinza (DESEMP-10 transparência) — omitir bloco se `semCarteira.length === 0`.
    8. Design tokens dark/glass conforme padrão Nps/Index.jsx.
    9. Remover toggle `period` (7/30/90/180) se controller Plan 74-04 não mais aceitar. Preservar apenas se backend continuar recebendo.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('resources/js/Pages/Performance/Index.jsx','utf8'); const t=['sem_carteira','faixa_bonus','faixa_promovida','nota_final','delta_vs_mes_passado','var_faturamento_pct','var_margem_pct','nps_medio']; const m=t.filter(x=>!s.includes(x)); if(m.length){console.error('MISSING',m);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Todos os tokens presentes: sem_carteira, faixa_bonus, faixa_promovida, nota_final, delta_vs_mes_passado, var_faturamento_pct, var_margem_pct, nps_medio.
    - Filtro `.filter(r => !r.sem_carteira)` presente no useMemo.
    - Link para `/desempenho/configuracao` OU `desempenho.configuracao.index` presente.
    - Link para `/manual/desempenho-bonificacao` presente.
    - Nenhuma métrica v1 residual: `crescimento_ajustado_pct`, `execucao_ads_pct`, `recuperacao_pct`, `atingimento_meta_pct`.
  </acceptance_criteria>
  <done>Index.jsx reescrito com filtro sem_carteira + colunas novas + transparência de exclusão.</done>
</task>

<task type="auto">
  <name>Task 3: Reescrever Performance/Show.jsx (view individual)</name>
  <files>resources/js/Pages/Performance/Show.jsx</files>
  <read_first>
    - resources/js/Pages/Performance/Show.jsx (existente — estrutura de página de detalhe)
    - resources/js/Pages/Performance/Dashboard.jsx (Task 1 desta wave — reusar helpers formatPercent, faixaLabel, mesExtenso; extrair para local util se conveniente)
    - .planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-CONTEXT.md (D-19)
  </read_first>
  <action>
    Reescrever `resources/js/Pages/Performance/Show.jsx`:

    1. `Show({ user, resultado, mes_fechado, meses_disponiveis })`:
       - Estado `[mesSelecionado, setMesSelecionado] = useState(mes_fechado)`.
       - Toggle para escolher outro mês fechado (via `router.get(route('performance.show', user.id), {mes: mesSelecionado})`).
    2. Cabeçalho: "Desempenho de {user.name}" + chip `resultado.cargo_label`.
    3. Se `resultado.sem_carteira === true`: renderizar bloco amarelo grande "Sem carteira em {mesExtenso(resultado.mes_referencia)}" + motivo. NÃO renderizar cards.
    4. Caso contrário, mesma grid de 4 cards do Dashboard.jsx (NPS/Faturamento/Margem/Absenteísmo) + card destaque Faixa de bônus + block Empresas.
    5. Card Absenteísmo renderiza SEMPRE placeholder "—" + badge "Em breve" (mesmo que outros valores existam).
    6. Se `resultado.faixa_promovida === true`, badge destaque "Promovida (2 meses consecutivos)" no card Faixa.
    7. Bloco explicativo pt-BR abaixo: linka para `/manual/desempenho-bonificacao` (Plan 74-08).
    8. Design tokens dark/glass consistente com Dashboard/Index.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('resources/js/Pages/Performance/Show.jsx','utf8'); const t=['sem_carteira','faixa_bonus','faixa_promovida','Em breve','nota_final']; const m=t.filter(x=>!s.includes(x)); if(m.length){console.error('MISSING',m);process.exit(1);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Tokens presentes: sem_carteira, faixa_bonus, faixa_promovida, Em breve, nota_final.
    - Bloco condicional sem_carteira renderiza motivo em pt-BR.
    - Card Absenteísmo com badge "Em breve".
    - Link para `/manual/desempenho-bonificacao`.
    - Toggle de mês (via URL query param) presente.
  </acceptance_criteria>
  <done>Show.jsx reescrito com view individual dos 4 parâmetros + placeholder Absenteísmo.</done>
</task>

<task type="auto">
  <name>Task 4: Rodar build do frontend</name>
  <files>public/build/manifest.json</files>
  <read_first>
    - package.json (verificar script build)
    - CLAUDE.md (convenção "Run npm run build after every frontend change")
  </read_first>
  <action>
    Executar `npm run build` na raiz do projeto. Verificar que não há erros de sintaxe/build. Se erro de import (SlidersHorizontal, cn, AppLayout, etc), corrigir no arquivo apropriado. NÃO commitar o `public/build/` — Vite gera assets, o convention deste projeto pede build local antes de commit conforme CLAUDE.md.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    Saída deve conter "built in" (ou similar sucesso do Vite) sem erros vermelhos.
  </verify>
  <acceptance_criteria>
    - `npm run build` exit code 0.
    - Nenhuma linha "ERROR" ou "SyntaxError" na saída.
    - `public/build/manifest.json` atualizado (mtime recente).
  </acceptance_criteria>
  <done>Build passa; assets JSX atualizados.</done>
</task>

</tasks>

<verification>
Login como admin no dev, navegar em `/performance` (ranking), clicar em user (`/performance/{id}`), e navegar para `/dashboard/mercadolivre` como analista (Performance/Dashboard.jsx). Nenhum crash na console do browser. Cards renderizam. Absenteísmo mostra badge "Em breve".
</verification>

<success_criteria>
- 3 arquivos JSX reescritos.
- Todos os componentes consomem o novo shape (nota_final, faixa_bonus, componentes, sem_carteira).
- Card Absenteísmo placeholder correto (DESEMP-06).
- Ranking filtra sem_carteira (DESEMP-10).
- Dashboard tem toggle mês fechado/parcial/diário.
- Design consistente com Nps/Index.jsx dark/glass.
- Build passa sem erro.
</success_criteria>

<output>
Criar `.planning/phases/74-m-dulo-desempenho-simplifica-o-para-4-par-metros-bonifica-o/74-06-SUMMARY.md` ao concluir, listando os 3 arquivos, output do build, decisões D-18/D-19/D-20, requisitos DESEMP-04/05/06/10/14.
</output>
