---
phase: 60-base-multi-fonte-backend-ml-adman-unificado
plan: 04
type: execute
wave: 4
depends_on:
  - 60-03
files_modified:
  - app/Services/Metrics/UnifiedMetricsService.php
  - tests/Feature/Phase60/UnifiedMetricsServiceTest.php
  - tests/Feature/Phase60/BaselineRegressionTest.php
autonomous: true
requirements:
  - DATA-04
  - DATA-06
tags:
  - unified-metrics
  - integration
  - baseline

must_haves:
  truths:
    - "`UnifiedMetricsService::for(Company, Carbon, Carbon)` retorna DTO agregado respeitando regra de precedência campo-a-campo do ADR DATA-04"
    - "Empresa só-Adman → DTO combina campos de Adman + nulls onde ML seria fonte primária"
    - "Empresa só-ML → DTO combina campos de ML + nulls onde Adman seria enriquecimento"
    - "Empresa em ambos → DTO combina campos com ML tendo prioridade para revenue/ad_spend/sold_quantity, Adman preenchendo net_billing/taxes/etc"
    - "Divergência de TACOS > 5% entre ML e Adman gera log warning sem falhar (per ADR DATA-04)"
    - "Suite Feature completa mantém contagem verde igual ou maior à baseline pré-Phase-60 (delta regressão = 0)"
  artifacts:
    - path: "app/Services/Metrics/UnifiedMetricsService.php"
      provides: "Orquestrador multi-fonte respeitando ADR DATA-04"
      contains: "class UnifiedMetricsService"
    - path: "tests/Feature/Phase60/UnifiedMetricsServiceTest.php"
      provides: "3 casos (só-Adman/só-ML/ambos) + edge cases + divergência TACOS"
      min_lines: 150
    - path: "tests/Feature/Phase60/BaselineRegressionTest.php"
      provides: "Smoke test dos consumidores atuais de adman_metrics (Dashboard/Portfolio/Company)"
      min_lines: 60
  key_links:
    - from: "app/Services/Metrics/UnifiedMetricsService.php"
      to: "app/Services/Metrics/MetricsProviderFactory.php"
      via: "constructor injection"
      pattern: "MetricsProviderFactory"
    - from: "tests/Feature/Phase60/UnifiedMetricsServiceTest.php"
      to: "app/Services/Metrics/UnifiedMetricsService.php"
      via: "assertions dos 3 casos"
      pattern: "so_adman|so_ml|ambos"
    - from: "tests/Feature/Phase60/BaselineRegressionTest.php"
      to: "app/Http/Controllers/DashboardController.php"
      via: "GET nas rotas que leem adman_metrics"
      pattern: "route.dashboard.|admin.financeiro"
---

<objective>
Fechar a Phase 60 entregando o serviço orquestrador `UnifiedMetricsService`, a suite de testes explícita para os 3 casos exigidos pelo Success Criterion #3 do ROADMAP (só-Adman / só-ML / ambos), e um teste de baseline que prova SC #4 (consumidores atuais de `adman_metrics` continuam funcionando sem regressão).

Purpose: Este plan é o "goal-backward final" — todos os artefatos anteriores (ADR, contract, DTO, providers, factory) precisam se compor num serviço testável que cumpre os 4 success criteria da phase de uma só vez. Sem este plan, o resto é infra sem prova.

Output: 1 arquivo de produção (`UnifiedMetricsService`) + 2 arquivos de teste (funcional 3 casos + baseline regressão). Zero modificação em controllers/services de produção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/adrs/DATA-04-precedencia-multifonte.md
@.planning/phases/60-base-multi-fonte-backend-ml-adman-unificado/60-01-SUMMARY.md
@.planning/phases/60-base-multi-fonte-backend-ml-adman-unificado/60-02-SUMMARY.md
@.planning/phases/60-base-multi-fonte-backend-ml-adman-unificado/60-03-SUMMARY.md
@app/Contracts/MetricsProvider.php
@app/Services/Metrics/UnifiedMetricsDto.php
@app/Services/Metrics/AdmanMetricsProvider.php
@app/Services/Metrics/MlMetricsProvider.php
@app/Services/Metrics/MetricsProviderFactory.php
@app/Models/AdmanMetric.php
@app/Models/Company.php

<interfaces>
<!-- Contract e DTO ja existentes (nao modificar): -->

`App\Contracts\MetricsProvider` — supports/name/readForCompany
`App\Services\Metrics\UnifiedMetricsDto` — 18 propriedades readonly (id, source, from, to + 15 campos)

<!-- Tabela de precedencia campo-a-campo do ADR DATA-04 (guia de implementacao): -->

- campo revenue           — fonte primaria ml,     fallback adman, ambos-diferem: ml ganha + log se |delta| > 5%
- campo ad_spend          — fonte primaria ml,     fallback adman, ambos-diferem: ml ganha + log se |delta| > 5%
- campo sold_quantity     — fonte primaria ml,     fallback adman, ambos-diferem: ml ganha + log se |delta| > 10%
- campo tacos             — derivado (ml.ad_spend/ml.revenue), fallback adman.tacos, ambos-diferem: log se |delta| > 5pp
- campo net_billing       — fonte primaria adman,  fallback nenhum, so adman preenche
- campo sales_fee         — fonte primaria ml,     fallback adman, ml ganha (ML expoe por pedido)
- campo taxes             — fonte primaria adman,  fallback nenhum, so adman preenche
- campo shipping_cost     — fonte primaria adman,  fallback nenhum, so adman preenche
- campo product_cost      — fonte primaria adman,  fallback nenhum, so adman preenche
- campo contribution_margin — fonte primaria adman, fallback nenhum, so adman preenche
- campo acos              — fonte primaria ml,     fallback nenhum, so ml preenche
- campo roas              — fonte primaria ml,     fallback nenhum, so ml preenche
- campo clicks            — fonte primaria ml,     fallback nenhum, so ml preenche
- campo impressions       — fonte primaria ml,     fallback nenhum, so ml preenche
- campo orders_count      — fonte primaria ml,     fallback nenhum, so ml preenche

**IMPORTANTE**: A tabela acima e uma reproducao do que o executor DEVE ler do arquivo `.planning/adrs/DATA-04-precedencia-multifonte.md` — se o ADR final divergir, o ADR e fonte de verdade.

<!-- Consumidores atuais de adman_metrics (baseline regressao) -->

Rotas que consultam `AdmanMetric` (validado por grep):
- `route('dashboard')` -> DashboardController (admin agrega adman_metrics)
- `route('portfolio')` -> PortfolioController (carteira) — validar nome via `route:list`
- `route('companies.show', $id)` -> CompanyController (ficha empresa)
- `route('admin.financeiro')` -> AdminController (fechamento)

Baseline test smoke: GET nas 4 rotas retorna 200 com admin autenticado, sem exception, sem tocar em nenhum arquivo de producao.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UnifiedMetricsService — 3 casos + divergencia TACOS (RED -> GREEN)</name>
  <files>app/Services/Metrics/UnifiedMetricsService.php, tests/Feature/Phase60/UnifiedMetricsServiceTest.php</files>
  <read_first>
    - .planning/adrs/DATA-04-precedencia-multifonte.md (tabela de precedencia campo-a-campo e a especificacao executavel — leitura OBRIGATORIA)
    - app/Services/Metrics/MetricsProviderFactory.php (metodo `available()` diz qual caso e: so-Adman/so-ML/ambos)
    - app/Services/Metrics/AdmanMetricsProvider.php + MlMetricsProvider.php (retornos DTO com quais campos preenchidos)
    - app/Services/Metrics/UnifiedMetricsDto.php (constructor promoted parameters — ordem exata)
    - tests/Feature/Phase60/AdmanMetricsProviderTest.php + MlMetricsProviderTest.php (padroes de fixture usados neste projeto)
  </read_first>
  <behavior>
    - Constructor: `__construct(private MetricsProviderFactory $factory)` — DI Laravel.
    - Metodo `public function for(Company $company, Carbon $from, Carbon $to): UnifiedMetricsDto`:
      1. Determina caso via `$this->factory->available($company)` -> `['adman']`, `['ml']`, `['adman','ml']`, ou `[]`
      2. Caso `[]`: retorna DTO com todos campos numericos null, `source='none'` (novo valor valido no DTO), `company_id`, `period_from`, `period_to` preenchidos
      3. Caso `['adman']`: chama `admanProvider->readForCompany()` e retorna esse DTO (source='adman' — ja esta)
      4. Caso `['ml']`: chama `mlProvider->readForCompany()` e retorna esse DTO (source='ml')
      5. Caso `['adman','ml']`: chama AMBOS providers, funde campo-a-campo respeitando tabela do ADR, retorna DTO com source='unified'
      6. Antes de retornar caso `['adman','ml']`: detectar divergencia TACOS/revenue/ad_spend/sold_quantity conforme thresholds do ADR (revenue/ad_spend 5%, sold_quantity 10%, tacos 5pp). Em cada divergencia: `Log::warning('[UnifiedMetrics] Divergencia {campo}', ['company_id' => $c->id, 'ml' => X, 'adman' => Y, 'delta_pct' => Z]);` — NUNCA lancar exception.
    - Metodo `public function available(Company): array` — passa direto para `$this->factory->available()`. Util para consumidores saberem qual badge exibir na Phase 61.
    - Testes exigidos (todos com fixtures explicitas + assertions numericas):
      - **Caso so-Adman**:
        - T1: `test_caso_so_adman_le_todos_campos_disponiveis` — empresa com `adman_account_id` sem mlToken. Rows AdmanMetric no periodo com revenue=1000, ad_spend=100, net_billing=800, taxes=50. Assert: DTO tem revenue=1000, ad_spend=100, net_billing=800, taxes=50, acos=null, roas=null, clicks=null, source='adman'.
        - T2: `test_caso_so_adman_sem_rows_no_periodo_retorna_dto_com_nulls` — empresa Adman mas 0 rows: DTO com todos campos numericos null, source='adman'.
      - **Caso so-ML**:
        - T3: `test_caso_so_ml_le_todos_campos_disponiveis` — empresa sem adman_account_id, com mlToken.status='active'. Http::fake dos endpoints orders (revenue=1000, sold_quantity=5, orders_count=3, sales_fee=90) + ads (ad_spend=100, clicks=200, impressions=10000, acos=10, roas=10). Assert: DTO com esses valores + net_billing=null, taxes=null, shipping_cost=null, product_cost=null, contribution_margin=null, source='ml'.
        - T4: `test_caso_so_ml_com_api_falhando_retorna_dto_com_nulls_sem_exception` — Http::fake responde 500 nos 2 endpoints. Assert: DTO com revenue=null, ad_spend=null, sem exception vazando.
      - **Caso ambos**:
        - T5: `test_caso_ambos_ml_ganha_para_revenue_ad_spend_sold_quantity` — Adman: revenue=800, ad_spend=90, sold_quantity=4. ML: revenue=1000, ad_spend=100, sold_quantity=5. Assert: DTO tem revenue=1000, ad_spend=100, sold_quantity=5 (ML ganhou).
        - T6: `test_caso_ambos_adman_preenche_campos_exclusivos` — Adman: net_billing=800, taxes=50, product_cost=200. ML: revenue=1000. Assert: DTO tem net_billing=800, taxes=50, product_cost=200 (so Adman), revenue=1000 (ML).
        - T7: `test_caso_ambos_divergencia_tacos_gera_log_warning_sem_falhar` — usar `Log::spy()` ou `Log::shouldReceive('warning')`. Fixtures ML e Adman com TACOS diferindo em >5pp. Assert: DTO retornado + log warning capturado + zero exception. Trailer no log tem chaves `company_id`, `ml`, `adman`, `delta_pct` (ou `delta_pp` para TACOS).
        - T8: `test_caso_ambos_divergencia_pequena_nao_gera_log` — delta < 5%. Log::spy() confirma `warning` NAO foi chamado para revenue/ad_spend.
        - T9: `test_caso_ambos_source_field_igual_a_unified` — DTO retornado tem `source === 'unified'`.
      - **Caso sem integracao**:
        - T10: `test_caso_sem_integracao_retorna_dto_nulls_source_none` — empresa sem adman_account_id e sem mlToken. Assert: DTO com todos campos numericos null, source='none', sem exception.
      - **available()**:
        - T11: `test_available_delega_para_factory` — retorna EXATAMENTE o que factory retornaria.
  </behavior>
  <action>
    Antes de tudo: reler ADR DATA-04 secao "Tabela de precedencia" e transcrever a versao final para uma constante privada `PRECEDENCE_MAP` OU um metodo `private static function precedenceMap(): array` na classe — assim o ADR e o codigo ficam sincronizados via um unico ponto (SPOT: se ADR mudar, alterar UM lugar).

    Criar `App\Services\Metrics\UnifiedMetricsService`. PHPDoc de classe em pt-BR: referencia ao ADR DATA-04, proposito (orquestrar leitura multi-fonte respeitando precedencia), consumidor esperado (Phase 61 dashboards).

    Constructor: `__construct(private MetricsProviderFactory $factory)`.

    Metodo `for(Company, Carbon, Carbon): UnifiedMetricsDto`:
    - Deteccao de caso via `$available = $this->factory->available($company);` — Array com nomes.
    - Switch por `count($available)` e conteudo:
      - 0 -> retornar DTO com nulls + source='none'
      - 1 e ['adman'] -> return `$this->factory->for($c, 'adman')->readForCompany($c, $from, $to);` (o provider ja retorna DTO com source='adman')
      - 1 e ['ml'] -> return `$this->factory->for($c, 'ml')->readForCompany(...)`
      - 2 -> chamar ambos providers, fundir, retornar DTO com source='unified'

    Metodo privado `merge(UnifiedMetricsDto $adman, UnifiedMetricsDto $ml, Company $c): UnifiedMetricsDto` — implementa a tabela de precedencia campo-a-campo. Para cada campo do DTO, pega o valor conforme regra do PRECEDENCE_MAP. Emite log warning quando divergencia excede threshold.

    Metodo privado `detectDivergences(UnifiedMetricsDto $adman, UnifiedMetricsDto $ml, Company $c): void` — chama `logSeDivergente()` para os 4 campos monitorados: revenue (5%), ad_spend (5%), sold_quantity (10%), tacos (5pp — pontos percentuais).

    Metodo privado `logSeDivergente(string $campo, ?float $ml, ?float $adman, int $companyId, float $thresholdPct, bool $pontosPercentuais = false): void` — calculo do delta. Se ambos preenchidos e > threshold -> `Log::warning('[UnifiedMetrics] Divergencia ' . $campo, [...])`.

    Metodo `available(Company $c): array` — passa direto para `$this->factory->available($c)`.

    **CRITICO — modificacao necessaria no DTO**: o campo `$source` do DTO precisa aceitar `'none'` e `'unified'` alem de `'adman'` e `'ml'`. Se DTO ja e `string` sem enum, ok. Se for enum PHP 8.2 — adicionar os 2 casos. Este ajuste NAO conta como modificacao de arquivo criado neste plan porque e ampliacao estrita de vocabulario: o `UnifiedMetricsDto.php` ganha docblock reconhecendo os 4 valores possiveis (`adman`, `ml`, `unified`, `none`). Adicionar `app/Services/Metrics/UnifiedMetricsDto.php` no `files_modified` deste plan apenas SE o executor precisar ampliar comentario/validacao (nao e ampliar assinatura). **Guarda de escopo**: se o DTO ja aceita `string` sem restricao, NADA precisa ser tocado nele.

    Criar `tests/Feature/Phase60/UnifiedMetricsServiceTest.php` namespace `Tests\Feature\Phase60`, `use RefreshDatabase`, `Http::fake` para endpoints ML. Setup helpers privados no test class: `createCompanyAdmanOnly()`, `createCompanyMlOnly()`, `createCompanyBoth()`, `seedAdmanMetric($company, $date, $data)`, `mockMlOrders($revenue, $soldQty, $ordersCount, $salesFee)`, `mockMlAds($adSpend, $clicks, $impressions, $acos, $roas)`. Reduz duplicacao e forca spec executavel.

    Rodar `php artisan test --filter=UnifiedMetricsServiceTest` — 11 verdes.

    Sanity adicional: `php artisan test --filter=Phase60 2>&1 | tail -10` — soma acumulada Phase 60 = 13 (Adman) + 11 (Ml) + 10 (Factory) + 11 (Unified) = 45 verdes.

    Comitar como `test(60-04): RED — UnifiedMetricsServiceTest 3 casos + divergencia` e `feat(60-04): GREEN — UnifiedMetricsService respeita ADR DATA-04`.
  </action>
  <verify>
    <automated>test -f app/Services/Metrics/UnifiedMetricsService.php &amp;&amp; grep -q "class UnifiedMetricsService" app/Services/Metrics/UnifiedMetricsService.php &amp;&amp; grep -q "MetricsProviderFactory" app/Services/Metrics/UnifiedMetricsService.php &amp;&amp; grep -q "Log::warning" app/Services/Metrics/UnifiedMetricsService.php &amp;&amp; php artisan test --filter=UnifiedMetricsServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `class_exists(App\Services\Metrics\UnifiedMetricsService::class)` retorna true
    - `app(UnifiedMetricsService::class)` resolve sem erro
    - `php artisan test --filter=UnifiedMetricsServiceTest` exit 0, 11 verdes
    - Os 3 casos aparecem NOMEADAMENTE nos testes (grep `so_adman|so_ml|ambos` no arquivo de teste retorna >= 3 ocorrencias totais nos nomes de metodo)
    - Log de divergencia e acionado so quando threshold excedido (T7 verde, T8 verde — nao trocado)
    - Zero modificacao em `AdmanMetricsProvider.php`, `MlMetricsProvider.php`, `MetricsProviderFactory.php`, contract, providers Sugadores (Phase 39/42), controllers de producao
    - `git diff --stat app/Http/Controllers/ app/Services/AdmanService.php app/Services/MercadoLivreService.php` retorna zero linhas modificadas
  </acceptance_criteria>
  <done>UnifiedMetricsService implementa fusao multi-fonte respeitando ADR, 11 testes verdes cobrindo os 3 casos + divergencia + edge cases.</done>
</task>

<task type="auto">
  <name>Task 2: BaselineRegressionTest — smoke consumidores de adman_metrics</name>
  <files>tests/Feature/Phase60/BaselineRegressionTest.php</files>
  <read_first>
    - .planning/STATE.md linhas 100-146 (padrao de baseline test usado em Phases anteriores — Phase 40 Plan 40-04 tem gate de zero regressao referenciado)
    - app/Http/Controllers/DashboardController.php linhas 220-410 (rotas que consomem AdmanMetric)
    - app/Http/Controllers/CompanyController.php (ficha empresa le AdmanMetric)
    - app/Http/Controllers/PortfolioController.php (carteira le AdmanMetric)
    - app/Http/Controllers/AdminController.php (fechamento le AdmanMetric)
    - routes/web.php (nomes de rota exatos)
    - tests/Feature/Phase58/DashboardNavigationSmokeTest.php (padrao de smoke test Inertia + admin acting)
  </read_first>
  <action>
    Criar `tests/Feature/Phase60/BaselineRegressionTest.php`. Objetivo: PROVAR que as rotas existentes que consultam `adman_metrics` continuam retornando 200 sem exception, DEPOIS da Phase 60 introduzir os novos servicos. Este e o teste que cumpre SC #4 do ROADMAP (delta regressao = 0).

    Estrutura: namespace `Tests\Feature\Phase60`, `use RefreshDatabase`. Setup em `setUp()`: cria admin (`role='admin'`), cria empresa com `adman_account_id` + 1 row `AdmanMetric` no dia D-1. Sem mocks de ML — foco e o path Adman puro.

    Testes obrigatorios (smoke — nao valida valores, so ausencia de regressao):
    - T1: `test_route_dashboard_ainda_retorna_200` — `$this->actingAs($admin)->get(route('dashboard'))->assertOk();`
    - T2: `test_route_admin_financeiro_ainda_retorna_200` — GET em `route('admin.financeiro')` ou equivalente, `assertOk()`.
    - T3: `test_route_companies_show_ainda_retorna_200` — GET em `route('companies.show', $company)`, `assertOk()`.
    - T4: `test_route_portfolio_ainda_retorna_200` — **precondição**: executor DEVE rodar `php artisan route:list --json | grep -i portfolio` ANTES de escrever o teste para pegar o NOME REAL da rota (`portfolio.index` OU o que quer que seja hoje). Escrever o teste com o nome exato. Se `Route::has('nome_exato')` retornar false quando o teste rodar, usar `$this->fail('Rota portfolio esperada mas ausente — atualizar teste com nome real via route:list; markTestSkipped mascara SC #4 do ROADMAP')`. **NÃO usar `markTestSkipped`** — baseline não permite skip silencioso (SC #4 exige delta = 0 sem hedge).
    - T5: `test_query_admanmetric_agregada_por_company_ainda_funciona` — replica a query bruta usada em `DashboardController` (SELECT SUM(revenue) GROUP BY company_id em adman_metrics no periodo) e asserta resultado esperado com fixture montada. Prova que o schema+model continuam integros — se Plan 60-02/03 tivesse tocado por engano a AdmanMetric, quebraria aqui.
    - T6: `test_admanservice_legado_pode_conviver_com_admanmetricsprovider_novo` — instanciar AMBOS (`app(AdmanService::class)` + `app(AdmanMetricsProvider::class)`) e chamar `->name()` do novo para confirmar coexistencia sem interferencia. Sanity de DI.

    Nota importante: rotas exatas podem ter mudado — o executor DEVE rodar `php artisan route:list --path=dashboard` e `--path=financeiro` primeiro para pegar os nomes atuais. Se um nome mudou, ajustar o teste e documentar no SUMMARY.

    Rodar `php artisan test --filter=BaselineRegressionTest` — deve passar (5-6 verdes conforme quantas rotas existem).

    Rodar suite Feature completa: `php artisan test --testsuite=Feature 2>&1 | tail -20`. Comparar contagem verde/falha com baseline anotado antes do inicio do Plan 60-01. Delta VERDE = +45 (todos os testes da Phase 60). Delta VERMELHO = 0. Se qualquer teste que ERA verde ficou vermelho -> investigar antes de commit.

    Comitar como `test(60-04): baseline regressao consumidores adman_metrics`.

    Fechar SUMMARY do plan com numero exato de verdes acumulados Phase 60 + confirmacao de zero regressao.
  </action>
  <verify>
    <automated>test -f tests/Feature/Phase60/BaselineRegressionTest.php &amp;&amp; grep -q "route.'dashboard'\|route.\"dashboard\"" tests/Feature/Phase60/BaselineRegressionTest.php &amp;&amp; grep -q "AdmanMetric" tests/Feature/Phase60/BaselineRegressionTest.php &amp;&amp; php artisan test --filter=BaselineRegressionTest &amp;&amp; php artisan test --filter=Phase60</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/Feature/Phase60/BaselineRegressionTest.php` existe
    - Pelo menos 4 testes ativos (nao marcados como skipped), cobrindo as rotas existentes de fato no route:list atual
    - `php artisan test --filter=BaselineRegressionTest` exit 0
    - `php artisan test --filter=Phase60` exit 0 — soma acumulada da phase = 45 (ou +/- 2 se algum teste foi consolidado)
    - Suite Feature completa NAO reduziu contagem verde: comparar `php artisan test --testsuite=Feature` antes vs depois deste plan; delta vermelho = 0
    - `git diff --stat app/` retorna somente `app/Services/Metrics/*.php` e `app/Contracts/MetricsProvider.php` como modificados/criados. Controllers e services legados (`AdmanService`, `MercadoLivreService`, `AdminController`, `DashboardController`, `CompanyController`, `PortfolioController`) tem 0 linhas alteradas.
  </acceptance_criteria>
  <done>Baseline regressao provada por 4+ smoke tests verdes + delta suite Feature = 0 vermelho novo. Phase 60 pronta para deploy (mediante autorizacao caso-a-caso — memory `feedback_perguntar_antes_deploy_v9`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- **UnifiedMetricsService**: nenhuma nova boundary externa. Wrap por cima de providers que ja tem suas proprias boundaries mitigadas.
- **Divergence logging**: entrada de log serializa dados agregados; sem PII ou credenciais.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04-01 | Information Disclosure | Log de divergencia | mitigate | Log inclui apenas `company_id`, valores numericos agregados e delta. NUNCA inclui token, email, telefone ou raw payload da API. Grep obrigatorio: `grep -c "access_token\|password\|api_key" app/Services/Metrics/UnifiedMetricsService.php` == 0. |
| T-60-04-02 | Denial of Service | Chamadas duplicadas por request | mitigate | Cache do MlMetricsProvider (Plan 60-03) evita 2a HTTP call por request. UnifiedMetricsService NAO adiciona proprio cache — delega para providers. |
| T-60-04-03 | Tampering | Ordem de merge inconsistente | mitigate | PRECEDENCE_MAP como constante estatica; tabela de precedencia campo-a-campo espelha ADR. Alteracao exige update coordenado ADR + codigo. |
| T-60-04-04 | Repudiation | Divergencia nao rastreavel | mitigate | Log warning estruturado com company_id + timestamp implicito por driver Laravel. Log em canal padrao (`storage/logs/laravel.log`) audit-friendly. |
</threat_model>

<verification>
Ao termino:
- `php artisan test --filter=Phase60` retorna 45 verdes (13 Adman + 11 Ml + 10 Factory + 11 Unified) — ajustar contagem se Task 2 (baseline) consolidou/pulou testes por rotas inexistentes
- `php artisan test --testsuite=Feature 2>&1 | tail -5` mostra >= baseline pre-Phase-60 em verdes; delta vermelho novo = 0
- `git diff --stat app/Http/Controllers/ app/Services/AdmanService.php app/Services/MercadoLivreService.php app/Services/Sugadores/` == 0 linhas alteradas
- `grep -R "UnifiedMetricsService\|UnifiedMetricsDto" app/Http/Controllers/` == 0 (Phase 60 NAO migra consumidores — isso e Phase 61)
- Todos os 4 success criteria do ROADMAP Phase 60 tem prova executavel:
  - SC #1 (leitura sem quebrar em 3 casos): testes T1-T4 + T10 da Task 1 deste plan
  - SC #2 (ADR de precedencia): arquivo `.planning/adrs/DATA-04-precedencia-multifonte.md` criado no Plan 60-01
  - SC #3 (testes 3 casos com RefreshDatabase + fixtures): UnifiedMetricsServiceTest cobre os 3 casos nomeadamente
  - SC #4 (delta regressao = 0): BaselineRegressionTest verde + suite Feature sem novos vermelhos
</verification>

<success_criteria>
1. UnifiedMetricsService entrega os 3 casos + fusao correta + log de divergencia sem falhar
2. Todos os 4 success criteria do ROADMAP Phase 60 tem teste automatizado provando-os
3. Zero regressao em suite Feature (delta vermelho = 0)
4. Zero modificacao em controllers ou services legados (coexistencia total, migracao vira Phase 61)
5. Log de divergencia ML vs Adman gera dados auditaveis para futura tomada de decisao (documentado no SUMMARY)
</success_criteria>

<output>
Criar `.planning/phases/60-base-multi-fonte-backend-ml-adman-unificado/60-04-SUMMARY.md` sumarizando:
- Contagem exata verde final Phase 60 (soma de 60-01/02/03/04)
- Baseline pre vs pos suite Feature (numero absoluto)
- Confirmacao git diff zero para arquivos legados
- Amostragem de log de divergencia (se aparecer nos testes) — decisao para Phase 61 dashboards
- Status dos 4 success criteria do ROADMAP: prova por prova
- Proximo passo: `/gsd:complete-phase 60` ou avancar para Phase 61 (dashboards multi-fonte + badge de origem)
</output>
