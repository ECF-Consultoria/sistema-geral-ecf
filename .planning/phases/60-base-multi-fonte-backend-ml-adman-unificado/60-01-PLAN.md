---
phase: 60-base-multi-fonte-backend-ml-adman-unificado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/adrs/DATA-04-precedencia-multifonte.md
autonomous: true
requirements:
  - DATA-06
tags:
  - adr
  - multi-fonte
  - precedencia

must_haves:
  truths:
    - "Existe um ADR versionado dentro de .planning/adrs/ que define regra de precedência ML vs Adman"
    - "ADR nomeia os 3 casos de leitura possíveis: só-Adman, só-ML, ambos"
    - "ADR define o que é 'fonte ML' na prática (API ML via MercadoLivreService, não tabela local ainda inexistente)"
    - "ADR define TTL/estratégia de cache para chamadas ML (custo de API + rate limit)"
    - "ADR está referenciado por link nos plans 60-02, 60-03, 60-04"
  artifacts:
    - path: ".planning/adrs/DATA-04-precedencia-multifonte.md"
      provides: "Decisão formal de precedência multi-fonte para métricas de empresa"
      contains: "id: DATA-04"
      contains_also: "status: accepted"
  key_links:
    - from: ".planning/adrs/DATA-04-precedencia-multifonte.md"
      to: ".planning/adrs/DATA-01-multi-marketplace-model.md"
      via: "related_phases + pivot company_marketplaces é a base do multi-fonte"
      pattern: "DATA-01"
---

<objective>
Registrar em ADR versionado a regra de precedência entre fontes de métricas (ML API vs `adman_metrics` local), o comportamento esperado nos 3 casos (só-Adman, só-ML, ambos), e a definição operacional do que significa "fonte ML" na prática — dado que o repositório NÃO tem tabela local de vendas ML e a fonte de ML canônica é a API `MercadoLivreService::fetchOrdersSummary()` + `fetchAdsSummary()`.

Purpose: Success criterion #2 do ROADMAP Phase 60 exige "regra de precedência documentada em ADR versionado dentro de `.planning/`". Sem esse ADR, os plans 60-02/03/04 não têm âncora para decidir "quem ganha quando ambas as fontes têm dado" — decisão dispersa em código vira débito.

Output: `.planning/adrs/DATA-04-precedencia-multifonte.md` seguindo o mesmo template de `DATA-01-multi-marketplace-model.md` (Phase 57). Zero código de produção neste plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/adrs/DATA-01-multi-marketplace-model.md
@app/Services/AdmanService.php
@app/Services/MercadoLivreService.php
@app/Models/AdmanMetric.php
@app/Models/Company.php
@database/migrations/2026_07_03_190000_create_company_marketplaces_table.php

<interfaces>
<!-- Realidade das fontes de dados hoje (fatos que o ADR precisa reconhecer): -->

Fonte Adman (leitura LOCAL, offline, cached):
- Tabela: `adman_metrics` (migration `2026_04_26_152220_create_adman_metrics_table.php` + `2026_04_27_100001_add_fields_to_adman_metrics_table.php`)
- Model: `App\Models\AdmanMetric` — colunas relevantes: `company_id`, `reference_date`, `revenue`, `net_billing`, `sales_fee`, `ad_spend`, `tacos`, `sold_quantity`, `synced_at`
- Populada por: `AdmanService::syncCompany()` chamado por scheduler diário
- Consumidores atuais: DashboardController, CompanyController, PerformanceController, AdminController, PortfolioController, AdmanController

Fonte ML (leitura LIVE via API, sem tabela local de vendas):
- Serviço: `App\Services\MercadoLivreService`
- Métodos relevantes:
  - `fetchOrdersSummary(Company $c, string $from, string $to): array` — retorna `revenue, sold_quantity, orders_count, sales_fee, raw_orders`
  - `fetchAdsSummary(Company $c, string $from, string $to): array` — retorna `ad_spend, ad_revenue, clicks, impressions, acos, roas, cpc, sold_quantity`
- Precondição: `$company->mlToken?->status === 'active'` (accessor `Company::is_ml_driven`)
- Rate limit: throttle interno via `MercadoLivreService` — cache TTL a decidir no ADR
- Não existe tabela `ml_orders`/`ml_sales` local hoje — verificado via glob em `database/migrations/`

Pivot `company_marketplaces` (Phase 57 v13.0, herdada):
- Enum: `meli`, `shopee`, `amazon`, `magalu`
- Colunas: `store_id`, `adman_id`, `is_primary`, `active`, `integracao_status`
- Fonte de verdade sobre "em que marketplaces esta empresa está" — é o input para decidir quais providers consultar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar ADR DATA-04 de precedência multi-fonte</name>
  <files>.planning/adrs/DATA-04-precedencia-multifonte.md</files>
  <read_first>
    - .planning/adrs/DATA-01-multi-marketplace-model.md (template + tom + estrutura de seções)
    - .planning/ROADMAP.md linhas 29-38 (Phase 60 goal + success criteria — o ADR precisa fechar SC #2)
    - .planning/REQUIREMENTS.md linhas 15-17 (DATA-04, DATA-05, DATA-06 — texto fonte)
    - app/Services/AdmanService.php linhas 1-100 (para saber o que a leitura Adman entrega hoje)
    - app/Services/MercadoLivreService.php linhas 400-570 (fetchOrdersSummary + fetchAdsSummary — o que ML entrega)
    - app/Models/AdmanMetric.php (schema exato de adman_metrics)
    - app/Models/Company.php linhas 74-93 (accessors `cust_id` e `is_ml_driven` — inputs de decisão)
  </read_first>
  <action>
    Criar `.planning/adrs/DATA-04-precedencia-multifonte.md` seguindo template EXATO de DATA-01. Frontmatter obrigatório: `id: DATA-04`, `title: Precedência multi-fonte de métricas (ML vs Adman)`, `status: accepted`, `date: 2026-07-07`, `related_phases: [57, 60]`, `related_requirements: [DATA-04, DATA-06]`.

    Seções obrigatórias em pt-BR:

    (a) **## Contexto** — descrever a realidade atual em 4-6 parágrafos: (i) `adman_metrics` é a única tabela local de métricas de venda/ads; (ii) `MercadoLivreService::fetchOrdersSummary`/`fetchAdsSummary` são a fonte ML canônica LIVE (sem tabela local); (iii) 6 controllers já leem `adman_metrics` direto (listar por nome); (iv) o pivot `company_marketplaces` (Phase 57) diz em que MPs a empresa está mas não onde estão as métricas dela; (v) discrepância conhecida de TACOS (referenciar memory `project_adman_data_sources`); (vi) empresas ML-only retornam 422 em endpoints Adman MCP (referenciar memory `project_ml_only_companies_adman_endpoints`).

    (b) **## Decisão** — enumerar EXPLICITAMENTE:
      - **Regra de precedência**: quando ambas as fontes retornam dado válido para o mesmo (empresa, período, dimensão), ML é fonte PRIMÁRIA (fresh, canonical desde v11.0 cutover Phase 42). Adman é ENRIQUECIMENTO opcional (usado só para campos que ML não expõe: `net_billing`, `taxes`, `product_cost`, `contribution_margin` etc — enumerar exatamente quais).
      - **3 casos formalizados**:
        1. só-Adman (`mlToken` inexistente ou não-active E `adman_account_id` presente): lê 100% de `adman_metrics`.
        2. só-ML (`mlToken` active E sem `adman_account_id`): lê 100% via `MercadoLivreService`. Campos exclusivos-Adman retornam `null` sem quebrar.
        3. ambos (`mlToken` active E `adman_account_id` presente): ML dita `revenue`/`sold_quantity`/`ad_spend`; Adman preenche complementos (net_billing/taxes/etc). Precedência é campo-a-campo, documentada em tabela.
      - **Detecção do caso (só-Adman / só-ML / ambos)** — decisão explícita: a detecção usa os **campos denormalizados** de `companies` (`mlToken?->status === 'active'` e `adman_account_id`) e NÃO consulta o pivot `company_marketplaces` para essa decisão específica. Justificativa: (i) os accessors legacy da Phase 57 mantêm contrato — desnormalizado permanece source-of-truth funcional; (ii) evita JOIN adicional em hotpath de dashboards; (iii) migração completa para pivot é `DATA-FUTURE-02` (out-of-scope v14.0). Consequência: se um dia a denormalização divergir do pivot, a detecção pode cair no caso errado — mitigação registrada como candidato de teste de consistência em futura milestone (não bloqueia esta phase).
      - **Vocabulário do campo `source` no `UnifiedMetricsDto`** — enumerar os **4 valores válidos** produzidos ao longo desta phase, para travar decisão antes da implementação:
        - `'adman'` — DTO produzido por `AdmanMetricsProvider` (fonte única = leitura de `adman_metrics`).
        - `'ml'` — DTO produzido por `MlMetricsProvider` (fonte única = API ML via `MercadoLivreService`).
        - `'unified'` — DTO resultado da fusão campo-a-campo em `UnifiedMetricsService` (caso "ambos" após aplicar precedência).
        - `'none'` — DTO sentinela emitido quando a empresa não tem NENHUMA integração (sem `mlToken` active e sem `adman_account_id`); todos os campos numéricos ficam null e o consumidor sabe distinguir "empresa sem integração" de "erro na leitura".
      - **Definição operacional de "fonte ML"**: API ML via `MercadoLivreService` (`fetchOrdersSummary` + `fetchAdsSummary`), com cache TTL a definir aqui (sugestão: 15 min por par empresa+período, mesma janela dos dashboards ML atuais). Explicitar que criação de tabela `ml_metrics_daily` local FICA FORA DO ESCOPO da Phase 60 (é candidato de futura milestone).
      - **Tabela de precedência campo-a-campo** (formato markdown): coluna `campo`, `fonte_primaria`, `fonte_fallback`, `caso_ambos_diferem`. Cobrir no mínimo: revenue, ad_spend, sold_quantity, tacos, net_billing, sales_fee, taxes, shipping_cost, product_cost, contribution_margin, acos, roas, clicks, impressions, orders_count.
      - **Reconciliação de discrepância TACOS**: quando ML.ad_spend/ML.revenue difere de Adman.tacos em >5%, gravar log estruturado (`Log::warning('[UnifiedMetrics] TACOS divergente', [...])`) sem falhar. Referência para plan 60-04 implementar.

    (c) **## Consequências positivas** — no mínimo: (i) plans 60-02/03/04 têm âncora clara de decisão; (ii) dashboards Phase 61 sabem qual badge mostrar (ML/Adman/Agregado); (iii) rollback simples se ML API cair (fallback para Adman documentado); (iv) empresas ML-only param de "gaguejar" com 422 nos endpoints Adman.

    (d) **## Consequências negativas** — no mínimo: (i) chamadas ML API custam rate limit (mitigar com cache); (ii) débito consciente de não ter tabela local ML (fresh mas custoso); (iii) tabela de precedência é lista curada — se ML expuser campo novo, ADR precisa update.

    (e) **## Alternativas consideradas** — no mínimo 3 opções rejeitadas:
      - **A. Adman primário, ML enriquece** — rejeitada: v11.0 já promoveu ML como canonical; regressão de decisão.
      - **B. Criar tabela `ml_metrics_daily` local com sync noturno** — rejeitada nesta phase por escopo/prazo (candidato v14.x ou v15.x).
      - **C. Retornar união de todas as métricas sem precedência (deixa consumidor decidir)** — rejeitada: quebra "sem duplicação" do SC #2.

    (f) **## Estratégia de cache ML** — TTL, chave de cache (`unified_metrics:{company_id}:{from}:{to}:{source}`), invalidação (via `Cache::forget` no fim do sync Adman quando aplicável).

    (g) **## Rollout e feature flag** — declarar que Phase 60 entrega infra em coexistência com `AdmanService` legado; consumidores (`DashboardController` etc) NÃO são migrados nesta phase (isso vira Phase 61). Feature flag `UNIFIED_METRICS_ENABLED` (env, default false) opcional, decisão neste ADR.

    (h) **## Referências** — links para: `.planning/adrs/DATA-01-multi-marketplace-model.md`, memory `project_adman_data_sources`, memory `project_ml_only_companies_adman_endpoints`, roadmap ROADMAP.md linhas 29-38, REQUIREMENTS.md linhas 15-17.

    Comentários e texto integralmente em pt-BR (per CLAUDE.md + memory `feedback_gsd_language_pt_br`). Termos técnicos consagrados (mlToken, cust_id, cache, endpoint, TTL, rate limit) mantidos em inglês.
  </action>
  <verify>
    <automated>test -f .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "^id: DATA-04" .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "^status: accepted" .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "só-Adman" .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "só-ML" .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "ambos" .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "UNIFIED_METRICS_ENABLED\|feature flag" .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "Detecção do caso\|denormalizado\|company_marketplaces" .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "'adman'" .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "'unified'" .planning/adrs/DATA-04-precedencia-multifonte.md &amp;&amp; grep -q "'none'" .planning/adrs/DATA-04-precedencia-multifonte.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/adrs/DATA-04-precedencia-multifonte.md` existe
    - Frontmatter YAML com `id: DATA-04`, `title:`, `status: accepted`, `date: 2026-07-07`, `related_phases:` e `related_requirements:`
    - Seções obrigatórias presentes: `## Contexto`, `## Decisão`, `## Consequências positivas`, `## Consequências negativas`, `## Alternativas consideradas`, `## Estratégia de cache ML`, `## Rollout e feature flag`, `## Referências`
    - Tabela markdown de precedência campo-a-campo com header `campo | fonte_primaria | fonte_fallback | caso_ambos_diferem` e no mínimo 15 linhas de campo (revenue, ad_spend, sold_quantity, tacos, net_billing, sales_fee, taxes, shipping_cost, product_cost, contribution_margin, acos, roas, clicks, impressions, orders_count)
    - Os 3 casos (só-Adman, só-ML, ambos) estão nomeados e definidos com precondição de detecção (baseada em `mlToken.status` e `adman_account_id`)
    - Seção `Detecção do caso` presente e declara explicitamente uso dos **campos denormalizados** (`mlToken.status` + `adman_account_id`) e NÃO do pivot `company_marketplaces`, com justificativa (accessors legacy + hotpath)
    - Seção sobre `source` do DTO enumera os **4 valores**: `'adman'`, `'ml'`, `'unified'`, `'none'` com semântica clara de cada um
    - Referência a `DATA-01-multi-marketplace-model.md` presente
    - Zero placeholder `[TODO]`, `[TBD]`, `[definir]` no corpo do ADR
    - Texto em pt-BR (executor faz sanity check via leitura do arquivo)
  </acceptance_criteria>
  <done>ADR DATA-04 criado, versionado em `.planning/adrs/`, cobrindo os 3 casos, tabela de precedência campo-a-campo, alternativas rejeitadas e estratégia de cache — pronto para servir de âncora para plans 60-02/03/04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova trust boundary neste plan — apenas documentação em `.planning/`. Sem input externo, sem execução de código.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01-01 | Information Disclosure | ADR em repo | accept | ADR não contém segredo. Referência a memory de credentials expostas (`project_hubspot_creds_exposed`) NÃO deve ser incluída no ADR; se necessário, referenciar por ID sem transcrever chave. |
| T-60-01-02 | Repudiation | ADR sem rastreabilidade | mitigate | Frontmatter `date: 2026-07-07` + git commit obrigatório ao fim do plan (activity_log natural via git log). |
</threat_model>

<verification>
Ao término:
- `test -f .planning/adrs/DATA-04-precedencia-multifonte.md` retorna sucesso
- `git status` mostra o ADR como novo arquivo pronto para commit
- Nenhum arquivo em `app/` ou `database/` foi tocado neste plan (leitura-only fora de `.planning/`)
</verification>

<success_criteria>
1. ADR DATA-04 existe em `.planning/adrs/`
2. Regra de precedência entre ML e Adman está formalmente escrita e é auditável
3. Os 3 casos operacionais estão nomeados e reconhecíveis por outros plans
4. Estratégia de cache ML está declarada (TTL + chave de cache)
5. Feature flag `UNIFIED_METRICS_ENABLED` está declarada (default e semântica)
</success_criteria>

<output>
Criar `.planning/phases/60-base-multi-fonte-backend-ml-adman-unificado/60-01-SUMMARY.md` ao concluir, sumarizando: (a) decisão de precedência tomada (ML primário, Adman enriquece), (b) 3 casos formalizados, (c) TTL de cache decidido, (d) status do feature flag, (e) próximo plan (60-02).
</output>
