---
phase: 60-base-multi-fonte-backend-ml-adman-unificado
plan: 02
type: execute
wave: 2
depends_on:
  - 60-01
files_modified:
  - app/Contracts/MetricsProvider.php
  - app/Services/Metrics/UnifiedMetricsDto.php
  - app/Services/Metrics/AdmanMetricsProvider.php
  - tests/Feature/Phase60/AdmanMetricsProviderTest.php
autonomous: true
requirements:
  - DATA-04
tags:
  - contract
  - adman
  - metrics-provider

must_haves:
  truths:
    - "Existe um contract `App\\Contracts\\MetricsProvider` com métodos `supports(Company)`, `name()`, `readForCompany(Company, Carbon, Carbon)`"
    - "`AdmanMetricsProvider` implementa o contract lendo apenas de `adman_metrics` local (nunca chama API Adman em runtime)"
    - "Empresa sem `adman_account_id` faz `supports()` retornar false — não lança exceção"
    - "Empresa com `adman_account_id` mas sem rows em `adman_metrics` no período retorna DTO com campos null/zero, não exception"
  artifacts:
    - path: "app/Contracts/MetricsProvider.php"
      provides: "Contract de leitura unificada de métricas"
      contains: "interface MetricsProvider"
    - path: "app/Services/Metrics/UnifiedMetricsDto.php"
      provides: "DTO imutável com shape canônico das métricas"
      contains: "class UnifiedMetricsDto"
    - path: "app/Services/Metrics/AdmanMetricsProvider.php"
      provides: "Provider Adman lendo adman_metrics local"
      contains: "class AdmanMetricsProvider implements MetricsProvider"
    - path: "tests/Feature/Phase60/AdmanMetricsProviderTest.php"
      provides: "Testes de contrato + comportamento do provider Adman"
      min_lines: 80
  key_links:
    - from: "app/Services/Metrics/AdmanMetricsProvider.php"
      to: "app/Models/AdmanMetric.php"
      via: "query AdmanMetric::whereBetween('reference_date', ...)"
      pattern: "AdmanMetric::"
    - from: "tests/Feature/Phase60/AdmanMetricsProviderTest.php"
      to: "app/Services/Metrics/AdmanMetricsProvider.php"
      via: "resolve via container ou new()"
      pattern: "AdmanMetricsProvider"
---

<objective>
Estabelecer o contract `MetricsProvider` (análogo estrutural ao `SugadoresAdsProvider` da Phase 39) e a primeira implementação — `AdmanMetricsProvider` — que lê exclusivamente da tabela `adman_metrics` local, sem chamar API Adman em runtime. Definir também o DTO canônico `UnifiedMetricsDto` que ambos os providers (Adman deste plan + ML do 60-03) vão retornar.

Purpose: DATA-04 (leitura multi-fonte sem quebrar quando uma fonte é ausente) exige uma interface uniforme; o refactor Phase 39 provou que `contract + factory` é o padrão certo para esta base de código. Este plan entrega Adman side; 60-03 entrega ML side; 60-04 orquestra.

Output: 3 arquivos de produção + 1 arquivo de teste. Coexistência total com `AdmanService` legado — zero modificação em consumidores existentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/adrs/DATA-04-precedencia-multifonte.md
@.planning/phases/60-base-multi-fonte-backend-ml-adman-unificado/60-01-SUMMARY.md
@app/Contracts/SugadoresAdsProvider.php
@app/Services/Sugadores/AdmanSugadoresProvider.php
@app/Models/AdmanMetric.php
@app/Models/Company.php

<interfaces>
<!-- Padrão a REPLICAR: SugadoresAdsProvider (Phase 39 Plan 39-01) -->

Contract vigente `App\Contracts\SugadoresAdsProvider` (fonte de replicação estrutural):
```
interface SugadoresAdsProvider {
    public function supports(Company $company): bool;
    public function name(): string; // 'adman' | 'ml'
    public function fetchCampaigns(Company $company): array;
    public function fetchCampaignsMetrics(Company $company, Carbon $from, Carbon $to): array;
    public function fetchAdgroupsMetrics(Company $company, Carbon $from, Carbon $to): array;
    public function fetchAdgroupMlbs(Company $company, Carbon $from, Carbon $to): array;
}
```

Schema `adman_metrics` (fonte de dados deste provider):
```
company_id, reference_date, tacos, revenue, net_billing, sales_fee, taxes,
shipping_cost, product_cost, return_cost, profit_share, sold_quantity,
ad_spend, contribution_margin, contribution_margin_pct, products_total,
products_without_cost, revenue_prev_period, raw_data (json), synced_at
```

Model `App\Models\AdmanMetric` — todos os campos numéricos com cast `decimal:2` (ou `decimal:4` para tacos/contribution_margin_pct/profit_share).

`Company::is_ml_driven` (accessor existente) — usado por `supports()` como sinal negativo (empresa ML-only tem `mlToken` active; nesses casos AdmanMetricsProvider deve continuar suportando SE ela tiver `adman_account_id`, porque adman_metrics local pode ter histórico — precedência campo-a-campo é responsabilidade do UnifiedMetricsService, plan 60-04).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Definir contract MetricsProvider + DTO UnifiedMetricsDto (RED)</name>
  <files>app/Contracts/MetricsProvider.php, app/Services/Metrics/UnifiedMetricsDto.php, tests/Feature/Phase60/AdmanMetricsProviderTest.php</files>
  <read_first>
    - app/Contracts/SugadoresAdsProvider.php (padrão estrutural OBRIGATÓRIO — nomes de métodos, docblock, uso de Carbon)
    - app/Models/AdmanMetric.php (schema exato para saber quais campos o DTO precisa)
    - app/Models/Company.php linhas 74-93 (accessors `cust_id` e `is_ml_driven`)
    - .planning/adrs/DATA-04-precedencia-multifonte.md (tabela de precedência campo-a-campo — o DTO PRECISA cobrir todos os campos listados no ADR)
    - tests/Feature/Phase58/DashboardNavigationSmokeTest.php linhas 1-40 (padrão de teste Feature usado no projeto: `namespace Tests\Feature\PhaseXX`, `use RefreshDatabase`, factories, PHPUnit 11 attributes)
  </read_first>
  <behavior>
    - Contract `App\Contracts\MetricsProvider` expõe 3 métodos: `supports(Company): bool`, `name(): string`, `readForCompany(Company, Carbon $from, Carbon $to): UnifiedMetricsDto`
    - `UnifiedMetricsDto` é imutável (readonly properties PHP 8.2+) e cobre TODOS os 15 campos numéricos listados no ADR DATA-04 (revenue, ad_spend, sold_quantity, tacos, net_billing, sales_fee, taxes, shipping_cost, product_cost, contribution_margin, acos, roas, clicks, impressions, orders_count) + `source: string` **com um dos 4 valores enumerados do ADR** (`'adman' | 'ml' | 'unified' | 'none'`) + `period_from: Carbon` + `period_to: Carbon` + `company_id: int` — total: **4 metadados + 15 numéricos = 19 propriedades**
    - Todos os campos numéricos do DTO são `?float` ou `?int` (nullable) — permite representar "fonte não expõe este campo" sem quebrar
    - Método `toArray(): array` para serialização
    - Teste RED da Task 1: (i) interface `MetricsProvider` existe via `interface_exists()`; (ii) DTO tem exatamente **15 campos numéricos + 4 metadados** readonly (contar via reflection para não travar em número mágico); (iii) DTO->toArray() retorna array com chaves esperadas; (iv) DTO aceita construção com todos os campos numéricos null (fonte vazia é caso legítimo); (v) DTO rejeita/aceita apenas os 4 valores válidos de `source` (validação opcional — se implementada, cobrir teste)
  </behavior>
  <action>
    Criar `App\Contracts\MetricsProvider` com PHPDoc explicando (em pt-BR): propósito, referência ao ADR DATA-04, referência ao padrão SugadoresAdsProvider. Assinaturas:
    - `public function supports(Company $company): bool` — retorna true se o provider tem dados de origem disponíveis para esta empresa (Adman: existência de `adman_account_id`; ML: `mlToken?->status === 'active'`).
    - `public function name(): string` — retorna `'adman'` ou `'ml'`. Não traduzir; usado como chave em logs, badges de UI (DATA-05 Phase 61) e chaves de cache.
    - `public function readForCompany(Company $company, Carbon $from, Carbon $to): UnifiedMetricsDto` — leitura agregada no período `[from, to]` (inclusive). Sem dados: retorna DTO com campos null (não lançar exception).

    Criar `App\Services\Metrics\UnifiedMetricsDto` como classe `final readonly` (PHP 8.2+) com constructor promoted properties na ordem: `int $company_id`, `string $source`, `Carbon $period_from`, `Carbon $period_to`, `?float $revenue`, `?float $ad_spend`, `?int $sold_quantity`, `?float $tacos`, `?float $net_billing`, `?float $sales_fee`, `?float $taxes`, `?float $shipping_cost`, `?float $product_cost`, `?float $contribution_margin`, `?float $acos`, `?float $roas`, `?int $clicks`, `?int $impressions`, `?int $orders_count`. Método `toArray(): array` que serializa Carbon como `'Y-m-d'`.

    Criar `tests/Feature/Phase60/AdmanMetricsProviderTest.php` já apontando para a implementação futura (Task 2) — Task 1 grava APENAS os 4 testes de fundação: `test_contract_metrics_provider_existe`, `test_dto_cobre_15_campos_numericos_e_4_metadados` (assertion via `ReflectionClass::getProperties(ReflectionProperty::IS_READONLY)` — 15 tipos `?float|?int` + 4 metadados = 19 total), `test_dto_to_array_serializa_carbon_como_string_ymd`, `test_dto_aceita_construcao_com_campos_numericos_nulos`. Namespace `Tests\Feature\Phase60`. Extends `Tests\TestCase`. Uso de `use RefreshDatabase` mesmo que ainda não escreva no DB — mantém padrão do projeto. Rodar `php artisan test --filter=AdmanMetricsProviderTest` — 4 testes devem falhar (RED) na Task 1 porque implementação real só vem na Task 2 (parte dos testes falha por refletir contract/DTO, parte por método ainda não implementado).

    Comitar como `test(60-02): RED — contract MetricsProvider + DTO`.
  </action>
  <verify>
    <automated>test -f app/Contracts/MetricsProvider.php &amp;&amp; test -f app/Services/Metrics/UnifiedMetricsDto.php &amp;&amp; test -f tests/Feature/Phase60/AdmanMetricsProviderTest.php &amp;&amp; grep -q "interface MetricsProvider" app/Contracts/MetricsProvider.php &amp;&amp; grep -q "final readonly class UnifiedMetricsDto\|final class UnifiedMetricsDto\|readonly class UnifiedMetricsDto" app/Services/Metrics/UnifiedMetricsDto.php &amp;&amp; grep -q "public function readForCompany" app/Contracts/MetricsProvider.php</automated>
  </verify>
  <acceptance_criteria>
    - `interface_exists(App\Contracts\MetricsProvider::class)` retorna true via `php artisan tinker --execute="var_export(interface_exists(App\\Contracts\\MetricsProvider::class));"`
    - `class_exists(App\Services\Metrics\UnifiedMetricsDto::class)` retorna true
    - Contract tem exatamente 3 métodos abstratos (grep `public function` em `MetricsProvider.php` retorna 3 linhas)
    - DTO tem `readonly` marker e propriedades promoted no constructor (verificar via `grep -c 'public readonly\|readonly' app/Services/Metrics/UnifiedMetricsDto.php` >= 1 SE PHP nativo aceitar `final readonly class`; senão cada property individual marcada `public readonly`)
    - Arquivo de teste tem os 4 métodos `test_*` listados
    - Comportamento esperado na Task 1: `php artisan test --filter=AdmanMetricsProviderTest` termina com > 0 falhas (RED confirmado)
  </acceptance_criteria>
  <done>Contract MetricsProvider + DTO UnifiedMetricsDto criados; teste RED committed com 4 assertions básicas falhando por falta da implementação Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar AdmanMetricsProvider + testes de comportamento (GREEN)</name>
  <files>app/Services/Metrics/AdmanMetricsProvider.php, tests/Feature/Phase60/AdmanMetricsProviderTest.php</files>
  <read_first>
    - app/Contracts/MetricsProvider.php (contract criado na Task 1 — assinaturas EXATAS a respeitar)
    - app/Services/Metrics/UnifiedMetricsDto.php (constructor promoted params — ordem importa)
    - app/Services/Sugadores/AdmanSugadoresProvider.php (padrão de composição/estrutura do provider Adman correspondente em outro contract — replicar tom/comentários pt-BR)
    - app/Models/AdmanMetric.php (query e sums)
    - app/Http/Controllers/DashboardController.php linhas 320-410 (padrão de agregação `AdmanMetric::query()->selectRaw('SUM(revenue) as total')->groupBy('company_id')` — replicar mas SEM groupBy porque escopo é 1 empresa)
    - tests/Feature/Phase60/AdmanMetricsProviderTest.php (arquivo criado na Task 1 — vai receber os testes de comportamento adicionais)
  </read_first>
  <behavior>
    - `AdmanMetricsProvider::supports(Company $c)`: true se `$c->adman_account_id !== null && $c->adman_account_id !== ''`. Não checa se existe row em adman_metrics (contas sem histórico ainda são "suportadas" — retornam DTO com nulls).
    - `AdmanMetricsProvider::name()`: retorna literalmente `'adman'`.
    - `AdmanMetricsProvider::readForCompany($c, $from, $to)`: (i) `AdmanMetric::where('company_id', $c->id)->whereBetween('reference_date', [$from->toDateString(), $to->toDateString()])->get()`; (ii) se coleção vazia → retorna DTO com todos os campos numéricos = null (mas `company_id`, `source='adman'`, `period_from`, `period_to` preenchidos); (iii) se >0 rows → SOMA revenue, ad_spend, sold_quantity, net_billing, sales_fee, taxes, shipping_cost, product_cost e MÉDIA de tacos (weighted por revenue quando revenue > 0, senão média simples); (iv) `orders_count`/`clicks`/`impressions`/`acos`/`roas`/`contribution_margin` retornam null (Adman NÃO expõe esses campos por dia — sinal explícito de "fonte não cobre").
    - Testes de comportamento (adicionar aos 4 testes básicos da Task 1):
      - T5: `test_supports_true_quando_adman_account_id_presente`
      - T6: `test_supports_false_quando_adman_account_id_ausente`
      - T7: `test_name_retorna_adman`
      - T8: `test_readForCompany_soma_revenue_e_ad_spend_no_periodo` (criar 3 rows de AdmanMetric via factory em dias distintos dentro do range; asserar `$dto->revenue == 300.0` para valores 100+100+100)
      - T9: `test_readForCompany_retorna_dto_com_nulls_quando_periodo_sem_rows` (empresa com adman_account_id mas 0 rows no período — DTO válido, campos numéricos null)
      - T10: `test_readForCompany_calcula_tacos_ponderado_por_revenue` (2 rows: revenue=100 tacos=10% + revenue=200 tacos=20% → tacos_result ~16.67%)
      - T11: `test_readForCompany_campos_nao_cobertos_por_adman_retornam_null` (asserta clicks/impressions/acos/roas/contribution_margin/orders_count == null)
      - T12: `test_readForCompany_ignora_rows_fora_do_range` (row em D-10, range D-3 a D-1 → não entra na soma)
      - T13: `test_readForCompany_source_field_igual_a_adman` (DTO retorna `->source === 'adman'`)
  </behavior>
  <action>
    Criar `App\Services\Metrics\AdmanMetricsProvider` implementando `App\Contracts\MetricsProvider`. Constructor sem dependências (não injeta `AdmanService` — este provider é puro DB-read, não faz HTTP). Comentário de classe em pt-BR explicando: (a) esta classe NUNCA chama API Adman em runtime; (b) fonte de dados é `adman_metrics` local; (c) coexistência total com `AdmanService::syncCompany()` — este continua populando a tabela, o provider só lê.

    Implementar `supports()` com guard `$company->adman_account_id !== null && $company->adman_account_id !== ''` — sem NP-check no DB (queries no `readForCompany` já resolvem caso vazio).

    Implementar `readForCompany()`: usar `AdmanMetric::query()->where('company_id', $company->id)->whereBetween('reference_date', [$from->toDateString(), $to->toDateString()])->get()`. Se coleção vazia, retornar `new UnifiedMetricsDto(company_id: $company->id, source: 'adman', period_from: $from, period_to: $to, revenue: null, ad_spend: null, sold_quantity: null, tacos: null, net_billing: null, sales_fee: null, taxes: null, shipping_cost: null, product_cost: null, contribution_margin: null, acos: null, roas: null, clicks: null, impressions: null, orders_count: null)`.

    Para coleção com rows, agregar via helpers de Collection: `->sum('revenue')`, `->sum('ad_spend')`, etc. TACOS ponderado: implementar método `private static function tacosPonderado(Collection $rows): ?float` — se soma revenue > 0: `SUM(revenue*tacos) / SUM(revenue)`; senão retornar média simples de tacos; se todas as tacos null, retornar null.

    Campos NÃO cobertos por Adman (`clicks`, `impressions`, `acos`, `roas`, `contribution_margin`, `orders_count`): retornar literalmente `null` — não inferir/calcular. Comentar isso inline em pt-BR referenciando o ADR DATA-04 tabela de precedência.

    Adicionar os testes T5-T13 no arquivo `AdmanMetricsProviderTest.php` (namespace `Tests\Feature\Phase60`). Usar `Company::factory()` e criar rows de `AdmanMetric` via `AdmanMetric::create([...])` explícito (sem factory de AdmanMetric — checar em `database/factories/` se já existe; se sim, usar; se não, criar via `AdmanMetric::create` direto). Datas via `Carbon::parse('2026-07-05')`.

    Rodar `php artisan test --filter=AdmanMetricsProviderTest` — todos 13 testes devem passar (GREEN).

    Rodar `php artisan test tests/Feature/Sugadores` (ou equivalente scope — se existir) para provar zero regressão em testes existentes de Sugadores/Adman/Dashboard.

    Comitar como `feat(60-02): GREEN — AdmanMetricsProvider lê adman_metrics local`.
  </action>
  <verify>
    <automated>test -f app/Services/Metrics/AdmanMetricsProvider.php &amp;&amp; grep -q "class AdmanMetricsProvider implements MetricsProvider\|class AdmanMetricsProvider implements \\\\App\\\\Contracts\\\\MetricsProvider" app/Services/Metrics/AdmanMetricsProvider.php &amp;&amp; php artisan test --filter=AdmanMetricsProviderTest</automated>
  </verify>
  <acceptance_criteria>
    - `class_exists(App\Services\Metrics\AdmanMetricsProvider::class)` retorna true
    - `(new AdmanMetricsProvider())->name() === 'adman'` (via tinker ou test)
    - `php artisan test --filter=AdmanMetricsProviderTest` exit 0, 13 testes verdes, 0 falhas
    - `grep -c "AdmanService" app/Services/Metrics/AdmanMetricsProvider.php` retorna 0 (provider NUNCA importa AdmanService — só Model e Contract)
    - `grep -c "Http::\|::get(\|->get(\$" app/Services/Metrics/AdmanMetricsProvider.php` retorna 0 (sem HTTP calls no provider)
    - Nenhum teste Feature/Unit existente quebrou: `php artisan test --testsuite=Feature 2>&1 | tail -5` mostra mesmo ou maior número de testes verdes que baseline pré-Task-2
    - Comentários em pt-BR (executor faz spot check)
  </acceptance_criteria>
  <done>`AdmanMetricsProvider` implementa o contract, lê de `adman_metrics` sem HTTP calls, 13 testes verdes, zero regressão em testes existentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

- Nenhuma nova trust boundary de rede — provider é read-only local DB.
- Boundary lógica: consumidor confia que `UnifiedMetricsDto` tem shape estável — mitigada por `final readonly` + typed properties.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-01 | Tampering | UnifiedMetricsDto | mitigate | `final readonly class` (PHP 8.2+) impede modificação pós-construção. |
| T-60-02-02 | Information Disclosure | AdmanMetricsProvider | accept | Provider retorna dados agregados de `adman_metrics`; consumidores já tinham acesso via AdmanMetric direto. Sem escalação de privilégio. |
| T-60-02-03 | Denial of Service | Query sem index | mitigate | `AdmanMetric` já tem index em `(company_id, reference_date)` (verificar em migration se ainda não existe). Se não existir, adicionar como follow-up em Phase 60-04 (mas escopo desta phase não altera schema). |
</threat_model>

<verification>
Ao término deste plan:
- `php artisan test --filter=AdmanMetricsProviderTest` passa (13/13)
- `php artisan test --testsuite=Feature` mantém ou aumenta contagem de testes verdes (zero regressão baseline)
- Nenhum arquivo em `app/Http/Controllers/`, `app/Services/AdmanService.php`, `app/Models/AdmanMetric.php` foi modificado
- Nova classe visível via `php artisan tinker --execute="echo class_exists('App\\Services\\Metrics\\AdmanMetricsProvider') ? 'ok' : 'fail';"` retorna `ok`
</verification>

<success_criteria>
1. Contract `App\Contracts\MetricsProvider` existe e cobre `supports/name/readForCompany`
2. DTO `UnifiedMetricsDto` imutável cobrindo os 15 campos + metadados (company_id, source, period)
3. `AdmanMetricsProvider` lê `adman_metrics` sem quebrar em nenhum dos 3 sub-casos (com adman_account_id + com rows, com adman_account_id + sem rows, sem adman_account_id)
4. TACOS ponderado por revenue implementado e testado
5. Zero regressão em testes Feature existentes
</success_criteria>

<output>
Criar `.planning/phases/60-base-multi-fonte-backend-ml-adman-unificado/60-02-SUMMARY.md` sumarizando: arquivos criados, contagem de testes por Task, cobertura efetiva dos campos DTO (quais campos Adman preenche vs deixa null), baseline pré/pós regressão, próximo plan (60-03).
</output>
