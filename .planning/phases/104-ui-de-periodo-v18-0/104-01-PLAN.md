---
plan: 104-01
phase: 104
wave: 1
autonomous: true
depends_on: []
authored: main-thread-inline (planner subagent indisponível por limite de sessão 2026-07-17 — rodar gsd-plan-checker antes de executar)
requirements: [UIP-02, UIP-03]
files_modified:
  - app/Http/Controllers/PerformanceController.php
  - app/Http/Controllers/PortfolioController.php
  - tests/Feature/V18/PeriodoBonusPayloadTest.php
must_haves:
  truths:
    - "O payload do ranking /performance carrega o objeto `periodo` (current_start/end, baseline_start/end, mode, comparison_mode, is_current_month, is_closed) vindo do resultado do compute — não mais a string estática 'Últimos 30 dias'"
    - "No modo bônus/mês-fechado, o payload carrega `bonus` (competence_month, payment_month); no modo em-curso, `bonus` é null"
    - "O preset 'Bônus atual' resolve o último mês fechado via MetricPeriodResolver (last_closed_month) e computa por ele — competência do mês fechado, não o mês em curso"
    - "A carteira (individual + consolidada) já expõe `periodo` (Fase 103); esta task só ADICIONA o bloco `bonus` quando o período resolvido é fechado"
  artifacts:
    - path: app/Http/Controllers/PerformanceController.php
      provides: "passthrough de periodo/bonus no payload do ranking + preset bônus-atual (último mês fechado)"
    - path: tests/Feature/V18/PeriodoBonusPayloadTest.php
      provides: "prova periodo/bonus no payload nos 3 modos"
---

# 104-01 — Backend: payload periodo/bonus + preset Bônus atual

## Contexto verificado (2026-07-17, thread principal)
- `PerformanceController` (linha ~85) escolhe `$mesReferencia` de `?mes=YYYY-MM` ou default mês corrente; `$ehMesEmCurso` (89); chama `computeCached($u, $mesReferencia)` (117/123). O service da Fase 102 já retorna `periodo` e `bonus` no resultado do `compute()`.
- Payload hoje: linha ~563 tem `'periodo' => 'Últimos 30 dias'` **string estática** — trocar pelo objeto real.
- `PortfolioController` já expõe `periodo` (linhas 469, 799 — Fase 103). Falta só o bloco `bonus` no modo fechado.
- `computeOficial(User)` existe (Fase 102) mas o ranking NÃO o chama — usa `computeCached($u, $mes)`. Para "Bônus atual", resolver o último mês fechado e passar como `$mesReferencia` (o `compute()` já usa a régua nova para mês fechado).

## Task 1 (tdd): PerformanceController passa periodo/bonus + preset bônus-atual
<files>app/Http/Controllers/PerformanceController.php, tests/Feature/V18/PeriodoBonusPayloadTest.php</files>
<action>
1. RED: teste (Http::preventStrayRequests + Http::fake vazio→fallback determinístico) provando 3 modos:
   - `?modo=em_curso` (ou ausência) → payload.periodo.is_current_month=true, payload.bonus=null.
   - `?modo=bonus_atual` com now=2026-07-20 → periodo do último fechado (01/06..30/06 vs 02/05..31/05), bonus.competence_month='2026-06', payment_month='2026-07'.
   - `?mes=2026-05` (mês fechado específico) → periodo com baseline janela-de-mesmo-tamanho (31/03..30/04), bonus.competence_month='2026-05'.
2. GREEN: no `index()`, resolver o `$mesReferencia` também a partir de um param semântico `?modo=`:
   - `em_curso` → mês corrente (comportamento atual).
   - `bonus_atual` → resolver `last_closed_month` via MetricPeriodResolver e usar a competência como `$mesReferencia`.
   - `?mes=YYYY-MM` → mês fechado específico (comportamento atual, mantido).
   O `?modo=` é conveniência de UI; NÃO cria segundo score (DESEMP-02) — só decide QUAL mês. Manter `computeCached`.
3. Trocar a linha ~563 `'periodo' => 'Últimos 30 dias'` por `'periodo' => $periodoResolvido` (o objeto do resolver, resolvido 1× no nível da página) e ADICIONAR `'bonus' => $bonusMeta` (competence/payment quando fechado; null quando em curso). Fonte: o `periodo`/`bonus` já vêm no resultado do `computeCached` (idênticos entre profissionais do mesmo mês) — pode ler do primeiro resultado OU resolver no controller via MetricPeriodResolver (preferir o resolver direto no controller, 1 chamada, sem depender de haver ≥1 profissional).
</action>
<verify><automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/V18/PeriodoBonusPayloadTest.php</automated></verify>
<done>Payload do ranking carrega periodo (objeto) + bonus nos 3 modos; preset bônus-atual resolve o último fechado; sem segundo score.</done>

## Task 2 (tdd): PortfolioController adiciona bloco `bonus` no modo fechado
<files>app/Http/Controllers/PortfolioController.php, tests/Feature/V18/PeriodoBonusPayloadTest.php</files>
<action>
Nas duas funções já migradas (renderCarteiraProfissional, renderCarteirasConsolidadas), quando o período resolvido é fechado (`is_closed=true`), ADICIONAR ao payload `bonus` = {competence_month, payment_month} derivado do resolver; quando em curso, `bonus=null`. NÃO recomputar período (já resolvido na Fase 103) — só derivar o bloco bonus do `periodo` já em mãos. FRONTEIRA: só o bloco bonus; não tocar cálculo/elegibilidade.
</action>
<verify><automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/V18/PeriodoBonusPayloadTest.php tests/Feature/V18/CarteiraPeriodoDiffTest.php tests/Feature/V18/CarteiraConsolidadaPeriodoTest.php</automated></verify>
<done>Carteira individual e consolidada carregam bonus.competence/payment no modo fechado; testes da 103 seguem verdes.</done>

## Fronteira
Só PerformanceController + PortfolioController (payload) + teste V18. NÃO tocar DesempenhoScoreService/resolver/diff-service (fechados), CarteiraContextService, .jsx (Wave 2), NPS. Sessão paralela ativa: git add caminhos explícitos; git status antes de cada commit; sem gsd-sdk state.*; sem STATE.md.

## Isolamento HTTP
O ranking/carteira alcançam compute() → AdmanMetricDiffService (HTTP real). Todo teste desta task com Http::preventStrayRequests() + Http::fake() determinístico.
