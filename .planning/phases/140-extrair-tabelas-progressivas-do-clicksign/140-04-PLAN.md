---
phase: 140-extrair-tabelas-progressivas-do-clicksign
plan: 04
type: execute
wave: 3
depends_on: [140-03]
files_modified:
  - database/migrations/2026_09_08_100000_create_contrato_tabela_propostas_table.php
  - app/Models/ContratoTabelaProposta.php
  - database/factories/ContratoTabelaPropostaFactory.php
  - app/Console/Commands/ClicksignExtrairTabelas.php
  - tests/Feature/Phase140/Phase140PropostaSchemaTest.php
  - tests/Feature/Phase140/Phase140GravarPropostasTest.php
autonomous: true
requirements: [TAB-07]

must_haves:
  truths:
    - "O que o comando leu pode ser conferido depois, sem rodar a leitura de novo"
    - "Rodar o comando duas vezes não duplica proposta nem apaga o que alguém já conferiu"
    - "Nenhuma tabela de cobrança do sistema muda por causa deste plano"
  artifacts:
    - path: "database/migrations/2026_09_08_100000_create_contrato_tabela_propostas_table.php"
      provides: "tabela contrato_tabela_propostas"
      contains: "contrato_tabela_propostas"
    - path: "app/Models/ContratoTabelaProposta.php"
      provides: "model auditável da proposta de tabela"
      contains: "LogsActivity"
  key_links:
    - from: "app/Console/Commands/ClicksignExtrairTabelas.php"
      to: "app/Models/ContratoTabelaProposta.php"
      via: "opção --gravar"
      pattern: "ContratoTabelaProposta"
---

<objective>
Guardar o que a leitura descobriu, para uma pessoa poder conferir depois com calma.

Propósito: aprovado o relatório (checkpoint do 140-03), a conferência de ~123 contratos não cabe numa
sessão de terminal. As propostas precisam existir como dado para a tela do plano 140-05 mostrar e para
o trabalho de conferência ser retomável.

Saída: a tabela `contrato_tabela_propostas`, seu model e a opção `--gravar` no comando existente.

⛔ **BLOQUEADO até o checkpoint do plano 140-03 ser respondido com "seguir".**

⚠️ Este plano continua **sem tocar em cobrança**: gravar proposta não é gravar tabela. Nenhuma linha
de `empresa_faixas_faturamento` muda aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-extrair-tabelas-progressivas-do-clicksign/140-CONTEXT.md
@.planning/phases/140-extrair-tabelas-progressivas-do-clicksign/140-03-SUMMARY.md
@app/Models/EmpresaFaixaFaturamento.php
@database/migrations/2026_09_02_100002_create_empresa_faixas_faturamento_table.php

<interfaces>
<!-- Armadilhas de banco JÁ PAGAS neste projeto. Respeitar as três. -->

1. **Nome de índice acima de 64 caracteres** quebra no MariaDB (erro 1059) e passa no SQLite dos
   testes — a migration fica `Pending` em produção com a tabela criada SEM o índice.
   → Nomear os índices à mão, curtos: `ctp_envelope_unq`, `ctp_situacao_idx`.

2. **`nullOnDelete()` exige `nullable()` antes** (erro 1830 no MariaDB, invisível no SQLite).
   → `$table->foreignId('company_id')->nullable()->constrained()->nullOnDelete();`

3. **`enum()` quebra o SQLite dos testes.**
   → Usar `string()` + constantes `public const` no model, como o resto do projeto.

Estrutura de faixa que o parser (140-02) devolve e que a coluna `faixas` guarda como JSON:
`[{"ordem":1,"limite_superior":500000.00,"valor":3000.00,"valor_e_piso":false}, ...]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: tabela e model da proposta de tabela de cobrança</name>
  <files>database/migrations/2026_09_08_100000_create_contrato_tabela_propostas_table.php, app/Models/ContratoTabelaProposta.php, database/factories/ContratoTabelaPropostaFactory.php, tests/Feature/Phase140/Phase140PropostaSchemaTest.php</files>
  <behavior>
    - A migration cria `contrato_tabela_propostas` e o rollback a remove.
    - `clicksign_envelope_id` é único: o mesmo contrato não vira duas propostas.
    - Apagar a empresa deixa a proposta viva com `company_id` nulo (não some o histórico da leitura).
    - `faixas` volta como array PHP já decodificado.
    - Alterar uma proposta gera registro no log de atividade.
    - A situação nasce `pendente`.
  </behavior>
  <action>
    Migration `create_contrato_tabela_propostas_table` com as colunas:

    | coluna | tipo | observação |
    |---|---|---|
    | `id` | id | |
    | `clicksign_envelope_id` | string(64) | índice único nomeado `ctp_envelope_unq` |
    | `nome_envelope` | string(255) | como veio da Clicksign |
    | `envelope_situacao` | string(32) | `closed`, `canceled`… |
    | `envelope_data` | date nullable | a data resolvida pela cadeia defensiva do 140-01 |
    | `company_id` | foreignId nullable → companies, `nullOnDelete` | o palpite, ainda não confirmado |
    | `confianca` | string(16) | `certo` / `provavel` / `incerto` |
    | `pontuacao` | decimal(5,2) nullable | |
    | `ambiguo` | boolean default false | |
    | `candidatos` | json nullable | os 3 melhores palpites, para a tela oferecer |
    | `tipo_cobranca` | string(16) | `tabela` / `valor_fixo` / `indefinido` / `ilegivel` |
    | `valor_fixo` | decimal(10,2) nullable | |
    | `faixas` | json nullable | shape de `EmpresaFaixaFaturamento` |
    | `cnpj_lido` | string(14) nullable | só dígitos |
    | `razao_social_lida` | string(255) nullable | |
    | `motivo` | string(255) nullable | por que não deu para ler |
    | `situacao` | string(16) default `pendente` | `pendente` / `confirmada` / `descartada` |
    | `confirmado_por` | foreignId nullable → users, `nullOnDelete` | |
    | `confirmado_em` | timestamp nullable | |
    | timestamps | | |

    Mais um índice comum nomeado `ctp_situacao_idx` em (`situacao`).

    ⚠️ Respeitar as três armadilhas listadas em `<interfaces>` — nome de índice curto, `nullable()`
    antes de `nullOnDelete()`, nada de `enum()`. Escrever num comentário da migration por que, com
    referência ao aprendizado do projeto (elas passam no SQLite e quebram no deploy).

    Model `App\Models\ContratoTabelaProposta` com `HasFactory`, `LogsActivity` (log name
    `tabela_proposta`, descrição em pt-BR — o dado vira cobrança, precisa de trilha), `$fillable`,
    casts (`faixas` e `candidatos` → `array`, `valor_fixo`/`pontuacao` → `decimal`, `ambiguo` → bool,
    `envelope_data` → date, `confirmado_em` → datetime), constantes
    `SITUACAO_PENDENTE|CONFIRMADA|DESCARTADA`, `CONFIANCA_CERTO|PROVAVEL|INCERTO`,
    `TIPO_TABELA|VALOR_FIXO|INDEFINIDO|ILEGIVEL`, relação `company()` e `confirmadoPor()`, e scope
    `pendentes()`.

    Factory em `database/factories/ContratoTabelaPropostaFactory.php` com dados fictícios.

    Testes em `tests/Feature/Phase140/Phase140PropostaSchemaTest.php`
    (`Phase140PropostaSchemaTest`), com `RefreshDatabase`: colunas existem, unique do envelope
    barra duplicata, `company_id` vira null ao apagar a empresa, casts funcionam, atividade é
    registrada na alteração.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter=Phase140PropostaSchemaTest</automated>
  </verify>
  <done>A tabela existe com as guardas de MariaDB respeitadas, o model é auditável e a duplicata de envelope é impossível.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: opção --gravar no comando de leitura</name>
  <files>app/Console/Commands/ClicksignExtrairTabelas.php, tests/Feature/Phase140/Phase140GravarPropostasTest.php</files>
  <behavior>
    - Sem `--gravar`, o comando continua exatamente como estava: gera relatório e não escreve nada.
    - Com `--gravar`, cada contrato lido vira (ou atualiza) uma proposta `pendente`.
    - Rodar duas vezes seguidas mantém uma proposta por envelope.
    - Uma proposta já `confirmada` ou `descartada` NÃO é sobrescrita por uma nova rodada.
    - Mesmo com `--gravar`, a contagem de `empresa_faixas_faturamento` não muda.
    - O relatório continua sendo gerado do mesmo jeito.
  </behavior>
  <action>
    Acrescentar `{--gravar}` à assinatura de `ClicksignExtrairTabelas` e, quando ligada, gravar cada
    linha lida em `ContratoTabelaProposta` via `updateOrCreate(['clicksign_envelope_id' => ...], [...])`.

    Duas guardas explícitas (TAB-07):
    1. **Nunca sobrescrever conferência humana**: antes do `updateOrCreate`, se já existe proposta com
       `situacao` diferente de `pendente`, pular e somar num contador "já conferidas, mantidas". Uma
       rodada nova não pode desfazer o trabalho de quem conferiu.
    2. **Escrita restrita a esta tabela**: o comando não pode escrever em
       `empresa_faixas_faturamento`, `grupo_faixas_faturamento` nem `companies`. Quem grava tabela de
       cobrança é o plano 140-05, e só com confirmação humana (D-05/D-06). Comentário no código
       dizendo isso, com o motivo: um vínculo errado grava a tabela de uma empresa em outra e ninguém
       revisa depois.

    A opção entra na descrição do comando em pt-BR, sem jargão: *"--gravar: guarda o que foi lido
    para conferir depois na tela. Continua sem alterar cobrança."*

    Ao final, o resumo impresso ganha duas linhas: quantas propostas foram criadas/atualizadas e
    quantas foram mantidas por já terem sido conferidas.

    Testes em `tests/Feature/Phase140/Phase140GravarPropostasTest.php`
    (`Phase140GravarPropostasTest`), com `Http::fake()` + `Storage::fake('local')` +
    `RefreshDatabase` + `--pausa-ms=0`: sem `--gravar` nada é criado; com `--gravar` as propostas
    aparecem; rodar duas vezes não duplica; proposta `confirmada` sobrevive intacta a uma segunda
    rodada; `empresa_faixas_faturamento` continua com a mesma contagem.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter=Phase140GravarPropostasTest</automated>
  </verify>
  <done>Com `--gravar` as propostas ficam guardadas e re-rodar é seguro; sem a opção o comando segue só lendo; cobrança segue intocada.</done>
</task>

</tasks>

<threat_model>
## Fronteiras de confiança

| Fronteira | Descrição |
|-----------|-----------|
| Leitura automática → banco do sistema | dado inferido de PDF entra como registro persistente |
| Rodada nova → trabalho humano já feito | reprocessamento pode apagar conferência |

## Registro STRIDE

| ID | Categoria | Componente | Disposição | Mitigação |
|----|-----------|------------|------------|-----------|
| T-140-14 | Tampering | `--gravar` | mitigate | escrita restrita a `contrato_tabela_propostas`; teste prova que `empresa_faixas_faturamento` não muda |
| T-140-15 | Tampering | reprocessamento | mitigate | proposta `confirmada`/`descartada` nunca é sobrescrita; unique por envelope impede duplicata |
| T-140-16 | Repudiation | proposta alterada | mitigate | `LogsActivity` no model (`tabela_proposta`); `confirmado_por`/`confirmado_em` nascem preparados para o plano 140-05 |
| T-140-17 | Information Disclosure | `contrato_tabela_propostas` | accept | fica no banco do sistema, atrás de autenticação; o mesmo grau de exposição das faixas que já existem |
| T-140-18 | Denial of Service | migration em produção | mitigate | índices nomeados curtos e `nullable()` antes de `nullOnDelete()` — os dois erros de MariaDB que o SQlite dos testes não pega |
</threat_model>

<verification>
1. `C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase140"` — verde.
2. `C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase122|Phase136|Phase137|Phase138|Phase139|Phase140"` — gate preservado.
3. `grep -n "enum(" database/migrations/2026_09_08_100000_create_contrato_tabela_propostas_table.php` — zero ocorrências.
4. `grep -n "nullable()->constrained()" database/migrations/2026_09_08_100000_create_contrato_tabela_propostas_table.php` — presente nas duas FKs.
5. `grep -rn "EmpresaFaixaFaturamento\|GrupoFaixaFaturamento" app/Console/Commands/ClicksignExtrairTabelas.php` — zero ocorrências (o comando não grava cobrança).
</verification>

<success_criteria>
- O resultado da leitura sobrevive ao fim da rodada e pode ser conferido depois.
- Re-rodar é seguro: não duplica e não desfaz conferência humana.
- Nenhuma tabela de cobrança do sistema é tocada por este plano.
- A migration respeita as três armadilhas de MariaDB já pagas pelo projeto.
</success_criteria>

<output>
Criar `.planning/phases/140-extrair-tabelas-progressivas-do-clicksign/140-04-SUMMARY.md` ao terminar.
</output>
