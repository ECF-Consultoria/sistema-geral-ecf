---
phase: 115-suite-e2e-documenta-o-da-regra-de-valor-v20-0
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/hubspot-regra-de-valor.md
autonomous: true
requirements: [HUB-DOC-01]

must_haves:
  truths:
    - "Existe um doc técnico curto que explica a regra de valor mensal×anual sem jargão não-explicado"
    - "O doc cobre: normalização monthly×annually (P1Y), MRR×ARR, serviço único×mensal, inferência por tolerância de 5%, e quando valor_revisar é marcado"
    - "O doc é fiel ao comportamento real do HubspotValueResolver (não inventa regra) e aponta a coluna de auditoria onde cada valor fica"
  artifacts:
    - path: "docs/hubspot-regra-de-valor.md"
      provides: "Documentação da regra de valor operacional mensal×anual do handoff Comercial HubSpot"
      min_lines: 40
      contains: "valor_revisar"
  key_links:
    - from: "docs/hubspot-regra-de-valor.md"
      to: "app/Services/Hubspot/HubspotValueResolver.php"
      via: "descreve o shape de saída (valor_operacional, confidence, warning) e os ramos da regra"
      pattern: "valor_operacional"
---

<objective>
Escrever a documentação curta da regra de valor operacional mensal×anual do
handoff Comercial HubSpot (HUB-DOC-01) — o último critério de aceite da
milestone v20.0.

Decisão de local: **doc técnico dedicado** em `docs/hubspot-regra-de-valor.md`
(o ROADMAP permite "CLAUDE.md OU novo doc técnico"). Motivo: manter CLAUDE.md
estável e evitar churn de arquivo compartilhado entre sessões paralelas
(convenção do projeto sobre a árvore de trabalho compartilhada); um doc
dedicado é auto-contido e fácil de referenciar.

O doc deve explicar, de forma curta e sem jargão não-explicado (convenção do
projeto: se o termo não é auto-explicativo, explicar), a regra que resolve o
bug real R$ 36.000 (anual) × R$ 3.000 (mensal) e ser FIEL ao comportamento
real do `HubspotValueResolver` — não inventar regra nova.

Auditoria de cobertura desta fatia:
- HUB-DOC-01 (doc da regra) → NÃO EXISTIA; criado por esta tarefa.

Purpose: quem for manter o handoff HubSpot entende em 5 minutos por que
`valor_contratado` grava o mensal e onde o valor anual bruto fica guardado.
Output: `docs/hubspot-regra-de-valor.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Escrever docs/hubspot-regra-de-valor.md</name>
  <files>docs/hubspot-regra-de-valor.md</files>
  <read_first>
    - app/Services/Hubspot/HubspotValueResolver.php (fonte da verdade: shape de 8 chaves + ramos monthly/annually/mrr/único/tolerância/valor_revisar — o doc DEVE espelhar exatamente estes ramos)
    - .planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-CONTEXT.md (bloco decisions e specifics: os 6 casos e a invariante de não-regressão)
    - app/Models/ContratoServico.php (colunas de auditoria hubspot_valor_original/hubspot_valor_normalizado_mensal/hubspot_valor_confidence/hubspot_valor_warning/hubspot_billing_frequency onde a proveniência fica gravada)
  </read_first>
  <action>
    Criar o doc em pt-BR, curto, com EXATAMENTE estas 7 sub-seções nomeadas (headings de nível 2, "## "):
    (1) "## Por que a regra existe" — o bug R$36.000 anual × R$3.000 mensal; o valor OPERACIONAL gravado em contratos_servico.valor_contratado é sempre o mensal quando o serviço é mensal.
    (2) "## Mensal x Anual (normalizacao)" — line item recurringbillingfrequency 'monthly' + price numérico gera price×quantity (invariante de não-regressão); 'annually' + price gera price×quantity/12; confiança 'high' só quando hs_recurring_billing_period começa com 'P1Y' (12 meses), senão 'medium'.
    (3) "## MRR x ARR" — hs_mrr (receita mensal recorrente) é fonte forte: prevalece e vira o valor_operacional; hs_arr (receita anual recorrente) fica como valor_original/anual bruto. Explicar as siglas em português na primeira menção (MRR = receita mensal recorrente; ARR = receita anual recorrente).
    (4) "## Servico unico x servico mensal" — serviço único NUNCA divide por 12 (usa amount ou price×quantity cheio); só o serviço mensal normaliza para mensal.
    (5) "## Inferencia por tolerancia (5%)" — quando só há amount (line item ou deal.amount legado), compara amount/12 com o valor_padrao do serviço (ou com o price) dentro de 5%; se bate, usa amount/12 com confiança 'medium' + warning de inferência.
    (6) "## Quando marca valor_revisar" — inferência não segura (amount/12 não bate na tolerância, ou faltam dados) gera confiança 'low' + warning contendo a marca 'valor_revisar'; isso vira a pendência valor_revisar na listagem Comercial (só para empresas origem HubSpot).
    (7) "## Onde a proveniencia fica gravada" — tabela curta mapeando as chaves de saída (valor_operacional gera valor_contratado; valor_original gera hubspot_valor_original; normalizado_mensal gera hubspot_valor_normalizado_mensal; confidence gera hubspot_valor_confidence; warning gera hubspot_valor_warning; billing_frequency gera hubspot_billing_frequency).
    Fechar com uma linha apontando os testes que provam a regra (tests/Unit/HubspotValueResolverTest.php). NÃO colar o código-fonte do resolver; descrever comportamento. Manter curto (foco, não exaustivo).
  </action>
  <acceptance_criteria>
    - O arquivo docs/hubspot-regra-de-valor.md existe
    - `grep -c '^## ' docs/hubspot-regra-de-valor.md` retorna 7 (as 7 sub-seções nomeadas)
    - `grep -i 'valor_revisar' docs/hubspot-regra-de-valor.md` encontra a explicação de quando a marca é aplicada
    - `grep -i 'P1Y' docs/hubspot-regra-de-valor.md` e `grep -i 'MRR' docs/hubspot-regra-de-valor.md` retornam resultado
    - `grep -i 'hubspot_valor_original' docs/hubspot-regra-de-valor.md` retorna resultado (mapa de auditoria presente)
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/hubspot-regra-de-valor.md && [ "$(grep -c '^## ' docs/hubspot-regra-de-valor.md)" -eq 7 ] && grep -qi 'valor_revisar' docs/hubspot-regra-de-valor.md && grep -qi 'P1Y' docs/hubspot-regra-de-valor.md && grep -qi 'hubspot_valor_original' docs/hubspot-regra-de-valor.md && echo OK</automated>
  </verify>
  <done>O doc existe com as 7 sub-seções, fiel ao resolver, e explica quando valor_revisar é marcado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc técnico → repositório | O doc não pode conter segredos (tokens, HMAC) nem PII real; só descreve a regra com exemplos sintéticos (R$36.000/R$3.000) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03-01 | Information Disclosure | docs/hubspot-regra-de-valor.md | mitigate | Revisar que o doc usa só valores/exemplos sintéticos; nenhum token, segredo ou dado de cliente real |
| T-115-03-SC | Tampering | dependências npm/pip/cargo | accept | Doc-only — nenhum pacote instalado; nenhum código de produção alterado |
</threat_model>

<verification>
- O comando de <verify> passa (arquivo existe, 7 sub-seções, termos-chave presentes).
- Revisão de conteúdo: o doc não contradiz o comportamento real do HubspotValueResolver.
</verification>

<success_criteria>
- HUB-DOC-01 entregue: doc curto, em pt-BR, fiel à regra, cobrindo os 5 eixos (mensal×anual, MRR×ARR, único×mensal, tolerância, valor_revisar) + mapa de auditoria.
- Sem segredos nem PII no doc.
</success_criteria>

<output>
Create `.planning/phases/115-suite-e2e-documenta-o-da-regra-de-valor-v20-0/115-03-SUMMARY.md` when done
</output>
