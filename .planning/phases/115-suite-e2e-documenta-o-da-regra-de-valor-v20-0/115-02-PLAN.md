---
phase: 115-suite-e2e-documenta-o-da-regra-de-valor-v20-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Phase114HubspotReplayTest.php
  - tests/Feature/Phase114ComercialListagemEnrichmentTest.php
  - tests/Feature/Phase115HubspotInvariantesTest.php
autonomous: true
requirements: [HUB-TEST-04, HUB-TEST-05]

must_haves:
  truths:
    - "O replay prova: line item sem mapping não cria contrato → admin cadastra mapping → replay cria o contrato faltante e o efeito prático da pendência some (contrato materializado)"
    - "O replay é idempotente: rodar 2x não duplica company nem contrato"
    - "A listagem Comercial prova contato/observação/confiança/warning por linha e valor_revisar SÓ para empresa origem HubSpot (empresa legada não recebe pendência nova)"
    - "Um teste-guarda dedicado falha se qualquer requisição HubSpot real vazar (Http::preventStrayRequests) — 100% das chamadas são fake para api.hubapi.com"
    - "Nenhum token/segredo HubSpot (access_token 'token-fake', client_secret HMAC) aparece no canal de log ecf-webhooks durante o processamento do webhook"
  artifacts:
    - path: "tests/Feature/Phase114HubspotReplayTest.php"
      provides: "Cobertura do replay (mapping tardio + idempotência)"
      contains: "test_replay_cria_contrato_faltante_apos_mapping_cadastrado"
    - path: "tests/Feature/Phase114ComercialListagemEnrichmentTest.php"
      provides: "Cobertura da listagem enriquecida + pendências só-HubSpot"
      contains: "test_empresa_legada_NAO_recebe_nenhuma_pendencia_nova"
    - path: "tests/Feature/Phase115HubspotInvariantesTest.php"
      provides: "Invariantes transversais: guarda anti-rede-real + tokens fora do log"
      min_lines: 60
      contains: "preventStrayRequests"
  key_links:
    - from: "tests/Feature/Phase115HubspotInvariantesTest.php"
      to: "Illuminate\\Support\\Facades\\Http"
      via: "Http::preventStrayRequests antes do fake"
      pattern: "preventStrayRequests"
    - from: "tests/Feature/Phase115HubspotInvariantesTest.php"
      to: "canal de log ecf-webhooks"
      via: "Monolog TestHandler capturando registros"
      pattern: "ecf-webhooks"
---

<objective>
Fechar a fatia final de teste da milestone v20.0: auditar o replay
(`Phase114HubspotReplayTest`) e a listagem Comercial enriquecida
(`Phase114ComercialListagemEnrichmentTest`), ambos já escritos nas Fases 114, e
CRIAR a suíte de invariantes transversais que hoje NÃO existe:
`Phase115HubspotInvariantesTest`.

Auditoria de cobertura desta fatia:
- HUB-TEST-04 (replay) → JÁ COBERTO por Phase114HubspotReplayTest (Tarefa 1).
- HUB-TEST-05 (listagem) → campos/pendências JÁ COBERTOS por
  Phase114ComercialListagemEnrichmentTest (Tarefa 2); PORÉM os dois invariantes
  transversais do critério ("nenhum teste chama HubSpot real" + "tokens nunca
  em log") NÃO têm teste-guarda dedicado → LACUNA fechada na Tarefa 3.

Purpose: garantir que o efeito prático do replay é comprovado, que a listagem
não vaza pendência para empresa legada, e que os dois invariantes de segurança
da milestone são executáveis (não só implícitos).
Output: 2 suítes auditadas verdes + 1 suíte nova de invariantes verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-ui-comercial-campos-pend-ncias-novas-comando-de-replay-v20-0/114-CONTEXT.md

<interfaces>
<!-- Contratos que o executor precisa para a suíte nova de invariantes. Extraído do código. -->

Canal de log (config/logging.php):
- 'ecf-webhooks' → driver 'daily', path storage_path('logs/ecf-webhooks.log').
  O controller loga via Log::channel('ecf-webhooks')->info/warning/error.

Token/segredo (App\Services\HubspotApiClient):
- Http::withToken($this->token) onde $token = config('services.hubspot.access_token').
  Nos testes o access_token é 'token-fake' e o client_secret é a SECRET local.

Padrão de disparo do webhook (replicar de Phase113HubspotEnrichmentTest):
- POST /api/webhooks/hubspot com HMAC v3 = base64(hmac_sha256(secret, 'POST'+url+body+ts)).
- Header X-HubSpot-Signature-v3 + X-HubSpot-Request-Timestamp.
- Http::fake com hosts api.hubapi.com/crm/v3/objects/... (deals/companies/contacts/line_items).

Comando de replay:
- `php artisan hubspot:reprocess-event {id}` (classe App\Console\Commands\ReprocessHubspotEvent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Auditar Phase114HubspotReplayTest (HUB-TEST-04)</name>
  <files>tests/Feature/Phase114HubspotReplayTest.php</files>
  <read_first>
    - tests/Feature/Phase114HubspotReplayTest.php (estado real — 3 métodos: efeito prático, idempotência, evento inexistente)
    - app/Console/Commands/ReprocessHubspotEvent.php (comando hubspot:reprocess-event)
    - app/Http/Controllers/Api/HubspotWebhookController.php (reprocessarEvento + guard hubspot_line_item_id em persistirContratos + payload line_items_nao_mapeados)
  </read_first>
  <action>
    Confirmar que o SC4 está provado: line item "Serviço X" sem
    HubspotLineItemMapping → empresa criada, contrato NÃO (assert
    ContratoServico::count()===0 + payload tem line_items_nao_mapeados); admin
    cadastra Servico + mapping; replay cria o contrato (assert count 0→1 +
    servico_id/company_id corretos). Reforçar o "zera o efeito prático da
    pendência" com UM assert adicional: após o replay, recarregar o
    HubspotEvento e assertar que o line item "Serviço X" NÃO consta mais em
    payload['line_items_nao_mapeados'] (ou que o array ficou vazio) — provando
    que a pendência foi materializada, não só que o contrato existe. Confirmar
    idempotência (rodar 2x não duplica) e o caso de id inexistente (exit 1).
    Adicionar docblock de rastreabilidade SC4→método. Http::fake em 100%.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=Phase114HubspotReplayTest` sai com código 0
    - O arquivo contém test_replay_cria_contrato_faltante_apos_mapping_cadastrado e test_replay_e_idempotente_rodando_2x
    - O teste do efeito prático assere que line_items_nao_mapeados não contém mais "Serviço X" após o replay
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=Phase114HubspotReplayTest</automated>
  </verify>
  <done>O SC4 está rastreado, o efeito prático da pendência é comprovado e a suíte roda verde.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Auditar Phase114ComercialListagemEnrichmentTest (HUB-TEST-05 — listagem)</name>
  <files>tests/Feature/Phase114ComercialListagemEnrichmentTest.php</files>
  <read_first>
    - tests/Feature/Phase114ComercialListagemEnrichmentTest.php (estado real — 16 métodos: pendências, payload, isolamento origem)
    - app/Http/Controllers/ComercialController.php (método listagem: payload enriquecido + calcularPendenciasComerciais + guarda de origem HubSpot)
  </read_first>
  <action>
    Confirmar que o SC5 (parte listagem) está provado: contato/observação
    expostos por linha (test_payload_expoe_campos_de_contato_e_ids_hubspot),
    bloco de valor confiança/warning por contrato
    (test_contrato_expoe_bloco_de_valor_hubspot com hubspot_valor_confidence e
    hubspot_valor_warning), e o gate crítico "valor_revisar/pendências novas SÓ
    para origem HubSpot" (test_empresa_legada_NAO_recebe_nenhuma_pendencia_nova
    assere is_origem_hubspot=false + pendencias_comerciais===[]). Adicionar
    docblock de rastreabilidade SC5(listagem)→método. NÃO duplicar cenários; só
    reforçar assert se algum dos campos (nome_contato, cargo_contato,
    hubspot_observacao, hubspot_valor_confidence, hubspot_valor_warning) não
    estiver literalmente assertado na listagem.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=Phase114ComercialListagemEnrichmentTest` sai com código 0
    - O arquivo contém test_payload_expoe_campos_de_contato_e_ids_hubspot, test_contrato_expoe_bloco_de_valor_hubspot e test_empresa_legada_NAO_recebe_nenhuma_pendencia_nova
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=Phase114ComercialListagemEnrichmentTest</automated>
  </verify>
  <done>A parte-listagem do SC5 está rastreada (contato/observação/confiança/warning + valor_revisar só-HubSpot) e verde.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Criar Phase115HubspotInvariantesTest — guarda anti-rede-real + tokens fora do log (HUB-TEST-05 — transversais)</name>
  <files>tests/Feature/Phase115HubspotInvariantesTest.php</files>
  <read_first>
    - tests/Feature/Phase113HubspotEnrichmentTest.php (padrão de setUp/config, HMAC v3, disparaWebhook, mock de fakes — REPLICAR esse padrão, não reinventar)
    - tests/Feature/Phase34HubspotWebhookTest.php (uso existente de Http::preventStrayRequests/assertNothingSent como referência de convenção)
    - app/Services/HubspotApiClient.php (Http::withToken($token) — onde o Bearer entra; token vem de config('services.hubspot.access_token'))
    - config/logging.php (canal 'ecf-webhooks' driver daily)
    - app/Http/Controllers/Api/HubspotWebhookController.php (todos os Log::channel('ecf-webhooks')-> que rodam durante o webhook)
  </read_first>
  <action>
    Criar a suíte nova `Phase115HubspotInvariantesTest` (namespace Tests\Feature,
    use RefreshDatabase) com o MESMO setUp/HMAC/fakes de
    Phase113HubspotEnrichmentTest. Dois métodos:

    (1) test_nenhuma_chamada_hubspot_real_no_processamento_do_webhook — chamar
    Http::preventStrayRequests() ANTES do Http::fake (que mocka SOMENTE hosts
    api.hubapi.com); disparar o webhook de um deal fechado-ganho; assertar
    status 200 (prova que nenhuma StrayRequestException foi lançada) e usar
    Http::assertSent() garantindo que TODA requisição enviada tem host
    api.hubapi.com (str_contains(url, 'api.hubapi.com')). Rodar também o comando
    de replay hubspot:reprocess-event sob a mesma guarda para cobrir o caminho
    de replay.

    (2) test_tokens_e_segredos_nunca_aparecem_no_log_ecf_webhooks — injetar um
    Monolog\Handler\TestHandler no logger do canal 'ecf-webhooks'
    (Log::channel('ecf-webhooks')->getLogger()->pushHandler($handler)); disparar
    o webhook com Http::fake; percorrer $handler->getRecords() e, para cada
    registro, serializar mensagem+contexto (json_encode) e assertar que NÃO
    contém a substring do access_token ('token-fake') nem do client_secret HMAC
    (a SECRET de teste). Garantir que ao menos 1 registro foi emitido (senão o
    assert é vácuo) — assertNotEmpty($handler->getRecords()).

    Usar apenas dados sintéticos nas fixtures (sem PII real). Comentários em
    pt-BR.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=Phase115HubspotInvariantesTest` sai com código 0
    - O arquivo existe e contém os métodos test_nenhuma_chamada_hubspot_real_no_processamento_do_webhook e test_tokens_e_segredos_nunca_aparecem_no_log_ecf_webhooks
    - Grep confirma `preventStrayRequests` e `TestHandler` presentes no arquivo
    - O teste de log emite ao menos 1 registro e a asserção de ausência de token não é vácua (assertNotEmpty nos records)
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=Phase115HubspotInvariantesTest</automated>
  </verify>
  <done>Os dois invariantes transversais da milestone (zero rede real HubSpot + zero token em log) são executáveis e verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller HubSpot → canal de log ecf-webhooks | Logs de processamento não podem conter Bearer token nem o segredo HMAC |
| suíte de teste → rede externa | Nenhuma requisição pode escapar para api.hubapi.com real (preventStrayRequests) |
| fixtures → repositório | Payloads sintéticos, sem PII real de clientes nem credenciais reais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-02-01 | Information Disclosure | Log::channel('ecf-webhooks') no fluxo do webhook/replay | mitigate | Tarefa 3: TestHandler captura registros e assere ausência do access_token e do client_secret |
| T-115-02-02 | Spoofing/Tampering | chamadas HTTP reais a partir da suíte | mitigate | Tarefa 3: Http::preventStrayRequests + assert de host api.hubapi.com em toda requisição |
| T-115-02-03 | Information Disclosure | fixtures de deal/company/contato | mitigate | Dados sintéticos (ex.: replay@cliente.com, 12312312000112); nenhuma PII real commitada |
| T-115-02-SC | Tampering | dependências npm/pip/cargo | accept | Fase de teste/doc — nenhum pacote novo; Monolog já é dependência transitiva do Laravel |
</threat_model>

<verification>
- As 3 suítes rodam verdes individualmente.
- Gate amplo: `php artisan test --filter=Hubspot` verde (a suíte nova aparece no filtro por conter "Hubspot" no nome).
</verification>

<success_criteria>
- HUB-TEST-04 rastreado + efeito prático da pendência comprovado.
- HUB-TEST-05 (listagem) rastreado.
- HUB-TEST-05 (transversais) executável: guarda anti-rede-real + token-fora-do-log verdes.
- Zero regressão em `--filter=Hubspot`.
</success_criteria>

<output>
Create `.planning/phases/115-suite-e2e-documenta-o-da-regra-de-valor-v20-0/115-02-SUMMARY.md` when done
</output>
