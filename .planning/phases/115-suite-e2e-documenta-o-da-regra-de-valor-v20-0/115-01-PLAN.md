---
phase: 115-suite-e2e-documenta-o-da-regra-de-valor-v20-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Unit/HubspotValueResolverTest.php
  - tests/Feature/Phase113HubspotEnrichmentTest.php
  - tests/Feature/Phase113HubspotDedupTest.php
autonomous: true
requirements: [HUB-TEST-01, HUB-TEST-02, HUB-TEST-03]

must_haves:
  truths:
    - "Os 6 casos-âncora da regra de valor (monthly, annually P1Y, MRR×ARR, serviço único, inferência por tolerância, valor_revisar) têm um método de teste nomeado que os prova, e a suíte roda verde"
    - "O enriquecimento prova contato email+telefone escolhido, mobilephone como fallback, nome_contato estruturado, IDs HubSpot gravados e snapshot com deal/company/contact/line_items"
    - "O dedup prova enriquecer por CNPJ sem duplicar, contrato novo por hubspot_company_id sem duplicar, e match fraco por nome gerando warning sem merge agressivo"
    - "Toda chamada HubSpot nas 3 suítes usa Http::fake — nenhuma requisição de rede real"
  artifacts:
    - path: "tests/Unit/HubspotValueResolverTest.php"
      provides: "Cobertura dos 6 casos da regra de valor + docblock de rastreabilidade SC→método"
      contains: "test_infere\\|test_tolerancia\\|test_servico_unica"
    - path: "tests/Feature/Phase113HubspotEnrichmentTest.php"
      provides: "Cobertura do enriquecimento de contato/empresa + snapshot"
      contains: "test_contato_principal_escolhido"
    - path: "tests/Feature/Phase113HubspotDedupTest.php"
      provides: "Cobertura do dedup forte/fraco"
      contains: "test_match_forte_por_cnpj_enriquece_sem_duplicar"
  key_links:
    - from: "tests/Unit/HubspotValueResolverTest.php"
      to: "app/Services/Hubspot/HubspotValueResolver.php"
      via: "resolve(Servico, lineItem, dealProps)"
      pattern: "->resolve\\("
    - from: "tests/Feature/Phase113HubspotEnrichmentTest.php"
      to: "app/Http/Controllers/Api/HubspotWebhookController.php"
      via: "webhook POST /api/webhooks/hubspot + Http::fake"
      pattern: "Http::fake"
---

<objective>
Auditar e fechar o gate de regressão das 3 suítes nucleares da milestone v20.0
que já foram escritas durante as Fases 112–113: a regra de valor mensal×anual
(`HubspotValueResolverTest`), o enriquecimento de contato/empresa
(`Phase113HubspotEnrichmentTest`) e o dedup de empresa
(`Phase113HubspotDedupTest`).

**Esta NÃO é criação de testes do zero.** Os arquivos já existem e cobrem os
critérios de aceite. O trabalho é: (1) confrontar cada bullet dos Success
Criteria do ROADMAP contra o método de teste que o prova, (2) rodar cada suíte
e garantir verde, (3) adicionar asserts/casos faltantes SOMENTE onde a redação
do critério não estiver literalmente comprovada, e (4) gravar um docblock curto
de rastreabilidade (SC do ROADMAP → método) no topo de cada arquivo para
auditoria futura. NÃO criar classes de teste duplicadas.

Auditoria de cobertura desta fatia:
- HUB-TEST-01 (resolver) → JÁ COBERTO por 8 métodos (ver Tarefa 1).
- HUB-TEST-02 (enriquecimento) → JÁ COBERTO por 3 métodos (ver Tarefa 2).
- HUB-TEST-03 (dedup) → JÁ COBERTO por métodos forte/fraco (ver Tarefa 3).

Purpose: transformar cobertura existente em cobertura RASTREÁVEL e blindada por
gate verde, sem inflar a suíte.
Output: 3 arquivos de teste auditados (docblock de rastreabilidade + eventuais
asserts de reforço), todos verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-hubspotvalueresolver-extra-o-do-handoff-service-v20-0-n-cleo/112-CONTEXT.md
@.planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Auditar HubspotValueResolverTest (HUB-TEST-01)</name>
  <files>tests/Unit/HubspotValueResolverTest.php</files>
  <read_first>
    - tests/Unit/HubspotValueResolverTest.php (estado real — 8 métodos existentes)
    - app/Services/Hubspot/HubspotValueResolver.php (a regra sob teste: shape de 8 chaves, ramos monthly/annually/mrr/único/tolerância)
    - .planning/phases/112-.../112-CONTEXT.md (bloco <specifics>: os 6 casos-âncora)
  </read_first>
  <action>
    Confrontar os 6 casos do ROADMAP contra os métodos existentes e confirmar o
    mapeamento: caso 1 monthly → test_line_item_mensal_monthly_usa_price_vezes_quantity_high;
    caso 2 annually P1Y → test_line_item_mensal_annually_36000_p1y_vira_3000;
    caso 3 MRR×ARR → test_line_item_mensal_hs_mrr_prevalece_sobre_arr;
    caso 4 serviço único → test_servico_unica_amount_36000_nao_divide_por_12;
    caso 5 inferência por tolerância → test_tolerancia_dentro_de_5pct_aceita_com_medium
    + test_tolerancia_fora_de_5pct_cai_conservador_low;
    caso 6 valor_revisar → test_sem_line_item_valor_padrao_incompativel_marca_valor_revisar.
    Adicionar no topo da classe um docblock curto de rastreabilidade
    "SC ROADMAP Fase 115 nº1 → método" (uma linha por caso). NÃO reescrever
    testes existentes nem alterar asserts que já passam. Se algum dos 6 casos
    não tiver método que o prove (não é o caso hoje), adicionar o método
    faltante seguindo o padrão puro já usado (new Servico([...]) em memória, sem
    RefreshDatabase).
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=HubspotValueResolverTest` sai com código 0
    - O arquivo contém os métodos: test_servico_unica_amount_36000_nao_divide_por_12, test_tolerancia_dentro_de_5pct_aceita_com_medium, test_tolerancia_fora_de_5pct_cai_conservador_low, test_sem_line_item_valor_padrao_incompativel_marca_valor_revisar
    - O topo da classe contém um docblock mapeando os 6 casos do ROADMAP aos métodos
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=HubspotValueResolverTest</automated>
  </verify>
  <done>Os 6 casos-âncora estão rastreados a métodos nomeados e a suíte roda verde.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Auditar Phase113HubspotEnrichmentTest (HUB-TEST-02)</name>
  <files>tests/Feature/Phase113HubspotEnrichmentTest.php</files>
  <read_first>
    - tests/Feature/Phase113HubspotEnrichmentTest.php (estado real — mock de 3 contatos, snapshot)
    - app/Services/Hubspot/HubspotDealHandoffService.php + app/Services/Hubspot/HubspotHandoffData.php (company_data/contact_data no DTO)
    - app/Services/Hubspot/HubspotContactSelector.php (tiers de escolha do contato principal)
    - app/Http/Controllers/Api/HubspotWebhookController.php (gravação de nome_contato/telefone/hubspot_* e snapshot)
  </read_first>
  <action>
    Confirmar que os 5 pontos do SC2 estão provados: contato email+telefone
    escolhido (assert nome_contato='Ana Costa' + hubspot_contact_id='302'),
    mobilephone como fallback de telefone (assert telefone='11977776666' quando a
    company vem sem phone), nome_contato estruturado, IDs HubSpot gravados
    (hubspot_deal_id/hubspot_company_id/hubspot_domain) e snapshot completo
    (assertArrayHasKey deal/company/contacts/primary_contact_id/line_items/
    warnings/captured_at + assertCount(3, contacts)). Adicionar docblock de
    rastreabilidade SC2→método no topo. Confirmar que TODOS os fetches usam
    Http::fake (mockaHubSpotComTresContatos). Não duplicar cenários; só reforçar
    assert se algum dos 5 pontos não estiver literalmente coberto.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=Phase113HubspotEnrichmentTest` sai com código 0
    - O arquivo contém test_contato_principal_escolhido_entre_3_e_campos_estruturados_gravados e test_snapshot_completo_contem_todos_os_contatos_e_metadados
    - Grep confirma Http::fake presente e ausência de qualquer chamada Http sem fake (nenhum Http::get/post fora de fake)
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=Phase113HubspotEnrichmentTest</automated>
  </verify>
  <done>Os 5 pontos do SC2 estão rastreados e a suíte roda verde com Http::fake em 100%.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Auditar Phase113HubspotDedupTest (HUB-TEST-03)</name>
  <files>tests/Feature/Phase113HubspotDedupTest.php</files>
  <read_first>
    - tests/Feature/Phase113HubspotDedupTest.php (estado real — Tarefa 1 unit-style do matcher + Tarefa 3 E2E)
    - app/Services/Hubspot/HubspotCompanyMatcher.php (precedência hubspot_company_id > cnpj > email > domain > nome)
    - app/Services/Hubspot/HubspotNameNormalizer.php (dedup fraco anti-falso-positivo)
    - app/Http/Controllers/Api/HubspotWebhookController.php (persistirContratos guard hubspot_line_item_id; enriquecerEmpresaExistente)
  </read_first>
  <action>
    Confirmar que os 3 pontos do SC3 estão provados: (a) match forte por CNPJ
    enriquece SEM duplicar e preserva campo manual → test_match_forte_por_cnpj_
    enriquece_sem_duplicar (assert Company::count()===1 + campo manual 'dor'
    intocado + campos vazios enriquecidos); (b) contrato novo por
    hubspot_company_id sem duplicar contrato existente → test_match_forte_por_
    hubspot_company_id_guard_contrato_line_item_inedito (assert só o line item
    inédito 21002 vira contrato, 21001 permanece 1x); (c) match fraco por nome
    cria empresa nova + grava warning possivel_duplicidade sem mesclar a
    candidata → test_match_fraco_por_nome_cria_empresa_nova_e_grava_warning.
    Adicionar docblock de rastreabilidade SC3→método. Confirmar Http::fake em
    todos os cenários E2E. Só reforçar assert se algum ponto não estiver coberto.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=Phase113HubspotDedupTest` sai com código 0
    - O arquivo contém test_match_forte_por_cnpj_enriquece_sem_duplicar, test_match_forte_por_hubspot_company_id_guard_contrato_line_item_inedito e test_match_fraco_por_nome_cria_empresa_nova_e_grava_warning
    - Grep confirma Http::fake em todos os cenários que tocam webhook
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=Phase113HubspotDedupTest</automated>
  </verify>
  <done>Os 3 pontos do SC3 estão rastreados e a suíte roda verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixtures de teste → repositório | Payloads de deal/company/contato commitados não podem conter PII real de clientes nem segredos |
| config de teste → asserts | access_token/client_secret são valores fake ('token-fake', SECRET de teste); nunca credenciais reais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01-01 | Information Disclosure | fixtures Http::fake nas 3 suítes | mitigate | Auditar que os payloads usam nomes/emails/CNPJs sintéticos (ex.: clientemulti.com, 77788899000111); nenhum dado real de cliente |
| T-115-01-02 | Information Disclosure | config('services.hubspot.*') nos testes | accept | Já são valores fake declarados no setUp; risco nulo (a redação em log é coberta no plano 115-02) |
| T-115-01-SC | Tampering | dependências npm/pip/cargo | accept | Fase de teste/doc — nenhum pacote novo instalado |
</threat_model>

<verification>
- As 3 suítes rodam verdes: `php artisan test --filter=HubspotValueResolverTest`, `--filter=Phase113HubspotEnrichmentTest`, `--filter=Phase113HubspotDedupTest`.
- Gate de não-regressão amplo: `php artisan test --filter=Hubspot` continua verde (nenhuma suíte HubSpot quebrada pela edição de docblocks/asserts).
</verification>

<success_criteria>
- HUB-TEST-01/02/03 têm rastreabilidade explícita SC→método e gate verde.
- Nenhuma chamada HubSpot real nas 3 suítes (Http::fake em 100%).
- Zero regressão em `--filter=Hubspot`.
</success_criteria>

<output>
Create `.planning/phases/115-suite-e2e-documenta-o-da-regra-de-valor-v20-0/115-01-SUMMARY.md` when done
</output>
