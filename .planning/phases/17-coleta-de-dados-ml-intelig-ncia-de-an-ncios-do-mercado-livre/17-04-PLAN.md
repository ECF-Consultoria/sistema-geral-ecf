---
phase: 17-coleta-de-dados-ml
plan: 04
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - app/Support/Permissions.php
  - app/Http/Controllers/MlbController.php
  - routes/web.php
  - tests/Feature/Phase17ColetaTest.php
autonomous: true
requirements: [D-06, D-07]
must_haves:
  truths:
    - "Permission mlb.coleta existe no catálogo Permissions sob grupo Publicações (MLB)"
    - "GET /mlb/coleta lista coletas (visíveis a quem tem checkPubAccess; admin vê tudo)"
    - "POST /mlb/coleta valida keyword (required|max:255), cria MlbColeta status=pendente, dispara MlbColetaJob e redireciona"
    - "GET /mlb/coleta/{id} renderiza a coleta selecionada (Inertia Mlb/Coleta)"
    - "GET /mlb/coleta/{id}/status retorna JSON com chave status (rodando>10min vira erro/timeout)"
    - "Usuário sem publication_role recebe 403 em /mlb/coleta"
  artifacts:
    - path: "app/Support/Permissions.php"
      provides: "Nova permission key mlb.coleta"
      contains: "MLB_COLETA"
    - path: "app/Http/Controllers/MlbController.php"
      provides: "Actions coletaIndex/coletaStore/coletaShow/coletaStatus"
      contains: "public function coletaStore"
    - path: "routes/web.php"
      provides: "Rotas nomeadas mlb.coleta.index|store|show|status"
      contains: "coleta.store"
    - path: "tests/Feature/Phase17ColetaTest.php"
      provides: "Cobertura D-06 (store+status) e D-07 (403)"
      contains: "test_acesso_403_sem_pub_role"
  key_links:
    - from: "app/Http/Controllers/MlbController.php"
      to: "app/Jobs/MlbColetaJob.php"
      via: "MlbColetaJob::dispatch no coletaStore"
      pattern: "MlbColetaJob::dispatch"
    - from: "routes/web.php"
      to: "app/Http/Controllers/MlbController.php"
      via: "rotas mlb.coleta.* apontam para as actions"
      pattern: "MlbController::class, 'coleta"
---

<objective>
Expor a coleta via HTTP no módulo MLB: registrar a permission `mlb.coleta`, adicionar as 4
actions ao `MlbController` (`coletaIndex`, `coletaStore`, `coletaShow`, `coletaStatus`),
nomear as rotas `mlb.coleta.*` dentro do grupo MLB existente, e cobrir D-06 (store + status
JSON) e D-07 (403 sem permissão) com a suíte Feature.

Reutiliza o `checkPubAccess()` já existente no controller (gating por `publication_role`) —
NÃO cria middleware novo. Depende do model + Job (Plan 17-02). Roda em paralelo com o Plan
17-03 (sem overlap de arquivos: este toca controller/rotas/permissions; o 17-03 toca o service).

Purpose: Dar entrada (formulário/dispatch), saída (relatório) e polling (status JSON) à
feature, com acesso restrito ao módulo Publicação.
Output: edições em Permissions/MlbController/routes + `Phase17ColetaTest.php`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-RESEARCH.md
@.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md

<interfaces>
<!-- Identificadores existentes confirmados no codebase — usar diretamente. -->

config/services.php: services.mercadolivre.client_id / client_secret (já existem).

app/Support/Permissions.php:
  Bloco de constantes MLB termina em `public const MLB_METAS = 'mlb.metas';` (linha 56).
  Catálogo grupo 'Publicações (MLB)' última entrada MLB_METAS (linha 129).

routes/web.php:
  Grupo: Route::middleware(['auth','verified'])->prefix('mlb')->name('mlb.')->group(...) na linha 240.

app/Http/Controllers/MlbController.php:
  private function checkPubAccess(?string $permission = null): void  (linha 32) — usar SEM modificar;
  já importa Inertia\Inertia, Inertia\Response, App\Models\* .

App\Models\MlbColeta (Plan 17-02): create([...]), STATUS_PENDENTE, started_at (datetime cast).
App\Jobs\MlbColetaJob (Plan 17-02): MlbColetaJob::dispatch(int $coletaId).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Permission mlb.coleta + actions no MlbController + rotas mlb.coleta.*</name>
  <files>app/Support/Permissions.php, app/Http/Controllers/MlbController.php, routes/web.php</files>
  <read_first>
    - app/Support/Permissions.php — bloco de constantes MLB_* (linhas 46-56) e entradas do catálogo 'Publicações (MLB)' (linhas 119-129) — padrão exato a replicar
    - app/Http/Controllers/MlbController.php — checkPubAccess() (linha 32) + uma action de listagem (historico/vendas) e uma de store com dispatch, para espelhar Inertia::render e validação inline
    - routes/web.php — grupo mlb (linha 240) e bloco de rotas nomeadas de Metas/Implementação (padrão de nomeação ->name('coleta.*'))
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md (seções Permissions, MlbController novas actions, routes — assinaturas e validação exatas)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-UI-SPEC.md (Copywriting + nome da página Inertia 'Mlb/Coleta')
  </read_first>
  <action>
    Em `app/Support/Permissions.php`: adicionar a constante `public const MLB_COLETA = 'mlb.coleta';` logo após `MLB_METAS` (linha 56), e a entrada de catálogo `['key' => self::MLB_COLETA, 'label' => 'Pub · Int. Anúncios', 'description' => 'Coleta e mineração de keywords de concorrentes MLB']` no grupo 'Publicações (MLB)' após a entrada de MLB_METAS (linha 129).
    Em `app/Http/Controllers/MlbController.php`: adicionar `use App\Models\MlbColeta;` e `use App\Jobs\MlbColetaJob;` aos imports. Adicionar 4 actions, todas chamando `$this->checkPubAccess('coleta')` no topo (gating pela nova key):
      • `coletaIndex(Request $request): Response` — lista `MlbColeta::latest()->get()` mapeada (id, keyword, categoria_id, status, created_at formatado d/m/Y H:i, duração) e renderiza `Inertia::render('Mlb/Coleta', [...])` com `coletas` e `coleta=null`. Visibilidade colaborativa: todos com checkPubAccess veem todas as coletas; admin idem (RESEARCH Q2 — Information Disclosure resolvido como compartilhado dentro do módulo).
      • `coletaStore(Request $request): RedirectResponse` — `$request->validate(['keyword'=>'required|string|max:255','categoria_id'=>'nullable|string|max:50','faixa_preco'=>'nullable|string|max:20','condicao'=>'nullable|in:new,used'])`; cria `MlbColeta::create([... 'user_id'=>auth()->id(), 'status'=>MlbColeta::STATUS_PENDENTE])`; `MlbColetaJob::dispatch($coleta->id)`; `return redirect()->route('mlb.coleta.show', $coleta->id)`.
      • `coletaShow(Request $request, int $id): Response` — carrega a coleta, mapeia o relatório (keyword, status, resultado, erro_mensagem) e renderiza `Inertia::render('Mlb/Coleta', [...])` com a lista `coletas` + a `coleta` selecionada.
      • `coletaStatus(int $id): JsonResponse` — `MlbColeta::findOrFail($id)`; se `status==='rodando'` e `started_at < now()->subMinutes(10)` retorna status 'erro' (timeout — RESEARCH §Pitfall 5); senão retorna `response()->json(['status'=>$coleta->status])`.
    Em `routes/web.php`: dentro do grupo `prefix('mlb')->name('mlb.')` (após o bloco de Metas), adicionar as 4 rotas nomeadas: GET `/coleta`→coletaIndex `->name('coleta.index')`; POST `/coleta`→coletaStore `->name('coleta.store')`; GET `/coleta/{id}`→coletaShow `->name('coleta.show')`; GET `/coleta/{id}/status`→coletaStatus `->name('coleta.status')`.
    Comentários em pt-BR. NÃO modificar `checkPubAccess()`.
  </action>
  <verify>
    <automated>php artisan route:list --name=mlb.coleta</automated>
  </verify>
  <acceptance_criteria>
    - `app/Support/Permissions.php` contém `MLB_COLETA = 'mlb.coleta'` e a entrada de catálogo com label 'Pub · Int. Anúncios'
    - `app/Http/Controllers/MlbController.php` contém `public function coletaStore`, `coletaIndex`, `coletaShow`, `coletaStatus`, cada uma chamando `checkPubAccess('coleta')`, e `MlbColetaJob::dispatch(`
    - `coletaStore` contém `'keyword' => 'required|string|max:255'` (V5 validação de entrada)
    - `php artisan route:list --name=mlb.coleta` lista 4 rotas: index, store, show, status
  </acceptance_criteria>
  <done>Permission, actions e rotas mlb.coleta.* registradas e visíveis em route:list.</done>
</task>

<task type="auto">
  <name>Task 2: Phase17ColetaTest (Feature) — store cria pendente, status JSON, 403 sem permissão</name>
  <files>tests/Feature/Phase17ColetaTest.php</files>
  <read_first>
    - tests/Feature/Phase14MlbControllerFiltroTest.php — analog de Feature test do módulo MLB (helper criarAdmin, actingAs, RefreshDatabase)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md (seção Phase17ColetaTest — os 3 testes com asserts exatos)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-RESEARCH.md (§Validation Architecture D-06/D-07 + §Domínio de Segurança controle de acesso)
    - app/Http/Controllers/MlbController.php — actions criadas na Task 1 (rotas exercitadas)
  </read_first>
  <action>
    Criar `tests/Feature/Phase17ColetaTest.php` no namespace `Tests\Feature`, `use RefreshDatabase`, `@group phase17`, com helper `criarAdmin()` retornando `User::factory()->create(['role'=>'admin'])`. Três testes:
      • `test_store_cria_coleta_pendente`: `Queue::fake()`; `actingAs(admin)->post('/mlb/coleta', ['keyword'=>'fone bluetooth'])`; `assertRedirect()`; `assertDatabaseHas('mlb_coletas', ['keyword'=>'fone bluetooth','status'=>'pendente'])`; opcional `Queue::assertPushed(MlbColetaJob::class)`.
      • `test_status_endpoint_json`: cria MlbColeta status='rodando'; `actingAs(admin)->getJson("/mlb/coleta/{id}/status")` → `assertOk()->assertJsonFragment(['status'=>'rodando'])`.
      • `test_acesso_403_sem_pub_role`: `User::factory()->create(['role'=>'consultor'])` (sem acesso MLB); `actingAs(user)->get('/mlb/coleta')` → `assertForbidden()`.
    Comentários em pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter Phase17ColetaTest</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/Phase17ColetaTest.php` contém os 3 métodos `test_store_cria_coleta_pendente`, `test_status_endpoint_json`, `test_acesso_403_sem_pub_role` e `@group phase17`
    - `php artisan test --filter Phase17ColetaTest` exits 0 (3/3 GREEN)
    - `php artisan test --filter Phase17` (suíte completa da fase) permanece verde
  </acceptance_criteria>
  <done>Suíte Feature verde: store/status/403 cobertos (D-06, D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (usuário autenticado) → MlbController | input keyword/filtros cruza aqui — não confiável |
| usuário sem publication_role → rotas /mlb/coleta | tentativa de acesso não autorizado |
| usuário → histórico de coletas de outros | leitura de dados de terceiros do mesmo módulo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Tampering | coletaStore input | mitigate | `$request->validate(['keyword'=>'required|string|max:255', 'condicao'=>'nullable|in:new,used', ...])` na entrada da action. |
| T-17-12 | Elevation of Privilege | acesso às rotas mlb.coleta.* | mitigate | Toda action chama `checkPubAccess('coleta')`; usuário sem `publication_role`/`mlb.coleta` recebe 403 (coberto por test_acesso_403_sem_pub_role). |
| T-17-13 | Information Disclosure | histórico de coletas | accept | Visibilidade compartilhada dentro do módulo Publicação (decisão RESEARCH Q2 — colaborativo é mais útil; admin vê tudo). Nenhum dado pessoal de comprador é persistido (garantido no Plan 17-03). |
| T-17-SC | Tampering | npm/composer installs | accept | Nenhum pacote externo novo nesta fase (RESEARCH). Sem superfície de supply-chain. |
</threat_model>

<verification>
- `php artisan route:list --name=mlb.coleta` lista as 4 rotas
- `php artisan test --filter Phase17ColetaTest` exits 0 (3/3)
- `php artisan test --filter Phase17` permanece verde
</verification>

<success_criteria>
- Entrada validada e coleta criada pendente com dispatch do Job (D-06)
- Endpoint de status JSON pronto para o polling do frontend (D-06)
- Acesso restrito por publication_role via checkPubAccess (D-07)
- Permission mlb.coleta atribuível pelo catálogo de setores
</success_criteria>

<output>
Create `.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-04-SUMMARY.md` when done
</output>
