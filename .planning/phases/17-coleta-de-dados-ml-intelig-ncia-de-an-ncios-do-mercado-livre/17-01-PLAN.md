---
phase: 17-coleta-de-dados-ml
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/MlKeywordMinerService.php
  - tests/Unit/MlKeywordMinerTest.php
autonomous: true
requirements: [D-04, D-05]
must_haves:
  truths:
    - "normalizarToken('Fone Estéreo') retorna 'fone estereo' (lowercase + sem acentos)"
    - "Stopwords pt-BR são removidas dos tokens"
    - "rankingKeywords gera unigramas/bigramas/trigramas ranqueados por frequência"
    - "rankingKeywords marca eh_tendencia=true para termos presentes em /trends"
    - "recomendacaoHeuristica monta título sugerido e palavras-top a partir do ranking (sem IA)"
  artifacts:
    - path: "app/Services/MlKeywordMinerService.php"
      provides: "Mineração estatística pt-BR em PHP puro + recomendação heurística"
      contains: "class MlKeywordMinerService"
      min_lines: 120
    - path: "tests/Unit/MlKeywordMinerTest.php"
      provides: "Cobertura unitária D-04-a/b/c"
      contains: "class MlKeywordMinerTest"
  key_links:
    - from: "tests/Unit/MlKeywordMinerTest.php"
      to: "app/Services/MlKeywordMinerService.php"
      via: "new MlKeywordMinerService() no setUp"
      pattern: "new MlKeywordMinerService"
---

<objective>
Implementar o `MlKeywordMinerService` — a unidade de mineração estatística pura (PHP, sem
dependência externa, sem DB, sem IA) que tokeniza títulos pt-BR, remove stopwords, conta
n-gramas e produz o ranking de keywords + a recomendação heurística (D-04, D-05).

Esta é a única unidade da fase totalmente isolável e testável por TDD — não depende de banco,
HTTP nem do Job. Constrói-se primeiro porque o `MlColetaService` (Plan 17-03) consome estes
métodos diretamente.

Purpose: Entregar a inteligência estatística da coleta (ranking + heurística) de forma
determinística e coberta por testes, sem nenhuma camada de IA (IA = Fase 2, diferida).
Output: `MlKeywordMinerService.php` + `MlKeywordMinerTest.php` (suíte verde).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-RESEARCH.md
@.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md
@.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar MlKeywordMinerTest (RED) cobrindo D-04-a/b/c</name>
  <files>tests/Unit/MlKeywordMinerTest.php</files>
  <read_first>
    - tests/Unit/CobrancaCalculatorTest.php — analog de teste unitário PHP puro (sem RefreshDatabase, instancia o service no setUp)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md (seção `tests/Unit/MlKeywordMinerTest.php` — estrutura base e asserts exatos)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-VALIDATION.md (Mapa de Verificação — comandos por teste)
  </read_first>
  <behavior>
    - test_normaliza_token: normalizarToken('Fone Estéreo') === 'fone estereo'
    - test_stopwords_filtradas: tokenizar('fone de ouvido bluetooth esportivo') NÃO contém 'de', contém 'fone'
    - test_ranking_keywords: rankingKeywords(['Fone Bluetooth Esportivo','Fone Bluetooth Sem Fio','Fone Bluetooth 5.0']) → ranking[0]['termo']==='bluetooth' e ranking[0]['frequencia']===3
    - test_recomendacao_heuristica: recomendacaoHeuristica(ranking, [], 'fone bluetooth') retorna array com chaves titulo_sugerido, palavras_top_incluir, aviso — e aviso contém 'heurística'
  </behavior>
  <action>
    Criar `tests/Unit/MlKeywordMinerTest.php` no namespace `Tests\Unit`, estendendo `Tests\TestCase`, com `@group phase17`. Instanciar `$this->miner = new MlKeywordMinerService()` no `setUp()` (sem RefreshDatabase — service é PHP puro). Escrever os 4 métodos de teste descritos em `<behavior>` com os asserts exatos do analog em 17-PATTERNS.md. Estes testes começam RED (a classe MlKeywordMinerService ainda não existe). Comentários em pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter MlKeywordMinerTest</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Unit/MlKeywordMinerTest.php` contém `class MlKeywordMinerTest` e `@group phase17`
    - Os 4 métodos `test_normaliza_token`, `test_stopwords_filtradas`, `test_ranking_keywords`, `test_recomendacao_heuristica` existem
    - `php artisan test --filter MlKeywordMinerTest` falha em RED (erro de classe inexistente) ANTES da Task 2 — confirmando que os testes exercitam código real
  </acceptance_criteria>
  <done>Suíte MlKeywordMinerTest existe com 4 testes em estado RED (service ainda não implementado).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar MlKeywordMinerService (GREEN) — normalização, n-gramas, ranking, heurística</name>
  <files>app/Services/MlKeywordMinerService.php</files>
  <read_first>
    - app/Support/CobrancaCalculator.php — analog de service stateless PHP puro (sem DI Laravel, métodos estáticos/de instância simples)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-RESEARCH.md (§Padrão 3 mapa de acentos completo + §Exemplos lista ~120 stopwords pt-BR + §Pitfall 4/6 normalização)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md (seção `MlKeywordMinerService` — assinaturas exatas de normalizarToken/tokenizar/ngrams/rankingKeywords)
    - tests/Unit/MlKeywordMinerTest.php — o contrato que a implementação deve satisfazer (criado na Task 1)
  </read_first>
  <action>
    Criar `app/Services/MlKeywordMinerService.php` no namespace `App\Services`, sem imports Laravel (PHP puro). Definir as constantes privadas `ACCENT_MAP` (mapa completo de acentos pt-BR da §Padrão 3 do RESEARCH) e `STOPWORDS_PT` (~120 palavras da §Exemplos do RESEARCH, incluindo stopwords de domínio e-commerce). Implementar métodos públicos: `normalizarToken(string): string` (mb_strtolower UTF-8 + strtr ACCENT_MAP); `tokenizar(string): array` (preg_replace removendo pontuação `[^\p{L}\p{N}\s]`, preg_split por espaço, filtro mb_strlen>2 e not in STOPWORDS_PT); `ngrams(array,int): array`; `rankingKeywords(array $titulos, array $trends = []): array` (merge unigramas+bigramas+trigramas, array_count_values + arsort, top 30, cada item com termo/frequencia/eh_tendencia/tipo). Implementar `agruparPerguntas(array $perguntas): array` (tokeniza textos das perguntas → frequência de termos → top temas com exemplo). Implementar `recomendacaoHeuristica(array $ranking, array $topDuvidas, string $keyword): array` baseada SOMENTE em regras (D-05): titulo_sugerido = combinação das top keywords + keyword, palavras_top_incluir = top unigramas, pontos_fortes_antecipar = temas das top dúvidas, e `aviso` com o texto de disclaimer "Recomendação heurística (regras) — análise qualitativa por IA disponível na Fase 2". NÃO usar nenhuma chamada a IA/Anthropic (Deferred Idea — proibido nesta fase). Comentários em pt-BR; docblock de classe explicando ausência de intl/Normalizer.
  </action>
  <verify>
    <automated>php artisan test --filter MlKeywordMinerTest</automated>
  </verify>
  <acceptance_criteria>
    - `app/Services/MlKeywordMinerService.php` contém `class MlKeywordMinerService`, `private const ACCENT_MAP`, `private const STOPWORDS_PT`
    - Métodos públicos presentes: `normalizarToken`, `tokenizar`, `ngrams`, `rankingKeywords`, `agruparPerguntas`, `recomendacaoHeuristica`
    - `grep -v '^//' app/Services/MlKeywordMinerService.php` NÃO contém referência a Anthropic/Claude/IA (sem camada de IA — D-05/Deferred)
    - `php artisan test --filter MlKeywordMinerTest` exits 0 (4/4 verdes — GREEN)
  </acceptance_criteria>
  <done>MlKeywordMinerService implementado; suíte MlKeywordMinerTest 4/4 verde; nenhuma dependência de IA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| título de produto ML → tokenizador | texto externo (vindo da API ML) entra no minerador; é dado, nunca código executável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | MlKeywordMinerService::tokenizar | accept | Entrada é tratada apenas como string para contagem de frequência; sem eval/include/regex dinâmica. `preg_replace` usa padrão fixo `[^\p{L}\p{N}\s]`. Sem superfície de injeção. |
| T-17-02 | Information Disclosure | recomendacaoHeuristica | accept | Service não toca DB, log nem token; opera só sobre arrays em memória. Nenhum dado pessoal processado (perguntas chegam só como texto no Plan 17-03). |
</threat_model>

<verification>
- `php artisan test --filter MlKeywordMinerTest` exits 0 (4/4)
- `php artisan test --filter Phase17` inclui esta suíte e permanece verde
</verification>

<success_criteria>
- MlKeywordMinerService normaliza pt-BR sem intl/Normalizer (strtr + mb_strtolower)
- Ranking de unigramas/bigramas/trigramas determinístico e ordenado por frequência
- Recomendação 100% heurística (sem IA) com aviso explícito de Fase 1
- Suíte unitária verde (D-04-a/b/c cobertos)
</success_criteria>

<output>
Create `.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-01-SUMMARY.md` when done
</output>
