---
phase: 17-coleta-de-dados-ml
plan: 05
type: execute
wave: 3
depends_on: ["17-04"]
files_modified:
  - resources/js/Pages/Mlb/Coleta.jsx
  - resources/js/Layouts/AppLayout.jsx
autonomous: false
requirements: [D-06, D-07]
must_haves:
  truths:
    - "Página Mlb/Coleta.jsx renderiza formulário (keyword + categoria + condição) e botão Iniciar Coleta"
    - "Submit via useForm POST mlb.coleta.store; erros de validação aparecem abaixo do campo"
    - "Polling 3s via fetch(route('mlb.coleta.status', id)); ao concluir faz router.reload({only:['coleta']})"
    - "Estados pendente/rodando/concluido/erro têm badge com as cores semânticas do design system"
    - "Relatório mostra Ranking de Keywords, Top Dúvidas e Recomendação Heurística com aviso de Fase 1"
    - "Quando questions_disponivel=false, exibe aviso de fallback sem quebrar a página"
    - "Item de nav 'Int. Anúncios' aparece no AppLayout gated por permission mlb.coleta"
  artifacts:
    - path: "resources/js/Pages/Mlb/Coleta.jsx"
      provides: "Página de coleta: formulário + progresso + histórico + relatório"
      contains: "mlb.coleta.status"
      min_lines: 180
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "Item de navegação Int. Anúncios"
      contains: "mlb.coleta.index"
  key_links:
    - from: "resources/js/Pages/Mlb/Coleta.jsx"
      to: "route('mlb.coleta.status')"
      via: "fetch dentro do setInterval de polling"
      pattern: "mlb.coleta.status"
    - from: "resources/js/Pages/Mlb/Coleta.jsx"
      to: "route('mlb.coleta.store')"
      via: "form.post no submit"
      pattern: "mlb.coleta.store"
    - from: "resources/js/Layouts/AppLayout.jsx"
      to: "route('mlb.coleta.index')"
      via: "nav item routeName"
      pattern: "mlb.coleta.index"
---

<objective>
Construir a página `Mlb/Coleta.jsx` (formulário + barra de progresso/polling + histórico +
relatório de resultado) e o item de navegação 'Int. Anúncios' no `AppLayout`, seguindo o
contrato visual aprovado no 17-UI-SPEC (design `ecf-*`, dark, `card-ecf`, `cn()`, primitivos
shadcn/Radix). Sem state global — só `useState` + `useForm` (D-07).

Depende das rotas/controller do Plan 17-04 (o polling consome `mlb.coleta.status`; o submit
consome `mlb.coleta.store`).

Purpose: Dar ao usuário do módulo Publicação a interface completa — pesquisar keyword,
acompanhar o progresso assíncrono e ler o relatório heurístico — no tema dark consistente.
Output: `Coleta.jsx` + edição de `AppLayout.jsx` + build verde + checkpoint visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-UI-SPEC.md
@.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md

<interfaces>
<!-- Rotas Ziggy expostas pelo Plan 17-04 — usar via route() helper. -->
route('mlb.coleta.index')             // GET listagem (página)
route('mlb.coleta.store')             // POST { keyword, categoria_id?, faixa_preco?, condicao? }
route('mlb.coleta.show', id)          // GET relatório de uma coleta
route('mlb.coleta.status', id)        // GET JSON { status }

<!-- Props Inertia entregues por coletaIndex/coletaShow (Plan 17-04): -->
coletas: Array<{ id, keyword, categoria_id, status, created_at, duracao }>
coleta:  null | { id, keyword, status, resultado, erro_mensagem }
// resultado segue a estrutura de 17-RESEARCH.md §Estrutura JSON:
//   meta{ keyword, categoria_nome, questions_disponivel, total_produtos_analisados, ... }
//   ranking_keywords[]{ termo, frequencia, eh_tendencia, tipo }
//   top_duvidas[]{ tema, frequencia, exemplo }
//   recomendacao{ titulo_sugerido, palavras_top_incluir[], pontos_fortes_antecipar[], aviso }
//   produtos_analisados[]{ item_id, titulo, sold_quantity, posicao_ranking }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Item de nav 'Int. Anúncios' no AppLayout</name>
  <files>resources/js/Layouts/AppLayout.jsx</files>
  <read_first>
    - resources/js/Layouts/AppLayout.jsx — bloco NAV_ITEMS de Publicações MLB (linhas ~43-54) e o import de ícones de lucide-react (linhas ~6-10) — padrão exato do objeto nav item
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-UI-SPEC.md (§Navegação — Item de Menu)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md (seção AppLayout — forma do objeto e import de Search)
  </read_first>
  <action>
    Em `resources/js/Layouts/AppLayout.jsx`: adicionar `Search` ao import de `lucide-react` (se ainda não importado). Inserir, no bloco de itens de Publicações MLB, logo após o item 'Metas', o objeto: `{ label: 'Int. Anúncios', routeName: 'mlb.coleta.index', page: 'Mlb/Coleta', icon: Search, permission: 'mlb.coleta' }`. Manter a forma exata dos demais itens MLB (gating por permission). Comentário pt-BR.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `resources/js/Layouts/AppLayout.jsx` contém `routeName: 'mlb.coleta.index'` e `permission: 'mlb.coleta'`
    - `Search` consta no import de lucide-react
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Nav item 'Int. Anúncios' presente e gated por mlb.coleta; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Página Mlb/Coleta.jsx — formulário + polling + histórico + relatório</name>
  <files>resources/js/Pages/Mlb/Coleta.jsx</files>
  <read_first>
    - resources/js/Pages/Grants/Index.jsx — analog EXATO de polling (setInterval + fetch + router.reload({only})) e useForm de submit
    - resources/js/Pages/Mlb/Historico.jsx — analog de layout de página MLB (cabeçalho h1+subtítulo, card-ecf, select nativo)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-UI-SPEC.md (CONTRATO COMPLETO — composição de componentes [1]-[5], tipografia 4 tamanhos/2 pesos, cores, copywriting exato, acessibilidade)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md (seção Coleta.jsx — imports, polling, useForm, card-ecf, badges de status)
    - resources/js/Components/ui/badge.jsx, progress.jsx, table.jsx — primitivos shadcn já presentes
    - resources/js/lib/utils.js — helper cn()
  </read_first>
  <action>
    Criar `resources/js/Pages/Mlb/Coleta.jsx` exportando um componente default que recebe props `coletas` e `coleta`. Importar `AppLayout`, `useForm`/`router` de @inertiajs/react, `useState`/`useEffect`/`useRef`, ícones lucide (`RefreshCw`, `Search`, `MessageSquare`, `Info`, `Lightbulb`, `CheckCircle2`), `cn` de @/lib/utils e os primitivos `Badge`/`Progress`/`Table` de @/Components/ui.
    Montar as 5 regiões do 17-UI-SPEC dentro de `<AppLayout title="Inteligência de Anúncios">` + `<div className="space-y-5 max-w-[1200px]">`:
      [1] Cabeçalho: h1 `font-display font-bold text-2xl` "Inteligência de Anúncios" + subtítulo `text-white/40 text-[13px]`.
      [2] Formulário em `card-ecf rounded-2xl p-5`: campo keyword (input `h-9 ... focus:border-ecf-yellow/40` com label uppercase 11px), selects opcionais categoria/condição (Qualquer/Novo/Usado), botão `h-10 px-6 rounded-xl bg-ecf-yellow text-black font-bold text-[13px]` "Iniciar Coleta"/"Coletando…". Submit via `form.post(route('mlb.coleta.store'))`. Erros de validação `text-red-400 text-[13px]` abaixo do campo. Usar `aria-disabled` + `<label htmlFor>` (acessibilidade UI-SPEC).
      [3] Barra de progresso (condicional `status pendente|rodando`): bloco `card-ecf border-ecf-yellow/20` com `<RefreshCw className="animate-spin" aria-hidden>` + texto "Coleta em andamento — analisando {keyword}…" + `<Progress className="h-2 [&>div]:bg-ecf-yellow">`. Timeout 10min mostra a copy de timeout do UI-SPEC.
      [4] Histórico em `card-ecf rounded-2xl` com `<Table>` (colunas Keyword|Categoria|Status|Duração|Data|Ação); badge de status por linha com as 4 cores semânticas; linha selecionada `bg-ecf-yellow/5 border-l-2 border-ecf-yellow`; estado vazio com a copy "Nenhuma coleta realizada ainda…".
      [5] Relatório da `coleta` selecionada em 3 cards: 5a Ranking de Keywords (posição mono + termo + badge "Tendência" amarelo quando `eh_tendencia` + frequência; top 1 em `text-ecf-yellow font-bold`); 5b Top Dúvidas (ícone MessageSquare azul; se `meta.questions_disponivel===false` exibir o bloco de aviso azul com a copy de fallback); 5c Recomendação Heurística (aviso amarelo proeminente com `Lightbulb` e a copy "Recomendação gerada por regras (heurística). Análise qualitativa por IA estará disponível na Fase 2." + titulo_sugerido mono + chips de palavras_top_incluir + pontos_fortes_antecipar com `CheckCircle2`). `sold_quantity` null → "N/D".
    Polling: replicar o padrão de `Grants/Index.jsx` — `useRef` + `setInterval(3000)` chamando `fetch(route('mlb.coleta.status', coleta.id))`, ao receber concluido faz `router.reload({ only: ['coleta'] })`, ao receber erro mostra mensagem, deadline 10min; `clearInterval` no cleanup do `useEffect`; iniciar polling apenas se `coleta?.status` for pendente/rodando. Toda copy EXATA do §Contrato de Copywriting do UI-SPEC. APENAS tamanhos 11/13/20/24px e pesos normal/bold (sem font-semibold/medium). Comentários em pt-BR.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `resources/js/Pages/Mlb/Coleta.jsx` contém `route('mlb.coleta.store')`, `route('mlb.coleta.status'` e `router.reload({ only: ['coleta'] })`
    - Contém a copy de disclaimer heurístico "Análise qualitativa por IA estará disponível na Fase 2." e o tratamento `questions_disponivel`
    - `grep -E 'font-semibold|font-medium|font-extrabold' resources/js/Pages/Mlb/Coleta.jsx` retorna zero matches (tipografia travada do UI-SPEC)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Coleta.jsx completa conforme UI-SPEC, com polling e relatório; build verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual humana da página de coleta</name>
  <what-built>
    Página `/mlb/coleta` (Inteligência de Anúncios) com formulário de keyword, barra de
    progresso com polling 3s, histórico de coletas e relatório (ranking de keywords, top
    dúvidas, recomendação heurística) no design ecf-* dark. Item de nav 'Int. Anúncios'.
    Backend (Plans 17-01..17-04) já entregue e com suíte Phase17 verde.
  </what-built>
  <how-to-verify>
    1. Garantir queue worker rodando (`php artisan queue:work`) e `npm run build` aplicado.
    2. Logar como usuário admin/Publicação e abrir `/mlb/coleta` pelo item de menu 'Int. Anúncios'.
    3. Submeter a keyword "fone bluetooth esportivo" e confirmar: redireciona, mostra status
       "Aguardando na fila…"/"Coleta em andamento…" e a barra amarela com spinner; o polling
       atualiza sozinho a cada ~3s sem reload manual.
    4. Ao concluir, conferir o relatório: Ranking de Keywords (top 1 em amarelo, badges
       "Tendência"), Top Dúvidas (ou o aviso azul de fallback se `questions_disponivel=false`),
       e Recomendação Heurística com o aviso amarelo de Fase 1.
    5. Conferir tema dark consistente (sem cores fora da paleta), tipografia (sem
       font-semibold/medium) e que `sold_quantity` oculto aparece como "N/D".
    6. (Premissa A1/A2 — RESEARCH §Verificações Manuais) No 1º run real, conferir no
       `resultado` se `questions_disponivel=true`; se 401/403, validar que o fallback entrega
       o ranking sem quebrar a página.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas visuais/funcionais encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → mlb.coleta.status (polling) | leitura repetida de status; autenticada |
| resultado (texto de terceiros) → render React | conteúdo externo renderizado como texto (React escapa por padrão) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-14 | Information Disclosure | render do resultado | mitigate | React escapa texto por padrão; NÃO usar `dangerouslySetInnerHTML` para títulos/perguntas vindos da API ML. |
| T-17-15 | Elevation of Privilege | item de nav / página | mitigate | Nav item gated por `permission: 'mlb.coleta'`; as rotas exigem `checkPubAccess('coleta')` no backend (Plan 17-04) — UI gating é defesa em profundidade, não a única barreira. |
| T-17-16 | Denial of Service | polling infinito | mitigate | Deadline de 10min no setInterval + `clearInterval` no cleanup do useEffect; polling para ao concluido/erro. |
</threat_model>

<verification>
- `npm run build` exits 0
- `php artisan test --filter Phase17` permanece verde (sem regressão de backend)
- Checkpoint humano aprovado
</verification>

<success_criteria>
- Página de coleta funcional com polling sem WebSockets (D-06)
- Relatório heurístico com aviso explícito de Fase 1 (D-05)
- Fallback visual quando questions indisponível (D-02)
- Design ecf-* consistente, sem state global (D-07)
- Nav item gated por mlb.coleta (D-07)
</success_criteria>

<output>
Create `.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-05-SUMMARY.md` when done
</output>
