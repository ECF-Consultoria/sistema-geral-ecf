---
phase: 17-coleta-de-dados-ml
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_01_120000_create_mlb_coletas_table.php
  - app/Models/MlbColeta.php
  - app/Jobs/MlbColetaJob.php
  - tests/Unit/MlbColetaJobTest.php
autonomous: true
requirements: [D-06]
must_haves:
  truths:
    - "Tabela mlb_coletas existe com colunas keyword, status (enum pendente/rodando/concluido/erro), resultado (json), started_at, finished_at, erro_mensagem"
    - "MlbColeta model expõe constantes STATUS_* e casts resultado=>array, started_at/finished_at=>datetime"
    - "MlbColetaJob.handle() marca status=rodando, chama o pipeline e grava status=concluido + resultado"
    - "MlbColetaJob.failed() grava status=erro e erro_mensagem (Falha definitiva)"
    - "Logging usa tag [MLB Coleta] com id da coleta; nunca loga o app token"
  artifacts:
    - path: "database/migrations/2026_06_01_120000_create_mlb_coletas_table.php"
      provides: "Schema da tabela mlb_coletas"
      contains: "Schema::create('mlb_coletas'"
    - path: "app/Models/MlbColeta.php"
      provides: "Model Eloquent da coleta"
      contains: "class MlbColeta extends Model"
    - path: "app/Jobs/MlbColetaJob.php"
      provides: "Job assíncrono que orquestra o pipeline e rastreia status"
      contains: "class MlbColetaJob implements ShouldQueue"
    - path: "tests/Unit/MlbColetaJobTest.php"
      provides: "Cobertura do failed() hook (D-06)"
      contains: "test_failed_marca_erro"
  key_links:
    - from: "app/Jobs/MlbColetaJob.php"
      to: "app/Services/MlColetaService.php"
      via: "executarPipeline injetado no handle()"
      pattern: "executarPipeline"
    - from: "app/Jobs/MlbColetaJob.php"
      to: "app/Models/MlbColeta.php"
      via: "MlbColeta::findOrFail no handle()"
      pattern: "MlbColeta::findOrFail"
---

<objective>
Construir a fundação de dados e o orquestrador assíncrono da coleta: a migration + model
`MlbColeta`, e o `MlbColetaJob` (queue `database`) com rastreamento de status pendente →
rodando → concluido/erro e o hook `failed()` (D-06).

O Job é a casca de orquestração — ele injeta `MlColetaService` e `MlKeywordMinerService` e
chama `$service->executarPipeline($coleta, $miner)`. A implementação do pipeline vem no Plan
17-03 (interface-first: o contrato `executarPipeline(MlbColeta, MlKeywordMinerService): array`
é definido aqui e consumido lá). Este plano NÃO depende do 17-01 nem do 17-03 para existir
como casca testável (o teste cobre apenas o `failed()` hook, que não invoca o service).

Purpose: Entregar a persistência (histórico/reuso) e o ciclo de vida assíncrono com status
visível, sem rodar nada no ciclo HTTP.
Output: migration + `MlbColeta.php` + `MlbColetaJob.php` + `MlbColetaJobTest.php`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-RESEARCH.md
@.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md

<interfaces>
<!-- Contrato que o Plan 17-03 (MlColetaService) DEVE implementar — definido aqui (interface-first). -->
<!-- O executor do 17-03 implementa este método; o Job o consome sem explorar o codebase. -->

MlColetaService (Plan 17-03) expõe:
  public function executarPipeline(\App\Models\MlbColeta $coleta, \App\Services\MlKeywordMinerService $miner): array
  // Retorna o array `resultado` (estrutura JSON documentada em 17-RESEARCH.md §Estrutura JSON),
  // com chaves: meta, ranking_keywords, tendencias, top_duvidas, recomendacao, produtos_analisados.

MlKeywordMinerService (Plan 17-01) expõe:
  public function rankingKeywords(array $titulos, array $trends = []): array
  public function agruparPerguntas(array $perguntas): array
  public function recomendacaoHeuristica(array $ranking, array $topDuvidas, string $keyword): array
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration create_mlb_coletas_table + Model MlbColeta</name>
  <files>database/migrations/2026_06_01_120000_create_mlb_coletas_table.php, app/Models/MlbColeta.php</files>
  <read_first>
    - database/migrations/2026_05_22_100001_create_mlb_sync_vendas_logs_table.php — analog exato de migration (status enum, timestamps de ciclo de vida, índices)
    - app/Models/MlbSyncVendasLog.php — analog exato de model (constantes STATUS_*, $fillable, $casts)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md (seções migration e MlbColeta — colunas e casts exatos)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-RESEARCH.md (§Migration sugerida + §Estrutura JSON do campo resultado)
  </read_first>
  <action>
    Criar a migration `database/migrations/2026_06_01_120000_create_mlb_coletas_table.php` que cria a tabela `mlb_coletas` com: `id`; `user_id` foreignId nullable constrained('users') nullOnDelete; colunas de entrada `keyword` (string), `categoria_id` (string nullable), `faixa_preco` (string nullable), `condicao` (string nullable); ciclo de vida `status` enum `['pendente','rodando','concluido','erro']` default 'pendente'; `erro_mensagem` (text nullable); `resultado` (json nullable); `started_at`/`finished_at` (timestamp nullable); `timestamps()`; índices em `keyword`, `status`, `created_at`. `down()` faz `Schema::dropIfExists('mlb_coletas')`.
    Criar o model `app/Models/MlbColeta.php` no namespace `App\Models` estendendo `Model`, com constantes `STATUS_PENDENTE='pendente'`, `STATUS_RODANDO='rodando'`, `STATUS_CONCLUIDO='concluido'`, `STATUS_ERRO='erro'`; `$fillable` = [user_id, keyword, categoria_id, faixa_preco, condicao, status, erro_mensagem, resultado, started_at, finished_at]; `$casts` = [resultado=>'array', started_at=>'datetime', finished_at=>'datetime']. Comentários em pt-BR. Rodar `php artisan migrate` no host (CLI PHP não disponível no agente — incluir como nota; o executor roda no XAMPP).
  </action>
  <verify>
    <automated>php artisan migrate --pretend | grep -q mlb_coletas; php artisan test --filter MlbColetaJobTest</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo de migration contém `Schema::create('mlb_coletas'` e o enum `['pendente', 'rodando', 'concluido', 'erro']`
    - `app/Models/MlbColeta.php` contém `class MlbColeta extends Model`, as 4 constantes `STATUS_*`, e o cast `'resultado' => 'array'`
    - Após `php artisan migrate` no host, a tabela `mlb_coletas` existe (verificável via `php artisan migrate:status`)
  </acceptance_criteria>
  <done>Tabela mlb_coletas migrada e model MlbColeta com constantes + casts disponíveis.</done>
</task>

<task type="auto">
  <name>Task 2: MlbColetaJob (orquestração + status + failed) + MlbColetaJobTest (D-06)</name>
  <files>app/Jobs/MlbColetaJob.php, tests/Unit/MlbColetaJobTest.php</files>
  <read_first>
    - app/Jobs/SyncMlCompanyJob.php — analog exato do Job (tries, timeout, backoff(), failed() que grava log de erro)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md (seção MlbColetaJob — estrutura completa handle()/failed() + imports)
    - app/Models/MlbColeta.php — model criado na Task 1 (constantes de status que o Job grava)
    - .planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-PATTERNS.md (seção MlbColetaJobTest — assert exato do test_failed_marca_erro)
  </read_first>
  <action>
    Criar `app/Jobs/MlbColetaJob.php` no namespace `App\Jobs`, implementando `ShouldQueue` com os traits Dispatchable/InteractsWithQueue/Queueable/SerializesModels. Propriedades: `tries=2`, `timeout=300`. Construtor `__construct(public readonly int $coletaId)`. Método `backoff(): array` retornando `[60, 300]`. Método `handle(MlColetaService $service, MlKeywordMinerService $miner): void` que: faz `MlbColeta::findOrFail($this->coletaId)`, grava `status=rodando` + `started_at=now()`, e em try/catch chama `$service->executarPipeline($coleta, $miner)` (contrato definido no `<interfaces>` deste plano — implementação no Plan 17-03), gravando `status=concluido` + `resultado` + `finished_at` no sucesso; no catch `\Throwable` grava `status=erro` + `erro_mensagem` + `finished_at`, loga `Log::error("[MLB Coleta] Erro coleta {id}: ...")` e re-lança. Método `failed(\Throwable $e): void` loga `[MLB Coleta] Falha definitiva` e atualiza a coleta para `status=erro` + `erro_mensagem='Falha definitiva: '...`. NUNCA logar o app token (Threat T-17-ID). Comentários em pt-BR.
    Criar `tests/Unit/MlbColetaJobTest.php` no namespace `Tests\Unit`, com `use RefreshDatabase` e `@group phase17`, contendo `test_failed_marca_erro`: cria uma MlbColeta com status='rodando', instancia `new MlbColetaJob($coleta->id)`, chama `->failed(new \RuntimeException('Erro simulado'))` e assertDatabaseHas('mlb_coletas', ['id'=>$coleta->id, 'status'=>'erro']).
  </action>
  <verify>
    <automated>php artisan test --filter MlbColetaJobTest</automated>
  </verify>
  <acceptance_criteria>
    - `app/Jobs/MlbColetaJob.php` contém `class MlbColetaJob implements ShouldQueue`, `public int $timeout = 300`, `public function backoff()` e `public function failed(\Throwable $e)`
    - `handle()` chama `$service->executarPipeline(` (consome o contrato do Plan 17-03)
    - `grep -i 'token' app/Jobs/MlbColetaJob.php` NÃO retorna nenhuma linha de `Log::` com token (token nunca logado)
    - `tests/Unit/MlbColetaJobTest.php` contém `test_failed_marca_erro` e `php artisan test --filter MlbColetaJobTest` exits 0 (GREEN — failed() não invoca o service, então não depende do Plan 17-03)
  </acceptance_criteria>
  <done>MlbColetaJob completo com ciclo de status + failed() hook; teste de failed() verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job worker → DB mlb_coletas | escreve status/resultado/erro; conteúdo do resultado vem da API ML (já validado no pipeline do 17-03) |
| Job worker → log do servidor | mensagens de log podem vazar segredos se mal escritas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Information Disclosure | MlbColetaJob logging | mitigate | Logar apenas `coleta->id` + status + `keyword`; NUNCA logar o app token nem corpo de resposta com credenciais. Mensagens com tag `[MLB Coleta]`. |
| T-17-04 | Denial of Service | Job em status=rodando eterno (worker cai) | mitigate | `timeout=300` + `tries=2`; o endpoint de status (Plan 17-04) trata `rodando > 10min` como timeout (RESEARCH §Pitfall 5). |
| T-17-05 | Tampering | enum status no DB | mitigate | Coluna `status` é enum restrito a 4 valores no schema; valores gravados vêm das constantes STATUS_* do model, não de input do usuário. |
| T-17-SC | Tampering | npm/composer installs | accept | Nenhum pacote externo novo instalado nesta fase (RESEARCH confirma mineração em PHP puro). Sem superfície de supply-chain. |
</threat_model>

<verification>
- `php artisan migrate:status` mostra a migration mlb_coletas aplicada
- `php artisan test --filter MlbColetaJobTest` exits 0
- `php artisan test --filter Phase17` permanece verde
</verification>

<success_criteria>
- Persistência de coleta + resultado disponível para histórico/reuso (D-06)
- Job assíncrono na queue database com status pendente/rodando/concluido/erro
- failed() grava status=erro sem perder a causa
- Contrato executarPipeline definido para o Plan 17-03 consumir
</success_criteria>

<output>
Create `.planning/phases/17-coleta-de-dados-ml-intelig-ncia-de-an-ncios-do-mercado-livre/17-02-SUMMARY.md` when done
</output>
