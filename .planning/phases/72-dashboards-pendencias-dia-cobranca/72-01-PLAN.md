---
phase: 72-dashboards-pendencias-dia-cobranca
milestone: v15.0
plan: 72-01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Nps/NpsPendingService.php
  - app/Http/Controllers/NpsController.php
  - routes/web.php
  - resources/js/Pages/Nps/Configuracao.jsx
autonomous: true
requirements: [NPS-E-01, NPS-E-04]
tags: [nps, backend, service, pending, dia-cobranca, config, admin, contract-first, phase72]

must_haves:
  truths:
    - "NpsPendingService::forCarteira(User) retorna array de empresas pendentes na carteira do usuário — shape [{company_id, name, template_id, template_nome, month_reference, dias_atraso}] ordenado por name ASC"
    - "NpsPendingService::isPendente(Company, ?Carbon = null) retorna bool — true quando mês corrente >= dia_cobranca && não existe survey completed do (company, month_reference, template_id resolvido); resolveção do template usa NpsTemplateService (Phase 69-01)"
    - "NpsPendingService::diaCobranca() retorna int 1..31 lido de Configuracao::get('nps_dia_cobranca', 25) — default 25 quando ausente"
    - "Configuracao chave 'nps_dia_cobranca' aceita apenas int 1..31; guard interno normaliza (clamp) valores fora do range para evitar crash em edge cases"
    - "PATCH /nps/configuracao/dia-cobranca (admin only) valida 1..31 e persiste via Configuracao::set — retorna back()->with('success')"
    - "Configuracao.jsx (Phase 70 novo) ganha widget compacto no topo com input type=number min=1 max=31 + botão Salvar + feedback flash"
    - "Escopo de 'carteira' segue padrão do projeto: analista/estrategista veem apenas suas empresas via company_users; admin vê todas (usa flag $user->isAdmin())"
    - "Empresas sem template_id resolvível (Runtime Exception NpsTemplateService) são SILENCIOSAMENTE excluídas de forCarteira — log warning estruturado; nunca crashar a lista"
  artifacts:
    - path: app/Services/Nps/NpsPendingService.php
      provides: "Service stateless com forCarteira/isPendente/diaCobranca; usa NpsTemplateService via DI"
      min_lines: 130
    - path: app/Http/Controllers/NpsController.php
      provides: "Método atualizarDiaCobranca() (admin only via middleware role:admin) + validação"
      min_lines_delta: 25
    - path: routes/web.php
      provides: "1 rota PATCH /nps/configuracao/dia-cobranca nomeada nps.configuracao.dia-cobranca.update"
      min_lines: 3
    - path: resources/js/Pages/Nps/Configuracao.jsx
      provides: "Widget de configuração global 'Dia de cobrança mensal' no header (acima da TemplatesList) com input + Salvar"
      min_lines_delta: 45
  key_links:
    - from: app/Services/Nps/NpsPendingService.php
      to: app/Services/Nps/NpsTemplateService.php
      via: "$template = $templateService->resolveForCompany($company) dentro de isPendente/forCarteira"
      pattern: "resolveForCompany"
    - from: app/Services/Nps/NpsPendingService.php
      to: app/Models/Configuracao.php
      via: "Configuracao::get('nps_dia_cobranca', 25)"
      pattern: "Configuracao::get..'nps_dia_cobranca'"
    - from: app/Services/Nps/NpsPendingService.php
      to: app/Models/NpsSurvey.php
      via: "NpsSurvey::where(company_id, month_reference, template_id)->where('status', 'completed')->exists()"
      pattern: "'status', 'completed'"
---

<objective>
Entregar a **fundação backend da Phase 72**: `NpsPendingService` (contrato NPS-E-04) + config global "dia de cobrança mensal" persistido em `Configuracao` com CRUD admin (NPS-E-01). Cobre 100% dos 2 REQs. Serve de base para os Plans 72-02 (dashboards backend) e 72-03 (frontend badge + widget).

Purpose: Todos os consumidores da Phase 72 (badges em Portfolio/Companies, widgets em Dashboard/Performance, futura notificação NPS-FUTURE-03) precisam de UMA fonte de verdade para "esta empresa está pendente no mês corrente?". Sem este service, cada consumidor duplicaria a lógica de resolução do template + escopo de carteira + dedup — divergência é inevitável. O service centraliza a regra.

Output:
- 1 service novo `App\Services\Nps\NpsPendingService`
- 1 método novo em `NpsController` (`atualizarDiaCobranca`)
- 1 rota admin nomeada
- 1 widget no `Configuracao.jsx` (frontend v15 novo — Phase 70-05)
- Zero mudança em NpsTemplateService, NpsScoreCalculator ou controllers de templates
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/v15-nps-templates-schema.md
@.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/PHASE-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/PHASE-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-01-SUMMARY.md
@.planning/phases/70-ui-de-configuracao-admin/PHASE-SUMMARY.md
@app/Services/Nps/NpsTemplateService.php
@app/Models/Configuracao.php
@app/Models/Company.php
@app/Models/User.php
@app/Models/NpsSurvey.php
@app/Models/NpsTemplate.php
@app/Http/Controllers/NpsController.php
@resources/js/Pages/Nps/Configuracao.jsx
@routes/web.php

<interfaces>
From app/Services/Nps/NpsTemplateService.php (Phase 69-01):
- `resolveForCompany(Company $company): NpsTemplate` — usa priority DESC + is_default fallback; dispara RuntimeException se nem default existir

From app/Models/Configuracao.php:
- `static get(string $chave, $default = null): mixed` — key-value store
- `static set(string $chave, $valor)` — persiste update-or-insert

From app/Models/User.php:
- `isAdmin(): bool`
- relação `companies(): BelongsToMany` via pivot `company_users` (Phase 33/34 pattern)

From app/Models/Company.php:
- id, name (todos os controllers usam esse par)

From app/Models/NpsSurvey.php:
- fillable: company_id, template_id, month_reference (Carbon date), status
- statuses: pending, completed, expired

From app/Http/Controllers/NpsController.php:
- Padrão de método CRUD admin: recebe FormRequest ou validate inline; retorna back()->with()

From routes/web.php linhas 100-160 (grupo role:admin):
- Padrão `Route::patch/put ('/nps/...', [Controller::class, 'method'])->name('nps.configuracao.<recurso>.<acao>')`

From resources/js/Pages/Nps/Configuracao.jsx (Phase 70-05, 236 LOC):
- Header atual: `<h1>Templates de NPS</h1>` + link legado
- É o ponto natural para adicionar o widget "Dia de cobrança"
- Já usa useForm, router, cn, Button, Input do design system ecf-*
</interfaces>

<research_reference>
ROADMAP Phase 72 SCs:
- **SC#1** — Sistema tem config global "dia de cobrança mensal" (int 1-31) que dispara marcação de pendência a partir daquele dia — ESTE PLAN entrega o config + UI + método (`diaCobranca()` do service)
- **SC#4** — `NpsPendingService::forCarteira(User)` existe com contrato documentado para futura integração com notificações (NPS-FUTURE-03) — ESTE PLAN entrega o contrato

Decisões de arquitetura:
1. **Config em `Configuracao` (key-value)** e não em `.env` — admin precisa editar sem redeploy; `.env` requer VPS access
2. **Default = 25** — próximo do fim do mês (aumenta janela para responder antes da cobrança)
3. **`isPendente(Company)` como método público** — os badges em Portfolio/Show e Companies/Index precisam de check O(1) por empresa; `forCarteira` seria overkill quando só precisa checar 1 empresa
4. **Escopo de carteira** — reusa `$user->companies()->pluck('companies.id')` pattern do projeto (mesmo usado em SugadorController e outros)

Guard de degradação (empresas sem template resolvível):
- `NpsTemplateService::resolveForCompany` dispara `RuntimeException` quando nem default existe
- O seed "NPS Padrão" (Plan 68-03) garante que sempre há default — mas defesa em profundidade: envolver em try/catch e logar warning + skipar (nunca crashar `forCarteira`)
</research_reference>
</context>

<tasks>
<task id="72-01-T1">
<title>Criar NpsPendingService com forCarteira + isPendente + diaCobranca</title>

<read_first>
- app/Services/Nps/NpsTemplateService.php (padrão service stateless da Phase 69-01)
- app/Models/Configuracao.php (get/set API)
- app/Models/Company.php + User.php (relações pivot company_users)
- app/Models/NpsSurvey.php (fillable, statuses)
- .planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-01-SUMMARY.md (RuntimeException guard)
</read_first>

<action>
Criar `app/Services/Nps/NpsPendingService.php` no namespace `App\Services\Nps`.

Estrutura:

```php
<?php

namespace App\Services\Nps;

use App\Models\Company;
use App\Models\Configuracao;
use App\Models\NpsSurvey;
use App\Models\NpsTemplate;
use App\Models\User;
use Carbon\Carbon;
use Illuminate\Support\Facades\Log;

/**
 * NpsPendingService — Phase 72 v15.0.
 *
 * Fonte única de verdade para "empresa X está pendente de NPS no mês Y?".
 * Consumidores:
 *  - Badges em Portfolio/Show.jsx e Companies/Index.jsx (Plan 72-03)
 *  - Widgets em Dashboard/Admin.jsx e Performance/Dashboard.jsx (Plan 72-02)
 *  - Futura integração com sistema de notificações (NPS-FUTURE-03)
 *
 * Contrato pendência (SC#1):
 *   Empresa Y é PENDENTE no mês M quando TODAS as condições abaixo forem verdadeiras:
 *   1. Data atual >= diaCobranca() no mês M
 *   2. NpsTemplateService::resolveForCompany(Y) retorna template T sem crash
 *   3. NÃO existe NpsSurvey com (company_id=Y, template_id=T, month_reference=M, status='completed')
 *
 * Escopo de carteira:
 *   forCarteira(User $user):
 *     - Admin: todas as companies do sistema
 *     - Não-admin: apenas $user->companies() (pivot company_users)
 *
 * Config:
 *   diaCobranca() lê Configuracao::get('nps_dia_cobranca', 25) — clamp 1..31
 */
class NpsPendingService
{
    public function __construct(
        private NpsTemplateService $templateService,
    ) {
    }

    /**
     * Dia (1..31) a partir do qual as pendências são cobradas no mês corrente.
     * Lê Configuracao::get('nps_dia_cobranca', 25) e faz clamp 1..31 como
     * defesa em profundidade contra corrupção de valor no banco.
     */
    public function diaCobranca(): int
    {
        $dia = (int) Configuracao::get('nps_dia_cobranca', 25);
        return max(1, min(31, $dia));
    }

    /**
     * True quando a empresa está pendente no mês de referência (default: mês corrente).
     *
     * Overload: passar $mesReferencia=Carbon::parse('2026-06-01')->startOfMonth() para checar mês passado.
     * NpsTemplateService lança RuntimeException se nem default existir — capturamos e retornamos false
     * (não é "pendente"; é anômalo — logamos warning para monitoramento).
     */
    public function isPendente(Company $company, ?Carbon $mesReferencia = null): bool
    {
        $mes = $mesReferencia?->copy()->startOfMonth() ?? now()->startOfMonth();

        // Guard: só marca pendente se o mês corrente já passou do dia de cobrança.
        // Meses passados sempre são "elegíveis" (ignora guard temporal para uso em relatórios históricos).
        $ehMesCorrente = $mes->equalTo(now()->startOfMonth());
        if ($ehMesCorrente && now()->day < $this->diaCobranca()) {
            return false;
        }

        try {
            $template = $this->templateService->resolveForCompany($company);
        } catch (\RuntimeException $e) {
            Log::warning('[NpsPendingService] empresa sem template aplicável', [
                'company_id' => $company->id,
                'name'       => $company->name,
                'reason'     => $e->getMessage(),
            ]);
            return false;
        }

        // Existe survey completa para (company, template, mês)? Se sim, NÃO é pendente.
        $completou = NpsSurvey::where('company_id', $company->id)
            ->where('template_id', $template->id)
            ->whereDate('month_reference', $mes->toDateString())
            ->where('status', 'completed')
            ->exists();

        return ! $completou;
    }

    /**
     * Lista empresas pendentes na carteira do usuário para o mês corrente (ou informado).
     *
     * Shape do retorno (documentado para consumidores):
     *   [
     *     [
     *       'company_id'      => int,
     *       'name'            => string,
     *       'template_id'     => int,
     *       'template_nome'   => string,
     *       'month_reference' => 'YYYY-MM-DD',   // primeiro dia do mês
     *       'dias_atraso'     => int,             // now()->day - diaCobranca() no mês corrente; 0 em meses passados
     *     ],
     *     ...
     *   ]
     *
     * Ordenado por name ASC (facilita consumo por UI).
     */
    public function forCarteira(User $user, ?Carbon $mesReferencia = null): array
    {
        $mes = $mesReferencia?->copy()->startOfMonth() ?? now()->startOfMonth();

        $companies = $user->isAdmin()
            ? Company::query()->orderBy('name')->get(['id', 'name'])
            : $user->companies()->orderBy('name')->get(['companies.id', 'name']);

        $ehMesCorrente = $mes->equalTo(now()->startOfMonth());
        $diasAtraso    = $ehMesCorrente ? max(0, now()->day - $this->diaCobranca()) : 0;

        $pendentes = [];
        foreach ($companies as $company) {
            if (! $this->isPendente($company, $mes)) {
                continue;
            }
            try {
                $template = $this->templateService->resolveForCompany($company);
            } catch (\RuntimeException $e) {
                continue; // já logado em isPendente
            }
            $pendentes[] = [
                'company_id'      => $company->id,
                'name'            => $company->name,
                'template_id'     => $template->id,
                'template_nome'   => $template->nome,
                'month_reference' => $mes->toDateString(),
                'dias_atraso'     => $diasAtraso,
            ];
        }

        return $pendentes;
    }
}
```

**Nota de performance:** `forCarteira` faz N chamadas de `resolveForCompany` (cada uma com 1-2 queries). Para carteiras grandes (>200 empresas), otimizar em v15.1 com batch fetch. Para v15.0, aceitável — endpoints admin usam LIMIT 100 (padrão Plan 70-04) e widgets de dashboard mostram top N.
</action>

<acceptance_criteria>
- File `app/Services/Nps/NpsPendingService.php` exists com namespace `App\Services\Nps`
- 3 métodos públicos: `diaCobranca(): int`, `isPendente(Company, ?Carbon = null): bool`, `forCarteira(User, ?Carbon = null): array` — assertar via `grep -c "public function \\(diaCobranca\\|isPendente\\|forCarteira\\)" app/Services/Nps/NpsPendingService.php` >= 3
- Constructor DI de NpsTemplateService — assertar via `grep -n "NpsTemplateService \\$templateService" app/Services/Nps/NpsPendingService.php`
- Clamp 1..31 em diaCobranca — assertar via `grep -nE "max\\(1.*min\\(31|min\\(31.*max\\(1" app/Services/Nps/NpsPendingService.php`
- try/catch RuntimeException em isPendente e forCarteira — assertar via `grep -c "RuntimeException" app/Services/Nps/NpsPendingService.php` >= 2
- Log::warning presente — assertar via `grep -n "Log::warning" app/Services/Nps/NpsPendingService.php`
- `->where('status', 'completed')` — assertar via `grep -n "'status'.*'completed'" app/Services/Nps/NpsPendingService.php`
- `$user->isAdmin()` branching — assertar via `grep -n "isAdmin" app/Services/Nps/NpsPendingService.php`
- `php -l app/Services/Nps/NpsPendingService.php` retorna "No syntax errors detected"
</acceptance_criteria>
</task>

<task id="72-01-T2">
<title>Adicionar NpsController::atualizarDiaCobranca + rota PATCH admin</title>

<read_first>
- app/Http/Controllers/NpsController.php (padrão de método CRUD admin com validate inline)
- routes/web.php (grupo role:admin com rotas existentes)
- app/Models/Configuracao.php (set API)
</read_first>

<action>
Adicionar método em `app/Http/Controllers/NpsController.php` (perto do `atualizarConfiguracao` para agrupar por semântica):

```php
/**
 * PATCH /nps/configuracao/dia-cobranca — persiste config global "dia de cobrança
 * mensal" (Phase 72 Plan 01, NPS-E-01).
 *
 * Aceita int 1..31; qualquer valor fora do range é rejeitado com 422.
 * Persistido em Configuracao::set('nps_dia_cobranca', $valor).
 */
public function atualizarDiaCobranca(Request $request)
{
    $validated = $request->validate([
        'dia' => 'required|integer|min:1|max:31',
    ], [
        'dia.min' => 'O dia deve ser entre 1 e 31.',
        'dia.max' => 'O dia deve ser entre 1 e 31.',
    ]);

    Configuracao::set('nps_dia_cobranca', (string) $validated['dia']);

    return back()->with('success', "Dia de cobrança do NPS atualizado para {$validated['dia']}.");
}
```

Em `routes/web.php`, dentro do MESMO grupo `Route::middleware(['auth','role:admin'])` já existente das rotas de templates (Phase 70), adicionar 1 rota — logo após as rotas do Plan 70-04:

```php
// ─── Phase 72 Plan 01 — Config global NPS (dia de cobrança) ─────────
Route::patch('/nps/configuracao/dia-cobranca',
    [NpsController::class, 'atualizarDiaCobranca'])
    ->name('nps.configuracao.dia-cobranca.update');
```
</action>

<acceptance_criteria>
- Método `atualizarDiaCobranca(Request $request)` presente em NpsController.php — assertar via `grep -n "public function atualizarDiaCobranca" app/Http/Controllers/NpsController.php`
- Regra `min:1|max:31` presente — assertar via `grep -nE "min:1.*max:31|integer.*min:1.*max:31" app/Http/Controllers/NpsController.php`
- `Configuracao::set('nps_dia_cobranca'` presente — assertar via `grep -n "Configuracao::set..'nps_dia_cobranca'" app/Http/Controllers/NpsController.php`
- Rota registrada em `routes/web.php` — assertar via `grep -n "nps.configuracao.dia-cobranca.update" routes/web.php`
- Verbo PATCH — assertar via `grep -nE "Route::patch.*dia-cobranca" routes/web.php`
- `php artisan route:list --path=nps/configuracao/dia-cobranca` mostra a rota com middleware role:admin
- `php -l app/Http/Controllers/NpsController.php` verde
</acceptance_criteria>
</task>

<task id="72-01-T3">
<title>Widget "Dia de cobrança" no Configuracao.jsx (header, acima da TemplatesList)</title>

<read_first>
- resources/js/Pages/Nps/Configuracao.jsx (Phase 70-05 versão atual, 236 LOC)
- resources/js/Components/ui/input.jsx, button.jsx, label.jsx
- resources/js/lib/utils.js (cn)
- app/Http/Controllers/NpsTemplateController.php (linha 82+ props injetadas — expandir com dia_cobranca)
</read_first>

<action>
**Passo 1** — Em `app/Http/Controllers/NpsTemplateController.php::index`, adicionar prop `dia_cobranca` no array de props (linha ~82 ou onde estão tipos_pergunta, dimensoes_labels, servicos_disponiveis):

```php
return Inertia::render('Nps/Configuracao', [
    'templates'            => ...,
    'tipos_pergunta'       => NpsTemplateQuestion::TIPOS,
    'dimensoes_labels'     => NpsTemplateQuestion::dimensoesLabels(),
    'servicos_disponiveis' => $servicosDisponiveis,
    'dia_cobranca'         => (int) \App\Models\Configuracao::get('nps_dia_cobranca', 25),  // Phase 72 Plan 01
]);
```

**Passo 2** — Em `resources/js/Pages/Nps/Configuracao.jsx`, adicionar receita da prop na assinatura + widget acima do grid principal:

Alterar assinatura:
```jsx
export default function Configuracao({
    templates,
    tipos_pergunta,
    dimensoes_labels,
    servicos_disponiveis,
    dia_cobranca,   // Phase 72 Plan 01
}) {
```

Adicionar widget entre o `<header>` e o `<div className="grid...">`:

```jsx
{/* Phase 72 Plan 01 — Configuração global "dia de cobrança" */}
<DiaCobrancaWidget diaAtual={dia_cobranca} />
```

Criar componente inline no mesmo arquivo (ou em `resources/js/Components/Nps/Config/DiaCobrancaWidget.jsx` para consistência com os outros — decidir na hora conforme padrão do projeto; se optar inline, adicionar antes do `export default function Configuracao`):

```jsx
function DiaCobrancaWidget({ diaAtual }) {
    const { data, setData, patch, processing, errors, wasSuccessful } = useForm({
        dia: diaAtual ?? 25,
    });

    const submit = (e) => {
        e.preventDefault();
        patch(route('nps.configuracao.dia-cobranca.update'), { preserveScroll: true });
    };

    return (
        <div className="bg-ecf-card border border-white/[0.08] rounded-2xl p-4 mb-6 flex items-center gap-4">
            <div className="flex-1">
                <div className="text-white text-sm font-medium">Dia de cobrança mensal</div>
                <div className="text-white/50 text-xs">
                    A partir deste dia do mês, empresas sem resposta são marcadas como pendentes.
                </div>
            </div>
            <form onSubmit={submit} className="flex items-center gap-2">
                <input
                    type="number"
                    min={1}
                    max={31}
                    value={data.dia}
                    onChange={(e) => setData('dia', parseInt(e.target.value, 10) || 1)}
                    className="w-20 bg-white/[0.03] border border-white/[0.08] rounded-lg px-3 py-2 text-white text-sm"
                />
                <Button
                    type="submit"
                    disabled={processing}
                    className="bg-ecf-yellow text-[#050507] hover:bg-ecf-yellow/90 text-sm px-4"
                >
                    {processing ? 'Salvando...' : 'Salvar'}
                </Button>
            </form>
            {errors.dia && <span className="text-red-400 text-xs ml-2">{errors.dia}</span>}
        </div>
    );
}
```

Import necessário se não presente: `import { useForm } from '@inertiajs/react';` (já usado no arquivo).
</action>

<acceptance_criteria>
- NpsTemplateController::index injeta prop `dia_cobranca` — assertar via `grep -n "'dia_cobranca'" app/Http/Controllers/NpsTemplateController.php`
- Configuracao.jsx recebe `dia_cobranca` na assinatura — assertar via `grep -n "dia_cobranca" resources/js/Pages/Nps/Configuracao.jsx`
- Componente `DiaCobrancaWidget` presente (inline ou import) — assertar via `grep -n "DiaCobrancaWidget" resources/js/Pages/Nps/Configuracao.jsx`
- Chama `route('nps.configuracao.dia-cobranca.update')` — assertar via `grep -n "nps.configuracao.dia-cobranca.update" resources/js/Pages/Nps/Configuracao.jsx`
- Input type=number min=1 max=31 — assertar via `grep -nE 'type=.number.*min=.1.*max=.31|min=.1..*max=.31' resources/js/Pages/Nps/Configuracao.jsx`
</acceptance_criteria>
</task>

<task id="72-01-T4">
<title>Build + smoke test</title>

<read_first>
- app/Services/Nps/NpsPendingService.php (T1)
- app/Http/Controllers/NpsController.php (T2)
- resources/js/Pages/Nps/Configuracao.jsx (T3)
</read_first>

<action>
1. `/c/xampp/php/php.exe -l app/Services/Nps/NpsPendingService.php` — sem erros
2. `/c/xampp/php/php.exe -l app/Http/Controllers/NpsController.php` — sem erros
3. `/c/xampp/php/php.exe -l app/Http/Controllers/NpsTemplateController.php` — sem erros
4. `/c/xampp/php/php.exe artisan route:list --path=nps/configuracao/dia-cobranca` — 1 rota PATCH com middleware role:admin
5. `npm run build` — verde; manifest inclui Configuracao asset atualizado
6. Sanity DI via tinker: `/c/xampp/php/php.exe artisan tinker --execute='dd(app(App\Services\Nps\NpsPendingService::class))'` — resolve sem erro
7. Sanity contract: `/c/xampp/php/php.exe artisan tinker --execute='echo app(App\Services\Nps\NpsPendingService::class)->diaCobranca();'` — retorna int (25 se não persistido)
8. Rodar suite baseline: `/c/xampp/php/php.exe artisan test tests/Feature/Phase31 tests/Feature/Phase33 tests/Feature/Phase68 tests/Feature/Phase69 tests/Feature/Phase70 tests/Feature/Phase71 --stop-on-failure 2>&1 | tail -5` — deve continuar 118 verdes
</action>

<acceptance_criteria>
- `php -l` verde em todos os arquivos PHP modificados
- `npm run build` verde
- Rota `nps.configuracao.dia-cobranca.update` listada em `route:list`
- DI de NpsPendingService funciona (tinker resolve sem erro)
- diaCobranca() retorna int válido
- Baseline suite 118 verde (zero regressão nas Phases 31/33/68/69/70/71)
</acceptance_criteria>
</task>
</tasks>

<verification>
1. NpsPendingService entrega 3 métodos com contrato documentado no docblock
2. diaCobranca() aplica clamp 1..31 (defesa em profundidade)
3. isPendente() é O(1) por empresa; forCarteira() é O(N) mas limitado por escopo de carteira
4. RuntimeException handled — nunca crasha lista de pendentes
5. Config admin CRUD funciona via UI
6. Zero mudança em NpsTemplateService, NpsScoreCalculator, controllers de templates
</verification>

<must_haves_summary>
- REQ NPS-E-01 100% coberto (config admin + validação 1..31 + persistência Configuracao)
- REQ NPS-E-04 100% coberto (NpsPendingService::forCarteira com contrato documentado)
- Fundação para Plans 72-02 (dashboards backend) e 72-03 (frontend) — sem duplicação de regra de negócio
</must_haves_summary>
