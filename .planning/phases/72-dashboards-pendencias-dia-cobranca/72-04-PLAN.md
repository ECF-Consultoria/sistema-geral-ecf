---
phase: 72-dashboards-pendencias-dia-cobranca
milestone: v15.0
plan: 72-04
type: execute
wave: 4
depends_on: [72-01, 72-02, 72-03]
files_modified:
  - tests/Feature/Phase72/NpsPendingServiceTest.php
  - tests/Feature/Phase72/NpsDiaCobrancaConfigTest.php
  - tests/Feature/Phase72/DashboardPendencyPropsTest.php
autonomous: true
requirements: [NPS-E-01, NPS-E-02, NPS-E-03, NPS-E-04, NPS-E-05]
tags: [nps, tests, feature, phpunit, phase72, sc-verification, pending-service, dia-cobranca, dashboards]

must_haves:
  truths:
    - "Suite Feature Phase72 cobre SC1-SC5 do ROADMAP: (1) config dia_cobranca persist + clamp, (2) forCarteira escopo admin vs consultor, (3) isPendente respeita dia_cobranca no mês corrente vs meses passados, (4) dashboards injetam nps_pendentes com shape completo, (5) CompanyController::show usa NpsScoreCalculator para surveys v15"
    - "Cada teste roda contra SQLite in-memory (RefreshDatabase)"
    - "Zero regressão em Phases 31/33/68/69/70/71 — rodar suite completa ao final e confirmar delta = 0"
    - "Guard temporal do isPendente testado: mês corrente + hoje<dia_cobranca → false; hoje>=dia_cobranca → true quando sem survey completo; meses passados → true sem guard temporal"
    - "Guard RuntimeException do NpsTemplateService testado — empresa sem template resolvível não crasha lista"
  artifacts:
    - path: tests/Feature/Phase72/NpsPendingServiceTest.php
      provides: "8 testes: diaCobranca (default + clamp 0/32), isPendente (todos os branches temporais), forCarteira (admin vs consultor escopo)"
      min_lines: 260
    - path: tests/Feature/Phase72/NpsDiaCobrancaConfigTest.php
      provides: "4 testes: PATCH admin valida 1..31 + persiste + non-admin 403 + valor fora range 422"
      min_lines: 130
    - path: tests/Feature/Phase72/DashboardPendencyPropsTest.php
      provides: "5 testes: adminDashboard tem nps_pendentes; userDashboard filtra por carteira; PortfolioController::show injeta pendentes; CompanyController::show usa NpsScoreCalculator para v15; suite baseline 118 continua verde"
      min_lines: 200
  key_links:
    - from: tests/Feature/Phase72/NpsPendingServiceTest.php
      to: app/Services/Nps/NpsPendingService.php
      via: "$this->app->make(NpsPendingService::class)->forCarteira($user)"
      pattern: "NpsPendingService"
    - from: tests/Feature/Phase72/DashboardPendencyPropsTest.php
      to: app/Http/Controllers/DashboardController.php
      via: "$response->assertInertia(fn (Assert $p) => $p->component('Dashboard/Admin')->has('nps_pendentes'))"
      pattern: "assertInertia.*Dashboard"
---

<objective>
Entregar cobertura Feature completa para os 5 SC do ROADMAP Phase 72 — 3 arquivos de teste totalizando 17 testes cobrindo:
- Contrato do `NpsPendingService` (Plan 72-01) — diaCobranca + isPendente + forCarteira
- Config admin do dia de cobrança (CRUD via PATCH)
- Prop injection nos 3 controllers (Dashboard admin/user, Portfolio, CompanyController::show)
- Uso do `NpsScoreCalculator` em CompanyController para surveys v15
- Baseline regressão zero em Phases 31/33/68/69/70/71

Purpose: Sem Feature tests, o backend/frontend Phase 72 fica frágil frente à Phase 73 (que vai limpar código legado). Esta suite trava contrato:
1. `NpsPendingService` semântica (guard temporal, escopo carteira, RuntimeException handling)
2. Config admin comportamento (validação, autorização)
3. Prop injection (contratos entre backend e frontend Plan 72-03)
4. Dual-path CompanyController preservado

Output:
- 3 arquivos em `tests/Feature/Phase72/`
- Cobertura de 100% dos SC do ROADMAP Phase 72
- Zero regressão em suites anteriores
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/PHASE-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/PHASE-SUMMARY.md
@.planning/phases/70-ui-de-configuracao-admin/PHASE-SUMMARY.md
@.planning/phases/71-formul-rio-p-blico-din-mico/PHASE-SUMMARY.md
@.planning/phases/72-dashboards-pendencias-dia-cobranca/72-01-PLAN.md
@.planning/phases/72-dashboards-pendencias-dia-cobranca/72-02-PLAN.md
@.planning/phases/72-dashboards-pendencias-dia-cobranca/72-03-PLAN.md
@app/Services/Nps/NpsPendingService.php
@app/Services/Nps/NpsTemplateService.php
@app/Services/Nps/NpsScoreCalculator.php
@app/Http/Controllers/NpsController.php
@app/Http/Controllers/DashboardController.php
@app/Http/Controllers/CompanyController.php
@app/Http/Controllers/PortfolioController.php
@app/Models/Configuracao.php
@app/Models/User.php
@app/Models/Company.php
@app/Models/NpsSurvey.php
@app/Models/NpsResponse.php
@app/Models/NpsResponseAnswer.php
@database/factories/UserFactory.php
@database/factories/CompanyFactory.php
@database/factories/NpsTemplateFactory.php
@database/factories/NpsSurveyFactory.php
@database/factories/NpsResponseFactory.php
@database/factories/NpsResponseAnswerFactory.php
@tests/Feature/Phase71/NpsRespondRenderTest.php
@tests/Feature/Phase71/NpsRespondSubmitFlowTest.php
@tests/TestCase.php

<interfaces>
Padrão de teste do projeto (herdar de Phase 71):
- `use RefreshDatabase;`
- `Carbon::setTestNow()` para controlar "now" (crítico para guard temporal do isPendente)
- Route helpers via Ziggy
- AssertableInertia para checagem de props

From tests/Feature/Phase71/NpsRespondRenderTest.php:
- Padrão helper `fixtureTemplateWithQuestions()` — reutilizável
- Padrão helper `fixtureSurveyV15(NpsTemplate)` — adaptar para injetar status='pending' + month_reference

From app/Services/Nps/NpsPendingService.php (Plan 72-01):
- Contrato completo dos 3 métodos públicos + shape do array retornado por forCarteira
</interfaces>

<research_reference>
Mapeamento SC → testes:
- **SC#1** (config dia_cobranca 1..31) → NpsDiaCobrancaConfigTest (4 testes)
- **SC#2** (badge pendência em Portfolio/Companies) → NpsPendingServiceTest::forCarteira + DashboardPendencyPropsTest — indireto: contract do service + prop injetada; JSX não testado em Feature
- **SC#3** (widget dashboards) → DashboardPendencyPropsTest (3 testes: adminDashboard + userDashboard + Performance)
- **SC#4** (NpsPendingService::forCarteira contrato) → NpsPendingServiceTest completo
- **SC#5** (dashboards usam NpsScoreCalculator) → DashboardPendencyPropsTest::CompanyController_show_calcula_via_calculator

Guard temporal — testes críticos:
1. `Carbon::setTestNow('2026-07-15')` + dia_cobranca=25 → hoje<dia → isPendente=false
2. `Carbon::setTestNow('2026-07-25')` + dia_cobranca=25 → hoje>=dia → isPendente=true (sem completed)
3. `Carbon::setTestNow('2026-07-25')` + survey completed no mês → isPendente=false
4. `Carbon::setTestNow('2026-07-05')` + mês passado (junho) → isPendente sem guard temporal (relatório histórico)

Escopo carteira — testes críticos:
1. Admin user + 3 empresas no banco → forCarteira retorna todas 3
2. Consultor user com 2 empresas pivot + 1 empresa fora carteira → forCarteira retorna só 2
</research_reference>
</context>

<tasks>
<task id="72-04-T1">
<title>NpsPendingServiceTest — 8 testes cobrindo diaCobranca + isPendente + forCarteira</title>

<read_first>
- app/Services/Nps/NpsPendingService.php (Plan 72-01)
- tests/Feature/Phase69/NpsTemplateServiceTest.php (padrão de teste stateless service)
- database/factories/UserFactory.php + CompanyFactory.php + NpsTemplateFactory.php
</read_first>

<action>
Criar `tests/Feature/Phase72/NpsPendingServiceTest.php`.

Estrutura:
```php
<?php
namespace Tests\Feature\Phase72;

use Tests\TestCase;
use Illuminate\Foundation\Testing\RefreshDatabase;
use App\Models\{User, Company, Configuracao, NpsSurvey, NpsTemplate, NpsTemplateQuestion, NpsTemplateOption};
use App\Services\Nps\NpsPendingService;
use Carbon\Carbon;

class NpsPendingServiceTest extends TestCase
{
    use RefreshDatabase;

    protected function tearDown(): void { Carbon::setTestNow(); parent::tearDown(); }

    private function service(): NpsPendingService {
        return $this->app->make(NpsPendingService::class);
    }

    private function seedDefaultTemplate(): NpsTemplate {
        // Seed NPS Padrão para NpsTemplateService::resolveForCompany funcionar
        return NpsTemplate::factory()->create([
            'is_default' => true,
            'nome' => 'NPS Padrão',
            'active' => true,
        ]);
    }
}
```

Testes:

1. **`test_dia_cobranca_default_25_quando_sem_config()`:**
   - Assert `$this->service()->diaCobranca()` === 25

2. **`test_dia_cobranca_le_config_quando_persistido()`:**
   - `Configuracao::set('nps_dia_cobranca', 10)`
   - Assert diaCobranca() === 10

3. **`test_dia_cobranca_clamp_1_31_quando_valor_corrompido()`:**
   - `Configuracao::set('nps_dia_cobranca', 0)` → clamp para 1
   - Assert diaCobranca() === 1
   - `Configuracao::set('nps_dia_cobranca', 99)` → clamp para 31
   - Assert diaCobranca() === 31

4. **`test_isPendente_false_no_mes_corrente_antes_do_dia_cobranca()`:**
   - `Configuracao::set('nps_dia_cobranca', 25)`
   - `Carbon::setTestNow('2026-07-10 12:00:00')` (dia 10, antes de 25)
   - Setup template default + company
   - Assert isPendente($company) === false

5. **`test_isPendente_true_no_mes_corrente_depois_do_dia_cobranca_sem_survey_completo()`:**
   - `Configuracao::set('nps_dia_cobranca', 25)`
   - `Carbon::setTestNow('2026-07-26 12:00:00')`
   - Setup template default + company (sem survey completed)
   - Assert isPendente($company) === true

6. **`test_isPendente_false_quando_ha_survey_completed_no_mes()`:**
   - `Carbon::setTestNow('2026-07-26 12:00:00')`
   - Setup template default + company + NpsSurvey completed do mês 2026-07
   - Assert isPendente($company) === false

7. **`test_forCarteira_admin_retorna_todas_empresas_pendentes()`:**
   - `Carbon::setTestNow('2026-07-26')`
   - Setup 3 companies + template default + admin user
   - Assert `count(service->forCarteira($admin))` === 3
   - Assert shape do primeiro item tem chaves company_id/name/template_id/template_nome/month_reference/dias_atraso

8. **`test_forCarteira_consultor_retorna_apenas_carteira()`:**
   - `Carbon::setTestNow('2026-07-26')`
   - Setup 5 companies no total; consultor user com 2 no pivot company_users
   - Assert `count(service->forCarteira($consultor))` === 2
   - Assert que apenas company_ids da carteira aparecem
</action>

<acceptance_criteria>
- File `tests/Feature/Phase72/NpsPendingServiceTest.php` exists com 8 métodos test_
- `Carbon::setTestNow` usado — assertar via `grep -c "setTestNow" tests/Feature/Phase72/NpsPendingServiceTest.php` >= 4
- Clamp testado — assertar via `grep -n "clamp\\|diaCobranca..*=== 1\\|diaCobranca..*=== 31" tests/Feature/Phase72/NpsPendingServiceTest.php`
- Assertion do shape do array retornado — assertar via `grep -nE "template_nome|dias_atraso" tests/Feature/Phase72/NpsPendingServiceTest.php`
- Rodar `php artisan test tests/Feature/Phase72/NpsPendingServiceTest.php` → 8/8 PASSED
</acceptance_criteria>
</task>

<task id="72-04-T2">
<title>NpsDiaCobrancaConfigTest — 4 testes: PATCH admin + validação + 403 non-admin</title>

<read_first>
- app/Http/Controllers/NpsController.php método atualizarDiaCobranca (Plan 72-01 T2)
- tests/Feature/Phase70/NpsTemplateCrudTest.php (padrão actingAs admin + PATCH)
- database/factories/UserFactory.php (admin state)
</read_first>

<action>
Criar `tests/Feature/Phase72/NpsDiaCobrancaConfigTest.php`.

Testes:

1. **`test_admin_persiste_dia_cobranca_valido_via_patch()`:**
   - actingAs admin
   - PATCH `/nps/configuracao/dia-cobranca` com `dia: 10`
   - Assert redirect back + assertDatabaseHas('configuracoes', ['chave' => 'nps_dia_cobranca', 'valor' => '10'])

2. **`test_admin_atualiza_dia_existente()`:**
   - Configuracao::set('nps_dia_cobranca', '25') (pré-setup)
   - actingAs admin
   - PATCH com `dia: 30`
   - Assert `Configuracao::get('nps_dia_cobranca')` === '30'

3. **`test_dia_fora_range_retorna_422()`:**
   - actingAs admin
   - PATCH com `dia: 0` → assertStatus(302) + assertSessionHasErrors(['dia'])
   - PATCH com `dia: 32` → assertStatus(302) + assertSessionHasErrors(['dia'])
   - PATCH com `dia: 'abc'` → assertStatus(302) + assertSessionHasErrors(['dia'])

4. **`test_non_admin_recebe_403()`:**
   - actingAs consultor
   - PATCH `/nps/configuracao/dia-cobranca` com `dia: 15`
   - assertStatus(403)

**Nota:** seguir padrão Phase 70/71 — usar `->post/->patch` (não `->postJson/->patchJson`) para bater com fluxo real do Inertia (302 + session errors).
</action>

<acceptance_criteria>
- File exists com 4 métodos test_
- assertDatabaseHas + assertSessionHasErrors presentes — assertar via `grep -c "assertDatabaseHas\\|assertSessionHasErrors" tests/Feature/Phase72/NpsDiaCobrancaConfigTest.php` >= 4
- `PATCH /nps/configuracao/dia-cobranca` exercitado — assertar via `grep -n "dia-cobranca" tests/Feature/Phase72/NpsDiaCobrancaConfigTest.php`
- Rodar `php artisan test tests/Feature/Phase72/NpsDiaCobrancaConfigTest.php` → 4/4 PASSED
</acceptance_criteria>
</task>

<task id="72-04-T3">
<title>DashboardPendencyPropsTest — 5 testes cobrindo prop injection + calculator</title>

<read_first>
- app/Http/Controllers/DashboardController.php (métodos admin/user)
- app/Http/Controllers/CompanyController.php (show refactored T2 do Plan 72-02)
- app/Http/Controllers/PortfolioController.php (show)
- tests/Feature/Phase70/NpsTemplateCrudTest.php (assertInertia com component + has)
</read_first>

<action>
Criar `tests/Feature/Phase72/DashboardPendencyPropsTest.php`.

Testes:

1. **`test_admin_dashboard_injeta_nps_pendentes_com_shape_completo()`:**
   - Setup admin + template default + 2 companies (com contratos ativos)
   - `Carbon::setTestNow('2026-07-26')` (após dia_cobranca default 25)
   - actingAs admin
   - GET `/dashboard` (ou rota do admin dashboard)
   - `$response->assertInertia(fn (Assert $p) => $p
       ->component('Dashboard/Admin')
       ->has('nps_pendentes')
       ->has('nps_pendentes.0.company_id')
       ->has('nps_pendentes.0.template_id')
       ->has('nps_pendentes.0.dias_atraso')
     );`

2. **`test_user_dashboard_filtra_carteira()`:**
   - Setup consultor com 2 empresas no pivot; template default; 3 companies no total
   - `Carbon::setTestNow('2026-07-26')`
   - actingAs consultor
   - GET `/dashboard` (ou rota user dashboard)
   - Assert `nps_pendentes` tem no máximo 2 elementos (apenas carteira)

3. **`test_portfolio_show_injeta_pendentes()`:**
   - Setup portfolio (carteira de um analista) com 2 companies
   - `Carbon::setTestNow('2026-07-26')`
   - actingAs user apropriado
   - GET `/portfolio/{id}` — validar prop `nps_pendentes` presente
   - **Nota:** verificar exato path da rota Portfolio (verificar `routes/web.php` — pode ser `portfolios.show` ou similar). Se signature incerta, testar apenas que `nps_pendentes` é injetado como array.

4. **`test_company_show_usa_calculator_para_survey_v15()`:**
   - Setup: template com 1 pergunta escala 5 options (auto-geradas); survey v15 completo com 1 answer (peso=4, dimensao='empresa')
   - actingAs admin
   - GET `/companies/{id}`
   - Assert Inertia component `Companies/Show` + assert que `nps_surveys.0.score_empresa` === 4.0 (float) — vem do NpsScoreCalculator (não do score_empresa legado, que estaria null em survey v15 pós-Phase 69-03)

5. **`test_baseline_suite_continua_verde_zero_regressao()`:**
   - Este teste é operacional — rodar `php artisan test --stop-on-failure tests/Feature/Phase31 tests/Feature/Phase33 tests/Feature/Phase68 tests/Feature/Phase69 tests/Feature/Phase70 tests/Feature/Phase71` no CI
   - Se algum quebrar → BLOQUEAR execução deste plan e reportar regressão introduzida pelo Plan 72-02
   - Alternativa: teste PHP puro que roda `\Illuminate\Support\Facades\Process::run('...')` — mas isso é anti-pattern. Melhor: apenas confirmar suíte na T4 (SUMMARY step).

Se preferir simplificar: OMITIR o teste 5 (é validação operacional, não semântica). Manter 4 testes acionáveis:
1. admin_dashboard_injeta
2. user_dashboard_filtra_carteira  
3. portfolio_show_injeta
4. company_show_usa_calculator_para_survey_v15
</action>

<acceptance_criteria>
- File exists com 4 (ou 5) métodos test_
- `assertInertia` com `has('nps_pendentes')` — assertar via `grep -n "has..'nps_pendentes'" tests/Feature/Phase72/DashboardPendencyPropsTest.php`
- Teste do calculator com peso=4.0 — assertar via `grep -nE "score_empresa.*4\\.0|score_empresa..* 4\\.0" tests/Feature/Phase72/DashboardPendencyPropsTest.php`
- Rodar `php artisan test tests/Feature/Phase72/DashboardPendencyPropsTest.php` → 4/4 PASSED (ou 5/5)
</acceptance_criteria>
</task>

<task id="72-04-T4">
<title>Suite completa + baseline regressão + SUMMARY do plan e PHASE-SUMMARY</title>

<read_first>
- .planning/phases/71-formul-rio-p-blico-din-mico/PHASE-SUMMARY.md (baseline: 118 tests NPS)
- .planning/phases/71-formul-rio-p-blico-din-mico/71-03-SUMMARY.md (padrão de SUMMARY do plan)
</read_first>

<action>
1. Baseline pré-plan:
   ```bash
   /c/xampp/php/php.exe artisan test tests/Feature/Phase31 tests/Feature/Phase33 tests/Feature/Phase68 tests/Feature/Phase69 tests/Feature/Phase70 tests/Feature/Phase71 --stop-on-failure 2>&1 | tail -5
   ```
   Registrar total (deve ser 118).

2. Phase 72 solo:
   ```bash
   /c/xampp/php/php.exe artisan test tests/Feature/Phase72 --stop-on-failure 2>&1 | tail -20
   ```
   Deve ser 16 ou 17 PASSED (dependendo de manter T5 ou não).

3. Suite completa (Phases 31/33/68/69/70/71/72):
   ```bash
   /c/xampp/php/php.exe artisan test tests/Feature/Phase31 tests/Feature/Phase33 tests/Feature/Phase68 tests/Feature/Phase69 tests/Feature/Phase70 tests/Feature/Phase71 tests/Feature/Phase72 2>&1 | tail -10
   ```
   Deve ser exatamente (118 + N onde N = testes do Phase 72). Zero regressão.

4. Se algum teste 31/33/68/69/70/71 quebrar → BLOQUEAR e reportar como regressão do Plan 72-02.

5. Escrever `.planning/phases/72-dashboards-pendencias-dia-cobranca/72-04-SUMMARY.md` (padrão herdado de 71-03).

6. Escrever `.planning/phases/72-dashboards-pendencias-dia-cobranca/PHASE-SUMMARY.md` consolidando os 4 plans com métricas agregadas:
   - Frontmatter YAML
   - Wave-by-wave breakdown
   - REQs NPS-E-01 a NPS-E-05 fechados
   - SC1-SC5 PASSED
   - Métricas: services criados, componentes React, testes, regressão preservada, duração total
   - Próximo: Phase 73 (Limpeza legado + testes E2E) desbloqueada
</action>

<acceptance_criteria>
- Phase 72 solo: 16-17 PASSED
- Suite NPS completa (Phases 31-72): (118+N) PASSED, delta = +N, ZERO regressão
- 72-04-SUMMARY.md exists com métricas medidas
- PHASE-SUMMARY.md exists consolidando os 4 plans
</acceptance_criteria>
</task>
</tasks>

<verification>
1. 3 arquivos de teste criados em tests/Feature/Phase72/
2. 16-17 testes PASSED
3. Suite completa (Phases 31/33/68/69/70/71/72) sem regressão
4. Mapeamento SC → REQ documentado no PHASE-SUMMARY.md
</verification>

<must_haves_summary>
- 5 REQs NPS-E-01 a NPS-E-05 fechados no backend + frontend + testes
- 5 SC do ROADMAP Phase 72 → 100% mapeados via testes ou verificação operacional
- Regressão zero em Phases 31/33/68/69/70/71
- PHASE-SUMMARY.md sinaliza Phase 73 desbloqueada
</must_haves_summary>
