---
phase: 54-refinamentos-sugadores-uat-filtros
plan: "04"
type: execute
wave: 4
depends_on:
  - 54-02
  - 54-03
autonomous: false
requirements:
  - REQ-54-04
files_modified:
  - .planning/phases/54-refinamentos-sugadores-uat-filtros/54-04-UAT.md
  - .planning/phases/54-refinamentos-sugadores-uat-filtros/PHASE-SUMMARY.md
  - .planning/todos/pending/270702-refinamentos-sugadores-uat-e-magic-ui.md
  - .planning/ROADMAP.md
must_haves:
  truths:
    - "Deploy da Phase 54 aplicado em prod após aprovação explícita do operador (memory rule: perguntar antes de deploy.sh na v9.0)"
    - "Checklist UAT com 5 itens (A1, A2, A3, B1, B2) executado em prod com evidência OK/BUG"
    - "Blocos A e B do TODO 270702 (linhas 8-33) marcados como concluídos; Bloco C (Phase 55) permanece pending"
    - "ROADMAP.md registra Phase 54 como concluída com data"
    - "PHASE-SUMMARY.md agrega decisões-chave das 3 waves de código"
  artifacts:
    - path: ".planning/phases/54-refinamentos-sugadores-uat-filtros/54-04-UAT.md"
      provides: "Checklist executado com timestamp, resultado por item, screenshots opcionais"
    - path: ".planning/phases/54-refinamentos-sugadores-uat-filtros/PHASE-SUMMARY.md"
      provides: "Sumário final — props backend novas (is_admin, analistas, periodo, periodo_presets), decisões de UI (grid-cols-4 vs minmax, remoção Link Detalhes, ConfigPickerModal órfão)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 54 marcada como Concluída"
  key_links:
    - from: "deploy.sh"
      to: "VPS Hostinger 177.7.53.164"
      via: "pscp/plink; supervisorctl restart ecf-worker; php artisan cache:clear"
      pattern: "memory rule perguntar antes do deploy.sh na v9.0 (outro dev em paralelo); memory `project_ecf_drive_cache_stale` — cache:clear obrigatório pós-deploy que altera controller"
    - from: "UAT checklist"
      to: "5 itens A1/A2/A3/B1/B2 do 54-CONTEXT.md"
      via: "confronto item-a-item; cada item vira linha OK/BUG"
      pattern: "52-04-UAT.md formato de referência"
---

<objective>
Fechar a Phase 54 com deploy controlado + UAT humana em prod cobrindo os 5 refinamentos A1/A2/A3/B1/B2. Wave sem código — só operações + validação + documentação.

**Bloqueio de autonomia:** requer confirmação explícita do operador antes do `deploy.sh` (memory rule: v9.0 tem outro dev em paralelo; push e demais comandos VPS seguem autorização permanente, mas deploy.sh específico exige confirmação caso-a-caso).

Após UAT verde, fechar Bloco A + B do TODO 270702, deixar Bloco C (Phase 55 — Magic UI + redesign visual) pending.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-CONTEXT.md
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-01-SUMMARY.md
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-02-SUMMARY.md
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-03-SUMMARY.md
@.planning/todos/pending/270702-refinamentos-sugadores-uat-e-magic-ui.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Deploy da Phase 54 em prod agora?</decision>
  <context>
    Todas as 3 waves de código estão verdes localmente:
    - Wave 1 (54-01): 11 testes PHPUnit Feature (4 filtro analista + 7 filtro período), 100% verdes
    - Wave 2 (54-02): layout 2 colunas + filtro período + click row em EmpresaListagem.jsx; `npm run build` OK
    - Wave 3 (54-03): Show.jsx limpo + Index.jsx com busca e select analista; `npm run build` OK

    **Memory rule aplicada** (`feedback_perguntar_antes_deploy_v9`): v9.0 tem outro dev em paralelo — confirmar deploy caso-a-caso.

    Arquivos afetados (backend + frontend, sem migration):
    - app/Http/Controllers/SugadorController.php
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
    - resources/js/Pages/Sugadores/Show.jsx
    - resources/js/Pages/Sugadores/Index.jsx
    - public/build/* (bundle Vite)

    Deploy path: `bash deploy.sh` (full) OU `bash deploy_parcial.sh` (só alterados).

    **Efeito colateral esperado** (documentar antes do deploy): filtro `periodo` default = `hoje` no `/sugadores/empresa/{id}` é BREAKING — sugadores em_acao antigos somem da tela por default (research §Common Pitfalls #3). Comportamento locked no CONTEXT.md decision B1. Operador precisa estar ciente antes de aprovar deploy — se ver "vazio" em empresa que sabidamente tem em_acao, mudar filtro para "Todos".
  </context>
  <options>
    <option id="deploy-full">
      <name>bash deploy.sh (full)</name>
      <pros>Garantia máxima — composer install + migrate (no-op) + cache clear + supervisor restart</pros>
      <cons>Mais lento (~2-3min); mais chance de interferir com o outro dev</cons>
    </option>
    <option id="deploy-parcial">
      <name>bash deploy_parcial.sh</name>
      <pros>Rápido, só sobe arquivos alterados; menor risco de colisão</pros>
      <cons>Não roda migrate (OK — sem migration nesta phase); precisa `cache:clear` manual pós-deploy</cons>
    </option>
    <option id="wait">
      <name>Aguardar coordenação com o outro dev</name>
      <pros>Zero risco de colisão</pros>
      <cons>Bloqueia entrega até coordenação</cons>
    </option>
  </options>
  <resume-signal>Responder: `deploy-full`, `deploy-parcial` ou `wait`</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Deploy aplicado no VPS 177.7.53.164 conforme opção escolhida acima. Bundle Vite sincronizado, cache limpo, workers reiniciados.

    **Sequência recomendada** (executor guia o operador):
    1. `git stash` local (proteger WIP do outro dev na v9.0) — SÓ SE o operador confirmar que há branch v9.0 ativa
    2. `bash deploy.sh` OU `bash deploy_parcial.sh`
    3. Via SSH ou script pós-deploy no VPS:
       - `php artisan cache:clear` (memory `project_ecf_drive_cache_stale` — cache mascara fix de dados)
       - `php artisan route:list --path=sugadores | grep -E "empresa|index"` → 2+ rotas listadas
       - `sudo supervisorctl status ecf-worker:*` → workers RUNNING
    4. `git stash pop` local (restaurar WIP se stashado)
    5. Abrir `https://admin.ecfconsultoria.com.br/sugadores` — carrega sem 500
  </what-built>
  <how-to-verify>
    Operador responde após:
    - Deploy retornou sem erro
    - `cache:clear` executado
    - Workers RUNNING
    - Página `/sugadores` sanity-checked (200 OK, header novo aparece com busca + select se admin)
  </how-to-verify>
  <resume-signal>Responder "deploy-ok" com output resumido; se falhar colar erro; se pulou algum passo, mencionar</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    UAT completo dos 5 itens em prod. Criar `54-04-UAT.md` com checklist preenchido item-a-item.
  </what-built>
  <how-to-verify>
    Testar em `https://admin.ecfconsultoria.com.br/sugadores` com dois perfis:
    - Login **admin** (verificar A3 dropdown analista + regressões amplas)
    - Login **analista carteira** (verificar A3 dropdown escondido + regressão de carteira preservada)

    Executar checklist em ordem A1 → A2 → A3 → B1 → B2:

    ### A1 — Layout 2 colunas em /sugadores/empresa/{id}
    - [ ] Abrir uma empresa com ao menos 1 sugador → layout tem tabela à ESQUERDA + ConfigResumoCard à DIREITA (não mais em cima)
    - [ ] Botão "Rodar análise" está no card da direita
    - [ ] Cronômetro (quando análise roda) aparece dentro do card direito
    - [ ] Redimensionar janela pra <1024px → sidebar cai pra CIMA da tabela (grid-cols-1)
    - [ ] Scrollar tabela em desktop → sidebar sticky mantém visível (não some com scroll)

    ### A2 — Show.jsx sem ConfigResumoCard
    - [ ] Abrir /sugadores/{qualquer sugador} → NENHUM card "Configuração de detecção" na tela
    - [ ] NENHUM botão "Rodar análise" na tela
    - [ ] Formulário status + acao_tomada continua funcional
    - [ ] Ações (mover SGI / pausar / resolvido / ignorado) continuam funcionais
    - [ ] Branch adgroup: MlbsDoAdgroup aparece em largura total (sem grid lateral)

    ### A3 — Header /sugadores com busca + filtro analista
    - [ ] Header do /sugadores NÃO tem mais botão "Configurar" com ícone Settings
    - [ ] Header tem input "Buscar empresa..." (com ícone lupa)
    - [ ] Digitar parte do nome de uma empresa → cards filtram em tempo real (client-side)
    - [ ] **Login admin:** aparece select "Todos analistas" ao lado do input
    - [ ] **Login admin:** selecionar um analista → cards reduzem para empresas em que ele é analista
    - [ ] **Login analista:** select analista NÃO aparece (só o input busca)
    - [ ] Chip "Continuar com [Empresa]" continua funcional
    - [ ] Empty state customizado: buscar termo inexistente → "Nenhuma empresa encontrada para \"xxx\""

    ### B1 — Filtro de período em /sugadores/empresa/{id}
    - [ ] Abrir empresa qualquer → select "Período" no topo com valor "Hoje" (default)
    - [ ] Sugadores exibidos só os de hoje (reference_date = today)
    - [ ] Mudar para "Últimos 7 dias" → URL vira `?periodo=7d`, tabela atualiza
    - [ ] Mudar para "Últimos 30 dias" → URL vira `?periodo=30d`
    - [ ] Mudar para "Todos" → URL vira `?periodo=todos`, todos pendente+em_acao aparecem
    - [ ] Scroll preservado após mudança (`preserveScroll`)
    - [ ] Refresh (F5) mantém filtro atual da URL

    ### B2 — Click row navega para Show
    - [ ] Clicar em área vazia da linha (nome, status, data) → navega para /sugadores/{id}
    - [ ] Hover em linha muda cor (`hover:bg-white/[0.05]`)
    - [ ] Cursor pointer visível ao pairar sobre linha
    - [ ] Clicar no checkbox → marca/desmarca sugador, NÃO navega
    - [ ] Clicar em "MLBs" → copia MLBs, NÃO navega
    - [ ] Botão "Detalhes" REMOVIDO (não aparece mais na linha)
    - [ ] Bulk copy MLBs continua funcional (selecionar 3+ e copiar)

    ### Regressões críticas
    - [ ] Filtros de status/tipo/data no Index continuam funcionando
    - [ ] Analista fora da carteira ainda recebe 403 ao tentar `/sugadores/empresa/{outra}` (via URL manual)
    - [ ] `Mover para SGI` (bulk-move) e ações individuais continuam OK
    - [ ] Testes de PhpUnit executados no VPS: `php artisan test --filter=Phase54` verde
    - [ ] `SugadorPolicy::manage` regressão: analista com carteira ainda vê "Configurar" no ConfigResumoCard da drilldown

    Se qualquer item falhar → registrar em `54-04-UAT.md` como BUG com passos + screenshot; abrir tarefa de hotfix (extra plan 54-05 ou revisão focada).
  </how-to-verify>
  <resume-signal>Responder "uat-ok" com contagem (5/5 OK), ou "uat-bug: A? / B?" listando falhas</resume-signal>
</task>

<task type="auto">
  <name>Tarefa 4: Fechar TODO 270702 (Blocos A+B) + PHASE-SUMMARY + ROADMAP</name>
  <files>
    - .planning/todos/pending/270702-refinamentos-sugadores-uat-e-magic-ui.md
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/PHASE-SUMMARY.md
    - .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/todos/pending/270702-refinamentos-sugadores-uat-e-magic-ui.md — linhas 1-45 completas
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/54-01-SUMMARY.md, 54-02-SUMMARY.md, 54-03-SUMMARY.md, 54-04-UAT.md
    - .planning/ROADMAP.md — localizar entrada Phase 54 e formato de fechamento (imitar Phase 52 recente)
    - .planning/phases/52-melhorias-ui-ux-comportamento-sugadores/PHASE-SUMMARY.md — template de referência
  </read_first>
  <action>
    1. **Atualizar TODO 270702:**
       - No topo do Bloco A (linha ~9 "## Bloco A — Correções pós-UAT"), adicionar: `**Status: CONCLUÍDO em 2026-07-02 na Phase 54 (54-04-UAT.md).**`
       - No topo do Bloco B (linha ~26 "## Bloco B — Features novas"), adicionar: `**Status: CONCLUÍDO em 2026-07-02 na Phase 54 (54-04-UAT.md).**`
       - Bloco C (Phase 55 — Magic UI + redesign visual) fica intacto
       - Considerar mover o arquivo para `.planning/todos/in-progress/` (parcialmente feito — Bloco C pending) OU deixar em `pending/` (Bloco C ainda dá o tom)
    2. **Criar `PHASE-SUMMARY.md`:**
       - Header: nome + data + waves executadas
       - **Backend (Wave 1):** novas props `is_admin`, `analistas`, `periodo`, `periodo_presets`; novos filtros `?analista_id` (index, admin only) + `?periodo` (porEmpresa, default `hoje`); 11 testes verdes
       - **UI EmpresaListagem (Wave 2):** layout grid-cols-4 col-span-3/col-span-1, sidebar sticky, ConfigResumoCard só na direita, filtro período `<select>` nativo, click row completo, botão "Detalhes" removido
       - **UI Show + Index (Wave 3):** Show.jsx ~85 linhas removidas (ConfigResumoCard + Rodar análise + cronômetro + componente local + hooks); Index.jsx botão Configurar substituído por busca client-side + select analista (admin only)
       - Decisões-chave:
         - Grid canônico `lg:grid-cols-4` (consistência com header atual) — não `[minmax(0,1fr)_320px]`
         - `<input>` e `<select>` NATIVOS (não shadcn/ui/input.jsx e ui/select.jsx — quebram dark theme)
         - Filtro período default `hoje` é BREAKING intencional (locked no CONTEXT B1)
         - Prop `is_admin` explícita (não reusar `can_manage` que também vale para analistas)
         - Link "Detalhes" removido (click-row cobre; discretion aprovada)
       - Débitos abertos:
         - Props `sugador_config` + `can_manage_config` ainda enviadas por `SugadorController::show` sem consumo (cleanup em seed futuro)
         - ConfigPickerModal órfão (se aplicável, conforme SUMMARY 54-03) — seed
         - `?analista_id` NÃO propagado no EmpresaListagem (research Pitfall #5: não faz sentido lá — decisão de design)
       - Próximo passo: Phase 55 (Magic UI + redesign visual) usando o TODO 270702 Bloco C
    3. **Atualizar ROADMAP.md:**
       - Localizar entrada Phase 54, marcar como Concluída em 2026-07-02
       - Formato: imitar como Phase 52 foi fechada (checkbox + data + link ao PHASE-SUMMARY)
    4. **Commit:** `docs(54): fecha Phase 54 — UAT refinamentos aprovado em prod`
  </action>
  <verify>
    <automated>ls .planning/phases/54-refinamentos-sugadores-uat-filtros/PHASE-SUMMARY.md && grep -c "Phase 54" .planning/ROADMAP.md</automated>
  </verify>
  <done>PHASE-SUMMARY.md existe, TODO 270702 blocos A+B marcados concluídos, ROADMAP.md registra Phase 54 concluída, commit doc registrado.</done>
</task>

</tasks>

<verification>
1. UAT humano em prod concluído — checklist 5/5 OK ou hotfix criado
2. PHASE-SUMMARY.md criado com todas as decisões
3. TODO 270702 blocos A+B marcados concluídos; Bloco C intacto para Phase 55
4. ROADMAP.md atualizado
5. Commit doc `docs(54): fecha Phase 54 ...` presente no git log
</verification>

<success_criteria>
- [ ] Deploy aprovado explicitamente pelo operador (não autopilot)
- [ ] Deploy aplicado + cache clear + workers ok + smoke page 200
- [ ] Checklist UAT com 5 itens executado (A1/A2/A3/B1/B2)
- [ ] Nenhuma regressão crítica (bulk copy MLBs, analista 403, filtros index)
- [ ] PHASE-SUMMARY.md com decisões e débitos abertos
- [ ] TODO 270702 blocos A+B fechados; Bloco C intacto
- [ ] ROADMAP.md registra Phase 54 concluída
- [ ] Commit final `docs(54): ...` presente
</success_criteria>

<output>
Phase 54 fechada. Próximo passo sugerido ao operador: `/gsd:plan-phase 55` para iniciar Modernização visual + Magic UI (Bloco C do TODO 270702).
</output>
