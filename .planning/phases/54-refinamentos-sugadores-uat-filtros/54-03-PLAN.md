---
phase: 54-refinamentos-sugadores-uat-filtros
plan: "03"
type: execute
wave: 3
depends_on:
  - 54-01
autonomous: true
requirements:
  - REQ-54-03
files_modified:
  - resources/js/Pages/Sugadores/Show.jsx
  - resources/js/Pages/Sugadores/Index.jsx
must_haves:
  truths:
    - "Show.jsx NÃO renderiza mais o ConfigResumoCard nem o botão 'Rodar análise' — só dados do sugador único (A2)"
    - "Show.jsx tem ~85 linhas a menos: bloco render (500-532), hooks/handler (145-188), componente local (671-760)"
    - "Show.jsx mantém 100% funcionalidades restantes: MLBs do adgroup (branch adgroup), campos individuais do sugador, ações (mover SGI, pausar, resolvido, ignorado), formulário status/acao_tomada"
    - "Index.jsx header substitui botão 'Configurar' (linhas 436-445) por: `<input>` busca de empresa + `<select>` filtro analista (visível SÓ quando is_admin=true)"
    - "Input busca é NATIVO com classes ECF (research §3 — não usar ui/input.jsx)"
    - "Select analista é NATIVO com classes ECF, populado da prop `analistas` (backend 54-01)"
    - "Busca filtra `companies_summary` CLIENT-SIDE via `useMemo` sobre `card.name.toLowerCase().includes(q.toLowerCase())` — mesmo pattern do ConfigPickerModal linhas 245-247"
    - "Filtro analista dispara `router.get(route('sugadores.index'), { analista_id }, { preserveState, preserveScroll })` — SERVER-SIDE"
    - "Estado do input busca (q) é local (useState); valor `analista_id` vem da URL (props Inertia via useForm ou lido direto)"
    - "Analistas veem apenas o input de busca (sem dropdown) — bypass client-side não funciona porque backend também ignora o param para não-admin (validado em 54-01)"
    - "npm run build verde"
  artifacts:
    - path: "resources/js/Pages/Sugadores/Show.jsx"
      provides: "Show individual limpo — sem ConfigResumoCard, sem 'Rodar análise', sem cronômetro"
      contains: "// Phase 54 (A2): ConfigResumoCard e Rodar analise removidos"
    - path: "resources/js/Pages/Sugadores/Index.jsx"
      provides: "Header com input busca + select analista (admin only) — substitui botão Configurar"
      contains: "{is_admin && <select"
  key_links:
    - from: "Index.jsx select analista"
      to: "backend index (54-01) filtro ?analista_id"
      via: "router.get preserveState/preserveScroll"
      pattern: "Portfolio/Carteiras.jsx:25-30 applyPeriod"
    - from: "Index.jsx input busca"
      to: "useMemo local sobre companies_summary"
      via: "case-insensitive name.includes"
      pattern: "Index.jsx:245-247 ConfigPickerModal filtro"
    - from: "Show.jsx (limpo)"
      to: "EmpresaListagem (config volta a viver lá)"
      via: "click row navega para Show; config só via drilldown por empresa"
      pattern: "CONTEXT.md decision A2 — Show é sobre 1 sugador único, config é per-empresa"
---

<objective>
Duas mudanças de UI independentes que compartilham a mesma wave por não conflitar em arquivos:

1. **A2** — Limpar `Show.jsx`: remover ConfigResumoCard + botão "Rodar análise" + cronômetro (adicionados na Phase 52 Wave 3, contexto errado — view individual é sobre 1 sugador)
2. **A3** — Reformar header do `Index.jsx`: substituir botão "Configurar" por input de busca de empresa + select de filtro por analista (admin only), consumindo props `is_admin` e `analistas` de 54-01

Sem TDD estrito (UI puro; verificação via UAT humano em Wave 4). Verificação: `npm run build` + grep de sanidade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-CONTEXT.md
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-RESEARCH.md
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-01-SUMMARY.md
@resources/js/Pages/Sugadores/Show.jsx
@resources/js/Pages/Sugadores/Index.jsx
@resources/js/Pages/Portfolio/Carteiras.jsx
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Limpar Show.jsx — remover ConfigResumoCard + Rodar análise + cronômetro (A2)</name>
  <files>
    - resources/js/Pages/Sugadores/Show.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/Show.jsx:130-200 — props destructuring + hooks (analyzing, elapsed, rodarAnalise)
    - resources/js/Pages/Sugadores/Show.jsx:490-540 — bloco render que embrulha ConfigResumoCard + MlbsDoAdgroup em grid (linhas exatas conforme research §2)
    - resources/js/Pages/Sugadores/Show.jsx:665-770 — definição do componente local ConfigResumoCard (a deletar por completo)
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/54-RESEARCH.md §2 (linhas exatas a remover)
  </read_first>
  <action>
    1. **Remover hooks e handler (~44 linhas):** deletar `const [analyzing, setAnalyzing] = useState(false);` + `const [elapsed, setElapsed] = useState(0);` + `useEffect` do cronômetro + `function rodarAnalise()` completo (linhas ~153-188 do arquivo atual). Adicionar comentário curto:
       ```jsx
       // Phase 54 (A2): hooks analyzing/elapsed e handler rodarAnalise removidos.
       // ConfigResumoCard e "Rodar análise" agora vivem SÓ em EmpresaListagem
       // (config é per-empresa, não per-sugador).
       ```
    2. **Remover props consumidas apenas por ConfigResumoCard:** `sugador_config = null` e `can_manage_config = false` do destructuring (linhas ~137-141). Backend continua enviando (research §2 orientou deixar como está — cleanup vira seed futuro).
    3. **Remover bloco render (~33 linhas):** na área linhas ~500-532:
       - Para branch `sugador.tipo === 'adgroup'`: hoje há `<div className="grid grid-cols-1 lg:grid-cols-4 gap-4 mb-4">` com `<div className="lg:col-span-3"><MlbsDoAdgroup ... /></div>` + `<div className="lg:col-span-1"><ConfigResumoCard ... /></div>`. Substituir por SÓ `<MlbsDoAdgroup ... />` largura total (remover o grid wrapper).
       - Para branch `sugador.tipo === 'campanha'` (linhas ~522-531): remover o `<div className="mb-4"><ConfigResumoCard ... /></div>` inteiro. Nada substitui.
    4. **Remover componente local (~90 linhas):** deletar a função `function ConfigResumoCard({ ... }) { ... }` completa (linhas ~671-760). Se sobrarem imports usados apenas por ele (`Settings`, `AlertTriangle` de lucide-react — verificar via grep), remover dos imports também. Cuidado: `AlertTriangle` pode ser usado em outros lugares — grep antes.
    5. **Sanity de imports:** após remoção, rodar mentalmente: `analyzing / elapsed / rodarAnalise / setAnalyzing / setElapsed / ConfigResumoCard / sugador_config / can_manage_config` não devem aparecer em NENHUMA outra parte do arquivo. Se aparecerem, é bug de remoção incompleta.
    6. **Preservar tudo o resto:** formulário status/acao_tomada, mover SGI modal, MlbsDoAdgroup, MlbHighlight fallback, breadcrumb, ações do sugador (pausar/removido/reduzido_lance/reativado), links url_anuncio + url_ads, informações do adgroup.
    7. `npm run build` — deve passar.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. `grep -c "ConfigResumoCard" resources/js/Pages/Sugadores/Show.jsx` = 0 (grep -v '^//' para descartar comentário Phase 54). `grep -c "rodarAnalise" Show.jsx` = 0. `grep -c "analyzing" Show.jsx` = 0.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Header Index.jsx — busca + filtro analista (A3)</name>
  <files>
    - resources/js/Pages/Sugadores/Index.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/Index.jsx:1-90 — imports + helpers + destructuring de props
    - resources/js/Pages/Sugadores/Index.jsx:245-275 — ConfigPickerModal (pattern input nativo + useMemo filter)
    - resources/js/Pages/Sugadores/Index.jsx:310-330 — props do componente principal (companies_summary, can_manage, users)
    - resources/js/Pages/Sugadores/Index.jsx:432-450 — bloco header a substituir (`can_manage && <button Configurar>`)
    - resources/js/Pages/Sugadores/Index.jsx:479-516 — renderização dos cards (para saber onde aplicar o filtro client-side)
    - resources/js/Pages/Portfolio/Carteiras.jsx:1-60 — pattern `router.get` + `<select>` nativo (referência canônica)
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/54-RESEARCH.md §3
  </read_first>
  <action>
    1. **Adicionar props novas no destructuring do componente principal:** localizar assinatura tipo `export default function Sugadores({ companies_summary, can_manage, users, ... })` e adicionar `is_admin = false, analistas = [], analista_id = null`. Defaults seguros evitam crash se backend antigo cachear.
    2. **Adicionar state e memo para busca client-side:** no topo do componente:
       ```jsx
       const [q, setQ] = useState('');
       const companiesFiltradas = useMemo(() => {
           if (!q.trim()) return companies_summary;
           const needle = q.toLowerCase();
           return companies_summary.filter(c => (c.name || '').toLowerCase().includes(needle));
       }, [companies_summary, q]);
       ```
    3. **Adicionar handler router para filtro analista:**
       ```jsx
       const aplicarFiltroAnalista = (value) => {
           router.get(
               route('sugadores.index'),
               value ? { analista_id: value } : {},
               { preserveState: true, preserveScroll: true }
           );
       };
       ```
    4. **Substituir bloco `{can_manage && (<button Configurar>)}` (linhas ~436-445)** por:
       ```jsx
       <div className="relative">
           <Search size={14} className="absolute left-2.5 top-1/2 -translate-y-1/2 text-white/40 pointer-events-none" />
           <input
               type="text"
               value={q}
               onChange={e => setQ(e.target.value)}
               placeholder="Buscar empresa..."
               className="w-56 h-9 pl-8 pr-3 rounded-lg border border-white/[0.08] bg-white/[0.03] text-[13px] text-white/80 placeholder:text-white/30 focus:outline-none focus:border-ecf-yellow/40"
           />
       </div>
       {is_admin && (
           <select
               value={analista_id ?? ''}
               onChange={e => aplicarFiltroAnalista(e.target.value)}
               className="appearance-none h-9 pl-3 pr-8 rounded-lg border border-white/[0.08] bg-white/[0.03] text-[13px] text-white/80 focus:outline-none focus:ring-1 focus:ring-ecf-yellow/40"
               title="Filtrar empresas por analista"
           >
               <option value="" className="bg-ecf-card">Todos analistas</option>
               {analistas.map(a => (
                   <option key={a.id} value={a.id} className="bg-ecf-card">
                       {a.name}
                   </option>
               ))}
           </select>
       )}
       ```
       Import `Search` de `lucide-react` se ainda não estiver (verificar imports — provável já está para outras coisas). O botão "Configurar" some do header — CONTEXT decision A3 esclarece que ele continua acessível via drilldown na EmpresaListagem (ConfigResumoCard leva a `sugadores.config.show`).
    5. **Substituir consumo de `companies_summary` no render dos cards por `companiesFiltradas`:** procurar `companies_summary.length === 0` (linha ~480) e `companies_summary.map(...)` (renderização dos cards) — trocar para `companiesFiltradas`. Mensagem de "Nenhuma empresa visível" pode virar:
       ```jsx
       {companiesFiltradas.length === 0 ? (
           <div className="card-ecf rounded-xl p-12 text-center">
               <Building2 size={32} className="mx-auto text-white/20 mb-3" />
               <p className="text-white/60 text-sm font-medium">
                   {q.trim()
                       ? `Nenhuma empresa encontrada para "${q}".`
                       : (analista_id ? 'Nenhuma empresa para este analista.' : 'Nenhuma empresa visível.')}
               </p>
               ...
           </div>
       ) : (
           companiesFiltradas.map(...)
       )}
       ```
    6. **Remover import `Settings`** de lucide-react se não for mais usado em nenhum lugar do arquivo (grep antes — pode estar em outros headers/modais). Se ConfigPickerModal (linhas 245+) ainda usa, MANTER.
    7. **Sanity `setConfigPickerOpen`:** se o botão "Configurar" era o ÚNICO trigger de abrir o ConfigPickerModal, o modal fica órfão. Verificar via grep: se sim, decidir — deixar componente definido mas sem trigger (débito técnico documentado no SUMMARY, remove em phase futura). CONTEXT decision A3 confirma que o botão sai do header; o modal em si pode virar seed.
    8. `npm run build` — deve passar.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. Grep sanidade: `grep -c "is_admin" Index.jsx` >= 1, `grep -c "aplicarFiltroAnalista" Index.jsx` >= 1, `grep -c "companiesFiltradas" Index.jsx` >= 3 (definição + no length check + no map).</done>
</task>

<task type="auto">
  <name>Tarefa 3: Build final + git commit consolidado</name>
  <files>
    - resources/js/Pages/Sugadores/Show.jsx
    - resources/js/Pages/Sugadores/Index.jsx
    - public/build/manifest.json
  </files>
  <action>
    1. `npm run build` final.
    2. Diff review: `git diff resources/js/Pages/Sugadores/Show.jsx resources/js/Pages/Sugadores/Index.jsx`.
    3. Contagem esperada Show.jsx: ~-85 linhas net (subtração + comentário curto adicionado).
    4. Contagem esperada Index.jsx: ~+30 linhas (input + select + handler + useMemo + comentário).
    5. Commit: `feat(54-03): Show.jsx sem ConfigResumoCard e Index.jsx com busca + filtro analista`
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde, diff revisado, commit registrado.</done>
</task>

</tasks>

<verification>
1. `npm run build` — 0 warnings JSX
2. Show.jsx: `grep -v '^ *//' resources/js/Pages/Sugadores/Show.jsx | grep -c "ConfigResumoCard"` → 0 (ignorando comentário deixado)
3. Show.jsx: `grep -v '^ *//' resources/js/Pages/Sugadores/Show.jsx | grep -c "rodarAnalise\|analyzing\|elapsed"` → 0
4. Index.jsx: `grep -c "is_admin && " resources/js/Pages/Sugadores/Index.jsx` → 1 (o dropdown gated)
5. Index.jsx: `grep -c "companiesFiltradas" resources/js/Pages/Sugadores/Index.jsx` → >=3
</verification>

<success_criteria>
- [ ] Show.jsx sem ConfigResumoCard, sem botão "Rodar análise", sem cronômetro
- [ ] Show.jsx mantém MlbsDoAdgroup + formulário status + ações + breadcrumb
- [ ] Index.jsx header substitui "Configurar" por busca + select analista
- [ ] Busca filtra client-side via useMemo (case-insensitive)
- [ ] Select analista aparece SÓ para is_admin=true
- [ ] Handler dispara router.get com preserveState/preserveScroll
- [ ] `npm run build` verde
- [ ] Regressão zero: ConfigPickerModal (se ainda usado em outros triggers) continua funcionando
</success_criteria>

<output>
Criar `.planning/phases/54-refinamentos-sugadores-uat-filtros/54-03-SUMMARY.md` documentando:
- Contagem de linhas removidas Show.jsx (esperado ~85)
- Decisão sobre ConfigPickerModal órfão (se aplicável): mantido / removido / marcado como seed
- Confirmação de que backend props (`is_admin`, `analistas`) são consumidas
- Débitos abertos:
  - Props `sugador_config` + `can_manage_config` ainda enviadas por `SugadorController::show` mas não consumidas — cleanup seed futuro
  - ConfigPickerModal código morto (se aplicável) — seed
</output>
