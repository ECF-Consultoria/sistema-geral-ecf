---
phase: 54-refinamentos-sugadores-uat-filtros
plan: "02"
type: execute
wave: 2
depends_on:
  - 54-01
autonomous: true
requirements:
  - REQ-54-02
files_modified:
  - resources/js/Pages/Sugadores/EmpresaListagem.jsx
must_haves:
  truths:
    - "Layout 2 colunas (grid-cols-1 lg:grid-cols-4) — coluna esquerda `lg:col-span-3` com breadcrumb + nome empresa + filtro período + barra bulk + tabela; coluna direita `lg:col-span-1` com ConfigResumoCard sticky (`lg:sticky lg:top-4`)"
    - "Card `ConfigResumoCard` (com botão Rodar análise + cronômetro) fica AGORA na sidebar direita, NÃO mais em cima da tabela"
    - "Mobile (<lg): sidebar aparece EM CIMA da tabela (grid-cols-1 padrão)"
    - "Filtro de período no topo da tabela via `<select>` NATIVO com classes ECF (não shadcn/ui/select)"
    - "Presets: Hoje / Últimos 7 dias / Últimos 30 dias / Todos — vindos da prop `periodo_presets` do backend (54-01)"
    - "onChange do select dispara `router.get(route('sugadores.empresa.listagem', company.id), { periodo }, { preserveState: true, preserveScroll: true })` — pattern Portfolio/Carteiras.jsx"
    - "Value do select vem da prop `periodo` (backend garante default 'hoje') — persistido na URL após navegação"
    - "Row `<tr>` recebe `onClick={() => router.visit(route('sugadores.show', s.id))}` + classes `cursor-pointer hover:bg-white/[0.05]`"
    - "Checkbox `<td>` (linha 466 atual) recebe `onClick={e => e.stopPropagation()}` — não navega ao selecionar"
    - "Botões inline (`<td>` linha 500 atual: copiar MLBs + Link Detalhes) recebem `onClick={e => e.stopPropagation()}` no `<td>` wrapper"
    - "Link `Detalhes` (linha 519-526) removido — click-row cobre navegação (research §5 recomenda; CONTEXT decision discretion linha 65 aprova)"
    - "Bulk copy MLBs continua 100% funcional — nenhum handler existente removido"
    - "npm run build passa sem warning JSX"
  artifacts:
    - path: "resources/js/Pages/Sugadores/EmpresaListagem.jsx"
      provides: "Layout 2 colunas + filtro período + click row + stopPropagation em controles interativos"
      contains: "grid-cols-1 lg:grid-cols-4; lg:sticky lg:top-4; onClick={() => router.visit(route('sugadores.show'"
  key_links:
    - from: "EmpresaListagem.jsx select periodo"
      to: "backend porEmpresa (54-01)"
      via: "router.get com { periodo } preserveState/preserveScroll"
      pattern: "Portfolio/Carteiras.jsx:25-30 (applyPeriod) + linhas 46-54 (select nativo)"
    - from: "EmpresaListagem.jsx <tr onClick>"
      to: "SugadorController::show"
      via: "router.visit(route('sugadores.show', s.id))"
      pattern: "Performance/Index.jsx:324 (click row + stopPropagation em botões)"
    - from: "ConfigResumoCard"
      to: "sidebar sticky direita"
      via: "grid lg:col-span-1 + lg:sticky lg:top-4 lg:self-start"
      pattern: "grid-cols-4 col-span-3/col-span-1 já usado no header atual (linha 347)"
---

<objective>
Refactor puro de UI em `EmpresaListagem.jsx`. Zero mudança de backend (Wave 1 cobriu). Três mudanças coordenadas no mesmo arquivo:

1. **A1** — Layout 2 colunas: mover ConfigResumoCard do topo (linhas 367-377 atuais) para sidebar sticky à direita cobrindo header + tabela + bulk
2. **B1** — Filtro de período `<select>` nativo no topo da coluna esquerda (acima da barra bulk) usando props `periodo` + `periodo_presets` de 54-01
3. **B2** — Row clicável em toda `<tr>` da tabela + `stopPropagation` nos controles interativos + remoção do botão "Detalhes" redundante

Sem TDD estrito (UI puro, sem business logic — coberto via UAT humano em Wave 4). Verificação: `npm run build` + smoke visual local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-CONTEXT.md
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-RESEARCH.md
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-01-SUMMARY.md
@resources/js/Pages/Sugadores/EmpresaListagem.jsx
@resources/js/Pages/Portfolio/Carteiras.jsx
@resources/js/Pages/Performance/Index.jsx
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Layout 2 colunas + sidebar sticky com ConfigResumoCard (A1)</name>
  <files>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx:334-538 — return completo (referência integral)
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx:346-378 — grid header atual (a manter parcialmente, mover card)
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx:78-171 — componente ConfigResumoCard (usar como está, só mudar posição)
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/54-RESEARCH.md §1 — pattern grid-cols-4 confirmado como canônico
  </read_first>
  <action>
    1. Aceitar props novas no componente: além de `sugadores, company, sugador_config, can_manage_config, can_analyze` (já existentes), adicionar `periodo = 'hoje', periodo_presets = []` como defaults seguros.
    2. Envolver o retorno atual (a partir da linha 337 breadcrumb até linha 535 antes do fechamento AppLayout) num wrapper grid:
       ```jsx
       <div className="grid grid-cols-1 lg:grid-cols-4 gap-4">
           <div className="lg:col-span-3 min-w-0 space-y-4">
               {/* breadcrumb + header simplificado (SÓ nome empresa, SEM ConfigResumoCard) + filtro período + bulk bar + tabela */}
           </div>
           <aside className="lg:col-span-1 lg:sticky lg:top-4 lg:self-start space-y-4">
               <ConfigResumoCard
                   config={sugador_config}
                   companyId={company?.id}
                   canManage={can_manage_config}
                   canAnalyze={can_analyze}
                   analyzing={analyzing}
                   elapsed={elapsed}
                   onRodarAnalise={rodarAnalise}
               />
           </aside>
       </div>
       ```
       `min-w-0` na coluna esquerda é OBRIGATÓRIO — sem ele tabela empurra o grid (research §1).
    3. Simplificar o bloco header original (linhas 346-378 atuais): dentro do `lg:col-span-3`, manter APENAS o card-ecf com nome empresa + contagem de sugadores (bloco linhas 348-365 do arquivo atual). REMOVER o grid interno atual `<div className="grid grid-cols-1 lg:grid-cols-4 gap-4 mb-4">` (linha 347) e as `<div className="lg:col-span-3">` / `<div className="lg:col-span-1">` — o wrapper externo já cuida disso.
    4. **NÃO** duplicar ConfigResumoCard: garantir que ele aparece SÓ na aside direita — remover a instância do header antigo (linhas 367-377).
    5. Preservar TODA a lógica de `analyzing / elapsed / rodarAnalise / setSelectedIds / copiarMlbsLinha / copiarMlbsBulk / clearSelection` — só o marcador visual muda, os handlers ficam idênticos.
    6. Confirmar que `<aside>` fica visível em mobile — no default (grid-cols-1 sem breakpoint lg) a aside cai NAturalmente para cima da lista. Não precisar de order flip.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. Local smoke: abrir `/sugadores/empresa/{qualquer}` renderiza layout 2 colunas em desktop (>=1024px), 1 coluna em mobile, sidebar segue scroll (sticky).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Filtro de período no topo da coluna esquerda (B1)</name>
  <files>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
  </files>
  <read_first>
    - resources/js/Pages/Portfolio/Carteiras.jsx:1-60 — pattern applyPeriod + `<select>` nativo (referência canônica)
    - resources/js/Pages/Sugadores/Index.jsx:267-275 — pattern input nativo com classes ECF
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/54-RESEARCH.md §3 e §4 — decisão de usar nativos (evitar ui/select.jsx e ui/input.jsx)
  </read_first>
  <action>
    1. Import `router` de `@inertiajs/react` se ainda não estiver (verificar topo do arquivo — Show.jsx já importa, provável).
    2. Adicionar handler dentro do componente (após o `rodarAnalise`):
       ```jsx
       const aplicarPeriodo = (value) => {
           router.get(
               route('sugadores.empresa.listagem', company.id),
               { periodo: value },
               { preserveState: true, preserveScroll: true }
           );
       };
       ```
    3. Dentro da coluna esquerda (`lg:col-span-3`), INSERIR entre o card-ecf do nome empresa e a barra bulk (linha 380 atual) um novo bloco:
       ```jsx
       <div className="flex items-center gap-3">
           <label htmlFor="periodo-filter" className="text-white/50 text-xs uppercase tracking-wide">Período</label>
           <select
               id="periodo-filter"
               value={periodo}
               onChange={e => aplicarPeriodo(e.target.value)}
               className="appearance-none h-9 pl-3 pr-8 rounded-xl border border-white/[0.08] bg-white/[0.03] text-[13px] text-white/80 focus:outline-none focus:ring-1 focus:ring-ecf-yellow/40"
           >
               {periodo_presets.map(p => (
                   <option key={p.value} value={p.value} className="bg-ecf-card">
                       {p.label}
                   </option>
               ))}
           </select>
           <span className="text-white/30 text-[11px]">
               {sugadores.length === 0
                   ? 'Nenhum sugador no período'
                   : `${sugadores.length} sugador${sugadores.length !== 1 ? 'es' : ''}`}
           </span>
       </div>
       ```
       Classes ECF do research §3 (Portfolio/Carteiras.jsx:46-54 exato). `className="bg-ecf-card"` nas `<option>` evita fundo branco padrão do browser em `<select>` nativo dark theme.
    4. Verificar rota nomeada existente: research menciona `sugadores.empresa.listagem` — confirmar em `routes/web.php` que é o nome usado no `Route::get('/sugadores/empresa/{company}', ...)`. Se for outro nome (ex.: `sugadores.por-empresa`), usar o correto. Grep `sugadores.empresa` em routes/web.php antes de escrever.
    5. Fallback defensivo: se `periodo_presets.length === 0` (backend rollback / cache stale), select renderiza vazio mas não crasha (map de array vazio → sem `<option>`).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. Local smoke: mudar select → URL vira `?periodo=7d`, tabela filtrada, scroll preservado, sidebar sticky mantida.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Click row inteiro + stopPropagation + remover botão Detalhes (B2)</name>
  <files>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx:462-530 — bloco `<tr>` atual completo
    - resources/js/Pages/Performance/Index.jsx:320-332 — pattern canônico (research §5)
    - resources/js/Pages/Dev/SugadoresMlOnboarding/Index.jsx:388 — precedente `<td onClick stopPropagation>` em row clicável
  </read_first>
  <action>
    1. Na `<tr>` (linha 462), adicionar:
       - `onClick={() => router.visit(route('sugadores.show', s.id))}`
       - Alterar `className`: incluir `cursor-pointer` e trocar `hover:bg-white/[0.02]` por `hover:bg-white/[0.05]` (research §5: affordance visual mais forte).
       ```jsx
       <tr
           key={s.id}
           onClick={() => router.visit(route('sugadores.show', s.id))}
           className={cn(
               'border-b border-white/[0.03] last:border-0 hover:bg-white/[0.05] cursor-pointer transition-colors',
               isSelected && 'bg-ecf-yellow/[0.03]',
           )}
       >
       ```
    2. `<td>` do checkbox (linha 466) — adicionar `onClick={e => e.stopPropagation()}`:
       ```jsx
       <td onClick={e => e.stopPropagation()} className="p-3">
           <input type="checkbox" ... />
       </td>
       ```
    3. `<td>` das ações (linha 500 "text-right") — adicionar `onClick={e => e.stopPropagation()}`:
       ```jsx
       <td onClick={e => e.stopPropagation()} className="p-3 text-right">
           <div className="inline-flex items-center gap-1.5 justify-end flex-wrap">
               {isElegivel && (
                   <button type="button" onClick={() => copiarMlbsLinha(s.id)} ...>MLBs</button>
               )}
               {/* NOTA: Link "Detalhes" removido — click-row cobre navegação */}
           </div>
       </td>
       ```
    4. **REMOVER** o `<Link href={route('sugadores.show', s.id)}>` inteiro (linhas 519-526) — click-row substitui. Manter apenas o botão "MLBs" quando `isElegivel`.
    5. Se após a remoção o `<td>` ficar visualmente vazio para sugadores tipo=campanha (isElegivel=false), verificar se precisa ajustar min-width para o `<td>` não colapsar horrendamente. Se sim, adicionar `min-w-[80px]` no `<td>`. Se não, deixar.
    6. Ícone `<Copy>` no botão MLBs continua com `stopPropagation` via bubbling do `<td>` — não precisa handler extra no botão.
    7. Remover import de `ExternalLink` do `lucide-react` se não for mais usado em NENHUM outro lugar do arquivo (grep antes).
    8. Remover import de `Link` de `@inertiajs/react` se não for mais usado (grep — o breadcrumb linha 338 ainda usa: MANTER).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. Local smoke: clicar em qualquer área não-checkbox e não-botão-MLBs da linha navega para `/sugadores/{id}`. Clicar no checkbox marca/desmarca sem navegar. Clicar em "MLBs" copia sem navegar. Bulk copy MLBs continua funcional.</done>
</task>

<task type="auto">
  <name>Tarefa 4: Build final + git commit consolidado</name>
  <files>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
    - public/build/manifest.json
  </files>
  <action>
    1. `npm run build` — precisa passar limpo.
    2. Sanity local (dev pode reabrir /sugadores/empresa/{id} no browser após `php artisan serve` se DB local disponível — memory `project_mariadb_local_corrompido` alerta que pode não estar).
    3. Verificar diff: `git diff resources/js/Pages/Sugadores/EmpresaListagem.jsx` — confirmar que:
       - ConfigResumoCard NÃO aparece 2x (só na aside)
       - Grid interno antigo do header removido
       - `<Link Detalhes>` removido
       - Todos os handlers stopPropagation em lugar
    4. Commit: `feat(54-02): layout 2 colunas + filtro periodo + click row em EmpresaListagem`
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde, diff revisado, commit registrado.</done>
</task>

</tasks>

<verification>
1. `npm run build` — 0 warnings JSX, bundle gerado
2. Grep manual: `grep -n "ConfigResumoCard" resources/js/Pages/Sugadores/EmpresaListagem.jsx` → APENAS 2 matches (definição + 1 uso na aside)
3. Grep manual: `grep -n "stopPropagation" resources/js/Pages/Sugadores/EmpresaListagem.jsx` → pelo menos 2 matches (checkbox td + actions td)
4. Grep manual: `grep -n "hover:bg-white/\\[0.05\\]" resources/js/Pages/Sugadores/EmpresaListagem.jsx` → match na `<tr>`
</verification>

<success_criteria>
- [ ] Layout 2 colunas responsivo funcionando (desktop = 3+1, mobile = stack)
- [ ] ConfigResumoCard aparece SÓ na sidebar (não duplicado)
- [ ] Sidebar sticky em desktop (não some ao rolar tabela longa)
- [ ] Select de período visível, presets vindos do backend, onChange dispara router.get
- [ ] Click em qualquer parte da linha (exceto checkbox e botão MLBs) navega para /sugadores/{id}
- [ ] Checkbox e botão MLBs mantêm comportamento (não navegam)
- [ ] Link "Detalhes" removido
- [ ] Bulk copy MLBs 100% funcional (regressão zero)
- [ ] `npm run build` verde
</success_criteria>

<output>
Criar `.planning/phases/54-refinamentos-sugadores-uat-filtros/54-02-SUMMARY.md` documentando:
- Decisão do wrapper `lg:grid-cols-4` (não `[minmax(0,1fr)_320px]` — consistência com header atual)
- Remoção do Link "Detalhes" (research recomendou; discretion aprovada)
- Rota nomeada confirmada (`sugadores.empresa.listagem` ou nome real do routes/web.php)
- Débitos abertos: nenhum previsto (mudança fechada em 1 arquivo)
</output>
