---
phase: 54-refinamentos-sugadores-uat-filtros
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-54-01
files_modified:
  - app/Http/Controllers/SugadorController.php
  - tests/Feature/Phase54/SugadorIndexAnalistaFilterTest.php
  - tests/Feature/Phase54/SugadorPorEmpresaPeriodoFilterTest.php
must_haves:
  truths:
    - "GET /sugadores como admin recebe prop `is_admin=true` e `analistas` = users com pivot `company_users.role='analista'` ordenados por nome (id, name)"
    - "GET /sugadores como não-admin (consultor / analista / mentor) recebe `is_admin=false` e `analistas=[]` — dropdown de analista fica escondido no frontend"
    - "GET /sugadores?analista_id=X reduz `companies_summary` para APENAS empresas em que o user X aparece em `company_users` com `role='analista'`"
    - "GET /sugadores?analista_id=X ignorado (não aplica filtro) quando usuário logado não é admin — evita bypass"
    - "GET /sugadores/empresa/{id}?periodo=hoje (default) filtra sugadores via `whereDate('reference_date', today())` — mesmo pattern do index linha 109"
    - "GET /sugadores/empresa/{id}?periodo=7d filtra `reference_date >= now()->subDays(7)->toDateString()`"
    - "GET /sugadores/empresa/{id}?periodo=30d filtra `reference_date >= now()->subDays(30)->toDateString()`"
    - "GET /sugadores/empresa/{id}?periodo=todos NÃO aplica filtro de data (traz todos pendente + em_acao)"
    - "porEmpresa propaga na prop `periodo` o valor atual + prop `periodo_presets` com labels prontos ({hoje, 7d, 30d, todos})"
    - "porEmpresa mantém regressão: analista fora da carteira recebe 403 (não afeta pela mudança do filtro)"
    - "Cada teste RED (failing) tem commit separado do GREEN (implementação) — padrão TDD Phase 52"
  artifacts:
    - path: "app/Http/Controllers/SugadorController.php"
      provides: "index() com prop `is_admin` + prop `analistas` (admin only) + filtro `?analista_id` aplicado em `$visibleIds` antes do bloco de agregação (linha ~155); porEmpresa() com filtro `?periodo=hoje|7d|30d|todos` e default `hoje`"
      contains: "\\$user->isAdmin() && \\$request->filled('analista_id'); match (\\$periodo)"
    - path: "tests/Feature/Phase54/SugadorIndexAnalistaFilterTest.php"
      provides: "PHPUnit Feature — admin recebe analistas + is_admin=true; não-admin recebe analistas=[] e is_admin=false; filtro ?analista_id=X reduz companies_summary; não-admin com ?analista_id=X ignora filtro"
    - path: "tests/Feature/Phase54/SugadorPorEmpresaPeriodoFilterTest.php"
      provides: "PHPUnit Feature — periodo=hoje filtra sugadores detectados hoje; periodo=7d/30d aplicam janela; periodo=todos remove WHERE de data; regressão 403 analista fora da carteira"
  key_links:
    - from: "SugadorController::index"
      to: "companies_summary via \\$visibleIds"
      via: "intersect com company_users.role='analista' ANTES do aggregates SUM/CASE"
      pattern: "SugadorController.php:94-101 (mesmo padrão whereIn subquery), aplicado sobre \\$visibleIds ao invés de \\$query"
    - from: "SugadorController::porEmpresa"
      to: "sugadores query"
      via: "match(\\$periodo) com where('reference_date', '>=', ...) ou whereDate hoje"
      pattern: "SugadorController.php:109 whereDate('reference_date', today())"
    - from: "prop is_admin"
      to: "frontend Index.jsx (54-03)"
      via: "Inertia render props"
      pattern: "similar a can_manage (linha 311 Index.jsx pós-Phase 52)"
---

<objective>
Preparar o backend da Phase 54: adicionar filtro por analista no /sugadores (A3) e filtro de período no /sugadores/empresa/{id} (B1). Zero mudança de UI aqui — puramente controller + props + testes.

**Wave 1 é TDD obrigatório.** Filtro de analista mexe em `$visibleIds` (universo agregado, alta chance de regredir count_hoje/total_pendentes). Filtro de período muda default de porEmpresa (era "tudo pendente/em_acao", passa a ser "só hoje") — precisa cobertura para regressões críticas em prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-CONTEXT.md
@.planning/phases/54-refinamentos-sugadores-uat-filtros/54-RESEARCH.md
@app/Http/Controllers/SugadorController.php
@app/Models/User.php
@app/Models/Company.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Testes RED — filtro analista no index (SugadorIndexAnalistaFilterTest)</name>
  <files>
    - tests/Feature/Phase54/SugadorIndexAnalistaFilterTest.php
  </files>
  <read_first>
    - tests/Feature/Phase52/SugadorPolicyManageAnalistaTest.php — padrão de setup admin/analista + carteira (referência)
    - app/Http/Controllers/SugadorController.php:35-234 — index() completo (props enviadas)
    - app/Http/Controllers/SugadorController.php:94-101 — subquery company_users existente (a copiar)
    - CLAUDE.md memory `project_mariadb_local_corrompido` — usar SQLite in-memory
  </read_first>
  <behavior>
    - `test_admin_recebe_prop_is_admin_true_e_lista_analistas`: cria 2 users role='consultor', vincula 1 como analista em company_users (role='analista') de empresa A e outro como consultor de B. Login admin → GET /sugadores → assertInertia prop `is_admin` === true, prop `analistas` array com exatamente 1 item (o vinculado como analista), com chaves `id` e `name`.
    - `test_nao_admin_recebe_is_admin_false_e_analistas_vazio`: login consultor com carteira → assert `is_admin===false`, `analistas === []`.
    - `test_filtro_analista_id_reduz_companies_summary_para_admin`: cria empresas A (com analista X), B (com analista Y), C (sem analista). Login admin → GET /sugadores?analista_id={X.id} → prop `companies_summary` contém APENAS A.
    - `test_filtro_analista_id_ignorado_para_nao_admin`: login consultor com carteira nas 3 empresas → GET /sugadores?analista_id={X.id} → `companies_summary` traz TODAS as 3 da carteira (filtro não aplica). Segurança contra bypass.
    - `test_filtro_analista_id_sem_match_retorna_summary_vazio`: admin, `analista_id` de user sem vínculo `role='analista'` → `companies_summary === []` (ou array vazio).
  </behavior>
  <action>
    1. Criar diretório `tests/Feature/Phase54/` se não existir (mkdir).
    2. Escrever `SugadorIndexAnalistaFilterTest.php` extends `Tests\TestCase` com `use RefreshDatabase;`.
    3. Namespace `Tests\Feature\Phase54`.
    4. `setUp()`: `parent::setUp()` + garantir migrations (RefreshDatabase cobre) + factories básicas.
    5. Implementar os 4 testes acima.
    6. Cada teste faz login via `actingAs()` com User factory (role='admin' ou 'consultor' via `->state(['role' => 'admin'])`).
    7. Criar company via factory + attach user via `$company->users()->attach($user->id, ['role' => 'analista', 'assigned_at' => now()])` (pivot manual).
    8. Assertion via `assertInertia(fn (Assert $page) => $page->where('is_admin', true)->has('analistas', 1)...)`.
    9. Rodar `php artisan test --filter=SugadorIndexAnalistaFilterTest` → todos DEVEM falhar (RED).
    10. Commit RED: `test(54-01): failing tests para filtro de analista no /sugadores`
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=SugadorIndexAnalistaFilterTest --stop-on-failure</automated>
  </verify>
  <done>4 testes escritos, todos falhando por motivo correto (prop `is_admin` ou `analistas` inexistente, ou filtro não aplicado). Commit RED registrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Testes RED — filtro período em porEmpresa (SugadorPorEmpresaPeriodoFilterTest)</name>
  <files>
    - tests/Feature/Phase54/SugadorPorEmpresaPeriodoFilterTest.php
  </files>
  <read_first>
    - app/Http/Controllers/SugadorController.php:277-359 — porEmpresa() completo
    - app/Models/Sugador.php — factory + status STATUS_PENDENTE/STATUS_EM_ACAO
    - tests/Feature/Phase52/ (padrões de factory Sugador com reference_date)
  </read_first>
  <behavior>
    - `test_default_periodo_hoje_filtra_apenas_sugadores_do_dia`: cria 3 sugadores na empresa (reference_date = hoje, ontem, 10 dias atrás, todos status=pendente). GET /sugadores/empresa/{id} (sem query param) → prop `sugadores` contém APENAS o de hoje (1 item).
    - `test_periodo_7d_traz_ultima_semana`: mesmos 3 sugadores. GET com ?periodo=7d → contém hoje e ontem (2 itens), exclui o de 10 dias atrás.
    - `test_periodo_30d_traz_ultimo_mes`: adiciona um sugador reference_date=45 dias atrás. GET ?periodo=30d → traz hoje, ontem, o de 10 dias (3 itens), exclui o de 45.
    - `test_periodo_todos_nao_aplica_filtro_data`: GET ?periodo=todos → traz todos os 4 sugadores pendentes/em_acao independente da data.
    - `test_prop_periodo_atual_propagada`: GET ?periodo=7d → assertInertia prop `periodo === '7d'`. Sem query param → `periodo === 'hoje'`.
    - `test_periodo_invalido_cai_no_default_hoje`: GET ?periodo=xpto → filtra como `hoje` (1 item, prop `periodo === 'hoje'`). Segurança contra query manipulation.
    - `test_regressao_analista_fora_da_carteira_403`: analista sem vínculo à empresa → GET /sugadores/empresa/{outra}?periodo=todos → 403.
  </behavior>
  <action>
    1. Criar `tests/Feature/Phase54/SugadorPorEmpresaPeriodoFilterTest.php`.
    2. Namespace `Tests\Feature\Phase54`, extends `Tests\TestCase`, `use RefreshDatabase`.
    3. `setUp()`: helper `createSugador($company, $refDate, $status = 'pendente')` que aceita Carbon ou string.
    4. Escrever os 7 testes acima.
    5. Login como admin (via `role='admin'`) — evita conflito com regras de carteira dos analistas em testes de filtro; teste de regressão 403 usa role='consultor'.
    6. `assertInertia(fn (Assert $page) => $page->component('Sugadores/EmpresaListagem')->has('sugadores', N))`.
    7. Rodar `php artisan test --filter=SugadorPorEmpresaPeriodoFilterTest` → todos DEVEM falhar (RED).
    8. Commit RED: `test(54-01): failing tests para filtro de periodo em porEmpresa`
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=SugadorPorEmpresaPeriodoFilterTest --stop-on-failure</automated>
  </verify>
  <done>7 testes escritos, todos falhando. Commit RED registrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: GREEN — implementar filtros no SugadorController::index e ::porEmpresa</name>
  <files>
    - app/Http/Controllers/SugadorController.php
  </files>
  <read_first>
    - app/Http/Controllers/SugadorController.php:35-234 (index inteiro — não quebrar filtro user_id existente linhas 94-101)
    - app/Http/Controllers/SugadorController.php:277-359 (porEmpresa inteiro)
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/54-RESEARCH.md §3 e §4 e §6 (patterns confirmados)
  </read_first>
  <action>
    1. **index() — prop `is_admin`:** localizar bloco Inertia::render final do index (props array). Adicionar `'is_admin' => $user->isAdmin(),` na lista de props (ao lado de `can_manage`).
    2. **index() — prop `analistas`:** ANTES do render, calcular:
       ```php
       $analistas = $user->isAdmin()
           ? \App\Models\User::whereIn('id', function ($sub) {
               $sub->select('user_id')
                   ->from('company_users')
                   ->where('role', 'analista');
             })
             ->orderBy('name')
             ->get(['id', 'name'])
           : collect();
       ```
       Enviar em `'analistas' => $analistas,`.
    3. **index() — filtro `?analista_id`:** APÓS calcular `$visibleIds` (linha ~155, DEPOIS de `$companies->pluck('id')->all()`) e ANTES do bloco `if (!empty($visibleIds))` que gera `$aggregates` (linha ~157):
       ```php
       if ($user->isAdmin() && $request->filled('analista_id')) {
           $analistaId = (int) $request->analista_id;
           $visibleIds = collect($visibleIds)->intersect(
               \DB::table('company_users')
                   ->where('user_id', $analistaId)
                   ->where('role', 'analista')
                   ->pluck('company_id')
           )->values()->all();
           // Também filtrar $companies para que o map final (linhas 193-228) não gere summary de empresa fora do filtro
           $companies = $companies->whereIn('id', $visibleIds)->values();
       }
       ```
       Guard `$user->isAdmin()` explícito bloqueia bypass (não-admin ignora silenciosamente — comportamento locked no CONTEXT decision A3).
    4. **porEmpresa() — filtro `?periodo`:** substituir o bloco atual (linhas 298-301) por:
       ```php
       $periodo = $request->input('periodo', 'hoje');
       if (!in_array($periodo, ['hoje', '7d', '30d', 'todos'], true)) {
           $periodo = 'hoje';
       }

       $sugadoresQuery = Sugador::where('company_id', $company->id)
           ->whereIn('status', [Sugador::STATUS_PENDENTE, Sugador::STATUS_EM_ACAO]);

       match ($periodo) {
           '7d'    => $sugadoresQuery->where('reference_date', '>=', now()->subDays(7)->toDateString()),
           '30d'   => $sugadoresQuery->where('reference_date', '>=', now()->subDays(30)->toDateString()),
           'todos' => null, // sem filtro
           default => $sugadoresQuery->whereDate('reference_date', today()),
       };

       $sugadores = $sugadoresQuery
           ->orderBy('reference_date', 'desc')
           ->orderBy('id', 'desc')
           ->get([...])
           ->map(...)
           ->values()
           ->all();
       ```
       Preserva `->get([...cols])->map(...)` exatamente como está hoje (linhas 302-335).
    5. **porEmpresa() — propagar `periodo` nas props:** adicionar no `Inertia::render('Sugadores/EmpresaListagem', [...])`:
       ```php
       'periodo'         => $periodo,
       'periodo_presets' => [
           ['value' => 'hoje',  'label' => 'Hoje'],
           ['value' => '7d',    'label' => 'Últimos 7 dias'],
           ['value' => '30d',   'label' => 'Últimos 30 dias'],
           ['value' => 'todos', 'label' => 'Todos'],
       ],
       ```
    6. Comentário curto em pt-BR acima do bloco `analista_id` explicando por que aplicar em `$visibleIds` e não em `$query` (research §3: aggregates precisam do universo já reduzido).
    7. Rodar `php artisan test --filter=Phase54` → 11 testes DEVEM passar (GREEN).
    8. Commit GREEN: `feat(54-01): filtros analista_id (index) e periodo (porEmpresa) no SugadorController`
  </action>
  <verify>
    <automated>vendor/bin/phpunit --filter=Phase54 --stop-on-failure</automated>
  </verify>
  <done>11 testes passando (4 do index + 7 do porEmpresa). Commit GREEN registrado. Nenhum teste da Phase 52/anteriores regride (`vendor/bin/phpunit --group sugadores` ou equivalente segue verde).</done>
</task>

</tasks>

<verification>
1. `vendor/bin/phpunit --filter=Phase54` → 11/11 verdes
2. `vendor/bin/phpunit --filter=Phase52` → mantém verde (regressão)
3. Log verificar que nenhuma nova query N+1 aparece no `index` (bloco aggregates continua único SELECT SUM CASE)
4. Manual local (opcional): `php artisan tinker` → simular `Route::get('/sugadores?analista_id=X')` e ver payload
</verification>

<success_criteria>
- [ ] 4 testes de filtro analista escritos, verdes
- [ ] 7 testes de filtro período escritos, verdes
- [ ] `SugadorController::index` envia props `is_admin` + `analistas`
- [ ] Filtro `?analista_id` reduz `companies_summary` apenas para admin
- [ ] `SugadorController::porEmpresa` aplica match($periodo) com default `hoje`
- [ ] Prop `periodo` + `periodo_presets` propagadas ao frontend
- [ ] Regressão Phase 52 verde (nenhum teste anterior quebra)
- [ ] Commits RED e GREEN separados por tarefa (2 RED + 1 GREEN = mínimo 3 commits nesta wave)
</success_criteria>

<output>
Criar `.planning/phases/54-refinamentos-sugadores-uat-filtros/54-01-SUMMARY.md` documentando:
- Decisão sobre onde aplicar `?analista_id` (em `$visibleIds` — research §3)
- Decisão sobre validação `periodo` inválido (cai no default `hoje` — Common Pitfall #3 do research)
- Débito técnico aberto: `?analista_id` na URL do EmpresaListagem NÃO faz sentido (research Pitfall #5) — decisão de não propagar
- Contagem de testes verdes + regressão Phase 52
</output>
