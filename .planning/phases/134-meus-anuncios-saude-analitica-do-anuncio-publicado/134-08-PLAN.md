---
phase: 134-meus-anuncios-saude-analitica-do-anuncio-publicado
plan: 08
type: execute
wave: 5
depends_on: ["134-07"]
files_modified:
  - resources/js/Pages/Mlb/ModoAnuncioTabs.jsx
  - resources/js/Pages/Mlb/MeusAnuncios.jsx
  - tests/js/estrutura-meus-anuncios.test.js
autonomous: false
requirements: [D-03, D-04, D-08, D-09, D-11, D-12, D-13, D-14, D-18, D-21, D-22]

must_haves:
  truths:
    - "O módulo abre em Meus Anúncios — as 4 abas são Meus Anúncios, Individual, Em massa, Histórico, nessa ordem"
    - "O topo da sub-aba Publicados diz quantos anúncios precisam de atenção e por quê, e clicar num motivo filtra a lista"
    - "Coleta velha mostra banner de defasagem em vez de tela em branco"
    - "A nota aparece como X de 86, nunca convertida para 100"
    - "Nenhum botão de pausar, editar ou mover anúncio existe na tela — nem desabilitado"
  artifacts:
    - path: "resources/js/Pages/Mlb/ModoAnuncioTabs.jsx"
      provides: "4ª aba Meus Anúncios, na primeira posição"
      contains: "mlb.anuncios.meus"
    - path: "resources/js/Pages/Mlb/MeusAnuncios.jsx"
      provides: "Página da aba: sub-abas, banner de defasagem, triagem, barra utilitária e tabela"
      contains: "de 86"
    - path: "tests/js/estrutura-meus-anuncios.test.js"
      provides: "Gate de conformidade com o contrato de design e com o D-11"
  key_links:
    - from: "resources/js/Pages/Mlb/MeusAnuncios.jsx"
      to: "mlb.anuncios.meus"
      via: "router.get com preserveState para filtro, busca e sub-aba"
      pattern: "router\\.get\\("
    - from: "resources/js/Pages/Mlb/MeusAnuncios.jsx"
      to: "mlb.anuncios.meus.atualizar"
      via: "router.post do botão Atualizar agora"
      pattern: "meus\\.atualizar"
---

<objective>
Construir a tela: a 4ª aba do módulo, a sub-aba Publicados com triagem acionável no topo e a tabela densa ordenada por gravidade, e todos os selos de honestidade — defasagem, origem, não avaliado e nota em base 86.

Purpose: é o que o usuário pediu com todas as letras — "abro, escolho a empresa, e em 5 segundos sei quais anúncios estão saudáveis, quais estão perdendo venda e por quê, sem clicar em nada escondido". O `134-UI-SPEC.md` é contrato aprovado nas 6 dimensões e vinculante para tudo que é visual.
Output: `ModoAnuncioTabs` com 4 abas, `MeusAnuncios.jsx` e o gate estrutural que trava tipografia, peso, espaçamento e ausência de qualquer ação de escrita.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-CONTEXT.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-PATTERNS.md
@resources/js/Pages/Mlb/ModoAnuncioTabs.jsx
@resources/js/Pages/Mlb/AnunciosHistorico.jsx
@resources/js/Pages/Mlb/anuncioHistoricoUtils.js
@resources/js/Components/ui/table.jsx
@resources/js/Components/ui/select.jsx
@resources/js/Layouts/AppLayout.jsx
@tests/js/estrutura-grade-glide.test.js
@tests/js/_fonte.js

<interfaces>
<!-- Props recebidas do controller (plano 134-07). Contrato completo. -->
```
empresa: { id, nome }
sub: 'publicados' | 'rascunhos'
subTotais: { publicados: number, rascunhos: number }
anuncios: LengthAwarePaginator<{
  ml_item_id, titulo, thumbnail, permalink, origem, rascunho_id, listing_tier, status,
  estoque, vendas, visitas, visitas_dias, buybox_status, buybox_nao_avaliado,
  nota_ecf, nota_base, motivos, severidade
}>
triagem: { total: number, chips: [{ chave, label, count, cor: 'red'|'amber' }], nao_avaliado: number }
filtros: { busca: string, status: 'ativos'|'pausados'|'encerrados'|'todos', motivo: string|null }
defasagem: { coletado_em, horas, defasado, nunca_coletado, motivo }
saudeMlDisponivel: boolean
rotacaoN: number
```

<!-- Array final de MODOS em ModoAnuncioTabs.jsx (UI-SPEC seção 1) -->
```js
const MODOS = [
    { chave: 'meus',       label: 'Meus Anúncios', rota: 'mlb.anuncios.meus',      Icone: Gauge },
    { chave: 'individual', label: 'Individual',     rota: 'mlb.anuncios.wizard',    Icone: FileText },
    { chave: 'massa',      label: 'Em massa',       rota: 'mlb.anuncios.massa',     Icone: Grid3x3 },
    { chave: 'historico',  label: 'Histórico',      rota: 'mlb.anuncios.historico', Icone: History },
];
```

<!-- Vocabulário fechado do contrato de design (UI-SPEC seções Typography, Spacing e Color) -->
```
Tamanhos permitidos no código novo: text-[11px] (Label), text-sm (Body 14),
                                    text-base (Heading 16), text-xl (Display 20)
PROIBIDOS: text-xs, text-[10px], text-[12px], text-[13px], qualquer outro

Pesos permitidos: font-normal (400), font-semibold (600)
PROIBIDOS: font-medium (500), font-bold (700)

Espaçamento: só múltiplos de 4 — gap-1/px-1/py-1 (4px), gap-2/px-2 (8), p-4/gap-4 (16),
             p-6 (24), gap-8 (32)
PROIBIDOS no código novo: qualquer classe com meio-passo (px-2.5, gap-1.5, py-0.5, mt-0.5...)

Accent ecf-yellow: só aba ativa nível 1, sub-aba ativa nível 2, botão Atualizar agora,
                   botão Publicar lote, selo de origem ECF, linha Nota ECF do gráfico
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4ª aba + shell da página, sub-abas, banner de defasagem e barra utilitária</name>
  <files>resources/js/Pages/Mlb/ModoAnuncioTabs.jsx, resources/js/Pages/Mlb/MeusAnuncios.jsx</files>
  <read_first>
    - `resources/js/Pages/Mlb/ModoAnuncioTabs.jsx` — arquivo inteiro (58 linhas): o array `MODOS`, a função `trocarModo` com `router.get(route(item.rota, { company: empresaId }))` e as classes da pill ativa/inativa. **Só o array muda**; a mecânica não
    - `resources/js/Pages/Mlb/AnunciosHistorico.jsx` — cabeçalho da página com chip da empresa, o campo de busca com ícone `Search` e input transparente, a função `buscar()` com `router.get(..., { preserveState: true, replace: true })`, e o bloco de paginação com `links` do paginator
    - `resources/js/Components/ui/select.jsx` — API do `Select`/`SelectTrigger`/`SelectContent`/`SelectItem`. **Nunca usar `value=""` num `SelectItem`**: derruba o render inteiro (armadilha já registrada no projeto); usar sentinela textual
    - `resources/js/Layouts/AppLayout.jsx` — como as páginas do módulo montam o layout e o `<Head>`
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md` seções "Estrutura da página", 1, 2, 5, 6 e o Copywriting Contract
  </read_first>
  <action>
Em `ModoAnuncioTabs.jsx`, substituir o array `MODOS` pelo do bloco `<interfaces>` — a entrada `meus` entra na **primeira** posição (D-13: é a aba inicial) e o ícone `Gauge` é importado de `lucide-react`, o mesmo ícone do painel "Saúde do anúncio" do wizard. Nada mais no arquivo muda: nem a assinatura do componente, nem `trocarModo`, nem as classes das pills. Os três consumidores atuais (`AnunciarML`, `AnunciarMassa`, `AnunciosHistorico`) continuam passando o mesmo `modo` e não precisam de alteração.

Criar `resources/js/Pages/Mlb/MeusAnuncios.jsx` — componente `default` recebendo as props do bloco `<interfaces>`, dentro de `AppLayout`, com `<Head title="Meus Anúncios" />`.

Ordem vertical exatamente como a "Estrutura da página" do UI-SPEC:
1. Cabeçalho: ícone `Gauge`, título "Meus Anúncios" em Display (`text-xl font-semibold`), e o chip da empresa no mesmo padrão de `AnunciosHistorico`.
2. `<ModoAnuncioTabs empresaId={empresa.id} modo="meus" />`.
3. Sub-abas Publicados e Rascunhos, com o JSX da seção 2 do UI-SPEC copiado como está: estilo sublinhado (`-mb-px border-b-2`), ativa com `border-ecf-yellow font-semibold text-white`, inativa com `border-transparent font-normal text-white/40`, contador ao lado em `text-[11px] tabular-nums text-white/30`. A troca de sub-aba é **round-trip ao servidor** com `router.get(route('mlb.anuncios.meus', { company: empresa.id }), { sub, ...filtrosAtuais }, { preserveState: true, replace: true })` — nunca `useState` local, porque as duas sub-abas exigem queries de backend diferentes.
4. Banner de defasagem, renderizado só quando `defasagem.defasado` ou `defasagem.nunca_coletado`. Usar o JSX da seção 5a do UI-SPEC: borda e fundo âmbar, ícone `Clock`, texto em Body (`text-sm`) e o botão "Atualizar agora" alinhado à direita. Tom âmbar, nunca vermelho — não é erro do usuário. A copy é a do Copywriting Contract: `Última coleta há {N} dia(s){, motivo: {motivo}} — os dados abaixo podem estar desatualizados.`
5. Barra utilitária: campo de busca por título ou id (reusando o padrão `Search` mais input transparente de `AnunciosHistorico`), `Select` de status com as opções Ativos, Pausados, Encerrados e Todos (Ativos é o default, D-03), e o botão "Atualizar agora" à direita.

Botão "Atualizar agora": JSX da seção 6 do UI-SPEC, tratamento outline-amarelo (o sólido-amarelo fica reservado para "Publicar lote"). Estados: idle mostra `RefreshCw` mais "Atualizar agora"; ao clicar, `router.post(route('mlb.anuncios.meus.atualizar', { company: empresa.id }), {}, { preserveScroll: true })`, o botão fica desabilitado com `Loader2` girando e "Enfileirando…"; ao voltar, o flash de sucesso do Inertia mostra a mensagem, e o botão só reabilita depois de um cooldown de 30 segundos no cliente — sem polling, sem websocket.

Estados vazios, com a copy literal do Copywriting Contract (não reescrever nenhuma das três):
- Publicados sem nenhuma coleta: heading "Ainda não coletamos os anúncios desta empresa." mais o corpo e o botão "Atualizar agora" em destaque.
- Publicados com coleta mas sem itens no filtro atual: heading "Esta empresa não tem anúncios ativos no Mercado Livre." mais o corpo que aponta as abas Individual/Em massa e o filtro de status.
- A sub-aba Rascunhos e seu estado vazio entram no plano 134-09; aqui, quando `sub` for `rascunhos`, renderizar um contêiner vazio que o próximo plano preenche — **sem** texto de "em breve" nem placeholder visível ao usuário.

Todo filtro, busca e troca de sub-aba faz round-trip com `preserveState` e `replace`, nunca filtragem no cliente: filtrar as 50 linhas da página corrente mentiria sobre as outras 66 mil.

Vocabulário visual: só os 4 tamanhos, os 2 pesos e os espaçamentos múltiplos de 4 do bloco `<interfaces>`. `cn()` de `@/lib/utils` para composição condicional. Nenhuma biblioteca de UI nova, nenhum componente novo em `Components/ui/`. Comentários em pt-BR.

**Não tocar em `AnunciosHistorico.jsx` nem em `AnunciarML.jsx` neste plano** — o espaçamento em meio-passo daqueles arquivos é débito herdado do módulo e o UI-SPEC declara explicitamente que corrigi-lo não é escopo desta fase.
  </action>
  <acceptance_criteria>
    - `resources/js/Pages/Mlb/ModoAnuncioTabs.jsx` tem `meus` como primeiro item do array `MODOS`, com `rota: 'mlb.anuncios.meus'` e `Icone: Gauge`, e `Gauge` importado de `lucide-react`
    - `resources/js/Pages/Mlb/MeusAnuncios.jsx` contém as três frases literais de estado vazio e a frase do banner de defasagem, exatamente como no Copywriting Contract
    - `resources/js/Pages/Mlb/MeusAnuncios.jsx` contém `modo="meus"` e `route('mlb.anuncios.meus.atualizar'`
    - Nenhuma ocorrência de `value=""` em `SelectItem`
    - `npm run build` sai com código 0
    - `git diff --name-only` não inclui `resources/js/Pages/Mlb/AnunciosHistorico.jsx` nem `resources/js/Pages/Mlb/AnunciarML.jsx`
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>O módulo abre em Meus Anúncios, a página monta com sub-abas, banner de defasagem honesto e barra utilitária que faz round-trip.</done>
</task>

<task type="auto">
  <name>Task 2: Triagem acionável + tabela de anúncios com os selos de honestidade</name>
  <files>resources/js/Pages/Mlb/MeusAnuncios.jsx</files>
  <read_first>
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md` seções 3, 4, 5b, 7, 8 e 11 — triagem, as 8 colunas da tabela, selo de defasagem em nível de campo, selo de origem, nota ECF com as faixas de cor, e o chip "Não avaliado". O JSX dessas seções é para ser usado como está
    - `resources/js/Components/ui/table.jsx` — `Table`, `TableHeader`, `TableRow`, `TableHead`, `TableCell`; o wrapper já traz `overflow-auto`
    - `resources/js/Pages/Mlb/anuncioHistoricoUtils.js` — `rotuloTier()`, que traduz `gold_special` e `gold_pro` para Clássico e Premium. **Não** usar `linkAnuncioMl()` aqui: o `permalink` já vem pronto da API
    - `resources/js/Pages/Mlb/AnunciarML.jsx` linhas 2761-2810 — o painel "Saúde do anúncio", de onde vem a régua de cor por faixa que a barra da Nota ECF reusa (ler apenas; este arquivo não é modificado neste plano)
    - `resources/js/Pages/Mlb/AnunciosHistorico.jsx` — o bloco de paginação com `links` do paginator, a ser reusado
  </read_first>
  <action>
Acrescentar a `MeusAnuncios.jsx`, dentro da sub-aba Publicados, os dois blocos que faltam.

**Triagem (seção 3 do UI-SPEC), acima da barra utilitária.** Bloco full-width em `bg-ecf-card border border-white/[0.08] rounded-2xl p-4`. Heading em `text-base font-semibold`: quando `triagem.total` é maior que zero, "{total} anúncios precisam de você" com `AlertTriangle` em `text-ecf-yellow`; quando é zero, "Nenhum anúncio precisa de atenção agora" com `CheckCircle2` em `text-emerald-400` — é estado real, não ausência do bloco. Logo abaixo, sempre que houver sobreposição, a nota em `text-[11px] text-white/30`: "(um anúncio pode aparecer em mais de um motivo)". O número exibido é `triagem.total`, que o backend já calculou como anúncios distintos com pelo menos um motivo — **nunca somar os `count` dos chips no cliente**; foi exatamente esse tipo de conta paralela que já produziu neste projeto um card que não fechava com a própria conta.

Chips por motivo, em linha, na ordem que vem do backend (que já é a ordem de gravidade), usando o JSX da seção 3: `aria-pressed` refletindo o filtro ativo, cor vermelha ou âmbar conforme `chip.cor`, contador em `<b className="tabular-nums">`. Clicar aplica `?motivo={chave}` por round-trip; clicar de novo no chip já ativo limpa o filtro. À direita e à parte, com estilo neutro tracejado, o chip "Não avaliado" com `triagem.nao_avaliado` — **informativo, nunca somado ao total**.

**Tabela (seção 4 do UI-SPEC).** `Table` de `@/Components/ui/table.jsx`, 8 colunas na ordem exata: Anúncio, Tier, Estoque, Vendas, Visitas, Nota ECF, Motivos, Ação.
- Anúncio: thumbnail 40 por 40 com `object-contain` e fallback `ImageOff` em `text-white/15`, título em Body com `line-clamp-2`, e o selo de origem abaixo do título.
- Tier: `rotuloTier(listing_tier)`.
- Estoque: `tabular-nums`, com `0` em `text-red-400 font-semibold`.
- Vendas: `tabular-nums text-white/60`.
- Visitas: o valor mais o micro-selo de defasagem de nível de campo (seção 5b, ícone `Clock` em 9px, `text-[11px] text-white/30`, `title` com a data completa) quando `visitas_dias` é maior que zero; quando `visitas` é nulo, o chip "Não avaliado" da seção 11, com o `title` "Ainda não coletado pela rotação — cobertura completa em até {rotacaoN} dias", lendo `rotacaoN` da prop e nunca com número escrito à mão.
- Nota ECF: a barra da seção 8, largura proporcional a `nota_ecf / 86`, cor por faixa (verde a partir de 69, âmbar de 43 a 68, vermelho abaixo de 43), e o número seguido do sufixo `de 86` em `text-[11px] font-normal text-white/30`. **A nota nunca é convertida para base 100** — a proporção da barra usa 86 como denominador e o texto diz "de 86", que é o que o D-22 trava. Quando `saudeMlDisponivel` é falso (Variante B), o cabeçalho desta coluna ganha um ícone `Info` com o `title` único do Copywriting Contract: "O Mercado Livre não expõe mais um score de qualidade comparável para este item — mostramos só a Nota ECF." A explicação aparece **uma vez, no cabeçalho**, nunca repetida por linha. Quando é verdadeiro (Variante A), a coluna passa a exibir os dois segmentos lado a lado — "ML: {buybox_status}" como badge mais a barra "ECF: X de 86" — com largura fixa para não quebrar o layout linha a linha.
- Motivos: chips compactos com as mesmas cores da triagem, até 2 visíveis e "+N" quando houver mais.
- Ação: link `<a>` para `permalink` com ícone `ExternalLink` e `aria-label="Abrir no Mercado Livre"` (ícone só, sem texto ao lado — sem o `aria-label` é botão mudo para leitor de tela), mais um botão "Abrir rascunho" com ícone `Pencil` e `aria-label`, renderizado **apenas** quando `origem` é `ecf` e `rascunho_id` não é nulo, apontando para o wizard com `?rascunho={id}`.

Selo de origem (seção 7): mapa de classes **local à página**, não reusar o componente `SourceBadge` — as variantes dele estão travadas por outro ADR com significado diferente (fonte de métrica, não origem de anúncio). Três variantes: `ecf` em amarelo, `time` em azul (mesma classe de `STATUS_BADGE.validado`), `legado` em cinza, todos com `title` nativo conforme a tabela da seção 7 e classe base `inline-flex items-center gap-1 rounded-full border px-1 py-1 text-[11px] font-semibold uppercase tracking-wide`.

Clique no título ou na thumbnail agrupados num único `<button type="button">`, que no plano 134-10 abrirá o modal de detalhe. Neste plano o handler existe e ainda não abre nada — deixar a função nomeada e o `onClick` ligado, com um comentário apontando o plano 134-10. O link do permalink e o botão de rascunho ficam na coluna Ação como **irmãos**, nunca aninhados dentro desse botão: botão dentro de botão é HTML inválido, armadilha já documentada em `AnunciosHistorico.jsx`.

Paginação: reusar o bloco de `links` do paginator de `AnunciosHistorico.jsx`. Sem seletor de itens por página.

**Nenhum controle de escrita.** Não existe botão de pausar, editar, mover ou republicar anúncio nesta tela — nem desabilitado, nem com rótulo "em breve" (D-11). As únicas ações são filtrar, ordenar (fixo), abrir o permalink, abrir o rascunho e "Atualizar agora".

Sort por coluna está fora de escopo (decisão A14 do UI-SPEC): os cabeçalhos não são clicáveis.
  </action>
  <acceptance_criteria>
    - `resources/js/Pages/Mlb/MeusAnuncios.jsx` contém as strings literais `precisam de você`, `Nenhum anúncio precisa de atenção agora`, `(um anúncio pode aparecer em mais de um motivo)`, `Não avaliado`, `de 86` e `Abrir no Mercado Livre`
    - `grep -c 'triagem.total' resources/js/Pages/Mlb/MeusAnuncios.jsx` é maior que 0, e não existe nenhuma soma dos `count` dos chips no cliente (`reduce` sobre `triagem.chips` não aparece no arquivo)
    - Não existe `/ 100` nem `* 100 / 86` no cálculo da barra da nota; o denominador literal `86` aparece
    - Nenhuma ocorrência de `linkAnuncioMl` no arquivo (o `permalink` vem pronto)
    - Nenhuma ocorrência de `SourceBadge` no arquivo
    - Nenhuma ocorrência das palavras `pausar`, `Pausar`, `em breve` ou `Em breve` no arquivo
    - `npm run build` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>A triagem responde "o que precisa de você e por quê" em um olhar, a tabela mostra as 8 colunas, e todo dado incerto está rotulado como incerto.</done>
</task>

<task type="auto">
  <name>Task 3: Gate estrutural do contrato de design + checkpoint visual</name>
  <files>tests/js/estrutura-meus-anuncios.test.js</files>
  <read_first>
    - `tests/js/estrutura-grade-glide.test.js` — arquivo inteiro: o molde de teste estrutural com `lerSemComentarios()`
    - `tests/js/_fonte.js` — o helper que remove comentários antes de assertar. **Obrigatório**: a prosa pt-BR deste projeto cita os próprios identificadores, e um gate cru contaria o comentário e passaria pelo motivo errado
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md` seções Typography, Spacing Scale e Color — o vocabulário fechado que o teste trava
    - `resources/js/Pages/Mlb/MeusAnuncios.jsx` — o arquivo sob teste
  </read_first>
  <action>
Criar `tests/js/estrutura-meus-anuncios.test.js`, usando `lerSemComentarios('resources/js/Pages/Mlb/MeusAnuncios.jsx')`. Seis grupos de asserção:

1. **Tipografia — 4 tamanhos, sem quinto.** Assere que a fonte não casa com `\btext-xs\b`, nem com `text-\[1[023]px\]` (pega 10px, 12px e 13px). Os únicos tamanhos aceitos no arquivo novo são `text-[11px]`, `text-sm`, `text-base` e `text-xl`.
2. **Peso — 2 pesos, sem terceiro.** Assere que a fonte não casa com `font-medium` nem com `font-bold`. O painel do wizard usa `font-bold` para o número do score, e esta fase declarou explicitamente que não herda esse terceiro peso.
3. **Espaçamento — múltiplos de 4, sem meio-passo.** Assere que a fonte não casa com o padrão de classe utilitária terminando em `.5` (por exemplo `px-2.5`, `gap-1.5`, `py-0.5`, `mt-0.5`). Escrever a expressão regular cobrindo os prefixos `p`, `px`, `py`, `pt`, `pb`, `pl`, `pr`, `m`, `mx`, `my`, `mt`, `mb`, `ml`, `mr`, `gap`, `space-x`, `space-y`, `w`, `h`.
4. **D-11 — tela só de leitura.** Assere que a fonte não contém `router.put`, `router.delete`, `router.patch`, e que o único `router.post` presente aponta para `meus.atualizar`. Nenhuma outra escrita sai desta tela.
5. **D-22 — base 86 preservada.** Assere que a fonte contém a string `de 86` e que **não** contém `/ 100` nem `* 100`. Se alguém renormalizar a nota, quebra aqui.
6. **Abas — ordem e destino.** Lendo `ModoAnuncioTabs.jsx` sem comentários, assere que `mlb.anuncios.meus` aparece **antes** de `mlb.anuncios.wizard` na fonte (a aba inicial é a primeira do array) e que as quatro rotas do módulo estão presentes.

Este arquivo é o gate de conformidade com o contrato de design aprovado, e é o que impede que o vocabulário volte a se fragmentar em sete tamanhos e três pesos, que foi exatamente o que as revisões 2 e 3 do UI-SPEC corrigiram.
  </action>
  <acceptance_criteria>
    - `node --test tests/js/estrutura-meus-anuncios.test.js` sai com código 0
    - `npm run test:js` sai com código 0
    - O teste 2 falha se um `font-medium` for inserido em `MeusAnuncios.jsx` — provado inserindo temporariamente e revertendo, com o resultado no SUMMARY
    - O teste 3 falha se um `px-2.5` for inserido em `MeusAnuncios.jsx` — provado inserindo temporariamente e revertendo, com o resultado no SUMMARY
    - `npm run build` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npm run test:js</automated>
  </verify>
  <done>Tipografia, peso, espaçamento, ausência de escrita e base 86 têm gate automatizado sobre a fonte do arquivo novo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Conferência visual da tela em ambiente local</name>
  <files>nenhum arquivo é modificado — conferência manual sobre o que as tasks 1 a 3 construíram</files>
  <what-built>
A aba "Meus Anúncios" como aba inicial do módulo, com sub-abas Publicados e Rascunhos, banner de defasagem, triagem acionável por motivo, tabela de 8 colunas ordenada por gravidade, selos de origem, nota em base 86, chip "Não avaliado" e botão "Atualizar agora".
  </what-built>
  <action>
Executar o roteiro de `<how-to-verify>` em ambiente local, na ordem, e registrar o resultado de cada um dos 6 passos no SUMMARY. Semear antes o dado de conferência descrito no passo 2 — o banco local não tem empresa com token ML. Nenhum arquivo de código é alterado nesta task: divergência encontrada vira correção na task correspondente, nunca conserto silencioso aqui.
  </action>
  <how-to-verify>
1. Rodar `npm run build`. Se a tela não aparecer no navegador, conferir se `public/hot` ficou órfão de um Vite dev encerrado — remover o arquivo, rebuildar e dar hard reload (armadilha já registrada no projeto).
2. O banco local não tem empresa com token ML. Semear dados de conferência: criar uma `Company` com `MlToken` (status `active`, `ml_user_id` qualquer) e algumas dezenas de linhas em `ml_acervo_itens` cobrindo, no mínimo: um item pausado, um com estoque zero, um com ficha incompleta, um com `catalog_listing` verdadeiro e `buybox_status` nulo, um com origem `ecf`, um com origem `time`, um com origem `legado`, e um saudável.
3. Abrir `/mlb/anuncios/meus/{company}` logado como admin. Conferir, nesta ordem:
   - As 4 abas aparecem na ordem Meus Anúncios, Individual, Em massa, Histórico, com a primeira ativa.
   - O número do heading da triagem é igual à quantidade de anúncios **distintos** com problema, e não à soma dos chips.
   - Clicar num chip filtra a tabela; clicar de novo limpa.
   - O item de catálogo sem buy box mostra "Não avaliado", e não um status inventado.
   - A nota aparece como "X de 86", nunca como percentual nem sobre 100.
   - Os três selos de origem aparecem com as três cores e os três tooltips.
   - Nenhum botão de pausar, editar ou mover anúncio existe em lugar nenhum da tela.
4. Envelhecer `coletado_em` das linhas (por exemplo, 3 dias atrás) e recarregar: o banner âmbar de defasagem precisa aparecer, com a lista **continuando visível** abaixo dele.
5. Clicar em "Atualizar agora" e confirmar que o botão vai para "Enfileirando…", volta com o flash de sucesso e fica em cooldown — sem a página congelar. Confirmar na fila que um `SyncMlAcervoCompanyJob` foi enfileirado.
6. Alternar o tema para modo claro e confirmar que a tela continua legível (o vocabulário de tokens usado já é coberto pelo `light.css`).
  </how-to-verify>
  <read_first>
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md` — o contrato visual completo, para comparar o que está na tela com o que foi aprovado
    - `resources/js/Pages/Mlb/MeusAnuncios.jsx` — a tela recém-construída
  </read_first>
  <acceptance_criteria>
    - Os 6 itens do roteiro foram executados e o resultado de cada um está registrado no SUMMARY
    - Nenhum controle de escrita sobre o anúncio foi encontrado na tela (D-11)
    - A nota aparece em base 86 em todos os lugares onde aparece (D-22)
    - O total da triagem bate com a contagem manual de anúncios distintos com problema (D-09)
  </acceptance_criteria>
  <verify>
    <human-check>Usuário confirma os 6 itens do roteiro, com atenção especial a: total da triagem batendo com a contagem manual de anúncios distintos com problema (D-09), nota exibida como "X de 86" (D-22), chip "Não avaliado" no item de catálogo sem buy box (D-18) e ausência total de controle de escrita (D-11).</human-check>
  </verify>
  <done>Os 6 itens do roteiro foram conferidos na tela real e o resultado de cada um está registrado no SUMMARY; o usuário aprovou explicitamente.</done>
  <resume-signal>Digitar "aprovado" ou descrever o que está diferente do contrato de design</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| props do servidor → DOM | Título, permalink e thumbnail vêm da API do Mercado Livre e são renderizados na tela |
| clique do usuário → rota POST | O único caminho de escrita da tela |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-20 | Tampering | Renderização de `titulo` e `permalink` vindos do ML | mitigate | React escapa texto por padrão; nenhum `dangerouslySetInnerHTML` no arquivo novo; `permalink` entra só em `href`, e o gate estrutural pode ser estendido para assertar ausência de `dangerouslySetInnerHTML` |
| T-134-04 | Tampering | Ações da tela | mitigate | Gate estrutural assere que não existe `router.put`, `router.delete` nem `router.patch`, e que o único `router.post` aponta para `meus.atualizar` (D-11) |
| T-134-01 | Information Disclosure | Filtros no cliente | mitigate | Todo filtro é round-trip ao servidor, onde o `where('company_id', ...)` é aplicado; nenhum dado de outra empresa chega ao navegador para ser filtrado depois |
| T-134-21 | Repudiation | Números da triagem calculados em dois lugares | mitigate | O total exibido é o `triagem.total` vindo do backend; o gate estrutural assere que não existe `reduce` sobre `triagem.chips` no cliente — uma conta paralela é justamente o que produz card que não fecha com a própria conta |
| T-134-SC | Tampering | npm install | accept | Nenhum pacote novo: só Tailwind com tokens `ecf-*`, primitivas já existentes em `Components/ui/` e `lucide-react`, todos já no `package.json` |
</threat_model>

<verification>
- `npm run build` verde
- `npm run test:js` verde
- `C:\xampp\php\php.exe artisan test --filter=Phase134` continua verde (nenhuma regressão de backend)
- Checkpoint visual aprovado pelo usuário
</verification>

<success_criteria>
1. O módulo abre em Meus Anúncios, com as 4 abas na ordem contratada (D-13).
2. A triagem é o primeiro conteúdo da sub-aba Publicados, o número fecha com a conta e clicar num motivo filtra a lista (D-09).
3. Coleta velha mostra banner âmbar com motivo e a lista continua visível (D-08).
4. A nota é exibida como "X de 86", nunca renormalizada (D-22).
5. Item de catálogo não coberto pela rotação aparece como "Não avaliado" (D-18).
6. Nenhum controle de escrita sobre o anúncio existe na tela (D-11).
</success_criteria>

<output>
Criar `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-08-SUMMARY.md` ao terminar.
</output>
