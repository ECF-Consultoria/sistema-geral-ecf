---
phase: 134-meus-anuncios-saude-analitica-do-anuncio-publicado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/mlb_acervo.php
  - app/Console/Commands/SondarAcervoMl.php
  - tests/Unit/Phase134/SondagemSaudeMlTest.php
  - tests/fixtures/phase134/scroll-pagina-1.json
  - tests/fixtures/phase134/scroll-pagina-2.json
  - tests/fixtures/phase134/scroll-pagina-3.json
  - tests/fixtures/phase134/multiget-lote.json
  - tests/fixtures/phase134/multiget-item-com-variacoes.json
  - tests/fixtures/phase134/price-to-win.json
  - tests/fixtures/phase134/visits.json
  - tests/fixtures/phase134/performance-sondagem.json
autonomous: true
requirements: [D-11, D-18, D-21, D-23]

must_haves:
  truths:
    - "A pergunta do D-21 tem resposta registrada: `/item/{id}/performance` responde ou não para item clássico deste app"
    - "A fase inteira tem um único lugar para ler N da rotação, retenção, limiar de defasagem e a variante de saúde do ML"
    - "Os testes da fase têm payload REAL da API do ML como fixture, não shape inventado"
  artifacts:
    - path: "config/mlb_acervo.php"
      provides: "Parâmetros da fase (rotacao_n, chunk_detalhe, retencao_dias, defasagem_horas, saude_ml_disponivel)"
      contains: "saude_ml_disponivel"
    - path: "app/Console/Commands/SondarAcervoMl.php"
      provides: "Comando read-only de sondagem e captura de fixtures"
      contains: "mlb:acervo-sondar"
    - path: "tests/fixtures/phase134/multiget-lote.json"
      provides: "Corpo real de GET /items?ids= para Http::fake()"
  key_links:
    - from: "app/Console/Commands/SondarAcervoMl.php"
      to: "app/Services/MercadoLivreService.php"
      via: "somente $ml->get() — nenhum post/put/delete"
      pattern: "\\$ml->get\\("
---

<objective>
Responder à pergunta que o D-21 deixou aberta (o Mercado Livre ainda expõe um score de saúde comparável para item clássico deste app?) e deixar a fase com dois insumos que todos os planos seguintes consomem: um arquivo de configuração com os parâmetros de operação e um conjunto de fixtures capturadas da API real.

Purpose: sem a sondagem, a coluna "Saúde" da tela sai por adivinhação — e o D-21 proíbe simular número. Sem fixture real, os testes das waves seguintes validariam um shape inventado (a pesquisa já provou que a documentação do ML mente em pelo menos um ponto).
Output: `config/mlb_acervo.php`, comando `mlb:acervo-sondar` (somente leitura), 8 fixtures JSON e o teste que prova que o caminho de sondagem não escreve na API do ML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-CONTEXT.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-RESEARCH.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-VALIDATION.md
@app/Services/MercadoLivreService.php
@app/Console/Commands/SyncMlData.php
@deploy.sh

<interfaces>
<!-- Contrato de `config/mlb_acervo.php`. Todos os planos seguintes leem daqui. -->
<!-- Valores decididos pelo planner a partir do 134-RESEARCH.md §A-03/§3/§4. -->

```php
return [
    // D-23 — cobertura completa da camada cara em N dias. Default 7.
    // Conta: 406.932 itens ativos + ~180 mil chamadas de price_to_win = ~587 mil
    // chamadas/dia se fosse cobertura total diária (~6,5 h a 25 req/s). Com N=7 cai
    // para ~84 mil/dia (~56 min), na mesma ordem de grandeza da janela do ml:sync.
    'rotacao_n' => (int) env('MLB_ACERVO_ROTACAO_N', 7),

    // Tamanho do lote de itens por SyncMlAcervoDetalheJob. Mantém cada job em
    // dezenas de segundos, muito abaixo do retry_after da fila (Pitfall 1 do RESEARCH).
    'chunk_detalhe' => (int) env('MLB_ACERVO_CHUNK_DETALHE', 500),

    // Multiget de itens: 20 ids é o teto REAL da API (erro "only allows 20 elements").
    'lote_multiget' => 20,

    // Página do scroll (search_type=scan). 50 mantém o round-trip curto — o scroll_id
    // tem TTL de 5 min e a enumeração roda sem delay artificial.
    'pagina_scroll' => 50,

    // D-07 — retenção da série diária.
    'retencao_dias' => (int) env('MLB_ACERVO_RETENCAO_DIAS', 90),

    // D-08 — a partir de quantas horas sem coleta a tela mostra o banner de defasagem.
    'defasagem_horas' => (int) env('MLB_ACERVO_DEFASAGEM_HORAS', 24),

    // D-21 — veredicto da sondagem. false = Variante B do 134-UI-SPEC (só Nota ECF).
    // Só vira true se a Task 2 deste plano provar que o endpoint responde.
    'saude_ml_disponivel' => (bool) env('MLB_ACERVO_SAUDE_ML', false),
];
```

<!-- Assinatura do comando de sondagem -->
```
mlb:acervo-sondar {--company= : ID da Company} {--item= : MLB id específico} {--fixtures : grava as fixtures em tests/fixtures/phase134/}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config da fase + comando de sondagem read-only</name>
  <files>config/mlb_acervo.php, app/Console/Commands/SondarAcervoMl.php</files>
  <read_first>
    - `app/Services/MercadoLivreService.php` — assinaturas de `get(Company, string, array, array)`, `ensureValidToken()`, `comRetry429()`, constante `API_BASE`, e o padrão de `fetchItemStatus()` (fail-open, nunca loga `access_token`)
    - `app/Console/Commands/SyncMlData.php` — forma de `protected $signature` com opções nomeadas, `whereHas('mlToken', fn($q) => $q->where('status','active'))`, e o retorno `self::SUCCESS`
    - `app/Models/MlToken.php` — coluna `ml_user_id` (é ela que entra em `/users/{id}/items/search`)
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-RESEARCH.md` seções §1, §2, §A-02 e `## Code Examples` — endpoints, parâmetros e formato de resposta já confirmados por chamada real
  </read_first>
  <action>
Criar `config/mlb_acervo.php` exatamente com as 8 chaves e os defaults do bloco `<interfaces>` acima, com os comentários pt-BR que justificam cada número (o comentário do `rotacao_n` precisa citar a conta de 406.932 itens e ~587 mil chamadas). Nenhuma outra chave.

Criar `app/Console/Commands/SondarAcervoMl.php` — comando `mlb:acervo-sondar`, classe `SondarAcervoMl`, com as opções `--company=`, `--item=` e `--fixtures`. Ele é o único ponto desta fase que fala com a API real, e é estritamente de leitura: toda chamada sai por `MercadoLivreService::get()`; é proibido usar `post()`, `put()`, `delete()` ou `Http::` direto neste arquivo (D-11).

Resolução da empresa: se `--company=` vier, usa aquela `Company`; senão pega a primeira `Company` com `mlToken` de `status = active`, seguindo o mesmo `whereHas` do `SyncMlData`. Se nenhuma existir, o comando imprime que não há token ML neste ambiente e retorna `self::FAILURE` — não inventa dado.

O comando executa, nesta ordem, e imprime um relatório de uma linha por passo:
1. Enumeração por scroll: `GET /users/{ml_user_id}/items/search` com `search_type=scan` e `limit` = `config('mlb_acervo.pagina_scroll')`, repetindo por 3 páginas, carregando o `scroll_id` da resposta anterior. Reporta quantos ids vieram e se houve id repetido entre páginas.
2. Multiget: `GET /items` com `ids` = os 20 primeiros ids separados por vírgula e `attributes` = a lista do `## Code Examples` do RESEARCH (`id,status,sub_status,available_quantity,sold_quantity,shipping,listing_type_id,tags,catalog_listing,catalog_product_id,variations,title,price,permalink,thumbnail,category_id,attributes,pictures`). Reporta quantos vieram com `code` 200.
3. Seleção do item da sondagem D-21: entre os itens do multiget, escolhe o primeiro com `catalog_listing` falso e **sem** `user_product_id` no payload — é o item "clássico" que a pesquisa não conseguiu testar. Se `--item=` vier, usa esse id direto.
4. Sondagem D-21 propriamente dita: `GET /item/{id}/performance` (singular). Registra status HTTP e corpo. Trata exceção com `try/catch (\Throwable)` e segue — falha de sondagem é resultado válido, não crash.
5. Buy box: `GET /items/{id}/price_to_win` com `version=v2` num item com `catalog_listing` verdadeiro, se houver algum no lote.
6. Visitas: `GET /items/{id}/visits` com `date_from`/`date_to` cobrindo os últimos 30 dias, para 1 item.

Ao final imprime o VEREDICTO em uma linha começando com `VEREDICTO D-21:` seguido de `DISPONIVEL` ou `INDISPONIVEL`, e a instrução de qual valor colocar em `MLB_ACERVO_SAUDE_ML`.

Com `--fixtures`, grava os corpos recebidos em `tests/fixtures/phase134/` com os nomes exatos: `scroll-pagina-1.json`, `scroll-pagina-2.json`, `scroll-pagina-3.json`, `multiget-lote.json`, `multiget-item-com-variacoes.json` (o primeiro item do lote cujo `variations` não seja vazio; se nenhum tiver, buscar mais um lote até achar), `price-to-win.json`, `visits.json`, `performance-sondagem.json` (corpo ou o erro capturado). Grava JSON identado.

Log com a tag do módulo: `Log::info("[MLB Anuncios] ...")`. Nunca imprimir nem logar `access_token` — só `company_id` e o MLB id, seguindo o padrão já documentado em `MercadoLivreService::fetchItemStatus()`.
  </action>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan list --format=json` inclui o comando `mlb:acervo-sondar`
    - `config/mlb_acervo.php` retorna array com exatamente as chaves `rotacao_n`, `chunk_detalhe`, `lote_multiget`, `pagina_scroll`, `retencao_dias`, `defasagem_horas`, `saude_ml_disponivel` — provado por `C:\xampp\php\php.exe artisan tinker --execute="echo implode(',', array_keys(config('mlb_acervo')));"`
    - `config('mlb_acervo.rotacao_n')` devolve `7` e `config('mlb_acervo.saude_ml_disponivel')` devolve `false` sem env definida
    - `grep -v '^\s*//' app/Console/Commands/SondarAcervoMl.php | grep -cE '->(post|put|delete)\(' ` devolve `0`
    - `C:\xampp\php\php.exe -l app/Console/Commands/SondarAcervoMl.php` sai sem erro
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase134</automated>
  </verify>
  <done>Config existe com os 7 parâmetros decididos; comando registrado no artisan; nenhuma chamada de escrita à API do ML na fonte do comando.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar a sondagem contra produção e capturar as fixtures reais</name>
  <files>config/mlb_acervo.php, tests/fixtures/phase134/scroll-pagina-1.json, tests/fixtures/phase134/scroll-pagina-2.json, tests/fixtures/phase134/scroll-pagina-3.json, tests/fixtures/phase134/multiget-lote.json, tests/fixtures/phase134/multiget-item-com-variacoes.json, tests/fixtures/phase134/price-to-win.json, tests/fixtures/phase134/visits.json, tests/fixtures/phase134/performance-sondagem.json</files>
  <read_first>
    - `deploy.sh` — de onde vêm `VPS_HOST`, `VPS_USER`, `SSH_ARGS`, `PLINK` e a variável `REMOTE` (o diretório da aplicação na VPS). **Não duplicar credencial dentro do plano nem do commit** — reusar as variáveis do próprio `deploy.sh`
    - `app/Console/Commands/SondarAcervoMl.php` — o comando criado na Task 1
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-CONTEXT.md` seção `<specifics>` — a autorização explícita do usuário para gastar chamada de leitura em produção durante o desenvolvimento
  </read_first>
  <action>
O banco local tem **zero** empresas com token ML (registrado no `134-RESEARCH.md` § Environment Availability), então a sondagem só produz resposta contra produção. Este é o único ponto da fase que toca a API real, e o usuário autorizou explicitamente leituras em produção (CONTEXT.md `<specifics>`, 2026-08-10).

Copiar o comando para a VPS e executá-lo por lá, ou — preferencialmente, para não subir arquivo solto (o `reset --hard` do próximo deploy apagaria) — commitar a Task 1 primeiro e executar na VPS com o código já versionado. O caminho concreto: abrir sessão com `plink` reusando `SSH_ARGS` do `deploy.sh`, entrar em `$REMOTE` e rodar `php artisan mlb:acervo-sondar --fixtures`. Trazer os 8 arquivos de `tests/fixtures/phase134/` de volta com `pscp` e commitá-los no repositório local.

Registrar o veredicto:
- Se a saída trouxer `VEREDICTO D-21: DISPONIVEL`, trocar o default de `saude_ml_disponivel` em `config/mlb_acervo.php` para `true` e anotar no comentário da chave a data da sondagem, o MLB id sondado e o formato do campo de saúde devolvido. A fase segue com a **Variante A** do `134-UI-SPEC.md` seção 8.
- Se trouxer `INDISPONIVEL`, manter `false` e anotar no comentário da chave a data, o MLB id sondado e a mensagem de erro literal da API. A fase segue com a **Variante B** — só Nota ECF, com o tooltip único no cabeçalho da coluna (copy já fechada no `134-UI-SPEC.md`, seção Copywriting).
- Se o acesso à VPS não estiver disponível na execução, manter `false` (Variante B, o fallback honesto contratado pelo D-21) e registrar no SUMMARY que a sondagem ficou pendente, com o comando exato para repeti-la. **Em nenhuma hipótese preencher a coluna de saúde do ML com número simulado, aproximado ou derivado da Nota ECF.**

As duas variantes estão contratadas pelo UI-SPEC: nenhum plano posterior fica bloqueado pelo resultado — o plano 134-06 lê `config('mlb_acervo.saude_ml_disponivel')` e o 134-08 renderiza a variante correspondente.

Se qualquer fixture vier vazia ou com erro, gravar mesmo assim o corpo real recebido — fixture que documenta o erro real vale mais que fixture inventada. Registrar no SUMMARY qual conta e quais MLB ids geraram cada fixture.
  </action>
  <acceptance_criteria>
    - Os 8 arquivos existem em `tests/fixtures/phase134/` e todos são JSON válido — provado por `node -e "require('fs').readdirSync('tests/fixtures/phase134').forEach(f=>JSON.parse(require('fs').readFileSync('tests/fixtures/phase134/'+f)))"` saindo com código 0
    - `tests/fixtures/phase134/multiget-lote.json` decodifica para um array cujos elementos têm as chaves `code` e `body`, e ao menos um `body` tem as chaves `status`, `available_quantity`, `catalog_listing`, `listing_type_id`, `permalink`
    - `tests/fixtures/phase134/multiget-item-com-variacoes.json` decodifica para um objeto com `variations` não vazio e com `available_quantity` presente no nível do item-pai (é o agregado do D-17)
    - As 3 fixtures de scroll têm `scroll_id` diferentes entre si e nenhum id de `results` repetido entre as páginas
    - O comentário da chave `saude_ml_disponivel` em `config/mlb_acervo.php` cita a data da sondagem e o MLB id sondado
    - O SUMMARY do plano contém a linha literal do `VEREDICTO D-21:` produzida pelo comando (ou a justificativa de sondagem pendente)
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase134</automated>
  </verify>
  <done>O D-21 tem resposta escrita e rastreável; a fase sabe se sai na Variante A ou B; os testes das waves seguintes têm payload real para `Http::fake()`.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de zero-write e de contrato da config</name>
  <files>tests/Unit/Phase134/SondagemSaudeMlTest.php</files>
  <read_first>
    - `tests/Feature/Phase86/HistoricoAnunciosTest.php` — convenção de `@group`, uso de `Http::fake()` no `setUp()` e nomes de teste em snake_case pt-BR
    - `app/Console/Commands/SondarAcervoMl.php` — o alvo do teste
    - `config/mlb_acervo.php` — as chaves e defaults que o teste trava
  </read_first>
  <action>
Criar `tests/Unit/Phase134/SondagemSaudeMlTest.php` com `@group phase134`. Três testes:

1. `config_da_fase_tem_os_parametros_decididos()` — assere que `config('mlb_acervo')` tem exatamente as 7 chaves e que `rotacao_n` é 7, `chunk_detalhe` é 500, `lote_multiget` é 20, `pagina_scroll` é 50, `retencao_dias` é 90 e `defasagem_horas` é 24. Este teste é o que impede alguém de mudar N silenciosamente depois — se o número mudar, o teste força a mudança a ser deliberada.

2. `comando_de_sondagem_nunca_escreve_na_api_do_ml()` — lê a fonte de `app/Console/Commands/SondarAcervoMl.php`, remove as linhas de comentário (`^\s*//` e `^\s*\*`) antes de contar, e assere que não há nenhuma ocorrência de `->post(`, `->put(`, `->delete(`, `->patch(` nem de `Http::` na fonte restante. O filtro de comentário é obrigatório: a prosa pt-BR deste projeto cita os próprios identificadores, e um gate por `grep` cru contaria o comentário e passaria pelo motivo errado.

3. `fixtures_da_api_real_existem_e_sao_json_valido()` — assere que os 8 arquivos de `tests/fixtures/phase134/` existem, que cada um decodifica com `json_decode(..., true)` sem erro, e que `multiget-item-com-variacoes.json` tem `variations` não vazio e `available_quantity` no nível do item. Se as fixtures ainda não existirem porque a Task 2 ficou pendente, este teste falha — e é para falhar mesmo: fixture ausente é lacuna de Wave 0, não detalhe.
  </action>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan test --filter=SondagemSaudeMl` sai com código 0 e roda 3 testes
    - O teste 2 falha se alguém inserir `->post(` fora de comentário na fonte do comando — provado alterando o arquivo temporariamente e revertendo, com o resultado registrado no SUMMARY
    - `tests/Unit/Phase134/SondagemSaudeMlTest.php` contém a anotação `@group phase134`
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase134</automated>
  </verify>
  <done>Os parâmetros da fase estão travados por teste, o comando de sondagem está provado como somente-leitura e as fixtures reais estão versionadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aplicação → API do Mercado Livre | Sai token OAuth do cliente; qualquer verbo de escrita muda a conta real de um cliente em produção |
| máquina de desenvolvimento → VPS de produção | Sessão SSH com credencial; comando executado lá roda com dado real de 74 empresas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-04 | Tampering | `SondarAcervoMl` contra `api.mercadolibre.com` | mitigate | Comando usa exclusivamente `MercadoLivreService::get()`; teste `comando_de_sondagem_nunca_escreve_na_api_do_ml()` assere ausência de `->post(`/`->put(`/`->delete(`/`->patch(`/`Http::` na fonte sem comentários (D-11) |
| T-134-03 | Information Disclosure | Log e stdout do comando | mitigate | Só `company_id` e MLB id vão para `Log::info("[MLB Anuncios] ...")` e para a saída; `access_token` nunca é impresso nem logado — mesmo padrão de `MercadoLivreService::fetchItemStatus()` |
| T-134-05 | Information Disclosure | Fixtures versionadas em `tests/fixtures/phase134/` | mitigate | Antes de commitar, conferir que nenhum corpo capturado contém `access_token`, `refresh_token` ou dado pessoal de comprador; as respostas usadas são de `/items`, `/price_to_win` e `/visits`, que não carregam credencial |
| T-134-06 | Elevation of Privilege | Credencial da VPS em `deploy.sh` | accept | Credencial já vive versionada em `deploy.sh` (estado pré-existente do repositório); este plano reusa as variáveis existentes e **não** duplica o segredo em arquivo novo, plano ou mensagem de commit |
| T-134-SC | Tampering | npm/composer install | accept | Esta fase inteira não instala nenhum pacote — `package.json` e `composer.json` ficam intactos. Gate de legitimidade de pacote não se aplica |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=Phase134` verde
- `C:\xampp\php\php.exe artisan list` mostra `mlb:acervo-sondar`
- Os 8 arquivos de `tests/fixtures/phase134/` existem, são JSON válido e vieram de chamada real
- O veredicto do D-21 está escrito no comentário de `config/mlb_acervo.php` e no SUMMARY
</verification>

<success_criteria>
1. O D-21 deixou de ser pergunta aberta: existe um veredicto escrito, datado e rastreável ao MLB id sondado.
2. `config('mlb_acervo')` é o único lugar da fase onde N da rotação, retenção, limiar de defasagem e a variante de saúde do ML vivem — e os valores estão travados por teste.
3. Nenhum verbo de escrita saiu para `api.mercadolibre.com`, provado por teste sobre a fonte.
4. As waves seguintes têm payload real do ML para `Http::fake()`.
</success_criteria>

<output>
Criar `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-01-SUMMARY.md` ao terminar, incluindo a linha literal do `VEREDICTO D-21:` e a lista de conta/MLB ids que geraram cada fixture.
</output>
