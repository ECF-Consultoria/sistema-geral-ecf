---
phase: 134-meus-anuncios-saude-analitica-do-anuncio-publicado
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_08_11_090000_create_ml_acervo_itens_table.php
  - database/migrations/2026_08_11_090100_create_ml_acervo_metricas_diarias_table.php
  - app/Models/MlAcervoItem.php
  - app/Models/MlAcervoMetricaDiaria.php
  - tests/Unit/Phase134/SchemaAcervoTest.php
autonomous: true
requirements: [D-04, D-07, D-08, D-12, D-17, D-18]

must_haves:
  truths:
    - "Existe uma linha corrente por anúncio, por empresa, com chave de upsert única"
    - "Existe uma série diária enxuta capaz de responder 'como este anúncio evoluiu'"
    - "Anúncio com variação ocupa UMA linha, com o agregado que o próprio ML devolve"
    - "'Não avaliado' é representável no schema — buy box nunca nasce com valor inventado"
  artifacts:
    - path: "database/migrations/2026_08_11_090000_create_ml_acervo_itens_table.php"
      provides: "Tabela ml_acervo_itens com índices nomeados à mão"
      contains: "mai_company_item_unq"
    - path: "database/migrations/2026_08_11_090100_create_ml_acervo_metricas_diarias_table.php"
      provides: "Série diária com PK lógica (company_id, ml_item_id, data)"
      contains: "mamd_company_item_data_unq"
    - path: "app/Models/MlAcervoItem.php"
      provides: "Model com casts de json/datetime e as constantes de origem, motivo e severidade"
      contains: "ORIGEM_LEGADO"
  key_links:
    - from: "app/Models/MlAcervoItem.php"
      to: "ml_acervo_itens"
      via: "protected $table + casts"
      pattern: "protected \\$table\\s*=\\s*'ml_acervo_itens'"
---

<objective>
Criar as duas tabelas que sustentam a fase inteira — a linha corrente por anúncio (que a tela lê) e a série diária enxuta (que responde "como isso evoluiu") — mais os dois models com as constantes de domínio que PHP e JSX vão espelhar.

Purpose: o D-05 trava que a tela lê exclusivamente do banco; sem schema, nada mais da fase existe. O D-07 diz textualmente que retenção e índices são requisito, não polimento — a tabela corrente vai para a ordem de 500 mil linhas e a série diária para dezenas de milhões.
Output: 2 migrations com índices nomeados manualmente, 2 models e o teste que trava o contrato do schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-CONTEXT.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-PATTERNS.md
@.planning/learnings/desempenho-bonificacao.md
@database/migrations/2026_07_10_120000_create_ml_anuncio_rascunhos_table.php
@database/migrations/2026_08_03_120000_create_desempenho_company_score_snapshots_table.php
@app/Models/MlAnuncioRascunho.php

<interfaces>
<!-- Contrato de colunas de `ml_acervo_itens`. Decidido pelo planner a partir do -->
<!-- inventário de campos do 134-RESEARCH.md §1 e da tabela de sinais do §5. -->

```
id                          bigIncrements
company_id                  foreignId, constrained, cascadeOnDelete
ml_item_id                  string(20)              MLBxxxxxxxxxx
title                       string(512) nullable
category_id                 string(32)  nullable
status                      string(20)  nullable    active | paused | closed | under_review
sub_status                  json        nullable
listing_type_id             string(32)  nullable    gold_special = Clássico | gold_pro = Premium
price                       decimal(12,2) nullable
available_quantity          integer     nullable    D-17: agregado que o ML já devolve no item-pai
sold_quantity               integer     nullable    vitalício, não "vendas do dia"
permalink                   string(512) nullable    já vem pronto da API — não reconstruir
thumbnail                   string(512) nullable
fotos_count                 unsignedInteger default 0
has_variations              boolean     default false
variations                  json        nullable    payload bruto — mesma filosofia de ml_anuncio_rascunhos.payload
catalog_listing             boolean     default false
catalog_product_id          string(32)  nullable
shipping                    json        nullable
tags                        json        nullable
nota_ecf                    unsignedTinyInteger nullable   D-22: 0..86, NUNCA renormalizada para 100
nota_sinais                 json        nullable          breakdown que TEM que somar nota_ecf
motivos                     json        nullable          ['pausado','sem_estoque','ficha_incompleta','perdendo_catalogo','foto_insuficiente']
severidade                  unsignedTinyInteger default 0  2=crítico, 1=atenção, 0=saudável/não avaliado
origem                      string(8)   default 'legado'   ecf | time | legado
rascunho_id                 unsignedBigInteger nullable     id em ml_anuncio_rascunhos (sem FK — ver nota)
publicacao_vendas_qty       integer     nullable            snapshot de Publicacao.vendas_qty
publicacao_desconsiderado   boolean     default false       snapshot de Publicacao.desconsiderado
visitas_30d                 unsignedInteger nullable        camada cara — null = não avaliado (D-18)
buybox_status               string(24)  nullable            winning|sharing_first_place|losing|listed — null = não avaliado (D-18)
detalhe_coletado_em         timestamp   nullable            frescor PRÓPRIO da camada cara (D-08 nível de campo)
coletado_em                 timestamp   nullable            frescor da camada barata
coleta_erro                 string(255) nullable            motivo da última falha (D-08 banner)
timestamps
```

Índices nomeados à mão (prefixo `mai_`):
```
unique(['company_id','ml_item_id'],            'mai_company_item_unq')
index (['company_id','status'],                'mai_company_status_idx')
index (['company_id','severidade','nota_ecf'], 'mai_company_sev_nota_idx')
index (['company_id','detalhe_coletado_em'],   'mai_company_detalhe_idx')
```

<!-- Contrato de colunas de `ml_acervo_metricas_diarias` -->
```
id                bigIncrements
company_id        foreignId, constrained, cascadeOnDelete
ml_item_id        string(20)
data              date
visitas           unsignedInteger nullable      FLUXO — buraco é ausência real de coleta
sold_quantity     unsignedInteger nullable      ESTADO — cumulativo
nota_ecf          unsignedTinyInteger nullable  ESTADO
created_at        timestamp nullable            sem updated_at: a linha nunca é atualizada
```

Índices nomeados à mão (prefixo `mamd_`):
```
unique(['company_id','ml_item_id','data'], 'mamd_company_item_data_unq')
index (['data'],                            'mamd_data_idx')
```

<!-- Constantes de domínio de `MlAcervoItem` (public const, espelhadas em JS depois) -->
```php
public const ORIGEM_ECF    = 'ecf';
public const ORIGEM_TIME   = 'time';
public const ORIGEM_LEGADO = 'legado';

public const MOTIVO_PAUSADO           = 'pausado';
public const MOTIVO_SEM_ESTOQUE       = 'sem_estoque';
public const MOTIVO_FICHA_INCOMPLETA  = 'ficha_incompleta';
public const MOTIVO_PERDENDO_CATALOGO = 'perdendo_catalogo';
public const MOTIVO_FOTO_INSUFICIENTE = 'foto_insuficiente';

public const SEVERIDADE_SAUDAVEL = 0;
public const SEVERIDADE_ATENCAO  = 1;
public const SEVERIDADE_CRITICA  = 2;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations das duas tabelas, com índices nomeados à mão</name>
  <files>database/migrations/2026_08_11_090000_create_ml_acervo_itens_table.php, database/migrations/2026_08_11_090100_create_ml_acervo_metricas_diarias_table.php</files>
  <read_first>
    - `database/migrations/2026_07_10_120000_create_ml_anuncio_rascunhos_table.php` — forma da migration de módulo MLB (classe anônima, `up()`/`down()`, `foreignId('company_id')->constrained()->cascadeOnDelete()`, `json` para payload)
    - `database/migrations/2026_08_03_120000_create_desempenho_company_score_snapshots_table.php` linhas 94-99 — o padrão de índice com nome explícito abreviado, que é exatamente o que evita o erro 1059 do MariaDB
    - `database/migrations/2026_05_22_100001_create_mlb_sync_vendas_logs_table.php` — comentário pt-BR no topo explicando o propósito da tabela
    - `.planning/learnings/desempenho-bonificacao.md` item 6 — as três armadilhas de MariaDB que o SQLite dos testes não pega
  </read_first>
  <action>
Criar as duas migrations com exatamente as colunas e os nomes de índice do bloco `<interfaces>` acima. Cada arquivo começa com um comentário pt-BR de 3 a 5 linhas explicando o papel da tabela na fase, citando a decisão que a exige (D-07 para as duas, D-17 para `has_variations`/`variations`, D-18 para `buybox_status` nullable).

Nome da tabela da série: `ml_acervo_metricas_diarias`, não `ml_acervo_item_metricas_diarias` como o `134-PATTERNS.md` sugeriu. Divergência deliberada e documentada no comentário do arquivo: encurtar o nome dá 6 caracteres de folga a mais contra o teto de 64 do MariaDB. Os índices são nomeados manualmente de qualquer forma, mas a folga é gratuita.

`rascunho_id` é `unsignedBigInteger` **sem** `foreignId`/`constrained`: uma FK com `nullOnDelete` exigiria coluna `nullable()` (erro 1830 já documentado no learning) e criaria dependência de ordem de drop entre `ml_anuncio_rascunhos` e a tabela nova. A ligação é lógica e o `origem` já registra o vínculo semanticamente. Documentar essa escolha em comentário na própria linha.

Nenhuma coluna `enum` — `status`, `origem` e `buybox_status` são `string` com constante no model. Enum em migration exige branch de SQLite e quebra os testes (learning item 6).

`ml_acervo_metricas_diarias` não tem `updated_at`: a linha é imutável depois de escrita. Usar `$table->timestamp('created_at')->nullable()` em vez de `$table->timestamps()`.

`down()` das duas migrations faz `Schema::dropIfExists()` da respectiva tabela. A da série cai primeiro na ordem de rollback por ser a mais nova — não há FK entre elas, então não há ordem obrigatória, mas manter a convenção.
  </action>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan migrate --pretend` lista as duas migrations sem erro de sintaxe
    - Nenhuma chamada a `->index(` ou `->unique(` nas duas migrations sem segundo argumento — provado por `grep -v '^\s*//' database/migrations/2026_08_11_09*.php | grep -cE '->(index|unique)\([^)]*\)\s*;' ` retornando 0 para o padrão sem vírgula-nome
    - Os 6 nomes de índice `mai_company_item_unq`, `mai_company_status_idx`, `mai_company_sev_nota_idx`, `mai_company_detalhe_idx`, `mamd_company_item_data_unq`, `mamd_data_idx` aparecem literalmente nos arquivos e todos têm 64 caracteres ou menos
    - Nenhuma ocorrência de `->enum(` nas duas migrations
    - `C:\xampp\php\php.exe -l` sai sem erro nos dois arquivos
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=SchemaAcervo</automated>
  </verify>
  <done>As duas tabelas nascem com índice nomeado à mão, sem enum e sem FK que exija ordem de drop.</done>
</task>

<task type="auto">
  <name>Task 2: Models MlAcervoItem e MlAcervoMetricaDiaria</name>
  <files>app/Models/MlAcervoItem.php, app/Models/MlAcervoMetricaDiaria.php</files>
  <read_first>
    - `app/Models/MlAnuncioRascunho.php` — forma exata do model do módulo: `protected $table`, bloco de constantes com divisor `// ─── ... ───`, `$fillable`, `$casts` com `'payload' => 'array'` e `'published_at' => 'datetime'`, relação `company(): BelongsTo`
    - `app/Models/MlbSyncVendasLog.php` — model de série/log simples, sem trait de activity log
    - `app/Models/Publicacao.php` — de onde vêm `vendas_qty` e `desconsiderado`, e o scope `considerado()` (linha 171) que **não** entra na listagem desta fase (D-04)
  </read_first>
  <action>
Criar `app/Models/MlAcervoItem.php` (`protected $table = 'ml_acervo_itens'`) com as 11 constantes do bloco `<interfaces>` acima (3 de origem, 5 de motivo, 3 de severidade), agrupadas sob divisores pt-BR. Cada bloco de constantes leva um comentário curto citando a decisão que o define — D-04 para origem, D-09 para motivo, D-12 para severidade.

`$fillable` cobre todas as colunas exceto `id` e timestamps. `$casts`: `sub_status`, `variations`, `shipping`, `tags`, `motivos`, `nota_sinais` como `array`; `has_variations`, `catalog_listing`, `publicacao_desconsiderado` como `boolean`; `price` como `decimal:2`; `available_quantity`, `sold_quantity`, `fotos_count`, `nota_ecf`, `severidade`, `visitas_30d`, `publicacao_vendas_qty` como `integer`; `coletado_em` e `detalhe_coletado_em` como `datetime`.

Relação `company(): BelongsTo` para `Company::class`. Relação `metricas(): HasMany` para `MlAcervoMetricaDiaria::class`, casando por `company_id` **e** `ml_item_id` não é expressável em `hasMany` simples — usar `hasMany(MlAcervoMetricaDiaria::class, 'ml_item_id', 'ml_item_id')` com um comentário avisando que quem consome precisa filtrar por `company_id` também, ou omitir a relação e deixar a consulta explícita no service de detalhe. Escolher a segunda opção (não declarar a relação) e registrar em comentário o motivo — uma relação que mente sobre escopo é pior que nenhuma relação, e o escopo por empresa é justamente o que o T-134-01 protege.

Adicionar dois helpers de leitura, ambos puros e sem query:
- `naoAvaliadoBuyBox(): bool` — devolve verdadeiro quando `catalog_listing` é verdadeiro e `buybox_status` é nulo. É a materialização do D-18: item de catálogo ainda não coberto pela rotação. Item que não é de catálogo devolve falso (não é "não avaliado", é "não se aplica").
- `visitasNaoAvaliadas(): bool` — devolve verdadeiro quando `detalhe_coletado_em` é nulo.

Criar `app/Models/MlAcervoMetricaDiaria.php` (`protected $table = 'ml_acervo_metricas_diarias'`), `public $timestamps = false` com `const CREATED_AT`/`UPDATED_AT` tratados, ou `$timestamps = true` com `const UPDATED_AT = null` — escolher a forma que o Laravel 12 aceita e que grava só `created_at`. `$fillable` com as 6 colunas de dado. `$casts`: `data` como `date`, `visitas`/`sold_quantity`/`nota_ecf` como `integer`.

Nenhum dos dois models usa o trait `LogsActivity`: são tabelas de snapshot alimentadas por job, e registrar cada upsert no `activity_log` inflaria a auditoria em centenas de milhares de linhas por dia sem nenhum ganho de rastreabilidade — documentar essa escolha em comentário de classe.
  </action>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan tinker --execute="echo App\Models\MlAcervoItem::ORIGEM_LEGADO . ',' . App\Models\MlAcervoItem::MOTIVO_PERDENDO_CATALOGO . ',' . App\Models\MlAcervoItem::SEVERIDADE_CRITICA;"` imprime `legado,perdendo_catalogo,2`
    - `app/Models/MlAcervoItem.php` contém `protected $table = 'ml_acervo_itens'` e as 11 constantes públicas
    - `app/Models/MlAcervoItem.php` contém `public function naoAvaliadoBuyBox(): bool` e `public function visitasNaoAvaliadas(): bool`
    - Nenhuma ocorrência de `LogsActivity` nos dois models
    - `C:\xampp\php\php.exe -l` sai sem erro nos dois arquivos
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=SchemaAcervo</automated>
  </verify>
  <done>Os dois models existem com casts corretos e as constantes de domínio como fonte única para o PHP.</done>
</task>

<task type="auto">
  <name>Task 3: Teste de contrato do schema, incluindo o gate de nome de índice</name>
  <files>tests/Unit/Phase134/SchemaAcervoTest.php</files>
  <read_first>
    - `tests/Feature/Phase86/HistoricoAnunciosTest.php` — `use RefreshDatabase;`, `@group`, nomes de teste em snake_case pt-BR
    - `database/migrations/2026_08_11_090000_create_ml_acervo_itens_table.php` e `..._090100_create_ml_acervo_metricas_diarias_table.php` — os arquivos sob teste
    - `.planning/learnings/desempenho-bonificacao.md` item 6 — por que o gate de nome de índice precisa ser um teste sobre a FONTE da migration, e não sobre o banco: o SQLite dos testes aceita nomes que o MariaDB recusa
  </read_first>
  <action>
Criar `tests/Unit/Phase134/SchemaAcervoTest.php` com `@group phase134` e `use RefreshDatabase;`. Quatro testes:

1. `tabela_de_itens_tem_as_colunas_contratadas()` — usa `Schema::hasColumns('ml_acervo_itens', [...])` com a lista completa de 32 colunas do bloco `<interfaces>`. Falha citando quais faltam.

2. `serie_diaria_tem_as_colunas_contratadas()` — mesmo padrão sobre `ml_acervo_metricas_diarias` com as 7 colunas, e assere que `updated_at` **não** existe (a linha é imutável).

3. `nomes_de_indice_cabem_no_limite_do_mariadb()` — lê a fonte das duas migrations, remove comentários, e para cada ocorrência de `->index(` e `->unique(` assere que existe um segundo argumento literal entre aspas e que a string tem 64 caracteres ou menos. Este é o gate que o SQLite não dá de graça: no MariaDB, nome acima de 64 caracteres cria a tabela, mata no índice e deixa a migration como `Pending` com a tabela existindo e sem o índice.

4. `upsert_por_company_e_item_e_unico()` — insere uma linha em `ml_acervo_itens` e tenta inserir outra com o mesmo par `(company_id, ml_item_id)`, esperando `QueryException`. Depois insere o mesmo `ml_item_id` para outra `company_id` e assere que passa. É a chave de upsert do D-05/D-07 e a primeira linha de defesa do escopo por empresa.
  </action>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan test --filter=SchemaAcervo` sai com código 0 e roda 4 testes
    - O teste 3 falha se alguém trocar `unique(['company_id','ml_item_id'], 'mai_company_item_unq')` por `unique(['company_id','ml_item_id'])` — provado alterando temporariamente e revertendo, com o resultado registrado no SUMMARY
    - O teste 4 prova que o mesmo `ml_item_id` pode existir para duas empresas distintas
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase134</automated>
  </verify>
  <done>O schema tem contrato executável, e o gate de nome de índice roda no SQLite dos testes mesmo protegendo contra uma falha que só aconteceria no MariaDB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados de empresa A → dados de empresa B | A tabela nova concentra o acervo de 74 clientes na mesma linha de armazenamento; a única fronteira é `company_id` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01 | Information Disclosure | `ml_acervo_itens` | mitigate | `company_id` é `foreignId` obrigatória, faz parte da chave única `mai_company_item_unq` e é o primeiro campo de todos os 4 índices — nenhuma query eficiente é escrita sem ele; o teste `upsert_por_company_e_item_e_unico()` prova que o mesmo MLB id coexiste em empresas distintas sem colisão |
| T-134-07 | Denial of Service | `ml_acervo_metricas_diarias` | mitigate | Retenção obrigatória por `mamd_data_idx` sobre `data`, consumida pelo comando de limpeza do plano 134-07; sem o índice, o `delete()` de retenção varreria dezenas de milhões de linhas |
| T-134-08 | Tampering | migrations em MariaDB | mitigate | Nome de índice explícito e abreviado, sem `enum`, sem FK com `nullOnDelete`; gate automatizado sobre a fonte da migration (`nomes_de_indice_cabem_no_limite_do_mariadb`) porque o SQLite dos testes não reproduz os erros 1059/1830/1553 |
| T-134-SC | Tampering | npm/composer install | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=Phase134` verde
- `C:\xampp\php\php.exe artisan migrate --pretend` lista as duas migrations sem erro
- Os 6 nomes de índice existem literalmente nas migrations e têm 64 caracteres ou menos
</verification>

<success_criteria>
1. A linha corrente por anúncio existe, com chave de upsert `(company_id, ml_item_id)` e índices que suportam o filtro por status (D-03), a ordenação por gravidade (D-12) e a seleção da fatia da rotação (D-23).
2. A série diária existe, imutável, com retenção viável por `data`.
3. Anúncio com variação cabe em uma linha, com o agregado do item-pai e o payload bruto em `variations` (D-17).
4. `buybox_status` e `visitas_30d` nulos representam "não avaliado" no schema — nenhum default mascara ausência de coleta (D-18).
</success_criteria>

<output>
Criar `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-02-SUMMARY.md` ao terminar.
</output>
