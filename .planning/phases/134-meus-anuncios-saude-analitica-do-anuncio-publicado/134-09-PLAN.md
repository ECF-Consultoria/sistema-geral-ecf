---
phase: 134-meus-anuncios-saude-analitica-do-anuncio-publicado
plan: 09
type: execute
wave: 6
depends_on: ["134-08"]
files_modified:
  - app/Http/Controllers/MlbAnuncioController.php
  - resources/js/Pages/Mlb/components/RascunhosPainel.jsx
  - resources/js/Pages/Mlb/MeusAnuncios.jsx
  - resources/js/Pages/Mlb/AnunciarML.jsx
  - tests/js/estrutura-anunciar-ml.test.js
  - tests/js/estrutura-meus-anuncios.test.js
  - tests/Feature/Phase134/RascunhosMeusAnunciosTest.php
autonomous: false
requirements: [D-14, D-16]

must_haves:
  truths:
    - "O rascunho tem alvo de clique de verdade: o card inteiro abre o rascunho no wizard"
    - "Publicar lote continua funcionando depois de sair do aside do wizard"
    - "O painel 'Saúde do anúncio' continua intacto no aside do wizard"
    - "O bloco de Rascunhos recentes não existe mais no wizard, e não sobrou código morto"
    - "O contrato de vocabulário visual do UI-SPEC vale para RascunhosPainel.jsx com teste permanente, não só com um grep durante a task"
  artifacts:
    - path: "resources/js/Pages/Mlb/components/RascunhosPainel.jsx"
      provides: "Barra de lote + grid de cards de rascunho"
      contains: "Publicar lote"
    - path: "tests/js/estrutura-anunciar-ml.test.js"
      provides: "Gate do D-16 — Saúde fica, Rascunhos sai"
    - path: "tests/js/estrutura-meus-anuncios.test.js"
      provides: "Gate de vocabulário visual estendido — helper travaVocabularioVisual() passa a cobrir também RascunhosPainel.jsx"
      contains: "RascunhosPainel"
    - path: "tests/Feature/Phase134/RascunhosMeusAnunciosTest.php"
      provides: "Gate de que publicarLote segue funcionando a partir da tela nova"
  key_links:
    - from: "resources/js/Pages/Mlb/components/RascunhosPainel.jsx"
      to: "mlb.anuncios.publicar-lote"
      via: "window.axios.post com company_id e rascunho_ids"
      pattern: "publicar-lote"
    - from: "resources/js/Pages/Mlb/components/RascunhosPainel.jsx"
      to: "mlb.anuncios.wizard"
      via: "router.get com ?rascunho={id} ao clicar no card"
      pattern: "rascunho:"
    - from: "tests/js/estrutura-meus-anuncios.test.js"
      to: "resources/js/Pages/Mlb/components/RascunhosPainel.jsx"
      via: "travaVocabularioVisual() — 4 tamanhos, 2 pesos, espaçamento múltiplo de 4"
      pattern: "travaVocabularioVisual"
---

<objective>
Tirar o bloco de Rascunhos recentes do aside do wizard e recolocá-lo como sub-aba de primeira classe em Meus Anúncios, com card clicável de verdade — resolvendo a queixa literal do usuário sobre o botão "Abrir" de 10 pixels — e provando por teste que a "Saúde do anúncio" do wizard não foi tocada.

Purpose: o D-14 move o bloco e o botão "Publicar lote" junto; o D-16 protege o painel de saúde do wizard, que guia a criação e alimenta o guard-rail de `publicar()`. Mover é a parte fácil; a parte que precisa de gate é provar que o que ficou continuou lá e que o que saiu não deixou código morto.
Output: `RascunhosPainel.jsx`, a sub-aba ligada, o wizard limpo e três testes de regressão.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-CONTEXT.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-PATTERNS.md
@resources/js/Pages/Mlb/AnunciarML.jsx
@resources/js/Pages/Mlb/AnunciosHistorico.jsx
@resources/js/Pages/Mlb/MeusAnuncios.jsx
@app/Http/Controllers/MlbAnuncioController.php
@tests/js/estrutura-grade-glide.test.js
@tests/js/estrutura-meus-anuncios.test.js
@tests/js/_fonte.js

<interfaces>
<!-- Props que o controller passa quando sub = 'rascunhos' -->
```
rascunhos: [
  {
    id, status, titulo, categoria, category_id, listing_tier,
    foto, updated_at, erro_resumo, ml_item_id
  }
]
```
Mesmo shape do que `wizard()` já monta (`MlbAnuncioController.php:97`), acrescido de
`categoria` (nome legível via `nomeCategoria()`) e `foto` (`payload.pictures.0.source`),
e **sem** `payload` completo — a tela de listagem não precisa dele, e ele é o campo
mais pesado do registro.

<!-- Rotas já existentes, reusadas sem alteração -->
```
POST   mlb.anuncios.publicar-lote     body: { company_id, rascunho_ids: [] }   (BULK-01)
DELETE mlb.anuncios.rascunho.destroy  param: { rascunho }
GET    mlb.anuncios.wizard            query: { rascunho: id }  -> wizard abre o rascunho
```

<!-- O que SAI de AnunciarML.jsx (confirmado por leitura do arquivo) -->
```
linhas 15-25    STATUS_BADGE e STATUS_LABEL  (usados SÓ no bloco removido — migram)
linhas 1128-1130 selecionados, publicandoLote, errosLote
linhas 1500-1521 rascunhosSelecionaveis, toggleSelecionado, toggleTodos
linhas 1524-1540 publicarLote
linhas 2831-2976 o bloco JSX "Rascunhos recentes"
```

<!-- O que FICA em AnunciarML.jsx, intocado -->
```
linhas 2761-2810 painel "Saúde do anúncio"           (D-16 — não tocar)
linha  1044      a prop `rascunhos` na assinatura     (ainda necessária)
linhas 1217-1266 abrirRascunho()                      (hidrata o formulário)
linhas 1269-1279 efeito de abrirRascunhoId            (deep-link ?rascunho=N)
linha  1562      polling de temPublicando             (status da publicação individual)
```

<!-- Forma-alvo do helper em tests/js/estrutura-meus-anuncios.test.js depois desta task -->
```js
// Criado no plano 134-08 sobre MeusAnuncios.jsx; extraído para helper AQUI e reusado
// pelo plano 134-10 sobre ModalDetalheAnuncio.jsx. Um único lugar para as 3 regras.
function travaVocabularioVisual(caminho) { /* 4 tamanhos, 2 pesos, espaçamento múltiplo de 4 */ }

travaVocabularioVisual('resources/js/Pages/Mlb/MeusAnuncios.jsx');
travaVocabularioVisual('resources/js/Pages/Mlb/components/RascunhosPainel.jsx');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Props de rascunhos na action meus() + garantia do deep-link do wizard</name>
  <files>app/Http/Controllers/MlbAnuncioController.php</files>
  <read_first>
    - `app/Http/Controllers/MlbAnuncioController.php` linhas 78-125 — a action `wizard()` e o mapeamento da prop `rascunhos` (linha 97), que é o shape a reusar, e o `limit(50)`
    - `app/Http/Controllers/MlbAnuncioController.php` action `meus()` (criada no plano 134-07) — onde as props novas entram
    - `app/Http/Controllers/MlbAnuncioController.php` método `nomeCategoria()` — usado por `historico()` para traduzir `category_id` em nome legível
    - `app/Models/MlAnuncioRascunho.php` — colunas e status
  </read_first>
  <action>
Em `meus()`, quando `sub` for `rascunhos`, montar a prop `rascunhos` com o shape do bloco `<interfaces>`: todos os rascunhos da empresa (não só 50 — esta agora é a tela oficial deles), ordenados por `updated_at` decrescente, com `titulo` vindo de `payload->title`, `foto` de `payload.pictures.0.source`, `categoria` de `nomeCategoria($r->category_id)` e `erro_resumo` do helper já existente. **Não** incluir o `payload` completo: é o campo mais pesado do registro e a listagem não precisa dele — quem precisa é o wizard, que carrega o rascunho por conta própria ao abrir.

Quando `sub` for `publicados`, devolver `rascunhos` como array vazio — a tela não paga por dado que não vai mostrar. O contador `subTotais.rascunhos` continua sendo calculado nos dois casos (já feito no plano 134-07).

Corrigir, na action `wizard()`, um problema que este plano cria ao mover a lista para fora do aside: hoje a prop `rascunhos` do wizard é `latest()->limit(50)`, e o deep-link `?rascunho=N` só funciona se o rascunho alvo estiver entre esses 50 (o efeito de `AnunciarML.jsx:1276` procura o alvo dentro da lista recebida). Com a listagem completa agora em Meus Anúncios, o usuário passa a clicar em rascunhos antigos com frequência, e o link falharia em silêncio — a tela abriria em branco sem erro. Correção: quando `$request->query('rascunho')` estiver presente e o id não estiver entre os 50 mais recentes, buscar aquele rascunho especificamente (escopado por `company_id`) e acrescentá-lo à coleção antes de mapear. Comentar a razão citando este plano.

**Nenhuma outra alteração em `wizard()`.** O caminho de publicação não muda (D-16).
  </action>
  <acceptance_criteria>
    - A action `meus()` monta `rascunhos` com as chaves `id`, `status`, `titulo`, `categoria`, `category_id`, `listing_tier`, `foto`, `updated_at`, `erro_resumo`, `ml_item_id` e **sem** `payload`
    - `sed -n '/function meus(/,/^    }/p' app/Http/Controllers/MlbAnuncioController.php | grep -c "'payload'"` devolve `0`
    - A action `wizard()` contém a busca complementar pelo rascunho de `?rascunho=`, escopada por `company_id`
    - `C:\xampp\php\php.exe artisan test --filter=Phase134` continua verde
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase134</automated>
  </verify>
  <done>A sub-aba Rascunhos recebe a lista completa e enxuta, e o deep-link do wizard funciona para rascunho de qualquer idade.</done>
</task>

<task type="auto">
  <name>Task 2: RascunhosPainel — barra de lote e cards com alvo de clique de verdade</name>
  <files>resources/js/Pages/Mlb/components/RascunhosPainel.jsx, resources/js/Pages/Mlb/MeusAnuncios.jsx</files>
  <read_first>
    - `resources/js/Pages/Mlb/AnunciarML.jsx` linhas 15-25, 1128-1130, 1500-1540 e 2831-2976 — o bloco inteiro que está sendo movido: `STATUS_BADGE`, `STATUS_LABEL`, os três estados, `rascunhosSelecionaveis`, `toggleSelecionado`, `toggleTodos`, `publicarLote` e o JSX com o tratamento de `errosLote`. **Mover, não reescrever** a lógica
    - `resources/js/Pages/Mlb/AnunciarML.jsx` linha ~1283 — a copy de confirmação do "Excluir rascunho", que migra **palavra por palavra**
    - `resources/js/Pages/Mlb/AnunciosHistorico.jsx` `GradeCards`/`CardAnuncio` linhas 33-103 — o grid responsivo a reusar e o comentário sobre botão aninhado ser HTML inválido
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md` seção 9 — o JSX completo do card redesenhado, a barra de lote e a nota de que as cores de status **não** devem ser redesenhadas
    - `tests/js/estrutura-meus-anuncios.test.js` — as 3 regras de vocabulário que a Task 3 vai passar a aplicar também sobre este arquivo; escrever já dentro delas evita retrabalho
    - `resources/js/Pages/Mlb/anuncioHistoricoUtils.js` — `rotuloTier()`
  </read_first>
  <action>
Criar `resources/js/Pages/Mlb/components/RascunhosPainel.jsx`, componente que recebe `empresaId` e `rascunhos` e renderiza os dois blocos da seção 9 do UI-SPEC.

**Barra de lote**, acima do grid: checkbox "Selecionar todos", contador e o botão "Publicar lote (N)" em tratamento sólido-amarelo (precedente BULK-01, inalterado), mais o painel de `errosLote` quando houver. A lógica de `rascunhosSelecionaveis`, `toggleSelecionado`, `toggleTodos` e `publicarLote` é **movida** do wizard — mesmo `window.axios.post` para `route('mlb.anuncios.publicar-lote')` com `{ company_id, rascunho_ids }`, mesmo tratamento de erro, mesmo teto de seleção. Não reimplementar nem "melhorar" o contrato do endpoint: ele já tem double-check de empresa e teto de 50 por chamada no backend.

**Grid de cards**: reusa `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-3` de `AnunciosHistorico`. Cada card segue o JSX da seção 9:
- Topo com o checkbox de seleção à esquerda e o badge de status à direita, usando `STATUS_BADGE` e `STATUS_LABEL` migrados do wizard — **as cores de status não são redesenhadas**, só o padding do badge muda para `px-1 py-1` (múltiplo de 4).
- Toda a área central — foto, título, categoria e tier — dentro de **um único** `<button type="button">` que abre o rascunho no wizard com `router.get(route('mlb.anuncios.wizard', { company: empresaId }), { rascunho: r.id })`. É esta a correção da queixa literal do usuário: "do jeito que está não gostei" sobre o botão "Abrir" de 10 pixels. O alvo agora é o card inteiro.
- Rodapé com a dica "Clique para abrir →" que ganha cor no hover do grupo, e o botão Excluir com ícone `Trash2` e `aria-label="Excluir rascunho"`, com a confirmação cuja copy migra literalmente: "Excluir este rascunho («{título}»)? Isso não remove o anúncio já publicado no Mercado Livre."
- O checkbox e o botão Excluir ficam **fora** do `<button>` de abrir, como irmãos. Botão dentro de botão é HTML inválido — armadilha já documentada no próprio módulo.
- Foto com `loading="lazy"` e fallback `ImageOff`.

Estado vazio, com a copy literal do Copywriting Contract: heading "Nenhum rascunho aqui ainda." e corpo "Comece um anúncio na aba Individual ou Em massa — ele aparece aqui até ser publicado."

Em `MeusAnuncios.jsx`, importar `RascunhosPainel` e renderizá-lo no lugar do contêiner vazio deixado pelo plano 134-08 quando `sub` é `rascunhos`, passando `empresaId` e `rascunhos`.

Vocabulário visual: os mesmos 4 tamanhos, 2 pesos e espaçamentos múltiplos de 4 do contrato. Isso **não** fica no honor system: a Task 3 deste mesmo plano estende `tests/js/estrutura-meus-anuncios.test.js` para aplicar as mesmas três regras sobre este arquivo, como gate permanente de regressão — igual ao que `MeusAnuncios.jsx` já tem desde o plano 134-08.
  </action>
  <acceptance_criteria>
    - `resources/js/Pages/Mlb/components/RascunhosPainel.jsx` contém `Publicar lote`, `mlb.anuncios.publicar-lote`, `aria-label="Excluir rascunho"` e a copy literal `Isso não remove o anúncio já publicado no Mercado Livre.`
    - O arquivo contém `Nenhum rascunho aqui ainda.` e `Comece um anúncio na aba Individual ou Em massa — ele aparece aqui até ser publicado.`
    - Nenhum `<button` aninhado dentro de outro `<button` no arquivo
    - Nenhuma ocorrência de `font-medium`, `font-bold`, `text-xs`, `text-[10px]` nem de classe com meio-passo no arquivo
    - `resources/js/Pages/Mlb/MeusAnuncios.jsx` importa e renderiza `RascunhosPainel`
    - `npm run build` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>A sub-aba Rascunhos tem card clicável de verdade e o "Publicar lote" funcionando no novo endereço.</done>
</task>

<task type="auto">
  <name>Task 3: Limpar o wizard + gates do D-16, do Publicar lote e do vocabulário visual</name>
  <files>resources/js/Pages/Mlb/AnunciarML.jsx, tests/js/estrutura-anunciar-ml.test.js, tests/js/estrutura-meus-anuncios.test.js, tests/Feature/Phase134/RascunhosMeusAnunciosTest.php</files>
  <read_first>
    - `resources/js/Pages/Mlb/AnunciarML.jsx` — o arquivo inteiro nas faixas listadas no bloco `<interfaces>`: o que sai e, principalmente, o que **fica**
    - `tests/js/estrutura-meus-anuncios.test.js` — criado no plano 134-08; os 6 grupos de asserção, dos quais os 3 primeiros (tipografia, peso, espaçamento) viram helper reusável nesta task
    - `tests/js/estrutura-grade-glide.test.js` e `tests/js/_fonte.js` — o molde do teste estrutural e o helper que remove comentários antes de assertar
    - `tests/Feature/Phase134/MeusAnunciosTest.php` — os helpers de fixture criados no plano 134-07
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-CONTEXT.md` D-16 — a trava literal
  </read_first>
  <action>
Em `AnunciarML.jsx`, remover **exatamente** o que está na lista "o que SAI" do bloco `<interfaces>`: as constantes `STATUS_BADGE` e `STATUS_LABEL` (confirmadamente usadas só no bloco removido), os três estados de lote, `rascunhosSelecionaveis`, `toggleSelecionado`, `toggleTodos`, `publicarLote` e o bloco JSX de Rascunhos recentes.

Remover também os imports que ficarem órfãos depois disso — em especial os ícones usados só pelo bloco (`Rocket`, e conferir `Pencil`/`Trash2` antes de tirar, que podem ter outro uso no wizard). Este projeto não tem ESLint e o build passa com identificador indefinido: import órfão não quebra nada visivelmente, mas identificador **usado** e não definido também passa no build e só quebra em runtime. Conferir cada remoção com busca no arquivo antes de apagar.

**Não tocar em nada da lista "o que FICA"**: o painel "Saúde do anúncio" (linhas 2761-2810), a prop `rascunhos` na assinatura, `abrirRascunho()`, o efeito de `abrirRascunhoId` e o polling de `temPublicando`. A prop `rascunhos` continua sendo necessária — é ela que o efeito do deep-link percorre para achar o rascunho a abrir. Remover a prop quebraria o "Anunciar semelhante" da Fase 86.

**Estender `tests/js/estrutura-meus-anuncios.test.js` para cobrir `RascunhosPainel.jsx`.** O arquivo já existe desde o plano 134-08 e trava, sobre `MeusAnuncios.jsx`, os 3 grupos de vocabulário visual (tipografia de 4 tamanhos, 2 pesos, espaçamento múltiplo de 4) mais 3 grupos específicos daquela tela. Extrair os 3 grupos de vocabulário para uma função `travaVocabularioVisual(caminho)` — que faz `lerSemComentarios(caminho)` e roda as mesmas três asserções — e chamá-la duas vezes: para `resources/js/Pages/Mlb/MeusAnuncios.jsx` e para `resources/js/Pages/Mlb/components/RascunhosPainel.jsx`. Os 3 grupos específicos de `MeusAnuncios.jsx` (D-11, D-22 base 86, ordem das abas) **continuam apenas sobre aquele arquivo** — não fazem sentido no painel de rascunhos e ficam fora do helper.

Por que isso é uma task e não uma promessa: sem o helper, o contrato de design em `RascunhosPainel.jsx` seria conferido uma única vez por `grep` durante a Task 2 e nunca mais. Um `font-medium` acrescentado três semanas depois passaria no build, passaria em `npm run test:js` e fragmentaria o vocabulário exatamente como as revisões 2 e 3 do UI-SPEC corrigiram. O plano 134-10 reusa o mesmo helper para `ModalDetalheAnuncio.jsx`; escrever a função aqui é o que torna aquilo uma linha.

Criar `tests/js/estrutura-anunciar-ml.test.js` com `lerSemComentarios('resources/js/Pages/Mlb/AnunciarML.jsx')`. Seis asserções:
1. A fonte **contém** `Saúde do anúncio` — o painel do D-16 continua lá.
2. A fonte **contém** `analisarAnuncio` — a análise que alimenta o guard-rail de publicação continua ligada.
3. A fonte **não contém** `Rascunhos recentes`.
4. A fonte **não contém** `toggleTodos`, `publicarLote`, `rascunhosSelecionaveis` nem `errosLote` — nenhum código morto sobrou.
5. A fonte **contém** `abrirRascunhoId` e `abrirRascunho` — o deep-link do "Anunciar semelhante" não regrediu.
6. A fonte **não contém** `STATUS_BADGE` nem `STATUS_LABEL` — eles migraram para `RascunhosPainel.jsx`; e, lendo a fonte de `RascunhosPainel.jsx`, assere que os dois estão **lá**.

Criar `tests/Feature/Phase134/RascunhosMeusAnunciosTest.php` com `@group phase134`, `RefreshDatabase` e `Http::fake()`. Quatro testes:
1. `sub_aba_rascunhos_lista_os_rascunhos_da_empresa()` — acessa a rota com `?sub=rascunhos` e assere que a prop `rascunhos` traz os rascunhos da empresa, com `categoria` e `foto` preenchidos e **sem** a chave `payload`.
2. `sub_aba_rascunhos_nao_vaza_rascunho_de_outra_empresa()`.
3. `publicar_lote_continua_funcionando_a_partir_da_tela_nova()` — com `Queue::fake()`, faz o POST em `mlb.anuncios.publicar-lote` com `company_id` e dois `rascunho_ids` da empresa, e assere a resposta de sucesso e os jobs enfileirados. Estende o comportamento já coberto pelos testes de BULK-01 — o endpoint não mudou, o que mudou foi quem o chama.
4. `deep_link_do_wizard_abre_rascunho_antigo()` — cria 60 rascunhos, acessa `mlb.anuncios.wizard` com `?rascunho=` apontando para o mais antigo, e assere que ele está presente na prop `rascunhos` do wizard. É o gate da correção da Task 1.
  </action>
  <acceptance_criteria>
    - `node --test tests/js/estrutura-anunciar-ml.test.js` sai com código 0 e cobre as 6 asserções
    - `tests/js/estrutura-meus-anuncios.test.js` contém `travaVocabularioVisual` e a chamada com o caminho de `RascunhosPainel.jsx`
    - `node --test tests/js/estrutura-meus-anuncios.test.js` sai com código 0 e as 3 regras de vocabulário rodam sobre **dois** arquivos
    - O gate estendido falha se um `font-medium` for inserido em `RascunhosPainel.jsx` — provado inserindo temporariamente e revertendo, com o resultado no SUMMARY
    - O gate estendido falha se um `px-2.5` for inserido em `RascunhosPainel.jsx` — provado inserindo temporariamente e revertendo, com o resultado no SUMMARY
    - `C:\xampp\php\php.exe artisan test --filter=RascunhosMeusAnuncios` sai com código 0 e roda 4 testes
    - `grep -c 'Saúde do anúncio' resources/js/Pages/Mlb/AnunciarML.jsx` é maior que 0
    - `grep -c 'Rascunhos recentes' resources/js/Pages/Mlb/AnunciarML.jsx` devolve `0`
    - `npm run build` sai com código 0 e `npm run test:js` continua verde
    - A suíte do módulo continua verde: `C:\xampp\php\php.exe artisan test --filter=Mlb` sem regressão em relação ao baseline registrado no SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase134</automated>
  </verify>
  <done>O wizard perdeu o bloco de rascunhos sem perder o painel de saúde nem o deep-link, e os três fatos — D-16, Publicar lote e vocabulário visual do painel novo — têm gate automatizado permanente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Conferência visual da sub-aba Rascunhos e do wizard limpo</name>
  <files>nenhum arquivo é modificado — conferência manual sobre o que as tasks 1 a 3 construíram</files>
  <what-built>
A sub-aba Rascunhos em Meus Anúncios, com barra de lote ("Selecionar todos" mais "Publicar lote (N)") e grid de cards em que o card inteiro é o alvo de clique; e o aside do wizard sem o bloco de Rascunhos recentes, com a "Saúde do anúncio" intacta.
  </what-built>
  <action>
Executar o roteiro de `<how-to-verify>` em ambiente local, na ordem, e registrar o resultado de cada um dos 5 passos no SUMMARY. Criar antes os rascunhos de conferência descritos no passo 2, em pelo menos três status diferentes. Nenhum arquivo de código é alterado nesta task: regressão encontrada vira correção na task correspondente, nunca conserto silencioso aqui.
  </action>
  <how-to-verify>
1. Rodar `npm run build`.
2. Criar alguns rascunhos na empresa de teste (aba Individual), em status diferentes — pelo menos um `rascunho`, um `validado` e um `erro`.
3. Abrir `/mlb/anuncios/meus/{company}?sub=rascunhos` e conferir:
   - Cada card mostra foto, título, categoria, tier e badge de status, com as **mesmas cores** de status de antes.
   - Clicar em **qualquer ponto** do miolo do card abre aquele rascunho no wizard, com o formulário hidratado. Não existe mais um link minúsculo de "Abrir".
   - O checkbox e o botão Excluir funcionam sem abrir o rascunho (são irmãos, não filhos do botão).
   - Selecionar dois rascunhos e clicar em "Publicar lote (2)" enfileira a publicação e mostra o mesmo feedback de antes.
   - Excluir um rascunho mostra a confirmação com a frase que menciona não remover o anúncio já publicado.
   - Com zero rascunhos, aparece "Nenhum rascunho aqui ainda."
4. Abrir `/mlb/anuncios/wizard/{company}` e conferir:
   - O painel "Saúde do anúncio" continua no aside, com o mesmo comportamento ao escolher categoria e preencher campos.
   - O bloco de Rascunhos recentes **não** existe mais.
   - A publicação individual continua funcionando ponta a ponta, e o status de "publicando" continua sendo atualizado.
5. Abrir o Histórico (`/mlb/anuncios/historico/{company}`) e usar "Anunciar semelhante em massa" em um lote — confirmar que a Fase 86 não regrediu.
  </how-to-verify>
  <read_first>
    - `resources/js/Pages/Mlb/components/RascunhosPainel.jsx` e `resources/js/Pages/Mlb/AnunciarML.jsx` — o que foi criado e o que foi removido
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md` seção 9
  </read_first>
  <acceptance_criteria>
    - Os 5 itens do roteiro foram executados e o resultado de cada um está no SUMMARY
    - O clique no card inteiro abre o rascunho — a queixa original está resolvida
    - "Saúde do anúncio" continua no wizard e o caminho de publicação individual não mudou (D-16)
    - "Anunciar semelhante em massa" do Histórico continua funcionando (dependência da Fase 86)
  </acceptance_criteria>
  <verify>
    <human-check>Usuário confirma que o card inteiro abre o rascunho (queixa original resolvida), que "Publicar lote" funciona no endereço novo, que "Saúde do anúncio" continua no aside do wizard (D-16) e que o "Anunciar semelhante em massa" do Histórico não regrediu (Fase 86).</human-check>
  </verify>
  <done>Os 5 itens do roteiro foram conferidos na tela real e o resultado de cada um está registrado no SUMMARY; o usuário aprovou explicitamente.</done>
  <resume-signal>Digitar "aprovado" ou descrever o que regrediu</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `mlb.anuncios.publicar-lote` | Único endpoint desta fase que efetivamente escreve no Mercado Livre — é o BULK-01 preexistente, agora chamado de outra tela |
| navegador → `mlb.anuncios.rascunho.destroy` | Exclusão de registro local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-22 | Elevation of Privilege | `publicarLote` chamado da tela nova | mitigate | O endpoint não muda: o double-check de empresa (T-80-02) e o teto de 50 rascunhos por chamada continuam no backend; a tela nova só troca quem chama. Teste `publicar_lote_continua_funcionando_a_partir_da_tela_nova()` prova que o contrato foi preservado |
| T-134-01 | Information Disclosure | Prop `rascunhos` da sub-aba | mitigate | Montada com `where('company_id', $company->id)`; teste `sub_aba_rascunhos_nao_vaza_rascunho_de_outra_empresa()`; a busca complementar do deep-link em `wizard()` também é escopada por `company_id` |
| T-134-23 | Information Disclosure | Payload completo do rascunho na listagem | mitigate | `payload` deliberadamente fora da prop da listagem — é o campo mais pesado e contém o anúncio inteiro; quem precisa dele é o wizard, que o carrega ao abrir. Gate por `grep` na action |
| T-134-24 | Repudiation | Regressão silenciosa do D-16 | mitigate | `estrutura-anunciar-ml.test.js` assere presença de "Saúde do anúncio" e de `analisarAnuncio`, e ausência de `Rascunhos recentes`, `toggleTodos`, `publicarLote` e `rascunhosSelecionaveis` na fonte sem comentários |
| T-134-27 | Repudiation | Deriva silenciosa do contrato de design no arquivo novo | mitigate | `travaVocabularioVisual()` em `estrutura-meus-anuncios.test.js` aplica as 3 regras do UI-SPEC sobre `RascunhosPainel.jsx` como gate permanente, não como conferência única durante a task |
| T-134-SC | Tampering | npm install | accept | Nenhum pacote novo nesta fase |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=Phase134` verde
- `C:\xampp\php\php.exe artisan test --filter=Mlb` sem regressão contra o baseline registrado
- `npm run test:js` e `npm run build` verdes
- `travaVocabularioVisual()` roda sobre `MeusAnuncios.jsx` e `RascunhosPainel.jsx`
- Checkpoint visual aprovado
</verification>

<success_criteria>
1. O rascunho tem alvo de clique de verdade — o card inteiro abre o rascunho no wizard (D-14, queixa literal do usuário).
2. "Publicar lote" migrou junto e continua funcionando (D-14).
3. "Saúde do anúncio" continua intacta no aside do wizard (D-16).
4. O bloco de Rascunhos recentes saiu do wizard sem deixar código morto (D-16).
5. O deep-link `?rascunho=N` do "Anunciar semelhante" funciona para rascunho de qualquer idade.
6. O contrato de vocabulário visual do UI-SPEC vale para `RascunhosPainel.jsx` com teste permanente, e o helper está pronto para o plano 134-10 reusar.
</success_criteria>

<output>
Criar `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-09-SUMMARY.md` ao terminar, incluindo o baseline de `--filter=Mlb` antes e depois.
</output>
