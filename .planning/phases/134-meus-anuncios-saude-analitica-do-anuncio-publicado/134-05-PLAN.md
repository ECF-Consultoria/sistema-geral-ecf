---
phase: 134-meus-anuncios-saude-analitica-do-anuncio-publicado
plan: 05
type: execute
wave: 3
depends_on: ["134-02", "134-03"]
files_modified:
  - app/Services/Mlb/Acervo/MlAcervoDetalheService.php
  - app/Jobs/SyncMlAcervoDetalheJob.php
  - tests/Unit/Phase134/RotacaoDetalheTest.php
autonomous: true
requirements: [D-08, D-11, D-18, D-19, D-23]

must_haves:
  truths:
    - "Todo item ativo do acervo é coberto pela camada cara dentro da janela de N dias, sem cauda longa permanentemente cega"
    - "Item ainda não coberto pela rotação aparece como não avaliado — nunca com status de buy box inventado"
    - "Visitas e status de catálogo carregam frescor próprio, diferente do resto da linha"
    - "A camada cara escreve SÓ os campos dela — nada do que a camada barata gravou é apagado"
  artifacts:
    - path: "app/Services/Mlb/Acervo/MlAcervoDetalheService.php"
      provides: "Camada cara: visitas por item e price_to_win, com seleção da fatia da rotação"
      contains: "price_to_win"
    - path: "app/Jobs/SyncMlAcervoDetalheJob.php"
      provides: "Job de fatia curta e previsível, por lote de itens"
      contains: "ShouldQueue"
    - path: "tests/Unit/Phase134/RotacaoDetalheTest.php"
      provides: "Gates de D-23 (cobertura em N execuções), D-18 (não avaliado) e da não-sobrescrita das colunas da camada barata"
  key_links:
    - from: "app/Services/Mlb/Acervo/MlAcervoDetalheService.php"
      to: "ml_acervo_itens.detalhe_coletado_em"
      via: "ordenação da fatia — mais antigo primeiro, nulos na frente"
      pattern: "detalhe_coletado_em"
    - from: "app/Services/Mlb/Acervo/MlAcervoDetalheService.php"
      to: "ml_acervo_itens (colunas da camada barata)"
      via: "update nomeado de 5 colunas — nunca upsert, nunca save() de model inteiro"
      pattern: "->update\\(\\["
---

<objective>
Construir a camada cara da coleta — visitas por item e status de buy box — em rotação por fatia, de modo que todo item ativo seja coberto dentro de uma janela de N dias sem que nenhuma execução diária tente cobrir o acervo inteiro.

Purpose: os dois campos que faltam para a triagem ficar completa custam uma chamada HTTP por item, sem lote (confirmado por chamada real: `/visits/items?ids=` promete lote de 20 na documentação e a API recusa com limite 1). Cobertura total diária seria cerca de 587 mil chamadas, na faixa de horas. O D-23 resolve isso com rotação, e o D-18 exige que o item ainda não coberto seja exibido como "não avaliado", nunca com status inventado.
Output: `MlAcervoDetalheService`, `SyncMlAcervoDetalheJob` e os gates de cobertura, de "não avaliado" e de fronteira de escrita entre as duas camadas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-CONTEXT.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-RESEARCH.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-PATTERNS.md
@app/Services/MercadoLivreService.php
@app/Services/Mlb/Acervo/AnuncioSaudeService.php
@app/Models/MlAcervoItem.php
@app/Models/MlAcervoMetricaDiaria.php
@app/Jobs/PublicarAnuncioMlJob.php

<interfaces>
<!-- API pública de MlAcervoDetalheService (camada cara, rotação D-23). -->

```php
namespace App\Services\Mlb\Acervo;

class MlAcervoDetalheService
{
    /**
     * As ÚNICAS colunas que a camada cara escreve em `ml_acervo_itens`.
     * Espelho da proibição registrada no plano 134-04: cada camada só toca no que é dela.
     * `motivos` e `severidade` são compartilhadas e recomputadas a partir das colunas
     * JÁ PERSISTIDAS pela camada barata — nunca de um payload vazio.
     */
    private const COLUNAS_CAMADA_CARA = [
        'visitas_30d', 'buybox_status', 'motivos', 'severidade', 'detalhe_coletado_em',
    ];

    public function __construct(
        private MercadoLivreService $ml,
        private AnuncioSaudeService $saude,
    ) {}

    /**
     * Seleciona a fatia da rotação desta execução: 1/N do acervo ATIVO da empresa,
     * os menos recentemente detalhados primeiro (nulos na frente).
     * @return string[]  MLB ids
     */
    public function selecionarFatia(Company $company, int $n): array;

    /**
     * Coleta visitas e buy box para um lote de ids e atualiza a linha corrente
     * + a série diária. Fail-open por item.
     * @return array{itens:int, falhas:int}
     */
    public function coletarDetalhe(Company $company, array $mlItemIds): array;
}
```

<!-- Endpoints da camada cara, já confirmados por chamada real (134-RESEARCH.md §1/§A-02) -->
```
GET /items/{id}/visits?date_from=YYYY-MM-DDT00:00:00.000-00:00&date_to=...   -> 1 item por chamada
GET /items/{id}/price_to_win?version=v2                                      -> 1 item por chamada
   status: winning | sharing_first_place | losing | listed
```

<!-- Ordenação da fatia, portável entre MariaDB e o SQLite dos testes -->
```
->orderByRaw('detalhe_coletado_em IS NULL DESC')
->orderBy('detalhe_coletado_em')
->orderBy('ml_item_id')
```

<!-- Fronteira de propriedade de coluna entre as duas camadas (contrato desta fase) -->
```
CAMADA CARA   (este plano)     -> visitas_30d, buybox_status, detalhe_coletado_em
CAMADA BARATA (plano 134-04)   -> title, status, price, available_quantity, sold_quantity,
                                  nota_ecf, nota_sinais, origem, rascunho_id, coletado_em,
                                  coleta_erro e as demais de COLUNAS_CAMADA_BARATA
COMPARTILHADAS                 -> motivos, severidade
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MlAcervoDetalheService — seleção da fatia e coleta de visitas e buy box</name>
  <files>app/Services/Mlb/Acervo/MlAcervoDetalheService.php</files>
  <read_first>
    - `app/Services/MercadoLivreService.php` linhas 240-340 e 400-520 — `get()`, `comRetry429()` e o padrão fail-open de `fetchItemStatus()`. **Não reimplementar retry de 429**: `comRetry429()` já honra `Retry-After` com backoff e teto
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-RESEARCH.md` §A-02, §1 e `## Code Examples` — o `price_to_win` real capturado, os valores de `status` e a prova de que visitas não aceita lote
    - `app/Services/Mlb/Acervo/AnuncioSaudeService.php` — `triagem()`, que precisa ser rechamada depois que `buybox_status` muda, e **quais campos do item ela lê** (`status` e `available_quantity`)
    - `app/Models/MlAcervoItem.php` — `naoAvaliadoBuyBox()`, `visitasNaoAvaliadas()` e as constantes de motivo/severidade
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-04-PLAN.md` Task 2 — a fronteira de propriedade de coluna que este plano precisa respeitar do outro lado
    - `config/mlb_acervo.php` — `rotacao_n`
  </read_first>
  <action>
Criar `app/Services/Mlb/Acervo/MlAcervoDetalheService.php` com o construtor e as duas assinaturas públicas do bloco `<interfaces>`.

`selecionarFatia(Company $company, int $n)`: conta os itens de `ml_acervo_itens` da empresa com `status` igual a `active`, calcula `ceil(total / max(1, $n))` e devolve essa quantidade de MLB ids, com a ordenação do bloco `<interfaces>` — menos recentemente detalhados primeiro, com os nunca detalhados na frente. Usar `orderByRaw('detalhe_coletado_em IS NULL DESC')` porque `NULLS FIRST` não é portável entre MariaDB e o SQLite dos testes.

**Decisão do planner, registrada em comentário:** a camada cara cobre apenas itens com `status = active`. Pausado e encerrado não precisam de visitas nem de buy box com urgência — o motivo deles já é conhecido pela camada barata, e incluí-los inflaria a rotação sem nenhum ganho de triagem. Isso é coerente com o D-03, que faz da tela uma leitura de ativos por padrão.

A rotação **não guarda cursor**. O próprio `detalhe_coletado_em` é o cursor: quem foi coletado hoje vai para o fim da fila, quem falhou continua sendo o mais antigo e volta na próxima execução. É auto-corretivo por construção e não tem estado que possa dessincronizar. Documentar isso — é a razão de não existir uma tabela de controle de rotação.

`coletarDetalhe(Company $company, array $mlItemIds)`: carregar as linhas de `ml_acervo_itens` daqueles ids (escopadas por `company_id`) e, para cada uma, dentro de `try/catch (\Throwable $e)`:
1. `GET /items/{id}/visits` com `date_from` e `date_to` cobrindo a janela dos 30 dias que termina na data da coleta. Somar as visitas da janela e gravar em `visitas_30d`. **Decisão do planner, em comentário:** a medida é a janela móvel de 30 dias, não "visitas de ontem". Com a rotação do D-23 tocando cada item a cada N dias, uma contagem de um único dia seria ruído — a janela de 30 dias é comparável entre coletas espaçadas.
2. Só se a linha tiver `catalog_listing` verdadeiro: `GET /items/{id}/price_to_win` com `version=v2`, e gravar `status` em `buybox_status`. Item que não é de catálogo mantém `buybox_status` nulo para sempre — e o modelo distingue isso de "não avaliado" por `catalog_listing` (ver `MlAcervoItem::naoAvaliadoBuyBox()`).
3. Recalcular a triagem chamando `AnuncioSaudeService::triagem()` com os `nota_sinais` já persistidos na linha e o `buybox_status` recém-obtido, atualizando `motivos` e `severidade`. Sem esse passo o motivo "perdendo catálogo" nunca apareceria na tela, porque a camada barata roda sem buy box.
4. Gravar `detalhe_coletado_em` igual a `now()` — é o frescor PRÓPRIO desses campos, que o D-08 exige exibir separadamente do resto da linha.
5. Atualizar a série diária do dia corrente com `visitas` (mesmo valor de `visitas_30d`) pela mesma regra de "só quando muda" já estabelecida no plano 134-04. Esse `updateOrCreate` grava **só** a chave `visitas` além da chave lógica `(company_id, ml_item_id, data)`: passar `sold_quantity` ou `nota_ecf` nulos aqui apagaria o que a camada barata escreveu na linha do mesmo dia.

**Escrita da linha corrente — update nomeado, nunca upsert e nunca `save()` de model inteiro.** A atualização é um `MlAcervoItem::query()->where('company_id', $company->id)->where('ml_item_id', $id)->update([...])` cujo array tem **exatamente** as cinco chaves de `COLUNAS_CAMADA_CARA`: `visitas_30d`, `buybox_status`, `motivos`, `severidade` e `detalhe_coletado_em`. Nenhuma coluna da camada barata (`title`, `status`, `available_quantity`, `sold_quantity`, `nota_ecf`, `nota_sinais`, `origem`, `rascunho_id`, `coletado_em`, `coleta_erro`, entre outras) entra nesse array — a camada barata é a dona delas e é ela que sabe o valor fresco.

Proibido usar `upsert()` aqui, e proibido hidratar o model, mexer em atributos e chamar `save()`: a linha necessariamente já existe (a fatia sai de `ml_acervo_itens`), e escrita de linha inteira com payload parcial é exatamente o mecanismo pelo qual uma camada apaga a outra. É o espelho literal da proibição registrada no plano 134-04, agora na direção inversa. Comentar isso na linha do `update()`.

`motivos` e `severidade` são compartilhadas pelas duas camadas e por isso exigem cuidado extra: `triagem()` lê `status` e `available_quantity` do item para emitir `MOTIVO_PAUSADO` e `MOTIVO_SEM_ESTOQUE`. Montar o pseudo-payload passado a `triagem()` a partir das colunas **já persistidas na linha** (`status`, `available_quantity`, `catalog_listing`), nunca de um array vazio — chamar `triagem([], $sinais, $buyboxStatus)` faria a camada cara apagar os motivos que a camada barata tinha acabado de escrever, que é a mesma falha do 134-04 na direção inversa e some sem deixar rastro na tela.

Falha em um item: `Log::warning("[MLB Anuncios] detalhe falhou — empresa {id}, item {mlb}: {mensagem}")`, conta em `falhas`, **não** escreve `buybox_status` e **não** avança `detalhe_coletado_em`. Deixar o campo nulo é o comportamento correto do D-18: o item continua "não avaliado" e volta ao topo da fila na próxima execução. Preencher com um valor de consolo seria exatamente a mentira que a decisão proíbe.

Toda chamada sai por `$this->ml->get($company, ...)`. Nenhum `Http::` direto, nenhum verbo além de `get`.

Pacing: **não** implementar sleep manual. `comRetry429()` já cuida do 429 com `Retry-After` e backoff. O espaçamento desta camada é estrutural — a fatia é 1/N do acervo e vem quebrada em jobs pequenos (Task 2), com delay escalonado no fan-out (plano 134-07).
  </action>
  <acceptance_criteria>
    - `app/Services/Mlb/Acervo/MlAcervoDetalheService.php` contém `orderByRaw('detalhe_coletado_em IS NULL DESC')` e `price_to_win`
    - `grep -v '^\s*\(//\|\*\)' app/Services/Mlb/Acervo/MlAcervoDetalheService.php | grep -cE '(->post\(|->put\(|->delete\(|->patch\(|Http::|sleep\()'` devolve `0`
    - `grep -v '^\s*\(//\|\*\)' app/Services/Mlb/Acervo/MlAcervoDetalheService.php | grep -cE '(::upsert\(|->save\(\))'` devolve `0` — a camada cara nunca escreve linha inteira
    - A escrita usa `->update([` e o array tem exatamente as 5 chaves de `COLUNAS_CAMADA_CARA`: `sed -n '/->update(\[/,/\]);/p' app/Services/Mlb/Acervo/MlAcervoDetalheService.php | grep -cE "'(title|status|price|available_quantity|sold_quantity|nota_ecf|nota_sinais|origem|rascunho_id|coletado_em|coleta_erro)'\s*=>"` devolve `0`
    - O array passado a `triagem()` é montado a partir das colunas persistidas — o arquivo **não** contém `triagem([]` nem `triagem([],`
    - `public function selecionarFatia(Company $company, int $n): array` existe com essa assinatura
    - O arquivo contém comentário justificando que a camada cara cobre só `status = active`, que a janela de visitas é de 30 dias, e que o `update()` é nomeado por causa da fronteira de propriedade de coluna com o plano 134-04
    - `C:\xampp\php\php.exe -l app/Services/Mlb/Acervo/MlAcervoDetalheService.php` sai sem erro
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=RotacaoDetalhe</automated>
  </verify>
  <done>A fatia da rotação é selecionada sem cursor persistido, visitas e buy box são coletados com fail-open que preserva o estado "não avaliado", e a escrita não invade as colunas da camada barata.</done>
</task>

<task type="auto">
  <name>Task 2: SyncMlAcervoDetalheJob — job de lote curto e previsível</name>
  <files>app/Jobs/SyncMlAcervoDetalheJob.php</files>
  <read_first>
    - `app/Jobs/PublicarAnuncioMlJob.php` — arquivo inteiro: `ShouldBeUnique`, `uniqueId()`, `uniqueFor()` maior que `timeout` mais backoff, `backoff()`, `failed()`
    - `app/Jobs/SyncMlCompanyJob.php` — construtor com propriedade promovida `readonly` e `failed()` com log citando empresa e nome
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-RESEARCH.md` `## Common Pitfalls` item 1 — `retry_after` de 90s no driver `database` e 2000s em produção, e por que a camada cara precisa de job pequeno
    - `config/mlb_acervo.php` — `chunk_detalhe`
  </read_first>
  <action>
Criar `app/Jobs/SyncMlAcervoDetalheJob.php`: `implements ShouldQueue, ShouldBeUnique`, construtor com `public readonly int $companyId` e `public readonly array $mlItemIds`.

Receber `companyId` como inteiro em vez do model `Company` serializado: o job carrega uma lista de até 500 ids no payload e serializar o model junto engorda a linha da fila sem necessidade. Resolver a `Company` no `handle()` com `Company::findOrFail($this->companyId)`.

`$tries = 2`, `$timeout = 900`, `backoff()` devolvendo `[120, 600]`, `uniqueId()` devolvendo o `companyId` concatenado com um hash curto (`md5` dos ids) — dois lotes diferentes da mesma empresa podem e devem rodar sem se bloquear; o que não pode é o **mesmo** lote entrar duas vezes. `uniqueFor()` devolvendo 1800, sempre maior que `timeout` mais o backoff máximo.

`handle(MlAcervoDetalheService $detalhe)` chama `coletarDetalhe()` e loga o resumo com a tag `[MLB Anuncios]`, citando `company_id`, quantidade de itens e falhas.

`failed(\Throwable $e)` loga `Log::error` com a tag do módulo, `company_id` e a quantidade de ids do lote. **Não** marcar nada como coletado — os itens do lote continuam com `detalhe_coletado_em` nulo ou antigo e voltam naturalmente ao topo da fila na próxima execução, que é a propriedade auto-corretiva da rotação.

Dimensionamento, para registrar em comentário de classe: com `chunk_detalhe` igual a 500 e uma chamada por item (mais uma de `price_to_win` quando o item é de catálogo), cada job faz entre 500 e 1.000 chamadas HTTP e leva na ordem de dezenas de segundos — muito abaixo do `retry_after` da fila. Job monolítico por empresa numa conta de 66 mil itens é exatamente o padrão que já causou execução duplicada neste projeto (`project_polos_sync_job_retry_after_bug`).
  </action>
  <acceptance_criteria>
    - `app/Jobs/SyncMlAcervoDetalheJob.php` contém `implements ShouldQueue, ShouldBeUnique`, `public int $timeout = 900` e `uniqueFor(): int` devolvendo `1800`
    - O construtor recebe `int $companyId` e `array $mlItemIds` — nenhum model `Company` serializado no payload do job
    - `uniqueId()` combina `companyId` com um hash dos ids do lote, e não só o `companyId`
    - `C:\xampp\php\php.exe -l app/Jobs/SyncMlAcervoDetalheJob.php` sai sem erro
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=RotacaoDetalhe</automated>
  </verify>
  <done>Cada job da camada cara processa um lote pequeno, com lock que sobrevive ao timeout e sem serializar model na fila.</done>
</task>

<task type="auto">
  <name>Task 3: Gates de cobertura da rotação, de "não avaliado" e de fronteira de escrita</name>
  <files>tests/Unit/Phase134/RotacaoDetalheTest.php</files>
  <read_first>
    - `tests/Feature/Phase86/HistoricoAnunciosTest.php` — `RefreshDatabase`, `Http::fake()` no `setUp()`, helper de criação de `Company` mais `MlToken` ativo
    - `tests/fixtures/phase134/price-to-win.json` e `tests/fixtures/phase134/visits.json` — corpos reais capturados no plano 134-01
    - `app/Services/Mlb/Acervo/MlAcervoDetalheService.php` e `app/Jobs/SyncMlAcervoDetalheJob.php` — o que está sob teste
    - `app/Models/MlAcervoItem.php` — para semear linhas direto, sem passar pela camada barata
  </read_first>
  <action>
Criar `tests/Unit/Phase134/RotacaoDetalheTest.php` com `@group phase134`, `use RefreshDatabase;` e `Http::fake()` alimentado pelas fixtures reais. Seis testes:

1. `rotacao_cobre_todo_o_acervo_em_n_execucoes()` — semeia 70 itens ativos de uma empresa, com `detalhe_coletado_em` nulo. Roda `selecionarFatia($company, 7)` e `coletarDetalhe()` sete vezes seguidas, avançando o relógio com `Carbon::setTestNow()` entre execuções. Assere que ao fim das 7 execuções **nenhum** item ficou com `detalhe_coletado_em` nulo, e que nenhum item foi coletado duas vezes antes de todos terem sido coletados uma. É o gate literal do D-23: cobertura completa em N execuções, sem cauda longa cega.

2. `fatia_prioriza_o_mais_antigo_e_os_nunca_coletados()` — semeia itens com `detalhe_coletado_em` variado, incluindo nulos, e assere que a primeira fatia contém todos os nulos antes de qualquer item já coletado, e que entre os já coletados os mais antigos vêm primeiro.

3. `item_de_catalogo_nao_coberto_fica_nao_avaliado()` — semeia um item com `catalog_listing` verdadeiro e `buybox_status` nulo, **sem** rodar a coleta de detalhe. Assere que `naoAvaliadoBuyBox()` é verdadeiro, que `motivos` não contém `MOTIVO_PERDENDO_CATALOGO`, e que a severidade não subiu por causa disso. É o D-18.

4. `falha_na_camada_cara_nao_avanca_o_frescor_nem_inventa_status()` — configura o `Http::fake()` para devolver erro em `price_to_win`, roda `coletarDetalhe()` e assere que `buybox_status` continua nulo, `detalhe_coletado_em` continua com o valor anterior (ou nulo) e que o item volta na fatia seguinte. Prova que o desenho é auto-corretivo e que erro não vira dado.

5. `camada_cara_nunca_escreve_na_api_do_ml()` — depois de rodar `coletarDetalhe()`, assere com `Http::assertSent()` que nenhuma requisição para `api.mercadolibre.com` usou método diferente de GET (D-11).

6. `camada_cara_nao_apaga_dados_da_camada_barata()` — semeia uma linha **completa** de item ativo, com `title`, `sold_quantity`, `nota_ecf`, `nota_sinais`, `origem` igual a `ecf`, `rascunho_id` e `coletado_em` preenchidos, e `available_quantity` igual a 0 (para que a triagem legítima inclua `MOTIVO_SEM_ESTOQUE`), com `motivos` já contendo esse motivo. Roda `coletarDetalhe()` com sucesso e assere, recarregando a linha:
   - `title`, `sold_quantity`, `nota_ecf`, `nota_sinais`, `origem`, `rascunho_id` e `coletado_em` continuam **exatamente** com os valores semeados;
   - `MOTIVO_SEM_ESTOQUE` **continua** em `motivos` — prova que `triagem()` recebeu o pseudo-payload montado das colunas persistidas, e não um array vazio;
   - só `visitas_30d`, `buybox_status`, `severidade` e `detalhe_coletado_em` mudaram;
   - a linha da série do dia mantém `sold_quantity` e `nota_ecf` já escritos pela camada barata, ganhando apenas `visitas`.
   É o espelho do teste 7 de `ColetaAcervoTest`, fechando a mesma classe de risco na direção inversa.

Nos testes 1, 2, 4 e 6, semear as linhas de `ml_acervo_itens` diretamente (sem passar pela camada barata) — a rotação é uma propriedade da seleção, e amarrar o teste à coleta o tornaria lento e frágil.
  </action>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan test --filter=RotacaoDetalhe` sai com código 0 e roda 6 testes
    - O teste 1 falha se `selecionarFatia()` passar a devolver sempre os mesmos ids (por exemplo, removendo a ordenação por `detalhe_coletado_em`) — provado alterando temporariamente e revertendo, com o resultado no SUMMARY
    - O teste 3 falha se `triagem()` passar a emitir `MOTIVO_PERDENDO_CATALOGO` com `buyboxStatus` nulo — provado alterando temporariamente e revertendo, com o resultado no SUMMARY
    - O teste 6 falha se o `update()` da camada cara passar a incluir qualquer coluna da camada barata, ou se `triagem()` passar a receber um array vazio no lugar do pseudo-payload — provado alterando temporariamente e revertendo, com o resultado no SUMMARY
    - `tests/Unit/Phase134/RotacaoDetalheTest.php` contém `@group phase134`
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase134</automated>
  </verify>
  <done>A cobertura da rotação, o estado "não avaliado" e a fronteira de escrita entre as camadas têm gate automatizado, e falha de coleta comprovadamente não vira dado inventado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aplicação → API do Mercado Livre | Camada cara é a de maior volume de chamadas; token do cliente sai em cada uma |
| resposta de `price_to_win` → status exibido na triagem | Valor de terceiro vira rótulo de problema na tela do usuário |
| camada cara → colunas da camada barata | Dois escritores na mesma linha, com frequências diferentes; um pode apagar o trabalho do outro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-04 | Tampering | `MlAcervoDetalheService` contra `api.mercadolibre.com` | mitigate | Só `MercadoLivreService::get()`; gate de fonte por `grep` sem comentários mais o teste `camada_cara_nunca_escreve_na_api_do_ml()` (D-11) |
| T-134-14 | Spoofing | `buybox_status` derivado de resposta externa | mitigate | Só os quatro valores documentados (`winning`, `sharing_first_place`, `losing`, `listed`) são aceitos; qualquer outro valor ou erro deixa a coluna nula, e nulo em item de catálogo é exibido como "não avaliado" (D-18) — nunca há status inventado |
| T-134-12 | Denial of Service | Fila e rate limit da API | mitigate | Fatia de 1/N do acervo por execução; lote de 500 itens por job mantendo cada job em dezenas de segundos, abaixo do `retry_after`; `ShouldBeUnique` por lote; `comRetry429()` honra `Retry-After` |
| T-134-01 | Information Disclosure | Seleção da fatia | mitigate | `selecionarFatia()` filtra por `company_id` da `Company` recebida; o job carrega `companyId` explícito e resolve a empresa antes de qualquer escrita; o `update()` da linha também é escopado por `company_id` |
| T-134-03 | Information Disclosure | Logs da camada cara | mitigate | Log com tag `[MLB Anuncios]` levando só `company_id` e MLB id; `access_token` nunca logado |
| T-134-26 | Tampering | Escrita da camada cara sobre colunas da camada barata | mitigate | `update()` nomeado com exatamente as 5 chaves de `COLUNAS_CAMADA_CARA`; proibido `upsert()` e `save()` de model inteiro; `triagem()` recebe pseudo-payload montado das colunas persistidas, nunca array vazio; gates de fonte por `sed`+`grep` mais o teste `camada_cara_nao_apaga_dados_da_camada_barata()` |
| T-134-SC | Tampering | npm/composer install | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=Phase134` verde
- Nenhum `Http::`, verbo de escrita ou `sleep(` na fonte de `MlAcervoDetalheService` (fora de comentário)
- Nenhum `::upsert(` nem `->save()` em `MlAcervoDetalheService`
- O `update()` da linha corrente contém só as 5 colunas da camada cara
- `SyncMlAcervoDetalheJob` com `uniqueFor` maior que `timeout` mais backoff
</verification>

<success_criteria>
1. Todo item ativo é coberto pela camada cara dentro de N execuções, provado por teste (D-23).
2. Item de catálogo ainda não coberto aparece como "não avaliado" e não gera motivo de problema (D-18).
3. Falha de coleta não avança o frescor nem preenche o status — o item volta ao topo da fila (D-08).
4. Nenhum verbo de escrita sai para a API do ML (D-11).
5. A camada cara escreve só as colunas dela; nada do que a camada barata gravou é apagado, inclusive os motivos da triagem.
</success_criteria>

<output>
Criar `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-05-SUMMARY.md` ao terminar.
</output>
