---
phase: 134-meus-anuncios-saude-analitica-do-anuncio-publicado
plan: 04
type: execute
wave: 3
depends_on: ["134-02", "134-03"]
files_modified:
  - app/Services/Mlb/Acervo/MlAcervoService.php
  - app/Jobs/SyncMlAcervoCompanyJob.php
  - tests/Unit/Phase134/ColetaAcervoTest.php
autonomous: true
requirements: [D-01, D-03, D-04, D-07, D-09, D-11, D-12, D-17, D-18, D-19, D-20, D-23]

must_haves:
  truths:
    - "A coleta enxerga TODOS os itens da conta ML do cliente, inclusive os que este módulo nunca publicou"
    - "Conta com mais de mil itens é varrida por completo, sem perder item silenciosamente"
    - "Cada anúncio coletado sabe dizer de onde veio: ECF, time ou legado do cliente"
    - "Anúncio com variação vira uma linha, com o agregado que o próprio ML entrega"
    - "Item que falha dentro de um lote não derruba o lote nem a coleta da empresa"
    - "Rodar a camada barata de novo NÃO apaga o que a camada cara já coletou"
  artifacts:
    - path: "app/Services/Mlb/Acervo/MlAcervoService.php"
      provides: "Varredura por scroll_id, multiget de 20 ids e upsert da linha corrente + série diária"
      contains: "search_type"
    - path: "app/Jobs/SyncMlAcervoCompanyJob.php"
      provides: "Coleta da camada barata de uma empresa, na fila"
      contains: "ShouldQueue"
    - path: "tests/Unit/Phase134/ColetaAcervoTest.php"
      provides: "Gates de D-20 (scroll), D-17 (variações), D-04 (origem), D-11 (zero write) e D-23/D-18 (a camada barata preserva os campos da camada cara)"
  key_links:
    - from: "app/Services/Mlb/Acervo/MlAcervoService.php"
      to: "app/Services/MercadoLivreService.php"
      via: "somente get() — token, refresh e retry 429 já resolvidos lá"
      pattern: "\\$this->ml->get\\("
    - from: "app/Services/Mlb/Acervo/MlAcervoService.php"
      to: "app/Services/Mlb/Acervo/AnuncioSaudeService.php"
      via: "avaliar() + triagem() por item, antes do upsert"
      pattern: "->(avaliar|triagem)\\("
    - from: "app/Services/Mlb/Acervo/MlAcervoService.php"
      to: "ml_acervo_itens (colunas da camada cara)"
      via: "3º argumento explícito do upsert — buybox_status, visitas_30d e detalhe_coletado_em ficam DE FORA da lista de update"
      pattern: "COLUNAS_CAMADA_BARATA"
---

<objective>
Construir a camada barata da coleta: varrer o acervo inteiro da conta ML por `scroll_id`, buscar os campos que vêm de graça no multiget de 20 ids, calcular nota e triagem, e gravar a linha corrente por anúncio mais a série diária.

Purpose: é o que faz a tela existir. O D-01 exige o acervo inteiro, não só o que este módulo publicou; o D-20 exige `scroll_id` desde o dia 1 porque `offset` estoura em torno de mil itens e a maior conta tem 66.747; o D-19 separa esta camada (20.347 chamadas, cerca de 14 minutos para 406.932 itens) da camada cara, que é uma chamada por item.
Output: `MlAcervoService`, `SyncMlAcervoCompanyJob` e os gates automatizados de scroll, variações, origem, zero-write e preservação da camada cara.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-CONTEXT.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-RESEARCH.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-PATTERNS.md
@app/Services/MercadoLivreService.php
@app/Services/Mlb/Publicacao/MlCatalogoMetaService.php
@app/Services/Mlb/Acervo/AnuncioSaudeService.php
@app/Models/MlAcervoItem.php
@app/Models/MlAnuncioRascunho.php
@app/Models/Publicacao.php

<interfaces>
<!-- API pública de MlAcervoService (camada barata). -->

```php
namespace App\Services\Mlb\Acervo;

class MlAcervoService
{
    /**
     * Colunas que a CAMADA BARATA é dona e, por isso, as únicas que o upsert atualiza.
     * `buybox_status`, `visitas_30d` e `detalhe_coletado_em` NÃO estão aqui: pertencem
     * à camada cara (plano 134-05) e seriam apagadas a cada execução diária se entrassem.
     */
    private const COLUNAS_CAMADA_BARATA = [
        'title', 'category_id', 'status', 'sub_status', 'listing_type_id', 'price',
        'available_quantity', 'sold_quantity', 'permalink', 'thumbnail', 'fotos_count',
        'has_variations', 'variations', 'catalog_listing', 'catalog_product_id',
        'shipping', 'tags', 'nota_ecf', 'nota_sinais', 'motivos', 'severidade',
        'origem', 'rascunho_id', 'publicacao_vendas_qty', 'publicacao_desconsiderado',
        'coletado_em', 'coleta_erro', 'updated_at',
    ];

    public function __construct(
        private MercadoLivreService $ml,
        private MlCatalogoMetaService $meta,
        private AnuncioSaudeService $saude,
    ) {}

    /**
     * Enumera TODOS os ids de item da conta por scroll_id (D-20).
     * @return \Generator<string>  MLB ids, em streaming — nunca materializa 66 mil ids em memória
     */
    public function enumerarIds(Company $company, ?string $status = null): \Generator;

    /**
     * Camada barata completa de uma empresa: enumera, multiget em lotes de 20,
     * calcula nota/triagem, faz upsert da linha corrente e alimenta a série diária.
     * @return array{itens:int, lotes:int, falhas:int}
     */
    public function coletarCamadaBarata(Company $company): array;
}
```

<!-- Endpoints e parâmetros já confirmados por chamada real (134-RESEARCH.md §1/§2) -->
```
GET /users/{ml_user_id}/items/search?search_type=scan&limit=50[&scroll_id=...][&status=active]
GET /items?ids={ate 20 ids separados por virgula}&attributes=id,status,sub_status,available_quantity,
    sold_quantity,shipping,listing_type_id,tags,catalog_listing,catalog_product_id,variations,title,
    price,permalink,thumbnail,category_id,attributes,pictures

Resposta do multiget: [{ "code": 200, "body": { ...item... } }, ...]
```

<!-- Fronteira de propriedade de coluna entre as duas camadas (contrato desta fase) -->
```
CAMADA BARATA (este plano, roda TODO dia + a cada "Atualizar agora")  -> COLUNAS_CAMADA_BARATA, acima
CAMADA CARA   (plano 134-05, roda em rotação 1/N)                     -> visitas_30d, buybox_status,
                                                                          detalhe_coletado_em
COMPARTILHADAS (as duas escrevem, cada uma recomputando a triagem)    -> motivos, severidade
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MlAcervoService — varredura por scroll_id e multiget de 20 ids</name>
  <files>app/Services/Mlb/Acervo/MlAcervoService.php</files>
  <read_first>
    - `app/Services/MercadoLivreService.php` linhas 240-340 e 400-520 — `ensureValidToken()`, `get()`, `comRetry429()` e o padrão fail-open de `fetchItemStatus()`. O cliente HTTP, o refresh de token e o backoff de 429 já estão resolvidos ali: **reusar, nunca reimplementar**
    - `app/Services/Shopee/ShopeeService.php` método `fetchOrdersSummary` — o único precedente de laço `do...while` por cursor contra API externa neste projeto; empresta a FORMA do laço, não o mecanismo (o `scroll_id` do ML tem TTL de 5 minutos e semântica própria)
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-RESEARCH.md` §2 e `## Code Examples` — o código de scroll e de multiget já testado contra produção; **usar aquele trecho como fonte primária**
    - `config/mlb_acervo.php` — `pagina_scroll` e `lote_multiget`
  </read_first>
  <action>
Criar `app/Services/Mlb/Acervo/MlAcervoService.php` com o construtor e as duas assinaturas públicas do bloco `<interfaces>`.

`enumerarIds()` é um `\Generator` que emite MLB ids um a um. Laço `do...while` sobre `GET /users/{ml_user_id}/items/search` com `search_type=scan` e `limit` vindo de `config('mlb_acervo.pagina_scroll')`; na primeira chamada não manda `scroll_id`, nas seguintes manda o valor devolvido pela chamada anterior; o laço termina quando `results` volta vazio. **Nunca usar `offset`** — a pesquisa capturou o erro real 400 a partir de mil e poucos itens, e as 6 contas grandes passam disso com folga.

Nenhum `sleep` nem delay artificial entre páginas do scroll: o `scroll_id` tem TTL de 5 minutos e a conta de 66.747 itens exige cerca de 1.335 páginas sequenciais. O throttle desta fase mora entre empresas (fan-out do plano 134-07) e na camada cara, nunca na enumeração. Documentar isso em comentário, senão a próxima pessoa "melhora" o código adicionando um sleep e passa a perder itens por expiração de cursor.

Se o scroll estourar o TTL no meio (erro da API mencionando `scroll_id` inválido ou expirado), reiniciar o scroll do zero uma única vez, logando `Log::warning("[MLB Anuncios] scroll expirado, reiniciando — empresa {id}")`. Reiniciar é seguro e idempotente porque o consumo a jusante é upsert por `(company_id, ml_item_id)`. Na segunda expiração, propagar a exceção — repetir indefinidamente esconderia um problema real.

O parâmetro `$status` é opcional e, quando informado, vai como `status=` na querystring. A coleta agendada varre a conta inteira (sem filtro) porque o D-03 diz que a coleta pode varrer tudo e só a primeira tela é que não paga por isso — pausados precisam estar no banco para o filtro de status funcionar.

`buscarLotes()` (privado): recebe um iterável de ids e devolve os `body` dos itens, consumindo `GET /items` com `ids` de no máximo `config('mlb_acervo.lote_multiget')` ids e a lista de `attributes` do bloco `<interfaces>`. A resposta vem no formato de envelope `[{code, body}, ...]`: item com `code` diferente de 200 é **pulado com `Log::warning`**, sem abortar o lote nem a empresa — é o mesmo fail-open de `fetchItemStatus()`, e com 500 mil itens algum vai falhar todo dia.

Toda chamada HTTP sai por `$this->ml->get($company, ...)`. Nenhuma chamada direta a `Http::`, e nenhum verbo além de `get` neste arquivo (D-11).
  </action>
  <acceptance_criteria>
    - `app/Services/Mlb/Acervo/MlAcervoService.php` contém `'search_type' => 'scan'` e `scroll_id`
    - `grep -v '^\s*\(//\|\*\)' app/Services/Mlb/Acervo/MlAcervoService.php | grep -c 'offset'` devolve `0`
    - `grep -v '^\s*\(//\|\*\)' app/Services/Mlb/Acervo/MlAcervoService.php | grep -cE '(->post\(|->put\(|->delete\(|->patch\(|Http::)'` devolve `0`
    - `grep -v '^\s*\(//\|\*\)' app/Services/Mlb/Acervo/MlAcervoService.php | grep -c 'sleep('` devolve `0`
    - `public function enumerarIds(Company $company, ?string $status = null): \Generator` existe com essa assinatura
    - `C:\xampp\php\php.exe -l app/Services/Mlb/Acervo/MlAcervoService.php` sai sem erro
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=ColetaAcervo</automated>
  </verify>
  <done>A varredura enumera o acervo inteiro por cursor e o multiget traz os campos baratos em lotes de 20, com fail-open por item.</done>
</task>

<task type="auto">
  <name>Task 2: Persistência — upsert da linha corrente, selo de origem e série diária</name>
  <files>app/Services/Mlb/Acervo/MlAcervoService.php</files>
  <read_first>
    - `app/Models/MlAcervoItem.php` — colunas, casts e as constantes `ORIGEM_*`, `MOTIVO_*`, `SEVERIDADE_*`
    - `app/Models/MlAcervoMetricaDiaria.php` — a série imutável
    - `app/Models/MlAnuncioRascunho.php` — as três colunas que identificam item nascido neste módulo: `ml_item_id`, `ml_item_id_classico`, `ml_item_id_premium`
    - `app/Models/Publicacao.php` — `mlb_code` (string 20, unique), `vendas_qty`, `desconsiderado`, e o scope `considerado()` na linha 171, que **não** entra aqui
    - `app/Services/Mlb/Publicacao/MlCatalogoMetaService.php` linhas 80-100 — `atributos($categoryId)` com `Cache::remember` e TTL de 7 dias; é o cache por categoria que torna o sinal de ficha obrigatória barato
    - `app/Services/Mlb/Acervo/AnuncioSaudeService.php` — `avaliar()` e `triagem()`
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-05-PLAN.md` bloco `<interfaces>` — quais colunas pertencem à camada cara; esta task precisa saber **quais NÃO tocar**
  </read_first>
  <action>
Estender `MlAcervoService` com a persistência, terminando o método público `coletarCamadaBarata(Company $company)`.

Para cada item recebido do multiget:
1. Resolver a definição de atributos da categoria por `$this->meta->atributos($item['category_id'])`, que já cacheia por 7 dias. São poucas categorias por conta contra milhares de itens — é o que impede uma chamada por item.
2. Chamar `$this->saude->avaliar($item, $atributosCategoria)` e guardar `nota` e `sinais`.
3. Chamar `$this->saude->triagem($item, $sinais, $buyboxStatusJaPersistido)` — o status de buy box vem da linha já existente no banco (a camada cara escreve nela, o plano 134-05), nunca de chamada nova aqui. Item nunca coberto pela rotação entra com nulo e, por D-18, não gera o motivo de catálogo. **`$buyboxStatusJaPersistido` é de LEITURA ESTRITA**: existe só para alimentar `triagem()` e **nunca** é reescrito no banco por esta camada. Carregar o mapa `ml_item_id => buybox_status` da empresa uma única vez antes do laço de lotes (`select('ml_item_id','buybox_status')->pluck(...)`), não item a item.
4. Derivar o selo de origem (D-04), nesta ordem de precedência: se o MLB id casar com `ml_item_id`, `ml_item_id_classico` ou `ml_item_id_premium` de algum `MlAnuncioRascunho` da mesma empresa, origem é `ORIGEM_ECF` e `rascunho_id` recebe o id do rascunho; senão, se casar com `Publicacao.mlb_code` da mesma empresa, origem é `ORIGEM_TIME` e as colunas `publicacao_vendas_qty` e `publicacao_desconsiderado` recebem os valores daquela publicação; senão origem é `ORIGEM_LEGADO`. **O scope `considerado()` não entra nessa consulta** — a tela LISTA, não conta (regra dura do D-04 e do `Publicacao.php:171`); aplicar o scope aqui esconderia da tela justamente o anúncio que o time marcou como fora de metodologia, que é o que o usuário mais precisa ver.
5. Montar o array da linha com **exatamente** as chaves de `COLUNAS_CAMADA_BARATA` mais `company_id` e `ml_item_id`, com `coletado_em` igual a `now()` e `coleta_erro` nulo. `available_quantity` e `sold_quantity` vêm **do nível do item-pai**, que é onde o ML já entrega o agregado das variações (D-17) — não somar as variações à mão. `has_variations` é `variations` não vazio; `variations` guarda o payload bruto; `fotos_count` é a contagem de `pictures`. **O array não carrega as chaves `buybox_status`, `visitas_30d` nem `detalhe_coletado_em` — nem com valor nulo.** Em item novo, essas três nascem nulas pelo default do schema, que é a representação correta de "não avaliado" (D-18); em linha que já existe, a ausência delas no array é o que garante que a camada barata não as toque.

Eficiência da resolução de origem: **não** consultar `MlAnuncioRascunho` e `Publicacao` item a item. Antes do laço de lotes, carregar em memória, por empresa, os mapas de MLB id para rascunho e de `mlb_code` para publicação (uma consulta cada, com `select` das colunas necessárias, `pluck`/`keyBy`). São dezenas ou centenas de linhas por empresa contra dezenas de milhares de itens.

**Escrita da linha corrente em lotes — o 3º argumento do `upsert()` é OBRIGATÓRIO e é uma lista literal.** Acumular os arrays e chamar, a cada lote de multiget:

`MlAcervoItem::upsert($lote, ['company_id', 'ml_item_id'], self::COLUNAS_CAMADA_BARATA)`

nunca uma query por item. `COLUNAS_CAMADA_BARATA` é a constante privada declarada no bloco `<interfaces>`, escrita literalmente na classe, com as 28 colunas listadas lá.

**`buybox_status`, `visitas_30d` e `detalhe_coletado_em` NÃO entram nessa lista, e é proibido omitir o 3º argumento.** No Laravel 12 (`Illuminate\Database\Eloquent\Builder::upsert()`), omitir o 3º argumento faz atualizar **todas** as chaves presentes no array de valores. Esta camada roda todo dia pelo agendamento do D-06 **e** a cada clique em "Atualizar agora" (o plano 134-07 despacha só `SyncMlAcervoCompanyJob`, nunca o job da camada cara). Deixar as três colunas entrarem no update apagaria o trabalho da rotação a cada passagem: a economia de 587 mil para 84 mil chamadas por dia que o D-23 existe para obter iria a zero, e todo item já avaliado voltaria ao estado "não avaliado" do D-18 — que passaria a mentir por omissão em vez de dizer a verdade. Escrever esse motivo em comentário na própria linha do `upsert()`, citando o D-23 e o D-18: sem o comentário, a próxima pessoa "simplifica" removendo o argumento e nada na tela denuncia o estrago.

**Série diária (D-07b) — decisão do planner, registrada aqui:** a linha do dia em `ml_acervo_metricas_diarias` só é gravada quando algum dos três campos rastreados (`visitas`, `sold_quantity`, `nota_ecf`) difere da última linha existente daquele item. Motivo: gravar sempre chega a cerca de 45 milhões de linhas em regime (500 mil itens vezes 90 dias), e em contas de autopeças — que são justamente as maiores — o catálogo é estático e a maioria das linhas seria idêntica à anterior. Consequência assumida e tratada: a série tem buracos por desenho, e o consumidor (plano 134-10) faz preenchimento para frente de `sold_quantity` e `nota_ecf`, que são campos de ESTADO, e **não** faz para `visitas`, que é campo de FLUXO — buraco em visitas é ausência real de coleta, e o gráfico o exibe como buraco com `connectNulls={false}`, conforme o `134-UI-SPEC.md`. Documentar essa decisão em comentário de método, com esses três termos: ESTADO, FLUXO, e a conta dos 45 milhões.

O upsert da série usa `updateOrCreate` pela chave `(company_id, ml_item_id, data)` com `data` igual a `now()->toDateString()` — duas execuções no mesmo dia atualizam a mesma linha em vez de duplicar. A camada barata **não** escreve a coluna `visitas` da série (quem a alimenta é a camada cara, plano 134-05): passar `visitas => null` num `updateOrCreate` apagaria a visita que a rotação já tinha gravado no mesmo dia. É a mesma proibição do upsert da linha corrente, aplicada à série.

Toda exceção por item é capturada com `try/catch (\Throwable $e)`, logada com `Log::warning("[MLB Anuncios] ...")` incluindo `company_id` e o MLB id, e conta em `falhas` no retorno. Falha em nível de empresa (token inválido, por exemplo) grava `coleta_erro` nas linhas existentes daquela empresa — atualização nomeada só dessa coluna, nunca um upsert de linha inteira — e propaga a exceção para o job tratar; o D-08 exige que a tela saiba **por que** a coleta está velha, não só que está.
  </action>
  <acceptance_criteria>
    - `app/Services/Mlb/Acervo/MlAcervoService.php` contém `MlAcervoItem::upsert(` com o array de chave única `['company_id', 'ml_item_id']` e com **três** argumentos — o terceiro é `self::COLUNAS_CAMADA_BARATA`, nunca omitido
    - `grep -A2 'MlAcervoItem::upsert(' app/Services/Mlb/Acervo/MlAcervoService.php | grep -c 'COLUNAS_CAMADA_BARATA'` devolve valor maior que `0` — a forma de 2 argumentos não passa neste gate
    - As três colunas da camada cara não aparecem na lista de update: `sed -n '/COLUNAS_CAMADA_BARATA = \[/,/\];/p' app/Services/Mlb/Acervo/MlAcervoService.php | grep -cE "'(buybox_status|visitas_30d|detalhe_coletado_em)'"` devolve `0`
    - Nenhuma das três é atribuída no array de valores do lote: `grep -v '^\s*\(//\|\*\)' app/Services/Mlb/Acervo/MlAcervoService.php | grep -cE "'(buybox_status|visitas_30d|detalhe_coletado_em|visitas)'\s*=>"` devolve `0`
    - Existe comentário na linha do `upsert()` citando `D-23` e `D-18` e explicando por que as três colunas ficam de fora
    - `grep -v '^\s*\(//\|\*\)' app/Services/Mlb/Acervo/MlAcervoService.php | grep -c 'considerado()'` devolve `0`
    - Os três nomes de coluna `ml_item_id_classico`, `ml_item_id_premium` e `mlb_code` aparecem no arquivo (resolução de origem completa)
    - O comentário do método da série contém as palavras `ESTADO`, `FLUXO` e o número `45`
    - `C:\xampp\php\php.exe -l app/Services/Mlb/Acervo/MlAcervoService.php` sai sem erro
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=ColetaAcervo</automated>
  </verify>
  <done>Cada item coletado vira uma linha corrente com nota, motivos, severidade e selo de origem; a série diária cresce só quando algo muda; e nenhuma coluna da camada cara é tocada.</done>
</task>

<task type="auto">
  <name>Task 3: Job por empresa + gates de scroll, variações, origem, zero-write e preservação da camada cara</name>
  <files>app/Jobs/SyncMlAcervoCompanyJob.php, tests/Unit/Phase134/ColetaAcervoTest.php</files>
  <read_first>
    - `app/Jobs/SyncMlCompanyJob.php` — arquivo inteiro: `implements ShouldQueue`, traits, `$tries`, `$timeout`, `backoff()`, `handle(MercadoLivreService $ml)`, `failed(\Throwable $e)` com `Log::error` citando empresa e nome
    - `app/Jobs/PublicarAnuncioMlJob.php` — o padrão `ShouldBeUnique` com `uniqueId()` e `uniqueFor()` maior que `timeout` mais o backoff máximo
    - `tests/Feature/Phase86/HistoricoAnunciosTest.php` — `Http::fake()` no `setUp()`, helper `criarFixture()` que monta `Company` mais `MlToken` ativo, `@group`
    - `tests/fixtures/phase134/scroll-pagina-1.json`, `scroll-pagina-2.json`, `scroll-pagina-3.json`, `multiget-lote.json`, `multiget-item-com-variacoes.json` — payload real capturado no plano 134-01, que é o corpo do `Http::fake()`
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-RESEARCH.md` `## Common Pitfalls` item 1 — por que a camada barata e a cara moram em jobs diferentes
  </read_first>
  <action>
Criar `app/Jobs/SyncMlAcervoCompanyJob.php` no molde de `SyncMlCompanyJob`: `implements ShouldQueue, ShouldBeUnique`, construtor com `public readonly Company $company`, `$tries = 3`, `$timeout = 1800` (a camada barata da maior conta são cerca de 3.340 chamadas de multiget; meia hora dá folga larga), `backoff()` devolvendo `[60, 300, 900]`, `uniqueId()` devolvendo o id da empresa e `uniqueFor()` devolvendo 3600 — sempre maior que `timeout` mais o backoff máximo, senão o lock some antes do job terminar e a mesma empresa é processada duas vezes.

`handle(MlAcervoService $acervo)` chama `coletarCamadaBarata($this->company)` e loga o resumo com `Log::info("[MLB Anuncios] camada barata empresa {id} ({nome}): {itens} itens, {lotes} lotes, {falhas} falhas")`.

`failed(\Throwable $e)` loga `Log::error("[MLB Anuncios] falha definitiva na coleta da empresa {id} ({nome}): {mensagem}")` e grava `coleta_erro` nas linhas de `ml_acervo_itens` daquela empresa — `update(['coleta_erro' => ...])` nomeado, nunca upsert de linha inteira — para o banner de defasagem do D-08 poder dizer o motivo.

Este job cobre **só** a camada barata. A camada cara vive em job separado (plano 134-05), por decisão do RESEARCH `Pitfall 1`: um job monolítico numa conta de 66 mil itens estouraria o `retry_after` da fila e o worker pegaria o mesmo job de novo, duplicando chamadas à API — padrão que este projeto já viveu em `project_polos_sync_job_retry_after_bug`.

Criar `tests/Unit/Phase134/ColetaAcervoTest.php` com `@group phase134`, `use RefreshDatabase;` e `Http::fake()` no `setUp()` alimentado pelas fixtures reais. Sete testes:

1. `varredura_usa_scroll_e_nao_offset()` — encadeia as 3 fixtures de scroll mais uma resposta final com `results` vazio, roda `enumerarIds()` e assere que os ids devolvidos são a união das 3 páginas sem repetição. Depois assere, com `Http::assertSent()`, que nenhuma requisição a `/items/search` levou o parâmetro `offset` e que todas levaram `search_type=scan` (D-20).

2. `conta_com_mais_de_mil_itens_e_varrida_por_completo()` — monta um fake com 25 páginas de 50 ids (1.250 itens, acima do penhasco real de mil e pouco) e assere que `enumerarIds()` devolve os 1.250 ids distintos. É o teste que falharia se alguém trocar o cursor por offset.

3. `item_com_variacoes_grava_uma_linha_com_o_agregado()` — usa `multiget-item-com-variacoes.json`, roda `coletarCamadaBarata()` e assere que existe **uma** linha em `ml_acervo_itens` para aquele MLB id, com `has_variations` verdadeiro, `variations` com a mesma contagem de variações do payload, e `available_quantity` igual ao valor do item-pai (não à soma feita à mão) — D-17.

4. `selo_de_origem_cobre_os_tres_casos()` — semeia um `MlAnuncioRascunho` com `ml_item_id` igual ao id do primeiro item da fixture, uma `Publicacao` com `mlb_code` igual ao do segundo, e deixa o terceiro sem nada. Roda a coleta e assere `ORIGEM_ECF`, `ORIGEM_TIME` e `ORIGEM_LEGADO` respectivamente, mais `rascunho_id` preenchido no primeiro e `publicacao_vendas_qty` no segundo (D-04). Semear a `Publicacao` com `desconsiderado` verdadeiro e assere que **mesmo assim** ela aparece — o scope `considerado()` não entra na listagem.

5. `serie_diaria_so_grava_quando_algo_muda()` — roda `coletarCamadaBarata()` duas vezes com a mesma fixture e assere que `ml_acervo_metricas_diarias` tem exatamente 1 linha por item. Depois altera `sold_quantity` na fixture, avança a data com `Carbon::setTestNow()`, roda de novo e assere que veio uma segunda linha.

6. `coleta_nunca_escreve_na_api_do_ml()` — depois de rodar a coleta completa, assere com `Http::assertSent()` que **nenhuma** requisição para `api.mercadolibre.com` usou método diferente de GET (D-11). Este teste é o gate global de escrita do caminho de coleta.

7. `camada_barata_preserva_dados_da_camada_cara_ja_coletados()` — semeia em `ml_acervo_itens` uma linha da empresa com o MLB id do primeiro item de `multiget-lote.json` já **preenchida pela camada cara**: `buybox_status` igual a `winning`, `visitas_30d` igual a 137 e `detalhe_coletado_em` numa data conhecida (por exemplo, ontem), mais valores propositalmente velhos nas colunas baratas (`sold_quantity` diferente do da fixture). Roda `coletarCamadaBarata()` uma vez e assere, recarregando a linha do banco:
   - `buybox_status` continua `winning`, `visitas_30d` continua 137 e `detalhe_coletado_em` continua com a data semeada — **os três sobrevivem intactos**;
   - `sold_quantity` e `coletado_em` foram atualizados com o valor novo — a camada barata de fato escreveu o que é dela;
   - a linha da série do dia mantém a `visitas` que já existia (se semeada), em vez de virar nula.
   Também rodar `coletarCamadaBarata()` uma **segunda** vez e reconferir os três campos: o estrago do upsert sem 3º argumento aparece já na primeira passagem, mas a rotação do D-23 vive de execuções repetidas e o teste precisa refletir isso. Sem este teste, o upsert errado zeraria a rotação em silêncio a cada execução diária e a cada clique em "Atualizar agora", e nenhum outro gate da fase perceberia.
  </action>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan test --filter=ColetaAcervo` sai com código 0 e roda 7 testes
    - O teste 1 falha se `search_type=scan` for removido do service — provado alterando temporariamente e revertendo, com o resultado no SUMMARY
    - O teste 6 falha se um `->post(` for adicionado ao caminho de coleta — provado alterando temporariamente e revertendo, com o resultado no SUMMARY
    - O teste 7 falha se o 3º argumento do `upsert()` for omitido ou se `buybox_status`/`visitas_30d`/`detalhe_coletado_em` forem acrescentados a `COLUNAS_CAMADA_BARATA` — provado alterando temporariamente e revertendo, com o resultado no SUMMARY
    - `app/Jobs/SyncMlAcervoCompanyJob.php` contém `implements ShouldQueue, ShouldBeUnique`, `public int $timeout = 1800` e `uniqueFor(): int` devolvendo valor maior que 1800
    - `C:\xampp\php\php.exe -l app/Jobs/SyncMlAcervoCompanyJob.php` sai sem erro
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase134</automated>
  </verify>
  <done>A camada barata roda numa fila, por empresa, com lock que sobrevive ao timeout, e os cinco comportamentos que mais podem quebrar em silêncio têm gate automatizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API do Mercado Livre → banco do ECF | Payload de terceiro entra direto em colunas persistidas e vira conteúdo de tela |
| aplicação → API do Mercado Livre | Token OAuth do cliente sai a cada chamada; verbo de escrita mudaria a conta real |
| empresa A → empresa B | O mesmo job roda para 74 empresas com tokens diferentes |
| camada barata → colunas da camada cara | Dois escritores na mesma linha, com frequências diferentes; um pode apagar o trabalho do outro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-04 | Tampering | `MlAcervoService` contra `api.mercadolibre.com` | mitigate | Só `MercadoLivreService::get()`; gate de fonte (`grep` sem comentários por `->post(`/`->put(`/`->delete(`/`->patch(`/`Http::` devolvendo 0) mais o teste `coleta_nunca_escreve_na_api_do_ml()` com `Http::assertSent()` (D-11) |
| T-134-01 | Information Disclosure | Upsert e resolução de origem | mitigate | `company_id` do `Company` recebido no job entra em toda escrita e em toda consulta de rascunho/publicação; a chave de upsert `(company_id, ml_item_id)` impede que o item de uma empresa sobrescreva o de outra que tenha o mesmo MLB id |
| T-134-03 | Information Disclosure | Logs da coleta | mitigate | `Log::info`/`Log::warning` com tag `[MLB Anuncios]` levam só `company_id`, nome da empresa e MLB id; `access_token` nunca entra em log, seguindo `MercadoLivreService::fetchItemStatus()` |
| T-134-12 | Denial of Service | Fila e API do ML | mitigate | `ShouldBeUnique` com `uniqueFor` maior que `timeout` mais backoff evita processamento duplicado da mesma empresa (Pitfall 1); `comRetry429()` honra `Retry-After`; a camada cara fica em job separado para o tempo por job continuar previsível |
| T-134-13 | Tampering | Payload de item malformado | mitigate | Envelope do multiget com `code` diferente de 200 é pulado com log; `try/catch (\Throwable)` por item conta em `falhas` sem abortar a empresa; `AnuncioSaudeService` já acessa o payload por caminho tolerante |
| T-134-26 | Tampering | Upsert da camada barata sobre colunas da camada cara | mitigate | 3º argumento do `upsert()` obrigatório e literal (`COLUNAS_CAMADA_BARATA`), sem `buybox_status`, `visitas_30d` nem `detalhe_coletado_em`; essas chaves também não entram no array de valores; gates de fonte por `sed`+`grep` mais o teste `camada_barata_preserva_dados_da_camada_cara_ja_coletados()`. Sem isso, a rotação do D-23 seria zerada a cada execução e o D-18 passaria a mentir, marcando como "não avaliado" item que já foi avaliado |
| T-134-SC | Tampering | npm/composer install | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=Phase134` verde
- Nenhuma ocorrência de `offset`, `Http::` ou verbo de escrita na fonte de `MlAcervoService` (fora de comentário)
- `MlAcervoItem::upsert` usa `['company_id','ml_item_id']` como chave e `self::COLUNAS_CAMADA_BARATA` como 3º argumento
- `buybox_status`, `visitas_30d` e `detalhe_coletado_em` não aparecem em `COLUNAS_CAMADA_BARATA` nem no array de valores
- `considerado()` não aparece em `MlAcervoService`
</verification>

<success_criteria>
1. A coleta enumera o acervo inteiro por `scroll_id` e sobrevive a conta com mais de mil itens (D-01, D-20).
2. Anúncio com variação vira uma linha, com o agregado do item-pai (D-17).
3. Cada linha sabe se o anúncio nasceu no ECF, foi publicado pelo time ou é legado do cliente (D-04).
4. Nenhum verbo de escrita sai para a API do ML no caminho de coleta (D-11).
5. A série diária cresce só quando algo muda, e o desenho registra explicitamente o que é ESTADO e o que é FLUXO (D-07).
6. Rodar a camada barata não apaga `buybox_status`, `visitas_30d` nem `detalhe_coletado_em` — a rotação do D-23 sobrevive à execução diária e ao "Atualizar agora", e "não avaliado" (D-18) continua significando o que diz.
</success_criteria>

<output>
Criar `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-04-SUMMARY.md` ao terminar.
</output>
