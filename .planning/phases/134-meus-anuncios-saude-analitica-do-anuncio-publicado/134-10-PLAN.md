---
phase: 134-meus-anuncios-saude-analitica-do-anuncio-publicado
plan: 10
type: execute
wave: 7
depends_on: ["134-09"]
files_modified:
  - routes/mlb_anuncios.php
  - app/Http/Controllers/MlbAnuncioController.php
  - resources/js/Pages/Mlb/components/ModalDetalheAnuncio.jsx
  - resources/js/Pages/Mlb/MeusAnuncios.jsx
  - tests/Feature/Phase134/DetalheAnuncioTest.php
autonomous: false
requirements: [D-05, D-07, D-10, D-11, D-18, D-21, D-22]

must_haves:
  truths:
    - "Ao abrir um anúncio, o usuário vê quais sinais faltaram e quanto cada um vale"
    - "A soma dos sinais do checklist é exatamente a nota exibida, e o total é sempre 86"
    - "A evolução do anúncio aparece como série de até 90 dias, com buraco onde não houve coleta — nunca interpolado"
    - "O detalhe não vem no payload da página: carrega ao abrir"
  artifacts:
    - path: "app/Http/Controllers/MlbAnuncioController.php"
      provides: "Action detalheAnuncio() com checklist de sinais e série de 90 dias"
      contains: "public function detalheAnuncio"
    - path: "resources/js/Pages/Mlb/components/ModalDetalheAnuncio.jsx"
      provides: "Modal com bloco de saúde, checklist e gráfico Recharts"
      contains: "connectNulls"
  key_links:
    - from: "resources/js/Pages/Mlb/components/ModalDetalheAnuncio.jsx"
      to: "mlb.anuncios.meus.detalhe"
      via: "window.axios.get lazy ao abrir o modal"
      pattern: "meus\\.detalhe"
    - from: "app/Http/Controllers/MlbAnuncioController.php"
      to: "ml_acervo_metricas_diarias"
      via: "série dos últimos 90 dias, escopada por company_id e ml_item_id"
      pattern: "MlAcervoMetricaDiaria"
---

<objective>
Entregar o segundo nível de leitura da tela: abrir um anúncio e ver, sem sair da página, quais sinais da nota faltaram, quanto cada um vale, e como visitas, vendas e nota evoluíram nos últimos 90 dias.

Purpose: o D-10 pede que a divergência entre as medidas de saúde seja diagnóstico, não só rótulo — e diagnóstico exige o breakdown. O D-07 pediu "evolução" explicitamente, e sem a série ela não existe. O D-22 exige que a conta mostrada feche com a nota, e é aqui, no checklist, que ela é mostrada.
Output: rota e action de detalhe, `ModalDetalheAnuncio.jsx` com o gráfico Recharts, e o teste que trava a soma do checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md
@.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-CONTEXT.md
@.planning/learnings/desempenho-bonificacao.md
@routes/mlb_anuncios.php
@app/Http/Controllers/MlbAnuncioController.php
@app/Models/MlAcervoItem.php
@app/Models/MlAcervoMetricaDiaria.php
@app/Services/Mlb/Acervo/AnuncioSaudeService.php
@resources/js/Pages/Dashboard/MeuPainel.jsx
@resources/js/Components/ui/dialog.jsx
@resources/js/Pages/Mlb/MeusAnuncios.jsx

<interfaces>
<!-- Rota nova, dentro do mesmo grupo role:admin -->
```php
Route::get('/meus/{company}/{mlItemId}/detalhe', [MlbAnuncioController::class, 'detalheAnuncio'])
    ->where('mlItemId', 'MLB[0-9]+')
    ->name('meus.detalhe');
```

<!-- Resposta JSON de detalheAnuncio() -->
```
{
  "item": {
    "ml_item_id", "titulo", "thumbnail", "permalink", "origem", "rascunho_id",
    "status", "listing_tier", "estoque", "vendas", "visitas", "visitas_dias",
    "buybox_status", "buybox_nao_avaliado", "nota_ecf", "nota_base": 86, "motivos"
  },
  "checklist": [
    { "chave": "titulo",            "label": "Título ≥ 20 caracteres",            "peso": 12, "ok": true,  "critico": false },
    { "chave": "categoria",         "label": "Categoria definida",                 "peso": 12, "ok": true,  "critico": false },
    { "chave": "ficha_obrigatoria", "label": "Ficha técnica obrigatória completa", "peso": 20, "ok": false, "critico": true  },
    { "chave": "ficha_opcional",    "label": "Ficha técnica opcional ≥ 60%",       "peso": 14, "ok": true,  "critico": false },
    { "chave": "foto",              "label": "Ao menos 1 foto",                    "peso": 16, "ok": true,  "critico": true  },
    { "chave": "dimensoes",         "label": "Dimensões de pacote completas",      "peso":  8, "ok": false, "critico": false },
    { "chave": "preco",             "label": "Preço definido",                     "peso":  4, "ok": true,  "critico": false }
  ],
  "checklistTotal": 86,
  "serie": [ { "data": "2026-05-13", "visitas": 41, "vendas": 3, "notaEcf": 74 }, ... ],
  "saudeMlDisponivel": bool
}
```

<!-- Gráfico Recharts — JSX exato aprovado no UI-SPEC (seção Modal de Detalhe, item 4) -->
```jsx
<ResponsiveContainer width="100%" height={220}>
  <LineChart data={serie} margin={{ top: 5, right: 10, left: 0, bottom: 5 }}>
    <CartesianGrid strokeDasharray="3 3" stroke="rgba(255,255,255,0.05)" />
    <XAxis dataKey="data" stroke="rgba(255,255,255,0.1)" tick={{ fontSize: 10, fill: 'rgba(255,255,255,0.4)' }} />
    <YAxis stroke="rgba(255,255,255,0.1)" tick={{ fontSize: 10, fill: 'rgba(255,255,255,0.4)' }} />
    <Tooltip contentStyle={{ background: '#0f1116', border: '1px solid rgba(255,255,255,0.08)' }} />
    <Line type="monotone" dataKey="visitas" name="Visitas" stroke="#60a5fa" strokeWidth={2.5} dot={false} activeDot={{ r: 4 }} connectNulls={false} />
    <Line type="monotone" dataKey="vendas"  name="Vendas"  stroke="#10b981" strokeWidth={2.5} dot={false} activeDot={{ r: 4 }} connectNulls={false} />
    <Line type="monotone" dataKey="notaEcf" name="Nota ECF" stroke="#ffe600" strokeWidth={2.5} dot={false} activeDot={{ r: 4 }} connectNulls={false} />
  </LineChart>
</ResponsiveContainer>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rota + action detalheAnuncio() com checklist e série de 90 dias</name>
  <files>routes/mlb_anuncios.php, app/Http/Controllers/MlbAnuncioController.php</files>
  <read_first>
    - `app/Http/Controllers/MlbAnuncioController.php` action `meus()` (plano 134-07) — o topo com `loadMissing('mlToken')` e `abort_unless`, que se repete aqui sem exceção
    - `app/Http/Controllers/MlbAnuncioController.php` `produtosDoClienteMassa()` — o padrão de action que devolve `JsonResponse` neste controller
    - `app/Models/MlAcervoItem.php` — a coluna `nota_sinais`, que já guarda o breakdown persistido pela coleta
    - `app/Services/Mlb/Acervo/AnuncioSaudeService.php` — `PESOS` e `BASE`, fonte dos rótulos e dos pesos do checklist
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md` seção "Modal de Detalhe do Anúncio" — os 7 sinais com rótulo em pt-BR, quais dois são críticos quando falham, e a nota de rodapé sobre a descrição
    - `.planning/learnings/desempenho-bonificacao.md` — o caso da métrica que não fechava com a própria conta; o checklist é literalmente "a conta mostrada na tela"
  </read_first>
  <action>
Em `routes/mlb_anuncios.php`, acrescentar a rota do bloco `<interfaces>` **dentro do grupo existente**, com a restrição `where('mlItemId', 'MLB[0-9]+')` — o parâmetro vem da URL e o formato de MLB id é conhecido, então restringir na rota elimina a classe inteira de entrada malformada antes de chegar ao controller.

Criar `detalheAnuncio(Request $request, Company $company, string $mlItemId): JsonResponse` em `MlbAnuncioController`.

Topo idêntico ao das outras actions: `loadMissing('mlToken')` e `abort_unless($company->mlToken !== null, 404, 'Empresa sem conta ML conectada.')`. Em seguida, buscar o `MlAcervoItem` por `where('company_id', $company->id)->where('ml_item_id', $mlItemId)->firstOrFail()` — o `company_id` na cláusula é o que impede que um MLB id de outra empresa seja lido trocando a URL.

**Checklist:** montar a partir de `nota_sinais` já persistido na linha, não recalculando nada. Para cada uma das 7 chaves de `AnuncioSaudeService::PESOS`, na ordem do bloco `<interfaces>`, devolver `chave`, `label` em pt-BR (os rótulos da tabela do UI-SPEC), `peso` (lido de `PESOS`, nunca escrito à mão), `ok` (lido de `nota_sinais`) e `critico` verdadeiro apenas para `ficha_obrigatoria` e `foto` — são exatamente os dois sinais que `analisarAnuncio()` classifica como `erro` bloqueante no wizard, e a distinção não é arbitrária.

`checklistTotal` é `AnuncioSaudeService::BASE`, sempre 86 — nunca somado no cliente e nunca 100.

Acrescentar uma asserção defensiva: se a soma dos `peso` dos itens com `ok` verdadeiro for diferente de `nota_ecf` da linha, logar `Log::warning("[MLB Anuncios] checklist não fecha com a nota — empresa {id}, item {mlb}")` e devolver o checklist mesmo assim, com um campo `divergencia` verdadeiro. Não silenciar: a tela precisa poder mostrar que algo está errado em vez de exibir uma conta que não fecha. Esse é o cenário exato que este projeto já viveu com `nps_medio` e `pontos_componentes.nps`.

**Série:** buscar em `ml_acervo_metricas_diarias` as linhas de `(company_id, ml_item_id)` dos últimos 90 dias, ordenadas por `data`. Como a coleta grava só quando algo muda (decisão registrada no plano 134-04), a série vem esparsa. Montar o array de saída com um ponto por dia do intervalo, aplicando:
- `vendas` e `notaEcf`: **preenchimento para frente** — são campos de ESTADO, o último valor conhecido continua válido até a próxima mudança. Dia anterior ao primeiro registro fica nulo.
- `visitas`: **sem preenchimento** — é campo de FLUXO, e buraco significa que a rotação do D-23 não passou por aquele item naquele dia. Interpolar seria inventar tráfego que ninguém mediu, e o `connectNulls={false}` do gráfico existe justamente para mostrar esse buraco.
Comentar essa assimetria no código com os termos ESTADO e FLUXO, e citar o D-08 — é a mesma disciplina de honestidade do selo de defasagem.

`saudeMlDisponivel` vem de `config('mlb_acervo.saude_ml_disponivel')`.

**Nenhuma chamada HTTP nesta action.** Tudo vem do banco (D-05).
  </action>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan route:list --name=mlb.anuncios.meus.detalhe` lista a rota com middleware `role:admin` e a restrição `MLB[0-9]+`
    - A action contém `AnuncioSaudeService::PESOS` e `AnuncioSaudeService::BASE` — nenhum peso nem o total 86 escritos à mão
    - `sed -n '/function detalheAnuncio(/,/^    }/p' app/Http/Controllers/MlbAnuncioController.php | grep -cE '(Http::|MercadoLivreService)'` devolve `0`
    - A action contém `->where('company_id', $company->id)` antes do `firstOrFail()`
    - O comentário do trecho da série contém as palavras `ESTADO` e `FLUXO`
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=DetalheAnuncio</automated>
  </verify>
  <done>O detalhe de um anúncio é servido do banco, com checklist que fecha com a nota e série honesta sobre o que não foi medido.</done>
</task>

<task type="auto">
  <name>Task 2: ModalDetalheAnuncio — bloco de saúde, checklist e gráfico</name>
  <files>resources/js/Pages/Mlb/components/ModalDetalheAnuncio.jsx, resources/js/Pages/Mlb/MeusAnuncios.jsx</files>
  <read_first>
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md` seção "Modal de Detalhe do Anúncio" — a ordem dos 5 blocos, o JSX do gráfico, a tabela de sinais com os dois críticos, a nota de rodapé literal e a copy de erro de carregamento
    - `resources/js/Components/ui/dialog.jsx` — API de `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`
    - `resources/js/Pages/Mlb/AnunciarML.jsx` linhas 2982-2983 — o padrão de override de largura do `DialogContent` já em produção
    - `resources/js/Pages/Dashboard/MeuPainel.jsx` linhas 725-750 — o padrão de estilo de `ResponsiveContainer` mais `LineChart` do projeto (eixos com `fontSize: 10`)
    - `resources/js/Pages/Mlb/MeusAnuncios.jsx` — o handler nomeado deixado no plano 134-08, que agora passa a abrir o modal
  </read_first>
  <action>
Criar `resources/js/Pages/Mlb/components/ModalDetalheAnuncio.jsx`, recebendo `empresaId`, `mlItemId` (ou nulo quando fechado), `saudeMlDisponivel` e `onClose`.

Usar `Dialog` com `DialogContent className="max-w-2xl"` — override do `max-w-lg` padrão, mesmo padrão já em produção no wizard.

Carregamento **lazy**: ao abrir (quando `mlItemId` deixa de ser nulo), chamar `window.axios.get(route('mlb.anuncios.meus.detalhe', { company: empresaId, mlItemId }))` dentro de um `try/finally` que controla o estado de carregamento local, no mesmo padrão de `usarComoTemplate()` do wizard. Enquanto carrega: `Loader2` centralizado mais "Carregando evolução…" — sem skeleton, que não existe em nenhum outro lugar do módulo. Em erro: a copy literal do Copywriting Contract, "Não foi possível carregar a evolução deste anúncio agora.", mais um botão "Tentar de novo".

O detalhe **não** vem no payload da página: 50 itens vezes até 90 pontos de série seriam até 4.500 registros extras que ninguém vê na maioria das aberturas (decisão A10 do UI-SPEC).

Conteúdo, na ordem da seção "Modal de Detalhe" do UI-SPEC:
1. Cabeçalho com foto, título, link do permalink com `ExternalLink` e `aria-label`, e o selo de origem.
2. Bloco de saúde: a barra grande da Nota ECF com o texto "X de 86", a saúde do ML ao lado **apenas** quando `saudeMlDisponivel` for verdadeiro (Variante A), e a lista de motivos ativos com os mesmos chips da triagem.
3. Checklist dos 7 sinais: para cada um, ícone `CheckCircle2` em `text-emerald-400` quando `ok`; quando falhou, `XCircle` em `text-red-400` se `critico`, ou `Circle` em `text-white/20` se não. Rótulo, e o peso à direita. Rodapé do checklist com a nota literal: "Descrição (14 pts) fica fora da nota — exigiria 1 chamada extra por item à API do ML (decisão D-22)." O total exibido é `checklistTotal` vindo do backend, **nunca somado no cliente**. Se a resposta trouxer `divergencia` verdadeira, exibir um aviso âmbar discreto de que a conta não fechou — mostrar o problema, nunca escondê-lo atrás de um número maquiado.
4. Gráfico: o JSX do bloco `<interfaces>`, exatamente como está, importando `ResponsiveContainer`, `LineChart`, `Line`, `XAxis`, `YAxis`, `CartesianGrid` e `Tooltip` de `recharts` (já no `package.json`, nenhuma dependência nova). `connectNulls={false}` é **obrigatório e não é escolha estética**: a rotação do D-23 deixa buracos reais na série de visitas, e interpolá-los mentiria sobre dado que não existe. Abaixo do gráfico, uma nota em Label esclarecendo que visitas é a soma da janela de 30 dias anteriores a cada coleta, não do dia.
5. Rodapé: permalink no Mercado Livre e "Abrir rascunho no wizard", este último só quando `origem` é `ecf` e `rascunho_id` não é nulo. **Nenhuma ação nova** — são as mesmas duas da coluna Ação da tabela.

Em `MeusAnuncios.jsx`, ligar o handler já nomeado no plano 134-08: clicar no título ou na thumbnail de uma linha, ou na célula da Nota ECF, define `mlItemId` e abre o modal; fechar zera o estado. Remover o comentário que apontava para este plano.

Vocabulário visual: os mesmos 4 tamanhos, 2 pesos e espaçamentos múltiplos de 4. Os `fontSize: 10` dos eixos do Recharts são prop interna da biblioteca, renderização SVG, e o próprio UI-SPEC já os declara fora da escala tipográfica de interface — não trocar por classe Tailwind.

**Nenhum controle de escrita no modal** (D-11): sem pausar, sem editar, sem republicar.
  </action>
  <acceptance_criteria>
    - `resources/js/Pages/Mlb/components/ModalDetalheAnuncio.jsx` contém `connectNulls={false}` nas três `<Line>` e `max-w-2xl` no `DialogContent`
    - O arquivo contém as strings literais `Carregando evolução…`, `Não foi possível carregar a evolução deste anúncio agora.` e `Descrição (14 pts) fica fora da nota`
    - O arquivo contém `checklistTotal` e **não** contém `reduce` somando os pesos do checklist no cliente
    - Nenhuma ocorrência de `font-medium`, `font-bold`, `text-xs` nem de classe com meio-passo no arquivo
    - Nenhuma ocorrência de `pausar`, `Pausar`, `em breve` ou `Em breve`
    - `resources/js/Pages/Mlb/MeusAnuncios.jsx` importa e renderiza `ModalDetalheAnuncio`
    - `npm run build` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Abrir um anúncio mostra quais sinais faltaram, quanto cada um vale e como o anúncio evoluiu, com buraco onde não houve medição.</done>
</task>

<task type="auto">
  <name>Task 3: Testes do detalhe — escopo, checklist que fecha e série honesta</name>
  <files>tests/Feature/Phase134/DetalheAnuncioTest.php</files>
  <read_first>
    - `tests/Feature/Phase134/MeusAnunciosTest.php` — os helpers de fixture e o padrão de `Http::fake()` no `setUp()`
    - `app/Http/Controllers/MlbAnuncioController.php` action `detalheAnuncio()` — o que está sob teste
    - `app/Models/MlAcervoMetricaDiaria.php` — para semear a série esparsa
    - `app/Services/Mlb/Acervo/AnuncioSaudeService.php` — `PESOS` e `BASE`
  </read_first>
  <action>
Criar `tests/Feature/Phase134/DetalheAnuncioTest.php` com `@group phase134`, `RefreshDatabase` e `Http::fake()` no `setUp()`. Sete testes:

1. `detalhe_devolve_checklist_com_os_sete_sinais_e_total_86()` — semeia um item com `nota_sinais` conhecido, chama a rota e assere que `checklist` tem 7 entradas, que `checklistTotal` é 86, e que os pesos batem com `AnuncioSaudeService::PESOS`.
2. `soma_dos_sinais_verdadeiros_fecha_com_a_nota()` — para o mesmo item, assere que a soma dos `peso` com `ok` verdadeiro é exatamente `item.nota_ecf`. É a asserção que operacionaliza a pegadinha travada no D-10 e no D-22.
3. `checklist_marca_como_criticos_apenas_ficha_obrigatoria_e_foto()` — assere `critico` verdadeiro só nessas duas chaves.
4. `serie_preenche_estado_para_frente_e_deixa_buraco_em_visitas()` — semeia linhas esparsas (por exemplo, dias 1, 5 e 12 dos últimos 90) e assere que, num dia sem registro, `vendas` e `notaEcf` repetem o último valor conhecido enquanto `visitas` vem nulo. É o gate da assimetria ESTADO contra FLUXO.
5. `detalhe_nao_vaza_item_de_outra_empresa()` — pede o MLB id da empresa B na URL da empresa A e assere 404. T-134-01.
6. `detalhe_nao_faz_chamada_ao_ml()` — assere `Http::assertNothingSent()` depois da chamada (D-05).
7. `consultor_nao_acessa_o_detalhe()` — usuário sem `role:admin` recebe 403 (D-15).

Acrescentar um oitavo teste de defesa: `checklist_divergente_e_sinalizado_nao_silenciado()` — semeia um item cuja `nota_ecf` foi adulterada para não bater com `nota_sinais`, chama a rota e assere que a resposta traz `divergencia` verdadeira. Provar que a inconsistência aparece em vez de ser mascarada é o que separa este caso do histórico de `nps_medio`.
  </action>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan test --filter=DetalheAnuncio` sai com código 0 e roda 8 testes
    - O teste 2 falha se o checklist passar a incluir o sinal de descrição — provado alterando temporariamente e revertendo, com o resultado no SUMMARY
    - O teste 4 falha se `visitas` passar a ser preenchido para frente — provado alterando temporariamente e revertendo, com o resultado no SUMMARY
    - `C:\xampp\php\php.exe artisan test --filter=Phase134` verde no total
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase134</automated>
  </verify>
  <done>O checklist, a série e o escopo por empresa têm gate automatizado, e divergência de conta é sinalizada em vez de silenciada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Conferência visual do modal e fechamento da fase</name>
  <files>nenhum arquivo é modificado — conferência manual e gate de fechamento da fase</files>
  <what-built>
O modal de detalhe do anúncio, aberto ao clicar no título, na foto ou na nota de uma linha: cabeçalho com permalink, bloco de saúde, checklist dos 7 sinais com pesos, gráfico de evolução de 90 dias e rodapé com as duas ações de leitura.
  </what-built>
  <action>
Executar o roteiro de `<how-to-verify>` em ambiente local, na ordem, e registrar o resultado de cada um dos 7 passos no SUMMARY. Semear antes a série esparsa descrita no passo 2, com buracos deliberados, senão o comportamento de `connectNulls` não fica observável. O passo 6 é o gate de fechamento da fase: rodar as quatro suítes e registrar os quatro resultados. O passo 7 é uma proibição: não deployar. Nenhum arquivo de código é alterado nesta task.
  </action>
  <how-to-verify>
1. Rodar `npm run build`.
2. Na empresa de teste, semear série em `ml_acervo_metricas_diarias` para um item, deixando **buracos de propósito** — por exemplo, registros nos dias 1, 5, 12 e 40 dos últimos 90.
3. Abrir `/mlb/anuncios/meus/{company}`, clicar no título de um anúncio e conferir:
   - O modal abre com spinner e "Carregando evolução…", e depois preenche.
   - O checklist mostra os 7 sinais com os pesos 12, 12, 20, 14, 16, 8 e 4, e a **soma dos que passaram é exatamente a nota** mostrada acima.
   - O rodapé do checklist diz que a descrição fica fora da nota, e o total é 86 — nunca 100.
   - Os dois sinais críticos (ficha obrigatória e foto) aparecem em vermelho quando falham; os outros cinco, em cinza neutro.
   - O gráfico mostra as três linhas e a linha de **visitas tem buracos visíveis** onde não houve coleta, sem ligar os pontos por cima do vazio. As linhas de vendas e nota continuam contínuas.
   - Não existe nenhum botão de pausar, editar ou republicar dentro do modal.
4. Fechar e reabrir em outro anúncio, confirmando que o conteúdo troca e não fica dado do anterior na tela.
5. Simular erro de carregamento (por exemplo, apontando temporariamente para um id inexistente) e confirmar que aparece "Não foi possível carregar a evolução deste anúncio agora." com o botão "Tentar de novo".
6. Fechamento da fase: rodar `C:\xampp\php\php.exe artisan test --filter=Phase134`, `C:\xampp\php\php.exe artisan test --filter=Mlb`, `npm run test:js` e `npm run build`, registrando o resultado dos quatro no SUMMARY. A suíte completa sem filtro pode não concluir por um timeout conhecido em `MercadoLivreAdsService` — se acontecer, registrar como conhecido, não como regressão desta fase.
7. **Não deployar.** A autorização de deploy é sempre explícita e separada, e não foi dada nesta fase.
  </how-to-verify>
  <read_first>
    - `resources/js/Pages/Mlb/components/ModalDetalheAnuncio.jsx` — o que foi construído
    - `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-UI-SPEC.md` seção "Modal de Detalhe do Anúncio"
  </read_first>
  <acceptance_criteria>
    - Os 7 itens do roteiro foram executados e o resultado de cada um está no SUMMARY
    - A soma dos sinais que passaram no checklist é igual à nota exibida, conferida à mão em pelo menos 2 anúncios
    - A linha de visitas tem buraco visível onde não houve coleta (D-23 mais D-08)
    - Nenhum controle de escrita existe no modal (D-11)
    - Nenhum comando de deploy foi executado
  </acceptance_criteria>
  <verify>
    <human-check>Usuário confirma que a soma dos sinais que passaram no checklist é igual à nota exibida, conferida à mão em pelo menos 2 anúncios (D-22); que a linha de visitas tem buraco visível onde não houve coleta (D-23 mais D-08); que não existe controle de escrita no modal (D-11); e que as quatro suítes do passo 6 estão registradas no SUMMARY.</human-check>
  </verify>
  <done>Os 7 itens do roteiro foram conferidos, as quatro suítes estão registradas no SUMMARY, nenhum deploy foi executado e o usuário aprovou explicitamente.</done>
  <resume-signal>Digitar "aprovado" ou descrever o que está diferente do contrato de design</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → identificador de anúncio | `mlItemId` vem do caminho da URL e é usado para buscar dado de uma empresa específica |
| navegador → endpoint JSON | Primeiro endpoint da fase que responde JSON em vez de página Inertia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-01 | Information Disclosure | `detalheAnuncio()` com `mlItemId` da URL | mitigate | Busca com `where('company_id', $company->id)` antes do `firstOrFail()`; empresa vem por route model binding sob `role:admin`; teste `detalhe_nao_vaza_item_de_outra_empresa()` prova o 404 |
| T-134-18 | Tampering | `mlItemId` malformado | mitigate | Restrição `where('mlItemId', 'MLB[0-9]+')` na própria rota, rejeitando o formato inválido antes do controller |
| T-134-04 | Tampering | Caminho do detalhe | mitigate | Nenhuma chamada HTTP na action; teste `detalhe_nao_faz_chamada_ao_ml()` com `Http::assertNothingSent()`; nenhuma ação de escrita no modal (D-11) |
| T-134-21 | Repudiation | Conta do checklist que não fecha | mitigate | Asserção defensiva no backend com `Log::warning` e campo `divergencia`; a tela **mostra** a divergência em vez de mascará-la; teste `checklist_divergente_e_sinalizado_nao_silenciado()` |
| T-134-25 | Repudiation | Série interpolada mentindo sobre tráfego | mitigate | `visitas` nunca é preenchida para frente e o gráfico usa `connectNulls={false}`; teste `serie_preenche_estado_para_frente_e_deixa_buraco_em_visitas()` trava a assimetria |
| T-134-19 | Denial of Service | Série de 90 dias por item | mitigate | Carregamento lazy por item aberto, nunca no payload da página; consulta escopada por `(company_id, ml_item_id)` sobre `mamd_company_item_data_unq` |
| T-134-SC | Tampering | npm install | accept | `recharts` já está no `package.json` (versão ^3.8.1); nenhum pacote novo |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=Phase134` verde
- `C:\xampp\php\php.exe artisan test --filter=Mlb` sem regressão contra o baseline
- `npm run test:js` e `npm run build` verdes
- Checkpoint visual aprovado
- Nenhum deploy executado
</verification>

<success_criteria>
1. Abrir um anúncio mostra os 7 sinais com peso, e a soma dos que passaram é exatamente a nota (D-10, D-22).
2. O total do checklist é sempre 86, com a nota de rodapé explicando o sinal ausente (D-22).
3. A evolução de 90 dias aparece com buraco onde não houve coleta, sem interpolação (D-07, D-23).
4. O detalhe carrega ao abrir, não no payload da página, e sem nenhuma chamada ao Mercado Livre (D-05).
5. Divergência entre a nota e o breakdown é sinalizada, não silenciada.
</success_criteria>

<output>
Criar `.planning/phases/134-meus-anuncios-saude-analitica-do-anuncio-publicado/134-10-SUMMARY.md` ao terminar, com o resultado das quatro suítes do item 6 do checkpoint.
</output>
