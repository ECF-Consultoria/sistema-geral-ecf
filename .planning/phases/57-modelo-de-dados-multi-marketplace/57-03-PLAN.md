---
plan: 57-03
phase: 57
wave: 3
depends_on: [57-01, 57-02]
files_modified:
  - .planning/phases/57-modelo-de-dados-multi-marketplace/57-UAT.md
  - .planning/phases/57-modelo-de-dados-multi-marketplace/PHASE-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03]
---

# Wave 3 — Deploy + backfill em prod + UAT + PHASE-SUMMARY

## Objective

Colocar Phase 57 em prod: deploy do código, execução do backfill em prod (169+ empresas migradas), UAT humano (validar Dashboard/Sugadores/Performance carregam), e fechamento formal da phase.

`autonomous: false` porque o backfill em prod grava dados novos (169+ rows na tabela pivot) e o UAT precisa de humano confirmando as 3 rotas críticas.

## Tasks

### Task 1 — Push + deploy do código

<action>
Fluxo padrão da milestone (memory `feedback_perguntar_antes_deploy_v9`: manter cuidado de coordenação com outro dev):

1. Verificar working tree:
```bash
git status --porcelain | grep -v '^??'
```

2. Se há mudanças de outro dev (padrão da milestone atual — 4 arquivos), stash:
```bash
git stash push -m "pre-deploy-57" -- app/Http/Controllers/CompanyController.php \
    app/Http/Controllers/GoalController.php \
    app/Http/Controllers/MercadoLivreOAuthController.php \
    resources/js/Pages/Companies/Show.jsx
```

3. Rebase sobre origin/main (outro dev pode ter pushed):
```bash
git pull --rebase origin main
```

4. Se houver conflito em `AppLayout.jsx` ou outros: resolver manualmente + `git rebase --continue`.

5. Push:
```bash
git push origin main
```

6. Deploy:
```bash
bash deploy.sh
```

7. Verificar output do deploy: `INFO Nothing to migrate` NÃO deve aparecer — deve rodar a migration nova de `company_marketplaces`. Se a migration não subiu, `deploy.sh` executa `php artisan migrate --force` na VPS automaticamente.

8. Confirmar migration em prod (sem `php` local disponível, checar via curl smoke da Task 3 ou SSH direto se necessário).

9. Restore stash:
```bash
git stash pop
```
</action>

<read_first>
- deploy.sh (padrão da milestone)
- git log --oneline -5 (para saber qual commit está indo)
</read_first>

<acceptance_criteria>
1. `git push origin main` retorna sucesso (fast-forward ou rebase resolvido)
2. `bash deploy.sh` termina com "Deploy VPS concluído!"
3. Output do deploy contém `Migration ran` ou similar (pra confirmar que create_company_marketplaces_table executou)
4. `git stash pop` restaura arquivos do outro dev sem conflito
</acceptance_criteria>

### Task 2 — Executar backfill em PROD

<action>
**BLOQUEIO ATIVO:** confirmar com o usuário antes de rodar backfill em prod.

Comando de execução (via plink+SSH, mesmo pattern do deploy.sh):

Primeiro, dry-run pra validar contagens antes de gravar:

```bash
"$(dirname deploy.sh)/plink.exe" -ssh -P 22 -pw "ECF-100376vps" \
  -hostkey "SHA256:rA83w6TmPmvPKqDsjUNZclPFdeiZq4mduyBUhNzvKc8" \
  -batch root@177.7.53.164 \
  "cd /var/www/ecf_admin && php artisan companies:backfill-marketplaces --dry-run"
```

Se dry-run mostra contagens razoáveis (~169 empresas processadas, ~135 meli + 33 shopee + 1 amazon + N extras), executar de verdade:

```bash
"$(dirname deploy.sh)/plink.exe" -ssh -P 22 -pw "ECF-100376vps" \
  -hostkey "SHA256:rA83w6TmPmvPKqDsjUNZclPFdeiZq4mduyBUhNzvKc8" \
  -batch root@177.7.53.164 \
  "cd /var/www/ecf_admin && php artisan companies:backfill-marketplaces"
```

Validar contagem pós-backfill:

```bash
"$(dirname deploy.sh)/plink.exe" -ssh -P 22 -pw "ECF-100376vps" \
  -hostkey "SHA256:rA83w6TmPmvPKqDsjUNZclPFdeiZq4mduyBUhNzvKc8" \
  -batch root@177.7.53.164 \
  "cd /var/www/ecf_admin && php artisan tinker --execute='echo \App\Models\CompanyMarketplace::count() . \" rows / \" . \App\Models\Company::count() . \" empresas\";'"
```

Rows esperadas: >= número de empresas (1 primary/empresa + N extras).
</action>

<read_first>
- deploy.sh (para copiar o padrão plink)
- app/Console/Commands/BackfillCompanyMarketplaces.php (para revisar mensagens de output)
</read_first>

<acceptance_criteria>
1. Dry-run em prod executa sem erro, mostrando ~169 empresas
2. Após confirmação do usuário: execução real termina com relatório `Concluído` + stats
3. Contagem pós-backfill: `CompanyMarketplace::count() >= Company::count()`
4. Zero erros logados (`storage/logs/laravel-*.log` sem entradas `[Phase57]` de nível `ERROR`)
</acceptance_criteria>

### Task 3 — UAT humano em prod

<action>
Criar `.planning/phases/57-modelo-de-dados-multi-marketplace/57-UAT.md`:

```markdown
---
phase: 57
status: pendente
tested_at: null
tested_by: null
environment: prod
---

# Phase 57 — UAT Checklist

**Milestone:** v13.0 Reorganização Multi-Marketplace
**Requirements cobertos:** DATA-01, DATA-02, DATA-03

## Preparação

- [x] Deploy do commit da Wave 1 + Wave 2 concluído
- [x] Migration `create_company_marketplaces_table` executada em prod
- [x] Backfill executado em prod (rows criadas na pivot)

## Checkpoints funcionais

### C1 — Dashboard (DATA-03 smoke em prod)
- [ ] `/dashboard` carrega em <5s sem erro
- [ ] KPIs (GMV, vendas, ROAS) mostram valores idênticos ao pré-deploy (mesmo período)
- [ ] Filtros de período funcionam (30d, 7d, custom)
- [ ] Filtro de empresa funciona (dropdown popula corretamente)
- [ ] Console do navegador sem erros novos

### C2 — Sugadores (DATA-03 smoke em prod)
- [ ] `/sugadores` carrega em <10s (contas grandes podem levar mais)
- [ ] Listagem mostra sugadores por empresa como antes
- [ ] Drilldown `/sugadores/empresa/{id}` de 1 empresa ML carrega
- [ ] Botão "Copiar MLBs" continua funcionando
- [ ] Zero mensagem "Empresa sem adman_account_id" pra empresas que devem funcionar

### C3 — /performance (DATA-03 smoke em prod)
- [ ] `/performance` carrega e mostra ranking geral
- [ ] Tabs Analistas/Estrategistas continuam funcionando
- [ ] EvolucaoDrawer (gráfico) abre pra 1 usuário

### C4 — Modelo de dados (DATA-01, DATA-02)
- [ ] Query direta via `/dev/desenvolvimento` (ou SSH) confirma:
  - `SELECT COUNT(*) FROM company_marketplaces` >= 169
  - `SELECT marketplace, COUNT(*) FROM company_marketplaces GROUP BY marketplace` mostra distribuição correta (~135 meli, ~33 shopee, ~1 amazon + extras)
- [ ] `SELECT * FROM company_marketplaces WHERE is_primary=1` retorna 1 row por empresa
- [ ] Empresa qualquer via tinker: `\App\Models\Company::find(X)->marketplacesAtivos()` retorna coleção correta
- [ ] `\App\Models\Company::find(X)->adman_account_id` retorna o valor esperado (accessor funcionando)

### C5 — Zero regressão em outras áreas
- [ ] Comercial (cadastrar empresa) carrega e funciona
- [ ] Administrativo > Fechamento carrega
- [ ] NPS listagem carrega
- [ ] Publicação > Dashboard carrega

## Fecha

- [ ] Todos os C1-C5 marcados [x]
- [ ] `status: aprovado`, `tested_at: <data>`, `tested_by: <nome>`

Se algum C falhar → `status: gaps_found`, listar itens.
```
</action>

<read_first>
- .planning/phases/56-.../56-UAT.md (padrão de UAT da milestone)
</read_first>

<acceptance_criteria>
1. Arquivo `57-UAT.md` existe com frontmatter YAML válido
2. 5 seções de checkpoints (C1-C5)
3. Após UAT humano: `status` é `aprovado` OU `gaps_found`
</acceptance_criteria>

### Task 4 — PHASE-SUMMARY.md canônico

<action>
Criar `.planning/phases/57-modelo-de-dados-multi-marketplace/PHASE-SUMMARY.md` com formato canônico:

```markdown
---
phase: 57
name: Modelo de dados multi-marketplace
milestone: v13.0
status: complete
completed_at: <data>
requirements_completed: [DATA-01, DATA-02, DATA-03]
plans_delivered: [57-01, 57-02, 57-03]
uat_status: aprovado
deployed_to_prod: true
---

# Phase 57 — PHASE-SUMMARY

**Milestone:** v13.0 Reorganização Multi-Marketplace
**Escopo:** Formalizar modelo N:N Company × Marketplace via tabela pivot nova, sem quebrar consumidores existentes (~8 arquivos que leem `companies.marketplace` continuam iguais).

## O que foi entregue

### Wave 1 — Schema + Model + Accessors + Backfill (TDD)
- Migration `create_company_marketplaces_table`: id, company_id (FK), marketplace, store_id, adman_id, is_primary, active, integracao_status
- Model `CompanyMarketplace` com casts + relacionamento company
- Ajustes em `Company.php`:
  - Relacionamento `marketplaces()` HasMany
  - Helpers: `isInMarketplace`, `marketplacesAtivos`, `primaryMarketplace`, `storeIdFor`
  - Accessors legacy (leem pivot, fallback flat): `adman_account_id`, `ml_store_id`
  - Mutators legacy: escrevem em ambos pivot + coluna flat
- Comando `companies:backfill-marketplaces --dry-run` idempotente
- ADR-DATA-01 documenta o modelo híbrido intencional
- 15 testes verdes (helpers, accessors, backfill)

### Wave 2 — Smoke tests dos consumidores críticos
- DashboardSmokeTest (com pivot / sem pivot fallback)
- SugadoresSmokeTest (listagem + drilldown)
- PerformanceSmokeTest
- 5 smoke tests verdes; total 20 testes Phase57

### Wave 3 — Deploy + backfill em prod + UAT
- Deploy via `deploy.sh`
- Backfill em prod: N rows criadas (~169 empresas × 1 primary + N extras)
- UAT humano: C1-C5 aprovados

## Requirements cobertos

| REQ-ID | Descrição | Como validado |
|--------|-----------|---------------|
| DATA-01 | ADR + modelo N:N | ADR-DATA-01 escrito + migration criada + backfill em prod |
| DATA-02 | Migration + backfill 100% empresas | Comando executou; contagem CompanyMarketplace >= Company |
| DATA-03 | Testes cobrindo queries críticas | 20 testes verdes + UAT em prod C1-C3 |

## Arquivos modificados/criados

- `database/migrations/*create_company_marketplaces_table.php` (NOVO)
- `app/Models/CompanyMarketplace.php` (NOVO)
- `app/Models/Company.php` (helpers + accessors + mutator adicionados)
- `app/Console/Commands/BackfillCompanyMarketplaces.php` (NOVO)
- `database/factories/CompanyMarketplaceFactory.php` (NOVO)
- `.planning/adrs/DATA-01-multi-marketplace-model.md` (NOVO)
- `tests/Feature/Phase57/*` (6 arquivos NOVOS)

**Não modificado (por decisão):**
- `app/Services/AdmanService.php`
- `app/Services/SugadorAnalysisService.php`
- `app/Services/Sugadores/*`
- Comandos que leem `$company->marketplace`

## Decisões implementadas

1. ✓ N:N vazio via pivot; schema legacy preservado em paralelo
2. ✓ Store IDs movem pra pivot com accessor de fallback
3. ✓ Testes: 3 grupos (helpers, accessors, backfill) + 3 smoke (Dashboard, Sugadores, Performance)

## Habilita phases

- **Phase 58** (Dashboard ECF agregado): tabela pivot pronta para queries `SELECT SUM(gmv) FROM ... JOIN company_marketplaces` — dashboard agregado através de marketplaces vira query direta
- **Phase 59** (Desacoplamento áreas transversais): não bloqueada por 57; pode rodar em paralelo

## Notas para consulta futura

- **Modelo híbrido intencional:** colunas flat legacy (`marketplace`, `marketplaces_extras`, `adman_account_id`, `ml_store_id`) coexistem com pivot. Consumidores continuam usando accessors legacy; refactor gradual em milestones futuras.
- **Extensibilidade:** adicionar novo marketplace ('magalu' já no ENUM; adicionar outros altera 2 lugares — migration + `BackfillCompanyMarketplaces::processCompany`).
- **Rollback:** `php artisan migrate:rollback --step=1` remove a pivot; colunas flat ficam intactas; consumidores continuam funcionando.
```

Data de completed_at = data do fechamento do UAT.
</action>

<read_first>
- .planning/phases/56-.../PHASE-SUMMARY.md (padrão canônico da milestone)
- .planning/phases/57-.../57-UAT.md (para saber status e data)
</read_first>

<acceptance_criteria>
1. Arquivo `PHASE-SUMMARY.md` existe com frontmatter completo
2. `requirements_completed: [DATA-01, DATA-02, DATA-03]`
3. `plans_delivered: [57-01, 57-02, 57-03]`
4. Corpo lista Waves 1-3 entregues + requirements table + arquivos modificados
</acceptance_criteria>

### Task 5 — Atualizar ROADMAP + REQUIREMENTS + STATE

<action>
1. **ROADMAP.md** — marcar Phase 57:
   - Linha resumo: `- [ ] **Phase 57:** ...` → `- [x] **Phase 57:** ... *(completed <data>, deployed to prod)*`
   - Detail section `### Phase 57:` → `**Status:** Complete (2026-07-03, deployed)`

2. **REQUIREMENTS.md** — trocar Traceability v13.0:
   - `DATA-01 | Phase 57 | Pending` → `DATA-01 | Phase 57 | Complete`
   - `DATA-02 | Phase 57 | Pending` → `DATA-02 | Phase 57 | Complete`
   - `DATA-03 | Phase 57 | Pending` → `DATA-03 | Phase 57 | Complete`
   - E marcar checkboxes DATA-01..03 na seção do milestone v13.0 como `[x]`

3. **STATE.md** — registrar sessão de fechamento via SDK:
```bash
gsd-sdk query state.record-session \
  --stopped-at "Phase 57 fechada (UAT aprovado em prod, backfill executado)" \
  --resume-file ".planning/phases/57-modelo-de-dados-multi-marketplace/PHASE-SUMMARY.md"
```
</action>

<read_first>
- .planning/ROADMAP.md (linhas ~155 sumário Phase 57 + linhas detalhe)
- .planning/REQUIREMENTS.md (seção Traceability v13.0)
</read_first>

<acceptance_criteria>
1. Grep `- [x] **Phase 57:` em ROADMAP.md retorna 1 match
2. Grep `- [ ] **Phase 57:` retorna 0 matches
3. DATA-01/02/03 na tabela Traceability = Complete
4. STATE.md atualizado via SDK
</acceptance_criteria>

### Task 6 — Commit final + push

<action>
Commit único de fechamento com mensagem canônica pt-BR:

```bash
git add .planning/ROADMAP.md \
        .planning/REQUIREMENTS.md \
        .planning/STATE.md \
        .planning/phases/57-modelo-de-dados-multi-marketplace/PHASE-SUMMARY.md \
        .planning/phases/57-modelo-de-dados-multi-marketplace/57-UAT.md

git commit -m "$(cat <<'EOF'
docs(57): fecha Phase 57 — modelo N:N multi-marketplace (v13.0)

UAT em prod aprovado (<data>) apos deploy + backfill:
- Migration create_company_marketplaces_table executada
- Backfill em prod: N rows criadas (~169 empresas x 1 primary + extras)
- Dashboard/Sugadores/Performance carregam sem regressao (UAT C1-C3)
- Queries diretas confirmam distribuicao correta (meli/shopee/amazon)

Wave 1 (57-01): schema + model + accessors + backfill + 15 tests
Wave 2 (57-02): 5 smoke tests dos 3 consumidores criticos
Wave 3 (57-03): deploy + prod backfill + UAT + fechamento

Modelo hibrido intencional: schema legacy (marketplace ENUM +
marketplaces_extras JSON + adman/ml IDs flat) preservado em paralelo
com pivot. Accessors fazem sync. Consumidores nao precisam refatorar.

Habilita Phase 58 (Dashboard ECF agregado — pivot pronta para queries
agregadas atraves de marketplaces).

Requirements DATA-01, DATA-02, DATA-03: Complete

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"

# Se necessário, rebase sobre origin/main e push
git stash push -m "pre-push-57-close" -- app/Http/Controllers/CompanyController.php \
    app/Http/Controllers/GoalController.php \
    app/Http/Controllers/MercadoLivreOAuthController.php \
    resources/js/Pages/Companies/Show.jsx 2>/dev/null || true

git pull --rebase origin main
git push origin main
git stash pop 2>/dev/null || true
```
</action>

<acceptance_criteria>
1. Commit `docs(57): fecha Phase 57` no git log
2. `git push origin main` retorna sucesso
3. `git status` limpo pra planning/ e arquivos Phase 57
</acceptance_criteria>

## Must haves

- Migration executada em prod (create_company_marketplaces_table)
- Backfill em prod: rows na pivot >= empresas
- UAT humano validado (5 checkpoints)
- PHASE-SUMMARY.md + ROADMAP/REQUIREMENTS/STATE atualizados
- Push do commit de fechamento

## Deferred (NÃO fazer nesta wave)

- Refactor de consumidores (AdmanService, etc) pra usar `marketplaces()` diretamente — milestone futura
- Drop das colunas flat legacy — quando refactor completar
- UI de gestão de marketplaces por empresa — se necessário
- Feature tests dos 5 outros consumidores (comandos de diagnóstico) — smoke em prod cobre
