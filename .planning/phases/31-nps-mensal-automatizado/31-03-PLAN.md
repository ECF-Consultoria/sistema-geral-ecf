---
phase: 31-nps-mensal-automatizado
plan: 03
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - resources/js/Pages/Nps/Respond.jsx
autonomous: true
requirements:
  - REQ-31-06

must_haves:
  truths:
    - "Cliente vê 3 perguntas com escala 1-5 (Estrategista, Analista quando aplicável, Empresa)"
    - "Bloco do Analista só aparece quando survey.tem_analista === true (D-07 mentoria pura)"
    - "Textarea livre com placeholder 'Opiniões, sugestões ou outra coisa que queira compartilhar' (D-08, max 2000)"
    - "Campo nome do respondente é OPCIONAL (D-07 — respondent_name nullable)"
    - "POST envia score_estrategista, score_analista (ou omite), score_empresa, comment, respondent_name para route('nps.submit', token)"
    - "Estética dark theme ecf-bg/ecf-card com tokens existentes (CLAUDE.md constraint)"
  artifacts:
    - path: "resources/js/Pages/Nps/Respond.jsx"
      provides: "Form NPS reescrito 1-5"
      contains: "score_estrategista"
      min_lines: 100
  key_links:
    - from: "resources/js/Pages/Nps/Respond.jsx"
      to: "route('nps.submit', token)"
      via: "useForm().post()"
      pattern: "nps\\.submit"
    - from: "resources/js/Pages/Nps/Respond.jsx"
      to: "survey.tem_analista prop"
      via: "conditional render {survey.tem_analista && ...}"
      pattern: "tem_analista"
---

<objective>
Reescrever a página pública `Nps/Respond.jsx` para a nova escala 1-5 com 3 dimensões (Estrategista, Analista quando aplicável, Empresa) + textarea livre e nome opcional. Substitui o seletor 0-10 atual.

Purpose: Sem a UI nova, o cliente não consegue submeter resposta no novo formato — o backend já validou o schema novo no Plan 31-02.
Output: 1 arquivo JSX reescrito + build verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md
@.planning/phases/31-nps-mensal-automatizado/31-01-SUMMARY.md

@resources/js/Pages/Nps/Respond.jsx
@resources/js/Pages/Nps/ThankYou.jsx

<interfaces>
<!-- Props vindas do NpsController::respond (após Plan 31-02 Task 3b) -->

survey = {
  token: string,
  company_name: string,
  estrategista_name: string | null,
  analista_name: string | null,
  tem_analista: boolean,
}

<!-- Payload do POST para route('nps.submit', token) — validação backend (Plan 31-02 Task 3c) -->

{
  respondent_name?: string|null,          // nullable, max:255
  score_estrategista: 1|2|3|4|5,          // required
  score_analista?: 1|2|3|4|5|null,        // nullable (omitir quando tem_analista=false)
  score_empresa: 1|2|3|4|5,               // required
  comment?: string|null,                  // nullable, max:2000
}

<!-- Componentes UI disponíveis (shadcn-style) -->

import { Button } from '@/Components/ui/button';
import { Input } from '@/Components/ui/input';
import { Label } from '@/Components/ui/label';
import { Textarea } from '@/Components/ui/textarea';
import { cn } from '@/lib/utils';

<!-- Tokens Tailwind do projeto (CLAUDE.md) -->
ecf-bg     = #050507  (background)
ecf-card   = #0f1116  (card)
ecf-yellow = #ffe600  (primary)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrita Nps/Respond.jsx — 3 sliders 1-5 + textarea + nome opcional</name>
  <read_first>
    - resources/js/Pages/Nps/Respond.jsx (arquivo INTEIRO atual — substituição completa)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-06/D-07/D-08)
    - resources/js/Components/ui/button.jsx (verificar variants disponíveis)
  </read_first>
  <files>
    - resources/js/Pages/Nps/Respond.jsx
  </files>
  <action>
    Substituir o conteúdo INTEIRO de `resources/js/Pages/Nps/Respond.jsx` por uma versão nova:

    1. **Componente local `RatingPicker`** (substitui o `ScorePicker` antigo de 11 botões 0-10):
       - Renderiza 5 botões grandes (1 a 5), cada um com tamanho `w-12 h-12 rounded-xl`.
       - Estado visual selecionado:
         - `value === 1`: `bg-red-500 border-red-500 text-white`
         - `value === 2`: `bg-orange-500 border-orange-500 text-white`
         - `value === 3`: `bg-yellow-500 border-yellow-500 text-black`
         - `value === 4`: `bg-lime-500 border-lime-500 text-black`
         - `value === 5`: `bg-emerald-500 border-emerald-500 text-white`
       - Não-selecionado: `border-white/[0.08] bg-white/[0.03] text-white/50 hover:border-ecf-yellow/40 hover:text-ecf-yellow`
       - Abaixo dos botões: 2 labels `text-xs text-white/40` lado-a-lado "Muito ruim" (esq) e "Excelente" (dir)
       - Props: `{ value: number|null, onChange: (v:number)=>void, label: string }`. Label em `text-sm font-medium text-white/80`.

    2. **Componente default `NpsRespond({ survey })`**:
       - `useForm({ respondent_name: '', score_estrategista: null, score_analista: null, score_empresa: null, comment: '' })`
       - **Não** definir `required` HTML no input nome (nullable backend — D-07)
       - Função `submit(e)`: `e.preventDefault(); post(route('nps.submit', survey.token))`
       - Cálculo `isValid`:
         ```
         const isValid = data.score_estrategista !== null
             && data.score_empresa !== null
             && (!survey.tem_analista || data.score_analista !== null);
         ```

    3. **Layout (mesmo padrão visual do arquivo atual, mas com novos tokens)**:
       - Wrapper `min-h-screen bg-ecf-bg flex items-center justify-center p-4`
       - Card central `max-w-xl w-full`:
         - Header: logo ECF (mantém `bg-ecf-yellow text-black` com letra "E"), título "Avaliação de Atendimento", nome empresa `{survey.company_name}`, subtítulo "ECF Consultoria"
         - Container do form `rounded-xl border border-white/[0.08] bg-ecf-card p-6 space-y-6`:
           - Bloco "Seu nome (opcional)" — Input regular
           - Bloco "Avalie de 1 a 5" (h2 `text-sm font-semibold uppercase tracking-wide text-white/70`)
           - `RatingPicker` Estrategista: `label = "O atendimento do " + (survey.estrategista_name || "Estrategista")` — sempre renderiza
           - `RatingPicker` Analista: **somente se `survey.tem_analista === true`**: `label = "O atendimento do " + (survey.analista_name || "Analista")`
           - `RatingPicker` Empresa: `label = "A ECF está atendendo suas expectativas?"`
           - Textarea com Label "Opiniões, sugestões ou outra coisa que queira compartilhar (opcional)", `rows={4}`, `maxLength={2000}`, placeholder igual ao label
           - Mostrar `errors.score_*` e `errors.comment` abaixo de cada campo (text-red-400 text-xs)
           - Button submit ocupa largura total, texto "Enviar avaliação" (processando: "Enviando..."), `disabled={processing || !isValid}`
         - Footer fora do card: `text-center text-white/40 text-xs mt-4` "ECF Consultoria · Suas respostas são confidenciais"

    4. **Acessibilidade mínima**: cada botão de score tem `aria-label={\`Nota ${i}\`}` e `aria-pressed={value === i}`.

    5. NÃO importar Lucide icons novos (manter footprint pequeno).

    6. Após editar, rodar `npm run build` no host — REQUERIDO pelo workflow do projeto (CLAUDE.md "Run npm run build after every frontend change").
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20</automated>
    <human-check>Abrir um survey ativo em `/nps/{token}` e confirmar: (a) só Estrategista + Empresa aparecem quando survey.tem_analista=false; (b) os 3 quando true; (c) 5 botões 1-5 com gradient vermelho→verde; (d) botão Enviar desabilita até preencher; (e) Sem `Seu nome` preenchido envia normal.</human-check>
  </verify>
  <done>
    - Arquivo `resources/js/Pages/Nps/Respond.jsx` totalmente reescrito; grep `score_consultant\|score_mentor\|score_overall` retorna 0 hits (após filtro de comentários `grep -v '^//'`)
    - Grep `score_estrategista\|score_analista\|score_empresa` retorna ≥ 3 hits
    - Grep `survey\.tem_analista` retorna ≥ 1 hit (renderização condicional)
    - `npm run build` termina sem erros nem warnings críticos
    - Bundle Nps/Respond gerado em `public/build/assets/`
  </done>
</task>

</tasks>

<verification>
- `npm run build` verde.
- Grep `Array.from({ length: 11 }` retorna 0 hits (escala 0-10 sumiu).
- Grep `1-5\|1..5\|min={1}` cobre a nova faixa.
- Inspeção manual no browser: 5 botões coloridos, label condicional do Analista, textarea de comentário, nome opcional.
</verification>

<success_criteria>
- UI cliente entrega exatamente a experiência descrita em D-06/D-07/D-08.
- Mentoria pura (sem analista) não vê o slider do analista.
- Form submit alinhado ao endpoint do Plan 31-02 Task 3c (nomes de chaves idênticos).
</success_criteria>

<output>
Criar `.planning/phases/31-nps-mensal-automatizado/31-03-SUMMARY.md` ao final.
</output>
