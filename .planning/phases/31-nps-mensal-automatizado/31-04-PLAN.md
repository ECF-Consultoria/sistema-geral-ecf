---
phase: 31-nps-mensal-automatizado
plan: 04
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - app/Http/Controllers/CompanyController.php
  - app/Http/Controllers/ComercialController.php
  - resources/js/Pages/Companies/Index.jsx
  - resources/js/Pages/Comercial/Empresas.jsx
autonomous: true
requirements:
  - REQ-31-01

must_haves:
  truths:
    - "Admin pode preencher/editar `email_cliente` no modal de criar/editar empresa em /companies"
    - "Comercial pode preencher/editar `email_cliente` no formulário de editar empresa em /comercial/empresas"
    - "CompanyController::store + update validam email_cliente como nullable|email|max:255 e persistem na coluna"
    - "ComercialController::update (route comercial.empresas.update) também valida e persiste"
    - "Edição mantém valor existente (não zera ao salvar sem mexer no campo)"
    - "Listagem GET /companies envia o email_cliente no payload Inertia para o openEdit() pré-preencher"
  artifacts:
    - path: "app/Http/Controllers/CompanyController.php"
      provides: "Validação + persistência de email_cliente em store/update + payload index/show"
      contains: "email_cliente"
    - path: "app/Http/Controllers/ComercialController.php"
      provides: "Validação + persistência de email_cliente em update + payload empresas()"
      contains: "email_cliente"
    - path: "resources/js/Pages/Companies/Index.jsx"
      provides: "Modal admin com campo Email do Cliente"
      contains: "email_cliente"
    - path: "resources/js/Pages/Comercial/Empresas.jsx"
      provides: "Form comercial com campo Email do Cliente"
      contains: "email_cliente"
  key_links:
    - from: "resources/js/Pages/Companies/Index.jsx"
      to: "route('companies.update')"
      via: "useForm.put"
      pattern: "email_cliente"
    - from: "resources/js/Pages/Comercial/Empresas.jsx"
      to: "route('comercial.empresas.update')"
      via: "useForm.put"
      pattern: "email_cliente"
---

<objective>
Expor o campo `companies.email_cliente` (criado no Plan 31-01) nas duas UIs de edição de empresa (admin `/companies` e comercial `/comercial/empresas`) com validação backend correspondente. Sem este plano, nenhum operador consegue preencher o email destinatário e o comando do Plan 31-02 nunca dispara nada útil.

Purpose: Habilitar o preenchimento operacional do destinatário do email NPS (D-04).
Output: 2 controllers + 2 JSX atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md
@.planning/phases/31-nps-mensal-automatizado/31-01-SUMMARY.md

@app/Http/Controllers/CompanyController.php
@app/Http/Controllers/ComercialController.php
@resources/js/Pages/Companies/Index.jsx
@resources/js/Pages/Comercial/Empresas.jsx

<interfaces>
<!-- CompanyController::update payload atual (linhas 387-422) -->

$data = $request->validate([
    'name'             => 'required|string|max:255',
    'cnpj'             => 'nullable|string|max:18',
    'adman_store_id'   => 'nullable|string|max:100',
    'ml_store_id'      => 'nullable|string|max:100',
    'segment'          => 'nullable|string|max:100',
    'notes'            => 'nullable|string',
    'active'           => 'boolean',
    'consultor_id'     => 'nullable|exists:users,id',
    'estrategista_id'  => 'nullable|exists:users,id',
]);
$company->update($data);

<!-- CompanyController::store linhas 362-385: validação parecida sem 'active', sem 'consultor_id'/'estrategista_id' no $data salvo na Company (são attaches manuais via pivot) -->

<!-- CompanyController::index (linhas 32-135) — payload por empresa que vai para Companies/Index.jsx:
    'id', 'name', 'cnpj', 'segment', 'active', 'status', 'cust_id_status', 'notes',
    'adman_account_id', 'adman_store_id', 'ml_store_id', 'consultor', 'estrategista',
    'contratos_servico', 'ml_token_status'
NÃO inclui email_cliente — precisa ser adicionado. -->

<!-- Companies/Index.jsx (linhas 170-191): openEdit popula data com:
    name, cnpj, adman_store_id, ml_store_id, segment, notes, consultor_id, estrategista_id
Modal form (linhas 410-440) tem grid 2 colunas com campos Nome, CNPJ, ID Loja Adman, ID Loja ML, Segmento, Analista, Estrategista, Observações.
Falta: input Email do Cliente. -->

<!-- Comercial/Empresas.jsx FormularioEditar (linhas 93-218): useForm tem só { name, cnpj, notes } e usa put(route('comercial.empresas.update', company.id)).
ComercialController::update precisa ser auditado para descobrir validação atual. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompanyController store/update/index + ComercialController update + audit</name>
  <read_first>
    - app/Http/Controllers/CompanyController.php (linhas 32-135 index; 362-422 store/update)
    - app/Http/Controllers/ComercialController.php (action update — buscar com grep no arquivo inteiro)
    - app/Models/Company.php (fillable após Plan 31-01 deve ter email_cliente)
  </read_first>
  <files>
    - app/Http/Controllers/CompanyController.php
    - app/Http/Controllers/ComercialController.php
  </files>
  <action>
    1. `CompanyController::store()` (linha 362): adicionar regra `'email_cliente' => 'nullable|email|max:255'` ao array de validate. O `Company::create($data)` na linha 375 já persiste (fillable inclui email_cliente após Plan 31-01).

    2. `CompanyController::update()` (linha 387): adicionar `'email_cliente' => 'nullable|email|max:255'` ao validate. O `$company->update($data)` na linha 401 já persiste.

    3. `CompanyController::index()` (linha 53 — `$companies->map(...)`): adicionar `'email_cliente' => $c->email_cliente,` na array que vira o payload Inertia. Inserir entre `'notes'` e `'adman_account_id'` para legibilidade.

    4. `CompanyController::show()` (linha 269 — payload Inertia): adicionar `'email_cliente' => $company->email_cliente,` se este controller é usado por algo (o atual Companies/Show.jsx não tem form de edição, mas inclua para consistência e uso futuro). Inserir após `'notes'`.

    5. `ComercialController::update()`:
       - Localizar a action (grep `public function update` no arquivo). Provável validação atual: `name`, `cnpj`, `notes` (espelho do form em Comercial/Empresas.jsx FormularioEditar).
       - Adicionar `'email_cliente' => 'nullable|email|max:255'` ao validate.
       - Confirmar que `$company->update(...)` persiste (idêntico ao CompanyController::update — fillable cobre).

    6. `ComercialController::empresas()` (action que renderiza `/comercial/empresas`): localizar via grep `Inertia::render('Comercial/Empresas'` ou similar. No map por empresa, adicionar `'email_cliente' => $c->email_cliente,`. Se o mapa expõe o model completo (em vez de array transform), pode já vir via `$casts`/`$fillable` — verificar e ajustar.

    Comentário pt-BR em cada validate adicionado: `// Phase 31 D-04 — destinatario do email NPS mensal`.
  </action>
  <verify>
    <automated>php artisan test --filter="CompanyController" 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - Grep `grep -n "email_cliente" app/Http/Controllers/CompanyController.php` retorna ≥ 4 hits (store validate + update validate + index map + show map)
    - Grep `grep -n "email_cliente" app/Http/Controllers/ComercialController.php` retorna ≥ 2 hits (update validate + empresas map)
    - `php artisan tinker --execute="$c=Company::first(); $c->update(['email_cliente'=>'x@y.com']); echo $c->fresh()->email_cliente;"` imprime `x@y.com`
    - POST `/companies/{id}` com email inválido retorna 422 com erro em `errors.email_cliente`
  </done>
</task>

<task type="auto">
  <name>Task 2: Companies/Index.jsx modal admin — campo Email do Cliente</name>
  <read_first>
    - resources/js/Pages/Companies/Index.jsx (arquivo inteiro — entender Modal e openEdit)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-04)
  </read_first>
  <files>
    - resources/js/Pages/Companies/Index.jsx
  </files>
  <action>
    1. `useForm({ ... })` (procurar instanciação inicial, próxima a linha 138 ou semelhante): adicionar `email_cliente: ''` ao state inicial.

    2. `openCreate()` (se existir): adicionar `email_cliente: ''` ao `setData({...})`.

    3. `openEdit(c)` (linhas 169-182): adicionar `email_cliente: c.email_cliente || '',` ao `setData({...})`.

    4. Modal de Form (linhas ~370-440 — após o campo Segmento e antes do Analista, ou após Observações conforme melhor encaixe visual):
       Inserir bloco:
       ```jsx
       <div className="col-span-2 space-y-1.5">
           <Label>Email do Cliente</Label>
           <Input
               type="email"
               value={data.email_cliente}
               onChange={e => setData('email_cliente', e.target.value)}
               placeholder="cliente@empresa.com.br"
           />
           {errors.email_cliente && <p className="text-destructive text-xs">{errors.email_cliente}</p>}
           <p className="text-white/30 text-[11px]">Destinatário do email mensal de NPS. Deixe em branco para pausar o envio.</p>
       </div>
       ```
       Posicionar logicamente após o campo Observações (linha ~436-439) na grid 2-col, ou criar uma nova linha que ocupe `col-span-2`.

    5. Após edit: `npm run build` (mandatório).
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
    <human-check>Em /companies (logado como admin): abrir modal "Nova Empresa" e verificar que o campo Email do Cliente existe e aceita digitação. Em uma empresa existente, clicar Editar — campo já vem com valor atual (ou vazio). Salvar com email válido → persiste no banco. Email inválido → mostra erro 422.</human-check>
  </verify>
  <done>
    - Grep `grep -n "email_cliente" resources/js/Pages/Companies/Index.jsx` retorna ≥ 4 hits (state init, openEdit setData, label/input, errors)
    - `npm run build` verde
  </done>
</task>

<task type="auto">
  <name>Task 3: Comercial/Empresas.jsx form comercial — campo Email do Cliente</name>
  <read_first>
    - resources/js/Pages/Comercial/Empresas.jsx (linhas 80-220, foco no FormularioEditar)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-04)
  </read_first>
  <files>
    - resources/js/Pages/Comercial/Empresas.jsx
  </files>
  <action>
    1. `FormularioEditar` component (linhas 93-218):
       - useForm inicial (linha 96-100): adicionar `email_cliente: company.email_cliente ?? ''`
       - Após o bloco de Descricao/Observacoes (linhas 158-168), inserir novo bloco:
         ```jsx
         <div className="space-y-1.5">
             <label className="block text-xs text-white/60 font-medium">Email do Cliente <span className="text-white/30 text-[11px] font-normal">(opcional)</span></label>
             <input
                 type="email"
                 value={data.email_cliente}
                 onChange={e => setData('email_cliente', e.target.value)}
                 placeholder="cliente@empresa.com.br"
                 className={cn('w-full bg-white/[0.04] border rounded-lg px-3 py-2.5 text-white text-sm placeholder:text-white/20 focus:outline-none focus:border-ecf-yellow/40 transition-colors', errors.email_cliente ? 'border-red-500/50' : 'border-white/[0.08]')}
             />
             {errors.email_cliente && <p className="text-red-400 text-xs">{errors.email_cliente}</p>}
             <p className="text-white/30 text-[11px]">Destinatário do email mensal de NPS. Deixe em branco para pausar o envio.</p>
         </div>
         ```

    2. (Opcional, se a comercial também cria empresa via outro formulário): grep `useForm` em Comercial/Empresas.jsx para qualquer outro form (create) e replicar. Se não houver create inline, pular.

    3. Após edit: `npm run build` (mandatório).
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
    <human-check>Em /comercial/empresas: abrir editar de uma empresa → campo Email do Cliente aparece após Observações; preencher e salvar → persiste; checar via /companies admin que o mesmo email aparece (mesma coluna).</human-check>
  </verify>
  <done>
    - Grep `grep -n "email_cliente" resources/js/Pages/Comercial/Empresas.jsx` retorna ≥ 3 hits
    - `npm run build` verde
  </done>
</task>

</tasks>

<verification>
- 4 arquivos editados, 0 quebras de build.
- POST `/companies/{id}` e PUT `/comercial/empresas/{id}` ambos persistem `email_cliente`.
- Testes existentes (`CompanyController`, `ComercialController`) seguem verdes — a adição de regras nullable não quebra fluxos atuais.
- Comando do Plan 31-02 agora consegue achar empresas com email para disparar.
</verification>

<success_criteria>
- Admin e Comercial podem ambos editar `email_cliente`.
- Validação `email|max:255` ativa em ambos endpoints.
- Empresa salva sem email continua válida (nullable).
- Build frontend verde.
</success_criteria>

<output>
Criar `.planning/phases/31-nps-mensal-automatizado/31-04-SUMMARY.md` ao final.
</output>
