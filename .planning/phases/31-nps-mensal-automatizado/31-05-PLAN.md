---
phase: 31-nps-mensal-automatizado
plan: 05
type: execute
wave: 3
depends_on:
  - 31-02
files_modified:
  - app/Http/Controllers/NpsController.php
  - app/Http/Controllers/DashboardController.php
  - resources/js/Pages/Nps/Index.jsx
  - resources/js/Pages/Dashboard/Admin.jsx
autonomous: true
requirements:
  - REQ-31-07

must_haves:
  truths:
    - "Admin acessa /nps e vê filtro por mês (default = mês corrente)"
    - "Admin vê 3 cards de média do mês selecionado: Estrategista, Analista, Empresa (cada um com média 1-5 e contagem de respostas)"
    - "Admin vê gráfico Recharts LineChart de 12 meses com 3 séries (Estrategista / Analista / Empresa) — variação histórica"
    - "Admin vê lista paginada de respostas do mês com nome, empresa, 3 notas e comentário"
    - "Filtro de mês usa month_reference do survey (D-specifics — 'Audiência admin')"
    - "Lista mostra respostas auto_generated e manuais sem distinção visual (toggle opcional)"
    - "Geração manual de link (REQ-31-08 — preservada no Plan 31-02) continua acessível via botão 'Gerar Link NPS' no topo da página"
    - "Widget NPS no Dashboard Admin foi ajustado para nova escala (mapeamento score_empresa: 5=promotor, 4=neutro, 1-3=detrator — D-09) — não quebra com tabela nps_responses recriada"
  artifacts:
    - path: "app/Http/Controllers/NpsController.php"
      provides: "index() reescrito com agregações por month_reference + série 12m"
      contains: "score_estrategista"
    - path: "app/Http/Controllers/DashboardController.php"
      provides: "nps_distribution baseado em score_empresa 1-5"
      contains: "score_empresa"
    - path: "resources/js/Pages/Nps/Index.jsx"
      provides: "UI admin completa (filtro mês + cards + chart + lista)"
      contains: "LineChart"
      min_lines: 200
    - path: "resources/js/Pages/Dashboard/Admin.jsx"
      provides: "Pie ajustado para nova escala (rótulos mantidos por compat)"
      contains: "nps_distribution"
  key_links:
    - from: "app/Http/Controllers/NpsController.php index"
      to: "nps_responses + nps_surveys"
      via: "query SUM/AVG GROUP BY month_reference"
      pattern: "AVG.*score_estrategista|avg\\('score_estrategista'\\)"
    - from: "resources/js/Pages/Nps/Index.jsx"
      to: "Recharts LineChart"
      via: "import { LineChart, Line, XAxis, YAxis, Tooltip, Legend, ResponsiveContainer }"
      pattern: "LineChart"
    - from: "app/Http/Controllers/DashboardController.php nps_distribution"
      to: "score_empresa column"
      via: "filter score_empresa==5 / ==4 / 1-3"
      pattern: "score_empresa"
---

<objective>
Entregar a admin section em `/nps`: filtro por mês, 3 cards de média (estrategista/analista/empresa), gráfico LineChart 12 meses × 3 séries, e lista paginada das respostas do mês. Em paralelo, ajustar o widget NPS do Dashboard Admin (`nps_distribution`) para a nova escala 1-5 (D-09) — sem isso, o Dashboard quebra após o drop+recreate do Plan 31-01.

Purpose: É o entregável visível do REQ-31-07 + tampa a quebra do widget Dashboard (D-09).
Output: Controller index reescrito + DashboardController ajustado + Nps/Index.jsx expandido + Dashboard/Admin.jsx widget atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md
@.planning/phases/31-nps-mensal-automatizado/31-02-SUMMARY.md

@app/Http/Controllers/NpsController.php
@app/Http/Controllers/DashboardController.php
@resources/js/Pages/Nps/Index.jsx
@resources/js/Pages/Dashboard/Admin.jsx

<interfaces>
<!-- Após Plan 31-01 + 31-02: schema disponível -->

nps_surveys: id, token, company_id, generated_by (nullable após Plan 31-02), expires_at, completed_at,
             status, month_reference (date YYYY-MM-01 nullable), auto_generated (bool default false), timestamps

nps_responses: id, survey_id, respondent_name (nullable), score_estrategista (1-5),
               score_analista (1-5 nullable), score_empresa (1-5), comment (nullable), timestamps

<!-- NpsController::index atual (linhas 14-52) — vai ser reescrito -->

Lê auth user; se !isAdmin filtra por carteira via $user->companies()->pluck('companies.id').
Pagina 20 surveys.
Para cada survey: 'score_consultant', 'score_mentor', 'score_overall' (chaves OBSOLETAS após Plan 31-01).

<!-- DashboardController nps_distribution atual (linhas 357-398) — vai ser ajustado -->

$npsResponses = NpsSurvey::with('response')->whereIn(...).where(status,completed).where(completed_at,>=,$since)->get();
$npsDistribution = [
    'promotores' => filter score_overall>=9,
    'neutros'    => filter score_overall>=7 && <9,
    'detratores' => filter score_overall<7,
];

Após Plan 31-01 a coluna score_overall NÃO EXISTE — vai retornar tudo zerado/null.

<!-- Recharts LineChart pattern (consistência com Painel Executivo Phase 24, Dashboard Admin)-->

import {
  ResponsiveContainer, LineChart, Line, XAxis, YAxis,
  Tooltip, Legend, CartesianGrid
} from 'recharts';

<LineChart data={chartData}>
  <CartesianGrid strokeDasharray="3 3" stroke="#1a1d24" />
  <XAxis dataKey="mes" stroke="#9ba0aa" fontSize={11} />
  <YAxis domain={[1, 5]} stroke="#9ba0aa" fontSize={11} />
  <Tooltip {...chartStyle.tooltip} />
  <Legend wrapperStyle={{ fontSize: 11, color: '#9ba0aa' }} />
  <Line type="monotone" dataKey="estrategista" stroke="#ffe600" strokeWidth={2} />
  <Line type="monotone" dataKey="analista"     stroke="#19e06a" strokeWidth={2} />
  <Line type="monotone" dataKey="empresa"      stroke="#60a5fa" strokeWidth={2} />
</LineChart>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NpsController::index reescrito — filtro mês + cards + série 12m + lista</name>
  <read_first>
    - app/Http/Controllers/NpsController.php (action index linhas 14-52)
    - app/Models/NpsSurvey.php (após Plan 31-01)
    - app/Models/NpsResponse.php (após Plan 31-01)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (REQ-31-07, &lt;specifics&gt; Audiência admin)
  </read_first>
  <files>
    - app/Http/Controllers/NpsController.php
  </files>
  <action>
    Substituir a action `index(Request $request)` mantendo as outras 3 actions (generate, respond, submitResponse) intactas:

    ```
    public function index(Request $request)
    {
        $user = $request->user();

        // Filtro de mes - default mes corrente. Aceita YYYY-MM via query string.
        $mesFiltro = $request->input('mes', now()->format('Y-m'));
        if (!preg_match('/^\d{4}-\d{2}$/', $mesFiltro)) {
            $mesFiltro = now()->format('Y-m');
        }
        $mesInicio = \Carbon\Carbon::parse($mesFiltro . '-01')->startOfMonth();
        $mesFim    = $mesInicio->copy()->endOfMonth();

        // Audiência: surveys do mes selecionado por month_reference (auto)
        // OR (manual) survey criado no mes selecionado. Lista mostra ambos.
        $baseQuery = NpsSurvey::with(['company', 'response', 'generatedBy'])
            ->where(function ($q) use ($mesInicio, $mesFim) {
                $q->whereBetween('month_reference', [$mesInicio->toDateString(), $mesFim->toDateString()])
                  ->orWhere(function ($qq) use ($mesInicio, $mesFim) {
                      $qq->whereNull('month_reference')
                         ->whereBetween('created_at', [$mesInicio, $mesFim]);
                  });
            })
            ->orderBy('created_at', 'desc');

        if (!$user->isAdmin()) {
            $companyIds = $user->companies()->pluck('companies.id');
            $baseQuery->whereIn('company_id', $companyIds);
        }

        $surveys = $baseQuery->paginate(20)->withQueryString()->through(fn($s) => [
            'id'                 => $s->id,
            'token'              => $s->token,
            'company_name'       => $s->company->name,
            'company_id'         => $s->company_id,
            'status'             => $s->status,
            'auto_generated'     => (bool) $s->auto_generated,
            'generated_by'       => $s->generatedBy?->name,
            'created_at'         => $s->created_at->format('d/m/Y H:i'),
            'expires_at'         => $s->expires_at?->format('d/m/Y'),
            'completed_at'       => $s->completed_at?->format('d/m/Y H:i'),
            'score_estrategista' => $s->response?->score_estrategista,
            'score_analista'     => $s->response?->score_analista,
            'score_empresa'      => $s->response?->score_empresa,
            'respondent'         => $s->response?->respondent_name,
            'comment'            => $s->response?->comment,
            'link'               => route('nps.respond', $s->token),
        ]);

        // 3 cards de media (somente respostas do mes filtrado)
        $responsesQuery = NpsResponse::query()
            ->whereHas('survey', function ($q) use ($mesInicio, $mesFim, $user) {
                $q->where(function ($qq) use ($mesInicio, $mesFim) {
                    $qq->whereBetween('month_reference', [$mesInicio->toDateString(), $mesFim->toDateString()])
                       ->orWhere(function ($qqq) use ($mesInicio, $mesFim) {
                           $qqq->whereNull('month_reference')
                               ->whereBetween('created_at', [$mesInicio, $mesFim]);
                       });
                });
                if (!$user->isAdmin()) {
                    $q->whereIn('company_id', $user->companies()->pluck('companies.id'));
                }
            });

        $cards = [
            'estrategista' => [
                'media' => (float) round((clone $responsesQuery)->avg('score_estrategista') ?? 0, 2),
                'total' => (clone $responsesQuery)->whereNotNull('score_estrategista')->count(),
            ],
            'analista' => [
                'media' => (float) round((clone $responsesQuery)->avg('score_analista') ?? 0, 2),
                'total' => (clone $responsesQuery)->whereNotNull('score_analista')->count(),
            ],
            'empresa' => [
                'media' => (float) round((clone $responsesQuery)->avg('score_empresa') ?? 0, 2),
                'total' => (clone $responsesQuery)->whereNotNull('score_empresa')->count(),
            ],
        ];

        // Serie 12 meses para LineChart: medias por mes nos ultimos 12 meses
        $serieMeses = [];
        $inicio12m = now()->startOfMonth()->subMonths(11);
        for ($i = 0; $i < 12; $i++) {
            $m = $inicio12m->copy()->addMonths($i);
            $mFim = $m->copy()->endOfMonth();

            $q = NpsResponse::query()->whereHas('survey', function ($qq) use ($m, $mFim, $user) {
                $qq->where(function ($qqq) use ($m, $mFim) {
                    $qqq->whereBetween('month_reference', [$m->toDateString(), $mFim->toDateString()])
                        ->orWhere(function ($qqqq) use ($m, $mFim) {
                            $qqqq->whereNull('month_reference')
                                 ->whereBetween('created_at', [$m, $mFim]);
                        });
                });
                if (!$user->isAdmin()) {
                    $qq->whereIn('company_id', $user->companies()->pluck('companies.id'));
                }
            });

            $serieMeses[] = [
                'mes'          => $m->format('M/y'),  // ex: 'Jun/26'
                'mes_iso'      => $m->format('Y-m'),
                'estrategista' => (float) round((clone $q)->avg('score_estrategista') ?? 0, 2),
                'analista'     => (float) round((clone $q)->avg('score_analista') ?? 0, 2),
                'empresa'      => (float) round((clone $q)->avg('score_empresa') ?? 0, 2),
            ];
        }

        $companies = $user->isAdmin()
            ? Company::where('active', true)->get(['id', 'name'])
            : $user->companies()->get(['companies.id', 'companies.name']);

        return Inertia::render('Nps/Index', [
            'surveys'     => $surveys,
            'companies'   => $companies,
            'cards'       => $cards,
            'serie_12m'   => $serieMeses,
            'mes_filtro'  => $mesFiltro,
        ]);
    }
    ```

    Comentários pt-BR em cada bloco. Não mexer nas outras actions.

    Performance: o loop de 12 meses dispara 12×3 queries de avg. Para 12 meses isso é ~36 queries — aceitável pelos volumes esperados (~150 empresas × 12 meses = 1800 respostas no pior caso). Documentar como "trade-off consciente — se virar gargalo, agregar via 1 query single GROUP BY YYYY-MM" no comentário do bloco.
  </action>
  <verify>
    <automated>php artisan tinker --execute="$r = (new \App\Http\Controllers\NpsController)->index(request()->merge(['mes' => now()->format('Y-m')])); echo 'OK';" 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - Endpoint GET /nps responde 200 (smoke via tinker ou curl autenticado)
    - Props Inertia contém chaves: surveys, companies, cards, serie_12m, mes_filtro
    - `cards.estrategista.media` é float entre 0 e 5
    - `serie_12m` é array de 12 itens cada um com chaves mes, mes_iso, estrategista, analista, empresa
    - Grep `score_overall\|score_consultant\|score_mentor` no NpsController::index retorna 0 hits
  </done>
</task>

<task type="auto">
  <name>Task 2: DashboardController nps_distribution ajustado para escala 1-5</name>
  <read_first>
    - app/Http/Controllers/DashboardController.php (linhas 357-398 - npsResponses + npsDistribution)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-09)
  </read_first>
  <files>
    - app/Http/Controllers/DashboardController.php
  </files>
  <action>
    Linhas 357-398 do `DashboardController` (action `adminDashboard` ou similar):

    1. Substituir `$avgNps = $npsResponses->avg(fn($s) => $s->response?->score_overall) ?? 0;` (linha 363) por:
       ```
       // Phase 31 D-09 - escala 1-5 substituiu 0-10. Card 'NPS Score' agora mostra
       // a media de score_empresa (1-5) - dimensao geral 'A ECF esta atendendo
       // suas expectativas?'.
       $avgNps = $npsResponses->avg(fn($s) => $s->response?->score_empresa) ?? 0;
       ```

    2. Substituir o array `$npsDistribution` (linhas 394-398) por mapeamento D-09:
       ```
       // Phase 31 D-09 - mapeamento 1-5 → categorias legadas:
       //   score_empresa == 5     → promotor
       //   score_empresa == 4     → neutro
       //   score_empresa 1-3      → detrator
       // Mantemos as chaves promotores/neutros/detratores porque o Pie do
       // Dashboard/Admin.jsx ainda consome esse shape (vai ser ajustado em
       // Task 4 deste plano).
       $npsDistribution = [
           'promotores' => $npsResponses->filter(fn($s) => ($s->response?->score_empresa ?? 0) === 5)->count(),
           'neutros'    => $npsResponses->filter(fn($s) => ($s->response?->score_empresa ?? 0) === 4)->count(),
           'detratores' => $npsResponses->filter(fn($s) => ($s->response?->score_empresa ?? 0) >= 1
                                                       && ($s->response?->score_empresa ?? 0) <= 3)->count(),
       ];
       ```

    3. Verificar se há outros usos de `score_overall`/`score_consultant`/`score_mentor` no DashboardController via grep — corrigir todos. Conferir Companies/Show e outros consumidores de `nps_surveys` em outros controllers via grep adicional. Para Companies/Show CompanyController::show (linhas 305-314), substituir as chaves:
       - `'score_overall' => $s->response->score_overall` → `'score_empresa' => $s->response->score_empresa`
       - `'score_consultant' => $s->response->score_consultant` → `'score_analista' => $s->response->score_analista`
       - `'score_mentor' => $s->response->score_mentor` → `'score_estrategista' => $s->response->score_estrategista`
       Manter as 3 chaves antigas? **NÃO** — o frontend Companies/Show consumer foi inspecionado e não exibe individual de NPS score; só lista. Substituir + atualizar JSX consumidor se aparecer warning de build no Plan 31-04. Se JSX consumir, substituir as referências também.
       (Se o consumidor JSX de Companies/Show.jsx usa `nps_surveys[i].response.score_overall`, substituir para `score_empresa`.)
  </action>
  <verify>
    <automated>php artisan tinker --execute="echo (Schema::hasColumn('nps_responses','score_empresa') ? 'OK' : 'MISS');" &amp;&amp; grep -c "score_overall\|score_consultant\|score_mentor" app/Http/Controllers/DashboardController.php</automated>
  </verify>
  <done>
    - Grep `score_overall\|score_consultant\|score_mentor` em `app/Http/Controllers/DashboardController.php` retorna 0 hits
    - Grep `score_empresa\|score_estrategista\|score_analista` em `app/Http/Controllers/DashboardController.php` retorna ≥ 1 hit
    - GET `/dashboard` (admin) responde 200 (smoke via tinker)
  </done>
</task>

<task type="auto">
  <name>Task 3: Nps/Index.jsx expandido — filtro + cards + LineChart + lista</name>
  <read_first>
    - resources/js/Pages/Nps/Index.jsx (arquivo inteiro)
    - resources/js/Pages/Dashboard/Admin.jsx (linhas 90-145 e 370-392 — referência de Recharts + cards no projeto)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (REQ-31-07)
  </read_first>
  <files>
    - resources/js/Pages/Nps/Index.jsx
  </files>
  <action>
    Reescrever `resources/js/Pages/Nps/Index.jsx` mantendo a estrutura de AppLayout + manter o Modal "Gerar Link NPS" (REQ-31-08 preservação manual) MAS com nova estrutura:

    1. **Imports adicionar**:
       ```
       import { ResponsiveContainer, LineChart, Line, XAxis, YAxis, Tooltip, Legend, CartesianGrid } from 'recharts';
       import { Star, Plus, Copy, CheckCircle, Link as LinkIcon, Users, TrendingUp } from 'lucide-react';
       ```

    2. **Props default** (substituir signature do componente):
       ```
       export default function NpsIndex({ surveys, companies, cards = {}, serie_12m = [], mes_filtro = '' }) {
       ```

    3. **Filtro de mês** (no topo, antes da lista de cards):
       - Gera array de opções dos últimos 12 meses + próximo 1 (ou usa `serie_12m` para opções):
         ```
         const mesOpcoes = serie_12m.map(s => ({ value: s.mes_iso, label: s.mes }));
         ```
       - `<select>` controlado: `<select value={mes_filtro} onChange={e => router.get(route('nps.index'), { mes: e.target.value }, { preserveState: true, preserveScroll: true })}>` — popula com `mesOpcoes`. Estilo seguindo o `<select>` ecf padrão (CompanyIndex linha 211-220).

    4. **3 Cards de média** (grid 3 colunas):
       ```jsx
       <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
           <CardMedia titulo="Estrategista" media={cards.estrategista?.media ?? 0} total={cards.estrategista?.total ?? 0} cor="ecf-yellow" />
           <CardMedia titulo="Analista"     media={cards.analista?.media     ?? 0} total={cards.analista?.total     ?? 0} cor="emerald" />
           <CardMedia titulo="Empresa"      media={cards.empresa?.media      ?? 0} total={cards.empresa?.total      ?? 0} cor="blue" />
       </div>
       ```
       Componente local `CardMedia({ titulo, media, total, cor })`:
       - Container `card-ecf rounded-2xl p-5`
       - Header pequeno `text-white/50 text-[11px] uppercase tracking-widest` com `titulo`
       - Valor grande: `text-white font-display font-extrabold text-3xl` mostrando `media.toFixed(2)` + `/5` em `text-white/40 text-sm ml-1`
       - Sub: `text-white/40 text-xs mt-1` mostrando `${total} respostas`
       - Empty state: se `total === 0`, mostrar valor `—` ao invés do número

    5. **LineChart 12 meses**:
       ```jsx
       <div className="card-ecf rounded-2xl p-6">
           <p className="text-white/50 text-[11px] font-semibold tracking-widest uppercase mb-1">Variação 12 meses</p>
           <p className="text-white font-display font-extrabold text-lg mb-5 tracking-tight">Média NPS por mês</p>
           <ResponsiveContainer width="100%" height={280}>
               <LineChart data={serie_12m}>
                   <CartesianGrid strokeDasharray="3 3" stroke="#1a1d24" />
                   <XAxis dataKey="mes" stroke="#9ba0aa" fontSize={11} />
                   <YAxis domain={[1, 5]} stroke="#9ba0aa" fontSize={11} />
                   <Tooltip contentStyle={{ background: '#0f1116', border: '1px solid rgba(255,255,255,0.08)', borderRadius: 8, fontSize: 12 }} />
                   <Legend wrapperStyle={{ fontSize: 11, color: '#9ba0aa' }} />
                   <Line type="monotone" dataKey="estrategista" name="Estrategista" stroke="#ffe600" strokeWidth={2} dot={false} />
                   <Line type="monotone" dataKey="analista"     name="Analista"     stroke="#19e06a" strokeWidth={2} dot={false} />
                   <Line type="monotone" dataKey="empresa"      name="Empresa"      stroke="#60a5fa" strokeWidth={2} dot={false} />
               </LineChart>
           </ResponsiveContainer>
       </div>
       ```

    6. **Lista de respostas** (Card com Table mantendo a estrutura atual mas com colunas novas):
       - Colunas: Empresa, Respondente, Estrategista, Analista, Empresa, Comentário, Status, Origem (`auto_generated ? 'Mensal' : 'Manual'` com Badge), Link
       - `NpsScore` componente local: para escala 1-5 colorir 5=emerald, 4=lime, 3=yellow, 2=orange, 1=red
       - Quando `score_analista` é null mostrar `<span className="text-muted-foreground">—</span>`

    7. **Modal "Gerar Link NPS" PRESERVADO** (back-compat REQ-31-08) — manter o Dialog atual com Select de empresa + form `nps.generate`.

    8. **Paginação**: continuar usando `surveys.last_page`/`surveys.current_page` com botões Anterior/Próxima, mas passar `mes` no router.get para preservar filtro:
       ```
       router.get(route('nps.index'), { mes: mes_filtro, page: surveys.current_page + 1 })
       ```

    9. Após edit: `npm run build`.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -15</automated>
    <human-check>Em /nps logado como admin: ver filtro de mês com 12 opções (default = mês atual); 3 cards com `0.00/5` (zero dados); LineChart com 12 ticks no eixo X mas linhas planas em 0; lista vazia ou com surveys de teste; botão "Gerar Link NPS" abre Dialog e preserva fluxo manual. Mudar filtro de mês → URL muda para ?mes=YYYY-MM, lista filtra.</human-check>
  </verify>
  <done>
    - Grep `LineChart` em `resources/js/Pages/Nps/Index.jsx` retorna ≥ 1 hit
    - Grep `score_estrategista\|score_analista\|score_empresa` em `resources/js/Pages/Nps/Index.jsx` retorna ≥ 3 hits
    - Grep `score_consultant\|score_mentor\|score_overall` retorna 0 hits (após filtro de comentários `grep -v '^//'`)
    - `npm run build` verde
    - Dialog "Gerar Link NPS" funcional preservado
  </done>
</task>

<task type="auto">
  <name>Task 4: Dashboard/Admin.jsx — ajustar Pie NPS para nova escala (não mudar shape)</name>
  <read_first>
    - resources/js/Pages/Dashboard/Admin.jsx (linhas 95-145 + 374-392 — Pie NPS)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-09)
  </read_first>
  <files>
    - resources/js/Pages/Dashboard/Admin.jsx
  </files>
  <action>
    O Pie atual consome `nps_distribution.promotores/neutros/detratores` — após Task 2 deste plano o controller passa esse shape (mapeado de score_empresa). Logo o JSX não precisa de mudança estrutural, mas:

    1. **Atualizar labels visuais** no array `npsDataFilled` (próximo à linha 142):
       - Em vez de `Promotores/Neutros/Detratores`, usar rótulos mais alinhados à nova escala:
         - `Excelente (5)` → cor `#19e06a` (era promotor)
         - `Bom (4)` → cor `#ffe600` (era neutro)
         - `Ruim (1-3)` → cor `#ff4d4d` (era detrator)
       - Substituir as 3 entradas do array:
         ```
         const npsDataFilled = [
             { name: 'Excelente (5)', value: nps_distribution.promotores ?? 0, color: '#19e06a' },
             { name: 'Bom (4)',       value: nps_distribution.neutros ?? 0,    color: '#ffe600' },
             { name: 'Ruim (1-3)',    value: nps_distribution.detratores ?? 0, color: '#ff4d4d' },
         ];
         ```

    2. **Atualizar cálculo de `npsScore`** (linha 138) — manter a fórmula de NPS clássica adaptada, ou substituir por média ponderada simples:
       ```
       // Phase 31 D-09 - novo "score" do widget = (5*excelente + 4*bom + media_ruim*detratores) / total
       // ou simplesmente: media ponderada de score_empresa (1-5) extrapolada para 0-100
       // Manter o calculo legado por simplicidade: (excelente - ruim) / total * 100
       const npsScore = npsTotal > 0
           ? Math.round(((nps_distribution.promotores - nps_distribution.detratores) / npsTotal) * 100)
           : 0;
       ```
       (Manter a fórmula atual — a semântica de NPS continua válida com a nova segmentação.)

    3. **Atualizar Card "NPS Médio" / "NPS Score"** (linha 204 e 340) — onde mostra `s.avg_nps`, agora o backend passa `score_empresa` médio (Task 2). Mudar o sub-label de `Score: ${npsScore}` para indicar que é escala 1-5:
       - Card linha ~340: mudar `sub={\`Score: ${npsScore}\`}` para `sub={\`Média ${(s.avg_nps ?? 0).toFixed(2)}/5\`}`.
       - Card linha ~204: idem.

    4. Após edit: `npm run build`.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10</automated>
    <human-check>Em /dashboard (admin): widget NPS continua renderizando sem erro; Pie mostra 3 segmentos com novos labels (Excelente/Bom/Ruim); KPI Card "NPS Médio" mostra média 1-5 (ex "Média 4.20/5") em vez de "Score: 70".</human-check>
  </verify>
  <done>
    - `npm run build` verde
    - Pie do dashboard tem 3 segments com labels "Excelente (5)" / "Bom (4)" / "Ruim (1-3)"
    - KPI Card NPS mostra média em escala 1-5
    - Grep `Promotores\|Neutros\|Detratores` em `resources/js/Pages/Dashboard/Admin.jsx` retorna 0 hits (rótulos antigos substituídos)
  </done>
</task>

</tasks>

<verification>
- Smoke local: rodar `npm run build` e abrir 3 telas — `/nps`, `/dashboard` admin, `/companies/{id}` — todas renderizam sem console errors.
- `php artisan test` suite verde (especialmente os Feature tests Phase31* dos planos 31-02).
- GET `/nps?mes=2026-05` filtra dados de maio/2026 (lista, cards e mantém serie 12m).
- Dashboard widget NPS volta a popular conforme respostas chegarem na nova escala (D-09 — aceito).
</verification>

<success_criteria>
- Admin entrega completo do REQ-31-07 (filtro mês + 3 cards + linha 12m + lista).
- Widget NPS do Dashboard ajustado para escala 1-5 sem quebrar layout.
- Geração manual de link continua funcional (REQ-31-08 — verificado pelos botões e Dialog preservados).
- Outros consumidores de `nps_responses` (CompanyController::show, etc) ajustados para usar novas chaves.
</success_criteria>

<output>
Criar `.planning/phases/31-nps-mensal-automatizado/31-05-SUMMARY.md` ao final.
</output>
