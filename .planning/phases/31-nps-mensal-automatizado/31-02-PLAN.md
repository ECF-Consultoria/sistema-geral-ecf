---
phase: 31-nps-mensal-automatizado
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - app/Mail/NpsMonthlyMail.php
  - resources/views/emails/nps/mensal.blade.php
  - app/Console/Commands/NpsDispararMensal.php
  - routes/console.php
  - app/Http/Controllers/NpsController.php
autonomous: true
requirements:
  - REQ-31-04
  - REQ-31-05
  - REQ-31-08

must_haves:
  truths:
    - "Comando `php artisan nps:disparar-mensal` roda sem erro e respeita idempotência (não duplica survey do mesmo mês para a mesma empresa)"
    - "Para empresas ativas COM email_cliente E aniversário do cadastro hoje, o comando cria 1 survey auto_generated=true por empresa e dispara 1 NpsMonthlyMail"
    - "Empresas sem email_cliente são silenciosamente puladas (D-04)"
    - "Edge case D-03 funciona: empresa criada dia 31 dispara no último dia do mês de fevereiro"
    - "Schedule diário em routes/console.php às 09:00 BRT roda o comando 1x/dia"
    - "NpsController::submitResponse aceita os 3 scores 1-5 + comment + respondent_name opcional"
    - "NpsController::respond payload inclui nome de estrategista, nome de analista (nullable) e tem_analista (bool) para a UI decidir mostrar/não o campo"
    - "Endpoint nps.generate (link manual) cria survey com auto_generated=false (back-compat — REQ-31-08)"
  artifacts:
    - path: "app/Mail/NpsMonthlyMail.php"
      provides: "Mailable Markdown via SMTP Gmail"
      contains: "NpsMonthlyMail"
    - path: "resources/views/emails/nps/mensal.blade.php"
      provides: "Template HTML do email"
      contains: "{{ $linkPublico }}"
    - path: "app/Console/Commands/NpsDispararMensal.php"
      provides: "Comando que itera empresas e dispara email"
      contains: "nps:disparar-mensal"
    - path: "routes/console.php"
      provides: "Schedule diário 09:00"
      contains: "nps:disparar-mensal"
    - path: "app/Http/Controllers/NpsController.php"
      provides: "submitResponse 1-5 + respond payload com analista/estrategista + generate auto_generated=false"
      contains: "score_estrategista"
  key_links:
    - from: "app/Console/Commands/NpsDispararMensal.php"
      to: "app/Mail/NpsMonthlyMail.php"
      via: "Mail::to($emailCliente)->send(new NpsMonthlyMail(...))"
      pattern: "NpsMonthlyMail"
    - from: "routes/console.php"
      to: "nps:disparar-mensal command"
      via: "Schedule::command('nps:disparar-mensal')->dailyAt('09:00')"
      pattern: "nps:disparar-mensal.*09:00"
    - from: "app/Http/Controllers/NpsController.php submitResponse"
      to: "nps_responses table"
      via: "NpsResponse::create([...])"
      pattern: "score_estrategista.*score_analista.*score_empresa"
---

<objective>
Backend completo da automação NPS: Mailable Markdown reusando SMTP Gmail (Phase 28), comando Artisan idempotente que dispara emails no aniversário do cadastro (D-01/D-03), agendamento diário em `routes/console.php` (D-02), e ajuste do `NpsController` para a escala 1-5 com 3 dimensões + preservação do `nps.generate` manual com `auto_generated=false` (REQ-31-08).

Purpose: Sem este plano, o cliente nunca recebe o email automaticamente nem consegue submeter resposta na escala nova.
Output: Mailable + template Blade + comando + schedule + controller atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md
@.planning/phases/31-nps-mensal-automatizado/31-01-SUMMARY.md

@app/Mail/RelatorioMensalMail.php
@resources/views/emails/relatorios/mensal-email.blade.php
@app/Console/Commands/RelatorioMensalDisparar.php
@routes/console.php
@app/Http/Controllers/NpsController.php
@app/Models/Company.php
@app/Models/NpsSurvey.php

<interfaces>
<!-- Padrão Mailable Markdown SMTP Gmail (Phase 28 — RelatorioMensalMail) -->

class RelatorioMensalMail extends Mailable {
    use Queueable, SerializesModels;
    public function __construct(public string $period, public string $pdfPath, public array $resumo = []) {}
    public function envelope(): Envelope { return new Envelope(subject: "..."); }
    public function content(): Content { return new Content(view: 'emails.relatorios.mensal-email', with: [...]); }
}

<!-- Pivot company_users (D-07 — resolver Estrategista e Analista) -->

Company::estrategista() retorna belongsToMany users via wherePivot('role','estrategista')
Company::consultor()    retorna belongsToMany users via wherePivot('role','consultor')  // ← Analista hoje

Para um único user:
$estrategista = $company->estrategista()->first();  // ?User
$analista     = $company->consultor()->first();     // ?User

<!-- Schedule pattern Laravel 11+ em routes/console.php -->

Schedule::command('nome:do-comando')
    ->dailyAt('HH:MM')
    ->name('slug-unico')
    ->withoutOverlapping();

<!-- NpsSurvey idempotência -->

NpsSurvey::where('company_id', $c->id)
    ->where('month_reference', $mesAtual)  // Carbon::today()->startOfMonth()
    ->exists()

<!-- NpsController atual — validação a substituir -->

submitResponse() hoje valida:
    'respondent_name'  => 'required|string|max:255',
    'score_consultant' => 'required|integer|min:0|max:10',
    'score_mentor'     => 'required|integer|min:0|max:10',
    'score_overall'    => 'required|integer|min:0|max:10',
    'comment'          => 'nullable|string|max:1000',

Vai virar (D-06/D-07/D-08):
    'respondent_name'    => 'nullable|string|max:255',
    'score_estrategista' => 'required|integer|min:1|max:5',
    'score_analista'     => 'nullable|integer|min:1|max:5',
    'score_empresa'      => 'required|integer|min:1|max:5',
    'comment'            => 'nullable|string|max:2000',

<!-- NpsController::generate — preservar back-compat REQ-31-08 -->

Hoje cria com expires_at=now()->addDays(7), status='pending'.
Adicionar 'auto_generated' => false explicitamente.
Não passar month_reference (fica null para manuais — D-12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NpsMonthlyMail (Mailable Markdown) + template Blade pt-BR</name>
  <read_first>
    - app/Mail/RelatorioMensalMail.php (padrão Phase 28 — copiar estrutura envelope/content)
    - resources/views/emails/relatorios/mensal-email.blade.php (padrão visual — header amarelo ecf, footer)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-05)
  </read_first>
  <behavior>
    - Test 1: instanciar `new NpsMonthlyMail($companyName, $estrategistaName, $analistaName, $linkPublico, $mesLabel)` não lança exceção
    - Test 2: `->envelope()->subject` contém "[ECF Admin] Pesquisa de satisfação" e o mesLabel ex "Junho/2026"
    - Test 3: `->content()->view === 'emails.nps.mensal'` e `->content()->with` contém todas as 5 chaves
    - Test 4: render do template Blade não dispara exceção; output contém `$linkPublico` literal e `$estrategistaName` literal
    - Test 5: quando `$analistaName === null`, render NÃO contém label "Analista:" (omissão D-05)
  </behavior>
  <files>
    - app/Mail/NpsMonthlyMail.php
    - resources/views/emails/nps/mensal.blade.php
    - tests/Feature/Phase31NpsMonthlyMailTest.php
  </files>
  <action>
    Criar `app/Mail/NpsMonthlyMail.php`:
    - Namespace `App\Mail`, extends `Illuminate\Mail\Mailable`
    - Traits: `Queueable`, `SerializesModels`
    - Construtor com promoted properties: `public string $companyName`, `public string $estrategistaName`, `public ?string $analistaName`, `public string $linkPublico`, `public string $mesLabel` (ex "Junho/2026")
    - `envelope()`: subject `"[ECF Admin] Pesquisa de satisfação — {$mesLabel}"`
    - `content()`: `new Content(view: 'emails.nps.mensal', with: ['companyName','estrategistaName','analistaName','linkPublico','mesLabel'])`
    - Sem `attachments()` (nenhum anexo)
    - PHPDoc pt-BR no topo explicando que reusa SMTP Gmail validado em Phase 28

    Criar `resources/views/emails/nps/mensal.blade.php` (HTML simples, inspirado em `mensal-email.blade.php`):
    - Header com borda amarela `#ffe600` e título "Pesquisa de Satisfação ECF" + subtítulo `{{ $mesLabel }}`
    - Saudação: "Olá!"
    - Parágrafo: "Esta é a pesquisa mensal de satisfação da ECF Consultoria com {{ $companyName }}."
    - Bloco "Quem te atende:" — sempre exibe Estrategista; só exibe Analista se `$analistaName` truthy (use `@if($analistaName)`)
    - CTA: link `<a href="{{ $linkPublico }}" style="display:inline-block;background:#ffe600;color:#050507;...">Responder pesquisa</a>`
    - Disclaimer pt-BR curto: "Leva menos de 1 minuto. Suas respostas são confidenciais."
    - Footer "ECF Consultoria · Pesquisa enviada automaticamente"

    Criar `tests/Feature/Phase31NpsMonthlyMailTest.php` cobrindo os 5 testes do `<behavior>`. Usar `view()->make()->render()` para Test 4/5.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase31NpsMonthlyMailTest --stop-on-failure</automated>
  </verify>
  <done>
    - 5 testes verdes
    - `app/Mail/NpsMonthlyMail.php` existe com construtor de 5 args
    - `resources/views/emails/nps/mensal.blade.php` renderiza sem erro
    - Quando analista é null, a label "Analista:" não aparece no HTML
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comando nps:disparar-mensal idempotente + edge case dia 31</name>
  <read_first>
    - app/Console/Commands/RelatorioMensalDisparar.php (padrão signature/description/handle)
    - app/Models/Company.php (linhas 130-155 — relações consultor/estrategista)
    - app/Models/NpsSurvey.php (fillable após Plan 31-01)
    - app/Mail/NpsMonthlyMail.php (criado no Task 1)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-01/D-02/D-03/D-04, idempotência em &lt;specifics&gt;)
  </read_first>
  <behavior>
    - Test 1 (happy): empresa ativa COM email_cliente E `created_at` dia 18 e hoje (mock now) é dia 18 → cria 1 NpsSurvey com `auto_generated=true`, `month_reference = inicio do mes corrente`, `expires_at = now()->addDays(30)`, status='pending', token UUID; e envia 1 NpsMonthlyMail para `email_cliente` (assert via `Mail::fake()` + `Mail::assertSent`)
    - Test 2 (sem email): empresa ativa, dia bate, mas `email_cliente` é null → NENHUM survey criado, NENHUM email enviado (D-04 — silenciosamente pulada)
    - Test 3 (dia não bate): empresa ativa com email, dia hoje !== DAY(created_at) → nada criado
    - Test 4 (idempotência): rodar comando 2× no mesmo dia → segundo run NÃO cria survey duplicado nem envia email duplicado
    - Test 5 (edge case dia 31 → fevereiro): empresa criada 31/01, mock hoje = 28/02 (não-bissexto), comando dispara (último dia do mês). Usar `min($diaOriginal, $now->daysInMonth)` e comparar com `$now->day`.
    - Test 6 (empresa inativa): `active=false`, dia bate, tem email → nada criado
  </behavior>
  <files>
    - app/Console/Commands/NpsDispararMensal.php
    - tests/Feature/Phase31NpsDispararMensalTest.php
  </files>
  <action>
    Criar `app/Console/Commands/NpsDispararMensal.php`:
    - `protected $signature = 'nps:disparar-mensal {--dry-run : Lista o que seria disparado sem enviar email nem criar survey}'`
    - `protected $description = 'Cria survey NPS auto_generated + envia email no aniversario do cadastro (DAY(companies.created_at) == DAY(today)). Idempotente.'`
    - `handle()`:
      1. `$now = now()->setTimezone('America/Sao_Paulo')` — comparar dia em horário Brasília
      2. `$mesAtual = $now->copy()->startOfMonth()->toDateString()` — chave de idempotência
      3. Iterar `Company::where('active', true)->whereNotNull('email_cliente')->where('email_cliente', '!=', '')->chunk(50, ...)`:
         a. Para cada empresa, calcular `$diaAlvo = min($company->created_at->day, $now->daysInMonth)` — D-03
         b. Se `$now->day !== $diaAlvo` → `continue` silencioso
         c. Idempotência: `if (NpsSurvey::where('company_id', $company->id)->where('month_reference', $mesAtual)->exists()) { continue; }`
         d. Resolver estrategista: `$estrategista = $company->estrategista()->first()` — se null, log warning `[NPS Mensal] empresa {$company->id} sem estrategista atribuido, pulando` e continue
         e. Resolver analista (nullable): `$analista = $company->consultor()->first()`
         f. Se `--dry-run`: `$this->line("[DRY] empresa {$company->id} ({$company->name}) - dispararia para {$company->email_cliente}")` e continue
         g. Criar survey: `NpsSurvey::create([token => Str::uuid()->toString(), company_id => $company->id, generated_by => null, expires_at => $now->copy()->addDays(30), status => 'pending', auto_generated => true, month_reference => $mesAtual])` — atenção: `generated_by` precisa aceitar null. Verificar schema atual: `foreignId('generated_by')->constrained('users')->cascadeOnDelete()` é NOT NULL por padrão.
           → **DECISÃO TÉCNICA**: criar migration auxiliar (incluída neste task, MESMO arquivo se cabivel) ou usar usuário system fallback `User::where('email','dev.01@ecfconsultoria.com.br')->first()->id` como `generated_by`. Optar pelo **fallback do primeiro admin** (`User::where('role','admin')->orderBy('id')->first()->id`) — evita migration extra, mantém integridade FK, e o usuário admin é "dono semântico" do envio automático. Documentar a escolha no PHPDoc do comando.
         h. Calcular `$mesLabel` via helper `mesLabelPt()` (duplicar de `RelatorioMensalDisparar.php` linha 87 — duplicação consciente Phase 28 D-D)
         i. `Mail::to($company->email_cliente)->send(new NpsMonthlyMail($company->name, $estrategista->name, $analista?->name, route('nps.respond', $survey->token), $mesLabel))`
         j. `Log::info("[NPS Mensal] enviado para empresa {$company->id} ({$company->name}) email={$company->email_cliente} survey_id={$survey->id}")`
         k. Incrementar contador `$enviados++`
      4. Ao fim: `$this->info("✓ Disparados {$enviados} emails NPS mensais.")` e retornar `self::SUCCESS`
      5. Capturar `\Throwable` por empresa dentro do loop com `Log::error("[NPS Mensal] falha empresa {$company->id}: ".$e->getMessage())` e continue (não derrubar comando inteiro)

    Comentário pt-BR no topo do arquivo explicando D-01/D-03 e o fallback de generated_by.

    Criar `tests/Feature/Phase31NpsDispararMensalTest.php` cobrindo os 6 testes do `<behavior>`. Usar `Mail::fake()`, `Carbon::setTestNow()`, factories para Company/User/pivot company_users. Para Test 5 usar `Carbon::setTestNow(Carbon::create(2027,2,28))` com `created_at = '2027-01-31 10:00'`.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase31NpsDispararMensalTest --stop-on-failure</automated>
  </verify>
  <done>
    - 6 testes verdes
    - `php artisan list | grep nps:disparar-mensal` mostra o comando registrado
    - `php artisan nps:disparar-mensal --dry-run` roda sem erro no banco local
    - Logs `[NPS Mensal]` aparecem em `storage/logs/laravel.log` após dry-run com empresa elegível
  </done>
</task>

<task type="auto">
  <name>Task 3: Schedule diário 09:00 BRT + NpsController submitResponse/respond/generate</name>
  <read_first>
    - routes/console.php (linhas 1-130 — padrão Schedule::command, nomes únicos, withoutOverlapping)
    - app/Http/Controllers/NpsController.php (controller completo a editar)
    - app/Models/NpsResponse.php (fillable pós Plan 31-01)
    - app/Models/NpsSurvey.php (fillable pós Plan 31-01)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-02, D-06, D-07, D-08, REQ-31-08)
  </read_first>
  <files>
    - routes/console.php
    - app/Http/Controllers/NpsController.php
    - tests/Feature/Phase31NpsSubmitTest.php
  </files>
  <action>
    1. Editar `routes/console.php`: adicionar bloco antes do final do arquivo:
       ```
       // Phase 31 - Disparo mensal de pesquisa NPS no aniversario do cadastro
       // (D-02). 09:00 BRT — fora dos horarios de pico (adman 11:00,
       // gross billing 12:45, sugadores 12:00).
       Schedule::command('nps:disparar-mensal')
           ->dailyAt('09:00')
           ->timezone('America/Sao_Paulo')
           ->name('nps-disparar-mensal')
           ->withoutOverlapping();
       ```

    2. Editar `app/Http/Controllers/NpsController.php`:

    2a. `generate(Request $request)`:
       - Manter validação atual `company_id => required|exists:companies,id`
       - Manter auth check pra não-admin
       - No `NpsSurvey::create([...])`: adicionar `'auto_generated' => false` explicitamente (REQ-31-08 — manuais ficam false; `month_reference` fica null por default)
       - Manter `expires_at => now()->addDays(7)` para manuais (D-12)

    2b. `respond(string $token)`:
       - Continuar lendo estrategista e consultor via wherePivot existente
       - Renomear variável `$consultant` para `$analista` para clareza pt-BR (sem quebrar nada — variável local)
       - Substituir payload Inertia render por:
         ```
         'survey' => [
             'token'              => $survey->token,
             'company_name'       => $survey->company->name,
             'estrategista_name'  => $estrategista?->name,
             'analista_name'      => $analista?->name,
             'tem_analista'       => $analista !== null,
         ],
         ```
       - REMOVER as chaves legacy `mentor_name` e `consultant_name` (UI cliente nova no Plan 31-03 só consome as chaves novas)

    2c. `submitResponse(Request $request, string $token)`:
       - Substituir validação para escala 1-5 com 3 dimensões e condicional analista:
         ```
         $data = $request->validate([
             'respondent_name'    => 'nullable|string|max:255',
             'score_estrategista' => 'required|integer|min:1|max:5',
             'score_analista'     => 'nullable|integer|min:1|max:5',
             'score_empresa'      => 'required|integer|min:1|max:5',
             'comment'            => 'nullable|string|max:2000',
         ]);
         ```
       - `NpsResponse::create([...$data, 'survey_id' => $survey->id])` continua igual (fillable já tem score_estrategista/analista/empresa do Plan 31-01)
       - Update do survey continua igual: `status=completed, completed_at=now()`
       - Retorna `Inertia::render('Nps/ThankYou')` continua

    2d. `index(Request $request)` — Plan 31-05 vai reescrever esta action; **NÃO TOCAR aqui**. Deixar o `index()` atual lendo `score_consultant/mentor/overall` — ele vai quebrar nas respostas novas (zero respostas em prod hoje pós-truncate), mas Plan 31-05 vem em sequência e corrige antes de qualquer usuário ver. Adicionar TODO no topo: `// TODO Phase 31 Plan 05: reescrever index() para nova taxonomia (estrategista/analista/empresa)`.

    3. Criar `tests/Feature/Phase31NpsSubmitTest.php`:
       - Test 1: POST `/nps/{token}` válido com 3 scores 1-5 + comment → 200, cria NpsResponse com as 3 colunas novas, survey vira completed
       - Test 2: POST sem `score_analista` (mentoria pura) → 200 OK (campo é nullable), grava NULL na coluna `score_analista`
       - Test 3: POST com `score_estrategista=0` ou `=6` → 422 com erro de validação na chave correta
       - Test 4: POST com `respondent_name` ausente → 200 OK (agora é nullable)
       - Test 5: GET `/nps/{token}` (respond) retorna payload Inertia com chaves `estrategista_name`, `analista_name`, `tem_analista` (NÃO mais `mentor_name`/`consultant_name`)
       - Test 6: POST `/nps/generate` (auth como admin) cria survey com `auto_generated=false`
  </action>
  <verify>
    <automated>php artisan test --filter=Phase31NpsSubmitTest --stop-on-failure &amp;&amp; php artisan schedule:list 2>&amp;1 | grep "nps:disparar-mensal"</automated>
  </verify>
  <done>
    - 6 testes verdes
    - `php artisan schedule:list` mostra entrada `nps-disparar-mensal` rodando dailyAt 09:00 America/Sao_Paulo
    - Submissão de 3 scores 1-5 grava nas colunas novas; submissão fora de 1-5 retorna 422
    - Endpoint `/nps/{token}` retorna props sem `mentor_name`/`consultant_name`
    - Endpoint `/nps/generate` ainda funciona (REQ-31-08) e marca `auto_generated=false`
  </done>
</task>

</tasks>

<verification>
- `php artisan list | grep nps:` mostra `nps:disparar-mensal`.
- `php artisan schedule:list` mostra `nps-disparar-mensal` 09:00 BRT.
- Suite de testes Phase31 verde: `php artisan test --filter=Phase31`.
- Mailable instancia sem erro: `php artisan tinker --execute="new App\Mail\NpsMonthlyMail('X','Y',null,'http://a.b/c','Junho/2026');"`.
- Dry-run não muta o banco: `php artisan nps:disparar-mensal --dry-run` deixa contagem `NpsSurvey::count()` inalterada.
</verification>

<success_criteria>
- Comando, mailable, template e schedule entregues e testados.
- `NpsController::submitResponse` aceita 3 scores 1-5 (estrategista required, analista nullable, empresa required) e `respondent_name` opcional.
- `NpsController::generate` mantém-se funcional e marca surveys manuais com `auto_generated=false` (REQ-31-08).
- Idempotência confirmada: 2 runs no mesmo dia produzem 0 duplicatas.
- Edge case dia 31 → último dia do mês confirmado por teste.
- Empresas sem `email_cliente` não causam erro nem warning (D-04).
</success_criteria>

<output>
Criar `.planning/phases/31-nps-mensal-automatizado/31-02-SUMMARY.md` ao final.
</output>
