---
phase: 31-nps-mensal-automatizado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_10_100001_add_email_cliente_to_companies_table.php
  - database/migrations/2026_06_10_100002_recreate_nps_responses_table.php
  - database/migrations/2026_06_10_100003_add_month_reference_and_auto_generated_to_nps_surveys_table.php
  - app/Models/Company.php
  - app/Models/NpsResponse.php
  - app/Models/NpsSurvey.php
autonomous: true
requirements:
  - REQ-31-01
  - REQ-31-02
  - REQ-31-03

must_haves:
  truths:
    - "Banco tem coluna companies.email_cliente nullable varchar 255"
    - "Tabela nps_responses foi recriada com colunas score_estrategista (1-5), score_analista (1-5 nullable), score_empresa (1-5) e respondent_name nullable"
    - "Tabela nps_surveys tem month_reference (date nullable) e auto_generated (boolean default false)"
    - "Histórico antigo de nps_surveys e nps_responses foi apagado (são testes — D-11)"
    - "Models PHP refletem a nova taxonomia (fillable, casts)"
  artifacts:
    - path: "database/migrations/2026_06_10_100001_add_email_cliente_to_companies_table.php"
      provides: "Adiciona companies.email_cliente"
      contains: "email_cliente"
    - path: "database/migrations/2026_06_10_100002_recreate_nps_responses_table.php"
      provides: "Drop + recreate nps_responses com escala 1-5"
      contains: "score_estrategista"
    - path: "database/migrations/2026_06_10_100003_add_month_reference_and_auto_generated_to_nps_surveys_table.php"
      provides: "Adiciona month_reference e auto_generated em nps_surveys + truncate"
      contains: "month_reference"
    - path: "app/Models/NpsResponse.php"
      provides: "Model com novos fillable/casts"
      contains: "score_estrategista"
    - path: "app/Models/NpsSurvey.php"
      provides: "Model com month_reference e auto_generated"
      contains: "auto_generated"
    - path: "app/Models/Company.php"
      provides: "Fillable inclui email_cliente"
      contains: "email_cliente"
  key_links:
    - from: "app/Models/NpsResponse.php"
      to: "nps_responses table"
      via: "$fillable + $casts"
      pattern: "score_estrategista.*score_analista.*score_empresa"
    - from: "app/Models/NpsSurvey.php"
      to: "nps_surveys table"
      via: "$fillable + $casts"
      pattern: "month_reference.*auto_generated"
---

<objective>
Recriar o schema NPS para a escala 1-5 com 3 dimensões (estrategista/analista/empresa), adicionar `companies.email_cliente` para o disparo automático, e instrumentar `nps_surveys` com `month_reference` + `auto_generated` para distinguir surveys mensais de manuais. Apaga histórico antigo (são testes, D-11).

Purpose: Fundação de dados para toda a Phase 31. Sem este plano, nem o comando mensal nem a UI de resposta funcionam.
Output: 3 migrations + 3 models atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md

@database/migrations/2026_04_26_152218_create_nps_surveys_table.php
@database/migrations/2026_04_26_152219_create_nps_responses_table.php
@app/Models/Company.php
@app/Models/NpsSurvey.php
@app/Models/NpsResponse.php

<interfaces>
<!-- Schema atual (referência) -->

Tabela nps_surveys (atual):
- id, token (uuid unique), company_id (FK), generated_by (FK users)
- expires_at (timestamp nullable), completed_at (timestamp nullable)
- status enum('pending','completed','expired') default 'pending'
- timestamps

Tabela nps_responses (atual — será DROPADA):
- id, survey_id (FK)
- respondent_name (string NOT NULL)
- score_consultant (tinyint unsigned 0-10)
- score_mentor (tinyint unsigned 0-10)
- score_overall (tinyint unsigned 0-10)
- comment (text nullable), timestamps

Tabela companies (relevante):
- já tem: id, name, cnpj, segment, active, status, notes, created_at, updated_at
- NÃO tem: email_cliente

Company model fillable atual:
```
'name', 'cnpj', 'adman_account_id', 'adman_store_id', 'ml_store_id',
'cust_id_status', 'marketplace', 'segment', 'active', 'status', 'notes',
'parent_company_id', 'ml_link_generated_at', 'ml_link_url',
```

NpsSurvey model fillable atual:
```
'token', 'company_id', 'generated_by', 'expires_at', 'completed_at', 'status'
```

NpsResponse model fillable atual:
```
'survey_id', 'respondent_name', 'score_consultant', 'score_mentor',
'score_overall', 'comment'
```

NpsResponse helper atual `getNpsCategory($score)` mapeia 0-10 → promotor/neutro/detrator — será REMOVIDO ou reescrito para escala 1-5 no plano 31-05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — companies.email_cliente nullable</name>
  <read_first>
    - database/migrations/2026_06_02_180000_add_cust_id_status_to_companies.php (padrão pra `ALTER` em companies)
    - database/migrations/2026_06_02_190000_add_marketplace_to_companies.php (padrão)
    - app/Models/Company.php (fillable a editar)
  </read_first>
  <files>
    - database/migrations/2026_06_10_100001_add_email_cliente_to_companies_table.php
    - app/Models/Company.php
  </files>
  <action>
    Criar migration `2026_06_10_100001_add_email_cliente_to_companies_table.php` que executa
    `Schema::table('companies', fn(Blueprint $t) => $t->string('email_cliente', 255)->nullable()->after('notes'))`.
    O `down()` faz `dropColumn('email_cliente')`. Comentário em pt-BR explicando que é o destinatário do email NPS mensal (D-04).

    Adicionar `'email_cliente'` ao array `$fillable` do `app/Models/Company.php` (mantém ordem alfabética dos vizinhos: inserir após `'notes'`). NÃO adicionar a `getActivitylogOptions()->logOnly([...])` por enquanto (a coluna pode mudar via UI livre — registrar mudaria muito o activity log; manter fora).
  </action>
  <verify>
    <automated>php artisan migrate --pretend 2>&amp;1 | grep "email_cliente" &amp;&amp; php artisan migrate &amp;&amp; php artisan tinker --execute="echo Schema::hasColumn('companies','email_cliente') ? 'OK' : 'MISS';"</automated>
  </verify>
  <done>
    - Migration criada e executada com sucesso
    - `Schema::hasColumn('companies','email_cliente')` retorna `true`
    - `Company::create([..., 'email_cliente'=>'x@y.com'])->email_cliente === 'x@y.com'` (mass assignment funciona)
  </done>
</task>

<task type="auto">
  <name>Task 2: Migration drop+recreate nps_responses com escala 1-5</name>
  <read_first>
    - database/migrations/2026_04_26_152219_create_nps_responses_table.php (schema atual)
    - app/Models/NpsResponse.php (model a reescrever)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-06, D-07, D-08, D-10)
  </read_first>
  <files>
    - database/migrations/2026_06_10_100002_recreate_nps_responses_table.php
    - app/Models/NpsResponse.php
  </files>
  <action>
    Criar migration `2026_06_10_100002_recreate_nps_responses_table.php`:
    - `up()`:
      1. `Schema::dropIfExists('nps_responses')` (D-10 — apaga histórico, são testes)
      2. `Schema::create('nps_responses', function (Blueprint $t) { ... })` com colunas:
         - `id()`, `foreignId('survey_id')->constrained('nps_surveys')->cascadeOnDelete()`
         - `string('respondent_name')->nullable()` (D-07 — nome agora é opcional)
         - `unsignedTinyInteger('score_estrategista')->comment('1-5')`
         - `unsignedTinyInteger('score_analista')->nullable()->comment('1-5, null se empresa não tem analista')`
         - `unsignedTinyInteger('score_empresa')->comment('1-5')`
         - `text('comment')->nullable()`
         - `timestamps()`
    - `down()` no-op informativo via `// reverter destrói dados — manual via backup` (padrão Phase 14).

    Reescrever `app/Models/NpsResponse.php`:
    - `$fillable = ['survey_id', 'respondent_name', 'score_estrategista', 'score_analista', 'score_empresa', 'comment']`
    - `$casts = ['score_estrategista' => 'integer', 'score_analista' => 'integer', 'score_empresa' => 'integer']`
    - `survey()` belongsTo NpsSurvey continua
    - REMOVER `getNpsCategory()` (lógica 0-10 obsoleta — escala 1-5 não tem promotor/detrator clássico; o widget Dashboard será ajustado no Plan 31-05)
    - Comentário pt-BR no topo: "Resposta NPS na escala 1-5 com 3 dimensões (estrategista, analista, empresa) — Phase 31 D-06/D-07"
  </action>
  <verify>
    <automated>php artisan migrate &amp;&amp; php artisan tinker --execute="echo (Schema::hasColumn('nps_responses','score_estrategista') &amp;&amp; Schema::hasColumn('nps_responses','score_analista') &amp;&amp; Schema::hasColumn('nps_responses','score_empresa') &amp;&amp; !Schema::hasColumn('nps_responses','score_consultant')) ? 'OK' : 'FAIL';"</automated>
  </verify>
  <done>
    - Tabela `nps_responses` recriada
    - 3 novas colunas existem: `score_estrategista`, `score_analista` (nullable), `score_empresa`
    - Colunas antigas (`score_consultant`, `score_mentor`, `score_overall`) NÃO existem
    - `respondent_name` é nullable
    - `NpsResponse` model não tem mais método `getNpsCategory()` (grep retorna 0 matches no arquivo)
  </done>
</task>

<task type="auto">
  <name>Task 3: Migration nps_surveys.month_reference + auto_generated + truncate</name>
  <read_first>
    - database/migrations/2026_04_26_152218_create_nps_surveys_table.php (schema base)
    - app/Models/NpsSurvey.php (fillable + casts a estender)
    - .planning/phases/31-nps-mensal-automatizado/31-CONTEXT.md (D-11, D-12)
  </read_first>
  <files>
    - database/migrations/2026_06_10_100003_add_month_reference_and_auto_generated_to_nps_surveys_table.php
    - app/Models/NpsSurvey.php
  </files>
  <action>
    Criar migration `2026_06_10_100003_add_month_reference_and_auto_generated_to_nps_surveys_table.php`:
    - `up()`:
      1. `DB::table('nps_surveys')->truncate()` ANTES do ALTER — D-11 apaga surveys existentes (são testes; também precisa ser antes porque a tabela `nps_responses` já foi dropada/recriada vazia no Task 2 e ON DELETE CASCADE não dispara em truncate)
      2. `Schema::table('nps_surveys', function (Blueprint $t) {
           $t->date('month_reference')->nullable()->after('completed_at')->comment('YYYY-MM-01 — apenas surveys auto_generated');
           $t->boolean('auto_generated')->default(false)->after('month_reference');
         })`
    - `down()`:
      ```
      Schema::table('nps_surveys', function (Blueprint $t) {
          $t->dropColumn(['month_reference', 'auto_generated']);
      });
      ```
      Comentário pt-BR: truncate não é revertível.

    Atualizar `app/Models/NpsSurvey.php`:
    - `$fillable`: adicionar `'month_reference'`, `'auto_generated'`
    - `$casts`: adicionar `'month_reference' => 'date'`, `'auto_generated' => 'boolean'`
    - Não alterar `getActivitylogOptions()` — `month_reference` e `auto_generated` não precisam entrar no log (mudanças massivas em batches mensais poluiriam).
  </action>
  <verify>
    <automated>php artisan migrate &amp;&amp; php artisan tinker --execute="echo (Schema::hasColumn('nps_surveys','month_reference') &amp;&amp; Schema::hasColumn('nps_surveys','auto_generated') &amp;&amp; DB::table('nps_surveys')-&gt;count() === 0) ? 'OK' : 'FAIL';"</automated>
  </verify>
  <done>
    - Schema tem `month_reference` (date nullable) e `auto_generated` (bool default false)
    - `nps_surveys` está vazia (truncate aplicado)
    - `NpsSurvey::create(['..', 'month_reference' => '2026-06-01', 'auto_generated' => true])` funciona (mass assignment + cast date)
    - `NpsSurvey::first()?->month_reference` retorna instância `Carbon` após create
  </done>
</task>

</tasks>

<verification>
- Todas as 3 migrations rodam sem erro: `php artisan migrate:status` mostra-as como `Ran`.
- Rollback funciona para email_cliente e month_reference/auto_generated; nps_responses tem down no-op com comentário.
- Grep `grep -n "score_estrategista\|score_analista\|score_empresa" app/Models/NpsResponse.php` retorna 3 hits.
- Grep `grep -n "score_consultant\|score_mentor\|score_overall" app/Models/NpsResponse.php` retorna 0 hits.
- Grep `grep -n "email_cliente" app/Models/Company.php` retorna pelo menos 1 hit (fillable).
- `php artisan tinker --execute="echo NpsSurvey::create(['token'=>Str::uuid()->toString(),'company_id'=>Company::first()->id,'generated_by'=>User::first()->id,'auto_generated'=>true,'month_reference'=>'2026-06-01'])->id;"` retorna id numérico.
</verification>

<success_criteria>
- 3 migrations aplicadas com sucesso em DB local.
- Models `Company`, `NpsSurvey`, `NpsResponse` refletem novo schema (fillable + casts).
- `NpsResponse::getNpsCategory` removido (escala 0-10 não existe mais).
- Plano 31-02, 31-03, 31-04 podem começar a referenciar `companies.email_cliente`, `nps_surveys.month_reference`, `nps_surveys.auto_generated` e `nps_responses.score_estrategista/analista/empresa` sem erro.
</success_criteria>

<output>
Criar `.planning/phases/31-nps-mensal-automatizado/31-01-SUMMARY.md` ao final.
</output>
