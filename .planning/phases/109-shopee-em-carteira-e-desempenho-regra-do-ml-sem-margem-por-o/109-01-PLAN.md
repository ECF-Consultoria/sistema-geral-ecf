---
phase: 109-shopee-em-carteira-e-desempenho-regra-do-ml-sem-margem-por-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Metrics/ShopeeMetricDiffService.php
  - app/Services/Metrics/MetricDiffDispatcher.php
  - app/Services/Portfolio/CarteiraContextService.php
  - tests/Unit/Metrics/ShopeeMetricDiffServiceTest.php
  - tests/Unit/Metrics/MetricDiffDispatcherTest.php
  - tests/Unit/Portfolio/CarteiraContextShopeeElegivelTest.php
autonomous: true
requirements: [SHOP-CAR-01]
must_haves:
  truths:
    - "ShopeeMetricDiffService::compute(Company, $periodo) devolve o MESMO shape do AdmanMetricDiffService, com contribution_margin_value/pct sempre null"
    - "Vínculo de setor 'shopee' passa a ser financial_metrics_eligible=true com financial_source='shopee'"
    - "MetricDiffDispatcher roteia por fonte ('adman'|'shopee') para o service correto"
  artifacts:
    - path: "app/Services/Metrics/ShopeeMetricDiffService.php"
      provides: "Diff de período Shopee (revenue + investimento) lendo shopee_metrics, margem null"
      contains: "function compute"
    - path: "app/Services/Metrics/MetricDiffDispatcher.php"
      provides: "Dispatcher por fonte financeira (adman/shopee)"
      contains: "function compute"
    - path: "app/Services/Portfolio/CarteiraContextService.php"
      provides: "Branch shopee em flagsFinanceirasPorSetor()"
      contains: "SETOR_SHOPEE"
  key_links:
    - from: "MetricDiffDispatcher"
      to: "ShopeeMetricDiffService / AdmanMetricDiffService"
      via: "match por financial_source"
      pattern: "match.*source"
    - from: "CarteiraContextService::flagsFinanceirasPorSetor"
      to: "financial_source='shopee'"
      via: "match ($setor) branch Servico::SETOR_SHOPEE"
      pattern: "SETOR_SHOPEE"
---

<objective>
Fundação da Fase 109: criar a fonte de dados financeira Shopee espelhando o
contrato do `AdmanMetricDiffService`, abrir o portão de elegibilidade financeira
para o setor `shopee` e introduzir um dispatcher por fonte. Nenhum consumidor
(carteira/desempenho) é alterado neste plano — só a base que os dois herdam.

Purpose: hoje `AdmanMetricDiffService::compute()` é a fonte ÚNICA de números da
carteira e do desempenho, e só o setor `performance` é elegível financeiro. Este
plano cria o irmão Shopee e o roteador, sem tocar em nenhuma tela — os Planos 02
(carteira) e 03 (desempenho) plugam depois.

Output: `ShopeeMetricDiffService`, `MetricDiffDispatcher`, branch `shopee` em
`flagsFinanceirasPorSetor()`, cobertos por testes unit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-shopee-em-carteira-e-desempenho-regra-do-ml-sem-margem-por-o/109-CONTEXT.md
@.planning/phases/109-shopee-em-carteira-e-desempenho-regra-do-ml-sem-margem-por-o/109-RESEARCH.md

<interfaces>
<!-- Contrato a ESPELHAR (fonte única atual). Extraído do codebase. -->

AdmanMetricDiffService::compute(Company $company, array $periodo): array
Retorno (shape a replicar EXATAMENTE):
  [
    'company_id' => int,
    'period'     => array,           // o $periodo recebido
    'metrics'    => [
       'revenue'                   => ['value'=>?float,'diff_pct'=>?float,'diff_source'=>?string],
       'contribution_margin_value' => ['value'=>?float,'diff_pct'=>?float,'diff_source'=>?string],
       'contribution_margin_pct'   => ['value'=>?float,'diff_pct'=>?float,'diff_source'=>?string],
    ],
    'quality'    => ['status'=>string,'source'=>string,'computed_at'=>string],
  ]

$periodo (MetricPeriodResolver::resolve) — chaves usadas: current_start,
current_end, baseline_start, baseline_end, comparison_mode (todas 'Y-m-d').

ShopeeMetric (model): colunas company_id, reference_date (date),
revenue (decimal:2), ad_expense (decimal:2, NULLABLE), orders_count.
Sem margem/CMV. company() belongsTo.

CarteiraContextService::flagsFinanceirasPorSetor(string $setor): array
Hoje: só Servico::SETOR_PERFORMANCE → source='adman', eligible=true; default → false/null/false.

Servico::SETOR_SHOPEE = 'shopee'; Servico::isShopee().
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário autenticado (admin/consultor) → controllers → services | leitura interna, sem input externo novo nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Information Disclosure | ShopeeMetricDiffService lê shopee_metrics por company_id | mitigate | service não recebe input do usuário; company_id vem de vínculos já autorizados pelo CarteiraContextService |
| T-109-02 | Tampering | dispatcher recebe string $source | mitigate | `match` com default que lança InvalidArgumentException para fonte desconhecida (whitelist adman/shopee) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShopeeMetricDiffService espelhando o contrato Adman</name>
  <read_first>
    - app/Services/Metrics/AdmanMetricDiffService.php (contrato/shape a espelhar: buildResult, emptyMetrics, diffPctGuardado, somasComGuards, cacheDay, política de cache por qualidade)
    - app/Models/ShopeeMetric.php (colunas revenue, ad_expense nullable, reference_date)
    - app/Services/Metrics/MetricPeriodResolver.php (chaves de $periodo: current_start/current_end/baseline_start/baseline_end)
    - database/migrations/2026_07_19_120000_create_shopee_metrics_table.php e 2026_07_22_120000_add_ads_columns_to_shopee_metrics_table.php
  </read_first>
  <behavior>
    - revenue.value = SUM(shopee_metrics.revenue) na janela current; dias sem venda não têm linha → contam como zero (somar direto, NÃO aplicar guard de "interseção de dias-comuns" do Adman — ausência aqui é zero real de venda, não dado faltante).
    - revenue.diff_pct = (soma_current - soma_baseline)/soma_baseline*100, 2 casas; baseline <= 0 → null (reusar semântica de diffPctGuardado do Adman); diff_source sempre 'calculated_fallback' (Shopee não tem endpoint .diff nativo).
    - contribution_margin_value e contribution_margin_pct: value/diff_pct/diff_source SEMPRE null (Shopee não tem margem).
    - Campo EXTRA para a carteira: investment (fora do bloco metrics OU como chave adicional acordada) = SUM(ad_expense) na janela current; se COUNT(ad_expense NOT NULL) na janela = 0 → investment.value = null (distinguir "sem dado de Ads / fora dos ~6 meses" de "R$ 0 investido"); investment.diff_pct segue a mesma regra de baseline<=0 → null.
    - quality.source = 'shopee'; quality.status via a mesma lógica de buildQuality (complete/partial/missing) contando só as métricas com value não-null — como margem é sempre null, um Shopee com revenue presente é 'partial' pelo critério Adman; documentar isso e NÃO deixar o status Shopee 'partial' poluir consumidores (a carteira decide status próprio; o desempenho lê só diff_pct).
    - Cache: mesma chave por (company_id, janela current, DIA BRT) e mesmo padrão de TTL do Adman (cacheDay()); prefixo 'shopee:diff:v1:'.
  </behavior>
  <action>
    Criar `App\Services\Metrics\ShopeeMetricDiffService` com `compute(Company $company, array $periodo): array` devolvendo o shape do bloco `<interfaces>`. Somar `shopee_metrics.revenue` nas janelas `current`/`baseline` do `$periodo` (whereBetween reference_date), tratando dias ausentes como zero. Derivar `revenue.diff_pct` com guard de baseline<=0 → null (espelhar `diffPctGuardado`). Deixar `contribution_margin_value`/`contribution_margin_pct` sempre null. Expor `investment` (SUM ad_expense; null quando nenhuma linha da janela tem ad_expense NOT NULL). Reusar `buildResult`/`emptyMetrics`/`buildQuality`/`cacheDay` como referência (implementação própria, sem herança — Adman é HTTP, Shopee é só DB). Comentários pt-BR. NÃO chamar AdmanService nem HTTP — Shopee é 100% leitura local determinística. NÃO alterar MetricPeriodResolver.
  </action>
  <verify>
    <automated>php artisan test tests/Unit/Metrics/ShopeeMetricDiffServiceTest.php</automated>
  </verify>
  <acceptance_criteria>
    - Teste: revenue.value = soma esperada; diff_pct correto vs baseline; baseline zero → diff_pct null.
    - Teste: dia sem linha na janela conta como zero (não infla nem quebra).
    - Teste: contribution_margin_value.value E contribution_margin_pct.value sempre null.
    - Teste: investment.value null quando toda a janela tem ad_expense NULL; número quando há ao menos 1 dia com ad_expense.
    - Teste: shape idêntico ao Adman (chaves metrics.revenue/contribution_margin_value/contribution_margin_pct presentes; company_id, period, quality).
  </acceptance_criteria>
  <done>ShopeeMetricDiffService existe, espelha o contrato, margem sempre null, investimento null-aware, testes unit verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Dispatcher por fonte + branch shopee elegível</name>
  <read_first>
    - app/Services/Portfolio/CarteiraContextService.php (flagsFinanceirasPorSetor :247-263; normalizar :224-237; decisão 4 do docblock sobre setores default)
    - app/Services/Metrics/AdmanMetricDiffService.php (assinatura compute)
    - app/Services/Metrics/ShopeeMetricDiffService.php (criado na Task 1)
    - app/Models/Servico.php (SETOR_SHOPEE, SETOR_PERFORMANCE)
  </read_first>
  <action>
    (1) Em `CarteiraContextService::flagsFinanceirasPorSetor()`, adicionar o branch `Servico::SETOR_SHOPEE` ANTES do `default`, retornando `has_financial_source=true`, `financial_source='shopee'`, `financial_metrics_eligible=true`. Manter o branch `performance` intocado e o `default` (polos/publicacao/outros) como está. Atualizar o comentário pt-BR que hoje diz "Shopee ... caem aqui — sem fonte financeira" para refletir a nova regra (decisão travada 2026-07-23).
    (2) Criar `App\Services\Metrics\MetricDiffDispatcher` com `compute(Company $company, array $periodo, string $source): array` que injeta `AdmanMetricDiffService` e `ShopeeMetricDiffService` e roteia via `match($source)`: 'adman' → adman, 'shopee' → shopee, default → `throw InvalidArgumentException` (whitelist, T-109-02). O dispatcher NÃO decide a fonte — recebe o `financial_source` que o consumidor já resolveu do vínculo.
  </action>
  <verify>
    <automated>php artisan test tests/Unit/Metrics/MetricDiffDispatcherTest.php tests/Unit/Portfolio/CarteiraContextShopeeElegivelTest.php</automated>
  </verify>
  <acceptance_criteria>
    - Teste: vínculo de setor 'shopee' resolvido por forUser() vem com financial_metrics_eligible=true e financial_source='shopee'.
    - Teste: vínculo 'performance' segue financial_source='adman', eligible=true (regressão).
    - Teste: setor desconhecido segue eligible=false (default intocado).
    - Teste: dispatcher com source='shopee' delega ao ShopeeMetricDiffService; 'adman' ao AdmanMetricDiffService; source inválido lança InvalidArgumentException.
  </acceptance_criteria>
  <done>Branch shopee elegível ativo; dispatcher roteia por fonte com whitelist; testes verdes; nenhum consumidor alterado ainda.</done>
</task>

</tasks>

<verification>
- `php artisan test tests/Unit/Metrics tests/Unit/Portfolio` verde.
- Suíte existente de CarteiraContextService/Desempenho NÃO deve quebrar por este plano SOZINHO exceto testes que assumam Shopee=não-elegível (esses são atualizados no Plano 03, que muda a semântica do score). Se algum teste unit de carteira quebrar aqui por elegibilidade Shopee, atualizar a expectativa neste plano.
</verification>

<success_criteria>
- ShopeeMetricDiffService produz revenue+investimento por período (mesma janela do resolver) e margem null.
- flagsFinanceirasPorSetor reconhece shopee como fonte financeira elegível.
- MetricDiffDispatcher roteia adman/shopee.
</success_criteria>

<output>
Criar `.planning/phases/109-shopee-em-carteira-e-desempenho-regra-do-ml-sem-margem-por-o/109-01-SUMMARY.md` ao concluir.
</output>
