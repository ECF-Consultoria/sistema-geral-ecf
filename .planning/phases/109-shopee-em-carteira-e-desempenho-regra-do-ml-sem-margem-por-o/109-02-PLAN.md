---
phase: 109-shopee-em-carteira-e-desempenho-regra-do-ml-sem-margem-por-o
plan: 02
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - app/Http/Controllers/PortfolioController.php
  - resources/js/Pages/Portfolio/AdminCarteira.jsx
  - resources/js/Pages/Portfolio/Transparencia.jsx
  - resources/js/Pages/Portfolio/Carteiras.jsx
  - resources/js/Pages/Portfolio/Show.jsx
  - tests/Feature/PortfolioShopeeCarteiraTest.php
autonomous: true
requirements: [SHOP-CAR-01, SHOP-CAR-02]
must_haves:
  truths:
    - "Profissional com empresa Shopee vê faturamento e investimento Shopee por período (em-curso e bônus), MESMA janela do ML"
    - "Empresa Shopee mostra margem como '—' (sem dado), sem poluir banner de erro de sync"
    - "Empresa ML+Shopee não duplica faturamento; profissional só-Shopee não quebra a tela"
    - "Auto-visualização /portfolio (renderPortfolio → Portfolio/Show.jsx) do próprio responsável Shopee mostra faturamento+investimento Shopee, margem '—', SEM re-rotear nem mexer nas features ricas (sugadores/PPA/NPS/meta)"
  artifacts:
    - path: "app/Http/Controllers/PortfolioController.php"
      provides: "Dispatch/leitura por fonte nas 4 telas de carteira (transparencia/renderCarteiraProfissional/renderCarteirasConsolidadas/renderPortfolio)"
      contains: "MetricDiffDispatcher"
    - path: "resources/js/Pages/Portfolio/AdminCarteira.jsx"
      provides: "Render de faturamento+investimento Shopee, margem '—'"
    - path: "resources/js/Pages/Portfolio/Show.jsx"
      provides: "Bloco financeiro da self-view /portfolio inclui Shopee (faturamento+investimento, margem '—'); features ricas intactas"
    - path: "tests/Feature/PortfolioShopeeCarteiraTest.php"
      provides: "Cobertura carteira Shopee (só-Shopee, misto, em-curso, bônus, self-view /portfolio)"
  key_links:
    - from: "PortfolioController::transparencia / renderCarteiraProfissional"
      to: "MetricDiffDispatcher::compute(company, periodo, financial_source)"
      via: "fonte resolvida do vínculo (financial_source)"
      pattern: "diffDispatcher->compute"
    - from: "renderCarteirasConsolidadas"
      to: "shopee_metrics (SUM revenue/ad_expense)"
      via: "caminho Shopee para empresas de fonte shopee"
      pattern: "ShopeeMetric|shopee_metrics"
    - from: "renderPortfolio (self-view /portfolio → Portfolio/Show.jsx)"
      to: "shopee_metrics (SUM revenue/ad_expense)"
      via: "caminho Shopee para empresas de fonte shopee no bloco financeiro"
      pattern: "ShopeeMetric|shopee_metrics"
---

<objective>
Plugar a fonte Shopee nas QUATRO telas de carteira, roteando por fonte:
Transparência, carteira individual (AdminCarteira), carteira consolidada admin
(Carteiras) e a auto-visualização do próprio profissional (renderPortfolio →
Portfolio/Show.jsx, rota `/portfolio`). Empresas Shopee passam a exibir
faturamento + investimento por período (mesma janela do ML), com margem "—".

Purpose: fecha SHOP-CAR-01/02 no lado da carteira. Hoje empresa Shopee cai em
`fonte='shopee'`/`status='sem_fonte'` com números null; após o Plano 01 tornar
o vínculo elegível, os consumidores precisam rotear para o service Shopee (não
para o Adman) e a UI precisa mostrar os números reais. A self-view `/portfolio`
foi INCLUÍDA no escopo por decisão do usuário (2026-07-23, pós plan-check) — é a
tela que o próprio Gustavo/Felipe usa pra ver a carteira dele.

Output: PortfolioController com dispatch/leitura por fonte nas 4 telas, 4 JSX
renderizando Shopee, feature tests (incluindo self-view), build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-shopee-em-carteira-e-desempenho-regra-do-ml-sem-margem-por-o/109-CONTEXT.md
@.planning/phases/109-shopee-em-carteira-e-desempenho-regra-do-ml-sem-margem-por-o/109-01-SUMMARY.md

<interfaces>
<!-- Extraído do codebase. O executor NÃO precisa explorar. -->

MetricDiffDispatcher::compute(Company $company, array $periodo, string $source): array
  → mesmo shape do AdmanMetricDiffService; para source='shopee' devolve
    metrics.revenue com value/diff_pct, contribution_margin_* null, e campo
    extra investment.value/diff_pct.

Vínculo (CarteiraContextService::forUser) expõe por linha:
  company_id, setor, financial_source ('adman'|'shopee'|null),
  financial_metrics_eligible (bool).

PortfolioController — pontos de integração (linhas aproximadas, confirmadas no código):
  - CAMINHO DISPATCHER (diff por empresa via admanDiffService->compute):
    - transparencia() :144, chamada diff :249, render Portfolio/Transparencia :287
    - renderCarteiraProfissional() :330, chamada diff :577, leituras diretas
      AdmanMetric $atualPorEmpresa/$anteriorPorEmpresa :438-446, render Portfolio/AdminCarteira :753
  - CAMINHO SUM-BATCH (SUM AdmanMetric agregado, sem dispatcher):
    - renderCarteirasConsolidadas() :840-1150, SUM AdmanMetric :1031-1033
      (agregação por analista/estrategista), render Portfolio/Carteiras :1121
    - renderPortfolio() :1152-1560 → SELF-VIEW da rota `/portfolio`
      (auto-visualização; renderiza Portfolio/Show.jsx). Usa `$user->companies()`
      :1189 + SUM AdmanMetric mês atual/anterior :1201-1216 + batch de cache
      Adman :1243-1248 + map por empresa :1272-1387. É AQUI que fica o SUM
      AdmanMetric das linhas ~1201-1216 (NÃO em renderCarteirasConsolidadas).
  - contextoFiltro() :84 (whitelist ?contexto=todos/performance/shopee — já existe)

REGRA DE DESEMPATE DE FONTE (TRAVADA — decisão do usuário 2026-07-23; TEXTO
IDÊNTICO ao Plano 03 / Desempenho): resolver a `financial_source` por empresa a
partir dos vínculos elegíveis da empresa no mês — quando a empresa tiver vínculo
performance elegível E vínculo shopee, a fonte é **'adman'** (performance vence);
empresa só-shopee → fonte **'shopee'**; empresa só-performance → fonte **'adman'**.
Carteira (renderCarteiraProfissional / renderCarteirasConsolidadas / transparencia
/ renderPortfolio) e Desempenho (DesempenhoScoreService) DEVEM aplicar EXATAMENTE
esta regra — senão os números divergem entre carteira, ranking e bônus.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário autenticado → PortfolioController → dispatcher | ?mes/?contexto/?modo já whitelisted (regex/enum existentes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-03 | Tampering | ?contexto=shopee / ?mes=YYYY-MM | mitigate | reusar contextoFiltro() (whitelist) e regex de ?mes já existentes; não repassar string crua ao resolver |
| T-109-04 | Information Disclosure | carteira de outro profissional | accept | rota /admin/users/{id}/portfolio já protegida por role; /portfolio é a própria (show() :120 roteia self→renderPortfolio); sem mudança de escopo de acesso |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch por fonte em transparencia e renderCarteiraProfissional</name>
  <read_first>
    - app/Http/Controllers/PortfolioController.php (transparencia :144-312; renderCarteiraProfissional :330-760; construtor :31-38; imports :1-27)
    - app/Services/Metrics/MetricDiffDispatcher.php (do Plano 01)
    - app/Services/Metrics/ShopeeMetricDiffService.php (campo investment)
  </read_first>
  <action>
    Injetar `MetricDiffDispatcher` no construtor do PortfolioController (manter `admanDiffService` para os call-sites não migrados). Nas duas funções, onde hoje se chama `$this->admanDiffService->compute($c, $periodo)` (linhas :249 e :577), resolver a `financial_source` da empresa a partir dos vínculos já carregados (`$vs`/`$porEmpresa` → financial_source do vínculo elegível da empresa) e chamar `$this->diffDispatcher->compute($c, $periodo, $source)`.
    REGRA DE DESEMPATE DE FONTE (TRAVADA — decisão do usuário 2026-07-23; TEXTO IDÊNTICO ao Plano 03 / Desempenho): resolver a `financial_source` por empresa a partir dos vínculos elegíveis da empresa no mês — quando a empresa tiver vínculo performance elegível E vínculo shopee, a fonte é **'adman'** (performance vence); empresa só-shopee → fonte **'shopee'**; empresa só-performance → fonte **'adman'**. Carteira (renderCarteiraProfissional / renderCarteirasConsolidadas / transparencia / renderPortfolio) e Desempenho DEVEM aplicar EXATAMENTE esta regra — senão os números divergem entre carteira, ranking e bônus.
    Para empresa Shopee: `faturamento`/`faturamento_var_pct` vêm de metrics.revenue; `ad_spend`/`investimento` do campo `investment`; margem_rs/margem_pct/margem_variacao_pct = null (margem "—"); o badge `fonte` já resolve 'shopee'. Ajustar a derivação de `status` para que empresa Shopee com revenue presente NÃO caia em 'sem_dados'/'sem_fonte' — status próprio (ex.: 'completo' quando há faturamento, 'sem_baseline' quando diff_pct null), sem depender de margem (Shopee nunca tem). Manter empresa performance 100% no caminho Adman (regressão zero).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/PortfolioShopeeCarteiraTest.php --filter=transparencia</automated>
  </verify>
  <acceptance_criteria>
    - Empresa Shopee na transparência/AdminCarteira retorna faturamento != null e investimento do ad_expense, margem null.
    - Empresa performance segue idêntica ao comportamento atual (fonte='adman', margem presente).
    - status de empresa Shopee com faturamento não é 'sem_fonte' nem 'sem_dados'.
    - Empresa com vínculo performance elegível E shopee resolve fonte='adman' (desempate travado).
  </acceptance_criteria>
  <done>transparencia e renderCarteiraProfissional roteiam por fonte (com desempate travado); Shopee mostra faturamento+investimento, margem null.</done>
</task>

<task type="auto">
  <name>Task 2: Caminho Shopee em renderCarteirasConsolidadas + renderPortfolio (self-view /portfolio) + feature tests</name>
  <read_first>
    - app/Http/Controllers/PortfolioController.php (renderCarteirasConsolidadas :840-1150 — SUM AdmanMetric :1031-1033; renderPortfolio :1152-1560 — $user->companies() :1189, SUM AdmanMetric mês atual/anterior :1201-1216, batch cache Adman :1243-1248, map por empresa :1272-1387, totais :1490-1514; render Carteiras :1121; render Show :~1555)
    - app/Models/ShopeeMetric.php
    - app/Models/Company.php (shopeeToken() — empresa conectada Shopee; consultorDoServico/estrategistaDoServico)
  </read_first>
  <action>
    (A) Em `renderCarteirasConsolidadas`, adicionar um caminho de leitura Shopee para as empresas cuja fonte financeira é shopee (detectar via vínculo shopee elegível OU `shopeeToken` ativo — usar o mesmo critério de "empresa Shopee" do CONTEXT; NUNCA filtrar company_users só por role — usar os helpers `consultorDoServico`/`estrategistaDoServico`/vínculo por serviço). Para essas empresas, `revenue`/`ad_spend` do período vêm de `SUM(shopee_metrics.revenue)`/`SUM(shopee_metrics.ad_expense)` nas janelas atual/anterior (dias ausentes = zero; ad_expense null-aware), e as colunas de margem ficam null. Empresa performance segue no SUM AdmanMetric atual (:1031-1033).

    (B) Em `renderPortfolio` (self-view da rota `/portfolio` → Portfolio/Show.jsx), ESPELHAR o tratamento financeiro de renderCarteiraProfissional: injetar os dados Shopee (faturamento + investimento por período, MESMA janela) no bloco financeiro por empresa, para as empresas de fonte 'shopee'. Onde hoje o revenue/ad_spend vêm do SUM AdmanMetric (:1201-1216) / batch cache Adman (:1243-1248), adicionar a leitura Shopee (`SUM(shopee_metrics.revenue)`/`SUM(shopee_metrics.ad_expense)` nas janelas atual/anterior, dias ausentes=zero, ad_expense null-aware) para empresas cuja fonte é 'shopee'; margem (R$/%/var) = null nessas empresas. RESTRIÇÕES DE ESCOPO (decisão do usuário 2026-07-23): (1) SÓ o bloco financeiro passa a incluir Shopee — NÃO re-rotear, NÃO mexer nas features ricas de Show.jsx (sugadores/PPA/NPS/meta); (2) NÃO consertar o bug legado de consolidação `$user->companies()` (:1189) — está FORA do escopo (documentado no CONTEXT); só garantir que os números Shopee apareçam corretos via o caminho por fonte, sem PIORAR o legado.

    (C) DESEMPATE DE FONTE — aplicar em (A) e (B) exatamente a mesma REGRA TRAVADA do Task 1 / Plano 03: empresa com vínculo performance elegível E shopee → fonte='adman' (performance vence); só-shopee → 'shopee'; só-performance → 'adman'. Garantir que empresa ML+Shopee (mesma company_id em dois vínculos) NÃO some faturamento duas vezes (dedup por company_id + fonte).

    (D) Escrever a feature test cobrindo: só-Shopee vê faturamento/investimento em `?modo=em_curso` e `?modo=bonus_atual`; misto não duplica; só-Shopee não quebra a tela; e a SELF-VIEW `/portfolio` (renderPortfolio) de um responsável Shopee mostra faturamento+investimento Shopee e margem null, sem quebrar as features ricas.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/PortfolioShopeeCarteiraTest.php</automated>
  </verify>
  <acceptance_criteria>
    - Carteiras consolidadas mostra faturamento Shopee somado de shopee_metrics na janela do período.
    - Investimento Shopee = SUM(ad_expense); null quando janela sem Ads.
    - Empresa com vínculo performance E shopee não conta faturamento em dobro (desempate 'adman' vence).
    - Teste passa nos dois modos (em_curso e bonus_atual) usando MetricPeriodResolver (janela idêntica ao ML).
    - Self-view `/portfolio` (renderPortfolio): responsável Shopee vê faturamento+investimento Shopee, margem null; features ricas (sugadores/PPA/NPS/meta) continuam presentes no payload (não regridem).
  </acceptance_criteria>
  <done>renderCarteirasConsolidadas e renderPortfolio leem Shopee de shopee_metrics (com desempate travado); feature tests verdes cobrindo só-Shopee/misto/em-curso/bônus/self-view.</done>
</task>

<task type="auto">
  <name>Task 3: UI carteira Shopee (4 páginas) + build</name>
  <read_first>
    - resources/js/Pages/Portfolio/AdminCarteira.jsx (render de faturamento/margem/investimento, badge fonte)
    - resources/js/Pages/Portfolio/Transparencia.jsx (tabela por empresa)
    - resources/js/Pages/Portfolio/Carteiras.jsx (carteira consolidada)
    - resources/js/Pages/Portfolio/Show.jsx (self-view /portfolio — SÓ o bloco financeiro/tabela de empresas; NÃO tocar sugadores/PPA/NPS/meta)
    - app/Http/Controllers/PortfolioController.php (chaves do payload que as páginas consomem: fonte, faturamento, faturamento_var_pct, margem_*, ad_spend/investimento, status)
  </read_first>
  <action>
    Ajustar as 4 páginas para renderizar corretamente empresa `fonte='shopee'`: exibir faturamento e investimento (ad_spend) como números reais; margem (R$ e %) como "—" com tooltip pt-BR "Shopee ainda não fornece margem"; badge de fonte "Shopee"; não mostrar banner de erro de sync para Shopee (margem null é esperada, não é problema). Em Show.jsx, mexer SOMENTE no bloco financeiro/tabela de empresas — deixar sugadores/PPA/NPS/meta e demais widgets do profissional intocados. Reaproveitar os formatadores existentes (fmtBRL, fmtPct). PITFALL ROLLUP: qualquer flag booleana derivada (ex.: `ehShopee = fonte === 'shopee'`) DEVE ser computada DENTRO do callback do `.map()` de cada linha, nunca como variável de escopo do componente usada dentro do map (senão o bundle Rollup some com a variável — ReferenceError em prod). Rodar `npm run build` ao final (convenção do projeto).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Build Vite conclui sem erro.
    - As 4 páginas exibem faturamento+investimento Shopee e margem "—".
    - Show.jsx: bloco financeiro inclui Shopee; features ricas (sugadores/PPA/NPS/meta) inalteradas.
    - Flags booleanas por-linha computadas dentro do .map() (sem var de escopo usada no map).
  </acceptance_criteria>
  <done>UI das 4 carteiras renderiza Shopee corretamente; build ok.</done>
</task>

</tasks>

<verification>
- `php artisan test tests/Feature/PortfolioShopeeCarteiraTest.php` verde.
- Regressão: suíte de carteira/portfolio existente verde (empresas performance intocadas; features ricas de Show.jsx intactas).
- `npm run build` sem erro.
</verification>

<success_criteria>
- Empresas Shopee aparecem nas 4 carteiras (incluindo self-view /portfolio) com faturamento + investimento por período, margem "—", nos modos em-curso e bônus.
- Nenhuma regressão em empresas performance nem nas features ricas de Show.jsx.
</success_criteria>

<output>
Criar `.planning/phases/109-shopee-em-carteira-e-desempenho-regra-do-ml-sem-margem-por-o/109-02-SUMMARY.md` ao concluir.
</output>
