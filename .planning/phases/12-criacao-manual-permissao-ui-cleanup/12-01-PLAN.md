---
phase: 12-criacao-manual-permissao-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/NotificacaoController.php
  - app/Notifications/ManualNotification.php
  - app/Console/Commands/NotificationsCleanup.php
  - routes/console.php
  - routes/web.php
  - resources/js/Layouts/AppLayout.jsx
  - resources/js/Pages/Notificacoes/Nova.jsx
  - tests/Feature/Notifications/Phase12ManualTest.php
autonomous: true
requirements:
  - ENVIO-01
  - ENVIO-02
  - ENVIO-03
  - ENVIO-04
  - ENVIO-05
  - PERM-04
  - POLL-04
  - POLL-05

must_haves:
  truths:
    - "Existe `App\\Notifications\\ManualNotification` extends `BaseNotification` com construtor `(string $titulo, string $mensagem, int $autorUserId, ?array $meta = [])` — categoria fixa `Categoria::MANUAL`, `url = route('notificacoes.index')`."
    - "Existem 2 métodos novos em `NotificacaoController`: `nova(Request $request): Response` (renderiza Inertia `Notificacoes/Nova`) e `criar(Request $request): RedirectResponse` (recebe form, valida, resolve destinatários, dispatcha, loga e redireciona com flash success contendo a contagem)."
    - "Ambos os métodos são gated por middleware `permission:notificacoes.criar`. Usuários sem a permissão recebem 403 ao acessar `/notificacoes/nova` (ENVIO-05). A permission já existe no catálogo desde Phase 8."
    - "Validation no `criar()`: `titulo` required|string|max:100, `mensagem` required|string|max:1000, `publico` required|in:usuario,setor,lideres,todos, `usuario_id` required_if:publico,usuario|exists:users,id, `setor_id` required_if:publico,setor|exists:setores,id (ENVIO-03)."
    - "Resolução de destinatários no `criar()`: `usuario` → 1 User; `setor` → `Setor::find($setor_id)->membros`; `lideres` → `User::whereHas('setoresLiderados')->get()`; `todos` → `User::where('active', true)->get()` (ENVIO-02)."
    - "Após dispatch, controller usa `activity()->causedBy($request->user())->withProperties([...])->log('Notificação manual enviada')` registrando `publico`, `usuario_id` (quando aplicável), `setor_id` (quando aplicável), `destinatarios_count`, `titulo` (POLL-05). Disparos automáticos da Phase 11 NÃO chamam `activity()` — só os manuais."
    - "Flash success contém a contagem exata: `back()->with('success', \"Notificação enviada para {$count} destinatário(s).\")` (ENVIO-04)."
    - "AppLayout sidebar registra novo item: `{ label: 'Enviar notificação', routeName: 'notificacoes.nova', page: 'Notificacoes/Nova', icon: Send, permission: 'notificacoes.criar' }` — aparece somente para users com permission (ENVIO-01, alinhado com pattern já usado de filtragem por `effectivePermissions`)."
    - "Existe `App\\Console\\Commands\\NotificationsCleanup` com signature `notifications:cleanup`. Handler executa `DatabaseNotification::whereNotNull('read_at')->where('read_at', '<', now()->subDays(30))->delete()` e log info com contagem deletada."
    - "`routes/console.php` declara `Schedule::command('notifications:cleanup')->dailyAt('04:00')->name('notifications-cleanup')->withoutOverlapping();` (POLL-04, horário fora da janela do calculate-goal-results 06:00)."
    - "Página `resources/js/Pages/Notificacoes/Nova.jsx` consome `useForm` do Inertia, valida client-side via maxLength, mostra contadores `{n}/100` e `{n}/1000`, e usa `<select>` nativo para o público + busca/select para usuário ou setor conforme escolha."
    - "Suíte `Phase12ManualTest` tem 9 testes: PERM-04 (catalog contém grupo), ENVIO-01 (sidebar/route gating via response), ENVIO-02 (4 cenários de público), ENVIO-03 (validation falha sem título/mensagem ou >limit), ENVIO-04 (flash success + count), ENVIO-05 (user sem permission → 403 em GET e POST), POLL-04 (command remove lidas >30d, preserva unread e lidas recentes), POLL-05 (activity_log entry criada com causedBy + properties corretas)."
  artifacts:
    - path: "app/Notifications/ManualNotification.php"
      provides: "Subclasse concreta para envios manuais — categoria MANUAL + autor_user_id real"
      contains: "class ManualNotification extends BaseNotification"
    - path: "app/Console/Commands/NotificationsCleanup.php"
      provides: "Artisan command para cleanup diário"
      contains: "notifications:cleanup"
    - path: "resources/js/Pages/Notificacoes/Nova.jsx"
      provides: "UI de envio manual com targeting"
      contains: "useForm"
    - path: "tests/Feature/Notifications/Phase12ManualTest.php"
      provides: "9 testes E2E cobrindo todos os requirements"
      contains: "class Phase12ManualTest"
---

## Phase Goal

**As a** admin (ou usuário com permissão `notificacoes.criar`), **I want to** enviar uma notificação manual escolhendo o público (usuário/setor/líderes/todos) e ver registrado quem mandou no activity log, **so that** comunicações ad-hoc cheguem na UI já entregue (sino + página) sem precisar de Tinker, e a tabela `notifications` permanece enxuta via cleanup diário.

<objective>
Fechamento da milestone v3.0 — última fase. Cria a página de envio manual, o command artisan de cleanup, o activity log do envio, e atualiza o sidebar. PERM-04 já está entregue pela Phase 8 (catalog auto-injetado em `/sistema/setores`), mas adicionamos teste de PERM-04 nesta fase para fechar a cobertura.

**Decisões inline:**
- D-01: `ManualNotification` é classe concreta separada de `BaseNotification` para clareza nos testes e `activity_log` (filtro por type FQCN).
- D-02: Gating do endpoint usa middleware `permission:notificacoes.criar` (já existe no projeto — `EnsureUserHasPermission`). Verificar nome exato do alias no `bootstrap/app.php`; se for `permission`, manter; se for outro nome, ajustar.
- D-03: `publico = 'lideres'` resolve via `User::whereHas('setoresLiderados')->get()` para incluir qualquer user em `setor_lideres`, conforme REQUIREMENT literal.
- D-04: `publico = 'todos'` filtra `where('active', true)` — descobrimos que User tem `active` boolean (verificado).
- D-05: Activity log nas dispatch sites manuais APENAS — automáticos (Phase 11) não logam para evitar inundação (POLL-05 explícito).
- D-06: Cleanup roda às 04:00 (antes do calculate-goal-results 06:00 e sync adman). Janela exclusiva e idempotente (deletes sem cascata).
- D-07: Item de sidebar usa ícone `Send` (lucide). Permission filter já é padrão do AppLayout — adicionar o item ao array e pronto.

**Output:**
- 1 controller estendido (+2 métodos)
- 2 rotas novas: `notificacoes.nova` (GET) e `notificacoes.criar` (POST)
- 1 classe Notification concreta
- 1 Artisan command
- 1 entrada no scheduler
- 1 item no sidebar
- 1 página React nova
- 1 suíte com 9 testes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@./CLAUDE.md
@app/Notifications/BaseNotification.php
@app/Notifications/Categoria.php
@app/Notifications/MetaAtribuidaNotification.php
@app/Support/Permissions.php
@app/Http/Controllers/NotificacaoController.php
@app/Models/User.php
@app/Models/Setor.php
@app/Http/Controllers/Admin/SetorController.php
@routes/web.php
@routes/console.php
@resources/js/Layouts/AppLayout.jsx
@resources/js/Pages/Notificacoes/Index.jsx
@tests/Feature/Notifications/Phase9BackendTest.php
@bootstrap/app.php

<interfaces>
**`App\Notifications\ManualNotification` (template):**
```php
namespace App\Notifications;

class ManualNotification extends BaseNotification
{
    /** Notificação manual criada por usuário com `notificacoes.criar`. */
    public function __construct(string $titulo, string $mensagem, int $autorUserId, ?array $meta = [])
    {
        parent::__construct(
            titulo:      $titulo,
            mensagem:    $mensagem,
            categoria:   Categoria::MANUAL,
            autorUserId: $autorUserId,
            url:         route('notificacoes.index'),
            meta:        $meta ?? [],
        );
    }
}
```

**Métodos novos no controller:**
```php
public function nova(Request $request): Response
{
    return Inertia::render('Notificacoes/Nova', [
        'setores' => \App\Models\Setor::orderBy('nome')->get(['id', 'nome']),
    ]);
}

public function criar(Request $request): RedirectResponse
{
    $data = $request->validate([
        'titulo'      => 'required|string|max:100',
        'mensagem'    => 'required|string|max:1000',
        'publico'     => 'required|in:usuario,setor,lideres,todos',
        'usuario_id'  => 'required_if:publico,usuario|exists:users,id',
        'setor_id'    => 'required_if:publico,setor|exists:setores,id',
    ]);

    $destinatarios = match ($data['publico']) {
        'usuario' => User::where('id', $data['usuario_id'])->get(),
        'setor'   => \App\Models\Setor::find($data['setor_id'])->membros,
        'lideres' => User::whereHas('setoresLiderados')->get(),
        'todos'   => User::where('active', true)->get(),
    };

    $count = $destinatarios->count();

    if ($count > 0) {
        \Illuminate\Support\Facades\Notification::send(
            $destinatarios,
            new \App\Notifications\ManualNotification(
                titulo:      $data['titulo'],
                mensagem:    $data['mensagem'],
                autorUserId: $request->user()->id,
                meta:        ['publico' => $data['publico']]
                    + (isset($data['usuario_id']) ? ['usuario_id' => $data['usuario_id']] : [])
                    + (isset($data['setor_id'])   ? ['setor_id'   => $data['setor_id']]   : []),
            )
        );

        activity()
            ->causedBy($request->user())
            ->withProperties([
                'publico'             => $data['publico'],
                'usuario_id'          => $data['usuario_id'] ?? null,
                'setor_id'            => $data['setor_id'] ?? null,
                'destinatarios_count' => $count,
                'titulo'              => $data['titulo'],
            ])
            ->log('Notificação manual enviada');
    }

    return back()->with('success', "Notificação enviada para {$count} destinatário(s).");
}
```

**Rotas (dentro do grupo `auth + verified` em `routes/web.php`):**
```php
Route::middleware('permission:notificacoes.criar')->group(function () {
    Route::get('/notificacoes/nova',  [NotificacaoController::class, 'nova'])->name('notificacoes.nova');
    Route::post('/notificacoes/nova', [NotificacaoController::class, 'criar'])->name('notificacoes.criar');
});
```
**Confirmar o alias correto do middleware em `bootstrap/app.php`**. Se for `permission` (Spatie-style) ou outro nome, ajustar. Se NÃO existir alias, criar com escopo mínimo: `Route::get/post` + `function (Request $request, Closure $next) { abort_unless($request->user()?->hasPermission('notificacoes.criar'), 403); return $next($request); }` inline ou via método controller `__construct` com `$this->middleware`.

**Sidebar item (em `resources/js/Layouts/AppLayout.jsx`):**

Adicionar import de `Send` da `lucide-react`. Adicionar item no array `mainNav` (ou onde os itens vivem) — sugestão: logo após "Setores" ou junto aos demais utilitários, com `permission: 'notificacoes.criar'`:
```jsx
{ label: 'Enviar notificação', routeName: 'notificacoes.nova', page: 'Notificacoes/Nova', icon: Send, permission: 'notificacoes.criar' },
```

**`NotificationsCleanup` (Artisan command):**
```php
namespace App\Console\Commands;

use Illuminate\Console\Command;
use Illuminate\Notifications\DatabaseNotification;
use Illuminate\Support\Facades\Log;

class NotificationsCleanup extends Command
{
    protected $signature = 'notifications:cleanup';
    protected $description = 'Remove notificações lidas com mais de 30 dias (POLL-04).';

    public function handle(): int
    {
        $cutoff = now()->subDays(30);
        $count = DatabaseNotification::whereNotNull('read_at')
            ->where('read_at', '<', $cutoff)
            ->delete();
        $this->info("Removidas {$count} notificações lidas com mais de 30 dias.");
        Log::info("[NotificationsCleanup] removed={$count}");
        return self::SUCCESS;
    }
}
```

**`routes/console.php` (adicionar ao final):**
```php
// Cleanup diário de notificações lidas com >30 dias (POLL-04)
Schedule::command('notifications:cleanup')
    ->dailyAt('04:00')
    ->name('notifications-cleanup')
    ->withoutOverlapping();
```

**`Notificacoes/Nova.jsx` (estrutura):**
```jsx
import { Head, useForm } from '@inertiajs/react';
import { Send } from 'lucide-react';
import AppLayout from '@/Layouts/AppLayout';

export default function Nova({ setores }) {
    const { data, setData, post, processing, errors } = useForm({
        titulo: '', mensagem: '', publico: 'todos', usuario_id: '', setor_id: '',
    });

    const submit = (e) => {
        e.preventDefault();
        post(route('notificacoes.criar'));
    };

    return (
        <AppLayout title="Enviar notificação">
            <Head title="Enviar notificação" />
            <form onSubmit={submit} className="max-w-xl mx-auto space-y-4 p-4">
                <h1 className="text-xl font-bold text-white">Enviar notificação</h1>

                <div>
                    <label className="text-xs text-white/60 block mb-1">Título</label>
                    <input
                        type="text" maxLength={100} value={data.titulo}
                        onChange={e => setData('titulo', e.target.value)}
                        className="w-full bg-ecf-card border border-white/[0.08] rounded px-3 py-2 text-sm text-white"
                    />
                    <div className="text-[10px] text-white/40 mt-1">{data.titulo.length}/100</div>
                    {errors.titulo && <div className="text-xs text-red-400 mt-1">{errors.titulo}</div>}
                </div>

                <div>
                    <label className="text-xs text-white/60 block mb-1">Mensagem</label>
                    <textarea
                        maxLength={1000} rows={5} value={data.mensagem}
                        onChange={e => setData('mensagem', e.target.value)}
                        className="w-full bg-ecf-card border border-white/[0.08] rounded px-3 py-2 text-sm text-white"
                    />
                    <div className="text-[10px] text-white/40 mt-1">{data.mensagem.length}/1000</div>
                    {errors.mensagem && <div className="text-xs text-red-400 mt-1">{errors.mensagem}</div>}
                </div>

                <div>
                    <label className="text-xs text-white/60 block mb-1">Público</label>
                    <select
                        value={data.publico}
                        onChange={e => setData('publico', e.target.value)}
                        className="w-full bg-ecf-card border border-white/[0.08] rounded px-3 py-2 text-sm text-white"
                    >
                        <option value="todos">Todos os usuários ativos</option>
                        <option value="lideres">Todos os líderes</option>
                        <option value="setor">Um setor específico</option>
                        <option value="usuario">Um usuário específico</option>
                    </select>
                </div>

                {data.publico === 'setor' && (
                    <div>
                        <label className="text-xs text-white/60 block mb-1">Setor</label>
                        <select
                            value={data.setor_id}
                            onChange={e => setData('setor_id', e.target.value)}
                            className="w-full bg-ecf-card border border-white/[0.08] rounded px-3 py-2 text-sm text-white"
                        >
                            <option value="">— Escolha o setor —</option>
                            {setores.map(s => <option key={s.id} value={s.id}>{s.nome}</option>)}
                        </select>
                        {errors.setor_id && <div className="text-xs text-red-400 mt-1">{errors.setor_id}</div>}
                    </div>
                )}

                {data.publico === 'usuario' && (
                    <div>
                        <label className="text-xs text-white/60 block mb-1">ID do usuário</label>
                        <input
                            type="number" value={data.usuario_id}
                            onChange={e => setData('usuario_id', e.target.value)}
                            className="w-full bg-ecf-card border border-white/[0.08] rounded px-3 py-2 text-sm text-white"
                            placeholder="ID numérico (MVP — Phase futura: busca por nome)"
                        />
                        {errors.usuario_id && <div className="text-xs text-red-400 mt-1">{errors.usuario_id}</div>}
                    </div>
                )}

                <button
                    type="submit" disabled={processing}
                    className="bg-ecf-yellow text-ecf-bg font-bold px-4 py-2 rounded text-sm hover:opacity-90 disabled:opacity-50 flex items-center gap-2"
                >
                    <Send size={14} /> Enviar
                </button>
            </form>
        </AppLayout>
    );
}
```

**Atenção sobre middleware permission:** Antes de adicionar `permission:notificacoes.criar`, confirme o alias em `bootstrap/app.php`. Procure por `$middleware->alias([...])` ou similar. Se o alias for `EnsureUserHasPermission`, use o nome registrado. O pattern já está usado em outras rotas (ex: `permission:lideranca.dashboard_setor` na linha ~279 de `routes/web.php`), então o alias provavelmente já existe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar `ManualNotification` + Artisan command `notifications:cleanup` + scheduler entry</name>
  <files>
    app/Notifications/ManualNotification.php,
    app/Console/Commands/NotificationsCleanup.php,
    routes/console.php
  </files>
  <read_first>
    - app/Notifications/MetaAtribuidaNotification.php (template — Phase 11)
    - routes/console.php (ver schedule pattern)
    - app/Console/Commands/ (algum command existente como template)
  </read_first>
  <behavior>Criar 2 arquivos novos + 1 entry de scheduler conforme `<interfaces>`. Comentários em pt-BR.</behavior>
  <action>3 escritas.</action>
  <verify>
    <automated>/c/xampp/php/php -l app/Notifications/ManualNotification.php && /c/xampp/php/php -l app/Console/Commands/NotificationsCleanup.php && /c/xampp/php/php artisan list 2>&1 | grep notifications:cleanup</automated>
  </verify>
  <acceptance_criteria>
    - `ManualNotification` extends `BaseNotification` e usa `Categoria::MANUAL`.
    - `notifications:cleanup` aparece em `artisan list`.
    - `routes/console.php` contém `notifications:cleanup` e `dailyAt('04:00')`.
  </acceptance_criteria>
  <done>Backend de notificação manual + cleanup pronto.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Estender `NotificacaoController` com `nova()` e `criar()` + adicionar rotas com gating</name>
  <files>app/Http/Controllers/NotificacaoController.php, routes/web.php</files>
  <read_first>
    - app/Http/Controllers/NotificacaoController.php (estado atual)
    - routes/web.php (procurar `permission:` no grupo auth — ver alias correto)
    - bootstrap/app.php (confirmar alias do middleware permission)
  </read_first>
  <behavior>
    Adicionar 2 métodos no controller conforme `<interfaces>`. Importar `Setor` e `User` no topo. Importar `Notification` facade.

    Em `routes/web.php`, no mesmo grupo `auth + verified` onde já estão as outras 5 rotas notificacoes.*, adicionar sub-grupo com middleware `permission:notificacoes.criar`:
    ```php
    Route::middleware('permission:notificacoes.criar')->group(function () {
        Route::get('/notificacoes/nova',  [NotificacaoController::class, 'nova'])->name('notificacoes.nova');
        Route::post('/notificacoes/nova', [NotificacaoController::class, 'criar'])->name('notificacoes.criar');
    });
    ```

    Verificar o alias em `bootstrap/app.php`: se for `permission` (linha como `'permission' => EnsureUserHasPermission::class`), manter. Senão, ajustar para o nome correto. Se NENHUM alias estiver registrado, usar a forma FQCN: `Route::middleware(\App\Http\Middleware\EnsureUserHasPermission::class . ':notificacoes.criar')->group(...)`.

    Activity log usa helper global `activity()` do `spatie/laravel-activitylog` — confirme está disponível (PROJECT usa essa lib).
  </behavior>
  <action>2 edits.</action>
  <verify>
    <automated>/c/xampp/php/php artisan route:list --name=notificacoes 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `route:list --name=notificacoes` mostra 7 rotas (5 antigas + 2 novas).
    - Controller contém `public function nova` E `public function criar`.
    - `criar()` chama `activity()->causedBy` E `Notification::send`.
  </acceptance_criteria>
  <done>Endpoints prontos.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Adicionar item de sidebar "Enviar notificação" no `AppLayout.jsx`</name>
  <files>resources/js/Layouts/AppLayout.jsx</files>
  <read_first>resources/js/Layouts/AppLayout.jsx — imports lucide + array do menu</read_first>
  <behavior>
    1. Adicionar `Send` ao import block de `lucide-react`.
    2. Inserir o item no array de navegação (perto dos outros utilitários, antes do "Meu Setor" ou após "Setores" — escolher local coerente):
    ```jsx
    { label: 'Enviar notificação', routeName: 'notificacoes.nova', page: 'Notificacoes/Nova', icon: Send, permission: 'notificacoes.criar' },
    ```
    O AppLayout já filtra por permission via `auth.permissions` shared prop — usuários sem `notificacoes.criar` NÃO veem o item (ENVIO-01 + ENVIO-05).
  </behavior>
  <action>1 edit.</action>
  <verify>
    <automated>grep "Enviar notificação\|notificacoes.criar\|notificacoes.nova" resources/js/Layouts/AppLayout.jsx | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo contém o item com `routeName: 'notificacoes.nova'` E `permission: 'notificacoes.criar'`.
    - Import `Send` adicionado.
  </acceptance_criteria>
  <done>Menu pronto.</done>
</task>

<task type="auto">
  <name>Tarefa 4: Criar página `Notificacoes/Nova.jsx`</name>
  <files>resources/js/Pages/Notificacoes/Nova.jsx</files>
  <read_first>
    - resources/js/Pages/Notificacoes/Index.jsx (estilo da Phase 10)
    - resources/js/Pages/Profile/* (algum form `useForm` já existente como template)
  </read_first>
  <behavior>Criar arquivo conforme `<interfaces>`. Comentário no topo em pt-BR.</behavior>
  <action>1 escrita nova.</action>
  <verify>
    <automated>test -f resources/js/Pages/Notificacoes/Nova.jsx && grep "useForm\|Enviar" resources/js/Pages/Nova.jsx 2>/dev/null; grep -c "useForm\|setData" resources/js/Pages/Notificacoes/Nova.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe.
    - Usa `useForm` e `setData`.
    - Tem `maxLength={100}` no título e `maxLength={1000}` na mensagem.
    - Tem `<select>` com 4 opções de público.
    - Submit chama `post(route('notificacoes.criar'))`.
  </acceptance_criteria>
  <done>UI pronta.</done>
</task>

<task type="auto">
  <name>Tarefa 5: Criar `Phase12ManualTest` com 9 testes</name>
  <files>tests/Feature/Notifications/Phase12ManualTest.php</files>
  <read_first>
    - tests/Feature/Notifications/Phase9BackendTest.php (template)
    - tests/Feature/Notifications/Phase11AutoTest.php (helpers de setor/membros)
  </read_first>
  <behavior>
    9 testes:

    1. `test_perm_04_catalog_inclui_notificacoes_criar`: assert `Permissions::catalog()['Notificações']` existe e contém entry com `'key' => 'notificacoes.criar'` (PERM-04 — sanity check do que Phase 8 entregou).
    2. `test_envio_05_user_sem_permissao_recebe_403_em_get_nova`: criar user sem permission, `actingAs($user)->get(route('notificacoes.nova'))` → assertStatus(403).
    3. `test_envio_05_user_sem_permissao_recebe_403_em_post_criar`: análogo com POST.
    4. `test_envio_01_user_com_permissao_acessa_nova_com_setores`: criar setor + user via setor com permission (atribuir `notificacoes.criar` ao setor, atachar user ao setor), `actingAs($user)->get(route('notificacoes.nova'))` → 200 + Inertia component `Notificacoes/Nova` + prop `setores` contém o setor.
    5. `test_envio_02_publico_setor_envia_para_membros_apenas`: setor S com 3 membros M1/M2/M3 + outro user U fora. Autor A com `notificacoes.criar`. POST `criar` com `publico=setor, setor_id=S->id`. Assertar 3 notifications criadas, nenhuma para U.
    6. `test_envio_02_publico_lideres_envia_para_lideres_apenas`: 2 lideres L1/L2 (cada um liderado de algum setor) + 3 não-lideres N1/N2/N3. POST `criar publico=lideres`. Assertar 2 notifications, todas para lideres.
    7. `test_envio_02_publico_todos_envia_para_users_ativos`: 3 active + 1 inactive (active=false). POST `criar publico=todos`. Assertar 3 notifications (excluindo inactive). NOTE: o autor é um dos active, ele recebe a notif também (não há exclusão self) — assertar 4 se incluir autor; ou 3 se excluir. Decisão: NÃO excluir autor — POST envia para todos active inclusive autor. Assertar 4.
    8. `test_envio_03_validation_rejeita_titulo_e_mensagem_vazios_ou_excedendo_limite`: 4 cenários combinados (titulo vazio, titulo >100, mensagem vazia, mensagem >1000) → cada um retorna `Illuminate\Validation\ValidationException` (assertSessionHasErrors).
    9. `test_envio_04_e_poll_05_flash_count_e_activity_log_registrado`: setor com 2 membros, POST `criar publico=setor`. Assertar: response redireciona com flash `success` contendo "2 destinatário(s)"; `activity_log` table tem 1 entry com `description='Notificação manual enviada'`, `causer_id = autor->id`, `properties->destinatarios_count = 2`, `properties->publico = 'setor'`.

    Bonus (10º teste, separado): `test_poll_04_cleanup_remove_lidas_antigas_e_preserva_unread_e_recentes`: criar 3 notifications: A (read 40 dias atrás), B (read 5 dias atrás), C (unread). Rodar `Artisan::call('notifications:cleanup')`. Assertar: A removida, B e C preservadas, contagem total = 2.

    Helpers: para atribuir permission ao user, criar setor → adicionar `notificacoes.criar` em `setor.permissoes()->create(['permission_key' => 'notificacoes.criar'])` → atachar user ao setor via `$setor->membros()->attach($user->id, ['cargo_id' => null])`. Ou usar role=admin para short-circuit (mais simples para alguns testes).

    Não usar `Notification::fake()`. Comentários em pt-BR.
  </behavior>
  <action>Criar arquivo de teste.</action>
  <verify>
    <automated>/c/xampp/php/php -l tests/Feature/Notifications/Phase12ManualTest.php</automated>
  </verify>
  <acceptance_criteria>
    - 9 testes (ou 10 com bonus cleanup) declarados.
    - Cada teste tem `: void` público.
    - PHP lint passa.
  </acceptance_criteria>
  <done>Suíte criada — 9-10 testes prontos para validação.</done>
</task>

<task type="auto">
  <name>Tarefa 6: Build Vite + rodar suítes Phases 8-12 + smoke route:list</name>
  <files>(verificação)</files>
  <read_first></read_first>
  <behavior>
    1. `npm run build` — gera Vite manifest com `Nova.jsx`.
    2. `php artisan test --filter='Phase8FoundationTest|Phase9BackendTest|Phase10UiTest|Phase11AutoTest|Phase12ManualTest'` — esperar 32-33 testes GREEN.
    3. `php artisan route:list --name=notificacoes` — esperar 7 rotas.
    4. `php artisan list` mostra `notifications:cleanup`.
    5. Smoke `php artisan schedule:list` mostra a entrada `notifications-cleanup` rodando às 04:00.

    Se algo falhar, voltar à tarefa que provê o código.
  </behavior>
  <action>Rodar verificações.</action>
  <verify>
    <automated>npm run build && /c/xampp/php/php artisan test --filter='Phase8FoundationTest|Phase9BackendTest|Phase10UiTest|Phase11AutoTest|Phase12ManualTest'</automated>
  </verify>
  <acceptance_criteria>
    - Todos os testes GREEN.
    - 7 rotas notificacoes.*.
    - schedule:list contém `notifications-cleanup`.
  </acceptance_criteria>
  <done>Milestone v3.0 completa.</done>
</task>

</tasks>

<success_criteria>
- ENVIO-01..05 ✓ PERM-04 ✓ POLL-04 ✓ POLL-05 ✓ (8 requirements)
- Phase12ManualTest: 9-10/9-10 GREEN
- Regression Phases 8-11: 23/23 GREEN
- Total Notifications: ~32-33 testes GREEN
- 7 rotas `notificacoes.*` (5 antigas + nova + criar)
- Scheduler entry `notifications-cleanup` ativo
- Milestone v3.0 (Sistema de Notificações) — encerrada
</success_criteria>

<output>
Criar `.planning/phases/12-criacao-manual-permissao-ui-cleanup/12-01-SUMMARY.md` ao final.
</output>
