---
phase: 130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-
plan: 06
type: execute
wave: 4
depends_on: ["130-03", "130-05"]
files_modified:
  - app/Notifications/VarreduraParadaNotification.php
  - app/Console/Commands/ClicksignVerificarVarredura.php
  - routes/console.php
  - tests/Feature/Phase130/AutoMonitoramentoCarimboTest.php
autonomous: true
requirements: [REDE-02, REDE-04]

must_haves:
  truths:
    - "A própria rede de segurança avisa quando ela mesma parou de rodar (D-09)"
    - "Os três comandos da fase rodam automaticamente, em horários diferentes e sem sobreposição (D-06)"
    - "A limitação da checagem de ausência está declarada por escrito, não escondida"
  artifacts:
    - path: "app/Console/Commands/ClicksignVerificarVarredura.php"
      provides: "Checagem de ausência: acusa quando o carimbo da varredura está velho ou não existe"
      contains: "clicksign_reconciliacao_status"
    - path: "app/Notifications/VarreduraParadaNotification.php"
      provides: "Aviso in-app de que a varredura de reconciliação não rodou"
      contains: "extends BaseNotification"
    - path: "routes/console.php"
      provides: "Agendamento diário dos 3 comandos da Fase 130"
      contains: "clicksign:reconciliar"
  key_links:
    - from: "app/Console/Commands/ClicksignVerificarVarredura.php"
      to: "Configuracao::get('clicksign_reconciliacao_status')"
      via: "leitura do carimbo gravado pelo plano 130-03"
      pattern: "clicksign_reconciliacao_status"
    - from: "routes/console.php"
      to: "clicksign:reconciliar / clicksign:alertar-presos / clicksign:verificar-varredura"
      via: "Schedule::command(...)->dailyAt(...)->withoutOverlapping()"
      pattern: "Schedule::command\\('clicksign:"
---

<objective>
Fechar o círculo: a rede de segurança passa a vigiar a si mesma, e os três comandos da fase entram
no agendador.

Propósito: D-09 — sem isto, a própria rede de segurança pode cair no silêncio que ela existe para
eliminar. E sem o agendamento, nada do que os planos 130-03 e 130-05 construíram roda sozinho.
Saída: `VarreduraParadaNotification`, comando `clicksign:verificar-varredura`, 3 entradas em
`routes/console.php` e 1 teste.

**Limitação declarada, não escondida (D-09):** esta checagem cobre "o comando `clicksign:reconciliar`
especificamente parou de rodar" — o cenário provável. Ela **NÃO** cobre o crontab do sistema
operacional morrendo por inteiro, porque nesse caso o próprio comando de verificação também para.
Isso é limitação ESTRUTURAL de qualquer verificação agendada, não um detalhe de implementação, e
deve aparecer por escrito no código e no `routes/console.php` — nunca ser omitido. Fechar esse
cenário exigiria mover o check para fora do cron (ex.: `HandleInertiaRequests`), o que está fora do
escopo desta fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-CONTEXT.md
@.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-PATTERNS.md
@.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-VALIDATION.md

<interfaces>
<!-- Contratos extraídos do código real e dos planos anteriores desta fase. -->

De `app/Models/Configuracao.php`:
- `get(string $chave, $default = null): mixed` / `set(string $chave, $valor): void` (`valor` é `text`)

Carimbo gravado pelo plano 130-03 na chave `clicksign_reconciliacao_status` (JSON):
`{ executado_em: ISO8601, vistos: int, corrigidos: int, pdfs_redisparados: int, erro: string|null }`

De `app/Notifications/BaseNotification.php`:
`__construct(string $titulo, string $mensagem, Categoria $categoria, ?int $autorUserId = null,
?string $url = null, array $meta = [])`; `via()` → `['database']`; `toArray()` com 6 chaves.
NÃO sobrescrever `via()` nem `toArray()`.

De `app/Support/AudienciaRedeSeguranca.php` (plano 130-02):
- `public static function adminsEComercial(): Collection`

Padrão de agendamento — `routes/console.php` linhas 15-38:
```
Schedule::command('adman:sync')
    ->dailyAt('11:00')
    ->name('adman-sync-d1')
    ->withoutOverlapping();
```
Todo bloco existente tem comentário acima JUSTIFICANDO o horário escolhido (dependência de outro
job, fuso BRT). Seguir a mesma disciplina.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Checagem de ausência da varredura (D-09)</name>
  <files>
    app/Notifications/VarreduraParadaNotification.php,
    app/Console/Commands/ClicksignVerificarVarredura.php,
    tests/Feature/Phase130/AutoMonitoramentoCarimboTest.php
  </files>
  <read_first>
    - `app/Console/Commands/ClicksignReconciliar.php` — o formato EXATO do carimbo gravado (plano 130-03); a leitura precisa casar com a escrita
    - `app/Notifications/ContratoPresoNotification.php` — o molde de subclasse concreta desta fase (plano 130-05); esta notificação é irmã dela
    - `app/Notifications/BaseNotification.php` — construtor e as 6 chaves canônicas
    - `app/Support/AudienciaRedeSeguranca.php` — `adminsEComercial()` (plano 130-02)
    - `app/Http/Controllers/Api/HubspotWebhookController.php` linhas 976-1012 — o padrão de audiência vazia → `Log::warning`
    - `.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-RESEARCH.md` §"Pergunta 2" — a tabela que separa o cenário A (coberto) do cenário B (não coberto)
  </read_first>
  <action>
Criar `app/Notifications/VarreduraParadaNotification.php` estendendo `BaseNotification`, sem
sobrescrever `via()`/`toArray()`:
- Construtor recebe `?string $executadoEm` (ISO da última execução conhecida, ou `null`) e
  `?string $erro` (a mensagem gravada no carimbo, se houver).
- `titulo`: 'A verificação automática de contratos não rodou'.
- `mensagem` em linguagem simples, cobrindo os três casos e dizendo o que fazer:
  nunca rodou / rodou pela última vez em {data} / rodou mas terminou com erro. Sem jargão
  ("cron", "scheduler" e "job" só podem aparecer se explicados na própria frase).
- `categoria: Categoria::MANUAL`, `autorUserId: null`, `meta` com `executado_em`, `erro`, e
  `fonte => 'rede_seguranca'`. Sem `url` obrigatória (a ação é de infraestrutura, não de tela).

Criar `app/Console/Commands/ClicksignVerificarVarredura.php` com
`$signature = 'clicksign:verificar-varredura'` e `$description` em pt-BR ("Acusa quando a
verificação automática de contratos parou de rodar").

`handle(): int`:
1. Constantes públicas `CHAVE_LIMIAR_HORAS = 'rede_varredura_limiar_horas'` e
   `DEFAULT_LIMIAR_HORAS = 26`, com comentário explicando que 26h dá 2h de folga sobre o ciclo
   diário de 24h.
2. `$statusJson = Configuracao::get('clicksign_reconciliacao_status');`
   `$status = $statusJson ? json_decode($statusJson, true) : null;`
   `$executadoEm = isset($status['executado_em']) ? Carbon::parse($status['executado_em']) : null;`
   Tratar JSON inválido como "sem carimbo" (não deixar `json_decode` derrubar o comando).
3. Dispara alerta quando: não há carimbo, OU `executadoEm->lt(now()->subHours($limiar))`, OU
   `filled($status['erro'])`.
4. Ao disparar: `AudienciaRedeSeguranca::adminsEComercial()`; se vazia,
   `Log::channel('ecf-webhooks')->warning(...)` e sair; senão `Notification::send($audiencia,
   new VarreduraParadaNotification(...))` mais `Log::channel('ecf-webhooks')->info(...)` com
   `destinatarios_count`.
5. Cooldown simples para não repetir a cada execução: gravar
   `Configuracao::set('rede_varredura_ultimo_alerta_em', now()->toIso8601String())` após enviar, e
   pular o envio quando esse carimbo for mais recente que o limiar. Comentário em pt-BR: o comando
   roda 1x/dia, mas o cooldown protege contra execução manual repetida.
6. Sem alerta → `$this->info(...)` e `return self::SUCCESS`.

**Bloco de comentário obrigatório no topo do comando**, em pt-BR, com a limitação da D-09 escrita
literalmente: esta checagem detecta o caso "o comando `clicksign:reconciliar` parou" (bug, remoção
acidental do agendamento, exceção antes de gravar o carimbo); ela NÃO detecta o caso "o agendador do
sistema operacional parou de disparar `schedule:run`", porque nesse cenário este próprio comando
também deixa de rodar. Registrar que monitorar o agendador do SO é responsabilidade de
infraestrutura, fora do escopo desta fase, e que fechar esse cenário exigiria mover o check para
fora do cron.

Criar `tests/Feature/Phase130/AutoMonitoramentoCarimboTest.php` (`Tests\Feature\Phase130`,
`RefreshDatabase`, `Notification::fake()`), sempre reconsultando o banco:
1. `clicksign:reconciliar` grava o carimbo: rodar o comando e ler
   `json_decode(Configuracao::get('clicksign_reconciliacao_status'), true)` — tem `executado_em`,
   `vistos`, `corrigidos`, `pdfs_redisparados` e `erro === null`.
2. Sem nenhum carimbo no banco → `clicksign:verificar-varredura` envia
   `VarreduraParadaNotification` para o admin ativo.
3. Carimbo com `executado_em` de 2h atrás → `Notification::assertNothingSent()`.
4. Carimbo com `executado_em` de 30h atrás → alerta enviado.
5. Carimbo recente mas com `erro` preenchido → alerta enviado (o comando rodou e quebrou).
6. Carimbo com JSON corrompido → alerta enviado e o comando sai com código 0 (não explode).
7. Duas execuções seguidas do comando de verificação → apenas UM envio (cooldown), conferido por
   `Notification::assertSentTimes(VarreduraParadaNotification::class, 1)`.
8. Audiência vazia → `assertNothingSent()` e nenhuma exceção.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=AutoMonitoramentoCarimboTest</automated>
  </verify>
  <acceptance_criteria>
    - `app/Console/Commands/ClicksignVerificarVarredura.php` declara `$signature = 'clicksign:verificar-varredura'`
    - O arquivo lê `Configuracao::get('clicksign_reconciliacao_status')` e trata JSON inválido sem lançar exceção
    - O comentário de topo do comando contém, por escrito, que a checagem NÃO cobre o agendador do sistema operacional parado
    - `VarreduraParadaNotification extends BaseNotification` e não redefine `via(` nem `toArray(`
    - O teste cobre os 4 gatilhos (sem carimbo, carimbo velho, carimbo com erro, JSON corrompido) e o cooldown
    - `C:\xampp\php\php.exe artisan clicksign:verificar-varredura` sai com código 0 num banco vazio
    - `C:\xampp\php\php.exe artisan test --filter=AutoMonitoramentoCarimboTest` sai com código 0
  </acceptance_criteria>
  <done>Se a varredura parar de rodar ou quebrar, a equipe é avisada pelo sino — e a limitação real dessa garantia está escrita no código.</done>
</task>

<task type="auto">
  <name>Task 2: Agendamento diário dos três comandos (D-06)</name>
  <files>routes/console.php</files>
  <read_first>
    - `routes/console.php` — ler INTEIRO: o arquivo que será editado, os horários já ocupados (11:00 `adman:sync`, 11:40/06:00 `adman:warm-diff`, 11:45 `goals:calculate`, 03:00 `grants:sync-ecf`) e o estilo de comentário que justifica cada horário
    - `app/Console/Commands/ClicksignReconciliar.php` (plano 130-03), `ClicksignAlertarPresos.php` (plano 130-05) e `ClicksignVerificarVarredura.php` (Task 1) — os três `$signature` exatos
    - `.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-PATTERNS.md` §Agendamento
  </read_first>
  <action>
Adicionar em `routes/console.php`, num bloco próprio com cabeçalho de seção no estilo do arquivo
("Fase 130 — Rede de segurança Clicksign"), três entradas seguindo o padrão
`Schedule::command(...)->dailyAt(...)->name(...)->withoutOverlapping();`:

- `clicksign:reconciliar` às `07:00`, `name('clicksign-reconciliar')`.
- `clicksign:alertar-presos` às `07:30`, `name('clicksign-alertar-presos')`.
- `clicksign:verificar-varredura` às `08:00`, `name('clicksign-verificar-varredura')`.

Comentário em pt-BR ACIMA do bloco justificando as escolhas (é a disciplina do arquivo):
1. Por que cedo (07:00): a varredura enfileira jobs que consomem o bucket global de 3 chamadas/min
   da Clicksign; rodar antes do pico de sync do Adman (11:00) evita disputar janela de fila.
2. Por que a checagem de ausência (08:00) roda em horário DIFERENTE e DEPOIS da varredura, com 1h
   de folga: ela existe para ler o carimbo que a varredura acabou de gravar.
3. Por que `withoutOverlapping()` nos três: a varredura pode enfileirar dezenas de jobs e não pode
   se sobrepor à execução anterior.
4. **A limitação da D-09, repetida aqui**: este trio detecta "um destes comandos parou"; nenhum
   deles detecta "o `schedule:run` do sistema operacional parou", porque todos dependem dele —
   monitorar o cron do SO é responsabilidade de infraestrutura.
5. Lembrete de que a reconciliação é REDE DE SEGURANÇA e nunca o mecanismo principal (o webhook é),
   conforme `REQUIREMENTS-v22.md` §"Out of Scope" — não aumentar a frequência para transformá-la em
   polling.

Não alterar nenhuma entrada existente do arquivo.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan schedule:list</automated>
  </verify>
  <acceptance_criteria>
    - `C:\xampp\php\php.exe artisan schedule:list` lista `clicksign:reconciliar`, `clicksign:alertar-presos` e `clicksign:verificar-varredura`
    - Os três aparecem com horários distintos (07:00, 07:30, 08:00) e a verificação vem DEPOIS da reconciliação
    - `routes/console.php` contém `->withoutOverlapping()` nas três entradas novas
    - O comentário do bloco cita explicitamente a limitação "não detecta o agendador do sistema operacional parado"
    - Nenhuma entrada pré-existente de `routes/console.php` foi alterada (`git diff` mostra apenas linhas adicionadas)
    - `C:\xampp\php\php.exe artisan test --filter=Phase130` continua verde
  </acceptance_criteria>
  <done>Os três comandos rodam sozinhos todo dia, em horários que não brigam entre si nem com o resto do agendamento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Configuracao` (carimbo) → comando de verificação | Valor de texto livre lido e parseado como JSON |
| agendador do SO → Laravel | Gatilho externo do qual TODOS os comandos dependem — e que nenhum deles consegue verificar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-06-01 | Denial of Service | `json_decode` de carimbo corrompido derruba o comando de verificação, silenciando o vigia | mitigate | Tratar retorno `null` de `json_decode` como "sem carimbo" e ALERTAR; teste dedicado com JSON corrompido saindo com código 0 |
| T-130-06-02 | Information Disclosure | Mensagem de exceção do carimbo (`erro`) exibida na notificação contendo PII | mitigate | O carimbo já é gravado com mensagem truncada em 500 chars e sem e-mails pelo plano 130-03; a notificação apenas repassa esse texto e não acrescenta dado novo |
| T-130-06-03 | Denial of Service | Alerta de varredura parada repetindo a cada execução manual e virando ruído | mitigate | Cooldown via `rede_varredura_ultimo_alerta_em`; teste assere `assertSentTimes(..., 1)` em duas execuções seguidas |
| T-130-06-04 | Repudiation | Cenário B (agendador do SO morto) ficando descoberto sem ninguém saber que ficou | mitigate | Limitação escrita literalmente no comentário do comando E no bloco de `routes/console.php`, e já registrada em `130-VALIDATION.md` §"Limitação estrutural declarada" — declarada, não escondida |
| T-130-06-05 | Tampering | Aumento da frequência da varredura transformando rede de segurança em polling primário | mitigate | Comentário no bloco de agendamento cita `REQUIREMENTS-v22.md` §"Out of Scope"; horário diário fixo (D-06) |
| T-130-06-SC | Tampering | Instalação de pacote npm/composer | accept | Não aplicável — nenhum pacote novo nesta fase |
</threat_model>

<verification>
1. `C:\xampp\php\php.exe artisan test --filter=Phase130` verde.
2. `C:\xampp\php\php.exe artisan schedule:list` mostra os três comandos nos horários certos.
3. `C:\xampp\php\php.exe artisan clicksign:verificar-varredura` sai 0 e, em banco sem carimbo,
   a reconsulta a `DB::table('notifications')->count()` mostra o alerta gravado.
4. `C:\xampp\php\php.exe artisan test` (suíte completa) verde.
</verification>

<success_criteria>
- A ausência/erro/atraso da varredura vira alerta in-app para a mesma audiência da D-02
- Os três comandos da fase estão agendados, em horários distintos e sem sobreposição
- A limitação estrutural da D-09 está escrita no código e no agendamento
</success_criteria>

<output>
Criar `.planning/phases/130-rede-de-seguran-a-reconcilia-o-alerta-e-libera-o-manual-v22-/130-06-SUMMARY.md` ao concluir.
</output>
