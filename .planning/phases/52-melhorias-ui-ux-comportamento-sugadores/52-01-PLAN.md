---
phase: 52-melhorias-ui-ux-comportamento-sugadores
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-52-01
files_modified:
  - app/Policies/SugadorPolicy.php
  - app/Http/Controllers/SugadorController.php
  - routes/web.php
  - tests/Feature/Phase52/SugadorPolicyManageAnalistaTest.php
  - tests/Feature/Phase52/SugadorMlbsHintEndpointTest.php
  - tests/Feature/Phase52/SugadorBulkCopyMlbsTest.php
must_haves:
  truths:
    - "Usuário com `publication_role='analista'` (permissão CORE_SUGADORES) recebe true em `SugadorPolicy::manage()` e consegue abrir /sugadores/configs/{company} da sua carteira"
    - "Admin, gestor e lider continuam com manage=true (regressão evitada)"
    - "GET /sugadores/{sugador}/mlbs-hint retorna 200 JSON `{mlbs: string[], total: int}` lendo AdgroupMlbMapRepository::getMlbsForAdgroup — funciona para empresas Adman-only, ML-only e híbridas"
    - "GET mlbs-hint valida view por Gate::authorize('view', $sugador) — usuário fora da carteira recebe 403"
    - "GET mlbs-hint retorna `{mlbs: [], reason}` com 200 quando sugador.tipo != adgroup (mesmo shape de sucesso, sem 422)"
    - "POST /sugadores/bulk-copy-mlbs valida sugador_ids array required min:1 max:500, autoriza view por item, agrega MLBs via AdgroupMlbMapRepository (mesma fonte), dedupla e retorna `{mlbs: string[], total: int, sugadores_processados: int}`"
    - "Cada RED (teste failing) tem commit separado do GREEN (implementação)"
  artifacts:
    - path: "app/Policies/SugadorPolicy.php"
      provides: "manage() relaxado — analista incluso via CORE_SUGADORES + docblock §9 atualizado"
      contains: "return \\$user->isAdmin() || \\$user->hasPermission(Permissions::CORE_SUGADORES)"
    - path: "app/Http/Controllers/SugadorController.php"
      provides: "Dois métodos públicos novos: mlbsHint(Sugador \\$sugador) e bulkCopyMlbs(Request \\$request)"
      contains: "Gate::authorize('view', ...); \\$this->adgroupMlbMap->getMlbsForAdgroup(...)"
    - path: "routes/web.php"
      provides: "Duas rotas novas: sugadores.mlbs-hint (GET) e sugadores.bulk-copy-mlbs (POST)"
      contains: "Route::get('/sugadores/{sugador}/mlbs-hint', ...); Route::post('/sugadores/bulk-copy-mlbs', ...)"
    - path: "tests/Feature/Phase52/SugadorPolicyManageAnalistaTest.php"
      provides: "Cobertura policy — analista carteira manage=true; publicador manage=false; regressão admin/gestor/lider"
    - path: "tests/Feature/Phase52/SugadorMlbsHintEndpointTest.php"
      provides: "Cobertura endpoint hint — happy path, empresa ML-only, sugador tipo campanha, autorização view"
    - path: "tests/Feature/Phase52/SugadorBulkCopyMlbsTest.php"
      provides: "Cobertura bulk — agregação + dedup, autorização por item, validação max:500"
  key_links:
    - from: "SugadorPolicy::manage"
      to: "Permissions::CORE_SUGADORES"
      via: "\\$user->hasPermission()"
      pattern: "SugadorPolicy::analyze linha 79 (mesma constante)"
    - from: "SugadorController::mlbsHint"
      to: "AdgroupMlbMapRepository::getMlbsForAdgroup"
      via: "constructor injection (\\$this->adgroupMlbMap — SugadorController:27)"
      pattern: "SugadorController::show linhas 275-277 (mesma chamada)"
    - from: "SugadorController::bulkCopyMlbs"
      to: "Sugador::whereIn + Gate::authorize('view') loop"
      via: "pattern do bulkMove (SugadorController.php:520-586)"
      pattern: "foreach (\\$sugadores as \\$s) Gate::authorize('view', \\$s)"
---

<objective>
Preparar o backend da Phase 52: liberar analista para configurar sugadores (A1), criar endpoint neutro `mlbs-hint` que resolve o 422 para empresas ML-only (A5) e novo endpoint `bulk-copy-mlbs` para ação em massa (A6).

Sem TDD estas três mudanças poderiam quebrar policies existentes ou regredir a autorização — Wave 1 é TDD obrigatório. Waves 2/3 (UI pura) seguem sem TDD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-CONTEXT.md
@.planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: SugadorPolicy::manage inclui analista (RED → GREEN)</name>
  <files>
    - app/Policies/SugadorPolicy.php
    - tests/Feature/Phase52/SugadorPolicyManageAnalistaTest.php
  </files>
  <read_first>
    - app/Policies/SugadorPolicy.php (linhas 56-71) — método `manage()` atual retorna true só para admin + hasGlobalView (gestor/lider)
    - app/Policies/SugadorPolicy.php (linhas 77-80) — `analyze()` já usa `Permissions::CORE_SUGADORES` (mesma constante que analista tem)
    - app/Policies/SugadorPolicy.php (linhas 9-19) — docblock §9 com tabela de acesso (atualizar coluna "Configura" para analista=✓)
    - app/Support/Permissions.php — constante `CORE_SUGADORES`
    - tests/Feature/Sugadores/AutoResolveTest.php (setUp inteiro) — pattern de teste Sugador com fábrica User + Company + attach
    - CLAUDE.md — MariaDB local corrompido; usar `RefreshDatabase` + SQLite in-memory (`phpunit.xml` já configura `DB_CONNECTION=sqlite` e `DB_DATABASE=:memory:` para testes — validar antes de rodar)
  </read_first>
  <behavior>
    Cenários que o teste `SugadorPolicyManageAnalistaTest` deve cobrir (RED primeiro, GREEN depois):

    1. **analista_com_permissao_manage_true**: `User` com `publication_role='analista'` e `attach` de `Permissions::CORE_SUGADORES` retorna `true` em `(new SugadorPolicy())->manage(\\$user)`.
    2. **analista_sem_permissao_manage_false**: `User` analista sem a permissão retorna `false` (proteção contra role sem permission).
    3. **publicador_manage_false**: `publication_role='publicador'` sem `CORE_SUGADORES` retorna `false` (não regride).
    4. **admin_manage_true**: `role='admin'` retorna `true` mesmo sem permission (bypass).
    5. **gestor_manage_true**: `publication_role='gestor'` com `CORE_SUGADORES_GLOBAL` retorna `true` (regressão).
    6. **lider_manage_true**: `publication_role='lider'` com `CORE_SUGADORES_GLOBAL` retorna `true` (regressão).
  </behavior>
  <action>
    1. **RED**: criar `tests/Feature/Phase52/SugadorPolicyManageAnalistaTest.php` com os 6 cenários acima. Cada teste instancia `new SugadorPolicy()` e chama `->manage(\\$user)`. Rodar `php artisan test --filter=SugadorPolicyManageAnalistaTest` — deve falhar em pelo menos `analista_com_permissao_manage_true`.
    2. Commit RED: `test(52-01): SugadorPolicy::manage — cenários analista + regressão admin/gestor/lider`
    3. **GREEN**: em `app/Policies/SugadorPolicy.php:64-71` trocar corpo de `manage()` para:
       `return \\$user->isAdmin() || \\$user->hasPermission(Permissions::CORE_SUGADORES);`
    4. Atualizar docblock (linhas 56-63) — remover parágrafo "Analistas comuns continuam fora" e adicionar linha: "2026-07-01 (Phase 52 A1): analista incluso — operacional precisa ajustar thresholds da própria carteira. Autorização de carteira continua sendo aplicada em `view`/`update`."
    5. Atualizar tabela do docblock (linhas 9-19): trocar `| analista (publication)        | Carteira  | Carteira    | ✗         |` para `| analista (publication)        | Carteira  | Carteira    | ✓         |`
    6. Rodar `php artisan test --filter=SugadorPolicyManageAnalistaTest` — todos verdes.
    7. Commit GREEN: `feat(52-01): SugadorPolicy::manage inclui analista (A1)`
  </action>
  <verify>
    <automated>php artisan test --filter=SugadorPolicyManageAnalistaTest</automated>
  </verify>
  <done>
    - 6 testes verdes em `SugadorPolicyManageAnalistaTest.php`
    - `SugadorPolicy::manage` retorna true para analista com `CORE_SUGADORES`
    - Docblock §9 atualizado refletindo A1
    - Nenhum teste existente em `tests/Feature/Phase*/Sugador*` regride (rodar `php artisan test --filter=Sugador` como sanity — não obrigatório verde 100%, mas anotar diffs)
    - RED e GREEN em commits separados
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Endpoint GET /sugadores/{sugador}/mlbs-hint (RED → GREEN)</name>
  <files>
    - app/Http/Controllers/SugadorController.php
    - routes/web.php
    - tests/Feature/Phase52/SugadorMlbsHintEndpointTest.php
  </files>
  <read_first>
    - app/Http/Controllers/SugadorController.php (linhas 265-286) — método `show()` cria o mesmo shape (`\\$this->adgroupMlbMap->getMlbsForAdgroup(\\$sugador->company_id, (string) \\$sugador->adgroup_id)`); replicar essa lógica no novo endpoint JSON
    - app/Http/Controllers/SugadorController.php (linhas 607-622) — método `mlbs()` (para NÃO reusar — depende de `adman_account_id` e retorna 422 em ML-only)
    - app/Repositories/AdgroupMlbMapRepository.php (linhas 42-55) — `getMlbsForAdgroup(int \\$companyId, string \\$adgroupId): array` — funciona para Adman + ML (resolve `cust_id` via `Company::cust_id` que faz fallback para `ml_store_id`)
    - routes/web.php (linhas 303-316) — bloco Sugadores; inserir nova rota logo após linha 305 (`sugadores.mlbs`)
    - .planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-RESEARCH.md §A5 (linhas 101-125) — decisão locked = criar mlbs-hint, não modificar mlbs()
    - tests/Feature/Phase19/SugadoresVistaDefaultTest.php (linhas 1-40) — pattern Feature test com Sugador::factory (verificar se factory existe; se não, criar via `Sugador::create([...])`)
  </read_first>
  <behavior>
    Cenários que o teste `SugadorMlbsHintEndpointTest` deve cobrir:

    1. **happy_path_adman_company**: Sugador tipo=adgroup em empresa com `adman_account_id`; seed 3 rows em `adman_adgroup_mlbs` (cust_id=adman_account_id, adgroup_id=match); GET → 200 JSON `{mlbs: ['MLB1','MLB2','MLB3'], total: 3}` (ordenados por mlb_id).
    2. **ml_only_company_returns_mlbs**: Sugador em empresa com `adman_account_id=NULL` mas `ml_store_id='SEL123'`; seed 2 rows com `cust_id='SEL123'`; GET → 200 JSON `{mlbs: [...], total: 2}` — CONFIRMA que resolve via `Company::cust_id` accessor.
    3. **sugador_tipo_campanha_returns_empty**: Sugador com `tipo=campanha`; GET → 200 JSON `{mlbs: [], total: 0, reason: 'O drilldown de MLBs só está disponível para sugadores do tipo adgroup.'}` (mesmo shape, sem 422 — decisão locked).
    4. **usuario_fora_carteira_recebe_403**: User consultor sem attach de company; GET → 403.
    5. **admin_autoriza**: Admin acessa qualquer sugador → 200.
    6. **sem_mapeamento_returns_empty**: Sugador adgroup válido mas sem rows em `adman_adgroup_mlbs`; GET → 200 JSON `{mlbs: [], total: 0}` (sem reason — só array vazio).
  </behavior>
  <action>
    1. **RED**: criar `tests/Feature/Phase52/SugadorMlbsHintEndpointTest.php` com os 6 cenários. Usar SQLite in-memory + criação direta de rows via `AdmanAdgroupMlb::create([...])` (não usar factory se não existir — inspecionar `app/Models/AdmanAdgroupMlb.php` primeiro). Rodar `php artisan test --filter=SugadorMlbsHintEndpointTest` — falha (rota não existe).
    2. Commit RED: `test(52-01): endpoint mlbs-hint — happy path + ML-only + autorização`
    3. **GREEN**: em `SugadorController.php` adicionar método `mlbsHint` (colocar logo após `show()`, antes de `updateStatus()`):
       ```
       public function mlbsHint(Sugador $sugador)
       ```
       - `Gate::authorize('view', $sugador)`
       - Se `$sugador->tipo !== Sugador::TIPO_ADGROUP` → retornar JSON 200 `{mlbs: [], total: 0, reason: 'O drilldown de MLBs só está disponível para sugadores do tipo adgroup.'}`
       - Se `$sugador->adgroup_id === null || $sugador->adgroup_id === ''` → retornar JSON 200 `{mlbs: [], total: 0}`
       - Buscar: `$mlbs = $this->adgroupMlbMap->getMlbsForAdgroup($sugador->company_id, (string) $sugador->adgroup_id);`
       - Retornar `response()->json(['mlbs' => $mlbs, 'total' => count($mlbs)])`
       - Docblock em pt-BR explicando: "Phase 52 A5 — endpoint neutro que funciona para empresas Adman e ML-only. Substitui a call de `sugadores.mlbs` no botão 'Copiar MLBs' da listagem, resolvendo o 422 quando adman_account_id é NULL."
    4. Em `routes/web.php` logo após linha 305:
       `Route::get('/sugadores/{sugador}/mlbs-hint', [SugadorController::class, 'mlbsHint'])->name('sugadores.mlbs-hint');`
    5. Rodar `php artisan test --filter=SugadorMlbsHintEndpointTest` — todos verdes.
    6. Commit GREEN: `feat(52-01): endpoint sugadores.mlbs-hint resolve 422 ML-only (A5)`
  </action>
  <verify>
    <automated>php artisan test --filter=SugadorMlbsHintEndpointTest</automated>
  </verify>
  <done>
    - 6 testes verdes
    - Rota `sugadores.mlbs-hint` registrada e retorna JSON com shape estável
    - Empresa ML-only (adman_account_id=NULL, ml_store_id≠NULL) retorna MLBs corretamente
    - RED e GREEN em commits separados
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: Endpoint POST /sugadores/bulk-copy-mlbs (RED → GREEN)</name>
  <files>
    - app/Http/Controllers/SugadorController.php
    - routes/web.php
    - tests/Feature/Phase52/SugadorBulkCopyMlbsTest.php
  </files>
  <read_first>
    - app/Http/Controllers/SugadorController.php (linhas 515-586) — método `bulkMove()`: pattern canônico de bulk (validate `sugador_ids array min:1 max:500`, `Sugador::whereIn->get()`, loop `Gate::authorize`, retorno)
    - app/Http/Controllers/SugadorController.php (linhas 265-286) — `show()`: como obter MLBs via `$this->adgroupMlbMap->getMlbsForAdgroup(...)`
    - routes/web.php (linha 310) — rota `sugadores.bulk-move` (POST) — colocar `bulk-copy-mlbs` logo abaixo, agrupando os bulks
    - .planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-RESEARCH.md §A6 (linhas 127-144) — decisão: retornar `{mlbs, total}` (não `back()->with('success')` porque frontend precisa do array pra escrever no clipboard)
    - app/Models/Sugador.php — constante `TIPO_ADGROUP` (verificar valor exato)
  </read_first>
  <behavior>
    Cenários que o teste `SugadorBulkCopyMlbsTest` deve cobrir:

    1. **agrega_e_dedupla**: 3 sugadores adgroup, dois deles compartilham 1 MLB (MLB999); seed rows para cada adgroup; POST `{sugador_ids: [1,2,3]}` → JSON 200 `{mlbs: [...], total: N, sugadores_processados: 3}` onde N reflete a lista deduplicada (union, não soma).
    2. **ignora_tipo_campanha**: 1 sugador tipo=adgroup + 1 tipo=campanha; POST → só o adgroup contribui; `sugadores_processados` conta apenas os elegíveis processados.
    3. **valida_min_max**: POST sem `sugador_ids` → 422; POST com array vazio → 422; POST com 501 items → 422.
    4. **autoriza_view_por_item**: User consultor com carteira parcial (empresa X apenas); POST com sugador_ids de X + Y (Y fora carteira) → 403 (falha na primeira autorização de Y, seguindo pattern bulkMove).
    5. **admin_autoriza_tudo**: Admin POST com sugadores de várias empresas → 200 agrega todos.
    6. **sugadores_inexistentes**: POST com ID fantasma → 200 com warning ou apenas ignora (documentar comportamento — pattern bulkMove retorna `'warning'` quando `$sugadores->isEmpty()`; nosso caso é análogo mas com JSON: retornar `{mlbs: [], total: 0, sugadores_processados: 0}`).
  </behavior>
  <action>
    1. **RED**: criar `tests/Feature/Phase52/SugadorBulkCopyMlbsTest.php` com os 6 cenários. Rodar `php artisan test --filter=SugadorBulkCopyMlbsTest` — falha (rota não existe).
    2. Commit RED: `test(52-01): endpoint bulk-copy-mlbs — dedup + autorização + validação`
    3. **GREEN**: em `SugadorController.php` adicionar método `bulkCopyMlbs` (após `bulkMove` na mesma seção):
       - `$data = $request->validate(['sugador_ids' => 'required|array|min:1|max:500', 'sugador_ids.*' => 'integer']);`
       - `$sugadores = Sugador::whereIn('id', $data['sugador_ids'])->get();`
       - Se vazio → `response()->json(['mlbs' => [], 'total' => 0, 'sugadores_processados' => 0])`
       - `foreach ($sugadores as $s) Gate::authorize('view', $s);`
       - Coletar MLBs: `$allMlbs = [];` `$processados = 0;`
         `foreach ($sugadores as $s) { if ($s->tipo !== Sugador::TIPO_ADGROUP || $s->adgroup_id === null || $s->adgroup_id === '') continue; $mlbs = $this->adgroupMlbMap->getMlbsForAdgroup($s->company_id, (string) $s->adgroup_id); $allMlbs = array_merge($allMlbs, $mlbs); $processados++; }`
       - Dedup: `$unique = array_values(array_unique($allMlbs));`
       - `sort($unique);` (determinístico p/ testes)
       - Return `response()->json(['mlbs' => $unique, 'total' => count($unique), 'sugadores_processados' => $processados])`
       - Docblock pt-BR: "Phase 52 A6 — ação em massa 'Copiar MLBs dos selecionados'. Segue pattern de `bulkMove` (autoriza cada item). Diferente de `mlbsByCompany`, opera em N sugadores explicitamente selecionados via checkbox."
    4. Em `routes/web.php` logo após linha 310:
       `Route::post('/sugadores/bulk-copy-mlbs', [SugadorController::class, 'bulkCopyMlbs'])->name('sugadores.bulk-copy-mlbs');`
    5. Rodar `php artisan test --filter=SugadorBulkCopyMlbsTest` — verdes.
    6. Commit GREEN: `feat(52-01): endpoint sugadores.bulk-copy-mlbs (A6)`
  </action>
  <verify>
    <automated>php artisan test --filter=SugadorBulkCopyMlbsTest</automated>
  </verify>
  <done>
    - 6 testes verdes
    - Rota `sugadores.bulk-copy-mlbs` registrada e retorna JSON `{mlbs, total, sugadores_processados}`
    - Autorização view aplicada por item (403 quando qualquer item fora carteira)
    - Dedup determinístico (sort asc)
    - RED e GREEN em commits separados
    - Sanity: `php artisan test --filter=Phase52` (todos 3 arquivos verdes)
  </done>
</task>

</tasks>

<verification>
Verificar antes de fechar o plan:
- `php artisan test --filter=Phase52` → 3 arquivos, ~18 testes verdes
- `php artisan route:list --path=sugadores | grep -E "mlbs-hint|bulk-copy-mlbs"` → 2 rotas listadas com nomes esperados
- `grep -n "hasPermission(Permissions::CORE_SUGADORES)" app/Policies/SugadorPolicy.php` → aparece em `analyze()` E em `manage()` (2 ocorrências mínimo)
- `grep -c "sugadores.mlbs-hint\|sugadores.bulk-copy-mlbs" routes/web.php` → ≥2 (uma pra cada rota)
- Sanity regressão: `php artisan test --filter=SugadorPolicy` (todos os testes existentes de policy) — se alguma regressão nova aparecer, é bug no diff da Tarefa 1
- Ver o arquivo do teste de `bulkMove` (se existir) e confirmar que ele continua verde
</verification>

<success_criteria>
- [ ] `SugadorPolicy::manage()` retorna true para User com publication_role=analista + CORE_SUGADORES (regressão admin/gestor/lider preservada)
- [ ] Endpoint `GET /sugadores/{sugador}/mlbs-hint` retorna 200 JSON com shape `{mlbs: string[], total: int}` para empresa Adman-only, ML-only e híbrida
- [ ] Endpoint `POST /sugadores/bulk-copy-mlbs` valida input, autoriza view por item, agrega + dedupla + ordena MLBs, retorna JSON `{mlbs, total, sugadores_processados}`
- [ ] 3 arquivos de teste em `tests/Feature/Phase52/` com ≥6 cenários cada, todos verdes
- [ ] Cada tarefa tem commit RED separado do GREEN (histórico auditável)
- [ ] Docblock §9 de `SugadorPolicy` reflete o novo estado (tabela + linha 2026-07-01)
</success_criteria>

<output>
Criar `.planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-01-SUMMARY.md` ao concluir, listando:
- Testes que passaram e count total
- Rotas novas registradas (com HTTP verb + nome)
- Diff da policy (antes/depois no docblock)
- Any gotcha encontrado ao rodar (ex.: constante `TIPO_ADGROUP` tem valor diferente do esperado, `AdmanAdgroupMlb` model factory ausente, etc)
</output>
