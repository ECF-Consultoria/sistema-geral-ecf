---
phase: 52-melhorias-ui-ux-comportamento-sugadores
plan: "02"
type: execute
wave: 2
depends_on:
  - 52-01
autonomous: true
requirements:
  - REQ-52-02
files_modified:
  - resources/js/Pages/Sugadores/Index.jsx
  - resources/js/Pages/Sugadores/Show.jsx
must_haves:
  truths:
    - "Nenhum texto/badge referencia 'Análise diária', 'Análise OK hoje', 'Sem análise hoje', 'Análise diária roda às 12:00 BRT' ou 'API Adman D-1' na UI de /sugadores"
    - "CompanyCard não tem mais botão 'Reanalisar' nem tooltip mencionando 'Análise diária já rodou hoje'"
    - "Header de /sugadores não tem mais botão 'Rodar análise' (global) — analista/gestor perdem essa entrada de disparo em massa acidental"
    - "Tabela lista completa (Index.jsx:876-907 toggle + Index.jsx:1014+ bloco lista) foi removida; `abrirDrilldown(companyId)` foi migrado para navegar até `/sugadores/{id}` ou até uma versão simplificada da tabela por-empresa (decisão registrada no SUMMARY)"
    - "Coluna 'Empresa' foi removida no fluxo drilldown por empresa (visão simplificada mantida ou substituída — coerente com A9)"
    - "npm run build compila sem erros nem warnings novos"
  artifacts:
    - path: "resources/js/Pages/Sugadores/Index.jsx"
      provides: "Página Index limpa: sem `AnaliseBadge`, sem botão Reanalisar no card, sem botão 'Rodar análise' global, sem toggle cards/lista, sem bloco de lista completa"
    - path: "resources/js/Pages/Sugadores/Show.jsx"
      provides: "Show sem textos legacy de 'era Adman'; tabela de sugadores relacionados (se existir na Show) sem coluna 'Empresa' redundante"
  key_links:
    - from: "Index.jsx CompanyCard"
      to: "abrirDrilldown ou router.visit(route('sugadores.show', ...))"
      via: "onClick do card / botão 'Ver sugadores'"
      pattern: "Index.jsx:210-215 já usa `onVer(card.company_id)` — migração: em vez de abrir modo lista, abrir uma rota que continua servindo drilldown por empresa"
    - from: "Index.jsx removido"
      to: "SugadorController::index"
      via: "prop `view_mode` deixa de ser consumida — backend continua enviando, sem quebra"
      pattern: "props ignoradas no cliente não geram erro"
---

<objective>
Limpeza pesada da UI: remover textos/UX legacy da era Adman, botões redundantes de rodar análise, e a visualização em lista completa (A2, A4, A7, A9). Sem TDD porque tudo é remoção de elementos UI puros. Wave 3 adiciona as novas features (mlbs-hint, bulk-copy, config card, botão análise per-empresa) em cima do Index já limpo.

**IMPORTANTE — A9:** o CONTEXT locka "remover a tabela lista inteira". O RESEARCH alerta (§A9 linhas 236-239) que `abrirDrilldown` levava ao modo lista com `company_id` filtrado. Antes de deletar em massa, decidir e registrar no SUMMARY:
- **Opção X**: `abrirDrilldown` passa a navegar para `/sugadores/{id}` do sugador mais recente da empresa (só se houver 1 empresa 1 sugador dominante — não é o caso).
- **Opção Y (preferível):** manter uma tabela por-empresa MUITO simplificada (sem toggle, acessada só via card) — remove toggle + bloco lista global, mas mantém a lista quando `company_id` é filtrado. A4 (remover coluna empresa) faz sentido nesse contexto.
- **Opção Z:** eliminar completamente qualquer tabela em /sugadores/index e forçar o operador a entrar em cada sugador via card → drill em Show. Requer novo fluxo (agrupar cards por empresa com contagem clicável). Escopo maior.

Executor DEVE registrar no SUMMARY qual opção adotou e por quê. Recomendação padrão: **Opção Y** — menor delta, coerente com A4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-CONTEXT.md
@.planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-RESEARCH.md
@.planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Remover textos e badges da era Adman (A2)</name>
  <files>
    - resources/js/Pages/Sugadores/Index.jsx
    - resources/js/Pages/Sugadores/Show.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/Index.jsx (linhas 148-173) — componente `AnaliseBadge` inteiro
    - resources/js/Pages/Sugadores/Index.jsx (linhas 185-188) — chamada do `AnaliseBadge` dentro de `CompanyCard`
    - resources/js/Pages/Sugadores/Index.jsx (linhas 251-273) — tooltips com "Análise diária já rodou hoje" (linhas 252-254) e bloco `card.analisado_hoje && (<p>...</p>)` (linhas 269-273)
    - resources/js/Pages/Sugadores/Index.jsx (linhas 862-874) — banner header "Análise diária roda às 12:00 BRT · Última execução..."
    - .planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-RESEARCH.md §A2 (linhas 39-64) — tabela completa de matches; Show.jsx §A2 nota linhas 54-61 confirmam que as mensagens da Show sobre "última sincronização" SÃO LEGÍTIMAS e devem ser MANTIDAS (não confundir)
  </read_first>
  <action>
    1. Em `Index.jsx`:
       - **Deletar** componente `AnaliseBadge` inteiro (linhas 148-173, incluindo comentário linha 148-150 sobre AdmanSyncLog)
       - **Deletar** a chamada `<AnaliseBadge ... />` em `CompanyCard` (linha 187)
       - **Simplificar** tooltip do botão Reanalisar (linhas 252-254) — este botão será removido inteiro na Tarefa 2, então não gastar edição fina aqui: só deixar consistente até a Tarefa 2 rodar
       - **Deletar** bloco `{card.analisado_hoje && (<p>Análise diária já rodou hoje ...)}` (linhas 269-273)
       - **Deletar** o `<span>` do banner header inteiro (linhas 868-873) — remover também o comentário de contexto acima (linhas 865-867)
       - Se após remoção o import `AnaliseBadge` ou variáveis auxiliares (`sincronizou_hoje`, `analise_diaria`) ficarem sem uso, remover imports/desestruturações órfãs (o executor deve buscar `grep -n "analise_diaria\|sincronizou_hoje" Index.jsx` e limpar referências mortas)
    2. Em `Show.jsx`:
       - **NÃO REMOVER** as linhas 710, 714, 718 (RESEARCH §A2 linhas 54-61 confirma que são UI legítima do drilldown MLBs)
       - Verificar (grep no arquivo) se há alguma menção a "Análise diária" ou "cron Adman" fora dessas 3 — se houver, remover
  </action>
  <verify>
    <automated>grep -n "Análise diária\|Análise OK hoje\|Sem análise hoje\|AnaliseBadge\|API Adman D-1\|analise_diaria?" resources/js/Pages/Sugadores/Index.jsx | grep -v "^\\s*//" | wc -l</automated>
  </verify>
  <done>
    - Zero matches (não-comentário) das strings acima em Index.jsx
    - Show.jsx mantém as 3 mensagens legítimas de "última sincronização" nas linhas próximas de 710-718
    - Componente `AnaliseBadge` não existe mais
    - Nenhuma variável órfã (executor confirma via `npm run build` no fim da Tarefa 4)
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Remover botão Reanalisar do CompanyCard (A7 parte 1)</name>
  <files>
    - resources/js/Pages/Sugadores/Index.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/Index.jsx (linhas 238-259) — bloco `{canAnalyze && card.can_analyze && (<button ...>Reanalisar</button>)}`
    - resources/js/Pages/Sugadores/Index.jsx (linha 175) — assinatura `CompanyCard({ card, canAnalyze, enqueuedAt, onReanalisar, onVer, ... })` — remover props `canAnalyze` e `onReanalisar`
    - resources/js/Pages/Sugadores/Index.jsx (linhas 275-279) — bloco `{enqueuedAt && !card.analisado_hoje && (<p>Enfileirado...`
    - resources/js/Pages/Sugadores/Index.jsx (linhas 600-602) — estado `const [enqueuedAt, setEnqueuedAt] = useState({});`
    - resources/js/Pages/Sugadores/Index.jsx (linhas 762-778) — função `reanalisarEmpresa` (busca via grep — RESEARCH cita esta faixa)
    - .planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-RESEARCH.md §A7 (linhas 148-164)
  </read_first>
  <action>
    1. Deletar bloco `{canAnalyze && card.can_analyze && (...)}` — linhas 238-259 do research
    2. Deletar bloco `{enqueuedAt && !card.analisado_hoje && (...)}` — linhas 275-279
    3. Remover props `canAnalyze` e `onReanalisar` da desestruturação da assinatura de `CompanyCard` (linha 175)
    4. Remover estado `enqueuedAt` (linha 602 + setter) e função `reanalisarEmpresa` (linhas ~762-778 no research). Executor grep `enqueuedAt` e `reanalisarEmpresa` no arquivo pra pegar todas as referências e limpar 100%
    5. Nas chamadas de `<CompanyCard ... />` no JSX, remover as props `canAnalyze={...}` e `onReanalisar={...}` que forem passadas
  </action>
  <verify>
    <automated>grep -c "reanalisarEmpresa\|enqueuedAt\|onReanalisar" resources/js/Pages/Sugadores/Index.jsx</automated>
  </verify>
  <done>
    - Grep retorna 0
    - CompanyCard não tem mais botão Reanalisar
    - Nenhum warning de React sobre prop indefinida na build
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: Remover botão 'Rodar análise' global do header (A7 parte 2)</name>
  <files>
    - resources/js/Pages/Sugadores/Index.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/Index.jsx (linhas 928-937) — botão `{can_analyze && (<button onClick={runAnalysis} ...>Rodar análise</button>)}` no header
    - resources/js/Pages/Sugadores/Index.jsx (linhas 730-737) — função `runAnalysis` com `confirm('Rodar análise para TODAS as empresas ...`
    - resources/js/Pages/Sugadores/Index.jsx — imports `PlayCircle` (linha ~5 top imports; verificar se ainda é usado em outro lugar após remoção)
    - resources/js/Pages/Sugadores/Index.jsx — estado `analyzing` (grep por `const [analyzing, setAnalyzing]`) — remover se `runAnalysis` for o único consumidor
    - .planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-RESEARCH.md §A8 (linhas 199-207) — confirma remoção do alert global
  </read_first>
  <action>
    1. Deletar botão do header (linhas 928-937)
    2. Deletar função `runAnalysis` (linhas 730-737) incluindo o `confirm(...)`
    3. Deletar estado `analyzing` + setter se não houver outro consumidor (executor confirma com grep)
    4. Remover import de `PlayCircle` se não houver outro consumidor (grep no arquivo)
    5. **NÃO** deletar a rota `sugadores.analyze-all` no backend — mantida para Artisan/scripts (RESEARCH §A8 linha 206)
  </action>
  <verify>
    <automated>grep -c "runAnalysis\|Rodar análise para TODAS\|route('sugadores.analyze-all'" resources/js/Pages/Sugadores/Index.jsx</automated>
  </verify>
  <done>
    - Grep retorna 0 no Index.jsx (backend intacto)
    - Header não tem mais botão global amarelo "Rodar análise"
  </done>
</task>

<task type="auto">
  <name>Tarefa 4: Remover toggle Cards/Lista + tabela lista completa (A9) + coluna Empresa (A4) + npm run build</name>
  <files>
    - resources/js/Pages/Sugadores/Index.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/Index.jsx (linhas 876-907) — bloco `<div className="inline-flex ... p-0.5">` com botões Cards/Lista
    - resources/js/Pages/Sugadores/Index.jsx (linhas 740-746) — função `switchView`
    - resources/js/Pages/Sugadores/Index.jsx (linhas 749-759) — função `abrirDrilldown` (usa `view: 'list'` — precisa migrar)
    - resources/js/Pages/Sugadores/Index.jsx (linhas 908-917) — bloco `{view_mode === 'list' && (<button ...Filtros...)}`
    - resources/js/Pages/Sugadores/Index.jsx (linhas 982-1014) — bloco `{view_mode === 'cards' && (...)}` — MANTER (é a visão default)
    - resources/js/Pages/Sugadores/Index.jsx (linhas 1014-1391) — bloco `{view_mode === 'list' && (...)}` — decidir conforme A9 (Opção Y = manter simplificado com filtro obrigatório de `company_id`; Opção Z = remover tudo)
    - resources/js/Pages/Sugadores/Index.jsx (linhas 1194 e 1244-1249) — `<th>Empresa</th>` e `<td>...{s.company?.name}</td>` da coluna a remover (A4)
    - resources/js/Pages/Sugadores/Index.jsx (linhas 720, 727) — usos de `view_mode` em `applyFilters` e `clearFilters` — limpar
    - .planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-RESEARCH.md §A4 (linhas 85-98) e §A9 (linhas 210-239)
    - CLAUDE.md — deploy exige `npm run build` após frontend change
  </read_first>
  <action>
    **Decisão A9 (registrar no SUMMARY qual opção adotou):**

    **Opção Y (recomendada — menor delta):**
    1. Deletar toggle Cards/Lista (linhas 876-907)
    2. Deletar função `switchView` (linhas 740-746)
    3. Reescrever `abrirDrilldown`: em vez de `router.get(..., { company_id, view: 'list' })`, passar só `{ company_id }` (sem `view`). No backend/prop `view_mode` isso vai fazer `view_mode='cards'` (default) — precisamos adaptar: introduzir uma nova variável derivada `showCompanyList = !!f.company_id` que ativa o bloco de lista SÓ quando há filtro de empresa
    4. Substituir todas as `view_mode === 'list'` por `showCompanyList` e todas `view_mode === 'cards'` por `!showCompanyList` (o toggle visual some, o modo continua alternando pela presença de `company_id`)
    5. No bloco de lista (linha 1194 header + 1244-1249 body), aplicar condicional: `{!f.company_id && <th>Empresa</th>}` no header e `{!f.company_id && <td>...</td>}` no body — resolve A4 (quando drilldown está ativo, coluna some)
    6. Limpar linha 720 (`if (view_mode) clean.view = view_mode;` — deletar) e 727 (`router.get(route('sugadores.index'), { view: view_mode }, ...)` — tirar o `{ view: view_mode }`, deixar `{}`)
    7. Deletar bloco `{view_mode === 'list' && (<button>Filtros</button>)}` (linhas 908-917) — mover botão Filtros para sempre visível quando há company_id filtrado
    8. Remover imports `LayoutGrid`, `List` do lucide-react se não usados em outro lugar (grep)
    9. Remover prop `view_mode` da desestruturação de `props` que Inertia envia — mesmo que backend continue enviando, deixar de consumir

    **Opção Z (se executor decidir):** deletar todo o bloco 1014-1391 + refazer `abrirDrilldown` para `router.visit(route('sugadores.show', s.id))` do primeiro sugador da empresa (requer prop `first_sugador_id_por_empresa` ou fetch on-demand). MAIS TRABALHO. Só adotar se Opção Y ficar visualmente ruim.

    **Depois da decisão (qualquer opção):**
    10. `npm run build` → deve compilar sem erros nem warnings NOVOS (warnings pré-existentes de outros arquivos são tolerados)
    11. Registrar no SUMMARY: opção adotada, motivo, se Opção Y então quantos usos de `view_mode ===` foram trocados
  </action>
  <verify>
    <automated>grep -c "switchView\|view_mode === 'list'\|view_mode === 'cards'\|LayoutGrid" resources/js/Pages/Sugadores/Index.jsx</automated>
  </verify>
  <done>
    - Grep retorna 0 (toggle e switchView foram removidos)
    - Se Opção Y adotada: `showCompanyList` está definido e substitui os checks de `view_mode`
    - Se Opção Z adotada: bloco 1014-1391 foi deletado e `abrirDrilldown` navega para outra rota
    - Coluna "Empresa" some quando `company_id` está filtrado (A4)
    - `npm run build` OK
    - Decisão A9 registrada no SUMMARY com justificativa
  </done>
</task>

</tasks>

<verification>
- `grep -c "AnaliseBadge\|Análise diária\|runAnalysis\|reanalisarEmpresa\|switchView\|view_mode ===" resources/js/Pages/Sugadores/Index.jsx` → 0
- `npm run build` sai com código 0 e sem warnings novos
- Abrir `/sugadores` visualmente (após deploy da Wave 4) — cards continuam funcionando; toggle sumiu; header sem botão amarelo "Rodar análise"; nenhum texto sobre análise diária/API Adman
- Filtrar por uma empresa e verificar que:
  - Se Opção Y: aparece a tabela sem coluna "Empresa"
  - Se Opção Z: navega direto para outra rota
- Confirmar visualmente que o comportamento de "cards por default; drilldown ao clicar" está preservado
</verification>

<success_criteria>
- [ ] Zero matches de textos legacy de "análise diária"/"Adman D-1"/"AnaliseBadge" no Index.jsx (grep válida)
- [ ] CompanyCard sem botão Reanalisar; header sem botão "Rodar análise"; toggle Cards/Lista removido
- [ ] `enqueuedAt`, `reanalisarEmpresa`, `runAnalysis`, `switchView`, `AnaliseBadge` removidos + estados/imports órfãos limpos
- [ ] Coluna "Empresa" some quando `company_id` filtrado (A4)
- [ ] `npm run build` verde
- [ ] SUMMARY registra decisão A9 (Opção Y vs Z) com justificativa
</success_criteria>

<output>
Criar `.planning/phases/52-melhorias-ui-ux-comportamento-sugadores/52-02-SUMMARY.md` listando:
- Linhas removidas por bloco (aproximado)
- Decisão A9 adotada (Opção Y / Z) com motivo
- Se Opção Y: quantos usos de `view_mode ===` foram trocados por `showCompanyList` (grep antes/depois)
- Warnings de build (se houver, indicar quais são pré-existentes)
</output>
