---
phase: 96-nps-anti-burlamento-endurecimento-e-gest-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_17_090001_add_blocked_event_type_to_nps_survey_events.php
  - app/Models/NpsSurveyEvent.php
  - app/Http/Controllers/NpsController.php
  - resources/js/Pages/Nps/Blocked.jsx
  - tests/Feature/Phase96/NpsBloqueioSessaoInternaTest.php
autonomous: true
requirements: [AB-96-1]
user_setup: []

must_haves:
  truths:
    - "Usuário interno logado que abre um link NPS e tenta responder (POST) é bloqueado — nenhuma NpsResponse é criada"
    - "O bloqueio é auditado como evento `blocked` em nps_survey_events com o user_id da sessão"
    - "A ABERTURA (GET /nps/{token}) continua funcionando para sessão interna, exatamente como na Fase 94"
    - "O usuário bloqueado vê uma mensagem amigável em pt-BR que não revela o mecanismo de detecção"
  artifacts:
    - path: "database/migrations/2026_07_17_090001_add_blocked_event_type_to_nps_survey_events.php"
      provides: "7º event_type `blocked` no enum (branch MySQL) sem quebrar SQLite (branch string)"
      contains: "getDriverName"
    - path: "app/Models/NpsSurveyEvent.php"
      provides: "constante TYPE_BLOCKED e entrada em TYPES"
      contains: "TYPE_BLOCKED"
    - path: "resources/js/Pages/Nps/Blocked.jsx"
      provides: "página de bloqueio amigável, molde de Expired.jsx"
      min_lines: 10
    - path: "tests/Feature/Phase96/NpsBloqueioSessaoInternaTest.php"
      provides: "prova de bloqueio + evento + GET permitido"
  key_links:
    - from: "app/Http/Controllers/NpsController.php"
      to: "NpsSurveyEvent (blocked) + Inertia Nps/Blocked"
      via: "submitResponse() intercepta auth()->check() antes do dispatch v15/legacy"
      pattern: "auth\\(\\)->check\\(\\)"
---

<objective>
Endurecer a Regra 4 da Fase 94 (que hoje apenas MARCA como suspeita): um submit (POST) em sessão autenticada de usuário interno passa a ser BLOQUEADO antes de qualquer `NpsResponse::create()`, com evento auditado e mensagem amigável. A ABERTURA (GET) permanece inalterada.

Purpose: Impedir que colaborador interno logado registre respostas NPS (burla interna), fechando o vetor de tampering do bônus na origem.
Output: Migration do 7º event_type, constante no model, interceptação em `submitResponse()`, página `Nps/Blocked.jsx`, suíte de testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md

<interfaces>
<!-- Contratos que o executor usa direto — não explorar o codebase. -->

app/Http/Controllers/NpsController.php — ponto de bloqueio (linha ~865):
```php
public function submitResponse(Request $request, string $token)
{
    $survey = NpsSurvey::where('token', $token)->where('status', 'pending')->firstOrFail();
    if ($survey->isExpired()) {
        return response()->json(['error' => 'Pesquisa expirada.'], 422);
    }
    // <<< INSERIR BLOQUEIO AQUI, antes do if abaixo >>>
    if ($survey->template_id !== null) {
        return $this->submitResponseV15($request, $survey);
    }
    return $this->submitResponseLegacy($request, $survey);
}
```

app/Models/NpsSurveyEvent.php — constantes existentes (linhas 23-37):
```php
public const TYPE_GENERATED    = 'generated';
public const TYPE_OPENED       = 'opened';
public const TYPE_SUBMITTED    = 'submitted';
public const TYPE_EXPIRED      = 'expired';
public const TYPE_SENT_EMAIL   = 'sent_email';
public const TYPE_SENT_DIGISAC = 'sent_digisac';
public const TYPES = [ ...os 6 acima... ];  // metadata é cast 'array'
```

Colunas de NpsSurveyEvent (Fase 94): survey_id, event_type, ip_address, user_agent, user_id, metadata.

Rota: `POST /nps/{token}` → `nps.submit` (routes/web.php:333) — PÚBLICA, sem middleware auth. Nenhuma rota nova é necessária.

Molde da página: resources/js/Pages/Nps/Expired.jsx e Nps/AlreadyCompleted.jsx (páginas públicas full-screen, sem AppLayout, dark theme, ícone lucide-react centralizado).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration do 7º event_type `blocked` + constante no model + teste RED</name>
  <files>database/migrations/2026_07_17_090001_add_blocked_event_type_to_nps_survey_events.php, app/Models/NpsSurveyEvent.php, tests/Feature/Phase96/NpsBloqueioSessaoInternaTest.php</files>
  <read_first>
    - .planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md (seção "Migration — 7º event_type `blocked`" e Pitfall 3)
    - app/Models/NpsSurveyEvent.php (constantes TYPE_* e array TYPES)
    - database/migrations/2026_07_14_100001_add_shopee_to_servicos_setor_enum.php (padrão enum+SQLite comprovado)
  </read_first>
  <behavior>
    - Teste: `NpsSurveyEvent::create(['event_type' => 'blocked', ...])` persiste sem violar CHECK constraint no SQLite dos testes.
    - Teste: `NpsSurveyEvent::TYPE_BLOCKED === 'blocked'` e está presente em `NpsSurveyEvent::TYPES`.
  </behavior>
  <action>
    Criar a migration seguindo EXATAMENTE o padrão enum+SQLite do módulo (Pitfall 3 do RESEARCH): no branch `sqlite`, `$table->string('event_type', 20)->change()` (remove o CHECK); no branch MySQL, `DB::statement("ALTER TABLE nps_survey_events MODIFY COLUMN event_type ENUM('generated','opened','submitted','expired','sent_email','sent_digisac','blocked') NOT NULL")`. `down()` idempotente que aborta com RuntimeException se existirem eventos `blocked` órfãos (impede rollback destrutivo), e no branch sqlite apenas retorna. Adicionar `public const TYPE_BLOCKED = 'blocked';` em NpsSurveyEvent e incluí-la em `TYPES`. Criar o diretório tests/Feature/Phase96/ e o arquivo de teste com os cenários acima (esta task deixa o teste RED só na parte que depende da Task 2; os cenários de migration/constante já ficam GREEN).
  </action>
  <verify>
    <automated>php artisan migrate --env=testing && php artisan test --filter=NpsBloqueioSessaoInternaTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=NpsBloqueioSessaoInternaTest` roda o arquivo (não "No tests found"); cenários de constante/persistência do event_type `blocked` passam em SQLite.
    - `grep -c "TYPE_BLOCKED" app/Models/NpsSurveyEvent.php` retorna >= 2 (constante + entrada em TYPES).
  </acceptance_criteria>
  <done>Enum `blocked` persiste no SQLite dos testes; constante disponível no model; arquivo de teste Phase96 criado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Interceptar submit de sessão interna em submitResponse() + página Blocked.jsx</name>
  <files>app/Http/Controllers/NpsController.php, resources/js/Pages/Nps/Blocked.jsx, tests/Feature/Phase96/NpsBloqueioSessaoInternaTest.php</files>
  <read_first>
    - app/Http/Controllers/NpsController.php (método submitResponse ~linha 865; submitResponseV15/submitResponseLegacy para confirmar que nada é criado antes do dispatch)
    - resources/js/Pages/Nps/Expired.jsx (molde exato da página full-screen)
    - .planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md (Padrão 1, diagrama de bloqueio)
  </read_first>
  <behavior>
    - Teste: usuário interno autenticado faz POST /nps/{token} → resposta Inertia renderiza componente 'Nps/Blocked', NENHUMA linha em nps_responses é criada, e existe 1 evento `blocked` com o user_id da sessão.
    - Teste: submit ANÔNIMO (sem auth) para o mesmo token continua criando a NpsResponse normalmente (não regride o fluxo público).
    - Teste: GET /nps/{token} logado como interno continua 200 e emite `opened` (regressão do comportamento Fase 94 — pode reusar NpsOpenTrailTest como referência, mas asserir aqui).
  </behavior>
  <action>
    Em `submitResponse()`, ANTES do `if ($survey->template_id !== null)`, inserir o bloqueio compartilhado pelos dois paths (per AB-96-1): se `auth()->check()`, criar `NpsSurveyEvent` com `event_type => NpsSurveyEvent::TYPE_BLOCKED`, `ip_address => $request->ip()`, `user_agent => $request->userAgent()`, `user_id => auth()->id()`, e retornar `Inertia::render('Nps/Blocked')`. NÃO duplicar a lógica dentro de submitResponseV15/Legacy (mesmo princípio anti-duplicação da Fase 94). Criar `Nps/Blocked.jsx` no molde de `Expired.jsx`: ícone `ShieldAlert` (lucide-react), título "Não foi possível registrar sua resposta", texto orientando abrir o link em janela anônima/outro navegador. A mensagem NÃO deve mencionar IP, sessão, login nem "usuário interno" (per decisão locked: não revelar o gatilho). Completar os cenários RED do teste da Task 1.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsBloqueioSessaoInternaTest</automated>
  </verify>
  <acceptance_criteria>
    - Suíte NpsBloqueioSessaoInternaTest 100% verde, incluindo asserção `assertDatabaseCount('nps_responses', 0)` após o submit logado.
    - `php artisan test --filter=Nps` verde (baseline 264 preservado, sem regressão do fluxo público).
    - Blocked.jsx existe e não contém as palavras "IP", "login", "sessão interna" ou "usuário interno" na mensagem exibida.
  </acceptance_criteria>
  <done>Submit logado é bloqueado com evento auditado e página amigável; submit anônimo e GET logado permanecem intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → POST /nps/{token} | Entrada pública anônima; agora pode conter uma sessão autenticada interna (cookie web) que NÃO deveria responder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | NpsController::submitResponse() | mitigate | Bloqueio server-side por `auth()->check()` ANTES de qualquer `NpsResponse::create()` — nenhum dado suspeito entra na base; prova por `assertDatabaseCount('nps_responses', 0)` |
| T-96-02 | Information Disclosure | resources/js/Pages/Nps/Blocked.jsx | mitigate | Mensagem genérica sem citar IP/sessão/login — impede que o usuário deduza a regra de detecção por tentativa/erro |
| T-96-03 | Repudiation | nps_survey_events | mitigate | Evento `blocked` grava user_id + ip + user_agent — trilha append-only de quem tentou burlar |
| T-96-SC | Tampering | npm/composer installs | accept | Nenhum pacote instalado nesta fase (RESEARCH: "nenhuma dependência nova") — sem superfície de supply-chain |
</threat_model>

<verification>
- `php artisan test --filter=NpsBloqueioSessaoInternaTest` verde
- `php artisan test --filter=Nps` verde (264 baseline, sem regressão)
- Migration reversível: `php artisan migrate:rollback` aborta com mensagem clara se houver eventos `blocked` (não roda em SQLite)
</verification>

<success_criteria>
- Sessão interna logada não consegue submeter; o bloqueio fica auditado como `blocked`
- GET e submit anônimo inalterados
- Nenhum jargão na mensagem ao usuário
</success_criteria>

<output>
Criar `.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-01-SUMMARY.md` ao concluir.
</output>
