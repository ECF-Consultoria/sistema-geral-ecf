---
phase: 96-nps-anti-burlamento-endurecimento-e-gest-o
plan: 04
type: execute
wave: 4
depends_on: [96-03]
files_modified:
  - app/Services/DesempenhoScoreService.php
  - app/Http/Controllers/PerformanceController.php
  - app/Http/Controllers/DashboardController.php
  - app/Http/Controllers/PortfolioController.php
  - app/Jobs/CalculateGoalResults.php
  - tests/Feature/Phase96/NpsInvalidacaoCallSitesTest.php
  - tests/Feature/V16/BonusDualPathRegressaoTest.php
autonomous: true
requirements: [AB-96-3]
user_setup: []

must_haves:
  truths:
    - "Uma resposta com invalidated_at preenchido NÃO aparece em NENHUM dos 8 call-sites de agregação (bônus + dashboards + metas)"
    - "O bônus (DesempenhoScoreService::compute) não conta a resposta invalidada em nenhum dos dois ramos (atribuições e legado)"
    - "Os widgets da carteira (PerformanceController) e o Histórico NPS mensal (PortfolioController) não mostram a resposta invalidada"
    - "Os widgets de dashboard (home, userDashboard, buildRanking) não contam a resposta invalidada"
    - "As metas NPS mensais (CalculateGoalResults::computeNps) não contam a resposta invalidada"
    - "A regressão do bônus (V16/Desempenho) permanece verde — nenhuma resposta VÁLIDA foi excluída por engano"
  artifacts:
    - path: "app/Services/DesempenhoScoreService.php"
      provides: "notasPorAtribuicao() e notasLegado() filtram invalidated_at"
      contains: "invalidated_at"
    - path: "tests/Feature/Phase96/NpsInvalidacaoCallSitesTest.php"
      provides: "1 asserção de exclusão por call-site (checklist do RESEARCH Padrão 3)"
    - path: "tests/Feature/V16/BonusDualPathRegressaoTest.php"
      provides: "cenário 'resposta invalidada não conta em nenhum dos dois ramos'"
  key_links:
    - from: "8 call-sites de leitura de NpsResponse/NpsScoreAssignment"
      to: "NpsResponse::scopeValida() / whereNull('invalidated_at')"
      via: "JOIN (whereNull r.invalidated_at) ou eager-load (fn($q)=>$q->valida())"
      pattern: "invalidated_at|valida\\(\\)"
---

<objective>
Aplicar o filtro de invalidação (`scopeValida` / `whereNull('invalidated_at')`) nos 8 call-sites de agregação EXTERNOS ao NpsController — os que alimentam o BÔNUS (financeiro), dashboards e metas — cada um com um teste de regressão dedicado provando que a resposta invalidada some daquele ponto. Este é o plano mais arriscado da fase: esquecer 1 call-site deixa a invalidação "meio-contando" e corrompe pagamento de bônus.

Purpose: Garantir consistência TOTAL da invalidação em toda a superfície de leitura, sem regredir o cálculo do bônus para respostas legítimas.
Output: 6 arquivos de produção com o filtro aplicado + suíte de call-sites (1 asserção por ponto) + extensão do BonusDualPathRegressaoTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md

<interfaces>
<!-- Contrato entregue pelo Plano 96-03 (JÁ DEVE EXISTIR quando este plano roda): -->
app/Models/NpsResponse.php: `public function scopeValida($query) { return $query->whereNull('invalidated_at'); }`
Coluna: nps_responses.invalidated_at (nullable). Preenchida = resposta desconsiderada.

<!-- CHECKLIST EXAUSTIVO dos 8 call-sites (RESEARCH Padrão 3, tabela confirmada por leitura direta) -->
| # | Arquivo | Método (~linha) | Mudança exata |
|---|---------|-----------------|---------------|
| 1 | DesempenhoScoreService.php | notasPorAtribuicao() (~485) | JOIN `nps_responses as r` já existe → adicionar `->whereNull('r.invalidated_at')` |
| 2 | DesempenhoScoreService.php | notasLegado() (~567) | `NpsSurvey::with('response')->principal()` → `->with(['response' => fn($q) => $q->valida()])` (o foreach já pula `$response === null`) |
| 3 | PerformanceController.php | notasNpsDoUsuarioPorResposta() ramo A (~602) | JOIN já existe → `->whereNull('r.invalidated_at')` (alimenta os 3 widgets da carteira numa passagem) |
| 4 | PerformanceController.php | notasNpsDoUsuarioPorResposta() ramo B legado (~637) | `->with(['response.answers','response.survey'])->principal()` → `->with(['response' => fn($q) => $q->valida()->with(['answers','survey'])])` |
| 5 | DashboardController.php | home() widgets NPS (~540) | `->with('response')->principal()` → `->with(['response' => fn($q) => $q->valida()])` |
| 6 | DashboardController.php | userDashboard() widgets NPS (~1058) | mesmo padrão do #5 |
| 7 | DashboardController.php | buildRanking() $npsHistory (~1583) + avgNotaDimensao() (~1139) | mesmo padrão do #5 (avgNotaDimensao já filtra nulos — só filtrar o eager-load basta) |
| 8 | PortfolioController.php | "Histórico NPS mensal" (~1600) | implementação PRÓPRIA (single-path, SEM NpsScoreAssignment) → mesmo padrão do #4 |
| 9 | CalculateGoalResults.php | computeNps() (~210) | `NpsResponse::query()->whereHas('survey', ...)` → `->whereNull('invalidated_at')` direto (query de NpsResponse) |

ATENÇÃO (Pitfall 4/6): PortfolioController e PerformanceController NÃO compartilham a função — são fixes SEPARADOS. Qualquer PR que não toque ≥5 arquivos quase certamente esqueceu um call-site.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filtro nos call-sites do BÔNUS (DesempenhoScoreService + PerformanceController) + regressão V16</name>
  <files>app/Services/DesempenhoScoreService.php, app/Http/Controllers/PerformanceController.php, tests/Feature/Phase96/NpsInvalidacaoCallSitesTest.php, tests/Feature/V16/BonusDualPathRegressaoTest.php</files>
  <read_first>
    - app/Services/DesempenhoScoreService.php (notasPorAtribuicao ~485, notasLegado ~567)
    - app/Http/Controllers/PerformanceController.php (notasNpsDoUsuarioPorResposta ramos A ~602 e B ~637)
    - tests/Feature/V16/BonusDualPathRegressaoTest.php (cenários existentes; estender sem quebrar)
    - .planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md (Padrão 3, call-sites #1-#4)
  </read_first>
  <behavior>
    - Teste: com 1 resposta v15 invalidada, `DesempenhoScoreService::compute($user, $mes)` produz o MESMO resultado que teria sem aquela resposta (ramo atribuições, #1) — a nota invalidada não entra na média/bônus.
    - Teste: com 1 resposta LEGADA invalidada, o ramo `notasLegado()` (#2) não a conta.
    - Teste: PerformanceController — os widgets de carteira (ramo A #3) e o ramo legado (#4) não retornam a resposta invalidada.
    - Teste (V16): "resposta invalidada não conta em nenhum dos dois ramos (A/B)" — cenário novo em BonusDualPathRegressaoTest.
    - Regressão: respostas VÁLIDAS continuam contando exatamente como antes (baseline V16 verde).
  </behavior>
  <action>
    Aplicar as mudanças #1-#4 do checklist. #1 e #3 (JOIN): adicionar `->whereNull('r.invalidated_at')` na query (usar o alias real do JOIN lido no código). #2 e #4 (eager-load): trocar `->with('response')` / `->with(['response.answers','response.survey'])` por closure `fn($q) => $q->valida()...` preservando os nested loads. Escrever a suíte NpsInvalidacaoCallSitesTest com 1 asserção de exclusão por call-site #1-#4 e estender BonusDualPathRegressaoTest com o cenário dual-ramo. Não alterar a assinatura pública de `compute()`/`notasNpsDoUsuarioPorResposta()`.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsInvalidacaoCallSitesTest && php artisan test --filter=V16 && php artisan test --filter=Desempenho</automated>
  </verify>
  <acceptance_criteria>
    - NpsInvalidacaoCallSitesTest verde para os call-sites #1-#4.
    - Suítes V16 e Desempenho 100% verdes (regressão do bônus provada — nenhuma resposta válida perdida).
    - `grep -c "invalidated_at\|->valida()" app/Services/DesempenhoScoreService.php` >= 2 e idem em PerformanceController.php.
  </acceptance_criteria>
  <done>Bônus e widgets de carteira excluem a resposta invalidada nos dois ramos; regressão do bônus verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filtro nos call-sites de dashboards e metas (DashboardController ×3 + PortfolioController + CalculateGoalResults)</name>
  <files>app/Http/Controllers/DashboardController.php, app/Http/Controllers/PortfolioController.php, app/Jobs/CalculateGoalResults.php, tests/Feature/Phase96/NpsInvalidacaoCallSitesTest.php</files>
  <read_first>
    - app/Http/Controllers/DashboardController.php (home ~540, userDashboard ~1058, buildRanking ~1583, avgNotaDimensao ~1139)
    - app/Http/Controllers/PortfolioController.php ("Histórico NPS mensal" ~1600 — implementação própria)
    - app/Jobs/CalculateGoalResults.php (computeNps ~210)
    - .planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md (Padrão 3, call-sites #5-#9)
  </read_first>
  <behavior>
    - Teste: DashboardController home() (#5), userDashboard() (#6) e buildRanking()/$npsHistory (#7) não contam a resposta invalidada nos widgets NPS.
    - Teste: PortfolioController "Histórico NPS mensal" (#8) não mostra a resposta invalidada.
    - Teste: CalculateGoalResults::computeNps() (#9) não conta a resposta invalidada no resultado da meta NPS.
    - Regressão: respostas válidas contam normalmente em todos.
  </behavior>
  <action>
    Aplicar #5-#9 do checklist. #5/#6/#7/#8 (eager-load): trocar `->with('response')` / `->with(['response.answers','response.survey'])` por closure `fn($q) => $q->valida()...` preservando nested loads; `avgNotaDimensao()` (#7) não precisa mudar (já filtra nulos) — só o eager-load de `response`. #9: adicionar `->whereNull('invalidated_at')` direto na query `NpsResponse::query()` de `computeNps()`. Estender NpsInvalidacaoCallSitesTest com 1 asserção de exclusão por call-site #5-#9. Confirmar que PortfolioController é fix SEPARADO do PerformanceController (não presumir compartilhamento).
  </action>
  <verify>
    <automated>php artisan test --filter=NpsInvalidacaoCallSitesTest && php artisan test --filter=Nps</automated>
  </verify>
  <acceptance_criteria>
    - NpsInvalidacaoCallSitesTest verde para TODOS os 9 call-sites (#1-#9) — a suíte é o checklist completo do RESEARCH.
    - `php artisan test --filter=Nps` verde (baseline preservado).
    - `grep -c "invalidated_at\|->valida()" app/Http/Controllers/DashboardController.php` >= 3 (3 pontos no arquivo).
    - Pelo menos 5 arquivos de produção tocados no total da fase de invalidação (DesempenhoScoreService, PerformanceController, DashboardController, PortfolioController, CalculateGoalResults) — sanidade do Pitfall 4.
  </acceptance_criteria>
  <done>Dashboards e metas NPS excluem a resposta invalidada; todos os 9 call-sites cobertos por teste de exclusão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nps_responses.invalidated_at → cada consumidor de agregação | Cada tela reimplementa sua leitura NPS; a exclusão precisa ser replicada em todos (não há camada única) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-11 | Tampering / Integrity | 1 call-site esquecido "meio-conta" a resposta invalidada | mitigate | Suíte NpsInvalidacaoCallSitesTest com 1 asserção de exclusão POR call-site (#1-#9) — checklist executável, não confiança |
| T-96-12 | Integrity (dado financeiro) | bônus corrompido por resposta suspeita vazada | mitigate | Filtro no ramo atribuições (#1) + legado (#2) do DesempenhoScoreService + regressão obrigatória V16/Desempenho no gate |
| T-96-13 | Availability / Correctness | filtro exclui resposta VÁLIDA por engano (falso positivo) | mitigate | Cenários de regressão asseguram que respostas sem invalidated_at continuam contando; V16 baseline deve permanecer verde |
| T-96-SC | Tampering | npm/composer installs | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `php artisan test --filter=NpsInvalidacaoCallSitesTest` verde (9 call-sites)
- `php artisan test --filter=V16` + `--filter=Desempenho` verdes (regressão do BÔNUS obrigatória)
- `php artisan test --filter=Nps` verde
</verification>

<success_criteria>
- Resposta invalidada some de todos os 8 call-sites de agregação
- Bônus não regride para respostas válidas
- Cada call-site tem teste de exclusão dedicado
</success_criteria>

<output>
Criar `.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-04-SUMMARY.md` ao concluir.
</output>
