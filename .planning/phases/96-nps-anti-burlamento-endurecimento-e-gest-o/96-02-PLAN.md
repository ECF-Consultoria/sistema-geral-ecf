---
phase: 96-nps-anti-burlamento-endurecimento-e-gest-o
plan: 02
type: execute
wave: 2
depends_on: [96-01]
files_modified:
  - app/Services/Nps/NpsSuspicionService.php
  - app/Http/Controllers/NpsController.php
  - routes/web.php
  - resources/js/Pages/Nps/Configuracao.jsx
  - tests/Feature/Phase96/NpsConfiguracaoIpsInternosTest.php
  - tests/Feature/Phase94/NpsSuspicionServiceTest.php
autonomous: true
requirements: [AB-96-2]
user_setup: []

must_haves:
  truths:
    - "Admin edita a lista de IPs/CIDRs internos pela tela NPS > Configuração, sem tocar em .env nem fazer deploy"
    - "Um IP cadastrado SÓ pela UI (ausente do .env) já dispara a Regra 1 de suspeita no NpsSuspicionService"
    - "O .env (ECF_INTERNAL_IPS/ECF_INTERNAL_CIDRS) continua valendo como fallback — a lista efetiva é a UNIÃO (.env ∪ UI)"
    - "A rota de edição é admin-only; não-admin recebe 403"
    - "Entradas em formato inválido de IP/CIDR são rejeitadas com mensagem pt-BR"
  artifacts:
    - path: "app/Services/Nps/NpsSuspicionService.php"
      provides: "isInternalIp() lê config .env ∪ Configuracao (UI) — assinatura pública evaluate() intacta"
      contains: "Configuracao"
    - path: "app/Http/Controllers/NpsController.php"
      provides: "atualizarIpsInternos() com validação de IP/CIDR"
      contains: "atualizarIpsInternos"
    - path: "resources/js/Pages/Nps/Configuracao.jsx"
      provides: "widget de IPs internos no modo 'list', molde DiaCobrancaWidget"
    - path: "tests/Feature/Phase96/NpsConfiguracaoIpsInternosTest.php"
      provides: "prova de persistência, validação e admin-only"
  key_links:
    - from: "resources/js/Pages/Nps/Configuracao.jsx"
      to: "/nps/configuracao/ips-internos (PATCH)"
      via: "useForm().patch(route('nps.configuracao.ips-internos.update'))"
      pattern: "ips-internos"
    - from: "app/Services/Nps/NpsSuspicionService.php"
      to: "App\\Models\\Configuracao::get('nps_internal_ips'/'nps_internal_cidrs')"
      via: "array_merge dentro de isInternalIp()"
      pattern: "nps_internal_ips"
---

<objective>
Tornar a lista de IPs/CIDRs internos da ECF editável pela UI (tela NPS > Configuração), persistida via `App\Models\Configuracao` (padrão key/valor já usado por `nps_dia_cobranca`), com o `.env` como fallback. `NpsSuspicionService::isInternalIp()` passa a ler a UNIÃO (.env ∪ UI) sem alterar a assinatura pública `evaluate()`.

Purpose: Permitir ajuste operacional dos IPs internos sem deploy/acesso ao servidor — resolve a pendência deixada na Fase 94.
Output: Extensão do service, endpoint PATCH admin-only + rota, widget no Configuracao.jsx, testes (novo Phase96 + extensão do NpsSuspicionServiceTest da Fase 94).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md

<interfaces>
<!-- Contratos que o executor usa direto. -->

app/Services/Nps/NpsSuspicionService.php (linhas 27, 59-64):
```php
public function evaluate(?string $ip, int $durationSeconds, bool $isAuthenticatedSession): array // ASSINATURA PÚBLICA — NÃO MUDAR
private function isInternalIp(string $ip): bool
{
    $ranges = array_merge(
        config('nps.anti_burlamento.internal_ips', []),
        config('nps.anti_burlamento.internal_cidrs', []),
        // <<< AB-96-2: somar (∪) as chaves da UI aqui >>>
    );
    // ... IpUtils::checkIp($ip, $ranges)
}
```

App\Models\Configuracao — API key/valor (mesma usada por nps_dia_cobranca/nps_textos):
```php
Configuracao::get(string $chave, $default = null): ?string  // devolve string bruta
Configuracao::set(string $chave, $valor): void
```
Novas chaves: `nps_internal_ips` e `nps_internal_cidrs`, cada uma um JSON array de strings.

Molde do endpoint PATCH (irmão de atualizarDiaCobranca ~linha 1293) e da rota:
- Rota no grupo `role:admin` (routes/web.php, junto de nps.configuracao.*), ANTES da rota pública `/nps/{token}`.
- Nome sugerido: `nps.configuracao.ips-internos.update` → `PATCH /nps/configuracao/ips-internos`.

Molde do widget frontend: DiaCobrancaWidget (resources/js/Pages/Nps/Configuracao.jsx:50-106) — `useForm`, `patch(route(...))`, renderizado no bloco `mode === 'list'` (linha ~269, ao lado de `<DiaCobrancaWidget diaAtual={dia_cobranca} />`). A prop com os valores atuais vem do controller que serve `/nps/configuracao` (NpsTemplateController::index) — passar `ips_internos` / `cidrs_internos` como props.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Endpoint PATCH admin-only + rota + leitura efetiva no NpsSuspicionService</name>
  <files>app/Http/Controllers/NpsController.php, routes/web.php, app/Services/Nps/NpsSuspicionService.php, tests/Feature/Phase96/NpsConfiguracaoIpsInternosTest.php, tests/Feature/Phase94/NpsSuspicionServiceTest.php</files>
  <read_first>
    - app/Http/Controllers/NpsController.php (atualizarDiaCobranca ~linha 1293 como molde do PATCH+validação)
    - app/Services/Nps/NpsSuspicionService.php (isInternalIp linha 59 e evaluate linha 27)
    - config/nps.php (bloco anti_burlamento, linhas 12-24)
    - app/Models/Configuracao.php (contrato get/set)
    - tests/Feature/Phase94/NpsSuspicionServiceTest.php (cenários existentes a estender)
  </read_first>
  <behavior>
    - Teste (Phase96): admin faz PATCH com `ips=['203.0.113.5']`, `cidrs=['10.0.0.0/8']` → 200/redirect, `Configuracao::get('nps_internal_ips')` decodifica para o array salvo.
    - Teste: PATCH com IP inválido ('abc') ou CIDR mal-formado ('10.0.0.0') → 422/erro de validação, nada persistido.
    - Teste: não-admin faz PATCH → 403.
    - Teste (Phase94 estendido): IP presente SÓ na Configuracao (UI), ausente do .env, faz `evaluate()` retornar suspeita por IP interno (Regra 1) — provando a UNIÃO.
  </behavior>
  <action>
    Criar `atualizarIpsInternos(Request $request)` em NpsController: `abort_unless($request->user()->isAdmin(), 403)`; validar `ips` (nullable array; cada item `filter_var($v, FILTER_VALIDATE_IP)`) e `cidrs` (nullable array; cada item regex CIDR `^\d{1,3}(\.\d{1,3}){3}\/\d{1,2}$`) com mensagens pt-BR; persistir via `Configuracao::set('nps_internal_ips', json_encode(...))` e `Configuracao::set('nps_internal_cidrs', json_encode(...))`; `return back()->with('success', 'IPs internos atualizados.')`. Registrar a rota PATCH `nps.configuracao.ips-internos.update` no MESMO grupo `role:admin` de `nps.configuracao.dia-cobranca.update`, ANTES da rota pública `/nps/{token}`. Estender `NpsSuspicionService::isInternalIp()` para somar (∪) ao `array_merge` as duas chaves da UI: `json_decode(Configuracao::get('nps_internal_ips', '[]'), true) ?: []` e idem para cidrs — SEM tocar em `evaluate()`. Escrever os cenários de teste em ambos os arquivos.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsConfiguracaoIpsInternosTest && php artisan test --filter=NpsSuspicionServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - NpsConfiguracaoIpsInternosTest verde (persistência + validação + 403 não-admin).
    - NpsSuspicionServiceTest verde com o novo cenário de IP vindo só da UI.
    - `grep -vn '^\s*//' app/Services/Nps/NpsSuspicionService.php | grep -c "nps_internal_ips"` >= 1 (leitura da chave presente fora de comentário).
    - Assinatura de `evaluate(` inalterada (mesmos parâmetros).
  </acceptance_criteria>
  <done>Endpoint admin-only persiste e valida IPs/CIDRs; service lê a união .env ∪ UI; suíte Nps segue verde.</done>
</task>

<task type="auto">
  <name>Task 2: Widget de IPs internos no Configuracao.jsx + props do controller</name>
  <files>resources/js/Pages/Nps/Configuracao.jsx, app/Http/Controllers/NpsController.php</files>
  <read_first>
    - resources/js/Pages/Nps/Configuracao.jsx (DiaCobrancaWidget linhas 50-106 e o render do modo 'list' ~linha 269)
    - app/Http/Controllers/NpsTemplateController.php (index — método que renderiza /nps/configuracao e passa dia_cobranca; adicionar as novas props no mesmo Inertia::render)
  </read_first>
  <action>
    Criar um `IpsInternosWidget` no molde do `DiaCobrancaWidget`: `useForm({ ips: [...], cidrs: [...] })`, lista editável de chips (adicionar/remover entrada), botão salvar via `patch(route('nps.configuracao.ips-internos.update'))`, exibição de `errors` de validação. Renderizá-lo no bloco `mode === 'list'`, ao lado do `<DiaCobrancaWidget />`. Passar as props `ips_internos` e `cidrs_internos` (decodificadas de `Configuracao::get(...)`) no `Inertia::render` de `/nps/configuracao` (NpsTemplateController::index) para popular o estado inicial do widget. Labels/textos em pt-BR claro, sem jargão. IMPORTANTE (pitfall Rollup): computar quaisquer flags booleanas DENTRO do callback do `.map()` dos chips — não referenciar variável de escopo externo do componente dentro do map (evita ReferenceError no bundle).
  </action>
  <verify>
    <automated>npx vite build 2>&1 | grep -qiv "error" && echo "build-ok"</automated>
  </verify>
  <acceptance_criteria>
    - Widget renderizado no modo 'list' com estado inicial vindo das props `ips_internos`/`cidrs_internos`.
    - `route('nps.configuracao.ips-internos.update')` referenciado no JSX.
    - `npm run build` (ou `npx vite build`) conclui com exit 0, sem ReferenceError de escopo.
  </acceptance_criteria>
  <done>Admin gerencia IPs/CIDRs internos pela tela de Configuração no dark theme; salvar persiste via o endpoint da Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador admin → PATCH /nps/configuracao/ips-internos | Entrada de strings (IP/CIDR) que alimentam a regra de detecção — precisa de authz + validação |
| Configuracao (persistido) → NpsSuspicionService | Config lida em runtime na avaliação de cada submit NPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04 | Elevation of Privilege | atualizarIpsInternos() | mitigate | `abort_unless($request->user()->isAdmin(), 403)` no controller + middleware `role:admin` no grupo da rota (defesa em profundidade) |
| T-96-05 | Tampering / Input Validation | validação do PATCH | mitigate | `filter_var(FILTER_VALIDATE_IP)` para IPs e regex CIDR estrita — nunca persistir string livre não validada em `Configuracao` |
| T-96-06 | Tampering | rota PATCH | mitigate | CSRF padrão do grupo `web` (o projeto só desabilita CSRF em /implementacao/*) — a rota admin fica protegida |
| T-96-SC | Tampering | npm/composer installs | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `php artisan test --filter=NpsConfiguracaoIpsInternosTest` verde
- `php artisan test --filter=Nps` verde (sem regressão da suíte de suspeita)
- `npm run build` exit 0
</verification>

<success_criteria>
- Admin gerencia IPs internos pela UI; .env vira fallback
- IP cadastrado só pela UI dispara a Regra 1
- Não-admin bloqueado (403); formato inválido rejeitado
</success_criteria>

<output>
Criar `.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-02-SUMMARY.md` ao concluir.
</output>
