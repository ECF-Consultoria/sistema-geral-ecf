---
phase: 96-nps-anti-burlamento-endurecimento-e-gest-o
plan: 05
type: execute
wave: 5
depends_on: [96-01, 96-02, 96-03, 96-04]
files_modified: []
autonomous: false
requirements: [AB-96-1, AB-96-2, AB-96-3]
user_setup: []

must_haves:
  truths:
    - "As suítes Nps + V16 + Desempenho estão 100% verdes juntas (regressão do bônus obrigatória) antes do sign-off"
    - "O bundle frontend compila (npm run build exit 0) com os 3 arquivos de UI da fase (Blocked.jsx, Configuracao.jsx, Index.jsx)"
    - "Um humano confirma visualmente, no dark theme: mensagem de bloqueio amigável, tela de IPs internos e ação de invalidar/revalidar"
  artifacts: []
  key_links: []
---

<objective>
Gate final da Fase 96: rodar o baseline de regressão completo (Nps + V16 + Desempenho — o bônus NÃO pode ter regredido e a resposta invalidada precisa sumir de cada call-site), garantir o build do frontend e submeter as 3 telas ao checkpoint visual humano no dark theme. Sem tocar em nenhum arquivo de produção — este plano é só verificação e sign-off.

Purpose: Provar que as três frentes convivem sem regressão e que a UX das partes visíveis está correta antes de encerrar a fase.
Output: Confirmação automatizada das suítes + build + aprovação humana do checkpoint visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de regressão completo + build frontend</name>
  <files>(nenhum — só verificação)</files>
  <read_first>
    - .planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-VALIDATION.md (Sampling Rate + Phase gate)
  </read_first>
  <action>
    Rodar as suítes de regressão OBRIGATÓRIAS juntas e o build. A suíte V16/Desempenho é obrigatória (não opcional) porque a invalidação mexe na fonte direta do bônus. Se qualquer suíte falhar, PARAR e reportar o call-site/teste vermelho (não prosseguir para o checkpoint). Rodar `npm run build` (não apenas `vite build`) para reproduzir o comando de convenção do projeto.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase96 && php artisan test --filter=Nps && php artisan test --filter=V16 && php artisan test --filter=Desempenho && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Phase96, Nps, V16 e Desempenho todas verdes na mesma execução.
    - `npm run build` retorna exit 0, sem ReferenceError de escopo (pitfall Rollup).
  </acceptance_criteria>
  <done>Baseline de regressão do bônus e do módulo NPS verde; bundle compila.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual das 3 telas no dark theme</name>
  <action>
    Pausar para verificação humana APÓS toda a automação estar concluída (não substitui nenhuma automação). Apresentar as 3 telas conforme o roteiro em <how-to-verify> e aguardar aprovação.
  </action>
  <what-built>
    Fase 96 completa e automatizada: (1) bloqueio de submit para sessão interna com página amigável; (2) tela de configuração de IPs/CIDRs internos pela UI; (3) ação admin de invalidar/revalidar resposta suspeita na listagem de /nps, com efeito no NPS médio e no bônus.
  </what-built>
  <how-to-verify>
    1. BLOQUEIO (AB-96-1): logado como usuário interno, abra um link NPS (`/nps/{token}`) e tente responder. Confirme que aparece a página "Não foi possível registrar sua resposta" (dark theme, ícone escudo) — mensagem clara, sem erro cru e sem citar IP/login/sessão.
    2. IPs (AB-96-2): como admin, vá em NPS > Configuração. Confirme o widget de IPs internos; adicione um IP/CIDR, salve, recarregue e veja que persistiu. Tente salvar um valor inválido e confirme a mensagem de erro em pt-BR.
    3. INVALIDAÇÃO (AB-96-3): como admin, abra o modal de uma resposta na lista de /nps, clique em "Invalidar resposta", opcionalmente informe um motivo. Confirme: a resposta ganha marca de invalidada na lista (continua visível para gestão), e o NPS médio/bônus do profissional recalcula desconsiderando-a. Depois clique em "Revalidar" e confirme que volta a contar.
    4. Confirme que tudo respeita o dark theme e os tokens ecf-* (sem quebra visual).
  </how-to-verify>
  <verify>
    <human-check>Roteiro de <how-to-verify> executado e aprovado pelo usuário.</human-check>
  </verify>
  <done>As 3 telas aprovadas visualmente pelo usuário no dark theme.</done>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Plano de verificação/sign-off — não introduz superfície nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-14 | Integrity | regressão silenciosa do bônus entre planos | mitigate | Gate obrigatório roda Nps + V16 + Desempenho juntas antes do sign-off — bloqueia encerramento se o bônus regrediu |
| T-96-SC | Tampering | npm/composer installs | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- Suítes Phase96 + Nps + V16 + Desempenho verdes na mesma execução
- `npm run build` exit 0
- Checkpoint visual humano aprovado
</verification>

<success_criteria>
- Regressão do bônus provada verde no gate final
- As 3 telas validadas visualmente no dark theme
- Fase pronta para /gsd:verify-work
</success_criteria>

<output>
Criar `.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-05-SUMMARY.md` ao concluir.
</output>
