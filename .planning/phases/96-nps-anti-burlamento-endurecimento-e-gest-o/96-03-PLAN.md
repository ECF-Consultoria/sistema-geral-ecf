---
phase: 96-nps-anti-burlamento-endurecimento-e-gest-o
plan: 03
type: execute
wave: 3
depends_on: [96-01, 96-02]
files_modified:
  - database/migrations/2026_07_17_090002_add_invalidation_to_nps_responses_table.php
  - app/Models/NpsResponse.php
  - app/Http/Controllers/NpsController.php
  - routes/web.php
  - resources/js/Pages/Nps/Index.jsx
  - tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php
autonomous: true
requirements: [AB-96-3]
user_setup: []

must_haves:
  truths:
    - "Admin invalida uma resposta suspeita pela listagem/modal de /nps, com motivo textual livre opcional"
    - "A invalidação grava invalidated_at/invalidated_by em nps_responses SEM apagar nenhum snapshot (nps_response_scores/nps_score_assignments intactos)"
    - "A ação é auditada via activity()->causedBy()->log() explícito (quem/quando/motivo), sem poluir a auditoria com respostas legítimas"
    - "A invalidação de uma resposta de mês fechado faz Cache::forget() das chaves de bônus dos user_ids afetados"
    - "A revalidação (invalidated_at=null) restaura o efeito e também busta o cache"
    - "Os cards/série 12m do próprio /nps deixam de contar a resposta invalidada, mas a LISTAGEM PAGINADA continua mostrando-a (admin precisa gerir/revalidar)"
    - "A invalidação NÃO reverte NpsSurvey.status para pending (evita ambiguidade no hasOne)"
    - "As ações de invalidar/revalidar são admin-only (403 para não-admin)"
  artifacts:
    - path: "database/migrations/2026_07_17_090002_add_invalidation_to_nps_responses_table.php"
      provides: "colunas nullable invalidated_at + invalidated_by (FK users nullOnDelete)"
      contains: "invalidated_at"
    - path: "app/Models/NpsResponse.php"
      provides: "fillable invalidated_at/invalidated_by + scopeValida()"
      contains: "scopeValida"
    - path: "app/Http/Controllers/NpsController.php"
      provides: "invalidarResposta()/revalidarResposta() + cache-busting + activity log; filtro em index() cards/série"
      contains: "invalidarResposta"
    - path: "tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php"
      provides: "prova de flag, activity log, cache-busting, cards/série filtrados, listagem preservada, revalidação"
  key_links:
    - from: "resources/js/Pages/Nps/Index.jsx"
      to: "/nps/{survey}/response/invalidar (PATCH)"
      via: "botão Invalidar/Revalidar no DialogFooter do modal"
      pattern: "invalidar"
    - from: "app/Http/Controllers/NpsController.php invalidarResposta()"
      to: "Cache::forget('desempenho.compute.v4.{userId}.{Y-m}')"
      via: "NpsScoreAssignment::where('nps_response_id', ...)->pluck('user_id')"
      pattern: "Cache::forget"
    - from: "app/Models/NpsResponse.php scopeValida()"
      to: "whereNull('invalidated_at')"
      via: "scope Eloquent reutilizável pelos call-sites (Plan 96-04)"
      pattern: "whereNull\\('invalidated_at'\\)"
---

<objective>
Estabelecer o mecanismo de invalidação de resposta NPS: coluna-flag em `nps_responses`, scope Eloquent reutilizável (`scopeValida`), ações admin-only `invalidarResposta`/`revalidarResposta` com trilha de auditoria e cache-busting do bônus, e a UI de invalidar no modal de `/nps`. Este plano fecha os call-sites que vivem DENTRO do NpsController (cards/série de `index()`) e entrega o contrato (`scopeValida` + migration) que o Plano 96-04 aplica nos 8 call-sites de agregação externos.

Purpose: Dar ao admin uma correção pós-fato reversível que respeita o congelamento dos snapshots da Fase 79 e não deixa a resposta suspeita contaminar dashboards/bônus.
Output: Migration, scope+fillable no model, 2 métodos de controller + rotas + cache-busting + activitylog, filtro nos cards/série do index(), UI no Index.jsx, suíte de testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md
@.planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-01-SUMMARY.md

<interfaces>
<!-- Contratos que o executor usa direto. -->

Cache key do bônus (CONFIRMADO no código, DesempenhoScoreService.php:191):
`desempenho.compute.v4.{user_id}.{Y-m}`  — RELER a versão (v4) no momento da execução; o projeto já bumpou v1→v4. Não hardcodar cego se tiver mudado.

Fonte dos user_ids afetados (só existe para respostas v15 com template):
`App\Models\NpsScoreAssignment::where('nps_response_id', $response->id)->pluck('user_id')->unique()`
Respostas LEGADAS (template_id=null) NÃO têm assignment → cache-busting não se aplica e isso é CORRETO (elas só afetam ramos recalculados live).

Padrão activity() explícito (molde: NotificacaoController.php:264-273):
`activity()->causedBy($request->user())->performedOn($survey->response)->withProperties([...])->log('Resposta NPS invalidada')`
NpsResponse NÃO deve receber o trait LogsActivity (poluiria com 'created' por resposta legítima).

Rotas existentes (routes/web.php, grupo role:admin, ANTES de /nps/{token}):
`DELETE /nps/{survey}/response` → nps.responses.destroy (excluirResposta linha 1397 — ATENÇÃO: essa ação REVERTE status para pending; a invalidação NÃO deve imitar isso).
Novas rotas sugeridas: `PATCH /nps/{survey}/response/invalidar` (nps.responses.invalidar) e `PATCH /nps/{survey}/response/revalidar` (nps.responses.revalidar).

NpsController::index() — call-sites de AGREGAÇÃO a filtrar (RESEARCH Padrão 3):
- `$responsesMes` (~linha 340) e o loop `$serieMeses` (~linha 370): `NpsResponse::query()->with([...])->whereHas('survey', ...)` → adicionar `->whereNull('invalidated_at')`.
- A paginação `$surveys = $baseQuery->paginate(...)` NÃO deve filtrar (admin vê a invalidada para revalidar).

NpsSurvey::response() é hasOne. NpsSurvey.completed_at → mês fechado via `->startOfMonth()`.

Modal em Index.jsx: DialogFooter (linhas ~1304/1331) e ConfiancaBadge/auditoria já presentes (Fase 95). O botão novo entra no DialogFooter do modal de detalhe (modalSurvey), ao lado de "Excluir resposta". `modalSurvey.auditoria` só existe para admin.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration invalidação + scopeValida()/fillable no NpsResponse + teste RED</name>
  <files>database/migrations/2026_07_17_090002_add_invalidation_to_nps_responses_table.php, app/Models/NpsResponse.php, tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php</files>
  <read_first>
    - app/Models/NpsResponse.php (fillable linha 29, casts linha 44 — adicionar as colunas)
    - .planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md (Code Example "Migration invalidação" + Padrão 3 scopeValida)
    - database/migrations/2026_07_16_*.php (Fase 94, padrão nullable + idempotência Schema::hasColumn)
  </read_first>
  <behavior>
    - Teste: `NpsResponse::factory()->create()` e depois `->update(['invalidated_at' => now()])` persiste; `NpsResponse::valida()->count()` exclui a invalidada.
    - Teste: `scopeValida` aplica `whereNull('invalidated_at')` (resposta limpa continua contando).
  </behavior>
  <action>
    Criar a migration idempotente (`Schema::hasColumn`): `invalidated_at` timestamp nullable `after('suspicion_reasons')`; `invalidated_by` foreignId nullable `after('invalidated_at')` `constrained('users')->nullOnDelete()` — `nullable()` ANTES do `nullOnDelete()` (evita erro 1830 no MariaDB, memória do projeto). `down()` idempotente com dropForeign+dropColumn. Em NpsResponse: adicionar `invalidated_at`/`invalidated_by` ao `$fillable`, cast `invalidated_at => 'datetime'`, e `public function scopeValida($query) { return $query->whereNull('invalidated_at'); }`. Criar o arquivo de teste com os cenários acima + os stubs (RED) das tasks seguintes.
  </action>
  <verify>
    <automated>php artisan migrate --env=testing && php artisan test --filter=NpsInvalidacaoRespostaTest</automated>
  </verify>
  <acceptance_criteria>
    - Migration roda em SQLite; `grep -c "scopeValida" app/Models/NpsResponse.php` >= 1.
    - Cenários de scope/flag verdes; migration reversível (`down()` idempotente).
  </acceptance_criteria>
  <done>Colunas de invalidação e scope reutilizável disponíveis; teste Phase96 de invalidação criado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: invalidarResposta/revalidarResposta (admin-only) + cache-busting + activitylog + rotas + filtro em index()</name>
  <files>app/Http/Controllers/NpsController.php, routes/web.php, tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php</files>
  <read_first>
    - app/Http/Controllers/NpsController.php (excluirResposta ~linha 1397 como molde de authz; index() cards/série ~linhas 340/370)
    - app/Services/DesempenhoScoreService.php (computeCached ~linha 191 — CONFIRMAR a versão da cache key)
    - app/Http/Controllers/NotificacaoController.php:264-273 (padrão activity() explícito)
    - .planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-RESEARCH.md (Trilha de invalidação + cache-busting + Pitfall 1/2/5)
  </read_first>
  <behavior>
    - Teste: admin invalida → invalidated_at/by preenchidos; existe 1 activity log 'Resposta NPS invalidada' com causer=admin e properties incluindo o motivo textual quando enviado.
    - Teste: invalidar resposta v15 de mês FECHADO → `Cache::forget` chamado para cada user_id de NpsScoreAssignment daquela resposta (asserir via Cache::has antes/depois OU spy do facade Cache).
    - Teste: invalidar resposta LEGADA (sem assignment) → não quebra e não precisa de forget (comportamento correto, Pitfall 5).
    - Teste: cards/série de index() (`$responsesMes`/série 12m) NÃO contam a invalidada; a listagem paginada AINDA a mostra.
    - Teste: NpsSurvey.status permanece 'completed' após invalidar (não vira pending).
    - Teste: revalidar restaura (invalidated_at=null) + activity 'Resposta NPS revalidada' + cache forget de novo.
    - Teste: não-admin → 403 em ambas as ações.
  </behavior>
  <action>
    Criar `invalidarResposta(Request $request, NpsSurvey $survey)`: `abort_unless($request->user()->isAdmin(), 403)`; validar `motivo` (nullable|string|max:500); guard-clauses (resposta inexistente / já invalidada → back()->with('error')); `$survey->response->update(['invalidated_at' => now(), 'invalidated_by' => $request->user()->id])`; NÃO tocar em `NpsSurvey.status`/`completed_at` (Pitfall 2); `activity()->causedBy()->performedOn($survey->response)->withProperties(['survey_id' => ..., 'company_id' => ..., 'motivo' => $validated['motivo'] ?? null])->log('Resposta NPS invalidada')`; cache-busting: para o mês de `$survey->completed_at->startOfMonth()`, pluck user_ids de `NpsScoreAssignment::where('nps_response_id', $response->id)` e `Cache::forget(sprintf('desempenho.compute.v4.%d.%s', $userId, $mes->format('Y-m')))` — RELER a versão da key no código. Criar `revalidarResposta` simétrico (invalidated_at=null, log 'revalidada', mesmo cache-busting). Registrar as 2 rotas PATCH no grupo role:admin ANTES de /nps/{token}. Em `index()`, adicionar `->whereNull('invalidated_at')` em `$responsesMes` e no loop da série 12m; NÃO tocar na paginação. Completar os cenários RED do teste.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsInvalidacaoRespostaTest</automated>
  </verify>
  <acceptance_criteria>
    - NpsInvalidacaoRespostaTest verde em todos os cenários (flag, activity, cache-busting v15 + legado, cards/série filtrados, paginação preservada, status intacto, revalidação, 403).
    - `grep -vn '^\s*//' app/Http/Controllers/NpsController.php | grep -c "Cache::forget"` >= 1.
    - `php artisan test --filter=Nps` verde (baseline preservado).
  </acceptance_criteria>
  <done>Ações admin de invalidar/revalidar com auditoria e cache-busting; cards/série de /nps deixam de contar a invalidada; listagem preserva a resposta para gestão.</done>
</task>

<task type="auto">
  <name>Task 3: UI de invalidar/revalidar no modal de Index.jsx (motivo textual + dark theme)</name>
  <files>resources/js/Pages/Nps/Index.jsx</files>
  <read_first>
    - resources/js/Pages/Nps/Index.jsx (DialogFooter ~1304/1331, modalSurvey, ConfiancaBadge ~1345, bloco auditoria ~1393)
    - .planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-01-SUMMARY.md (shape confianca/auditoria; guard = existência da chave)
  </read_first>
  <action>
    No modal de detalhe (modalSurvey), adicionar no DialogFooter um botão "Invalidar resposta" (quando `modalSurvey.auditoria` existe = admin e a resposta ainda é válida) e "Revalidar resposta" (quando já invalidada). O botão de invalidar abre um campo/textarea opcional de `motivo` (pt-BR, sem jargão) e dispara `router.patch(route('nps.responses.invalidar', survey), { motivo })`; revalidar dispara `route('nps.responses.revalidar', survey)`. Indicar visualmente no item/modal quando a resposta está invalidada (badge/tag discreta no dark theme). Guard SEMPRE pela EXISTÊNCIA de `modalSurvey.auditoria` (não-admin nunca recebe a chave — padrão da Fase 95). Pitfall Rollup: calcular flags (ex.: `estaInvalidada`) dentro do render do item/modal, não em variável de escopo referenciada dentro de `.map()`.
  </action>
  <verify>
    <automated>npx vite build 2>&1 | grep -qiv "error" && echo "build-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `route('nps.responses.invalidar'` e `route('nps.responses.revalidar'` referenciados no JSX.
    - Botões só aparecem quando `modalSurvey.auditoria` existe (guard por existência de chave).
    - `npm run build` exit 0 sem ReferenceError.
  </acceptance_criteria>
  <done>Admin invalida/revalida pela UI com motivo opcional; estado invalidado é visível; não-admin não vê a ação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador admin → PATCH invalidar/revalidar | Ação que altera dado financeiro (fonte do bônus) — exige authz forte + trilha |
| nps_responses.invalidated_at → 8 call-sites de agregação | A flag precisa ser respeitada por todos os leitores (Plano 96-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-07 | Elevation of Privilege | invalidarResposta/revalidarResposta | mitigate | `abort_unless($request->user()->isAdmin(), 403)` server-side + middleware `role:admin` no grupo da rota |
| T-96-08 | Repudiation / Tampering | admin invalida resposta legítima p/ inflar bônus | mitigate | `activity()->causedBy()->withProperties(motivo)` grava quem/quando/por quê — auditável a posteriori |
| T-96-09 | Tampering (dado stale) | cache do bônus (7 dias, mês fechado) | mitigate | `Cache::forget()` explícito por user_id afetado no momento da invalidação/revalidação (achado crítico do RESEARCH — sem ele o REQ quebra silenciosamente) |
| T-96-10 | Integrity | reversibilidade / congelamento Fase 79 | mitigate | Flag em vez de deleção — snapshots intactos; revalidar é `invalidated_at=null`, sem re-rodar NpsSnapshotService |
| T-96-SC | Tampering | npm/composer installs | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `php artisan test --filter=NpsInvalidacaoRespostaTest` verde
- `php artisan test --filter=Nps` verde (264 baseline)
- `npm run build` exit 0
</verification>

<success_criteria>
- Invalidar remove o efeito nos cards/série de /nps, com auditoria e cache-busting
- Listagem paginada preserva a resposta para gestão; revalidação restaura
- Status do survey intocado; ações admin-only
</success_criteria>

<output>
Criar `.planning/phases/96-nps-anti-burlamento-endurecimento-e-gest-o/96-03-SUMMARY.md` ao concluir.
</output>
