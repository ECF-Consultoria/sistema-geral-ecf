---
phase: 152-checklist-administrativo-trava-de-finaliza-o-v23-0
plan: 08
type: execute
wave: 7
depends_on: ["152-05", "152-06", "152-07"]
files_modified:
  - routes/web.php
  - app/Http/Controllers/ContratoAdminController.php
  - tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php
  - tests/Feature/Phase152/ChecklistEndpointsTest.php
autonomous: true
requirements: [ADMIN-01, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06]
user_setup: []

must_haves:
  truths:
    - "D-17: `admin.contratos.show` passa a aceitar `permission:admin.contratos,comercial.entrada` (OR nativo do `EnsurePermission`) — quem tem só `comercial.entrada` abre a ficha sem 403"
    - "D-17: a SEÇÃO Contrato do payload (grupo `contrato` do checklist, envelopes e signatários) só é enviada para quem tem `admin.contratos` — a permissão de rota abre a ficha, a permissão de módulo decide o que aparece nela"
    - "D-09: nenhuma chave de permissão nova é criada — só `admin.contratos` e `comercial.entrada`, ambas já liberáveis por setor sem deploy"
    - "D-08: a ficha é uma só, com as duas seções, e é a mesma rota aberta pelas duas listagens"
    - "ADMIN-04: o ator de toda marcação é `$request->user()` — nenhum endpoint aceita `user_id`/`feito_por` vindo do corpo"
    - "ADMIN-05: o payload traz `pode_finalizar` calculado no servidor pela mesma régua que recusa o POST — o botão nunca decide sozinho"
    - "D-04: `adman_register_url` chega ao payload a partir de `config('services.adman.register_url')`, nunca hard-coded no frontend"
    - "D-15: a camada HTTP é o ÚNICO lugar que dispara `ChecklistEtapaSincronizadorService::sincronizar()` — o controller injeta os três serviços do checklist como IRMÃOS, e nenhum deles depende do outro em ciclo de construtor"
    - "D-15: TODO caminho que fecha ou reabre item passa pelo mesmo funil privado do controller, que sempre sincroniza a etapa depois da mutação — os 3 endpoints de mutação delegam ao funil, o `show()` sincroniza ao carregar (é onde o envelope enviado pelo webhook é observado) e o FINALIZAR sincroniza ANTES de transicionar 4→5. Existe exatamente UMA chamada a `->sincronizar(` no controller inteiro"
  artifacts:
    - path: "routes/web.php"
      provides: "show com permissão em OR + 4 rotas de ação do checklist"
      contains: "permission:admin.contratos,comercial.entrada"
    - path: "app/Http/Controllers/ContratoAdminController.php"
      provides: "payload do checklist gated por módulo, funil único de mutação+sincronização e os 4 endpoints"
      contains: "sincronizarEtapaChecklist"
    - path: "tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php"
      provides: "D-17 — abre sem 403 e não vê a seção Contrato"
    - path: "tests/Feature/Phase152/ChecklistEndpointsTest.php"
      provides: "ADMIN-04/05/06 pela porta HTTP + prova de que todo caminho de mutação sincroniza a etapa (D-15)"
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Middleware/EnsurePermission.php"
      via: "permission:a,b — OR nativo já suportado"
      pattern: "permission:admin\\.contratos,comercial\\.entrada"
    - from: "app/Http/Controllers/ContratoAdminController.php"
      to: "app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php"
      via: "funil privado sincronizarEtapaChecklist(), chamado por show(), pelo funil de mutação e pelo finalizar"
      pattern: "->sincronizar\\("
    - from: "app/Http/Controllers/ContratoAdminController.php"
      to: "app/Services/ChecklistAdministrativo/FinalizarEntradaAdministrativaService.php"
      via: "podeFinalizar() no payload e finalizar() no POST"
      pattern: "podeFinalizar"
---

<objective>
Abrir a ficha única para as duas listagens (D-08/D-17), servir o checklist no payload de
`ContratoAdminController::show()` com a seção Contrato gated pela permissão de módulo, criar os
quatro endpoints de ação, e — o ponto novo — **ser a camada que orquestra a sincronização de etapa**
(D-15), já que os serviços do checklist não se conhecem em ciclo.

Purpose: é a camada HTTP que liga tudo o que os planos 152-05/06/07 construíram. Sem a mudança de
permissão da D-17, o "Abrir" da listagem Entrada devolve 403 e a D-08 vira letra morta. E como o
`ChecklistAdministrativoService` **não** recebe o sincronizador no construtor (senão o container
fecha ciclo e a ficha vira 500), é aqui que a D-15 é de fato disparada.
Output: rotas, métodos de controller e 2 arquivos de teste Feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-CONTEXT.md
@.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-PATTERNS.md
@.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-RESEARCH.md

**Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada`. No Bash, o PHP é
`/c/xampp/php/php.exe`. Nunca `php artisan test`.

<interfaces>
<!-- Já existentes — medidos no código. -->
app/Http/Middleware/EnsurePermission.php:22-38
  handle(Request $request, Closure $next, string ...$keys)  // aceita N chaves em OR, nativamente
  // docblock literal: "Permite acesso à rota se o user tem QUALQUER uma das permission keys"
  // registrado como alias 'permission' em bootstrap/app.php:50

app/Support/Permissions.php
  const ADMIN_CONTRATOS   = 'admin.contratos';     // linha 75
  const COMERCIAL_ENTRADA = 'comercial.entrada';   // linha 99

app/Models/User::hasPermission(string $key): bool   // linha 193

routes/web.php:1443  (ESTADO ATUAL — a rota show está DENTRO do grupo)
  Route::middleware(['auth','verified','permission:admin.contratos'])
      ->prefix('administrativo/contratos')->name('admin.contratos.')->group(function () {
          Route::get('/', ...)->name('index');
          Route::get('/empresa/{company}', [ContratoAdminController::class,'show'])->name('show');
          Route::patch('/empresa/{company}/cadastro', ...)->name('cadastro');
          Route::post('/empresa/{company}/gerar', ...)->name('gerar');
          Route::post('/{contratoAssinatura}/signatarios/{signatario}/reenviar', ...)->name('reenviar');
          Route::post('/{contratoAssinatura}/cancelamento', ...)->name('cancelamento');
          Route::post('/{contratoAssinatura}/refazer', ...)->name('refazer');
          Route::post('/liberacao-manual', ...)->name('liberacao-manual');
      });

app/Http/Controllers/ContratoAdminController::show()  (assinatura atual, linha 507)
  show(Company $company, ContratoDadosMinimosService $dados, GatilhoContratoAdministrativoService $gatilho,
       ContratoClicksignService $clicksign, ContratosPresosService $presos,
       CongelamentoEmissaoService $congelamento, ContratoPdfService $pdfDados): \Inertia\Response
  // Inertia::render('Admin/ContratoDetalhe', [...]) na linha 583, com as chaves:
  //   company, contratos_servico, contratos, faltantes, pode_gerar_contrato, motivo_bloqueio, ...

app/Http/Controllers/ContratoAdminController::liberarManual()  (linhas 1201-1243)
  // o molde de endpoint de ação: $request->validate() com catálogo fechado,
  // $request->user() como ator, return back()->with('success', ...)

<!-- Criados nos planos 152-05/06/07. Os TRÊS são injetados aqui como IRMÃOS. -->
ChecklistAdministrativoService                    // deps: 4 resolvers + OnboardingLinkService
  paraEmpresa(Company $company): array            // 1 argumento — NÃO recebe ator
  concluirManualmente(Company, string $chave, User, bool $forcar = false)
  reabrirItem(Company, string $chave, User)
  gerarConexaoEcf(Company, User): OnboardingLink
FinalizarEntradaAdministrativaService             // deps: ChecklistAdministrativoService + EtapaTransicaoService
  podeFinalizar(Company): array                   // ['permitido','requisito_faltante']
  finalizar(Company, User): array                 // ['status','requisito_faltante','marketplace_destino','etapa']
ChecklistEtapaSincronizadorService                // deps: os dois acima + EtapaTransicaoService
  sincronizar(Company, User): array               // ['transicoes','etapa_final']
ChecklistAdministrativoDefinicao
  chaves(bool $exigeContrato): array
  item(string $chave): ?array                     // ['grupo' => 'contrato'|'entrada', ...]

⚠️ Grafo de injeção (DAG, sem ciclo — não inverter nenhuma seta):
  Sincronizador ──► ChecklistAdministrativoService
       │       └──► FinalizarEntradaAdministrativaService ──► ChecklistAdministrativoService
  Controller ──► os três, como irmãos
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rota da ficha em OR e as 4 rotas de ação (D-17)</name>
  <files>routes/web.php, tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php</files>
  <read_first>
    - `routes/web.php` linhas 1428-1480 (o grupo `admin.contratos.*` completo e o grupo `comercial.entrada.*` logo abaixo — os dois padrões de declaração e os comentários que explicam por que estão fora de `role:admin`)
    - `app/Http/Middleware/EnsurePermission.php` (arquivo inteiro, 45 linhas — o OR nativo; **nenhuma mudança de middleware é necessária**, só de string)
    - `tests/Feature/Phase151/ComercEntradaPermissaoRotaTest.php` (arquivo inteiro — o molde deste teste: `userComPermissaoViaSetor()` e a asserção por `Route::getRoutes()->getByName()->gatherMiddleware()`)
    - `app/Support/Permissions.php` linhas 70-105 e 180-205 (as duas constantes e o catálogo exposto na tela de setores — confirmar que **nenhuma chave nova** é acrescentada)
  </read_first>
  <action>
Em `routes/web.php`, **tirar a rota `show` do grupo** `permission:admin.contratos` e declará-la
separada, imediatamente antes do grupo, com
`Route::middleware(['auth', 'verified', 'permission:admin.contratos,comercial.entrada'])
->get('/administrativo/contratos/empresa/{company}', [ContratoAdminController::class, 'show'])
->name('admin.contratos.show')`. As demais rotas do grupo (`index`, `cadastro`, `gerar`, `reenviar`,
`cancelamento`, `refazer`, `liberacao-manual`) **permanecem** só com `permission:admin.contratos` —
só a `show` é compartilhada.

Comentário pt-BR acima da rota registrando D-17 e o porquê: `admin.contratos.show` é a ficha única
(D-08) aberta pelas duas listagens; sem o OR, quem tem só `comercial.entrada` toma 403 ao clicar
"Abrir" na listagem Entrada. `EnsurePermission` já aceita N chaves em OR nativamente — nenhuma chave
de permissão nova é criada (D-09). E a advertência: a permissão de **rota** abre a ficha; a permissão
de **módulo** decide o que aparece dentro dela (o gating do payload está na Task 2 e não pode ser
removido sem reabrir um vazamento de dado contratual).

Declarar as quatro rotas de ação num grupo novo, irmão, com
`permission:admin.contratos,comercial.entrada`, prefixo `administrativo/contratos` e nome
`admin.contratos.`:
- `POST /empresa/{company}/checklist/{chave}/concluir` → `admin.contratos.checklist.concluir`
- `POST /empresa/{company}/checklist/{chave}/reabrir` → `admin.contratos.checklist.reabrir`
- `POST /empresa/{company}/checklist/conexao-ecf` → `admin.contratos.checklist.conexao-ecf`
- `POST /empresa/{company}/finalizar-entrada` → `admin.contratos.finalizar-entrada`

Comentário pt-BR: a `{chave}` é sempre validada contra o catálogo fechado no controller e o
`{company}` vem por route-model-binding — **nenhum id de linha de checklist atravessa a fronteira**,
o que remove por construção a classe de IDOR que o RESEARCH apontou (V4/Security Domain). O recorte
por grupo de item (Contrato exige `admin.contratos`) é feito **dentro** do controller, porque a
mesma rota serve os dois grupos.

Criar `tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php` copiando o molde de
`ComercEntradaPermissaoRotaTest` (o helper `userComPermissaoViaSetor()` com `Setor`/`SetorPermissao`
e `$setor->membros()->attach(...)`). Casos:
1. asserção **por nome de rota**: `Route::getRoutes()->getByName('admin.contratos.show')->gatherMiddleware()`
   contém a string exata `'permission:'.Permissions::ADMIN_CONTRATOS.','.Permissions::COMERCIAL_ENTRADA`
   — atenção: `permission:a,b` é **uma** string com vírgula, não duas entradas na lista;
2. as mesmas asserções para as quatro rotas de ação;
3. usuário com **só** `comercial.entrada` (via setor) faz GET na ficha e recebe **200**, não 403;
4. usuário com **só** `admin.contratos` faz GET na ficha e recebe **200**;
5. usuário **sem** nenhuma das duas recebe **403**;
6. **não-regressão**: `Route::getRoutes()->getByName('admin.contratos.liberacao-manual')->gatherMiddleware()`
   continua com `permission:admin.contratos` **sem** a vírgula — o OR não vazou para o grupo inteiro;
   idem para `admin.contratos.gerar` e `admin.contratos.index`;
7. `Permissions` não ganhou chave nova: afirmar que a lista de chaves exposta na tela de setores
   contém `admin.contratos` e `comercial.entrada` e **nenhuma** chave começando com `checklist.`.
  </action>
  <acceptance_criteria>
    - `routes/web.php` contém a string literal `permission:admin.contratos,comercial.entrada`
    - A rota `admin.contratos.show` está declarada **fora** do grupo `permission:admin.contratos` (o grupo não contém mais a linha `->name('show')`)
    - `routes/web.php` contém os 4 nomes de rota `checklist.concluir`, `checklist.reabrir`, `checklist.conexao-ecf` e `finalizar-entrada`
    - `app/Support/Permissions.php` **não** foi modificado (`git diff --name-only -- app/Support/Permissions.php` retorna vazio)
    - `tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php` contém `gatherMiddleware()` e `assertStatus(403)`
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php --colors=never` sai com código 0
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase131 tests/Feature/Phase151 --colors=never` sai com código 0 (a mudança de rota não quebrou a permissão da Fase 131 nem a navegação da 138)
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php tests/Feature/Phase131 tests/Feature/Phase151 --colors=never &amp;&amp; grep -q 'permission:admin.contratos,comercial.entrada' routes/web.php &amp;&amp; test 0 -eq $(git diff --name-only -- app/Support/Permissions.php | wc -l) &amp;&amp; echo ROTAS_OK</automated>
  </verify>
  <done>A ficha abre para quem tem qualquer uma das duas permissões, as demais rotas do grupo continuam restritas, e nenhuma chave de permissão nova foi criada.</done>
</task>

<task type="auto">
  <name>Task 2: show() serve o checklist gated por módulo e é o ponto único de sincronização de etapa</name>
  <files>app/Http/Controllers/ContratoAdminController.php, tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php</files>
  <read_first>
    - `app/Http/Controllers/ContratoAdminController.php` linhas 507-600 (a assinatura de `show()`, os serviços já injetados, a montagem de `$contratosServicoAtivos`/`$contratos` e a chamada `Inertia::render('Admin/ContratoDetalhe', [...])` na linha 583)
    - `app/Http/Controllers/ContratoAdminController.php` linhas 600-700 (o restante do array de props, para saber exatamente quais chaves são de conteúdo contratual e quais não são)
    - `app/Support/Permissions.php` linhas 70-105 (as duas constantes)
    - `app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php` (plano 152-05 — `paraEmpresa(Company)`, de **um** argumento; ele não conhece o sincronizador)
    - `app/Services/ChecklistAdministrativo/ChecklistEtapaSincronizadorService.php` (plano 152-07 — `sincronizar(Company, User)`)
    - `app/Services/ChecklistAdministrativo/FinalizarEntradaAdministrativaService.php` (plano 152-06 — `podeFinalizar()`)
  </read_first>
  <action>
Alterar `ContratoAdminController::show()`:

1. Acrescentar `Request $request` como **primeiro** parâmetro e injetar os **três** serviços do
   checklist como irmãos, junto dos já existentes: `ChecklistAdministrativoService $checklist`,
   `FinalizarEntradaAdministrativaService $finalizar` e
   `ChecklistEtapaSincronizadorService $sincronizador`. O container resolve por type-hint; a ordem
   dos demais não muda.
   ⚠️ Comentário pt-BR obrigatório: os três são injetados **aqui**, lado a lado, exatamente porque
   nenhum deles pode injetar o outro em ciclo. `ChecklistAdministrativoService` **não** recebe o
   sincronizador no construtor — isso fecharia
   `ChecklistAdministrativoService → ChecklistEtapaSincronizadorService → ChecklistAdministrativoService`
   e o container lançaria `CircularDependencyException`, derrubando esta própria action com 500.
   A orquestração é responsabilidade do chamador, não dos serviços.
2. Criar o método privado
   `sincronizarEtapaChecklist(Request $request, Company $company): void`, que chama
   `$this->sincronizador->sincronizar($company->refresh(), $request->user())`.
   **Este método é o ÚNICO ponto do sistema que invoca `sincronizar()`.** Comentário pt-BR: existe
   como funil para que acrescentar um endpoint novo de checklist no futuro não deixe a D-15 morrer
   em silêncio — quem esquecer de sincronizar quebra o teste de etapa por rota, não produz um bug
   invisível. Como o controller recebe os serviços por injeção de método em `show()` mas os
   endpoints são actions separadas, resolver o sincronizador dentro do helper por
   `app(ChecklistEtapaSincronizadorService::class)` **ou** injetá-lo no construtor do controller —
   escolher uma das duas e usar a mesma forma em todos os métodos; não misturar.
3. Calcular `$podeVerContrato = $request->user()->hasPermission(Permissions::ADMIN_CONTRATOS);`.
4. Ordem das chamadas em `show()`: primeiro `$checklist->paraEmpresa($company)` (persiste o
   resultado dos resolvers), depois `$this->sincronizarEtapaChecklist($request, $company)`, e só
   então `$finalizar->podeFinalizar($company)`. A razão de `show()` sincronizar vai em comentário:
   o degrau 2→3 depende de um evento **externo** (o webhook do Clicksign gravando `enviado_em`), que
   não passa por nenhuma ação do checklist — o carregamento da ficha é o único momento em que o
   sistema observa esse fato.
5. Quando `$podeVerContrato` for `false`, **remover a chave `'contrato'`** de `grupos` no array do
   checklist antes de enviá-lo. O `progresso` continua sendo o da empresa inteira — comentar em
   pt-BR: é a régua do FINALIZAR, não uma métrica da seção visível; o usuário de Entrada vê a barra
   completa e o `requisito_faltante` textual, mas não vê os itens contratuais.
6. Quando `$podeVerContrato` for `false`, enviar as props **já existentes** de conteúdo contratual
   neutralizadas: `contratos => []`, `pode_gerar_contrato => false`, `motivo_bloqueio => null`.
   ⚠️ Esta é a parte que impede a mudança de rota da D-17 de virar vazamento: até hoje só quem tinha
   `admin.contratos` alcançava esta ficha, e o payload traz envelopes e **signatários**. Comentar
   citando a T-131-04-04 (o array achatado que já limita signatário a id/nome/papel/situacao) e
   registrando que o gating aqui é a defesa nova, não uma redundância.
   As props `company`, `contratos_servico` e `faltantes` **continuam** para os dois perfis: a
   listagem Entrada já exibe empresa, serviços e pendências de cadastro — não há exposição nova.
7. Acrescentar ao array do `Inertia::render`: `'checklist' => $checklistPayload`,
   `'pode_ver_contrato' => $podeVerContrato`,
   `'pode_finalizar' => $finalizar->podeFinalizar($company)` (o array
   `['permitido','requisito_faltante']` inteiro — o front usa os dois) e
   `'adman_register_url' => config('services.adman.register_url')` (D-04 — o link fixo chega do
   servidor, nunca hard-coded no JSX).

Não alterar nenhuma outra lógica de `show()` — em particular não tocar em `$temServicoDuplicado`,
`$congelamento->ativo()` nem `$idMaisAntigoPorServico`.

Acrescentar ao `ChecklistAcessoPorEntradaTest.php` (criado na Task 1) as asserções deste gating:
usuário só-`comercial.entrada` recebe payload com `pode_ver_contrato = false`, `contratos` vazio e
`checklist.grupos` **sem** a chave `contrato`; usuário com `admin.contratos` recebe
`pode_ver_contrato = true` e a chave `contrato` presente, com os envelopes preenchidos. Usar
`$response->assertInertia(fn ($page) => ...)` se o projeto já usar esse helper em outros testes;
caso contrário, inspecionar `$response->viewData('page')['props']`.

Acrescentar também um caso de **sincronização pela abertura da ficha** (D-15): empresa na etapa 2,
com serviço que exige contrato e envelope com `enviado_em` gravado direto no banco (nenhuma ação de
checklist envolvida); um GET na ficha deixa `companies.etapa` em `aguardando_assinatura`, verificado
por reconsulta ao banco. É a prova de que o evento externo é observado.
  </action>
  <acceptance_criteria>
    - `ContratoAdminController.php` contém `'checklist' =>`, `'pode_ver_contrato' =>` e `'pode_finalizar' =>` dentro do `Inertia::render('Admin/ContratoDetalhe'`
    - Contém `private function sincronizarEtapaChecklist(Request $request, Company $company): void`
    - Contém **exatamente uma** ocorrência de `->sincronizar(` no arquivo inteiro (`grep -c -- '->sincronizar(' app/Http/Controllers/ContratoAdminController.php` retorna `1`)
    - Contém `paraEmpresa($company)` com **um** argumento — não `paraEmpresa($company, $request->user())`
    - Contém `hasPermission(Permissions::ADMIN_CONTRATOS)`
    - Contém `'adman_register_url' => config('services.adman.register_url')`
    - A assinatura de `show()` começa por `Request $request`
    - `git diff --name-only -- app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php` retorna vazio — o service não é alterado por este plano
    - `ChecklistAcessoPorEntradaTest.php` contém asserção de que `contratos` vem vazio para o usuário só-`comercial.entrada` e o caso de sincronização pela abertura da ficha
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php tests/Feature/Phase131 --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase152/ChecklistAcessoPorEntradaTest.php tests/Feature/Phase131 --colors=never &amp;&amp; grep -q "'pode_ver_contrato'" app/Http/Controllers/ContratoAdminController.php &amp;&amp; grep -q 'adman_register_url' app/Http/Controllers/ContratoAdminController.php &amp;&amp; test 1 -eq $(grep -c -- '->sincronizar(' app/Http/Controllers/ContratoAdminController.php) &amp;&amp; test 0 -eq $(git diff --name-only -- app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php | wc -l) &amp;&amp; echo SHOW_OK</automated>
  </verify>
  <done>A ficha serve o checklist e a régua do FINALIZAR no payload, quem não tem `admin.contratos` não recebe os itens do grupo Contrato nem os envelopes, e abrir a ficha sincroniza a etapa por um funil único.</done>
</task>

<task type="auto">
  <name>Task 3: Os 4 endpoints por um funil único que sempre sincroniza a etapa</name>
  <files>app/Http/Controllers/ContratoAdminController.php, tests/Feature/Phase152/ChecklistEndpointsTest.php</files>
  <read_first>
    - `app/Http/Controllers/ContratoAdminController.php` linhas 1195-1250 (`liberarManual()` — o molde do endpoint de ação: validação com catálogo fechado, `$request->user()` como ator, `abort(422, ...)` quando o recurso não pertence à empresa do POST, `return back()->with('success', ...)`)
    - `app/Http/Controllers/ContratoAdminController.php` (o método privado `sincronizarEtapaChecklist()` criado na Task 2 — o funil que estas actions precisam usar)
    - `app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php` (as três mutações e as `DomainException` que elas lançam)
    - `app/Services/ChecklistAdministrativo/FinalizarEntradaAdministrativaService.php` (`finalizar()` e a forma do retorno)
    - `app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php` (`chaves()` e `item()` — a validação da `{chave}` de rota)
    - `app/Http/Middleware/HandleInertiaRequests.php` (as chaves de `flash` já compartilhadas — `success`, `error`, `aviso`; **não** inventar chave nova sem acrescentá-la aqui, armadilha já registrada em `project_nps_link_grupo_invisivel_e_flash_260820`)
  </read_first>
  <action>
Acrescentar a `ContratoAdminController` um **funil privado** e quatro métodos públicos.

**Guarda comum** — `guardaChecklist(Request $request, Company $company, ?string $chave): void`:
- Validar a `{chave}` de rota contra o catálogo fechado:
  `abort_unless(in_array($chave, ChecklistAdministrativoDefinicao::chaves(true), true), 404)`.
  A `{chave}` é segmento de URL, não corpo — `$request->validate()` não a alcança; a checagem
  explícita é obrigatória.
- Recortar por grupo (D-09): se `ChecklistAdministrativoDefinicao::item($chave)['grupo']` for
  `contrato`, exigir `$request->user()->hasPermission(Permissions::ADMIN_CONTRATOS)`, senão
  `abort(403, 'Você não tem permissão para agir nos itens de Contrato.')`. Comentar em pt-BR: a
  rota está em OR (D-17) porque a ficha é única; o recorte por grupo é o que preserva a D-09.

**Funil de mutação** —
`executarMutacaoChecklist(Request $request, Company $company, \Closure $mutacao, string $sucesso): RedirectResponse`:
- roda `$mutacao()` dentro de `try/catch (\DomainException $e)`, devolvendo
  `back()->with('error', $e->getMessage())` na exceção **sem** sincronizar (nada mudou);
- no caminho de sucesso, chama `$this->sincronizarEtapaChecklist($request, $company)` e devolve
  `back()->with('success', $sucesso)`.
- Comentário pt-BR obrigatório explicando por que o funil existe e não é cerimônia: **todo** caminho
  que fecha ou reabre item precisa sincronizar a etapa, senão a D-15 morre em silêncio — a empresa
  fica presa na etapa 1 e o FINALIZAR nunca habilita, sem erro nenhum na tela. Com o funil, o
  endpoint novo que alguém acrescentar amanhã ou passa por aqui (e sincroniza) ou fica de fora do
  padrão de forma visível na revisão. A garantia complementar é o teste: cada endpoint de mutação
  tem um caso que afirma o avanço da etapa.

Os quatro métodos públicos:

`concluirItemChecklist(Request $request, Company $company, string $chave)` — guarda + funil com a
mutação `fn () => $checklist->concluirManualmente($company, $chave, $request->user())` e mensagem
'Item marcado como concluído.'. **Nunca** encaminhar o parâmetro `$forcar` do service — ele não é
exposto por HTTP nesta fase.

`reabrirItemChecklist(Request $request, Company $company, string $chave)` — guarda + funil com
`reabrirItem()` e mensagem 'Item desmarcado.'. Passa pelo funil mesmo sabendo que o sincronizador só
avança: o valor é o caminho único, não o efeito.

`gerarConexaoEcfChecklist(Request $request, Company $company)` — sem `{chave}`; a guarda só valida a
permissão de módulo de Entrada (o item 8 é do grupo Entrada, então basta a permissão da rota).
Funil com `gerarConexaoEcf()` e mensagem 'Conexão com o sistema ECF gerada.'. Idempotente (D-14).

`finalizarEntradaAdministrativa(Request $request, Company $company)` — **sincroniza ANTES**:
chamar `$this->sincronizarEtapaChecklist($request, $company)` e só então
`$finalizar->finalizar($company, $request->user())`. A ordem é essencial e vai comentada: a empresa
pode estar na etapa 2 ou 3 quando o usuário clica, e a tabela `TRANSICOES_PERMITIDAS` só permite
chegar na 5 vindo da 4 — sincronizar primeiro é o que leva a empresa até a 4 pela régua da D-15,
em vez de o clique "pular" degraus sem histórico.
Traduzir o retorno: `status === 'finalizado'` → `back()->with('success', 'Entrada administrativa
finalizada. Empresa movida para Aguardando Distribuição.')`; `status === 'recusado'` →
`back()->with('error', $resultado['requisito_faltante'])`; `status === 'erro'` →
`Log::error('[Checklist] falha ao finalizar entrada administrativa', [...])` e
`back()->with('error', 'Não foi possível finalizar agora. Tente novamente.')`.

Usar **apenas** as chaves de flash já compartilhadas por `HandleInertiaRequests` (`success`,
`error`, `aviso`). Comentar em pt-BR: chave de flash nova não chega ao front sem uma linha nova
naquele middleware — armadilha já paga uma vez neste projeto.

Nenhum dos métodos lê `user_id`/`feito_por` do corpo. Comentar citando a disciplina T-150-02.

Criar `tests/Feature/Phase152/ChecklistEndpointsTest.php` com `RefreshDatabase` e o `setUp()`
blindado. Todos os casos pela porta HTTP e com asserção **por reconsulta ao banco**:
1. POST em `checklist.concluir` para `grupo_whatsapp_criado` numa empresa na etapa 1: 302 + item
   concluído + `feito_por` do usuário da sessão + **`companies.etapa` avançou para
   `administrativo_andamento`** (D-15 pela rota);
2. POST com um `user_id` **diferente** no corpo: a autoria gravada é a do usuário autenticado, o
   corpo é ignorado (ADMIN-04 e T-150-02);
3. POST em `checklist.concluir` para `contrato_enviado` (item automático): flash de erro,
   **nenhuma linha** criada e **etapa inalterada** (o funil não sincroniza no ramo de exceção);
4. POST com `{chave}` inexistente: **404**;
5. usuário só-`comercial.entrada` POSTando num item do grupo `contrato`: **403**, nada gravado (D-09);
6. POST em `checklist.reabrir` de item concluído: item volta a `aberto` com os dois campos de
   autoria nulos, e a etapa **não** retrocede;
7. POST em `checklist.conexao-ecf` duas vezes: uma única linha em `onboarding_links`; e, se esse for
   o primeiro item concluído da empresa, a etapa avançou para `administrativo_andamento` (D-15 pelo
   endpoint de geração);
8. POST em `finalizar-entrada` com item pendente: flash de erro e `companies.etapa` inalterada;
9. POST em `finalizar-entrada` com tudo pronto e empresa já na etapa 4: `companies.etapa` vira
   `aguardando_distribuicao` e `Company::count()` não muda;
10. **sincronização antes do finalizar** — empresa na **etapa 2** com todos os itens concluídos e
    contrato assinado: um único POST em `finalizar-entrada` termina com a empresa em
    `aguardando_distribuicao`, e o histórico em `company_etapa_transicoes` registra os degraus
    intermediários (nada de pulo sem linha). ⚠️ Isto **não contradiz** o caso 5 de
    `FinalizarTransicaoEtapaTest` (plano 152-06), que afirma `recusado` para a mesma situação: lá se
    testa `finalizar()` **sozinho**, no nível de service; aqui se testa a rota, que sincroniza antes.
    Escrever essa distinção em comentário no teste, para ninguém "corrigir" um dos dois;
11. **empresa trocada** — POST em `checklist.concluir` usando o `{company}` de uma empresa A: o item
    marcado pertence à empresa **da rota** e a outra empresa continua sem nenhuma linha de checklist;
12. **legado** — empresa com `etapa = null`: POST em `checklist.concluir` marca o item normalmente e
    a etapa continua `null`, sem linha em `company_etapa_transicoes`.
  </action>
  <acceptance_criteria>
    - `ContratoAdminController.php` contém os quatro métodos `concluirItemChecklist`, `reabrirItemChecklist`, `gerarConexaoEcfChecklist` e `finalizarEntradaAdministrativa`
    - Contém `private function executarMutacaoChecklist` e `private function guardaChecklist`
    - Os três endpoints de mutação chamam `executarMutacaoChecklist(` — `grep -c 'executarMutacaoChecklist(' ` retorna pelo menos `4` (1 definição + 3 chamadas)
    - `finalizarEntradaAdministrativa` chama `sincronizarEtapaChecklist(` antes de `->finalizar(`
    - Continua havendo **exatamente uma** ocorrência de `->sincronizar(` no arquivo inteiro
    - Contém `abort_unless(in_array($chave, ChecklistAdministrativoDefinicao::chaves(` e `abort(403`
    - Contém `catch (\DomainException` no funil
    - Nenhum método contém `$request->input('user_id')` nem `$request->input('feito_por')`
    - As chaves de flash usadas são apenas `success`, `error` e `aviso`
    - `tests/Feature/Phase152/ChecklistEndpointsTest.php` contém pelo menos 12 métodos `public function test_`
    - Contém `assertStatus(404)`, `assertStatus(403)` e `Company::ETAPA_ADMINISTRATIVO_ANDAMENTO`
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase152 tests/Unit/Phase152 --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase152 tests/Unit/Phase152 --colors=never &amp;&amp; grep -q 'public function finalizarEntradaAdministrativa' app/Http/Controllers/ContratoAdminController.php &amp;&amp; test 1 -eq $(grep -c -- '->sincronizar(' app/Http/Controllers/ContratoAdminController.php) &amp;&amp; test 4 -le $(grep -c 'executarMutacaoChecklist(' app/Http/Controllers/ContratoAdminController.php) &amp;&amp; test 0 -eq $(grep -c "input('user_id')" app/Http/Controllers/ContratoAdminController.php) &amp;&amp; echo ENDPOINTS_OK</automated>
  </verify>
  <done>Os quatro endpoints existem, validam a chave contra o catálogo fechado, recortam por grupo, usam o usuário da sessão como ator, e todo caminho de mutação sincroniza a etapa por um funil único — provado por um caso de teste de etapa em cada endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → rota `admin.contratos.show` | fronteira que a D-17 **alarga** de propósito: passa a admitir `comercial.entrada` |
| payload Inertia → navegador do usuário de Entrada | dado contratual (envelopes, signatários) atravessaria aqui se o gating de módulo faltasse |
| corpo/URL da requisição → mutação de checklist e de etapa | `chave`, `company` e ator atravessam aqui |
| controller → serviços do checklist | é a camada de orquestração; a direção da injeção entre os serviços é one-way e o controller é quem os junta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-08-00 | Denial of Service | ciclo de injeção derrubando `show()` com 500 | mitigate | O controller injeta os três serviços como **irmãos**; `ChecklistAdministrativoService` não é alterado (asserção: `git diff --name-only` daquele arquivo vazio) e `paraEmpresa()` continua de 1 argumento. O teste do plano 152-07 resolve os três pelo container e falha se o ciclo voltar |
| T-152-08-01 | Information Disclosure | escalada por rota: `comercial.entrada` passa a alcançar a ficha e enxergar envelopes e signatários | mitigate | `show()` neutraliza `contratos`, `pode_gerar_contrato` e `motivo_bloqueio` e remove `grupos.contrato` quando o usuário não tem `admin.contratos`; `pode_ver_contrato` permite ao JSX esconder o card. Caso de teste dedicado |
| T-152-08-02 | Elevation of Privilege | usuário de Entrada marcando item do grupo Contrato pela rota em OR | mitigate | `guardaChecklist()` compara o `grupo` do item no catálogo com `hasPermission(ADMIN_CONTRATOS)` e faz `abort(403)`; caso de teste 5 |
| T-152-08-03 | Tampering | IDOR — marcar item de checklist de outra empresa | mitigate | Removido **por construção**: a rota carrega `{company}` (route-model-binding) e `{chave}` (catálogo fechado); nenhum id de linha atravessa a fronteira. Caso de teste 11 confirma o isolamento |
| T-152-08-04 | Tampering | `chave` livre criando linha arbitrária | mitigate | `abort_unless(in_array(...), 404)` antes de qualquer chamada ao service, e o service ainda lança `DomainException` como segunda barreira |
| T-152-08-05 | Spoofing | autoria/ator forjados por `user_id` no corpo | mitigate | Todo endpoint usa `$request->user()`; asserção de código-fonte de que `input('user_id')` não aparece, e caso de teste 2 envia `user_id` diferente e verifica que é ignorado |
| T-152-08-06 | Elevation of Privilege | finalizar por POST direto com item pendente | mitigate | `finalizar()` reavalia `podeFinalizar()` no servidor (plano 152-06); caso de teste 8 confirma que a etapa não muda |
| T-152-08-07 | Tampering | CSRF nas rotas novas de mutação | mitigate | As quatro rotas ficam no grupo autenticado padrão, com CSRF ativo — **nunca** replicar a exceção de `/implementacao/*` |
| T-152-08-08 | Denial of Service | mensagem de erro perdida por chave de flash não compartilhada | mitigate | Apenas `success`/`error`/`aviso`, já presentes em `HandleInertiaRequests` |
| T-152-08-09 | Repudiation | caminho de mutação que fecha item sem sincronizar a etapa, deixando a empresa presa e o FINALIZAR morto **sem erro visível** | mitigate | Funil único `executarMutacaoChecklist()` que sempre sincroniza no ramo de sucesso; exatamente **uma** chamada a `->sincronizar(` no controller, dentro de `sincronizarEtapaChecklist()`; e um caso de teste de avanço de etapa para **cada** endpoint de mutação (casos 1, 6, 7) mais o `show()` (Task 2) e o finalizar (caso 10) |
| T-152-08-SC | Tampering | npm/composer installs | mitigate | Nenhum pacote instalado — `152-RESEARCH.md` §Package Legitimacy Audit: "não aplicável" |
</threat_model>

<verification>
1. `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase152 tests/Unit/Phase152 --colors=never` sai 0.
2. `/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase131 tests/Feature/Phase151 tests/Unit/Phase150 tests/Feature/Phase150 --colors=never` igual à `152-BASELINE-TESTES.md`.
3. `grep -c -- '->sincronizar(' app/Http/Controllers/ContratoAdminController.php` retorna exatamente `1`.
4. `git diff --name-only -- app/Services/ChecklistAdministrativo/ChecklistAdministrativoService.php` retorna vazio.
5. `git diff --name-only -- app/Support/Permissions.php` retorna vazio (D-09).
</verification>

<success_criteria>
- Ficha única acessível pelas duas permissões, com seção Contrato gated por módulo
- Quatro endpoints funcionando, com chave validada contra catálogo fechado e ator da sessão
- Todo caminho de mutação, mais o `show()` e o FINALIZAR, sincroniza a etapa por um funil único
- Grafo de injeção acíclico: os três serviços são irmãos no controller
- Nenhuma chave de permissão nova
- Suítes 131/137/138 sem regressão contra a baseline
</success_criteria>

<output>
Criar `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-08-SUMMARY.md` ao terminar.
</output>
