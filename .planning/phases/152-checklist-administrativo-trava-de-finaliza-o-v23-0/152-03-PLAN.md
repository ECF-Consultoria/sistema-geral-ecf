---
phase: 152-checklist-administrativo-trava-de-finaliza-o-v23-0
plan: 03
type: execute
wave: 2
depends_on: ["152-01"]
files_modified:
  - app/Contracts/ChecklistResolver.php
  - app/Services/ChecklistAdministrativo/ChecklistResolverResultado.php
  - app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php
  - tests/Unit/Phase152/ChecklistDefinicaoCatalogoTest.php
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04]
user_setup: []

must_haves:
  truths:
    - "D-01: o catálogo tem exatamente 9 itens — a lista do §5, não os 12 do §3; `acompanhar a assinatura`, `gerar mensagem de boas-vindas` e `inserir todos os links necessários` não existem como itens"
    - "D-03: os 9 itens são exatamente os da tabela do CONTEXT.md, cada um com grupo (`contrato`/`entrada`) e natureza (`manual`/`auto`) declarados no catálogo — itens 2, 3, 7 e 8 são `auto`; itens 1, 4, 5, 6 e 9 são `manual`"
    - "D-06: o item 1 (`contrato_revisado`) é declarado `manual` no catálogo — exceção explícita ao ADMIN-02"
    - "D-13: os quatro itens sem sinal observável (`grupo_whatsapp_criado`, `email_colaborador_criado`, `link_adman_entregue`, `boas_vindas_enviada`) são `manual`, e nenhum resolver automático é inventado para eles"
    - "D-07: `ChecklistAdministrativoDefinicao::itens(false)` devolve 6 itens (só o grupo Entrada) e `itens(true)` devolve 9 — o grupo Contrato NÃO EXISTE para serviço isento, em vez de existir marcado"
    - "D-02: nenhum item do catálogo tem estado ou flag `nao_aplicavel`"
    - "D-10: o resultado de resolver é um value object de 3 estados (`concluido | nao_coletado | indeterminado`), nunca `bool` — shape copiado de `OnboardingResolverResultado`"
  artifacts:
    - path: "app/Contracts/ChecklistResolver.php"
      provides: "Interface síncrona de resolver, com chave()/label()/ajuda()/resolver(Company)"
      contains: "interface ChecklistResolver"
    - path: "app/Services/ChecklistAdministrativo/ChecklistResolverResultado.php"
      provides: "Value object de 3 estados"
      contains: "const INDETERMINADO"
    - path: "app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php"
      provides: "Catálogo fechado dos 9 itens, com montagem condicional do grupo Contrato"
      contains: "contrato_revisado"
    - path: "tests/Unit/Phase152/ChecklistDefinicaoCatalogoTest.php"
      provides: "Prova de D-01 (9 itens), D-07 (6 para isento) e D-03 (natureza por item)"
  key_links:
    - from: "app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php"
      to: "app/Contracts/ChecklistResolver.php"
      via: "chave de resolver declarada em cada item auto"
      pattern: "'auto_fonte'"
---

<objective>
Escrever o contrato de resolver, o value object de 3 estados e o catálogo fechado dos 9 itens do
checklist administrativo — a lista do §5 (D-01), com grupo e natureza de cada item (D-03) e a
montagem condicional que faz o grupo Contrato **não existir** para serviço isento (D-07).

Purpose: é o vocabulário que todos os planos seguintes usam. Definido aqui uma vez, nenhum plano
posterior precisa reabrir "quais são os itens" nem "quais fecham sozinhos".
Output: 1 interface, 1 value object, 1 catálogo, 1 teste unitário. Nenhum comportamento observável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-CONTEXT.md
@.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-PATTERNS.md

**Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada`. No Bash, o PHP é
`/c/xampp/php/php.exe`.

<interfaces>
<!-- Contrato que este plano CRIA. Os planos 152-04 (resolvers) e 152-05 (service) consomem -->
<!-- exatamente estes nomes — não renomear. -->

App\Contracts\ChecklistResolver
  public function chave(): string;                    // bate com 'auto_fonte' do catálogo
  public function label(): string;
  public function ajuda(): string;
  public function resolver(Company $company): ChecklistResolverResultado;

App\Services\ChecklistAdministrativo\ChecklistResolverResultado  (final readonly)
  const CONCLUIDO = 'concluido'; const NAO_COLETADO = 'nao_coletado'; const INDETERMINADO = 'indeterminado';
  static concluido(array $valor = []): self
  static naoColetado(?string $motivo = null, array $valor = []): self
  static indeterminado(string $motivo): self
  ehConcluido(): bool; ehNaoColetado(): bool; ehIndeterminado(): bool
  public string $estado; public array $valor; public ?string $motivo;

App\Services\ChecklistAdministrativo\ChecklistAdministrativoDefinicao
  const GRUPO_CONTRATO = 'contrato'; const GRUPO_ENTRADA = 'entrada';
  const NATUREZA_MANUAL = 'manual'; const NATUREZA_AUTO = 'auto';
  const AUTO_FONTE_CONTRATO_ENVIADO  = 'contrato_enviado';
  const AUTO_FONTE_CONTRATO_ASSINADO = 'contrato_assinado';
  const AUTO_FONTE_ML_OAUTH          = 'ml_oauth_conectado';
  const AUTO_FONTE_CONEXAO_ECF       = 'conexao_ecf';
  static itens(bool $exigeContrato): array   // 9 quando true, 6 quando false
  static item(string $chave): ?array         // null para chave fora do catálogo
  static chaves(bool $exigeContrato): array

Item do catálogo (shape de cada elemento, molde: app/Support/Onboarding/DefinicaoOnboarding.php):
  ['ordem' => int, 'chave' => string, 'titulo' => string, 'grupo' => string,
   'natureza' => string, 'auto_fonte' => ?string, 'ajuda' => string]

Os 9 itens (D-01/D-03), em ordem:
  1 contrato_revisado         grupo contrato  natureza manual  auto_fonte null
  2 contrato_enviado          grupo contrato  natureza auto    auto_fonte contrato_enviado
  3 contrato_assinado         grupo contrato  natureza auto    auto_fonte contrato_assinado
  4 grupo_whatsapp_criado     grupo entrada   natureza manual  auto_fonte null
  5 email_colaborador_criado  grupo entrada   natureza manual  auto_fonte null
  6 link_adman_entregue       grupo entrada   natureza manual  auto_fonte null
  7 grant_consultoria_ml      grupo entrada   natureza auto    auto_fonte ml_oauth_conectado
  8 conexao_ecf_gerada        grupo entrada   natureza auto    auto_fonte conexao_ecf
  9 boas_vindas_enviada       grupo entrada   natureza manual  auto_fonte null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato ChecklistResolver e value object de 3 estados</name>
  <files>app/Contracts/ChecklistResolver.php, app/Services/ChecklistAdministrativo/ChecklistResolverResultado.php</files>
  <read_first>
    - `app/Contracts/OnboardingResolver.php` (arquivo inteiro, 56 linhas — a interface análoga; note o método `assincrono()` e a assinatura `resolver(Onboarding, OnboardingPasso)`)
    - `app/Services/Onboarding/OnboardingResolverResultado.php` (arquivo inteiro, 84 linhas — o value object a copiar quase literalmente)
    - `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-PATTERNS.md` seções `app/Contracts/ChecklistResolver.php` e `ChecklistResolverResultado.php` ("O que muda")
  </read_first>
  <action>
Criar `app/Contracts/ChecklistResolver.php` no namespace `App\Contracts`, com os métodos
`chave(): string`, `label(): string`, `ajuda(): string` e
`resolver(\App\Models\Company $company): \App\Services\ChecklistAdministrativo\ChecklistResolverResultado`.

Diferenças deliberadas em relação ao `OnboardingResolver`, a registrar em comentário pt-BR no
docblock da interface: (a) `resolver()` recebe `Company` direto, não `Onboarding`/`OnboardingPasso`
— não existe entidade "onboarding" nesta fase, o checklist é ancorado em `company_id` (D-10);
(b) o método `assincrono(): bool` **não existe** aqui — os 4 resolvers desta fase são todos
síncronos (leitura de coluna local, sem rede), e manter um método que todo implementador responderia
`false` seria ruído. Se uma fase futura precisar de resolver assíncrono, a interface volta à mesa
naquele momento.

O docblock de `chave()` deve dizer que o valor bate 1:1 com a chave `auto_fonte` do item no
`ChecklistAdministrativoDefinicao` (catálogo fechado) — nunca uma string livre vinda de requisição.

Criar `app/Services/ChecklistAdministrativo/ChecklistResolverResultado.php` como
`final readonly class`, cópia do análogo com estas mudanças: nome da classe; remover
`CHAVE_COLETA_EM_ANDAMENTO`/`sinalizouColetaEmAndamento()` (não há coleta assíncrona nesta fase);
manter os três estados `CONCLUIDO`, `NAO_COLETADO`, `INDETERMINADO` e os três construtores estáticos
e os três predicados. Comentário em pt-BR registrando que `INDETERMINADO` fica no catálogo mesmo sem
nenhum resolver desta fase o produzir hoje: é a disciplina do D-10 — "nunca concluir a partir de um
estado indeterminado" precisa existir como estado nomeável antes de alguém precisar dele.
  </action>
  <acceptance_criteria>
    - `app/Contracts/ChecklistResolver.php` contém `interface ChecklistResolver` e a assinatura `public function resolver(Company $company): ChecklistResolverResultado;`
    - `app/Contracts/ChecklistResolver.php` **não** contém a string `assincrono`
    - `app/Services/ChecklistAdministrativo/ChecklistResolverResultado.php` contém `final readonly class ChecklistResolverResultado`
    - Contém as três constantes `const CONCLUIDO`, `const NAO_COLETADO`, `const INDETERMINADO`
    - Contém os três métodos estáticos `public static function concluido`, `naoColetado`, `indeterminado`
    - **Não** contém a string `CHAVE_COLETA_EM_ANDAMENTO`
    - `/c/xampp/php/php.exe -l app/Contracts/ChecklistResolver.php` e `-l` no value object saem sem erro de sintaxe
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe -l app/Contracts/ChecklistResolver.php &amp;&amp; /c/xampp/php/php.exe -l app/Services/ChecklistAdministrativo/ChecklistResolverResultado.php &amp;&amp; grep -q 'const INDETERMINADO' app/Services/ChecklistAdministrativo/ChecklistResolverResultado.php &amp;&amp; ! grep -q 'assincrono' app/Contracts/ChecklistResolver.php &amp;&amp; echo CONTRATO_OK</automated>
  </verify>
  <done>Existe uma interface síncrona de resolver e um value object de 3 estados, sem os ramos assíncronos do motor de Onboarding.</done>
</task>

<task type="auto">
  <name>Task 2: Catálogo fechado dos 9 itens (ChecklistAdministrativoDefinicao)</name>
  <files>app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php</files>
  <read_first>
    - `app/Support/Onboarding/DefinicaoOnboarding.php` linhas 1-165 e 365-470 (docblock da classe, `paraServico()`, `eGestao()` e a abertura de `gestao()` — o molde de "itens em código", com o shape de array associativo por passo; **é a lacuna que o PATTERNS.md pediu para ler antes de escrever esta classe**)
    - `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-CONTEXT.md` D-01, D-03, D-04, D-05, D-06, D-07, D-13 (a tabela dos 9 itens e a razão de cada natureza)
    - `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-PATTERNS.md` seção `ChecklistAdministrativoDefinicao.php` ("O que muda")
    - `app/Models/Servico.php` linhas 285-300 (`exigeContrato()` — a isenção que o parâmetro booleano representa)
  </read_first>
  <action>
Criar `app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php`.

Diferença estrutural em relação ao molde, a registrar no docblock da classe em pt-BR: o
`DefinicaoOnboarding` é parametrizado por `Servico` e versionado (`VERSAO`, copiado para colunas no
nascimento) porque um onboarding em andamento não pode mudar debaixo do cliente. Aqui **não há
versionamento e não há cópia para colunas**: o catálogo é lido a cada montagem, os 9 itens são a
lista do §5 travada pela D-01, e mudar um rótulo é mudar uma constante em código. A D-02 já aceitou
esse custo — não existe item configurável em runtime.

Constantes públicas: `GRUPO_CONTRATO = 'contrato'`, `GRUPO_ENTRADA = 'entrada'`;
`NATUREZA_MANUAL = 'manual'`, `NATUREZA_AUTO = 'auto'`; e as quatro chaves de resolver
`AUTO_FONTE_CONTRATO_ENVIADO = 'contrato_enviado'`, `AUTO_FONTE_CONTRATO_ASSINADO = 'contrato_assinado'`,
`AUTO_FONTE_ML_OAUTH = 'ml_oauth_conectado'`, `AUTO_FONTE_CONEXAO_ECF = 'conexao_ecf'`.

Método privado estático `todos(): array` com os 9 itens **na ordem 1..9 da tabela do D-03**, cada um
no shape `['ordem', 'chave', 'titulo', 'grupo', 'natureza', 'auto_fonte', 'ajuda']`. Chaves e grupos
exatamente como listados no bloco `<interfaces>` deste plano. Títulos em pt-BR, na letra do D-03:
"Contrato revisado", "Contrato enviado", "Contrato assinado", "Grupo de WhatsApp criado",
"E-mail colaborador criado", "Link Adman entregue", "Grant da consultoria (OAuth Mercado Livre)",
"Conexão com o sistema ECF gerada", "Boas-vindas enviada". Grafia **Adman**, nunca ADMA (D-04).

Comentário inline em pt-BR justificando a `natureza` de cada item que a carrega por decisão, não por
falta de opção: item 1 é manual porque revisar contrato é ato humano e nenhum dos 7 estados do
envelope o representa (D-06); itens 4, 5, 6 e 9 são manuais porque **não existe sinal observável** e
fabricar um seria marcar concluído sem evidência (D-13); item 6 é manual em particular porque o link
do Adman é fixo, igual para todas as empresas, e mora em `config('services.adman.register_url')`
(D-04); item 7 fecha só quando o **cliente conectou**, nunca quando o link foi gerado (D-05); item 8
fecha por **existência** do `onboarding_links` da empresa (D-14).

Métodos públicos estáticos:
- `itens(bool $exigeContrato): array` — devolve os 9 quando `true` e **apenas os 6 do grupo
  Entrada** quando `false`. Docblock em pt-BR citando D-07: para serviço isento (hoje só `Polos`,
  `servicos.id=2`, com 5 vínculos ativos) o grupo Contrato **não existe** — não é item marcado
  "não aplicável" (proibido pela D-02), é item que nem é instanciado, e o denominador do progresso
  acompanha. Sem isso, essas empresas teriam 3 itens pendentes para sempre e nunca poderiam
  finalizar.
- `item(string $chave): ?array` — busca no catálogo completo (9 itens) e devolve `null` para chave
  desconhecida. `null` é a resposta correta e esperada para chave órfã — nunca lançar exceção aqui;
  quem valida entrada de requisição é o controller, com `Rule::in()` sobre `chaves()`.
- `chaves(bool $exigeContrato): array` — só os slugs, para uso em `Rule::in()`.

Nada nesta classe consulta o banco, injeta serviço ou chama resolver. É catálogo puro.
  </action>
  <acceptance_criteria>
    - `app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php` contém as 9 chaves literais: `contrato_revisado`, `contrato_enviado`, `contrato_assinado`, `grupo_whatsapp_criado`, `email_colaborador_criado`, `link_adman_entregue`, `grant_consultoria_ml`, `conexao_ecf_gerada`, `boas_vindas_enviada`
    - Contém as 4 constantes `AUTO_FONTE_CONTRATO_ENVIADO`, `AUTO_FONTE_CONTRATO_ASSINADO`, `AUTO_FONTE_ML_OAUTH`, `AUTO_FONTE_CONEXAO_ECF`
    - Contém as assinaturas `public static function itens(bool $exigeContrato): array`, `public static function item(string $chave): ?array` e `public static function chaves(bool $exigeContrato): array`
    - **Não** contém as strings `nao_aplicavel`, `ADMA `, `acompanhar_assinatura`, `inserir_links`, `VERSAO`
    - **Não** contém `use Illuminate\Support\Facades\DB` nem nenhuma chamada a `::query(`
    - `/c/xampp/php/php.exe -l app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php` sai sem erro
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe -l app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php &amp;&amp; grep -q 'grant_consultoria_ml' app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php &amp;&amp; ! grep -q 'nao_aplicavel' app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php &amp;&amp; echo CATALOGO_OK</automated>
  </verify>
  <done>Existe um catálogo fechado com os 9 itens do §5, cada um com grupo/natureza/auto_fonte, e `itens(false)` devolve só os 6 do grupo Entrada.</done>
</task>

<task type="auto">
  <name>Task 3: Teste unitário do catálogo (D-01, D-03, D-07)</name>
  <files>tests/Unit/Phase152/ChecklistDefinicaoCatalogoTest.php</files>
  <read_first>
    - `app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php` (criado na Task 2 — as assinaturas e o shape de item que o teste assere)
    - `tests/Unit/Phase150/CompanyEtapaConstantesTest.php` (arquivo inteiro — o molde de teste unitário puro sobre catálogo de constantes, sem banco)
  </read_first>
  <action>
Criar `tests/Unit/Phase152/ChecklistDefinicaoCatalogoTest.php` estendendo `Tests\TestCase`,
**sem** `RefreshDatabase` (é catálogo puro, não toca banco).

Casos a cobrir, um método por afirmação:
1. `itens(true)` devolve exatamente 9 elementos e `itens(false)` devolve exatamente 6 (D-01, D-07).
2. `itens(false)` não contém nenhum item com `grupo === GRUPO_CONTRATO` (D-07 — o grupo não existe,
   não é item marcado).
3. A lista de chaves de `itens(true)` é exatamente a lista das 9 chaves do D-03, na ordem 1..9.
4. Os itens `contrato_enviado`, `contrato_assinado`, `grant_consultoria_ml` e `conexao_ecf_gerada`
   têm `natureza === NATUREZA_AUTO` e `auto_fonte` não-nulo; todos os outros cinco têm
   `natureza === NATUREZA_MANUAL` e `auto_fonte === null` (D-03, D-06, D-13).
5. `item('chave_que_nao_existe')` devolve `null` sem lançar exceção — a defesa da chave órfã.
6. Nenhum item do catálogo tem a chave `nao_aplicavel` em array nenhum (D-02) — varrer os 9 itens.
7. O item 6 tem título contendo a palavra `Adman` e **não** contém `ADMA` isolado (D-04).

Nomes de método em `snake_case` prefixado por `test_`, descrição em pt-BR nos comentários.
  </action>
  <acceptance_criteria>
    - `tests/Unit/Phase152/ChecklistDefinicaoCatalogoTest.php` existe
    - Contém pelo menos 7 métodos que começam com `public function test_`
    - Contém as asserções literais `assertCount(9,` e `assertCount(6,`
    - Contém a string `NATUREZA_AUTO` e a string `NATUREZA_MANUAL`
    - `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase152/ChecklistDefinicaoCatalogoTest.php --colors=never` sai com código 0
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase152/ChecklistDefinicaoCatalogoTest.php --colors=never</automated>
  </verify>
  <done>Um teste unitário prova que são 9 itens com contrato e 6 sem, com a natureza correta em cada um e sem `nao_aplicavel` em lugar nenhum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpo da requisição → `chave` de item | no plano 152-08 o usuário envia uma `chave`; o catálogo fechado criado aqui é a lista de valores aceitáveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-03-01 | Tampering | `chave` de item livre vinda de requisição | mitigate | `ChecklistAdministrativoDefinicao::chaves()` existe justamente para alimentar `Rule::in()` no controller (plano 152-08). `item()` devolve `null` para chave desconhecida em vez de aceitar — nunca string livre, mesmo padrão de `ContratoLiberacao::MOTIVOS_MANUAIS` |
| T-152-03-02 | Elevation of Privilege | item do grupo Contrato aparecendo para serviço isento | mitigate | `itens(false)` não instancia o grupo Contrato (D-07); asserção de teste de que nenhum item devolvido tem `grupo === GRUPO_CONTRATO` |
| T-152-03-03 | Repudiation | item marcado "concluído" sem evidência por resolver inventado | mitigate | O catálogo declara `auto_fonte = null` para os 5 itens manuais (D-06, D-13); o plano 152-05 usa essa declaração para **recusar** conclusão manual em item `auto` e para nunca auto-marcar item `manual` |
| T-152-03-SC | Tampering | npm/composer installs | mitigate | Nenhum pacote instalado — `152-RESEARCH.md` §Package Legitimacy Audit: "não aplicável" |
</threat_model>

<verification>
1. `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase152/ChecklistDefinicaoCatalogoTest.php --colors=never` sai 0.
2. `grep -c 'nao_aplicavel' app/Services/ChecklistAdministrativo/ChecklistAdministrativoDefinicao.php` retorna 0.
3. `grep -c 'assincrono' app/Contracts/ChecklistResolver.php` retorna 0.
4. Regressão: a suíte 137/138 continua igual à `152-BASELINE-TESTES.md`.
</verification>

<success_criteria>
- 9 itens com contrato, 6 sem, provado por teste
- Cada item declara grupo, natureza e (quando `auto`) a chave do resolver
- Value object de 3 estados existe e não tem os ramos de coleta assíncrona
- Nenhuma consulta a banco na classe de catálogo
</success_criteria>

<output>
Criar `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-03-SUMMARY.md` ao terminar.
</output>
