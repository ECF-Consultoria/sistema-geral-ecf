---
phase: 152-checklist-administrativo-trava-de-finaliza-o-v23-0
plan: 10
type: execute
wave: 9
depends_on: ["152-09"]
files_modified:
  - .planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-REGRESSAO-FINAL.md
autonomous: false
requirements: [ADMIN-01, ADMIN-03, ADMIN-05, ADMIN-06]
user_setup: []

must_haves:
  truths:
    - "A suíte da fase fecha verde e as suítes 131/137/138 continuam iguais à `152-BASELINE-TESTES.md` — nenhuma regressão introduzida pelos três novos chamadores de `EtapaTransicaoService`"
    - "D-08: a mesma ficha abre pelas DUAS listagens (Contrato e Entrada), verificado no navegador, não só por teste"
    - "D-17: usuário com só `comercial.entrada` abre a ficha sem 403 e não enxerga a seção Contrato na tela"
    - "D-05: o botão do item 7 copia o link para a área de transferência — nunca abre o Mercado Livre no navegador de quem clicou"
    - "ADMIN-05/ADMIN-06: o FINALIZAR está desabilitado com item pendente e, quando habilitado, move a empresa para Aguardando Distribuição sem criar cadastro novo"
    - "As duas páginas React alteradas entram no manifest do Vite e renderizam de verdade — a armadilha do re-export não falha o build, só a runtime"
  artifacts:
    - path: ".planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-REGRESSAO-FINAL.md"
      provides: "Comparação da suíte final contra a baseline capturada no plano 152-01"
      contains: "152-BASELINE-TESTES.md"
  key_links:
    - from: ".planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-REGRESSAO-FINAL.md"
      to: ".planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-BASELINE-TESTES.md"
      via: "comparação nominal de falhas"
      pattern: "BASELINE"
---

<objective>
Fechar a fase: reexecutar a suíte inteira contra a baseline do plano 152-01 e submeter ao usuário as
verificações que teste nenhum pega — o render real nas duas listagens, os dois perfis de permissão,
o comportamento de "copiar link" do item 7 e o efeito do FINALIZAR.

Purpose: a fase toca a máquina de estados que a Fase 150 entregou e alarga a permissão de uma rota
de contrato. As duas coisas precisam de confirmação humana antes de a fase ser considerada pronta.
Output: `152-REGRESSAO-FINAL.md` e dois checkpoints aprovados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-VALIDATION.md
@.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-CONTEXT.md
@.planning/learnings/painel-polos-status-e-meta.md

**Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada`. No Bash, o PHP é
`/c/xampp/php/php.exe`. `php artisan test` e `--testsuite=Feature` **não terminam** neste ambiente.
Conta local para a conferência visual: `admin@ecfconsultoria.com.br` / `Admin@ecf2024`.

⚠️ **Nenhum passo de deploy nesta fase.** Nada de `deploy.sh`, `pscp` ou `plink`. A fase termina no
localhost.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regressão final contra a baseline</name>
  <files>.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-REGRESSAO-FINAL.md</files>
  <read_first>
    - `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-BASELINE-TESTES.md` (criado no plano 152-01 — os números e as falhas nominais a comparar)
    - `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-VALIDATION.md` (o comando de suíte por wave e as restrições do ambiente)
    - `.planning/learnings/painel-polos-status-e-meta.md` §2 (as ~10 falhas de Polos que continuam fora do gate)
  </read_first>
  <action>
Rodar, na raiz do worktree, os dois comandos, nesta ordem:

1. Suíte da fase:
   `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase152 tests/Feature/Phase152 --colors=never`
2. Regressão das fases que esta encosta:
   `/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase150 tests/Feature/Phase150 tests/Feature/Phase151 tests/Feature/Phase131 --colors=never`

Gravar `152-REGRESSAO-FINAL.md` com os dois comandos, os dois resumos numéricos literais e uma
comparação **nominal** contra `152-BASELINE-TESTES.md`: para cada teste que falhar agora, dizer se
ele já falhava na baseline. Qualquer falha nova é bloqueante e precisa ser corrigida antes de seguir
para os checkpoints — não relatar como "conhecida".

Registrar explicitamente que `tests/Feature/Phase38/PolosControllerTest.php` e
`tests/Feature/Polos/PolosFaturamentoSnapshotTest.php` continuam **fora** do gate, com a razão
(falhas antigas, não são regressão desta fase).

Rodar também `/c/xampp/php/php.exe artisan migrate:status | grep checklist_administrativo` contra o
MariaDB local e registrar que a migration desta fase consta como aplicada — a confirmação de que o
erro 1059 não deixou a tabela criada sem índice.

Commit por caminho (`git commit -- <caminhos>`), nunca `git add -A`.
  </action>
  <acceptance_criteria>
    - `152-REGRESSAO-FINAL.md` existe e contém os dois comandos na forma `/c/xampp/php/php.exe vendor/bin/phpunit ...`
    - Contém a string `152-BASELINE-TESTES.md` e uma comparação nominal por teste
    - Contém a saída de `migrate:status` para `checklist_administrativo_itens`
    - A suíte da fase (`tests/Unit/Phase152 tests/Feature/Phase152`) sai com código 0
    - A suíte de regressão (`Phase150`, `Phase151`, `Phase131`) sai com código 0, ou toda falha remanescente está nominalmente presente na baseline
  </acceptance_criteria>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase152 tests/Feature/Phase152 --colors=never &amp;&amp; /c/xampp/php/php.exe vendor/bin/phpunit tests/Unit/Phase150 tests/Feature/Phase150 tests/Feature/Phase151 tests/Feature/Phase131 --colors=never &amp;&amp; test -f .planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-REGRESSAO-FINAL.md &amp;&amp; echo REGRESSAO_OK</automated>
  </verify>
  <done>A suíte da fase está verde e nenhuma falha nova aparece nas suítes 131/137/138 em relação à baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Conferência visual — as duas listagens, os dois perfis e o item 7</name>
  <files>—</files>
  <read_first>
    - `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-VALIDATION.md` seção "Verificações manuais (o teste não pega)"
    - `.planning/learnings/painel-polos-status-e-meta.md` §4 (a página React que some do manifest do Vite — o motivo de esta conferência existir apesar de o build passar)
  </read_first>
  <what-built>
Checklist administrativo renderizado na ficha da empresa (`/administrativo/contratos/empresa/{id}`),
com as seções Contrato e Entrada, barra de progresso e botão FINALIZAR; ação "Abrir" na listagem
`/comercial/entrada` apontando para a mesma ficha; rota da ficha aceitando `admin.contratos` **ou**
`comercial.entrada`; item 7 com botão de copiar o link do OAuth do Mercado Livre; item 6 exibindo o
link fixo do Adman vindo de `config('services.adman.register_url')`.

Tudo já verificado por teste automatizado (`ChecklistAcessoPorEntradaTest`, `ChecklistEndpointsTest`,
`ChecklistContagemItensTest`) e `npm run build` verde com as duas páginas no manifest. O que falta é
o que teste PHP não alcança: o render real no navegador e o comportamento da área de transferência.
  </what-built>
  <action>
Antes de chamar o usuário, deixar o ambiente pronto: rodar `npm run build`, conferir que
`public/build/manifest.json` contém as duas páginas alteradas, e subir o servidor local com
`/c/xampp/php/php.exe artisan serve --port=8139` a partir da raiz do worktree. Preparar (ou
identificar) os três cenários que a conferência exige — uma empresa com serviço que exige contrato,
uma empresa isenta (serviço Polos, `exige_contrato = 0`) e um usuário de setor com **apenas** a
permissão `comercial.entrada` — e informar ao usuário os ids/URLs exatos de cada um, para que ele
não precise procurar. Só então apresentar o checkpoint. Nenhum deploy, `pscp` ou `plink`.
  </action>
  <how-to-verify>
1. Subir o servidor local a partir da raiz do worktree:
   `/c/xampp/php/php.exe artisan serve --port=8139` e abrir `http://127.0.0.1:8139`.
2. Entrar com `admin@ecfconsultoria.com.br` / `Admin@ecf2024`.
3. Abrir `/administrativo/contratos`, clicar em "Abrir" numa empresa **com** serviço que exige
   contrato. Esperado: a ficha mostra o card "Checklist administrativo" com **duas** seções
   (Contrato e Entrada) e **9** itens no total, cada um com Pendente/Concluído.
4. Voltar e abrir `/comercial/entrada`. Esperado: existe uma coluna "Ações" com o link "Abrir"; ele
   leva à **mesma** ficha da etapa 3, sem erro de "Unable to locate file in Vite manifest".
5. Abrir a ficha de uma empresa **isenta de contrato** (serviço Polos). Esperado: **6** itens e
   **nenhuma** seção Contrato — nem vazia, nem com itens marcados "não aplicável".
6. Marcar à mão "Grupo de WhatsApp criado". Esperado: o item vira Concluído e aparece
   "Concluído por {seu nome} em {data}". Desmarcar: volta a Pendente e a linha de autoria some.
7. No item "Grant da consultoria (OAuth Mercado Livre)", clicar em "Copiar link de autorização".
   ⚠️ **Esperado: o link vai para a área de transferência e NADA abre.** Se o navegador navegar
   para o Mercado Livre, é falha bloqueante — clique interno com sessão ML ativa autoriza a conta
   errada (incidente `project_polos_oauth_link_boas_vindas_260827`). Colar num bloco de notas para
   confirmar que a URL veio.
8. No item "Link Adman entregue", conferir que a URL exibida é
   `https://app.ad-man.io/register?ref=588D0DD78C4F` e que ela é a **mesma** em duas empresas
   diferentes (é link fixo, não gerado por empresa).
9. Criar (ou usar) um usuário de setor com **apenas** a permissão `comercial.entrada`, entrar com
   ele e abrir a ficha pela listagem Entrada. Esperado: **não** dá 403; a seção **Contrato não
   aparece**; os envelopes/signatários não aparecem em lugar nenhum da tela.
  </how-to-verify>
  <acceptance_criteria>
    - A ficha abre pelas duas listagens sem erro de manifest do Vite
    - Empresa com contrato mostra 9 itens em duas seções; empresa isenta mostra 6 itens sem a seção Contrato
    - Marcar/desmarcar exibe e remove a linha de autoria
    - O botão do item 7 **copia** o link e não navega para o Mercado Livre
    - O link do Adman é idêntico em duas empresas diferentes e bate com o valor de `config('services.adman.register_url')`
    - Usuário só-`comercial.entrada` abre a ficha (sem 403) e não vê a seção Contrato nem dado de signatário
  </acceptance_criteria>
  <verify>
    <human-check>Conferência no navegador conforme os 9 passos acima</human-check>
    <automated>npm run build &amp;&amp; grep -q 'resources/js/Pages/Comercial/Entrada.jsx' public/build/manifest.json &amp;&amp; grep -q 'resources/js/Pages/Admin/ContratoDetalhe.jsx' public/build/manifest.json &amp;&amp; echo MANIFEST_OK</automated>
  </verify>
  <resume-signal>Responda "aprovado" ou descreva o que divergiu, passo a passo</resume-signal>
  <done>O usuário confirmou o render nas duas listagens, os dois perfis de permissão e que o item 7 copia em vez de abrir.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Conferência do FINALIZAR de ponta a ponta</name>
  <files>—</files>
  <read_first>
    - `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-CONTEXT.md` D-15 (a régua das etapas) e D-12 (o destino por marketplace)
    - `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-REGRESSAO-FINAL.md` (produzido na Task 1 — a evidência de que a máquina de estados não regrediu)
  </read_first>
  <what-built>
A trava do FINALIZAR (`podeFinalizar()`) e o efeito (`finalizar()`), mais o sincronizador que faz o
progresso do checklist avançar a etapa da empresa de 1 até 4 (D-15). Tudo coberto por
`FinalizarTravaTest`, `FinalizarTransicaoEtapaTest` e `ChecklistDirigeEtapaTest`, incluindo o salto
2→4 da empresa isenta e o caso de `is_primary` duplicado.

O que falta é a confirmação de que, olhando a tela, a pessoa que vai operar isso entende **por que**
o botão está desabilitado e vê a empresa sair da listagem Entrada depois de finalizar.
  </what-built>
  <action>
Antes de chamar o usuário, preparar uma empresa de teste no localhost com itens ainda pendentes e
anotar a URL da ficha, o estado inicial de `companies.etapa` e a lista dos itens que faltam.
Deixar à mão o caminho para fechar o item "Contrato assinado" no passo 3 (gravar `assinado_em` no
envelope por tinker, ou usar a liberação manual pela própria tela) e informá-lo ao usuário junto do
checkpoint. Reconferir a etapa **por reconsulta ao banco** antes e depois, nunca por stdout da tela.
Nenhum deploy, `pscp` ou `plink`.
  </action>
  <how-to-verify>
1. Com o servidor local no ar, abrir a ficha de uma empresa com itens pendentes. Esperado: o botão
   **FINALIZAR ENTRADA ADMINISTRATIVA** está desabilitado e, logo abaixo dele, há uma frase dizendo
   o que falta (ex.: "Faltam 3 de 9 itens — o primeiro pendente é ...") — nunca botão cinza mudo.
2. Concluir todos os itens obrigatórios menos o "Contrato assinado". Esperado: a frase muda para
   falar de contrato, não de contagem.
3. Fechar o item "Contrato assinado" (numa empresa de teste: gravar `assinado_em` no envelope, ou
   registrar liberação manual pela própria tela). Esperado: o botão **habilita** neste momento e não
   antes.
4. Conferir a etapa da empresa **antes** de clicar. Esperado: `Administrativo concluído` — o
   checklist já a levou até lá sozinho (D-15). Se ela ainda estiver em `Administrativo em andamento`
   ou `Aguardando assinatura`, recarregar a ficha uma vez (o sincronizador roda na montagem).
5. Clicar em FINALIZAR. Esperado: mensagem de sucesso, e a empresa passa para
   **Aguardando Distribuição**.
6. Voltar a `/comercial/entrada`. Esperado: a empresa **saiu** da listagem (corte de saída na etapa
   5, D-07 da Fase 151). Abrir `/administrativo/contratos`: a empresa **continua** lá — a listagem
   Contrato não tem corte por etapa, e isso é intencional.
7. Confirmar que **não** nasceu cadastro novo: a empresa em Aguardando Distribuição é a mesma linha,
   com o mesmo nome e CNPJ, e não há duplicata na busca por nome.
  </how-to-verify>
  <acceptance_criteria>
    - Botão desabilitado sempre acompanhado da frase do que falta
    - A frase muda de "faltam N itens" para "contrato não assinado" quando esse é o único pendente
    - O botão habilita exatamente quando o último requisito é cumprido, nunca antes
    - Após finalizar, a empresa aparece em Aguardando Distribuição e sai da listagem Entrada
    - A empresa continua visível na listagem Contrato
    - Nenhum cadastro novo foi criado (mesma empresa, sem duplicata)
  </acceptance_criteria>
  <verify>
    <human-check>Conferência no navegador conforme os 7 passos acima</human-check>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase152/FinalizarTravaTest.php tests/Feature/Phase152/FinalizarTransicaoEtapaTest.php tests/Feature/Phase152/ChecklistDirigeEtapaTest.php --colors=never</automated>
  </verify>
  <resume-signal>Responda "aprovado" ou descreva o que divergiu, passo a passo</resume-signal>
  <done>O usuário confirmou que a trava explica o que falta, habilita no instante certo, e que finalizar move a mesma empresa para Aguardando Distribuição.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambiente de teste (SQLite) → ambiente real (MariaDB) | a suíte não prova o que só o MariaDB quebra |
| build do Vite → runtime do navegador | o build passa mesmo quando a página some do manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-10-01 | Spoofing | autorização OAuth falsa passar despercebida porque nenhum teste PHP alcança a área de transferência | mitigate | Passo 7 do checkpoint é explícito e bloqueante: se o navegador navegar para o Mercado Livre, é falha, não observação |
| T-152-10-02 | Information Disclosure | seção Contrato visível para `comercial.entrada` apesar do gating de servidor | mitigate | Passo 9 do checkpoint verifica com um usuário real de setor, não só por asserção de payload |
| T-152-10-03 | Denial of Service | migration meio-aplicada no MariaDB (erro 1059) escapando porque a suíte roda em SQLite | mitigate | `migrate:status` contra o MariaDB local registrado na Task 1 |
| T-152-10-04 | Denial of Service | página fora do manifest do Vite derrubando a rota em runtime sem falhar o build | mitigate | Verificação direta das duas entradas em `public/build/manifest.json` (automatizada) mais o passo 4 do checkpoint (render real) |
| T-152-10-05 | Repudiation | falha nova sendo relatada como "falha conhecida de Polos" | mitigate | `152-REGRESSAO-FINAL.md` exige comparação **nominal** contra a baseline, teste a teste — não comparação de contagem |
| T-152-10-SC | Tampering | npm/composer installs | mitigate | Nenhum pacote instalado em nenhuma task desta fase |
</threat_model>

<verification>
1. `152-REGRESSAO-FINAL.md` existe, com os dois resumos e a comparação nominal contra a baseline.
2. Suíte da fase verde; suítes 131/137/138 sem falha nova.
3. `public/build/manifest.json` contém as duas páginas alteradas.
4. Os dois checkpoints foram aprovados pelo usuário.
5. Nenhum passo de deploy foi executado em nenhum plano da fase.
</verification>

<success_criteria>
- Regressão comparada nominalmente contra a baseline, sem falha nova
- Render confirmado no navegador pelas duas listagens e pelos dois perfis de permissão
- Item 7 comprovadamente copia o link, não abre
- FINALIZAR explica o que falta, habilita no instante certo e move a mesma empresa para Aguardando Distribuição
</success_criteria>

<output>
Criar `.planning/phases/152-checklist-administrativo-trava-de-finaliza-o-v23-0/152-10-SUMMARY.md` ao terminar.
</output>
