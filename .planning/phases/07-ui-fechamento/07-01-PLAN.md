---
phase: 07-ui-fechamento
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/Pages/Admin/Financeiro.jsx
autonomous: false
requirements: [FCH-06, FCH-07, FCH-08, CFG-01]

must_haves:
  truths:
    - "Bloco TotalConsolidado visível no topo da página com soma de faturamento e valor_mensal das empresas ok"
    - "Empresa com estado ok exibe faturamento formatado em BRL e período coberto (ex: '01/05 a 18/05') na linha, sem expandir accordion"
    - "Accordion expandido de empresa ok exibe barra de progresso com porcentagem e texto 'Falta R$X para a próxima faixa'"
    - "Accordion expandido de empresa na faixa maxima exibe bloco 'Faixa máxima' com ícone TrendingUp, sem barra"
    - "Campo additional_service visível no accordion (valor ou '—'), acima do ServiceForm"
    - "Formulário ServiceForm (tipo de serviço + datas de contrato) continua funcionando sem regressão"
    - "npm run build termina com 0 erros"
  artifacts:
    - path: "resources/js/Pages/Admin/Financeiro.jsx"
      provides: "Financeiro.jsx expandido com TotalConsolidado, FaixaProgresso, FechamentoRow e FechamentoAccordion atualizados"
      contains: "FAIXAS_LIMITES"
  key_links:
    - from: "Financeiro (export default)"
      to: "TotalConsolidado"
      via: "prop companies passada diretamente"
      pattern: "TotalConsolidado.*empresas"
    - from: "FechamentoAccordion"
      to: "FaixaProgresso"
      via: "faturamento e faixa do objeto empresa"
      pattern: "FaixaProgresso.*faturamento.*faixa"
    - from: "FaixaProgresso"
      to: "FAIXAS_LIMITES"
      via: "indexação por faixa key"
      pattern: "FAIXAS_LIMITES\\[faixa\\]"
---

<objective>
Expandir Financeiro.jsx para consumir as props entregues pela Phase 6 e exibir os três blocos
de UI requeridos por FCH-06, FCH-07 e FCH-08.

Purpose: Admin passa a ver, na mesma tela de cadastro já existente, o faturamento mensal de
cada empresa, sua posição na faixa de investimento e o total consolidado a cobrar no mês —
sem acessar servidor ou planilha.

Output: Financeiro.jsx expandido (mesmo arquivo, mesma rota), com cinco adições: constante
FAIXAS_LIMITES, helper fmtBRL local, componente TotalConsolidado, componente FaixaProgresso,
e expansões em FechamentoRow e FechamentoAccordion.
</objective>

<execution_context>
@/c/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-backend-fechamento/06-02-SUMMARY.md

<!-- Estado atual do arquivo alvo — lido integralmente antes de qualquer edição -->
@resources/js/Pages/Admin/Financeiro.jsx

<!-- Design system -->
@tailwind.config.js
</context>

<interfaces>
<!-- Props shape entregue por AdminController::fechamento() — confirmado em 06-02-SUMMARY.md -->
<!-- Cada elemento do array `companies` tem a seguinte forma no JSON Inertia: -->

companies: Array<{
  id: number,
  name: string,
  service_type: 'polo' | 'assessoria' | 'incubadora' | null,
  contract_start: string | null,      // ISO date "YYYY-MM-DD"
  contract_end: string | null,        // ISO date "YYYY-MM-DD"
  additional_service: string | null,
  has_adman: boolean,
  estado: 'sem_integracao' | 'sem_dados' | 'ok',
  faturamento: number | null,         // int no JSON (ex: 3000, 1000000) — usar Number()
  periodo_inicio: string | null,      // 'd/m' ex: '01/05'
  periodo_fim: string | null,         // 'd/m' ex: '18/05'
  faixa: string | null,               // 'ate_499k' | '500k_999k' | '1m_1999k' | '2m_2999k' | '3m_3999k' | '4m_4999k' | 'maxima'
  valor_mensal: number | null,        // int no JSON (ex: 3000, 4500) — usar Number()
}>

<!-- Tokens de design relevantes (tailwind.config.js) -->
ecf.yellow = '#ffe600'   (bg-ecf-yellow, text-ecf-yellow, border-ecf-yellow/*)
ecf.card   = '#0f1116'   (.card-ecf background)
ecf.bg     = '#050507'

<!-- utils.js exporta: cn(), formatCurrency() (COM centavos), formatDate(), formatPercent() -->
<!-- NÃO usar formatCurrency para faturamento — ela inclui decimais (R$ 1.000.000,00) -->
<!-- fmtBRL local com minimumFractionDigits: 0 é obrigatório por D-14 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expandir Financeiro.jsx com todos os 5 novos elementos de UI</name>
  <files>resources/js/Pages/Admin/Financeiro.jsx</files>
  <action>
Ler o arquivo atual integralmente antes de editar. O arquivo tem 224 linhas e deve ser
expandido — não recriado do zero. Manter todos os sub-componentes existentes inalterados
(ServiceBadge, IntegrationBadge, ServiceForm, FechamentoList) exceto onde explicitado abaixo.

Adicionar ao import de lucide-react o ícone TrendingUp (linha 4):
  import { Banknote, ChevronDown, Building2, WifiOff, TrendingUp } from 'lucide-react';

Após SERVICE_COLORS (linha 17), antes de ServiceBadge, inserir dois blocos:

1. Constante FAIXAS_LIMITES (espelha AdminController::FAIXAS — per D-08):
   Chaves: ate_499k, '500k_999k', '1m_1999k', '2m_2999k', '3m_3999k', '4m_4999k'
   Cada entry: { min: <int>, proximo: <int> }
   Valores: { min: 0, proximo: 500000 }, { min: 500000, proximo: 1000000 },
            { min: 1000000, proximo: 2000000 }, { min: 2000000, proximo: 3000000 },
            { min: 3000000, proximo: 4000000 }, { min: 4000000, proximo: 5000000 }
   Usar underscore numeric separators (500_000) para legibilidade.
   NÃO incluir entry 'maxima' — per D-09 e Pitfall 3 do RESEARCH.md.

2. Helper fmtBRL local (per D-14 — BRL sem centavos):
   const fmtBRL = (n) => n == null ? '—'
       : Number(n).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL',
           minimumFractionDigits: 0, maximumFractionDigits: 0 });
   Usar loose equality (== null) para cobrir undefined também.
   NÃO usar !n como guard — zero monetário (R$ 0) é valor válido (Pitfall 5 do RESEARCH.md).

Após IntegrationBadge, inserir novo componente TotalConsolidado (per D-02, D-03, D-04):
  function TotalConsolidado({ empresas })
  - Filtrar: const ok = empresas.filter(e => e.estado === 'ok')
  - Somar: totalFat = ok.reduce((s, e) => s + Number(e.faturamento ?? 0), 0)
  - Somar: totalMensal = ok.reduce((s, e) => s + Number(e.valor_mensal ?? 0), 0)
  - const temDados = ok.length > 0
  - Layout: container rounded-xl border border-white/[0.08] bg-white/[0.02] p-4
  - Header do bloco: flex items-center gap-2 mb-3 com ícone Banknote size=15
    text-ecf-yellow/60, e texto 'Total consolidado · {ok.length} empresa{ok.length !== 1 ? "s" : ""} com dados'
    em text-white/50 text-[11px] font-semibold tracking-widest uppercase
  - Grid: grid grid-cols-2 gap-3
  - Mini-stat 1: label "Faturado (mês)", valor fmtBRL(totalFat) em text-ecf-yellow,
    "—" se !temDados (per D-04)
  - Mini-stat 2: label "A cobrar (mês)", valor fmtBRL(totalMensal) em text-emerald-400,
    "—" se !temDados (per D-04)
  - Cada mini-stat: rounded-xl bg-white/[0.03] border border-white/[0.06] p-3
    label: text-white/40 text-[11px] mb-1
    valor: font-display font-bold text-xl

Após TotalConsolidado, inserir novo componente FaixaProgresso (per D-07 a D-11):
  function FaixaProgresso({ faturamento, faixa })
  - Branch 1 — faixa === 'maxima' (per D-09): retornar div com flex items-center gap-2 py-3
    contendo TrendingUp size=14 text-ecf-yellow shrink-0,
    texto 'Faixa máxima' em text-ecf-yellow text-[13px] font-semibold,
    e span 'acima de R$ 5.000.000' em text-white/30 text-[12px]
  - Branch 2 — faixa ausente ou faturamento null: retornar null
  - Branch 3 — caso normal:
    const faixaData = FAIXAS_LIMITES[faixa]; if (!faixaData) return null;
    const pct = Math.min(100, Math.max(0,
        ((Number(faturamento) - faixaData.min) / (faixaData.proximo - faixaData.min)) * 100
    ));
    const falta = Math.max(0, faixaData.proximo - Number(faturamento));
    Layout externo: div py-3
    Header: flex items-center justify-between mb-1.5
      esquerda: 'Posição na faixa' em text-white/50 text-[11px] uppercase tracking-wider
      direita: '{Math.round(pct)}%' em text-white/50 text-[11px]
    Barra: div h-1.5 bg-ecf-yellow/30 rounded-full overflow-hidden
      filho: div h-full rounded-full transition-all style={{ width: `${pct}%`, background: '#ffe600' }}
    Texto: 'Falta {fmtBRL(falta)} para a próxima faixa'
      em text-white/40 text-[12px] mt-1.5
    Nota: usar style={{ background: '#ffe600' }} (não className bg-ecf-yellow) porque Tailwind
    não gera utilitário bg-* para cores customizadas via JIT em todos os contextos de prod — per
    padrão Mlb/Dashboard.jsx linha 183 verificado no RESEARCH.md.

Modificar FechamentoRow (linhas 43–72) para exibir faturamento + período per D-05, D-06:
  - Substituir: <span className="flex-1 text-white font-semibold text-[13px] truncate">
  - Por um div wrapper flex-1 min-w-0 contendo:
    span: text-white font-semibold text-[13px] truncate block (nome da empresa)
    condicional {empresa.estado === 'ok' && (
      <span className="text-white/40 text-[12px] mt-0.5 block">
        {fmtBRL(empresa.faturamento)} · {empresa.periodo_inicio} a {empresa.periodo_fim}
      </span>
    )}
  - Manter todos os outros elementos do row intactos (ChevronDown, ServiceBadge,
    IntegrationBadge, span datas).

Modificar FechamentoAccordion (linhas 159–165) para incluir FaixaProgresso e serviço adicional
per D-07, D-12, D-13:
  Novo corpo do componente (o div externo existente permanece: px-4 py-4 bg-black/30 border-t
  border-white/[0.04]):

  1. Bloco FaixaProgresso — condicional para estado ok:
     {empresa.estado === 'ok' && (
         <FaixaProgresso faturamento={empresa.faturamento} faixa={empresa.faixa} />
     )}

  2. Bloco serviço adicional (sempre visível per D-12):
     div mb-4
       label: text-[11px] uppercase tracking-wider text-white/40 mb-1 — "Serviço adicional"
       valor: text-white/80 text-[13px] — {empresa.additional_service || '—'}

  3. Divider + ServiceForm (existente):
     div border-t border-white/[0.04] pt-4
       ServiceForm empresa={empresa} onClose={onClose} (sem alteração)

Modificar export default Financeiro para inserir TotalConsolidado acima da lista per D-03:
  - No body do main, dentro de max-w-4xl mx-auto space-y-6, após o bloco do header (título +
    subtítulo), e antes da div rounded-xl que contém FechamentoList, inserir:
    <TotalConsolidado empresas={companies} />
  - O space-y-6 do container já adiciona o espaçamento vertical correto entre os blocos.

Atualizar o subtítulo da página (p text-[13px] text-white/40):
  De: "Tipo de serviço e datas de contrato por empresa ativa."
  Para: "Faturamento mensal, faixa de investimento e dados de contrato por empresa ativa."
</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('resources/js/Pages/Admin/Financeiro.jsx','utf8'); const checks=['FAIXAS_LIMITES','fmtBRL','TotalConsolidado','FaixaProgresso','faixa === .maxima.','bg-ecf-yellow/30','additional_service','per D-','minimumFractionDigits: 0']; checks.forEach(s=>{ if(!c.includes(s)) throw new Error('FALTA: '+s); }); console.log('OK — todos os 9 marcadores presentes');"</automated>
  </verify>
  <done>
Financeiro.jsx contém FAIXAS_LIMITES, fmtBRL, TotalConsolidado, FaixaProgresso, o guard
faixa === 'maxima', bg-ecf-yellow/30, exibição de additional_service, e fmtBRL com
minimumFractionDigits: 0. Todos os sub-componentes Phase 5 preservados.
  </done>
</task>

<task type="auto">
  <name>Task 2: npm run build — verificar 0 erros</name>
  <files>resources/js/Pages/Admin/Financeiro.jsx</files>
  <action>
Executar o build do Vite para verificar que o JSX expandido compila sem erros. Nenhum arquivo
adicional deve ser modificado nesta task — apenas verificação do resultado do build.

Comando a executar no diretório raiz do projeto:
  npm run build

Verificar na saída:
  - Ausência de linhas contendo "error" ou "ERROR" (case insensitive) referentes ao build
  - Presença de linha "built in" indicando conclusão bem-sucedida
  - Presença de "dist/build" ou "public/build" na saída de assets

Se o build falhar com erro de JSX (ex: "Unexpected token"), ler o erro, identificar a linha
em Financeiro.jsx e corrigir antes de prosseguir para a Task 3.

Se o build falhar com erro de import não resolvido (TrendingUp não encontrado), verificar que
o import de lucide-react na linha 4 inclui TrendingUp.

Nota: per convenção do projeto (CLAUDE.md e memory), npm run build é obrigatório após qualquer
edição JSX.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
A saída do build contém "built in" e não contém erros de compilação JSX. Os assets em
public/build são atualizados com timestamp recente.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual — Financeiro.jsx expandido</name>
  <what-built>
Task 1 expandiu Financeiro.jsx com: (a) bloco TotalConsolidado no topo da página,
(b) faturamento formatado + período coberto visível na linha de cada empresa com estado ok,
(c) barra de progresso FaixaProgresso no accordion (ou badge "Faixa máxima" para maxima),
(d) campo additional_service no accordion, acima do ServiceForm.
Task 2 confirmou build sem erros.
  </what-built>
  <how-to-verify>
Abrir http://localhost/ecf_admin/public/administrativo/financeiro (ou URL equivalente do
ambiente local) com usuário admin logado. Verificar cada item da lista:

1. Sidebar "Fechamento" presente (não "Financeiro") — label correto no menu lateral
2. Bloco de total consolidado visível no topo — dois cards lado a lado com "Faturado (mês)" e
   "A cobrar (mês)"; exibe fmtBRL sem centavos (ex: "R$ 1.000.000" e não "R$ 1.000.000,00")
3. Empresa com estado ok: linha exibe nome + sub-linha com faturamento e período
   ex: "R$ 1.000.000 · 01/05 a 18/05" abaixo do nome
4. Empresa com estado sem_dados ou sem_integracao: linha exibe apenas nome, sem sub-linha de
   faturamento (condicional estado === 'ok' correto)
5. Expandir accordion de empresa com estado ok e faixa diferente de maxima:
   - Barra de progresso visível com porcentagem no canto direito
   - Texto "Falta R$ X para a próxima faixa" abaixo da barra
6. Expandir accordion de empresa com faixa maxima (se houver no ambiente):
   - Ícone TrendingUp + texto "Faixa máxima" visível em ecf-yellow
   - Sem barra de progresso
7. Campo "Serviço adicional" visível no accordion, acima do formulário de edição;
   mostra valor ou "—" se vazio
8. Formulário de edição (tipo de serviço + datas de contrato) ainda abre, salva e fecha
   normalmente — regressão zero
9. Não há erros de console JavaScript (abrir DevTools > Console antes de navegar)

Se algum item falhar, reportar o número do item e o comportamento observado para correção.
  </how-to-verify>
  <resume-signal>
Digite "aprovado" se todos os 9 itens estiverem corretos, ou descreva os itens com problema
(ex: "item 5 falhou — barra não aparece") para que a correção seja feita antes de marcar a
fase como completa.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| HTTP request → AdminController | Rota protegida por EnsureUserHasRole; nenhuma mudança de rota nesta fase |
| Inertia props → React | Dados de faturamento chegam como int/null; fmtBRL faz guard null-safe |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Plano de mitigação |
|-----------|-----------|------------|------------|-------------------|
| T-07-01 | Information Disclosure | Financeiro.jsx exibe faturamento de todas as empresas | accept | Rota já protegida por EnsureUserHasRole (role:admin) — nenhuma mudança de controle de acesso nesta fase |
| T-07-02 | Tampering | Cálculo pct em FaixaProgresso com faturamento fora do intervalo da faixa | mitigate | Math.min(100, Math.max(0, pct)) obrigatório — impede overflow visual; guard faixaData null antes de acessar .proximo |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — npm install não é executado |
</threat_model>

<verification>
Após Task 3 aprovada:

1. Suite PHP intacta — nenhuma mudança de backend nesta fase:
   /c/xampp/php/php vendor/bin/phpunit --stop-on-failure
   Resultado esperado: 57/57 (exceto ExampleTest pré-existente)

2. Arquivo alvo existe e contém todos os marcadores:
   grep -c "FAIXAS_LIMITES\|fmtBRL\|TotalConsolidado\|FaixaProgresso" resources/js/Pages/Admin/Financeiro.jsx
   Resultado esperado: 4 (uma ocorrência de cada constante/componente no mínimo)

3. Build limpo confirmado (Task 2 já verificou).
</verification>

<success_criteria>
1. FCH-06: Barra de progresso FaixaProgresso exibida no accordion de empresas com estado ok
   e faixa !== 'maxima'; badge "Faixa máxima" exibido para faixa === 'maxima' sem barra
2. FCH-07: TotalConsolidado no topo soma apenas empresas com estado ok; período coberto
   visível na linha da empresa sem precisar expandir
3. FCH-08: Campo additional_service visível no accordion (valor ou "—"), sem lógica de edição
4. CFG-01: Label "Fechamento" no sidebar já implementado na Phase 5 — sem regressão
5. Regressão zero: ServiceForm (salvar tipo de serviço e datas) continua funcional
6. npm run build: 0 erros de compilação
7. Checkpoint humano aprovado com os 9 itens da checklist
</success_criteria>

<output>
Criar `.planning/phases/07-ui-fechamento/07-01-SUMMARY.md` ao concluir, seguindo o template
em @/c/Users/User/.claude/get-shit-done/templates/summary.md.
</output>
