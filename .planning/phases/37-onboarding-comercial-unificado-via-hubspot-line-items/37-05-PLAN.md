---
phase: 37-onboarding-comercial-unificado-via-hubspot-line-items
plan: 05
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-02
  - 37-04
files_modified:
  - app/Http/Controllers/ComercialController.php
  - routes/web.php
  - resources/js/Pages/Comercial/EmpresasListagem.jsx
  - resources/js/Layouts/AppLayout.jsx
  - tests/Feature/Phase37ComercialListagemTest.php
autonomous: true
requirements:
  - REQ-37-05
  - REQ-37-06
  - REQ-37-08
  - REQ-37-10
user_setup: []
tags: [comercial, listagem, filtros, pendencias, grupos, ui, snake_case]

must_haves:
  truths:
    - "Nova rota GET /comercial/empresas/listagem renderiza Comercial/EmpresasListagem.jsx"
    - "Listagem exibe TODAS as empresas (todos os setores) com filtros snake_case empilháveis: servico, setor, ordem, pendencia, q"
    - "Filtros preservam-se entre aplicações (lição Phase 18 — applyFilter preserva os demais)"
    - "Header tem 5 cards de pendência comercial: sem_servico, sem_valor, servico_nao_reconhecido, sem_setor, dados_close_incompletos"
    - "Pendências comerciais SÓ aparecem para empresas com origem HubSpot (EXISTS hubspot_eventos.company_id_criada = companies.id)"
    - "Empresas legacy (sem registro em hubspot_eventos) aparecem na listagem SEM badge de pendência comercial"
    - "Aba/seção de Grupos permite CRUD (criar, editar, deletar) reaproveitando rotas company-groups.* existentes"
    - "Menu sidebar Comercial ganha sub-item 'Empresas (todos os setores)' apontando para /comercial/empresas/listagem"
  artifacts:
    - path: "app/Http/Controllers/ComercialController.php"
      provides: "Método listagem(Request) com filtros snake_case + pendências comerciais"
      contains: "public function listagem"
    - path: "routes/web.php"
      provides: "GET /comercial/empresas/listagem nomeada comercial.empresas.listagem"
      contains: "comercial.empresas.listagem"
    - path: "resources/js/Pages/Comercial/EmpresasListagem.jsx"
      provides: "Página React com header de filtros, cards de pendência, tabela paginada, gestão de grupos"
      contains: "EmpresasListagem"
      min_lines: 250
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "Sub-item 'Empresas (todos os setores)' no grupo Comercial"
      contains: "comercial.empresas.listagem"
    - path: "tests/Feature/Phase37ComercialListagemTest.php"
      provides: "Suite cobrindo controller (filtros, pendências, origem HubSpot)"
      contains: "Phase37ComercialListagemTest"
  key_links:
    - from: "resources/js/Pages/Comercial/EmpresasListagem.jsx"
      to: "/comercial/empresas/listagem (Inertia visit)"
      via: "router.get com params em snake_case"
      pattern: "router.(get|visit)"
    - from: "app/Http/Controllers/ComercialController::listagem"
      to: "hubspot_eventos.company_id_criada"
      via: "EXISTS subquery / whereExists / direct join"
      pattern: "hubspot_eventos"
---

<objective>
Nova listagem Comercial em `/comercial/empresas/listagem` que cobre TODAS as empresas (todos os setores) com filtros snake_case empilháveis (servico, setor, ordem, pendencia, q), pendências comerciais isoladas (sem_servico, sem_valor, servico_nao_reconhecido, sem_setor, dados_close_incompletos) calculadas apenas para empresas de origem HubSpot, e gestão de grupos integrada (CRUD reaproveitando rotas company-groups.*). Sidebar ganha o ponto de entrada.

Purpose: REQ-37-05 (rota+controller+UI), REQ-37-06 (pendências comerciais isoladas), REQ-37-08 (CRUD de grupos migra para Comercial), REQ-37-10 (legacy não geram pendência).

Output: 1 método controller, 1 rota nova, 1 página Inertia React (~250+ linhas), modificação cirúrgica no AppLayout, suíte de testes Phase37ComercialListagemTest (8+ cenários: filtros empilháveis, pendências apenas para origem HubSpot, paginação, busca).

Compatibilidade: rota antiga `comercial.empresas` (redirect para `comercial.empresas.novo`) preservada — esta plan adiciona um NOVO endpoint, não substitui. Rota `company-groups.*` continua viva e é reaproveitada pela UI nova.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-dashboard-precisa-com-filtros-empilhaveis/18-01-SUMMARY.md

<interfaces>
<!-- Padrão snake_case empilhável Phase 18: -->
<!-- Backend (controller): -->
<!--   $filters = ['servico' => $request->input('servico'), 'setor' => ..., ...]; -->
<!--   return Inertia::render('...', ['filters' => $filters, ...]); -->
<!-- Frontend (JSX): -->
<!--   const { filters } = usePage().props; -->
<!--   const applyFilter = (key, value) => router.get(route('...'), {...filters, [key]: value || undefined}, {preserveState: true, preserveScroll: true}); -->

<!-- Pendências comerciais (definição Phase 37 — REQ-37-06): -->
<!-- - sem_servico: empresa origem HubSpot + 0 ContratoServico ativos -->
<!-- - sem_valor: empresa origem HubSpot + tem ContratoServico ativo + valor_contratado = 0 -->
<!-- - servico_nao_reconhecido: empresa origem HubSpot + HubspotEvento.payload contém line_items_nao_mapeados (Plan 37-04) -->
<!-- - sem_setor: empresa origem HubSpot + tem ContratoServico mas TODOS os servicos tem setor='outros' -->
<!-- - dados_close_incompletos: empresa origem HubSpot + nicho IS NULL OR dor IS NULL OR faturamento_mensal IS NULL -->

<!-- Origem HubSpot (REQ-37-10): -->
<!-- EXISTS(SELECT 1 FROM hubspot_eventos WHERE company_id_criada = companies.id) -->
<!-- Implementação: Eloquent withExists/whereExists OU subquery direta. -->
<!-- Companies sem esse registro NÃO geram pendência comercial — flag is_origem_hubspot=false. -->

<!-- Tabela companies: -->
<!-- - id, name, cnpj, status, active, segment, notes, cust_id_status, marketplace, -->
<!--   nicho, dor, vende_ml, faturamento_mensal, marketplaces_extras (json), -->
<!--   email_cliente, email_colaborador, telefone, empresa_nova, visto_em, visto_por, -->
<!--   company_group_id, parent_company_id, ml_store_id, adman_account_id, adman_store_id -->

<!-- Tabela hubspot_eventos: -->
<!-- - company_id_criada (FK nullable) ← origem do match -->
<!-- - payload (json) ← contém line_items_nao_mapeados quando Plan 37-04 detecta -->

<!-- Rotas company-groups.* existentes (routes/web.php linhas 317-321): -->
<!-- - POST /company-groups → company-groups.store -->
<!-- - PUT /company-groups/{group} → company-groups.update -->
<!-- - DELETE /company-groups/{group} → company-groups.destroy -->
<!-- - POST /company-groups/{group}/atribuir-servico → company-groups.atribuir-servico -->
<!-- Estas rodam dentro do grupo role:admin no routes/web.php — Comercial não-admin -->
<!-- não pode CRUDar grupos via essas rotas. **Decisão pragmática**: gerentes -->
<!-- comerciais não-admin verão a aba "Grupos" em READ-ONLY (lista + filtro por grupo); -->
<!-- ações CRUD ficam disponíveis APENAS para admin via os mesmos botões + endpoints. -->

<!-- Companies/Index.jsx atual tem aba Grupos via componente GruposManager — -->
<!-- replicar/importar esse componente na nova página (Plan 37-06 vai remover -->
<!-- a aba de lá; aqui apenas usamos em Comercial). -->

<!-- Paginação: usar Laravel paginator padrão (->paginate(50)). -->
</interfaces>

@app/Http/Controllers/ComercialController.php
@app/Http/Controllers/CompanyController.php
@routes/web.php
@resources/js/Pages/Companies/Index.jsx
@resources/js/Components/GruposManager.jsx
@app/Models/Company.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComercialController::listagem com filtros snake_case + pendências origem HubSpot</name>
  <files>
    app/Http/Controllers/ComercialController.php,
    routes/web.php
  </files>
  <read_first>
    - app/Http/Controllers/ComercialController.php (arquivo a estender)
    - app/Http/Controllers/CompanyController.php (linhas 33-204 — pattern de pendências + eager loading + snake_case filters)
    - app/Http/Controllers/DashboardController.php (snake_case applyFilter pattern Phase 18)
    - routes/web.php (linhas 160-189 — grupo comercial.* existente)
    - app/Models/HubspotEvento.php (relação companyCriada)
  </read_first>
  <behavior>
    Comportamento:
    - GET /comercial/empresas/listagem renderiza Inertia::render('Comercial/EmpresasListagem', [...])
    - Sem filtros: lista todas as empresas active=true, paginadas (50/page), ordenadas por created_at DESC (mais recentes primeiro)
    - ?servico=ID → whereHas('contratosServico.servico', fn$q => $q->where('servicos.id', ID)->where('contratos_servico.ativo', true))
    - ?setor=performance → whereHas('contratosServico.servico', fn$q => $q->where('servicos.setor', 'performance')->where('contratos_servico.ativo', true))
    - ?ordem=recentes → orderBy('created_at', 'desc'); ?ordem=antigas → asc
    - ?pendencia=sem_servico → filtra para empresas com a flag is_origem_hubspot=true E que casam com a definição da pendência
    - ?q=ACME → where('name', 'like', '%ACME%') ou cnpj like
    - Payload exporta `filters` em snake_case com os 5 valores (servico, setor, ordem, pendencia, q)
    - Cada empresa no payload tem campo `is_origem_hubspot` (bool) e array `pendencias_comerciais` (lista de slugs)
    - Payload exporta `servico_counts` (lista [{id, nome, setor, total}]) para chips de filtro
    - Payload exporta `pendencia_counts` (map slug → count) para cards de header
    - Payload exporta `grupos` (lista com count de empresas) para a aba Grupos
    - Payload exporta `servicos_disponiveis` (catálogo ativo)
  </behavior>
  <action>
    **A — Adicionar `listagem(Request $request)` em `ComercialController`:**

    Permissão: `abort_unless(auth()->user()->hasPermission('comercial.cadastrar_empresa') || auth()->user()->isAdmin(), 403)` (mesmo padrão dos demais métodos).

    1) Sanitizar filtros snake_case:
    ```
    $filters = [
        'servico'   => $request->input('servico'),
        'setor'     => in_array($request->input('setor'), ['performance', 'publicacao', 'outros'], true) ? $request->input('setor') : null,
        'ordem'     => in_array($request->input('ordem'), ['recentes', 'antigas'], true) ? $request->input('ordem') : 'recentes',
        'pendencia' => in_array($request->input('pendencia'), ['sem_servico', 'sem_valor', 'servico_nao_reconhecido', 'sem_setor', 'dados_close_incompletos'], true) ? $request->input('pendencia') : null,
        'q'         => $request->input('q'),
    ];
    ```

    2) Query base:
    ```
    $query = Company::with([
        'contratosServico' => fn($q) => $q->where('ativo', true)->with('servico'),
        'consultor',
        'estrategista',
        'grupo:id,name,color',
    ])
    ->where('active', true)
    // Flag de origem HubSpot — coluna virtual via withExists.
    ->withExists(['hubspotEventoOrigem'])
    ;
    ```

    **PRÉ-REQUISITO** — adicionar relação `hubspotEventoOrigem()` no `app/Models/Company.php`:
    ```
    public function hubspotEventoOrigem(): \Illuminate\Database\Eloquent\Relations\HasOne {
        return $this->hasOne(\App\Models\HubspotEvento::class, 'company_id_criada');
    }
    ```
    Comentário pt-BR no model: "Phase 37 Plan 37-05 (REQ-37-10) — relação para flag is_origem_hubspot via withExists; empresas SEM registro em hubspot_eventos.company_id_criada são legacy e NÃO geram pendência comercial."

    3) Aplicar filtros:
    ```
    if ($filters['servico']) {
        $query->whereHas('contratosServico', fn($q) =>
            $q->where('servico_id', $filters['servico'])->where('ativo', true)
        );
    }
    if ($filters['setor']) {
        $query->whereHas('contratosServico.servico', fn($q) =>
            $q->where('setor', $filters['setor'])
        )->whereHas('contratosServico', fn($q) => $q->where('ativo', true));
    }
    if ($filters['q']) {
        $q = '%' . trim($filters['q']) . '%';
        $query->where(fn($w) => $w->where('name', 'like', $q)->orWhere('cnpj', 'like', $q));
    }
    $query->orderBy('created_at', $filters['ordem'] === 'antigas' ? 'asc' : 'desc');
    ```

    4) Executar query, materializar coleção, mapear para payload com `pendencias_comerciais`:

    Para cada Company, calcular pendências usando helper privado novo:
    ```
    private function calcularPendenciasComerciais(Company $c): array {
        if (!$c->hubspot_evento_origem_exists) {
            return []; // legacy não tem pendência comercial (REQ-37-10)
        }
        $p = [];
        $contratosAtivos = $c->contratosServico->where('ativo', true);
        if ($contratosAtivos->isEmpty()) $p[] = 'sem_servico';
        if ($contratosAtivos->isNotEmpty() && $contratosAtivos->contains(fn($ct) => (float) $ct->valor_contratado === 0.0)) $p[] = 'sem_valor';
        // servico_nao_reconhecido: verifica HubspotEvento.payload.line_items_nao_mapeados via subquery cacheada
        if ($this->temLineItemsNaoMapeados($c)) $p[] = 'servico_nao_reconhecido';
        if ($contratosAtivos->isNotEmpty() && $contratosAtivos->every(fn($ct) => optional($ct->servico)->setor === 'outros' || $ct->servico === null)) $p[] = 'sem_setor';
        if ($c->nicho === null || $c->dor === null || $c->faturamento_mensal === null) $p[] = 'dados_close_incompletos';
        return $p;
    }

    private function temLineItemsNaoMapeados(Company $c): bool {
        // 1 query por empresa (cacheável em memória durante o request)
        return \App\Models\HubspotEvento::where('company_id_criada', $c->id)
            ->whereJsonContains('payload->line_items_nao_mapeados', [['name' => true]]) // pseudo — ajustar
            ->exists();
        // ALTERNATIVA mais portátil (SQLite compat): carregar o payload e inspecionar em PHP
        // Implementação concreta: $ev = HubspotEvento::where('company_id_criada', $c->id)->latest()->first();
        // return !empty($ev?->payload['line_items_nao_mapeados'] ?? null);
    }
    ```

    **Usar a alternativa portátil** (load+inspect em PHP). Para evitar N+1 em produção: usar Eloquent `with(['hubspotEventos' => fn($q) => $q->latest()->limit(1)])` na query base e iterar a coleção.

    Aplicar filtro `pendencia` DEPOIS de calcular: `$companies->filter(fn($c) => in_array($filters['pendencia'], $c->pendencias_comerciais ?? []))`.

    5) `$pendenciaCounts` — calcular contagem absoluta de cada pendência (sem aplicar o filtro 'pendencia'):
    ```
    $pendenciaCounts = ['sem_servico' => 0, 'sem_valor' => 0, 'servico_nao_reconhecido' => 0, 'sem_setor' => 0, 'dados_close_incompletos' => 0];
    foreach ($companiesAll as $c) {
        foreach ($c->pendencias_comerciais as $p) { $pendenciaCounts[$p]++; }
    }
    ```

    6) `$servicoCounts` (DB direto, replica pattern CompanyController::index linhas 174-181) — adicionar coluna `setor` no SELECT.

    7) `$grupos` (CompanyGroup::withCount('companies')->orderBy('name')->get(...))

    8) `$servicosDisponiveis` (Servico::ativo + setor já no SELECT)

    9) Paginação manual + map para payload:
    ```
    $paginator = $companies->paginate(50)->withQueryString();
    ```

    Payload final:
    ```
    return Inertia::render('Comercial/EmpresasListagem', [
        'companies'            => $paginator,
        'filters'              => $filters,
        'pendencia_counts'     => $pendenciaCounts,
        'servico_counts'       => $servicoCounts,
        'grupos'               => $grupos,
        'servicos_disponiveis' => $servicosDisponiveis,
    ]);
    ```

    **B — Rota:**

    Em `routes/web.php`, dentro do grupo `comercial.*` (após linha 188 — `atribuir-servico`):
    ```
    Route::get('/empresas/listagem', [ComercialController::class, 'listagem'])->name('empresas.listagem');
    ```
  </action>
  <verify>
    <automated>php artisan test --filter Phase37ComercialListagemTest</automated>
  </verify>
  <done>
    Endpoint funcional; filtros snake_case empilháveis; pendências apenas para is_origem_hubspot=true; payload conforme spec; rota nomeada comercial.empresas.listagem.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comercial/EmpresasListagem.jsx (página React com filtros, cards, tabela, grupos)</name>
  <files>
    resources/js/Pages/Comercial/EmpresasListagem.jsx,
    resources/js/Layouts/AppLayout.jsx
  </files>
  <read_first>
    - resources/js/Pages/Companies/Index.jsx (pattern de filtros + cards de pendência + ServicoBadges + GruposManager)
    - resources/js/Components/GruposManager.jsx (componente reaproveitado)
    - resources/js/Layouts/AppLayout.jsx (sub-itens do grupo Comercial)
    - resources/js/Pages/Dashboard/Admin.jsx (pattern de applyFilter snake_case empilhável Phase 18)
  </read_first>
  <behavior>
    UI esperada:
    - Header com search (Input value=filters.q), select setor (Todos/Performance/Publicação/Outros), select ordem (Mais recentes / Mais antigas)
    - 5 cards de pendência clicáveis (filtra pendencia=X), mostram pendencia_counts
    - Chips de serviço (clicáveis, filtram servico=X) com totais
    - Tabela com colunas: Empresa, Origem (HubSpot ou Legacy badge), Serviços (ServicoBadges), Setor (badge do setor dominante), Status, Pendências (badges das pendencias_comerciais), Ações
    - Paginação navegável via Link Inertia
    - Aba "Grupos" lateral ou em accordion, reaproveitando GruposManager
    - Botão "Cadastrar empresa" no topo apontando para comercial.empresas.novo
    - Botão "Atribuir serviço" em cada linha (link para comercial.atribuir-servico/{id})
    - Tudo dark theme com tokens ecf-* (CLAUDE.md)
  </behavior>
  <action>
    **A — Criar `resources/js/Pages/Comercial/EmpresasListagem.jsx`:**

    Imports: `usePage`, `router`, `Link` from `@inertiajs/react`; `AppLayout` from layouts; `Input`/`Button`/`Card`/`Badge`/`Table` from `@/Components/ui/*`; `GruposManager` from `@/Components/GruposManager`; ícones lucide (Search, Filter, Plus, Building2, Tag, AlertCircle); `cn` from `@/lib/utils`.

    Componente default `EmpresasListagem({ companies, filters, pendencia_counts, servico_counts, grupos, servicos_disponiveis })`.

    Helper `applyFilter(key, value)` (pattern Phase 18 snake_case):
    ```
    const applyFilter = (key, value) => {
      router.get(route('comercial.empresas.listagem'), {
        ...filters,
        [key]: value || undefined,
      }, { preserveState: true, preserveScroll: true });
    };
    ```

    Local `tab` state (useState('empresas' | 'grupos')) com tabs visuais idênticas ao Companies/Index.jsx.

    Constants pt-BR:
    ```
    const PENDENCIAS_LABELS = {
      sem_servico: 'Sem serviço',
      sem_valor: 'Sem valor',
      servico_nao_reconhecido: 'Serviço não reconhecido',
      sem_setor: 'Sem setor (catálogo)',
      dados_close_incompletos: 'Close incompleto',
    };
    const SETOR_LABELS = {
      performance: 'Performance',
      publicacao: 'Publicação',
      outros: 'Outros',
    };
    ```

    Estrutura JSX (alto nível):
    ```jsx
    <AppLayout title="Comercial · Empresas">
      <div className="space-y-4">
        {/* Tabs Empresas / Grupos */}
        <Tabs />

        {tab === 'empresas' && (
          <>
            {/* Header: search + select setor + select ordem + botão Cadastrar */}
            <div className="flex gap-2 flex-wrap items-center">
              <Input placeholder="Buscar empresa..." defaultValue={filters.q || ''}
                onChange={debounce(e => applyFilter('q', e.target.value), 400)} />
              <select value={filters.setor || ''} onChange={e => applyFilter('setor', e.target.value)}>
                <option value="">Todos os setores</option>
                <option value="performance">Performance</option>
                <option value="publicacao">Publicação</option>
                <option value="outros">Outros</option>
              </select>
              <select value={filters.ordem || 'recentes'} onChange={e => applyFilter('ordem', e.target.value)}>
                <option value="recentes">Mais recentes</option>
                <option value="antigas">Mais antigas</option>
              </select>
              <Link href={route('comercial.empresas.novo')}>
                <Button className="bg-ecf-yellow text-black hover:bg-ecf-yellow/90">Cadastrar empresa</Button>
              </Link>
            </div>

            {/* 5 Cards de pendência */}
            <div className="grid grid-cols-2 md:grid-cols-5 gap-2">
              {Object.entries(PENDENCIAS_LABELS).map(([key, label]) => (
                <button key={key} onClick={() => applyFilter('pendencia', filters.pendencia === key ? null : key)}
                  className={cn('rounded-xl border px-3 py-2 text-left', filters.pendencia === key ? 'border-ecf-yellow' : 'border-white/[0.08]')}>
                  <div className="text-2xl font-bold tabular-nums">{pendencia_counts[key] ?? 0}</div>
                  <div className="text-[12px] text-white/60">{label}</div>
                </button>
              ))}
            </div>

            {/* Chips de serviço */}
            <div className="flex flex-wrap gap-2">
              <button onClick={() => applyFilter('servico', null)}
                className={cn('chip', !filters.servico && 'chip-active')}>Todos</button>
              {servico_counts.map(s => (
                <button key={s.id} onClick={() => applyFilter('servico', String(s.id))}
                  className={cn('chip', String(s.id) === filters.servico && 'chip-active')}>
                  {s.nome} <Badge>{s.total}</Badge>
                </button>
              ))}
            </div>

            {/* Tabela */}
            <Card>
              <CardContent className="p-0">
                <Table>
                  <TableHeader>
                    <TableRow>
                      <TableHead>Empresa</TableHead>
                      <TableHead>Origem</TableHead>
                      <TableHead>Serviços</TableHead>
                      <TableHead>Setor</TableHead>
                      <TableHead>Pendências</TableHead>
                      <TableHead className="text-right">Ações</TableHead>
                    </TableRow>
                  </TableHeader>
                  <TableBody>
                    {companies.data.map(c => (
                      <TableRow key={c.id}>
                        <TableCell>{c.name}</TableCell>
                        <TableCell>{c.is_origem_hubspot ? <Badge>HubSpot</Badge> : <Badge variant="outline">Legacy</Badge>}</TableCell>
                        <TableCell><ServicoBadges contratos={c.contratos_servico || []} /></TableCell>
                        <TableCell>{c.setor_dominante ? SETOR_LABELS[c.setor_dominante] : '—'}</TableCell>
                        <TableCell>{c.pendencias_comerciais?.map(p => <Badge key={p} variant="warning">{PENDENCIAS_LABELS[p]}</Badge>)}</TableCell>
                        <TableCell className="text-right">
                          <Link href={route('comercial.atribuir-servico', c.id)}>
                            <Button size="sm" variant="ghost">Atribuir serviço</Button>
                          </Link>
                          <Link href={route('companies.show', c.id)}>
                            <Button size="icon" variant="ghost"><Eye className="h-4 w-4" /></Button>
                          </Link>
                        </TableCell>
                      </TableRow>
                    ))}
                  </TableBody>
                </Table>
              </CardContent>
            </Card>

            {/* Paginação Inertia */}
            <Paginator links={companies.links} />
          </>
        )}

        {tab === 'grupos' && (
          <GruposManager grupos={grupos} companies={companies.data || []} servicos={servicos_disponiveis} />
        )}
      </div>
    </AppLayout>
    ```

    Componente local `ServicoBadges` — copiar de `Companies/Index.jsx` ou importar se já existe em `@/Components`. Detectar via grep no read; se existir como componente compartilhado, importar; senão, copiar implementação inline para a página.

    Componente local `Paginator` — renderiza `companies.links` (array do Laravel paginator) como botões Inertia Link com preserveScroll.

    Setor dominante (campo `c.setor_dominante`): o controller precisa exportar isso por empresa. Adicionar no payload Task 1:
    ```
    'setor_dominante' => $c->contratosServico->where('ativo', true)->pluck('servico.setor')->first(),
    ```

    Activity log silencioso — Comercial NÃO altera nada via listagem (read-only); CRUD continua via outras rotas.

    `npm run build` ao final (executor faz). Sem ENGLISH em UI.

    **B — Modificar `AppLayout.jsx`:**

    No grupo Comercial (linhas ~85-97), ADICIONAR como primeiro sub-item:
    ```
    { label: 'Empresas (todos os setores)', routeName: 'comercial.empresas.listagem', page: 'Comercial/EmpresasListagem', icon: Building2, permission: 'comercial.cadastrar_empresa' },
    ```

    Manter sub-item existente "Cadastrar empresa". O sub-item "Serviços" e "Grupos" vêm no Plan 37-07 (reorg final + checkpoint humano).

    Após edição, executor roda `npm run build` (cross-cutting CLAUDE.md).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Acessar /comercial/empresas/listagem; conferir filtros empilháveis (selecionar serviço + setor + busca; trocar ordem; nenhum filtro se perde); ver cards de pendência atualizando; alternar para aba Grupos</human-check>
  </verify>
  <done>
    Página renderiza; filtros snake_case empilháveis; cards de pendência clicáveis; chips de serviço; tabela paginada; aba Grupos via GruposManager; sub-item no AppLayout.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suíte Phase37ComercialListagemTest (filtros + pendências + origem HubSpot)</name>
  <files>tests/Feature/Phase37ComercialListagemTest.php</files>
  <read_first>
    - tests/Feature/Phase18DashboardFiltersTest.php (se existir; padrão Phase 18 de filtros snake_case)
    - tests/Feature/Phase34HubspotWebhookTest.php (helper de criar HubspotEvento)
    - app/Http/Controllers/ComercialController.php (após Task 1)
  </read_first>
  <behavior>
    Cenários:
    - test_lista_todas_empresas_active_sem_filtros: cria 3 empresas active=true + 1 inactive; GET retorna 3
    - test_filtro_servico_aplicado: 2 empresas com contrato servico_A, 1 com servico_B; ?servico=A → 2
    - test_filtro_setor_performance: empresas com contratos em servicos performance/publicacao/outros; ?setor=performance → só as performance
    - test_ordem_recentes_vs_antigas: 3 empresas com created_at distintos; ?ordem=antigas inverte ordem
    - test_busca_q_match_name_ou_cnpj: ?q=ACME retorna empresas com 'ACME' no name OU cnpj
    - test_empresa_origem_hubspot_gera_pendencias: empresa com HubspotEvento.company_id_criada apontando + 0 contratos → pendencias_comerciais inclui 'sem_servico'
    - test_empresa_legacy_NAO_gera_pendencias_REQ_37_10: empresa SEM HubspotEvento + 0 contratos → pendencias_comerciais === []
    - test_pendencia_servico_nao_reconhecido: HubspotEvento com payload.line_items_nao_mapeados → pendência listada
    - test_pendencia_sem_setor: empresa origem HubSpot com contrato em servico setor='outros' → pendência 'sem_setor'
    - test_pendencia_dados_close_incompletos: empresa origem HubSpot com nicho=null → pendência listada
    - test_pendencia_counts_corresponde_a_lista_completa: counts do header independem do filtro pendencia
    - test_filtros_empilhaveis_query_string_preservada: aplicar servico=X depois setor=Y → URL final tem ambos preservados (testa que applyFilter passa ...filters)
    - test_acesso_403_sem_permissao: usuário sem comercial.cadastrar_empresa + não admin → 403
  </behavior>
  <action>
    Criar `tests/Feature/Phase37ComercialListagemTest.php` com `RefreshDatabase`.

    Helpers:
    - `criarServico(string $nome, string $setor): Servico` — usa Servico::create
    - `criarMapping(string $name, Servico $s)` — para o teste de servico_nao_reconhecido (via HubspotEvento payload, não via mapping; o mapping serve só como insumo do Plan 37-04)
    - `criarEmpresa(array $overrides = []): Company` — Company::create com active=true, status='ativo' (defaults)
    - `criarHubspotEvento(Company $c, ?array $payload = null): HubspotEvento` — registra evento processado apontando para company_id_criada
    - `criarContrato(Company $c, Servico $s, array $overrides = []): ContratoServico` — wrapper para ContratoServico::create
    - `actingAsAdmin(): User` — cria User role=admin + Auth::login
    - `actingAsComercial(): User` — User com permission comercial.cadastrar_empresa via setor_permissoes

    Padrão do request: `$this->get(route('comercial.empresas.listagem', $params))` retorna InertiaResponse. Asserts:
    - `$response->assertOk()`
    - `$response->assertInertia(fn($page) => $page->component('Comercial/EmpresasListagem')->where('filters.servico', $expected))`
    - `$page->where('pendencia_counts.sem_servico', 1)` etc.

    Para preservação de query string (test_filtros_empilhaveis), validar via payload:
    `$page->where('filters', ['servico' => '1', 'setor' => 'performance', 'ordem' => 'recentes', 'pendencia' => null, 'q' => null])`.

    Skip de tests que requerem Inertia paginator no `assertInertia` — usar `$response->json()` ou `getOriginalContent()->getData()['page']['props']` se necessário.
  </action>
  <verify>
    <automated>php artisan test --filter Phase37ComercialListagemTest</automated>
  </verify>
  <done>
    10+ testes verdes cobrindo filtros, pendências, origem HubSpot, autorização. Suíte da Phase 34 verde (zero regressão).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→Comercial controller | Filtros via query string; sanitização whitelist (in_array nos enums) evita SQL injection |
| Controller→DB | EXISTS subquery em hubspot_eventos é read-only; pendência calculada sem efeito colateral |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-12 | Tampering | filtros via query string | mitigate | Whitelist em PHP (in_array para setor/ordem/pendencia); LIKE com bindings para q (sem concatenação direta) |
| T-37-13 | Information Disclosure | Lista de empresas + pendências | mitigate | Middleware permission:comercial.cadastrar_empresa + abort_unless no método; consultor regular não acessa |
| T-37-14 | Denial of Service | Cálculo de pendências N empresas × HubspotEvento | mitigate | Eager loading `hubspotEventos => latest()->limit(1)` evita N+1; paginação 50/page limita carga; cache stat em memória durante request |
</threat_model>

<verification>
- `php artisan route:list | grep comercial.empresas.listagem` mostra rota nomeada
- `php artisan test --filter Phase37ComercialListagemTest` verde
- `npm run build` verde
- Acesso visual: /comercial/empresas/listagem renderiza tabela + cards + chips
- Origem HubSpot badge aparece apenas em empresas com HubspotEvento associado
</verification>

<success_criteria>
- Endpoint /comercial/empresas/listagem operacional com filtros snake_case empilháveis
- 5 cards de pendência comercial (REQ-37-06) calculados apenas para origem HubSpot (REQ-37-10)
- Aba Grupos integrada via GruposManager (REQ-37-08)
- Sub-item "Empresas (todos os setores)" no AppLayout sidebar grupo Comercial
- Suíte Phase37ComercialListagemTest verde
- Zero regressão Phase 34/35/36
</success_criteria>

<output>
Create `.planning/phases/37-onboarding-comercial-unificado-via-hubspot-line-items/37-05-SUMMARY.md` when done
</output>
