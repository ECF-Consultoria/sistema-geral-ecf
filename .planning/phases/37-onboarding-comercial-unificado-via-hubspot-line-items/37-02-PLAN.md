---
phase: 37-onboarding-comercial-unificado-via-hubspot-line-items
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_18_100003_create_hubspot_line_item_mapping_table.php
  - database/migrations/2026_06_18_100004_seed_hubspot_line_item_mapping.php
  - app/Models/HubspotLineItemMapping.php
  - tests/Feature/Phase37LineItemMappingTest.php
autonomous: true
requirements:
  - REQ-37-02
user_setup: []
tags: [schema, hubspot, line-items, mapping, comercial]

must_haves:
  truths:
    - "Tabela hubspot_line_item_mapping existe com colunas line_item_name (unique), servico_id (FK), ativo, timestamps"
    - "Seed cria mapeamentos canônicos: MAP/Polo/Brigada/Gestão/Mentoria/Publicação → Servico correspondente"
    - "FK servico_id usa onDelete cascade (mapeamento sem servico não faz sentido)"
    - "Coluna line_item_name é UNIQUE (1 nome HubSpot → 1 servico)"
    - "Model HubspotLineItemMapping tem scope ativo() + relação servico() + método estático paraNome(string \$nome)"
    - "paraNome() faz match case-insensitive via LOWER comparison"
    - "Migration de seed é idempotente (usa firstOrCreate por line_item_name)"
  artifacts:
    - path: "database/migrations/2026_06_18_100003_create_hubspot_line_item_mapping_table.php"
      provides: "Schema da tabela hubspot_line_item_mapping"
      contains: "create('hubspot_line_item_mapping'"
    - path: "database/migrations/2026_06_18_100004_seed_hubspot_line_item_mapping.php"
      provides: "Seed inicial mapeando 6+ famílias de line_items HubSpot"
      contains: "firstOrCreate"
    - path: "app/Models/HubspotLineItemMapping.php"
      provides: "Model com relação + scope + helper estático case-insensitive"
      contains: "paraNome"
    - path: "tests/Feature/Phase37LineItemMappingTest.php"
      provides: "Suite cobrindo schema, FK, scope, case-insensitive lookup"
      contains: "Phase37LineItemMappingTest"
  key_links:
    - from: "app/Models/HubspotLineItemMapping.php"
      to: "app/Models/Servico.php"
      via: "belongsTo (servico_id)"
      pattern: "belongsTo\\(Servico::class"
---

<objective>
Tabela `hubspot_line_item_mapping` (line_item_name → servico_id) que vira fonte de verdade do webhook HubSpot (Plan 37-04). Permite ao admin cadastrar mapeamentos novos sem deploy via UI futura (Plan 37-07). Cobertura inicial via seed para os 6 grupos canônicos comuns no HubSpot da ECF.

Purpose: REQ-37-02. Sem esta tabela, o webhook do Plan 37-04 não consegue resolver `line_item.name='Gestão MEDIANO'` → Servico::where('nome', 'Gestão')->first(). Catálogo `servicos` tem nomes Title Case padronizados; HubSpot manda variações livres → mapping desacopla.

Output: 2 migrations idempotentes, model HubspotLineItemMapping com scope + relação + helper paraNome() case-insensitive, suíte Phase37LineItemMappingTest (5 cenários).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Catálogo de servicos canônico (Phase 14 seed 2026_05_27_100001): -->
<!-- 'Publicação', 'Polos', 'Assessoria', 'Incubadora', 'Publicidade', 'Gestão' -->
<!-- Phase 37 Plan 37-01 acrescenta possivelmente 'Mentoria' (se já no catálogo). -->

<!-- Padrão FK + nullable Phase 34 (referência idempotência): -->
<!-- $table->foreignId('company_id_criada')->nullable()->constrained('companies')->nullOnDelete(); -->

<!-- Servico::where('nome', X)->first() — match case-sensitive Title Case. -->
<!-- HubSpot manda line_item.name livre, ex: "Mentoria Premium 12 meses", "Gestão Light", "POLOS SP". -->
<!-- Solução: tabela mapping aceita line_item_name com qualquer capitalização e lookup faz LOWER comparison. -->
</interfaces>

@app/Models/Servico.php
@database/migrations/2026_05_27_100001_seed_servicos_catalog.php
@database/migrations/2026_06_12_300002_create_hubspot_eventos_table.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration create_hubspot_line_item_mapping_table</name>
  <files>database/migrations/2026_06_18_100003_create_hubspot_line_item_mapping_table.php</files>
  <read_first>
    - database/migrations/2026_06_12_300002_create_hubspot_eventos_table.php (padrão de migration HubSpot deste projeto)
    - database/migrations/2026_05_26_120001_create_servicos_table.php (referência tabela servicos)
  </read_first>
  <behavior>
    - Test 1: `Schema::hasTable('hubspot_line_item_mapping')` retorna true após migrate
    - Test 2: campos id, line_item_name (string), servico_id (FK), ativo (bool default true), timestamps existem
    - Test 3: UNIQUE em line_item_name impede 2 rows com mesmo valor (lança QueryException)
    - Test 4: FK servico_id em onDelete cascade — deletar Servico apaga o mapeamento
  </behavior>
  <action>
    Criar migration `database/migrations/2026_06_18_100003_create_hubspot_line_item_mapping_table.php`.

    `up()`:
    - Schema::create('hubspot_line_item_mapping', function (Blueprint $table) { ... })
    - $table->id();
    - $table->string('line_item_name', 255)->unique() (UNIQUE — 1 nome HubSpot mapeia para no máximo 1 servico)
    - $table->foreignId('servico_id')->constrained('servicos')->cascadeOnDelete() (servico deletado → mapeamento sai junto)
    - $table->boolean('ativo')->default(true) (admin pode desativar mapeamento sem deletar)
    - $table->text('observacoes')->nullable() (notas opcionais — ex: "MAP = Mentoria Acelerada Premium")
    - $table->timestamps();
    - Index composto: $table->index(['ativo', 'line_item_name']) (alimenta lookup do webhook).

    `down()`: Schema::dropIfExists('hubspot_line_item_mapping').

    PHPDoc no topo: "Phase 37 Plan 37-02 (REQ-37-02) — tabela de mapeamento line_item HubSpot → Servico do catálogo. Editável pelo admin via UI /sistema/hubspot-line-items (Plan 37-07) — permite cadastrar mapeamentos novos sem deploy quando o Comercial cria nomes de line item novos no HubSpot. Match case-insensitive via LOWER comparison no Model."
  </action>
  <verify>
    <automated>php artisan migrate && php artisan tinker --execute="echo Schema::hasTable('hubspot_line_item_mapping') ? 'OK' : 'FAIL';"</automated>
  </verify>
  <done>
    Tabela criada com colunas + UNIQUE + FK cascade + index; rollback funciona limpa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Model HubspotLineItemMapping + Migration de seed inicial</name>
  <files>
    app/Models/HubspotLineItemMapping.php,
    database/migrations/2026_06_18_100004_seed_hubspot_line_item_mapping.php
  </files>
  <read_first>
    - app/Models/HubspotEvento.php (estilo de model deste módulo)
    - app/Models/Servico.php (relação alvo)
    - database/migrations/2026_05_27_100001_seed_servicos_catalog.php (padrão de seed idempotente via DB::table)
  </read_first>
  <behavior>
    - Test 1: `HubspotLineItemMapping::ativo()->count()` retorna apenas rows com ativo=true
    - Test 2: `$mapping->servico` retorna o Servico associado
    - Test 3: `HubspotLineItemMapping::paraNome('mAp PrEmIuM')` faz match contra row com line_item_name='MAP PREMIUM' (case-insensitive)
    - Test 4: `paraNome('inexistente')` retorna null
    - Test 5: `paraNome($nome)` ignora linhas com ativo=false
    - Test 6: Seed canônico cria pelo menos 6 mapeamentos cobrindo MAP/Polo/Brigada/Gestão/Mentoria/Publicação
    - Test 7: Re-rodar seed é no-op (firstOrCreate)
  </behavior>
  <action>
    **Model `app/Models/HubspotLineItemMapping.php`:**

    Classe `HubspotLineItemMapping extends Model`. `protected $table = 'hubspot_line_item_mapping';`. `$fillable = ['line_item_name', 'servico_id', 'ativo', 'observacoes']`. `$casts = ['ativo' => 'boolean']`.

    Relação `public function servico(): BelongsTo { return $this->belongsTo(Servico::class); }`.

    Scope `public function scopeAtivo($q) { return $q->where('ativo', true); }`.

    Helper estático principal — match case-insensitive:
    ```
    public static function paraNome(string $nome): ?self
    ```
    Implementação: `return self::ativo()->whereRaw('LOWER(line_item_name) = LOWER(?)', [trim($nome)])->with('servico')->first();`. Comentário pt-BR: "Match case-insensitive — HubSpot manda variações livres (`MAP PREMIUM`, `Map Premium`); a UI admin permite cadastrar em qualquer caixa."

    PHPDoc no topo: "Phase 37 Plan 37-02 (REQ-37-02) — mapeamento line_item.name (HubSpot) → Servico do catálogo. Consumido pelo HubspotWebhookController (Plan 37-04) ao processar line items do deal closedwon. Admin gerencia via /sistema/hubspot-line-items (Plan 37-07)."

    **Migration `2026_06_18_100004_seed_hubspot_line_item_mapping.php`:**

    `up()`: dentro de DB::transaction. Para cada par (line_item_name, nome_servico) abaixo:
    - 'MAP' → Servico 'Gestão' (Mentoria Acelerada Premium)
    - 'MAP PREMIUM' → Servico 'Gestão'
    - 'Polo' → Servico 'Polos'
    - 'POLO' → Servico 'Polos'
    - 'Brigada' → Servico 'Gestão' (cliente legado)
    - 'Gestão' → Servico 'Gestão'
    - 'Mentoria' → Servico 'Mentoria' (se existir; caso contrário, fallback 'Gestão')
    - 'Publicação' → Servico 'Publicação'

    Lookup: `$gestao = DB::table('servicos')->where('nome', 'Gestão')->first()`. Pular silencioso se servico não existir (`if (!$gestao) continue;`). Inserção via `DB::table('hubspot_line_item_mapping')->updateOrInsert(['line_item_name' => $nome], ['servico_id' => $servico->id, 'ativo' => true, 'updated_at' => now(), 'created_at' => DB::raw('IFNULL(created_at, NOW())')])`.

    Alternativa mais clara: usar `firstOrCreate(['line_item_name' => $nome], ['servico_id' => $id, 'ativo' => true])` via Eloquent — idempotente por design.

    `down()`: DB::table('hubspot_line_item_mapping')->whereIn('line_item_name', [...])->delete() (apaga apenas os mapeamentos seedados, não mexe em mappings criados via UI).

    PHPDoc: "Phase 37 Plan 37-02 (REQ-37-02) — seed inicial idempotente. Cobre famílias canônicas do HubSpot ECF. Novos mapeamentos via UI admin (Plan 37-07) sem deploy."
  </action>
  <verify>
    <automated>php artisan migrate && php artisan test --filter Phase37LineItemMappingTest</automated>
  </verify>
  <done>
    Model carrega; relação servico() retorna Servico; paraNome('mAp') retorna mapping com servico Gestão; scope ativo() filtra; seed cria ≥6 rows; re-rodar seed é no-op.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suíte Phase37LineItemMappingTest (schema + helpers + seed)</name>
  <files>tests/Feature/Phase37LineItemMappingTest.php</files>
  <read_first>
    - tests/Feature/Phase34HubspotWebhookTest.php (padrão da suíte HubSpot)
    - app/Models/HubspotLineItemMapping.php (verifica métodos públicos)
  </read_first>
  <behavior>
    Cobertura:
    - test_tabela_existe_e_unique_em_line_item_name: insere 2 rows com mesmo nome → 2ª lança QueryException
    - test_fk_cascade_em_servico_id: cria mapping, deleta Servico → mapping desaparece (whereId)->doesntExist()
    - test_scope_ativo_filtra_apenas_ativos: 2 ativos + 1 inativo → ativo()->count() === 2
    - test_paraNome_case_insensitive: line_item_name='MAP PREMIUM'; paraNome('mAp PrEmIuM') retorna o row
    - test_paraNome_ignora_inativos: mapping ativo=false; paraNome() retorna null
    - test_paraNome_inexistente_retorna_null: paraNome('Inexistente XYZ') === null
    - test_paraNome_retorna_servico_eager_loaded: paraNome($n)->servico->nome carrega sem N+1 query (assertEquals direto)
    - test_seed_inicial_cria_mapeamentos_canonicos: dispara o conteúdo do seed manualmente; assertContains('MAP', DB::table->pluck('line_item_name'))
  </behavior>
  <action>
    Criar `tests/Feature/Phase37LineItemMappingTest.php` extends `Tests\TestCase` com `RefreshDatabase`.

    Helpers locais:
    - `criarServico(string $nome): Servico` → cria via `Servico::create([...])` com `setor='outros'` (default Phase 37-01).
    - `criarMapping(string $nome, Servico $s, bool $ativo = true): HubspotLineItemMapping` → cria via `HubspotLineItemMapping::create([...])`.

    Para o teste de seed: replica inline o conteúdo do seed (não chama Artisan migrate isolado) para isolar do estado de migrations. Comentário no teste: "Replica o pattern do seed para validar o EFEITO; isola a suíte do estado de migrations no SQLite."

    Sem mocks de HubSpot — esta plan é puramente schema + model. Tests rodam contra SQLite RefreshDatabase.
  </action>
  <verify>
    <automated>php artisan test --filter Phase37LineItemMappingTest</automated>
  </verify>
  <done>
    8 testes verdes; cobertura schema + FK + scope + lookup case-insensitive + seed canônico.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot→Mapping | line_item.name é input HubSpot — lookup case-insensitive é defesa contra capitalização variável; UNIQUE impede mapping duplicado |
| Admin UI→Mapping | UI futura (Plan 37-07) permite criar mappings — sem validação, admin malicioso poderia apontar 'MAP' → 'Polos' e bagunçar contratos. Aceitável: módulo admin-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Tampering | hubspot_line_item_mapping table | mitigate | UNIQUE constraint impede duplicação; FK cascade preserva integridade; admin-only (Plan 37-07) restringe quem altera |
| T-37-04 | Information Disclosure | line_item_name + observacoes | accept | Dados internos da operação ECF; sem PII; visível apenas a admin |
</threat_model>

<verification>
- Tabela `hubspot_line_item_mapping` existe
- Seed cria ≥6 mapeamentos cobrindo MAP/Polo/Brigada/Gestão/Mentoria/Publicação
- Re-rodar seed é no-op (firstOrCreate)
- `HubspotLineItemMapping::paraNome('mAp')` faz match case-insensitive
- `npm run build` NÃO necessário (sem JSX)
</verification>

<success_criteria>
- 8 testes Phase37LineItemMappingTest verdes
- Tabela com UNIQUE em line_item_name + FK cascade em servico_id
- Model com scope ativo() + relação servico() + helper paraNome() case-insensitive
- Seed canônico idempotente cobrindo as 6 famílias principais
- Pronto para consumo pelo Plan 37-04 (webhook)
</success_criteria>

<output>
Create `.planning/phases/37-onboarding-comercial-unificado-via-hubspot-line-items/37-02-SUMMARY.md` when done
</output>
