---
phase: 37-onboarding-comercial-unificado-via-hubspot-line-items
plan: 04
type: execute
wave: 2
depends_on:
  - 37-01
  - 37-02
  - 37-03
files_modified:
  - app/Http/Controllers/Api/HubspotWebhookController.php
  - tests/Feature/Phase37WebhookLineItemsTest.php
autonomous: true
requirements:
  - REQ-37-04
user_setup: []
tags: [hubspot, webhook, line-items, contratos-servico, mapping, transaction]

must_haves:
  truths:
    - "Após criarEmpresa, o webhook busca line items via HubspotApiClient::fetchDealLineItems"
    - "Cada line item.name é resolvido via HubspotLineItemMapping::paraNome (case-insensitive)"
    - "Mapping encontrado + ativo → cria ContratoServico com servico_id do mapping, valor_contratado=item.price, tipo_cobranca derivado de recurringbillingfrequency"
    - "monthly|annually → tipo_cobranca='mensal'; ausente ou null → tipo_cobranca='unica'"
    - "Mapping AUSENTE → empresa criada SEM esse contrato + warning no HubspotEvento.payload + status='processado' (webhook retorna 200)"
    - "Toda a criação Company + Mapeamentos + ContratoServico acontece em DB::transaction única"
    - "Idempotência preservada: 2º webhook do mesmo deal NÃO duplica contratos"
    - "Compatibilidade com bloco existente (line_item da Phase 34 via deal.amount) — line items HubSpot têm prioridade quando presentes"
  artifacts:
    - path: "app/Http/Controllers/Api/HubspotWebhookController.php"
      provides: "Método privado processarLineItems(Company, dealId): array; integração no criarEmpresa"
      contains: "processarLineItems"
    - path: "tests/Feature/Phase37WebhookLineItemsTest.php"
      provides: "5+ testes Feature cobrindo cenários webhook + line items"
      contains: "Phase37WebhookLineItemsTest"
  key_links:
    - from: "app/Http/Controllers/Api/HubspotWebhookController.php"
      to: "app/Services/HubspotApiClient::fetchDealLineItems"
      via: "$api->fetchDealLineItems((string) \$evento->object_id)"
      pattern: "fetchDealLineItems"
    - from: "HubspotWebhookController::processarLineItems"
      to: "HubspotLineItemMapping::paraNome"
      via: "lookup case-insensitive"
      pattern: "HubspotLineItemMapping::paraNome"
---

<objective>
Estender o `HubspotWebhookController` (Phase 34-04) para, após criar a empresa, buscar os line items do deal HubSpot via `HubspotApiClient::fetchDealLineItems` (Plan 37-03) e materializar `ContratoServico` para cada line item mapeado em `HubspotLineItemMapping` (Plan 37-02). Tudo dentro da mesma `DB::transaction` que cria a empresa — atomicidade total.

Purpose: REQ-37-04. Este é o coração da Phase 37: empresa criada por webhook nasce COM serviço(s) + valor(es) + setor (derivado do catálogo) preenchidos automaticamente — o Comercial não precisa mais ir em /comercial/atribuir-servico/{id} para empresas vindas do HubSpot.

Output: 1 método privado novo (`processarLineItems`) + integração no fluxo de `criarEmpresa`. Bloco legado (Servico nome bate com deal.servico → ContratoServico com deal.amount) é DEPRECATED em favor dos line items; mantido apenas como fallback quando o deal NÃO tem line items (deals legacy do HubSpot). Suíte Phase37WebhookLineItemsTest com 7 cenários ponta-a-ponta.

Compatibilidade obrigatória: roteamento MlbEmpresa (POLO/ASSESSORIA/INCUBADORA) da Phase 35 continua intacto — line items mapeados para Servico 'Polos'/'Assessoria'/'Incubadora' continuam disparando criação de MlbEmpresa via servicoDisparaImplementacao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-cadastro-comercial-otimizado-hubspot/34-04-SUMMARY.md
@.planning/phases/35-fix-cadastro-hubspot-v2/35-02-SUMMARY.md

<interfaces>
<!-- HubspotWebhookController::criarEmpresa atual (linhas ~237-353 do arquivo): -->
<!-- - private function criarEmpresa(array $deal, ?array $hubCompany, ?array $hubContact = null): Company -->
<!-- - Tudo em DB::transaction(function () use (...) {...}); -->
<!-- - 1) Cria Company com empresa_nova=true, status='pendente', active=true -->
<!-- - 2) Anexa contato (firstname+lastname) em notes -->
<!-- - 3) Bloco "Tenta vincular ContratoServico" (linhas ~296-320): -->
<!--    - lê $servicoNome = $dprops[$propsDeal['servico']] ?? null -->
<!--    - lookup Servico::where('nome', $servicoNome)->where('ativo', true)->first() -->
<!--    - se $servico encontrado: cria ContratoServico com valor = $dprops['amount'] ?? $servico->valor_padrao -->
<!--    - se $servicoNome existe mas não bate: grava em notes -->
<!-- - 4) Roteamento MlbEmpresa via ComercialController::servicoDisparaImplementacao($servicoNome) -->
<!-- - 5) Return $company; -->

<!-- DECISÃO ARQUITETURAL Phase 37 Plan 37-04 (esta plan): -->
<!-- - Line items do HubSpot têm PRIORIDADE sobre o campo "servico" do deal -->
<!-- - Quando fetchDealLineItems retorna ≥1 item, processamos LINE ITEMS exclusivamente -->
<!--   (bloco legado Servico/amount/MlbEmpresa servicoDisparaImplementacao roda por CADA -->
<!--   line item mapeado, não pelo servico do deal) -->
<!-- - Quando fetchDealLineItems retorna [] (deal sem line items), CAI no fluxo legado -->
<!--   (Servico::where nome + deal.amount + MlbEmpresa servicoDisparaImplementacao) -->
<!--   — preserva 100% do comportamento Phase 34/35 para deals legacy -->
<!-- - Atomicidade: TUDO dentro da mesma DB::transaction da criarEmpresa atual; -->
<!--   adicionar um segundo passo "processarLineItems" dentro da mesma transaction -->

<!-- TIPO_COBRANCA do ContratoServico (Phase 14): enum string 'mensal'|'unica' -->
<!-- Mapeamento Phase 37: -->
<!--   recurringbillingfrequency='monthly'   → tipo_cobranca='mensal' -->
<!--   recurringbillingfrequency='annually'  → tipo_cobranca='mensal' (cobra mensal recorrente, sem distinção anual hoje) -->
<!--   recurringbillingfrequency=null/ausente → tipo_cobranca='unica' (setup fee, one-shot) -->

<!-- IDEMPOTÊNCIA Phase 34-04 (já existente — não mexer): -->
<!-- HubspotEvento::where('object_id', $id)->where('id', '!=', $evento->id)->whereNotNull('company_id_criada')->exists() -->
<!-- → 2º webhook do mesmo deal cai como "ignorado" ANTES de chamar criarEmpresa. -->
<!-- Portanto: NÃO precisa de novo guard de idempotência específico para line items — -->
<!-- a idempotência do evento cobre. -->
</interfaces>

@app/Http/Controllers/Api/HubspotWebhookController.php
@app/Services/HubspotApiClient.php
@app/Models/HubspotLineItemMapping.php
@app/Models/ContratoServico.php
@app/Models/Servico.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar processarLineItems + integrar no criarEmpresa</name>
  <files>app/Http/Controllers/Api/HubspotWebhookController.php</files>
  <read_first>
    - app/Http/Controllers/Api/HubspotWebhookController.php (arquivo completo — entender criarEmpresa e seu DB::transaction)
    - app/Models/HubspotLineItemMapping.php (assinatura paraNome)
    - app/Services/HubspotApiClient.php (assinatura fetchDealLineItems)
    - app/Models/ContratoServico.php (campos fillable + relação servico)
    - app/Http/Controllers/ComercialController.php (servicoDisparaImplementacao — reaproveitar)
  </read_first>
  <behavior>
    Comportamento esperado após esta task:
    - Deal closedwon com 2 line items mapeados (MAP=Gestão + Polo=Polos) → 1 Company + 2 ContratoServico (Gestão R$ valor_item1 mensal/unica conforme freq; Polos R$ valor_item2) + 1 MlbEmpresa POLO via servicoDisparaImplementacao('Polos')
    - Deal closedwon com line item NÃO mapeado ('XYZ Premium') → 1 Company SEM ContratoServico p/ esse item + warning no payload do HubspotEvento ("line_items_nao_mapeados" array com [name=XYZ Premium])
    - Deal closedwon SEM line items (fetchDealLineItems retorna []) → cai no bloco legado Phase 34/35 (Servico::where nome do deal + amount + MlbEmpresa via servicoDisparaImplementacao) — comportamento idêntico ao Phase 34/35
    - 2º webhook do mesmo deal (mesmo object_id) → status='ignorado' por idempotência ANTES de chamar criarEmpresa (zero side-effect) — comportamento Phase 34-04 preservado
    - Line item com recurringbillingfrequency='monthly' → ContratoServico.tipo_cobranca='mensal'
    - Line item sem recurringbillingfrequency → tipo_cobranca='unica'
  </behavior>
  <action>
    **Passo A — Injetar fetchDealLineItems no fluxo do `processar()`:**

    No método `processar()` (linhas ~125-223), localizar a chamada `$company = $this->criarEmpresa($deal, $hubCompany, $hubContact);`. ANTES dela, adicionar:

    ```
    $lineItems = $api->fetchDealLineItems((string) $evento->object_id);
    ```

    Comentário pt-BR: "Phase 37 Plan 37-04 — busca line items do deal. Quando vazio, criarEmpresa cai no fluxo legado (Servico::where nome do deal)."

    Passar `$lineItems` como 4º argumento de `criarEmpresa`.

    **Passo B — Atualizar assinatura de `criarEmpresa`:**

    Mudar para:
    ```
    private function criarEmpresa(array $deal, ?array $hubCompany, ?array $hubContact = null, array $lineItems = []): Company
    ```

    PHPDoc atualizado: "Phase 37 Plan 37-04 (REQ-37-04): aceita $lineItems para criar ContratoServico baseado em mapping HubSpot. Quando $lineItems vazio, cai no fluxo legado (Servico nome do deal + deal.amount). MlbEmpresa via servicoDisparaImplementacao é avaliada por CADA servico identificado (line items ou fallback)."

    **Passo C — Branch entre line items e fluxo legado dentro do DB::transaction:**

    No bloco DB::transaction da criarEmpresa, APÓS a criação da Company + notes do contato (linha ~280-293), SUBSTITUIR o bloco existente "Tenta vincular ContratoServico se nome do servico bate" + roteamento MlbEmpresa por:

    ```
    if (!empty($lineItems)) {
        // Phase 37 — line items HubSpot têm prioridade.
        $servicosCriados = $this->processarLineItems($company, $lineItems, $evento);
    } else {
        // Fluxo legado Phase 34/35 — deal sem line items, cai no nome do servico do deal.
        $servicosCriados = $this->processarServicoLegado($company, $dprops, $propsDeal);
    }

    // Roteamento MlbEmpresa: avaliado por CADA servico criado (Polos/Assessoria/Incubadora).
    foreach ($servicosCriados as $nomeServico) {
        $this->rotearImplementacao($company, $nomeServico);
    }
    ```

    `$evento` precisa estar disponível no closure — adicionar ao `use ()`. ALTERNATIVA mais limpa: mover o bloco de line items / legado para FORA do DB::transaction e fazer o transaction abraçar apenas a criação da Company + notes; depois processar line items em transaction separada. **Escolha: manter DENTRO da mesma transaction** (D-04 atomicidade Phase 37) — passar $evento via use.

    **Passo D — Implementar `processarLineItems`:**

    Novo método privado:

    ```
    private function processarLineItems(Company $company, array $lineItems, HubspotEvento $evento): array
    ```

    Implementação:
    - $servicosCriados = [];
    - $naoMapeados = [];
    - foreach ($lineItems as $item):
      - $nome = (string) ($item['name'] ?? '');
      - if ($nome === '') { continue; }
      - $mapping = HubspotLineItemMapping::paraNome($nome);
      - if (!$mapping || !$mapping->servico || !$mapping->servico->ativo):
        - $naoMapeados[] = ['name' => $nome, 'price' => $item['price'] ?? null, 'recurringbillingfrequency' => $item['recurringbillingfrequency'] ?? null];
        - continue;
      - $valor = $item['price'] ?? null;
      - $valor = is_numeric($valor) ? (float) $valor : (float) ($mapping->servico->valor_padrao ?? 0);
      - $freq = $item['recurringbillingfrequency'] ?? null;
      - $tipoCobranca = in_array($freq, ['monthly', 'annually'], true) ? Servico::TIPO_MENSAL : Servico::TIPO_UNICA;
      - ContratoServico::create([
          'company_id' => $company->id,
          'servico_id' => $mapping->servico_id,
          'valor_contratado' => $valor,
          'tipo_cobranca' => $tipoCobranca, // (se ContratoServico tem essa coluna; caso contrário, fica em servico->tipo_cobranca)
          'data_contratacao' => now()->toDateString(),
          'data_vencimento' => null,
          'ativo' => true,
        ]);
      - $servicosCriados[] = $mapping->servico->nome;
    - if (!empty($naoMapeados)):
      - $payload = $evento->payload ?? [];
      - $payload['line_items_nao_mapeados'] = $naoMapeados;
      - $evento->payload = $payload;
      - $evento->save();
      - Log::channel('ecf-webhooks')->warning('[HubSpot Webhook] Line items sem mapeamento', [
          'evento_id' => $evento->id,
          'company_id' => $company->id,
          'itens' => array_column($naoMapeados, 'name'),
        ]);
    - return $servicosCriados;

    **Importante sobre `ContratoServico.tipo_cobranca`:** verificar via leitura do model — se ContratoServico NÃO tem essa coluna (tipo_cobranca vive apenas em Servico), REMOVER esse campo do create. Validar no read inicial; ajustar action accordingly. NÃO inferir.

    **Passo E — Implementar `processarServicoLegado`:**

    Extrair o bloco legacy "Tenta vincular ContratoServico se nome do servico bate" do criarEmpresa atual para método privado:

    ```
    private function processarServicoLegado(Company $company, array $dprops, array $propsDeal): array
    ```

    Implementação: copiar o código atual (linhas ~296-320 do controller existente) sem alterar comportamento. Retornar array com [$servicoNome] se servico encontrado, ou [] se não.

    **Passo F — Implementar `rotearImplementacao`:**

    Extrair o bloco "roteamento MlbEmpresa por servico" para método privado:

    ```
    private function rotearImplementacao(Company $company, string $nomeServico): void
    ```

    Implementação: copiar o código atual (linhas ~322-350) — chamada `ComercialController::servicoDisparaImplementacao($nomeServico)` + switch entre 'polos'/'assessoria'/'incubadora' criando MlbEmpresa. Guard contra duplicação: antes de criar MlbEmpresa, verificar `MlbEmpresa::where('company_id', $company->id)->exists()` → se já existe, pular (cenário: empresa com 2 line items que dão Polo+Assessoria — cria a 1ª, pula a 2ª).

    **Passo G — Reordenar `Log::channel('ecf-webhooks')->info('[HubSpot Webhook] Empresa criada'`:**

    Após a transaction, no `processar()`, o info log existente deve incluir `'servicos_criados' => count($servicosCriados ?? [])` + `'line_items_total' => count($lineItems)`. Variável `$servicosCriados` precisa ser exposta pelo retorno de criarEmpresa OU calculada dentro do mesmo escopo. Solução simples: retornar uma tupla [Company, ['servicos' => [], 'nao_mapeados' => []]] do criarEmpresa OU usar uma propriedade transient no Company (descartável). **Escolha mais limpa**: retornar array `['company' => $company, 'meta' => ['servicos_criados' => [...], 'nao_mapeados' => [...]]]`. Ajustar callers no processar() accordingly.

    Comentários em pt-BR em todos os blocos novos referenciando "Phase 37 Plan 37-04 (REQ-37-04)".
  </action>
  <verify>
    <automated>php artisan test --filter Phase37WebhookLineItemsTest</automated>
  </verify>
  <done>
    Webhook closedwon com line items mapeados cria 1 Company + N ContratoServico + (opcional) MlbEmpresa atomicamente. Sem line items cai no fluxo legado Phase 34/35. Line items não mapeados não falham — viram warning no payload e log. Suíte verde + zero regressão em Phase34HubspotWebhookTest.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suíte Phase37WebhookLineItemsTest (7+ cenários ponta-a-ponta)</name>
  <files>tests/Feature/Phase37WebhookLineItemsTest.php</files>
  <read_first>
    - tests/Feature/Phase34HubspotWebhookTest.php (padrão de teste integral webhook + helper servidor() + Http::fake + HMAC signature)
    - app/Http/Controllers/Api/HubspotWebhookController.php (já modificado pela Task 1)
    - app/Models/HubspotLineItemMapping.php (helper paraNome)
    - app/Models/Servico.php (constants SETOR_*)
  </read_first>
  <behavior>
    Casos cobertos:
    1. test_deal_com_1_line_item_mapeado_cria_contrato_servico: deal MAP price=500 monthly; assertCount(1, Company::all()); assertCount(1, ContratoServico::all()); assertDatabaseHas('contratos_servico', ['servico_id' => $gestaoId, 'valor_contratado' => 500, 'ativo' => true])
    2. test_deal_com_2_line_items_mapeados_cria_2_contratos: assertCount(2, ContratoServico::all()); contratos têm servicos distintos
    3. test_deal_com_line_item_nao_mapeado_grava_em_payload: assertCount(0, ContratoServico::all()); $hubspotEvento->fresh()->payload['line_items_nao_mapeados'][0]['name'] === 'XYZ Premium'; status === 'processado'
    4. test_deal_sem_line_items_cai_no_fluxo_legado: fetchDealLineItems mocka retorno []; deal tem servico nome='Gestão' + amount=1000; assertCount(1, ContratoServico::all()) com valor 1000 (fluxo Phase 34/35 preservado)
    5. test_recurringbillingfrequency_monthly_vira_mensal: line item monthly → ContratoServico associated to servico cujo tipo_cobranca='mensal' (se tipo_cobranca vive em ContratoServico) OU log indica mensal
    6. test_recurringbillingfrequency_ausente_vira_unica: line item sem freq → tipo='unica'
    7. test_idempotencia_segundo_webhook_nao_duplica_contratos: dispara webhook 2x mesmo object_id; assertCount(1, Company::all()); assertCount(1, ContratoServico::all()); 2º HubspotEvento.status === 'ignorado'
    8. test_line_item_polos_cria_mlb_empresa_polo: line item mapping 'Polo'→Servico 'Polos'; assertDatabaseHas('mlb_empresas', ['tipo' => 'POLO', 'company_id' => ...])
    9. test_line_item_com_servico_inativo_no_mapping_pula_silencioso: mapping->ativo=false; line item não vira contrato (paraNome filtra por scope ativo); status='processado'; payload tem line_items_nao_mapeados
    10. test_zero_regressao_phase_34_hubspot: replica 1 teste-âncora da Phase 34 (deal closedwon com Servico nome batendo) — deve continuar passando
  </behavior>
  <action>
    Criar `tests/Feature/Phase37WebhookLineItemsTest.php` extends `Tests\TestCase` com `RefreshDatabase`.

    Helpers locais:
    - `helperServidor(array $headers): array` — copiar de Phase34HubspotWebhookTest (formato HTTP_X_* / $_SERVER)
    - `assinarHubspot(string $body, string $secret, int $ts, string $url): string` — calcula HMAC v3 idêntico à spec do controller (METHOD+URI+body+ts → base64(hmac_sha256))
    - `criarServicoCanonico(string $nome, string $setor = 'outros'): Servico` — cria Servico via Eloquent
    - `criarMapping(string $lineItemName, Servico $servico): HubspotLineItemMapping` — cria mapping ativo
    - `disparaWebhook(array $payload, string $secret = 'test-secret', int $ts = null): \Illuminate\Testing\TestResponse` — assina + manda via $this->call('POST', '/api/webhooks/hubspot', ...)

    Setup (`setUp`):
    - `config(['services.hubspot.client_secret' => 'test-secret'])`
    - `config(['services.hubspot.access_token' => 'fake-token'])`
    - `config(['services.hubspot.stage_fechado_ganho_id' => 'closedwon'])`
    - `config(['services.hubspot.props.deal' => [...]])` (props mínimas; copiar de Phase34HubspotWebhookTest)
    - `config(['services.hubspot.props.company' => [...]])`
    - `config(['services.hubspot.props.contact' => [...]])`

    Para cada teste, mockar Http::fake com:
    - `https://api.hubapi.com/crm/v3/objects/deals/{id}` → deal payload com properties
    - `https://api.hubapi.com/crm/v3/objects/deals/{id}/associations/companies` → results vazio (não precisa de company para esses testes)
    - `https://api.hubapi.com/crm/v3/objects/deals/{id}/associations/contacts` → results vazio
    - `https://api.hubapi.com/crm/v3/objects/deals/{id}/associations/line_items` → results com IDs
    - `https://api.hubapi.com/crm/v3/objects/line_items/{id}*` → properties com {name, price, quantity, hs_product_id, recurringbillingfrequency}

    Para o teste de fluxo legado (caso 4), mockar `/associations/line_items` retornando results=[] e setar prop `servico` no deal payload.

    Asserts: usar `assertDatabaseHas('contratos_servico', [...])` para o estado final + `$response->assertOk()` (webhook responde 200 mesmo com line items não mapeados — comportamento D-04 da Phase 34).
  </action>
  <verify>
    <automated>php artisan test --filter Phase37WebhookLineItemsTest && php artisan test --filter Phase34HubspotWebhookTest</automated>
  </verify>
  <done>
    Suíte Phase37 com 10 testes verdes; suíte Phase34HubspotWebhookTest 100% verde (zero regressão); fluxo end-to-end webhook→fetchDealLineItems→mapping→ContratoServico atômico.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot→Receiver | Already mitigado por HMAC v3 (Phase 34-04); valida ANTES de chamar fetchDealLineItems |
| Receiver→DB | Criação atômica de Company+ContratoServico+MlbEmpresa em DB::transaction — falha parcial faz rollback total |
| line_item.name→Mapping | Lookup case-insensitive evita ataque de capitalização; mapping admin-only previne injeção de servicos falsos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08 | Tampering | line_item.price | mitigate | Cast `is_numeric` + (float) defensivo (já feito no Plan 37-03); fallback para valor_padrao do Servico se inválido |
| T-37-09 | Spoofing | line_item.name não mapeado | accept | Cenário válido: comercial cadastrou serviço novo no HubSpot antes do admin cadastrar no mapping; warning preserva o nome no payload do HubspotEvento para replay manual |
| T-37-10 | Repudiation | Auditoria de quem aceitou o contrato | mitigate | HubspotEvento.payload guarda raw line items + line_items_nao_mapeados; activity log da Company existente (Phase 34 logOnly fields) |
| T-37-11 | DoS | Loop nos line items dentro do DB::transaction | accept | Deal típico tem 1-3 itens; cap natural pela latência HubSpot; processarLineItems é O(n) sem chamadas externas dentro do loop (chamadas já feitas em fetchDealLineItems antes do transaction) |
</threat_model>

<verification>
- Suíte Phase37WebhookLineItemsTest verde (10 testes)
- Suíte Phase34HubspotWebhookTest verde (zero regressão)
- Inspeção: `grep -n 'fetchDealLineItems' app/Http/Controllers/Api/HubspotWebhookController.php` retorna a integração + comentário Phase 37
- Inspeção: `grep -nE 'processarLineItems|processarServicoLegado|rotearImplementacao' app/Http/Controllers/Api/HubspotWebhookController.php` mostra os 3 métodos privados novos
</verification>

<success_criteria>
- Webhook cria empresa+contratos+MlbEmpresa atomicamente quando há line items mapeados
- Line items não mapeados não bloqueiam o webhook (status='processado', warning no payload)
- Fluxo legado Phase 34/35 preservado quando deal não tem line items
- Idempotência Phase 34-04 mantida (2º webhook não duplica)
- recurringbillingfrequency monthly|annually → tipo_cobranca='mensal'; ausente → 'unica'
- Zero regressão na suíte Phase34HubspotWebhookTest
- Tipo_cobranca: depende do schema real de ContratoServico — se não existe coluna lá, configuração vem do Servico associado (ajustar action durante execução conforme leitura)
</success_criteria>

<output>
Create `.planning/phases/37-onboarding-comercial-unificado-via-hubspot-line-items/37-04-SUMMARY.md` when done
</output>
