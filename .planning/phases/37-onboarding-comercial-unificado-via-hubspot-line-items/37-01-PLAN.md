---
phase: 37-onboarding-comercial-unificado-via-hubspot-line-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_18_100001_add_setor_to_servicos_table.php
  - database/migrations/2026_06_18_100002_seed_servicos_setor.php
  - app/Models/Servico.php
  - tests/Feature/Phase37ServicoSetorTest.php
autonomous: true
requirements:
  - REQ-37-03
user_setup: []
tags: [schema, servicos, catalogo, setor, performance, publicacao]

must_haves:
  truths:
    - "Tabela servicos tem coluna setor (enum: performance, publicacao, outros) com default 'outros'"
    - "Servicos do catálogo cujo nome contém 'Gestão' ou 'Mentoria' tem setor='performance' após seed"
    - "Servico cujo nome contém 'Publicação' tem setor='publicacao' após seed"
    - "Demais servicos do catálogo permanecem setor='outros' (default)"
    - "Migration é idempotente (re-rodar não cria coluna duplicada)"
    - "Seed é idempotente (re-rodar não altera servicos já corrigidos)"
    - "Servico::SETORES expõe as 3 constantes legíveis (PERFORMANCE, PUBLICACAO, OUTROS)"
    - "Servico::isPerformance() e Servico::isPublicacao() retornam bool"
  artifacts:
    - path: "database/migrations/2026_06_18_100001_add_setor_to_servicos_table.php"
      provides: "Coluna servicos.setor enum com default outros"
      contains: "$table->enum('setor'"
    - path: "database/migrations/2026_06_18_100002_seed_servicos_setor.php"
      provides: "Atualiza setor dos servicos canônicos (Gestão/Mentoria → performance, Publicação → publicacao)"
      contains: "Gest"
    - path: "app/Models/Servico.php"
      provides: "Constants SETOR_* + helpers isPerformance()/isPublicacao() + scope porSetor"
      contains: "const SETOR_PERFORMANCE"
    - path: "tests/Feature/Phase37ServicoSetorTest.php"
      provides: "3 testes feature cobrindo coluna, seed, helpers"
      contains: "Phase37ServicoSetorTest"
  key_links:
    - from: "app/Models/Servico.php"
      to: "servicos.setor (coluna DB)"
      via: "fillable + cast"
      pattern: "'setor'"
---

<objective>
Fundação de schema da Phase 37: adicionar coluna `servicos.setor` (enum performance/publicacao/outros) ao catálogo unificado da Phase 14 e seed-ar os serviços canônicos (Gestão+Mentoria→performance, Publicação→publicacao). Habilita o filtro de Performance em `/companies` (Plan 37-06) e a categorização visual na listagem Comercial (Plan 37-05) sem criar coluna paralela em `companies`.

Purpose: REQ-37-03 (coluna setor + seed canônico). Cross-cutting constraint da Phase 37 — setor sempre derivado do catálogo `servicos`, não do `companies`.

Output: 2 migrations idempotentes, model `Servico` com constants + helpers + scope, suíte de testes Phase37ServicoSetorTest com 3 cenários (coluna existe + default, seed atualiza Gestão/Mentoria/Publicação, helpers retornam booleans corretos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Schema atual da tabela `servicos` (Phase 14 — 2026_05_26_120001_create_servicos_table.php) -->
<!-- Colunas existentes: id, nome (string 120), valor_padrao (decimal 12,2 default 0), -->
<!-- tipo_cobranca (string), ativo (boolean default true), timestamps. -->
<!-- NÃO existe coluna `setor` ainda — esta plan adiciona. -->

<!-- Model atual app/Models/Servico.php (existente — modificar): -->
<!-- - $fillable: ['nome', 'valor_padrao', 'tipo_cobranca', 'ativo'] (adicionar 'setor') -->
<!-- - $casts: ['valor_padrao' => 'decimal:2', 'ativo' => 'boolean'] (manter, adicionar 'setor' string puro) -->
<!-- - TIPO_MENSAL = 'mensal', TIPO_UNICA = 'unica' (manter padrão de constants) -->
<!-- - tiposCobranca() retorna mapa tipo→label (replicar pattern em setoresLabels()) -->
<!-- - scopeActive($q) → $q->where('ativo', true) (replicar pattern em scopePorSetor) -->
<!-- - LogOptions logOnly(['nome', 'valor_padrao', 'tipo_cobranca', 'ativo']) → ADICIONAR 'setor' -->

<!-- Seed atual em 2026_05_27_100001_seed_servicos_catalog.php popula nomes canônicos: -->
<!-- 'Publicação', 'Polos', 'Assessoria', 'Incubadora', 'Publicidade', 'Gestão'. -->
<!-- Phase 37 acrescenta possíveis nomes adicionais: 'Mentoria' (não está no seed Phase 14 — -->
<!-- verificar com SELECT antes; se ausente, seed apenas atualiza Gestão para performance e -->
<!-- pula Mentoria silenciosamente). -->
</interfaces>

@app/Models/Servico.php
@database/migrations/2026_05_26_120001_create_servicos_table.php
@database/migrations/2026_05_27_100001_seed_servicos_catalog.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration add_setor_to_servicos_table</name>
  <files>database/migrations/2026_06_18_100001_add_setor_to_servicos_table.php</files>
  <read_first>
    - database/migrations/2026_05_26_120001_create_servicos_table.php (esquema atual; copiar estilo de declaração)
    - database/migrations/2026_06_02_180000_add_cust_id_status_to_companies.php (padrão idempotente de enum + Schema::hasColumn guard)
    - app/Models/Servico.php (referência das constants/casts atuais)
  </read_first>
  <behavior>
    - Test 1: após `php artisan migrate`, `Schema::hasColumn('servicos', 'setor')` retorna true
    - Test 2: novos rows inseridos sem `setor` recebem 'outros' (default)
    - Test 3: re-rodar a migration sem rollback é no-op (não tenta criar coluna duplicada)
  </behavior>
  <action>
    Criar migration `database/migrations/2026_06_18_100001_add_setor_to_servicos_table.php`.

    `up()`: usar Schema::table('servicos', function (Blueprint $table) {...}) com guard `if (!Schema::hasColumn('servicos', 'setor'))` para idempotência. Adicionar coluna `$table->enum('setor', ['performance', 'publicacao', 'outros'])->default('outros')->after('tipo_cobranca')`.

    `down()`: Schema::table('servicos', fn($t) => $t->dropColumn('setor')) com guard `if (Schema::hasColumn(...))`.

    Comentário PHPDoc em pt-BR explicando: "Phase 37 Plan 37-01 (REQ-37-03) — adiciona setor ao catálogo de serviços. Habilita filtro de Performance em /companies (whereHas contratosServico.servico setor=performance) sem nova coluna em companies. Idempotente."
  </action>
  <verify>
    <automated>php artisan migrate && php artisan tinker --execute="echo Schema::hasColumn('servicos', 'setor') ? 'OK' : 'FAIL';"</automated>
  </verify>
  <done>
    Coluna `servicos.setor` existe com enum + default='outros'; migration roda 2x sem erro (idempotência); rollback restaura schema anterior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration seed_servicos_setor + Servico model com SETORES const + helpers + scope</name>
  <files>
    database/migrations/2026_06_18_100002_seed_servicos_setor.php,
    app/Models/Servico.php
  </files>
  <read_first>
    - app/Models/Servico.php (modificar in-place)
    - database/migrations/2026_05_27_100001_seed_servicos_catalog.php (padrão de seed via DB::table update)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-02-SUMMARY.md (padrão idempotente Phase 14 — firstOrCreate + DB::transaction)
  </read_first>
  <behavior>
    - Test 1: após seed, Servico cujo nome bate em /Gestão|Mentoria/ tem setor='performance'
    - Test 2: após seed, Servico cujo nome bate em /Publicação/ (case-sensitive Title Case) tem setor='publicacao'
    - Test 3: outros nomes do catálogo (Polos, Assessoria, Incubadora, Publicidade) permanecem 'outros'
    - Test 4: re-rodar seed mantém setor já correto (idempotente)
    - Test 5: `Servico::SETOR_PERFORMANCE === 'performance'`, `Servico::SETOR_PUBLICACAO === 'publicacao'`, `Servico::SETOR_OUTROS === 'outros'`
    - Test 6: `Servico::SETORES` retorna array com as 3 chaves
    - Test 7: `$servico->isPerformance()` retorna true só quando setor='performance'
    - Test 8: `Servico::porSetor('performance')->get()` filtra corretamente via scope
  </behavior>
  <action>
    **Migration `2026_06_18_100002_seed_servicos_setor.php`:**

    `up()`: dentro de DB::transaction — para cada par nome→setor, fazer DB::table('servicos')->where('nome', 'LIKE', $padrao)->update(['setor' => $setor]). Pares:
    - LIKE '%Gestão%' → 'performance'
    - LIKE '%Mentoria%' → 'performance' (silencioso se não existir no catálogo)
    - LIKE '%Publicação%' → 'publicacao'

    `down()`: DB::table('servicos')->update(['setor' => 'outros']) (reset para default).

    Comentário PHPDoc: "Phase 37 Plan 37-01 (REQ-37-03) — atualiza setor dos servicos canônicos. Idempotente: re-rodar não altera dados já corretos. Match LIKE case-sensitive Title Case (D-02 Phase 14 — catálogo já está em Title Case)."

    **Modificações em `app/Models/Servico.php`:**

    1. Adicionar `'setor'` em $fillable.
    2. Adicionar constants públicos: `const SETOR_PERFORMANCE = 'performance'; const SETOR_PUBLICACAO = 'publicacao'; const SETOR_OUTROS = 'outros';`
    3. Adicionar array `public const SETORES = [self::SETOR_PERFORMANCE, self::SETOR_PUBLICACAO, self::SETOR_OUTROS];`
    4. Adicionar método estático `public static function setoresLabels(): array` retornando ['performance' => 'Performance', 'publicacao' => 'Publicação', 'outros' => 'Outros'].
    5. Adicionar métodos `public function isPerformance(): bool { return $this->setor === self::SETOR_PERFORMANCE; }` e `public function isPublicacao(): bool { return $this->setor === self::SETOR_PUBLICACAO; }`.
    6. Adicionar scope `public function scopePorSetor($query, string $setor) { return $query->where('setor', $setor); }`.
    7. Atualizar `getActivitylogOptions()` para incluir 'setor' em `logOnly([...])`.

    PHPDoc no topo da classe: complementar com "Phase 37 Plan 37-01 (REQ-37-03) — coluna setor permite categorização Performance/Publicação/Outros usada por /companies (filtro Performance) e /comercial/empresas/listagem (filtros)."
  </action>
  <verify>
    <automated>php artisan migrate && php artisan test --filter Phase37ServicoSetorTest</automated>
  </verify>
  <done>
    Servico::where('nome', 'Gestão')->first()->setor === 'performance'; Servico::where('nome', 'Publicação')->first()->setor === 'publicacao'; isPerformance()/isPublicacao() funcionam; scope `porSetor('performance')` retorna apenas servicos performance.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suíte Phase37ServicoSetorTest cobrindo coluna + seed + helpers</name>
  <files>tests/Feature/Phase37ServicoSetorTest.php</files>
  <read_first>
    - tests/Feature/Phase14ConsolidacaoServicosTest.php (se existir — padrão da suíte de testes da Phase 14)
    - app/Models/Servico.php (verifica que constants/helpers existem)
  </read_first>
  <behavior>
    Suite mínima cobrindo:
    - test_coluna_setor_existe_e_default_outros: cria Servico sem setor → setor === 'outros'
    - test_seed_atualiza_gestao_para_performance: cria Servico nome='Gestão', roda migration de seed, recarrega, setor === 'performance'
    - test_seed_atualiza_publicacao_para_publicacao: cria Servico nome='Publicação', roda seed, setor === 'publicacao'
    - test_constants_setores_expostas: assertEquals('performance', Servico::SETOR_PERFORMANCE) etc; assertCount(3, Servico::SETORES)
    - test_helper_is_performance_e_is_publicacao: 2 Servicos com setores distintos; assertTrue($srv->isPerformance()); assertFalse outro
    - test_scope_por_setor_filtra: cria 2 Servicos performance + 1 publicacao; Servico::porSetor('performance')->count() === 2
  </behavior>
  <action>
    Criar `tests/Feature/Phase37ServicoSetorTest.php` extends `Tests\TestCase` usando `RefreshDatabase`.

    Para o teste de seed (test_seed_atualiza_*), criar o Servico via DB::table('servicos')->insert sem setor (deixa default 'outros'), depois disparar a lógica do seed via Artisan::call('migrate', ['--path' => 'database/migrations/2026_06_18_100002_seed_servicos_setor.php']) OU re-aplicar manualmente o UPDATE inline no teste para garantir determinismo independente do estado de migrations no SQLite de testes.

    Recomendação concreta: usar o approach "executa o UPDATE manualmente dentro do teste", replicando o conteúdo do seed — assim o teste valida o EFEITO (não a migration em si). Comentário no teste: "Replica o UPDATE da migration 100002 para isolar o teste do estado de migrations no SQLite (RefreshDatabase só roda as migrations padrão)."

    Todos os asserts em pt-BR nos comentários explicando comportamento. Sem helpers desnecessários — testes lineares e legíveis.
  </action>
  <verify>
    <automated>php artisan test --filter Phase37ServicoSetorTest</automated>
  </verify>
  <done>
    6 testes verdes cobrindo coluna, default, seed pattern, constants, helpers, scope.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration→DB | Migrations rodam com privilégio total no schema — risco operacional se rollback for executado em prod sem backup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | Migration de seed | accept | Idempotente + LIKE específico Title Case (não atinge nomes arbitrários); risco baixo (dados internos do catálogo) |
| T-37-02 | Denial of Service | Coluna enum servicos.setor | mitigate | Default 'outros' garante que inserções sem setor não falham por NOT NULL; constraint do enum bloqueia valores inesperados |
</threat_model>

<verification>
- Migrations rodam limpas em prod (Schema::hasColumn guard) e SQLite de testes (RefreshDatabase)
- `Servico::porSetor('performance')->get()` retorna Gestão (e Mentoria se existir no catálogo)
- `getActivitylogOptions()` inclui 'setor' (mudança no setor via UI futura gera entrada no activity log)
- `npm run build` NÃO necessário (sem JSX nesta plan)
</verification>

<success_criteria>
- 6 testes Phase37ServicoSetorTest verdes
- Coluna `servicos.setor` existe com default 'outros'
- Servico catálogo: Gestão e Mentoria → performance; Publicação → publicacao; demais → outros
- Servico::isPerformance() / isPublicacao() / porSetor() funcionam
- Zero regressão na suíte Phase 14 ServicoTest existente
</success_criteria>

<output>
Create `.planning/phases/37-onboarding-comercial-unificado-via-hubspot-line-items/37-01-SUMMARY.md` when done
</output>
