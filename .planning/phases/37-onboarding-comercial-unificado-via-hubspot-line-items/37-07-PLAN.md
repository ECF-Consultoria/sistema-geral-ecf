---
phase: 37-onboarding-comercial-unificado-via-hubspot-line-items
plan: 07
type: execute
wave: 3
depends_on:
  - 37-02
  - 37-05
files_modified:
  - resources/js/Layouts/AppLayout.jsx
  - app/Http/Controllers/Sistema/HubspotLineItemMappingController.php
  - routes/web.php
  - resources/js/Pages/Sistema/HubspotLineItems.jsx
  - tests/Feature/Phase37HubspotLineItemMappingAdminTest.php
autonomous: false
requirements:
  - REQ-37-09
  - REQ-37-02
user_setup: []
tags: [admin, ui, mapping, sidebar, reorg, checkpoint-humano]

must_haves:
  truths:
    - "Sidebar grupo 'Comercial' contém os sub-itens: Empresas (todos os setores), Cadastrar empresa, Grupos, Serviços, HubSpot Line Items"
    - "Item 'Serviços' removido do nível raiz da sidebar (estava em linha 48 do AppLayout)"
    - "Item 'Grupos' aparece como sub-item do Comercial apontando para /comercial/empresas/listagem?tab=grupos"
    - "Rota GET /sistema/hubspot-line-items renderiza HubspotLineItems.jsx (admin-only)"
    - "Página admin permite criar / editar / deletar / ativar-desativar mappings"
    - "Cada row mostra line_item_name, servico (select), ativo (switch), observacoes, ações (Edit, Delete)"
    - "POST/PUT/DELETE em /sistema/hubspot-line-items/{mapping} funcional"
    - "Checkpoint humano após a reorg do menu — usuário valida visualmente que nada quebrou"
  artifacts:
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "Sidebar reorganizado: Comercial expansível com 5 sub-itens; Serviços removido do raiz"
      contains: "HubSpot Line Items"
    - path: "app/Http/Controllers/Sistema/HubspotLineItemMappingController.php"
      provides: "CRUD admin do mapping (index/store/update/destroy)"
      contains: "HubspotLineItemMappingController"
    - path: "routes/web.php"
      provides: "Rotas /sistema/hubspot-line-items.* role:admin"
      contains: "hubspot-line-items"
    - path: "resources/js/Pages/Sistema/HubspotLineItems.jsx"
      provides: "Página admin com listagem editável + form inline create"
      contains: "HubspotLineItems"
      min_lines: 150
    - path: "tests/Feature/Phase37HubspotLineItemMappingAdminTest.php"
      provides: "Suite cobrindo CRUD + autorização"
      contains: "Phase37HubspotLineItemMappingAdminTest"
  key_links:
    - from: "resources/js/Layouts/AppLayout.jsx"
      to: "comercial.empresas.listagem + servicos.index + sistema.hubspot-line-items.index"
      via: "sub-itens do grupo Comercial"
      pattern: "group: 'Comercial'"
    - from: "app/Http/Controllers/Sistema/HubspotLineItemMappingController.php"
      to: "app/Models/HubspotLineItemMapping.php"
      via: "Model CRUD"
      pattern: "HubspotLineItemMapping::"
---

<objective>
Finalizar a Phase 37 com a reorganização da sidebar (REQ-37-09) e a UI admin para CRUD dos mappings HubSpot (REQ-37-02 parte final). O grupo Comercial vira o lar de Empresas (listagem completa), Cadastrar empresa, Grupos, Serviços (movido do nível raiz) e HubSpot Line Items (novo).

Purpose: REQ-37-09 (reorg sidebar) + REQ-37-02 conclusão (UI admin permite cadastrar novos mappings sem precisar de deploy). Esta é a plan FINAL da Phase 37; checkpoint humano garante que a reorg não quebrou navegação para usuários reais (consultor, mentor, publicador) que usam o sidebar diariamente.

Output: AppLayout reorganizado, controller admin novo em namespace Sistema\, página React de listagem editável, suíte de testes, checkpoint humano de validação visual da sidebar.

NÃO AUTÔNOMO: `autonomous: false` por causa do checkpoint humano após a reorg do menu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- AppLayout.jsx atual (linhas 41-140 — alguns trechos relevantes): -->
<!--   linha 48: { label: 'Serviços', routeName: 'servicos.index', page: 'Servicos', ... } ← NÍVEL RAIZ, REMOVER -->
<!--   linhas 85-97: group 'Comercial' com 1 sub-item (Cadastrar empresa) -->
<!-- -->
<!-- Plan 37-05 ADICIONOU sub-item 'Empresas (todos os setores)'. Plan 37-07 acrescenta: -->
<!--   - 'Grupos' (link para /comercial/empresas/listagem?tab=grupos — apenas redirect via query param) -->
<!--   - 'Serviços' (servicos.index, movido do raiz) -->
<!--   - 'HubSpot Line Items' (sistema.hubspot-line-items.index, admin-only via excludeRoles ou permission) -->
<!-- -->
<!-- Permissions atuais: -->
<!--   - servicos.index requer permission='sistema.servicos' (linha 48) -->
<!--   - novo item HubSpot Line Items deve requerer role admin → usar excludeRoles -->
<!--     ['consultor', 'mentor', 'publicador', 'analista', 'gestor', 'lider'] (mesmo pattern -->
<!--     da Configuração NPS — linha 62) OU permission='sistema.hubspot_line_items' (criar). -->
<!--   - Escolha PRAGMÁTICA: usar excludeRoles (não exige nova permission no catálogo). -->
<!-- -->
<!-- Rotas atuais do CompanyGroup (vivem em role:admin guard nas linhas 317-321 do routes/web.php): -->
<!-- POST /company-groups, PUT/DELETE /company-groups/{group}, POST /company-groups/{group}/atribuir-servico -->
<!-- Reaproveitar — não criar duplicação. -->
<!-- -->
<!-- HubspotLineItemMapping CRUD novo — namespace Sistema (consistente com /sistema/setores etc): -->
<!--   GET /sistema/hubspot-line-items → index (lista) -->
<!--   POST /sistema/hubspot-line-items → store (criar) -->
<!--   PUT /sistema/hubspot-line-items/{mapping} → update (editar nome, servico_id, ativo, observacoes) -->
<!--   DELETE /sistema/hubspot-line-items/{mapping} → destroy -->
<!-- Middleware: role:admin (todos os endpoints) -->
</interfaces>

@resources/js/Layouts/AppLayout.jsx
@app/Models/HubspotLineItemMapping.php
@app/Models/Servico.php
@routes/web.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Controller + Rotas /sistema/hubspot-line-items CRUD admin</name>
  <files>
    app/Http/Controllers/Sistema/HubspotLineItemMappingController.php,
    routes/web.php
  </files>
  <read_first>
    - routes/web.php (procurar grupo `role:admin` existente para sistema.* — linhas próximas a 317)
    - app/Models/HubspotLineItemMapping.php (Plan 37-02 — model com helper paraNome)
    - app/Models/Servico.php (catálogo a popular o select da UI)
    - app/Http/Controllers/CompanyGroupController.php (padrão CRUD compacto deste projeto)
  </read_first>
  <behavior>
    - GET /sistema/hubspot-line-items renderiza Inertia::render('Sistema/HubspotLineItems', ['mappings', 'servicos_disponiveis'])
    - POST cria mapping com line_item_name UNIQUE, servico_id existe e ativo
    - PUT edita line_item_name, servico_id, ativo, observacoes
    - DELETE remove mapping (soft via ativo=false NÃO — hard delete; admin sabe o que faz)
    - Não-admin recebe 403 em qualquer endpoint
    - Validação: line_item_name required, max 255, unique exceto-self no update
    - servico_id required exists ativos
    - Activity log em pt-BR para cada ação (created/updated/deleted)
  </behavior>
  <action>
    **A — Criar `app/Http/Controllers/Sistema/HubspotLineItemMappingController.php`:**

    Namespace `App\Http\Controllers\Sistema`. Extends `App\Http\Controllers\Controller`.

    Métodos:

    1) `public function index()`:
    ```
    $mappings = HubspotLineItemMapping::with('servico:id,nome,setor,tipo_cobranca')
        ->orderBy('line_item_name')
        ->get()
        ->map(fn($m) => [
            'id' => $m->id,
            'line_item_name' => $m->line_item_name,
            'servico_id' => $m->servico_id,
            'servico_nome' => $m->servico?->nome,
            'servico_setor' => $m->servico?->setor,
            'ativo' => (bool) $m->ativo,
            'observacoes' => $m->observacoes,
        ]);
    $servicosDisponiveis = Servico::where('ativo', true)
        ->orderBy('nome')
        ->get(['id', 'nome', 'setor', 'tipo_cobranca']);
    return Inertia::render('Sistema/HubspotLineItems', compact('mappings', 'servicosDisponiveis'))
        ->with('servicos_disponiveis', $servicosDisponiveis); // snake_case prop final
    ```

    Manter prop name em snake_case (`mappings`, `servicos_disponiveis`) para consistência Phase 37.

    2) `public function store(Request $request)`:
    Validação:
    ```
    $validated = $request->validate([
        'line_item_name' => 'required|string|max:255|unique:hubspot_line_item_mapping,line_item_name',
        'servico_id'     => ['required', 'integer', Rule::exists('servicos', 'id')->where('ativo', true)],
        'ativo'          => 'sometimes|boolean',
        'observacoes'    => 'nullable|string|max:2000',
    ]);
    $mapping = HubspotLineItemMapping::create($validated + ['ativo' => $validated['ativo'] ?? true]);
    activity('sistema')
        ->causedBy($request->user())
        ->withProperties(['line_item_name' => $mapping->line_item_name, 'servico_id' => $mapping->servico_id])
        ->log('Mapeamento HubSpot criado: "' . $mapping->line_item_name . '"');
    return back()->with('success', 'Mapeamento criado.');
    ```

    3) `public function update(Request $request, HubspotLineItemMapping $mapping)`:
    Validação idem com `Rule::unique('hubspot_line_item_mapping', 'line_item_name')->ignore($mapping->id)`. Activity log "Mapeamento HubSpot atualizado".

    4) `public function destroy(HubspotLineItemMapping $mapping)`:
    $nome = $mapping->line_item_name; $mapping->delete(); activity log "Mapeamento HubSpot removido: $nome".

    PHPDoc no topo da classe: "Phase 37 Plan 37-07 (REQ-37-02) — CRUD admin para hubspot_line_item_mapping. Permite cadastrar/editar mapeamentos sem deploy quando o Comercial cria nomes de line item novos no HubSpot. Consumido pelo HubspotWebhookController (Plan 37-04) via HubspotLineItemMapping::paraNome (case-insensitive)."

    **B — Rotas:**

    Em `routes/web.php`, localizar o grupo `role:admin` (próximo às rotas company-groups linha 317). Adicionar dentro do grupo:

    ```
    // Phase 37 Plan 37-07 (REQ-37-02) — UI admin pra mapping HubSpot line_item → Servico.
    Route::get('/sistema/hubspot-line-items', [Sistema\HubspotLineItemMappingController::class, 'index'])->name('sistema.hubspot-line-items.index');
    Route::post('/sistema/hubspot-line-items', [Sistema\HubspotLineItemMappingController::class, 'store'])->name('sistema.hubspot-line-items.store');
    Route::put('/sistema/hubspot-line-items/{mapping}', [Sistema\HubspotLineItemMappingController::class, 'update'])->name('sistema.hubspot-line-items.update');
    Route::delete('/sistema/hubspot-line-items/{mapping}', [Sistema\HubspotLineItemMappingController::class, 'destroy'])->name('sistema.hubspot-line-items.destroy');
    ```

    Importar no topo: `use App\Http\Controllers\Sistema;` ou direto `use App\Http\Controllers\Sistema\HubspotLineItemMappingController;`.

    Route model binding: registrar binding implícito (`{mapping}` resolve `HubspotLineItemMapping` automaticamente via PSR class injection na assinatura do método).
  </action>
  <verify>
    <automated>php artisan test --filter Phase37HubspotLineItemMappingAdminTest && php artisan route:list | grep hubspot-line-items</automated>
  </verify>
  <done>
    Controller existe com CRUD; 4 rotas nomeadas; middleware role:admin aplicado; activity log em pt-BR; rota model binding funcional.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sistema/HubspotLineItems.jsx (página admin com listagem editável)</name>
  <files>resources/js/Pages/Sistema/HubspotLineItems.jsx</files>
  <read_first>
    - resources/js/Pages/Servicos/Index.jsx (se existir — pattern de listagem CRUD admin compacto deste projeto)
    - resources/js/Pages/Companies/Index.jsx (modal + Inertia useForm pattern)
    - resources/js/Components/ui/dialog.jsx (Dialog do shadcn)
  </read_first>
  <behavior>
    UI:
    - Header: título "Mapeamentos HubSpot Line Items", botão "Novo mapeamento" (abre Dialog)
    - Tabela com colunas: Nome do Line Item, Serviço (select inline OU readonly + Edit button), Setor (badge), Ativo (Switch), Observações, Ações (Edit, Delete)
    - Linha clicável abre modal de edição (mesma estrutura do form create)
    - Form fields: Input line_item_name, Select servico_id (lista servicos_disponiveis), Switch ativo, Textarea observacoes
    - Submit via useForm do Inertia (router.post/put/delete)
    - Activity flash messages aparecem no toast (sistema já tem)
    - Dark theme com tokens ecf-* (CLAUDE.md mandate)
  </behavior>
  <action>
    Criar `resources/js/Pages/Sistema/HubspotLineItems.jsx`.

    Imports:
    ```
    import { useState } from 'react';
    import { useForm, usePage, router } from '@inertiajs/react';
    import AppLayout from '@/Layouts/AppLayout';
    import { Input } from '@/Components/ui/input';
    import { Button } from '@/Components/ui/button';
    import { Card, CardContent } from '@/Components/ui/card';
    import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/Components/ui/table';
    import { Badge } from '@/Components/ui/badge';
    import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from '@/Components/ui/dialog';
    import { Plus, Pencil, Trash2, Link2 } from 'lucide-react';
    import { cn } from '@/lib/utils';
    ```

    Componente default `HubspotLineItems({ mappings, servicos_disponiveis })`.

    State local:
    - `modal` (null | 'create' | mapping object para edit)
    - `confirmDelete` (null | mapping)

    useForm:
    ```
    const form = useForm({
      line_item_name: modal?.line_item_name || '',
      servico_id: modal?.servico_id || '',
      ativo: modal?.ativo ?? true,
      observacoes: modal?.observacoes || '',
    });
    ```

    Constants pt-BR:
    ```
    const SETOR_LABELS = { performance: 'Performance', publicacao: 'Publicação', outros: 'Outros' };
    const SETOR_CLS = {
      performance: 'bg-emerald-500/15 text-emerald-300 border-emerald-500/30',
      publicacao: 'bg-blue-500/15 text-blue-300 border-blue-500/30',
      outros: 'bg-white/[0.05] text-white/60 border-white/[0.08]',
    };
    ```

    Estrutura:
    ```jsx
    <AppLayout title="Mapeamentos HubSpot">
      <div className="space-y-4">
        <div className="flex items-center justify-between">
          <h1 className="text-xl font-semibold text-white">Mapeamentos HubSpot Line Items</h1>
          <Button onClick={() => setModal('create')} className="bg-ecf-yellow text-black hover:bg-ecf-yellow/90">
            <Plus className="h-4 w-4 mr-2" /> Novo mapeamento
          </Button>
        </div>

        <p className="text-sm text-white/60">
          Mapeie nomes de line items vindos do HubSpot (ex: <code>MAP PREMIUM</code>, <code>Polo SP</code>) para serviços do catálogo. O webhook usa este mapping para criar ContratoServico automaticamente quando o deal vira "Fechado Ganho". Match é case-insensitive.
        </p>

        <Card>
          <CardContent className="p-0">
            <Table>
              <TableHeader>
                <TableRow>
                  <TableHead>Nome no HubSpot</TableHead>
                  <TableHead>Serviço</TableHead>
                  <TableHead>Setor</TableHead>
                  <TableHead>Ativo</TableHead>
                  <TableHead>Observações</TableHead>
                  <TableHead className="text-right">Ações</TableHead>
                </TableRow>
              </TableHeader>
              <TableBody>
                {mappings.map(m => (
                  <TableRow key={m.id} className={cn(!m.ativo && 'opacity-50')}>
                    <TableCell className="font-mono text-[13px]">{m.line_item_name}</TableCell>
                    <TableCell>{m.servico_nome || '—'}</TableCell>
                    <TableCell>
                      <Badge className={cn('border', SETOR_CLS[m.servico_setor] || SETOR_CLS.outros)}>
                        {SETOR_LABELS[m.servico_setor] || '—'}
                      </Badge>
                    </TableCell>
                    <TableCell>{m.ativo ? <Badge variant="success">Sim</Badge> : <Badge variant="outline">Não</Badge>}</TableCell>
                    <TableCell className="text-[12px] text-white/60">{m.observacoes || '—'}</TableCell>
                    <TableCell className="text-right">
                      <Button size="icon" variant="ghost" onClick={() => setModal(m)} title="Editar">
                        <Pencil className="h-4 w-4" />
                      </Button>
                      <Button size="icon" variant="ghost" className="text-red-400 hover:text-red-300 hover:bg-red-500/10"
                        onClick={() => setConfirmDelete(m)} title="Remover">
                        <Trash2 className="h-4 w-4" />
                      </Button>
                    </TableCell>
                  </TableRow>
                ))}
                {mappings.length === 0 && (
                  <TableRow>
                    <TableCell colSpan={6} className="text-center text-muted-foreground py-8">
                      <Link2 className="h-8 w-8 mx-auto mb-2 opacity-40" />
                      Nenhum mapeamento cadastrado
                    </TableCell>
                  </TableRow>
                )}
              </TableBody>
            </Table>
          </CardContent>
        </Card>

        {modal && (
          <Dialog open={!!modal} onOpenChange={() => setModal(null)}>
            <DialogContent>
              <DialogHeader>
                <DialogTitle>{modal === 'create' ? 'Novo mapeamento' : 'Editar mapeamento'}</DialogTitle>
              </DialogHeader>
              {/* Form fields aqui — Input, Select, Switch, Textarea */}
              <DialogFooter>
                <Button variant="outline" onClick={() => setModal(null)}>Cancelar</Button>
                <Button onClick={salvar} disabled={form.processing}>Salvar</Button>
              </DialogFooter>
            </DialogContent>
          </Dialog>
        )}

        {confirmDelete && (
          <Dialog open={!!confirmDelete} onOpenChange={() => setConfirmDelete(null)}>
            <DialogContent>
              <DialogHeader>
                <DialogTitle>Remover mapeamento?</DialogTitle>
              </DialogHeader>
              <p className="text-sm text-white/60">
                Mapeamento "<code>{confirmDelete.line_item_name}</code>" será removido. Line items novos com esse nome cairão como "não reconhecido" no webhook.
              </p>
              <DialogFooter>
                <Button variant="outline" onClick={() => setConfirmDelete(null)}>Cancelar</Button>
                <Button variant="destructive" onClick={() => deletar(confirmDelete)}>Remover</Button>
              </DialogFooter>
            </DialogContent>
          </Dialog>
        )}
      </div>
    </AppLayout>
    ```

    Handlers `salvar()` e `deletar(m)` usam `router.post('/sistema/hubspot-line-items', form.data, {onSuccess: ...})` e `router.delete(route('sistema.hubspot-line-items.destroy', m.id))`.

    Após edição, executor roda `npm run build`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Acessar /sistema/hubspot-line-items; ver listagem; clicar Novo mapeamento; preencher form; salvar; ver linha aparecer; editar; deletar; verificar flash messages</human-check>
  </verify>
  <done>
    Página renderiza; CRUD funcional via UI; modal Dialog com form; activity flash exibida; dark theme com tokens ecf-*; build verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AppLayout sidebar reorg — Comercial expansível + Serviços removido do raiz</name>
  <files>resources/js/Layouts/AppLayout.jsx</files>
  <read_first>
    - resources/js/Layouts/AppLayout.jsx (arquivo completo — entender estrutura ITENS_SIDEBAR/menu, padrão group/children, permission/excludeRoles)
  </read_first>
  <behavior>
    Após edição:
    - Item 'Serviços' do nível raiz SUMIU (estava em linha 48)
    - Grupo 'Comercial' agora expansível com sub-itens (na ordem):
      1. 'Empresas (todos os setores)' → comercial.empresas.listagem (já adicionado no Plan 37-05)
      2. 'Cadastrar empresa' → comercial.empresas.novo (existente)
      3. 'Grupos' → comercial.empresas.listagem (com query ?tab=grupos OU rota dedicada)
      4. 'Serviços' → servicos.index (movido do raiz; permission sistema.servicos)
      5. 'HubSpot Line Items' → sistema.hubspot-line-items.index (excludeRoles para deixar admin-only OU permission própria)
    - Outros grupos/itens não-Comercial intactos
  </behavior>
  <action>
    **A — Remover linha 48** (item 'Serviços' do nível raiz):

    ```
    { label: 'Serviços', routeName: 'servicos.index', page: 'Servicos', icon: Briefcase, permission: 'sistema.servicos' },
    ```

    Deletar inteira. Comentário pt-BR acima do bloco vizinho: "Phase 37 Plan 37-07 (REQ-37-09) — item Serviços removido do nível raiz; movido para grupo Comercial abaixo."

    **B — Expandir grupo 'Comercial' (linhas ~85-97) com sub-itens novos:**

    Substituir o bloco atual por:
    ```
    // ── Grupo: Comercial ────────────────────────────────────────────────────
    // Phase 37 Plan 37-07 (REQ-37-09) — sidebar consolidada do setor Comercial.
    // "Serviços" migrou pra cá do nível raiz; "HubSpot Line Items" é nova UI admin
    // pra gerenciar mappings line_item → Servico (Plan 37-07 Task 1-2).
    {
        group: 'Comercial',
        icon: Briefcase,
        children: [
            // Phase 37 Plan 37-05 — listagem completa Comercial (já adicionado).
            { label: 'Empresas (todos os setores)', routeName: 'comercial.empresas.listagem', page: 'Comercial/EmpresasListagem', icon: Building2, permission: 'comercial.cadastrar_empresa' },
            { label: 'Cadastrar empresa', routeName: 'comercial.empresas.novo', page: 'Comercial/NovaEmpresa', icon: Building2, permission: 'comercial.cadastrar_empresa' },
            // Phase 37 Plan 37-07 — Grupos apontam para a mesma listagem com query tab=grupos.
            { label: 'Grupos', routeName: 'comercial.empresas.listagem', page: 'Comercial/EmpresasListagem', icon: ListChecks, permission: 'comercial.cadastrar_empresa', query: { tab: 'grupos' } },
            // Phase 37 Plan 37-07 — Serviços movido do nível raiz.
            { label: 'Serviços', routeName: 'servicos.index', page: 'Servicos', icon: Briefcase, permission: 'sistema.servicos' },
            // Phase 37 Plan 37-07 — HubSpot Line Items UI admin (Plan 37-02 mapping).
            { label: 'HubSpot Line Items', routeName: 'sistema.hubspot-line-items.index', page: 'Sistema/HubspotLineItems', icon: Link2, excludeRoles: ['consultor', 'mentor', 'publicador', 'analista', 'gestor', 'lider'] },
        ],
    },
    ```

    Imports a verificar:
    - `ListChecks` (já importado — usado no grupo Polos)
    - `Link2` (já importado — usado em Dev/ML OAuth linha 81)

    Se `query: { tab: 'grupos' }` não é suportado pelo helper de menu, sub-item Grupos deve apontar para uma rota nomeada que faça redirect. **Mais simples**: criar no Plan 37-05 a aba inicial via query string (`?tab=grupos`) e usar `routeName: 'comercial.empresas.listagem'` com `href` calculado no helper. Verificar pattern do AppLayout via read. Se não existe `query` prop suportado, FALLBACK: sub-item Grupos vira `{ label: 'Grupos', href: '/comercial/empresas/listagem?tab=grupos', icon: ListChecks, permission: 'comercial.cadastrar_empresa' }` — adicionar suporte ao `href` direto no helper se necessário (apenas se o helper atual não aceita).

    **Decisão pragmática**: se o helper só aceita routeName, deixar sub-item Grupos apontando para `comercial.empresas.listagem` SEM query — usuário verá a página com tab default 'empresas' e clica na tab 'Grupos' manualmente. Anotar no comentário: "TODO: refinar deep-link para ?tab=grupos quando o helper de menu suportar query param".

    Após edição, executor roda `npm run build`.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>VER ABAIXO checkpoint humano detalhado</human-check>
  </verify>
  <done>
    Sidebar renderiza com Comercial expansível contendo 5 sub-itens; item Serviços do raiz removido; build verde; admin vê HubSpot Line Items; consultor não vê HubSpot Line Items (excludeRoles).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Suíte Phase37HubspotLineItemMappingAdminTest (CRUD + autorização)</name>
  <files>tests/Feature/Phase37HubspotLineItemMappingAdminTest.php</files>
  <read_first>
    - tests/Feature/Phase37LineItemMappingTest.php (Plan 37-02 — padrão de criação de Servico + Mapping)
    - app/Http/Controllers/Sistema/HubspotLineItemMappingController.php (após Task 1)
  </read_first>
  <behavior>
    Cenários:
    - test_index_renderiza_componente_correto: GET /sistema/hubspot-line-items como admin → Inertia 'Sistema/HubspotLineItems'
    - test_index_lista_mappings_com_servico_embedado: 2 mappings + servico associado; payload tem servico_nome
    - test_store_cria_mapping_valido: POST com payload válido cria row; activity_log entry exists
    - test_store_falha_line_item_name_duplicado: tentar criar com nome já existente → 422 (unique constraint)
    - test_store_falha_servico_inativo: servico ativo=false; tentar criar mapping → 422
    - test_update_edita_campos: PUT com novo nome+servico_id+ativo; assertDatabaseHas
    - test_destroy_remove_mapping: DELETE → row sumiu; activity_log entry exists
    - test_acesso_admin_apenas: usuário consultor não-admin → 403 em todos 4 endpoints
    - test_unique_no_update_ignora_self: PUT com mesmo line_item_name do próprio row → 200 (não bloqueia)
  </behavior>
  <action>
    Criar `tests/Feature/Phase37HubspotLineItemMappingAdminTest.php` com `RefreshDatabase`.

    Helpers:
    - `actingAsAdmin(): User` — User::create role=admin + actingAs
    - `actingAsConsultor(): User` — role=consultor (sem permissão admin)
    - `criarServico(string $nome): Servico` — Servico::create([..., 'ativo' => true, 'setor' => 'outros'])
    - `criarMapping(string $nome, Servico $s): HubspotLineItemMapping`

    Asserts CRUD: `assertDatabaseHas('hubspot_line_item_mapping', [...])`, `assertDatabaseMissing(...)`, response status 200/302 (back), 422 (validation), 403 (autorização).

    Activity log: `assertDatabaseHas('activity_log', ['log_name' => 'sistema', 'description' => $stringEsperado])`.

    Para autorização: `$this->actingAs($consultor)->post(route('sistema.hubspot-line-items.store'), [...])->assertForbidden()`.
  </action>
  <verify>
    <automated>php artisan test --filter Phase37HubspotLineItemMappingAdminTest</automated>
  </verify>
  <done>
    9+ testes verdes cobrindo CRUD + validações + autorização.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    AppLayout sidebar reorganizado (Phase 37 Plan 37-07 — REQ-37-09):
    - Item "Serviços" removido do nível raiz
    - Grupo "Comercial" expansível contém: Empresas (todos os setores), Cadastrar empresa, Grupos, Serviços, HubSpot Line Items
    - Nova rota /sistema/hubspot-line-items (admin-only) renderizada
  </what-built>
  <how-to-verify>
    1. Logar como admin no sistema (dev.01@ecfconsultoria.com.br)
    2. **Sidebar — Comercial:**
       - Expandir grupo "Comercial"
       - Confirmar 5 sub-itens (na ordem): Empresas (todos os setores), Cadastrar empresa, Grupos, Serviços, HubSpot Line Items
       - Clicar em cada sub-item — todos devem abrir página correspondente sem 404/erro
    3. **Sidebar — nível raiz:**
       - Confirmar que NÃO há item "Serviços" no nível raiz (foi movido para Comercial)
       - Demais itens raiz devem estar intactos: Carteira, Empresas, Usuários, Setores, Enviar notificação, Reuniões, grupo NPS, Metas, PPA, Sugadores, Desempenho, Meu Setor, grupo Dev, grupo Comercial, grupo Publicações, grupo Polos, grupo Administrativo, Manual do Sistema
    4. **Página /sistema/hubspot-line-items:**
       - Vê listagem dos mappings seedados pelo Plan 37-02 (MAP, Polo, Brigada, Gestão, Mentoria, Publicação)
       - Botão "Novo mapeamento" abre Dialog
       - Cria 1 mapping de teste (line_item_name="TEST PHASE37", servico=Polos) — sucesso
       - Edita o teste alterando setor — sucesso
       - Remove o teste — sucesso
       - Flash messages aparecem
    5. **Acesso de não-admin:**
       - Logar como consultor (ou usuário sem role admin)
       - Confirmar que sub-item "HubSpot Line Items" NÃO aparece na sidebar (excludeRoles)
       - Acessar /sistema/hubspot-line-items diretamente → 403
    6. **Smoke /companies (Plan 37-06):**
       - Sidebar > Empresas (nível raiz) → vê apenas empresas Performance
       - Aba "Grupos" NÃO aparece em /companies (movida pro Comercial)
       - Aba "Pendências" NÃO mostra mais o card "Sem serviço"
    7. **Smoke /comercial/empresas/listagem (Plan 37-05):**
       - Filtros snake_case empilháveis (servico + setor + ordem + pendencia + q)
       - 5 cards de pendência comercial visíveis
       - Aba Grupos via GruposManager funcional
  </how-to-verify>
  <resume-signal>Type "approved" ou descreva o problema encontrado para revisão</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→/sistema/hubspot-line-items | role:admin guard impede acesso não-autorizado |
| Admin→Mapping CRUD | Admin pode alterar mappings arbitrariamente; risco aceito (escopo admin-only por design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-17 | Elevation of Privilege | Acesso não-admin a /sistema/hubspot-line-items | mitigate | Middleware role:admin + excludeRoles na sidebar + abort_unless redundante no controller (defesa em profundidade) |
| T-37-18 | Tampering | Mapping malicioso (admin aponta MAP → servico errado) | accept | Activity log registra cada alteração causedBy admin; auditoria via spatie/laravel-activitylog |
| T-37-19 | Repudiation | Admin nega ter alterado mapping | mitigate | Activity log timestampado + causedBy fixa autoria |
</threat_model>

<verification>
- `php artisan route:list | grep hubspot-line-items` mostra 4 rotas nomeadas
- `php artisan test --filter Phase37HubspotLineItemMappingAdminTest` verde
- `npm run build` verde
- Sidebar visual checked manualmente (checkpoint humano)
- Suíte Phase 37 completa verde (Plans 37-01..37-07 zero regressão)
- Suíte Phase 14/34/35/36 verde (zero regressão de fluxos legados)
</verification>

<success_criteria>
- AppLayout reorganizado conforme REQ-37-09
- Rota + controller + UI admin de mappings HubSpot funcional
- Checkpoint humano da reorg aprovado
- 9+ testes Phase37HubspotLineItemMappingAdminTest verdes
- Phase 37 inteira pronta para deploy AGRUPADO (Plans 37-01..37-07)
</success_criteria>

<output>
Create `.planning/phases/37-onboarding-comercial-unificado-via-hubspot-line-items/37-07-SUMMARY.md` when done
</output>
