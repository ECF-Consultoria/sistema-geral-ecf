---
phase: 37-onboarding-comercial-unificado-via-hubspot-line-items
plan: 06
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-05
files_modified:
  - app/Http/Controllers/CompanyController.php
  - resources/js/Pages/Companies/Index.jsx
  - tests/Feature/Phase37CompaniesPerformanceFilterTest.php
autonomous: true
requirements:
  - REQ-37-07
user_setup: []
tags: [companies, performance, scope, pendencias, cleanup, ui]

must_haves:
  truths:
    - "GET /companies passa a listar APENAS empresas com ≥1 contrato ATIVO em Servico::setor='performance'"
    - "Query usa whereHas('contratosServico.servico', fn$q => $q->where('servicos.setor', 'performance')->where('contratos_servico.ativo', true))"
    - "Pendência 'sem_servico' REMOVIDA do cálculo de pendCounts no CompanyController::index"
    - "Pendência 'sem_servico' REMOVIDA do array de pendências por empresa no payload"
    - "Aba Grupos REMOVIDA do Companies/Index.jsx (movida para /comercial/empresas/listagem no Plan 37-05)"
    - "Demais pendências OPERACIONAIS (sem_responsavel, sem_cust_id, sem_email_colaborador, sem_grant_ativo, empresa_nova) permanecem intactas"
    - "Empresas de Publicação criadas via HubSpot continuam em /mlb/empresas (whereDoesntHave mlbEmpresa em /companies preserva isso)"
  artifacts:
    - path: "app/Http/Controllers/CompanyController.php"
      provides: "Scope performance + remoção do sem_servico do payload e pendCounts"
      contains: "setor"
    - path: "resources/js/Pages/Companies/Index.jsx"
      provides: "Remoção da aba Grupos + remoção da pendência sem_servico do PENDENCIAS"
      contains: "TABS"
    - path: "tests/Feature/Phase37CompaniesPerformanceFilterTest.php"
      provides: "Suite cobrindo filtro performance + ausência sem_servico + ausência aba Grupos"
      contains: "Phase37CompaniesPerformanceFilterTest"
  key_links:
    - from: "app/Http/Controllers/CompanyController.php"
      to: "servicos.setor = 'performance'"
      via: "whereHas('contratosServico.servico', $q -> setor performance ativo)"
      pattern: "setor.*performance"
---

<objective>
Refocar `/companies` em Performance. CompanyController::index passa a filtrar exclusivamente empresas com ≥1 contrato ATIVO em Servico::setor='performance' (Gestão + Mentoria). Remove a pendência `sem_servico` (migrada para o Comercial no Plan 37-05) e a aba "Grupos" do Companies/Index.jsx (também migrada). Demais pendências operacionais (sem_responsavel, sem_cust_id, sem_email_colaborador, sem_grant_ativo, empresa_nova) ficam intactas.

Purpose: REQ-37-07. `/companies` vira a aba do time de Performance (Gestão+Mentoria) — empresas de Publicação seguem em `/mlb/empresas` (já filtrado pelo `whereDoesntHave('mlbEmpresa')` existente) e empresas comerciais SEM contrato de Performance somem dessa listagem (ficam visíveis na nova `/comercial/empresas/listagem` do Plan 37-05).

Output: scope adicional no query base de CompanyController::index, cleanup do pendencias + pendCounts, remoção da aba Grupos do JSX, suíte Phase37CompaniesPerformanceFilterTest (5+ cenários).

Compatibilidade: TODOS os filtros existentes (?cust_id_status, ?sort) continuam funcionando. Empresa que tem MlbEmpresa associada (already filtrada por whereDoesntHave('mlbEmpresa')) continua excluída — zero regressão do fluxo MLB/Publicação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-fix-cadastro-hubspot-v2/35-01-SUMMARY.md

<interfaces>
<!-- CompanyController::index — query base atual (linhas 55-74): -->
<!-- Company::with([...]) -->
<!--   ->withCount(['grants as grants_ativos_count' => fn($q) => $q->where('status', 'active')]) -->
<!--   ->whereDoesntHave('mlbEmpresa')   ← preserva (REQ-37-07 zero regressão Publicação) -->
<!--   ->when($custIdStatusFilter, ...) -->
<!--   ->when($sort, fn($q) => $q->orderBy('created_at', ...), fn($q) => $q->orderBy('name')) -->
<!--   ->get(); -->

<!-- PHASE 37 ADIÇÃO — scope Performance: -->
<!-- ->whereHas('contratosServico', fn($q) => -->
<!--     $q->where('contratos_servico.ativo', true) -->
<!--       ->whereHas('servico', fn($qs) => $qs->where('setor', 'performance')) -->
<!-- ) -->

<!-- Companies/Index.jsx — TABS atual (linhas ~344-348): -->
<!--   const TABS = [ -->
<!--     { key: 'empresas', label: 'Empresas (N)' }, -->
<!--     { key: 'pendencias', label: 'Pendências (N)' }, -->
<!--     { key: 'grupos', label: 'Grupos (N)' }, ← REMOVER -->
<!--   ]; -->
<!-- E remover o bloco `{tab === 'grupos' && (<GruposManager ... />)}` (linhas ~637-639). -->

<!-- PENDENCIAS no JSX (mapa que pinta os cards do header da aba Pendências): -->
<!-- Verificar — sem_servico aparece como key. Remover do objeto PENDENCIAS para -->
<!-- não exibir o card mais. -->

<!-- pendCounts no payload do backend (linhas ~133-141 do controller): -->
<!-- Remover o cálculo `($c->contratosServico->isEmpty()) ? 'sem_servico' : null` -->
<!-- da lista de array_values(array_filter([...])). -->
<!-- Manter sem_responsavel, sem_cust_id, sem_email_colaborador, sem_grant_ativo, empresa_nova. -->
</interfaces>

@app/Http/Controllers/CompanyController.php
@resources/js/Pages/Companies/Index.jsx
@app/Models/Company.php
@app/Models/ContratoServico.php
@app/Models/Servico.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CompanyController::index com scope Performance + cleanup pendência sem_servico</name>
  <files>app/Http/Controllers/CompanyController.php</files>
  <read_first>
    - app/Http/Controllers/CompanyController.php (linhas 33-204 — método index completo)
    - app/Models/Servico.php (constants SETOR_PERFORMANCE)
    - .planning/phases/35-fix-cadastro-hubspot-v2/35-01-SUMMARY.md (zero regressão do whereDoesntHave mlbEmpresa)
  </read_first>
  <behavior>
    Comportamento esperado:
    - GET /companies retorna apenas empresas com ≥1 contrato ATIVO em servico setor=performance
    - Empresa com contratos ativos APENAS em servicos setor=publicacao OU outros NÃO aparece em /companies
    - Empresa com 0 contratos ativos NÃO aparece em /companies
    - Empresa com MlbEmpresa associada continua excluída (filtro existente preservado)
    - pendencias array por empresa NÃO contém 'sem_servico' nunca
    - pendCounts NÃO retorna chave 'sem_servico'
    - Demais filtros (?cust_id_status, ?sort) continuam funcionando
  </behavior>
  <action>
    **Modificação A — Adicionar scope Performance no query base do index():**

    Localizar o `Company::with([...])` (linhas 55-74). DEPOIS do `->whereDoesntHave('mlbEmpresa')` e ANTES do `->when($custIdStatusFilter, ...)`, adicionar:

    ```
    // Phase 37 Plan 37-06 (REQ-37-07) — /companies refoca em Performance.
    // Empresas com contratos APENAS em Publicação/Outros aparecem em
    // /comercial/empresas/listagem (Plan 37-05). MlbEmpresa segue excluído acima.
    ->whereHas('contratosServico', fn($q) =>
        $q->where('contratos_servico.ativo', true)
          ->whereHas('servico', fn($qs) =>
              $qs->where('setor', Servico::SETOR_PERFORMANCE)
          )
    )
    ```

    Adicionar `use App\Models\Servico;` no topo se ainda não importado (já está — confirmar via read).

    **Modificação B — Remover pendência 'sem_servico' do array de pendências por empresa:**

    Localizar o map de empresas (linhas ~76-142). Dentro de `'pendencias' => array_values(array_filter([...]))`, REMOVER esta linha:
    ```
    ($c->contratosServico->isEmpty()) ? 'sem_servico' : null,
    ```

    Manter as outras 5 pendências (sem_responsavel, sem_cust_id, sem_email_colaborador, sem_grant_ativo, empresa_nova).

    **Modificação C — Verificar se há cálculo de pendCounts:**

    Se o controller tem um cálculo separado de `pendCounts` (mapa slug→count), confirmar via grep no método index. Se existe, remover a chave `sem_servico`. Se pendCounts vem direto do frontend lendo `companies.pendencias`, sem alteração no backend é necessária — frontend deixará de pintar o card automaticamente após Task 2.

    Comentário pt-BR em cada modificação referenciando "Phase 37 Plan 37-06 (REQ-37-07) — pendência sem_servico migrou para /comercial/empresas/listagem".

    Manter activity log + logging existentes intactos.
  </action>
  <verify>
    <automated>php artisan test --filter Phase37CompaniesPerformanceFilterTest && php artisan test --filter Phase35</automated>
  </verify>
  <done>
    Query scope adicional aplicado; pendência sem_servico removida do payload; demais filtros e pendências preservados; suíte Phase37 verde + suíte Phase35 sem regressão.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Companies/Index.jsx remove aba Grupos + remove card pendência sem_servico</name>
  <files>resources/js/Pages/Companies/Index.jsx</files>
  <read_first>
    - resources/js/Pages/Companies/Index.jsx (arquivo completo, focar em TABS + PENDENCIAS constant + bloco tab === 'grupos')
  </read_first>
  <behavior>
    Após edição:
    - Apenas 2 abas visíveis: Empresas, Pendências
    - Aba Pendências NÃO mostra o card "Sem serviço" (foi removido)
    - GruposManager NÃO é renderizado em /companies (mas continua acessível em /comercial/empresas/listagem do Plan 37-05)
    - Import de GruposManager pode ser removido (cleanup) se não houver outro uso
    - npm run build verde
  </behavior>
  <action>
    **A — Remover `grupos` do array TABS (linhas ~344-348):**

    Substituir:
    ```
    const TABS = [
        { key: 'empresas',   label: `Empresas (${totalAtivas})` },
        { key: 'pendencias', label: `Pendências (${pendentes.length})` },
        { key: 'grupos',     label: `Grupos (${grupos.length})` },
    ];
    ```

    Por:
    ```
    // Phase 37 Plan 37-06 (REQ-37-07) — aba Grupos migrou para /comercial/empresas/listagem (Plan 37-05).
    const TABS = [
        { key: 'empresas',   label: `Empresas (${totalAtivas})` },
        { key: 'pendencias', label: `Pendências (${pendentes.length})` },
    ];
    ```

    **B — Remover o bloco `{tab === 'grupos' && (...)}` (linhas ~637-639):**

    Localizar bloco que renderiza `<GruposManager ... />` e deletá-lo completamente. Substituir por comentário JSX:
    `{/* Phase 37 Plan 37-06 — aba Grupos migrou para /comercial/empresas/listagem. */}`

    **C — Remover entrada `sem_servico` do objeto PENDENCIAS (constant local):**

    Buscar onde está definido o map `const PENDENCIAS = { ... }`. Deletar a entrada `sem_servico: { ... }`. Comentário pt-BR: "Phase 37 Plan 37-06 (REQ-37-07) — pendência sem_servico migrou para /comercial/empresas/listagem."

    **D — Remover cleanup imports não usados:**

    Após remoção do bloco grupos, verificar se `GruposManager` ainda é usado. Se NÃO, remover `import GruposManager from '@/Components/GruposManager';` (linha 16).

    Verificar também se prop `grupos` continua sendo recebida — se o backend ainda envia (Task 1 não removeu), pode ficar como prop não-usada. Não remover do backend nesta plan; o backend continua enviando para compatibilidade futura caso queira reativar.

    Após edição, executor roda `npm run build` (cross-cutting CLAUDE.md).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Acessar /companies; conferir que NÃO há aba Grupos; aba Pendências não mostra mais o card "Sem serviço"; lista mostra apenas empresas Performance (Gestão/Mentoria)</human-check>
  </verify>
  <done>
    UI mostra 2 abas (Empresas, Pendências); aba Grupos sumiu; card sem_servico sumiu; lista exibe apenas empresas Performance; build verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suíte Phase37CompaniesPerformanceFilterTest</name>
  <files>tests/Feature/Phase37CompaniesPerformanceFilterTest.php</files>
  <read_first>
    - tests/Feature/Phase34HubspotWebhookTest.php (padrão de criação Company + ContratoServico em testes)
    - app/Http/Controllers/CompanyController.php (após Task 1)
  </read_first>
  <behavior>
    Cenários:
    - test_empresa_com_contrato_performance_aparece: cria Company + Servico setor=performance + ContratoServico ativo; GET /companies inclui empresa
    - test_empresa_com_contrato_publicacao_NAO_aparece: similar mas setor=publicacao; GET /companies NÃO inclui empresa
    - test_empresa_com_contrato_outros_NAO_aparece: setor=outros; ausente
    - test_empresa_com_contrato_performance_inativo_NAO_aparece: ContratoServico.ativo=false; ausente
    - test_empresa_sem_contratos_NAO_aparece: 0 contratos; ausente
    - test_empresa_com_contratos_mistos_aparece: 1 contrato performance ativo + 1 outros ativo; aparece (basta 1 performance)
    - test_empresa_com_mlb_empresa_NAO_aparece: cria MlbEmpresa associated; ausente (zero regressão Phase 35)
    - test_payload_NAO_contem_pendencia_sem_servico: empresa com 0 contratos performance... hmm, empresa não aparece (filtro acima). Substituir: empresa com contrato performance ativo mas SEM cumprir outras pendências → assertCount(0, array_filter($pendencias, fn$p => $p === 'sem_servico'))
    - test_payload_contem_demais_pendencias: empresa sem consultor/estrategista → assertContains('sem_responsavel', $pendencias)
    - test_filtro_cust_id_status_invalido_continua_funcional: empresa com cust_id_status='invalido' + contrato performance ativo; GET /companies?cust_id_status=invalido inclui
    - test_acesso_admin_apenas: usuário não-admin sem permissão → 403 (testar via middleware existente)
  </behavior>
  <action>
    Criar `tests/Feature/Phase37CompaniesPerformanceFilterTest.php` com `RefreshDatabase`.

    Helpers locais:
    - `criarServicoPerformance(string $nome = 'Gestão'): Servico` — Servico::create([..., 'setor' => 'performance'])
    - `criarServicoPublicacao(string $nome = 'Publicação'): Servico` — setor publicacao
    - `criarServicoOutros(string $nome = 'Polos'): Servico` — setor outros
    - `criarEmpresaComContrato(Servico $s, bool $ativo = true): Company` — Company + ContratoServico
    - `actingAsAdmin(): User`

    Asserts via Inertia: `$response->assertInertia(fn($page) => $page->component('Companies/Index')->has('companies', $expectedCount))`.

    Para test_payload_NAO_contem_pendencia_sem_servico:
    `$page->where('companies.0.pendencias', fn($p) => !in_array('sem_servico', $p))`.

    Para test_payload_contem_demais_pendencias:
    `$page->where('companies.0.pendencias', fn($p) => in_array('sem_responsavel', $p))`.

    Manter compat com testes existentes — usar `setUp` que cria User admin + auth.

    Sem `npm run build` no teste backend.
  </action>
  <verify>
    <automated>php artisan test --filter Phase37CompaniesPerformanceFilterTest</automated>
  </verify>
  <done>
    8+ testes verdes; cobertura scope Performance + remoção sem_servico + preservação demais pendências + zero regressão Phase 35 (whereDoesntHave mlbEmpresa).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→CompanyController | Filtros (cust_id_status, sort) já validados via whitelist Phase 18; nova whereHas adiciona escopo sem novo input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-15 | Information Disclosure | Empresas filtradas | accept | Restrição é positiva — empresas que não bate em Performance simplesmente não aparecem; nenhum vazamento |
| T-37-16 | Tampering | Constant Servico::SETOR_PERFORMANCE | mitigate | Plan 37-01 define const pública; uso direto em PHP evita string literal solta |
</threat_model>

<verification>
- /companies renderiza apenas empresas com contrato Performance ativo
- Aba Grupos sumiu da UI
- Card "Sem serviço" sumiu da aba Pendências
- Demais pendências operacionais continuam funcionando
- Suíte Phase35 verde (zero regressão whereDoesntHave mlbEmpresa)
- Suíte Phase 14 ServicoTest verde (zero regressão modelo de serviços)
- `npm run build` verde
</verification>

<success_criteria>
- Companies::index filtra performance via whereHas
- Pendência sem_servico removida do payload e da UI
- Aba Grupos removida do Companies/Index.jsx
- Empresas de Publicação seguem em /mlb/empresas (whereDoesntHave mlbEmpresa preservado)
- Empresas comerciais sem contrato Performance ficam acessíveis em /comercial/empresas/listagem (Plan 37-05)
- 8+ testes Phase37CompaniesPerformanceFilterTest verdes
</success_criteria>

<output>
Create `.planning/phases/37-onboarding-comercial-unificado-via-hubspot-line-items/37-06-SUMMARY.md` when done
</output>
