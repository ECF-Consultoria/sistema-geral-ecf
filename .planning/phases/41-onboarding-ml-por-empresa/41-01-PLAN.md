---
phase: 41-onboarding-ml-por-empresa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_25_410101_create_ml_advertisers_table.php
  - database/migrations/2026_06_25_410102_create_sugador_ml_company_config_table.php
  - app/Models/MlAdvertiser.php
  - app/Models/SugadorMlCompanyConfig.php
  - app/Models/Company.php
  - tests/Feature/Phase41/Phase41SchemaTest.php
autonomous: true
requirements: [REQ-41-01, REQ-41-02]

must_haves:
  truths:
    - "Tabela `ml_advertisers` existe com colunas exatas (id, company_id FK unique, advertiser_id idx, seller_id nullable, site_id default 'MLB', raw_data json, discovered_at timestampTz, timestamps)"
    - "Tabela `sugador_ml_company_config` existe com colunas exatas (id, company_id FK unique, shadow_enabled bool default false idx, primary_enabled bool default false idx, shadow_started_at date nullable, primary_promoted_at date nullable, notes text nullable, timestamps)"
    - "FK cascade funciona: deletar Company apaga linhas em ambas tabelas"
    - "Model `App\\Models\\MlAdvertiser` tem $fillable + $casts (raw_data => array, discovered_at => immutable_datetime) + relacao company() BelongsTo"
    - "Model `App\\Models\\SugadorMlCompanyConfig` tem $fillable + $casts (shadow_enabled/primary_enabled => boolean, shadow_started_at/primary_promoted_at => date) + relacao company() BelongsTo"
    - "Company ganha relacoes hasOne: `mlAdvertiser()` e `sugadorMlConfig()`"
  artifacts:
    - path: "database/migrations/2026_06_25_410101_create_ml_advertisers_table.php"
      provides: "Migration idempotente (Schema::hasTable guard) criando ml_advertisers"
    - path: "database/migrations/2026_06_25_410102_create_sugador_ml_company_config_table.php"
      provides: "Migration idempotente criando sugador_ml_company_config"
    - path: "app/Models/MlAdvertiser.php"
      provides: "Eloquent Model com casts/relations"
    - path: "app/Models/SugadorMlCompanyConfig.php"
      provides: "Eloquent Model com casts/relations"
    - path: "app/Models/Company.php"
      provides: "Relacoes mlAdvertiser() + sugadorMlConfig() adicionadas (sem remover nada)"
    - path: "tests/Feature/Phase41/Phase41SchemaTest.php"
      provides: "Suite Feature 6+ tests validando schema, FK cascade, casts, relacoes"
  key_links:
    - from: "ml_advertisers.company_id"
      to: "companies.id"
      via: "FK cascadeOnDelete"
      pattern: "foreignId.*company_id.*cascadeOnDelete"
    - from: "sugador_ml_company_config.company_id"
      to: "companies.id"
      via: "FK cascadeOnDelete (unique)"
      pattern: "foreignId.*company_id.*unique.*cascadeOnDelete"
    - from: "Company"
      to: "MlAdvertiser"
      via: "hasOne relation"
      pattern: "hasOne\\(MlAdvertiser::class\\)"
    - from: "Company"
      to: "SugadorMlCompanyConfig"
      via: "hasOne relation"
      pattern: "hasOne\\(SugadorMlCompanyConfig::class\\)"
---

<objective>
Cria fundacao de dados da Phase 41: 2 tabelas auxiliares (`ml_advertisers` para cache do advertiser/seller/site discovery + `sugador_ml_company_config` para flags shadow/primary por empresa) + 2 Models Eloquent + relacoes em Company. Substitui a fonte do `--company=all` do comando `sugadores:shadow-ml` (Phase 40-04, que hoje le env CSV) por uma tabela que a UI admin (Plan 41-05) pode escrever.

Purpose: destravar Plans 41-02 (refactor MercadoLivreAdsService que precisa do cache `ml_advertisers`), 41-03 (command shadow-ml que precisa ler `sugador_ml_company_config`) e 41-05 (UI que escreve em ambas).

Output: 2 migrations + 2 Models + edit Company + 1 suite de tests Feature validando schema + FK cascade + casts + relacoes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-01-SUMMARY.md
@database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php
@app/Models/Company.php
@app/Models/MlToken.php

<interfaces>
<!-- Padrao de migration idempotente + FK cascade extraido de Plan 40-01 -->

Pattern de migration (Plan 40-01 ja entregue — usar como template):
```php
if (!Schema::hasTable('XYZ')) {
    Schema::create('XYZ', function (Blueprint $table) {
        $table->id();
        $table->foreignId('company_id')->constrained('companies')->cascadeOnDelete();
        // ... outras colunas
        $table->timestamps();
    });
}
```

Down() ordem importa quando ha FK encadeada (filha antes da pai). Para Plan 41-01, as 2 tabelas sao IRMAS (ambas FK direta para companies) — ordem do down nao importa entre elas, mas mantenha o padrao `dropIfExists` por seguranca.

Pattern de Model (extraido de SugadorProviderRun do Plan 40-01):
```php
namespace App\Models;

use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

class XYZ extends Model
{
    protected $fillable = [...];
    protected $casts = [...];

    public function company(): BelongsTo
    {
        return $this->belongsTo(Company::class);
    }
}
```

Company atual ja tem (linhas 222-244 — Read confirmou):
- `sugadorConfig()` hasOne SugadorConfig
- `contratosServico()` hasMany ContratoServico
- `mlToken()` hasOne MlToken

Adicionar IMEDIATAMENTE APOS `mlToken()` (linha ~244):
- `mlAdvertiser()` hasOne MlAdvertiser
- `sugadorMlConfig()` hasOne SugadorMlCompanyConfig
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): Suite Phase41SchemaTest com 6 testes esperando os 2 schemas + casts + relacoes</name>
  <files>tests/Feature/Phase41/Phase41SchemaTest.php</files>

  <read_first>
    - .planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md (Implementation Decisions §1 e §2 — schema exato)
    - .planning/phases/40-shadow-mode-tabelas-de-compara-o/40-01-SUMMARY.md (pattern de teste schema + FK cascade)
    - database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php (template idempotente)
    - tests/Feature/Phase40/ (referencia de pattern PHPUnit 11 com `#[Test]` atributo)
    - app/Models/Company.php (linhas 222-244 — relacoes existentes)
  </read_first>

  <behavior>
    - Test 1 `tabela_ml_advertisers_tem_colunas_esperadas`: Schema::hasColumns('ml_advertisers', ['id','company_id','advertiser_id','seller_id','site_id','raw_data','discovered_at','created_at','updated_at']) === true.
    - Test 2 `tabela_sugador_ml_company_config_tem_colunas_esperadas`: Schema::hasColumns('sugador_ml_company_config', ['id','company_id','shadow_enabled','primary_enabled','shadow_started_at','primary_promoted_at','notes','created_at','updated_at']) === true.
    - Test 3 `fk_company_id_cascade_em_ml_advertisers`: cria Company + MlAdvertiser linkado, deleta Company, MlAdvertiser::count() == 0. Ativa `PRAGMA foreign_keys = ON` no setUp (SQLite default desliga FK; pattern Plan 40-01 Test 5/6).
    - Test 4 `fk_company_id_cascade_em_sugador_ml_company_config`: idem para SugadorMlCompanyConfig.
    - Test 5 `model_ml_advertiser_casts_e_relacoes`: cria registro, recarrega, valida `raw_data` retornado como array (cast), `discovered_at` Carbon, `company()->associate()` resolve para Company.
    - Test 6 `model_sugador_ml_company_config_casts_e_relacoes`: cria registro, valida `shadow_enabled` boolean (insere 1 → leitura true), `primary_enabled` boolean, `shadow_started_at` Carbon Date, `notes` string, `company()` resolve.
    - Test 7 `company_relacoes_novas`: cria Company + MlAdvertiser + SugadorMlCompanyConfig, valida `$company->mlAdvertiser` retorna MlAdvertiser instance e `$company->sugadorMlConfig` retorna SugadorMlCompanyConfig instance.
    - Test 8 `company_id_unique_em_ambas_tabelas`: tentar criar 2 MlAdvertiser para a mesma company_id deve lancar QueryException (unique constraint); idem para SugadorMlCompanyConfig.
  </behavior>

  <action>
    Criar arquivo `tests/Feature/Phase41/Phase41SchemaTest.php` namespace `Tests\Feature\Phase41` extends `Tests\TestCase`, usa `RefreshDatabase`. Setup: ativar `DB::statement('PRAGMA foreign_keys = ON')` em `setUp()` para SQLite respeitar FK cascade (pattern do Plan 40-01).

    Cada teste com atributo PHP 8 `#[\PHPUnit\Framework\Attributes\Test]` (NAO usar doccomment `/** @test */` — PHPUnit 11 deprecou e Plan 40-01 ja migrou).

    Helper privado `makeCompany(): Company` igual ao pattern Plan 40-04 (cnpj 14 chars, name, active=true). NAO precisa de adman_account_id para os testes de schema.

    Cobrir os 8 cenarios da `<behavior>`. Tests devem FALHAR todos com `BadMethodCallException`/`Schema not found`/`Class App\Models\MlAdvertiser does not exist` confirmando estado RED.

    NAO incluir helper que crie/dropa tabelas manualmente — RefreshDatabase + as migrations a serem criadas na Tarefa 2 cobrem isso.

    Comentarios em pt-BR conforme CLAUDE.md.
  </action>

  <verify>
    <automated>php artisan test --filter=Phase41SchemaTest</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `tests/Feature/Phase41/Phase41SchemaTest.php` existe.
    - `grep -c "#\[Test\]" tests/Feature/Phase41/Phase41SchemaTest.php` retorna >= 8.
    - `php artisan test --filter=Phase41SchemaTest` retorna 8/8 FAIL ou ERROR (estado RED). Stderr/stdout DEVE conter pelo menos uma destas substrings: `MlAdvertiser`, `does not exist`, `no such table`, `hasColumn`.
    - Commit criado com prefixo `test(41-01):` mencionando "RED".
  </acceptance_criteria>

  <done>
    Suite RED commitada. 8/8 tests falhando antes da implementacao. Nenhum codigo de producao (migration/model/edit Company) foi tocado nesta tarefa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): Implementa 2 migrations + 2 Models + edita Company com 2 relacoes</name>
  <files>
    database/migrations/2026_06_25_410101_create_ml_advertisers_table.php,
    database/migrations/2026_06_25_410102_create_sugador_ml_company_config_table.php,
    app/Models/MlAdvertiser.php,
    app/Models/SugadorMlCompanyConfig.php,
    app/Models/Company.php
  </files>

  <read_first>
    - .planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md (Implementation Decisions §1, §2, §7 nao-tocar)
    - database/migrations/2026_06_25_400001_create_sugador_provider_runs_and_items_tables.php (template idempotente)
    - app/Models/Company.php (linhas 222-260 — onde inserir as 2 relacoes novas SEM tocar nas existentes)
    - tests/Feature/Phase41/Phase41SchemaTest.php (escrito na Tarefa 1)
  </read_first>

  <action>
    **Migration 1** (`database/migrations/2026_06_25_410101_create_ml_advertisers_table.php`):
    - Schema EXATO do §1 do CONTEXT:
      - `$table->id()`
      - `$table->foreignId('company_id')->unique()->constrained('companies')->cascadeOnDelete()`
      - `$table->string('advertiser_id', 64)->index()`
      - `$table->string('seller_id', 64)->nullable()`
      - `$table->string('site_id', 8)->default('MLB')`
      - `$table->json('raw_data')->nullable()`
      - `$table->timestampTz('discovered_at')`
      - `$table->timestamps()`
    - Guard `if (!Schema::hasTable('ml_advertisers'))` no up(); `dropIfExists` no down().
    - PHPDoc pt-BR no topo da classe anonima explicando: cache do discoverAdvertiser, expiracao logica de 7d (gerenciada pelo MercadoLivreAdsService Plan 41-02), nao substitui mlToken.

    **Migration 2** (`database/migrations/2026_06_25_410102_create_sugador_ml_company_config_table.php`):
    - Schema EXATO do §2 do CONTEXT:
      - `$table->id()`
      - `$table->foreignId('company_id')->unique()->constrained('companies')->cascadeOnDelete()`
      - `$table->boolean('shadow_enabled')->default(false)->index()`
      - `$table->boolean('primary_enabled')->default(false)->index()`
      - `$table->date('shadow_started_at')->nullable()`
      - `$table->date('primary_promoted_at')->nullable()`
      - `$table->text('notes')->nullable()`
      - `$table->timestamps()`
    - Guard `if (!Schema::hasTable('sugador_ml_company_config'))`; dropIfExists no down().
    - PHPDoc pt-BR: substitui env CSV `SUGADORES_ML_SHADOW_COMPANIES` da Phase 40 (com fallback no Plan 41-03), Phase 42 vai escrever em `primary_enabled` (defensa adicional em factory).

    **Model `app/Models/MlAdvertiser.php`** namespace `App\Models`:
    - `$fillable = ['company_id', 'advertiser_id', 'seller_id', 'site_id', 'raw_data', 'discovered_at']`
    - `$casts = ['raw_data' => 'array', 'discovered_at' => 'immutable_datetime']`
    - Relacao `public function company(): BelongsTo { return $this->belongsTo(Company::class); }`
    - PHPDoc pt-BR explicando cache Phase 41-02.
    - NAO usar LogsActivity (Phase 40 Plan 40-01 ja documentou que tabelas auxiliares nao precisam — audit fica na fonte canonica).

    **Model `app/Models/SugadorMlCompanyConfig.php`** namespace `App\Models`:
    - `protected $table = 'sugador_ml_company_config'` (singular nao pluraliza automaticamente — Eloquent tentaria `sugador_ml_company_configs`).
    - `$fillable = ['company_id', 'shadow_enabled', 'primary_enabled', 'shadow_started_at', 'primary_promoted_at', 'notes']`
    - `$casts = ['shadow_enabled' => 'boolean', 'primary_enabled' => 'boolean', 'shadow_started_at' => 'date', 'primary_promoted_at' => 'date']`
    - Relacao `company(): BelongsTo`.
    - PHPDoc pt-BR.

    **Edit `app/Models/Company.php`**:
    - APENAS adicionar 2 metodos imediatamente apos `mlToken()` (linha ~244):
      ```
      public function mlAdvertiser()
      {
          return $this->hasOne(MlAdvertiser::class);
      }

      public function sugadorMlConfig()
      {
          return $this->hasOne(SugadorMlCompanyConfig::class);
      }
      ```
    - Comentario pt-BR curto antes de cada metodo (Phase 41 cache advertiser / config shadow+primary).
    - ZERO mudanca em qualquer outro metodo, fillable, casts, ou relacao existente. Validar via `git diff app/Models/Company.php` mostrando APENAS adicoes.

    Apos editar tudo, rodar `php artisan migrate` no MariaDB? **NAO** — bloqueio conhecido. Os tests usam RefreshDatabase com SQLite em-memory, que reaplica as migrations a cada test.

    Comentarios em pt-BR conforme CLAUDE.md.
  </action>

  <verify>
    <automated>php artisan test --filter=Phase41SchemaTest && php artisan test --filter=Phase40 && php artisan test --filter=Sugador</automated>
  </verify>

  <acceptance_criteria>
    - Arquivos criados existem: 2 migrations + 2 Models.
    - `grep -c "Schema::create('ml_advertisers'" database/migrations/2026_06_25_410101_*.php` retorna 1.
    - `grep -c "Schema::create('sugador_ml_company_config'" database/migrations/2026_06_25_410102_*.php` retorna 1.
    - `grep -c "cascadeOnDelete" database/migrations/2026_06_25_4101*.php` retorna >= 2 (1 por migration).
    - `grep -c "mlAdvertiser\|sugadorMlConfig" app/Models/Company.php` retorna >= 2.
    - `grep -c "hasOne(MlAdvertiser::class)\|hasOne(SugadorMlCompanyConfig::class)" app/Models/Company.php` retorna 2.
    - `git diff app/Models/Company.php` mostra APENAS linhas com `+` (nenhuma modificacao em metodo existente). Sem `-` em qualquer linha funcional.
    - `php artisan test --filter=Phase41SchemaTest` retorna 8/8 PASS.
    - `php artisan test --filter=Phase40` retorna 52/52 PASS (zero regressao — baseline do SUMMARY 40-04).
    - `php artisan test --filter=Sugador` retorna >= 92/92 PASS (zero regressao + os 8 novos = 100/100 esperado, mas aceitar exatamente >= 92).
    - `php artisan migrate:status` (SQLite em-memory via tinker) lista as 2 migrations novas como Ran.
    - Commit criado com prefixo `feat(41-01):` mencionando "GREEN" + REQ-41-01 + REQ-41-02.
  </acceptance_criteria>

  <done>
    Schema da Phase 41 entregue. Plans 41-02 (cache advertiser) + 41-03 (lookup shadow_enabled) + 41-05 (UI escreve em ambas) destravados. 8/8 Phase41Schema verdes + zero regressao Phase 40/Sugador.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB schema | Nenhuma; rodada por dev/admin via CLI |
| Eloquent Model fillable | Nenhuma (sem entrada HTTP nesta plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | Migration nao-idempotente quebra re-run | mitigate | Guard `Schema::hasTable` em ambos `Schema::create` (pattern Plan 40-01) |
| T-41-01-02 | Denial-of-Service | Falta de FK cascade deixa registros orfaos | mitigate | `cascadeOnDelete()` em ambos FK company_id |
| T-41-01-03 | Information disclosure | raw_data pode conter PII do advertiser | accept | Tabela acessada so via path admin Plan 41-05 (role:admin); raw_data ja existe hoje no payload ML que `MercadoLivreAdsService` recebe; sem exposicao nova |

Severidade global: LOW (so cria schema; nenhum HTTP/endpoint novo nesta plan).
</threat_model>

<verification>
- `php artisan test --filter=Phase41SchemaTest` → 8/8 PASS
- `php artisan test --filter=Phase40` → 52/52 PASS (zero regressao)
- `php artisan test --filter=Sugador` → >= 92/92 PASS (zero regressao)
- `grep -rn "ml_advertisers\|sugador_ml_company_config" app/Models/` mostra Company + 2 Models novos
- `grep -c "hasOne" app/Models/Company.php` aumenta em 2 vs baseline (sugadorConfig + mlToken existentes + 2 novos = 4)
</verification>

<success_criteria>
Plan 41-01 completo quando:
- 2 migrations criadas + 2 Models criados + Company com 2 relacoes novas
- Tests Phase41Schema 8/8 verdes
- Phase 40 + Sugador suites sem regressao
- 2 commits (RED + GREEN) na arvore
- REQ-41-01 + REQ-41-02 cobertos
- Plans 41-02, 41-03, 41-05 destravados
</success_criteria>

<output>
Criar `.planning/phases/41-onboarding-ml-por-empresa/41-01-SUMMARY.md` quando done.
</output>
