---
phase: 41-onboarding-ml-por-empresa
plan: 05
type: execute
wave: 3
depends_on: ["41-04"]
files_modified:
  - app/Http/Controllers/Dev/SugadoresMlOnboardingController.php
  - routes/web.php
  - resources/js/Pages/Dev/SugadoresMlOnboarding/Index.jsx
  - resources/js/Layouts/AppLayout.jsx
  - tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php
autonomous: true
requirements: [REQ-41-08, REQ-41-09, REQ-41-10]

must_haves:
  truths:
    - "Rota GET /dev/sugadores-ml-onboarding renderiza Inertia component 'Dev/SugadoresMlOnboarding/Index'"
    - "Payload `companies` eh array com 1 item por Company active=true, cada item tem keys: id, name, token_state, advertiser, smoke_last_at, shadow_paridade_7d, status, actions"
    - "token_state ∈ {missing, expired, error_refresh, active}; calculado de mlToken.expires_at + mlToken.status"
    - "advertiser ∈ {none, ok}; calculado da existencia de MlAdvertiser para a empresa"
    - "shadow_paridade_7d retorna numero 0..100 ou null quando nao ha SugadorProviderRun nos ultimos 7 dias"
    - "status geral ∈ {adman_only, ml_shadow, ml_primary_candidate, ml_primary}; derivado de SugadorMlCompanyConfig + paridade 7d"
    - "POST /dev/sugadores-ml-onboarding/{company}/toggle-shadow flip do shadow_enabled na SugadorMlCompanyConfig (firstOrCreate); preenche shadow_started_at = now() ao ativar, null ao desativar"
    - "POST /dev/sugadores-ml-onboarding/{company}/smoke dispatcha comando `sugadores:ml-smoke --company={id}` via Artisan::queue (async); retorna back() com flash success"
    - "POST /dev/sugadores-ml-onboarding/{company}/shadow dispatcha `sugadores:shadow-ml --company={id} --days=1` via Artisan::queue; retorna back() com flash success"
    - "Sidebar admin tem item novo 'Sugadores ML Onboarding' dentro do grupo Sistema (excludeRoles para outros roles)"
    - "`npm run build` executado com sucesso (apos edicao JSX — convencao do CLAUDE.md)"
    - "Todas as 4 rotas (1 GET + 3 POST) gated por middleware role:admin + auth + verified"
  artifacts:
    - path: "app/Http/Controllers/Dev/SugadoresMlOnboardingController.php"
      provides: "Controller com 4 actions: index, runSmoke, runShadow, toggleShadow + helper privado buildRow"
    - path: "routes/web.php"
      provides: "Grupo `dev/sugadores-ml-onboarding` com 4 rotas nomeadas dev.sugadores_ml_onboarding.*"
    - path: "resources/js/Pages/Dev/SugadoresMlOnboarding/Index.jsx"
      provides: "Pagina React com tabela 6 colunas + cabecalho com contadores + filtros por status/token_state + acoes inline via router.post()"
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "Item de sidebar Sugadores ML Onboarding adicionado dentro do grupo Sistema (excludeRoles para nao-admin)"
    - path: "tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php"
      provides: "Suite 10+ tests cobrindo: index render Inertia + payload, role:admin gate (403 outros), toggleShadow flip + timestamps, runSmoke/Shadow dispatch correto via Artisan fake"
  key_links:
    - from: "/dev/sugadores-ml-onboarding"
      to: "SugadoresMlOnboardingController@index"
      via: "Route::get nomeada dev.sugadores_ml_onboarding.index"
      pattern: "dev/sugadores-ml-onboarding"
    - from: "SugadoresMlOnboardingController::buildRow"
      to: "SugadorProviderRun summary->ml_metrics + ProviderComparisonService"
      via: "Eloquent + service call"
      pattern: "SugadorProviderRun::|ProviderComparisonService"
    - from: "AppLayout.jsx sidebar"
      to: "dev.sugadores_ml_onboarding.index"
      via: "routeName attribute"
      pattern: "dev\\.sugadores_ml_onboarding\\.index"
---

<objective>
Entrega a UI admin completa de onboarding ML: rota `/dev/sugadores-ml-onboarding` renderiza tabela com 1 linha por empresa ativa mostrando estado de `mlToken`, advertiser (cache Plan 41-01), ultimo smoke (do `storage/app/sugadores/ml-smoke/`), paridade shadow ultimos 7 dias (calculada via `ProviderComparisonService::compareWindow` Plan 40-03), status geral derivado e acoes inline (rodar smoke, rodar shadow, toggle shadow_enabled).

Purpose: dar ao admin uma vista operacional unica de "qual o estado de cada empresa na migracao ML Sugadores" sem precisar SSH no VPS / abrir Tinker / consultar logs. Acoes inline disparam comandos async via Artisan::queue, retornando `back()` com flash — sem polling em tempo real (deferido conforme CONTEXT §Deferred).

Output: 1 controller + 4 rotas + 1 React page + 1 edit AppLayout + 1 suite Feature 10+ tests + `npm run build` ok.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md
@.planning/phases/41-onboarding-ml-por-empresa/41-01-PLAN.md
@.planning/phases/41-onboarding-ml-por-empresa/41-02-PLAN.md
@.planning/phases/41-onboarding-ml-por-empresa/41-03-PLAN.md
@.planning/phases/41-onboarding-ml-por-empresa/41-04-PLAN.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-03-SUMMARY.md
@app/Models/Company.php
@app/Models/MlToken.php
@resources/js/Pages/Dev/Desenvolvimento.jsx
@resources/js/Layouts/AppLayout.jsx
@routes/web.php
@CLAUDE.md

<interfaces>
<!-- Pattern de Controller admin (referencia DevController existente — role:admin grupo) -->

Rotas devem entrar DENTRO do grupo existente `Route::middleware('role:admin')->group(...)` em `routes/web.php` (linha 300 — onde dev.desenvolvimento e dev.resync ja moram). Adicionar imediatamente APOS as rotas existentes do `/dev/*`:

```php
Route::prefix('dev/sugadores-ml-onboarding')->name('dev.sugadores_ml_onboarding.')->group(function () {
    Route::get('/', [SugadoresMlOnboardingController::class, 'index'])->name('index');
    Route::post('/{company}/smoke', [SugadoresMlOnboardingController::class, 'runSmoke'])->name('smoke');
    Route::post('/{company}/shadow', [SugadoresMlOnboardingController::class, 'runShadow'])->name('shadow');
    Route::post('/{company}/toggle-shadow', [SugadoresMlOnboardingController::class, 'toggleShadow'])->name('toggle_shadow');
});
```

Import no topo do routes/web.php: `use App\Http\Controllers\Dev\SugadoresMlOnboardingController;`.

**Controller** namespace `App\Http\Controllers\Dev` (cria dir `app/Http/Controllers/Dev/` se nao existir).

Helper privado `buildRow(Company $c, ?array $shadowStats): array` constroi payload por empresa. Eager load `with(['mlToken', 'mlAdvertiser', 'sugadorMlConfig'])` para evitar N+1.

Calculo de `token_state`:
- `mlToken === null` → 'missing'
- `mlToken->status === 'error_refresh'` ou `'revoked'` → 'error_refresh'
- `mlToken->isExpired()` → 'expired'
- senao → 'active'

Calculo de `advertiser`:
- `mlAdvertiser === null` → 'none'
- senao → 'ok'

Calculo de `smoke_last_at`:
- Procurar em `storage/app/sugadores/ml-smoke/` por arquivo cujo prefixo eh `{company->id}-*.json`. Pegar mtime do mais recente. Se nenhum, null.
- Usar `Storage::disk('local')->files('sugadores/ml-smoke')` + filter por prefixo + `lastModified()`.

Calculo de `shadow_paridade_7d`:
- Buscar `SugadorProviderRun::where('company_id', $c->id)->where('reference_date', '>=', now()->subDays(7)->toDateString())->where('status', 'completed')->orderBy('reference_date', 'desc')`.
- Se 0 rows → null.
- Senao: chamar `ProviderComparisonService::compareWindow($c->id, now()->subDays(7), now())` e extrair `paridade_motivos_pct` (ou nome similar do payload Plan 40-03). Verificar o nome exato da chave via Read em `app/Services/Sugadores/ProviderComparisonService.php` antes de implementar.

Calculo de `status` geral:
- `sugadorMlConfig?->primary_enabled === true` → 'ml_primary'
- `sugadorMlConfig?->shadow_enabled === true && shadow_paridade_7d !== null && shadow_paridade_7d >= 95` → 'ml_primary_candidate'
- `sugadorMlConfig?->shadow_enabled === true` → 'ml_shadow'
- default → 'adman_only'

Calculo de `actions` (array de boolean flags que o frontend usa para mostrar/esconder botoes):
- `can_run_smoke`: token_state === 'active'
- `can_run_shadow`: token_state === 'active' && advertiser === 'ok'
- `can_toggle_shadow`: token_state === 'active' && advertiser === 'ok'

**Actions:**

```php
public function runSmoke(Company $company)
{
    // Dispatch async via Artisan::queue - nao bloqueia request.
    Artisan::queue('sugadores:ml-smoke', ['--company' => $company->id]);
    return back()->with('success', "Smoke ML disparado para {$company->name}.");
}

public function runShadow(Company $company)
{
    Artisan::queue('sugadores:shadow-ml', ['--company' => (string) $company->id, '--days' => 1]);
    return back()->with('success', "Shadow ML disparado para {$company->name}.");
}

public function toggleShadow(Company $company)
{
    $config = SugadorMlCompanyConfig::firstOrNew(['company_id' => $company->id]);
    $wasEnabled = (bool) $config->shadow_enabled;
    $config->shadow_enabled = ! $wasEnabled;
    $config->shadow_started_at = $wasEnabled ? null : now()->toDateString();
    if (! $config->exists) {
        $config->primary_enabled = false;
    }
    $config->save();

    $msg = $wasEnabled
        ? "Shadow desativado para {$company->name}."
        : "Shadow ativado para {$company->name}.";

    return back()->with('success', $msg);
}
```

**React Page** `resources/js/Pages/Dev/SugadoresMlOnboarding/Index.jsx`:

- import AppLayout, cn, router, lucide-react icons (RefreshCw, Activity, AlertTriangle, Check, ChevronRight, etc), useState, useMemo
- recebe prop `companies` (array) + `flash` (do shared)
- header com contadores (Total | Sem token | Shadow ativo | Candidatos primary | ML primary)
- filtros: chips para `status` (todos, adman_only, ml_shadow, ml_primary_candidate, ml_primary), `token_state` (todos, missing, expired, error_refresh, active) + busca por nome
- Tabela com 6 colunas: Empresa (nome + id), mlToken (badge), Advertiser (✓ ou —), Smoke (data ou —), Shadow (paridade % ou — + toggle), Status (chip), Acoes (botoes "Rodar smoke" / "Rodar shadow" / "Toggle shadow" — gated pelo `actions.can_*`)
- Cada acao dispara `router.post(route('dev.sugadores_ml_onboarding.smoke', { company: row.id }))` etc.
- Usar tokens `ecf-*` + `cn()` + `DevCard` (definido em Desenvolvimento.jsx OU re-implementado local — usar local re-imp para nao acoplar a Desenvolvimento.jsx).
- pt-BR.

**Sidebar** (`AppLayout.jsx`):
- Grupo Sistema (linhas ~79-84) atualmente tem 3 children: 'Log', 'Desenvolvimento', 'ML OAuth'.
- Adicionar entre 'Desenvolvimento' e 'ML OAuth':
  ```
  { label: 'Sugadores ML Onboarding', routeName: 'dev.sugadores_ml_onboarding.index', page: 'Dev/SugadoresMlOnboarding/Index', icon: Activity, excludeRoles: ['consultor', 'mentor', 'publicador', 'analista', 'gestor', 'lider'] },
  ```
- (Activity icone ja eh importado por Desenvolvimento.jsx em lucide-react — verificar import no AppLayout; se faltar, adicionar)
- Adicionar tambem permissao `sistema.desenvolvimento` OU usar `excludeRoles` para restringir ao admin (consistente com pattern Phase 24 Painel Executivo). Adotar `excludeRoles` para evitar criar permissao nova.

**Test patterns:**

Suite Feature `Tests\Feature\Phase41\SugadoresMlOnboardingControllerTest`:
- Use `RefreshDatabase`, `Inertia::testing` se necessario.
- Helper `actingAsAdmin(): User` — cria User com role=admin.
- Helper `actingAsConsultor(): User`.
- Helper `Storage::fake('local')` para smoke files.

Tests (10):
- T1 `index_renderiza_inertia_component`: actingAsAdmin → GET /dev/sugadores-ml-onboarding → assertOk + Inertia assertComponent 'Dev/SugadoresMlOnboarding/Index'.
- T2 `index_payload_tem_companies_array`: cria 2 empresas active=true + 1 active=false → GET → asserts payload `companies` eh array com 2 itens (active=false excluida).
- T3 `index_company_row_tem_keys_esperadas`: GET → para cada row asserts keys `id`, `name`, `token_state`, `advertiser`, `smoke_last_at`, `shadow_paridade_7d`, `status`, `actions` presentes.
- T4 `index_token_state_calculado_corretamente`: cria 4 empresas com mlToken nos 4 estados (missing, expired, error_refresh, active) → asserts cada row tem token_state esperado.
- T5 `index_advertiser_state_ok_quando_mlAdvertiser_existe`: cria empresa + MlAdvertiser → asserts row.advertiser === 'ok'.
- T6 `index_status_geral_derivado`: cria 4 empresas com configs distintas → asserts cada uma com status correto (adman_only / ml_shadow / ml_primary_candidate / ml_primary).
- T7 `index_nao_admin_recebe_403`: actingAsConsultor → GET → assertForbidden (403).
- T8 `toggleShadow_flip_funciona`: actingAsAdmin + cria empresa. POST `/toggle-shadow` → assertRedirect back + DB tem SugadorMlCompanyConfig::shadow_enabled = true + shadow_started_at = hoje. POST de novo → shadow_enabled = false + shadow_started_at = null.
- T9 `runSmoke_e_runShadow_dispatcham_comandos`: `Artisan::fake()` ou `Bus::fake()` apropriado. POST smoke → assert Artisan::queue chamada com 'sugadores:ml-smoke' + --company=id. POST shadow → assert Artisan::queue chamada com 'sugadores:shadow-ml'.
- T10 `rotas_protegidas_por_role_admin`: as 4 rotas (GET index + 3 POST) testadas com actingAsConsultor → todas retornam 403.

Cada test `#[\PHPUnit\Framework\Attributes\Test]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): Suite SugadoresMlOnboardingControllerTest com 10 tests Feature</name>
  <files>tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php</files>

  <read_first>
    - .planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md (§5 UI admin contrato)
    - .planning/phases/41-onboarding-ml-por-empresa/41-01-PLAN.md (Models MlAdvertiser + SugadorMlCompanyConfig)
    - .planning/phases/41-onboarding-ml-por-empresa/41-04-PLAN.md (ShadowRunService injeta ml_metrics)
    - .planning/phases/40-shadow-mode-tabelas-de-compara-o/40-03-SUMMARY.md (ProviderComparisonService::compareWindow assinatura)
    - app/Models/Company.php
    - app/Models/MlToken.php (estados active/error_refresh/expirado)
    - routes/web.php (pattern role:admin existente + DevController grupo)
    - tests/Feature/Phase40/SugadoresShadowMlCommandTest.php (helpers pattern Mockery)
  </read_first>

  <behavior>
    Implementar os 10 testes da `<interfaces>` (Tests T1..T10). Helpers obrigatorios:

    - `actingAsAdmin(): User` — usa User factory ou Model::create com role=admin + active=true; `$this->actingAs($user)`.
    - `actingAsConsultor(): User` — analogo com role=consultor.
    - `makeCompanyWithToken(string $tokenState = 'active'): Company` — cria empresa + mlToken correspondente:
      - 'missing' → sem mlToken
      - 'expired' → mlToken expires_at = now()->subHour(), status = 'active'
      - 'error_refresh' → mlToken expires_at = now()->addHour(), status = 'error_refresh'
      - 'active' → mlToken expires_at = now()->addHour(), status = 'active'
    - `enableShadowFor(Company $c, bool $primary = false): SugadorMlCompanyConfig`.
    - `createShadowRunWithParidade(Company $c, float $pct): SugadorProviderRun` — cria 1 row provider='ml' + 1 row provider='adman' com items que resultem em paridade ~$pct (ou mockar `ProviderComparisonService` via bind container retornando payload com chave `paridade_motivos_pct => $pct`).

    Para T9 (dispatch): usar `Artisan::shouldReceive('queue')->once()->with('sugadores:ml-smoke', Mockery::on(fn ($args) => $args['--company'] === $company->id))` OU `Artisan::fake()` + `Artisan::assertSent`.

    Cada test com `#[Test]`. tearDown: Mockery::close.

    Para T1/T2/T3 (Inertia), usar:
    ```php
    $response = $this->actingAs($admin)->get('/dev/sugadores-ml-onboarding');
    $response->assertOk();
    $response->assertInertia(fn (Assert $page) => $page
        ->component('Dev/SugadoresMlOnboarding/Index')
        ->has('companies', 2)
    );
    ```
    (Usar `Inertia\Testing\AssertableInertia as Assert` — pattern existente do projeto Phase 24 etc.)

    Tests devem FALHAR todos (RED) — Controller, rotas, e React page nao existem ainda.
  </behavior>

  <action>
    Criar `tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php`.

    Imports:
    - `App\Models\{User, Company, MlToken, MlAdvertiser, SugadorMlCompanyConfig, SugadorProviderRun, SugadorProviderItem}`
    - `App\Services\Sugadores\ProviderComparisonService`
    - `Illuminate\Foundation\Testing\RefreshDatabase`
    - `Illuminate\Support\Facades\{Artisan, Storage}`
    - `Inertia\Testing\AssertableInertia as Assert`
    - `Mockery`, `PHPUnit\Framework\Attributes\Test`, `Carbon\Carbon`

    Implementar 10 tests com comentarios pt-BR.

    Para evitar dependencia real do ProviderComparisonService nos testes que validam paridade (T6), bindar mock no container retornando payload com chave esperada — verificar via Read em `app/Services/Sugadores/ProviderComparisonService.php` o nome exato da chave (ex: `paridade_motivos_pct` ou `paridade_pct` ou similar).

    Tests devem FALHAR todos com 404 (rota nao existe), `Class App\Http\Controllers\Dev\SugadoresMlOnboardingController does not exist`, ou similar.
  </action>

  <verify>
    <automated>php artisan test --filter=SugadoresMlOnboardingControllerTest</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php` existe.
    - `grep -c "#\[Test\]" tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php` retorna >= 10.
    - `grep -c "actingAsAdmin\|actingAs" tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php` retorna >= 10.
    - `grep -c "Inertia\|Inertia::testing\|AssertableInertia" tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php` retorna >= 3.
    - `php artisan test --filter=SugadoresMlOnboardingControllerTest` retorna 10/10 FAIL/ERROR (RED).
    - Commit prefixo `test(41-05):` mencionando "RED".
  </acceptance_criteria>

  <done>
    Suite RED commitada. 10 tests falhando. Nenhum arquivo de producao (Controller, route, JSX, sidebar) tocado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): Implementa Controller + 4 rotas + React page + edita sidebar + npm run build</name>
  <files>
    app/Http/Controllers/Dev/SugadoresMlOnboardingController.php,
    routes/web.php,
    resources/js/Pages/Dev/SugadoresMlOnboarding/Index.jsx,
    resources/js/Layouts/AppLayout.jsx
  </files>

  <read_first>
    - tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php (escrito na Tarefa 1)
    - .planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md (§5 UI completa)
    - routes/web.php (linha 300 grupo role:admin existente — entrada das rotas novas)
    - resources/js/Layouts/AppLayout.jsx (linhas 79-84 grupo Sistema)
    - resources/js/Pages/Dev/Desenvolvimento.jsx (pattern DevCard, cn(), tokens ecf-*)
    - app/Services/Sugadores/ProviderComparisonService.php (assinatura compareWindow + chave de paridade no payload)
    - app/Models/MlToken.php (isExpired, status enum)
    - CLAUDE.md (pt-BR, npm run build, DevCard)
  </read_first>

  <action>
    **1. Criar `app/Http/Controllers/Dev/SugadoresMlOnboardingController.php`**:

    Cria dir `app/Http/Controllers/Dev/` se nao existir.

    Namespace `App\Http\Controllers\Dev`. Extends `App\Http\Controllers\Controller`.

    Constructor: `public function __construct(private ProviderComparisonService $comparison) {}`.

    Implementar 4 actions (index, runSmoke, runShadow, toggleShadow) e 1 helper privado `buildRow` conforme `<interfaces>` do `<context>`.

    `index()`:
    - Eager load: `Company::with(['mlToken', 'mlAdvertiser', 'sugadorMlConfig'])->where('active', true)->orderBy('name')->get()`.
    - Para cada empresa, calcular paridade 7d (via try/catch — se ProviderComparisonService lancar, fica null com log warning, NAO interrompe a pagina).
    - Map para `$this->buildRow(...)`.
    - `return Inertia::render('Dev/SugadoresMlOnboarding/Index', ['companies' => $rows])`.

    `buildRow(Company $c, ?array $shadowStats): array` retorna:
    ```php
    return [
        'id' => $c->id,
        'name' => $c->name,
        'token_state' => $this->resolveTokenState($c->mlToken),
        'advertiser' => $c->mlAdvertiser ? 'ok' : 'none',
        'smoke_last_at' => $this->resolveLastSmoke($c->id),
        'shadow_paridade_7d' => $shadowStats['paridade_motivos_pct'] ?? null,
        'status' => $this->resolveStatus($c->sugadorMlConfig, $shadowStats),
        'actions' => [
            'can_run_smoke' => $tokenState === 'active',
            'can_run_shadow' => $tokenState === 'active' && $c->mlAdvertiser !== null,
            'can_toggle_shadow' => $tokenState === 'active' && $c->mlAdvertiser !== null,
        ],
    ];
    ```

    `resolveLastSmoke(int $companyId): ?string`:
    ```php
    $files = Storage::disk('local')->files('sugadores/ml-smoke');
    $matches = array_filter($files, fn ($f) => str_starts_with(basename($f), "{$companyId}-"));
    if (empty($matches)) return null;
    $latest = max(array_map(fn ($f) => Storage::disk('local')->lastModified($f), $matches));
    return \Carbon\Carbon::createFromTimestamp($latest)->toIso8601String();
    ```

    `resolveStatus(...)`: switch/match conforme `<interfaces>`.

    Imports: Company, MlAdvertiser, MlToken, SugadorMlCompanyConfig, SugadorProviderRun, ProviderComparisonService, Carbon, Storage, Artisan, Inertia, Log.

    PHPDoc pt-BR explicando que e UI admin Phase 41 + nao grava em sugadores + Artisan::queue eh assincrono.

    **2. Edit `routes/web.php`**:

    Adicionar import no topo: `use App\Http\Controllers\Dev\SugadoresMlOnboardingController;`

    Adicionar bloco dentro do grupo `role:admin` existente (procurar bloco linhas 300-313 onde dev.desenvolvimento + dev.resync moram) imediatamente APOS `dev.resync`:
    ```php
    // Phase 41 — UI admin de onboarding ML por empresa
    Route::prefix('dev/sugadores-ml-onboarding')->name('dev.sugadores_ml_onboarding.')->group(function () {
        Route::get('/', [SugadoresMlOnboardingController::class, 'index'])->name('index');
        Route::post('/{company}/smoke', [SugadoresMlOnboardingController::class, 'runSmoke'])->name('smoke');
        Route::post('/{company}/shadow', [SugadoresMlOnboardingController::class, 'runShadow'])->name('shadow');
        Route::post('/{company}/toggle-shadow', [SugadoresMlOnboardingController::class, 'toggleShadow'])->name('toggle_shadow');
    });
    ```

    ZERO mudanca em outras rotas, middlewares, ou imports existentes.

    **3. Criar `resources/js/Pages/Dev/SugadoresMlOnboarding/Index.jsx`**:

    - Default export funcao componente `SugadoresMlOnboardingIndex({ companies })`.
    - Wraps em `<AppLayout>` (import default).
    - Header `<div>` com titulo "Sugadores ML — Onboarding por empresa" + subtitle pt-BR explicando proposito.
    - Contadores: useMemo para calcular `{ total, sem_token, shadow_ativo, candidatos_primary, ml_primary }` a partir de companies array.
    - Filtros: 2 selects nativos (status + token_state) + input busca. State via useState.
    - Tabela: 6 colunas. Cada row mapeia uma company filtrada. Botoes acionam `router.post(route('dev.sugadores_ml_onboarding.smoke', { company: row.id }))` etc.
    - Use `cn()` de `@/lib/utils`. Use tokens `ecf-yellow`, `ecf-card`, `bg-white/[0.02]`, `border-white/[0.08]`. Use Lucide icons (Activity, AlertTriangle, Check, RefreshCw, ToggleLeft, ToggleRight).
    - Componentes locais: `TokenBadge`, `StatusBadge`, `ParityCell` para reusar logica visual. Use date-fns format para `smoke_last_at` (mesmo pattern do Desenvolvimento.jsx).
    - pt-BR em TODAS as labels/tooltips/mensagens.

    Tamanho-alvo: 250-350 linhas. Nao replicar todo o sistema de Polos/etc — eh uma pagina admin enxuta.

    **4. Edit `resources/js/Layouts/AppLayout.jsx`**:

    No grupo Sistema (linhas 79-84), entre 'Desenvolvimento' e 'ML OAuth', adicionar:
    ```jsx
    { label: 'Sugadores ML Onboarding', routeName: 'dev.sugadores_ml_onboarding.index', page: 'Dev/SugadoresMlOnboarding/Index', icon: Activity, excludeRoles: ['consultor', 'mentor', 'publicador', 'analista', 'gestor', 'lider'] },
    ```

    Verificar se `Activity` ja eh import do lucide-react no topo de AppLayout.jsx; se nao, adicionar ao import. NAO duplicar imports.

    ZERO mudanca em outros items, gating, ou estrutura do menu.

    **5. Rodar build**:

    `npm run build` (CLAUDE.md mandate apos qualquer JSX). Validar que termina com `0 erros` e gera arquivos em `public/build/assets/`.

    Comentarios em pt-BR conforme CLAUDE.md em TODOS os arquivos.

    Apos tudo, rodar a suite completa de tests Phase 41 + suites de regressao.
  </action>

  <verify>
    <automated>php artisan test --filter=SugadoresMlOnboardingControllerTest && php artisan test --filter=Phase41 && php artisan test --filter=Phase40 && php artisan test --filter=Sugador && php artisan route:list --columns=method,uri,name | grep sugadores_ml_onboarding && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `app/Http/Controllers/Dev/SugadoresMlOnboardingController.php` existe + namespace correto.
    - `grep -c "function (index|runSmoke|runShadow|toggleShadow)" app/Http/Controllers/Dev/SugadoresMlOnboardingController.php` retorna >= 4 (regex EREgrep — adaptar se necessario para basic grep: 4 invocacoes manuais via `grep -E "public function (index|runSmoke|runShadow|toggleShadow)"`).
    - `php artisan route:list --columns=method,uri,name | grep -c "dev.sugadores_ml_onboarding\."` retorna >= 4 (4 rotas registradas).
    - Arquivo `resources/js/Pages/Dev/SugadoresMlOnboarding/Index.jsx` existe.
    - `grep -c "dev.sugadores_ml_onboarding.index" resources/js/Layouts/AppLayout.jsx` retorna 1.
    - `grep -c "Sugadores ML Onboarding" resources/js/Layouts/AppLayout.jsx` retorna 1.
    - `npm run build` exit code 0; saida termina sem erros (`grep -i "error" build_output.log` retorna 0 OU saida nao contem "error" no contexto de erro real — aceitar avisos npm).
    - Apos build, `ls public/build/assets/ | grep -i "SugadoresMlOnboarding"` retorna pelo menos 1 arquivo (chunk JS gerado).
    - `php artisan test --filter=SugadoresMlOnboardingControllerTest` retorna 10/10 PASS.
    - `php artisan test --filter=Phase41` retorna >= 40/40 PASS (acumulado 41-01..05 = 8+12+5+5+10 = 40).
    - `php artisan test --filter=Phase40` retorna 52/52 PASS (zero regressao).
    - `php artisan test --filter=Sugador` retorna >= 92/92 PASS (zero regressao baseline) — pode chegar a 132 se o filter pegar Phase 41 inteira.
    - Commit prefixo `feat(41-05):` mencionando "GREEN" + REQ-41-08 + REQ-41-09 + REQ-41-10.
  </acceptance_criteria>

  <done>
    UI admin completa. 4 rotas registradas + Controller + React page + sidebar item. npm build OK. 10/10 tests Phase 41-05 + acumulado Phase 41 >= 40 verde + zero regressao Phase 40/Sugador.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → Controller | Gated por auth + verified + role:admin |
| Controller → Storage local (smoke files) | Trusted (mesma maquina) |
| Controller → Artisan::queue | Trusted (mesmo processo, comandos validados Phase 38/40) |
| Controller → DB writes (SugadorMlCompanyConfig) | Confiavel — apenas admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05-01 | Elevation of privilege | Rota acessada por nao-admin | mitigate | Middleware role:admin (existente) + Tests T7+T10 validam 403 |
| T-41-05-02 | Tampering | toggleShadow para empresa nao-existente | accept | Route model binding lanca 404 automaticamente |
| T-41-05-03 | Denial-of-Service | Multiplos POST /smoke spamados | accept | Artisan::queue eh idempotente operacionalmente (smoke roda em background); pode-se adicionar throttling em iteracao futura |
| T-41-05-04 | Information disclosure | Payload index expoe paridade + metricas operacionais | accept | role:admin (mesmo nivel de DevController + ActivityLog) |
| T-41-05-05 | Tampering | primary_enabled mutado acidentalmente | mitigate | toggleShadow NAO toca primary_enabled. Phase 42 vai criar action separada com checkpoint adicional. Defensive coding: na firstOrNew, primary_enabled = false explicito ao criar nova row. |

Severidade global: MEDIUM (rota POST que escreve em DB + dispatcha comandos; mitigado por role:admin + bindings).
</threat_model>

<verification>
- `php artisan test --filter=SugadoresMlOnboardingControllerTest` → 10/10 PASS
- `php artisan test --filter=Phase41` → >= 40/40 PASS (acumulado 41-01..05)
- `php artisan test --filter=Phase40` → 52/52 PASS (zero regressao)
- `php artisan test --filter=Sugador` → >= 92/92 PASS (zero regressao baseline ou maior se filtrar Phase 41)
- `php artisan route:list --columns=method,uri,name | grep -c sugadores_ml_onboarding` → >= 4
- `npm run build` → exit 0 + chunks gerados em public/build/assets/
- Sidebar: `grep "Sugadores ML Onboarding" resources/js/Layouts/AppLayout.jsx` retorna 1 linha
- Manual sanity (NAO bloqueante — smoke real depende de MariaDB voltar): `php artisan tinker --execute "dd(route('dev.sugadores_ml_onboarding.index'));"` retorna URL valida
</verification>

<success_criteria>
Plan 41-05 completo quando:
- Controller `App\Http\Controllers\Dev\SugadoresMlOnboardingController` com 4 actions
- 4 rotas registradas no grupo role:admin
- React page Dev/SugadoresMlOnboarding/Index renderiza tabela + filtros + acoes
- Sidebar admin tem item novo gated por excludeRoles
- npm run build executado com sucesso
- 10/10 tests Phase 41-05 verdes
- Acumulado Phase 41: >= 40/40 (8 + 12 + 5 + 5 + 10)
- Suite Phase 40 + Sugador sem regressao
- 2 commits (RED + GREEN)
- REQ-41-08, REQ-41-09, REQ-41-10 cobertos
- **Phase 41 INTEIRA FECHADA** (5/5 plans)
</success_criteria>

<output>
Criar `.planning/phases/41-onboarding-ml-por-empresa/41-05-SUMMARY.md` quando done. Esse SUMMARY deve fechar a Phase 41 inteira documentando: 5 plans entregues, requirements REQ-41-01..10 cobertos, total de tests novos (>= 40), zero regressao em Phase 38/39/40/Sugador, build npm OK, prontidao para deploy.
</output>
