---
phase: 41-onboarding-ml-por-empresa
plan: 04
type: execute
wave: 3
depends_on: ["41-02", "41-03"]
files_modified:
  - app/Services/Sugadores/ShadowRunService.php
  - tests/Feature/Phase41/ShadowRunServiceMlMetricsTest.php
autonomous: true
requirements: [REQ-41-06]

must_haves:
  truths:
    - "ShadowRunService::executeForProvider injeta `ml_metrics` no summary JSON da run apenas quando provider='ml'"
    - "ml_metrics contem 6 chaves: total_calls, pages_read, rate_limit_429, refresh_token_count, total_duration_ms, backoff_sleep_ms"
    - "Provider 'adman' continua gerando summary SEM chave ml_metrics (back-compat com Plan 40-02 ShadowRunServiceTest)"
    - "Metricas vem do MercadoLivreAdsService::getLastRunMetrics() (Plan 41-02)"
    - "Coleta defensiva: se MercadoLivreAdsService nao estiver disponivel (DI/binding) ou lancar excecao no getLastRunMetrics, summary fica sem ml_metrics e log warning eh emitido — NAO interrompe a run"
  artifacts:
    - path: "app/Services/Sugadores/ShadowRunService.php"
      provides: "Injecao opcional do MercadoLivreAdsService via DI no constructor + mescla de metricas no summary apos run ml"
    - path: "tests/Feature/Phase41/ShadowRunServiceMlMetricsTest.php"
      provides: "Suite 5 tests cobrindo: ml summary tem ml_metrics, adman summary nao tem, valores reais sao propagados, excecao no getLastRunMetrics nao quebra a run, summary preserva chaves Plan 40-02 (campanhas/adgroups/items/skipped/reason)"
  key_links:
    - from: "ShadowRunService"
      to: "MercadoLivreAdsService"
      via: "DI no constructor (opcional via ?MercadoLivreAdsService)"
      pattern: "MercadoLivreAdsService"
    - from: "ShadowRunService::executeForProvider provider=ml"
      to: "summary.ml_metrics"
      via: "$this->mlAds->getLastRunMetrics() apos analyzer"
      pattern: "getLastRunMetrics"
---

<objective>
Estende `ShadowRunService` (Phase 40-02) para mesclar as metricas operacionais coletadas pelo `MercadoLivreAdsService` (Plan 41-02 `getLastRunMetrics`) no campo `summary` JSON da row `sugador_provider_runs` correspondente ao provider 'ml'. Provider 'adman' permanece SEM mudanca (gate de zero regressao).

Purpose: a UI admin (Plan 41-05) precisa exibir saude do path ML por empresa (429 hits, refresh count, duration) e essas metricas hoje sao perdidas — ficam apenas em property interna do service. Phase 40 ja deixou o `summary` JSON livre para extensao; este plan apenas plumbing.

Output: 1 service estendido + 1 suite Feature 5 tests verdes + zero regressao Plan 40-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md
@.planning/phases/41-onboarding-ml-por-empresa/41-02-PLAN.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-02-SUMMARY.md
@app/Services/Sugadores/ShadowRunService.php
@tests/Feature/Phase40/ShadowRunServiceTest.php

<interfaces>
<!-- Estado atual ShadowRunService (Phase 40-02). -->

Constructor atual:
```php
public function __construct(
    private SugadorAnalysisService $analysis,
) {}
```

Refactor Plan 41-04 — adicionar parametro opcional:
```php
use App\Services\Sugadores\MercadoLivreAdsService;

public function __construct(
    private SugadorAnalysisService $analysis,
    private ?MercadoLivreAdsService $mlAds = null,
) {}
```

Laravel container resolve `MercadoLivreAdsService` automaticamente (mesmo pattern do Plan 40-02). Param opcional + nullable permite que tests Mockery NAO precisem mockar o MercadoLivreAdsService quando apenas o provider 'adman' eh exercido.

`executeForProvider` apos `$result = $this->analysis->analyzeCompany(...)`, antes do `$run->update([...])`:

```php
$summary = [
    'campanhas' => (int) ($result['campanhas'] ?? 0),
    'adgroups'  => (int) ($result['adgroups']  ?? 0),
    'items'     => $items,
    'skipped'   => (bool) ($result['skipped'] ?? false),
    'reason'    => $result['reason'] ?? null,
];

// Phase 41-04 — mescla metricas operacionais ML no summary apenas quando provider='ml'
if ($providerName === 'ml' && $this->mlAds !== null) {
    try {
        $summary['ml_metrics'] = $this->mlAds->getLastRunMetrics();
    } catch (\Throwable $e) {
        Log::warning("[Sugadores Shadow] Falha ao coletar ml_metrics empresa {$company->id}: " . $e->getMessage());
    }
}

$run->update([
    'status'      => 'completed',
    'finished_at' => now(),
    'summary'     => $summary,
]);
```

Suite Plan 40-02 (`tests/Feature/Phase40/ShadowRunServiceTest.php` 9 tests) NAO pode regredir. Em particular Test 8 valida shape de retorno `['adman_run_id', 'ml_run_id', 'adman_status', 'ml_status']` — preservado.

Suite Plan 40-02 que valida `summary` (provavelmente Test 4 "status completed+summary") espera as 5 chaves atuais. Plan 41-04 ADICIONA `ml_metrics` apenas quando provider='ml' — Test 4 pode continuar valido se ele nao usa `assertEquals` strict no array de summary do provider ml (verificar). Caso contrario, Test 4 da Phase 40-02 PRECISA ser alterado para usar `assertArrayHasKey` no lugar de `assertEquals` — mas esta plan **NAO modifica tests Plan 40-02**. Em vez disso:
- Tests da Plan 41-04 cobrem o lado novo (`ml_metrics` injetada)
- A regressao do Plan 40-02 vai depender se o test atual usa `assertEquals` ou `assertArrayHasKey`.

**Decisao defensiva:** verificar antes do GREEN se Test 4 do Plan 40-02 quebra com a injecao. Se quebrar, ajustar o test no estilo Rule 1 (test obsoleto por mudanca de contrato — adicionar `assertArrayHasKey('ml_metrics', $summary)` quando provider='ml' OU usar `assertEqualsCanonicalizing` em vez de `assertEquals` no subset comum). Documentar decisao no SUMMARY.

`getLastRunMetrics` retorna 6 chaves (Plan 41-02 definiu): `total_calls`, `pages_read`, `rate_limit_429`, `refresh_token_count`, `backoff_sleep_ms`, `total_duration_ms`.

Para os tests novos da Plan 41-04, mockar `MercadoLivreAdsService` e configurar `getLastRunMetrics` para retornar um payload conhecido.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): Suite ShadowRunServiceMlMetricsTest com 5 tests</name>
  <files>tests/Feature/Phase41/ShadowRunServiceMlMetricsTest.php</files>

  <read_first>
    - .planning/phases/40-shadow-mode-tabelas-de-compara-o/40-02-SUMMARY.md (pattern Mockery + bind SugadorAnalysisService)
    - tests/Feature/Phase40/ShadowRunServiceTest.php (helpers makeCompanyWithConfig, detalhesFixture, mockAnalyzer)
    - .planning/phases/41-onboarding-ml-por-empresa/41-02-PLAN.md (contrato getLastRunMetrics — 6 chaves)
    - app/Services/Sugadores/ShadowRunService.php (estado atual Phase 40-02)
    - .planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md (§6 metricas operacionais)
  </read_first>

  <behavior>
    Tests Feature `Tests\Feature\Phase41\ShadowRunServiceMlMetricsTest` extends `Tests\TestCase`, `RefreshDatabase`. Reaproveitar helpers do Plan 40-02:
    - `makeCompanyWithConfig(): Company` (idem padrao Plan 40-02)
    - `detalhesFixture(int $count = 2): array`
    - `mockAnalyzer(array $admanPayload, array $mlPayload): void` — mock SugadorAnalysisService bindado.
    - **NOVO** `mockMlAds(array $metrics): void` — `Mockery::mock(MercadoLivreAdsService::class)` com `getLastRunMetrics()->andReturn($metrics)`, bind no container.

    setUp: `Carbon::setTestNow(Carbon::create(2026, 6, 26, 12, 0, 0))` para determinismo. tearDown: Mockery::close + Carbon::setTestNow(null).

    Tests (5):

    - **Test 1** `summary_ml_contem_ml_metrics_quando_provider_ml`:
      - mockAnalyzer com payload Adman (2 detalhes) + payload ML (3 detalhes)
      - mockMlAds com metrics `['total_calls' => 7, 'pages_read' => 2, 'rate_limit_429' => 1, 'refresh_token_count' => 0, 'backoff_sleep_ms' => 1500, 'total_duration_ms' => 4200]`
      - Executa `app(ShadowRunService::class)->run($company, $referenceDate)`
      - Asserts: SugadorProviderRun::where(provider='ml')->first()->summary contem `ml_metrics`; ml_metrics tem as 6 chaves esperadas; valores batem com o mock.

    - **Test 2** `summary_adman_nao_contem_ml_metrics`:
      - mesmo setup do Test 1
      - SugadorProviderRun::where(provider='adman')->first()->summary NAO contem `ml_metrics` (assertArrayNotHasKey).
      - summary do adman continua tendo as 5 chaves originais Plan 40-02 (campanhas, adgroups, items, skipped, reason).

    - **Test 3** `summary_ml_preserva_chaves_baseline_plan_40_02`:
      - mesmo setup
      - SugadorProviderRun::where(provider='ml')->first()->summary tem TODAS as chaves: `campanhas`, `adgroups`, `items`, `skipped`, `reason`, `ml_metrics` (6 keys total: 5 antigas + 1 nova).
      - Valida via assertArrayHasKey em loop (5 keys originais).

    - **Test 4** `excecao_em_getLastRunMetrics_nao_interrompe_run`:
      - mockAnalyzer normal
      - mockMlAds com `getLastRunMetrics()->andThrow(new \RuntimeException('Failed to compute'))`
      - Executa run
      - Asserts:
        - exit nao lanca excecao (try/catch interno engole)
        - SugadorProviderRun ml status == 'completed' (analyzer foi bem sucedido; so a coleta de metricas falhou)
        - summary ml NAO tem chave `ml_metrics` (skip silencioso + log warning)
        - Log::shouldHaveReceived('warning') com substring `[Sugadores Shadow]` e `ml_metrics` (use Log::spy ou assertion no `Log::warning` mockado).

    - **Test 5** `mlAds_null_no_constructor_continua_funcionando`:
      - **NAO** mockMlAds (passa null no construtor diretamente)
      - Construir o service via `new ShadowRunService(app(SugadorAnalysisService::class), null)` — mockAnalyzer ja bindou SugadorAnalysisService.
      - Asserts: run completa OK, summary ml nao tem ml_metrics (defensa contra DI nao resolvida).

    Cada test `#[\PHPUnit\Framework\Attributes\Test]`.
  </behavior>

  <action>
    Criar `tests/Feature/Phase41/ShadowRunServiceMlMetricsTest.php`. Copiar boa parte do scaffold do `tests/Feature/Phase40/ShadowRunServiceTest.php` para preservar pattern.

    Imports adicionais: `App\Services\Sugadores\MercadoLivreAdsService`, `Illuminate\Support\Facades\Log`.

    Implementar 5 tests da `<behavior>` com comentarios pt-BR explicando cada caso. Para Test 4, usar `Log::spy()` ou `Log::shouldReceive('warning')->once()->withArgs(fn ($msg) => str_contains($msg, '[Sugadores Shadow]'))`.

    Tests devem FALHAR todos (RED) — ShadowRunService atual nao injeta MercadoLivreAdsService nem mescla ml_metrics.
  </action>

  <verify>
    <automated>php artisan test --filter=ShadowRunServiceMlMetricsTest</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `tests/Feature/Phase41/ShadowRunServiceMlMetricsTest.php` existe.
    - `grep -c "#\[Test\]" tests/Feature/Phase41/ShadowRunServiceMlMetricsTest.php` retorna >= 5.
    - `grep -c "MercadoLivreAdsService" tests/Feature/Phase41/ShadowRunServiceMlMetricsTest.php` retorna >= 3.
    - `grep -c "ml_metrics" tests/Feature/Phase41/ShadowRunServiceMlMetricsTest.php` retorna >= 5.
    - `php artisan test --filter=ShadowRunServiceMlMetricsTest` retorna 5/5 FAIL/ERROR (RED).
    - Commit prefixo `test(41-04):` mencionando "RED".
  </acceptance_criteria>

  <done>
    Suite RED commitada. 5 tests falhando. Service atual intacto.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): Estende ShadowRunService para injetar MercadoLivreAdsService + mesclar ml_metrics no summary</name>
  <files>app/Services/Sugadores/ShadowRunService.php</files>

  <read_first>
    - tests/Feature/Phase41/ShadowRunServiceMlMetricsTest.php (escrito na Tarefa 1)
    - tests/Feature/Phase40/ShadowRunServiceTest.php (em especial Test 4 — verificar se summary eh assertEquals strict ou assertArrayHasKey)
    - app/Services/Sugadores/ShadowRunService.php (estado atual)
    - .planning/phases/41-onboarding-ml-por-empresa/41-02-PLAN.md (contrato getLastRunMetrics)
  </read_first>

  <action>
    Editar `app/Services/Sugadores/ShadowRunService.php`:

    1. **Adicionar import** no topo:
       ```
       use App\Services\Sugadores\MercadoLivreAdsService;
       ```

    2. **Estender construtor** (linhas 37-39 atual):
       ```php
       public function __construct(
           private SugadorAnalysisService $analysis,
           private ?MercadoLivreAdsService $mlAds = null,
       ) {}
       ```

       Comentario pt-BR explicando que mlAds eh opcional (Plan 41-04) para nao quebrar test Plan 40-02 que mockava so SugadorAnalysisService.

    3. **Modificar `executeForProvider`** — apos a chamada `$result = $this->analysis->analyzeCompany(...)` (linha 110-115 atual) e ANTES do `$run->update(['status' => 'completed', ...])` (linha 124):
       ```php
       $summary = [
           'campanhas' => (int) ($result['campanhas'] ?? 0),
           'adgroups'  => (int) ($result['adgroups']  ?? 0),
           'items'     => $items,
           'skipped'   => (bool) ($result['skipped'] ?? false),
           'reason'    => $result['reason'] ?? null,
       ];

       // Phase 41-04 — mescla metricas operacionais ML no summary apenas quando
       // provider='ml'. Coleta defensiva: excecao no getLastRunMetrics NAO
       // interrompe a run (apenas warning no log).
       if ($providerName === 'ml' && $this->mlAds !== null) {
           try {
               $summary['ml_metrics'] = $this->mlAds->getLastRunMetrics();
           } catch (\Throwable $e) {
               Log::warning("[Sugadores Shadow] Falha ao coletar ml_metrics empresa {$company->id}: " . $e->getMessage());
           }
       }

       $run->update([
           'status'      => 'completed',
           'finished_at' => now(),
           'summary'     => $summary,
       ]);
       ```

    4. **Verificar Plan 40-02 Test 4 (regressao):**
       - ANTES de commitar, rodar `php artisan test --filter=ShadowRunServiceTest` para validar.
       - Se Test 4 falhar com `expected array X, got array Y with extra ml_metrics`, aplicar Rule 1 minimo: ajustar a assertion daquele test para usar `assertArrayHasKey` em vez de `assertEquals` strict, OU validar subset com `assertEqualsCanonicalizing(array_intersect_key($actual, $expected), $expected)`. Documentar a mudanca como "Rule 1 — test atualizado para refletir extensao opcional Phase 41-04, comportamento original preservado para provider=adman".
       - Se Test 4 ja eh tolerante (`assertArrayHasKey`), nada a fazer.

    ZERO mudanca em:
    - `run()` (orquestrador externo)
    - `buildItemPayload()` 
    - `computeRawHash()`, `ksortRecursive()`, `isAssoc()`
    - logica de gate `$dryRun = true` (REQ-40-02 intacto)
    - try/catch por provider (preservado)
    - shape do retorno publico de `run()`
    - PHPDoc principal do gate REQ-40-02

    Adicionar PHPDoc pt-BR no construtor explicando a injecao opcional do MercadoLivreAdsService.

    Verificar via `git diff app/Services/Sugadores/ShadowRunService.php` que o diff esta concentrado em ~20 linhas + import.
  </action>

  <verify>
    <automated>php artisan test --filter=ShadowRunServiceMlMetricsTest && php artisan test --filter=ShadowRunServiceTest && php artisan test --filter=Phase40 && php artisan test --filter=Sugador</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "MercadoLivreAdsService" app/Services/Sugadores/ShadowRunService.php` retorna >= 2 (import + property).
    - `grep -c "ml_metrics" app/Services/Sugadores/ShadowRunService.php` retorna 1 (a unica chave nova).
    - `grep -c "getLastRunMetrics" app/Services/Sugadores/ShadowRunService.php` retorna 1.
    - `grep -c "dryRun" app/Services/Sugadores/ShadowRunService.php` retorna 5 (gate REQ-40-02 intacto — Plan 40-02 SUMMARY validou).
    - `grep -c "Sugador::create\|Sugador::insert\|->sugadores->" app/Services/Sugadores/ShadowRunService.php` retorna 0 (gate REQ-40-02 estatico intacto).
    - `php artisan test --filter=ShadowRunServiceMlMetricsTest` retorna 5/5 PASS.
    - `php artisan test --filter=ShadowRunServiceTest` retorna 9/9 PASS (zero regressao Plan 40-02 — se foi necessario ajuste Rule 1 no Test 4, documentar).
    - `php artisan test --filter=Phase40` retorna 52/52 PASS.
    - `php artisan test --filter=Phase41` retorna >= 25/25 PASS (8 schema 41-01 + 12 backoff 41-02 + 5 config table 41-03 + 5 metrics 41-04 = 30 — aceitar >= 25 caso algum nao tenha rodado ainda na pipeline).
    - `php artisan test --filter=Sugador` retorna >= 92/92 PASS (zero regressao).
    - Commit prefixo `feat(41-04):` mencionando "GREEN" + REQ-41-06.
  </acceptance_criteria>

  <done>
    ShadowRunService mescla ml_metrics no summary quando provider=ml. Coleta defensiva nao interrompe run. Suite Plan 41-04 verde + zero regressao Plan 40-02/40/Phase 41-01/02/03/Sugador. Plan 41-05 pode ler `summary->ml_metrics` da row para exibir saude por empresa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MercadoLivreAdsService → summary JSON | Confiavel (componente do mesmo subsystem) |
| summary JSON → UI Plan 41-05 | Output read-only, sem privilege escalation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04-01 | Denial-of-Service | Excecao em getLastRunMetrics interrompe a row 'completed' | mitigate | try/catch dedicado; log warning + summary fica sem ml_metrics, sem alterar status |
| T-41-04-02 | Tampering | mlAds null poderia ser sobrescrito acidentalmente | accept | Property private + nullable + readonly por contrato (Laravel resolve automaticamente em prod) |
| T-41-04-03 | Information disclosure | ml_metrics expoe rate_limit_429 e refresh_count em endpoint admin | accept | UI da Plan 41-05 ja gated por role:admin |

Severidade global: LOW (extensao opcional + try/catch defensivo).
</threat_model>

<verification>
- `php artisan test --filter=ShadowRunServiceMlMetricsTest` → 5/5 PASS
- `php artisan test --filter=ShadowRunServiceTest` → 9/9 PASS (sem regressao)
- `php artisan test --filter=Phase40` → 52/52 PASS
- `php artisan test --filter=Phase41` → >= 30/30 PASS (acumulado 41-01..04)
- `php artisan test --filter=Sugador` → >= 92/92 PASS
- Gate REQ-40-02 preservado: `grep "dryRun" app/Services/Sugadores/ShadowRunService.php | wc -l` >= 5
</verification>

<success_criteria>
Plan 41-04 completo quando:
- ShadowRunService injeta MercadoLivreAdsService opcional via DI
- summary ml contem ml_metrics com 6 chaves quando provider=ml e mlAds disponivel
- summary adman intacto (5 chaves Plan 40-02)
- Excecao em getLastRunMetrics nao interrompe run
- 5/5 tests Plan 41-04 verdes
- 9/9 tests Plan 40-02 continuam verdes (zero regressao OU Rule 1 documentada)
- Suite Phase 40 + Sugador intactas
- 2 commits (RED + GREEN)
- REQ-41-06 fechado (parte ShadowRunService)
</success_criteria>

<output>
Criar `.planning/phases/41-onboarding-ml-por-empresa/41-04-SUMMARY.md` quando done.
</output>
