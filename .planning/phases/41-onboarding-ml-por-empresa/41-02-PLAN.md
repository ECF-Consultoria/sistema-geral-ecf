---
phase: 41-onboarding-ml-por-empresa
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - app/Services/Sugadores/MercadoLivreAdsService.php
  - app/Providers/AppServiceProvider.php
  - tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php
autonomous: true
requirements: [REQ-41-03, REQ-41-04, REQ-41-05, REQ-41-06]

must_haves:
  truths:
    - "Service ganha metodo privado `callWithBackoff(string $url, array $headers, ?string $sellerId, int $maxAttempts=5): array` aplicado nos 4 metodos publicos"
    - "429: respeita header Retry-After (clamp 1..60s) + jitter 0..1s + retry"
    - "5xx: backoff exponencial 2^n com cap 60s + jitter + retry"
    - "401: tenta refresh token 1x (delegando ao MercadoLivreService::ensureValidToken via get()) + retry; segundo 401 lanca RuntimeException"
    - "403: lanca RuntimeException IMEDIATA sem retry (motivo 'scope_missing')"
    - "discoverAdvertiser le cache `ml_advertisers` primeiro: se existe e discovered_at >= now()-7d → retorna do cache sem HTTP; senao busca + upsert"
    - "RateLimiter::for('ml-api', ...) registrado em AppServiceProvider com Limit::perMinute(60)->by(\\$sellerId)"
    - "Helper privado `withRateLimit(string $sellerId, callable $cb)` aborta com RuntimeException se RateLimiter::attempt retornar false"
    - "Service coleta metricas (total_calls, pages_read, rate_limit_429, refresh_token_count, total_duration_ms) durante a chamada e expoe via `getLastRunMetrics(): array`"
    - "resetMetrics() reseta o contador (chamado no inicio de cada metodo publico)"
  artifacts:
    - path: "app/Services/Sugadores/MercadoLivreAdsService.php"
      provides: "callWithBackoff + cache advertiser + withRateLimit + getLastRunMetrics integrados"
    - path: "app/Providers/AppServiceProvider.php"
      provides: "RateLimiter::for('ml-api', ...) registrado em boot()"
    - path: "tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php"
      provides: "Suite Feature 10+ tests Http::fake cobrindo 429/5xx/401/403, cache hit/miss/expirado, rate limit triggered, metricas"
  key_links:
    - from: "MercadoLivreAdsService::discoverAdvertiser"
      to: "ml_advertisers table"
      via: "MlAdvertiser::firstOrNew(['company_id' => $company->id])"
      pattern: "MlAdvertiser::(firstOrNew|where|updateOrCreate)"
    - from: "MercadoLivreAdsService::withRateLimit"
      to: "RateLimiter ml-api bucket"
      via: "RateLimiter::attempt('ml-api:{seller_id}', 60, $cb)"
      pattern: "RateLimiter::(attempt|tooManyAttempts).*ml-api"
    - from: "MercadoLivreAdsService::callWithBackoff"
      to: "MercadoLivreService::get|Http facade"
      via: "Http::withHeaders chained"
      pattern: "Http::(fake|withHeaders|get)"
---

<objective>
Refatora `MercadoLivreAdsService` para producao real: substitui as chamadas diretas a `MercadoLivreService::get()` por um wrapper interno `callWithBackoff` que cobre 429 (Retry-After + cap 60s + jitter), 5xx (backoff exponencial cap 60s + jitter), 401 (refresh 1x via path Phase 20 ja existente), 403 (sem retry, scope_missing); adiciona cache de advertiser persistente na tabela `ml_advertisers` (Plan 41-01) com TTL 7 dias; registra rate limiter `ml-api` por seller_id (60 req/min) em `AppServiceProvider`; e coleta metricas operacionais expostas via `getLastRunMetrics()` para o `ShadowRunService` (Plan 41-04) mesclar no summary JSON.

Purpose: zerar risco operacional de bater rate limit ML (429 cascateando), reduzir latencia de discoverAdvertiser (chamadas repetidas por scheduler) via cache 7d, e habilitar visibilidade de saude do path ML.

Output: 1 service refatorado + 1 AppServiceProvider editado + 1 suite Http::fake 10+ tests verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md
@.planning/phases/41-onboarding-ml-por-empresa/41-01-PLAN.md
@.planning/phases/38-smoke-ml-piloto-bymobile/38-01-SUMMARY.md
@app/Services/Sugadores/MercadoLivreAdsService.php
@app/Services/MercadoLivreService.php
@app/Providers/AppServiceProvider.php

<interfaces>
<!-- Estado atual do MercadoLivreAdsService Phase 38 Plan 38-01 (que sera refatorado) -->

Constructor atual: `public function __construct(private MercadoLivreService $ml) {}`

4 metodos publicos atuais (vao ser wrapados com callWithBackoff + withRateLimit + metricas):
- `discoverAdvertiser(Company $company): array` → ja documentado, retorna {advertiser_id, site_id, seller_id, raw, url, status}
- `listCampaigns(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array` → loop de paginacao
- `listAds(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array` → loop de paginacao
- `tryFetchAdsMetrics(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array` → wrapper try/catch sobre listAds

`MercadoLivreService::get(Company, string, array, array)` (Phase 20 — linha 271) hoje JA faz:
- Refresh token 1x em 401 (linhas 283-298)
- Lanca RuntimeException em 401 persistente

A logica `callWithBackoff` da Phase 41 substitui a chamada `Http::withToken(...)->get(...)` DENTRO de `MercadoLivreService::get()` ou orquestra ao redor? **Decisao desta plan:** mantemos `MercadoLivreService::get()` intacto (out of scope — esta no §7 nao-tocar do CONTEXT). O `callWithBackoff` do MercadoLivreAdsService chama `$this->ml->get(...)` DENTRO de um loop com try/catch — captura `RuntimeException` que o `ml->get()` lanca e classifica via prefixo de mensagem (`401 persistente`, `Erro 4xx/5xx`, etc) OU melhor: usa Http::fake nos tests e chama `Http::withToken(...)` diretamente.

**Decisao operacional (melhor opcao):** o `callWithBackoff` do MercadoLivreAdsService chama `Http::withToken($token)->withHeaders(...)->get($url, $query)` DIRETO (nao via ml->get()) para ter acesso ao `$response->status()` puro. O `$token` vem de `$this->ml->ensureValidToken($company)` (ja existe — pattern do MercadoLivreService linha 273). Refresh em 401 chama `$this->ml->ensureValidToken($company, force: true)` ou similar — usar pattern existente.

API_BASE constante do MercadoLivreService (https://api.mercadolibre.com) — exportar como const publica ou expor via `MercadoLivreService::API_BASE` se ja for publica. Verificar via grep.

Pattern RateLimiter ja existente no AppServiceProvider (linhas 45-47):
```php
RateLimiter::for('adman-api', function () {
    return Limit::perMinute(8)->by('global');
});
```

Para 'ml-api' a chave NAO eh global — eh por seller. Sintaxe Laravel:
```php
RateLimiter::for('ml-api', function (Request $request, $sellerId = 'unknown') {
    return Limit::perMinute(60)->by($sellerId);
});
```

E o consumidor faz:
```php
$key = "ml-api:{$sellerId}";
if (RateLimiter::tooManyAttempts($key, 60)) {
    throw new \RuntimeException("Rate limit ML excedido para seller {$sellerId}");
}
RateLimiter::hit($key, 60);
return $callback();
```

(Esse padrao com tooManyAttempts + hit eh mais explicito que RateLimiter::attempt para metricas — permite incrementar `rate_limit_429` quando estouramos antes da chamada.)

Tabela `ml_advertisers` (Plan 41-01):
- company_id unique FK
- advertiser_id, seller_id, site_id, raw_data (array), discovered_at (immutable_datetime)
- Cache miss: discovered_at == null OU discovered_at < now()->subDays(7)
- Cache hit: retorna `['advertiser_id' => (int) $row->advertiser_id, 'site_id' => $row->site_id, 'seller_id' => $row->seller_id, 'raw' => $row->raw_data, 'url' => 'cache://ml_advertisers/'.$company->id, 'status' => 200, 'cached' => true]`

Metricas (estado interno via property privada `array $metrics`):
- total_calls (incrementa em cada Http::get real, NAO em cache hit)
- pages_read (incrementa em cada iteracao do do-while de listCampaigns/listAds)
- rate_limit_429 (incrementa em cada response com status 429, antes do sleep+retry)
- refresh_token_count (incrementa quando 401 dispara refresh)
- total_duration_ms (timestamp inicio no resetMetrics + delta em getLastRunMetrics)
- backoff_sleep_ms (somatorio de sleep em ms — opcional mas util)

`resetMetrics()` chamado no inicio de cada metodo publico (discoverAdvertiser, listCampaigns, listAds, tryFetchAdsMetrics) zera e marca timestamp inicio.

`getLastRunMetrics(): array` retorna snapshot. Chamado pelo `ShadowRunService` em Plan 41-04.

Pattern de teste Http::fake para multiplas respostas sequenciais:
```php
Http::fake([
    'api.mercadolibre.com/advertising/advertisers*' => Http::sequence()
        ->push(['advertisers' => []], 429, ['Retry-After' => '1'])
        ->push(['advertisers' => [['advertiser_id' => 999, 'seller_id' => '12345', 'site_id' => 'MLB']]], 200),
]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): Suite MercadoLivreAdsServiceBackoffTest com 10+ Http::fake scenarios</name>
  <files>tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php</files>

  <read_first>
    - .planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md (§3 rate limiter + §4 backoff + §6 metricas)
    - .planning/phases/38-smoke-ml-piloto-bymobile/38-01-SUMMARY.md (estado atual do service)
    - .planning/phases/41-onboarding-ml-por-empresa/41-01-PLAN.md (schema ml_advertisers — para mockar Cache hits)
    - app/Services/Sugadores/MercadoLivreAdsService.php (assinaturas atuais dos 4 metodos publicos)
    - app/Services/MercadoLivreService.php (linha 271 — `get()` e `ensureValidToken()`)
    - tests/Feature/Phase38/MlSmokeCommandTest.php (pattern Http::fake do projeto — Plan 38-02)
    - app/Providers/AppServiceProvider.php (pattern RateLimiter existente)
  </read_first>

  <behavior>
    Tests Feature usando RefreshDatabase + `Http::fake` + `Cache::flush()` no setUp + criacao de Company + MlToken valido. Helper `makeCompanyWithToken()` igual pattern Phase 38.

    Helpers privados:
    - `makeCompanyWithToken(): Company` — cria Company + MlToken status=active expires_at=+1h, access_token='fake-tok'.
    - `service(): MercadoLivreAdsService` — resolve do container (DI).

    **Backoff scenarios (REQ-41-03):**
    - Test 1 `discoverAdvertiser_retry_em_429_com_Retry_After`: Http::sequence push 429 com header `Retry-After: 1` → push 200 com payload valido. Asserts: response final retorna advertiser_id, total_calls metrica >= 2, rate_limit_429 == 1, getLastRunMetrics()['backoff_sleep_ms'] >= 1000.
    - Test 2 `discoverAdvertiser_backoff_exponencial_em_500`: Http::sequence push 500 → 500 → 200. Asserts: 3 tentativas, sleep entre tentativas >= 2s + 4s (= 6s minimo somado), retorna advertiser_id final.
    - Test 3 `discoverAdvertiser_refresh_token_em_401`: Http::sequence push 401 → 200. Mock `MercadoLivreService::ensureValidToken` Mockery para registrar 2 chamadas (uma inicial + uma com force=true). Asserts: refresh_token_count == 1, retorna 200.
    - Test 4 `discoverAdvertiser_aborta_em_403_sem_retry`: Http::fake → 403. Asserts: 1 chamada apenas (total_calls == 1, sem retry), RuntimeException lancada com mensagem contendo "scope" ou "403" ou "permiss".
    - Test 5 `discoverAdvertiser_aborta_apos_maxAttempts`: Http::fake sempre 500. Asserts: total_calls == 5 (maxAttempts), RuntimeException lancada.

    **Cache scenarios (REQ-41-04):**
    - Test 6 `discoverAdvertiser_cache_miss_grava_em_ml_advertisers`: ml_advertisers vazio. Http::fake 200 com advertiser_id=999. Asserts: response retorna 999, MlAdvertiser::where(company_id=...)->count() == 1, discovered_at recente, raw_data array.
    - Test 7 `discoverAdvertiser_cache_hit_pula_http`: pre-criar MlAdvertiser para company com discovered_at = now()->subDays(3). Http::fake registrado mas NAO chamado. Asserts: total_calls == 0 (cache hit), response['cached'] === true, advertiser_id == valor pre-existente. Http::assertNothingSent().
    - Test 8 `discoverAdvertiser_cache_expirado_chama_http_e_atualiza`: pre-criar MlAdvertiser com discovered_at = now()->subDays(8). Http::fake 200 com advertiser_id NOVO. Asserts: total_calls == 1, MlAdvertiser::count() == 1 (updateOrCreate), advertiser_id atualizado, discovered_at >= now()->subMinute().

    **Rate limiter scenarios (REQ-41-05):**
    - Test 9 `withRateLimit_aborta_quando_60_no_mesmo_minuto`: simular 60 hits no bucket `ml-api:SELLER123` via `RateLimiter::hit('ml-api:SELLER123', 60)` em loop 60x → 61a chamada via `listCampaigns` deve lancar RuntimeException contendo "rate limit" e "SELLER123". Asserts: RateLimiter::tooManyAttempts('ml-api:SELLER123', 60) === true antes da 61a.
    - Test 10 `AppServiceProvider_registra_ml_api_limiter`: assert `RateLimiter::limiter('ml-api')` is callable; invocar e validar retorna Limit instance com perMinute=60 (introspeccao via reflection ou comparacao `->maxAttempts === 60`).

    **Metricas (REQ-41-06):**
    - Test 11 `getLastRunMetrics_tem_chaves_esperadas`: chamar discoverAdvertiser uma vez, validar `$service->getLastRunMetrics()` contem keys: ['total_calls','pages_read','rate_limit_429','refresh_token_count','total_duration_ms','backoff_sleep_ms']. Tipos int (com `total_duration_ms` >= 0).
    - Test 12 `resetMetrics_zera_entre_chamadas`: chamar discoverAdvertiser, validar metricas != 0; chamar de novo (em endpoint diferente para evitar cache), validar pages_read/total_calls condizente com a 2a chamada apenas (nao acumula).

    Cada test usa `#[\PHPUnit\Framework\Attributes\Test]`. setUp/tearDown limpa Mockery + Cache + RateLimiter (`app('cache')->flush()` ou similar).
  </behavior>

  <action>
    Criar `tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php`. Namespace `Tests\Feature\Phase41`. Extends `Tests\TestCase`. Use `RefreshDatabase`.

    Imports essenciais: `App\Models\Company`, `App\Models\MlToken`, `App\Models\MlAdvertiser`, `App\Services\Sugadores\MercadoLivreAdsService`, `App\Services\MercadoLivreService`, `Illuminate\Support\Facades\Http`, `Illuminate\Support\Facades\RateLimiter`, `Illuminate\Support\Facades\Cache`, `Mockery`, `Carbon\Carbon`, `PHPUnit\Framework\Attributes\Test`.

    setUp: `parent::setUp(); Cache::flush(); RateLimiter::clear('ml-api:*');` (se metodo existir; senao limpar via reflection ou clear individual `ml-api:SELLER123`).

    tearDown: `Mockery::close(); parent::tearDown();`.

    Implementar os 12 testes da `<behavior>`. Para Test 3 (refresh token), mockar `MercadoLivreService` com `Mockery::mock(MercadoLivreService::class)` bindado via `$this->app->instance(MercadoLivreService::class, $mock)` — `ensureValidToken()` deve retornar MlToken instance.

    Para Test 7 (cache hit) + Test 8 (cache expirado): usar `Carbon::setTestNow(Carbon::create(2026, 6, 25))` em setUp para determinismo de `now()->subDays(7)`.

    Comentarios em pt-BR explicando o cenario coberto.

    Tests devem FALHAR todos (estado RED) — service atual nao tem callWithBackoff, cache, withRateLimit, nem metricas.
  </action>

  <verify>
    <automated>php artisan test --filter=MercadoLivreAdsServiceBackoffTest</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php` existe.
    - `grep -c "#\[Test\]" tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php` retorna >= 12.
    - `grep -c "Http::fake\|Http::sequence" tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php` retorna >= 8.
    - `grep -c "RateLimiter" tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php` retorna >= 2.
    - `grep -c "getLastRunMetrics" tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php` retorna >= 4.
    - `php artisan test --filter=MercadoLivreAdsServiceBackoffTest` retorna >= 12/12 FAIL ou ERROR (estado RED).
    - Mensagens de erro mencionam `callWithBackoff`, `getLastRunMetrics`, `ml-api`, `MlAdvertiser`, ou similares (sinalizando que o codigo de producao nao tem essas APIs ainda).
    - Commit prefixo `test(41-02):` mencionando "RED".
  </acceptance_criteria>

  <done>
    Suite RED commitada. 12+ tests falhando antes da Tarefa 2. Service de producao intacto.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): Refactor MercadoLivreAdsService + registra rate limiter ml-api em AppServiceProvider</name>
  <files>
    app/Services/Sugadores/MercadoLivreAdsService.php,
    app/Providers/AppServiceProvider.php
  </files>

  <read_first>
    - .planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md (§3, §4, §6)
    - tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php (escrito na Tarefa 1 — contrato esperado)
    - app/Services/Sugadores/MercadoLivreAdsService.php (estado atual Phase 38)
    - app/Services/MercadoLivreService.php (`get()`, `ensureValidToken()`, `refreshToken()` — assinaturas)
    - app/Providers/AppServiceProvider.php (pattern adman-api existente)
  </read_first>

  <action>
    **AppServiceProvider edit:**

    Adicionar APENAS dentro de `boot()` (apos `RateLimiter::for('adman-api', ...)`):
    ```
    // Phase 41 — Rate limiter ML por seller (NAO global). 60 req/min por seller_id
    // alinha com §3 do plano de migracao Sugadores Adman→ML ("Comecar conservador,
    // 60 req/min por seller"). Bucket dinamico via Limit::by($sellerId) — workers
    // concorrentes batem no MESMO bucket por seller via cache backend.
    RateLimiter::for('ml-api', function (Request $request, $sellerId = 'unknown') {
        return Limit::perMinute(60)->by($sellerId);
    });
    ```

    Import ja existe (`use Illuminate\Cache\RateLimiting\Limit; use Illuminate\Http\Request; use Illuminate\Support\Facades\RateLimiter;`). Validar via Read.

    ZERO mudanca em outras partes do AppServiceProvider (register(), Vite::prefetch, Event listeners, ecf-webhook limiter, adman-api limiter, etc).

    **MercadoLivreAdsService refactor:**

    Adicionar:
    - Property privada `private array $metrics = [];`
    - Property privada `private float $metricsStartedAt = 0.0;`
    - Constante `private const MAX_ATTEMPTS = 5;`
    - Constante `private const RATE_LIMIT_PER_MIN = 60;`
    - Constante `private const CACHE_TTL_DAYS = 7;`
    - Constante `private const BACKOFF_CAP_SECONDS = 60;`

    Adicionar metodos privados:
    ```
    private function resetMetrics(): void
    {
        $this->metrics = [
            'total_calls' => 0,
            'pages_read' => 0,
            'rate_limit_429' => 0,
            'refresh_token_count' => 0,
            'backoff_sleep_ms' => 0,
        ];
        $this->metricsStartedAt = microtime(true);
    }

    public function getLastRunMetrics(): array
    {
        return $this->metrics + [
            'total_duration_ms' => (int) round((microtime(true) - $this->metricsStartedAt) * 1000),
        ];
    }

    private function withRateLimit(?string $sellerId, callable $cb)
    {
        $sellerId ??= 'unknown';
        $key = "ml-api:{$sellerId}";
        if (RateLimiter::tooManyAttempts($key, self::RATE_LIMIT_PER_MIN)) {
            throw new \RuntimeException("Rate limit ML excedido para seller {$sellerId}");
        }
        RateLimiter::hit($key, 60);
        return $cb();
    }

    private function callWithBackoff(Company $company, string $url, array $query, array $headers, ?string $sellerId): array
    {
        $attempt = 0;
        while ($attempt < self::MAX_ATTEMPTS) {
            $attempt++;

            // Token resolvido a cada tentativa para suportar refresh em 401.
            $token = $this->ml->ensureValidToken($company);
            if (!$token) {
                throw new \RuntimeException("[MercadoLivreAds] Empresa {$company->id} sem token valido.");
            }

            $response = $this->withRateLimit($sellerId, fn () =>
                Http::withToken($token->access_token)
                    ->withHeaders($headers)
                    ->get($url, $query)
            );
            $this->metrics['total_calls']++;

            $status = $response->status();

            if ($status >= 200 && $status < 300) {
                return ['status' => $status, 'body' => $response->json() ?? [], 'url' => $url];
            }

            if ($status === 429) {
                $this->metrics['rate_limit_429']++;
                $retryAfter = (int) ($response->header('Retry-After') ?? 1);
                $wait = min(max($retryAfter, 1), self::BACKOFF_CAP_SECONDS);
                $waitMs = ($wait * 1000) + random_int(0, 1000);
                $this->metrics['backoff_sleep_ms'] += $waitMs;
                usleep($waitMs * 1000);
                continue;
            }

            if ($status >= 500) {
                $wait = min(2 ** $attempt, self::BACKOFF_CAP_SECONDS);
                $waitMs = ($wait * 1000) + random_int(0, 1000);
                $this->metrics['backoff_sleep_ms'] += $waitMs;
                usleep($waitMs * 1000);
                continue;
            }

            if ($status === 401) {
                if ($attempt === 1) {
                    // Refresh delegado ao MercadoLivreService (Phase 20)
                    try {
                        $this->ml->refreshToken($token);
                        $this->metrics['refresh_token_count']++;
                        continue;
                    } catch (\Throwable $e) {
                        throw new \RuntimeException("[MercadoLivreAds] Token ML invalido apos refresh: " . $e->getMessage());
                    }
                }
                throw new \RuntimeException("[MercadoLivreAds] 401 persistente apos refresh em {$url}");
            }

            if ($status === 403) {
                throw new \RuntimeException("[MercadoLivreAds] Permissao/scope ML ausente (HTTP 403) em {$url}");
            }

            throw new \RuntimeException("[MercadoLivreAds] HTTP {$status} em {$url}: " . mb_substr((string) $response->body(), 0, 500));
        }
        throw new \RuntimeException("[MercadoLivreAds] Max attempts ({$attempt}) excedido em {$url}");
    }
    ```

    **Cache de advertiser em `discoverAdvertiser`:**

    Refatorar `discoverAdvertiser(Company $company): array`:
    ```
    public function discoverAdvertiser(Company $company): array
    {
        $this->resetMetrics();

        $cached = MlAdvertiser::where('company_id', $company->id)->first();
        if ($cached && $cached->discovered_at && $cached->discovered_at->gt(now()->subDays(self::CACHE_TTL_DAYS))) {
            return [
                'advertiser_id' => (int) $cached->advertiser_id,
                'site_id'       => $cached->site_id,
                'seller_id'     => $cached->seller_id,
                'raw'           => (array) $cached->raw_data,
                'url'           => 'cache://ml_advertisers/' . $company->id,
                'status'        => 200,
                'cached'        => true,
            ];
        }

        $sellerId = $cached?->seller_id; // pode ser null no primeiro discover
        $result = $this->callWithBackoff(
            $company,
            self::API_BASE . self::ENDPOINT_ADVERTISERS,
            ['product_id' => 'PADS'],
            self::API_VERSION_HEADER,
            $sellerId,
        );

        $advertisers = $result['body']['advertisers'] ?? [];
        $advertiserId = isset($advertisers[0]['advertiser_id']) ? (int) $advertisers[0]['advertiser_id'] : null;
        $siteId       = $advertisers[0]['site_id']   ?? 'MLB';
        $sellerIdNew  = isset($advertisers[0]['seller_id']) ? (string) $advertisers[0]['seller_id'] : null;

        if ($advertiserId !== null) {
            MlAdvertiser::updateOrCreate(
                ['company_id' => $company->id],
                [
                    'advertiser_id' => (string) $advertiserId,
                    'seller_id'     => $sellerIdNew,
                    'site_id'       => $siteId,
                    'raw_data'      => $result['body'],
                    'discovered_at' => now(),
                ],
            );
        }

        return [
            'advertiser_id' => $advertiserId,
            'site_id'       => $siteId,
            'seller_id'     => $sellerIdNew,
            'raw'           => $result['body'],
            'url'           => $result['url'] . '?product_id=PADS',
            'status'        => $result['status'],
            'cached'        => false,
        ];
    }
    ```

    Note: precisa adicionar constante publica `public const API_BASE = 'https://api.mercadolibre.com';` no service (ou importar de MercadoLivreService). Verificar via Read se MercadoLivreService::API_BASE eh publica; se sim, usar `MercadoLivreService::API_BASE`; se nao, declarar local.

    **Refactor `listCampaigns` e `listAds`:**

    Substituir `$page = $this->ml->get(...)` por `$result = $this->callWithBackoff($company, self::API_BASE . $endpoint, $query, self::API_VERSION_HEADER, $sellerId); $page = $result['body'];` e incrementar `$this->metrics['pages_read']++` a cada iteracao do do-while.

    `$sellerId` resolvido no inicio de listCampaigns/listAds via `MlAdvertiser::where('company_id', $company->id)->value('seller_id')` (cache local) ou null se ainda nao discoveredem.

    `resetMetrics()` chamado no INICIO de cada metodo publico (discoverAdvertiser, listCampaigns, listAds, tryFetchAdsMetrics).

    `tryFetchAdsMetrics` mantem try/catch atual mas opera sobre o novo listAds.

    Adicionar imports no topo do service:
    - `use App\Models\MlAdvertiser;`
    - `use Illuminate\Support\Facades\Http;`
    - `use Illuminate\Support\Facades\RateLimiter;`

    PHPDoc do service atualizado para mencionar Phase 41 (backoff + cache + rate limiter + metricas), preservando referencia a Phase 38-01.

    Comentarios em pt-BR.

    Rodar `php artisan config:clear` nao necessario (codigo, nao config).
  </action>

  <verify>
    <automated>php artisan test --filter=MercadoLivreAdsServiceBackoffTest && php artisan test --filter=Phase38 && php artisan test --filter=Phase40 && php artisan test --filter=Sugador</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "callWithBackoff" app/Services/Sugadores/MercadoLivreAdsService.php` retorna >= 4 (declaracao + 3+ chamadas nos metodos publicos).
    - `grep -c "withRateLimit" app/Services/Sugadores/MercadoLivreAdsService.php` retorna >= 2.
    - `grep -c "getLastRunMetrics\|resetMetrics" app/Services/Sugadores/MercadoLivreAdsService.php` retorna >= 4.
    - `grep -c "MlAdvertiser" app/Services/Sugadores/MercadoLivreAdsService.php` retorna >= 2.
    - `grep -c "RateLimiter::for('ml-api'" app/Providers/AppServiceProvider.php` retorna 1.
    - `grep -c "perMinute(60)" app/Providers/AppServiceProvider.php` retorna 1.
    - `grep -c "by(\\\$sellerId)" app/Providers/AppServiceProvider.php` retorna 1.
    - `php artisan test --filter=MercadoLivreAdsServiceBackoffTest` retorna 12/12 PASS.
    - `php artisan test --filter=Phase38` retorna PASS (zero regressao — Phase 38-02 MlSmokeCommandTest 4/4).
    - `php artisan test --filter=Phase40` retorna 52/52 PASS (zero regressao — Plan 40-04 baseline).
    - `php artisan test --filter=Sugador` retorna >= 92/92 PASS (zero regressao).
    - Sanidade do contrato §2.3 preservada: `tryFetchAdsMetrics` ainda retorna `['ok','data','error']`.
    - Commit prefixo `feat(41-02):` mencionando "GREEN" + REQ-41-03..06.
  </acceptance_criteria>

  <done>
    MercadoLivreAdsService production-ready com backoff + cache + rate limiter + metricas. AppServiceProvider tem rate limiter `ml-api` registrado. Plans 41-04 (ShadowRunService mescla getLastRunMetrics no summary) e 41-05 (UI exibe metricas) destravados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → Mercado Livre API | Untrusted: 429/5xx/401/403 sao codigos esperados |
| Cache `ml_advertisers` → service | Trusted (escrita exclusivamente pelo proprio service) |
| Rate limiter bucket | Compartilhado entre workers via cache backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Denial-of-Service | Loop infinito de retry em 5xx persistente | mitigate | MAX_ATTEMPTS=5 + backoff cap 60s; lanca RuntimeException apos esgotar |
| T-41-02-02 | Denial-of-Service | Rate limit ML estourado por job mal-comportado | mitigate | RateLimiter ml-api bucket por seller_id; tooManyAttempts lanca antes da chamada |
| T-41-02-03 | Tampering | Cache `ml_advertisers` poderia ficar stale e bloquear discovery novo | mitigate | TTL 7 dias via comparacao `discovered_at < now()->subDays(7)`; updateOrCreate refaz quando expira |
| T-41-02-04 | Information disclosure | Token vazado em log do RuntimeException | mitigate | Mensagens de erro mencionam apenas company.id e endpoint, NUNCA `$token->access_token`. Validar via grep no codigo final |
| T-41-02-05 | Elevation of privilege | 401 persistente leva a uso indefinido do token expirado | mitigate | Apenas 1 tentativa de refresh; segundo 401 aborta com RuntimeException |

Severidade global: MEDIUM (path HTTP externo). Validacao crucial: anti-leak token gate.
</threat_model>

<verification>
- `php artisan test --filter=MercadoLivreAdsServiceBackoffTest` → 12/12 PASS
- `php artisan test --filter=Phase38` → PASS (zero regressao smoke command)
- `php artisan test --filter=Phase40` → 52/52 PASS (zero regressao)
- `php artisan test --filter=Sugador` → >= 92/92 PASS (zero regressao)
- `grep -c "access_token" app/Services/Sugadores/MercadoLivreAdsService.php` retorna 1 apenas (a chamada Http::withToken — sem log/throw/return)
- `grep -c "->access_token" app/Services/Sugadores/MercadoLivreAdsService.php` retorna 1 (a chamada `Http::withToken($token->access_token)`)
- `php artisan tinker --execute "echo get_class(\\\\Illuminate\\\\Support\\\\Facades\\\\RateLimiter::limiter('ml-api'));"` retorna `Closure` ou similar (limiter registrado)
</verification>

<success_criteria>
Plan 41-02 completo quando:
- callWithBackoff cobre 429 (Retry-After), 5xx (exponencial), 401 (refresh 1x), 403 (sem retry)
- discoverAdvertiser consulta cache ml_advertisers primeiro; updateOrCreate apos miss
- AppServiceProvider tem RateLimiter::for('ml-api', perMinute 60 by sellerId)
- withRateLimit aborta com RuntimeException quando tooManyAttempts
- getLastRunMetrics retorna 6 chaves
- 12+ tests Feature verdes + zero regressao Phase 38/40/Sugador
- 2 commits (RED + GREEN)
- REQ-41-03, REQ-41-04, REQ-41-05, REQ-41-06 cobertos
</success_criteria>

<output>
Criar `.planning/phases/41-onboarding-ml-por-empresa/41-02-SUMMARY.md` quando done.
</output>
