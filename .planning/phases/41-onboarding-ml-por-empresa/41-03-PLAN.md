---
phase: 41-onboarding-ml-por-empresa
plan: 03
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - app/Console/Commands/SugadoresShadowMl.php
  - tests/Feature/Phase41/SugadoresShadowMlCommandConfigTableTest.php
autonomous: true
requirements: [REQ-41-07]

must_haves:
  truths:
    - "Comando `sugadores:shadow-ml --company=all` consulta `SugadorMlCompanyConfig::where('shadow_enabled', true)->pluck('company_id')` PRIMEIRO"
    - "Se a tabela retorna >= 1 row, fallback env CSV nao eh consultado"
    - "Se a tabela esta vazia (sem registros shadow_enabled=true), fallback chama `config('sugadores.ml_shadow_companies')` (env CSV)"
    - "Se ambos vazios, mensagem pt-BR atualizada cita 'tabela sugador_ml_company_config OU env SUGADORES_ML_SHADOW_COMPANIES'"
    - "`--company={id}` numerico nao muda comportamento (continua exigindo existencia da empresa, exit 1 se nao encontrada)"
    - "Zero regressao na suite Phase 40-04 (SugadoresShadowMlCommandTest continua 8/8 PASS)"
  artifacts:
    - path: "app/Console/Commands/SugadoresShadowMl.php"
      provides: "resolveCompanies() refatorado para priorizar DB sobre env"
    - path: "tests/Feature/Phase41/SugadoresShadowMlCommandConfigTableTest.php"
      provides: "Suite 5 tests cobrindo: DB com 2 rows ignora env, DB vazio cai em env, ambos vazios aborta, --company={id} ignora ambos, mensagem de erro atualizada"
  key_links:
    - from: "SugadoresShadowMl::resolveCompanies('all')"
      to: "SugadorMlCompanyConfig::where(shadow_enabled=true)"
      via: "Eloquent query"
      pattern: "SugadorMlCompanyConfig::where\\(['\"]shadow_enabled"
    - from: "SugadoresShadowMl::resolveCompanies('all')"
      to: "config('sugadores.ml_shadow_companies')"
      via: "fallback Laravel config"
      pattern: "config\\(['\"]sugadores\\.ml_shadow_companies"
---

<objective>
Refatora `SugadoresShadowMl::resolveCompanies('all')` (Phase 40-04) para PRIORIZAR a tabela `sugador_ml_company_config` (Plan 41-01) como fonte de empresas elegiveis para shadow. Fallback para o env CSV `SUGADORES_ML_SHADOW_COMPANIES` (config('sugadores.ml_shadow_companies')) eh preservado para back-compat enquanto a tabela esta sendo populada via UI Plan 41-05.

Purpose: dar a UI admin (Plan 41-05) um lugar persistente onde escrever quais empresas entram em shadow (com timestamps, notes) sem precisar editar .env no VPS. Mantem o caminho env por seguranca operacional (rollback rapido caso a tabela tenha problema).

Output: 1 command editado + 1 suite Feature 5 tests verdes + zero regressao na suite 40-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md
@.planning/phases/41-onboarding-ml-por-empresa/41-01-PLAN.md
@.planning/phases/40-shadow-mode-tabelas-de-compara-o/40-04-SUMMARY.md
@app/Console/Commands/SugadoresShadowMl.php
@tests/Feature/Phase40/SugadoresShadowMlCommandTest.php
@config/sugadores.php

<interfaces>
<!-- Estado atual da resolveCompanies (Phase 40-04, linhas 124-137 do command) -->

```php
private function resolveCompanies(string $opt): ?array
{
    if ($opt === 'all') {
        $ids = (array) config('sugadores.ml_shadow_companies', []);
        return array_map('intval', $ids);
    }
    if (!ctype_digit($opt)) {
        $this->error("--company invalido: '{$opt}'. Use ID numerico ou 'all'.");
        return null;
    }
    return [(int) $opt];
}
```

Refactor para Plan 41-03:
```php
private function resolveCompanies(string $opt): ?array
{
    if ($opt === 'all') {
        // PRIORIDADE: tabela sugador_ml_company_config (Plan 41-01).
        // Empresas com shadow_enabled=true entram. Fonte canonica gerenciada pela
        // UI admin (Plan 41-05). Fallback env preservado para back-compat enquanto
        // a tabela esta sendo populada.
        $fromDb = SugadorMlCompanyConfig::where('shadow_enabled', true)
            ->orderBy('company_id')
            ->pluck('company_id')
            ->all();
        if (!empty($fromDb)) {
            return array_map('intval', $fromDb);
        }
        // Fallback env CSV
        $ids = (array) config('sugadores.ml_shadow_companies', []);
        return array_map('intval', $ids);
    }
    if (!ctype_digit($opt)) {
        $this->error("--company invalido: '{$opt}'. Use ID numerico ou 'all'.");
        return null;
    }
    return [(int) $opt];
}
```

Mensagem de erro em `handle()` (linha 57 do command atual) atualizada:
- Antes: `'Nenhuma empresa elegivel. Defina --company={id} ou configure SUGADORES_ML_SHADOW_COMPANIES no .env.'`
- Depois: `'Nenhuma empresa elegivel. Defina --company={id}, marque shadow_enabled=true em sugador_ml_company_config OU configure SUGADORES_ML_SHADOW_COMPANIES no .env.'`

Suite Phase 40-04 (`tests/Feature/Phase40/SugadoresShadowMlCommandTest.php`) tem 8 tests que NAO podem regredir. Em particular Test 3 (`--company=all respeita config('sugadores.ml_shadow_companies')`) continua valido porque a tabela esta vazia nesses tests (RefreshDatabase + sem seed de SugadorMlCompanyConfig).

Tests novos da Phase 41 devem POPULAR a tabela para validar a prioridade DB > env.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 (RED): Suite SugadoresShadowMlCommandConfigTableTest com 5 tests</name>
  <files>tests/Feature/Phase41/SugadoresShadowMlCommandConfigTableTest.php</files>

  <read_first>
    - .planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md (§2 tabela sugador_ml_company_config + decisao §1 da Implementation)
    - tests/Feature/Phase40/SugadoresShadowMlCommandTest.php (pattern Mockery + bind ShadowRunService)
    - app/Console/Commands/SugadoresShadowMl.php (estado atual)
    - .planning/phases/41-onboarding-ml-por-empresa/41-01-PLAN.md (Model SugadorMlCompanyConfig — fillable + relacao)
  </read_first>

  <behavior>
    Tests Feature `Tests\Feature\Phase41\SugadoresShadowMlCommandConfigTableTest` extends `Tests\TestCase`, `RefreshDatabase`. setUp: configura mock ShadowRunService no container via `$this->app->instance(...)` retornando payload padrao `runReturn()` ja existente no pattern Plan 40-04.

    Helpers privados (copiar pattern do Plan 40-04):
    - `makeCompany(?int $id = null, string $name = 'X'): Company`
    - `runReturn(int $a = 1, int $m = 2, string $as = 'completed', string $ms = 'completed'): array`
    - `enableShadowFor(Company $company): void` — `SugadorMlCompanyConfig::updateOrCreate(['company_id' => $company->id], ['shadow_enabled' => true, 'shadow_started_at' => now(), 'primary_enabled' => false])`.

    **Tests (5):**

    - Test 1 `db_com_shadow_enabled_prioriza_sobre_env`: cria 2 empresas (id distintos) + `enableShadowFor` em AMBAS. Configura `config(['sugadores.ml_shadow_companies' => [999999]])` (env aponta para empresa inexistente). Mock `ShadowRunService::run` Mockery::times(2) (1 chamada por empresa do DB; env IGNORADO). Executa `Artisan::call('sugadores:shadow-ml', ['--company' => 'all'])`. Asserts: exitCode == 0, mock recebeu 2 calls (validar via expectacao `->twice()`), empresa 999999 NAO foi tentada (sem erro "Empresa 999999 nao encontrada").

    - Test 2 `db_vazio_cai_em_env_csv`: NENHUM SugadorMlCompanyConfig com shadow_enabled. Cria 1 empresa, configura `config(['sugadores.ml_shadow_companies' => [$company->id]])`. Mock ShadowRunService->shouldReceive('run')->once(). Asserts: exitCode == 0, mock recebeu 1 call (fallback funcionou).

    - Test 3 `db_e_env_vazios_aborta_com_mensagem_atualizada`: nenhum SugadorMlCompanyConfig + `config(['sugadores.ml_shadow_companies' => []])`. Executa `Artisan::call('sugadores:shadow-ml', ['--company' => 'all'])`. Asserts: exitCode == 1, output contem substring 'sugador_ml_company_config' E substring 'SUGADORES_ML_SHADOW_COMPANIES' (assertStringContainsString em `Artisan::output()`).

    - Test 4 `company_id_numerico_ignora_db_e_env`: cria empresa A + B. enableShadowFor em A (logo DB tem [A]). config env aponta [B]. Mock->shouldReceive('run')->once() (apenas C). Executa `--company={C.id}` onde C eh uma 3a empresa diferente. Asserts: exitCode == 0, mock recebeu 1 call apenas, validar via Mockery argumento que foi com Company $c->id == C.id (nao A nem B).

    - Test 5 `db_com_shadow_disabled_nao_entra`: cria empresa com `SugadorMlCompanyConfig::create(['company_id' => $c->id, 'shadow_enabled' => false, 'primary_enabled' => false])`. config env vazio. Executa `--company=all`. Asserts: exitCode == 1 (caiu no fallback env vazio porque DB NAO tem shadow_enabled=true), mensagem contem 'Nenhuma empresa elegivel'.

    Cada test com `#[\PHPUnit\Framework\Attributes\Test]`. tearDown: Mockery::close.
  </behavior>

  <action>
    Criar `tests/Feature/Phase41/SugadoresShadowMlCommandConfigTableTest.php`. Reutilizar pattern de `tests/Feature/Phase40/SugadoresShadowMlCommandTest.php` (helpers, Mockery, bind container, Artisan::call).

    Imports: `App\Models\Company`, `App\Models\SugadorMlCompanyConfig`, `App\Services\Sugadores\ShadowRunService`, `Mockery`, `Mockery\MockInterface`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Illuminate\Support\Facades\Artisan`, `Carbon\Carbon`, `PHPUnit\Framework\Attributes\Test`, `Tests\TestCase`.

    Cobrir os 5 cenarios. Comentarios em pt-BR explicando cada um.

    Tests devem FALHAR todos (RED) — `resolveCompanies` atual nao consulta SugadorMlCompanyConfig, vai cair sempre no env.

    Test 1 vai FALHAR especificamente porque o comando atual ignora a tabela e usa env, entao Mock->twice() nao sera satisfeito (mock recebera 0 calls porque env aponta para 999999 que nao existe e aborta antes).
  </action>

  <verify>
    <automated>php artisan test --filter=SugadoresShadowMlCommandConfigTableTest</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `tests/Feature/Phase41/SugadoresShadowMlCommandConfigTableTest.php` existe.
    - `grep -c "#\[Test\]" tests/Feature/Phase41/SugadoresShadowMlCommandConfigTableTest.php` retorna >= 5.
    - `grep -c "SugadorMlCompanyConfig" tests/Feature/Phase41/SugadoresShadowMlCommandConfigTableTest.php` retorna >= 3 (helpers + asserts).
    - `php artisan test --filter=SugadoresShadowMlCommandConfigTableTest` retorna 5/5 FAIL/ERROR (estado RED).
    - Commit prefixo `test(41-03):` mencionando "RED".
  </acceptance_criteria>

  <done>
    Suite RED commitada. 5 tests falhando antes do refactor. Comando intacto.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2 (GREEN): Refactor SugadoresShadowMl::resolveCompanies prioriza DB sobre env + atualiza mensagem</name>
  <files>app/Console/Commands/SugadoresShadowMl.php</files>

  <read_first>
    - tests/Feature/Phase41/SugadoresShadowMlCommandConfigTableTest.php (escrito na Tarefa 1)
    - .planning/phases/41-onboarding-ml-por-empresa/41-CONTEXT.md (§2)
    - app/Console/Commands/SugadoresShadowMl.php (estado Phase 40-04 — refactor minimo)
    - .planning/phases/40-shadow-mode-tabelas-de-compara-o/40-04-SUMMARY.md (8 tests baseline que NAO podem regredir)
  </read_first>

  <action>
    Editar `app/Console/Commands/SugadoresShadowMl.php`:

    1. **Adicionar import** no topo:
       ```
       use App\Models\SugadorMlCompanyConfig;
       ```

    2. **Refatorar metodo `resolveCompanies(string $opt): ?array`** (linhas 124-137 atuais) para o codigo do `<interfaces>` do `<context>` desta plan (priorizar DB sobre env).

    3. **Atualizar mensagem de erro** em `handle()` (linha 57 atual) para incluir referencia a tabela:
       ```
       $this->error('Nenhuma empresa elegivel. Defina --company={id}, marque shadow_enabled=true em sugador_ml_company_config OU configure SUGADORES_ML_SHADOW_COMPANIES no .env.');
       ```

    4. **Atualizar PHPDoc** do bloco `--company=` no signature (descricao) e no docblock da classe explicando que `--company=all` agora le DB primeiro com fallback env.

    5. **Comentario inline** acima do `resolveCompanies` explicando a prioridade DB > env (Plan 41-03).

    ZERO mudanca em:
    - signature do comando (linhas 31-33)
    - construtor (linha 37)
    - logica do `handle()` apos a chamada `resolveCompanies` (loop empresa×dia, contadores, summary)
    - clamp de --days
    - validacao de existencia das empresas
    - exit codes

    Validar via `git diff app/Console/Commands/SugadoresShadowMl.php` — diff deve estar concentrado em:
    - 1 linha de import adicionada
    - ~8 linhas no resolveCompanies (logica nova) + manutencao do fallback
    - 1 linha na mensagem de erro
    - 2-3 linhas de comentario/PHPDoc

    Comentarios em pt-BR.
  </action>

  <verify>
    <automated>php artisan test --filter=SugadoresShadowMlCommandConfigTableTest && php artisan test --filter=SugadoresShadowMlCommandTest && php artisan test --filter=Phase40 && php artisan test --filter=Sugador</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "SugadorMlCompanyConfig::where" app/Console/Commands/SugadoresShadowMl.php` retorna 1.
    - `grep -c "config('sugadores.ml_shadow_companies'" app/Console/Commands/SugadoresShadowMl.php` retorna 1 (fallback preservado).
    - `grep -c "sugador_ml_company_config" app/Console/Commands/SugadoresShadowMl.php` retorna >= 1 (na mensagem de erro).
    - `php artisan test --filter=SugadoresShadowMlCommandConfigTableTest` retorna 5/5 PASS.
    - `php artisan test --filter=SugadoresShadowMlCommandTest` retorna 8/8 PASS (zero regressao Phase 40-04 — em particular o Test 3 que valida fallback env continua passando porque a tabela esta vazia nesses tests).
    - `php artisan test --filter=Phase40` retorna 52/52 PASS.
    - `php artisan test --filter=Sugador` retorna >= 92/92 PASS (somando ainda os 8 da Phase 41-01).
    - `php artisan list | grep sugadores:shadow-ml` retorna a linha (comando continua registrado).
    - Commit prefixo `feat(41-03):` mencionando "GREEN" + REQ-41-07.
  </acceptance_criteria>

  <done>
    Command shadow-ml prioriza DB sobre env. Mensagem de erro atualizada cita ambas as fontes. Suite 41-03 verde + zero regressao 40-04/Phase 40/Sugador. UI Plan 41-05 pode escrever em SugadorMlCompanyConfig e o scheduler diario passa a respeitar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → comando | Operador admin/cron — confiavel |
| DB SugadorMlCompanyConfig → comando | Escrita controlada pela UI Plan 41-05 (role:admin) |
| env CSV → comando | Editado pelo operador via .env do VPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Tampering | UI marca empresa errada em shadow_enabled | accept | Toggle eh acao manual de admin; activity_log da Phase 41-05 registra (planejado); rollback trivial via UI |
| T-41-03-02 | Denial-of-Service | DB query no resolveCompanies fica lenta com 1000+ rows | accept | Tabela tem indice em `shadow_enabled` (Plan 41-01); volumes reais ate Phase 42 sao <50 empresas |
| T-41-03-03 | Information disclosure | resolveCompanies retorna company_ids em log do comando | accept | Output do comando vai pra log do scheduler; company_id nao eh sensivel (consta em /companies admin) |

Severidade global: LOW (apenas leitura de tabela; sem mudanca de gravacao em sugadores).
</threat_model>

<verification>
- `php artisan test --filter=SugadoresShadowMlCommandConfigTableTest` → 5/5 PASS
- `php artisan test --filter=SugadoresShadowMlCommandTest` → 8/8 PASS (sem regressao)
- `php artisan test --filter=Phase40` → 52/52 PASS
- `php artisan test --filter=Sugador` → >= 92/92 PASS
- `git diff app/Console/Commands/SugadoresShadowMl.php` mostra diff concentrado e <30 linhas adicionadas
- `php artisan schedule:list | grep shadow-ml` continua mostrando entry agendada
</verification>

<success_criteria>
Plan 41-03 completo quando:
- resolveCompanies consulta DB primeiro, env como fallback
- Mensagem de erro atualizada cita ambas as fontes
- 5/5 tests Phase 41-03 verdes
- 8/8 tests Phase 40-04 continuam verdes (zero regressao)
- Suite Phase 40 + Sugador intactas
- 2 commits (RED + GREEN)
- REQ-41-07 coberto
</success_criteria>

<output>
Criar `.planning/phases/41-onboarding-ml-por-empresa/41-03-SUMMARY.md` quando done.
</output>
