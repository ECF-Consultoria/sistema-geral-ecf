---
phase: 39-provider-pattern-mercadolivresugadoresprovider-sem-gravar
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - app/Services/Sugadores/MercadoLivreSugadoresProvider.php
  - app/Services/Sugadores/SugadoresAdsProviderFactory.php
  - tests/Unit/Phase39/MercadoLivreSugadoresProviderTest.php
  - tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php
autonomous: true
requirements:
  - REQ-39-03
  - REQ-39-05
tags: [phase-39, sugadores, provider, ml, mercado-ads, tdd, speculative, ml-migration]
must_haves:
  truths:
    - "MercadoLivreSugadoresProvider implementa SugadoresAdsProvider consumindo MercadoLivreAdsService (Phase 38) — sem duplicar lógica HTTP nem OAuth"
    - "Factory ganha branch ml: forceName='ml' retorna MercadoLivreSugadoresProvider; capability fallback prefere Adman se ambos suportam (até Phase 42)"
    - "Tests Unit Http::fake do MercadoLivreSugadoresProvider verdes com payload ESPECULATIVO (marcado // CANDIDATO no código E nos tests)"
    - "Provider e tests carregam comentário `// CANDIDATO — revalidar após smoke real Phase 38 Tarefa 3` em campos/fixtures derivados de hipótese da doc ML"
    - "Suite Sugador existente continua verde — zero regressão"
  artifacts:
    - path: "app/Services/Sugadores/MercadoLivreSugadoresProvider.php"
      provides: "Implementação ML do contract via composição de MercadoLivreAdsService"
      contains: "implements SugadoresAdsProvider"
      min_lines: 120
    - path: "tests/Unit/Phase39/MercadoLivreSugadoresProviderTest.php"
      provides: "Tests Http::fake speculative cobrindo normalização ML→contrato §2.3"
      min_lines: 150
  key_links:
    - from: "app/Services/Sugadores/MercadoLivreSugadoresProvider.php"
      to: "app/Services/Sugadores/MercadoLivreAdsService.php"
      via: "constructor injection (Phase 38 Plan 01)"
      pattern: "private MercadoLivreAdsService"
    - from: "app/Services/Sugadores/SugadoresAdsProviderFactory.php"
      to: "app/Services/Sugadores/MercadoLivreSugadoresProvider.php"
      via: "constructor injection (branch ml)"
      pattern: "private MercadoLivreSugadoresProvider"
---

<objective>
Implementar `MercadoLivreSugadoresProvider` consumindo o `MercadoLivreAdsService` da Phase 38, normalizando o payload ML para o contrato §2.3 do plano canônico. Expandir o factory para resolver a branch `ml`. Tests cobertos com Http::fake usando payload **especulativo** baseado em §2.3 + doc oficial ML (fixture será substituída pela real quando Phase 38 Tarefa 3 destravar pós-recovery do MariaDB).

Purpose: dar à factory um provider ML funcional para que o Plan 39-04 (refactor SugadorAnalysisService) tenha as duas branches disponíveis. NÃO grava em sugadores — gravação é Phase 42.

Output: 1 arquivo de produção novo + 1 arquivo de tests novo + 2 arquivos editados (factory + factory test).

Wave justification: depende de 39-01 (precisa do contract + factory minimal). Independente de 39-03 (repository), por isso ambos podem rodar em Wave 2 paralelos sem conflito de arquivos.

**ATENÇÃO ESTADO SPECULATIVE:** o smoke real da Phase 38 (Tarefa 3) NÃO rodou (bloqueio MariaDB local). Implicação:
- O endpoint `/advertising/advertisers/{id}/product_ads/items` está marcado `// CANDIDATO` no `MercadoLivreAdsService` (linha 33) e o shape de retorno é hipótese.
- Este plan implementa o provider contra a melhor interpretação de §2.3 + doc oficial ML; o código DEVE conter `// CANDIDATO — revalidar após smoke real Phase 38 Tarefa 3` em campos cuja origem é hipótese.
- Tests usam fixtures Http::fake com payload especulativo (marcar `// CANDIDATO` na fixture e no docblock do test).
- Quando o smoke real rodar (deferred item conhecido), fixture será substituída pela real e ajustes feitos sem mudança de assinatura. Esse trabalho é DEFERRED, não bloqueante para este plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md
@.planning/phases/38-smoke-ml-piloto-bymobile/38-01-SUMMARY.md
@.planning/phases/38-smoke-ml-piloto-bymobile/38-02-SUMMARY.md
@plano-migracao-sugadores-ml-direto.md

<interfaces>
<!-- Contratos extraídos do código existente (Phase 38) que o provider ML consome. -->

From app/Services/Sugadores/MercadoLivreAdsService.php (Phase 38 Plan 01 — NÃO modificar):
```php
namespace App\Services\Sugadores;

class MercadoLivreAdsService
{
    public function __construct(private MercadoLivreService $ml) {}

    /** @return array{advertiser_id: ?int, site_id: ?string, seller_id: ?string, raw: array, url: string, status: int} */
    public function discoverAdvertiser(Company $company): array;

    /** @return array{count: int, results: array, raw_first_page: ?array, endpoints_tried: string[]} */
    public function listCampaigns(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array;

    /** @return array{count: int, results: array, raw_first_page: ?array, endpoints_tried: string[]} */
    public function listAds(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array;

    /** @return array{ok: bool, data: ?array, error: ?array{message: string, class: string}} */
    public function tryFetchAdsMetrics(Company $company, int $advertiserId, string $dateFrom, string $dateTo): array;
}
```

From app/Contracts/SugadoresAdsProvider.php (Plan 39-01 — implementar fiel):
```php
public function supports(Company $company): bool;          // mlToken ativo
public function name(): string;                            // 'ml'
public function fetchCampaigns(Company $c): array;
public function fetchCampaignsMetrics(Company $c, Carbon $from, Carbon $to): array;
public function fetchAdgroupsMetrics(Company $c, Carbon $from, Carbon $to): array;
public function fetchAdgroupMlbs(Company $c, Carbon $from, Carbon $to): array;
```

From app/Models/Company.php:
```php
public function getIsMlDrivenAttribute(): bool;   // optional($this->mlToken)->status === 'active'
public function mlToken();                        // hasOne MlToken
```

From plano-migracao-sugadores-ml-direto.md §2.3 (CONTRATO NORMALIZADO — fonte canônica de chaves):
adgroup_id, adgroup_name, campaign_id, thumbnail, adgroup_type, catalog_listing,
mlb_id, mlb_titulo, investment (=cost ML), revenue (=total_amount ML),
sold_quantity (=units_quantity ML), clicks, impressions (=prints ML),
cpc, ctr, acos, roas, organic_amount, organic_units, raw

From MercadoLivreAdsService::DEFAULT_METRICS (linha 40 — Phase 38 Plan 01):
"cost,clicks,prints,total_amount,direct_amount,indirect_amount,acos,roas,cpc,units_quantity,ctr"

Mapeamento ML → contrato (CANDIDATO — confirmar pós-smoke real):
- cost          → investment
- clicks        → clicks
- prints        → impressions
- total_amount  → revenue
- units_quantity → sold_quantity
- cpc / acos / roas / ctr → idem (fallback safe_div)
- direct_amount / indirect_amount → ainda não mapeados (deferred)
- organic_amount / organic_units → AUSENTES no payload Mercado Ads → retornar null (alinhado com nota §2.3)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Tests Http::fake speculative + factory test atualizado</name>
  <files>tests/Unit/Phase39/MercadoLivreSugadoresProviderTest.php, tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php</files>
  <read_first>
    - tests/Feature/Phase38/MercadoLivreAdsServiceTest.php (referência de Http::fake pattern + makeCompanyWithMlToken fixture)
    - app/Services/Sugadores/MercadoLivreAdsService.php (linha 27-33: endpoints; linha 40: DEFAULT_METRICS; linhas 70-86: discoverAdvertiser shape; linhas 101-140: listCampaigns shape; linhas 156-194: listAds shape)
    - .planning/phases/38-smoke-ml-piloto-bymobile/38-CONTEXT.md (§"Contrato normalizado")
    - plano-migracao-sugadores-ml-direto.md (§2.3 contrato + §2.2 endpoints)
    - tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php (criado Plan 39-01)
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md (§decisions "MercadoLivreSugadoresProvider" — ATENÇÃO estado pre-smoke)
  </read_first>
  <behavior>
    Tests cobrindo (todos com Http::preventStrayRequests + Http::fake com payload SPECULATIVE):
    1. test_supports_returns_true_when_company_has_active_ml_token — fixture Company + MlToken status=active
    2. test_supports_returns_false_when_company_has_no_ml_token
    3. test_supports_returns_false_when_ml_token_inactive — MlToken status=revoked
    4. test_name_returns_ml_string
    5. test_fetchAdgroupsMetrics_normalizes_ml_payload_to_contract_keys — Http::fake encadeando discoverAdvertiser + listAds com payload SPECULATIVE com chaves cost/clicks/prints/total_amount/units_quantity/cpc/acos/roas; assertar provider retorna lista com TODAS as 20 chaves do §2.3 (investment, revenue, sold_quantity, clicks, impressions, cpc, ctr, acos, roas, organic_amount=null, organic_units=null, mlb_id, mlb_titulo, raw...)
    6. test_fetchAdgroupsMetrics_returns_empty_when_advertiser_id_null — conta sem PADS (advertisers=[]) — fluxo válido
    7. test_fetchCampaigns_returns_normalized_list_via_listCampaigns — Http::fake do listCampaigns; assertar chaves campaign_id, campaign_name, campaign_status, raw
    8. test_fetchCampaignsMetrics_returns_normalized_list_with_period — Http::fake; chaves campaign_id, campaign_name, campaign_status, investment, revenue, sold_quantity, clicks, impressions, cpc, acos, roas, raw
    9. test_fetchAdgroupMlbs_extracts_mlb_ids_from_ads_payload — Http::fake retorna ads com item_id MLB; assertar map adgroup_id → [mlb_ids]
    10. test_safe_div_helper_returns_null_when_denominator_is_zero_or_null — testar fallback cpc/acos/roas quando ML não envia

    Tests do factory atualizar:
    11. test_for_with_force_name_ml_returns_ml_provider — assert instanceof MercadoLivreSugadoresProvider
    12. test_for_default_prefers_adman_when_both_supported — fixture Company com adman_account_id AND mlToken ativo; assert retorna AdmanProvider (regra "prefere Adman até Phase 42")
    13. test_for_falls_back_to_ml_when_only_ml_supports — fixture sem adman_account_id, com mlToken ativo; assert retorna MercadoLivreSugadoresProvider
  </behavior>
  <action>
Crie `tests/Unit/Phase39/MercadoLivreSugadoresProviderTest.php`:
- `namespace Tests\Unit\Phase39;`
- `use RefreshDatabase` (precisa de Company + MlToken persistidos para mlToken relation funcionar)
- `setUp`: `Http::preventStrayRequests()`
- Helper privado `makeCompanyWithMlToken(string $status = 'active'): Company` — copiar pattern de `tests/Feature/Phase38/MercadoLivreAdsServiceTest.php`
- 10 tests listados em <behavior>
- COMENTÁRIO OBRIGATÓRIO no topo do arquivo (pt-BR):
  ```
  // CANDIDATO — Phase 38 Tarefa 3 (smoke real ML) DEFERIDA por bloqueio MariaDB.
  // Fixtures abaixo são ESPECULATIVAS: baseadas em §2.3 do plano canônico
  // + MercadoLivreAdsService::DEFAULT_METRICS (cost, clicks, prints, total_amount,
  // units_quantity, cpc, acos, roas, ctr). Quando o smoke real rodar, substituir
  // por fixture gravada em storage/app/sugadores/ml-smoke/{id}-{date}.json.
  ```
- Cada Http::fake payload do test 5/7/8/9 deve ter comentário inline `// CANDIDATO — payload speculative; ajustar pós-smoke`
- Test 5 fixture (representativa, NÃO copiar literal — adaptar para o test): payload mínimo com chaves cost, clicks, prints, total_amount, units_quantity, cpc, acos, roas + item_id MLB12345 + title

Edite `tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php` (criado Plan 39-01):
- Atualizar constructor stub do factory para incluir MercadoLivreSugadoresProvider (pode usar Mockery para evitar depender do código novo nesta task RED)
- Adicionar tests 11/12/13 listados em <behavior>

Rode `php artisan test --filter='MercadoLivreSugadoresProviderTest|SugadoresAdsProviderFactoryTest'` — DEVE FALHAR (RED — classe MercadoLivreSugadoresProvider ainda não existe; factory ainda não tem branch ml).

Commit: `test(39-02): adiciona suite Http::fake speculative do MercadoLivreSugadoresProvider + factory ml branch (RED)`.
  </action>
  <verify>
    <automated>php artisan test --filter='MercadoLivreSugadoresProviderTest|SugadoresAdsProviderFactoryTest' 2>&amp;1 | grep -E "FAILED|Errors|Class.*not found"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/Unit/Phase39/MercadoLivreSugadoresProviderTest.php` exit 0
    - `grep -c "public function test_" tests/Unit/Phase39/MercadoLivreSugadoresProviderTest.php` retorna >=10
    - `grep -c "CANDIDATO" tests/Unit/Phase39/MercadoLivreSugadoresProviderTest.php` retorna >=2 (cabeçalho do arquivo + pelo menos 1 fixture)
    - `grep -c "preventStrayRequests" tests/Unit/Phase39/MercadoLivreSugadoresProviderTest.php` retorna >=1
    - Tests novos no factory test (count >= count anterior + 3): `grep -c "public function test_" tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php` retorna valor maior que o original do Plan 39-01
    - `php artisan test --filter='MercadoLivreSugadoresProviderTest|SugadoresAdsProviderFactoryTest'` exit != 0 (RED)
    - Commit `test(39-02): ... (RED)` em git log
  </acceptance_criteria>
  <done>10 tests do MlProvider + 3 tests novos do factory criados, todos vermelhos; comentário `// CANDIDATO — revalidar após smoke real` presente; commit RED feito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implementar MercadoLivreSugadoresProvider + factory branch ml</name>
  <files>app/Services/Sugadores/MercadoLivreSugadoresProvider.php, app/Services/Sugadores/SugadoresAdsProviderFactory.php</files>
  <read_first>
    - app/Services/Sugadores/AdmanSugadoresProvider.php (criado Plan 39-01 — usar como template de estrutura: constructor, safe_div helper, métodos do contract)
    - app/Services/Sugadores/MercadoLivreAdsService.php (linhas 47-49 constructor, 70-86 discoverAdvertiser shape, 101-140 listCampaigns shape, 156-194 listAds shape)
    - tests/Unit/Phase39/MercadoLivreSugadoresProviderTest.php (criado Task 1 — implementação deve fazê-los passar)
    - app/Services/Sugadores/SugadoresAdsProviderFactory.php (criado Plan 39-01 — adicionar branch ml)
    - plano-migracao-sugadores-ml-direto.md (§2.3 — chaves obrigatórias; §2.2 — descrição dos endpoints ML)
  </read_first>
  <action>
Crie `app/Services/Sugadores/MercadoLivreSugadoresProvider.php`:
- `namespace App\Services\Sugadores;`
- `use App\Contracts\SugadoresAdsProvider; use App\Models\Company; use Carbon\Carbon;`
- `class MercadoLivreSugadoresProvider implements SugadoresAdsProvider`
- Constructor: `public function __construct(private MercadoLivreAdsService $ads) {}`
- PHPDoc class em pt-BR cabeçalho com bloco `// CANDIDATO — revalidar após smoke real Phase 38 Tarefa 3 (smoke deferido por bloqueio MariaDB local). Normalização ML→contrato §2.3 baseada em hipótese da doc oficial + MercadoLivreAdsService::DEFAULT_METRICS. Quando o smoke real rodar, validar shape contra fixture real em storage/app/sugadores/ml-smoke/{id}-{date}.json.`
- Implementar:
  - `supports(Company $c): bool` → `return optional($c->mlToken)->status === 'active';` (espelha Company::is_ml_driven)
  - `name(): string` → `return 'ml';`
  - `fetchCampaigns(Company $c): array`:
    - Resolver `$advertiserId = $this->ads->discoverAdvertiser($c)['advertiser_id'];`
    - Se null → return `[]` (conta sem PADS, estado válido)
    - Janela default 30d: `$to = now()->toDateString(); $from = now()->subDays(30)->toDateString();` (fetchCampaigns não recebe range no contract — usa default 30d)
    - `$raw = $this->ads->listCampaigns($c, $advertiserId, $from, $to);`
    - Mapear cada `result` para `['campaign_id' => (string)($r['id'] ?? ''), 'campaign_name' => $r['name'] ?? null, 'campaign_status' => $r['status'] ?? null, 'raw' => $r]`
    - Comentário inline: `// CANDIDATO — campo 'id' pode vir como 'campaign_id' na payload real; ajustar pós-smoke`
  - `fetchCampaignsMetrics(Company $c, Carbon $from, Carbon $to): array`:
    - Mesma resolução advertiser
    - `$raw = $this->ads->listCampaigns($c, $advertiserId, $from->toDateString(), $to->toDateString());`
    - Mapear normalizando: `investment=$r['metrics']['cost']`, `revenue=$r['metrics']['total_amount']`, `sold_quantity=$r['metrics']['units_quantity']`, `clicks/impressions=prints/cpc/acos/roas` (com safe_div fallback)
    - Comentário CANDIDATO no mapeamento
  - `fetchAdgroupsMetrics(Company $c, Carbon $from, Carbon $to): array`:
    - Resolver advertiser_id
    - `$wrap = $this->ads->tryFetchAdsMetrics($c, $advertiserId, $from->toDateString(), $to->toDateString());`
    - if `!$wrap['ok']` → log warning + return `[]`
    - Para cada `result` em `$wrap['data']['results']`, normalizar para contrato §2.3 completo (20 chaves):
      ```
      [
        'adgroup_id'      => (string)($r['id'] ?? ''),
        'adgroup_name'    => $r['title'] ?? null,
        'campaign_id'    => (string)($r['campaign_id'] ?? ''),
        'thumbnail'      => $r['thumbnail'] ?? null,
        'adgroup_type'   => $r['type'] ?? null,
        'catalog_listing' => (bool)($r['catalog_listing'] ?? false),
        'mlb_id'         => $r['item_id'] ?? null,
        'mlb_titulo'     => $r['title'] ?? null,
        'investment'     => (float)($r['metrics']['cost'] ?? 0),
        'revenue'        => (float)($r['metrics']['total_amount'] ?? 0),
        'sold_quantity'  => (int)($r['metrics']['units_quantity'] ?? 0),
        'clicks'         => (int)($r['metrics']['clicks'] ?? 0),
        'impressions'    => (int)($r['metrics']['prints'] ?? 0),
        'cpc'            => $r['metrics']['cpc'] ?? $this->safe_div($cost, $clicks),
        'ctr'            => $r['metrics']['ctr'] ?? $this->safe_div($clicks, $prints),
        'acos'           => $r['metrics']['acos'] ?? $this->safe_div($cost * 100, $revenue),
        'roas'           => $r['metrics']['roas'] ?? $this->safe_div($revenue, $cost),
        'organic_amount' => null,  // Mercado Ads não retorna organic — deferred
        'organic_units'  => null,  // Mercado Ads não retorna organic — deferred
        'raw'            => $r,
      ]
      ```
    - Comentário `// CANDIDATO — chaves item_id/title/type podem variar; revalidar pós-smoke`
  - `fetchAdgroupMlbs(Company $c, Carbon $from, Carbon $to): array`:
    - Reusar `fetchAdgroupsMetrics` ou chamar `tryFetchAdsMetrics` direto
    - Agrupar por adgroup_id → lista de mlb_id (item_id)
    - return `array<string, array<int, string>>`
- Método privado `safe_div(float|int|null $num, float|int|null $den): ?float` — idêntico ao do AdmanSugadoresProvider (extrair para trait `SafeDiv` opcional se for reduzir duplicação; OK manter duplicado para reduzir surface)

Edite `app/Services/Sugadores/SugadoresAdsProviderFactory.php`:
- Adicionar segundo constructor param: `private MercadoLivreSugadoresProvider $mlProvider`
- Substituir o stub do throw no Plan 39-01 por: `if ($forceName === 'ml') return $this->mlProvider;`
- No fallback default, regra: prefere Adman se ambos supportam (mantém compat até Phase 42), cai para ML se Adman não suporta:
  ```php
  if ($this->admanProvider->supports($company)) return $this->admanProvider;
  if ($this->mlProvider->supports($company))    return $this->mlProvider;
  throw new \RuntimeException("Empresa {$company->id} sem provider compatível");
  ```

Rode `php artisan test --filter='MercadoLivreSugadoresProviderTest|SugadoresAdsProviderFactoryTest|AdmanSugadoresProviderTest'` — DEVE PASSAR (GREEN).

Rode regressão: `php artisan test --filter=Sugador` — deve passar com mesmo count.

Rode `grep -c "CANDIDATO" app/Services/Sugadores/MercadoLivreSugadoresProvider.php` — deve retornar >= 3 (cabeçalho + 2+ inline em métodos).

Commit: `feat(39-02): implementa MercadoLivreSugadoresProvider + factory branch ml (GREEN)`.
  </action>
  <verify>
    <automated>php artisan test --filter='MercadoLivreSugadoresProviderTest|SugadoresAdsProviderFactoryTest|AdmanSugadoresProviderTest' && php artisan test --filter=Sugador</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/Services/Sugadores/MercadoLivreSugadoresProvider.php` exit 0
    - `grep -c "implements SugadoresAdsProvider" app/Services/Sugadores/MercadoLivreSugadoresProvider.php` retorna 1
    - `grep -c "private MercadoLivreAdsService" app/Services/Sugadores/MercadoLivreSugadoresProvider.php` retorna >=1
    - `grep -c "CANDIDATO" app/Services/Sugadores/MercadoLivreSugadoresProvider.php` retorna >=3
    - `grep -c "private MercadoLivreSugadoresProvider" app/Services/Sugadores/SugadoresAdsProviderFactory.php` retorna >=1
    - `git diff --name-only HEAD app/Services/Sugadores/MercadoLivreAdsService.php` retorna vazio (Phase 38 service intocado)
    - `git diff --name-only HEAD app/Services/MercadoLivreService.php` retorna vazio (Phase 20 service intocado)
    - `git diff --name-only HEAD app/Services/AdmanService.php` retorna vazio (Adman intocado)
    - `php artisan test --filter='MercadoLivreSugadoresProviderTest|SugadoresAdsProviderFactoryTest|AdmanSugadoresProviderTest'` exit 0 (todos verdes)
    - `php artisan test --filter=Sugador` exit 0 (zero regressão)
    - Commit `feat(39-02): ... (GREEN)` em git log
  </acceptance_criteria>
  <done>MercadoLivreSugadoresProvider implementa o contract via composição de MercadoLivreAdsService; factory ganhou branch ml com regra "prefere Adman até Phase 42"; comentários CANDIDATO presentes em código e tests; todos os tests Unit Phase 39 verdes; zero regressão; commit GREEN feito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MercadoLivreAdsService → MercadoLivreSugadoresProvider | Camada interna PHP — provider só consome método público que já trata auth/refresh |
| Provider → ML API (via service) | OAuth bearer token delegado integralmente ao MercadoLivreService Phase 20; provider NUNCA toca access_token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | Information Disclosure | Logs do provider | mitigate | Provider só loga `$company->id` e mensagem do erro — nunca payload bruto nem access_token. Grep `access_token\|refresh_token` deve retornar 0 ocorrências em `app/Services/Sugadores/MercadoLivreSugadoresProvider.php`. |
| T-39-02-02 | Tampering | Composição MercadoLivreAdsService Phase 38 | accept | Provider só LÊ via métodos públicos do service Phase 38; não modifica estado interno nem cache OAuth. |
| T-39-02-03 | Speculative shape risk | Mapeamento item_id/title/cost/etc. | accept | Documentado: comentários CANDIDATO em código + tests; smoke real Phase 38 Tarefa 3 (deferred) substituirá fixture e ajustará mapeamento se necessário. Não é bug; é hipótese honesta. |
| T-39-02-04 | DoS via paginação | listAds, listCampaigns | mitigate | Tetos PAGE_LIMIT=50, ADS_MAX_OFFSET=2000, CAMPAIGNS_MAX_OFFSET=500 já vêm do MercadoLivreAdsService Phase 38. Provider não introduz loop novo. |
</threat_model>

<verification>
- Tests Unit Phase 39-02 verdes: `php artisan test --filter='MercadoLivreSugadoresProviderTest|SugadoresAdsProviderFactoryTest|AdmanSugadoresProviderTest'`
- Regressão Sugador: `php artisan test --filter=Sugador` mesma contagem
- Regressão Phase 38: `php artisan test --filter='MercadoLivreAdsService|MlSmokeCommand'` continua verde
- Anti-leak: `grep -ic "access_token\|refresh_token" app/Services/Sugadores/MercadoLivreSugadoresProvider.php` == 0
- CANDIDATO presente: `grep -c "CANDIDATO" app/Services/Sugadores/MercadoLivreSugadoresProvider.php` >= 3
- Não modificou: AdmanService, SugadorAnalysisService, MercadoLivreService, MercadoLivreAdsService
</verification>

<success_criteria>
- `MercadoLivreSugadoresProvider implements SugadoresAdsProvider` via composição de `MercadoLivreAdsService`
- Factory tem branches `adman` E `ml` funcionando com regra "prefere Adman até Phase 42"
- Provider normaliza ML→contrato §2.3 com TODAS as 20 chaves (organic_amount/organic_units = null por hora)
- Comentários `// CANDIDATO — revalidar após smoke real Phase 38 Tarefa 3` em código e fixtures
- 10+ tests Unit Phase 39-02 verdes
- Zero regressão Phase 38 ou Sugador atual
- Zero leak de access_token no provider
- 2 commits: RED + GREEN com prefixo `(39-02)`
</success_criteria>

<output>
Criar `.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-02-SUMMARY.md` ao finalizar.
</output>
