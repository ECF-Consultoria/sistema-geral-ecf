---
phase: 39-provider-pattern-mercadolivresugadoresprovider-sem-gravar
plan: 05
type: execute
wave: 4
depends_on:
  - 39-04
files_modified:
  - app/Console/Commands/AnalyzeSugadores.php
  - tests/Feature/Phase39/AnalyzeSugadoresCommandTest.php
autonomous: true
requirements:
  - REQ-39-07
  - REQ-39-08
tags: [phase-39, sugadores, command, dry-run, provider, tdd, ml-migration]
must_haves:
  truths:
    - "Comando sugadores:analyze aceita --provider={adman|ml} além das flags atuais (--company, --date, --dry-run)"
    - "--dry-run NÃO grava em sugadores: count(Sugador::all()) antes == depois"
    - "--provider=ml sem --dry-run aborta com exit 1 e mensagem 'Modo ml_primary só disponível em Phase 42 — use --dry-run para testar leitura sem gravação'"
    - "--provider=adman (default) sem --dry-run preserva comportamento atual de produção bit-a-bit"
    - "Tests Feature do command verdes com Mockery do factory + SQLite em-memory; zero requerimento de MariaDB local nem token ML real"
  artifacts:
    - path: "app/Console/Commands/AnalyzeSugadores.php"
      provides: "Comando Artisan estendido com flag --provider e guard ml_primary"
      contains: "--provider"
    - path: "tests/Feature/Phase39/AnalyzeSugadoresCommandTest.php"
      provides: "Tests Feature cobrindo dry-run, provider adman, provider ml com guard, provider ml com dry-run"
      min_lines: 100
  key_links:
    - from: "app/Console/Commands/AnalyzeSugadores.php"
      to: "app/Services/SugadorAnalysisService.php"
      via: "DI ($service->analyzeCompany($company, $referenceDate, $dryRun, $forceProvider))"
      pattern: "analyzeCompany"
---

<objective>
Estender o comando `php artisan sugadores:analyze` (já existente em `app/Console/Commands/AnalyzeSugadores.php`) com a flag `--provider={adman|ml}` e um guard de segurança que aborta quando `--provider=ml` é usado sem `--dry-run` (proteção contra gravação acidental no path ML antes da Phase 42). Comportamento default (sem `--provider`) preserva 100% o pipeline atual de produção.

Purpose: dar ao operador uma maneira segura de exercitar o path ML via factory (depende do refactor do Plan 39-04) sem risco de poluir a tabela `sugadores` com dados ML não-validados. O dry-run retorna os mesmos detalhes/motivos que o path antigo retornaria — operador compara visualmente para validar paridade antes do shadow mode (Phase 40).

Output: 1 arquivo de produção editado + 1 arquivo de tests Feature novo.

Wave justification: depende de 39-04 (refactor SugadorAnalysisService — sem ele o command não consegue passar `$forceProvider`). Última wave da phase.

**LIMITAÇÃO conhecida:** este plan NÃO consegue rodar smoke real `php artisan sugadores:analyze --provider=ml --dry-run --company=<bymobille>` contra a API ML porque (a) MariaDB local está corrompido (deferred do Phase 38-02) e (b) o smoke real da Phase 38 Tarefa 3 ainda não validou shape do payload ML. Tests automatizados deste plan rodam 100% verdes com Http::fake + SQLite em-memory. Smoke real fica como DEFERRED item — testado em ambiente futuro quando MariaDB voltar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md
@plano-migracao-sugadores-ml-direto.md

<interfaces>
<!-- Estado atual do command a ESTENDER (não recriar). -->

From app/Console/Commands/AnalyzeSugadores.php (linhas 12-15 — signature atual):
```php
protected $signature = 'sugadores:analyze
    {--company= : Analisa apenas uma empresa específica (ID)}
    {--date=    : Força reference_date (YYYY-MM-DD, padrão: hoje)}
    {--dry-run  : Mostra quem seria flagado sem gravar no banco}';

public function handle(SugadorAnalysisService $service): int
```

Linha 38: `$r = $service->analyzeCompany($company, $referenceDate, $dryRun);` — DEPOIS DO PLAN 39-04 vai aceitar `$forceProvider` como 4º parâmetro.
Linha 57: `$totals = $service->analyzeAll($referenceDate, $dryRun);` — não recebe $forceProvider (analyzeAll itera empresas; cada uma pode ter provider diferente — esta phase NÃO adiciona força no path global; só no path --company)

From app/Services/SugadorAnalysisService.php (após Plan 39-04):
```php
public function analyzeCompany(Company $company, ?Carbon $referenceDate = null, bool $dryRun = false, ?string $forceProvider = null): array;
public function analyzeAll(?Carbon $referenceDate = null, bool $dryRun = false): array;  // sem forceProvider
```

From app/Models/Sugador.php:
```php
namespace App\Models;
class Sugador extends Model {
    // counts: Sugador::count() — para assertar zero gravação em --dry-run
}
```

Behavior matrix esperado pelo command estendido:

| --provider | --dry-run | Resultado esperado |
|------------|-----------|--------------------|
| (omitido)  | (omitido) | Path Adman default, GRAVA em sugadores (comportamento atual prod) |
| adman      | (omitido) | Path Adman explícito, GRAVA em sugadores (idem default) |
| (omitido)  | sim       | Path Adman default, NÃO grava (comportamento dry-run atual) |
| adman      | sim       | Path Adman explícito, NÃO grava |
| ml         | sim       | Path ML, NÃO grava — retorna detalhes para inspeção manual |
| ml         | (omitido) | ABORTA com exit 1 e mensagem clara (proteção pré-Phase 42) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Tests Feature do command estendido</name>
  <files>tests/Feature/Phase39/AnalyzeSugadoresCommandTest.php</files>
  <read_first>
    - app/Console/Commands/AnalyzeSugadores.php (estado atual completo — 94 linhas)
    - app/Services/SugadorAnalysisService.php (estado pós Plan 39-04)
    - tests/Feature/Phase39/SugadorAnalysisServiceRefactorTest.php (Plan 39-04 — padrão de mock do factory)
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md (§"Comando sugadores:analyze" — matriz de behavior)
  </read_first>
  <behavior>
    Tests Feature (todos com RefreshDatabase + SQLite em-memory + Mockery do SugadorAnalysisService quando útil):
    1. test_command_with_provider_ml_without_dryrun_aborts_with_exit_1 — `$this->artisan('sugadores:analyze', ['--company' => $company->id, '--provider' => 'ml'])->expectsOutput('/Modo ml_primary só disponível em Phase 42/')->assertExitCode(1);`
    2. test_command_with_provider_ml_and_dryrun_passes_force_provider_to_service — Mockery do SugadorAnalysisService; assert analyzeCompany foi chamado com $forceProvider == 'ml'
    3. test_command_with_provider_adman_and_dryrun_passes_force_provider_to_service — assert chamado com 'adman'
    4. test_command_without_provider_passes_null_force_provider — assert chamado com null (default)
    5. test_command_dryrun_does_not_write_to_sugadores_table — fixture Company + SugadorConfig; rodar comando real (sem mock — exercita fluxo completo) com mock do AdmanService retornando 1 ad que bate critérios; assert Sugador::count() == 0 após command
    6. test_command_without_dryrun_writes_to_sugadores_table — mesmo fixture, sem --dry-run; assert Sugador::count() == 1 após command
    7. test_command_with_invalid_provider_value_rejects_with_clear_message — `--provider=invalid` → mensagem "Provider inválido: 'invalid'. Valores aceitos: adman, ml" + exit 1
    8. test_command_signature_includes_provider_option — `$this->artisan('list')->expectsOutputToContain('--provider')` OU reflection na propriedade $signature
  </behavior>
  <action>
Crie `tests/Feature/Phase39/AnalyzeSugadoresCommandTest.php`:
- `namespace Tests\Feature\Phase39;`
- `use RefreshDatabase;`
- Helper `makeCompanyWithSugadorConfig(): Company` — Company + SugadorConfig com gasto_minimo_sem_venda=100 etc.
- 8 tests listados em <behavior>
- Test 5/6 usa Mockery::mock(AdmanService::class) injetado via `$this->app->instance()`; retorna fixture com 1 ad com investment=200, sold_quantity=0 (bate critério gasto_sem_venda)
- Test 1: assertar mensagem com ml_primary; expectExitCode(1)

Rode `php artisan test --filter='AnalyzeSugadoresCommandTest'` — DEVE FALHAR (RED — command ainda não tem --provider).

Commit: `test(39-05): adiciona suite Feature command sugadores:analyze --provider (RED)`.
  </action>
  <verify>
    <automated>php artisan test --filter='AnalyzeSugadoresCommandTest' 2>&amp;1 | grep -E "FAILED|Errors"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase39/AnalyzeSugadoresCommandTest.php` exit 0
    - `grep -c "public function test_" tests/Feature/Phase39/AnalyzeSugadoresCommandTest.php` retorna >=8
    - `grep -c "ml_primary\|Phase 42\|--provider" tests/Feature/Phase39/AnalyzeSugadoresCommandTest.php` retorna >=3
    - `php artisan test --filter='AnalyzeSugadoresCommandTest'` exit != 0 (RED)
    - Commit `test(39-05): ... (RED)` em git log
  </acceptance_criteria>
  <done>8 tests Feature do command estendido criados e vermelhos esperadamente; commit RED feito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Estender AnalyzeSugadores command com --provider + guard ml_primary</name>
  <files>app/Console/Commands/AnalyzeSugadores.php</files>
  <read_first>
    - app/Console/Commands/AnalyzeSugadores.php (inteiro — 94 linhas, preservar lógica atual)
    - app/Services/SugadorAnalysisService.php (após Plan 39-04 — analyzeCompany aceita $forceProvider)
    - tests/Feature/Phase39/AnalyzeSugadoresCommandTest.php (Task 1 — implementação deve fazê-los passar)
  </read_first>
  <action>
Edite `app/Console/Commands/AnalyzeSugadores.php`:

**1. Signature (linhas 12-15):**
- ANTES:
  ```
  protected $signature = 'sugadores:analyze
      {--company= : Analisa apenas uma empresa específica (ID)}
      {--date=    : Força reference_date (YYYY-MM-DD, padrão: hoje)}
      {--dry-run  : Mostra quem seria flagado sem gravar no banco}';
  ```
- DEPOIS:
  ```
  protected $signature = 'sugadores:analyze
      {--company= : Analisa apenas uma empresa específica (ID)}
      {--date=    : Força reference_date (YYYY-MM-DD, padrão: hoje)}
      {--dry-run  : Mostra quem seria flagado sem gravar no banco}
      {--provider= : Força provider de dados (adman|ml). Default = capability detection. ml sem --dry-run aborta (Phase 39 — gravação ML disponível em Phase 42).}';
  ```

**2. Description (linha 17):**
- Atualizar comentário sobre flag `--provider` no docblock; manter texto atual de descrição base.

**3. handle() (linhas 19-74):**
- Extrair flag: `$provider = $this->option('provider');` (depois de `$dryRun = (bool) $this->option('dry-run');`)
- VALIDAR provider:
  ```php
  // Phase 39: --provider=ml sem --dry-run é proibido (gravação ML em Phase 42).
  if ($provider !== null && !in_array($provider, ['adman', 'ml'], true)) {
      $this->error("Provider inválido: '{$provider}'. Valores aceitos: adman, ml");
      return self::FAILURE;
  }
  if ($provider === 'ml' && !$dryRun) {
      $this->error('Modo ml_primary só disponível em Phase 42 — use --dry-run para testar leitura sem gravação.');
      return self::FAILURE;
  }
  ```
- Linha info adicional após `$this->info("Reference date: ...")`:
  ```php
  if ($provider !== null) {
      $this->info("Provider forçado: {$provider}");
  }
  ```
- Linha 38 (chamada analyzeCompany no path --company): ADICIONAR 4º param:
  - ANTES: `$r = $service->analyzeCompany($company, $referenceDate, $dryRun);`
  - DEPOIS: `$r = $service->analyzeCompany($company, $referenceDate, $dryRun, $provider);`
- Linha 57 (chamada analyzeAll no path global): NÃO modificar (analyzeAll não recebe $forceProvider; cada empresa cai no default via factory)
  - Adicionar warning se `--provider` foi passado SEM `--company`:
    ```php
    if ($provider !== null && !$companyId) {
        $this->warn('--provider só tem efeito com --company (path global usa capability detection por empresa).');
    }
    ```

**4. Preservar 100% inalterado:**
- Lógica de printDetalhes (linhas 76-93)
- Lógica de table summary (linhas 60-71)
- Comentários originais

Rode `php artisan test --filter='AnalyzeSugadoresCommandTest'` — GREEN (8 verdes).

Rode regressão: `php artisan test --filter=Sugador` — número de verdes igual ao baseline do Plan 39-04 (não capturar baseline novo; reusar do Plan 39-04).

Smoke manual (operador, OPCIONAL — pode rodar depois quando MariaDB voltar):
```
# Path adman dry-run (deve listar motivos sem gravar):
php artisan sugadores:analyze --company=<id_adman> --dry-run --provider=adman

# Path ml dry-run (deve listar motivos via path ML — REQUER MariaDB + bymobille):
php artisan sugadores:analyze --company=<id_bymobille> --dry-run --provider=ml

# Proteção:
php artisan sugadores:analyze --company=<id_bymobille> --provider=ml
# → exit 1 + mensagem ml_primary
```

Commit: `feat(39-05): estende sugadores:analyze com --provider + guard ml_primary (GREEN)`.
  </action>
  <verify>
    <automated>php artisan test --filter='AnalyzeSugadoresCommandTest' &amp;&amp; php artisan test --filter=Sugador</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\\-\\-provider" app/Console/Commands/AnalyzeSugadores.php` retorna >=2 (signature + uso em handle)
    - `grep -c "ml_primary só disponível em Phase 42" app/Console/Commands/AnalyzeSugadores.php` retorna >=1
    - `grep -c "Provider inválido" app/Console/Commands/AnalyzeSugadores.php` retorna >=1
    - `grep -c "in_array.\\\$provider, \\[.adman., .ml.\\]" app/Console/Commands/AnalyzeSugadores.php` retorna >=1
    - `grep -c "analyzeCompany.\\\$company, \\\$referenceDate, \\\$dryRun, \\\$provider" app/Console/Commands/AnalyzeSugadores.php` retorna >=1 (4 params passados)
    - `php artisan list 2>&1 | grep sugadores:analyze` retorna linha do comando
    - `php artisan sugadores:analyze --help 2>&1 | grep -c "\\-\\-provider"` retorna >=1
    - `php artisan test --filter='AnalyzeSugadoresCommandTest'` exit 0 (8 GREEN)
    - `php artisan test --filter=Sugador` exit 0 — count de verdes igual ao baseline do Plan 39-04 (zero regressão acumulada na phase)
    - Commit `feat(39-05): ... (GREEN)` em git log
  </acceptance_criteria>
  <done>Command sugadores:analyze ganha flag --provider; guard contra --provider=ml sem --dry-run funciona com exit 1; --dry-run continua não gravando; default (sem flag) preserva path Adman bit-a-bit; 8 tests Feature verdes; zero regressão Sugador; commit GREEN feito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (operador) → command | Input controlado por operador local (não HTTP); validação básica via in_array suficiente |
| command → SugadorAnalysisService::analyzeCompany | DI interna; $forceProvider já validado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05-01 | Gravação acidental ML path | --provider=ml sem --dry-run | mitigate | Guard explícito em handle() retorna FAILURE antes de chamar analyzeCompany. Acceptance: test 1 prova exit 1 + mensagem clara. |
| T-39-05-02 | Provider value injection | --provider=`<arbitrary string>` | mitigate | Whitelist `in_array($provider, ['adman', 'ml'], true)` — apenas 2 valores aceitos. Test 7 prova rejeição com mensagem clara. |
| T-39-05-03 | Regression do dry-run atual | --dry-run sem --provider | mitigate | Test 5 assertar Sugador::count() == 0 após command — gate automatizado. |
| T-39-05-04 | Regression do path adman default prod | sem flags (cron diário) | mitigate | Test 6 assertar Sugador::count() == 1 (gravou via path Adman default); baseline regressão Plan 39-04 cobre a suite Sugador inteira. |
</threat_model>

<verification>
- Tests Phase 39-05 verdes: `php artisan test --filter='AnalyzeSugadoresCommandTest'` (8/8)
- Regressão acumulada na phase: `php artisan test --filter=Sugador` — IGUAL baseline pré-Plan 39-04
- Command help mostra `--provider`: `php artisan sugadores:analyze --help 2>&1 | grep provider`
- Phase 38 service intocado: `git diff app/Services/Sugadores/MercadoLivreAdsService.php` vazio
- AdmanService intocado: `git diff app/Services/AdmanService.php` vazio
</verification>

<success_criteria>
- Comando sugadores:analyze aceita --provider={adman|ml}
- --provider=ml sem --dry-run aborta com exit 1 e mensagem mencionando Phase 42
- --provider inválido aborta com exit 1 e mensagem mostrando valores aceitos
- --dry-run continua não gravando em sugadores (Sugador::count() == 0)
- Comportamento default (sem flags) preserva path Adman + gravação (count incrementa)
- 8 tests Feature verdes
- Zero regressão acumulada na suite Sugador (gate do Plan 39-04 preservado)
- 2 commits: RED + GREEN com prefixo `(39-05)`
</success_criteria>

<output>
Criar `.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-05-SUMMARY.md` ao finalizar.
</output>
