---
phase: 39-provider-pattern-mercadolivresugadoresprovider-sem-gravar
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - app/Repositories/AdgroupMlbMapRepository.php
  - app/Services/AdmanAdgroupMlbsRepository.php
  - app/Jobs/SyncCompanyAdgroupMlbsJob.php
  - app/Http/Controllers/SugadorController.php
  - tests/Unit/Phase39/AdgroupMlbMapRepositoryTest.php
autonomous: true
requirements:
  - REQ-39-04
tags: [phase-39, sugadores, repository, refactor, ml-migration]
must_haves:
  truths:
    - "AdgroupMlbMapRepository existe em App\\Repositories namespace com os 3 métodos: getMlbsForAdgroup, setMlbsForAdgroup, bulkSetFromProvider"
    - "AdmanAdgroupMlbsRepository legado encaminha (delega) chamadas para o novo AdgroupMlbMapRepository, OU é convertido em alias/proxy; método público antigo continua existindo para preservar callsites"
    - "SugadorController::mlbs e SyncCompanyAdgroupMlbsJob continuam funcionando — assinatura externa preservada (zero regressão)"
    - "Tests Unit usam SQLite em-memory para validar repository CRUD + bulk; verdes sem MariaDB local"
    - "Suite Sugador existente continua verde — zero regressão funcional"
  artifacts:
    - path: "app/Repositories/AdgroupMlbMapRepository.php"
      provides: "Repository neutro para tabela adman_adgroup_mlbs (esconde nome legado) com API definida no §decisions do 39-CONTEXT"
      contains: "class AdgroupMlbMapRepository"
      min_lines: 100
    - path: "tests/Unit/Phase39/AdgroupMlbMapRepositoryTest.php"
      provides: "Tests Unit cobrindo getMlbsForAdgroup, setMlbsForAdgroup, bulkSetFromProvider via SQLite em-memory"
      min_lines: 80
  key_links:
    - from: "app/Repositories/AdgroupMlbMapRepository.php"
      to: "adman_adgroup_mlbs (tabela)"
      via: "DB::table ou AdmanAdgroupMlb model"
      pattern: "adman_adgroup_mlbs"
    - from: "app/Services/AdmanAdgroupMlbsRepository.php"
      to: "app/Repositories/AdgroupMlbMapRepository.php"
      via: "delegation (compat layer)"
      pattern: "AdgroupMlbMapRepository|app(AdgroupMlbMapRepository"
---

<objective>
Criar `App\Repositories\AdgroupMlbMapRepository` com a API definida no §decisions do CONTEXT (`getMlbsForAdgroup`, `setMlbsForAdgroup`, `bulkSetFromProvider`) que esconde o nome legado `adman_adgroup_mlbs`. Manter o `AdmanAdgroupMlbsRepository` existente (Phase 30 Plan 04) como **adapter/proxy** que delega para o novo, preservando 100% compat com call-sites atuais (`SugadorController::mlbs`, `SyncCompanyAdgroupMlbsJob`).

Purpose: dar à camada de provider (Plans 39-02 + 39-04) uma API neutra para persistir adgroup→MLB sem citar "adman" no nome — Phase 42 vai usar isso quando o path ML começar a popular a tabela; Phase 43 renomeia a tabela físicamente.

Output: 1 arquivo de produção novo (Repository) + 1 arquivo de tests novo + 1 arquivo editado (legacy Repository vira proxy) + 0 a 2 arquivos editados (callsites — opcional pois o proxy preserva API).

Wave justification: independente do Plan 39-02 (não compartilha arquivos). Pode rodar em paralelo na Wave 2 (mesmo wave que 39-02 desde que files_modified não overlap — e não overlapam: 39-02 mexe em `Sugadores/Mercado*` e `Tests/Phase39/Mercado*`; 39-03 mexe em `Repositories/AdgroupMlbMap*`, `AdmanAdgroupMlbs*`, `SugadorController::mlbs`, `SyncCompanyAdgroupMlbsJob`, `Tests/Phase39/AdgroupMlbMap*`).

NÃO renomear `adman_adgroup_mlbs` (a tabela). Rename físico é Phase 43 (§5 do plano canônico).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md
@plano-migracao-sugadores-ml-direto.md

<interfaces>
<!-- Estado atual do código a ser reorganizado. -->

From app/Services/AdmanAdgroupMlbsRepository.php (Phase 30 Plan 04 — preservar API pública):
```php
namespace App\Services;

class AdmanAdgroupMlbsRepository
{
    public function findByAdgroup(string $custId, string $adgroupName, string $periodFrom, string $periodTo): Collection;
    public function findByAdgroupId(string $custId, string $adgroupId, string $periodFrom, string $periodTo): Collection;
    public function findByCampaign(string $custId, string $campaignName): Collection;
    public function upsertBatch(array $rows): int;        // chave única: cust_id, adgroup_id, mlb_id, period_from, period_to
    public function lastSyncForCompany(string $custId): ?Carbon;
    public function mlbsCountForCompany(string $custId, string $periodFrom, string $periodTo): int;
}
```

From app/Models/AdmanAdgroupMlb.php:
```php
namespace App\Models;
class AdmanAdgroupMlb extends Model {
    protected $table = 'adman_adgroup_mlbs';
    // colunas: cust_id, adgroup_id, adgroup_name, campaign_id, campaign_name,
    //         mlb_id, title, status, period_from, period_to, metrics (json),
    //         last_synced_at, created_at, updated_at
}
```

From database/migrations/2026_06_09_000001_create_adman_adgroup_mlbs_table.php (NÃO modificar — rename físico é Phase 43):
- Tabela: adman_adgroup_mlbs
- Unique constraint: (cust_id, adgroup_id, mlb_id, period_from, period_to) → 'adgmlb_unique'
- Índices: idx_adgmlb_lookup (cust_id, adgroup_id), idx_adgmlb_by_name (cust_id, adgroup_name), idx_adgmlb_synced (last_synced_at)

Call-sites atuais do legacy Repository (grep "AdmanAdgroupMlbsRepository" app/):
- app/Jobs/SyncCompanyAdgroupMlbsJob.php:6, :94 — type-hint na assinatura handle()
- app/Http/Controllers/SugadorController.php:569 — type-hint inline no método mlbs()

API alvo do NOVO repository (§decisions do CONTEXT — chaves diferentes do legacy):
```php
namespace App\Repositories;

class AdgroupMlbMapRepository
{
    /** @return array<int, string> MLB IDs para o adgroup; vazio se sem cache */
    public function getMlbsForAdgroup(int $companyId, string $adgroupId): array;

    /** @param array<int, string> $mlbIds */
    public function setMlbsForAdgroup(int $companyId, string $adgroupId, array $mlbIds, ?Carbon $lastSeenAt = null): void;

    /** Bulk upsert pra economizar queries quando o provider vier com muitos adgroups */
    public function bulkSetFromProvider(int $companyId, array $adgroupMlbsMap): int;
}
```

Mapeamento companyId (int) → cust_id (string Adman): NOVA API recebe Company ID; resolve internamente para cust_id (via Company::find OR receber custId direto? — decidir: receber companyId int E resolver cust_id internamente é mais limpo; alinhado com §decisions).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Tests Unit do AdgroupMlbMapRepository</name>
  <files>tests/Unit/Phase39/AdgroupMlbMapRepositoryTest.php</files>
  <read_first>
    - app/Services/AdmanAdgroupMlbsRepository.php (entender API atual e schema da tabela)
    - app/Models/AdmanAdgroupMlb.php
    - database/migrations/2026_06_09_000001_create_adman_adgroup_mlbs_table.php (unique constraint adgmlb_unique)
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md (§"AdgroupMlbMapRepository — abstrai adman_adgroup_mlbs" — 3 métodos exatos)
    - phpunit.xml (config testing DB — SQLite em-memory)
  </read_first>
  <behavior>
    Tests cobrindo (todos com RefreshDatabase + SQLite em-memory):
    1. test_getMlbsForAdgroup_returns_empty_array_when_no_data_cached — fresh DB, assert []
    2. test_getMlbsForAdgroup_returns_mlb_ids_from_cache — inserir 3 rows via DB::table, assert array com 3 MLB IDs ordenados
    3. test_getMlbsForAdgroup_resolves_company_id_to_cust_id_internally — fixture Company com adman_account_id='ABC123'; inserir rows com cust_id='ABC123'; assert getMlbsForAdgroup(company->id, ...) retorna MLBs
    4. test_getMlbsForAdgroup_returns_empty_when_company_has_no_cust_id — fixture Company sem adman_account_id sem ml_store_id; assert []
    5. test_setMlbsForAdgroup_inserts_rows_with_unique_constraint — chamar setMlbsForAdgroup, query DB direto, assert 3 rows inseridas com cust_id resolvido + period_from/to default
    6. test_setMlbsForAdgroup_updates_existing_rows_on_conflict — chamar 2x com mesmos params, assert ainda só 3 rows (idempotente via unique constraint)
    7. test_bulkSetFromProvider_inserts_multiple_adgroups_in_one_call — passar map ['adg1' => ['MLB1','MLB2'], 'adg2' => ['MLB3']]; assert 3 rows total, return value == 3
    8. test_bulkSetFromProvider_returns_zero_when_map_empty — assert 0 sem query
  </behavior>
  <action>
Crie `tests/Unit/Phase39/AdgroupMlbMapRepositoryTest.php`:
- `namespace Tests\Unit\Phase39;`
- `use Illuminate\Foundation\Testing\RefreshDatabase;` — Repository testa contra DB real (SQLite em-memory já é default em phpunit.xml)
- Helper `makeCompanyWithCustId(string $custId = 'CUST123'): Company` — `Company::create(['name'=>'Test','cnpj'=>'00000000000000','adman_account_id'=>$custId,'active'=>true])`
- 8 tests listados em <behavior>
- Verificações DB usar `DB::table('adman_adgroup_mlbs')->where(...)->count()` ou `->get()` para inspeção pós-call

Rode `php artisan test --filter='AdgroupMlbMapRepositoryTest'` — DEVE FALHAR (RED — Repository ainda não criado).

Commit: `test(39-03): adiciona suite Unit do AdgroupMlbMapRepository (RED)`.
  </action>
  <verify>
    <automated>php artisan test --filter='AdgroupMlbMapRepositoryTest' 2>&amp;1 | grep -E "FAILED|Errors|Class.*not found"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/Unit/Phase39/AdgroupMlbMapRepositoryTest.php` exit 0
    - `grep -c "public function test_" tests/Unit/Phase39/AdgroupMlbMapRepositoryTest.php` retorna >=8
    - `grep -c "use Illuminate\\\\Foundation\\\\Testing\\\\RefreshDatabase" tests/Unit/Phase39/AdgroupMlbMapRepositoryTest.php` retorna >=1
    - `php artisan test --filter='AdgroupMlbMapRepositoryTest'` exit != 0 (RED)
    - Commit `test(39-03): ... (RED)` em git log
  </acceptance_criteria>
  <done>8 tests Unit cobrindo os 3 métodos do novo repository criados e falhando esperadamente; commit RED feito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Criar AdgroupMlbMapRepository + delegar legacy AdmanAdgroupMlbsRepository</name>
  <files>app/Repositories/AdgroupMlbMapRepository.php, app/Services/AdmanAdgroupMlbsRepository.php</files>
  <read_first>
    - app/Services/AdmanAdgroupMlbsRepository.php (TODAS as 6 funções públicas — preservar API ao virar proxy)
    - app/Models/AdmanAdgroupMlb.php (schema + casts + table name)
    - app/Models/Company.php (atributo cust_id — getCustIdAttribute → adman_account_id ?: ml_store_id)
    - tests/Unit/Phase39/AdgroupMlbMapRepositoryTest.php (criado Task 1 — implementação deve fazê-los passar)
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md (§"AdgroupMlbMapRepository — abstrai adman_adgroup_mlbs")
  </read_first>
  <action>
Crie `app/Repositories/AdgroupMlbMapRepository.php`:
- `namespace App\Repositories;`
- `use App\Models\AdmanAdgroupMlb; use App\Models\Company; use Carbon\Carbon; use Illuminate\Support\Facades\DB;`
- `class AdgroupMlbMapRepository`
- PHPDoc class pt-BR: explica que abstrai a tabela `adman_adgroup_mlbs` (decisão §5 do plano: manter tabela, renomear em Phase 43), exposta com nome neutro para que o provider pattern (Phase 39+) e o cut-over (Phase 42) não falem em "adman" diretamente. Phase 43 vai renomear `adman_adgroup_mlbs` → `sugador_adgroup_mlbs`.
- Métodos:
  ```
  public function getMlbsForAdgroup(int $companyId, string $adgroupId): array
  {
      $custId = $this->resolveCustId($companyId);
      if ($custId === null) return [];
      return AdmanAdgroupMlb::query()
          ->where('cust_id', $custId)
          ->where('adgroup_id', $adgroupId)
          ->orderBy('mlb_id')
          ->pluck('mlb_id')
          ->all();
  }

  public function setMlbsForAdgroup(int $companyId, string $adgroupId, array $mlbIds, ?Carbon $lastSeenAt = null): void
  {
      $custId = $this->resolveCustId($companyId);
      if ($custId === null) return;
      $when = ($lastSeenAt ?? now());
      $today = $when->copy()->startOfDay()->toDateString();
      $rows = [];
      foreach ($mlbIds as $mlbId) {
          $rows[] = [
              'cust_id'        => $custId,
              'adgroup_id'     => $adgroupId,
              'mlb_id'         => $mlbId,
              'period_from'    => $today,
              'period_to'      => $today,
              'last_synced_at' => $when,
              'created_at'     => $when,
              'updated_at'     => $when,
          ];
      }
      if (empty($rows)) return;
      AdmanAdgroupMlb::upsert(
          $rows,
          ['cust_id', 'adgroup_id', 'mlb_id', 'period_from', 'period_to'],
          ['last_synced_at', 'updated_at']
      );
  }

  public function bulkSetFromProvider(int $companyId, array $adgroupMlbsMap): int
  {
      $custId = $this->resolveCustId($companyId);
      if ($custId === null || empty($adgroupMlbsMap)) return 0;
      $when = now();
      $today = $when->copy()->startOfDay()->toDateString();
      $rows = [];
      foreach ($adgroupMlbsMap as $adgroupId => $mlbIds) {
          foreach ($mlbIds as $mlbId) {
              $rows[] = [
                  'cust_id'        => $custId,
                  'adgroup_id'     => (string) $adgroupId,
                  'mlb_id'         => (string) $mlbId,
                  'period_from'    => $today,
                  'period_to'      => $today,
                  'last_synced_at' => $when,
                  'created_at'     => $when,
                  'updated_at'     => $when,
              ];
          }
      }
      if (empty($rows)) return 0;
      $totalAffected = 0;
      foreach (array_chunk($rows, 500) as $chunk) {
          $totalAffected += AdmanAdgroupMlb::upsert(
              $chunk,
              ['cust_id', 'adgroup_id', 'mlb_id', 'period_from', 'period_to'],
              ['last_synced_at', 'updated_at']
          );
      }
      return count($rows);  // retorna número total de pares (Adman MySQL retorna 0 em update; cliente quer ver total processado)
  }

  private function resolveCustId(int $companyId): ?string
  {
      $company = Company::find($companyId);
      return $company?->cust_id;  // accessor: adman_account_id ?: ml_store_id
  }
  ```
- Comentário no `setMlbsForAdgroup`: `// CANDIDATO Phase 39 — period_from/to default = hoje. Phase 40 (shadow mode) pode passar range explícito do snapshot.`

Edite `app/Services/AdmanAdgroupMlbsRepository.php` (manter API pública 100% inalterada para call-sites existentes):
- Adicionar constructor opcional `public function __construct(private ?\App\Repositories\AdgroupMlbMapRepository $delegate = null) {}` — apenas para futuro (Plan 39-04 pode usar). Atualmente todos os métodos do legacy continuam funcionando exatamente como hoje (a delegação real só vai acontecer em Plan 42 quando provider ML começar a popular). Não modificar comportamento.
- Adicionar comentário no topo do arquivo (pt-BR): `// PHASE 39 — este Repository (Phase 30) permanece para compat com SugadorController::mlbs e SyncCompanyAdgroupMlbsJob. Novo código deve usar App\Repositories\AdgroupMlbMapRepository (API neutra). Phase 43 vai consolidar em um único repository quando adman_adgroup_mlbs for renomeada.`

NÃO modificar `SugadorController::mlbs`, `SyncCompanyAdgroupMlbsJob`, nem a migration. Eles continuam usando o legacy repository (compat preservada).

Rode `php artisan test --filter='AdgroupMlbMapRepositoryTest'` — DEVE PASSAR (GREEN).

Rode regressão: `php artisan test --filter=Sugador` — mesma contagem. Em particular tests do SugadorController::mlbs e SyncCompanyAdgroupMlbsJob continuam verdes (proxy preservou API).

Commit: `feat(39-03): adiciona AdgroupMlbMapRepository neutro + comentário compat no legacy (GREEN)`.
  </action>
  <verify>
    <automated>php artisan test --filter='AdgroupMlbMapRepositoryTest' && php artisan test --filter=Sugador</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/Repositories/AdgroupMlbMapRepository.php` exit 0
    - `grep -c "class AdgroupMlbMapRepository" app/Repositories/AdgroupMlbMapRepository.php` retorna 1
    - `grep -c "public function getMlbsForAdgroup\|public function setMlbsForAdgroup\|public function bulkSetFromProvider" app/Repositories/AdgroupMlbMapRepository.php` retorna 3
    - `grep -c "adman_adgroup_mlbs" app/Repositories/AdgroupMlbMapRepository.php` retorna 0 (zero referência ao nome legado — usa AdmanAdgroupMlb model que encapsula table name)
    - `grep -c "PHASE 39" app/Services/AdmanAdgroupMlbsRepository.php` retorna >=1 (comentário compat adicionado)
    - `git diff --name-only HEAD database/migrations/` retorna vazio (migration NÃO modificada)
    - `git diff --name-only HEAD app/Http/Controllers/SugadorController.php` retorna vazio (controller NÃO modificado — proxy preservou API)
    - `git diff --name-only HEAD app/Jobs/SyncCompanyAdgroupMlbsJob.php` retorna vazio (job NÃO modificado)
    - `php artisan test --filter='AdgroupMlbMapRepositoryTest'` exit 0 (8 tests verdes)
    - `php artisan test --filter=Sugador` exit 0 (zero regressão — tests do SugadorController::mlbs + SyncCompanyAdgroupMlbsJob continuam verdes via legacy repository)
    - Commit `feat(39-03): ... (GREEN)` em git log
  </acceptance_criteria>
  <done>AdgroupMlbMapRepository neutro criado com 3 métodos do CONTEXT; legacy AdmanAdgroupMlbsRepository ganhou comentário compat mas mantém comportamento 100% inalterado; SugadorController e SyncCompanyAdgroupMlbsJob continuam usando o legacy sem mudança; 8 tests Phase 39-03 verdes; zero regressão; commit GREEN feito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → adman_adgroup_mlbs table | Read/write interno ao processo PHP — sem entrada HTTP nova |
| companyId (int) → cust_id (string) | Resolução interna via Company::find — sem input untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03-01 | Tampering | Concurrent writes mesma chave | mitigate | Unique constraint `adgmlb_unique (cust_id, adgroup_id, mlb_id, period_from, period_to)` na migration garante idempotência; upsert nunca duplica row. |
| T-39-03-02 | DoS via bulk insert | bulkSetFromProvider com map gigante | mitigate | `array_chunk($rows, 500)` evita payload SQL gigante (mesmo limite usado em SyncCompanyAdgroupMlbsJob:163). |
| T-39-03-03 | Regression em call-sites existentes | SugadorController::mlbs, SyncCompanyAdgroupMlbsJob | mitigate | Legacy repository mantido sem mudança comportamental; suite Sugador continua verde (acceptance criteria). |
</threat_model>

<verification>
- Tests Unit Phase 39-03 verdes: `php artisan test --filter='AdgroupMlbMapRepositoryTest'`
- Regressão Sugador: `php artisan test --filter=Sugador` mesma contagem
- Migration intacta: `git diff database/migrations/` vazio
- Call-sites intactos: `git diff app/Http/Controllers/SugadorController.php app/Jobs/SyncCompanyAdgroupMlbsJob.php` vazio
- Nome legado escondido no novo repository: `grep -c "adman_adgroup_mlbs" app/Repositories/AdgroupMlbMapRepository.php` == 0
</verification>

<success_criteria>
- `App\Repositories\AdgroupMlbMapRepository` criado com 3 métodos exatos do §decisions do CONTEXT
- API neutra: sem citar `adman_adgroup_mlbs` na implementação (encapsulado via Model `AdmanAdgroupMlb`)
- Legacy `App\Services\AdmanAdgroupMlbsRepository` preservado sem mudança comportamental (compat com call-sites Phase 30)
- SugadorController e SyncCompanyAdgroupMlbsJob continuam funcionando sem mudança
- 8 tests Unit Phase 39-03 verdes via SQLite em-memory
- Zero regressão suite Sugador
- Zero modificação na migration `adman_adgroup_mlbs` (rename físico é Phase 43)
- 2 commits: RED + GREEN com prefixo `(39-03)`
</success_criteria>

<output>
Criar `.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-03-SUMMARY.md` ao finalizar.
</output>
