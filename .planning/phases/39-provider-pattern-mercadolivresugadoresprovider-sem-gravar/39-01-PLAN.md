---
phase: 39-provider-pattern-mercadolivresugadoresprovider-sem-gravar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Contracts/SugadoresAdsProvider.php
  - app/Services/Sugadores/AdmanSugadoresProvider.php
  - app/Services/Sugadores/SugadoresAdsProviderFactory.php
  - tests/Unit/Phase39/AdmanSugadoresProviderTest.php
  - tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php
autonomous: true
requirements:
  - REQ-39-01
  - REQ-39-02
  - REQ-39-05
tags: [phase-39, sugadores, contract, provider, adman, ddd, tdd, ml-migration]
must_haves:
  truths:
    - "Interface PHP App\\Contracts\\SugadoresAdsProvider existe declarando exatamente os 6 métodos definidos no §decisions do 39-CONTEXT"
    - "AdmanSugadoresProvider implementa o contract, recebe AdmanService via constructor injection e NÃO modifica AdmanService"
    - "Factory resolve provider Adman quando company.adman_account_id presente, lança RuntimeException quando ambos ausentes; aceita forceName='adman'"
    - "Tests Unit Phase 39 (Adman + Factory) verdes via Mockery — não chamam HTTP real nem requerem MariaDB"
    - "Suite Sugador existente (php artisan test --filter=Sugador) continua verde — zero regressão"
  artifacts:
    - path: "app/Contracts/SugadoresAdsProvider.php"
      provides: "Interface PHP SugadoresAdsProvider com 6 métodos abstratos + PHPDoc descrevendo cada chave do contrato normalizado"
      contains: "interface SugadoresAdsProvider"
      min_lines: 60
    - path: "app/Services/Sugadores/AdmanSugadoresProvider.php"
      provides: "Implementação Adman do contract via composição de AdmanService"
      contains: "implements SugadoresAdsProvider"
      min_lines: 80
    - path: "app/Services/Sugadores/SugadoresAdsProviderFactory.php"
      provides: "Factory de resolução de provider por (forceName, company.adman_account_id, company.mlToken)"
      contains: "class SugadoresAdsProviderFactory"
      min_lines: 40
    - path: "tests/Unit/Phase39/AdmanSugadoresProviderTest.php"
      provides: "Tests Unit Mockery cobrindo normalização Adman→contrato"
      min_lines: 100
    - path: "tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php"
      provides: "Tests Unit cobrindo resolução por (forceName + capability detection)"
      min_lines: 60
  key_links:
    - from: "app/Services/Sugadores/AdmanSugadoresProvider.php"
      to: "app/Services/AdmanService.php"
      via: "constructor injection (composição, não herança)"
      pattern: "private AdmanService"
    - from: "app/Services/Sugadores/SugadoresAdsProviderFactory.php"
      to: "app/Contracts/SugadoresAdsProvider.php"
      via: "return type"
      pattern: "SugadoresAdsProvider"
---

<objective>
Fundação da Phase 39: criar o contract `SugadoresAdsProvider` (interface PHP), implementar o `AdmanSugadoresProvider` encapsulando o `AdmanService` atual via composição, e criar o `SugadoresAdsProviderFactory` ainda apenas com a branch Adman. ZERO mudança no `SugadorAnalysisService` ou no `AdmanService`. Tudo testado com Mockery (sem HTTP, sem MariaDB).

Purpose: separar o "como buscar dados de anúncios" do "como detectar sugadores", permitindo que Plans 39-02 (provider ML) e 39-04 (refactor do analysis service) sejam construídos contra um contract estável.

Output: 3 arquivos de produção novos + 2 arquivos de teste novos. Wave 1 sozinha (fundação que outros plans dependem).

Wave justification: Plan 39-02 (provider ML) depende deste contract. Plan 39-03 (repository) é independente e roda em paralelo (Wave 2). Plan 39-04 (refactor SugadorAnalysisService) depende de 39-01 + 39-02 (precisa do factory completo). Plan 39-05 (command) depende de 39-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md
@plano-migracao-sugadores-ml-direto.md

<interfaces>
<!-- Contratos extraídos do código existente que o executor consome diretamente. -->

From app/Services/AdmanService.php (Plan 39-01 NÃO modifica; só consome):
```php
namespace App\Services;

class AdmanService
{
    // Métodos consumidos pelo AdmanSugadoresProvider (assinaturas atuais do prod):
    public function fetchAdsMetrics(string $custId, string $dateFrom, string $dateTo, int $limit = 50, string $marketplace = 'meli'): array;
    public function fetchCampaignsRange(string $custId, string $dateFrom, string $dateTo, string $marketplace = 'meli'): array;
    public function fetchAllCampaigns(string $custId, string $marketplace = 'meli'): array;
    // OBS: AdmanService já normaliza payloads para chaves: campaign_id, campaign_name,
    // campaign_status, adgroup_id, adgroup_name, investment, revenue, sold_quantity,
    // clicks, impressions, cpc, ctr, acos, roas, organic_amount, organic_units, raw.
    // O AdmanSugadoresProvider só precisa repassar (talvez aplicar safe_div em valores null).
}
```

From app/Models/Company.php:
```php
public function getCustIdAttribute(): ?string;   // adman_account_id ?: ml_store_id
public function getIsMlDrivenAttribute(): bool;  // optional($this->mlToken)->status === 'active'
public function mlToken();                       // hasOne MlToken
// Atributo cru usado pelo AdmanSugadoresProvider::supports():
//   $company->adman_account_id  (string|null)
```

From plano-migracao-sugadores-ml-direto.md §2.3 (CONTRATO NORMALIZADO — fonte canônica):
Cada item de fetchAdgroupsMetrics deve ter as chaves:
  adgroup_id, adgroup_name, campaign_id, thumbnail, adgroup_type, catalog_listing,
  mlb_id, mlb_titulo, investment, revenue, sold_quantity, clicks, impressions,
  cpc, ctr, acos, roas, organic_amount, organic_units, raw
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Criar contract + tests Unit failing</name>
  <files>app/Contracts/SugadoresAdsProvider.php, tests/Unit/Phase39/AdmanSugadoresProviderTest.php, tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php</files>
  <read_first>
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md (§decisions — assinatura exata dos 6 métodos do contract)
    - plano-migracao-sugadores-ml-direto.md (§2.3 — chaves obrigatórias do contrato normalizado)
    - app/Services/AdmanService.php (entender assinaturas atuais que AdmanSugadoresProvider vai encapsular — métodos fetchAdsMetrics, fetchCampaignsRange, fetchAllCampaigns)
    - tests/Feature/Phase38/MercadoLivreAdsServiceTest.php (referência de estilo de tests; tests Unit aqui NÃO usam RefreshDatabase pois não tocam DB)
  </read_first>
  <behavior>
    Tests Unit cobrindo:
    1. AdmanSugadoresProviderTest::test_supports_returns_true_when_company_has_adman_account_id — fixture Company com adman_account_id='123', assert supports() == true
    2. AdmanSugadoresProviderTest::test_supports_returns_false_when_company_has_no_adman_account_id — fixture sem adman_account_id, assert false
    3. AdmanSugadoresProviderTest::test_name_returns_adman_string — assertSame('adman', $provider->name())
    4. AdmanSugadoresProviderTest::test_fetchAdgroupsMetrics_delegates_to_adman_service_and_returns_normalized_contract — Mockery::mock(AdmanService::class)->shouldReceive('fetchAdsMetrics')->andReturn([...payload com chaves do §2.3...]); assert provider->fetchAdgroupsMetrics retorna o mesmo array com todas as chaves obrigatórias do contrato §2.3
    5. AdmanSugadoresProviderTest::test_fetchCampaignsMetrics_delegates_to_fetchCampaignsRange — Mockery do AdmanService, assert delegation correta com from/to em formato YYYY-MM-DD
    6. AdmanSugadoresProviderTest::test_fetchCampaigns_delegates_to_fetchAllCampaigns_and_normalizes — Mockery, assert keys [campaign_id, campaign_name, campaign_status]
    7. AdmanSugadoresProviderTest::test_fetchAdgroupMlbs_returns_empty_array_when_adman_path_used — provider Adman NÃO precisa popular adgroup→MLBs (vem do AdmanAdgroupMlbsRepository sync separado). Retorna [].
    8. SugadoresAdsProviderFactoryTest::test_for_with_force_name_adman_returns_adman_provider
    9. SugadoresAdsProviderFactoryTest::test_for_with_no_force_falls_back_to_adman_when_company_has_adman_account_id
    10. SugadoresAdsProviderFactoryTest::test_for_throws_runtimeexception_when_no_provider_supports_company — fixture Company sem adman_account_id e sem mlToken
  </behavior>
  <action>
Crie `app/Contracts/SugadoresAdsProvider.php` (interface PHP, namespace `App\Contracts`):
- Métodos abstratos (signatures EXATAS, com PHPDoc descrevendo chaves do contrato normalizado §2.3):
  - `public function supports(Company $company): bool;`
  - `public function name(): string;`
  - `public function fetchCampaigns(Company $company): array;` — retorna `array<int, array{campaign_id: string, campaign_name: ?string, campaign_status: ?string, raw: ?array}>`
  - `public function fetchCampaignsMetrics(Company $company, Carbon $from, Carbon $to): array;` — retorna lista com chaves `campaign_id, campaign_name, campaign_status, investment, revenue, sold_quantity, clicks, impressions, cpc, acos, roas, raw`
  - `public function fetchAdgroupsMetrics(Company $company, Carbon $from, Carbon $to): array;` — retorna lista com TODAS as chaves do §2.3 do plano (adgroup_id, adgroup_name, campaign_id, thumbnail, adgroup_type, catalog_listing, mlb_id, mlb_titulo, investment, revenue, sold_quantity, clicks, impressions, cpc, ctr, acos, roas, organic_amount, organic_units, raw)
  - `public function fetchAdgroupMlbs(Company $company, Carbon $from, Carbon $to): array;` — retorna `array<string adgroup_id, array<int, string mlb_id>>`
- PHPDoc da classe explica o contract em pt-BR + cita Phase 39 + cita §2.3 do plano canônico
- Use declarations: `App\Models\Company`, `Carbon\Carbon`

Crie `tests/Unit/Phase39/AdmanSugadoresProviderTest.php`:
- `namespace Tests\Unit\Phase39;`
- `extends Tests\TestCase` (NÃO usar RefreshDatabase — testes puramente Unit com Mockery)
- 7 tests listados em <behavior>
- Setup: cada test cria Company via `new Company(['adman_account_id' => ...])` SEM persistir (Company não é necessário no banco para os tests de delegation; usar `tap(new Company([...]))->setRawAttributes([...])` ou similar)
- Mockery: `$admanMock = \Mockery::mock(\App\Services\AdmanService::class);` em cada test, configurar shouldReceive específico, passar via constructor
- Test 4 (fetchAdgroupsMetrics): payload fixture com TODAS as 20 chaves do §2.3 — verificar que provider preserva todas

Crie `tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php`:
- 3 tests listados em <behavior>
- Para evitar dependência do MercadoLivreSugadoresProvider (criado em Plan 39-02), o factory neste plan recebe APENAS AdmanSugadoresProvider via constructor. Tests usam um stub de `SugadoresAdsProvider` Mockery para simular "ML provider ausente" no Plan 39-01.

NÃO modificar AdmanService, SugadorAnalysisService, MercadoLivreService, MercadoLivreAdsService.

Rode `php artisan test --filter='AdmanSugadoresProviderTest|SugadoresAdsProviderFactoryTest'` — DEVE FALHAR (RED). Commitar com mensagem `test(39-01): adiciona suite Unit do contract + Adman provider + factory (RED)`.
  </action>
  <verify>
    <automated>php artisan test --filter='AdmanSugadoresProviderTest|SugadoresAdsProviderFactoryTest' 2>&amp;1 | grep -E "FAILED|Errors|Class.*not found"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/Contracts/SugadoresAdsProvider.php` exit 0
    - `grep -c "interface SugadoresAdsProvider" app/Contracts/SugadoresAdsProvider.php` retorna 1
    - `grep -c "public function supports\|public function name\|public function fetchCampaigns\b\|public function fetchCampaignsMetrics\|public function fetchAdgroupsMetrics\|public function fetchAdgroupMlbs" app/Contracts/SugadoresAdsProvider.php` retorna 6
    - `test -f tests/Unit/Phase39/AdmanSugadoresProviderTest.php` exit 0
    - `test -f tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php` exit 0
    - `grep -c "public function test_" tests/Unit/Phase39/AdmanSugadoresProviderTest.php` retorna >=7
    - `grep -c "public function test_" tests/Unit/Phase39/SugadoresAdsProviderFactoryTest.php` retorna >=3
    - `php artisan test --filter='AdmanSugadoresProviderTest|SugadoresAdsProviderFactoryTest'` exit code != 0 (RED state — classes provider/factory ainda não existem)
    - Commit `test(39-01): ... (RED)` presente em `git log --oneline -1`
  </acceptance_criteria>
  <done>Interface SugadoresAdsProvider criada com 6 métodos abstratos + PHPDoc; 2 arquivos de tests Unit existem com tests vermelhos esperados (classes provider/factory ainda não implementadas); commit RED feito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implementar AdmanSugadoresProvider + SugadoresAdsProviderFactory</name>
  <files>app/Services/Sugadores/AdmanSugadoresProvider.php, app/Services/Sugadores/SugadoresAdsProviderFactory.php</files>
  <read_first>
    - app/Services/AdmanService.php (assinaturas exatas dos métodos consumidos: fetchAdsMetrics, fetchCampaignsRange, fetchAllCampaigns — mesma forma que SugadorAnalysisService chama)
    - app/Services/SugadorAnalysisService.php (linhas 124, 144, 218, 536 — pontos onde AdmanService é chamado hoje; o provider deve preservar EXATAMENTE a mesma normalização de saída)
    - app/Contracts/SugadoresAdsProvider.php (criado na Task 1)
    - tests/Unit/Phase39/AdmanSugadoresProviderTest.php (criado na Task 1 — implementação deve fazê-los passar)
    - .planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-CONTEXT.md (§"AdmanSugadoresProvider — encapsula Adman atual" — regras de normalização)
  </read_first>
  <action>
Crie `app/Services/Sugadores/AdmanSugadoresProvider.php`:
- `namespace App\Services\Sugadores;`
- `use App\Contracts\SugadoresAdsProvider; use App\Models\Company; use App\Services\AdmanService; use Carbon\Carbon;`
- `class AdmanSugadoresProvider implements SugadoresAdsProvider`
- Constructor promoted property: `public function __construct(private AdmanService $adman) {}`
- Implementar os 6 métodos:
  - `supports(Company $c): bool` → `return !empty($c->adman_account_id);`
  - `name(): string` → `return 'adman';`
  - `fetchCampaigns(Company $c): array` → `$raw = $this->adman->fetchAllCampaigns($c->adman_account_id, $c->marketplace ?? 'meli');` e mapear cada item para `['campaign_id' => (string)($r['campaignId'] ?? $r['id'] ?? ''), 'campaign_name' => $r['name'] ?? null, 'campaign_status' => $r['status'] ?? null, 'raw' => $r]`
  - `fetchCampaignsMetrics(Company $c, Carbon $from, Carbon $to): array` → `$this->adman->fetchCampaignsRange($c->adman_account_id, $from->toDateString(), $to->toDateString(), $c->marketplace ?? 'meli')` retorna já com chaves campaign_id/etc; aplicar `safe_div` privado para CPC/ACOS/ROAS null
  - `fetchAdgroupsMetrics(Company $c, Carbon $from, Carbon $to): array` → `$this->adman->fetchAdsMetrics($c->adman_account_id, $from->toDateString(), $to->toDateString(), 50, $c->marketplace ?? 'meli')` retorna já normalizado; aplicar `safe_div` para campos calculados null
  - `fetchAdgroupMlbs(Company $c, Carbon $from, Carbon $to): array` → retorna `[]` (path Adman popula via Job separado SyncCompanyAdgroupMlbsJob → AdmanAdgroupMlbsRepository; o provider Adman NÃO repopula adgroup→MLBs)
- Método privado `safe_div(float|int|null $num, float|int|null $den): ?float` retorna null se $den nulo/zero, senão $num/$den
- PHPDoc class explica em pt-BR: encapsula AdmanService via composição, NÃO modifica AdmanService, normalização §2.3 preservada

Crie `app/Services/Sugadores/SugadoresAdsProviderFactory.php`:
- `namespace App\Services\Sugadores;`
- `use App\Contracts\SugadoresAdsProvider; use App\Models\Company;`
- `class SugadoresAdsProviderFactory`
- Constructor: `public function __construct(private AdmanSugadoresProvider $admanProvider) {}` (no Plan 39-01 SÓ Adman; Plan 39-02 adiciona MercadoLivreSugadoresProvider)
- `public function for(Company $company, ?string $forceName = null): SugadoresAdsProvider`
  - if `$forceName === 'adman'` → return `$this->admanProvider`
  - if `$forceName === 'ml'` → throw new `\RuntimeException('Provider "ml" não disponível neste plano — adicionado em Plan 39-02')` (placeholder; Plan 39-02 substitui)
  - if `$this->admanProvider->supports($company)` → return `$this->admanProvider`
  - throw new `\RuntimeException("Empresa {$company->id} sem provider compatível (Phase 39: só Adman disponível antes do Plan 39-02)")`
- PHPDoc explica que o factory será expandido em Plan 39-02 (branch ML) e que a regra default "prefere Adman" valerá até Phase 42 introduzir cut-over por empresa

Rode `php artisan test --filter='AdmanSugadoresProviderTest|SugadoresAdsProviderFactoryTest'` — DEVE PASSAR (GREEN).

Rode regressão: `php artisan test --filter=Sugador` — DEVE passar com mesmo count que antes (zero regressão no path atual do SugadorAnalysisService, que ainda não foi tocado).

Commitar GREEN: `feat(39-01): implementa AdmanSugadoresProvider + factory (GREEN)`.
  </action>
  <verify>
    <automated>php artisan test --filter='AdmanSugadoresProviderTest|SugadoresAdsProviderFactoryTest' && php artisan test --filter=Sugador</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/Services/Sugadores/AdmanSugadoresProvider.php` exit 0
    - `test -f app/Services/Sugadores/SugadoresAdsProviderFactory.php` exit 0
    - `grep -c "implements SugadoresAdsProvider" app/Services/Sugadores/AdmanSugadoresProvider.php` retorna 1
    - `grep -c "private AdmanService" app/Services/Sugadores/AdmanSugadoresProvider.php` retorna >=1 (composição via constructor)
    - `git diff --name-only HEAD app/Services/AdmanService.php` retorna vazio (AdmanService NÃO modificado)
    - `php artisan test --filter='AdmanSugadoresProviderTest|SugadoresAdsProviderFactoryTest'` exit 0 (GREEN)
    - `php artisan test --filter=Sugador` exit 0 com mesmo count de tests que antes do plan (gravar count em deferred-items.md se algum test pré-existente falhar — SCOPE BOUNDARY)
    - Commit `feat(39-01): ... (GREEN)` presente em git log
  </acceptance_criteria>
  <done>AdmanSugadoresProvider implementa o contract via composição de AdmanService (sem modificá-lo); factory minimal funciona; 10 tests Phase 39-01 verdes; suite Sugador existente continua verde; commit GREEN feito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AdmanService → AdmanSugadoresProvider | Camada interna PHP — sem entrada externa nova |
| Factory → caller | Caller pode passar `forceName` — string interna, não input HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Tampering | Composição AdmanService | accept | Provider só LÊ via métodos públicos do AdmanService — não modifica estado interno. Severidade NONE. |
| T-39-01-02 | Information Disclosure | Logs do provider | accept | Provider não loga payloads (delega tudo ao AdmanService que já tem logging). Sem novos pontos de leak de credencial. |
| T-39-01-03 | Denial of Service | Loop infinito de chamadas | mitigate | AdmanService já tem rate-limit `adman-api` (Plan 30-01); provider só repassa chamadas, não introduz loop. |
</threat_model>

<verification>
- Suite Phase 39-01 (Unit) verde: `php artisan test --filter='AdmanSugadoresProviderTest|SugadoresAdsProviderFactoryTest'`
- Suite Sugador (regressão path atual): `php artisan test --filter=Sugador` — count de tests não cai
- `git diff --name-only HEAD~2 HEAD app/Services/AdmanService.php` vazio (AdmanService intocado)
- `git diff --name-only HEAD~2 HEAD app/Services/SugadorAnalysisService.php` vazio (analysis service intocado — refactor é Plan 39-04)
- Phpstan/pint (se rodáveis localmente): `vendor/bin/pint --test app/Contracts app/Services/Sugadores` 0 violações
</verification>

<success_criteria>
- Contract `SugadoresAdsProvider` declarado com 6 métodos exatos do §decisions do CONTEXT
- `AdmanSugadoresProvider implements SugadoresAdsProvider` via composição de `AdmanService`
- Factory resolve provider Adman por (a) forceName='adman' ou (b) capability detection via supports()
- 10+ tests Unit Phase 39-01 verdes (7 Adman + 3 Factory)
- Zero regressão na suite Sugador existente
- Zero modificações em AdmanService, SugadorAnalysisService, MercadoLivreService, MercadoLivreAdsService
- 2 commits: RED + GREEN com prefixo `(39-01)`
</success_criteria>

<output>
Criar `.planning/phases/39-provider-pattern-mercadolivresugadoresprovider-sem-gravar/39-01-SUMMARY.md` ao finalizar.
</output>
