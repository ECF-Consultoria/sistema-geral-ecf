---
phase: 68-schema-modelos-e-seed-retroativo-nps-padr-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php
  - database/migrations/2026_07_07_100002_alter_nps_surveys_add_template_id.php
  - database/migrations/2026_07_07_100003_alter_nps_responses_scores_nullable.php
autonomous: true
requirements: [NPS-A-01, NPS-A-04]

must_haves:
  truths:
    - "Após `php artisan migrate:fresh --env=testing`, existem 5 tabelas novas no schema: `nps_templates`, `nps_template_questions`, `nps_template_options`, `nps_template_service_scopes`, `nps_response_answers`"
    - "`nps_surveys` ganha coluna `template_id` (unsignedBigInteger nullable FK para `nps_templates`, nullOnDelete)"
    - "`nps_responses.score_estrategista/analista/empresa` viram NULLABLE — rows antigas mantém valores, mas novas rows podem gravar null (cálculo vai vir de `nps_response_answers` a partir da Phase 69)"
    - "`nps_response_answers` armazena snapshot congelado (`question_texto_snapshot`, `question_dimensao_snapshot`, `option_label_snapshot`, `option_peso_snapshot`) — mudanças futuras no template NÃO alteram histórico"
    - "Migrations são idempotentes (guards `Schema::hasTable` / `Schema::hasColumn`) e reversíveis (down() dropa em ordem correta de FK)"
  artifacts:
    - path: "database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php"
      provides: "Cria as 5 tabelas novas + índices + FKs"
      contains: "Schema::create('nps_templates'"
    - path: "database/migrations/2026_07_07_100002_alter_nps_surveys_add_template_id.php"
      provides: "Adiciona coluna template_id em nps_surveys"
      contains: "->foreignId('template_id')"
    - path: "database/migrations/2026_07_07_100003_alter_nps_responses_scores_nullable.php"
      provides: "Torna score_estrategista/analista/empresa nullable"
      contains: "->nullable()->change()"
  key_links:
    - from: "nps_response_answers.response_id"
      to: "nps_responses.id"
      via: "FK cascadeOnDelete"
      pattern: "foreignId.*response_id.*cascadeOnDelete"
    - from: "nps_template_questions.template_id"
      to: "nps_templates.id"
      via: "FK cascadeOnDelete"
      pattern: "foreignId.*template_id.*constrained.*nps_templates.*cascadeOnDelete"
    - from: "nps_surveys.template_id"
      to: "nps_templates.id"
      via: "FK nullOnDelete (nullable)"
      pattern: "foreignId.*template_id.*nullable.*nullOnDelete"
    - from: "nps_template_service_scopes"
      to: "nps_templates + servicos"
      via: "pivot composto (template_id, servico_id) unique"
      pattern: "unique.*template_id.*servico_id"
---

<objective>
Criar as 5 tabelas novas do schema NPS Templates (v15.0) + alterar `nps_surveys` e `nps_responses` para acomodar a nova arquitetura de templates configuráveis. Nenhum seed, nenhum Model — apenas o schema físico.

**Propósito:** fundação de dados. Sem essas tabelas, nada da v15.0 funciona (Modelos Eloquent do Plan 68-02, seed do 68-03, dedup_key do 68-04, testes do 68-05 e todas as phases 69-73 dependem).

**Output:** 3 arquivos de migration com timestamp `2026_07_07_100001..100003` que rodam em ordem, criam as tabelas com constraints e índices conforme spec do research §1, e são idempotentes/reversíveis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v15-nps-templates-schema.md
@CLAUDE.md

<!-- Padrões source-of-truth pra replicar (snapshot per-row + índices) -->
@database/migrations/2026_06_12_100001_create_nps_perguntas_customizadas_table.php
@database/migrations/2026_06_12_100002_create_nps_respostas_customizadas_table.php
@database/migrations/2026_06_10_100002_recreate_nps_responses_table.php
@database/migrations/2026_06_10_100003_add_month_reference_and_auto_generated_to_nps_surveys_table.php

<interfaces>
<!-- Referências extraídas dos arquivos existentes que os schema novos devem espelhar em estilo -->

De `nps_respostas_customizadas` (padrão de snapshot per-row a replicar):
- FK cascadeOnDelete pro dono (`response_id`)
- FK nullOnDelete pra referência viva (`pergunta_id`)
- Colunas `pergunta_texto_snapshot` (varchar 500), `tipo_snapshot` (varchar 20), `valor` (text)
- Índice em `response_id` para acelerar eager load

De `nps_surveys` (schema atual — target do alter):
- Já tem: id, uuid token, company_id FK, generated_by FK nullable, expires_at, completed_at, status enum, timestamps, month_reference date nullable, auto_generated bool
- Falta adicionar: `template_id` FK nullable pra `nps_templates`

De `nps_responses` (schema atual — target do alter):
- Já tem: id, survey_id FK, respondent_name nullable, score_estrategista tinyint (NOT NULL), score_analista tinyint nullable, score_empresa tinyint (NOT NULL), comment text nullable
- Precisa: score_estrategista + score_empresa viram NULLABLE (score_analista já é)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration principal — cria as 5 tabelas novas</name>
  <files>database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php</files>
  <read_first>
    - .planning/research/v15-nps-templates-schema.md §1 (colunas exatas de `nps_response_answers` + índice)
    - database/migrations/2026_06_12_100001_create_nps_perguntas_customizadas_table.php (padrão de índice ativa+ordem, comentários pt-BR)
    - database/migrations/2026_06_12_100002_create_nps_respostas_customizadas_table.php (padrão snapshot per-row + FKs cascade/nullOnDelete)
    - CLAUDE.md (comentários em pt-BR)
  </read_first>
  <action>
    Criar uma única migration `2026_07_07_100001_create_nps_templates_v15_tables.php` que cria as 5 tabelas em sequência (idempotente via `Schema::hasTable` guard antes de cada `create`). Docblock pt-BR no topo referenciando Phase 68 Plan 01, research §1, e REQ NPS-A-01/NPS-A-04.

    **Tabela 1 — `nps_templates`:**
    - `id` (bigint pk)
    - `nome` (varchar 200) — título visível ao admin
    - `descricao` (text nullable) — descrição interna
    - `active` (boolean default true) — soft flag (nunca hard-delete)
    - `is_default` (boolean default false) — flag do template fallback
    - `priority` (integer default 0) — usado em `NpsTemplateService::resolveForCompany` (Phase 69) via `ORDER BY priority DESC`
    - `envio_automatico_mensal` (boolean default false) — hint pro comando `nps:disparar-mensal` (Phase 69/72)
    - timestamps
    - **Índice** em `(active, priority)` — acelera resolveForCompany
    - Comentários pt-BR em cada coluna explicando propósito

    **Tabela 2 — `nps_template_questions`:**
    - `id` (bigint pk)
    - `template_id` (foreignId → `nps_templates` cascadeOnDelete)
    - `texto` (varchar 500) — enunciado renderizado no form público
    - `tipo` (enum ['escala', 'opcoes']) — hint pra UI admin (Phase 70); backend uniforme via options
    - `dimensao` (enum ['estrategista', 'analista', 'empresa', 'geral']) — `NpsScoreCalculator` filtra por isso (Phase 69, NPS-B-02)
    - `obrigatoria` (boolean default false)
    - `active` (boolean default true) — soft flag
    - `ordem` (integer default 0) — ordem ASC de renderização
    - timestamps
    - **Índice** `(template_id, active, ordem)` — acelera carregamento do template para render/preview

    **Tabela 3 — `nps_template_options`:**
    - `id` (bigint pk)
    - `template_question_id` (foreignId → `nps_template_questions` cascadeOnDelete)
    - `label` (varchar 200) — visível ao cliente
    - `peso` (unsignedTinyInteger) — 1..5 usado em `AVG(option_peso_snapshot)` (NPS-B-02); comment "1-5" na coluna
    - `ordem` (integer default 0)
    - timestamps
    - **Índice** `(template_question_id, ordem)`

    **Tabela 4 — `nps_template_service_scopes` (pivot):**
    - `id` (bigint pk)
    - `template_id` (foreignId → `nps_templates` cascadeOnDelete)
    - `servico_id` (foreignId → `servicos` cascadeOnDelete)
    - timestamps
    - **Unique** composto `(template_id, servico_id)` — nome `nps_tpl_scope_uniq`
    - **Índice** simples em `servico_id` — acelera `whereIn('servico_id', ...)` do `resolveForCompany`

    **Tabela 5 — `nps_response_answers`:**
    - `id` (bigint pk)
    - `response_id` (foreignId → `nps_responses` cascadeOnDelete)
    - `template_question_id` (foreignId → `nps_template_questions` nullable + nullOnDelete) — FK viva, não fonte de cálculo (research §1)
    - `template_option_id` (foreignId → `nps_template_options` nullable + nullOnDelete)
    - `question_texto_snapshot` (varchar 500) — congelado no submit
    - `question_dimensao_snapshot` (enum ['estrategista', 'analista', 'empresa', 'geral']) — dashboard filtra por isso; NÃO nullable
    - `option_label_snapshot` (varchar 200)
    - `option_peso_snapshot` (unsignedTinyInteger) — `AVG` usa isso
    - `comentario` (text nullable) — resposta livre opcional (research §1)
    - timestamps
    - **Índice** `(response_id, question_dimensao_snapshot)` — nome `nps_ans_response_dim_idx` — acelera `NpsScoreCalculator` (NPS-B-02)

    **Método `down()`:** dropa as 5 tabelas na ORDEM REVERSA de dependência de FK: primeiro `nps_response_answers` (referencia questions/options), depois `nps_template_service_scopes`, depois `nps_template_options` (referencia questions), depois `nps_template_questions` (referencia templates), por último `nps_templates`. Todos com `Schema::dropIfExists` (idempotente).

    Comentários pt-BR obrigatórios em CADA tabela explicando propósito + referência a REQ (NPS-A-01/NPS-A-04) e ao research (§1). Espelhar estilo do docblock da migration `2026_06_12_100001` (padrão do projeto).
  </action>
  <verify>
    <automated>DB_CONNECTION=sqlite DB_DATABASE=:memory: php artisan migrate --path=/database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php --pretend 2>&1 | grep -c "create table" | grep -qE "^5$"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` retorna 0
    - Migration file passa sanity check: `php -l` verde
    - `grep -c "Schema::create" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` retorna 5
    - `grep -c "Schema::hasTable" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` retorna 5 (idempotência)
    - `grep -c "Schema::dropIfExists" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` retorna 5
    - `grep -c "cascadeOnDelete" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` >= 4 (questions.template_id, options.template_question_id, service_scopes.template_id + servico_id, response_answers.response_id)
    - `grep -c "nullOnDelete" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` >= 2 (response_answers.template_question_id + template_option_id)
    - `grep -q "question_texto_snapshot" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` (snapshot per-row research §1)
    - `grep -q "question_dimensao_snapshot" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php`
    - `grep -q "option_peso_snapshot" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php`
    - `grep -q "nps_ans_response_dim_idx" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` (índice composto nomeado)
    - `grep -q "nps_tpl_scope_uniq" database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php` (unique composto)
    - Rodar `php artisan migrate:fresh --env=testing` (SQLite in-memory) sem erros
  </acceptance_criteria>
  <done>
    Migration cria 5 tabelas com FKs + índices + constraints conforme research §1. Comentários pt-BR presentes. `migrate:fresh` verde em SQLite in-memory. Idempotente e reversível.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migration alter — adiciona template_id em nps_surveys</name>
  <files>database/migrations/2026_07_07_100002_alter_nps_surveys_add_template_id.php</files>
  <read_first>
    - database/migrations/2026_06_10_100003_add_month_reference_and_auto_generated_to_nps_surveys_table.php (padrão de alter em nps_surveys com comentários pt-BR)
    - app/Models/NpsSurvey.php (schema atual — confirmar colunas existentes antes do alter)
    - .planning/research/v15-nps-templates-schema.md §2 (motivação do template_id — dedup_key composto usa esta coluna no Plan 68-04)
  </read_first>
  <action>
    Criar migration `2026_07_07_100002_alter_nps_surveys_add_template_id.php` que adiciona coluna `template_id` em `nps_surveys` como FK nullable pra `nps_templates` com `nullOnDelete`.

    - `up()`: usar `Schema::hasColumn('nps_surveys', 'template_id')` guard antes do `Schema::table` (idempotência). Dentro do table:
      ```
      $table->foreignId('template_id')
            ->nullable()
            ->after('auto_generated')
            ->constrained('nps_templates')
            ->nullOnDelete()
            ->comment('Template usado no snapshot da survey — NULL só até seed retro-associar (Plan 68-03)');
      ```
    - `down()`: guard `Schema::hasColumn`, então `dropForeign` + `dropColumn('template_id')`.
    - Docblock pt-BR referenciando Phase 68 Plan 01, REQ NPS-A-01, e explicando que a coluna vai ser retro-populada pelo Plan 68-03 (seed) e usada como componente do `dedup_key` do Plan 68-04.
    - IMPORTANTE: `nullOnDelete` (não `cascadeOnDelete`) — se o template "NPS Padrão" for apagado (não deve ser), surveys históricas viram órfãs no template mas sobrevivem com o snapshot congelado em `nps_response_answers` (que é fonte de verdade histórica per research §1).
  </action>
  <verify>
    <automated>DB_CONNECTION=sqlite DB_DATABASE=:memory: php artisan migrate --env=testing 2>&1 | grep -q "2026_07_07_100002_alter_nps_surveys_add_template_id"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f database/migrations/2026_07_07_100002_alter_nps_surveys_add_template_id.php` retorna 0
    - Migration file passa `php -l`
    - `grep -q "template_id" database/migrations/2026_07_07_100002_alter_nps_surveys_add_template_id.php`
    - `grep -q "nullOnDelete" database/migrations/2026_07_07_100002_alter_nps_surveys_add_template_id.php`
    - `grep -q "Schema::hasColumn" database/migrations/2026_07_07_100002_alter_nps_surveys_add_template_id.php` (idempotência)
    - Após `migrate:fresh`, `\Schema::hasColumn('nps_surveys', 'template_id')` retorna true (validar via `php artisan tinker --execute="echo Schema::hasColumn('nps_surveys','template_id')?1:0;"`)
  </acceptance_criteria>
  <done>
    `nps_surveys.template_id` existe como FK nullable com `nullOnDelete`. Idempotente. Comentários pt-BR presentes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migration alter — nps_responses.score_* viram nullable</name>
  <files>database/migrations/2026_07_07_100003_alter_nps_responses_scores_nullable.php</files>
  <read_first>
    - database/migrations/2026_06_10_100002_recreate_nps_responses_table.php (schema atual — `score_estrategista` e `score_empresa` são NOT NULL; `score_analista` já é nullable)
    - CLAUDE.md (Laravel 12 exige `doctrine/dbal` pra `->change()`? — Laravel 12 já resolveu essa dependência internamente, mas confirmar comportamento SQLite)
    - .planning/research/v15-nps-templates-schema.md (Phase 69 vai calcular scores dinamicamente via `nps_response_answers`; colunas legadas ficam pra rows antigas)
  </read_first>
  <action>
    Criar migration `2026_07_07_100003_alter_nps_responses_scores_nullable.php` que torna `score_estrategista` e `score_empresa` nullable em `nps_responses`. `score_analista` já é nullable (validado no arquivo de recreate) — não modificar.

    **Motivação:** a partir da Phase 69, novas responses vão persistir dados em `nps_response_answers` (snapshot per-row), e as colunas legadas `score_*` ficam NULL. `NpsScoreCalculator` (Phase 69) vai preferir `nps_response_answers` quando existir; fallback pras colunas legadas quando não. Rows antigas (retro-associadas ao "NPS Padrão" no 68-03) mantêm valores nas colunas legadas.

    **Estratégia por driver (evita bug de `->change()` no SQLite):**
    - `up()`: usar `DB::connection()->getDriverName()`:
      - Se `mysql` ou `mariadb`: `Schema::table('nps_responses', function ($t) { $t->unsignedTinyInteger('score_estrategista')->nullable()->change(); $t->unsignedTinyInteger('score_empresa')->nullable()->change(); });`
      - Se `sqlite`: no-op + log info explicando que SQLite não suporta `->change()` limpo em coluna NOT NULL sem recreate; nos testes o schema é recriado do zero via `migrate:fresh` e cada CREATE já declara essas colunas como nullable neste path? — NÃO, a Task 1 não recria `nps_responses`. **Solução:** no path SQLite, usar `DB::statement` puro com `PRAGMA foreign_keys=OFF` + copy table (padrão Laravel doctrine já faz internamente na maioria dos casos). ALTERNATIVA MAIS SIMPLES: tentar `->change()` diretamente — Laravel 12 usa doctrine/dbal ou nativo, se falhar em SQLite documentar isso e adicionar comentário pt-BR.
    - **Approach final recomendado:** usar `->change()` direto no `Schema::table` sem branch por driver. Rodar `migrate:fresh` em SQLite in-memory; se falhar, adicionar branch com `DB::statement` (SQLite recria a tabela internamente, o que é aceitável em teste).
    - `down()`: reverter `score_estrategista` + `score_empresa` para NOT NULL — MAS marcar como no-op informativo se houver rows com NULL (reversão destrutiva); imprimir warning via `Log::warning('[Phase 68] Reverter esta migration exige backfill manual das colunas score_*')` e não rodar `->change()` no down (defensivo, padrão da migration `2026_06_10_100002`).

    Docblock pt-BR explicando: (a) motivação (Phase 69 grava tudo em `nps_response_answers`), (b) rows antigas preservam valores nas colunas legadas (dashboard atual continua funcionando via NPS-A seed), (c) `NpsScoreCalculator` coalescence.

    **Comportamento em produção MySQL/MariaDB:** ALTER COLUMN modifica NULL sem tocar dados existentes. Zero risco.
  </action>
  <verify>
    <automated>DB_CONNECTION=sqlite DB_DATABASE=:memory: php artisan migrate --env=testing 2>&1 | grep -q "Migrated"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f database/migrations/2026_07_07_100003_alter_nps_responses_scores_nullable.php` retorna 0
    - Migration file passa `php -l`
    - `grep -q "score_estrategista" database/migrations/2026_07_07_100003_alter_nps_responses_scores_nullable.php`
    - `grep -q "score_empresa" database/migrations/2026_07_07_100003_alter_nps_responses_scores_nullable.php`
    - `grep -qv "score_analista.*change" database/migrations/2026_07_07_100003_alter_nps_responses_scores_nullable.php` (não altera score_analista — já nullable)
    - Após `migrate:fresh --env=testing`, inserir um `NpsResponse` com `score_estrategista=null, score_empresa=null` NÃO deve gerar SQLSTATE 23000. Validar via tinker: `try{DB::table('nps_responses')->insert([...null scores...]); echo 'OK';}catch(\Throwable $e){echo $e->getMessage();}` retorna "OK"
    - Baseline: teste Phase 31 `Feature\NpsFluxoTest` (se existir) continua verde após aplicar esta migration + Task 1 + Task 2
  </acceptance_criteria>
  <done>
    `nps_responses.score_estrategista` e `nps_responses.score_empresa` passam a aceitar NULL. Testes existentes de NPS Phase 31 continuam verdes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → produção | ALTER em tabela legada (`nps_surveys`, `nps_responses`) com dados históricos preservados |
| snapshot vs live FK | `nps_response_answers` guarda snapshot congelado + FK viva; FK vive mas snapshot é a fonte de leitura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01-01 | Tampering | Migration destrutiva em prod | mitigate | Docblock explícito; `down()` de `100003` é no-op se houver NULL; `100001.down()` dropa em ordem reversa correta de FK; nenhum `truncate` |
| T-68-01-02 | Information Disclosure | Dados históricos NPS revelados a template errado | mitigate | Snapshot per-row em `nps_response_answers` é imutável; alteração futura de template no admin NÃO afeta histórico (research §1) |
| T-68-01-03 | Denial of Service | Índices ausentes causam full table scan em produção com N surveys | mitigate | Índice `(response_id, question_dimensao_snapshot)` obrigatório (research §1); índice `(template_id, active, ordem)` em `nps_template_questions`; índice `(active, priority)` em `nps_templates` |
| T-68-01-04 | Repudiation | Historico NPS alterado sem trilha | accept | Fase 68 não adiciona `LogsActivity` nas novas tabelas — dados são append-only via `NpsResponseAnswer`; auditoria interna via activity_log fica pra Phase 70 (UI Config) se surgir demanda |
</threat_model>

<verification>
Após aplicar as 3 migrations:

1. `php artisan migrate:fresh --env=testing` sem erros
2. Schema check: as 5 tabelas novas existem + `nps_surveys.template_id` existe + `nps_responses.score_estrategista/empresa` são nullable
3. Contagem: `select count(*) from information_schema.tables where table_name in ('nps_templates','nps_template_questions','nps_template_options','nps_template_service_scopes','nps_response_answers')` retorna 5 em MySQL; equivalente em SQLite via `sqlite_master`
4. Índices nomeados presentes (validar via `SHOW INDEX` MySQL / `PRAGMA index_list` SQLite):
   - `nps_ans_response_dim_idx` em `nps_response_answers`
   - `nps_tpl_scope_uniq` em `nps_template_service_scopes`
5. FKs corretas: `nps_response_answers.response_id` cascade; `nps_response_answers.template_question_id` nullOnDelete; `nps_surveys.template_id` nullOnDelete
6. Baseline de testes: rodar `php artisan test --filter=NpsFluxoTest` (Phase 31) — todos verdes; regressão zero confirmada
</verification>

<success_criteria>
- 3 arquivos de migration criados (`100001`, `100002`, `100003`)
- `migrate:fresh --env=testing` roda sem erros
- 5 tabelas novas existem + `nps_surveys.template_id` FK existe + `nps_responses.score_*` NULLABLE
- Índices nomeados conforme spec: `nps_ans_response_dim_idx`, `nps_tpl_scope_uniq`
- Snapshot columns em `nps_response_answers` presentes: `question_texto_snapshot`, `question_dimensao_snapshot`, `option_label_snapshot`, `option_peso_snapshot`
- REQ NPS-A-01 (schema) e NPS-A-04 (snapshot per-row) atendidos
- Zero regressão em testes de NPS existentes (Phase 31)
</success_criteria>

<output>
Criar `.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/68-01-SUMMARY.md` quando terminar.

**Não deployar sem autorização explícita** (deploy gate ativo — CLAUDE.md).
</output>
