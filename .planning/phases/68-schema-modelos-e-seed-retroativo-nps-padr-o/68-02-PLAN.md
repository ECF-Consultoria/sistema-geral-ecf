---
phase: 68-schema-modelos-e-seed-retroativo-nps-padr-o
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - app/Models/NpsTemplate.php
  - app/Models/NpsTemplateQuestion.php
  - app/Models/NpsTemplateOption.php
  - app/Models/NpsResponseAnswer.php
  - app/Models/NpsSurvey.php
  - app/Models/NpsResponse.php
  - database/factories/NpsTemplateFactory.php
  - database/factories/NpsTemplateQuestionFactory.php
  - database/factories/NpsTemplateOptionFactory.php
  - database/factories/NpsResponseAnswerFactory.php
autonomous: true
requirements: [NPS-A-02]

must_haves:
  truths:
    - "`NpsTemplate::first()` retorna instância e permite acessar `->questions` (HasMany NpsTemplateQuestion), `->serviceScopes` (BelongsToMany via pivot `nps_template_service_scopes`), `->surveys` (HasMany NpsSurvey via template_id)"
    - "`NpsTemplateQuestion::first()->options` retorna Collection de `NpsTemplateOption`"
    - "`NpsTemplateOption::first()->question` retorna a `NpsTemplateQuestion` dona"
    - "`NpsResponse::first()->answers` retorna Collection de `NpsResponseAnswer`"
    - "`NpsResponseAnswer::first()` responde: `->response`, `->templateQuestion`, `->templateOption`"
    - "Casts booleanos ativos: `NpsTemplate.active`, `NpsTemplate.is_default`, `NpsTemplate.envio_automatico_mensal`, `NpsTemplateQuestion.active`, `NpsTemplateQuestion.obrigatoria` — round-trip DB→model retorna PHP bool, não int"
    - "Factories `NpsTemplateFactory`, `NpsTemplateQuestionFactory`, `NpsTemplateOptionFactory`, `NpsResponseAnswerFactory` existem e produzem rows válidas em SQLite in-memory pra testes do Plan 68-05"
    - "`NpsSurvey` ganha relação `template()` (BelongsTo NpsTemplate) — `NpsSurvey::first()->template` funciona"
  artifacts:
    - path: "app/Models/NpsTemplate.php"
      provides: "Modelo Eloquent do template + relações"
      contains: "hasMany.*NpsTemplateQuestion"
    - path: "app/Models/NpsTemplateQuestion.php"
      provides: "Modelo pergunta + relação com options + template"
      contains: "hasMany.*NpsTemplateOption"
    - path: "app/Models/NpsTemplateOption.php"
      provides: "Modelo option + relação com question"
    - path: "app/Models/NpsResponseAnswer.php"
      provides: "Modelo snapshot per-row com relações vivas nullable"
    - path: "app/Models/NpsSurvey.php"
      provides: "Adiciona relação template() + fillable"
      contains: "belongsTo.*NpsTemplate"
    - path: "app/Models/NpsResponse.php"
      provides: "Adiciona relação answers() + score_* casts permanecem"
      contains: "hasMany.*NpsResponseAnswer"
    - path: "database/factories/NpsTemplateFactory.php"
      provides: "Factory pra testes"
  key_links:
    - from: "NpsTemplate"
      to: "NpsTemplateQuestion"
      via: "hasMany('template_id')"
      pattern: "hasMany\\(NpsTemplateQuestion::class\\)"
    - from: "NpsTemplate"
      to: "Servico"
      via: "belongsToMany via pivot nps_template_service_scopes"
      pattern: "belongsToMany.*Servico.*nps_template_service_scopes"
    - from: "NpsResponse"
      to: "NpsResponseAnswer"
      via: "hasMany('response_id')"
      pattern: "hasMany.*NpsResponseAnswer.*response_id"
    - from: "NpsSurvey"
      to: "NpsTemplate"
      via: "belongsTo('template_id')"
      pattern: "belongsTo.*NpsTemplate"
---

<objective>
Criar os 4 modelos Eloquent novos (`NpsTemplate`, `NpsTemplateQuestion`, `NpsTemplateOption`, `NpsResponseAnswer`) com relationships, casts e $fillable adequados; atualizar `NpsSurvey` (adicionar `template()` e `template_id` em $fillable) e `NpsResponse` (adicionar `answers()` HasMany). Criar 4 factories pra suporte a testes do Plan 68-05 e das Phases 69-73.

**Propósito:** camada de aplicação sobre o schema do Plan 68-01. Sem esses models, o seed do Plan 68-03 teria que usar `DB::table()` (menos idiomático) e os services Phase 69 (`NpsTemplateService::resolveForCompany`, `NpsScoreCalculator::compute`) não funcionariam.

**Output:** 6 arquivos PHP modificados/criados em `app/Models/` + 4 factories em `database/factories/`. Todos com PHPDoc pt-BR, casts corretos, relationships explícitas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/v15-nps-templates-schema.md
@CLAUDE.md

<!-- Padrões source-of-truth de modelo Eloquent com relationships + PHPDoc pt-BR -->
@app/Models/NpsSurvey.php
@app/Models/NpsResponse.php
@app/Models/NpsPerguntaCustomizada.php
@app/Models/NpsRespostaCustomizada.php
@app/Models/Servico.php
@app/Models/Company.php

<!-- Migrations criadas no Plan 68-01 são o schema alvo dos modelos -->
@database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php
@database/migrations/2026_07_07_100002_alter_nps_surveys_add_template_id.php

<interfaces>
<!-- Padrões extraídos que os novos Models devem replicar -->

De `NpsPerguntaCustomizada` (padrão pt-BR + $casts):
```php
protected $casts = [
    'opcoes'      => 'array',
    'obrigatorio' => 'bool',
    'ativa'       => 'bool',
    'ordem'       => 'int',
];
public const TIPOS = ['escala_1_5', 'texto', 'sim_nao', 'multipla'];
```

De `NpsRespostaCustomizada` (snapshot per-row com FK viva nullable):
```php
protected $fillable = ['response_id','pergunta_id','pergunta_texto_snapshot','tipo_snapshot','valor'];
public function pergunta(): BelongsTo { return $this->belongsTo(NpsPerguntaCustomizada::class, 'pergunta_id'); }
public function response(): BelongsTo { return $this->belongsTo(NpsResponse::class, 'response_id'); }
```

De `Servico` (padrão de constants + labels):
```php
public const SETOR_PERFORMANCE = 'performance';
public const SETORES = [self::SETOR_PERFORMANCE, ...];
public static function setoresLabels(): array { return [...]; }
```

De `NpsSurvey` (LogsActivity + $casts datetime — replicar em NpsTemplate):
```php
use LogsActivity;
public function getActivitylogOptions(): LogOptions { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4 modelos Eloquent novos + updates em NpsSurvey/NpsResponse</name>
  <files>
    app/Models/NpsTemplate.php,
    app/Models/NpsTemplateQuestion.php,
    app/Models/NpsTemplateOption.php,
    app/Models/NpsResponseAnswer.php,
    app/Models/NpsSurvey.php,
    app/Models/NpsResponse.php
  </files>
  <read_first>
    - app/Models/NpsSurvey.php (target do update — adicionar `template()` + `template_id` em $fillable)
    - app/Models/NpsResponse.php (target do update — adicionar `answers()` HasMany)
    - app/Models/NpsPerguntaCustomizada.php (padrão de $casts + $fillable + PHPDoc pt-BR + `const TIPOS`)
    - app/Models/NpsRespostaCustomizada.php (padrão snapshot per-row com FK viva nullable via BelongsTo)
    - app/Models/Servico.php (padrão SETORES const + label statics — replicar em NpsTemplateQuestion pra dimensões)
    - .planning/research/v15-nps-templates-schema.md §1 (colunas de `nps_response_answers`), §4 (`NpsTemplate::priority`, `is_default`, `serviceScopes`), §5 (tipo `escala`/`opcoes`)
  </read_first>
  <action>
    **NpsTemplate.php (novo):**
    - Namespace `App\Models`, `use Illuminate\Database\Eloquent\Model;` + `Spatie\Activitylog\{LogOptions, Traits\LogsActivity};`
    - Trait `LogsActivity` (é entidade admin-visible, faz sentido auditar mudanças — segue padrão `NpsSurvey`)
    - `$table = 'nps_templates'`
    - `$fillable`: `['nome', 'descricao', 'active', 'is_default', 'priority', 'envio_automatico_mensal']`
    - `$casts`: `['active' => 'bool', 'is_default' => 'bool', 'envio_automatico_mensal' => 'bool', 'priority' => 'int']`
    - `getActivitylogOptions(): LogOptions`: `logOnly(['nome','active','is_default','priority','envio_automatico_mensal'])->logOnlyDirty()->dontSubmitEmptyLogs()` com labels pt-BR (Template criado/atualizado/excluído)
    - Relacionamentos:
      - `questions(): HasMany` → `NpsTemplateQuestion::class`
      - `serviceScopes(): HasMany` → nova model `NpsTemplateServiceScope`? — **DECISÃO**: NÃO criar Model separado pro pivot; usar `belongsToMany(Servico::class, 'nps_template_service_scopes')->withTimestamps()` diretamente. Chamar de `servicos(): BelongsToMany`. E ADICIONAR SEGUNDO helper `serviceScopes(): HasMany` que retorna `hasMany(TemplateServiceScopeRelation::class)`? — **NÃO**, manter simples: só `servicos()` BelongsToMany + para o `resolveForCompany` (Phase 69) usar `whereHas('servicos', fn($q)=>$q->whereIn('id', $ids))`. Ver research §4 — o `whereHas('serviceScopes'...)` é apenas linguagem convencional; funcionalmente uma pivot BelongsToMany dá o mesmo resultado.
      - **REVISÃO PÓS-LEITURA DO RESEARCH §4:** research usa nome `serviceScopes` — vou expor AMBOS os nomes:
        - `servicos(): BelongsToMany` → `belongsToMany(Servico::class, 'nps_template_service_scopes')->withTimestamps()` (idiomático Laravel)
        - `serviceScopes(): BelongsToMany` → alias que retorna o mesmo (compat com research §4 e Phase 69). Documentar no PHPDoc que ambos apontam pro mesmo pivot.
      - `surveys(): HasMany` → `NpsSurvey::class, 'template_id'` (fk explícita)
    - PHPDoc pt-BR de classe explicando: template configurável de NPS (Phase 68 v15.0), `is_default=true` é o "NPS Padrão" seed pra fallback (research §4), `priority` DESC usado por `NpsTemplateService::resolveForCompany` (Phase 69).

    **NpsTemplateQuestion.php (novo):**
    - `$table = 'nps_template_questions'`
    - `$fillable`: `['template_id', 'texto', 'tipo', 'dimensao', 'obrigatoria', 'active', 'ordem']`
    - `$casts`: `['obrigatoria' => 'bool', 'active' => 'bool', 'ordem' => 'int']`
    - Constants:
      - `public const TIPO_ESCALA = 'escala';`
      - `public const TIPO_OPCOES = 'opcoes';`
      - `public const TIPOS = [self::TIPO_ESCALA, self::TIPO_OPCOES];` (Rule::in usa isso)
      - `public const DIMENSAO_ESTRATEGISTA = 'estrategista';`
      - `public const DIMENSAO_ANALISTA = 'analista';`
      - `public const DIMENSAO_EMPRESA = 'empresa';`
      - `public const DIMENSAO_GERAL = 'geral';`
      - `public const DIMENSOES = [...]`
      - `public static function dimensoesLabels(): array` — labels pt-BR pra UI admin (Phase 70)
    - Relacionamentos:
      - `template(): BelongsTo` → `NpsTemplate::class`
      - `options(): HasMany` → `NpsTemplateOption::class, 'template_question_id'` (ordenado por `->orderBy('ordem')`)
    - PHPDoc pt-BR referenciando research §1 (dimensao é a chave de agregação do `NpsScoreCalculator`) e §5 (tipo `escala` auto-gera 5 options).

    **NpsTemplateOption.php (novo):**
    - `$table = 'nps_template_options'`
    - `$fillable`: `['template_question_id', 'label', 'peso', 'ordem']`
    - `$casts`: `['peso' => 'int', 'ordem' => 'int']`
    - Relação: `question(): BelongsTo` → `NpsTemplateQuestion::class, 'template_question_id'`
    - PHPDoc pt-BR: `peso` é o valor 1..5 usado em `AVG(option_peso_snapshot)` do Calculator (Phase 69, research §5).

    **NpsResponseAnswer.php (novo):**
    - `$table = 'nps_response_answers'`
    - `$fillable`: `['response_id', 'template_question_id', 'template_option_id', 'question_texto_snapshot', 'question_dimensao_snapshot', 'option_label_snapshot', 'option_peso_snapshot', 'comentario']`
    - `$casts`: `['option_peso_snapshot' => 'int']`
    - Relacionamentos (todos BelongsTo):
      - `response(): BelongsTo` → `NpsResponse::class`
      - `templateQuestion(): BelongsTo` → `NpsTemplateQuestion::class, 'template_question_id'` (nullable — hard-delete da question tolerado; snapshot é a verdade)
      - `templateOption(): BelongsTo` → `NpsTemplateOption::class, 'template_option_id'` (nullable idem)
    - PHPDoc pt-BR: snapshot per-row congelado (research §1); FK viva é `nullable` e serve pra debug/UI mas snapshot é fonte de leitura pro `NpsScoreCalculator::compute()`. Replicar linguagem/estilo do PHPDoc de `NpsRespostaCustomizada`.

    **NpsSurvey.php (update):**
    - Adicionar `'template_id'` ao `$fillable` (após `auto_generated`)
    - Adicionar método `template(): BelongsTo` → `belongsTo(NpsTemplate::class, 'template_id')`
    - Adicionar método `answers()` NÃO — respostas viveem em NpsResponse, não Survey.
    - Manter `LogsActivity`, `logOnly` atualizar pra incluir `template_id`.
    - NÃO alterar comportamento existente (relação `response()` HasOne, `isExpired()`).

    **NpsResponse.php (update):**
    - Adicionar método `answers(): HasMany` → `hasMany(NpsResponseAnswer::class, 'response_id')`
    - $fillable e $casts existentes permanecem intactos (score_* seguem como integer no cast — Eloquent trata NULL corretamente)
    - PHPDoc atualizar mencionando que a partir de v15.0 (Phase 68) as respostas dinâmicas vivem em `nps_response_answers` (novos) e as colunas score_* legadas continuam sendo populadas pra rows criadas antes da Phase 69 (compat com dashboards).

    Todos os arquivos: PHPDoc pt-BR, `use` statements alfabéticos, 4 espaços de indentação (padrão PSR-12 + convenção do projeto).
  </action>
  <verify>
    <automated>php -l app/Models/NpsTemplate.php && php -l app/Models/NpsTemplateQuestion.php && php -l app/Models/NpsTemplateOption.php && php -l app/Models/NpsResponseAnswer.php && php -l app/Models/NpsSurvey.php && php -l app/Models/NpsResponse.php</automated>
  </verify>
  <acceptance_criteria>
    - Todos 6 arquivos existem e passam `php -l`
    - `grep -c "hasMany.*NpsTemplateQuestion" app/Models/NpsTemplate.php` retorna 1
    - `grep -c "belongsToMany.*Servico.*nps_template_service_scopes" app/Models/NpsTemplate.php` retorna 1 (ou mais se houver alias `serviceScopes`)
    - `grep -c "hasMany.*NpsTemplateOption" app/Models/NpsTemplateQuestion.php` retorna 1
    - `grep -c "belongsTo.*NpsTemplate" app/Models/NpsSurvey.php` retorna 1
    - `grep -c "hasMany.*NpsResponseAnswer" app/Models/NpsResponse.php` retorna 1
    - `grep -q "template_id" app/Models/NpsSurvey.php` (fillable atualizado)
    - `grep -q "TIPO_ESCALA" app/Models/NpsTemplateQuestion.php` (constant)
    - `grep -q "DIMENSAO_ESTRATEGISTA" app/Models/NpsTemplateQuestion.php`
    - `grep -qE "question_texto_snapshot|option_peso_snapshot" app/Models/NpsResponseAnswer.php` ($fillable com snapshot cols)
    - Executar `php artisan tinker --execute="app(App\Models\NpsTemplate::class); echo 'OK';"` retorna "OK" (autoload verde)
    - Executar `php artisan tinker --execute="(new App\Models\NpsTemplate)->questions(); echo 'OK';"` retorna "OK" (relation chama sem erro)
    - Baseline: `php artisan test --filter=NpsFluxoTest` (Phase 31, se existe) continua verde
  </acceptance_criteria>
  <done>
    4 modelos novos + 2 atualizados. Relationships, casts, constants presentes. PHPDoc pt-BR consistente com padrão do projeto. Zero regressão em testes existentes.
  </done>
</task>

<task type="auto">
  <name>Task 2: 4 Factories pra suporte a testes (Phase 68-05 + Phases 69-73)</name>
  <files>
    database/factories/NpsTemplateFactory.php,
    database/factories/NpsTemplateQuestionFactory.php,
    database/factories/NpsTemplateOptionFactory.php,
    database/factories/NpsResponseAnswerFactory.php
  </files>
  <read_first>
    - database/factories/CompanyFactory.php (se existe — padrão de factory do projeto)
    - app/Models/NpsTemplate.php (do Task 1 — os factories precisam bater com $fillable)
    - app/Models/NpsTemplateQuestion.php (Task 1)
    - Documentação Laravel 12 Factory pattern (definição via `define(fn () => [...])`)
  </read_first>
  <action>
    Criar 4 factories em `database/factories/`:

    **NpsTemplateFactory.php:**
    - Namespace `Database\Factories`
    - Extends `Illuminate\Database\Eloquent\Factories\Factory`
    - Protected `$model = \App\Models\NpsTemplate::class`
    - `definition()`: retorna array com defaults sanos:
      ```php
      return [
          'nome' => $this->faker->words(3, true),
          'descricao' => $this->faker->optional(0.5)->sentence(),
          'active' => true,
          'is_default' => false, // Se precisar default, usar o seed real do Plan 68-03 (única row)
          'priority' => 0,
          'envio_automatico_mensal' => false,
      ];
      ```
    - Adicionar `NpsTemplate::factory()->has(NpsTemplateQuestion::factory()->count(3))` capabilty via `HasFactory` trait (já garantido pelo Model).

    **NpsTemplateQuestionFactory.php:**
    - `$model = \App\Models\NpsTemplateQuestion::class`
    - `definition()`:
      ```php
      return [
          'template_id' => \App\Models\NpsTemplate::factory(),
          'texto' => $this->faker->sentence(6),
          'tipo' => \App\Models\NpsTemplateQuestion::TIPO_ESCALA,
          'dimensao' => $this->faker->randomElement(NpsTemplateQuestion::DIMENSOES),
          'obrigatoria' => false,
          'active' => true,
          'ordem' => 0,
      ];
      ```

    **NpsTemplateOptionFactory.php:**
    - `$model = \App\Models\NpsTemplateOption::class`
    - `definition()`:
      ```php
      return [
          'template_question_id' => \App\Models\NpsTemplateQuestion::factory(),
          'label' => (string) $this->faker->numberBetween(1, 5),
          'peso' => $this->faker->numberBetween(1, 5),
          'ordem' => 0,
      ];
      ```

    **NpsResponseAnswerFactory.php:**
    - `$model = \App\Models\NpsResponseAnswer::class`
    - `definition()`:
      ```php
      return [
          'response_id' => \App\Models\NpsResponse::factory(),
          'template_question_id' => null,
          'template_option_id' => null,
          'question_texto_snapshot' => $this->faker->sentence(),
          'question_dimensao_snapshot' => 'empresa',
          'option_label_snapshot' => '4',
          'option_peso_snapshot' => 4,
          'comentario' => null,
      ];
      ```
    - Comentário pt-BR: snapshot columns são intencionalmente strings/int hardcoded — teste que precisar FK viva usa `->for($question, 'templateQuestion')`

    **Se `NpsResponse::factory()` NÃO EXISTE** hoje (verificar): criar quick check em `database/factories/NpsResponseFactory.php` — se não existir, criar minimal pra suportar `NpsResponseAnswerFactory` (só necessário se Phase 31 não tiver deixado factory pronto).

    Adicionar `use HasFactory;` em cada Model criado no Task 1 (verificar; NpsTemplate/Question/Option/Answer já devem ter porque Task 1 vai criar assim — se não, adicionar).
  </action>
  <verify>
    <automated>php -l database/factories/NpsTemplateFactory.php && php -l database/factories/NpsTemplateQuestionFactory.php && php -l database/factories/NpsTemplateOptionFactory.php && php -l database/factories/NpsResponseAnswerFactory.php</automated>
  </verify>
  <acceptance_criteria>
    - Todos 4 arquivos de factory existem e passam `php -l`
    - `grep -q "NpsTemplate::class" database/factories/NpsTemplateFactory.php`
    - `grep -q "NpsTemplateQuestion::class" database/factories/NpsTemplateQuestionFactory.php`
    - `grep -q "NpsTemplateOption::class" database/factories/NpsTemplateOptionFactory.php`
    - `grep -q "NpsResponseAnswer::class" database/factories/NpsResponseAnswerFactory.php`
    - Cada Model tem `use HasFactory;` (validar via `grep -l "HasFactory" app/Models/NpsTemplate*.php app/Models/NpsResponseAnswer.php` retorna todos 4)
    - Sanity via tinker: `\App\Models\NpsTemplate::factory()->make()->nome` retorna string não vazia
    - Sanity: em SQLite in-memory após `migrate:fresh`, `\App\Models\NpsTemplate::factory()->create()` insere row sem erro
    - Sanity end-to-end: `\App\Models\NpsTemplate::factory()->has(\App\Models\NpsTemplateQuestion::factory()->count(2)->has(\App\Models\NpsTemplateOption::factory()->count(5), 'options'), 'questions')->create()` cria 1 template + 2 perguntas + 10 opções sem erro
  </acceptance_criteria>
  <done>
    4 factories funcionais permitem criar templates+perguntas+opções+answers em testes. `HasFactory` trait presente nos 4 Models novos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model $fillable | Mass assignment via `NpsTemplate::create($request->all())` — Phase 70 CRUD vai depender disso |
| Snapshot vs FK | `NpsResponseAnswer` tem FKs `template_question_id`/`template_option_id` nullable — teste pode passar FK inválida por engano se não usar factory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02-01 | Elevation of Privilege | Mass assignment em NpsTemplate expõe campos internos (`is_default`) | mitigate | `$fillable` explícito lista campos permitidos; Phase 70 vai validar `is_default` via policy admin |
| T-68-02-02 | Tampering | Alteração acidental de snapshot columns via factory sem valores explícitos | accept | Factories usam defaults sanos + testes explicitam valores esperados quando testam snapshot |
| T-68-02-03 | Information Disclosure | LogsActivity em NpsTemplate expõe mudanças de config | accept | Fluxo intencional pra auditoria admin; sem PII do cliente |
</threat_model>

<verification>
- 6 arquivos Model + 4 factories criados/atualizados
- Sanity de relações via tinker: `NpsTemplate::factory()->has(NpsTemplateQuestion::factory()->count(2), 'questions')->create()` funciona
- Todas as relações bidirecionais respondem: `$template->questions->first()->template->is($template)` retorna true
- `NpsSurvey::first()->template` retorna null quando `template_id` é NULL (não crasha)
- Baseline zero regressão: `php artisan test --filter=Nps` continua verde
</verification>

<success_criteria>
- 4 Models novos + 2 Models atualizados + 4 Factories
- Relationships funcionam: `template->questions`, `question->options`, `question->template`, `option->question`, `response->answers`, `answer->response`, `answer->templateQuestion`, `answer->templateOption`, `survey->template`
- Casts booleanos retornam PHP bool (não int)
- Constants (`TIPOS`, `DIMENSOES`) presentes em `NpsTemplateQuestion`
- Factories permitem chain `has()->count()` pra testes complexos
- REQ NPS-A-02 atendido
- Zero regressão em suite Nps existente
</success_criteria>

<output>
Criar `.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/68-02-SUMMARY.md` quando terminar.

**Não deployar sem autorização explícita.**
</output>
