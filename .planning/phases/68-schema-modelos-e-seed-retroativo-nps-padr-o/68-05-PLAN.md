---
phase: 68-schema-modelos-e-seed-retroativo-nps-padr-o
plan: 05
type: execute
wave: 4
depends_on: [68-01, 68-02, 68-03, 68-04]
files_modified:
  - tests/Feature/Phase68/NpsSchemaTest.php
  - tests/Feature/Phase68/NpsSeedRetroactiveTest.php
  - tests/Feature/Phase68/NpsBackwardCompatTest.php
autonomous: true
requirements: [NPS-A-01, NPS-A-02, NPS-A-03, NPS-A-04]

must_haves:
  truths:
    - "3 arquivos de teste Feature existem em `tests/Feature/Phase68/`"
    - "`NpsSchemaTest`: valida que as 5 tabelas novas + coluna `template_id` + coluna virtual `dedup_key` (ou índice partial em SQLite) existem; valida FKs cascade/nullOnDelete comportam-se corretamente"
    - "`NpsSeedRetroactiveTest`: valida que o template 'NPS Padrão' foi seedado com 3 perguntas + 15 opções; valida que surveys criadas ANTES da migration são retro-associadas (100%); valida idempotência (rodar seed 2x não duplica)"
    - "`NpsBackwardCompatTest`: valida que dashboards atuais não quebram após migration — rota `/nps` renderiza; `PerformanceController@dashboard` responde 200; `AVG(score_estrategista)` em query legada ainda retorna resultado numérico pras rows históricas"
    - "Todos os testes usam `RefreshDatabase` (SQLite in-memory) — isolados, rápidos, sem efeito colateral"
    - "Suite Phase 68 completa (3 arquivos) roda em < 15s"
    - "Zero regressão: `php artisan test --filter=Nps` (todos os testes NPS anteriores — Phase 31, 32, 33) continua com o mesmo count e status"
  artifacts:
    - path: "tests/Feature/Phase68/NpsSchemaTest.php"
      provides: "Valida schema físico das 5 tabelas + FKs + índices"
      min_lines: 100
    - path: "tests/Feature/Phase68/NpsSeedRetroactiveTest.php"
      provides: "Valida seed + retro-associação + idempotência + snapshot per-row congelado"
      min_lines: 100
    - path: "tests/Feature/Phase68/NpsBackwardCompatTest.php"
      provides: "Valida dashboards atuais não quebram após migration"
      min_lines: 80
  key_links:
    - from: "NpsSchemaTest"
      to: "5 tabelas novas"
      via: "Schema::hasTable + hasColumns"
      pattern: "assertTrue.*Schema::hasTable"
    - from: "NpsSeedRetroactiveTest"
      to: "NpsSurvey retro-associação"
      via: "assertDatabaseHas nps_surveys template_id=<padrao>"
      pattern: "template_id.*=.*"
    - from: "NpsBackwardCompatTest"
      to: "PerformanceController::dashboard rota"
      via: "Http GET /desempenho"
      pattern: "->get\\('/desempenho"
---

<objective>
Escrever 3 arquivos de teste Feature que provam observavelmente os 5 Success Criteria do ROADMAP Phase 68:

1. `NpsSchemaTest` — SC1 (5 tabelas com FKs/índices/constraints) + SC4 (snapshot per-row)
2. `NpsSeedRetroactiveTest` — SC3 (seed + retro-associação 100%) + idempotência + snapshot congelado após template mudar
3. `NpsBackwardCompatTest` — SC5 (dashboards existentes NPS mensal + `Performance/Dashboard.jsx` continuam funcionando)

SC2 (Modelos Eloquent + relationships) é validado indiretamente em todos os 3 testes via uso das factories e relations.

**Propósito:** rede de segurança observável — antes de qualquer plan da Phase 69 tocar código, esta suite prova que a fundação está sólida. Testes são pequenos, rápidos, focados em contrato observável (não implementação interna).

**Output:** 3 arquivos em `tests/Feature/Phase68/`. Todos usam `RefreshDatabase` + factories do Plan 68-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/v15-nps-templates-schema.md
@CLAUDE.md

<!-- Padrão de testes Feature do projeto (Phase 39/40 recente) -->
<!-- Grep por tests/Feature/Phase*/*Test.php pra ver padrão de #[Test] attribute vs docblock -->

@app/Models/NpsTemplate.php
@app/Models/NpsTemplateQuestion.php
@app/Models/NpsTemplateOption.php
@app/Models/NpsResponseAnswer.php
@app/Models/NpsSurvey.php
@app/Models/NpsResponse.php

@database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php
@database/migrations/2026_07_07_100002_alter_nps_surveys_add_template_id.php
@database/migrations/2026_07_07_100003_alter_nps_responses_scores_nullable.php
@database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php
@database/migrations/2026_07_07_100005_add_dedup_key_to_nps_surveys.php

<interfaces>
<!-- PHPUnit 11.x — usar attribute #[Test] em vez de docblock @test (padrão do projeto pós-Phase 40) -->

Padrão de teste do projeto:
```php
<?php
namespace Tests\Feature\Phase68;

use Illuminate\Foundation\Testing\RefreshDatabase;
use PHPUnit\Framework\Attributes\Test;
use Tests\TestCase;

class NpsSchemaTest extends TestCase
{
    use RefreshDatabase;

    #[Test]
    public function it_creates_5_tables(): void
    {
        // ...
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NpsSchemaTest — valida as 5 tabelas + FKs + snapshot cols + dedup</name>
  <files>tests/Feature/Phase68/NpsSchemaTest.php</files>
  <read_first>
    - .planning/research/v15-nps-templates-schema.md §1 (colunas exatas de `nps_response_answers` + índice `(response_id, question_dimensao_snapshot)`) e §2 (dedup_key)
    - database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php (schema criado)
    - database/migrations/2026_07_07_100005_add_dedup_key_to_nps_surveys.php (dedup)
  </read_first>
  <action>
    Criar `tests/Feature/Phase68/NpsSchemaTest.php` com trait `RefreshDatabase` e attribute `#[Test]` em cada método (padrão PHPUnit 11.x per Phase 40).

    **Testes obrigatórios (mínimo 7):**

    1. `test_it_creates_5_new_tables()` — asserta:
       - `Schema::hasTable('nps_templates')` true
       - `Schema::hasTable('nps_template_questions')` true
       - `Schema::hasTable('nps_template_options')` true
       - `Schema::hasTable('nps_template_service_scopes')` true
       - `Schema::hasTable('nps_response_answers')` true

    2. `test_nps_templates_has_expected_columns()` — asserta:
       - `Schema::hasColumns('nps_templates', ['id','nome','descricao','active','is_default','priority','envio_automatico_mensal','created_at','updated_at'])` true

    3. `test_nps_template_questions_has_expected_columns_and_enum_dimensao()` — asserta:
       - `Schema::hasColumns('nps_template_questions', ['id','template_id','texto','tipo','dimensao','obrigatoria','active','ordem'])` true
       - Insere pergunta com dimensao='geral' → sucesso
       - Insere pergunta com dimensao='invalida' → falha (`QueryException` em SQLite via check constraint OU aceito silenciosamente — validar comportamento real; se SQLite aceita, ok, o Model tem const `DIMENSOES` pra validação a nível de app)

    4. `test_nps_response_answers_has_snapshot_columns_and_index()` — asserta:
       - `Schema::hasColumns('nps_response_answers', ['question_texto_snapshot','question_dimensao_snapshot','option_label_snapshot','option_peso_snapshot','comentario'])` true
       - Em SQLite, `DB::selectOne("SELECT name FROM sqlite_master WHERE type='index' AND name='nps_ans_response_dim_idx'")` retorna row (índice existe)

    5. `test_fk_cascade_response_id()` — asserta:
       - Cria `NpsTemplate`, `NpsTemplateQuestion`, `NpsTemplateOption` via factory
       - Cria `NpsResponse` + 3 `NpsResponseAnswer` linkados
       - `NpsResponseAnswer::count()` retorna 3
       - Deleta `NpsResponse` → `NpsResponseAnswer::count()` retorna 0 (cascade funcionou)

    6. `test_fk_nullondelete_template_question_id()` — asserta:
       - Cria template + question + option + response + answer (linkado a question)
       - `NpsResponseAnswer::first()->template_question_id` NÃO é null
       - Deleta `NpsTemplateQuestion` → recarrega o `NpsResponseAnswer`
       - `NpsResponseAnswer::first()->template_question_id` é null (nullOnDelete funcionou)
       - Snapshot column `question_texto_snapshot` continua com o texto original (defesa contra hard-delete — research §1)

    7. `test_nps_surveys_has_template_id_fk_nullable()` — asserta:
       - `Schema::hasColumn('nps_surveys', 'template_id')` true
       - Inserir `NpsSurvey` com `template_id = null` NÃO gera erro
       - Deletar o template referenciado NÃO deleta a survey (nullOnDelete) — a survey persiste com `template_id = null`

    8. `test_dedup_partial_unique_index_blocks_duplicate_completed()` — asserta (SQLite):
       - Setup: criar template + company via factory
       - Criar survey #1: `NpsSurvey::factory()->create(['status'=>'completed','completed_at'=>now(),'month_reference'=>'2026-07-01','template_id'=>$template->id,'company_id'=>$company->id])`
       - Tentar criar survey #2 com valores idênticos → `expectException(\Illuminate\Database\QueryException::class)` — SQLSTATE 23xxx
       - **Sanity NÃO-bloqueio pending**: criar survey #3 pending com mesmos valores (status='pending', completed_at=null) NÃO gera exceção
       - **Sanity NÃO-bloqueio template diferente**: criar survey #4 completed com template diferente → NÃO gera exceção

    **Diretrizes:**
    - Cada teste ~10-25 linhas — foco em contrato observável
    - Namespace `Tests\Feature\Phase68`
    - `use Illuminate\Foundation\Testing\RefreshDatabase;`
    - `use PHPUnit\Framework\Attributes\Test;`
    - `use Illuminate\Support\Facades\Schema;` `use Illuminate\Support\Facades\DB;`
    - Attributes `#[Test]` — NUNCA docblock `/** @test */` (PHPUnit 11.x warning per Phase 40)
    - Comentários pt-BR em decisões-chave (por que `expectException` em vez de try/catch, etc)
  </action>
  <verify>
    <automated>php artisan test --filter=NpsSchemaTest --env=testing 2>&1 | grep -qE "(OK|PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase68/NpsSchemaTest.php` retorna 0
    - Arquivo passa `php -l`
    - `grep -c "#\[Test\]" tests/Feature/Phase68/NpsSchemaTest.php` retorna >= 8
    - `grep -c "public function test_" tests/Feature/Phase68/NpsSchemaTest.php` retorna >= 8 (ou métodos com `#[Test]`)
    - `grep -q "RefreshDatabase" tests/Feature/Phase68/NpsSchemaTest.php`
    - `php artisan test --filter=NpsSchemaTest --env=testing` — 8/8 verdes (ou mais)
    - Tempo total < 5s
  </acceptance_criteria>
  <done>
    NpsSchemaTest com 8+ testes verdes. Valida schema físico, FKs, snapshot cols, índices, e dedup unique.
  </done>
</task>

<task type="auto">
  <name>Task 2: NpsSeedRetroactiveTest — valida seed + retro-associação + idempotência</name>
  <files>tests/Feature/Phase68/NpsSeedRetroactiveTest.php</files>
  <read_first>
    - .planning/research/v15-nps-templates-schema.md §4 (`priority` DESC + `is_default` fallback)
    - database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php (comportamento do seed)
    - app/Models/NpsTemplate.php + NpsTemplateQuestion.php + NpsTemplateOption.php (Models pra assertions)
  </read_first>
  <action>
    Criar `tests/Feature/Phase68/NpsSeedRetroactiveTest.php`.

    **Testes obrigatórios (mínimo 6):**

    1. `test_seed_creates_nps_padrao_template_with_is_default_true()` — asserta:
       - Após `migrate:fresh` (RefreshDatabase já faz isso), `NpsTemplate::where('is_default', true)->count()` retorna 1
       - `NpsTemplate::where('is_default', true)->first()->nome === 'NPS Padrão'`
       - `->active === true`, `->priority === 0`

    2. `test_seed_creates_3_questions_with_correct_dimensoes()` — asserta:
       - `$padrao = NpsTemplate::where('is_default', true)->first()`
       - `$padrao->questions()->count() === 3`
       - Dimensões presentes: `$padrao->questions->pluck('dimensao')->sort()->values()->all() === ['analista','empresa','estrategista']`
       - Pergunta 'estrategista' tem `obrigatoria=true`
       - Pergunta 'analista' tem `obrigatoria=false`
       - Pergunta 'empresa' tem `obrigatoria=true`
       - Todas com `tipo='escala'` e `active=true`

    3. `test_seed_creates_5_options_per_question_pesos_1_to_5()` — asserta:
       - `$padrao->questions()->with('options')->get()` — pra cada pergunta:
         - `options()->count() === 5`
         - `options()->pluck('peso')->sort()->values()->all() === [1,2,3,4,5]`
         - `options()->pluck('label')->sort()->values()->all() === ['1','2','3','4','5']`

    4. `test_retro_associates_legacy_surveys_to_padrao()` — asserta:
       - **Setup complexo**: precisa criar surveys ANTES do seed rodar. Estratégia: usar `RefreshDatabase` cria schema + roda seed automaticamente. Pra simular "legado", INSERTAR surveys manualmente com `DB::table('nps_surveys')->insert([...])` deixando `template_id=NULL` e depois chamar `Artisan::call('migrate:refresh', ['--path' => '/database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php'])` — MAS isso é frágil.
       - **Estratégia mais simples**: sobrescrever `template_id` das surveys criadas após seed (setUp faz migrate:fresh que roda seed → todas surveys criadas depois teriam template_id NULL apenas se factory NÃO setar template_id). CONFIRMAR: o factory de `NpsSurvey` NÃO seta template_id por default (Plan 68-02 factory decisão). 
       - **Fluxo real do teste**:
         1. `Company::factory()->count(3)->create()` — 3 companies
         2. `DB::table('nps_surveys')->insert([...])` — 5 surveys com `template_id=null` (bypass Model pra simular INSERT direto legado)
         3. Rodar `Artisan::call('migrate:refresh', ['--path' => '/database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php'])` OU rodar direto a migration por instância
         4. Assert: `NpsSurvey::whereNull('template_id')->count() === 0`
         5. Assert: `NpsSurvey::where('template_id', $padrao->id)->count() === 5`

       Adicionar comentário pt-BR: "Simula surveys criadas em produção ANTES da migration da Phase 68 rodar. Retro-associação deve popular template_id=<padrao> em 100%."

    5. `test_seed_is_idempotent_no_duplicates_on_rerun()` — asserta:
       - `$countAntes = NpsTemplate::where('is_default', true)->count()` (esperado 1)
       - Rodar `Artisan::call('migrate:refresh', ['--path' => '/database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php'])` novamente
       - `$countDepois = NpsTemplate::where('is_default', true)->count()` — asserta `=== 1`
       - `NpsTemplateQuestion::where('template_id', $padrao->id)->count() === 3`
       - `NpsTemplateOption::whereIn('template_question_id', $padrao->questions->pluck('id'))->count() === 15`

    6. `test_snapshot_freeze_survives_template_edit()` — asserta (fecha SC4 do ROADMAP + REQ NPS-A-04):
       - Cria template padrão via seed (já feito no setUp)
       - Cria uma `NpsResponse` linkada a uma `NpsSurvey`, e um `NpsResponseAnswer` com `question_texto_snapshot='Texto ORIGINAL'`, `option_peso_snapshot=4`, `question_dimensao_snapshot='empresa'`
       - Edita a pergunta original: `$question->update(['texto' => 'Texto EDITADO'])` (simula admin editando texto)
       - Recarrega o `NpsResponseAnswer`: `->fresh()`
       - Assert: `->question_texto_snapshot === 'Texto ORIGINAL'` (snapshot IMUTÁVEL)
       - Assert: `->option_peso_snapshot === 4` (peso congelado)
       - Assert: `->question_dimensao_snapshot === 'empresa'` (dimensão congelada)
       - Extra: mesma coisa com deletar option — snapshot survive

    **Diretrizes:**
    - Namespace + imports iguais ao Task 1
    - Comentários pt-BR em setUp complexo
    - Comentários pt-BR em cada assertion não-óbvia
  </action>
  <verify>
    <automated>php artisan test --filter=NpsSeedRetroactiveTest --env=testing 2>&1 | grep -qE "(OK|PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase68/NpsSeedRetroactiveTest.php` retorna 0
    - Arquivo passa `php -l`
    - `grep -c "#\[Test\]" tests/Feature/Phase68/NpsSeedRetroactiveTest.php` retorna >= 6
    - `php artisan test --filter=NpsSeedRetroactiveTest --env=testing` — 6/6 verdes
    - `grep -q "is_default" tests/Feature/Phase68/NpsSeedRetroactiveTest.php`
    - `grep -q "question_texto_snapshot" tests/Feature/Phase68/NpsSeedRetroactiveTest.php` (test snapshot freeze)
    - `grep -q "migrate:refresh" tests/Feature/Phase68/NpsSeedRetroactiveTest.php` (idempotência)
    - Tempo total < 5s
  </acceptance_criteria>
  <done>
    NpsSeedRetroactiveTest com 6+ testes verdes. Valida seed correto + retro-associação 100% + idempotência + snapshot congelado.
  </done>
</task>

<task type="auto">
  <name>Task 3: NpsBackwardCompatTest — dashboards existentes não quebram</name>
  <files>tests/Feature/Phase68/NpsBackwardCompatTest.php</files>
  <read_first>
    - app/Http/Controllers/NpsController.php (rota atual `/nps` — precisa continuar renderizando)
    - app/Http/Controllers/PerformanceController.php linha ~301 (uso legado de `score_estrategista/analista/empresa` — precisa continuar funcionando)
    - app/Http/Controllers/CompanyController.php (uso de `score_*` no payload de `/companies/{id}`)
    - resources/js/Pages/Companies/Show.jsx (consome `nps_surveys`)
    - routes/web.php (nomes de rota — `nps.index`, `performance.dashboard`, etc)
  </read_first>
  <action>
    Criar `tests/Feature/Phase68/NpsBackwardCompatTest.php` — foco em SC5 do ROADMAP: **dashboards existentes NÃO quebram após migration**.

    **Testes obrigatórios (mínimo 5):**

    1. `test_nps_index_route_renders_after_migration()` — asserta:
       - Setup: cria `User` admin, login via `$this->actingAs($user)`
       - `$response = $this->get('/nps')` → status 200
       - Ou usar rota nomeada: `$this->get(route('nps.index'))` se existir
       - Assertion: response é 200 e é Inertia render (não crash 500)
       - Se a rota exigir outro role/permissão, ajustar setup do user

    2. `test_performance_dashboard_route_renders_after_migration()` — asserta:
       - Login user consultor/mentor
       - `$this->get(route('performance.dashboard'))` (ou `/desempenho`) → 200
       - Response é Inertia (não crash)
       - Comentário pt-BR: "Fase 68 não altera cálculo de PerformanceController — dashboards legados devem continuar funcionando com colunas score_* legadas nas rows históricas"

    3. `test_companies_show_route_still_exposes_nps_surveys_payload()` — asserta:
       - Cria company + user com relação
       - Login admin
       - Cria 1 `NpsSurvey::factory()` + `NpsResponse::factory()` com scores legados populados (score_estrategista=4, score_analista=5, score_empresa=4)
       - `$this->get("/companies/{$company->id}")` → 200
       - Assertion: response Inertia contém prop `company` OU `nps_surveys` OU whichever chave é a atual (grep no CompanyController)
       - Se `nps_surveys` é embarcado no payload, verificar contagem >= 1

    4. `test_legacy_avg_score_query_still_works_on_historic_rows()` — asserta:
       - Setup: cria template padrão via seed (já done no setUp)
       - Cria 3 `NpsResponse` com `score_estrategista=[3,4,5]` (não null — rows históricas mantêm valores)
       - `$avg = DB::table('nps_responses')->whereNotNull('score_estrategista')->avg('score_estrategista')`
       - Assert: `$avg` é float ~ 4.0
       - Comentário pt-BR: "Rows legadas mantêm score_* populados. Novas rows Phase 69 gravam null e calculam via NpsResponseAnswer. Cálculo dinâmico entra na Phase 69."

    5. `test_nps_columns_score_are_nullable_after_migration()` — asserta:
       - Após migrations rodarem, criar `NpsResponse::create([..., 'score_estrategista' => null, 'score_empresa' => null, 'score_analista' => null])`
       - Não gera exceção
       - Row persiste com `score_estrategista=null`
       - Comentário pt-BR: "Confirma NPS-A-01 parte 2 — score_* NULLABLE pra suportar novas responses da Phase 69 sem quebrar rows legadas."

    **Estratégia se rotas exigirem middleware complexo:**
    - Se rota `/desempenho` exige permissão ou dados de company/portfolio complexos, mockar o mínimo pra chegar em 200 sem erro. Foco é: dashboard NÃO CRASHA por causa de mudança de schema Phase 68.
    - Se dificuldade excessiva, degradar teste 2 pra: `route('performance.dashboard')` está declarada (Router::has) — smoke test mínimo. Documentar decisão no comentário.

    **Diretrizes:**
    - Namespace + imports padrão
    - `RefreshDatabase` trait — cada teste independe
    - Comentários pt-BR explicando por que cada rota é crítica
    - Todos os testes usam `$this->actingAs(User::factory()->create(['role'=>'admin']))` (ou role apropriado)
  </action>
  <verify>
    <automated>php artisan test --filter=NpsBackwardCompatTest --env=testing 2>&1 | grep -qE "(OK|PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase68/NpsBackwardCompatTest.php` retorna 0
    - Arquivo passa `php -l`
    - `grep -c "#\[Test\]" tests/Feature/Phase68/NpsBackwardCompatTest.php` retorna >= 5
    - `php artisan test --filter=NpsBackwardCompatTest --env=testing` — 5/5 verdes
    - `grep -q "score_estrategista" tests/Feature/Phase68/NpsBackwardCompatTest.php` (nullable test)
    - `grep -qE "get\\('/(nps|desempenho|companies)" tests/Feature/Phase68/NpsBackwardCompatTest.php` (smoke rotas)
    - Tempo total < 10s
    - **Gate final**: `php artisan test --filter=Phase68` — TODOS (Task 1 + 2 + 3 combinados) verdes; tempo total < 15s
    - **Gate zero regressão**: `php artisan test --filter=Nps` (todos os testes NPS — Phase 31/32/33 + Phase 68) — mesmo count e status que baseline pré-Phase 68
  </acceptance_criteria>
  <done>
    NpsBackwardCompatTest com 5+ testes verdes. Rotas legadas renderizando. `score_*` nullable validado. Gate final Phase 68 completo verde. Zero regressão em suite NPS legada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Teste automatizado | Único mecanismo observável de gate; se testes forem afrouxados, regressão passa despercebida |
| Rota HTTP | Testes fazem GET real nas rotas críticas — cobre integração Inertia/Controller/Model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-05-01 | Tampering | Teste que testa apenas "sem crash" mascara regressão comportamental | mitigate | Testes de dashboard verificam status 200 + presença de props Inertia esperadas; não apenas "não crashou" |
| T-68-05-02 | Denial of Service | Suite muito lenta desincentiva rodar em CI | mitigate | Alvo < 15s pra Phase 68 total; RefreshDatabase + SQLite in-memory |
| T-68-05-03 | Repudiation | Testes verdes com asserts inúteis | mitigate | Cada `#[Test]` tem assert observável (contagem, valor exato, exceção esperada) — não `assertTrue(true)` |
</threat_model>

<verification>
- 3 arquivos de teste criados em `tests/Feature/Phase68/`
- Contagem total: >= 19 testes verdes (8 schema + 6 seed + 5 backward-compat)
- `php artisan test --filter=Phase68` retorna todos verdes em < 15s
- Gate zero regressão: `php artisan test --filter=Nps` mostra mesmo count/status que pré-Phase 68 baseline
- Se algum teste do Phase 31/32/33 quebrar após aplicar migrations Phase 68, o executor precisa investigar (não afrouxar teste)
</verification>

<success_criteria>
- 3 arquivos de teste Feature em `tests/Feature/Phase68/`
- `NpsSchemaTest`: 8+ testes verdes (schema, FKs, índices, snapshot, dedup)
- `NpsSeedRetroactiveTest`: 6+ testes verdes (seed padrão, 3 perguntas + 15 opções, retro-associação 100%, idempotência, snapshot freeze)
- `NpsBackwardCompatTest`: 5+ testes verdes (rotas legadas 200, colunas score_* nullable, AVG legado funciona)
- Suite Phase 68 total < 15s
- Zero regressão em `php artisan test --filter=Nps`
- REQ NPS-A-01/02/03/04 validados observavelmente
</success_criteria>

<output>
Criar `.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/68-05-SUMMARY.md` quando terminar.

**Não deployar sem autorização explícita.**

Ao final desta plan, a Phase 68 estará FECHADA — todas as 5 SC do ROADMAP validadas observavelmente por teste automatizado.
</output>
