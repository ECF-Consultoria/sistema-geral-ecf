---
phase: 95-nps-anti-burlamento-ui-de-confian-a-admin-only
plan: 02
type: execute
wave: 2
depends_on: [95-01]
files_modified:
  - resources/js/Pages/Nps/Index.jsx
autonomous: false
requirements: [AB-95-1, AB-95-2, AB-95-3]
tags: [react, inertia, nps, anti-burlamento, badge, dark-theme]

must_haves:
  truths:
    - "Admin vê badge tri-estado ('Confiável' verde / 'Atenção' amarelo / 'Suspeita' vermelho) na linha de cada NPS respondido"
    - "Admin vê o filtro 'Todos / Confiáveis / Com alerta / Suspeitos' na barra de filtros e ele dispara o query param server-side"
    - "Admin vê a seção 'Auditoria' dentro do modal 'Ver respostas' com os 12 campos em linguagem pt-BR clara (zero jargão cru)"
    - "Não-admin vê a tela byte-idêntica à de hoje — sem coluna, badge, filtro ou seção nova (o dado nem chega do servidor)"
    - "`npm run build` conclui com exit 0 (nenhum ReferenceError de escopo do Rollup em produção)"
  artifacts:
    - path: "resources/js/Pages/Nps/Index.jsx"
      provides: "ConfiancaBadge + GlassSelect de filtro de confiança + seção Auditoria no Dialog modalSurvey"
      contains: "ConfiancaBadge"
  key_links:
    - from: "resources/js/Pages/Nps/Index.jsx"
      to: "item.confianca (payload do plano 95-01)"
      via: "renderização guardada por existência da chave (s.confianca && ...) — nunca por isAdmin sobre dado já entregue"
      pattern: "confianca"
    - from: "resources/js/Pages/Nps/Index.jsx"
      to: "GET /nps?confianca="
      via: "handleConfiancaChange → aplicarFiltros → router.get(route('nps.index'))"
      pattern: "handleConfiancaChange"
    - from: "resources/js/Pages/Nps/Index.jsx"
      to: "modalSurvey.auditoria"
      via: "seção Auditoria renderizada só quando modalSurvey.auditoria existe"
      pattern: "auditoria"
---

<objective>
Consumir no `resources/js/Pages/Nps/Index.jsx` (nenhuma página nova) o payload admin-only entregue pelo plano 95-01: badge tri-estado na listagem (AB-95-1), filtro de confiança na barra de filtros (AB-95-3) e seção "Auditoria" no modal "Ver respostas" (AB-95-2). O front é consumidor puro — toda a blindagem já aconteceu no servidor; aqui os guards são por EXISTÊNCIA da chave no dado, nunca decisão de segurança.

Purpose: primeira exposição visual da camada anti-burlamento, exclusiva para admin, no dark theme padrão do projeto.
Output: `Nps/Index.jsx` estendido + bundle de produção validado (`npm run build`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-CONTEXT.md
@.planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-01-SUMMARY.md
@resources/js/Pages/Nps/Index.jsx
@resources/js/Pages/Sugadores/Index.jsx

<interfaces>
<!-- Shape do payload entregue pelo plano 95-01 (só presente quando admin): -->

Por item de `surveys.data`:
```js
confianca: null | {                    // null = survey pendente (sem resposta)
  status:  'confiavel' | 'atencao' | 'suspeita',
  motivos: string[],                   // textos pt-BR da Fase 94
}
auditoria: {
  gerado_em: 'dd/mm/aaaa hh:mm',       gerado_por: string|null,   // null = disparo mensal automático
  aberto_primeira: string|null,        aberto_ultima: string|null,
  aberto_contagem: number,             respondido_em: string|null,
  tempo_ate_resposta: number|null,     // segundos brutos — formatar no front
  ip_abertura: string|null,            ip_resposta: string|null,
  user_agent: string|null,             canal: string,             // 'Email' | 'Digisac' | 'Email + Digisac' | 'Manual (link gerado por admin)' | 'Não confirmado'
  motivos: string[],
}
```

Props raiz (só quando admin): `pode_ver_confianca: true`, `filtros.confianca: 'todos'|'confiavel'|'atencao'|'suspeita'`.
Para não-admin NENHUMA dessas chaves existe — destructuring precisa de defaults (`pode_ver_confianca = false`).

Padrão de badge a copiar (resources/js/Pages/Sugadores/Index.jsx:13-30, 96-107): constantes module-scope `STATUS_LABELS`/`STATUS_BADGE` + componente com `cn()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfiancaBadge na listagem + GlassSelect de filtro (AB-95-1 + AB-95-3, camada de apresentação)</name>
  <files>resources/js/Pages/Nps/Index.jsx</files>
  <read_first>
    - .planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-01-SUMMARY.md (shape final do payload — fonte de verdade sobre nomes de chave)
    - resources/js/Pages/Nps/Index.jsx linhas 449-530 (TableCard: gridCols e estrutura), 649-791 (o filtrados.map das rows — onde o badge entra), 931-949 (aplicarFiltros + handlers), 997-1088 (barra de filtros com os GlassSelect existentes), 847-861 (destructuring de props + isAdmin)
    - resources/js/Pages/Sugadores/Index.jsx linhas 13-30 e 96-107 (padrão StatusBadge/STATUS_LABELS/STATUS_BADGE a copiar)
    - Memória do projeto "Rollup elimina var de escopo usada dentro de .map()" + precedentes corretos no próprio arquivo (linhas 1033 e ~1214: "derivar value dentro do map")
  </read_first>
  <action>
    1. Constantes module-scope (fora do componente, junto de STATUS_LABEL/STATUS_COLOR existentes): `CONFIANCA_LABELS = { confiavel: 'Confiável', atencao: 'Atenção', suspeita: 'Suspeita' }` e `CONFIANCA_BADGE = { confiavel: 'bg-emerald-500/15 text-emerald-300 border-emerald-500/30', atencao: 'bg-amber-500/15 text-amber-300 border-amber-500/30', suspeita: 'bg-rose-500/15 text-rose-300 border-rose-500/30' }` — nunca "flag"/"fraud score"/jargão (regra sistêmica do projeto).
    2. Componente `ConfiancaBadge({ confianca })` no mesmo arquivo (padrão dos badges locais PesoBadge/DimensaoBadge): retorna `null` se `!confianca`; senão `<span>` pill com `cn('inline-flex items-center px-2 py-0.5 rounded-full text-[11px] font-semibold border', CONFIANCA_BADGE[confianca.status])`, texto `CONFIANCA_LABELS[confianca.status]` e `title={confianca.motivos?.join(' ')}` como tooltip. Lookups feitos NO RENDER do componente (nunca variável do corpo do TableCard herdada pelo `.map()` — pitfall Rollup do projeto).
    3. Posição na listagem (Claude's Discretion do CONTEXT, decidida aqui): dentro da célula STATUS de cada row do `filtrados.map`, logo abaixo do chip de status existente (`<ConfiancaBadge confianca={s.confianca} />` com `marginTop` pequeno) — não mexe em `gridCols`, não cria coluna, e o guard é a própria existência de `s.confianca` (não-admin nunca tem a chave; NUNCA usar `isAdmin &&` sobre dado do payload).
    4. Filtro: destructuring `pode_ver_confianca = false` nas props do `NpsIndex`. Novo `GlassSelect` na barra de filtros logo após o select de modelo, renderizado apenas em `{pode_ver_confianca && (...)}` (mesmo molde do bloco `pode_filtrar_por_pessoa` da linha 1041): `value={filtros.confianca || 'todos'}`, options com 4 `SelectItem` — `todos`→'Todos', `confiavel`→'Confiáveis', `atencao`→'Com alerta', `suspeita`→'Suspeitos' (labels exatos do CONTEXT AB-95-3), width ~170, ícone `ShieldCheck` do lucide-react.
    5. Wiring: `handleConfiancaChange = (v) => aplicarFiltros({ confianca: v === 'todos' ? undefined : v })`; e no payload base do `aplicarFiltros` acrescentar `confianca: filtros.confianca && filtros.confianca !== 'todos' ? filtros.confianca : undefined` para o filtro persistir quando o admin troca mês/empresa/modelo (o delete-falsy da linha 940 limpa o `undefined` sozinho).
    6. Comentários pt-BR referenciando AB-95-1/AB-95-3 e o pitfall Rollup.
  </action>
  <verify>
    <automated>npm run build && php artisan test --filter=Nps</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0 (valida escopo Rollup em produção — o pitfall só aparece no bundle)
    - `php artisan test --filter=Nps` verde (nenhuma mudança de backend nesta task — regressão trivial, mas obrigatória por wave)
    - `grep -c "ConfiancaBadge" resources/js/Pages/Nps/Index.jsx` >= 2 (definição + uso na row)
    - Nenhuma renderização usa `isAdmin &&` para decidir exibição de `confianca` (guard é existência da chave); `grep -n "isAdmin && s.confianca\|isAdmin && modalSurvey.auditoria" resources/js/Pages/Nps/Index.jsx` vazio
    - Nenhum arquivo além de `resources/js/Pages/Nps/Index.jsx` no diff (páginas públicas intocadas)
  </acceptance_criteria>
  <done>Badge tri-estado aparece nas rows (dado presente = admin) e o filtro de confiança dispara `?confianca=` server-side, persistindo entre trocas de outros filtros.</done>
</task>

<task type="auto">
  <name>Task 2: Seção "Auditoria" no modal "Ver respostas" (AB-95-2)</name>
  <files>resources/js/Pages/Nps/Index.jsx</files>
  <read_first>
    - resources/js/Pages/Nps/Index.jsx linhas 1259-1338 (Dialog do modalSurvey — estrutura, DialogHeader, bloco respostas_customizadas, footer com botão de excluir do admin)
    - .planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-01-SUMMARY.md (nomes exatos das 12 chaves de `auditoria`)
    - 95-CONTEXT.md seção "Seção de auditoria no detalhe" (lista de campos e regra de linguagem clara)
  </read_first>
  <action>
    1. Helper local module-scope `fmtDuracao(segundos)`: `null`→'—'; `< 60`→'`Xs`'; senão '`Xmin Ys`' (ex.: 754 → '12min 34s'). Definir junto dos helpers fmt existentes (linhas ~55-110).
    2. Dentro do `DialogContent` do `modalSurvey`, após o bloco `respostas_customizadas` e antes do footer: seção "Auditoria" renderizada APENAS quando `modalSurvey.auditoria` existir (não-admin nunca tem a chave — nada de `isAdmin &&`). Estilo consistente com o modal: título de seção uppercase pequeno (mesmo padrão de "Todas as respostas"), container `bg-white/[0.03] border border-white/[0.08] rounded-xl` via `cn()`, grid de 2 colunas de pares rótulo/valor.
    3. Pares rótulo→valor (rótulos pt-BR exatos, zero jargão): 'Gerado em'→`gerado_em`; 'Gerado por'→`gerado_por ?? 'Disparo mensal automático'`; 'Aberto em'→`aberto_primeira` + (quando `aberto_contagem > 1`) sufixo '· última {aberto_ultima} · {aberto_contagem} aberturas'; 'Respondido em'→`respondido_em ?? '—'`; 'Tempo até resposta'→`fmtDuracao(tempo_ate_resposta)`; 'IP da abertura'→`ip_abertura ?? '—'`; 'IP da resposta'→`ip_resposta ?? '—'`; 'Navegador'→`user_agent` truncado com `title` completo no hover; 'Canal de envio'→`canal`.
    4. Bloco "Motivos de suspeita": renderizado apenas quando `auditoria.motivos.length > 0` — lista dos textos pt-BR gravados pela Fase 94 (ex.: 'Resposta enviada a partir da rede interna da ECF.') com tom de alerta (`text-amber-300`/`text-rose-300` conforme `modalSurvey.confianca?.status`, derivando cor DENTRO do callback do `.map()` de motivos — pitfall Rollup).
    5. Reutilizar `<ConfiancaBadge confianca={modalSurvey.confianca} />` (Task 1) ao lado do `DialogTitle`, guard por existência da chave.
    6. Rodar o gate de fase: `npm run build` + suite completa.
  </action>
  <verify>
    <automated>npm run build && php artisan test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0
    - `php artisan test` suite completa verde (phase gate do VALIDATION.md)
    - `grep -c "Auditoria" resources/js/Pages/Nps/Index.jsx` >= 1 (título da seção presente)
    - Cor/label de cada motivo derivada dentro do callback do `.map()` (inspeção do diff — nenhuma variável do corpo do componente referenciada dentro do map de motivos)
    - Rótulos da seção não contêm jargão técnico cru ('user_agent' aparece como 'Navegador'; nenhum 'flag'/'score' no texto visível)
  </acceptance_criteria>
  <done>Modal "Ver respostas" do admin exibe a seção Auditoria completa (12 campos + motivos) no dark theme; para não-admin o modal permanece idêntico ao atual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação visual — badge, filtro e auditoria no dark theme</name>
  <what-built>UI de confiança admin-only completa: badge tri-estado na listagem NPS, filtro Todos/Confiáveis/Com alerta/Suspeitos e seção Auditoria no modal — tudo consumindo o payload blindado do plano 95-01. Todos os testes automatizados e o build de produção já passaram.</what-built>
  <how-to-verify>
    1. `npm run build` já executado — subir o ambiente local (`php artisan serve` ou XAMPP) e logar como ADMIN
    2. Abrir `/nps` (selecionar "Todos os modelos" se a lista vier vazia): conferir badge verde "Confiável" nas respostas limpas; se houver resposta suspeita no banco local, conferir amarelo/vermelho (alternativa: setar `suspicion_reasons` numa resposta via tinker para ver os 3 estados)
    3. Usar o filtro de confiança e confirmar que a lista + paginação reagem (URL ganha `?confianca=...`)
    4. Clicar no olho ("Ver respostas") de um NPS respondido: conferir a seção "Auditoria" — legibilidade dos 12 campos no dark theme, motivos em pt-BR claro
    5. Logar como CONSULTOR (não-admin): tela `/nps` idêntica à de antes da fase — sem badge, sem filtro, sem seção; conferir no DevTools (aba Network, response do GET /nps) que o JSON não contém `confianca`/`auditoria`/`pode_ver_confianca`
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os ajustes visuais necessários</resume-signal>
  <files>nenhum — verificação humana, sem mudança de código</files>
  <action>Apresentar ao usuário o roteiro de verificação acima e aguardar aprovação (nenhuma automação pendente — build e testes já verdes nas Tasks 1-2).</action>
  <verify>
    <human-check>Badge nos 3 estados legível no dark theme; seção Auditoria clara; tela de não-admin idêntica à atual e payload sem chaves de confiança</human-check>
  </verify>
  <done>Usuário respondeu "aprovado" (ou os ajustes descritos foram aplicados e reaprovados).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload Inertia → React | O front só pode renderizar o que o servidor entregou — nenhuma decisão de segurança acontece aqui |
| Dados da Fase 94 → DOM | `user_agent` e motivos são strings vindas de requests externos — renderização precisa escapar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-05 | Information Disclosure | Renderização condicional no JSX | mitigate | Guards por EXISTÊNCIA da chave (`s.confianca &&`), nunca `isAdmin &&` sobre dado já entregue — o dado sensível nem chega ao browser do não-admin (blindado no plano 95-01); acceptance criteria proíbe o padrão via grep |
| T-95-06 | XSS (Tampering) | Render de `user_agent`/`motivos` (strings de origem externa) no modal | mitigate | Renderização exclusivamente via JSX interpolado (React escapa por padrão); PROIBIDO `dangerouslySetInnerHTML` em qualquer parte da seção Auditoria |
| T-95-SC | Tampering | Supply chain (npm) | accept | Nenhum pacote novo instalado nesta fase (auditoria de legitimidade: N/A conforme RESEARCH) |
</threat_model>

<verification>
- `npm run build` exit 0 (obrigatório — fase toca frontend; pitfall Rollup só aparece no bundle de produção)
- `php artisan test` suite completa verde antes do `/gsd:verify-work`
- Checkpoint visual (Task 3): 3 estados do badge + seção Auditoria no dark theme + tela de não-admin idêntica à atual
</verification>

<success_criteria>
- AB-95-1: badge tri-estado visível para admin na listagem, com labels pt-BR travados ('Confiável'/'Atenção'/'Suspeita')
- AB-95-2: seção Auditoria completa no modal, linguagem clara, motivos da Fase 94 exibidos literalmente
- AB-95-3: filtro de 4 estados dispara query server-side e persiste entre trocas de outros filtros
- Não-admin: experiência visual e payload idênticos aos de antes da fase
- Bundle de produção validado
</success_criteria>

<output>
Criar `.planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-02-SUMMARY.md` ao concluir (em pt-BR).
</output>
