---
phase: 95-nps-anti-burlamento-ui-de-confian-a-admin-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/NpsController.php
  - tests/Feature/Phase95/NpsConfiancaPayloadTest.php
  - tests/Feature/Phase95/NpsConfiancaFiltroTest.php
autonomous: true
requirements: [AB-95-1, AB-95-2, AB-95-3, AB-95-4]
tags: [laravel, inertia, nps, anti-burlamento, payload-gating, tdd]

must_haves:
  truths:
    - "Payload Inertia de admin em /nps traz, por item respondido, `confianca` (status tri-estado + motivos pt-BR) e `auditoria` (todos os campos do CONTEXT)"
    - "Payload Inertia de não-admin NÃO contém as chaves `confianca`, `auditoria`, `pode_ver_confianca` nem qualquer string de suspeita/IP/user-agent — as chaves nunca saem do servidor"
    - "Admin filtra a lista por ?confianca=confiavel|atencao|suspeita com paginação server-side correta"
    - "Não-admin que manda ?confianca=suspeita recebe resposta 200 idêntica à do GET sem o parâmetro (sem 403/422 — o filtro não é revelado)"
    - "Resposta legada (sem rastro da Fase 94) cai em 'Confiável' sem erro"
  artifacts:
    - path: "app/Http/Controllers/NpsController.php"
      provides: "index() com payload condicional por role + helpers privados confiancaDe()/auditoriaDe() + filtro confianca validado por whitelist"
      contains: "confiancaDe"
    - path: "tests/Feature/Phase95/NpsConfiancaPayloadTest.php"
      provides: "Prova de AB-95-1/AB-95-2/AB-95-4 no payload bruto (admin vs não-admin)"
      min_lines: 80
    - path: "tests/Feature/Phase95/NpsConfiancaFiltroTest.php"
      provides: "Prova de AB-95-3 (filtro server-side + ignorado silenciosamente para não-admin)"
      min_lines: 60
  key_links:
    - from: "app/Http/Controllers/NpsController.php"
      to: "nps_responses.suspicion_reasons (Fase 94)"
      via: "leitura pura de suspicion_reasons['severity'] no helper confiancaDe()"
      pattern: "suspicion_reasons"
    - from: "app/Http/Controllers/NpsController.php"
      to: "nps_survey_events (Fase 94)"
      via: "eager-load 'events' (só admin) para derivar canal de envio em auditoriaDe()"
      pattern: "events"
    - from: "app/Http/Controllers/NpsController.php"
      to: "escopo de carteira existente"
      via: "filtro confianca aplicado DEPOIS do whereIn(company_id) — nunca substituindo"
      pattern: "isAdmin"
---

<objective>
Estender `NpsController::index()` (nenhuma rota nova) para: (1) entregar ao admin — e SOMENTE ao admin — as chaves `confianca` (badge tri-estado) e `auditoria` (seção de detalhe) em cada item da lista paginada, lendo os dados já persistidos pela Fase 94 sem recalcular nada; (2) aceitar o filtro server-side `?confianca=` validado por whitelist, ignorado silenciosamente para não-admin.

Purpose: AB-95-4 é um controle de segurança — a blindagem nasce no servidor (chaves ausentes do JSON, não escondidas na renderização). Este plano é 100% backend + testes; a Wave 2 consome o payload.
Output: `NpsController::index()` estendido + 2 arquivos de teste Feature novos em `tests/Feature/Phase95/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-CONTEXT.md
@.planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-RESEARCH.md
@app/Http/Controllers/NpsController.php
@app/Services/Nps/NpsSuspicionService.php
@app/Models/NpsSurvey.php
@app/Models/NpsResponse.php
@app/Models/NpsSurveyEvent.php
@tests/Feature/V16/NpsResponsaveisPayloadTest.php

<interfaces>
<!-- Contratos já existentes (Fase 94) que o executor usa direto — sem exploração. -->

De app/Models/NpsSurvey.php (colunas + relações relevantes):
```php
// casts: first_opened_at/last_opened_at => datetime, open_count => integer
// colunas: open_ip_address, open_user_agent (strings nullable)
public function events(): HasMany   // NpsSurveyEvent, FK survey_id
public function generatedBy()       // belongsTo User (nullable = disparo mensal)
```

De app/Models/NpsResponse.php:
```php
// casts: response_duration_seconds => integer, is_suspicious => boolean,
//        suspicion_reasons => array
// FATO CONFIRMADO NO CÓDIGO (capturarRastroEAvaliarSuspeita, NpsController:770-772):
//   resposta LIMPA persiste suspicion_reasons = NULL (nunca {severity:'nenhuma'});
//   resposta suspeita persiste {reasons: string[], severity: 'media'|'alta'}
```

De app/Models/NpsSurveyEvent.php:
```php
public const TYPE_GENERATED = 'generated';   public const TYPE_OPENED = 'opened';
public const TYPE_SUBMITTED = 'submitted';   public const TYPE_EXPIRED = 'expired';
public const TYPE_SENT_EMAIL = 'sent_email'; public const TYPE_SENT_DIGISAC = 'sent_digisac';
// casts: metadata => array  (evento generated manual tem metadata['origem'] = 'manual')
```

De tests/Feature/V16/NpsResponsaveisPayloadTest.php (molde — helpers a replicar/adaptar):
```php
private function admin(): User                                   // factory role=admin
private function propsDoIndex(User $user): array                 // GET /nps?template_id=__todos__ → props Inertia
private function surveyNoPayload(array $props, string $token): ?array
// trait Tests\Feature\V16\CriaCenarioResponsaveis: inserirPivot($companyId, $userId, $role, $servicoId)
// setUp(): PRAGMA foreign_keys=ON + Carbon::setTestNow (congela o mês do filtro)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Payload admin-only `confianca` + `auditoria` com blindagem server-side (AB-95-1, AB-95-2, AB-95-4)</name>
  <files>app/Http/Controllers/NpsController.php, tests/Feature/Phase95/NpsConfiancaPayloadTest.php</files>
  <read_first>
    - tests/Feature/V16/NpsResponsaveisPayloadTest.php (molde COMPLETO: helpers propsDoIndex/surveyNoPayload/admin, armadilha do template_id=__todos__, setUp com Carbon::setTestNow)
    - tests/Feature/V16/CriaCenarioResponsaveis.php (helper inserirPivot para colocar empresa na carteira do não-admin)
    - app/Http/Controllers/NpsController.php linhas 112-135 (eager-load + escopo carteira) e 242-264 (o ->through() atual, arrow fn a converter)
    - app/Http/Controllers/NpsController.php linhas 749-774 (capturarRastroEAvaliarSuspeita — prova do shape persistido: limpo = NULL)
    - 95-RESEARCH.md Pattern 2 (confiancaDe) e Pattern 4 (auditoriaDe) — implementações de referência
  </read_first>
  <behavior>
    RED primeiro — criar `tests/Feature/Phase95/NpsConfiancaPayloadTest.php` (namespace `Tests\Feature\Phase95`, molde estrutural do V16, comentários pt-BR). Seed determinístico: criar surveys `completed` + `NpsResponse::create()` direto com `suspicion_reasons` explícito (como o Teste 2 do molde faz no ramo legado) — NÃO via POST /nps/{token}, porque o POST dispararia a Regra 2 (resposta rápida) e contaminaria o cenário "confiável". Cenários mínimos:
    - Teste 1 (verde): resposta com `suspicion_reasons=null` → admin vê `confianca.status='confiavel'` e `confianca.motivos=[]`
    - Teste 2 (amarelo): `suspicion_reasons={reasons:['Resposta enviada em menos de 1 minuto após geração do link.'], severity:'media'}` → `confianca.status='atencao'` e o motivo pt-BR presente em `confianca.motivos`
    - Teste 3 (vermelho): `severity:'alta'` → `confianca.status='suspeita'`
    - Teste 4 (auditoria completa): survey com rastro completo (first/last_opened_at, open_count=2, open_ip_address, open_user_agent, completed_at, response com response_ip_address/response_user_agent/response_duration_seconds) + evento `sent_email` seedado em nps_survey_events → admin vê `auditoria` com TODAS as 12 chaves: gerado_em, gerado_por, aberto_primeira, aberto_ultima, aberto_contagem, respondido_em, tempo_ate_resposta, ip_abertura, ip_resposta, user_agent, canal ('Email'), motivos
    - Teste 5 (canal manual): survey sem eventos de envio mas com evento `generated` de `metadata={'origem':'manual'}` → `auditoria.canal = 'Manual (link gerado por admin)'`
    - Teste 6 (blindagem AB-95-4 — o teste central da fase): não-admin (`role='consultor'`, empresa na carteira via `inserirPivot` para o item aparecer na lista — senão o teste passa por vacuidade) → item PRESENTE no payload, `assertArrayNotHasKey('confianca')` + `assertArrayNotHasKey('auditoria')`; `json_encode($item)` não contém `is_suspicious`, `suspicion_reasons`, `response_ip_address`, `open_ip_address`, `user_agent`; e nos props raiz `assertArrayNotHasKey('pode_ver_confianca', $props)`
    - Teste 7 (pendente): survey `pending` sem response → admin vê `confianca` presente porém `null` (sem crash no helper)
  </behavior>
  <action>
    GREEN — estender `NpsController::index()` (referência: 95-RESEARCH Patterns 1, 2 e 4; decisões D do CONTEXT "Badge na listagem"/"Seção de auditoria"/"Blindagem de payload"):
    1. Eager-load condicional: montar o array do `with([...])` numa variável e acrescentar `'events'` SOMENTE quando `$user->isAdmin()` (não-admin nunca paga a query da trilha).
    2. Converter o `->through(fn($s) => [...])` (linha 242) para closure completa `function ($s) use ($user, $notaDe, $extrasDe, $responsaveisDe)`: montar `$item` com os campos atuais IDÊNTICOS, e só depois `if ($user->isAdmin()) { $item['confianca'] = $this->confiancaDe($s->response); $item['auditoria'] = $this->auditoriaDe($s); }`. Para não-admin as chaves simplesmente não existem no array — nunca `null`, nunca filtradas depois.
    3. Helper privado `confiancaDe(?NpsResponse $response): ?array` — retorna `null` se sem response; senão lê `$response->suspicion_reasons['severity'] ?? 'nenhuma'` e mapeia via `match`: `'alta'`→`'suspeita'`, `'media'`→`'atencao'`, default→`'confiavel'` (resposta limpa persiste `suspicion_reasons=null`, então legados e limpos caem em confiável automaticamente — comportamento travado no CONTEXT). Retorna `['status' => ..., 'motivos' => $response->suspicion_reasons['reasons'] ?? []]`. NUNCA ler `is_suspicious` para a cor (pitfall 5 do RESEARCH: perderia o estado amarelo).
    4. Helper privado `auditoriaDe(NpsSurvey $s): array` — Pattern 4 do RESEARCH literal: deriva `canal` de `$s->events` (`sent_email`+`sent_digisac`→'Email + Digisac'; só email→'Email'; só digisac→'Digisac'; evento generated com `metadata['origem']==='manual'`→'Manual (link gerado por admin)'; default→'Não confirmado'). Datas formatadas `d/m/Y H:i` (consistente com o payload atual); `tempo_ate_resposta` = inteiro bruto de `response_duration_seconds` (o front formata); `gerado_por` = `$s->generatedBy?->name` (null = disparo mensal automático); NUNCA recalcular duração via diffInSeconds (pitfall Carbon 3 documentado no SUMMARY 94-02).
    5. Props: adicionar `'pode_ver_confianca' => true` ao `Inertia::render()` SOMENTE quando `$user->isAdmin()` (merge condicional no array de props). Para não-admin a chave não existe — mais estrito que o RESEARCH sugeriu, exigido pelo CONTEXT ("não recebe nem sinal de que ela existe — inclusive no payload").
    Comentários em pt-BR referenciando AB-95-1/2/4. Não tocar em `respond()`/`submitResponseV15()`/`submitResponseLegacy()` nem em nenhuma página pública.
    Commits atômicos: `test(95-01)` RED → `feat(95-01)` GREEN.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsConfiancaPayloadTest</automated>
  </verify>
  <acceptance_criteria>
    - RED: os 7 cenários falham antes da implementação (chaves ausentes do payload)
    - GREEN: `php artisan test --filter=NpsConfiancaPayloadTest` 100% verde
    - Teste 6 prova ausência das CHAVES no array de props bruto (via toArray()['props']), nunca via assertDontSee/DOM
    - `php artisan test --filter=Nps` continua verde (baseline 250/250 + novos — nenhuma regressão no módulo)
    - Nenhum arquivo de página pública (Respond/ThankYou/AlreadyCompleted/Expired) modificado
  </acceptance_criteria>
  <done>Admin recebe `confianca` + `auditoria` por item; não-admin recebe payload byte-idêntico ao de hoje (sem chaves novas, sem `pode_ver_confianca`); suite Nps verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filtro server-side `?confianca=` — whitelist + ignorado silenciosamente para não-admin (AB-95-3)</name>
  <files>app/Http/Controllers/NpsController.php, tests/Feature/Phase95/NpsConfiancaFiltroTest.php</files>
  <read_first>
    - app/Http/Controllers/NpsController.php linhas 44-51 ($mesFiltro — o molde exato de validação por whitelist com fallback silencioso) e 129-135 (escopo de carteira — o filtro novo entra DEPOIS dele, nunca no lugar)
    - 95-RESEARCH.md Pattern 3 + "Nota de driver" (usar operador JSON nativo do Eloquent, NÃO whereRaw JSON_EXTRACT) + Pitfall 4 (comportamento byte-idêntico para não-admin)
    - tests/Feature/Phase95/NpsConfiancaPayloadTest.php (criado na Task 1 — reutilizar os mesmos helpers de seed)
  </read_first>
  <behavior>
    RED primeiro — criar `tests/Feature/Phase95/NpsConfiancaFiltroTest.php`. Seed: 3 surveys respondidos no mês corrente congelado (limpo `suspicion_reasons=null`, média, alta — mesmo padrão de seed direto da Task 1) + 1 survey `pending`. Todos os GETs com `template_id=__todos__` (armadilha documentada no molde V16). Cenários:
    - Admin `?confianca=confiavel` → lista contém SÓ o token do limpo (o pendente também sai — filtro implica respondidos)
    - Admin `?confianca=atencao` → só o token da média; `?confianca=suspeita` → só o da alta
    - Admin `?confianca=todos` e GET sem o parâmetro → os 4 surveys presentes (contagens iguais)
    - Admin `?confianca=xpto` (valor inválido) → idêntico a `todos` (fallback silencioso, sem erro)
    - Admin: props trazem `filtros.confianca` refletindo o valor aplicado
    - Não-admin (consultor com as empresas na carteira) `?confianca=suspeita` → status 200 e lista com os MESMOS tokens/contagem do GET sem o parâmetro (filtro ignorado, nunca 403/422 — não revelar a camada); props sem a chave `confianca` dentro de `filtros`
  </behavior>
  <action>
    GREEN — em `index()`, logo após o bloco do `$mesFiltro`:
    1. Whitelist estrita: `$confiancaValidos = ['todos', 'confiavel', 'atencao', 'suspeita']`; `$confiancaFiltro = in_array($request->input('confianca', 'todos'), $confiancaValidos, true) ? ... : 'todos'` (mesmo molde do mês — inválido cai no default sem alarde).
    2. Aplicação — DEPOIS do escopo de carteira e do `$aplicarFiltrosSurveys` (defesa em profundidade: o filtro novo se soma ao escopo, jamais o substitui): `if ($user->isAdmin() && $confiancaFiltro !== 'todos')` → `$baseQuery->whereHas('response', ...)` com: `confiavel` → `whereNull('suspicion_reasons')` (fato confirmado: resposta limpa persiste NULL — não existe severity 'nenhuma' no banco); `atencao` → `where('suspicion_reasons->severity', 'media')`; `suspeita` → `where('suspicion_reasons->severity', 'alta')`. Usar o operador JSON nativo do Eloquent (`coluna->chave`) que o Laravel traduz por driver (MySQL/MariaDB e SQLite dos testes) — PROIBIDO `whereRaw("JSON_EXTRACT(...)")`.
    3. Para não-admin o parâmetro é simplesmente ignorado — nenhum branch de erro, nenhum log visível, comportamento idêntico à ausência do parâmetro (pitfall 4 do RESEARCH: um 403 denunciaria a feature).
    4. Props: dentro do array `'filtros'` já existente, adicionar a chave `'confianca' => $confiancaFiltro` SOMENTE quando `$user->isAdmin()` (merge condicional — não-admin não recebe nem a chave).
    5. Comentário pt-BR no bloco explicando o porquê do silêncio (AB-95-3 + não revelar a camada).
    Nota de escopo consciente: cards e série 12m NÃO recebem o filtro de confiança (o CONTEXT pede filtro na LISTAGEM; médias continuam íntegras — invalidação de resposta é Fase 96).
    Commits atômicos: `test(95-01)` RED → `feat(95-01)` GREEN.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsConfiancaFiltroTest && php artisan test --filter=Nps</automated>
  </verify>
  <acceptance_criteria>
    - RED: cenários de filtro falham antes da implementação
    - GREEN: `php artisan test --filter=NpsConfiancaFiltroTest` 100% verde
    - Regressão do módulo: `php artisan test --filter=Nps` totalmente verde (baseline 250/250 + testes da fase)
    - Grep de sanidade: `grep -c "whereRaw" app/Http/Controllers/NpsController.php` não aumentou em relação ao baseline (nenhum JSON_EXTRACT cru novo)
    - Não-admin com `?confianca=suspeita`: mesma contagem e mesmos tokens do GET sem parâmetro, HTTP 200
  </acceptance_criteria>
  <done>Filtro tri-estado funciona server-side (afeta paginação) para admin e é invisível/inócuo para qualquer outro role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `GET /nps` | Query string não confiável (`confianca`, `mes`, `template_id`...) cruza para o controller |
| Controller → Browser (payload Inertia) | Dados de suspeita/auditoria só podem cruzar para sessões com role `admin` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Information Disclosure | `NpsController::index()` payload Inertia | mitigate | Chaves `confianca`/`auditoria`/`pode_ver_confianca` construídas condicionalmente no backend (`if ($user->isAdmin())`) — ausentes do JSON para não-admin; Teste 6 inspeciona o array de props bruto |
| T-95-02 | Information Disclosure | Probing de `?confianca=` por não-admin | mitigate | Whitelist `in_array` estrito + parâmetro ignorado silenciosamente para não-admin (resposta idêntica à ausência do parâmetro; nunca 403/422) — coberto por teste dedicado |
| T-95-03 | Elevation of Privilege | Filtro `confianca` combinado com escopo de carteira | mitigate | Filtro aplicado DEPOIS do `whereIn('company_id', ...)` existente, dentro de `if ($user->isAdmin())` — nunca substitui o escopo |
| T-95-04 | Injection (V5) | Valor de `confianca` na query | mitigate | Whitelist fixa de 4 valores + operador JSON nativo do Eloquent (bindings) — nenhum valor do usuário chega a SQL cru |
| T-95-SC | Tampering | Supply chain (composer/npm) | accept | Nenhum pacote novo instalado nesta fase (auditoria de legitimidade: N/A conforme RESEARCH) |
</threat_model>

<verification>
- `php artisan test --filter=Phase95` — todos os testes da fase verdes
- `php artisan test --filter=Nps` — regressão do módulo NPS completa (baseline 250/250 + novos, 0 falhas)
- Payload público (`Nps/Respond` e afins) intocado — nenhum arquivo de página pública no diff
</verification>

<success_criteria>
- AB-95-1/2 (camada de dados): admin recebe `confianca` tri-estado + `auditoria` com os 12 campos; motivos em pt-BR vindos direto da Fase 94
- AB-95-3: filtro server-side com whitelist, refletido em `filtros.confianca` só para admin
- AB-95-4: teste automatizado prova ausência de TODA chave sensível no payload de não-admin
- Suite Nps 100% verde; nenhuma página pública tocada
</success_criteria>

<output>
Criar `.planning/phases/95-nps-anti-burlamento-ui-de-confian-a-admin-only/95-01-SUMMARY.md` ao concluir (em pt-BR), documentando o shape exato de `confianca`/`auditoria` para a Wave 2 consumir.
</output>
