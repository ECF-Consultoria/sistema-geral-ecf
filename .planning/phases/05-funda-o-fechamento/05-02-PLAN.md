---
phase: 05-funda-o-fechamento
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/Http/Controllers/AdminController.php
  - routes/web.php
autonomous: true
requirements:
  - FCH-01
  - FCH-02
  - FCH-03

must_haves:
  truths:
    - "GET /administrativo/financeiro retorna componente Admin/Financeiro com prop companies contendo todas as empresas com active=true, ordenadas por nome"
    - "Cada empresa no array companies tem os campos id, name, service_type, contract_start (YYYY-MM-DD ou null), contract_end (YYYY-MM-DD ou null), additional_service, has_adman (boolean)"
    - "Empresa sem adman_account_id tem has_adman=false na prop"
    - "Empresa inativa (active=false) não aparece na prop companies"
    - "PATCH /administrativo/financeiro/{company} valida e persiste service_type, contract_start e contract_end; rejeita service_type inválido com 422"
    - "PATCH com contract_end anterior a contract_start retorna 422"
    - "Após PATCH bem-sucedido, back()->with('success', ...) emite flash que o AppLayout exibe"
  artifacts:
    - path: "app/Http/Controllers/AdminController.php"
      provides: "Métodos fechamento() e updateFechamento() funcionais"
      contains: "fechamento"
      exports: ["fechamento", "updateFechamento"]
    - path: "routes/web.php"
      provides: "Rota PATCH /administrativo/financeiro/{company} → admin.financeiro.update"
      contains: "financeiro/{company}"
  key_links:
    - from: "routes/web.php"
      to: "AdminController::updateFechamento"
      via: "Route::patch dentro do grupo admin"
      pattern: "patch.*financeiro.*updateFechamento"
    - from: "AdminController::fechamento"
      to: "Company model"
      via: "Company::where('active', true)->orderBy('name')->get()->map()"
      pattern: "Company::where.*active.*orderBy.*name"
    - from: "AdminController::updateFechamento"
      to: "Company model"
      via: "Route model binding + $company->update($validated)"
      pattern: "company->update"
---

<objective>
Backend end-to-end da Phase 5: adicionar os métodos fechamento() e updateFechamento() ao AdminController + registrar a rota PATCH no grupo admin de routes/web.php.

Purpose: Conectar o schema criado no Plan 01 ao frontend — o controller entrega as props necessárias para a UI (Plan 03) e processa as submissões do formulário accordion.
Output: GET /administrativo/financeiro entrega props Inertia com companies completas; PATCH /administrativo/financeiro/{company} valida e persiste; AdminFechamentoControllerTest verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-funda-o-fechamento/05-CONTEXT.md
@.planning/phases/05-funda-o-fechamento/05-RESEARCH.md
@.planning/phases/05-funda-o-fechamento/05-PATTERNS.md
@.planning/phases/05-funda-o-fechamento/05-01-SUMMARY.md

<interfaces>
<!-- Estado atual de AdminController.php — verificado diretamente -->

namespace App\Http\Controllers;
use Inertia\Inertia;

class AdminController extends Controller
{
    public function empresas() { return Inertia::render('Admin/Empresas'); }
    public function relatorio() { return Inertia::render('Admin/Relatorio'); }
    public function financeiro() { return Inertia::render('Admin/Financeiro'); }  // ← substituir
    public function inventario() { return Inertia::render('Admin/Inventario'); }
}

<!-- Estado atual de routes/web.php — grupo admin (linhas 232-238) -->

Route::middleware(['auth', 'verified', 'role:admin'])->prefix('administrativo')->name('admin.')->group(function () {
    Route::get('/empresas',   [AdminController::class, 'empresas'])->name('empresas');
    Route::get('/relatorio',  [AdminController::class, 'relatorio'])->name('relatorio');
    Route::get('/financeiro', [AdminController::class, 'financeiro'])->name('financeiro');
    Route::get('/inventario', [AdminController::class, 'inventario'])->name('inventario');
    // ← adicionar PATCH aqui dentro
});

<!-- Campos do model Company após Plan 01 -->

$fillable: [..., 'service_type', 'contract_start', 'contract_end', 'additional_service']
$casts: ['active' => 'boolean', 'contract_start' => 'date', 'contract_end' => 'date']
contract_start e contract_end retornam Carbon (nullsafe com ?->toDateString() necessário no map)

<!-- Padrão de PATCH com route model binding (routes/web.php linha 127) -->
Route::patch('/sugadores/{sugador}/status', [SugadorController::class, 'updateStatus'])->name('sugadores.update-status');

<!-- Padrão validate+update+back (SugadorController::updateStatus) -->
$validated = $request->validate([...]);
$model->update($validated);
return back()->with('success', 'Mensagem.');
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminController — substituir financeiro() e adicionar updateFechamento()</name>
  <files>
    app/Http/Controllers/AdminController.php
  </files>
  <read_first>
    - app/Http/Controllers/AdminController.php — ler completo; o método financeiro() atual é stub vazio; preservar os outros 3 métodos intactos
    - app/Http/Controllers/SugadorController.php — padrão updateStatus(): validate + $model->update($validated) + back()->with('success')
    - app/Models/Company.php — confirmar que $fillable contém os 4 campos novos e que $casts inclui contract_start e contract_end como 'date'
  </read_first>
  <behavior>
    fechamento():
    - Company::where('active', true)->orderBy('name')->get() retorna coleção com pelo menos 1 empresa quando active=true existe
    - map() produz array com chaves: id, name, service_type, contract_start (string YYYY-MM-DD ou null), contract_end (string YYYY-MM-DD ou null), additional_service, has_adman (bool)
    - has_adman é (bool) $c->adman_account_id — true se não-null, false se null
    - contract_start e contract_end usam nullsafe operator: $c->contract_start?->toDateString()
    - retorna Inertia::render('Admin/Financeiro', compact('companies'))

    updateFechamento(Request $request, Company $company):
    - $request->validate com: service_type => 'nullable|in:polo,assessoria,incubadora', contract_start => 'nullable|date', contract_end => 'nullable|date|after_or_equal:contract_start'
    - service_type='invalido' → ValidationException (422 em testes HTTP)
    - contract_end anterior a contract_start → ValidationException (422)
    - Válido → $company->update($validated) + return back()->with('success', 'Fechamento atualizado.')
  </behavior>
  <action>
    Editar app/Http/Controllers/AdminController.php:

    1. Adicionar imports no topo do arquivo: use App\Models\Company; e use Illuminate\Http\Request; (após o use Inertia\Inertia; existente)

    2. Substituir o método financeiro() atual (stub) pelo novo:
       public function fechamento()
       {
           $companies = Company::where('active', true)
               ->orderBy('name')
               ->get()
               ->map(fn (Company $c) => [
                   'id'                 => $c->id,
                   'name'               => $c->name,
                   'service_type'       => $c->service_type,
                   'contract_start'     => $c->contract_start?->toDateString(),
                   'contract_end'       => $c->contract_end?->toDateString(),
                   'additional_service' => $c->additional_service,
                   'has_adman'          => (bool) $c->adman_account_id,
               ]);

           return Inertia::render('Admin/Financeiro', compact('companies'));
       }

       ATENÇÃO: O nome do método PHP muda de financeiro() para fechamento(). A rota GET continuará apontando para fechamento() via routes/web.php (atualizado na Task 2 deste plano).

    3. Adicionar método updateFechamento() após fechamento():
       public function updateFechamento(Request $request, Company $company)
       {
           $validated = $request->validate([
               'service_type'   => 'nullable|in:polo,assessoria,incubadora',
               'contract_start' => 'nullable|date',
               'contract_end'   => 'nullable|date|after_or_equal:contract_start',
           ]);

           $company->update($validated);

           return back()->with('success', 'Fechamento atualizado.');
       }

    4. Não remover os métodos empresas(), relatorio(), inventario() — edição cirúrgica apenas em financeiro() e novos imports.

    Flash message copy: 'Fechamento atualizado.' — per RESEARCH.md Pattern 5. (UI-SPEC usa 'Dados de fechamento salvos com sucesso.' para o toast; o flash do controller é interno e o AppLayout exibe conforme padrão existente.)
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin && php artisan test --filter=AdminFechamentoControllerTest 2>&1 | tail -15</automated>
  </verify>
  <done>AdminFechamentoControllerTest passa com todos os 8 testes verdes; php artisan route:list --name=admin.financeiro mostra GET e PATCH registradas; php artisan test --filter=Fechamento passa 100%.</done>
</task>

<task type="auto">
  <name>Task 2: routes/web.php — atualizar GET e adicionar PATCH no grupo admin</name>
  <files>
    routes/web.php
  </files>
  <read_first>
    - routes/web.php — ler as linhas 232-240 do grupo Módulo Administrativo; confirmar estrutura exata do grupo com prefix('administrativo')->name('admin.')
  </read_first>
  <action>
    Editar routes/web.php dentro do grupo Route::middleware(['auth', 'verified', 'role:admin'])->prefix('administrativo')->name('admin.'):

    1. Atualizar a rota GET /financeiro para apontar para o método renomeado:
       Route::get('/financeiro', [AdminController::class, 'fechamento'])->name('financeiro');
       (muda apenas o segundo argumento: 'financeiro' → 'fechamento'; o nome da rota 'financeiro' e o prefix permanecem — per D-01)

    2. Adicionar após a rota GET /financeiro (dentro do mesmo grupo):
       Route::patch('/financeiro/{company}', [AdminController::class, 'updateFechamento'])->name('financeiro.update');

    Nome completo resultante da rota PATCH: admin.financeiro.update (prefixo 'admin.' + nome 'financeiro.update').

    Verificar que a rota PATCH está dentro do grupo de middleware — não fora. O middleware ['auth', 'verified', 'role:admin'] protege automaticamente ambas as rotas.

    Não alterar nenhuma outra rota fora do grupo Administrativo.
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin && php artisan route:list --name=admin.financeiro 2>&1</automated>
  </verify>
  <done>php artisan route:list --name=admin.financeiro mostra 2 rotas: GET /administrativo/financeiro → AdminController@fechamento e PATCH /administrativo/financeiro/{company} → AdminController@updateFechamento; php artisan test --filter=Fechamento passa 100%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → Controller | Requisições chegam ao AdminController; PATCH aceita dados do usuário (service_type, contract_start, contract_end) |
| Route middleware | Grupo admin protegido por auth + verified + role:admin; nenhuma verificação adicional necessária no controller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | updateFechamento() — service_type input | mitigate | Validação 'nullable\|in:polo,assessoria,incubadora' no $request->validate() rejeit qualquer valor não listado com 422 |
| T-05-04 | Tampering | updateFechamento() — date inputs (contract_start, contract_end) | mitigate | Validação 'nullable\|date' + 'after_or_equal:contract_start' via Laravel Validator; Eloquent usa prepared statements — sem SQL injection |
| T-05-05 | Elevation of Privilege | GET /administrativo/financeiro e PATCH /administrativo/financeiro/{company} | mitigate | Middleware EnsureUserHasRole lança abort(403) automaticamente para qualquer usuário sem role=admin; testado em test_nao_admin_recebe_403 |
| T-05-06 | Tampering | Route model binding — Company {company} no PATCH | accept | Não há multi-tenancy nesta app; qualquer admin pode editar qualquer empresa (comportamento intencional do painel administrativo) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
Verificação completa do Plan 02:
- php artisan route:list --name=admin.financeiro mostra GET e PATCH registradas com controllers corretos
- php artisan test --filter=AdminFechamentoControllerTest: 8 testes verdes
- php artisan test --filter=Fechamento: todos os testes verdes (FechamentoMigrationTest + CompanyServiceTypeTest + AdminFechamentoControllerTest)
- php artisan test (suite completa): sem regressões
</verification>

<success_criteria>
- AdminController.php contém métodos fechamento() e updateFechamento() com imports Company e Request
- fechamento() usa Company::where('active', true)->orderBy('name')->get()->map() com $c->contract_start?->toDateString() (nullsafe)
- updateFechamento() valida service_type nullable|in:polo,assessoria,incubadora, contract_start nullable|date, contract_end nullable|date|after_or_equal:contract_start
- routes/web.php tem Route::patch('/financeiro/{company}', [AdminController::class, 'updateFechamento'])->name('financeiro.update') dentro do grupo admin
- php artisan test --filter=AdminFechamentoControllerTest: 8/8 verdes
- php artisan test suite completa: sem regressões
</success_criteria>

<output>
Criar `.planning/phases/05-funda-o-fechamento/05-02-SUMMARY.md` ao concluir.
</output>
