---
phase: 05-funda-o-fechamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_05_19_100001_add_service_fields_to_companies.php
  - app/Models/Company.php
  - tests/Feature/AdminFechamentoControllerTest.php
  - tests/Unit/CompanyServiceTypeTest.php
  - tests/Feature/FechamentoMigrationTest.php
autonomous: true
requirements:
  - FCH-02
  - FCH-03

must_haves:
  truths:
    - "A tabela companies contém as colunas service_type (varchar nullable), contract_start (date nullable), contract_end (date nullable) e additional_service (varchar nullable)"
    - "O model Company aceita atribuição em massa de service_type, contract_start, contract_end e additional_service via $fillable"
    - "Carbon retorna automaticamente os campos contract_start e contract_end como instâncias de Carbon via $casts"
    - "Mudanças em service_type, contract_start e contract_end são auditadas no activity_log via logOnly"
    - "Os três arquivos de teste (Wave 0) existem com stubs RED que falham com 'not implemented' antes da implementação do controller"
  artifacts:
    - path: "database/migrations/2026_05_19_100001_add_service_fields_to_companies.php"
      provides: "DDL que adiciona 4 colunas à tabela companies"
      contains: "Schema::table('companies'"
    - path: "app/Models/Company.php"
      provides: "Model atualizado com fillable, casts e logOnly expandidos"
      contains: "service_type"
    - path: "tests/Feature/AdminFechamentoControllerTest.php"
      provides: "Stubs RED para FCH-01, FCH-02, FCH-03 e controle de acesso"
      contains: "AdminFechamentoControllerTest"
    - path: "tests/Unit/CompanyServiceTypeTest.php"
      provides: "Stubs RED para validação de service_type"
      contains: "CompanyServiceTypeTest"
    - path: "tests/Feature/FechamentoMigrationTest.php"
      provides: "Stubs RED para verificação de colunas na tabela companies"
      contains: "FechamentoMigrationTest"
  key_links:
    - from: "database/migrations/2026_05_19_100001_add_service_fields_to_companies.php"
      to: "app/Models/Company.php"
      via: "Colunas adicionadas pela migration são expostas via $fillable e $casts"
      pattern: "service_type.*fillable"
    - from: "app/Models/Company.php"
      to: "activity_log table"
      via: "LogsActivity trait + getActivitylogOptions logOnly"
      pattern: "logOnly.*service_type"
---

<objective>
Fundação de dados da Phase 5: adicionar 4 colunas à tabela companies + atualizar o model Company (fillable, casts, logOnly) + criar os 3 arquivos de teste Wave 0 com stubs RED.

Purpose: Preparar o schema e o model para suportar tipo de serviço, datas de contrato e serviço adicional — base que o controller (Plan 02) e a UI (Plan 03) irão consumir.
Output: Migration executada, Company model atualizado, 3 arquivos de teste com stubs que cobrem FCH-01/02/03 e controle de acesso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-funda-o-fechamento/05-CONTEXT.md
@.planning/phases/05-funda-o-fechamento/05-RESEARCH.md
@.planning/phases/05-funda-o-fechamento/05-PATTERNS.md

<interfaces>
<!-- Estado atual de Company.php — lido diretamente do arquivo. -->

$fillable atual (linha 28-31):
protected $fillable = [
    'name', 'cnpj', 'adman_account_id', 'adman_store_id', 'ml_store_id',
    'segment', 'active', 'notes',
];

$casts atual (linha 33):
protected $casts = ['active' => 'boolean'];

logOnly atual (linha 17):
->logOnly(['name', 'cnpj', 'segment', 'active', 'notes', 'adman_account_id', 'ml_store_id'])

Analog de migration (add_fields_to_adman_metrics_table.php):
Schema::table('tabela', function (Blueprint $table) {
    $table->string('campo')->nullable()->after('coluna_existente');
});
down(): $table->dropColumn([...]);

Padrão de teste Feature (DevControllerTest.php):
use RefreshDatabase;
private function criarAdmin(): User { return User::factory()->create(['role' => 'admin']); }
Company::create(['name' => '...', 'cnpj' => '...', 'active' => true]);
$this->actingAs($admin)->get('/rota')->assertOk()->assertInertia(...);
$this->actingAs($admin)->patch('/rota/id', [...])->assertRedirect()->assertSessionHas('success');
$this->actingAs($consultor)->get('/rota')->assertStatus(403);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Criar stubs de teste RED</name>
  <files>
    tests/Feature/AdminFechamentoControllerTest.php
    tests/Unit/CompanyServiceTypeTest.php
    tests/Feature/FechamentoMigrationTest.php
  </files>
  <read_first>
    - tests/Feature/DevControllerTest.php — padrão exato de namespace, use declarations, RefreshDatabase, criarAdmin(), Company::create() sem factory, actingAs, assertInertia, assertDatabaseHas, assertStatus(403)
    - tests/Unit/ExampleTest.php — esqueleto de unit test
    - phpunit.xml — confirmar DB_DATABASE=:memory: e configuração SQLite
  </read_first>
  <behavior>
    AdminFechamentoControllerTest.php (Feature):
    - test_fechamento_retorna_empresas_ativas_com_has_adman: GET /administrativo/financeiro retorna component Admin/Financeiro com prop companies contendo a empresa criada com active=true
    - test_empresa_sem_adman_recebe_has_adman_false: empresa criada sem adman_account_id tem companies.0.has_adman === false
    - test_empresa_inativa_nao_aparece: empresa com active=false não aparece na prop companies
    - test_update_persiste_service_type: PATCH /administrativo/financeiro/{id} com service_type=polo → assertRedirect + assertSessionHas('success') + assertDatabaseHas('companies', ['service_type' => 'polo'])
    - test_update_rejeita_service_type_invalido: PATCH com service_type=invalido → assertStatus(422)
    - test_update_persiste_datas_contrato: PATCH com contract_start=2026-01-01 e contract_end=2026-12-31 → assertDatabaseHas com ambas as datas
    - test_update_rejeita_contract_end_anterior: PATCH com contract_end=2025-01-01 e contract_start=2026-01-01 → assertStatus(422)
    - test_nao_admin_recebe_403: actingAs(User com role=consultor) GET /administrativo/financeiro → assertStatus(403)

    CompanyServiceTypeTest.php (Unit):
    - test_service_type_aceita_polo: Company criada com service_type=polo persiste valor 'polo' no banco

    FechamentoMigrationTest.php (Feature):
    - test_migration_adiciona_colunas: após RefreshDatabase, Schema::hasColumn('companies', 'service_type') === true, idem para contract_start, contract_end, additional_service
  </behavior>
  <action>
    Criar os três arquivos de teste com stubs. Cada stub deve ter o método de teste com asserção correta mas marcado com $this->markTestIncomplete('não implementado — aguarda Plan 02') OU implementar os testes completos desde já (preferir implementação completa seguindo o padrão DevControllerTest.php).

    AdminFechamentoControllerTest.php: namespace Tests\Feature; use App\Models\Company; use App\Models\User; use Illuminate\Foundation\Testing\RefreshDatabase; use Tests\TestCase. Classe com use RefreshDatabase e método privado criarAdmin() retornando User::factory()->create(['role' => 'admin']). Todos os 8 métodos de teste listados na behavior implementados completamente.

    CompanyServiceTypeTest.php: namespace Tests\Unit; use App\Models\Company; use Illuminate\Foundation\Testing\RefreshDatabase; use Tests\TestCase. Um método test_service_type_aceita_polo que cria Company via Eloquent direto e assertDatabaseHas.

    FechamentoMigrationTest.php: namespace Tests\Feature; use Illuminate\Foundation\Testing\RefreshDatabase; use Illuminate\Support\Facades\Schema; use Tests\TestCase. Método test_migration_adiciona_colunas com assertTrue para Schema::hasColumn para cada uma das 4 colunas novas.

    Os testes vão falhar (RED) até a migration ser criada (Task 2) — esse é o estado esperado neste momento.
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin && php artisan test --filter=AdminFechamentoControllerTest 2>&1 | tail -5</automated>
  </verify>
  <done>Os 3 arquivos de teste existem; php artisan test --filter=Fechamento executa sem erro de sintaxe PHP (pode falhar em asserção — RED é esperado antes da migration).</done>
</task>

<task type="auto">
  <name>Task 2: Migration — adicionar 4 colunas à tabela companies</name>
  <files>
    database/migrations/2026_05_19_100001_add_service_fields_to_companies.php
  </files>
  <read_first>
    - database/migrations/2026_04_27_100001_add_fields_to_adman_metrics_table.php — analog exato: Schema::table + dropColumn em up/down
    - database/migrations/2026_04_26_152217_create_companies_table.php — confirmar nome atual das últimas colunas (para uso correto de ->after())
  </read_first>
  <action>
    Criar migration com nome de arquivo 2026_05_19_100001_add_service_fields_to_companies.php seguindo o padrão anonymous class do analog.

    up(): Schema::table('companies', function (Blueprint $table) com:
    - $table->string('service_type')->nullable()->after('notes');
    - $table->date('contract_start')->nullable()->after('service_type');
    - $table->date('contract_end')->nullable()->after('contract_start');
    - $table->string('additional_service')->nullable()->after('contract_end');

    down(): Schema::table('companies', function (Blueprint $table) com:
    - $table->dropColumn(['service_type', 'contract_start', 'contract_end', 'additional_service']);

    Usar string (não enum) para service_type — per D-02.
    Usar date (não datetime) para contract_start e contract_end — per D-03.
    Não incluir docblock PHPDoc nos métodos up()/down() — padrão do analog mais recente.

    Após criar o arquivo, executar: php artisan migrate
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin && php artisan migrate:status 2>&1 | grep add_service_fields</automated>
  </verify>
  <done>php artisan migrate:status mostra a migration add_service_fields_to_companies como Ran; FechamentoMigrationTest verde após php artisan test --filter=FechamentoMigrationTest.</done>
</task>

<task type="auto">
  <name>Task 3: Model Company — expandir fillable, casts e logOnly</name>
  <files>
    app/Models/Company.php
  </files>
  <read_first>
    - app/Models/Company.php — ler o arquivo completo antes de editar; extrair $fillable atual, $casts atual e o array de logOnly em getActivitylogOptions()
  </read_first>
  <action>
    Editar app/Models/Company.php aplicando três mudanças cirúrgicas:

    1. $fillable: adicionar 'service_type', 'contract_start', 'contract_end', 'additional_service' ao final do array existente. Estado alvo:
       protected $fillable = [
           'name', 'cnpj', 'adman_account_id', 'adman_store_id', 'ml_store_id',
           'segment', 'active', 'notes',
           'service_type', 'contract_start', 'contract_end', 'additional_service',
       ];

    2. $casts: expandir para array multi-chave mantendo 'active' => 'boolean'. Estado alvo:
       protected $casts = [
           'active'         => 'boolean',
           'contract_start' => 'date',
           'contract_end'   => 'date',
       ];

    3. logOnly em getActivitylogOptions(): adicionar 'service_type', 'contract_start', 'contract_end' ao array existente sem remover os campos já presentes. Estado alvo do array:
       ['name', 'cnpj', 'segment', 'active', 'notes', 'adman_account_id', 'ml_store_id', 'service_type', 'contract_start', 'contract_end']

    Não alterar nenhum outro método ou relacionamento — edição cirúrgica apenas nas 3 estruturas mencionadas (per D-05, D-06, D-07).
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin && php artisan test --filter=Fechamento 2>&1 | tail -10</automated>
  </verify>
  <done>php artisan test --filter=CompanyServiceTypeTest passa verde; $fillable contém todos os 4 campos novos; $casts contém contract_start e contract_end como 'date'; logOnly contém service_type, contract_start e contract_end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DDL schema change | Migration modifica schema de produção; reversível via down() |
| Model mass assignment | $fillable controla quais campos podem ser atribuídos em massa via Eloquent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Company.$fillable | mitigate | Apenas os 4 campos explicitamente listados no $fillable podem ser atribuídos em massa; campos sensíveis (cnpj, adman_account_id) já estavam no fillable por design do projeto |
| T-05-02 | Tampering | Migration down() dropColumn | accept | down() existe para desenvolvimento local; em produção migrations nunca são revertidas; SQLite pode ter limitação com dropColumn múltiplo — isolado a ambiente de testes |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — sem risco de supply chain |
</threat_model>

<verification>
Fase completa quando:
- php artisan migrate:status | grep add_service_fields mostra "Ran"
- php artisan test --filter=Fechamento executa todos os testes sem erro de sintaxe
- FechamentoMigrationTest verde (4 colunas existem)
- CompanyServiceTypeTest verde (service_type persiste)
- AdminFechamentoControllerTest RED ou INCOMPLETE até Plan 02 implementar o controller (esperado)
</verification>

<success_criteria>
- Migration 2026_05_19_100001_add_service_fields_to_companies.php existe e foi executada (php artisan migrate:status confirma "Ran")
- Schema::hasColumn('companies', 'service_type') === true (e idem para as outras 3 colunas)
- Company.$fillable contém: service_type, contract_start, contract_end, additional_service
- Company.$casts contém: contract_start => 'date', contract_end => 'date'
- Company.getActivitylogOptions() logOnly contém: service_type, contract_start, contract_end
- 3 arquivos de teste Wave 0 existem e executam sem erro de parse PHP
</success_criteria>

<output>
Criar `.planning/phases/05-funda-o-fechamento/05-01-SUMMARY.md` ao concluir.
</output>
