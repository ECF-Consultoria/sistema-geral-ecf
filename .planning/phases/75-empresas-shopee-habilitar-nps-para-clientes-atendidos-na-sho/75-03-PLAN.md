---
phase: 75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho
plan: 03
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - app/Http/Controllers/ComercialController.php
  - tests/Feature/Phase75/Phase75CadastroShopeeTest.php
autonomous: true
requirements: [DEC-1]

must_haves:
  truths:
    - "Cadastrar uma empresa com o serviço 'Shopee' cria a Company (adman_account_id/ml_store_id NULOS) sem quebrar"
    - "O cadastro cria 1 ContratoServico ativo apontando para o serviço de setor 'shopee'"
    - "O cadastro NÃO cria nenhuma MlbEmpresa para o serviço Shopee"
    - "O serviço 'Shopee' aparece em servicos_disponiveis no wizard do Comercial"
  artifacts:
    - path: "tests/Feature/Phase75/Phase75CadastroShopeeTest.php"
      provides: "Prova de cadastro Shopee sem ML e sem MlbEmpresa"
      contains: "MlbEmpresa"
  key_links:
    - from: "ComercialController::servicoDisparaImplementacao('Shopee')"
      to: "null"
      via: "nome exato 'Shopee' não casa prefixos ML (Polos/Assessoria/Incubadora)"
      pattern: "servicoDisparaImplementacao"
    - from: "ComercialController::store"
      to: "Company sem adman_account_id/ml_store_id"
      via: "esses campos não aparecem no Company::create"
      pattern: "Company::create"
---

<objective>
Garantir que a origem única de cadastro (`ComercialController::store`) crie uma empresa atendida na Shopee — contrato de serviço setor `shopee`, sem nenhum dado ML (`adman_account_id`/`ml_store_id` nulos), sem disparar o fluxo de implementação/`MlbEmpresa`.

Purpose: DEC-1 exige que empresa sem dado ML cadastre e salve sem quebrar, e que o serviço "Shopee" não caia no roteamento ML. A PATTERNS indica que os helpers `servicoDisparaImplementacao()` e `slugSetorParaServico()` já retornam `null` para o nome exato "Shopee" (Pitfall 3) — logo a ação principal é PROVAR o comportamento por teste, e só editar se o teste revelar um caminho quebrado.
Output: Feature test cobrindo o cadastro Shopee end-to-end no backend; ajuste no `ComercialController` apenas se o teste falhar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-CONTEXT.md
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-PATTERNS.md
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-01-PLAN.md

<interfaces>
app/Http/Controllers/ComercialController.php:
  - create() :71 — monta servicos_disponiveis a partir de Servico::ativo (Shopee entra automaticamente)
  - store() :499 — DB::transaction :554; cria Company status='pendente' :556-576 (adman_account_id/ml_store_id NÃO estão no create); 1 ContratoServico por serviço :587; roteamento MLB :601-633
  - servicoDisparaImplementacao() :54-62 — str_contains só casa 'Polos'|'Assessoria'|'Incubadora' → "Shopee" retorna null
  - slugSetorParaServico() :867-878 — só casa Polos/Assessoria/Publicação/Publicidade/Gestão/Incubadora → "Shopee" retorna null
  - calcularPendenciasComerciais() :404
Rotas: routes/web.php:366-408 (grupo permission:comercial.cadastrar_empresa)
Servico::SETOR_SHOPEE e serviço "Shopee" — criados no Plan 01
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Teste de cadastro Comercial de empresa Shopee sem ML (RED→GREEN)</name>
  <read_first>
    - app/Http/Controllers/ComercialController.php:499-633 (store completo — transaction, Company::create, ContratoServico, roteamento MLB)
    - app/Http/Controllers/ComercialController.php:54-62 e :867-878 (helpers que devem retornar null p/ "Shopee")
    - tests/Feature/Phase37* (padrão de POST autenticado + asserção de Company/ContratoServico); helper actingAsAdmin / criarServico
    - app/Models/Company.php:28-58 (fillable/casts — confirmar adman_account_id/ml_store_id nullable)
  </read_first>
  <behavior>
    - Teste A: POST na rota de store do Comercial com o serviço "Shopee" selecionado cria 1 `Company` com `adman_account_id` e `ml_store_id` NULOS, status inicial esperado ('pendente'), sem exception.
    - Teste B: é criado exatamente 1 `ContratoServico` ativo cujo `servico.setor === 'shopee'`.
    - Teste C: NENHUMA `MlbEmpresa` é criada para essa company (assertDatabaseMissing / count 0).
    - Teste D (unit dos helpers): `servicoDisparaImplementacao('Shopee')` e `slugSetorParaServico('Shopee')` retornam null (garante que renomear o serviço no futuro não quebre — regressão do Pitfall 3).
  </behavior>
  <action>
    Criar `tests/Feature/Phase75/Phase75CadastroShopeeTest.php` (namespace `Tests\Feature\Phase75`, `RefreshDatabase`). Semear o serviço "Shopee" (rodar a migration de seed do Plan 01 já ocorre no RefreshDatabase; ou `Servico::firstOrCreate` no setUp) e autenticar um usuário com `comercial.cadastrar_empresa` (ou admin). Montar o payload do wizard conforme o `store()` espera (inspecionar a validação em store() para os campos obrigatórios: nome/cnpj/segmento/serviços selecionados/valores). Escrever os 4 casos de <behavior]. Rodar. Se algum caminho quebrar (ex.: store pressupõe algum campo ML), NÃO alterar o comportamento de outros serviços — corrigir de forma cirúrgica e comentada em pt-BR apenas o ramo que assume ML indevidamente. Se o teste passar sem edição no controller (esperado pela PATTERNS), documentar no SUMMARY que a ação foi verificação-only.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase75CadastroShopeeTest</automated>
  </verify>
  <acceptance_criteria>Os 4 casos verdes: empresa Shopee cadastra com dados ML nulos, gera contrato setor shopee ativo, zero MlbEmpresa, helpers retornam null. Nenhuma regressão introduzida em cadastros ML (rodar também Comercial adjacente na verificação de wave).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard Comercial (input do usuário) → store() | Payload do formulário cruza para criação de Company/ContratoServico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-06 | Tampering | Mass assignment no store() | mitigate | `store()` usa `$request->validate()` e monta o `Company::create` com campos explícitos; Shopee NÃO adiciona campos fillable novos (RESEARCH Security V5) |
| T-75-07 | Elevation of Privilege | Acesso ao cadastro | accept | Rota já gated por `permission:comercial.cadastrar_empresa` (routes/web.php:366-408); inalterado nesta phase |
| T-75-08 | Tampering | Serviço Shopee dispara MlbEmpresa indevida | mitigate | Teste C prova zero MlbEmpresa; Teste D trava a regressão do nome (Pitfall 3) |
</threat_model>

<verification>
- `php artisan test --filter=Phase75CadastroShopeeTest` verde (4 casos).
- Regressão: `php artisan test --filter=Comercial` sem novas falhas.
</verification>

<success_criteria>
Empresa Shopee cadastrável pelo Comercial sem qualquer dado ML, com contrato de setor shopee ativo e sem MlbEmpresa.
</success_criteria>

<output>
Criar `.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-03-SUMMARY.md` ao concluir.
</output>
