---
phase: 75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Support/Permissions.php
  - tests/Feature/Phase75/Phase75PermissaoCatalogoTest.php
autonomous: true
requirements: [DEC-3]

must_haves:
  truths:
    - "A key 'shopee.empresas' existe no catálogo estático de permissões"
    - "Permissions::all() inclui 'shopee.empresas'"
    - "Admin recebe 'shopee.empresas' automaticamente via short-circuit isAdmin()"
  artifacts:
    - path: "app/Support/Permissions.php"
      provides: "Constante SHOPEE_EMPRESAS + grupo 'Shopee' no catalog()"
      contains: "shopee.empresas"
    - path: "tests/Feature/Phase75/Phase75PermissaoCatalogoTest.php"
      provides: "Prova de catálogo + herança de admin"
      contains: "shopee.empresas"
  key_links:
    - from: "Permissions::catalog()"
      to: "Permissions::all()"
      via: "all() agrega automaticamente qualquer grupo novo"
      pattern: "shopee.empresas"
    - from: "User::isAdmin()"
      to: "shopee.empresas"
      via: "short-circuit concede TODAS as keys ao admin"
      pattern: "hasPermission"
---

<objective>
Registrar a permission key `shopee.empresas` no catálogo estático `app/Support/Permissions.php`, sob um novo grupo "Shopee". Essa key será o único gate de acesso da aba Empresas da Shopee (rota + menu). Admin a recebe automaticamente; o Setor Shopee (criado manualmente pelo usuário em `/setores`) poderá recebê-la.

Purpose: Sem a key no catálogo, nem o middleware `EnsurePermission` nem a tela de setores nem o menu conseguem referenciá-la. É pré-requisito das rotas (Plan 04) e do menu (Plan 05). Independente do Plan 01 (config pura, sem tocar schema) → roda em paralelo na Wave 1.
Output: Constante `Permissions::SHOPEE_EMPRESAS` + grupo "Shopee" em `catalog()` + teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-CONTEXT.md
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-PATTERNS.md

<interfaces>
app/Support/Permissions.php:
  - Constantes MLB_* em :46-60 (ponto de inserção da nova constante logo após)
  - catalog() :123-180 — array associativo grupo→lista de {key,label,description}; grupo 'Publicações (MLB)' :140-154 é o shape a espelhar
  - all() :187-196 — agrega automaticamente todos os grupos de catalog()
  - isValid() :198-202
User::isAdmin() / User::hasPermission() — short-circuit: admin recebe todas as keys (RESEARCH Pattern 6)
EnsurePermission middleware — casa `permission:shopee.empresas` chamando hasPermission()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Escrever teste do catálogo de permissões (RED)</name>
  <read_first>
    - app/Support/Permissions.php:187-202 (all(), isValid())
    - tests/Feature/ (procurar um teste que já use actingAs admin — ex. helper actingAsAdmin em Phase37CompaniesPerformanceFilterTest.php:41-52)
  </read_first>
  <behavior>
    - Teste A: `Permissions::all()` contém 'shopee.empresas' e `Permissions::isValid('shopee.empresas')` é true.
    - Teste B: `Permissions::catalog()` contém a chave de grupo 'Shopee' com pelo menos 1 entrada cujo key === 'shopee.empresas'.
    - Teste C (herança admin): um usuário admin retorna true em `hasPermission('shopee.empresas')` sem que a key esteja atribuída explicitamente a nenhum setor.
  </behavior>
  <action>
    Criar `tests/Feature/Phase75/Phase75PermissaoCatalogoTest.php` (namespace `Tests\Feature\Phase75`, extends `Tests\TestCase`, `RefreshDatabase`). Escrever os 3 testes de <behavior>. Para o Teste C, criar um User admin (role='admin') e assertar `hasPermission('shopee.empresas')`. Comentários pt-BR. Deve falhar (RED) pois a key ainda não existe.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase75PermissaoCatalogoTest</automated>
  </verify>
  <acceptance_criteria>Teste existe com 3 casos; roda e FALHA (RED) por key ausente no catálogo.</acceptance_criteria>
</task>

<task type="auto">
  <name>Tarefa 2: Adicionar constante SHOPEE_EMPRESAS + grupo "Shopee" no catálogo</name>
  <read_first>
    - app/Support/Permissions.php:54-60 (constantes MLB_*), :140-154 (grupo Publicações), :187-196 (all())
  </read_first>
  <action>
    Em `app/Support/Permissions.php`: (a) adicionar `public const SHOPEE_EMPRESAS = 'shopee.empresas';` após as constantes MLB_* (com docblock pt-BR "Aba Empresas do marketplace Shopee — listagem enxuta + atribuição p/ NPS"). (b) Em `catalog()`, adicionar um novo grupo `'Shopee' => [ ['key' => self::SHOPEE_EMPRESAS, 'label' => 'Shopee · Empresas', 'description' => 'Empresas atendidas na Shopee (habilita NPS)'] ]` — posicionar de forma coerente (ex.: após o grupo 'Publicações (MLB)'). NÃO tocar em `all()` nem `isValid()` (agregam sozinhos). NÃO conceder a key manualmente ao admin (short-circuit isAdmin já cobre).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase75PermissaoCatalogoTest</automated>
  </verify>
  <acceptance_criteria>Os 3 testes verdes. `grep -n "shopee.empresas" app/Support/Permissions.php` mostra a constante e a entrada de catálogo.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catálogo de permissões → RBAC de rotas e menu | A key define quem acessa a aba Shopee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04 | Elevation of Privilege | Nova key com escopo amplo demais | mitigate | Key granular `shopee.empresas` NÃO reusa `core.empresas`; só habilita a aba enxuta (DEC-3/DEC-4). Nenhum acesso ao CRUD core é concedido |
| T-75-05 | Spoofing | Key inválida aceita em setor | accept | `isValid()` já valida contra `all()`; a nova key entra na lista automaticamente |
</threat_model>

<verification>
- `php artisan test --filter=Phase75PermissaoCatalogoTest` verde.
- `grep -n "SHOPEE_EMPRESAS\|shopee.empresas" app/Support/Permissions.php` retorna constante + entrada de catálogo.
</verification>

<success_criteria>
Key `shopee.empresas` disponível no catálogo, agregada por `all()`, herdada por admin, e atribuível ao Setor Shopee.
</success_criteria>

<output>
Criar `.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-02-SUMMARY.md` ao concluir.
</output>
