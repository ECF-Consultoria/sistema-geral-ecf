---
phase: 75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho
plan: 05
type: execute
wave: 3
depends_on: ["75-02", "75-04"]
files_modified:
  - resources/js/Pages/Shopee/Empresas.jsx
  - resources/js/Layouts/AppLayout.jsx
autonomous: false
requirements: [DEC-3, DEC-4, DEC-5]

must_haves:
  truths:
    - "A sidebar mostra um grupo 'Shopee' real (não mais o stub 'Em breve' de topo) com o filho 'Empresas'"
    - "O item 'Empresas' só aparece para quem tem a permission shopee.empresas (admin + Setor Shopee)"
    - "A página lista empresas Shopee com abas Todas/Pendências e badges de pendência"
    - "É possível atribuir Analista/Estrategista pela página (posta em shopee.empresas.bulk-assign)"
    - "Cada linha tem botão 'Gerar NPS' que dispara POST /nps/generate e recebe o link em flash.nps_link"
  artifacts:
    - path: "resources/js/Pages/Shopee/Empresas.jsx"
      provides: "Página enxuta: abas, pendências, atribuição, Gerar NPS"
      min_lines: 80
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "Grupo Shopee no NAV_TREE com filho Empresas gated"
      contains: "shopee.empresas"
  key_links:
    - from: "Shopee/Empresas.jsx"
      to: "route('shopee.empresas.bulk-assign')"
      via: "router.post na atribuição"
      pattern: "shopee.empresas.bulk-assign"
    - from: "Shopee/Empresas.jsx"
      to: "route('nps.generate')"
      via: "useForm.post no botão Gerar NPS"
      pattern: "nps.generate"
    - from: "AppLayout.jsx grupo Shopee → filho Empresas"
      to: "permission shopee.empresas"
      via: "itemVisivel() gate por permission"
      pattern: "shopee.empresas"
---

<objective>
Entregar a interface da aba "Empresas" da Shopee: a página React enxuta (`Shopee/Empresas.jsx`, versão reduzida de `Companies/Index.jsx`) e a transformação do stub "Shopee — Em breve" da sidebar num grupo real com o filho "Empresas" gated por `shopee.empresas`, mantendo o Dashboard stub como filho.

Purpose: Fecha o fluxo user-facing — sem a página e o item de menu, o backend do Plan 04 fica inacessível pela UI. É a camada de apresentação; RBAC de menu é defesa em profundidade (o backend é a fonte de verdade).
Output: Página `Shopee/Empresas.jsx`, edição do `NAV_TREE` em `AppLayout.jsx`, `npm run build`, e um checkpoint de verificação humana (menu + Gerar NPS são UX visuais).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-CONTEXT.md
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-PATTERNS.md
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-04-PLAN.md

<interfaces>
MOLDE — resources/js/Pages/Companies/Index.jsx:
  - imports :1-15 (shadcn/ui Card/Button/Select/Dialog/Table; useForm/router/usePage; cn/formatDate; lucide)
  - PENDENCIAS dict :100-106; PendenciaBadges :108-119 (genérico, copiar verbatim)
  - abas Todas/Pendências :361/:498; atribuição no modal :712-729; bulk assign :544-558 / :268-271
  - NpsPendingBadge :17 (opcional)
  - REMOVER na versão enxuta: IMaskInput, MARKETPLACES_EXTRAS, ServicoBadges se não usados, colunas de métrica/cust_id

Props recebidas do ShopeeEmpresasController@index (Plan 04): companies (com pendencias), estrategistas, analistas (, grupos)
Rotas (Plan 04): shopee.empresas.index, shopee.empresas.bulk-assign
NPS: route('nps.generate') — POST {company_id, template_id:''}; link volta em usePage().props.flash.nps_link (padrão Nps/Index.jsx:864-871, :909-914)

MOLDE NAV — resources/js/Layouts/AppLayout.jsx:
  - grupo 'Mercado Livre' :48-99 (shape de grupo com children)
  - stub Shopee de topo :108-115 (badgeText 'Em breve', aponta shopee.dashboard/Dashboard/ShopeeShell) → substituir por grupo
  - itemVisivel() :296-303 gate por permission; grupo some se nenhum filho visível
  - ícones já importados :4-9: Building2, LayoutDashboard, ShoppingCart
  - rota shopee.dashboard já existe (routes/web.php:325)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Página Shopee/Empresas.jsx (versão enxuta)</name>
  <read_first>
    - resources/js/Pages/Companies/Index.jsx (molde completo — imports, PENDENCIAS, PendenciaBadges, abas, modal de atribuição, bulk assign)
    - resources/js/Pages/Nps/Index.jsx:864-871, :909-914, :1109-1153 (padrão Gerar NPS + captura flash.nps_link)
    - 75-PATTERNS.md secção "Shopee/Empresas.jsx" (dicionário de pendências + o que remover)
  </read_first>
  <action>
    Criar `resources/js/Pages/Shopee/Empresas.jsx` espelhando `Companies/Index.jsx` na versão enxuta: (a) copiar imports necessários (Card/Button/Select/Dialog/Table, useForm/router/usePage, cn/formatDate, ícones lucide) e REMOVER IMaskInput/MARKETPLACES_EXTRAS/ServicoBadges se não usados. (b) Dicionário PENDENCIAS com SÓ as 3 chaves da DEC-2: sem_responsavel (vermelho), sem_contato (laranja), empresa_nova (amarelo ecf-yellow). (c) Copiar PendenciaBadges verbatim. (d) Abas "Todas"/"Pendências" e coluna de pendências. (e) Atribuição de Analista/Estrategista via modal + bulk, postando em `route('shopee.empresas.bulk-assign')` (NÃO companies.bulk-assign). (f) Botão "Gerar NPS" por linha: `useForm({ company_id:'', template_id:'' })`, ao clicar `setData('company_id', id)` e `post(route('nps.generate'))`, capturando `usePage().props.flash.nps_link` num useEffect para exibir/copiar o link. Tokens ecf-*, dark theme, `cn()`. Comentários pt-BR. Colunas: nome/segmento/status/contato/responsáveis/pendências/ações — SEM métrica/cust_id.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>`npm run build` conclui sem erro; o bundle inclui a página Shopee/Empresas; nenhuma referência a rotas companies.* dentro da página (usa shopee.empresas.* e nps.generate).</acceptance_criteria>
</task>

<task type="auto">
  <name>Tarefa 2: Transformar stub Shopee em grupo real no NAV_TREE</name>
  <read_first>
    - resources/js/Layouts/AppLayout.jsx:48-99 (grupo Mercado Livre — shape), :108-115 (stub Shopee a substituir), :296-303 (itemVisivel)
  </read_first>
  <action>
    Em `resources/js/Layouts/AppLayout.jsx`, substituir o item de topo stub "Shopee" (:108-115) por um grupo com `children`, espelhando o shape do grupo "Mercado Livre": `{ group: 'Shopee', icon: ShoppingCart, iconSrc: '/images/shopee-icon.svg', children: [ { label:'Empresas', routeName:'shopee.empresas.index', page:'Shopee/Empresas', icon: Building2, permission:'shopee.empresas' }, { label:'Dashboard', routeName:'shopee.dashboard', page:'Dashboard/ShopeeShell', icon: LayoutDashboard, badgeText:'Em breve' } ] }`. Manter o Dashboard stub como filho (rota já existe). `itemVisivel()` já gate por permission — o item Empresas só aparece com a key; o grupo some se nenhum filho for visível. Não gate o Dashboard stub (permanece 'Em breve' visível). Comentário pt-BR.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>`npm run build` sem erro; `grep -n "shopee.empresas.index" resources/js/Layouts/AppLayout.jsx` retorna o filho Empresas dentro do grupo Shopee; o stub de topo 'Em breve' foi removido em favor do grupo.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 3: Verificação visual — menu Shopee + Gerar NPS</name>
  <what-built>
    Grupo "Shopee → Empresas" na sidebar (gated por shopee.empresas) e a página enxuta com abas Todas/Pendências, badges de pendência, atribuição de Analista/Estrategista e botão "Gerar NPS" por linha reaproveitando POST /nps/generate.
  </what-built>
  <how-to-verify>
    1. (Pré-requisito manual do usuário) Em `/setores`, criar/editar o Setor Shopee e conceder a key `shopee.empresas`; adicionar um usuário de teste a esse setor.
    2. Logar como admin → confirmar que o grupo "Shopee" aparece na sidebar com os filhos "Empresas" e "Dashboard (Em breve)".
    3. Acessar "Empresas" → confirmar a listagem de empresas com contrato Shopee, as abas Todas/Pendências, e os badges (Sem responsável / Sem contato / Empresa nova) coerentes.
    4. Atribuir um Analista e um Estrategista a uma empresa → confirmar persistência (recarregar e ver o responsável).
    5. Clicar "Gerar NPS" numa empresa com estrategista + contato → confirmar que o link NPS é gerado (flash) e abre/copia corretamente.
    6. Logar como o usuário do Setor Shopee → confirmar que vê "Empresas"; logar como usuário SEM a key → confirmar que o grupo/item NÃO aparece e que `GET /shopee/empresas` retorna 403.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| menu (frontend) → visibilidade de itens | itemVisivel decide o que renderizar, mas NÃO é o gate de segurança |
| página → POST bulk-assign / nps.generate | Ações do usuário cruzam para endpoints já gated no backend (Plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-14 | Information Disclosure | Menu vaza item sem gate de backend | mitigate | `itemVisivel` gate por `permission:shopee.empresas` (frontend) + `EnsurePermission` na rota (backend, Plan 04) — defesa dupla; nunca confiar só no menu |
| T-75-15 | Elevation of Privilege | Página posta em endpoint core | mitigate | Página usa exclusivamente `route('shopee.empresas.bulk-assign')` e `route('nps.generate')`; nenhuma referência a companies.* (asserção de grep) |
| T-75-16 | Tampering | Deep-link NPS manipulado | accept | `NpsController::generate` valida auth (admin OU membro do pivot) server-side; a UI só reaproveita o endpoint (DEC-5) |
</threat_model>

<verification>
- `npm run build` sem erro.
- `grep -n "shopee.empresas.index\|shopee.empresas.bulk-assign\|nps.generate" resources/js/Pages/Shopee/Empresas.jsx resources/js/Layouts/AppLayout.jsx` confirma os links corretos.
- `grep -n "companies.bulk-assign\|companies.update" resources/js/Pages/Shopee/Empresas.jsx` NÃO retorna nada (não vaza para rotas core).
- Checkpoint humano aprovado (menu + Gerar NPS).
</verification>

<success_criteria>
Menu Shopee real com filho Empresas gated; página enxuta funcional (abas, pendências, atribuição, Gerar NPS); build limpo; verificação visual aprovada.
</success_criteria>

<output>
Criar `.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-05-SUMMARY.md` ao concluir.
</output>
