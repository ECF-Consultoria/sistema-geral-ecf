---
phase: 75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho
plan: 04
type: execute
wave: 2
depends_on: ["75-01", "75-02"]
files_modified:
  - app/Http/Controllers/ShopeeEmpresasController.php
  - routes/web.php
  - tests/Feature/Phase75/Phase75ShopeeEmpresasTest.php
  - tests/Feature/Phase75/Phase75NpsShopeeTest.php
autonomous: true
requirements: [DEC-2, DEC-4, DEC-5]

must_haves:
  truths:
    - "GET /shopee/empresas lista SÓ empresas com contrato ativo de setor 'shopee'"
    - "Empresa multi-marketplace (contrato ML E Shopee) aparece na aba Shopee"
    - "O payload NÃO contém cust_id/adman_account_id/ml_store_id/ml_token_status nem contagem de grants"
    - "Pendências retornadas = sem_responsavel / sem_contato / empresa_nova (e NADA de sem_cust_id/sem_grant_ativo)"
    - "sem_contato = email_cliente vazio E digisac_group_contact_id vazio"
    - "Atribuição grava pivot company_users: 'Analista'→role=consultor, 'Estrategista'→role=estrategista"
    - "Rota gated por permission:shopee.empresas: 403 sem a key, 200 para admin"
    - "Empresa Shopee (sem métrica) gera NPS via POST /nps/generate com fallback template is_default"
  artifacts:
    - path: "app/Http/Controllers/ShopeeEmpresasController.php"
      provides: "index (payload enxuto + pendências) + bulkAssign (pivot)"
      exports: ["index", "bulkAssign"]
      min_lines: 60
    - path: "routes/web.php"
      provides: "grupo permission:shopee.empresas com index + bulk-assign"
      contains: "shopee.empresas"
    - path: "tests/Feature/Phase75/Phase75ShopeeEmpresasTest.php"
      provides: "Prova de filtro, pendências, atribuição e gate"
      contains: "shopee"
    - path: "tests/Feature/Phase75/Phase75NpsShopeeTest.php"
      provides: "Prova de geração de NPS para empresa Shopee sem métrica"
      contains: "nps.generate"
  key_links:
    - from: "ShopeeEmpresasController::index"
      to: "contratos_servico setor=shopee"
      via: "whereHas contratosServico ativo + servico.setor=SETOR_SHOPEE"
      pattern: "SETOR_SHOPEE"
    - from: "routes/web.php shopee.empresas.*"
      to: "middleware permission:shopee.empresas"
      via: "Route::middleware('permission:shopee.empresas')"
      pattern: "permission:shopee.empresas"
    - from: "ShopeeEmpresasController::bulkAssign"
      to: "company_users pivot"
      via: "attach role=consultor|estrategista"
      pattern: "company_users"
---

<objective>
Construir o backend da aba "Empresas" da Shopee: um `ShopeeEmpresasController` que espelha `CompanyController@index` numa versão ENXUTA (filtro por contrato de setor `shopee`, sem qualquer payload de métrica/cust_id/grant), com pendências mínimas pro NPS (DEC-2) e atribuição de Analista/Estrategista via pivot; rotas dedicadas gated por `permission:shopee.empresas` (nunca `core.empresas`); e a prova de que uma empresa Shopee gera NPS pelo motor existente sem alteração (DEC-5).

Purpose: É o coração funcional da phase — expõe as empresas Shopee e as ações mínimas (atribuir responsável, gerar NPS) sem arrastar acoplamento a Adman/ML. RBAC dedicado é requisito de segurança (V4/Elevation of Privilege).
Output: Controller novo, grupo de rotas `shopee.empresas.*`, e dois arquivos de teste (aba + NPS).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-CONTEXT.md
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-RESEARCH.md
@.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-PATTERNS.md

<interfaces>
MOLDE — app/Http/Controllers/CompanyController.php:
  - construtor :23-27 injeta AdmanService/EcfDriveService/MetricsProviderFactory → NÃO injetar no controller Shopee
  - index() :76-243 — eager-loads consultor/estrategista/contratosServico.servico/grupo; filtro ML :108 whereDoesntHave('mlbEmpresa') → NÃO aplicar; :113-118 whereHas contratosServico setor=performance → trocar por SETOR_SHOPEE
  - :107 withCount grants → REMOVER; :103-104 mlToken → REMOVER
  - payload map :129-200 — REMOVER cust_id_status :138, adman_account_id/adman_store_id/ml_store_id :155-157, ml_token_status :172; MANTER id/name/cnpj/segment/active/status/email_cliente/telefone/empresa_nova/consultor/estrategista/contratos_servico/grupo
  - pendências :189-199 → substituir pelas 3 da DEC-2
  - usersPorCargo() :206-222 + $estrategistas/$analistas :221-222 → copiar verbatim; $grupos :225-227 opcional; $servicoCounts :234-243 → NÃO copiar
  - bulkAssign() :683-700 → copiar verbatim (valida role in:consultor,estrategista; ids.* exists; user_id exists)
  - update() sync pivot :617-628 → opcional (por linha)
  - userCanViewCompany() :49-66

Company.php:157-179 — users()/consultor()(role=consultor)/estrategista()(role=estrategista)
NpsDispararMensal.php:146-162 — sem_contato = email_cliente vazio E digisac_group_contact_id vazio (as DUAS condições)
NpsController::generate :353-412 — auth admin OU membro do pivot :370-375; template fallback is_default :390; NÃO lê métricas; link volta em flash.nps_link :410. Rota nps.generate (routes/web.php:100)
Servico::SETOR_SHOPEE — Plan 01; permission shopee.empresas — Plan 02
ROTAS analog — routes/web.php: grupo permission:core.empresas :502-504; companies.bulk-assign :613; companies.update :603
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Testes da aba Shopee (filtro, pendências, atribuição, gate) + NPS (RED)</name>
  <read_first>
    - tests/Feature/Phase37CompaniesPerformanceFilterTest.php:41-289 (molde quase 1:1 — helpers actingAsAdmin :41-52, criarServico :54-63, criarEmpresa :65-78, criarContrato :80-89, payloadCompanies :91-94; cenários de filtro e pendências)
    - app/Http/Controllers/CompanyController.php:113-118, :189-199, :683-700 (filtro, pendências, bulkAssign)
    - app/Console/Commands/NpsDispararMensal.php:146-162 (definição exata de sem_contato)
    - app/Http/Controllers/NpsController.php:353-412 (generate — auth + fallback template)
  </read_first>
  <behavior>
    - Phase75ShopeeEmpresasTest:
      · Filtro: empresa com contrato ativo setor='shopee' aparece; empresas com contrato só publicacao/outros, contrato shopee INATIVO, ou sem contrato NÃO aparecem.
      · Multi-marketplace: empresa com contrato ML (performance) E contrato Shopee APARECE na aba Shopee (contraste com o teste ML que exclui MlbEmpresa).
      · Payload enxuto: o array de cada empresa NÃO contém chaves cust_id_status/adman_account_id/ml_store_id/ml_token_status/grants_ativos_count.
      · Pendências: empresa sem responsável + sem email/digisac + empresa_nova retorna exatamente ['sem_responsavel','sem_contato','empresa_nova'] (ordem/subset conforme filtro); empresa com estrategista atribuído e email preenchido não retorna sem_responsavel nem sem_contato; NENHUM caso retorna 'sem_cust_id' ou 'sem_grant_ativo'.
      · sem_contato: só aparece quando email_cliente E digisac_group_contact_id ambos vazios; se um dos dois estiver preenchido, não aparece.
      · Atribuição: POST bulk-assign com role='consultor' cria pivot company_users role=consultor; role='estrategista' cria role=estrategista; role inválida → 422.
      · Gate: GET shopee.empresas.index → 403 para user sem a key; 200 para admin; 200 para user cujo setor tem a key.
    - Phase75NpsShopeeTest:
      · Empresa Shopee (sem adman/ml, sem métrica) com estrategista atribuído gera NPS: POST route('nps.generate') {company_id} retorna sucesso e cria NpsSurvey usando template is_default (fallback), sem exigir métrica.
  </behavior>
  <action>
    Criar `tests/Feature/Phase75/Phase75ShopeeEmpresasTest.php` e `tests/Feature/Phase75/Phase75NpsShopeeTest.php` (namespace `Tests\Feature\Phase75`, `RefreshDatabase`). Reaproveitar os helpers do Phase37CompaniesPerformanceFilterTest (copiar actingAsAdmin/criarServico/criarEmpresa/criarContrato/payloadCompanies, adaptando setor para 'shopee'). Para o gate, criar um user com setor que possua a key `shopee.empresas` (via SetorPermissao) e um user sem. Escrever todos os cenários de <behavior>. Comentários pt-BR. Deve falhar (RED): controller e rotas ainda não existem.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase75ShopeeEmpresasTest && php artisan test --filter=Phase75NpsShopeeTest</automated>
  </verify>
  <acceptance_criteria>Ambos os arquivos existem com todos os cenários; rodam e FALHAM (RED) por rota/controller ausentes — não por erro de setup.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: ShopeeEmpresasController (index enxuto + pendências + bulkAssign)</name>
  <read_first>
    - app/Http/Controllers/CompanyController.php:76-243 (index completo — copiar esqueleto), :683-700 (bulkAssign)
    - 75-PATTERNS.md secção "ShopeeEmpresasController.php" (lista exata de remoções/manutenções)
    - app/Models/Company.php:157-179 (relações do pivot)
  </read_first>
  <behavior>
    - index() retorna Inertia render de 'Shopee/Empresas' com props: companies (payload enxuto + pendencias), estrategistas, analistas (e grupos se aplicável).
    - Filtro: whereHas contratosServico ativo com servico.setor=Servico::SETOR_SHOPEE; SEM whereDoesntHave('mlbEmpresa').
    - Pendências por empresa: sem_responsavel (consultor E estrategista vazios), sem_contato (email_cliente vazio E digisac_group_contact_id vazio), empresa_nova (flag).
    - bulkAssign() valida role in:consultor,estrategista + ids.* exists + user_id exists; sincroniza pivot (delete role antigo do company + attach com assigned_at).
  </behavior>
  <action>
    Criar `app/Http/Controllers/ShopeeEmpresasController.php` (namespace App\Http\Controllers). Construtor VAZIO (não injetar AdmanService/MetricsProviderFactory/EcfDriveService). `index()`: copiar o esqueleto de CompanyController::index removendo tudo que é ML/métrica conforme a PATTERNS (sem withCount grants, sem mlToken, sem whereDoesntHave mlbEmpresa; filtro por SETOR_SHOPEE; payload map sem cust_id_status/adman_account_id/adman_store_id/ml_store_id/ml_token_status; pendências = as 3 da DEC-2; copiar usersPorCargo() e $estrategistas/$analistas verbatim; NÃO copiar $servicoCounts). `Inertia::render('Shopee/Empresas', [...])`. `bulkAssign()`: copiar de CompanyController::bulkAssign verbatim (mesma validação e sync do pivot). Opcional: `update()` por linha reusando o sync de CompanyController::update :617-628. Comentários pt-BR. NÃO reabrir core.empresas.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase75ShopeeEmpresasTest</automated>
  </verify>
  <acceptance_criteria>Cenários de filtro, multi-marketplace, payload enxuto, pendências e atribuição verdes (o gate depende das rotas — Tarefa 3).</acceptance_criteria>
</task>

<task type="auto">
  <name>Tarefa 3: Rotas shopee.empresas.* gated por permission:shopee.empresas</name>
  <read_first>
    - routes/web.php:502-504 (grupo permission:core.empresas — analog), :603-613 (companies.update/bulk-assign)
    - app/Http/Middleware/EnsurePermission.php (enforcement do middleware permission:)
  </read_first>
  <action>
    Em `routes/web.php`, adicionar um grupo `Route::middleware('permission:shopee.empresas')->group(...)` com: `GET /shopee/empresas` → `ShopeeEmpresasController@index` name `shopee.empresas.index`; `POST /shopee/empresas/bulk-assign` → `bulkAssign` name `shopee.empresas.bulk-assign`; (se `update()` foi implementado) `PUT /shopee/empresas/{company}` → `update` name `shopee.empresas.update`. Importar o controller no topo. NÃO usar `permission:core.empresas`. Posicionar de forma coerente com os demais grupos por permission. Comentário pt-BR.
  </action>
  <verify>
    <automated>php artisan route:list --name=shopee.empresas && php artisan test --filter=Phase75ShopeeEmpresasTest && php artisan test --filter=Phase75NpsShopeeTest</automated>
  </verify>
  <acceptance_criteria>`route:list` mostra shopee.empresas.index/bulk-assign com middleware permission:shopee.empresas; todos os cenários das duas suítes (incluindo gate 403/200 e NPS) verdes.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → GET /shopee/empresas | Requisição autenticada precisa da key shopee.empresas |
| cliente → POST bulk-assign | company_id/user_id/role vindos do cliente cruzam para o pivot |
| aba Shopee → POST /nps/generate | company_id cruza para o motor NPS (auth própria já existe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-09 | Elevation of Privilege | Usuário Shopee acessando CRUD core de /companies | mitigate | Rotas dedicadas gated por `permission:shopee.empresas`, jamais `core.empresas` (DEC-4); teste de gate prova 403 sem a key |
| T-75-10 | Tampering (IDOR) | bulkAssign com company_id fora do escopo | mitigate | Validação `ids.* exists:companies,id` + `user_id exists` + `role in:consultor,estrategista`; endpoint é admin-equivalente (gated pela key). Considerar restringir a empresas com contrato Shopee se o Setor Shopee não for admin — avaliar no controller |
| T-75-11 | Information Disclosure | Payload vazando dados ML/métrica | mitigate | Payload enxuto sem cust_id/adman/ml/token/grants; teste assertN as chaves ausentes |
| T-75-12 | Elevation of Privilege | Geração de NPS por não-autorizado | accept | NpsController::generate :370-375 já valida admin OU membro do pivot; motor NPS intocado (DEC-5) |
| T-75-13 | Spoofing | role arbitrária no bulkAssign | mitigate | `role in:consultor,estrategista` rejeita qualquer outro valor (422) |
</threat_model>

<verification>
- `php artisan test --filter=Phase75ShopeeEmpresasTest` e `--filter=Phase75NpsShopeeTest` verdes.
- `php artisan route:list --name=shopee.empresas` mostra o grupo com middleware correto.
- Regressão: `php artisan test --filter=Companies` e `--filter=Nps` sem novas falhas.
</verification>

<success_criteria>
Aba Shopee lista apenas empresas de contrato setor shopee (multi-marketplace incluída), payload ML-free, pendências DEC-2 corretas, atribuição no pivot funcionando, gate RBAC provado, e NPS gerável para empresa Shopee sem métrica.
</success_criteria>

<output>
Criar `.planning/phases/75-empresas-shopee-habilitar-nps-para-clientes-atendidos-na-sho/75-04-SUMMARY.md` ao concluir.
</output>
