---
phase: 127-service-administrativo-de-contrato-orquestra-o-v22-0
plan: 07
type: execute
wave: 5
depends_on: ["127-05", "127-06"]
files_modified:
  - .planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md
  - .planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md
autonomous: false
requirements: [CLICK-08, DADOS-06]

must_haves:
  truths:
    - "Sabemos, por medição, se o prazo definido na criação sobrevive à ativação feita pela interface web"
    - "A conta de produção só é consultada com autorização explícita do usuário, registrada"
    - "O modelo .docx de produção teve suas variáveis confrontadas antes de qualquer contrato de cliente"
  artifacts:
    - path: ".planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md"
      provides: "Registro das 3 medições com resposta HTTP observada e veredito"
      contains: "MEDIDO"
  key_links:
    - from: "127-GATE.md"
      to: ".planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md"
      via: "seção nova com as medições desta fase"
      pattern: "Fase 127"
---

<objective>
Fechar os **3 gates da fase** — as coisas que só param de ser palpite depois de medidas contra a API
real — e registrar o resultado onde a próxima fase vai procurar.

Purpose: quatro bugs desta milestone nasceram do mesmo erro (modelar a partir da resposta e presumir
que a mesma forma vale na entrada; o `Http::fake()` confirma alegremente payload errado). Os três
itens abaixo são exatamente os que a suíte **não consegue** provar.

Output: `127-GATE.md` com resposta HTTP observada por item + veredito, e a seção correspondente
acrescentada ao empírico.

⚠️ **A conta de PRODUÇÃO nunca foi consultada — nenhuma vez.** Consultar exige autorização explícita
do usuário nesta sessão, e é a razão de este plano existir como checkpoint bloqueante.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-CONTEXT.md
@.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md
@.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-05-SUMMARY.md
@.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Montar, no SANDBOX, um envelope completo com prazo customizado</name>
  <files>.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md</files>
  <action>
    Automatizar tudo o que é automatizável antes de pedir qualquer coisa ao humano.

    Confirmar que o `.env` local aponta para **sandbox** (`CLICKSIGN_ENV`/`CLICKSIGN_BASE_URL` com
    `sandbox`) — se não estiver, PARAR e reportar; não trocar credencial por conta própria.

    Rodar o fluxo real desta fase contra o sandbox, com uma empresa de teste e prazo curto
    (ex.: 10 dias / lembrete 7 — os mesmos valores já medidos em 11/08/2026, para comparar maçã com
    maçã): `ContratoClicksignService::iniciarParaEmpresa($company, prazoDias: 10, lembreteDias: 7)`
    via `artisan tinker --execute=...`, com o job processado (execução síncrona serve).

    Registrar em `127-GATE.md`, para cada item: a requisição enviada, a **resposta HTTP observada**
    (anonimizada — nunca colar e-mail, telefone, IP real ou chave real; usar faixa RFC 5737 e UUID
    de exemplo, como já é praxe no empírico) e o veredito.
    Confirmar e anotar:
    - `deadline_at` devolvido bate com hoje + 10 dias;
    - `remind_interval` devolvido é 7;
    - o envelope ficou em `draft` (não ativou — D-02);
    - contagem total de chamadas do envelope (esperado: ≤ 15).

    Anotar o `envelope_id` do sandbox — o humano vai precisar dele no Task 2.
  </action>
  <verify>
    <automated>grep -c "MEDIDO" .planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md</automated>
  </verify>
  <done>`127-GATE.md` traz a resposta observada da criação, com `deadline_at`/`remind_interval` conferidos e o `envelope_id` do sandbox anotado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: GATE 1 — o prazo sobrevive à ativação feita pela interface web?</name>
  <files>.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md</files>
  <what-built>
    Um envelope **completo** (documento gerado do modelo, 4 signatários, 8 requisitos) já existe na
    conta **sandbox**, em `draft`, criado com prazo de 10 dias e lembrete a cada 7 — os valores
    foram aceitos e devolvidos pela API na criação (Task 1).

    Isto é o que a D-03 **ainda não sabe**: se ativar o envelope pela interface web da Clicksign
    (que é o que o Comercial vai fazer, por causa da D-02) **preserva** esse prazo ou o
    sobrescreve pelo padrão de 30/3. A sonda anterior deu 422 porque o envelope de teste estava
    vazio — limitação do teste, não da API.
  </what-built>
  <how-to-verify>
    1. Abrir a conta **sandbox** da Clicksign no navegador e localizar o envelope `{envelope_id}`
       anotado no `127-GATE.md`.
    2. Conferir, antes de ativar, se o prazo exibido é o de 10 dias.
    3. Ativar o envelope pela interface (é o gesto que o Comercial fará).
       ⚠️ Isto dispara e-mail para os signatários de teste — confirmar que são endereços de teste,
       não de cliente real.
    4. Avisar aqui quando tiver ativado: eu consulto o envelope pela API e comparo `deadline_at` e
       `remind_interval` antes × depois, registrando o resultado no `127-GATE.md`.
    5. Se o prazo **não** sobreviver, isto é achado bloqueante para a Fase 129/131: o prazo
       precisaria ser reaplicado na ativação, o que hoje ninguém faz — registrar como pendência
       explícita, não como detalhe.
  </how-to-verify>
  <action>
    Pausar e apresentar o `envelope_id` ao usuário. Depois do sinal de retomada, consultar o
    envelope pela API (`ClicksignClient::consultarEnvelope()`), comparar
    `deadline_at`/`remind_interval` antes × depois da ativação e gravar o veredito no `127-GATE.md`
    como **MEDIDO**; se o usuário pular, registrar como **NÃO MEDIDO** com a consequência escrita
    por extenso (o prazo customizado do DADOS-06 fica sem garantia após a ativação humana).
  </action>
  <resume-signal>Responda "ativei" (com o id, se for outro envelope) ou "pular gate 1" para registrar como NÃO MEDIDO e seguir</resume-signal>
  <verify>
    <automated>grep -c "GATE 1" .planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md</automated>
  </verify>
  <done>`127-GATE.md` registra se o prazo definido na criação sobreviveu à ativação pela interface — ou registra explicitamente que segue NÃO MEDIDO, com a consequência.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: GATE 2 — autorização para consultar a conta de PRODUÇÃO</name>
  <files>.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md</files>
  <decision>Consultar, pela primeira vez, a conta de produção da Clicksign para validar o `CLICKSIGN_TEMPLATE_ID` e listar os modelos cadastrados</decision>
  <context>
    A conta de produção **nunca foi consultada** — nem uma vez, em toda a milestone. Tudo que
    sabemos veio do sandbox, e sandbox e produção têm UUIDs de modelo diferentes. Sem esta
    validação, a Fase 132 (cutover) vai descobrir na hora do uso real se o `CLICKSIGN_TEMPLATE_ID`
    de produção existe e aponta para o modelo certo.

    A consulta pretendida é **somente leitura** (`clicksign:sondar-modelo --listar` apontado para
    produção — conferir a flag real com `artisan clicksign:sondar-modelo --help` antes de rodar).
    Nenhuma criação, nenhuma exclusão, nenhum envelope. Ainda assim, exige autorização explícita
    porque toca a conta que emite contrato de cliente.
  </context>
  <options>
    <option id="autorizar-agora">
      <name>Autorizar a consulta somente-leitura agora</name>
      <pros>Fecha o gate 2 e destrava o gate 3 nesta fase; a Fase 132 (cutover) começa sabendo o que existe lá</pros>
      <cons>Exige as credenciais de produção disponíveis na máquina para a consulta</cons>
    </option>
    <option id="adiar-para-132">
      <name>Adiar para a Fase 132 (cutover)</name>
      <pros>Nada de produção é tocado nesta fase</pros>
      <cons>Os gates 2 e 3 ficam abertos e entram como risco declarado da fase; a Fase 132 herda a incerteza</cons>
    </option>
  </options>
  <action>
    Pausar e apresentar a decisão. Se `autorizar-agora`: conferir a flag real do comando com
    `artisan clicksign:sondar-modelo --help`, executar **somente leitura** contra produção e colar a
    saída anonimizada no `127-GATE.md`. Se `adiar-para-132`: registrar no `127-GATE.md` como
    pendência herdada pela Fase 132 e **não executar nada** contra produção.
    Em nenhum dos caminhos rodar deploy ou comando no VPS.
  </action>
  <resume-signal>Selecione: autorizar-agora ou adiar-para-132</resume-signal>
  <verify>
    <automated>grep -c "GATE 2" .planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md</automated>
  </verify>
  <done>A decisão do usuário está registrada no `127-GATE.md`, junto com a saída da consulta (se autorizada) ou a pendência declarada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: GATE 3 — confronto de variáveis do modelo de produção</name>
  <files>.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md</files>
  <what-built>
    Se o Task 3 autorizou: rodei `clicksign:sondar-modelo` contra produção e o `127-GATE.md` traz a
    tabela de confronto entre as `{{variaveis}}` do `.docx` cadastrado lá e os nomes que
    `ContratoVariaveisModeloService::nomes()` emite.

    Por que este gate não é opcional: **variável faltando vira campo em BRANCO no contrato, sem erro
    nenhum** (§10.5, medido). A API aceita 3 de 7 variáveis e devolve 201, e o documento gerado não
    deixa nenhum `{{marcador}}` cru — só um espaço vazio. Não existe resposta HTTP que denuncie. O
    confronto é a única rede.

    O CONTEXT também registra que o modelo cadastrado em produção é a versão **sem os nomes no
    rodapé** — precisa ser recadastrado antes de valer.
  </what-built>
  <how-to-verify>
    1. Ler a tabela de confronto no `127-GATE.md`: nenhuma variável do nosso mapa pode estar
       ausente no `.docx`, e nenhuma `{{variavel}}` do `.docx` pode estar fora do nosso mapa.
    2. Se houver divergência: recadastrar o `.docx` corrigido na Clicksign (trabalho humano no Word),
       me avisar, e eu rodo o confronto de novo.
    3. Confirmar que a regra fica escrita: **toda vez que o `.docx` for recadastrado, rodar o
       confronto antes de gerar contrato de cliente.**
    4. ⚠️ Nenhum deploy e nenhum comando no VPS acontecem nesta fase sem uma autorização sua
       específica, separada desta.
  </how-to-verify>
  <action>
    Pausar e apresentar a tabela de confronto (ou a razão de ela não existir, se o gate 2 foi
    adiado). Depois do sinal de retomada, registrar o veredito no `127-GATE.md` e, se o usuário
    recadastrar o `.docx`, rodar o confronto de novo antes de fechar.
  </action>
  <resume-signal>Responda "confronto ok", "recadastrei — rodar de novo", ou "adiar" para registrar como pendência da Fase 132</resume-signal>
  <verify>
    <automated>grep -c "GATE 3" .planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md</automated>
  </verify>
  <done>Tabela de confronto aprovada e registrada, ou pendência explicitamente declarada para a Fase 132.</done>
</task>

<task type="auto">
  <name>Task 5: Registrar as medições no empírico</name>
  <files>.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md, .planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md</files>
  <action>
    Acrescentar ao `CLICKSIGN-SANDBOX-EMPIRICO.md` uma seção nova (`## 11. Quarta sessão — gates da
    Fase 127`) com o que foi efetivamente observado, no mesmo estilo das seções 9 e 10: requisição,
    resposta, veredito, e uma lista honesta de **"ainda não medido"** para o que ficou aberto.
    Marcar cada item como **MEDIDO** ou **NÃO MEDIDO** e, nos payloads, como **ENTRADA** ou
    **SAÍDA** — a regra existe justamente porque quatro bugs desta milestone nasceram de presumir
    forma de payload.
    ⚠️ **Anonimizar antes de colar qualquer resposta**: já houve incidente neste arquivo (IP público
    real e chave real de signatário viraram PII permanente no histórico do git, achado WR-07 do code
    review da Fase 125). IP de exemplo da faixa RFC 5737, UUIDs de exemplo.
    Fechar o `127-GATE.md` com o veredito dos 3 gates e as pendências que sobrarem para a Fase 132.
  </action>
  <verify>
    <automated>grep -c "Fase 127" .planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md</automated>
  </verify>
  <done>O empírico tem a seção da Fase 127 com veredito por gate, e o `127-GATE.md` lista as pendências herdadas pela Fase 132.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|---|---|
| ambiente local → conta de PRODUÇÃO da Clicksign | Conta que emite contrato de cliente real; nunca foi tocada |
| resposta de API → documento de pesquisa versionado no git | Já houve vazamento de PII por cópia literal |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|---|---|---|---|---|
| T-127-22 | Tampering | conta de produção | mitigate | Consulta somente-leitura, e só após autorização explícita registrada (Task 3) |
| T-127-23 | Information Disclosure | `CLICKSIGN-SANDBOX-EMPIRICO.md` | mitigate | Anonimização obrigatória antes de colar resposta (precedente WR-07) |
| T-127-24 | Repudiation | contrato com campo em branco chegando ao cliente | mitigate | Confronto de variáveis obrigatório a cada recadastro do `.docx` (Task 4) |
</threat_model>

<verification>
- `127-GATE.md` existe, com veredito dos 3 gates.
- Seção da Fase 127 presente no empírico, com itens marcados MEDIDO/NÃO MEDIDO.
- **Nenhum deploy e nenhum comando no VPS** foram executados nesta fase sem autorização específica.
</verification>

<success_criteria>
- Gate 1 respondido por medição (ou registrado explicitamente como NÃO MEDIDO, com a consequência escrita).
- Gate 2 só executado com autorização registrada.
- Gate 3 com tabela de confronto ou pendência declarada para a Fase 132.
</success_criteria>

<output>
Criar `.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-07-SUMMARY.md` ao terminar.
</output>
