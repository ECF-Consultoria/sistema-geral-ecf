---
phase: 127-service-administrativo-de-contrato-orquestra-o-v22-0
plan: 04
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - database/migrations/2026_08_12_100001_add_clicksign_template_id_to_servicos_table.php
  - app/Models/Servico.php
  - app/Models/ContratoAssinatura.php
  - config/services.php
  - .env.example
  - tests/Feature/Phase127/MigrationsFase127ConvencoesTest.php
  - tests/Feature/Phase127/ModeloPorServicoTest.php
autonomous: true
requirements: [CLICK-08, DADOS-06]

must_haves:
  truths:
    - "Cada serviço do catálogo pode apontar para o seu próprio modelo .docx na Clicksign"
    - "Serviço sem modelo próprio cai no CLICKSIGN_TEMPLATE_ID padrão, sem quebrar"
    - "Um contrato sem prazo escolhido usa o padrão de configuração; com prazo escolhido, usa o dele"
    - "Cadastrar um serviço novo com modelo próprio não exige deploy"
  artifacts:
    - path: "database/migrations/2026_08_12_100001_add_clicksign_template_id_to_servicos_table.php"
      provides: "Coluna clicksign_template_id em servicos"
      contains: "clicksign_template_id"
    - path: "app/Models/Servico.php"
      provides: "Resolução do modelo .docx do serviço, com fallback para a config"
      contains: "clicksignTemplateId"
    - path: "app/Models/ContratoAssinatura.php"
      provides: "prazoDiasEfetivo()/lembreteDiasEfetivo() — coluna do contrato ou padrão da config"
      contains: "prazoDiasEfetivo"
  key_links:
    - from: "app/Models/Servico.php"
      to: "config('services.clicksign.template_id')"
      via: "fallback quando a coluna do serviço está vazia"
      pattern: "services\\.clicksign\\.template_id"
    - from: "app/Models/ContratoAssinatura.php"
      to: "config('services.clicksign.prazo_dias_padrao')"
      via: "acessor de prazo efetivo"
      pattern: "prazo_dias_padrao"
---

<objective>
Resolver as duas configurações que a montagem do envelope vai precisar: **qual modelo `.docx` usar
para cada serviço** (D-21: um modelo por serviço; hoje existe um único `CLICKSIGN_TEMPLATE_ID`) e
**qual prazo/lembrete aplicar em cada contrato** (D-03 + DADOS-06).

Purpose: sem isto, a fase inteira só funciona para um serviço. O ROADMAP lista "escolher o modelo
pelo serviço" como **código que não existe** e o CONTEXT registra explicitamente que isso **não é
ideia diferida — é trabalho desta fase**.

Output: uma coluna no catálogo de serviços, dois acessores no model do contrato, as chaves de
configuração e a documentação delas no `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-CONTEXT.md
@.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-RESEARCH.md
@.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-01-SUMMARY.md
@app/Models/Servico.php
@app/Models/ContratoAssinatura.php
@config/services.php

<interfaces>
`config/services.php` → bloco `'clicksign'` já existente (linha 205):
`env`, `base_url`, `access_token`, `webhook_secret`, `api_user_email`, `max_upload_bytes`,
`template_id` (`env('CLICKSIGN_TEMPLATE_ID')`), `signatarios_ecf` (3 signatários fixos via `env()`).
⚠️ Sandbox e produção têm UUIDs de modelo DIFERENTES; valor real só no `.env`, nunca no
`.env.example` nem no repositório.

`ClicksignClient::ativarEnvelope(string $envelopeId, int $prazoDias = 30, int $lembreteDias = 3)` —
os defaults 30/3 estão hoje **hardcoded na assinatura** e são os valores MEDIDOS como padrão da
Clicksign (`deadline_at` = +30 dias, `remind_interval` = 3, §"Envelope" do empírico). Este plano os
promove a configuração; não alterar a assinatura do client.

`app/Models/Servico.php`: `$fillable = ['nome','valor_padrao','tipo_cobranca','ativo','setor']`,
`use LogsActivity` (não usa `HasFactory`), `getActivitylogOptions()`, `scopeActive()`.

`contrato_assinaturas` ganhou no plano 127-01: `prazo_dias` e `lembrete_dias`
(`unsignedSmallInteger` nullable; `null` = "usou o padrão da configuração").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes de resolução de modelo e de prazo</name>
  <files>tests/Feature/Phase127/ModeloPorServicoTest.php, tests/Feature/Phase127/MigrationsFase127ConvencoesTest.php</files>
  <behavior>
    `ModeloPorServicoTest` (`RefreshDatabase`):
    - Teste 1 — serviço com `clicksign_template_id` preenchido: `clicksignTemplateId()` devolve o
      UUID do serviço.
    - Teste 2 — serviço com a coluna vazia e `config('services.clicksign.template_id')` definido:
      devolve o valor da config (fallback).
    - Teste 3 — coluna vazia e config vazia: devolve `null` (quem decide o que fazer com isso é o
      job do plano 127-05, que precisa falhar com mensagem clara, não gerar contrato errado).
    - Teste 4 — dois serviços com modelos diferentes devolvem UUIDs diferentes: prova que a escolha
      é **por serviço**, não global.
    - Teste 5 — `prazoDiasEfetivo()`: contrato com `prazo_dias = 10` devolve 10; com `null` devolve
      `config('services.clicksign.prazo_dias_padrao')`.
    - Teste 6 — `lembreteDiasEfetivo()`: mesma dupla de casos com `lembrete_dias`.
    - Teste 7 — os defaults de configuração, sem `.env`, valem **30 e 3** (os valores medidos como
      padrão da Clicksign), garantindo que a promoção a config não mudou o comportamento.

    `MigrationsFase127ConvencoesTest`: acrescentar o caminho da migration nova em
    `caminhosMigrations()` — as mesmas asserções (sem `->enum(`, nome de índice ≤ 64 caracteres)
    passam a cobrir os dois arquivos da fase.
  </behavior>
  <action>
    Reusar os helpers do arquivo de convenções criado no plano 127-01 — não duplicar a classe.
    Serviços do catálogo já semeado (`Servico::query()->orderBy('id')->take(2)`), mesma regra dos
    planos anteriores.
    Para os testes de config, usar `config()->set(...)` no corpo do teste em vez de mexer em `.env`.
    RED esperado nos testes 1-7. Commit do RED.
  </action>
  <verify>
    <automated>"C:\xampp\php\php.exe" artisan test tests/Feature/Phase127/ModeloPorServicoTest.php 2>&1 | grep -E "FAIL|Tests:"</automated>
  </verify>
  <done>Testes existem e falham por método/coluna inexistentes.</done>
</task>

<task type="auto">
  <name>Task 2: Migration da coluna e resolução no model Servico</name>
  <files>database/migrations/2026_08_12_100001_add_clicksign_template_id_to_servicos_table.php, app/Models/Servico.php</files>
  <action>
    Migration: `$table->string('clicksign_template_id', 64)->nullable()->after('setor');` na tabela
    `servicos`. Nullable de propósito — serviço sem modelo próprio cai no padrão. **Sem índice novo**
    (não há leitura por esse campo), então a armadilha 1059 não se aplica; **sem `->enum(`**;
    **sem `nullOnDelete`**, então a 1830 não se aplica. Registrar as três em comentário, como a
    Fase 125 fez, e explicar em pt-BR: D-21 — cada serviço tem seu próprio contrato, com escopo
    específico (a cláusula 2.1 de Gestão de ADS não pode sair num contrato de Shopee), por isso o
    modelo é **dado**, não `if` no código: cadastrar serviço novo com modelo próprio não pode exigir
    deploy.
    `down()`: `dropColumn('clicksign_template_id')`.

    `Servico`:
    - `$fillable` += `'clicksign_template_id'`.
    - `public function clicksignTemplateId(): ?string` — devolve
      `filled($this->clicksign_template_id) ? $this->clicksign_template_id : config('services.clicksign.template_id')`,
      normalizando string vazia para `null`.
    - Docblock em pt-BR: sandbox e produção têm UUIDs diferentes; o valor de produção nunca entra no
      repositório; e **toda vez que o `.docx` for recadastrado, rodar `clicksign:sondar-modelo` antes
      de gerar contrato de cliente** — variável faltando vira campo em branco silencioso (§10.5 do
      empírico), não existe resposta HTTP que denuncie, o confronto é a única rede.
    - `getActivitylogOptions()`: incluir `clicksign_template_id` no `logOnly` se a lista for
      explícita — trocar o modelo de um serviço é mudança auditável.
  </action>
  <verify>
    <automated>"C:\xampp\php\php.exe" artisan test tests/Feature/Phase127/ModeloPorServicoTest.php tests/Feature/Phase127/MigrationsFase127ConvencoesTest.php</automated>
  </verify>
  <done>Testes 1-4 e o de convenções passam.</done>
</task>

<task type="auto">
  <name>Task 3: Prazo e lembrete configuráveis (D-03 / CLICK-08 / DADOS-06)</name>
  <files>config/services.php, .env.example, app/Models/ContratoAssinatura.php</files>
  <action>
    `config/services.php`, dentro do bloco `clicksign`:
    - `'prazo_dias_padrao' => (int) env('CLICKSIGN_PRAZO_DIAS', 30),`
    - `'lembrete_dias_padrao' => (int) env('CLICKSIGN_LEMBRETE_DIAS', 3),`
    Comentar em pt-BR: **30 e 3 são os valores MEDIDOS como padrão da Clicksign** (`deadline_at` =
    +30 dias, `remind_interval` = 3) — não são palpite. D-03: prazo e lembrete vão na **CRIAÇÃO** do
    envelope, não na ativação, porque a ativação acontece fora do sistema (D-02) e o valor se
    perderia. CLICK-08: o lembrete é nativo da Clicksign — **não criar scheduler próprio**,
    duplicaria notificação.

    `.env.example`: documentar `CLICKSIGN_PRAZO_DIAS=30` e `CLICKSIGN_LEMBRETE_DIAS=3`.
    ⚠️ **Não** preencher `CLICKSIGN_TEMPLATE_ID` com valor de exemplo (a regra já registrada em
    `config/services.php`: exemplo preenchido vira produção por engano).

    `ContratoAssinatura`:
    - `public function prazoDiasEfetivo(): int` → `$this->prazo_dias ?? (int) config('services.clicksign.prazo_dias_padrao')`.
    - `public function lembreteDiasEfetivo(): int` → idem com `lembrete_dias` / `lembrete_dias_padrao`.
    - Docblock: `null` na coluna significa "usou o padrão vigente na época"; o valor escolhido fica
      persistido para a **Fase 130** (alerta de contrato preso) conseguir calcular há quanto tempo é
      aceitável esperar — é por isso que persistimos em vez de só mandar no payload.
  </action>
  <verify>
    <automated>"C:\xampp\php\php.exe" artisan test tests/Feature/Phase127 tests/Feature/Phase125 tests/Feature/Phase126</automated>
  </verify>
  <done>Testes 5-7 passam; baseline Phase125+Phase126 continua verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|---|---|
| configuração/catálogo → documento jurídico | O UUID do modelo decide QUAL contrato o cliente assina |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|---|---|---|---|---|
| T-127-10 | Tampering (contrato errado ao cliente) | `servicos.clicksign_template_id` | mitigate | Resolução por serviço + activity log da mudança; o gate 3 do plano 127-07 confronta as variáveis do modelo antes de uso real |
| T-127-11 | Information Disclosure | UUID de modelo de produção no repositório | mitigate | `.env.example` fica SEM valor; leitura só por `env()`, como já é feito com `template_id` e `signatarios_ecf` |
| T-127-12 | Repudiation | prazo aplicado no contrato | mitigate | `prazo_dias`/`lembrete_dias` persistidos por contrato (DADOS-06), não só no envelope de terceiro |
</threat_model>

<verification>
- `"C:\xampp\php\php.exe" artisan test tests/Feature/Phase127 tests/Feature/Phase125 tests/Feature/Phase126` verde.
- `grep -n "CLICKSIGN_TEMPLATE_ID" .env.example` — se aparecer, tem que estar **sem valor**.
- Nenhuma chamada real à Clicksign; nenhum deploy.
</verification>

<success_criteria>
- Modelo resolvido por serviço, com fallback e sem `if` hardcoded por nome de serviço.
- Prazo/lembrete configuráveis e por contrato, com defaults iguais aos valores medidos.
</success_criteria>

<output>
Criar `.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-04-SUMMARY.md` ao terminar.
</output>
