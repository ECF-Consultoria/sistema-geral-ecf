---
phase: 127-service-administrativo-de-contrato-orquestra-o-v22-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_08_12_100000_add_servico_e_prazo_to_contrato_assinaturas_table.php
  - app/Models/ContratoAssinatura.php
  - database/factories/ContratoAssinaturaFactory.php
  - tests/Feature/Phase127/MigrationsFase127ConvencoesTest.php
  - tests/Feature/Phase127/ContratoAssinaturaServicoTest.php
  - tests/Feature/Phase125/ContratoAssinaturaSchemaTest.php
autonomous: true
requirements: [DADOS-06]

must_haves:
  truths:
    - "Uma empresa com 2 serviços ativos consegue ter 2 ContratoAssinatura em rascunho ao mesmo tempo"
    - "Uma empresa NÃO consegue ter 2 ContratoAssinatura em andamento do MESMO serviço"
    - "Um contrato guarda o prazo e o lembrete que foram usados, para a Fase 130 poder auditar"
    - "A suíte Phase125 + Phase126 continua verde (147 testes)"
  artifacts:
    - path: "database/migrations/2026_08_12_100000_add_servico_e_prazo_to_contrato_assinaturas_table.php"
      provides: "Colunas servico_id, servico_id_em_andamento, prazo_dias, lembrete_dias + índice único composto"
      contains: "ca_empresa_servico_andamento_uniq"
    - path: "app/Models/ContratoAssinatura.php"
      provides: "Hook saving preenchendo as DUAS colunas derivadas + emAndamentoDoServico()"
      contains: "emAndamentoDoServico"
    - path: "tests/Feature/Phase127/MigrationsFase127ConvencoesTest.php"
      provides: "Guarda estática das armadilhas de MariaDB para a migration desta fase"
  key_links:
    - from: "app/Models/ContratoAssinatura.php"
      to: "servico_id_em_andamento"
      via: "hook saving preenche junto com company_id_em_andamento"
      pattern: "servico_id_em_andamento"
    - from: "database/factories/ContratoAssinaturaFactory.php"
      to: "servicos"
      via: "servico_id resolvido do catálogo semeado por migration"
      pattern: "servico_id"
---

<objective>
Aplicar a **D-06**: a trava de unicidade de contrato em andamento deixa de ser por EMPRESA e passa a
ser por **(empresa + serviço)**. Sem isto, o segundo serviço de qualquer empresa bate na constraint
`ca_company_andamento_uniq` da Fase 125 **sempre** — não é corrida, é determinístico (Pitfall 1 da
pesquisa). Todo o resto desta fase depende deste plano.

Purpose: destravar a D-21 (um modelo `.docx` por serviço → N contratos por empresa) e a D-01
(gerar os N pela fila) sem reabrir o modelo de dados depois que existir contrato de cliente real —
as duas tabelas já estão em produção, mas **vazias** (verificado no CONTEXT), então esta é a janela
barata para a correção.

Output: uma migration nova, o model ajustado, a factory ajustada e a guarda estática de convenções
de schema desta fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-CONTEXT.md
@.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-RESEARCH.md
@app/Models/ContratoAssinatura.php
@database/migrations/2026_08_10_100000_create_contrato_assinaturas_table.php
@tests/Feature/Phase125/MigrationsFase125ConvencoesTest.php
@database/factories/ContratoAssinaturaFactory.php

<interfaces>
<!-- Contratos já existentes que o executor NÃO precisa ir procurar no código. -->

`app/Models/ContratoAssinatura.php` (Fase 125):
- `public const STATUS_RASCUNHO = 'rascunho'` … `STATUS_ERRO = 'erro'` (7 estados)
- `public const STATUS_EM_ANDAMENTO = [STATUS_RASCUNHO, STATUS_AGUARDANDO_ASSINATURAS]`
- `protected static function booted()` → hook `saving` que hoje preenche SÓ `company_id_em_andamento`
- `public static function emAndamentoDaEmpresa(int $companyId): ?self` — consulta por
  `company_id` + `whereIn('status', STATUS_EM_ANDAMENTO)` (a FONTE, não o espelho derivado)
- `$fillable` explícito (nunca `$guarded = []`, T-125-01); `$casts['servicos_snapshot'] = 'array'`

`contrato_assinaturas` hoje: `company_id` (FK, NOT NULL, cascadeOnDelete), `status` string(40),
`company_id_em_andamento` (unsignedBigInteger nullable, SEM FK, espelho derivado),
`clicksign_envelope_id`, `clicksign_document_id`, `enviado_em`, `assinado_em`, `liberado_em`,
`erro_mensagem`, `servicos_snapshot` (json), `pdf_path`, `pdf_assinado_path`, timestamps.
Índices: `ca_company_andamento_uniq` (unique, 1 coluna), `ca_clicksign_envelope_uniq`,
`ca_company_status_idx`.

`app/Models/Servico.php`: `$fillable = ['nome','valor_padrao','tipo_cobranca','ativo','setor']`;
`public const TIPO_MENSAL = 'mensal'`. **Não tem `HasFactory`** — não existe `ServicoFactory`.
O catálogo é semeado por migration (`2026_05_27_100001_seed_servicos_catalog.php`), então sob
`RefreshDatabase` a tabela `servicos` já vem populada.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes da D-06 (schema + hook composto) — escrever ANTES da migration</name>
  <files>tests/Feature/Phase127/ContratoAssinaturaServicoTest.php, tests/Feature/Phase127/MigrationsFase127ConvencoesTest.php</files>
  <behavior>
    `ContratoAssinaturaServicoTest` (usa `RefreshDatabase`):
    - Teste 1 — **o caso que hoje quebra**: uma empresa com DOIS serviços diferentes cria dois
      `ContratoAssinatura` em `rascunho` (um por serviço) e ambos persistem. Este é o teste que
      prova a D-06; com o schema da Fase 125 ele falha com `QueryException`.
    - Teste 2 — a trava continua existindo no recorte novo: dois `ContratoAssinatura` em `rascunho`
      para a MESMA empresa e o MESMO `servico_id` estouram `QueryException` (SQLSTATE `23000`).
      Usar `$this->expectException(\Illuminate\Database\QueryException::class)`.
    - Teste 3 — o hook preenche as duas colunas derivadas juntas: contrato salvo em `rascunho` tem
      `company_id_em_andamento === company_id` E `servico_id_em_andamento === servico_id`; ao mudar
      `status` para `assinado` e salvar, **as duas** viram `null` (recarregar com `fresh()`).
    - Teste 4 — sair de andamento libera o slot: depois que o contrato do serviço A vira
      `cancelado`, um contrato novo do serviço A na mesma empresa é criado sem erro.
    - Teste 5 — `emAndamentoDoServico($companyId, $servicoId)` devolve o contrato do serviço pedido
      e `null` para outro serviço da mesma empresa.
    - Teste 6 — `emAndamentoDaEmpresa($companyId)` (assinatura antiga, usada pela suíte da Fase 125)
      continua devolvendo um contrato quando existe qualquer um em andamento.

    `MigrationsFase127ConvencoesTest`: cópia adaptada de
    `tests/Feature/Phase125/MigrationsFase125ConvencoesTest.php` — **não roda banco**, lê o TEXTO da
    migration desta fase. Reusar o helper `migrationSemComentarios()` (remove blocos `/* */` e linhas
    `//` ANTES de qualquer checagem — sem isso a guarda se auto-invalida, porque a própria migration
    documenta as armadilhas nos comentários) e o helper `colunaDeclaradaNullable()`.
    Asserções sobre `database/migrations/2026_08_12_100000_add_servico_e_prazo_to_contrato_assinaturas_table.php`:
    - não contém `->enum(`;
    - todo nome de índice literal no arquivo tem no máximo 64 caracteres (armadilha 1059);
    - **o `->unique([...], 'ca_empresa_servico_andamento_uniq')` aparece ANTES do
      `dropUnique('ca_company_andamento_uniq')` no texto do arquivo** (armadilha 1553 — dropar índice
      usado por FK falha no MariaDB e o SQLite dos testes não pega). Comparar por
      `strpos()` das duas ocorrências no código sem comentários.
  </behavior>
  <action>
    Criar o diretório `tests/Feature/Phase127/` com namespace `Tests\Feature\Phase127`.
    Nos testes que precisam de empresa e serviços, usar `Company::factory()` e pegar dois serviços
    distintos do catálogo já semeado: `Servico::query()->orderBy('id')->take(2)->pluck('id')` —
    **não** criar `ServicoFactory`, o `Servico` não usa `HasFactory` e o catálogo vem por migration.
    Se a query devolver menos de 2 registros, criar os que faltarem com
    `Servico::create(['nome' => ..., 'valor_padrao' => 0, 'tipo_cobranca' => Servico::TIPO_MENSAL,
    'ativo' => true, 'setor' => Servico::query()->value('setor')])` — copiar o `setor` de um registro
    existente, nunca inventar um valor novo (a coluna é `enum` legado e o CHECK do SQLite derruba a
    suíte).
    Os testes DEVEM falhar agora (RED) — 1, 3 e 5 por schema/método inexistentes. Commit do RED.
  </action>
  <verify>
    <automated>"C:\xampp\php\php.exe" artisan test tests/Feature/Phase127 2>&1 | grep -E "FAIL|Tests:"</automated>
  </verify>
  <done>Os dois arquivos de teste existem, rodam, e falham pelos motivos certos (coluna/método ausentes) — não por erro de sintaxe ou classe não encontrada.</done>
</task>

<task type="auto">
  <name>Task 2: Migration da D-06 — coluna do serviço, prazo e índice composto</name>
  <files>database/migrations/2026_08_12_100000_add_servico_e_prazo_to_contrato_assinaturas_table.php</files>
  <action>
    Criar a migration com comentários em pt-BR explicando D-06 e as armadilhas (o texto explicativo
    é obrigatório: a guarda estática remove comentários antes de checar, então comentar não invalida
    o teste).

    `up()` sobre `contrato_assinaturas`, **nesta ordem exata**:
    1. `$table->foreignId('servico_id')->nullable()->after('company_id')->constrained('servicos')->restrictOnDelete();`
       — ⚠️ `nullable()` NÃO é frouxidão de modelagem: o SQLite dos testes recusa
       `ADD COLUMN ... NOT NULL` sem default ("Cannot add a NOT NULL column with default value
       NULL"), mesmo em tabela vazia. A obrigatoriedade real fica no model (Task 3) e no service da
       Fase 127. Registrar esse motivo no comentário. `restrictOnDelete` porque um contrato não pode
       sobreviver à exclusão do serviço que ele representa; não é `nullOnDelete`, então a armadilha
       1830 não se aplica aqui.
    2. `$table->unsignedBigInteger('servico_id_em_andamento')->nullable()->after('company_id_em_andamento');`
       — espelho derivado, **sem FK própria** (mesma escolha da coluna de empresa na Fase 125:
       a integridade já vem da FK de `servico_id`).
    3. `$table->unsignedSmallInteger('prazo_dias')->nullable();` e
       `$table->unsignedSmallInteger('lembrete_dias')->nullable();` — DADOS-06: o prazo escolhido
       fica auditável no nosso banco, não só dentro do envelope da Clicksign. `null` significa
       "usou o padrão da configuração" e é resolvido no acessor (plano 127-04). A Fase 130 (alerta
       de contrato preso) precisa saber o prazo real para calcular "há quanto tempo é aceitável
       esperar".
    4. Num segundo `Schema::table(...)`: **criar o índice novo ANTES de dropar o antigo** —
       `$table->unique(['company_id_em_andamento', 'servico_id_em_andamento'], 'ca_empresa_servico_andamento_uniq');`
       e SÓ DEPOIS `$table->dropUnique('ca_company_andamento_uniq');`.
       ⚠️ Esta ordem é a armadilha 1553 do MariaDB (dropar índice usado por FK falha) e o SQLite dos
       testes passa verde nas duas ordens — é regressão que só aparece no deploy.
    5. Manter o índice `ca_clicksign_envelope_uniq` e `ca_company_status_idx` intocados.

    `down()`: inverso simétrico — recriar `ca_company_andamento_uniq` ANTES de dropar
    `ca_empresa_servico_andamento_uniq`, dropar a FK antes da coluna
    (`$table->dropForeign(['servico_id'])` antes de `dropColumn`), depois as colunas novas.

    Nenhum `->enum(`. Todo nome de índice literal com ≤ 64 caracteres.
  </action>
  <verify>
    <automated>"C:\xampp\php\php.exe" artisan test tests/Feature/Phase127/MigrationsFase127ConvencoesTest.php</automated>
  </verify>
  <done>A guarda estática passa; `artisan migrate --pretend` não acusa erro de sintaxe; os testes de schema do Task 1 ainda podem falhar (o hook ainda não existe).</done>
</task>

<task type="auto">
  <name>Task 3: Model e factory acompanhando a chave composta</name>
  <files>app/Models/ContratoAssinatura.php, database/factories/ContratoAssinaturaFactory.php</files>
  <action>
    `ContratoAssinatura`:
    - `$fillable` += `'servico_id'`, `'servico_id_em_andamento'`, `'prazo_dias'`, `'lembrete_dias'`
      (sem isto o mass assignment falha EM SILÊNCIO — foi exatamente o alerta deixado pela Fase 126).
    - `$casts` += `'prazo_dias' => 'integer'`, `'lembrete_dias' => 'integer'`.
    - Hook `saving` em `booted()`: preencher as DUAS colunas derivadas na mesma passagem —
      em andamento → `company_id_em_andamento = company_id` **e** `servico_id_em_andamento =
      servico_id`; fora de andamento → **ambas** `null`. Atualizar o docblock gigante que já existe
      ali (ele diz "o hook é o que ALIMENTA a trava") para falar das duas colunas, e manter o aviso
      sobre `updateQuietly`/query builder — que agora precisa zerar **as duas** colunas na mesma
      query.
    - Guard de obrigatoriedade que o banco não pode dar (a coluna é `nullable()` por causa do
      SQLite): no mesmo hook `saving`, se `servico_id` estiver vazio, lançar
      `\RuntimeException("ContratoAssinatura exige servico_id (D-06) — um contrato representa UM serviço.")`.
    - Novo: `public static function emAndamentoDoServico(int $companyId, int $servicoId): ?self` —
      mesma disciplina do método existente: consultar por `company_id` + `servico_id` +
      `whereIn('status', self::STATUS_EM_ANDAMENTO)` (a FONTE), nunca pelas colunas espelho.
    - Manter `emAndamentoDaEmpresa()` como está (a suíte da Fase 125 usa) e ajustar o docblock para
      "qualquer serviço da empresa"; marcar no docblock que quem vai CRIAR contrato deve usar
      `emAndamentoDoServico()`.
    - Relação `public function servico(): BelongsTo` para `Servico::class`.
    - Docblock de classe: registrar que, pós-D-06, **um `ContratoAssinatura` representa UM serviço** e
      o `servicos_snapshot` passa a ter um único item.

    `ContratoAssinaturaFactory`:
    - `definition()` += `'servico_id' => fn () => $this->servicoDeTeste()`.
    - Helper privado `servicoDeTeste(): int` — `Servico::query()->value('id')` e, se vier `null`,
      criar um registro mínimo copiando o `setor` de um existente (mesma regra do Task 1: nunca
      inventar valor de `setor`, o CHECK do SQLite derruba a suíte).
    - `comSnapshot()`: manter a assinatura, mas trocar o default de 3 serviços por **um único item**
      (`'servico' => 'Gestão de Tráfego — Mercado Livre'` etc.), refletindo a D-06. Conferir se a
      suíte da Fase 126 depende do default de 3 itens — se depender, ajustar os testes afetados para
      passar a lista explicitamente em vez de mudar a semântica do model.
  </action>
  <verify>
    <automated>"C:\xampp\php\php.exe" artisan test tests/Feature/Phase127 tests/Feature/Phase125 tests/Feature/Phase126</automated>
  </verify>
  <done>Todos os testes do Task 1 passam (GREEN) e a baseline Phase125+Phase126 continua verde — 147 testes, zero regressão.</done>
</task>

<task type="auto">
  <name>Task 4: Adaptar os 2 testes da Fase 125 que a D-06 quebra POR CONSTRUÇÃO</name>
  <files>tests/Feature/Phase125/ContratoAssinaturaSchemaTest.php</files>
  <read_first>
    - `tests/Feature/Phase125/ContratoAssinaturaSchemaTest.php` — só os dois métodos citados abaixo.
  </read_first>
  <action>
    ⚠️ **Esta task existe porque a D-06 muda a SEMÂNTICA do índice, não só o nome.** Dois testes hoje
    verdes da Fase 125 quebram por construção, e nenhum deles é regressão sua — ignorar isso faria o
    `<done>` do Task 3 ("147 verdes, zero regressão") ser impossível de cumprir.

    **1. `indices_tem_nome_explicito_e_cabem_em_64_caracteres()`** (~linha 47)
    Ele faz `assertContains('ca_company_andamento_uniq', $nomes)`, e a Task 2 dropa exatamente esse
    índice. Trocar a asserção para o índice novo (`ca_empresa_servico_andamento_uniq`), mantendo as
    outras duas (`ca_clicksign_envelope_uniq`, `ca_company_status_idx`) e o laço dos 64 caracteres
    intactos — é ele que guarda a armadilha 1059.

    **2. `indice_unico_barra_segundo_contrato_em_andamento()`** (~linha 62)
    Ele insere DUAS vezes via `DB::table()->insert()` com o mesmo `company_id_em_andamento` e espera
    `QueryException`. Com a chave composta isso **para de lançar exceção**, e o motivo não é óbvio:
    o insert cru não preenche `servico_id_em_andamento`, então as duas linhas ficam
    `(company_id, NULL)` — e **índice único com NULL numa das colunas nunca rejeita duplicata**, em
    SQL padrão, tanto no SQLite dos testes quanto no MariaDB de produção.

    Reescrever preenchendo `servico_id_em_andamento` com o MESMO `servico_id` nos dois inserts, para
    que a trava composta seja de fato exercitada. Manter o insert cru (`DB::table()`) de propósito:
    o valor deste teste é justamente provar que a trava é **do banco**, não do hook do model.

    **Acrescentar um terceiro caso** que trava a descoberta acima como comportamento conhecido:
    mesmo `company_id_em_andamento` com `servico_id_em_andamento` **diferentes** → os dois inserts
    passam, sem exceção. É a prova executável de que a D-06 permite N contratos por empresa (um por
    serviço), que é o objetivo inteiro desta migration.

    Renomear o teste alterado para refletir a nova regra
    (ex.: `indice_unico_barra_segundo_contrato_do_MESMO_servico`), e comentar no docblock que a
    mudança veio da D-06, com a data — para uma auditoria futura não ler como afrouxamento de teste.
  </action>
  <verify>
    <automated>"C:\xampp\php\php.exe" artisan test tests/Feature/Phase125</automated>
  </verify>
  <done>
    A suíte da Fase 125 volta ao verde com a semântica nova, e existe teste provando que dois
    contratos da MESMA empresa em serviços DIFERENTES convivem — o que a D-06 exige.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|---|---|
| aplicação → banco | O invariante "um contrato em andamento por empresa+serviço" é dado como certo por todo o resto da fase |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|---|---|---|---|---|
| T-127-01 | Tampering | `contrato_assinaturas` — colunas derivadas | mitigate | Hook `saving` preenche as duas colunas juntas; teste 3 do Task 1 prova a sincronia nos dois sentidos |
| T-127-02 | Denial of Service (auto-infligido) | índice `ca_empresa_servico_andamento_uniq` | mitigate | Teste 4 prova que sair de andamento libera o slot — sem ele, empresa presa para sempre sem contrato novo |
| T-127-03 | Elevation of Privilege | mass assignment em `ContratoAssinatura` | mitigate | `$fillable` explícito estendido; nunca `$guarded = []` (T-125-01) |
| T-127-SC | Tampering | instalação de pacotes | accept | Nenhum pacote novo nesta fase (pesquisa: "Standard Stack — nenhuma biblioteca nova"); nada a auditar |
</threat_model>

<verification>
- `"C:\xampp\php\php.exe" artisan test tests/Feature/Phase127 tests/Feature/Phase125 tests/Feature/Phase126` verde.
- `"C:\xampp\php\php.exe" artisan migrate --pretend` mostra o `create unique index` ANTES do
  `drop index` do antigo.
- Nenhum deploy nesta tarefa. A migration só roda em produção depois do gate do plano 127-07.
</verification>

<success_criteria>
- Empresa com 2 serviços ativos suporta 2 contratos em rascunho simultâneos.
- Empresa + mesmo serviço em andamento duas vezes = `QueryException` 23000.
- Baseline de 147 testes (Phase125 + Phase126) intacta.
</success_criteria>

<output>
Criar `.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-01-SUMMARY.md` ao terminar.
</output>
