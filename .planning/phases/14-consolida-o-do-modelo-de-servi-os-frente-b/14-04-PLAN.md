---
phase: 14-consolida-o-do-modelo-de-servi-os-frente-b
plan: 04
type: execute
wave: 2
depends_on:
  - 14-02
  - 14-03
files_modified:
  - app/Http/Controllers/ComercialController.php
  - resources/js/Pages/Comercial/NovaEmpresa.jsx
  - tests/Feature/Phase14ComercialTest.php
  - tests/Unit/ComercialControllerHelperTest.php
autonomous: true
requirements:
  - SVC-05
must_haves:
  truths:
    - "ComercialController::store recebe array `servicos[]` (lista de servico_id + valor_contratado opcional) em vez de `service_type` enum"
    - "ComercialController cria atomicamente em DB::transaction: companies + N contratos_servico + (opcional) mlb_empresas + mlb_implementacao"
    - "Helper estático `ComercialController::servicoDisparaImplementacao(string $nome): ?string` retorna 'polos'/'assessoria'/'incubadora'/null conforme str_contains do nome"
    - "Roteamento Phase 13 PRESERVADO: cadastrar com serviço 'Polos' ainda cria mlb_empresas + mlb_implementacao (COM-04); com 'Publicidade' não cria mlb (COM-06)"
    - "NovaEmpresa.jsx form substitui checkbox service_type por seletor multi do catálogo de Servicos ativos"
    - "EmpresaCadastradaNotification chamada com `array $servicosNomes` (não mais string com implode '+')"
    - "ComercialController::update() valida APENAS name, cnpj, notes — os 6 campos legacy não são mais persistidos"
  artifacts:
    - path: "app/Http/Controllers/ComercialController.php"
      provides: "store() refatorado + helper servicoDisparaImplementacao"
      contains: "servicoDisparaImplementacao"
    - path: "resources/js/Pages/Comercial/NovaEmpresa.jsx"
      provides: "Seletor multi do catálogo (substituindo TIPOS hardcoded)"
      contains: "servicos_disponiveis"
    - path: "tests/Feature/Phase14ComercialTest.php"
      provides: "Roteamento por nome de serviço — preserva COM-04/05/06"
      contains: "class Phase14ComercialTest"
    - path: "tests/Unit/ComercialControllerHelperTest.php"
      provides: "Helper de roteamento puro testado isoladamente"
      contains: "class ComercialControllerHelperTest"
  key_links:
    - from: "app/Http/Controllers/ComercialController.php"
      to: "App\\Models\\Servico (catálogo)"
      via: "valida servicos[].servico_id como exists:servicos,id E ativo=true"
      pattern: "exists:servicos"
    - from: "app/Http/Controllers/ComercialController.php"
      to: "App\\Models\\ContratoServico"
      via: "create dentro de DB::transaction"
      pattern: "ContratoServico::create"
    - from: "resources/js/Pages/Comercial/NovaEmpresa.jsx"
      to: "ComercialController::create (route comercial.empresas.novo)"
      via: "prop servicos_disponiveis do catálogo"
      pattern: "servicos_disponiveis"
---

<objective>
Refatorar o cadastro Comercial: ComercialController::store passa a aceitar lista de `servicos[]` (do catálogo da Frente A) em vez do enum `service_type`, criando atomicamente companies + N contratos_servico + (opcionalmente) mlb_empresas + mlb_implementacao via roteamento por nome de serviço (helper puro testável). A UI `NovaEmpresa.jsx` substitui os 3 checkboxes hardcoded por seletor multi do catálogo. Roteamento Phase 13 (COM-04, COM-05, COM-06) preservado.

Purpose: SVC-05 — Cadastro Comercial passa a usar o modelo unificado de serviços, fechando o ciclo de produção de dados pelo modelo novo. Sem este plan, novas empresas continuariam produzindo dados legacy que precisariam ser migrados depois.

Output: 1 controller refatorado + 1 página JSX refatorada + 2 suítes de teste. Commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md

<interfaces>
<!-- ComercialController atual (refatorar) -->

ComercialController.php:70-140 (store):
```
Validation atual: 'service_type' => 'required|array|min:1', 'service_type.*' => 'in:publicacao,publicidade,gestao,incubadora'
Cria Company::create([...'service_type' => $types, 'status' => 'pendente', ...])
Notifica via EmpresaCadastradaNotification($company->name, implode('+', $company->service_type), $userId)
```

ComercialController.php:212-222 (resolverSlugsSetores — helper privado):
```
polos/assessoria/publicacao → slug 'publicacao'
Outros → Str::slug($type)
```

ComercialController.php:234-254 (criarImplementacaoPolo — helper privado):
```
Cria MlbImplementacao a partir de dadosPadrao() + implementacaoPadroes()
```

<!-- Helper a criar (D-05) -->

```
public static function servicoDisparaImplementacao(string $nome): ?string {
    return match(true) {
        str_contains($nome, 'Polos')      => 'polos',
        str_contains($nome, 'Assessoria') => 'assessoria',
        str_contains($nome, 'Incubadora') => 'incubadora',
        default                            => null,
    };
}
```

<!-- NovaEmpresa.jsx atual (refatorar) -->

Linhas 22-29: const TIPOS = [{value: 'publicacao', label, hint}, {value: 'publicidade', ...}, {value: 'gestao', ...}]
Linhas 31-51: useForm({nome, cnpj, service_type: []})
Linhas 41-43: toggleTipo via service_type array

<!-- Servico catálogo (Frente A — usar aqui) -->

`Servico` model — campos: id, nome, valor_padrao, tipo_cobranca, ativo
Scope `active()` filtra `where('ativo', true)`

<!-- Phase 13 success criteria a PRESERVAR -->

COM-04: Polos cria companies + mlb_empresas (tipo=POLO, projeto=POLOS) + mlb_implementacao (token + dadosPadrao + implementacaoPadroes)
COM-05: Assessoria cria companies + mlb_empresas (tipo=ASSESSORIA)
COM-06: Publicidade/Gestão cria apenas companies (sem mlb_empresas)

<!-- EmpresaCadastradaNotification (refatorado no Plan 14-03 para aceitar string|array) -->

```
public function __construct(string $nomeEmpresa, string|array $servicos, ?int $autorUserId)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar ComercialController — adicionar helper servicoDisparaImplementacao + reescrever store() + atualizar empresas()/create()/update()</name>
  <files>app/Http/Controllers/ComercialController.php</files>
  <read_first>
    - app/Http/Controllers/ComercialController.php (arquivo completo — 256 LOC)
    - app/Models/MlbEmpresa.php (struct para criar polos/assessoria)
    - app/Models/MlbImplementacao.php (dadosPadrao + criarImplementacaoPolo helper existente)
    - app/Models/Servico.php (catálogo)
    - app/Models/ContratoServico.php (campos fillable)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-05 form refator)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §7 §9 (testes esperados + refator notification)
  </read_first>
  <action>
    Editar `app/Http/Controllers/ComercialController.php`:

    **1. Adicionar helper estático no topo da classe** (depois das primeiras propriedades):

    ```
    /**
     * Roteia um nome de serviço (catálogo Frente A) para o slug de
     * implementação que dispara criação de mlb_empresas/mlb_implementacao.
     * Helper puro — testável sem montar request.
     *
     * @return 'polos'|'assessoria'|'incubadora'|null
     */
    public static function servicoDisparaImplementacao(string $nome): ?string
    {
        return match(true) {
            str_contains($nome, 'Polos')      => 'polos',
            str_contains($nome, 'Assessoria') => 'assessoria',
            str_contains($nome, 'Incubadora') => 'incubadora',
            default                            => null,
        };
    }
    ```

    **2. Adicionar método `create()`** (formulário GET) que retorna props para a UI:
    ```
    public function create()
    {
        abort_unless(auth()->user()->hasPermission('comercial.cadastrar_empresa') || auth()->user()->isAdmin(), 403);

        $servicos = Servico::where('ativo', true)->orderBy('nome')->get(['id', 'nome', 'valor_padrao', 'tipo_cobranca']);

        return Inertia::render('Comercial/NovaEmpresa', [
            'servicos_disponiveis' => $servicos,
        ]);
    }
    ```

    Importar `use App\Models\Servico;` no topo.

    Verificar `routes/web.php` — se a rota `comercial.empresas.novo` (GET) já aponta para `create()`, ok; se não, ajustar a rota (preferir adicionar a rota a manter compat). Inspecionar.

    **3. Reescrever `store()` (linhas 70-140)**:

    Substituir validation por:
    ```
    $validated = $request->validate([
        'nome'                       => 'required|string|max:255',
        'cnpj'                       => 'nullable|string|max:20|unique:companies,cnpj',
        'notes'                      => 'nullable|string|max:2000',
        'servicos'                   => 'required|array|min:1',
        'servicos.*.servico_id'      => ['required', 'integer',
                                         \Illuminate\Validation\Rule::exists('servicos', 'id')->where('ativo', true)],
        'servicos.*.valor_contratado' => 'nullable|numeric|min:0',
    ]);
    ```

    Após guard de duplicata (manter linhas 87-94 inalteradas), substituir a `DB::transaction(...)` por:

    ```
    $company = null;
    $servicosCriados = collect();

    DB::transaction(function () use ($validated, $request, &$company, &$servicosCriados) {
        $userId = $request->user()->id;

        // (a) Cria company com status pendente
        $company = Company::create([
            'name'   => $validated['nome'],
            'cnpj'   => $validated['cnpj'] ?? null,
            'notes'  => $validated['notes'] ?? null,
            'status' => 'pendente',
            'active' => true,
        ]);

        // (b) Cria 1 contrato_servico por servico selecionado
        foreach ($validated['servicos'] as $item) {
            $servico = \App\Models\Servico::find($item['servico_id']);
            if (!$servico) continue;

            \App\Models\ContratoServico::create([
                'company_id'       => $company->id,
                'servico_id'       => $servico->id,
                'valor_contratado' => isset($item['valor_contratado'])
                    ? (float) $item['valor_contratado']
                    : (float) $servico->valor_padrao,
                'data_contratacao' => now()->toDateString(),
                'data_vencimento'  => null,
                'ativo'            => true,
            ]);

            $servicosCriados->push($servico);
        }

        // (c) Roteamento Phase 13 — inspeciona NOMES dos serviços
        $tiposImplementacao = $servicosCriados
            ->map(fn($s) => self::servicoDisparaImplementacao($s->nome))
            ->filter()
            ->unique()
            ->values();

        foreach ($tiposImplementacao as $tipo) {
            if ($tipo === 'polos') {
                $mlbEmp = \App\Models\MlbEmpresa::create([
                    'nome'       => $company->name,
                    'tipo'       => 'POLO',
                    'projeto'    => 'POLOS',
                    'company_id' => $company->id,
                ]);
                $this->criarImplementacaoPolo($mlbEmp);
            } elseif ($tipo === 'assessoria') {
                \App\Models\MlbEmpresa::create([
                    'nome'       => $company->name,
                    'tipo'       => 'ASSESSORIA',
                    'company_id' => $company->id,
                ]);
            } elseif ($tipo === 'incubadora') {
                \App\Models\MlbEmpresa::create([
                    'nome'       => $company->name,
                    'tipo'       => 'INCUBADORA',
                    'company_id' => $company->id,
                ]);
            }
            // Publicidade/Gestão (servicoDispara retorna null) — sem mlb_empresas (COM-06)
        }
    });

    // (d) Activity log fora da transaction
    $nomesServicos = $servicosCriados->pluck('nome')->toArray();
    activity('comercial')
        ->causedBy($request->user())
        ->withProperties(['empresa' => $company->name, 'servicos' => $nomesServicos])
        ->log('Empresa cadastrada pelo Comercial: "' . $company->name . '"');

    // (e) Notifica líderes dos setores de destino
    // Mapeamento nome do serviço → slug do setor (preserva resolverSlugsSetores)
    $slugsSetores = $servicosCriados
        ->map(fn($s) => $this->slugSetorParaServico($s->nome))
        ->filter()
        ->unique()
        ->values();

    foreach ($slugsSetores as $slug) {
        $setor = Setor::where('slug', $slug)->first();
        if ($setor && $setor->lideres->isNotEmpty()) {
            Notification::send(
                $setor->lideres,
                new EmpresaCadastradaNotification(
                    $company->name,
                    $nomesServicos,  // array (não mais implode '+')
                    $request->user()->id,
                ),
            );
        }
    }

    return back()->with('success', 'Empresa "' . $company->name . '" cadastrada com sucesso.');
    ```

    **4. Adicionar helper privado `slugSetorParaServico(string $nome): ?string`**:
    ```
    private function slugSetorParaServico(string $nome): ?string
    {
        return match(true) {
            str_contains($nome, 'Polos') || str_contains($nome, 'Assessoria') || str_contains($nome, 'Publicação') => 'publicacao',
            str_contains($nome, 'Publicidade') => 'publicidade',
            str_contains($nome, 'Gestão')      => 'gestao',
            str_contains($nome, 'Incubadora')  => 'incubadora',
            default => null,
        };
    }
    ```

    **5. Marcar `resolverSlugsSetores()` (linhas 212-222) como `@deprecated`** — remover apenas no Plan 14-06 quando service_type for dropped (mantemos para evitar quebra silenciosa caso outro método ainda chame).

    **6. Refatorar `update()` (linhas 149-174) — VALIDATION ENXUTA**:

    Validation final do `update()` lista APENAS os 3 campos que persistem:

    ```
    $validated = $request->validate([
        'name'  => 'required|string|max:255',
        'cnpj'  => 'nullable|string|max:20',
        'notes' => 'nullable|string|max:2000',
    ]);
    ```

    Body do método:
    ```
    $company->update($validated);
    return back()->with('success', 'Empresa atualizada.');
    ```

    Os 6 campos legacy (`service_type`, `contract_type`, `contract_start`, `contract_end`, `additional_service`, `additional_service_price`) NÃO são validados nem persistidos. O cliente Inertia (Comercial/Empresas.jsx) ainda pode enviá-los, mas o controller os ignora (campos não validados não chegam em `$validated`).

    Adicionar comentário pt-BR no topo do método: `// Phase 14 Plan 14-04: validação limpa de campos legacy. Gestão de serviços é feita via rotas /empresas/{company}/contratos-servico (Frente A). Plan 14-06 dropa as 6 colunas legacy do schema.`

    NOTA importante: NÃO precisamos esperar pelo Plan 14-06 para fazer essa limpeza no `update()` — o Plan 14-06 vai dropar as COLUNAS, mas a validação aqui já não persiste nada legacy. Plan 14-06 cuidará apenas do drop do schema + cleanup do mapper Inertia em `empresas()`.

    **7. Refatorar `empresas()` (linhas 35-50)**: remover `service_type` da lista de `get([...])` e adicionar eager loading + mapper:
    ```
    $companies = Company::where('active', true)
        ->with(['contratosServico' => fn($q) => $q->where('ativo', true)->with('servico')])
        ->orderByRaw("CASE WHEN status = 'pendente' THEN 0 ELSE 1 END")
        ->orderBy('name')
        ->get(['id', 'name', 'cnpj', 'status', 'created_at', 'adman_account_id', 'ml_store_id', 'notes']);

    $companies->transform(function ($c) {
        $nomes = $c->contratosServico->where('ativo', true)->pluck('servico.nome')->filter();
        // Reconstrói service_type[] compat temporária para UI Comercial/Empresas.jsx (cleanup no Plan 14-06)
        $mapa = ['Publicação' => 'publicacao', 'Polos' => 'polos', 'Assessoria' => 'assessoria',
                 'Incubadora' => 'incubadora', 'Publicidade' => 'publicidade', 'Gestão' => 'gestao'];
        $c->service_type = $nomes->map(fn($n) => $mapa[$n] ?? null)->filter()->values()->toArray();
        $c->servicos_contratados = $nomes->values()->toArray();
        return $c;
    });
    ```

    Comentário pt-BR explicando: a UI `Comercial/Empresas.jsx` (refator escopado para Plan 14-07) ainda lê `service_type[]`; reconstruímos a partir de contratos para compat temporária. No Plan 14-06 a coluna é dropada e essa reconstrução substitui o read da coluna; no Plan 14-07 o JSX é refatorado para ler `servicos_contratados` diretamente, e a reconstrução de `service_type` cai junto.

    Per D-05, D-07, RESEARCH §7. Imports adicionais: `App\Models\Servico`, `Illuminate\Validation\Rule`.
  </action>
  <verify>
    <automated>php -l app/Http/Controllers/ComercialController.php</automated>
  </verify>
  <done>
    Arquivo sem syntax errors. Helper `servicoDisparaImplementacao` é `public static`. Método `create()` adicionado retornando `servicos_disponiveis`. `store()` valida `servicos[]` (não mais `service_type`). DB::transaction cria company + N contratos + mlb_empresas/mlb_implementacao conforme roteamento por nome. `update()` valida apenas name/cnpj/notes — os 6 campos legacy não são persistidos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refatorar NovaEmpresa.jsx — seletor multi do catálogo + valor por contrato</name>
  <files>resources/js/Pages/Comercial/NovaEmpresa.jsx</files>
  <read_first>
    - resources/js/Pages/Comercial/NovaEmpresa.jsx (arquivo completo — ~180 LOC)
    - resources/js/Pages/Companies/Show.jsx linhas 17-80 (padrão TipoBadge, useState para contratos, modal pattern)
    - resources/js/Pages/Servicos/Index.jsx (analog: tabela do catálogo — referência de campos do Servico)
    - resources/js/Layouts/AppLayout.jsx (padrão sidebar + cn helper)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-05)
  </read_first>
  <action>
    Editar `resources/js/Pages/Comercial/NovaEmpresa.jsx`:

    1. **Remover a const `TIPOS`** (linhas 22-29) — substituída pelo catálogo dinâmico.

    2. **Adicionar prop `servicos_disponiveis`** no destructuring:
    ```
    export default function NovaEmpresa({ servicos_disponiveis = [] }) {
        const { flash } = usePage().props;
        // ...
    ```

    3. **Mudar `useForm`** para refletir a nova estrutura:
    ```
    const { data, setData, post, processing, errors, reset } = useForm({
        nome: '',
        cnpj: '',
        notes: '',
        servicos: [],  // array de { servico_id, valor_contratado }
    });
    ```

    4. **Adicionar helpers no componente**:
    ```
    function toggleServico(servicoId) {
        const cur = data.servicos;
        const exists = cur.find(s => s.servico_id === servicoId);
        if (exists) {
            setData('servicos', cur.filter(s => s.servico_id !== servicoId));
        } else {
            const cat = servicos_disponiveis.find(s => s.id === servicoId);
            setData('servicos', [...cur, {
                servico_id: servicoId,
                valor_contratado: cat?.valor_padrao ?? 0,
            }]);
        }
    }

    function updateValor(servicoId, valor) {
        setData('servicos', data.servicos.map(s =>
            s.servico_id === servicoId ? { ...s, valor_contratado: valor } : s
        ));
    }
    ```

    5. **Substituir o bloco de checkboxes TIPOS** (linhas ~128-165) por uma lista renderizada do catálogo:
    ```
    <div className="space-y-2">
        <label className="text-white/70 text-sm font-medium">Serviços contratados</label>
        <div className={cn(
            "border rounded-xl p-3 space-y-2 bg-ecf-bg",
            errors.servicos ? 'border-red-500/50' : 'border-white/[0.08]',
        )}>
            {servicos_disponiveis.map(servico => {
                const selecionado = data.servicos.find(s => s.servico_id === servico.id);
                return (
                    <div key={servico.id} className="flex items-center gap-3 py-1">
                        <input
                            type="checkbox"
                            id={`servico-${servico.id}`}
                            checked={!!selecionado}
                            onChange={() => toggleServico(servico.id)}
                            className="rounded border-white/20 bg-ecf-card text-ecf-yellow focus:ring-ecf-yellow"
                        />
                        <label htmlFor={`servico-${servico.id}`} className="flex-1 text-white/80 text-sm cursor-pointer">
                            {servico.nome}
                            <span className="text-white/40 text-xs ml-2">({servico.tipo_cobranca})</span>
                        </label>
                        {selecionado && (
                            <input
                                type="number"
                                min="0"
                                step="0.01"
                                value={selecionado.valor_contratado}
                                onChange={e => updateValor(servico.id, e.target.value)}
                                placeholder="R$ 0,00"
                                className="w-24 px-2 py-1 rounded bg-ecf-card border border-white/[0.08] text-white text-xs text-right"
                            />
                        )}
                    </div>
                );
            })}
            {servicos_disponiveis.length === 0 && (
                <p className="text-white/40 text-xs">Nenhum serviço cadastrado no catálogo. Acesse /servicos para adicionar.</p>
            )}
        </div>
        {errors.servicos && <p className="text-red-400 text-xs mt-1">{errors.servicos}</p>}
        {data.servicos.length > 0 && (
            <p className="text-white/40 text-xs">
                {data.servicos.length} serviço(s) selecionado(s) — após cadastro, líderes do setor de destino serão notificados.
            </p>
        )}
    </div>
    ```

    6. **Atualizar comentário de cabeçalho** (linhas 1-16) — substituir referências a `service_type` por `servicos`. Listar nova estrutura do payload:
    ```
    // Form via useForm do Inertia:
    //   - nome:     obrigatório (texto livre)
    //   - cnpj:     opcional (texto livre)
    //   - notes:    opcional (texto livre)
    //   - servicos: array de { servico_id, valor_contratado } — pelo menos 1 obrigatório
    //
    // Submit chama POST comercial.empresas.store — backend cria atomicamente:
    //   - Polos:       Company + MlbEmpresa (tipo=POLO) + MlbImplementacao
    //   - Assessoria:  Company + MlbEmpresa (tipo=ASSESSORIA)
    //   - Incubadora:  Company + MlbEmpresa (tipo=INCUBADORA)
    //   - Publicidade/Gestão: apenas Company (sem mlb_empresas)
    // E notifica líderes do setor de destino.
    ```

    7. **Botão submit**: trocar `disabled={processing || data.service_type.length === 0}` por `disabled={processing || data.servicos.length === 0}`.

    8. **Remover qualquer renderização condicional** que dependa de `TIPOS.filter(t => data.service_type.includes(t.value))` (provavelmente linha ~158) — substituir por loop sobre `data.servicos` mostrando os nomes selecionados, ou simplesmente remover esse bloco.

    Comentários adicionais em pt-BR. Per D-05.

    **MANDATORY**: Após salvar o arquivo, rodar `npm run build` (CLAUDE.md mandate — Pitfall 6 do RESEARCH).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    `npm run build` completa com 0 erros. Bundle de `Comercial/NovaEmpresa` registrado no manifest `public/build/manifest.json`. Grep `service_type` no arquivo retorna 0 matches (exceto comentário de história, opcional).
  </done>
</task>

<task type="auto">
  <name>Task 3: Testes — helper puro + roteamento preservando COM-04/05/06</name>
  <files>tests/Unit/ComercialControllerHelperTest.php, tests/Feature/Phase14ComercialTest.php</files>
  <read_first>
    - app/Http/Controllers/ComercialController.php (estado pós Task 1)
    - tests/Feature/ (analog: qualquer Phase13ComercialTest.php — padrões para cadastro + assertNotNull MlbEmpresa)
    - app/Models/Servico.php, app/Models/MlbEmpresa.php, app/Models/MlbImplementacao.php
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §7 (exemplos de teste)
  </read_first>
  <action>
    **Parte A** — Criar `tests/Unit/ComercialControllerHelperTest.php` (namespace `Tests\Unit`, SEM RefreshDatabase — helper puro):

    Testes:
    1. `test_polos_dispara_implementacao_polos()` — `assertEquals('polos', ComercialController::servicoDisparaImplementacao('Polos'))`
    2. `test_polos_sp_dispara_implementacao_polos()` — `assertEquals('polos', ComercialController::servicoDisparaImplementacao('Polos SP'))` (str_contains case-sensitive)
    3. `test_assessoria_dispara_implementacao_assessoria()` — `assertEquals('assessoria', ComercialController::servicoDisparaImplementacao('Assessoria Premium'))`
    4. `test_incubadora_dispara_implementacao_incubadora()` — `assertEquals('incubadora', ComercialController::servicoDisparaImplementacao('Incubadora Tech'))`
    5. `test_publicidade_retorna_null()` — `assertNull(ComercialController::servicoDisparaImplementacao('Publicidade'))`
    6. `test_gestao_retorna_null()` — `assertNull(ComercialController::servicoDisparaImplementacao('Gestão'))`
    7. `test_treinamento_arbitrario_retorna_null()` — `assertNull(ComercialController::servicoDisparaImplementacao('Treinamento Vendas'))`
    8. `test_polos_lowercase_retorna_null_por_case_sensitive()` — `assertNull(ComercialController::servicoDisparaImplementacao('polos'))` (D-02 garante Title Case via mb_convert_case; nomes lowercase nunca entram no catálogo via fluxo normal)

    **Parte B** — Criar `tests/Feature/Phase14ComercialTest.php` (RefreshDatabase):

    Setup `setUp()`:
    ```
    protected function setUp(): void {
        parent::setUp();
        Artisan::call('migrate'); // ou specific paths para garantir Servicos table
        // Cria os 6 servicos canônicos (mimicking Plan 14-02 migration 1)
        foreach (['Publicação', 'Polos', 'Assessoria', 'Incubadora', 'Publicidade', 'Gestão'] as $nome) {
            Servico::firstOrCreate(['nome' => $nome], ['valor_padrao' => 0, 'tipo_cobranca' => 'mensal', 'ativo' => true]);
        }
    }
    ```

    Testes:

    1. **`test_cadastra_empresa_polos_cria_mlb_implementacao()`** (COM-04 preservado):
       - `$admin = User::factory()->create(['role' => 'admin']); $this->actingAs($admin);`
       - `$polos = Servico::where('nome', 'Polos')->first();`
       - `$response = $this->post('/comercial/empresas', ['nome' => 'Empresa Teste Polos', 'servicos' => [['servico_id' => $polos->id, 'valor_contratado' => 0]]]);`
       - `$response->assertRedirect();`
       - `$company = Company::where('name', 'Empresa Teste Polos')->first();`
       - `$this->assertNotNull($company);`
       - `$this->assertEquals('pendente', $company->status);`
       - `$this->assertEquals(1, $company->contratosServico()->count());`
       - `$this->assertNotNull(MlbEmpresa::where('company_id', $company->id)->where('tipo', 'POLO')->first());`
       - `$this->assertNotNull(MlbImplementacao::whereHas('mlbEmpresa', fn($q) => $q->where('company_id', $company->id))->first());` (verificar nome correto do relation no model)

    2. **`test_cadastra_empresa_publicidade_nao_cria_mlb()`** (COM-06 preservado):
       - actingAs admin
       - `$pub = Servico::where('nome', 'Publicidade')->first();`
       - `$response = $this->post('/comercial/empresas', ['nome' => 'Empresa Pub', 'servicos' => [['servico_id' => $pub->id]]]);`
       - `$response->assertRedirect();`
       - `$company = Company::where('name', 'Empresa Pub')->first();`
       - `$this->assertNotNull($company);`
       - `$this->assertNull(MlbEmpresa::where('company_id', $company->id)->first());`

    3. **`test_cadastra_empresa_assessoria_cria_mlb_sem_implementacao()`** (COM-05 preservado):
       - actingAs admin
       - `$ass = Servico::where('nome', 'Assessoria')->first();`
       - POST com servicos=[{servico_id: $ass->id}]
       - assertNotNull MlbEmpresa com tipo='ASSESSORIA'
       - assertNull MlbImplementacao (assessoria não cria implementação per COM-05)

    4. **`test_cadastra_empresa_com_multiplos_servicos_cria_contratos_e_mlb_apropriado()`**:
       - POST com servicos=[{servico_id: polos}, {servico_id: gestao}]
       - assertEquals(2, $company->contratosServico()->count())
       - assertNotNull MlbEmpresa (porque polos)
       - Sem MlbEmpresa duplicada (1 só)

    5. **`test_cadastro_sem_servicos_falha_validation()`**:
       - POST com servicos=[]
       - assertSessionHasErrors(['servicos'])
       - Nenhuma company criada com aquele nome

    6. **`test_cadastro_com_servico_inativo_falha_validation()`**:
       - Cria Servico extra com `ativo = false`
       - POST com servicos=[{servico_id: inativo}]
       - assertSessionHasErrors(['servicos.0.servico_id'])

    7. **`test_cadastro_aciona_notificacao_para_lideres_do_setor()`** (COM-08 preservado):
       - Cria setor 'publicacao' + líder + permission
       - actingAs admin, POST com servico Polos
       - `Notification::fake()` no setup do teste — assert que `EmpresaCadastradaNotification` foi enviada para o líder
       - Verifica que a notification foi criada com `$servicos` como array (não string)

    8. **`test_update_ignora_campos_legacy()`** (validação enxuta):
       - Cria empresa via factory (com colunas legacy ainda existindo no schema)
       - actingAs admin
       - `$this->put("/comercial/empresas/{$company->id}", ['name' => 'Nome Novo', 'service_type' => ['polos'], 'additional_service_price' => 999])`
       - assertRedirect
       - Recarrega `$company->refresh()`
       - assertEquals('Nome Novo', $company->name) — campo persistido
       - assertNotEquals(['polos'], $company->service_type) — campo legacy NÃO foi persistido (mantém o valor anterior ou null)
       - assertNotEquals(999, $company->additional_service_price) — idem

    Comentários e mensagens de assert em pt-BR. Use factories existentes onde possível. Per D-05, COM-04/05/06/08 preservation.
  </action>
  <verify>
    <automated>php artisan test --filter=ComercialControllerHelperTest && php artisan test --filter=Phase14ComercialTest</automated>
  </verify>
  <done>
    Ambas suítes verdes. Total 16+ testes (8 unit + 8 feature). Roteamento Phase 13 PRESERVADO (COM-04, COM-05, COM-06, COM-08 verificados explicitamente). Update enxuto verificado por teste 8.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /comercial/empresas | Usuário do Comercial envia `servicos[]` com IDs do catálogo + valores opcionais; trust-untrusted; valida exists + ativo |
| Browser → PUT /comercial/empresas/{id} | Usuário do Comercial envia name/cnpj/notes; payload com 6 campos legacy é silenciosamente ignorado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | ComercialController::store aceita servico_id arbitrário no payload | mitigate | Validation `Rule::exists('servicos', 'id')->where('ativo', true)` rejeita IDs inválidos ou de serviços inativos |
| T-14-05 | Tampering | Valor contratado manipulado client-side (Comercial envia 0 pra serviço pago) | accept | Decisão UX D-05 — Comercial pode customizar valor por contrato; activity log via LogsActivity da Frente A registra (audit trail) |
| T-14-06 | Integrity | Empresa cadastrada com nome duplicado via race condition | mitigate | DB::transaction + guard `LOWER(name) = LOWER(?)` já existente do Phase 13; preservado intacto |
| T-14-10 | Tampering | Cliente envia campos legacy no update() esperando persistência | mitigate | Validation enxuta ignora silenciosamente (campos não validados não chegam em $validated) |
</threat_model>

<verification>
- `php artisan test --filter=ComercialControllerHelperTest` → 8 testes verdes
- `php artisan test --filter=Phase14ComercialTest` → 8 testes verdes (cobre COM-04/05/06/08 + update enxuto)
- `npm run build` → 0 erros, bundle de NovaEmpresa registrado
- `grep -n "service_type" resources/js/Pages/Comercial/NovaEmpresa.jsx` → 0 matches em código vivo (comentários de história OK)
- `grep -n "service_type" app/Http/Controllers/ComercialController.php` → matches apenas em `resolverSlugsSetores` (deprecated) e `empresas()` reconstrução compat
- `php -l app/Http/Controllers/ComercialController.php` → sem erros
- `php artisan phase14:verificar-cobranca` → ainda 0 divergências (cadastro Comercial não afeta empresas existentes)
</verification>

<success_criteria>
1. Helper `ComercialController::servicoDisparaImplementacao` é estático puro testado isoladamente
2. `store()` valida `servicos[].servico_id` exists+ativo + cria contratos N:N atomicamente
3. Roteamento Phase 13 preservado: Polos→mlb_empresas+mlb_implementacao; Assessoria→mlb_empresas; Publicidade/Gestão→só company
4. `NovaEmpresa.jsx` mostra catálogo dinâmico em vez dos 3 checkboxes hardcoded
5. `npm run build` 0 erros
6. EmpresaCadastradaNotification recebe array de nomes (sem implode '+')
7. `update()` valida APENAS name/cnpj/notes; campos legacy enviados pelo cliente são ignorados
8. 16+ assertions verdes entre as 2 suítes de teste
</success_criteria>

<output>
Criar `.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-04-SUMMARY.md` listando: arquivos modificados, novas rotas (se houver), confirmação de preservação de COM-04/05/06/08, confirmação da validação enxuta do update(), e nota "Cadastro Comercial NÃO PRODUZ MAIS dados legacy. Plan 14-06 NÃO precisa limpar update() — já feito aqui. Próximo: Plan 14-05 (UI Financeiro)".

Commit (pt-BR):
```
refactor(14): ComercialController + NovaEmpresa.jsx usam catálogo de serviços

- ComercialController.store() valida servicos[] + cria contratos N:N
- ComercialController.update() valida apenas name/cnpj/notes (campos legacy ignorados)
- Helper estático servicoDisparaImplementacao() roteia por nome
- NovaEmpresa.jsx: seletor multi do catálogo com valor por contrato
- Testes: 8 unit do helper + 8 feature (COM-04/05/06/08 preservados + update enxuto)

Per D-05. Cadastro Comercial não produz mais dados legacy.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
</output>
</content>
