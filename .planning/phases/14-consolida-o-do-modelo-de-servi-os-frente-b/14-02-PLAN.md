---
phase: 14-consolida-o-do-modelo-de-servi-os-frente-b
plan: 02
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - database/migrations/2026_05_27_100001_seed_servicos_catalog.php
  - database/migrations/2026_05_27_100002_migrate_legacy_service_data.php
  - tests/Feature/Phase14MigrationTest.php
autonomous: true
requirements:
  - SVC-01
must_haves:
  truths:
    - "Tabela `servicos` contém os 6 tipos canônicos pós-migration 1: Publicação, Polos, Assessoria, Incubadora, Publicidade, Gestão"
    - "Para cada empresa com `service_type` populado, existe 1 `contratos_servico` por tipo com valor_contratado=0, data_contratacao=contract_start ?? created_at, data_vencimento=contract_end"
    - "Para cada empresa com `additional_service` populado, existe 1 `contratos_servico` adicional com servico_id mapeado por Title Case + valor_contratado=additional_service_price"
    - "Empresas com ambos service_type vazio E additional_service vazio recebem ZERO contratos"
    - "Migrations rodam 2x sem duplicar (idempotência)"
  artifacts:
    - path: "database/migrations/2026_05_27_100001_seed_servicos_catalog.php"
      provides: "Seed idempotente do catálogo (6 tipos canônicos)"
      contains: "firstOrCreate"
    - path: "database/migrations/2026_05_27_100002_migrate_legacy_service_data.php"
      provides: "Migration de dados — cria contratos derivados dos campos legacy"
      contains: "DB::transaction"
    - path: "tests/Feature/Phase14MigrationTest.php"
      provides: "Testes de criação correta + idempotência"
      contains: "class Phase14MigrationTest"
  key_links:
    - from: "database/migrations/2026_05_27_100002_migrate_legacy_service_data.php"
      to: "servicos table"
      via: "lookup por nome (cache Servico::pluck('id', 'nome'))"
      pattern: "Servico::pluck\\('id', 'nome'\\)"
    - from: "database/migrations/2026_05_27_100002_migrate_legacy_service_data.php"
      to: "contratos_servico table"
      via: "guard de idempotência (where company_id + servico_id + valor_contratado)"
      pattern: "->where\\('company_id'.*->where\\('servico_id'.*->exists"
---

<objective>
Criar as duas primeiras migrations da sequência: (1) seed do catálogo `servicos` com os 6 tipos canônicos legacy mapeados (D-01) e (2) data migration que itera todas as empresas existentes e cria os `contratos_servico` derivados dos campos legacy (D-04). Ambas idempotentes (rodam 2x sem duplicar). Após este plan, o sistema entra em estado de COEXISTÊNCIA: campos legacy permanecem populados E contratos novos populados — runtime AINDA lê dos legacy (refator dos consumers só começa no Plan 14-03).

Purpose: SVC-01 — fonte de dados unificada. Sem este plan, o novo modelo fica vazio e os consumers refatorados nos planos seguintes não têm o que ler.

Output: 2 migrations + 1 suíte de testes. Commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md

<interfaces>
<!-- Schema da Frente A (já existente — só consumir) -->

`servicos` (database/migrations/2026_05_26_120001_create_servicos_table.php):
- id, nome (string unique), valor_padrao (decimal 12,2), tipo_cobranca (string), ativo (boolean), timestamps

`contratos_servico` (database/migrations/2026_05_26_120002_create_contratos_servico_table.php):
- id, company_id (FK cascadeOnDelete), servico_id (FK restrictOnDelete), valor_contratado (decimal 12,2),
  data_contratacao (date), data_vencimento (date nullable), ativo (boolean default true), observacoes (text nullable), timestamps
- Index: (company_id, ativo)

<!-- Campos legacy a serem migrados (ainda existem após este plan) -->

`companies` table (após migrations Phase 5/7/13):
- service_type (TEXT — JSON array de enum ['publicacao','polos','assessoria','incubadora','publicidade','gestao'])
- contract_type (string nullable — 'fixo' ou 'progressao') — NÃO migrado neste plan (não há campo equivalente em contratos_servico)
- contract_start (date nullable)
- contract_end (date nullable)
- additional_service (string nullable)
- additional_service_price (decimal nullable)

<!-- Padrão de timestamp das migrations Phase 14 (D-06) -->
Sequência: 2026_05_27_100001 → _100002 → _100003 (Plan 14-06 cria a _100003)

<!-- Mapeamento D-01 (exato — hard-coded) -->
$mapaLegacy = [
    'publicacao'  => 'Publicação',
    'polos'       => 'Polos',
    'assessoria'  => 'Assessoria',
    'incubadora'  => 'Incubadora',
    'publicidade' => 'Publicidade',
    'gestao'      => 'Gestão',
];

<!-- Pitfall RESEARCH.md #3 — empresa com service_type=[] vs null -->
Guard de idempotência usa (company_id + servico_id + valor_contratado) — combinação exata por contrato.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 1 — seed_servicos_catalog (idempotente via firstOrCreate)</name>
  <files>database/migrations/2026_05_27_100001_seed_servicos_catalog.php</files>
  <read_first>
    - app/Models/Servico.php (modelo Frente A — campos fillable)
    - database/migrations/2026_05_26_120001_create_servicos_table.php (schema da tabela alvo)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-01)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §1 (firstOrCreate vs updateOrCreate)
    - database/migrations/2026_05_26_120002_create_contratos_servico_table.php (timestamp anchor mais recente)
  </read_first>
  <action>
    Criar arquivo `database/migrations/2026_05_27_100001_seed_servicos_catalog.php` com classe anônima `return new class extends Migration { ... }`.

    Método `up()`:
    - Itera o array `['Publicação', 'Polos', 'Assessoria', 'Incubadora', 'Publicidade', 'Gestão']` (apenas valores — os 6 nomes em pt-BR conforme D-01)
    - Para cada nome: `\App\Models\Servico::firstOrCreate(['nome' => $nome], ['valor_padrao' => 0, 'tipo_cobranca' => 'mensal', 'ativo' => true])`
    - NÃO usar `updateOrCreate` — `firstOrCreate` preserva edições manuais de `valor_padrao` em re-runs (RESEARCH §1)

    Método `down()`:
    - Comentário pt-BR: "Não deletar — contratos vinculados via restrictOnDelete impediriam o drop. Em dev, re-seed é trivial; em prod, contratos preservados."
    - Corpo vazio (apenas comentário)

    Comentário de cabeçalho em pt-BR explicando: propósito (seed idempotente do catálogo), referência a D-01 e ao Plan 14-02, e o fato de que rodar 2x não duplica.

    Per D-01, D-06.
  </action>
  <verify>
    <automated>php artisan migrate --pretend --path=database/migrations/2026_05_27_100001_seed_servicos_catalog.php</automated>
  </verify>
  <done>
    Arquivo existe em `database/migrations/2026_05_27_100001_seed_servicos_catalog.php`. `php -l` sem erros. Conteúdo usa `firstOrCreate` (não `updateOrCreate`). Down() não deleta dados.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migration 2 — migrate_legacy_service_data (regra cumulativa D-04, idempotente)</name>
  <files>database/migrations/2026_05_27_100002_migrate_legacy_service_data.php</files>
  <read_first>
    - app/Models/Company.php linhas 28-42 ($fillable, $casts — confirma que service_type é cast array, contract_start/end são date)
    - app/Models/ContratoServico.php (modelo Frente A)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-02 normalização Title Case + D-04 regra cumulativa)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §1 (padrão completo de migration de dados com chunk + guards)
    - database/migrations/2026_05_27_100001_seed_servicos_catalog.php (Task 1 — catálogo já está populado quando esta migration roda)
  </read_first>
  <action>
    Criar arquivo `database/migrations/2026_05_27_100002_migrate_legacy_service_data.php` com classe anônima.

    Cabeçalho com comentário pt-BR explicando: propósito (migrar dados legacy em contratos_servico), referências D-02 e D-04, garantia de idempotência via guard `where(company_id, servico_id, valor_contratado)->exists()`, e modelo de coexistência (campos legacy permanecem populados; Plan 14-06 dropa).

    Método `up()`:
    - Envolve TODO o trabalho em `\DB::transaction(function() { ... })` (atomicidade — RESEARCH §1)
    - Define `$mapaLegacy = ['publicacao' => 'Publicação', 'polos' => 'Polos', 'assessoria' => 'Assessoria', 'incubadora' => 'Incubadora', 'publicidade' => 'Publicidade', 'gestao' => 'Gestão']` (verbatim D-01)
    - Cache: `$servicosByNome = \App\Models\Servico::pluck('id', 'nome')` (1 query pra resolver todos os ids — RESEARCH §1)
    - Itera empresas via `\App\Models\Company::chunk(100, function($companies) use ($mapaLegacy, $servicosByNome) { foreach (...) { $this->migrarContratosLegacy($c, $mapaLegacy, $servicosByNome); } })` (Pitfall 2 — evita OOM em prod)

    Implementar `private function migrarContratosLegacy(\App\Models\Company $c, array $mapaLegacy, \Illuminate\Support\Collection $servicosByNome): void` que executa:

    (a) Calcula `$dataContratacao = $c->contract_start ? $c->contract_start->toDateString() : $c->created_at->toDateString()` (Carbon → string Y-m-d).

    (b) Para cada `$tipo` em `(array) $c->service_type` (D-04 item 1):
        - `$nome = $mapaLegacy[$tipo] ?? null;` (skip se não mapeado)
        - `if (!$nome || !isset($servicosByNome[$nome])) continue;`
        - Guard idempotência: `$jaExiste = \App\Models\ContratoServico::where('company_id', $c->id)->where('servico_id', $servicosByNome[$nome])->where('valor_contratado', 0)->exists();`
        - Se não existe: `\App\Models\ContratoServico::create([...])` com `valor_contratado => 0`, `data_contratacao => $dataContratacao`, `data_vencimento => $c->contract_end?->toDateString()`, `ativo => true`

    (c) Se `!empty(trim($c->additional_service ?? ''))` (D-04 item 2):
        - `$nome = mb_convert_case(trim($c->additional_service), MB_CASE_TITLE, 'UTF-8');` (D-02 normalização agressiva)
        - `$servico = \App\Models\Servico::firstOrCreate(['nome' => $nome], ['valor_padrao' => (float) ($c->additional_service_price ?? 0), 'tipo_cobranca' => 'mensal', 'ativo' => true]);` (find-or-create no catálogo)
        - Guard idempotência: `$valorAdicional = (float) ($c->additional_service_price ?? 0); $jaExiste = \App\Models\ContratoServico::where('company_id', $c->id)->where('servico_id', $servico->id)->where('valor_contratado', $valorAdicional)->exists();`
        - Se não existe: `\App\Models\ContratoServico::create([...])` com `valor_contratado => $valorAdicional`, mesmas datas, `ativo => true`

    (d) Se ambos vazios: NÃO criar nenhum contrato (estado neutro — D-04 item 3).

    Método `down()`:
    - Comentário pt-BR: "Down não reverte dados criados — contratos_servico permanecem. Para reverter completamente, restaurar backup do banco. Migration de schema (drop) faz própria reversão via recriação de colunas no Plan 14-06."
    - Corpo vazio (apenas comentário)

    Comentários inline em pt-BR. Pitfall 4 do RESEARCH (cast decimal:2 retorna string em SQLite) — usar `(float)` antes de operações aritméticas. Per D-02, D-04, D-06.
  </action>
  <verify>
    <automated>php artisan migrate --pretend --path=database/migrations/2026_05_27_100002_migrate_legacy_service_data.php</automated>
  </verify>
  <done>
    Arquivo criado. `php -l` sem erros. Migration tem `DB::transaction` envolvente, usa `chunk(100)`, e guards explícitos `where(...)->exists()` antes de criar contratos. Down() não deleta dados.
  </done>
</task>

<task type="auto">
  <name>Task 3: Testes de criação + idempotência das migrations 1 e 2</name>
  <files>tests/Feature/Phase14MigrationTest.php</files>
  <read_first>
    - database/migrations/2026_05_27_100001_seed_servicos_catalog.php (Task 1)
    - database/migrations/2026_05_27_100002_migrate_legacy_service_data.php (Task 2)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §1 (exemplo de test_migration_pode_rodar_2x_sem_duplicar)
    - tests/Feature/ (estrutura existente — analog: qualquer Phase13*Test.php para padrão de RefreshDatabase + Artisan::call)
    - database/factories/ (verificar se CompanyFactory existe; se não, criar empresa via Company::create direto)
  </read_first>
  <action>
    Criar `tests/Feature/Phase14MigrationTest.php` (namespace `Tests\Feature`, extends `Tests\TestCase`, usa `RefreshDatabase`).

    Importar: `App\Models\Company`, `App\Models\Servico`, `App\Models\ContratoServico`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Illuminate\Support\Facades\Artisan`.

    Implementar 5 testes:

    1. **`test_migration_cria_6_servicos_canonicos()`**: roda apenas a migration 1 via `Artisan::call('migrate', ['--path' => 'database/migrations/2026_05_27_100001_seed_servicos_catalog.php'])`. Assert que `Servico::count()` >= 6 (pode ter mais se outros testes criaram); `Servico::where('nome', 'Publicação')->exists()` true para todos os 6 nomes do D-01; assert que `Servico::where('nome', 'Publicação')->first()->tipo_cobranca === 'mensal'` e `->valor_padrao == 0`.

    2. **`test_migration_cria_contratos_para_empresa_com_service_type()`**: cria empresa via `Company::create(['name' => 'Empresa A', 'active' => true, 'service_type' => ['polos', 'gestao'], 'contract_start' => '2026-01-01', 'contract_end' => '2026-12-31'])` (campos legacy AINDA EXISTEM nesta wave). Roda migrations 1 e 2 separadamente. Assert `ContratoServico::where('company_id', $c->id)->count() === 2` (1 Polos + 1 Gestão); cada contrato tem `valor_contratado == 0`, `data_contratacao == '2026-01-01'`, `data_vencimento == '2026-12-31'`, `ativo == true`. Confirma que `$contrato->servico->nome` está em `['Polos', 'Gestão']`.

    3. **`test_migration_cria_contrato_adicional_com_normalizacao_title_case()`**: cria empresa com `service_type` vazio, `additional_service => '  TREINAMENTO PERSONALIZADO  '`, `additional_service_price => 350.00`, `contract_start => null`, `contract_end => null`. Roda migrations. Assert `Servico::where('nome', 'Treinamento Personalizado')->exists()` (Title Case + trim aplicados). Assert `ContratoServico::where('company_id', $c->id)->count() === 1` e o contrato tem `valor_contratado == 350.00`, `data_contratacao == $c->created_at->toDateString()` (fallback), `data_vencimento == null`.

    4. **`test_migration_skip_empresa_sem_service_type_nem_additional()`**: cria empresa com `service_type => []` (array vazio), `additional_service => null`. Roda migrations. Assert `ContratoServico::where('company_id', $c->id)->count() === 0`.

    5. **`test_migration_pode_rodar_2x_sem_duplicar()`**: cria empresa com `service_type => ['polos']`, `additional_service => 'Consultoria'`, `additional_service_price => 100.00`, `contract_start => '2026-01-01'`. Roda migration 1 + migration 2. Verifica `ContratoServico::where('company_id', $c->id)->count() === 2`. **Re-roda manualmente** invocando uma função privada que duplica a lógica do up() OU `Artisan::call('migrate:fresh')` + factory + re-call das 2 migrations (escolher o que for mais simples na suite). Após re-run: ainda count === 2 (idempotência).

    Cada teste usa `assertEquals`, `assertTrue`, `assertCount`. Comentários e mensagens de assert em pt-BR.

    Pitfall 8 (SQLite vs MySQL): Company::factory pode não existir; usar `Company::create(...)` diretamente. Datas em SQLite às vezes vêm como string — usar `->toDateString()` ao comparar.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase14MigrationTest</automated>
  </verify>
  <done>
    Suíte `Phase14MigrationTest` roda com 5 testes verdes. `php -l tests/Feature/Phase14MigrationTest.php` sem erros.
  </done>
</task>

</tasks>

<verification>
- `php artisan test --filter=Phase14MigrationTest` → 5 testes verdes
- `php artisan migrate:fresh` → as 2 migrations Phase 14 (junto com todas as outras) rodam sem erro
- `php artisan tinker --execute='echo \App\Models\Servico::count();'` (após migrate:fresh + popular dados) → >= 6
- Migrations Phase 14 não dropam nenhum dado (down() é no-op informativo)
- Nenhum arquivo de produção PHP (controllers, models, jobs) foi tocado neste plan
</verification>

<success_criteria>
1. Migration 1 popula `servicos` com os 6 nomes canônicos via `firstOrCreate` (idempotente)
2. Migration 2 cria contratos derivados de `service_type` (1 por tipo) e `additional_service` (1 adicional)
3. Guards de idempotência: re-runs não duplicam contratos
4. Empresas sem dados legacy ficam sem contratos (estado neutro)
5. Datas migradas corretamente: `data_contratacao = contract_start ?? created_at`, `data_vencimento = contract_end`
6. Normalização Title Case + trim funciona para `additional_service`
7. Suíte `Phase14MigrationTest` verde com 5 testes cobrindo as 4 branches do D-04 + idempotência
</success_criteria>

<output>
Criar `.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-02-SUMMARY.md` ao final, listando: arquivos criados, contagem de empresas afetadas em dev (rodar `php artisan migrate` localmente e contar contratos criados via tinker), e nota explícita "Sistema agora em COEXISTÊNCIA — campos legacy populados E contratos_servico populados. Runtime AINDA lê dos legacy até Plan 14-03".

Commit (pt-BR):
```
feat(14): migrations de seed do catálogo + migração de dados legacy

- 2026_05_27_100001_seed_servicos_catalog — firstOrCreate dos 6 tipos canônicos
- 2026_05_27_100002_migrate_legacy_service_data — cria contratos por empresa via DB::transaction + chunk(100)
- tests/Feature/Phase14MigrationTest — 5 testes (criação + normalização + idempotência)

Per D-01, D-02, D-04, D-06. Sistema entra em coexistência: legacy populado E novo modelo populado.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
</output>
</content>
</invoke>