---
phase: 14-consolida-o-do-modelo-de-servi-os-frente-b
plan: 05
type: execute
wave: 3
depends_on:
  - 14-03
files_modified:
  - resources/js/Pages/Admin/Financeiro.jsx
  - resources/views/admin/relatorio-fechamento.blade.php
  - resources/views/admin/relatorio-geral.blade.php
  - resources/views/admin/relatorio-geral-pdf.blade.php
  - tests/Feature/Phase14BladeRefactorTest.php
  - tests/Feature/Phase14FechamentoUiTest.php
autonomous: false
requirements:
  - SVC-03
  - SVC-04
must_haves:
  truths:
    - "Admin/Financeiro.jsx substitui o editor inline de 'Serviço adicional' (texto livre + preço único) pela seção de contratos do padrão Companies/Show.jsx"
    - "Admin/Financeiro.jsx tem modal 'Adicionar contrato' que chama POST /empresas/{company}/contratos-servico (rota Frente A — não criar nova)"
    - "Admin/Financeiro.jsx lista contratos ativos com nome + valor + ações editar/desativar (PUT e DELETE para as rotas Frente A)"
    - "ServiceBadge no Fechamento exibe nomes do array `servicos_contratados` (passado pelo backend no Plan 14-03)"
    - "3 Blade views (relatorio-fechamento, relatorio-geral, relatorio-geral-pdf) trocam Company::labelFromTypes(\\$company->service_type) por \\$company->serviceTypeLabel() (D-09)"
    - "npm run build completa com 0 erros"
    - "Pre-flight confirma nomes exatos das rotas de contratos antes do refator JSX"
    - "Teste feature Phase14BladeRefactorTest renderiza relatório e confirma nomes de serviços na saída HTML"
    - "Teste feature Phase14FechamentoUiTest confirma prop `servicos_contratados` na Inertia response"
    - "Checkpoint humano confirma visualmente: cadastro de contrato funciona, lista de contratos correta, total consolidado conta os mensais corretamente"
  artifacts:
    - path: "resources/js/Pages/Admin/Financeiro.jsx"
      provides: "UI de contratos por empresa no Fechamento (substitui editor legacy)"
      contains: "contratos-servico"
    - path: "resources/views/admin/relatorio-fechamento.blade.php"
      provides: "Blade usa serviceTypeLabel() em vez de labelFromTypes(service_type)"
      contains: "serviceTypeLabel"
    - path: "resources/views/admin/relatorio-geral.blade.php"
      provides: "Blade usa serviceTypeLabel()"
      contains: "serviceTypeLabel"
    - path: "resources/views/admin/relatorio-geral-pdf.blade.php"
      provides: "Blade usa serviceTypeLabel()"
      contains: "serviceTypeLabel"
    - path: "tests/Feature/Phase14BladeRefactorTest.php"
      provides: "Teste de renderização Blade com nomes de serviços"
      contains: "class Phase14BladeRefactorTest"
    - path: "tests/Feature/Phase14FechamentoUiTest.php"
      provides: "Teste Inertia component + props servicos_contratados"
      contains: "class Phase14FechamentoUiTest"
  key_links:
    - from: "resources/js/Pages/Admin/Financeiro.jsx"
      to: "URL /empresas/{empresa.id}/contratos-servico"
      via: "router.post/put/delete inertia"
      pattern: "contratos-servico"
    - from: "Blade views (3)"
      to: "Company::serviceTypeLabel"
      via: "chamada de método no template Blade"
      pattern: "serviceTypeLabel\\(\\)"
---

<objective>
Substituir o editor inline de "Serviço adicional" (texto livre + preço único) na página `/administrativo/financeiro` pela mesma UI de gestão de contratos já existente em `Companies/Show.jsx` (Frente A). Refatorar as 3 Blade views de relatório para usar `$company->serviceTypeLabel()` (que agora deriva de contratos — D-09) em vez de `Company::labelFromTypes($company->service_type)`. Cobrir Blades e UI com testes feature pós-refator. Checkpoint humano valida visualmente.

Purpose: SVC-03, SVC-04 — fechar o ciclo UI. Após este plan, NENHUMA UI lê os campos legacy diretamente, apenas via método refatorado do Model.

Output: 1 página JSX reescrita + 3 Blade views ajustadas + 2 suítes de teste + checkpoint humano. Commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md

<interfaces>
<!-- Padrão UI a copiar (Frente A — Companies/Show.jsx) -->

`resources/js/Pages/Companies/Show.jsx` linhas 56-80:
```
const todosContratos = company.contratos_servico || [];
const [showInativos, setShowInativos] = useState(false);
const [contratoOpen, setContratoOpen] = useState(false);
const [editingContrato, setEditingContrato] = useState(null);

const contratosVisiveis = useMemo(
    () => showInativos ? todosContratos : todosContratos.filter(c => c.ativo),
    [todosContratos, showInativos],
);

const TIPO_COBRANCA = {
    mensal: { label: 'Mensal', className: 'bg-ecf-yellow/15 text-ecf-yellow border-ecf-yellow/25' },
    unica:  { label: 'Única',  className: 'bg-white/10 text-white/80 border-white/15' },
};
```

Rotas usadas pelo modal (já criadas pela Frente A — NÃO criar de novo):
- `POST /empresas/{company}/contratos-servico` → store contrato
- `PUT /empresas/{company}/contratos-servico/{contrato}` → update
- `DELETE /empresas/{company}/contratos-servico/{contrato}` → destroy (soft via ativo=false)

<!-- Props vindas do AdminController após Plan 14-03 -->

Cada item em `companies[]` agora tem `servicos_contratados` adicionado:
```
servicos_contratados: [
  { id, servico_id, servico_nome, valor_contratado, tipo_cobranca, data_contratacao, data_vencimento, ativo }
]
```

(Chaves legacy `service_type`, `additional_service`, `additional_service_price` permanecem por compat — REMOVIDAS no Plan 14-06/14-07)

<!-- Financeiro.jsx atual a refatorar -->

Linhas 9-16: const SERVICE_LABELS (manter — usado pelo ServiceBadge para mapear enum legacy → label)
Linhas 52-70: function ServiceBadge — recebe `tipo` (array ou string legacy)
Linhas 467 (no FechamentoRow): `<ServiceBadge tipo={empresa.service_type} />`
Linhas 481-489: `useForm({service_type: ..., contract_type, contract_start, contract_end, additional_service, additional_service_price})`
Linhas 513-526: bloco de checkboxes service_type no editor
Linhas 528-606: blocos de inputs para contract_type, contract_start, contract_end, additional_service (texto + valor)
Linhas 927: const FILTROS_INICIAL = { busca: '', service_type: '', contract_type: '', estado: '', recebido: '' }
Linhas 942-953: dropdowns de filtro service_type/contract_type
Linhas 988-989: filtros que usam e.service_type / e.contract_type

<!-- 3 Blade views a refatorar -->

`resources/views/admin/relatorio-fechamento.blade.php`:
- linha 275: `{{ \App\Models\Company::labelFromTypes($company->service_type) }}` — empresa pai
- linha 406: `{{ \App\Models\Company::labelFromTypes($v['service_type'] ?? null) }}` — filhas (array)

`resources/views/admin/relatorio-geral.blade.php`:
- linhas 375 e 506 — análogo

`resources/views/admin/relatorio-geral-pdf.blade.php`:
- linhas 319 e 426 — análogo

<!-- D-09 helper Company refatorado no Plan 14-03 -->

```
public function serviceTypeLabel(): string {
    return static::labelFromServicos($this->contratosServico->where('ativo', true)->pluck('servico'));
}
// API ESTÁTICA preservada — Blades chamam $company->serviceTypeLabel()
```

Para as filhas em arrays (`$v['service_type']`): o AdminController após Plan 14-03 adiciona a chave `servicos_contratados` no array — usar `$v['servicos_contratados'] ?? '—'` na Blade.

<!-- Nomes EXATOS das rotas (verificados em routes/web.php) -->

```
Route::post('/empresas/{company}/contratos-servico',                  ...)->name('empresas.contratos.store');
Route::put('/empresas/{company}/contratos-servico/{contrato}',        ...)->name('empresas.contratos.update');
Route::delete('/empresas/{company}/contratos-servico/{contrato}',     ...)->name('empresas.contratos.destroy');
```

<!-- CLAUDE.md mandate -->
`npm run build` obrigatório após qualquer edição JSX. Pitfall 6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: PRE-FLIGHT — verificar nomes exatos das rotas de contratos antes do refator JSX</name>
  <files></files>
  <read_first>
    - routes/web.php (procurar por "contratos-servico" e "contratos.")
  </read_first>
  <action>
    Executar:
    ```
    php artisan route:list | grep -i contrato
    ```

    OU se o `php artisan route:list` não estiver disponível no ambiente do executor, fazer grep direto em `routes/web.php`:
    ```
    grep -n "contratos-servico\|contratos\." routes/web.php
    ```

    Esperado (já confirmado durante o planejamento — usar como referência):
    ```
    routes/web.php:226: Route::post('/empresas/{company}/contratos-servico',           [CompanyController::class, 'storeContrato'])->name('empresas.contratos.store');
    routes/web.php:227: Route::put('/empresas/{company}/contratos-servico/{contrato}', [CompanyController::class, 'updateContrato'])->name('empresas.contratos.update');
    routes/web.php:228: Route::delete('/empresas/{company}/contratos-servico/{contrato}', ...)->name('empresas.contratos.destroy');
    ```

    Confirmar que os nomes nomeados são EXATAMENTE:
    - `empresas.contratos.store`
    - `empresas.contratos.update`
    - `empresas.contratos.destroy`

    **DECISÃO de implementação**: para evitar acoplamento ao nome nomeado (Ziggy/route helper), o JSX usará **URLs cruas** em vez de `route('...')`:
    - POST  → `/empresas/${empresa.id}/contratos-servico`
    - PUT   → `/empresas/${empresa.id}/contratos-servico/${contrato.id}`
    - DELETE→ `/empresas/${empresa.id}/contratos-servico/${contrato.id}`

    Isso elimina o risco de quebra se o nome nomeado mudar.

    SE o pre-flight revelar que as rotas têm caminhos DIFERENTES de `/empresas/{company}/contratos-servico` (improvável, mas possível), ABORTAR o refator e reportar ao usuário para alinhamento.

    Atualizar comentário em Task 2 ao usar essas URLs.
  </action>
  <verify>
    <automated>grep -n "contratos-servico" routes/web.php</automated>
  </verify>
  <done>
    Pre-flight executado; nomes/URLs confirmados; decisão de usar URL crua registrada no início da Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 1: Refatorar 3 Blade views (relatorio-fechamento, relatorio-geral, relatorio-geral-pdf) — labelFromTypes → serviceTypeLabel / servicos_contratados</name>
  <files>resources/views/admin/relatorio-fechamento.blade.php, resources/views/admin/relatorio-geral.blade.php, resources/views/admin/relatorio-geral-pdf.blade.php</files>
  <read_first>
    - resources/views/admin/relatorio-fechamento.blade.php linhas 270-290 e 400-415 (uso atual de labelFromTypes)
    - resources/views/admin/relatorio-geral.blade.php linhas 370-385 e 500-515
    - resources/views/admin/relatorio-geral-pdf.blade.php linhas 315-330 e 420-435
    - app/Models/Company.php (estado pós Plan 14-03 — labelFromServicos + serviceTypeLabel refatorado)
    - app/Http/Controllers/AdminController.php (estado pós Plan 14-03 — chave `servicos_contratados` adicionada nos arrays de filhas e pai)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-09)
  </read_first>
  <action>
    **Para cada uma das 3 Blade views**:

    1. **Para empresa pai** (chamada com objeto Company):
       Substituir `{{ \App\Models\Company::labelFromTypes($company->service_type) }}`
       por `{{ $company->serviceTypeLabel() }}`

       (O método `serviceTypeLabel()` já foi refatorado no Plan 14-03 para derivar de contratos ativos via `labelFromServicos`.)

    2. **Para filhas em arrays** (chamada com `$v['service_type']`):
       Substituir `{{ \App\Models\Company::labelFromTypes($v['service_type'] ?? null) }}`
       por `{{ $v['servicos_contratados'] ?? '—' }}`

       (No Plan 14-03 o AdminController passa `servicos_contratados` como string já formatada na lista de filhas — ver mapper `pluck('servico.nome')->implode(', ')`.)

    Comentários inline em pt-BR explicando: "Phase 14: labelFromTypes(legacy) → serviceTypeLabel(derivado de contratos)" em cada Blade.

    Verificar TODAS as ocorrências via grep (`grep -n "labelFromTypes\|service_type" resources/views/admin/`) antes E depois para confirmar 0 matches legacy nas 3 views.

    Per D-09.
  </action>
  <verify>
    <automated>php -l resources/views/admin/relatorio-fechamento.blade.php && php -l resources/views/admin/relatorio-geral.blade.php && php -l resources/views/admin/relatorio-geral-pdf.blade.php</automated>
  </verify>
  <done>
    `grep -nE "labelFromTypes|service_type" resources/views/admin/` retorna 0 matches em código vivo. Os 3 arquivos Blade sem syntax errors (php -l). Templates renderizam `$company->serviceTypeLabel()` em vez do helper estático.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refatorar Admin/Financeiro.jsx — substituir editor legacy por modal de contratos (estilo Companies/Show.jsx)</name>
  <files>resources/js/Pages/Admin/Financeiro.jsx</files>
  <read_first>
    - resources/js/Pages/Admin/Financeiro.jsx (arquivo completo — ~1000 LOC; concentrar nas linhas 420-610 e 920-1000)
    - resources/js/Pages/Companies/Show.jsx (padrão UI a copiar — seção "Serviços contratados", modal Add/Edit, toggle showInativos)
    - resources/js/Components/ui/dialog.jsx (componente Dialog que Show.jsx usa)
    - app/Http/Controllers/AdminController.php (estado pós Plan 14-03 — confirmar shape de `servicos_contratados`)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-09 + bloco "Reusable Assets")
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §8 (componentes reusáveis Frente A)
  </read_first>
  <action>
    Editar `resources/js/Pages/Admin/Financeiro.jsx`. Mudanças cirúrgicas — manter restante da página (cabeçalho, total consolidado, tabelas, gráficos) intacto.

    **NOTA pre-flight (Task 0)**: usar URL crua nos `router.post/put/delete` (não `route('...')`):
    - POST  → `/empresas/${empresa.id}/contratos-servico`
    - PUT   → `/empresas/${empresa.id}/contratos-servico/${contrato.id}`
    - DELETE→ `/empresas/${empresa.id}/contratos-servico/${contrato.id}`

    **1. Atualizar ServiceBadge (linhas 52-70)** para aceitar `servicos_contratados` (array de objetos) além do fallback legacy:

    ```
    function ServiceBadge({ servicos_contratados, tipo }) {
        // Preferência: nova prop servicos_contratados (lista de objetos)
        if (Array.isArray(servicos_contratados) && servicos_contratados.length > 0) {
            return (
                <span className="inline-flex items-center gap-1 flex-wrap">
                    {servicos_contratados.map(c => (
                        <span key={c.id} className="text-[11px] font-semibold px-2 py-0.5 rounded-full border bg-ecf-yellow/10 text-ecf-yellow border-ecf-yellow/20">
                            {c.servico_nome}
                        </span>
                    ))}
                </span>
            );
        }
        // Fallback: tipo legacy (compat até Plan 14-06)
        const types = Array.isArray(tipo) ? tipo : (tipo ? [tipo] : []);
        if (types.length === 0) {
            return <span className="bg-white/[0.05] text-white/40 border-white/[0.08] text-[11px] font-semibold px-2 py-0.5 rounded-full border">Sem serviços</span>;
        }
        return (
            <span className="inline-flex items-center gap-1 flex-wrap">
                {types.map(t => (
                    <span key={t} className={cn('text-[11px] font-semibold px-2 py-0.5 rounded-full border', SERVICE_COLORS[t])}>
                        {SERVICE_LABELS[t] ?? t}
                    </span>
                ))}
            </span>
        );
    }
    ```

    Atualizar a chamada (linha 467) para passar ambas props: `<ServiceBadge servicos_contratados={empresa.servicos_contratados} tipo={empresa.service_type} />`.

    **2. Substituir o componente de editor inline de "Serviço adicional" (linhas ~480-610 do ServiceForm/FormSection ou seja qual for o nome do componente local)**:

    REMOVER do `useForm`:
    - `additional_service: ...`
    - `additional_service_price: ...`
    - `contract_type: ...`
    - `contract_start: ...`
    - `contract_end: ...`
    - `service_type: ...`

    Manter o componente edit-mode existente (provavelmente um Accordion ou Modal local) mas substituir TODO o bloco de inputs por uma SEÇÃO DE CONTRATOS análoga a `Companies/Show.jsx`:

    ```
    {/* Seção de Contratos (Plan 14-05 — substitui editor legacy) */}
    <div className="space-y-3 pt-2 border-t border-white/[0.08]">
        <div className="flex items-center justify-between">
            <h4 className="text-white/80 text-sm font-semibold">Serviços contratados</h4>
            <button
                type="button"
                onClick={() => abrirModalContrato(empresa, null)}
                className="inline-flex items-center gap-1 px-2 py-1 text-xs rounded-md bg-ecf-yellow/10 hover:bg-ecf-yellow/20 text-ecf-yellow border border-ecf-yellow/20"
            >
                <Plus size={12} /> Adicionar contrato
            </button>
        </div>

        {(empresa.servicos_contratados || []).length === 0 ? (
            <p className="text-white/40 text-xs">Nenhum contrato ativo.</p>
        ) : (
            <table className="w-full text-xs">
                <thead>
                    <tr className="text-white/40 border-b border-white/[0.06]">
                        <th className="text-left py-1">Serviço</th>
                        <th className="text-right py-1">Valor</th>
                        <th className="text-center py-1">Tipo</th>
                        <th className="text-right py-1">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    {(empresa.servicos_contratados || []).map(c => (
                        <tr key={c.id} className="text-white/70 border-b border-white/[0.03] last:border-0">
                            <td className="py-1.5">{c.servico_nome}</td>
                            <td className="py-1.5 text-right font-mono">{fmtBRL(c.valor_contratado)}</td>
                            <td className="py-1.5 text-center">
                                <span className={cn('px-1.5 py-0.5 rounded text-[10px] font-semibold border',
                                    c.tipo_cobranca === 'mensal'
                                        ? 'bg-ecf-yellow/10 text-ecf-yellow border-ecf-yellow/20'
                                        : 'bg-white/10 text-white/60 border-white/15')}>
                                    {c.tipo_cobranca === 'mensal' ? 'Mensal' : 'Única'}
                                </span>
                            </td>
                            <td className="py-1.5 text-right">
                                <button type="button" onClick={() => abrirModalContrato(empresa, c)} className="text-white/40 hover:text-ecf-yellow mr-2"><Pencil size={12} /></button>
                                <button type="button" onClick={() => desativarContrato(empresa, c)} className="text-white/40 hover:text-red-400"><PowerOff size={12} /></button>
                            </td>
                        </tr>
                    ))}
                </tbody>
            </table>
        )}
    </div>
    ```

    **3. Adicionar Modal de Contrato no nível do componente principal** (state global da página):

    ```
    const [contratoModal, setContratoModal] = useState({ open: false, empresa: null, contrato: null });
    const [contratoForm, setContratoForm] = useState({ servico_id: '', valor_contratado: '', data_contratacao: '', data_vencimento: '', observacoes: '', ativo: true });

    function abrirModalContrato(empresa, contrato = null) {
        setContratoModal({ open: true, empresa, contrato });
        setContratoForm(contrato ? {
            servico_id: contrato.servico_id,
            valor_contratado: contrato.valor_contratado,
            data_contratacao: contrato.data_contratacao || '',
            data_vencimento: contrato.data_vencimento || '',
            observacoes: contrato.observacoes || '',
            ativo: contrato.ativo,
        } : {
            servico_id: '', valor_contratado: '', data_contratacao: new Date().toISOString().slice(0, 10), data_vencimento: '', observacoes: '', ativo: true,
        });
    }

    function salvarContrato() {
        // URL crua (decisão pre-flight Task 0) — evita acoplamento a route helper
        const baseUrl = `/empresas/${contratoModal.empresa.id}/contratos-servico`;
        const url = contratoModal.contrato ? `${baseUrl}/${contratoModal.contrato.id}` : baseUrl;
        const method = contratoModal.contrato ? 'put' : 'post';

        router[method](url, contratoForm, {
            preserveScroll: true,
            onSuccess: () => setContratoModal({ open: false, empresa: null, contrato: null }),
        });
    }

    function desativarContrato(empresa, contrato) {
        if (!confirm(`Desativar contrato "${contrato.servico_nome}"?`)) return;
        router.delete(`/empresas/${empresa.id}/contratos-servico/${contrato.id}`, {
            preserveScroll: true,
        });
    }
    ```

    **4. Adicionar a renderização do Dialog** ao final do componente principal (antes do `</AppLayout>` fechar):

    ```
    {/* Modal Adicionar/Editar Contrato */}
    {contratoModal.open && (
        <Dialog open={contratoModal.open} onOpenChange={(open) => !open && setContratoModal({ open: false, empresa: null, contrato: null })}>
            <DialogContent className="bg-ecf-card border-white/[0.08] max-w-md">
                <DialogHeader>
                    <DialogTitle>{contratoModal.contrato ? 'Editar' : 'Adicionar'} contrato — {contratoModal.empresa?.name}</DialogTitle>
                </DialogHeader>
                <div className="space-y-3 py-3">
                    {/* select servico_id, input valor_contratado, inputs datas, textarea observacoes, switch ativo */}
                    {/* Pattern espelho de Companies/Show.jsx — copiar visual mas pode ser inline (não componentizar) */}
                </div>
                <DialogFooter>
                    <button onClick={() => setContratoModal({ open: false, empresa: null, contrato: null })} className="px-3 py-1.5 rounded border border-white/[0.08] text-white/70 text-sm">Cancelar</button>
                    <button onClick={salvarContrato} className="px-3 py-1.5 rounded bg-ecf-yellow text-ecf-bg font-semibold text-sm">Salvar</button>
                </DialogFooter>
            </DialogContent>
        </Dialog>
    )}
    ```

    **5. Filtros (linhas 927+)**: REMOVER os dropdowns de `contract_type` e `service_type` legacy. Substituir por um único dropdown "Serviço" que lista os nomes únicos do catálogo. Derivar do `companies[].servicos_contratados[].servico_nome` no client:

    ```
    const todosServicosNomes = useMemo(
        () => [...new Set(companies.flatMap(c => (c.servicos_contratados || []).map(s => s.servico_nome)))].sort(),
        [companies],
    );
    ```

    E o filtro:
    ```
    <select value={filtros.servico_nome} onChange={e => onChange({ ...filtros, servico_nome: e.target.value })} className={sel}>
        <option value="">Todos os serviços</option>
        {todosServicosNomes.map(nome => <option key={nome} value={nome}>{nome}</option>)}
    </select>
    ```

    Atualizar `FILTROS_INICIAL` removendo `service_type` e `contract_type`, adicionando `servico_nome: ''`.

    Atualizar a função de filtragem para usar `filtros.servico_nome` em vez de `service_type` e remover `contract_type` (no longer applicable):
    ```
    if (filtros.servico_nome && !(e.servicos_contratados || []).some(s => s.servico_nome === filtros.servico_nome)) return false;
    ```

    **6. Imports**: adicionar `Plus, Pencil, PowerOff` de `lucide-react`. Adicionar `Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter` de `@/Components/ui/dialog`.

    **7. Remover qualquer referência a `contract_start`/`contract_end`/`additional_service` no display (badges, accordions, etc.)**. Substituir por linha que mostra "Contratos: {N} ativos" derivado de `servicos_contratados.length`.

    Comentários em pt-BR. Per D-09. **MANDATORY**: rodar `npm run build` ao final (Pitfall 6).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    `npm run build` 0 erros. Bundle `Admin/Financeiro` registrado no manifest. Grep `additional_service|contract_start|contract_end|contract_type` no arquivo retorna apenas comentários explicativos (ou 0 matches). Grep `service_type` aparece apenas no fallback do ServiceBadge (compat até Plan 14-06). Todos os `router.post/put/delete` usam URL crua `/empresas/${empresa.id}/contratos-servico[/${contrato.id}]`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Teste feature — renderização das 3 Blade views com nomes de serviços corretos (Phase14BladeRefactorTest)</name>
  <files>tests/Feature/Phase14BladeRefactorTest.php</files>
  <read_first>
    - app/Http/Controllers/AdminController.php (métodos gerarRelatorio + gerarRelatorioGeral pós Plan 14-03)
    - resources/views/admin/relatorio-fechamento.blade.php (estado pós Task 1)
    - app/Models/Company.php (estado pós Plan 14-03 — serviceTypeLabel deriva de contratos)
    - tests/Feature/ (analog: qualquer feature test existente que renderize Blade — padrão actingAs + assertSeeText)
  </read_first>
  <action>
    Criar `tests/Feature/Phase14BladeRefactorTest.php` (namespace `Tests\Feature`, `RefreshDatabase`):

    Imports: `App\Models\Company`, `App\Models\Servico`, `App\Models\ContratoServico`, `App\Models\User`, `App\Models\AdmanMetric`, `Carbon\Carbon`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Illuminate\Support\Facades\Artisan`.

    Setup `setUp()`:
    ```
    protected function setUp(): void {
        parent::setUp();
        // Roda migrations Phase 14 + seed do catálogo
        Artisan::call('migrate');
        foreach (['Publicação', 'Polos', 'Assessoria', 'Incubadora', 'Publicidade', 'Gestão'] as $nome) {
            Servico::firstOrCreate(['nome' => $nome], ['valor_padrao' => 0, 'tipo_cobranca' => 'mensal', 'ativo' => true]);
        }
    }
    ```

    Testes:

    1. **`test_relatorio_fechamento_renderiza_nomes_servicos_de_contratos_ativos()`**:
       - Cria 1 admin via factory + actingAs
       - Cria empresa pai "Empresa Pai" com 2 contratos ativos: Polos (R$ 0,00) + Gestão (R$ 0,00)
       - Cria AdmanMetric para a empresa pai com revenue suficiente (ex: 600.000) para cair em faixa válida
       - Hit `GET /administrativo/relatorio/{empresa-pai-id}` (ou rota real do `gerarRelatorio` — verificar `routes/web.php`; pode ser `/administrativo/relatorio-fechamento/{id}` ou similar)
       - assertStatus(200)
       - `$response->assertSeeText('Polos', escaped: false)` — nome do serviço aparece no HTML
       - `$response->assertSeeText('Gestão', escaped: false)` — idem
       - `$response->assertDontSee('labelFromTypes')` — método legacy não aparece em código vazado

    2. **`test_relatorio_fechamento_renderiza_servicos_contratados_de_empresa_filha()`**:
       - Cria empresa pai com 1 contrato ativo (Polos)
       - Cria empresa filha vinculada à pai com 2 contratos ativos (Assessoria + Publicidade) — usar `parent_company_id`
       - Cria AdmanMetric para ambas
       - actingAs admin
       - Hit `GET /administrativo/relatorio/{empresa-pai-id}`
       - assertStatus(200)
       - assertSeeText('Assessoria') — serviço da filha aparece no HTML (vem da chave `servicos_contratados` string formatada pelo mapper)
       - assertSeeText('Publicidade')

    3. **`test_relatorio_geral_renderiza_nomes_servicos()`**:
       - Cria 2 empresas com diferentes contratos
       - actingAs admin
       - Hit `GET /administrativo/relatorio-geral`
       - assertStatus(200)
       - assertSeeText do nome de pelo menos 1 serviço de cada empresa

    NOTA sobre rotas: usar grep em `routes/web.php` para descobrir os nomes/URLs exatos:
    ```
    grep -nE "gerarRelatorio|relatorio-fechamento|relatorio-geral" routes/web.php
    ```
    Ajustar testes para a URL real. Se a rota exige POST com filtros, montar `$this->post(...)` com payload mínimo.

    Comentários e mensagens de assert em pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase14BladeRefactorTest</automated>
  </verify>
  <done>
    Suíte `Phase14BladeRefactorTest` verde com 3 testes. Pelo menos 1 teste por Blade view (fechamento + geral). Assertions confirmam que nomes do catálogo aparecem renderizados no HTML.
  </done>
</task>

<task type="auto">
  <name>Task 4: Teste feature — Inertia component + props servicos_contratados (Phase14FechamentoUiTest)</name>
  <files>tests/Feature/Phase14FechamentoUiTest.php</files>
  <read_first>
    - app/Http/Controllers/AdminController.php (método fechamento pós Plan 14-03)
    - resources/js/Pages/Admin/Financeiro.jsx (estado pós Task 2)
    - tests/Feature/ (analog: qualquer feature test que use `assertInertia` — Laravel Inertia testing helpers)
  </read_first>
  <action>
    Criar `tests/Feature/Phase14FechamentoUiTest.php` (namespace `Tests\Feature`, `RefreshDatabase`):

    Imports: `App\Models\Company`, `App\Models\Servico`, `App\Models\ContratoServico`, `App\Models\User`, `App\Models\AdmanMetric`, `Inertia\Testing\AssertableInertia as Assert`, `Carbon\Carbon`, `Illuminate\Foundation\Testing\RefreshDatabase`, `Illuminate\Support\Facades\Artisan`.

    Setup `setUp()`:
    ```
    protected function setUp(): void {
        parent::setUp();
        Artisan::call('migrate');
        foreach (['Publicação', 'Polos', 'Assessoria', 'Incubadora', 'Publicidade', 'Gestão'] as $nome) {
            Servico::firstOrCreate(['nome' => $nome], ['valor_padrao' => 0, 'tipo_cobranca' => 'mensal', 'ativo' => true]);
        }
    }
    ```

    Testes:

    1. **`test_financeiro_inertia_component_e_company_props_estrutura()`**:
       - Cria admin + actingAs
       - Cria 1 empresa "Empresa Teste" com 2 contratos ativos: Polos (R$ 100) + Gestão (R$ 50)
       - Cria AdmanMetric para ela (revenue=600.000 → faixa_2 R$ 4.500)
       - Hit `GET /administrativo/financeiro`
       - assertStatus(200)
       - `$response->assertInertia(fn (Assert $page) => $page
             ->component('Admin/Financeiro')
             ->has('companies')
             ->has('companies.0.servicos_contratados', fn (Assert $servicos) => $servicos
                 ->has(2)  // 2 contratos esperados
                 ->first(fn (Assert $servico) => $servico
                     ->has('id')
                     ->has('servico_id')
                     ->has('servico_nome')
                     ->has('valor_contratado')
                     ->has('tipo_cobranca')
                     ->etc()
                 )
             )
         );`

    2. **`test_financeiro_companies_inclui_chave_servicos_contratados_em_todas_empresas()`**:
       - Cria 3 empresas com contratos variados (1 sem contratos, 1 com 1 contrato, 1 com 3 contratos)
       - actingAs admin
       - Hit `/administrativo/financeiro`
       - assertInertia → para cada item de `companies[]`, `servicos_contratados` é array (pode ser vazio)
       - Verifica especificamente: `$page->where('companies.0.servicos_contratados', fn ($value) => is_array($value))` (ou usar `assertSee` indireto)

    3. **`test_financeiro_props_inclui_cobranca_mensal_calculada_de_contratos()`**:
       - Cria 1 empresa com 1 contrato Polos mensal R$ 200 (NÃO usar additional_service_price)
       - Cria AdmanMetric com revenue suficiente p/ faixa_2 (R$ 4.500)
       - actingAs admin, hit `/administrativo/financeiro`
       - assertInertia → `companies.0.cobranca_mensal === 4700.0` (4500 faixa + 200 contrato)

    NOTA: se `Inertia\Testing\AssertableInertia` não estiver disponível no projeto (verificar via `grep "AssertableInertia" tests/`), usar a forma simplificada com `$response->json()` ou `$response->getOriginalContent()` para extrair props. Inertia 2.0 + Laravel 12 inclui o helper por padrão.

    Comentários e mensagens de assert em pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase14FechamentoUiTest</automated>
  </verify>
  <done>
    Suíte `Phase14FechamentoUiTest` verde com 3 testes. assertInertia confirma component='Admin/Financeiro' E shape de servicos_contratados em cada company.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Checkpoint humano — verificação visual da UI Fechamento</name>
  <what-built>
    UI do Fechamento (`/administrativo/financeiro`) refatorada para:
    - ServiceBadge exibe nomes do catálogo via `servicos_contratados` (não mais enum legacy)
    - Editor inline substituído por seção de contratos com botão "Adicionar contrato"
    - Modal de adicionar/editar contrato (mesmo padrão de `Companies/Show.jsx`)
    - Botão desativar contrato (soft via PUT ativo=false ou DELETE)
    - Filtro de serviço derivado dos contratos do dataset
    - 3 Blade views de relatório (`relatorio-fechamento`, `relatorio-geral`, `relatorio-geral-pdf`) agora usam `$company->serviceTypeLabel()` (derivado de contratos)
  </what-built>
  <how-to-verify>
    1. Rodar `npm run build` no host XAMPP e confirmar 0 erros
    2. Acessar `https://admin.ecfconsultoria.com.br/administrativo/financeiro` (login admin)
    3. Confirmar que cada empresa exibe badges com nomes vindos do catálogo (ex: "Polos", "Gestão") — não enums (ex: "POLO" maiúsculo do legacy)
    4. Expandir uma empresa qualquer (clicar para abrir o editor inline ou accordion existente)
    5. Confirmar visualmente:
       - Tabela de "Serviços contratados" aparece com colunas: Serviço, Valor, Tipo, Ações
       - Botão "Adicionar contrato" abre modal
       - No modal: select de serviço populado com o catálogo, inputs de valor/datas, switch ativo, botões Cancelar/Salvar
    6. Adicionar 1 contrato novo via modal (escolher um serviço do catálogo, valor R$ 100, datas hoje) — após salvar, redireciona e o novo contrato aparece na lista
    7. Editar o contrato recém-criado (mudar valor para R$ 150) — confirmar atualização
    8. Desativar o contrato — confirmar que some da lista (ou aparece marcado se houver toggle "mostrar inativos")
    9. Verificar que o "Total consolidado" no topo da página soma corretamente: faixa + SUM(contratos mensais ativos)
    10. Gerar 1 relatório individual via botão "Gerar relatório" (rota `gerarRelatorio`) — confirmar que o tipo de serviço aparece na tela do relatório (Blade) com nomes corretos
    11. Gerar relatório geral — idem
    12. Ver no console do navegador (F12) que não há erros JS
  </how-to-verify>
  <resume-signal>Aprovado: a UI funciona conforme acima OU descreva o problema encontrado (anexar screenshot se possível).</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` → 0 erros, bundle Financeiro registrado
- `grep -nE "additional_service|contract_start|contract_end|contract_type" resources/js/Pages/Admin/Financeiro.jsx` → 0 matches em código vivo
- `grep -nE "labelFromTypes" resources/views/admin/` → 0 matches
- 3 Blade views `php -l` sem erros
- `php artisan test --filter=Phase14BladeRefactorTest` → verde
- `php artisan test --filter=Phase14FechamentoUiTest` → verde
- Checkpoint humano aprovou visualmente
- Comando `phase14:verificar-cobranca` ainda retorna 0 divergências
</verification>

<success_criteria>
1. Admin/Financeiro.jsx substitui inputs legacy por seção de contratos + modal
2. Modal usa URL crua `/empresas/${id}/contratos-servico[/{contrato_id}]` (decisão pre-flight Task 0)
3. ServiceBadge exibe `servicos_contratados` (preferência) com fallback legacy
4. 3 Blade views usam `$company->serviceTypeLabel()` (derivado de contratos)
5. Filtros refatorados para derivar serviços do dataset
6. npm run build 0 erros
7. Feature test `Phase14BladeRefactorTest` cobre renderização das 3 Blades
8. Feature test `Phase14FechamentoUiTest` cobre prop `servicos_contratados` na Inertia response
9. Checkpoint humano confirma fluxo funcional fim-a-fim
</success_criteria>

<output>
Criar `.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-05-SUMMARY.md` listando: arquivos modificados, decisão de manter ServiceBadge com fallback legacy (compat até Plan 14-06/14-07), confirmação do checkpoint humano. Nota: "UI Fechamento usa exclusivamente contratos_servico. 3 Blade views idem. Plan 14-06 dropa schema; Plan 14-07 limpa demais JSX e fallback do ServiceBadge".

Commit (pt-BR):
```
refactor(14): UI Fechamento + 3 Blade views consomem contratos_servico

- Admin/Financeiro.jsx: editor de service_type/additional_service substituído por seção de contratos
- Modal Add/Edit usa URL crua /empresas/{id}/contratos-servico (rotas Frente A)
- ServiceBadge prioriza servicos_contratados com fallback legacy
- 3 Blade views: Company::labelFromTypes($service_type) → $company->serviceTypeLabel() (D-09)
- Feature tests: Phase14BladeRefactorTest + Phase14FechamentoUiTest
- Checkpoint humano aprovado

Per D-09, SVC-03, SVC-04.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
</output>
</content>
