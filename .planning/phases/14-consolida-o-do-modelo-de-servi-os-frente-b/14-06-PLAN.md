---
phase: 14-consolida-o-do-modelo-de-servi-os-frente-b
plan: 06
type: execute
wave: 4
depends_on:
  - 14-03
  - 14-04
  - 14-05
files_modified:
  - database/migrations/2026_05_27_100003_drop_legacy_service_columns_from_companies.php
  - app/Models/Company.php
  - app/Http/Controllers/AdminController.php
  - app/Http/Controllers/ComercialController.php
  - app/Http/Controllers/MlbController.php
  - app/Http/Controllers/CompanyController.php
  - app/Jobs/EnviarRelatorioFechamentoJob.php
autonomous: false
requirements:
  - SVC-06
  - SVC-04
must_haves:
  truths:
    - "Comando phase14:verificar-cobranca retorna 0 divergências como pre-flight do drop (ABORT se divergir)"
    - "Migration 3 dropa as 6 colunas: service_type, contract_type, contract_start, contract_end, additional_service, additional_service_price"
    - "Down() recria as 6 colunas EXATAMENTE com tipos originais (service_type como TEXT — não string — per Pitfall 7)"
    - "Company.php: $fillable, $casts, logOnly removem as 6 colunas; labelFromTypes removido (apenas labelFromServicos sobrevive)"
    - "Compat temporária dos 5 controllers/job removida — mapper Inertia não passa mais chaves legacy"
    - "Grep `service_type|contract_type|contract_start|contract_end|additional_service|additional_service_price` em app/ retorna 0 matches em código vivo (excluindo migrations históricas e comentários)"
    - "Checkpoint humano aprova: pre-flight + smoke test pós-drop dos endpoints"
  artifacts:
    - path: "database/migrations/2026_05_27_100003_drop_legacy_service_columns_from_companies.php"
      provides: "Drop das 6 colunas legacy; down() recria estrutura"
      contains: "dropColumn"
    - path: "app/Models/Company.php"
      provides: "Sem campos legacy; labelFromTypes removido"
      contains: "labelFromServicos"
  key_links:
    - from: "database/migrations/2026_05_27_100003_drop_legacy_service_columns_from_companies.php"
      to: "Pre-flight obrigatório"
      via: "humano executa phase14:verificar-cobranca antes de migrate"
      pattern: "phase14:verificar-cobranca"
---

<objective>
Cleanup BACKEND da Phase 14: dropar as 6 colunas legacy de `companies`, remover toda compat temporária do MODEL e CONTROLLERS (Company.php fillable/casts/logOnly, mappers Inertia em AdminController/MlbController/CompanyController/ComercialController/Job, reconstruções de `service_type` derivadas, validation rules dormentes). Checkpoint humano confirma pré-flight de cobrança limpo ANTES do drop, e visualmente pós-drop confirma que os endpoints HTTP backend continuam funcionais (200 OK).

**ESCOPO REDUZIDO:** Este plan cobre apenas BACKEND + MIGRATION (Migration 3 + Model + 5 controllers/job). Os 5 JSX consumers (Admin/Empresas, Comercial/Empresas, Mlb/Empresas, Companies/Index, Admin/Financeiro fallback) ficam no Plan 14-07 (mesma Wave 4, sequencial após este).

Purpose: SVC-06, SVC-04 — Fechar o backend da Phase 14. Após este plan: zero referência funcional aos 6 campos legacy em PHP. Próximo: Plan 14-07 (frontend).

Output: 1 migration + 5 controllers/job atualizados + 1 model + checkpoint humano. Commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md

<interfaces>
<!-- 6 colunas a dropar -->

`companies` table — colunas legacy (todas adicionadas em phases anteriores):
1. service_type (TEXT — JSON array, convertido em 2026_05_25_300001_convert_service_type_to_json_array.php)
2. contract_type (string nullable — adicionado em add_contract_type_to_companies.php)
3. contract_start (date nullable)
4. contract_end (date nullable)
5. additional_service (string nullable)
6. additional_service_price (decimal nullable)

<!-- Decisão D-06 -->
contract_type ESTAVA fora do D-06 original mas o D-07 atualizado inclui (10 consumers, com contract_type listado). DECISÃO: DROPAR contract_type junto.

<!-- Schema down() — RECRIAÇÃO EXATA (Pitfall 7) -->

```
public function down(): void {
    Schema::table('companies', function (Blueprint $table) {
        $table->text('service_type')->nullable()->after('notes');         // text, NÃO string
        $table->string('contract_type')->nullable()->after('service_type');
        $table->date('contract_start')->nullable()->after('contract_type');
        $table->date('contract_end')->nullable()->after('contract_start');
        $table->string('additional_service')->nullable()->after('contract_end');
        $table->decimal('additional_service_price', 10, 2)->nullable()->after('additional_service');
    });
}
```

<!-- Pre-flight gate (Plan 14-01) -->

```
php artisan phase14:verificar-cobranca --abort-on-divergence
# Exit 0 → safe to drop
# Exit 1 → ABORT, investigar divergência
```

<!-- Compat temporária do Plan 14-03/04 a remover -->

AdminController.php (estado pós Plan 14-03):
- mapper Inertia em fechamento() (linhas ~259-285) — chaves service_type, contract_type, contract_start, contract_end, additional_service, additional_service_price
- mapper Inertia em empresas() (linhas ~31-52) — idem
- gerarRelatorio/gerarRelatorioGeral mappers de filhas — idem
- validation rules em updateEmpresa() (linhas 56-66) — campos legacy
- validation rules em updateFechamento() (linhas 370-389) — campos legacy

MlbController.php (estado pós Plan 14-03):
- `get([...'service_type'...])` — remover service_type da lista de campos
- transform que adiciona `servicos_contratados` (manter; só remove o select de `service_type`)

CompanyController.php (estado pós Plan 14-03):
- mesmo cleanup do MlbController

ComercialController.php (estado pós Plan 14-04):
- empresas() transform que reconstrói service_type compat — REMOVER reconstrução, manter apenas adição de `servicos_contratados`
- update() já está enxuto desde o Plan 14-04 — NÃO precisa mais limpar (verificar via grep)
- resolverSlugsSetores() helper privado deprecated — REMOVER

EnviarRelatorioFechamentoJob.php (estado pós Plan 14-03):
- Mapper de vinculadas tem chaves legacy + servicos_contratados — REMOVER as 6 chaves legacy, manter apenas servicos_contratados

Company.php:
- $fillable linha 28-34: REMOVER service_type, contract_type, contract_start, contract_end, additional_service, additional_service_price
- $casts linha 36-42: REMOVER contract_start, contract_end (date casts), service_type (array cast)
- logOnly linha 17: REMOVER service_type, contract_start, contract_end
- labelFromTypes() linhas 50-66 (deprecated proxy mantido no Plan 14-03): REMOVER. Apenas labelFromServicos() sobrevive.
- getServiceTypeLabelAttribute() linhas 68-71: já refatorado no Plan 14-03 para usar contratos; manter

<!-- IMPORTANTE: ordem cirúrgica -->
1. Pre-flight verificar-cobranca (humano)
2. Remover compat temporária dos controllers + model + job (sem isso, props passam para JSX antigos referenciando colunas que existirão)
3. Migration 3 (drop) — humano roda
4. Checkpoint humano: smoke test apenas dos endpoints HTTP backend
5. Plan 14-07 (próximo) cuida da UI JSX restante

Erro mais provável: aplicar migration 3 ANTES de remover $fillable em Company → save() falha porque mass-assignment tenta gravar coluna que não existe.

<!-- AVISO sobre Plan 14-07 -->
Os JSX consumers (Admin/Empresas, Comercial/Empresas, Mlb/Empresas, Companies/Index, Admin/Financeiro fallback) AINDA leem `service_type` no momento em que este plan rodar. Após o drop + cleanup do mapper, esses JSX vão receber `undefined` para essas chaves. Isso resulta em:
- Listas mostrando campos vazios ou "—" onde antes mostravam service_type
- NENHUMA quebra crítica (undefined silencioso, não throw)
- Smoke test do checkpoint deve VERIFICAR ESPECIFICAMENTE que páginas carregam (200 OK) MESMO com undefined nos campos legacy. NÃO espera UI funcionar perfeitamente até o Plan 14-07.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight obrigatório — phase14:verificar-cobranca antes de QUALQUER cleanup</name>
  <what-built>
    Os Plans 14-01 e 14-02 já entregaram o helper + migrations + comando. Antes de qualquer mudança de código que torne o cálculo legacy inviável (drop ou remoção de $fillable), o usuário precisa confirmar in-loco que o cálculo novo bate com o legacy.
  </what-built>
  <how-to-verify>
    1. No host XAMPP, com banco populado (migrações Plans 14-02 já aplicadas), rodar:
       ```
       php artisan phase14:verificar-cobranca --abort-on-divergence
       ```
    2. Inspecionar a saída:
       - Se "Todas as N empresas conferem (0 divergências)" → APROVADO (continue)
       - Se "X empresa(s) com divergência" → ABORT — não prossiga; descrever as divergências encontradas para investigação manual

    3. Se houver divergências, NÃO continuar. As mais comuns:
       - Empresa com additional_service preenchido mas sem contrato adicional correspondente → re-rodar migration 2 (idempotente)
       - Empresa com contract_end no passado e contrato marcado ativo → inspecionar manualmente
       - Empresa com additional_service vazio mas additional_service_price preenchido → caso de canto; documentar e decidir manualmente
  </how-to-verify>
  <resume-signal>Aprovado: 0 divergências confirmadas no output. OU: descreva o problema com cópia da saída.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remover compat temporária — Company.php $fillable/$casts/logOnly + labelFromTypes</name>
  <files>app/Models/Company.php</files>
  <read_first>
    - app/Models/Company.php (arquivo completo, estado pós Plan 14-03)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-07, D-09)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §6 (logs históricos preservados)
  </read_first>
  <action>
    Editar `app/Models/Company.php`:

    1. **`$fillable` (linhas 28-34)** — REMOVER as 6 colunas legacy:
       ```
       protected $fillable = [
           'name', 'cnpj', 'adman_account_id', 'adman_store_id', 'ml_store_id',
           'segment', 'active', 'status', 'notes',
           'parent_company_id',
       ];
       ```

    2. **`$casts` (linhas 36-42)** — REMOVER `contract_start`, `contract_end`, `service_type`:
       ```
       protected $casts = [
           'active' => 'boolean',
           'status' => 'string',
       ];
       ```

    3. **`logOnly` (linha 17)** — REMOVER `service_type`, `contract_start`, `contract_end`:
       ```
       ->logOnly(['name', 'cnpj', 'segment', 'active', 'status', 'notes', 'adman_account_id', 'ml_store_id'])
       ```

    4. **`labelFromTypes()` (linhas 50-66 — deprecated proxy do Plan 14-03)** — REMOVER COMPLETAMENTE. Apenas `labelFromServicos()` (adicionada no Plan 14-03) sobrevive.

    5. **`getServiceTypeLabelAttribute()` (linhas 68-71)** — MANTER (já refatorado no Plan 14-03 para usar contratos). Confirmar que chama `static::labelFromServicos(...)` e NÃO `labelFromTypes`.

    Comentários em pt-BR documentando o que foi removido e por quê ("Phase 14: campos legacy dropados via migration 2026_05_27_100003").

    Per D-09. Activity log de mudanças históricas continua legível (RESEARCH §6 — logs são JSON imutáveis).
  </action>
  <verify>
    <automated>php -l app/Models/Company.php</automated>
  </verify>
  <done>
    Arquivo sem syntax errors. Grep `service_type|contract_type|contract_start|contract_end|additional_service` no arquivo retorna 0 matches em código (comentários explicando o cleanup podem citar os nomes — OK).
  </done>
</task>

<task type="auto">
  <name>Task 3: Cleanup dos 5 controllers/job — remover compat temporária + validation legacy + reconstruções service_type</name>
  <files>app/Http/Controllers/AdminController.php, app/Http/Controllers/ComercialController.php, app/Http/Controllers/MlbController.php, app/Http/Controllers/CompanyController.php, app/Jobs/EnviarRelatorioFechamentoJob.php</files>
  <read_first>
    - app/Http/Controllers/AdminController.php (estado pós Plan 14-03 — mappers ainda têm chaves legacy)
    - app/Http/Controllers/ComercialController.php (estado pós Plan 14-04 — empresas() tem transform compat; update() JÁ ENXUTO no 14-04)
    - app/Http/Controllers/MlbController.php (estado pós Plan 14-03 — get() inclui service_type)
    - app/Http/Controllers/CompanyController.php (estado pós Plan 14-03)
    - app/Jobs/EnviarRelatorioFechamentoJob.php (estado pós Plan 14-03 — mapper com chaves legacy + servicos_contratados)
  </read_first>
  <action>
    **AdminController.php**:

    1. **`empresas()` mapper (linhas ~31-52)** — REMOVER as 6 chaves legacy (service_type, contract_type, contract_start, contract_end, additional_service, additional_service_price). Manter `servicos_contratados`.

    2. **`updateEmpresa()` (linhas 54-92)** — REMOVER validation rules das 6 colunas. Validation final deve conter apenas `parent_company_id` e `filha_ids`. Remover comentário `TODO Plan 14-06`.

    3. **`fechamento()` mapper (linhas ~259-285)** — REMOVER as 6 chaves legacy do array `$dadosPorId[$c->id]`. Manter `servicos_contratados` e `cobranca_mensal`.

    4. **`updateFechamento()` (linhas 370-389)** — REMOVER validation rules completas. Decisão: manter o método existindo mas como NO-OP com `return back();` (caso a rota PATCH ainda seja chamada por algum lugar). Inspecionar route('admin.financeiro.update') no projeto — se nenhum caller, REMOVER método + rota; se algum caller, manter no-op com comentário pt-BR explicando.

    5. **`gerarRelatorio()` mapper de filhas (linhas ~487-506)** — REMOVER chaves legacy. Manter `servicos_contratados` (string).

    6. **`gerarRelatorio()` cálculo pai (linhas ~510-528)** — REMOVER `$adicionalPai`. Confirmar que usa apenas `CobrancaCalculator::novo`. Adicionar `'servicos_contratados_pai' => ...` no array da view (se ainda não estiver).

    7. **`gerarRelatorioGeral()` (linhas 537-720)** — mesmo cleanup das filhas e pai.

    **ComercialController.php**:

    1. **`empresas()` (linhas ~35-50)** — no `transform()`, REMOVER a reconstrução de `$c->service_type = $nomes->map(...)`. Manter apenas a linha que define `$c->servicos_contratados`.

    2. **`update()` (linhas 149-174)** — já enxuto desde Plan 14-04. Apenas REMOVER o comentário `// Plan 14-06 dropa as 6 colunas legacy do schema` (substituir por `// Phase 14: colunas legacy dropadas`).

    3. **`resolverSlugsSetores()` (linhas 212-222)** — REMOVER método completo (deprecated no Plan 14-04, sem callers após Plan 14-04). Confirmar via grep `resolverSlugsSetores` em app/ → 0 matches antes de remover.

    **MlbController.php**:

    1. **`empresas()` (linhas ~1525-1546)** — no `get(['id', 'name', 'service_type', 'notes', 'created_at'])` REMOVER `service_type` da lista de campos. No `transform()`, manter apenas a linha que define `$e->servicos_contratados`.

    **CompanyController.php**:

    1. **`index()` (linhas ~83-97)** — análogo: REMOVER `service_type` do `get([...])` + manter apenas adição de `servicos_contratados` no transform.

    **EnviarRelatorioFechamentoJob.php**:

    1. **Mapper de vinculadas (linhas ~115-140)** — REMOVER as 6 chaves legacy (`'service_type' => $f->service_type`, etc). Manter APENAS `'servicos_contratados' => ...`.

    Comentários em pt-BR documentando "Phase 14 Plan 14-06: compat temporária removida — campos legacy dropados via migration 2026_05_27_100003".
  </action>
  <verify>
    <automated>php -l app/Http/Controllers/AdminController.php && php -l app/Http/Controllers/ComercialController.php && php -l app/Http/Controllers/MlbController.php && php -l app/Http/Controllers/CompanyController.php && php -l app/Jobs/EnviarRelatorioFechamentoJob.php</automated>
  </verify>
  <done>
    Todos os 5 arquivos sem syntax errors. `grep -rnE "service_type|contract_type|contract_start|contract_end|additional_service|additional_service_price" app/Http/Controllers/ app/Jobs/` retorna apenas matches em comentários explicativos (de história) ou 0 matches. Validation rules de updateEmpresa/updateFechamento limpas.
  </done>
</task>

<task type="auto">
  <name>Task 4: Adicionar prop servicos_disponiveis em AdminController::empresas + Comercial empresas + fechamento (necessário para modal de contratos do Plan 14-07)</name>
  <files>app/Http/Controllers/AdminController.php, app/Http/Controllers/ComercialController.php</files>
  <read_first>
    - app/Http/Controllers/AdminController.php (método empresas + fechamento pós Task 3 deste plan)
    - app/Http/Controllers/ComercialController.php (método empresas pós Task 3 deste plan)
    - app/Models/Servico.php
  </read_first>
  <action>
    **AdminController::empresas()** — adicionar prop `servicos_disponiveis`:
    ```
    $servicosDisponiveis = \App\Models\Servico::where('ativo', true)->orderBy('nome')->get(['id', 'nome', 'valor_padrao', 'tipo_cobranca']);
    return Inertia::render('Admin/Empresas', [
        'companies' => $companies,
        'servicos_disponiveis' => $servicosDisponiveis,
    ]);
    ```

    **AdminController::fechamento()** — adicionar a mesma prop (necessário para modal Add Contrato do Plan 14-05/14-07):
    ```
    return Inertia::render('Admin/Financeiro', [
        // ... props existentes ...
        'servicos_disponiveis' => \App\Models\Servico::where('ativo', true)->orderBy('nome')->get(['id', 'nome', 'valor_padrao', 'tipo_cobranca']),
    ]);
    ```

    **ComercialController::empresas()** — análogo:
    ```
    $servicosDisponiveis = \App\Models\Servico::where('ativo', true)->orderBy('nome')->get(['id', 'nome', 'valor_padrao', 'tipo_cobranca']);
    return Inertia::render('Comercial/Empresas', [
        'companies' => $companies,
        'servicos_disponiveis' => $servicosDisponiveis,
    ]);
    ```

    O Plan 14-07 consome essas props para alimentar os selects dos modais Add/Edit contrato.
  </action>
  <verify>
    <automated>php -l app/Http/Controllers/AdminController.php && php -l app/Http/Controllers/ComercialController.php</automated>
  </verify>
  <done>
    Ambos controllers passam `servicos_disponiveis` para os respectivos componentes Inertia (Admin/Empresas, Admin/Financeiro, Comercial/Empresas).
  </done>
</task>

<task type="auto">
  <name>Task 5: Criar migration 3 — drop das 6 colunas legacy com down() de recriação exata</name>
  <files>database/migrations/2026_05_27_100003_drop_legacy_service_columns_from_companies.php</files>
  <read_first>
    - database/migrations/2026_05_19_100001_add_service_fields_to_companies.php (down() referência — usa dropColumn em array)
    - database/migrations/2026_05_25_300001_convert_service_type_to_json_array.php (confirma que service_type virou TEXT)
    - database/migrations/2026_05_20_100002_add_additional_service_price_to_companies.php
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-06)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §2 (Pitfall 7 — recriar como TEXT)
  </read_first>
  <action>
    Criar `database/migrations/2026_05_27_100003_drop_legacy_service_columns_from_companies.php`:

    Cabeçalho com comentário pt-BR documentando:
    - Propósito: drop final das 6 colunas legacy de companies
    - Pre-requisito: `php artisan phase14:verificar-cobranca` retornou 0 divergências (Plan 14-01 + Task 1 deste plan)
    - Coexistência: Plans 14-03/04/05 + Tasks 2-4 deste plan já removeram TODOS os reads e writes das 6 colunas em PHP; Plan 14-07 cuida dos JSX restantes
    - Rollback: down() recria estrutura vazia (sem rollback de dados pós-drop — documentado)

    `up()`:
    ```
    public function up(): void
    {
        Schema::table('companies', function (Blueprint $table) {
            $table->dropColumn([
                'service_type',
                'contract_type',
                'contract_start',
                'contract_end',
                'additional_service',
                'additional_service_price',
            ]);
        });
    }
    ```

    `down()` — recreate EXACT (Pitfall 7 do RESEARCH — TEXT não string):
    ```
    public function down(): void
    {
        Schema::table('companies', function (Blueprint $table) {
            // Comentário pt-BR: tipos EXATAMENTE como estavam pré-drop.
            // service_type era TEXT (não string) — convertido pela migration
            // 2026_05_25_300001_convert_service_type_to_json_array.php.
            // down() recria estrutura vazia; dados pós-drop NÃO são restaurados.
            $table->text('service_type')->nullable()->after('notes');
            $table->string('contract_type')->nullable()->after('service_type');
            $table->date('contract_start')->nullable()->after('contract_type');
            $table->date('contract_end')->nullable()->after('contract_start');
            $table->string('additional_service')->nullable()->after('contract_end');
            $table->decimal('additional_service_price', 10, 2)->nullable()->after('additional_service');
        });
    }
    ```

    Comentários inline em pt-BR. Per D-06 e RESEARCH §2 §Pitfall 7.

    NÃO RODAR a migration neste task — humano roda no Task 6 (próximo) após confirmação explícita. O arquivo apenas existe pronto para `php artisan migrate --path=...`.
  </action>
  <verify>
    <automated>php artisan migrate --pretend --path=database/migrations/2026_05_27_100003_drop_legacy_service_columns_from_companies.php</automated>
  </verify>
  <done>
    Arquivo existe. `--pretend` mostra o SQL `ALTER TABLE companies DROP COLUMN service_type, ...` esperado. Down() usa `text(...)` (não `string()`) para service_type. `php -l` sem erros.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Checkpoint humano — aplicar migration 3 + smoke test BACKEND (sem UI ainda)</name>
  <what-built>
    - Plans 14-01 a 14-05 + cleanup das Tasks 2-5 deste plan já entregaram:
      - Helper CobrancaCalculator + comando phase14:verificar-cobranca
      - Migrations 1 e 2 (catálogo + dados)
      - Refator dos 7 PHP consumers + 3 Blade views + UI Fechamento (Plan 14-05)
      - Compat temporária BACKEND removida (mappers Inertia limpos, Company.php limpo, controllers/job sem chaves legacy)
    - Migration 3 (drop) está PRONTA mas não foi aplicada.
    - Pre-flight (Task 1) já foi aprovado.
  </what-built>
  <how-to-verify>
    1. **Pre-flight final** (re-confirmar — pode haver mudanças desde Task 1):
       ```
       php artisan phase14:verificar-cobranca --abort-on-divergence
       ```
       Confirme 0 divergências. SE divergente, ABORT.

    2. **Backup recomendado** (Hostinger VPS — operação manual):
       ```
       # No servidor de produção (não local):
       mysqldump -u USER -p DATABASE > backup_pre_phase14_drop_$(date +%Y%m%d_%H%M%S).sql
       ```
       Em dev local com SQLite: cópia do .sqlite file.

    3. **Aplicar migration 3**:
       ```
       php artisan migrate --path=database/migrations/2026_05_27_100003_drop_legacy_service_columns_from_companies.php
       ```
       Confirme saída: "Migrating: 2026_05_27_100003_drop_legacy_service_columns_from_companies" → "Migrated".

    4. **Verificar que as colunas sumiram**:
       ```
       php artisan tinker --execute="echo (int) \\Schema::hasColumn('companies', 'service_type');"
       # Esperado: 0
       php artisan tinker --execute="echo (int) \\Schema::hasColumn('companies', 'additional_service_price');"
       # Esperado: 0
       ```

    5. **Smoke test BACKEND pós-drop** (endpoints retornam 200 — UI ainda não está limpa, isso é esperado):
       a. `/administrativo/financeiro` carrega sem 500 — campos legacy na UI mostram vazio/undefined (CORRIGIDO no Plan 14-07)
       b. `/comercial/empresas/novo` (POST) cadastra empresa nova com servicos[] do catálogo; resultado redireciona com flash success
       c. `/comercial/empresas` carrega sem 500
       d. `/mlb/empresas` carrega sem 500
       e. `/companies` carrega sem 500
       f. Gerar relatório individual (`gerarRelatorio`): Blade renderiza sem erro 500 (Blades já refatoradas no Plan 14-05)
       g. Gerar relatório geral (`gerarRelatorioGeral`): idem

       **IMPORTANTE**: visual incompleto é ESPERADO neste momento. O check é que NENHUM endpoint retorna 500. Plan 14-07 corrige a UI completamente.

    6. **Suíte completa de testes**:
       ```
       php artisan test
       ```
       Confirme verde. Se algum teste falhar, NÃO completar — investigar primeiro.

    7. **Grep final no BACKEND** (no shell do projeto):
       ```
       grep -rnE "service_type|contract_type|contract_start|contract_end|additional_service|additional_service_price" app/ | grep -v ".planning" | grep -v "//"
       ```
       Esperado: apenas comentários explicativos OU 0 matches. Migrations históricas em `database/migrations/` que CRIARAM as colunas (2026_05_19_, 2026_05_20_, 2026_05_25_) podem aparecer — IGNORAR.

    8. **`phase14:verificar-cobranca` pós-drop**:
       ```
       php artisan phase14:verificar-cobranca
       ```
       Esperado: comando AINDA funciona (lê apenas contratos_servico — não tocou colunas legacy). Output: "0 divergências".
  </how-to-verify>
  <resume-signal>Aprovado: drop aplicado + endpoints retornam 200 + suíte verde + grep backend limpo. Plan 14-07 vai limpar JSX restantes. OU descreva o problema (anexar saída de comando ou screenshot).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database migration | Operação irreversível em prod sem backup; pre-flight + checkpoint humano mitigam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Denial of Service | Migration 3 dropa colunas usadas por código não-refatorado (escapou ao audit) | mitigate | Tasks 2-4 cleanup obrigatório ANTES da Task 5 (criação da migration); Task 6 grep final como gate; rollback via down() recria estrutura vazia (dados perdidos, mas app volta a funcionar com seed manual) |
| T-14-08 | Information Disclosure | Backup pré-drop pode conter PII (cnpj, name) — armazenado sem encriptação | accept | Backup ficará no servidor sob permissão restrita; PII em logs/activitylog já é prática do projeto |
| T-14-09 | Tampering | Operador roda `php artisan migrate` sem `--path` específico, aplicando migrations não auditadas | mitigate | Task 6 instrução explícita usa `--path=` para a migration 3; pre-flight como gate visual |
</threat_model>

<verification>
- `php artisan phase14:verificar-cobranca` → 0 divergências (pre + pós drop)
- `php artisan migrate --path=...100003...` → "Migrated"
- `Schema::hasColumn('companies', 'service_type')` → false
- `grep -rnE "service_type|contract_type|contract_start|contract_end|additional_service|additional_service_price" app/ | grep -v "//"` → 0 matches funcionais
- `php artisan test` → suite completa verde
- Endpoints HTTP retornam 200 (UI ainda quebrada visualmente é OK — Plan 14-07 corrige)
- Checkpoint humano aprovado smoke test backend
</verification>

<success_criteria>
1. As 6 colunas legacy não existem mais em `companies` (Schema::hasColumn retorna false)
2. Down() da migration 3 recria estrutura EXATA (service_type como TEXT — Pitfall 7)
3. Company.php: $fillable, $casts, logOnly limpos das 6 colunas; labelFromTypes removido
4. 5 controllers + 1 job livres de referências funcionais aos campos legacy
5. Suíte completa de testes verde
6. Endpoints backend retornam 200 (UI completa virá no Plan 14-07)
7. `phase14:verificar-cobranca` continua funcionando e retorna 0 divergências
8. Grep `service_type|contract_type|...` em app/ retorna 0 matches funcionais
</success_criteria>

<output>
Criar `.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-06-SUMMARY.md` listando: arquivos modificados, número de empresas afetadas pela migration de drop, output do `phase14:verificar-cobranca` final (cole o número de empresas verificadas), confirmação do checkpoint humano backend. Nota: "Backend cleanup COMPLETO + drop aplicado. Próximo: Plan 14-07 (limpeza dos 5 JSX consumers + fallback do ServiceBadge)".

Commit (pt-BR):
```
chore(14): drop colunas legacy + cleanup backend (Plan 14-06)

- migration 2026_05_27_100003_drop_legacy_service_columns_from_companies — dropa 6 colunas
- Company.php — $fillable/$casts/logOnly limpos; labelFromTypes removido
- 5 controllers + job — mappers Inertia sem chaves legacy; validation rules limpas
- AdminController/ComercialController — adicionam servicos_disponiveis (necessário para Plan 14-07)
- Pre-flight phase14:verificar-cobranca → 0 divergências
- Checkpoint humano aprovou smoke test backend

Per D-06, D-07, D-08, D-09. Próximo: Plan 14-07 (frontend cleanup).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
</output>
</content>
