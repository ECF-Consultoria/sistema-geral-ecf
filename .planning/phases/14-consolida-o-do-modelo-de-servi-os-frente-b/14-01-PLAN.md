---
phase: 14-consolida-o-do-modelo-de-servi-os-frente-b
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/Support/CobrancaCalculator.php
  - app/Console/Commands/Phase14VerificarCobranca.php
  - tests/Unit/CobrancaCalculatorTest.php
  - tests/Feature/Phase14VerificarCobrancaTest.php
autonomous: true
requirements:
  - SVC-02
must_haves:
  truths:
    - "Helper estático puro `CobrancaCalculator::legacy()` e `::novo()` existe e é testável sem container Laravel"
    - "Comando Artisan `phase14:verificar-cobranca` itera todas as empresas e compara cálculo legacy vs novo"
    - "Comando retorna exit code 1 quando há divergência > R$ 0,01 com flag `--abort-on-divergence`"
    - "Teste unitário do helper passa com >=8 cenários cobrindo (legacy null, legacy completo, novo vazio, novo com mensal+unica, novo filtrando inativos, novo filtrando tipo_cobranca, edge cases R$ 0,01)"
  artifacts:
    - path: "app/Support/CobrancaCalculator.php"
      provides: "Helper puro com métodos estáticos legacy() e novo()"
      contains: "class CobrancaCalculator"
    - path: "app/Console/Commands/Phase14VerificarCobranca.php"
      provides: "Comando Artisan `phase14:verificar-cobranca`"
      contains: "protected $signature = 'phase14:verificar-cobranca"
    - path: "tests/Unit/CobrancaCalculatorTest.php"
      provides: "Testes unitários do helper (>= 8 casos)"
      contains: "class CobrancaCalculatorTest"
    - path: "tests/Feature/Phase14VerificarCobrancaTest.php"
      provides: "Teste integração do comando Artisan"
      contains: "class Phase14VerificarCobrancaTest"
  key_links:
    - from: "app/Console/Commands/Phase14VerificarCobranca.php"
      to: "app/Support/CobrancaCalculator.php"
      via: "uso estático CobrancaCalculator::legacy() e ::novo()"
      pattern: "CobrancaCalculator::(legacy|novo)"
---

<objective>
Criar o helper estático puro `CobrancaCalculator` (`app/Support/`) com métodos `legacy()` e `novo()` testáveis sem container Laravel, e o comando Artisan `phase14:verificar-cobranca` que itera todas as empresas comparando ambos cálculos. Este plan é PURO PREP — nenhuma mudança em produção, apenas infraestrutura de verificação que será usada pelo Plan 14-06 como pre-flight do drop das colunas legacy.

Purpose: Garantir invariante de SVC-02 (fatura idêntica entre legacy e novo modelo até R$ 0,01) com falha-loud automatizada antes do drop irreversível das colunas. Sem isso, o drop é um salto sem rede.

Output: 1 helper + 1 comando Artisan + 2 suítes de testes verdes. Commit atômico em pt-BR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Modelos da Frente A (NÃO MODIFICAR) — para referência do helper -->

`App\Models\Servico` (app/Models/Servico.php:17):
```
class Servico extends Model {
    use LogsActivity;
    protected $table = 'servicos';
    protected $fillable = ['nome', 'valor_padrao', 'tipo_cobranca', 'ativo'];
    protected $casts = ['valor_padrao' => 'decimal:2', 'ativo' => 'boolean'];
    public const TIPO_MENSAL = 'mensal';
    public const TIPO_UNICA  = 'unica';
    public function contratos(): HasMany;          // hasMany(ContratoServico)
    public function scopeActive($query);           // where('ativo', true)
}
```

`App\Models\ContratoServico` (app/Models/ContratoServico.php:17):
```
class ContratoServico extends Model {
    use LogsActivity;
    protected $table = 'contratos_servico';
    protected $fillable = ['company_id', 'servico_id', 'valor_contratado',
                           'data_contratacao', 'data_vencimento', 'ativo', 'observacoes'];
    protected $casts = ['valor_contratado' => 'decimal:2',
                        'data_contratacao' => 'date:Y-m-d',
                        'data_vencimento'  => 'date:Y-m-d',
                        'ativo'            => 'boolean'];
    public function company(): BelongsTo;          // belongsTo(Company)
    public function servico(): BelongsTo;          // belongsTo(Servico)
    public function scopeActive($query);           // where('ativo', true)
}
```

`App\Models\Company` (app/Models/Company.php:170-173) — relação já existente da Frente A:
```
public function contratosServico() {
    return $this->hasMany(ContratoServico::class);
}
```

Tabela FAIXAS (AdminController.php:22-29) — constante hardcoded retornada por `calcularFaixa()`:
```
[
    ['limite' => 499_999.99, 'label' => 'faixa_1', 'valor' => 3_000.00],
    // ... 6 faixas no total
]
// calcularFaixa() retorna ['faixa' => 'faixa_X', 'valor' => 3000.00] ou null
```

Fórmula legacy (AdminController.php:280-282):
```
'cobranca_mensal' => ($faixaData !== null || $c->additional_service_price)
    ? (float) ($faixaData['valor'] ?? 0) + (float) ($c->additional_service_price ?? 0)
    : null,
```

phpunit.xml configura SQLite em memória — testes do helper devem funcionar com mocks puros (sem touch no DB).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar CobrancaCalculator helper + suíte de testes unitários (RED → GREEN)</name>
  <files>app/Support/CobrancaCalculator.php, tests/Unit/CobrancaCalculatorTest.php</files>
  <read_first>
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-03, D-08, D-10 — fórmulas exatas)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §3 §4 (helper puro + pitfall 4)
    - app/Http/Controllers/AdminController.php linhas 22-29 (FAIXAS) e 280-282 (fórmula legacy atual)
    - app/Models/Servico.php (constants TIPO_MENSAL/TIPO_UNICA)
    - app/Models/ContratoServico.php (campos ativo, valor_contratado, servico relation)
  </read_first>
  <behavior>
    Testes (8 casos, todos no namespace `Tests\Unit` SEM `RefreshDatabase`):
    - Test 1: `legacy(null, null)` retorna `0.0`
    - Test 2: `legacy(['valor' => 3000.00], null)` retorna `3000.0`
    - Test 3: `legacy(null, 200.50)` retorna `200.5`
    - Test 4: `legacy(['valor' => 3000.00], 200.50)` retorna `3200.5`
    - Test 5: `novo(null, [])` retorna `0.0`
    - Test 6: `novo(['valor' => 3000.00], [])` retorna `3000.0`
    - Test 7: `novo(null, [contrato(150, 'mensal', true), contrato(50, 'unica', true)])` retorna `150.0` (filtra tipo_cobranca='mensal' apenas)
    - Test 8: `novo(['valor' => 3000.00], [contrato(150, 'mensal', false), contrato(200, 'mensal', true)])` retorna `3200.0` (ignora contrato inativo)

    Os "contratos" nos testes são objetos anônimos `(object)['ativo' => true, 'valor_contratado' => 150, 'servico' => (object)['tipo_cobranca' => 'mensal']]` — não tocam DB (Pitfall 4 do RESEARCH).
  </behavior>
  <action>
    Criar `app/Support/CobrancaCalculator.php` com namespace `App\Support` e duas funções estáticas puras:

    1. `public static function legacy(?array $faixaData, ?float $additionalServicePrice): float` — retorna `(float) ($faixaData['valor'] ?? 0) + (float) ($additionalServicePrice ?? 0)`. Pura aritmética.

    2. `public static function novo(?array $faixaData, iterable $contratos): float` — itera `$contratos` filtrando por `$c->ativo === true && $c->servico && $c->servico->tipo_cobranca === Servico::TIPO_MENSAL`; soma `(float) $c->valor_contratado` desses; retorna `(float) ($faixaData['valor'] ?? 0) + $soma`. Aceita `iterable` para permitir Collection eager-loaded OU array de objetos anônimos em testes (Pitfall 4 do RESEARCH §3).

    DECISÃO técnica (preservar semântica legacy de "null quando sem nada"): os 3 call-sites em AdminController retornam `null` quando `$faixaData === null && componente_extra == 0`. **Para o helper puro, retornar float sempre** (mais simples, testável); o caller (no Plan 14-03) decide quando converter para null via `?: null` no array Inertia. Documentar isso em PHPDoc no método.

    Comentários em pt-BR. PHPDoc completo em ambas as funções com `@param` e `@return`. Usar `Servico::TIPO_MENSAL` (não string literal) para o filtro — referenciar a constante via `use App\Models\Servico` no topo.

    Criar `tests/Unit/CobrancaCalculatorTest.php` com namespace `Tests\Unit` estendendo `Tests\TestCase` SEM `RefreshDatabase` (puro — sem DB). Implementar os 8 testes do `<behavior>`. Cada teste usa `$this->assertEqualsWithDelta(esperado, atual, 0.001)` para floats (NÃO `assertEquals` direto em float). Helper interno `private function contratoMock(float $valor, string $tipo, bool $ativo): object { return (object)['ativo' => $ativo, 'valor_contratado' => $valor, 'servico' => (object)['tipo_cobranca' => $tipo]]; }`.

    Mensagens de assert e comentários em pt-BR. Per D-10.
  </action>
  <verify>
    <automated>php artisan test --filter=CobrancaCalculatorTest</automated>
  </verify>
  <done>
    `tests/Unit/CobrancaCalculatorTest.php` roda verde com 8 testes (>=8 assertions). `app/Support/CobrancaCalculator.php` tem ambos métodos `legacy()` e `novo()` estáticos públicos, sem dependência de Laravel container (não usa `app()`, `resolve()`, ou facades). `php -l app/Support/CobrancaCalculator.php` retorna "No syntax errors detected".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar comando Artisan phase14:verificar-cobranca + teste de integração</name>
  <files>app/Console/Commands/Phase14VerificarCobranca.php, tests/Feature/Phase14VerificarCobrancaTest.php</files>
  <read_first>
    - app/Support/CobrancaCalculator.php (criado na Task 1)
    - app/Http/Controllers/AdminController.php linhas 22-29 (FAIXAS const) + 99-332 (calcularFaixa, lógica fechamento)
    - app/Models/Company.php (campos legacy AINDA EXISTEM nesta wave: service_type, additional_service_price)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §4 (sequência de execução em prod)
    - app/Console/Commands/MetricsCleanup.php (analog: estrutura padrão de comando neste projeto, padrão `[Phase14]` em logs)
  </read_first>
  <behavior>
    Comando assinado: `phase14:verificar-cobranca {--abort-on-divergence : Aborta com exit 1 se houver divergência}`.

    Comportamento:
    - Itera `Company::with(['contratosServico' => fn($q) => $q->where('ativo', true)->with('servico')])->chunk(100, ...)` (eager loading obrigatório — Pitfall 2 do RESEARCH)
    - Para cada empresa: calcula `$faixaData` usando a mesma `calcularFaixa()` do AdminController (PROBLEMA: método é privado/protegido; SOLUÇÃO: replicar a tabela FAIXAS como constante no próprio comando, mantendo paridade com AdminController::FAIXAS verbatim — duplicação aceita porque é código temporário, removido pós-Phase 14)
    - Faturamento usado pelo helper: SUM de `adman_metrics.revenue` do mês atual (últimos 30 dias) para a empresa, replicando lógica de `AdminController::fechamento` (chunk lê `\App\Models\AdmanMetric::whereBetween(...)->whereNotNull('revenue')->where('company_id', $c->id)->sum('revenue')`)
    - `$legacy = CobrancaCalculator::legacy($faixaData, (float) ($c->additional_service_price ?? 0))`
    - `$novo = CobrancaCalculator::novo($faixaData, $c->contratosServico)`
    - Se `abs($legacy - $novo) > 0.01`: incrementa contador; loga via `Log::error('[Phase14] cobranca divergente empresa #{id} ({name}): legacy=R$ X novo=R$ Y')` E `$this->error(...)` mesma mensagem
    - Ao fim: se `$divergencias > 0`: `$this->error("[Phase14] {N} empresa(s) com divergência")` + retorna `1` se `--abort-on-divergence`, senão `0`
    - Se 0 divergências: `$this->info("[Phase14] Todas as {N} empresas conferem (0 divergências).")` + retorna `0`

    Testes (`tests/Feature/Phase14VerificarCobrancaTest.php` com `RefreshDatabase`):
    - Test 1: `test_zero_divergencias_quando_dados_consistentes()` — cria 1 empresa com additional_service_price=200 + 1 servico mensal "Treinamento" valor_padrao=200 + 1 contrato ativo valor_contratado=200 vinculado; executa `Artisan::call('phase14:verificar-cobranca')`; assertEquals(0, $exit); assertStringContainsString('0 divergências', Artisan::output())
    - Test 2: `test_aborta_com_divergencia()` — cria 1 empresa com additional_service_price=200 SEM contrato correspondente; executa `Artisan::call('phase14:verificar-cobranca', ['--abort-on-divergence' => true])`; assertEquals(1, $exit); assertStringContainsString('divergência', Artisan::output())
    - Test 3: `test_sem_abort_flag_retorna_zero_mesmo_com_divergencia()` — mesma divergência do Test 2 SEM flag; assertEquals(0, $exit)
  </behavior>
  <action>
    Criar `app/Console/Commands/Phase14VerificarCobranca.php` (namespace `App\Console\Commands`, extends `Illuminate\Console\Command`) seguindo padrão dos outros comandos do projeto:
    - `protected $signature` = `'phase14:verificar-cobranca {--abort-on-divergence : Aborta com exit code 1 se houver divergência}'`
    - `protected $description` = `'Phase 14: compara cálculo de cobrança legacy vs novo (contratos_servico) em todas as empresas. Roda ANTES do drop das colunas legacy (Plan 14-06).'`
    - Método `handle(): int` implementa o loop conforme `<behavior>`. Imports: `App\Support\CobrancaCalculator`, `App\Models\Company`, `App\Models\AdmanMetric`, `Carbon\Carbon`, `Illuminate\Support\Facades\Log`.

    Replicar a tabela FAIXAS como `private const FAIXAS = [...]` (copy/paste verbatim de AdminController.php:22-29). Implementar `private function calcularFaixa(?float $faturamento): ?array` que itera as 6 faixas retornando o match ou null se faturamento for null. Adicionar comentário pt-BR explicando que essa duplicação é intencional e temporária (removida após Phase 14 concluída).

    Implementar `private function faturamentoDoMes(int $companyId): ?float` que chama `AdmanMetric::whereBetween('reference_date', [Carbon::now()->subDays(30), Carbon::now()])->whereNotNull('revenue')->where('company_id', $companyId)->sum('revenue') ?: null`.

    No loop principal usar `chunk(100)` (Pitfall 2 — performance). Eager loading conforme `<behavior>`.

    Importante: a comparação tolera R$ 0,01 via `abs($legacy - $novo) > 0.01` (não `!=`).

    Comentários em pt-BR. Prefixo de log `[Phase14]` consistente com o resto do projeto. Per D-08, D-10.

    Criar `tests/Feature/Phase14VerificarCobrancaTest.php` (namespace `Tests\Feature`, extends `Tests\TestCase`, usa `RefreshDatabase`). Implementar os 3 testes do `<behavior>`. Usar factories existentes (`Company::factory()->create(...)`) e criar Servico/ContratoServico via `Model::create(...)` direto (factories podem não existir ainda — preferir criação manual). Cada teste retorna `Artisan::call(...)` e inspeciona `Artisan::output()` para mensagens.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase14VerificarCobrancaTest</automated>
  </verify>
  <done>
    Comando `php artisan list | grep phase14` mostra `phase14:verificar-cobranca`. Suite `Phase14VerificarCobrancaTest` passa com 3 testes verdes. `php -l app/Console/Commands/Phase14VerificarCobranca.php` sem erros.
  </done>
</task>

</tasks>

<verification>
- `php artisan test --filter=CobrancaCalculatorTest` → todos verdes
- `php artisan test --filter=Phase14VerificarCobrancaTest` → todos verdes
- `php artisan list | findstr phase14` → mostra `phase14:verificar-cobranca`
- `php -l app/Support/CobrancaCalculator.php` → "No syntax errors detected"
- `php -l app/Console/Commands/Phase14VerificarCobranca.php` → "No syntax errors detected"
- Nenhum arquivo de produção (Company.php, AdminController.php, migrations, JSX) foi tocado neste plan
</verification>

<success_criteria>
1. Helper estático `CobrancaCalculator::legacy()` e `::novo()` retornam valores conforme fórmulas D-03/D-10
2. Comando `phase14:verificar-cobranca` itera todas empresas e printa relatório de divergências
3. Flag `--abort-on-divergence` força exit 1 quando há divergência > R$ 0,01; sem flag retorna 0 mesmo divergente
4. 11+ assertions verdes entre as 2 suítes de teste (8 unit + 3 feature)
5. Zero impacto em rotas, views, JSX ou queries de produção
</success_criteria>

<output>
Criar `.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-01-SUMMARY.md` ao final, listando: arquivos criados, comando registrado, comandos de teste rodados, e nota explícita "Helper pronto para consumo pelos Plans 14-03 e 14-06".

Commit (pt-BR conforme CLAUDE.md):
```
feat(14): cria helper CobrancaCalculator + comando phase14:verificar-cobranca

- app/Support/CobrancaCalculator.php — helper estático puro com legacy() e novo()
- app/Console/Commands/Phase14VerificarCobranca.php — pre-flight do drop das colunas legacy
- tests/Unit/CobrancaCalculatorTest.php — 8 cenários cobrindo todas as branches
- tests/Feature/Phase14VerificarCobrancaTest.php — 3 testes de integração

Per D-08, D-10. Prep work — nenhum arquivo de produção tocado.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
</output>
</content>
