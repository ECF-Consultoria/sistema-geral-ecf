---
phase: 14-consolida-o-do-modelo-de-servi-os-frente-b
plan: 07
type: execute
wave: 4
depends_on:
  - 14-06
files_modified:
  - resources/js/Pages/Admin/Empresas.jsx
  - resources/js/Pages/Admin/Financeiro.jsx
  - resources/js/Pages/Comercial/Empresas.jsx
  - resources/js/Pages/Mlb/Empresas.jsx
  - resources/js/Pages/Companies/Index.jsx
autonomous: false
requirements:
  - SVC-06
  - SVC-04
must_haves:
  truths:
    - "Os 5 JSX consumers (Admin/Empresas, Admin/Financeiro fallback, Comercial/Empresas, Mlb/Empresas, Companies/Index) leem exclusivamente `servicos_contratados` (não mais `service_type`/`contract_*`/`additional_service*`)"
    - "Admin/Empresas.jsx e Comercial/Empresas.jsx substituem editores inline com 6 campos legacy por seção de contratos + modal Add/Edit"
    - "Mlb/Empresas.jsx e Companies/Index.jsx (seção 'Empresas pendentes') leem `p.servicos_contratados` em vez de `p.service_type`"
    - "Admin/Financeiro.jsx ServiceBadge: fallback legacy removido — apenas branch de `servicos_contratados`"
    - "npm run build completa com 0 erros"
    - "Grep `service_type|contract_type|contract_start|contract_end|additional_service|additional_service_price` em resources/js/Pages retorna 0 matches em código vivo"
    - "Checkpoint humano confirma smoke test fim-a-fim de todas as 5 telas"
  artifacts:
    - path: "resources/js/Pages/Admin/Empresas.jsx"
      provides: "Editor inline substituído por seção de contratos + modal"
      contains: "servicos_contratados"
    - path: "resources/js/Pages/Admin/Financeiro.jsx"
      provides: "ServiceBadge sem fallback legacy"
      contains: "servicos_contratados"
    - path: "resources/js/Pages/Comercial/Empresas.jsx"
      provides: "Editor inline substituído por seção de contratos + modal"
      contains: "servicos_contratados"
    - path: "resources/js/Pages/Mlb/Empresas.jsx"
      provides: "Pendentes leem servicos_contratados"
      contains: "servicos_contratados"
    - path: "resources/js/Pages/Companies/Index.jsx"
      provides: "Pendentes leem servicos_contratados"
      contains: "servicos_contratados"
  key_links:
    - from: "resources/js/Pages/Admin/Empresas.jsx"
      to: "URL /empresas/{empresa.id}/contratos-servico"
      via: "modal Add/Edit usa URL crua (decisão pre-flight do Plan 14-05)"
      pattern: "contratos-servico"
    - from: "resources/js/Pages/Comercial/Empresas.jsx"
      to: "URL /empresas/{empresa.id}/contratos-servico"
      via: "modal Add/Edit usa URL crua"
      pattern: "contratos-servico"
---

<objective>
Cleanup FRONTEND da Phase 14: limpar os 5 JSX consumers que ainda leem campos legacy após o drop das colunas (Plan 14-06). Admin/Empresas.jsx e Comercial/Empresas.jsx substituem seus editores inline (que tinham os 6 campos legacy) pela mesma seção de contratos + modal já implementada em Admin/Financeiro.jsx (Plan 14-05). Mlb/Empresas.jsx e Companies/Index.jsx ajustam a renderização da seção "Empresas pendentes" para ler `servicos_contratados`. Admin/Financeiro.jsx remove o fallback legacy do ServiceBadge (adicionado no Plan 14-05 como compat).

**Decisão D-07 ajustada:** Comercial/Empresas.jsx (originalmente listado no D-07 CONTEXT) é consolidado neste plan junto com os outros 4 JSX cleanups. Razão: agrupamento de mudanças JSX permite 1 único `npm run build` e 1 único checkpoint humano de smoke test fim-a-fim.

Purpose: SVC-06, SVC-04 — Fechar o frontend da Phase 14. Após este plan: zero referência funcional aos 6 campos legacy em código aplicativo (PHP + JSX). Fase 14 COMPLETA.

Output: 5 JSX refatorados + checkpoint humano. Commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md

<interfaces>
<!-- Props vindas dos controllers pós Plan 14-06 -->

Cada item em `companies[]` AGORA tem apenas:
- chaves novas: `id, name, status, servicos_contratados[], cobranca_mensal, ...`
- AS 6 chaves legacy NÃO existem mais (controllers limpos no Plan 14-06)

Adicionalmente, `Admin/Empresas`, `Admin/Financeiro` e `Comercial/Empresas` recebem:
- `servicos_disponiveis: [{ id, nome, valor_padrao, tipo_cobranca }]` (Plan 14-06 Task 4)

<!-- Padrão UI a copiar (Plan 14-05) -->

`Admin/Financeiro.jsx` (estado pós Plan 14-05) tem:
- Modal Add/Edit contrato
- Seção de tabela de contratos por empresa
- Helpers `abrirModalContrato`, `salvarContrato`, `desativarContrato` usando URL crua `/empresas/${empresa.id}/contratos-servico[/${contrato.id}]`

Esses helpers e o JSX da seção/modal devem ser COPIADOS (não criar abstração comum por ora — duplicação aceita; refator posterior é fase futura) para Admin/Empresas.jsx e Comercial/Empresas.jsx.

<!-- Arquivos JSX a limpar (estado pré-cleanup) -->

Admin/Empresas.jsx — linhas 131, 147, 424-428, 467, 484-489, 516-526, 528-606, 927, 942-950, 988-989 — LIMPEZA EXTENSA
Comercial/Empresas.jsx — linhas 39-42, 76-95, 106-111, 161-264 — LIMPEZA EXTENSA
Mlb/Empresas.jsx — linha 936-944 — substituir leitura de p.service_type por p.servicos_contratados
Companies/Index.jsx — linha 146 — idem
Admin/Financeiro.jsx — linhas 52-70 (ServiceBadge): limpar fallback do ServiceBadge (passa a ler exclusivamente servicos_contratados)

<!-- Rotas (decisão pre-flight Plan 14-05) -->

URL crua para todos os modais de contrato:
- POST  → `/empresas/${empresa.id}/contratos-servico`
- PUT   → `/empresas/${empresa.id}/contratos-servico/${contrato.id}`
- DELETE→ `/empresas/${empresa.id}/contratos-servico/${contrato.id}`

<!-- CLAUDE.md mandate -->
`npm run build` obrigatório após qualquer edição JSX. Pitfall 6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin/Empresas.jsx — substituir editor inline (6 campos legacy) por seção de contratos + modal</name>
  <files>resources/js/Pages/Admin/Empresas.jsx</files>
  <read_first>
    - resources/js/Pages/Admin/Empresas.jsx (arquivo completo — ~1000+ LOC)
    - resources/js/Pages/Admin/Financeiro.jsx (estado pós Plan 14-05 — padrão da seção de contratos + modal)
    - resources/js/Components/ui/dialog.jsx
  </read_first>
  <action>
    Editar `resources/js/Pages/Admin/Empresas.jsx`:

    1. **REMOVER constants/maps relacionadas a `contract_type`** (CONTRACT_LABELS, CONTRACT_COLORS) e filtros `service_type`/`contract_type`.

    2. **Adicionar prop `servicos_disponiveis`** no destructuring do componente principal (Plan 14-06 Task 4 já passa essa prop).

    3. **Substituir bloco de inputs do form** (linhas ~512-610) pela seção de contratos + modal (mesmo padrão do Plan 14-05 em `Admin/Financeiro.jsx`). Reusar rotas existentes `/empresas/{company}/contratos-servico/*` (URL crua).

    4. **Atualizar filtros**: REMOVER `service_type` e `contract_type` do `FILTROS_INICIAL`; adicionar `servico_nome` único derivado de `companies[].servicos_contratados[].servico_nome`. Pattern idêntico ao do Plan 14-05.

    5. **Refatorar render do row** (linhas ~424-470) para mostrar `servicos_contratados` em vez de `contract_start`/`contract_end`/`contract_type` (mostrar contagem ou primeiros 2 nomes).

    6. **`useForm({service_type, contract_type, ...})` substituir por `useForm({name, cnpj, notes, parent_company_id, filha_ids})`** (campos que o `updateEmpresa` pós-cleanup do Plan 14-06 aceita).

    7. **Adicionar imports**: `Plus, Pencil, PowerOff` de `lucide-react`. `Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter` de `@/Components/ui/dialog`.

    8. **Modal Add/Edit Contrato**: copiar a estrutura do Plan 14-05 (mesma JSX do modal, mesmos helpers `abrirModalContrato`/`salvarContrato`/`desativarContrato` — duplicação aceita; refator posterior é fase futura).

    Comentários em pt-BR documentando "Phase 14 Plan 14-07: editor legacy substituído por seção de contratos (padrão Companies/Show.jsx + Plan 14-05)".

    **MANDATORY**: rodar `npm run build` ao final da task (parte da verify).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    `npm run build` 0 erros. `grep -nE "service_type|contract_type|contract_start|contract_end|additional_service" resources/js/Pages/Admin/Empresas.jsx` retorna 0 matches em código vivo. Arquivo usa exclusivamente `servicos_contratados` e tem seção de contratos + modal funcional.
  </done>
</task>

<task type="auto">
  <name>Task 2: Comercial/Empresas.jsx — substituir editor inline (6 campos legacy) por seção de contratos + modal</name>
  <files>resources/js/Pages/Comercial/Empresas.jsx</files>
  <read_first>
    - resources/js/Pages/Comercial/Empresas.jsx (arquivo completo)
    - resources/js/Pages/Admin/Financeiro.jsx (padrão da seção de contratos + modal pós Plan 14-05)
    - resources/js/Pages/Admin/Empresas.jsx (estado pós Task 1 — padrão aplicado neste plan; pode copiar diretamente)
    - resources/js/Components/ui/dialog.jsx
  </read_first>
  <action>
    Editar `resources/js/Pages/Comercial/Empresas.jsx` aplicando o MESMO padrão da Task 1 (cleanup Admin/Empresas.jsx) — duplicação aceita:

    1. REMOVER editor inline com 6 campos legacy (linhas 161-264 do arquivo atual).
    2. Substituir por seção de contratos + modal Add/Edit (mesmo padrão de Admin/Empresas.jsx pós Task 1).
    3. Atualizar render do row (linhas ~76-111) para mostrar `servicos_contratados` (string ou primeiros 2 nomes) em vez de Chips de `contract_type` + `contract_start`/`end` + `additional_service`.
    4. `useForm` reduzido para campos sem legacy (`name`, `cnpj`, `notes`).
    5. Adicionar prop `servicos_disponiveis` no destructuring (Plan 14-06 Task 4 já passa essa prop).
    6. Imports + modal idênticos à Task 1.
    7. Filtros: remover `service_type`/`contract_type`; adicionar `servico_nome` derivado dos contratos.

    Comentários em pt-BR.

    **MANDATORY**: `npm run build` continua válido ao final do plan (Task 4).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    `npm run build` 0 erros. `grep -nE "service_type|contract_type|contract_start|contract_end|additional_service" resources/js/Pages/Comercial/Empresas.jsx` retorna 0 matches em código vivo. Arquivo usa exclusivamente `servicos_contratados`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mlb/Empresas.jsx + Companies/Index.jsx + Admin/Financeiro.jsx (fallback ServiceBadge) — cleanup pontual</name>
  <files>resources/js/Pages/Mlb/Empresas.jsx, resources/js/Pages/Companies/Index.jsx, resources/js/Pages/Admin/Financeiro.jsx</files>
  <read_first>
    - resources/js/Pages/Mlb/Empresas.jsx linhas ~930-950 (uso de p.service_type em pendentes)
    - resources/js/Pages/Companies/Index.jsx linhas ~140-160 (uso de p.service_type em pendentes)
    - resources/js/Pages/Admin/Financeiro.jsx (estado pós Plan 14-05 — ServiceBadge tem fallback legacy nas linhas ~52-70)
  </read_first>
  <action>
    **Mlb/Empresas.jsx** (linha ~936-944):

    Substituir o bloco:
    ```
    {(Array.isArray(p.service_type) ? p.service_type : (p.service_type ? [p.service_type] : []))
        .map(...)}
    ```
    Por:
    ```
    {(p.servicos_contratados || []).map(nome => (
        <span key={nome} className="...badge classes existentes...">{nome}</span>
    ))}
    ```

    Backend (`MlbController::empresas` pós Plan 14-06 Task 3) passa `servicos_contratados` (array de nomes). Comentário pt-BR: `// Phase 14 Plan 14-07: leitura de p.service_type substituída por p.servicos_contratados.`

    **Companies/Index.jsx** (linha ~146):

    Análogo a Mlb/Empresas.jsx — substituir leitura `p.service_type` por `p.servicos_contratados`. Mesmo padrão e comentário.

    **Admin/Financeiro.jsx** (cleanup do fallback adicionado no Plan 14-05):

    1. Em `ServiceBadge`, REMOVER o else branch que checa `tipo` array (fallback legacy adicionado no Plan 14-05). Apenas o branch `servicos_contratados` permanece. Renomear assinatura do componente para apenas `function ServiceBadge({ servicos_contratados })`.
    2. Atualizar chamada `<ServiceBadge servicos_contratados={empresa.servicos_contratados} tipo={empresa.service_type} />` para `<ServiceBadge servicos_contratados={empresa.servicos_contratados} />`.
    3. Confirmar que NÃO há mais nenhum read de `empresa.service_type`/`empresa.additional_service`/`empresa.contract_*` no arquivo.

    Comentários pt-BR em cada arquivo.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>
    `npm run build` 0 erros. Grep `service_type|contract_type|contract_start|contract_end|additional_service` em cada um dos 3 arquivos retorna 0 matches em código vivo. ServiceBadge de Financeiro.jsx tem apenas branch de `servicos_contratados`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint humano final — smoke test fim-a-fim de TODAS as 5 telas</name>
  <what-built>
    Cleanup completo dos 5 JSX consumers:
    - Admin/Empresas.jsx: editor inline substituído por seção de contratos + modal Add/Edit
    - Comercial/Empresas.jsx: idem
    - Mlb/Empresas.jsx: pendentes leem servicos_contratados
    - Companies/Index.jsx: pendentes leem servicos_contratados
    - Admin/Financeiro.jsx: ServiceBadge sem fallback legacy

    Plan 14-06 já dropou as colunas e limpou backend; Plan 14-05 já refatorou as Blades e UI Fechamento.

    Após este checkpoint, Phase 14 está COMPLETA — SVC-01..07 fechados.
  </what-built>
  <how-to-verify>
    1. Confirme que `npm run build` rodou com 0 erros após a Task 3.

    2. Acesse `https://admin.ecfconsultoria.com.br` (login admin) e teste cada tela:

       **a) `/administrativo/empresas` (Admin/Empresas.jsx)**:
       - Página carrega sem 500 nem JS errors
       - Lista mostra empresas com `servicos_contratados` (badges de nomes do catálogo)
       - Filtro "Serviço" funciona (filtra empresas por nome de serviço)
       - Botão "Editar" abre editor sem campos legacy (apenas name/cnpj/notes + seção contratos)
       - Modal Add Contrato: select de serviço populado, salvar funciona, novo contrato aparece
       - Modal Edit Contrato: edita valor, salva, reflete
       - Botão desativar contrato funciona

       **b) `/comercial/empresas` (Comercial/Empresas.jsx)**:
       - Página carrega sem 500
       - Lista mostra empresas com `servicos_contratados`
       - Filtro funciona
       - Editor sem campos legacy; seção de contratos + modal funcionam

       **c) `/comercial/empresas/novo` (Comercial/NovaEmpresa.jsx, já refatorado no Plan 14-04)**:
       - Cadastra empresa nova com servicos[] do catálogo
       - Redireciona com flash success

       **d) `/mlb/empresas` (Mlb/Empresas.jsx)**:
       - Página carrega sem 500
       - Seção "Empresas pendentes" mostra nomes de serviços via `servicos_contratados` (não undefined)

       **e) `/companies` (Companies/Index.jsx)**:
       - Página carrega sem 500
       - Seção "Empresas pendentes" idem

       **f) `/administrativo/financeiro` (Admin/Financeiro.jsx)**:
       - Página carrega sem 500
       - ServiceBadge mostra nomes do catálogo (não vazio)
       - Modal Add/Edit Contrato continua funcionando (do Plan 14-05)
       - Total consolidado correto

    3. Console do browser (F12) sem erros JS de undefined `service_type` etc em NENHUMA das 5 telas.

    4. **Suíte completa de testes**:
       ```
       php artisan test
       ```
       Confirme verde.

    5. **Grep final FRONTEND**:
       ```
       grep -rnE "service_type|contract_type|contract_start|contract_end|additional_service|additional_service_price" resources/js/Pages/ | grep -v "//"
       ```
       Esperado: 0 matches funcionais (comentários explicativos OK).

    6. **`phase14:verificar-cobranca` final**:
       ```
       php artisan phase14:verificar-cobranca
       ```
       Esperado: "0 divergências". (Comando segue funcional; está pronto para ser removido em fase futura.)
  </how-to-verify>
  <resume-signal>Aprovado: todas as 5 telas funcionam fim-a-fim + suíte de testes verde + grep frontend limpo. Phase 14 COMPLETA. OU descreva o problema.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` → 0 erros após cada Task
- `grep -rnE "service_type|contract_type|contract_start|contract_end|additional_service|additional_service_price" resources/js/Pages/ | grep -v "//"` → 0 matches funcionais
- `php artisan test` → suite completa verde
- Smoke test humano aprovado para as 5 telas
- `php artisan phase14:verificar-cobranca` → 0 divergências
</verification>

<success_criteria>
1. Os 5 JSX consumers leem exclusivamente `servicos_contratados` (zero referência funcional aos 6 campos legacy)
2. Admin/Empresas + Comercial/Empresas têm editor com seção de contratos + modal Add/Edit funcionais
3. Mlb/Empresas + Companies/Index renderizam pendentes via `servicos_contratados`
4. Admin/Financeiro ServiceBadge sem fallback legacy
5. `npm run build` 0 erros
6. Smoke test humano aprova fluxo fim-a-fim em todas as 5 telas
7. Suíte completa de testes verde
8. Phase 14 COMPLETA — SVC-01..07 fechados
</success_criteria>

<output>
Criar `.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-07-SUMMARY.md` listando: arquivos modificados, confirmação de cleanup frontend completo, confirmação do checkpoint humano fim-a-fim, output final do `phase14:verificar-cobranca`. Nota final: "Phase 14 COMPLETA — modelo unificado de serviços. SVC-01..07 fechados. Próxima fase: TBD pelo usuário".

Commit (pt-BR):
```
chore(14): cleanup final dos 5 JSX consumers (Phase 14 completa)

- Admin/Empresas.jsx: editor legacy substituído por seção de contratos + modal
- Comercial/Empresas.jsx: idem
- Mlb/Empresas.jsx + Companies/Index.jsx: pendentes leem servicos_contratados
- Admin/Financeiro.jsx: ServiceBadge sem fallback legacy
- Smoke test humano fim-a-fim aprovado em todas as 5 telas

Per D-06, D-07. Phase 14 COMPLETA — SVC-01..07.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
</output>
</content>
