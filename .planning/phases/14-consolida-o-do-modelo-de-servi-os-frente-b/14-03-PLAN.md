---
phase: 14-consolida-o-do-modelo-de-servi-os-frente-b
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - app/Http/Controllers/AdminController.php
  - app/Models/Company.php
  - app/Http/Controllers/MlbController.php
  - app/Http/Controllers/CompanyController.php
  - app/Notifications/EmpresaCadastradaNotification.php
  - app/Jobs/EnviarRelatorioFechamentoJob.php
  - tests/Feature/Phase14AdminControllerCobrancaTest.php
  - tests/Feature/Phase14MlbControllerFiltroTest.php
autonomous: true
requirements:
  - SVC-02
  - SVC-04
  - SVC-07
must_haves:
  truths:
    - "AdminController::fechamento, gerarRelatorio e gerarRelatorioGeral usam CobrancaCalculator::novo em vez da fórmula inline com additional_service_price"
    - "Eager loading `with(['contratosServico' => fn($q) => $q->where('ativo', true)->with('servico')])` aplicado em todas as queries que alimentam o helper (Pitfall 2 evita N+1)"
    - "MlbController::empresas e CompanyController::index trocam whereJsonContains('service_type') por whereHas('contratosServico') JOIN servicos.nome"
    - "EmpresaCadastradaNotification.meta troca chave 'service_type' por 'servicos' (lista de nomes)"
    - "EnviarRelatorioFechamentoJob payload troca service_type/additional_service por 'servicos_contratados' (string formatada)"
    - "Company::labelFromTypes refatorado para labelFromServicos preservando API estática; serviceTypeLabel() deriva de contratos ativos"
    - "Mapper Inertia coexiste: mantém chaves legacy + ADICIONA servicos_contratados (chaves legacy removidas só no Plan 14-06)"
    - "Comando phase14:verificar-cobranca retorna 0 divergências após este plan"
  artifacts:
    - path: "app/Http/Controllers/AdminController.php"
      provides: "3 call-sites de cálculo refatorados para CobrancaCalculator::novo"
      contains: "CobrancaCalculator::novo"
    - path: "app/Models/Company.php"
      provides: "labelFromServicos() helper + serviceTypeLabel() refatorado"
      contains: "labelFromServicos"
    - path: "tests/Feature/Phase14AdminControllerCobrancaTest.php"
      provides: "Teste golden: cobranca_mensal pre vs pós refator é idêntica"
      contains: "class Phase14AdminControllerCobrancaTest"
  key_links:
    - from: "app/Http/Controllers/AdminController.php"
      to: "app/Support/CobrancaCalculator.php"
      via: "uso estático CobrancaCalculator::novo"
      pattern: "CobrancaCalculator::novo"
    - from: "app/Http/Controllers/MlbController.php"
      to: "app/Models/ContratoServico.php"
      via: "whereHas('contratosServico')"
      pattern: "whereHas\\('contratosServico'"
---

<objective>
Refatorar os 6 consumers PHP que leem campos legacy: AdminController (3 call-sites de cálculo + filtros + props), Company.php (labelFromTypes → labelFromServicos preservando API estática), MlbController (filtro whereJsonContains → whereHas), CompanyController (filtro idem), EmpresaCadastradaNotification (meta key), EnviarRelatorioFechamentoJob (payload). Após este plan, o runtime PHP NÃO LÊ MAIS os 6 campos legacy em CÁLCULOS — apenas a UI (Plan 14-05) e migration 3 (Plan 14-06) restam para fechar a fase.

**ESTRATÉGIA DE COEXISTÊNCIA (Wave 2):** Os mappers Inertia (arrays de props passados para JSX) MANTÊM as chaves legacy E ADICIONAM `servicos_contratados`. Isso garante que a UI atual (Admin/Financeiro.jsx, Mlb/Empresas.jsx, Companies/Index.jsx) continue funcionando entre a Wave 2 e a Wave 4. As chaves legacy só serão REMOVIDAS no Plan 14-06 (Wave 4), simultaneamente ao drop das colunas e à refatoração final dos JSX. NÃO remover chaves legacy dos arrays Inertia neste plan.

Purpose: SVC-02, SVC-04, SVC-07 — Cortar a dependência de leitura dos campos legacy nos CÁLCULOS do backend. Pré-requisito para o drop ocorrer com segurança no Plan 14-06.

Output: 6 arquivos PHP refatorados + 2 suítes de teste. Commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md
@.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md

<interfaces>
<!-- Helper criado no Plan 14-01 (USAR aqui) -->

`App\Support\CobrancaCalculator` (app/Support/CobrancaCalculator.php):
```
public static function legacy(?array $faixaData, ?float $additionalServicePrice): float
public static function novo(?array $faixaData, iterable $contratos): float
```

<!-- 3 sites de cálculo em AdminController (refatorar todos) -->

AdminController.php:280-282 (`fechamento()`):
```
'cobranca_mensal' => ($faixaData !== null || $c->additional_service_price)
    ? (float) ($faixaData['valor'] ?? 0) + (float) ($c->additional_service_price ?? 0)
    : null,
```

AdminController.php:506 (`gerarRelatorio()` mapper de filhas):
```
'cobranca_mensal' => ($valorMensal ?? 0) + ($adicional ?? 0) ?: null,
// onde $adicional = $f->additional_service_price ? (float) $f->additional_service_price : null;
```

AdminController.php:511-512 (`gerarRelatorio()` cálculo pai):
```
$adicionalPai = $company->additional_service_price ? (float) $company->additional_service_price : null;
$cobrancaMensal = ($valorMensalPai ?? 0) + ($adicionalPai ?? 0) ?: null;
```

AdminController.php:638, 658, 663-664 (`gerarRelatorioGeral()` mesma estrutura)

<!-- Filtros whereJsonContains a refatorar -->

MlbController.php:1525-1533:
```
$empresasPendentes = Company::where(function ($q) {
        $q->whereJsonContains('service_type', 'publicacao')
          ->orWhereJsonContains('service_type', 'polos')
          ->orWhereJsonContains('service_type', 'assessoria');
    })
    ->where('status', 'pendente')
    ->orderBy('created_at', 'desc')
    ->get(['id', 'name', 'service_type', 'notes', 'created_at']);
```

CompanyController.php:83-90:
```
$empresasPendentes = Company::where(function ($q) {
        $q->whereJsonContains('service_type', 'publicidade')
          ->orWhereJsonContains('service_type', 'gestao');
    })
    ->where('status', 'pendente')
    ->orderBy('created_at', 'desc')
    ->get(['id', 'name', 'service_type', 'created_at']);
```

AdminController.php:572-574 (filtro em gerarRelatorioGeral):
```
if ($request->filled('service_type')) {
    $query->whereJsonContains('service_type', $request->input('service_type'));
}
```

<!-- Notification.meta (refatorar) -->

EmpresaCadastradaNotification.php:25-34:
```
public function __construct(string $nomeEmpresa, string $serviceType, ?int $autorUserId)
{
    parent::__construct(
        // ...
        meta: ['empresa' => $nomeEmpresa, 'service_type' => $serviceType],
    );
}
```

<!-- Job payload (refatorar) -->

EnviarRelatorioFechamentoJob.php:128-133:
```
'service_type'             => $f->service_type,
'contract_type'            => $f->contract_type,
'contract_start'           => $f->contract_start ? Carbon::parse($f->contract_start)->format('d/m/Y') : null,
'contract_end'             => $f->contract_end  ? Carbon::parse($f->contract_end)->format('d/m/Y')  : null,
'additional_service'       => $f->additional_service,
'additional_service_price' => $f->additional_service_price ? (float) $f->additional_service_price : null,
```

<!-- Company labelFromTypes atual (refatorar — D-09) -->

Company.php:50-71:
```
public static function labelFromTypes(mixed $types): string {
    // recebe service_type (string ou JSON array de enum legacy)
    // retorna "Publicação + POLO" via lookup interno
}
public function getServiceTypeLabelAttribute(): string {
    return static::labelFromTypes($this->service_type);
}
```

<!-- Comando do Plan 14-01 — usar para validar refator -->
`php artisan phase14:verificar-cobranca --abort-on-divergence`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar Company.php (labelFromTypes → labelFromServicos D-09) + 3 sites em AdminController para CobrancaCalculator::novo + eager loading + adicionar servicos_contratados aos mappers Inertia</name>
  <files>app/Models/Company.php, app/Http/Controllers/AdminController.php</files>
  <read_first>
    - app/Models/Company.php (lê arquivo inteiro — alterações múltiplas)
    - app/Http/Controllers/AdminController.php linhas 31-52 (empresas mapper — campos legacy nos props), 99-332 (fechamento completo), 412-535 (gerarRelatorio), 537-720 (gerarRelatorioGeral)
    - app/Support/CobrancaCalculator.php (Plan 14-01)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-03 fórmula, D-09 labelFromServicos)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §3 §6 (pitfall 2 eager loading, §6 activity log mantém histórico OK)
  </read_first>
  <action>
    **REGRA GERAL DESTE TASK (estratégia de coexistência Wave 2):**
    - SUBSTITUIR a fórmula de `cobranca_mensal` por `CobrancaCalculator::novo` em todos os 3 call-sites.
    - ADICIONAR a chave `servicos_contratados` aos arrays Inertia (sem remover nenhuma chave existente).
    - ADICIONAR eager loading nas queries principais (Pitfall 2).
    - MANTER as chaves legacy (`service_type`, `contract_type`, `contract_start`, `contract_end`, `additional_service`, `additional_service_price`) intactas nos arrays Inertia — compat com UI atual.
    - MANTER as validation rules existentes em `updateEmpresa()` e `updateFechamento()` intactas, adicionando comentário `// TODO Plan 14-06: remover após drop das colunas legacy`.
    - MANTER `$fillable`, `$casts` e `logOnly` de Company.php intactos (campos físicos ainda existem — drop é Plan 14-06).

    ---

    **Parte A — Company.php** (per D-09):

    1. NÃO importar `App\Support\CobrancaCalculator` (helper é usado pelo Controller, não pelo Model).

    2. MANTER `$fillable` e `$casts` INTOCADOS (campos legacy ainda existem fisicamente — drop é Plan 14-06).

    3. MANTER `logOnly` INTOCADO (Plan 14-06 cuida disso).

    4. **Refatorar `labelFromTypes()` (linhas 50-66)** — adicionar uma NOVA função estática `public static function labelFromServicos(iterable $servicos): string` que: `return collect($servicos)->pluck('nome')->filter()->implode(', ');` (joiner ', ' conforme D-09 — não mais ' + '). Comentário pt-BR explicando que recebe coleção de `Servico` (ou objetos com property `nome`).

    5. **Manter `labelFromTypes()` como deprecated proxy**: adicionar PHPDoc `@deprecated Use labelFromServicos. Será removido no Plan 14-06.`. Internamente mantém a lógica antiga (array $map atual) — usado APENAS pelas 6 chamadas em Blade views que são refatoradas no Plan 14-05. Isso evita quebrar as Blades imediatamente.

    6. **Refatorar `getServiceTypeLabelAttribute()` (linhas 68-71)**: agora chama `static::labelFromServicos($this->contratosServico->where('ativo', true)->pluck('servico'))`. Adicionar `$this->loadMissing('contratosServico.servico')` antes do collect para garantir eager loading. Comentário pt-BR esclarecendo que API permanece a mesma para callers JSX/Blade.

    ---

    **Parte B — AdminController.php**:

    1. Importar `use App\Support\CobrancaCalculator;` no topo.

    2. **`fechamento()` (linha ~99-332)**:
       - ADICIONAR eager loading na query principal de companies: `->with(['contratosServico' => fn($q) => $q->where('ativo', true)->with('servico')])` (Pitfall 2)
       - Linha 280-282: SUBSTITUIR fórmula inline por: `'cobranca_mensal' => CobrancaCalculator::novo($faixaData, $c->contratosServico) ?: null,` (usar `?: null` para preservar semântica legacy de "null quando soma é 0")
       - ADICIONAR no array `$dadosPorId[$c->id]` (ao lado das chaves legacy já existentes, não as remover):
         ```
         'servicos_contratados' => $c->contratosServico->where('ativo', true)->map(fn($ct) => [
             'id'              => $ct->id,
             'servico_id'      => $ct->servico_id,
             'servico_nome'    => $ct->servico?->nome,
             'valor_contratado'=> (float) $ct->valor_contratado,
             'tipo_cobranca'   => $ct->servico?->tipo_cobranca,
             'data_contratacao'=> $ct->data_contratacao?->toDateString(),
             'data_vencimento' => $ct->data_vencimento?->toDateString(),
             'ativo'           => true,
         ])->values()->toArray(),
         ```

    3. **`updateFechamento()` (linhas 370-389)**: MANTER validation rules existentes (UI atual ainda pode submeter). ADICIONAR comentário pt-BR no topo do método: `// TODO Plan 14-06: remover validation rules legacy após drop das colunas`.

    4. **`empresas()` (linhas 31-52)**: ADICIONAR eager loading + ADICIONAR `servicos_contratados` ao mapper (mesmo padrão de fechamento). MANTER chaves legacy intactas.

    5. **`updateEmpresa()` (linhas 54-92)**: MANTER validation rules existentes. ADICIONAR comentário pt-BR: `// TODO Plan 14-06: remover validation rules legacy após drop das colunas`.

    6. **`gerarRelatorio()` (linhas 412-535)**:
       - ADICIONAR eager loading no `$company->load([...])` adicionando `'contratosServico' => fn($q) => $q->where('ativo', true)->with('servico'), 'filhas.contratosServico' => fn($q) => $q->where('ativo', true)->with('servico')`
       - Linha 487-506 (mapper de filhas): SUBSTITUIR cálculo `$adicional`/`cobranca_mensal` por `'cobranca_mensal' => CobrancaCalculator::novo(['valor' => $valorMensal], $f->contratosServico) ?: null,`. ADICIONAR `'servicos_contratados' => $f->contratosServico->where('ativo', true)->pluck('servico.nome')->filter()->implode(', ')` (string simples para a Blade). MANTER chaves legacy intactas no array da filha.
       - Linhas 511-512: SUBSTITUIR por `$cobrancaMensal = CobrancaCalculator::novo($faixaPai, $company->contratosServico) ?: null;` (calcular direto, sem variável intermediária `$adicionalPai`)
       - No array passado para `view('admin.relatorio-fechamento', [...])`: ADICIONAR `'servicos_contratados_pai' => $company->contratosServico->where('ativo', true)->pluck('servico.nome')->filter()->implode(', ')`. MANTER `service_type` legacy do array.

    7. **`gerarRelatorioGeral()` (linhas 537-720)**:
       - Linha 572-574 (filtro `service_type`): SUBSTITUIR por:
         ```
         if ($request->filled('service_type')) {
             $tipo = $request->input('service_type');
             $nomeServico = ['publicacao' => 'Publicação', 'polos' => 'Polos', 'assessoria' => 'Assessoria', 'incubadora' => 'Incubadora', 'publicidade' => 'Publicidade', 'gestao' => 'Gestão'][$tipo] ?? $tipo;
             $query->whereHas('contratosServico', fn($q) => $q->where('ativo', true)->whereHas('servico', fn($qs) => $qs->where('nome', $nomeServico)));
         }
         ```
       - Eager loading no `->with([...])` da query principal: ADICIONAR `'contratosServico' => fn($q) => $q->where('ativo', true)->with('servico'), 'filhas.contratosServico' => fn($q) => $q->where('ativo', true)->with('servico')`
       - Mesma refator das linhas 638-665 (filhas + pai) que `gerarRelatorio()`: SUBSTITUIR cobranca via `CobrancaCalculator::novo`. ADICIONAR `servicos_contratados` ao array de filhas. MANTER chaves legacy intactas.

    Comentários em pt-BR. Per D-03, D-09. Per Pitfall 2 (eager loading obrigatório).
  </action>
  <verify>
    <automated>php -l app/Models/Company.php && php -l app/Http/Controllers/AdminController.php</automated>
  </verify>
  <done>
    `Company::labelFromServicos()` existe e funciona. `Company::labelFromTypes()` mantido como deprecated proxy. `AdminController` usa `CobrancaCalculator::novo` nas 3 chamadas (grep retorna 3 matches). Eager loading aplicado nas queries principais de `fechamento()`, `gerarRelatorio()`, `gerarRelatorioGeral()`. Chaves `servicos_contratados` ADICIONADAS aos arrays Inertia ao lado das chaves legacy (chaves legacy permanecem — removidas só no Plan 14-06).
  </done>
</task>

<task type="auto">
  <name>Task 2: Refatorar MlbController + CompanyController (whereJsonContains → whereHas) + EmpresaCadastradaNotification + EnviarRelatorioFechamentoJob</name>
  <files>app/Http/Controllers/MlbController.php, app/Http/Controllers/CompanyController.php, app/Notifications/EmpresaCadastradaNotification.php, app/Jobs/EnviarRelatorioFechamentoJob.php</files>
  <read_first>
    - app/Http/Controllers/MlbController.php linhas 1500-1546 (método `empresas` — filtro pendentes)
    - app/Http/Controllers/CompanyController.php linhas 60-100 (método `index` — filtro pendentes)
    - app/Notifications/EmpresaCadastradaNotification.php (arquivo completo — 36 LOC)
    - app/Jobs/EnviarRelatorioFechamentoJob.php linhas 100-180 (mapper de vinculadas)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §5 (whereHas pattern) e §9 (refator notifications/jobs)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-CONTEXT.md (D-07)
  </read_first>
  <action>
    **Parte A — MlbController.php (linha 1525-1533)**:

    SUBSTITUIR o bloco `$empresasPendentes = Company::where(function ($q) { $q->whereJsonContains('service_type', 'publicacao')->orWhereJsonContains(...); })` por:

    ```
    $empresasPendentes = Company::where('status', 'pendente')
        ->whereHas('contratosServico', fn($q) =>
            $q->where('ativo', true)
              ->whereHas('servico', fn($qs) =>
                  $qs->whereIn('nome', ['Publicação', 'Polos', 'Assessoria'])
              )
        )
        ->with(['contratosServico' => fn($q) => $q->where('ativo', true)->with('servico')])
        ->orderBy('created_at', 'desc')
        ->get(['id', 'name', 'service_type', 'notes', 'created_at']);
    ```

    MANTER `service_type` na lista de campos do `get([])` — a coluna ainda existe até o Plan 14-06; remover o read da coluna agora geraria undefined no JSX antigo.

    Após o `get()`, ADICIONAR mapping `servicos_contratados` para que a UI nova (no Plan 14-06) possa consumir já agora:

    ```
    $empresasPendentes->transform(function ($e) {
        $nomes = $e->contratosServico->where('ativo', true)->pluck('servico.nome')->filter();
        $e->servicos_contratados = $nomes->values()->toArray();
        return $e;
    });
    ```

    Comentário pt-BR explicando: filtro agora usa JOIN via `whereHas` (não mais JSON-scan); chave `servicos_contratados` adicionada para coexistir com `service_type` legacy; Plan 14-06 remove `service_type` (coluna + chave).

    **Parte B — CompanyController.php (linha 83-90)**:

    SUBSTITUIR o bloco análogo (filtra `publicidade` e `gestao`) por:

    ```
    $empresasPendentes = Company::where('status', 'pendente')
        ->whereHas('contratosServico', fn($q) =>
            $q->where('ativo', true)
              ->whereHas('servico', fn($qs) =>
                  $qs->whereIn('nome', ['Publicidade', 'Gestão'])
              )
        )
        ->with(['contratosServico' => fn($q) => $q->where('ativo', true)->with('servico')])
        ->orderBy('created_at', 'desc')
        ->get(['id', 'name', 'service_type', 'created_at']);
    ```

    MANTER `service_type` no select. Mesmo `transform()` que adiciona `servicos_contratados` (sem remover/reconstruir `service_type`).

    Comentário pt-BR análogo.

    **Parte C — EmpresaCadastradaNotification.php (linha 25-34)**:

    REFATORAR o construtor para aceitar `string|array $servicos` (backward compat):

    ```
    public function __construct(string $nomeEmpresa, string|array $servicos, ?int $autorUserId)
    {
        $servicosNomes = is_array($servicos) ? $servicos : array_filter(explode('+', $servicos));
        $servicosLabel = implode(', ', $servicosNomes) ?: 'sem serviços';

        parent::__construct(
            titulo:      'Nova empresa cadastrada: ' . $nomeEmpresa,
            mensagem:    'O setor Comercial cadastrou a empresa "' . $nomeEmpresa . '" (serviços: ' . $servicosLabel . '). Verifique os pendentes.',
            categoria:   Categoria::MANUAL,
            autorUserId: $autorUserId,
            url:         route('notificacoes.index'),
            meta:        ['empresa' => $nomeEmpresa, 'servicos' => $servicosNomes],
        );
    }
    ```

    Adicionar PHPDoc explicando o aceite dual e marcando string como deprecated (será removido no Plan 14-04 quando ComercialController for refatorado).

    Per D-07 item 6, RESEARCH §9.

    **Parte D — EnviarRelatorioFechamentoJob.php (linhas 128-133)**:

    No mapper de `$vinculadas`, ADICIONAR a chave `servicos_contratados` (sem remover as 6 linhas legacy — coexistência):

    ```
    'service_type'             => $f->service_type,         // legacy — removido no Plan 14-06
    'contract_type'            => $f->contract_type,        // legacy — removido no Plan 14-06
    'contract_start'           => $f->contract_start ? Carbon::parse($f->contract_start)->format('d/m/Y') : null,
    'contract_end'             => $f->contract_end  ? Carbon::parse($f->contract_end)->format('d/m/Y')  : null,
    'additional_service'       => $f->additional_service,
    'additional_service_price' => $f->additional_service_price ? (float) $f->additional_service_price : null,
    'servicos_contratados'     => $f->loadMissing('contratosServico.servico')
        ->contratosServico->where('ativo', true)
        ->map(fn($c) => $c->servico->nome . ' (R$ ' . number_format((float) $c->valor_contratado, 2, ',', '.') . ')')
        ->implode(', '),
    ```

    ADICIONAR `with(['filhas.contratosServico.servico'])` no `$rawCompanies = $query->...->get()` do método handle() (linha próxima ao topo). E `with(['contratosServico.servico'])` na empresa pai também.

    Comentário pt-BR explicando que conteúdo do email agora inclui também `servicos_contratados`; Plan 14-06 remove as 6 linhas legacy. Se houver uma view Blade do email (`resources/views/emails/relatorio-fechamento.blade.php`), inspecionar; refator dela é responsabilidade do Plan 14-05 (Blade views).

    Per D-07 item 7, RESEARCH §9.
  </action>
  <verify>
    <automated>php -l app/Http/Controllers/MlbController.php && php -l app/Http/Controllers/CompanyController.php && php -l app/Notifications/EmpresaCadastradaNotification.php && php -l app/Jobs/EnviarRelatorioFechamentoJob.php</automated>
  </verify>
  <done>
    Os 4 arquivos sem syntax errors. Grep `whereJsonContains.*service_type` em `app/Http/Controllers/` retorna 0 matches (todos refatorados). `EmpresaCadastradaNotification` aceita string OU array (backward compat). Job payload tem chave `servicos_contratados` ao lado das chaves legacy.
  </done>
</task>

<task type="auto">
  <name>Task 3: Testes — golden cobranca pre/pós refator + filtro MlbController + verificação automatizada</name>
  <files>tests/Feature/Phase14AdminControllerCobrancaTest.php, tests/Feature/Phase14MlbControllerFiltroTest.php</files>
  <read_first>
    - app/Http/Controllers/AdminController.php (estado pós Task 1 desta wave)
    - app/Http/Controllers/MlbController.php (estado pós Task 2 desta wave)
    - app/Support/CobrancaCalculator.php (Plan 14-01)
    - .planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-RESEARCH.md §4 §5 §7 (exemplos de teste)
    - tests/Feature/ (analog: qualquer Phase13*Test.php para padrão actingAs + create + assertRedirect)
  </read_first>
  <action>
    Criar `tests/Feature/Phase14AdminControllerCobrancaTest.php` (namespace `Tests\Feature`, RefreshDatabase):

    Teste 1: **`test_cobranca_mensal_legacy_e_novo_modelo_batem_para_empresa_com_additional_service()`** (golden):
    - Cria 1 admin via `User::factory()->create(['role' => 'admin'])` + actingAs
    - Cria 1 empresa com `additional_service_price = 200.00`, `additional_service = 'Treinamento'`, `service_type = ['polos']`, `active = true`, `contract_start = '2026-01-01'`
    - Cria 1 AdmanMetric com `company_id => $c->id, reference_date => Carbon::now()->subDays(5)->toDateString(), revenue => 600_000` (cai na faixa_2 = R$ 4.500)
    - Roda as migrations Phase 14 (1 + 2) via `Artisan::call('migrate', ['--path' => '...'])`
    - Calcula valor esperado legacy: 4500 + 200 = 4700
    - Hit no endpoint `GET /administrativo/financeiro`
    - Assert `$response->status() === 200`
    - Extrai prop Inertia `companies` e busca pela empresa pelo nome
    - Assert `$empresa['cobranca_mensal'] === 4700.0` (até R$ 0,01)

    Teste 2: **`test_phase14_verificar_cobranca_retorna_zero_divergencias_apos_migrations()`**:
    - Cria 3 empresas com configurações variadas (com/sem additional_service, com/sem service_type)
    - Para cada uma, cria AdmanMetric correspondente
    - Roda migrations 1 + 2
    - `$exit = Artisan::call('phase14:verificar-cobranca', ['--abort-on-divergence' => true]);`
    - `$this->assertEquals(0, $exit, 'Helper novo diverge do legacy após migration de dados');`

    Teste 3: **`test_fechamento_props_inclui_servicos_contratados()`**:
    - Cria 1 empresa com service_type=['publicacao'] e roda migrations
    - Hit `/administrativo/financeiro`
    - Assert props Inertia `companies[0]['servicos_contratados']` é array (não null) e contém ao menos `['servico_nome' => 'Publicação', 'valor_contratado' => 0.0, ...]`

    Criar `tests/Feature/Phase14MlbControllerFiltroTest.php` (Tests\Feature, RefreshDatabase):

    Teste 1: **`test_mlb_empresas_filtra_pendentes_via_whereHas_contratos()`**:
    - Cria user com permission 'mlb.empresas' ou role admin
    - Cria 2 empresas pendentes: empresa A com service_type=['polos'], empresa B com service_type=['publicidade']
    - Cria 1 empresa ativa (status != 'pendente') com service_type=['polos'] — não deve aparecer
    - Roda migrations Phase 14
    - Hit `GET /mlb/empresas`
    - Assert prop `empresas_pendentes` tem 1 entrada (apenas empresa A — polos cai no filtro do MlbController; B é publicidade, vai pro CompanyController)
    - Assert essa entrada tem nome 'Empresa A'

    Teste 2: **`test_companies_index_filtra_pendentes_publicidade_gestao()`**:
    - Análogo: cria empresa pendente com service_type=['publicidade'] e empresa pendente com service_type=['polos']
    - Hit `GET /companies` (ou rota equivalente do CompanyController::index)
    - Assert prop `empresas_pendentes` tem 1 entrada (apenas publicidade)

    Cada teste em pt-BR (mensagens de assert). Usar `actingAs` no início para autorizar. Roda migrations Phase 14 via `Artisan::call`.

    NOTA: Se a rota `/mlb/empresas` ou `/companies` exigir permissions específicas e teste falhar com 403, criar user via `User::factory()->create(['role' => 'admin'])` (admin tem short-circuit). Se o método não existir / rota diferente, ajustar para a URL real verificando `routes/web.php`.
  </action>
  <verify>
    <automated>php artisan test --filter=Phase14AdminControllerCobrancaTest && php artisan test --filter=Phase14MlbControllerFiltroTest</automated>
  </verify>
  <done>
    Ambas suítes verdes. Total 5+ testes. Golden test confirma `cobranca_mensal` pré e pós refator são idênticos para empresa com additional_service. Comando `phase14:verificar-cobranca` retorna 0 divergências.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /administrativo/financeiro | Admin autenticado lê dados de cobrança; nenhuma escrita de valor neste endpoint nesta wave |
| Browser → /comercial/empresas (POST) | Caller que ainda passa `string $serviceType` legacy para `EmpresaCadastradaNotification` — refatorado no Plan 14-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | AdminController::fechamento cálculo de cobrança | mitigate | Helper estático testado em isolamento (Plan 14-01) + golden test pré/pós refator nesta wave (Task 3 Teste 1) + comando de verificação automatizada (Task 3 Teste 2) — falha-loud se cálculo divergir |
| T-14-02 | Information Disclosure | Mapper Inertia coexiste com campos legacy + novo `servicos_contratados` | accept | Decisão de coexistência Wave 2 — campos legacy removidos do payload no Plan 14-06; sem PII em jogo (campos financeiros já visíveis ao admin) |
| T-14-03 | Tampering | EmpresaCadastradaNotification.meta troca chave de `service_type` (string) para `servicos` (array) | accept | Aceita ambos formatos via union type `string\|array` — backward compat até Plan 14-04 |
</threat_model>

<verification>
- `php artisan test --filter=Phase14AdminControllerCobrancaTest` → verde
- `php artisan test --filter=Phase14MlbControllerFiltroTest` → verde
- `php artisan test --filter=Phase14VerificarCobrancaTest` (do Plan 14-01) → ainda verde
- `php artisan phase14:verificar-cobranca` em ambiente dev → "0 divergências"
- `grep -nE "whereJsonContains.*service_type" app/Http/Controllers/` → 0 matches
- `grep -nE "additional_service_price" app/Http/Controllers/AdminController.php` → matches apenas em validation rules de updateFechamento/updateEmpresa (com comentário TODO Plan 14-06) e nos arrays Inertia (coexistência); ZERO matches no cálculo de cobrança_mensal
- `php -l` em todos os 6 arquivos PHP modificados → sem erros
- `php artisan test` (suite completa) → não regredir (rodar e verificar que testes existentes continuam verdes)
</verification>

<success_criteria>
1. AdminController usa `CobrancaCalculator::novo` nos 3 sites de cálculo (grep 3 matches)
2. Eager loading evita N+1 nas queries de fechamento/relatorio
3. MlbController e CompanyController usam `whereHas` (sem `whereJsonContains` em consumers de produção)
4. EmpresaCadastradaNotification aceita backward compat (string ou array de nomes)
5. EnviarRelatorioFechamentoJob payload tem chave `servicos_contratados` com formatação "Nome (R$ X,XX)"
6. Company::labelFromServicos existe; labelFromTypes mantido como deprecated proxy
7. `phase14:verificar-cobranca` retorna 0 divergências em ambiente de teste e dev
8. Golden test confirma idempotência financeira
9. Coexistência respeitada: chaves legacy permanecem nos mappers Inertia ao lado de `servicos_contratados`
</success_criteria>

<output>
Criar `.planning/phases/14-consolida-o-do-modelo-de-servi-os-frente-b/14-03-SUMMARY.md` listando: arquivos modificados, número de chamadas refatoradas em AdminController, decisão explícita de estratégia de coexistência (chaves legacy mantidas + servicos_contratados adicionado), e nota explícita "Backend PHP NÃO LÊ MAIS campos legacy em CÁLCULOS — apenas no mapper Inertia (coexistência) e validation rules dormentes (com TODO). Próximo: Plan 14-04 (ComercialController) e Plan 14-05 (UI Financeiro)".

Commit (pt-BR):
```
refactor(14): consumers PHP usam contratos_servico em cálculos (coexistência mantida)

- AdminController — 3 call-sites de cobrança_mensal usam CobrancaCalculator::novo + eager loading
- Company.php — labelFromServicos novo; labelFromTypes deprecated proxy
- MlbController + CompanyController — filtros via whereHas (não mais whereJsonContains)
- EmpresaCadastradaNotification — meta key 'servicos' (backward compat com string)
- EnviarRelatorioFechamentoJob — payload servicos_contratados ao lado das chaves legacy
- 2 suítes de teste (golden cobranca + filtros)

Per D-03, D-07, D-09. Cálculos NÃO LEEM MAIS legacy. Mappers Inertia mantêm chaves legacy + servicos_contratados (coexistência removida no Plan 14-06).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
</output>
</content>
