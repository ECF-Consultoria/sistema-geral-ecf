---
phase: 136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-
plan: 07
type: execute
wave: 5
depends_on: [136-03, 136-05, 136-06]
files_modified:
  - .planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-VERIFICACAO-MANUAL.md
autonomous: false
requirements: [D-03, D-04, D-09, D-10, D-11]

must_haves:
  truths:
    - "A suíte Feature completa está verde exceto as falhas pré-existentes registradas na baseline — e o número não cresceu"
    - "A grade admin foi aberta por um humano, com alternância de eixo, sinal de divergência e read-only da competência consolidada conferidos na tela"
    - "O selo de D-04 aparece na métrica certa em /performance/{user}, com tooltip, e sem nome de quem lançou"
    - "O efeito do CMV manual na nota de carteira só-Shopee foi conferido por reconsulta ao banco, nunca por stdout"
    - "O efeito de D-10 sobre o gate FIXMARG-03 foi medido pelo exit code de desempenho:verificar-consolidacao, antes de qualquer consolidação"
  artifacts:
    - path: ".planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-VERIFICACAO-MANUAL.md"
      provides: "Registro do gate de regressão e das verificações manuais da fase"
      contains: "FIXMARG-03"
  key_links:
    - from: ".planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-VERIFICACAO-MANUAL.md"
      to: ".planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-BASELINE-TESTES.md"
      via: "comparação antes × depois do número de falhas"
      pattern: "BASELINE"
---

<objective>
Fechar a fase: rodar o gate de regressão completo comparando contra a baseline congelada no
Plano 01, e submeter ao humano as quatro verificações que nenhum teste automatizado cobre —
a grade em uso, o selo na tela do profissional, o efeito real na nota de carteira só-Shopee, e o
gate FIXMARG-03.

Purpose: esta fase mexe em número que decide pagamento de bônus. Learnings §2 documenta uma
pessoa que perdeu o bônus de junho **sem nenhuma mudança de código**, por estar a 0,24 ponto
percentual da fronteira. Um "passou nos testes" não é suficiente para declarar isso pronto — e
learnings §4 é explícito: conferência é por reconsulta ao banco e por exit code, nunca por
stdout.

Output: `136-VERIFICACAO-MANUAL.md` com o resultado do gate e das conferências, e a aprovação
(ou a lista de problemas) do usuário.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-CONTEXT.md
@.planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-VALIDATION.md
@.planning/learnings/desempenho-bonificacao.md
@CLAUDE.md

<interfaces>
<!-- Contratos e regras de conferência. Não explorar o codebase. -->

**Cobertura de decisões desta fase** (todo D-ID do CONTEXT.md precisa estar coberto; esta é a
tabela de conferência do gate):

| Decisão | Onde foi implementada |
|---|---|
| D-01 fonte explícita por célula | Planos 02 (schema), 03 (override), 04 (escrita), 05 (grade) |
| D-02 manual nunca reverte sozinho | Planos 03 (ausência de condição no override), 04 (`ativo=false` sem deletar), 05 (sinal de divergência) |
| D-03 consolidar trava e deixa rastro | Plano 03 (Task 3 — `quality.*_fonte` no snapshot) + Plano 04 (trava na escrita) |
| D-04 selo discreto no lado do profissional | Plano 05 (Task 2), com o sinal vindo do Plano 03 |
| D-05 célula manual compara mês cheio | Plano 03 (`diff_source='manual_mes_calendario'`) |
| D-06 lado base em cascata | Plano 03 (Task 1) |
| D-07 dois eixos independentes | Planos 02 (chave da tabela), 03, 04, 05 |
| D-08 origem do faturamento na derivação da margem | Plano 03 (Task 1) |
| D-09 fronteira de "não consolidada" | Planos 02 (`competenciaConsolidada()`), 04 (trava com lock), 05 (read-only na tela) |
| D-10 três call-sites, resolvedor único | Plano 01 |
| D-11 retroatividade com relatório read-only | Plano 06 |
| D-12 trilha de auditoria no banco | Planos 02 (`LogsActivity` + colunas), 04 (`valor_anterior`, sem delete) |
| D-EXC-01 exceção estreita ao hotfix de 2026-07-24 | Plano 03 (docblock do `ManualMetricOverrideService`) |

**Consequências obrigatórias medidas — onde cada uma foi paga:** bump v19→v20 (Plano 01 Task 2);
4 arquivos de teste com a chave hardcoded, não 6 (Plano 01 Task 2, divergência do CONTEXT.md
registrada); gate de hash rotacionado nos 5 arquivos (Plano 01 Task 3); baseline de falhas
pré-existentes (Plano 01 Task 1, reconferida na Task 3); gate FIXMARG-03 (este plano);
armadilhas MariaDB da migration (Plano 02 Task 1 e a verificação manual deste plano);
proibição de `cache:clear` (Planos 01 e 04); `git commit -- <caminhos>` (todos).

**Como conferir o gate FIXMARG-03 (learnings §4):** o veredito é o **exit code** de
`C:\xampp\php\php.exe artisan desempenho:verificar-consolidacao --mes=YYYY-MM --json`, nunca o
texto impresso. E `comando | tail -20; echo $?` devolve o exit code do `tail` — capturar antes do
pipe ou redirecionar para arquivo.

**Como conferir efeito na nota (learnings §4):** reconsulta ao banco
(`desempenho_company_score_snapshots` / o payload de `computeCached()` inspecionado via tinker),
nunca leitura de stdout de um comando que "parecia ter funcionado".

**Privacidade no documento de verificação (learnings §11):** contadores de carteira podem ser
versionados; **nome pareado com faixa de bônus, nota final ou valor de bonificação, não**.
Referir-se a "o profissional de carteira só-Shopee" e apontar a conferência para o banco local de
quem executa. Isto já aconteceu antes: um commit da Fase 123 gravou tabela nominal com faixa de
bônus e precisou de amend.

**Deploy continua proibido sem autorização explícita do usuário** (`CLAUDE.md`). Este plano não
deploya nada e não pede para deployar. As verificações que só existem em produção ficam
registradas como pendências.

**Ambiente:** `php` NÃO está no PATH do Bash tool. Usar sempre `C:\xampp\php\php.exe`.
Se a tela não aparecer no local, checar `public/hot` órfão de um Vite dev caído (remover o
arquivo, rodar `npm run build`, hard reload).
**Árvore compartilhada:** sempre `git commit -- <caminhos>`, NUNCA `git add -A`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de regressão da fase e registro do resultado</name>

  <files>.planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-VERIFICACAO-MANUAL.md</files>

  <read_first>
    - `.planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-BASELINE-TESTES.md` (o "antes" congelado no Plano 01)
    - `.planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-VALIDATION.md` (Sampling Rate e a tabela de verificação por decisão)
    - `.planning/learnings/desempenho-bonificacao.md` §0.02 (as falhas que não são regressão), §4 (armadilha do pipe) e §11 (o que não pode ser versionado)
  </read_first>

  <action>
Rodar, nesta ordem, capturando o exit code de cada um **antes de qualquer pipe**:
1. `C:\xampp\php\php.exe artisan test --filter="Phase136"`
2. `C:\xampp\php\php.exe artisan test --filter="Phase119"`
3. `C:\xampp\php\php.exe artisan test --filter="CarteiraPeriodoDiffTest|DesempenhoPeriodoOficialTest|DesempenhoShopeeScoreTest|ConsolidarMesJanelaNpsTest|JanelaNpsBonusTest"` — o mesmo comando da baseline
4. `C:\xampp\php\php.exe artisan test --testsuite=Feature`
5. `C:\xampp\php\php.exe artisan desempenho:verificar-consolidacao --mes=<a competência aberta mais recente> --json`, redirecionando para arquivo e lendo o **exit code**

Criar `136-VERIFICACAO-MANUAL.md` no diretório da fase, com frontmatter (`phase: 136`,
`verificado_em`, SHA do HEAD) e as seções:
- **Gate de regressão:** tabela com os 5 comandos, exit code e contagem de passed/failed;
  comparação explícita do item 3 contra o número registrado em `136-BASELINE-TESTES.md`, com
  veredito "não cresceu" ou a lista nominal das falhas novas.
- **Gate FIXMARG-03:** o exit code do item 5 e a leitura do que ele significa (0 = sem
  inconsistências; qualquer outro = investigar antes de qualquer consolidação). Registrar que
  este é o efeito de D-10 sobre `margem_amostra.legado.n_elegivel` e que a direção esperada
  (medida como raciocínio, não em produção) é a cobertura **aumentar**, porque o denominador
  encolhe sem o numerador encolher — se o observado for o contrário, é sinal de que a suposição
  A2 do RESEARCH estava errada e precisa de investigação, não de contorno.
- **Cobertura de decisões:** copiar a tabela D-01..D-EXC-01 do bloco `<interfaces>` deste plano,
  marcando cada linha com o plano que a entregou e o teste que a prova.
- **Pendências que só existem em produção:** reconciliação de `cust_id` via
  `desempenho:relatorio-impacto-fonte --json` contra produção (RESEARCH A1); migration contra
  MariaDB conferindo que a tabela **e** o índice existem (o erro 1059 deixa a migration `Pending`
  com a tabela criada, e o SQLite dos testes não pega); decisão de reconsolidação de competência
  fechada, que a fase deliberadamente não toma (D-11).

Se qualquer falha **nova** aparecer no item 3 ou 4, registrá-la nominalmente com a suíte e a
mensagem, e **não** fechar esta task como concluída — falha nova é regressão desta fase até que
se prove o contrário contra a baseline.

Nenhum nome de profissional pareado com faixa de bônus, nota final ou valor de bonificação pode
entrar neste documento.
  </action>

  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter="Phase136"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/136-.../136-VERIFICACAO-MANUAL.md` existe, com `verificado_em` e SHA do HEAD no frontmatter
    - `C:\xampp\php\php.exe artisan test --filter="Phase136"` sai com exit code 0
    - `C:\xampp\php\php.exe artisan test --filter="Phase119"` sai com exit code 0
    - O número de falhas do comando da baseline é **menor ou igual** ao registrado em `136-BASELINE-TESTES.md`, e a comparação está escrita no documento
    - O exit code de `desempenho:verificar-consolidacao --mes=... --json` está registrado como número no documento, capturado **antes** de qualquer pipe
    - A tabela de cobertura tem uma linha para cada um de D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12 e D-EXC-01 — 13 linhas
    - `grep -ci "faixa\|bonific" 136-VERIFICACAO-MANUAL.md` retorna 0 em qualquer linha que também contenha nome de pessoa
  </acceptance_criteria>

  <done>O gate de regressão está rodado e registrado com números, a comparação contra a baseline está escrita, e as 13 decisões têm rastro de onde foram entregues.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Conferência humana da grade, do selo e do efeito na nota</name>

  <files>.planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-VERIFICACAO-MANUAL.md</files>

  <read_first>
    - `.planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-VALIDATION.md` (seção "Manual-Only Verifications" — os 5 itens que nenhum teste cobre)
    - `.planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-VERIFICACAO-MANUAL.md` (produzido na Task 1)
    - `.planning/learnings/desempenho-bonificacao.md` §0.00 (o teto de 3,33 da carteira só-Shopee, que é o efeito a conferir) e §11 (privacidade no documento)
  </read_first>

  <what-built>
Grade admin de lançamento manual em `/desempenho/metricas-manuais` (alternância independente de
faturamento e CMV por empresa e mês, com sinal de divergência contra a API e read-only em
competência consolidada); marcador discreto por métrica em `/performance/{user}` identificando o
número lançado à mão, sem nome de autor; correção do desempate de fonte financeira nos três
call-sites; e o comando read-only `desempenho:relatorio-impacto-fonte`.
  </what-built>

  <action>
Parada bloqueante para conferência humana. O executor **não** aprova esta task sozinho: ela
confere números que decidem pagamento de bônus, e a autorização de modo autônomo desta casa vale
para checkpoint visual de UI, não para validar cálculo de bonificação.

Preparar o ambiente antes de chamar o usuário: confirmar que `npm run build` foi rodado (Plano
05), que não existe `public/hot` órfão de um Vite dev caído, e que há dado suficiente no banco
local para os 5 itens de `<how-to-verify>` — em especial uma empresa de vínculo Shopee com CMV
lançável na competência corrente e na anterior, e uma competência com linha
`origem='consolidar_mes'` para conferir o read-only.

Apresentar ao usuário o roteiro de `<how-to-verify>` item a item e transcrever o veredito de cada
um para a seção "Conferência humana" de `136-VERIFICACAO-MANUAL.md`, respeitando a regra de
privacidade (contadores sim; nome pareado com faixa, nota ou valor, não). Não deployar nada.
  </action>

  <how-to-verify>
**Este checkpoint NÃO é auto-aprovável.** Ele confere números que decidem pagamento de bônus.

1. **Grade (D-01/D-02/D-07/D-09).** Entrar como usuário `role='admin'` e abrir
   `/desempenho/metricas-manuais`.
   - Trocar a competência no seletor: mês consolidado precisa aparecer marcado e com as células
     em somente leitura, com o aviso explicando o porquê. Mês não consolidado, mesmo antigo,
     precisa continuar editável.
   - Numa empresa de vínculo Shopee, alternar **só o CMV** para manual e salvar. O faturamento
     daquela linha precisa continuar em automático — não pode ter sido zerado nem exigido.
   - Recarregar a página: o valor precisa persistir.
   - Numa célula manual, conferir o valor da API exibido ao lado; quando os dois divergirem, o
     sinal de divergência precisa aparecer, e o valor manual precisa continuar mandando.
   - Voltar a célula para automático e conferir que ela volta a exibir o valor da API — e que,
     no banco, a linha continua existindo com `ativo = 0` (`select * from
     desempenho_metricas_manuais` no banco local), nunca apagada.
2. **Selo (D-04).** Abrir `/performance/{user}?mes=YYYY-MM` de um profissional que tenha a
   empresa lançada. Conferir: o marcador aparece **na coluna da métrica** que foi lançada (não na
   coluna do nome da empresa), o tooltip diz "valor lançado manualmente", e **o nome de quem
   lançou não aparece em lugar nenhum** da tela nem do payload (inspecionar as props do Inertia
   no devtools e procurar por `lancado_por`).
3. **Efeito na nota (o motivo da fase existir).** Para o profissional de carteira só-Shopee,
   conferir por **reconsulta ao banco** (nunca por stdout) que, com CMV manual lançado na
   competência e no mês anterior, a linha da empresa passa a ter `margem_pontos` preenchido e a
   nota da carteira deixa de estar presa ao teto de 3,33. Registrar no documento **contadores**
   ("N empresas passaram a pontuar em margem") — nunca o nome pareado com a nota ou a faixa.
4. **Gate FIXMARG-03.** Conferir o exit code registrado na Task 1 e confirmar que ele é 0. Se
   não for, **parar aqui**: o efeito de D-10 sobre a cobertura de margem precisa ser entendido
   antes de qualquer consolidação.
5. **Nada foi deployado.** Confirmar que nenhum passo desta fase deployou — `CLAUDE.md` proíbe
   deploy sem autorização explícita.

Anotar o resultado de cada item na seção correspondente de `136-VERIFICACAO-MANUAL.md`.
  </how-to-verify>

  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter="Phase136"</automated>
  </verify>

  <acceptance_criteria>
    - `136-VERIFICACAO-MANUAL.md` ganhou uma seção "Conferência humana" com os 5 itens acima, cada um com veredito escrito (aprovado / problema encontrado, com descrição)
    - Comportamento conferido na tela: competência consolidada abre em read-only e mês antigo não consolidado permanece editável
    - Comportamento conferido na tela: alternar só o CMV não altera o eixo de faturamento da mesma linha
    - Comportamento conferido no banco: reverter para automático deixa a linha com `ativo = 0` e ela continua existindo
    - Comportamento conferido no payload: a string `lancado_por` não aparece nas props de `/performance/{user}`
    - Comportamento conferido por reconsulta ao banco: empresa Shopee com CMV manual tem `margem_pontos` não nulo no snapshot/payload
    - O exit code de `desempenho:verificar-consolidacao` registrado é 0, ou o problema está descrito e a fase não é fechada
    - Nenhuma linha do documento pareia nome de pessoa com faixa de bônus, nota final ou valor de bonificação
  </acceptance_criteria>

  <resume-signal>Responder "aprovado" ou descrever os problemas encontrados, item por item.</resume-signal>

  <done>Um humano confirmou, na tela e no banco, que a grade funciona, que o selo diz a verdade sem expor autor, e que o CMV manual de fato tira a carteira só-Shopee do teto de 3,33.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| resultado da suíte → decisão de "está pronto" | Baseline de 9 falhas pré-existentes pode mascarar regressão nova se comparada contra zero |
| documento de verificação → histórico do git | Dado individual de bonificação não pode ser versionado |
| exit code × stdout | O gate FIXMARG-03 reporta apenas contagem no texto; o veredito é o exit code |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Plano de mitigação |
|-----------|-----------|------------|------------|--------------------|
| T-136-22 | Information Disclosure | `136-VERIFICACAO-MANUAL.md` versionado | mitigate | Instrução explícita de registrar contadores e nunca nome pareado com faixa/nota/valor (learnings §11, precedente real corrigido por amend na Fase 123); acceptance criteria confere |
| T-136-24 | Repudiation | "Passou nos testes" declarado contra o número errado | mitigate | Comparação obrigatória contra `136-BASELINE-TESTES.md`, congelado no Plano 01 com SHA do HEAD; falha nova é tratada como regressão até prova em contrário |
| T-136-23 | Repudiation | Veredito do FIXMARG-03 lido do stdout ou depois de um pipe | mitigate | Exit code capturado antes de qualquer pipe e registrado como número no documento; a saída humana é redirecionada para arquivo |
| T-136-25 | Elevation of Privilege | Checkpoint auto-aprovado sem conferência real | mitigate | Marcado `gate="blocking-human"` e declarado não auto-aprovável no corpo — confere números que decidem pagamento |
| T-136-SC | Tampering | instalação de pacotes | accept | Nenhum pacote novo em nenhum plano desta fase |
</threat_model>

<verification>
1. Exit code 0 em `--filter="Phase136"` e `--filter="Phase119"`.
2. Falhas do comando da baseline ≤ ao registrado em `136-BASELINE-TESTES.md`.
3. Exit code de `desempenho:verificar-consolidacao --mes=... --json` registrado como número.
4. Aprovação humana registrada em `136-VERIFICACAO-MANUAL.md`.
5. Nenhum deploy executado.
</verification>

<success_criteria>
- Suíte Feature verde exceto a dívida pré-existente registrada, sem crescimento
- Gate de hash da Fase 119 verde
- Gate FIXMARG-03 conferido por exit code, antes de qualquer consolidação
- As 13 decisões (D-01..D-12 + D-EXC-01) com rastro escrito de onde foram entregues
- Grade, selo e efeito na nota conferidos por humano, com o efeito medido por reconsulta ao banco
- Pendências de produção registradas, sem nenhum deploy executado
</success_criteria>

<output>
Criar `.planning/phases/136-m-tricas-manuais-por-empresa-m-s-no-desempenho-override-api-/136-07-SUMMARY.md` ao concluir.
</output>
