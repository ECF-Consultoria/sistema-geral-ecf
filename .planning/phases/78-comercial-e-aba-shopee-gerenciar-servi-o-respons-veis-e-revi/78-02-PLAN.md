---
phase: 78-comercial-e-aba-shopee-gerenciar-servi-o-respons-veis-e-revi
plan: 02
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - resources/js/Pages/Shopee/Empresas.jsx
autonomous: false
requirements: [DEC-78-2, DEC-78-3, DEC-78-4]

must_haves:
  truths:
    - "O botão/ação 'Gerar NPS' foi REMOVIDO da aba Shopee (sem referência a route('nps.generate'))"
    - "A aba Pendências tem um botão 'Resolver' por linha que abre um popup (modal) de edição da empresa"
    - "O popup Resolver tem select de Analista (options = props.analistas, já escopadas ao Setor Shopee pelo backend) e select de Estrategista (props.estrategistas) + campo de email do cliente"
    - "Salvar o popup posta em route('shopee.empresas.resolver') e as pendências resolvidas somem ao recarregar"
    - "Cada linha tem ação 'Excluir' que posta em route('shopee.empresas.cancelar-servico') (cancela só o serviço Shopee) com confirmação"
    - "npm run build passa verde (sem ReferenceError de variável de escopo em .map())"
  artifacts:
    - path: "resources/js/Pages/Shopee/Empresas.jsx"
      provides: "Aba revisada: sem Gerar NPS; Resolver popup com selects Shopee + email; Excluir=cancelar serviço"
      contains: "shopee.empresas.resolver"
  key_links:
    - from: "Shopee/Empresas.jsx botão Resolver"
      to: "route('shopee.empresas.resolver')"
      via: "useForm.post no salvar do popup"
      pattern: "shopee.empresas.resolver"
    - from: "Shopee/Empresas.jsx ação Excluir"
      to: "route('shopee.empresas.cancelar-servico')"
      via: "router.post com confirmação"
      pattern: "cancelar-servico"
---

<objective>
Revisar a UI da aba Shopee (DEC-78-2/3/4): remover "Gerar NPS"; adicionar botão "Resolver" na aba Pendências que abre um popup de edição (select Analista Shopee + select Estrategista Shopee — as props já vêm escopadas do backend 78-01 — + email do cliente), postando em shopee.empresas.resolver; e a ação "Excluir" que cancela só o serviço Shopee (shopee.empresas.cancelar-servico). Checkpoint humano visual ao final.

Purpose: entrega a clarificação do usuário (Resolver com selects do Setor Shopee) e alinha a aba ao spec v2 (sem Gerar NPS avulso; Excluir não apaga empresa).
Output: Shopee/Empresas.jsx revisado + build verde + checkpoint visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-comercial-e-aba-shopee-gerenciar-servi-o-respons-veis-e-revi/78-CONTEXT.md

<interfaces>
resources/js/Pages/Shopee/Empresas.jsx (atual):
  - props: companies, estrategistas, analistas, grupos.
  - Gerar NPS: npsForm + route('nps.generate') (~:130-171) — REMOVER (função, botão nas 2 abas, imports órfãos como Star se só usado aqui).
  - bulk-assign: route('shopee.empresas.bulk-assign') (~:127) — manter (atribuição em massa na aba Todas/Pendências).
  - TABS Todas/Pendências (~:156-158); aba pendencias render (~:302); PendenciaBadges (~:28).
molde de modal de edição/atribuição: resources/js/Pages/Companies/Index.jsx (modal com selects Analista/Estrategista + campos). Reusar Dialog do design system (@/Components/ui/dialog), Select nativo/ECFSelect e tokens ecf-*.
Backend (78-01): props analistas/estrategistas JÁ escopadas ao Setor Shopee; rotas shopee.empresas.resolver (POST company_id, analista_id, estrategista_id, email_cliente) e shopee.empresas.cancelar-servico (POST company_id).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Remover Gerar NPS + adicionar Resolver popup + ação Excluir</name>
  <read_first>
    - resources/js/Pages/Shopee/Empresas.jsx (estrutura completa)
    - resources/js/Pages/Companies/Index.jsx (molde do modal de atribuição/edição)
    - resources/js/Components/ui/dialog.jsx (Dialog do design system)
  </read_first>
  <files>resources/js/Pages/Shopee/Empresas.jsx</files>
  <action>
    (a) REMOVER a função de Gerar NPS (npsForm, gerarNps) e todos os botões "Gerar NPS" das abas Todas e Pendências + o comentário do topo sobre nps.generate; remover imports que ficarem órfãos (ex.: Star se só usado ali).
    (b) Adicionar estado de modal Resolver (empresa selecionada) e um botão "Resolver" por linha NA ABA PENDÊNCIAS. Ao clicar, abrir um Dialog (design system) com: Select Analista (options = props.analistas — já escopadas ao Setor Shopee), Select Estrategista (props.estrategistas), input Email do cliente (pré-preenchido com c.email_cliente). Usar useForm({ company_id, analista_id, estrategista_id, email_cliente }). Ao submeter → post(route('shopee.empresas.resolver'), { preserveScroll, onSuccess: fechar modal }). Comentar em pt-BR.
    (c) Adicionar ação "Excluir" por linha (nas duas abas) com confirmação (window.confirm ou AlertDialog) → router.post(route('shopee.empresas.cancelar-servico'), { company_id: c.id }, { preserveScroll }). Deixar claro no texto que cancela só o serviço Shopee.
    (d) Cuidar do gotcha de escopo de variável em .map() (memória feedback_rollup_map_scope_bug): computar flags/derivados DENTRO do callback do map. Manter tokens ecf-*, cn(), padrão de tabela existente.
  </action>
  <verify><automated>npm run build 2>&1 | tail -8 && grep -c "nps.generate" resources/js/Pages/Shopee/Empresas.jsx</automated></verify>
  <acceptance_criteria>build verde; grep de "nps.generate" retorna 0; arquivo contém route('shopee.empresas.resolver') e route('shopee.empresas.cancelar-servico'); botão Resolver presente na aba Pendências.</acceptance_criteria>
</task>

<task type="human-verify">
  <name>Tarefa 2: Checkpoint visual (o orquestrador conduz com o usuário)</name>
  <action>
    Documentar no SUMMARY os passos de validação visual (não travar esperando input):
    1. /shopee/empresas → aba Pendências → botão "Resolver" abre popup.
    2. No popup, select Analista lista só analistas do Setor Shopee (Gustavo etc.), Estrategista só estrategistas Shopee (Felipe etc.); atribuir + salvar → pendência some.
    3. Ação "Excluir" numa empresa multi-marketplace → cancela só o serviço Shopee (some da aba Shopee, continua na aba ML).
    4. Não há mais botão "Gerar NPS".
  </action>
  <verify><automated>echo "checkpoint visual — ver SUMMARY"</automated></verify>
  <acceptance_criteria>SUMMARY lista os 4 passos de UAT; build verde na Tarefa 1.</acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat | Category | Mitigation |
|---|---|---|
| T-78-05 | Tampering | ações postam só company_id/ids do escopo; backend (78-01) revalida escopo shopee + RBAC — o front não é a fronteira de confiança |
| T-78-06 | — | remoção de Gerar NPS não afeta o motor NPS (só tira o deep-link da aba) |
</threat_model>

<verification>
- `npm run build` verde; `grep nps.generate Shopee/Empresas.jsx` = 0.
- Checkpoint humano valida Resolver/selects/Excluir.
</verification>

<success_criteria>
Aba sem Gerar NPS; Resolver popup com selects escopados ao Setor Shopee + email; Excluir cancela só o serviço Shopee.
</success_criteria>

<output>Criar 78-02-SUMMARY.md ao concluir (com os passos do checkpoint visual).</output>
