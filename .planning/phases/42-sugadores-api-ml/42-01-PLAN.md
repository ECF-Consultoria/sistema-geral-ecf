---
phase: 42-sugadores-api-ml
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_06_26_420101_add_cpc_minimo_cliques_to_sugador_configs_table.php
  - app/Models/SugadorConfig.php
  - app/Services/SugadorAnalysisService.php
  - tests/Feature/Phase42/CpcMinimoCliquesSchemaTest.php
  - tests/Unit/Phase42/EvaluateMetricsCpcCompostoTest.php
autonomous: true
requirements: [REQ-42-04]

must_haves:
  truths:
    - "Coluna `cpc_minimo_cliques` (unsignedInteger nullable) existe em `sugador_configs` apos rodar migrate"
    - "`SugadorConfig::$fillable` inclui `cpc_minimo_cliques` (insert via fill funciona)"
    - "`SugadorConfig::$casts['cpc_minimo_cliques']` => integer (round-trip preserva null e int)"
    - "`SugadorConfig::DEFAULTS['cpc_minimo_cliques']` === null (regra D-01: opt-in, nao quebra empresas existentes)"
    - "Logica composta de cpc_alto: `sold_quantity == 0 && cpc > cpc_maximo && (cpc_minimo_cliques === null || clicks >= cpc_minimo_cliques)` aplicada em `evaluateMetrics`"
    - "Quando `cpc_minimo_cliques` eh null, comportamento legacy preservado (apenas CPC + zero vendas + threshold flagam)"
    - "Quando `cpc_minimo_cliques=5` e clicks=3, criterio cpc_alto NAO marca hit (mesmo com CPC > limite e zero vendas)"
    - "Quando `cpc_minimo_cliques=5` e clicks=5 (boundary inclusivo), criterio cpc_alto marca hit"
  artifacts:
    - path: "database/migrations/2026_06_26_420101_add_cpc_minimo_cliques_to_sugador_configs_table.php"
      provides: "Migration idempotente adicionando coluna nullable apos cpc_maximo_logic"
    - path: "app/Models/SugadorConfig.php"
      provides: "Campo `cpc_minimo_cliques` em $fillable + $casts + DEFAULTS"
    - path: "app/Services/SugadorAnalysisService.php"
      provides: "evaluateMetrics aplica gating composto no criterio cpc_alto"
    - path: "tests/Feature/Phase42/CpcMinimoCliquesSchemaTest.php"
      provides: "Suite Feature validando schema, fillable, casts, DEFAULTS"
    - path: "tests/Unit/Phase42/EvaluateMetricsCpcCompostoTest.php"
      provides: "Suite Unit cobrindo 5 cenarios da logica composta (D-01)"
  key_links:
    - from: "sugador_configs.cpc_minimo_cliques"
      to: "SugadorConfig::$fillable + $casts"
      via: "Eloquent mass-assignment + cast integer"
      pattern: "'cpc_minimo_cliques'"
    - from: "SugadorConfig::DEFAULTS"
      to: "forCompany() firstOrCreate"
      via: "Default null garante opt-in"
      pattern: "'cpc_minimo_cliques'\\s*=>\\s*null"
    - from: "SugadorConfig->cpc_minimo_cliques"
      to: "evaluateMetrics criterio cpc_alto"
      via: "leitura como ?int + gating logico"
      pattern: "cpc_minimo_cliques"
---

<objective>
Adiciona o campo `cpc_minimo_cliques` (nullable int) em `sugador_configs` conforme D-01 (Opcao B do briefing §8) e integra a logica composta no criterio `cpc_alto` do `SugadorAnalysisService::evaluateMetrics`. Migration + Model + Service + 2 suites de tests (Feature schema + Unit logica).

Purpose: destrava a regra de negocio "CPC > R$ 4 com pelo menos 5 cliques sem venda" como flag composta sem novo motivo (`cpc_alto` continua sendo o motivo retornado). Eh fundacao backend pre-UI (Plan 42-02 adiciona o campo no formulario) e pre-cut-over ML (Plans 42-03/04 dependem dessa logica ja estar no service para validar paridade quando rodar pelo provider ML).

Output: 1 migration + edit Model + edit Service (~6 linhas no metodo evaluateMetrics) + 2 suites de tests. Zero quebra de comportamento existente — quando `cpc_minimo_cliques === null`, criterio cpc_alto opera identico ao legado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-sugadores-api-ml/42-CONTEXT.md
@.planning/phases/41-onboarding-ml-por-empresa/41-05-SUMMARY.md
@database/migrations/2026_05_22_173333_add_criteria_logic_to_sugador_configs_table.php
@app/Models/SugadorConfig.php
@app/Services/SugadorAnalysisService.php

<interfaces>
<!-- SugadorConfig atual (Plan 42-01 acrescenta apenas 1 campo) -->

From app/Models/SugadorConfig.php:
```php
protected $fillable = [
    'company_id', 'dias_analise',
    'gasto_minimo_sem_venda', 'gasto_minimo_logic',
    'cpc_maximo', 'cpc_maximo_logic',
    'acos_maximo_pct', 'acos_maximo_logic',
    'cliques_minimos_sem_venda', 'cliques_minimos_logic',
    'pct_anuncios_para_flag_campanha',
    'incluir_campanhas', 'incluir_anuncios', 'ativo', 'updated_by',
];

protected $casts = [
    'dias_analise' => 'integer',
    'gasto_minimo_sem_venda' => 'decimal:2',
    'cpc_maximo' => 'decimal:2',
    'cliques_minimos_sem_venda' => 'integer',
    'pct_anuncios_para_flag_campanha' => 'integer',
    'incluir_campanhas' => 'boolean',
    'incluir_anuncios' => 'boolean',
    'ativo' => 'boolean',
    // ...
];

public const DEFAULTS = [
    'cpc_maximo' => null,
    'cpc_maximo_logic' => self::LOGIC_OPTIONAL,
    // ... outros campos ...
];
```

From app/Services/SugadorAnalysisService.php (criterio atual cpc_alto):
```php
// Criterio 2: CPC alto sem vender
if ($config->cpc_maximo !== null && $cpc !== null) {
    $threshold = (float) $config->cpc_maximo;
    $criteria[] = [
        'key'   => 'cpc_alto',
        'hit'   => $vendas === 0 && (float) $cpc > $threshold,
        'logic' => $config->cpc_maximo_logic ?? SugadorConfig::LOGIC_OPTIONAL,
    ];
}
```

Spec de D-01 / briefing §8 (Opcao B):
```text
$soldQuantity == 0
    && $cpc > $config->cpc_maximo
    && (
        $config->cpc_minimo_cliques === null
        || $clicks >= $config->cpc_minimo_cliques
    );
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — suites de tests para schema e logica composta</name>
  <files>tests/Feature/Phase42/CpcMinimoCliquesSchemaTest.php, tests/Unit/Phase42/EvaluateMetricsCpcCompostoTest.php</files>
  <behavior>
    Suite Feature `CpcMinimoCliquesSchemaTest` (RefreshDatabase, SQLite in-memory) — 4 tests:
    - T1 schema_tem_coluna_cpc_minimo_cliques: `Schema::hasColumn('sugador_configs', 'cpc_minimo_cliques')` === true
    - T2 fillable_inclui_cpc_minimo_cliques: cria `SugadorConfig` com `cpc_minimo_cliques=5` via mass-assign; reload; valor persiste como int 5 (NAO string)
    - T3 cast_integer_preserva_null: cria `SugadorConfig` sem o campo; reload; `cpc_minimo_cliques === null` (NAO 0, NAO false)
    - T4 defaults_inclui_chave_com_valor_null: `SugadorConfig::DEFAULTS['cpc_minimo_cliques']` === null (presente como chave, nao undefined)

    Suite Unit `EvaluateMetricsCpcCompostoTest` (RefreshDatabase + factory de Company/Config) — 5 tests cobrindo D-01:
    - T1 cpc_alto_legacy_quando_minimo_cliques_eh_null: config `cpc_maximo=4`, `cpc_maximo_logic=optional`, `cpc_minimo_cliques=null`; metrics {investment=10, sold_quantity=0, cpc=5.0, clicks=2}; `evaluateMetrics` retorna `['cpc_alto']` (legacy preservado)
    - T2 cpc_alto_bloqueado_quando_clicks_abaixo_minimo: config `cpc_maximo=4`, `cpc_minimo_cliques=5`; metrics {sold_quantity=0, cpc=5.0, clicks=3}; retorna `[]` (gate de cliques falha)
    - T3 cpc_alto_marca_hit_no_boundary_inclusivo: config `cpc_maximo=4`, `cpc_minimo_cliques=5`; metrics {sold_quantity=0, cpc=5.0, clicks=5}; retorna `['cpc_alto']` (boundary >= 5 inclusivo)
    - T4 cpc_alto_marca_hit_acima_do_minimo: config `cpc_maximo=4`, `cpc_minimo_cliques=5`; metrics {sold_quantity=0, cpc=5.0, clicks=10}; retorna `['cpc_alto']`
    - T5 cpc_alto_nao_marca_quando_venda_existe: config `cpc_maximo=4`, `cpc_minimo_cliques=5`; metrics {sold_quantity=1, cpc=5.0, clicks=10}; retorna `[]` (criterio so vale com sold_quantity=0 mesmo em ambos os modos)

    Cada test usa `#[\\PHPUnit\\Framework\\Attributes\\Test]` (PHPUnit 11) e nomes pt-BR. Sem mocks externos — `evaluateMetrics` eh pure function que recebe array e SugadorConfig instanciado direto via `SugadorConfig::DEFAULTS` + override.
  </behavior>
  <read_first>
    - tests/Feature/Sugadores/SugadoresIndexTest.php (padrao de setUp + factory de Company)
    - tests/Feature/Phase41/Phase41SchemaTest.php (padrao Schema::hasColumn + RefreshDatabase)
    - app/Models/SugadorConfig.php (DEFAULTS atual + LOGIC_OPTIONAL constant)
    - app/Services/SugadorAnalysisService.php (linhas 466-547 — assinatura evaluateMetrics)
  </read_first>
  <action>
    Criar `tests/Feature/Phase42/CpcMinimoCliquesSchemaTest.php` com namespace `Tests\\Feature\\Phase42`, uses `RefreshDatabase`, helper `makeConfig(array $overrides = [])` que faz `SugadorConfig::firstOrCreate(['company_id' => Company::factory()->create()->id], array_merge(SugadorConfig::DEFAULTS, $overrides))`. Os 4 tests rodam isolados — T1 valida schema sem criar row.

    Criar `tests/Unit/Phase42/EvaluateMetricsCpcCompostoTest.php` com namespace `Tests\\Unit\\Phase42`, uses `RefreshDatabase` (precisa porque SugadorConfig eh Eloquent Model), helper `evaluator()` retorna `app(SugadorAnalysisService::class)`. Cada test monta `$config = new SugadorConfig(array_merge(SugadorConfig::DEFAULTS, [...]))` (instancia in-memory, sem persistir) e chama `$evaluator->evaluateMetrics([...], $config)`.

    Rodar `php artisan test tests/Feature/Phase42 tests/Unit/Phase42` deve falhar (RED) — coluna nao existe, fillable nao tem o campo, evaluator nao aplica gating.

    Commit: `test(42-01): suites schema cpc_minimo_cliques + logica composta cpc_alto RED — 9 tests`
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase42/CpcMinimoCliquesSchemaTest.php tests/Unit/Phase42/EvaluateMetricsCpcCompostoTest.php --stop-on-failure 2>&1 | grep -E "FAIL|ERROR" | head -5</automated>
  </verify>
  <done>
    - Arquivo `tests/Feature/Phase42/CpcMinimoCliquesSchemaTest.php` existe com 4 metodos `#[Test]`
    - Arquivo `tests/Unit/Phase42/EvaluateMetricsCpcCompostoTest.php` existe com 5 metodos `#[Test]`
    - `grep -c "#\\[.*Test\\]" tests/Feature/Phase42/CpcMinimoCliquesSchemaTest.php` retorna 4
    - `grep -c "#\\[.*Test\\]" tests/Unit/Phase42/EvaluateMetricsCpcCompostoTest.php` retorna 5
    - Tests RED commitados (esperam Task 2 para virar GREEN)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — migration + Model + logica composta no evaluator</name>
  <files>database/migrations/2026_06_26_420101_add_cpc_minimo_cliques_to_sugador_configs_table.php, app/Models/SugadorConfig.php, app/Services/SugadorAnalysisService.php</files>
  <behavior>
    Migration `2026_06_26_420101_add_cpc_minimo_cliques_to_sugador_configs_table.php`:
    - `up()`: dentro de `Schema::table('sugador_configs', ...)`, adicionar `unsignedInteger('cpc_minimo_cliques')->nullable()->after('cpc_maximo_logic')`. Idempotencia via guard `if (! Schema::hasColumn('sugador_configs', 'cpc_minimo_cliques'))` antes do add.
    - `down()`: dropColumn idempotente via guard.

    `SugadorConfig.php`:
    - Adicionar `'cpc_minimo_cliques'` em `$fillable` (logo apos `'cpc_maximo_logic'`)
    - Adicionar `'cpc_minimo_cliques' => 'integer'` em `$casts` (logo apos `'cpc_maximo' => 'decimal:2'`)
    - Adicionar `'cpc_minimo_cliques' => null` em `DEFAULTS` (logo apos `'cpc_maximo_logic'`)

    `SugadorAnalysisService::evaluateMetrics`:
    - Substituir o bloco do criterio 2 (cpc_alto) por logica composta conforme D-01 (briefing §8 Opcao B):
      ```
      $hit = $vendas === 0
          && (float) $cpc > $threshold
          && ($config->cpc_minimo_cliques === null || $clicks >= (int) $config->cpc_minimo_cliques);
      ```
    - Manter assinatura, manter chave 'cpc_alto', manter 'logic' do `cpc_maximo_logic`. Adicionar comentario pt-BR explicando D-01 (Phase 42 — Opcao B do briefing §8).

    Rodar `php artisan migrate` no SQLite em-memory durante test resolve schema (RefreshDatabase). Os 9 tests devem virar GREEN.
  </behavior>
  <read_first>
    - database/migrations/2026_05_22_173333_add_criteria_logic_to_sugador_configs_table.php (padrao migration de adicao com idempotencia)
    - app/Models/SugadorConfig.php (estado atual fillable/casts/DEFAULTS)
    - app/Services/SugadorAnalysisService.php (linhas 487-495 — bloco cpc_alto atual)
  </read_first>
  <action>
    Criar migration usando o padrao da Phase 22 (return new class extends Migration). Ordem das alteracoes no Model importa pra alinhamento visual — manter `cpc_minimo_cliques` SEMPRE imediatamente apos `cpc_maximo_logic` em fillable/casts/DEFAULTS (alinha com posicionamento da coluna no banco via `after('cpc_maximo_logic')`).

    No `evaluateMetrics`, NAO criar variavel auxiliar nem refatorar adjacente — apenas substituir o `'hit' => ...` por expressao composta. Bloco continua dentro do `if ($config->cpc_maximo !== null && $cpc !== null)`. Adicionar comentario `// Phase 42 D-01: gating composto opcional via cpc_minimo_cliques (briefing §8 Opcao B)`.

    Rodar `php artisan test tests/Feature/Phase42 tests/Unit/Phase42` — esperar 9/9 GREEN. Validar regressao via `php artisan test --filter=Sugador` (a suite Sugador acumulada deve continuar verde, incluindo legacy `cpc_alto` tests que usam `cpc_minimo_cliques=null` implicitamente).

    Commit: `feat(42-01): GREEN cpc_minimo_cliques + logica composta cpc_alto (REQ-42-04)`
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase42/CpcMinimoCliquesSchemaTest.php tests/Unit/Phase42/EvaluateMetricsCpcCompostoTest.php 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Migration existe e `php artisan migrate --pretend` mostra `ALTER TABLE sugador_configs ADD cpc_minimo_cliques`
    - `grep -c "'cpc_minimo_cliques'" app/Models/SugadorConfig.php` retorna >= 3 (fillable + casts + DEFAULTS)
    - `grep -c "cpc_minimo_cliques" app/Services/SugadorAnalysisService.php` retorna >= 1
    - 9 tests GREEN: `php artisan test tests/Feature/Phase42 tests/Unit/Phase42` exit 0
    - `php artisan test --filter=Sugador` permanece verde (zero regressao)
    - Sem fenced code completo dentro do bloco modificado em SugadorAnalysisService (regra do projeto)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → DB | DDL irreversivel se executada errado em prod |
| evaluator → config | leitura de cpc_minimo_cliques pode receber valor invalido (string vs int) se cast falhar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | Tampering | Migration UP em prod sem rollback testado | mitigate | down() implementado + idempotencia Schema::hasColumn antes de ADD/DROP. Migration ja roda em SQLite em-memory durante test, validando UP/DOWN ciclico |
| T-42-01-02 | Information disclosure | `cpc_minimo_cliques` cast falha e leitura retorna string "5" no eval | mitigate | Cast `integer` explicito + `(int) $config->cpc_minimo_cliques` no service. T1 do schema test valida cast |
| T-42-01-03 | Denial of service | Logica composta nova gera false negative em larga escala (flags antigos somem da listagem) | accept | Sugadores ja detectados NAO sao reavaliados retroativamente (warning visivel no UI Config.jsx atual linha 363). Mudanca afeta apenas proximas analises — risco operacional baixo |
| T-42-01-SC | Tampering | npm/composer installs | accept | Esta phase NAO instala packages — apenas DDL + edit em Model/Service existentes |
</threat_model>

<verification>
- `php artisan migrate --pretend` mostra `ALTER TABLE` correto
- 9 tests RED→GREEN (4 schema + 5 logica composta)
- Suite Sugador acumulada continua verde (zero regressao)
- `grep -c "Phase 42 D-01" app/Services/SugadorAnalysisService.php` >= 1 (comentario de rastreabilidade)
- `php artisan tinker --execute "dd(\\App\\Models\\SugadorConfig::DEFAULTS['cpc_minimo_cliques'])"` retorna `null`
</verification>

<success_criteria>
1. Schema: `cpc_minimo_cliques` (unsignedInteger, nullable, posicao apos cpc_maximo_logic) presente em sugador_configs
2. Model: campo em fillable/casts(integer)/DEFAULTS(null); round-trip preserva null e int
3. Service: criterio cpc_alto aplica gate composto conforme D-01; null preserva legacy; numero positivo gate clicks
4. Tests: 9/9 verdes (4 Feature + 5 Unit); 0 regressao na suite Sugador
5. REQ-42-04 backend coberto (frontend fica para Plan 42-02)
</success_criteria>

<output>
Criar `.planning/phases/42-sugadores-api-ml/42-01-SUMMARY.md` ao terminar, listando arquivos alterados, decisoes tomadas, tests passando, gates batidos.
</output>
