---
phase: 42-sugadores-api-ml
plan: 06
type: execute
wave: 4
depends_on: ["42-04"]
files_modified:
  - tests/Feature/Phase42/AceitacaoMlFluxoCompletoTest.php
  - tests/Feature/Phase42/RegressaoSugadoresExistentesTest.php
autonomous: true
requirements: [REQ-42-08, REQ-42-10]

must_haves:
  truths:
    - "Suite `AceitacaoMlFluxoCompletoTest` rodada manualmente reproduz os 10 success criteria do ROADMAP Phase 42 (briefing §14): janela 30d, gasto_sem_venda, cpc composto, SGI skipada, status travado, idempotencia, ByMobille E2E"
    - "Suite `RegressaoSugadoresExistentesTest` confirma que TODOS os testes Feature pre-existentes em `tests/Feature/Sugadores/` continuam passando sem ALTERACAO (REQ-42-10)"
    - "Suite Sugador acumulada (legacy + Phase 38/39/40/41/42) permanece verde — zero regressao"
    - "ByMobille - Teste (#298) ou equivalente factory roda fluxo E2E: Http::fake ML → analyzeCompany → Sugador::count() > 0 com origem ML detectada no raw_data"
    - "Suite Phase42 acumulada >= 47 verdes (9 + 5 + 7 + 8 + 12 + ~6 deste plan)"
  artifacts:
    - path: "tests/Feature/Phase42/AceitacaoMlFluxoCompletoTest.php"
      provides: "Suite de aceite cobrindo os 10 success criteria do ROADMAP Phase 42 numa sequencia narrativa"
    - path: "tests/Feature/Phase42/RegressaoSugadoresExistentesTest.php"
      provides: "Suite guard que documenta e roda os tests Feature/Sugadores legados; falha se algum quebrar"
  key_links:
    - from: "Tests Feature/Sugadores legados"
      to: "Suite Sugador acumulada"
      via: "exec sem modificacao"
      pattern: "Feature.Sugadores"
    - from: "ByMobille piloto E2E"
      to: "Sugador via ML provider"
      via: "Http::fake + analyzeCompany"
      pattern: "fluxo_completo_ByMobille"
---

<objective>
Plan final da Phase 42. Entrega 2 suites de tests Feature numa fatia VERTICAL (apenas tests — sem mudancas em codigo de producao):

1. **Aceitacao E2E (REQ-42-08)**: 6 tests narrativos cobrindo cada um dos 10 success criteria do briefing §14 + ROADMAP Phase 42. Um deles eh fluxo completo simulando ByMobille - Teste rodando analise via ML com Http::fake completo (listCampaigns + product_ads/ads) e assert que sugadores aparecem em `/sugadores` com origem ML detectada e cards de metricas corretos.

2. **Guard de regressao (REQ-42-10)**: garante explicitamente que os 2 tests pre-existentes em `tests/Feature/Sugadores/` (AutoResolveTest, SugadoresIndexTest) continuam verdes apos toda Phase 42 — eh um meta-test que dispara os legados como subprocess via `Artisan::call('test', ['--testsuite' => 'Feature', '--filter' => 'Sugadores'])` ou via assertion estrutural sobre os tests legados.

Purpose: certificacao final de que a Phase 42 atinge todos os 10 must-haves do ROADMAP e que zero teste legado quebra. Fornece artefato auditavel para o `/gsd:verify-phase` validar antes de fechar a Phase 42 e abrir Phase 43 (remocao Adman).

Output: 2 arquivos de teste. ZERO mudanca em codigo de producao. Esta plan eh integralmente sobre VERIFICACAO.

Sem deploy nesta plan. Smoke real de ByMobille em prod fica para post-deploy do consolidate-wave da Phase 42.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-sugadores-api-ml/42-CONTEXT.md
@.planning/phases/42-sugadores-api-ml/42-01-PLAN.md
@.planning/phases/42-sugadores-api-ml/42-03-PLAN.md
@.planning/phases/42-sugadores-api-ml/42-04-PLAN.md
@.planning/phases/42-sugadores-api-ml/42-05-PLAN.md
@tests/Feature/Sugadores/AutoResolveTest.php
@tests/Feature/Sugadores/SugadoresIndexTest.php

<interfaces>
<!-- 10 Success Criteria da Phase 42 (ROADMAP) -->

1. `/sugadores` continua sendo a unica tela operacional; nenhum item novo na sidebar
2. `/sugadores/config/{company}` mostra novo campo `cpc_minimo_cliques` ao lado de `cpc_maximo` + `cpc_maximo_logic`
3. ByMobille - Teste roda analise via comando manual; sugadores aparecem em `/sugadores` com origem ML transparente
4. Janela: 26/05/2026 → 24/06/2026 quando rodado em 25/06/2026
5. `gasto_minimo_sem_venda=20 OU` → sugador com gasto>=20 e zero vendas = `pendente`
6. `cpc_maximo=4` + `cpc_minimo_cliques=5` → so flaga quando CPC > 4 E cliques >= 5 E zero vendas
7. Campanha `SGI - Lentes` eh pulada (quarentena §12)
8. Sugador em em_acao/resolvido permanece nesse status apos re-analise ML do mesmo dia
9. Item sidebar 'Onboarding ML' nao aparece; rota direta continua respondendo (admin)
10. Todos os testes Feature de Sugadores existentes passam

Tests legacy em tests/Feature/Sugadores/:
- AutoResolveTest.php (auto-resolucao via reference_date anterior)
- SugadoresIndexTest.php (listagem filtros default carteira)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite AceitacaoMlFluxoCompletoTest (6 tests E2E cobrindo 10 success criteria)</name>
  <files>tests/Feature/Phase42/AceitacaoMlFluxoCompletoTest.php</files>
  <action>
    Criar `tests/Feature/Phase42/AceitacaoMlFluxoCompletoTest.php` (namespace Tests\\Feature\\Phase42, uses RefreshDatabase + Carbon::setTestNow('2026-06-25 12:00:00')) com 6 tests narrativos. Cada test cobre 1+ success criteria do ROADMAP Phase 42:

    **T1 sc01_sc02_sc09_sidebar_e_config_ui**: cobre SC#1 (sem novo item sidebar), SC#2 (campo cpc_minimo_cliques visivel em /sugadores/configs), SC#9 (Onboarding ML escondido).
    - actingAs admin; GET `/sugadores/configs/{company}` (criar Company com sugadorConfig) — Inertia component `Sugadores/Config`; payload `config.cpc_minimo_cliques` chave presente (null OK)
    - GET `/dashboard`; HTML inicial NAO contem `Onboarding ML`
    - GET `/dev/sugadores-ml-onboarding`; assertOk (rota direta funciona)

    **T2 sc03_sc04_bymobille_e2e_analise_ml**: cobre SC#3 (ByMobille E2E) + SC#4 (janela 30d).
    - Criar Company que simula ByMobille: nome='ByMobille - Teste', sem `adman_account_id`, com mlToken status='active' (factory). Anexar advertiser_id via MlAdvertiser factory.
    - Http::fake do `listCampaigns` retornando 1 campanha id=C1 name='Campanha Padrao' status=active
    - Http::fake do `product_ads/ads` retornando 1 adgroup com campaign_id=C1, metrics={cost=50, units_quantity=0, clicks=12, prints=200}, item_id=MLB123, type=product_ad
    - Rodar `php artisan sugadores:analyze --company={id}` via Artisan::call
    - Assert: `Sugador::count() === 1`; sugador tem `tipo=adgroup`, `campaign_id=C1`, `mlb_id=MLB123`, `status=pendente`, `motivos` contem `gasto_sem_venda`
    - Validar janela: `periodo_inicio === '2026-05-27'` e `periodo_fim === '2026-06-24'`
    - Validar origem ML no raw_data: `raw_data` contem `metrics` aninhado E `item_id`
    - GET `/sugadores`; payload Inertia contem este sugador na lista

    **T3 sc05_sc06_criterios_gasto_e_cpc_composto**: cobre SC#5 (gasto_sem_venda flaga) + SC#6 (cpc composto so flaga com cliques>=5).
    - Setup: Company ML + sugadorConfig override `gasto_minimo_sem_venda=20`+optional, `cpc_maximo=4`+optional, `cpc_minimo_cliques=5`
    - Caso 5a (gasto): Http::fake adgroup metrics={cost=25, units_quantity=0, cpc=2.0, clicks=3}; rodar analyze; sugador criado com motivos contendo `gasto_sem_venda`
    - Caso 5b (gasto abaixo): cost=10; rodar; Sugador::count() permanece 0
    - Caso 6a (cpc + cliques OK): cost=5, cpc=6.0, clicks=10; sugador com motivos contendo `cpc_alto`
    - Caso 6b (cpc OK mas cliques abaixo): cost=5, cpc=6.0, clicks=3; sugador NAO criado (gate `cpc_minimo_cliques=5` falha)
    - Validar idempotencia: rodar 2x mesmo dia; Sugador::count() final == 1 (mesma chave canonica)

    **T4 sc07_quarentena_sgi**: cobre SC#7 (campanha SGI pulada).
    - Http::fake listCampaigns retorna 1 campanha id=C1 name='SGI - Lentes' status=active
    - product_ads/ads retorna 1 adgroup que bateria criterio gasto_sem_venda
    - Rodar analyze; `Sugador::count() === 0`

    **T5 sc08_status_travado_preservado**: cobre SC#8 (status travado em re-analise).
    - 1a analise: Http::fake → Sugador criado (status=pendente). Manualmente: `$sugador->update(['status' => 'em_acao'])`.
    - 2a analise no mesmo dia: Http::fake mesma resposta. Sugador atualizado mas `status` permanece 'em_acao' (NAO volta a 'pendente'). Validar via `assertEquals('em_acao', $sugador->fresh()->status)`.
    - Validar tambem: `updated_at` mudou; metricas atualizadas (mesmas neste caso).
    - Validar tambem status=`resolvido`: criar 2o sugador, marcar resolvido, re-analisar; permanece resolvido.

    **T6 sc09_sidebar_para_consultor_e_role_gate**: cobre SC#9 reforco (consultor nao ve sidebar nem rota).
    - actingAs consultor; GET `/dashboard`; HTML inicial NAO contem `Onboarding ML`
    - GET `/dev/sugadores-ml-onboarding`; assertForbidden (role:admin gate intacto)

    Helpers compartilhados:
    - `actingAsAdmin()`, `actingAsConsultor()`
    - `makeBymobileLike(): Company` (Company name='ByMobille - Teste', com mlToken active + MlAdvertiser linkado)
    - `mlConfigComCpcComposto(Company $c, int $cliques = 5): SugadorConfig` (override DEFAULTS com cpc_maximo=4, cpc_minimo_cliques=$cliques)
    - `httpFakeMlAds(array $campaigns, array $ads)` (helper para fixar endpoints `advertising/.../campaigns` e `.../product_ads/ads`)
    - `runAnalyze(int $companyId)` (chama Artisan + retorna exit code)

    Cada test `#[\\PHPUnit\\Framework\\Attributes\\Test]`. setUp Carbon::setTestNow + tearDown Mockery::close.
  </action>
  <read_first>
    - tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php (helpers Http::fake ML do Plan 42-03)
    - tests/Feature/Phase42/CutOverMlPrimaryTest.php (factory + cut-over do Plan 42-04)
    - tests/Feature/Phase42/SidebarAndAdsLinkTest.php (sidebar gating do Plan 42-05)
    - tests/Feature/Phase42/SugadorConfigCpcMinimoCliquesUiTest.php (config UI do Plan 42-02)
    - tests/Feature/Sugadores/SugadoresIndexTest.php (padrao listagem)
  </read_first>
  <verify>
    <automated>php artisan test tests/Feature/Phase42/AceitacaoMlFluxoCompletoTest.php 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 6 tests narrativos GREEN
    - Cada test referencia explicitamente os SC#X do ROADMAP no nome do metodo (sc01_sc02_sc09_..., sc03_sc04_..., etc)
    - `grep -c "#\\[.*Test\\]" tests/Feature/Phase42/AceitacaoMlFluxoCompletoTest.php` retorna 6
    - `grep -c "ByMobille" tests/Feature/Phase42/AceitacaoMlFluxoCompletoTest.php` >= 2 (factory + assertions)
    - Suite Phase42 acumulada >= 47
  </done>
</task>

<task type="auto">
  <name>Task 2: Suite RegressaoSugadoresExistentesTest (guard zero-regressao)</name>
  <files>tests/Feature/Phase42/RegressaoSugadoresExistentesTest.php</files>
  <action>
    Criar `tests/Feature/Phase42/RegressaoSugadoresExistentesTest.php` (namespace Tests\\Feature\\Phase42) com 2 tests guard que fazem assertion estrutural sobre os testes legados:

    Estrategia: NAO chamar Artisan::call('test', ...) (nested test invocation eh fragil). Em vez disso, validar estrutura dos arquivos legados + carregar um caso de uso minimo de cada arquivo via reflexao + executar 1 metodo de cada.

    Versao mais simples e robusta:

    **T1 arquivos_legados_existem_e_nao_foram_modificados_nesta_phase**:
    - Validar que `tests/Feature/Sugadores/AutoResolveTest.php` existe (file_exists)
    - Validar que `tests/Feature/Sugadores/SugadoresIndexTest.php` existe
    - Validar via `git log --oneline -- tests/Feature/Sugadores/ ^HEAD~10..HEAD` (atraves de `exec()` opcional) que NENHUM commit de Phase 42 modificou esses arquivos. Alternativa robusta: contar metodos `#[Test]` em cada arquivo (snapshot) — se a contagem mudou, fail.
    - Assertions: file_exists em ambos arquivos + assertGreaterThanOrEqual(1, $autoResolveCount) + assertGreaterThanOrEqual(1, $indexTestCount). Numeros snapshot ESPECIFICOS (descobrir via grep no Task 1.5 pre-write).

    **T2 suite_legada_passa_quando_executada_em_isolamento**:
    - Eh meta-test. Strategy: usar Symfony Process para spawn `php artisan test tests/Feature/Sugadores/ --no-coverage` como subprocess e capturar exit code.
    - Assert: exit code === 0
    - Se Process nao disponivel, FALLBACK: marcar test como `markTestSkipped("Validacao manual: rodar 'php artisan test tests/Feature/Sugadores/' antes do consolidate-wave")` e documentar em SUMMARY que o gate eh manual (orquestrador roda essa suite no consolidate-wave de qualquer forma).

    Cada test `#[Test]`. Comentario pt-BR no topo do arquivo explicando que esta suite eh GUARDA de REQ-42-10 — se um test legado quebrar, esta suite falha como early warning antes do orquestrador consolidar.
  </action>
  <read_first>
    - tests/Feature/Sugadores/AutoResolveTest.php (contar tests; snapshot da estrutura)
    - tests/Feature/Sugadores/SugadoresIndexTest.php
  </read_first>
  <verify>
    <automated>php artisan test tests/Feature/Phase42/RegressaoSugadoresExistentesTest.php 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 2 tests GREEN
    - Snapshot dos arquivos legados documentado no test (numero de #[Test] esperado)
    - T2 documenta estrategia (Process subprocess OU markTestSkipped com instrucao manual)
    - Suite Phase42 acumulada >= 49 (47 + 2)
    - Suite Sugador acumulada permanece verde (gate de regressao explicito)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests narrativos → producao | falso positivo (test passa mas comportamento real falha em ByMobille prod) |
| Suite guard → tests legados | snapshot pode ficar desatualizado se outro PR alterar tests legados em paralelo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-06-01 | Tampering | Http::fake do ML em T2 nao espelha 100% o payload real do Mercado Ads, gerando falso positivo | mitigate | Fixture do Http::fake usa shape derivado do `MercadoLivreSugadoresProvider` Phase 39-02 (CANDIDATO documentado) + ajustado pelos Plans 42-03 (campaign_name merge). Smoke real Bymobille post-deploy fecha a discrepancia. CONTEXT.md ja documenta este caminho como "validacao iterativa pos-smoke" |
| T-42-06-02 | Repudiation | Process subprocess em T2 do guard nao consegue invocar `php artisan test` em ambiente CI | accept | Fallback markTestSkipped explicito documenta gate manual. Orquestrador consolidate-wave RODA a suite legada de qualquer forma — esta suite eh meta-guard, nao gate critico |
| T-42-06-03 | DoS | Tests E2E pesados (10+ Http::fake calls) podem demorar > 60s no SQLite em-memory | accept | Cada test do Plan 42-06 eh autonomo; rodar isoladamente cabe em < 5s. Numero total esperado pelo Phase42: <= 50 tests, < 60s combinado |
| T-42-06-SC | Tampering | npm/composer installs | accept | Esta phase NAO instala packages |
</threat_model>

<verification>
- 8 tests novos (6 aceite E2E + 2 guard) GREEN
- Suite Phase42 acumulada >= 49 verdes
- Suite Sugador acumulada permanece verde (gate cumprido)
- 10 success criteria do ROADMAP Phase 42 cobertos por pelo menos 1 test (mapeamento documentado nos nomes dos metodos T1-T6 da Task 1)
- Smoke real Bymobille em prod fica DEFERRED para post-deploy do consolidate-wave (esperado — briefing §14 lista como "Antes de considerar pronto" + STATE.md ja documenta MariaDB local corrompido)
</verification>

<success_criteria>
1. REQ-42-08 cobertura formal (T2 do AceitacaoMlFluxoCompletoTest faz ByMobille E2E)
2. REQ-42-10 cobertura formal (RegressaoSugadoresExistentesTest valida tests legados)
3. 10 success criteria do ROADMAP Phase 42 cada um cobertos por nome explicito de test
4. Phase 42 INTEIRA validada via tests + pronta para consolidate-wave do orquestrador
5. Smoke real Bymobille fica DOCUMENTADO no SUMMARY como follow-up post-deploy
</success_criteria>

<output>
Criar `.planning/phases/42-sugadores-api-ml/42-06-SUMMARY.md` com:
- Matriz SC#X → metodo de test que cobre
- Smoke real Bymobille listed como follow-up post-deploy
- Atualizar STATE.md sinalizando Phase 42 inteira pronta para `/gsd:complete-phase 42`
</output>
