---
phase: 42-sugadores-api-ml
plan: 05
type: execute
wave: 4
depends_on: ["42-04"]
files_modified:
  - resources/js/Layouts/AppLayout.jsx
  - app/Models/Sugador.php
  - tests/Feature/Phase42/SidebarAndAdsLinkTest.php
  - tests/Unit/Phase42/LinkAdsMlUnitTest.php
autonomous: true
requirements: [REQ-42-07, REQ-42-09]

must_haves:
  truths:
    - "Item 'Onboarding ML' (`Dev/SugadoresMlOnboarding/Index`) NAO aparece em AppLayout.jsx para nenhum usuario, incluindo admin (D-02: esconde da sidebar)"
    - "Rota nomeada `dev.sugadores_ml_onboarding.index` PERMANECE registrada em routes/web.php (acesso via URL direta como ferramenta tecnica) — esta plan NAO altera routes/web.php"
    - "Controller `Dev/SugadoresMlOnboardingController` e React page `Dev/SugadoresMlOnboarding/Index.jsx` PERMANECEM intactos no repositorio (D-02: nao remover arquivos)"
    - "`Sugador::linkAdsML()` aceita sugador de origem ML e retorna URL do painel Mercado Ads (ads.mercadolivre.com.br) usando campaign_id/ad_id/item_id disponivel em raw_data quando aplicavel"
    - "Quando `raw_data->advertiser_id` e `raw_data->campaign_id` estao presentes (origem ML), URL formato `https://www.mercadolivre.com.br/anuncios/product-ads/anuncios?campaignId={X}` (deep link Mercado Ads — confirmado via fixture pos-Phase 38)"
    - "Quando sugador eh de origem Adman (raw_data nao tem chaves Mercado Ads), `linkAdsML()` MANTEM o link atual (`mercadolivre.com.br/anuncios/campanhas/{campaign_id}`) para nao quebrar Adman legacy"
  artifacts:
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "Item Onboarding ML removido do array children do grupo Dev"
    - path: "app/Models/Sugador.php"
      provides: "linkAdsML() roteia para Mercado Ads URL quando origem ML (raw_data detectado), preservando comportamento Adman"
    - path: "tests/Feature/Phase42/SidebarAndAdsLinkTest.php"
      provides: "Suite Feature: 3 tests sidebar (admin/consultor + GET direto) + 4 tests linkAdsML origem ML/Adman"
    - path: "tests/Unit/Phase42/LinkAdsMlUnitTest.php"
      provides: "Suite Unit: 5 cenarios de raw_data → URL gerada"
  key_links:
    - from: "AppLayout.jsx"
      to: "sidebar children Dev"
      via: "remocao do item Onboarding ML"
      pattern: "Onboarding ML"
    - from: "Sugador->linkAdsML()"
      to: "raw_data origem ML"
      via: "deteccao de chaves ML no raw_data"
      pattern: "advertiser_id|campaignId"
---

<objective>
Fecha os requisitos UI da Phase 42 (REQ-42-07 esconder sidebar Onboarding ML; REQ-42-09 botao Painel de Ads abre Mercado Ads para sugadores ML). Duas mudancas independentes:

1. **REQ-42-07**: Remover o item 'Onboarding ML' do array `children` do grupo Dev em AppLayout.jsx (linha 86). Item desaparece da sidebar para todos os usuarios. Rota, controller e React page PERMANECEM no repo — acesso via URL direta `/dev/sugadores-ml-onboarding` continua respondendo com role:admin como ferramenta tecnica (D-02). NAO alterar routes/web.php nem deletar arquivos.

2. **REQ-42-09**: `Sugador::linkAdsML()` atual gera URL Adman `mercadolivre.com.br/anuncios/campanhas/{campaign_id}` que NAO eh o painel Mercado Ads correto para sugadores ML. Adicionar deteccao de origem ML via `raw_data` (chaves caracteristicas: `metrics`, `item_id`, `type` — pattern do payload Mercado Ads via Plan 42-03) e gerar URL deep-link Mercado Ads: `https://www.mercadolivre.com.br/anuncios/product-ads/anuncios?campaignId={campaign_id}` (formato confirmado pos-Phase 38 smoke ou tratado como CANDIDATO — revalidar pos-deploy). Sugadores Adman mantem URL atual (zero regressao).

Purpose: analista clica "Painel de Ads" no detalhe de um sugador ML e abre o painel Mercado Ads (nao o painel Adman antigo). Sidebar nao tem mais item de ferramenta tecnica que confunde usuarios finais (operacao continua so em `/sugadores`).

Output: 1 linha removida de AppLayout.jsx (+ deps de import) + ~20 linhas em Sugador::linkAdsML + 2 suites de tests (Feature integracao + Unit pura).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-sugadores-api-ml/42-CONTEXT.md
@.planning/phases/41-onboarding-ml-por-empresa/41-05-SUMMARY.md
@resources/js/Layouts/AppLayout.jsx
@app/Models/Sugador.php
@resources/js/Pages/Sugadores/Show.jsx

<interfaces>
<!-- AppLayout atual (grupo Dev, linha 77-89) -->

From resources/js/Layouts/AppLayout.jsx:
```jsx
{
    group: 'Dev',
    icon: Code2,
    children: [
        { label: 'Log',            routeName: 'activity-log.index',  ..., permission: 'sistema.activity_log' },
        { label: 'Desenvolvimento', routeName: 'dev.desenvolvimento', ..., permission: 'sistema.desenvolvimento' },
        // Phase 41 Plan 41-05 — UI admin de onboarding ML por empresa.
        { label: 'Onboarding ML', routeName: 'dev.sugadores_ml_onboarding.index', ..., excludeRoles: ['consultor', 'mentor', 'publicador', 'analista', 'gestor', 'lider'] },
        { label: 'ML OAuth',       routeName: 'ml.oauth.index',      ..., permission: 'sistema.ml_oauth' },
    ],
},
```

From app/Models/Sugador.php (linkAdsML atual, linha 186):
```php
public function linkAdsML(): string
{
    $base = 'https://www.mercadolivre.com.br/anuncios';
    if ($this->campaign_id) {
        return $base . '/campanhas/' . $this->campaign_id;
    }
    return $base;
}
```

From resources/js/Pages/Sugadores/Show.jsx (botao Painel de Ads, linha 273-283):
```jsx
{url_ads && (
    <a href={url_ads} target="_blank" rel="noopener noreferrer" ...>
        Painel de Ads
    </a>
)}
```
URL eh injetada via prop `sugador.url_ads` montada por SugadorController::show (linha 272: `'url_ads' => $sugador->linkAdsML()`).

D-02 (briefing §10.1): NAO REMOVER arquivos. Rota acessivel via URL.
D-09 spec do briefing §14 (botao Painel): "abrir o painel correto do Mercado Livre usando campaign_id/ad_id/item_id disponivel".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Esconder item 'Onboarding ML' da sidebar</name>
  <files>resources/js/Layouts/AppLayout.jsx</files>
  <action>
    Em `resources/js/Layouts/AppLayout.jsx`, no array `children` do grupo `Dev` (linhas 81-88):

    1. **Remover** a linha completa:
       ```
       { label: 'Onboarding ML', routeName: 'dev.sugadores_ml_onboarding.index', page: 'Dev/SugadoresMlOnboarding/Index', icon: Activity, excludeRoles: [...] },
       ```
       (+ as 2 linhas de comentario pt-BR acima dela)

    2. **Verificar imports**: `Activity` foi adicionado em Plan 41-05 especificamente para este item. Se nenhum outro item usa `Activity`, remover do import lucide-react. Validacao: `grep -c "\\bActivity\\b" resources/js/Layouts/AppLayout.jsx` — se >= 2 apos remocao do item, manter `Activity` no import (algum outro lugar usa); se == 1 (so o import), remover do import tambem.

    3. Adicionar comentario pt-BR no lugar do item removido (1 linha):
       ```
       // Phase 42 D-02 / REQ-42-07: item 'Onboarding ML' escondido da sidebar.
       // Rota /dev/sugadores-ml-onboarding permanece acessivel via URL direta (role:admin) como ferramenta tecnica.
       ```

    4. NAO alterar:
       - routes/web.php (rotas permanecem)
       - app/Http/Controllers/Dev/SugadoresMlOnboardingController.php (intacto)
       - resources/js/Pages/Dev/SugadoresMlOnboarding/Index.jsx (intacto)
       - SugadorMlCompanyConfig, MlAdvertiser, etc — DB layer intacto
  </action>
  <read_first>
    - resources/js/Layouts/AppLayout.jsx (linhas 1-100; foco em linhas 77-89)
    - .planning/phases/41-onboarding-ml-por-empresa/41-05-SUMMARY.md (linhas 130-142 — como o item foi inserido)
  </read_first>
  <verify>
    <automated>grep -c "dev.sugadores_ml_onboarding" resources/js/Layouts/AppLayout.jsx</automated>
  </verify>
  <done>
    - `grep -c "Onboarding ML" resources/js/Layouts/AppLayout.jsx` retorna 0
    - `grep -c "dev.sugadores_ml_onboarding" resources/js/Layouts/AppLayout.jsx` retorna 0
    - `grep -c "Phase 42 D-02" resources/js/Layouts/AppLayout.jsx` retorna 1 (comentario de rastreabilidade)
    - `grep -c "dev.sugadores_ml_onboarding" routes/web.php` retorna >= 4 (rotas intactas)
    - app/Http/Controllers/Dev/SugadoresMlOnboardingController.php NAO modificado (`git diff --stat` sem hit)
    - npm run build (no consolidate-wave) sem erros JSX
  </done>
</task>

<task type="auto">
  <name>Task 2: Sugador::linkAdsML roteia por origem (ML vs Adman)</name>
  <files>app/Models/Sugador.php</files>
  <action>
    Em `app/Models/Sugador.php::linkAdsML` (linha 186-193):

    Substituir corpo do metodo por logica que detecta origem ML via `raw_data` e gera URL Mercado Ads quando aplicavel; mantem URL Adman como fallback.

    Heuristica de deteccao de origem ML no raw_data (chaves caracteristicas do payload do MercadoLivreSugadoresProvider em Plan 42-03):
    - `raw_data` contem chave `metrics` (estrutura aninhada de Mercado Ads) — sinal forte
    - OR `raw_data` contem chave `item_id` E `type` no nivel raiz — pattern de ads ML

    Quando origem ML detectada:
    ```
    $base = 'https://www.mercadolivre.com.br/anuncios/product-ads/anuncios';
    if ($this->campaign_id) {
        return $base . '?campaignId=' . urlencode($this->campaign_id);
    }
    return 'https://www.mercadolivre.com.br/anuncios/product-ads';
    ```
    Quando origem Adman (raw_data ausente ou sem chaves ML):
    ```
    // Mantém comportamento existente Adman
    $base = 'https://www.mercadolivre.com.br/anuncios';
    if ($this->campaign_id) {
        return $base . '/campanhas/' . $this->campaign_id;
    }
    return $base;
    ```

    Extrair helper privado `private function isOrigemMl(): bool` retornando true quando heuristica bate. Cast defensivo: `$raw = $this->raw_data; if (!is_array($raw)) return false;`.

    Adicionar PHPDoc pt-BR no metodo explicando:
    - REQ-42-09: deep link Mercado Ads para sugadores ML
    - Formato `?campaignId=` eh CANDIDATO — revalidar pos smoke real Bymobille (briefing §14); ajustar se ML usar `/campaigns/{id}/edit` ou outra rota
    - Backward compatibility: sugador antigo sem raw_data cai no path Adman

    NAO alterar `urlAnuncioML` (linha 168 — ja funciona para ambas origens via mlb_id).
  </action>
  <read_first>
    - app/Models/Sugador.php (linhas 1-194)
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php (linhas 154-194 — pos Plan 42-03; chaves do raw)
  </read_first>
  <verify>
    <automated>grep -c "product-ads" app/Models/Sugador.php</automated>
  </verify>
  <done>
    - `grep -c "product-ads" app/Models/Sugador.php` retorna >= 1
    - `grep -c "isOrigemMl" app/Models/Sugador.php` retorna >= 2 (declaracao + chamada)
    - `grep -c "REQ-42-09" app/Models/Sugador.php` retorna >= 1 (rastreabilidade)
    - PHPDoc pt-BR marca formato como CANDIDATO (revalidar pos-smoke)
    - Sintaxe valida: `php -l app/Models/Sugador.php` sem erros
  </done>
</task>

<task type="auto">
  <name>Task 3: Suites Feature (sidebar + integracao) + Unit (linkAdsML)</name>
  <files>tests/Feature/Phase42/SidebarAndAdsLinkTest.php, tests/Unit/Phase42/LinkAdsMlUnitTest.php</files>
  <action>
    **A) Feature `SidebarAndAdsLinkTest`** (namespace Tests\\Feature\\Phase42, uses RefreshDatabase) — 7 tests:

    Sidebar:
    - T1 sidebar_nao_contem_onboarding_ml_para_admin: actingAs admin + GET `/dashboard` (qualquer pagina autenticada) — Inertia component renderiza AppLayout; `$response->getContent()` (HTML inicial) NAO contem string `Onboarding ML`. Alternativa Inertia: validar que arquivo JSX nao tem string (parse via file_get_contents)
    - T2 sidebar_nao_contem_onboarding_ml_para_consultor: actingAs consultor + GET / — mesma validacao
    - T3 rota_direta_continua_acessivel_para_admin: actingAs admin + GET `/dev/sugadores-ml-onboarding` — assertOk (rota intacta)

    Integracao linkAdsML via SugadorController::show:
    - T4 url_ads_aponta_para_mercado_ads_quando_origem_ml: criar Sugador com `raw_data` contendo `['metrics' => ['cost' => 10], 'item_id' => 'MLB123', 'type' => 'product_ad']`; GET `/sugadores/{id}` — payload Inertia `sugador.url_ads` contem `product-ads`
    - T5 url_ads_mantem_formato_adman_quando_raw_nao_eh_ml: criar Sugador com `raw_data` Adman-like (sem chaves `metrics`/`item_id`/`type` aninhadas Mercado Ads) — `sugador.url_ads` NAO contem `product-ads` (formato legacy)
    - T6 url_ads_sem_campaign_id_retorna_base: criar Sugador com campaign_id=null + raw_data ML — `sugador.url_ads` eh base sem query string
    - T7 url_ads_sem_raw_data_cai_em_adman: criar Sugador com raw_data=null + campaign_id=C123 — url contem `/anuncios/campanhas/C123`

    **B) Unit `LinkAdsMlUnitTest`** (namespace Tests\\Unit\\Phase42, uses RefreshDatabase porque Sugador eh Eloquent) — 5 tests cobrindo combinacoes raw_data → URL gerada (mesma matriz, isolada):

    - T1 origem_ml_completa: raw_data={metrics: {cost: 10}, item_id: 'MLB1', type: 'product_ad'} + campaign_id='C1' → url contem `product-ads/anuncios?campaignId=C1`
    - T2 origem_ml_so_metrics: raw_data={metrics: {...}} + campaign_id='C1' → url contem `product-ads`
    - T3 origem_ml_so_item_id_e_type: raw_data={item_id, type} sem metrics + campaign_id='C1' → url contem `product-ads`
    - T4 raw_data_adman: raw_data={campaignId: 123, accountId: 456} + campaign_id='C1' → url eh `https://www.mercadolivre.com.br/anuncios/campanhas/C1`
    - T5 raw_data_null: raw_data=null + campaign_id='C1' → url eh path Adman legacy

    Cada test `#[\\PHPUnit\\Framework\\Attributes\\Test]`. Helpers `makeSugador(array $overrides)` com factory minima.
  </action>
  <read_first>
    - tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php (padrao actingAsAdmin/actingAsConsultor + Inertia render)
    - tests/Feature/Sugadores/SugadoresIndexTest.php (padrao Sugador factory)
    - app/Models/Sugador.php (apos Task 2)
    - resources/js/Layouts/AppLayout.jsx (apos Task 1)
  </read_first>
  <verify>
    <automated>php artisan test tests/Feature/Phase42/SidebarAndAdsLinkTest.php tests/Unit/Phase42/LinkAdsMlUnitTest.php 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 7 Feature tests + 5 Unit tests = 12 GREEN
    - Suite Phase42 acumulada >= 41 (29 Plan 42-01..04 + 12 deste)
    - Suite Sugador acumulada permanece verde (linkAdsML existente para Adman preservado — T7)
    - Suite Phase41 permanece verde (rota Onboarding ML intacta — T3)
    - Zero modificacao em routes/web.php, controller Dev/SugadoresMlOnboardingController, Pages/Dev/SugadoresMlOnboarding/Index.jsx (validado por `git diff --stat`)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidebar items → frontend filter | item escondido pode reaparecer se cache do Vite usar build antigo |
| linkAdsML → user clicks | URL gerada eh aberta em new tab; URL invalida = 404 no destino |
| heuristica isOrigemMl → raw_data | raw_data pode ser malformado (Adman antigo com `metrics` chave generica) e gerar falso positivo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05-01 | Information disclosure | rota /dev/sugadores-ml-onboarding aparece em sitemap interno apos esconder da sidebar | accept | Rota mantem role:admin middleware (Phase 41-05). Briefing §10.1 explicita que ferramenta tecnica eh aceitavel; descobrirem a URL nao da acesso a nao-admins |
| T-42-05-02 | Tampering | heuristica isOrigemMl falsos positivos — sugador Adman com raw_data contendo `metrics` chave (palavra comum) cai em url ML | mitigate | Heuristica usa CONJUNCAO de chaves Mercado Ads (item_id + type aninhados E metrics) — payload Adman tipico nao tem essa estrutura. T4/T5 do Feature test validam que fixtures Adman NAO ativam path ML |
| T-42-05-03 | DoS | mercadolivre.com.br/anuncios/product-ads/anuncios pode redirecionar 302 para login — abrir new tab abre fluxo de login do ML | accept | Comportamento esperado — analista ja logado no painel ML em outra aba. Briefing §15 cita "abrir o painel correto" — login eh parte do fluxo do dono da conta |
| T-42-05-04 | Repudiation | URL Mercado Ads mudou formato pos-Phase 38; PHPDoc CANDIDATO documenta risco | mitigate | Smoke real Bymobille (post-deploy) valida formato. Se incorreto, fix incremental e barato (1 linha do metodo isOrigemMl ou linkAdsML) |
| T-42-05-SC | Tampering | npm/composer installs | accept | Esta phase NAO instala packages |
</threat_model>

<verification>
- 12 tests GREEN (7 Feature + 5 Unit)
- Suite Phase42 acumulada >= 41
- Sidebar: visual humano post-deploy confirma item desaparece para admin (smoke deferred)
- linkAdsML: URL ML gerada por Sugador com raw_data ML; URL Adman gerada por Sugador legacy
- Zero modificacao em routes, controller Dev, React page do Onboarding ML
</verification>

<success_criteria>
1. REQ-42-07 ATENDIDO — item Onboarding ML escondido da sidebar (D-02); rota e arquivos permanecem
2. REQ-42-09 ATENDIDO — linkAdsML gera URL Mercado Ads para sugadores ML; Adman preservado
3. Tests cobertura: T1-T2 sidebar admin/consultor; T3 rota direta acessivel; T4-T7 url_ads condicional por origem; Unit T1-T5 matriz raw_data
4. Zero regressao
</success_criteria>

<output>
Criar `.planning/phases/42-sugadores-api-ml/42-05-SUMMARY.md` listando 1 linha removida da sidebar + 1 metodo refeito no Sugador + 12 tests + decisao de CANDIDATO no formato da URL ML.
</output>
