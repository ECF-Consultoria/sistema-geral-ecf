---
phase: 42-sugadores-api-ml
plan: 03
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - app/Services/SugadorAnalysisService.php
  - app/Services/Sugadores/MercadoLivreSugadoresProvider.php
  - tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php
autonomous: true
requirements: [REQ-42-01, REQ-42-03, REQ-42-05]

must_haves:
  truths:
    - "Janela default de analise eh fixada em 30 dias fechados conforme D-03: `periodo_fim = reference_date - 1 dia`, `periodo_inicio = periodo_fim - 29 dias`. Total 30 dias, exclui o dia em curso"
    - "`SugadorAnalysisService::analyzeCompany` quando rodado em 25/06/2026 com `dias_analise=30` retorna `periodo_inicio=2026-05-27` e `periodo_fim=2026-06-24` (exemplo do briefing §4, validado em teste)"
    - "`MercadoLivreSugadoresProvider::fetchCampaigns` retorna lista normalizada com campaign_id, campaign_name, campaign_status — TODAS as 3 chaves presentes mesmo quando ML payload omite (vira null)"
    - "`MercadoLivreSugadoresProvider::fetchAdgroupsMetrics` retorna lista normalizada com TODAS as 20 chaves do contrato §3 do briefing: adgroup_id, adgroup_name, campaign_id, campaign_name (resolvido via merge com fetchCampaigns), campaign_status, thumbnail, adgroup_type, catalog_listing, mlb_id, mlb_titulo, investment, revenue, sold_quantity, clicks, impressions, cpc, ctr, acos, roas, organic_amount, organic_units, raw"
    - "Adgroup ML com `campaign_id` que aponta para campanha de quarentena (nome contendo `SGI`/`Sugador`/`Sugadores`, ou status paused/closed/ended) eh SKIPADO pelo SugadorAnalysisService antes de chegar ao evaluator (REQ-42-05)"
    - "Adgroup ML sem `campaign_id` resolvido permanece com 'campaign_name' = null (fail-open — eh quarentena que pula, NAO eh analise que pula)"
  artifacts:
    - path: "app/Services/SugadorAnalysisService.php"
      provides: "Comentario de rastreabilidade D-03 explicitando janela 30d fechada (sem mudanca funcional — comportamento ja eh este via dias_analise=30)"
    - path: "app/Services/Sugadores/MercadoLivreSugadoresProvider.php"
      provides: "Normalizer: resolver campaign_name na lista de adgroups via merge com fetchCampaigns (advertiser_id por empresa cacheado por chamada)"
    - path: "tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php"
      provides: "Suite Feature 7 tests: janela 30d, contrato normalizado completo, quarentena SGI por nome+status, campaign_name resolvido"
  key_links:
    - from: "SugadorAnalysisService::analyzeCompany"
      to: "periodo_inicio/periodo_fim"
      via: "Calculo `$referenceDate->subDay()->subDays(dias_analise - 1)`"
      pattern: "subDays.*dias_analise.*1"
    - from: "MercadoLivreSugadoresProvider::fetchAdgroupsMetrics"
      to: "buildCampaignsInfoFromProvider (lookup quarentena)"
      via: "campaign_id eh propagado por adgroup; lookup eh feito no service"
      pattern: "campaign_id"
    - from: "MercadoLivreSugadoresProvider::fetchAdgroupsMetrics"
      to: "fetchCampaigns (resolucao de campaign_name)"
      via: "merge interno por campaign_id"
      pattern: "campaign_name"
---

<objective>
Garante que o provider ML alimenta o `SugadorAnalysisService` com o contrato §3 do briefing COMPLETO (sem campos ausentes) e que a janela de 30 dias fechados (D-03) eh aplicada. Inclui RESOLUCAO de `campaign_name` no payload de adgroups (hoje o provider deixa null porque `tryFetchAdsMetrics` so retorna campaign_id) — isso eh PRE-REQUISITO para REQ-42-05 (quarentena SGI), porque o filtro de quarentena por NOME do briefing §12 precisa do nome.

Tambem adiciona comentario de rastreabilidade D-03 no service (sem mudanca funcional — calculo `periodoFim = referenceDate->subDay()` + `periodoInicio = periodoFim->subDays(dias_analise - 1)` ja produz a janela correta com `dias_analise=30`).

Purpose: destrava cut-over real ML (Plan 42-04). Provider precisa entregar contrato canonico completo + nome de campanha resolvido pra quarentena funcionar; service ja gera janela certa, so falta validar via teste de aceite.

Output: edit Provider (~25 linhas no fetchAdgroupsMetrics — merge de campaign_name por dicionario campaign_id→name) + comentarios no Service + suite Feature 7 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-sugadores-api-ml/42-CONTEXT.md
@.planning/phases/41-onboarding-ml-por-empresa/41-02-SUMMARY.md
@.planning/phases/39-provider-pattern-sugadores-ml/39-02-SUMMARY.md
@app/Services/SugadorAnalysisService.php
@app/Services/Sugadores/MercadoLivreSugadoresProvider.php
@app/Services/Sugadores/MercadoLivreAdsService.php

<interfaces>
<!-- Contrato §3 do briefing 42-CONTEXT.md -->

Contrato canonico (todas chaves esperadas pelo SugadorAnalysisService::analyzeCompany):
```php
[
    'adgroup_id' => string,
    'adgroup_name' => string|null,
    'campaign_id' => string,
    'campaign_name' => string|null,  // <-- HOJE provider ML deixa null no fetchAdgroupsMetrics
    'campaign_status' => string|null, // <-- HOJE provider ML deixa null no fetchAdgroupsMetrics
    'thumbnail' => string|null,
    'adgroup_type' => string|null,
    'catalog_listing' => bool,
    'mlb_id' => string|null,
    'mlb_titulo' => string|null,
    'investment' => float|null,
    'revenue' => float|null,
    'sold_quantity' => int|null,
    'clicks' => int|null,
    'impressions' => int|null,
    'cpc' => float|null,
    'ctr' => float|null,
    'acos' => float|null,
    'roas' => float|null,
    'organic_amount' => float|null,
    'organic_units' => int|null,
    'raw' => array,
]
```

From app/Services/Sugadores/MercadoLivreSugadoresProvider.php (fetchAdgroupsMetrics atual):
```php
$out[] = [
    'adgroup_id' => (string) ($r['id'] ?? ''),
    'adgroup_name' => $r['title'] ?? null,
    'campaign_id' => (string) ($r['campaign_id'] ?? ''),
    'thumbnail' => $r['thumbnail'] ?? null,
    // ... falta campaign_name e campaign_status no map
];
```

From app/Services/SugadorAnalysisService.php (buildCampaignsInfoFromProvider):
```php
private function buildCampaignsInfoFromProvider(SugadoresAdsProvider $provider, Company $company): array
{
    try {
        $campaigns = $provider->fetchCampaigns($company);
    } catch (\Throwable $e) { return []; }

    $lookup = [];
    foreach ($campaigns as $c) {
        $id = (string) ($c['campaign_id'] ?? '');
        if ($id === '') continue;
        $lookup[$id] = [
            'name'   => $c['campaign_name']   ?? null,
            'status' => $c['campaign_status'] ?? null,
        ];
    }
    return $lookup;
}
```

Janela atual (analyzeCompany linhas 136-139):
```php
$periodoFim    = $referenceDate->copy()->subDay();
$periodoInicio = $periodoFim->copy()->subDays($config->dias_analise - 1);
```
Com `referenceDate=2026-06-25` e `dias_analise=30`: periodoFim=2026-06-24, periodoInicio=2026-05-27 ✓

Regex de quarentena (linha 46) ja cobre §12 do briefing:
```php
private const QUARANTINE_NAME_REGEX = '/\\b(sgi|sugadores?)\\b/iu';
private const QUARANTINE_STATUSES = ['paused', 'closed', 'ended'];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolver campaign_name nos adgroups ML via merge interno</name>
  <files>app/Services/Sugadores/MercadoLivreSugadoresProvider.php</files>
  <action>
    Em `MercadoLivreSugadoresProvider::fetchAdgroupsMetrics`:

    1. Apos resolver `$advertiserId` (linha 138) e ANTES do try/catch do `tryFetchAdsMetrics`, chamar `$this->ads->listCampaigns($company, $advertiserId, $from->toDateString(), $to->toDateString())` E construir lookup local `$campaignNames` = array `campaign_id => ['name' => string|null, 'status' => string|null]`. Fail-open: try/catch retorna `[]` se falhar — adgroups ainda fluem com campaign_name=null (mesmo comportamento de hoje em caso de erro).

    2. Dentro do `foreach ($results as $r)`, ao montar a row `$out[]`, adicionar chaves:
       - `'campaign_name' => $campaignNames[$campaignIdAdg]['name'] ?? null`
       - `'campaign_status' => $campaignNames[$campaignIdAdg]['status'] ?? null`
       onde `$campaignIdAdg = (string) ($r['campaign_id'] ?? '')`

    3. Posicao das duas chaves novas no array de retorno: imediatamente apos `'campaign_id'`, para preservar a ordem do contrato §3.

    4. Adicionar `// CANDIDATO — revalidar pos-smoke Phase 38 Tarefa 3` proximo as duas chaves novas (mesmo padrao do resto do arquivo).

    5. NAO alterar `fetchCampaigns()` nem `fetchCampaignsMetrics()` nem `fetchAdgroupMlbs()` — apenas `fetchAdgroupsMetrics()`.

    6. Adicionar comentario pt-BR no metodo explicando que campaign_name eh resolvido localmente para alimentar o filtro de quarentena do SugadorAnalysisService (REQ-42-05).
  </action>
  <read_first>
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php (linhas 136-197 — metodo fetchAdgroupsMetrics atual)
    - app/Services/Sugadores/MercadoLivreAdsService.php (assinatura listCampaigns)
    - app/Services/SugadorAnalysisService.php (linhas 597-619 — buildCampaignsInfoFromProvider que valida campaign_name no lookup)
  </read_first>
  <verify>
    <automated>grep -c "'campaign_name'" app/Services/Sugadores/MercadoLivreSugadoresProvider.php</automated>
  </verify>
  <done>
    - `grep -c "'campaign_name'" app/Services/Sugadores/MercadoLivreSugadoresProvider.php` retorna >= 2 (uma em fetchCampaigns ja existente + uma nova em fetchAdgroupsMetrics)
    - `grep -c "'campaign_status'" app/Services/Sugadores/MercadoLivreSugadoresProvider.php` retorna >= 2
    - `grep -c "listCampaigns" app/Services/Sugadores/MercadoLivreSugadoresProvider.php` retorna >= 2 (fetchCampaigns + fetchAdgroupsMetrics)
    - Comentario pt-BR de rastreabilidade REQ-42-05 presente no metodo
    - Sintaxe valida: `php -l app/Services/Sugadores/MercadoLivreSugadoresProvider.php` retorna `No syntax errors detected`
  </done>
</task>

<task type="auto">
  <name>Task 2: Comentario rastreabilidade janela D-03 no service</name>
  <files>app/Services/SugadorAnalysisService.php</files>
  <action>
    Em `SugadorAnalysisService::analyzeCompany`, imediatamente acima das linhas 136-139 (calculo `$periodoFim` e `$periodoInicio`), adicionar bloco de comentario pt-BR:

    ```
    // Phase 42 D-03 (briefing §4): janela de 30 dias FECHADOS por padrao.
    // periodoFim = ontem (referenceDate - 1 dia); periodoInicio = ontem - 29 dias.
    // Total: 30 dias, exclui o dia em curso. Comportamento ja vigente via
    // dias_analise=30 (DEFAULT); este comentario apenas explicita a regra
    // pra rastreabilidade. Override `$config->dias_analise != 30` muda apenas
    // o tamanho da janela — `periodoFim = referenceDate - 1 dia` permanece fixo.
    ```

    NAO mudar o calculo. NAO mudar `dias_analise` DEFAULT (continua 30 em SugadorConfig::DEFAULTS).
  </action>
  <read_first>
    - app/Services/SugadorAnalysisService.php (linhas 112-140 — assinatura analyzeCompany)
  </read_first>
  <verify>
    <automated>grep -c "Phase 42 D-03" app/Services/SugadorAnalysisService.php</automated>
  </verify>
  <done>
    - Comentario presente, sem mudanca funcional
    - `git diff --stat app/Services/SugadorAnalysisService.php` mostra apenas +N -0 (insercao pura)
  </done>
</task>

<task type="auto">
  <name>Task 3: Suite Feature de aceite — contrato + janela + quarentena</name>
  <files>tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php</files>
  <action>
    Criar `tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php` (namespace `Tests\\Feature\\Phase42`, uses RefreshDatabase + Http::fake + Carbon::setTestNow) com 7 tests:

    - T1 fetchCampaigns_retorna_normalizado_com_3_chaves: Http::fake da rota `advertising/advertisers/{id}/product_ads/campaigns` retornando 2 campanhas com `{id, name, status}` — assertSee `campaign_id`, `campaign_name`, `campaign_status` em cada item do retorno
    - T2 fetchAdgroupsMetrics_retorna_contrato_completo_22_chaves: Http::fake de ambos `listCampaigns` (com 1 campanha id=C1, name=Campanha A, status=active) e `product_ads/ads` (com 1 adgroup retornando campaign_id=C1) — verifica que o adgroup retornado tem TODAS as 22 chaves do contrato §3 (incluindo campaign_name='Campanha A', campaign_status='active', organic_amount=null, organic_units=null, raw=array)
    - T3 fetchAdgroupsMetrics_resolve_campaign_name_via_merge: Http::fake `listCampaigns` retorna `{id=C1, name=Campanha A}` + ads retorna `{id=AD1, campaign_id=C1, ...}` — assertEquals `'Campanha A'` no `campaign_name` do adgroup
    - T4 fetchAdgroupsMetrics_fail_open_em_listCampaigns_quebrado: Http::fake `listCampaigns` retorna 500 + ads retorna 1 adgroup — adgroup ainda flui com `campaign_name=null` (fail-open documentado)
    - T5 janela_30d_fechada_quando_reference_date_25_06_2026: Carbon::setTestNow('2026-06-25 12:00:00') + criar Company com sugadorConfig (dias_analise=30) + mock SugadoresAdsProvider via app->instance retornando array vazio em fetchAdgroupsMetrics — espionar argumentos: from=`Carbon('2026-05-27 00:00:00')`, to=`Carbon('2026-06-24 00:00:00')`
    - T6 quarentena_pula_adgroup_em_campanha_SGI: Http::fake — listCampaigns retorna `{id=C1, name='SGI - Lentes', status=active}`; ads retorna `{id=AD1, campaign_id=C1, metrics={cost=100, units_quantity=0, clicks=10}}`; rodar `analyzeCompany($company, '2026-06-25', dryRun=false)` — `Sugador::count()` permanece 0 (adgroup skipado pelo `shouldSkipCampaign`)
    - T7 quarentena_pula_adgroup_em_campanha_paused: listCampaigns retorna `{id=C1, name='Campanha Normal', status='paused'}` + ads com 1 adgroup que bateria criterio gasto_sem_venda — `Sugador::count() === 0` apos analyze

    Para os tests 5/6/7, usar mlToken active na Company (criar via factory). Para T5, `$this->app->instance(MercadoLivreSugadoresProvider::class, $mock)` + Mockery shouldReceive('fetchAdgroupsMetrics')->once()->with(Mockery::any(), Mockery::on(fn ($from) => $from->toDateString() === '2026-05-27'), Mockery::on(fn ($to) => $to->toDateString() === '2026-06-24'))->andReturn([]).

    Para T6/T7, usar Http::fake real (sem mockar provider — eh teste end-to-end do path ML).

    Cada test `#[\\PHPUnit\\Framework\\Attributes\\Test]`. Helpers `actingAsAdmin`, `makeCompanyWithMlToken($status='active')`, `httpFakeMlEndpoints(array $campaignsResp, array $adsResp)`. `tearDown` Carbon::setTestNow() + Mockery::close().
  </action>
  <read_first>
    - tests/Feature/Phase41/MercadoLivreAdsServiceBackoffTest.php (padrao Http::fake)
    - tests/Feature/Phase39/MercadoLivreSugadoresProviderTest.php (padrao especulativo do provider — confirmar como Http::fake escreve respostas ML)
    - app/Services/Sugadores/MercadoLivreSugadoresProvider.php (apos Task 1)
    - app/Services/SugadorAnalysisService.php (apos Task 2)
  </read_first>
  <verify>
    <automated>php artisan test tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 7 tests GREEN
    - `grep -c "#\\[.*Test\\]" tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php` retorna 7
    - Suite Phase42 acumulada >= 21 verdes (9 Plan 42-01 + 5 Plan 42-02 + 7 deste)
    - Suite Phase39/40/41 permanece verde — sem regressao
    - Suite Sugador acumulada permanece verde
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ML API → Provider | payload ML pode omitir campaign_status; provider deve normalizar para null |
| Provider → SugadorAnalysisService | contrato §3 eh acordo de interface — qualquer chave ausente quebra evaluator |
| listCampaigns chamada extra | ML API rate-limited; chamada redundante pode estourar quota |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03-01 | Denial of service | listCampaigns chamada por adgroups + por buildCampaignsInfoFromProvider geram 2 calls duplicados por analise | accept | MercadoLivreAdsService (Phase 41-02) tem cache de 10min por advertiser_id+from+to. Validar pos-deploy. Risk score baixo — uma duplicacao cabe no quota |
| T-42-03-02 | Tampering | adgroup com campaign_id falso/null escapa quarentena por nao bater no lookup | accept | shouldSkipCampaign tem `if (!$info) return false; // fail-open` — comportamento documentado. Quarentena por nome eh do briefing §12; sem campaign_id, nao tem como pular. Cobertura via T6/T7 (caminho positivo) |
| T-42-03-03 | Information disclosure | Log pode vazar nome de campanha do cliente em listCampaigns falha | mitigate | Log::warning ja existente cita apenas `company_id` (linha 147 do provider). Padrao mantido — nao incluir nome no novo log |
| T-42-03-04 | Tampering | dias_analise editavel pelo usuario gera janela > 30d | accept | UI Config.jsx ja limita 7-90 (linha 222) + controller valida `integer|min:7|max:90`. Briefing §4 menciona "salvo se a tela permitir explicitamente outro intervalo" — comportamento de override permitido conscientemente |
| T-42-03-SC | Tampering | npm/composer installs | accept | Esta phase NAO instala packages |
</threat_model>

<verification>
- 21+ tests GREEN acumulados em Phase42 (9+5+7)
- Provider entrega contrato §3 completo: `grep -c "'campaign_name'" provider` >= 2, `grep -c "'organic_amount'" provider` >= 2
- Service ja gera janela 30d (validado por T5 do test)
- Quarentena ML funciona (validado por T6 + T7)
- Zero regressao em Phase 38/39/40/41 + suite Sugador
</verification>

<success_criteria>
1. REQ-42-01 (normalizer contrato §3) ATENDIDO — todos os 22 campos do contrato presentes nos retornos do provider
2. REQ-42-03 (janela 30d fechada) ATENDIDO — calculo periodoFim/periodoInicio validado por T5
3. REQ-42-05 (quarentena SGI) ATENDIDO — adgroups em campanha SGI/Sugador/paused/closed/ended skipados (T6+T7)
4. Provider fail-open em listCampaigns broken (T4) — degradacao controlada
5. Zero regressao em Phase 38/39/40/41/Sugador
</success_criteria>

<output>
Criar `.planning/phases/42-sugadores-api-ml/42-03-SUMMARY.md` listando mapeamento chave-a-chave do contrato + status dos 7 tests + decisoes (cache de listCampaigns Phase 41-02 reaproveitado).
</output>
