---
phase: 42-sugadores-api-ml
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - app/Http/Controllers/SugadorConfigController.php
  - resources/js/Pages/Sugadores/Config.jsx
  - tests/Feature/Phase42/SugadorConfigCpcMinimoCliquesUiTest.php
autonomous: true
requirements: [REQ-42-04]

must_haves:
  truths:
    - "Pagina `/sugadores/configs/{company}` renderiza Inertia component `Sugadores/Config` recebendo prop `config.cpc_minimo_cliques` (default null)"
    - "Formulario tem campo numerico inteiro `cpc_minimo_cliques` ao lado de `cpc_maximo`, com label 'Cliques minimos para validar CPC' (conforme briefing §8 Opcao B)"
    - "Campo aceita vazio (null = nao gate) e inteiro positivo (>= 0)"
    - "Submit PUT /companies/{company}/sugador-config valida `cpc_minimo_cliques` como `nullable|integer|min:0` (rejeita string nao-numerico e negativo)"
    - "Valor `cpc_minimo_cliques` persiste em `sugador_configs` apos save (Eloquent fillable + cast integer Plan 42-01)"
    - "Hint (texto auxiliar) explicita pra o usuario: 'Quando preenchido, CPC alto so eh flagado se o anuncio tiver pelo menos X cliques no periodo'"
    - "Campo NAO tem toggle E/OU (eh modificador do criterio cpc_alto existente, nao criterio novo)"
  artifacts:
    - path: "app/Http/Controllers/SugadorConfigController.php"
      provides: "Validacao + persistencia de cpc_minimo_cliques"
    - path: "resources/js/Pages/Sugadores/Config.jsx"
      provides: "Campo + label + hint no card 'Criterios de deteccao'"
    - path: "tests/Feature/Phase42/SugadorConfigCpcMinimoCliquesUiTest.php"
      provides: "Suite Feature cobrindo prop, validacao, persistencia"
  key_links:
    - from: "Sugadores/Config.jsx"
      to: "useForm.data.cpc_minimo_cliques"
      via: "useForm initial state + setData"
      pattern: "cpc_minimo_cliques"
    - from: "PUT /companies/{company}/sugador-config"
      to: "SugadorConfig::updateOrCreate"
      via: "validate + fill"
      pattern: "cpc_minimo_cliques.*nullable.*integer"
    - from: "SugadorConfigController::show"
      to: "Inertia prop config.cpc_minimo_cliques"
      via: "Eloquent serialization"
      pattern: "cpc_minimo_cliques"
---

<objective>
Adiciona o campo `cpc_minimo_cliques` no formulario `/sugadores/configs/{company}` (Sugadores/Config.jsx) + validacao no SugadorConfigController. Fecha o lado UI do REQ-42-04 (Plan 42-01 fez backend). Sem refator visual — encaixa o campo entre `cpc_maximo` e o proximo criterio, mantendo o layout grid-cols-2 existente.

Purpose: permitir ao analista configurar a regra "CPC > R$ 4 com pelo menos 5 cliques sem venda" via UI sem mexer no banco direto. Mantem operacao do analista no MESMO fluxo de hoje (`/sugadores/configs/{company}`) — zero tela nova.

Output: edit Controller (1 regra de validacao + 1 chave no return) + edit Config.jsx (1 sub-campo no card de criterios + texto explicativo) + 1 suite Feature de 5 tests cobrindo render + validacao + persistencia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-sugadores-api-ml/42-CONTEXT.md
@.planning/phases/42-sugadores-api-ml/42-01-PLAN.md
@app/Http/Controllers/SugadorConfigController.php
@resources/js/Pages/Sugadores/Config.jsx
@app/Models/SugadorConfig.php

<interfaces>
<!-- Estado atual do SugadorConfigController e Config.jsx -->

From app/Http/Controllers/SugadorConfigController.php (update):
```php
$data = $request->validate([
    'dias_analise'                    => 'required|integer|min:7|max:90',
    'gasto_minimo_sem_venda'          => 'nullable|numeric|min:0',
    'gasto_minimo_logic'              => 'nullable|in:required,optional',
    'cpc_maximo'                      => 'nullable|numeric|min:0',
    'cpc_maximo_logic'                => 'nullable|in:required,optional',
    // ...
]);
```

From resources/js/Pages/Sugadores/Config.jsx (useForm + CriterioField CPC):
```jsx
const { data, setData, put, processing, errors } = useForm({
    cpc_maximo: config?.cpc_maximo ?? null,
    cpc_maximo_logic: config?.cpc_maximo_logic ?? 'optional',
    // ...
});

<CriterioField
    icon={DollarSign}
    label="CPC máximo (sem venda)"
    hint="Se o CPC médio passar deste valor E não houver venda."
    logic={data.cpc_maximo_logic}
    // ...
>
    <NumberInput value={data.cpc_maximo} ... />
</CriterioField>
```

Comportamento esperado (per D-01):
- `cpc_minimo_cliques` eh um "modificador opcional" do criterio cpc_alto, NAO criterio novo
- UI: subcampo dentro do mesmo CriterioField OU campo separado adjacente com Field (sem LogicToggle)
- Hint: orienta que vazio = nao gate; numero positivo = gate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validacao + props no SugadorConfigController</name>
  <files>app/Http/Controllers/SugadorConfigController.php</files>
  <action>
    Em `SugadorConfigController::update`:
    - Adicionar regra `'cpc_minimo_cliques' => 'nullable|integer|min:0'` no array de `$request->validate` imediatamente apos `'cpc_maximo_logic'`.
    - NAO precisa adicionar em `foreach` dos _logic defaults (eh integer, nao enum).

    Em `SugadorConfigController::show`:
    - NAO precisa alterar — `$config` ja vem do Eloquent com fillable atualizado (Plan 42-01) e a serializacao automatica do Inertia inclui o novo campo. Se houver `$company->sugadorConfig` null, o fallback `new SugadorConfig(array_merge(['company_id' => $company->id], SugadorConfig::DEFAULTS))` ja contempla `cpc_minimo_cliques => null` (DEFAULTS atualizado em Plan 42-01).

    Adicionar comentario pt-BR acima da nova regra: `// Phase 42 D-01: gate opcional de cliques minimos para validar CPC alto (briefing §8 Opcao B)`.
  </action>
  <read_first>
    - app/Http/Controllers/SugadorConfigController.php (estado atual completo)
    - app/Models/SugadorConfig.php (apos Plan 42-01 — confirmar fillable inclui cpc_minimo_cliques)
  </read_first>
  <verify>
    <automated>grep -c "'cpc_minimo_cliques' => 'nullable\\|integer\\|min:0'" app/Http/Controllers/SugadorConfigController.php</automated>
  </verify>
  <done>
    - Regra de validacao adicionada apos cpc_maximo_logic
    - Posicao: imediatamente apos `'cpc_maximo_logic'` no array (alinhamento visual com restante)
    - Comentario pt-BR de rastreabilidade D-01 presente
  </done>
</task>

<task type="auto">
  <name>Task 2: Campo cpc_minimo_cliques no formulario Sugadores/Config.jsx</name>
  <files>resources/js/Pages/Sugadores/Config.jsx</files>
  <action>
    Em `resources/js/Pages/Sugadores/Config.jsx`:

    1. **useForm initial state**: adicionar linha `cpc_minimo_cliques: config?.cpc_minimo_cliques ?? null,` IMEDIATAMENTE apos `cpc_maximo_logic`. Manter alinhamento de colunas com `:` (estilo existente).

    2. **Card "Criterios de deteccao"**: dentro do `grid grid-cols-1 md:grid-cols-2 gap-4`, manter o `CriterioField` do CPC maximo como esta (com LogicToggle). Adicionar um `Field` simples (sem LogicToggle — D-01 deixa claro que eh modificador, nao criterio novo) NO MESMO grid, ocupando 1 celula adjacente, com:
       - icon: `MousePointer` (consistente com criterio de cliques)
       - label: `"Cliques minimos para validar CPC"`
       - hint: `"Vazio = sem gate. Quando preenchido, CPC alto so eh flagado se o anuncio tiver pelo menos X cliques no periodo (briefing Opcao B)."`
       - `NumberInput` com `step="1"`, `placeholder="ex: 5"`, `value={data.cpc_minimo_cliques}`, `onChange={v => setData('cpc_minimo_cliques', v ? parseInt(v, 10) : null)}`, `error={errors.cpc_minimo_cliques}`
       - exibir `errors.cpc_minimo_cliques` abaixo em vermelho (mesmo padrao do `dias_analise`)

    3. NAO adicionar import novo de lucide-react — `MousePointer` ja eh importado (linha 6).

    4. Posicao no grid: imediatamente apos o CriterioField do `cpc_maximo` (ordem visual: CPC maximo → Cliques minimos para validar CPC → ACOS → Cliques sem conversao).

    Texto explicativo pt-BR (briefing §8): label e hint identicos ao spec.
  </action>
  <read_first>
    - resources/js/Pages/Sugadores/Config.jsx (linhas 131-316 — useForm + card de criterios)
  </read_first>
  <verify>
    <automated>grep -c "cpc_minimo_cliques" resources/js/Pages/Sugadores/Config.jsx</automated>
  </verify>
  <done>
    - `grep -c "cpc_minimo_cliques" resources/js/Pages/Sugadores/Config.jsx` retorna >= 3 (useForm + value + onChange)
    - `grep -c "Cliques minimos para validar CPC" resources/js/Pages/Sugadores/Config.jsx` retorna 1
    - Campo aparece no grid de criterios usando `Field` (sem LogicToggle)
    - Hint cita explicitamente "briefing Opcao B" pra rastreabilidade D-01
    - npm run build (no consolidate-wave do orquestrador) sem erros JSX
  </done>
</task>

<task type="auto">
  <name>Task 3: Suite Feature cobrindo render + validacao + persistencia</name>
  <files>tests/Feature/Phase42/SugadorConfigCpcMinimoCliquesUiTest.php</files>
  <action>
    Criar `tests/Feature/Phase42/SugadorConfigCpcMinimoCliquesUiTest.php` (namespace `Tests\\Feature\\Phase42`, uses RefreshDatabase + AssertableInertia) com 5 tests:

    - T1 show_inclui_cpc_minimo_cliques_no_payload: GET `/sugadores/configs/{company}` (acting as admin) — Inertia component `Sugadores/Config` + `->has('config.cpc_minimo_cliques')` (chave existe; valor null aceito)
    - T2 show_retorna_default_null_quando_config_nao_existe: company sem `sugadorConfig` — `config.cpc_minimo_cliques === null` no payload
    - T3 update_persiste_valor_inteiro: PUT `/companies/{company}/sugador-config` com payload incluindo `cpc_minimo_cliques: 5` (+ outros campos obrigatorios) — `sugadorConfig->cpc_minimo_cliques === 5` apos reload
    - T4 update_persiste_null_quando_campo_vazio: PUT com `cpc_minimo_cliques: null` — apos reload, valor eh null (`assertNull`)
    - T5 update_rejeita_string_nao_numerica_e_negativo: PUT com `cpc_minimo_cliques: "abc"` — `assertSessionHasErrors(['cpc_minimo_cliques'])`; PUT com `-1` — mesmo assertion

    Helper `validPayload(array $overrides = [])` retorna array base com todos os required (`dias_analise=30`, `pct_anuncios_para_flag_campanha=50`, `incluir_campanhas=false`, `incluir_anuncios=true`, `ativo=true`) + merge com overrides. Helper `actingAsAdmin()` cria User via factory com role=admin.

    Cada test usa `#[\\PHPUnit\\Framework\\Attributes\\Test]`. Sem Mockery — eh teste de integracao real do controller.
  </action>
  <read_first>
    - tests/Feature/Phase14FechamentoUiTest.php (padrao AssertableInertia + actingAs)
    - tests/Feature/Phase41/SugadoresMlOnboardingControllerTest.php (padrao admin gating + assertSessionHasErrors)
    - app/Http/Controllers/SugadorConfigController.php (apos Task 1)
  </read_first>
  <verify>
    <automated>php artisan test tests/Feature/Phase42/SugadorConfigCpcMinimoCliquesUiTest.php 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 5 tests GREEN no SugadorConfigCpcMinimoCliquesUiTest
    - Suite Phase42 acumulada >= 14 verdes (9 Plan 42-01 + 5 deste)
    - Suite Sugador acumulada permanece verde (zero regressao)
    - `grep -c "#\\[.*Test\\]" tests/Feature/Phase42/SugadorConfigCpcMinimoCliquesUiTest.php` retorna 5
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → controller | usuario pode mandar string nao-numerico no campo cpc_minimo_cliques |
| Inertia serialization → React | config sem cpc_minimo_cliques (rows antigos) precisa renderizar como null sem crashar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Tampering | usuario envia `cpc_minimo_cliques="-1"` ou `"abc"` no PUT | mitigate | Validate `nullable|integer|min:0` no controller. T5 do test valida rejeicao explicita |
| T-42-02-02 | Information disclosure | rows antigas sem o campo geram payload Inertia com chave ausente, crashando React `data.cpc_minimo_cliques` | mitigate | useForm `config?.cpc_minimo_cliques ?? null` cobre ausencia E null. T2 valida. |
| T-42-02-03 | Elevation of privilege | usuario nao-admin acessa /sugadores/configs/{company} | accept | Rota ja protegida por `Gate::authorize('manage', Sugador::class)` (linha 20 do controller). Cobertura existente — nao precisa retestar nesta plan. |
| T-42-02-SC | Tampering | npm/composer installs | accept | Esta phase NAO instala packages |
</threat_model>

<verification>
- Apos consolidate-wave: `npm run build` sem warning de JSX
- `php artisan test --filter=Phase42` retorna >= 14 verdes (9 Plan 42-01 + 5 deste)
- `php artisan test --filter=Sugador` permanece verde
- Visualmente (post-deploy, smoke humano deferido): abrir `/sugadores/configs/{any-company}`, ver o campo "Cliques minimos para validar CPC" no card de criterios, salvar `5`, voltar e ver `5` no campo
</verification>

<success_criteria>
1. Campo "Cliques minimos para validar CPC" aparece no formulario, sem LogicToggle, ao lado de CPC maximo
2. Validacao server-side rejeita string nao-numerica e negativo (HTTP 422)
3. Persistencia funciona (5/5 tests verdes); regressao 0 na suite Sugador
4. REQ-42-04 FECHADO (backend Plan 42-01 + frontend Plan 42-02)
</success_criteria>

<output>
Criar `.planning/phases/42-sugadores-api-ml/42-02-SUMMARY.md` listando arquivos alterados + posicao do campo no formulario + status dos tests.
</output>
