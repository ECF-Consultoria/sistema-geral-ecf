---
phase: 42-sugadores-api-ml
plan: 04
type: execute
wave: 3
depends_on: ["42-01", "42-03"]
files_modified:
  - app/Console/Commands/AnalyzeSugadores.php
  - app/Services/Sugadores/SugadoresAdsProviderFactory.php
  - app/Services/SugadorAnalysisService.php
  - app/Http/Controllers/SugadorController.php
  - tests/Feature/Phase42/CutOverMlPrimaryTest.php
autonomous: true
requirements: [REQ-42-02, REQ-42-06, REQ-42-08]

must_haves:
  truths:
    - "Guard `ml_primary` em AnalyzeSugadores command REMOVIDO — `sugadores:analyze --company=298 --provider=ml` SEM `--dry-run` agora grava em `sugadores` (cut-over Phase 42)"
    - "`SugadoresAdsProviderFactory::for($company)` quando empresa tem `mlToken status=active` retorna MercadoLivreSugadoresProvider POR DEFAULT (sem precisar forceProvider)"
    - "Empresa com SOMENTE `adman_account_id` (sem mlToken active) continua roteada para AdmanSugadoresProvider (fallback preservado)"
    - "Empresa ByMobille - Teste (#298, sem adman_account_id, com mlToken active) eh roteada para ML provider — comando `sugadores:analyze --company=298` (sem flag) grava sugadores via ML"
    - "Idempotencia preservada: chave estavel `company_id|reference_date|tipo|campaign_id|adgroup_id` (linha 152 do service ja contempla); re-rodar o mesmo dia atualiza metricas mas NAO duplica linhas"
    - "Status travado (em_acao/resolvido/ignorado/movido/auto_resolvido) eh PRESERVADO em re-analise ML conforme D-06 (linha 406 do service via STATUS_TRAVADOS — comportamento existente reutilizado)"
    - "`SugadorController::analyzeCompany` (HTTP POST) NAO requer mais `adman_account_id` — passa se empresa tem `adman_account_id` OU `ml_store_id` + mlToken active (logica `is_ml_driven` da Company)"
  artifacts:
    - path: "app/Console/Commands/AnalyzeSugadores.php"
      provides: "Guard ml_primary removido; description atualizada"
    - path: "app/Services/Sugadores/SugadoresAdsProviderFactory.php"
      provides: "Prioridade INVERTIDA: ML eh preferido quando ambos suportam (cut-over Phase 42)"
    - path: "app/Services/SugadorAnalysisService.php"
      provides: "Comentario rastreabilidade D-06 (status travados preservados em ML) + comentario D-05 (fluxo ML→service→sugadores)"
    - path: "app/Http/Controllers/SugadorController.php"
      provides: "analyzeCompany aceita empresas ML-driven (sem adman_account_id)"
    - path: "tests/Feature/Phase42/CutOverMlPrimaryTest.php"
      provides: "Suite Feature 8 tests: cut-over factory, command sem guard, idempotencia, status travado, ByMobille E2E"
  key_links:
    - from: "AnalyzeSugadores command"
      to: "SugadorAnalysisService::analyzeCompany"
      via: "remove guard ml_primary"
      pattern: "Modo ml_primary"
    - from: "SugadoresAdsProviderFactory::for"
      to: "MercadoLivreSugadoresProvider (preferido)"
      via: "auto-detection invertida"
      pattern: "mlProvider->supports"
    - from: "SugadorController::analyzeCompany"
      to: "Empresa ML-driven sem adman_account_id"
      via: "accept ml_store_id"
      pattern: "is_ml_driven|ml_store_id"
---

<objective>
Faz o cut-over real do path Sugadores para ML como provider PRIMARIO. Tres mudancas atomicas:

1. **Factory**: prioridade auto-detection passa a ser `ML > Adman` (era Adman > ML como compat ate Phase 42). Empresas com mlToken active rodam ML; empresas sem mlToken active mas com adman_account_id continuam Adman (fallback).
2. **Command**: remove guard `ml_primary` que abortava `--provider=ml` sem `--dry-run` (Phase 39-05). Apos cut-over, gravar via ML eh comportamento esperado.
3. **Controller HTTP**: aceita empresas ML-driven sem adman_account_id na rota `POST /sugadores/companies/{company}/analyze` (atualmente exige adman_account_id, bloqueando ByMobille).

Tambem adiciona comentarios rastreabilidade D-05/D-06 no service (sem mudanca funcional — comportamento ja existe via STATUS_TRAVADOS + idempotencia).

Purpose: ByMobille - Teste (#298, piloto D-08) consegue rodar analise via ML pela primeira vez. Empresas existentes ML+Adman migram pra ML automaticamente. Adman vira fallback so quando nao tem mlToken active.

Output: edit Factory (~3 linhas — inversao supports) + edit Command (remover bloco do guard) + edit Service (comentarios) + edit Controller (gate Adman ou ML) + suite Feature 8 tests cobrindo cut-over E2E.

**Risco controlado:** Esta plan NAO bloqueia Adman para empresas que ja rodam por Adman — apenas inverte preferencia quando AMBOS providers suportam. Empresas ML-only (ByMobille) destravam. Phase 43 fara a remocao completa do Adman.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-sugadores-api-ml/42-CONTEXT.md
@.planning/phases/42-sugadores-api-ml/42-01-PLAN.md
@.planning/phases/42-sugadores-api-ml/42-03-PLAN.md
@.planning/phases/39-provider-pattern-sugadores-ml/39-05-SUMMARY.md
@app/Console/Commands/AnalyzeSugadores.php
@app/Services/Sugadores/SugadoresAdsProviderFactory.php
@app/Services/SugadorAnalysisService.php
@app/Http/Controllers/SugadorController.php

<interfaces>
<!-- Estado atual factory (prioridade Adman > ML pre-Phase 42) -->

From app/Services/Sugadores/SugadoresAdsProviderFactory.php:
```php
public function for(Company $company, ?string $forceName = null): SugadoresAdsProvider
{
    if ($forceName === 'adman') return $this->admanProvider;
    if ($forceName === 'ml')    return $this->mlProvider;

    // Auto-detecao: Adman tem prioridade quando ambos suportam (compat ate Phase 42)
    if ($this->admanProvider->supports($company)) return $this->admanProvider;
    if ($this->mlProvider->supports($company))    return $this->mlProvider;

    throw new \\RuntimeException("Empresa {$company->id} sem provider compativel ...");
}
```

From app/Console/Commands/AnalyzeSugadores.php (guard ml_primary):
```php
if ($provider === 'ml' && !$dryRun) {
    $this->error('Modo ml_primary so disponivel em Phase 42 — use --dry-run para testar leitura sem gravacao.');
    return self::FAILURE;
}
```

From app/Http/Controllers/SugadorController.php (analyzeCompany linha 339-353):
```php
public function analyzeCompany(Request $request, Company $company)
{
    Gate::authorize('manage', Sugador::class);
    if (!$company->adman_account_id) {
        return back()->with('warning', 'Empresa sem adman_account_id — analise pulada.');
    }
    AnalyzeCompanySugadoresJob::dispatch($company);
    return back()->with('success', "Analise enfileirada para {$company->name}. ...");
}
```

From app/Models/Company.php (is_ml_driven accessor):
```php
// Linha 92: retorna true se mlToken existe e status=active
return optional($this->mlToken)->status === 'active';
```

D-06 (briefing §13): status travados preservados via STATUS_TRAVADOS — linha 406 do service.
D-05 (briefing §10.2): fluxo API ML → normalizer → SugadorAnalysisService → tabela sugadores → /sugadores. Sem tela paralela.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut-over factory + remove guard command</name>
  <files>app/Services/Sugadores/SugadoresAdsProviderFactory.php, app/Console/Commands/AnalyzeSugadores.php</files>
  <action>
    **A) SugadoresAdsProviderFactory::for**

    Inverter ordem de auto-detection. Substituir:
    ```
    if ($this->admanProvider->supports($company)) return $this->admanProvider;
    if ($this->mlProvider->supports($company))    return $this->mlProvider;
    ```
    Por:
    ```
    // Phase 42 D-05: cut-over para ML como provider primario.
    // Auto-detection prefere ML quando empresa tem mlToken active; Adman vira fallback.
    // Plano de remocao completa do Adman (Phase 43) so inicia apos paridade >= 95% por 7d.
    if ($this->mlProvider->supports($company))    return $this->mlProvider;
    if ($this->admanProvider->supports($company)) return $this->admanProvider;
    ```

    Mensagem da RuntimeException permanece (empresa sem ambos).

    **B) AnalyzeSugadores command**

    Remover INTEIRAMENTE o bloco do guard ml_primary (linhas 35-41 do estado atual):
    ```
    if ($provider === 'ml' && !$dryRun) {
        $this->error('Modo ml_primary so disponivel em Phase 42 — use --dry-run para testar leitura sem gravacao.');
        return self::FAILURE;
    }
    ```

    Atualizar a description do `--provider` no signature (linha 16):
    Antes: `Default = capability detection. ml sem --dry-run aborta (Phase 39 — gravacao ML disponivel em Phase 42).`
    Depois: `Default = capability detection (ML preferido apos Phase 42).`

    Atualizar tambem o `$description` da classe (linha 18):
    Antes: `Detecta adgroups (e opcionalmente campanhas) "sugadores" que drenam investimento sem retorno via provider de anuncios (Adman ou Mercado Livre)`
    Depois: `Detecta adgroups e campanhas "sugadores" que drenam investimento sem retorno — usa Mercado Livre por padrao (Phase 42), Adman como fallback (legacy)`

    NAO alterar validacao whitelist `in_array($provider, ['adman', 'ml'], true)`. NAO alterar warn `--provider sem --company`.

    Adicionar comentario na linha onde estava o guard: `// Phase 42 D-05: guard ml_primary removido — cut-over autorizado.`
  </action>
  <read_first>
    - app/Services/Sugadores/SugadoresAdsProviderFactory.php (estado completo)
    - app/Console/Commands/AnalyzeSugadores.php (linhas 1-60)
    - .planning/phases/39-provider-pattern-sugadores-ml/39-05-SUMMARY.md (rationale original do guard)
  </read_first>
  <verify>
    <automated>grep -c "mlProvider->supports\\(\\$company\\)" app/Services/Sugadores/SugadoresAdsProviderFactory.php</automated>
  </verify>
  <done>
    - Factory: 1a linha do auto-detection eh `mlProvider->supports` (ordem invertida)
    - Command: `grep -c "Modo ml_primary so disponivel em Phase 42" app/Console/Commands/AnalyzeSugadores.php` retorna 0
    - Description do command menciona "Mercado Livre por padrao (Phase 42)"
    - Comentarios pt-BR D-05 presentes em ambos arquivos
    - Sintaxe valida: `php -l` ambos retornam `No syntax errors detected`
  </done>
</task>

<task type="auto">
  <name>Task 2: SugadorController aceita empresas ML-driven</name>
  <files>app/Http/Controllers/SugadorController.php</files>
  <action>
    Em `SugadorController::analyzeCompany` (linha 339-353):

    Substituir o gate `if (!$company->adman_account_id)` por gate que aceita Adman OU ML-driven:

    ```
    // Phase 42 D-05: aceita empresas ML-driven (mlToken active) sem adman_account_id.
    // ByMobille - Teste (#298) e primeira empresa ML-only — destravado neste cut-over.
    // Mantemos rejeicao para empresas sem nenhum provider compativel.
    $company->loadMissing('mlToken');
    $hasAdman = !empty($company->adman_account_id);
    $hasMl    = optional($company->mlToken)->status === 'active';
    if (!$hasAdman && !$hasMl) {
        return back()->with('warning', "Empresa {$company->name} sem adman_account_id nem mlToken ativo — analise pulada.");
    }
    ```

    Em `SugadorController::analyzeAll` (linha 362-383), expandir a query:
    Antes:
    ```
    $companies = Company::where('active', true)
        ->whereNotNull('adman_account_id')
        ->where('adman_account_id', '!=', '')
        ->where(...)
        ->get();
    ```
    Depois:
    ```
    // Phase 42 D-05: inclui empresas ML-driven (mlToken active) alem de Adman.
    $companies = Company::where('active', true)
        ->where(function ($q) {
            $q->where(function ($q2) {
                $q2->whereNotNull('adman_account_id')->where('adman_account_id', '!=', '');
            })->orWhereHas('mlToken', fn($q2) => $q2->where('status', 'active'));
        })
        ->where(function ($q) {
            $q->whereHas('sugadorConfig', fn($q) => $q->where('ativo', true))
              ->orWhereDoesntHave('sugadorConfig');
        })
        ->get();
    ```

    NAO alterar `sgiCampaigns`, `mlbs`, ou outros metodos do controller — estao FORA DE ESCOPO (sao detalhes Adman-specific que serao tratados em Phase 43 ou em fix incremental).

    Para `analyzeCompany` HTTP, `AnalyzeCompanySugadoresJob::dispatch($company)` continua sendo o entrypoint. O job ja chama `analyzeCompany(...)` do service que via factory escolhe ML para empresas ML-driven.
  </action>
  <read_first>
    - app/Http/Controllers/SugadorController.php (linhas 335-385)
    - app/Models/Company.php (linhas 86-95 — is_ml_driven + mlToken relation)
    - app/Jobs/AnalyzeCompanySugadoresJob.php (entrypoint do dispatch)
  </read_first>
  <verify>
    <automated>grep -c "mlToken" app/Http/Controllers/SugadorController.php</automated>
  </verify>
  <done>
    - `grep -c "Phase 42 D-05" app/Http/Controllers/SugadorController.php` retorna >= 2 (analyzeCompany + analyzeAll)
    - `grep -c "orWhereHas\\('mlToken'" app/Http/Controllers/SugadorController.php` retorna 1
    - analyzeCompany aceita company com mlToken active sem adman_account_id (validado por T8 do Plan 42-04)
    - Outros metodos (sgiCampaigns/mlbs/etc) inalterados — `git diff --stat` mostra mudancas localizadas
  </done>
</task>

<task type="auto">
  <name>Task 3: Comentarios D-05/D-06 no service + suite cut-over</name>
  <files>app/Services/SugadorAnalysisService.php, tests/Feature/Phase42/CutOverMlPrimaryTest.php</files>
  <action>
    **A) SugadorAnalysisService comentarios**

    Em `analyzeCompany`, ANTES do `Sugador::upsert(...)` (linha 316), adicionar comentario explicando idempotencia + D-05:
    ```
    // Phase 42 D-05 (briefing §10.2 + §13): fluxo final eh API ML → normalizer →
    // SugadorAnalysisService → tabela sugadores → /sugadores. Idempotencia por
    // chave estavel (company_id, reference_date, tipo, campaign_id, adgroup_id)
    // garante re-analise atualizar metricas sem duplicar linhas. Status travados
    // (D-06: em_acao/resolvido/ignorado/movido/auto_resolvido) sao preservados
    // em buildRow via STATUS_TRAVADOS — comportamento valido para ambos providers.
    ```

    Em `buildRow` (linha 402), ANTES do `$status = ...`, adicionar:
    ```
    // Phase 42 D-06: STATUS_TRAVADOS preservado — re-analise via ML NAO sobrescreve
    // status de sugador ja em em_acao/resolvido/ignorado/movido/auto_resolvido.
    // Metricas (cliques, cpc, acos, etc) e raw_data atualizam normalmente.
    ```

    NAO mudar logica.

    **B) Suite Feature `CutOverMlPrimaryTest`** (`tests/Feature/Phase42/CutOverMlPrimaryTest.php`, namespace Tests\\Feature\\Phase42, uses RefreshDatabase) — 8 tests:

    - T1 factory_prefere_ml_quando_ambos_supportam: Company com adman_account_id='123' E mlToken status='active' → factory.for() retorna instancia de `MercadoLivreSugadoresProvider`
    - T2 factory_fallback_adman_quando_ml_inativo: Company com adman_account_id='123' E mlToken status='expired' → factory.for() retorna `AdmanSugadoresProvider`
    - T3 factory_fallback_adman_sem_ml_token: Company com adman_account_id='123' SEM mlToken → AdmanSugadoresProvider
    - T4 factory_throws_quando_nenhum_suporta: Company sem adman_account_id E sem mlToken → RuntimeException
    - T5 command_aceita_provider_ml_sem_dry_run: `Artisan::call('sugadores:analyze', ['--company' => $company->id, '--provider' => 'ml'])` (sem --dry-run) — exit 0 (era 1 antes do cut-over); mockar `SugadorAnalysisService::analyzeCompany` para validar 4o arg='ml' E dryRun=false; provider apos cut-over deve gravar (Sugador::count() > 0 quando criterio bater)
    - T6 controller_analyzeCompany_aceita_company_ml_only: POST `/sugadores/companies/{ml_only_company}/analyze` — assertRedirect + session('success'); `AnalyzeCompanySugadoresJob` foi dispatchado (Queue::fake assertPushed)
    - T7 idempotencia_re_analise_atualiza_sem_duplicar: criar 1 sugador via 1a analise + re-rodar com mesma reference_date — `Sugador::count() === 1`; `updated_at` mudou; `created_at` NAO mudou
    - T8 status_travado_preservado_em_re_analise_ml: criar sugador (status=em_acao) via 1a analise; rodar 2a analise no mesmo dia — status permanece 'em_acao'; metricas atualizam

    Para T5/T7/T8, usar Http::fake completo (mockando listCampaigns + product_ads/ads) com dados que batem o criterio gasto_sem_venda. Para T8, simular acao manual entre as analises via `$sugador->update(['status' => 'em_acao'])`.

    Cada test `#[Test]`. Helpers `actingAsAdmin`, `makeCompany(['ml' => bool, 'adman' => bool])`, `httpFakeMlAds(array $adgroups)`. Carbon::setTestNow('2026-06-25 12:00:00').
  </action>
  <read_first>
    - tests/Feature/Phase39/AnalyzeSugadoresCommandTest.php (padrao Artisan::call + assertExitCode)
    - tests/Feature/Phase39/SugadoresAdsProviderFactoryTest.php (padrao Mockery factory)
    - tests/Feature/Phase42/AnalyzeCompanyMlWindowQuarantineTest.php (padrao Http::fake ML pos-Plan 42-03)
    - app/Jobs/AnalyzeCompanySugadoresJob.php
  </read_first>
  <verify>
    <automated>php artisan test tests/Feature/Phase42/CutOverMlPrimaryTest.php 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 8 tests GREEN
    - `grep -c "Phase 42 D-0[56]" app/Services/SugadorAnalysisService.php` >= 2
    - Suite Phase42 acumulada >= 29 (9 + 5 + 7 + 8)
    - Suite Sugador acumulada permanece verde (status travado preservado — T8)
    - Suite Phase39 permanece verde (T1-T4 da factory PASS por inversao, T5-T8 do command sao na Phase42)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| factory auto-detection → provider escolhido | empresa erroneamente classificada como ML por mlToken stale grava sugadores invalidos |
| command sem guard → DB | gravacao ML real, sem dry-run, sem rollback automatico |
| HTTP analyzeCompany → AnalyzeCompanySugadoresJob | empresa ML-only enfileirada pode falhar se MercadoLivreSugadoresProvider lancar excecao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04-01 | Tampering | mlToken expirado mas status ainda 'active' (token stale) gera ML escolhido + chamada 401 | mitigate | MercadoLivreService.Phase 20 ja tem refresh automatico (`refreshIfNeeded`). Se refresh falhar, provider retorna [] (fail-open). Sugadores nao sao criados — estado consistente |
| T-42-04-02 | Tampering | factory cut-over erra empresa que ainda nao foi onboardada via ML (mlToken active mas advertiser_id falha) | mitigate | MercadoLivreSugadoresProvider::resolveAdvertiserId retorna null → fetchAdgroupsMetrics retorna [] → analyzeCompany flui sem detalhes. Log do provider registra "advertiser nao encontrado". Operador detecta via UI Onboarding (Phase 41-05) |
| T-42-04-03 | Information disclosure | Comando sem guard pode gravar sugadores ML mass em prod por engano | accept | Operador admin executa o comando manualmente. Scheduler atual (routes/console.php:42) ja roda `sugadores:analyze` sem --provider — usa auto-detection (ML preferido). Estado pos-cut-over eh intencional |
| T-42-04-04 | Repudiation | re-analise apaga raw_data antigo (auditoria perdida) | mitigate | Sugador::upsert da linha 316 atualiza raw_data com payload novo. Histórico de status fica em SugadorAcao (auditavel via activity_log). Decisao consciente: raw_data eh fonte de snapshot, nao log |
| T-42-04-05 | Denial of service | Empresa com >100 adgroups+ML rate limit estoura quota durante analise | accept | Phase 41-02 entregou rate limiter `ml-api:{seller_id}` por seller + backoff exponencial. Pre-existente — esta plan nao introduz nova fonte de pressao |
| T-42-04-SC | Tampering | npm/composer installs | accept | Esta phase NAO instala packages |
</threat_model>

<verification>
- 29+ tests GREEN acumulados em Phase42 (9+5+7+8)
- Cut-over factory validado: T1-T4 cobrem matrizes ML/Adman
- Guard ml_primary removido: T5 valida sucesso ML sem --dry-run
- Controller aceita ML-only: T6 valida POST /sugadores/companies/{ml_only}/analyze
- Idempotencia preservada: T7
- Status travado preservado: T8
- Suite Sugador acumulada permanece verde
- Smoke real ByMobille (#298) destravado mas validacao em prod fica para post-deploy (briefing §14)
</verification>

<success_criteria>
1. REQ-42-02 (fluxo ML grava sugadores com idempotencia) ATENDIDO — T1, T5, T7
2. REQ-42-06 (status travado preservado em re-analise ML) ATENDIDO — T8
3. REQ-42-08 (ByMobille destravado para piloto) PARCIAL — controller aceita empresa ML-only (T6); smoke real em prod fica pos-deploy
4. Adman fallback preservado para empresas sem mlToken active — T2/T3
5. Zero regressao em Phase 38/39/40/41/Sugador
6. Phase 42 BACKEND COMPLETO (Plans 42-01..04). Restam UI cleanup (42-05) e suite acceptance E2E (42-06).
</success_criteria>

<output>
Criar `.planning/phases/42-sugadores-api-ml/42-04-SUMMARY.md` listando inversao da factory + linhas removidas do guard + mudancas do controller + status de 8 tests + matriz de cobertura cut-over.
</output>
