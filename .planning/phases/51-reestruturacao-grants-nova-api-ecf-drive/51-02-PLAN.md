---
phase: 51-reestruturacao-grants-nova-api-ecf-drive
plan: "02"
type: execute
wave: 2
depends_on:
  - 51-01
autonomous: true
requirements:
  - REQ-51-02
files_modified:
  - app/Services/EcfDriveService.php
  - app/Http/Controllers/GrantController.php
  - tests/Feature/Phase51/GrantsResumoTest.php
must_haves:
  truths:
    - "EcfDriveService::grantsResumo() retorna payload remoto cacheado por 300s (5min) — espelho de carteiraResumo"
    - "EcfDriveService::grantsDistribuicao('programa') retorna payload remoto cacheado por 3600s (1h) — espelho de carteiraBreakdown; qualquer outra dimensão lança InvalidArgumentException (locked como out-of-scope)"
    - "GrantController::index() tenta grantsResumo() em try/catch — sucesso popula stats.buckets + stats.divergencia + flag stats.source='remote'; falha loga warning [Grants] e cai para fallback local com stats.source='local'"
    - "Universo de no_grant respeita o critério locked: companies.active=true E (cust_id IS NOT NULL OR EXISTS ml_token ativo) E NOT EXISTS company_grant ativo"
    - "Buckets locais (fallback) computam contagens reais de expirando em 7/15/30/60/90d a partir de company_grants"
    - "RED commit separado do GREEN em cada bloco TDD"
  artifacts:
    - path: "app/Services/EcfDriveService.php"
      provides: "Dois métodos públicos novos: grantsResumo() e grantsDistribuicao(string $dimensao)"
      contains: "Cache::remember TTL=300s e TTL=3600s respectivamente; validação dimensao IN ['programa']"
    - path: "app/Http/Controllers/GrantController.php"
      provides: "index() expandido com consumo remoto + fallback gracioso + universo no_grant corrigido + buckets locais"
    - path: "tests/Feature/Phase51/GrantsResumoTest.php"
      provides: "Cobertura Http::fake do happy path remoto + fallback quando API erra + universo no_grant + buckets locais"
  key_links:
    - from: "GrantController::index()"
      to: "EcfDriveService::grantsResumo()"
      via: "constructor injection ou type-hint em index()"
      pattern: "try/catch \\Throwable + Log::warning('[Grants] ...') — PolosController.php:524-528"
    - from: "GrantController::index() universo no_grant"
      to: "companies + ml_tokens + company_grants"
      via: "WhereExists composto (cust_id OR ml_token ativo) AND NOT EXISTS grant ativo"
      pattern: "SQL locked em 51-CONTEXT.md linhas 74-82"
    - from: "EcfDriveService::grantsResumo/grantsDistribuicao"
      to: "Cache::remember + helper get('/grants/...')"
      via: "$this->cacheKey($path, $params) → 300s ou 3600s"
      pattern: "carteiraResumo linha 311 (TTL 300s) e carteiraBreakdown linha 367 (TTL 3600s)"
---

<objective>
Consumir os dois novos endpoints remotos `/grants/resumo` e `/grants/distribuicao` da API ECF Drive, com fallback gracioso quando offline, e corrigir o universo SQL de "Empresas sem grants" para alinhar com a regra locked pelo operador 2026-06-30 (cust_id OR OAuth ML ativo).

Esta wave entrega o backend de leitura. Wave 3 expõe os novos dados na UI (StatCards de buckets, card de divergência, badge "API offline", colunas opcionais).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-CONTEXT.md
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-RESEARCH.md
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: EcfDriveService — grantsResumo() + grantsDistribuicao() (RED → GREEN)</name>
  <files>
    - app/Services/EcfDriveService.php
    - tests/Feature/Phase51/GrantsResumoTest.php
  </files>
  <read_first>
    - app/Services/EcfDriveService.php (linhas 311-318) — `carteiraResumo()`: modelo de método sem parâmetros com Cache::remember TTL 300s
    - app/Services/EcfDriveService.php (linhas 367-380) — `carteiraBreakdown(string $dimensao, ?string $timMonthId)`: modelo de método com parâmetro + array_filter + Cache::remember TTL 3600s
    - app/Services/EcfDriveService.php (linhas 393-399) — `carteiraSegmentacao()`: modelo de validação com InvalidArgumentException para parâmetros inválidos
    - app/Services/EcfDriveService.php (linhas 557-595) — helpers privados `get()` e `cacheKey()`
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-RESEARCH.md §1 (linhas 41-74) — recomendação acionável com signatures finais
    - tests/Feature/Phase20/SyncGrantsFromEcfDriveTest.php (linhas 24-45) — pattern setUp() + fakeGrants() com `Http::fake([url => Http::response(...)])`
  </read_first>
  <behavior>
    Métodos novos em EcfDriveService (adicionar na seção "── Clientes ──" após linha 155, depois de acoesPendentes()):

    ```
    public function grantsResumo(): array
    {
        return Cache::remember(
            $this->cacheKey('/grants/resumo'),
            300,
            fn () => $this->get('/grants/resumo'),
        );
    }

    public function grantsDistribuicao(string $dimensao): array
    {
        $permitidas = ['programa']; // Phase 51 — outras dimensões fora de escopo (ML uniforme; expandir quando ML diversificar)
        if (! in_array($dimensao, $permitidas, true)) {
            throw new \InvalidArgumentException(
                "grantsDistribuicao: dimensão '{$dimensao}' não suportada nesta phase. Permitidas: " . implode(', ', $permitidas)
            );
        }
        $params = ['dimensao' => $dimensao];
        $path   = '/grants/distribuicao';
        return Cache::remember(
            $this->cacheKey($path, $params),
            3600,
            fn () => $this->get($path, $params),
        );
    }
    ```

    Comportamentos sob teste:
    - `grantsResumo()` chama GET /grants/resumo e retorna o payload decodificado
    - Cache hit: 2ª chamada não bate na API (Http::assertSentCount(1))
    - `grantsDistribuicao('programa')` chama GET /grants/distribuicao?dimensao=programa
    - `grantsDistribuicao('localidade')` lança InvalidArgumentException sem fazer chamada HTTP
    - HTTP 5xx → propaga \RuntimeException (helper get() já cobre)
  </behavior>
  <action>
    1. Criar diretório `tests/Feature/Phase51/` se não existir.
    2. Criar `tests/Feature/Phase51/GrantsResumoTest.php` com namespace `Tests\Feature\Phase51`, classe `GrantsResumoTest extends TestCase`, use `RefreshDatabase`. setUp() configura `services.ecf.base` e `services.ecf.key` igual ao pattern Phase 20 (linhas 24-33).
    3. Adicionar testes RED (todos devem falhar — métodos não existem):
       - `test_grants_resumo_chama_endpoint_e_retorna_payload`: `Http::fake(['*/grants/resumo' => Http::response($payload, 200)])`; invoca `app(EcfDriveService::class)->grantsResumo()`; assertSame ao payload; `Http::assertSentCount(1)`.
       - `test_grants_resumo_cache_evita_chamada_duplicada`: 2 invocações seguidas → `Http::assertSentCount(1)` (TTL 300s).
       - `test_grants_distribuicao_programa_chama_endpoint_com_query_param`: assertSent inspecionando que URL contém `dimensao=programa`.
       - `test_grants_distribuicao_dimensao_invalida_lanca_excecao_sem_http`: expectException InvalidArgumentException; `Http::assertSentCount(0)`.
       - `test_grants_resumo_propaga_runtime_exception_em_erro_http`: `Http::fake(['*/grants/resumo' => Http::response(['erro'=>'oops'], 500)])`; expectException(\RuntimeException::class); `Cache::clear()` antes para garantir não bater no cache.
    4. Rodar `php artisan test --filter=Phase51\\\\GrantsResumoTest` — DEVE FALHAR (métodos não existem). Confirmar RED.
    5. Commitar RED: `test(51-02): RED — testes para EcfDriveService::grantsResumo/grantsDistribuicao`
    6. Editar `app/Services/EcfDriveService.php`:
       - Adicionar os 2 métodos descritos em <behavior> após o método `acoesPendentes()` (após linha 155)
       - Manter docblocks pt-BR no mesmo estilo dos métodos existentes (referência `@see API-GUIDE.md` + nota de cache)
    7. Rodar `php artisan test --filter=Phase51\\\\GrantsResumoTest` → GREEN.
    8. Commitar GREEN: `feat(51-02): EcfDriveService.grantsResumo + grantsDistribuicao (cache 300s/3600s)`
  </action>
  <verify>
    <automated>php artisan test --filter=Phase51\\GrantsResumoTest</automated>
    Os 5 testes GREEN; nenhum método antigo do EcfDriveService quebra (sanity: `php artisan test --filter=EcfDriveService` se houver testes legados).
  </verify>
  <done>2 métodos públicos novos no EcfDriveService com docblock pt-BR; 5 testes GREEN; commits RED→GREEN separados</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: GrantController::index() — consumo remoto + fallback + universo no_grant corrigido + buckets locais</name>
  <files>
    - app/Http/Controllers/GrantController.php
    - tests/Feature/Phase51/GrantsResumoTest.php
  </files>
  <read_first>
    - app/Http/Controllers/GrantController.php (linhas 13-76) — `index()` atual; refator inseparável aqui
    - app/Http/Controllers/PolosController.php (linhas 524-528) — pattern canônico try/catch + Log::warning('[Modulo] ...') + retorno fallback (referência RESEARCH §6 linhas 226-244)
    - app/Models/CompanyGrant.php (linhas 50-56) — scope `expiringSoon($days)` já existe; reutilizar para os buckets locais
    - app/Models/MlToken.php — checar coluna `status` e relação company_id (mencionado em CONTEXT.md como universo OAuth ML ativo)
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-CONTEXT.md (linhas 74-82) — SQL exato do universo no_grant
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-CONTEXT.md (linhas 118-138) — payload esperado de /grants/resumo (estimado)
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-RESEARCH.md §6 (linhas 226-275) — recomendação acionável do refator
  </read_first>
  <behavior>
    `GrantController::index()` recebe `EcfDriveService $service` por DI no método (mesmo pattern de PolosController).

    1. Universo no_grant corrigido (substitui o whereNotIn atual em GrantController.php:57-61):
       ```
       $noGrantQuery = Company::query()
           ->where('active', true)
           ->where(function ($q) {
               $q->whereNotNull('cust_id')
                 ->orWhereExists(function ($sub) {
                     $sub->select(DB::raw(1))
                         ->from('ml_tokens')
                         ->whereColumn('ml_tokens.company_id', 'companies.id')
                         ->where('ml_tokens.status', 'active');
                 });
           })
           ->whereNotExists(function ($sub) {
               $sub->select(DB::raw(1))
                   ->from('company_grants')
                   ->whereColumn('company_grants.company_id', 'companies.id')
                   ->where('company_grants.status', 'active');
           })
           ->orderBy('name');
       $noGrant = $noGrantQuery->get(['id', 'name']);
       ```

    2. Buckets locais (sempre computar — usados no fallback E como dado base):
       ```
       $bucketsLocal = [
           'd7'  => CompanyGrant::expiringSoon(7)->count(),
           'd15' => CompanyGrant::expiringSoon(15)->count(),
           'd30' => CompanyGrant::expiringSoon(30)->count(),
           'd60' => CompanyGrant::expiringSoon(60)->count(),
           'd90' => CompanyGrant::expiringSoon(90)->count(),
       ];
       ```

    3. Consumo remoto com fallback gracioso (pattern PolosController:524-528):
       ```
       $source       = 'local';
       $buckets      = $bucketsLocal;
       $divergencia  = null;
       $totaisRemoto = null;
       try {
           $resumo       = $service->grantsResumo();
           $source       = 'remote';
           $buckets      = $resumo['buckets']     ?? $bucketsLocal;
           $divergencia  = $resumo['divergencia']['sellers_em_base_sem_contatos_cpp'] ?? null;
           $totaisRemoto = $resumo['totais']      ?? null;
       } catch (\Throwable $e) {
           Log::warning('[Grants] /grants/resumo offline — usando contagem local', ['error' => $e->getMessage()]);
       }
       ```

    4. `stats` final passado ao Inertia inclui:
       - total_companies, active_grants, expiring_soon, expired_grants, no_grant (todos como já estão)
       - buckets => array d7/d15/d30/d60/d90
       - divergencia_ml => int|null
       - source => 'remote'|'local'
       - totais_remoto => array|null (caso queira mostrar comparação ativa/expirado direto da API)

    Observação: o `'expiring_soon'` legado (linha 67) continua existindo — não quebrar UI atual; os buckets são uma NOVA camada de dado.

    Testes adicionais ao `GrantsResumoTest.php`:
    - `test_index_usa_payload_remoto_quando_api_ok`: cria companies + grants seed; `Http::fake` para `/grants/resumo` retorna buckets={d7:5,d15:10,...,d90:50}; `$this->get(route('grants.index'))`; assert que prop `stats.buckets.d7 === 5` e `stats.source === 'remote'`.
    - `test_index_fallback_local_quando_api_falha`: `Http::fake` retorna 500 em `/grants/resumo`; cria 2 grants expirando em <7d via factory/create direto; renderiza index; assert `stats.buckets.d7 === 2` e `stats.source === 'local'`.
    - `test_index_universo_no_grant_inclui_company_com_cust_id_e_sem_grant_ativo`: cria 3 companies — A com cust_id+sem grant, B com cust_id+com grant ativo, C sem cust_id e sem ml_token. Assert que stats.no_grant === 1 (só A; C não entra no universo).
    - `test_index_universo_no_grant_inclui_company_com_ml_token_ativo_sem_grant`: cria company sem cust_id mas com ml_token status='active' e sem grant; assert que entra no no_grant.
    - `test_index_divergencia_ml_propagada_quando_remoto_ok`: payload remoto com divergencia.sellers_em_base_sem_contatos_cpp=726; assert stats.divergencia_ml === 726.

    Para asserções Inertia, usar `$response->assertInertia(fn ($page) => $page->where('stats.source','remote')->where('stats.buckets.d7',5))` (Inertia Laravel já expõe esse testing helper).
  </behavior>
  <action>
    1. Adicionar os 5 testes acima ao final do `tests/Feature/Phase51/GrantsResumoTest.php`. Helpers necessários (criar dentro da classe):
       - `makeCompany(array $attrs = []): Company` igual ao pattern Phase 20 (sequência cnpj única)
       - `makeMlToken(int $companyId, string $status = 'active'): void` se o model MlToken estiver disponível (inspecionar campos obrigatórios via tinker antes — incluir o mínimo: company_id, status; demais opcionais)
       - `makeGrant(int $companyId, ?string $expiresAt = null, string $status = 'active'): CompanyGrant`
    2. Rodar `php artisan test --filter=Phase51\\\\GrantsResumoTest` → os 5 novos testes DEVEM FALHAR (controller ainda usa whereNotIn antigo e não consome /grants/resumo). Os 5 testes da Tarefa 1 continuam GREEN.
    3. Commitar RED: `test(51-02): RED — testes para GrantController.index com consumo remoto + fallback + universo`
    4. Editar `app/Http/Controllers/GrantController.php`:
       - Adicionar imports: `use App\Services\EcfDriveService;`, `use Illuminate\Support\Facades\DB;`, `use Illuminate\Support\Facades\Log;`
       - Mudar assinatura `public function index()` → `public function index(EcfDriveService $service)`
       - Substituir bloco linhas 57-61 (universo no_grant) pelo novo whereExists/whereNotExists conforme <behavior> ponto 1
       - Inserir após bloco do no_grant: cálculo de `$bucketsLocal` conforme ponto 2
       - Inserir try/catch chamando `$service->grantsResumo()` conforme ponto 3 (acima do `return Inertia::render`)
       - Expandir array `stats` no `return Inertia::render` adicionando `buckets`, `divergencia_ml`, `source`, `totais_remoto`
       - NÃO alterar `$grants`, `$expiringSoon`, comportamento de `sync_pending` — esses seguem idênticos
    5. Rodar `php artisan test --filter=Phase51\\\\GrantsResumoTest` → TUDO GREEN (10 testes Phase 51).
    6. Sanity: rodar `php artisan test` full ou pelo menos Phase 20 + Phase 51 → sem regressões.
    7. Commitar GREEN: `feat(51-02): GrantController consume /grants/resumo com fallback local + corrige universo no_grant + buckets`
  </action>
  <verify>
    <automated>php artisan test --filter=Phase51\\GrantsResumoTest && php artisan test --filter=Phase20\\SyncGrantsFromEcfDriveTest</automated>
    Suite Phase 51 (≥10 testes) GREEN; suite Phase 20 sem regressão; smoke manual: abrir `/grants` em dev e confirmar payload no Inertia (DevTools → Initial Page Data) inclui `stats.source` e `stats.buckets`.
  </verify>
  <done>GrantController::index expandido; 5 testes novos GREEN; universo no_grant respeita cust_id || ml_token ativo; buckets locais sempre presentes; flag source distingue remoto/local; commits RED→GREEN separados</done>
</task>

</tasks>

<verification>
1. `php artisan test --filter=Phase51\\GrantsResumoTest` → todos GREEN (≥10 testes)
2. `php artisan test --filter=Phase20\\SyncGrantsFromEcfDriveTest` → sem regressão
3. Tinker em dev: `app(\App\Services\EcfDriveService::class)->grantsDistribuicao('iniciativa')` → throws InvalidArgumentException
4. Smoke `/grants` em dev: DevTools (Network → Inertia response) mostra `stats.source: 'remote'|'local'`, `stats.buckets.d7..d90`, `stats.divergencia_ml`
5. Provocar fallback: deixar `ECF_DRIVE_KEY` inválida temporariamente em `.env` → recarregar `/grants` → log warning `[Grants]` em `storage/logs/laravel.log` + `stats.source === 'local'`
6. Verificar no DB que companies só-cust_id (sem ml_token) entram no no_grant, e que companies sem cust_id E sem ml_token NÃO entram (SQL via `\App\Models\Company::query()->where('active',true)->whereNotNull('cust_id')->whereNotExists(...)->count()`)
</verification>

<success_criteria>
- [ ] `EcfDriveService::grantsResumo()` implementado, com Cache::remember TTL=300s
- [ ] `EcfDriveService::grantsDistribuicao(string $dimensao)` implementado, validando dimensão `['programa']` only com InvalidArgumentException
- [ ] `GrantController::index(EcfDriveService $service)` chama grantsResumo() em try/catch e propaga buckets + divergencia + source
- [ ] Universo `no_grant` corrigido: companies.active=true AND (cust_id IS NOT NULL OR ml_token status='active') AND NOT EXISTS grant ativo
- [ ] `stats.buckets.d7/d15/d30/d60/d90` sempre presente (do remoto ou do fallback local)
- [ ] `stats.source === 'local'` quando a chamada falha; `'remote'` quando OK
- [ ] Log warning `[Grants] /grants/resumo offline — usando contagem local` no fallback (não erro 500)
- [ ] Suite Phase 51 ≥10 testes GREEN, suite Phase 20 sem regressão
- [ ] Commits TDD separados RED → GREEN em cada tarefa
</success_criteria>

<output>
Criar `.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-02-SUMMARY.md` ao final com: shapes finais de `stats` enviados ao Inertia, payload real confirmado de `/grants/resumo` (se diferente do estimado em CONTEXT.md, registrar para a UI usar a chave correta), contagem de testes GREEN/RED, e qualquer ajuste no universo SQL após validação real no DB. Próximo: Wave 3 (51-03-PLAN.md — UI: StatCards de buckets + card divergência + badge "API offline" + colunas opcionais na tabela).
</output>
