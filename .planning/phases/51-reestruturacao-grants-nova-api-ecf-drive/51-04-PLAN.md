---
phase: 51-reestruturacao-grants-nova-api-ecf-drive
plan: "04"
type: execute
wave: 4
depends_on:
  - 51-03
autonomous: false
requirements:
  - REQ-51-04
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-04-UAT.md
must_haves:
  truths:
    - "Deploy `bash deploy.sh` concluído sem erro (confirmar caso-a-caso com o operador na milestone vigente — outro dev em paralelo, conforme feedback_perguntar_antes_deploy_v9)"
    - "Migration `add_metadata_fields_to_company_grants` aplicada em prod (via deploy.sh com migrate --force)"
    - "`/grants` em prod exibe linha nova de buckets 7/15/30/60/90d com cores progressivas"
    - "Card 'Divergência ML' exibe contagem real (ou explica caso zero)"
    - "Universo 'Empresas sem grants' reflete cust_id || ml_token ativo (validar via consulta DB ou inspeção visual)"
    - "Tabela: colunas Programa/Nível/Medalha In/Medalha Out aparecem com valores para grants resincados; '—' para legacy"
    - "Sync command (`php artisan grants:sync-ecf`) rodado em prod — 8 campos novos populam para empresas cujo payload da API ECF Drive inclui esses campos"
    - "Cache::clear executado em prod após o deploy (memória project_ecf_drive_cache_stale.md)"
    - "ROADMAP e STATE atualizados marcando Phase 51 concluída"
  artifacts:
    - path: ".planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-04-UAT.md"
      provides: "Registro do UAT — checklist marcado + observações do operador"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 51 marcada como concluída no roadmap"
    - path: ".planning/STATE.md"
      provides: "Phase 51 movida de in_progress para done"
  key_links:
    - from: "deploy.sh"
      to: "VPS Hostinger 177.7.53.164 / admin.ecfconsultoria.com.br"
      via: "PuTTY pscp/plink + composer install --no-dev + migrate --force + cache rebuild + supervisor restart"
      pattern: "convenção CLAUDE.md seção Platform Requirements"
    - from: "php artisan grants:sync-ecf em prod"
      to: "API ECF Drive /clientes/grants (com 8 campos novos)"
      via: "EcfDriveService::listGrants() chamado dentro do command"
      pattern: "loop de paginação atual em SyncGrantsFromEcfDrive.php:40-89 (intacto)"
---

<objective>
Validação humana em produção da Phase 51. Wave Não-autônoma — requer interação com o operador para autorizar o deploy (outro dev em paralelo na milestone) e confirmar visualmente cada entrega das Waves 1–3 no ambiente real. Encerra a Phase com ROADMAP + STATE atualizados.

Esta wave NÃO cria código novo. É exclusivamente: confirmar deploy → rodar sync → inspecionar UI → registrar UAT → atualizar artefatos GSD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-CONTEXT.md
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-01-SUMMARY.md
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-02-SUMMARY.md
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Tarefa 1: Decisão de deploy (confirmar com operador)</name>
  <decision>Autorização explícita para `bash deploy.sh` da Phase 51 — milestone v11.0 ativa, há outro dev em paralelo (memória `feedback_perguntar_antes_deploy_v9`)</decision>
  <context>
    Phase 51 mexe em: migration company_grants (8 colunas nullable, totalmente reversível), EcfDriveService (2 métodos novos), GrantController::index, Grants/Index.jsx, SyncGrantsFromEcfDrive::mapToDb.

    Aditivo puro: zero quebra de rotas, zero breaking change em schema, zero impacto em outras phases da milestone v11.0.

    Risco percebido: BAIXO. Mas a milestone tem outro dev mexendo em paralelo — confirmar com o operador se este é o momento certo de rodar o deploy ou se ele prefere agrupar com outras mudanças.

    Comandos VPS pós-deploy seguem com autorização permanente (memória `feedback_autorizacao_permanente_deploy`). Apenas o `bash deploy.sh` exige confirmação caso-a-caso nesta milestone.
  </context>
  <options>
    <option id="deploy-now">
      <name>Deploy agora — `bash deploy.sh` no terminal local</name>
      <pros>Mudança fica disponível para uso imediato; UAT pode rodar hoje</pros>
      <cons>Pode colidir com mudanças em andamento do outro dev — operador é quem sabe</cons>
    </option>
    <option id="deploy-later">
      <name>Pausar nesta etapa, retomar quando operador confirmar janela limpa</name>
      <pros>Zero risco de colisão; Phase 51 fica em prateleira aguardando deploy</pros>
      <cons>UAT atrasa; mudança fica ociosa</cons>
    </option>
    <option id="deploy-with-other">
      <name>Agrupar com deploy de outra phase em andamento</name>
      <pros>Single deploy window; menor sobrecarga de cache:clear + supervisor restart</pros>
      <cons>Phase 51 vira refém do timing da outra; UAT amarrado a deploy combinado</cons>
    </option>
  </options>
  <resume-signal>Selecionar: deploy-now, deploy-later, ou deploy-with-other (com referência à outra phase se "with-other")</resume-signal>
</task>

<task type="auto">
  <name>Tarefa 2: Executar deploy + smoke pós-deploy em prod</name>
  <files>
    - (nenhum arquivo do repo — apenas execução de scripts + checagens)
  </files>
  <read_first>
    - CLAUDE.md seção "Platform Requirements" — comandos de deploy + VPS
    - deploy.sh do projeto — confirmar que rodaria composer install --no-dev + npm run build + php artisan migrate --force + caches + supervisor (pular leitura se já familiar)
    - Memória `project_ecf_drive_cache_stale` — Cache::clear obrigatório pós-deploy
  </read_first>
  <behavior>
    Executar (assumindo Tarefa 1 resolveu para `deploy-now`):

    1. `bash deploy.sh` (no terminal local, PuTTY ativo)
    2. Aguardar conclusão (logs no terminal mostram supervisor restart e cache:clear)
    3. Verificar `/grants` em https://admin.ecfconsultoria.com.br (smoke visual mínimo: página carrega sem 500, header tem badge ou não, layout não quebrou)
    4. Rodar via SSH no VPS: `php artisan migrate:status | grep add_metadata_fields_to_company_grants` → confirmar `Ran`
    5. Rodar via SSH: `php artisan cache:clear` (extra de segurança — deploy.sh já faz, mas garantir devido ao cache TTL=300s/3600s de grantsResumo/grantsDistribuicao)
    6. Rodar via SSH: `php artisan grants:sync-ecf` — aguardar conclusão; copiar a tabela final (Recebidos/Matched/Criados/Atualizados/Órfãos) para o UAT
  </behavior>
  <action>
    1. Confirmar com operador (saída da Tarefa 1): se `deploy-now`, executar; se `deploy-later`, marcar Phase 51 como ready-to-deploy e ENCERRAR esta Wave sem prosseguir Tarefas 3–4; se `deploy-with-other`, registrar dependência e encerrar.
    2. Para `deploy-now`: executar `bash deploy.sh` localmente. Aguardar até ver `Deploy concluído com sucesso` (ou equivalente do script).
    3. Capturar:
       - Output final do deploy.sh (últimas 20 linhas) → colar no UAT
       - `migrate:status` confirmando a nova migration → colar no UAT
       - Tabela de output do `grants:sync-ecf` → colar no UAT
    4. Se qualquer etapa falhar: NÃO seguir para Tarefa 3. Documentar erro no UAT e voltar para o operador.
  </action>
  <verify>
    <automated>echo "verificação humana via SSH — sem comando local automatizado nesta tarefa"</automated>
    Verificação humana — checklist no UAT registra outputs reais.
  </verify>
  <done>Deploy concluído; migration aplicada em prod; sync rodado; outputs colados em 51-04-UAT.md</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 3: UAT em /grants em produção</name>
  <what-built>
    Wave 1 (write-path): 8 colunas novas em `company_grants` + mapToDb() expandido + testes Phase 51 GREEN
    Wave 2 (read-path): `EcfDriveService::grantsResumo/grantsDistribuicao` + `GrantController::index` com fallback gracioso + universo no_grant corrigido
    Wave 3 (UI): linha de buckets 7/15/30/60/90d + card Divergência ML + badge "API offline" + 4 colunas opcionais na tabela
  </what-built>
  <how-to-verify>
    Abrir https://admin.ecfconsultoria.com.br/grants logado como admin. Verificar cada item abaixo:

    1. **Linha original de 5 StatCards** ainda aparece intacta (Empresas totais, Grants ativos, Expirando em 30d, Expirados, Sem grant). [layout sem regressão]

    2. **Linha NOVA de 6 StatCards** aparece imediatamente abaixo:
       - Expira em 7d  — cor vermelha (text-red-400) + borda vermelha se > 0
       - Expira em 15d — cor laranja (text-orange-400) + borda vermelha se > 0
       - Expira em 30d — cor amarela ECF (text-ecf-yellow) + borda vermelha se > 0
       - Expira em 60d — cor cinza claro (text-white/60)
       - Expira em 90d — cor cinza escuro (text-white/40)
       - Divergência ML — cor âmbar (text-amber-400); hover mostra tooltip "Sellers em BASE_VENDEDORES sem cadastro ContatosCPP — divergência de cadastro ML"

    3. **Universo "Sem grant" correto:**
       - Pegar o número do card "Sem grant"
       - Executar via SSH/tinker: `\App\Models\Company::where('active',true)->whereNotIn('id', \App\Models\CompanyGrant::where('status','active')->pluck('company_id')->unique())->where(function($q){ $q->whereNotNull('cust_id')->orWhereExists(function($s){ $s->select(\DB::raw(1))->from('ml_tokens')->whereColumn('ml_tokens.company_id','companies.id')->where('status','active'); }); })->count()`
       - Conferir que bate com o card.
       - Adicional: confirmar que empresas SEM cust_id E sem ml_token NÃO aparecem no painel "Empresas sem grant cadastrado" abaixo da tabela.

    4. **Tabela principal** tem 4 colunas novas:
       - Programa, Nível, Medalha In, Medalha Out
       - Grants resincados pós-Phase 51 mostram valores (procurar pelo menos 1)
       - Grants legacy mostram '—' (esperado — payload da API não tinha esses campos antes)

    5. **Badge "API offline"**:
       - Caso a API ECF Drive esteja operacional → badge AUSENTE (esperado)
       - Para forçar teste: trocar `ECF_DRIVE_KEY` em `.env` de prod temporariamente por chave inválida + `php artisan config:clear`; recarregar `/grants`; confirmar badge "API offline" no header + stats voltam a vir do fallback local; reverter `.env` + `config:clear` ao terminar. [OPCIONAL — pular se julgar arriscado em prod]

    6. **Sync command** rodou e populou 8 campos:
       - SSH: `php artisan tinker` → `\App\Models\CompanyGrant::whereNotNull('programa')->count()` → DEVE ser > 0 (operador confirma)
       - Caso seja 0 e a API esteja entregando os 8 campos: investigar payload real vs mapping — registrar discrepância no UAT.

    7. **Cache não está mascarando**:
       - Após qualquer mudança visível no banco/API, se algo não refletir, rodar `php artisan cache:clear` no VPS e recarregar.

    Marcar cada item como ✅ ou anotar problema no UAT.
  </how-to-verify>
  <resume-signal>Digitar "approved" se tudo passou, ou descrever problema(s) encontrado(s) para que a Phase entre em modo de correção (`/gsd:plan-phase 51 --gaps`)</resume-signal>
</task>

<task type="auto">
  <name>Tarefa 4: Encerrar Phase — ROADMAP + STATE + UAT.md final</name>
  <files>
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-04-UAT.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <read_first>
    - .planning/ROADMAP.md — entrada da Phase 51 e seção da milestone v11.0
    - .planning/STATE.md — bloco de phases em progresso
  </read_first>
  <behavior>
    Após "approved" na Tarefa 3:

    1. Criar/finalizar `51-04-UAT.md` com:
       - Data do UAT
       - Quem aprovou (operador)
       - Checklist de 7 itens da Tarefa 3 com ✅ ou ❌
       - Outputs colados na Tarefa 2 (deploy.sh tail, migrate:status, grants:sync-ecf table)
       - Qualquer observação de discrepância payload (campos esperados vs reais)

    2. ROADMAP.md:
       - Marcar Phase 51 como `[x]` concluída
       - Atualizar contador de phases concluídas da milestone v11.0 se aplicável

    3. STATE.md:
       - Mover Phase 51 de in_progress / planned para done
       - Atualizar timestamp de última atividade
       - Registrar 2-3 bullets de aprendizados (ex: "API ECF Drive comporta-se exatamente como Phase 20 — pattern Http::fake validado novamente", "Cache TTL 300s grantsResumo / 3600s grantsDistribuicao não causou stale durante UAT")

    4. Commit final pt-BR:
       `docs(51): encerra Phase 51 — reestruturação /grants com nova API ECF Drive aprovada em UAT`
  </behavior>
  <action>
    1. Escrever `51-04-UAT.md` conforme template implícito (checklist + outputs + observações).
    2. Editar ROADMAP.md marcando Phase 51 como concluída.
    3. Editar STATE.md (mover bloco de phase + acrescentar aprendizados).
    4. Commitar tudo numa única commit pt-BR com Co-Authored-By Claude.
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -i "51"</automated>
    Último commit menciona Phase 51 e está em pt-BR.
  </verify>
  <done>Phase 51 marcada concluída em ROADMAP/STATE; UAT.md finalizado com outputs reais; commit final criado</done>
</task>

</tasks>

<verification>
1. UAT.md preenchido com 7/7 itens checked (ou problemas documentados)
2. ROADMAP.md tem Phase 51 marcada `[x]`
3. STATE.md atualizado movendo a Phase para done + 2-3 aprendizados
4. Migration aplicada em prod confirmada via `migrate:status`
5. Sync `grants:sync-ecf` executado com sucesso e tabela de output capturada
6. `/grants` em prod exibe TODAS as novidades visuais sem regressão das partes antigas
7. Commit final pt-BR criado com referência à Phase 51
</verification>

<success_criteria>
- [ ] Operador autorizou explicitamente o deploy desta Phase nesta janela (Tarefa 1)
- [ ] `bash deploy.sh` concluído sem erro
- [ ] Migration `add_metadata_fields_to_company_grants` em estado `Ran` em prod
- [ ] `php artisan grants:sync-ecf` rodado em prod e tabela de output capturada no UAT
- [ ] `php artisan cache:clear` executado em prod pós-sync
- [ ] UAT visual aprovado (7 itens da Tarefa 3 verificados pelo operador)
- [ ] `51-04-UAT.md` criado com outputs reais + checklist marcado
- [ ] ROADMAP.md e STATE.md atualizados refletindo Phase 51 concluída
- [ ] Commit final pt-BR criado encerrando a Phase
</success_criteria>

<output>
Criar `.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-04-SUMMARY.md` ao final com: data do deploy, output da tabela do sync, lista de validações UAT aprovadas, qualquer item da TODO/seed que tenha surgido (ex: "discrepância no nome do campo X → criar seed para investigar na próxima janela"). Esta é a última wave da Phase 51.
</output>
