---
phase: 51-reestruturacao-grants-nova-api-ecf-drive
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-51-01
files_modified:
  - database/migrations/2026_06_30_000051_add_metadata_fields_to_company_grants.php
  - app/Models/CompanyGrant.php
  - app/Console/Commands/SyncGrantsFromEcfDrive.php
  - tests/Feature/Phase20/SyncGrantsFromEcfDriveTest.php
must_haves:
  truths:
    - "Tabela `company_grants` ganha 8 colunas nullable: programa, iniciativa, nivel_solucion, nombre_solucion, parceiro, localidade, medalha_fecha_in (date), medalha_fecha_out (date)"
    - "CompanyGrant::$fillable inclui as 8 novas colunas (sem isso, updateOrCreate ignora silenciosamente)"
    - "SyncGrantsFromEcfDrive::mapToDb() propaga 8 campos do payload camelCase do ECF Drive (nivelSolucion → nivel_solucion, medalhaFechaIn → medalha_fecha_in) para colunas snake_case"
    - "Sync com payload INCLUINDO os 8 campos persiste valores corretos no banco"
    - "Sync com payload SEM os 8 campos (regressão Phase 20) continua funcionando — colunas ficam NULL, nenhum erro, idempotência preservada"
    - "RED commit separado do GREEN commit em cada bloco TDD"
  artifacts:
    - path: "database/migrations/2026_06_30_000051_add_metadata_fields_to_company_grants.php"
      provides: "Migration aditiva nullable — espelho de 2026_06_05_120000_add_segmento_to_company_grants.php"
      contains: "Schema::table + 8 colunas (string 6× + date 2×), ->nullable()->after(...)"
    - path: "app/Models/CompanyGrant.php"
      provides: "Model com $fillable expandido + $casts para medalha_fecha_in/out (date)"
    - path: "app/Console/Commands/SyncGrantsFromEcfDrive.php"
      provides: "Comando expandido — mapToDb() mapeia 8 campos via ?? null e Carbon::parse() para datas"
    - path: "tests/Feature/Phase20/SyncGrantsFromEcfDriveTest.php"
      provides: "2 testes novos (campos presentes / campos ausentes), 0 regressão nos existentes"
  key_links:
    - from: "SyncGrantsFromEcfDrive::mapToDb()"
      to: "CompanyGrant::$fillable (8 colunas novas)"
      via: "updateOrCreate em SyncGrantsFromEcfDrive.php:75-78"
      pattern: "mass-assignment exige $fillable atualizado — checar antes de testar"
    - from: "Migration add_metadata_fields_to_company_grants"
      to: "tabela company_grants (após coluna segmento — Phase 20)"
      via: "Schema::table + ->after('segmento')"
      pattern: "mesma família da migration Phase 20 (add_segmento)"
---

<objective>
Persistência local dos 8 novos campos opcionais que a API ECF Drive `/clientes/grants` passou a expor em 2026-06-30. Migration aditiva nullable + atualização do model + expansão do `mapToDb()` no command de sync. Aditivo puro — Phase 20 segue funcionando sem regressão.

Esta wave entrega APENAS o write-path. Wave 2 lê do remoto (`/grants/resumo` e `/grants/distribuicao`); Wave 3 expõe na UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-CONTEXT.md
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Migration aditiva + $fillable expandido (RED → GREEN)</name>
  <files>
    - database/migrations/2026_06_30_000051_add_metadata_fields_to_company_grants.php
    - app/Models/CompanyGrant.php
    - tests/Feature/Phase20/SyncGrantsFromEcfDriveTest.php
  </files>
  <read_first>
    - database/migrations/2026_06_05_120000_add_segmento_to_company_grants.php (linhas 10-24) — template canônico de migration aditiva nullable na MESMA tabela (Phase 20)
    - app/Models/CompanyGrant.php (linhas 28-38) — $fillable atual + $casts atual (granted_at, expires_at, regranted_at já são `date`)
    - tests/Feature/Phase20/SyncGrantsFromEcfDriveTest.php (linhas 24-33) — setUp() com config services.ecf.* e unlink de log
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-RESEARCH.md §2 (linhas 82-114) — schema final das colunas
  </read_first>
  <behavior>
    Schema acrescido em `company_grants`:
    - programa            (string, nullable, after segmento)
    - iniciativa          (string, nullable, after programa)
    - nivel_solucion      (string, nullable, after iniciativa)
    - nombre_solucion     (string, nullable, after nivel_solucion)
    - parceiro            (string, nullable, after nombre_solucion)
    - localidade          (string, nullable, after parceiro)
    - medalha_fecha_in    (date,   nullable, after localidade)
    - medalha_fecha_out   (date,   nullable, after medalha_fecha_in)

    Model CompanyGrant:
    - $fillable expandido com as 8 colunas
    - $casts inclui 'medalha_fecha_in' => 'date' e 'medalha_fecha_out' => 'date'
    - Sem alteração em getActivitylogOptions (campos novos não entram no log para evitar ruído — confirmado decisão lean)

    RED esperado: teste `test_apply_persiste_campos_metadata_phase51` (descrito em RESEARCH §4 linhas 170-194) DEVE FALHAR antes da migration/fillable porque as colunas não existem nem são fillable.
  </behavior>
  <action>
    1. Adicionar ao final do arquivo `tests/Feature/Phase20/SyncGrantsFromEcfDriveTest.php` o teste novo:
       ```
       public function test_apply_persiste_campos_metadata_phase51(): void
       ```
       Cria company com adman_account_id='999'; fakeGrants com payload contendo os 8 campos novos (programa='CPP', iniciativa='Mentor', nivelSolucion='Gold', nombreSolucion='Acelerador', parceiro='ECF', localidade='SP', medalhaFechaIn='2026-01-15', medalhaFechaOut='2026-12-31'); roda `grants:sync-ecf`; busca CompanyGrant; assertSame para cada um dos 8 campos. Para datas, assertar `?->toDateString()`.
    2. Rodar `php artisan test --filter=test_apply_persiste_campos_metadata_phase51` — DEVE FALHAR (coluna não existe / não é fillable).
    3. Commitar RED: `test(51-01): RED — testes para 8 campos metadata em company_grants`
    4. Criar `database/migrations/2026_06_30_000051_add_metadata_fields_to_company_grants.php` espelhando 1:1 a estrutura de `2026_06_05_120000_add_segmento_to_company_grants.php`:
       - Header pt-BR explicando "Phase 51 — campos opcionais expandidos vindos da API ECF Drive a partir de 2026-06-30"
       - `Schema::table('company_grants', ...)` com as 8 colunas conforme <behavior>
       - `down()` faz `dropColumn(['programa','iniciativa','nivel_solucion','nombre_solucion','parceiro','localidade','medalha_fecha_in','medalha_fecha_out'])`
    5. Em `app/Models/CompanyGrant.php`:
       - Acrescentar as 8 colunas ao array `$fillable` (após 'segmento')
       - Acrescentar `'medalha_fecha_in' => 'date'` e `'medalha_fecha_out' => 'date'` ao `$casts`
    6. Rodar `php artisan migrate` (dev).
    7. Validar que o teste novo da Tarefa 1 ainda está RED — vai falhar agora por outro motivo (mapToDb() não mapeia). Esse é o gancho da Tarefa 2.
    8. Antes de seguir, rodar a suite Phase 20 inteira:
       `php artisan test --filter=Phase20\\\\SyncGrantsFromEcfDriveTest` — TODOS os testes Phase 20 existentes (10 atuais) devem continuar GREEN. Se quebrar algum, é regressão; corrigir antes de prosseguir.
    9. Commitar GREEN-parcial: `feat(51-01): migration 8 colunas metadata + $fillable + casts em CompanyGrant`
  </action>
  <verify>
    <automated>php artisan migrate:status && php artisan test --filter=Phase20\\SyncGrantsFromEcfDriveTest</automated>
    Migration aparece em `php artisan migrate:status`; testes existentes Phase 20 GREEN; teste novo `test_apply_persiste_campos_metadata_phase51` ainda RED (esperado — Tarefa 2 vai fechar).
  </verify>
  <done>Migration rodada; 8 colunas existem; $fillable e $casts atualizados; testes Phase 20 pré-existentes seguem GREEN; commits RED e GREEN-parcial criados</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Expandir mapToDb() em SyncGrantsFromEcfDrive (fecha o GREEN)</name>
  <files>
    - app/Console/Commands/SyncGrantsFromEcfDrive.php
    - tests/Feature/Phase20/SyncGrantsFromEcfDriveTest.php
  </files>
  <read_first>
    - app/Console/Commands/SyncGrantsFromEcfDrive.php (linhas 170-194) — função `mapToDb()` atual; aqui entra a expansão
    - app/Console/Commands/SyncGrantsFromEcfDrive.php (linhas 75-78) — chamada `updateOrCreate` que consome o array retornado
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-CONTEXT.md (linhas 154-164) — mapping autoritativo dos 8 campos camelCase → snake_case
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-RESEARCH.md §3 (linhas 135-156) — recomendação acionável com Carbon::parse() defensivo
  </read_first>
  <behavior>
    O array retornado por `mapToDb($grant)` ganha 8 chaves novas (preservando as existentes ml_cust_id/ml_email/ml_phone/segmento/granted_at/expires_at/status):

    - 'programa'         => $grant['programa']        ?? null
    - 'iniciativa'       => $grant['iniciativa']      ?? null
    - 'nivel_solucion'   => $grant['nivelSolucion']   ?? null
    - 'nombre_solucion'  => $grant['nombreSolucion']  ?? null
    - 'parceiro'         => $grant['parceiro']        ?? null
    - 'localidade'       => $grant['localidade']      ?? null
    - 'medalha_fecha_in' => ! empty($grant['medalhaFechaIn'])  ? Carbon::parse($grant['medalhaFechaIn'])->toDateString()  : null
    - 'medalha_fecha_out'=> ! empty($grant['medalhaFechaOut']) ? Carbon::parse($grant['medalhaFechaOut'])->toDateString() : null

    Comportamentos garantidos:
    - Payload SEM os 8 campos → as 8 colunas viram NULL no banco (regressão Phase 20 mantida)
    - Payload COM todos os 8 campos → todos persistem com valores
    - String vazia em medalhaFechaIn/Out → coluna NULL (o `! empty(...)` cobre)
  </behavior>
  <action>
    1. RED do segundo cenário — adicionar 1 teste extra ao arquivo de testes:
       ```
       public function test_apply_sem_campos_metadata_phase51_persiste_null(): void
       ```
       Cria company com adman_account_id='999'; `fakeGrants([$this->grantPayload()])` SEM overrides (helper já retorna payload Phase 20 puro); roda `grants:sync-ecf`; busca CompanyGrant; assertNull para cada um dos 8 campos novos; assertSame('Moda', $g->segmento) — confirma que o resto continua intacto.

       Junto, deixar o teste da Tarefa 1 (`test_apply_persiste_campos_metadata_phase51`) AINDA RED. Rodar:
       `php artisan test --filter=Phase20\\\\SyncGrantsFromEcfDriveTest` — os 2 testes Phase 51 devem estar RED, restante GREEN.
    2. Commitar RED: `test(51-01): RED — mapToDb() ainda não mapeia 8 campos novos`
    3. Em `app/Console/Commands/SyncGrantsFromEcfDrive.php`, editar `mapToDb()` (linhas 170-194):
       - Acrescentar as 8 chaves descritas em <behavior> ao array de retorno, entre 'segmento' e 'granted_at' para manter agrupamento lógico
       - Não alterar nada mais — status, granted_at, expires_at, ml_cust_id permanecem como estão
    4. Rodar `php artisan test --filter=Phase20\\\\SyncGrantsFromEcfDriveTest` → TUDO GREEN (10 antigos + 2 Phase 51).
    5. Smoke manual em dev:
       `php artisan grants:sync-ecf --dry-run` — confirmar que o output inclui as 8 chaves novas no JSON impresso para empresas com payload contendo esses campos.
    6. Commitar GREEN-final: `feat(51-01): mapToDb propaga 8 campos metadata da API ECF Drive`
  </action>
  <verify>
    <automated>php artisan test --filter=Phase20\\SyncGrantsFromEcfDriveTest</automated>
    Todos os testes Phase 20 + Phase 51 (≥12 testes) GREEN. Nenhuma regressão.
  </verify>
  <done>mapToDb() expandido; 2 testes Phase 51 GREEN; 10 testes Phase 20 sem regressão; dry-run mostra 8 campos novos no payload; commits RED→GREEN separados</done>
</task>

</tasks>

<verification>
1. `php artisan migrate:status | grep add_metadata_fields_to_company_grants` → migrada
2. Tinker: `\App\Models\CompanyGrant::create(['company_id'=>1,'ml_cust_id'=>'X','programa'=>'CPP','medalha_fecha_in'=>'2026-01-15'])` → persiste sem erro de mass-assignment e $cast date funciona
3. `php artisan test --filter=Phase20\\SyncGrantsFromEcfDriveTest` → todos GREEN (≥12 testes)
4. `php artisan grants:sync-ecf --dry-run` em ambiente dev → preview mostra 8 campos novos quando payload tem; mostra null/ausente quando não tem
5. Inspeção visual do `mapToDb()` confirma mapeamento camelCase→snake_case correto
</verification>

<success_criteria>
- [ ] Migration `add_metadata_fields_to_company_grants` criada, rodada e reversível (down() funciona)
- [ ] 8 colunas existem em `company_grants` com tipos corretos (6× string nullable + 2× date nullable)
- [ ] `CompanyGrant::$fillable` inclui as 8 colunas; `$casts` mapeia datas
- [ ] `SyncGrantsFromEcfDrive::mapToDb()` propaga 8 chaves novas com fallback `?? null` e `Carbon::parse` defensivo
- [ ] Teste novo `test_apply_persiste_campos_metadata_phase51` GREEN
- [ ] Teste novo `test_apply_sem_campos_metadata_phase51_persiste_null` GREEN (zero regressão)
- [ ] 10 testes Phase 20 pré-existentes seguem GREEN
- [ ] Commits TDD separados: 2 RED + 2 GREEN (1 por tarefa)
</success_criteria>

<output>
Criar `.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-01-SUMMARY.md` ao final com: nome da migration criada, linhas exatas do `mapToDb()` expandido, contagem de testes GREEN/RED, qualquer ajuste no payload real vs estimado em RESEARCH. Próximo: Wave 2 (51-02-PLAN.md — `EcfDriveService::grantsResumo/grantsDistribuicao` + `GrantController::index` com fallback gracioso).
</output>
