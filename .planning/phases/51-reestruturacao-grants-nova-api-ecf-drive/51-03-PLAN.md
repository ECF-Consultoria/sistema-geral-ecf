---
phase: 51-reestruturacao-grants-nova-api-ecf-drive
plan: "03"
type: execute
wave: 3
depends_on:
  - 51-02
autonomous: true
requirements:
  - REQ-51-03
files_modified:
  - resources/js/Pages/Grants/Index.jsx
must_haves:
  truths:
    - "Linha nova de StatCards (5 buckets 7/15/30/60/90d) renderiza ABAIXO da linha atual de 5 StatCards, usando o componente local StatCard sem extensão"
    - "Card adicional 'Divergência ML' renderiza com cor amber-400, contagem vinda de stats.divergencia_ml, e tooltip via title attr"
    - "Badge 'API offline' renderiza no header quando stats.source === 'local'"
    - "Tabela ganha 4 colunas opcionais (Programa, Nível, Medalha In, Medalha Out) — célula renderiza '—' quando NULL para não poluir; ocultar célula inteira não é necessário (grid mantém alinhamento)"
    - "Cores progressivas dos buckets seguem o RESEARCH §5: text-red-400 → text-orange-400 → text-ecf-yellow → text-white/60 → text-white/40"
    - "npm run build verde — sem warnings de variável não utilizada"
  artifacts:
    - path: "resources/js/Pages/Grants/Index.jsx"
      provides: "Página /grants com novas UI elements integrados sem regressão visual"
      contains: "5 StatCards de bucket + card Divergência + badge source + 4 colunas opcionais"
  key_links:
    - from: "Grants/Index.jsx StatCards de bucket"
      to: "stats.buckets.{d7,d15,d30,d60,d90}"
      via: "props.stats fornecido por GrantController::index() (Wave 2)"
      pattern: "reuso direto do componente local StatCard (linhas 21-34); zero extensão"
    - from: "Grants/Index.jsx Badge API offline"
      to: "stats.source === 'local'"
      via: "render condicional no header da seção stats"
      pattern: "condicional inline ala syncResult já presente"
    - from: "Grants/Index.jsx colunas opcionais"
      to: "g.programa / g.nivel_solucion / g.medalha_fecha_in / g.medalha_fecha_out (controller passa do Eloquent)"
      via: "g.X || '—' inline em cada célula"
      pattern: "espelha tratamento atual de g.ml_email/ml_phone (linhas 262-263)"
---

<objective>
Expor na UI tudo que Wave 2 entregou no backend: linha nova de 5 StatCards de buckets de expiração (7/15/30/60/90d com cores progressivas), card adicional "Divergência ML" com tooltip, badge "API offline" no header quando o fallback estiver ativo, e 4 colunas opcionais na tabela principal de grants. Reuso máximo do componente local StatCard já existente; zero extensão de componentes — alinhado ao locked "manter layout atual; só refinar/adicionar" do 51-CONTEXT.md.

Sem mudanças no controller, no model, no command. Pure frontend mais 1 ajuste no GrantController para passar os 4 campos novos no map() de grants (sem isso, a tabela renderiza '—' permanente).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-CONTEXT.md
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-RESEARCH.md
@.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: GrantController — adicionar 4 campos opcionais ao map() de grants</name>
  <files>
    - app/Http/Controllers/GrantController.php
  </files>
  <read_first>
    - app/Http/Controllers/GrantController.php (linhas 35-54) — map() atual de `$grants` (Wave 2 não tocou; aqui adicionar 4 keys)
  </read_first>
  <behavior>
    O array retornado em cada grant ganha 4 chaves novas:
    - programa => $g->programa
    - nivel_solucion => $g->nivel_solucion
    - medalha_fecha_in => $g->medalha_fecha_in?->toDateString()
    - medalha_fecha_out => $g->medalha_fecha_out?->toDateString()

    Demais campos do map() permanecem inalterados. Não passamos `iniciativa`, `nombre_solucion`, `parceiro`, `localidade` — fora do escopo de UI desta Phase (locked em 51-CONTEXT.md decisão de tabela: só Programa/Nível/Medalha In/Medalha Out).
  </behavior>
  <action>
    1. Editar `app/Http/Controllers/GrantController.php` no método `index()` map() de `$grants` (linhas 35-54):
       Adicionar as 4 chaves novas ao array dentro do `fn($g) => [...]`, idealmente entre 'segmento' e 'status' para agrupamento lógico:
       ```
       'programa'           => $g->programa,
       'nivel_solucion'     => $g->nivel_solucion,
       'medalha_fecha_in'   => $g->medalha_fecha_in?->toDateString(),
       'medalha_fecha_out'  => $g->medalha_fecha_out?->toDateString(),
       ```
    2. Não alterar nada mais. Não acrescentar para o map() de `$expiringSoon` — ele segue compacto.
    3. Smoke manual: abrir `/grants` em dev, inspecionar DevTools → Inertia initial data → confirmar que cada item em `props.grants` agora tem as 4 chaves novas (com NULL para grants antigos que ainda não foram resyncados).
  </action>
  <verify>
    <automated>php artisan test --filter=Phase51\\GrantsResumoTest</automated>
    Testes Wave 2 seguem GREEN; smoke inertia mostra 4 campos novos em cada grant.
  </verify>
  <done>4 chaves novas no map() de grants; testes Phase 51 sem regressão</done>
</task>

<task type="auto">
  <name>Tarefa 2: Grants/Index.jsx — linha de buckets + card Divergência + badge API offline</name>
  <files>
    - resources/js/Pages/Grants/Index.jsx
  </files>
  <read_first>
    - resources/js/Pages/Grants/Index.jsx (linhas 11) — imports lucide-react; precisamos adicionar `WifiOff` (badge offline) e usar `AlertTriangle` (bucket d7) e `Eye` ou `AlertCircle` (divergência)
    - resources/js/Pages/Grants/Index.jsx (linhas 21-34) — componente local StatCard (REUSAR sem alterar)
    - resources/js/Pages/Grants/Index.jsx (linhas 194-201) — grid atual `grid-cols-2 sm:grid-cols-5 gap-3` com 5 StatCards
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-RESEARCH.md §5 (linhas 200-222) — cores progressivas dos buckets confirmadas
    - .planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-CONTEXT.md (linhas 102-103) — UI buckets/divergência (5 cards + 1 informativo)
  </read_first>
  <behavior>
    Estrutura visual nova (entre a barra de info atual em linha 179 e o alerta de expiring_soon em linha 204):

    1. Grid atual de 5 StatCards permanece como está (linhas 195-201).
    2. NOVA linha de StatCards de bucket — grid `grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-3`:
       - 5 cards de bucket (d7/d15/d30/d60/d90) com cores progressivas
       - 1 card "Divergência ML" com cor amber-400, tooltip via title attr
    3. Badge "API offline" — posicionado próximo ao botão "Sincronizar Agora" (linha 184), renderiza quando `stats.source === 'local'`. Usar Badge component já importado (linha 3) com variant secondary + classe customizada amber/orange + ícone WifiOff.

    Componentes a usar:
    - `<StatCard label="Expira em 7d"  value={stats.buckets?.d7  ?? 0} color="text-red-400"    icon={AlertTriangle} alert={(stats.buckets?.d7  ?? 0) > 0} />`
    - `<StatCard label="Expira em 15d" value={stats.buckets?.d15 ?? 0} color="text-orange-400" icon={Clock}         alert={(stats.buckets?.d15 ?? 0) > 0} />`
    - `<StatCard label="Expira em 30d" value={stats.buckets?.d30 ?? 0} color="text-ecf-yellow" icon={Clock}         alert={(stats.buckets?.d30 ?? 0) > 0} />`
    - `<StatCard label="Expira em 60d" value={stats.buckets?.d60 ?? 0} color="text-white/60"   icon={Clock} />`
    - `<StatCard label="Expira em 90d" value={stats.buckets?.d90 ?? 0} color="text-white/40"   icon={Clock} />`
    - `<StatCard label="Divergência ML" value={stats.divergencia_ml ?? '—'} color="text-amber-400" icon={AlertCircle} />` com wrapper `<div title="Sellers em BASE_VENDEDORES sem cadastro ContatosCPP — divergência de cadastro ML">...</div>`

    Badge offline:
    ```
    {stats.source === 'local' && (
        <span className="ml-2 inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-orange-500/10 border border-orange-500/30 text-orange-300 text-[10px] font-semibold">
            <WifiOff size={10} /> API offline
        </span>
    )}
    ```
    Coloca-se dentro do `<div className="flex items-center gap-1.5">` (atual linha 180-183), ao lado do "Info" texto.
  </behavior>
  <action>
    1. Editar `resources/js/Pages/Grants/Index.jsx`:
       a) Linha 11 — atualizar import lucide-react adicionando `WifiOff` e `AlertCircle`:
          ```
          import { ShieldCheck, Pencil, Trash2, RefreshCw, AlertTriangle, AlertCircle, CheckCircle2, Clock, XCircle, Building2, Info, Wifi, WifiOff, RotateCcw } from 'lucide-react';
          ```
       b) Após o grid atual de 5 StatCards (após linha 201), inserir NOVO grid de 6 cards (5 buckets + divergência) com classe `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-3` envolvendo os JSX do <behavior>.
       c) Para o card de divergência, envelopar com `<div title="...">` para tooltip nativo.
       d) Dentro do `<div className="flex items-center gap-1.5">` (linha 180), após o texto `Lista importada do ECF Drive (API HTTP) automaticamente às 03:00.`, acrescentar o JSX condicional do badge "API offline" descrito em <behavior>.
    2. NÃO mexer em nada mais (sync controls, expiring_soon panel, dialogs).
    3. Rodar `npm run build` — DEVE compilar sem warnings de variável não usada e sem erros.
    4. Smoke visual em dev (`npm run dev` paralelo + `php artisan serve` ou apenas reload via Vite): abrir `/grants`. Verificar:
       - Linha de 5 StatCards original aparece como antes
       - Linha NOVA de 6 cards aparece logo abaixo
       - Badge "API offline" aparece SE stats.source==='local' (forçar via .env temporário com chave inválida)
  </action>
  <verify>
    <automated>npm run build</automated>
    Build verde, zero warning de identifier não usado, zero erro de import. Inspeção visual confirma layout.
  </verify>
  <done>2 grids de StatCards (5 + 6); badge "API offline" condicional; build verde; layout não regrediu</done>
</task>

<task type="auto">
  <name>Tarefa 3: Grants/Index.jsx — 4 colunas opcionais na tabela de grants</name>
  <files>
    - resources/js/Pages/Grants/Index.jsx
  </files>
  <read_first>
    - resources/js/Pages/Grants/Index.jsx (linhas 239-296) — header da tabela + map() das linhas; ATUAL: `grid-cols-[1fr_7rem_8rem_8rem_6rem_7rem_7rem_5rem]` com 8 colunas
    - resources/js/Pages/Grants/Index.jsx (linhas 263-271) — exemplo de como segmento atualmente é renderizado com tooltip via title
  </read_first>
  <behavior>
    Adicionar 4 colunas opcionais ao final da tabela ANTES da coluna de Ações:

    - Programa
    - Nível
    - Medalha In
    - Medalha Out

    Decisão de design: largura total cresce; mudar grid para acomodar.
    Grid novo: `grid-cols-[1fr_7rem_8rem_8rem_6rem_7rem_7rem_6rem_6rem_6rem_6rem_5rem]` (12 colunas).

    Cada célula renderiza valor ou `'—'` quando NULL (mesmo pattern de ml_email/ml_phone linhas 262-263).

    Para colunas que podem ter strings longas (Programa, Nível), adicionar `title={g.programa || ''}` + `truncate` (espelha o tratamento atual do segmento).

    NÃO renderizar as colunas como "ocultas quando todas NULL" — operador locked em CONTEXT.md linha 56 diz: "ocultar célula quando NULL pra não poluir" — interpretação prática: célula mostra '—' fino (text-white/30) quando vazio, tabela toda permanece. Não ocultar coluna inteira (quebra alinhamento do grid Tailwind).
  </behavior>
  <action>
    1. Editar `resources/js/Pages/Grants/Index.jsx`:
       a) Linha 239 — substituir grid class do header de:
          `grid grid-cols-[1fr_7rem_8rem_8rem_6rem_7rem_7rem_5rem] gap-3 px-5 py-2.5 ...`
          para:
          `grid grid-cols-[1fr_7rem_8rem_8rem_6rem_7rem_7rem_6rem_6rem_6rem_6rem_5rem] gap-3 px-5 py-2.5 ...`
       b) Após `<span>Expira</span>` (linha 246), antes de `<span className="text-right">Ações</span>` (linha 247), inserir:
          ```
          <span>Programa</span>
          <span>Nível</span>
          <span>Medalha In</span>
          <span>Medalha Out</span>
          ```
       c) Linha 250 — substituir grid class do body row pelo mesmo grid novo.
       d) Após `<span className="text-white/50 text-[12px]">{g.expires_at || '—'}</span>` (linha 271), antes da div de Ações (linha 272), inserir:
          ```
          <span className="text-white/50 text-[12px] truncate" title={g.programa || ''}>{g.programa || '—'}</span>
          <span className="text-white/50 text-[12px] truncate" title={g.nivel_solucion || ''}>{g.nivel_solucion || '—'}</span>
          <span className="text-white/50 text-[12px]">{g.medalha_fecha_in || '—'}</span>
          <span className="text-white/50 text-[12px]">{g.medalha_fecha_out || '—'}</span>
          ```
    2. Rodar `npm run build` — verde.
    3. Smoke visual: abrir `/grants` em dev. Verificar:
       - Tabela tem 4 colunas novas
       - Linhas de grants sincronizados pós-Phase 51 mostram valores; grants legacy mostram '—'
       - Header alinha com body (grid funciona)
       - Tooltip nativo aparece ao hover em Programa/Nível truncado
  </action>
  <verify>
    <automated>npm run build</automated>
    Build verde; inspeção visual confirma grid de 12 colunas alinha header e body.
  </verify>
  <done>4 colunas opcionais adicionadas; grid expandido; valores ou '—' renderizam corretamente; tooltips em Programa/Nível</done>
</task>

</tasks>

<verification>
1. `php artisan test --filter=Phase51\\GrantsResumoTest` → sem regressão (Wave 2 intacto)
2. `npm run build` → verde, zero warning
3. Smoke `/grants` em dev:
   a) Linha original de 5 StatCards intacta
   b) Linha nova de 6 cards (5 buckets + Divergência ML) renderiza
   c) Cores progressivas vermelho → laranja → amarelo → cinza claro → cinza escuro funcionam
   d) Tooltip do card Divergência ML aparece no hover
   e) Tabela mostra 4 colunas extras com alinhamento OK
   f) Forçar fallback (chave ECF inválida) → badge "API offline" aparece no header e stats.source vira 'local'
4. Rodar `php artisan grants:sync-ecf` em dev (uma execução real ou Http::fake em script tinker) → recarregar `/grants` → grants resincados exibem valores Programa/Nível na tabela
</verification>

<success_criteria>
- [ ] `GrantController::index` map() de grants passa 4 chaves novas (programa, nivel_solucion, medalha_fecha_in, medalha_fecha_out)
- [ ] Grid novo de 6 StatCards (5 buckets 7/15/30/60/90d + Divergência ML) renderiza abaixo dos 5 cards originais
- [ ] Cores progressivas dos buckets seguem RESEARCH §5 (red-400 → orange-400 → ecf-yellow → white/60 → white/40)
- [ ] Card Divergência ML tem tooltip via `title=` explicando o significado (sellers BASE_VENDEDORES sem ContatosCPP)
- [ ] Badge "API offline" renderiza quando `stats.source === 'local'`, com ícone WifiOff
- [ ] Tabela ganha 4 colunas opcionais (Programa, Nível, Medalha In, Medalha Out); grid expandido para 12 colunas
- [ ] `npm run build` verde, sem warnings de identifier não usado
- [ ] Layout existente (sync alerts, expiring_soon panel, dialogs Edit/Regrant) não regrediu
</success_criteria>

<output>
Criar `.planning/phases/51-reestruturacao-grants-nova-api-ecf-drive/51-03-SUMMARY.md` ao final com: nome do componente local reutilizado, lista de classes Tailwind usadas para cores progressivas, decisão final de exibir '—' (não ocultar) para colunas NULL, screenshot path (se gerado), e qualquer ajuste no grid devido a quebras em viewports menores. Próximo: Wave 4 (51-04-PLAN.md — UAT humano em prod).
</output>
