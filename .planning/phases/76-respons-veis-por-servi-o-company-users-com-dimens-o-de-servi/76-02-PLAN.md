---
phase: 76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - app/Models/Company.php
  - app/Models/User.php
  - tests/Feature/V16/ResponsaveisConsolidadoInvarianteTest.php
  - tests/Feature/V16/CarteiraBonusNaoDobraTest.php
autonomous: true
requirements: ["DEC-A2"]

must_haves:
  truths:
    - "Company::consultor()/estrategista() retornam o MESMO responsável de hoje mesmo com uma 2ª linha (servico_id Shopee) presente"
    - "User::companies()/consultorCompanies()/estrategistaCompanies() contam a empresa 1x mesmo com linhas ML+Shopee do mesmo par"
    - "Existem variantes service-aware (por servico_id) sem quebrar as relações consolidadas"
    - "A carteira que alimenta o bônus não dobra quando a empresa tem responsável em 2 serviços"
  artifacts:
    - path: "app/Models/Company.php"
      provides: "consultor()/estrategista() com ->distinct() + variantes consultorDoServico()/estrategistaDoServico()"
      contains: "distinct"
    - path: "app/Models/User.php"
      provides: "companies()/consultorCompanies()/estrategistaCompanies() com select('companies.*')->distinct()"
      contains: "companies.*"
    - path: "tests/Feature/V16/ResponsaveisConsolidadoInvarianteTest.php"
      provides: "Prova invariante consolidado com linha Shopee simulada"
    - path: "tests/Feature/V16/CarteiraBonusNaoDobraTest.php"
      provides: "Prova carteira do bônus conta empresa 1x com ML+Shopee"
  key_links:
    - from: "app/Models/User.php::companies"
      to: "company_users"
      via: "select('companies.*')->distinct() — dedup robusto a assigned_at divergente"
      pattern: "select\\('companies\\.\\*'\\)"
    - from: "app/Models/Company.php::consultor"
      to: "company_users"
      via: "wherePivot('role','consultor')->distinct() sem servico_id no SELECT"
      pattern: "distinct"
---

<objective>
Blindar a leitura consolidada (DEC-A2): `Company::consultor()/estrategista()` e as carteiras
de `User` continuam retornando o responsável consolidado de hoje, mas com dedup defensivo
(`->distinct()` / `->select('companies.*')->distinct()`) para não double-contar quando a Phase 78
adicionar a linha Shopee. Adiciona variantes service-aware (não usadas pelos leitores atuais,
consumidas pela Phase 78). Prova o invariante e a não-duplicação da carteira do bônus por teste.

Purpose: Garantir que nenhum dos ~15 leitores consolidados (carteira → bônus) mude de resultado.
NÃO reescrever lógica de bônus aqui.

Output: Company.php e User.php com relações blindadas + variantes; 2 testes Feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-CONTEXT.md
@.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-RESEARCH.md

<interfaces>
<!-- Estado atual das relações (extraído do código). Editar in loco. -->

app/Models/Company.php:157-179
  users()       => belongsToMany(User, 'company_users')->withPivot('role','assigned_at')->withTimestamps()
  consultor()   => belongsToMany(User, 'company_users')->wherePivot('role','consultor')
  estrategista()=> belongsToMany(User, 'company_users')->wherePivot('role','estrategista')

app/Models/User.php:195-218
  companies()            => belongsToMany(Company, 'company_users')->withPivot('role','assigned_at')->withTimestamps()
  consultorCompanies()   => belongsToMany(Company, 'company_users')->wherePivot('role','consultor')->withPivot('role','assigned_at')
  estrategistaCompanies()=> belongsToMany(Company, 'company_users')->wherePivot('role','estrategista')->withPivot('role','assigned_at')

RESEARCH Pattern 3 / Pitfall 4:
  - consultor()/estrategista() NÃO fazem withPivot('servico_id') → servico_id fica FORA do SELECT →
    ->distinct() colapsa ML+Shopee para 1 linha.
  - companies() tem withPivot('assigned_at') → assigned_at divergente entre linhas fura o distinct →
    precisa ->select('companies.*')->distinct().
  - Assumption A2 do RESEARCH: nenhum consumidor de companies() lê ->pivot->role dessa relação (só
    pluck('companies.id')/get/exists) — confirmar por grep antes de aplicar o select('companies.*').

Trait de fixture (Plano 01): tests/Feature/V16/CriaCenarioResponsaveis.php
  - criarCenarioMlComResponsaveis(): array; inserirLinhaShopee(companyId,userId,role): array
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Blindar relações consolidadas + variantes service-aware</name>
  <read_first>
    - app/Models/Company.php:157-179 (users/consultor/estrategista)
    - app/Models/User.php:195-218 (companies/consultorCompanies/estrategistaCompanies)
    - .planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-RESEARCH.md (Pattern 3, Pitfall 4, Assumption A2)
  </read_first>
  <files>app/Models/Company.php, app/Models/User.php</files>
  <action>
    ANTES de editar User::companies(): rodar grep por consumidores que leem `->pivot->role`/`->pivot->assigned_at`
    a partir de $user->companies. Achado já mapeado (W3 do plan-checker): o ÚNICO consumidor de `->pivot->role`
    dessa relação é `PortfolioController` (~:670 declara `$user->companies()->withPivot('role')` e ~:849 lê
    `$c->pivot->role`) — e esse call-site **re-declara** o `withPivot('role')` por conta própria; **nenhum** consumidor
    lê `->pivot->assigned_at`. Portanto:
    Em User.php — na base `companies()`: aplicar `->select('companies.*')->distinct()` e **REMOVER** o
    `withPivot('assigned_at')` da base (ele reinjeta assigned_at no SELECT e FURA o distinct — Pitfall 4/W1;
    ninguém consome assigned_at). Manter apenas o mínimo necessário; call-sites como o PortfolioController
    continuam re-declarando `withPivot('role')` quando precisam do papel. Em `consultorCompanies()`/`estrategistaCompanies()`
    aplicar `->select('companies.*')->distinct()` (o role é fixado pelo wherePivot, não precisa vir no SELECT).
    Se o grep revelar um consumidor NOVO de assigned_at, aí sim manter o pivot e deduplicar via `groupBy('companies.id')`
    — documentar em comentário pt-BR.
    Em Company.php: acrescentar `->distinct()` a consultor() e estrategista() (servico_id não está no SELECT →
    colapsa corretamente). Adicionar 2 variantes service-aware: consultorDoServico(int $servicoId) e
    estrategistaDoServico(int $servicoId) — belongsToMany com wherePivot('role',...) + wherePivot('servico_id',$servicoId).
    Comentar em pt-BR que são para a aba Shopee (Phase 78) e que os leitores atuais NÃO as usam.
    NÃO adicionar withPivot('servico_id') nas relações consolidadas (o attach persiste servico_id independente disso).
    Rodar `--filter=Portfolio` após a edição para confirmar que o `pivot->role` do PortfolioController continua resolvendo.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16 --stop-on-failure 2>&1 | tail -6</automated>
  </verify>
  <done>consultor()/estrategista() com distinct; companies()/consultorCompanies()/estrategistaCompanies() com select('companies.*')->distinct(); variantes *DoServico() presentes; Assumption A2 confirmada por grep e documentada.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Testes de invariante consolidado + carteira não dobra</name>
  <read_first>
    - tests/Feature/V16/CriaCenarioResponsaveis.php (helpers de cenário)
    - app/Models/Company.php (relações editadas na Tarefa 1)
    - app/Models/User.php (carteiras editadas na Tarefa 1)
    - .planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-VALIDATION.md (provas c)
  </read_first>
  <files>tests/Feature/V16/ResponsaveisConsolidadoInvarianteTest.php, tests/Feature/V16/CarteiraBonusNaoDobraTest.php</files>
  <behavior>
    - ResponsaveisConsolidadoInvarianteTest: dado o cenário ML (1 empresa, analista+estrategista na pivot com
      servico_id performance), Company::consultor()->count()==1 e estrategista()->count()==1. Após inserirLinhaShopee
      (mesma empresa, MESMO analista, role 'consultor', servico_id shopee), Company::consultor()->count() CONTINUA 1
      (dedup) e retorna o mesmo user id. consultorDoServico(servicoShopee)->count()==1 e consultorDoServico(servicoPerf)->count()==1
      (as variantes distinguem por serviço).
    - CarteiraBonusNaoDobraTest: dado o analista com 1 empresa na carteira (linha performance), $analista->companies()->count()==1,
      consultorCompanies()->count()==1. Após inserirLinhaShopee para a MESMA empresa+analista com assigned_at DIFERENTE,
      companies()->count() CONTINUA 1 e consultorCompanies()->count() CONTINUA 1 (prova o select('companies.*')->distinct()
      contra o Pitfall 4). Contar via ->count() e via ->get()->count() para cobrir os dois caminhos.
  </behavior>
  <action>
    Escrever os 2 testes Feature no namespace Tests\Feature\V16 usando RefreshDatabase + trait CriaCenarioResponsaveis.
    O ponto crítico do CarteiraBonusNaoDobraTest é inserir a linha Shopee com assigned_at divergente (ex.: now()->subDays(3))
    para exercitar exatamente o furo do distinct descrito no Pitfall 4. Asserts com mensagens pt-BR explicando o invariante.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16/ResponsaveisConsolidadoInvarianteTest.php tests/Feature/V16/CarteiraBonusNaoDobraTest.php --stop-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>Ambos os testes verdes: consolidado imutável com linha Shopee; carteira conta 1x com assigned_at divergente; variantes service-aware distinguem por servico_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pivot company_users → leitores da carteira/bônus | Duplicação de linha (ML+Shopee) poderia dobrar a carteira e o bônus se a leitura não deduplicar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04 | Tampering | User::companies() / carteira do bônus | mitigate | select('companies.*')->distinct() (Pitfall 4); provado por CarteiraBonusNaoDobraTest com assigned_at divergente |
| T-76-06 | Information Disclosure | Company::consultor()/estrategista() | mitigate | ->distinct() mantém 1 responsável consolidado; invariante provado com linha Shopee simulada |
| T-76-SC | Tampering | composer/npm installs | accept | Nenhum pacote instalado nesta fase (sem libs novas) |
</threat_model>

<verification>
- `php artisan test tests/Feature/V16 --stop-on-failure` verde.
- Assumption A2 confirmada: nenhum consumidor lê `->pivot` de User::companies() (ou tratado caso a caso).
</verification>

<success_criteria>
- Relações consolidadas blindadas por dedup; variantes service-aware disponíveis.
- Invariante consolidado e não-duplicação da carteira provados por teste.
- Nenhuma lógica de bônus reescrita.
</success_criteria>

<output>
Criar `.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-02-SUMMARY.md` ao concluir.
</output>
