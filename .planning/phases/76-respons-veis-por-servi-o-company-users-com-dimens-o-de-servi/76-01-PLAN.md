---
phase: 76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_14_000001_add_servico_id_to_company_users.php
  - tests/Feature/V16/CriaCenarioResponsaveis.php
  - tests/Feature/V16/MigrationCompanyUsersServicoTest.php
  - tests/Feature/V16/DataMigrationServicoTest.php
autonomous: true
requirements: ["DEC-A1"]

must_haves:
  truths:
    - "A coluna company_users.servico_id existe, nullable, e o unique passa a (company_id, user_id, role, servico_id)"
    - "Duas linhas consolidadas (servico_id NULL) do mesmo (company, user, role) coexistem sem violar o unique"
    - "Linha de empresa COM contrato performance ativo recebe o servico_id desse serviço; empresa SEM performance permanece NULL"
    - "Rodar a data-migration 2x não altera linhas já migradas (idempotente via whereNull)"
  artifacts:
    - path: "database/migrations/2026_07_14_000001_add_servico_id_to_company_users.php"
      provides: "Migration cross-driver em passos: add coluna nullable, FK só no MySQL, swap do unique, data-migration idempotente"
      contains: "migrarLinhasExistentes"
      min_lines: 60
    - path: "tests/Feature/V16/CriaCenarioResponsaveis.php"
      provides: "Trait de fixture (DB::table) — serviço performance/shopee + contrato ativo + users na pivot"
      min_lines: 40
    - path: "tests/Feature/V16/MigrationCompanyUsersServicoTest.php"
      provides: "Prova migrate:fresh + unique novo + coexistência de NULLs"
    - path: "tests/Feature/V16/DataMigrationServicoTest.php"
      provides: "Prova data-migration performance→servico_id e idempotência"
  key_links:
    - from: "migration.migrarLinhasExistentes"
      to: "contratos_servico + servicos"
      via: "join+groupBy+pluck (1 query) filtrando s.setor='performance' e ct.ativo=true"
      pattern: "groupBy.*company_id"
    - from: "migration.up() branch FK"
      to: "servicos.id"
      via: "foreign() somente quando DB::getDriverName()==='mysql'"
      pattern: "getDriverName.*mysql"
---

<objective>
Fundação de schema da v16.0: dar à pivot `company_users` a dimensão de serviço
(`servico_id` nullable + FK `servicos`), trocar o unique de 3 para 4 colunas, e
migrar as linhas existentes para o `servico_id` do contrato performance ativo
(ou NULL = consolidado/legado). Estabelece também a infra de testes `tests/Feature/V16/`.

Purpose: Habilitar responsável por-serviço sem quebrar o comportamento consolidado.
Este plano é o GATE bloqueante da fase — prova o unique novo por `migrate:fresh` ANTES
de qualquer reescrita de leitura/escrita (DEC-A1).

Output: Migration cross-driver idempotente, trait de fixture V16 e 2 testes Feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-CONTEXT.md
@.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-RESEARCH.md
@.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-VALIDATION.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real — não explorar. -->

Padrão cross-driver por DB::getDriverName() já usado no projeto:
  database/migrations/2026_05_22_200001_rename_mentor_to_estrategista_in_company_users.php
  - unique atual: (company_id, user_id, role) → nome derivado
    'company_users_company_id_user_id_role_unique' (existe em MySQL E SQLite,
    porque a 2026_05_22 recriou o unique no branch SQLite).

Servico (app/Models/Servico.php):
  - const SETOR_PERFORMANCE = 'performance'; const SETOR_SHOPEE = 'shopee';
  - coluna: servicos.setor (string); scopeActive() => where('ativo', true)

ContratoServico (app/Models/ContratoServico.php):
  - colunas: company_id, servico_id, ativo (bool); scopeActive() => where('ativo', true)
  - NÃO tem coluna 'setor' — o setor vem de servicos.setor (join obrigatório)

phpunit.xml: DB_CONNECTION=sqlite, DB_DATABASE=:memory: (SQLite enforça constraints).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Trait de fixture V16 (CriaCenarioResponsaveis)</name>
  <read_first>
    - tests/ (conferir base TestCase e uso de RefreshDatabase no projeto)
    - app/Models/Servico.php (constantes SETOR_*, coluna setor)
    - app/Models/ContratoServico.php (colunas company_id, servico_id, ativo)
    - database/factories/CompanyFactory.php, database/factories/UserFactory.php (padrão de factory existente)
  </read_first>
  <files>tests/Feature/V16/CriaCenarioResponsaveis.php</files>
  <action>
    Criar o namespace Tests\Feature\V16 e o trait CriaCenarioResponsaveis usado pelos testes
    da fase (evita criar ServicoFactory/ContratoServicoFactory que não existem — usar DB::table).
    O trait expõe helpers pt-BR que constroem, via DB::table, o cenário mínimo reutilizável:
    (a) criarServico(string $setor, bool $ativo = true): int — insere em `servicos` (nome, setor,
    ativo) e retorna o id; (b) criarContrato(int $companyId, int $servicoId, bool $ativo = true): int
    — insere em `contratos_servico`; (c) criarCenarioMlComResponsaveis(): array — cria 1 Company
    (via CompanyFactory), 1 serviço performance ativo + contrato performance ativo, 1 user analista
    e 1 user estrategista, e insere as 2 linhas em `company_users` com servico_id do performance
    (role 'consultor' e 'estrategista'); retorna ['company','servicoPerf','analista','estrategista'].
    (d) inserirLinhaShopee(int $companyId, int $userId, string $role): array — cria (se preciso) serviço
    shopee ativo + contrato shopee ativo para a empresa e insere a linha company_users com esse servico_id;
    retorna ['servicoShopee','row']. Comentários em pt-BR. Não usar Eloquent para as inserções de pivot
    (usar DB::table para controlar servico_id explicitamente).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16 --stop-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>Trait existe no namespace Tests\Feature\V16; helpers criam serviço/contrato/pivot via DB::table; nenhuma factory nova criada.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Migration cross-driver + data-migration + testes bloqueantes</name>
  <read_first>
    - database/migrations/2026_05_22_200001_rename_mentor_to_estrategista_in_company_users.php (padrão branch por driver + nome do unique)
    - database/migrations/2026_04_26_152217_create_company_users_table.php (unique original 3-col)
    - .planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-RESEARCH.md (Pattern 1 e 2, Pitfalls 1/2/5)
    - tests/Feature/V16/CriaCenarioResponsaveis.php (trait da Tarefa 1)
  </read_first>
  <files>database/migrations/2026_07_14_000001_add_servico_id_to_company_users.php, tests/Feature/V16/MigrationCompanyUsersServicoTest.php, tests/Feature/V16/DataMigrationServicoTest.php</files>
  <behavior>
    - MigrationCompanyUsersServicoTest: após migrate, `company_users` tem coluna servico_id; inserir 2 linhas
      (company,user,role) ambas com servico_id NULL NÃO lança QueryException (múltiplos NULL coexistem no unique);
      inserir 2 linhas idênticas com o MESMO servico_id não-NULL LANÇA violação de unique.
    - DataMigrationServicoTest: empresa COM contrato performance ativo → após rodar a data-migration a linha
      company_users passa a ter servico_id = id do serviço performance; empresa SEM contrato performance →
      servico_id permanece NULL; a idempotência é provada assim (W2): a lógica de backfill vive num método
      **público** `migrarLinhasExistentes()` na migration (classe anônima retornada pelo arquivo); o teste faz
      `$m = require database_path('migrations/2026_07_14_000001_add_servico_id_to_company_users.php')` e chama
      `$m->migrarLinhasExistentes()` uma 2ª vez APÓS o migrate — asserta que 0 linhas mudaram (comparar snapshot
      de servico_id antes/depois, ou o retorno da contagem de linhas afetadas == 0, graças ao filtro whereNull).
    - Empresa com contrato performance INATIVO não deve ser migrada (permanece NULL).
  </behavior>
  <action>
    Escrever a migration `2026_07_14_000001_add_servico_id_to_company_users.php` seguindo o Pattern 1 do RESEARCH,
    em 4 passos separados dentro de up():
    (1) Schema::table add `unsignedBigInteger('servico_id')->nullable()->after('role')` + `index('servico_id')`.
    NÃO encadear ->constrained() (SQLite não adiciona FK em ALTER — Pitfall 5).
    (2) Só quando `DB::getDriverName() === 'mysql'`: Schema::table adiciona
    `foreign('servico_id')->references('id')->on('servicos')->nullOnDelete()`.
    (3) Swap do unique: Schema::table `dropUnique(['company_id','user_id','role'])` (nome derivado
    company_users_company_id_user_id_role_unique) seguido de `unique(['company_id','user_id','role','servico_id'])`.
    (4) Chamar `$this->migrarLinhasExistentes()` — método **público** (W2: precisa ser invocável pelo teste de
    idempotência) que monta o mapa company_id→servico_id em UMA query
    (join contratos_servico ct + servicos s, where ct.ativo=true e s.setor='performance', groupBy ct.company_id,
    pluck de MIN(ct.servico_id) por company_id) e faz UPDATE em company_users filtrado por `whereNull('servico_id')`
    para cada empresa do mapa. Empresas sem performance ativo permanecem NULL. NUNCA usar `where('servico_id', null)`
    (Pitfall 1) — sempre whereNull.
    down(): reverter em ordem inversa — `update(['servico_id'=>null])`, drop unique 4-col, restaurar unique 3-col,
    (só MySQL) dropForeign, dropColumn. Comentários em pt-BR explicando cada passo e a regra "nunca inventar serviço".
    Escrever os 2 testes usando o trait CriaCenarioResponsaveis + RefreshDatabase.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --env=testing >/dev/null 2>&1; php artisan test tests/Feature/V16/MigrationCompanyUsersServicoTest.php tests/Feature/V16/DataMigrationServicoTest.php --stop-on-failure 2>&1 | tail -12</automated>
  </verify>
  <done>migrate:fresh aplica a migration nos dois branches sem erro; unique 4-col ativo; múltiplos NULL coexistem; data-migration preenche performance e é idempotente. Ambos os testes verdes. GATE da fase liberado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → dados de produção | A data-migration reescreve servico_id de linhas de responsáveis existentes — corromper aqui derruba carteira/bônus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03 | Tampering | data-migration `migrarLinhasExistentes` | mitigate | UPDATE filtrado por whereNull (idempotente); nunca inventa serviço (empresa sem performance → NULL); down() reversível; provado por DataMigrationServicoTest |
| T-76-05 | Denial of Service | ALTER cross-driver | mitigate | FK só no branch MySQL; SQLite exercita constraints nos testes via migrate:fresh (Pitfall 5) |
| T-76-SC | Tampering | npm/pip/composer installs | accept | Nenhum pacote instalado nesta fase (constraint CONTEXT: "sem libs novas") |
</threat_model>

<verification>
- `php artisan migrate:fresh --env=testing` roda sem erro (branch SQLite).
- `php artisan test tests/Feature/V16 --stop-on-failure` verde.
- MANUAL (pós-deploy, fora desta fase): validar branch MySQL da FK no VPS — `migrate` + conferir constraint `servico_id` em `company_users` (SQLite não exercita FK-em-ALTER).
</verification>

<success_criteria>
- Coluna servico_id nullable + FK (MySQL) + unique 4-col em `company_users`.
- Data-migration idempotente performance→servico_id (NULL quando sem performance).
- Trait de fixture V16 + 2 testes Feature verdes.
- GATE bloqueante liberado para os planos 02/03.
</success_criteria>

<output>
Criar `.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-01-SUMMARY.md` ao concluir.
</output>
