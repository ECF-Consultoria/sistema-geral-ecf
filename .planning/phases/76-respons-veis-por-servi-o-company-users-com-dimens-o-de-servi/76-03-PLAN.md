---
phase: 76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi
plan: 03
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - app/Http/Controllers/ShopeeEmpresasController.php
  - app/Http/Controllers/CompanyController.php
  - tests/Feature/V16/AtribuicaoPorServicoIsolamentoTest.php
autonomous: true
requirements: ["DEC-A3"]

must_haves:
  truths:
    - "ShopeeEmpresasController::bulkAssign grava/apaga só o slot (company_id, role, servico_id=shopee) — não toca linhas ML"
    - "CompanyController::bulkAssign e update sync gravam com servico_id performance (ou NULL consolidado p/ ML puro) — não tocam linhas Shopee"
    - "O detach() total em CompanyController::update foi substituído por detach escopado ao slot performance/consolidado"
    - "Atribuir Shopee nunca apaga o responsável ML e vice-versa (isolamento por servico_id)"
    - "O guard anti-IDOR da Phase 75 em ShopeeEmpresasController continua ativo"
  artifacts:
    - path: "app/Http/Controllers/ShopeeEmpresasController.php"
      provides: "bulkAssign escopado por servico_id shopee, guard IDOR preservado"
      contains: "servico_id"
    - path: "app/Http/Controllers/CompanyController.php"
      provides: "bulkAssign + update sync escopados por servico_id performance/consolidado (whereNull)"
      contains: "whereNull"
    - path: "tests/Feature/V16/AtribuicaoPorServicoIsolamentoTest.php"
      provides: "Prova isolamento ML×Shopee nas 3 escritas"
  key_links:
    - from: "ShopeeEmpresasController::bulkAssign"
      to: "company_users (servico_id shopee)"
      via: "delete filtrado por (company_id, role, servico_id shopee) + attach com servico_id"
      pattern: "where\\('servico_id'"
    - from: "CompanyController::update"
      to: "company_users (slot performance/consolidado)"
      via: "detach escopado (whereNull ou servico_id performance) — nunca detach() total"
      pattern: "whereNull\\('servico_id'\\)"
---

<objective>
Reescrever as 3 escritas de atribuição (DEC-A3) para operarem escopadas por `servico_id`,
corrigindo o risco da Phase 75 (hoje apagam por `(company_id, role)` e o `update` faz `detach()`
de TUDO — sobrescreveriam o responsável do outro canal). ShopeeEmpresasController::bulkAssign grava
com o servico_id do contrato Shopee ativo; CompanyController::bulkAssign e update sync gravam com o
servico_id do contrato performance ativo (ou NULL consolidado para empresas ML puras). Preserva o
guard anti-IDOR da Phase 75.

Purpose: Atribuir Shopee nunca apaga o responsável ML e vice-versa.
Output: 2 controllers reescritos + teste de isolamento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-CONTEXT.md
@.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-RESEARCH.md

<interfaces>
<!-- Estado atual das 3 escritas (extraído do código). -->

app/Http/Controllers/ShopeeEmpresasController.php:160-186 (bulkAssign)
  - validate: ids[] (exists + closure guard IDOR empresasShopeeBaseQuery() :168-172), role in:consultor,estrategista, user_id exists
  - loop: DB::table('company_users')->where('company_id',$c->id)->where('role',$data['role'])->delete();  // :180 apaga TUDO do role
           $c->users()->attach($data['user_id'], ['role'=>..., 'assigned_at'=>now()->toDateString()]);

app/Http/Controllers/CompanyController.php:683-700 (bulkAssign) — mesmo padrão destrutivo em :694
app/Http/Controllers/CompanyController.php:617-628 (update sync)
  - monta $sync[consultor_id]/[estrategista_id]; if(!empty($sync)){ $company->users()->detach(); $company->users()->attach($sync); }  // :626 detach() de TUDO

RESEARCH Pattern 4 / Pitfalls 1 e 3:
  - servico_id shopee: DB::table('contratos_servico as ct')->join('servicos as s','s.id','=','ct.servico_id')
      ->where('ct.company_id',$id)->where('ct.ativo',true)->where('s.setor','shopee')->value('ct.servico_id')
  - servico_id performance: idem com s.setor='performance'; pode ser NULL (ML puro) → usar whereNull no filtro.
  - GOTCHA (Pitfall 1): ->where('servico_id', null) NUNCA casa. Usar whereNull/whereNotNull.
  - GOTCHA (Pitfall 3): substituir detach() total por detach escopado ao slot performance/consolidado.
  - attach com servico_id no array de pivot persiste a coluna independe de withPivot.

Servico::SETOR_PERFORMANCE='performance', SETOR_SHOPEE='shopee'.
Trait de fixture (Plano 01): tests/Feature/V16/CriaCenarioResponsaveis.php
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: bulkAssign Shopee escopado por servico_id (guard IDOR preservado)</name>
  <read_first>
    - app/Http/Controllers/ShopeeEmpresasController.php:150-187 (bulkAssign + guard :168-172 + empresasShopeeBaseQuery)
    - .planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-RESEARCH.md (Pattern 4, Security Domain)
  </read_first>
  <files>app/Http/Controllers/ShopeeEmpresasController.php</files>
  <action>
    Reescrever o loop de bulkAssign (:178-182) para resolver, por empresa, o servico_id do contrato Shopee ATIVO
    (join contratos_servico + servicos where s.setor='shopee' e ct.ativo=true, ->value('ct.servico_id')). O guard IDOR
    (:168-172) já garante que a empresa tem escopo Shopee, mas se o servico_id resolvido vier null, pular a empresa
    (não gravar linha órfã). Apagar SÓ o slot Shopee: DB::table('company_users')->where('company_id',$c->id)
    ->where('role',$data['role'])->where('servico_id',$servicoShopeeId)->delete() — nunca apagar por (company_id, role)
    apenas. Atribuir via $c->users()->attach($data['user_id'], ['role'=>$data['role'], 'servico_id'=>$servicoShopeeId,
    'assigned_at'=>now()->toDateString()]). NÃO alterar a validação nem o closure guard anti-IDOR (V4 Access Control —
    preservar fail-closed). Comentários em pt-BR explicando o escopo por serviço.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16 --stop-on-failure 2>&1 | tail -6</automated>
  </verify>
  <done>bulkAssign Shopee apaga/grava só o slot servico_id shopee; guard IDOR :168-172 intacto; empresa sem contrato shopee ativo é pulada.</done>
</task>

<task type="auto">
  <name>Tarefa 2: CompanyController bulkAssign + update sync escopados (performance/consolidado)</name>
  <read_first>
    - app/Http/Controllers/CompanyController.php:600-700 (update sync :617-628 + bulkAssign :683-700)
    - .planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-RESEARCH.md (Pattern 4, Pitfalls 1 e 3, Open Question 1)
  </read_first>
  <files>app/Http/Controllers/CompanyController.php</files>
  <action>
    Criar um helper privado pt-BR (ex.: servicoPerformanceAtivoId(Company $c): ?int) que resolve o servico_id do contrato
    performance ATIVO (join contratos_servico + servicos where s.setor='performance' e ct.ativo=true, ->value). Retorna null
    para empresa ML pura sem contrato performance (Open Question 1: gravar servico_id performance quando existir, senão NULL
    consolidado — consistente com a data-migration do Plano 01).
    (a) bulkAssign (:692-696): substituir o delete por (company_id, role) por delete escopado — se $servicoMlId===null usar
    whereNull('servico_id'), senão where('servico_id',$servicoMlId) (Pitfall 1). attach com 'servico_id'=>$servicoMlId no array de pivot.
    (b) update sync (:625-628): substituir `$company->users()->detach()` (apaga TUDO — Pitfall 3) por detach ESCOPADO ao slot
    performance/consolidado: DB::table('company_users')->where('company_id',$company->id)->whereIn('role',['consultor','estrategista'])
    e filtrar por servico_id (whereNull quando null, where quando não-null) antes do delete; depois attach do $sync com
    'servico_id'=>$servicoMlId em cada linha. NUNCA tocar linhas com servico_id de outros serviços (Shopee). Comentar pt-BR.
    Se $sync vazio, não apagar nada (preservar comportamento atual de não mexer quando nada informado).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16 --stop-on-failure 2>&1 | tail -6</automated>
  </verify>
  <done>bulkAssign e update sync operam escopados por servico_id performance/consolidado (whereNull p/ NULL); detach() total eliminado; linhas Shopee nunca são apagadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: Teste de isolamento ML×Shopee nas 3 escritas</name>
  <read_first>
    - tests/Feature/V16/CriaCenarioResponsaveis.php (cenário + inserirLinhaShopee)
    - app/Http/Controllers/ShopeeEmpresasController.php (bulkAssign editado)
    - app/Http/Controllers/CompanyController.php (bulkAssign + update editados)
    - .planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-VALIDATION.md (prova d)
  </read_first>
  <files>tests/Feature/V16/AtribuicaoPorServicoIsolamentoTest.php</files>
  <behavior>
    - Dado empresa com contrato performance + shopee ativos, responsável ML (servico_id performance) e responsável Shopee
      (servico_id shopee) já na pivot: chamar ShopeeEmpresasController::bulkAssign trocando o responsável Shopee NÃO altera a
      linha ML (a linha servico_id performance permanece com o mesmo user_id).
    - Chamar CompanyController::bulkAssign (ou update) trocando o responsável performance NÃO altera a linha Shopee.
    - Após re-atribuição consolidada (servico_id NULL) repetida NÃO restam 2 linhas NULL do mesmo (company, role) — prova o
      whereNull do slot consolidado (Pitfall 1).
    - Guard IDOR: bulkAssign Shopee com id de empresa fora do escopo Shopee retorna 422 e não grava nada.
  </behavior>
  <action>
    Escrever AtribuicaoPorServicoIsolamentoTest no namespace Tests\Feature\V16 (RefreshDatabase + trait). Exercitar as rotas
    reais das 3 escritas via chamadas HTTP autenticadas (actingAs) OU invocando os controllers diretamente com Request forjado —
    preferir chamada HTTP às rotas nomeadas para cobrir validação + guard. Asserir por DB::table('company_users') o count e o
    user_id de cada slot (servico_id performance / shopee / NULL) antes e depois. Mensagens pt-BR. Cobrir o caso 422 do guard IDOR.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16/AtribuicaoPorServicoIsolamentoTest.php --stop-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>Isolamento provado: atribuir Shopee não toca ML e vice-versa; sem duplicata NULL; guard IDOR 422 mantido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → endpoints de atribuição (bulkAssign/update) | Entrada não confiável define empresa/role/responsável; escopo Shopee é RBAC-sensível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Elevation/Tampering | ShopeeEmpresasController::bulkAssign (IDOR) | mitigate | Preservar closure guard :168-172 (empresasShopeeBaseQuery, fail-closed 422); teste cobre id fora do escopo |
| T-76-02 | Tampering | escrita cross-serviço (ML apaga Shopee / vice-versa) | mitigate | delete/attach escopados por (company_id, role, servico_id); detach() total eliminado; provado por AtribuicaoPorServicoIsolamentoTest |
| T-76-07 | Tampering | slot consolidado servico_id NULL | mitigate | whereNull em todo filtro do slot NULL (Pitfall 1); delete-then-attach mantido (múltiplos NULL não colidem no unique — Pitfall 2) |
| T-76-SC | Tampering | composer/npm installs | accept | Nenhum pacote instalado nesta fase (sem libs novas) |
</threat_model>

<verification>
- `php artisan test tests/Feature/V16 --stop-on-failure` verde.
- Validação de entrada das 3 escritas (ids exists, role in-list, user_id exists) preservada (V5).
- Guard anti-IDOR (V4) preservado — teste cobre 422 fora do escopo.
</verification>

<success_criteria>
- 3 escritas escopadas por servico_id (whereNull para o slot consolidado).
- detach() total substituído por detach escopado.
- Isolamento ML×Shopee e guard IDOR provados por teste.
</success_criteria>

<output>
Criar `.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-03-SUMMARY.md` ao concluir.
</output>
