---
phase: 76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi
plan: 04
type: execute
wave: 3
depends_on: ["76-01", "76-02", "76-03"]
files_modified:
  - tests/Feature/V16/LeitoresConsolidadoRegressaoTest.php
autonomous: true
requirements: ["DEC-A2"]

must_haves:
  truths:
    - "Leitores Grupo A (NpsDispararMensal, Goal/CalculateGoalResults, Dashboard self-join) retornam o responsável consolidado idêntico mesmo com linha Shopee presente"
    - "Leitores Grupo B (DesempenhoScoreService, PortfolioController, PortfolioGoal, NpsPendingService) contam a carteira 1x por empresa"
    - "A suite completa de regressão (Portfolio/Desempenho/Nps) passa verde — nenhum dos ~15 leitores regrediu"
  artifacts:
    - path: "tests/Feature/V16/LeitoresConsolidadoRegressaoTest.php"
      provides: "Regressão dos leitores Grupo A/B com linha Shopee simulada"
  key_links:
    - from: "LeitoresConsolidadoRegressaoTest"
      to: "NpsDispararMensal / DesempenhoScoreService / PortfolioGoal"
      via: "cenário ML+Shopee → asserir responsável consolidado 1x"
      pattern: "->count\\(\\)"
---

<objective>
Prova final do invariante DEC-A2 nos leitores reais de responsável/carteira e phase gate:
com o cenário ML+Shopee (linha duplicada por serviço), os leitores do Grupo A (leem
consultor()/estrategista()) e do Grupo B (carteira via companies()/*Companies()) mantêm o
MESMO resultado de hoje — o bônus não dobra. Roda também as suites de regressão existentes
(Portfolio/Desempenho/Nps) como gate da fase.

Purpose: Garantir que nenhum dos ~15 leitores consolidados regrediu após a dimensão de serviço.
Output: 1 teste de regressão Feature + confirmação da suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-RESEARCH.md
@.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-VALIDATION.md

<interfaces>
<!-- Leitores priorizados para regressão (RESEARCH § Leitores). -->

Grupo A (leem consultor()/estrategista()) — prioridade alta:
  - app/Console/Commands/NpsDispararMensal.php:198,206 (estrategista()->first(), consultor()->first())
  - app/Models/Goal.php:28-33 + app/Jobs/CalculateGoalResults.php:91-99 (with(['consultor','estrategista']); merge()->unique('id'))
  - app/Http/Controllers/DashboardController.php:680-688 (self-join company_users com ->distinct())

Grupo B (carteira via companies()/*Companies()) — vetor de bônus dobrado (crítico):
  - app/Services/DesempenhoScoreService.php:251,289 (companies()->get() iterado → carteira do bônus)
  - app/Http/Controllers/PortfolioController.php:517-518,615-616,1406 (*Companies()->get() iterado)
  - app/Models/PortfolioGoal.php:98-103 (whereHas('users', role)->get() iterado)
  - app/Services/NpsPendingService.php:157-162 (companies()->get())

Grupo C (dup-safe por construção — pluck('companies.id')->whereIn / exists): NÃO precisam assert dedicado.

Trait de fixture (Plano 01): tests/Feature/V16/CriaCenarioResponsaveis.php
Nota (RESEARCH Runtime State): cache do Desempenho (desempenho:*) só mascararia se a carteira mudasse —
o teste prova que NÃO muda; cache:clear é opcional no VPS, não obrigatório nesta fase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Regressão Grupo A/B com linha Shopee simulada</name>
  <read_first>
    - tests/Feature/V16/CriaCenarioResponsaveis.php (cenário + inserirLinhaShopee)
    - app/Console/Commands/NpsDispararMensal.php:190-212 (uso de consultor/estrategista first)
    - app/Services/DesempenhoScoreService.php:245-291 (companies()->get() → carteira)
    - app/Models/PortfolioGoal.php:90-104 (whereHas users role)
    - app/Models/Goal.php:28-33 + app/Jobs/CalculateGoalResults.php:91-99 (merge unique)
  </read_first>
  <files>tests/Feature/V16/LeitoresConsolidadoRegressaoTest.php</files>
  <behavior>
    - Cenário ML com analista+estrategista; capturar o responsável consolidado retornado por Company::consultor()->first()
      e estrategista()->first(). Após inserirLinhaShopee (mesma empresa, MESMO analista, role consultor, servico_id shopee),
      Company::consultor()->first()->id CONTINUA o mesmo e ->count()==1 (fonte de NpsDispararMensal).
    - Carteira Grupo B: para o analista, consultorCompanies()->get()->count()==1 e companies()->get()->count()==1 mesmo com
      a linha Shopee (assigned_at divergente) — vetor DesempenhoScoreService/PortfolioController.
    - PortfolioGoal: Company::whereHas('users', fn=>role='consultor')->get() conta a empresa 1x (whereHas não duplica linha
      da companies, mas asserir explicitamente para blindar).
    - PortfolioController pivot->role (W3): o call-site em PortfolioController (~:670 `companies()->withPivot('role')`, lido em
      ~:849 `$c->pivot->role`) deve continuar resolvendo APÓS o dedup da base companies() — asserir que
      `$user->companies()->withPivot('role')->first()->pivot->role` retorna o role esperado (ex.: 'consultor') e não vem null.
    - Opcional (se barato de instanciar): DesempenhoScoreService — a universo/carteira do analista conta a empresa 1x.
  </behavior>
  <action>
    Escrever LeitoresConsolidadoRegressaoTest no namespace Tests\Feature\V16 (RefreshDatabase + trait). Focar nos leitores
    Grupo A e B que representam o vetor do bônus (NpsDispararMensal-like via Company::consultor()->first(); carteira via
    consultorCompanies/companies; PortfolioGoal via whereHas). Asserir contagem/identidade antes e depois da linha Shopee,
    sempre com a linha Shopee usando assigned_at divergente. Mensagens pt-BR referenciando o leitor que cada assert protege.
    Não reimplementar a lógica dos leitores — exercitar as relações/queries que eles usam.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16/LeitoresConsolidadoRegressaoTest.php --stop-on-failure 2>&1 | tail -10</automated>
  </verify>
  <done>Regressão verde: Grupo A retorna mesmo responsável consolidado; Grupo B conta carteira 1x; PortfolioGoal não duplica.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Phase gate — suite completa de regressão verde</name>
  <read_first>
    - .planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-VALIDATION.md (Sampling Rate / Phase gate)
  </read_first>
  <files>tests/Feature/V16/LeitoresConsolidadoRegressaoTest.php</files>
  <action>
    Rodar a suite V16 completa e as suites de regressão do bônus/carteira/NPS. Se alguma falhar por regressão real,
    investigar o leitor afetado (Grupo A/B) e ajustar — NÃO reescrever lógica de bônus; se um leitor específico precisar
    de dedup próprio que a relação não cobre, documentar no SUMMARY como achado para a Phase 80. Confirmar que nenhum leitor
    consolidado mudou de resultado. Esta tarefa não edita código de produção — é o gate de verificação da fase.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16 2>&1 | tail -5 && php artisan test --filter=Portfolio 2>&1 | tail -4 && php artisan test --filter=Desempenho 2>&1 | tail -4 && php artisan test --filter=Nps 2>&1 | tail -4</automated>
  </verify>
  <done>tests/Feature/V16 verde; filtros Portfolio/Desempenho/Nps sem novas falhas; nenhum leitor consolidado regrediu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pivot company_users → leitores do bônus/carteira | Regressão silenciosa (double-count ou responsável trocado) impactaria pagamento do bônus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-08 | Tampering | ~15 leitores consolidados (bônus/carteira) | mitigate | Regressão dos Grupo A/B com linha Shopee simulada + suite completa verde como phase gate |
| T-76-09 | Repudiation | cache do Desempenho (desempenho:*) mascarar mudança | accept | Carteira consolidada não muda (provado por teste) → sem bump de cache nesta fase; cache:clear opcional no VPS |
| T-76-SC | Tampering | composer/npm installs | accept | Nenhum pacote instalado nesta fase (sem libs novas) |
</threat_model>

<verification>
- `php artisan test tests/Feature/V16` verde.
- `php artisan test --filter=Portfolio && --filter=Desempenho && --filter=Nps` sem novas falhas.
- Nenhum dos leitores Grupo A/B mudou de resultado com a dimensão de serviço.
</verification>

<success_criteria>
- Regressão dos leitores consolidados provada com cenário ML+Shopee.
- Suite completa de regressão do bônus/carteira/NPS verde (phase gate).
- DEC-A2 fechado: comportamento consolidado idêntico ao de hoje.
</success_criteria>

<output>
Criar `.planning/phases/76-respons-veis-por-servi-o-company-users-com-dimens-o-de-servi/76-04-SUMMARY.md` ao concluir.
</output>
