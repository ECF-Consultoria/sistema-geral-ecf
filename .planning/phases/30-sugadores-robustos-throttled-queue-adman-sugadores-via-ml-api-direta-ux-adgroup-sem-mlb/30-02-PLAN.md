---
plan_id: 30-02
phase: 30
plan: 02
type: execute
wave: 2
depends_on: [30-01]
autonomous: false
mode: mvp
files_modified:
  - app/Services/SugadorAnalysisServiceMl.php
  - app/Http/Controllers/SugadorController.php
  - tests/Feature/Phase30/SugadorMlPathTest.php
must_haves:
  truths:
    - "Bymobile teste (empresa ML-driven sem adman_account_id) consegue clicar Carregar MLBs e ver lista real"
    - "Empresas com adman_account_id (sem mlToken active) continuam usando Adman path SEM regressão"
    - "Empresas com is_ml_driven=true e mlToken active usam novo SugadorAnalysisServiceMl que consome MercadoLivreService"
    - "Empresas sem nem adman_account_id nem mlToken ativo recebem 422 'sem fonte' clara"
  artifacts:
    - path: "app/Services/SugadorAnalysisServiceMl.php"
      provides: "Análise sugadores via ML API direta — API pública espelha SugadorAnalysisService"
      contains: "public function analyzeCompany"
    - path: "app/Http/Controllers/SugadorController.php"
      provides: "Branching is_ml_driven em 4 actions"
      contains: "is_ml_driven"
    - path: "tests/Feature/Phase30/SugadorMlPathTest.php"
      provides: "Cenário Adman path + cenário ML path + cenário sem fonte"
  key_links:
    - from: "SugadorController::analyzeCompany"
      to: "SugadorAnalysisServiceMl quando is_ml_driven=true"
      via: "app(SugadorAnalysisServiceMl::class) vs app(SugadorAnalysisService::class)"
      pattern: "is_ml_driven"
    - from: "SugadorAnalysisServiceMl"
      to: "MercadoLivreService (Phase 20)"
      via: "constructor injection"
      pattern: "MercadoLivreService"
---

<objective>
W2 do Phase 30 — Sugadores via ML API direta destrava empresas ML-only.

Resolver o 3º problema: Bymobile teste (e futuras empresas com mlToken ativo SEM adman_account_id) hoje mostram "Empresa sem adman_account_id — análise pulada" em 4 actions do SugadorController. Sugadores não roda pra elas.

Estratégia (D-05, D-06): novo `SugadorAnalysisServiceMl` espelha API pública de `SugadorAnalysisService` mas consome `MercadoLivreService` internamente. Controller faz branching `$company->is_ml_driven ? MlService : AdmanService`.

Purpose: Destravar Sugadores pra Bymobile teste em prod + validar pattern Sugadores-via-ML que vira base de aprendizado pro Milestone v10.0 (Fontes Unificadas). Smoke prod EXCLUSIVO Bymobile teste (D-07).
Output: Service novo, branching nas 4 actions, suíte Feature, smoke prod único.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-sugadores-robustos-throttled-queue-adman-sugadores-via-ml-api-direta-ux-adgroup-sem-mlb/CONTEXT.md
@.planning/phases/30-sugadores-robustos-throttled-queue-adman-sugadores-via-ml-api-direta-ux-adgroup-sem-mlb/30-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos chave já existentes que o executor precisa respeitar. -->

`SugadorAnalysisService` (Adman — fonte de verdade da API pública a espelhar):
```php
public function analyzeCompany(Company $company, ?Carbon $referenceDate = null, bool $dryRun = false): array
// Retorno: ['skipped' => bool, 'reason' => string|null, 'campanhas' => int, 'adgroups' => int, 'detalhes' => array]
```
- Linha 101: `$custId = $company->adman_account_id ?: $company->ml_store_id;` (NÃO mexer — service Adman fica intacto, D-07 garante zero regressão).
- Internamente usa: `AdmanService` (REST API), checa `SugadorConfig::forCompany($company)`, calcula período `$config->dias_analise`, persiste `Sugador` upserts respeitando `STATUS_TRAVADOS`.

`Company::getIsMlDrivenAttribute(): bool`:
- Linha 72-75: `return optional($this->mlToken)->status === 'active'`
- Carregar via `->loadMissing('mlToken')` antes de acessar pra evitar N+1.

`MercadoLivreService` (Phase 20 wrapper ML API direta):
- Já existe em `app/Services/MercadoLivreService.php`. Já lida com refresh transparente de token (vide CONTEXT.md Pitfall 3 — confirmar no Task 1).
- Tem métodos `getItemsBySeller($sellerId)`, `getItemInsights(...)`, `getOrdersByProduct(...)` ou similar — o executor PRECISA ler o source para mapear ao quadro abaixo.

`MlColetaService` (Phase 20):
- Pattern de OAuth + coleta. Referência arquitetural — não consumido diretamente pelo novo Service.

Mapeamento Adman MCP ↔ ML API direta (do CONTEXT.md D-05):
| Método público necessário | Adman MCP (legacy reference) | ML API direta (nova implementação) |
|---------------------------|------------------------------|-------------------------------------|
| Listar adgroups (campanhas) | `getProductAdsCampaigns` | `/items/search?seller_id=X` agrupado por `catalog_product_id` |
| Métricas por adgroup | `getMarketplaceAdsCustIdProductAdsmetrics` | `/insights/orders/by-product` + `/products/{id}/visits` |
| Detalhar MLB | `getMarketplaceAdsItem` | `/items/{ID}` |

Lógica de detecção de sugador (consumo sem retorno) é IDÊNTICA — só muda a origem dos números.

`Sugador::MOTIVO_*` — não há motivos novos neste plan. Sugadores criados pelo path ML usam mesmos motivos do Adman (acos_alto, sem_vendas, etc — verificar Sugador.php pra lista exata). Não criar `MOTIVO_MANUAL_ANALISTA` aqui (é W3 do Plan 30-03).

`SugadorController` actions a modificar (CONTEXT.md mapeia 4 sites):
- Linha 343-345: `analyzeCompany(Request, Company)` — flash warning "Empresa sem adman_account_id"
- Linha 410-414: `sgiCampaigns(Request, Company)` — retorna JSON 422 "sem adman_account_id"
- Linha 586-591: `mlbs(Request, Sugador)` — retorna JSON 422 "Empresa sem adman_account_id"
- Linha 693-702: `mlbsByCompany(Request, Company)` — retorna JSON 422 "Empresa sem adman_account_id"

Política unificada para os 4 sites:
1. SE `$company->is_ml_driven` → resolver `app(SugadorAnalysisServiceMl::class)` e/ou métodos ML equivalentes
2. SENÃO SE `$company->adman_account_id` → resolver `app(SugadorAnalysisService::class)` / `AdmanMcpService` como hoje
3. SENÃO → manter erro 422/warning ("Empresa sem fonte ativa — sem adman_account_id e sem token ML ativo")

`AnalyzeCompanySugadoresJob::handle(SugadorAnalysisService $service)` — atualmente injeta Adman service. Precisa branching também: usar `app(SugadorAnalysisServiceMl::class)` quando `$this->company->is_ml_driven`. (Ver Task 2 abaixo.)
</interfaces>

<read_first>
- app/Services/SugadorAnalysisService.php (inteiro — é a fonte de verdade pra espelhar; foco nos métodos públicos e shape de retorno)
- app/Services/MercadoLivreService.php (inteiro — mapear quais métodos existem hoje pra cumprir Mapeamento Adman MCP ↔ ML API direta acima; CONFIRMAR refresh transparente de token. Se ausente, anotar em deferred-items.md e parar — não inventar fluxo OAuth)
- app/Models/Company.php (linhas 60-80 — `is_ml_driven` + `mlToken` relação; linhas 195-220 — relações sugadores/contratosServico)
- app/Models/MlToken.php (status enum, fields `access_token`, `refresh_token`, `expires_at`)
- app/Models/Sugador.php (constantes TIPO_*, STATUS_*, MOTIVO_* — confirmar inventário pra reusar no path ML sem duplicar)
- app/Http/Controllers/SugadorController.php (linhas 339-353 + 393-435 + 580-664 + 680-720 — 4 actions com guard `adman_account_id`)
- app/Jobs/AnalyzeCompanySugadoresJob.php (linha 65 — `handle(SugadorAnalysisService $service)` precisa branching)
- .planning/phases/30-sugadores-robustos-throttled-queue-adman-sugadores-via-ml-api-direta-ux-adgroup-sem-mlb/CONTEXT.md (D-05 a D-08 + Pitfall 3 e 4 — LOCKED, NÃO revisitar)
- routes/web.php (linhas 187-198 — rotas Sugadores; sem mudanças neste plan)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar SugadorAnalysisServiceMl espelhando API pública de SugadorAnalysisService</name>
  <files>
    app/Services/SugadorAnalysisServiceMl.php
    tests/Feature/Phase30/SugadorMlPathTest.php
  </files>

  <read_first>
    - app/Services/SugadorAnalysisService.php (TODO o arquivo — é o reference exato a espelhar; especificamente: assinatura de `analyzeCompany`, shape do retorno, lógica de upsert respeitando STATUS_TRAVADOS, cálculo de `periodoInicio`/`periodoFim` via `SugadorConfig::forCompany`)
    - app/Services/MercadoLivreService.php (TODO — mapear métodos disponíveis; se faltar algum equivalente do mapeamento Adman MCP ↔ ML, registrar TODO e parar)
    - app/Models/MlToken.php (fields + status; confirmar pattern de refresh)
    - app/Models/Sugador.php (linhas 33-113 — fillable + casts + constantes; manter mesmo schema de persistência)
    - app/Models/Company.php (linhas 60-75 + 210-220 — is_ml_driven + mlToken)
    - .planning/phases/30-sugadores-robustos-throttled-queue-adman-sugadores-via-ml-api-direta-ux-adgroup-sem-mlb/CONTEXT.md (D-05, Pitfall 3, Pitfall 4)
  </read_first>

  <behavior>
    - Test 1: instanciação via container — `app(SugadorAnalysisServiceMl::class)` retorna instância sem erro (DI funciona via constructor injection do `MercadoLivreService`).
    - Test 2: `analyzeCompany($company)` rejeita empresa SEM `mlToken` ativo — retorna `['skipped' => true, 'reason' => 'sem token ML ativo', ...]` (com counts zero).
    - Test 3: `analyzeCompany($company)` com mlToken ativo + `SugadorConfig` ativo + mock de MercadoLivreService retornando 2 produtos com vendas zero E acos alto → cria 2 sugadores `tipo=adgroup` com `status=pendente` e `motivos` corretos (mesma heurística que Adman path).
    - Test 4: idempotência — segunda execução COM mesmo dataset não cria duplicatas; respeita `STATUS_TRAVADOS` (se sugador já está `em_acao`, NÃO sobrescreve para `pendente`).
    - Test 5: shape do retorno — `analyzeCompany` retorna chaves `['skipped', 'reason', 'campanhas', 'adgroups', 'detalhes']` (idêntico ao Adman service).
    - Test 6 (Pitfall 3): se `MercadoLivreService` lança `\RuntimeException` com "token expired" / "401", `analyzeCompany` captura, loga `[Sugadores/ML] Token expirado empresa {id}`, retorna `['skipped' => true, 'reason' => 'ml_token_expired']` sem propagar.
  </behavior>

  <action>
    Criar `app/Services/SugadorAnalysisServiceMl.php` com namespace `App\Services`, comentário de cabeçalho pt-BR explicando: "Phase 30 D-05: caminho ML direto para Sugadores. Espelha API pública de `SugadorAnalysisService` (Adman) mas consome `MercadoLivreService` (Phase 20). Heurística de detecção idêntica — só muda origem dos números".

    Construtor com promoted property: `public function __construct(private MercadoLivreService $ml) {}`. Importar `App\Models\Company`, `App\Models\Sugador`, `App\Models\SugadorConfig`, `Carbon\Carbon`, `Illuminate\Support\Facades\Log`, `Illuminate\Support\Facades\DB`.

    Método público principal `analyzeCompany(Company $company, ?Carbon $referenceDate = null, bool $dryRun = false): array`:
    - Guarda inicial: `$company->loadMissing('mlToken')`; se `!$company->is_ml_driven` retorna skip `'sem token ML ativo'` (não usar `adman_account_id` aqui — D-06 garante que controller já roteou pro path correto).
    - Carrega `$config = SugadorConfig::forCompany($company)`; skip se `!$config->ativo`.
    - Calcula `$periodoFim = ($referenceDate ?? now()->startOfDay())->copy()->subDay()` e `$periodoInicio = $periodoFim->copy()->subDays($config->dias_analise - 1)`.
    - Try/catch em volta da chamada ao MercadoLivreService: capturar `\RuntimeException` com mensagens contendo "expired"/"401"/"unauthorized" → log + return skip `'ml_token_expired'`.
    - Sob try, chamar métodos do `MercadoLivreService` para listar itens do seller + obter métricas (visits + orders + revenue). Agrupar por `catalog_product_id` (adgroup equivalente).
    - Para cada grupo, avaliar heurística (mesma lógica de `SugadorAnalysisService` Adman — ler config thresholds: `acos_minimo`, `vendas_minimo_dias`, etc.) e marcar como sugador se `consumo > 0 && vendas == 0` OU `acos > threshold`.
    - Persistir via `DB::transaction` com upsert: `Sugador::updateOrCreate(['company_id', 'reference_date', 'tipo' => Sugador::TIPO_ADGROUP, 'campaign_id', 'adgroup_id'], [...])`. ANTES do update, verificar `$existing = Sugador::where(...)->first()`; se `$existing && in_array($existing->status, Sugador::STATUS_TRAVADOS)`, pular update (idempotência).
    - Retorno: `['skipped' => false, 'reason' => null, 'campanhas' => 0, 'adgroups' => count($flagados), 'detalhes' => $detalhes]`.
    - Se `$dryRun` é true, calcula tudo mas NÃO persiste (igual Adman service).

    Criar `tests/Feature/Phase30/SugadorMlPathTest.php` com namespace `Tests\Feature\Phase30`, extendendo `Tests\TestCase`, com `RefreshDatabase` trait. Implementar Tests 1-6 da behavior. Mockar `MercadoLivreService` via `$this->mock(MercadoLivreService::class)` ou subclass de teste.

    Pitfall 4 (heurística empírica): incluir no docblock da classe um comentário pt-BR explicando "Heurística porta os thresholds do Adman path. Validação empírica pendente — Bymobile teste é cobaia no smoke W4. Comparar sugadores detectados pelos 2 paths em casos reais antes de habilitar outras ML-driven (D-07)".
  </action>

  <verify>
    <automated>php artisan test --filter=SugadorMlPathTest</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `app/Services/SugadorAnalysisServiceMl.php` existe
    - `grep -c 'class SugadorAnalysisServiceMl' app/Services/SugadorAnalysisServiceMl.php` retorna `1`
    - `grep -c 'public function analyzeCompany' app/Services/SugadorAnalysisServiceMl.php` retorna `1`
    - `grep -cE 'private (readonly )?MercadoLivreService' app/Services/SugadorAnalysisServiceMl.php` retorna >= `1` (constructor injection)
    - `grep -c 'STATUS_TRAVADOS' app/Services/SugadorAnalysisServiceMl.php` retorna >= `1` (idempotência respeitada)
    - `php artisan tinker --execute="dump(get_class_methods(\App\Services\SugadorAnalysisServiceMl::class))"` mostra `analyzeCompany` na lista
    - Suíte `SugadorMlPathTest` Tests 1-6 verdes
    - Shape do retorno IDÊNTICO ao Adman: chaves `['skipped', 'reason', 'campanhas', 'adgroups', 'detalhes']`
  </acceptance_criteria>

  <done>
    SugadorAnalysisServiceMl criado com API espelhada; idempotência respeitando STATUS_TRAVADOS; tratamento de token expirado; 6 testes verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Branching is_ml_driven em 4 actions do SugadorController + AnalyzeCompanySugadoresJob</name>
  <files>
    app/Http/Controllers/SugadorController.php
    app/Jobs/AnalyzeCompanySugadoresJob.php
    tests/Feature/Phase30/SugadorMlPathTest.php
  </files>

  <read_first>
    - app/Http/Controllers/SugadorController.php (linhas 1-50 — imports + constructor; linhas 339-353 + 393-435 + 580-664 + 680-720 — 4 actions com guards adman_account_id; CONFIRMAR injeção atual do AdmanService/MCP no constructor)
    - app/Jobs/AnalyzeCompanySugadoresJob.php (linha 65 — `handle(SugadorAnalysisService $service)`; precisa branching)
    - app/Services/SugadorAnalysisServiceMl.php (criado no Task 1)
    - app/Models/Company.php (linha 72 — `getIsMlDrivenAttribute()`)
  </read_first>

  <behavior>
    - Test 7 (Adman path preservado): Feature test — empresa com `adman_account_id` preenchido e `mlToken=null` chama `POST /sugadores/companies/{company}/analyze`; o AnalyzeCompanySugadoresJob enfileira; ao executar, usa `SugadorAnalysisService` (Adman). Assert via `Queue::fake()` + introspecção do Job ou via mock binding.
    - Test 8 (ML path novo): empresa com `adman_account_id=null` E `mlToken->status='active'` chama mesma rota; Job usa `SugadorAnalysisServiceMl`.
    - Test 9 (sem fonte): empresa SEM `adman_account_id` E SEM `mlToken` ativo recebe HTTP redirect com flash warning "Empresa sem fonte ativa — sem adman_account_id e sem token ML ativo" (não 500).
    - Test 10 (sgiCampaigns ML path): empresa ML-driven chama `GET /sugadores/companies/{company}/sgi-campaigns` — retorna 200 (não mais 422). Mock service ML.
    - Test 11 (mlbs ML path): Sugador de empresa ML-driven chama `GET /sugadores/{sugador}/mlbs` — retorna 200 com mlbs (mockados).
    - Test 12 (mlbsByCompany ML path): empresa ML-driven chama `GET /sugadores/companies/{company}/mlbs-todos` — retorna 200.
  </behavior>

  <action>
    Em `SugadorController.php`:

    (a) Imports: ADICIONAR `use App\Services\SugadorAnalysisServiceMl;` se ainda não houver.

    (b) Helper privado pra reduzir duplicação. Adicionar método `private function resolveAnalyzer(Company $company): SugadorAnalysisService|SugadorAnalysisServiceMl` (PHP 8.2 union return) que faz `$company->loadMissing('mlToken'); return $company->is_ml_driven ? app(SugadorAnalysisServiceMl::class) : app(SugadorAnalysisService::class);`. Docblock pt-BR menciona D-06.

    (c) Action `analyzeCompany` (linha ~339): substituir o guard `if (!$company->adman_account_id)` por:
    ```php
    $company->loadMissing('mlToken');
    if (!$company->adman_account_id && !$company->is_ml_driven) {
        return back()->with('warning', 'Empresa sem fonte ativa — sem adman_account_id e sem token ML ativo.');
    }
    ```
    (Comentário inline pt-BR: "Phase 30 D-06: aceita Adman OU ML como fonte; Job interno faz branching pra escolher service.")

    (d) Action `sgiCampaigns` (linha ~393): mesmo padrão. Substituir o guard JSON 422. Quando is_ml_driven, em vez de chamar `$this->adman->fetchSugadorCampaigns(...)`, chamar `app(SugadorAnalysisServiceMl::class)->listCampaigns($company)` — NOTA: este método auxiliar precisa existir no service ML. Se ele não existir no Task 1, o executor adiciona um método público `listCampaigns(Company $company): array` no SugadorAnalysisServiceMl que retorna o mesmo shape do `AdmanService::fetchSugadorCampaigns` (array de `['campaign_id', 'campaign_name', 'status', 'budget']`). Para empresas ML, derivar de produtos do seller agrupados.

    (e) Action `mlbs` (linha ~580): guarda hoje verifica `$sugador->company->adman_account_id`. Substituir por:
    ```php
    $sugador->loadMissing('company.mlToken');
    if (!$sugador->company->adman_account_id && !$sugador->company->is_ml_driven) {
        return response()->json(['mlbs' => [], 'reason' => 'Empresa sem fonte ativa.'], 422);
    }
    ```
    Para empresas ML-driven, em vez de chamar `$this->mcp->fetchMlbsByCampaign(...)`, chamar `app(SugadorAnalysisServiceMl::class)->fetchMlbsByCampaign($sugador->company, $sugador->campaign_id, $dateFrom, $dateTo)` — método auxiliar a adicionar no Task 1 ou neste task no service ML. Retornar mesmo shape `['mlbs', 'truncated', 'pages_read', 'total_pages']` (para empresas ML, paginação não se aplica do mesmo jeito: retornar `pages_read=null`, `total_pages=null`, `truncated=false`).

    (f) Action `mlbsByCompany` (linha ~680): mesma estratégia da (e). Quando ML-driven, iterar sobre `$adgroups` chamando método equivalente do service ML.

    (g) IMPORTANTE D-08: o middleware `RateLimited('adman-api')` adicionado no Plan 30-01 NÃO deve aplicar ao path ML. Como o middleware foi posto no Job (não no controller HTTP), e o Job ainda é o mesmo (`AnalyzeCompanySugadoresJob`) — verificar no Task 2(h) abaixo.

    (h) Em `AnalyzeCompanySugadoresJob::handle()`: substituir injeção `handle(SugadorAnalysisService $service)` por `handle(): void` + body que faz `$this->company->loadMissing('mlToken'); $service = $this->company->is_ml_driven ? app(SugadorAnalysisServiceMl::class) : app(SugadorAnalysisService::class); $r = $service->analyzeCompany($this->company);` (resto do log permanece). Pitfall D-08: o middleware `RateLimited('adman-api')` AINDA está declarado no `middleware()` — quando a empresa for ML-driven, o middleware ainda aplica. Decisão: ACEITAR esse custo no W2 (limit 8/min global é folgado; ML-driven são minoria; otimizar split de middleware fica como TODO em deferred-items.md). Anotar inline `// Phase 30 D-08 TODO: separar middleware Adman vs ML em fase futura — por ora ambos compartilham bucket 8/min`.

    Adicionar Tests 7-12 ao `SugadorMlPathTest.php`.
  </action>

  <verify>
    <automated>php artisan test --filter=SugadorMlPathTest</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'private function resolveAnalyzer' app/Http/Controllers/SugadorController.php` retorna `1`
    - `grep -c 'SugadorAnalysisServiceMl' app/Http/Controllers/SugadorController.php` retorna >= `4` (1 import + uso nas 4 actions ou via resolveAnalyzer)
    - `grep -c 'is_ml_driven' app/Http/Controllers/SugadorController.php` retorna >= `4` (1 por action)
    - `grep -c 'is_ml_driven' app/Jobs/AnalyzeCompanySugadoresJob.php` retorna >= `1`
    - `grep -c 'SugadorAnalysisServiceMl' app/Jobs/AnalyzeCompanySugadoresJob.php` retorna >= `1`
    - Suíte `SugadorMlPathTest` Tests 1-12 verdes (acumulado com Task 1)
    - Mensagem de "sem fonte" pt-BR mencionando "sem adman_account_id e sem token ML ativo"
    - Sem regressão: `php artisan test --filter=Sugador` (suítes existentes) verdes
  </acceptance_criteria>

  <done>
    Controller faz branching nas 4 actions; Job também; sem fonte → erro claro; suítes 12/12 verdes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Smoke prod W2 — Bymobile teste destravada (PILOTO EXCLUSIVO)</name>

  <what-built>
    `SugadorAnalysisServiceMl` (path ML direto para Sugadores) + branching `is_ml_driven` em 4 actions do `SugadorController` + Job atualizado. **PILOTO EXCLUSIVO Bymobile teste** (D-07) — NÃO ativar para outras empresas ML-driven sem revisão manual.
  </what-built>

  <how-to-verify>
    Pré-requisito: usuário autoriza explicitamente o deploy.sh (CLAUDE.md `feedback_perguntar_antes_deploy_v9` — outro dev ativo).

    1. Pré-deploy local: confirmar Bymobile teste no DB tem `adman_account_id=null` e `mlToken->status='active'` (ssh tinker: `Company::where('name', 'like', '%Bymobile%teste%')->first()->only(['adman_account_id', 'is_ml_driven'])` deve retornar `is_ml_driven=true` e `adman_account_id=null`).
    2. Deploy autorizado e aplicado.
    3. Acessar `/sugadores` em prod logado como admin.
    4. Localizar card de "Bymobile teste".
    5. Clicar em "Reanalisar" — verificar log do worker em paralelo: `ssh vps "tail -f /var/www/admin/storage/logs/laravel.log | grep -E 'Sugadores/ML|AnalyzeCompanySugadores'"`.
    6. Validar que o Job rodou com `SugadorAnalysisServiceMl` (mensagem de log indica path ML).
    7. Após ~2-5min, abrir drilldown da Bymobile teste — sugadores devem aparecer (ou lista vazia COM banner explicativo, não erro "Empresa sem adman_account_id").
    8. Clicar em "Carregar MLBs" num adgroup da Bymobile — deve retornar JSON 200 (não 422).
    9. Comparar visualmente N sugadores detectados vs realidade conhecida da conta (Pitfall 4 — heurística empírica).

    **NÃO testar com outras empresas ML-driven em prod neste smoke** (D-07). Se houver demanda, criar fase futura com revisão manual por empresa.

    Resultado esperado:
    - Bymobile teste carregou sem erro 422
    - Pelo menos 1 sugador foi detectado OU mensagem clara "0 sugadores detectados — período sem campanhas com consumo"
    - Zero stack trace nos logs
    - Empresas Adman existentes (qualquer outra) seguem funcionando normalmente (regressão zero)
  </how-to-verify>

  <resume-signal>Type "approved" se Bymobile destravou + zero regressão Adman; ou descreva o erro/comportamento inesperado</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller → SugadorAnalysisServiceMl | input controlado (Company model + Sugador); branch decision baseada em `is_ml_driven` |
| SugadorAnalysisServiceMl → MercadoLivreService | wrapper Phase 20 lida com token refresh; Pitfall 3 cobre falha por token expirado |
| controller → Bymobile teste (piloto) | escopo limitado por D-07; outras ML-driven NÃO são afetadas porque carteira não muda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Information Disclosure | SugadorController actions vazando dados ML de outra empresa | mitigate | Guards `$user->companies()->where(...)` mantidos sem mudança nas 4 actions; gate Policy intacto |
| T-30-06 | Tampering | Sugador upsert idempotência ML path | mitigate | Respeita `STATUS_TRAVADOS` (Test 4); analista que marcou `em_acao` não perde decisão |
| T-30-07 | Denial of Service | ML API rate limit | accept | D-08 — ML API tem 8000 req/dia/app, sem throttle agressivo. Bymobile teste sozinha consome &lt;100 req/dia |
| T-30-08 | Repudiation | Branching de fonte | accept | Activity log de Sugador continua via `LogsActivity` trait (`logOnlyDirty`); path de origem registrado em log app `[Sugadores/ML]` |
</threat_model>

<verification>
- `php artisan test --filter=SugadorMlPathTest` → 12/12 verdes
- `php artisan test --filter=Sugador` → suítes existentes sem regressão
- `grep -c 'is_ml_driven' app/Http/Controllers/SugadorController.php` >= 4
- `grep -c 'SugadorAnalysisServiceMl' app/Http/Controllers/SugadorController.php` >= 4
- `php artisan tinker --execute="echo class_exists(\App\Services\SugadorAnalysisServiceMl::class) ? 'OK' : 'MISSING';"` retorna `OK`
- Smoke prod com Bymobile teste — sugadores listados sem erro 422
</verification>

<success_criteria>
1. `app/Services/SugadorAnalysisServiceMl.php` criado, API pública idêntica a `SugadorAnalysisService` (chaves de retorno + assinatura `analyzeCompany`)
2. SugadorController faz branching `is_ml_driven` em 4 actions (`analyzeCompany`, `sgiCampaigns`, `mlbs`, `mlbsByCompany`)
3. `AnalyzeCompanySugadoresJob::handle()` também faz branching
4. Empresas sem nem `adman_account_id` nem `mlToken` ativo recebem mensagem clara "sem fonte ativa"
5. Testes Feature: cenário Adman path + cenário ML path + cenário sem fonte
6. Smoke prod W4: Bymobile teste consegue clicar "Carregar MLBs" e ver lista real (PILOTO EXCLUSIVO)
7. Zero regressão em empresas Adman existentes
</success_criteria>

<output>
Criar `.planning/phases/30-sugadores-robustos-throttled-queue-adman-sugadores-via-ml-api-direta-ux-adgroup-sem-mlb/30-02-SUMMARY.md` documentando: estrutura do novo Service ML, sites do branching, resultados empíricos do smoke Bymobile (quantos sugadores detectados), próximos passos pra W3.
</output>
