---
plan_id: 30-03
phase: 30
plan: 03
type: execute
wave: 3
depends_on: [30-02]
autonomous: false
mode: mvp
files_modified:
  - app/Models/Sugador.php
  - app/Http/Controllers/SugadorController.php
  - resources/js/Pages/Sugadores/Show.jsx
  - resources/js/Pages/Sugadores/Index.jsx
  - routes/web.php
  - tests/Feature/Phase30/ManualSugadorTest.php
must_haves:
  truths:
    - "Quando 'Nenhum MLB encontrado para esta campanha no período' aparece, analista vê botão 'Marcar adgroup como sugador (análise manual)'"
    - "Ao clicar, modal exige observação textual (>=10 chars) e cria Sugador com motivo=MOTIVO_MANUAL_ANALISTA"
    - "Sugadores manuais ganham badge textual 'Manual' (sem emoji por convenção CLAUDE.md) no card e na lista"
    - "Filtro `?fonte=manual` na lista `/sugadores` mostra somente sugadores manuais"
    - "Pausa de adgroup segue funcionando normalmente — não muda fluxo D-11"
  artifacts:
    - path: "app/Models/Sugador.php"
      contains: "MOTIVO_MANUAL_ANALISTA"
    - path: "app/Http/Controllers/SugadorController.php"
      provides: "Endpoint markManual + filtro fonte=manual"
      contains: "public function markManual"
    - path: "resources/js/Pages/Sugadores/Show.jsx"
      provides: "Botão 'Marcar adgroup como sugador' + modal observação"
    - path: "resources/js/Pages/Sugadores/Index.jsx"
      provides: "Filtro fonte=manual + badge Manual"
    - path: "routes/web.php"
      contains: "sugadores.mark-manual"
  key_links:
    - from: "Show.jsx empty state"
      to: "POST /sugadores/companies/{company}/mark-manual"
      via: "useForm + router.post"
      pattern: "mark-manual"
    - from: "SugadorController::markManual"
      to: "Sugador::create([motivos => ['manual_analista']])"
      via: "validação observação min 10 chars"
      pattern: "MOTIVO_MANUAL_ANALISTA"
---

<objective>
W3 do Phase 30 — UX para adgroup-sem-MLB no período.

Resolver caso operacional: analista abre drilldown de um adgroup que SUSPEITA ser sugador, mas a varredura MLB retorna vazia no período (período sem vendas/visitas suficientes para detectar MLBs). Hoje analista fica sem ação — precisa abrir chamado pra suporte.

Estratégia (D-09, D-10, D-11): botão "Marcar adgroup como sugador (análise manual)" aparece no estado vazio do drilldown, modal exige observação textual obrigatória, cria `Sugador` com `motivos=['manual_analista']` (NOVA constante `Sugador::MOTIVO_MANUAL_ANALISTA`). Badge textual "Manual" diferencia visualmente. Filtro `?fonte=manual` permite auditoria. Fluxo de pausa não muda — sugador manual segue funcionando para todos os efeitos.

Purpose: Praticidade — analista resolve adgroup sem MLB sem precisar suporte. Acertividade — decisão manual fica rastreável no DB com autor + observação.
Output: Constante de motivo, endpoint controller, rota, UI no Show + Index, suíte Feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-sugadores-robustos-throttled-queue-adman-sugadores-via-ml-api-direta-ux-adgroup-sem-mlb/CONTEXT.md
@.planning/phases/30-sugadores-robustos-throttled-queue-adman-sugadores-via-ml-api-direta-ux-adgroup-sem-mlb/30-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos chave já existentes que o executor precisa respeitar. -->

`Sugador` Model (Phase 15 base):
- `motivos` é `array` cast (JSON column). Sugadores hoje recebem motivos como `['sem_vendas', 'acos_alto']`.
- Constantes existentes: `TIPO_CAMPANHA`, `TIPO_ADGROUP`, `STATUS_PENDENTE`, `STATUS_EM_ACAO`, `STATUS_RESOLVIDO`, `STATUS_IGNORADO`, `STATUS_MOVIDO`, `STATUS_AUTO_RESOLVIDO`.
- `STATUS_TRAVADOS` inclui `EM_ACAO, RESOLVIDO, IGNORADO, MOVIDO, AUTO_RESOLVIDO` — reanálise NÃO sobrescreve.
- `LogsActivity` ativo: cria entrada `activity_log` em create/update/delete.

`SugadorController` (rotas web.php linhas 187-198):
- Padrão de Gate authorization: `Gate::authorize('manage', Sugador::class)` em actions que criam/alteram.
- Padrão de retorno Inertia: `back()->with('success', '...')`.
- Padrão de validação: `$request->validate([...])` direto na action.

`Sugadores/Show.jsx` (linhas 717-724 — estado vazio):
- Componente renderiza `<p>...Nenhum MLB encontrado para esta campanha no período {from} → {to}.</p>` quando `state.loaded && shown.length === 0 && allMlbs.length === 0`.
- Tem acesso ao `sugador` prop com `campaign_id, adgroup_id, adgroup_name, campaign_name, periodo_inicio, periodo_fim, company_id`.
- Pattern de form Inertia: `import { useForm, router } from '@inertiajs/react'` + `const form = useForm({ observacao: '' })`.

`Sugadores/Index.jsx`:
- Já tem filtros `status`, `tipo`, `company_id`. Adicionar `fonte` (valores: `''` default, `'manual'`, `'auto'`).
- Já tem `StatusBadge` e `MotivoBadge` local components.
- Renderização de cards usa `count_hoje` e `total_pendentes`.

`Tailwind tokens ECF` (CLAUDE.md):
- Borda dark: `border-white/[0.08]`, bg card: `bg-ecf-card`, accent: `text-ecf-yellow`.
- Badge pattern usa `cn()` de `@/lib/utils`.

Convenção CLAUDE.md: comentários em pt-BR, sem emojis em UI, `npm run build` obrigatório após JSX.
</interfaces>

<read_first>
- app/Models/Sugador.php (inteiro — onde adicionar `MOTIVO_MANUAL_ANALISTA`; confirmar não tem coluna `fonte` separada — usar `motivos` array)
- app/Http/Controllers/SugadorController.php (linhas 1-60 — imports + estrutura; linhas 339-353 — pattern de action criar+enfileirar como modelo; linhas 440-475 — pattern de `move()` com `$request->validate` + create de SugadorAcao como referência exata pro `markManual`)
- resources/js/Pages/Sugadores/Show.jsx (linhas 690-735 — estado vazio + componente MlbRow; identificar onde injetar o botão "Marcar como sugador")
- resources/js/Pages/Sugadores/Index.jsx (encontrar lista de filtros existente + chip de filtros aplicados)
- routes/web.php (linhas 187-206 — rotas Sugadores; encontrar boa posição pra rota nova)
- .planning/phases/30-sugadores-robustos-throttled-queue-adman-sugadores-via-ml-api-direta-ux-adgroup-sem-mlb/CONTEXT.md (D-09, D-10, D-11 — LOCKED)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constante MOTIVO_MANUAL_ANALISTA + endpoint markManual + rota</name>
  <files>
    app/Models/Sugador.php
    app/Http/Controllers/SugadorController.php
    routes/web.php
    tests/Feature/Phase30/ManualSugadorTest.php
  </files>

  <read_first>
    - app/Models/Sugador.php (linhas 33-120 — fillable + constantes; adicionar nova constante junto às existentes mantendo agrupamento visual)
    - app/Http/Controllers/SugadorController.php (linhas 1-60 imports; linhas 440-475 método `move` como modelo arquitetural — validate + create + SugadorAcao + activity log)
    - routes/web.php (linhas 187-206 — rotas sugadores; rota nova vai junto)
    - app/Models/SugadorAcao.php (entender `ACAO_*` constants pra registrar ação manual; se não tiver constante adequada, adicionar `ACAO_MARCADO_MANUAL = 'marcado_manual'`)
  </read_first>

  <behavior>
    - Test 1: constante `Sugador::MOTIVO_MANUAL_ANALISTA` é a string `'manual_analista'`.
    - Test 2: rota `POST /sugadores/companies/{company}/mark-manual` está registrada com nome `sugadores.mark-manual`.
    - Test 3: chamada sem `observacao` retorna 422 (validation error pt-BR).
    - Test 4: chamada com `observacao` < 10 chars retorna 422 com mensagem "A observação precisa ter pelo menos 10 caracteres".
    - Test 5: chamada com payload válido cria 1 `Sugador` com `tipo=adgroup`, `motivos=['manual_analista']`, `status=pendente`, `reference_date=hoje`, `observacao=<texto>`, `company_id` da rota, `campaign_id` e `adgroup_id` do payload.
    - Test 6: cria 1 `SugadorAcao` registrando autor + status_anterior=null + status_novo=pendente + observação.
    - Test 7: Activity log registra entrada (`spatie/laravel-activitylog`).
    - Test 8: idempotência leve — duas chamadas COM mesmo `(company_id, campaign_id, adgroup_id, reference_date=hoje)` NÃO duplicam; segunda chamada retorna 200 (ou 422 com aviso) e referencia o sugador existente. Decisão: usar `firstOrCreate` no controller pra evitar duplicata; segunda chamada retorna flash `info` "Adgroup já marcado como sugador manual hoje".
    - Test 9 (Gate): usuário sem permissão (sem Policy `manage`) recebe 403.
  </behavior>

  <action>
    Em `app/Models/Sugador.php`:
    - ADICIONAR após `STATUS_TRAVADOS` (linha ~113) um novo bloco de constantes de motivo: `public const MOTIVO_MANUAL_ANALISTA = 'manual_analista';` (D-10). Comentário pt-BR: "Phase 30 D-10: motivo aplicado quando analista marca adgroup-sem-MLB como sugador via UI manual. Diferencia visualmente na lista (badge 'Manual')".

    Em `app/Models/SugadorAcao.php`:
    - Se ainda não houver, ADICIONAR constante `public const ACAO_MARCADO_MANUAL = 'marcado_manual';` (verificar lista existente; manter coerente com `ACAO_PAUSADO`, `ACAO_REMOVIDO` etc).

    Em `app/Http/Controllers/SugadorController.php`:
    - ADICIONAR método público `markManual(Request $request, Company $company)`:
      ```
      // (Não codar fenced aqui — apenas descrição estrutural pra executor)
      ```
      Estrutura:
      1. `Gate::authorize('manage', Sugador::class);`
      2. `$data = $request->validate(['campaign_id' => 'required|string|max:64', 'campaign_name' => 'nullable|string|max:255', 'adgroup_id' => 'required|string|max:64', 'adgroup_name' => 'nullable|string|max:255', 'periodo_inicio' => 'required|date', 'periodo_fim' => 'required|date|after_or_equal:periodo_inicio', 'observacao' => 'required|string|min:10|max:5000']);` — mensagem custom em pt-BR pro `min:10`: "A observação precisa ter pelo menos 10 caracteres".
      3. `DB::transaction` envolvendo:
         - `$sugador = Sugador::firstOrCreate([...chave única...], [...atributos...]);` chave: `company_id`, `campaign_id`, `adgroup_id`, `reference_date=today()->toDateString()`. Atributos no `firstOrCreate` 2º array: `tipo => TIPO_ADGROUP`, `status => STATUS_PENDENTE`, `motivos => [Sugador::MOTIVO_MANUAL_ANALISTA]`, `campaign_name`, `adgroup_name`, `periodo_inicio`, `periodo_fim`, `observacao`. Campos métricos zerados (`investimento_periodo => 0`, etc.) — pode usar defaults do model ou explicitar.
         - Se `$sugador->wasRecentlyCreated`: criar `SugadorAcao` com `acao => SugadorAcao::ACAO_MARCADO_MANUAL`, `user_id => $request->user()->id`, `status_anterior => null`, `status_novo => Sugador::STATUS_PENDENTE`, `observacao => $data['observacao']`.
         - Se NÃO foi recentemente criado: retornar flash `info` "Adgroup já marcado como sugador manual hoje — atualize a observação direto na lista se precisar".
      4. Retorno: `back()->with('success', 'Adgroup marcado como sugador. Aparece na lista pendentes e segue o fluxo normal de pausa.')`.
      Comentário de método pt-BR mencionando D-09/D-10/D-11.

    Em `routes/web.php`:
    - ADICIONAR após linha 198 (rota `mlbs-by-company`): `Route::post('/sugadores/companies/{company}/mark-manual', [SugadorController::class, 'markManual'])->name('sugadores.mark-manual');`

    Criar `tests/Feature/Phase30/ManualSugadorTest.php` com namespace `Tests\Feature\Phase30`, `RefreshDatabase` trait. Implementar Tests 1-9 da behavior.
  </action>

  <verify>
    <automated>php artisan test --filter=ManualSugadorTest</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "MOTIVO_MANUAL_ANALISTA = 'manual_analista'" app/Models/Sugador.php` retorna `1`
    - `grep -c 'public function markManual' app/Http/Controllers/SugadorController.php` retorna `1`
    - `grep -c "sugadores.mark-manual" routes/web.php` retorna `1`
    - `grep -c "min:10" app/Http/Controllers/SugadorController.php` retorna >= `1` (validação observação)
    - Suíte `ManualSugadorTest` Tests 1-9 verdes
    - `php artisan route:list | grep mark-manual` mostra a rota POST registrada (smoke comando)
    - Comentários pt-BR em todos os pontos de mudança
  </acceptance_criteria>

  <done>
    Constante, endpoint, rota e Action registrados; idempotência por (company, campaign, adgroup, date); 9 testes verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Botão "Marcar adgroup como sugador" + modal observação em Sugadores/Show.jsx</name>
  <files>
    resources/js/Pages/Sugadores/Show.jsx
    tests/Feature/Phase30/ManualSugadorTest.php
  </files>

  <read_first>
    - resources/js/Pages/Sugadores/Show.jsx (linhas 690-735 — estado vazio "Nenhum MLB encontrado"; também inspecionar topo do arquivo pra entender props recebidos e padrão de imports de UI)
    - resources/js/Components/ui/dialog.jsx (se existir — shadcn Dialog pra usar no modal; se não, fallback nativo)
    - resources/js/Components/ui/button.jsx (componente Button do shadcn)
    - resources/js/Components/ui/textarea.jsx (Textarea pra observação)
    - resources/js/lib/utils.js (utilitário `cn()`)
  </read_first>

  <behavior>
    Verificação UI manual no checkpoint W4 + smoke automated:
    - Test 10 (backend smoke do form submit): chamada `POST` com payload completo simulando submit do modal retorna 302 com flash success (já coberto Test 5 do Task 1; este Test 10 reforça que o payload do JSX usa os mesmos campos: `campaign_id, campaign_name, adgroup_id, adgroup_name, periodo_inicio, periodo_fim, observacao`).
  </behavior>

  <action>
    Em `resources/js/Pages/Sugadores/Show.jsx`:

    (a) ADICIONAR imports necessários (caso não existam): `useState` from 'react', `router, useForm` from '@inertiajs/react', `Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter` from '@/Components/ui/dialog' (shadcn), `Button` from '@/Components/ui/button', `Textarea` from '@/Components/ui/textarea', `AlertCircle` ou `Flag` from 'lucide-react'.

    (b) Dentro do componente do drilldown (que renderiza o bloco "Nenhum MLB encontrado..." nas linhas 717-724), adicionar estado local: `const [showManualModal, setShowManualModal] = useState(false);` e form Inertia:
    ```
    const manualForm = useForm({
      campaign_id: sugador?.campaign_id ?? state.data?.campaign_id ?? '',
      campaign_name: sugador?.campaign_name ?? '',
      adgroup_id: sugador?.adgroup_id ?? '',
      adgroup_name: sugador?.adgroup_name ?? state.data?.adgroup_name ?? '',
      periodo_inicio: state.data?.periodo_inicio ?? '',
      periodo_fim: state.data?.periodo_fim ?? '',
      observacao: '',
    });
    ```
    (Comentário pt-BR: "Phase 30 D-09: form pré-preenchido com contexto do adgroup que está visível no drilldown.")

    (c) No bloco de estado vazio (linha ~717), substituir a tag `<p>` única por um `<div>` contendo:
    - A mensagem original "Nenhum MLB encontrado para esta campanha no período {from} → {to}." (mantém)
    - Botão abaixo: `<Button variant="secondary" onClick={() => setShowManualModal(true)}>Marcar adgroup como sugador (análise manual)</Button>`
    - Pequeno texto explicativo cinza claro: "Use quando você suspeita que o adgroup é sugador mas o sistema não retornou MLBs no período selecionado."

    (d) Renderizar modal condicionalmente fora do bloco condicional (no nível root do componente):
    ```
    <Dialog open={showManualModal} onOpenChange={setShowManualModal}>
      <DialogContent className="bg-ecf-card border-white/[0.08]">
        <DialogHeader>
          <DialogTitle>Marcar adgroup como sugador</DialogTitle>
        </DialogHeader>
        {/* Mostrar contexto não-editável: adgroup_name, campaign_name, periodo */}
        {/* Textarea pra observacao com label "Observação (obrigatória, mín. 10 caracteres)" */}
        {/* Erros do manualForm.errors.observacao se houver */}
        <DialogFooter>
          <Button variant="ghost" onClick={() => setShowManualModal(false)}>Cancelar</Button>
          <Button
            disabled={manualForm.processing || manualForm.data.observacao.length < 10}
            onClick={() => {
              manualForm.post(`/sugadores/companies/${sugador.company_id}/mark-manual`, {
                preserveScroll: true,
                onSuccess: () => {
                  setShowManualModal(false);
                  manualForm.reset('observacao');
                },
              });
            }}
          >
            Confirmar
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
    ```

    (e) Após a edição, rodar `npm run build` (convenção CLAUDE.md). Documentar no SUMMARY que build foi executado.

    Adicionar Test 10 ao `ManualSugadorTest.php` validando que o payload aceito pelo backend bate com os campos enviados pelo form (campaign_id, adgroup_id, periodo_inicio, periodo_fim, observacao).
  </action>

  <verify>
    <automated>npm run build &amp;&amp; php artisan test --filter=ManualSugadorTest</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Marcar adgroup como sugador" resources/js/Pages/Sugadores/Show.jsx` retorna >= `1`
    - `grep -c "showManualModal" resources/js/Pages/Sugadores/Show.jsx` retorna >= `2` (useState + uso)
    - `grep -c "mark-manual" resources/js/Pages/Sugadores/Show.jsx` retorna >= `1` (POST endpoint)
    - `grep -c "obrigatória, mín. 10" resources/js/Pages/Sugadores/Show.jsx` retorna >= `1` (mensagem pt-BR de validação min chars)
    - `npm run build` finaliza sem erros
    - Suíte `ManualSugadorTest` Tests 1-10 verdes
    - Sem emojis no JSX (CLAUDE.md: "sem emoji por convenção projeto"). `grep -P '[\x{1F300}-\x{1FAFF}]' resources/js/Pages/Sugadores/Show.jsx` retorna 0 matches NOS DIFFS introduzidos por este task (verificar comparando com commit anterior)
    - Comentários pt-BR
  </acceptance_criteria>

  <done>
    Botão + modal funcional pré-preenchido com contexto do adgroup; submit hits endpoint correto; build limpo.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Filtro fonte=manual + badge "Manual" em Sugadores/Index.jsx</name>
  <files>
    app/Http/Controllers/SugadorController.php
    resources/js/Pages/Sugadores/Index.jsx
    tests/Feature/Phase30/ManualSugadorTest.php
  </files>

  <read_first>
    - app/Http/Controllers/SugadorController.php (método `index` — encontrar onde filtros existentes como `status`, `tipo`, `company_id` são aplicados ao query builder; adicionar `fonte`)
    - resources/js/Pages/Sugadores/Index.jsx (encontrar lista de filtros aplicados no header + componente MotivoBadge ou similar — adicionar lógica de badge "Manual")
  </read_first>

  <behavior>
    - Test 11: `GET /sugadores?fonte=manual` retorna apenas sugadores que tem `'manual_analista'` em `motivos`. Implementação via `whereJsonContains('motivos', Sugador::MOTIVO_MANUAL_ANALISTA)`.
    - Test 12: `GET /sugadores?fonte=auto` retorna apenas sugadores SEM `'manual_analista'` em `motivos` (`whereJsonDoesntContain`).
    - Test 13: `GET /sugadores` (sem `fonte`) retorna todos (comportamento atual preservado).
    - Test 14: shape do prop Inertia inclui `filters.fonte` para o frontend ler.
  </behavior>

  <action>
    Em `app/Http/Controllers/SugadorController.php::index`:
    - Adicionar leitura do query param: `$fonte = $request->query('fonte');` (validar enum `['manual', 'auto']` ou null).
    - Aplicar ao query builder existente:
      - Se `$fonte === 'manual'`: `->whereJsonContains('motivos', Sugador::MOTIVO_MANUAL_ANALISTA)`
      - Se `$fonte === 'auto'`: `->whereJsonDoesntContain('motivos', Sugador::MOTIVO_MANUAL_ANALISTA)` (ou `->where(function($q) { $q->whereNull('motivos')->orWhereJsonDoesntContain('motivos', Sugador::MOTIVO_MANUAL_ANALISTA); })` se o JSON column pode ser null).
    - Adicionar `'fonte' => $fonte` ao array de `filters` retornado pra Inertia.
    - Comentário pt-BR mencionando D-10.

    Em `resources/js/Pages/Sugadores/Index.jsx`:
    - Encontrar onde renderiza filtros do header (chips de "tipo", "status", "empresa") — ADICIONAR um chip ou select para `fonte` com 3 valores: `''` (todos), `'manual'` (Manual), `'auto'` (Automático).
    - Encontrar componente `MotivoBadge` (ou equivalente) que renderiza os badges dos `motivos` do sugador na linha — ADICIONAR caso especial: se `motivos.includes('manual_analista')`, renderizar badge com texto "Manual" e estilo `bg-amber-500/15 text-amber-300 border-amber-400/30` (sem emoji). Tooltip "Marcado como sugador via análise manual do analista" via `title=...`.
    - Atualizar a `useForm`/state que controla a query string para incluir `fonte` ao navegar.

    Rodar `npm run build` ao final.

    Adicionar Tests 11-14 ao `ManualSugadorTest.php`.
  </action>

  <verify>
    <automated>npm run build &amp;&amp; php artisan test --filter=ManualSugadorTest</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "whereJsonContains.*MOTIVO_MANUAL_ANALISTA" app/Http/Controllers/SugadorController.php` retorna >= `1`
    - `grep -c "'fonte'" app/Http/Controllers/SugadorController.php` retorna >= `1`
    - `grep -c "manual_analista" resources/js/Pages/Sugadores/Index.jsx` retorna >= `1`
    - `grep -c "Manual" resources/js/Pages/Sugadores/Index.jsx` retorna >= `1` (badge texto)
    - `npm run build` finaliza sem erros
    - Suíte `ManualSugadorTest` Tests 1-14 verdes
    - Sem emojis no JSX (D-10): `grep -P '[\x{1F300}-\x{1FAFF}]' resources/js/Pages/Sugadores/Index.jsx` retorna 0 matches nos diffs novos
    - Comentários pt-BR
  </acceptance_criteria>

  <done>
    Filtro fonte=manual/auto funciona end-to-end; badge "Manual" textual; build limpo; suíte 14/14 verdes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke prod W3 — adgroup manual + filtro + pausa</name>

  <what-built>
    Endpoint + UI permitem analista marcar adgroup como sugador mesmo quando "Nenhum MLB encontrado no período". Sugador manual aparece com badge "Manual" textual. Filtro `?fonte=manual` na lista. Pausa via Adman MCP segue funcionando normalmente (D-11).
  </what-built>

  <how-to-verify>
    Pré-requisito: usuário autoriza explicitamente o deploy.sh (CLAUDE.md `feedback_perguntar_antes_deploy_v9`).

    1. Deploy autorizado e aplicado.
    2. Em prod, escolher 1 empresa com Sugadores existente (qualquer Adman path — não precisa Bymobile).
    3. Abrir drilldown de um adgroup específico. Se o adgroup retornar MLBs, escolher outro — ou ajustar período (ex: setar período de 1 semana sem vendas) pra forçar estado vazio.
    4. Quando aparecer "Nenhum MLB encontrado para esta campanha no período X → Y":
       - Validar que o botão "Marcar adgroup como sugador (análise manual)" aparece logo abaixo.
       - Clicar no botão. Modal abre pré-preenchido com adgroup name e período.
       - Tentar confirmar sem observação ou com texto < 10 chars → ver mensagem de erro pt-BR.
       - Escrever observação >= 10 chars, confirmar.
       - Flash success aparece.
    5. Voltar pra `/sugadores`. Validar que o sugador criado aparece COM badge "Manual" (texto, sem emoji).
    6. Aplicar filtro `?fonte=manual` na URL. Lista mostra somente sugadores manuais.
    7. Aplicar filtro `?fonte=auto`. Lista NÃO mostra o sugador recém-criado.
    8. Abrir o sugador manual recém-criado. Tentar pausar via fluxo normal (sgi-campaigns + move). Confirmar que pausa funciona (D-11).
    9. Tentar clicar 2x no mesmo botão "Marcar como sugador" no mesmo dia → segunda vez deve mostrar mensagem informativa "Adgroup já marcado como sugador manual hoje" (idempotência Test 8).

    Resultado esperado:
    - Botão aparece SOMENTE quando MLB lista vazia
    - Modal exige observação
    - Sugador manual diferencia visualmente
    - Filtro fonte funciona
    - Pausa segue funcionando
    - Idempotência confirmada
  </how-to-verify>

  <resume-signal>Type "approved" se todos os 9 passos passaram, ou descreva falha</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /mark-manual | input do analista autenticado (observação + IDs do adgroup); Gate `manage` |
| controller → DB | upsert respeitando idempotência (firstOrCreate por chave única) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Tampering | Sugador criado manualmente sem coleta de dados | mitigate | `observacao` obrigatória (min 10 chars) + activity log + SugadorAcao registra autor; auditoria preservada |
| T-30-10 | Repudiation | analista nega que marcou | mitigate | `SugadorAcao.user_id` + Activity log via `LogsActivity` trait |
| T-30-11 | Information Disclosure | analista cria sugador em empresa fora da carteira | mitigate | Gate `manage` + verificação de carteira nas actions existentes (não muda) |
| T-30-12 | Spoofing (XSS) | observação inserida em JSON | mitigate | Laravel escapa por padrão em Blade/Inertia; React escapa por padrão; sem dangerouslySetInnerHTML |
| T-30-13 | DoS | submit em massa de mark-manual | accept | rate limit padrão do Laravel session-auth (60/min) + Gate `manage` + carteira filter; risco baixo |
</threat_model>

<verification>
- `php artisan test --filter=ManualSugadorTest` → 14/14 verdes
- `npm run build` sem warnings
- `php artisan route:list | grep mark-manual` mostra POST /sugadores/companies/{company}/mark-manual
- `grep -c MOTIVO_MANUAL_ANALISTA app/Models/Sugador.php` retorna `1`
- Smoke prod: 9 passos do W4 manual confirmados
- Sem regressão: `php artisan test --filter=Sugador` suítes existentes verdes
</verification>

<success_criteria>
1. Constante `Sugador::MOTIVO_MANUAL_ANALISTA = 'manual_analista'`
2. Rota `sugadores.mark-manual` registrada (POST)
3. Endpoint `SugadorController::markManual` cria sugador com motivo + ação + activity log; idempotência por `(company, campaign, adgroup, date)`
4. Botão "Marcar adgroup como sugador (análise manual)" aparece em Sugadores/Show.jsx no estado vazio
5. Modal exige observação textual obrigatória (min 10 chars) com mensagem pt-BR
6. Sugador criado com `motivos=['manual_analista']`
7. Lista `/sugadores` filtra por `?fonte=manual` mostrando só esses
8. Badge "Manual" textual (sem emoji por D-10/CLAUDE.md) visível na lista
9. Pausa segue funcionando normalmente (D-11)
10. Smoke prod W4 valida 9 passos manuais
</success_criteria>

<output>
Criar `.planning/phases/30-sugadores-robustos-throttled-queue-adman-sugadores-via-ml-api-direta-ux-adgroup-sem-mlb/30-03-SUMMARY.md` documentando: estrutura do endpoint, UX do botão + modal, validação observação, sites do badge + filtro, resultados do smoke (quantos manuais foram criados em teste, se houve issues de UX).
</output>
