---
phase: 01-diagn-stico-adman
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/DevControllerTest.php
  - database/migrations/2026_05_18_100001_create_adman_sync_logs_table.php
  - app/Models/AdmanSyncLog.php
  - app/Models/Company.php
autonomous: true
requirements: [DEV-01, DEV-02, DEV-03, DEV-04]
tags: [laravel, eloquent, migration, phpunit, adman, diagnostico]

must_haves:
  truths:
    - "Suite PHPUnit roda e o arquivo DevControllerTest.php existe com 5 testes vermelhos descrevendo o comportamento esperado de DEV-01/02/03/04"
    - "Tabela adman_sync_logs existe no banco e aceita inserts com created_count/updated_count/skipped_count/error_message/synced_at"
    - "Model AdmanSyncLog faz belongsTo Company com casts corretos"
    - "Company expõe latestAdmanSyncLog() retornando AdmanSyncLog mais recente"
  artifacts:
    - path: "tests/Feature/DevControllerTest.php"
      provides: "Cinco testes feature falhando descrevendo DEV-01/02/03/04 + autorização"
      contains: "test_index_retorna_empresas_com_synced_at"
    - path: "database/migrations/2026_05_18_100001_create_adman_sync_logs_table.php"
      provides: "Schema adman_sync_logs"
      contains: "Schema::create('adman_sync_logs'"
    - path: "app/Models/AdmanSyncLog.php"
      provides: "Model AdmanSyncLog com fillable/casts/belongsTo"
      contains: "class AdmanSyncLog extends Model"
    - path: "app/Models/Company.php"
      provides: "Relacionamentos admanSyncLogs() e latestAdmanSyncLog()"
      contains: "latestAdmanSyncLog"
  key_links:
    - from: "app/Models/AdmanSyncLog.php"
      to: "app/Models/Company.php"
      via: "belongsTo(Company::class)"
      pattern: "belongsTo\\(Company::class\\)"
    - from: "app/Models/Company.php"
      to: "app/Models/AdmanSyncLog.php"
      via: "hasOne(...)->latestOfMany('created_at')"
      pattern: "AdmanSyncLog::class"
---

## Phase Goal

**As a** admin do ECF Admin, **I want to** inspecionar e disparar o sync Adman de cada empresa direto no painel, **so that** eu identifique falhas e force atualizações sem precisar de acesso ao servidor.

<objective>
Estabelecer a fundação de dados e o contrato de teste para a Phase 1 — Diagnóstico Adman.

Esta é a primeira fatia vertical MVP: cria os testes feature falhantes (Wave 0) que descrevem
todos os comportamentos requeridos (DEV-01/02/03/04), e a infraestrutura mínima de banco/model
para que os testes possam ser executados e validados pelas fatias seguintes.

Purpose: Garante que toda a Phase 1 é guiada por testes vermelhos antes de qualquer
implementação de controller/UI, e que o registro de diff (DEV-03) tem persistência estruturada.

Output:
- Suite DevControllerTest.php com 5 testes vermelhos (4 funcionais + 1 autorização)
- Migration + tabela adman_sync_logs
- Model AdmanSyncLog
- Relacionamentos latestAdmanSyncLog() / admanSyncLogs() em Company
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-diagn-stico-adman/01-CONTEXT.md
@.planning/phases/01-diagn-stico-adman/01-RESEARCH.md
@.planning/phases/01-diagn-stico-adman/01-PATTERNS.md
@.planning/phases/01-diagn-stico-adman/01-VALIDATION.md
@CLAUDE.md
@app/Models/AdmanMetric.php
@app/Models/Company.php
@app/Jobs/SyncAdmanCompanyJob.php
@database/migrations/2026_04_26_152220_create_adman_metrics_table.php
@tests/Feature/ProfileTest.php

<interfaces>
<!-- Contratos relevantes extraídos do codebase para o executor não precisar explorar -->

AdmanMetric (app/Models/AdmanMetric.php) — usado nos testes:
- $fillable inclui 'company_id', 'reference_date', 'raw_data', 'synced_at'
- $casts: 'raw_data' => 'array', 'synced_at' => 'datetime', 'reference_date' => 'date'
- public function company() { return $this->belongsTo(Company::class); }

Company (app/Models/Company.php) — modificada nesta plan:
- $fillable inclui 'name', 'cnpj', 'adman_account_id', 'active'
- Já tem método latestMetrics() retornando hasOne(AdmanMetric::class)->latestOfMany('reference_date')
- HasFactory trait JÁ presente, mas database/factories/ só contém UserFactory.php (sem CompanyFactory)
  → Testes devem criar Company via Company::create([...]) explícito, NÃO Company::factory()

User (app/Models/User.php) — referência para autorização:
- Coluna 'role' com valores 'admin' / 'consultor' / 'mentor'
- Tem UserFactory disponível: User::factory()->create(['role' => 'admin'])
- Middleware role:admin via EnsureUserHasRole

SyncAdmanCompanyJob (app/Jobs/SyncAdmanCompanyJob.php) — referenciado nos testes:
- Construtor: __construct(public readonly Company $company, public readonly ?string $date = null)
- handle(AdmanService) chama $adman->syncCompany($this->company, $this->date)

Migration analog (database/migrations/2026_04_26_152220_create_adman_metrics_table.php):
- Usa $table->foreignId('company_id')->constrained()->cascadeOnDelete();
- Índice composto para queries
- Schema::dropIfExists no down()

Rotas a serem usadas pelos testes (ainda não existem — serão criadas no Plan 02):
- GET /dev/desenvolvimento → DevController::index (com middleware role:admin)
- POST /dev/adman/{company}/sync → DevController::dispatchSync (com middleware role:admin)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar migration e model AdmanSyncLog</name>
  <files>
    database/migrations/2026_05_18_100001_create_adman_sync_logs_table.php,
    app/Models/AdmanSyncLog.php
  </files>
  <read_first>
    - database/migrations/2026_04_26_152220_create_adman_metrics_table.php (analog exato da migration)
    - app/Models/AdmanMetric.php (analog exato do model — fillable, casts, belongsTo)
    - .planning/phases/01-diagn-stico-adman/01-PATTERNS.md (seção AdmanSyncLog model + seção Migration)
  </read_first>
  <behavior>
    - Migration cria tabela adman_sync_logs com: id, company_id (FK constrained cascadeOnDelete),
      created_count (integer default 0), updated_count (integer default 0),
      skipped_count (integer default 0), error_message (text nullable),
      synced_at (timestamp nullable), timestamps()
    - Index composto: ['company_id', 'created_at']
    - down() faz dropIfExists('adman_sync_logs')
    - Model AdmanSyncLog tem $fillable com os campos acima
    - Model tem $casts: created_count/updated_count/skipped_count => 'integer', synced_at => 'datetime'
    - Model define public function company() retornando belongsTo(Company::class)
    - php artisan migrate roda sem erro
    - AdmanSyncLog::create([...]) com campos válidos persiste e retorna model com id preenchido
  </behavior>
  <action>
    Criar database/migrations/2026_05_18_100001_create_adman_sync_logs_table.php seguindo
    EXATAMENTE a estrutura da migration analog create_adman_metrics_table.php (`Schema::create`,
    `Blueprint`, `foreignId('company_id')->constrained()->cascadeOnDelete()`, `timestamps()`,
    `$table->index(['company_id', 'created_at'])`, `Schema::dropIfExists` no down).

    Colunas obrigatórias na adman_sync_logs:
    - id (auto)
    - company_id (foreignId constrained cascadeOnDelete)
    - created_count (integer, default 0) — comentário pt-BR: "AdmanMetric criado (wasRecentlyCreated)"
    - updated_count (integer, default 0) — comentário pt-BR: "AdmanMetric atualizado"
    - skipped_count (integer, default 0) — comentário pt-BR: "sem alteração"
    - error_message (text, nullable) — comentário pt-BR: "mensagem de erro HTTP"
    - synced_at (timestamp, nullable) — comentário pt-BR: "timestamp do sync"
    - timestamps() padrão Laravel
    - Index composto ['company_id', 'created_at']

    Criar app/Models/AdmanSyncLog.php em namespace App\Models, estendendo Model.
    Espelhar a estrutura de AdmanMetric: bloco $fillable com TODOS os campos da migration
    (company_id, created_count, updated_count, skipped_count, error_message, synced_at),
    bloco $casts com tipagem (integer/integer/integer/-/datetime), e método company() retornando
    $this->belongsTo(Company::class). Comentários em pt-BR conforme convenção do projeto.

    Rodar `php artisan migrate` ao final para confirmar criação no banco de desenvolvimento.
  </action>
  <verify>
    <automated>php artisan migrate --pretend 2>&1 | grep -i "create table .adman_sync_logs"</automated>
    <automated>php artisan migrate</automated>
    <automated>php artisan tinker --execute="echo class_exists(\App\Models\AdmanSyncLog::class) ? 'OK' : 'FAIL';"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo database/migrations/2026_05_18_100001_create_adman_sync_logs_table.php existe
    - Arquivo app/Models/AdmanSyncLog.php existe e declara `class AdmanSyncLog extends Model`
    - `php artisan migrate` executa sem erro e a tabela `adman_sync_logs` aparece no banco
    - AdmanSyncLog::create(['company_id' => 1, 'created_count' => 1, 'updated_count' => 0, 'skipped_count' => 0, 'synced_at' => now()]) persiste sem exceção (testar via tinker se necessário)
    - grep -E "belongsTo\(Company::class\)" app/Models/AdmanSyncLog.php retorna 1 match
    - grep "\$table->index\(\['company_id', 'created_at'\]\)" na migration retorna 1 match
  </acceptance_criteria>
  <done>
    Migration aplicada, tabela adman_sync_logs no banco, model AdmanSyncLog instanciável,
    relacionamento company() funcional.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar relacionamentos AdmanSyncLog ao model Company</name>
  <files>
    app/Models/Company.php
  </files>
  <read_first>
    - app/Models/Company.php (estado atual completo, ler todas as 119 linhas)
    - app/Models/AdmanSyncLog.php (criado na Task 1)
    - .planning/phases/01-diagn-stico-adman/01-PATTERNS.md (seção Company.php — analog latestMetrics())
  </read_first>
  <behavior>
    - Company tem novo método admanSyncLogs() retornando hasMany(AdmanSyncLog::class)->orderBy('created_at', 'desc')
    - Company tem novo método latestAdmanSyncLog() retornando hasOne(AdmanSyncLog::class)->latestOfMany('created_at')
    - Métodos existentes (latestMetrics, admanMetrics, users, etc.) permanecem intactos
    - $company->latestAdmanSyncLog retorna o AdmanSyncLog mais recente daquela empresa (ou null)
  </behavior>
  <action>
    Editar app/Models/Company.php SEM remover nenhum método existente.

    Adicionar dois novos métodos imediatamente APÓS o método existente latestMetrics()
    (atualmente nas linhas 74-77), seguindo a mesma estrutura `hasMany`/`hasOne(...)->latestOfMany(...)`
    já usada por admanMetrics() / latestMetrics():

    1. admanSyncLogs() — return $this->hasMany(AdmanSyncLog::class)->orderBy('created_at', 'desc');
    2. latestAdmanSyncLog() — return $this->hasOne(AdmanSyncLog::class)->latestOfMany('created_at');

    Comentários em pt-BR antes dos métodos explicando o propósito ("Todos os logs de sync Adman"
    e "Último log de sync Adman desta empresa").

    NÃO adicionar import use App\Models\AdmanSyncLog explícito — Company.php já está no mesmo
    namespace App\Models. Verificar nas linhas 1-9 do arquivo atual que o namespace é App\Models
    e nenhum import de AdmanMetric foi feito (referência via classe não-qualificada funciona).

    NÃO criar latestAdmanMetric() — o método latestMetrics() já existente cobre esse caso e
    será o que o DevController usa (per nota crítica em PATTERNS.md item 1).
  </action>
  <verify>
    <automated>php artisan tinker --execute="\$c = new \App\Models\Company(); echo method_exists(\$c, 'admanSyncLogs') && method_exists(\$c, 'latestAdmanSyncLog') ? 'OK' : 'FAIL';"</automated>
  </verify>
  <acceptance_criteria>
    - grep "public function admanSyncLogs" app/Models/Company.php retorna 1 match
    - grep "public function latestAdmanSyncLog" app/Models/Company.php retorna 1 match
    - grep "latestOfMany('created_at')" app/Models/Company.php retorna 1 match
    - grep "AdmanSyncLog::class" app/Models/Company.php retorna pelo menos 2 matches (um por método)
    - Métodos existentes preservados: grep -c "public function" app/Models/Company.php retorna valor >= valor anterior + 2
    - tinker confirma method_exists para 'admanSyncLogs' e 'latestAdmanSyncLog' → output 'OK'
  </acceptance_criteria>
  <done>
    Relacionamentos admanSyncLogs() e latestAdmanSyncLog() disponíveis no model Company,
    sem regressão nos métodos existentes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar suite DevControllerTest.php com 5 testes vermelhos descrevendo DEV-01/02/03/04</name>
  <files>
    tests/Feature/DevControllerTest.php
  </files>
  <read_first>
    - tests/Feature/ProfileTest.php (analog de estrutura PHPUnit + RefreshDatabase + actingAs)
    - app/Models/AdmanMetric.php (campos para inserir mocks)
    - app/Models/AdmanSyncLog.php (criado na Task 1 — campos para inserir mocks)
    - app/Models/Company.php (verificar fillable; sem factory disponível)
    - app/Models/User.php (verificar coluna 'role' e UserFactory)
    - app/Jobs/SyncAdmanCompanyJob.php (assertPushed reference)
    - .planning/phases/01-diagn-stico-adman/01-VALIDATION.md (mapa de testes por requisito)
    - .planning/phases/01-diagn-stico-adman/01-PATTERNS.md (seção DevControllerTest.php — analog ProfileTest)
    - phpunit.xml (confirma QUEUE_CONNECTION=sync e DB sqlite in-memory)
  </read_first>
  <behavior>
    Cinco testes feature dentro da classe DevControllerTest extends TestCase usando trait RefreshDatabase:

    1. test_index_retorna_empresas_com_synced_at (DEV-01)
       - Cria admin (User::factory()->create(['role' => 'admin']))
       - Cria Company::create([...]) com adman_account_id preenchido e active=true
       - Cria AdmanMetric::create([...]) vinculado à empresa com synced_at = now() e raw_data = ['x' => 1]
       - actingAs(admin)->get('/dev/desenvolvimento') → assertOk()
       - assertInertia: component='Dev/Desenvolvimento', has('empresas', 1), empresas.0.synced_at != null

    2. test_index_retorna_raw_data_no_payload (DEV-02)
       - Mesmo setup acima com raw_data = ['grossBilling' => ['value' => 12345.67]]
       - Verifica empresas.0.raw_data.grossBilling.value === 12345.67

    3. test_index_retorna_diff_do_ultimo_log (DEV-03)
       - Setup acima + AdmanSyncLog::create([company_id, created_count=2, updated_count=3, skipped_count=1, synced_at=now()])
       - Verifica empresas.0.criados === 2, empresas.0.atualizados === 3, empresas.0.ignorados === 1, empresas.0.status === 'ok'

    4. test_dispatch_sync_enfileira_job (DEV-04)
       - Queue::fake()
       - actingAs(admin)->post("/dev/adman/{$company->id}/sync")
       - assertRedirect() + assertSessionHas('success')
       - Queue::assertPushed(SyncAdmanCompanyJob::class, fn($j) => $j->company->id === $company->id)

    5. test_dispatch_sync_rejeita_nao_admin (DEV-04 + Access Control)
       - Cria user com role='consultor'
       - actingAs(consultor)->post("/dev/adman/{$company->id}/sync") → assertStatus(403)

    Todos os 5 testes devem FALHAR neste momento (controller/rotas ainda não existem).
    O comando `php artisan test --filter DevControllerTest` deve mostrar erros como
    "Route [dev.desenvolvimento] not defined" ou similar — isso é o esperado (RED state).
  </behavior>
  <action>
    Criar tests/Feature/DevControllerTest.php em namespace Tests\Feature, classe DevControllerTest
    estendendo TestCase, usando trait RefreshDatabase (Illuminate\Foundation\Testing\RefreshDatabase).

    Imports obrigatórios no topo:
    - use App\Jobs\SyncAdmanCompanyJob;
    - use App\Models\AdmanMetric;
    - use App\Models\AdmanSyncLog;
    - use App\Models\Company;
    - use App\Models\User;
    - use Illuminate\Foundation\Testing\RefreshDatabase;
    - use Illuminate\Support\Facades\Queue;
    - use Tests\TestCase;

    Criar um método privado de helper `criarAdminEEmpresa(): array` que retorna ['admin' => $admin, 'company' => $company]
    para evitar duplicação de setup nos 5 testes. Dentro:
    - $admin = User::factory()->create(['role' => 'admin'])
    - $company = Company::create(['name' => 'Empresa Teste', 'cnpj' => '00000000000000', 'adman_account_id' => 'ACC123', 'active' => true])

    IMPORTANTE: NÃO usar Company::factory() porque database/factories/ contém apenas UserFactory.php.
    Usar Company::create([...]) explicitamente com todos os campos não-nullable do fillable.

    Nomes dos 5 métodos de teste (exatos, em snake_case conforme convenção PHPUnit do projeto):
    1. test_index_retorna_empresas_com_synced_at — DEV-01
    2. test_index_retorna_raw_data_no_payload — DEV-02
    3. test_index_retorna_diff_do_ultimo_log — DEV-03
    4. test_dispatch_sync_enfileira_job — DEV-04
    5. test_dispatch_sync_rejeita_nao_admin — DEV-04 / Access Control

    Cada teste usa $this->actingAs($admin)->get('/dev/desenvolvimento') ou ->post("/dev/adman/{$company->id}/sync").

    Para assertInertia, usar a closure padrão: $response->assertInertia(fn ($page) => $page
    ->component('Dev/Desenvolvimento')->has('empresas', 1)->where('empresas.0.synced_at', fn($v) => !is_null($v))).

    Comentários em pt-BR explicando o que cada teste cobre (mapear DEV-01/02/03/04 explicitamente).

    Ao final, rodar `php artisan test --filter DevControllerTest` — esperado: 5 FAILURES/ERRORS
    (RED state). Capturar e registrar a saída no SUMMARY indicando "todos os 5 testes vermelhos como esperado".
  </action>
  <verify>
    <automated>test -f tests/Feature/DevControllerTest.php && echo "FILE_OK"</automated>
    <automated>grep -c "public function test_" tests/Feature/DevControllerTest.php</automated>
    <automated>php artisan test --filter DevControllerTest 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo tests/Feature/DevControllerTest.php existe
    - grep -c "public function test_" tests/Feature/DevControllerTest.php retorna 5
    - grep "test_index_retorna_empresas_com_synced_at" tests/Feature/DevControllerTest.php retorna 1 match
    - grep "test_index_retorna_raw_data_no_payload" tests/Feature/DevControllerTest.php retorna 1 match
    - grep "test_index_retorna_diff_do_ultimo_log" tests/Feature/DevControllerTest.php retorna 1 match
    - grep "test_dispatch_sync_enfileira_job" tests/Feature/DevControllerTest.php retorna 1 match
    - grep "test_dispatch_sync_rejeita_nao_admin" tests/Feature/DevControllerTest.php retorna 1 match
    - grep "Queue::fake" tests/Feature/DevControllerTest.php retorna pelo menos 1 match
    - grep "use Illuminate\\\\Foundation\\\\Testing\\\\RefreshDatabase" tests/Feature/DevControllerTest.php retorna 1 match
    - `php artisan test --filter DevControllerTest` executa e retorna FAIL/ERROR em 5 testes (RED state esperado — rotas não existem ainda)
    - Nenhum dos testes usa `Company::factory()` (verificar com grep -c "Company::factory" deve retornar 0)
  </acceptance_criteria>
  <done>
    Suite DevControllerTest.php existe com exatamente 5 testes cobrindo DEV-01/02/03/04 + autorização,
    todos vermelhos no momento (RED state confirmado), prontos para virar verdes nos Plans 02/03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db→model | Migration introduz nova tabela; cascade onDelete protege integridade referencial |
| test→app | Suite roda em DB SQLite isolado (RefreshDatabase) — sem impacto em dados de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Migration adman_sync_logs | mitigate | foreignId('company_id')->constrained()->cascadeOnDelete() impede órfãos; index ['company_id','created_at'] previne table-scan |
| T-01-02 | Information Disclosure | error_message (text nullable) | accept | Campo pode conter detalhes técnicos do erro HTTP da API Adman; acesso restrito por role:admin nas rotas (Plan 02) — sem exposição direta |
| T-01-03 | Repudiation | AdmanSyncLog | mitigate | Tabela tem timestamps() padrão Laravel — toda inserção registra created_at/updated_at |
| T-01-SC | Tampering | Package installs (npm/pip/cargo) | accept | Nenhum pacote novo instalado — todas as dependências (PHPUnit, Laravel, Eloquent) já presentes no composer.json/package.json verificados em RESEARCH.md |
</threat_model>

<verification>
**Confirmar antes de avançar para Plan 02:**

1. `php artisan migrate` aplicou a migration sem erro
2. Tabela `adman_sync_logs` existe no banco com as 8 colunas esperadas (id, company_id, created_count, updated_count, skipped_count, error_message, synced_at, created_at, updated_at)
3. `php artisan test --filter DevControllerTest` executa e retorna 5 testes em RED state (esperado — controller/rotas ainda não existem)
4. Mensagens de erro nos testes mencionam "Route [dev.desenvolvimento] not defined" ou similar (confirma que estamos vermelhos pelo motivo certo)
5. Nenhum teste existente quebrou: `php artisan test --filter ProfileTest` continua verde
</verification>

<success_criteria>
- [x] tests/Feature/DevControllerTest.php existe com 5 testes vermelhos
- [x] database/migrations/2026_05_18_100001_create_adman_sync_logs_table.php aplicada
- [x] app/Models/AdmanSyncLog.php existe e instanciável
- [x] app/Models/Company.php tem latestAdmanSyncLog() e admanSyncLogs() funcionais
- [x] Suite ProfileTest existente continua verde (zero regressão)
- [x] Comentários pt-BR em arquivos PHP novos
- [x] Migration usa cascadeOnDelete + index composto
</success_criteria>

<output>
Criar `.planning/phases/01-diagn-stico-adman/01-01-SUMMARY.md` ao final, registrando:
- Confirmação de que a migration foi aplicada
- Output de `php artisan test --filter DevControllerTest` (esperado: 5 testes vermelhos)
- Confirmação de que os 5 testes ficaram vermelhos pelo motivo correto (rotas/controller ausentes)
- Decisões tomadas (ex: por que Company::create vs factory)
</output>
