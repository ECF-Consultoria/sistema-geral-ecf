---
phase: 138-tabela-do-grupo-e-aviso-de-mudanca-de-faixa
plan: 06
type: execute
wave: 3
depends_on: [138-01, 138-04]
files_modified:
  - routes/web.php
  - app/Http/Controllers/FechamentoController.php
  - resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx
  - resources/js/Pages/Admin/Financeiro.jsx
  - tests/Feature/Phase138/Phase138FaixasGrupoCrudTest.php
autonomous: false
requirements: [D-01]

must_haves:
  truths:
    - "Um admin consegue cadastrar, substituir e remover a tabela de faixas de um grupo pela tela"
    - "A tela diz, em português claro, quando o grupo está usando a tabela de uma das empresas dele e qual empresa é"
    - "Competência fechada bloqueia a edição da tabela do grupo, como já bloqueia a da empresa"
    - "A tabela do grupo passa pela mesma validação de sobreposição das outras duas"
  artifacts:
    - path: "app/Http/Controllers/FechamentoController.php"
      provides: "salvarFaixasGrupo e removerFaixasGrupo"
      contains: "GrupoFaixaFaturamento"
    - path: "resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx"
      provides: "Bloco da tabela do grupo com a frase de herança"
      contains: "tabela_herdada_de_nome"
  key_links:
    - from: "resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx"
      to: "admin.financeiro.faixas.grupo"
      via: "router.post do formulário de faixas do grupo"
      pattern: "financeiro.faixas.grupo"
---

<objective>
Fechar D-01 pela tela: cadastrar a tabela do grupo e tornar a herança visível. Enquanto não existir
o cadastro, a tabela do grupo criada no plano 01 não tem como ser preenchida por quem opera; e
enquanto a tela não disser de qual empresa a tabela veio, metade do problema original continua de pé.

Propósito: o grupo que negociou tabela própria precisa de onde registrá-la, e o Administrativo
precisa parar de ver "tabela do serviço X" sem saber que quem escolheu o X foi a empresa que faturou
mais naquele mês.

Saída: duas rotas + dois métodos no `FechamentoController`, o bloco de grupo no
`TabelaFaixasSection.jsx` e o ajuste de rótulo em `Financeiro.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/138-tabela-do-grupo-e-aviso-de-mudanca-de-faixa/138-CONTEXT.md
@.planning/phases/138-tabela-do-grupo-e-aviso-de-mudanca-de-faixa/138-01-SUMMARY.md
@.planning/phases/138-tabela-do-grupo-e-aviso-de-mudanca-de-faixa/138-04-SUMMARY.md

@app/Http/Controllers/FechamentoController.php
@app/Http/Requests/SalvarFaixasFaturamentoRequest.php
@resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx
</context>

<interfaces>
<!-- Contratos medidos em 2026-09-03 + entregues pelos planos 01 e 04. Não reexplorar. -->

Rotas existentes a espelhar (`routes/web.php`, ~linhas 1412-1414, grupo administrativo com `role:admin`):
    POST   /financeiro/faixas/servico/{servico}   -> admin.financeiro.faixas.servico
    POST   /financeiro/faixas/empresa/{company}   -> admin.financeiro.faixas.empresa
    DELETE /financeiro/faixas/empresa/{company}   -> admin.financeiro.faixas.empresa.remover

Métodos a espelhar em `FechamentoController`: `salvarFaixasServico`, `salvarFaixasEmpresa`,
`removerFaixasEmpresa` — todos com `DB::transaction`, apagando a tabela inteira e recriando a partir
de `$request->validated('faixas')`, e devolvendo `back()->with('success', ...)`.

`SalvarFaixasFaturamentoRequest` já valida a régua completa (ordem única, no máximo uma faixa sem
teto e ela é a última, `valor_e_piso` só na faixa sem teto, limites estritamente crescentes) e já
autoriza só admin. É reaproveitado como está — NÃO criar FormRequest novo.

Props da linha de grupo entregues pelo plano 04: `tipo` ('grupo'), `company_group_id`,
`tabela_origem` ('grupo'|'propria'|'servico'|null), `tabela_grupo_nome`, `tabela_herdada_de_nome`,
`tabela_servico_nome`. Prop de página nova: `faixas_por_grupo` (itens com `id`, `nome`, `faixas[]`).

`TabelaFaixasSection.jsx` já tem: `FaixaFormDialog` reutilizável (envia sempre a tabela inteira),
`fmtBRL`, `fmtValorFaixa`, os três estados por empresa ('servico' / 'propria' / null), as classes
`btnNeutro`/`btnAccent` e o bloqueio por `competenciaFechada`. Reaproveitar tudo — não recriar.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarefa 1: Rotas e CRUD da tabela do grupo</name>
  <files>routes/web.php, app/Http/Controllers/FechamentoController.php</files>
  <action>
Adicionar duas rotas no mesmo grupo administrativo das rotas de faixas existentes, imediatamente
depois delas: `POST /financeiro/faixas/grupo/{grupo}` com nome `admin.financeiro.faixas.grupo` e
`DELETE /financeiro/faixas/grupo/{grupo}` com nome `admin.financeiro.faixas.grupo.remover`. O
parâmetro `{grupo}` casa com o type-hint `CompanyGroup $grupo` por route model binding implícito.

Em `FechamentoController`, criar `salvarFaixasGrupo(SalvarFaixasFaturamentoRequest $request,
CompanyGroup $grupo)` e `removerFaixasGrupo(Request $request, CompanyGroup $grupo)`, copiando
exatamente a forma dos métodos de empresa: transação, `GrupoFaixaFaturamento::where('company_group_id',
$grupo->id)->delete()`, recriação linha a linha a partir de `validated('faixas')`, e retorno
`back()->with('success', ...)` com mensagem em pt-BR sem jargão. No `removerFaixasGrupo`, guardar o
`abort_unless` de admin como o `removerFaixasEmpresa` faz.

Atualizar o docblock da classe listando o terceiro caso do CRUD (grupo) e a precedência de D-01. Não
criar FormRequest novo — `SalvarFaixasFaturamentoRequest` já cobre a régua inteira e é o mesmo
contrato de payload.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe artisan route:list --name=financeiro.faixas.grupo</automated>
  </verify>
  <done>As duas rotas aparecem em `route:list` sob o middleware `role:admin`, e os métodos gravam/apagam `grupo_faixas_faturamento`.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Bloco da tabela do grupo na tela, com a herança dita em voz alta</name>
  <files>resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx, resources/js/Pages/Admin/Financeiro.jsx</files>
  <action>
Em `TabelaFaixasSection.jsx`, receber a prop nova `faixasPorGrupo` e acrescentar um ramo que só
aparece quando a linha é de grupo (`empresa.tipo === 'grupo'`), sem tocar nos três estados por
empresa que já existem. Dois casos:

Caso A — o grupo TEM tabela própria (`tabela_origem === 'grupo'`): mostrar um selo "Tabela deste
grupo", a tabela em modo leitura montada a partir de `faixasPorGrupo` (reaproveitar a mesma marcação
de tabela já usada no estado 'servico', incluindo `fmtValorFaixa` para a faixa sem teto), e dois
botões: "Substituir tabela do grupo" (abre o `FaixaFormDialog` pré-preenchido com as faixas atuais do
grupo) e "Voltar a usar a tabela da empresa" (confirmação + DELETE na rota nova).

Caso B — o grupo NÃO tem tabela própria: escrever, em texto claro, de quem a tabela foi herdada,
usando `tabela_herdada_de_nome`. Copy sugerida, sem jargão: "Este grupo está usando a tabela da
empresa {nome}." seguida de uma linha explicando por que isso muda sozinho: "Quem manda é a empresa
do grupo que mais faturou no mês — se outra empresa passar na frente, a tabela muda junto." Abaixo, o
botão "Criar tabela do grupo" (accent, `FaixaFormDialog` pré-preenchido com a tabela aplicada hoje,
para o admin partir dela). Manter, abaixo desse bloco, os botões existentes de editar a tabela da
empresa/serviço da âncora — hoje é por eles que a tabela da empresa-âncora é editável e remover isso
seria perda de função.

Nada de "snapshot", "competência", "reconsolidação", "rollup", "faixa piso" ou "âncora" na tela: quem
lê é o time Administrativo. "Âncora" em particular é o termo interno do código e não deve aparecer —
usar "a empresa do grupo que mais faturou no mês".

Respeitar o bloqueio já existente: com `competenciaFechada`, todos os botões do bloco de grupo ficam
desabilitados, igual aos de empresa.

Em `Financeiro.jsx`: (a) passar `faixas_por_grupo` para `TabelaFaixasSection` como `faixasPorGrupo`,
ao lado de `faixas_por_servico`; (b) na linha "Composição do grupo" (~linha 923), o rótulo entre
parênteses hoje só distingue 'propria' de serviço — acrescentar o caso 'grupo' escrevendo "tabela do
grupo", senão um membro de grupo com tabela própria aparece rotulado errado como "tabela do serviço".

Rodar `npm run build` ao final, conforme convenção do projeto.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -c "tabela_herdada_de_nome" resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx</automated>
  </verify>
  <done>Build passa; o bloco de grupo aparece nas duas variações e a frase de herança nomeia a empresa.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Teste de CRUD e de contrato da tela</name>
  <files>tests/Feature/Phase138/Phase138FaixasGrupoCrudTest.php</files>
  <action>
Criar o teste no molde de `tests/Feature/Phase137/Phase137FaixasCrudTest.php` e da frente de ARQUIVO
de `Phase137FinanceiroUiContratoTest.php`, cobrindo:
(a) admin salva a tabela de um grupo e as linhas aparecem em `grupo_faixas_faturamento` na ordem certa;
(b) salvar de novo SUBSTITUI a tabela inteira (all-or-nothing), sem sobrar linha antiga;
(c) payload com faixas sobrepostas é recusado com a mensagem do `SalvarFaixasFaturamentoRequest` —
prova de que a regra é reaproveitada e não reimplementada;
(d) usuário não-admin recebe 403 nas duas rotas novas;
(e) DELETE remove todas as linhas do grupo e o resolver volta a herdar da empresa;
(f) trava de arquivo: `TabelaFaixasSection.jsx` contém `tabela_herdada_de_nome` e a rota
`admin.financeiro.faixas.grupo`, e `Financeiro.jsx` passa `faixasPorGrupo` — é a única defesa barata
contra a regressão silenciosa de UI, já que o projeto não tem framework de teste JS.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase138FaixasGrupoCrudTest"</automated>
  </verify>
  <done>Os seis casos passam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 4: Conferência visual da tela de fechamento</name>
  <files>resources/js/Pages/Admin/Financeiro/TabelaFaixasSection.jsx</files>
  <action>
Pausar e pedir a conferência humana descrita abaixo. Nenhum arquivo é alterado nesta tarefa — se o
usuário apontar problema de copy ou de layout, o ajuste volta para a Tarefa 2 antes de fechar o plano.
  </action>
  <what-built>
Cadastro da tabela de faixas do grupo na tela de fechamento, com a frase que diz de qual empresa a
tabela foi herdada quando o grupo ainda não tem a sua. Rotas, CRUD, validação reaproveitada e trava
de competência fechada já estão cobertos por teste automatizado — falta a leitura humana da copy e do
layout, que é onde a regra "sem jargão" do projeto se decide.
  </what-built>
  <how-to-verify>
1. Rodar o app local e abrir `/administrativo/financeiro` como admin, num mês AINDA ABERTO.
2. Abrir o accordion de um grupo de empresas. Conferir na seção "Tabela de faixas aplicada":
   - aparece a frase dizendo que o grupo está usando a tabela de uma empresa, com o nome dela;
   - a frase explica que isso muda conforme quem faturou mais, em português comum;
   - nenhuma palavra de jargão interno na tela ("snapshot", "competência", "reconsolidação",
     "rollup", "âncora").
3. Clicar em "Criar tabela do grupo", conferir que o formulário abre pré-preenchido com a tabela que
   está valendo, salvar uma tabela com valores diferentes.
4. Conferir que a seção passa a mostrar "Tabela deste grupo" com as faixas novas, e que a linha de
   herança some.
5. Trocar para um mês JÁ FECHADO e confirmar que todos os botões do bloco de grupo ficam
   desabilitados com o aviso de mês fechado.
6. Clicar em "Voltar a usar a tabela da empresa" e conferir que o grupo volta a exibir a herança.
  </how-to-verify>
  <verify>
    <human-check>Copy sem jargão, herança nomeada, bloqueio de mês fechado funcionando.</human-check>
  </verify>
  <done>Usuário confirmou que a tela nomeia a empresa de quem a tabela foi herdada, que a copy não tem jargão interno e que o mês fechado bloqueia a edição.</done>
  <resume-signal>Responder "aprovado" ou descrever o que ficou confuso na tela</resume-signal>
</task>

</tasks>

<threat_model>
## Fronteiras de confiança

| Fronteira | Descrição |
|-----------|-----------|
| navegador (admin) → `POST/DELETE /administrativo/financeiro/faixas/grupo/{grupo}` | Entrada de usuário que define valor de cobrança |

## Registro STRIDE

| Ameaça | Categoria | Componente | Disposição | Mitigação |
|--------|-----------|------------|------------|-----------|
| T-138-16 | Elevation of Privilege | rotas novas de faixa de grupo | mitigar | Guard duplo: middleware `role:admin` do grupo de rotas administrativo + `authorize()` do `SalvarFaixasFaturamentoRequest` (e `abort_unless` no método de remoção) |
| T-138-17 | Tampering | régua de faixas inconsistente (sobreposição/buraco) | mitigar | Validação reaproveitada de `SalvarFaixasFaturamentoRequest`, sem reimplementação no front — o front só exibe a mensagem que veio do backend |
| T-138-18 | Tampering | edição de tabela de mês já fechado | mitigar | Botões desabilitados por `competenciaFechada` na tela e valor do mês fechado lido do snapshot congelado (D-11 da Fase 137), nunca recalculado |
| T-138-19 | Repudiation | mudança de valor de cobrança sem rastro | mitigar | `LogsActivity` no `GrupoFaixaFaturamento` (plano 01), com `useLogName('faixa_faturamento')` |
</threat_model>

<verification>
- `route:list` mostra as duas rotas novas sob `role:admin`.
- Payload inválido é recusado pela mesma mensagem de sobreposição já usada por serviço e empresa.
- A tela nomeia a empresa de quem a tabela foi herdada.
</verification>

<success_criteria>
- `Phase138FaixasGrupoCrudTest` verde.
- Gate `Phase122|Phase136|Phase137` em 0 falhas.
- `npm run build` sem erro.
- Checkpoint visual aprovado pelo usuário.
</success_criteria>

<output>
Criar `.planning/phases/138-tabela-do-grupo-e-aviso-de-mudanca-de-faixa/138-06-SUMMARY.md` ao terminar.
</output>

---

## Precisão que faltava (plan-checker, 2026-09-03) — encadeamento de 

A prop **não** vai direto de  para . Ela atravessa dois
componentes intermediários, exatamente como  já faz hoje:



Replicar o mesmo encadeamento de 2 níveis para . Usar  no mesmo
arquivo como molde literal — não inventar caminho novo.

**Chave de busca:** dentro do componente, achar a tabela do grupo por ****.

> Os números de linha são de 2026-09-03 e servem de referência — o arquivo tem ~1300 linhas e mudou
> bastante nas últimas horas. Localize pelo nome do componente, não pela linha.
