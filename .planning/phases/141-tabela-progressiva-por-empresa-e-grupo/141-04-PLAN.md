---
phase: 141-tabela-progressiva-por-empresa-e-grupo
plan: 04
type: execute
wave: 2
depends_on: [141-01, 141-02, 141-03]
files_modified:
  - app/Services/Fechamento/FechamentoFaixaResolver.php
  - app/Console/Commands/ConsolidarMesFechamento.php
  - tests/Feature/Phase141/Phase141ResolverCutoverTest.php
  - tests/Feature/Phase141/Phase141ConsolidarRegraNovaTest.php
  - tests/Feature/Phase141/Phase141CongeladoNaoMudaTest.php
autonomous: true
requirements: [TPE-01, TPE-02, TPE-03, TPE-04, TPE-08]

must_haves:
  truths:
    - "Com a regra ligada, a tabela do serviço não classifica mais ninguém"
    - "Com a regra ligada, o faturamento das plataformas contratadas soma e dá UMA faixa só"
    - "Com a regra ligada, a mensalidade é o valor da faixa — a soma de mensalidades some"
    - "Empresa sem tabela cobra o valor fixo do contrato e isso aparece como resultado normal"
    - "Com a regra desligada, tudo continua exatamente como está hoje"
    - "Ligar a regra não muda nenhum número de competência já congelada"
  artifacts:
    - path: "app/Services/Fechamento/FechamentoFaixaResolver.php"
      provides: "Degrau da tabela do serviço condicionado à flag + procedência da tabela própria"
      contains: "procedencia"
    - path: "app/Console/Commands/ConsolidarMesFechamento.php"
      provides: "Consolidação pela regra nova quando a flag está ligada"
      contains: "CobrancaCalculator::mensalidade"
    - path: "tests/Feature/Phase141/Phase141ConsolidarRegraNovaTest.php"
      provides: "Cenário BARAOSHOP ponta a ponta, com o valor ANTES e o valor DEPOIS afirmados no mesmo teste"
    - path: "tests/Feature/Phase141/Phase141CongeladoNaoMudaTest.php"
      provides: "Trava de D-11 — competência congelada é imune à virada da flag"
  key_links:
    - from: "app/Services/Fechamento/FechamentoFaixaResolver.php"
      to: "app/Services/Fechamento/FechamentoRegraTabela.php"
      via: "injeção no construtor, lida uma vez por instância"
      pattern: "FechamentoRegraTabela"
    - from: "app/Console/Commands/ConsolidarMesFechamento.php"
      to: "app/Services/Fechamento/FechamentoRollupService.php"
      via: "porEmpresa(..., somenteContratadas: regra nova)"
      pattern: "porEmpresa"
---

<objective>
A virada de regra propriamente dita, no **motor** do fechamento: resolver de tabela e comando de
consolidação.

Propósito: é aqui que D-01, D-02, D-03 e D-04 do CONTEXT viram comportamento. A tabela do serviço
para de classificar; o faturamento das plataformas contratadas soma e dá uma faixa só; a mensalidade
passa a ser o valor da faixa; quem não tem tabela cobra o valor fixo do contrato.

Saída: os dois arquivos que decidem o número gravado, com os dois comportamentos coexistindo atrás da
flag — e testes que afirmam o valor ANTES e o valor DEPOIS no mesmo cenário.

⚠️ **Este é o plano que muda o valor cobrado.** Nenhum número muda em produção enquanto a flag
estiver desligada (ela nasce desligada e só é ligada no plano 141-07, depois da conferência).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141-tabela-progressiva-por-empresa-e-grupo/141-CONTEXT.md
@app/Services/Fechamento/FechamentoFaixaResolver.php
@app/Console/Commands/ConsolidarMesFechamento.php
@.planning/phases/141-tabela-progressiva-por-empresa-e-grupo/141-01-SUMMARY.md
@.planning/phases/141-tabela-progressiva-por-empresa-e-grupo/141-02-SUMMARY.md
@.planning/phases/141-tabela-progressiva-por-empresa-e-grupo/141-03-SUMMARY.md

<interfaces>
Entregue pelos planos 01, 02 e 03 (usar direto, sem explorar):

```
App\Services\Fechamento\FechamentoRegraTabela
  public const CHAVE = 'fechamento_tabela_por_empresa_ativa';
  public function ativa(): bool          // memoizado por instância
  public function esquecer(): void

App\Services\Fechamento\FechamentoRollupService
  public function porEmpresa(string $mes, ?Collection $companies = null, bool $somenteContratadas = false): array
  // por company_id: ['faturamento_ml'=>?float, 'faturamento_shopee'=>?float,
  //                  'faturamento_total'=>?float, 'plataformas_consideradas'=>array]
  public function plataformasElegiveis(Company $company): array   // subconjunto de ['ml','shopee']

App\Support\CobrancaCalculator
  public static function mensalidade(?array $classificacao, iterable $contratos): ?float
  public static function novo(?array $faixaData, iterable $contratos): float       // preservado

App\Models\FechamentoSnapshot::ESTADO_VALOR_FIXO = 'valor_fixo'

App\Models\EmpresaFaixaFaturamento::ORIGEM_MANUAL | ORIGEM_CONTRATO | ORIGEM_PRESUMIDA_SERVICO
  // coluna `origem` (string 20) + `servico_origem_id` (nullable)
```

Shape atual de `FechamentoFaixaResolver::paraEmpresa()`/`paraGrupo()` — 8 chaves SEMPRE presentes:
`origem`, `servico_id`, `servico_nome`, `grupo_id`, `grupo_nome`, `herdada_de_company_id`,
`herdada_de_company_name`, `faixas`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: A tabela do serviço deixa de classificar (resolver)</name>
  <files>app/Services/Fechamento/FechamentoFaixaResolver.php, tests/Feature/Phase141/Phase141ResolverCutoverTest.php</files>
  <behavior>
    - Flag DESLIGADA: empresa sem tabela própria e sem grupo continua resolvendo pela tabela do
      serviço, com `origem = 'servico'` — comportamento de hoje, byte a byte.
    - Flag LIGADA: a mesma empresa resolve `null` (estado "sem tabela"), nunca faixa aproximada.
    - Flag LIGADA: empresa com tabela própria resolve `origem = 'propria'` normalmente.
    - Flag LIGADA: empresa de grupo com tabela de grupo continua resolvendo `origem = 'grupo'`.
    - `paraGrupo()` com tabela do grupo ausente continua herdando a tabela da empresa-âncora e
      marcando `herdada_de_company_name` — com a flag ligada, essa herança só acontece quando a
      âncora tem tabela PRÓPRIA (a do serviço não existe mais como régua).
    - A tabela resolvida diz sua procedência: `procedencia` vale `'manual'`, `'contrato'` ou
      `'presumida_servico'` quando `origem = 'propria'`, e `null` nos demais casos.
  </behavior>
  <action>
    Injetar `FechamentoRegraTabela` no construtor de `FechamentoFaixaResolver` (o serviço hoje não
    tem construtor; criar um com promoted property, padrão do projeto).

    Em `paraEmpresa()`, envolver os degraus 3 a 5 (serviço candidato → contrato combinado →
    `ServicoFaixaFaturamento`) num `if (! $this->regra->ativa())`. Com a regra ligada, depois de não
    achar tabela de grupo nem tabela própria, devolver `null` direto.

    ⚠️ Não apagar `escolherServicoCandidato()` nem o degrau: o caminho antigo precisa continuar
    executável para a comparação ANTES × DEPOIS do plano 141-05 e para o rollback (desligar a flag
    tem que restaurar o comportamento inteiro, sem deploy).

    Acrescentar ao shape de retorno a 9ª chave `procedencia`, sempre presente (`null` quando não se
    aplica), preenchida a partir da coluna `origem` da PRIMEIRA linha de faixa quando
    `origem = 'propria'`. Nas origens `'grupo'` e `'servico'` fica `null` — tabela de grupo é sempre
    cadastro humano e tabela de serviço não é mais régua.

    Reescrever o docblock da classe em pt-BR para descrever a ordem de resolução NOVA (grupo →
    própria → nada), dizendo explicitamente que a etapa do serviço só existe enquanto a flag da Fase
    141 estiver desligada, e por quê (transição: sem a materialização do plano 141-03 aplicada, 127
    de 201 empresas ficariam sem régua). Manter a documentação da regra antiga marcada como
    transitória, não apagá-la.

    ⚠️ Conferir se alguma suíte existente afirma o conjunto exato de chaves do retorno
    (`Phase137FaixaResolverTest`, `Phase138*`): se afirmar, atualizar a asserção para incluir
    `procedencia` — nunca esconder a chave nova para o teste passar.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase141ResolverCutoverTest|Phase137FaixaResolverTest|Phase138"</automated>
  </verify>
  <done>Os seis comportamentos acima estão provados, e as suítes 137/138 do resolver continuam verdes com a flag desligada.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: A consolidação passa a cobrar pela regra nova</name>
  <files>app/Console/Commands/ConsolidarMesFechamento.php, tests/Feature/Phase141/Phase141ConsolidarRegraNovaTest.php</files>
  <behavior>
    - Cenário BARAOSHOP montado com factories (empresa com Gestão/ML e Gestão de ADS Shopee, contrato
      Shopee mensal de R$ 2.500, faturamento nas duas plataformas somando R$ 488.262,90 e tabela
      própria com as faixas reais): com a flag desligada, a linha gravada tem `cobranca_mensal` =
      valor da faixa + 2.500; com a flag ligada, `cobranca_mensal` = valor da faixa da soma, e nada
      mais. O teste afirma os dois números na mesma classe, nomeando cada um como ANTES e DEPOIS.
    - Com a flag ligada, a faixa é classificada sobre a SOMA das plataformas contratadas — um teste
      monta a empresa de forma que a soma caia numa faixa diferente da que o ML sozinho daria, e
      afirma a faixa da soma.
    - Empresa sem tabela e com contrato mensal ativo: estado `valor_fixo`, `faixa_ordem` nula e
      `cobranca_mensal` igual à soma dos contratos mensais.
    - Empresa sem tabela e sem contrato mensal: continua `sem_tabela`, `cobranca_mensal` nula.
    - Grupo com tabela própria: `cobranca_mensal` do grupo é o valor da faixa da soma do grupo — não
      soma mais contrato nenhum das empresas-membro.
    - O gate de cobertura de faturamento não recusa mais o fechamento por causa de empresas que não
      têm régua: elas saem do denominador.
  </behavior>
  <action>
    No `handle()`, logo no início, resolver a flag UMA vez (`$regraNova = $this->regra->ativa();`,
    com `FechamentoRegraTabela` injetado no construtor) e passá-la adiante como booleano local —
    nunca reler dentro do laço de 201 empresas.

    Passo 2: passar `somenteContratadas: $regraNova` NAS DUAS chamadas de
    `porEmpresa()` (competência atual e mês anterior). ⚠️ As duas juntas: comparar o mês corrente
    recortado com um mês anterior não recortado inventaria evolução de faixa falsa — é a mesma
    disciplina de "nunca comparar réguas diferentes" que o comando já documenta.

    Passo 3 (linha de empresa):
    - Estado, quando `$regraNova` — precedência NOVA:
      1. sem integração → `ESTADO_SEM_INTEGRACAO`;
      2. `$faixaData === null` **e** existe contrato mensal ativo → `ESTADO_VALOR_FIXO`;
      3. `faturamento_total === null` → `ESTADO_SEM_FATURAMENTO`;
      4. `$faixaData === null || $classificacao === null` → `ESTADO_SEM_TABELA`;
      5. senão → `ESTADO_OK`.
      Quando `$regraNova` for falso, manter a precedência de hoje intacta.
    - Cobrança: `$regraNova ? CobrancaCalculator::mensalidade($classificacao, $company->contratosServico) : <expressão atual>`.
      Com a regra nova, o guard `$temContratoMensal` deixa de ser necessário para decidir se calcula
      — `mensalidade()` já devolve `null` quando não há nem faixa nem contrato mensal.

    Passo 5 (linha de grupo): mesma troca de cálculo
    (`CobrancaCalculator::mensalidade($classificacaoGrupo, $todosContratosDoGrupo)`) e mesma
    precedência de estado, incluindo `valor_fixo` quando o grupo não tem régua e há contrato mensal
    em qualquer empresa-membro.

    Passo 6 (gate de cobertura) — ⚠️ **armadilha que trava o fechamento inteiro**: com a regra nova,
    empresa sem plataforma elegível fica com `faturamento_total = null` e hoje entraria no
    denominador da cobertura, podendo derrubar o índice abaixo de 0,7 e RECUSAR o congelamento de
    todo mundo. Excluir do denominador, além de `ESTADO_SEM_INTEGRACAO`, também `ESTADO_VALOR_FIXO`
    — quem não é cobrado por tabela não tem faturamento a cobrir. Comentar em pt-BR o motivo.

    Atualizar o docblock do comando descrevendo o que muda com a flag ligada, e mantendo o registro
    de que competência já fechada continua exigindo `--motivo=` (D-12).
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase141ConsolidarRegraNovaTest|Phase137ConsolidarMesTest|Phase138ConsolidarGrupoTabela"</automated>
  </verify>
  <done>Os seis comportamentos estão provados; as suítes 137/138 de consolidação continuam verdes com a flag desligada.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: Trava — competência congelada é imune à virada</name>
  <files>tests/Feature/Phase141/Phase141CongeladoNaoMudaTest.php</files>
  <behavior>
    - Uma competência é consolidada com a flag DESLIGADA e congelada.
    - A flag é ligada, uma tabela de empresa é cadastrada e as métricas são alteradas.
    - Reler as linhas de `fechamento_snapshots` e `fechamento_grupo_snapshots` daquela competência
      devolve exatamente os mesmos `faturamento_total`, `faixa_ordem`, `valor_faixa`,
      `cobranca_mensal` e `estado` de antes.
    - Reconsolidar a mesma competência SEM `--motivo=` continua sendo recusado, mesmo com a flag
      ligada.
  </behavior>
  <action>
    Escrever a suíte que trava D-11 da Fase 137 sob a regra nova. A conferência é por RECONSULTA às
    tabelas de snapshot (nunca pelo texto impresso do comando) — disciplina do
    `.planning/learnings/desempenho-bonificacao.md`.

    Se algum comportamento aqui falhar, o defeito é de produto, não do teste: a correção é no código
    do Passo 7/writer, nunca afrouxar a asserção.
  </action>
  <verify>
    <automated>C:/xampp/php/php.exe vendor/bin/phpunit --filter=Phase141CongeladoNaoMudaTest</automated>
  </verify>
  <done>A suíte prova que virar a flag e mexer nos dados de origem não move um único número de competência congelada.</done>
</task>

</tasks>

<threat_model>
## Fronteiras de confiança

| Fronteira | Descrição |
|-----------|-----------|
| Flag → fórmula de cobrança | um interruptor troca a fórmula que fatura 201 empresas |
| Métricas de plataforma → faixa → mensalidade | dado sincronizado define, sozinho, o valor da fatura |

## Registro STRIDE

| ID | Categoria | Componente | Disposição | Mitigação |
|----|-----------|------------|------------|-----------|
| T-141-11 | Tampering | `ConsolidarMesFechamento` | mitigate | competência fechada exige `--motivo=` (trava já existente, reafirmada por teste nesta fase) |
| T-141-12 | Denial of Service | gate de cobertura | mitigate | denominador passa a excluir `valor_fixo`, impedindo que a regra nova recuse o fechamento de todos por um efeito colateral |
| T-141-13 | Tampering | `paraEmpresa()` com a régua removida | mitigate | ausência de tabela vira estado visível (`sem_tabela`/`valor_fixo`), nunca faixa aproximada nem R$ 0 |
| T-141-14 | Repudiation | número que mudou sem rastro | mitigate | caminho antigo preservado e testado, permitindo reproduzir o valor ANTES; desligar a flag restaura tudo sem deploy |
</threat_model>

<verification>
1. `C:/xampp/php/php.exe vendor/bin/phpunit --filter="Phase122|Phase136|Phase137|Phase138|Phase139|Phase140|Phase141|Quick260909"` — nenhuma falha
   além das pré-existentes listadas no CONTEXT.
2. Conferir por leitura que TODA mudança de comportamento está sob `$regraNova` — `grep -n "regraNova"`
   em `ConsolidarMesFechamento.php` deve cobrir rollup, estado, cobrança de empresa, cobrança de grupo
   e gate de cobertura.
</verification>

<success_criteria>
- Com a flag desligada, o sistema é idêntico ao de hoje (suítes 137/138/139 verdes sem edição de asserção).
- Com a flag ligada, o cenário BARAOSHOP cobra o valor da faixa da soma das plataformas, e só ele.
- Competência congelada não se move.
</success_criteria>

<output>
Criar `.planning/phases/141-tabela-progressiva-por-empresa-e-grupo/141-04-SUMMARY.md` ao terminar,
registrando o valor ANTES e o valor DEPOIS medidos no cenário BARAOSHOP dos testes.

⚠️ Ao commitar: árvore compartilhada com outra sessão ativa. Commitar SOMENTE os caminhos deste plano
(`git commit -- <arquivo> ...`). Nunca `git add -A`, `git add .`, `git commit -a` ou `git stash`.
</output>
