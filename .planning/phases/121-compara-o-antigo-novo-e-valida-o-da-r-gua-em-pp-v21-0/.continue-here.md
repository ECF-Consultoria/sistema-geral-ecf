# Retomada — Fase 121, Plano 05 (GATE de execução real)

**Estado:** 4/5 planos completos. O plano 121-05 parou **entre a Task 1 e a Task 2**, por decisão de janela.
**Criado em:** 2026-07-31 11:10 BRT

## O que já foi decidido (NÃO re-perguntar)

| Decisão | Resposta do usuário |
|---------|---------------------|
| Task 1 — ambiente | `vps-com-deploy` — **deploy autorizado explicitamente** nesta sessão |
| Task 1 — janela | Esperar o bloco de crons passar: rodar **depois das ~13:45 BRT de 31/07/2026** |

## Competências alvo

Hoje é 31/07 — julho ainda não fechou. Última competência fechada = **2026-06**, mais as duas anteriores (**2026-05**, **2026-04**), via `--mes=2026-06 --historico=2`.

## Pré-condições já conferidas (11:00 BRT de 31/07)

- Árvore de trabalho limpa nos arquivos rastreados.
- `grep PERFORMANCE_COMPANY_FIRST_SCORE .env .env.example` → 0 linhas. **Flag desligada, e continua desligada** — esta fase produz evidência, não ativação.
- `deploy.sh` **não** roda `cache:clear` (só `migrate --force`, `config/route/view:cache`, `chown`, `supervisorctl restart ecf-worker:*`). Nunca rodar `cache:clear` no VPS (incidente 2026-07-30).
- Commits da fase (120 + 121) empurrados para `origin/main` — o `deploy.sh` exige `HEAD == origin/main`.
- Relógio do VPS é UTC; o schedule do Laravel roda em BRT (UTC−3).

## Critical Anti-Patterns

| Anti-pattern | Severity | Detalhe |
|--------------|----------|---------|
| Deployar ou rodar o comparador dentro do bloco 11:00–13:30 BRT | blocking | `adman:sync` (11:00) **enfileira** `SyncAdmanCompanyJob` com delay de 7s por empresa; `supervisorctl restart ecf-worker:*` do deploy mata esses jobs em voo → gap de sync → baseline distorcido (mesmo modo de falha de 12–13/06). E as duas competências frias competiriam com `adman:sync-margem` (11:20), `adman:sync-faturamento` (11:30), `adman:warm-diff` (11:40) pela Adman → 429 → número envenenado, exatamente o que o gate FIXMARG-03 recusa. **Mecanismo:** conferir `date` no VPS e `ps aux \| grep artisan` antes de qualquer comando; só prosseguir depois das 13:45 BRT. |

## Próximos passos, em ordem

1. Conferir a janela no VPS: `date` (esperar ≥ 16:45 UTC = 13:45 BRT) e `ps aux | grep artisan` sem sync em voo.
2. `bash deploy.sh` (autorizado; publica também os commits da Fase 120 já mergeados em main). A migration das duas tabelas do comparador sobe aqui.
3. Pré-aquecer **uma competência de cada vez**, observando tempo e 429 entre elas: `php artisan adman:warm-diff --period=2026-05` e depois `--period=2026-04`. Ao primeiro sinal de contenção, **parar** e reavaliar a janela com o usuário — nunca empilhar carga.
4. `php artisan desempenho:comparar-score-empresa --mes=2026-06 --historico=2`. Registrar tempo total, contadores OK/falhas/sem-carteira e o `run_id` do cabeçalho.
5. Conferência **por reconsulta ao banco pelo `run_id`**, nunca por stdout: linhas nas duas tabelas para as 3 competências, contagem de profissionais/empresas distintas por competência, e nenhum `company_id` de `bonus_invalidacoes` da competência alvo presente.
6. Task 3 (checkpoint bloqueante): reimprimir com `--run=<run_id>` e conduzir o usuário pelos 7 pontos de `how-to-verify` do plano; registrar **literalmente** a decisão dele e o número da tela que a embasou.
7. Escrever `121-05-SUMMARY.md` com decisão + número + `run_id`. Só então a fase vai para verificação.

## Proibido na retomada

Ligar a flag, `cache:clear`, alterar `config/metrics.php`, alterar snapshots de desempenho, rodar `desempenho:consolidar-mes`, `gsd-sdk query state.advance-plan`, `git add -A`.

Lembrete: o GATE **MPP-04 continua `reprovado`** (faltam 2 rodadas, incluindo a de contenção). Aprovar o delta aqui **não** liga a flag sozinho.
