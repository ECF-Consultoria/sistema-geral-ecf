---
phase: 131-tela-administrativa-completar-cadastro-contratos-badge-comer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Support/Permissions.php
  - config/services.php
  - .env.example
  - database/migrations/2026_08_16_100000_add_cancelamento_solicitado_to_contrato_assinaturas_table.php
  - app/Models/ContratoAssinatura.php
  - resources/js/lib/contratoStatus.js
  - tests/Feature/Phase131/MigrationFase131ConvencoesTest.php
autonomous: true
requirements: [UI-05, CLICK-10, ADM-03]

must_haves:
  truths:
    - "A permission `admin.contratos` existe no catálogo e é válida para `Permissions::isValid()`"
    - "Todo usuário `role:admin` responde `true` para `hasPermission('admin.contratos')` no dia do deploy, sem nenhum dado gravado"
    - "`config('services.clicksign.painel_url')` devolve a URL do painel certa para o ambiente, derivada do mesmo `CLICKSIGN_ENV` que já decide a `base_url` da API"
    - "A tabela `contrato_assinaturas` guarda motivo, autor e data do cancelamento solicitado"
    - "Os 7 rótulos e as 7 cores da D-04 existem num único módulo JS, sem duplicação entre telas"
  artifacts:
    - path: "app/Support/Permissions.php"
      provides: "Constante ADMIN_CONTRATOS + entrada no catálogo do grupo Administrativo"
      contains: "admin.contratos"
    - path: "config/services.php"
      provides: "URL do PAINEL da Clicksign derivada de CLICKSIGN_ENV — destino do CTA do cancelamento (UI-SPEC)"
      contains: "painel_url"
    - path: "database/migrations/2026_08_16_100000_add_cancelamento_solicitado_to_contrato_assinaturas_table.php"
      provides: "3 colunas aditivas do cancelamento solicitado (D-13)"
      contains: "cancelamento_solicitado_em"
    - path: "resources/js/lib/contratoStatus.js"
      provides: "Mapa de rótulos/cores dos 7 estados + formatação pt-BR de 'há N dias'"
      exports: ["CONTRATO_STATUS_LABELS", "CONTRATO_STATUS_CLS", "formatarHaDias"]
  key_links:
    - from: "app/Support/Permissions.php"
      to: "app/Models/User.php::hasPermission()"
      via: "catálogo consultado por effectivePermissions()"
      pattern: "ADMIN_CONTRATOS"
    - from: "app/Models/ContratoAssinatura.php"
      to: "contrato_assinaturas.cancelamento_motivo"
      via: "$fillable + $casts"
      pattern: "cancelamento_solicitado_em"
---

<objective>
Fundação da fase: a permission `admin.contratos` passa a existir (UI-05/D-09), a tabela
`contrato_assinaturas` ganha onde guardar o cancelamento solicitado (CLICK-10/D-13), e os 7
rótulos/cores da D-04 nascem num módulo único que as três telas desta fase vão importar.

Purpose: nenhum dos outros 5 planos começa sem estas peças — são os contratos de interface
do resto da fase. Fazê-las juntas evita que 3 planos diferentes redeclarem o mesmo mapa de
estados e derivem.

Output: constante + entrada de catálogo, a URL do painel da Clicksign em `config/services.php`,
migration aditiva com `$fillable`/`$casts`, módulo `resources/js/lib/contratoStatus.js` e o teste
de convenções da migration.

⚠️ **ADM-03 já está cumprida — este plano NÃO implementa remoção nenhuma.** A D-12 do
`131-CONTEXT.md` registra: `email_colaborador` saiu do formulário do Comercial na quick task
`260805-eqk` e seguiu editável em `/companies` esse tempo todo, então a janela que a ADM-03
existia para evitar ("ninguém consegue cadastrar o dado") nunca se abriu. A segunda metade do
requisito — o Administrativo ganhar onde preencher — é entregue pelo plano `131-04`.
O `mlb_implementacoes.gmail_colaborador` do Polos **fica onde está**: é outro campo, de outro
fluxo, para um serviço isento de contrato (D9 da milestone).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-CONTEXT.md
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-UI-SPEC.md
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-PATTERNS.md
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-VALIDATION.md
@CLAUDE.md

<interfaces>
Contratos que os planos seguintes consomem. Extraídos do código, não inventados.

`app/Models/User.php:193-198` — o short-circuit que dispensa migration/seeder (D-09):
`hasPermission(string $key): bool` devolve `true` imediatamente quando `isAdmin()`, antes de
consultar `effectivePermissions()`.

`app/Support/Permissions.php` — API pública: `catalog(): array`, `all(): array`,
`isValid(string $key): bool`. `all()` deriva de `catalog()`, então basta a entrada no catálogo
para a key passar a existir em todo o sistema.

`app/Models/ContratoAssinatura.php` — os 7 estados já existem como `public const STATUS_RASCUNHO`,
`STATUS_AGUARDANDO_ASSINATURAS`, `STATUS_ASSINADO`, `STATUS_RECUSADO`, `STATUS_EXPIRADO`,
`STATUS_CANCELADO`, `STATUS_ERRO`, agrupados em `public const STATUS_TODOS`. Este plano NÃO cria
estado novo: "cancelamento solicitado" é a presença de `cancelamento_solicitado_em`, nunca um
oitavo valor de `status`.
</interfaces>
</context>

<aviso_de_ambiente>
⚠️ **Filtro do PHPUnit que não casa com nada SAI 0 — e ainda varre a suíte inteira.**

Medido neste repositório: `C:\xampp\php\php.exe artisan test --filter=ClasseInexistente` carrega
TODA a suíte, imprime `INFO No tests found.` e devolve **EXIT_CODE 0**. Duas consequências:
1. Um `<verify>` que aponta para classe de teste ainda inexistente **passa em falso**.
2. Dispara exatamente a varredura sem filtro que a `131-VALIDATION.md` proíbe (estoura o timeout de
   300s num ponto pré-existente do `MercadoLivreAdsService`). Filtro sem match ≠ filtro que limita.

Regras desta fase por causa disso:
- Nenhuma task verifica com `--filter=` de classe criada por task POSTERIOR. O teste nasce na MESMA
  task do código que ele prova.
- Ao rodar qualquer `--filter=`, conferir que a saída traz `Tests: N` com **N > 0**.
  `No tests found` é FALHA, mesmo com exit code 0.
</aviso_de_ambiente>

<tasks>

<task type="auto">
  <name>Task 1: Declarar a permission `admin.contratos` e a URL do painel da Clicksign</name>
  <files>app/Support/Permissions.php, config/services.php, .env.example</files>
  <read_first>
    - `app/Support/Permissions.php` — linhas 60-80 (constantes `ADMIN_*`) e 160-180 (grupo `'Administrativo'` do `catalog()`); é o próprio análogo apontado pelo `131-PATTERNS.md`
    - `app/Models/User.php` linhas 190-230 — `hasPermission()` e `effectivePermissions()`, para confirmar por que a D-09 não precisa de migration nem seeder
    - `config/services.php` linhas 198-255 — o bloco `'clicksign'` inteiro: `env` (`sandbox|producao`), `base_url` e o padrão de comentário pt-BR que explica CADA chave
    - `.env.example` linhas 184-193 — o bloco Clicksign, onde a chave nova precisa aparecer documentada
  </read_first>
  <action>
    **Parte 1 — a permission (UI-05/D-09).**

    Acrescentar em `app/Support/Permissions.php`, junto às demais constantes `ADMIN_*` (logo
    depois de `ADMIN_INVENTARIO`), a constante `public const ADMIN_CONTRATOS = 'admin.contratos';`
    com docblock curto em pt-BR: é a tela de contratos do Administrativo — completar cadastro,
    gerar contrato e acompanhar a assinatura (UI-05).

    No método `catalog()`, dentro do grupo `'Administrativo'`, acrescentar a entrada seguindo o
    formato literal das quatro vizinhas: `key` `self::ADMIN_CONTRATOS`, `label` `'Adm · Contratos'`,
    `description` `'Tela de contratos: completar cadastro, gerar contrato e acompanhar assinatura'`.

    NÃO criar migration, seeder, nem gravar linha em `setor_permissoes` (D-09): o short-circuit de
    `User::hasPermission()` já concede a key a toda a `role:admin` no dia do deploy, só por ela
    existir no catálogo. Refinar quem fica é decisão posterior, feita na tela de setores
    (`admin.setores.permissoes.sync`), sem deploy.

    NÃO tocar em nenhuma outra key do catálogo.

    **Parte 2 — a URL do PAINEL da Clicksign (destino do CTA de cancelamento, UI-SPEC).**

    Acrescentar ao bloco `'clicksign'` de `config/services.php` a chave `'painel_url'`, derivada do
    MESMO `CLICKSIGN_ENV` que já decide a `base_url` da API: usar `env('CLICKSIGN_PAINEL_URL')`
    quando informada; senão `https://app.clicksign.com` se `env('CLICKSIGN_ENV', 'sandbox')` for
    `'producao'`, e `https://sandbox.clicksign.com` em qualquer outro caso (default seguro — apontar
    para o sandbox nunca leva ninguém a mexer num contrato real por engano).

    Comentário pt-BR obrigatório em cima da chave, no padrão das vizinhas, dizendo POR QUE ela
    existe: o `131-UI-SPEC.md` aprovado trava o CTA de confirmação do cancelamento em
    **"Registrar e ir para a Clicksign"** (plano `131-05`), e um rótulo que promete levar a um lugar
    precisa de fato levar — é a mesma régua que fez a fase recusar "Cancelar contrato" para um botão
    que não cancela. Deixar explícito que é a URL do **painel** (onde a pessoa conclui o
    cancelamento à mão, porque a API não cancela envelope em andamento), NÃO a `base_url` da API.

    Documentar a chave em `.env.example`, dentro do bloco Clicksign já existente, comentada e sem
    valor (`# CLICKSIGN_PAINEL_URL=` + a nota de que o default sai do `CLICKSIGN_ENV`). Nenhum valor
    de produção entra ali.

    ⚠️ A URL nunca é hardcoded no JSX: a tela recebe como prop vinda do backend (plano `131-05`).
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan tinker --execute="echo (App\Support\Permissions::isValid('admin.contratos') && str_starts_with((string) config('services.clicksign.painel_url'), 'https://') && ! str_contains((string) config('services.clicksign.painel_url'), '/api/')) ? 'OK' : 'FALHOU';"</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima imprime `OK` — permission válida E `painel_url` presente, em https e sem `/api/`
    - `grep -c "ADMIN_CONTRATOS" app/Support/Permissions.php` retorna 2 (constante + entrada do catálogo)
    - `grep -c "painel_url" config/services.php` retorna 1 e `grep -c "app.clicksign.com" config/services.php` retorna 1 — a derivação por ambiente está na FONTE (o valor em runtime depende do `.env` da máquina)
    - `grep -c "CLICKSIGN_PAINEL_URL" .env.example` retorna 1, comentada e sem valor
    - `git diff --stat app/Support/Permissions.php` mostra apenas adições — nenhuma key existente alterada ou removida
    - Nenhum arquivo em `database/migrations/` ou `database/seeders/` foi criado/alterado por esta task
  </acceptance_criteria>
  <done>Comando de <verify> verde e todos os itens de <acceptance_criteria> conferidos — inclusive as conferencias por reconsulta ao banco.</done>
</task>

<task type="auto">
  <name>Task 2: Migration aditiva do cancelamento solicitado (D-13) + fillable/casts</name>
  <files>database/migrations/2026_08_16_100000_add_cancelamento_solicitado_to_contrato_assinaturas_table.php, app/Models/ContratoAssinatura.php, tests/Feature/Phase131/MigrationFase131ConvencoesTest.php</files>
  <read_first>
    - `database/migrations/2026_08_15_100001_add_ultimo_alerta_em_to_contrato_assinaturas_table.php` — molde direto (MESMA tabela alvo, guarda `Schema::hasColumn` no `up()` e no `down()`)
    - `database/migrations/2026_08_15_100000_add_motivo_slug_to_contrato_liberacoes_table.php` — molde do docblock que documenta por que NÃO usa `enum()`
    - `database/migrations/2026_04_26_152218_create_meetings_table.php` linha ~23 — único padrão correto de FK opcional: `foreignId('created_by')->nullable()->constrained('users')->nullOnDelete()`
    - `app/Models/ContratoAssinatura.php` linhas 40-130 — `$fillable`, `$casts` e os avisos de "sem isto o mass assignment falha EM SILÊNCIO"; e `STATUS_TODOS`, que NÃO pode ganhar valor novo
    - `tests/Feature/Phase126/MigrationFase126ConvencoesTest.php` — teste de convenções de migration já estabelecido no projeto; é o molde do arquivo de teste novo
  </read_first>
  <action>
    Criar `database/migrations/2026_08_16_100000_add_cancelamento_solicitado_to_contrato_assinaturas_table.php`
    (timestamp posterior ao `2026_08_15_100001`, hoje a última desta tabela), aditiva, com as três
    colunas da D-13, todas guardadas por `Schema::hasColumn` no `up()` e no `down()`:
    `cancelamento_motivo` como `text()->nullable()`; `cancelamento_solicitado_por_user_id` como
    `foreignId()->nullable()->constrained('users')->nullOnDelete()`; `cancelamento_solicitado_em`
    como `timestamp()->nullable()`.

    Armadilhas do projeto que a migration DEVE respeitar e documentar em comentário pt-BR no topo:
    1. `enum()` de banco derruba a suíte SQLite pelo CHECK — o motivo é `text()` livre, sem lista
       fechada. O UI-SPEC confirma texto livre para cancelamento, ao contrário do `motivo_slug` da
       liberação manual, que tem `MOTIVOS_MANUAIS`.
    2. FK com `nullOnDelete()` EXIGE `->nullable()` na mesma linha, senão o MariaDB de produção
       recusa com erro 1830 e o SQLite dos testes não pega.
    3. O nome que o Laravel geraria para a FK
       (`contrato_assinaturas_cancelamento_solicitado_por_user_id_foreign`) tem 63 caracteres —
       à beira do teto de 64 do MariaDB (erro 1059), e qualquer renomeação futura da coluna
       estoura. Nomear a FK explicitamente e curto: `ca_cancel_solic_user_fk`, passando o nome
       como segundo argumento de `foreignId(...)`/`constrained(...)` conforme a assinatura
       disponível no Laravel 12, ou declarando a coluna e a `foreign(...)->references('id')
       ->on('users')->nullOnDelete()` com o nome explícito. Nenhum índice/chave anônimo pode sair
       desta migration.
    4. `down()` derruba a FK antes das colunas (`dropForeign('ca_cancel_solic_user_fk')` seguido de
       `dropColumn`) e é idempotente.

    Em `app/Models/ContratoAssinatura.php`: acrescentar as três colunas a `$fillable` com o mesmo
    comentário de alerta das vizinhas (sem isso o mass assignment falha em silêncio) e
    `'cancelamento_solicitado_em' => 'datetime'` a `$casts`. NÃO acrescentar valor novo a
    `STATUS_TODOS` nem a `STATUS_EM_ANDAMENTO` — "cancelamento solicitado" é derivado da presença
    de `cancelamento_solicitado_em`; virar um oitavo status quebraria as 7 contagens do resumo da
    D-04 e mudaria a semântica do índice único `ca_empresa_servico_andamento_uniq`.

    Criar `tests/Feature/Phase131/MigrationFase131ConvencoesTest.php` copiando a estrutura do
    `MigrationFase126ConvencoesTest.php`, com os casos: a migration existe; não usa `enum(`; não
    declara `nullOnDelete` sem `nullable` na mesma linha; nenhum índice/chave é anônimo e nenhum
    nome declarado passa de 64 caracteres; `up()` e `down()` são guardados por `Schema::hasColumn`;
    as três colunas novas estão declaradas; as três estão no `$fillable` do model; `STATUS_TODOS`
    continua com exatamente 7 elementos.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=MigrationFase131ConvencoesTest</automated>
  </verify>
  <acceptance_criteria>
    - `--filter=MigrationFase131ConvencoesTest` sai 0 com no mínimo 7 testes verdes
    - `C:\xampp\php\php.exe artisan test --filter="Phase126|Phase129|Phase130"` sai 0 — a migration não regride nada do que já usa `contrato_assinaturas`
    - `grep -v '^\s*//' database/migrations/2026_08_16_100000_add_cancelamento_solicitado_to_contrato_assinaturas_table.php | grep -c "enum("` retorna 0
    - `grep -c "cancelamento_motivo\|cancelamento_solicitado_por_user_id\|cancelamento_solicitado_em" app/Models/ContratoAssinatura.php` retorna 4 ou mais (3 no `$fillable` + 1 no `$casts`)
    - `count(ContratoAssinatura::STATUS_TODOS) === 7` continua verdadeiro (asserido pelo teste novo)
  </acceptance_criteria>
  <done>Comando de <verify> verde e todos os itens de <acceptance_criteria> conferidos — inclusive as conferencias por reconsulta ao banco.</done>
</task>

<task type="auto">
  <name>Task 3: Módulo único dos 7 estados — rótulos, cores e "há N dias"</name>
  <files>resources/js/lib/contratoStatus.js</files>
  <read_first>
    - `131-UI-SPEC.md` — seções "Mapa de cor por estado do contrato" (as 7 linhas de classes Tailwind) e "Copywriting Contract" (os 7 rótulos, o "Aguardando Administrativo" e as regras de singular/plural do "há N dias")
    - `resources/js/Pages/Comercial/EmpresasListagem.jsx` linhas 43-51, 77-81 e 100-107 — `PENDENCIAS_CLS`/`SETOR_CLS` e `SetorBadge`: molde exato de mapa de classe por chave com fallback
    - `resources/js/lib/utils.js` — precedente de módulo compartilhado (`cn`, `formatCurrency`, `formatDate`)
    - `app/Models/ContratoAssinatura.php` linhas 100-118 — as 7 chaves `STATUS_*` que os objetos JS espelham manualmente
  </read_first>
  <action>
    Criar `resources/js/lib/contratoStatus.js` com comentário de topo em pt-BR explicando que é o
    espelho manual de `ContratoAssinatura::STATUS_*` (o projeto não tem enum compartilhado PHP↔JS)
    e que o módulo é compartilhado de propósito: três telas desta fase consomem o mesmo mapa, e
    duplicá-lo por página faria os rótulos derivarem.

    Exportar:
    - `CONTRATO_STATUS_LABELS` — exatamente 7 chaves, com a redação FINAL do UI-SPEC:
      `rascunho` = "Não enviado"; `aguardando_assinaturas` = "Esperando assinatura";
      `assinado` = "Assinado"; `recusado` = "Cliente recusou"; `expirado` = "Prazo venceu";
      `cancelado` = "Cancelado"; `erro` = "Falha no envio".
    - `SEM_CONTRATO` = `'aguardando_administrativo'` e `SEM_CONTRATO_LABEL` = `'Aguardando Administrativo'`
      — estado só de exibição (empresa que ainda não teve contrato gerado). NUNCA acrescentar a
      `CONTRATO_STATUS_LABELS`, senão o resumo de 7 contagens da D-04 vira 8.
    - `CONTRATO_STATUS_CLS` — as 7 classes literais do UI-SPEC (slate, âmbar, esmeralda, vermelho,
      laranja, mute, rosa) mais a chave `outros` com `'bg-white/10 text-white/60 border-white/10'`
      usada como fallback, no mesmo espírito de `SETOR_CLS.outros`.
    - `rotuloContrato(status)` — devolve o rótulo, com fallback para o próprio `status` cru se vier
      chave inesperada. Nunca lançar, nunca renderizar `undefined`.
    - `classeContrato(status)` — idem, com fallback em `CONTRATO_STATUS_CLS.outros`.
    - `formatarHaDias(dias)` — pluralização pt-BR travada pelo UI-SPEC: `0` → "há menos de 1 dia";
      `1` → "há 1 dia"; `>= 2` → "há N dias"; nulo/negativo cai em "há menos de 1 dia".

    NÃO importar `lucide-react` aqui — os ícones (`FileEdit`, `Send`, `CheckCircle2`, `XCircle`,
    `TimerOff`, `Ban`, `AlertTriangle`) são importados por página. Registrar a correspondência
    estado→ícone apenas como comentário, para o executor de cada tela não ter que voltar ao UI-SPEC.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./resources/js/lib/contratoStatus.js').then(m=>{if(Object.keys(m.CONTRATO_STATUS_LABELS).length!==7)throw new Error('rotulos');if(m.formatarHaDias(0)!=='há menos de 1 dia')throw new Error('zero');if(m.formatarHaDias(1)!=='há 1 dia')throw new Error('um');if(m.formatarHaDias(6)!=='há 6 dias')throw new Error('plural');if(m.rotuloContrato('xpto')!=='xpto')throw new Error('fallback');if(!m.CONTRATO_STATUS_CLS.outros)throw new Error('cls');console.log('OK');})"</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima imprime `OK` e sai 0
    - `grep -c "aguardando_administrativo" <(grep -A 12 "CONTRATO_STATUS_LABELS" resources/js/lib/contratoStatus.js)` retorna 0 — o estado de exibição não está entre os 7
    - `grep -c "lucide-react" resources/js/lib/contratoStatus.js` retorna 0
    - O arquivo não importa nada de `resources/js/Pages/` (módulo de biblioteca, sem dependência de tela)
  </acceptance_criteria>
  <done>Comando de <verify> verde e todos os itens de <acceptance_criteria> conferidos — inclusive as conferencias por reconsulta ao banco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessão autenticada → catálogo de permissões | a key nova passa a ser concedível pela tela de setores a qualquer usuário |
| aplicação → banco (DDL) | migration aditiva sobre tabela de evidência jurídica em produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01-01 | Elevation of Privilege | `Permissions::ADMIN_CONTRATOS` | mitigate | A key só concede acesso onde uma rota a exigir; este plano NÃO cria rota. O acesso real nasce no plano `131-03`, com `permission:admin.contratos` — nunca `role:admin` |
| T-131-01-02 | Elevation of Privilege | D-09 (nasce concedida a todo `role:admin`) | accept | Decisão explícita do usuário: no dia do deploy ninguém perde acesso. O universo concedido é idêntico ao de hoje (`role:admin` da rota da Fase 130); nenhum usuário novo ganha nada |
| T-131-01-03 | Tampering | `contrato_assinaturas` (DDL) | mitigate | Migration estritamente aditiva, guardada por `Schema::hasColumn` nos dois sentidos; nenhuma coluna existente é renomeada ou removida; `down()` idempotente |
| T-131-01-04 | Denial of Service | deploy no MariaDB | mitigate | FK com nome explícito curto (erro 1059), `nullable()` obrigatório com `nullOnDelete()` (erro 1830) e ausência de `enum()` (CHECK no SQLite) — as três verificadas por `MigrationFase131ConvencoesTest` |
| T-131-01-05 | Repudiation | autor do cancelamento | mitigate | `cancelamento_solicitado_por_user_id` com FK `nullOnDelete` preserva o registro do motivo mesmo se o usuário for apagado — o motivo e a data continuam legíveis |
| T-131-01-SC | Tampering | npm/composer installs | mitigate | Nenhum pacote novo é instalado neste plano (`131-RESEARCH.md` §"Standard Stack": reuso, não instalação); qualquer install descoberto na execução exige checkpoint humano antes |
</threat_model>

<verification>
1. `C:\xampp\php\php.exe artisan tinker --execute="echo App\Support\Permissions::isValid('admin.contratos') ? 'OK' : 'FALHOU';"` imprime `OK`
2. `C:\xampp\php\php.exe artisan test --filter=Phase131` sai 0
3. Regressão cruzada: `C:\xampp\php\php.exe artisan test --filter="Phase126|Phase129|Phase130|Phase131"` sai 0
4. ⛔ NUNCA rodar a suíte sem filtro — estoura o timeout de 300s num ponto pré-existente do `MercadoLivreAdsService`
5. Conferência por RECONSULTA AO BANCO (não por stdout): após `artisan migrate`, confirmar as 3 colunas em `contrato_assinaturas` com `artisan tinker --execute="echo implode(',', Schema::getColumnListing('contrato_assinaturas'));"`
</verification>

<success_criteria>
- `Permissions::isValid('admin.contratos')` devolve `true` e a key aparece no grupo `Administrativo` do catálogo
- `config('services.clicksign.painel_url')` existe, é derivada de `CLICKSIGN_ENV` e aponta para o painel (não para a API)
- As 3 colunas da D-13 existem em `contrato_assinaturas`, com FK nomeada à mão e sem `enum()`
- `ContratoAssinatura::STATUS_TODOS` continua com 7 elementos
- `resources/js/lib/contratoStatus.js` exporta 7 rótulos, 7 classes + fallback e a pluralização pt-BR
- `--filter="Phase126|Phase129|Phase130|Phase131"` verde
- ADM-03 registrada como já atendida no SUMMARY, com a justificativa da D-12 e sem nenhuma remoção de código
</success_criteria>

<output>
Criar `.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-01-SUMMARY.md` ao terminar
</output>
