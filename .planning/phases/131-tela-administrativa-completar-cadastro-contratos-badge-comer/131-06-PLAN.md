---
phase: 131-tela-administrativa-completar-cadastro-contratos-badge-comer
plan: 06
type: execute
wave: 5
depends_on: ["131-05"]
files_modified:
  - app/Http/Controllers/ContratoAdminController.php
  - routes/web.php
  - resources/js/Pages/Admin/ContratoDetalhe.jsx
  - resources/js/Pages/Admin/ContratosLiberacaoManual.jsx
  - app/Http/Controllers/ContratoLiberacaoManualController.php
  - app/Notifications/ContratoPresoNotification.php
  - tests/Feature/Phase130/LiberacaoManualTest.php
  - tests/Feature/Phase130/LiberacaoManualEstadoRealTest.php
  - tests/Feature/Phase130/AlertaContratoPresoTest.php
  - tests/Feature/Phase131/LiberacaoManualAbsorvidaTest.php
  - tests/Feature/Phase131/LiberacaoManualRotaAntigaRemovidaTest.php
autonomous: true
requirements: [UI-05, UI-06]

must_haves:
  truths:
    - "Liberar manualmente é uma ação dentro da tela de detalhe, ao lado das demais — o Administrativo tem uma tela só"
    - "A rota antiga da Fase 130 não existe mais: dá 404, não redireciona"
    - "As mitigações de segurança da Fase 130 continuam valendo depois da absorção"
    - "A faixa vermelha com o estado real continua aparecendo ANTES de confirmar em causa problemática"
    - "O alerta do sino continua levando a uma tela que existe — e agora cai direto na empresa certa"
  artifacts:
    - path: "app/Http/Controllers/ContratoAdminController.php"
      provides: "liberarManual() com a validação e o guard de IDOR preservados"
      exports: ["liberarManual"]
    - path: "tests/Feature/Phase131/LiberacaoManualAbsorvidaTest.php"
      provides: "Prova de que as mitigações da Fase 130 sobreviveram"
    - path: "tests/Feature/Phase131/LiberacaoManualRotaAntigaRemovidaTest.php"
      provides: "Prova de 404 (não redirect) nas rotas antigas"
    - path: "app/Notifications/ContratoPresoNotification.php"
      provides: "URL do alerta do sino apontando para a tela nova"
      contains: "admin.contratos.show"
  key_links:
    - from: "app/Http/Controllers/ContratoAdminController.php"
      to: "App\\Services\\Operacional\\EmpresaOperacionalRouter::liberarEmpresa()"
      via: "delegação intacta, backend definitivo"
      pattern: "liberarEmpresa"
    - from: "app/Http/Controllers/ContratoAdminController.php"
      to: "App\\Models\\ContratoLiberacao::MOTIVOS_MANUAIS"
      via: "Rule::in fechado no motivo_slug"
      pattern: "MOTIVOS_MANUAIS"
    - from: "app/Notifications/ContratoPresoNotification.php"
      to: "admin.contratos.show"
      via: "url do alerta do sino, montada no construtor"
      pattern: "admin.contratos.show"
---

<objective>
Fechar a D-10: a tela descartável da Fase 130 é absorvida como ação dentro do detalhe da empresa,
e a rota antiga é REMOVIDA (404, não redirect). O Administrativo passa a ter uma tela só.

Purpose: a Fase 130 construiu o backend definitivo da liberação manual com uma superfície
explicitamente descartável. Esta é a hora de descartá-la — sem perder nenhuma das mitigações que
o `130-SECURITY.md` fechou (41/41 ameaças).

⚠️ **O backend NÃO é reescrito.** `EmpresaOperacionalRouter::liberarEmpresa()` e
`ContratoLiberacao` ficam intactos. O que se move é a superfície: rota, tela e o corpo do
`store()`, que passa a ser a action `liberarManual()` do `ContratoAdminController`.

⚠️ **A rota antiga tem um consumidor escondido fora do `routes/web.php`:**
`app/Notifications/ContratoPresoNotification.php` linha 65 monta
`url: route('contratos.liberacao-manual.index')` **no construtor**. É o alerta do sino da Fase 130
(D-01 daquela fase: o canal é o sino, nunca e-mail). Remover a rota sem repontar esta linha faz o
alerta lançar `RouteNotFoundException` na hora de notificar — a rede de segurança inteira para de
avisar, em produção, sem ninguém perceber.

Output: a action absorvida, a rota nova, o modal na tela de detalhe, a URL do alerta repontada, a
remoção do controller e da tela antigos, o reapontamento dos testes da Fase 130 e os 2 testes novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-CONTEXT.md
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-UI-SPEC.md
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-PATTERNS.md
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-VALIDATION.md
@.planning/phases/130-rede-de-seguranca-do-fluxo-administrativo-v22-0/130-SECURITY.md
@CLAUDE.md

<interfaces>
`app/Http/Controllers/ContratoLiberacaoManualController.php` (125 linhas) — a fonte a absorver.
O `store()` (linhas 81-123) contém, e a absorção precisa preservar LITERALMENTE:
- `Rule::in(ContratoLiberacao::MOTIVOS_MANUAIS)` no `motivo_slug` (D-12 da Fase 130 — lista
  fechada, nunca string livre)
- `motivo_detalhe` obrigatório, `min:5`, `max:1000` — obrigatório MESMO com o slug preenchido
- `exists:companies,id`, `exists:servicos,id`, `exists:contrato_assinaturas,id`
- guard de IDOR (T-130-04-03): o contrato informado tem que pertencer à MESMA empresa/serviço do
  POST, senão `abort(422, 'O contrato informado não pertence a esta empresa/serviço.')`
- delegação a `EmpresaOperacionalRouter::liberarEmpresa($company, $servico, ContratoLiberacao::VIA_MANUAL,
  contrato: ..., liberadoPorUserId: $request->user()->id, motivo: ..., motivoSlug: ...)`

`app/Models/ContratoLiberacao.php` — `MOTIVOS_MANUAIS` / `MOTIVOS_MANUAIS_LABELS` (linhas 80-96)
e `VIA_MANUAL`. Os 4 rótulos são reusados LITERALMENTE (D-10), não reescritos.

`app/Services/Contratos/ContratosPresosService.php` — `causa()` devolve
`recusado_pelo_cliente|prazo_expirado|cancelado|erro_tecnico|...`, que é o que decide a faixa
vermelha da D-11 da Fase 130.

`routes/web.php` linhas 107-117 — o par de rotas `contratos.liberacao-manual.index|store` que sai.
`routes/web.php` linhas 98-105 — `contratos.pdf-assinado`, que FICA como está (`role:admin`),
fora do escopo desta absorção.
</interfaces>
</context>

<aviso_de_ambiente>
⚠️ **Filtro do PHPUnit que não casa com nada SAI 0 — e ainda varre a suíte inteira.**

Medido neste repositório: `C:\xampp\php\php.exe artisan test --filter=ClasseInexistente` carrega
TODA a suíte, imprime `INFO No tests found.` e devolve **EXIT_CODE 0**. Duas consequências:
1. Um `<verify>` que aponta para classe de teste ainda inexistente **passa em falso**.
2. Dispara exatamente a varredura sem filtro que a `131-VALIDATION.md` proíbe (estoura o timeout de
   300s num ponto pré-existente do `MercadoLivreAdsService`). Filtro sem match ≠ filtro que limita.

Regras desta fase por causa disso:
- Nenhuma task verifica com `--filter=` de classe criada por task POSTERIOR. O teste nasce na MESMA
  task do código que ele prova — é por isso que a Task 1 abaixo já cria o teste da rota removida e
  já reaponta a asserção de URL do `AlertaContratoPresoTest`.
- Ao rodar qualquer `--filter=`, conferir que a saída traz `Tests: N` com **N > 0**.
  `No tests found` é FALHA, mesmo com exit code 0.
- `artisan route:list` também sai 0 quando nada casa; por isso o comando passa a saída por
  `grep -c`, que falha com 0 ocorrências.
</aviso_de_ambiente>

<tasks>

<task type="auto">
  <name>Task 1: Absorver liberarManual() preservando as mitigações da Fase 130</name>
  <files>app/Http/Controllers/ContratoAdminController.php, routes/web.php, app/Notifications/ContratoPresoNotification.php, tests/Feature/Phase131/LiberacaoManualRotaAntigaRemovidaTest.php, tests/Feature/Phase130/AlertaContratoPresoTest.php</files>
  <read_first>
    - `app/Http/Controllers/ContratoLiberacaoManualController.php` — arquivo INTEIRO (125 linhas), especialmente `store()` linhas 81-123 e os comentários que nomeiam as ameaças (T-130-04-03, T-130-04-05)
    - `.planning/phases/130-rede-de-seguranca-do-fluxo-administrativo-v22-0/130-SECURITY.md` — as mitigações que precisam sobreviver
    - `app/Models/ContratoLiberacao.php` linhas 70-100 — `MOTIVOS_MANUAIS`, `MOTIVOS_MANUAIS_LABELS`, `VIA_MANUAL`
    - `app/Services/Operacional/EmpresaOperacionalRouter.php` — assinatura de `liberarEmpresa()` (não alterar)
    - `app/Http/Controllers/ContratoAdminController.php` — o arquivo dos planos 131-03/04/05
    - `app/Notifications/ContratoPresoNotification.php` linhas 40-80 — o construtor monta `url: route('contratos.liberacao-manual.index')`; é o ÚNICO consumidor da rota fora do `routes/web.php` e dos testes
  </read_first>
  <action>
    Acrescentar ao `ContratoAdminController` a action
    **`liberarManual(Request $request, EmpresaOperacionalRouter $router): RedirectResponse`**,
    copiando o corpo de `ContratoLiberacaoManualController::store()` **verbatim**, incluindo os
    comentários que nomeiam as ameaças. Só duas coisas mudam:
    1. o destino do retorno — `back()` continua servindo, já que a ação passa a ser disparada de
       dentro do detalhe (`admin.contratos.show`);
    2. o namespace/classe onde ela mora.

    ⛔ NÃO "melhorar", NÃO simplificar, NÃO trocar `abort(422)` por exception, NÃO afrouxar o
    `Rule::in`, NÃO tornar `motivo_detalhe` opcional. Cada uma dessas linhas fecha uma ameaça
    catalogada no `130-SECURITY.md`; o `LiberacaoManualAbsorvidaTest` existe para pegar exatamente
    isso.

    Acrescentar à prop `contratos` de `show()` (plano 131-04), se ainda não estiver lá, o que o
    modal precisa: `causa` (já previsto) e um booleano `ja_liberado` (`liberado_em !== null`), para
    a UI decidir se oferece "Liberar manualmente". Também expor `motivos_manuais` =
    `ContratoLiberacao::MOTIVOS_MANUAIS_LABELS` como prop da tela de detalhe — o mesmo array que a
    tela antiga já consumia.

    Em `routes/web.php`:
    - acrescentar, dentro do grupo `admin.contratos.`:
      `Route::post('/liberacao-manual', [ContratoAdminController::class, 'liberarManual'])->name('liberacao-manual');`
    - **REMOVER** o par de rotas antigo `contratos.liberacao-manual.index` e
      `contratos.liberacao-manual.store` (linhas ~107-117), junto com o comentário de bloco que as
      apresenta. Remoção, não redirect: a `131-VALIDATION.md` exige 404.
    - ⚠️ NÃO tocar em `contratos.pdf-assinado`.

    **Repontar o alerta do sino — obrigatório na MESMA task da remoção.** Em
    `app/Notifications/ContratoPresoNotification.php`, trocar
    `url: route('contratos.liberacao-manual.index')` por
    `url: route('admin.contratos.show', $contrato->company_id)`.
    Dois motivos: (1) sem isso a notificação lança `RouteNotFoundException` no construtor e a rede
    de segurança da Fase 130 para de avisar em produção; (2) o destino fica melhor do que era — o
    alerta passa a cair direto na empresa do contrato parado, em vez de numa lista genérica.
    A audiência não muda: a rota antiga era `role:admin` e a nova exige `admin.contratos`, que a
    D-09 concede a todo `role:admin` no dia do deploy.
    ⚠️ O construtor já recebe o `ContratoAssinatura` e já usa `$contrato->company->name` — usar
    `$contrato->company_id`, sem introduzir query nova ali.

    **Testes NA MESMA TASK (obrigatório — ver `<aviso_de_ambiente>`):**
    1. Criar `tests/Feature/Phase131/LiberacaoManualRotaAntigaRemovidaTest.php` já aqui — é o teste
       da remoção que ESTA task faz: `Route::has('contratos.liberacao-manual.index')` e `...store`
       são `false`; `GET` e `POST` no caminho antigo devolvem **404 exato** (assertar o status,
       nunca `assertRedirect` — ninguém pode "resolver" a remoção com um redirect).
    2. Reapontar, também aqui, a única asserção de
       `tests/Feature/Phase130/AlertaContratoPresoTest.php` que trava a URL do alerta
       (`assertSame(route('contratos.liberacao-manual.index'), $dados['url'])`, linha ~57) para
       `assertSame(route('admin.contratos.show', $contrato->company_id), $dados['url'])`.
       Tem que ser NESTA task, não na Task 3: quem remove a rota e reponta a notificação é esta
       task, e deixar a asserção velha para depois é exatamente o buraco em que a rede de segurança
       da Fase 130 pararia de avisar sem ninguém ver.
    ⚠️ Os outros dois arquivos da Fase 130 (`LiberacaoManualTest`, `LiberacaoManualEstadoRealTest`)
    ficam VERMELHOS entre esta task e a Task 3 — isso é esperado e a Task 3 os reaponta. Não
    "consertar" apagando asserção.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter="LiberacaoManualRotaAntigaRemovidaTest|AlertaContratoPresoTest" && C:\xampp\php\php.exe artisan route:list --name=admin.contratos.liberacao-manual -v | grep -c "permission:admin.contratos"</automated>
  </verify>
  <acceptance_criteria>
    - A saída do `--filter` traz `Tests:` com no mínimo 5 testes verdes — `No tests found` é FALHA, mesmo saindo 0
    - O `route:list ... | grep -c` retorna 1 ou mais (o `grep` é o gate: `route:list` sozinho sai 0 mesmo sem casar nada)
    - `C:\xampp\php\php.exe artisan route:list --name=admin.contratos` lista `liberacao-manual` com `permission:admin.contratos`
    - `C:\xampp\php\php.exe artisan route:list --name=contratos.liberacao-manual` não lista NENHUMA rota
    - `C:\xampp\php\php.exe artisan route:list --name=contratos.pdf-assinado` continua listando a rota, com `role:admin`
    - `grep -c "MOTIVOS_MANUAIS" app/Http/Controllers/ContratoAdminController.php` retorna 2 ou mais (`Rule::in` + labels na prop)
    - `grep -c "min:5" app/Http/Controllers/ContratoAdminController.php` retorna 1 — `motivo_detalhe` continua obrigatório com mínimo de 5
    - `grep -c "não pertence a esta empresa" app/Http/Controllers/ContratoAdminController.php` retorna 1 — o guard de IDOR sobreviveu
    - `grep -c "contratos.liberacao-manual" app/Notifications/ContratoPresoNotification.php` retorna 0, e `grep -c "admin.contratos.show"` no mesmo arquivo retorna 1
    - `C:\xampp\php\php.exe artisan test --filter=AlertaContratoPresoTest` sai 0 — a notificação continua sendo construída sem lançar
  </acceptance_criteria>
  <done>Comando de <verify> verde e todos os itens de <acceptance_criteria> conferidos — inclusive as conferencias por reconsulta ao banco.</done>
</task>

<task type="auto">
  <name>Task 2: Modal de liberação no detalhe + remoção da tela e do controller antigos</name>
  <files>resources/js/Pages/Admin/ContratoDetalhe.jsx, resources/js/Pages/Admin/ContratosLiberacaoManual.jsx, app/Http/Controllers/ContratoLiberacaoManualController.php</files>
  <read_first>
    - `resources/js/Pages/Admin/ContratosLiberacaoManual.jsx` — arquivo INTEIRO (198 linhas): é a fonte do TEXTO e do fluxo do modal, que a D-10 manda reusar literalmente
    - `resources/js/Pages/Admin/ContratoDetalhe.jsx` — a tela dos planos 131-04/05
    - `131-UI-SPEC.md` — seção "Liberar manualmente (D-10 — absorve a Fase 130, preserva D-11)", com os 4 rótulos de motivo e as 4 faixas de destaque, texto por texto
  </read_first>
  <action>
    Levar para `Admin/ContratoDetalhe.jsx` o botão **"Liberar manualmente"** (disponível quando o
    contrato ainda não foi liberado) e o modal, reusando **literalmente** o texto já validado na
    tela antiga — não reescrever:
    - select de motivo com os 4 rótulos de `MOTIVOS_MANUAIS_LABELS` vindos como prop
    - textarea de detalhe obrigatória, mínimo 5 caracteres, com o placeholder literal
      "Explique o que aconteceu, mesmo que seja um resumo curto."
    - CTA "Confirmar liberação"
    - **Faixa de destaque vermelha ANTES de confirmar (D-11 da Fase 130 — preservar exatamente)**,
      exibida quando `causa` é `recusado_pelo_cliente`, `prazo_expirado`, `cancelado` ou
      `erro_tecnico`, com os 4 textos literais da tabela do UI-SPEC. Esta faixa é o ponto da D-11:
      o operador vê o estado REAL antes de confirmar, e a liberação fica registrada com o nome dele
      e o motivo.
    - `POST` para `admin.contratos.liberacao-manual` enviando `company_id`, `servico_id`,
      `contrato_assinatura_id`, `motivo_slug` e `motivo_detalhe`.

    Em seguida, **apagar**:
    - `resources/js/Pages/Admin/ContratosLiberacaoManual.jsx`
    - `app/Http/Controllers/ContratoLiberacaoManualController.php`
    Antes de apagar cada um, rodar um `grep -rn` no repositório (excluindo `node_modules`,
    `public/build` e `.planning`) confirmando que não sobrou nenhuma referência viva. Se alguma
    aparecer fora dos testes tratados na Task 3, PARAR e reportar em vez de apagar.

    Rodar `npm run build` e confirmar que `Pages/Admin/ContratoDetalhe.jsx` continua no manifest e
    que `Pages/Admin/ContratosLiberacaoManual.jsx` **saiu** dele.
  </action>
  <verify>
    <automated>npm run build && grep -c "Pages/Admin/ContratosLiberacaoManual.jsx" public/build/manifest.json; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` termina sem erro
    - `grep -c "Pages/Admin/ContratoDetalhe.jsx" public/build/manifest.json` retorna 1 ou mais
    - `grep -c "Pages/Admin/ContratosLiberacaoManual.jsx" public/build/manifest.json` retorna 0
    - Os dois arquivos antigos não existem mais (`test ! -f resources/js/Pages/Admin/ContratosLiberacaoManual.jsx` e `test ! -f app/Http/Controllers/ContratoLiberacaoManualController.php`)
    - `grep -c "Explique o que aconteceu, mesmo que seja um resumo curto." resources/js/Pages/Admin/ContratoDetalhe.jsx` retorna 1 — o texto foi reusado, não reescrito
    - Os 4 textos de faixa de destaque da D-11 aparecem literalmente no arquivo novo
  </acceptance_criteria>
  <done>Comando de <verify> verde e todos os itens de <acceptance_criteria> conferidos — inclusive as conferencias por reconsulta ao banco.</done>
</task>

<task type="auto">
  <name>Task 3: Reapontar os testes restantes da Fase 130 + o teste da absorção</name>
  <files>tests/Feature/Phase130/LiberacaoManualTest.php, tests/Feature/Phase130/LiberacaoManualEstadoRealTest.php, tests/Feature/Phase131/LiberacaoManualAbsorvidaTest.php</files>
  <read_first>
    - `tests/Feature/Phase130/LiberacaoManualTest.php` — arquivo inteiro (200 linhas); 12 referências à rota/tela antigas
    - `tests/Feature/Phase130/LiberacaoManualEstadoRealTest.php` — 8 referências; é o teste da D-11 (estado real antes de confirmar)
    - `tests/Feature/Phase130/AlertaContratoPresoTest.php` — ⚠️ **já reapontado na Task 1**, junto com a mudança de URL que ele prova; não mexer de novo
    - `tests/Feature/Phase130/LiberacaoManualCorridaTest.php` — NÃO referencia a rota (exercita o backend direto); serve de confirmação de que a lógica não muda
  </read_first>
  <action>
    **Reapontar, com o menor diff possível**, as referências dos 2 arquivos da Fase 130 que
    sobraram — `LiberacaoManualTest` e `LiberacaoManualEstadoRealTest` (o `AlertaContratoPresoTest`
    já foi reapontado na Task 1):
    `route('contratos.liberacao-manual.store')` → `route('admin.contratos.liberacao-manual')`;
    `route('contratos.liberacao-manual.index')` → `route('admin.contratos.show', $company)`;
    componente Inertia `'Admin/ContratosLiberacaoManual'` → `'Admin/ContratoDetalhe'`, ajustando o
    nome da prop consultada quando o teste olhar a lista de contratos (`contratos` continua sendo o
    nome da prop no detalhe).
    ⛔ NÃO enfraquecer nenhuma asserção para "fazer passar". Se uma asserção não couber na tela
    nova, isso é sinal de que a absorção perdeu comportamento — PARAR e reportar, não relaxar o
    teste. O `LiberacaoManualEstadoRealTest` em especial é a prova viva da D-11.

    **Criar `tests/Feature/Phase131/LiberacaoManualAbsorvidaTest.php`** (D-10 / segurança) — mesmo
    corpo do teste da Fase 130, na rota nova, provando que as mitigações sobreviveram:
    - `motivo_slug` fora de `MOTIVOS_MANUAIS` → erro de validação, nada gravado
    - `motivo_detalhe` ausente ou com menos de 5 caracteres → erro de validação, nada gravado
      (mesmo com `motivo_slug` válido)
    - `company_id`/`servico_id`/`contrato_assinatura_id` inexistentes → erro de validação
    - contrato de OUTRA empresa/serviço → 422 (guard de IDOR), nada gravado
    - caminho feliz grava `ContratoLiberacao` com `via = manual`, autor e motivo — conferido por
      **RECONSULTA AO BANCO**
    - usuário sem `admin.contratos` → 403

    `tests/Feature/Phase131/LiberacaoManualRotaAntigaRemovidaTest.php` **já existe** — nasceu na
    Task 1, junto com a remoção que ele prova (404 exato, nunca redirect). Esta task só confere que
    ele continua verde: não recriar, não relaxar, não trocar por `assertRedirect`.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter="Phase130|Phase131"</automated>
  </verify>
  <acceptance_criteria>
    - `--filter="Phase130|Phase131"` sai 0 — nenhum teste da Fase 130 ficou vermelho pela absorção
    - `grep -rc "contratos.liberacao-manual" tests/Feature/Phase130/` retorna 0 em todos os arquivos
    - `LiberacaoManualRotaAntigaRemovidaTest` (criado na Task 1) continua verde e assere status **404** exato (não `assertRedirect`, não `assertStatus(302)`)
    - `LiberacaoManualAbsorvidaTest` tem no mínimo 6 testes verdes
    - `C:\xampp\php\php.exe artisan test --filter="Phase126|Phase129|Phase130|Phase131"` sai 0
  </acceptance_criteria>
  <done>Comando de <verify> verde e todos os itens de <acceptance_criteria> conferidos — inclusive as conferencias por reconsulta ao banco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → liberação operacional | ação que libera uma empresa para o operacional MESMO sem contrato assinado |
| superfície antiga → superfície nova | janela de regressão durante a troca de rota/tela |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-06-01 | Tampering (IDOR) | `contrato_assinatura_id` no POST | mitigate | Guard preservado verbatim da Fase 130 (T-130-04-03): o contrato precisa pertencer à mesma empresa/serviço, senão `abort(422)`; asserido por teste dedicado |
| T-131-06-02 | Tampering | `motivo_slug` como string livre | mitigate | `Rule::in(ContratoLiberacao::MOTIVOS_MANUAIS)` preservado — lista fechada, D-12 da Fase 130 |
| T-131-06-03 | Repudiation | liberação sem justificativa | mitigate | `motivo_detalhe` obrigatório com `min:5` mesmo com o slug preenchido, e `liberadoPorUserId` sempre de `$request->user()->id` |
| T-131-06-04 | Elevation of Privilege | rota nova | mitigate | Herda `permission:admin.contratos`; teste de 403 para quem não tem a key |
| T-131-06-05 | Elevation of Privilege | rota antiga sobrevivendo com `role:admin` | mitigate | Rotas removidas de fato (404 asserido, não redirect) — uma rota órfã manteria um caminho paralelo fora do novo controle |
| T-131-06-06 | Tampering | perda silenciosa de mitigação na cópia | mitigate | O corpo do `store()` é copiado verbatim com os comentários de ameaça; `LiberacaoManualAbsorvidaTest` reexecuta os mesmos cenários na rota nova |
| T-131-06-07 | Denial of legitimate action | operador liberando às cegas | mitigate | Faixa vermelha com o estado real ANTES de confirmar (D-11 da Fase 130), preservada com os 4 textos literais |
| T-131-06-08 | Denial of legitimate action | `ContratoPresoNotification` apontando para rota removida | mitigate | A URL do alerta é repontada para `admin.contratos.show` na MESMA task da remoção; `AlertaContratoPresoTest` prova que a notificação continua sendo construída sem lançar. Sem isso, a rede de segurança da Fase 130 para de avisar em silêncio |
| T-131-06-SC | Tampering | npm/composer installs | mitigate | Nenhum pacote novo; qualquer install descoberto exige checkpoint humano antes |
</threat_model>

<verification>
1. `C:\xampp\php\php.exe artisan test --filter="Phase130|Phase131"` sai 0
2. Regressão cruzada completa: `C:\xampp\php\php.exe artisan test --filter="Phase126|Phase129|Phase130|Phase131"` sai 0
3. ⛔ NUNCA rodar a suíte sem filtro
4. `C:\xampp\php\php.exe artisan route:list --name=contratos.liberacao-manual` não lista nada; `--name=admin.contratos` lista as 7 rotas da fase
5. `npm run build` verde; `ContratoDetalhe.jsx` no manifest e `ContratosLiberacaoManual.jsx` fora dele
6. Conferência por RECONSULTA AO BANCO: após uma liberação manual pela tela nova, reconsultar
   `contrato_liberacoes` e confirmar `via`, autor, `motivo_slug` e `motivo` — nunca acreditar no toast
</verification>

<success_criteria>
- Liberar manualmente é ação do detalhe da empresa; não existe mais uma segunda tela
- `GET`/`POST /admin/contratos/liberacao-manual` devolvem 404 (não redirect)
- Todas as mitigações da Fase 130 continuam asseridas por teste na rota nova
- A faixa vermelha com o estado real continua aparecendo antes de confirmar
- `Admin/ContratosLiberacaoManual.jsx` e `ContratoLiberacaoManualController.php` não existem mais
- O alerta do sino aponta para `admin.contratos.show` e continua sendo construído sem lançar
- `--filter="Phase126|Phase129|Phase130|Phase131"` verde
</success_criteria>

<output>
Criar `.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-06-SUMMARY.md` ao terminar
</output>
