---
phase: 131-tela-administrativa-completar-cadastro-contratos-badge-comer
plan: 03
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - app/Http/Controllers/ContratoAdminController.php
  - routes/web.php
  - resources/js/Layouts/AppLayout.jsx
  - resources/js/Pages/Admin/Contratos.jsx
  - tests/Feature/Phase131/ContratoAdminPermissaoTest.php
  - tests/Feature/Phase131/ContratoAdminListaTest.php
  - tests/Feature/Phase131/ContratoAdminListaExcluiPolosTest.php
autonomous: true
requirements: [UI-01, UI-05, UI-06]

must_haves:
  truths:
    - "Quem tem `admin.contratos` — por ser admin OU por ter recebido a permission num setor — abre a lista de contratos"
    - "Quem não tem a permission recebe 403, e o item não aparece no menu"
    - "A lista mostra os 7 estados com filtro por situação e busca por empresa"
    - "O resumo devolve exatamente 7 contagens, uma por estado, sem agrupar"
    - "Empresa cujo único serviço é isento de contrato nunca aparece na lista"
  artifacts:
    - path: "app/Http/Controllers/ContratoAdminController.php"
      provides: "index() — lista, resumo de 7 contagens, filtro e busca"
      exports: ["index"]
    - path: "routes/web.php"
      provides: "Grupo admin/contratos sob permission:admin.contratos"
      contains: "permission:admin.contratos"
    - path: "resources/js/Pages/Admin/Contratos.jsx"
      provides: "Tela da lista com grid de 7 cards clicáveis como filtro"
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Middleware/EnsurePermission.php"
      via: "alias permission:admin.contratos"
      pattern: "permission:admin\\.contratos"
    - from: "resources/js/Layouts/AppLayout.jsx"
      to: "admin.contratos.index"
      via: "item de menu com permission"
      pattern: "admin\\.contratos\\.index"
    - from: "resources/js/Pages/Admin/Contratos.jsx"
      to: "resources/js/lib/contratoStatus.js"
      via: "import dos rótulos e cores"
      pattern: "contratoStatus"
---

<objective>
A tela onde o Administrativo enxerga o estado real de cada contrato sem abrir o banco: lista
com filtro por situação, busca por empresa e resumo com a contagem de cada uma das 7 situações
(UI-01/D-04), atrás da permission própria `admin.contratos` (UI-05/D-09).

Purpose: é a porta de entrada da fase — o detalhe (plano 131-04) e as ações (131-05, 131-06)
penduram nela. Sem esta tela, `admin.contratos` seria uma key declarada e inoperante.

Output: `ContratoAdminController::index()`, o grupo de rotas com `permission:admin.contratos`,
o item de menu, a tela `Admin/Contratos.jsx` e os 3 testes de gate/lista/isenção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-CONTEXT.md
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-UI-SPEC.md
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-PATTERNS.md
@.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-VALIDATION.md
@CLAUDE.md

<interfaces>
`app/Http/Middleware/EnsurePermission.php` — já registrado como alias `permission:`; aceita
múltiplas keys variádicas e dá `abort(403)` quando nenhuma casa.

`app/Models/ContratoAssinatura.php`
- `STATUS_TODOS` — os 7 estados, na ordem canônica
- `company(): BelongsTo`, `servico(): BelongsTo`

`app/Services/Contratos/ContratosPresosService.php`
- `dataBase()`, `diasParado()`, `causa()` — puros, aplicáveis em memória
- ⛔ `listar()` — **NÃO USAR**: filtra por "preso" e esconderia `assinado`/`cancelado` e todo
  contrato dentro do limiar. A UI-01 precisa dos 7 estados SEMPRE

`app/Models/Servico.php` — `exigeContrato(): bool` sobre `servicos.exige_contrato`: a regra
canônica de isenção (D9 da milestone). Nunca comparar `setor === 'polos'` na mão.

`resources/js/lib/contratoStatus.js` (plano 131-01) — `CONTRATO_STATUS_LABELS` (7),
`CONTRATO_STATUS_CLS`, `rotuloContrato()`, `classeContrato()`, `formatarHaDias()`,
`SEM_CONTRATO`, `SEM_CONTRATO_LABEL`.
</interfaces>
</context>

<aviso_de_ambiente>
⚠️ **Filtro do PHPUnit que não casa com nada SAI 0 — e ainda varre a suíte inteira.**

Medido neste repositório: `C:\xampp\php\php.exe artisan test --filter=ClasseInexistente` carrega
TODA a suíte, imprime `INFO No tests found.` e devolve **EXIT_CODE 0**. Duas consequências:
1. Um `<verify>` que aponta para classe de teste ainda inexistente **passa em falso**.
2. Dispara exatamente a varredura sem filtro que a `131-VALIDATION.md` proíbe (estoura o timeout de
   300s num ponto pré-existente do `MercadoLivreAdsService`). Filtro sem match ≠ filtro que limita.

Regras desta fase por causa disso:
- Nenhuma task verifica com `--filter=` de classe criada por task POSTERIOR. O teste nasce na MESMA
  task do código que ele prova — é por isso que a Task 1 abaixo já cria o `ContratoAdminListaTest`.
- Ao rodar qualquer `--filter=`, conferir que a saída traz `Tests: N` com **N > 0**.
  `No tests found` é FALHA, mesmo com exit code 0.
- `artisan route:list` também não serve sozinho como gate: quando nada casa, ele imprime aviso e
  ainda assim sai 0. Por isso os comandos abaixo passam a saída por `grep -c`, que falha com 0
  ocorrências.
</aviso_de_ambiente>

<tasks>

<task type="auto">
  <name>Task 1: ContratoAdminController::index() + grupo de rotas com permission:admin.contratos</name>
  <files>app/Http/Controllers/ContratoAdminController.php, routes/web.php, tests/Feature/Phase131/ContratoAdminListaTest.php</files>
  <read_first>
    - `app/Http/Controllers/ContratoLiberacaoManualController.php` — arquivo inteiro (125 linhas): imports, `index()` com array ACHATADO (nunca o model inteiro), e o bloco de validação/IDOR que o plano 131-06 vai absorver
    - `routes/web.php` linhas 639-654 — o molde CORRETO: `Route::middleware('permission:core.empresas')` e `permission:shopee.empresas`
    - `routes/web.php` linhas 1021-1058 — o molde a NÃO copiar: o grupo `/administrativo` inteiro roda sob `role:admin` mesmo com `admin.empresas` no catálogo; colar as rotas novas ali deixaria a UI-05 declarada e inoperante
    - `app/Http/Controllers/ComercialController.php` linhas 180-300 — paginação manual via `LengthAwarePaginator` preservando `queryString`, e contagens agregadas ANTES do filtro
    - `app/Services/Contratos/ContratosPresosService.php` linhas 60-160
    - `app/Models/ContratoAssinatura.php` linhas 100-130
    - `tests/Feature/Phase130/LiberacaoManualTest.php` — helpers privados (`admin()`, `naoAdmin()`), `assertInertia(fn ($page) => $page->component(...)->has(...)->where(...))` e a disciplina de conferir por reconsulta ao banco; é o molde do arquivo de teste que nasce nesta task
    - `database/factories/ContratoAssinaturaFactory.php` — `servico_id` obrigatório; states `emAndamento()` e `assinado()`
  </read_first>
  <action>
    Criar `app/Http/Controllers/ContratoAdminController.php` (namespace `App\Http\Controllers`),
    com docblock de classe em pt-BR explicando o papel da tela e citando a D-01 (duas telas) e a
    D-10 (esta classe vai absorver a liberação manual no plano 131-06).

    `index(Request $request, ContratosPresosService $presos): \Inertia\Response`:

    1. **Universo (D9 — isenção)**: empresas ativas que tenham ao menos um `ContratoServico` ativo
       cujo `servico.exige_contrato` seja `true`. Filtrar NO BACKEND, antes de paginar — nunca
       esconder no client. Eager load `contratosServico.servico` para não abrir query por linha.
    2. **Linhas**: uma por par (empresa, serviço que exige contrato). Buscar os contratos numa
       única query (`ContratoAssinatura::whereIn('company_id', ...)` com eager load de
       `company` e `servico`), indexar em memória por `company_id` + `servico_id` pegando o de
       maior `id`, e casar com cada par. Par sem contrato vira linha com
       `status = 'aguardando_administrativo'` (estado só de exibição, ver passo 4).
    3. **Array ACHATADO por linha** — nunca o model inteiro, nunca dado de signatário
       (nome/e-mail/CPF) atravessando para o browser: `contrato_id` (nulo quando ainda não há
       contrato), `company_id`, `company_nome`, `servico_id`, `servico_nome`, `status`,
       `dias_parado`, `causa`, `enviado_em`, `assinado_em`, `liberado_em`,
       `cancelamento_solicitado_em`, todos os timestamps em `toIso8601String()`.
       `dias_parado` vem de `$presos->diasParado($contrato)`; para a linha sem contrato, de
       `companies.created_at` até agora (mesma base que o badge do Comercial usa).
    4. **Resumo (D-04)**: array com EXATAMENTE 7 chaves, uma por valor de
       `ContratoAssinatura::STATUS_TODOS`, todas inicializadas em zero e incrementadas sobre a
       coleção completa — **antes** de aplicar o filtro de situação, como o `pendencia_counts` do
       Comercial faz. NÃO acrescentar uma oitava chave: `aguardando_administrativo` vai numa prop
       separada `sem_contrato_count` (escalar), porque a D-04 trava o resumo em 7 contagens e o
       UI-SPEC trava o grid em 7 colunas.
    5. **Filtros**: `situacao` com whitelist em PHP = `ContratoAssinatura::STATUS_TODOS` mais
       `'aguardando_administrativo'` (qualquer outro valor vira `null`, mesmo padrão do
       `ComercialController`); `q` = busca por `companies.name` ou `companies.cnpj` com `like`.
       Aplicar `situacao` em memória (depende do estado derivado), `q` em SQL.
    6. **Ordenação padrão**: `dias_parado` decrescente — "mais tempo parado primeiro" (UI-SPEC).
    7. **Paginação manual** via `LengthAwarePaginator`, 50 por página, com `path` e `query`
       preservados.
    8. `Inertia::render('Admin/Contratos', [...])` com as props `linhas` (paginator), `filters`,
       `resumo` (7 chaves) e `sem_contrato_count`.

    ⛔ Não usar `ContratosPresosService::listar()` em lugar nenhum deste controller — ele filtra
    por "preso" e esconderia contratos saudáveis, que a UI-01 precisa mostrar.

    Em `routes/web.php`, criar um bloco NOVO imediatamente APÓS o fechamento do grupo
    `Módulo Administrativo` (o grupo `role:admin` que termina por volta da linha 1058), com
    comentário em pt-BR explicando por que ele fica FORA daquele grupo:

    `Route::middleware(['auth', 'verified', 'permission:admin.contratos'])->prefix('administrativo/contratos')->name('admin.contratos.')->group(...)`
    contendo por enquanto uma única rota: `Route::get('/', [ContratoAdminController::class, 'index'])->name('index');`

    ⚠️ `permission:admin.contratos`, NUNCA `role:admin` — se o grupo `role:admin` envolver estas
    rotas, um usuário que receba `admin.contratos` pela tela de setores continua batendo 403 e a
    UI-05 vira letra morta.
    ⚠️ NÃO tocar na rota `contratos.pdf-assinado` (`routes/web.php` ~linha 98-105): ela continua
    `role:admin`, está fora do escopo da D-10 e desta fase.
    ⚠️ NÃO remover ainda as rotas `contratos.liberacao-manual.*` — a remoção é do plano 131-06,
    que só acontece depois da ação estar absorvida; removê-las agora deixaria a liberação manual
    sem nenhuma superfície.

    **Teste NA MESMA TASK (obrigatório — ver `<aviso_de_ambiente>`):** criar aqui
    `tests/Feature/Phase131/ContratoAdminListaTest.php` com o núcleo do que ESTA task entrega:
    (a) `GET route('admin.contratos.index')` como admin devolve 200 e o componente `Admin/Contratos`;
    (b) a prop `resumo` tem EXATAMENTE 7 chaves, iguais a `ContratoAssinatura::STATUS_TODOS`;
    (c) `sem_contrato_count` existe, é inteiro e fica FORA do `resumo`.
    Os demais casos (filtro por situação, whitelist, busca, ordenação, ausência de dado de
    signatário) entram na Task 3, no MESMO arquivo. O teste nasce aqui porque um `--filter=` de
    classe inexistente sai 0 sem provar nada — e ainda roda a suíte inteira.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=ContratoAdminListaTest && C:\xampp\php\php.exe artisan route:list --name=admin.contratos.index -v | grep -c "permission:admin.contratos"</automated>
  </verify>
  <acceptance_criteria>
    - A saída do `--filter` traz `Tests:` com no mínimo 3 testes verdes — `No tests found` é FALHA, mesmo saindo 0
    - O `route:list ... | grep -c` retorna 1 ou mais (o `grep` é o gate: `route:list` sozinho sai 0 mesmo sem casar nada)
    - `C:\xampp\php\php.exe artisan route:list --name=admin.contratos.index -v` lista a rota com o middleware `permission:admin.contratos` e SEM `role:admin`
    - A prop `resumo` tem exatamente 7 chaves, iguais a `ContratoAssinatura::STATUS_TODOS`
    - A prop `sem_contrato_count` existe e é um inteiro, fora do `resumo`
    - `grep -c "listar()" app/Http/Controllers/ContratoAdminController.php` retorna 0
    - `grep -c "role:admin" <(git diff routes/web.php)` retorna 0 nas linhas adicionadas do grupo novo
    - As rotas `contratos.liberacao-manual.index` e `contratos.liberacao-manual.store` continuam existindo neste plano
  </acceptance_criteria>
  <done>Comando de <verify> verde e todos os itens de <acceptance_criteria> conferidos — inclusive as conferencias por reconsulta ao banco.</done>
</task>

<task type="auto">
  <name>Task 2: Tela Admin/Contratos.jsx + item de menu</name>
  <files>resources/js/Pages/Admin/Contratos.jsx, resources/js/Layouts/AppLayout.jsx</files>
  <read_first>
    - `resources/js/Pages/Comercial/EmpresasListagem.jsx` linhas 1-16 (imports), 614-638 (grid de cards clicáveis que também são o filtro) e 161+ (componente `Paginator` manual do Inertia)
    - `resources/js/Pages/Admin/Empresas.jsx` linhas 1-70 — família visual `Admin/`: `<main className="p-6">`, comentário de topo em pt-BR, `Chip`/badge local, `cn()` para estado ativo
    - `resources/js/Layouts/AppLayout.jsx` linhas 3-10 (import do `lucide-react`) e 262-274 (grupo `'Administrativo'` do menu)
    - `resources/js/lib/contratoStatus.js` — rótulos/cores/pluralização (plano 131-01)
    - `131-UI-SPEC.md` — "Telas desta fase" (ponto focal = o grid de resumo), "Copywriting Contract", "Estados vazios / carregando / erro genérico", "Spacing Scale", "Typography", "Color"
  </read_first>
  <action>
    Criar `resources/js/Pages/Admin/Contratos.jsx`, exportando um único componente default, com
    comentário de topo em pt-BR explicando a tela e citando UI-01/D-04. Composto SÓ das primitivas
    existentes em `@/Components/ui/` (Card, Table, Badge, Button, Input) — esta fase não cria
    componente de design system novo.

    Estrutura:
    - `<AppLayout>` + `<main className="p-6">`, H1 `text-xl` semibold com ícone `FileSignature` em
      `ecf-yellow` ao lado (único uso de amarelo no cabeçalho).
    - **Ponto focal — grid de resumo/filtro**: 7 cards clicáveis, molde literal do grid de
      pendências do Comercial, mas com `grid grid-cols-2 md:grid-cols-4 xl:grid-cols-7 gap-2`
      (7 estados, não 8 pendências — copiar `xl:grid-cols-8` deixaria uma coluna vazia).
      Cada card: contagem em `text-2xl font-bold tabular-nums` + rótulo `text-[12px] text-white/60`
      vindo de `CONTRATO_STATUS_LABELS`. Card ativo: `border-ecf-yellow bg-ecf-yellow/[0.06]`.
      Clique liga/desliga o filtro `situacao` via `router.get` preservando os demais filtros.
    - Logo abaixo do grid, uma linha de texto (não um oitavo card) com `sem_contrato_count`:
      "N empresas aguardando o Administrativo", clicável para filtrar por
      `situacao=aguardando_administrativo`.
    - Campo de busca (`Input`) por empresa, com `focus:border-ecf-yellow/40`.
    - Tabela compacta (`text-[13px]`, badges `text-[10-11px]`): Empresa · Serviço · Situação ·
      Parado há · Ações. A coluna Situação usa `classeContrato()`/`rotuloContrato()`; "Parado há"
      usa `formatarHaDias()`.
    - Paginação no molde do `Paginator` manual de `EmpresasListagem.jsx`.
    - Estados vazios com a copy LITERAL do UI-SPEC: "Nenhum contrato encontrado" + corpo; busca sem
      resultado com a frase que cita o termo buscado. Carregando = spinner sem texto (nenhuma tela
      `Admin/*.jsx` usa skeleton hoje — não inventar um).

    ⚠️ **A linha ainda NÃO é clicável neste plano.** A rota `admin.contratos.show` nasce no plano
    131-04; chamar `route('admin.contratos.show', ...)` antes disso faz o Ziggy lançar e derruba a
    tela inteira. Deixar a coluna "Ações" preparada e vazia, com comentário apontando o plano 131-04.
    ⚠️ Em nenhum momento o `ecf-yellow` aparece em mais de um elemento ao mesmo tempo (UI-SPEC).
    ⚠️ UI-06: nenhuma ocorrência de "envelope", "signatário", "webhook" ou "Clicksign" em texto
    visível. Toda a copy vem do UI-SPEC ou de `contratoStatus.js` — o executor não inventa texto.

    Em `resources/js/Layouts/AppLayout.jsx`: acrescentar `FileSignature` à lista de imports do
    `lucide-react` (conferir antes se já não está importado) e, no array `children` do grupo
    `'Administrativo'`, a linha
    `{ label: 'Contratos', routeName: 'admin.contratos.index', page: 'Admin/Contratos', icon: FileSignature, permission: 'admin.contratos' }`.
    O item só pode ser acrescentado neste plano porque a rota nomeada passa a existir na Task 1 —
    `AppLayout` resolve `route(child.routeName)` na renderização e lançaria com rota inexistente.

    Rodar `npm run build` ao final e confirmar a página no manifest do Vite (página fora do
    manifest simplesmente some da aplicação — já aconteceu neste projeto).
  </action>
  <verify>
    <automated>npm run build && grep -c "Pages/Admin/Contratos.jsx" public/build/manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` termina sem erro e `grep -c "Pages/Admin/Contratos.jsx" public/build/manifest.json` retorna 1 ou mais
    - `grep -c "xl:grid-cols-7" resources/js/Pages/Admin/Contratos.jsx` retorna 1; `grep -c "xl:grid-cols-8"` retorna 0
    - `grep -c "admin.contratos.show" resources/js/Pages/Admin/Contratos.jsx` retorna 0 — nenhum link para rota inexistente
    - `grep -ci "envelope\|signat\|webhook\|clicksign" resources/js/Pages/Admin/Contratos.jsx` retorna 0
    - `grep -c "admin.contratos.index" resources/js/Layouts/AppLayout.jsx` retorna 1
    - `grep -c "ecf-yellow" resources/js/Pages/Admin/Contratos.jsx` retorna no máximo 4 (cabeçalho, card ativo — borda e fundo — e foco do input), sem terceiro elemento amarelo simultâneo
  </acceptance_criteria>
  <done>Comando de <verify> verde e todos os itens de <acceptance_criteria> conferidos — inclusive as conferencias por reconsulta ao banco.</done>
</task>

<task type="auto">
  <name>Task 3: Testes de gate da permission, da isenção Polos e o restante da lista</name>
  <files>tests/Feature/Phase131/ContratoAdminPermissaoTest.php, tests/Feature/Phase131/ContratoAdminListaTest.php, tests/Feature/Phase131/ContratoAdminListaExcluiPolosTest.php</files>
  <read_first>
    - `tests/Feature/Phase130/LiberacaoManualTest.php` — arquivo inteiro: helpers `admin()`/`naoAdmin()`, `assertForbidden()`, `assertInertia(fn ($page) => $page->component(...)->has(...)->where(...))`
    - `tests/Feature/Phase75ShopeeEmpresasTest.php` linhas 517-546 — os 3 cenários de gate por permission (403 sem a key, 200 para admin, 200 para quem recebeu via setor)
    - `app/Models/Setor.php` linha ~65 (`permissoes(): HasMany` sobre `SetorPermissao`, coluna `permission_key`) e `app/Models/User.php` linhas 190-230 (`effectivePermissions()` lendo `SetorPermissao` pelos setores do usuário)
    - `database/factories/ContratoAssinaturaFactory.php` — `servico_id` obrigatório; states `emAndamento()` e `assinado()`
  </read_first>
  <action>
    Criar os DOIS arquivos novos em `tests/Feature/Phase131/` e COMPLETAR o terceiro (que já nasceu
    na Task 1):

    **`ContratoAdminPermissaoTest`** (UI-05):
    - `role:admin` recebe 200 em `GET route('admin.contratos.index')` — o short-circuit de
      `hasPermission()` (D-09) garante isso sem nenhum dado gravado.
    - Usuário `consultor` sem a key recebe 403.
    - Usuário NÃO-admin que pertence a um setor com `SetorPermissao` gravada para
      `'admin.contratos'` recebe 200. Este é o cenário que prova que a UI-05 não está inoperante:
      se alguém colar a rota dentro do grupo `role:admin`, este teste fica vermelho.
    - **Asserção de fonte/comportamento sobre o middleware**: obter a rota nomeada
      `admin.contratos.index` de `Route::getRoutes()` e assertar que
      `gatherMiddleware()`/`middleware()` contém `permission:admin.contratos` e NÃO contém
      `role:admin`. Preferir esta asserção a um `grep` do arquivo — ela pega também o caso de a
      rota ser envolvida por um grupo externo.

    **`ContratoAdminListaTest`** (UI-01/D-04) — **já existe** (Task 1: 200 + componente, `resumo`
    com 7 chaves, `sem_contrato_count` fora dele). Completar o MESMO arquivo, sem reescrever os
    casos verdes:
    - As contagens do `resumo` batem com o que foi criado (criar pelo menos 3 contratos em estados
      diferentes).
    - As contagens do `resumo` NÃO mudam quando o filtro `?situacao=` é aplicado (contagens
      absolutas, como o `pendencia_counts` do Comercial).
    - `?situacao=aguardando_assinaturas` devolve apenas as linhas daquele estado.
    - `?situacao=valor_invalido` é ignorado (whitelist) e devolve a lista completa.
    - `?q=` filtra por nome da empresa.
    - A ordenação padrão traz primeiro a linha com maior `dias_parado`.
    - Nenhuma linha da prop carrega nome, e-mail ou CPF de signatário (asserir a AUSÊNCIA dessas
      chaves no array achatado).

    **`ContratoAdminListaExcluiPolosTest`** (UI-01/D9):
    - Empresa cujo único `ContratoServico` ativo aponta para um `Servico` com
      `exige_contrato = false` NÃO aparece em nenhuma linha da lista, em nenhum estado.
    - Empresa com um serviço isento E um que exige contrato aparece — com a linha do serviço que
      exige, e sem linha para o isento (regra do UI-SPEC: nunca omitir a empresa inteira).

    Usar sempre as constantes do model, nunca strings soltas. Conferir por RECONSULTA (props do
    Inertia + banco), nunca por mensagem de tela.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter="ContratoAdminPermissaoTest|ContratoAdminListaTest|ContratoAdminListaExcluiPolosTest"</automated>
  </verify>
  <acceptance_criteria>
    - Os três filtros saem 0, somando no mínimo 12 testes verdes (incluindo os 3 da Task 1) — `No tests found` em qualquer um deles é FALHA
    - `ContratoAdminPermissaoTest` fica VERMELHO se a rota for movida para dentro de um grupo `role:admin` (validar essa inversão manualmente uma vez e desfazer)
    - `C:\xampp\php\php.exe artisan test --filter="Phase126|Phase129|Phase130|Phase131"` sai 0
  </acceptance_criteria>
  <done>Comando de <verify> verde e todos os itens de <acceptance_criteria> conferidos — inclusive as conferencias por reconsulta ao banco.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser não autorizado → rota administrativa | rota nova que expõe o estado de contrato de todas as empresas |
| query string do usuário → query SQL | `situacao` e `q` chegam do request |
| backend → browser (props Inertia) | linhas de contrato de toda a base |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-03-01 | Elevation of Privilege | grupo de rotas `admin/contratos` | mitigate | `permission:admin.contratos` via `EnsurePermission`; teste dedicado com os 3 cenários (403 sem key, 200 admin, 200 via `SetorPermissao`) + asserção sobre o middleware da rota nomeada |
| T-131-03-02 | Elevation of Privilege | rota colada por engano no grupo `role:admin` | mitigate | Bloco de rota separado, com comentário explicando; `ContratoAdminPermissaoTest` fica vermelho se alguém mover |
| T-131-03-03 | Tampering | parâmetro `situacao` | mitigate | Whitelist em PHP contra `ContratoAssinatura::STATUS_TODOS` + `'aguardando_administrativo'`; valor fora da lista vira `null`, nunca vai para a query |
| T-131-03-04 | Tampering / Injection | parâmetro `q` | mitigate | Usado só em `where(... 'like', ...)` via query builder com binding — nunca concatenado em SQL cru |
| T-131-03-05 | Information Disclosure | props Inertia | mitigate | Array achatado com chaves nomeadas; nenhum model completo, nenhum dado de signatário (nome/e-mail/CPF), nenhum id de envelope, nenhum `erro_mensagem` cru |
| T-131-03-06 | Information Disclosure | item de menu | mitigate | Item gateado por `permission: 'admin.contratos'`; a trava real é o middleware — esconder o menu é UX, não controle |
| T-131-03-07 | Denial of Service | `index()` sobre toda a base | mitigate | Query em lote (uma para contratos, uma para empresas) + paginação manual de 50; nenhum cálculo por linha que abra query |
| T-131-03-SC | Tampering | npm/composer installs | mitigate | Nenhum pacote novo; qualquer install descoberto exige checkpoint humano antes |
</threat_model>

<verification>
1. `C:\xampp\php\php.exe artisan test --filter=Phase131` sai 0
2. Regressão cruzada: `C:\xampp\php\php.exe artisan test --filter="Phase126|Phase129|Phase130|Phase131"` sai 0
3. ⛔ NUNCA rodar a suíte sem filtro
4. `C:\xampp\php\php.exe artisan route:list --name=admin.contratos` mostra a rota com `permission:admin.contratos` e sem `role:admin`
5. `npm run build` verde e `Pages/Admin/Contratos.jsx` presente em `public/build/manifest.json`
6. Conferência por RECONSULTA AO BANCO: as 7 contagens do resumo batem com `ContratoAssinatura::where('status', ...)->count()` reconsultado, não com o que a tela mostrou
</verification>

<success_criteria>
- `GET /administrativo/contratos` devolve 200 para admin e para quem recebeu `admin.contratos` num setor, e 403 para os demais
- O item "Contratos" aparece no menu Administrativo só para quem tem a permission
- A lista filtra por situação, busca por empresa e ordena por tempo parado decrescente
- O resumo tem exatamente 7 contagens; `sem_contrato_count` fica fora dele
- Empresa só-isenta (Polos) nunca aparece
- `--filter="ContratoAdminPermissaoTest|ContratoAdminListaTest|ContratoAdminListaExcluiPolosTest"` verde
</success_criteria>

<output>
Criar `.planning/phases/131-tela-administrativa-completar-cadastro-contratos-badge-comer/131-03-SUMMARY.md` ao terminar
</output>
