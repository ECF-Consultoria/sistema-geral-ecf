---
phase: 63-metas-onboarding-obrigat-rio-tratamento-de-legacy-activity-l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_04_26_152219_create_goals_table.php
  - app/Models/Goal.php
  - app/Http/Controllers/ComercialController.php
  - tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php
autonomous: true
requirements: [META-02]
tags: [meta-02, onboarding, backend, comercial, validation]

must_haves:
  truths:
    - "SQLite (test suite) aceita metric='revenue' na tabela goals — CHECK constraint expandido para incluir os 12 metrics correntes"
    - "Goal::logOnly cobre 7 campos (company_id, metric, target_value, value_type, period_type, description, active) — mudança em QUALQUER um gera activity_log entry (META-05 completo)"
    - "Uma empresa nova cadastrada via Comercial nao pode ser criada sem meta_inicial_mensal"
    - "Payload sem meta_inicial_mensal retorna 422 com erro em pt-BR no campo"
    - "Payload com meta_inicial_mensal valido (>0) cria Company + Goal ativa em UMA unica transacao atomica"
    - "Rollback funcional: falha na criacao da Goal desfaz a Company (transaction integrity)"
    - "A Goal criada tem metric='revenue', target_value=meta_inicial_mensal, value_type='currency', period_type='monthly', active=true, description='Meta inicial de onboarding'"
    - "activity_log recebe entrada 'Meta criada' com causer_id = user Comercial que fez o cadastro"
  artifacts:
    - path: "app/Http/Controllers/ComercialController.php"
      provides: "Validacao meta_inicial_mensal + criacao Goal dentro da DB::transaction"
      contains: "meta_inicial_mensal"
    - path: "tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php"
      provides: "Cobertura de META-02: sem meta 422, meta invalida 422, meta valida cria Company+Goal, rollback"
      min_lines: 120
  key_links:
    - from: "ComercialController::store"
      to: "Goal::create"
      via: "DB::transaction dentro do metodo store, apos criacao da Company"
      pattern: "Goal::create\\("
    - from: "Goal::create"
      to: "activity_log"
      via: "trait LogsActivity + booted::created hook ja existente no model"
      pattern: "logOnly.*target_value"
---

<objective>
Alterar `ComercialController::store` para exigir `meta_inicial_mensal` como campo obrigatorio no payload de cadastro de nova empresa. Dentro da DB::transaction ja existente, apos criar a Company, criar uma Goal ativa associada com metric='revenue', target_value=$meta_inicial_mensal, value_type='currency', period_type='monthly', description='Meta inicial de onboarding'. Backend end-to-end: sem meta -> 422; com meta valida -> Company + Goal criadas atomicamente.

Purpose: Fecha SC #1 da Phase 63 (META-02) — resolve o gap identificado no seed 270629. Novas empresas entram no sistema com meta ja definida, sem depender de acao posterior do estrategista/analista.

Output: ComercialController modificado + Feature test cobrindo 5 cenarios de validacao/criacao/rollback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/seeds/270629-modificar-entrada-empresa-meta-onboarding.md
@.planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-01-SUMMARY.md
@CLAUDE.md
@app/Http/Controllers/ComercialController.php
@app/Models/Goal.php
@app/Models/Company.php

<interfaces>
<!-- Contratos ja disponiveis no codebase — nao explorar mais. -->

Goal model (app/Models/Goal.php):
- $fillable = ['company_id', 'metric', 'target_value', 'value_type', 'period_type', 'active', 'description']
- LogsActivity trait ATIVO com logOnly(['company_id','metric','target_value','active']) e setDescriptionForEvent (created='Meta criada')
- static::booted() dispara MetaAtribuidaNotification aos consultores/estrategistas — early-return silencioso se destinatarios vazio (novo cadastro Comercial NAO tem consultor/estrategista ainda, entao NENHUMA notificacao dispara aqui)
- metric enum aceita: revenue, tacos, nps, absenteeism, contribution_margin, revenue_growth, products_without_cost, ppa_completion, avg_ticket, net_billing, acos, margin

Company model (app/Models/Company.php):
- Relacao `goals(): HasMany(Goal::class)` na linha 185-187

ComercialController::store (app/Http/Controllers/ComercialController.php linhas 499-666):
- Auth: abort_unless via permission 'comercial.cadastrar_empresa' OR admin
- Validacao ja aceita: nome (required), cnpj, notes, email_cliente, telefone, nicho, dor, vende_ml, faturamento_mensal, marketplaces_extras, email_colaborador, servicos[] (required)
- DB::transaction ja cria: Company + N ContratoServico + (opcional) MlbEmpresa + MlbImplementacao
- Activity log 'comercial' disparado FORA da transaction (linhas 636-642) — padrao a seguir

Route ja existente: POST /comercial/empresas -> comercial.empresas.store (routes/web.php:198)
</interfaces>

<decision_context>
Escopo do REQ META-02 fala em "empresas Mercado Livre". A ECF Consultoria e uma consultoria 100% Mercado Livre — todo cliente cadastrado via Comercial e implicitamente ML. Nao existe select de marketplace na tela de cadastro. Por isso, meta_inicial_mensal e OBRIGATORIA sempre (nao condicional a nenhuma flag). Documentar esta decisao no proprio codigo via comentario pt-BR ligado a REQ-META-02.

metric='revenue' e a metrica default apropriada (Faturamento — coluna 'revenue' no adman_metrics e ml sync). value_type='currency' e coerente com faturamento em R$. period_type='monthly' e coerente com "meta inicial mensal".

Ordenacao dentro da DB::transaction:
1. Company::create (ja existe)
2. Goal::create (NOVO — antes dos ContratoServico ou depois, tanto faz porque nao ha dependencia entre eles; sugerido depois dos contratos para nao poluir bloco logico existente)
3. ContratoServico::create loop (ja existe)
4. MlbEmpresa/MlbImplementacao (ja existe)

Seed 270629 anti-pattern: NAO criar meta com valor sugerido/default arbitrario. Aqui NAO fazemos default — usuario preenche explicitamente no form. OK.

Migração automática de empresas legacy sem meta (com sugestão baseada em revenue médio 3m) fica **OUT-OF-SCOPE** desta phase — tracked como META-FUTURE-02 em REQUIREMENTS.md linha 70. Empresas legadas serão tratadas com flag "Meta não definida" + CTA em Plan 63-03; sem default automático.

**Foundation fixes (Task 0):** Antes de tocar em ComercialController, corrigir 2 débitos herdados de v13.0 que quebrariam META-02 e META-05:
1. **SQLite CHECK constraint em `goals.metric`** — migração inicial `2026_04_26_152219` só aceita 7 metrics; a migração `2026_04_28_000001` expande para 12 mas pula SQLite (linha 19: `if driver !== sqlite`). Consequência: `Goal::create(['metric' => 'revenue'])` falha CHECK em test suite. Fix: EDITAR a migração inicial `2026_04_26_152219_create_goals_table.php` incluindo os 12 metrics correntes na declaração ENUM. Zero impacto em prod MySQL (migração já rodada); SQLite (tests) fresh-migrate pega a lista expandida.
2. **Goal::logOnly incompleto (META-05 gap)** — hoje trackeia só `['company_id', 'metric', 'target_value', 'active']`. Alterações em `description`, `period_type`, `value_type` NÃO geram activity_log (dontSubmitEmptyLogs). Fix: expandir para `['company_id', 'metric', 'target_value', 'value_type', 'period_type', 'description', 'active']` — 2 linhas de código.
</decision_context>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Foundation fixes — expandir ENUM metric em SQLite + Goal::logOnly completo (META-05)</name>
  <files>
    - database/migrations/2026_04_26_152219_create_goals_table.php
    - app/Models/Goal.php
  </files>
  <read_first>
    - database/migrations/2026_04_26_152219_create_goals_table.php (LEITURA COMPLETA — 37 linhas; foco no ENUM declarado linhas 17-20 com só 7 metrics)
    - database/migrations/2026_04_28_000001_add_value_type_and_new_metrics_to_goals_table.php (verificar que ela EXPANDE ENUM para 12 metrics em MySQL mas skipa SQLite via `if driver !== sqlite` linha 19)
    - app/Models/Goal.php (LEITURA COMPLETA; foco em `logOnly` linha 56 — hoje só 4 campos)
    - .planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-01-SUMMARY.md §Auto-ajustes 1 (contexto: Phase 62 tests contornaram usando metric='tacos' — evitar reproduzir workaround)
  </read_first>
  <action>
    **Passo 0.1 — Expandir ENUM na migração inicial (2026_04_26_152219_create_goals_table.php):**
    Substituir a declaração `$table->enum('metric', [...])` linhas 17-20 pela lista COMPLETA dos 12 metrics correntes (batendo com o que a migração 2026_04_28_000001 expandiu em MySQL):
    ```php
    $table->enum('metric', [
        'tacos', 'nps', 'absenteeism', 'contribution_margin',
        'revenue_growth', 'products_without_cost', 'ppa_completion',
        'revenue', 'avg_ticket', 'margin', 'net_billing', 'acos'
    ]);
    ```
    Comentário pt-BR acima explicando: `// Metrics expandidos aqui para bater com a migracao 2026_04_28 (que expande em MySQL mas pula SQLite). Ver Phase 63 §Foundation fixes.`.
    **Impacto:** ZERO em prod MySQL (migração já rodada — arquivo não roda de novo). SQLite (test suite via RefreshDatabase) passa a aceitar `metric='revenue'` desde o migrate inicial.

    **Passo 0.2 — Expandir Goal::logOnly (META-05 completo):**
    Em `app/Models/Goal.php` linha ~56, substituir:
    ```php
    ->logOnly(['company_id', 'metric', 'target_value', 'active'])
    ```
    Por:
    ```php
    ->logOnly(['company_id', 'metric', 'target_value', 'value_type', 'period_type', 'description', 'active'])
    ```
    Comentário pt-BR acima explicando: `// META-05 (Phase 63): trackear todos os campos editáveis da meta para audit trail completo.`.
    **Impacto:** próximas alterações de description/period_type/value_type passam a gerar row em activity_log. Alterações antigas não são retroativas (padrão Spatie).

    **Passo 0.3 — Validar:**
    - `php artisan migrate:fresh --env=testing 2>&1 | tail -10` deve rodar SEM erro CHECK constraint
    - `grep -c "revenue" database/migrations/2026_04_26_152219_create_goals_table.php` == 1
    - `grep -c "period_type" app/Models/Goal.php` >= 2 (logOnly + eventual outro uso)
    - `grep -c "description" app/Models/Goal.php` >= 2 (logOnly + fillable)

    **Passo 0.4 — Regression check:**
    - `php artisan test tests/Feature/Phase62/` → 19/19 ainda verdes (Phase 62 Goals tests continuam OK; logOnly expandido é retrocompatível)
    - `php artisan test tests/Feature/Phase60 tests/Feature/Phase61` → 77 ainda verdes

    Commit: `fix(63-01): expand goals.metric ENUM in SQLite + expand Goal::logOnly (META-05 audit trail completo)`
  </action>
  <verify>
    <automated>php artisan migrate:fresh --env=testing 2>&amp;1 | tail -5 &amp;&amp; grep -c "'revenue'" database/migrations/2026_04_26_152219_create_goals_table.php &amp;&amp; grep -c "period_type" app/Models/Goal.php &amp;&amp; php artisan test tests/Feature/Phase62/ 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Migração inicial `2026_04_26_152219_create_goals_table.php` tem 12 metrics no ENUM (grep confirma `'revenue'`, `'avg_ticket'`, `'margin'`, `'net_billing'`, `'acos'`)
    - `Goal.php` `logOnly` array tem exatamente 7 campos: `company_id, metric, target_value, value_type, period_type, description, active`
    - `php artisan migrate:fresh --env=testing` roda sem erro CHECK constraint
    - `php artisan test tests/Feature/Phase62/` continua 19/19 verdes (zero regressão)
    - Zero mudança em qualquer outro arquivo (migração 2026_04_28 intocada; ComercialController intocado; testes de Phase 62 intactos)
  </acceptance_criteria>
  <done>Foundation preparada: SQLite aceita `metric='revenue'` desde o migrate inicial; Goal::logOnly cobre os 7 campos editáveis. Phase 62 tests inalterados. Prod MySQL sem impacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Test RED — cenarios de validacao + criacao atomica Company+Goal</name>
  <files>tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php</files>
  <read_first>
    - tests/Feature/Phase62/CompanyShowGoalsPayloadTest.php (padrao de setup Company + user + permissao Comercial)
    - tests/Feature/Phase62/GoalUpdateAuthTest.php (padrao actingAs + assertDatabaseHas para Goal)
    - app/Http/Controllers/ComercialController.php linhas 499-666 (fluxo store atual + payload validado + transaction)
    - app/Models/Goal.php (LogsActivity + fillable + booted hook)
  </read_first>
  <behavior>
    - Test 1 (RED): POST /comercial/empresas SEM `meta_inicial_mensal` -> 422 com erro pt-BR na chave `meta_inicial_mensal`
    - Test 2 (RED): POST com `meta_inicial_mensal=0` -> 422 (min:0.01)
    - Test 3 (RED): POST com `meta_inicial_mensal=-100` -> 422
    - Test 4 (RED): POST com `meta_inicial_mensal="abc"` -> 422 (numeric)
    - Test 5 (RED): POST com payload valido cria Company AND Goal atomicamente — assertDatabaseHas companies + assertDatabaseHas goals com metric=revenue, target_value=meta_inicial_mensal, value_type=currency, period_type=monthly, active=1
    - Test 6 (RED): activity_log recebe entrada com subject_type=Goal, causer_id=user_id, description='Meta criada' apos criacao
    - Test 7 (RED): duplicata de nome ainda funciona (guard existente) — meta_inicial_mensal valido + nome duplicado -> 422 no `nome`, NENHUMA Goal criada (rollback da transaction)
  </behavior>
  <action>
    Criar `tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php` com namespace `Tests\Feature\Phase63`, use `RefreshDatabase`, extends `TestCase`. Setup: criar User com role='admin' (short-circuit permission) via `User::factory()->create(['role' => 'admin', 'active' => true])`, `actingAs($user)`. Payload base valido: `['nome' => 'Empresa Teste Onboarding', 'servicos' => [['servico_id' => $servico->id, 'valor_contratado' => 1000.0]], 'meta_inicial_mensal' => 15000.50]`. Criar Servico via `Servico::create([...])` no setUp (ativo=true). Cada test faz `$this->post(route('comercial.empresas.store'), $payload)` e assert com `assertSessionHasErrors` ou `assertRedirect` + `assertDatabaseHas('companies', ...)` + `assertDatabaseHas('goals', ['metric' => 'revenue', 'target_value' => 15000.50, ...])`. Para Test 6, apos POST, query `\Spatie\Activitylog\Models\Activity::where('subject_type', Goal::class)->latest()->first()` — assert `description === 'Meta criada'` e `causer_id === $user->id`. Para Test 7, criar Company anterior com mesmo nome, entao POST + assert `assertDatabaseCount('goals', 0)` para garantir rollback. Rodar `php artisan test tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php` e confirmar TODAS as 7 falham antes de mexer no controller. Nao usar metric='revenue' se rodando em SQLite: usar metric='tacos' se assertion do enum falhar (workaround documentado em 62-01-SUMMARY §Auto-ajustes 1). Preferencia: metric='revenue' para producao real. Commit: `test(63-01): add failing tests for onboarding com meta obrigatoria`.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php 2>&amp;1 | tail -20 &amp;&amp; echo "RED confirmed"</automated>
    <human-check>7 tests falham com mensagens claras: 4 sobre validacao ausente, 2 sobre Goal nao criada, 1 sobre rollback nao ocorrido</human-check>
  </verify>
  <done>Arquivo `tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php` existe com 7 tests todos RED (comando `php artisan test` mostra 7 failures ou errors); commit `test(63-01): ...` criado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Green — implementar validacao meta_inicial_mensal + criacao Goal na DB::transaction</name>
  <files>app/Http/Controllers/ComercialController.php</files>
  <read_first>
    - app/Http/Controllers/ComercialController.php linhas 499-666 (metodo store completo)
    - app/Models/Goal.php (fillable + booted hook — validar que criacao dispara LogsActivity created event)
  </read_first>
  <action>
    Editar `ComercialController::store` (linhas 499-666):

    (a) No `$request->validate([...])` array (linhas 507-538), ADICIONAR apos `'email_colaborador' => 'nullable|email|max:255',` (linha 530) e ANTES do bloco `'servicos'` (linha 531), a nova regra:
    ```
    // Phase 63 (META-02, seed 270629) — meta inicial mensal obrigatoria no
    // cadastro. Toda empresa entra com meta definida; sem default arbitrario.
    'meta_inicial_mensal'         => 'required|numeric|min:0.01|max:99999999.99',
    ```

    (b) Dentro do `DB::transaction(function () use (...))` (linha 554), na captura do `use` da closure, adicionar `$validated` (ja esta), `&$company`, `&$servicosCriados` (ja estao). Nenhuma mudanca na assinatura da closure.

    (c) Apos o loop `foreach ($validated['servicos'] as $item) { ContratoServico::create(...) }` (linhas 581-599) e ANTES do bloco de roteamento MlbEmpresa/MlbImplementacao (linha 601), INSERIR:
    ```
    // Phase 63 (META-02) — meta inicial mensal como Goal ativa vinculada a
    // Company recem-criada. metric='revenue' + value_type='currency' + period_type='monthly'
    // sao defaults sensatos para "meta mensal de faturamento". Notificacao
    // MetaAtribuidaNotification (Goal::booted::created) faz early-return
    // silencioso pois a empresa recem-criada ainda nao tem consultor/estrategista
    // atribuido — nao ha notification storm no onboarding.
    Goal::create([
        'company_id'   => $company->id,
        'metric'       => 'revenue',
        'target_value' => (float) $validated['meta_inicial_mensal'],
        'value_type'   => 'currency',
        'period_type'  => 'monthly',
        'active'       => true,
        'description'  => 'Meta inicial de onboarding',
    ]);
    ```

    (d) Adicionar `use App\Models\Goal;` no bloco de `use` do topo do arquivo (apos `use App\Models\Company;` — manter ordem alfabetica).

    Rodar `php artisan test tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php` — TODOS 7 tests devem passar (GREEN). Rodar tambem baseline anti-regressao: `php artisan test tests/Feature/Phase37/` (fluxo Comercial existente) e `php artisan test tests/Feature/Phase62/` (Goal-related). Zero regressao. Grep gates: `grep -c "meta_inicial_mensal" app/Http/Controllers/ComercialController.php` deve retornar >=2 (regra validate + comentario+key na Goal::create); `grep -c "Goal::create" app/Http/Controllers/ComercialController.php` deve retornar 1. Commit: `feat(63-01): meta inicial mensal obrigatoria no onboarding Comercial (META-02)`.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php 2>&amp;1 | tail -5 &amp;&amp; grep -c "meta_inicial_mensal" app/Http/Controllers/ComercialController.php &amp;&amp; grep -c "Goal::create" app/Http/Controllers/ComercialController.php</automated>
    <human-check>Suite Phase63 7/7 verdes; Phase37 + Phase62 sem novas falhas</human-check>
  </verify>
  <done>7/7 tests Phase63/CompanyOnboardingWithGoalTest verdes; grep gates passam (meta_inicial_mensal >=2, Goal::create ==1); commit `feat(63-01): ...` criado; rollback teste passa (assertDatabaseCount goals == 0 quando nome duplicado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descricao |
|----------|-----------|
| Comercial user -> HTTP POST /comercial/empresas | User autenticado com permission 'comercial.cadastrar_empresa' OR admin cruza aqui; input `meta_inicial_mensal` e untrusted |
| ComercialController::store -> DB via Goal::create | Valor pos-validacao entra no banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01-01 | Tampering | `meta_inicial_mensal` input | mitigate | Validacao Laravel `required\|numeric\|min:0.01\|max:99999999.99` bloqueia valores negativos, zero, strings, e overflow decimal |
| T-63-01-02 | Denial of Service | Payload meta = 10^10 (overflow decimal(15,2)) | mitigate | Regra `max:99999999.99` (8 digitos inteiros + 2 decimais) alinhada com schema `decimal(15,2)` do goals table |
| T-63-01-03 | Elevation of Privilege | User sem permissao Comercial cria empresa | accept | Guard ja existente em linha 501 (`abort_unless permission OR admin`) preserva rota; Phase 63 nao muda nada aqui |
| T-63-01-04 | Repudiation | Cadastro sem trilha de auditoria | mitigate | Goal::create dispara LogsActivity (`Meta criada` com causer_id) + activity_log('comercial') ja existente na linha 639 |
</threat_model>

<verification>
- `php artisan test tests/Feature/Phase63/CompanyOnboardingWithGoalTest.php` -> 7/7 verdes
- `php artisan test tests/Feature/Phase37/` -> baseline sem novas falhas (fluxo Comercial existente)
- `php artisan test tests/Feature/Phase62/` -> baseline sem novas falhas (Goal-related)
- `grep -c "meta_inicial_mensal" app/Http/Controllers/ComercialController.php` -> >= 2
- `grep -c "Goal::create" app/Http/Controllers/ComercialController.php` -> == 1
- `git diff app/Http/Controllers/ComercialController.php` -> apenas +12/-0 linhas aproximadas (validate + Goal::create + comentarios + use)
</verification>

<success_criteria>
- POST /comercial/empresas sem `meta_inicial_mensal` retorna 422 com erro pt-BR no campo
- POST com `meta_inicial_mensal` valido cria Company e Goal ativa em uma unica DB::transaction (rollback funcional se guard duplicata dispara)
- activity_log recebe entrada `Meta criada` com causer_id = user Comercial autenticado
- Zero regressao nas suites Phase37 (Comercial existente) e Phase62 (Goals)
</success_criteria>

<output>
Criar `.planning/phases/63-metas-onboarding-obrigat-rio-tratamento-de-legacy-activity-l/63-01-SUMMARY.md` ao completar
</output>
