---
phase: 63-metas-onboarding-obrigat-rio-tratamento-de-legacy-activity-l
plan: 02
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - resources/js/Pages/Comercial/NovaEmpresa.jsx
autonomous: true
requirements: [META-02]
tags: [meta-02, onboarding, frontend, ux, react]

must_haves:
  truths:
    - "Passo 2 do wizard exibe campo 'Meta inicial mensal (R$)' marcado como obrigatorio (asterisco amarelo)"
    - "Passo 2 nao pode ser submetido sem preencher meta_inicial_mensal (botao desabilitado)"
    - "Submit envia `meta_inicial_mensal` no payload POST para comercial.empresas.store"
    - "Erro server-side 422 na chave `meta_inicial_mensal` aparece embaixo do campo em pt-BR"
    - "Ao cadastrar com sucesso, form reseta incluindo meta_inicial_mensal='' e volta para passo 1"
  artifacts:
    - path: "resources/js/Pages/Comercial/NovaEmpresa.jsx"
      provides: "Campo obrigatorio meta_inicial_mensal no passo 2 do wizard; validacao client-side + client sensitive submit"
      contains: "meta_inicial_mensal"
      min_lines: 730
  key_links:
    - from: "NovaEmpresa.jsx form"
      to: "route('comercial.empresas.store')"
      via: "useForm.post com data.meta_inicial_mensal"
      pattern: "meta_inicial_mensal"
    - from: "NovaEmpresa.jsx button submit passo 2"
      to: "passo2Valido"
      via: "disabled inclui checagem de meta_inicial_mensal preenchida"
      pattern: "passo2Valido"
---

<objective>
Adicionar campo `meta_inicial_mensal` (input numeric, R$ mensal) no passo 2 do wizard de cadastro em `Comercial/NovaEmpresa.jsx`, marcado como obrigatorio (asterisco amarelo), com validacao client-side (bloqueia submit se vazio) e feedback para erro server-side. Front consome o endpoint alterado no Plan 63-01.

Purpose: Fecha o loop de UI da META-02 — o operador Comercial VE o campo obrigatorio ao cadastrar, evitando 422 confuso e garantindo que meta chegue ao backend.

Output: NovaEmpresa.jsx modificado com campo novo + rodar `npm run build` (obrigatorio por convencao do projeto — pos-edit JSX).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/63-metas-onboarding-obrigat-rio-tratamento-de-legacy-activity-l/63-01-PLAN.md
@resources/js/Pages/Comercial/NovaEmpresa.jsx
@CLAUDE.md

<interfaces>
<!-- Componentes/helpers/API ja disponiveis. Nao explorar. -->

Wizard NovaEmpresa.jsx (linhas 79-589):
- useForm hook (linha 85) com `data`, `setData`, `post`, `processing`, `errors`, `reset`, `transform`
- Estado inicial da form (linhas 85-107) contem: nome, cnpj, notes, email_cliente, telefone, servicos[], nicho, dor, vende_ml, faturamento_mensal, marketplaces_extras, email_colaborador, polo, nome_contato, gmail_colaborador, grupo_whatsapp
- `passo1Valido = !!data.nome.trim()` (linha 159)
- `passo2Valido = data.servicos.length > 0 && (!poloSelecionado || (!!data.nome_contato && !!data.polo))` (linha 160)
- Passo 2 tem um bloco "Informacoes do close (opcional)" (linhas 419-531) contendo o campo Nicho, Dor, Vende no ML, Faturamento mensal, Marketplaces extras, Email colaborador
- helper `<Field label="..." required error={errors.X} icon={Icon}>` (linhas 606-624) — pattern para renderizar input com label/erro
- helper `inputCls(erro, comIcone)` (linhas 595-602) — classes tailwind consistentes
- Ao submit com sucesso: `onSuccess: () => { reset(); setStep(1); }` (linhas 181-184)
- Ao submit com erro em campo do passo 1: volta para passo 1 (linhas 185-190) — STEP1_FIELDS array

Backend endpoint (Plan 63-01): POST /comercial/empresas com validacao `meta_inicial_mensal => required|numeric|min:0.01|max:99999999.99`.
</interfaces>

<decision_context>
Onde colocar o campo:
- OPCAO A: Passo 2 (Servicos + onboarding) fora do bloco "Informacoes do close (opcional)" — em bloco proprio destacado. VENCEDOR — meta e obrigatoria, nao pode ficar no bloco visualmente rotulado "opcional".
- OPCAO B: Passo 1 (Dados da empresa). PERDE — passo 1 e sobre identificacao (nome/cnpj/contato); meta e sobre a operacao/objetivo.

Onde exatamente no passo 2:
- Apos catalogo de servicos e antes do bloco Polos (se aplicavel) e antes do bloco "Informacoes do close". Meta = compromisso comercial mensal explicito, precisa destaque.

Icone: `Target` de lucide-react (ja disponivel via `import { Target }` — adicionar ao import se ausente).

Validacao passo 2: adicionar `!!data.meta_inicial_mensal && Number(data.meta_inicial_mensal) > 0` ao `passo2Valido`. Nao usar apenas `!!` pois `0` e string vazia sao ambos falsy — usar checagem numerica explicita.

Erro server-side: adicionar `meta_inicial_mensal` ao STEP1_FIELDS? NAO — o campo esta no passo 2, entao erro NAO deve voltar para passo 1. Mantem STEP1_FIELDS intocado; erro do meta_inicial_mensal aparece no proprio passo 2.
</decision_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campo Meta inicial mensal (R$) obrigatorio no passo 2 do wizard</name>
  <files>resources/js/Pages/Comercial/NovaEmpresa.jsx</files>
  <read_first>
    - resources/js/Pages/Comercial/NovaEmpresa.jsx completo (arquivo tem 727 linhas — ler tudo em uma passada)
  </read_first>
  <action>
    Editar `resources/js/Pages/Comercial/NovaEmpresa.jsx` com 4 mudancas cirurgicas:

    (a) Import (linha 34-37): ADICIONAR `Target` na lista de icones importados de `lucide-react`. Verificar se ja esta importado antes de duplicar.

    (b) useForm data (linhas 85-107): ADICIONAR `meta_inicial_mensal: '',` apos `telefone: '',` (linha 90), respeitando ordem alfabetica das linhas do bloco. Adicionar comentario pt-BR uma linha acima: `// Phase 63 (META-02, seed 270629) — meta obrigatoria de faturamento mensal no onboarding.`.

    (c) Validacao passo 2 (linha 160): ALTERAR de `const passo2Valido = data.servicos.length > 0 && (!poloSelecionado || (!!data.nome_contato && !!data.polo));` para:
    ```
    const passo2Valido =
        data.servicos.length > 0
        &amp;&amp; Number(data.meta_inicial_mensal) > 0
        &amp;&amp; (!poloSelecionado || (!!data.nome_contato &amp;&amp; !!data.polo));
    ```

    (d) UI do passo 2: INSERIR novo bloco Field APOS o fechamento do bloco `<div>` do catalogo de servicos (linha 351, apos `{errors.servicos && ... }` fechamento) e ANTES do `{poloSelecionado && ...}` (linha 354). Bloco em card destacado com border-ecf-yellow para deixar claro que e obrigatorio e sensivel:
    ```
    {/* Phase 63 (META-02, seed 270629) — Meta inicial mensal.
        Obrigatoria: novas empresas entram com meta ja definida, evita
        default arbitrario atras dos panos (memoria seed 270629). */}
    <div className="rounded-xl border border-ecf-yellow/25 bg-ecf-yellow/[0.04] overflow-hidden">
        <div className="flex items-center gap-2 px-4 py-3 border-b border-ecf-yellow/15 bg-ecf-yellow/[0.04]">
            <Target size={14} className="text-ecf-yellow" />
            <p className="text-xs text-ecf-yellow font-semibold tracking-wide">Meta comercial</p>
        </div>
        <div className="p-4">
            <Field
                label="Meta inicial mensal (R$)"
                required
                error={errors.meta_inicial_mensal}
                note="Faturamento mensal alvo acordado no fechamento. Cria a primeira meta ativa da empresa."
            >
                <input
                    type="number"
                    step="0.01"
                    min="0.01"
                    value={data.meta_inicial_mensal}
                    onChange={e => setData('meta_inicial_mensal', e.target.value)}
                    placeholder="Ex: 50000,00"
                    className={inputCls(errors.meta_inicial_mensal, false)}
                />
            </Field>
        </div>
    </div>
    ```

    NAO adicionar `meta_inicial_mensal` ao STEP1_FIELDS (esta no passo 2 — erro server-side deve renderizar em contexto). Manter todos os outros campos e blocos intocados. Nao renomear helper `Field`/`inputCls`/`ServiceCard`/`Stepper`.

    Apos edicao, rodar `npm run build` (obrigatorio pela convencao do projeto — pos-edit JSX). Confirmar 0 erros. Grep gates: `grep -c "meta_inicial_mensal" resources/js/Pages/Comercial/NovaEmpresa.jsx` deve retornar >= 4 (useForm + passo2Valido + errors.meta_inicial_mensal na Field + setData onChange). `grep -n "Target" resources/js/Pages/Comercial/NovaEmpresa.jsx | head -2` deve mostrar import + uso no JSX.

    Commit: `feat(63-02): meta inicial mensal obrigatoria no wizard NovaEmpresa (META-02)`.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -c "meta_inicial_mensal" resources/js/Pages/Comercial/NovaEmpresa.jsx</automated>
    <human-check>Abrir /comercial/empresas/novo em dev, chegar no passo 2 e ver o card amarelo "Meta comercial" com input R$ e asterisco. Botao "Cadastrar Empresa" fica desabilitado se meta vazio ou <=0.</human-check>
  </verify>
  <done>Build Vite sem erros; grep meta_inicial_mensal >=4 no JSX; campo aparece renderizado com asterisco obrigatorio; commit criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descricao |
|----------|-----------|
| Browser user -> Inertia POST | Payload `meta_inicial_mensal` gerado pelo React chega ao backend; sujeito a manipulacao via DevTools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-02-01 | Tampering | Client-side bypass do required do input | mitigate | Validacao FINAL e server-side (Plan 63-01). Client-side (`passo2Valido`) e apenas UX — impede submit vazio no fluxo normal, mas usuario malicioso pode remover disabled via DevTools. Backend rejeita com 422 (T-63-01-01 do 63-01) |
| T-63-02-02 | Information disclosure | Erro server-side revela detalhes de schema | accept | Mensagem default do Laravel para `required\|numeric\|min` e generica ("O campo meta inicial mensal e obrigatorio.") — nao vaza schema |
</threat_model>

<verification>
- `npm run build` -> 0 erros
- `grep -c "meta_inicial_mensal" resources/js/Pages/Comercial/NovaEmpresa.jsx` -> >= 4
- Manual: abrir /comercial/empresas/novo, avancar para passo 2, VER card amarelo "Meta comercial" com input R$; deixar vazio -> botao Cadastrar disabled; preencher 50000 e servico -> submit ok
</verification>

<success_criteria>
- Campo obrigatorio "Meta inicial mensal (R$)" visivel no passo 2 do wizard com destaque visual amarelo
- Botao "Cadastrar Empresa" desabilitado enquanto meta_inicial_mensal <= 0
- Submit passa `meta_inicial_mensal` no payload; erro server-side renderiza embaixo do campo
- Zero regressao no wizard: passo 1 -> passo 2 -> submit continua funcionando para outros campos
</success_criteria>

<output>
Criar `.planning/phases/63-metas-onboarding-obrigat-rio-tratamento-de-legacy-activity-l/63-02-SUMMARY.md` ao completar
</output>
