---
phase: 63-metas-onboarding-obrigat-rio-tratamento-de-legacy-activity-l
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Phase63/GoalActivityLogTest.php
autonomous: true
requirements: [META-05]
tags: [meta-05, activity-log, audit, backend, tests-only]

must_haves:
  truths:
    - "Criacao de Goal (via qualquer caller) gera entrada em activity_log com description='Meta criada' e properties.attributes populado"
    - "Update de Goal (target_value/period_type/description/active) gera entrada com description='Meta atualizada' contendo properties.old e properties.attributes"
    - "Delete/desativacao de Goal (via admin) gera entrada correspondente"
    - "causer_id em cada entrada = user autenticado que executou a operacao"
    - "subject_id em cada entrada = goal.id — isolamento garantido"
  artifacts:
    - path: "tests/Feature/Phase63/GoalActivityLogTest.php"
      provides: "Cobertura de META-05: valida audit trail existente via LogsActivity + booted hooks"
      min_lines: 130
  key_links:
    - from: "Goal::create/update/delete"
      to: "activity_log via Spatie LogsActivity trait"
      via: "getActivitylogOptions com logOnly + setDescriptionForEvent no Goal model"
      pattern: "logOnly.*target_value.*active"
    - from: "Test setUp"
      to: "Sanctum/Session actingAs"
      via: "actingAs($user) garante causer_id populado por Spatie"
      pattern: "actingAs"
---

<objective>
Criar suite de testes Feature `tests/Feature/Phase63/GoalActivityLogTest.php` que VALIDA o comportamento de audit ja fornecido pelo trait `LogsActivity` no `App\Models\Goal` (fechado em Phase 62 Plan 62-01, mas nao coberto por testes explicitos ligados a META-05). Zero mudanca de codigo de producao — apenas tests como safety net.

Purpose: Fecha SC #3 da Phase 63 (META-05). Cada alteracao de meta e registrada em `activity_log` com autor, valor anterior e valor novo — provavelmente ja funciona, mas META-05 exige cobertura explicita. Se tests falharem (=comportamento nao alinhado), abrir plano corretivo em 63-05 (nao pre-planejado — so se necessario).

Output: `tests/Feature/Phase63/GoalActivityLogTest.php` novo com 5-6 tests cobrindo create/update/delete/causer/subject_id.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-metas-apresenta-o-clara-edi-o-r-pida/62-01-SUMMARY.md
@app/Models/Goal.php
@app/Http/Controllers/GoalController.php

<interfaces>
<!-- Ja disponiveis. Nao explorar. -->

Goal model (app/Models/Goal.php):
- `use LogsActivity` (linha 13)
- `getActivitylogOptions()` (linhas 53-65):
  - `logOnly(['company_id', 'metric', 'target_value', 'value_type', 'period_type', 'description', 'active'])` — **7 campos após expansão em Task 0 de 63-01** (foundation fix). Todas as mudanças nesses campos entram em `properties.attributes/old`.
  - `logOnlyDirty()` — so entra o que mudou
  - `dontSubmitEmptyLogs()` — dry-update nao vira noise
  - `setDescriptionForEvent`: created='Meta criada', updated='Meta atualizada', deleted='Meta excluida'
- `booted()` created hook (linhas 25-51) dispara notification (nao afeta activity_log)

Activity log query pattern (do 62-01):
```
\Spatie\Activitylog\Models\Activity::with('causer')
    ->where('subject_type', Goal::class)
    ->where('subject_id', $goal->id)
    ->orderByDesc('created_at')->orderByDesc('id')
    ->limit(10)->get()
```

GoalController::store (linhas 118-149): admin OR estrategista da empresa; cria Goal e retorna redirect
GoalController::update (linhas 151-188): admin OR estrategista; atualiza campos permitidos; bloqueia active=false para nao-admin (`unset($data['active'])`)
GoalController::destroy (linhas 190-194): so admin (grupo `role:admin` em routes/web.php:390); soft-delete via `active = false`

Rota nomeadas envolvidas:
- POST /goals -> goals.store
- PUT /goals/{goal} -> goals.update
- DELETE /goals/{goal} -> goals.destroy (admin-only)
</interfaces>

<decision_context>
Este plan e defensivo: valida META-05 sem tocar codigo de producao. Se algum test falhar, o SUMMARY deve documentar o gap e o proximo plano corretivo.

Cenarios cobertos (5 tests):
1. Create Goal via GoalController::store -> activity_log com description='Meta criada', subject_id=goal.id, causer_id=user.id
2. Update Goal via GoalController::update -> description='Meta atualizada', properties.old + properties.attributes contem target_value antigo e novo
3. Delete Goal via GoalController::destroy (admin) -> description='Meta atualizada' (o destroy do controller faz `update(['active' => false])`, entao dispara updated) OU 'Meta excluida' se model->delete() for chamado — verificar durante o teste
4. causer_id populado corretamente com user autenticado
5. subject_id isola por goal — outra Goal na mesma empresa NAO aparece nas entries filtradas

**Nota SQLite (Task 0 do 63-01 resolveu):** o CHECK constraint de `metric` foi expandido para os 12 metrics correntes em Task 0 de 63-01 (modificação da migração inicial). Podemos usar `metric='revenue'` + `value_type='currency'` normalmente aqui, sem workaround. Fallback: se por algum motivo Task 0 não tiver rodado antes deste plan, cair pra `metric='tacos'` + `value_type='percentage'` (target_value 10.00 → 20.00 → 30.00).

Nao adicionar testes E2E do endpoint /goals/{goal}/history — ja coberto por Phase 62 (GoalHistoryEndpointTest).
</decision_context>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Suite de tests validando activity_log em create/update/delete de Goal</name>
  <files>tests/Feature/Phase63/GoalActivityLogTest.php</files>
  <read_first>
    - tests/Feature/Phase62/GoalUpdateAuthTest.php (padrao setup + actingAs + assertDatabaseHas activity_log)
    - tests/Feature/Phase62/GoalHistoryEndpointTest.php (padrao query Activity model + shape properties)
    - app/Models/Goal.php linhas 53-65 (LogsActivity contract)
    - app/Http/Controllers/GoalController.php linhas 118-194 (endpoints envolvidos)
  </read_first>
  <behavior>
    - Test 1: apos POST /goals com payload valido, activity_log tem row com subject_type=Goal::class, subject_id=goal.id, description='Meta criada', causer_id=user.id, properties.attributes contem target_value
    - Test 2: apos PUT /goals/{goal} alterando target_value, activity_log tem row description='Meta atualizada', properties.old.target_value = valor antigo, properties.attributes.target_value = valor novo
    - Test 3: apos update do campo `active` de true para false (via admin), activity_log tem row description='Meta atualizada' com properties.old.active=true e properties.attributes.active=false
    - Test 4: em qualquer update, causer_id da row = id do user autenticado (fixture $admin)
    - Test 5: criar 2 Goals em empresas diferentes, atualizar ambas, verificar que query filtrada por subject_id=goal1.id retorna APENAS entries relacionadas a goal1 (nao vaza cross-goal)
  </behavior>
  <action>
    Criar `tests/Feature/Phase63/GoalActivityLogTest.php` com namespace `Tests\Feature\Phase63`, `use RefreshDatabase`, extends `Tests\TestCase`. Setup helper que cria: `User` admin + `Company` associada (SEM Servico — Goal não tem relação com Servico, ruído desnecessário). Usar `metric='revenue'` + `value_type='currency'` para os fixtures (Task 0 do 63-01 expandiu CHECK constraint). target_value 10000.00 → 15000.00 → 20000.00 (faturamento mensal realista em R$).

    **Testes adicionais (aproveitando logOnly expandido em Task 0):**
    - Test 5.5: alteração isolada de `description` gera row com `properties.old.description` e `properties.attributes.description`
    - Test 5.6: alteração isolada de `period_type` (monthly → quarterly) gera row
    - Test 5.7: alteração isolada de `value_type` (currency → percentage) gera row — atenção: mudar value_type pode exigir mudar target_value junto para não quebrar validação; alternativamente testar via `$goal->update([...])` direto sem passar pelo controller.

    Cada test:
    - Chamar `\Spatie\Activitylog\Models\Activity::where('subject_type', Goal::class)->where('subject_id', $goal->id)->orderByDesc('id')->get()` apos operacao
    - Assert count esperado
    - Assert `description` matches
    - Assert `properties['attributes']` / `properties['old']` populados corretamente
    - Assert `causer_id === $user->id`

    Test 3 detalhe: primeiro criar Goal via `Goal::create([...active=>true...])` (bypass controller), depois `actingAs($admin)` e `PUT /goals/{goal}` com `active=false` no payload. Verificar que ambas rows (created + updated) existem.

    Test 5 detalhe: criar 2 companies + 2 goals + 1 admin; update goal1; update goal2; query filtrada por subject_id=goal1.id retorna 2 rows (created + updated), NENHUMA delas com subject_id=goal2.id.

    Rodar `php artisan test tests/Feature/Phase63/GoalActivityLogTest.php`. Se TODOS passarem: META-05 confirmada como coberta pelo trait existente. Se algum falhar: documentar gap no SUMMARY (nao tentar consertar aqui — abrir plano corretivo se necessario).

    Commit: `test(63-04): validate activity_log audit trail em Goal create/update/delete (META-05)`.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase63/GoalActivityLogTest.php 2>&amp;1 | tail -10</automated>
    <human-check>Console mostra 5/5 verdes ou lista clara de tests falhando com properties.old/attributes divergindo do esperado</human-check>
  </verify>
  <done>Suite Phase63/GoalActivityLogTest com 5 tests: se todos verdes -> META-05 validada. Se algum falhar -> gap documentado no SUMMARY como TODO para 63-05 corretivo. Commit `test(63-04): ...` criado em qualquer cenario (tests documentam o comportamento esperado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhum novo threat boundary — este plan e tests-only, nao expoe superficie. Reforca defesa em profundidade (T-63-01-04 do Plan 63-01 depende do audit trail funcionar corretamente).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04-01 | Repudiation | Meta alterada sem trilha auditavel | mitigate | Suite de tests Phase63/GoalActivityLogTest garante que qualquer regressao no LogsActivity trait do Goal (ex: alguem remove `logOnly` ou muda `setDescriptionForEvent`) e detectada CI antes de vazar para producao |
</threat_model>

<verification>
- `php artisan test tests/Feature/Phase63/GoalActivityLogTest.php` -> 5/5 verdes idealmente
- Se tests falharem: documentar no SUMMARY exatamente qual assertion falhou e qual o comportamento real
- `grep -c "public function test_" tests/Feature/Phase63/GoalActivityLogTest.php` -> >= 5
</verification>

<success_criteria>
- Suite Phase63/GoalActivityLogTest com pelo menos 5 tests cobrindo create/update/delete/causer/isolation
- Se todos verdes: META-05 confirmada (audit trail funciona ponta a ponta)
- Se algum falhar: gap documentado explicitamente no SUMMARY com proximo passo (nao tentar consertar aqui, fora do escopo)
- Zero mudanca em codigo de producao (`git diff --stat app/ config/ resources/` retorna vazio ao final desta plan)
</success_criteria>

<output>
Criar `.planning/phases/63-metas-onboarding-obrigat-rio-tratamento-de-legacy-activity-l/63-04-SUMMARY.md` ao completar
</output>
