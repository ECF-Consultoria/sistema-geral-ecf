---
phase: 63-metas-onboarding-obrigat-rio-tratamento-de-legacy-activity-l
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/Pages/Companies/Show.jsx
autonomous: true
requirements: [META-03]
tags: [meta-03, legacy, frontend, ux, react, seed-270629]

must_haves:
  truths:
    - "Empresa sem meta ativa exibe callout visivel 'Meta nao definida' na Section 'Metas Ativas'"
    - "Callout tem tom cinza-ambar (nao vermelho — nao gritar) com icone AlertTriangle"
    - "Callout inclui CTA 'Definir meta' que abre o dialog Nova meta ja existente no arquivo"
    - "CTA so aparece se permissions.can_create_goals=true; senao apenas o texto informativo"
    - "Zero default arbitrario criado atras dos panos (seed 270629 anti-pattern)"
  artifacts:
    - path: "resources/js/Pages/Companies/Show.jsx"
      provides: "Empty state substituido por flag META-03 com CTA condicional"
      contains: "meta-nao-definida"
  key_links:
    - from: "Show.jsx Section 'Metas Ativas'"
      to: "abrirNovaMeta()"
      via: "onClick do CTA reusa handler existente (linha 431) que abre o Dialog goalOpen"
      pattern: "abrirNovaMeta"
    - from: "Show.jsx flag META-03"
      to: "permissions.can_create_goals"
      via: "backend ja envia essa flag (CompanyController::show linha 543)"
      pattern: "canCreateGoals"
---

<objective>
Substituir o empty state atual "Nenhuma meta cadastrada" na `Section` "Metas Ativas" do `Companies/Show.jsx` (linhas 834-840) por um callout visivel "Meta nao definida" com icone AlertTriangle em tom cinza-ambar, mensagem explicativa em pt-BR, e CTA "Definir meta" (condicional a `canCreateGoals`). O CTA reusa o handler `abrirNovaMeta()` ja existente que abre o Dialog goalOpen. Zero default arbitrario — usuario preenche explicitamente.

Purpose: Fecha SC #2 da Phase 63 (META-03). Empresas legadas cadastradas antes do Plan 63-01 (que ainda nao tem Goal ativa) ganham flag visivel com CTA explicito para definicao manual — nao ha suggestao/default automatico (respeita seed 270629 anti-pattern).

Output: Show.jsx modificado com callout novo + rodar `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/seeds/270629-modificar-entrada-empresa-meta-onboarding.md
@resources/js/Pages/Companies/Show.jsx
@CLAUDE.md

<interfaces>
<!-- Ja disponivel no arquivo. Nao explorar. -->

Show.jsx state/handlers (linhas 368-455):
- `company.goals` (array) — enviado pelo CompanyController::show linha 455-473 com shape `{id, metric, metric_label, target_value, value_type, period_type, active, results[]}`
- `canCreateGoals = !!permissions.can_create_goals` (linha 380) — computed no top do component
- `abrirNovaMeta = () => { if (!canCreateGoals) return; goalForm.setData({...}); goalForm.clearErrors(); setGoalOpen(true); }` (linhas 431-443)
- `<Dialog open={goalOpen} onOpenChange={setGoalOpen}>` (linha 1077) — dialog "Nova meta" completo com metric select + target_value + value_type + period_type + description, form submit para route('goals.store')

Icones ja importados na linha 10: `AlertTriangle` esta na lista.

Empty state atual (linhas 834-840):
```
{(company.goals || []).filter(g => g.active).length === 0 ? (
    <p data-testid="company-goals-empty" className="text-white/25 text-sm text-center py-8">
        Nenhuma meta cadastrada
    </p>
) : (
    ...
)}
```

Componente `<Button size="sm">` de `@/Components/ui/button` (import linha 5) — variante default (dark theme, sem props extras). CTA usa `<Button size="sm" onClick={abrirNovaMeta}>`.
</interfaces>

<decision_context>
Tom visual: cinza-ambar (nao vermelho) — feedback de discussao acima. Amber comunica "atencao/pendencia", vermelho comunica "erro/falha". Empresa sem meta e pendencia, nao erro.

Classes tailwind sugeridas (harmonizadas com resto do arquivo dark-theme + tokens ecf):
- container: `rounded-xl border border-amber-500/20 bg-amber-500/[0.04] p-6`
- icone: `AlertTriangle size={18} className="text-amber-400/80"`
- titulo: `text-white/85 text-sm font-medium`
- corpo: `text-white/50 text-xs leading-relaxed`
- botao: `<Button size="sm" onClick={abrirNovaMeta}>` (variante default do projeto)

data-testid = "company-goals-missing-flag" (nao apagar `company-goals-empty` do JSX antigo — vamos SUBSTITUIR a tag `<p>` pelo callout; testes que ainda querem "empty" precisam ser atualizados noutro plano; verificar se existem — cf. Task 2 abaixo).

CTA aparece SOMENTE se `canCreateGoals=true`. Estrategista vinculado a empresa pela pivot company_users OR admin ja passam esse gate (backend linha 543). Consultor/mentor/user sem permissao veem apenas o texto "Meta nao definida" com sub-linha "Solicite ao estrategista da empresa para definir a meta." (mensagem alternativa).

Se `canCreateGoals=false`, exibir texto alternativo pt-BR em vez de botao. Nao esconder callout — pendencia continua visivel.

**Migração automática de empresas legacy (com sugestão baseada em revenue médio 3m) fica OUT-OF-SCOPE** desta phase — tracked como `META-FUTURE-02` em REQUIREMENTS.md linha 70. A empresa legada continua aparecendo com a flag "Meta não definida" até que um estrategista/admin abra o CTA e defina a meta manualmente. Zero default arbitrário atrás dos panos (seed 270629 anti-pattern respeitado).
</decision_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir empty state por callout "Meta nao definida" com CTA condicional</name>
  <files>resources/js/Pages/Companies/Show.jsx</files>
  <read_first>
    - resources/js/Pages/Companies/Show.jsx linhas 1-100 (imports + helpers Section/InfoRow)
    - resources/js/Pages/Companies/Show.jsx linhas 368-460 (state + abrirNovaMeta + goalForm setup)
    - resources/js/Pages/Companies/Show.jsx linhas 820-860 (Section "Metas Ativas" atual)
    - resources/js/Pages/Companies/Show.jsx linhas 1077-1180 (Dialog Nova meta ja existente — validar que nao precisa mudar)
  </read_first>
  <action>
    Editar `resources/js/Pages/Companies/Show.jsx`:

    (a) Localizar bloco (linhas 834-840):
    ```
    {(company.goals || []).filter(g => g.active).length === 0 ? (
        <p
            data-testid="company-goals-empty"
            className="text-white/25 text-sm text-center py-8"
        >
            Nenhuma meta cadastrada
        </p>
    ) : (
    ```

    SUBSTITUIR o bloco do `<p data-testid="company-goals-empty">...</p>` pelo callout novo. Manter o wrapper condicional (`... === 0 ? (...) : (...)`). Novo conteudo do branch true (sem meta):
    ```
    <div
        data-testid="company-goals-missing-flag"
        className="rounded-xl border border-amber-500/20 bg-amber-500/[0.04] p-6 flex flex-col sm:flex-row items-start sm:items-center gap-4"
    >
        <div className="flex items-center gap-3 flex-1">
            <div className="flex items-center justify-center w-10 h-10 rounded-full bg-amber-500/10 shrink-0">
                <AlertTriangle size={18} className="text-amber-400/80" />
            </div>
            <div className="min-w-0">
                <p className="text-white/85 text-sm font-medium">Meta nao definida</p>
                <p className="text-white/50 text-xs leading-relaxed mt-0.5">
                    {canCreateGoals
                        ? 'Esta empresa ainda nao tem meta ativa. Defina agora para acompanhar progresso e evitar decisoes sem baseline.'
                        : 'Esta empresa ainda nao tem meta ativa. Solicite ao estrategista da empresa para definir.'}
                </p>
            </div>
        </div>
        {canCreateGoals && (
            <Button size="sm" onClick={abrirNovaMeta} data-testid="company-goals-define-cta">
                <Plus className="h-3.5 w-3.5 mr-1" /> Definir meta
            </Button>
        )}
    </div>
    ```

    (b) O bloco `{canCreateGoals && (<div className="flex justify-end mb-3"><Button ...>Nova meta</Button></div>)}` (linhas 827-833) fica mantido — quando ha meta(s) ativa(s), esse botao continua permitindo adicionar mais. Nao remover.

    (c) NAO remover o comentario existente da Section "Metas Ativas" (linhas 821-825) — manter contexto historico.

    (d) Confirmar que `AlertTriangle` esta no import (linha 10) e que `Plus` tambem (linha 10) — ambos ja estao, mas verificar.

    (e) Rodar `npm run build` (obrigatorio pos-edit JSX). Confirmar 0 erros. Grep gates: `grep -c "company-goals-missing-flag" resources/js/Pages/Companies/Show.jsx` -> ==1. `grep -c "Meta nao definida" resources/js/Pages/Companies/Show.jsx` -> ==1. `grep -c "company-goals-empty" resources/js/Pages/Companies/Show.jsx` -> ==0 (removido). `grep -c "abrirNovaMeta" resources/js/Pages/Companies/Show.jsx` -> >=2 (definicao linha 431 + botao "Nova meta" existente linha 829 + CTA novo).

    (f) Verificar se algum teste Feature/E2E referencia `data-testid="company-goals-empty"`:
    ```
    grep -rn "company-goals-empty" tests/ 2>/dev/null
    ```
    Se resultado NAO-vazio, adicionar nota no SUMMARY listando arquivos afetados para atualizacao futura (nao mexer nos testes agora — fora do escopo desta plan). Se vazio, seguir sem preocupacao.

    Commit: `feat(63-03): flag 'Meta nao definida' com CTA em Companies/Show (META-03)`.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -10 &amp;&amp; grep -c "company-goals-missing-flag" resources/js/Pages/Companies/Show.jsx &amp;&amp; grep -c "Meta nao definida" resources/js/Pages/Companies/Show.jsx</automated>
    <human-check>Abrir /companies/{id} de uma empresa sem meta ativa (via tinker: `Company::whereDoesntHave('goals', fn($q) => $q->where('active', true))->first()->id`). VER callout amber com AlertTriangle + texto "Meta nao definida" + botao "Definir meta" (se admin) OU sub-texto "Solicite ao estrategista" (se consultor). Clicar em "Definir meta" abre o Dialog Nova meta existente.</human-check>
  </verify>
  <done>Build Vite sem erros; grep gates passam; callout visivel para empresa sem meta ativa; CTA abre Dialog existente; commit criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descricao |
|----------|-----------|
| Browser user com/sem permission -> UI Show.jsx | React renderiza CTA condicional; render de callout nao expoe dados privilegiados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03-01 | Elevation of Privilege | User sem permission ve/clica CTA | mitigate | `canCreateGoals` condiciona render do botao. Mesmo se user manipular DOM via DevTools, backend `GoalController::store` (linhas 130-137) ja checa admin OR estrategista via wherePivot — 403 no submit |
| T-63-03-02 | Information disclosure | Callout revela informacao sobre empresa | accept | Texto e generico ("Meta nao definida") — nao revela dados sensiveis alem do fato ja implicito na Section vazia |
</threat_model>

<verification>
- `npm run build` -> 0 erros
- `grep -c "company-goals-missing-flag" resources/js/Pages/Companies/Show.jsx` -> == 1
- `grep -c "Meta nao definida" resources/js/Pages/Companies/Show.jsx` -> == 1
- `grep -c "company-goals-empty" resources/js/Pages/Companies/Show.jsx` -> == 0
- Manual: `/companies/{id}` de empresa legacy sem meta mostra callout amber; clicar em CTA abre dialog Nova meta (reuso de handler existente)
</verification>

<success_criteria>
- Empresa sem meta ativa exibe callout visivel "Meta nao definida" tom cinza-ambar na Section "Metas Ativas"
- CTA "Definir meta" so aparece para `canCreateGoals=true` (admin OU estrategista vinculado)
- Users sem permissao veem texto informativo alternativo direcionando ao estrategista
- Clicar em CTA abre Dialog "Nova meta" ja existente no arquivo (reuso do handler `abrirNovaMeta`)
- Zero default arbitrario criado no backend por consequencia desta plan (respeita seed 270629)
</success_criteria>

<output>
Criar `.planning/phases/63-metas-onboarding-obrigat-rio-tratamento-de-legacy-activity-l/63-03-SUMMARY.md` ao completar
</output>
