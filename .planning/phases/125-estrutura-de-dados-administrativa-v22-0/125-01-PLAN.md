---
phase: 125-estrutura-de-dados-administrativa-v22-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_08_10_100000_create_contrato_assinaturas_table.php
  - app/Models/ContratoAssinatura.php
  - database/factories/ContratoAssinaturaFactory.php
  - app/Models/Company.php
  - tests/Feature/Phase125/ContratoAssinaturaSchemaTest.php
  - tests/Feature/Phase125/ContratoAssinaturaModelTest.php
autonomous: true
requirements: [DADOS-01, DADOS-04]

must_haves:
  truths:
    - "Toda empresa pode ter um contrato de assinatura com estado atual e as datas de envio, assinatura e liberação"
    - "Os 7 estados (rascunho, aguardando_assinaturas, assinado, recusado, expirado, cancelado, erro) persistem e voltam do banco iguais, sem tipo restrito"
    - "recusado e expirado nunca resolvem para o mesmo valor interno que cancelado ou erro"
    - "A mesma empresa não consegue ter dois contratos em andamento ao mesmo tempo — o banco recusa o segundo"
    - "A mesma empresa pode ter vários contratos encerrados no histórico, sem colisão"
    - "ContratoAssinaturaFactory cria um contrato válido sem receber argumento nenhum"
    - "Os serviços e valores ficam congelados no próprio contrato e voltam idênticos do banco"
  artifacts:
    - path: "database/migrations/2026_08_10_100000_create_contrato_assinaturas_table.php"
      provides: "Tabela contrato_assinaturas com índices nomeados à mão"
      contains: "ca_company_andamento_uniq"
    - path: "app/Models/ContratoAssinatura.php"
      provides: "Constantes dos 7 estados, casts, LogsActivity, sincronia da coluna auxiliar"
      contains: "STATUS_AGUARDANDO_ASSINATURAS"
    - path: "database/factories/ContratoAssinaturaFactory.php"
      provides: "Factory + state emAndamento()"
      contains: "emAndamento"
    - path: "tests/Feature/Phase125/ContratoAssinaturaSchemaTest.php"
      provides: "Prova de schema (colunas, índices nomeados, unicidade)"
    - path: "tests/Feature/Phase125/ContratoAssinaturaModelTest.php"
      provides: "Prova dos 7 estados, do cast array e do guard D-01"
  key_links:
    - from: "app/Models/Company.php"
      to: "App\\Models\\ContratoAssinatura"
      via: "hasMany contratoAssinaturas()"
      pattern: "hasMany\\(ContratoAssinatura::class\\)"
    - from: "app/Models/ContratoAssinatura.php"
      to: "coluna company_id_em_andamento"
      via: "hook booted()/saving que espelha company_id enquanto o status é de andamento e zera fora dele"
      pattern: "company_id_em_andamento"
---

<objective>
Criar a tabela `contrato_assinaturas`, seu model, sua factory e as provas automatizadas —
o continente onde o processo de assinatura de cada empresa passa a viver.

Propósito: hoje não existe lugar nenhum para responder "em que pé está o contrato desta
empresa?". Esta é a fundação que as Fases 126 (Clicksign), 129 (webhook), 130 (liberação
manual) e 131 (tela) vão preencher e ler.

Saída: 1 migration, 1 model, 1 factory, 1 relação nova em `Company`, 2 arquivos de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-estrutura-de-dados-administrativa-v22-0/125-CONTEXT.md
@.planning/phases/125-estrutura-de-dados-administrativa-v22-0/125-RESEARCH.md
@.planning/phases/125-estrutura-de-dados-administrativa-v22-0/125-VALIDATION.md
@CLAUDE.md

<decisoes_travadas_aplicaveis>
Vindas do `125-CONTEXT.md`. Não reabrir nenhuma:

- **D-01** — no máximo **um contrato em andamento por empresa**. Garantia DUPLA: coluna
  auxiliar nullable + índice único no banco (garantia real, sobrevive a duplo clique e a
  retry de fila) **e** guard no código (mensagem amigável).
- **D-02** — reemissão cria registro novo. Contrato recusado/expirado/cancelado permanece
  intacto como histórico. O schema tem que **permitir** vários contratos por empresa.
- **D-03** — `expira_em` **NÃO** entra nesta fase (é Fase 127). O estado `expirado` existe,
  mas nada aqui calcula expiração. Não criar coluna de prazo.
- **D-04** — `status` é `string` + constantes públicas no model. **Nunca** `enum` de banco.
- **D-05** — `liberado` **não é estado**. O contrato para em `assinado`; a soltura da empresa
  é a data `liberado_em`. Contrato `assinado` com `liberado_em` nulo é caso legítimo.
- **D-06** — os 7 estados: `rascunho`, `aguardando_assinaturas`, `assinado`, `recusado`,
  `expirado`, `cancelado`, `erro`. `recusado` e `expirado` **nunca** colapsam em
  `cancelado`/`erro`.
- **D-10** — serviços e valores **congelados em JSON** no próprio contrato, no instante da
  geração. Não é lido ao vivo de `contratos_servico`, e não vira terceira tabela.
</decisoes_travadas_aplicaveis>

<armadilhas_de_mariadb_desta_fase>
As três já custaram deploy quebrado neste projeto e **nenhuma é pega pelo SQLite dos testes**:

1. **`enum` + SQLite** — nenhum `$table->enum(` nesta fase, em coluna nenhuma (D-04).
2. **FK `nullOnDelete()` sem `->nullable()`** → erro **1830** no MariaDB. Neste plano não há
   FK `nullOnDelete` (a única FK é `company_id`, `cascadeOnDelete`, NOT NULL de propósito).
3. **Nome de índice > 64 caracteres** → erro **1059** no MariaDB. O SQLite aceita, o deploy
   quebra, **a tabela nasce sem o índice** e a migration fica `Pending` em silêncio.
   **Todo índice desta fase é nomeado à mão.** Vale também para o nome do constraint de FK:
   `contrato_assinaturas_company_id_foreign` tem 39 caracteres — cabe, mas conferir.
</armadilhas_de_mariadb_desta_fase>

<constraints>
- Comentários e docblocks em **pt-BR**, citando a fase de origem (`// Fase 125 — …`).
- Execução sequencial na árvore principal, **repositório compartilhado com outro dev e com
  outras sessões**: ao commitar, usar **sempre caminhos explícitos**
  (`git commit -- <arquivo> <arquivo>`). **Nunca** `git add -A` nem `git add .`.
- Runtime PHP fora do PATH: chamar sempre `& "C:\xampp\php\php.exe"`.
- **Nunca** rodar `phpunit` sem argumento — `set_time_limit(300)` em
  `app/Console/Commands/SyncGrantsFromEcfDrive.php:23` mata o processo antes do relatório.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration contrato_assinaturas + prova de schema</name>

  <files>
    database/migrations/2026_08_10_100000_create_contrato_assinaturas_table.php
    tests/Feature/Phase125/ContratoAssinaturaSchemaTest.php
  </files>

  <read_first>
    - database/migrations/2026_08_03_120000_create_desempenho_company_score_snapshots_table.php
      — **o analog principal**. Índices nomeados à mão (`dcss_user_company_mes_unique`) com o
      comentário que explica o erro 1059, e `$table->string('status')` com o comentário que
      explica por que nunca é tipo restrito por lista fixa.
    - database/migrations/2026_07_23_100000_create_company_manager_history_table.php
      — migration curta do mesmo estilo, com FK para `companies`.
    - tests/Feature/Phase125/../Phase122/CompanyScoreSnapshotSchemaTest.php
      (caminho real: `tests/Feature/Phase122/CompanyScoreSnapshotSchemaTest.php`)
      — **molde direto deste teste**: `tabela_existe_com_todas_as_colunas_esperadas`,
      `chave_unica_barra_duplicata_de_user_company_mes` (usa `DB::table()` + `expectException`),
      `status_aceita_string_arbitraria_sem_tipo_restrito`.
  </read_first>

  <action>
Criar a migration `create_contrato_assinaturas_table` com docblock pt-BR citando a Fase 125,
os requisitos DADOS-01/DADOS-04 e as decisões D-01/D-04/D-05/D-06/D-10.

Colunas de `contrato_assinaturas`, nesta ordem:

- `id()`
- `company_id` — `foreignId('company_id')->constrained()->cascadeOnDelete()`, NOT NULL de
  propósito (não usar `nullOnDelete` aqui: exigiria `nullable()` e o contrato não faz sentido
  sem empresa).
- `status` — `string('status', 40)->default('rascunho')`. **STRING, nunca `enum`** (D-04);
  replicar o comentário do analog explicando o motivo.
- `company_id_em_andamento` — `unsignedBigInteger(...)->nullable()`. Coluna auxiliar da D-01:
  espelha `company_id` **somente** enquanto o status é de andamento, e fica `NULL` fora dele.
  **Sem FK própria** — é espelho, não relacionamento; uma segunda FK só traria mais um
  identificador longo para caber em 64 caracteres. Comentar isso na migration.
- `clicksign_envelope_id` — `string(64)->nullable()`. Preenchido pela Fase 126
  (`125-CONTEXT.md` → Integration Points: "A Fase 126 preenche `clicksign_*`").
- `clicksign_document_id` — `string(64)->nullable()`. Idem.
- `enviado_em` — `timestamp(...)->nullable()` (DADOS-01, data de envio).
- `assinado_em` — `timestamp(...)->nullable()` (DADOS-01, data de assinatura; a Fase 129 preenche).
- `liberado_em` — `timestamp(...)->nullable()` (DADOS-01 + **D-05**: é data, não estado; a
  Fase 130 preenche na liberação manual). Comentar que `assinado` com `liberado_em` nulo é o
  caso legítimo que o alerta da REDE-02 precisa enxergar.
- `erro_mensagem` — `text(...)->nullable()`. Detalhe da falha técnica quando `status = erro`
  (DADOS-04 separa falha técnica de recusa do cliente).
- `servicos_snapshot` — `json(...)->nullable()`. **D-10** — congelamento dos serviços e
  valores no instante da geração; comentar que NÃO é relido ao vivo de `contratos_servico`
  (o `hs_mrr = 0` do HubSpot já zerou 3 contratos de R$ 3.000 neste projeto).
- `timestamps()`

**NÃO criar** coluna de prazo/`expira_em` — é a Fase 127 (D-03).

Índices, **todos com nome explícito** (armadilha 1059):

- `unique('company_id_em_andamento', 'ca_company_andamento_uniq')` — 25 caracteres. É a
  garantia real da D-01: MariaDB e SQLite tratam múltiplos `NULL` como distintos, então
  contratos encerrados (coluna `NULL`) não colidem entre si, e dois em andamento colidem.
- `unique('clicksign_envelope_id', 'ca_clicksign_envelope_uniq')` — 26 caracteres. Um envelope
  da Clicksign nunca pertence a dois contratos; múltiplos `NULL` continuam permitidos.
- `index(['company_id', 'status'], 'ca_company_status_idx')` — 21 caracteres. Leitura
  "histórico de contratos desta empresa".

`down()` faz `Schema::dropIfExists('contrato_assinaturas')`.

Criar o diretório `tests/Feature/Phase125/` e o arquivo `ContratoAssinaturaSchemaTest.php`
(namespace `Tests\Feature\Phase125`, `use RefreshDatabase`, atributos `#[Test]` como no molde
da Phase122). Este arquivo é **schema puro** — usa `Schema`, `DB::table()` e `Company::factory()`,
e **não** referencia o model `ContratoAssinatura` (ele só nasce na Task 2). Testes:

1. `tabela_existe_com_todas_as_colunas_esperadas` — `Schema::hasTable` + `Schema::hasColumns`
   com a lista completa de colunas acima, incluindo `created_at`/`updated_at`.
2. `indices_tem_nome_explicito_e_cabem_em_64_caracteres` — ler `Schema::getIndexes('contrato_assinaturas')`,
   extrair a coluna `name` de cada entrada, afirmar que os três nomes acima estão presentes e
   que **todo** nome retornado tem `strlen() <= 64`. É a única guarda automatizada possível
   contra o erro 1059 antes do deploy.
3. `indice_unico_barra_segundo_contrato_em_andamento` — inserir via `DB::table()` duas linhas
   para a mesma empresa com `company_id_em_andamento` preenchido; esperar `QueryException` na
   segunda (mesmo molde de `chave_unica_barra_duplicata_de_user_company_mes`).
4. `varios_contratos_encerrados_da_mesma_empresa_convivem` — inserir três linhas para a mesma
   empresa com `company_id_em_andamento = null` e status `recusado`/`expirado`/`cancelado`;
   afirmar `count() === 3`. Prova a D-02 no nível do banco.
5. `status_aceita_string_arbitraria_sem_tipo_restrito` — inserir um status inexistente via
   `DB::table()` e reler; prova que não há `enum`/CHECK (D-04).
  </action>

  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase125/ContratoAssinaturaSchemaTest.php</automated>
  </verify>

  <acceptance_criteria>
    - Os 5 testes de `ContratoAssinaturaSchemaTest` passam (saída `OK` / sem `FAILURES`).
    - `grep -n 'enum(' database/migrations/2026_08_10_100000_create_contrato_assinaturas_table.php`
      não retorna nenhuma linha de código (ocorrências apenas em comentário são aceitáveis;
      conferir a olho que não há `$table->enum(`).
    - Os três nomes de índice aparecem literalmente na migration:
      `ca_company_andamento_uniq`, `ca_clicksign_envelope_uniq`, `ca_company_status_idx`.
    - Nenhuma coluna de prazo/`expira_em` existe na migration (D-03).
  </acceptance_criteria>

  <done>
    A tabela `contrato_assinaturas` existe no schema de teste com todas as colunas de
    DADOS-01/D-10, os três índices nomeados à mão, e o banco recusa o segundo contrato em
    andamento da mesma empresa enquanto aceita quantos encerrados existirem.
  </done>
</task>

<task type="auto">
  <name>Task 2: Model ContratoAssinatura + factory + relação em Company</name>

  <files>
    app/Models/ContratoAssinatura.php
    database/factories/ContratoAssinaturaFactory.php
    app/Models/Company.php
    tests/Feature/Phase125/ContratoAssinaturaModelTest.php
  </files>

  <read_first>
    - app/Models/ContratoServico.php — molde de model com `LogsActivity`,
      `getActivitylogOptions()` com `logOnly()` + `logOnlyDirty()` +
      `setDescriptionForEvent()` em pt-BR, e o cast `'hubspot_snapshot' => 'array'` que a
      D-10 replica.
    - app/Models/HubspotEvento.php — molde de model de processo: `status` em `string` com a
      **lista de estados documentada no docblock da classe**.
    - app/Models/Sugador.php — molde das constantes `public const SCREAMING_SNAKE` e do array
      agregador de constantes (`STATUS_TRAVADOS`).
    - app/Models/Company.php — linhas 340-410 (`npsSurveys()` e `contratosServico()`) para o
      lugar e o estilo da nova relação `hasMany`.
    - database/factories/NpsSurveyFactory.php — molde de `definition()` + `state()` nomeado.
    - database/migrations/2026_08_10_100000_create_contrato_assinaturas_table.php — a
      migration escrita na Task 1 (nomes finais das colunas).
  </read_first>

  <action>
**1) `app/Models/ContratoAssinatura.php`** — docblock de classe em pt-BR citando Fase 125 e
listando os 7 estados com uma linha de significado cada (molde `HubspotEvento`), incluindo a
observação da D-05 de que `liberado` **não** é estado e a da D-03 de que nada aqui expira
contrato sozinho (falta a data, que é da Fase 127).

- `use LogsActivity;`, `protected $table = 'contrato_assinaturas';`
- `$fillable` **explícito** com todas as colunas da migration exceto `id`/timestamps.
  Nunca `$guarded = []` (mass assignment).
- `$casts`: `enviado_em`, `assinado_em`, `liberado_em` → `datetime`; `servicos_snapshot` →
  `array` (D-10, mesmo cast de `ContratoServico::$casts['hubspot_snapshot']`).
- Constantes dos 7 estados (D-04/D-06), alinhadas por coluna:
  `STATUS_RASCUNHO`, `STATUS_AGUARDANDO_ASSINATURAS`, `STATUS_ASSINADO`, `STATUS_RECUSADO`,
  `STATUS_EXPIRADO`, `STATUS_CANCELADO`, `STATUS_ERRO`.
- `public const STATUS_TODOS` — array agregador com os 7, na ordem da D-06.
- `public const STATUS_EM_ANDAMENTO` — array com **apenas** `STATUS_RASCUNHO` e
  `STATUS_AGUARDANDO_ASSINATURAS`. Comentar que é esta lista, e só ela, que ocupa o slot do
  índice único da D-01.
- `protected static function booted(): void` com um hook `saving` que sincroniza a coluna
  auxiliar: se `status` estiver em `STATUS_EM_ANDAMENTO`, `company_id_em_andamento` recebe
  `company_id`; caso contrário recebe `null`. Comentar que isto torna impossível a coluna
  auxiliar desincronizar do `status`, e que a garantia final continua sendo o índice único
  do banco — o hook é conveniência, não a trava.
- `public function estaEmAndamento(): bool` — `in_array($this->status, self::STATUS_EM_ANDAMENTO, true)`.
- `public static function emAndamentoDaEmpresa(int $companyId): ?self` — o **guard de código**
  da D-01: devolve o contrato em andamento da empresa (ou `null`), consultando por
  `company_id_em_andamento`. Docblock explicando que quem for criar contrato (Fase 127) deve
  chamar isto antes, para o usuário ver "esta empresa já tem contrato em andamento" em vez de
  um 500 de constraint.
- `getActivitylogOptions()` — `logOnly(['status', 'enviado_em', 'assinado_em', 'liberado_em'])`
  + `logOnlyDirty()` + `dontSubmitEmptyLogs()` + descrições pt-BR por evento.
  ⚠️ **Não** incluir `servicos_snapshot` no `logOnly`: são valores de contrato, o
  `activity_log` viraria cópia paralela do congelamento (ver `<threat_model>`).
- Relações: `company(): BelongsTo`. **Não** declarar `signatarios()` aqui — a relação nasce
  no plano 125-02, junto com o model do signatário.

**2) `database/factories/ContratoAssinaturaFactory.php`** — `@extends Factory<ContratoAssinatura>`,
docblock pt-BR citando a Fase 125.

- `definition()`: `company_id => Company::factory()`, `status => ContratoAssinatura::STATUS_RASCUNHO`,
  `servicos_snapshot => null`. **Não** setar `company_id_em_andamento` na definition — quem
  cuida disso é o hook `saving` do model.
- `emAndamento(): static` — state que só troca o `status` para `STATUS_AGUARDANDO_ASSINATURAS`.
  Comentar explicitamente que **não** se deve copiar `$attributes['company_id']` para a coluna
  auxiliar aqui: dentro de um state esse valor pode ser uma instância de `Factory`, não um
  inteiro — o hook do model resolve depois de a empresa existir.
- `assinado(): static` — `status = STATUS_ASSINADO` + `enviado_em`/`assinado_em` no passado e
  `liberado_em = null` (o caso da D-05 que a REDE-02 precisa enxergar).

**3) `app/Models/Company.php`** — adicionar `contratoAssinaturas()` retornando
`$this->hasMany(ContratoAssinatura::class)`, junto das outras relações `hasMany`, com
comentário `// Fase 125 — contratos de assinatura (Clicksign) da empresa.`
Alteração **aditiva**: não tocar em mais nada deste arquivo.

**4) `tests/Feature/Phase125/ContratoAssinaturaModelTest.php`** (namespace `Tests\Feature\Phase125`,
`RefreshDatabase`, `#[Test]`):

1. `factory_cria_contrato_valido_sem_argumento` — `ContratoAssinatura::factory()->create()`
   persiste, tem `company_id` preenchido e `status === STATUS_RASCUNHO` (Success Criteria 1).
2. `os_sete_estados_persistem_e_voltam_iguais` — iterar `STATUS_TODOS`, criar um contrato com
   cada valor e afirmar que `fresh()->status` é idêntico; afirmar também
   `count(STATUS_TODOS) === 7` (DADOS-04).
3. `recusado_e_expirado_nunca_resolvem_para_cancelado_nem_erro` — afirmar que os quatro valores
   de constante são distintos entre si (`assertNotSame` par a par) **e** que quatro registros
   criados com eles voltam do banco com os quatro valores diferentes. É o Success Criteria 3
   do ROADMAP, provado no valor interno e no round-trip (DADOS-04).
4. `round_trip_de_servicos_snapshot_via_cast_array` — gravar um array com dois serviços
   (`servico`, `valor_mensal`, `servico_id`) e afirmar `assertSame($payload, $row->fresh()->servicos_snapshot)`
   (D-10; molde `round_trip_de_quality_via_cast_array` da Phase122).
5. `empresa_nao_pode_ter_dois_contratos_em_andamento` — criar um `->emAndamento()` para a
   empresa e esperar `QueryException` ao criar o segundo `->emAndamento()` para a **mesma**
   empresa (D-01 pelo caminho do model).
6. `empresa_pode_ter_novo_contrato_depois_de_um_encerrado` — criar um `emAndamento()`, mudar
   seu status para `STATUS_RECUSADO` e salvar, depois criar outro `emAndamento()` para a mesma
   empresa sem exceção; afirmar que a empresa tem 2 contratos e que o primeiro ficou com
   `company_id_em_andamento` nulo (D-02 + hook de sincronia).
7. `guard_de_codigo_encontra_o_contrato_em_andamento` — `ContratoAssinatura::emAndamentoDaEmpresa($company->id)`
   devolve o contrato em andamento e devolve `null` para empresa sem contrato (D-01, lado código).
8. `company_expoe_os_contratos_de_assinatura` — `$company->contratoAssinaturas` traz os
   contratos criados para ela.
  </action>

  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase125/</automated>
  </verify>

  <acceptance_criteria>
    - Todos os testes de `tests/Feature/Phase125/` passam (schema + model), sem `FAILURES`.
    - `ContratoAssinatura::STATUS_TODOS` tem exatamente 7 elementos e
      `ContratoAssinatura::STATUS_EM_ANDAMENTO` tem exatamente 2.
    - `app/Models/ContratoAssinatura.php` não contém `$guarded` e contém `$fillable` explícito.
    - `getActivitylogOptions()` não lista `servicos_snapshot` no `logOnly`.
    - `app/Models/Company.php` mudou **apenas** pela adição de `contratoAssinaturas()`
      (conferir com `git diff -- app/Models/Company.php`).
  </acceptance_criteria>

  <done>
    O model existe com os 7 estados como constantes, a factory cria contratos válidos e em
    andamento, `Company` expõe a relação, e os testes provam DADOS-01, DADOS-04, D-01, D-02,
    D-05 e D-10.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código PHP interno → banco | Única fronteira desta fase. **Não há rota, controller, request HTTP nem input de usuário** — a tabela é escrita por código interno (Fases 126/129/130) e pela suíte de testes. Ameaça de superfície HTTP não se aplica aqui. |
| model → `activity_log` (spatie) | Dado do contrato é copiado para uma segunda tabela de auditoria com retenção própria. O que entra no `logOnly()` passa a existir em dois lugares. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Tampering | `ContratoAssinatura` (mass assignment) | mitigate | `$fillable` explícito com a lista fechada de colunas; **proibido** `$guarded = []`. Verificado no acceptance criteria da Task 2. |
| T-125-02 | Tampering / DoS de integridade | `contrato_assinaturas` — duplo clique ou retry de fila criando 2 contratos em andamento | mitigate | Índice único `ca_company_andamento_uniq` sobre a coluna auxiliar (garantia real, D-01) + hook `saving` que sincroniza a coluna + guard `emAndamentoDaEmpresa()` para mensagem amigável. Teste 5 da Task 2 prova que o banco recusa. |
| T-125-03 | Information Disclosure | `servicos_snapshot` (valores contratados) vazando para `activity_log` | mitigate | `logOnly()` restrito a `status` e às três datas. Valores contratados **não** são logados — evita cópia paralela do congelamento numa tabela com política de retenção diferente. |
| T-125-04 | Tampering | Envelope da Clicksign vinculado a dois contratos | mitigate | Índice único `ca_clicksign_envelope_uniq` (múltiplos `NULL` seguem permitidos, então não atrapalha contratos ainda não enviados). |
| T-125-05 | Repudiation | Perda silenciosa da constraint em produção (erro 1059 cria a tabela **sem** o índice e deixa a migration `Pending`) — a unicidade da D-01 deixaria de existir sem erro visível | mitigate | Todo índice nomeado à mão com nome curto; teste `indices_tem_nome_explicito_e_cabem_em_64_caracteres`; gate humano em MariaDB real no plano 125-03. |
| T-125-06 | Repudiation | Apagar a `Company` apaga os contratos (`cascadeOnDelete`) | accept | Não existe fluxo de exclusão de empresa nesta milestone; contrato sem empresa não tem significado jurídico próprio. Registrado aqui para a Fase 131 saber que exclusão de empresa apaga o histórico de contratos em cascata. |
| T-125-SC | Tampering | instalação de pacote (npm/composer) | n/a | **Nenhum pacote novo é instalado nesta fase** — `125-RESEARCH.md` § Package Legitimacy Audit registra que o gate não é acionado. Nenhum `composer require` deve aparecer neste plano. |
</threat_model>

<verification>
1. `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase125/` — tudo verde.
2. Regressão de vizinhança (a fase toca `Company`):
   `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase122/`
   `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/BonusInvalidacaoEmpresaTest.php`
3. Leitura da migration contra o analog `2026_08_03_120000_create_desempenho_company_score_snapshots_table.php`:
   nenhum `$table->enum(`, nenhum `unique(`/`index(` sem nome explícito, nenhum `nullOnDelete`.
4. ⚠️ Teste verde **não** prova o deploy — a prova em MariaDB é o plano 125-03.
</verification>

<success_criteria>
- [ ] `contrato_assinaturas` existe com `status`, `enviado_em`, `assinado_em`, `liberado_em`,
      `servicos_snapshot` e a coluna auxiliar de andamento (DADOS-01, D-05, D-10)
- [ ] Nenhuma coluna de prazo/`expira_em` foi criada (D-03)
- [ ] Os 7 estados são constantes públicas do model e persistem sem tipo restrito (D-04, D-06, DADOS-04)
- [ ] `recusado` e `expirado` nunca resolvem para o mesmo valor que `cancelado`/`erro` (Success Criteria 3 do ROADMAP)
- [ ] O banco recusa o segundo contrato em andamento da mesma empresa e aceita N encerrados (D-01, D-02)
- [ ] `ContratoAssinaturaFactory` cria contrato válido sem argumento (Success Criteria 1 do ROADMAP)
- [ ] `Company::contratoAssinaturas()` existe e devolve os contratos da empresa
- [ ] Os três índices são nomeados à mão e cabem em 64 caracteres
</success_criteria>

<output>
Criar `.planning/phases/125-estrutura-de-dados-administrativa-v22-0/125-01-SUMMARY.md` ao terminar.
</output>
