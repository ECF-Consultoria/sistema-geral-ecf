---
phase: 137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - public/hot
  - .planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-BASELINE-TESTES.md
autonomous: true
requirements: [ETAPA-01, ETAPA-02]
must_haves:
  truths:
    - "Uma Feature test que renderiza página Inertia passa neste worktree sem erro de Vite manifest"
    - "Existe um registro escrito do estado da suíte ANTES de qualquer migration desta fase"
    - "As ~10 falhas pré-existentes de Polos estão nomeadas no registro, para não serem confundidas com regressão"
  artifacts:
    - path: ".planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-BASELINE-TESTES.md"
      provides: "Baseline verde registrada antes da migration (exigência do CLAUDE.md § GSD obrigatório)"
      contains: "PolosControllerTest"
  key_links:
    - from: "137-BASELINE-TESTES.md"
      to: "todos os planos 137-02..137-07"
      via: "campo `caminho:` diz se as Feature tests exigem recriar public/hot"
      pattern: "caminho:\\s*(npm-build|public-hot)"
---

<objective>
Destravar o ambiente de teste deste worktree e registrar a baseline verde ANTES de qualquer
migration da fase.

Propósito: sem isto, a primeira suíte vermelha desta fase é confundida com regressão. Este
worktree não tem `node_modules/`, `public/build/` nem `public/hot` — medido pela pesquisa,
`tests/Feature/Phase37CompaniesPerformanceFilterTest.php` dá **20 falhas de 24 por ambiente**,
todas com a mesma causa (`Vite manifest not found`). E o `CLAUDE.md` (§ "GSD obrigatório")
exige baseline de testes antes de migration em tabela com dado de produção — que é exatamente
o que os planos 137-02 e 137-05 fazem.

Este plano NÃO toca código de produção. Nenhum arquivo em `app/`, `database/` ou `resources/`
é modificado aqui.

Saída: ambiente capaz de rodar Feature tests + `137-BASELINE-TESTES.md` com os números medidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-VALIDATION.md
@.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-RESEARCH.md
@.planning/learnings/painel-polos-status-e-meta.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Destravar o ambiente Inertia deste worktree</name>
  <files>public/hot</files>
  <read_first>
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-RESEARCH.md` (Pitfall 4, linhas 557-590) — a causa raiz medida e as duas saídas possíveis
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-VALIDATION.md` (bloco "⚠ Pré-requisito de ambiente")
    - `CLAUDE.md` — regra "npm run build ao fim de qualquer alteração de frontend" (o plano 137-07 altera `Companies/Index.jsx`)
  </read_first>
  <action>
    Tente PRIMEIRO o caminho definitivo: `npm install` seguido de `npm run build` na raiz do
    worktree. Ele resolve os dois problemas de uma vez — cria `public/build/manifest.json` (que
    destrava as Feature tests) e deixa o worktree apto a satisfazer a regra de build do
    `CLAUDE.md` quando o plano 137-07 alterar `resources/js/Pages/Companies/Index.jsx`. Use
    timeout de 600000 ms; `npm install` restaura exatamente o `package-lock.json` já versionado
    e NÃO adiciona nenhum pacote — `package.json` e `package-lock.json` não podem ser
    modificados por esta task.

    Se `npm install` ou `npm run build` falhar (rede, versão de Node, permissão), caia no
    atalho medido pela pesquisa: criar o arquivo `public/hot` com a única linha
    `http://localhost:5173`. O `laravel-vite-plugin` detecta esse arquivo e emite tag de dev
    server em vez de ler o manifest, o que basta para o PHPUnit — os testes nunca buscam o JS
    pela rede.

    Se e somente se o atalho for usado: `public/hot` é ARQUIVO ÓRFÃO PERIGOSO. Um `public/hot`
    esquecido faz `php artisan serve` tentar servir assets de um dev server inexistente e o app
    "não carrega o JSX" sem erro visível (learning `project_vite_hot_orfao_local.md`). Remova-o
    ao final de cada sessão de teste e registre na Task 2 que ele precisa ser recriado antes de
    cada suíte Feature dos planos seguintes.

    Registre qual dos dois caminhos foi usado — a Task 2 grava isso no campo `caminho:`.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php --colors=never</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php` sai com código 0
    - Nenhuma linha da saída contém `Vite manifest not found`
    - `git status --porcelain package.json package-lock.json` não devolve nenhuma linha (as duas dependências ficam idênticas ao versionado)
    - Existe `public/build/manifest.json` OU existe `public/hot` contendo exatamente `http://localhost:5173`
  </acceptance_criteria>
  <done>Feature tests que renderizam Inertia rodam neste worktree sem erro de manifest.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar a baseline verde antes de qualquer migration</name>
  <files>.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-BASELINE-TESTES.md</files>
  <read_first>
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-VALIDATION.md` (seções "Test Infrastructure" e "Falhas pré-existentes")
    - `.planning/learnings/painel-polos-status-e-meta.md` §2 — as ~10 falhas antigas de Polos que NÃO são regressão
    - `CLAUDE.md` § "GSD obrigatório" — por que a baseline precisa ser anterior à migration
  </read_first>
  <action>
    Rode, nesta ordem, e capture a contagem literal de cada execução (testes, assertions,
    falhas):

    1. `vendor/bin/phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php --colors=never`
       (o `Baseline command` do 137-VALIDATION.md — a pesquisa mediu 24 testes / 44 assertions verdes)
    2. A suíte completa: `/c/xampp/php/php.exe artisan test` com timeout de 600000 ms. Se
       estourar o timeout, rode em duas partes — `vendor/bin/phpunit --testsuite=Unit` e
       `vendor/bin/phpunit --testsuite=Feature` — e registre as duas.

    Crie `137-BASELINE-TESTES.md` no diretório da fase com: a data; o campo `caminho:` com o
    valor `npm-build` ou `public-hot` conforme a Task 1; o comando exato de cada execução; a
    contagem literal de cada uma; e uma seção "Falhas pré-existentes — NÃO são regressão da 137"
    listando nominalmente `tests/Feature/Phase38/PolosControllerTest.php` e
    `tests/Feature/Polos/PolosFaturamentoSnapshotTest.php` com a contagem observada de falhas em
    cada uma e o ponteiro para `.planning/learnings/painel-polos-status-e-meta.md` §2.

    Se aparecerem falhas fora desses dois arquivos, liste-as também — elas passam a fazer parte
    da baseline (são estado anterior à fase), NÃO viram tarefa de correção desta fase.

    Se a Task 1 usou o atalho `public-hot`, inclua um aviso no topo do arquivo: os planos
    137-02..137-07 precisam recriar `public/hot` antes de rodar Feature tests e removê-lo ao
    final de cada task.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe vendor/bin/phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php --colors=never && grep -v '^#' .planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-BASELINE-TESTES.md | grep -c "PolosControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `137-BASELINE-TESTES.md` existe no diretório da fase
    - O arquivo contém a string `caminho: npm-build` OU `caminho: public-hot`
    - O arquivo cita nominalmente `tests/Feature/Phase38/PolosControllerTest.php` e `tests/Feature/Polos/PolosFaturamentoSnapshotTest.php` com contagem de falhas observada
    - O arquivo registra a contagem literal (testes/assertions) do `Baseline command`
    - O `Baseline command` sai com código 0
    - `git log --oneline -1 -- database/migrations` não aponta para nenhuma migration desta fase (a baseline é anterior à migration — 137-02 ainda não rodou)
  </acceptance_criteria>
  <done>Baseline escrita, datada e anterior à migration; falhas pré-existentes nomeadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro npm → worktree local | `npm install` baixa código de terceiros para dentro da árvore de trabalho |
| `public/hot` → runtime local | arquivo que altera de onde o Blade carrega assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-137-SC | Tampering | `npm install` | mitigate | Restaura exatamente o `package-lock.json` já versionado; nenhum pacote novo é adicionado. `package.json`/`package-lock.json` inalterados é critério de aceite da Task 1. RESEARCH § "Package Legitimacy Audit" registra que esta fase não instala nenhuma dependência nova, logo o protocolo de slopcheck não se aplica |
| T-137-08 | Denial of Service | `public/hot` órfão | mitigate | Remoção obrigatória ao final da sessão de teste + aviso registrado em `137-BASELINE-TESTES.md`. Um `public/hot` esquecido derruba o carregamento de JSX no `artisan serve` local sem erro visível (`project_vite_hot_orfao_local.md`) |
</threat_model>

<verification>
- `vendor/bin/phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php` sai 0
- `137-BASELINE-TESTES.md` existe e nomeia as falhas pré-existentes
- `git status --porcelain app/ database/ resources/` não devolve nenhuma linha (este plano não toca produção)
</verification>

<success_criteria>
1. Feature test que renderiza Inertia passa neste worktree
2. Baseline registrada por escrito ANTES de qualquer migration da fase
3. As ~10 falhas antigas de Polos estão nomeadas para não virarem caça a fantasma
</success_criteria>

<output>
Create `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-01-SUMMARY.md` when done
</output>
