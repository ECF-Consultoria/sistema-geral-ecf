---
phase: 137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0
plan: 08
type: execute
gap_closure: true
wave: 5
depends_on: ["137-07"]
files_modified:
  - tests/Feature/Phase137/EtapaPontoUnicoTest.php
autonomous: true
requirements: [ETAPA-03]

must_haves:
  truths:
    - "Escrever `companies.etapa` com um nome de variável diferente de `$company` (ex.: `$empresa->etapa = ...`) faz a suíte FALHAR, nomeando arquivo e linha"
    - "Montar o array de update indiretamente (`$dados['etapa'] = ...` seguido de `->update($dados)` no mesmo corpo) faz a suíte FALHAR"
    - "Escrever via `DB::table('companies')->update(['etapa' => ...])` faz a suíte FALHAR"
    - "O gate continua VERDE na árvore atual — nenhum uso legítimo de `OnboardingPasso::etapa` vira falso positivo"
    - "Um comentário contendo o texto `->etapa = ` não derruba mais o gate — comentários são neutralizados antes da varredura"
  artifacts:
    - path: "tests/Feature/Phase137/EtapaPontoUnicoTest.php"
      provides: "Gate estático permanente do ponto único de escrita de companies.etapa, com as 3 formas de bypass fechadas"
      contains: "database/migrations"
  key_links:
    - from: "tests/Feature/Phase137/EtapaPontoUnicoTest.php"
      to: "árvores app/ e database/migrations/"
      via: "varredura por token_get_all() com comentários neutralizados"
      pattern: "->etapa\\\\s\\*=\\(\\?!=\\)"
---

<objective>
Fechar o **G1 (CRITICAL)** do `137-VERIFICATION.md` / `137-REVIEW.md` (CR-01): a "prova
permanente" do Success Criteria nº 2 da Fase 137 é contornável por três caminhos, todos já
idiomáticos neste repositório e todos comprovados por injeção real de código.

O invariante ("só `EtapaTransicaoService` grava `companies.etapa`") é **verdadeiro hoje** —
grep independente confirma zero ocorrências de `->etapa =` em `app/`. O risco é prospectivo:
as Fases 138-142 acrescentam os primeiros chamadores reais do serviço espalhados por
controllers e jobs, exatamente onde nomes de variável em pt-BR (`$empresa`, medido **204
vezes** em `app/`) e comandos de diagnóstico com `DB::table('companies')` (dois precedentes
vivos) aparecem.

Purpose: o gate precisa recusar as três formas ANTES de existir o primeiro chamador de
produção, não depois.
Output: `EtapaPontoUnicoTest.php` com a varredura ampliada, provada vermelha contra as três
sondas e verde na árvore atual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-VERIFICATION.md
@.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-REVIEW.md

> ⚠️ **Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada` (worktree, branch
> `feat/fluxo-entrada-empresas`). NÃO é `C:\xampp\htdocs\ecf_admin`. Conferir com `pwd`
> antes de confiar em qualquer `git status` ou listagem.

> ⚠️ **PHP não está no PATH do Bash:** usar `C:\xampp\php\php.exe`. **Nunca** rodar
> `php artisan test` nem `--testsuite=Feature` — a suíte Feature completa não termina neste
> worktree (cascata de timeout de rede de 300s medida em `137-BASELINE-TESTES.md`, ANTES
> desta fase existir). Só os comandos de `137-VALIDATION.md`.
</context>

<measured_facts>
Fatos medidos pelo orquestrador nesta árvore, em 2026-09-01/02. Não re-descobrir — usar.

| Fato | Valor medido |
|---|---|
| `$empresa->` em `app/` | 204 ocorrências |
| `->etapa\s*=[^=]` em `app/` | **0** ocorrências |
| `->etapa\s*=[^=]` em `database/` | **0** ocorrências |
| `DB::table('companies')` em `app/` | 2 (`DiagnoseCustId.php:206`, `ImportMarketplaceFromCsv.php:145`) |
| `['etapa'] =` (offset de array) em `app/` + `database/` | **0** ocorrências |
| Suíte da fase hoje | `tests/Unit/Phase137 tests/Feature/Phase137` = **46/46 verde** |

**Desenho já validado empiricamente pelo orquestrador** (sonda rodada contra `app/` inteiro):
com as regras ampliadas descritas na Task 1, a varredura devolve **0 falsos positivos** em
`app/` e em `database/migrations/`, e **pega as três sondas de bypass**. O desenho abaixo não é
hipótese — é a configuração medida.

**Quase-colisões conhecidas** (passam hoje, e por quê — documentar no docblock):
- `app/Services/Onboarding/OnboardingEngineService.php:368` e
  `app/Services/Onboarding/OnboardingLinkService.php:96` têm `'etapa' =>` de
  `OnboardingPasso` (coluna homônima, sem relação com `companies.etapa`); passam porque o
  corpo de função não contém chamada de escrita reconhecida em `Company`.
- `database/migrations/2026_08_17_120000_add_etapa_to_onboarding_passos_table.php:69` tem
  `->update(['etapa' => $etapa])`; passa porque o arquivo **não cita** `Company` (0
  ocorrências) e a tabela é `onboarding_passos`, não `companies`.

Se algum desses corpos ganhar um `->update(`/`->save(` no futuro, o gate fica vermelho por
falso positivo. A saída correta nesse dia é acrescentar **exceção nomeada com comentário**,
nunca afrouxar a regra.
</measured_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ampliar a varredura estática para fechar as 3 formas de bypass</name>

  <read_first>
    - `tests/Feature/Phase137/EtapaPontoUnicoTest.php` — o arquivo a alterar. Ler INTEIRO,
      inclusive o bloco de comentário das linhas 137-171, que explica por que a chave crua
      `'etapa' =>` não pode ser o padrão (colisão com `OnboardingPasso::etapa`). Essa
      restrição continua valendo — a ampliação não pode reintroduzir o falso positivo.
    - `tests/Feature/Phase137/EtapaPendenciaParaleloTest.php` (linhas 150-185) — o gate irmão
      da pendência, citado pelo `137-VERIFICATION.md` como a forma que **não** sofre bypass.
      ⚠️ Ler para entender o princípio (varrer o NOME DA COLUNA, não um padrão de variável),
      **mas NÃO copiar a forma**: ali funciona porque `pendencia_aberta` é um nome único no
      repositório. `etapa` é homônimo de `onboarding_passos.etapa` em 8 arquivos — uma
      varredura por substring crua nasceria vermelha. É exatamente por isso que este gate
      precisa de regras, e não de `str_contains`.
    - `app/Services/FluxoEntrada/EtapaTransicaoService.php` — o único arquivo permitido; ver
      as duas formas de escrita legítima que ele usa (`$company->update(['etapa' => ...])` e
      `Company::whereIn(...)->update(['etapa' => ...])`).
    - `app/Console/Commands/DiagnoseCustId.php` (linha 206) e
      `app/Console/Commands/ImportMarketplaceFromCsv.php` (linha 145) — o idioma
      `DB::table('companies')->where(...)->update([...])` real que a regra 3 precisa cobrir.
  </read_first>

  <files>tests/Feature/Phase137/EtapaPontoUnicoTest.php</files>

  <behavior>
    - Sonda 1 (`$empresa->etapa = Company::ETAPA_EM_OPERACAO;` num arquivo novo de `app/`) → gate FALHA
    - Sonda 2 (`$dados['etapa'] = 'em_operacao'; $company->update($dados);`) → gate FALHA
    - Sonda 3 (`DB::table('companies')->where('id', $id)->update(['etapa' => 'em_operacao']);`) → gate FALHA
    - Árvore atual de `app/` + `database/migrations/`, sem sonda → gate PASSA (0 ofensores)
    - Um comentário contendo literalmente `$company->etapa = ` → gate PASSA (comentário neutralizado)
    - `tests/` NÃO é varrido — testes precisam poder simular escrita fora do serviço
  </behavior>

  <action>
Alterar SOMENTE `EtapaPontoUnicoTest.php`. O Grupo 1 (comportamental, `PUT /companies/{company}`)
fica intacto — ele já está VERIFIED. Toda a mudança é no Grupo 2.

**1. Neutralizar comentários antes de varrer.** Acrescentar um helper privado que recebe o
código-fonte e devolve uma versão em que o texto de todo token `T_COMMENT` e `T_DOC_COMMENT`
é substituído por uma string contendo **apenas os `\n` que aquele token continha** — preservando
a numeração de linha para que `linhaDoPrimeiroMatch()` continue apontando a linha certa. Todas as
regras passam a rodar sobre esse código limpo, e `corposDeFuncao()` também recebe ele. Motivo
registrado no `137-VERIFICATION.md`: o verificador teve um falso positivo causado por um
comentário que continha o texto `$company->etapa = ` — e o comentário-guarda de 16 linhas em
`CompanyController::update()` é exatamente esse tipo de texto vivendo em `app/`.

**2. Ampliar o escopo da varredura.** Além de `base_path('app')`, varrer também
`base_path('database/migrations')`. Motivo: `DB::table('companies')->update([...])` é o idioma
natural de uma migration corretiva futura, e migration não passa pelo serviço. Medido: 0 falsos
positivos ali hoje. `tests/` continua **fora** do escopo, de propósito — este próprio plano e o
137-10 precisam escrever `companies.etapa` fora do serviço dentro de testes, para simular
violação e estado obsoleto.

**3. Regra A — atribuição direta de propriedade, sem o nome da variável.** Trocar
`/\$company->etapa\s*=(?!=)/` por `/->etapa\s*=(?!=)/`, avaliada sobre o **arquivo limpo
inteiro** (não por corpo de função — pega também atribuição em propriedade de classe ou fora
de método). É literalmente o item 1 da lista `missing:` do `137-VERIFICATION.md`: aceitar o
custo de um falso positivo eventual, tratado por exceção nomeada, porque hoje **não existe
nenhuma ocorrência legítima** de `->etapa =` em `app/` nem em `database/`. Declarar uma
constante de exceções nomeadas (uma lista de caminhos relativos) que nasce **vazia**, com
comentário explicando que acrescentar item ali é ato deliberado e revisado, nunca conveniência.

**4. Regra B — escrita em offset de array (bypass 2).** Novo padrão
`/\[\s*(['"])etapa\1\s*\]\s*=(?!=)/` avaliado **por corpo de função**, contando como violação
só quando o mesmo corpo satisfaz o detector de escrita em `Company` do item 6. Cobre
`$dados['etapa'] = ...;` seguido de `->update($dados)`.

**5. Regra C — chave literal de array.** Manter o padrão atual `/(['"])etapa\1\s*=>/` por corpo
de função, combinado com o mesmo detector do item 6. É esta regra que preserva a decisão de
137-06 contra o falso positivo de `OnboardingPasso::etapa`.

**6. Ampliar o detector `$escreveNaCompany`.** Passa a ser verdadeiro quando QUALQUER uma valer:
(a) o corpo casa `/Company::(create|updateOrCreate|firstOrCreate|forceCreate)\s*\(/`;
(b) o corpo casa `/DB::table\(\s*(['"])companies\1\s*\)/` — **novo**, fecha o bypass 3;
(c) o **arquivo** cita `/\bCompany\b/` **e** o corpo casa `/->(update|fill|forceFill|save)\s*\(/`
    — **novo**, remove o hardcode do nome `$company`. O guard de `Company` é avaliado no
    ARQUIVO e não no corpo de propósito: `corposDeFuncao()` extrai a partir da chave de
    abertura e **descarta a assinatura**, então um corpo como `$dados['etapa'] = 'em_operacao';
    $company->update($dados);` não contém o token `Company` em lugar nenhum e escaparia de um
    guard escopado ao corpo;
(d) o corpo casa `/Company::\w+\s*\(/` **e** casa `/->update\s*\(/` — regra atual, mantida
    (cobre o `Company::whereIn(...)->update([...])` de `carimbarBackfill()`).

**7. Mensagem de falha.** Cada violação continua nomeando `arquivo:linha` e o motivo, agora
dizendo QUAL regra disparou (A/B/C) para que quem receber o vermelho saiba se é violação real
ou falso positivo a virar exceção nomeada.

**8. Atualizar o bloco de comentário do Grupo 2** para descrever as três regras, o detector
ampliado, o motivo do guard de `Company` ser por arquivo, e as três quase-colisões nomeadas na
seção `<measured_facts>` deste plano.

**Não fazer:** não mexer no Grupo 1; não mexer em nenhum arquivo de `app/` ou `database/`
(este plano é só o gate); não relaxar nenhuma regra para "ficar verde" — se aparecer vermelho
na árvore atual, é bug do desenho ou violação real, e as duas saídas são investigar, nunca
afrouxar.
  </action>

  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/Phase137/EtapaPontoUnicoTest.php --colors=never</automated>
  </verify>

  <acceptance_criteria>
    - `phpunit tests/Feature/Phase137/EtapaPontoUnicoTest.php` termina `OK` — 0 ofensores na
      árvore atual, sem nenhuma entrada na lista de exceções nomeadas
    - `grep -c "database/migrations" tests/Feature/Phase137/EtapaPontoUnicoTest.php` ≥ 1
      (escopo ampliado presente)
    - `grep -vE '^\s*(//|\*|/\*)' tests/Feature/Phase137/EtapaPontoUnicoTest.php | grep -c 'T_DOC_COMMENT'` ≥ 1
      (neutralização de comentário presente em código, não só em comentário)
    - `grep -vE '^\s*(//|\*|/\*)' tests/Feature/Phase137/EtapaPontoUnicoTest.php | grep -c 'DB::table'` ≥ 1
      (detector do bypass 3 presente em código)
    - `grep -c '\$company->etapa' tests/Feature/Phase137/EtapaPontoUnicoTest.php` — a regra A
      **não** pode mais depender do nome literal; a única ocorrência aceitável de
      `$company->etapa` no arquivo é dentro de comentário explicativo
    - Suíte completa da fase verde: `phpunit tests/Unit/Phase137 tests/Feature/Phase137` = 46/46
  </acceptance_criteria>

  <done>O gate roda verde na árvore atual, varre `app/` e `database/migrations/`, ignora comentários, e as três regras + o detector ampliado estão implementados.</done>
</task>

<task type="auto">
  <name>Task 2: Provar o gate por injeção real das 3 sondas de bypass, e desfazer</name>

  <read_first>
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-VERIFICATION.md`
      seção "Achado: gate estático da ETAPA-03 é contornável" — traz o código EXATO das duas
      primeiras sondas que o verificador usou e o resultado que elas produziram (`OK`, ou seja,
      escaparam). A prova desta task é o espelho: as mesmas sondas agora precisam produzir
      FALHA.
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-06-SUMMARY.md`
      — o executor de 137-06 já usou exatamente esta técnica ("comprove desfazendo e
      refazendo") para o único caso que o gate cobria. Seguir o mesmo rito, inclusive a
      conferência de árvore limpa ao final.
    - `tests/Feature/Phase137/EtapaPontoUnicoTest.php` — já alterado pela Task 1.
  </read_first>

  <files>tests/Feature/Phase137/EtapaPontoUnicoTest.php</files>

  <action>
Provar o gate por injeção real, uma sonda por vez, **sempre removendo a anterior antes de criar
a próxima** (para que o vermelho seja atribuível a uma única causa):

**Sonda 1 — nome de variável diferente.** Criar um arquivo PHP temporário em
`app/Http/Controllers/` com um método que recebe uma `Company` numa variável chamada `$empresa`
e faz atribuição direta a `$empresa->etapa`, seguida de `save()`. Rodar o gate. Esperado:
FALHA nomeando esse arquivo e a linha da atribuição, com o motivo da regra A.

**Sonda 2 — array indireto.** Substituir por um método que monta `$dados` como array vazio,
grava a chave `'etapa'` por offset com um valor **literal em string** (deliberadamente **sem**
usar a constante `Company::ETAPA_*`, para que o token `Company` NÃO apareça no corpo — é
exatamente esse o caso que um guard escopado ao corpo deixaria passar), e chama
`->update($dados)` sobre a instância. Rodar o gate. Esperado: FALHA pela regra B.

**Sonda 3 — `DB::table('companies')`.** Substituir por um método que faz
`DB::table('companies')->where(...)->update([...])` com a chave `'etapa'`, copiando o idioma
literal de `DiagnoseCustId.php:206`. Rodar o gate. Esperado: FALHA pela regra C com o detector
`DB::table('companies')`.

**Sonda 4 — regressão da forma já coberta.** Repetir o caso original (`$company->etapa = ...`
com o nome literal) para garantir que a ampliação não perdeu o que já funcionava. Esperado:
FALHA.

**Sonda 5 — falso positivo de comentário.** Criar um arquivo cujo ÚNICO conteúdo relevante é um
comentário contendo o texto `$company->etapa = Company::ETAPA_EM_OPERACAO;`, sem nenhum código
de escrita. Rodar o gate. Esperado: **PASSA** — prova a neutralização de comentários.

Ao final: remover TODOS os arquivos-sonda, rodar `git status --short` e confirmar árvore limpa
(nenhum arquivo `??` em `app/`), e rodar a suíte da fase inteira de novo.

Registrar o resultado das 5 sondas — cada uma com o comando rodado e o veredito — num bloco de
comentário no topo do Grupo 2 do próprio `EtapaPontoUnicoTest.php`, datado, e também no
`137-08-SUMMARY.md`. É a documentação que o item 3 da lista `missing:` do `137-VERIFICATION.md`
cobra explicitamente.

**Não fazer:** não deixar nenhum arquivo-sonda commitado; não usar `git add -A` nem `git add .`
(árvore compartilhada por mais de uma sessão — `CLAUDE.md`); commitar só por caminho explícito.
  </action>

  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Unit/Phase137 tests/Feature/Phase137 --colors=never</automated>
  </verify>

  <acceptance_criteria>
    - Sondas 1, 2, 3 e 4 produziram, cada uma isoladamente, FALHA do gate nomeando o arquivo-sonda
      e a linha — evidência colada no `137-08-SUMMARY.md` (saída real do phpunit, não paráfrase)
    - Sonda 5 (comentário puro) produziu `OK` — nenhum falso positivo por comentário
    - `git status --short` não lista nenhum arquivo novo em `app/` depois da limpeza
    - `phpunit tests/Unit/Phase137 tests/Feature/Phase137` = 46/46 verde com a árvore limpa
    - Baseline `/companies` intacta: `phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php` = 24/24
    - Bloco de comentário datado no Grupo 2 do teste lista as 5 sondas e seus vereditos
  </acceptance_criteria>

  <done>As três formas de bypass do `137-VERIFICATION.md` estão provadas fechadas por injeção real, o falso positivo de comentário está provado ausente, e a árvore está limpa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| desenvolvedor futuro → `companies.etapa` | Código novo das Fases 138-142 escrito sob pressão, em controllers e jobs que hoje não existem, atravessa aqui |
| migration corretiva → `companies` | Migration de fix roda com privilégio total e não passa por serviço nenhum |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-137-21 | Tampering | `companies.etapa` gravado por variável de nome divergente (`$empresa`, `$c`, `$emp`) | mitigate | Regra A ampliada para `->etapa\s*=(?!=)` sem nome de variável, arquivo inteiro, exceções nomeadas vazias |
| T-137-22 | Tampering | `companies.etapa` gravado por array montado indiretamente | mitigate | Regra B nova (`['etapa'] =` por offset) combinada ao detector de escrita em `Company` |
| T-137-23 | Tampering | `companies.etapa` gravado por `DB::table('companies')->update()`, fora de Eloquent e sem histórico | mitigate | Detector ampliado com `DB::table('companies')`; escopo da varredura estendido a `database/migrations/` |
| T-137-24 | Repudiation | Escrita fora do serviço não gera linha em `company_etapa_transicoes`, quebrando o insumo de SLA da Fase 143 | mitigate | Mesmas regras acima — o gate é a única barreira automática, já que a coluna está em `$fillable` |
| T-137-25 | Denial of Service (do próprio gate) | Falso positivo em massa faz a próxima sessão afrouxar a regra para destravar a suíte | accept | Quase-colisões nomeadas no docblock + medição de 0 falsos positivos hoje; a saída documentada é exceção nomeada, nunca afrouxamento |

Sem instalação de pacote npm/composer neste plano — `T-137-SC` (legitimidade de pacote) não se
aplica e nenhum checkpoint de instalação é criado.
</threat_model>

<verification>
- `phpunit tests/Unit/Phase137 tests/Feature/Phase137 --colors=never` → 46/46 verde
- `phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php --colors=never` → 24/24 verde
- `git status --short` sem arquivo-sonda residual
- ⚠️ NÃO rodar `php artisan test` nem `--testsuite=Feature` (não termina neste worktree)
</verification>

<success_criteria>
As três formas de bypass documentadas no `137-VERIFICATION.md` e no `137-REVIEW.md` (CR-01)
falham o gate quando injetadas, a árvore atual permanece verde sem nenhuma exceção nomeada, e
comentário deixou de ser fonte de falso positivo.
</success_criteria>

<output>
Criar `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-08-SUMMARY.md`.

Commit por caminho explícito (nunca `git add -A` / `git add .`), mensagem em pt-BR:
`test(137-08): fecha os 3 bypasses do gate de ponto unico de escrita de companies.etapa`
</output>
