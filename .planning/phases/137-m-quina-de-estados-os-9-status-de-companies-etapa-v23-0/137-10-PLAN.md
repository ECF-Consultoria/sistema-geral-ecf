---
phase: 137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0
plan: 10
type: execute
gap_closure: true
wave: 6
depends_on: ["137-08", "137-09"]
files_modified:
  - app/Services/FluxoEntrada/EtapaTransicaoService.php
  - tests/Unit/Phase137/EtapaTransicaoServiceTest.php
  - app/Console/Commands/EtapaBackfill.php
  - tests/Feature/Phase137/EtapaBackfillTest.php
  - .planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-01-PLAN.md
autonomous: true
requirements: [ETAPA-02, ETAPA-03, ETAPA-06]

must_haves:
  truths:
    - "`transicionar()` decide sobre o estado do banco travado dentro da transação, nunca sobre o `Company` que o chamador tinha em memória"
    - "Uma transição decidida com estado obsoleto é RECUSADA com o requisito nomeado, em vez de gravar"
    - "Duas empresas do balde 1 com o MESMO `name` recebem ambas `etapa = em_operacao` no backfill"
    - "O frontmatter de `137-01-PLAN.md` não reivindica requirement que aquele plano não entregou, e os 6 IDs seguem cobertos"
  artifacts:
    - path: "app/Services/FluxoEntrada/EtapaTransicaoService.php"
      provides: "Transição serializada por lockForUpdate() dentro da transação, seguindo o precedente do projeto"
      contains: "lockForUpdate"
    - path: "app/Console/Commands/EtapaBackfill.php"
      provides: "Balde 1 chaveado por id, imune a colisão de nome"
  key_links:
    - from: "app/Services/FluxoEntrada/EtapaTransicaoService.php"
      to: "companies (linha travada)"
      via: "Company::whereKey(...)->lockForUpdate() dentro de DB::transaction()"
      pattern: "lockForUpdate"
---

<objective>
Fechar três achados de WARNING do `137-REVIEW.md` mais um defeito de frontmatter, todos no
caminho de produção da máquina de estados e nenhum deles com colisão de arquivo entre si:

- **G4 / WR-01 (TOCTOU):** `EtapaTransicaoService::transicionar()` avalia `podeTransicionar()`
  sobre o `Company` em memória **antes** de `DB::transaction()` abrir, e a closure reusa o mesmo
  objeto sem `lockForUpdate()` nem re-leitura. Duas chamadas concorrentes para a mesma empresa
  podem ler a mesma etapa "antiga", ambas passar na régua e ambas gravar. Latente hoje (o
  serviço não tem chamador de produção, de propósito), mas a Fase 138 pluga isto num webhook —
  a superfície mais sujeita a reentrega e retry concorrente que existe.
- **G5 / WR-02 (chave não única):** `EtapaBackfill` usa `pluck('id', 'name')` e `companies.name`
  não tem `unique()`. Duas empresas do balde 1 com o mesmo nome colapsam numa só e a primeira
  **desaparece em silêncio** do lote. Localmente só **1 de 180** empresas cai no balde 1 —
  produção tem ~500, então a probabilidade de colisão lá é literalmente não testada.
- **G6 (frontmatter):** `137-01-PLAN.md` declara `requirements: [ETAPA-01, ETAPA-02]`, mas
  aquele plano só destravou o ambiente de teste e não tocou `app/` nem `database/`.

Purpose: o serviço e o comando são o caminho que as Fases 138-142 vão usar; corrigir agora é
barato, corrigir depois do primeiro chamador de produção não é.
Output: transição serializada, backfill imune a homônimo, traceability honesta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-REVIEW.md
@.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-CONTEXT.md

> ⚠️ **Diretório de trabalho:** `C:\xampp\htdocs\ecf_fluxo_entrada`. Conferir com `pwd`.
> ⚠️ **PHP:** `C:\xampp\php\php.exe`. **Nunca** `php artisan test` nem `--testsuite=Feature`.
</context>

<measured_facts>
| Fato | Valor medido |
|---|---|
| `lockForUpdate()` já usado no projeto | `app/Services/Desempenho/CompanyScoreSnapshotWriter.php:68` e `app/Http/Controllers/DesempenhoMetricasManuaisController.php:180` |
| Efeito de `lockForUpdate()` no SQLite dos testes | **no-op** — o docblock D-122-08 de `CompanyScoreSnapshotWriter` diz isso com todas as letras: "Em SQLite (testes) é no-op; em MariaDB (produção) é o que faz a trava valer sob concorrência" |
| Empresas em `EtapaTransicaoServiceTest` | todas persistidas via `Company::factory()->create()` — nenhum `new Company(...)` não salvo, então a re-leitura por chave é sempre resolvível |
| `companies.name` tem `unique()`? | **Não** — `create_companies_table` declara só `string('name')`; confirmado também por `SHOW CREATE TABLE` |
| Colisão de nome na base local hoje | 0 em 180 empresas — por isso o defeito é invisível localmente |
| Escopo do gate estático de 137-08 | `app/` + `database/migrations/`. **`tests/` está FORA, de propósito** — este plano precisa escrever `companies.etapa` fora do serviço dentro de um teste, para simular estado obsoleto |
| Requirements dos demais planos | 137-02 `[ETAPA-01]` · 137-05 `[ETAPA-02]` — zerar 137-01 **não** deixa nenhum dos 6 IDs órfão |
</measured_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serializar transicionar() — decidir sobre a linha travada, não sobre o objeto em memória (G4)</name>

  <read_first>
    - `app/Services/FluxoEntrada/EtapaTransicaoService.php` — INTEIRO. Prestar atenção em três
      contratos que **não** podem mudar: `podeTransicionar()` continua público e **puro** (D-12
      — a mesma régua alimenta o botão desabilitado da UI em fases futuras); a forma do array de
      retorno de `transicionar()` (`status`, `de`, `para`, `requisito_faltante`, `erro`); e o
      comentário do Pitfall 6 explicando por que o `update()` grava **só** `etapa` (o
      `CompanyGatilhoContratoObserver` reage a outros campos).
    - `app/Services/Desempenho/CompanyScoreSnapshotWriter.php` linhas 19-75 — o precedente do
      projeto para "leitura que decide dentro da mesma transação da escrita", incluindo o
      docblock D-122-08 que registra que em SQLite o lock é no-op. Seguir esta forma, não
      inventar outra.
    - `app/Http/Controllers/DesempenhoMetricasManuaisController.php` linhas 159-190 — o segundo
      precedente, com o mesmo padrão aplicado a `updateOrCreate` de célula.
    - `tests/Unit/Phase137/EtapaTransicaoServiceTest.php` — os testes que já cobrem
      `transicionar()` (linhas 105-165) e que precisam continuar verdes sem alteração de
      semântica.
  </read_first>

  <files>app/Services/FluxoEntrada/EtapaTransicaoService.php, tests/Unit/Phase137/EtapaTransicaoServiceTest.php</files>

  <behavior>
    - Empresa com `etapa` `NULL` em memória, mas já em `aguardando_administrativo` no banco;
      `transicionar(..., 'aguardando_administrativo')` → RECUSADO com a mensagem que nomeia a
      etapa atual ("a empresa já está na etapa ..."), e **nenhuma** linha nova em
      `company_etapa_transicoes`
    - Mesmo cenário, destino `administrativo_andamento` → transiciona, e o `de` retornado e o
      `etapa_anterior` gravado são `aguardando_administrativo` (o estado do BANCO), nunca `NULL`
    - Depois de uma transição bem-sucedida, o objeto `Company` que o chamador passou reflete a
      etapa nova em memória — nenhum objeto obsoleto sobra
    - Empresa apagada entre a chamada e o lock → `status` `erro`, sem exceção vazando
    - Os testes existentes de `transicionar()` (recusa nomeada, salto 2→4, retrocesso sem
      motivo, retrocesso com motivo, ator registrado) continuam passando sem alteração de
      expectativa
  </behavior>

  <action>
Alterar **apenas** `transicionar()`. `podeTransicionar()` e `carimbarBackfill()` ficam intactos.

**1. Mover a decisão para dentro da transação.** Abrir `DB::transaction()` como primeiro passo
(ainda dentro do `try`/`catch (\Throwable)` já existente). Dentro da closure, reler a empresa
por chave primária com `lockForUpdate()`. Se a re-leitura devolver `null` (empresa removida
entre a chamada e o lock), devolver o resultado com `status` `erro` e mensagem nomeando o id —
não deixar exceção vazar.

**2. Reavaliar sobre a linha travada.** Chamar `podeTransicionar()` passando a instância
**travada**, não a recebida. Manter a ordem das regras: recusa da régua primeiro, depois a
exigência de motivo do retrocesso (D-15). As duas recusas retornam de dentro da closure — a
transação fecha sem nenhuma escrita, que é o comportamento correto (nada a desfazer).

**3. Escrever sobre a instância travada.** `etapa_anterior` do histórico e o campo `de` do
retorno passam a vir da instância travada. O `update()` continua gravando **só** `etapa` — o
comentário do Pitfall 6 continua válido e não pode ser removido.

**4. Sincronizar o objeto do chamador.** Depois da transição bem-sucedida, atualizar a instância
`Company` que o chamador passou para refletir a etapa nova (as Fases 138-142 vão usar o retorno
e o objeto logo em seguida; deixar um objeto obsoleto trocaria um bug por outro).

**5. Documentar no docblock de `transicionar()`** por que o lock existe: citar WR-01 do
`137-REVIEW.md`, o cenário concreto (webhook Clicksign reentregue por timeout na Fase 138,
duplo-clique em FINALIZAR na Fase 139), os dois precedentes do projeto pelo caminho de arquivo,
e — explicitamente — que `lockForUpdate()` é **no-op no SQLite dos testes** e só vale em
MariaDB, de modo que o que a suíte prova é a **re-leitura** do estado, não a serialização em si.
Sem essa frase, a próxima sessão vai ler o teste verde como prova de concorrência, que ele não é.

**6. Acrescentar os testes** do bloco `<behavior>` em `EtapaTransicaoServiceTest.php`. Para
simular estado obsoleto, o teste escreve `companies.etapa` fora do serviço via
`DB::table('companies')->where('id', ...)->update([...])` — deliberado, e a ser explicado num
comentário no próprio teste: é a única forma de produzir divergência entre memória e banco, e
`tests/` está fora do escopo do gate estático de 137-08 exatamente para permitir isto.

**Não fazer:** não mudar a assinatura nem a pureza de `podeTransicionar()`; não mudar a forma do
array de retorno; não mexer em `carimbarBackfill()`; não introduzir retry/backoff (não é o
defeito); não usar `git add -A`.
  </action>

  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Unit/Phase137 tests/Feature/Phase137 --colors=never</automated>
  </verify>

  <acceptance_criteria>
    - `grep -vE '^\s*(\*|//|/\*)' app/Services/FluxoEntrada/EtapaTransicaoService.php | grep -c 'lockForUpdate'` = 1
      (o lock existe em CÓDIGO, não só em comentário)
    - `grep -vE '^\s*(\*|//|/\*)' app/Services/FluxoEntrada/EtapaTransicaoService.php | grep -c 'podeTransicionar('`
      confirma que a única chamada dentro de `transicionar()` recebe a instância travada
      (conferir por leitura do trecho, além do grep)
    - Novos testes de estado obsoleto passam; provado dirigido: revertendo temporariamente a
      decisão para fora da transação, os novos testes FALHAM — evidência colada no SUMMARY, e a
      reversão desfeita com `git status --short` limpo
    - Os 5 testes pré-existentes de `transicionar()` seguem verdes **sem alteração de
      expectativa** (o `git diff` do arquivo de teste só mostra linhas ADICIONADAS nesse bloco)
    - Gate estático de 137-08 continua verde: `phpunit tests/Feature/Phase137/EtapaPontoUnicoTest.php` = `OK`
  </acceptance_criteria>

  <done>`transicionar()` decide e escreve sobre a mesma linha travada dentro de uma transação, seguindo o precedente do projeto, e a re-leitura está provada por teste.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chavear o balde 1 do backfill por id, não por nome (G5)</name>

  <read_first>
    - `app/Console/Commands/EtapaBackfill.php` — INTEIRO. Prestar atenção no docblock (o
      contrato dos dois baldes de D-04/D-05 não muda), no `pluck('id', 'name')` da linha 77, e
      no `map(fn ($id, $name) => [$id, $name])` da amostra do dry-run, que só funciona porque a
      coleção está chaveada por nome — os dois mudam juntos.
    - `tests/Feature/Phase137/EtapaBackfillTest.php` — os 9 testes que já cobrem os dois baldes,
      o dry-run e a preservação do payload de `/companies`; o novo teste entra ao lado, sem
      alterar os existentes.
    - `app/Models/Company.php` — as relações `analistaPerformance()` e `estrategistaPerformance()`
      usadas pelo balde 1, e o papel `consultor` na pivot (Pitfall 5: reescrever a query à mão
      filtrando pelo slug do cargo devolveria zero linhas em silêncio).
    - `app/Services/FluxoEntrada/EtapaTransicaoService.php::carimbarBackfill()` — o consumidor
      da lista de ids; confirmar que ele espera uma lista simples de ids.
  </read_first>

  <files>app/Console/Commands/EtapaBackfill.php, tests/Feature/Phase137/EtapaBackfillTest.php</files>

  <behavior>
    - Duas empresas do balde 1 com `name` IDÊNTICO: `etapa:backfill --apply` carimba **as duas**
      com `em_operacao` (hoje uma some em silêncio)
    - O contador impresso e o retorno de `carimbarBackfill()` refletem 2, não 1
    - O dry-run continua imprimindo a tabela `id`/`name` com no máximo 20 linhas e o aviso de
      amostra truncada quando o balde é maior
    - Os dois baldes de D-04/D-05 continuam valendo: quem não satisfaz o balde 1 permanece com
      `etapa` `NULL`, e nenhuma etapa intermediária é carimbada
  </behavior>

  <action>
**1. Trocar a chave da coleção.** O balde 1 passa a ser coletado como lista simples de ids
(`pluck('id')`), eliminando `name` como chave de array. A query em si — o `where` com
`whereHas('analistaPerformance')->orWhereHas('estrategistaPerformance')` — **não muda**: ela é
idêntica ao cálculo de `em_operacao` de `CompanyController::index()` e o `137-VERIFICATION.md`
já confirmou ausência de drift.

**2. Buscar a amostra do dry-run separadamente**, por `whereIn('id', <os 20 primeiros ids>)`
selecionando `id` e `name`, e montar a tabela a partir dela. A amostra passa a ser só
apresentação; a decisão de quem é carimbado deixa de depender dela.

**3. Ajustar a passagem para `carimbarBackfill()`** para a lista de ids já achatada.

**4. Comentar o porquê no código**, curto e específico: `companies.name` não tem `unique()`, e
`pluck('id', 'name')` fazia a segunda empresa homônima sobrescrever a primeira na coleção,
descartando-a do lote em silêncio; citar WR-02 do `137-REVIEW.md` e o fato de que localmente só
1 de 180 empresas cai no balde 1, enquanto produção tem ~500 — o defeito era invisível aqui por
volume, não por desenho.

**5. Acrescentar o teste** do bloco `<behavior>` em `EtapaBackfillTest.php`, criando duas
empresas com `name` idêntico, ambas qualificadas para o balde 1 (usar as mesmas relações que os
testes existentes já montam), e afirmando que as DUAS terminam com `etapa = em_operacao`.

**Não fazer:** não acrescentar `--limite` nem `--servico` (D-04 travado: os dois baldes não
admitem execução parcial); não deduzir etapa intermediária (D-05); não rodar `etapa:backfill
--apply` contra o MariaDB local nesta task — o backfill já foi executado e conferido em 137-05,
e re-executar não acrescenta prova nenhuma; não usar `git add -A`.
  </action>

  <verify>
    <automated>C:\xampp\php\php.exe vendor/bin/phpunit tests/Feature/Phase137/EtapaBackfillTest.php --colors=never</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "pluck('id', 'name')" app/Console/Commands/EtapaBackfill.php` = 0
    - `grep -vE '^\s*(\*|//|/\*)' app/Console/Commands/EtapaBackfill.php | grep -c "pluck('id')"` = 1
    - Novo teste de nome duplicado passa; provado dirigido: revertendo temporariamente para
      `pluck('id', 'name')`, o novo teste FALHA acusando que só 1 das 2 empresas foi carimbada —
      evidência colada no SUMMARY, reversão desfeita
    - Os 9 testes pré-existentes de `EtapaBackfillTest` seguem verdes
    - Gate estático de 137-08 continua verde (o comando não passou a gravar `etapa` direto):
      `phpunit tests/Feature/Phase137/EtapaPontoUnicoTest.php` = `OK`
  </acceptance_criteria>

  <done>O balde 1 é chaveado por id e uma colisão de nome deixa de descartar empresa em silêncio, provado por teste que já foi visto falhando contra o código antigo.</done>
</task>

<task type="auto">
  <name>Task 3: Corrigir o frontmatter de 137-01-PLAN.md (G6)</name>

  <read_first>
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-01-PLAN.md`
      — frontmatter (linhas 1-30) e `<objective>`, para confirmar por leitura que o plano só
      destravou o ambiente de teste e registrou a baseline.
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-01-SUMMARY.md`
      — a confirmação de que nenhum arquivo de `app/` ou `database/` foi tocado.
    - `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-VERIFICATION.md`
      seção "Nota sobre defeito de frontmatter, não de entrega" — o achado, com o veredito de
      que ETAPA-01 foi entregue por 137-02 e ETAPA-02 por 137-05.
    - `.planning/REQUIREMENTS-v23.md` seção "Traceability" — a tabela que mapeia ETAPA-01..06
      para a Fase 137 (⚠️ é este arquivo, **não** `.planning/REQUIREMENTS.md`, que pertence à
      milestone v17 antiga).
  </read_first>

  <files>.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-01-PLAN.md</files>

  <action>
Trocar `requirements: [ETAPA-01, ETAPA-02]` por `requirements: []` no frontmatter de
`137-01-PLAN.md`, e acrescentar logo abaixo um comentário YAML de uma linha registrando o
motivo: o plano 137-01 destravou o ambiente de teste Inertia e registrou a baseline
pré-migration, sem tocar `app/` nem `database/`; ETAPA-01 é entregue por 137-02 e ETAPA-02 por
137-05 (achado do `137-VERIFICATION.md`).

Nada mais do arquivo muda — nem `must_haves`, nem tasks, nem o corpo. Este é um plano já
executado; o registro histórico do que foi feito permanece intacto, só a reivindicação de
requirement que ele não entregou é removida.

**Não fazer:** não mexer em nenhum outro `137-0*-PLAN.md`; não mexer no `ROADMAP.md` nem no
`REQUIREMENTS-v23.md` (a cobertura dos 6 IDs continua completa sem eles); não usar `git add -A`.
  </action>

  <verify>
    <automated>grep -c "^requirements: \[\]" .planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-01-PLAN.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "^requirements:" .planning/phases/137-*/137-01-PLAN.md` devolve `requirements: []`
    - `grep -h "^requirements:" .planning/phases/137-*/137-0*-PLAN.md .planning/phases/137-*/137-1*-PLAN.md`
      — os seis IDs `ETAPA-01`..`ETAPA-06` seguem aparecendo em pelo menos um plano cada
      (ETAPA-01 em 137-02; ETAPA-02 em 137-05 e 137-10; ETAPA-03 em 137-03/06/08/09/10;
      ETAPA-04 em 137-04; ETAPA-05 em 137-07; ETAPA-06 em 137-03/09/10)
    - `git diff --numstat` de `137-01-PLAN.md` mostra alteração de no máximo 2 linhas
  </acceptance_criteria>

  <done>A traceability do `REQUIREMENTS-v23.md` deixa de conter reivindicação falsa, sem nenhum dos seis IDs ficar órfão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| webhook externo (Fase 138) → `transicionar()` | Reentrega e retry concorrente são o comportamento normal do emissor, não a exceção |
| operação de produção → `etapa:backfill --apply` | Roda uma vez contra ~500 empresas reais; erro silencioso não tem segunda chance |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-137-31 | Tampering | `EtapaTransicaoService::transicionar()` — lost update por decisão sobre estado obsoleto | mitigate | Re-leitura com `lockForUpdate()` dentro da transação, seguindo `CompanyScoreSnapshotWriter` (D-122-08) |
| T-137-32 | Repudiation | Transição concorrente grava par `de/para` que a tabela `TRANSICOES_PERMITIDAS` nunca autorizaria se lida em série, poluindo o insumo de SLA da Fase 143 | mitigate | Mesma correção — `etapa_anterior` do histórico passa a vir da linha travada |
| T-137-33 | Information Disclosure | Ausência de dado: empresa descartada por homônimo fica com `etapa` `NULL` e some do filtro `?etapa=` sem erro | mitigate | Balde 1 chaveado por `id`; teste com nome duplicado |
| T-137-34 | Denial of Service | `lockForUpdate()` segurando linha de `companies` sob carga de webhook | accept | Transação curta (um `update` + um `insert`), mesmo perfil dos dois precedentes já em produção |
| T-137-35 | Repudiation | Traceability declarando entrega que não houve, mascarando requirement órfão numa auditoria futura | mitigate | `requirements: []` em 137-01 + conferência de que os 6 IDs seguem cobertos |

Sem instalação de pacote npm/composer neste plano — `T-137-SC` não se aplica.
</threat_model>

<verification>
- `phpunit tests/Unit/Phase137 tests/Feature/Phase137 --colors=never` verde
- `phpunit tests/Feature/Phase37CompaniesPerformanceFilterTest.php tests/Feature/V16/CompanyControllerResponsavelPerformanceTest.php --colors=never` = 24/24
- `phpunit tests/Feature/Phase137/EtapaPontoUnicoTest.php` = `OK` (o gate de 137-08 não ficou vermelho com as edições em `app/`)
- ⚠️ NÃO rodar `php artisan test` nem `--testsuite=Feature`
- ⚠️ NÃO rodar `etapa:backfill --apply` contra o MariaDB local
</verification>

<success_criteria>
A transição decide sobre a linha travada e não sobre memória; o backfill não descarta mais
empresa por homônimo; e o frontmatter de 137-01 não reivindica entrega que não fez. Os três
comportamentos estão provados por teste que já foi visto falhando contra o código antigo.
</success_criteria>

<output>
Criar `.planning/phases/137-m-quina-de-estados-os-9-status-de-companies-etapa-v23-0/137-10-SUMMARY.md`.

Commits por caminho explícito (nunca `git add -A` / `git add .`), mensagens em pt-BR:
- `fix(137-10): transicionar() decide sobre a linha travada dentro da transacao (WR-01)`
- `fix(137-10): balde 1 do backfill chaveado por id, imune a nome duplicado (WR-02)`
- `docs(137-10): 137-01 nao reivindica mais ETAPA-01/ETAPA-02`
</output>
