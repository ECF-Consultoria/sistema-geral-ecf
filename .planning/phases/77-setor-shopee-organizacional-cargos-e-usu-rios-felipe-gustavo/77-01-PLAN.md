---
phase: 77-setor-shopee-organizacional-cargos-e-usu-rios-felipe-gustavo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_14_120000_seed_setor_shopee_e_usuarios.php
  - tests/Feature/V16/SetorShopeeSeedTest.php
autonomous: true
requirements: [DEC-77-1, DEC-77-2, DEC-77-3, DEC-77-4]

must_haves:
  truths:
    - "Existe um Setor organizacional 'Shopee' (slug shopee, active, is_system=false) na tabela setores"
    - "O setor Shopee tem os cargos analista e estrategista (por setor_id+slug)"
    - "A permission shopee.empresas está vinculada ao setor Shopee em setor_permissoes — e nenhuma outra"
    - "Felipe (consultor.02@ecfconsultoria.com.br), se existir, é estrategista no setor Shopee e líder do setor Shopee (isLider=true)"
    - "Gustavo (suporte.11@ecfconsultoria.com.br), se existir, é analista no setor Shopee (e analista no Performance quando esse setor/cargo existir)"
    - "A migration roda sem erro mesmo quando os emails não existem no banco (skip + Log::warning)"
    - "Rodar a migration 2x não duplica setor, cargos, permissão nem vínculos"
    - "down() remove o que a migration inseriu sem apagar a membership Performance pré-existente de Gustavo"
  artifacts:
    - path: "database/migrations/2026_07_14_120000_seed_setor_shopee_e_usuarios.php"
      provides: "Seed idempotente do Setor Shopee + cargos + permissão + wiring por email de Felipe/Gustavo"
      contains: "slug' => 'shopee'"
    - path: "tests/Feature/V16/SetorShopeeSeedTest.php"
      provides: "Prova dos 6 pontos de validação (setor/cargos/permissão/wiring/idempotência/skip/down)"
      contains: "namespace Tests\\\\Feature\\\\V16"
  key_links:
    - from: "database/migrations/2026_07_14_120000_seed_setor_shopee_e_usuarios.php"
      to: "setores/cargos/setor_permissoes/user_setores/setor_lideres"
      via: "DB::table inserts idempotentes (guardas ->value('id')/->exists())"
      pattern: "DB::table\\('setores'\\)"
    - from: "database/migrations/2026_07_14_120000_seed_setor_shopee_e_usuarios.php"
      to: "users por email (consultor.02 / suporte.11)"
      via: "resolução de user_id por email; ausente -> Log::warning + skip"
      pattern: "Log::warning"
    - from: "tests/Feature/V16/SetorShopeeSeedTest.php"
      to: "migration up()/down()"
      via: "require do arquivo + invocação manual após criar fixture users"
      pattern: "require .*seed_setor_shopee"
---

<objective>
Criar o **Setor organizacional "Shopee"** (RBAC — tabela `setores`, eixo distinto do `servico.setor='shopee'`) com cargos `analista`/`estrategista`, vincular a permission `shopee.empresas`, e configurar Felipe (estrategista + líder Shopee) e Gustavo (analista Shopee + analista Performance) — tudo via **1 migration idempotente**, espelhando `2026_05_25_100003_seed_setor_comercial_and_retro_migrate.php`.

Purpose: Dá base RBAC de setor/cargo/liderança que as Fases 78 (Comercial/aba Shopee) e 79/80 (NPS/bônus por serviço) consomem. Sem esta fase não há setor Shopee organizacional nem os papéis de Felipe/Gustavo.
Output: 1 migration idempotente + 1 suite Feature (`tests/Feature/V16/SetorShopeeSeedTest.php`) provando os 6 pontos de validação do CONTEXT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-setor-shopee-organizacional-cargos-e-usu-rios-felipe-gustavo/77-CONTEXT.md

# Molde EXATO a espelhar (DB::table idempotente: setor + setor_permissoes)
@database/migrations/2026_05_25_100003_seed_setor_comercial_and_retro_migrate.php

# Trait de fixture da milestone v16 (reusar padrão DB::table para users/cenário)
@tests/Feature/V16/CriaCenarioResponsaveis.php

# Exemplo de teste V16 que roda migration via RefreshDatabase (padrão de namespace/assert)
@tests/Feature/V16/MigrationCompanyUsersServicoTest.php

<interfaces>
<!-- Contratos extraídos do codebase. O executor usa estes diretamente — sem exploração. -->

Schema `setores` (2026_05_20_200001): id, nome(unique,100), slug(unique,120), descricao(nullable),
  active(bool default true), is_system(bool default false), created_by(FK users nullable), timestamps, softDeletes.

Schema `cargos` (2026_05_20_200003): id, setor_id(FK setores cascade), nome(100), slug(120),
  meta_publicacoes(nullable), ordem(smallint default 0), active(bool default true), timestamps.
  UNIQUE (setor_id, slug)  ← slugs analista/estrategista DUPLICAM os do Performance; ok por escopar em setor_id.

Schema `user_setores` (2026_05_20_200004): id, user_id(FK cascade), setor_id(FK setores cascade),
  cargo_id(FK cargos nullable), is_principal(bool default false), assigned_at(timestamp useCurrent), timestamps.
  UNIQUE (user_id, setor_id)  ← idempotência do wiring por par user+setor.

Schema `setor_lideres` (2026_05_20_200005): setor_id(FK), user_id(FK), assigned_by(FK nullable),
  assigned_at(useCurrent), timestamps. PRIMARY (setor_id, user_id). Estar aqui => AUTO_LIDERANCA.

Schema `setor_permissoes` (2026_05_20_200002): id, setor_id(FK cascade), permission_key(string 60),
  timestamps. UNIQUE (setor_id, permission_key).

App\Support\Permissions:
  SHOPEE_EMPRESAS = 'shopee.empresas'
  AUTO_LIDERANCA => concedido a quem está em setor_lideres (dashboard_setor, definir_metas, ver_membros, notificacoes.criar)
  slug do setor Performance = 'performance'

App\Models\User (relevante):
  setores(): belongsToMany(Setor, 'user_setores')->withPivot('cargo_id','is_principal','assigned_at')
  setoresLiderados(): belongsToMany(Setor, 'setor_lideres')
  isLider(): bool  — true se existe linha em setor_lideres
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration idempotente do Setor Shopee + cargos + permissão + wiring por email</name>
  <files>database/migrations/2026_07_14_120000_seed_setor_shopee_e_usuarios.php</files>
  <read_first>
    - database/migrations/2026_05_25_100003_seed_setor_comercial_and_retro_migrate.php (molde de idempotência DB::table)
    - Schemas em interfaces (setores/cargos/user_setores/setor_lideres/setor_permissoes)
    - app/Support/Permissions.php (SHOPEE_EMPRESAS, slug 'performance')
  </read_first>
  <action>
    Criar migration anônima (padrão `return new class extends Migration`), comentários em pt-BR, seções com dividers `// ─── ... ───`.

    up() — TODO idempotente e cross-driver (SQLite dos testes), usando DB::table puro com guardas `->value('id')` / `->exists()`:

    ETAPA 1 (DEC-77-1): resolver `$setorId` de setores where slug='shopee' via ->value('id'); se null, insertGetId com nome 'Shopee', slug 'shopee', descricao curta em pt-BR (ex.: 'Setor organizacional Shopee — gestão de ADS e NPS'), active=true, is_system=false, created_by=null, timestamps now().

    ETAPA 2 (DEC-77-2): para cada slug em ['analista','estrategista'], garantir cargo where setor_id=$setorId AND slug via ->value('id'); se null, insert com setor_id, nome ('Analista'/'Estrategista'), slug, ordem (estrategista=1, analista=0 — seniority), active=true, meta_publicacoes=null, timestamps. Guardar `$cargoEstrategistaId` e `$cargoAnalistaId`.

    ETAPA 3 (DEC-77-3): garantir setor_permissoes where setor_id=$setorId AND permission_key='shopee.empresas' via ->exists(); se não, insert. NÃO vincular nenhuma outra key.

    ETAPA 4 (DEC-77-4) — wiring por EMAIL, idempotente, skip+log se ausente. Helper interno (closure ou método privado) que resolve user_id por email via DB::table('users')->where('email',$email)->value('id'); se null → `Log::warning('[SetorShopee] usuário ausente, wiring pulado', ['email'=>$email])` e return.
      - Felipe (consultor.02@ecfconsultoria.com.br): upsert idempotente em user_setores por par (user_id,setor_id=Shopee): se não existir linha, insert cargo_id=$cargoEstrategistaId, is_principal=true, assigned_at now(); se existir, opcionalmente atualizar cargo_id para estrategista (mantendo idempotência). Depois garantir setor_lideres (setor_id=Shopee,user_id) via ->exists() antes de insert (assigned_by=null, assigned_at now()).
      - Gustavo (suporte.11@ecfconsultoria.com.br): garantir user_setores (setor Shopee, cargo analista, is_principal=false) idempotente por par. Em seguida, SOMENTE se existir setor slug='performance' E cargo analista nesse setor: garantir user_setores (setor Performance, cargo analista Performance) idempotente por par — pular se a linha já existir OU se setor/cargo Performance não existirem. is_principal de Gustavo: se ele já tiver algum setor principal, não mexer; se for novo e Performance existir, Performance=principal (Claude's Discretion do CONTEXT).

    down() (DEC-77-1) — reversível SEM destruir dados pré-existentes: deletar o setor Shopee (DB::table('setores')->where('slug','shopee')->delete()). Os FKs cascadeOnDelete de cargos/setor_permissoes/user_setores(shopee)/setor_lideres apagam automaticamente as linhas do Shopee. **NÃO** tocar em nenhuma linha de user_setores do setor Performance — a membership Performance de Gustavo permanece intacta. Comentar em pt-BR o porquê (não dá pra distinguir Performance pré-existente do que a migration inseriu; preservar é o comportamento seguro).

    Import: `use Illuminate\Support\Facades\DB;` e `use Illuminate\Support\Facades\Log;`. Usar a constante `\App\Support\Permissions::SHOPEE_EMPRESAS` em vez de string literal onde couber.
  </action>
  <verify>
    <automated>php artisan migrate:status | grep seed_setor_shopee_e_usuarios</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe com classe anônima Migration; up() e down() definidos.
    - up() cria setor 'shopee' + cargos analista/estrategista + setor_permissoes shopee.empresas de forma idempotente (guardas ->value/->exists).
    - Wiring por email presente com Log::warning no caminho de ausência.
    - down() deleta apenas o setor 'shopee' (cascade) e não referencia o setor Performance.
    - `php artisan migrate` roda sem erro em banco fresh (sem os emails → só loga).
  </acceptance_criteria>
  <done>Migration idempotente criada e aplicável em SQLite/MySQL; migrate + migrate:rollback não quebram.</done>
</task>

<task type="auto">
  <name>Task 2: Suite Feature SetorShopeeSeedTest — prova dos 6 pontos de validação</name>
  <files>tests/Feature/V16/SetorShopeeSeedTest.php</files>
  <read_first>
    - tests/Feature/V16/MigrationCompanyUsersServicoTest.php (padrão RefreshDatabase + namespace Tests\Feature\V16 + asserts)
    - tests/Feature/V16/CriaCenarioResponsaveis.php (padrão de fixture DB::table)
    - A migration criada na Task 1 (assinatura up()/down())
  </read_first>
  <action>
    Criar `tests/Feature/V16/SetorShopeeSeedTest.php`, namespace `Tests\Feature\V16`, `use RefreshDatabase;`, extends `Tests\TestCase`. Comentários pt-BR.

    Helper privado para invocar a migration manualmente (a suite roda com RefreshDatabase, então a migration JÁ rodou uma vez no setup — sem os emails, wiring pulado). Ex.: `private function rodarMigration(): void { $m = require database_path('migrations/2026_07_14_120000_seed_setor_shopee_e_usuarios.php'); $m->up(); }` (idempotente, seguro re-rodar). Análogo `rodarDown()` chamando `$m->down()`.

    Testes (cobrindo os 6 pontos da Validation Architecture):
    1. test_setor_e_cargos_criados: após RefreshDatabase, setores where slug='shopee' existe, active=1, is_system=0; cargos where setor_id=shopee tem exatamente slugs analista e estrategista.
    2. test_permissao_shopee_empresas_vinculada: setor_permissoes where setor_id=shopee tem permission_key 'shopee.empresas' e SOMENTE ela (assertSame count 1) — prova mitigação de elevação (T-77-01).
    3. test_wiring_felipe: criar User::factory()->create(['email'=>'consultor.02@ecfconsultoria.com.br']); rodarMigration(); assert user_setores (user Felipe, setor Shopee, cargo_id = cargo estrategista Shopee, is_principal=1) e setor_lideres (Shopee, Felipe). Recarregar o model e assertTrue($felipe->fresh()->isLider()).
    4. test_wiring_gustavo_shopee_e_performance: criar setor Performance (slug 'performance') + cargo analista nele via DB::table; criar User Gustavo (email suporte.11@...); rodarMigration(); assert user_setores Gustavo tem linha Shopee (cargo analista Shopee) E linha Performance (cargo analista Performance).
    5. test_gustavo_sem_performance_nao_cria_linha: sem criar setor Performance, criar Gustavo, rodarMigration(); assert existe user_setores Shopee para Gustavo mas NENHUMA linha Performance (o branch é pulado sem erro).
    6. test_skip_gracioso_sem_emails: sem criar nenhum user com esses emails, rodarMigration() de novo; assertSame(0, ...) linhas em user_setores para esses emails e nenhuma exceção lançada (o teste passa = sem erro).
    7. test_idempotencia: rodarMigration() 2x (após criar Felipe e Gustavo); assert exatamente 1 setor 'shopee', 2 cargos, 1 setor_permissoes, 1 user_setores por par, 1 setor_lideres (sem duplicar).
    8. test_down_preserva_performance: criar setor Performance + cargo analista + inserir user_setores Performance para Gustavo manualmente (simula membership PRÉ-EXISTENTE); criar Gustavo/Felipe; rodarMigration(); rodarDown(); assert setor 'shopee' removido (e cascade: cargos/permissoes/user_setores shopee/setor_lideres zerados) MAS a linha user_setores Performance de Gustavo AINDA existe — prova T-77-03 (não destrói dado pré-existente).

    Usar DB::table para asserts diretos (evita depender de models). Reusar padrão de fixture do trait quando conveniente (não é obrigatório usar o trait inteiro).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/V16/SetorShopeeSeedTest.php</automated>
  </verify>
  <acceptance_criteria>
    - 8 testes cobrindo: setor+cargos, permissão exclusiva, wiring Felipe (líder), wiring Gustavo (Shopee+Performance), Gustavo sem Performance, skip gracioso, idempotência (2x), down() preserva Performance.
    - Suite verde: `php artisan test tests/Feature/V16/SetorShopeeSeedTest.php` passa 100%.
    - Nenhum teste depende de emails reais existirem em prod — todos usam fixture users.
  </acceptance_criteria>
  <done>Suite Feature verde provando os 6 pontos de validação do CONTEXT + as 2 mitigações de elevação de privilégio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados seed da migration → RBAC (setores/cargos/permissões/liderança) | O que a migration insere vira permissão efetiva de usuários reais via `User::effectivePermissions` |
| email hardcoded → resolução de user_id | A migration concede papéis com base em correspondência exata de email |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Elevation of Privilege | setor_permissoes do setor Shopee | mitigate | Inserir SOMENTE `shopee.empresas`; teste `test_permissao_shopee_empresas_vinculada` assere count exato = 1 no setor. Setor Shopee não pode conceder mais que a aba Empresas. |
| T-77-02 | Elevation of Privilege | wiring user_setores/setor_lideres por email | mitigate | Matching por email EXATO (consultor.02 / suporte.11); apenas Felipe recebe estrategista+liderança, apenas Gustavo recebe analista. Testes de wiring assertam os cargos/liderança corretos por usuário; nenhum outro user é tocado. |
| T-77-03 | Tampering / Denial | down() destruindo membership Performance pré-existente de Gustavo | mitigate | down() deleta apenas o setor 'shopee' (cascade escopado ao Shopee); nunca referencia o setor Performance. `test_down_preserva_performance` prova que a linha Performance sobrevive. |
| T-77-04 | Denial of Service | migration falhando quando emails ausentes (banco de teste/prod parcial) | mitigate | Resolução por email com skip + `Log::warning`; `test_skip_gracioso_sem_emails` prova conclusão sem exceção. |
| T-77-SC | Supply Chain | instalação de pacotes | accept | Nenhum pacote npm/pip/composer novo — apenas migration + teste com libs já presentes. Sem superfície de supply-chain nesta fase. |
</threat_model>

<verification>
- `php artisan test tests/Feature/V16/SetorShopeeSeedTest.php` passa 100% (8 testes).
- `php artisan migrate` e `php artisan migrate:rollback --step=1` rodam sem erro (idempotência + reversibilidade).
- Grep: `grep -rn "shopee.empresas" database/migrations/2026_07_14_120000_seed_setor_shopee_e_usuarios.php` retorna a vinculação da permissão.
- Backend-only: nenhum `npm run build`, nenhum deploy nesta fase.
</verification>

<success_criteria>
- Setor 'shopee' (RBAC) existe com cargos analista/estrategista e permissão `shopee.empresas` (e só ela).
- Felipe (email) = estrategista + líder Shopee; Gustavo (email) = analista Shopee (+ Performance quando existir) — via migration idempotente que pula usuários ausentes sem erro.
- Todos os 6 pontos da Validation Architecture provados por teste verde.
- down() reverte sem apagar a membership Performance pré-existente de Gustavo.
</success_criteria>

<output>
Create `.planning/phases/77-setor-shopee-organizacional-cargos-e-usu-rios-felipe-gustavo/77-01-SUMMARY.md` when done
</output>
