---
phase: 128-gatilhos-do-fluxo-em-modo-observa-o-v22-0
plan: 06
type: execute
wave: 5
depends_on: ["128-05"]
files_modified:
  - .planning/phases/128-gatilhos-do-fluxo-em-modo-observa-o-v22-0/128-GATE.md
autonomous: false
requirements: [REDE-06, FLUXO-08]
user_setup:
  - service: clicksign-sandbox
    why: "Success Criteria 1 exige envelope REAL criado no sandbox pelo caminho do webhook — Http::fake() não serve"
    env_vars:
      - name: CLICKSIGN_API_TOKEN
        source: "Clicksign sandbox → Configurações → API (mesmas credenciais já usadas no gate da Fase 127)"

must_haves:
  truths:
    - "Existe um envelope REAL no sandbox Clicksign, criado pelo caminho do webhook, com id registrado"
    - "Está provado por medição — não por teste com fake — que a empresa continua sendo roteada ao operacional"
    - "Está provado que Polos não entra no fluxo em momento nenhum"
    - "A flag administrativo_bloqueio_ativo continua desligada ao fim da fase"
  artifacts:
    - path: ".planning/phases/128-gatilhos-do-fluxo-em-modo-observa-o-v22-0/128-GATE.md"
      provides: "as 3 medições da fase, com evidência bruta"
      contains: "Gate 1"
  key_links:
    - from: "128-GATE.md"
      to: "sandbox Clicksign"
      via: "id de envelope real registrado"
      pattern: "envelope"
---

<objective>
Fechar os 3 gates de `<gates_desta_fase>` do CONTEXT com **medição**, não com argumento. Este
plano existe porque cinco bugs desta milestone nasceram de presumir a forma de um payload em vez
de medi-la, e `Http::fake()` confirma alegremente um payload errado.

Propósito: Success Criteria 1 só é verdade com envelope real.
Saída: `128-GATE.md` com as 3 medições e a evidência bruta de cada uma.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/128-gatilhos-do-fluxo-em-modo-observa-o-v22-0/128-CONTEXT.md
@.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md
@.planning/phases/127-service-administrativo-de-contrato-orquestra-o-v22-0/127-GATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preparar o roteiro de medição e a suíte completa</name>
  <files>.planning/phases/128-gatilhos-do-fluxo-em-modo-observa-o-v22-0/128-GATE.md</files>
  <action>
Rodar a suíte da fase e as suítes de baseline, registrando o resultado por **nome de teste**
(a suíte completa tem ~117 falhas pré-existentes não relacionadas — comparar por contagem
enganaria):
- `C:\xampp\php\php.exe artisan test --filter=Phase128`
- `C:\xampp\php\php.exe artisan test --filter=Phase125`
- `C:\xampp\php\php.exe artisan test --filter=Phase126`
- `C:\xampp\php\php.exe artisan test --filter=Phase127`
- `C:\xampp\php\php.exe artisan test --filter=Phase124`

Criar `128-GATE.md` no diretório da fase, em pt-BR, com a estrutura dos gates da Fase 127
(`127-GATE.md` é o molde), deixando os 3 blocos preparados e o roteiro exato de cada medição:

**Gate 1 — envelope REAL no sandbox pelo caminho do webhook (SC1).** Roteiro: com credenciais de
sandbox no `.env`, disparar um POST real na rota do webhook HubSpot com payload de uma empresa
fictícia cujo serviço exige contrato e cujos dados mínimos estão completos; rodar o worker da fila
para processar `GerarContratoAssinaturaJob`; registrar no documento o **id do envelope criado no
sandbox**, o `ContratoAssinatura.id` local, o status resultante, e a resposta bruta relevante da
Clicksign (sem nenhum token/secret). Registrar também quantas chamadas HTTP foram consumidas da
janela medida de 20/min.

**Gate 2 — a empresa continua indo ao operacional na mesma hora (SC4).** A evidência automatizada
é `tests/Feature/Phase128/InvarianteRoteamentoTest.php` (plano 04). No documento: colar a saída da
execução dessa suíte e, na mesma rodada do Gate 1, registrar o `MlbEmpresa.id` criado para a
empresa fictícia e o intervalo entre `companies.created_at` e `mlb_empresas.created_at`.

**Gate 3 — Polos nunca entra no fluxo (SC0).** Roteiro: consultar no banco do ambiente de teste
`SELECT nome, exige_contrato FROM servicos` e colar a tabela inteira no documento; cadastrar uma
empresa fictícia só com Polos pelo caminho do Comercial e registrar
`ContratoAssinatura::where('company_id', X)->count()` = 0 e `MlbEmpresa` presente.
⚠️ Este gate também é a **primeira oportunidade real de medir a grafia de `Gestão de ADS Shopee`**
no catálogo — a pesquisa registrou confiança MÉDIA nesse nome porque o MariaDB local está fora do
ar. Se a grafia divergir do documentado na D9, registrar o valor real no documento; o
comportamento continua correto por causa do `default(true)`, mas o dado precisa virar fato medido.

Deixar cada bloco com campo `RESULTADO: [a preencher na medição]` — a Task 2 é quem preenche.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase128</automated>
  </verify>
  <done>`128-GATE.md` existe com os 3 roteiros e o resultado das suítes; suíte Phase128 verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Gate humano — autorização e execução das 3 medições</name>
  <what-built>
Os dois gatilhos administrativos rodando em paralelo ao roteamento: coluna `exige_contrato` com
Polos isento, `calcularUniversais()`, o orquestrador `GatilhoContratoAdministrativoService`
chamado nos dois pontos de entrada fora da transação, e a reavaliação automática por Observer.
Toda a cobertura automatizada está verde. Falta o que nenhum teste com `Http::fake()` pode provar.
  </what-built>
  <how-to-verify>
1. **Autorizar a medição contra o sandbox Clicksign.** Ela cria um envelope REAL e consome cota da
   janela de 20 req/min. Nenhum comando é rodado no VPS e nenhum deploy acontece sem sua
   autorização explícita nesta mesma resposta.
2. Confirmar qual ambiente usar (local com credenciais de sandbox, ou outro).
3. Executar os 3 roteiros descritos em `128-GATE.md` e preencher os campos `RESULTADO:`:
   - **Gate 1:** id do envelope no sandbox + `ContratoAssinatura.id` + status. Sem envelope real,
     o Success Criteria 1 **não** está atendido.
   - **Gate 2:** `MlbEmpresa.id` criada para a mesma empresa fictícia, provando que o roteamento
     não parou.
   - **Gate 3:** tabela `servicos` com a coluna `exige_contrato`, mostrando `Polos = false` e todos
     os demais `true`; e a empresa fictícia só-Polos com zero contratos.
4. Conferir ao final: `Configuracao::get('administrativo_bloqueio_ativo')` continua `'0'`. Esta
   fase **não liga** o bloqueio — isso é a Fase 133.
5. Limpar as empresas fictícias criadas na medição, ou registrar no documento quais ficaram e por quê.
  </how-to-verify>
  <resume-signal>Responder "aprovado" com os 3 resultados preenchidos, ou descrever o que divergiu.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| máquina de dev → API Clicksign sandbox | token real em `.env`; nenhuma credencial pode entrar no documento do gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-14 | Information Disclosure | `128-GATE.md` | mitigate | registrar id de envelope e status, **nunca** token, secret ou header de autorização |
| T-128-15 | Denial of Service | janela de 20 req/min do sandbox | mitigate | uma única empresa fictícia por rodada; contar e registrar as chamadas consumidas |
</threat_model>

<verification>
- `128-GATE.md` com os 3 `RESULTADO:` preenchidos por medição.
- `C:\xampp\php\php.exe artisan test --filter=Phase128` verde.
- Baseline Phase125+126+127 = 214 verdes, comparado por nome de teste.
</verification>

<success_criteria>
- Envelope real criado no sandbox pelo caminho do webhook, com id registrado.
- Invariante do roteamento medido, não só testado.
- Polos provadamente fora do fluxo, com o catálogo real conferido.
- Flag `administrativo_bloqueio_ativo` continua `'0'`.
</success_criteria>

<output>
Criar `.planning/phases/128-gatilhos-do-fluxo-em-modo-observa-o-v22-0/128-06-SUMMARY.md` ao terminar.
</output>
