---
phase: 25-analise-por-empresa-sellers-pagina-detail-360-graus-com-sell
plan: 01
type: execute
wave: 1
depends_on:
  - 22-01  # EcfDriveService::seller + sellerMetricasMensal + sellerMedalhas + sellerSignals
  - 23-01  # AlertaCard.jsx — visual base reutilizado em AlertasDoSeller (read-only) + link adaptado
  - 24-01  # BreakdownTabs.jsx tem as constantes CLUSTER_LABELS/FRETE_LABELS/PROGRAMA_LABELS a extrair
files_modified:
  # Backend novos
  - app/Http/Controllers/EmpresaAnaliseEcfController.php
  # Backend modificados
  - routes/web.php
  # Frontend novos
  - resources/js/lib/ecfDriveLabels.js
  - resources/js/Pages/EmpresaAnaliseEcf/Show.jsx
  - resources/js/Pages/EmpresaAnaliseEcf/components/EmpresaHeader.jsx
  - resources/js/Pages/EmpresaAnaliseEcf/components/EvolucaoEmpresaChart.jsx
  - resources/js/Pages/EmpresaAnaliseEcf/components/HistoricoMedalhas.jsx
  - resources/js/Pages/EmpresaAnaliseEcf/components/AlertasDoSeller.jsx
  # Frontend modificados (refator label-extract + link Alertas)
  - resources/js/Pages/PainelExecutivo/components/BreakdownTabs.jsx
  - resources/js/Pages/AlertasEstrategicos/components/AlertaCard.jsx
  # Testes novos
  - tests/Feature/Phase25/EmpresaAnaliseEcfControllerTest.php
autonomous: false  # W4 tem checkpoint humano blocking (smoke visual em prod, 3 empresas reais)
requirements:
  - ECF-25-01  # EmpresaAnaliseEcfController::show(Company) com try/catch global + 5 chamadas wrapper + 1 rota (auth, verified, role:admin,consultor,mentor)
  - ECF-25-02  # Auth fina dentro do controller: consultor/mentor só veem empresa da sua carteira (companies()->where('id', $company->id)->exists())
  - ECF-25-03  # Empty state "sem cust_id" (Shopee/Amazon) com mensagem pt-BR amigável a usuário não-tech (D-10 do CONTEXT)
  - ECF-25-04  # Empty state "404 no /sellers/{custId}" — flag naoEncontrada=true sem quebrar página (D-11 do CONTEXT)
  - ECF-25-05  # Refator label-extract: CLUSTER_LABELS/FRETE_LABELS/PROGRAMA_LABELS movidos de BreakdownTabs.jsx para lib/ecfDriveLabels.js (D-08 do CONTEXT) — zero regressão na Phase 24
  - ECF-25-06  # Página EmpresaAnaliseEcf/Show.jsx com 5 seções (Header / KPIs / Evolução 12m / Histórico Medalhas / Alertas) + flash de erro + estados condicionais
  - ECF-25-07  # AlertaCard.jsx (Phase 23): quando company resolvida, link do nome leva para /empresas/{id}/analise-ecf (não mais Dashboard) — D-07 do CONTEXT
  - ECF-25-08  # Testes Feature mínimo 7: 200 admin, 200 consultor-carteira, 403 consultor-fora, 403 publicador, 200 sem cust_id, 200 naoEncontrada, fallback erro ECF Drive

user_setup: []

must_haves:
  truths:
    - "Admin acessa `/empresas/{company}/analise-ecf` e vê ficha 360° com 5 seções: (1) Header com nome/cust_id/segmento/medalha atual/programa + link para Dashboard e Grant; (2) grid de KPI cards do mês atual (GMV, TSI, ACOS, TACOS, score Full, score PADS, margem) com delta MoM colorido; (3) gráfico linha 12 meses GMV (eixo esq) + Investimento ADS (eixo dir); (4) timeline horizontal de promoções/rebaixamentos de medalha; (5) lista read-only dos alertas signals daquele seller (limite 20)"
    - "Consultor/mentor acessa a página APENAS para empresas da sua carteira (`company_users` pivot). Acesso a empresa fora da carteira retorna 403 inline no controller (após middleware role:admin,consultor,mentor). Publicador/analista/gestor/lider recebem 403 do middleware. Guest é redirecionado para login (302)"
    - "Empresa SEM cust_id (sem `adman_account_id` e sem `ml_store_id` — típico Shopee/Amazon cadastradas só pelo Comercial): página carrega normalmente com flag `semCustId=true` exibindo card pt-BR amigável: 'Esta empresa não tem ID de lojista do Mercado Livre cadastrado. A análise ECF Drive não está disponível. Para habilitar, atualize o cadastro com `adman_account_id` ou `ml_store_id`.'"
    - "Empresa COM cust_id mas SEM dados no ECF Drive (cust_id de Shopee no Adman não existe na base ML — `/sellers/{custId}` retorna 404): página carrega com flag `naoEncontrada=true` exibindo card pt-BR: 'Esta empresa tem cust_id (X) mas não foi encontrada na base do parceiro ECF Drive. Pode ser uma conta que ainda não foi sincronizada ou que está fora do universo Mercado Livre.'"
    - "Quando ECF Drive cai (RuntimeException genérica em qualquer das 5 chamadas, EXCLUINDO 404 do /sellers/{custId} que é tratado como naoEncontrada): controller retorna props vazias (seller=null, metricas=[], medalhas=[], signals=[]) + erro pt-BR 'Não foi possível buscar dados da análise agora. Tente em alguns segundos.' — NUNCA quebra o pageload"
    - "Refator de labels (D-08 do CONTEXT): novo arquivo `resources/js/lib/ecfDriveLabels.js` exporta CLUSTER_LABELS, FRETE_LABELS, PROGRAMA_LABELS + helper `traduzirItem(tabKey, valor)`. BreakdownTabs.jsx (Phase 24) passa a importar do arquivo compartilhado — `grep -c \"const CLUSTER_LABELS\" BreakdownTabs.jsx` retorna 0 após refator. Testes Phase 24 continuam verdes (zero regressão)"
    - "AlertaCard.jsx (Phase 23): quando `company` resolvida vem do controller, nome da empresa vira link `<Link href={route('empresas.analise-ecf', company.id)}>` (Inertia preserveScroll) — antes apontava para Dashboard. Quando company NÃO resolvida (cliente externo), mantém span estático (sem link)"
    - "Suite Phase25/EmpresaAnaliseEcfControllerTest cobre: 200 admin (1), 200 consultor com empresa na carteira (1), 403 consultor fora da carteira (1), 403 publicador (1), 200 empresa sem cust_id com semCustId=true (1), 200 cust_id mas 404 ECF com naoEncontrada=true (1), 200 fallback genérico com erro pt-BR (1) — mínimo 7 testes verdes; Http::fake para mockar /sellers, /metricas/mensal, /medalhas, /signals"
    - "Após `npm run build` no fim do W2, sem warnings novos. AlertaCard.jsx + BreakdownTabs.jsx continuam funcionando — verificado por reload das páginas Alertas Estratégicos e Painel Executivo"
  artifacts:
    - path: "app/Http/Controllers/EmpresaAnaliseEcfController.php"
      provides: "Controller com show(Company) — auth fina inline + lookup cust_id via accessor + 5 chamadas wrapper sequenciais com try/catch + render Inertia"
      contains: "show, EcfDriveService, Company, seller, sellerMetricasMensal, sellerMedalhas, sellerSignals, cust_id, semCustId, naoEncontrada, erro, abort, Throwable, str_contains"
    - path: "routes/web.php"
      provides: "1 rota nova — GET /empresas/{company}/analise-ecf (empresas.analise-ecf) middleware auth+verified+role:admin,consultor,mentor"
      contains: "empresas.analise-ecf, EmpresaAnaliseEcfController, role:admin,consultor,mentor"
    - path: "resources/js/lib/ecfDriveLabels.js"
      provides: "Constantes compartilhadas (CLUSTER_LABELS/FRETE_LABELS/PROGRAMA_LABELS) + helper traduzirItem(tabKey, valor) — extraído de BreakdownTabs.jsx"
      contains: "CLUSTER_LABELS, FRETE_LABELS, PROGRAMA_LABELS, traduzirItem, export"
    - path: "resources/js/Pages/EmpresaAnaliseEcf/Show.jsx"
      provides: "Página principal com 5 seções + estados condicionais (semCustId / naoEncontrada / erro genérico)"
      contains: "EmpresaHeader, EvolucaoEmpresaChart, HistoricoMedalhas, AlertasDoSeller, AppLayout, semCustId, naoEncontrada, erro"
    - path: "resources/js/Pages/EmpresaAnaliseEcf/components/EmpresaHeader.jsx"
      provides: "Header com nome/cust_id/segmento/medalha atual + badge colorida por nível + programa + links para Dashboard e Grant"
      contains: "EmpresaHeader, PLATINUM, GOLD, SILVER, BRONZE, programa, cust_id, segmento, route, dashboard"
    - path: "resources/js/Pages/EmpresaAnaliseEcf/components/EvolucaoEmpresaChart.jsx"
      provides: "Recharts LineChart 12 meses com duplo eixo Y (GMV esquerdo amarelo + Investimento ADS direito azul) — reusa pattern do HistoricoChart Phase 24"
      contains: "ResponsiveContainer, LineChart, Line, XAxis, YAxis, yAxisId, fmtMesAno, recharts, gmv, investimento"
    - path: "resources/js/Pages/EmpresaAnaliseEcf/components/HistoricoMedalhas.jsx"
      provides: "Timeline horizontal de cards mostrando mudanças de NIVEL_SOLUCION ao longo do tempo (PLATINUM/GOLD/SILVER/BRONZE)"
      contains: "HistoricoMedalhas, PLATINUM, GOLD, SILVER, BRONZE, timeline, periodo"
    - path: "resources/js/Pages/EmpresaAnaliseEcf/components/AlertasDoSeller.jsx"
      provides: "Lista read-only dos alertas signals do seller (limite 20). Reusa visual do AlertaCard mas SEM botão ack"
      contains: "AlertasDoSeller, severity, eventType, payload, detectedAt, formatPayload"
    - path: "resources/js/Pages/PainelExecutivo/components/BreakdownTabs.jsx"
      provides: "Refator: importa CLUSTER_LABELS/FRETE_LABELS/PROGRAMA_LABELS + traduzirItem de @/lib/ecfDriveLabels"
      contains: "import.*ecfDriveLabels, CLUSTER_LABELS, FRETE_LABELS, PROGRAMA_LABELS, traduzirItem"
    - path: "resources/js/Pages/AlertasEstrategicos/components/AlertaCard.jsx"
      provides: "Atualização: nome da empresa vira <Link href={route('empresas.analise-ecf', company.id)}> quando company resolvida (D-07)"
      contains: "Link, empresas.analise-ecf, route, company"
    - path: "tests/Feature/Phase25/EmpresaAnaliseEcfControllerTest.php"
      provides: "7+ testes Http::fake validando roles, auth fina por carteira, empty states (semCustId/naoEncontrada) e fallback em erro genérico"
      contains: "test_index_admin_200, test_consultor_carteira_200, test_consultor_fora_403, test_publicador_403, test_sem_cust_id, test_nao_encontrada, test_fallback_erro, Http::fake"
  key_links:
    - from: "EmpresaAnaliseEcfController::show"
      to: "EcfDriveService::seller + sellerMetricasMensal + sellerMedalhas + sellerSignals"
      via: "lookup $custId = $company->cust_id; se null → semCustId=true (não chama wrapper); senão executa 5 chamadas em try/catch; catch específico p/ 404 em /sellers/{custId} → naoEncontrada=true; catch genérico Throwable → erro pt-BR"
      pattern: "seller\\(|sellerMetricasMensal|sellerMedalhas|sellerSignals|cust_id|semCustId|naoEncontrada|str_contains.*404"
    - from: "EmpresaAnaliseEcfController::show"
      to: "Auth fina por carteira"
      via: "after middleware role:admin,consultor,mentor, se !$user->isAdmin() && !$user->companies()->where('companies.id', $company->id)->exists() → abort(403); reusa pattern SugadorController"
      pattern: "isAdmin|companies\\(\\)->where|abort\\(403"
    - from: "EmpresaAnaliseEcf/Show.jsx"
      to: "EmpresaHeader + EvolucaoEmpresaChart + HistoricoMedalhas + AlertasDoSeller"
      via: "props.seller alimenta EmpresaHeader; props.metricas alimenta EvolucaoEmpresaChart; props.medalhas alimenta HistoricoMedalhas; props.signals alimenta AlertasDoSeller (slice 0,20)"
      pattern: "EmpresaHeader|EvolucaoEmpresaChart|HistoricoMedalhas|AlertasDoSeller"
    - from: "AlertaCard.jsx (Phase 23)"
      to: "rota empresas.analise-ecf"
      via: "Link Inertia href={route('empresas.analise-ecf', company.id)} quando company prop não-null"
      pattern: "empresas.analise-ecf|route\\("
    - from: "BreakdownTabs.jsx (Phase 24)"
      to: "lib/ecfDriveLabels.js (novo)"
      via: "import { CLUSTER_LABELS, FRETE_LABELS, PROGRAMA_LABELS, traduzirItem } from '@/lib/ecfDriveLabels'"
      pattern: "ecfDriveLabels"
---

<objective>
Entregar a **terceira aba visível da Milestone v8.0** consumindo o wrapper expandido da Phase 22 (`EcfDriveService::seller` + `sellerMetricasMensal` + `sellerMedalhas` + `sellerSignals`) — a **ficha 360° de uma empresa** acessível em `/empresas/{company}/analise-ecf`. Cria controller com auth fina por carteira + try/catch global, 5 componentes JSX (Header, Evolução 12m, Histórico Medalhas, Alertas, Show coordenador) e refatora o `BreakdownTabs.jsx` da Phase 24 para extrair as constantes de tradução para `lib/ecfDriveLabels.js`. Também atualiza o `AlertaCard.jsx` da Phase 23 para linkar diretamente para essa nova página.

Purpose:
- **Acertividade**: dados oficiais ML via ECF Drive (fonte da verdade) — substitui parcialmente o drilldown frágil do Adman MCP (Phase 19 sofreu com 429 frequente). Cache do wrapper (proxy/1h/6h/5min) absorve as 5 chamadas.
- **Praticidade**: quando o admin/consultor vê um alerta de "Queda de GMV — RELOJOARIA WENUS" em `/alertas-estrategicos`, clica e cai direto na ficha 360° dessa empresa com TUDO num lugar só: KPIs do mês, evolução 12m, histórico de medalhas, alertas ativos. Resposta natural ao pedido: "quero ver tudo sobre essa empresa".
- **Próximo passo da v8.0**: Phase 23 (Alertas) entregou reação tática; Phase 24 (Painel Executivo) entregou visão sistêmica da carteira; Phase 25 entrega o drill-down 1 nível abaixo — ficha por empresa. Convive com Sugadores (operacional adgroup-level) e com `Companies/Show.jsx` existente (CRUD/grant/sugadores).

Output:
- 1 PLAN.md (este arquivo)
- `app/Http/Controllers/EmpresaAnaliseEcfController.php` (NOVO — show com 5 chamadas ECF Drive + auth fina + try/catch global)
- 1 rota em `routes/web.php` (grupo `auth, verified, role:admin,consultor,mentor`)
- 6 arquivos JSX novos: `lib/ecfDriveLabels.js`, `Show.jsx`, `EmpresaHeader.jsx`, `EvolucaoEmpresaChart.jsx`, `HistoricoMedalhas.jsx`, `AlertasDoSeller.jsx`
- 2 arquivos JSX modificados: `PainelExecutivo/components/BreakdownTabs.jsx` (refator labels) + `AlertasEstrategicos/components/AlertaCard.jsx` (link)
- `tests/Feature/Phase25/EmpresaAnaliseEcfControllerTest.php` (NOVO — 7+ testes)
- W4: checkpoint humano blocking — smoke visual em prod (3 empresas reais)

**SEM migration, SEM model novo, SEM activity log, SEM shared prop, SEM edição/CRUD, SEM export PDF, SEM comparação entre sellers, SEM forecast**. **NÃO tocar em** `EcfDriveService.php` (Phase 22), `DashboardController`, `CompanyController` (CRUD da empresa continua intacto), `Companies/Show.jsx` (D-01 do CONTEXT — rota nova, não embedded).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-analise-por-empresa-sellers-pagina-detail-360-graus-com-sell/CONTEXT.md
@API-GUIDE.md

# Memory persistente relevante
@C:/Users/User/.claude/projects/c--xampp-htdocs-ecf-admin-ecf-admin/memory/feedback_lean_planning.md
@C:/Users/User/.claude/projects/c--xampp-htdocs-ecf-admin-ecf-admin/memory/feedback_gsd_language_pt_br.md
@C:/Users/User/.claude/projects/c--xampp-htdocs-ecf-admin-ecf-admin/memory/feedback_autorizacao_permanente_deploy.md

# Código autoritativo (LER antes de editar)
@app/Services/EcfDriveService.php
@app/Models/Company.php
@app/Http/Controllers/CompanyController.php
@app/Http/Controllers/SugadorController.php
@resources/js/Pages/Companies/Show.jsx
@resources/js/Pages/PainelExecutivo/components/KpiCard.jsx
@resources/js/Pages/PainelExecutivo/components/HistoricoChart.jsx
@resources/js/Pages/PainelExecutivo/components/BreakdownTabs.jsx
@resources/js/Pages/AlertasEstrategicos/components/AlertaCard.jsx
@routes/web.php

# Phase 23 e 24 (padrões já pousados — replicar try/catch global + structures)
@.planning/phases/23-alertas-estrategicos-signals-aba-dedicada-com-caixa-de-entra/23-01-PLAN.md
@.planning/phases/24-painel-executivo-carteira-ecf-carteira-resumo-gmv-vendas-ads/24-01-PLAN.md

<interfaces>
<!-- Contratos da Phase 22 (consumidos, NÃO modificados) -->

## EcfDriveService — métodos consumidos nesta phase

```php
// Phase 22 — wrapper expandido. Cache: proxy/1h/6h/5min conforme método.

public function seller(string $custId): array;
// GET /sellers/{custId} — SEM cache (proxy on-demand)
// Retorna: snapshot consolidado com dados do cliente + medalha atual + métrica mensal atual.
// Shape esperado (fonte: API-GUIDE.md §5 e smoke W4 Phase 22):
//   ['custId' => string, 'razaoSocial' => string, 'segmento' => ?string,
//    'programa' => 'CPP'|'POLOS'|'CDP', 'cluster' => ?string,
//    'medalhaAtual' => ['nivel' => 'PLATINUM'|'GOLD'|'SILVER'|'BRONZE', 'timMonthId' => 'YYYYMM'],
//    'metricaAtual' => ['timMonthId' => 'YYYYMM', 'tgmv_lc' => float, 'tsi' => int,
//                       'inv_pads' => float, 'tgmv_lc_pads' => float, 'visitas' => int,
//                       'score_final_full' => int, 'score_final_pads' => int, ...]
//   ]
// @throws \RuntimeException em HTTP != 2xx (incluindo 404 quando cust_id não existe na base ECF)

public function sellerMetricasMensal(string $custId, array $filtros = []): array;
// GET /sellers/{custId}/metricas/mensal — cache 1h
// Retorna: ['data' => [{timMonthId, tgmv_lc, inv_pads, tsi, visitas, score_final_full, ...}, ...]]
// Filtro 'from' aceita 'YYYYMM' (ex: 12 meses atrás).

public function sellerMedalhas(string $custId): array;
// GET /sellers/{custId}/medalhas — cache 6h
// Retorna: ['data' => [{timMonthId, nivel: 'PLATINUM'|..., delta?: 'subiu'|'caiu'|null}, ...]]

public function sellerSignals(string $custId): array;
// GET /sellers/{custId}/signals — cache 5min
// Retorna: ['data' => [{id, eventType, severity, payload, detectedAt, ackedAt}, ...]]
// SEM parâmetro de limit no wrapper Phase 22 — fazer slice(0,20) no controller PHP antes de passar pra view.
```

## Company — accessor cust_id (Phase 18 / D-02 do CONTEXT)

```php
// app/Models/Company.php — JÁ EXISTE
public function getCustIdAttribute(): ?string
{
    $custId = $this->ml_store_id ?: $this->adman_account_id;
    return $custId !== '' ? $custId : null;
}
```

Usar `$company->cust_id` (NÃO `$company->adman_account_id` direto).

## Auth de consultor/mentor por carteira (reusa pattern SugadorController)

```php
// app/Http/Controllers/SugadorController.php (pattern já estabelecido)
$user = $request->user();
$hasGlobalView = $user->isAdmin();  // Para Phase 25, só admin tem visão global
if (!$hasGlobalView) {
    // Reusa relação Company::users() pivot company_users (verificado em Phase 19)
    $emCarteira = $user->companies()->where('companies.id', $company->id)->exists();
    abort_unless($emCarteira, 403, 'Esta empresa não está na sua carteira.');
}
```

NOTA importante sobre o `where`: precisa de `'companies.id'` (qualificado) por causa do join implícito da relação `belongsToMany`. Já visto no codebase.

## Middleware role com múltiplos roles (Phase 23 já usou)

```php
// EnsureUserHasRole aceita varargs separados por vírgula (Phase 23 D-01)
Route::middleware(['auth', 'verified', 'role:admin,consultor,mentor'])
```

## Catch 404 separado de erro genérico (D-11 do CONTEXT)

Mensagem da exceção do wrapper Phase 22:
```php
throw new \RuntimeException("ECF Drive {$path} falhou: HTTP {$response->status()} - {$response->body()}");
```

Estratégia no controller:
```php
try {
    $seller = $this->ecf->seller($custId);
    // ... outras 4 chamadas
} catch (\Throwable $e) {
    // 404 separado de erro genérico: cust_id existe mas não está na base ECF
    if (str_contains($e->getMessage(), 'HTTP 404')) {
        return Inertia::render('EmpresaAnaliseEcf/Show', [
            'empresa' => ['id' => $company->id, 'name' => $company->name, 'cust_id' => $custId],
            'seller' => null, 'metricas' => [], 'medalhas' => [], 'signals' => [],
            'semCustId' => false, 'naoEncontrada' => true, 'erro' => null,
        ]);
    }
    report($e);
    return Inertia::render('EmpresaAnaliseEcf/Show', [
        'empresa' => [...],
        'seller' => null, 'metricas' => [], 'medalhas' => [], 'signals' => [],
        'semCustId' => false, 'naoEncontrada' => false,
        'erro' => 'Não foi possível buscar dados da análise agora. Tente em alguns segundos.',
    ]);
}
```

## Shape das props Inertia da página /empresas/{company}/analise-ecf

```jsonc
{
  "empresa": {
    "id": 123,
    "name": "RELOJOARIA WENUS",
    "cust_id": "570267839",       // null quando semCustId=true
    "segmento_local": "varejo"     // do CompanyGrant local, opcional (D-05 header)
  },
  "seller": {                       // null em erro/naoEncontrada/semCustId
    "razaoSocial": "...",
    "segmento": "Relógios",
    "programa": "CPP",
    "cluster": "Core",
    "medalhaAtual": { "nivel": "PLATINUM", "timMonthId": "202605" },
    "metricaAtual": { "timMonthId": "202605", "tgmv_lc": 2700000, "tsi": 16597,
                      "inv_pads": 45000, "tgmv_lc_pads": 250000, "visitas": 42000,
                      "score_final_full": 92, "score_final_pads": 85, ... }
  },
  "metricas": [                     // [] em erro — array das 12 últimas posições
    { "timMonthId": "202506", "tgmv_lc": 2500000, "inv_pads": 40000, ... },
    // ... até "202605"
  ],
  "medalhas": [                     // [] em erro
    { "timMonthId": "202509", "nivel": "GOLD" },
    { "timMonthId": "202512", "nivel": "PLATINUM" },
    // ...
  ],
  "signals": [                      // [] em erro — JÁ limitado a 20 no controller
    { "id": 8842, "eventType": "seller.gmv_queda_mom", "severity": "warning",
      "payload": {...}, "detectedAt": "2026-06-04T12:00:00Z", "ackedAt": null },
    // ... até 20
  ],
  "semCustId": false,               // true quando $company->cust_id === null
  "naoEncontrada": false,           // true quando seller() retorna HTTP 404
  "erro": null                      // string pt-BR quando catch genérico
}
```

## Reutilização de helpers (decisões)

- **fmtMoneyShort/fmtCountShort/fmtPercentSigned**: cópias locais nos cards (mesmo padrão Phase 24 KpiCard.jsx). NÃO criar `lib/utils.js` global ainda — só duplica.
- **fmtMesAno (timMonthId → 'mai/26')**: cópia local no `EvolucaoEmpresaChart.jsx` (mesmo lookup MESES_PT do Phase 24 HistoricoChart). Pequena duplicação aceitável; se aparecer em uma 3ª phase, promover para utils.
- **CLUSTER_LABELS/FRETE_LABELS/PROGRAMA_LABELS**: ESSAS sim viram compartilhadas → `lib/ecfDriveLabels.js` (W1-T1). 3 phases usariam (Phase 24 já usa, Phase 25 EmpresaHeader.jsx usa programa, Phase 27 futura usará todas).

## Constantes de medalha (LOCAL — não compartilhar ainda)

```js
// EmpresaHeader.jsx + HistoricoMedalhas.jsx (cópia local em ambos — só 2 usos)
const MEDALHA_BADGE = {
    PLATINUM: { label: 'Platinum',  bg: 'bg-cyan-500/15',   text: 'text-cyan-300',   border: 'border-cyan-500/30' },
    GOLD:     { label: 'Gold',      bg: 'bg-yellow-500/15', text: 'text-yellow-300', border: 'border-yellow-500/30' },
    SILVER:   { label: 'Silver',    bg: 'bg-slate-500/15',  text: 'text-slate-300',  border: 'border-slate-500/30' },
    BRONZE:   { label: 'Bronze',    bg: 'bg-amber-700/15',  text: 'text-amber-400',  border: 'border-amber-700/30' },
};
```

Classes Tailwind HARDCODED (não interpolar `bg-${nivel}-500/15` — JIT do Tailwind purgaria, mesmo problema da Phase 23 AlertaCard).

## NÃO usar shared prop

Phase 25 NÃO precisa de shared prop. Não há badge contínuo em outras páginas.
</interfaces>
</context>

<decisoes>

## Decisões importantes registradas

**D-01: Rota standalone `/empresas/{company}/analise-ecf` com route model binding (CONTEXT D-01)**
- *Por quê:* `Companies/Show.jsx` já tem responsabilidades próprias (CRUD/grant/sugadores). Embedded ali criaria página gigante e mistura de concerns. Rota standalone permite lazy load (só chama as 5 APIs quando o usuário entra de fato na análise) e UI focada na ficha 360°. Route model binding por `Company $company` injeta automaticamente.

**D-02: Middleware `role:admin,consultor,mentor` + auth fina INLINE no controller (CONTEXT D-03)**
- *Por quê:* admin vê todas; consultor/mentor só veem empresas da sua carteira. Middleware filtra os 3 roles, mas só sabe quem está autenticado — não conhece a relação empresa↔carteira. Logo, o controller faz a checagem fina via `$user->companies()->where('companies.id', $company->id)->exists()`. Reusa exatamente o pattern do `SugadorController::index` (linha ~50). Vírgulas no separador (Phase 23 D-01 já validou).

**D-03: Lookup cust_id via accessor `Company::cust_id` (CONTEXT D-02)**
- *Por quê:* Accessor já existe no model (Phase 18 D-02). Prioriza `ml_store_id ?? adman_account_id`. Quando ambos são vazios → retorna null → empty state `semCustId=true`. Mesma estratégia de `RefreshGrossBillingCacheJob`, `DashboardController` e `AdmanService::syncCompany`.

**D-04: Try/catch global Throwable com 404 separado de erro genérico (CONTEXT D-09 + D-11)**
- *Por quê:* O wrapper Phase 22 lança `\RuntimeException("ECF Drive {$path} falhou: HTTP 404 - ...")`. 404 no `/sellers/{custId}` é sinal específico de "cust_id existe mas não está na base ECF Drive" (típico de cust_id de empresa Shopee no Adman). Tratamento diferente do erro genérico (flag `naoEncontrada=true` vs `erro=pt-BR`). Detecção via `str_contains($e->getMessage(), 'HTTP 404')`. Não criar `sellerOuNull()` no wrapper (CONTEXT diz que vira fase futura) — fazer inline.

**D-05: 5 chamadas sequenciais ao ECF Drive (sem paralelização) — replica Phase 24 D-02**
- *Por quê:* Cache do wrapper absorve (proxy/1h/6h/5min). Em cold cache primeira carga ~5-8s; subsequentes <500ms. Paralelizar exigiria Http::pool, complexidade desproporcional. CONTEXT pitfall 1 trava decisão. Documentar latência em comentário do controller.
- Chamadas: (1) `seller()`, (2) `sellerMetricasMensal()` últimas 12 posições, (3) `sellerMedalhas()`, (4) `sellerSignals()`. Total 4 chamadas — não 5 como CONTEXT sugere. Ranking foi removido nesta fase (não há método `rankingDoSeller()` no wrapper — `ranking()` retorna top-N global, não posição de 1 seller). Decisão: deixar ranking para Phase 27 ou follow-up.

**D-06: signals limita 20 no controller PHP (não no wrapper)**
- *Por quê:* CONTEXT pitfall 4 — seller crítico pode ter 778 signals. Wrapper `sellerSignals` da Phase 22 não aceita parâmetro de limit. Solução: `array_slice($signalsResp['data'] ?? [], 0, 20)` no controller antes de passar para view. Trade-off: cache do wrapper armazena resposta inteira (caro em payload, mas o usuário não vê). Aceitável.

**D-07: Refator de labels — Wave 1 task pré-controller, pois é zero-risk e habilita testes Phase 24 verdes (CONTEXT D-08)**
- *Por quê:* Mover `CLUSTER_LABELS/FRETE_LABELS/PROGRAMA_LABELS` de `BreakdownTabs.jsx` para `lib/ecfDriveLabels.js` é pure refactor — comportamento idêntico. Rodar W1-T1 ANTES do controller permite validar zero regressão antes de qualquer mudança comportamental. Helper `traduzirItem(tabKey, valor)` também move junto (D-08 do CONTEXT). Phase 25 EmpresaHeader.jsx vai usar `PROGRAMA_LABELS` para exibir "CPP — Comercial Pessoa Privada" em vez de só "CPP".

**D-08: AlertaCard.jsx link atualizado para `empresas.analise-ecf` (CONTEXT D-07)**
- *Por quê:* Hoje (Phase 23) o nome da empresa em alertas é `<span>` sem link (não vimos link prévio na leitura — confirmar no executor). Decisão: virar `<Link href={route('empresas.analise-ecf', company.id)}>` quando `company` resolvida. Cliente externo (company=null) continua como span estático. `preserveScroll=true` no Link para manter posição da lista de alertas ao voltar.

**D-09: NÃO criar comparação 2+ sellers / NÃO criar export PDF / NÃO criar forecast**
- *Por quê:* CONTEXT trava como não-objetivos. Comparação (Phase 25.1 futura — wrapper já tem `compararSellers()` aceitando até 20 custIds). Forecast (Phase 27). PDF (Phase 28). Edição de qualquer campo: 100% read-only nesta página (o CRUD continua em `/companies/{id}`).

**D-10: Empty states com mensagens pt-BR amigáveis para usuário não-tech (lembrança Phase 21 Manual)**
- *Por quê:* Memory `feedback_lean_planning` + Phase 21 ensinou que ECF tem usuários não-tech. Mensagens não devem ter "RuntimeException", "404", "API", "HTTP". Devem usar linguagem direta:
  - semCustId: "Esta empresa não tem ID de lojista do Mercado Livre cadastrado. A análise ECF Drive não está disponível. Para habilitar, atualize o cadastro com o ID Adman ou ID da loja ML."
  - naoEncontrada: "Esta empresa tem ID de lojista (X) mas não foi encontrada na base do parceiro ECF Drive. Pode ser uma conta que ainda não foi sincronizada ou que está fora do universo Mercado Livre."
  - erro genérico: "Não foi possível buscar dados da análise agora. Tente em alguns segundos."
- Mensagens incluem ação sugerida ("atualizar cadastro com...") quando aplicável.

**D-11: Constantes de medalha LOCAL nos componentes (não compartilhadas ainda)**
- *Por quê:* `MEDALHA_BADGE` mapa só é usado em 2 lugares (EmpresaHeader + HistoricoMedalhas). Promover para `lib/` só quando aparecer em 3+ phases. Trade-off: 6 linhas duplicadas. Aceitável (D-04 Phase 24 já usa esta estratégia).

**D-12: Gráfico evolução com duplo eixo Y — GMV (esq) + Investimento ADS (dir)**
- *Por quê:* GMV (R$ milhões) e Investimento ADS (R$ dezenas de milhares) têm escalas distintas. Eixo único achatava. Recharts `yAxisId` suporta nativamente. Cores: GMV amarelo `#ffe600` (alinhado com Phase 24); ADS azul `#60a5fa` (distinto). Reusa estrutura do `PainelExecutivo/components/HistoricoChart.jsx` (cópia adaptada — fmtMesAno e tooltip pt-BR).

**D-13: SEM `npm run build` em paralelo — fica como última sub-task do W2**
- *Por quê:* Build é caro (~30s). Rodar várias vezes durante W2 desperdiça tempo. Última task do W2 (W2-T5: AlertaCard refactor + npm run build) consolida.

**D-14: W3 (testes) numa task única após W2 — não bloquear frontend**
- *Por quê:* Testes ficam mais sólidos quando há contrato finalizado (props finais). Rodar Http::fake com payload sintético depois das chamadas estarem cristalizadas no controller.

**D-15: W4 (smoke visual em prod) é checkpoint humano blocking — 3 empresas reais**
- *Por quê:* Primeira validação end-to-end em prod com 3 perfis diferentes:
  1. **RELOJOARIA WENUS (cust_id 570267839)** — top GMV (R$ 2,7M maio/26 conforme smoke Phase 22). Espera-se ficha rica: medalha PLATINUM/GOLD, alertas potenciais, gráfico cheio.
  2. **CAMILLO PARTS (cust_id 267873368)** — empresa típica de meio termo.
  3. **Empresa Shopee/Amazon sem cust_id ML** (qualquer uma com `marketplace='shopee'` e `ml_store_id=null` e `adman_account_id=null`) — deve cair em `semCustId=true` com card explicativo.
- Verificar: header completo, todos KPI cards renderizados, gráfico com duplo eixo, timeline de medalhas, alertas read-only, link voltando de AlertasCard para análise (não Dashboard).

## Riscos residuais (documentados — não bloqueiam o plan)

**R-01: Primeira carga ~5-8s em cold cache (4 chamadas sequenciais)**
- Mitigação: cache do wrapper. Documentar em comentário do controller. Próximas cargas <500ms.

**R-02: Shape exato de `seller()` e `sellerMedalhas()` ainda não foi smoke-tested em prod (Phase 22 W4 cobriu carteira/ranking/signals/relatorio)**
- Mitigação: defensiva no controller (`?? []` em todos os `['data']`), defensiva no frontend (`?.`). Se shape divergir, smoke W4 captura cedo (3 empresas) antes de afetar usuários.

**R-03: Auth fina `$user->companies()->where('companies.id', $company->id)->exists()` exige relação carregada — N+1 evitado porque é exists() simples, mas qualificar `companies.id` é essencial (sem qualificar, ambiguous column em SQLite/MySQL com pivot company_users)**
- Mitigação: documentado no controller. Teste 403 consultor fora da carteira valida.

**R-04: AlertaCard.jsx link Inertia — se `route('empresas.analise-ecf', company.id)` falhar (rota não registrada em Ziggy), página /alertas-estrategicos quebraria silenciosamente**
- Mitigação: ordem das tasks — controller+rota (W1-T2) ANTES do AlertaCard update (W2-T5). Ziggy refresh acontece no `npm run build`. Smoke W4 valida link.

**R-05: Refator labels (W1-T1) pode quebrar testes Phase 24 se import path mudar**
- Mitigação: pre-flight grep no executor para mapear callers atuais. Rodar `vendor/bin/phpunit tests/Feature/Phase24/` após o W1-T1 e antes de seguir.

**R-06: Cold cache em smoke W4 com 3 empresas em sequência = ~24s** (3 × 8s)
- Mitigação: aceitar primeira execução lenta. Smoke não tem timeout de UX. Cache esquenta entre acessos.

**R-07: Empresa pode ter `cust_id` populado mas seller() retornar 200 com `medalhaAtual=null` (seller novato sem medalha)**
- Mitigação: defensiva no `EmpresaHeader.jsx` — quando `seller.medalhaAtual === null`, exibir "Sem medalha cadastrada" em vez de quebrar. Mesma defensiva em `HistoricoMedalhas.jsx` quando `medalhas.length === 0`.

</decisoes>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════════ -->
<!--      WAVE 1 — Refator zero-risk + Backend (2 tasks)                      -->
<!-- ═══════════════════════════════════════════════════════════════════════ -->

<task type="auto" wave="1" id="W1-T1">
  <name>W1-T1: Refator lib/ecfDriveLabels.js + ajustar BreakdownTabs.jsx (zero comportamento)</name>
  <files>
    resources/js/lib/ecfDriveLabels.js (NOVO),
    resources/js/Pages/PainelExecutivo/components/BreakdownTabs.jsx
  </files>
  <behavior>
    - Criar arquivo NOVO `resources/js/lib/ecfDriveLabels.js` exportando 3 constantes + 1 helper:
      ```js
      // Dicionários pt-BR para termos do classificador interno do ML.
      // Extraídos de Pages/PainelExecutivo/components/BreakdownTabs.jsx (Phase 24)
      // para reuso em outras phases (25 EmpresaHeader, 27 futura, etc).

      export const CLUSTER_LABELS = {
          'Core':            'Núcleo maduro (Core)',
          'MeliPro':         'Premium (MeliPro)',
          // ... copiar EXATAMENTE como está em BreakdownTabs.jsx
          'SEM_CLUSTER':     'Não classificado',
      };

      export const FRETE_LABELS = {
          'ME2':  'Mercado Envios padrão (ME2)',
          'FULL': 'Mercado Envios Full',
          'FLEX': 'Mercado Envios Flex',
      };

      export const PROGRAMA_LABELS = {
          'CPP':   'CPP — Comercial Pessoa Privada',
          'POLOS': 'POLOS — Polos Regionais',
      };

      /** Aplica o dicionário pt-BR correto conforme a tab/contexto ativo. */
      export const traduzirItem = (tabKey, valor) => {
          if (!valor) return '—';
          if (tabKey === 'cluster')  return CLUSTER_LABELS[valor]  || valor;
          if (tabKey === 'frete')    return FRETE_LABELS[valor]    || valor;
          if (tabKey === 'programa') return PROGRAMA_LABELS[valor] || valor;
          return valor;
      };
      ```
    - Editar `resources/js/Pages/PainelExecutivo/components/BreakdownTabs.jsx`:
      - REMOVER as definições inline de `CLUSTER_LABELS`, `FRETE_LABELS`, `PROGRAMA_LABELS`, e a função `traduzirItem` (mantidas hoje nas linhas ~67-102 do arquivo atual).
      - ADICIONAR no topo: `import { CLUSTER_LABELS, FRETE_LABELS, PROGRAMA_LABELS, traduzirItem } from '@/lib/ecfDriveLabels';` (note: o componente JÁ usa apenas `traduzirItem` no JSX, mas mantemos as 3 constantes no import por compat caso o executor encontre outros usos não-óbvios).
      - NÃO mudar mais nada no arquivo — comportamento idêntico, apenas a origem das constantes muda.
    - Comentário pt-BR explicando o motivo do extract em `lib/ecfDriveLabels.js`.
  </behavior>
  <action>
    Sub-task A — Criar `resources/js/lib/ecfDriveLabels.js` copiando EXATAMENTE as 3 constantes (`CLUSTER_LABELS`, `FRETE_LABELS`, `PROGRAMA_LABELS`) e a função `traduzirItem` do `BreakdownTabs.jsx` atual (linhas ~67-102). Exportar todas com `export const`. Adicionar docblock no topo do arquivo explicando que é dicionário pt-BR de termos do classificador interno do ML, extraído da Phase 24 para reuso em Phase 25 e futuras.

    Sub-task B — Editar `BreakdownTabs.jsx`:
    1. Adicionar `import { CLUSTER_LABELS, FRETE_LABELS, PROGRAMA_LABELS, traduzirItem } from '@/lib/ecfDriveLabels';` na seção de imports no topo (após os imports de recharts e lucide).
    2. Remover as 3 declarações `const CLUSTER_LABELS = {...}`, `const FRETE_LABELS = {...}`, `const PROGRAMA_LABELS = {...}` (~linhas 69-93).
    3. Remover a função `const traduzirItem = (tabKey, valor) => {...}` (~linhas 96-102).
    4. NÃO mudar o resto do arquivo. `PizzaTooltip` continua usando `traduzirItem` (agora vindo do import).

    Sub-task C — Validação imediata (sem deploy):
    1. `grep -c "const CLUSTER_LABELS" resources/js/Pages/PainelExecutivo/components/BreakdownTabs.jsx` → deve retornar 0.
    2. `grep -c "import.*ecfDriveLabels" resources/js/Pages/PainelExecutivo/components/BreakdownTabs.jsx` → deve retornar 1.
    3. `grep -c "export const CLUSTER_LABELS" resources/js/lib/ecfDriveLabels.js` → deve retornar 1.
    4. Rodar suite Phase 24: `vendor/bin/phpunit tests/Feature/Phase24/ --stop-on-failure` → deve continuar verde. Se quebrar, ROLLBACK do refator e investigar antes de prosseguir.

    Comentários pt-BR. NÃO tocar em outros arquivos.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const lib=fs.readFileSync('resources/js/lib/ecfDriveLabels.js','utf8'); const bt=fs.readFileSync('resources/js/Pages/PainelExecutivo/components/BreakdownTabs.jsx','utf8'); if(!lib.includes('export const CLUSTER_LABELS')||!lib.includes('export const FRETE_LABELS')||!lib.includes('export const PROGRAMA_LABELS')||!lib.includes('export const traduzirItem')) throw new Error('ecfDriveLabels.js incompleto'); if(!bt.includes('from \\'@/lib/ecfDriveLabels\\'')) throw new Error('BreakdownTabs.jsx nao importa do lib novo'); if(bt.match(/const\\s+CLUSTER_LABELS\\s*=\\s*\\{/)) throw new Error('BreakdownTabs.jsx ainda tem CLUSTER_LABELS inline'); console.log('OK refator labels');"</automated>
    <!-- Garante: lib novo tem as 4 exports + BreakdownTabs importa do lib + remove inline -->
  </verify>
  <done>
    - `resources/js/lib/ecfDriveLabels.js` existe com 3 constantes + traduzirItem exportados
    - `BreakdownTabs.jsx` importa do `@/lib/ecfDriveLabels` e NÃO tem mais as constantes inline
    - Suite Phase 24 (`vendor/bin/phpunit tests/Feature/Phase24/`) continua verde
    - Commit: `refactor(phase-25): extrai CLUSTER/FRETE/PROGRAMA_LABELS para lib/ecfDriveLabels.js`
  </done>
</task>

<task type="auto" wave="1" id="W1-T2">
  <name>W1-T2: EmpresaAnaliseEcfController + 1 rota em routes/web.php (auth fina + 4 chamadas wrapper)</name>
  <files>
    app/Http/Controllers/EmpresaAnaliseEcfController.php (NOVO),
    routes/web.php
  </files>
  <behavior>
    - Arquivo NOVO `app/Http/Controllers/EmpresaAnaliseEcfController.php` com namespace `App\Http\Controllers`, estende `Controller`
    - Construtor com DI: `__construct(private EcfDriveService $ecf) {}`
    - Método `show(Request $request, Company $company)` (route model binding):
      - **Auth fina (D-02 do PLAN)**: lê `$user = $request->user()`. Se `!$user->isAdmin()`, executa `abort_unless($user->companies()->where('companies.id', $company->id)->exists(), 403, 'Esta empresa não está na sua carteira.');`. Admin sempre passa.
      - **Lookup cust_id (D-03)**: `$custId = $company->cust_id;`. Se null → retorna Inertia::render com `semCustId=true` e props vazias. NÃO chama o wrapper (zero overhead).
      - **Carrega grant local (opcional)**: `$company->loadMissing('grants')` + `$grantAtivo = $company->grants->where('status', 'active')->first();`. Usado para `segmento_local` (D-05 header).
      - **Try/catch global (D-04)**: dentro de try, executa 4 chamadas sequenciais:
        1. `$sellerResp = $this->ecf->seller($custId);`
        2. `$metricasResp = $this->ecf->sellerMetricasMensal($custId);`
        3. `$medalhasResp = $this->ecf->sellerMedalhas($custId);`
        4. `$signalsResp = $this->ecf->sellerSignals($custId);`
      - Extrai arrays: `$metricas = array_slice($metricasResp['data'] ?? [], -12);` (últimas 12 posições — mensal já vem ordenado asc); `$medalhas = $medalhasResp['data'] ?? [];`; `$signals = array_slice($signalsResp['data'] ?? [], 0, 20);` (D-06).
      - Retorna `Inertia::render('EmpresaAnaliseEcf/Show', ['empresa' => [...], 'seller' => $sellerResp, 'metricas' => $metricas, 'medalhas' => $medalhas, 'signals' => $signals, 'semCustId' => false, 'naoEncontrada' => false, 'erro' => null])`.
      - Em catch `\Throwable $e`:
        - Se `str_contains($e->getMessage(), 'HTTP 404')` → retorna props vazias com `naoEncontrada=true`, `erro=null`.
        - Senão: `report($e)` + retorna props vazias com `naoEncontrada=false`, `erro='Não foi possível buscar dados da análise agora. Tente em alguns segundos.'`.
      - O objeto `empresa` sempre vai com `['id' => $company->id, 'name' => $company->name, 'cust_id' => $custId, 'segmento_local' => $grantAtivo?->segmento]`.
    - Em `routes/web.php`:
      - Adicionar `use App\Http\Controllers\EmpresaAnaliseEcfController;` no topo (ordem alfabética — entre `ComercialController` e `ImplementacaoController` ou onde alfabeticamente couber).
      - Adicionar IMEDIATAMENTE APÓS o bloco `// ─── Painel Executivo Carteira ECF (Phase 24)` (~linha 278):
        ```php
        // ─── Análise por Empresa via ECF Drive (Phase 25) ───────────────────────
        // Ficha 360° de 1 empresa. Consome /sellers/{custId}/* da API ECF Drive
        // (Phase 22 wrapper). Acessível a admin, consultor e mentor (CONTEXT D-03).
        // Auth fina por carteira é feita inline no controller.
        Route::middleware(['auth', 'verified', 'role:admin,consultor,mentor'])
             ->group(function () {
                 Route::get('/empresas/{company}/analise-ecf',
                     [EmpresaAnaliseEcfController::class, 'show'])
                     ->name('empresas.analise-ecf');
             });
        ```
    - Docblocks pt-BR no controller documentando:
      - Estratégia defensiva (D-04): 4 chamadas em try/catch global, 404 separado de erro genérico.
      - Latência cold cache (R-01): ~5-8s primeira execução; <500ms depois.
      - Auth fina (D-02): admin vê todas; consultor/mentor restritos por `company_users`.
      - Decisão de limit 20 em signals (D-06).
    - NÃO mexer em `EcfDriveService.php`, `CompanyController`, `DashboardController` ou qualquer outro arquivo backend.
  </behavior>
  <action>
    Sub-task A — Criar `app/Http/Controllers/EmpresaAnaliseEcfController.php` com a estrutura definida em `<behavior>`. Imports necessários: `App\Http\Controllers\Controller`, `App\Models\Company`, `App\Services\EcfDriveService`, `Illuminate\Http\Request`, `Inertia\Inertia`. Construtor com DI do `EcfDriveService`. Método `show(Request $request, Company $company)` com route model binding.

    Implementar exatamente:
    1. Auth fina inline com `abort_unless` qualificando `companies.id` (R-03).
    2. Lookup `$custId = $company->cust_id` (accessor — D-03).
    3. Branch semCustId: se `$custId === null`, monta `$empresa = ['id' => ..., 'name' => ..., 'cust_id' => null, 'segmento_local' => null]` e retorna render com `'semCustId' => true`, demais props vazias/null.
    4. Carrega grant ativo: `$company->loadMissing('grants'); $grantAtivo = $company->grants->firstWhere('status', 'active'); $segmentoLocal = $grantAtivo?->segmento;` (atributo segmento existe em CompanyGrant Phase 20).
    5. Try com 4 chamadas sequenciais ao wrapper. Slice 12 últimas das metricas. Slice 20 dos signals.
    6. Catch específico HTTP 404 → `naoEncontrada=true`. Catch genérico → `report($e)` + mensagem pt-BR.
    7. Sempre retornar `Inertia::render('EmpresaAnaliseEcf/Show', [...])` com TODOS os 8 props (empresa, seller, metricas, medalhas, signals, semCustId, naoEncontrada, erro) — front sempre vai receber o shape completo.

    Mensagens pt-BR EXATAS:
    - 403 inline: `'Esta empresa não está na sua carteira.'`
    - Erro genérico: `'Não foi possível buscar dados da análise agora. Tente em alguns segundos.'`

    Sub-task B — Editar `routes/web.php`:
    1. Adicionar `use App\Http\Controllers\EmpresaAnaliseEcfController;` na lista de imports (alfabético).
    2. Adicionar o novo grupo IMEDIATAMENTE APÓS o bloco `// ─── Painel Executivo Carteira ECF (Phase 24) ─` (que termina ~linha 278 atual). Comentário pt-BR conforme `<behavior>`. Nome da rota: `empresas.analise-ecf`.

    Sub-task C — Validação rápida:
    ```bash
    php artisan route:list --name=empresas.analise-ecf 2>&1 | tail -5
    ```
    Esperado: 1 linha — `GET /empresas/{company}/analise-ecf` com middleware `web, auth, verified, role:admin,consultor,mentor` e controller `EmpresaAnaliseEcfController@show`.

    Comentários e docblocks em pt-BR. NÃO tocar em nenhum outro controller, service ou model.
  </action>
  <verify>
    <automated>php artisan route:list --name=empresas.analise-ecf 2&gt;&amp;1 | tail -5</automated>
    <!-- Esperado: 1 rota registrada (empresas.analise-ecf GET) com middleware role:admin,consultor,mentor -->
  </verify>
  <done>
    - `app/Http/Controllers/EmpresaAnaliseEcfController.php` existe com construtor DI + show()
    - Auth fina inline com `abort_unless($user->companies()->where('companies.id', $company->id)->exists(), 403, ...)`
    - Branch semCustId quando `$company->cust_id === null` (sem chamar wrapper)
    - 4 chamadas wrapper (seller, sellerMetricasMensal, sellerMedalhas, sellerSignals) em try/catch global
    - Catch separado HTTP 404 → `naoEncontrada=true`
    - Catch genérico Throwable → `report()` + erro pt-BR
    - signals limitados a 20 via array_slice
    - 1 rota registrada em routes/web.php (`empresas.analise-ecf`) no grupo `auth, verified, role:admin,consultor,mentor`
    - `php artisan route:list --name=empresas.analise-ecf` mostra a rota
    - Commit: `feat(phase-25): EmpresaAnaliseEcfController + rota /empresas/{company}/analise-ecf`
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════════ -->
<!--      WAVE 2 — Frontend (5 tasks)                                         -->
<!-- ═══════════════════════════════════════════════════════════════════════ -->

<task type="auto" wave="2" id="W2-T1">
  <name>W2-T1: EmpresaHeader.jsx (nome/cust_id/segmento/medalha atual/programa + links)</name>
  <files>
    resources/js/Pages/EmpresaAnaliseEcf/components/EmpresaHeader.jsx (NOVO)
  </files>
  <behavior>
    - Arquivo NOVO `resources/js/Pages/EmpresaAnaliseEcf/components/EmpresaHeader.jsx`
    - Props: `empresa` (`{id, name, cust_id, segmento_local}`), `seller` (objeto do `/sellers/{custId}` ou null)
    - Importa de `lucide-react`: `Building2, ExternalLink, Award`
    - Importa de `@inertiajs/react`: `Link`
    - Importa de `@/lib/ecfDriveLabels`: `PROGRAMA_LABELS` (D-07)
    - Constante local `MEDALHA_BADGE` (D-11) com 4 níveis hardcoded (Tailwind JIT).
    - Renderiza card com layout:
      - Header em flex row: ícone `Building2` em quadrado dourado/amarelo + nome da empresa em h1 (text-2xl bold white) + cust_id ao lado (text-xs text-white/40 monospace)
      - Linha de metadados em flex wrap: programa traduzido (`PROGRAMA_LABELS[seller?.programa] || seller?.programa || '—'`) + segmento (`seller?.segmento || empresa.segmento_local || '—'`) + cluster (`seller?.cluster || '—'`)
      - Badge de medalha atual: quando `seller?.medalhaAtual?.nivel` existe, renderizar `<span>` com classes de `MEDALHA_BADGE[nivel]` + ícone `Award` + label. Quando null/undefined: `<span className="text-xs text-white/30">Sem medalha cadastrada</span>`.
      - Linha de links: `<Link href={route('dashboard', { company: empresa.id })}>` "Ver no Dashboard" + `<Link href={route('companies.show', empresa.id)}>` "Ver cadastro" + opcionalmente `<Link>` para grant ativo se houver. Ícone `ExternalLink` ao lado.
    - Layout dark theme: bg `ecf-card`, border `white/[0.06]`, padding 5.
    - Defensiva: `seller` pode ser null (em erro / semCustId / naoEncontrada). Quando `seller === null`, mostrar só nome + cust_id + segmento_local local (sem programa/cluster/medalha).
    - Comentários pt-BR.
  </behavior>
  <action>
    Criar arquivo `resources/js/Pages/EmpresaAnaliseEcf/components/EmpresaHeader.jsx`.

    Imports:
    - `Building2, ExternalLink, Award` de `lucide-react`
    - `Link` de `@inertiajs/react`
    - `PROGRAMA_LABELS` de `@/lib/ecfDriveLabels`
    - `cn` de `@/lib/utils`

    Constante local `MEDALHA_BADGE` com 4 níveis (PLATINUM/GOLD/SILVER/BRONZE) com classes Tailwind hardcoded conforme `<interfaces>` deste plan.

    Componente `EmpresaHeader` (export default function) recebe `{ empresa, seller }`:
    1. Calcula `programaLabel = PROGRAMA_LABELS[seller?.programa] || seller?.programa || '—'`.
    2. Calcula `medalhaConfig = seller?.medalhaAtual?.nivel ? MEDALHA_BADGE[seller.medalhaAtual.nivel] : null`.
    3. Renderiza:
       - Container: `<div className="rounded-lg bg-ecf-card border border-white/[0.06] p-5 space-y-3">`
       - Topo (flex row gap-3):
         - Quadrado amarelo: `<div className="w-12 h-12 rounded-lg bg-ecf-yellow/15 border border-ecf-yellow/30 flex items-center justify-center shrink-0"><Building2 size={22} className="text-ecf-yellow" /></div>`
         - Coluna direita: nome em `<h1 className="text-2xl font-bold text-white truncate">{empresa.name}</h1>` + linha `<div className="flex items-center gap-2 text-xs"><span className="text-white/40 font-mono">cust_id: {empresa.cust_id ?? '—'}</span></div>`
       - Linha de metadados (flex wrap gap-3 text-sm):
         - "Programa: {programaLabel}" (text-white/70)
         - "Segmento: {seller?.segmento || empresa.segmento_local || '—'}" (text-white/70)
         - "Cluster: {seller?.cluster || '—'}" (text-white/70)
       - Medalha:
         - Quando `medalhaConfig`: `<span className={cn('inline-flex items-center gap-1.5 px-2.5 py-1 rounded-md text-xs font-semibold border', medalhaConfig.bg, medalhaConfig.text, medalhaConfig.border)}><Award size={12} /> Medalha atual: {medalhaConfig.label} {seller.medalhaAtual.timMonthId && '· ' + seller.medalhaAtual.timMonthId}</span>`
         - Quando null: `<span className="text-xs text-white/30">Sem medalha cadastrada</span>`
       - Linha de links (flex gap-4 text-xs text-ecf-yellow hover:underline pt-2 border-t border-white/[0.06]):
         - `<Link href={route('dashboard', { company: empresa.id })} className="inline-flex items-center gap-1"><ExternalLink size={11} /> Ver no Dashboard</Link>`
         - `<Link href={route('companies.show', empresa.id)} className="inline-flex items-center gap-1"><ExternalLink size={11} /> Ver cadastro</Link>`

    Defensiva: se `seller === null`, esconder linha de metadados (programa/segmento/cluster) e medalha — mostrar apenas nome, cust_id, links.

    Comentários inline pt-BR. NÃO usar emojis nem setas Unicode.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('resources/js/Pages/EmpresaAnaliseEcf/components/EmpresaHeader.jsx','utf8'); if(!c.includes('PROGRAMA_LABELS')||!c.includes('MEDALHA_BADGE')||!c.includes('Building2')||!c.includes('Link')||!c.includes('PLATINUM')) throw new Error('EmpresaHeader.jsx incompleto'); console.log('OK');"</automated>
  </verify>
  <done>
    - `EmpresaHeader.jsx` existe importando PROGRAMA_LABELS de @/lib/ecfDriveLabels
    - Renderiza nome, cust_id, segmento, programa traduzido, cluster, badge medalha colorida por nível
    - 4 níveis de medalha (PLATINUM/GOLD/SILVER/BRONZE) com classes Tailwind hardcoded
    - Links para Dashboard e companies.show
    - Defensiva quando `seller === null` (semCustId / naoEncontrada / erro)
    - Commit: `feat(phase-25): EmpresaHeader com medalha colorida e links`
  </done>
</task>

<task type="auto" wave="2" id="W2-T2">
  <name>W2-T2: EvolucaoEmpresaChart.jsx (recharts duplo eixo Y — GMV esquerda + Inv ADS direita)</name>
  <files>
    resources/js/Pages/EmpresaAnaliseEcf/components/EvolucaoEmpresaChart.jsx (NOVO)
  </files>
  <behavior>
    - Arquivo NOVO `resources/js/Pages/EmpresaAnaliseEcf/components/EvolucaoEmpresaChart.jsx`
    - Prop: `data` (array de `{timMonthId, tgmv_lc, inv_pads, ...}`, default `[]`)
    - Helpers locais (cópia adaptada do PainelExecutivo/HistoricoChart Phase 24):
      - `MESES_PT = ['jan','fev','mar','abr','mai','jun','jul','ago','set','out','nov','dez']`
      - `fmtMesAno(timMonthId)` — converte 'YYYYMM' para 'mai/26'
      - `fmtAxisShort(v)` — Intl compact pt-BR substituindo ' mi' por 'M' e ' mil' por 'k'
      - `fmtTooltipMoney(v)` — Intl currency BRL completo (sem abreviar)
    - Renderiza:
      - Empty state quando `data.length === 0`: div h-72 centralizado "Sem dados de evolução disponíveis."
      - Caso contrário: ResponsiveContainer height=300 com LineChart:
        - XAxis dataKey="mes" (formatado por fmtMesAno mapeado em dataMapeada)
        - YAxis yAxisId="left" com tickFormatter={fmtAxisShort}, tick color amarelo (#ffe600cc)
        - YAxis yAxisId="right" orientation="right" com tickFormatter={fmtAxisShort}, tick color azul (#60a5facc)
        - Tooltip customizado pt-BR mostrando ambos valores em formato completo
        - Legend no topo
        - Line yAxisId="left" dataKey="gmv" name="GMV" stroke="#ffe600" strokeWidth={2}
        - Line yAxisId="right" dataKey="investimento" name="Investimento ADS" stroke="#60a5fa" strokeWidth={2}
      - dataMapeada extrai: mes via fmtMesAno, gmv: d.tgmv_lc ?? 0, investimento: d.inv_pads ?? 0
    - Comentários pt-BR no topo explicando reuso adaptado do Phase 24.
  </behavior>
  <action>
    Criar `resources/js/Pages/EmpresaAnaliseEcf/components/EvolucaoEmpresaChart.jsx`.

    Imports de `recharts`: ResponsiveContainer, LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend.

    Helpers locais (cópia do Phase 24 HistoricoChart):
    - `MESES_PT` array.
    - `fmtMesAno(timMonthId)`: valida length===6, parseInt do mes, retorna `MESES_PT[mes-1] + '/' + ano.slice(2)`; fallback input cru.
    - `fmtAxisShort(v)`: `Intl.NumberFormat('pt-BR', { style:'currency', currency:'BRL', notation:'compact', maximumFractionDigits: 0 }).format(v).replace(' mi','M').replace(' mil','k')`.
    - `fmtTooltipMoney(v)`: `Intl.NumberFormat('pt-BR', { style:'currency', currency:'BRL', maximumFractionDigits: 0 }).format(v ?? 0)`.

    Componente `CustomTooltip({ active, payload, label })`: replica do Phase 24 HistoricoChart mas usando fmtTooltipMoney para AMBAS as séries.

    Componente `EvolucaoEmpresaChart({ data = [] })` (export default):
    1. Empty state quando data.length === 0.
    2. dataMapeada = data.map adicionando mes, gmv, investimento.
    3. ResponsiveContainer height 300 com LineChart.
    4. CartesianGrid strokeDasharray="3 3" stroke="#ffffff10".
    5. XAxis dataKey="mes" tick fill #ffffff60.
    6. 2 YAxis: left amarelo + right orientation="right" azul, ambos com tickFormatter=fmtAxisShort.
    7. Tooltip content={<CustomTooltip />}.
    8. Legend wrapperStyle fontSize 12.
    9. 2 Lines: GMV amarelo #ffe600 (yAxisId=left) + Investimento ADS azul #60a5fa (yAxisId=right), ambos type=monotone, strokeWidth=2, dot=false, activeDot r=4.

    Comentários pt-BR. Cópia local (não import de Phase 24) — isolamento.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('resources/js/Pages/EmpresaAnaliseEcf/components/EvolucaoEmpresaChart.jsx','utf8'); if(!c.includes('yAxisId')||!c.includes('tgmv_lc')||!c.includes('inv_pads')||!c.includes('#ffe600')||!c.includes('#60a5fa')) throw new Error('EvolucaoEmpresaChart.jsx incompleto'); console.log('OK');"</automated>
  </verify>
  <done>
    - `EvolucaoEmpresaChart.jsx` existe com recharts LineChart duplo eixo Y
    - GMV eixo esquerdo (amarelo ECF #ffe600); Investimento ADS eixo direito (azul #60a5fa)
    - fmtMesAno converte 'YYYYMM' para 'mai/26'
    - Tooltip pt-BR completo
    - Empty state quando data vazio
    - Commit: `feat(phase-25): EvolucaoEmpresaChart 12m duplo eixo Y`
  </done>
</task>

<task type="auto" wave="2" id="W2-T3">
  <name>W2-T3: HistoricoMedalhas.jsx (timeline horizontal com badges coloridas)</name>
  <files>
    resources/js/Pages/EmpresaAnaliseEcf/components/HistoricoMedalhas.jsx (NOVO)
  </files>
  <behavior>
    - Arquivo NOVO `resources/js/Pages/EmpresaAnaliseEcf/components/HistoricoMedalhas.jsx`
    - Prop: `medalhas` (array de `{timMonthId, nivel}`, default `[]`)
    - Importa `Award, TrendingUp, TrendingDown, Minus` de `lucide-react`
    - Importa `cn` de `@/lib/utils`
    - Constante local `MEDALHA_BADGE` (com chave `ordem: 4/3/2/1` para PLATINUM/GOLD/SILVER/BRONZE — D-11)
    - Helper local `fmtMesAno(timMonthId)` (cópia adaptada para timeline)
    - Helper local `compararNiveis(prev, atual)`: usa MEDALHA_BADGE[X].ordem; retorna 'subiu' | 'caiu' | 'manteve' | null (se prev null)
    - Renderiza:
      - Empty state h-32 centralizado: "Sem histórico de medalhas registrado."
      - Caso contrário: wrapper overflow-x-auto + flex row gap-3 dos cards. Cada card mostra:
        - timMonthId formatado (fmtMesAno) no topo
        - Badge da medalha com cor por nível (mesmas classes do MEDALHA_BADGE) + ícone Award
        - Indicador de mudança vs medalha anterior (TrendingUp verde / TrendingDown vermelho / Minus cinza) com label
      - Cada card: w-28 shrink-0 rounded-lg border bg-ecf-card p-3 flex flex-col items-center gap-1.5 text-center
    - Comentários pt-BR.
  </behavior>
  <action>
    Criar `resources/js/Pages/EmpresaAnaliseEcf/components/HistoricoMedalhas.jsx`.

    Imports: Award, TrendingUp, TrendingDown, Minus de lucide-react; cn de @/lib/utils.

    Constante MEDALHA_BADGE com 4 níveis + `ordem: 4|3|2|1` para PLATINUM/GOLD/SILVER/BRONZE. Classes Tailwind hardcoded conforme interfaces.

    Helpers:
    - fmtMesAno (cópia do EvolucaoEmpresaChart)
    - compararNiveis(prev, atual): retorna 'subiu' | 'caiu' | 'manteve' | null

    Componente `HistoricoMedalhas({ medalhas = [] })`:
    1. Empty state quando vazio.
    2. Mapeia cada medalha em card com timMonthId + badge + indicador de mudança comparando com medalhas[i-1]
    3. Wrapper scroll horizontal: <div className="overflow-x-auto"><div className="flex gap-3 min-w-min pb-2">{cards}</div></div>

    Comentários pt-BR.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('resources/js/Pages/EmpresaAnaliseEcf/components/HistoricoMedalhas.jsx','utf8'); if(!c.includes('PLATINUM')||!c.includes('GOLD')||!c.includes('SILVER')||!c.includes('BRONZE')||!c.includes('TrendingUp')||!c.includes('overflow-x-auto')) throw new Error('HistoricoMedalhas.jsx incompleto'); console.log('OK');"</automated>
  </verify>
  <done>
    - `HistoricoMedalhas.jsx` existe com timeline horizontal
    - 4 níveis (PLATINUM/GOLD/SILVER/BRONZE) com badges coloridas hardcoded
    - Indicador subiu/caiu/manteve comparando com mês anterior
    - Scroll horizontal quando muitas medalhas
    - Empty state pt-BR quando vazio
    - Commit: `feat(phase-25): HistoricoMedalhas timeline horizontal`
  </done>
</task>

<task type="auto" wave="2" id="W2-T4">
  <name>W2-T4: AlertasDoSeller.jsx (lista read-only — reusa visual do AlertaCard sem botão ack)</name>
  <files>
    resources/js/Pages/EmpresaAnaliseEcf/components/AlertasDoSeller.jsx (NOVO)
  </files>
  <behavior>
    - Arquivo NOVO `resources/js/Pages/EmpresaAnaliseEcf/components/AlertasDoSeller.jsx`
    - Prop: `signals` (array de `{id, eventType, severity, payload, detectedAt, ackedAt}`, default `[]`)
    - Importa `CheckCircle2, AlertTriangle` de `lucide-react`
    - Importa `formatDistanceToNow` de `date-fns` + `ptBR` de `date-fns/locale`
    - Importa `cn` de `@/lib/utils`
    - Importa `formatPayload` do AlertaCard.jsx Phase 23: `import { formatPayload } from '@/Pages/AlertasEstrategicos/components/AlertaCard';`
    - Constantes locais TYPE_LABELS, SEVERITY_LABELS, colorClasses (cópia adaptada do AlertaCard)
    - Renderiza:
      - Empty state quando vazio: "Sem alertas ativos para este seller."
      - Caso contrário: header com contagem + lista space-y-2 de cards
      - Cada card simplificado SEM botão ack:
        - Esquerda: dot colorido por severidade + severity label vertical
        - Centro: tipo + payload pt-BR (formatPayload) + tempo relativo
        - Direita: SE ackedAt, span "Já visto há X" (cinza). NÃO renderizar botão.
      - Header: "Mostrando {signals.length} alertas mais recentes deste seller. Para tomar ação (marcar como visto), acesse /alertas-estrategicos."
    - Comentários pt-BR — read-only por design.
  </behavior>
  <action>
    Criar `resources/js/Pages/EmpresaAnaliseEcf/components/AlertasDoSeller.jsx`.

    Imports conforme behavior. formatPayload do AlertaCard Phase 23 (reuso direto via named export).

    Constantes TYPE_LABELS (5 tipos), SEVERITY_LABELS (3), colorClasses (red/yellow/blue hardcoded) — cópia do AlertaCard.

    Componente AlertasDoSeller({ signals = [] }):
    1. Empty state pt-BR.
    2. Header explicativo.
    3. Lista de cards. Layout flex row: esquerda (dot + label severity vertical) + centro (tipo + payload + tempo relativo) + direita (ackedAt opcional).
    4. NÃO renderizar handleAck nem botão. NÃO importar router.

    try/catch em formatDistanceToNow para datas inválidas.

    Comentários pt-BR explicitando read-only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('resources/js/Pages/EmpresaAnaliseEcf/components/AlertasDoSeller.jsx','utf8'); if(!c.includes('formatPayload')||!c.includes('TYPE_LABELS')||!c.includes('Sem alertas ativos')||c.includes('handleAck')||c.includes('router.post')) throw new Error('AlertasDoSeller.jsx incompleto ou tem ack'); console.log('OK');"</automated>
  </verify>
  <done>
    - `AlertasDoSeller.jsx` existe com lista read-only
    - Importa `formatPayload` do AlertaCard.jsx (reuso)
    - 3 severity com cores hardcoded
    - SEM botão "Marcar como visto" (read-only)
    - Empty state pt-BR
    - Commit: `feat(phase-25): AlertasDoSeller lista read-only`
  </done>
</task>

<task type="auto" wave="2" id="W2-T5">
  <name>W2-T5: Show.jsx (página coordenadora) + AlertaCard.jsx link adaptado + npm run build</name>
  <files>
    resources/js/Pages/EmpresaAnaliseEcf/Show.jsx (NOVO),
    resources/js/Pages/AlertasEstrategicos/components/AlertaCard.jsx
  </files>
  <behavior>
    - Arquivo NOVO `resources/js/Pages/EmpresaAnaliseEcf/Show.jsx`:
      - Usa AppLayout + Head do Inertia
      - Lê props via usePage().props: empresa, seller, metricas, medalhas, signals, semCustId, naoEncontrada, erro
      - Estados condicionais (ordem de prioridade):
        1. semCustId === true: EmpresaHeader (seller=null) + card amarelo pt-BR amigável
        2. naoEncontrada === true: EmpresaHeader (seller=null) + card azul pt-BR informativo com cust_id literal
        3. erro !== null: banner vermelho + EmpresaHeader (sem seller)
        4. Caminho feliz: 5 seções (EmpresaHeader, KPIs, Evolução, Medalhas, Alertas)
      - Layout: bg ecf-bg, max-w-7xl, px-6 py-6, space-y-6
      - Comentários pt-BR
    - Editar AlertaCard.jsx (Phase 23):
      - Adicionar import Link de @inertiajs/react
      - Substituir o span do nome da empresa por Link href={route('empresas.analise-ecf', company.id)} preserveScroll mantendo classes visuais + hover:underline
      - Cliente externo (company === null) mantém span estático
    - Rodar npm run build no fim como sub-task última.
  </behavior>
  <action>
    Sub-task A — Criar Show.jsx conforme behavior acima.

    Imports: Head, usePage de @inertiajs/react; AppLayout de @/Layouts/AppLayout; EmpresaHeader, EvolucaoEmpresaChart, HistoricoMedalhas, AlertasDoSeller de ./components; KpiCard de @/Pages/PainelExecutivo/components/KpiCard (REUSO Phase 24); AlertTriangle, Info de lucide-react.

    Componente Show (export default):
    1. Lê props.
    2. AppLayout title=Análise ECF - empresa.name + Head title igual.
    3. Container px-6 py-6 max-w-7xl mx-auto space-y-6.
    4. SEMPRE renderiza EmpresaHeader no topo.
    5. Branch semCustId: card amarelo border-yellow-500/30 bg-yellow-500/10 com Info icon + mensagem pt-BR.
    6. Branch naoEncontrada: card azul border-blue-500/30 bg-blue-500/10 com Info icon + mensagem incluindo empresa.cust_id literal.
    7. Branch erro: banner vermelho border-red-500/30 bg-red-500/10 + AlertTriangle + mensagem da prop.
    8. Caminho feliz: 4 cards adicionais (Indicadores, Evolução, Medalhas, Alertas). KPIs lendo seller.metricaAtual (GMV, TSI, Visitas, Inv ADS, GMV ADS, Score Full, Score PADS). deltaPct null nesta phase (futura calcula MoM via metricas).

    Cada card: rounded-lg bg-ecf-card border border-white/[0.06] p-5 com h2 text-sm font-semibold text-white/80 mb-4.

    Sub-task B — Editar AlertaCard.jsx:
    1. Mudar import router para import Link + router de @inertiajs/react.
    2. Localizar bloco do nome da empresa (próximo a linha 108-116) e substituir span por Link com href via route helper empresas.analise-ecf.
    3. NÃO alterar mais nada (ack, formatPayload, etc).

    Sub-task C — npm run build: rodar e verificar sem erros novos.

    Comentários pt-BR. Commit único cobrindo Show + AlertaCard + build.
  </action>
  <verify>
    <automated>npm run build 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>
    - Show.jsx existe com 4 branches (semCustId/naoEncontrada/erro/caminho feliz)
    - Reusa KpiCard da Phase 24 (sem duplicar)
    - Caminho feliz: Header + 7 KPI cards + Evolução + Medalhas + Alertas
    - AlertaCard.jsx linka empresa para empresas.analise-ecf via Inertia Link
    - npm run build sem erros novos
    - Commit: feat(phase-25) Show + AlertaCard linka analise-ecf + build
  </done>
</task>

<task type="auto" wave="3" id="W3-T1">
  <name>W3-T1: EmpresaAnaliseEcfControllerTest (7+ testes — roles, carteira, empty states, fallback)</name>
  <files>
    tests/Feature/Phase25/EmpresaAnaliseEcfControllerTest.php (NOVO)
  </files>
  <behavior>
    - Arquivo NOVO em tests/Feature/Phase25/EmpresaAnaliseEcfControllerTest.php (criar diretório se não existir)
    - Namespace Tests\Feature\Phase25, extends Tests\TestCase, usa RefreshDatabase
    - Imports: App\Models\Company, App\Models\User, Illuminate\Support\Facades\Http, Inertia\Testing\AssertableInertia as Assert, RefreshDatabase
    - Helpers privados: criarEmpresaComCustId, criarEmpresaSemCustId, linkUserToCompany, mockSellerOk, mockSeller404, mockSellerErroGenerico
    - 7 testes mínimos:
      1. test_admin_acessa_200: admin + mock ok → 200 + props.seller != null + semCustId false + naoEncontrada false + erro null
      2. test_consultor_carteira_200: consultor linkado + mock ok → 200
      3. test_consultor_fora_carteira_403: consultor SEM link → 403 com mensagem pt-BR
      4. test_publicador_403: user role publicador (não está em admin/consultor/mentor) → 403 do middleware
      5. test_empresa_sem_cust_id_200_semCustId_true: empresa sem cust_id, admin → 200, props.semCustId true, Http::assertNothingSent
      6. test_cust_id_com_404_naoEncontrada_true: mockSeller404 → 200 com props.naoEncontrada true, erro null
      7. test_erro_generico_fallback: mockSellerErroGenerico → 200 com props.erro contendo mensagem pt-BR, naoEncontrada false
    - Comentários pt-BR
  </behavior>
  <action>
    Criar diretório tests/Feature/Phase25/ se não existir. Criar EmpresaAnaliseEcfControllerTest.php.

    Namespace Tests\Feature\Phase25. Extends TestCase. Use AssertableInertia as Assert. Use RefreshDatabase.

    Helpers privados:
    - criarEmpresaComCustId(array attrs = []): Company — usa Company::factory()->create(['adman_account_id' => '570267839', ...attrs]).
    - criarEmpresaSemCustId(): Company — Company::factory()->create(['adman_account_id' => null, 'ml_store_id' => null]).
    - linkUserToCompany(User user, Company company): void — user->companies()->attach com role consultor (confirmar shape do pivot via grep em testes existentes — Phase 14 ou Phase 19 já tem).
    - mockSellerOk(string custId): Http::fake com 4 padrões — ORDEM IMPORTA (mais específicos primeiro):
        - sellers/{custId}/metricas/mensal — 200 com data array mínimo (timMonthId, tgmv_lc, inv_pads, tsi, visitas, score_final_full, score_final_pads)
        - sellers/{custId}/medalhas — 200 com data array de 1 medalha
        - sellers/{custId}/signals — 200 com data: []
        - sellers/{custId} — 200 com custId, razaoSocial, programa CPP, medalhaAtual, metricaAtual
    - mockSeller404(custId): fake com sellers/{custId} retornando 404; demais retornando 200.
    - mockSellerErroGenerico(custId): fake com sellers/{custId} retornando 500.

    Implementar os 7 testes:
    1. Cada teste define Http::fake() ANTES do GET.
    2. Asserções via response->assertInertia com Assert page->component('EmpresaAnaliseEcf/Show')->where(...).
    3. test_empresa_sem_cust_id adiciona Http::assertNothingSent() (controller não chama wrapper).
    4. test_publicador_403: user com role publicador → assertStatus(403).
    5. test_consultor_fora_carteira_403: assertStatus(403). Mensagem pt-BR pode ser verificada via assertSeeText na response.

    Rodar vendor/bin/phpunit tests/Feature/Phase25/ --stop-on-failure — todos verdes.

    Comentários pt-BR. Cada teste tem docstring de 1 linha explicando o caso.
  </action>
  <verify>
    <automated>vendor/bin/phpunit tests/Feature/Phase25/ --stop-on-failure 2&gt;&amp;1 | tail -15</automated>
  </verify>
  <done>
    - tests/Feature/Phase25/EmpresaAnaliseEcfControllerTest.php existe com 7+ testes
    - Cobre 7 cenários conforme behavior
    - Http::fake mocka 4 endpoints do wrapper
    - Http::assertNothingSent no teste semCustId
    - vendor/bin/phpunit tests/Feature/Phase25/ verde
    - Commit: test(phase-25) EmpresaAnaliseEcfControllerTest 7 testes verdes
  </done>
</task>

<task type="checkpoint:human-verify" wave="4" id="W4-T1" gate="blocking">
  <name>W4-T1: Smoke visual em prod (3 empresas reais)</name>
  <what-built>
    - Rota /empresas/{company}/analise-ecf (autenticada admin/consultor/mentor com auth fina por carteira)
    - Controller com 4 chamadas ECF Drive (seller, sellerMetricasMensal, sellerMedalhas, sellerSignals) + try/catch global
    - Empty states: semCustId, naoEncontrada, erro generico
    - 5 secoes na pagina: Header com medalha + 7 KPI cards + Evolucao 12m + Historico Medalhas + Alertas read-only
    - AlertaCard.jsx (Phase 23) agora linka empresa para essa nova analise (nao mais Dashboard)
    - Refator de labels: CLUSTER/FRETE/PROGRAMA em lib/ecfDriveLabels.js (Phase 24 continua verde)
  </what-built>
  <how-to-verify>
    Apos deploy autorizado (autorizacao permanente registrada na memory):

    Cenario 1 - Empresa rica (RELOJOARIA WENUS, cust_id 570267839)
    1. Logar como admin em https://admin.ecfconsultoria.com.br
    2. Acessar /empresas/{id-da-relojoaria-wenus}/analise-ecf
    3. Esperar ate 8s na primeira carga (cold cache R-01)
    4. Verificar:
       - Header com nome RELOJOARIA WENUS, cust_id 570267839, programa CPP traduzido
       - Badge de medalha colorida (PLATINUM cyan / GOLD amarelo / SILVER cinza / BRONZE ambar) + timMonthId
       - 7 KPI cards renderizados (GMV em torno de R$ 2,7M conforme smoke Phase 22)
       - Grafico Evolucao 12m com 2 linhas (amarela GMV esquerda, azul Inv ADS direita)
       - Timeline de medalhas com scroll horizontal
       - Lista de alertas (pode estar vazia)
       - Links no header funcionam (Ver no Dashboard + Ver cadastro)
    5. Recarregar pagina (segunda carga deve ser <500ms cache quente)

    Cenario 2 - Empresa tipica (CAMILLO PARTS, cust_id 267873368)
    1. Acessar /empresas/{id-camillo-parts}/analise-ecf
    2. Verificar layout completo (mesmo checklist, valores menores)

    Cenario 3 - Empresa sem cust_id ML (Shopee/Amazon)
    1. Via Tinker: identificar empresa com marketplace shopee, sem adman_account_id e sem ml_store_id
    2. Acessar /empresas/{id}/analise-ecf
    3. Verificar:
       - Header com nome, cust_id como dash
       - Card amarelo com mensagem pt-BR amigavel
       - NAO renderiza KPI cards, grafico, medalhas, alertas
       - Carga rapida <300ms (sem chamadas externas)

    Cenario 4 - Link a partir dos Alertas Estrategicos
    1. Acessar /alertas-estrategicos
    2. Encontrar alerta com empresa resolvida
    3. Clicar no nome da empresa
    4. Verificar:
       - Navega via Inertia para /empresas/{id}/analise-ecf
       - Voltar para Alertas mantem scroll position (preserveScroll)
       - NAO navega mais para Dashboard

    Cenario 5 - Empresa fora da carteira (consultor)
    1. Logar como consultor real
    2. Identificar empresa que NAO esta na carteira desse consultor
    3. Tentar acessar /empresas/{id-fora}/analise-ecf direto via URL
    4. Verificar 403 com mensagem pt-BR
    5. Acessar empresa DA carteira: 200 OK

    Cenario 6 - Regressao Phase 24 + Phase 23
    1. Acessar /painel-executivo - tabs Programa/Frete/Cluster/Localidade trocam normalmente
    2. Verificar termos traduzidos continuam aparecendo (CPP, ME2, etc.)
    3. Acessar /alertas-estrategicos - cards renderizam, botao ack ainda funciona

    Reportar qual cenario passou/falhou e screenshots dos cenarios 1, 3, 4. Anotar latencia cold/warm cache.
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se todos os 6 cenarios passaram OU "deviation: cenario descricao" para registrar regressao/debito.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Laravel /empresas/{id}/analise-ecf | Usuario autenticado + role admin/consultor/mentor; auth fina por carteira via pivot company_users; route model binding garante ID inteiro |
| Laravel -> ECF Drive HTTP | API key em header X-Api-Key (config services.ecf.key); HTTPS obrigatorio; wrapper Phase 22 valida 2xx |
| AlertaCard -> rota empresas.analise-ecf (Ziggy) | Link Inertia gera URL via route() - XSS impossivel porque company.id e integer servidor-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | Acesso de consultor a empresa fora da carteira | mitigate | Auth fina inline com user companies()->where(companies.id, X)->exists() + abort 403 com mensagem pt-BR; teste W3 valida |
| T-25-02 | Information Disclosure | Acesso de publicador/analista/gestor/lider/guest a /empresas/{id}/analise-ecf | mitigate | Middleware role:admin,consultor,mentor no grupo de rota; testes W3 cobrem 403 publicador e 302 guest |
| T-25-03 | Denial of Service | Cold cache 4 chamadas sequenciais ~8s pode acumular | accept | Cache do wrapper absorve em segunda carga; carga executiva e baixa; aceitavel |
| T-25-04 | Tampering | URL manipulada com ID inexistente | mitigate | Route model binding retorna 404 automaticamente para Company inexistente |
| T-25-05 | Information Disclosure | Empresa sem cust_id chamando API com null | mitigate | Branch semCustId no controller ANTES de qualquer chamada wrapper; teste W3 com Http::assertNothingSent confirma |
| T-25-06 | Information Disclosure | Mensagens de erro do wrapper expondo stack/payload em UI | mitigate | Try/catch global retorna mensagem pt-BR amigavel; report(e) envia detalhes so ao log do servidor |
| T-25-07 | Repudiation | Read-only, sem mudanca de estado | accept | Pagina nao modifica nada (sem activity log necessario) |
| T-25-08 | Spoofing | Cliente externo (signal.custId sem company resolvida) | mitigate | AlertaCard mantem span estatico quando company === null; previne navegacao a /empresas/null/analise-ecf |
| T-25-09 | Elevation of Privilege | Consultor descobrindo nome de empresas fora da carteira via 403 vs 404 timing | accept | Mensagem 403 e generica (nao revela se empresa existe); aceitavel para uso interno |
</threat_model>

<verification>
Verificacoes automatizadas durante execucao:

- W1-T1: grep confirma extract dos labels + suite Phase 24 verde (zero regressao)
- W1-T2: php artisan route:list --name=empresas.analise-ecf mostra rota com middleware correto
- W2-T1 a W2-T4: node -e checks de existencia de imports/constantes nos JSX
- W2-T5: npm run build completa sem erros
- W3-T1: vendor/bin/phpunit tests/Feature/Phase25/ retorna verde (minimo 7 testes)

Verificacao manual (W4 - checkpoint humano blocking):
- 6 cenarios listados em W4-T1 cobrindo empresa rica, tipica, sem cust_id, link de Alertas, auth fina consultor, e regressao Phases 23+24
</verification>

<success_criteria>
1. Backend: 1 rota nova (empresas.analise-ecf) + controller com auth fina inline + 4 chamadas wrapper em try/catch global + branches semCustId/naoEncontrada/erro generico
2. Frontend: 5 componentes novos (Show + EmpresaHeader + EvolucaoEmpresaChart + HistoricoMedalhas + AlertasDoSeller) + lib/ecfDriveLabels.js extraido + AlertaCard.jsx linka para nova rota
3. Reuso: KpiCard da Phase 24 + formatPayload do AlertaCard Phase 23 + helpers Intl pt-BR (copia local)
4. Refator zero-risk: CLUSTER/FRETE/PROGRAMA_LABELS movidos para lib/ - Phase 24 continua verde
5. Empty states pt-BR amigaveis: 3 cenarios cobertos com mensagens nao-tecnicas
6. Auth: middleware role:admin,consultor,mentor + auth fina inline por carteira; teste W3 valida 403 consultor fora
7. Testes: minimo 7 testes verdes em tests/Feature/Phase25/
8. Build: npm run build sem warnings novos no fim do W2
9. Smoke W4: 6 cenarios validados em prod com 3 empresas reais + regressao Phases 23/24
</success_criteria>

<output>
Criar .planning/phases/25-analise-por-empresa-sellers-pagina-detail-360-graus-com-sell/25-01-SUMMARY.md ao concluir, registrando:
- Wave structure executada (4 waves, 9 tasks)
- Latencias cold/warm cache observadas no smoke W4
- Qualquer deviation registrada (especialmente se shape do seller() divergir do esperado)
- Proximos passos: Phase 26 (Webhooks HMAC) ou follow-ups (comparacao 2+ sellers, calcular delta MoM nos KPI cards via metricas)
</output>
