---
phase: 120-agrega-o-do-profissional-feature-flag-v21-0
plan: 03
type: execute
wave: 2
depends_on: ["120-01"]
files_modified:
  - app/Services/DesempenhoScoreService.php
  - tests/Feature/Phase120/AgregacaoProfissionalTest.php
  - tests/Feature/DesempenhoShopeeScoreTest.php
autonomous: true
requirements: [AGRE-01, AGRE-05, AGRE-06]

must_haves:
  truths:
    - "Com a flag ligada, nota_final e exatamente a media das nota_empresa das empresas com status complete, arredondada a 2 casas"
    - "Empresa partial, sem_fonte ou sem_dados fica FORA do denominador - nao entra com nota_empresa_parcial (D-01)"
    - "score_status deriva da cobertura de empresas complete sobre o universo: >= 0.7 official, abaixo partial, nenhuma empresa com nota alguma blocked (D-02/D-03)"
    - "Profissional so-Shopee continua official sem nenhum if especial para Shopee (AGRE-06)"
    - "computeNotaFinal e computeScoreStatus legados permanecem com assinatura e corpo intocados - o if escolhe qual par chamar, nunca uma funcao hibrida"
    - "Os 7 invariantes de DesempenhoShopeeScoreTest continuam verdes e intocados com a flag desligada; os 4 que dependem de margemPontos ganham cenarios espelho para flag-ligada"
    - "A flag permanece false ao fim da fase"
  artifacts:
    - path: "app/Services/DesempenhoScoreService.php"
      provides: "computeNotaFinalPorEmpresa, computeScoreStatusPorEmpresa, constante de cobertura e o if de bifurcacao"
      contains: "computeNotaFinalPorEmpresa"
    - path: "tests/Feature/Phase120/AgregacaoProfissionalTest.php"
      provides: "AGRE-01, AGRE-05 e AGRE-06 - media, denominador, cobertura, so-Shopee e escopo da carteira"
      contains: "performance_company_first_score"
    - path: "tests/Feature/DesempenhoShopeeScoreTest.php"
      provides: "Cenarios espelho D-05 para os 4 testes que dependem de margemPontos"
      contains: "performance_company_first_score"
  key_links:
    - from: "app/Services/DesempenhoScoreService.php"
      to: "computeNotaFinalPorEmpresa / computeScoreStatusPorEmpresa"
      via: "if (config(flag) && empresasScore !== null) no ponto de bifurcacao de compute()"
      pattern: "computeScoreStatusPorEmpresa"
---

<objective>
A bifurcação. Com a flag ligada, `nota_final` do profissional passa a ser a média das `nota_empresa` das empresas completas, e `score_status` passa a derivar da cobertura de empresas completas. É aqui que a milestone efetivamente muda o número que paga bônus — mas **atrás de uma flag que continua `false`**.

Purpose: as Fases 117, 118 e 119 foram todas aditivas. Este é o plano que liga o caminho novo ao motor, e que assume o risco herdado da Fase 119: régua-da-média não é média-das-réguas. O invariante que o docblock de `margemPontos()` declara — só-performance devolve exatamente `reguaMargem($varMargemReal)`, regressão zero — **não vale** no caminho novo. Isso é esperado, é o ponto da milestone, e significa que ligar a flag muda notas. Quantificar quanto é a Fase 121.

Output: `computeNotaFinalPorEmpresa()` e `computeScoreStatusPorEmpresa()` como métodos NOVOS e separados, o `if` de bifurcação em `compute()`, a suíte `AgregacaoProfissionalTest` cobrindo AGRE-01/05/06, e os cenários espelho no `DesempenhoShopeeScoreTest` (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-agrega-o-do-profissional-feature-flag-v21-0/120-CONTEXT.md
@.planning/phases/120-agrega-o-do-profissional-feature-flag-v21-0/120-PATTERNS.md
@.planning/phases/120-agrega-o-do-profissional-feature-flag-v21-0/120-VALIDATION.md
@.planning/phases/120-agrega-o-do-profissional-feature-flag-v21-0/120-01-SUMMARY.md
@.planning/phases/119-score-por-empresa-v21-0/119-04-SUMMARY.md
@app/Services/DesempenhoScoreService.php
@app/Services/Desempenho/CompanyScoreService.php
@tests/Feature/DesempenhoShopeeScoreTest.php

<interfaces>
Contratos disponíveis após o Plano 01. Usar diretamente, sem reexplorar o codebase.

app/Services/DesempenhoScoreService.php:
  compute(User $user, Carbon $mesReferencia, ?array $periodoOverride = null, bool $incluirEmpresasScore = false): array
  // $empresasScore ja e resolvido logo apos o filtro de $invalidadas, quando
  // $incluirEmpresasScore || config('metrics.performance_company_first_score')

  LEGADOS, NUNCA TOCAR (assinatura nem corpo):
  691  private function computeScoreStatus(array $contadores, ?float $varFat, ?float $margemPontos): string
  1258 private function computeNotaFinal(?float $nps, ?float $varFat, ?float $margemPontos): ?float
  1348 private function margemPontos(?float $varMargemReal, int $nComMargemReal, int $nShopeePlaceholder): ?float

  PONTO DE BIFURCACAO (linhas ~462 e ~465, hoje sem nenhum if):
    $nota        = $this->computeNotaFinal($nps, $varFat, $margemPontos);
    $scoreStatus = $this->computeScoreStatus($contadores, $varFat, $margemPontos);
    if ($scoreStatus === 'blocked') { $nota = null; }   // D-91-01, preservado nos DOIS ramos

Linha por empresa devolvida por CompanyScoreService::computeEmpresasScore() —
Collection<int, object> chaveada por company_id, campos relevantes aqui:
  status               string: 'complete' | 'partial' | 'sem_fonte' | 'sem_dados'
  nota_empresa         ?float — estrita, null se faltar 1 dos 3 componentes
  nota_empresa_parcial ?float — media dos componentes PRESENTES
  margem_var_pp        ?float
  company_id           int

Patamar de cobertura (D-03) — mesmo valor de:
  app/Console/Commands/ConsolidarMesDesempenho.php:76
  private const MARGEM_COBERTURA_MINIMA_CONGELAMENTO = 0.7;
</interfaces>
</context>

<invariantes_da_fase>
1. **A FLAG NASCE `false` E NÃO É LIGADA NESTA FASE.** O gate MPP-04 está `reprovado` (`cobertura_prev = 0.6415`, 5 rodadas). Ligar depende dele e do delta da Fase 121. Nenhuma entrada em `.env` ou `.env.example`.
2. **Nenhum método legado recebe `$empresasScore` nem lê a flag.** Só `compute()`, o orquestrador, lê a flag — uma única vez, para decidir qual par de funções chamar.
3. **Se o plano precisar de um `if` para Shopee, o desenho está errado.** Empresa Shopee é `complete` pelo placeholder (D-02 da Fase 119), então só-Shopee tem cobertura 100% e é `official` sem código especial. É assim que a trava da Fase 109 é preservada.
4. **Alterar qualquer um dos 7 invariantes de `DesempenhoShopeeScoreTest` para fazer algo passar é falha, não solução.**
5. **Árvore de trabalho compartilhada:** `git commit -- <paths>` explícitos, nunca `git add -A`, nunca `state.advance-plan`.
6. **Ambiente:** PHP em `C:\xampp\php\php.exe`. NUNCA `artisan test` sem `--filter`. Baseline de 14 falhas em `--filter=Desempenho`.
</invariantes_da_fase>

<tasks>

<task type="auto">
  <name>Task 1: computeNotaFinalPorEmpresa, computeScoreStatusPorEmpresa e o if de bifurcação (AGRE-01, AGRE-05, AGRE-06)</name>
  <files>app/Services/DesempenhoScoreService.php</files>
  <action>
**(a) Constante de cobertura.** Declarar, junto às demais constantes de classe, `private const COBERTURA_MINIMA_SCORE_STATUS = 0.7;` com docblock em pt-BR registrando a D-03: o valor é deliberadamente o MESMO de `ConsolidarMesDesempenho::MARGEM_COBERTURA_MINIMA_CONGELAMENTO`, que já governa a recusa de congelar snapshot degradado, para o sistema não passar a ter dois conceitos concorrentes de cobertura suficiente. A constante do comando é `private`, então o valor é duplicado de propósito — o docblock é o que amarra os dois; citar o arquivo e a linha (`app/Console/Commands/ConsolidarMesDesempenho.php:76`). Registrar também que o usuário não fixou este número e que ele é fácil de sobrepor.

**(b) `computeNotaFinalPorEmpresa(Collection $empresasScore): ?float`** — método NOVO e SEPARADO, nunca uma extensão de `computeNotaFinal()`. Filtrar as linhas com `status === 'complete'`; se nenhuma sobrar, devolver `null`; senão devolver a média de `nota_empresa` das completas, arredondada a 2 casas.

D-01, que o docblock precisa explicar: empresa `partial`, `sem_fonte` ou `sem_dados` fica FORA do denominador — **não entra com `nota_empresa_parcial`**. Entrar com a parcial misturaria empresa medida por 3 dimensões com empresa medida por 1, e a incompleta poderia até puxar a nota para CIMA (4,80 contra os 4,53 do caso âncora). A leitura literal do plano §3.4 — qualquer incompleta derruba o profissional para `partial` — foi descartada porque empresa sem baseline é caso comum neste projeto (`adman_metrics` começa por volta de 21/05; Shopee não tem baseline antes de 01/06): quase toda carteira cairia em `partial` e o status pararia de distinguir qualquer coisa. **A guarda de cobertura do item (c) é o que impede o abuso** de um profissional com 10 empresas e 2 completas ser julgado por 2 com aparência de nota oficial.

**(c) `computeScoreStatusPorEmpresa(Collection $empresasScore): string`** — também NOVO e SEPARADO; `computeScoreStatus()` legado não pode ganhar parâmetro nem mudar de corpo (Pitfall 3 do RESEARCH). Regras, na ordem:
  1. `blocked` — nenhuma empresa com nota alguma, ou seja, `nota_empresa_parcial` é `null` em todas. Caso extremo.
  2. Cobertura igual a empresas com `status === 'complete'` dividido pelo total de linhas do `$empresasScore` — o total é o universo elegível inteiro, incluindo as `sem_fonte`, que a D-03 da Fase 119 lista de propósito.
  3. `official` quando cobertura maior ou igual a `self::COBERTURA_MINIMA_SCORE_STATUS`, senão `partial`.

Docblock em pt-BR contrastando com o legado: a versão atual olha dois sinais agregados da carteira inteira (`$varFat` e `$margemPontos`, cada um presente ou nulo); esta olha a DISTRIBUIÇÃO de status por empresa — é métrica de cobertura, não de presença binária. E registrar o caso concreto: 26 empresas com 20 completas dá 77% e `official`; com 15 completas dá 58% e `partial`. E o AGRE-06: profissional só-Shopee tem todas as empresas `complete`, porque o placeholder de margem 1.0 conta como componente presente (D-02 da Fase 119), logo cobertura 100% e `official` — **sem nenhum `if` especial para Shopee**.

**(d) O `if` de bifurcação**, substituindo APENAS as duas linhas ~462 e ~465:

    if (config('metrics.performance_company_first_score') && $empresasScore !== null) {
        $nota        = $this->computeNotaFinalPorEmpresa($empresasScore);
        $scoreStatus = $this->computeScoreStatusPorEmpresa($empresasScore);
    } else {
        $nota        = $this->computeNotaFinal($nps, $varFat, $margemPontos);
        $scoreStatus = $this->computeScoreStatus($contadores, $varFat, $margemPontos);
    }

Nunca uma função híbrida: o `if` escolhe qual par chamar, e as duas árvores de chamada permanecem fisicamente separadas. Os quatro componentes legados continuam sendo SEMPRE calculados e SEMPRE reportados no payload, nos dois ramos — AGRE-04 preserva `pontos_componentes.margem`, `componentes.var_margem_pct` e companhia mesmo com a flag ligada; é isso que dá à Fase 121 os dois números lado a lado para comparar.

A linha seguinte, `if ($scoreStatus === 'blocked') { $nota = null; }` (D-91-01), fica exatamente onde está e passa a valer para os dois ramos.

**(e) Comentário de bloco em pt-BR acima do `if`**, registrando o risco herdado que a Fase 121 vai quantificar: `margemPontos()` aplica a régua UMA vez sobre a média agregada da carteira e pondera os placeholders Shopee por contagem; o caminho novo aplica a régua POR EMPRESA e promedia depois. Régua-da-média não é média-das-réguas. O invariante declarado no docblock de `margemPontos()` — só-performance devolve exatamente `reguaMargem($varMargemReal)`, regressão zero — **não vale neste ramo**, e isso é esperado. Ligar a flag muda notas; a Fase 121 mede quanto, e o gate MPP-04 (hoje `reprovado`) mais a aprovação do delta são pré-requisitos da ativação.

**Nada mais no arquivo muda.** Não aposentar `margemPontos()`, `reguaFaturamento()` nem `reguaMargem()` — enquanto a flag existir com dois caminhos, eles são o caminho de produção. A unificação das réguas duplicadas (débito da C-03 da Fase 119) está explicitamente diferida.
  </action>
  <verify>
    <automated>cd "C:/xampp/htdocs/ecf_admin/ecf_admin" && grep -q "computeNotaFinalPorEmpresa" app/Services/DesempenhoScoreService.php && grep -q "computeScoreStatusPorEmpresa" app/Services/DesempenhoScoreService.php && test "$(git diff -- app/Services/DesempenhoScoreService.php | grep -c '^-.*private function computeScoreStatus(array')" = "0" && test "$(git diff -- app/Services/DesempenhoScoreService.php | grep -c '^-.*private function computeNotaFinal(')" = "0" && C:/xampp/php/php.exe artisan test --filter="PayloadBaselineFlagOffTest|DesempenhoShopeeScoreTest|ShadowRoteamentoTest"</automated>
  </verify>
  <done>Os dois métodos novos existem, a constante de cobertura tem docblock amarrando a D-03 ao valor do `ConsolidarMesDesempenho`, o `if` de bifurcação está no lugar das duas linhas, as assinaturas legadas de `computeNotaFinal` e `computeScoreStatus` não foram tocadas, e com a flag desligada nada muda: teste dourado e os 7 invariantes verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Provar a agregação, o denominador, a cobertura e o escopo (AGRE-01, AGRE-05, AGRE-06)</name>
  <files>tests/Feature/Phase120/AgregacaoProfissionalTest.php</files>
  <action>
Criar a suíte em `Tests\Feature\Phase120`, `use RefreshDatabase;`, atributos `#[Test]`. Reusar o molde de fixture já estabelecido no `PayloadBaselineFlagOffTest` do Plano 01: stub do `MetricsProviderFactory`, `Http::preventStrayRequests()` mais `Http::fake()` 404, trait `CriaCenarioResponsaveis`, helpers de user com cargo em `user_setores` e de empresa. Ativar o caminho novo em cada teste com `config(['metrics.performance_company_first_score' => true])` e chamar `compute()` direto, nunca `computeCached()`, para não haver interferência de cache.

Testes:

  1. `test_nota_final_e_exatamente_a_media_das_notas_das_empresas_completas` (AGRE-01) — carteira com 2 ou 3 empresas todas `complete`, com `nota_empresa` distintas. Assertar que `nota_final` bate com a média aritmética dessas `nota_empresa`, arredondada a 2 casas, calculada no próprio teste a partir de `$r['empresas_score']` — e não com um literal solto, para o teste não virar tautologia de uma constante. Assertar também que `score_status` é `official`, cobertura 100%.

  2. `test_empresa_incompleta_fica_fora_do_denominador_e_nao_puxa_a_nota_para_cima` (AGRE-05, D-01) — o caso concreto do CONTEXT: uma empresa `complete` na faixa do caso âncora (nota por volta de 4,53) e uma empresa `partial` com `nota_empresa_parcial` mais ALTA (por volta de 4,80, NPS mais faturamento presentes e margem ausente). Assertar que `nota_final` é a nota da completa, não a média das duas — se a parcial entrasse, a nota subiria, que é exatamente o abuso que a D-01 fecha. Assertar `nota_empresa` da parcial igual a `null` e `nota_empresa_parcial` não nulo, provando que a linha existe no payload e só não conta no denominador.

  3. `test_cobertura_abaixo_do_patamar_derruba_para_partial_sem_zerar_a_nota` (AGRE-05, D-02/D-03) — carteira em que menos de 70% das empresas são `complete` (por exemplo 3 completas em 10, cobertura 30%). Assertar `score_status` igual a `partial` e `nota_final` diferente de `null` — a nota continua sendo a média das completas; o que muda é a etiqueta de confiança. E um segundo cenário com cobertura acima do patamar (por exemplo 8 de 10, 80%) assertando `official`. Comentário no teste registrando o caso do CONTEXT: 20 de 26 é 77% e passa; 15 de 26 é 58% e não passa.

  4. `test_so_shopee_continua_official_sem_codigo_especial` (AGRE-06) — carteira 100% Shopee. Assertar `score_status` igual a `official` e `nota_final` diferente de `null`, e que todas as linhas de `empresas_score` têm `status` igual a `complete`, porque o placeholder de margem 1.0 conta como componente presente. Comentário registrando que a trava da Fase 109 é preservada **sem exceção especial** e que, se este teste exigisse um `if` para Shopee no serviço, o desenho estaria errado.

  5. `test_blocked_so_quando_nenhuma_empresa_tem_nota_alguma` (D-02) — carteira em que todas as empresas têm `nota_empresa_parcial` igual a `null`. Assertar `score_status` igual a `blocked` e `nota_final` igual a `null`, este último via a linha D-91-01 que já existia e agora vale para os dois ramos.

  6. `test_agregacao_nao_sai_do_escopo_da_carteira_nem_reabsorve_empresa_invalidada` — ameaças T-120-06 e T-120-07. Criar uma empresa vinculada a OUTRO profissional e uma empresa da própria carteira invalidada na competência via `BonusInvalidacao`. Assertar que os `company_id` de `empresas_score` são exatamente o conjunto esperado: a empresa de outro profissional está ausente e a invalidada está ausente. É o que garante que a média não inclui empresa que não é do profissional nem empresa que a competência já excluiu do bônus.

  7. `test_flag_nasce_desligada_por_default` — sem nenhum `config([...])` de override, assertar `assertFalse(config('metrics.performance_company_first_score'))`. Trivial e barato, e é o gate que pega alguém mudando o default no config por engano (T-120-03).

Docblock de classe em pt-BR explicando que esta suíte cobre AGRE-01, AGRE-05 e AGRE-06 do `REQUIREMENTS-v21.md`, e que todos os cenários rodam com a flag ligada por `config()` dentro do teste — a flag de produção continua `false`.
  </action>
  <verify>
    <automated>cd "C:/xampp/htdocs/ecf_admin/ecf_admin" && C:/xampp/php/php.exe artisan test --filter=AgregacaoProfissionalTest && C:/xampp/php/php.exe artisan test --filter=PayloadBaselineFlagOffTest</automated>
  </verify>
  <done>Os sete testes passam. A média usa só as completas, a cobertura de 70% governa `official` contra `partial`, só-Shopee é `official` sem nenhum ramo especial de Shopee nos dois métodos novos, `blocked` só no caso extremo, o escopo da carteira e a invalidação por competência são respeitados, e a flag continua `false` por default.</done>
</task>

<task type="auto">
  <name>Task 3: Cenários espelho no DesempenhoShopeeScoreTest (D-05, gate no 3) e fechamento da fase</name>
  <files>tests/Feature/DesempenhoShopeeScoreTest.php</files>
  <action>
Acrescentar ao arquivo — **acrescentar, nunca reescrever** — os cenários espelho da D-05, e provar que os 7 invariantes originais continuam intactos.

Contexto do alvo, precisado pela pesquisa: dos 7 testes existentes, **4 dependem de `margemPontos()`** e por isso não generalizam para o caminho novo, porque ele deriva `margem_pontos` por empresa dentro do `CompanyScoreService` e agrega por média das notas, não por blend ponderado por contagem:
  - `test_so_performance_regressao_zero_margem_pontos_e_nota_identicos_ao_baseline` — assere `pontos_componentes.margem` igual a 4.0 e `nota_final` igual a 3.00;
  - `test_so_shopee_official_nota_final_nao_null_margem_placeholder_1` — assere `pontos_componentes.margem` igual a 1.0;
  - `test_misto_ml_shopee_margem_pontos_blend_ponderado` — assere 2.50;
  - `test_invalidacao_empresa_shopee_nao_infla_denominador_do_blend` — assere 2.50 pós-invalidação.
Os outros 3 — fonte financeira, dispatcher e cacheKey — valem nos dois modos e não precisam de espelho.

Para cada um dos 4, criar um método NOVO no mesmo arquivo, com sufixo que identifique o modo (por exemplo `..._com_flag_ligada`), duplicando o setup do original, ativando `config(['metrics.performance_company_first_score' => true])` dentro do teste e chamando `compute()` (não `computeCached()`, para não interferir no cache do teste legado). Cada espelho assere:
  - `nota_final` e `score_status` do caminho novo. **Rodar uma vez, capturar o valor real produzido e congelá-lo como literal**, com comentário explicando de onde ele vem (a média das `nota_empresa`). Onde divergir do original — e a divergência é esperada, é o ponto da milestone — o comentário precisa dizer explicitamente qual é o valor do caminho antigo, qual é o do novo, e que a diferença é a granularidade da régua, não regressão. O `119-04-SUMMARY.md` já traz o caso reproduzido: empresa A a −20% e empresa B a +2% dão 2,5 no caminho novo (média das réguas) contra 1,0 no antigo (régua da média de −9%);
  - que `pontos_componentes.margem` e `componentes.var_margem_pct` **continuam presentes e com o MESMO valor do teste original**, mesmo com a flag ligada. É a prova viva do AGRE-04: os quatro componentes legados continuam sendo calculados e reportados nos dois ramos, e é isso que dá à Fase 121 os dois números lado a lado;
  - no espelho da invalidação, que a empresa invalidada está ausente de `empresas_score`.

**Gate no 3 — os 7 originais ficam intocados.** O único que mudou nesta fase foi o do `cacheKey()`, e isso já aconteceu na Task 2 do Plano 01. A partir do commit do Plano 01, o diff deste arquivo pode conter **apenas linhas adicionadas**. Se algum dos 4 originais começar a falhar por causa da bifurcação, isso significa que o ramo da flag desligada foi contaminado — parar, não ajustar o valor esperado, e investigar. Alterar qualquer um dos 7 para fazer algo passar é falha, não solução.

Rodar, por fim, a regressão ampla e comparar contra a baseline de 14 falhas.
  </action>
  <verify>
    <automated>cd "C:/xampp/htdocs/ecf_admin/ecf_admin" && test "$(git diff -- tests/Feature/DesempenhoShopeeScoreTest.php | grep -c '^-[^-]')" = "0" && C:/xampp/php/php.exe artisan test --filter=DesempenhoShopeeScoreTest && C:/xampp/php/php.exe artisan test --filter=Phase120 && C:/xampp/php/php.exe artisan test --filter=Desempenho 2>&1 | tail -20</automated>
  </verify>
  <done>O diff deste arquivo tem zero linhas removidas — só adições. Os 7 invariantes originais continuam verdes com a flag desligada, os 4 espelhos passam com os valores do caminho novo congelados e documentados, `--filter=Phase120` verde e `--filter=Desempenho` dentro da baseline de 14 falhas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Config de produção → cálculo que paga bônus | Ligar a flag troca o número que vira faixa de bonificação, sem passar por nenhum outro controle |
| Universo da carteira → denominador da média | Toda empresa que entra em `empresas_score` entra na nota do profissional |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Plano de mitigação |
|-----------|-----------|------------|------------|--------------------|
| T-120-03 | Elevation of Privilege | Flag ligada em produção antes do gate MPP-04 e do delta da Fase 121 | mitigate | Default `false` no config; teste 7 da Task 2 assere `config(...) === false` sem override; nenhuma entrada em `.env`/`.env.example`; comentário de bloco acima do `if` e o SUMMARY reafirmam os dois pré-requisitos de ativação |
| T-120-06 | Information Disclosure | Escopo da agregação — empresa fora da carteira entrando na média | mitigate | `computeNotaFinalPorEmpresa()` e `computeScoreStatusPorEmpresa()` só REDUZEM a Collection recebida; nunca consultam banco nem reabrem o universo. O universo vem de `CompanyScoreService`, que usa `CarteiraContextService::forUser()`. Teste 6 da Task 2 assere o conjunto exato de `company_id` |
| T-120-07 | Tampering | Empresa invalidada na competência voltando a contar no bônus | mitigate | `compute()` passa `$invalidadas` já resolvido para `computeEmpresasScore()`; teste 6 da Task 2 e o espelho da invalidação na Task 3 asserem a ausência da empresa em `empresas_score` |
| T-120-10 | Repudiation | Nota nova sem rastro de como foi composta | mitigate | `empresas_score` fica no payload nos dois ramos, com `status`, `quality.motivos` e os três componentes por empresa; `consolidar-mes` o persiste em `breakdown_json` (Plano 02) |
| T-120-SC | Tampering | Instalação de pacotes (npm/composer) | accept | N/A — esta fase não roda `composer require` nem `npm install`; nenhum pacote novo a auditar |
</threat_model>

<verification>
1. `C:/xampp/php/php.exe artisan test --filter=Phase120` — verde (as três suítes da fase).
2. `C:/xampp/php/php.exe artisan test --filter=DesempenhoShopeeScoreTest` — os 7 originais mais os 4 espelhos, todos verdes.
3. `git diff -- tests/Feature/DesempenhoShopeeScoreTest.php | grep '^-[^-]'` — zero linhas nesta task.
4. `C:/xampp/php/php.exe artisan test --filter=Desempenho` — exatamente 14 falhas, a baseline.
5. `grep -n "performance_company_first_score" app/Services/DesempenhoScoreService.php` — exatamente duas ocorrências: a condição do shadow (Plano 01) e a condição da bifurcação (Task 1 deste plano). Nenhuma dentro de método legado.
6. `grep -rn "PERFORMANCE_COMPANY_FIRST_SCORE" .env .env.example` — zero linhas.
</verification>

<success_criteria>
- AGRE-01 fechado: com a flag ligada, `nota_final` é a média das `nota_empresa` das empresas `complete`, arredondada a 2 casas.
- AGRE-05 fechado: empresa incompleta fora do denominador, e a guarda de cobertura de 70% impede que uma carteira mal coberta pareça oficial.
- AGRE-06 fechado: só-Shopee continua `official`, sem nenhum ramo de código específico para Shopee.
- Gate no 1 continua verde: a flag desligada não moveu nenhum número.
- Gate no 3 fechado: os 7 invariantes intocados e verdes, 4 espelhos acrescentados.
- `--filter=Desempenho` dentro da baseline de 14 falhas.
- A flag permanece `false`.
</success_criteria>

<output>
Criar `.planning/phases/120-agrega-o-do-profissional-feature-flag-v21-0/120-03-SUMMARY.md` ao concluir. O SUMMARY é obrigado a conter:

(a) **Reafirmação explícita de que a flag fica desligada** até o gate MPP-04 aprovar (hoje `reprovado`, `cobertura_prev = 0.6415`) E o delta da Fase 121 ser aceito pelo usuário. Testes verdes não liberam a ativação.

(b) **O risco régua-da-média contra média-das-réguas**, herdado do `119-04-SUMMARY.md`: o invariante do docblock de `margemPontos()` não vale no caminho novo, ligar a flag muda notas, e quantificar quanto é a Fase 121. Registrar os pares de valores antigo contra novo capturados pelos 4 cenários espelho — eles são o primeiro dado numérico concreto da divergência e a Fase 121 vai partir deles.

(c) A lista do que ficou **diferido** de propósito: aposentar `margemPontos()` e unificar `reguaFaturamento`/`reguaMargem` (só quando a flag virar permanente), persistir `empresas_score` de forma estruturada (Fase 122) e exibir a lista de empresas com nota (Fase 123).
</output>
