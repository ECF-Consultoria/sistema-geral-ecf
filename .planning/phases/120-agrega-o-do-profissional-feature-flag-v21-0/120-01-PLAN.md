---
phase: 120-agrega-o-do-profissional-feature-flag-v21-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Phase120/PayloadBaselineFlagOffTest.php
  - app/Services/DesempenhoScoreService.php
  - config/metrics.php
  - tests/Feature/DesempenhoShopeeScoreTest.php
  - tests/Feature/Phase116/NpsFloorDesempenhoTest.php
  - tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php
  - tests/Feature/V18/DesempenhoMetadadosCacheTest.php
autonomous: true
requirements: [AGRE-02, AGRE-03, AGRE-04]

must_haves:
  truths:
    - "Existe um teste dourado que congela em literais o payload de compute() como ele e HOJE, escrito e verde ANTES de qualquer edicao em DesempenhoScoreService.php"
    - "A chave de cache sobe uma versao com docblock de justificativa nos 4 elementos da disciplina, e nenhum literal da versao antiga sobra em app/ ou tests/"
    - "config('metrics.performance_company_first_score') existe, nasce false e e lido so via config(), nunca env() em runtime"
    - "compute() e computeCached() aceitam bool incluirEmpresasScore = false; os ~40 call-sites existentes nao mudam uma linha"
    - "Com o shadow ligado o payload ganha empresas_score (raiz) e componentes.var_margem_pp, e NENHUMA chave legada some ou muda de tipo"
    - "shapeSemCarteira() ganha as duas chaves novas em simetria com o shape normal"
    - "nota_final e score_status continuam saindo exclusivamente do caminho legado neste plano"
  artifacts:
    - path: "tests/Feature/Phase120/PayloadBaselineFlagOffTest.php"
      provides: "Gate no 1 do VALIDATION - byte-equivalencia do caminho flag-desligada, substituto do sha256sum das Fases 117-119"
      contains: "CHAVES_LEGADAS"
    - path: "config/metrics.php"
      provides: "Feature flag performance_company_first_score, default false"
      contains: "performance_company_first_score"
    - path: "app/Services/DesempenhoScoreService.php"
      provides: "Parametro de shadow, payload aditivo e bump de cacheKey"
      contains: "incluirEmpresasScore"
  key_links:
    - from: "app/Services/DesempenhoScoreService.php"
      to: "app/Services/Desempenho/CompanyScoreService.php"
      via: "injecao no construtor + chamada condicional a computeEmpresasScore()"
      pattern: "companyScoreService->computeEmpresasScore"
    - from: "app/Services/DesempenhoScoreService.php"
      to: "config/metrics.php"
      via: "config('metrics.performance_company_first_score')"
      pattern: "metrics\\.performance_company_first_score"
---

<objective>
Armar a rede de proteção que substitui o gate de hash das Fases 117-119 e, só depois, abrir a superfície **aditiva** desta fase: a feature flag (nascendo `false`), o parâmetro de shadow, as duas chaves novas do payload e o bump da chave de cache.

**Este plano NÃO bifurca a nota.** `nota_final` e `score_status` continuam saindo exclusivamente de `computeNotaFinal()`/`computeScoreStatus()` legados. A bifurcação é do Plano 03. Aqui a única coisa que pode mudar no payload é a **presença** de duas chaves novas — nenhum número.

Purpose: as Fases 117-119 mantiveram `DesempenhoScoreService.php` byte-a-byte intocado e comparavam `sha256sum` a cada task — rede que pegou erros reais (literal errado de régua, contagem de rodadas, cobertura agregada). Esta é a primeira fase que modifica o arquivo de propósito, então a rede cai. O teste dourado da Task 1 é o que a substitui: se a flag desligada mudar qualquer número, ele reprova antes de a flag sequer ser ligada.

Output: `tests/Feature/Phase120/PayloadBaselineFlagOffTest.php`, a flag em `config/metrics.php`, o parâmetro de shadow + payload aditivo + bump de `cacheKey()` em `DesempenhoScoreService.php`, e os literais de cache atualizados nas 4 suítes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-agrega-o-do-profissional-feature-flag-v21-0/120-CONTEXT.md
@.planning/phases/120-agrega-o-do-profissional-feature-flag-v21-0/120-PATTERNS.md
@.planning/phases/120-agrega-o-do-profissional-feature-flag-v21-0/120-VALIDATION.md
@app/Services/DesempenhoScoreService.php
@app/Services/Desempenho/CompanyScoreService.php
@config/metrics.php
@tests/Feature/DesempenhoShopeeScoreTest.php

<interfaces>
Contratos que o executor precisa, extraídos do código atual em 2026-07-29.
Usar diretamente — não explorar o codebase para redescobrir.

app/Services/Desempenho/CompanyScoreService.php linha 120:
  public function computeEmpresasScore(User $user, Carbon $mes, array $periodo, ?Collection $invalidadas = null): Collection

  Devolve Collection<int, object> chaveada por company_id. Cada linha expõe:
    company_id, company_name, fonte_financeira,
    nps_pontos,
    faturamento_atual, faturamento_anterior, faturamento_var_pct, faturamento_pontos,
    margem_pct_atual, margem_pct_anterior, margem_var_pp, margem_pontos,
    componentes_presentes (int),
    nota_empresa (?float, estrita — null se faltar 1 dos 3 componentes),
    nota_empresa_parcial (?float, média dos componentes PRESENTES),
    status (string: complete | partial | sem_fonte | sem_dados),
    quality (revenue_diff_source, margin_diff_source, margin_source, motivos array)

  mesFechado NAO e parametro — deriva de (bool) (periodo['is_closed'] ?? false).
  invalidadas NULL resolve para BonusInvalidacao::companyIdsInvalidadas(mes), nunca collect() vazio.

app/Services/DesempenhoScoreService.php — âncoras de linha confirmadas hoje:
  118  __construct com 6 dependências promovidas
  228  computeCached(User $user, Carbon $mesReferencia, ?array $periodoOverride = null): array
  284  return Cache::remember($cacheKey, $ttl, fn () => $this->compute(...))
  302  cacheKey(int $userId, Carbon $mes): string
  335  return sprintf('desempenho.compute.v12.%d.%s', $userId, $periodKey)
  386  computeOficial(User $user): array — delega a compute($user, $mes, $periodo)
  394  compute(User $user, Carbon $mesReferencia, ?array $periodoOverride = null): array
  ~421 fim do filtro de $invalidadas — PONTO DE INSERCAO do shadow
  ~462 $nota = $this->computeNotaFinal(...) — INTOCADO neste plano
  ~465 $scoreStatus = $this->computeScoreStatus(...) — INTOCADO neste plano
  691  computeScoreStatus(array $contadores, ?float $varFat, ?float $margemPontos): string
  1258 computeNotaFinal(?float $nps, ?float $varFat, ?float $margemPontos): ?float
  1348 margemPontos(?float $varMargemReal, int $nComMargemReal, int $nShopeePlaceholder): ?float
  1448 shapeSemCarteira(User $user, Carbon $mes, string $motivo): array
</interfaces>
</context>

<invariantes_da_fase>
Valem para TODAS as tasks deste plano e dos planos 02 e 03.

1. **A flag nasce `false` e não é ligada nesta fase.** O gate MPP-04 está `reprovado`. Nada de `PERFORMANCE_COMPANY_FIRST_SCORE` em `.env` ou `.env.example`.
2. **Dois sinais independentes (C-01).** A flag decide QUAL resultado vira `nota_final`. O parâmetro `$incluirEmpresasScore` decide SE `CompanyScoreService` roda. Nenhum método legado (`computeNotaFinal`, `computeScoreStatus`, `computeVarFaturamento`, `computeVarMargem`, `computeNpsWindow`, `margemPontos`) pode receber `$empresasScore` nem ler a flag internamente. Só `compute()`, o orquestrador, lê a flag.
3. **AGRE-04 — nenhuma chave legada some ou muda de tipo.**
4. **Árvore de trabalho compartilhada.** Commitar SEMPRE com `git commit -- <paths>` explícitos. NUNCA `git add -A`. Não rodar `state.advance-plan`.
5. **Ambiente.** PHP em `C:\xampp\php\php.exe`. NUNCA `artisan test` sem `--filter`. Baseline conhecida: **14 falhas pré-existentes** em `--filter=Desempenho` — o critério é zero falhas NOVAS.
</invariantes_da_fase>

<tasks>

<task type="auto">
  <name>Task 1: Teste dourado — congelar em literais o payload de hoje (gate no 1) ANTES de tocar o serviço</name>
  <files>tests/Feature/Phase120/PayloadBaselineFlagOffTest.php</files>
  <action>
Criar a suíte que substitui o gate de `sha256sum` das Fases 117-119. Ela roda contra o código ATUAL, sem nenhuma edição em `DesempenhoScoreService.php` — esta task é a primeira do plano exatamente por isso.

Namespace `Tests\Feature\Phase120`, `use RefreshDatabase;`, atributos `#[Test]` do PHPUnit 11 (nunca docblock `/** @test */`). Reusar integralmente o molde de setup de `tests/Feature/DesempenhoShopeeScoreTest.php`: `DB::statement('PRAGMA foreign_keys = ON')`, `Carbon::setTestNow()`, stub local do `MetricsProviderFactory`, `Http::preventStrayRequests()` mais `Http::fake()` devolvendo 404 nos endpoints Adman, trait `Tests\Feature\V16\CriaCenarioResponsaveis`, e os helpers `criarUserComCargo()` e `criarEmpresa()` copiados de lá (setor mais cargo `analista` em `user_setores`). Zero HTTP real.

Montar UMA fixture rica e determinística, com um profissional e três empresas na carteira:
  (a) empresa Adman com faturamento e margem reais nas duas janelas, que entra completa;
  (b) empresa Shopee via `ShopeeMetric`, que exercita o placeholder de margem 1.0 e o blend por contagem de `margemPontos()`;
  (c) empresa Adman SEM baseline na janela anterior, que exercita o ramo de componente ausente.

Declarar duas constantes de classe que são o coração do gate:

  `private const CHAVES_LEGADAS` — as chaves de topo do payload atual, na ordem em que `compute()` as devolve: `user_id`, `user_name`, `mes_referencia`, `sem_carteira`, `motivo`, `empresas_carteira`, `empresas_com_baseline`, `margem_amostra`, `componentes`, `pontos_componentes`, `nota_final`, `faixa_bonus`, `faixa_promovida`, `periodo_meta`, `periodo`, `bonus`, `empresas_unicas`, `vinculos_servico`, `vinculos_financeiros`, `vinculos_sem_fonte_financeira`, `score_status`, `componentes_disponiveis`. Conferir a lista contra o `return` real de `compute()` antes de fixá-la — a lista acima é o que foi lido em 2026-07-29, mas a fonte de verdade é o arquivo.

  `private const CHAVES_ADITIVAS_PERMITIDAS = []` — vazia NESTA task. A Task 3 a atualiza para `['empresas_score']`. Esse literal é a única concessão auditável do gate: qualquer chave nova que apareça sem passar por uma edição explícita desta constante reprova o teste.

Escrever os testes:

  1. `test_chaves_de_topo_do_payload_sao_exatamente_as_legadas_mais_as_aditivas_declaradas` — `assertSame(self::CHAVES_LEGADAS, array_values(array_intersect(array_keys($payload), self::CHAVES_LEGADAS)))` prova que nenhuma legada sumiu nem trocou de ordem relativa; e `assertSame(self::CHAVES_ADITIVAS_PERMITIDAS, array_values(array_diff(array_keys($payload), self::CHAVES_LEGADAS)))` prova que nada apareceu sem declaração.
  2. `test_sub_chaves_de_componentes_pontos_componentes_margem_amostra_e_componentes_disponiveis` — mesma lógica de intersect e diff para `componentes` (legadas `nps_medio`, `var_faturamento_pct`, `var_margem_pct`, `absenteismo_pct`; aditivas permitidas vazias nesta task e `['var_margem_pp']` na Task 3), `pontos_componentes` (`nps`, `faturamento`, `margem`), `margem_amostra` (`n_real`, `n_elegivel`, `cobertura`) e `componentes_disponiveis` (`nps_medio`, `var_faturamento_pct`, `var_margem_pct`).
  3. `test_tipos_das_chaves_legadas_nao_mudam` — `assertIsInt`, `assertIsString`, `assertIsBool`, `assertIsArray` e, para os `?float`, `assertTrue(is_float($v) || is_null($v))`. Cobrir `nota_final`, `score_status`, `faixa_bonus`, `faixa_promovida`, `empresas_carteira`, `empresas_com_baseline` e os quatro sub-campos de `componentes`.
  4. `test_valores_congelados_do_caminho_legado` — o teste que realmente paga. Rodar a suíte uma vez, capturar os valores REAIS que o código produz HOJE e congelá-los como literais (`assertSame` para int, string e bool; `assertEqualsWithDelta(..., 0.001)` para float). Congelar no mínimo `nota_final`, `score_status`, `componentes.nps_medio`, `componentes.var_faturamento_pct`, `componentes.var_margem_pct`, `pontos_componentes.margem`, `pontos_componentes.faturamento`, `margem_amostra.n_real`, `margem_amostra.n_elegivel`, `margem_amostra.cobertura`, `empresas_carteira`, `empresas_com_baseline` e `vinculos_financeiros`. NÃO derivar os literais de expectativa teórica — o objetivo é fotografar o comportamento atual, esteja ele certo ou errado; qualquer divergência posterior é o sinal que o gate existe para dar.
  5. `test_shape_sem_carteira_tambem_esta_congelado` — profissional sem nenhum vínculo elegível; congelar as chaves e os valores do retorno de `shapeSemCarteira()` com a mesma disciplina de intersect e diff (a lista de topo desse shape é menor: não tem `margem_amostra`, `periodo_meta`, `periodo` nem `bonus`). É o Pitfall 4 do RESEARCH — caminho de retorno antecipado, fisicamente distante do resto do método, fácil de esquecer.

Docblock de classe em pt-BR explicando em uma frase o que este arquivo protege: as Fases 117-119 comparavam `sha256sum` de `DesempenhoScoreService.php` a cada task; a Fase 120 modifica o arquivo de propósito, e este teste é o substituto daquele gate — com a flag desligada, o payload tem de ser idêntico ao de antes da mudança, campo a campo.

Comentários de código em pt-BR, convenção do projeto.
  </action>
  <verify>
    <automated>cd "C:/xampp/htdocs/ecf_admin/ecf_admin" && C:/xampp/php/php.exe artisan test --filter=PayloadBaselineFlagOffTest && test -z "$(git diff --name-only -- app/Services/DesempenhoScoreService.php)"</automated>
  </verify>
  <done>A suíte roda verde contra o código não modificado, e `git diff --name-only` NÃO inclui `app/Services/DesempenhoScoreService.php`. O gate no 1 está armado antes de qualquer edição de produção.</done>
</task>

<task type="auto">
  <name>Task 2: Bump da chave de cache ANTES da mudança de shape (AGRE-03)</name>
  <files>app/Services/DesempenhoScoreService.php, tests/Feature/DesempenhoShopeeScoreTest.php, tests/Feature/Phase116/NpsFloorDesempenhoTest.php, tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php, tests/Feature/V18/DesempenhoMetadadosCacheTest.php</files>
  <action>
O bump vem ANTES da mudança de payload de propósito (risco 4 do RESEARCH): bumpar sozinho é inofensivo, só invalida chaves que expiram por TTL. Já mudar o shape sob a chave antiga faria o Redis servir payload velho, sem `empresas_score`, por até 7 dias em mês fechado, sob a mesma chave que o código novo escreve.

**Resolver a versão-alvo primeiro, não assumir.** Rodar `grep -n "desempenho\.compute\.v" app/Services/DesempenhoScoreService.php` e ler o literal corrente em `cacheKey()`, linha aproximada 335. A regra é **corrente mais um**:
  - literal corrente `v12`, que é o estado em 2026-07-29, resulta em alvo `v13` — exatamente o que AGRE-03 pede;
  - literal corrente `v13` resulta em alvo `v14`. A Fase 119.1, plano `119.1-03-PLAN.md`, ainda não executado quando este plano foi escrito, também bumpa a mesma chave, e o ROADMAP registra a corrida na nota logo acima da seção da Fase 120. Nunca reusar uma versão já consumida.
Registrar no SUMMARY qual versão foi efetivamente consumida.

Editar o `sprintf()` de `cacheKey()` para a versão-alvo e acrescentar, ao bloco de comentários histórico que já documenta v7 até v12, o parágrafo do bump novo — mesma disciplina de quatro elementos dos anteriores: (1) o que mudou tecnicamente, ou seja, o payload de `compute()` passa a ter `empresas_score` na raiz e `componentes.var_margem_pp`, e no Plano 03 desta fase a flag `metrics.performance_company_first_score` passa a poder trocar a origem de `nota_final` e `score_status`; (2) por que o valor cacheado antigo está desatualizado — shape menor, sem as duas chaves novas, que os consumidores das Fases 122 e 123 vão esperar; (3) a consequência de não bumpar — o Redis serve o payload antigo por até 7 dias em mês fechado sob a mesma chave que o código novo escreve, criando inconsistência de shape entre requests; (4) as chaves antigas viram órfãs e expiram sozinhas por TTL, não precisa nem deve rodar `cache:clear`.

Atualizar o literal nas 4 suítes que o carregam hardcoded — lista completa, verificada por grep exaustivo na pesquisa, nenhuma ocorrência adicional existe:
  - `tests/Feature/DesempenhoShopeeScoreTest.php` linha 363, 1 ocorrência. Renomear também o método `test_cache_key_bumpado_para_v12` para o nome da versão nova, para não ficar obsoleto.
  - `tests/Feature/Phase116/NpsFloorDesempenhoTest.php` linha 388, 1 ocorrência com `assertStringStartsWith`.
  - `tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php` linhas 246 e 348, 2 ocorrências.
  - `tests/Feature/V18/DesempenhoMetadadosCacheTest.php` linhas 232, 258, 260 e 277, 4 ocorrências.

Os arquivos que chamam o helper público `$service->cacheKey(...)` em vez de hardcodar — `Phase106/WarmDesempenhoCacheTest`, `Phase106/PerformanceControllerWarmDegradationTest`, `V16/DesempenhoElegibilidadeTest`, `V16/BonusDualPathRegressaoTest`, `Unit/DesempenhoScoreServiceCacheTest`, `NpsMaterializarNaoRespondidosCommandTest` — NÃO precisam de edição.

Fora do escopo desta task: `docs/nps-nao-respondido-nota-1.md` linha 219 cita a chave `v12` em prosa. Atualizar a menção se for barato, mas ela não entra em nenhum gate.
  </action>
  <verify>
    <automated>cd "C:/xampp/htdocs/ecf_admin/ecf_admin" && VELHA=$(git show HEAD:app/Services/DesempenhoScoreService.php | grep -o "desempenho\.compute\.v[0-9]*" | head -1) && echo "literal anterior: $VELHA" && test -z "$(grep -rn "$VELHA" app tests --include='*.php')" && C:/xampp/php/php.exe artisan test --filter="DesempenhoShopeeScoreTest|NpsFloorDesempenhoTest|NpsInvalidacaoRespostaTest|DesempenhoMetadadosCacheTest|PayloadBaselineFlagOffTest"</automated>
  </verify>
  <done>Nenhuma ocorrencia do literal completo `desempenho.compute.<versao_antiga>` sobra em `app/` ou `tests/`. O gate usa o literal inteiro de proposito: o bloco de comentario historico de `cacheKey()` menciona `v12`, `v11` etc. em prosa e deve mesmo continuar mencionando. As 4 suites passam com o literal novo e `PayloadBaselineFlagOffTest` continua verde — o bump nao toca nenhum numero.</done>
</task>

<task type="auto">
  <name>Task 3: Flag, parâmetro de shadow e as duas chaves aditivas do payload (AGRE-02, AGRE-04)</name>
  <files>config/metrics.php, app/Services/DesempenhoScoreService.php, tests/Feature/Phase120/PayloadBaselineFlagOffTest.php</files>
  <action>
**(a) `config/metrics.php` — a flag.** Acrescentar ao mesmo array de retorno, seguindo EXATAMENTE o padrão de quatro pontos de `unified_metrics_enabled` que já vive no arquivo: chave em snake_case, valor booleano com default `false` via `env(..., false)`, casting por `filter_var(..., FILTER_VALIDATE_BOOLEAN)`, e bloco de comentário acima explicando o rollout. O nome da chave é `performance_company_first_score` e a env correspondente é `PERFORMANCE_COMPANY_FIRST_SCORE`.

O bloco de comentário, em pt-BR, no formato do vizinho, precisa dizer: que a flag troca a origem de `nota_final` e `score_status` do modelo agregado por profissional para a média das notas por empresa, produzidas pelo `CompanyScoreService` da Fase 119; que o default `false` preserva 100% do comportamento legado; que o casting aceita `true`, `1` e `on` e rejeita ambíguos como `yes` e `sim`, defesa contra tampering, ameaça T-120-01; que a leitura em runtime é SEMPRE via `config('metrics.performance_company_first_score')` e nunca `env()` direto, porque `config:cache` invalida `env()` fora de config files em produção; e, o ponto que mais importa, que **ligar em produção depende do GATE MPP-04 aprovado, hoje `reprovado`, E do delta da Fase 121 aceito pelo usuário** — ligar antes disso muda o número que paga bônus sem evidência do tamanho da mudança.

NÃO adicionar `PERFORMANCE_COMPANY_FIRST_SCORE` a `.env` nem a `.env.example` nesta fase.

**(b) `DesempenhoScoreService.php` — dependência e parâmetro.**
  - Adicionar `use App\Services\Desempenho\CompanyScoreService;` no bloco de `use`, entre `MetricsProviderFactory` e `Nps\NpsImputationService`, ordem alfabética do namespace, convenção do arquivo.
  - Adicionar `private CompanyScoreService $companyScoreService,` como sétimo parâmetro promovido do construtor, linha aproximada 118. Todos os call-sites resolvem via container; nenhum `new DesempenhoScoreService(...)` existe fora dele.
  - `compute()`, linha 394, ganha um quarto parâmetro `bool $incluirEmpresasScore = false`. `computeCached()`, linha 228, ganha o mesmo quarto parâmetro com o mesmo default, propagando-o na closure do `Cache::remember` da linha 284: `fn () => $this->compute($user, $mesReferencia, $periodoOverride, $incluirEmpresasScore)`. O default `false` nos dois é o que mantém os ~40 call-sites existentes — incluindo `computeOficial()` na linha 386 e os três controllers de leitura interativa — sem uma linha de edição, com o shadow desligado, que é a D-04.
  - Docblock em pt-BR no parâmetro novo deixando a C-01 explícita: este parâmetro decide SE `CompanyScoreService::computeEmpresasScore()` roda; a feature flag decide QUAL resultado vira `nota_final`. São dois sinais independentes; confundi-los faz o custo — dispatcher por empresa mais NPS por empresa, com HTTP síncrono à Adman — vazar para a tela.

**(c) O shadow.** Inserir logo após o filtro de `$invalidadas`, fim do bloco na linha aproximada 421, onde `$mes`, `$periodo` e `$invalidadas` já estão prontos, que são exatamente os insumos que `computeEmpresasScore()` exige. A condição é `$incluirEmpresasScore || config('metrics.performance_company_first_score')`, atribuindo a `$empresasScore`, que nasce `null`. Passar `$invalidadas` explicitamente como quarto argumento — nunca deixar `null` para o serviço reresolver, e nunca `collect()` vazio, que silenciaria a invalidação por competência.

**(d) Os quatro componentes legados ficam INTOCADOS**, linhas aproximadas 436 a 459. Nenhuma linha delas muda. `$nota` e `$scoreStatus` continuam saindo de `computeNotaFinal()` e `computeScoreStatus()` sem nenhum `if` — a bifurcação é do Plano 03.

**(e) Payload aditivo.** No `return` de `compute()`:
  - `'empresas_score' => $empresasScore?->values()->all() ?? [],` como última chave de topo;
  - dentro de `componentes`, logo após `absenteismo_pct`, a chave `var_margem_pp` alimentada por um helper privado novo.

  Criar o helper privado `varMargemPpAgregado(?Collection $empresasScore): ?float`. Devolve `null` quando `$empresasScore` é `null`, ou seja, quando o shadow não rodou — é a C-03: reportar `null` é honesto, inventar um número agregado é pior, e reaproveitar `var_margem_pct` confundiria as duas unidades, que é justamente o que esta milestone existe para separar. Caso contrário, devolve a média dos `margem_var_pp` arredondada a 2 casas; `Collection::avg('margem_var_pp')` já descarta os `null` sozinho e devolve `null` se nenhum sobrar, o que cobre a carteira só-Shopee, onde o placeholder não tem pp real. Docblock em pt-BR registrando que este campo é **metadado de auditoria**, não insumo da nota: a nota lê `margem_pontos` por empresa, nunca esta média.

**(f) `shapeSemCarteira()`**, linha 1448 — simetria obrigatória: `'empresas_score' => []` no topo e `'var_margem_pp' => null` dentro de `componentes`.

**(g) Atualizar `PayloadBaselineFlagOffTest`** — e SÓ isto: `CHAVES_ADITIVAS_PERMITIDAS` de vazia para `['empresas_score']`, e a lista de aditivas de `componentes` para `['var_margem_pp']`. NENHUMA asserção de valor pode ser alterada. Se algum literal congelado na Task 1 precisar de outro número para passar, a byte-equivalência foi quebrada: parar, não ajustar o teste, e investigar o que contaminou o caminho legado — Pitfall 2 do RESEARCH, o shadow rodando antes dos componentes legados e mexendo em estado compartilhado.
  </action>
  <verify>
    <automated>cd "C:/xampp/htdocs/ecf_admin/ecf_admin" && C:/xampp/php/php.exe artisan test --filter="PayloadBaselineFlagOffTest|DesempenhoShopeeScoreTest|NpsFloorDesempenhoTest|NpsInvalidacaoRespostaTest|DesempenhoMetadadosCacheTest" && C:/xampp/php/php.exe artisan test --filter=Desempenho 2>&1 | tail -20</automated>
  </verify>
  <done>Flag existe com default `false`. `compute()` e `computeCached()` aceitam o parâmetro de shadow. O payload ganha `empresas_score` e `componentes.var_margem_pp` sem que nenhum valor congelado mude. `shapeSemCarteira()` está simétrico. `--filter=Desempenho` mantém exatamente as 14 falhas de baseline, zero novas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| env/config → runtime do motor de bônus | O valor de `PERFORMANCE_COMPANY_FIRST_SCORE` atravessa aqui e decide qual número paga bônus |
| Redis/cache → payload servido a controllers | Payload persistido entre deploys pode ter shape de uma versão de código diferente da que o lê |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Plano de mitigação |
|-----------|-----------|------------|------------|--------------------|
| T-120-01 | Tampering | `config/metrics.php` — leitura da env da flag | mitigate | `filter_var(env(...), FILTER_VALIDATE_BOOLEAN)` com default `false`; valores ambíguos (`yes`, `sim`, `off`) viram `false`. Mesmo padrão já em produção para `unified_metrics_enabled` |
| T-120-02 | Tampering | Cache do payload de desempenho | mitigate | Bump de `cacheKey()` executado na Task 2, ANTES da mudança de shape da Task 3 — nunca existe janela em que o código novo escreva shape novo sob a chave antiga |
| T-120-03 | Elevation of Privilege | Flag ligada em produção sem o gate MPP-04 | mitigate | Default `false` no config; docblock lista os dois pré-requisitos de ativação (gate MPP-04 aprovado e delta da Fase 121 aceito); nenhuma entrada em `.env`/`.env.example` nesta fase; SUMMARY reafirma que a flag fica desligada |
| T-120-04 | Denial of Service | Shadow rodando em leitura interativa | mitigate | `$incluirEmpresasScore` com default `false`; os três controllers continuam chamando `computeCached()` com dois argumentos, sem edição. Prova formal fica no Plano 02 |
| T-120-SC | Tampering | Instalação de pacotes (npm/composer) | accept | N/A — esta fase não roda `composer require` nem `npm install`; `composer.json` e `package.json` inalterados. Nenhum pacote novo a auditar |
</threat_model>

<verification>
1. `C:/xampp/php/php.exe artisan test --filter=PayloadBaselineFlagOffTest` — verde nas três tasks, com asserções de VALOR inalteradas entre a Task 1 e a Task 3.
2. `C:/xampp/php/php.exe artisan test --filter="DesempenhoShopeeScoreTest|NpsFloorDesempenhoTest|NpsInvalidacaoRespostaTest|DesempenhoMetadadosCacheTest"` — verde.
3. `C:/xampp/php/php.exe artisan test --filter=Desempenho` — exatamente 14 falhas, a baseline. Qualquer falha nova é regressão desta fase.
4. `grep -rn "desempenho\.compute\.v<versao_antiga>" app tests --include='*.php'` — zero linhas fora de comentário histórico.
5. `grep -n "performance_company_first_score" .env .env.example` — zero linhas. A flag não é ligada nesta fase.
6. `grep -n "empresasScore\|incluirEmpresasScore" app/Services/DesempenhoScoreService.php` — as ocorrências devem estar apenas na assinatura de `compute()`/`computeCached()`, no bloco do shadow, no helper `varMargemPpAgregado()` e no `return`. Nenhuma dentro de `computeNotaFinal`, `computeScoreStatus`, `computeVarFaturamento`, `computeVarMargem`, `computeNpsWindow` ou `margemPontos` (invariante 2).
</verification>

<success_criteria>
- Gate no 1 do VALIDATION armado e verde: os valores do caminho legado estão congelados em literais e não mudaram com a flag desligada.
- AGRE-03 fechado: cacheKey em `corrente + 1`, docblock com os quatro elementos, quatro suítes atualizadas, zero literal antigo sobrando.
- AGRE-04 fechado: `empresas_score` e `componentes.var_margem_pp` presentes; nenhuma chave legada removida ou com tipo alterado; `shapeSemCarteira()` simétrico.
- AGRE-02 parcialmente fechado: flag existe com default `false` e o parâmetro de shadow existe com default `false`. O roteamento (quem passa `true`) é o Plano 02.
- `--filter=Desempenho` dentro da baseline de 14 falhas.
</success_criteria>

<output>
Criar `.planning/phases/120-agrega-o-do-profissional-feature-flag-v21-0/120-01-SUMMARY.md` ao concluir, registrando obrigatoriamente: (a) a versão de cache efetivamente consumida e por quê; (b) os valores literais congelados pelo teste dourado, para a Fase 121 poder referenciá-los; (c) a reafirmação de que a flag permanece `false` e não pode ser ligada até o gate MPP-04 aprovar e o delta da Fase 121 ser aceito.
</output>
