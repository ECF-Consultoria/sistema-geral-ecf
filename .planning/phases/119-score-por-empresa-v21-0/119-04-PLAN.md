---
phase: 119-score-por-empresa-v21-0
plan: 04
type: execute
wave: 4
depends_on: [119-03]
files_modified:
  - tests/Feature/Phase119/CompanyScoreServiceFonteTest.php
  - tests/Feature/Phase119/CompanyScoreServiceStatusTest.php
  - tests/Feature/Phase119/CompanyScoreServiceReconciliacaoTest.php
  - app/Services/Desempenho/CompanyScoreService.php
autonomous: true
requirements: [EMPS-06, EMPS-07]

must_haves:
  truths:
    - "Empresa com vínculo Performance E Shopee elegíveis resolve fonte_financeira='adman' (Adman vence Shopee)"
    - "Empresa só-Shopee entra com margem_pontos=1.0 e quality.margin_source='placeholder_shopee', com status='complete' — nunca partial por ausência de margem"
    - "O caso âncora Shopee fecha: NPS 4,2 + faturamento 4 + margem 1,0 = 3,07"
    - "Os quatro status são testáveis e mutuamente exclusivos: complete, partial, sem_fonte, sem_dados"
    - "quality.motivos explica por que a empresa ficou incompleta, com conteúdo e ordem determinísticos"
    - "DesempenhoScoreService.php permanece byte-a-byte intocado e nenhum consumidor de produção referencia CompanyScoreService"
    - "O universo elegível e o mapa de fontes do caminho novo são idênticos aos do caminho antigo; só a nota diverge, por design"
  artifacts:
    - path: "tests/Feature/Phase119/CompanyScoreServiceFonteTest.php"
      provides: "EMPS-06 — desempate Adman×Shopee e placeholder de margem Shopee"
    - path: "tests/Feature/Phase119/CompanyScoreServiceStatusTest.php"
      provides: "EMPS-07 — taxonomia completa de status e quality.motivos"
    - path: "tests/Feature/Phase119/CompanyScoreServiceReconciliacaoTest.php"
      provides: "Reconciliação old×new (universo e fonte batem; nota diverge por design) — antecipa ROLL-01/02"
  key_links:
    - from: "CompanyScoreService"
      to: "vinculos.financial_source"
      via: "groupBy(company_id) + desempate 'adman' vence sobre o universo COMPLETO"
      pattern: "financial_source"
    - from: "CompanyScoreService"
      to: "quality.margin_source"
      via: "placeholder_shopee marcado na linha da empresa Shopee (D-02)"
      pattern: "placeholder_shopee"
---

<objective>
Fechar a fase provando a **fonte financeira vencedora** (EMPS-06) e a **taxonomia de `status`/`quality`** (EMPS-07) — a parte do contrato que existe para tornar auditável *por que* uma empresa ficou incompleta, e que a Fase 121 vai usar para explicar deltas.

Purpose: sem isso não dá para distinguir "empresa sem fonte financeira" de "empresa fora da carteira", nem justificar que uma empresa Shopee entra `complete` com margem placeholder. É também o plano que registra, para a Fase 120, o risco de que o invariante de `margemPontos()` deixa de valer no caminho novo.
Output: 2 suítes novas em `tests/Feature/Phase119/`; correções pontuais no `CompanyScoreService` se algum comportamento divergir; SUMMARY com o risco registrado.

**FASE ADITIVA:** `app/Services/DesempenhoScoreService.php` permanece byte-a-byte intocado (hash `cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-score-por-empresa-v21-0/119-CONTEXT.md
@.planning/phases/119-score-por-empresa-v21-0/119-PATTERNS.md
@.planning/phases/119-score-por-empresa-v21-0/119-VALIDATION.md
@.planning/phases/119-score-por-empresa-v21-0/119-03-SUMMARY.md
@app/Services/Desempenho/CompanyScoreService.php
@tests/Feature/DesempenhoShopeeScoreTest.php
</context>

<interfaces>
<!-- Regras já implementadas no plano 119-02 que estes testes exercitam -->

Desempate da fonte (sobre o universo COMPLETO, não pré-filtrado — a diferença em relação a `DesempenhoScoreService::computeUniverso():647-654`):

```php
$elegiveisPorEmpresa = $vinculos->where('financial_metrics_eligible', true)->groupBy('company_id');
// 'adman' vence quando a MESMA empresa tem os dois vínculos elegíveis;
// nenhum vínculo elegível ⇒ null ⇒ status 'sem_fonte' (D-03).
```

Flags por setor, resolvidas por vínculo dentro do `CarteiraContextService::flagsFinanceirasPorSetor():247-276` — não reimplementar:

```
performance → financial_source='adman',  financial_metrics_eligible=true
shopee      → financial_source='shopee', financial_metrics_eligible=true
polos | publicacao | outros → financial_source=null, financial_metrics_eligible=false
```

Taxonomia travada (Resposta 6 do `119-RESEARCH.md`), avaliada nesta ordem:

```
sem_fonte  ⇐ fonte_financeira === null                (D-03)
complete   ⇐ componentes_presentes === 3              (D-02: Shopee entra aqui)
sem_dados  ⇐ componentes_presentes === 0 com fonte
partial    ⇐ demais casos com fonte
// 'blocked', 'invalidada' e 'sem_baseline' NÃO existem no nível de empresa.
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EMPS-06 — Adman vence Shopee e placeholder de margem Shopee</name>
  <files>tests/Feature/Phase119/CompanyScoreServiceFonteTest.php, app/Services/Desempenho/CompanyScoreService.php</files>
  <behavior>
    - Empresa com DOIS vínculos elegíveis (serviço de setor `performance` e serviço de setor `shopee`) resolve `fonte_financeira === 'adman'` e produz **uma única** linha (`count()` da Collection não infla com a multiplicidade de vínculos).
    - Empresa só-Shopee: `fonte_financeira === 'shopee'`, `margem_pontos === 1.0`, `margem_var_pp === null`, `quality.margin_source === 'placeholder_shopee'`, `quality.motivos` **não** contém `margem_pp_indisponivel` (o placeholder não é componente ausente — D-02 e a trava da Fase 109), `componentes_presentes === 3` e `status === 'complete'`.
    - Caso âncora D-02: NPS 4,2 + faturamento com +2% (⇒ 4 pontos) + margem placeholder 1,0 ⇒ `nota_empresa === 3.07`.
    - A régua de margem NUNCA é aplicada para fonte Shopee: mesmo que `diff_pp` existisse, o valor da linha é o placeholder fixo.
  </behavior>
  <action>
Confirmar o gate de aditividade antes e depois (`sha256sum app/Services/DesempenhoScoreService.php`).

Criar `tests/Feature/Phase119/CompanyScoreServiceFonteTest.php` com o `setUp` já estabelecido na fase (`RefreshDatabase`, trait `Tests\Feature\V16\CriaCenarioResponsaveis`, `PRAGMA foreign_keys = ON`, `Carbon::setTestNow`, `Http::preventStrayRequests()` + `Http::fake()`).

Fixtures:
- Empresa mista: criar dois serviços (um de setor `performance`, um de setor `shopee`) via `criarServico()`, dois contratos via `criarContrato()` e dois vínculos em `company_users` com `servico_id` preenchido para a MESMA empresa e o MESMO profissional — é o cenário de multiplicidade documentado na memória `project_company_users_multi_linha_servico`.
- Empresa só-Shopee: vínculo com serviço de setor `shopee` + linhas em `shopee_metrics` nas duas janelas do período resolvido, calibradas para produzir `revenue.diff_pct` de +2% (ex.: baseline somando 100000 e janela atual somando 102000) ⇒ `reguaFaturamento(2.0) = 4.0`. Molde dos helpers de fixture Shopee em `tests/Feature/DesempenhoShopeeScoreTest.php`.
- NPS fixado por dublê (`$this->mock(NpsPorEmpresaService::class, ...)`) devolvendo `nota = 4.2` para a empresa Shopee.
- Período fechado via `app(MetricPeriodResolver::class)->resolve(['period_key' => '2026-06'])`.

Asserts obrigatórios: `assertSame('adman', ...)` no desempate; `assertCount()` provando uma linha por empresa apesar dos dois vínculos; `assertSame(1.0, $linha->margem_pontos)`; `assertSame('placeholder_shopee', $linha->quality['margin_source'])`; `assertNotContains('margem_pp_indisponivel', $linha->quality['motivos'])`; `assertSame('complete', $linha->status)`; `assertSame(3.07, $linha->nota_empresa)`.

Se algo divergir, corrigir o `CompanyScoreService`. Proibido: marcar empresa Shopee como `partial`/`blocked` por ausência de margem (contradiz a trava da Fase 109), e portar o blend `margemPontos()` do caminho antigo (D-04 — ele fica intocado e não é usado aqui).
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CompanyScoreServiceFonteTest</automated>
    <automated>sha256sum app/Services/DesempenhoScoreService.php | grep -q '^cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd'</automated>
  </verify>
  <done>Desempate Adman×Shopee provado com empresa de dois vínculos; empresa Shopee `complete` com placeholder marcado; caso âncora 3,07 fecha; hash inalterado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EMPS-07 — taxonomia de status e quality.motivos + regressão ampla</name>
  <files>tests/Feature/Phase119/CompanyScoreServiceStatusTest.php, app/Services/Desempenho/CompanyScoreService.php</files>
  <behavior>
    - `complete`: Adman com NPS, faturamento e margem presentes ⇒ `componentes_presentes === 3`, `nota_empresa` não-nula, `quality.motivos === []`.
    - `partial` (caso D-01): NPS 4,6 + faturamento 5 + margem ausente ⇒ `nota_empresa === null`, `nota_empresa_parcial === 4.8`, `componentes_presentes === 2`, `status === 'partial'`, `quality.motivos === ['margem_pp_indisponivel']`.
    - `sem_fonte` (caso D-03): empresa de setor `polos` com NPS 4,1 ⇒ `fonte_financeira === null`, `nota_empresa === null`, `nota_empresa_parcial === 4.1`, `componentes_presentes === 1`, `status === 'sem_fonte'`, `quality.motivos === ['sem_fonte_financeira']`.
    - `sem_dados`: empresa Adman com os dois endpoints respondendo 404 e NPS `null` (dublê devolvendo `nota => null`) ⇒ `componentes_presentes === 0`, `nota_empresa === null`, `nota_empresa_parcial === null`, `status === 'sem_dados'`, e `quality.motivos` contém `faturamento_sem_baseline`, `margem_pp_indisponivel` e `nps_janela_aberta` **nesta ordem**.
    - Os quatro status são mutuamente exclusivos: nenhuma linha de nenhum cenário devolve valor fora do conjunto `complete|partial|sem_fonte|sem_dados`.
  </behavior>
  <action>
Confirmar o gate de aditividade antes e depois.

Criar `tests/Feature/Phase119/CompanyScoreServiceStatusTest.php` com o mesmo `setUp` da fase, cobrindo os cinco comportamentos acima. Para cada cenário, asserir o array `quality['motivos']` **inteiro** com `assertSame` (não `assertContains` isolado) — a ordem é parte do contrato e é o que impede o motivo virar lixo não determinístico.

Cenário `sem_dados`: usar `Http::fake(['*/performance/*' => Http::response([], 404), '*/accounts/*/metrics*' => Http::response([], 404)])` mantendo `Http::preventStrayRequests()`, e dublê de NPS devolvendo uma entrada com `nota => null` para a empresa (simulando `origem='janela_aberta'`).

Acrescentar um teste final de coleção que roda um cenário com as quatro empresas juntas e assere que `pluck('status')->unique()->sort()->values()->all()` é exatamente `['complete','partial','sem_dados','sem_fonte']` — prova de exclusividade mútua e de que nenhum status legado (`blocked`/`invalidada`/`sem_baseline`) vazou para o nível de empresa.

Rodar, ao fim da task, a regressão ampla: `C:\xampp\php\php.exe artisan test --filter=Desempenho`. A baseline conhecida é de **14 falhas pré-existentes** (debug de margem já aberto). Qualquer número acima disso é regressão e deve ser investigado antes de fechar a fase. Nunca rodar `artisan test` sem `--filter`.

Confirmar também que nenhum consumidor de produção referencia o serviço novo: `grep -rn "CompanyScoreService" app/ routes/ | grep -v "app/Services/Desempenho/CompanyScoreService.php"` deve sair vazio.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CompanyScoreServiceStatusTest</automated>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase119</automated>
    <automated>C:\xampp\php\php.exe artisan test --filter=Desempenho</automated>
    <automated>sha256sum app/Services/DesempenhoScoreService.php | grep -q '^cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd'</automated>
  </verify>
  <done>Os quatro status provados e mutuamente exclusivos; `quality.motivos` asserido por igualdade de array em todos os cenários; suíte `Phase119` verde; `--filter=Desempenho` dentro da baseline de 14; hash inalterado; nenhum consumidor de produção.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconciliação caminho antigo × caminho novo (antecipa ROLL-01/02)</name>
  <files>tests/Feature/Phase119/CompanyScoreServiceReconciliacaoTest.php</files>
  <behavior>
    - Sobre a MESMA fixture de carteira e o MESMO `$periodo`, o conjunto de `company_id` com `fonte_financeira !== null` devolvido por `CompanyScoreService` é **idêntico** ao conjunto de ids de `companies_elegiveis` devolvido por `DesempenhoScoreService::computeUniverso()` (invocado por Reflection).
    - O mapa `company_id => fonte_financeira` do caminho novo é **idêntico** ao mapa `fontes` de `computeUniverso()` — o desempate Adman×Shopee não divergiu.
    - Empresa invalidada na competência está ausente nos DOIS caminhos.
    - Divergência POR DESIGN, asserida explicitamente: no cenário de granularidade (empresa A −20%, empresa B +2%), a média dos `faturamento_pontos` do caminho novo é 2,5 enquanto `reguaFaturamento()` aplicada sobre a variação agregada do caminho antigo dá 1,0. O teste documenta em comentário que isto é o objetivo da milestone (D3), não uma regressão.
  </behavior>
  <action>
Confirmar o gate de aditividade antes e depois.

Criar `tests/Feature/Phase119/CompanyScoreServiceReconciliacaoTest.php` — molde de reconciliação de `tests/Feature/Phase118/NpsPorEmpresaRamosTest.php` (rodar os dois caminhos sobre a mesma fixture e asserir onde batem e onde divergem por design).

Invocar `DesempenhoScoreService::computeUniverso(User $user, Carbon $mes)` por Reflection (mesmo helper de `ReflectionMethod` + `setAccessible(true)` já usado em `CompanyScoreServiceReguasTest`). Ele devolve um array com `companies_elegiveis` (EloquentCollection de `Company`) e `fontes` (Collection `company_id => 'adman'|'shopee'`). **Somente leitura** — o `DesempenhoScoreService` não pode ser modificado (gate de hash).

Comparar por conjuntos ordenados (`sort()->values()->all()`) para que a mensagem de falha aponte o `company_id` divergente. Manter `Http::preventStrayRequests()` + `Http::fake()`.

Este teste é a antecipação de ROLL-01/ROLL-02 (Fase 121): existe para que a primeira comparação old×new não seja a primeira vez que alguém olha os dois lado a lado. Não transformá-lo em teste de igualdade da nota final — as notas DEVEM divergir.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CompanyScoreServiceReconciliacaoTest</automated>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase119</automated>
    <automated>sha256sum app/Services/DesempenhoScoreService.php | grep -q '^cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd'</automated>
  </verify>
  <done>Universo elegível e mapa de fontes idênticos entre os dois caminhos; empresa invalidada ausente nos dois; divergência de nota asserida como esperada; hash inalterado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vínculos de carteira → fonte financeira | O desempate decide de qual fonte a empresa é pontuada; errar troca a origem do número do bônus |
| `quality`/`status` → auditoria (Fase 121) | É o canal que explica por que uma empresa ficou incompleta — informação errada aqui vira decisão errada depois |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-02 | Elevation of Privilege | Universo/desempate de fonte | mitigate | Desempate sempre sobre os vínculos de `forUser($user)`; empresa com dois vínculos vira UMA linha (assert de `assertCount`), impedindo dupla contagem da mesma empresa na carteira |
| T-119-11 | Repudiation | `quality.motivos` | mitigate | Conteúdo e ordem determinísticos, asseridos por igualdade de array — motivo não determinístico inviabilizaria a auditoria da Fase 121 |
| T-119-12 | Tampering | Placeholder Shopee | mitigate | `margem_pontos = 1.0` marcado por `quality.margin_source='placeholder_shopee'` — nunca indistinguível de margem real medida |
| T-119-04 | Denial of Service | Suíte → Adman | mitigate | `Http::preventStrayRequests()` + `Http::fake()` em todos os cenários (GATE MPP-04 pendente) |
| T-119-07 | Tampering | `DesempenhoScoreService.php` | mitigate | Gate de hash em toda task + `grep` confirmando ausência de consumidor de produção |
| T-119-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `--filter=Phase119` verde
- `--filter=Desempenho` dentro da baseline de 14 falhas pré-existentes
- `sha256sum app/Services/DesempenhoScoreService.php` inalterado e `git diff --name-only` sem esse arquivo
- `grep -rn "CompanyScoreService" app/ routes/` só encontra o próprio arquivo do serviço
</verification>

<success_criteria>
- EMPS-06 provado: Adman vence Shopee, uma linha por empresa, placeholder Shopee marcado e `complete`
- EMPS-07 provado: quatro status mutuamente exclusivos, `quality.motivos` determinístico
- Fase 119 fechada como aditiva: nenhum número de produção mudou
</success_criteria>

<output>
Criar `.planning/phases/119-score-por-empresa-v21-0/119-04-SUMMARY.md`.

**Registrar obrigatoriamente no SUMMARY, em seção própria (o `<risks>` do `119-CONTEXT.md` exige que a Fase 120 não descubra isso na hora):**

> **Régua-da-média ≠ média-das-réguas — o invariante da Fase 109 não vale no caminho novo.**
> O docblock de `DesempenhoScoreService::margemPontos()` declara como invariante testado em `DesempenhoShopeeScoreTest` que "só-performance (`$nShopeePlaceholder=0`) devolve exatamente `reguaMargem($varMargemReal)` — IDÊNTICO ao comportamento pré-Fase 109 (regressão zero)". No `CompanyScoreService` a régua roda **por empresa** e a média vem depois, então esse invariante **deixa de valer**.
> Na Fase 119 isso é inofensivo — a fase é aditiva, o caminho antigo segue intocado e `DesempenhoShopeeScoreTest` continua verde. **Na Fase 120 vira problema real quando a flag ligar:** o plano da 120 precisa decidir se `DesempenhoShopeeScoreTest` ganha cenários novos para o modo flag-ligada ou se os invariantes são reescritos.

Registrar também o débito herdado da Fase 118: a proteção contra divergência das réguas é um **teste de equivalência**, não a extração para classe compartilhada — a unificação fica para a Fase 120, quando o gate de aditividade sair.

Commits com caminhos explícitos (`git add <paths>` + `git commit -m ... -- <paths>`). Árvore compartilhada: **nunca** `git add -A`, **nunca** `git stash`.
</output>
