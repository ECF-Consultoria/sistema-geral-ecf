---
phase: 119-score-por-empresa-v21-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Desempenho/CompanyScoreService.php
  - tests/Feature/Phase119/CompanyScoreServiceReguasTest.php
  - tests/Feature/Phase119/CompanyScoreServiceContratoTest.php
  - tests/Feature/Phase119/CompanyScoreServiceFormulaTest.php
autonomous: true
requirements: [EMPS-01, EMPS-02, EMPS-04]

must_haves:
  truths:
    - "As réguas duplicadas no CompanyScoreService devolvem exatamente o mesmo valor que as de DesempenhoScoreService em todos os pontos de corte e boundaries, inclusive null"
    - "computeEmpresasScore devolve UMA linha por empresa do universo do profissional, chaveada por company_id, com todas as chaves do contrato §3.1"
    - "Empresa fora da carteira do profissional nunca aparece na Collection"
    - "Empresa invalidada na competência nunca aparece na Collection"
    - "A régua de faturamento é aplicada por empresa: duas empresas com -20% e +2% produzem pontos 1 e 4 (média 2,5), e não o 1 ponto que a régua-da-média daria"
    - "nota_empresa = round((nps+fat+margem)/3, 2) — o caso âncora 4,6+5+4 fecha em 4,53"
    - "DesempenhoScoreService.php permanece byte-a-byte intocado"
  artifacts:
    - path: "app/Services/Desempenho/CompanyScoreService.php"
      provides: "Linha de fato por (user_id, company_id) com os 3 componentes pontuados"
      contains: "function computeEmpresasScore"
    - path: "tests/Feature/Phase119/CompanyScoreServiceReguasTest.php"
      provides: "Teste de equivalência das réguas duplicadas (C-03)"
      contains: "ReflectionMethod"
    - path: "tests/Feature/Phase119/CompanyScoreServiceContratoTest.php"
      provides: "Contrato completo da linha por empresa (EMPS-01) + escopo de carteira + invalidação"
    - path: "tests/Feature/Phase119/CompanyScoreServiceFormulaTest.php"
      provides: "Casos âncora EMPS-02 e EMPS-04"
  key_links:
    - from: "app/Services/Desempenho/CompanyScoreService.php"
      to: "CarteiraContextService::forUser"
      via: "montagem do universo de empresas (nunca join direto em company_users)"
      pattern: "carteiraContext->forUser"
    - from: "app/Services/Desempenho/CompanyScoreService.php"
      to: "NpsPorEmpresaService::notasNpsPorEmpresa"
      via: "componente NPS pronto, UMA chamada por (user, mes)"
      pattern: "notasNpsPorEmpresa"
    - from: "app/Services/Desempenho/CompanyScoreService.php"
      to: "MetricDiffDispatcher::compute"
      via: "chamada ÚNICA por empresa, sempre dentro do guard de fonte não-nula"
      pattern: "diffDispatcher->compute"
---

<objective>
Criar `app/Services/Desempenho/CompanyScoreService.php` — o serviço **aditivo** que produz a linha de fato por `(user_id, company_id)` com NPS, faturamento e margem **já pontuados pela régua**, e a `nota_empresa` calculada.

Purpose: trocar a unidade do motor de bonificação — a régua passa a rodar **por empresa**, antes de qualquer média. Este plano entrega o serviço inteiro; os planos 119-03 e 119-04 acrescentam as provas duras (`diff_pp` × `diff_pct`, chamada única do dispatcher, Shopee, taxonomia de status).
Output: 1 serviço novo + 3 suítes em `tests/Feature/Phase119/`.

**FASE ADITIVA — invariante inegociável:** `app/Services/DesempenhoScoreService.php` fica **byte-a-byte intocado** (hash `cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd`). Nenhum consumidor de produção referencia `CompanyScoreService` — só testes. Nenhum número de produção muda nesta fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-score-por-empresa-v21-0/119-CONTEXT.md
@.planning/phases/119-score-por-empresa-v21-0/119-PATTERNS.md
@.planning/phases/119-score-por-empresa-v21-0/119-VALIDATION.md
@.planning/phases/119-score-por-empresa-v21-0/119-RESEARCH.md
@app/Services/Desempenho/NpsPorEmpresaService.php
@app/Services/Portfolio/CarteiraContextService.php
@app/Services/Metrics/MetricDiffDispatcher.php
@tests/Feature/Phase118/NpsJanelaResolverTest.php
@tests/Feature/V18/AdmanMetricDiffServiceTest.php
</context>

<interfaces>
<!-- Contratos extraídos do código. O executor NÃO precisa explorar o codebase. -->

Dependências injetadas (todas já existem, nenhuma é modificada):

```php
// app/Services/Portfolio/CarteiraContextService.php:106
public function forUser(User $user, array $filters = []): Collection
// cada item é um array com estas chaves (normalizar(), :224-237):
// user_id, company_id, company_name, servico_id, servico_nome, setor, role, role_label,
// has_financial_source, financial_source ('adman'|'shopee'|null), financial_metrics_eligible (bool)
// ATENÇÃO: NÃO colapsa vínculos — a MESMA empresa aparece 1x por serviço/vínculo.

// app/Services/Desempenho/NpsPorEmpresaService.php:110
public function notasNpsPorEmpresa(User $user, Carbon $mes, bool $mesFechado, ?Collection $invalidadas = null): Collection
// Collection chaveada por company_id de objetos:
// { company_id, nota: ?float, origem, total_notas, por_ramo, por_papel, papeis,
//   servico_ids, consolidado, notas_brutas, houve_survey }
// Garante 1 entrada para TODA empresa do MESMO forUser($user,['active'=>true]) menos $invalidadas.

// app/Services/Metrics/MetricDiffDispatcher.php:34
public function compute(Company $company, array $periodo, string $source): array
// lança InvalidArgumentException se $source não for 'adman' nem 'shopee'.

// app/Models/BonusInvalidacao.php:68
public static function companyIdsInvalidadas(Carbon $competencia): \Illuminate\Support\Collection
```

Shape devolvido pelo dispatcher (Adman `resolveField()`:237-261 e `resolveMargemPct()`:294-325; Shopee espelha o mesmo shape):

```php
[
  'company_id' => int,
  'period'     => array,
  'metrics' => [
    'revenue'                   => ['value'=>?float,'prev_value'=>?float,'diff_pct'=>?float,'diff_source'=>?string],
    'contribution_margin_value' => ['value'=>?float,'prev_value'=>?float,'diff_pct'=>?float,'diff_source'=>?string],
    'contribution_margin_pct'   => ['value'=>?float,'prev_value'=>?float,'diff_pct'=>?float,'diff_pp'=>?float,'diff_source'=>?string],
  ],
  'quality' => ['status'=>string,'source'=>string,'computed_at'=>string,'diff_pp_disponivel'=>bool],
]
// diff_pp = round(value - prev_value, 2) SOMENTE quando
// $periodo['comparison_mode'] === 'previous_equal_length_window' e value/prev_value são ambos numéricos.
// Shopee: contribution_margin_pct sempre com diff_pct e diff_pp null (MPP-05).
```

Contrato de saída de `computeEmpresasScore()` (§3.1 do plano canônico + D-01/D-02/D-03):

```php
(object) [
  'company_id' => int, 'company_name' => string, 'fonte_financeira' => ?string,
  'nps_pontos' => ?float,
  'faturamento_atual' => ?float, 'faturamento_anterior' => ?float,
  'faturamento_var_pct' => ?float, 'faturamento_pontos' => ?float,
  'margem_pct_atual' => ?float, 'margem_pct_anterior' => ?float,
  'margem_var_pp' => ?float, 'margem_pontos' => ?float,
  'componentes_presentes' => int, 'nota_empresa' => ?float, 'nota_empresa_parcial' => ?float,
  'status' => string,
  'quality' => ['revenue_diff_source'=>?string,'margin_diff_source'=>?string,'margin_source'=>?string,'motivos'=>array],
]
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classe CompanyScoreService + réguas duplicadas byte a byte + teste de equivalência (C-03)</name>
  <files>app/Services/Desempenho/CompanyScoreService.php, tests/Feature/Phase119/CompanyScoreServiceReguasTest.php</files>
  <behavior>
    - `reguaFaturamento(null)` devolve null; `-6.01` e `-6.0` → 1.0; `-5.0` e `-1.0` → 2.0; `-0.99`, `0.0` e `0.99` → 3.0; `1.0`, `4.0` e `5.0` → 4.0; `5.01` → 5.0.
    - `reguaMargem(null)` devolve null; `-5.01` e `-5.0` → 1.0; `-2.0` e `-1.99` → 2.0; `0.0` e `1.0` → 3.0; `1.01`, `3.39` e `4.0` → 4.0; `4.01` e `14.09` → 5.0.
    - Para cada valor da lista de boundaries, a régua do `CompanyScoreService` devolve **exatamente** (assertSame) o mesmo valor que a régua homônima de `DesempenhoScoreService`, invocada por Reflection.
    - O teste não é tautológico: além da comparação cruzada, assere os valores esperados literais acima (senão "null == null" passaria com duas implementações quebradas).
  </behavior>
  <action>
Antes de qualquer edição, confirmar o gate de aditividade: `sha256sum app/Services/DesempenhoScoreService.php` deve começar com `cfc16da2a8404fba`.

Criar `app/Services/Desempenho/CompanyScoreService.php` no namespace `App\Services\Desempenho`, seguindo o molde de imports e docblock de `NpsPorEmpresaService` (mesma pasta) descrito em `119-PATTERNS.md` § "app/Services/Desempenho/CompanyScoreService.php". Construtor com promoted properties privadas: `CarteiraContextService $carteiraContext`, `MetricDiffDispatcher $diffDispatcher`, `NpsPorEmpresaService $npsPorEmpresaService`.

Docblock de classe em 3 blocos, como o do análogo: (1) o que o serviço é e de onde vem (Fase 119, EMPS-01..07, consome o componente NPS da Fase 118); (2) "Regras travadas" numeradas referenciando D-01/D-02/D-03 e C-01..C-04 do `119-CONTEXT.md`; (3) bloco "Aditividade" registrando que `DesempenhoScoreService` é ESPELHADO e nunca modificado, com `@see` para o CONTEXT, para `app/Services/DesempenhoScoreService.php:1290,1311` e para `NpsPorEmpresaService`.

Nesta task implementar SOMENTE os dois métodos `private function reguaFaturamento(?float $pct): ?float` e `private function reguaMargem(?float $pct): ?float`, copiados **byte a byte** dos corpos de `DesempenhoScoreService:1290-1298` e `:1311-1319` (corpos literais reproduzidos em `119-PATTERNS.md` § "Corpo literal das duas réguas"). Não "melhorar", não reordenar comparações, não trocar `<=` por `<`.

Acrescentar sobre cada cópia um comentário em pt-BR explicando (a) que a duplicação é **intencional e temporária** porque o gate de aditividade proíbe tornar as originais `protected` (C-03), (b) que a unificação real fica para a Fase 120, quando o gate sair, e (c) especificamente em `reguaMargem`: os cortes são idênticos, mas aqui a função recebe `margem_var_pp` (pontos percentuais, `diff_pp`), **nunca** `diff_pct` — D2 da milestone.

Criar `tests/Feature/Phase119/CompanyScoreServiceReguasTest.php` (namespace `Tests\Feature\Phase119`, `use RefreshDatabase`), replicando o molde de invocação por Reflection de `tests/Feature/Phase118/NpsJanelaResolverTest.php:117-124`: helper privado que recebe o objeto de serviço, o nome do método e o `?float`, cria um `ReflectionMethod`, chama `setAccessible(true)` e invoca. Resolver os dois serviços pelo container (`app(DesempenhoScoreService::class)` e `app(CompanyScoreService::class)`).

Lista de boundaries obrigatória (a mesma para as duas réguas, cobrindo os exigidos pelo gate 2 do `119-VALIDATION.md`): `null, -6.01, -6.0, -5.01, -5.0, -2.01, -2.0, -1.01, -1.0, -0.99, 0.0, 0.99, 1.0, 1.01, 3.39, 4.0, 4.01, 5.0, 5.01, 14.09`. Mensagem de falha do assert deve citar o método e o `pct` divergente.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CompanyScoreServiceReguasTest</automated>
    <automated>sha256sum app/Services/DesempenhoScoreService.php | grep -q '^cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd'</automated>
    <automated>git diff --name-only | grep -qv 'app/Services/DesempenhoScoreService.php'</automated>
  </verify>
  <done>Suíte verde; as duas réguas concordam com as originais em todos os boundaries e nos valores literais; hash de `DesempenhoScoreService.php` inalterado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeEmpresasScore() completo + teste de contrato (EMPS-01)</name>
  <files>app/Services/Desempenho/CompanyScoreService.php, tests/Feature/Phase119/CompanyScoreServiceContratoTest.php</files>
  <behavior>
    - Profissional com 1 empresa Performance (Adman) e 1 empresa do setor `polos` (sem fonte) recebe uma Collection com **2 entradas**, chaveada por `company_id`, cada uma com TODAS as 17 chaves do contrato.
    - A empresa Adman traz `fonte_financeira='adman'`, `faturamento_atual`/`faturamento_anterior`/`faturamento_var_pct` vindos de `metrics.revenue`, `margem_pct_atual`/`margem_pct_anterior`/`margem_var_pp` vindos de `metrics.contribution_margin_pct`, e `quality.revenue_diff_source`/`quality.margin_diff_source` repassados do dispatcher.
    - A empresa `polos` traz `fonte_financeira=null`, todos os campos financeiros `null`, `status='sem_fonte'`, `quality.motivos === ['sem_fonte_financeira']` e `nota_empresa_parcial` igual à própria nota de NPS (D-03).
    - Empresa de OUTRO profissional não aparece na Collection.
    - Empresa invalidada na competência (`bonus_invalidacoes`) não aparece na Collection — nem como linha `sem_fonte`.
    - Com `is_closed=false` (mês em curso) o `NpsPorEmpresaService` real devolve piso 1.0 para toda a carteira — o teste de contrato usa esse caminho para provar o wiring real, sem fixture de NPS.
  </behavior>
  <action>
Confirmar o gate de aditividade antes e depois (mesmos dois comandos da Task 1).

Implementar em `CompanyScoreService` o método público:
`public function computeEmpresasScore(User $user, Carbon $mes, array $periodo, ?Collection $invalidadas = null): Collection`

Decisões de assinatura (D-05, registrar no docblock):
- `$periodo` chega **já resolvido** por quem chama (nunca resolver internamente) — garante janela byte-idêntica à que `DesempenhoScoreService::compute()` usou, senão a Fase 121 compararia notas divergentes por causa da janela, não da fórmula (anti-pattern explícito do RESEARCH).
- `$mesFechado` NÃO é parâmetro: deriva de `(bool) ($periodo['is_closed'] ?? false)`, que é o sinal canônico de mês fechado já usado por `DesempenhoScoreService::compute()` ao chamar `computeNpsWindow`. Uma fonte de verdade só.
- `$invalidadas` nulo ⇒ resolver internamente com `BonusInvalidacao::companyIdsInvalidadas($mes)` (NUNCA cair para `collect()` vazio, que silenciaria a invalidação) e repassar **exatamente esse mesmo objeto** ao `NpsPorEmpresaService`, garantindo universos idênticos entre os dois serviços.

Passos do método, nesta ordem (Resposta 7 do RESEARCH — invalidação antes de qualquer resolução de fonte ou HTTP):
1. Resolver `$invalidadas` (acima).
2. `$vinculos = $this->carteiraContext->forUser($user, ['active' => true])->reject(fn (array $v) => $invalidadas->contains($v['company_id']))`. Nunca `$user->companies()` nem join direto em `company_users`.
3. `$companiesUniverso = $vinculos->pluck('company_id')->unique()->values()`. Se vazio, devolver `collect()`.
4. Mapa `company_id => company_name` a partir do primeiro vínculo de cada empresa.
5. Mapa de fonte vencedora: agrupar por `company_id` **apenas** os vínculos com `financial_metrics_eligible === true`; para cada grupo, `'adman'` vence se `financial_source` contiver `'adman'`, senão o primeiro `financial_source`. Empresa sem nenhum vínculo elegível fica **fora** do mapa ⇒ `fonte_financeira = null` (D-03). O universo permanece o COMPLETO — nunca pré-filtrar por elegibilidade, diferente de `computeUniverso()`.
6. UMA chamada: `$notasNps = $this->npsPorEmpresaService->notasNpsPorEmpresa($user, $mes, $mesFechado, $invalidadas)`. Nunca chamar em loop por empresa.
7. Carregar os models `Company` apenas das empresas com fonte não-nula (`Company::whereIn('id', ...)->get()->keyBy('id')`) — são as únicas que precisam do dispatcher.
8. Para cada `company_id` do universo, montar a linha:
   - `nps_pontos`: `$notasNps[$companyId]->nota ?? null`. Se a chave não existir no retorno (não deveria acontecer — os dois serviços partem do mesmo `forUser()`), tratar defensivamente como `null`, acrescentar o motivo `nps_indisponivel` e emitir `Log::warning('[Score por Empresa] company_id ausente no retorno do NPS', ['user_id'=>..., 'company_id'=>..., 'competencia'=>$mes->format('Y-m')])`. Nunca lançar exceção.
   - **Guard C-04:** só entrar no ramo financeiro `if ($fonteFinanceira !== null)`. `MetricDiffDispatcher::compute()` lança `InvalidArgumentException` para fonte inválida — a empresa `sem_fonte` não pode chegar lá, e o guard vem ANTES da chamada, jamais num `catch`.
   - **EMPS-05:** dentro do guard, **uma única** chamada `$resultado = $this->diffDispatcher->compute($company, $periodo, $fonteFinanceira)`, e dela extrair todos os campos: `revenue.value`, `revenue.prev_value`, `revenue.diff_pct`, `revenue.diff_source`, `contribution_margin_pct.value`, `.prev_value`, `.diff_pp`, `.diff_source`. Nunca chamar o dispatcher duas vezes por empresa.
   - `faturamento_pontos = $this->reguaFaturamento($faturamentoVarPct)`.
   - `margem_pontos` por `match(true)`: fonte `'shopee'` ⇒ `1.0` fixo (D-02, nunca aplica régua); `margem_var_pp === null` ⇒ `null`; senão `$this->reguaMargem($margemVarPp)` — **sempre sobre `diff_pp`, jamais sobre `diff_pct`** (EMPS-03).
   - `componentes_presentes` = quantidade de não-nulos entre `nps_pontos`, `faturamento_pontos`, `margem_pontos`; `nota_empresa_parcial` = `round(média dos presentes, 2)` ou `null` se nenhum; `nota_empresa` = `nota_empresa_parcial` somente quando `componentes_presentes === 3`, senão `null` (D-01 — os DOIS números são reportados, nunca só um).
   - `status` por `match(true)` **nesta ordem**: `fonte_financeira === null` ⇒ `'sem_fonte'`; `componentes_presentes === 3` ⇒ `'complete'`; `componentes_presentes === 0` ⇒ `'sem_dados'`; senão `'partial'`. Os valores `blocked`, `invalidada` e `sem_baseline` são deliberadamente inexistentes (Resposta 6 do RESEARCH — justificar no docblock).
   - `quality.margin_source` = `'placeholder_shopee'` quando a fonte é `'shopee'`, senão `null`. `quality.revenue_diff_source`/`margin_diff_source` repassados do dispatcher (`null` quando sem fonte).
   - `quality.motivos`, montado **nesta ordem determinística** (os testes asseram o array inteiro): `'sem_fonte_financeira'` quando fonte nula; `'faturamento_sem_baseline'` quando há fonte e `faturamento_pontos === null`; `'margem_pp_indisponivel'` quando a fonte é `'adman'` e `margem_var_pp === null`; `'nps_janela_aberta'` quando a chave existe no retorno do NPS e `nota` é `null`; `'nps_indisponivel'` quando a chave não existe.
9. Devolver `Collection` chaveada por `company_id` (`mapWithKeys`), de objetos `(object)` — mesmo idioma de `NpsPorEmpresaService`.

Extrair a montagem da linha sem fonte para um helper privado `linhaSemFonte()`, molde em `119-PATTERNS.md`.

**Disciplina de log (T-119-01):** qualquer `Log::warning` leva apenas `user_id`, `company_id` e `competencia`. Nunca `company_name`, e-mail, texto de NPS, `adman_account_id`/`cust_id` ou qualquer credencial.

**Não portar** (C-02 / anti-patterns do RESEARCH): `$vars->avg()` de `computeVarMargem()`, os contadores `n_com_margem_real`/`n_elegivel`, e o blend `margemPontos()`. Nenhum gate de cobertura de margem deve ser inventado — C-01 confirma que `fallbackMargemPct()`/`coberturaMargem()` são código morto e que não existe patamar ativo.

Criar `tests/Feature/Phase119/CompanyScoreServiceContratoTest.php` cobrindo os 6 comportamentos do bloco `<behavior>`. Fixtures:
- `use RefreshDatabase; use CriaCenarioResponsaveis;` (trait `Tests\Feature\V16\CriaCenarioResponsaveis` — helpers `criarServico()`, `criarContrato()`), `DB::statement('PRAGMA foreign_keys = ON')` e `Carbon::setTestNow()` no `setUp`, `Carbon::setTestNow()` no `tearDown` — molde de `tests/Feature/DesempenhoShopeeScoreTest.php:46-90`.
- **Obrigatório em todo teste que toque o dispatcher:** `Http::preventStrayRequests()` seguido de `Http::fake([...])` — o GATE MPP-04 não autoriza nenhuma chamada real à Adman.
- `$periodo` construído pelo resolver real: `app(MetricPeriodResolver::class)->resolve(['period_key' => '2026-06'])` com `Carbon::setTestNow('2026-07-15 10:00:00')` — produz `comparison_mode='previous_equal_length_window'` e `is_closed=true`. Para o cenário de mês em curso usar `['period_key' => 'current_month']` (`is_closed=false`), que é o que faz o `NpsPorEmpresaService` real devolver piso 1.0 para toda a carteira.
- Empresa Adman: `Company::factory()->create(['adman_account_id' => 'CUST-CONTRATO-A'])` + vínculo `company_users` com serviço de setor `performance`; payload Adman via `Http::fake(['*/performance/*' => ..., '*/accounts/*/metrics*' => ...])` reusando os fixtures reais de `tests/Feature/V18/AdmanMetricDiffServiceTest.php:36-62`.
- Empresa sem fonte: vínculo com serviço de setor `polos`.
- Invalidação: inserir linha em `bonus_invalidacoes` para a competência e reasserir a ausência da empresa.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CompanyScoreServiceContratoTest</automated>
    <automated>sha256sum app/Services/DesempenhoScoreService.php | grep -q '^cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd'</automated>
  </verify>
  <done>Contrato completo devolvido por `computeEmpresasScore()`; empresa sem fonte listada com `status='sem_fonte'`; empresa de outro profissional e empresa invalidada ausentes; nenhuma chamada real à Adman; hash inalterado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Casos âncora — nota_empresa (EMPS-04) e régua por empresa × régua da média (EMPS-02)</name>
  <files>tests/Feature/Phase119/CompanyScoreServiceFormulaTest.php</files>
  <behavior>
    - Caso âncora §5.4: empresa com NPS 4,6 · faturamento +8% (⇒ 5 pontos) · margem +3,2 pp (⇒ 4 pontos) produz `nota_empresa === 4.53`, `nota_empresa_parcial === 4.53`, `componentes_presentes === 3`, `status === 'complete'`.
    - Caso de granularidade: empresa A com faturamento −20% recebe `faturamento_pontos === 1.0` e empresa B com +2% recebe `4.0`; a média dos dois pontos é `2.5`, enquanto `reguaFaturamento(-9.0)` (a média das variações, que é o que a regra antiga pontuaria) devolve `1.0` — as duas afirmações no mesmo teste, com comentário registrando que a divergência é o objetivo da milestone (D3), não uma regressão.
    - `nota_empresa` sempre `round(..., 2)`: um caso com componentes 4,6 / 5,0 / 4,0 fecha em 4,53 e não em 4,5333.
  </behavior>
  <action>
Criar `tests/Feature/Phase119/CompanyScoreServiceFormulaTest.php` com o mesmo `setUp` (PRAGMA, `Carbon::setTestNow`, `Http::preventStrayRequests()` + `Http::fake`) e as mesmas fixtures de carteira da Task 2.

Para fixar o `nps_pontos` em valores exatos (4,6 e 4,2), **injetar um dublê** de `NpsPorEmpresaService` no container antes de resolver o serviço sob teste: `$this->mock(NpsPorEmpresaService::class, fn ($m) => $m->shouldReceive('notasNpsPorEmpresa')->once()->andReturn($colecao))`, onde `$colecao` é chaveada por `company_id` com objetos contendo pelo menos `company_id` e `nota`. O `->once()` documenta de quebra a chamada única do componente NPS. Resolver `app(CompanyScoreService::class)` **depois** do bind.

Para fixar as variações financeiras por empresa, dar `adman_account_id` distinto a cada empresa (ex.: `CUST-A`, `CUST-B`) e usar `Http::fake()` com **closure** que inspeciona `$request->url()`: quando a URL contiver o custId da empresa A devolver o payload A, quando contiver o da B devolver o payload B. Payloads derivados do fixture real de `AdmanMetricDiffServiceTest`:
- faturamento +8%: `summarizedData.grossBilling.diff = 8.0` (caminho `adman_diff`, exige `comparison_mode='previous_equal_length_window'`);
- margem +3,2 pp: `metrics.percentageMargin.value = 18.2` e `.prev = 15.0` (⇒ `diff_pp = 3.2` ⇒ `reguaMargem(3.2) = 4.0`); manter `.diff` presente para o `diff_source` sair `adman_diff`;
- empresa A do caso de granularidade: `grossBilling.diff = -20.0`; empresa B: `grossBilling.diff = 2.0`.

No teste de granularidade, comparar explicitamente contra a regra antiga invocando `reguaFaturamento(-9.0)` de `DesempenhoScoreService` por Reflection (mesmo helper da Task 1) e asserindo `1.0` — é a prova literal de que régua-da-média ≠ média-das-réguas.

Cada teste deve asserir `assertSame` em floats já arredondados (ex.: `assertSame(4.53, $linha->nota_empresa)`), nunca `assertEquals` com tolerância implícita.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CompanyScoreServiceFormulaTest</automated>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase119</automated>
    <automated>sha256sum app/Services/DesempenhoScoreService.php | grep -q '^cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd'</automated>
  </verify>
  <done>Caso âncora fecha em 4,53; a divergência régua-por-empresa × régua-da-média está provada no mesmo teste; suíte `Phase119` inteira verde; hash inalterado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CarteiraContextService` → `CompanyScoreService` | Define QUAIS empresas pertencem ao profissional — errar aqui vaza dado de carteira alheia para o bônus |
| `bonus_invalidacoes` → cálculo | Decisão administrativa de tirar empresa do bônus da competência |
| Adman API (HTTP) → cálculo | Dado de terceiro que vira nota de bônus; credenciais trafegam no client |
| Suíte de testes → rede | GATE MPP-04 pendente: nenhum teste pode tocar a Adman real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Information Disclosure | `CompanyScoreService` (logs) | mitigate | `Log::warning` só com `user_id`/`company_id`/`competencia` — nunca `company_name`, e-mail, texto de NPS, `cust_id`/`adman_account_id` ou `ADMAN_API_KEY` (mesma disciplina de `NpsPorEmpresaService:264-271`) |
| T-119-02 | Elevation of Privilege | Montagem do universo (Task 2, passo 2) | mitigate | Universo SEMPRE via `CarteiraContextService::forUser($user, ['active'=>true])`; proibido join direto em `company_users`, `Company::all()` ou `$user->companies()`; `CompanyScoreServiceContratoTest` assere que empresa de outro profissional não aparece |
| T-119-03 | Tampering | Invalidação por competência | mitigate | `reject($invalidadas)` ANTES de resolver fonte e de qualquer chamada ao dispatcher; `$invalidadas` nulo resolve via `BonusInvalidacao::companyIdsInvalidadas($mes)`, nunca `collect()` vazio; o MESMO set é repassado ao `NpsPorEmpresaService`; teste com linha real em `bonus_invalidacoes` |
| T-119-04 | Denial of Service | Suíte de testes → Adman | mitigate | `Http::preventStrayRequests()` + `Http::fake()` em toda a suíte `Phase119`; nenhum `diff_pp` de teste vem de rede real (GATE MPP-04 pendente) |
| T-119-07 | Tampering | `DesempenhoScoreService.php` | mitigate | Gate de hash `sha256sum` verificado em TODA task; `git diff --name-only` não pode conter o arquivo |
| T-119-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote instalado (`119-RESEARCH.md` § Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=Phase119` verde
- `sha256sum app/Services/DesempenhoScoreService.php` = `cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd`
- `git diff --name-only` não inclui `app/Services/DesempenhoScoreService.php`
- `grep -rn "CompanyScoreService" app/ routes/ | grep -v "app/Services/Desempenho/CompanyScoreService.php"` não retorna nada — nenhum consumidor de produção
- Nunca rodar `artisan test` sem `--filter` (trava por HTTP real não mockada)
</verification>

<success_criteria>
- `CompanyScoreService::computeEmpresasScore()` devolve a Collection do contrato §3.1 chaveada por `company_id`
- Réguas duplicadas provadas equivalentes por Reflection em todos os boundaries
- Caso âncora 4,53 e divergência régua-por-empresa × régua-da-média provados
- Empresa sem fonte listada (D-03); empresa invalidada e empresa de outro profissional ausentes
- `DesempenhoScoreService` intocado; nenhum consumidor de produção do serviço novo
</success_criteria>

<output>
Criar `.planning/phases/119-score-por-empresa-v21-0/119-02-SUMMARY.md`.

Commits: usar SEMPRE caminhos explícitos (`git add <paths>` seguido de `git commit -m ... -- <paths>`). A árvore de trabalho é compartilhada com outras sessões e com outro dev — **nunca** `git add -A`, **nunca** `git stash`.
</output>
