---
phase: 119-score-por-empresa-v21-0
plan: 03
type: execute
wave: 3
depends_on: [119-02]
files_modified:
  - tests/Feature/Phase119/CompanyScoreServiceMargemTest.php
  - tests/Feature/Phase119/CompanyScoreServiceDispatcherTest.php
  - app/Services/Desempenho/CompanyScoreService.php
autonomous: true
requirements: [EMPS-03, EMPS-05]

must_haves:
  truths:
    - "A margem da linha por empresa é pontuada sobre diff_pp; com a fixture MPP-06 (value=27,47 / prev=24,08) a nota é 4, e NÃO os 5 que diff_pct=14,09 produziria"
    - "Quando diff_pp é null (sem prev, ou comparison_mode diferente de previous_equal_length_window), margem_pontos é null e quality.motivos contém margem_pp_indisponivel"
    - "MetricDiffDispatcher::compute() é chamado exatamente uma vez por empresa com fonte financeira"
    - "Empresa com status sem_fonte nunca chega ao dispatcher — o guard vem antes, e nenhuma InvalidArgumentException é lançada nem capturada"
    - "DesempenhoScoreService.php permanece byte-a-byte intocado"
  artifacts:
    - path: "tests/Feature/Phase119/CompanyScoreServiceMargemTest.php"
      provides: "Prova âncora de EMPS-03 — diff_pp e diff_pct dão notas diferentes na mesma fixture"
      contains: "27.47"
    - path: "tests/Feature/Phase119/CompanyScoreServiceDispatcherTest.php"
      provides: "Contagem de chamadas por empresa (EMPS-05) e guard de fonte nula (C-04)"
  key_links:
    - from: "CompanyScoreService"
      to: "metrics.contribution_margin_pct.diff_pp"
      via: "leitura do campo de pontos percentuais, nunca diff_pct"
      pattern: "diff_pp"
    - from: "CompanyScoreServiceDispatcherTest"
      to: "MetricDiffDispatcher"
      via: "dublê contador registrado no container via app()->instance()"
      pattern: "instance\\(MetricDiffDispatcher::class"
---

<objective>
Provar as duas afirmações que, se estiverem erradas, ficam **invisíveis**: que a margem é pontuada sobre `diff_pp` (EMPS-03) e que o dispatcher é chamado **uma única vez por empresa** (EMPS-05), sem nunca receber fonte nula (C-04).

Purpose: EMPS-03 é o teste âncora da fase — é ele que prova que a milestone de fato trocou a unidade da margem. A fixture MPP-06 foi escolhida **de propósito** porque `diff_pp = 3,39` (⇒ 4 pontos) e `diff_pct = 14,09` (⇒ 5 pontos) divergem: um erro de fio invisível vira teste vermelho, em vez de um bônus silenciosamente errado.
Output: 2 suítes novas em `tests/Feature/Phase119/`; correções pontuais no `CompanyScoreService` se algum comportamento divergir.

**FASE ADITIVA:** `app/Services/DesempenhoScoreService.php` permanece byte-a-byte intocado (hash `cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-score-por-empresa-v21-0/119-CONTEXT.md
@.planning/phases/119-score-por-empresa-v21-0/119-PATTERNS.md
@.planning/phases/119-score-por-empresa-v21-0/119-VALIDATION.md
@.planning/phases/119-score-por-empresa-v21-0/119-02-SUMMARY.md
@app/Services/Desempenho/CompanyScoreService.php
@tests/Feature/V18/AdmanMetricDiffServiceTest.php
@app/Services/Metrics/MetricDiffDispatcher.php
</context>

<interfaces>
<!-- Fixture MPP-06, já validada na Fase 117 (tests/Feature/V18/AdmanMetricDiffServiceTest.php:52-62) -->

```php
'metrics' => [
    'billing'           => ['value' => 530797.73, 'diff' => 101.24, 'prev' => 263768.55],
    'liquidMargin'      => ['value' => 141428.81, 'diff' => 147.96, 'prev' => 57036.05],
    'percentageMargin'  => ['value' => 27.47,     'diff' => 14.09,  'prev' => 24.08],
    'investment'        => ['value' => 9990.82,   'diff' => 80.36,  'prev' => 5539.43],
],
// diff_pp = round(27.47 - 24.08, 2) = 3.39  ⇒ reguaMargem(3.39) = 4.0   ← o correto (EMPS-03)
// diff_pct = 14.09                          ⇒ reguaMargem(14.09) = 5.0  ← o errado, e é O QUE O TESTE PROÍBE
```

```php
// app/Services/Metrics/MetricDiffDispatcher.php:22-42 — assinatura do construtor,
// necessária para construir o dublê contador:
public function __construct(
    private AdmanMetricDiffService $admanMetricDiffService,
    private ShopeeMetricDiffService $shopeeMetricDiffService,
) {}
public function compute(Company $company, array $periodo, string $source): array
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EMPS-03 — margem pontuada sobre diff_pp, nunca sobre diff_pct</name>
  <files>tests/Feature/Phase119/CompanyScoreServiceMargemTest.php, app/Services/Desempenho/CompanyScoreService.php</files>
  <behavior>
    - Fixture MPP-06 em janela igual: a linha traz `margem_pct_atual === 27.47`, `margem_pct_anterior === 24.08`, `margem_var_pp === 3.39` e `margem_pontos === 4.0`. O mesmo teste assere `assertNotSame(5.0, $linha->margem_pontos)` com comentário citando que 5,0 é a nota que `diff_pct = 14.09` produziria — a divergência é a prova.
    - `quality.margin_diff_source === 'adman_diff'` (o `.diff` nativo continua presente e repassado, só não é usado para pontuar).
    - Payload SEM `percentageMargin.prev`: `margem_var_pp === null`, `margem_pontos === null`, `quality.motivos` contém `margem_pp_indisponivel`, `componentes_presentes === 2` e `status === 'partial'`.
    - Período com `comparison_mode === 'same_interval_previous_month'` (mês em curso), mesmo com `value` e `prev` presentes: `margem_var_pp === null` e `margem_pontos === null` — o gate de `comparison_mode` vive dentro do `AdmanMetricDiffService` e a linha por empresa apenas o herda, sem recalcular `value - prev` por fora.
    - A régua de margem nunca recebe `diff_pct`: assere-se também que uma empresa cujo `diff_pct` seria 14,09 não recebe 5 pontos.
  </behavior>
  <action>
Confirmar o gate de aditividade antes e depois (`sha256sum app/Services/DesempenhoScoreService.php`).

Criar `tests/Feature/Phase119/CompanyScoreServiceMargemTest.php` reusando o `setUp` e as fixtures de carteira já estabelecidos em `CompanyScoreServiceContratoTest` (trait `Tests\Feature\V16\CriaCenarioResponsaveis`, `RefreshDatabase`, `PRAGMA foreign_keys = ON`, `Carbon::setTestNow('2026-07-15 10:00:00')` e `Carbon::setTestNow()` no `tearDown`).

Obrigatório: `Http::preventStrayRequests()` + `Http::fake()` — o GATE MPP-04 está pendente e nenhum teste desta fase pode tocar a Adman real. Reusar literalmente os helpers `respostaPerformance()` e `respostaAccountMetrics()` de `tests/Feature/V18/AdmanMetricDiffServiceTest.php:36-62` (fixture MPP-06 já validada), com um parâmetro para remover `percentageMargin.prev` no cenário negativo.

Períodos: janela igual via `app(MetricPeriodResolver::class)->resolve(['period_key' => '2026-06'])` (`comparison_mode='previous_equal_length_window'`); mês em curso via `['period_key' => 'current_month']` (`same_interval_previous_month`). Não montar arrays de período na mão — o resolver real evita divergência de janela.

Dar `adman_account_id` único por teste (ex.: `CUST-MARGEM-01`) para não colidir com o cache do `AdmanMetricDiffService` entre cenários (a cacheKey inclui custId e a janela).

O NPS deve estar fixado por dublê (`$this->mock(NpsPorEmpresaService::class, ...)`) para que os asserts de `componentes_presentes`/`status` não dependam de fixture de NPS.

Se algum assert falhar, corrigir o `CompanyScoreService` — nunca "ajustar" o teste para caber no código. Correções permitidas: leitura do campo, ordem dos motivos, derivação de `status`. Proibido: inventar gate de cobertura de margem (C-01 — `fallbackMargemPct()`/`coberturaMargem()` são código morto, não existe patamar ativo) e recalcular `value - prev` por fora do `AdmanMetricDiffService`.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CompanyScoreServiceMargemTest</automated>
    <automated>sha256sum app/Services/DesempenhoScoreService.php | grep -q '^cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd'</automated>
  </verify>
  <done>Fixture MPP-06 produz `margem_pontos = 4.0` e explicitamente não 5,0; cenários sem `prev` e de mês em curso produzem `margem_pontos = null` com motivo `margem_pp_indisponivel`; hash inalterado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EMPS-05 — dispatcher 1× por empresa + guard de fonte nula (C-04)</name>
  <files>tests/Feature/Phase119/CompanyScoreServiceDispatcherTest.php, app/Services/Desempenho/CompanyScoreService.php</files>
  <behavior>
    - Carteira com 2 empresas com fonte financeira (uma Adman, uma Shopee) e 1 empresa do setor `polos` (sem fonte): o contador registra **exatamente 1** chamada para cada empresa com fonte e **nenhuma** para a empresa sem fonte.
    - A soma total de chamadas é 2 — nunca 4 (que é o que a estrutura antiga, com `computeVarFaturamento` + `computeVarMargem`, produziria).
    - Nenhuma `InvalidArgumentException` é lançada em nenhum cenário: o guard vive ANTES da chamada, não num `catch`. Prova adicional: um dublê cujo `compute()` falha o teste (`$this->fail()`) se receber `$source` que não seja `'adman'` nem `'shopee'` nunca é acionado.
    - Da chamada única saem os DOIS componentes: faturamento e margem da mesma empresa vêm do mesmo `$resultado`.
  </behavior>
  <action>
Confirmar o gate de aditividade antes e depois.

Criar `tests/Feature/Phase119/CompanyScoreServiceDispatcherTest.php`. No mesmo arquivo, declarar uma classe de dublê no fim do arquivo (padrão já usado por `DesempenhoShopeeScoreTestProviderStub` em `tests/Feature/DesempenhoShopeeScoreTest.php`): `class DispatcherContador extends MetricDiffDispatcher`, com propriedade pública `array $chamadas = []` e override de `compute(Company $company, array $periodo, string $source): array` que (a) falha explicitamente se `$source` não for `'adman'`/`'shopee'`, (b) incrementa `$this->chamadas[$company->id]`, (c) delega em `parent::compute(...)`.

Registrar o dublê no container ANTES de resolver o serviço sob teste: construí-lo com `new DispatcherContador(app(AdmanMetricDiffService::class), app(ShopeeMetricDiffService::class))`, registrar com `$this->app->instance(MetricDiffDispatcher::class, $contador)` e só então `app(CompanyScoreService::class)`.

Montar a carteira com três vínculos: serviço de setor `performance` (empresa com `adman_account_id`), serviço de setor `shopee` (empresa com linhas em `shopee_metrics` nas duas janelas — molde dos helpers `mockShopee*` de `DesempenhoShopeeScoreTest`), e serviço de setor `polos` (empresa sem fonte). `Http::preventStrayRequests()` + `Http::fake()` em todos os cenários.

Asserts: `assertSame(1, $contador->chamadas[$empresaAdman->id])`, `assertSame(1, $contador->chamadas[$empresaShopee->id])`, `assertArrayNotHasKey($empresaPolos->id, $contador->chamadas)` e `assertSame(2, array_sum($contador->chamadas))`. Um segundo teste deve provar que a empresa `polos` ainda assim aparece na Collection com `status === 'sem_fonte'` — o guard não pode ter virado "descarta a empresa" (isso quebraria a D-03).

Se a contagem der 2 por empresa, corrigir o `CompanyScoreService` para extrair todos os campos de um único `$resultado` — nunca "resolver" duplicando cache ou memo.
  </action>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=CompanyScoreServiceDispatcherTest</automated>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase119</automated>
    <automated>sha256sum app/Services/DesempenhoScoreService.php | grep -q '^cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd'</automated>
  </verify>
  <done>Contagem de 1 chamada por empresa com fonte, zero para a empresa sem fonte, total 2; empresa sem fonte segue listada com `status='sem_fonte'`; suíte `Phase119` inteira verde; hash inalterado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adman API (HTTP) → nota de bônus | `percentageMargin.prev` passa a decidir pontos de margem |
| Suíte de testes → rede | GATE MPP-04 pendente: nenhum teste pode depender de comportamento real da Adman |
| `MetricDiffDispatcher` → serviços de fonte | `$source` é validado por whitelist e lança em valor inesperado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-04 | Denial of Service | Suíte `Phase119` → Adman | mitigate | `Http::preventStrayRequests()` + `Http::fake()` em todos os cenários; toda fixture de `diff_pp` vem de payload controlado (GATE MPP-04 pendente) |
| T-119-08 | Tampering | Leitura da métrica de margem | mitigate | Régua sempre sobre `metrics.contribution_margin_pct.diff_pp`; proibido recalcular `value - prev` por fora (o gate de `comparison_mode` vive dentro do `AdmanMetricDiffService`); teste âncora MPP-06 falha em vermelho se o fio trocar para `diff_pct` |
| T-119-09 | Denial of Service | `computeEmpresasScore()` em carteira grande | mitigate | Uma chamada por empresa (EMPS-05), provada por contador — nunca 2; a fase não aumenta o volume de HTTP à Adman em relação ao caminho atual |
| T-119-10 | Tampering | Guard de fonte nula | mitigate | `if ($fonteFinanceira !== null)` ANTES da chamada; nunca `try/catch` em volta do dispatcher (mascararia fonte inválida real); dublê falha o teste se receber `$source` fora da whitelist |
| T-119-01 | Information Disclosure | Logs / fixtures de teste | mitigate | Fixtures usam `cust_id` sintético (`CUST-MARGEM-01`); nenhum log de `cust_id`, `company_name` ou credencial Adman |
| T-119-07 | Tampering | `DesempenhoScoreService.php` | mitigate | Gate de hash verificado em toda task |
| T-119-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote instalado nesta fase |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=Phase119` verde
- Nenhuma chamada real à Adman (`Http::preventStrayRequests()` ativo em toda a suíte)
- `sha256sum app/Services/DesempenhoScoreService.php` inalterado
- Nunca rodar `artisan test` sem `--filter`
</verification>

<success_criteria>
- EMPS-03 provado com a fixture divergente MPP-06 (4 pontos, e explicitamente não 5)
- `margem_pp_indisponivel` provado nos dois caminhos que zeram `diff_pp` (sem `prev` e `comparison_mode` diferente)
- EMPS-05 provado por contagem: 1 chamada por empresa com fonte, 0 para a sem fonte
- C-04 provado: guard antes da chamada, nenhuma exceção lançada nem capturada, empresa sem fonte ainda listada
</success_criteria>

<output>
Criar `.planning/phases/119-score-por-empresa-v21-0/119-03-SUMMARY.md`.

Commits com caminhos explícitos (`git add <paths>` + `git commit -m ... -- <paths>`). Árvore compartilhada: **nunca** `git add -A`, **nunca** `git stash`.
</output>
