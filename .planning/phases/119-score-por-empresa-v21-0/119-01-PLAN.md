---
phase: 119-score-por-empresa-v21-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [EMPS-03]

must_haves:
  truths:
    - "O veredito do probe de estabilidade de percentageMargin.prev foi apresentado ao usuário e ele decidiu explicitamente se a Fase 119 pode consumir diff_pp para calcular nota"
    - "Nenhuma linha de código da Fase 119 é escrita antes desse veredito"
  artifacts:
    - path: ".planning/phases/119-score-por-empresa-v21-0/119-01-SUMMARY.md"
      provides: "Registro do veredito MPP-04 (aprovado / reprovado / instrumentacao_suspeita) com data, competência e número de leituras"
  key_links:
    - from: "119-01-SUMMARY.md"
      to: "adman_probe_margem_prev_vereditos"
      via: "reconsulta ao veredito persistido pelo comando adman:probe-margem-prev --relatorio"
      pattern: "adman:probe-margem-prev"
---

<objective>
Destravar (ou travar) a execução da Fase 119 pelo **GATE MPP-04**.

A Fase 119 é a primeira a **consumir `diff_pp` para calcular nota de bônus**. O `<blocking_dependency>` do `119-CONTEXT.md` e o `Depends on` do ROADMAP exigem que o probe de estabilidade de `percentageMargin.prev` (Fase 117) tenha rodado na VPS e que o veredito tenha sido **aprovado pelo usuário** antes de qualquer código desta fase ser escrito.

Estado em 2026-07-28: apenas a leitura L1 (`pico_tarde`) registrada; faltam ≥4 leituras, incluindo a obrigatória na janela 11:00-12:00 BRT (`contencao_11h`).

Purpose: impedir que o motor de bonificação passe a pagar sobre uma métrica cuja estabilidade nunca foi medida — o histórico de `project_adman_margem_diff_instavel_bonus` mostra que a mesma família de métrica já oscilou ±40-77% por empresa entre recomputes.
Output: veredito registrado no SUMMARY, e a decisão do usuário de seguir, parar ou mudar a forma da fase.

**Este plano NÃO escreve código.** Ele existe porque um checkpoint bloqueante não pode dividir plano com implementação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119-score-por-empresa-v21-0/119-CONTEXT.md
@.planning/phases/119-score-por-empresa-v21-0/119-VALIDATION.md
@.planning/phases/117-margem-em-pontos-percentuais-probe-de-estabilidade-de-prev/117-02-SUMMARY.md
@.planning/REQUIREMENTS-v21.md
</context>

<tasks>

<!-- ⚠️ TIPO CORRIGIDO APÓS PLAN-CHECK (2026-07-29).
     Era `checkpoint:human-verify`. Sob `workflow.human_verify_mode` AUSENTE do
     `.planning/config.json` — que é o caso deste projeto — o default é
     `end-of-phase`, e a referência do GSD é explícita
     (`references/planner-human-verify-mode.md:51`):
       "Only `checkpoint:human-verify` is suppressed."
     Como este plano não tem NENHUMA task `auto` para colher um
     `<verify><human-check>`, o gate seria simplesmente suprimido e o plano
     poderia ser dado como concluído sem veredito humano — liberando a wave 2 a
     escrever código de margem sobre `diff_pp` sem aprovação. A salvaguarda da
     fase inteira seria teatro.
     `checkpoint:decision` é imune ao modo end-of-phase E é semanticamente
     correto: o usuário ESCOLHE entre três caminhos distintos, não verifica
     trabalho já construído. -->
<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: GATE MPP-04 — veredito do probe de estabilidade de percentageMargin.prev</name>
  <decision>
    O `percentageMargin.prev` da Adman é estável o bastante para amarrar pagamento de bônus?

    Nada é construído nesta task. A Fase 117 (Plano 02) entregou o instrumento: comando `adman:probe-margem-prev` (leitura sem cache via `forceRefresh: true`) e as tabelas insert-only `adman_probe_margem_prev_leituras` / `adman_probe_margem_prev_vereditos`. O que falta é **rodar o desenho amostral na VPS e ler o veredito**.
  </decision>
  <options>
    - **`aprovado`** — cobertura de `prev` ≥ 0,8 · zero flip de nota · ≥1 leitura em `contencao_11h` · ≥5 rodadas · payloads NÃO idênticos bit-a-bit. ⇒ libera os planos 119-02, 119-03 e 119-04.
    - **`reprovado`** — alguma empresa muda de faixa da régua entre leituras, ou cobertura < 0,8. ⇒ a fase **muda de forma**; os planos 02/03/04 **não** devem ser executados como estão. Exige replanejamento com o usuário (plano B: congelar `prev` em snapshot × voltar ao cálculo local — o `117-CONTEXT.md` deliberadamente não pré-decidiu).
    - **`instrumentacao_suspeita`** — payloads idênticos bit-a-bit. **Nunca é sucesso.** Verificar se `forceRefresh: true` está sendo propagado antes de qualquer conclusão. ⇒ interrompe a fase.
  </options>
  <action>
Não escrever, editar ou criar nenhum arquivo de código nesta task. O trabalho do executor é: (1) apresentar ao usuário o estado atual do desenho amostral do probe — competência fixada, número de leituras registradas e presença da leitura obrigatória em `contencao_11h`; (2) se houver acesso à VPS e as leituras estiverem completas, rodar `php artisan adman:probe-margem-prev --relatorio --mes=<competência fixada>` para emitir o veredito e reconsultar a tabela `adman_probe_margem_prev_vereditos` para lê-lo — nunca confiar no stdout, mesma disciplina do gate FIXMARG-03; (3) pausar e aguardar a decisão explícita do usuário. Se o desenho amostral estiver incompleto, reportar exatamente o que falta e pausar — não estimar, não presumir aprovação, não seguir para os planos 02/03/04 enquanto isso.
  </action>
  <como-conferir>
    1. Na VPS, confirmar que o desenho amostral está completo (runbook em `117-02-SUMMARY.md` § "Runbook de Execução do Gate MPP-04"):
       - competência FIXA em todas as leituras (ex.: `--mes=2026-06`, nunca `last_closed_month`);
       - no mínimo **5 leituras** espalhadas em 24-48h;
       - pelo menos **1 leitura com `--janela=contencao_11h` dentro de 11:00-12:00 BRT** (obrigatória).
    2. Emitir/reconsultar o veredito, sem tocar a Adman:
       `php artisan adman:probe-margem-prev --relatorio --mes=2026-06`
    3. Ler o veredito persistido (nunca confiar só no stdout — mesma disciplina do gate FIXMARG-03): reconsultar `adman_probe_margem_prev_vereditos` pela competência e conferir `veredito`, cobertura de `prev`, contagem de rodadas e `empresas_com_flip`.
    4. Interpretar:
       - **`aprovado`** (cobertura de `prev` ≥ 0,8 · zero flip de nota · ≥1 leitura em `contencao_11h` · ≥5 rodadas · payloads NÃO idênticos bit-a-bit) ⇒ a Fase 119 pode seguir; responder `aprovado`.
       - **`reprovado`** ⇒ a fase **muda de forma**; NÃO executar os planos 02/03/04 como estão. Responder `reprovado` e o que fazer (plano B).
       - **`instrumentacao_suspeita`** (payloads idênticos bit-a-bit) ⇒ **nunca é sucesso**; verificar se `forceRefresh: true` está sendo propagado antes de qualquer conclusão. Responder `instrumentacao_suspeita`.
    5. Registrar no SUMMARY: veredito, competência usada, número de leituras, cobertura de `prev` e data.
  </como-conferir>
  <resume-signal>Responder `aprovado` (libera os planos 119-02, 119-03 e 119-04), `reprovado` ou `instrumentacao_suspeita` (ambos interrompem a fase e exigem replanejamento com o usuário).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adman API → motor de bônus | Métrica de terceiro (`percentageMargin.prev`) passa a decidir pagamento de bônus |
| VPS → sessão de planejamento | O veredito vem de execução externa; a sessão não pode presumi-lo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-05 | Tampering | Veredito do gate MPP-04 | mitigate | Veredito conferido por **reconsulta à tabela** `adman_probe_margem_prev_vereditos`, nunca por stdout; `instrumentacao_suspeita` tem precedência sobre qualquer leitura otimista |
| T-119-06 | Repudiation | Decisão de liberar a fase | mitigate | Veredito, competência, nº de leituras e data registrados no `119-01-SUMMARY.md` — auditável na Fase 121 |
| T-119-SC | Tampering | Instalação de pacotes (npm/composer) | accept | Nenhum pacote é instalado nesta fase (`119-RESEARCH.md` § Package Legitimacy Audit = N/A) — nada a auditar |
</threat_model>

<verification>
- Veredito lido por reconsulta à tabela `adman_probe_margem_prev_vereditos`, não por stdout
- Desenho amostral completo (≥5 rodadas, ≥1 `contencao_11h`, competência fixa)
- Decisão do usuário registrada no SUMMARY
</verification>

<success_criteria>
- Usuário respondeu `aprovado`, `reprovado` ou `instrumentacao_suspeita`
- Se `aprovado`: planos 119-02/03/04 liberados para execução
- Se qualquer outro: fase interrompida e o replanejamento é acordado com o usuário — **nenhum arquivo de código criado**
</success_criteria>

<output>
Criar `.planning/phases/119-score-por-empresa-v21-0/119-01-SUMMARY.md` com o veredito, a competência, o número de leituras, a cobertura de `prev` e a decisão do usuário.
</output>
