---
phase: 93-menu-grupo-transversal-gestao-ecf-v17-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/Layouts/AppLayout.jsx
autonomous: false
requirements: [MENU-01]

must_haves:
  truths:
    - "O menu lateral mostra um grupo 'Gestão ECF' contendo Carteira, Desempenho e Metas (nessa ordem)"
    - "O grupo 'Mercado Livre' não contém mais Carteira, Desempenho nem Metas — mantém Dashboard, Empresas, Sugadores, PPA, Grants + as seções Dados Estratégicos e Polos"
    - "O grupo 'Shopee' permanece intocado (Empresas, Dashboard) — comportamento idêntico"
    - "Um analista/estrategista continua vendo Carteira e Desempenho (permission core.carteira / core.performance preservadas, só mudaram de grupo)"
    - "A página Desempenho mantém o array explícito de páginas no highlight — não engole Performance/Dashboard nem o Dashboard do ML"
    - "Se todos os filhos de 'Gestão ECF' estiverem escondidos por permissão, o grupo some (lógica filhosReais preservada, sem regressão)"
  artifacts:
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "NAV_TREE com novo grupo transversal 'Gestão ECF' e grupo 'Mercado Livre' enxuto"
      contains: "group: 'Gestão ECF'"
  key_links:
    - from: "NAV_TREE grupo 'Gestão ECF' > Carteira"
      to: "portfolio.own"
      via: "routeName + permission core.carteira + page 'Portfolio'"
      pattern: "portfolio\\.own"
    - from: "NAV_TREE grupo 'Gestão ECF' > Desempenho"
      to: "performance.index"
      via: "routeName + permission core.performance + page array [Performance/Index, Performance/Show, Desempenho/Configuracao]"
      pattern: "performance\\.index"
    - from: "NAV_TREE grupo 'Gestão ECF' > Metas"
      to: "goals.index"
      via: "routeName + permission core.metas + page 'Goals'"
      pattern: "goals\\.index"
---

<objective>
Criar um grupo transversal "Gestão ECF" no menu lateral (`AppLayout.jsx`) contendo
**Carteira**, **Desempenho** e **Metas** — movendo esses três itens para fora do
grupo "Mercado Livre". O grupo "Mercado Livre" passa a conter apenas telas
realmente ML (Dashboard, Empresas, Sugadores, PPA, Grants + as seções
"Dados Estratégicos" e "Polos"). O grupo "Shopee" fica intocado.

Purpose: Carteira e Desempenho são setores transversais (ML + Shopee, multi-serviço)
— não pertencem visualmente dentro do grupo Mercado Livre. Alinha a navegação ao
plano canônico `plano-carteira-desempenho-multi-servico.md` (§5 "Ajustar menu").

Output: `resources/js/Layouts/AppLayout.jsx` reorganizado + `npm run build` verde +
checkpoint visual confirmando a nova árvore.

## Decisões desta fase (registradas)

- **D-93-01 — Posição do grupo**: "Gestão ECF" fica ABAIXO do item de topo
  "ECF Dashboard" (que permanece no topo, admin-only) e ACIMA do grupo
  "Mercado Livre" — espelha a sugestão do plano canônico (`Gestão ECF` antes de
  `Mercado Livre`), mantendo o dashboard consolidado da empresa no topo absoluto.
- **D-93-02 — Ícone**: `FolderKanban` (já importado em `AppLayout.jsx` linha 7,
  sem colisão semântica com Store/ShoppingCart/Building2). NÃO adicionar import novo.
- **D-93-03 — defaultOpen**: `true` — é onde analista/estrategista vivem; grupo
  começa aberto na primeira visita (mesma semântica do grupo Mercado Livre).
- **D-93-04 — Ordem interna**: Carteira → Desempenho → Metas (ordem do plano canônico).

## Pergunta em aberto (NÃO decidida — confirmar no checkpoint visual)

- **Empresas** (`companies.index`, painel Performance) PERMANECE no grupo "Mercado
  Livre" conforme o plano canônico (§ árvore sugerida — "Empresas" listado sob
  Mercado Livre, não sob Gestão ECF). O plano canônico deixa Empresas no ML apesar
  de `/companies` ser o painel Performance. Seguindo o plano; se o usuário quiser
  mover Empresas para Gestão ECF, decidir no checkpoint (fora do escopo travado desta fase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@plano-carteira-desempenho-multi-servico.md
@resources/js/Layouts/AppLayout.jsx

<interfaces>
<!-- Estado atual do NAV_TREE — o executor usa isto diretamente, sem explorar o codebase. -->
<!-- Os 3 itens a MOVER (do grupo 'Mercado Livre', linhas ~70/72/74): -->

Desempenho (preservar o page array EXATO — é o que impede engolir Performance/Dashboard):
  { label: 'Desempenho',  routeName: 'performance.index',   page: ['Performance/Index', 'Performance/Show', 'Desempenho/Configuracao'],  icon: Trophy,          permission: 'core.performance' },

Carteira:
  { label: 'Carteira',    routeName: 'portfolio.own',       page: 'Portfolio',    icon: Briefcase,       permission: 'core.carteira' },

Metas:
  { label: 'Metas',       routeName: 'goals.index',         page: 'Goals',        icon: Target,          permission: 'core.metas' },

<!-- O grupo 'Mercado Livre' MANTÉM (não mover): -->
  Dashboard (mercadolivre.dashboard, core.dashboard, page array [Dashboard/Admin, Performance/Dashboard])
  Empresas  (companies.index, core.empresas)  ← permanece no ML por decisão do plano canônico
  Sugadores (sugadores.index, core.sugadores, showBadge)
  PPA       (ppa.index, core.ppa)
  + divider 'Dados Estratégicos' e toda a seção (Painel Executivo, Concentração, Alertas, Grants)
  + divider 'Polos' e toda a seção (Painel Polos, Onboarding, Empresas Polos, Faturamento Polos)

<!-- Lógica de gating/visibilidade (JÁ existente — NÃO alterar, apenas confiar): -->
itemVisivel(item): esconde por excludeRoles OU por permission ausente. Dividers sempre visíveis.
filteredTree (useMemo): para cada grupo, filtra children por itemVisivel + removerDividersOrfaos;
  só empurra o grupo se `filhosReais.length > 0` → grupo com todos os filhos gated NÃO aparece.
openGroups (useState init): itera NAV_TREE; aplica defaultOpen na primeira visita e auto-abre
  o grupo cuja rota atual bate (startsWith em c.page). Genérico — funciona pro novo grupo sem tocar.
isActive(page): array de pages é OR de startsWith — por isso o array de Desempenho precisa ser
  preservado literalmente (senão o highlight regride).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar grupo "Gestão ECF" e enxugar o grupo "Mercado Livre"</name>
  <files>resources/js/Layouts/AppLayout.jsx</files>
  <action>
    Reorganização PURAMENTE VISUAL do array `NAV_TREE`. NÃO tocar rotas, controllers,
    permissões, nem a lógica de `itemVisivel`/`filteredTree`/`openGroups`/`isActive`.

    1. REMOVER do array `children` do grupo `{ group: 'Mercado Livre' }` os três objetos
       de item: Desempenho (`performance.index`), Carteira (`portfolio.own`) e
       Metas (`goals.index`). Após remover, o grupo ML deve manter, na ordem atual:
       Dashboard → Empresas → Sugadores → PPA → divider 'Dados Estratégicos' → seção
       Dados Estratégicos → divider 'Polos' → seção Polos. (Empresas PERMANECE no ML,
       por D-93 pergunta em aberto — não mover.)

    2. INSERIR um novo grupo transversal ENTRE o item de topo `{ label: 'ECF Dashboard' }`
       (linha ~46) e o grupo `{ group: 'Mercado Livre' }` (linha ~48), conforme D-93-01:

         {
             group: 'Gestão ECF',
             icon: FolderKanban,
             defaultOpen: true,
             children: [
                 { label: 'Carteira',   routeName: 'portfolio.own',     page: 'Portfolio', icon: Briefcase, permission: 'core.carteira' },
                 { label: 'Desempenho', routeName: 'performance.index', page: ['Performance/Index', 'Performance/Show', 'Desempenho/Configuracao'], icon: Trophy, permission: 'core.performance' },
                 { label: 'Metas',      routeName: 'goals.index',       page: 'Goals',     icon: Target,    permission: 'core.metas' },
             ],
         },

       CRÍTICO: copiar cada item com `permission`/`excludeRoles`/`page` EXATAMENTE como
       estavam no ML — quem via, continua vendo. O `page` de Desempenho é array literal
       (três strings) — preservar as três, sem simplificar, senão o highlight regride e
       o item passa a "engolir" Performance/Dashboard.

    3. Usar o ícone `FolderKanban` (D-93-02) — já está no import da linha 7. NÃO adicionar
       import novo (verificar que `FolderKanban` consta no import de `lucide-react`; se por
       acaso não constar, usar `LayoutList` que também já está importado, e registrar no SUMMARY).

    4. Adicionar um comentário de seção em pt-BR acima do novo grupo explicando a Fase 93
       (grupo transversal Gestão ECF — Carteira/Desempenho/Metas saem do ML por serem
       multi-marketplace/multi-serviço; ver plano canônico §5).

    5. NÃO alterar `filteredTree`, `openGroups`, `itemVisivel`, `isActive` — a lógica de
       grupo vazio (`filhosReais.length > 0`) e de defaultOpen já é genérica e cobre o novo grupo.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run build && grep -c "group: 'Gestão ECF'" resources/js/Layouts/AppLayout.jsx | grep -qx 1 && grep -v '^ *//' resources/js/Layouts/AppLayout.jsx | grep -c "routeName: 'performance.index'" | grep -qx 1 && echo GATES_OK</automated>
  </verify>
  <done>
    `npm run build` sai com exit 0; existe exatamente 1 grupo `group: 'Gestão ECF'`;
    `performance.index` aparece exatamente 1 vez (movido, não duplicado); o page array de
    Desempenho está preservado; grupo ML sem Carteira/Desempenho/Metas.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Menu lateral reorganizado: novo grupo "Gestão ECF" (Carteira, Desempenho, Metas)
    acima do grupo "Mercado Livre", que ficou só com telas ML. Build já rodou verde.
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev` (ou usar o ambiente já servido) e abrir o painel logado como **admin**.
    2. Na sidebar, confirmar a ordem dos grupos de cima pra baixo:
       ECF Dashboard (topo) → **Gestão ECF** (aberto) → Mercado Livre → Shopee → demais itens.
    3. Expandir "Gestão ECF": deve listar **Carteira, Desempenho, Metas** (nessa ordem).
    4. Expandir "Mercado Livre": Carteira/Desempenho/Metas **NÃO** aparecem lá; continuam
       Dashboard, Empresas, Sugadores, PPA, seção Dados Estratégicos e seção Polos.
    5. Clicar em "Desempenho" → a página abre e o item **Desempenho** fica destacado
       (NÃO o "Dashboard" do ML). Navegar por Performance/Index e Performance/Show — highlight
       segue em Desempenho.
    6. (Se possível) logar como **analista ou estrategista**: confirmar que **Carteira** e
       **Desempenho** continuam visíveis dentro de "Gestão ECF" (permissões preservadas).
    7. Confirmar que "Shopee" (Empresas, Dashboard) está idêntico ao anterior.
    8. Confirmar se você concorda em manter **Empresas** dentro de "Mercado Livre"
       (pergunta em aberto D-93) ou se quer movê-la para Gestão ECF numa fase futura.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que ajustar (inclui a decisão sobre Empresas).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova fronteira de confiança. Esta fase é reordenamento visual do array
`NAV_TREE` no frontend — não cria/altera rotas, controllers, endpoints, permissões
nem entrada de dados. O gating de visibilidade (`itemVisivel` por permission/excludeRoles)
permanece idêntico; a autorização real continua no backend (middleware/policies), não no menu.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Information Disclosure | NAV_TREE (menu client-side) | accept | Menu nunca foi fronteira de segurança; o backend autoriza cada rota. Preservar `permission`/`excludeRoles` exatos ao mover os itens evita mudar quem enxerga o link (não a autorização). Sem pacotes novos, sem SC. |
</threat_model>

<verification>
- `npm run build` exit 0 (obrigatório, sem warnings de bundle novos).
- Grep gate: exatamente 1 `group: 'Gestão ECF'`; `performance.index` uma única vez.
- Nenhuma mudança fora de `resources/js/Layouts/AppLayout.jsx` (fronteira da fase).
- Checkpoint visual humano confirma os 3 success criteria do ROADMAP.
</verification>

<success_criteria>
1. Sidebar mostra grupo "Gestão ECF" com Carteira, Desempenho e Metas.
2. Grupo "Mercado Livre" mantém apenas telas ML (Dashboard, Empresas, Sugadores, PPA, Grants,
   + Dados Estratégicos + Polos) — Carteira/Desempenho/Metas não aparecem mais lá.
3. Grupo "Shopee" (Empresas, Dashboard) inalterado.
4. Permissões preservadas: analista/estrategista continua vendo Carteira e Desempenho.
5. Desempenho mantém o page array (highlight não engole Performance/Dashboard).
6. `npm run build` verde.
</success_criteria>

<output>
Criar `.planning/phases/93-menu-grupo-transversal-gestao-ecf-v17-0/93-01-SUMMARY.md` ao concluir.
</output>
