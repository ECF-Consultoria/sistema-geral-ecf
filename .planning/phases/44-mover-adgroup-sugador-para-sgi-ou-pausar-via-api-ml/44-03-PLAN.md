---
phase: 44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml
plan: 03
type: execute
wave: 3
depends_on:
  - 44-02
files_modified:
  - resources/js/Components/MoveToSgiModal.jsx
  - resources/js/Components/UndoToast.jsx
  - resources/js/Pages/Sugadores/Show.jsx
  - app/Http/Controllers/SugadorController.php
autonomous: false
requirements:
  - REQ-44-07
  - REQ-44-08
  - REQ-44-09

must_haves:
  truths:
    - "Operador admin no `/sugadores/{id}` clica botão amarelo 'Mover para SGI', modal abre, escolhe SGI existente OU '+ Criar nova SGI', confirma duas vezes, e o adgroup é movido de fato no ML — visível tanto no toast quanto no painel ML do cliente"
    - "Modal MoveToSgiModal evoluído suporta DOIS modos via prop `mode`: 'audit_only' (legacy — Index bulk, comportamento atual de só registrar audit log) e 'api_call' (novo — Show individual, chama POST /sugadores/{id}/mover-sgi ou /criar-sgi-e-mover de verdade)"
    - "Quando mode='api_call' e operador escolhe '+ Criar nova SGI' no combobox, modal mostra input editável com placeholder 'SGI YYYY-MM' (mês corrente preenchido); botão de submit muda label pra 'Criar SGI e mover'"
    - "Quando mode='api_call' e SGI selecionada tem `status === 'active'`, modal exibe banner amber NÃO-bloqueante: 'Esta SGI está ATIVA — o adgroup vai continuar gastando após o move. Pause manualmente no painel do Mercado Livre depois.'"
    - "Botão de submit do modal em mode='api_call' é destrutivo (bg-red-600 text-white) — visualmente distinto do amarelo do legacy. Texto: 'Confirmar mover' (SGI existente) ou 'Criar SGI e mover' (nova SGI)"
    - "Após POST 200, toast UndoToast aparece bottom-right com mensagem 'Adgroup movido para {sgi_nome}.', botão 'Desfazer (Ns)' com contador decrescente 10→1, auto-dismiss em 10s. Click em 'Desfazer' chama POST /sugadores/{id}/desfazer-move e mostra novo toast de confirmação"
    - "Erro mapeado por código HTTP no banner inline do modal: 401 'Sessão com o ML expirou', 403 'Token sem permissão de escrita', 404 'Adgroup não existe mais no ML', 422 mensagem do body, 5xx 'ML instável', timeout 'demorou demais'"
    - "Bulk (Index) continua chamando o endpoint legacy /sugadores/{id}/move e /sugadores/bulk-move (audit-only) — comportamento existente preservado via mode='audit_only' (default da prop pra back-compat)"
    - "npm run build completa sem erros; nenhuma nova dependência npm é instalada"
    - "SugadorController::show passa novas props: `mode_mover_sgi` ('api_call' quando feature flag on; 'audit_only' caso contrário), `ml_scope_has_write` (bool, lido de mlToken.scope contém 'write'), e enriquece `sugador.company` com {id, name, ml_scope_has_write}"
  artifacts:
    - path: "resources/js/Components/MoveToSgiModal.jsx"
      provides: "Modal evoluído com mode prop ('audit_only'|'api_call'), input criar nova SGI, banner SGI ativa, botão destrutivo, mapa de erros HTTP"
      contains: "mode === 'api_call'"
      min_lines: 250
    - path: "resources/js/Components/UndoToast.jsx"
      provides: "Componente novo — toast bottom-right com botão Desfazer + contador decrescente + auto-dismiss"
      contains: "UndoToast"
      min_lines: 80
    - path: "resources/js/Pages/Sugadores/Show.jsx"
      provides: "Conexão Show ↔ MoveToSgiModal (mode='api_call') + UndoToast (state em memória)"
      contains: "UndoToast"
    - path: "app/Http/Controllers/SugadorController.php"
      provides: "show() passa props mode_mover_sgi + ml_scope_has_write"
      contains: "mode_mover_sgi"
  key_links:
    - from: "Show.jsx::botão 'Mover para SGI'"
      to: "MoveToSgiModal (mode='api_call')"
      via: "setShowMoveModal(true) + prop mode"
      pattern: "mode=.api_call."
    - from: "MoveToSgiModal::submit (mode='api_call')"
      to: "POST /sugadores/{id}/mover-sgi OR /criar-sgi-e-mover"
      via: "router.post via @inertiajs/react"
      pattern: "sugadores.mover-sgi|sugadores.criar-sgi-e-mover"
    - from: "MoveToSgiModal::onSuccess (mode='api_call')"
      to: "Show.jsx::setUndoState({sugadorId, originalCampaignId, sgiNome})"
      via: "callback prop"
      pattern: "setUndoState"
    - from: "UndoToast::onUndo"
      to: "POST /sugadores/{id}/desfazer-move"
      via: "router.post"
      pattern: "sugadores.desfazer-move"
  truths_extra: []
---

<objective>
Entregar a UX completa do fluxo "Mover para SGI via API ML" no `Sugadores/Show.jsx`, evoluindo o `MoveToSgiModal` existente (sem quebrar o uso bulk do Index) e adicionando componente `UndoToast` novo. Plan 44-02 entrega o backend; este plan conecta a UI.

Decisões de design (44-UI-SPEC.md):
- EVOLUIR `MoveToSgiModal`, não substituir (Opção A da UI-SPEC §1)
- Nova prop `mode: 'audit_only' | 'api_call'` (default `'audit_only'` pra preservar Index bulk)
- Novo componente `UndoToast.jsx` isolado — sem wiring de Radix Toast Provider novo
- Botão de submit muda de amarelo (legacy) pra vermelho destrutivo quando `mode='api_call'`
- Sem nova lib npm

Purpose: dar ao operador uma única ação clicável que substitui ~5 cliques manuais no painel ML (escolher campanha SGI, abrir adgroup, selecionar ads, mover, confirmar). Toast Desfazer 10s cobre 95% dos "cliquei errado".
Output: 2 componentes JSX (1 evoluído, 1 novo), Show.jsx atualizado pra orquestração, controller passa 2 props novas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-CONTEXT.md
@.planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-UI-SPEC.md

# Plan 44-02 SUMMARY tem o response shape final do backend — INPUT obrigatório
@.planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-02-SUMMARY.md

# Componente a evoluir (não substituir!)
@resources/js/Components/MoveToSgiModal.jsx

# Página que monta o modal hoje
@resources/js/Pages/Sugadores/Show.jsx

# Controller que passa props pra Show.jsx
@app/Http/Controllers/SugadorController.php

# Modelo MlToken (para checar scope contém 'write')
@app/Models/MlToken.php

<interfaces>
<!-- Contratos que o Plan 44-03 consome (do backend 44-02) e produz (props/callbacks novos). -->

From Plan 44-02 backend (response shape de sucesso, JSON):
```js
// POST /sugadores/{id}/mover-sgi (mode SGI existente)
{
  success: 3,
  failed: 0,
  total_mlbs: 3,
  sgi_id: 9999,
  sgi_nome: "SGI 2026-06",
  campaign_id_original: 12345,
  message: "Adgroup movido — 3 de 3 MLBs."
}

// POST /sugadores/{id}/criar-sgi-e-mover (mode nova SGI)
{
  success: 3,
  failed: 0,
  total_mlbs: 3,
  sgi_id: 7777,  // NEW campaign id criado
  sgi_nome: "SGI 2026-06",
  campaign_id_original: 12345,
  message: "..."
}

// POST /sugadores/{id}/desfazer-move
{
  success: 3,
  failed: 0,
  total_mlbs: 3,
  status_anterior_restaurado: "pendente",
  message: "Move desfeito."
}

// Erros (HTTP 422):
{ message: "..." [, failures: [...]] }
```

NOVAS props em Show.jsx (passadas pelo SugadorController::show modificado nesta plan):
```js
{
  sugador: {...},                  // existente
  url_anuncio: "...",              // existente
  url_ads: "...",                  // existente
  can_update: true,                // existente
  mlbs: [...],                     // existente
  mode_mover_sgi: 'api_call',      // NOVO — 'api_call' | 'audit_only'
  ml_scope_has_write: true,        // NOVO — bool
}
```

Modal props (CONTRATO NOVO — back-compat preservada via defaults):
```jsx
<MoveToSgiModal
  company={{ id, name }}                   // existente
  sugadorIds={[id]}                        // existente
  mode="api_call"                          // NOVO — default 'audit_only'
  sugadorAdgroupName="Tênis preto 42"      // NOVO — para double-confirm banner
  originalCampaignId={12345}               // NOVO — capturado p/ undo
  onClose={() => ...}                      // existente
  onSuccess={(result) => ...}              // EXPANDIDO — recebe response do backend
/>
```

UndoToast props (componente novo):
```jsx
<UndoToast
  message="Adgroup movido para SGI 2026-06."
  durationMs={10000}
  onUndo={() => router.post(route('sugadores.desfazer-move', sugador.id), ...)}
  onDismiss={() => setUndoState(null)}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Expandir SugadorController::show com 2 props novas (mode_mover_sgi + ml_scope_has_write)</name>
  <files>app/Http/Controllers/SugadorController.php</files>
  <read_first>
    - app/Http/Controllers/SugadorController.php (linhas 265-286 — método show)
    - app/Models/MlToken.php (coluna scope)
    - config/features.php (criada no Plan 44-02 Tarefa 2)
  </read_first>
  <behavior>
    - Show passa 2 props novas além das existentes:
      * `mode_mover_sgi`: string — `'api_call'` quando `config('features.sugadores_mover_sgi')` é true E empresa tem MlToken active E scope contém 'write'; caso contrário `'audit_only'`
      * `ml_scope_has_write`: bool — `true` se MlToken active E scope contém 'write'; usado no Plan 44-04 pro banner amarelo (escopo deste plan: passar apenas; banner é Plan 44-04)
    - Não muda assinatura, não muda loadMissing existente — só adiciona 2 props
    - Comentário pt-BR explicando o roteamento
  </behavior>
  <action>
    Editar método `show()` em `app/Http/Controllers/SugadorController.php`:

    Antes do `return Inertia::render(...)`:
    ```php
    // Phase 44 — props que governam UI de Mover SGI:
    //   mode_mover_sgi: 'api_call' (feature flag ON + token escopo write) OU 'audit_only' (legacy)
    //   ml_scope_has_write: bool (consumido pelo banner do Plan 44-04 quando false)
    $sugador->loadMissing('company.mlToken');
    $token             = $sugador->company?->mlToken;
    $mlScopeHasWrite   = $token && $token->status === 'active'
                         && str_contains((string) $token->scope, 'write');
    $featureFlagOn     = (bool) config('features.sugadores_mover_sgi', false);
    $modeMoverSgi      = ($featureFlagOn && $mlScopeHasWrite) ? 'api_call' : 'audit_only';
    ```

    Expandir o array do `Inertia::render`:
    ```php
    return Inertia::render('Sugadores/Show', [
        'sugador'            => $sugador,
        'url_anuncio'        => $sugador->urlAnuncioML(),
        'url_ads'            => $sugador->linkAdsML(),
        'can_update'         => Gate::allows('update', $sugador),
        'mlbs'               => $mlbs,
        'mode_mover_sgi'     => $modeMoverSgi,
        'ml_scope_has_write' => $mlScopeHasWrite,
    ]);
    ```

    NÃO tocar nos outros métodos do controller (move, bulkMove, mlbs, etc).
  </action>
  <verify>
    <automated>cd c:/xampp/htdocs/ecf_admin/ecf_admin &amp;&amp; grep -c "mode_mover_sgi\|ml_scope_has_write" app/Http/Controllers/SugadorController.php</automated>
    Esperado: >= 4 (2 declarações de variável + 2 chaves no array Inertia + uso em comparação).
    Adicional:
    - `php artisan test --filter=Sugador 2>&1 | tail -5` continua verde (zero regressão).
  </verify>
  <done>2 props novas no payload Inertia da action show; isolamento garantido (nenhum outro método tocado); test suite continua verde.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Criar componente UndoToast.jsx (novo)</name>
  <files>resources/js/Components/UndoToast.jsx</files>
  <read_first>
    - resources/js/Layouts/AppLayout.jsx (linhas 550-564 — pattern do toast custom existente, copiar visual)
    - .planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-UI-SPEC.md (§4 spec completa do toast)
    - resources/js/lib/utils.js (helper cn)
  </read_first>
  <behavior>
    Componente standalone, ~80-100 linhas. Props conforme `<interfaces>`.

    Layout pixel-spec da UI-SPEC §4:
    - Container: `fixed bottom-5 right-5 z-50 card-ecf rounded-xl px-4 py-3 shadow-2xl border border-emerald-500/30 bg-emerald-950/90 backdrop-blur-md flex items-center gap-3`
    - Ícone esquerdo: `<CheckCircle2 size={14} className="text-emerald-400 shrink-0" />`
    - Texto mensagem: `<span className="text-emerald-200 text-sm font-semibold">{message}</span>`
    - Botão Desfazer: `<button className="text-ecf-yellow font-bold underline-offset-2 hover:underline">Desfazer ({secondsLeft}s)</button>`
    - Botão fechar: `<button className="opacity-60 hover:opacity-100"><X size={14} /></button>`

    Comportamento:
    - useEffect inicia `setInterval(() => secondsLeft--, 1000)` ao montar
    - Quando secondsLeft === 0, chama `onDismiss()` automaticamente
    - Cleanup do interval no unmount (clearInterval)
    - Click no botão Desfazer: chama onUndo() PRIMEIRO + onDismiss() depois (toast some imediatamente)
    - Click no X: chama onDismiss() (sem onUndo)
    - ESC global: useEffect listen `keydown`, se key === 'Escape' → onDismiss()
    - aria-live="polite", role="status"
    - aria-label no botão Desfazer: `Desfazer move (auto-dismiss em ${secondsLeft} segundos)`

    Props (com defaults):
    ```jsx
    export default function UndoToast({
      message,
      durationMs = 10000,
      onUndo,
      onDismiss,
    }) { ... }
    ```

    Comentário pt-BR no topo: "Componente novo da Phase 44 — toast bottom-right com botão Desfazer (10s) seguindo padrão Gmail/Material. Sem dependência de Radix Toast (provider novo seria over-engineering pro 1 use-case). Estado contador em useState local; cleanup garantido."
  </behavior>
  <action>
    Criar `resources/js/Components/UndoToast.jsx` com:
    - imports: `import { useEffect, useState } from 'react'`, `import { CheckCircle2, X } from 'lucide-react'`, `import { cn } from '@/lib/utils'`
    - default export function component conforme `<behavior>`
    - Comentários pt-BR explicando cada bloco
    - 4-space indentation, single quotes, arrow functions em callbacks (conforme convenção CLAUDE.md)

    Não usar Radix Toast nem outras libs. Não tocar em outros arquivos.
  </action>
  <verify>
    <automated>cd c:/xampp/htdocs/ecf_admin/ecf_admin &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
    Esperado: "✓ built in ..." sem errors. Vite resolve `@/Components/UndoToast` quando importado pelo Show.jsx (Tarefa 4).
    Adicional:
    - `grep -c "UndoToast" resources/js/Components/UndoToast.jsx` >= 1
    - `grep -c "setInterval\|clearInterval" resources/js/Components/UndoToast.jsx` >= 2 (cleanup garantido)
    - `grep -c "Escape" resources/js/Components/UndoToast.jsx` >= 1 (ESC handler)
    - Visual conferido na Tarefa 5 checkpoint humano
  </verify>
  <done>Arquivo novo criado, ~80-100 linhas, build verde. Sem mudança em outros arquivos. Acessibilidade básica (role, aria-live, aria-label, ESC).</done>
</task>

<task type="auto">
  <name>Tarefa 3: Evoluir MoveToSgiModal.jsx (prop mode + criar nova SGI + banner ativa + botão destrutivo + erro mapeado)</name>
  <files>resources/js/Components/MoveToSgiModal.jsx</files>
  <read_first>
    - resources/js/Components/MoveToSgiModal.jsx (todo — mantemos a estrutura, expandimos)
    - .planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-UI-SPEC.md (§3 + §6 — UX e copy completos)
    - .planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-02-SUMMARY.md (response shape do backend pra extrair sgi_id, campaign_id_original)
  </read_first>
  <behavior>
    EXPANDIR o componente existente (NÃO substituir). Mudanças pontuais:

    1. **Assinatura props expandida com defaults preservando back-compat:**
       ```jsx
       export default function MoveToSgiModal({
           company,
           sugadorIds,
           countLabel,
           mode = 'audit_only',           // NOVO — default preserva Index bulk
           sugadorAdgroupName = null,     // NOVO — para double-confirm (api_call only)
           originalCampaignId = null,     // NOVO — capturado pro undo (api_call only)
           onClose,
           onSuccess,
       }) { ... }
       ```

    2. **Estado novo:**
       - `const [creatingNewSgi, setCreatingNewSgi] = useState(false);` // toggle do input nome SGI
       - `const [newSgiName, setNewSgiName] = useState('');` // valor do input nome
       - `const [errorBanner, setErrorBanner] = useState(null);` // {code: HTTP, msg: string} para banner inline

    3. **Combobox `<select>`:** adicionar `<option value="__create__">+ Criar nova SGI...</option>` no FIM da lista (apenas quando mode='api_call'). Quando operador escolhe `__create__`: `setCreatingNewSgi(true)`, focar input nome (via useRef + useEffect), preencher defaultValue com `SGI ${new Date().toISOString().slice(0,7)}` (ex: "SGI 2026-06").

    4. **Input nome SGI (renderizado condicionalmente quando creatingNewSgi=true):**
       ```jsx
       <div>
         <label className="block text-white/60 text-[11px] font-semibold uppercase tracking-wider mb-1.5">
           Nome da nova SGI
         </label>
         <input
           ref={newSgiNameRef}
           type="text"
           value={newSgiName}
           onChange={e => setNewSgiName(e.target.value)}
           maxLength={60}
           required
           className="w-full h-9 px-3 rounded-lg border border-white/[0.08] bg-white/[0.03] text-[13px] text-white/80 focus:outline-none focus:border-ecf-yellow/40"
         />
         <p className="text-white/40 text-[11px] mt-1 leading-relaxed">Será criada pausada. Você pode editar o nome.</p>
       </div>
       ```

    5. **Banner "SGI ativa" (renderizado quando mode='api_call' E selected?.status === 'active' E !creatingNewSgi):**
       Layout da UI-SPEC §3 — `border-amber-500/30 bg-amber-500/[0.08] text-amber-200 + AlertTriangle icon + texto literal: "Esta SGI está ATIVA — o adgroup vai continuar gastando após o move. Pause manualmente no painel do Mercado Livre depois."`

    6. **Double-confirm banner (renderizado quando mode='api_call' E (selected OR creatingNewSgi) E sugadorAdgroupName):**
       ```jsx
       <div className="mb-4 p-3 rounded-lg border border-white/[0.08] bg-white/[0.02] text-[13px] text-white/80">
         <p className="mb-1">Você vai mover:</p>
         <p>Adgroup: <b className="text-ecf-yellow">{sugadorAdgroupName}</b></p>
         <p>Para SGI: <b className="text-ecf-yellow">{creatingNewSgi ? newSgiName : selected?.name}</b></p>
       </div>
       ```

    7. **Parágrafo introdutório:**
       - mode='audit_only': mantém o texto atual ("esta tela não chama a API do ML...")
       - mode='api_call': REMOVER o texto atual E SUBSTITUIR por: "Esta ação chama a API do Mercado Livre e move o adgroup para a campanha selecionada. O move é registrado no histórico do sugador."

    8. **Botão de submit:** quando mode='api_call', cor muda pra destrutivo: `bg-red-600 hover:bg-red-500 text-white` (substituindo `bg-ecf-yellow text-[#252525]`). Label muda:
       - audit_only: "Confirmar move" (existente)
       - api_call + SGI existente: "Confirmar mover"
       - api_call + creatingNewSgi: "Criar SGI e mover"
       - durante request: `<Loader2 + "Movendo..."`

    9. **Submit handler — split em dois caminhos:**

       ```jsx
       function submit(e) {
           e.preventDefault();
           setSubmitting(true);
           setErrorBanner(null);

           if (mode === 'audit_only') {
               // Caminho LEGACY existente — mantém intacto
               // ... router.post pra sugadores.move ou sugadores.bulk-move ...
               return;
           }

           // mode === 'api_call'
           const isCreatingNew = creatingNewSgi;
           const url = isCreatingNew
               ? route('sugadores.criar-sgi-e-mover', sugadorIds[0])
               : route('sugadores.mover-sgi', sugadorIds[0]);
           const payload = isCreatingNew
               ? { nome: newSgiName }
               : { sgi_id: parseInt(selected.id, 10), sgi_nome: selected.name };

           router.post(url, payload, {
               preserveScroll: true,
               onSuccess: (page) => {
                   // Backend retorna props/flash; UndoToast precisa do response JSON
                   // — usar router.post com onSuccess que recebe page; backend deve
                   // estar em prop `flash.move_result` (Inertia shared) ou shape similar.
                   // Alternativa: usar fetch direto pra obter JSON limpo:
                   // const res = await fetch(...); const body = await res.json();
                   // Decisão: usar FETCH direto pra ter response JSON limpo e passar pro
                   // onSuccess callback com {sgi_id, sgi_nome, campaign_id_original}.
                   // O Plan 44-02 retorna response()->json([...]) — Inertia não captura JSON puro.
                   // → REIMPLEMENTAR via fetch:
               },
               onError: (errors) => {
                   // Plan 44-02 retorna 422 com {message}; mapear por status
                   setErrorBanner({ code: errors?.status || 'unknown', msg: errors?.message || 'Erro desconhecido' });
               },
               onFinish: () => setSubmitting(false),
           });

           // REVISÃO: como o backend devolve response()->json, usar fetch nativo:
           // (substituir router.post acima por fetch wrapper)
       }
       ```

       **Decisão de implementação:** o backend do Plan 44-02 retorna `response()->json(...)` (não `back()->with(...)`). Para o frontend receber o JSON limpo com `sgi_id` e `campaign_id_original`, usar `fetch` direto em vez de `router.post`. Padrão (igual o `MoveToSgiModal` existente já usa pra `sgi-campaigns`):

       ```jsx
       try {
           const res = await fetch(url, {
               method: 'POST',
               headers: {
                   'Content-Type': 'application/json',
                   'Accept': 'application/json',
                   'X-CSRF-TOKEN': document.querySelector('meta[name=csrf-token]')?.content
                       || (typeof window !== 'undefined' && window.csrf_token) || '',
                   'X-Requested-With': 'XMLHttpRequest',
               },
               credentials: 'same-origin',
               body: JSON.stringify(payload),
           });
           const body = await res.json().catch(() => null);
           if (!res.ok) {
               setErrorBanner(mapHttpError(res.status, body));
               setSubmitting(false);
               return;
           }
           // Sucesso → callback pra Show.jsx montar UndoToast com originalCampaignId
           onSuccess?.({
               sgi_id: body.sgi_id,
               sgi_nome: body.sgi_nome,
               campaign_id_original: body.campaign_id_original,
               success: body.success,
               failed: body.failed,
               total_mlbs: body.total_mlbs,
           });
           onClose?.();
       } catch (e) {
           setErrorBanner({ code: 'network', msg: 'Falha de conexão. Verifique sua internet e tente novamente.' });
           setSubmitting(false);
       }
       ```

    10. **Helper `mapHttpError(status, body)`:**
        ```jsx
        function mapHttpError(status, body) {
            const fallback = body?.message || 'Erro desconhecido';
            switch (status) {
                case 401: return { code: 401, msg: 'Sessão com o Mercado Livre expirou. Reabra o modal e tente novamente.' };
                case 403: return { code: 403, msg: 'O token não tem permissão de escrita no Mercado Ads. Reconecte a empresa com permissão expandida.' };
                case 404: return { code: 404, msg: 'Adgroup não existe mais no Mercado Livre.' };
                case 422: return { code: 422, msg: fallback };
                case 502:
                case 503:
                case 504: return { code: status, msg: 'A API do Mercado Livre está instável agora. Tente novamente em alguns minutos.' };
                default:
                    if (status >= 500) return { code: status, msg: 'A API do Mercado Livre está instável agora.' };
                    return { code: status, msg: fallback };
            }
        }
        ```

    11. **Banner erro inline (renderizado quando errorBanner !== null):**
        Reusar o estilo existente `border-red-500/20 bg-red-500/[0.05] + AlertTriangle icon + text-red-300 text-xs`, exibindo `errorBanner.msg`. Botão de submit volta a estar enabled pra retry; label: "Tentar novamente".

    12. **Disabled state do botão submit:**
        - mode='api_call' + !creatingNewSgi + !selected → disabled
        - mode='api_call' + creatingNewSgi + !newSgiName.trim() → disabled
        - submitting → disabled
  </behavior>
  <action>
    1. Editar `resources/js/Components/MoveToSgiModal.jsx`. NÃO substituir o arquivo — fazer mudanças pontuais conforme `<behavior>` pra preservar todo código legacy do mode='audit_only'.

    2. Adicionar imports faltantes: `useRef` de 'react'; (componente já tem `useEffect, useState`).

    3. Adicionar a função helper `mapHttpError` fora do componente exportado (escopo de módulo, igual o `copyToClipboard` em Show.jsx).

    4. Adicionar comentário pt-BR no topo: "Phase 44 — evoluído pra suportar mode='api_call' (chama API ML de verdade via POST mover-sgi/criar-sgi-e-mover) preservando mode='audit_only' legacy do Index bulk (back-compat)."

    5. `npm run build` deve passar verde.
  </action>
  <verify>
    <automated>cd c:/xampp/htdocs/ecf_admin/ecf_admin &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
    Esperado: "✓ built" sem errors.
    Adicional:
    - `grep -c "mode === 'api_call'\|mode==='api_call'" resources/js/Components/MoveToSgiModal.jsx` >= 3 (split em vários renderings)
    - `grep -c "creatingNewSgi" resources/js/Components/MoveToSgiModal.jsx` >= 4
    - `grep -c "mapHttpError\|errorBanner" resources/js/Components/MoveToSgiModal.jsx` >= 5
    - `grep -c "criar-sgi-e-mover\|mover-sgi" resources/js/Components/MoveToSgiModal.jsx` >= 2 (as 2 rotas)
    - `grep -c "router.post.*sugadores.move\b\|router.post.*sugadores.bulk-move" resources/js/Components/MoveToSgiModal.jsx` >= 2 (mode legacy preservado)
  </verify>
  <done>Modal evoluído, mode='audit_only' continua funcional (back-compat), mode='api_call' chama endpoints novos via fetch, banner de erro mapeado por HTTP, build verde.</done>
</task>

<task type="auto">
  <name>Tarefa 4: Atualizar Show.jsx — passar mode + capturar originalCampaignId + montar UndoToast</name>
  <files>resources/js/Pages/Sugadores/Show.jsx</files>
  <read_first>
    - resources/js/Pages/Sugadores/Show.jsx (linhas 1-300 — entender hook useState existente + props recebidas + handler do botão Mover SGI linhas 261-270)
    - resources/js/Pages/Sugadores/Show.jsx (linhas 559-566 — montagem do MoveToSgiModal hoje)
    - resources/js/Components/UndoToast.jsx (criado na Tarefa 2)
    - resources/js/Components/MoveToSgiModal.jsx (modificado na Tarefa 3)
    - .planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-UI-SPEC.md (§4 — estado em memória do undo)
  </read_first>
  <behavior>
    - Hook `useState` novo: `const [undoState, setUndoState] = useState(null)` ao lado do `showMoveModal`
    - Page agora desestrutura novos props: `const { sugador, url_anuncio, url_ads, can_update, mlbs, mode_mover_sgi = 'audit_only', ml_scope_has_write = false } = ...`
    - Import: `import UndoToast from '@/Components/UndoToast';`
    - Modal montagem: passar `mode={mode_mover_sgi}`, `sugadorAdgroupName={sugador.adgroup_name}`, `originalCampaignId={sugador.campaign_id ? parseInt(sugador.campaign_id, 10) : null}`, e onSuccess EXPANDIDO:
      ```jsx
      onSuccess={(result) => {
          // result = { sgi_id, sgi_nome, campaign_id_original, success, failed, total_mlbs }
          if (result && mode_mover_sgi === 'api_call') {
              setUndoState({
                  sugadorId: sugador.id,
                  sgiNome: result.sgi_nome,
                  originalCampaignId: result.campaign_id_original,
                  message: result.failed > 0
                      ? `Adgroup movido — ${result.success} de ${result.total_mlbs} MLBs. ${result.failed} falharam.`
                      : `Adgroup movido para ${result.sgi_nome}.`,
              });
              // Atraso pra Inertia refrescar o status do sugador
              router.reload({ only: ['sugador'] });
          }
      }}
      ```
    - Após o `</AppLayout>` (ou logo dentro dele depois do conteúdo principal), renderizar:
      ```jsx
      {undoState && (
          <UndoToast
              message={undoState.message}
              durationMs={10000}
              onUndo={async () => {
                  try {
                      const res = await fetch(route('sugadores.desfazer-move', undoState.sugadorId), {
                          method: 'POST',
                          headers: {
                              'Content-Type': 'application/json',
                              'Accept': 'application/json',
                              'X-CSRF-TOKEN': document.querySelector('meta[name=csrf-token]')?.content || '',
                              'X-Requested-With': 'XMLHttpRequest',
                          },
                          credentials: 'same-origin',
                          body: JSON.stringify({}),
                      });
                      setUndoState(null);
                      if (res.ok) {
                          router.reload({ only: ['sugador'] });
                          // TODO Plan 44-04 ou follow-up: novo toast efêmero "Move desfeito" 3s
                      }
                  } catch {
                      setUndoState(null);
                  }
              }}
              onDismiss={() => setUndoState(null)}
          />
      )}
      ```
    - Não tocar em outras partes do Show.jsx (drilldown MLBs, métricas, etc).
  </behavior>
  <action>
    1. Adicionar import `UndoToast` no topo.
    2. Desestruturar `mode_mover_sgi` e `ml_scope_has_write` das props (props arrive via `usePage().props` ou via destruturação da função Page). Confirmar padrão atual da página antes de editar — Show.jsx usa `function Page({ sugador, url_anuncio, ... }) {}` (props como argumentos), então expandir a lista de argumentos.
    3. Adicionar `useState(null)` pro `undoState` próximo ao `showMoveModal` existente.
    4. Atualizar montagem do `<MoveToSgiModal>` (linhas ~559-566) com novas props + onSuccess expandido.
    5. Renderizar `<UndoToast>` condicionalmente no fim do return (após o `</AppLayout>` ou imediatamente antes, conforme estrutura atual).
    6. Importar `router` de '@inertiajs/react' se ainda não importado.
    7. `npm run build` → verde.
  </action>
  <verify>
    <automated>cd c:/xampp/htdocs/ecf_admin/ecf_admin &amp;&amp; npm run build 2>&amp;1 | tail -10</automated>
    Esperado: "✓ built" sem errors.
    Adicional:
    - `grep -c "UndoToast" resources/js/Pages/Sugadores/Show.jsx` >= 2 (import + JSX)
    - `grep -c "mode_mover_sgi\|undoState\|setUndoState" resources/js/Pages/Sugadores/Show.jsx` >= 5
    - `grep -c "sugadores.desfazer-move" resources/js/Pages/Sugadores/Show.jsx` >= 1
  </verify>
  <done>Show.jsx orquestra modal + toast; props consumidas; build verde; comportamento preservado pra mode='audit_only' (não houve UndoToast antes — só ativa quando api_call).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 5: Verificação visual + funcional fim-a-fim (Bymobille #298)</name>
  <what-built>
    Tarefas 1-4 entregaram: backend já em prod (Plan 44-02), UI evoluída (modal + toast), Show.jsx orquestrando. Todos os tests automatizados passaram. Falta confirmação visual humana de 6 cenários no painel real com a Bymobille (#298, mesma empresa do smoke do Plan 44-01).

    Pré-requisito: feature flag ON em ambiente de teste. Setar `SUGADORES_MOVER_SGI=true` no `.env` local (XAMPP), rodar `php artisan config:clear`, rodar `php artisan serve` + `npm run dev`.
  </what-built>
  <how-to-verify>
    1. **Login + navegação:** login como admin no painel local; navegar até `/sugadores` filtrando empresa=Bymobille (#298); abrir 1 sugador-adgroup pendente (`/sugadores/{id}`). Confirmar:
       - Botão amarelo "Mover para SGI" aparece no header (ao lado de "Ver anúncio no ML" e "Painel de Ads")
       - Status do sugador = 'pendente' (não 'movido'/'resolvido')

    2. **Modal — SGI existente, sucesso total:**
       - Clicar "Mover para SGI" → modal abre
       - Confirmar título "Mover para campanha SGI" + nome da empresa Bymobille
       - Confirmar parágrafo introdutório NOVO: "Esta ação chama a API do Mercado Livre..." (NÃO o texto legacy "esta tela não chama a API do ML")
       - Combobox lista SGIs existentes da Bymobille (deve ter pelo menos a SGI residual do smoke do Plan 44-01)
       - Selecionar uma SGI existente; verificar:
         - Banner amarelo "SGI ATIVA" aparece SE a SGI escolhida está active (skipar se nenhuma da Bymobille está active — passar pra Cenário 3)
         - Banner double-confirm aparece com nomes literais em amarelo: "Adgroup: X / Para SGI: Y"
         - Botão de submit é VERMELHO (bg-red-600), label "Confirmar mover"
       - Clicar "Confirmar mover" → modal fecha → UndoToast aparece bottom-right verde com mensagem "Adgroup movido para {SGI}." + botão "Desfazer (10s)" decrescendo
       - Aguardar 10s — toast some sozinho
       - Confirmar status do sugador na página = "Movido p/ SGI" (badge amarelo); card "Marcado como movido para X" aparece
       - **Validação no painel ML do cliente Bymobille:** abrir o anúncio no painel ML e confirmar que ele está agora na SGI escolhida (não na campanha original)

    3. **Modal — Criar nova SGI:**
       - Voltar pra outro sugador-adgroup pendente
       - Abrir modal → escolher último item do combobox "+ Criar nova SGI..."
       - Confirmar input nome SGI aparece, preenchido com "SGI 2026-06" (mês corrente)
       - Editar nome pra "SGI Teste Plan 44-03"
       - Verificar botão de submit label muda pra "Criar SGI e mover" (ainda vermelho)
       - Confirmar — modal fecha, UndoToast aparece
       - **Validação ML:** abrir painel ML, confirmar nova campanha SGI Teste Plan 44-03 criada PAUSADA + ad foi movido pra lá

    4. **Undo dentro de 10s:**
       - Voltar pra outro sugador pendente, mover pra SGI existente
       - DENTRO de 10s clicar "Desfazer" no toast
       - Toast some imediatamente
       - Confirmar status do sugador volta pra 'pendente' (não fica em 'movido')
       - **Validação ML:** confirmar ad voltou pra campanha original

    5. **Erro 403 inline:**
       - Pra simular: desabilitar temporariamente write no scope do mlToken da Bymobille via Tinker:
         `App\Models\MlToken::where('company_id', 298)->update(['scope' => 'read offline_access'])`
       - Recarregar `/sugadores/{id}` — confirmar que botão "Mover para SGI" AINDA aparece (porque Tarefa 1 deve manter o botão visível só se mode_mover_sgi='api_call', que checa scope. Botão NÃO deve aparecer se scope perdeu 'write' — esse é o ponto do Plan 44-04 banner, mas aqui já bloqueamos no controller via `mode_mover_sgi='audit_only'`)
       - Esperado: botão "Mover para SGI" NÃO aparece OU modal ao submeter retorna 422 com mensagem "Esta empresa não tem permissão de escrita no Mercado Ads"
       - Restaurar scope: `App\Models\MlToken::where('company_id', 298)->update(['scope' => 'read write offline_access'])` + reconnect se necessário

    6. **Bulk preservado (Index audit-only):**
       - Navegar `/sugadores` (Index, lista paginada)
       - Selecionar 2-3 sugadores via checkbox (se UI tem bulk; senão pular)
       - Abrir modal bulk — confirmar que NÃO mostra "Criar nova SGI", NÃO mostra banner double-confirm em vermelho, botão de submit AINDA é amarelo (mode='audit_only' default). Comportamento idêntico ao Plan 19/30 original.
       - Submeter → continua sendo audit-only (não chama API ML)

    7. **Build limpo:** `npm run build` no terminal — sem warnings/errors novos vs commit anterior.

    8. **Sem regressão tests:** `php artisan test --filter='Phase44|Sugador'` continua todos verdes.
  </how-to-verify>
  <resume-signal>
    Operador responde com EXATAMENTE um dos formatos:

    - `approved 8/8` → tudo OK; Plan 44-03 fecha
    - `partial failed=N reason="..."` → cenários N falharam; planner deve criar follow-up Plan 44-03.1 OU abrir quick task pra fix targeted
    - `regression visual=... behavior=...` → mode='audit_only' bulk quebrou OU Show.jsx desformatou; rollback parcial da Tarefa 3/4
    - `blocked dependency-missing=...` → Plan 44-01 fixture sumiu OU Plan 44-02 endpoint não responde como esperado; voltar pra plan anterior
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Browser → POST /sugadores/{id}/mover-sgi (autenticado, CSRF) | Confiança no token CSRF da meta tag; backend Plan 44-02 já valida via web stack |
| UndoToast (memória JS) → fetch sugadores.desfazer-move | `originalCampaignId` mantido APENAS em memória client; recarregar página perde — aceito (UI-SPEC §4) |
| Show.jsx (props Inertia) → mode_mover_sgi | Backend Plan 44-03 Tarefa 1 controla; client confiável (mas defensivo: defaults sane) |
| MoveToSgiModal (modo api_call) → POST fetch direto | Bypassa Inertia wrapper pra obter JSON limpo; CSRF token via meta tag conforme padrão MoveToSgiModal existente |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-44-03-01 | Tampering | UndoToast.onUndo com originalCampaignId adulterado via DevTools | accept | Operador é admin; threat de "admin malicioso forja undo pra mover ad pra campaign_id arbitrário" — backend Plan 44-02 desfazerMove valida que SugadorAcao mais recente do sugador tem `acao='moveu_para_sgi'` E lê `campaign_id_original` das properties (não confia no client). Se client mandar payload arbitrário, é ignorado (desfazerMove é body-less por design). |
| T-44-03-02 | Information Disclosure | errorBanner expõe body['message'] do backend | mitigate | Plan 44-02 controla o `message` retornado em 422 — mensagens são pt-BR humanas, não vazam dados sensíveis (`access_token`, etc — anti-leak Plan 44-02 §T-44-02-01). |
| T-44-03-03 | DoS | Operador clica Mover 50x rapidamente | mitigate | `submitting` state desabilita botão durante request; rate limit por seller no backend (60/min) absorve abuso real. |
| T-44-03-04 | Repudiation | Operador desfaz move e nega ter feito | mitigate | Plan 44-02 desfazerMove cria SugadorAcao com `undo:true` na observação JSON — auditável via /activity-log. |
| T-44-03-05 | Information Disclosure | CSRF token leak via console.log debugging | accept | Sem `console.log` no código entregue; revisão visual da Tarefa 5 cobre. |
| T-44-03-SC | Tampering | npm installs | mitigate | ZERO new npm dependencies — todos os imports vêm de packages já no `package.json` (react, @inertiajs/react, lucide-react, @/lib/utils). Confirmado: `git diff package.json` deve mostrar zero alterações. |
</threat_model>

<verification>
1. `php artisan test --filter='Phase44|Sugador'` continua verde.
2. `npm run build` verde, sem warnings novos.
3. `git diff package.json package-lock.json` mostra zero alterações.
4. `grep -c "mode === 'api_call'" resources/js/Components/MoveToSgiModal.jsx` >= 3
5. `grep -c "UndoToast" resources/js/Pages/Sugadores/Show.jsx` >= 2
6. `grep -c "mode_mover_sgi" app/Http/Controllers/SugadorController.php` >= 4 (controller + interfaces no docblock se houver)
7. Checkpoint humano Tarefa 5 com `approved 8/8`.
</verification>

<success_criteria>
- 4 arquivos modificados (1 novo + 3 evoluídos)
- 0 novas deps npm
- `npm run build` verde
- Suite Sugador continua verde
- 8/8 cenários visuais da Tarefa 5 aprovados pelo operador
- Bulk Index continua funcionando audit-only (back-compat)
</success_criteria>

<output>
Criar `.planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-03-SUMMARY.md` ao final, contendo:
- Print/screenshot do modal mode='api_call' aberto na Bymobille
- Print/screenshot do UndoToast com contador visível
- Confirmação do scope contains 'write' (output `MlToken::where('company_id',298)->value('scope')`)
- Lista de SGIs detectadas na Bymobille pós-smoke do Plan 44-01
- Edge cases observados durante verificação visual que Plan 44-04 deve cobrir (ex: empresa Y sem token write — banner amarelo deve aparecer)
</output>
