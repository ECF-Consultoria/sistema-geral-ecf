---
phase: 44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml
plan: 04
type: execute
wave: 4
depends_on:
  - 44-03
files_modified:
  - resources/js/Pages/Sugadores/Show.jsx
  - .planning/STATE.md
  - tests/Feature/Phase44/ReauthBannerTest.php
autonomous: true
requirements:
  - REQ-44-10
  - REQ-44-11

must_haves:
  truths:
    - "Quando operador admin abre /sugadores/{id} e `ml_scope_has_write === false` E `sugador.tipo === 'adgroup'`, banner amarelo persistente aparece NO TOPO da página com mensagem: 'Esta empresa precisa reconectar com permissão de escrita no Mercado Ads para usar a ação Mover para SGI.' + botão 'Reconectar'"
    - "Click no botão 'Reconectar' do banner navega para rota OAuth ML existente (`/sistema/ml-oauth/conectar/{company.id}` ou equivalente Phase 20) — não há nova rota criada nesta plan"
    - "Banner NÃO aparece quando ml_scope_has_write === true (caso normal) OU quando sugador.tipo === 'campanha' (move SGI só faz sentido em adgroup)"
    - "STATE.md ganha bloco 'Operação Phase 44 — Re-auth ML com scope write' documentando passo-a-passo manual: (a) verificar permissão 'Advertising' na app DevCenter ML, (b) reconectar empresa em /sistema/ml-oauth/{company_id}, (c) confirmar via Tinker que MlToken.scope contém 'write'. Esse bloco é a referência operacional pro time quando uma empresa nova precisa do scope expandido"
    - "Test Feature valida: (a) banner não aparece quando scope tem write, (b) banner aparece quando scope só tem read, (c) banner não aparece em sugador tipo=campanha mesmo sem write"
  artifacts:
    - path: "resources/js/Pages/Sugadores/Show.jsx"
      provides: "Banner amarelo persistente quando empresa sem scope write"
      contains: "Reconectar"
    - path: ".planning/STATE.md"
      provides: "Bloco operacional 'Phase 44 — Re-auth ML scope write' documentando passos manuais"
      contains: "## Operação Phase 44"
    - path: "tests/Feature/Phase44/ReauthBannerTest.php"
      provides: "3 testes Inertia::props pra validar exposição correta de ml_scope_has_write em diferentes cenários"
      contains: "ReauthBannerTest"
  key_links:
    - from: "Show.jsx banner"
      to: "Rota OAuth ML existente Phase 20"
      via: "<a href={route('ml-oauth.connect', company.id)}> OU URL direta"
      pattern: "ml-oauth|/sistema/ml"
---

<objective>
Fechar a UX do roll-out da Phase 44 cobrindo a empresa que AINDA NÃO RECONECTOU pós-mudança de scope. Plan 44-01 atualizou `MercadoLivreService.php:53` para `'read write offline_access'` e o operador reconectou Bymobille manualmente. Mas as ~N outras empresas em prod com mlToken ATIVO ainda têm scope antigo `'read offline_access'` — todas elas vão receber 403 em qualquer PUT/POST de Mover SGI.

Sem este plan, operador abre `/sugadores/{id}` dessas empresas, NÃO VÊ o botão "Mover para SGI" (controle do Plan 44-03 Tarefa 1: mode_mover_sgi='audit_only' quando scope sem write), e fica sem entender por quê. Banner amarelo dá a explicação + 1 click pra ação corretiva (reconectar).

Decisão importante: NÃO criar rota OAuth nova. A Phase 20 já entregou `MercadoLivreService::buildAuthUrl` + callback. Banner aponta pra rota existente (verificar URL exata no codebase antes de codar — a rota nominal é `ml-oauth.connect` ou `ml.connect` conforme Phase 20).

Tarefa documental adicional: bloco no STATE.md servindo como runbook operacional — passo-a-passo pro time admin quando precisar destravar nova empresa.

Purpose: tornar a feature usável em larga escala sem suporte 1-a-1 do dev. Operador admin vê o banner, clica reconectar, faz consent, banner some, botão "Mover SGI" aparece.
Output: 1 componente JSX inline novo no Show.jsx (~30 linhas), 1 bloco em STATE.md, 3 tests Feature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-CONTEXT.md
@.planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-RESEARCH.md
@.planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-UI-SPEC.md

# Plan 44-03 já passou ml_scope_has_write como prop — esta plan SÓ exibe banner
@.planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-03-SUMMARY.md

# Página onde banner vai
@resources/js/Pages/Sugadores/Show.jsx

# Controller que já tem a prop (Plan 44-03 Tarefa 1)
@app/Http/Controllers/SugadorController.php

# Rota OAuth ML existente (descobrir nome exato — Phase 20)
@routes/web.php

<interfaces>
<!-- Prop já existente vinda do Plan 44-03 Tarefa 1: -->
{
  ml_scope_has_write: false,  // true quando MlToken.scope contém 'write'
  sugador: { tipo: 'adgroup', company: {...} },
  ...
}

<!-- Rota OAuth Phase 20: descobrir via grep -->
<!-- Provavel: route('ml-oauth.connect', companyId) OU route('ml.oauth.connect', companyId) -->
<!-- A descobrir no codebase durante a Tarefa 1 — grep -rn 'ml-oauth\|ml.oauth\|buildAuthUrl' routes/web.php -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Banner amarelo de re-auth no Show.jsx (componente inline + tests Feature)</name>
  <files>resources/js/Pages/Sugadores/Show.jsx, tests/Feature/Phase44/ReauthBannerTest.php</files>
  <read_first>
    - resources/js/Pages/Sugadores/Show.jsx (entender estrutura de header — banner vai LOGO ABAIXO do <Link "Voltar"> que abre a página, ANTES da seção do header do sugador)
    - app/Http/Controllers/SugadorController.php (método show — Plan 44-03 Tarefa 1 já passa ml_scope_has_write)
    - routes/web.php (descobrir nome exato da rota OAuth ML Phase 20: `grep -rn 'ml.oauth\|ml-oauth\|MercadoLivreOAuthController' routes/web.php`)
    - .planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-UI-SPEC.md (tokens visuais — banner amarelo segue padrão `border-amber-500/30 bg-amber-500/[0.08] text-amber-200`)
  </read_first>
  <behavior>
    Componente JSX inline definido no escopo do módulo Show.jsx (não exportado — uso local), ~25-30 linhas. Render condicional:

    ```jsx
    function ReauthBanner({ companyId, companyName }) {
        // URL OAuth: usar route() helper com o nome descoberto na Tarefa 1 leitura.
        // Fallback hardcoded `/sistema/ml-oauth/conectar/{id}` se route() não tem entry
        // (decidir baseado no grep do codebase).
        const reauthUrl = route('ml-oauth.connect', companyId);  // ajustar ao nome real
        return (
            <div
                role="alert"
                className="card-ecf rounded-xl p-4 mb-4 border-amber-500/30 bg-amber-500/[0.08]"
            >
                <div className="flex items-start gap-3">
                    <AlertTriangle size={16} className="text-amber-400 shrink-0 mt-0.5" />
                    <div className="flex-1 min-w-0">
                        <p className="text-amber-200 text-sm leading-relaxed">
                            <b>Reconexão necessária:</b> esta empresa ({companyName})
                            precisa reconectar com permissão de escrita no Mercado Ads
                            para usar a ação <b>Mover para SGI</b>.
                        </p>
                        <p className="text-amber-200/60 text-[11px] mt-1 leading-relaxed">
                            O token atual só tem permissão de leitura. Sem reconexão,
                            qualquer tentativa de move retorna 403.
                        </p>
                    </div>
                    <a
                        href={reauthUrl}
                        className="shrink-0 inline-flex items-center gap-2 h-9 px-4 rounded-lg bg-amber-500 text-amber-950 hover:bg-amber-400 text-[13px] font-bold"
                    >
                        Reconectar
                    </a>
                </div>
            </div>
        );
    }
    ```

    Renderização condicional no return principal do componente Page:
    ```jsx
    // Logo após o <Link 'Voltar'> existente, ANTES do header do sugador
    {!ml_scope_has_write && sugador.tipo === 'adgroup' && sugador.company && (
        <ReauthBanner companyId={sugador.company.id} companyName={sugador.company.name} />
    )}
    ```

    Tests Feature (`tests/Feature/Phase44/ReauthBannerTest.php`) — 3 cenários cobrindo a PROP que governa renderização:

    - Test 1 (scope contém write → banner não aparece): semeia Company+MlToken com scope='read write offline_access'+status=active. GET `/sugadores/{id}`. Assertion: prop `ml_scope_has_write === true` (banner não renderizaria).
    - Test 2 (scope sem write → banner aparece): scope='read offline_access'. Prop `ml_scope_has_write === false`. Banner DEVE renderizar pra tipo=adgroup.
    - Test 3 (tipo=campanha, scope sem write → banner NÃO aparece): semeia sugador tipo='campanha'. Prop `ml_scope_has_write === false` ainda, MAS lógica do banner também checa `sugador.tipo === 'adgroup'` → banner suprimido. Test confirma via `Inertia::test()` assertion de props.

    Padrão de teste: usar `Inertia::test()` helper se disponível (Inertia v2 expõe via package laravel-inertia) OU asserção tradicional `$response->viewData('page')['props']['ml_scope_has_write']`.
  </behavior>
  <action>
    1. Descobrir nome canônico da rota OAuth ML via grep no codebase:
       ```
       grep -rn "Route::get.*ml-oauth\|Route::get.*ml\\.oauth\|->name('ml" routes/web.php
       ```
       Documentar o achado em comentário pt-BR adjacente ao banner. Se nome for `ml-oauth.connect`, usar; se for outro, usar o real.

    2. Editar `resources/js/Pages/Sugadores/Show.jsx`:
       - Adicionar componente helper `ReauthBanner` no escopo do módulo (similar aos `Metric`, `StatusBadge` existentes locais ao arquivo)
       - Adicionar renderização condicional no return do Page component (depois do `Link 'Voltar'`, antes do header do sugador)
       - Comentário pt-BR no topo do bloco: "Phase 44 Plan 44-04 — banner re-auth para empresas com mlToken ativo MAS sem scope 'write' (legado pré-Phase 44-01). Não afeta empresas reconectadas pós Plan 44-01 nem empresas sem mlToken (essas caem em outros guards)."

    3. Escrever `tests/Feature/Phase44/ReauthBannerTest.php` com os 3 testes. Helper privado `seedSugadorComScopeAndTipo(string $scope, string $tipo): Sugador` (cria Company + MlToken + Sugador). Authenticate como admin antes de cada teste.

    4. `php artisan test --filter=Phase44/ReauthBannerTest` → 3/3 verde. `npm run build` → verde.
  </action>
  <verify>
    <automated>cd c:/xampp/htdocs/ecf_admin/ecf_admin &amp;&amp; php artisan test --filter=Phase44/ReauthBannerTest 2>&amp;1 | tail -10 &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
    Esperado: 3/3 tests verdes + npm build verde.
    Adicional:
    - `grep -c "ReauthBanner" resources/js/Pages/Sugadores/Show.jsx` >= 2 (declaração + JSX)
    - `grep -c "ml_scope_has_write" resources/js/Pages/Sugadores/Show.jsx` >= 2 (desestruturação prop + check render)
    - Suite Phase44 acumulada continua verde: `php artisan test --filter=Phase44` mostra 14 (Plan 44-02) + 3 (este) = 17+ verdes (Plan 44-01 tests também).
  </verify>
  <done>Banner renderiza condicionalmente; 3 tests Feature verdes; build verde; rota OAuth real verificada via grep e documentada em comentário.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Bloco operacional 'Phase 44 — Re-auth ML scope write' no STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (estrutura atual — adicionar bloco novo na seção "Accumulated Context" ou em "Decisões")
    - .planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-RESEARCH.md (§2 sobre permissão "Advertising" na app DevCenter ML)
  </read_first>
  <behavior>
    Adicionar bloco markdown completo no STATE.md servindo como runbook operacional pro time admin. Estrutura:

    ```markdown
    ## Operação Phase 44 — Re-auth ML com scope write

    **Quando rodar:** ao detectar empresa cujo `MlToken.scope` não contém `'write'`. Sintoma na UI: banner amarelo no `/sugadores/{id}` da empresa pedindo "Reconectar".

    **Pré-requisito único (vale pra todas empresas, faz só 1×):**
    Permissão funcional "Advertising — access, create and manage campaigns" ATIVADA na app ECF em `https://developers.mercadolivre.com.br`. Se já estiver ligada pula este passo.

    **Passo-a-passo por empresa:**

    1. Login no painel ECF como admin.
    2. Navegar para `/sistema/ml-oauth` (ou rota equivalente Phase 20).
    3. Localizar a empresa na lista.
    4. Clicar "Reconectar".
    5. Completar o consent do ML (popup) — operador deve ter credenciais da conta DONA da loja, não da ECF.
    6. Após callback de sucesso, validar via Tinker:
       ```
       php artisan tinker
       > App\Models\MlToken::where('company_id', {ID})->value('scope')
       # Esperado retornar string contendo 'write', ex: "read write offline_access"
       ```
    7. Voltar pra `/sugadores/{id}` de qualquer sugador-adgroup dessa empresa. Banner amarelo NÃO deve mais aparecer. Botão "Mover para SGI" amarelo no header deve aparecer.

    **Troubleshooting:**

    - 403 persistente após reconectar com scope write: verificar permissão "Advertising" na app DevCenter (passo único acima).
    - Operador não tem credenciais da conta ML: pedir pro contato comercial da empresa autorizar — não dá pra contornar.
    - MlToken.scope ainda mostra só "read offline_access" pós-reconnect: limpar cache do navegador, tentar de novo. Se persistir, problema na configuração do scope no `MercadoLivreService::buildAuthUrl` (verificar linha 53 contém literal `'read write offline_access'`).

    **Auditoria:**

    Para listar empresas em prod com scope SEM write (candidatas a precisar de re-auth):
    ```
    php artisan tinker
    > App\Models\MlToken::where('status', 'active')->get()->filter(fn($t) => !str_contains((string) $t->scope, 'write'))->pluck('company_id')
    ```
    ```
  </behavior>
  <action>
    Editar `.planning/STATE.md`. Localizar uma boa posição — sugestão: ao final da seção "Accumulated Context" (logo antes de "### Decisões herdadas do v1.0") ou logo após a entrada mais recente em "### Roadmap Evolution". Inserir o bloco acima sem alterar conteúdo existente. Comentários inline pt-BR no markdown.

    NÃO atualizar metadados de progress/percent — esta task só adiciona documentação operacional.
  </action>
  <verify>
    <automated>cd c:/xampp/htdocs/ecf_admin/ecf_admin &amp;&amp; grep -c "Operação Phase 44 — Re-auth ML" .planning/STATE.md</automated>
    Esperado: == 1 (bloco único inserido).
    Adicional:
    - `grep -c "buildAuthUrl\|Advertising — access" .planning/STATE.md` >= 2
    - Diff git mostra apenas adição de markdown, sem remoção/alteração de conteúdo existente
  </verify>
  <done>STATE.md ganhou bloco operacional; conteúdo existente intacto; runbook acessível pro time admin sem precisar abrir o RESEARCH.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Show.jsx prop `ml_scope_has_write` (do controller) | Controller Plan 44-03 Tarefa 1 já calcula a partir de MlToken.scope; trust no controller |
| Banner href OAuth ML | Aponta pra rota existente Phase 20; trust no roteamento Laravel + Sanctum/web stack |
| STATE.md (documentação) | Sem trust boundary — apenas markdown |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-44-04-01 | Spoofing | Banner com URL OAuth falsa | mitigate | `href` usa `route('ml-oauth.connect', companyId)` helper (não string concatenada) — Ziggy expõe APENAS rotas nomeadas conhecidas; impossível injetar URL arbitrária client-side. |
| T-44-04-02 | Information Disclosure | Banner expõe nome da empresa | accept | Operador já vê esse nome no header da página — banner não vaza nada novo. |
| T-44-04-03 | DoS | Operador clica Reconectar 50x | accept | Rota OAuth Phase 20 já tem proteção CSRF + rate limit Laravel default; bem-conhecido. |
| T-44-04-SC | Tampering | npm/composer | mitigate | ZERO new deps. Banner usa apenas Lucide `AlertTriangle` (já no package.json) + classes Tailwind padrão. STATE.md é só markdown. |
</threat_model>

<verification>
1. `php artisan test --filter=Phase44/ReauthBannerTest` → 3/3 verde
2. `php artisan test --filter=Phase44` acumulado (Plans 44-01 + 44-02 + 44-04) continua verde
3. `npm run build` → verde, sem warnings novos
4. `git diff package.json` → sem alterações
5. `grep "Operação Phase 44 — Re-auth" .planning/STATE.md` → 1 match
6. Manual: abrir `/sugadores/{id}` de uma empresa com mlToken active mas scope sem write → banner amarelo visível com botão "Reconectar"
7. Manual: abrir mesma página de empresa com scope write → SEM banner; botão amarelo "Mover para SGI" no header (Plan 44-03)
</verification>

<success_criteria>
- Banner condicional implementado + 3 tests Feature verdes
- STATE.md tem runbook operacional acessível
- Zero regressão Phase 44-01/02/03
- Zero deps novas
- Operador admin pode resolver "empresa sem scope write" em 5 minutos sem ajuda do dev
</success_criteria>

<output>
Criar `.planning/phases/44-mover-adgroup-sugador-para-sgi-ou-pausar-via-api-ml/44-04-SUMMARY.md` ao final, contendo:
- Lista de empresas em prod descobertas com scope sem write (output do comando Tinker do troubleshooting)
- Recomendação de roll-out por empresa: priorizar empresas-piloto (Bymobille já feito; sugerir próximas 3-5 baseadas em volume de sugadores-adgroup pendentes)
- Comprovação visual do banner (print de tela)
- Decisão se cria notification automática pro time quando MlToken.scope perde write (deferred follow-up — não bloqueia fechamento da Phase 44)
</output>
