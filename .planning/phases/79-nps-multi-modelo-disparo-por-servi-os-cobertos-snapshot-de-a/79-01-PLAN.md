---
phase: 79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_14_200001_create_nps_snapshot_tables.php
  - app/Models/NpsResponseScore.php
  - app/Models/NpsResponseCoveredService.php
  - app/Models/NpsScoreAssignment.php
  - tests/Feature/V16/SnapshotSchemaTest.php
autonomous: true
requirements: [DEC-79-C]
must_haves:
  truths:
    - "As 3 tabelas de snapshot existem e migram tanto no SQLite (testes) quanto no MySQL (prod)"
    - "nps_score_assignments referencia nps_response_scores por FK (nps_response_score_id)"
    - "Apagar uma nps_responses remove em cascata scores/covered/assignments"
  artifacts:
    - path: "database/migrations/2026_07_14_200001_create_nps_snapshot_tables.php"
      provides: "Schema das 3 tabelas de snapshot cross-driver"
      contains: "nps_response_scores"
    - path: "app/Models/NpsResponseScore.php"
      provides: "Model das médias por dimensão (contrato para o NpsSnapshotService da 79-04)"
    - path: "app/Models/NpsScoreAssignment.php"
      provides: "Model da atribuição congelada média×user×role×serviço"
  key_links:
    - from: "app/Models/NpsScoreAssignment.php"
      to: "app/Models/NpsResponseScore.php"
      via: "belongsTo(nps_response_score_id)"
      pattern: "nps_response_score_id"
---

<objective>
Criar a fundação de persistência do snapshot congelado do NPS multi-modelo: as 3 tabelas
`nps_response_scores`, `nps_response_covered_services` e `nps_score_assignments` (DEC-79-C) e
os 3 Eloquent models correspondentes.

Purpose: Estas tabelas são o contrato que o `NpsSnapshotService` (Plano 79-04) preencherá no
submit e que a Fase 80 (bônus/relatórios) consumirá. Congelam médias por dimensão, os serviços
cobertos e as atribuições média×pessoa no momento da resposta — histórico imutável.
Output: 1 migration cross-driver + 3 models + teste de schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-CONTEXT.md
@.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-RESEARCH.md

<interfaces>
<!-- Valores canônicos de dimensão (reusar EXATAMENTE — Pitfall 4/5 do RESEARCH) -->
De app/Models/NpsTemplateQuestion.php:85 — DIMENSOES = [estrategista, analista, empresa, geral].
A tabela nps_response_scores usa enum SÓ [estrategista, analista, empresa] (NÃO incluir 'geral' —
'geral' não gera score de pessoa nem atribuição).

De app/Models/NpsResponseAnswer.php — coluna existente `question_dimensao_snapshot`
enum('estrategista','analista','empresa','geral'). Os valores de dimensao das novas tabelas
DEVEM casar com estes para JOIN/filtro futuro.

De app/Models/Company.php:130-132 — serviço tem coluna `setor` (Servico::SETOR_PERFORMANCE='performance',
SETOR_SHOPEE='shopee'). `service_setor` das novas tabelas guarda esse snapshot (string).

Padrão de `role` na pivot company_users: valores 'consultor' (=Analista) e 'estrategista'.
DECISÃO TRAVADA neste plano: a coluna `role` de nps_score_assignments PERSISTE o valor da pivot
('consultor'/'estrategista') — para JOIN direto na Fase 80 (A3 do RESEARCH). NÃO usar analyst/strategist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1 (Wave 0): Teste de schema das 3 tabelas</name>
  <files>tests/Feature/V16/SnapshotSchemaTest.php</files>
  <read_first>
    - tests/Feature/V16/CriaCenarioResponsaveis.php (trait de fixtures reusável)
    - database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php (padrão de tabela v15 + índices)
    - phpunit.xml (conexão de teste = SQLite in-memory)
  </read_first>
  <action>
    Criar o teste no namespace `Tests\Feature\V16` usando `RefreshDatabase` (mesma base das outras
    suites V16). Escrever asserts que FALHAM hoje (tabelas ainda não existem) e passarão após a Tarefa 2:
    (a) `Schema::hasTable` verdadeiro para 'nps_response_scores', 'nps_response_covered_services' e
    'nps_score_assignments'; (b) `Schema::hasColumns` para as colunas exatas de cada tabela conforme
    DEC-79-C — scores: nps_response_id, company_id, dimensao, score_sum, question_count, average_score,
    calculated_at; covered_services: nps_response_id, servico_id, service_setor, captured_at;
    assignments: nps_response_id, nps_response_score_id, company_id, servico_id, service_setor, role,
    user_id, average_score, assigned_at; (c) inserir via `DB::table` uma linha em nps_responses (usar
    survey/response mínimos ou factory se houver) e uma linha em cada tabela nova encadeada por FK, depois
    deletar a nps_responses e afirmar que as 3 linhas filhas somem (cascadeOnDelete). Comentários em pt-BR.
    NÃO instalar libs. Usar `/c/xampp/php/php.exe artisan test tests/Feature/V16/SnapshotSchemaTest.php`.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16/SnapshotSchemaTest.php</automated>
  </verify>
  <done>Teste existe e FALHA por tabelas inexistentes (RED). Após Tarefa 2, passa (GREEN).</done>
</task>

<task type="auto">
  <name>Tarefa 2: Migration cross-driver das 3 tabelas de snapshot</name>
  <files>database/migrations/2026_07_14_200001_create_nps_snapshot_tables.php</files>
  <read_first>
    - database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php (Schema::create + foreignId()->constrained + índices — molde)
    - database/migrations/2026_07_14_000001_add_servico_id_to_company_users.php (padrão FK cross-driver Phase 76)
    - .planning/phases/79-.../79-RESEARCH.md → "Pattern 1: Migration cross-driver" (linhas 100-121)
  </read_first>
  <action>
    Migration anônima (estilo do projeto) com `Schema::create` das 3 tabelas NA ORDEM: primeiro
    `nps_response_scores`, depois `nps_response_covered_services`, por último `nps_score_assignments`
    (que tem FK para nps_response_scores — a ordem garante a FK cross-driver, ver RESEARCH nota 104).
    Todas as FKs via `foreignId(...)->constrained('<tabela>')->cascadeOnDelete()` para
    nps_response_id/company_id; `foreignId('servico_id')->constrained('servicos')->nullOnDelete()` e
    `foreignId('user_id')->constrained('users')->cascadeOnDelete()` em assignments; `nps_response_score_id`
    → `constrained('nps_response_scores')->cascadeOnDelete()`.
    Colunas por DEC-79-C: scores → `enum('dimensao', ['estrategista','analista','empresa'])`,
    `decimal('score_sum',8,2)`, `unsignedInteger('question_count')`, `decimal('average_score',5,2)`,
    `timestamp('calculated_at')`, `timestamps()`, `unique(['nps_response_id','dimensao'],'nps_resp_scores_dim_uniq')`,
    `index(['company_id','dimensao'])`.
    covered_services → `string('service_setor')`, `timestamp('captured_at')`, `timestamps()`,
    `unique(['nps_response_id','servico_id'],'nps_resp_cov_serv_uniq')`.
    assignments → `enum('role', ['consultor','estrategista'])`, `decimal('average_score',5,2)`,
    `string('service_setor')`, `timestamp('assigned_at')`, `timestamps()`, `index(['user_id','role'],'nps_score_assign_user_role_idx')`,
    `index(['service_setor'],'nps_score_assign_setor_idx')`.
    down(): `Schema::dropIfExists` na ordem inversa (assignments → covered_services → scores). Comentários pt-BR
    explicando o congelamento histórico. NÃO usar `virtualAs`/índice parcial aqui (não é necessário; enum simples
    é cross-driver — ver Pitfall 5, garantir valores válidos nos testes/seed).
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16/SnapshotSchemaTest.php</automated>
  </verify>
  <done>Migration cria as 3 tabelas com FKs e índices; SnapshotSchemaTest passa (GREEN) no SQLite.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Eloquent models das 3 tabelas (contrato para 79-04)</name>
  <files>app/Models/NpsResponseScore.php, app/Models/NpsResponseCoveredService.php, app/Models/NpsScoreAssignment.php</files>
  <read_first>
    - app/Models/NpsResponseAnswer.php (padrão de model NPS: fillable, casts, $table implícito, docblock pt-BR)
    - app/Models/NpsResponse.php (para adicionar as relações hasMany inversas)
  </read_first>
  <action>
    Criar os 3 models seguindo o padrão de NpsResponseAnswer.php (fillable explícito, casts de decimal→float
    e timestamps de snapshot→datetime, docblock pt-BR de responsabilidade). `NpsResponseScore`: fillable com
    todas as colunas de DEC-79-C; relação `belongsTo(NpsResponse::class,'nps_response_id')`; `hasMany(NpsScoreAssignment::class,'nps_response_score_id')`.
    `NpsResponseCoveredService`: belongsTo NpsResponse + belongsTo Servico. `NpsScoreAssignment`: belongsTo
    NpsResponse, belongsTo NpsResponseScore (via nps_response_score_id), belongsTo User, belongsTo Servico;
    cast de average_score→float. Estes models são o CONTRATO consumido pelo NpsSnapshotService (Plano 79-04):
    nenhuma lógica de negócio aqui, só schema/relations. Comentar que a coluna `role` guarda o valor da pivot
    ('consultor'=Analista, 'estrategista'). Não adicionar LogsActivity (dados de snapshot imutável, sem edição).
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16/SnapshotSchemaTest.php</automated>
  </verify>
  <done>Os 3 models existem, resolvem via container e mapeiam corretamente as tabelas; suite V16 verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → banco (prod MySQL vs testes SQLite) | Divergência de driver pode gerar schema diferente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01-01 | Tampering | Integridade do snapshot congelado | mitigate | FKs `nps_response_score_id`/`nps_response_id` + unique `(nps_response_id, dimensao)` garantem 1 linha/dimensão e amarração à resposta; sem edição (models sem LogsActivity, tabelas append-only no fluxo) |
| T-79-01-02 | Denial of Service | Migration incompatível cross-driver | mitigate | enum simples (sem virtualAs/índice parcial); SnapshotSchemaTest roda no SQLite; validação manual `SHOW CREATE TABLE` no VPS (79-VALIDATION Manual-Only) |
| T-79-01-SC | Tampering | npm/composer installs | accept | Fase não instala pacotes (RESEARCH "Package Legitimacy Audit: não aplicável") |
</threat_model>

<verification>
- `/c/xampp/php/php.exe artisan test tests/Feature/V16` — suite V16 verde.
- Cascade delete comprovado no teste (deletar nps_responses zera as 3 filhas).
</verification>

<success_criteria>
- 3 tabelas criadas com colunas/FKs/índices de DEC-79-C, migrando no SQLite (testes) sem erro.
- 3 models mapeando as tabelas com relations corretas — contrato pronto para o Plano 79-04.
- `role` de assignments persiste 'consultor'/'estrategista' (valor da pivot).
</success_criteria>

<output>
Criar `.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-01-SUMMARY.md` ao concluir.
</output>
