---
phase: 79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_14_200002_seed_nps_shopee_and_link_performance_scopes.php
  - tests/Feature/V16/SeedNpsShopeeTest.php
autonomous: true
requirements: [DEC-79-B, DEC-79-A]
must_haves:
  truths:
    - "Existe um template 'NPS Shopee' (is_default=false, active=true, envio_automatico_mensal=true) com 3 perguntas e 15 opções"
    - "O template 'NPS Shopee' está linkado ao serviço setor=shopee em nps_template_service_scopes"
    - "O template 'NPS Padrão' (is_default=true) está linkado a TODOS os serviços ativos setor=performance"
    - "Rodar a migration duas vezes não duplica templates/perguntas/opções/scopes (idempotente)"
  artifacts:
    - path: "database/migrations/2026_07_14_200002_seed_nps_shopee_and_link_performance_scopes.php"
      provides: "Seed idempotente do NPS Shopee + link performance→NPS Padrão"
      contains: "NPS Shopee"
  key_links:
    - from: "nps_templates (NPS Shopee)"
      to: "servicos (setor=shopee)"
      via: "nps_template_service_scopes"
      pattern: "nps_template_service_scopes"
    - from: "nps_templates (is_default=true)"
      to: "servicos (setor=performance)"
      via: "nps_template_service_scopes"
      pattern: "nps_template_service_scopes"
---

<objective>
Semear (idempotente) o modelo "NPS Shopee" espelhando o "NPS Padrão" e linkar os serviços cobertos:
NPS Shopee → serviço shopee; NPS Padrão → todos os serviços ativos setor=performance (DEC-79-B, DEC-79-A).

Purpose: Sem os service_scopes de performance no NPS Padrão, o disparo ESTRITO (Plano 79-03) faria
TODAS as empresas ML ficarem sem NPS. Por isso o seed e a mudança de disparo entram no MESMO deploy
(RESEARCH Pitfall 1). O NPS Shopee com scope shopee habilita o 2º NPS por empresa ML+Shopee.
Output: 1 migration/seed idempotente + teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-CONTEXT.md
@.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-RESEARCH.md

<interfaces>
<!-- MOLDE EXATO a espelhar -->
De database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php:
- Guards idempotentes: template por `where('nome',...)`/`where('is_default',...)->first()`;
  pergunta por `where(template_id, dimensao)->first()`; opção por `where(question_id, peso)->exists()`.
- 3 perguntas: dimensao=estrategista (obrigatoria=true, ordem=1, texto '{nome_estrategista}'…),
  dimensao=analista (obrigatoria=false, ordem=2, '{nome_analista}'…), dimensao=empresa (obrigatoria=true, ordem=3).
- tipo sempre 'escala'; 5 options por pergunta loop for i=1..5 (label=(string)i, peso=i, ordem=i).
- Coluna FK das options é `question_id` (NÃO template_question_id).
- Tudo dentro de DB::transaction, DB::table puro, now() nos timestamps.

De app/Models/Servico.php: SETOR_PERFORMANCE='performance', SETOR_SHOPEE='shopee'; coluna `ativo`.
De app/Models/NpsTemplate.php:129 relação serviceScopes()=servicos() sobre pivot nps_template_service_scopes.
Pivot columns: template_id, servico_id (+ timestamps).

Assumption A2 (RESEARCH): o serviço shopee (setor='shopee', ativo=true) já foi semeado na Phase 75
(migration 2026_07_14_100002). Guard `if ($servicoShopeeId)` cobre ausência.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1 (Wave 0): Teste do seed NPS Shopee + scopes performance (idempotência)</name>
  <files>tests/Feature/V16/SeedNpsShopeeTest.php</files>
  <read_first>
    - tests/Feature/V16/CriaCenarioResponsaveis.php (helper criarServico para semear serviços de teste)
    - database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php (guards idempotentes)
  </read_first>
  <action>
    Teste no namespace `Tests\Feature\V16` com `RefreshDatabase`. Como as migrations já rodam no setUp,
    o seed já terá executado 1x. Cenário: antes de re-rodar, criar via `criarServico` (do trait) um serviço
    setor=performance ativo e o serviço shopee ativo (se o seed da Phase 75 não estiver na base de teste,
    criar shopee manualmente). Depois invocar a migration do seed uma 2ª vez (via
    `Artisan::call('migrate', ...)` não reexecuta migrations já rodadas — em vez disso, INSTANCIAR a classe
    da migration e chamar `->up()` diretamente, padrão dos testes de seed do projeto) e afirmar:
    (a) existe exatamente 1 template `nome='NPS Shopee'` com is_default=false, active=true,
    envio_automatico_mensal=true, priority>0; (b) ele tem 3 perguntas (estrategista/analista/empresa) e 15
    opções (5 por pergunta, peso 1..5); (c) existe scope nps_template_service_scopes ligando NPS Shopee ao
    serviço setor=shopee; (d) o NPS Padrão (is_default=true) tem scope para o serviço setor=performance ativo;
    (e) IDEMPOTÊNCIA: contar linhas de nps_templates/nps_template_questions/nps_template_options/
    nps_template_service_scopes antes e depois de um `->up()` extra — contagens IGUAIS (2ª execução afeta 0 rows).
    Comentários pt-BR. NÃO instalar libs.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16/SeedNpsShopeeTest.php</automated>
  </verify>
  <done>Teste FALHA hoje (seed inexistente / NPS Shopee ausente). Após Tarefa 2, passa incluindo o check de idempotência.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Migration/seed idempotente do NPS Shopee + link performance→Padrão</name>
  <files>database/migrations/2026_07_14_200002_seed_nps_shopee_and_link_performance_scopes.php</files>
  <read_first>
    - database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php (molde completo — copiar estrutura de guards e loop de opções)
    - .planning/phases/79-.../79-RESEARCH.md → "Pattern 2: Seed idempotente espelhando 100004" (linhas 129-165)
  </read_first>
  <action>
    Migration anônima, tudo dentro de `DB::transaction`, seguindo o molde do 100004 (DB::table puro, guards).
    Passo A — Template NPS Shopee: guard `DB::table('nps_templates')->where('nome','NPS Shopee')->first()`;
    se ausente, insertGetId com nome='NPS Shopee', descricao='Modelo NPS para o serviço Shopee (Gestão de ADS
    Shopee).', active=true, is_default=false (NÃO principal — bônus continua no NPS Padrão por DEC-79-E),
    priority=10 (>0, DEC-79-B), envio_automatico_mensal=true, timestamps now().
    Passo B — 3 perguntas idempotentes por (template_id, dimensao) com os MESMOS textos/tipo/obrigatoriedade/ordem
    do 100004 (estrategista obrigatória ordem1, analista opcional ordem2, empresa obrigatória ordem3), tipo='escala';
    para cada, loop for i=1..5 inserindo option (question_id, label=(string)i, peso=i, ordem=i) com guard
    `where(question_id, peso)->exists()`.
    Passo C — Scope Shopee: `$servicoShopeeId = DB::table('servicos')->where('setor','shopee')->where('ativo',true)->value('id')`;
    `if ($servicoShopeeId)` → `updateOrInsert(['template_id'=>shopeeId,'servico_id'=>servicoShopeeId], [timestamps])`.
    Passo D — Link performance→NPS Padrão (DEC-79-A): `$padraoId = DB::table('nps_templates')->where('is_default',true)->value('id')`;
    para CADA serviço `where('setor','performance')->where('ativo',true)->get()` → `updateOrInsert(['template_id'=>padraoId,'servico_id'=>$s->id],[timestamps])`.
    Log::info com contagem de scopes criados. Se serviço shopee ausente → Log::warning estruturado (não abortar).
    down(): no-op intencional com Log::info (padrão do 100004 — reverter destruiria dados semânticos). Comentários pt-BR.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16/SeedNpsShopeeTest.php</automated>
  </verify>
  <done>NPS Shopee semeado (3 perguntas/15 opções/scope shopee); NPS Padrão linkado a performance; 2ª execução no-op. Teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed → estado de prod (scopes pré-existentes) | Scope performance pode já existir manualmente em prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | Tampering | Duplicação de template/scope em re-run | mitigate | Guards `where(...)->first()/exists()` + `updateOrInsert` garantem idempotência (teste (e) prova 0 rows na 2ª execução) |
| T-79-02-02 | Denial of Service | Empresa performance sem NPS por scope faltante | mitigate | Passo D linka TODOS os serviços performance ativos ao Padrão; roda no MESMO deploy do disparo (Pitfall 1); Log::info conta scopes criados |
| T-79-02-03 | Denial of Service | Serviço shopee ausente na base | accept | Guard `if ($servicoShopeeId)` + Log::warning — não aborta o resto do seed (A2 do RESEARCH) |
| T-79-02-SC | Tampering | npm/composer installs | accept | Fase não instala pacotes |
</threat_model>

<verification>
- `/c/xampp/php/php.exe artisan test tests/Feature/V16/SeedNpsShopeeTest.php` verde, incluindo idempotência.
- Regressão: `/c/xampp/php/php.exe artisan test --filter=Nps` continua verde (seed é aditivo).
</verification>

<success_criteria>
- NPS Shopee existe espelhando o NPS Padrão (3 perguntas escala, 5 opções peso 1..5 cada) com scope shopee.
- NPS Padrão linkado a todos os serviços ativos setor=performance.
- Migration idempotente (segunda execução afeta 0 rows).
</success_criteria>

<output>
Criar `.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-02-SUMMARY.md` ao concluir.
</output>
