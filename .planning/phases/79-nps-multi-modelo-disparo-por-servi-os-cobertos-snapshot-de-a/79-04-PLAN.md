---
phase: 79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a
plan: 04
type: execute
wave: 2
depends_on: ["79-01", "79-02"]
files_modified:
  - app/Services/Nps/NpsSnapshotService.php
  - app/Http/Controllers/NpsController.php
  - tests/Feature/V16/SubmitSnapshotTest.php
  - tests/Feature/V16/AtribuicaoPorServicoNpsTest.php
autonomous: true
requirements: [DEC-79-D, DEC-79-E]
must_haves:
  truths:
    - "Ao responder, grava 1 nps_response_scores por dimensão calculada (estrategista/analista/empresa) com sum/count/average"
    - "Congela os serviços cobertos do modelo em nps_response_covered_services no momento da resposta"
    - "Gera nps_score_assignments só para responsáveis dos serviços cobertos ∩ ATIVOS; responsável faltante → sem assignment + log"
    - "A média da empresa fica em scores (dimensao=empresa), sem assignment de pessoa"
    - "Submit legacy Phase 31, submit v15 atual e o bônus ->principal() continuam inalterados (regressão verde)"
  artifacts:
    - path: "app/Services/Nps/NpsSnapshotService.php"
      provides: "Helper que congela scores + covered_services + assignments no submit"
      contains: "registrar"
    - path: "app/Http/Controllers/NpsController.php"
      provides: "submitResponseV15 chama o snapshot dentro da transação após as answers"
      contains: "NpsSnapshotService"
  key_links:
    - from: "NpsController::submitResponseV15"
      to: "NpsSnapshotService::registrar"
      via: "chamada dentro da DB::transaction após foreach answers"
      pattern: "NpsSnapshotService"
    - from: "NpsSnapshotService"
      to: "Company::consultorDoServico / estrategistaDoServico"
      via: "resolução do responsável por serviço coberto"
      pattern: "DoServico"
---

<objective>
Congelar o snapshot no submit v15 (DEC-79-D): calcular as médias por dimensão via `NpsScoreCalculator`,
gravar em `nps_response_scores`, congelar os serviços cobertos do modelo em `nps_response_covered_services`,
e gerar as atribuições média×user×role×serviço em `nps_score_assignments` — SÓ para os responsáveis dos
serviços cobertos ∩ ativos. Bônus INTOCADO (DEC-79-E).

Purpose: Torna o histórico de atribuição imutável (trocar responsável/modelo/scope depois não altera notas
antigas) e deixa as atribuições prontas para a Fase 80 consumir. A dimensão empresa fica só em scores
(nunca vira nota de pessoa — meritocracia).
Output: novo `NpsSnapshotService` + wiring no `submitResponseV15` + testes de snapshot e isolamento por serviço.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-CONTEXT.md
@.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-RESEARCH.md

<interfaces>
<!-- MODELS de snapshot (do Plano 79-01) — CONTRATO a consumir -->
NpsResponseScore (nps_response_id, company_id, dimensao, score_sum, question_count, average_score, calculated_at)
NpsResponseCoveredService (nps_response_id, servico_id, service_setor, captured_at)
NpsScoreAssignment (nps_response_id, nps_response_score_id, company_id, servico_id, service_setor, role, user_id, average_score, assigned_at)
-> role guarda o valor da pivot: 'consultor' (Analista) / 'estrategista'.

<!-- CÁLCULO existente — reusar, NÃO recriar -->
De app/Services/Nps/NpsScoreCalculator.php:65 — compute($response, $dim): ?float.
  Retorna SUM(option_peso_snapshot)/N_perguntas_do_template na dimensão. Retorna null se a dimensão
  não existe no template → NÃO gravar linha de score nessa dimensão.
  Para gravar sum/count separadamente (RESEARCH Pattern 1, linhas 123-127):
    question_count = NpsTemplateQuestion::where('template_id', $survey->template_id)->where('dimensao',$dim)->count()
    score_sum = $response->answers()->where('question_dimensao_snapshot',$dim)->sum('option_peso_snapshot')
    average_score = compute() (mesmo valor).
  DIMENSÕES a percorrer: ['estrategista','analista','empresa'] (NÃO 'geral').

<!-- COBERTOS + RESPONSÁVEIS -->
De app/Models/NpsTemplate.php:129 serviceScopes() → serviços cobertos (cada um tem coluna `setor`).
De app/Models/Company.php:197-209 — consultorDoServico($id)->get() (Analistas, role=consultor),
  estrategistaDoServico($id)->get() (Estrategistas, role=estrategista).
De app/Models/Company.php:272 contratosServico()->active()->pluck('servico_id') = serviços ATIVOS da empresa.
company_id / template vêm de $survey (response->survey).

<!-- WIRING no submit — NpsController.php -->
- submitResponseV15 :653-718 — DB::transaction: cria NpsResponse (656), foreach answers (666-710),
  survey->update(completed) (714-717). O snapshot ENTRA depois do foreach das answers e DENTRO da mesma
  transação (Pitfall 3: antes das answers o calculator lê zero). Ordem sugerida: após o foreach (710),
  antes/depois do survey->update — mas dentro do try/DB::transaction para reverter junto no 23000.
- respond() :449-450 — display dos nomes hoje é consolidado (users()->wherePivot). Open Question (b).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1 (Wave 0): Testes de snapshot e isolamento de atribuição por serviço</name>
  <files>tests/Feature/V16/SubmitSnapshotTest.php, tests/Feature/V16/AtribuicaoPorServicoNpsTest.php</files>
  <read_first>
    - tests/Feature/V16/CriaCenarioResponsaveis.php (criarCenarioMlComResponsaveis, inserirLinhaShopee, inserirPivot)
    - app/Http/Controllers/NpsController.php:601-731 (fluxo submitResponseV15 + rota de submit)
    - database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php (perguntas/opções do NPS Padrão para montar respostas válidas)
  </read_first>
  <behavior>
    SubmitSnapshotTest:
    - Dado uma survey do NPS Padrão para empresa performance com analista+estrategista (servico_id perf) e
      respostas válidas (option_id das 3 dimensões), ao submeter: cria 1 nps_response_scores por dimensão
      calculada com score_sum = soma dos pesos, question_count = nº de perguntas do template na dimensão,
      average_score = sum/count. NÃO cria linha para dimensão sem pergunta/sem resposta (compute()==null).
    - Congela nps_response_covered_services com o(s) serviço(s) cobertos do template (service_setor = setor snapshot).
    - Dimensão empresa: existe nps_response_scores(dimensao=empresa) MAS nenhum nps_score_assignments para ela.
    - Regressão pontual: nps_response_answers continua sendo gravado normalmente (fluxo v15 intacto).
    AtribuicaoPorServicoNpsTest:
    - Empresa ML+Shopee com analista ML (servico_id perf) e analista Shopee (servico_id shopee) distintos.
      Survey do NPS Shopee (scope=shopee): a média do bloco analista gera nps_score_assignments para o analista
      SHOPEE (servico_id shopee, role=consultor), e NÃO para o analista ML. Idem estrategista.
    - Serviço coberto sem responsável na pivot → NENHUM assignment criado (sem user_id null) + Log::warning
      capturado (Log::spy) indicando a pendência.
    - Interseção vazia (serviço coberto não é contrato ATIVO da empresa) → sem assignment para aquele serviço.
  </behavior>
  <action>
    Escrever os 2 testes em `Tests\Feature\V16` com `RefreshDatabase`, reusando o trait `CriaCenarioResponsaveis`.
    Submeter respostas via POST na rota pública de submit (mesma que o `respond`/`submitResponse` usa — resolver
    pelo token do survey) OU chamando o controller conforme o padrão dos testes NPS existentes. Montar option_ids
    válidos a partir das perguntas/opções semeadas. Usar `Log::spy()` para os asserts de pendência. Comentários pt-BR.
    Estes testes FALHAM hoje (snapshot ainda não é gravado) e passam após as Tarefas 2 e 3.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16/SubmitSnapshotTest.php tests/Feature/V16/AtribuicaoPorServicoNpsTest.php</automated>
  </verify>
  <done>Ambos os testes existem e FALHAM (RED) por ausência de snapshot. Passam após Tarefas 2+3.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: NpsSnapshotService — congela scores, covered_services e assignments</name>
  <files>app/Services/Nps/NpsSnapshotService.php</files>
  <read_first>
    - app/Services/Nps/NpsScoreCalculator.php (contrato compute() + semântica null)
    - app/Models/Company.php:197-209, :272 (consultorDoServico/estrategistaDoServico/contratosServico)
    - app/Models/NpsTemplate.php:129 (serviceScopes)
    - .planning/phases/79-.../79-RESEARCH.md → "Fluxo (submit v15 estendido)" (46-76) e Anti-Patterns (167-173)
  </read_first>
  <behavior>
    - registrar(NpsResponse $response): para dim in [estrategista, analista, empresa]: se compute() != null,
      grava NpsResponseScore (sum/count/average/calculated_at=now). compute()==null → pula (sem linha).
    - congela serviços cobertos do template do survey em NpsResponseCoveredService (servico_id + service_setor snapshot).
    - interseção = servicos cobertos ∩ contratosServico()->active()->pluck(servico_id). Para cada servico da interseção:
        analista(s) = consultorDoServico(servico)->get(); se há score analista → 1 NpsScoreAssignment por analista
        (role='consultor', user_id, average_score=score analista, nps_response_score_id=id do score analista,
        servico_id, service_setor). Sem analista → nada + Log::warning pendência.
        estrategista(s) = estrategistaDoServico(servico)->get() → idem com role='estrategista' e score estrategista.
    - dimensão empresa: só score, nunca assignment.
  </behavior>
  <action>
    Criar o service stateless `App\Services\Nps\NpsSnapshotService` (padrão do NpsScoreCalculator: classe sem
    estado, resolvida via container; injeção de `NpsScoreCalculator` no construtor OU `app(...)`). Método público
    `registrar(NpsResponse $response): void` implementando o <behavior> acima usando os models do Plano 79-01 e os
    helpers já existentes. NÃO fazer AVG manual — usar `NpsScoreCalculator::compute()`. Guardar a referência do
    NpsResponseScore da dimensão analista/estrategista para preencher `nps_response_score_id` do assignment.
    Log::warning estruturado com tag `[NPS Snapshot]` para responsável faltante (company_id, servico_id, role).
    NÃO abrir transação própria — o método roda DENTRO da transação do submit (Tarefa 3). Docblock pt-BR
    explicando congelamento imutável. NÃO tocar DesempenhoScoreService/->principal() (DEC-79-E).
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16/SubmitSnapshotTest.php tests/Feature/V16/AtribuicaoPorServicoNpsTest.php</automated>
  </verify>
  <done>Service implementado; testes de snapshot/atribuição passam quando invocado pelo submit (Tarefa 3).</done>
</task>

<task type="auto">
  <name>Tarefa 3: Plugar o snapshot no submitResponseV15 (+ display por serviço no respond)</name>
  <files>app/Http/Controllers/NpsController.php</files>
  <read_first>
    - app/Http/Controllers/NpsController.php:601-731 (submitResponseV15 — transação e ordem)
    - app/Http/Controllers/NpsController.php:449-480 (respond — resolução de nomes para display)
  </read_first>
  <action>
    (1) Em `submitResponseV15`, DENTRO da `DB::transaction` (linha 653), APÓS o `foreach` que grava as answers
    (termina ~710) e ANTES ou DEPOIS do `$survey->update(['status'=>'completed'...])` (714-717) — mas dentro da
    mesma transação — invocar `app(NpsSnapshotService::class)->registrar($response)`. Ordem crítica (Pitfall 3):
    NUNCA antes do foreach das answers (o calculator leria zero). Se o 23000 do dedup estourar no update, a
    transação reverte o snapshot junto (comportamento correto — mantém o catch existente 719-728).
    (2) Display por serviço (Open Question b — TRAVADA como "incluir por ser barato", backend-only): em `respond()`
    (449-450), quando `$survey->template` tem serviceScopes cobrindo um serviço que também é contrato ATIVO da
    empresa, resolver `estrategistaDoServico($servicoId)->first()` / `consultorDoServico($servicoId)->first()`
    para os nomes exibidos, com FALLBACK para a leitura consolidada atual (users()->wherePivot) quando não houver
    escopo/responsável por serviço. Não alterar o front (Respond.jsx) — só a resolução dos nomes no backend.
    Manter 100% do fluxo v15 e legacy intactos (DEC-79-E: submit legacy Phase 31 e ->principal() inalterados).
    Comentários pt-BR.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16</automated>
  </verify>
  <done>submitResponseV15 grava o snapshot dentro da transação após as answers; respond mostra responsáveis do serviço coberto (com fallback). Suite V16 verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submit público (token) → gravação de snapshot/atribuição | Input do cliente vira notas atribuídas a pessoas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-04-01 | Tampering | Atribuição para responsável de serviço NÃO coberto/ativo | mitigate | Assignment só é criado na interseção `serviços cobertos ∩ contratosServico()->active()`; nunca fora dela (RESEARCH Known Threat "Assignment forjado") |
| T-79-04-02 | Tampering | Integridade do snapshot congelado | mitigate | Snapshot roda dentro da DB::transaction do submit; unique `(nps_response_id,dimensao)`; option_id validado por `Rule::in` (643) barra tampering antes de virar peso |
| T-79-04-03 | Information Disclosure | Nota da empresa vazando como nota de pessoa | mitigate | Dimensão empresa fica só em nps_response_scores, sem assignment (DEC-79-D item 4; teste dedicado) |
| T-79-04-04 | Denial of Service | Snapshot antes das answers → média zero | mitigate | Ordem garantida: registrar() após o foreach das answers, dentro da transação (Pitfall 3) |
| T-79-04-05 | Repudiation | Responsável faltante sem rastro | mitigate | Log::warning `[NPS Snapshot]` com company_id/servico_id/role em vez de assignment vazio |
| T-79-04-SC | Tampering | npm/composer installs | accept | Fase não instala pacotes |
</threat_model>

<verification>
- `/c/xampp/php/php.exe artisan test tests/Feature/V16` verde (schema+seed+disparo+snapshot+atribuição).
- Regressão OBRIGATÓRIA: `/c/xampp/php/php.exe artisan test --filter=Nps` e `--filter=Desempenho` verdes
  (submit legacy Phase 31 + submit v15 + bônus ->principal() inalterados — DEC-79-E).
</verification>

<success_criteria>
- Submit grava scores por dimensão + covered_services + assignments (só interseção cobertos∩ativos), dentro da transação após as answers.
- NPS Shopee atribui ao responsável Shopee (servico_id), não ao ML; responsável faltante → sem assignment + log.
- Empresa fica só em scores; bônus e submits legacy/v15 inalterados (regressão verde).
</success_criteria>

<output>
Criar `.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-04-SUMMARY.md` ao concluir.
</output>
