---
phase: 79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a
plan: 03
type: execute
wave: 2
depends_on: ["79-02"]
files_modified:
  - app/Console/Commands/NpsDispararMensal.php
  - tests/Feature/V16/DisparoEstritoTest.php
autonomous: true
requirements: [DEC-79-A]
must_haves:
  truths:
    - "Empresa só-performance recebe 1 survey do NPS Padrão; empresa só-shopee recebe 1 do NPS Shopee"
    - "Empresa performance+shopee recebe 2 surveys (um por modelo)"
    - "Empresa sem serviço coberto por nenhum modelo recebe 0 survey e gera Log::warning estruturado"
    - "O dedup impede duplicar survey por (company_id, mês, template_id) — nunca bloqueia o 2º modelo"
  artifacts:
    - path: "app/Console/Commands/NpsDispararMensal.php"
      provides: "Disparo estrito: 1 envio por modelo aplicável cujos serviços cobertos ∩ contratos ativos ≠ ∅"
      contains: "envio_automatico_mensal"
  key_links:
    - from: "NpsDispararMensal (loop por empresa)"
      to: "nps_template_service_scopes ∩ contratos_servico ativos"
      via: "whereHas('serviceScopes', whereIn servicoIds)"
      pattern: "serviceScopes"
---

<objective>
Reescrever o loop do `NpsDispararMensal` de "força o principal (is_default) para todas as empresas"
para o modo ESTRITO por serviços cobertos (DEC-79-A): para cada empresa elegível, gerar 1 envio por
modelo com `envio_automatico_mensal=true` cujos "Serviços cobertos" tenham interseção com um contrato
ATIVO da empresa. Sem cobertura → NENHUM NPS + Log::warning de observabilidade.

Purpose: Empresa ML+Shopee passa a receber 2 NPS separados por área; empresas ML continuam recebendo
o NPS Padrão (via scopes performance semeados no Plano 79-02). O log blinda o rollout mostrando quem
ficaria sem NPS antes do primeiro envio real.
Output: comando reescrito + teste da matriz de disparo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-CONTEXT.md
@.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-RESEARCH.md

<interfaces>
<!-- ESTADO ATUAL do comando (a mudar) — NpsDispararMensal.php -->
- Linhas 133-143: resolve `$principal = NpsTemplate::where('is_default',true)->first()` ANTES do loop
  e aborta se ausente. → SUBSTITUIR por carregar a coleção de modelos aplicáveis por empresa.
- Linhas 145-162: query base de empresas (guards de canal email/digisac). PRESERVAR intacta.
- Linhas 173-203: guards por-empresa (dia-alvo, estrategista obrigatório). PRESERVAR.
- Linhas 187-189: dedup ATUAL por-empresa `NpsSurvey::where(company_id)->whereDate(month_reference)->exists()`.
  → BUG conhecido (Pitfall 2): bloqueia o 2º modelo. MOVER para dentro do foreach-modelo e ADICIONAR
  `->where('template_id', $modelo->id)`.
- Linha 212: `$template = $principal;` → REMOVER (não há mais template único).
- Linhas 233-242: NpsSurvey::create com template_id. Manter os campos, trocar `$template->id` por `$modelo->id`.
- Assunto do email montado via NpsTextRenderer (textos globais de Configuracao::nps_textos).

<!-- CONTRATOS DISPONÍVEIS -->
De app/Models/Company.php:272 — `$empresa->contratosServico()` (hasMany); scope `active()` em ContratoServico (ativo=true).
De app/Models/NpsTemplate.php:129 serviceScopes() e :149 scopeActive().
Query de modelos aplicáveis (RESEARCH linhas 237-250):
  NpsTemplate::where('active',true)->where('envio_automatico_mensal',true)
    ->whereHas('serviceScopes', fn($q)=>$q->whereIn('servico_id',$servicoIds))->get()

<!-- OPEN QUESTION (a) — TRAVADA: assunto do email por modelo = $modelo->nome (MVP) -->
Prefixar/identificar o assunto com o nome do modelo ($modelo->nome) — sem criar campos novos por-template.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1 (Wave 0): Teste da matriz de disparo estrito + dedup + log</name>
  <files>tests/Feature/V16/DisparoEstritoTest.php</files>
  <read_first>
    - tests/Feature/V16/CriaCenarioResponsaveis.php (criarServico/criarContrato/inserirPivot)
    - app/Console/Commands/NpsDispararMensal.php (guards de canal/dia/estrategista para montar cenário elegível)
    - database/migrations/2026_07_07_100004_seed_nps_template_padrao_and_retro_associate.php (NPS Padrão já semeado no setUp)
  </read_first>
  <action>
    Teste `Tests\Feature\V16` com `RefreshDatabase`. As migrations (incl. seed NPS Shopee + scopes performance
    do Plano 79-02) rodam no setUp. Montar empresas ELEGÍVEIS (email_cliente preenchido, created_at no dia de
    hoje para casar `$diaAlvo`, estrategista atribuído via inserirPivot role=estrategista) e usar `Log::spy()`
    para capturar warnings. Casos:
    (a) empresa com contrato ATIVO só performance → após `Artisan::call('nps:disparar-mensal')`, existe 1 NpsSurvey
        com template_id = NPS Padrão e 0 do Shopee;
    (b) empresa só shopee (contrato shopee ativo) → 1 survey template_id = NPS Shopee;
    (c) empresa performance+shopee → 2 surveys (um por modelo);
    (d) empresa elegível mas SEM contrato coberto por nenhum scope → 0 surveys + Log::warning capturado
        contendo o id/nome da empresa (assert via Log::spy `shouldHaveReceived('warning')`);
    (e) DEDUP: rodar o comando 2x → não cria surveys duplicados para (company_id, mês, template_id), e a
        empresa performance+shopee continua com exatamente 2 (um por modelo, não 1 e não 4).
    Garantir que a config `nps_envio_email_ativo` esteja ligada no cenário (setar em Configuracao se necessário).
    Comentários pt-BR.
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16/DisparoEstritoTest.php</automated>
  </verify>
  <done>Teste FALHA hoje (comando ainda força o principal / dedup por-empresa). Após Tarefa 2, todos os casos passam.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Reescrever NpsDispararMensal para disparo estrito por serviços cobertos</name>
  <files>app/Console/Commands/NpsDispararMensal.php</files>
  <read_first>
    - app/Console/Commands/NpsDispararMensal.php (ler o handle inteiro — 120-368 — para preservar guards, contadores e envio email/digisac)
    - .planning/phases/79-.../79-RESEARCH.md → "Fluxo de dados (disparo estrito)" (linhas 78-98) e Anti-Patterns (169-173)
  </read_first>
  <action>
    (1) REMOVER a resolução única `$principal` (linhas 133-143) e a linha `$template = $principal;` (212). Manter
    o abort-cedo só para "ambos os canais desligados" (128-131) e a query base de empresas (145-162).
    (2) Dentro do foreach empresa, APÓS os guards de dia-alvo (180-182) e estrategista (198-203), calcular
    `$servicoIds = $empresa->contratosServico()->active()->pluck('servico_id')` e
    `$modelosAplicaveis = NpsTemplate::where('active',true)->where('envio_automatico_mensal',true)
      ->whereHas('serviceScopes', fn($q)=>$q->whereIn('servico_id',$servicoIds))->get()`.
    (3) Se `$modelosAplicaveis->isEmpty()` → `Log::warning("[NPS Mensal] empresa {$empresa->id} ({$empresa->name}) sem modelo aplicável — NENHUM NPS gerado (serviços ativos sem cobertura).")`,
    incrementar um contador novo `$puladosSemModelo`, `continue`. NÃO usar fallback is_default (DEC-79-A estrito).
    (4) `foreach ($modelosAplicaveis as $modelo)`: DEDUP por-template —
    `NpsSurvey::where('company_id',$empresa->id)->whereDate('month_reference',$mesAtual)->where('template_id',$modelo->id)->exists()`
    → se true, `$puladosIdempotencia++; continue;`. Substitui o guard por-empresa das linhas 187-189 (Pitfall 2).
    (5) Criar o survey (bloco 233-242) com `template_id => $modelo->id`; no dry-run e no assunto do email usar
    `$modelo->nome` para identificar a área (Open Question a — MVP). Reaproveitar o envio email/digisac existente,
    agora dentro do loop de modelo.
    (6) Ajustar o output final do comando para reportar o novo contador `$puladosSemModelo` (blindagem de rollout).
    PRESERVAR: contadores existentes, envio digisac, clamp de dia (177-178), tag de log `[NPS Mensal]`, catch \Throwable.
    Comentários pt-BR explicando a mudança de "só principal" → "1 envio por modelo aplicável".
  </action>
  <verify>
    <automated>/c/xampp/php/php.exe artisan test tests/Feature/V16/DisparoEstritoTest.php</automated>
  </verify>
  <done>Disparo estrito operacional: matriz performance/shopee/ambos/nenhum + dedup por template_id + Log::warning. Teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| comando agendado → geração de surveys | Decide quantos NPS cada empresa recebe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Denial of Service | Empresa performance sem NPS por config faltante | mitigate | Scopes performance semeados no 79-02 (mesmo deploy) + `$puladosSemModelo` no output + Log::warning por empresa sem cobertura; dry-run no VPS antes do envio real (79-VALIDATION Manual-Only) |
| T-79-03-02 | Tampering | Dedup bloqueia 2º modelo (bug atual) | mitigate | Guard de existência inclui `template_id` (Pitfall 2); dedup composto do banco `(company_id, month_reference, template_id)` já suporta N modelos |
| T-79-03-03 | Elevation of Privilege | Envio para empresa sem canal | mitigate | Guards de canal email/digisac (145-162) e estrategista (198-203) PRESERVADOS |
| T-79-03-SC | Tampering | npm/composer installs | accept | Fase não instala pacotes |
</threat_model>

<verification>
- `/c/xampp/php/php.exe artisan test tests/Feature/V16/DisparoEstritoTest.php` verde.
- Regressão: `/c/xampp/php/php.exe artisan test --filter=Nps` verde (submit legacy/v15 não tocados).
- Manual (pós-deploy VPS): `nps:disparar-mensal --dry-run` e conferir Log::warning de "sem modelo aplicável".
</verification>

<success_criteria>
- 1 envio por modelo aplicável (serviços cobertos ∩ contratos ativos ≠ ∅); sem cobertura → 0 + log.
- Empresa ML+Shopee recebe 2 surveys; dedup por (company, mês, template_id) não duplica nem bloqueia 2º modelo.
- Guards de canal/estrategista e envio email/digisac preservados.
</success_criteria>

<output>
Criar `.planning/phases/79-nps-multi-modelo-disparo-por-servi-os-cobertos-snapshot-de-a/79-03-SUMMARY.md` ao concluir.
</output>
