---
phase: 55-modernizacao-visual-sugadores-magic-ui
plan: "03"
type: execute
wave: 3
depends_on:
  - 55-02
autonomous: false
requirements:
  - REQ-55-03
files_modified:
  - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-03-UAT.md
  - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/PHASE-SUMMARY.md
  - .planning/todos/pending/270702-refinamentos-sugadores-uat-e-magic-ui.md
  - .planning/ROADMAP.md
must_haves:
  truths:
    - "Deploy da Phase 55 aplicado em prod APÓS confirmação explícita do operador (memory `feedback_perguntar_antes_deploy_v9`: outro dev em paralelo na v9.0 — confirmação caso-a-caso)"
    - "Checklist UAT com 6 blocos (tabela shadcn, actions dropdown, customize columns + persistência, blur fade, zero regressão, mobile) executado em prod com resultado OK/BUG por item"
    - "Bloco C do TODO 270702 (Phase 55 — Magic UI + redesign) marcado como concluído"
    - "ROADMAP.md registra Phase 55 como concluída com data 2026-07-03"
    - "PHASE-SUMMARY.md consolida decisões-chave (framer-motion cortado, drag OUT, column visibility greenfield)"
  artifacts:
    - path: ".planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-03-UAT.md"
      provides: "Checklist executado com timestamp, resultado por item, screenshots opcionais"
    - path: ".planning/phases/55-modernizacao-visual-sugadores-magic-ui/PHASE-SUMMARY.md"
      provides: "Sumário final da phase — decisões, deltas de bundle, débitos abertos"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 55 marcada como Concluída"
  key_links:
    - from: "deploy.sh"
      to: "VPS Hostinger 177.7.53.164"
      via: "pscp/plink; supervisorctl restart ecf-worker; php artisan cache:clear"
      pattern: "memory `feedback_perguntar_antes_deploy_v9` — perguntar antes; stash+deploy+pop"
    - from: "UAT checklist"
      to: "changeset das Waves 55-01 + 55-02"
      via: "confronto item-a-item; cada bloco vira linhas OK/BUG"
      pattern: "54-04-UAT.md formato de referência (Phase 54)"
---

<objective>
Fechar a Phase 55 com deploy controlado + UAT humana em prod cobrindo os entregáveis das Waves 55-01 e 55-02. Wave sem código — só operações + validação + documentação.

**Bloqueio de autonomia:** requer confirmação explícita do operador antes do `deploy.sh` (memory `feedback_perguntar_antes_deploy_v9`: v9.0 tem outro dev em paralelo — comandos VPS gerais têm autorização permanente, mas `deploy.sh` específico exige confirmação caso-a-caso).

Após UAT verde, fechar Bloco C do TODO 270702 (Phase 55 — Modernização visual /sugadores + Magic UI), registrar decisões-chave e atualizar ROADMAP.

**Escopo do UAT** — cobrir os 4 entregáveis das waves anteriores:

- **UAT-1 — Tabela shadcn + checkbox + Blur Fade (Wave 55-01)**
- **UAT-2 — Actions dropdown "⋯" (Wave 55-02 T1)**
- **UAT-3 — Customize Columns + persistência localStorage (Wave 55-02 T2)**
- **UAT-4 — Zero regressão das Phases 52 + 54 (bulk copy MLBs, click row, cronômetro, filtros, layout 2 colunas)**
- **UAT-5 — Mobile (<lg)**
- **UAT-6 — Auditoria de bundle**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-CONTEXT.md
@.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-RESEARCH.md
@.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-01-SUMMARY.md
@.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-02-SUMMARY.md
@.planning/todos/pending/270702-refinamentos-sugadores-uat-e-magic-ui.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Deploy da Phase 55 em prod agora?</decision>
  <context>
    Ambas as waves de código estão verdes localmente:
    - Wave 1 (55-01): tabela shadcn `<Table>` + wrapper `Components/ui/checkbox.jsx` + Blur Fade via `tailwindcss-animate`; `npm run build` OK
    - Wave 2 (55-02): actions dropdown "⋯" com 5 itens + Customize Columns + persistência `localStorage sugadores:col-visibility:v1`; `npm run build` OK

    **Memory rule aplicada** (`feedback_perguntar_antes_deploy_v9`): v9.0 tem outro dev em paralelo — confirmar deploy caso-a-caso.

    Arquivos afetados (frontend puro, ZERO backend, sem migration):
    - resources/js/Components/ui/checkbox.jsx (novo)
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx (refactor completo da tabela + actions dropdown + column visibility)
    - public/build/* (bundle Vite)

    Deploy path recomendado: `bash deploy_parcial.sh` — nenhum backend/migration/composer.lock mudou; só assets. Valida com o operador se ele quer o full por precaução.

    **Efeito colateral esperado** (documentar antes do deploy):
    - Layout visual muda notavelmente para operadores familiarizados com a versão pós-Phase 54 (tabela agora é shadcn com header dark suave + hover subtle + checkboxes quadrados amarelos ao invés dos redondos nativos)
    - Botões inline "MLBs" + "Detalhes" foram consolidados no menu "⋯" — treinamento breve pode ser necessário (o menu abre a mesma ação)
    - Colunas opcionais começam TODAS ocultas por padrão — layout inicial pós-deploy fica idêntico ao pré (5 colunas fixas). User precisa clicar em "Customizar colunas" para ver extras.
    - `sugadores:col-visibility:v1` no localStorage é novo — não conflita com nada existente
  </context>
  <options>
    <option id="deploy-parcial">
      <name>bash deploy_parcial.sh</name>
      <pros>Rápido, só sobe arquivos alterados; menor risco de colisão com v9.0 do outro dev</pros>
      <cons>Não roda migrate (OK — sem migration nesta phase); precisa `cache:clear` manual pós-deploy</cons>
    </option>
    <option id="deploy-full">
      <name>bash deploy.sh (full)</name>
      <pros>Garantia máxima — composer install + migrate (no-op) + cache clear + supervisor restart</pros>
      <cons>Mais lento (~2-3min); mais chance de interferir com o outro dev na v9.0</cons>
    </option>
    <option id="wait">
      <name>Aguardar coordenação com o outro dev</name>
      <pros>Zero risco de colisão</pros>
      <cons>Bloqueia entrega até coordenação</cons>
    </option>
  </options>
  <resume-signal>Responder: `deploy-parcial`, `deploy-full` ou `wait`</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Deploy aplicado no VPS 177.7.53.164 conforme opção escolhida acima. Bundle Vite sincronizado, cache limpo.

    **Sequência recomendada** (executor guia o operador):
    1. `git stash` local (proteger WIP do outro dev na v9.0) — SÓ SE o operador confirmar branch v9.0 ativa
    2. `bash deploy_parcial.sh` OU `bash deploy.sh`
    3. Via SSH ou script pós-deploy no VPS:
       - `php artisan cache:clear` (memory `project_ecf_drive_cache_stale`)
       - `php artisan route:list --path=sugadores | grep -E "empresa|update-status"` → rotas listadas
    4. `git stash pop` local (restaurar WIP se stashado)
    5. Abrir `https://admin.ecfconsultoria.com.br/sugadores` — carrega sem 500; navegar até `/sugadores/empresa/{qualquer id existente}`
  </what-built>
  <how-to-verify>
    Operador responde após:
    - Deploy retornou sem erro
    - `cache:clear` executado
    - Página `/sugadores/empresa/{id}` sanity-checked (200 OK, tabela nova aparece com header dark)
  </how-to-verify>
  <resume-signal>Responder "deploy-ok" com output resumido; se falhar colar erro; se pulou algum passo, mencionar</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    UAT completo dos entregáveis das Waves 55-01 e 55-02 em prod. Criar `55-03-UAT.md` com checklist preenchido item-a-item.
  </what-built>
  <how-to-verify>
    Testar em `https://admin.ecfconsultoria.com.br/sugadores/empresa/{id}` com pelo menos um perfil:
    - Login **admin** (regressão ampla + column visibility + actions completo)
    - Opcional: **analista carteira** (garantir 403 em outras empresas e que o dropdown de status funciona no que ele gerencia)

    ### UAT-1 — Tabela shadcn + checkbox quadrado + Blur Fade
    - [ ] Abrir `/sugadores/empresa/{id}` com sugadores presentes → tabela renderiza com header dark suave (não mais o `<thead>` cru anterior)
    - [ ] Header text: uppercase, tracking wide, opacidade `white/50`
    - [ ] Linhas do body: hover em `bg-white/[0.03]` (subtle, não a cor forte anterior)
    - [ ] Checkboxes agora são QUADRADOS com fundo `bg-ecf-yellow` quando marcados (não mais o `accent-ecf-yellow` circular do browser)
    - [ ] Ícone Check preto aparece dentro do checkbox quando marcado
    - [ ] Focus visível: ring amarelo (`ecf-yellow/40`) ao tabular sobre checkbox
    - [ ] Ao carregar a página, o conteúdo faz fade-in suave (~300ms) — Blur Fade
    - [ ] Nenhum erro no console (F12 → Console)

    ### UAT-2 — Actions dropdown "⋯"
    - [ ] Cada linha da tabela mostra APENAS o ícone `⋯` (MoreHorizontal) na célula direita
    - [ ] Botão "MLBs" inline não aparece mais (foi consolidado no menu)
    - [ ] Clicar no `⋯` abre menu com 5 itens: Copiar MLBs, Ver detalhes, Marcar em ação, Marcar resolvido, Ignorar
    - [ ] Menu tem fundo `bg-ecf-card` + border sutil (não fundo branco light do shadcn default)
    - [ ] Item "Copiar MLBs" fica DISABLED quando o sugador é `tipo=campanha` (não elegível)
    - [ ] Clicar em "Copiar MLBs" em um adgroup: MLBs copiados para clipboard + feedback no item (contagem verde ou erro vermelho)
    - [ ] Clicar em "Ver detalhes": navega para `/sugadores/{id}` (Show individual)
    - [ ] Clicar em "Marcar em ação": status muda para `em_acao`, página recarrega (via `router.patch preserveScroll`)
    - [ ] Clicar em "Marcar resolvido": status muda para `resolvido`, sugador some da listagem (que filtra por pendente/em_acao)
    - [ ] Clicar em "Ignorar": status muda para `ignorado`, sugador some da listagem
    - [ ] Clicar no botão `⋯` NÃO navega (stopPropagation OK)

    ### UAT-3 — Customize Columns + persistência
    - [ ] Botão "Customizar colunas" (com ícone SlidersHorizontal) aparece na mesma linha do filtro de período, alinhado à direita
    - [ ] Clicar abre menu com 5 checkbox items: Campaign name, MLB ID, Motivos, Investimento, Vendas
    - [ ] Todos os itens começam desmarcados (default)
    - [ ] Marcar "Investimento" → coluna aparece na tabela imediatamente (posição correta entre Detectado e Ações)
    - [ ] Menu NÃO fecha ao marcar item (permite toggar múltiplos)
    - [ ] Marcar mais 2 colunas (ex: MLB ID e Vendas) → tabela expande as 3 colunas
    - [ ] Refresh F5 → colunas marcadas continuam marcadas + visíveis na tabela
    - [ ] Abrir DevTools > Application > Local Storage: chave `sugadores:col-visibility:v1` com JSON refletindo as toggles
    - [ ] Desmarcar todas → tabela volta ao layout inicial (5 colunas fixas)
    - [ ] Fechar navegador, reabrir: preferência preservada
    - [ ] Colunas fixas (checkbox, Produto, Status, Detectado, Ações) NÃO aparecem no menu de toggle (não podem ser ocultadas)

    ### UAT-4 — Zero regressão (Phases 52 + 54)
    - [ ] Click em qualquer área não-checkbox/não-⋯ da linha → navega para `/sugadores/{id}` (Phase 54-02 B2)
    - [ ] Selecionar 3+ sugadores adgroup via checkbox → barra bulk aparece sticky no topo com "N selecionados" + botão amarelo "Copiar MLBs dos selecionados"
    - [ ] Botão bulk copy funciona: MLBs concatenados no clipboard, feedback "Copiado X MLBs"
    - [ ] Botão "Limpar seleção" limpa os checkboxes
    - [ ] Checkbox "Selecionar todos" no header marca/desmarca todos elegíveis (adgroups)
    - [ ] Checkbox de sugador tipo=campanha fica DISABLED (não elegível para bulk)
    - [ ] Layout 2 colunas: tabela à esquerda (col-span-3), ConfigResumoCard à direita (col-span-1) sticky
    - [ ] Filtro Período (select nativo) continua funcional: mudar para "7d" → URL `?periodo=7d`, tabela atualiza, sidebar sticky preservada
    - [ ] Botão "Rodar análise" no ConfigResumoCard funciona: cronômetro 30s, `router.reload({ only: ['sugadores'] })`
    - [ ] Botão "Configurar" no ConfigResumoCard navega para `/sugadores/config/{companyId}` (só admin ou manager)
    - [ ] Analista fora da carteira ainda recebe 403 ao tentar `/sugadores/empresa/{outraCompany}` (via URL manual)

    ### UAT-5 — Mobile (<1024px)
    - [ ] Redimensionar janela para <1024px → sidebar cai pra CIMA da tabela (grid-cols-1)
    - [ ] Tabela ganha overflow horizontal se conteúdo passa da largura → scroll horizontal funcional (wrapper `<Table>` já garante via `overflow-auto`)
    - [ ] Menu "⋯" ainda abre corretamente (portal do Radix)
    - [ ] Blur Fade continua suave em mobile
    - [ ] Customize Columns menu ainda usável (opções legíveis)

    ### UAT-6 — Auditoria de bundle
    - [ ] `du -sh public/build/assets/` → total dentro de +5 kB do baseline pré-Phase 55 (research §7: baseline 3.5 MB)
    - [ ] `ls -lh public/build/assets/EmpresaListagem-*.js` → chunk aumentou no MÁXIMO +4 kB vs baseline 15 kB (soma de Wave 1 +2kB e Wave 2 +2kB)
    - [ ] `grep -rn "framer-motion" package.json node_modules/.package-lock.json 2>/dev/null | head -3` → 0 matches
    - [ ] `ls resources/js/Components/magicui/ 2>/dev/null` → não existe (nenhum componente Magic UI foi copiado)

    ### Regressões críticas (bloqueia release se falhar)
    - [ ] Bulk copy MLBs 100% funcional
    - [ ] Click row navegando corretamente
    - [ ] ConfigResumoCard sticky em desktop
    - [ ] Filtro período funcionando com preserveScroll
    - [ ] Rodar análise + cronômetro OK
    - [ ] Nenhum crash em `/sugadores/empresa/{id}` com sugadores tipo=campanha (isElegivel=false)
    - [ ] Nenhum crash em `/sugadores/empresa/{id}` vazio (empty state renderiza)

    Se qualquer item falhar → registrar em `55-03-UAT.md` como BUG com passos + screenshot; decidir hotfix (extra plan 55-04) ou revisão focada.
  </how-to-verify>
  <resume-signal>Responder "uat-ok" com contagem (6/6 blocos OK), ou "uat-bug: UAT-N item Y" listando falhas</resume-signal>
</task>

<task type="auto">
  <name>Tarefa 4: Fechar TODO 270702 (Bloco C) + PHASE-SUMMARY + ROADMAP</name>
  <files>
    - .planning/todos/pending/270702-refinamentos-sugadores-uat-e-magic-ui.md
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/PHASE-SUMMARY.md
    - .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/todos/pending/270702-refinamentos-sugadores-uat-e-magic-ui.md — Bloco C completo
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-01-SUMMARY.md
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-02-SUMMARY.md
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-03-UAT.md
    - .planning/ROADMAP.md — localizar entrada Phase 55 (linha 138) e formato de fechamento (imitar Phase 52 e 54 recentes)
    - .planning/phases/54-refinamentos-sugadores-uat-filtros/PHASE-SUMMARY.md — template de referência
  </read_first>
  <action>
    1. **Atualizar TODO 270702:**
       - No topo do Bloco C, adicionar: `**Status: CONCLUÍDO em 2026-07-03 na Phase 55 (55-03-UAT.md).**`
       - Se A + B + C estão todos concluídos, MOVER o arquivo de `.planning/todos/pending/` para `.planning/todos/completed/` (ou onde o padrão do projeto guardar TODOs finalizados — grep antes: `ls .planning/todos/`).
    2. **Criar `PHASE-SUMMARY.md`:**
       - Header: nome da phase + data + waves executadas
       - **Wave 55-01 (base):**
         - Novo wrapper `Components/ui/checkbox.jsx` (Radix + shadcn oficial, dark-theme-friendly com `ecf-yellow`)
         - Refactor tabela `EmpresaListagem.jsx`: `<Table>`/`<TableHeader>`/`<TableRow>`/`<TableCell>` do wrapper compartilhado, com overrides dark theme APENAS no consumidor
         - Blur Fade via `animate-in fade-in duration-300` do `tailwindcss-animate` (0 kb novos)
       - **Wave 55-02 (polish):**
         - Actions dropdown "⋯" por linha com 5 itens (Copiar MLBs, Ver detalhes, Marcar em ação, Marcar resolvido, Ignorar)
         - Botão "Customizar colunas" com 5 opções opcionais (Campaign name, MLB ID, Motivos, Investimento, Vendas)
         - Persistência via `localStorage` com chave versionada `sugadores:col-visibility:v1`
         - Novo handler `mudarStatus(id, novoStatus)` usando `router.patch(sugadores.update-status)` — nome confirmado em routes/web.php: {preencher com nome real}
       - **Decisões-chave (research LOCKED):**
         - framer-motion NÃO instalado — Magic UI cortada exceto Blur Fade CSS-puro (research §3, ~50 kb evitados)
         - Drag handles OUT — sugadores ordenados por `detected_at`, sem caso de uso de priorização manual (research §6)
         - "+ Add Section" OUT — não se aplica ao domínio
         - Column visibility greenfield — pattern novo no projeto (0 matches existentes antes), documentado para reuso futuro em outras telas
         - Chave localStorage versionada (`:v1`) permite migração futura de schema sem quebrar UX
       - **Deltas de bundle** (medidos):
         - Baseline pré-Phase 55: chunk EmpresaListagem = 15 kB
         - Pós-Wave 55-01: {preencher com valor real medido}
         - Pós-Wave 55-02: {preencher com valor real medido}
         - Total dist: {preencher — expectativa ≤ +5 kB vs baseline 3.5 MB}
       - **Débitos abertos:**
         - Toast persistente para mudança de status (Copiar MLBs / Marcar resolvido) — Radix não tem toast nativo; se operador reportar necessidade, phase futura
         - Confirmação (`confirm()`) antes de "Ignorar" ou "Marcar resolvido" — se cliques acidentais aparecerem no UAT
         - Column visibility em outras telas (`Sugadores/Index.jsx`, `Comercial/EmpresasListagem.jsx`) — mesmo pattern reusável, mas não pedido
         - Redesign de `Show.jsx` e `Index.jsx` — deferidos no CONTEXT.md (não são escopo desta phase)
       - **Próximo passo:** verificar se há bugs abertos no UAT que virem hotfix (plan 55-04), ou seguir para próxima phase do backlog.
    3. **Atualizar ROADMAP.md:**
       - Localizar linha 138 (`- [ ] **Phase 55: Modernização visual /sugadores + Magic UI**`)
       - Trocar `[ ]` por `[x]` e reformular no formato usado nas Phases 52/54:
         `- [x] **Phase 55: Modernização visual /sugadores + Magic UI** — 2 waves de código + 1 UAT. Wave 1: tabela shadcn `<Table>` + wrapper `Components/ui/checkbox.jsx` + Blur Fade CSS-puro (`tailwindcss-animate`). Wave 2: actions dropdown "⋯" + Customize Columns com persistência `sugadores:col-visibility:v1`. framer-motion e drag handles CORTADOS no research (§3, §6). Delta bundle ≤ +4 kB no chunk EmpresaListagem. UAT prod aprovado em 2026-07-03.`
    4. **Commit:** `docs(55): fecha Phase 55 — UAT modernizacao visual aprovado em prod`
  </action>
  <verify>
    <automated>ls .planning/phases/55-modernizacao-visual-sugadores-magic-ui/PHASE-SUMMARY.md && grep -c "Phase 55" .planning/ROADMAP.md</automated>
  </verify>
  <done>PHASE-SUMMARY.md existe, TODO 270702 bloco C marcado concluído, ROADMAP.md registra Phase 55 concluída, commit `docs(55): ...` registrado.</done>
</task>

</tasks>

<verification>
1. UAT humano em prod concluído — 6 blocos executados; se algum bug, hotfix registrado
2. PHASE-SUMMARY.md criado com decisões, deltas de bundle e débitos
3. TODO 270702 bloco C marcado concluído; A + B + C todos fechados
4. ROADMAP.md marca Phase 55 como concluída
5. Commit `docs(55): fecha Phase 55 ...` presente no git log
</verification>

<success_criteria>
- [ ] Deploy aprovado explicitamente pelo operador (não autopilot)
- [ ] Deploy aplicado + cache clear + smoke page 200
- [ ] Checklist UAT com 6 blocos executado
- [ ] Nenhuma regressão crítica (bulk copy MLBs, click row, cronômetro, filtros, layout 2 colunas)
- [ ] Delta bundle EmpresaListagem ≤ +4 kB total; framer-motion não instalado; magicui/ não criado
- [ ] PHASE-SUMMARY.md com decisões e débitos abertos
- [ ] TODO 270702 Bloco C fechado
- [ ] ROADMAP.md registra Phase 55 concluída
- [ ] Commit `docs(55): fecha Phase 55 ...` presente
</success_criteria>

<output>
Phase 55 fechada. Se houver bugs do UAT, próximo passo é criar plan 55-04 focado no hotfix. Caso contrário, próximo item do backlog (Phase 51 já registrada — reestruturacao /grants com nova API ECF Drive).
</output>
