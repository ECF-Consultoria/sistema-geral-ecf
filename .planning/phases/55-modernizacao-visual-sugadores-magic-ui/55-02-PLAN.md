---
phase: 55-modernizacao-visual-sugadores-magic-ui
plan: "02"
type: execute
wave: 2
depends_on:
  - 55-01
autonomous: true
requirements:
  - REQ-55-02
files_modified:
  - resources/js/Pages/Sugadores/EmpresaListagem.jsx
must_haves:
  truths:
    - "Cada linha da tabela tem UM único botão trigger de dropdown (ícone `MoreHorizontal` do lucide-react) no lugar dos botões inline (`MLBs` continua acessível DENTRO do menu, não mais visível na linha)"
    - "Actions dropdown por linha usa `Components/ui/dropdown-menu.jsx` e expõe os itens: Copiar MLBs (disabled quando `!isElegivel`), Ver detalhes, Marcar em ação, Marcar resolvido, Ignorar"
    - "Handlers Copiar MLBs (`copiarMlbsLinha`), Ver detalhes (`router.visit(sugadores.show)`), e mudanças de status (via `router.patch(sugadores.update-status)`) funcionam a partir do dropdown"
    - "Trigger do dropdown `<TableCell>` mantém `onClick={e => e.stopPropagation()}` para não navegar ao abrir menu"
    - "Botão \"Customize Columns\" no topo da coluna esquerda (acima do filtro período OU alinhado com ele) abre DropdownMenu com `DropdownMenuCheckboxItem` para cada coluna opcional"
    - "Colunas opcionais toggláveis: `campaign_name`, `mlb_id`, `motivos`, `investimento`, `vendas` (default TODAS ocultas — evita quebrar layout atual e user opta explicitamente)"
    - "Colunas FIXAS (sem toggle): checkbox, produto, status, detectado, ações"
    - "Estado `colVis` persistido em `localStorage` com chave versionada `sugadores:col-visibility:v1` — leitura no init via `useState(() => …)` + escrita em `useEffect(() => …, [colVis])`"
    - "Após refresh (F5) a preferência de colunas é restaurada; usar chave inválida ou JSON corrompido cai no `DEFAULT_VIS` sem crash"
    - "Colunas opcionais NÃO renderizam `<TableHead>` nem `<TableCell>` quando `colVis[key] === false` (conditional render, não visibility:hidden — evita colunas fantasma no layout)"
    - "Bulk copy MLBs (barra sticky) + click row + cronômetro + filtros permanecem 100% funcionais"
    - "`npm run build` verde; chunk EmpresaListagem cresce no máximo +2 kB adicionais vs pós-55-01"
  artifacts:
    - path: "resources/js/Pages/Sugadores/EmpresaListagem.jsx"
      provides: "Actions dropdown por linha + botão Customize Columns + persistência localStorage; tudo em cima do baseline shadcn/checkbox de 55-01"
      contains: "MoreHorizontal; DropdownMenuCheckboxItem; sugadores:col-visibility:v1; localStorage.setItem"
  key_links:
    - from: "EmpresaListagem.jsx actions menu"
      to: "Components/ui/dropdown-menu.jsx (já usado em 20+ lugares — verificar grep)"
      via: "import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator }"
      pattern: "shadcn oficial — trigger é botão, content abre em Portal"
    - from: "EmpresaListagem.jsx column visibility"
      to: "window.localStorage (namespace `sugadores:col-visibility:v1`)"
      via: "useState(() => JSON.parse(localStorage.getItem(KEY)) ?? DEFAULT) + useEffect(() => localStorage.setItem(KEY, JSON.stringify(colVis)), [colVis])"
      pattern: "Research §2 (linhas 113-140) — pattern greenfield no projeto (0 matches existentes)"
    - from: "Actions dropdown items"
      to: "handlers existentes (copiarMlbsLinha, router.visit sugadores.show)"
      via: "onSelect={() => handler(s.id)} — não onClick, para Radix fechar menu antes de navegar"
      pattern: "shadcn DropdownMenuItem espera onSelect"
---

<objective>
Polish do redesign do `EmpresaListagem.jsx`. Ainda 100% UI, zero backend, mesmo arquivo da Wave 1. Três blocos coordenados:

1. **Actions dropdown por linha** — trigger `MoreHorizontal` do lucide-react abre menu com 5 itens: Copiar MLBs (disabled quando não elegível), Ver detalhes, Marcar em ação, Marcar resolvido, Ignorar. Substitui o botão inline "MLBs" da célula de ações (que a Wave 1 preservou). Preserva `stopPropagation` no `<TableCell>` wrapper.
2. **Customize Columns** — botão no topo da coluna esquerda abre um `DropdownMenu` com `DropdownMenuCheckboxItem` por coluna opcional. Estado `colVis` no componente + persistência em `localStorage` com chave versionada `sugadores:col-visibility:v1`.
3. **Colunas opcionais** — quando `colVis[key] === false`, o `<TableHead>` e a `<TableCell>` correspondente NÃO renderizam. Colunas opcionais: `campaign_name`, `mlb_id`, `motivos`, `investimento`, `vendas`. Default = todas ocultas (garante que layout Wave 1 continua idêntico até o operador escolher).

Sem TDD (UI puro). Verificação: `npm run build` verde + delta bundle.

**PRESERVAR 100%** do que a Wave 1 (55-01) entregou:
- Wrapper `<Table>` shadcn com overrides dark theme
- `<Checkbox>` quadrado (novo wrapper)
- Blur Fade no container principal
- Layout 2 colunas + sidebar sticky com ConfigResumoCard
- Filtro período (`aplicarPeriodo`) + `stopPropagation` nas células

**PRESERVAR 100%** de Phases anteriores:
- `sugadores.bulk-copy-mlbs` (Phase 52 A6) + `sugadores.mlbs-hint` (Phase 52 A5)
- Cronômetro Rodar análise (Phase 54-02)
- Click row inteiro navega para /sugadores/{id} (Phase 54-02 B2)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-CONTEXT.md
@.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-RESEARCH.md
@.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-01-SUMMARY.md
@resources/js/Pages/Sugadores/EmpresaListagem.jsx
@resources/js/Components/ui/dropdown-menu.jsx
@routes/web.php
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Actions dropdown por linha (MoreHorizontal + itens)</name>
  <files>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx (pós-Wave 1) — bloco tabela refatorado com wrapper shadcn
    - resources/js/Components/ui/dropdown-menu.jsx (linhas 26-45) — API de DropdownMenu + DropdownMenuItem
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-RESEARCH.md §4 (linhas 178-192) — ícones lucide-react recomendados
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-CONTEXT.md `<specifics>` linhas 105-114 — lista das 5 ações
    - routes/web.php — grep `sugadores.update-status` OU `sugadores.update` para confirmar nome da rota de mudança de status (usar existente, NÃO criar nova)
    - resources/js/Pages/Sugadores/Show.jsx — grep `route('sugadores.update-status'` para reusar o mesmo endpoint com o mesmo payload
  </read_first>
  <behavior>
    - Célula de ações de cada linha mostra apenas o botão `<MoreHorizontal>` (ícone 14 px, `text-white/60 hover:text-white`)
    - Ao clicar no botão, abre menu shadcn com 5 itens:
      1. **Copiar MLBs** — ícone `Copy`, disabled quando `!isElegivel`; ao selecionar chama `copiarMlbsLinha(s.id)`
      2. **Ver detalhes** — ícone `ExternalLink`, navega para `/sugadores/{s.id}`
      3. **Marcar em ação** — ícone `PlayCircle`, chama `mudarStatus(s.id, 'em_acao')`
      4. **Marcar resolvido** — ícone `CheckCircle2`, chama `mudarStatus(s.id, 'resolvido')`
      5. **Ignorar** — ícone `EyeOff`, chama `mudarStatus(s.id, 'ignorado')`
    - `mudarStatus(id, novoStatus)` faz `router.patch(route('sugadores.update-status', id), { status: novoStatus }, { preserveScroll: true })`
    - Menu fecha automaticamente após seleção (comportamento default Radix)
    - Trigger NÃO propaga click para o `<TableRow>` (que navega) — `stopPropagation` no `<TableCell>` wrapper
  </behavior>
  <action>
    1. **Confirmar rota de status:** grep em `routes/web.php` por `update-status` (deve existir dos módulos Phase 52-54). Se o nome for outro (`sugadores.update`, `sugadores.status`, `sugadores.patch`), usar o correto. Anotar em SUMMARY o nome final.
    2. **Imports:** adicionar em `EmpresaListagem.jsx` (junto do bloco lucide existente):
       ```jsx
       import {
           ArrowLeft, Building2, Tag, Megaphone, Copy, Check, Loader2,
           PlayCircle, Settings, SlidersHorizontal, AlertTriangle,
           MoreHorizontal, ExternalLink, CheckCircle2, EyeOff,
       } from 'lucide-react';
       ```
       E adicionar o dropdown-menu:
       ```jsx
       import {
           DropdownMenu, DropdownMenuTrigger, DropdownMenuContent,
           DropdownMenuItem, DropdownMenuSeparator, DropdownMenuLabel,
           DropdownMenuCheckboxItem,
       } from '@/Components/ui/dropdown-menu';
       ```
       (`DropdownMenuCheckboxItem` já é importado aqui porque a Tarefa 2 vai usar; evita segunda alteração de imports.)
    3. **Novo handler:** logo após `aplicarPeriodo` (linha ~236 pós-Wave 1), adicionar:
       ```jsx
       function mudarStatus(sugadorId, novoStatus) {
           router.patch(
               route('sugadores.update-status', sugadorId),
               { status: novoStatus },
               { preserveScroll: true, preserveState: false },
           );
       }
       ```
       Se o nome da rota confirmado no passo 1 for diferente, usar o correto aqui.
    4. **Substituir a célula de ações** (o `<TableCell onClick={e => e.stopPropagation()} className="py-3 text-right min-w-[80px]">` da Wave 1) pelo dropdown:
       ```jsx
       <TableCell onClick={e => e.stopPropagation()} className="py-3 text-right w-10">
           <DropdownMenu>
               <DropdownMenuTrigger asChild>
                   <button
                       type="button"
                       className="inline-flex items-center justify-center h-7 w-7 rounded-md text-white/60 hover:text-white hover:bg-white/[0.05] focus:outline-none focus-visible:ring-2 focus-visible:ring-ecf-yellow/40"
                       aria-label="Ações do sugador"
                   >
                       <MoreHorizontal size={14} />
                   </button>
               </DropdownMenuTrigger>
               <DropdownMenuContent align="end" className="bg-ecf-card border-white/[0.08] text-white/90 w-52">
                   <DropdownMenuLabel className="text-white/50 text-[10px] uppercase tracking-wider font-semibold">
                       Ações
                   </DropdownMenuLabel>
                   <DropdownMenuSeparator className="bg-white/[0.06]" />
                   <DropdownMenuItem
                       onSelect={() => copiarMlbsLinha(s.id)}
                       disabled={!isElegivel || copyingId === s.id}
                       className="gap-2 text-[13px] focus:bg-white/[0.05] focus:text-white"
                   >
                       {copyingId === s.id
                           ? <Loader2 size={13} className="animate-spin" />
                           : <Copy size={13} />}
                       Copiar MLBs
                       {fb?.ok && <span className="ml-auto text-emerald-300 text-[10px]">{fb.total}</span>}
                       {fb?.error && <span className="ml-auto text-red-300 text-[10px]">erro</span>}
                   </DropdownMenuItem>
                   <DropdownMenuItem
                       onSelect={() => router.visit(route('sugadores.show', s.id))}
                       className="gap-2 text-[13px] focus:bg-white/[0.05] focus:text-white"
                   >
                       <ExternalLink size={13} /> Ver detalhes
                   </DropdownMenuItem>
                   <DropdownMenuSeparator className="bg-white/[0.06]" />
                   <DropdownMenuItem
                       onSelect={() => mudarStatus(s.id, 'em_acao')}
                       className="gap-2 text-[13px] focus:bg-white/[0.05] focus:text-white"
                   >
                       <PlayCircle size={13} /> Marcar em ação
                   </DropdownMenuItem>
                   <DropdownMenuItem
                       onSelect={() => mudarStatus(s.id, 'resolvido')}
                       className="gap-2 text-[13px] focus:bg-white/[0.05] focus:text-white"
                   >
                       <CheckCircle2 size={13} /> Marcar resolvido
                   </DropdownMenuItem>
                   <DropdownMenuItem
                       onSelect={() => mudarStatus(s.id, 'ignorado')}
                       className="gap-2 text-[13px] focus:bg-white/[0.05] focus:text-white"
                   >
                       <EyeOff size={13} /> Ignorar
                   </DropdownMenuItem>
               </DropdownMenuContent>
           </DropdownMenu>
       </TableCell>
       ```
       Notas:
       - `asChild` no trigger repassa o handler onClick pro `<button>` interno — sem isso Radix renderiza um `<button>` extra e quebra estilo.
       - `align="end"` alinha o menu com a borda direita da célula.
       - `bg-ecf-card border-white/[0.08]` sobrescreve `bg-popover` (light) do wrapper compartilhado.
       - `focus:bg-white/[0.05] focus:text-white` sobrescreve `focus:bg-accent focus:text-accent-foreground` (light) do wrapper.
       - `mudarStatus` NÃO precisa de `stopPropagation` — o click já aconteceu no trigger, que está dentro do `<TableCell onClick={stopPropagation}>`.
    5. **Verificar feedback visual:** o feedback do copy (`fb?.ok` / `fb?.error`) que antes ficava no botão da linha agora aparece dentro do item do menu (ver código acima). Se o operador quiser um toast persistente futuro, deixar como débito no SUMMARY.
    6. Rodar `npm run build` — deve passar sem warning.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. Grep `MoreHorizontal` no arquivo: 1 match. Grep `DropdownMenuItem` no arquivo: 5 matches (5 ações). Grep `mudarStatus` no arquivo: ≥4 matches (definição + 3 chamadas dos status). Célula de ações não tem mais o botão MLBs inline; só o trigger `⋯`.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Customize Columns + persistência localStorage + render condicional</name>
  <files>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx (pós-Tarefa 1)
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-RESEARCH.md §2 (linhas 109-142) — pattern completo de estado + persistência
    - resources/js/Components/ui/dropdown-menu.jsx (linhas 38-46) — API DropdownMenuCheckboxItem (`checked`, `onCheckedChange`, `onSelect`)
  </read_first>
  <behavior>
    - Botão "Customize Columns" (ícone `SlidersHorizontal`, texto "Customizar colunas") aparece na coluna esquerda, na mesma linha do filtro de período (à direita do span de contagem, `ml-auto` alinha à direita)
    - Ao clicar, abre menu shadcn com 5 `DropdownMenuCheckboxItem`:
      - Campaign name
      - MLB ID
      - Motivos
      - Investimento
      - Vendas
    - Marcar/desmarcar item atualiza state `colVis` E dispara `useEffect` que escreve em `localStorage.setItem('sugadores:col-visibility:v1', JSON.stringify(colVis))`
    - `onSelect={e => e.preventDefault()}` no item — pattern Radix pra menu NÃO fechar ao marcar (usuário pode toggar várias em sequência)
    - Header da tabela renderiza `<TableHead>` para colunas fixas SEMPRE + condicional para opcionais quando `colVis[key] === true`
    - Body da tabela renderiza `<TableCell>` correspondentes com as mesmas condicionais — colunas ficam consistentes entre header/body
    - Após refresh F5: `useState` inicial lê `localStorage`, faz merge com `DEFAULT_VIS` (mantém default seguro se o JSON tiver chaves obsoletas)
    - Default = TODAS opcionais OCULTAS. Isso mantém o layout Wave 1 idêntico até user optar por mais colunas.
  </behavior>
  <action>
    1. **Constantes no topo do arquivo** (após bloco `// ─── Constantes de UI ───`, antes dos helpers `fmtBRL`):
       ```jsx
       // ─── Column visibility (Phase 55-02) ─────────────────────────────────
       // Chave versionada (:v1) permite invalidar cache em phases futuras
       // se o schema de colunas mudar sem quebrar UX do usuário.
       const COL_VIS_STORAGE_KEY = 'sugadores:col-visibility:v1';
       const DEFAULT_COL_VIS = {
           campaign:     false,
           mlb_id:       false,
           motivos:      false,
           investimento: false,
           vendas:       false,
       };
       const OPTIONAL_COLUMNS = [
           { key: 'campaign',     label: 'Campaign name' },
           { key: 'mlb_id',       label: 'MLB ID' },
           { key: 'motivos',      label: 'Motivos' },
           { key: 'investimento', label: 'Investimento' },
           { key: 'vendas',       label: 'Vendas' },
       ];
       ```
    2. **State + persistência dentro do componente** (adicionar junto dos outros `useState` do componente, tipicamente perto do `selectedIds`):
       ```jsx
       const [colVis, setColVis] = useState(() => {
           try {
               const raw = typeof window !== 'undefined'
                   ? window.localStorage.getItem(COL_VIS_STORAGE_KEY)
                   : null;
               return raw ? { ...DEFAULT_COL_VIS, ...JSON.parse(raw) } : DEFAULT_COL_VIS;
           } catch {
               return DEFAULT_COL_VIS;
           }
       });
       useEffect(() => {
           try {
               if (typeof window !== 'undefined') {
                   window.localStorage.setItem(COL_VIS_STORAGE_KEY, JSON.stringify(colVis));
               }
           } catch { /* localStorage cheio ou desabilitado — silencioso */ }
       }, [colVis]);
       ```
       Nota: SSR guard (`typeof window !== 'undefined'`) é defensivo — Inertia renderiza client-side, mas mantém padrão seguro.
    3. **Botão "Customize Columns"** no bloco do filtro de período (linhas ~390-411 pós-Wave 1 — o div `flex items-center gap-3 flex-wrap`). Reforma o wrapper:
       ```jsx
       <div className="flex items-center gap-3 flex-wrap">
           <label htmlFor="periodo-filter" className="text-white/50 text-xs uppercase tracking-wide">
               Período
           </label>
           <select
               id="periodo-filter"
               value={periodo}
               onChange={e => aplicarPeriodo(e.target.value)}
               className="appearance-none h-9 pl-3 pr-8 rounded-xl border border-white/[0.08] bg-white/[0.03] text-[13px] text-white/80 focus:outline-none focus:ring-1 focus:ring-ecf-yellow/40 focus:border-ecf-yellow/40 transition-all cursor-pointer"
           >
               {periodo_presets.map(p => (
                   <option key={p.value} value={p.value} className="bg-ecf-card">
                       {p.label}
                   </option>
               ))}
           </select>
           <span className="text-white/30 text-[11px]">
               {sugadores.length === 0
                   ? 'Nenhum sugador no período'
                   : `${fmtInt(sugadores.length)} no período`}
           </span>

           {/* Phase 55-02 — Customize Columns */}
           <div className="ml-auto">
               <DropdownMenu>
                   <DropdownMenuTrigger asChild>
                       <button
                           type="button"
                           className="inline-flex items-center gap-1.5 h-9 px-3 rounded-xl border border-white/[0.08] bg-white/[0.03] text-white/70 hover:text-white hover:bg-white/[0.05] text-[12px] font-medium focus:outline-none focus-visible:ring-2 focus-visible:ring-ecf-yellow/40"
                       >
                           <SlidersHorizontal size={12} />
                           Customizar colunas
                       </button>
                   </DropdownMenuTrigger>
                   <DropdownMenuContent align="end" className="bg-ecf-card border-white/[0.08] text-white/90 w-56">
                       <DropdownMenuLabel className="text-white/50 text-[10px] uppercase tracking-wider font-semibold">
                           Colunas opcionais
                       </DropdownMenuLabel>
                       <DropdownMenuSeparator className="bg-white/[0.06]" />
                       {OPTIONAL_COLUMNS.map(col => (
                           <DropdownMenuCheckboxItem
                               key={col.key}
                               checked={!!colVis[col.key]}
                               onCheckedChange={(val) => setColVis(prev => ({ ...prev, [col.key]: !!val }))}
                               onSelect={(e) => e.preventDefault()}
                               className="text-[13px] focus:bg-white/[0.05] focus:text-white"
                           >
                               {col.label}
                           </DropdownMenuCheckboxItem>
                       ))}
                   </DropdownMenuContent>
               </DropdownMenu>
           </div>
       </div>
       ```
       `ml-auto` empurra o botão pra direita da mesma linha do filtro de período.
    4. **Header condicional** — no `<TableHeader>` da tabela, adicionar `<TableHead>` opcionais entre Produto e Status:
       ```jsx
       <TableHead className="w-10 text-white/50 …">{/* checkbox — fixa */}</TableHead>
       <TableHead className="text-white/50 …">Produto</TableHead>
       {colVis.campaign && <TableHead className="text-white/50 …">Campaign name</TableHead>}
       {colVis.mlb_id   && <TableHead className="text-white/50 …">MLB ID</TableHead>}
       {colVis.motivos  && <TableHead className="text-white/50 …">Motivos</TableHead>}
       <TableHead className="text-white/50 …">Status</TableHead>
       <TableHead className="text-white/50 …">Detectado</TableHead>
       {colVis.investimento && <TableHead className="text-white/50 …">Investimento</TableHead>}
       {colVis.vendas       && <TableHead className="text-white/50 …">Vendas</TableHead>}
       <TableHead className="text-right text-white/50 …">Ações</TableHead>
       ```
       (Preservar as classes de override dark theme que a Wave 1 aplicou.)
    5. **Body condicional** — nas linhas do `map(s => ...)`, adicionar `<TableCell>` opcionais nas mesmas posições:
       ```jsx
       {/* checkbox (fixa) */}
       {/* produto (fixa) */}
       {colVis.campaign && (
           <TableCell className="py-3 text-white/70 text-xs truncate max-w-[180px]" title={s.campaign_name}>
               {s.campaign_name || '—'}
           </TableCell>
       )}
       {colVis.mlb_id && (
           <TableCell className="py-3 text-white/70 text-xs whitespace-nowrap">
               {s.mlb_id || '—'}
           </TableCell>
       )}
       {colVis.motivos && (
           <TableCell className="py-3 text-white/70 text-xs">
               {Array.isArray(s.motivos) && s.motivos.length > 0
                   ? s.motivos.slice(0, 2).join(', ') + (s.motivos.length > 2 ? '…' : '')
                   : '—'}
           </TableCell>
       )}
       {/* status (fixa) */}
       {/* detectado (fixa) */}
       {colVis.investimento && (
           <TableCell className="py-3 text-white/70 text-xs tabular-nums whitespace-nowrap">
               {fmtBRL(s.investimento_periodo)}
           </TableCell>
       )}
       {colVis.vendas && (
           <TableCell className="py-3 text-white/70 text-xs tabular-nums whitespace-nowrap">
               {fmtInt(s.vendas_periodo ?? 0)}
           </TableCell>
       )}
       {/* ações (fixa — dropdown MoreHorizontal) */}
       ```
       Se a prop enviada pelo backend usa nomes diferentes (`s.mlb_id` vs `s.mlbs`, `s.motivos` vs `s.criterios`), grep no `SugadorController` da Phase 54 pra confirmar antes de commitar. Se um campo não existir, cair no `'—'` (fallback já embutido).
    6. **Colspan do empty state** (se existir na Wave 1 dentro de `<TableBody>`) — se você adicionou um `<TableRow><TableCell colSpan={N}>` para "nenhum sugador", ajustar `N` dinamicamente:
       ```jsx
       const totalCols = 5 + Object.values(colVis).filter(Boolean).length;
       // usar <TableCell colSpan={totalCols}> no empty state
       ```
       (Só necessário se o empty state ficou dentro da `<Table>`. Na Wave 1 ele ficou FORA da tabela — se assim, ignorar.)
    7. Rodar `npm run build`. Testar mentalmente refresh: default = 5 colunas fixas visíveis. Marcar "Investimento" no menu → coluna aparece na 7ª posição. Refresh → coluna continua marcada (localStorage).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. Grep `sugadores:col-visibility:v1` no arquivo: 1 match. Grep `DropdownMenuCheckboxItem` no arquivo: ≥5 matches. Grep `OPTIONAL_COLUMNS` no arquivo: 2 matches (declaração + uso). Estrutura header/body preserva colunas fixas + adiciona opcionais quando `colVis[key]` é true.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Audit bundle + commit consolidado da wave</name>
  <files>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
    - public/build/manifest.json
  </files>
  <read_first>
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-01-SUMMARY.md — baseline pós-Wave 1 (tamanho do chunk)
  </read_first>
  <action>
    1. Rodar `npm run build` limpo.
    2. Capturar `ls -lh public/build/assets/EmpresaListagem-*.js` — comparar com o valor registrado em 55-01-SUMMARY. Delta aceitável: até +2 kB.
    3. Diff manual do arquivo (`git diff resources/js/Pages/Sugadores/EmpresaListagem.jsx`) — confirmar que:
       - Wave 1 preservada (wrapper `<Table>`, `<Checkbox>`, blur fade)
       - Actions dropdown adicionado com 5 itens
       - Botão "Customize Columns" adicionado
       - Estado `colVis` + `useEffect` de persistência presentes
       - Handlers antigos (`copiarMlbsLinha`, `copiarMlbsBulk`, `toggleOne`, `toggleAllVisible`, `clearSelection`, `rodarAnalise`, `aplicarPeriodo`) NÃO removidos
       - Handler novo `mudarStatus` adicionado
       - Bulk bar (linhas 413-454 originais) NÃO alterada — bulk copy continua acima da tabela
    4. Grep pós-edição para regressões:
       - `grep -n "type=\"checkbox\"" resources/js/Pages/Sugadores/EmpresaListagem.jsx` → 0 matches (Wave 1 já cobria)
       - `grep -n "MoreHorizontal|DropdownMenuCheckboxItem" resources/js/Pages/Sugadores/EmpresaListagem.jsx` → ≥2 matches
       - `grep -rn "framer-motion" package.json resources/js` → 0 matches
    5. Commit: `feat(55-02): actions dropdown + customize columns + localStorage v1`
       Incluir na mensagem: refs research §2 §4 e menção "sem framer-motion, sem @dnd-kit".
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. Delta bundle chunk EmpresaListagem ≤ +2 kB adicionais vs Wave 1. Commit `feat(55-02): ...` presente. Diff revisado sem regressão.</done>
</task>

</tasks>

<verification>
1. `npm run build` — 0 warnings JSX, bundle gerado
2. Grep em `resources/js/Pages/Sugadores/EmpresaListagem.jsx`:
   - `MoreHorizontal` → 1 match
   - `DropdownMenuItem` → ≥5 matches (5 ações do menu)
   - `DropdownMenuCheckboxItem` → ≥5 matches (5 colunas opcionais)
   - `sugadores:col-visibility:v1` → 1 match
   - `mudarStatus` → ≥4 matches
   - `type="checkbox"` → 0 matches (checkboxes nativos zero)
3. Grep em `package.json` e `resources/js/`: `framer-motion` → 0 matches
4. Manual: abrir DevTools > Application > Local Storage. Após toggle de coluna, chave `sugadores:col-visibility:v1` presente com JSON válido.
</verification>

<success_criteria>
- [ ] Actions dropdown por linha com 5 itens (Copiar MLBs, Ver detalhes, Marcar em ação, Marcar resolvido, Ignorar) usando `Components/ui/dropdown-menu.jsx`
- [ ] Trigger é o ícone `MoreHorizontal` do lucide-react
- [ ] `mudarStatus` chama a rota correta de update-status (nome confirmado em `routes/web.php`)
- [ ] Botão "Customize Columns" com `DropdownMenuCheckboxItem` para 5 colunas opcionais
- [ ] Estado `colVis` persistido em `localStorage` com chave versionada `sugadores:col-visibility:v1`
- [ ] Colunas opcionais renderizam condicionalmente (header + body) via `colVis[key] === true`
- [ ] Default de todas opcionais = false (layout Wave 1 preservado por padrão)
- [ ] Handlers antigos + Wave 1 (wrapper `<Table>`, `<Checkbox>`, blur fade, layout 2 colunas) totalmente preservados
- [ ] `stopPropagation` no `<TableCell>` do trigger dropdown mantido
- [ ] Delta bundle chunk EmpresaListagem ≤ +2 kB adicionais
- [ ] `npm run build` verde
</success_criteria>

<output>
Criar `.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-02-SUMMARY.md` documentando:
- Nome exato da rota de status confirmado em `routes/web.php`
- Delta bundle vs baseline pós-Wave 1
- Colunas opcionais escolhidas (5 keys) e default (todas false)
- Chave de persistência `sugadores:col-visibility:v1` (versionamento documentado)
- Débitos abertos: toast de feedback para status change (Radix não tem toast nativo — se operador quiser, phase futura); confirmação nativa via `confirm()` para "Ignorar"/"Resolvido" opcional (se operador reportar cliques acidentais)
</output>
