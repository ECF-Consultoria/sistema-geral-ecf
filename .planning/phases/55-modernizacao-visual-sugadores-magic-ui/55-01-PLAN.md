---
phase: 55-modernizacao-visual-sugadores-magic-ui
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-55-01
files_modified:
  - resources/js/Components/ui/checkbox.jsx
  - resources/js/Pages/Sugadores/EmpresaListagem.jsx
must_haves:
  truths:
    - "Novo wrapper `Components/ui/checkbox.jsx` existe (Radix `@radix-ui/react-checkbox` já em package.json:29) e exporta um componente `Checkbox` quadrado com estados data-[state=checked]:bg-ecf-yellow"
    - "EmpresaListagem.jsx substitui o `<table>` cru (linhas 469-571 atuais) por `<Table><TableHeader><TableRow><TableHead>…` do wrapper shadcn `Components/ui/table.jsx`"
    - "Overrides dark theme aplicados NO CONSUMIDOR (EmpresaListagem), não no wrapper compartilhado: header `border-white/[0.06] bg-white/[0.02]`, TableHead `text-white/50 font-medium text-xs uppercase tracking-wider`, TableRow body `border-white/[0.05] hover:bg-white/[0.03] transition-colors`, seleção `data-[state=selected]:bg-ecf-yellow/[0.03]`"
    - "Container principal do EmpresaListagem envolvido em `<div className=\"animate-in fade-in duration-300\">` (Blur Fade via `tailwindcss-animate` já instalado — 0 kb novos)"
    - "framer-motion NÃO é instalado nem importado em nenhum arquivo"
    - "Colunas atuais preservadas: checkbox, produto (nome + tipo/investimento), status, detectado, ações (botão MLBs)"
    - "Todos handlers preservados: `toggleOne`, `toggleAllVisible`, `clearSelection`, `copiarMlbsLinha`, `copiarMlbsBulk`, `rodarAnalise`, `aplicarPeriodo`"
    - "Click row inteiro navega para `/sugadores/{id}` (mantém pattern da Phase 54-02); `<TableCell>` do checkbox e das ações mantêm `onClick={e => e.stopPropagation()}`"
    - "`npm run build` verde; bundle do chunk `EmpresaListagem` cresce no máximo +2 kB vs baseline (~15 kB) — meta de research §7"
  artifacts:
    - path: "resources/js/Components/ui/checkbox.jsx"
      provides: "Wrapper shadcn oficial do Radix Checkbox, quadrado, dark-theme-friendly, com estado data-[state=checked] usando ecf-yellow"
      contains: "CheckboxPrimitive.Root; CheckboxPrimitive.Indicator; data-[state=checked]:bg-ecf-yellow"
    - path: "resources/js/Pages/Sugadores/EmpresaListagem.jsx"
      provides: "Tabela redesenhada com wrapper shadcn `<Table>`, checkbox quadrado, header dark suave, hover subtle, Blur Fade no container"
      contains: "TableHeader; TableRow; TableCell; animate-in fade-in duration-300; <Checkbox"
  key_links:
    - from: "resources/js/Components/ui/checkbox.jsx"
      to: "@radix-ui/react-checkbox (package.json:29)"
      via: "import * as CheckboxPrimitive from '@radix-ui/react-checkbox'"
      pattern: "shadcn oficial (research §1 código completo)"
    - from: "EmpresaListagem.jsx tabela"
      to: "Components/ui/table.jsx (wrapper compartilhado já usado por 9 páginas)"
      via: "import { Table, TableHeader, TableRow, TableHead, TableBody, TableCell } from '@/Components/ui/table'"
      pattern: "resources/js/Pages/Users/Index.jsx:154-225 (referência canônica limpa — research §1)"
    - from: "EmpresaListagem container"
      to: "tailwindcss-animate ^1.0.7 (package.json:24)"
      via: "className='animate-in fade-in duration-300' no wrapper mais externo do return"
      pattern: "Alternativa CSS puro para Blur Fade — research §3 recomendação #2"
---

<objective>
Base do redesign visual do drilldown `/sugadores/empresa/{id}`. Duas mudanças coordenadas + um componente novo compartilhado:

1. **Criar `Components/ui/checkbox.jsx`** — wrapper shadcn oficial do Radix Checkbox (~20 linhas). Zero deps novas (Radix já em package.json). Fica disponível pra qualquer futura tela do projeto.
2. **Refatorar tabela do `EmpresaListagem.jsx`** — substituir o `<table className="w-full">` cru (linhas 469-571 atuais) pelo wrapper shadcn `<Table>`. Aplicar overrides dark theme APENAS no consumidor (o wrapper compartilhado não muda). Trocar `<input type="checkbox">` nativo pelo novo `<Checkbox>` quadrado.
3. **Blur Fade no container principal** — apenas `animate-in fade-in duration-300` no wrapper mais externo do return (dentro do `<AppLayout>`). Zero kb de bundle novo, sem framer-motion.

Sem TDD (UI puro — sem business logic nova). Verificação: `npm run build` verde + comparação de bundle size. Polish (actions dropdown "⋯" e column visibility) fica na Wave 2 (55-02).

**PRESERVAR 100%** do comportamento entregue nas Phases 52/54:
- Endpoints `sugadores.bulk-copy-mlbs` (A6) e `sugadores.mlbs-hint` (A5)
- Filtro de período (`aplicarPeriodo`) e filtro de analista (herdado do Index)
- Cronômetro de "Rodar análise" (30s no ConfigResumoCard)
- Click row inteiro + `stopPropagation` no checkbox e nos botões (Phase 54-02 B2)
- Layout 2 colunas (grid-cols-4 col-span-3/col-span-1) com sidebar sticky (Phase 54-02 A1)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-CONTEXT.md
@.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-RESEARCH.md
@resources/js/Pages/Sugadores/EmpresaListagem.jsx
@resources/js/Components/ui/table.jsx
@resources/js/Pages/Users/Index.jsx
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar wrapper Components/ui/checkbox.jsx (Radix + shadcn oficial)</name>
  <files>
    - resources/js/Components/ui/checkbox.jsx
  </files>
  <read_first>
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-RESEARCH.md §1 (linhas 77-105) — código completo do wrapper com paleta ECF já embutida
    - resources/js/Components/ui/dropdown-menu.jsx (linhas 1-4) — pattern de import Radix + cn + lucide
    - package.json linha 29 — confirmar `@radix-ui/react-checkbox ^1.3.3` presente
  </read_first>
  <behavior>
    - `<Checkbox checked={boolean} onCheckedChange={fn} disabled={boolean} className="…" />` renderiza sem crash
    - Estado default: fundo `bg-white/[0.03]`, border `border-white/20`, quadrado (`h-4 w-4 rounded-sm`)
    - Estado checked: `bg-ecf-yellow`, texto/ícone preto, ícone `<Check>` do lucide-react
    - Estado disabled: `opacity-50 cursor-not-allowed`
    - Foco: ring `ecf-yellow/40`
    - Aceita `forwardRef` (não quebra composição em `<label>`)
  </behavior>
  <action>
    1. Criar novo arquivo `resources/js/Components/ui/checkbox.jsx` com o conteúdo EXATO do research §1 (linhas 81-105), adaptado ao padrão shadcn:
       ```jsx
       import * as React from 'react';
       import * as CheckboxPrimitive from '@radix-ui/react-checkbox';
       import { Check } from 'lucide-react';
       import { cn } from '@/lib/utils';

       const Checkbox = React.forwardRef(({ className, ...props }, ref) => (
           <CheckboxPrimitive.Root
               ref={ref}
               className={cn(
                   'peer h-4 w-4 shrink-0 rounded-sm border border-white/20 bg-white/[0.03]',
                   'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ecf-yellow/40',
                   'disabled:cursor-not-allowed disabled:opacity-50',
                   'data-[state=checked]:bg-ecf-yellow data-[state=checked]:text-black data-[state=checked]:border-ecf-yellow',
                   className,
               )}
               {...props}
           >
               <CheckboxPrimitive.Indicator className="flex items-center justify-center">
                   <Check className="h-3 w-3" strokeWidth={3} />
               </CheckboxPrimitive.Indicator>
           </CheckboxPrimitive.Root>
       ));
       Checkbox.displayName = 'Checkbox';

       export { Checkbox };
       ```
    2. NÃO alterar nenhum outro arquivo nesta tarefa — o consumo real fica na Tarefa 2.
    3. Confirmar visualmente que o arquivo tem exatamente 1 export nomeado (`Checkbox`) e nenhum export default.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Arquivo `resources/js/Components/ui/checkbox.jsx` existe com wrapper shadcn Radix; `npm run build` verde. Nada mais no repo mudou.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Refatorar tabela do EmpresaListagem.jsx para wrapper shadcn `<Table>`</name>
  <files>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx (arquivo completo, 593 linhas) — atenção às linhas 456-573 (bloco tabela) e linhas 173-186 (props do componente)
    - resources/js/Pages/Users/Index.jsx:152-227 — referência canônica de uso do wrapper shadcn `<Table>` no projeto
    - resources/js/Components/ui/table.jsx (46 linhas completas) — wrapper NÃO é modificado; overrides ficam no consumidor
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-RESEARCH.md §5 (linhas 196-210) — tabela de overrides dark theme por elemento
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-CONTEXT.md — bloco `<specifics>` linhas 85-99 (colunas visíveis por padrão)
  </read_first>
  <behavior>
    - Página renderiza sem crash com sugadores presentes ou lista vazia
    - Header dark suave (não mais o `bg-white/[0.02] border-white/[0.06]` cru do `<thead>` atual) — agora via TableHeader com override no consumidor
    - Linhas do body têm hover subtle (`hover:bg-white/[0.03]`) e transição de cor suave
    - Linha selecionada (checkbox marcado) mantém `data-state="selected"` + fundo `bg-ecf-yellow/[0.03]`
    - Checkboxes quadrados (não redondos nativos) — `<Checkbox>` do novo wrapper
    - `<Checkbox>` de "selecionar todos" no `<TableHead>` funciona com `toggleAllVisible` e mostra estado indeterminado ou marcado conforme `allElegiveisSelected`
    - Click em qualquer área não-checkbox da linha continua navegando para `/sugadores/{id}` (Phase 54-02 preserved)
    - `stopPropagation` nos `<TableCell>` do checkbox e das ações continua impedindo navegação ao interagir com controles
    - Botão "MLBs" por linha (adgroup) continua renderizando com todos os estados (loading, ok, error)
  </behavior>
  <action>
    1. **Imports:** No topo do arquivo (após linha 8 `import { cn } from '@/lib/utils'`), adicionar:
       ```jsx
       import {
           Table, TableHeader, TableBody, TableRow, TableHead, TableCell,
       } from '@/Components/ui/table';
       import { Checkbox } from '@/Components/ui/checkbox';
       ```
       Confirmar que os imports lucide existentes (`Copy`, `Check`, `Loader2`, `AlertTriangle`, `Tag`, `Megaphone`, `Building2`) continuam válidos — não remover nada.

    2. **Bloco tabela (linhas 468-572 do arquivo atual):** substituir o `<div className="card-ecf rounded-xl overflow-hidden">` + `<table>` + `<thead>` + `<tbody>` inteiros pelo padrão shadcn:
       ```jsx
       <div className="card-ecf rounded-xl overflow-hidden">
           <Table>
               <TableHeader className="border-white/[0.06] bg-white/[0.02]">
                   <TableRow className="hover:bg-transparent border-white/[0.06]">
                       <TableHead className="w-10 text-white/50 font-medium text-xs uppercase tracking-wider">
                           <Checkbox
                               checked={allElegiveisSelected}
                               onCheckedChange={toggleAllVisible}
                               disabled={elegiveisIds.length === 0}
                               aria-label="Selecionar todos os adgroups desta empresa"
                           />
                       </TableHead>
                       <TableHead className="text-white/50 font-medium text-xs uppercase tracking-wider">Produto</TableHead>
                       <TableHead className="text-white/50 font-medium text-xs uppercase tracking-wider">Status</TableHead>
                       <TableHead className="text-white/50 font-medium text-xs uppercase tracking-wider">Detectado</TableHead>
                       <TableHead className="text-right text-white/50 font-medium text-xs uppercase tracking-wider">Ações</TableHead>
                   </TableRow>
               </TableHeader>
               <TableBody>
                   {sugadores.map(s => {
                       const TipoIcon = TIPO_ICONS[s.tipo] || Tag;
                       const isSelected = selectedIds.has(s.id);
                       const isElegivel = s.tipo === 'adgroup';
                       const fb = copiedFeedback[s.id];
                       return (
                           <TableRow
                               key={s.id}
                               data-state={isSelected ? 'selected' : undefined}
                               onClick={() => router.visit(route('sugadores.show', s.id))}
                               className={cn(
                                   'border-white/[0.05] hover:bg-white/[0.03] cursor-pointer transition-colors',
                                   'data-[state=selected]:bg-ecf-yellow/[0.03]',
                               )}
                           >
                               <TableCell onClick={e => e.stopPropagation()} className="w-10 py-3">
                                   <Checkbox
                                       checked={isSelected}
                                       onCheckedChange={() => toggleOne(s.id)}
                                       disabled={!isElegivel}
                                       aria-label={isElegivel ? 'Selecionar sugador' : 'Sugadores de campanha não entram no bulk copy MLBs'}
                                   />
                               </TableCell>
                               <TableCell className="min-w-0 py-3">
                                   <div className="flex items-center gap-2 min-w-0">
                                       <TipoIcon size={12} className="text-white/40 shrink-0" />
                                       <span className="text-white/90 text-sm truncate" title={s.adgroup_name || s.campaign_name}>
                                           {s.adgroup_name || s.campaign_name || '—'}
                                       </span>
                                   </div>
                                   <p className="text-white/30 text-[11px] mt-0.5">
                                       {TIPO_LABELS[s.tipo] || s.tipo}
                                       {' · '}
                                       Invest. {fmtBRL(s.investimento_periodo)}
                                   </p>
                               </TableCell>
                               <TableCell className="py-3">
                                   <StatusBadge status={s.status} />
                               </TableCell>
                               <TableCell className="py-3 text-white/60 text-xs whitespace-nowrap">
                                   {fmtDate(s.reference_date)}
                                   {s.days_since_detected > 0 && (
                                       <span className="text-white/30 text-[10px] block">
                                           há {s.days_since_detected} dia{s.days_since_detected !== 1 ? 's' : ''}
                                       </span>
                                   )}
                               </TableCell>
                               <TableCell onClick={e => e.stopPropagation()} className="py-3 text-right min-w-[80px]">
                                   <div className="inline-flex items-center gap-1.5 justify-end flex-wrap">
                                       {isElegivel && (
                                           <button
                                               type="button"
                                               onClick={() => copiarMlbsLinha(s.id)}
                                               disabled={copyingId === s.id}
                                               className="inline-flex items-center gap-1.5 h-7 px-2 rounded-md border border-white/[0.08] bg-white/[0.03] text-white/70 hover:text-white hover:bg-white/[0.05] disabled:opacity-50 text-[11px]"
                                               title="Copiar MLBs deste adgroup"
                                           >
                                               {copyingId === s.id
                                                   ? <><Loader2 size={11} className="animate-spin" /> ...</>
                                                   : fb?.ok
                                                       ? <><Check size={11} className="text-emerald-300" /> {fb.total}</>
                                                       : fb?.error
                                                           ? <span className="text-red-300">{fb.error}</span>
                                                           : <><Copy size={11} /> MLBs</>}
                                           </button>
                                       )}
                                   </div>
                               </TableCell>
                           </TableRow>
                       );
                   })}
               </TableBody>
           </Table>
       </div>
       ```
       Notas importantes de override:
       - `hover:bg-transparent` no `<TableRow>` do `<TableHeader>` NEUTRALIZA o `hover:bg-muted/50` default do wrapper (que ficaria feio no header dark).
       - `data-state={isSelected ? 'selected' : undefined}` alimenta o override CSS `data-[state=selected]:bg-ecf-yellow/[0.03]` — segue a semântica shadcn.
       - Trocar `<input type="checkbox" onChange={...}>` por `<Checkbox onCheckedChange={...}>`: Radix usa `onCheckedChange`, NÃO `onChange`. Verificar as duas ocorrências (header + linha).
       - Painel de empty state (linhas 457-466 atuais — `sugadores.length === 0`) NÃO muda visualmente; preserva o `card-ecf` com `Building2` + textos.

    3. **NÃO tocar** em:
       - Header/breadcrumb (linhas 351-411)
       - Barra bulk (linhas 413-454)
       - Sidebar sticky com ConfigResumoCard (linhas 576-590)
       - Componentes locais `StatusBadge` e `ConfigResumoCard` (linhas 61-171)
       - Handlers e state hooks (linhas 187-350)

    4. Remover eventuais imports não usados após o refactor. Grep pós-edição: `grep -n "type=\"checkbox\"" resources/js/Pages/Sugadores/EmpresaListagem.jsx` → deve retornar ZERO matches (checkbox nativo foi 100% substituído).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. Grep `type="checkbox"` no arquivo retorna 0 matches. Grep `TableHeader|TableRow|TableCell` retorna ≥5 matches. Grep `<Checkbox` retorna 2 matches (header + linha). Comportamento visual: header dark suave, linhas com hover subtle, checkboxes quadrados amarelos quando marcados.</done>
</task>

<task type="auto">
  <name>Tarefa 3: Blur Fade no container principal + build final com auditoria de bundle</name>
  <files>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx
  </files>
  <read_first>
    - resources/js/Pages/Sugadores/EmpresaListagem.jsx (versão pós-Tarefa 2) — linha ~351 (`return (<AppLayout ...>`) até o fechamento
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-RESEARCH.md §3 (linhas 164-176) — recomendação de CSS puro via `tailwindcss-animate`
    - .planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-RESEARCH.md §7 (linhas 224-247) — baseline de bundle (chunk EmpresaListagem = 15 kB)
    - package.json linha 24 — confirmar `tailwindcss-animate ^1.0.7` presente
  </read_first>
  <behavior>
    - Ao carregar a página, o conteúdo do `<AppLayout>` faz fade-in suave em 300 ms (opacity 0 → 1)
    - Nenhuma dep nova em `package.json`; grep `framer-motion` no repo continua retornando 0 matches
    - Chunk `EmpresaListagem-*.js` cresce no MÁXIMO +2 kB vs baseline (research §7)
    - `npm run build` verde sem warning JSX
  </behavior>
  <action>
    1. Localizar o wrapper mais externo dentro do `<AppLayout>` (após linha 352). Estrutura atual:
       ```jsx
       <AppLayout title={`Sugadores · ${company?.name ?? ''}`}>
           <div className="mb-4 flex items-center gap-2 text-xs text-white/40">
               {/* breadcrumb */}
           </div>
           <div className="grid grid-cols-1 lg:grid-cols-4 gap-4">
               {/* col-span-3 + aside */}
           </div>
       </AppLayout>
       ```
    2. Envolver TODO o conteúdo entre `<AppLayout>` e `</AppLayout>` em um único `<div className="animate-in fade-in duration-300">`:
       ```jsx
       <AppLayout title={`Sugadores · ${company?.name ?? ''}`}>
           <div className="animate-in fade-in duration-300">
               <div className="mb-4 flex items-center gap-2 text-xs text-white/40">
                   {/* breadcrumb — inalterado */}
               </div>
               <div className="grid grid-cols-1 lg:grid-cols-4 gap-4">
                   {/* col-span-3 + aside — inalterado */}
               </div>
           </div>
       </AppLayout>
       ```
    3. NÃO adicionar nenhum outro `animate-in` — só o container mais externo. Adicionar em sub-elementos gera efeito redundante e não pedido no CONTEXT.
    4. NÃO instalar framer-motion. NÃO criar `resources/js/Components/magicui/` — cortado no research §3.
    5. **Auditoria de bundle:**
       - Antes: capturar `ls -lh public/build/assets/EmpresaListagem-*.js` (baseline research diz ~15 kB).
       - Rodar `npm run build`.
       - Depois: capturar novo `ls -lh public/build/assets/EmpresaListagem-*.js`.
       - Diff aceitável: até +2 kB. Se ficar acima de +3 kB, revisar se sobrou algum import não tree-shaken (raro; abrir issue).
    6. Commit consolidado desta wave: `feat(55-01): tabela shadcn + checkbox quadrado + blur fade no EmpresaListagem`
       Incluir na mensagem: refs research §1 §3 §5 §7 e menção "sem framer-motion".
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build verde. Grep `framer-motion` em `package.json` e `resources/js/`: 0 matches. Grep `animate-in fade-in duration-300` em EmpresaListagem.jsx: 1 match no container mais externo. Delta do chunk EmpresaListagem ≤ +2 kB vs baseline. Commit `feat(55-01): ...` presente.</done>
</task>

</tasks>

<verification>
1. `npm run build` — 0 warnings JSX, bundle gerado
2. `ls resources/js/Components/ui/checkbox.jsx` → arquivo existe (~22 linhas)
3. Grep manual em `resources/js/Pages/Sugadores/EmpresaListagem.jsx`:
   - `type="checkbox"` → 0 matches (100% substituídos por `<Checkbox>`)
   - `TableHeader|TableRow|TableCell|TableHead` → ≥5 matches
   - `animate-in fade-in duration-300` → 1 match no container mais externo
4. Grep em `package.json` e `resources/js/`: `framer-motion` → 0 matches
5. Delta do chunk `EmpresaListagem-*.js` em `public/build/assets/` ≤ +2 kB
</verification>

<success_criteria>
- [ ] `resources/js/Components/ui/checkbox.jsx` criado seguindo o padrão shadcn oficial (Radix wrapper com CVA-like variants inline via cn())
- [ ] Wrapper compartilhado `Components/ui/table.jsx` NÃO alterado (overrides ficam no consumidor)
- [ ] EmpresaListagem.jsx usa `<Table><TableHeader>…</Table>` com dark theme overrides no consumidor
- [ ] Checkboxes nativos 100% substituídos por `<Checkbox>` do novo wrapper (header + linha)
- [ ] Blur Fade via `animate-in fade-in duration-300` no container principal (0 kb novos)
- [ ] framer-motion NÃO instalado, `resources/js/Components/magicui/` NÃO criado
- [ ] Todos handlers (`toggleOne`, `toggleAllVisible`, `copiarMlbsLinha`, `copiarMlbsBulk`, `clearSelection`, `rodarAnalise`, `aplicarPeriodo`) preservados
- [ ] Click row + `stopPropagation` no checkbox e nas ações mantidos (Phase 54-02)
- [ ] Delta do chunk EmpresaListagem ≤ +2 kB
- [ ] `npm run build` verde
</success_criteria>

<output>
Criar `.planning/phases/55-modernizacao-visual-sugadores-magic-ui/55-01-SUMMARY.md` documentando:
- Wrapper `Checkbox` criado (arquivo + exports)
- Overrides dark theme aplicados por elemento (mesa da research §5 com valores usados)
- Baseline vs pós-refactor do chunk EmpresaListagem (medido via `ls -lh public/build/assets/`)
- Confirmação: framer-motion continua ausente
- Débitos abertos: actions dropdown "⋯" + column visibility ficam na Wave 2 (55-02)
</output>
