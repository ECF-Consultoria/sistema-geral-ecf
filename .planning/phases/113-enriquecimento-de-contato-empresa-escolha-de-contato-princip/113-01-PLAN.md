---
phase: 113-enriquecimento-de-contato-empresa-escolha-de-contato-princip
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/Services/Hubspot/HubspotContactSelector.php
  - app/Services/Hubspot/HubspotNameNormalizer.php
  - tests/Unit/Phase113ContactSelectorTest.php
  - tests/Unit/Phase113NameNormalizerTest.php
autonomous: true
requirements: [HUB-CONTATO-01, HUB-DEDUP-02]
must_haves:
  truths:
    - "Dado uma lista de contatos, o sistema escolhe SEMPRE o mesmo contato principal (determinístico)"
    - "Contato com email E telefone vence contato só com email"
    - "mobilephone conta como telefone na priorização"
    - "Dois nomes de empresas diferentes NUNCA normalizam para a mesma string (sem falso positivo)"
    - "Nomes iguais a menos de acento/pontuação/caixa/espaços normalizam para a mesma string"
  artifacts:
    - path: "app/Services/Hubspot/HubspotContactSelector.php"
      provides: "Regra determinística de contato principal (unidade pura, sem I/O)"
      contains: "class HubspotContactSelector"
      min_lines: 30
    - path: "app/Services/Hubspot/HubspotNameNormalizer.php"
      provides: "Normalização de nome de empresa para dedup fraco"
      contains: "class HubspotNameNormalizer"
      min_lines: 15
    - path: "tests/Unit/Phase113ContactSelectorTest.php"
      provides: "Cobertura de prioridade + empate determinístico"
    - path: "tests/Unit/Phase113NameNormalizerTest.php"
      provides: "Cobertura de equivalência + proteção contra falso positivo"
  key_links:
    - from: "HubspotContactSelector::selecionar"
      to: "consumidores (controller na 113-02)"
      via: "retorno do contato principal (array com chave id) ou null"
      pattern: "selecionar"
---

<objective>
Criar as DUAS unidades puras e testáveis do enriquecimento HubSpot, isoladas de I/O:
1. `HubspotContactSelector` — escolhe o contato principal de forma determinística.
2. `HubspotNameNormalizer` — normaliza nome de empresa para o match fraco de dedup.

Purpose: seleção de contato e normalização de nome são lógica pura com entrada/saída bem definidas — o candidato perfeito para TDD (RED→GREEN). Isolá-las agora permite que 113-02 (controller/handoff) e 113-03 (dedup) consumam contratos já provados, sem re-testar regra dentro de fluxo E2E pesado.
Output: 2 classes em `app/Services/Hubspot/` + 2 suites unitárias verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md
@.planning/STATE.md

<interfaces>
<!-- Contratos que os consumidores (113-02 controller, 113-03 dedup) vão usar. -->
<!-- Chaves LÓGICAS já normalizadas — o selector NÃO conhece nomes de property HubSpot. -->

HubspotContactSelector::selecionar(array $contatos): ?array
  Entrada: lista de contatos. Cada item é um array associativo com (todas opcionais exceto id):
    'id' (string|int), 'firstname', 'lastname', 'email', 'phone', 'mobilephone', 'jobtitle'
  Saída: o contato escolhido (o MESMO array de entrada) ou null se a lista for vazia.

HubspotNameNormalizer::normalizar(?string $nome): string
  Saída: string minúscula, sem acento, sem pontuação, espaços colapsados, trim. '' quando entrada vazia/null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: HubspotContactSelector (RED→GREEN) — regra determinística de contato principal</name>
  <files>app/Services/Hubspot/HubspotContactSelector.php, tests/Unit/Phase113ContactSelectorTest.php</files>
  <read_first>
    - .planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md (bloco "Contato principal (HUB-CONTATO-01)" — regra de prioridade)
    - app/Services/Hubspot/HubspotDealHandoffService.php (padrão de classe fina em App\Services\Hubspot; comentários pt-BR)
  </read_first>
  <behavior>
    - Lista vazia → retorna null.
    - Um único contato → retorna esse contato (independente de ter email/telefone).
    - Entre 3 contatos, um com email E (phone OU mobilephone) → vence os demais.
    - mobilephone conta como telefone: contato com email + mobilephone vence contato só com email.
    - Só emails disponíveis → escolhe entre os que têm email; empate resolvido pelo MENOR id (comparação numérica quando ambos numéricos, senão string).
    - Nenhum contato tem email nem telefone → escolhe o de MENOR id (determinístico).
    - Determinismo: chamar duas vezes com a mesma lista embaralhada retorna o mesmo id.
  </behavior>
  <action>
    Primeiro escreva Phase113ContactSelectorTest cobrindo cada caso do bloco behavior (um método de teste por caso, nomes pt-BR descritivos). Rode e confirme que falha (classe ainda não existe) — commit RED.
    Depois implemente HubspotContactSelector::selecionar como método público estático (ou de instância — discrição). Atribua a cada contato um "tier" de prioridade: email+telefone=3, email=2, telefone=1, nenhum=0, onde "tem email" = email não-vazio após trim e "tem telefone" = phone OU mobilephone não-vazio após trim. Selecione o maior tier; em empate de tier, escolha o menor id (cast para int quando ambos numéricos, senão comparação de string). Retorne o array do contato vencedor, ou null se a lista estiver vazia. Comentários em pt-BR explicando a regra de tier e o desempate. NÃO acesse banco, config, nem HTTP — unidade 100% pura.
    A "label útil de association" (Decision maker/Billing) é best-effort futuro: NÃO implementar agora; deixe um comentário pt-BR marcando o ponto de extensão. NOTA DE DISCRIÇÃO DOCUMENTADA (registrar no SUMMARY): a decisão (5) do CONTEXT diz "fallback: primeiro contato retornado", mas usamos "menor id" como desempate em TODOS os tiers — mais determinístico, porque a API HubSpot não garante ordem estável de retorno. O executor deve registrar essa escolha no SUMMARY como refinamento consciente da regra do CONTEXT (não é divergência de intenção — é o mesmo objetivo com garantia de determinismo). Rode os testes e confirme verde — commit GREEN.
  </action>
  <verify>
    <automated>php artisan test tests/Unit/Phase113ContactSelectorTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test tests/Unit/Phase113ContactSelectorTest.php` verde com todos os casos do behavior.
    - Entre 3 contatos, `selecionar` retorna o que tem email+telefone; entre só-emails retorna o de menor id; lista vazia retorna null.
    - Nenhum acesso a DB/config/HTTP na classe (grep não encontra `Company`, `config(`, `Http`, `DB` em HubspotContactSelector.php).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: HubspotNameNormalizer (RED→GREEN) — normalização anti-falso-positivo</name>
  <files>app/Services/Hubspot/HubspotNameNormalizer.php, tests/Unit/Phase113NameNormalizerTest.php</files>
  <read_first>
    - .planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md (bloco "Normalização de nome" em Dedup — "Testar contra falso positivo")
  </read_first>
  <behavior>
    - "Açaí & Cia. Ltda" e "acai   e   cia ltda" — atenção: pontuação e "&" viram espaço; após colapsar, ambos devem produzir a MESMA string quando os tokens coincidem; validar com um par realmente equivalente (ex.: "AÇAÍ E CIA" vs "acai e cia" → iguais).
    - Falso positivo bloqueado: "Padaria do Zé" e "Padaria da Ana" normalizam para strings DIFERENTES.
    - Falso positivo bloqueado: "Silva Ltda" e "Silva Ltda ME" normalizam para strings DIFERENTES (token extra preserva distinção).
    - Caixa/acento/espaço: "  São   João  " → "sao joao".
    - Entrada null ou "" → "".
  </behavior>
  <action>
    Escreva Phase113NameNormalizerTest primeiro (casos do behavior, incluindo os dois de falso positivo) — rode e confirme RED, commit.
    Implemente HubspotNameNormalizer::normalizar(?string): string — método público estático. Passos: (1) trata null/'' → ''; (2) minúsculas com mb_strtolower; (3) remove acentos (usar iconv 'UTF-8' → 'ASCII//TRANSLIT' com fallback, ou tabela de substituição manual — escolha o que for robusto no ambiente sem locale); (4) substitui tudo que não for [a-z0-9] por espaço; (5) colapsa espaços múltiplos em um; (6) trim. Comentários pt-BR. Sem I/O. Rode e confirme GREEN, commit.
    Importante: a normalização é AGRESSIVA só o suficiente para caixa/acento/pontuação — NÃO remova tokens (não descartar "ltda", "me", "sa"), para não colapsar empresas distintas. Isso é o que protege contra falso positivo no match fraco da 113-03.
  </action>
  <verify>
    <automated>php artisan test tests/Unit/Phase113NameNormalizerTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test tests/Unit/Phase113NameNormalizerTest.php` verde.
    - "Padaria do Zé" ≠ "Padaria da Ana" e "Silva Ltda" ≠ "Silva Ltda ME" após normalizar (asserções presentes e passando).
    - null/'' retornam ''.
    - Nenhum acesso a DB/config/HTTP.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot → seleção de contato | Dados de contato vêm de origem externa; a seleção deve ser total (nunca lançar por campo ausente/vazio) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Tampering | HubspotContactSelector com contatos malformados | mitigate | Tratar campos ausentes/vazios com trim + coalesce; nunca lançar; lista vazia → null |
| T-113-02 | Tampering | Falso positivo de nome colapsando empresas distintas | mitigate | Normalização conservadora (só caixa/acento/pontuação), sem remoção de tokens; teste anti-falso-positivo obrigatório |
| T-113-SC | Tampering | npm/composer installs | accept | Fase sem instalação de pacotes — nenhuma dependência nova |
</threat_model>

<verification>
- `php artisan test tests/Unit/Phase113ContactSelectorTest.php tests/Unit/Phase113NameNormalizerTest.php` verde.
- Ambas as classes são puras: `grep -nE 'config\(|Http::|DB::|Company' app/Services/Hubspot/HubspotContactSelector.php app/Services/Hubspot/HubspotNameNormalizer.php` retorna vazio.
</verification>

<success_criteria>
- HUB-CONTATO-01 (parte pura): regra de contato principal isolada, determinística e testada.
- HUB-DEDUP-02 (building block): normalização de nome testada contra falso positivo.
- Duas suites unitárias verdes; nenhuma regra de negócio embutida em fluxo E2E.
</success_criteria>

<output>
Criar `.planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-01-SUMMARY.md` ao concluir.
</output>
