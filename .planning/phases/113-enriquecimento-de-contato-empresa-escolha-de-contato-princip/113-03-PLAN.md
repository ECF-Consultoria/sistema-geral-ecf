---
phase: 113-enriquecimento-de-contato-empresa-escolha-de-contato-princip
plan: 03
type: execute
wave: 3
depends_on: [113-01, 113-02]
files_modified:
  - app/Services/Hubspot/HubspotCompanyMatcher.php
  - app/Http/Controllers/Api/HubspotWebhookController.php
  - tests/Feature/Phase113HubspotDedupTest.php
autonomous: true
requirements: [HUB-DEDUP-01, HUB-DEDUP-02]
must_haves:
  truths:
    - "Antes de criar Company, o webhook procura empresa existente (hubspot_company_id → cnpj → email → domain → nome normalizado)"
    - "Match forte (hubspot_company_id OU cnpj) NÃO duplica: enriquece só campos vazios, nunca sobrescreve preenchido manualmente"
    - "Match forte adiciona contrato novo só se não existir ContratoServico com o mesmo hubspot_line_item_id"
    - "Match fraco (só nome normalizado) NÃO faz merge de campos críticos: grava warning possivel_duplicidade no hubspot_eventos.payload e segue criando a empresa"
    - "DB fresco (nenhuma empresa existente) mantém o caminho de criação intacto — regressão zero"
  artifacts:
    - path: "app/Services/Hubspot/HubspotCompanyMatcher.php"
      provides: "resolução de empresa existente + classificação forte/fraco"
      contains: "class HubspotCompanyMatcher"
      min_lines: 40
    - path: "app/Http/Controllers/Api/HubspotWebhookController.php"
      provides: "dedup antes do create + enriquecimento de campos vazios + guard de contrato + warning fraco"
      contains: "possivel_duplicidade"
    - path: "tests/Feature/Phase113HubspotDedupTest.php"
      provides: "cobertura E2E de match forte (cnpj/hubspot_company_id) e fraco (nome)"
  key_links:
    - from: "HubspotWebhookController::criarEmpresa"
      to: "HubspotCompanyMatcher::encontrar"
      via: "resolução de empresa existente antes de Company::create"
      pattern: "encontrar"
    - from: "match fraco"
      to: "hubspot_eventos.payload"
      via: "warning possivel_duplicidade"
      pattern: "possivel_duplicidade"
---

<objective>
Adicionar dedup de empresa ao webhook: antes de criar a Company, resolver se já existe empresa correspondente. Match forte (hubspot_company_id ou cnpj) enriquece campos VAZIOS sem duplicar e adiciona contrato guardando hubspot_line_item_id; match fraco (só nome normalizado) grava warning `possivel_duplicidade` no payload do evento e segue criando a empresa (o humano decide depois na Fase 114). Extrair a resolução para `HubspotCompanyMatcher`, consumindo `HubspotNameNormalizer` da 113-01.

Purpose: hoje todo webhook cria uma Company nova — reprocessar ou receber um deal de cliente já existente duplica empresa/contrato. Esta entrega fecha o loop de dedup mantendo o handoff (nunca perde o deal) e sem merge agressivo em caso ambíguo.
Output: HubspotCompanyMatcher + integração no criarEmpresa + suite Phase113HubspotDedupTest; regressão zero.

ATENÇÃO — INVARIANTE DE NÃO-REGRESSÃO (lei desta fase): dedup só ALTERA comportamento quando JÁ EXISTE empresa correspondente no banco. Nos testes atuais (Phase34/35/112, DB fresco) nenhuma existe → o caminho segue criando exatamente como na 113-02. Essas suites DEVEM ficar verdes SEM alterar asserções.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md
@app/Http/Controllers/Api/HubspotWebhookController.php

<interfaces>
<!-- Da 113-01 (wave 1) — consumir na normalização de nome: -->
HubspotNameNormalizer::normalizar(?string $nome): string

<!-- Company: colunas de match já existem (111): hubspot_company_id, cnpj, email_cliente, hubspot_domain, name.
     Campos estruturados gravados pela 113-02: nome_contato, cargo_contato, hubspot_deal_id,
     hubspot_contact_id, hubspot_domain, hubspot_observacao, hubspot_snapshot. -->

<!-- ContratoServico: coluna hubspot_line_item_id existe (111) — usar no guard anti-duplicidade. -->

<!-- persistirContratos(Company, HubspotHandoffData, ?HubspotEvento): array — já existente (112);
     reusar no caminho de enriquecimento, com guard de line item. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: HubspotCompanyMatcher (RED→GREEN) — resolução + classificação forte/fraco</name>
  <files>app/Services/Hubspot/HubspotCompanyMatcher.php, tests/Feature/Phase113HubspotDedupTest.php</files>
  <read_first>
    - .planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md (bloco "Dedup (HUB-DEDUP-01 / HUB-DEDUP-02)" — ordem + classificação)
    - app/Services/Hubspot/HubspotNameNormalizer.php (113-01)
    - app/Models/Company.php (colunas de match; scopes/whereIn conforme convenção)
  </read_first>
  <behavior>
    - Nenhum critério bate → retorna sem empresa (match null).
    - Empresa com mesmo hubspot_company_id → match 'forte', via 'hubspot_company_id'.
    - Empresa com mesmo cnpj (normalizado só-dígitos) → match 'forte', via 'cnpj'.
    - Empresa só com mesmo email_cliente → match 'fraco', via 'email'.
    - Empresa só com mesmo hubspot_domain/domain → match 'fraco', via 'domain'.
    - Empresa só com mesmo nome normalizado (HubspotNameNormalizer) → match 'fraco', via 'nome'.
    - Ordem de precedência respeitada: hubspot_company_id > cnpj > email > domain > nome (primeiro hit vence).
  </behavior>
  <action>
    Crie HubspotCompanyMatcher em App\Services\Hubspot. Método público `encontrar(array $criterios): array` recebendo um array com hubspot_company_id, cnpj, email, domain, name (quaisquer podem ser null/''). Retorne um array associativo: `['company' => ?Company, 'match' => 'forte'|'fraco'|null, 'via' => string|null]`. Implemente a busca na ordem: (1) hubspot_company_id → forte; (2) cnpj (comparar por dígitos: strip não-numéricos dos dois lados) → forte; (3) email_cliente → fraco; (4) hubspot_domain → fraco; (5) nome normalizado via HubspotNameNormalizer comparado contra o name normalizado das candidatas → fraco. Pare no primeiro hit. Ignore critérios vazios (não faça where com valor vazio, para não casar empresas sem cnpj/email entre si). Use Eloquent com whereIn/where conforme convenção pt-BR; para o match por nome normalizado, compare em PHP (buscar candidatas por um filtro barato ou iterar — discrição; documente a escolha). Comentários pt-BR.
    Escreva os testes desta classe DENTRO de Phase113HubspotDedupTest (unit-style, com RefreshDatabase, criando Companies via factory/Company::create) cobrindo cada caso do behavior + a precedência. RED antes de implementar, GREEN depois. Inclua o caso anti-falso-positivo: duas empresas de nomes distintos NÃO casam por nome.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase113HubspotDedupTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `encontrar` classifica hubspot_company_id/cnpj como 'forte' e email/domain/nome como 'fraco', respeitando a precedência.
    - CNPJ compara por dígitos (formatação diferente ainda casa).
    - Critérios vazios não geram match espúrio.
    - Nomes distintos não casam por nome (usa HubspotNameNormalizer).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarefa 2: integrar dedup no criarEmpresa — match forte enriquece (HUB-DEDUP-01)</name>
  <files>app/Http/Controllers/Api/HubspotWebhookController.php</files>
  <read_first>
    - app/Http/Controllers/Api/HubspotWebhookController.php (criarEmpresa após 113-02; persistirContratos ~359-445 — guard hubspot_line_item_id)
    - app/Services/Hubspot/HubspotCompanyMatcher.php (tarefa 1)
    - .planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md (bloco "Match forte")
  </read_first>
  <action>
    No início do `criarEmpresa` (dentro da transaction, ANTES de Company::create), monte os critérios (hubspot_company_id, cnpj, email, domain, name a partir do deal/company/contato principal) e chame `HubspotCompanyMatcher::encontrar()`.
    Caminho MATCH FORTE: NÃO crie Company nova. Use a empresa existente e enriqueça APENAS colunas vazias (null/'' na empresa) com os dados do HubSpot — nunca sobrescreva valor já preenchido (dado manual do Comercial é soberano). Campos elegíveis a enriquecimento: cnpj, email_cliente, telefone, nome_contato, cargo_contato, nicho, dor, faturamento_mensal, vende_ml, hubspot_deal_id, hubspot_company_id, hubspot_contact_id, hubspot_domain, hubspot_observacao. Sobre o `hubspot_snapshot` na empresa existente: REESCREVA-O integralmente com o payload novo (deal + company + todos os contatos + primary_contact_id + line_items + warnings + captured_at — mesmo shape montado na 113-02), NÃO faça deep-merge do snapshot antigo — o snapshot é sempre o retrato do último evento processado, e o histórico anterior fica preservado no `hubspot_eventos.payload` do evento anterior. Esse comportamento de reescrita é a regra desta fase (documente no SUMMARY). NÃO remarque empresa_nova (empresa já existia). Delegue contratos ao handoff via persistirContratos, mas adicione um guard: pular contrato cujo hubspot_line_item_id já exista em ContratoServico dessa company (e, no fluxo legado sem line item, pular se já houver contrato ativo do mesmo servico_id) — sem duplicar contrato. Faça o roteamento MlbEmpresa só para serviços realmente criados (o guard de MlbEmpresa por company já existe em rotearImplementacao).
    Caminho SEM MATCH (null): comportamento idêntico à 113-02 (cria Company nova com campos estruturados + snapshot). O caminho fraco é a tarefa 3.
    Comentários pt-BR marcando "Fase 113 — dedup forte".
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase34HubspotWebhookTest.php tests/Feature/Phase112HubspotHandoffWebhookTest.php</automated>
  </verify>
  <acceptance_criteria>
    - Empresa existente por cnpj: após webhook, `Company::count()` permanece 1 (não duplica) e campos vazios são preenchidos.
    - Campo já preenchido manualmente na empresa existente NÃO é sobrescrito.
    - Empresa existente por hubspot_company_id recebe contrato novo apenas se hubspot_line_item_id inédito (sem duplicar contrato).
    - Após enriquecimento de empresa EXISTENTE, `companies.hubspot_snapshot` é reescrito com o shape completo (deal/company/contacts/primary_contact_id/line_items/warnings/captured_at) do evento novo — validado por teste.
    - DB fresco: Phase34 e Phase112 verdes SEM alterar asserções (segue criando).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: match fraco = warning possivel_duplicidade (HUB-DEDUP-02) + E2E dedup</name>
  <files>app/Http/Controllers/Api/HubspotWebhookController.php, tests/Feature/Phase113HubspotDedupTest.php</files>
  <read_first>
    - app/Http/Controllers/Api/HubspotWebhookController.php (criarEmpresa; persistirContratos grava payload de warnings ~431-442 — mesmo padrão para o warning fraco)
    - tests/Feature/Phase35HubspotV2Test.php (padrão Http::fake/disparaWebhook para o E2E)
    - .planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md (bloco "Match fraco" + casos-âncora 6-8)
  </read_first>
  <behavior>
    - Empresa existente por CNPJ (match forte) → Company::count()==1, campos vazios enriquecidos, campo manual intocado, e `hubspot_snapshot` reescrito com o payload do evento novo (contacts/line_items/captured_at presentes).
    - Empresa existente por hubspot_company_id (forte) → novo contrato só com hubspot_line_item_id inédito; sem duplicar company/contrato.
    - Match fraco (só nome normalizado bate) → CRIA a empresa nova + grava warning possivel_duplicidade no hubspot_eventos.payload (com id da candidata + via 'nome'); NÃO mescla campos críticos na candidata existente.
    - DB fresco → cria normalmente, sem warning.
  </behavior>
  <action>
    Implemente o caminho MATCH FRACO no criarEmpresa: quando `encontrar()` retornar match 'fraco', siga o caminho de CRIAÇÃO da empresa nova (113-02, com campos estruturados + snapshot) — para não perder o handoff — mas NÃO faça merge de campos críticos na empresa candidata. Grave um warning `possivel_duplicidade` no `hubspot_eventos.payload` (mesmo padrão de escrita de payload usado por persistirContratos ~431-442), contendo: candidate_company_id, via ('nome'/'email'/'domain'), e o name normalizado. Acrescente esse warning também ao array `warnings` do hubspot_snapshot da empresa recém-criada. NÃO renderize nada na UI (Fase 114). Log::channel('ecf-webhooks')->warning com contexto. Comentários pt-BR.
    Escreva os testes E2E RED em Phase113HubspotDedupTest reusando o padrão Http::fake: (a) cria Company com cnpj X e um campo manual preenchido, dispara webhook do mesmo cnpj, assere count==1 + campo manual intocado + campo vazio enriquecido + `hubspot_snapshot` reescrito com o shape completo (contacts/line_items/captured_at do evento novo); (b) cria Company com hubspot_company_id Y + contrato com line_item L1, dispara webhook com line items L1+L2, assere que só L2 vira contrato novo; (c) cria Company "Padaria Silva" e dispara webhook de outra "Padaria Silva" sem cnpj/hubspot_company_id → assere que uma NOVA company foi criada E que hubspot_eventos.payload tem possivel_duplicidade apontando a candidata. GREEN após implementar.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase113HubspotDedupTest.php</automated>
  </verify>
  <acceptance_criteria>
    - Match fraco por nome: nova Company criada + `hubspot_eventos.payload['possivel_duplicidade']` presente com candidate_company_id e via; campos críticos da candidata inalterados.
    - Match forte por cnpj: `Company::count()===1`; campo manual preservado; campo vazio enriquecido; `hubspot_snapshot` reescrito com o shape completo do evento novo (contacts/line_items/captured_at).
    - Match forte por hubspot_company_id: só o line item inédito vira contrato (guard hubspot_line_item_id).
    - `php artisan test tests/Feature/Phase113HubspotDedupTest.php` verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot webhook → dedup/persistência | Deal externo pode corresponder (ou colidir) com empresa já existente no banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-03-01 | Tampering | Match fraco mesclando dados na empresa errada | mitigate | Match fraco NUNCA faz merge de campos críticos; apenas warning no payload + cria empresa nova; humano decide na Fase 114 |
| T-113-03-02 | Integrity | Enriquecimento forte sobrescrevendo dado manual do Comercial | mitigate | Enriquecer somente colunas vazias (null/''); dado preenchido é soberano |
| T-113-03-03 | Integrity | Duplicação de contrato ao reprocessar deal de empresa existente | mitigate | Guard por hubspot_line_item_id (e servico_id ativo no fluxo legado) antes de criar ContratoServico |
| T-113-03-04 | DoS | Busca por nome normalizado varrendo toda a tabela companies | accept | Volume de companies baixo (centenas); nome só é consultado quando não há match forte; otimização adiada |
| T-113-03-SC | Tampering | npm/composer installs | accept | Fase sem instalação de pacotes |
</threat_model>

<verification>
- Suite nova verde: `php artisan test tests/Feature/Phase113HubspotDedupTest.php`.
- Enriquecimento (113-02) segue verde: `php artisan test tests/Feature/Phase113HubspotEnrichmentTest.php`.
- Regressão (asserções intocadas — NÃO confiar só em `--filter=Hubspot`, que não casa `Phase112HandoffServiceTest`): `php artisan test tests/Feature/Phase112HandoffServiceTest.php tests/Feature/Phase112HubspotHandoffWebhookTest.php tests/Feature/Phase35HubspotV2Test.php tests/Feature/Phase34HubspotWebhookTest.php`.
- Suite HubSpot completa: `php artisan test --filter=Hubspot`.
</verification>

<success_criteria>
- HUB-DEDUP-01: match forte (hubspot_company_id/cnpj) enriquece campos vazios sem duplicar empresa/contrato (guard hubspot_line_item_id).
- HUB-DEDUP-02: match fraco por nome gera warning possivel_duplicidade no payload e segue criando, sem merge agressivo.
- Regressão zero: DB fresco mantém o caminho de criação da 113-02 (Phase34/35/112 verdes sem editar asserções).
</success_criteria>

<output>
Criar `.planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-03-SUMMARY.md` ao concluir.
</output>
