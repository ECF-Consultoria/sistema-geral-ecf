---
phase: 113-enriquecimento-de-contato-empresa-escolha-de-contato-princip
plan: 02
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - app/Http/Controllers/Api/HubspotWebhookController.php
  - app/Services/Hubspot/HubspotDealHandoffService.php
  - app/Services/Hubspot/HubspotHandoffData.php
  - tests/Feature/Phase113HubspotEnrichmentTest.php
autonomous: true
requirements: [HUB-CONTATO-01, HUB-CONTATO-02, HUB-DEDUP-03]
must_haves:
  truths:
    - "O webhook busca TODOS os contatos associados (batch), não só o primeiro"
    - "companies.nome_contato e companies.cargo_contato são gravados estruturados a partir do contato principal"
    - "companies.hubspot_deal_id/hubspot_company_id/hubspot_contact_id/hubspot_domain são gravados"
    - "email_cliente/telefone seguem Company e caem para o contato principal (incl. mobilephone) quando a Company não tem"
    - "companies.hubspot_snapshot guarda deal + company + TODOS os contatos + line_items + primary_contact_id + captured_at"
    - "A linha 'Contato (HubSpot): {nome}' em notes continua existindo (fonte legada preservada)"
    - "Regressão zero: Phase34/Phase35/Phase112 verdes sem alterar asserções"
  artifacts:
    - path: "app/Http/Controllers/Api/HubspotWebhookController.php"
      provides: "fetch batch de contatos + seleção do principal + gravação estruturada + snapshot no caminho de criação"
      contains: "fetchAssociatedContactIds"
    - path: "app/Services/Hubspot/HubspotHandoffData.php"
      provides: "company_data/contact_data preenchidos (deixam de ser sempre null)"
      contains: "company_data"
    - path: "tests/Feature/Phase113HubspotEnrichmentTest.php"
      provides: "cobertura E2E de contato principal + campos estruturados + snapshot"
  key_links:
    - from: "HubspotWebhookController::processar"
      to: "HubspotContactSelector::selecionar"
      via: "seleção do contato principal a partir da lista batch"
      pattern: "selecionar"
    - from: "HubspotWebhookController::criarEmpresa"
      to: "companies.hubspot_snapshot"
      via: "gravação do snapshot com todos os contatos"
      pattern: "hubspot_snapshot"
---

<objective>
Trocar o fetch singular por batch no webhook, escolher o contato principal (via `HubspotContactSelector` da 113-01), gravar os campos estruturados da Company (nome_contato, cargo_contato, IDs HubSpot, domain, observacao) e persistir o `hubspot_snapshot` completo (deal + company + TODOS os contatos + line_items). Estender `HubspotDealHandoffService`/`HubspotHandoffData` para carregar `company_data`/`contact_data` no DTO.

Purpose: hoje o webhook pega só o primeiro contato e joga o nome em `notes`. Esta entrega torna os dados estruturados (colunas reais já criadas na 111) e auditáveis (snapshot), SEM tocar em dedup — o caminho de criação de empresa nova continua idêntico no observável exigido pelos testes atuais.
Output: controller usando batch + gravação estruturada + snapshot; DTO com company_data/contact_data preenchidos; suite Phase113HubspotEnrichmentTest verde; regressão intacta.

ATENÇÃO — INVARIANTE DE NÃO-REGRESSÃO (lei desta fase): o caminho comum (deal + 1 company + 1 contato, empresa NOVA) continua criando a Company com os mesmos campos e a mesma linha em `notes`. `Phase34HubspotWebhookTest`, `Phase35HubspotV2Test` e `Phase112HubspotHandoffWebhookTest` DEVEM ficar verdes SEM alterar asserções. O enriquecimento é ADITIVO. Como os testes usam `Http::fake` e os métodos batch batem nos MESMOS endpoints dos singulares (`deals/{id}/associations/contacts` e `contacts/{id}`), a troca single→batch não quebra os mocks; com 1 contato, a seleção retorna esse contato (idêntico ao fallback atual).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md
@app/Http/Controllers/Api/HubspotWebhookController.php
@app/Services/Hubspot/HubspotDealHandoffService.php
@app/Services/Hubspot/HubspotHandoffData.php

<interfaces>
<!-- Já existem (111) — usar diretamente, sem explorar o codebase: -->
HubspotApiClient::fetchAssociatedContactIds(string $dealId): array          // lista de ids (string)
HubspotApiClient::fetchContacts(array $ids, array $properties): array        // mapa id => properties
HubspotApiClient::fetchAssociatedCompanyId(string $dealId): ?string
HubspotApiClient::fetchCompany(string $id, array $properties): array

<!-- Da 113-01 (wave 1) — consumir: -->
HubspotContactSelector::selecionar(array $contatos): ?array                   // item com chaves lógicas id/firstname/lastname/email/phone/mobilephone/jobtitle

<!-- Config já ampliada (111): -->
config('services.hubspot.props.contact') => ['firstname','lastname','email','phone','mobilephone','jobtitle','additional_emails']
config('services.hubspot.props.company') => ['name','cnpj','email','phone','domain', ...]
config('services.hubspot.props.deal')    => [..., 'observacao' => 'observacao', ...]

<!-- Company: $fillable já tem nome_contato, cargo_contato, hubspot_deal_id, hubspot_company_id,
     hubspot_contact_id, hubspot_domain, hubspot_observacao, hubspot_snapshot (cast 'array'). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: fetch batch de contatos + company + seleção do principal no processar()</name>
  <files>app/Http/Controllers/Api/HubspotWebhookController.php</files>
  <read_first>
    - app/Http/Controllers/Api/HubspotWebhookController.php (processar ~162-239: bloco de fetch ~174-205; criarEmpresa recebe $hubContact hoje)
    - app/Services/HubspotApiClient.php (fetchAssociatedContactIds ~344, fetchContacts ~407, fetchAssociatedCompanyId ~67, fetchCompany ~89)
    - app/Services/Hubspot/HubspotContactSelector.php (113-01)
  </read_first>
  <action>
    No `processar()`, substitua o fetch singular de contato (fetchAssociatedContactId + fetchContact ~185-197) por batch: chame `fetchAssociatedContactIds($dealId)` e, se houver ids, `fetchContacts($ids, array_values($propsContact))` (mapa id => properties). Envolva em try/catch \Throwable com Log::channel('ecf-webhooks')->warning como já é feito hoje — ausência de contato NÃO bloqueia o cadastro.
    Converta o mapa id=>properties numa LISTA de contatos com chaves LÓGICAS (id + firstname/lastname/email/phone/mobilephone/jobtitle) mapeadas via $propsContact, e chame `HubspotContactSelector::selecionar($lista)` para obter o contato principal (array lógico) ou null. Preserve a lista completa normalizada (todos os contatos) numa variável para o snapshot da tarefa 3.
    Ajuste a assinatura de `criarEmpresa` para receber: o contato principal já em chaves lógicas (substituindo o antigo $hubContact bruto), a lista completa de contatos normalizados, o id da company associada e o id do contato principal — mantendo compatibilidade com a passagem de $deal/$hubCompany/$lineItems/$evento. Não altere o restante do fluxo (idempotência ~148, roteamento MlbEmpresa, notificação). Comentários pt-BR marcando "Fase 113".
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase34HubspotWebhookTest.php tests/Feature/Phase35HubspotV2Test.php</automated>
  </verify>
  <acceptance_criteria>
    - O controller chama `fetchAssociatedContactIds` + `fetchContacts` (grep confirma) e NÃO depende mais de `fetchContact`/`fetchAssociatedContactId` singulares no caminho principal.
    - Com 1 contato associado, o contato principal selecionado é exatamente esse contato (email/telefone de fallback continuam funcionando).
    - Phase34 e Phase35 verdes SEM alterar asserções.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarefa 2: gravar campos estruturados da Company (HUB-CONTATO-02) preservando notes e fallback</name>
  <files>app/Http/Controllers/Api/HubspotWebhookController.php, app/Services/Hubspot/HubspotDealHandoffService.php, app/Services/Hubspot/HubspotHandoffData.php</files>
  <read_first>
    - app/Http/Controllers/Api/HubspotWebhookController.php (criarEmpresa ~261-340: Company::create + fallback email/telefone + linha notes)
    - app/Services/Hubspot/HubspotDealHandoffService.php (build ~38-106; company_data/contact_data hoje nunca preenchidos)
    - app/Services/Hubspot/HubspotHandoffData.php (company_data/contact_data nullable, reservados p/ 113)
    - .planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md (bloco "Campos estruturados (HUB-CONTATO-02)")
  </read_first>
  <action>
    No `criarEmpresa`, ao montar o Company::create, adicione os campos estruturados (colunas já no $fillable): `nome_contato` = trim(firstname+' '+lastname) do contato principal; `cargo_contato` = jobtitle do contato principal; `hubspot_deal_id` = (string) $evento->object_id; `hubspot_company_id` = id da company associada (null se ausente); `hubspot_contact_id` = id do contato principal (null se ausente); `hubspot_domain` = domain da company via $propsCompany['domain']; `hubspot_observacao` = observação comercial do deal via $propsDeal['observacao'] (best-effort, null se ausente). Mantenha email_cliente/telefone com a MESMA regra de fallback já existente (Company > contato principal, incluindo `mobilephone` do contato quando nem company nem contact.phone existirem — hoje só usa phone; adicione o coalesce para mobilephone). Trate strings vazias como ausência (padrão já usado no arquivo). NÃO sobrescreva comportamento hoje asserido: email/telefone atuais dos testes continuam vindo do mesmo lugar.
    MANTENHA integralmente a linha `notes` "Contato (HubSpot): {nome}" (bloco ~311-322) — é fonte legada e há teste que a exige; ela passa a coexistir com nome_contato estruturado.
    Estenda `HubspotDealHandoffService::build()` para preencher o DTO com `company_data` (mapa estruturado: name/cnpj/email/telefone/domain normalizados) e `contact_data` (contato principal + lista completa de contatos). Discrição de assinatura: adicione parâmetros OPCIONAIS ao final de `build()` (ex.: `?array $hubCompany = null, array $contatosLogicos = [], ?array $contatoPrincipal = null, array $propsCompany = []`) com defaults que preservem 100% a lógica de valor/contratos atual — as chamadas existentes de 3 args (e Phase112) NÃO podem mudar de resultado. Preencha `company_data`/`contact_data` no `new HubspotHandoffData(...)`. O controller passa a ler esses campos para gravar a Company, mantendo o service como fonte da normalização. Comentários pt-BR.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase112HandoffServiceTest.php tests/Feature/Phase112HubspotHandoffWebhookTest.php tests/Feature/Phase35HubspotV2Test.php</automated>
  </verify>
  <acceptance_criteria>
    - Após webhook com contato principal, `companies.nome_contato` = firstname+lastname e `companies.cargo_contato` = jobtitle (verificável na nova suite).
    - `companies.hubspot_deal_id`, `hubspot_company_id`, `hubspot_contact_id`, `hubspot_domain` preenchidos quando presentes no HubSpot.
    - Company sem phone usa `contact.mobilephone` em `companies.telefone`.
    - `HubspotHandoffData->company_data` e `->contact_data` deixam de ser null quando há company/contatos.
    - Linha "Contato (HubSpot): {nome}" continua em `notes` (teste de regressão verde).
    - Phase35 e Phase112 verdes SEM alterar asserções (valor/contratos idênticos).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 3: hubspot_snapshot completo (HUB-DEDUP-03) + suite Phase113HubspotEnrichmentTest</name>
  <files>app/Http/Controllers/Api/HubspotWebhookController.php, tests/Feature/Phase113HubspotEnrichmentTest.php</files>
  <read_first>
    - app/Http/Controllers/Api/HubspotWebhookController.php (criarEmpresa — ponto após Company::create)
    - tests/Feature/Phase35HubspotV2Test.php (padrão mockaHubSpot com Http::fake, disparaWebhook, eventoPadrao — reusar como base do novo teste)
    - .planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-CONTEXT.md (bloco "Snapshot completo (HUB-DEDUP-03)" + casos-âncora 1-5)
  </read_first>
  <behavior>
    - Deal com 3 contatos (um com email+telefone) → nome_contato/cargo_contato vêm do contato com email+telefone; hubspot_contact_id = id dele.
    - Company sem phone → companies.telefone = contact.mobilephone.
    - companies.hubspot_snapshot é array e contém: deal, company, contacts (TODOS os 3, não só o principal), primary_contact_id, line_items, warnings, captured_at.
    - hubspot_deal_id/hubspot_company_id/hubspot_contact_id gravados.
  </behavior>
  <action>
    Monte o snapshot no `criarEmpresa` (dentro da mesma transaction, após Company::create/estruturados) como array associativo: `deal` (properties do deal), `company` (properties da company ou null), `contacts` (LISTA completa de contatos normalizados), `primary_contact_id`, `line_items` (line items normalizados já buscados), `warnings` (inicialmente do handoff; a 113-03 acrescenta possivel_duplicidade), `captured_at` (now()->toIso8601String()). Grave em `companies.hubspot_snapshot` (cast 'array' já configurado) — pode ser no próprio Company::create ou num update logo após. Comentário pt-BR "Fase 113 — snapshot completo p/ auditoria/replay (Fase 115)".
    Escreva primeiro os testes RED em Phase113HubspotEnrichmentTest reusando o padrão `Http::fake` do Phase35HubspotV2Test, mas com um mock de MÚLTIPLOS contatos (3 ids na association + 3 respostas de contacts/{id}). Cubra os 4 casos do behavior. Rode (RED), implemente o snapshot, rode (GREEN). Um caso deve assertar `count($company->hubspot_snapshot['contacts']) === 3`.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/Phase113HubspotEnrichmentTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `companies.hubspot_snapshot['contacts']` contém TODOS os contatos do deal (ex.: 3), com `primary_contact_id` apontando para o principal.
    - Snapshot contém deal, company, line_items, warnings e captured_at.
    - Deal com 3 contatos escolhe o de email+telefone (nome_contato/hubspot_contact_id conferem).
    - Company sem phone → telefone = mobilephone.
    - `php artisan test tests/Feature/Phase113HubspotEnrichmentTest.php` verde.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot webhook → Company | Payload externo (deal/company/contatos) atravessa para persistência; HMAC/idempotência já validados upstream (intocados) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02-01 | Tampering | fetch batch com contato ausente/erro HTTP | mitigate | try/catch \Throwable + warning; lista vazia → seleção null; cadastro segue com dados da company (paridade Phase35) |
| T-113-02-02 | Information disclosure | hubspot_snapshot guarda PII (emails/telefones de todos os contatos) | accept | Dado interno, acesso restrito a role admin/Comercial; sem exposição externa nova nesta fase |
| T-113-02-03 | Integrity | quebra da invariante de valor/contratos ao estender build() | mitigate | Parâmetros novos são OPCIONAIS com defaults; gate de regressão Phase112 obrigatório |
| T-113-02-SC | Tampering | npm/composer installs | accept | Fase sem instalação de pacotes |
</threat_model>

<verification>
- Suite nova verde: `php artisan test tests/Feature/Phase113HubspotEnrichmentTest.php`.
- Regressão (asserções intocadas — NÃO confiar só em `--filter=Hubspot`, que não casa `Phase112HandoffServiceTest`): `php artisan test tests/Feature/Phase112HandoffServiceTest.php tests/Feature/Phase112HubspotHandoffWebhookTest.php tests/Feature/Phase35HubspotV2Test.php tests/Feature/Phase34HubspotWebhookTest.php`.
- Suite HubSpot completa sem regressão: `php artisan test --filter=Hubspot`.
</verification>

<success_criteria>
- HUB-CONTATO-01: todos os contatos buscados (batch) + principal determinístico gravado.
- HUB-CONTATO-02: nome_contato/cargo_contato/IDs/domain/observacao estruturados; email/telefone com fallback incl. mobilephone; notes preservada.
- HUB-DEDUP-03: hubspot_snapshot com deal/company/todos os contatos/line_items.
- Regressão zero (Phase34/35/112 verdes sem editar asserções).
</success_criteria>

<output>
Criar `.planning/phases/113-enriquecimento-de-contato-empresa-escolha-de-contato-princip/113-02-SUMMARY.md` ao concluir.
</output>
