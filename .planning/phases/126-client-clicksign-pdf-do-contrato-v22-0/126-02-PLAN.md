---
phase: 126-client-clicksign-pdf-do-contrato-v22-0
plan: 02
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - app/Services/Clicksign/ClicksignClient.php
  - config/services.php
  - .env.example
  - tests/Feature/Phase126/ClicksignClientEnvelopeTest.php
autonomous: true
requirements: [CLICK-01]

must_haves:
  truths:
    - "O client sabe fazer as 7 famílias de chamada: envelope, documento, signatário, requisito, notificação, consulta e cancelamento"
    - "Cada signatário recebe requisito de qualificação (agree + role) e de autenticação (provide_evidence + auth email)"
    - "Os três signatários da ECF (dois contratada, uma testemunha) vêm de config lida do .env — nunca hardcoded"
    - "Todos os signatários ficam no mesmo grupo: assinam simultaneamente, sem ordenação"
    - "A ativação manda prazo de 30 dias e lembrete a cada 3 dias explicitamente, sem depender do default da Clicksign"
    - "Falha no meio da montagem cancela o envelope na Clicksign antes de propagar o erro"
    - "A montagem completa consome no máximo 15 chamadas (limite medido da janela é 20)"
  artifacts:
    - path: "app/Services/Clicksign/ClicksignClient.php"
      provides: "Métodos de signatário, requisito, ativação, consulta, notificação, cancelamento e montarEnvelope com rollback"
      contains: "montarEnvelope"
    - path: "config/services.php"
      provides: "Signatários fixos da ECF lidos do .env (D-08)"
      contains: "signatarios_ecf"
    - path: "tests/Feature/Phase126/ClicksignClientEnvelopeTest.php"
      provides: "Prova de D-07, D-08, D-09, D-12, D-13 e da contagem de chamadas"
  key_links:
    - from: "app/Services/Clicksign/ClicksignClient.php"
      to: "config('services.clicksign.signatarios_ecf')"
      via: "montarEnvelope acrescenta os três signatários fixos"
      pattern: "signatarios_ecf"
    - from: "montarEnvelope"
      to: "cancelarEnvelope"
      via: "try/catch que cancela o envelope criado antes de propagar (D-12)"
      pattern: "cancelarEnvelope"
---

<objective>
Completar o `ClicksignClient` com as chamadas que faltam para montar um envelope de ponta a ponta:
signatários, requisitos de qualificação e autenticação, ativação com prazo e lembrete explícitos,
consulta, reenvio de notificação e cancelamento — mais o método composto `montarEnvelope()`, que
cancela o que criou se falhar no meio (D-12).

Purpose: fecha o Success Criteria 1 (as 7 famílias de chamada) e responde o A2 do
`REQUIREMENTS-v22.md` (rollback de envelope montado pela metade), que estava listado como pergunta
em aberto da Fase 127. A Fase 127 vai chamar `montarEnvelope()` de dentro de um job (D-14) sem
precisar reimplementar sequência nem rollback.

Output: `ClicksignClient` completo, bloco de signatários da ECF em config, e teste cobrindo papéis,
simultaneidade, prazo, rollback e contagem de chamadas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-CONTEXT.md
@.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md
@.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Já existe (plano 126-01) — usar, não redefinir:

App\Services\Clicksign\ClicksignClient
  criarEnvelope(array $atributos): array
  anexarDocumento(string $envelopeId, string $nomeArquivo, string $pdfBinario): array
  private enviar(string $metodo, string $caminho, array $payload, string $contexto): array
App\Exceptions\ClicksignException::fromResponse(int $status, array $corpo): self
Tests\Fixtures\ClicksignSandboxFixtures

Vocabulário interno já travado no schema da Fase 125 (`app/Models/ContratoAssinaturaSignatario.php`):
  PAPEL_CONTRATANTE = 'contratante'
  PAPEL_CONTRATADA  = 'contratada'
  PAPEL_TESTEMUNHA  = 'testemunha'

Vocabulário da API (§6 do CLICKSIGN-SANDBOX-EMPIRICO.md):
  qualificação:   action = 'agree',            role = 'sign' | 'party' | 'contractor'
  autenticação:   action = 'provide_evidence', auth = 'email'
  ambos exigem relationships.document E relationships.signer
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Signatários e requisitos — papéis, autenticação por e-mail e assinatura simultânea</name>
  <files>
    app/Services/Clicksign/ClicksignClient.php,
    config/services.php,
    .env.example,
    tests/Feature/Phase126/ClicksignClientEnvelopeTest.php
  </files>
  <read_first>
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` §6 (os dois eixos de requisito) e §5 (default `group = 1` do signatário).
    - `app/Models/ContratoAssinaturaSignatario.php` linhas 47-95 — as constantes `PAPEL_*` que são o vocabulário interno a mapear.
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-CONTEXT.md` §D-07, §D-08, §D-09.
    - `config/services.php` — bloco `'clicksign'` criado no plano 126-01.
    - `app/Services/Clicksign/ClicksignClient.php` — o núcleo `enviar()` do plano 126-01, que estes métodos reusam.
  </read_first>
  <behavior>
    - `adicionarSignatario()` faz `POST {base}/envelopes/{id}/signers` com `name`, `email`, `communicate_by = 'email'` e `group = 1` para TODOS (D-09: sem ordenação).
    - `criarRequisitoQualificacao()` envia `action = 'agree'` e o `role` mapeado, com `relationships.document` e `relationships.signer`.
    - `criarRequisitoAutenticacao()` envia `action = 'provide_evidence'` e `auth = 'email'` (D-07), com as mesmas duas relationships.
    - O mapa de papéis converte `contratante`, `contratada` e `testemunha` para os valores do §6; papel desconhecido lança `ClicksignException` em vez de mandar valor inválido para a API.
    - `config('services.clicksign.signatarios_ecf')` devolve três entradas com `nome`, `email` e `papel`, todas vindas de `env()`.
    - Nenhum nome ou e-mail real da ECF aparece em arquivo versionado.
  </behavior>
  <action>
    Acrescentar ao `ClicksignClient` a constante pública `PAPEL_PARA_CLICKSIGN_ROLE` (array associativo,
    `SCREAMING_SNAKE` conforme convenção do projeto) mapeando as constantes `PAPEL_*` de
    `ContratoAssinaturaSignatario` para o vocabulário `sign`/`party`/`contractor` do §6 do arquivo empírico.
    Escolher o valor de cada papel a partir do que o §6 e o `plano-administrativo-clicksign.md` registram; se um
    dos três não tiver correspondência registrada, usar o valor mais próximo e marcar no docblock como
    **NÃO MEDIDO**, para o checkpoint do plano 126-06 confirmar contra o sandbox. Papel fora do mapa lança
    `ClicksignException` com mensagem pt-BR simples.

    Implementar `adicionarSignatario(string $envelopeId, string $nome, string $email): array` —
    `POST /envelopes/{id}/signers`, `data.type = 'signers'`, atributos `name`, `email`,
    `communicate_by = 'email'` e `group = 1` explícito (D-09: mesmo grupo para todos; não confiar no default).

    Implementar `criarRequisitoQualificacao(string $envelopeId, string $documentId, string $signerId, string $papelInterno): array`
    e `criarRequisitoAutenticacao(string $envelopeId, string $documentId, string $signerId): array`, ambos em
    `POST /envelopes/{id}/requirements` com `data.type = 'requirements'`, os atributos do §6 e o bloco
    `relationships` com `document` e `signer` (obrigatórios nos dois). A autenticação usa `auth = 'email'` fixo —
    método configurável por contrato está explicitamente deferido no CONTEXT.

    Acrescentar ao bloco `'clicksign'` de `config/services.php` a chave `signatarios_ecf`: array com as três
    pessoas fixas da ECF (D-08), cada uma com `nome`, `email` e `papel` (duas `contratada`, uma `testemunha`),
    todas lidas de `env()` — chaves sugeridas `CLICKSIGN_SIG1_NOME`/`CLICKSIGN_SIG1_EMAIL` e assim por diante.
    **Nenhum nome ou e-mail real entra no código nem no `.env.example`**: o `.env.example` recebe as 6 chaves
    vazias com comentário pt-BR explicando quem preenche.

    Criar `tests/Feature/Phase126/ClicksignClientEnvelopeTest.php` com os casos do bloco `<behavior>`, usando
    `Http::fake()` com as fixtures do plano 126-01 e `Http::assertSent()` para inspecionar corpo enviado
    (`action`, `role`, `auth`, `group`, presença das duas relationships).
  </action>
  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/ClicksignClientEnvelopeTest.php</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima passa, com casos nomeados para cada um dos três papéis, para `auth = 'email'`, para `group = 1` e para papel desconhecido lançando exceção.
    - `Select-String -Pattern '@ecfconsultoria' config/services.php, .env.example, app/Services/Clicksign/ClicksignClient.php` não retorna nada.
    - `config('services.clicksign.signatarios_ecf')` tem exatamente 3 entradas com as chaves `nome`, `email` e `papel`.
  </acceptance_criteria>
  <done>Signatários e requisitos implementados com o vocabulário correto e sem dado pessoal versionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ativação com prazo explícito, consulta, reenvio de notificação e cancelamento</name>
  <files>
    app/Services/Clicksign/ClicksignClient.php,
    tests/Feature/Phase126/ClicksignClientEnvelopeTest.php
  </files>
  <read_first>
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` §5 (defaults `deadline_at` +30 dias e `remind_interval` 3), §7 (429 próprio do endpoint de notificação, em texto puro; `GET` na notificação devolve 404 porque é POST-only) e §3 (o endpoint de eventos, que é onde a evidência vive).
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-CONTEXT.md` §D-13 e §D-11.
    - `app/Services/Clicksign/ClicksignClient.php` — `enviar()` e o tratamento de erro do plano 126-01.
  </read_first>
  <behavior>
    - `ativarEnvelope()` faz `PATCH {base}/envelopes/{id}` mandando `status = 'running'`, `deadline_at` a 30 dias da data atual e `remind_interval = 3` EXPLÍCITOS no corpo — nada é deixado para o default da API.
    - `consultarEnvelope()` faz `GET {base}/envelopes/{id}` e devolve `data`.
    - `listarEventosDoDocumento()` faz `GET {base}/envelopes/{id}/documents/{docId}/events` e devolve a lista de eventos.
    - `reenviarNotificacao()` faz `POST {base}/envelopes/{id}/signers/{signerId}/notifications`; um 429 vira `ClicksignException` com `httpStatus` 429 e mensagem pt-BR de "aguarde antes de reenviar", mesmo quando a resposta vier em texto puro (não JSON:API), e NÃO é retentado internamente.
    - `cancelarEnvelope()` faz `PATCH {base}/envelopes/{id}` mandando o status de cancelamento e nunca lança exceção nova quando chamado dentro do rollback (Task 3) — devolve booleano de sucesso.
  </behavior>
  <action>
    Implementar `ativarEnvelope(string $envelopeId, int $prazoDias = 30, int $lembreteDias = 3): array`. O corpo
    JSON:API leva `status = 'running'`, `deadline_at` calculado como `now()->addDays($prazoDias)` no formato
    ISO-8601 aceito pela API, e `remind_interval = $lembreteDias`. Docblock em pt-BR registrando a D-13: os
    valores coincidem com o default medido, e são enviados assim mesmo para o comportamento não depender de
    default de terceiro que pode mudar sem aviso. Os parâmetros já nascem com default para a Fase 127 tornar o
    prazo configurável por contrato (DADOS-06) sem tocar no client.

    Implementar `consultarEnvelope(string $envelopeId): array` e
    `listarEventosDoDocumento(string $envelopeId, string $documentId): array` — este último com docblock
    apontando §3 do empírico: a situação individual do signatário NÃO existe no recurso `signers`, só na lista
    de eventos; quem for consumir isso na Fase 129 precisa saber.

    Implementar `reenviarNotificacao(string $envelopeId, string $signerId): array`. Este endpoint tem rate limit
    ANTI-SPAM próprio, separado da janela geral (§7): o 429 aqui é resposta esperada, não falha do sistema, e
    a resposta pode vir em texto puro. Tratar: não retentar, decodificar defensivamente (se não for JSON:API,
    montar a `ClicksignException` só com o status) e produzir mensagem pt-BR simples orientando a aguardar.

    Implementar `cancelarEnvelope(string $envelopeId): bool` — `PATCH /envelopes/{id}` com o status de
    cancelamento. Devolve `true` em sucesso e `false` em falha, logando `warning` no canal `ecf-webhooks` com
    apenas `envelope_id` e `status`. Não propaga exceção: quem chama é o rollback, e uma falha ao cancelar não
    pode substituir o erro original. Marcar no docblock como **NÃO MEDIDO** o valor exato do status de
    cancelamento, a ser confirmado no checkpoint do plano 126-06.

    Estender `ClicksignClientEnvelopeTest` com os casos do bloco `<behavior>`, inclusive a asserção de que o
    corpo do PATCH de ativação contém `deadline_at` e `remind_interval` (e não apenas `status`).
  </action>
  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/ClicksignClientEnvelopeTest.php</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima passa, com caso que assere `deadline_at` ~30 dias à frente e `remind_interval = 3` no corpo enviado.
    - Caso de 429 em reenvio de notificação: exatamente 1 requisição sai (`Http::assertSentCount`) e a exceção traz `httpStatus` 429.
    - `cancelarEnvelope()` com resposta 500 devolve `false` sem lançar exceção.
  </acceptance_criteria>
  <done>As chamadas de ciclo de vida do envelope existem, com prazo explícito e 429 de notificação tratado como caso esperado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: montarEnvelope — sequência completa com rollback (D-12) e teto de 15 chamadas</name>
  <files>
    app/Services/Clicksign/ClicksignClient.php,
    tests/Feature/Phase126/ClicksignClientEnvelopeTest.php
  </files>
  <read_first>
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-CONTEXT.md` §D-12, §D-14 e `<restricao_medida>` (a tabela das 15 chamadas contra o limite medido de 20).
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` §1 (rate limit medido).
    - `app/Jobs/AnalyzeCompanySugadoresJob.php` — o precedente de job de fila deste projeto (a Fase 127 vai chamar `montarEnvelope()` de dentro de um job igual a este; o client não pode prender o worker).
    - `app/Services/Clicksign/ClicksignClient.php` — tudo que as Tasks 1 e 2 acabaram de criar.
  </read_first>
  <behavior>
    - `montarEnvelope()` executa, nesta ordem: criar envelope, anexar documento, criar os 4 signatários, criar 8 requisitos (qualificação + autenticação por signatário), ativar. Total: 15 requisições no caminho feliz (`Http::assertSentCount(15)`).
    - Devolve um array com pelo menos `envelope_id`, `document_id` e a lista de signatários criados com seus ids da Clicksign.
    - Se QUALQUER passo após a criação do envelope falhar, o envelope criado é cancelado (uma requisição de cancelamento sai) e a `ClicksignException` original é propagada — não a do cancelamento.
    - Se a criação do PRÓPRIO envelope falhar, nenhuma requisição de cancelamento sai (não há o que cancelar).
    - Os três signatários da ECF vêm de `config('services.clicksign.signatarios_ecf')` e são acrescentados ao signatário do cliente, sempre — 4 assinaturas por contrato.
  </behavior>
  <action>
    Implementar `montarEnvelope(array $dadosEnvelope, string $nomeArquivo, string $pdfBinario, array $signatarioCliente): array`,
    onde `$signatarioCliente` traz `nome`, `email` e o papel `contratante`. O método junta esse signatário aos três
    de `config('services.clicksign.signatarios_ecf')` (D-08), executa a sequência do bloco `<behavior>` reusando
    os métodos das Tasks 1 e 2, e devolve os identificadores criados.

    Rollback da D-12: guardar o `envelope_id` assim que a criação retornar; envolver todo o restante em
    `try/catch (ClicksignException $e)`; no catch, chamar `cancelarEnvelope()` — que não lança — e depois
    `throw $e`. A exceção propagada é SEMPRE a original: o operador precisa ver por que falhou, não que o
    cancelamento deu certo. Logar no canal `ecf-webhooks` um `warning` de rollback com apenas `envelope_id`,
    `passo` e `status`.

    Docblock de classe/método em pt-BR registrando a restrição medida: 15 chamadas por envelope contra uma
    janela de 20 — dois contratos seguidos batem em 429. O client não deve acrescentar nenhuma chamada
    redundante (nada de reconsultar o envelope após cada passo), e a Fase 127 precisa espaçar a geração em lote.
    Registrar também que o método é feito para rodar dentro de um job de fila (D-14): sem estado de request,
    sem `sleep()` longo.

    Estender `ClicksignClientEnvelopeTest` com: caminho feliz contando 15 requisições; falha no passo de
    requisito provocando exatamente uma requisição de cancelamento e propagando a exceção original; falha na
    criação do envelope sem nenhuma requisição de cancelamento.
  </action>
  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/ClicksignClientEnvelopeTest.php</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima passa, com um caso asserindo `Http::assertSentCount(15)` no caminho feliz.
    - Caso de rollback: a requisição de cancelamento é observada via `Http::assertSent()` e a exceção capturada tem a mensagem do passo que falhou, não a do cancelamento.
    - Suíte da fase inteira verde: `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/`.
  </acceptance_criteria>
  <done>Um envelope pode ser montado de ponta a ponta pelo client, e uma falha no meio não deixa lixo na conta Clicksign.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ECF Admin → API Clicksign | 15 requisições por envelope levam PII do signatário (nome, e-mail) para fora |
| Resposta de erro da Clicksign → log/exceção | O erro de validação ecoa o payload enviado, com nome e e-mail do signatário (WR-11) |
| `config/services.php` + `.env.example` (versionados) | Identidade das três pessoas fixas da ECF |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-06 | Information Disclosure | Log de erro de `adicionarSignatario`/requisito | mitigate | O `enviar()` do plano 126-01 loga só `contexto`, `status`, `codigo` e `ponteiro`; nenhum método desta task loga o payload enviado (WR-11) |
| T-126-07 | Information Disclosure | Nome/e-mail dos sócios e da Comercial em arquivo versionado | mitigate | `signatarios_ecf` lê 6 chaves de `env()`; `.env.example` fica com valores vazios; teste/acceptance varre `@ecfconsultoria` no código |
| T-126-08 | Tampering | Envelope montado pela metade na conta Clicksign | mitigate | `montarEnvelope()` guarda o `envelope_id` e cancela no `catch` antes de propagar (D-12); envelope em `draft` não dispara e-mail, então cancelar é invisível ao cliente |
| T-126-09 | Denial of Service | 15 chamadas contra janela medida de 20 | mitigate | Nenhuma chamada redundante; retry limitado a 429/5xx (plano 126-01); rollback custa 1 chamada; restrição documentada no docblock para a Fase 127 espaçar lotes |
| T-126-10 | Repudiation | Prazo/lembrete dependendo do default da Clicksign | mitigate | `deadline_at` e `remind_interval` enviados explicitamente na ativação (D-13); teste assere o corpo enviado |
| T-126-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/` — verde.
- `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase111HubspotApiClientTest.php` — verde (adjacente nomeado pela VALIDATION; garante que o padrão do precedente não foi quebrado).
- Nenhum dado pessoal real em `config/services.php`, `.env.example` ou no client.
</verification>

<success_criteria>
- As 7 famílias de chamada existem e estão exercitadas por teste.
- Papéis internos mapeados para o vocabulário da API, com papel desconhecido barrado.
- Assinatura simultânea (mesmo grupo), autenticação por e-mail, prazo e lembrete explícitos.
- Rollback provado; caminho feliz custa 15 chamadas.
</success_criteria>

<output>
Create `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-02-SUMMARY.md` when done.
⚠️ Commit com caminhos explícitos (`git commit -- <arquivos>`), nunca `git add -A`/`git add .`.
</output>

