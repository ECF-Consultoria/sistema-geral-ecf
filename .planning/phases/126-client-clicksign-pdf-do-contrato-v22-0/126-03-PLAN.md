---
phase: 126-client-clicksign-pdf-do-contrato-v22-0
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_08_10_120000_add_pdf_paths_to_contrato_assinaturas_table.php
  - app/Models/ContratoAssinatura.php
  - database/factories/ContratoAssinaturaFactory.php
  - tests/Feature/Phase126/MigrationFase126ConvencoesTest.php
  - tests/Feature/Phase126/ContratoAssinaturaPdfPathsTest.php
autonomous: true
requirements: [PDF-01]

must_haves:
  truths:
    - "A tabela contrato_assinaturas tem onde anotar o caminho do PDF gerado e o do PDF assinado"
    - "As duas colunas novas podem ser preenchidas por mass assignment — nada falha em silêncio"
    - "A migration é aditiva e idempotente: roda numa tabela que já existe em produção sem recriar nada"
    - "A migration não repete nenhuma das 3 armadilhas de schema do projeto (enum, nullOnDelete sem nullable, nome de índice acima de 64 chars)"
    - "Existe uma factory que produz servicos_snapshot com forma e volume de dado real, para o PDF ser testado"
  artifacts:
    - path: "database/migrations/2026_08_10_120000_add_pdf_paths_to_contrato_assinaturas_table.php"
      provides: "Colunas pdf_path e pdf_assinado_path (D-03)"
      contains: "pdf_assinado_path"
    - path: "app/Models/ContratoAssinatura.php"
      provides: "$fillable com as duas colunas novas"
      contains: "pdf_path"
    - path: "database/factories/ContratoAssinaturaFactory.php"
      provides: "State comSnapshot() com serviços e valores realistas"
      contains: "comSnapshot"
    - path: "tests/Feature/Phase126/MigrationFase126ConvencoesTest.php"
      provides: "Guarda estática das 3 armadilhas de schema"
  key_links:
    - from: "database/migrations/2026_08_10_120000_add_pdf_paths_to_contrato_assinaturas_table.php"
      to: "app/Models/ContratoAssinatura.php"
      via: "as duas colunas entram no $fillable na mesma entrega"
      pattern: "pdf_assinado_path"
    - from: "database/factories/ContratoAssinaturaFactory.php"
      to: "servicos_snapshot"
      via: "state comSnapshot() alimenta o teste do PDF (planos 126-04/05)"
      pattern: "servicos_snapshot"
---

<objective>
Abrir as duas colunas de caminho de arquivo que a Fase 125 não previu (`pdf_path` e
`pdf_assinado_path`), atualizar o `$fillable` do model na MESMA entrega, e dar à
`ContratoAssinaturaFactory` um state que produz `servicos_snapshot` com dado realista — o insumo
que os planos do PDF vão consumir.

Purpose: é a fase que passa a gerar o PDF, então é ela que cria onde anotar (D-03).
`pdf_assinado_path` nasce sem uso, para a Fase 129 encontrar pronta ao baixar o assinado. O
`$fillable` anda junto porque, sem ele, o mass assignment das colunas novas falha **em silêncio**
(Pitfall 4 do RESEARCH).

Output: migration aditiva idempotente, `$fillable` atualizado, state `comSnapshot()` e dois testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-CONTEXT.md
@.planning/phases/125-estrutura-de-dados-administrativa-v22-0/125-CONTEXT.md
@CLAUDE.md

<interfaces>
Schema atual da tabela (`database/migrations/2026_08_10_100000_create_contrato_assinaturas_table.php`),
já em produção via batches 110/111 — esta fase só ACRESCENTA:

  id, company_id (FK NOT NULL, cascadeOnDelete), status (string 40),
  company_id_em_andamento (unsignedBigInteger nullable), clicksign_envelope_id (string 64 nullable),
  clicksign_document_id (string 64 nullable), enviado_em, assinado_em, liberado_em (timestamps nullable),
  erro_mensagem (text nullable), servicos_snapshot (json nullable), timestamps

  Índices existentes: ca_company_andamento_uniq, ca_clicksign_envelope_uniq, ca_company_status_idx

App\Models\ContratoAssinatura
  $fillable hoje: company_id, status, company_id_em_andamento, clicksign_envelope_id,
                  clicksign_document_id, enviado_em, assinado_em, liberado_em, erro_mensagem,
                  servicos_snapshot
  $casts: enviado_em/assinado_em/liberado_em => datetime, servicos_snapshot => array
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration aditiva (D-03) + $fillable na mesma entrega + guarda estática</name>
  <files>
    database/migrations/2026_08_10_120000_add_pdf_paths_to_contrato_assinaturas_table.php,
    app/Models/ContratoAssinatura.php,
    tests/Feature/Phase126/MigrationFase126ConvencoesTest.php
  </files>
  <read_first>
    - `database/migrations/2026_08_10_100000_create_contrato_assinaturas_table.php` — o schema atual e, no cabeçalho, as 3 armadilhas de MariaDB descritas linha a linha.
    - `.planning/phases/125-estrutura-de-dados-administrativa-v22-0/125-CONTEXT.md` §pitfalls — a origem das 3 armadilhas.
    - `tests/Feature/Phase125/MigrationsFase125ConvencoesTest.php` — o molde da guarda estática, incluindo `migrationSemComentarios()`. ⚠️ Note que ele usa `assertNotEmpty($matches)` para índices: a migration desta fase NÃO cria índice, então essa asserção precisa ser adaptada, não copiada.
    - `app/Models/ContratoAssinatura.php` linhas 45-70 — `$fillable` e `$casts`.
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-RESEARCH.md` §Pitfall 4.
  </read_first>
  <behavior>
    - Depois de migrar, `Schema::hasColumn('contrato_assinaturas', 'pdf_path')` e `...'pdf_assinado_path'` são verdadeiros.
    - `ContratoAssinatura::create([... 'pdf_path' => 'contratos/x.pdf'])` persiste o valor (prova de que o `$fillable` acompanhou).
    - Guarda estática sobre o texto da migration, com comentários removidos: nenhum `->enum(`; nenhuma FK com `nullOnDelete` sem `->nullable()`; todo `unique`/`index`/`foreign` (se houver) nomeado à mão e com nome de até 64 caracteres; nenhum `Schema::create(` nem `dropColumn` no `up()` (a migration é aditiva sobre tabela de produção); `up()` e `down()` guardados por `Schema::hasColumn`.
  </behavior>
  <action>
    Criar `database/migrations/2026_08_10_120000_add_pdf_paths_to_contrato_assinaturas_table.php` com
    `Schema::table('contrato_assinaturas', ...)` acrescentando `pdf_path` (string 255, nullable) e
    `pdf_assinado_path` (string 255, nullable), ambas posicionadas após `servicos_snapshot` via `->after()`
    (o grammar do SQLite ignora o modificador; se a suíte reclamar, remover só o `->after()`). Nenhum índice é
    necessário — as colunas são carga, não critério de busca.

    Idempotência obrigatória: envolver a adição em `if (! Schema::hasColumn('contrato_assinaturas', ...))` e o
    `down()` em `if (Schema::hasColumn(...))` com `dropColumn` das duas. Motivo registrado no cabeçalho: é
    `ALTER TABLE` numa tabela que já existe em produção (batches 110/111), e uma migration que não pode ser
    re-executada com segurança vira incidente de deploy.

    Cabeçalho do arquivo em pt-BR, no mesmo estilo da migration da Fase 125: citar D-03, dizer que
    `pdf_assinado_path` nasce sem uso para a Fase 129, e listar as 3 armadilhas com a razão de cada uma não se
    aplicar ou estar evitada aqui.

    Acrescentar `'pdf_path'` e `'pdf_assinado_path'` ao `$fillable` de `App\Models\ContratoAssinatura`, com
    comentário pt-BR de uma linha citando a Fase 126 / D-03. **Esta alteração é parte desta task, não de
    outra** — separadas, o mass assignment falharia em silêncio.

    Criar `tests/Feature/Phase126/MigrationFase126ConvencoesTest.php` replicando a estrutura do teste da
    Fase 125, inclusive o `migrationSemComentarios()` (o cabeçalho desta migration menciona `enum`, `1830` e
    `1059` — sem remover comentários, a guarda acusaria a si mesma). Adaptar as asserções conforme o bloco
    `<behavior>`: como esta migration não declara índice, a checagem de índice anônimo deve iterar sobre zero
    ou mais ocorrências, **sem** `assertNotEmpty`. Toda mensagem de falha em pt-BR, citando o erro de MariaDB
    correspondente.
  </action>
  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/MigrationFase126ConvencoesTest.php</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima passa.
    - `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase125/` continua verde (o schema que a migration altera).
    - `$fillable` de `ContratoAssinatura` contém as duas colunas novas.
    - A migration não contém `Schema::create(` nem `->enum(` fora de comentário.
  </acceptance_criteria>
  <done>Colunas criadas de forma aditiva e idempotente, `$fillable` acompanhando, guarda estática no lugar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: State comSnapshot() na factory + teste de persistência das colunas</name>
  <files>
    database/factories/ContratoAssinaturaFactory.php,
    tests/Feature/Phase126/ContratoAssinaturaPdfPathsTest.php
  </files>
  <read_first>
    - `database/factories/ContratoAssinaturaFactory.php` — hoje `definition()` deixa `servicos_snapshot` como `null`; note o aviso sobre não copiar `$attributes['company_id']` dentro de state.
    - `app/Models/ContratoServico.php` linhas 20-55 — `$fillable` e `$casts` (`valor_contratado` decimal:2, `data_contratacao`/`data_vencimento` date:Y-m-d). É a forma dos dados que o snapshot congela.
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-CONTEXT.md` §D-04 e `<tensao_de_dados>` (a tabela do que existe e do que não existe no banco).
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-VALIDATION.md` §"A régua do Success Criteria 3".
  </read_first>
  <behavior>
    - `ContratoAssinatura::factory()->comSnapshot()->create()` grava `servicos_snapshot` como array com pelo menos 2 serviços, cada um com nome do serviço, `valor_contratado` decimal com centavos, `data_contratacao` e `data_vencimento` no formato `Y-m-d`.
    - `ContratoAssinatura::factory()->comSnapshot()->create()->servicos_snapshot` volta do banco como array (cast já existente).
    - Existe um state ou parâmetro que produz nome de empresa extremo — 80+ caracteres com acentuação e caractere especial — para o teste de PDF-03 do plano 126-05 consumir sem inventar dado.
    - `pdf_path` e `pdf_assinado_path` persistem e voltam do banco.
  </behavior>
  <action>
    Acrescentar à `ContratoAssinaturaFactory` o state `comSnapshot(?array $servicos = null)`, que preenche
    `servicos_snapshot` com uma estrutura de 2 a 3 serviços no formato que a Fase 127 vai congelar: por serviço,
    as chaves `servico` (nome legível), `valor_contratado` (decimal com centavos, ex.: valores distintos e não
    redondos), `data_contratacao` e `data_vencimento` (`Y-m-d`). Docblock em pt-BR citando D-04 e explicando
    que "empresa real" na régua do Success Criteria 3 significa **forma e volume de dado real**, não conexão ao
    banco de produção (que nem está no ar) — decisão registrada na VALIDATION.

    Acrescentar também o state `comEmpresaDeNomeExtremo()`, que cria a `Company` associada com `name` de 80+
    caracteres contendo acentuação e caractere especial (por exemplo combinando `Ç`, `Ã` e travessão) e um
    `cnpj` formatado. Esse state é o insumo do teste de PDF-03 no plano 126-05 — deixá-lo aqui evita que cada
    teste invente o seu.

    Criar `tests/Feature/Phase126/ContratoAssinaturaPdfPathsTest.php` (com `RefreshDatabase`) cobrindo o bloco
    `<behavior>`: colunas existem via `Schema::hasColumn`, mass assignment das duas colunas persiste, o state
    `comSnapshot()` devolve array com a forma esperada, e o state de nome extremo produz `name` com mais de 80
    caracteres e acentuação preservada.
  </action>
  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/ContratoAssinaturaPdfPathsTest.php</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima passa.
    - Nenhum dado pessoal real na factory (empresas e valores são fictícios — o nome extremo é inventado, não copiado do banco).
    - `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase125/` continua verde.
  </acceptance_criteria>
  <done>A factory produz o insumo realista que os planos do PDF consomem, e as colunas novas estão provadas ponta a ponta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → MariaDB de produção | `ALTER TABLE` numa tabela que já existe (batches 110/111) — falha aqui é incidente de deploy |
| Request → Eloquent (`$fillable`) | Mass assignment das colunas novas |
| `pdf_path` → filesystem | O caminho gravado aponta para um arquivo com CNPJ, contato e valores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-11 | Tampering | Migration sobre tabela de produção | mitigate | Aditiva, idempotente (`Schema::hasColumn` no `up()` e no `down()`), sem `Schema::create`, sem `dropColumn` de coluna alheia; guarda estática `MigrationFase126ConvencoesTest` |
| T-126-12 | Denial of Service | Armadilha 1059 (nome de índice) e 1830 (`nullOnDelete` sem `nullable`) | mitigate | Nenhum índice e nenhuma FK nova nesta migration; a guarda estática reprova se alguém acrescentar sem nome ou sem `nullable()` |
| T-126-13 | Elevation of Privilege | Mass assignment | mitigate | `$fillable` explícito ganha exatamente as duas colunas; `$guarded = []` continua proibido (T-125-01) |
| T-126-14 | Information Disclosure | `pdf_path` é caminho previsível | accept | O arquivo vive em `storage/app/`, fora de `public/` (D-06) — não há URL que o sirva nesta fase; a rota autenticada é escopo da Fase 131 |
| T-126-15 | Information Disclosure | Factory com dado copiado de fonte real | mitigate | Empresa, valores e nome extremo são inventados; nada é copiado de banco ou de documento de pesquisa (achado WR-07 da Fase 125) |
</threat_model>

<verification>
- `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/` — verde.
- `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase125/` — verde (adjacente nomeado pela VALIDATION).
- ⚠️ **Verificação manual pendente (não bloqueia este plano, é gate de deploy):** rodar a migration contra o
  MariaDB real com `migrate --path=` e autorização explícita, como foi feito na Fase 125. O SQLite dos testes
  não reproduz 1830 nem 1059. Está registrada como checkpoint no plano 126-06.
</verification>

<success_criteria>
- As duas colunas existem, são preenchíveis e são idempotentes na migration.
- A guarda estática das 3 armadilhas roda e passa.
- A factory entrega `servicos_snapshot` realista e uma empresa de nome extremo.
</success_criteria>

<output>
Create `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-03-SUMMARY.md` when done.
⚠️ Commit com caminhos explícitos (`git commit -- <arquivos>`), nunca `git add -A`/`git add .`.
</output>
