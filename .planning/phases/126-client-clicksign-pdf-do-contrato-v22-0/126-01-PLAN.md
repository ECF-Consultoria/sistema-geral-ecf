---
phase: 126-client-clicksign-pdf-do-contrato-v22-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/services.php
  - .env.example
  - app/Exceptions/ClicksignException.php
  - app/Services/Clicksign/ClicksignClient.php
  - tests/Fixtures/ClicksignSandboxFixtures.php
  - tests/Feature/Phase126/ClicksignConfigEFixturesTest.php
  - tests/Feature/Phase126/ClicksignClientTest.php
  - tests/Feature/Phase126/ClicksignClientNaoVazaTokenTest.php
autonomous: true
requirements: [CLICK-01]

must_haves:
  truths:
    - "O client fala com a Clicksign com o token PURO no header Authorization (sem 'Bearer') e recebe 200 no caminho feliz simulado"
    - "Nenhuma linha logada pelo client contém o token — em sucesso E em erro, na mensagem E no contexto"
    - "Erro da API vira ClicksignException com código HTTP e mensagem em pt-BR simples; 401 e 403 têm mensagens diferentes"
    - "content_base64 sai como Data URI completo (data:application/pdf;base64,...), nunca base64 puro"
    - "Retry acontece em 429 e 5xx e NUNCA em 4xx de dado"
    - "Nenhuma fixture do repositório carrega PII real (e-mail, IP público ou chave real de signatário)"
  artifacts:
    - path: "config/services.php"
      provides: "Bloco 'clicksign' lido via config(), nunca env() direto no client"
      contains: "'clicksign' =>"
    - path: "app/Exceptions/ClicksignException.php"
      provides: "Exceção própria com status HTTP + código da API (D-10)"
      exports: ["ClicksignException"]
    - path: "app/Services/Clicksign/ClicksignClient.php"
      provides: "Base do client: headers, retry, log seguro, criarEnvelope, anexarDocumento"
      min_lines: 120
    - path: "tests/Fixtures/ClicksignSandboxFixtures.php"
      provides: "Payloads do sandbox anonimizados (D-15)"
      exports: ["ClicksignSandboxFixtures"]
    - path: "tests/Feature/Phase126/ClicksignClientNaoVazaTokenTest.php"
      provides: "Prova do Success Criteria 1"
  key_links:
    - from: "app/Services/Clicksign/ClicksignClient.php"
      to: "config('services.clicksign')"
      via: "leitura de token e base_url no construtor"
      pattern: "config\\('services\\.clicksign"
    - from: "app/Services/Clicksign/ClicksignClient.php"
      to: "Log::channel('ecf-webhooks')"
      via: "log de falha só com status + errors[].detail"
      pattern: "channel\\('ecf-webhooks'\\)"
    - from: "tests/Feature/Phase126/ClicksignClientTest.php"
      to: "Tests\\Fixtures\\ClicksignSandboxFixtures"
      via: "Http::fake com payloads literais do sandbox"
      pattern: "ClicksignSandboxFixtures"
---

<objective>
Levantar a fundação do `ClicksignClient`: configuração, exceção própria, fixtures anonimizadas do
sandbox e o núcleo de chamada HTTP (headers corretos, retry disciplinado, log que nunca vaza a
credencial), já exercitado pelas duas primeiras chamadas do envelope — criar envelope e anexar
documento.

Purpose: o Success Criteria 1 da fase ("nenhuma linha de log jamais contém o token") e o
Success Criteria 2 (gates #2 header e #4 `content_base64`) se decidem aqui. Todo método que a
Fase 126-02 acrescentar vai passar pelo mesmo núcleo — se ele nasce seguro, os demais herdam.

Output: `config/services.php['clicksign']`, `ClicksignException`, `ClicksignClient` com
`criarEnvelope()` e `anexarDocumento()`, fixtures reutilizáveis e 3 arquivos de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-CONTEXT.md
@.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-VALIDATION.md
@.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md
@CLAUDE.md

<interfaces>
Contratos que este plano CRIA e que a 126-02 consome (definir na Task 2, não renegociar depois):

App\Services\Clicksign\ClicksignClient
  __construct(?string $token = null, ?string $baseUrl = null)   // fallback para config('services.clicksign.*')
  criarEnvelope(array $atributos): array                        // devolve o bloco `data` da resposta
  anexarDocumento(string $envelopeId, string $nomeArquivo, string $pdfBinario): array
  private baseRequest(): Illuminate\Http\Client\PendingRequest  // headers + timeout + retry
  private enviar(string $metodo, string $caminho, array $payload, string $contexto): array

App\Exceptions\ClicksignException extends \RuntimeException
  public readonly ?int    $httpStatus
  public readonly ?string $codigoApi     // errors[0].code
  public readonly ?string $ponteiro      // errors[0].source.pointer
  public static fromResponse(int $status, array $corpo): self

Tests\Fixtures\ClicksignSandboxFixtures  // métodos estáticos, arrays literais anonimizados
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config, exceção e fixtures anonimizadas do sandbox</name>
  <files>
    config/services.php,
    .env.example,
    app/Exceptions/ClicksignException.php,
    tests/Fixtures/ClicksignSandboxFixtures.php,
    tests/Feature/Phase126/ClicksignConfigEFixturesTest.php
  </files>
  <read_first>
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` — §1 (401/403 e headers), §2 (erro literal do `content_base64`), §5 (defaults), §6 (vocabulário `role`/`auth`), §7 (429 em texto puro) e o aviso de anonimização no fim do arquivo. **Precede a documentação oficial.**
    - `config/services.php` linhas 110-150 — bloco `'hubspot'`, o formato a espelhar (`env()` só aqui, nunca no service).
    - `.env.example` linhas 184-194 — as 5 chaves `CLICKSIGN_*` que a Fase 125 já preparou.
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-CONTEXT.md` §D-10 e §D-15.
  </read_first>
  <behavior>
    - `config('services.clicksign.base_url')` resolve para a URL do sandbox por padrão.
    - `config('services.clicksign.access_token')` existe como chave (valor vazio em ambiente de teste é aceitável).
    - `ClicksignException::fromResponse(401, ...)` produz mensagem diferente de `fromResponse(403, ...)`.
    - `ClicksignException` expõe `httpStatus`, `codigoApi` e `ponteiro`.
    - Serializado, nenhum payload de `ClicksignSandboxFixtures` contém e-mail fora de `@example.com`/`@example.org`, IP fora de `203.0.113.0/24`, nem UUID fora do padrão sintético `00000000-0000-4000-8000-0000000000NN`.
  </behavior>
  <action>
    Criar em `config/services.php` o bloco `'clicksign'` espelhando a estrutura do bloco `'hubspot'`, com as
    chaves: `env` (`CLICKSIGN_ENV`, default `sandbox`), `base_url` (`CLICKSIGN_BASE_URL`, default
    `https://sandbox.clicksign.com/api/v3`), `access_token` (`CLICKSIGN_ACCESS_TOKEN`, sem default),
    `webhook_secret` (`CLICKSIGN_WEBHOOK_SECRET`), `api_user_email` (`CLICKSIGN_API_USER_EMAIL`) e
    `max_upload_bytes` (`CLICKSIGN_MAX_UPLOAD_BYTES`, default `20971520`). Comentar em pt-BR, acima do bloco,
    que o token vai PURO no header (gate #2) e que `max_upload_bytes` é **NÃO MEDIDO** — é o gate #5, fechado
    pelo checkpoint do plano 126-06. Acrescentar `CLICKSIGN_MAX_UPLOAD_BYTES=20971520` ao `.env.example`, logo
    abaixo das chaves Clicksign existentes, com o mesmo aviso.

    Criar `App\Exceptions\ClicksignException` estendendo `\RuntimeException`, com as propriedades readonly
    `httpStatus`, `codigoApi` e `ponteiro`, e o construtor factory estático `fromResponse(int $status, array $corpo)`
    que lê `errors.0.code`, `errors.0.detail` e `errors.0.source.pointer`. A mensagem final é pt-BR e em
    linguagem simples (regra da Fase 124): 401 → token inválido ou ausente; 403 → e-mail do usuário da API não
    configurado no painel da Clicksign (§1 do empírico — 403 ≠ 401, quem barrou foi a camada seguinte);
    429 → limite de requisições atingido, tentar de novo em instantes; demais → prefixo `[Clicksign]` + o
    `detail` da API. **O construtor jamais recebe, guarda ou concatena o token.**

    Criar `Tests\Fixtures\ClicksignSandboxFixtures` (arquivo `tests/Fixtures/ClicksignSandboxFixtures.php`) com
    métodos estáticos devolvendo arrays: `envelopeCriado()`, `documentoCriado()`, `signatarioCriado()`,
    `requisitoCriado()`, `envelopeAtivado()`, `envelopeCancelado()`, `eventoSignDoDocumento()`,
    `erroContentBase64NaoDataUri()`, `erro401TokenInvalido()`, `erro403EmailApiNaoConfigurado()`,
    `erro429NotificacaoTexto()`. Regras: (a) os payloads de ERRO são cópia literal do arquivo empírico
    (o do `content_base64` traz `code: bad_request`, `status: 400`, `source.pointer:
    /data/attributes/content_base64` e o `detail` exato); (b) os defaults de `envelopeCriado()` seguem §5 —
    `remind_interval: 3`, `deadline_partial_signature_action: "closed"`, `rubric_enabled: true`, status `draft`;
    (c) `eventoSignDoDocumento()` reproduz o bloco `data.signer` de §3 com `address` na faixa `203.0.113.0/24`;
    (d) todo identificador é o UUID sintético `00000000-0000-4000-8000-0000000000NN`; (e) todo e-mail é
    `@example.com`. Cada método leva docblock em pt-BR citando a seção do empírico de onde veio, e marcando
    explicitamente como **NÃO MEDIDO** qualquer campo sintetizado por ausência de registro literal — em
    particular o valor de `status` devolvido no cancelamento.

    Criar `tests/Feature/Phase126/ClicksignConfigEFixturesTest.php` cobrindo o bloco `<behavior>`. A varredura de
    PII roda sobre `json_encode()` de cada fixture (array, não texto de arquivo): regex de e-mail cujo domínio
    não seja `example.com`/`example.org`, regex de IPv4 fora de `203.0.113.`, e regex de UUID que não case com
    `00000000-0000-4000-8000-0000000000[0-9a-f]{2}`. Mensagem de falha em pt-BR citando o achado WR-07 da Fase 125.
  </action>
  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/ClicksignConfigEFixturesTest.php</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima passa com todos os casos verdes.
    - `grep -n "clicksign" config/services.php` retorna o bloco (hoje retorna vazio — Pitfall 5 do RESEARCH).
    - Nenhum arquivo criado nesta task contém a string `env(` fora de `config/services.php`.
    - `ClicksignException::fromResponse(401, [])->getMessage()` difere de `fromResponse(403, [])->getMessage()`.
  </acceptance_criteria>
  <done>Config, exceção e fixtures existem, com teste automatizado que barra PII real no repositório.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Núcleo do ClicksignClient — headers, retry, criarEnvelope e anexarDocumento</name>
  <files>
    app/Services/Clicksign/ClicksignClient.php,
    tests/Feature/Phase126/ClicksignClientTest.php
  </files>
  <read_first>
    - `app/Services/HubspotApiClient.php` — o molde estrutural (construtor com token injetável, `Log::channel('ecf-webhooks')` com só IDs + status, docblocks em pt-BR). ⚠️ **Copiar a ESTRUTURA, nunca o `Http::withToken()`** — ele prefixa `Bearer` e a Clicksign devolve 401 (medido).
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` §1 (headers obrigatórios, rate limit 20), §2 (`content_base64` precisa ser Data URI completo — a doc oficial está errada).
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-RESEARCH.md` §Pattern 1 e §Common Pitfalls 1 e 2.
    - `tests/Feature/Phase111HubspotApiClientTest.php` — o estilo de `Http::fake()` + asserção deste projeto.
  </read_first>
  <behavior>
    - Toda chamada envia `Authorization` com o token PURO (asserção: o header existe, é igual ao token e NÃO contém `Bearer`), mais `Accept` e `Content-Type` iguais a `application/vnd.api+json`.
    - `criarEnvelope()` faz `POST {base}/envelopes` e devolve o bloco `data` da resposta.
    - `anexarDocumento()` faz `POST {base}/envelopes/{id}/documents` com `content_base64` começando exatamente por `data:application/pdf;base64,`.
    - Resposta 429 é retentada (mais de uma requisição sai) e, esgotadas as tentativas, vira `ClicksignException` com `httpStatus` 429.
    - Resposta 500 é retentada.
    - Resposta 400 e 422 NÃO são retentadas (exatamente 1 requisição sai) e viram `ClicksignException`.
    - 401 e 403 produzem mensagens diferentes entre si.
    - PDF acima de `config('services.clicksign.max_upload_bytes')` lança `ClicksignException` ANTES de qualquer requisição (`Http::assertNothingSent()`).
  </behavior>
  <action>
    Criar `App\Services\Clicksign\ClicksignClient` em `app/Services/Clicksign/ClicksignClient.php`. Construtor com
    `?string $token = null` e `?string $baseUrl = null`, ambos com fallback para
    `config('services.clicksign.access_token')` e `config('services.clicksign.base_url')` — nunca `env()` direto.

    Método privado `baseRequest()`: monta `Http::withHeaders([...])` com exatamente os três headers do §1 do
    empírico e `->timeout(30)`. Docblock em pt-BR de UMA linha explicando por que não é `withToken()` (o teste de
    não-vazamento do plano remove comentários antes de varrer o arquivo, então o comentário é seguro).

    Método privado `enviar(string $metodo, string $caminho, array $payload = [], string $contexto = '')`: ponto único
    por onde TODA chamada passa. Responsabilidades: (1) executar a requisição com retry limitado a no máximo 3
    tentativas e espera crescente e curta (teto total abaixo de 5 s — o client roda dentro de um job, D-14, e não
    pode prender o worker); (2) retentar SOMENTE em 429, 5xx e falha de conexão, nunca em 4xx de dado (D-11) —
    o comportamento exigido é o do bloco `<behavior>`, use `Http::retry()` com callback `when` se ele disparar
    para status HTTP no Laravel 12; se não disparar, implemente um laço privado próprio com `usleep` limitado,
    mas o resultado observável não muda; (3) em erro, logar via `Log::channel('ecf-webhooks')->warning()` com
    contexto contendo APENAS `contexto`, `status`, `codigo` (`errors.0.code`) e `ponteiro`
    (`errors.0.source.pointer`) — **nunca** `$res`, `$res->body()`, `$res->json()` inteiro, `$e`, nem o payload
    enviado (a resposta de erro ecoa nome e e-mail do signatário — achado WR-11); (4) lançar
    `ClicksignException::fromResponse()`; (5) em sucesso, `->info()` com `contexto` + `status` + o `id` do recurso,
    nada mais.

    `criarEnvelope(array $atributos): array` — `POST /envelopes` com corpo JSON:API
    (`data.type = 'envelopes'`, `data.attributes = $atributos`), devolve `data`.

    `anexarDocumento(string $envelopeId, string $nomeArquivo, string $pdfBinario): array` — antes de qualquer
    requisição, comparar `strlen($pdfBinario)` com `config('services.clicksign.max_upload_bytes')` e lançar
    `ClicksignException` com mensagem pt-BR simples se exceder (gate #5). Montar `content_base64` como
    `'data:application/pdf;base64,' . base64_encode($pdfBinario)` — nunca base64 puro (§2 do empírico, a doc
    oficial erra aqui). `POST /envelopes/{id}/documents` com `data.type = 'documents'` e
    `data.attributes = ['filename' => $nomeArquivo, 'content_base64' => ...]`. Devolve `data`.

    Criar `tests/Feature/Phase126/ClicksignClientTest.php` cobrindo todo o bloco `<behavior>`, usando
    `Http::fake()` com as fixtures do `ClicksignSandboxFixtures` e `Http::assertSent()` para inspecionar os
    headers enviados e o prefixo do `content_base64`. Instanciar o client com o token literal
    `'token-clicksign-falso'` para que as asserções de header sejam exatas.
  </action>
  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/ClicksignClientTest.php</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima passa, com casos nomeados para: header sem `Bearer`, Data URI, retry em 429, retry em 500, ausência de retry em 400 e em 422, mensagens distintas de 401 e 403, e guarda de tamanho sem tráfego HTTP.
    - A varredura `Select-String -Pattern 'withToken\(' app/Services/Clicksign/ClicksignClient.php` só casa dentro de comentário (nenhuma chamada real).
    - Nenhuma chamada a `Log::` no client recebe `$res`, `$e` ou `$payload` inteiros.
  </acceptance_criteria>
  <done>Client base existe, com header correto, Data URI correto e retry disciplinado, tudo provado por teste.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prova de que nenhum log vaza o token (Success Criteria 1)</name>
  <files>
    tests/Feature/Phase126/ClicksignClientNaoVazaTokenTest.php,
    app/Services/Clicksign/ClicksignClient.php
  </files>
  <read_first>
    - `tests/Feature/Phase111HubspotApiClientTest.php` linhas 249-274 — o molde literal: mock de `Psr\Log\LoggerInterface` com `withArgs()` inspecionando mensagem E contexto, e `Log::shouldReceive('channel')->with('ecf-webhooks')`.
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-VALIDATION.md` §"A régua do CLICK-01".
    - `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-RESEARCH.md` §Pitfall 2 — o vetor real é `Response->transferStats->getRequest()` (`vendor/laravel/framework/src/Illuminate/Http/Client/Response.php:484`), não a mensagem da `RequestException`.
    - `tests/Feature/Phase125/MigrationsFase125ConvencoesTest.php` — o helper de remoção de comentários (a guarda estática desta task tem o mesmo problema de auto-invalidação).
  </read_first>
  <behavior>
    - Caminho de SUCESSO: nenhuma chamada de `info`/`warning`/`error` no canal `ecf-webhooks` contém o token nem a string `Bearer`, na mensagem ou no contexto serializado.
    - Caminho de ERRO (400, 401, 429 e 500): idem, e a `ClicksignException` propagada também não traz o token na mensagem.
    - Guarda estática: o código do client, **com comentários removidos**, não contém `withToken(`, `transferStats`, `->dump(`, `->dd(`, `handlerStats`, nem `json_encode($res`.
    - Guarda estática: nenhuma chamada `Log::channel('ecf-webhooks')->...` do client passa uma variável de resposta ou de exceção inteira como valor de contexto.
  </behavior>
  <action>
    Criar `tests/Feature/Phase126/ClicksignClientNaoVazaTokenTest.php` replicando o molde do
    `Phase111HubspotApiClientTest` linhas 249-274: mock de `Psr\Log\LoggerInterface` com `shouldReceive('warning')`,
    `shouldReceive('error')` e `shouldReceive('info')`, todos com `withArgs()` que devolve `false` se
    `str_contains` achar o token literal ou `Bearer ` na mensagem OU no `json_encode($context)`. Usar
    `Log::shouldReceive('channel')->with('ecf-webhooks')->andReturn($logger)`. O token de teste é uma string
    inconfundível, ex.: `token-clicksign-falso-nao-deve-vazar`.

    Cobrir os dois caminhos exigidos pela VALIDATION: (a) `Http::fake()` devolvendo sucesso e chamando
    `criarEnvelope()` + `anexarDocumento()`; (b) `Http::fake()` devolvendo 400, 401, 429 e 500 (fixtures da
    Task 1), capturando a `ClicksignException` e asserindo que `$e->getMessage()` também não contém o token.

    Acrescentar no MESMO arquivo a guarda estática do bloco `<behavior>`: ler
    `app/Services/Clicksign/ClicksignClient.php`, remover blocos `/* */` e linhas `//` com o mesmo par de
    `preg_replace` do `MigrationsFase125ConvencoesTest::migrationSemComentarios()` — **passo obrigatório**,
    porque o próprio client documenta em comentário por que não usa `withToken()`; sem remover, a guarda
    acusaria a si mesma e seria inútil por construção. Cada asserção leva mensagem de falha em pt-BR citando
    o vetor real (`transferStats->getRequest()`).

    Se alguma asserção reprovar, ajustar o client — extrair campos nomeados antes de logar — e não afrouxar
    o teste.
  </action>
  <verify>
    <automated>& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/ClicksignClientNaoVazaTokenTest.php</automated>
  </verify>
  <acceptance_criteria>
    - O comando acima passa, com pelo menos um caso de sucesso e um caso por status de erro (400, 401, 429, 500).
    - Introduzir deliberadamente `Log::channel('ecf-webhooks')->warning('x', ['res' => $res]);` no client faz o teste FALHAR (confirmar e reverter).
    - A guarda estática usa remoção de comentários antes de qualquer `assertStringNotContainsString`.
  </acceptance_criteria>
  <done>O Success Criteria 1 está provado por teste automatizado que cobre sucesso e erro, mensagem e contexto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ECF Admin → API Clicksign | Credencial de terceiro (token de longa duração) atravessa em todo request |
| ClicksignClient → arquivos de log (`storage/logs`, canal `ecf-webhooks`, retenção 14 dias) | O que for logado fica em disco no VPS e pode ser lido por qualquer pessoa com acesso ao servidor |
| Repositório git (compartilhado, mais de um dev) | Fixtures e config versionados — o que entrar aqui é permanente no histórico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Information Disclosure | `ClicksignClient::enviar()` | mitigate | Logar apenas `contexto`, `status`, `errors.0.code` e `errors.0.source.pointer`; proibido passar `$res`/`$e`/payload inteiros (vetor real: `Response->transferStats->getRequest()`). Provado por `ClicksignClientNaoVazaTokenTest` em sucesso e erro |
| T-126-02 | Information Disclosure | Token em código/config | mitigate | Token só em `.env` → `config('services.clicksign.access_token')`; `env()` proibido dentro do service; `.env.example` sem valor |
| T-126-03 | Information Disclosure | `tests/Fixtures/ClicksignSandboxFixtures.php` | mitigate | RFC 5737 (`203.0.113.0/24`), UUID sintético e e-mails `@example.com` (D-15); `ClicksignConfigEFixturesTest` reprova qualquer padrão de PII — resposta direta ao achado WR-07 da Fase 125 |
| T-126-04 | Information Disclosure | Resposta de erro da Clicksign ecoando o payload enviado | mitigate | Só `errors[].detail`/`source.pointer` entram na exceção; o payload enviado nunca é logado (achado WR-11) |
| T-126-05 | Denial of Service | Rate limit medido de 20 requisições | mitigate | Retry no máximo 3 vezes, só em 429/5xx, com espera curta e teto abaixo de 5 s; nenhuma chamada redundante no núcleo |
| T-126-SC | Tampering | Instalação de pacotes | accept | Nenhum pacote novo nesta fase — `barryvdh/laravel-dompdf` e `dompdf/dompdf` já estão no `composer.lock` e o cliente HTTP é o nativo do Laravel (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `& "C:\xampp\php\php.exe" vendor/bin/phpunit --testdox tests/Feature/Phase126/` — verde (só os arquivos da fase; NUNCA a suíte inteira).
- `grep -n "clicksign" config/services.php` retorna o bloco novo.
- Revisão manual: nenhuma linha de `Log::` no client recebe objeto inteiro.
</verification>

<success_criteria>
- Client base responde às duas primeiras chamadas do envelope com header e `content_base64` corretos.
- Retry só em 429/5xx, provado por contagem de requisições.
- Nenhum log com o token em nenhum dos caminhos testados.
- Nenhuma PII real entrou no repositório.
</success_criteria>

<output>
Create `.planning/phases/126-client-clicksign-pdf-do-contrato-v22-0/126-01-SUMMARY.md` when done.
⚠️ Commit com caminhos explícitos (`git commit -- <arquivos>`), nunca `git add -A`/`git add .` —
a árvore é compartilhada com outro dev e outras sessões.
</output>

