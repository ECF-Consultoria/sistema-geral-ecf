---
phase: 69-backend-regras-de-neg-cio-c-lculo-e-dispatch
milestone: v15.0
plan: 69-06
type: execute
wave: 4
depends_on: [69-01, 69-02, 69-03, 69-04, 69-05]
files_modified:
  - tests/Feature/Phase69/NpsPhase69IntegrationTest.php
autonomous: true
requirements: [NPS-B-01, NPS-B-02, NPS-B-03, NPS-B-04, NPS-B-05]
tags: [nps, e2e, integracao, cross-plan, phase69, verification]

must_haves:
  truths:
    - "Fluxo E2E público completo: dispatch mensal cria survey com template_id → cliente responde /nps/{token} com payload dinâmico → NpsResponseAnswer gravado com snapshot → NpsScoreCalculator lê média por dimensão consistente"
    - "Fluxo E2E generate manual: admin gera link → survey com template_id → cliente responde → answers gravadas → NpsScoreCalculator lê nota"
    - "Fluxo E2E disparo mensal com 3 empresas mistas: 1 com scope match, 1 fallback default, 1 sem template — só as 2 primeiras recebem survey, terceira gera Log::warning, comando termina exit 0"
    - "Fluxo E2E dedup 23000: 2ª tentativa de completar survey com mesma tupla (company_id, month_reference, template_id) renderiza AlreadyCompleted em vez de crashar"
    - "Fluxo E2E validação dinâmica: template com pergunta obrigatória → cliente submete sem essa answer → 422 com erro estruturado"
  artifacts:
    - path: tests/Feature/Phase69/NpsPhase69IntegrationTest.php
      provides: "Suite Feature de integração com 5 fluxos E2E cobrindo os 5 SC do ROADMAP"
      min_lines: 260
  key_links:
    - from: tests/Feature/Phase69/NpsPhase69IntegrationTest.php
      to: NpsDispararMensal + NpsController + NpsTemplateService + NpsScoreCalculator + NpsResponseAnswer
      via: Artisan::call + POST /nps/{token} + get() Inertia + assert em nps_response_answers + NpsScoreCalculator::compute()
      pattern: "Artisan::call.*disparar-mensal.*NpsScoreCalculator"
---

<objective>
Entregar suite E2E que integra os 5 plans anteriores em fluxos verticais end-to-end, provando que:
1. `NpsTemplateService` + `NpsController::generate` + gravação snapshot + `NpsScoreCalculator` funcionam em cadeia (SC#1 + SC#2)
2. `NpsDispararMensal` + guard `RuntimeException` + `Log::warning` funcionam em batch heterogêneo (SC#4)
3. Guard `QueryException 23000` em race real (2 pendings mesma tupla) renderiza AlreadyCompleted (SC#3)
4. Validação dinâmica bloqueia payload inválido em fluxo público completo (SC#5)

Purpose: Testes unitários dos plans anteriores validam cada peça isoladamente. Este plan valida que a CADEIA funciona: um bug de integração (por exemplo, dispatch grava template_id mas submit não persiste answer com snapshot, ou o dedup dispara mas controller não pega, ou score calculator lê da tabela errada) fica invisível nos unit tests e visível apenas aqui.

Output:
- 1 suite Feature em `tests/Feature/Phase69/NpsPhase69IntegrationTest.php` com 5 fluxos E2E
- Consolidação final: `php artisan test tests/Feature/Phase69/` retorna 5+6+5+5+7+5 ≈ 33 tests verdes
- Regressão zero em Phase 31 (19) + Phase 33 (9) + Phase 68 (23) — total ~84 tests verdes preservados na milestone
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/PHASE-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-01-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-02-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-03-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-04-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-05-SUMMARY.md
@app/Services/Nps/NpsTemplateService.php
@app/Services/Nps/NpsScoreCalculator.php
@app/Http/Controllers/NpsController.php
@app/Console/Commands/NpsDispararMensal.php
@app/Models/NpsSurvey.php
@app/Models/NpsResponseAnswer.php
@tests/Feature/Phase31NpsDispararMensalTest.php
@tests/Feature/Phase31NpsSubmitTest.php

<interfaces>
Este plan não cria novas APIs — apenas integra os artefatos entregues pelos plans 69-01/02/03/04/05:
- Service `App\Services\Nps\NpsTemplateService::resolveForCompany(Company): NpsTemplate`
- Service `App\Services\Nps\NpsScoreCalculator::compute(NpsResponse, string): ?float`
- Controller `NpsController::generate` — POST /nps/generate com template_id populado
- Controller `NpsController::submitResponse` — POST /nps/{token} com discriminação v15/legacy
- Command `nps:disparar-mensal` — Artisan::call('nps:disparar-mensal')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite E2E — 5 fluxos verticais integrando os 5 plans anteriores</name>
  <read_first>
    - tests/Feature/Phase31NpsDispararMensalTest.php (padrão de teste Artisan::call + Mail::fake)
    - tests/Feature/Phase31NpsSubmitTest.php (padrão de teste POST /nps/{token})
    - .planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-01-SUMMARY.md
    - .planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-02-SUMMARY.md
    - .planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-03-SUMMARY.md
    - .planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-04-SUMMARY.md
    - .planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-05-SUMMARY.md
    - app/Models/NpsResponseAnswer.php (colunas para assertion)
    - app/Models/NpsSurvey.php (relação template)
  </read_first>
  <files>tests/Feature/Phase69/NpsPhase69IntegrationTest.php</files>
  <action>
    Criar `tests/Feature/Phase69/NpsPhase69IntegrationTest.php` (namespace `Tests\Feature\Phase69`) com 5 fluxos E2E. `use RefreshDatabase` + `Mail::fake()` onde necessário + `Log::spy()` no fluxo 3.

    **Fluxo 1 — `dispatch_mensal_completo_grava_answers_com_snapshot_e_calculator_le_media`:**
    - Setup: cria template padrão (is_default=true) com 3 perguntas (dim=estrategista pesos 1..5, dim=analista pesos 1..5, dim=empresa pesos 1..5); cria empresa ativa com email_cliente + estrategista + analista + created_at=hoje
    - Ato 1 (dispatch): Artisan::call('nps:disparar-mensal'); verifica NpsSurvey criado com template_id = padrao.id
    - Ato 2 (submit): pega token do survey, POST /nps/{token} com `answers: {q_est: opt_peso_4, q_ana: opt_peso_3, q_emp: opt_peso_5, respondent_name: 'Cliente Teste'}`; verifica 200 + Nps/ThankYou renderizado
    - Ato 3 (assertion snapshot): NpsResponseAnswer::count() === 3; cada answer com question_dimensao_snapshot correto e option_peso_snapshot congelado
    - Ato 4 (calculator): NpsScoreCalculator::compute($response, 'estrategista') === 4.0; ('analista') === 3.0; ('empresa') === 5.0; ('geral') === null (não tem pergunta desta dim)

    **Fluxo 2 — `generate_manual_cria_survey_com_template_e_ciclo_de_resposta_completo`:**
    - Setup: cria admin + template padrão + empresa ativa
    - Ato 1: actingAs(admin), POST /nps/generate {company_id}; verifica survey criado com template_id populado
    - Ato 2: extrai token; POST /nps/{token} com payload v15.0 válido; verifica 200 ThankYou
    - Ato 3: assertion — 3 NpsResponseAnswer com snapshot; NpsScoreCalculator::compute retorna valores consistentes

    **Fluxo 3 — `dispatch_batch_3_empresas_mistas_pula_a_sem_template_e_loga_warning`:**
    - Setup: template padrão is_default=true + template especifico priority=10 vinculado ao Servico A; 3 empresas ativas com email + estrategista + created_at=hoje:
      - Empresa #1: contrato ativo Servico A → deveria receber template especifico
      - Empresa #2: sem serviço → cai no default
      - Empresa #3: sem serviço + `NpsTemplate::where('is_default', true)->update(['is_default' => false])` só POR ESTA EMPRESA — na verdade não é possível "por empresa"; solução alternativa: DELETE do template padrão + criar apenas o template especifico (empresas #2 e #3 sem match viraram órfãs). MAS Empresa #1 tem match e ainda funciona.
      - Alternativa melhor: Fluxo E2E com só 2 empresas (uma com scope match, uma cai no default) + 3ª empresa em cenário separado no mesmo teste com estado alterado. **Simplificação:** fazer 2 empresas com dispatch OK + 1 empresa em teste separado que simula "sem template" via delete transitório da default:
        ```
        // Cenário 3a: 2 empresas OK (scope match + default fallback)
        Artisan::call('nps:disparar-mensal');
        // assert 2 surveys criadas com template_id populado
        // Cenário 3b: mesmo comando executado depois do DELETE do template default
        NpsTemplate::query()->delete();
        // cria 1 empresa nova aniversariando hoje
        Log::spy();
        Artisan::call('nps:disparar-mensal');
        Log::shouldHaveReceived('warning')->atLeast(1);
        // assert 0 surveys criadas nessa segunda rodada
        ```
    - Espera: exit code SUCCESS em ambas rodadas; nenhuma exception atinge o CLI

    **Fluxo 4 — `dedup_23000_2a_completacao_mesmo_mes_template_renderiza_already_completed`:**
    - Setup: cria template com 3 perguntas; cria empresa; cria 2 surveys pending com **mesma tupla** (company_id=X, month_reference=YYYY-MM-01, template_id=Y)
    - Ato 1: POST /nps/{token1} com payload válido; verifica 200 ThankYou; NpsSurvey #1 agora completed
    - Ato 2: POST /nps/{token2} com payload válido; verifica 200 + Inertia component === 'Nps/AlreadyCompleted' (via `$response->assertInertia(fn ($inertia) => $inertia->component('Nps/AlreadyCompleted'))`); NpsSurvey #2 permanece pending (rollback da transação preservado)
    - Justificativa pt-BR no teste: valida research §2 + Plan 68-04 unique parcial + Plan 69-03 catch 23000

    **Fluxo 5 — `validacao_dinamica_bloqueia_submit_sem_pergunta_obrigatoria`:**
    - Setup: template com 2 perguntas: Q1 obrigatoria=true dim=estrategista; Q2 obrigatoria=false dim=analista; cria empresa + survey com template_id
    - Ato 1: POST /nps/{token} com payload OMITINDO Q1 (`answers: {q2_id: opt_id}`); verifica 422 com erro contendo `answers.{q1_id}` (ou string "required" no error bag)
    - Ato 2 (feliz caminho): POST com Q1 preenchido e Q2 omitido; verifica 200 ThankYou + 1 NpsResponseAnswer criada (apenas Q1)

    Comentários pt-BR em cada fluxo explicando o REQ coberto:
    - Fluxo 1 → SC#1 + SC#2 (NpsTemplateService + NpsScoreCalculator)
    - Fluxo 2 → SC#1 (generate + template_id)
    - Fluxo 3 → SC#4 (dispatch batch + guard RuntimeException + Log::warning)
    - Fluxo 4 → SC#3 (dedup 23000 → AlreadyCompleted)
    - Fluxo 5 → SC#5 (validação dinâmica)

    Todos com `use RefreshDatabase`. Rodar `php artisan test tests/Feature/Phase69/NpsPhase69IntegrationTest.php` — esperar 5/5 verdes. Depois rodar `php artisan test tests/Feature/Phase69/` — esperar ~33 verdes acumulados. Depois rodar `php artisan test tests/Feature/Phase31Nps* tests/Feature/Phase33NpsPerguntasExtrasTest.php tests/Feature/Phase68/` — todos verdes preservados. Commit: `test(69-06): E2E integration for Phase 69 (5 vertical flows)`.
  </action>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase69/NpsPhase69IntegrationTest.php`
    - `grep -c "function test_" tests/Feature/Phase69/NpsPhase69IntegrationTest.php` retorna >= 5
    - `grep -q "dispatch_mensal_completo_grava_answers_com_snapshot_e_calculator_le_media" tests/Feature/Phase69/NpsPhase69IntegrationTest.php`
    - `grep -q "generate_manual_cria_survey_com_template_e_ciclo_de_resposta_completo" tests/Feature/Phase69/NpsPhase69IntegrationTest.php`
    - `grep -q "dispatch_batch_3_empresas_mistas_pula_a_sem_template_e_loga_warning" tests/Feature/Phase69/NpsPhase69IntegrationTest.php`
    - `grep -q "dedup_23000_2a_completacao_mesmo_mes_template_renderiza_already_completed" tests/Feature/Phase69/NpsPhase69IntegrationTest.php`
    - `grep -q "validacao_dinamica_bloqueia_submit_sem_pergunta_obrigatoria" tests/Feature/Phase69/NpsPhase69IntegrationTest.php`
    - `grep -q "Artisan::call.*disparar-mensal" tests/Feature/Phase69/NpsPhase69IntegrationTest.php`
    - `grep -q "NpsScoreCalculator" tests/Feature/Phase69/NpsPhase69IntegrationTest.php`
    - `grep -q "AlreadyCompleted" tests/Feature/Phase69/NpsPhase69IntegrationTest.php`
    - `php artisan test tests/Feature/Phase69/NpsPhase69IntegrationTest.php` retorna exit code 0 com 5 passed
    - `php artisan test tests/Feature/Phase69/` retorna exit code 0 (≥ 30 passed acumulados)
    - `php artisan test tests/Feature/Phase31NpsSubmitTest.php tests/Feature/Phase31NpsDispararMensalTest.php tests/Feature/Phase31NpsMonthlyMailTest.php tests/Feature/Phase33NpsPerguntasExtrasTest.php tests/Feature/Phase68/` retorna exit code 0 (zero regressão)
    - Commit atômico `test(69-06):`
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Phase69/NpsPhase69IntegrationTest.php --stop-on-failure 2>&1 | tail -6</automated>
  </verify>
  <done>Suite E2E com 5 fluxos verdes. Suite Phase 69 completa (~33 tests) verde. Zero regressão em suite NPS legada (Phase 31: 19, Phase 33: 9, Phase 68: 23 — total 51 tests preservados). 1 commit `test(69-06):`.</done>
</task>

</tasks>

<threat_model>
Sem novas trust boundaries — este plan é puramente de verificação E2E dos artefatos entregues nos plans 69-01 a 69-05, cujos threat models já cobrem os riscos individuais.
</threat_model>

<verification>
- Suite E2E `tests/Feature/Phase69/NpsPhase69IntegrationTest.php` — 5/5 verdes
- Suite Phase 69 completa: 5 (69-01) + 6 (69-02) + 7 (69-03) + 5 (69-04) + 5 (69-05) + 5 (69-06) = 33 tests verdes acumulados
- Suite regressão NPS: Phase 31 (19) + Phase 33 (9) + Phase 68 (23) = 51 tests verdes preservados
- Total esperado: 33 + 51 = 84 tests verdes no domínio NPS
</verification>

<success_criteria>
Cobre os 5 SC do ROADMAP em fluxo E2E:
- SC #1 (NpsTemplateService) — Fluxo 1 + Fluxo 2 + Fluxo 3
- SC #2 (NpsScoreCalculator) — Fluxo 1 + Fluxo 2
- SC #3 (guard QueryException 23000) — Fluxo 4
- SC #4 (dispatch com skip-log) — Fluxo 3
- SC #5 (validação dinâmica) — Fluxo 5

Cobre todos os 5 REQs em integração: NPS-B-01, NPS-B-02, NPS-B-03, NPS-B-04, NPS-B-05.
</success_criteria>

<output>
Criar `.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-06-SUMMARY.md` documentando:
- 5 fluxos entregues e o REQ/SC coberto por cada
- Contagem consolidada de tests da Phase 69 (~33)
- Regressão zero validada em suite NPS legada (51 tests preservados)
- Conclusão: Phase 69 completa e pronta para Phase 70 arrancar (UI Config em `/nps/configuracao`)
</output>
