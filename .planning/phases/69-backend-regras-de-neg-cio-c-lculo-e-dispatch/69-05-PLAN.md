---
phase: 69-backend-regras-de-neg-cio-c-lculo-e-dispatch
milestone: v15.0
plan: 69-05
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - app/Console/Commands/NpsDispararMensal.php
  - tests/Feature/Phase69/NpsDispararMensalTemplateTest.php
autonomous: true
requirements: [NPS-B-04]
tags: [nps, comando-artisan, dispatch, template-resolver, batch, log-warning, phase69]

must_haves:
  truths:
    - "Comando nps:disparar-mensal chama NpsTemplateService::resolveForCompany por empresa e cria survey com template_id populado"
    - "Empresa sem template aplicável (RuntimeException do resolveForCompany) é PULADA com Log::warning estruturado — comando NÃO crasha o batch"
    - "Idempotência preservada: 2ª execução no mesmo dia com mesmo (company_id, month_reference, template_id) NÃO cria survey duplicado"
    - "Empresa com template válido + sem estrategista continua sendo pulada com log (compat Phase 31 D-07)"
    - "Contador puladosSemTemplate exposto no summary final CLI"
  artifacts:
    - path: app/Console/Commands/NpsDispararMensal.php
      provides: "método handle() atualizado usando NpsTemplateService + contador puladosSemTemplate"
      min_lines_delta: 20
    - path: tests/Feature/Phase69/NpsDispararMensalTemplateTest.php
      provides: "Suite Feature com 5 testes cobrindo template happy path + fallback + skip + idempotência"
      min_lines: 180
  key_links:
    - from: app/Console/Commands/NpsDispararMensal.php::handle
      to: app/Services/Nps/NpsTemplateService.php
      via: DI via constructor
      pattern: "NpsTemplateService"
    - from: NpsSurvey::create
      to: NpsTemplate resolvido
      via: coluna template_id
      pattern: "'template_id'"
---

<objective>
Atualizar `App\Console\Commands\NpsDispararMensal::handle()` para:
1. Chamar `NpsTemplateService::resolveForCompany($empresa)` por empresa elegível
2. Associar `template_id` na criação do `NpsSurvey`
3. Guardar `RuntimeException` do resolver (empresa sem template aplicável E sem seed default) num `try/catch` local — logar `Log::warning` estruturado + pular a empresa individual + **continuar o batch** (não crasha o comando inteiro)
4. Adicionar contador `puladosSemTemplate` exposto no summary CLI

Purpose: Sem este plan, o comando ainda cria survey sem template_id — quebrando o dedup unique parcial da Plan 68-04 (que exige template_id não-null para bloquear duplicatas COMPLETED). Sem o guard `RuntimeException` do resolver, uma corrupção no seed (empresa isolada sem template default) derrubaria o batch inteiro do mês, causando SLA de dispatch.

Output:
- 1 comando `handle()` atualizado (DI injection + resolve + guard + contador)
- 1 suite Feature em `tests/Feature/Phase69/NpsDispararMensalTemplateTest.php`
- Zero regressão em `Phase31NpsDispararMensalTest`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/PHASE-SUMMARY.md
@.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-01-SUMMARY.md
@app/Console/Commands/NpsDispararMensal.php
@app/Services/Nps/NpsTemplateService.php
@app/Models/NpsSurvey.php
@app/Models/Company.php
@tests/Feature/Phase31NpsDispararMensalTest.php

<interfaces>
<!-- Contratos já disponíveis -->

From app/Console/Commands/NpsDispararMensal.php (código atual — Phase 31 + 32):
- Sem constructor (Command herda default do Laravel)
- `signature = 'nps:disparar-mensal {--dry-run}'`
- Handle itera `Company::where('active', true)->whereNotNull('email_cliente')->chunkById(50, ...)`
- Idempotência atual: `NpsSurvey::where('company_id', $empresa->id)->whereDate('month_reference', $mesAtual)->exists()` — Phase 69 NÃO precisa mudar isso (o dedup unique parcial da Plan 68-04 é defesa complementar em nível DB para o caso EDGE de 2 completions simultâneas)
- Guard skip por sem estrategista (linha 108-115) — preservar

From app/Services/Nps/NpsTemplateService.php (Plan 69-01):
- método `resolveForCompany(Company): NpsTemplate` — lança RuntimeException se seed default estiver ausente

From app/Models/NpsSurvey.php:
- $fillable inclui `template_id` (Phase 68 update); coluna nullable no schema

**Idempotência dupla:**
1. Comando: `NpsSurvey::where('company_id', $id)->whereDate('month_reference', $mesAtual)->exists()` — impede re-run no mesmo dia
2. DB: unique parcial `(company_id, month_reference, template_id) WHERE status=completed AND completed_at NOT NULL` (Plan 68-04) — impede 2ª COMPLETION mesmo se algum erro criar 2 pending

Mission prompt fixa: "idempotência do batch preserva regra (company_id, month_reference) + agora template_id". O check atual usa apenas (company_id, month_reference) — como um dia = uma empresa = um template deterministicamente (D-01 clamp de aniversário do created_at), incluir template_id não muda o comportamento observável. Manter check simples (backward compat) — mudar é decisão fora do escopo. Documentar no summary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar suite Feature RED cobrindo template resolver no batch + skip com log</name>
  <read_first>
    - app/Console/Commands/NpsDispararMensal.php (código atual)
    - tests/Feature/Phase31NpsDispararMensalTest.php (padrão de teste do comando via artisan + Mail::fake + Log::spy)
    - app/Services/Nps/NpsTemplateService.php
    - app/Models/Company.php linhas 148-173 (relações estrategista/consultor)
    - database/factories/NpsTemplateFactory.php (state padrao)
  </read_first>
  <files>tests/Feature/Phase69/NpsDispararMensalTemplateTest.php</files>
  <behavior>
    - Test 1 `comando_dispara_survey_com_template_id_populado_do_resolver`: cria template padrão + 1 empresa ativa com email + estrategista + created_at=hoje; roda `Artisan::call('nps:disparar-mensal')`; espera `NpsSurvey::latest()->first()->template_id === $padrao->id`
    - Test 2 `comando_prefere_template_de_maior_priority_quando_ha_scope`: cria template padrão + template Y (priority=10, scope Servico A) + empresa com contrato ativo Servico A + created_at=hoje; roda comando; espera survey com `template_id === $y->id` (NÃO o padrão)
    - Test 3 `comando_pula_empresa_sem_template_e_loga_warning_estruturado`: cria empresa ativa com email + estrategista + created_at=hoje; ZERO templates na base (DELETE dos padrões via `NpsTemplate::query()->delete()`); usa `Log::spy()`; roda comando; espera `NpsSurvey::count() === 0` + espera Log::warning chamado com mensagem contendo "sem template aplicavel" ou "sem_template" ou "NpsTemplateService" (verificar via `Log::shouldHaveReceived('warning')`)
    - Test 4 `comando_nao_crasha_batch_quando_uma_empresa_sem_template`: 3 empresas ativas com email + estrategista + created_at=hoje; template padrão existe MAS empresa #2 tem serviço que aponta pra template desativado E sem match de scope; usa fixture forçando skip do resolver na #2 via truque (por exemplo, criar template scope Servico Z para empresa #2 + desativar apenas o padrão temporariamente NÃO — mais simples: nunca criar o padrão + criar template scope específico para #1 e #3 apenas). Ajuste operável: garantir que #2 caia em RuntimeException. Espera exit code = 0 (SUCCESS), 2 surveys criadas (#1 e #3), 1 warning logado (#2)
    - Test 5 `comando_e_idempotente_quando_re_run_no_mesmo_dia_com_template_id`: template padrão + empresa com created_at=hoje; roda comando 2x consecutivos; espera exatamente 1 survey criada (idempotência preservada)

    Todos com `use RefreshDatabase` + `Mail::fake()` + `Log::spy()`. Comentários pt-BR justificando setup de aniversário via `Company::factory()->create(['created_at' => now()->startOfDay()])`.
  </behavior>
  <action>Criar `tests/Feature/Phase69/NpsDispararMensalTemplateTest.php` (namespace `Tests\Feature\Phase69`). Rodar teste — pelo menos Tests 1/2/3 falham por `template_id === null` (nunca é populado no código atual). Commit: `test(69-05): add failing dispatch template tests`.</action>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase69/NpsDispararMensalTemplateTest.php`
    - `grep -c "function test_" tests/Feature/Phase69/NpsDispararMensalTemplateTest.php` retorna >= 5
    - `grep -q "comando_pula_empresa_sem_template_e_loga_warning_estruturado" tests/Feature/Phase69/NpsDispararMensalTemplateTest.php`
    - `grep -q "comando_nao_crasha_batch_quando_uma_empresa_sem_template" tests/Feature/Phase69/NpsDispararMensalTemplateTest.php`
    - `php artisan test tests/Feature/Phase69/NpsDispararMensalTemplateTest.php` retorna exit code != 0 (RED)
    - Commit atômico `test(69-05):`
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Phase69/NpsDispararMensalTemplateTest.php 2>&1 | grep -E "FAIL|Failed" | head -3</automated>
  </verify>
  <done>Suite RED entregue com pelo menos 3 testes falhando (template_id ausente / batch crashing / warning ausente). 1 commit `test(69-05):`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atualizar NpsDispararMensal — DI + resolve + guard + contador — GREEN</name>
  <read_first>
    - tests/Feature/Phase69/NpsDispararMensalTemplateTest.php (RED da Task 1)
    - app/Console/Commands/NpsDispararMensal.php (código atual — linhas 46-243)
    - app/Services/Nps/NpsTemplateService.php (contract do resolveForCompany)
    - tests/Feature/Phase31NpsDispararMensalTest.php (não quebrar — 7/7 tests preservados)
  </read_first>
  <files>app/Console/Commands/NpsDispararMensal.php</files>
  <action>
    Modificar `NpsDispararMensal` cirurgicamente:

    1. **Adicionar constructor DI:** injetar `NpsTemplateService`. Padrão Laravel Command aceita DI via constructor. Adicionar:
       ```php
       public function __construct(
           private \App\Services\Nps\NpsTemplateService $templateService
       ) {
           parent::__construct();
       }
       ```
       Sem parent no lugar → o comando não é registrado. Confirmar via smoke `php artisan list | grep disparar-mensal` no fim.

    2. **Adicionar contador puladosSemTemplate** ao bloco de contadores no topo do handle():
       ```php
       $puladosSemTemplate = 0;
       ```

    3. **Chamar resolveForCompany + guard RuntimeException DENTRO do foreach empresa** — colocar ANTES do bloco de estrategista (para pular cedo se nem tem template):
       ```php
       // Phase 69 NPS-B-04 — resolve template aplicavel ANTES de qualquer outro trabalho.
       // Empresa sem template (nem padrao aplicavel) é pulada com log estruturado —
       // o batch NÃO crasha, apenas continua para próxima empresa.
       try {
           $template = $this->templateService->resolveForCompany($empresa);
       } catch (\RuntimeException $e) {
           Log::warning("[NPS Mensal] empresa {$empresa->id} ({$empresa->name}) sem template aplicavel — pulando disparo", [
               'company_id' => $empresa->id,
               'company_name' => $empresa->name,
               'reason' => $e->getMessage(),
           ]);
           $puladosSemTemplate++;
           continue;
       }
       ```
       Colocar logicamente APÓS os guards de idempotência + estrategista (para o warning só aparecer quando a empresa é elegível de fato) — melhor posicionar imediatamente ANTES do `$dryRun` check e do `NpsSurvey::create`, e DEPOIS de `$puladosIdempotencia++` guard + `$puladosSemEstrategista++` guard. Semântica: só reporta "sem template" para empresas que passariam pela linha do dispatch. Ver comentário inline.

    4. **Adicionar `'template_id' => $template->id`** ao array do `NpsSurvey::create(...)`. Preservar todas as outras chaves atuais (token, company_id, generated_by=null, expires_at, status=pending, month_reference, auto_generated=true).

    5. **Adicionar `$puladosSemTemplate` ao summary final CLI** (após o bloco `$puladosSemEstrategista`):
       ```
       if ($puladosSemTemplate > 0) {
           $this->line("  ↳ {$puladosSemTemplate} pulada(s) por nao ter template NPS aplicavel.");
       }
       ```
       E adicionar ao `Log::info` final:
       ```
       Log::info("[NPS Mensal] Concluído: {$criados} surveys, {$enviados} emails, {$elegiveisHoje} elegíveis, {$puladosIdempotencia} idempotentes, {$puladosSemEstrategista} sem estrategista, {$puladosSemTemplate} sem template");
       ```

    6. **NÃO alterar** o check de idempotência atual `NpsSurvey::where('company_id', ...)->whereDate('month_reference', ...)` — comportamento preserva Phase 31 D-12. Adicionar template_id ao check seria uma decisão fora do escopo (mission prompt sugere manter). Se time descobrir edge-case futuro, virá em outra phase. Documentar essa decisão no SUMMARY.

    7. **Preservar 100%** do resto do fluxo (Mail::send, NpsEmailEnvio::create, try/catch $mailErr).

    Rodar `php artisan test tests/Feature/Phase69/NpsDispararMensalTemplateTest.php` — 5/5 verdes. Rodar regressão: `php artisan test tests/Feature/Phase31NpsDispararMensalTest.php tests/Feature/Phase31NpsMonthlyMailTest.php tests/Feature/Phase31NpsSubmitTest.php tests/Feature/Phase33NpsPerguntasExtrasTest.php tests/Feature/Phase68/` — todos verdes preservados.

    Rodar smoke: `php artisan list | grep -i disparar` — deve listar `nps:disparar-mensal`.

    Commit: `feat(69-05): NpsDispararMensal uses NpsTemplateService with skip-log guard`.
  </action>
  <acceptance_criteria>
    - `grep -q "NpsTemplateService" app/Console/Commands/NpsDispararMensal.php`
    - `grep -q "resolveForCompany" app/Console/Commands/NpsDispararMensal.php`
    - `grep -q "catch (\\\\RuntimeException" app/Console/Commands/NpsDispararMensal.php` (ou `catch (RuntimeException` com use import)
    - `grep -q "puladosSemTemplate" app/Console/Commands/NpsDispararMensal.php`
    - `grep -q "'template_id'" app/Console/Commands/NpsDispararMensal.php`
    - `grep -q "sem template" app/Console/Commands/NpsDispararMensal.php`
    - `grep -q "public function __construct" app/Console/Commands/NpsDispararMensal.php` (constructor DI adicionado)
    - `grep -q "parent::__construct" app/Console/Commands/NpsDispararMensal.php`
    - `php artisan list 2>&1 | grep -q "nps:disparar-mensal"` (comando ainda registrado após DI)
    - `php artisan test tests/Feature/Phase69/NpsDispararMensalTemplateTest.php` retorna exit code 0 com 5 passed
    - `php artisan test tests/Feature/Phase31NpsDispararMensalTest.php tests/Feature/Phase31NpsMonthlyMailTest.php tests/Feature/Phase31NpsSubmitTest.php tests/Feature/Phase33NpsPerguntasExtrasTest.php tests/Feature/Phase68/` retorna exit code 0 (zero regressão)
    - Commit atômico `feat(69-05):`
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Phase69/NpsDispararMensalTemplateTest.php --stop-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>Comando atualizado, 5/5 tests verdes, zero regressão Phase 31/33/68 (~50+ tests preservados), comando ainda registrado no Artisan. 1 commit `feat(69-05):`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler cron → command | Comando roda em contexto trusted do worker Laravel — não recebe input externo além de --dry-run |
| command → service | Service recebe Company já filtrada do banco — sem input não-confiável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-05-01 | DoS | RuntimeException do resolver derrubaria batch inteiro | mitigate | try/catch local por empresa + continue — testado explicitamente por Test 4 |
| T-69-05-02 | Data corruption | Survey criado sem template_id quebraria dedup Plan 68-04 | mitigate | template_id populado obrigatoriamente antes de NpsSurvey::create; se resolver falhar, comando pula em vez de criar survey degradado |
| T-69-05-03 | Repudiation | Skip silencioso "sem template" impediria audit trail | mitigate | Log::warning estruturado com company_id + company_name + reason — testado por Test 3 |
</threat_model>

<verification>
- Suite Feature `tests/Feature/Phase69/NpsDispararMensalTemplateTest.php` — 5/5 verdes
- Zero regressão Phase 31 DispararMensal (7 tests) + MonthlyMail (5 tests) + Submit (7 tests) + Phase 33 PerguntasExtras (9 tests) + Phase 68 (23 tests) — total 51+ tests verdes preservados
- Smoke `php artisan list` confirma comando ainda registrado
- Grep confirma padrões chave
</verification>

<success_criteria>
- SC #4 do ROADMAP: comando nps:disparar-mensal chama NpsTemplateService::resolveForCompany por empresa; empresas sem template puladas com Log::warning estruturado — comando NÃO crasha o batch
- REQ NPS-B-04 fechado
</success_criteria>

<output>
Criar `.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-05-SUMMARY.md` documentando:
- Diff aplicado no handle()
- Decisão de manter idempotência (company_id, month_reference) — documentar rationale
- Contador puladosSemTemplate exposto
- 5 casos cobertos + zero regressão
</output>
