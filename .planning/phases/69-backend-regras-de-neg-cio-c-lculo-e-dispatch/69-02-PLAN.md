---
phase: 69-backend-regras-de-neg-cio-c-lculo-e-dispatch
milestone: v15.0
plan: 69-02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Nps/NpsScoreCalculator.php
  - tests/Feature/Phase69/NpsScoreCalculatorTest.php
autonomous: true
requirements: [NPS-B-02]
tags: [nps, service, score-calculator, dimensao, snapshot, avg, laravel-12, phase69]

must_haves:
  truths:
    - "Given NpsResponse com N NpsResponseAnswer da dimensão pedida, NpsScoreCalculator::compute retorna round(AVG(option_peso_snapshot), 2) — média aritmética dos pesos snapshot"
    - "Given NpsResponse SEM answers da dimensão pedida, NpsScoreCalculator::compute retorna null (NÃO 0, NÃO exception)"
    - "Given dimensão inválida (fora do enum estrategista/analista/empresa/geral), NpsScoreCalculator::compute retorna null (defesa) — NÃO exception"
    - "Cálculo lê option_peso_snapshot (fonte de verdade histórica) — nunca da FK viva template_option_id"
  artifacts:
    - path: app/Services/Nps/NpsScoreCalculator.php
      provides: "Service com método público compute(NpsResponse, string): ?float"
      min_lines: 55
    - path: tests/Feature/Phase69/NpsScoreCalculatorTest.php
      provides: "Suite Feature com 6 testes cobrindo happy path + edge cases"
      min_lines: 160
  key_links:
    - from: app/Services/Nps/NpsScoreCalculator.php
      to: app/Models/NpsResponseAnswer.php
      via: "NpsResponseAnswer::where('response_id', ...)->where('question_dimensao_snapshot', ...)->avg('option_peso_snapshot')"
      pattern: "avg\\('option_peso_snapshot'\\)"
    - from: app/Services/Nps/NpsScoreCalculator.php
      to: database/migrations/2026_07_07_100001_create_nps_templates_v15_tables.php
      via: "índice composto nps_ans_response_dim_idx (response_id, question_dimensao_snapshot) — Plan 68-01 já criado"
      pattern: "response_id.*question_dimensao_snapshot"
---

<objective>
Entregar `App\Services\Nps\NpsScoreCalculator` com método público `compute(NpsResponse $response, string $dimensao): ?float` que calcula a média aritmética dos pesos snapshot das answers de uma dimensão pedida, sobre `nps_response_answers`. Este service é consumido por dashboards (Phase 72) e pelo cálculo de meta NPS (Phase 73 REQ NPS-F-03). Cobre 100% do REQ NPS-B-02.

Purpose: Sem este service, dashboards continuam lendo das colunas legadas `score_estrategista/analista/empresa` do `nps_responses` — o que quebra o comportamento "template configurável" da milestone. Este service é a única fonte de verdade para "nota por dimensão" no fluxo v15.0.

Output:
- 1 service novo em `app/Services/Nps/NpsScoreCalculator.php` (namespace `App\Services\Nps`, stateless)
- 1 suite Feature com 6 testes cobrindo happy path + edge cases (0 answers, dimensão inválida, dimensões mistas, precisão do round)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/v15-nps-templates-schema.md
@.planning/phases/68-schema-modelos-e-seed-retroativo-nps-padr-o/PHASE-SUMMARY.md
@app/Models/NpsResponse.php
@app/Models/NpsResponseAnswer.php
@app/Models/NpsTemplateQuestion.php
@app/Services/Metrics/UnifiedMetricsService.php
@database/factories/NpsResponseFactory.php
@database/factories/NpsResponseAnswerFactory.php
@database/factories/NpsSurveyFactory.php

<interfaces>
<!-- Contratos já disponíveis (Phase 68) — usar direto -->

From app/Models/NpsResponseAnswer.php:
- $fillable inclui: `response_id`, `template_question_id` (nullable), `template_option_id` (nullable), `question_texto_snapshot`, `question_dimensao_snapshot`, `option_label_snapshot`, `option_peso_snapshot` (int), `comentario` (nullable)
- casts: `option_peso_snapshot => int`
- relação `response()` BelongsTo NpsResponse (via `response_id`)

From app/Models/NpsTemplateQuestion.php:
- consts DIMENSAO_ESTRATEGISTA='estrategista', DIMENSAO_ANALISTA='analista', DIMENSAO_EMPRESA='empresa', DIMENSAO_GERAL='geral'
- array `DIMENSOES` com as 4 opções canônicas — usar para validação de dimensão passada como parâmetro

From app/Models/NpsResponse.php:
- $fillable: `survey_id`, `respondent_name`, `score_estrategista` (int nullable pós Plan 68-01), `score_analista` (int nullable), `score_empresa` (int nullable), `comment`
- relação `answers(): HasMany` — HasMany NpsResponseAnswer via `response_id`
- **CRÍTICO:** NÃO usar as colunas legadas `score_*` — cálculo v15.0 lê SEMPRE de `nps_response_answers.option_peso_snapshot`

From índice nps_ans_response_dim_idx (Plan 68-01):
- Composto `(response_id, question_dimensao_snapshot)` — otimiza a query `WHERE response_id=? AND question_dimensao_snapshot=?`
</interfaces>

<research_reference>
Research §1 (v15-nps-templates-schema.md) fixa: cálculo lê SEMPRE do snapshot, nunca da FK viva. FKs `template_question_id`/`template_option_id` são `nullOnDelete` — snapshot é a fonte de verdade histórica.

Research §5 fixa: `NpsScoreCalculator` faz `AVG(option_peso_snapshot)` uniforme sobre tipos `escala` e `opcoes` — sem branch por tipo. Peso range 1..5.

**Precisão:** retornar `?float` arredondado a 2 casas decimais via `round($media, 2)` — mesmo tratamento das colunas legadas em `NpsController::index` (linha 153/157/161 já usa `round(...avg..., 2)`).
</research_reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar suite Feature RED com 6 casos do calculator</name>
  <read_first>
    - .planning/research/v15-nps-templates-schema.md §1 e §5
    - app/Models/NpsResponseAnswer.php (colunas snapshot)
    - app/Models/NpsTemplateQuestion.php const DIMENSOES
    - database/factories/NpsResponseAnswerFactory.php (states)
    - database/factories/NpsResponseFactory.php
    - database/factories/NpsSurveyFactory.php (state completed/monthly)
    - tests/Feature/Phase68/NpsSchemaTest.php (padrão factory + RefreshDatabase)
  </read_first>
  <files>tests/Feature/Phase69/NpsScoreCalculatorTest.php</files>
  <behavior>
    - Test 1 `compute_retorna_media_de_multiplas_answers_da_dimensao`: cria NpsResponse; anexa 3 NpsResponseAnswer com question_dimensao_snapshot='estrategista' pesos [3, 4, 5]; espera compute($r, 'estrategista') == 4.0
    - Test 2 `compute_retorna_valor_exato_quando_uma_unica_answer`: 1 answer dimensao='empresa' peso=5; espera 5.0
    - Test 3 `compute_retorna_null_quando_zero_answers_da_dimensao`: NpsResponse com 2 answers dimensao='estrategista'; espera compute($r, 'analista') == null (não 0.0)
    - Test 4 `compute_ignora_answers_de_outras_dimensoes_na_media`: 2 answers dimensao='estrategista' pesos [4, 4] + 3 answers dimensao='empresa' pesos [1, 1, 1]; espera compute($r, 'estrategista') == 4.0 (não influenciado pelas de 'empresa')
    - Test 5 `compute_retorna_null_para_dimensao_invalida`: passa dimensão 'inexistente' string arbitrária; espera null (defesa — sem lançar exception)
    - Test 6 `compute_arredonda_para_2_casas_decimais`: 3 answers dimensao='geral' pesos [2, 3, 4] → média exata 3.0; e outro cenário [1, 2, 2] → média 1.6666... → espera 1.67 exato (round HALF_UP)

    Todos com `use RefreshDatabase`. Comentários pt-BR justificando o setup. Usar `NpsResponse::factory()->create()` para o response; para cada answer usar `NpsResponseAnswer::factory()->create(['response_id' => $r->id, 'question_dimensao_snapshot' => 'estrategista', 'option_peso_snapshot' => 3])` diretamente (bypassing states se necessário).
  </behavior>
  <action>Criar `tests/Feature/Phase69/NpsScoreCalculatorTest.php` (namespace `Tests\Feature\Phase69`). Injetar via `app(NpsScoreCalculator::class)`. Rodar `php artisan test tests/Feature/Phase69/NpsScoreCalculatorTest.php` — deve falhar com "Class 'App\\Services\\Nps\\NpsScoreCalculator' not found" (RED). Commit: `test(69-02): add failing NpsScoreCalculator tests`.</action>
  <acceptance_criteria>
    - `test -f tests/Feature/Phase69/NpsScoreCalculatorTest.php`
    - `grep -c "function test_" tests/Feature/Phase69/NpsScoreCalculatorTest.php` retorna >= 6
    - `grep -q "compute_retorna_null_quando_zero_answers_da_dimensao" tests/Feature/Phase69/NpsScoreCalculatorTest.php`
    - `grep -q "compute_arredonda_para_2_casas_decimais" tests/Feature/Phase69/NpsScoreCalculatorTest.php`
    - `php artisan test tests/Feature/Phase69/NpsScoreCalculatorTest.php` retorna exit code 1 com mensagem contendo "NpsScoreCalculator" (RED)
    - Commit atômico `test(69-02):`
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Phase69/NpsScoreCalculatorTest.php --stop-on-failure 2>&1 | grep -qE "(NpsScoreCalculator.*not found|Class.*NpsScoreCalculator)"</automated>
  </verify>
  <done>Suite RED entregue, 6 testes falham por "Class not found", 1 commit `test(69-02):`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar NpsScoreCalculator — GREEN dos 6 testes</name>
  <read_first>
    - tests/Feature/Phase69/NpsScoreCalculatorTest.php (RED da Task 1)
    - app/Models/NpsResponseAnswer.php ($table='nps_response_answers')
    - app/Models/NpsTemplateQuestion.php const DIMENSOES (whitelist)
    - .planning/research/v15-nps-templates-schema.md §1 e §5
    - app/Services/Metrics/UnifiedMetricsService.php (padrão docblock pt-BR de service)
  </read_first>
  <files>app/Services/Nps/NpsScoreCalculator.php</files>
  <action>
    Criar `app/Services/Nps/NpsScoreCalculator.php` com namespace `App\Services\Nps`. Classe stateless, sem constructor. Método público único:

    `public function compute(App\Models\NpsResponse $response, string $dimensao): ?float`

    Comportamento:
    1. Defesa: se `!in_array($dimensao, NpsTemplateQuestion::DIMENSOES, true)` → retorna `null` (não lança exception — defesa contra chamador passar dimensão string arbitrária)
    2. Query direta em `NpsResponseAnswer`:
       ```
       $media = NpsResponseAnswer::query()
           ->where('response_id', $response->id)
           ->where('question_dimensao_snapshot', $dimensao)
           ->avg('option_peso_snapshot');
       ```
    3. Se `$media === null` (zero rows agregadas — AVG SQL retorna null em conjunto vazio) → retorna `null`
    4. Retorna `round((float) $media, 2)`

    Docblock pt-BR de classe explicando:
    - Papel na Phase 69 (fonte de verdade de nota por dimensão na v15.0)
    - Referência ao research §1 (snapshot per-row é fonte de verdade — nunca ler FK viva) e §5 (AVG uniforme, sem branch por tipo)
    - Referência ao índice `nps_ans_response_dim_idx` (Plan 68-01) que otimiza a query
    - Nota sobre `null` semântico: "não há perguntas dessa dimensão neste response" (não erro, não zero)
    - Referência ao Phase 72 (dashboards vão consumir) e Phase 73 REQ NPS-F-03 (CalculateGoalResults)

    Comentário inline explicando por que `!in_array` STRICT (evita cast implícito string→bool que aceitaria 0/'' como válidos).

    Rodar `php artisan test tests/Feature/Phase69/NpsScoreCalculatorTest.php` — 6/6 verdes. Commit: `feat(69-02): implement NpsScoreCalculator::compute`.
  </action>
  <acceptance_criteria>
    - `test -f app/Services/Nps/NpsScoreCalculator.php`
    - `grep -q "namespace App\\\\Services\\\\Nps;" app/Services/Nps/NpsScoreCalculator.php`
    - `grep -q "public function compute" app/Services/Nps/NpsScoreCalculator.php`
    - `grep -q "avg('option_peso_snapshot')" app/Services/Nps/NpsScoreCalculator.php`
    - `grep -q "question_dimensao_snapshot" app/Services/Nps/NpsScoreCalculator.php`
    - `grep -q "in_array" app/Services/Nps/NpsScoreCalculator.php`
    - `grep -q "round(" app/Services/Nps/NpsScoreCalculator.php`
    - `grep -q "?float" app/Services/Nps/NpsScoreCalculator.php`
    - `php artisan test tests/Feature/Phase69/NpsScoreCalculatorTest.php` retorna exit code 0 com 6 passed
    - `php artisan test tests/Feature/Phase31Nps* tests/Feature/Phase33NpsPerguntasExtrasTest.php tests/Feature/Phase68/` retorna exit code 0 (zero regressão)
    - Commit atômico `feat(69-02):`
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Phase69/NpsScoreCalculatorTest.php --stop-on-failure 2>&1 | tail -5</automated>
  </verify>
  <done>Calculator implementado, 6/6 testes verdes, zero regressão Phase 31/33/68, 1 commit `feat(69-02):`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chamador → service | Callers (Phase 72 dashboards, Phase 73 CalculateGoalResults) passam `NpsResponse` já autenticada + `string $dimensao` que pode vir de request user (defesa via whitelist) |
| service → DB | Query builder Eloquent puro, zero SQL raw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-02-01 | Tampering | Parâmetro `$dimensao` string arbitrário | mitigate | Whitelist via `in_array($dimensao, NpsTemplateQuestion::DIMENSOES, true)` (strict) → retorna null; SQL builder faz binding parametrizado (sem interpolação) |
| T-69-02-02 | Information Disclosure | Retorno null vs zero pode expor "não tem pergunta desta dimensão" para consumidor não autorizado | accept | Consumidores (Phase 72/73) já aplicam guard de carteira antes de chamar compute; null é semântica de negócio, não vazamento |
</threat_model>

<verification>
- Suite Feature `tests/Feature/Phase69/NpsScoreCalculatorTest.php` — 6/6 verdes
- Zero regressão em `tests/Feature/Phase31Nps*.php`, `tests/Feature/Phase33NpsPerguntasExtrasTest.php`, `tests/Feature/Phase68/`
- Grep confirma padrões chave: `avg('option_peso_snapshot')`, `question_dimensao_snapshot`, `in_array`, `?float`, `round(`
</verification>

<success_criteria>
- SC #2 do ROADMAP: `NpsScoreCalculator::compute(NpsResponse, dimensao)` calcula média dos option_peso_snapshot; retorna null quando não há perguntas dessa dimensão
- REQ NPS-B-02 fechado
</success_criteria>

<output>
Criar `.planning/phases/69-backend-regras-de-neg-cio-c-lculo-e-dispatch/69-02-SUMMARY.md` documentando:
- Signature do método
- Estratégia de fallback null (semântica: "sem perguntas desta dimensão")
- Índice consumido (nps_ans_response_dim_idx da Plan 68-01)
- 6 casos cobertos
- Zero regressão confirmada
</output>
