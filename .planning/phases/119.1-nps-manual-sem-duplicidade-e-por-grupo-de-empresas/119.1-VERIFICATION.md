---
phase: 119.1
verified: 2026-07-30T12:20:00Z
status: human_needed
score: 13/13 must-haves verificados no código e por execução real da suíte
overrides_applied: 0
human_verification:
  - test: "Caso âncora ponta a ponta com dados reais — grupo Camillo Parts (Luis + Ana Julia), gerar o link de grupo em produção, enviar de fato ao cliente e conferir a resposta real"
    expected: "Cliente responde com nota 5 em todos os quesitos; TODAS as empresas do grupo Camillo Parts ficam com nota 5 no mês, nenhuma sem nota; nenhuma empresa fora do grupo é afetada"
    why_human: "Só verificável com o cadastro REAL do grupo Camillo Parts em produção (responsáveis reais atualmente atribuídos) e um envio/resposta reais — nenhum teste automatizado ou leitura de código substitui rodar o fluxo completo com o cliente real. Item já listado como 'Só-Manual' na 119.1-VALIDATION.md (NPSMAN-03) e ainda não reportado como executado em nenhum SUMMARY."
---

# Phase 119.1: NPS manual, sem duplicidade e por grupo de empresas — Verification Report

**Phase Goal:** O NPS deixa de sair sozinho e passa a ser um ato deliberado do responsável — agendamento automático desligado (comando continua manual), bloqueio de segundo link para a mesma empresa+modelo+mês, NPS de grupo com replicação por responsáveis coincidentes, e contrapartida: empresa elegível sem nenhum link também conta nota 1 (inversão deliberada de D3/Fase 116).

**Verificado:** 2026-07-30
**Status:** human_needed (todos os truths automatizáveis PASSARAM; resta 1 item que só é verificável com dados reais de produção)
**Re-verificação:** Não — verificação inicial.

## Metodologia

Esta verificação não confiou em nenhuma alegação de SUMMARY.md. Para cada um dos 8 focos de risco listados no prompt de verificação, o código-fonte real foi lido diretamente (serviços, controllers, migrations, rotas, testes) e, onde possível, as suítes de teste foram **executadas de fato** neste ambiente (`php artisan test`, PHP colocado no PATH via PowerShell/Git Bash, nunca em background por escolha própria — os 2 casos que estouraram o timeout de 120s foram movidos automaticamente pelo próprio ambiente, não por decisão desta verificação).

Suítes executadas e resultado real observado (não copiado de SUMMARY):

| Suíte | Resultado observado NESTA verificação | Bate com o alegado nos SUMMARYs? |
|---|---|---|
| `--filter=Phase116` | **79 passed** (639 assertions), 148s | Sim — bate exatamente (72 originais + 5 do Plan04 + 2 do Plan08 = 79) |
| `--filter=Phase119_1` | **108 passed** (776 assertions), 166s | Sim — bate exatamente com o SUMMARY 08 |
| `--filter=Phase118` | **36 passed** (146 assertions), 151s | Sim — baseline exata, zero regressão |
| `--filter=Nps` (suíte completa) | **5 failed / 543 passed** (3718 assertions), 903s | Sim — as 5 falhas são exatamente as herdadas (2 `V18\ConsolidarMesJanelaNpsTest`, 1 `V18\JanelaNpsBonusTest`, 2 `expires_at` de survey manual dependentes de data — confirmadas pelo texto de erro real capturado nesta execução) |
| `--filter=Desempenho` | **14 failed / 94 passed** (407 assertions), 149s | Sim — baseline herdada exata (commit `25a958b3` de outra sessão, `AdmanMetricDiffService`/`var_margem_pct` null), nenhuma relação com esta fase |

Nenhuma falha nova foi encontrada em nenhuma suíte. As baselines herdadas (Nps: 5 falhas; Desempenho: 14 falhas) são idênticas antes e depois desta fase, confirmando "zero regressão" com execução real, não com a palavra do SUMMARY.

## Achados por foco de risco (do prompt de verificação)

### 1. Risco financeiro central (NPSMAN-05) — arquitetura survey-âncora + N espelhos reais

**VERIFICADO.**

- `git log --oneline -- app/Services/Nps/NpsSnapshotService.php` mostra que o último commit que tocou o arquivo é `ef1c1b75` (Fase 79) / `ca41e26c` (fix anterior) — **nenhum commit da Fase 119.1 (43 commits conferidos) toca este arquivo**. `NpsGrupoReplicacaoService::replicar()` chama `app(NpsSnapshotService::class)->registrar($response)` uma vez por empresa-espelho, exatamente como uma resposta de cliente distinto — código lido diretamente em `app/Services/Nps/NpsGrupoReplicacaoService.php:152`.
- `tests/Feature/Phase119_1/NpsGrupoDedupeTest.php` prova a equivalência de forma rigorosa, não frouxa: `test_grupo_de_5_e_5_links_individuais_produzem_resultado_identico` compara a média do bônus e a contagem de `nps_score_assignments` distintos do cenário GRUPO contra o cenário de 5 links INDIVIDUAIS reais, usando `assertSame` contra a MESMA constante (não `assertGreaterThan(0)`). `test_replicacao_parcial_luis_ve_4_notas_e_divergente_conta_1_pelo_d1_para_o_responsavel_real` prova "nem mais, nem menos" também no caso parcial (4 de 5).
- Executado nesta verificação como parte de `--filter=Phase119_1` (108 passed) — os 4 testes de `NpsGrupoDedupeTest` estão entre eles.

### 2. Inversão de D3 sem destruir o contrapeso

**VERIFICADO.**

- `tests/Feature/Phase116/NpsFloorCarteiraTest.php` (e os demais 4 arquivos segmentados) têm lado a lado `test_..._empresa_nao_elegivel_sem_survey_continua_null` (D3 preservado) e `test_..._empresa_elegivel_sem_survey_agora_conta_nota_1` (D1 novo) — nenhum teste original foi apagado, só renomeado/complementado.
- Execução real desta verificação: `--filter=Phase116` = **79 passed**, batendo exatamente a progressão 72 → 77 (Plan 04) → 79 (Plan 08) documentada nos SUMMARYs.
- `NpsPorEmpresaService.php` (D-04) tem os 3 ramos nomeados no código: `janela_aberta` → `nao_elegivel` (null, fora do denominador) → `sem_nps` (1.0) — ordem fixa confirmada por leitura direta (linhas 295-297).

### 3. D1 em 6 de 6 consumidores

**VERIFICADO.**

- Confirmado por grep direto (não por alegação) que os 4 consumidores do Plano 09 realmente chamam `NpsSemLinkService`: `CompanyController.php:460-461`, `DashboardController.php:826/1399/1576` (3 ocorrências — bate com "5 blocos" reportado: `buildRanking`, `userDashboard`, e um terceiro bloco de widget), `PerformanceController.php:903-906`, `PortfolioController.php:2280-2283`. Somados a `DesempenhoScoreService` (bônus, Plan 03) e `NpsController::index()` (área NPS, Plan 04) = 6 consumidores.
- `tests/Feature/Phase119_1/NpsCoerenciaD1Test.php` cruza 5 consumidores num único cenário (bônus, área NPS, relatório por empresa, score por empresa, carteira); `tests/Feature/Phase116/NpsFloorRegressaoTest.php::test_cenario_espelho_elegivel_sem_survey_conta_1_em_todos_os_consumidores` fecha os 2 que faltam (ranking `DashboardController::buildRanking()` e meta `CalculateGoalResults`) e a página da empresa — juntos, os 6 consumidores estão cobertos por teste de execução real, não por afirmação.
- **Busca ativa por um 7º call-site (pedida explicitamente no prompt):** `grep -rl "notasDoUsuario|notasDaEmpresa|computeNpsMedio|notasNpsPorEmpresa"` retornou exatamente os arquivos já mapeados (`CompanyController`, `DashboardController`, `PerformanceController`, `PortfolioController`, `CalculateGoalResults`, `CompanyScoreService`, `NpsPorEmpresaService`, `NpsSemLinkService`, `DesempenhoScoreService`, `NpsImputationService`). **Nenhum sétimo consumidor órfão encontrado.**
- `CalculateGoalResults.php` — confirmado por leitura direta que `pisoRetroativo()` aparece **só em comentário** (linha 265, dentro do bloco que documenta DEC-09-C), nunca chamado. `grep -c "pisoRetroativo(" ` (com parêntese, chamada real) = 0.

### 4. D6 + D3 combinados no grupo (referência = maior classe, não menor id)

**VERIFICADO.**

- `NpsGrupoCoberturaService::escolherReferencia()` lido por completo: escolhe a classe de equivalência de MAIOR tamanho (maioria), com desempate por menor id só dentro de classes empatadas — nunca elege a empresa de menor id só por ser a primeira.
- `tests/Feature/Phase119_1/NpsGrupoCoberturaTest.php::test_maioria_vence_mesmo_quando_divergente_tem_menor_id` cria a divergente PRIMEIRO (garantindo menor id por construção, com `assertLessThan` provando a pré-condição) e confirma que as 3 concordantes entram e a referência nunca é a divergente. Teste passou na execução real (`--filter=Phase119_1`, 108 passed inclui este arquivo).
- D6 (os DOIS papéis) confirmado no código (`const PAPEIS = ['estrategista', 'consultor']`, comparado em `compativel()`/`encontrarDivergencia()` para TODO serviço da interseção) e no teste dedicado `test_analista_diferente_tambem_exclui_d6`.

### 5. Piso retroativo (DEC-09-B/C)

**VERIFICADO.**

- `NpsSemLinkService::pisoRetroativo()` retorna `now()->subMonthNoOverflow()->startOfMonth()`, aplicado só nos 4 consumidores de janela rolante (grep confirmado acima).
- `CalculateGoalResults.php` (meta, competência FIXA) — confirmado que NÃO chama `pisoRetroativo()`; a única ocorrência da string é o comentário nominal que documenta a proibição (DEC-09-C), exatamente como pedido no foco de verificação.

### 6. Agendamento certo desligado

**VERIFICADO.**

- `routes/console.php` lido linha a linha: o bloco `Schedule::command('nps:disparar-mensal')` foi substituído por um comentário extenso documentando D2; `Schedule::call(...)->name('nps-materializar-nao-respondidos')` continua intacto, com a janela restrita a `[now()->subMonthNoOverflow(), now()]` (mês anterior + corrente, Fase 116) preservada byte a byte.
- `NpsDispararMensal extends Command` confirmado presente e com `$signature` válido — comando continua invocável manualmente.

### 7. Regressão de migration (índice parcial de dedup)

**VERIFICADO.**

- Commit `973add64` lido por completo: adiciona `restaurarIndiceParcialDedupSeSqlite()`, chamado em `up()` E `down()`, com guard `if (driver !== sqlite) return` — ramo MySQL/MariaDB (o `Schema::table` original) **não foi tocado**, confirmado por diff.
- `->nullable()` explícito presente na FK `group_survey_id` (evita a armadilha MySQL 1830 documentada no projeto).
- `tests/Feature/Phase119_1/NpsSurveysDedupParcialSobreviveMigrationTest.php` existe e prova os dois lados (2 pending coexistem; 2 completed disparam `QueryException`) — confirmado passando na execução real (`--filter=Phase119_1`, 108 passed).

### 8. UI sem jargão

**VERIFICADO.**

- `grep -nio "imputa[cç][aã]o|assignment|replica[cç][aã]o|elegibilidade|compet[eê]ncia|provis[oó]rio|definitivo"` em `resources/js/Pages/Nps/Index.jsx` e `Respond.jsx` retornou **zero ocorrências**.
- Componentes `CoberturaPreview`, `MotivosExclusao`, estado `linkJaExistia`/`modoGeracao` confirmados presentes e conectados (não apenas declarados) no JSX.

## Observable Truths (must-haves consolidados: ROADMAP + 119.1-VALIDATION.md NPSMAN-01..13)

| # | Truth | Status | Evidência |
|---|---|---|---|
| 1 | NPSMAN-01 — Agendamento diário removido; comando segue manual | ✓ VERIFIED | `routes/console.php` lido; `NpsAgendamentoRemovidoTest` (13/13 no Plan01, incluído nos 108 passed) |
| 2 | NPSMAN-02 — Segundo link bloqueado (empresa+modelo+mês), inclusive manual | ✓ VERIFIED | `NpsController::generate()` linha 1157-1169; `NpsGeracaoDuplicidadeTest`; guard análogo em `NpsGrupoController::generate()` |
| 3 | NPSMAN-03 — NPS de grupo replica nota (caso Camillo Parts) | ✓ VERIFIED (automatizado) / ver human_verification | `NpsGrupoSurveyTest::test_resposta_do_grupo_replica_nota_maxima_para_todas_as_empresas_cobertas`; ponta a ponta REAL ainda pendente (ver seção Human Verification) |
| 4 | NPSMAN-04 — Divergente não recebe nota, mesmo com menor id | ✓ VERIFIED | `NpsGrupoCoberturaService::escolherReferencia()`; `test_maioria_vence_mesmo_quando_divergente_tem_menor_id` |
| 5 | NPSMAN-05 — Grupo não conta N vezes nem de menos, nas 2 réguas | ✓ VERIFIED | `NpsGrupoDedupeTest` (4 testes, equivalência exata grupo×individual); `NpsSnapshotService::registrar()` confirmado intocado |
| 6 | NPSMAN-06 — Empresa elegível sem link conta 1 (D1) | ✓ VERIFIED | `NpsSemLinkService`; `NpsFloorD1Test`; `NpsCoerenciaD1Test` cenário 1 |
| 7 | NPSMAN-07 — Empresa não elegível não conta 1 (contrapeso) | ✓ VERIFIED | `NpsCoerenciaD1Test` cenário 2 (4 sub-motivos); `NpsFloorRegressaoTest` D3 preservado |
| 8 | NPSMAN-08 — Prévia de cobertura visível antes de enviar, com motivo | ✓ VERIFIED | `CoberturaPreview`/`MotivosExclusao` em `Index.jsx`; `previewCobertura()` no controller; checkpoint humano aprovado (Plan 07, "aprovado", 2026-07-30) |
| 9 | NPSMAN-09 — Rotina diária restrita a mês anterior+corrente | ✓ VERIFIED | `routes/console.php`; `NpsMaterializarNaoRespondidosCommandTest` (2 casos novos) |
| 10 | NPSMAN-10 — Doc operacional atualizado (inversão D3 + grupo) | ✓ VERIFIED | `docs/nps-nao-respondido-nota-1.md` lido, cobre os pontos exigidos |
| 11 | NPSMAN-11 — D1 em 6/6 consumidores, suíte verde | ✓ VERIFIED | Grep dos 6 call-sites; `NpsCoerenciaD1Test` + `NpsFloorRegressaoTest`; execução real de todas as suítes relevantes sem regressão nova |
| 12 | NPSMAN-12 — Sem canal conta 1, tela explica o motivo | ✓ VERIFIED | `tem_canal` em `NpsElegibilidadeService::empresasElegiveis()`; motivo `sem_contato` em `NpsController::index()`; texto "Sem contato cadastrado" em `Index.jsx` |
| 13 | NPSMAN-13 — D6 (estrategista E analista) bate sempre | ✓ VERIFIED | `const PAPEIS` em `NpsGrupoCoberturaService`; `test_analista_diferente_tambem_exclui_d6` |

**Score:** 13/13 truths automatizáveis verificados no código E por execução real da suíte. 1 item (parte de NPSMAN-03) só é verificável com dados reais de produção — ver Human Verification abaixo.

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `app/Services/Nps/NpsElegibilidadeService.php` | Fonte única de elegibilidade | ✓ VERIFIED | Lido por completo; memo por mês confirmado (instância, não static); `tem_canal`/D5 confirmado |
| `app/Http/Controllers/NpsController.php::generate()` | Guard de duplicidade individual | ✓ VERIFIED | Linhas 1157-1169 lidas diretamente |
| `app/Services/Desempenho/NpsSemLinkService.php` | 4º ramo (D) do bônus + janela multi-mês | ✓ VERIFIED | Lido por completo; `pisoRetroativo()`, `notasDoUsuario()`, `notasDoUsuarioNaJanela()`, `notasDaEmpresaSemLink()` todos presentes e coerentes com a documentação |
| `app/Services/Nps/NpsGrupoCoberturaService.php` | Cálculo de cobertura de grupo (D3/D4/D6) | ✓ VERIFIED | 393 linhas lidas por completo — algoritmo de maioria/desempate confirmado correto |
| `app/Services/Nps/NpsGrupoReplicacaoService.php` | Fan-out em N espelhos reais | ✓ VERIFIED | Lido por completo; transação DB, recalcula cobertura no submit, chama `NpsSnapshotService::registrar()` sem modificá-lo |
| `app/Http/Controllers/NpsGrupoController.php` | Prévia, geração, form público, submit | ✓ VERIFIED | 362 linhas lidas por completo |
| `database/migrations/2026_07_29_100000_...` + `..._100001_...` | Tabela + coluna de vínculo de grupo | ✓ VERIFIED | Ambas lidas; nullable correto; fix do índice parcial presente |
| `resources/js/Pages/Nps/Index.jsx` / `Respond.jsx` | UI sem jargão, prévia de cobertura, roteamento de submit de grupo | ✓ VERIFIED | Grep de jargão = 0; componentes wired confirmados |
| `docs/nps-nao-respondido-nota-1.md` | Doc operacional atualizado | ✓ VERIFIED | Lido, cobre a inversão de D3 e a regra de grupo |

## Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `NpsGrupoReplicacaoService::replicar()` | `NpsSnapshotService::registrar()` | 1x por espelho, serviço intocado | ✓ WIRED (confirmado por leitura + git log) |
| `NpsGrupoCoberturaService` | `Company::responsavelDoServicoOuConsolidado()` | resolução de responsáveis por serviço/papel | ✓ WIRED |
| `NpsGrupoController` | `NpsGrupoCoberturaService::calcular()` | prévia E de novo no submit (nunca confia no navegador) | ✓ WIRED |
| `routes/web.php` | `NpsGrupoController` | rotas `nps.grupo.*` declaradas ANTES de `/nps/{token}` | ✓ WIRED, ordem correta confirmada |
| `DashboardController`/`PerformanceController`/`PortfolioController`/`CompanyController`/`CalculateGoalResults` | `NpsSemLinkService` | D1 nos 6 consumidores | ✓ WIRED nos 6, confirmado por grep + testes de execução real |
| `Nps/Respond.jsx` | `nps.grupo.submit` | `survey.submit_url` com fallback para rota individual | ✓ WIRED |

## Behavioral Spot-Checks / Execução Real de Suíte

| Suíte | Comando | Resultado | Status |
|---|---|---|---|
| Phase116 | `php artisan test --filter=Phase116` | 79 passed (639 assertions), 148s | ✓ PASS — bate exatamente a progressão alegada |
| Phase119_1 | `php artisan test --filter=Phase119_1` | 108 passed (776 assertions), 166s | ✓ PASS — bate exatamente o SUMMARY 08 |
| Phase118 | `php artisan test --filter=Phase118` | 36 passed (146 assertions), 151s | ✓ PASS — baseline exata |
| Nps (completo) | `php artisan test --filter=Nps` | 5 failed / 543 passed (3718 assertions), 903s | ✓ PASS — as 5 falhas são exatamente as herdadas (confirmado pelo texto de erro capturado: `ConsolidarMesJanelaNpsTest`, `JanelaNpsBonusTest`) |
| Desempenho | `php artisan test --filter=Desempenho` | 14 failed / 94 passed (407 assertions), 149s | ✓ PASS — baseline herdada exata (commit `25a958b3`, fora do escopo desta fase) |

Nenhuma suíte completa (`php artisan test` sem filtro) foi executada, conforme instrução explícita do ambiente. Nenhum teste rodou em background por decisão desta verificação — os 2 casos que excederam 120s foram movidos automaticamente pelo próprio sistema de execução, e o resultado final foi lido do arquivo de saída depois de concluído.

## Requirements Coverage

`.planning/REQUIREMENTS.md` não contém nenhuma referência a `119.1` ou a `NPSMAN-*` — confirmado por grep direto (zero ocorrências). **Isto é o gap estrutural pré-existente já sinalizado no prompt de verificação** (o arquivo só cobre a milestone v17.0) — registrado como observação, não como falha desta fase. Os requisitos NPSMAN-01 a NPSMAN-13 foram verificados contra `119.1-VALIDATION.md` e `119.1-CONTEXT.md`, ambos artefatos formais desta fase.

## Anti-Patterns Found

Nenhum bloqueador. Achados de menor severidade, todos já documentados nos próprios SUMMARYs e confirmados por esta verificação como não-bloqueantes:

| File | Observação | Severidade | Impacto |
|---|---|---|---|
| `app/Http/Controllers/NpsGrupoController.php` | Espelhos de grupo não geram `NpsSurveyEvent` (trilha de auditoria da Fase 94) | ℹ️ Info | Reduz detalhe de auditoria técnica; não afeta nota, bônus ou regra de negócio nenhuma. Documentado no SUMMARY 06 como gap conhecido não-bloqueante. |
| `tests/Feature/Phase118/*` (3 arquivos) + `tests/Feature/Phase116/NpsFloorRegressaoTest.php` | Deviation do Plano 03 editou 4 arquivos fora do escopo declarado do plano | ℹ️ Info | Justificado: fixtures pré-119.1 nunca modelavam elegibilidade real; nenhuma asserção original foi enfraquecida — confirmado por leitura: os testes agora exercitam empresa GENUINAMENTE elegível/não-elegível, mais correto que antes. `--filter=Phase118` = 36 passed confirma zero regressão. |
| Commit `b4bcb22e` (Plano 05, Task 2) | `NpsGrupoCoberturaService.php` commitado antes do teste (ordem TDD invertida) | ℹ️ Info | Documentado explicitamente no commit e no SUMMARY como decorrência de 3 quedas de API, não descuido; o serviço passou 12/12 testes de primeira sem correção. |

Nenhum `TBD`/`FIXME`/`XXX` sem referência formal encontrado nos arquivos desta fase.

## Human Verification Required

### 1. Caso âncora ponta a ponta com dados reais de produção

**Teste:** Usar o cadastro REAL do grupo "Camillo Parts" em produção (confirmar que Luis e Ana Julia são hoje os responsáveis reais no serviço coberto pelo modelo de NPS usado), gerar o link de NPS de grupo pela tela, enviar de fato ao cliente, e conferir a resposta real quando ela chegar.

**Esperado:** Cliente responde com nota máxima (5) em todos os quesitos → todas as empresas do grupo Camillo Parts ficam com nota 5 naquele mês; nenhuma empresa do grupo fica sem nota; nenhuma empresa FORA do grupo é afetada.

**Por que humano:** Este é exatamente o "caso âncora do usuário" citado em `119.1-CONTEXT.md` e listado como verificação **Só-Manual** em `119.1-VALIDATION.md` ("Só verificável com dados reais"). Todos os testes automatizados desta fase usam fixtures sintéticas (mesmo o `NpsGrupoSurveyTest` com nome "Camillo Parts" — é uma empresa de teste criada via factory, não o registro real de produção). Nenhum SUMMARY reporta que esta verificação com dados reais e envio real já ocorreu. A mecânica está provada corretíssima em código e teste; o que falta é a confirmação de que o cadastro de responsáveis em produção está de fato como o usuário descreveu, e que o fluxo funciona ponta a ponta com um cliente real.

## Pendências Conhecidas (não são gaps desta fase)

- **Backfill retroativo do histórico da Fase 116 continua NÃO EXECUTADO** — gate de negócio do usuário, pendente desde a Fase 116, documentado nesta fase como intencionalmente fora de escopo (D1/119.1 só vale do mês corrente em diante, mesma régua da Fase 116).
- **REQUIREMENTS.md não referencia NPSMAN-*** — gap estrutural pré-existente do arquivo (só cobre v17.0), não uma omissão desta fase.

## Gaps Summary

Nenhum gap bloqueador encontrado. Todos os truths automatizáveis (13/13) foram verificados diretamente contra o código-fonte e confirmados por execução real das suítes de teste (não por leitura de SUMMARY.md). O único item pendente é uma verificação de produção com dados reais (caso âncora Camillo Parts respondido de verdade), que por natureza não pode ser confirmada por análise de código — está corretamente listada como verificação humana desde a criação de `119.1-VALIDATION.md` e nenhum SUMMARY afirma que já ocorreu. Por isso o status desta verificação é `human_needed`, não `passed` — mesmo com pontuação automatizada perfeita.

---

*Verificado: 2026-07-30*
*Verificador: Claude (gsd-verifier)*
