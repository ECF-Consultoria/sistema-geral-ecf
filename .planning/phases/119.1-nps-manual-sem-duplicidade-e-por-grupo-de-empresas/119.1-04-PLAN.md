---
phase: 119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas
plan: 04
type: execute
wave: 3
depends_on: ["119.1-02", "119.1-03"]
files_modified:
  - app/Http/Controllers/NpsController.php
  - app/Services/Desempenho/NpsSemLinkService.php
  - tests/Feature/Phase119_1/NpsAreaD1Test.php
  - tests/Feature/Phase116/NpsFloorAreaNpsTest.php
  - tests/Feature/Phase116/NpsFloorCarteiraTest.php
  - tests/Feature/Phase116/NpsFloorDashboardsTest.php
  - tests/Feature/Phase116/NpsFloorDesempenhoTest.php
  - tests/Feature/Phase116/NpsFloorRegressaoTest.php
  - tests/Feature/Phase116/NpsImputacaoServiceTest.php
autonomous: true
requirements: [NPSMAN-06, NPSMAN-07, NPSMAN-11, NPSMAN-12]

must_haves:
  truths:
    - "Na área NPS, empresa elegível sem link no mês entra na média com nota 1 depois que o mês fecha"
    - "A lista de empresas sem link diz por que cada uma está sem link — inclusive 'falta cadastrar o contato'"
    - "Uma empresa pode aparecer na lista de sem link E contribuir com nota 1 ao mesmo tempo — são duas informações diferentes"
    - "Empresa não elegível continua sem afetar nenhuma média em nenhuma tela"
    - "Os testes da Fase 116 que provavam o invariante antigo continuam existindo, agora restritos ao caso não elegível"
  artifacts:
    - path: "app/Http/Controllers/NpsController.php"
      provides: "index() com faltantes enriquecidos por motivo e contribuindo nota 1 nas médias"
      contains: "motivo"
    - path: "tests/Feature/Phase119_1/NpsAreaD1Test.php"
      provides: "Prova de D1 na área NPS e do motivo por faltante"
  key_links:
    - from: "app/Http/Controllers/NpsController.php"
      to: "app/Services/Desempenho/NpsSemLinkService.php"
      via: "notasDaEmpresaSemLink() somada às médias por dimensão"
      pattern: "NpsSemLinkService|notasDaEmpresaSemLink"
    - from: "app/Http/Controllers/NpsController.php"
      to: "app/Services/Nps/NpsElegibilidadeService.php"
      via: "filtro de elegibilidade sobre a lista de faltantes"
      pattern: "NpsElegibilidadeService"
---

<objective>
Levar D1 para a área NPS (a tela onde a operação lê as médias) e segmentar os testes da
Fase 116 que provavam o invariante agora invertido.

A tela já tem a lista `$faltantes` — "empresa da carteira sem link neste mês". Até hoje ela era
só operacional (quem eu preciso mandar à mão). A partir desta fase, faltante elegível também
**pesa** na média, e a tela precisa deixar isso explícito — inclusive o caso em que a pessoa não
consegue enviar porque a empresa não tem contato cadastrado (D5).

Purpose: se o número da tela e o número do bônus discordarem, ninguém confia em nenhum dos dois.
Output: `index()` coerente com `computeNpsMedio()`, motivo por faltante, e os 6 arquivos da
Fase 116 segmentados em "não elegível" (regra preservada) e "elegível" (regra nova).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-CONTEXT.md
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-RESEARCH.md
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-03-SUMMARY.md
@.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contratos extraídos do código real nesta sessão. -->

`NpsController::index()` já monta `$faltantes` (≈ linhas 459-513) — empresas ATIVAS com contrato
ativo em serviço do setor que NÃO têm survey daquele conjunto de templates no mês. A régua de
"tem survey no mês" já usa o fallback correto:

  whereBetween('month_reference', [$mesInicio, $mesFim])
  OR (whereNull('month_reference') AND whereBetween('created_at', [$mesInicio, $mesFim]))

Cada item hoje é: `['company_id', 'name', 'modelo', 'template_id', 'setor']`.
`$contadores['faltantes'] = count($faltantes);` e `$contadores['todos'] = $totalGeral + count($faltantes);`
`$faltantes` sai no payload Inertia como a chave `'faltantes'`.

Régua de dedupe da área NPS (≈ linhas 804-832, `notasImputadasPorDimensao()`): 1 nota por
`(survey_id, dimensao)` — `->get()->unique('survey_id')`. Para o ramo D1 não existe `survey_id`;
a chave substituta é `(company_id, template_id)`, a MESMA usada no plano 03.

Assinaturas disponíveis:
  NpsElegibilidadeService::empresasElegiveis(Carbon $mes): Collection
  NpsSemLinkService::notasDoUsuario(User $user, Carbon $inicio, Carbon $fim, ?Collection $invalidadas): Collection
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Área NPS — faltante elegível conta nota 1 e diz por que está sem link</name>

  <read_first>
    - `app/Http/Controllers/NpsController.php` (método `index()` completo, com atenção ao bloco `$faltantes` e ao `notasImputadasPorDimensao()` e como as médias por dimensão são montadas)
    - `app/Services/Desempenho/NpsSemLinkService.php` (criado no plano 03)
    - `app/Services/Nps/NpsElegibilidadeService.php` (criado no plano 01)
    - `app/Services/Nps/NpsImputationService.php` (`notasDaEmpresa()` — o shape por dimensão a espelhar)
    - `tests/Feature/Phase116/NpsFloorAreaNpsTest.php` (como a área NPS é testada hoje)
  </read_first>

  <files>app/Http/Controllers/NpsController.php, app/Services/Desempenho/NpsSemLinkService.php, tests/Feature/Phase119_1/NpsAreaD1Test.php</files>

  <behavior>
    - Mês de coleta fechado + empresa elegível sem link → a média por dimensão da área NPS inclui uma nota 1 daquela empresa.
    - Mês ainda aberto → a média não inclui nada daquela empresa.
    - Empresa NÃO elegível sem link → nunca entra na média (NPSMAN-07), mesmo com o mês fechado.
    - A mesma empresa continua aparecendo em `faltantes` E contribuindo com a nota 1 — as duas coisas convivem.
    - Cada item de `faltantes` traz `motivo = 'sem_contato'` quando a empresa não tem `email_cliente` nem `digisac_group_contact_id`, e `motivo = 'sem_link'` caso contrário.
    - Cada item de `faltantes` traz `conta_nota_1 = true` quando é elegível e o mês já fechou; `false` caso contrário.
    - Empresa invalidada para bônus na competência não entra na média nem aparece com `conta_nota_1 = true`.
  </behavior>

  <action>
    1. Em `app/Services/Desempenho/NpsSemLinkService.php`, acrescentar o método público
       `notasDaEmpresaSemLink(Collection $companyIds, string $dimensao, Carbon $de, Carbon $ate, ?Collection $invalidadas = null, ?Collection $templateIds = null): Collection`,
       espelhando a assinatura de `NpsImputationService::notasDaEmpresa()`. Resolve dimensões
       aplicáveis do template pela MESMA régua que `NpsImputationService::materializar()` usa
       (`contarPerguntasComPeso($templateId, $dimensao) > 0`) — ler o método antes de escrever;
       deduplica por `(company_id, template_id)`; devolve nota `1.0` por item.

    2. Em `NpsController::index()`, injetar `NpsElegibilidadeService` e `NpsSemLinkService` e:

       a) Enriquecer cada item de `$faltantes` com duas chaves novas:
          - `motivo`: `'sem_contato'` se `empty($c->email_cliente) && empty($c->digisac_group_contact_id)`,
            senão `'sem_link'`. (Selecionar as duas colunas no `get([...])` — hoje só busca `id` e `name`.)
          - `conta_nota_1`: booleano — `true` só se o par (empresa, modelo) está em
            `empresasElegiveis($mes)`, a empresa não está invalidada na competência, e o mês de
            coleta já fechou.
          Comentário pt-BR nominal registrando D5: falta de contato NÃO tira a empresa da regra;
          é só o motivo que a tela mostra para a pessoa saber o que fazer.

       b) Somar as notas de `notasDaEmpresaSemLink()` às médias por dimensão, no mesmo ponto em
          que `notasImputadasPorDimensao()` já é somada, preservando a régua de dedupe existente.
          Não alterar o cálculo das notas reais nem das imputadas — o ramo é aditivo e disjunto
          (uma empresa que tem survey no mês nunca entra aqui).

       c) Acrescentar ao payload Inertia o contador `$contadores['contam_nota_1']` = quantidade
          de faltantes com `conta_nota_1 = true`. O plano 07 (UI) consome essa chave.

    3. Criar `tests/Feature/Phase119_1/NpsAreaD1Test.php` (namespace `Tests\Feature\Phase119_1`,
       `RefreshDatabase`) com um teste por item do bloco `<behavior>`, batendo na rota da área NPS
       (`GET` autenticado) e assertando sobre as props Inertia (`faltantes`, contadores, médias).
       Congelar o tempo com `Carbon::setTestNow()`.

    Commitar só estes três arquivos com `git commit -- <paths>`.
  </action>

  <verify>
    <automated>php artisan test --filter=NpsAreaD1Test</automated>
  </verify>

  <acceptance_criteria>
    - `php artisan test --filter=NpsAreaD1Test` sai 0 com ≥ 7 testes.
    - `grep -c "'sem_contato'" app/Http/Controllers/NpsController.php` retorna ≥ 1.
    - `grep -c "conta_nota_1" app/Http/Controllers/NpsController.php` retorna ≥ 2.
    - `grep -c "notasDaEmpresaSemLink" app/Http/Controllers/NpsController.php` retorna ≥ 1.
    - `grep -c "digisac_group_contact_id" app/Http/Controllers/NpsController.php` retorna ≥ 1.
    - `php artisan test --filter=NpsFloorAreaNpsTest` roda; se falhar, a falha é do invariante invertido e será tratada na Task 2 (não relaxar nada aqui).
  </acceptance_criteria>

  <done>A área NPS conta nota 1 pela mesma régua do bônus e explica, por empresa, por que está sem link.</done>
</task>

<task type="auto">
  <name>Task 2: Segmentar (não apagar) os testes da Fase 116 que provavam o invariante invertido</name>

  <read_first>
    - `tests/Feature/Phase116/NpsFloorDesempenhoTest.php` (bloco 6 — assert `0.0` para empresa sem survey)
    - `tests/Feature/Phase116/NpsFloorAreaNpsTest.php` (bloco 3)
    - `tests/Feature/Phase116/NpsFloorCarteiraTest.php` (bloco 3 — `$linha['nps']` null)
    - `tests/Feature/Phase116/NpsFloorDashboardsTest.php` (bloco 6)
    - `tests/Feature/Phase116/NpsFloorRegressaoTest.php` (bloco 7 — `montarCenarioVazio()` e o comentário sobre a "EXCEÇÃO CONHECIDA" do `NpsPorEmpresaService`)
    - `tests/Feature/Phase116/NpsImputacaoServiceTest.php` (bloco 2 — "empresa SEM survey nunca gera linha (D3)")
    - `.planning/phases/116-nps-n-o-respondido-conta-como-nota-m-nima-1/116-CONTEXT.md` (o D3 original, para citar corretamente na segmentação)
  </read_first>

  <files>tests/Feature/Phase116/NpsFloorAreaNpsTest.php, tests/Feature/Phase116/NpsFloorCarteiraTest.php, tests/Feature/Phase116/NpsFloorDashboardsTest.php, tests/Feature/Phase116/NpsFloorDesempenhoTest.php, tests/Feature/Phase116/NpsFloorRegressaoTest.php, tests/Feature/Phase116/NpsImputacaoServiceTest.php</files>

  <action>
    **Regra inegociável: nenhum teste de D3 pode ser apagado.** Eles continuam válidos para
    empresa NÃO elegível — que é justamente o contrapeso de D1 (NPSMAN-07). O trabalho é
    SEGMENTAR: onde o cenário hoje é "empresa sem survey", explicitar de qual dos dois casos se
    trata e, quando necessário, quebrar em dois testes.

    Para cada arquivo:

    - `NpsImputacaoServiceTest.php` — o bloco 2 ("empresa SEM survey nunca gera linha") deve
      continuar passando SEM alteração de asserção, porque D1 é leitura e não materializa nada.
      Acrescentar só um comentário pt-BR acima do bloco registrando que, a partir da Fase 119.1,
      a ausência de linha nesta tabela **não** significa mais "não conta nota 1" — a nota da
      empresa elegível sem link é calculada em leitura por `NpsSemLinkService`. Se este bloco
      falhar, é sinal de que alguém materializou o ramo D e o caminho errado foi escolhido.

    - `NpsFloorDesempenhoTest.php` (bloco 6) — transformar o teste atual em
      `..._empresa_nao_elegivel_sem_survey_continua_zero` (fixture ajustada para tornar a empresa
      NÃO elegível de forma explícita: sem estrategista OU sem contrato ativo em serviço coberto)
      e adicionar um teste novo `..._empresa_elegivel_sem_survey_conta_nota_1` apontando para o
      ramo (D).

    - `NpsFloorAreaNpsTest.php` (bloco 3) — mesma segmentação. Registrar em comentário que a
      empresa passa a poder estar em `faltantes` **e** contribuir com nota 1 ao mesmo tempo: são
      informações diferentes, não contradição.

    - `NpsFloorCarteiraTest.php` (bloco 3) — mesma segmentação para `$linha['nps']`.

    - `NpsFloorDashboardsTest.php` (bloco 6) — mesma segmentação, com nota de rodapé pt-BR
      dizendo que D3 foi INVERTIDA de propósito pela Fase 119.1 (D1), não é regressão.

    - `NpsFloorRegressaoTest.php` (bloco 7) — `montarCenarioVazio()` vira DOIS cenários:
      `montarCenarioVazioNaoElegivel()` (D3 preservado, todos os consumidores continuam sem nota)
      e `montarCenarioVazioElegivel()` (D1 novo, os consumidores passam a ver nota 1). Atualizar
      o comentário existente sobre a "EXCEÇÃO CONHECIDA" do `NpsPorEmpresaService` (Fase 118):
      a partir de 119.1 aquilo deixa de ser exceção e vira a regra geral.

    Se algum teste segmentado revelar um consumidor que ficou incoerente (a área NPS conta e o
    dashboard não, por exemplo), **não** afrouxar a asserção — anotar o consumidor no SUMMARY
    para o plano 08 fechar a coerência.

    Commitar só estes 6 arquivos com `git commit -- <paths>`.
  </action>

  <verify>
    <automated>php artisan test --filter=Phase116</automated>
  </verify>

  <acceptance_criteria>
    - `php artisan test --filter=Phase116` sai 0.
    - `grep -rc "nao_elegivel" tests/Feature/Phase116/ | grep -v ':0' | wc -l` retorna ≥ 4 (a segmentação existe em pelo menos 4 arquivos).
    - `grep -rc "elegivel_sem_survey\|elegivel_sem_link" tests/Feature/Phase116/ | grep -v ':0' | wc -l` retorna ≥ 3 (os casos novos existem).
    - A contagem total de métodos `public function test` em `tests/Feature/Phase116/` é MAIOR que antes desta task (nenhum teste foi removido; foram adicionados).
    - `grep -rc "119.1" tests/Feature/Phase116/ | grep -v ':0' | wc -l` retorna ≥ 3 (a inversão está registrada nos arquivos).
  </acceptance_criteria>

  <done>Os 6 arquivos distinguem "não elegível" (regra preservada) de "elegível" (regra nova), nenhum teste foi apagado, e a suíte Phase116 está verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| usuário autenticado → área NPS | O escopo de empresas visíveis depende do papel (admin vê tudo, não-admin vê a carteira dele) |
| leitura de tela → percepção de bônus | O número da tela é o que a equipe usa para contestar o bônus; divergir do cálculo real gera desconfiança |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-119.1-14 | Information Disclosure | `faltantes` enriquecido com `motivo` pode vazar empresa fora da carteira do usuário | mitigate | O filtro `$filtroPessoaFaltante` já existente em `index()` continua aplicado ANTES do enriquecimento; nenhuma query nova sem esse filtro |
| T-119.1-15 | Tampering | Área NPS e bônus divergirem na régua de elegibilidade | mitigate | Ambos consomem `NpsElegibilidadeService`; a divergência seria detectada pelo teste de coerência do plano 08 |
| T-119.1-16 | Repudiation | Teste de D3 apagado para "passar a suíte" | mitigate | Acceptance exige contagem total de testes MAIOR que antes e presença de casos `nao_elegivel` |
| T-119.1-17 | Elevation of Privilege | Não-admin vendo `conta_nota_1` de empresa de outro responsável | mitigate | Herda o escopo de `index()`; teste com usuário não-admin assere que a empresa de outra carteira não aparece |
| T-119.1-SC | Tampering | Instalação de pacotes | mitigate | Nenhum pacote novo; qualquer `composer require`/`npm install` neste plano é desvio e deve parar a execução |
</threat_model>

<verification>
1. `php artisan test --filter=Phase119_1` verde.
2. `php artisan test --filter=Phase116` verde.
3. `php artisan test --filter=Phase118` verde (o `NpsPorEmpresaService` não pode ter mudado de comportamento).
4. `php artisan test --filter=Nps` sem falha nova versus baseline.
</verification>

<success_criteria>
- Área NPS e bônus concordam sobre quem conta nota 1.
- Cada faltante diz por que está sem link e se está pesando na média.
- Os 6 arquivos da Fase 116 segmentados, nenhum teste removido, suíte verde.
</success_criteria>

<output>
Criar `.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-04-SUMMARY.md` ao terminar.
Listar no SUMMARY qualquer consumidor que tenha ficado incoerente com o ramo D1 — o plano 08 fecha.
</output>
