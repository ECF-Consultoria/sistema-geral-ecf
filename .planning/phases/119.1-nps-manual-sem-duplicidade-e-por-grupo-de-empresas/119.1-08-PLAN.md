---
phase: 119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas
plan: 08
type: execute
wave: 5
depends_on: ["119.1-03", "119.1-04", "119.1-06", "119.1-07"]
files_modified:
  - tests/Feature/Phase119_1/NpsCoerenciaD1Test.php
  - tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php
  - docs/nps-nao-respondido-nota-1.md
autonomous: true
requirements: [NPSMAN-09, NPSMAN-10, NPSMAN-11]

must_haves:
  truths:
    - "Todos os lugares que mostram nota de NPS concordam sobre quem conta nota 1 e quem não conta"
    - "A rotina diária continua olhando só o mês anterior e o mês corrente, nunca o histórico inteiro"
    - "O documento operacional descreve a regra que está em produção hoje, não a da Fase 116"
    - "A suíte existente está verde depois da inversão do invariante"
  artifacts:
    - path: "tests/Feature/Phase119_1/NpsCoerenciaD1Test.php"
      provides: "Gate de coerência: o mesmo cenário lido por todos os consumidores dá o mesmo veredito"
    - path: "docs/nps-nao-respondido-nota-1.md"
      provides: "Doc operacional atualizado com a inversão de D3, a regra de grupo e o desligamento do automático"
  key_links:
    - from: "tests/Feature/Phase119_1/NpsCoerenciaD1Test.php"
      to: "todos os consumidores da regra de nota 1"
      via: "cenário único lido por bônus, área NPS, carteira e relatório por empresa"
      pattern: "computeNpsMedio|NpsPorEmpresaService|NpsController"
---

<objective>
Fechar a fase: provar que todos os lugares que leem NPS concordam entre si depois da inversão,
garantir que a rotina diária da Fase 116 não voltou a varrer o histórico, e atualizar o
documento operacional que hoje descreve a regra antiga.

O erro mais caro deste módulo, documentado duas vezes (Fases 96 e 116), é "esqueci um
call-site" — uma tela mostrando um número e o bônus pagando outro, sem exception e sem log.
Este plano é o gate contra isso.

Purpose: uma inversão de invariante sem gate de coerência vira reclamação de bônus em 30 dias.
Output: teste de coerência, caso de regressão explícito da janela da rotina, e doc reescrito.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-CONTEXT.md
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-RESEARCH.md
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-03-SUMMARY.md
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-04-SUMMARY.md
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate de coerência entre todos os consumidores da regra de nota 1</name>

  <read_first>
    - `tests/Feature/Phase116/NpsFloorRegressaoTest.php` (o padrão de "cenário único lido por todos os consumidores" — é o molde deste gate)
    - `app/Services/DesempenhoScoreService.php` (`computeNpsMedio()` com o ramo D do plano 03)
    - `app/Http/Controllers/NpsController.php` (`index()` com os faltantes do plano 04)
    - `app/Services/Desempenho/NpsPorEmpresaService.php` (o consumidor da Fase 118)
    - `.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-04-SUMMARY.md` (a lista de consumidores incoerentes, se houver)
  </read_first>

  <files>tests/Feature/Phase119_1/NpsCoerenciaD1Test.php</files>

  <action>
    Criar `tests/Feature/Phase119_1/NpsCoerenciaD1Test.php` (namespace `Tests\Feature\Phase119_1`,
    `RefreshDatabase`) no molde de `NpsFloorRegressaoTest`: **um único cenário**, lido por
    TODOS os consumidores, com o mesmo veredito esperado.

    Enumerar primeiro os consumidores reais com
    `grep -rln "NpsImputedAssignment\|computeNpsMedio\|notasImputadas" app/` e cobrir todos os
    que produzem número para tela ou bônus. Como mínimo: `DesempenhoScoreService::computeNpsMedio`
    (bônus), `NpsController::index` (área NPS), `NpsPorEmpresaService::notasNpsPorEmpresa`
    (relatório por empresa, Fase 118) e o consumidor de carteira usado por
    `Phase116/NpsFloorCarteiraTest`.

    Três cenários-espelho, cada um lido por todos os consumidores:

    1. **Empresa ELEGÍVEL sem link, mês fechado** → todos os consumidores devem enxergar nota 1
       para a empresa e para quem cuida dela.
    2. **Empresa NÃO ELEGÍVEL sem link, mês fechado** → NENHUM consumidor pode enxergar nota
       (contrapeso NPSMAN-07). Cobrir os 4 motivos de não-elegibilidade em sub-casos.
    3. **Empresa elegível sem link, mês AINDA ABERTO** → nenhum consumidor penaliza ninguém.

    Se algum consumidor divergir, **não** ajustar o número esperado do teste — corrigir o
    consumidor e registrar no SUMMARY. Um consumidor discordante é exatamente o bug que este
    gate existe para pegar.

    Congelar o tempo com `Carbon::setTestNow()`.

    Commitar só este arquivo com `git commit -- <path>`.
  </action>

  <verify>
    <automated>php artisan test --filter=NpsCoerenciaD1Test</automated>
  </verify>

  <acceptance_criteria>
    - `php artisan test --filter=NpsCoerenciaD1Test` sai 0 com ≥ 3 testes.
    - O arquivo referencia ≥ 4 consumidores distintos (conferir com `grep -c "computeNpsMedio\|NpsPorEmpresaService\|nps.index\|notasNpsPorEmpresa" tests/Feature/Phase119_1/NpsCoerenciaD1Test.php` ≥ 4).
    - O cenário 2 cobre os 4 motivos de não-elegibilidade (sem estrategista, sem contrato ativo em serviço coberto, empresa inativa, empresa invalidada na competência).
  </acceptance_criteria>

  <done>Um cenário, todos os consumidores, mesmo veredito — nos três casos.</done>
</task>

<task type="auto">
  <name>Task 2: Regressão explícita da janela da rotina diária (mês anterior + corrente)</name>

  <read_first>
    - `tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php` (os 14 testes existentes sobre a janela)
    - `routes/console.php` (o `Schedule::call` de `nps-materializar-nao-respondidos`, preservado no plano 01)
    - `app/Console/Commands/` — o comando `nps:materializar-nao-respondidos`
  </read_first>

  <files>tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php</files>

  <action>
    Acrescentar (sem remover nada) um teste de regressão explícito da Fase 119.1 ao arquivo:

    - o agendamento continua invocando o comando com `--mes` para **exatamente dois meses**
      (`now()->subMonthNoOverflow()` e `now()`), nunca sem `--mes`. Assertar sobre o closure do
      schedule ou, se isso não for acessível, sobre a presença literal do laço em
      `routes/console.php` via `file_get_contents(base_path('routes/console.php'))` — deixando
      claro em comentário pt-BR que a asserção textual existe porque o risco é alguém "simplificar"
      o laço num refactor futuro e ligar o backfill retroativo sem querer.
    - uma competência fechada ANTERIOR ao mês anterior continua intocada depois de rodar a rotina.

    Comentário pt-BR nominal no topo do teste novo: a Fase 119.1 desligou o disparo automático
    (plano 01) mas **não** mexeu nesta rotina de propósito — o backfill retroativo do histórico
    continua sendo operação manual com gate humano.

    Commitar só este arquivo com `git commit -- <path>`.
  </action>

  <verify>
    <automated>php artisan test --filter=NpsMaterializarNaoRespondidosCommandTest</automated>
  </verify>

  <acceptance_criteria>
    - `php artisan test --filter=NpsMaterializarNaoRespondidosCommandTest` sai 0 e tem MAIS testes que antes.
    - `grep -c "119.1" tests/Feature/Phase116/NpsMaterializarNaoRespondidosCommandTest.php` retorna ≥ 1.
    - `grep -c "subMonthNoOverflow" routes/console.php` retorna ≥ 1 (janela intacta).
  </acceptance_criteria>

  <done>A janela restrita da rotina está protegida por teste que cita explicitamente a Fase 119.1.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar o documento operacional com a inversão, o grupo e o desligamento</name>

  <read_first>
    - `docs/nps-nao-respondido-nota-1.md` (arquivo inteiro — em especial a seção 1 "A regra"/"Dois invariantes", a seção 3 "Onde a regra aparece", a seção "Rotina automática" e a seção 6 "Cache")
    - `.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-CONTEXT.md` (as 6 decisões travadas — a fonte do texto)
    - Os SUMMARYs dos planos 01, 03, 04 e 06
  </read_first>

  <files>docs/nps-nao-respondido-nota-1.md</files>

  <action>
    Reescrever as partes do doc que ficaram falsas, em pt-BR, mantendo o tom operacional já usado
    (é doc de operação, não de arquitetura):

    1. **Seção 1 (invariantes):** o invariante "só vira 1 o NPS efetivamente disparado; empresa
       sem disparo nunca entra" está SUPERADO. Reescrever para a regra atual: *empresa elegível
       que passou o mês sem nenhum link também conta 1; empresa NÃO elegível (sem contrato ativo
       em serviço coberto, sem modelo aplicável, sem estrategista, inativa, ou invalidada no mês)
       continua fora.* Registrar que a mudança é da Fase 119.1 e por quê (com o automático
       desligado, o invariante antigo virava a brecha "não enviar sai mais barato"), e que empresa
       **sem contato cadastrado conta 1 igual** (D5) — cadastrar o contato faz parte de cuidar da
       conta.

    2. **Seção nova "O NPS não sai mais sozinho":** o agendamento diário foi removido; o comando
       `php artisan nps:disparar-mensal` continua existindo para disparo manual em massa; e-mail e
       Digisac só saem quando alguém invoca o comando; a lista de empresas sem link na área NPS é
       a ferramenta operacional para saber o que falta enviar; a partir do dia de cobrança
       praticamente toda a carteira aparece como pendente, e isso é esperado.

    3. **Seção nova "NPS de grupo":** um link para o grupo; a nota replica para as empresas do
       grupo em que **estrategista e analista são os mesmos** em cada serviço que o modelo cobre;
       quem tem responsável diferente não recebe e segue valendo 1 até receber link individual;
       a prévia mostra tudo isso antes de enviar; internamente cada empresa coberta ganha o próprio
       registro de pesquisa, então as médias não contam nem a mais nem a menos.

    4. **Seção nova "Não dá para gerar dois links iguais":** empresa + modelo + mês só admite um
       link; ao tentar de novo, o sistema devolve o link que já existe para copiar.

    5. **Seção 6 (cache):** atualizar `desempenho.compute.v12` para `v13` e registrar que a Fase 120
       precisará subir para `v14` (o ROADMAP dela previa usar `v13`, consumido aqui).

    6. **Seção "Onde ficam os testes":** acrescentar `tests/Feature/Phase119_1/`.

    Não apagar o histórico da Fase 116 — marcar o que foi superado como superado, com a data e o
    motivo. Quem ler daqui a 6 meses precisa entender que foi decisão, não acidente.

    Commitar só este arquivo com `git commit -- <path>`.
  </action>

  <verify>
    <automated>grep -c "119.1" docs/nps-nao-respondido-nota-1.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "119.1" docs/nps-nao-respondido-nota-1.md` retorna ≥ 3.
    - `grep -c "desempenho.compute.v13" docs/nps-nao-respondido-nota-1.md` retorna ≥ 1 e `grep -c "desempenho.compute.v12" docs/nps-nao-respondido-nota-1.md` retorna 0.
    - `grep -ci "nps de grupo" docs/nps-nao-respondido-nota-1.md` retorna ≥ 1.
    - `grep -c "Phase119_1" docs/nps-nao-respondido-nota-1.md` retorna ≥ 1.
    - `grep -c "nps:disparar-mensal" docs/nps-nao-respondido-nota-1.md` retorna ≥ 1 (o uso manual está documentado).
  </acceptance_criteria>

  <done>O doc descreve a regra que está em produção hoje, marca o que foi superado, e explica o grupo e o desligamento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| documentação operacional → decisão humana | O doc é o que a equipe consulta antes de rodar backfill retroativo, operação que reescreve bônus congelado |
| rotina agendada → snapshot financeiro congelado | Uma janela mal ajustada reescreve competências fechadas sem gate humano |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-119.1-33 | Tampering | Refactor futuro "simplificar" o laço de dois meses e ligar o backfill retroativo | mitigate | Teste de regressão da Task 2 assere a presença do laço e a intocabilidade de competência anterior |
| T-119.1-34 | Repudiation | Doc descrevendo regra antiga levar alguém a rodar backfill errado | mitigate | Task 3 marca o invariante superado com data e motivo, em vez de apagar |
| T-119.1-35 | Tampering | Consumidor esquecido mostrando número diferente do bônus | mitigate | Gate de coerência da Task 1 lê o MESMO cenário por todos os consumidores enumerados via grep |
| T-119.1-36 | Information Disclosure | Doc operacional citar credencial ou token | accept | O doc é interno ao repositório e não contém segredo; a task não adiciona nenhum |
| T-119.1-SC | Tampering | Instalação de pacotes | mitigate | Nenhum pacote novo; qualquer `composer require`/`npm install` neste plano é desvio e deve parar a execução |
</threat_model>

<verification>
1. `php artisan test --filter=Phase119_1` verde (a suíte inteira da fase).
2. `php artisan test --filter=Phase116` verde.
3. `php artisan test --filter=Phase118` verde.
4. `php artisan test --filter=Phase119` verde (hash-gates).
5. `php artisan test --filter=Nps` e `--filter=Desempenho` reconferidos contra a baseline de
   falhas pré-existentes de `deferred-items.md` — a contagem NÃO pode crescer.
6. `php artisan schedule:list` — sem `nps:disparar-mensal`, com a materialização.
7. **Não rodar `php artisan test` sem filtro**: está documentado desde a Fase 116 que a suíte
   completa trava de forma reproduzível numa chamada HTTP real não mockada.
</verification>

<success_criteria>
- Todos os consumidores concordam nos três cenários (elegível / não elegível / mês aberto).
- A janela da rotina diária está protegida por teste que cita a Fase 119.1.
- `docs/nps-nao-respondido-nota-1.md` descreve a regra atual, o grupo, o desligamento e o cache `v13`.
- Todas as suítes filtradas verdes; nenhuma baseline de falha pré-existente cresceu.
</success_criteria>

<output>
Criar `.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-08-SUMMARY.md` ao terminar.
Registrar no SUMMARY, para a próxima fase: (a) a chave de cache está em `v13`, e a Fase 120 deve
subir para `v14`; (b) o backfill retroativo do histórico da Fase 116 continua pendente de gate
humano e NÃO foi executado por esta fase.
</output>
