---
phase: 119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas
plan: 03
type: execute
wave: 2
depends_on: ["119.1-01"]
files_modified:
  - app/Services/Desempenho/NpsSemLinkService.php
  - app/Services/DesempenhoScoreService.php
  - tests/Feature/Phase119_1/NpsFloorD1Test.php
  - tests/Feature/V18/DesempenhoMetadadosCacheTest.php
  - tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php
  - tests/Feature/DesempenhoShopeeScoreTest.php
  - tests/Feature/Phase116/NpsFloorDesempenhoTest.php
  - tests/Feature/Phase119/CompanyScoreServiceFonteTest.php
  - tests/Feature/Phase119/CompanyScoreServiceDispatcherTest.php
  - tests/Feature/Phase119/CompanyScoreServiceMargemTest.php
  - tests/Feature/Phase119/CompanyScoreServiceReconciliacaoTest.php
  - tests/Feature/Phase119/CompanyScoreServiceStatusTest.php
autonomous: true
requirements: [NPSMAN-06, NPSMAN-07, NPSMAN-12, NPSMAN-11]

must_haves:
  truths:
    - "Empresa elegível que passou o mês sem nenhum link conta nota 1 na média do estrategista e do analista"
    - "Empresa NÃO elegível (sem contrato ativo, sem modelo aplicável, sem estrategista, invalidada no mês) continua não contando nada"
    - "Empresa elegível sem e-mail nem WhatsApp cadastrado conta nota 1 do mesmo jeito"
    - "Enquanto o mês de coleta não fechou, empresa sem link não puxa a nota de ninguém"
    - "Uma empresa sem link nunca conta duas vezes para a mesma pessoa no mesmo papel"
    - "Nenhuma linha nova é escrita em nps_imputed_assignments — é cálculo de leitura"
    - "O bônus lido do cache velho não sobrevive ao deploy — a chave do cache subiu de versão"
  artifacts:
    - path: "app/Services/Desempenho/NpsSemLinkService.php"
      provides: "Notas 1.0 de leitura para empresa elegível sem link, por usuário e papel"
      exports: ["notasDoUsuario"]
      min_lines: 90
    - path: "tests/Feature/Phase119_1/NpsFloorD1Test.php"
      provides: "Prova de NPSMAN-06 (elegível conta), NPSMAN-07 (não elegível não conta) e NPSMAN-12 (sem contato conta)"
  key_links:
    - from: "app/Services/DesempenhoScoreService.php"
      to: "app/Services/Desempenho/NpsSemLinkService.php"
      via: "4º ramo (D) dentro de computeNpsMedio()"
      pattern: "NpsSemLinkService|notasSemLink"
    - from: "app/Services/Desempenho/NpsSemLinkService.php"
      to: "app/Services/Nps/NpsElegibilidadeService.php"
      via: "empresasElegiveis() + surveyExistenteNaCompetencia()"
      pattern: "NpsElegibilidadeService"
---

<objective>
Implementar D1 no cálculo do bônus: empresa ELEGÍVEL que passou o mês sem nenhum link de NPS
conta nota 1 para quem cuida dela.

Esta é a inversão consciente do invariante D3 da Fase 116 ("empresa sem disparo nunca entra").
Com o disparo automático desligado (plano 01), o D3 original viraria a brecha "não enviar sai
mais barato". Não é regressão — é supersessão, e o contrapeso (NPSMAN-07: empresa NÃO elegível
continua fora) é provado no mesmo plano.

**Arquitetura travada pela pesquisa:** implementar como 4º ramo de LEITURA em
`computeNpsMedio()`, generalizando o padrão que a Fase 118 já provou em
`NpsPorEmpresaService::linhaSemNota()`/`houveSurveyPorEmpresa()`. **NÃO tocar a tabela
`nps_imputed_assignments`** — tornar `survey_id` nullable quebraria os `whereHas('survey', ...)`
dos ~9 consumidores já em produção da Fase 116.

Purpose: sem isto, desligar o automático transforma "esquecer de enviar" na jogada
financeiramente ótima.
Output: `NpsSemLinkService` (leitura pura, zero escrita), ramo (D) em `computeNpsMedio()`,
cache bumpado para `v13` e os 5 hash-gates da Fase 119 recalculados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-CONTEXT.md
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-RESEARCH.md
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-PATTERNS.md
@.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-01-SUMMARY.md
@CLAUDE.md

Decisões travadas que este plano implementa:
- **D1** — empresa ELEGÍVEL sem NENHUM link no mês conta nota 1. Supersede D3 da Fase 116.
- **D5** — empresa sem canal de contato cadastrado conta 1 igual (cadastrar o contato é
  responsabilidade de quem cuida da conta).

<interfaces>
<!-- Contratos extraídos do código real nesta sessão. -->

`DesempenhoScoreService::computeNpsMedio(User $user, Carbon $mes, ?Collection $invalidadas = null): float`
hoje tem 3 ramos que fazem merge numa `$notas = collect()` e terminam em:

  if ($notas->isEmpty()) { return 0.0; }   // DESEMP-03: sem respostas PENALIZA
  return round($notas->avg(), 2);

Ramo (C), o analog mais próximo do que este plano adiciona:

  private function notasImputadas(User $user, Carbon $inicio, Carbon $fim, ?Collection $invalidadas = null): Collection
  {
      return $this->imputationService->notasDoUsuario($user, $inicio, $fim, $invalidadas)->pluck('nota');
  }

Shape que `NpsImputationService::notasDoUsuario()` devolve (a espelhar):

  (object) ['survey_id', 'company_id', 'servico_id', 'role', 'service_setor', 'competencia_nps', 'nota']
  deduplicado por `$linha->survey_id . '|' . $linha->role`

Janela: `computeNpsMedio` é chamado com `$mesNps = $mes->copy()->addMonthNoOverflow()` (o NPS da
competência M é coletado em M+1). A expressão de janela fechada já existente é
`now()->startOfDay()->gte($mesNps->copy()->endOfMonth()->startOfDay())` — **NÃO ALTERAR** essa
expressão nem `computeNpsWindow()`: `tests/Feature/Phase118/NpsJanelaResolverTest.php` a compara
por reflection. Usar `App\Services\Nps\NpsJanelaResolver::fechada($mes)` no serviço novo (já
provado equivalente pelo mesmo teste).

Assinaturas disponíveis desde o plano 01:

  NpsElegibilidadeService::empresasElegiveis(Carbon $mes): Collection
    // itens: (object) ['company_id','company','template_id','template','servico_ids','tem_canal']
  NpsElegibilidadeService::surveyExistenteNaCompetencia(int $companyId, int $templateId, Carbon $mes): ?NpsSurvey

Cache key atual (`DesempenhoScoreService`, ~linha 335):

  return sprintf('desempenho.compute.v12.%d.%s', $userId, $periodKey);

Hash-gate da Fase 119 (5 arquivos, mesma constante):

  private const HASH_DESEMPENHO_SCORE_SERVICE = 'cfc16da2a8404fba0d4a9a2bc62cd1a6f668bd17fe390fe6405cebd4e71a9edd';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NpsSemLinkService — nota 1 de leitura para empresa elegível sem link</name>

  <read_first>
    - `app/Services/Desempenho/NpsPorEmpresaService.php` (métodos `houveSurveyPorEmpresa()` e `linhaSemNota()` e o bloco D-04 que os chama — é o precedente exato a generalizar)
    - `app/Services/Nps/NpsImputationService.php` (método `notasDoUsuario()` — o shape a espelhar; e `materializar()` — como resolve dimensões aplicáveis por `contarPerguntasComPeso` e responsáveis por serviço)
    - `app/Services/Nps/NpsJanelaResolver.php` (a régua de janela fechada)
    - `app/Services/Nps/NpsElegibilidadeService.php` (criado no plano 01)
    - `app/Models/Company.php` (`responsavelDoServicoOuConsolidado()`)
    - `app/Models/NpsImputedAssignment.php` (as colunas `dimensao`/`role`/`service_setor` — o vocabulário a espelhar)
  </read_first>

  <files>app/Services/Desempenho/NpsSemLinkService.php, tests/Feature/Phase119_1/NpsFloorD1Test.php</files>

  <behavior>
    - Mês de coleta JÁ FECHADO + empresa elegível + nenhum link no mês → o estrategista e o analista da empresa recebem, cada um, uma nota `1.0`.
    - Mês de coleta AINDA ABERTO → nenhuma nota é devolvida (coleção vazia).
    - Empresa elegível SEM `email_cliente` e SEM `digisac_group_contact_id` → nota 1.0 do mesmo jeito (D5 / NPSMAN-12).
    - Empresa sem estrategista → nenhuma nota (não elegível, NPSMAN-07).
    - Empresa sem contrato ativo em serviço coberto por nenhum modelo → nenhuma nota (NPSMAN-07).
    - Empresa `active = false` → nenhuma nota (NPSMAN-07).
    - Empresa invalidada para bônus na competência (`$invalidadas`) → nenhuma nota (NPSMAN-07).
    - Empresa elegível COM link no mês (mesmo pendente, mesmo com `month_reference` NULL) → nenhuma nota por este serviço (quem cobre esse caso é o ramo (C) da Fase 116 — os ramos são disjuntos).
    - Empresa elegível para DOIS modelos, ambos sem link → o mesmo usuário no mesmo papel não recebe duas notas pela mesma empresa+modelo; recebe 1 nota por (empresa, modelo, papel).
    - Nenhuma linha é criada em `nps_imputed_assignments` (assertar `NpsImputedAssignment::count()` antes e depois).
  </behavior>

  <action>
    Criar `app/Services/Desempenho/NpsSemLinkService.php` (namespace `App\Services\Desempenho`),
    injetando `NpsElegibilidadeService` e `NpsJanelaResolver`. Docblock de classe em pt-BR
    registrando, nominalmente: (a) que este é o **4º ramo** da união disjunta de
    `computeNpsMedio()`; (b) que D1 da Fase 119.1 SUPERSEDE deliberadamente D3 da Fase 116 —
    inversão consciente, não regressão; (c) que é fallback de **LEITURA**, nunca materializado
    em `nps_imputed_assignments` (mesmo padrão de `NpsPorEmpresaService`, Fase 118 D-04), e
    por quê (a FK `survey_id` é NOT NULL por construção e os `whereHas('survey', ...)` dos
    consumidores da Fase 116 dependem disso).

    Método público:

      notasDoUsuario(User $user, Carbon $inicio, Carbon $fim, ?Collection $invalidadas = null): Collection

    Algoritmo:
    1. `$mes = $inicio->copy()->startOfMonth();` — se `! $this->janela->fechada($mes)`, retornar
       `collect()` imediatamente (enquanto o mês de coleta está aberto, ninguém é penalizado).
    2. `$elegiveis = $this->elegibilidade->empresasElegiveis($mes);`
    3. Descartar itens cuja empresa esteja em `$invalidadas`.
    4. Descartar itens em que `surveyExistenteNaCompetencia($company_id, $template_id, $mes) !== null`
       (garante disjunção com os ramos A/B/C — se existe survey, o caso é dos outros ramos).
    5. Para cada item restante, para cada `servico_id` do item, e para cada papel em
       `['estrategista', 'consultor']`: resolver
       `$item->company->responsavelDoServicoOuConsolidado($role, $servicoId)` — **usar este
       método e nenhuma query ad-hoc em `company_users`**, senão o gap do responsável
       CONSOLIDADO (`servico_id NULL`) volta e a pessoa some do cálculo. Se o `$user->id` estiver
       na coleção resolvida, emitir uma linha.
    6. Deduplicar por `company_id . '|' . template_id . '|' . role` — a chave substituta do
       `survey_id|role` do ramo (C), já que aqui não existe survey. Deixar comentário pt-BR
       nominal explicando essa substituição.
    7. Devolver objetos no MESMO shape de `NpsImputationService::notasDoUsuario()`, com
       `'survey_id' => null`, `'nota' => 1.0` e uma chave extra `'origem' => 'sem_link'`.

    Criar `tests/Feature/Phase119_1/NpsFloorD1Test.php` (namespace `Tests\Feature\Phase119_1`,
    `RefreshDatabase`) com um teste por item do bloco `<behavior>`. Incluir explicitamente um
    teste chamado com "nao_elegivel" no nome para cada um dos 4 motivos de não-elegibilidade
    (é o contrapeso NPSMAN-07) e um com "sem_contato" (NPSMAN-12).

    Congelar o tempo com `Carbon::setTestNow()` nos testes de janela — a régua depende de
    `now()`, e teste dependente de dia real fica intermitente.

    Commitar só estes dois arquivos com `git commit -- <paths>`.
  </action>

  <verify>
    <automated>php artisan test --filter=NpsFloorD1Test</automated>
  </verify>

  <acceptance_criteria>
    - `php artisan test --filter=NpsFloorD1Test` sai 0 com ≥ 10 testes.
    - `grep -c "responsavelDoServicoOuConsolidado" app/Services/Desempenho/NpsSemLinkService.php` retorna ≥ 1.
    - `grep -c "consultorDoServico\|estrategistaDoServico" app/Services/Desempenho/NpsSemLinkService.php` retorna 0 (não reintroduzir o gap do consolidado).
    - `grep -c "NpsImputedAssignment::create\|->save()\|::insert(" app/Services/Desempenho/NpsSemLinkService.php` retorna 0 (leitura pura, zero escrita).
    - `git diff --name-only HEAD~1 | grep -c "database/migrations"` retorna 0 (nenhuma migration nova — se aparecer alguma alterando `nps_imputed_assignments`, o caminho errado foi escolhido).
    - Um dos testes assere `NpsImputedAssignment::count()` inalterado antes/depois da chamada.
  </acceptance_criteria>

  <done>O serviço devolve nota 1 só para empresa elegível sem link com o mês fechado, respeita os 4 motivos de não-elegibilidade, cobre o caso sem contato, e não escreve nada.</done>
</task>

<task type="auto">
  <name>Task 2: 4º ramo (D) em computeNpsMedio() + bump da chave de cache para v13</name>

  <read_first>
    - `app/Services/DesempenhoScoreService.php` (o docblock de `computeNpsMedio()` e o corpo do método, os ramos A/B/C, `notasImputadas()`, `cacheKey()` e `computeNpsWindow()`)
    - `app/Services/Desempenho/NpsSemLinkService.php` (criado na Task 1)
    - `tests/Feature/Phase116/NpsFloorDesempenhoTest.php` (onde `desempenho.compute.v12` aparece hardcoded)
    - `.planning/ROADMAP.md` — seção "Phase 120", critério de sucesso 3 (ele previa que o bump `v12 → v13` aconteceria na Fase 120)
  </read_first>

  <files>app/Services/DesempenhoScoreService.php, tests/Feature/V18/DesempenhoMetadadosCacheTest.php, tests/Feature/Phase96/NpsInvalidacaoRespostaTest.php, tests/Feature/DesempenhoShopeeScoreTest.php, tests/Feature/Phase116/NpsFloorDesempenhoTest.php</files>

  <action>
    Em `app/Services/DesempenhoScoreService.php`:

    1. Injetar `NpsSemLinkService` (o service já injeta `NpsImputationService` — mesmo estilo).

    2. Adicionar o método privado `notasSemLink(User $user, Carbon $inicio, Carbon $fim, ?Collection $invalidadas = null): Collection`
       no molde EXATO de `notasImputadas()`: delega ao serviço e devolve `->pluck('nota')`.

    3. Dentro de `computeNpsMedio()`, adicionar o ramo (D) DEPOIS do ramo (C) e ANTES do
       `if ($notas->isEmpty())`, respeitando o mesmo toggle `$this->incluirImputadas`
       (o relatório de impacto do backfill precisa poder desligar os dois ramos juntos —
       conferir no código como `incluirImputadas` é usado antes de decidir):

         $notas = $notas->merge($this->notasSemLink($user, $inicio, $fim, $invalidadas));

    4. Atualizar o docblock de `computeNpsMedio()`: acrescentar o ramo `(D) SEM LINK
       (Fase 119.1 · D1)` à árvore ASCII já existente, explicando que é a inversão consciente
       do D3 da Fase 116 e que ele é DISJUNTO de (A)/(B)/(C) porque só entra quando
       `surveyExistenteNaCompetencia()` devolve `null`. Corrigir também a frase atual do
       docblock que afirma "uma empresa sem NENHUM survey disparado no mês continua caindo
       aqui (D3)" — ela ficou falsa e vai enganar o próximo leitor.

    5. **NÃO ALTERAR** a expressão de janela de `computeNpsWindow()` nem a linha
       `now()->startOfDay()->gte(...)` do chamador — `Phase118/NpsJanelaResolverTest` as compara
       por reflection.

    6. Bump da chave de cache: `desempenho.compute.v12` → `desempenho.compute.v13` em
       `cacheKey()`. **Obrigatório** — sem isso o Redis de produção serve o bônus com a regra
       antiga por até 7 dias depois do deploy.

    7. Atualizar os 4 arquivos de teste que hardcodam a string `desempenho.compute.v12` para
       `desempenho.compute.v13`. Confirmar a lista com
       `grep -rln "desempenho.compute.v12" tests/` antes e depois — o "depois" deve retornar vazio.

    8. Deixar comentário pt-BR nominal em cima do `cacheKey()` avisando que a **Fase 120 previa
       fazer o bump `v12 → v13`** (critério 3 do ROADMAP) e que, como a 119.1 entrou na frente
       e consumiu o `v13`, a Fase 120 deve subir para `v14`.

    Commitar só estes 5 arquivos com `git commit -- <paths>`.
  </action>

  <verify>
    <automated>php artisan test --filter=NpsFloorD1Test && php artisan test --filter=DesempenhoMetadadosCacheTest && php artisan test --filter=NpsInvalidacaoRespostaTest && php artisan test --filter=DesempenhoShopeeScoreTest</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "desempenho.compute.v13" app/Services/DesempenhoScoreService.php` retorna 1.
    - `grep -rc "desempenho.compute.v12" tests/ app/ | grep -v ':0' | wc -l` retorna 0 (nenhuma ocorrência de `v12` sobrou).
    - `grep -c "notasSemLink" app/Services/DesempenhoScoreService.php` retorna ≥ 2 (definição + chamada no ramo D).
    - `git diff HEAD~1 -- app/Services/DesempenhoScoreService.php | grep -c "computeNpsWindow"` retorna 0 (a janela não foi tocada).
    - `php artisan test --filter=Phase118` sai 0 (`NpsJanelaResolverTest` intacto).
    - Os 4 testes do comando de verificação saem 0.
  </acceptance_criteria>

  <done>O ramo (D) está em produção no cálculo do bônus, o docblock não mente mais sobre D3, e o cache não serve mais a regra antiga.</done>
</task>

<task type="auto">
  <name>Task 3: Recalcular os 5 hash-gates da Fase 119 (uma única vez, no fim)</name>

  <read_first>
    - `tests/Feature/Phase119/CompanyScoreServiceFonteTest.php` (a constante `HASH_DESEMPENHO_SCORE_SERVICE` e o método `assertHashDesempenhoScoreServiceIntocado()`)
    - `.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-PATTERNS.md` (Shared Pattern D — como recalcular)
  </read_first>

  <files>tests/Feature/Phase119/CompanyScoreServiceFonteTest.php, tests/Feature/Phase119/CompanyScoreServiceDispatcherTest.php, tests/Feature/Phase119/CompanyScoreServiceMargemTest.php, tests/Feature/Phase119/CompanyScoreServiceReconciliacaoTest.php, tests/Feature/Phase119/CompanyScoreServiceStatusTest.php</files>

  <action>
    A Fase 119 foi construída como ADITIVA sobre `DesempenhoScoreService.php` e blindou essa
    premissa com um SHA-256 do arquivo inteiro, travado em 5 arquivos de teste. A Fase 119.1
    quebra essa aditividade **de propósito** (o ramo D é o objetivo da fase) — recalcular o
    hash é o preço esperado, não um bug.

    Executar UMA ÚNICA VEZ, depois de a Task 2 estar fechada e sem mais edições pendentes em
    `DesempenhoScoreService.php`:

      php -r "echo hash_file('sha256', 'app/Services/DesempenhoScoreService.php'), PHP_EOL;"

    Substituir o valor da constante `HASH_DESEMPENHO_SCORE_SERVICE` nos 5 arquivos listados em
    `<files>` pelo hash novo. Acrescentar, em cada arquivo, um comentário pt-BR de uma linha
    acima da constante registrando que o hash foi rotacionado pela Fase 119.1 ao adicionar o
    4º ramo (D1) — para quem vier depois não achar que a aditividade da Fase 119 foi violada
    por acidente.

    Se `php artisan test --filter=Phase119` ainda acusar divergência de hash depois da troca,
    é porque `DesempenhoScoreService.php` foi editado de novo entre o cálculo e o commit —
    recalcular e repetir, **não** relaxar a asserção nem apagar o gate.

    Commitar só estes 5 arquivos com `git commit -- <paths>`.
  </action>

  <verify>
    <automated>php artisan test --filter=Phase119</automated>
  </verify>

  <acceptance_criteria>
    - `php artisan test --filter=Phase119` sai 0 (os 5 arquivos de hash-gate verdes).
    - `grep -rh "HASH_DESEMPENHO_SCORE_SERVICE = " tests/Feature/Phase119/ | sort -u | wc -l` retorna 1 (os 5 arquivos com o MESMO hash).
    - O hash presente nos arquivos é igual à saída de `php -r "echo hash_file('sha256','app/Services/DesempenhoScoreService.php');"`.
    - `grep -rc "assertHashDesempenhoScoreServiceIntocado" tests/Feature/Phase119/ | grep -v ':0' | wc -l` retorna 5 (nenhum gate foi apagado).
  </acceptance_criteria>

  <done>Os 5 hash-gates apontam para o arquivo novo, continuam existindo, e a suíte Phase119 está verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| cálculo de leitura → bonificação financeira | O ramo (D) muda a nota que decide quem bate o bônus; erro aqui vira dinheiro pago errado |
| cache Redis compartilhado → payload do bônus | Payload cacheado por até 7 dias atravessa o deploy se a chave não subir de versão |

## STRIDE Threat Register

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|------------|-----------|
| T-119.1-09 | Tampering | Ramo (D) contando duas vezes a mesma empresa | mitigate | Dedupe por `company_id\|template_id\|role` + descarte de itens com survey existente (disjunção com A/B/C), provados em `NpsFloorD1Test` |
| T-119.1-10 | Elevation of Privilege | Penalizar pessoa por empresa que ela não cuida | mitigate | Responsável resolvido só por `responsavelDoServicoOuConsolidado()`; teste com empresa de outro responsável assere ausência de nota |
| T-119.1-11 | Information Disclosure | Cache servindo bônus da regra antiga após deploy | mitigate | Bump obrigatório `desempenho.compute.v12` → `v13` + gate `grep` provando zero ocorrências de `v12` |
| T-119.1-12 | Repudiation | Hash-gate silenciosamente relaxado para "passar a suíte" | mitigate | Acceptance exige os 5 `assertHashDesempenhoScoreServiceIntocado` presentes e os 5 hashes idênticos entre si |
| T-119.1-13 | Denial of Service | `empresasElegiveis()` chamado por usuário dentro de laço do dashboard | accept | Consumido dentro de `compute()`, que já é cacheado por usuário/período; volume de empresas é baixo |
| T-119.1-SC | Tampering | Instalação de pacotes | mitigate | Nenhum pacote novo; qualquer `composer require`/`npm install` neste plano é desvio e deve parar a execução |
</threat_model>

<verification>
1. `php artisan test --filter=Phase119_1` verde.
2. `php artisan test --filter=Phase119` verde (hash-gates).
3. `php artisan test --filter=Phase118` verde (janela intocada).
4. `php artisan test --filter=Desempenho` sem falha nova versus a baseline de 14 falhas
   pré-existentes registrada em `deferred-items.md` — a contagem NÃO pode crescer.
5. `grep -rn "desempenho.compute.v12" app/ tests/` retorna vazio.
6. Nenhuma migration nova: `git diff --name-only <base> | grep database/migrations` vazio.
</verification>

<success_criteria>
- Empresa elegível sem link no mês fechado puxa nota 1 do estrategista e do analista.
- Empresa não elegível (4 motivos) e empresa com o mês ainda aberto continuam fora.
- Empresa sem contato cadastrado conta 1 (D5).
- `nps_imputed_assignments` intacta — nenhuma migration, nenhuma escrita nova.
- Cache em `v13`, os 4 testes com a string atualizados, os 5 hash-gates rotacionados.
</success_criteria>

<output>
Criar `.planning/phases/119.1-nps-manual-sem-duplicidade-e-por-grupo-de-empresas/119.1-03-SUMMARY.md` ao terminar.
Registrar no SUMMARY, em destaque, que a Fase 120 precisa subir a chave de cache para `v14`
(o critério 3 do ROADMAP da Fase 120 previa usar `v13`, que esta fase consumiu).
</output>
