---
phase: 46-historico-longitudinal-scores-desempenho
plan: "02"
type: execute
wave: 2
depends_on: ["46-01"]
autonomous: true
requirements:
  - REQ-46-02
files_modified:
  - app/Http/Controllers/PerformanceController.php
  - routes/web.php
  - tests/Feature/DesempenhoEvolucaoTest.php
must_haves:
  truths:
    - "Cada item do ranking em PerformanceController::index() carrega `delta_vs_ontem` (nullable float) e `delta_vs_semana_passada` (nullable float) lidos da tabela de snapshots"
    - "delta_vs_ontem = score_hoje − score do snapshot mais recente STRICTLY ANTERIOR a hoje (não exige ser exatamente D-1; se cron falhou, pega último disponível)"
    - "delta_vs_semana_passada = score_hoje − score do snapshot mais recente com ref_date ≤ today−7d (LIMIT 1 ORDER BY ref_date DESC)"
    - "Se nenhum snapshot anterior existe, ambos os deltas são `null` — UI lida graciosamente"
    - "Novo endpoint `GET /api/performance/{user}/evolucao?period=30` retorna response()->json com lista `[{date, score, ranking_pos}, ...]` ordenada ASC por date"
    - "Endpoint usa mesmo gate de permissão de `/performance` (middleware `permission:core.performance`)"
    - "Período aceito: 7, 30, 90, 180 (clamp); default 30"
  artifacts:
    - path: "app/Http/Controllers/PerformanceController.php"
      provides: "Método enriquecido `index()` + novo método público `evolucao(Request, User)`"
    - path: "routes/web.php"
      provides: "GET /api/performance/{user}/evolucao com name `performance.evolucao`"
    - path: "tests/Feature/DesempenhoEvolucaoTest.php"
      provides: "Cobertura PHPUnit: deltas calculados, deltas null sem histórico, endpoint 200/403, payload ordenado"
  key_links:
    - from: "PerformanceController::index() (ranking enrichment)"
      to: "DesempenhoScoreSnapshot::where(user_id=...)->where(ref_date < today)->orderBy(ref_date,desc)->first()"
      via: "leitura escalável: 1 query agrupada por user via collection ou pluck"
      pattern: "evitar N+1 — usar groupBy/keyBy"
    - from: "routes/web.php GET /api/performance/{user}/evolucao"
      to: "PerformanceController::evolucao(Request, User)"
      via: "middleware('permission:core.performance')"
      pattern: "mesmo gate de /performance — routes/web.php:242-247"
    - from: "PerformanceController::evolucao()"
      to: "DesempenhoScoreSnapshot::where(user_id=$user->id)->where(ref_date >= now()->subDays($period))->orderBy(ref_date)->get(['ref_date','score','ranking_pos'])"
      via: "query simples filtrada por janela"
      pattern: "endpoint JSON sob demanda — mesmo padrão de NotificacaoController:85-95"
---

<objective>
Wave 2 entrega a camada de leitura: PerformanceController::index() passa a enriquecer cada item do ranking com `delta_vs_ontem` e `delta_vs_semana_passada` lidos da tabela de snapshots criada na Wave 1, e expõe novo endpoint `GET /api/performance/{user}/evolucao` que serve JSON com a curva de score do user ao longo de N dias (default 30). Frontend da Wave 3 consome esses dados.

Nenhuma mudança de UI ainda. Endpoint é JSON puro consumido via fetch. Permissão idêntica à `/performance` (core.performance).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-CONTEXT.md
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-RESEARCH.md
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Enriquecer ranking em PerformanceController::index() com deltas (RED → GREEN)</name>
  <files>
    - app/Http/Controllers/PerformanceController.php
    - tests/Feature/DesempenhoEvolucaoTest.php
  </files>
  <read_first>
    - app/Http/Controllers/PerformanceController.php (linhas 22-127) — método index() existente (Phase 49 UAT removeu setor=polos)
    - app/Models/DesempenhoScoreSnapshot.php (criado na Wave 1)
    - app/Http/Controllers/PerformanceController.php (linhas 70-96) — shape de cada item do ranking que precisa ser enriquecido
  </read_first>
  <behavior>
    Após o ranking ser montado e ordenado (linha 108-114 do controller atual), adicionar enrichment de deltas SEM rodar N+1:

    1. Coletar `$userIds = $ranking->pluck('id')->all();`
    2. 1 query: pegar último snapshot anterior a hoje para cada user
       ```php
       $snapshotsOntem = DB::table('desempenho_score_snapshots')
           ->whereIn('user_id', $userIds)
           ->where('ref_date', '<', now()->toDateString())
           ->orderBy('ref_date', 'desc')
           ->get(['user_id', 'ref_date', 'score']);
       // keyBy mantém só o mais recente por user (após orderBy DESC + first() na collection)
       $ontem = $snapshotsOntem->groupBy('user_id')->map->first();
       ```
    3. 1 query: pegar último snapshot com ref_date <= today-7d
       ```php
       $snapshotsSemana = DB::table('desempenho_score_snapshots')
           ->whereIn('user_id', $userIds)
           ->where('ref_date', '<=', now()->subDays(7)->toDateString())
           ->orderBy('ref_date', 'desc')
           ->get(['user_id', 'ref_date', 'score']);
       $semanaPassada = $snapshotsSemana->groupBy('user_id')->map->first();
       ```
    4. Mapear o ranking adicionando 2 campos a cada item:
       ```php
       $ranking = $ranking->map(function ($r) use ($ontem, $semanaPassada) {
           $scoreHoje = (float) $r['score'];
           $rOntem  = $ontem->get($r['id']);
           $rSemana = $semanaPassada->get($r['id']);
           $r['delta_vs_ontem']          = $rOntem  ? round($scoreHoje - (float) $rOntem->score, 1)  : null;
           $r['delta_vs_semana_passada'] = $rSemana ? round($scoreHoje - (float) $rSemana->score, 1) : null;
           return $r;
       });
       ```
    5. Inertia::render permanece igual; ranking agora carrega os 2 novos campos por item.

    Casos a cobrir nos testes:
    - test_delta_null_quando_sem_snapshot_anterior: user sem nenhum snapshot anterior a hoje → delta_vs_ontem=null E delta_vs_semana_passada=null
    - test_delta_vs_ontem_calculado_corretamente: snapshot ontem score=70, hoje score=75 → delta_vs_ontem=5.0
    - test_delta_vs_semana_passada_usa_mais_recente_dentro_da_janela: snapshots em D-7, D-8, D-9 → usa D-7 (mais recente ≤ today-7d), não D-9
    - test_delta_vs_ontem_pode_ser_negativo: snapshot ontem score=80, hoje score=70 → delta_vs_ontem=-10.0
    - test_delta_vs_ontem_pega_anterior_disponivel_se_cron_falhou: snapshot mais recente é D-3 (não D-1) → ainda usa D-3 para delta_vs_ontem
  </behavior>
  <action>
    1. RED — criar `tests/Feature/DesempenhoEvolucaoTest.php` com os 5 testes acima usando RefreshDatabase. Setup: criar user com cargo `analista` via user_setores+cargos, inserir snapshots fakes diretamente em `desempenho_score_snapshots`, criar empresa básica para que `PortfolioScoreService::compute()` não exploda (ou mockar o service no container via `$this->instance(PortfolioScoreService::class, Mockery::mock(...))`). Rodar `php artisan test --filter=DesempenhoEvolucaoTest` → DEVE FALHAR (campos de delta ainda não existem no ranking).
    2. Commitar RED: `test(46-02): RED — testes de delta no ranking de PerformanceController`
    3. Editar `app/Http/Controllers/PerformanceController.php`:
       - Adicionar `use App\Models\DesempenhoScoreSnapshot;` no topo
       - Logo após o ranking final ser montado (após linha 114, mas antes do filtro de cargo na linha 117), inserir o enrichment de deltas conforme <behavior>
       - IMPORTANTE: o filtro de cargo da linha 117 deve permanecer DEPOIS do enrichment (filtra por cargo_slug; deltas já anexados; tudo igual antes)
    4. Rodar `php artisan test --filter=DesempenhoEvolucaoTest` → GREEN
    5. Commitar GREEN: `feat(46-02): enriquece ranking de /performance com delta_vs_ontem e delta_vs_semana_passada`
  </action>
  <verify>
    <automated>php artisan test --filter=DesempenhoEvolucaoTest</automated>
    Todos os 5 testes GREEN. `php artisan test tests/Feature/PerformanceCargoFilterTest.php` (criado em 49-01) continua GREEN — sem regressão.
  </verify>
  <done>index() enriquecido sem N+1 (2 queries adicionais total para deltas); testes GREEN; testes da Phase 49 (PerformanceCargoFilterTest) sem regressão</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Endpoint evolução individual + rota nomeada</name>
  <files>
    - app/Http/Controllers/PerformanceController.php
    - routes/web.php
    - tests/Feature/DesempenhoEvolucaoTest.php (adiciona casos)
  </files>
  <read_first>
    - .planning/phases/46-historico-longitudinal-scores-desempenho/46-RESEARCH.md §5 (linhas 264-330) — pseudocódigo do método `historicoUser` + rota
    - routes/web.php (linhas 242-252) — rotas existentes /performance e /publicacao/desempenho como referência de gate
    - app/Http/Controllers/NotificacaoController.php — pattern de método retornando JsonResponse com response()->json([...])
  </read_first>
  <behavior>
    Novo método público em PerformanceController:
    ```php
    public function evolucao(Request $request, User $user): \Illuminate\Http\JsonResponse
    {
        $period = (int) $request->query('period', 30);
        $period = max(7, min($period, 365));

        $since = now()->subDays($period)->toDateString();

        $serie = DesempenhoScoreSnapshot::where('user_id', $user->id)
            ->where('ref_date', '>=', $since)
            ->orderBy('ref_date', 'asc')
            ->get(['ref_date', 'score', 'ranking_pos'])
            ->map(fn ($s) => [
                'date'        => $s->ref_date->toDateString(),
                'score'       => (int) $s->score,
                'ranking_pos' => $s->ranking_pos !== null ? (int) $s->ranking_pos : null,
            ]);

        return response()->json([
            'user_id' => $user->id,
            'period'  => $period,
            'serie'   => $serie,
        ]);
    }
    ```

    Rota nova em routes/web.php (logo após /publicacao/desempenho linha 252):
    ```php
    // Phase 46 — endpoint JSON da curva de evolução do score de um user
    Route::get('/api/performance/{user}/evolucao', [PerformanceController::class, 'evolucao'])
        ->middleware('permission:core.performance')
        ->name('performance.evolucao');
    ```

    Casos a cobrir:
    - test_evolucao_retorna_200_com_permission_core_performance: actingAs(admin) → GET /api/performance/{user}/evolucao → 200 + JSON com chaves user_id, period, serie
    - test_evolucao_retorna_403_sem_permission: actingAs(user sem core.performance) → 403
    - test_evolucao_serie_ordenada_asc_por_date: inserir snapshots em D-5, D-3, D-1 → serie no JSON sai ordenada D-5 → D-3 → D-1
    - test_evolucao_respeita_period_query: ?period=7 → só inclui snapshots dos últimos 7d
    - test_evolucao_clamp_period: ?period=999 → trata como 365; ?period=2 → trata como 7
    - test_evolucao_serie_vazia_quando_sem_snapshots: user sem snapshots → serie = []
  </behavior>
  <action>
    1. RED — adicionar os 6 testes acima ao `DesempenhoEvolucaoTest.php`. Rodar `php artisan test --filter=DesempenhoEvolucaoTest` → DEVEM FALHAR (rota não existe). Commitar RED: `test(46-02): RED — endpoint /api/performance/{user}/evolucao`
    2. Editar `app/Http/Controllers/PerformanceController.php`:
       - Adicionar método `evolucao(Request $request, User $user): JsonResponse` conforme <behavior>
       - Adicionar `use Illuminate\Http\JsonResponse;` se necessário
    3. Editar `routes/web.php` para adicionar a rota nomeada `performance.evolucao` com middleware `permission:core.performance` (DENTRO do mesmo grupo auth onde estão as outras rotas de /performance)
    4. Rodar `php artisan route:list --name=performance.evolucao` → confirma rota registrada com método GET e middleware correto
    5. Rodar `php artisan test --filter=DesempenhoEvolucaoTest` → GREEN
    6. Commitar GREEN: `feat(46-02): endpoint GET /api/performance/{user}/evolucao com payload ordenado`
  </action>
  <verify>
    <automated>php artisan route:list --name=performance.evolucao && php artisan test --filter=DesempenhoEvolucaoTest</automated>
    Rota listada com método GET, middleware permission:core.performance, name performance.evolucao. Todos os testes GREEN.
  </verify>
  <done>Endpoint funcional retornando JSON ordenado; testes RED→GREEN separados; gate de permissão idêntico ao /performance</done>
</task>

</tasks>

<verification>
1. `php artisan test --filter=DesempenhoEvolucaoTest` → todos GREEN (11+ asserções)
2. `php artisan route:list --name=performance.evolucao` → GET /api/performance/{user}/evolucao + middleware permission:core.performance
3. Smoke: como admin autenticado, `curl -b cookie.txt http://localhost:8000/api/performance/1/evolucao?period=30` retorna JSON `{user_id, period, serie: [...]}` mesmo que serie esteja vazia (pré-deploy não tem snapshots)
4. Smoke: visitar `/performance` no browser e inspecionar payload Inertia (DevTools → Network → última requisição inertia) → cada item do ranking tem `delta_vs_ontem` e `delta_vs_semana_passada` (null aceitável pré-deploy do schedule)
5. Sem regressão: `php artisan test tests/Feature/PerformanceCargoFilterTest.php tests/Feature/PublicacaoDesempenhoRouteTest.php tests/Feature/DesempenhoScoreSnapshotTest.php` → todos continuam GREEN
</verification>

<success_criteria>
- [ ] PerformanceController::index() enriquece cada item do ranking com delta_vs_ontem e delta_vs_semana_passada
- [ ] Enrichment usa no máximo 2 queries adicionais (sem N+1)
- [ ] Quando não há snapshot anterior, deltas são null (não 0, não exception)
- [ ] Novo método público evolucao() retorna JsonResponse com payload `{user_id, period, serie}`
- [ ] Serie ordenada ASC por date; ranking_pos incluído por ponto
- [ ] Rota GET /api/performance/{user}/evolucao registrada com middleware permission:core.performance e name performance.evolucao
- [ ] Period clamp: min 7, max 365, default 30
- [ ] Testes PHPUnit GREEN: 5 testes de delta no index() + 6 testes de evolucao endpoint
- [ ] Sem regressão em testes de Phase 49 nem em testes Wave 1
- [ ] Commits separados RED → GREEN para cada bloco TDD
</success_criteria>

<output>
Criar `.planning/phases/46-historico-longitudinal-scores-desempenho/46-02-SUMMARY.md` com: linhas modificadas em PerformanceController e routes/web.php, output de route:list, asserções dos testes GREEN, e shape final do payload do endpoint. Próximo: Wave 3 (46-03-PLAN.md — UI de delta + drawer com gráfico).
</output>
