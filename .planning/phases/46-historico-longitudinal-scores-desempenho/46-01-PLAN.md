---
phase: 46-historico-longitudinal-scores-desempenho
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - REQ-46-01
files_modified:
  - database/migrations/2026_06_30_000001_create_desempenho_score_snapshots_table.php
  - app/Models/DesempenhoScoreSnapshot.php
  - app/Console/Commands/SnapshotDesempenhoScores.php
  - routes/console.php
  - tests/Feature/DesempenhoScoreSnapshotTest.php
must_haves:
  truths:
    - "Tabela `desempenho_score_snapshots` existe com unique(['user_id','ref_date']) — re-rodar mesmo dia NÃO duplica, atualiza"
    - "Comando `php artisan desempenho:snapshot-scores` itera os mesmos users do PerformanceController::index() (whereExists user_setores → cargos.slug IN ['analista','estrategista']) e persiste 1 linha/user via updateOrCreate"
    - "Após o lote: ranking_pos populado para a data via UPDATE com ROW_NUMBER() OVER (ORDER BY score DESC)"
    - "breakdown_json carrega o array `metricas` completo do PortfolioScoreService::compute() (cast → array no model)"
    - "Schedule diário às 13:30 BRT em routes/console.php com timezone('America/Sao_Paulo'), withoutOverlapping(), onOneServer() — depois da cascata D-1 (13:00)"
    - "Testes Feature: idempotência (2 runs mesmo dia = 1 linha), breakdown serializado como JSON, ranking_pos ordenado por score DESC"
  artifacts:
    - path: "database/migrations/2026_06_30_000001_create_desempenho_score_snapshots_table.php"
      provides: "Tabela desempenho_score_snapshots — espelho de polos_faturamento_snapshots"
      contains: "Schema::create + unique(['user_id','ref_date']) + index(['ref_date','score']) + guard Schema::hasTable"
    - path: "app/Models/DesempenhoScoreSnapshot.php"
      provides: "Model Eloquent com cast breakdown_json → array e $fillable"
    - path: "app/Console/Commands/SnapshotDesempenhoScores.php"
      provides: "Comando Artisan `desempenho:snapshot-scores`"
      contains: "constructor DI de PortfolioScoreService + handle() com try/catch + ranking_pos UPDATE"
    - path: "routes/console.php"
      provides: "Schedule entry — dailyAt('13:30') BRT"
    - path: "tests/Feature/DesempenhoScoreSnapshotTest.php"
      provides: "Cobertura PHPUnit: idempotência + ranking_pos + breakdown_json"
  key_links:
    - from: "SnapshotDesempenhoScores::handle()"
      to: "PortfolioScoreService::compute(User)"
      via: "constructor injection (DI)"
      pattern: "promoted property — vide PerformanceController:20"
    - from: "SnapshotDesempenhoScores user iteration"
      to: "User whereExists user_setores → cargos.slug IN ['analista','estrategista']"
      via: "mesmo filtro de PerformanceController::index() (linhas 47-54)"
      pattern: "fonte canônica desde quick 260610-f69 — `role` é legacy"
    - from: "routes/console.php Schedule::command('desempenho:snapshot-scores')"
      to: "13:30 BRT timezone America/Sao_Paulo"
      via: "dailyAt('13:30')->timezone(...)->withoutOverlapping()->onOneServer()"
      pattern: "mesmo de sugadores-shadow-ml-daily em routes/console.php:159-164"
---

<objective>
Criar o backbone de persistência diária do score: migration enxuta + model + comando Artisan que itera os mesmos users que aparecem no ranking de `/performance`, chama `PortfolioScoreService::compute()` para cada um, e persiste 1 snapshot/user/dia via `updateOrCreate`. Schedule diário às 13:30 BRT (logo após a cascata D-1 terminar às 13:00). Esta wave entrega a base — Wave 2 lê desta tabela.

Sem mudança de UI nesta wave. Sem alteração no PerformanceController existente. Pure backend write-path + agendamento + cobertura PHPUnit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-CONTEXT.md
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Migration desempenho_score_snapshots (RED — testes primeiro)</name>
  <files>
    - database/migrations/2026_06_30_000001_create_desempenho_score_snapshots_table.php
    - tests/Feature/DesempenhoScoreSnapshotTest.php
  </files>
  <read_first>
    - database/migrations/2026_06_22_000000_create_polos_faturamento_snapshots_table.php (linhas 7-61) — template literal (guard + Schema::create + unique composto + docblock)
    - .planning/phases/46-historico-longitudinal-scores-desempenho/46-RESEARCH.md §1 (linhas 15-71) — convenções confirmadas
  </read_first>
  <behavior>
    Schema final da tabela (exato):
    - id (PK)
    - user_id → FK constrained users + cascadeOnDelete
    - ref_date (date, indexed)
    - score (unsignedTinyInteger 0-100)
    - classificacao (string 16) — 'excelente'|'bom'|'atencao'|'critico'
    - ranking_pos (unsignedSmallInteger nullable) — populado em 2º passo no comando
    - tem_base_comparativa (boolean, default false)
    - empresas_carteira (unsignedSmallInteger, default 0)
    - empresas_eligiveis (unsignedSmallInteger, default 0)
    - breakdown_json (json) — array `metricas` do compute()
    - timestamps()
    - unique(['user_id','ref_date']) — idempotência
    - index(['ref_date','score']) — ranking histórico de uma data

    RED: criar arquivo de teste com asserções sobre o schema (colunas existem, unique funciona). Rodar `php artisan test --filter=DesempenhoScoreSnapshotTest` → DEVE FALHAR (migration ainda não existe).
  </behavior>
  <action>
    1. Criar `tests/Feature/DesempenhoScoreSnapshotTest.php` usando `RefreshDatabase`. Asserções iniciais:
       - test_tabela_tem_colunas_esperadas: usa `Schema::hasColumns('desempenho_score_snapshots', [...])` para cada coluna acima
       - test_unique_constraint_user_id_ref_date: inserir 2 linhas com mesmo (user_id, ref_date) → deve lançar QueryException
    2. Rodar `php artisan test --filter=DesempenhoScoreSnapshotTest` — DEVE FALHAR com "table doesn't exist". Confirmar RED.
    3. Commitar RED: `test(46-01): RED — testes para migration desempenho_score_snapshots`
    4. Criar `database/migrations/2026_06_30_000001_create_desempenho_score_snapshots_table.php` espelhando 1:1 a estrutura de `2026_06_22_000000_create_polos_faturamento_snapshots_table.php`:
       - Docblock no topo explicando "por que existe" (Phase 46 — snapshot diário do score de carteira para deltas e gráficos de tendência)
       - Guard `if (Schema::hasTable('desempenho_score_snapshots')) return;`
       - `Schema::create(...)` com os campos listados em <behavior>
       - `$table->down()` faz dropIfExists
    5. Rodar `php artisan migrate` (ambiente dev) e `php artisan test --filter=DesempenhoScoreSnapshotTest` → GREEN
    6. Commitar GREEN: `feat(46-01): migration desempenho_score_snapshots com unique(user_id,ref_date)`
  </action>
  <verify>
    <automated>php artisan test --filter=DesempenhoScoreSnapshotTest</automated>
    Schema tem todas as colunas; unique constraint dispara em inserção duplicada.
  </verify>
  <done>Migration criada e migrada; testes de schema + unique GREEN; commits RED e GREEN separados</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Model DesempenhoScoreSnapshot + Command snapshot-scores + Schedule</name>
  <files>
    - app/Models/DesempenhoScoreSnapshot.php
    - app/Console/Commands/SnapshotDesempenhoScores.php
    - routes/console.php
    - tests/Feature/DesempenhoScoreSnapshotTest.php (adiciona casos)
  </files>
  <read_first>
    - app/Console/Commands/SyncFaturamentoMensal.php (linhas 1-37) — template do comando (signature, description, handle, return Command::SUCCESS)
    - app/Http/Controllers/PerformanceController.php (linhas 22-54) — query exata de users a iterar (whereExists user_setores → cargos analista/estrategista)
    - app/Services/PortfolioScoreService.php (linhas 56, 277-320) — assinatura `compute(User $user): array` e shape do retorno (tem_base_comparativa, empresas_eligiveis, empresas_carteira, metricas, score, classificacao)
    - routes/console.php (linhas 156-164) — pattern de Schedule::command com timezone + withoutOverlapping + onOneServer (sugadores-shadow-ml-daily)
    - .planning/phases/46-historico-longitudinal-scores-desempenho/46-RESEARCH.md §2 (linhas 75-168) — pseudocódigo do comando
  </read_first>
  <behavior>
    Model `DesempenhoScoreSnapshot`:
    - `$fillable = ['user_id','ref_date','score','classificacao','ranking_pos','tem_base_comparativa','empresas_carteira','empresas_eligiveis','breakdown_json']`
    - `$casts = ['ref_date' => 'date', 'breakdown_json' => 'array', 'tem_base_comparativa' => 'boolean']`
    - belongsTo User

    Command `SnapshotDesempenhoScores` (signature `desempenho:snapshot-scores {--data=} {--user=}`):
    - Constructor injeta `PortfolioScoreService $scoreService`
    - handle():
      1. Resolve `$refDate` (option --data ou today)
      2. Query users com `whereExists` em user_setores → cargos.slug IN ['analista','estrategista'] + filtro opcional --user
      3. Loop: para cada user, try { compute(); updateOrCreate(['user_id','ref_date'=>$refDate], [score, classificacao, tem_base_comparativa, empresas_carteira, empresas_eligiveis, breakdown_json => $result['metricas']]); $ok++; } catch (Throwable $e) { Log::error("[Desempenho] Falha snapshot user X: ..."); $fail++; continue; }
      4. Após loop: `DB::statement` para popular ranking_pos via ROW_NUMBER() OVER (ORDER BY score DESC) WHERE ref_date = ?
         - IMPORTANTE: ROW_NUMBER() é MySQL 8+/MariaDB 10.2+. Para SQLite (testes), usar fallback Laravel: buscar snapshots da data ORDER BY score DESC, e fazer UPDATE iterativo com `$index + 1`. Detectar driver via `DB::getDriverName()`.
      5. Logar `"[Desempenho] OK: {$ok} · Falhas: {$fail}"` e retornar `self::SUCCESS`

    Schedule em routes/console.php (inserir após linha 164):
    ```php
    // Phase 46 — Snapshot diário do score de desempenho de cada analista/estrategista.
    // Roda às 13:30 BRT, logo após a cascata D-1 terminar (SyncPolosFaturamentoJob 13:00).
    // Lê dados consolidados do dia + persiste em desempenho_score_snapshots para
    // alimentar deltas (vs ontem / vs semana passada) e gráfico de evolução.
    Schedule::command('desempenho:snapshot-scores')
        ->dailyAt('13:30')
        ->timezone('America/Sao_Paulo')
        ->name('desempenho-snapshot-scores')
        ->onOneServer()
        ->withoutOverlapping();
    ```

    Testes Feature adicionais (no mesmo arquivo da Tarefa 1):
    - test_command_persiste_snapshot_por_user_eligivel: cria 2 users com cargo `analista` via user_setores+cargos; mocka PortfolioScoreService retornando payload fake; roda comando; assertCount(2) em desempenho_score_snapshots
    - test_command_idempotente: roda 2x no mesmo dia → assertCount(2, snapshots) (não 4) → score/breakdown atualizam para os do 2º run
    - test_breakdown_json_persistido_como_array: lê model, $snapshot->breakdown_json é array PHP (cast funciona), contém chaves esperadas
    - test_ranking_pos_ordenado_por_score_desc: 3 users com scores 50/80/65 → ranking_pos = 3/1/2 respectivamente
    - test_ignora_users_sem_cargo_analista_ou_estrategista: user com cargo 'publicador' não gera snapshot
  </behavior>
  <action>
    1. RED — adicionar os 5 testes acima ao arquivo existente. Rodar `php artisan test --filter=DesempenhoScoreSnapshotTest` → DEVEM FALHAR (model/command ainda não existem). Commitar `test(46-01): RED — comando snapshot-scores + idempotência + ranking_pos`
    2. Criar `app/Models/DesempenhoScoreSnapshot.php` com $fillable, $casts (incluindo breakdown_json → array), belongsTo(User)
    3. Criar `app/Console/Commands/SnapshotDesempenhoScores.php` espelhando `SyncFaturamentoMensal.php`:
       - signature/description (pt-BR)
       - constructor com `private PortfolioScoreService $scoreService`
       - handle() conforme <behavior>
       - prefixo log `[Desempenho]`
       - tratamento sqlite vs mysql para ranking_pos
    4. Adicionar Schedule entry em `routes/console.php` (conforme bloco em <behavior>)
    5. Rodar `php artisan test --filter=DesempenhoScoreSnapshotTest` → GREEN
    6. Rodar `php artisan desempenho:snapshot-scores --help` para confirmar registro do comando
    7. Commitar GREEN: `feat(46-01): comando desempenho:snapshot-scores + model + schedule 13:30 BRT`
  </action>
  <verify>
    <automated>php artisan test --filter=DesempenhoScoreSnapshotTest && php artisan list | grep desempenho:snapshot-scores</automated>
    Comando registrado; todos os 5 testes GREEN; schedule listado em `php artisan schedule:list`.
  </verify>
  <done>Model+Command+Schedule criados; suite GREEN; comando aparece em `php artisan list`; schedule aparece em `php artisan schedule:list` com horário 13:30 BRT</done>
</task>

</tasks>

<verification>
1. `php artisan test --filter=DesempenhoScoreSnapshotTest` → todos GREEN (8+ asserções)
2. `php artisan migrate:status | grep desempenho_score_snapshots` → migrada
3. `php artisan list | grep desempenho:snapshot-scores` → comando registrado
4. `php artisan schedule:list | grep desempenho-snapshot-scores` → schedule 13:30 America/Sao_Paulo
5. Smoke real (manual em dev): `php artisan desempenho:snapshot-scores` → cria N linhas em desempenho_score_snapshots; segunda execução no mesmo dia mantém N linhas (não duplica)
6. Verificar via tinker: `\App\Models\DesempenhoScoreSnapshot::first()->breakdown_json` retorna array com chaves de `metricas` (crescimento_ajustado_pct, atingimento_meta.pct, etc)
</verification>

<success_criteria>
- [ ] Migration `2026_06_30_000001_create_desempenho_score_snapshots_table.php` criada com unique(['user_id','ref_date']) e index(['ref_date','score'])
- [ ] Model `DesempenhoScoreSnapshot` com cast `breakdown_json → array` e $fillable completo
- [ ] Comando `desempenho:snapshot-scores` registrado, com DI de PortfolioScoreService e ranking_pos populado pós-lote
- [ ] Schedule entry em `routes/console.php` às 13:30 BRT com `withoutOverlapping()` + `onOneServer()`
- [ ] Idempotência: re-run no mesmo dia atualiza linha existente (unique constraint impede duplicata)
- [ ] Testes PHPUnit GREEN para: schema, unique, comando persiste, idempotência, breakdown_json array cast, ranking_pos ordenado, filtro de cargos
- [ ] Commits separados RED → GREEN para cada bloco TDD
</success_criteria>

<output>
Criar `.planning/phases/46-historico-longitudinal-scores-desempenho/46-01-SUMMARY.md` ao final com: linhas das migrations criadas, comando registrado, schedule confirmado, testes verdes, e qualquer desvio do plano. Próximo: Wave 2 (46-02-PLAN.md — leitura dos snapshots + endpoint de evolução).
</output>
