---
phase: 46-historico-longitudinal-scores-desempenho
plan: "03"
type: execute
wave: 3
depends_on: ["46-02"]
autonomous: true
requirements:
  - REQ-46-03
files_modified:
  - resources/js/Pages/Performance/Index.jsx
must_haves:
  truths:
    - "Cada linha da tabela RankingConsultoria mostra micro-indicadores `↑ +2.3 hoje · ↑ +5.1 sem.` ao lado do ScorePill, com cores verde/vermelho/cinza"
    - "Quando delta é null (sem snapshot anterior), mostra `—` em cinza claro — não quebra, não aparece como 0"
    - "Componente local `ScoreDelta` é definido no MESMO arquivo Index.jsx (NÃO criar componente reusável global)"
    - "Click no nome do profissional abre drawer lateral à direita com `max-w-2xl`, espelhando estrutura do PoloDrawer"
    - "Drawer header mostra nome, cargo, score atual e um KpiCard com delta_vs_ontem"
    - "Drawer renderiza LineChart Recharts da serie carregada via fetch('/api/performance/{id}/evolucao?period=30')"
    - "Gráfico mostra 2 séries: linha sólida do score do user + linha pontilhada da mediana do grupo (calculada client-side a partir do ranking + buscas paralelas)"
    - "Estado de loading exibido enquanto fetch está em andamento; estado vazio se serie.length=0"
    - "`npm run build` passa sem erros"
  artifacts:
    - path: "resources/js/Pages/Performance/Index.jsx"
      provides: "ScoreDelta local + Coluna delta na RankingConsultoria + clique abre EvolucaoDrawer com LineChart Recharts"
  key_links:
    - from: "RankingConsultoria row click"
      to: "useState para user selecionado + EvolucaoDrawer"
      via: "onClick handler — abre drawer ao invés de router.visit(portfolio.show)"
      pattern: "Estado controlado no PerformanceIndex (parent); drawer recebe user prop + onClose"
    - from: "EvolucaoDrawer useEffect"
      to: "fetch(route('performance.evolucao', user.id) + '?period=30')"
      via: "fetch nativo com Accept: application/json + credentials: same-origin"
      pattern: "Mesmo de NotificacaoController consumption — vide 46-RESEARCH.md §5 linhas 312-324"
    - from: "ScoreDelta component"
      to: "paleta de cores do SparklineCrescimento.jsx:22-49"
      via: "thresholds adaptados para score (>+1 verde, < -1 vermelho, entre estável)"
      pattern: "Inline component — convenção do projeto (Architecture.md)"
---

<objective>
Wave 3 entrega a UI: micro-indicadores de delta visíveis na linha de cada profissional + drawer lateral com gráfico de evolução individual de 30 dias comparado à mediana do grupo. Tudo dentro do mesmo arquivo `Performance/Index.jsx` (sem componentes reusáveis globais).

Decisões locked:
- Largura do drawer: `max-w-2xl` (acomoda Recharts ResponsiveContainer)
- Período default do gráfico: 30 dias
- Mediana calculada client-side a partir das fetches paralelas (não exige novo endpoint)
- Click no nome substitui o atual `router.visit(portfolio.show)` para a coluna nome especificamente — RankingConsultoria atual usa onClick no <div> inteiro (linha 227); precisamos refatorar para que o click na ROW abra o drawer, e adicionar uma seta dedicada de portfólio se ainda for desejável navegar para `/admin/users/{id}/portfolio` (Claude's Discretion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-CONTEXT.md
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-RESEARCH.md
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Componente ScoreDelta + coluna na RankingConsultoria</name>
  <files>resources/js/Pages/Performance/Index.jsx</files>
  <read_first>
    - resources/js/Pages/Performance/Index.jsx (linhas 184-205) — patterns existentes de tag colorido (PctTone, GrowthTone, Tendencia) — copiar estilo
    - resources/js/Pages/Performance/Index.jsx (linhas 207-275) — RankingConsultoria atual (grid header, divs de cada linha, ordem das colunas)
    - resources/js/Components/Carteira/SparklineCrescimento.jsx (linhas 22-49) — paleta de cores e setas Unicode ↑/↓/→ (referência canônica do projeto)
    - .planning/phases/46-historico-longitudinal-scores-desempenho/46-RESEARCH.md §3 (linhas 175-214) — pseudocódigo do ScoreDelta
  </read_first>
  <behavior>
    Adicionar logo após o componente `Tendencia` (linha 205) o novo componente local:

    ```jsx
    function ScoreDelta({ delta, label }) {
        if (delta === null || delta === undefined) {
            return <span className="text-white/20 font-semibold tabular-nums text-[10px]">{label}: —</span>;
        }
        const cls = delta > 1 ? 'text-emerald-300' : delta < -1 ? 'text-red-300' : 'text-white/40';
        const arrow = delta > 1 ? '↑' : delta < -1 ? '↓' : '→';
        const sign = delta > 0 ? '+' : '';
        return (
            <span className={cn('inline-flex items-center gap-0.5 text-[10px] font-semibold tabular-nums', cls)}>
                <span aria-hidden="true">{arrow}</span>
                <span>{sign}{delta.toFixed(1)}</span>
                <span className="text-white/30 ml-0.5">{label}</span>
            </span>
        );
    }
    ```

    Modificar `RankingConsultoria` (linhas 207-275) para incluir os deltas:
    - Decisão de layout: usar a coluna Score existente (template linha 210) — abaixo do `<ScorePill ... />` adicionar uma linha de deltas compacta com `flex flex-col` envolvendo o ScorePill + dois ScoreDelta
    - Ajustar a div da coluna Score (atualmente linha 253 = `<ScorePill .../>`) para:
      ```jsx
      <div className="flex flex-col gap-0.5">
          <ScorePill score={u.score} classif={u.classificacao} />
          <div className="flex items-center gap-1.5">
              <ScoreDelta delta={u.delta_vs_ontem}          label="hoje" />
              <span className="text-white/15">·</span>
              <ScoreDelta delta={u.delta_vs_semana_passada} label="sem." />
          </div>
      </div>
      ```
    - NÃO mexer no header (linha 210-221) — a coluna mantém o mesmo tamanho `8rem`; os deltas cabem dentro dela
  </behavior>
  <action>
    1. Ler `resources/js/Pages/Performance/Index.jsx` por inteiro (já lido parcialmente no read_first; agora ler completo para saber onde inserir)
    2. Adicionar componente `ScoreDelta` após `Tendencia` (linha 205)
    3. Localizar `<ScorePill score={u.score} classif={u.classificacao} />` na linha 253 e substituir pelo bloco `flex flex-col` com ScorePill + 2 ScoreDelta
    4. Rodar `npm run build` — deve passar sem erros
    5. Visual sanity check (dev): rodar `php artisan serve` + `npm run dev` localmente, abrir `/performance`, verificar que micro-deltas aparecem (mesmo que `—` pré-deploy)
    6. Commitar: `feat(46-03): ScoreDelta local + micro-indicadores de delta no ranking de /performance`
  </action>
  <verify>
    <automated>npm run build</automated>
    Build verde, sem erros de import. Visual confere com `—` para deltas null no estado pré-deploy.
  </verify>
  <done>ScoreDelta inline criado; coluna Score mostra ScorePill + 2 deltas; build verde</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: EvolucaoDrawer com LineChart Recharts + mediana do grupo</name>
  <files>resources/js/Pages/Performance/Index.jsx</files>
  <read_first>
    - resources/js/Pages/Polos/Index.jsx (linhas 399-436) — PoloDrawer header + backdrop + aside container (template a copiar 1:1)
    - resources/js/Pages/Performance/Index.jsx (linhas 1-10) — imports atuais; verificar quais lucide-react icons já estão e adicionar `X` e `TrendingUp`/`TrendingDown` se faltarem
    - resources/js/Pages/PainelExecutivo/components/KpiCard.jsx (linhas 101-114) — KpiCard com deltaPct (reuso para card de score atual no header do drawer)
    - .planning/phases/46-historico-longitudinal-scores-desempenho/46-RESEARCH.md §4 (linhas 216-260) — estrutura completa do drawer
  </read_first>
  <behavior>
    Adicionar componente local `EvolucaoDrawer` no mesmo arquivo, ANTES do `export default function PerformanceIndex` (linha 75) ou após `RankingConsultoria` — escolher posição que facilite a leitura:

    ```jsx
    function EvolucaoDrawer({ user, rankingItem, allRankingIds, onClose }) {
        const [serie, setSerie] = useState(null);
        const [grupoMedianoPorData, setGrupoMedianoPorData] = useState(null);
        const [loading, setLoading] = useState(true);

        useEffect(() => {
            if (!user) return;
            setLoading(true);

            // 1. fetch do user selecionado
            const fetchUser = fetch(route('performance.evolucao', user.id) + '?period=30', {
                headers: { 'Accept': 'application/json' },
                credentials: 'same-origin',
            }).then(r => r.json());

            // 2. fetch paralelo de todos os outros users do ranking (para mediana)
            const fetchGrupo = Promise.all(
                allRankingIds
                    .filter(id => id !== user.id)
                    .map(id =>
                        fetch(route('performance.evolucao', id) + '?period=30', {
                            headers: { 'Accept': 'application/json' },
                            credentials: 'same-origin',
                        }).then(r => r.json()).catch(() => ({ serie: [] }))
                    )
            );

            Promise.all([fetchUser, fetchGrupo]).then(([userData, grupoData]) => {
                setSerie(userData.serie ?? []);

                // calcular mediana por data
                const porData = {};
                grupoData.forEach(d => {
                    (d.serie ?? []).forEach(p => {
                        if (!porData[p.date]) porData[p.date] = [];
                        porData[p.date].push(p.score);
                    });
                });
                const mediana = {};
                Object.keys(porData).forEach(date => {
                    const sorted = [...porData[date]].sort((a, b) => a - b);
                    const mid = Math.floor(sorted.length / 2);
                    mediana[date] = sorted.length % 2 === 0
                        ? (sorted[mid - 1] + sorted[mid]) / 2
                        : sorted[mid];
                });
                setGrupoMedianoPorData(mediana);
                setLoading(false);
            });
        }, [user, allRankingIds]);

        // merge serie do user + mediana para o LineChart
        const chartData = (serie ?? []).map(p => ({
            date: p.date,
            score: p.score,
            mediana: grupoMedianoPorData?.[p.date] ?? null,
        }));

        return (
            <div className="fixed inset-0 z-50 flex justify-end">
                <div className="absolute inset-0 bg-black/60 backdrop-blur-sm" onClick={onClose} />
                <aside className="relative h-full w-full max-w-2xl overflow-y-auto border-l border-white/[0.1] bg-ecf-bg shadow-2xl">
                    {/* header sticky */}
                    <div className="sticky top-0 z-10 flex items-start justify-between gap-3 border-b border-white/[0.08] bg-ecf-bg/95 px-5 py-4 backdrop-blur">
                        <div>
                            <h2 className="text-white font-display font-extrabold text-lg leading-tight">{user.name}</h2>
                            <p className="text-white/40 text-xs">{rankingItem.cargo_label} · Score atual: {rankingItem.score}</p>
                        </div>
                        <button type="button" onClick={onClose} className="rounded-lg p-1.5 text-white/50 hover:bg-white/[0.06] hover:text-white">
                            <X size={18} />
                        </button>
                    </div>

                    {/* KpiCards no topo: score atual + delta_vs_ontem */}
                    <div className="grid grid-cols-2 gap-2 px-5 py-4 border-b border-white/[0.06]">
                        <div>
                            <p className="text-white/30 text-[10px] uppercase tracking-wider">Score hoje</p>
                            <p className="text-ecf-yellow font-display font-extrabold text-2xl mt-0.5">{rankingItem.score}</p>
                        </div>
                        <div>
                            <p className="text-white/30 text-[10px] uppercase tracking-wider">vs ontem</p>
                            <ScoreDelta delta={rankingItem.delta_vs_ontem} label="" />
                        </div>
                    </div>

                    {/* Gráfico Recharts */}
                    <div className="px-5 py-4">
                        <h3 className="text-white/60 text-xs uppercase tracking-wider mb-3">Evolução — últimos 30 dias</h3>
                        {loading && <p className="text-white/40 text-sm">Carregando histórico...</p>}
                        {!loading && chartData.length === 0 && (
                            <p className="text-white/40 text-sm">Sem snapshots históricos ainda. A curva aparecerá após alguns dias de coleta diária.</p>
                        )}
                        {!loading && chartData.length > 0 && (
                            <ResponsiveContainer width="100%" height={260}>
                                <LineChart data={chartData}>
                                    <XAxis dataKey="date" stroke="#666" fontSize={10} />
                                    <YAxis domain={[0, 100]} stroke="#666" fontSize={10} />
                                    <Tooltip contentStyle={{ background: '#0f1116', border: '1px solid rgba(255,255,255,0.1)' }} />
                                    <Line type="monotone" dataKey="score" stroke="#ffe600" strokeWidth={2} dot={false} name="Score do user" />
                                    <Line type="monotone" dataKey="mediana" stroke="rgba(255,255,255,0.4)" strokeDasharray="4 4" strokeWidth={1.5} dot={false} name="Mediana do grupo" />
                                </LineChart>
                            </ResponsiveContainer>
                        )}
                    </div>
                </aside>
            </div>
        );
    }
    ```

    Em `PerformanceIndex` (linha 75):
    - Adicionar `const [userSelecionado, setUserSelecionado] = useState(null);`
    - Em `RankingConsultoria` row, mudar o `onClick` (linha 227): atualmente faz `router.visit(route('portfolio.show', u.id))`. Substituir por `setUserSelecionado(u)` passado como prop, OU expor handler do parent. Estratégia mais simples: passar `onSelectUser={setUserSelecionado}` como prop para `RankingConsultoria` e usar dentro do row onClick.
    - Adicionar render condicional ao final do JSX do PerformanceIndex (após `</AppLayout>` NÃO — antes): `{userSelecionado && <EvolucaoDrawer user={userSelecionado} rankingItem={userSelecionado} allRankingIds={ranking.map(r => r.id)} onClose={() => setUserSelecionado(null)} />}`

    Imports a adicionar no topo:
    - De `lucide-react`: `X` (close button)
    - De `react`: `useState`, `useEffect`
    - De `recharts`: `LineChart`, `Line`, `XAxis`, `YAxis`, `Tooltip`, `ResponsiveContainer`

    Decisão sobre portfolio.show navigation:
    - O click na ROW atualmente vai para `/admin/users/{user}/portfolio` (linha 227 atual).
    - Mudança proposta: click no NOME abre drawer; click na seta `<ChevronRight>` na coluna final (linha 267-269) navega para portfolio.show.
    - Isso preserva acesso ao portfólio antigo e adiciona acesso ao drawer.
  </behavior>
  <action>
    1. Atualizar imports no topo do arquivo:
       - `import { useState, useEffect } from 'react';` (se ainda não tem useState)
       - Adicionar `X` em `from 'lucide-react'`
       - Nova linha: `import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';`
    2. Definir `EvolucaoDrawer` (componente local) após `RankingConsultoria` ou antes do `export default function PerformanceIndex`
    3. Refatorar `RankingConsultoria` para aceitar prop `onSelectUser`:
       - Mudar a assinatura: `function RankingConsultoria({ ranking, onSelectUser }) { ... }`
       - Mudar o `onClick` da row (linha 227): remover `router.visit(route('portfolio.show', u.id))` e substituir por `onSelectUser(u)` no click do bloco do nome (div com classes `<p className="text-white font-semibold...">`)
       - Manter a ChevronRight (linha 267-269) como link separado para o portfolio: envolver com `onClick={(e) => { e.stopPropagation(); router.visit(route('portfolio.show', u.id)); }}` + `cursor-pointer text-white/40 hover:text-white`
    4. No `PerformanceIndex`: adicionar `useState` para `userSelecionado`, passar `onSelectUser={setUserSelecionado}` para `RankingConsultoria`, e render condicional do `EvolucaoDrawer`
    5. Rodar `npm run build` — deve passar
    6. Smoke local: `php artisan serve` + `npm run dev` → abrir `/performance` → clicar no nome do primeiro user → drawer abre, mostra header + KpiCards + "Sem snapshots históricos ainda." (pré-deploy)
    7. Commitar: `feat(46-03): EvolucaoDrawer com Recharts LineChart + mediana do grupo no /performance`
  </action>
  <verify>
    <automated>npm run build</automated>
    Build verde; manual smoke confirma drawer abre/fecha; estado vazio renderiza sem erro JS; loading state aparece durante fetch.
  </verify>
  <done>EvolucaoDrawer funcional; estado vazio graceful; LineChart aparece quando dados chegam; ChevronRight ainda navega para portfolio.show; click no nome abre drawer</done>
</task>

</tasks>

<verification>
1. `npm run build` → sem erros
2. Browser smoke local (dev):
   - `/performance` carrega normalmente
   - Cada linha do RankingConsultoria mostra ScorePill + 2 micro-deltas (todos `—` pré-deploy)
   - Click no nome do user abre EvolucaoDrawer
   - Drawer header mostra nome, cargo_label, score atual
   - Drawer renderiza placeholder "Sem snapshots históricos ainda" pré-deploy
   - Fechar drawer via backdrop ou botão X funciona
   - ChevronRight na coluna final ainda navega para `/admin/users/{id}/portfolio`
3. Sem regressão visual no header da página (toolbar de período + filtro cargo)
4. Sem regressão em `/publicacao/desempenho` (RankingPolos não foi tocada — apenas RankingConsultoria)
</verification>

<success_criteria>
- [ ] `ScoreDelta` definido localmente em Performance/Index.jsx (não em Components/ui)
- [ ] Cada linha do ranking de consultoria exibe ScorePill + delta_vs_ontem + delta_vs_semana_passada
- [ ] Deltas null renderizam `—` em cinza claro (não 0, não bug)
- [ ] Click no nome do user abre EvolucaoDrawer com `max-w-2xl`
- [ ] EvolucaoDrawer faz fetch paralelo: 1 do user + N dos outros do ranking
- [ ] Mediana do grupo calculada client-side por data
- [ ] LineChart Recharts mostra linha sólida amarela (user) + linha pontilhada cinza (mediana)
- [ ] Estado de loading exibido enquanto fetches estão em andamento
- [ ] Estado vazio (sem snapshots ainda) exibido graciosamente
- [ ] `npm run build` verde
- [ ] ChevronRight ainda navega para portfolio.show (preservado)
- [ ] Sem regressão em RankingPolos
</success_criteria>

<output>
Criar `.planning/phases/46-historico-longitudinal-scores-desempenho/46-03-SUMMARY.md` com: linhas modificadas em Index.jsx, screenshots ou descrição do visual final, asset bundle do build, e qualquer desvio. Próximo: Wave 4 (46-04-PLAN.md — UAT humano em produção).
</output>
