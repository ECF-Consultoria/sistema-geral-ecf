---
phase: 46-historico-longitudinal-scores-desempenho
plan: "04"
type: execute
wave: 4
depends_on: ["46-03"]
autonomous: false
requirements:
  - REQ-46-04
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
must_haves:
  truths:
    - "Deploy executado para VPS Hostinger e o schedule diário rodou (ou rodará no próximo 13:30 BRT)"
    - "Pelo menos 1 snapshot existe em `desempenho_score_snapshots` após o primeiro 13:30 pós-deploy (verificável via supervisor logs ou tinker remoto)"
    - "Página `/performance` em produção renderiza micro-deltas (pré-1º snapshot: todos `—`; pós-1º snapshot: deltas reais)"
    - "Drawer abre ao clicar no nome do profissional e renderiza placeholder ou gráfico conforme dados disponíveis"
    - "Performance da página `/performance` não degradou — tempo de carregamento similar ao pré-Phase 46"
    - "Sem regressão em `/publicacao/desempenho` nem em /portfolio/{user}"
    - "ROADMAP.md marca Phase 46 como completa; STATE.md registra a Phase 46 nos completed phases"
  artifacts:
    - path: ".planning/phases/46-historico-longitudinal-scores-desempenho/46-04-UAT.md"
      provides: "Checklist UAT preenchido pelo operador humano"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 46 marcada como completa"
    - path: ".planning/STATE.md"
      provides: "Phase 46 nos completed_phases + última timestamp"
  key_links:
    - from: "Schedule daemon (supervisor) na VPS"
      to: "desempenho:snapshot-scores execução"
      via: "cron `* * * * * php artisan schedule:run` + dailyAt('13:30') timezone America/Sao_Paulo"
      pattern: "Mesma cascata de routes/console.php existente"
    - from: "UI /performance produção"
      to: "Inertia props ranking[i].delta_vs_ontem + delta_vs_semana_passada"
      via: "PerformanceController::index enriquecido"
      pattern: "Sem alterações no endpoint público — apenas leitura de tabela nova"
---

<objective>
Wave 4 é checkpoint humano: validação UAT em produção após deploy + finalização do roadmap. A Phase 46 introduz schedule diário e UI nova; precisa de verificação humana de que o cron rodou, dados aparecem no /performance, drawer funciona, e nada regrediu. Pré-deploy (antes do 1º snapshot existir) o comportamento esperado é "deltas mostram `—`, drawer mostra placeholder de estado vazio". Pós-1º snapshot (D+1 após deploy) os deltas reais começam a aparecer.

Sem código novo. Apenas:
1. Deploy (com autorização caso-a-caso por causa do dev paralelo na v11.0)
2. Smoke checklist em produção
3. Aguardar 13:30 BRT do primeiro dia pós-deploy e re-verificar
4. Aprovação final + atualização do ROADMAP/STATE
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-CONTEXT.md
@.planning/phases/46-historico-longitudinal-scores-desempenho/46-03-PLAN.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Deploy do Phase 46 para produção (VPS Hostinger)</decision>
  <context>
    Há outro desenvolvedor trabalhando em paralelo na v11.0 (Sugadores ML migration) — feedback `feedback_perguntar_antes_deploy_v9` exige confirmação caso-a-caso antes de invocar `deploy.sh`. O comando de deploy é potencialmente disruptivo (composer install --no-dev, migrate --force, supervisor restart).

    Phase 46 inclui migration nova (`2026_06_30_000001_create_desempenho_score_snapshots_table.php`) — `migrate --force` precisa rodar na VPS. Sem rollback automático em caso de falha.

    Wave 1-3 já têm testes GREEN; Wave 4 só executa em produção.
  </context>
  <options>
    <option id="deploy-full">
      <name>Deploy completo via `./deploy.sh` (sequência padrão)</name>
      <pros>1 comando; pipeline conhecido e estável; supervisor restart automático para pegar novo schedule.</pros>
      <cons>Risco de colidir com mudanças do outro dev se houver merge pendente; downtime de ~30s.</cons>
    </option>
    <option id="deploy-parcial">
      <name>Deploy parcial via `./deploy_parcial.sh` (apenas migrations + frontend)</name>
      <pros>Menos disruptivo; não toca composer/supervisor.</pros>
      <cons>Schedule novo em routes/console.php pode não ser registrado se o cache de routes não for limpo; verificar manualmente que `php artisan schedule:list` lista `desempenho-snapshot-scores` após o deploy.</cons>
    </option>
    <option id="wait">
      <name>Aguardar coordenação com o outro dev antes do deploy</name>
      <pros>Zero risco de conflito; ambiente limpo.</pros>
      <cons>Atrasa entrega; UAT fica em pausa.</cons>
    </option>
  </options>
  <resume-signal>Selecione: deploy-full, deploy-parcial, ou wait (e indique quando estará pronto)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 1-3 entregaram:
    1. Tabela `desempenho_score_snapshots` + comando `desempenho:snapshot-scores` + schedule diário 13:30 BRT
    2. PerformanceController::index() enriquece ranking com `delta_vs_ontem` e `delta_vs_semana_passada`
    3. Novo endpoint `GET /api/performance/{user}/evolucao`
    4. UI: micro-deltas na linha do ranking + drawer com LineChart Recharts + mediana do grupo

    Após o deploy aprovado na tarefa anterior, esta task valida cada um dos itens em produção.
  </what-built>
  <how-to-verify>
    **Imediatamente após o deploy (D+0):**

    1. Schedule registrado:
       - SSH na VPS Hostinger (`ssh user@177.7.53.164`)
       - Rodar `cd /home/user/public_html/ecf-admin && php artisan schedule:list | grep desempenho`
       - Esperado: linha com `desempenho-snapshot-scores · 30 13 * * * (America/Sao_Paulo)`

    2. Comando funciona manualmente:
       - Rodar `php artisan desempenho:snapshot-scores --data=2026-06-30` (data de hoje)
       - Verificar output: `[Desempenho] Snapshot YYYY-MM-DD — N users.` seguido de `[Desempenho] OK: X · Falhas: Y`
       - Verificar persistência: `php artisan tinker` → `\App\Models\DesempenhoScoreSnapshot::count()` retorna > 0
       - Verificar 1 linha: `\App\Models\DesempenhoScoreSnapshot::first()` traz score, classificacao, ranking_pos, breakdown_json (array)

    3. UI `/performance` em produção:
       - Login como admin → navegar para `/performance`
       - Cada linha do ranking de consultoria mostra ScorePill + micro-deltas (após smoke da Tarefa 2, deltas estarão `—` porque só existe 1 snapshot — sem anterior para comparar; ISSO É ESPERADO)
       - Click no nome do primeiro user → drawer abre à direita com `max-w-2xl`
       - Drawer mostra: nome + cargo + score atual + KpiCards + área do gráfico com mensagem "Sem snapshots históricos ainda. A curva aparecerá após alguns dias de coleta diária."
       - ChevronRight na linha ainda navega para `/admin/users/{id}/portfolio`
       - Fechar drawer via backdrop ou botão X funciona

    4. Endpoint JSON responde:
       - `curl -b cookie.txt https://admin.ecfconsultoria.com.br/api/performance/1/evolucao?period=30` (com cookie de sessão admin válido)
       - Esperado: 200 + `{user_id: 1, period: 30, serie: [{date, score, ranking_pos}]}` (serie pode estar quase vazia ainda)

    5. Permissão correta:
       - Tentar acessar `/api/performance/1/evolucao` SEM cookie de sessão (curl puro): redireciona para login OU 403
       - Acessar com user sem permission `core.performance`: 403

    6. Sem regressão:
       - `/publicacao/desempenho` ainda funciona (RankingPolos não foi tocado)
       - `/admin/users/{id}/portfolio` (portfolio.show) ainda carrega
       - Dashboard `/dashboard` carrega normal
       - Performance do `/performance`: tempo de carregamento via DevTools Network ≤ 1.5x do tempo pré-Phase 46 (enrichment de deltas adiciona 2 queries, devem ser milisegundos)

    **D+1 (próximo dia após 13:30 BRT):**

    7. Schedule rodou automaticamente:
       - SSH na VPS → `tail -200 /var/log/supervisor/ecf-worker-*.log` ou `tail storage/logs/laravel.log | grep Desempenho`
       - Esperado: entrada `[Desempenho] Snapshot 2026-07-01 — N users.` em torno de 13:30 BRT
       - `\App\Models\DesempenhoScoreSnapshot::where('ref_date', '2026-07-01')->count()` > 0

    8. UI mostra deltas reais:
       - Recarregar `/performance` → cada linha agora mostra `↑ +X.X hoje · ↑ +Y.Y sem.` (com setas/cores reais)
       - Click em qualquer user → drawer + LineChart Recharts renderiza com 2 pontos (D-1 e D-0) — gráfico ainda magro, mas funcional
       - Mediana do grupo aparece como linha pontilhada cinza
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se TODOS os itens 1-8 passaram.
    Digite "issue: <descrição>" para qualquer problema (cria seed para fix ou rollback).
    Digite "aguardando D+1" se o deploy foi feito mas ainda não rodou o 1º schedule (retomar amanhã).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 3: Atualizar ROADMAP.md + STATE.md marcando Phase 46 completa</name>
  <files>
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <read_first>
    - .planning/ROADMAP.md — encontrar entrada de Phase 46 (e como Phase 45 foi marcada como completa)
    - .planning/STATE.md — encontrar lista de completed_phases
  </read_first>
  <behavior>
    Após aprovação humana na tarefa anterior, marcar Phase 46 como completa nos artefatos canônicos do projeto:

    1. **ROADMAP.md** — encontrar `### Phase 46:` e:
       - Mudar status de "Em andamento" / "Planejada" para "✅ Completa (YYYY-MM-DD)"
       - Marcar todos os `[ ]` de plans como `[x]`
    2. **STATE.md** — adicionar entrada na lista de completed_phases com timestamp e link curto:
       - `- Phase 46 (histórico longitudinal de scores) — completa em YYYY-MM-DD via 46-04 UAT aprovado`
       - Atualizar a `last_updated` ou `current_position` para refletir que Phase 46 saiu da fila ativa
  </behavior>
  <action>
    1. Ler `.planning/ROADMAP.md`, localizar bloco de Phase 46
    2. Substituir status para `✅ Completa (DATA-DE-HOJE)` e marcar plans como concluídos
    3. Ler `.planning/STATE.md`, adicionar entrada de Phase 46 em completed_phases
    4. Commitar: `docs(46-04): Phase 46 marcada como completa após UAT aprovado em produção`
  </action>
  <verify>
    <automated>grep -A 3 "Phase 46" .planning/ROADMAP.md | head -10</automated>
    Status aparece como completa; STATE.md tem timestamp.
  </verify>
  <done>ROADMAP/STATE refletem que Phase 46 está em produção e validada</done>
</task>

</tasks>

<verification>
1. Após aprovação humana: ROADMAP.md mostra Phase 46 ✅ Completa
2. STATE.md inclui Phase 46 em completed_phases
3. UAT D+1 confirmou que `desempenho:snapshot-scores` rodou via schedule em produção
4. Snapshots reais existem em `desempenho_score_snapshots` (D+0 e D+1 no mínimo)
5. UI exibe deltas reais e drawer renderiza LineChart com pelo menos 2 pontos
6. Zero regressões em `/publicacao/desempenho`, `/portfolio/{user}`, `/dashboard`
</verification>

<success_criteria>
- [ ] Deploy executado com autorização específica do usuário
- [ ] `php artisan schedule:list` em produção lista `desempenho-snapshot-scores` no horário 13:30 America/Sao_Paulo
- [ ] Smoke manual de `desempenho:snapshot-scores --data=hoje` em produção persiste linhas com sucesso
- [ ] UI `/performance` exibe micro-deltas (mesmo que iniciais sejam `—`)
- [ ] Drawer abre, fecha, e renderiza placeholder/gráfico conforme dados disponíveis
- [ ] Endpoint `/api/performance/{user}/evolucao` responde 200 com payload válido
- [ ] D+1: schedule rodou automaticamente; deltas reais aparecem na UI; LineChart mostra ≥ 2 pontos
- [ ] Performance da página não degradou perceptivelmente
- [ ] ROADMAP.md e STATE.md atualizados
- [ ] Sem regressão observada em rotas adjacentes
</success_criteria>

<output>
Criar `.planning/phases/46-historico-longitudinal-scores-desempenho/46-04-SUMMARY.md` ao final com: data do deploy, opção escolhida (full/parcial), resultado do schedule:list em produção, contagem de snapshots em D+0 e D+1, screenshots ou descrição do drawer funcional, e timestamp da aprovação humana. Próximo: encerrar Phase 46. A Phase 47 (pesos por cargo) e a Phase futura (snapshot do ranking de publicação) podem reusar a tabela `desempenho_score_snapshots`.
</output>
