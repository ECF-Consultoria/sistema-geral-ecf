---
phase: 94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_07_16_100001_add_open_trail_to_nps_surveys_table.php
  - database/migrations/2026_07_16_100002_add_response_trail_and_suspicion_to_nps_responses_table.php
  - database/migrations/2026_07_16_100003_create_nps_survey_events_table.php
  - app/Models/NpsSurveyEvent.php
  - app/Models/NpsSurvey.php
  - app/Models/NpsResponse.php
  - database/factories/NpsSurveyEventFactory.php
  - config/nps.php
  - .env.example
  - app/Services/Nps/NpsSuspicionService.php
  - tests/Feature/Phase94/NpsSuspicionServiceTest.php
  - tests/Feature/Phase94/NpsAntiBurlamentoBackwardCompatTest.php
autonomous: true
requirements: [AB-94-3, AB-94-4, AB-94-5]

must_haves:
  truths:
    - "As tabelas nps_surveys e nps_responses ganham os campos de rastro (nullable) e a tabela nps_survey_events existe com os 6 event_types — sem tocar no enum nps_surveys.status"
    - "NpsSuspicionService::evaluate() aplica as 4 regras e retorna veredito com motivos em pt-BR e severidade (alta/media)"
    - "IPs/CIDRs internos e a janela de resposta rápida vêm de .env via config/nps.php — nunca hardcoded"
    - "Surveys/respostas legadas sem rastro continuam funcionando — suite Nps 207/207 permanece verde"
  artifacts:
    - path: "database/migrations/2026_07_16_100003_create_nps_survey_events_table.php"
      provides: "Tabela nps_survey_events (AB-94-3)"
      contains: "nps_survey_events"
    - path: "app/Models/NpsSurveyEvent.php"
      provides: "Model do evento com constantes TYPE_*"
      exports: ["NpsSurveyEvent"]
    - path: "app/Services/Nps/NpsSuspicionService.php"
      provides: "Serviço stateless de avaliação de suspeita (AB-94-4)"
      exports: ["NpsSuspicionService"]
    - path: "config/nps.php"
      provides: "Config anti_burlamento (internal_ips, internal_cidrs, fast_response_window_seconds)"
      contains: "anti_burlamento"
    - path: "tests/Feature/Phase94/NpsSuspicionServiceTest.php"
      provides: "Cobertura das 4 regras de suspeita"
      min_lines: 60
    - path: "tests/Feature/Phase94/NpsAntiBurlamentoBackwardCompatTest.php"
      provides: "Regressão de retrocompatibilidade (AB-94-5)"
      min_lines: 40
  key_links:
    - from: "app/Services/Nps/NpsSuspicionService.php"
      to: "config/nps.php"
      via: "config('nps.anti_burlamento...')"
      pattern: "config\\('nps\\.anti_burlamento"
    - from: "app/Services/Nps/NpsSuspicionService.php"
      to: "Symfony IpUtils"
      via: "IpUtils::checkIp"
      pattern: "IpUtils::checkIp"
---

<objective>
Fundação do Anti-Burlamento NPS: schema (colunas de rastro nullable em `nps_surveys`/`nps_responses` + tabela `nps_survey_events`), model/factory do evento, arquivo de config `.env`-driven e o `NpsSuspicionService` com as 4 regras de suspeita — tudo backend, sem nenhum ponto de código de produção instrumentado ainda (a instrumentação é dos planos 94-02/94-03).

Purpose: os planos 94-02 (NpsController) e 94-03 (NpsDispararMensal) dependem 100% deste schema, deste model e deste service. Nascer aqui, isolado, permite validar as regras de negócio sem HTTP.
Output: 3 migrations, `NpsSurveyEvent` (+factory), `NpsSurvey`/`NpsResponse` atualizados, `config/nps.php`, `NpsSuspicionService`, 2 arquivos de teste novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-CONTEXT.md
@.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-RESEARCH.md

<interfaces>
<!-- Contratos existentes que o executor usa diretamente — sem exploração de codebase. -->

De `app/Models/NpsSurvey.php` (estado atual — linhas 29-44):
```php
protected $fillable = [
    'token', 'company_id', 'generated_by', 'expires_at', 'completed_at', 'status',
    'month_reference', 'auto_generated',
    'template_id',
];

protected $casts = [
    'expires_at'      => 'datetime',
    'completed_at'    => 'datetime',
    'month_reference' => 'date',
    'auto_generated'  => 'boolean',
];
```

De `app/Models/NpsResponse.php` (estado atual — linhas 29-42):
```php
protected $fillable = [
    'survey_id', 'respondent_name', 'score_estrategista',
    'score_analista', 'score_empresa', 'comment',
];

protected $casts = [
    'score_estrategista' => 'integer',
    'score_analista'     => 'integer',
    'score_empresa'      => 'integer',
];
```

Padrão de service NPS existente (fonte: `app/Services/Nps/NpsSnapshotService.php`): classe stateless em `App\Services\Nps`, resolvida via `app(Classe::class)`, sem propriedades de instância.

`Symfony\Component\HttpFoundation\IpUtils::checkIp(string $ip, array $ranges): bool` — disponível transitivamente via `laravel/framework` (confirmado em composer.lock). Aceita IPs exatos e CIDRs (IPv4 e IPv6) na mesma lista.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations de rastro + tabela nps_survey_events + models + factory</name>
  <files>database/migrations/2026_07_16_100001_add_open_trail_to_nps_surveys_table.php, database/migrations/2026_07_16_100002_add_response_trail_and_suspicion_to_nps_responses_table.php, database/migrations/2026_07_16_100003_create_nps_survey_events_table.php, app/Models/NpsSurveyEvent.php, app/Models/NpsSurvey.php, app/Models/NpsResponse.php, database/factories/NpsSurveyEventFactory.php, tests/Feature/Phase94/NpsAntiBurlamentoBackwardCompatTest.php</files>
  <read_first>
    - database/migrations/2026_07_14_200001_create_nps_snapshot_tables.php (padrão de migration idempotente cross-driver do módulo NPS — replicar guards Schema::hasTable/hasColumn)
    - app/Models/NpsSurvey.php (fillable/casts atuais a estender)
    - app/Models/NpsResponse.php (fillable/casts atuais a estender)
    - database/factories/NpsSurveyFactory.php (shape de factory NPS existente)
    - database/factories/NpsResponseAnswerFactory.php (referência de factory mínima para model novo)
    - .planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-RESEARCH.md §Pattern 2 (snippets de referência das 3 migrations)
  </read_first>
  <behavior>
    - Teste RED (parte schema do NpsAntiBurlamentoBackwardCompatTest): Schema::hasColumn('nps_surveys', 'first_opened_at'/'last_opened_at'/'open_count'/'open_ip_address'/'open_user_agent') tudo true; Schema::hasColumn('nps_responses', 'response_ip_address'/'response_user_agent'/'response_duration_seconds'/'is_suspicious'/'suspicion_reasons') tudo true; Schema::hasTable('nps_survey_events') true
    - Teste: NpsSurveyEvent::factory()->create() persiste com survey_id válido e event_type ∈ NpsSurveyEvent::TYPES; cast metadata array funciona (grava array, lê array)
    - Teste: NpsSurvey::factory()->create() e NpsResponse criados SEM os campos novos ficam com first_opened_at/open_ip_address/suspicion_reasons null, open_count 0 e is_suspicious false (defaults de retrocompatibilidade — AB-94-5)
  </behavior>
  <action>
    Criar 3 migrations seguindo o padrão idempotente do módulo NPS (comentários pt-BR, referência "Phase 94 AB-94-X" no docblock):

    1. `2026_07_16_100001_add_open_trail_to_nps_surveys_table.php` — guard `if (! Schema::hasColumn('nps_surveys', 'first_opened_at'))`; adiciona após `completed_at`: `first_opened_at` timestamp nullable, `last_opened_at` timestamp nullable, `open_count` unsignedInteger default 0, `open_ip_address` string(45) nullable (45 = literal IPv6 máximo), `open_user_agent` text nullable. `down()` remove as 5 colunas com guard hasColumn. PROIBIDO tocar no enum `nps_surveys.status` (Pitfall 4 do RESEARCH).

    2. `2026_07_16_100002_add_response_trail_and_suspicion_to_nps_responses_table.php` — guard `if (! Schema::hasColumn('nps_responses', 'response_ip_address'))`; adiciona após `comment`: `response_ip_address` string(45) nullable, `response_user_agent` text nullable, `response_duration_seconds` unsignedInteger nullable, `is_suspicious` boolean default false, `suspicion_reasons` json nullable. `down()` simétrico com guard.

    3. `2026_07_16_100003_create_nps_survey_events_table.php` — guard `if (! Schema::hasTable('nps_survey_events'))`; colunas: id, `survey_id` foreignId constrained('nps_surveys') cascadeOnDelete, `event_type` enum('generated','opened','submitted','expired','sent_email','sent_digisac') — enum em Schema::create é seguro nos 2 drivers (só ALTER de enum é pitfall), `ip_address` string(45) nullable, `user_agent` text nullable, `user_id` foreignId **nullable()** constrained('users') nullOnDelete (nullable ANTES de nullOnDelete — erro 1830 MariaDB, memória project_mysql_nullondelete_nullable), `metadata` json nullable, timestamps; índices `idx_nps_survey_events_survey_type` em (survey_id, event_type) e `idx_nps_survey_events_created` em created_at. `down()` dropIfExists.

    Criar `app/Models/NpsSurveyEvent.php` conforme o snippet do RESEARCH §Code Examples: trait HasFactory; constantes `TYPE_GENERATED='generated'`, `TYPE_OPENED='opened'`, `TYPE_SUBMITTED='submitted'`, `TYPE_EXPIRED='expired'`, `TYPE_SENT_EMAIL='sent_email'`, `TYPE_SENT_DIGISAC='sent_digisac'` + array `TYPES` com os 6; `$fillable = ['survey_id','event_type','ip_address','user_agent','user_id','metadata']`; `$casts = ['metadata' => 'array']`; relações `survey(): BelongsTo` (NpsSurvey, FK survey_id) e `user(): BelongsTo` (User). Docblock pt-BR explicando que é trilha de auditoria técnica append-only (Phase 94 AB-94-3).

    Estender `app/Models/NpsSurvey.php`: acrescentar ao `$fillable`: 'first_opened_at','last_opened_at','open_count','open_ip_address','open_user_agent' (comentário "Phase 94 AB-94-1 — rastro de abertura"); acrescentar aos `$casts`: 'first_opened_at'=>'datetime', 'last_opened_at'=>'datetime', 'open_count'=>'integer'. Adicionar relação `events(): HasMany` para NpsSurveyEvent (FK survey_id).

    Estender `app/Models/NpsResponse.php`: acrescentar ao `$fillable`: 'response_ip_address','response_user_agent','response_duration_seconds','is_suspicious','suspicion_reasons' (comentário "Phase 94 AB-94-2/AB-94-4"); acrescentar aos `$casts`: 'response_duration_seconds'=>'integer', 'is_suspicious'=>'boolean', 'suspicion_reasons'=>'array'.

    Criar `database/factories/NpsSurveyEventFactory.php`: definition com `survey_id => NpsSurvey::factory()`, `event_type => NpsSurveyEvent::TYPE_OPENED`, `ip_address => fake()->ipv4()`, `user_agent => fake()->userAgent()`, `user_id => null`, `metadata => null`.

    TDD: escrever primeiro os asserts de schema/defaults em `tests/Feature/Phase94/NpsAntiBurlamentoBackwardCompatTest.php` (namespace `Tests\Feature\Phase94`, RefreshDatabase), rodar RED, depois criar migrations/models/factory até GREEN. Commit RED (`test(94-01): ...`) e GREEN (`feat(94-01): ...`) separados, mensagens pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsAntiBurlamentoBackwardCompatTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=NpsAntiBurlamentoBackwardCompatTest` verde (asserts de schema + defaults)
    - `grep -c "Schema::hasColumn\|Schema::hasTable" database/migrations/2026_07_16_100001_add_open_trail_to_nps_surveys_table.php database/migrations/2026_07_16_100002_add_response_trail_and_suspicion_to_nps_responses_table.php database/migrations/2026_07_16_100003_create_nps_survey_events_table.php` — cada migration tem guard de idempotência (≥1 ocorrência por arquivo)
    - Migration 100003 contém a sequência `->nullable()->constrained('users')->nullOnDelete()` no user_id (ordem exata: nullable antes de nullOnDelete)
    - Nenhuma migration contém a string `->change()` nem referencia a coluna `status` de nps_surveys
    - `app/Models/NpsSurveyEvent.php` define as 6 constantes TYPE_* com os valores exatos generated|opened|submitted|expired|sent_email|sent_digisac
    - NpsSurvey::$fillable contém 'open_count' e NpsResponse::$casts contém 'suspicion_reasons' => 'array'
  </acceptance_criteria>
  <done>Schema completo migrado (SQLite testes), models/factory prontos, defaults de retrocompatibilidade comprovados por teste.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config/nps.php + NpsSuspicionService com as 4 regras (TDD)</name>
  <files>config/nps.php, .env.example, app/Services/Nps/NpsSuspicionService.php, tests/Feature/Phase94/NpsSuspicionServiceTest.php</files>
  <read_first>
    - config/digisac.php (padrão de arquivo de config do projeto — env() com defaults, comentários pt-BR)
    - app/Services/Nps/NpsSnapshotService.php (padrão de service stateless do namespace App\Services\Nps a replicar)
    - .planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-RESEARCH.md §Code Examples (snippet completo de referência do service e do config)
  </read_first>
  <behavior>
    - Regra 1: evaluate(ip: '203.0.113.10', 3600s, false) com config internal_ips=['203.0.113.10'] → is_suspicious true, reasons contém "Resposta enviada a partir da rede interna da ECF.", severity 'media'
    - Regra 1 CIDR: ip '10.1.2.3' com internal_cidrs=['10.0.0.0/8'] → suspeita
    - Regra 2: evaluate(ip externo não-interno, 30s, false) com janela 60 → is_suspicious true, reasons contém "Resposta enviada em menos de 1 minuto após geração do link." (janela default 60 → texto em minutos), severity 'media'
    - Regra 2 janela custom: config fast_response_window_seconds=90 e duração 80s → motivo cita "90 segundos" (janela não-múltipla de 60 → texto em segundos)
    - Regra 3: IP interno E duração ≤ janela → reasons contém adicionalmente "Link gerado e respondido rapidamente a partir da rede interna." e severity 'alta'
    - Regra 4: evaluate(ip externo, 3600s, isAuthenticatedSession: true) → is_suspicious true, reasons contém "Resposta realizada em sessão autenticada de usuário interno.", severity 'media' — e NADA de bloqueio (service só retorna veredito)
    - Caso limpo: ip externo, duração longa, sem sessão → is_suspicious false, reasons [], severity 'nenhuma'
    - Config vazia (internal_ips e internal_cidrs []) → isInternalIp nunca dá match e não lança exceção
    - ip null (edge) → Regra 1 não avalia (não suspeita por IP), demais regras seguem normais
  </behavior>
  <action>
    Criar `config/nps.php` conforme o snippet do RESEARCH (decisão de discretion travada: arquivo NOVO, não chave em config existente — Assumption A5): chave raiz `anti_burlamento` com `internal_ips` (explode ',' de env('ECF_INTERNAL_IPS','') com trim + array_filter + array_values), `internal_cidrs` (idem de env('ECF_INTERNAL_CIDRS','')), `fast_response_window_seconds` ((int) env('NPS_SUSPICION_WINDOW_SECONDS', 60)). Comentários pt-BR explicando que UI de config é Fase 96.

    Acrescentar ao `.env.example` (bloco novo com comentário `# NPS Anti-Burlamento (Phase 94)`): `ECF_INTERNAL_IPS=`, `ECF_INTERNAL_CIDRS=`, `NPS_SUSPICION_WINDOW_SECONDS=60`.

    Criar `app/Services/Nps/NpsSuspicionService.php` seguindo o snippet do RESEARCH §Code Examples: classe stateless, namespace `App\Services\Nps`, método público único `evaluate(?string $ip, int $durationSeconds, bool $isAuthenticatedSession): array` retornando shape `['is_suspicious' => bool, 'reasons' => array<int,string>, 'severity' => string]`. Regras e textos EXATOS (travados pelo CONTEXT §NpsSuspicionService):
    - Regra 1 (IP interno): `$ip !== null` e `IpUtils::checkIp($ip, array_merge(internal_ips, internal_cidrs))` (import `Symfony\Component\HttpFoundation\IpUtils`; retornar false sem chamar checkIp se a lista mesclada estiver vazia). Motivo: "Resposta enviada a partir da rede interna da ECF."
    - Regra 2 (resposta rápida): `$durationSeconds <= fast_response_window_seconds`. Texto reflete a janela configurada (exigência do CONTEXT): se `janela % 60 === 0` → "Resposta enviada em menos de {N} minuto(s) após geração do link." (N = janela/60; singular "1 minuto", plural "X minutos"); senão → "Resposta enviada em menos de {S} segundos após geração do link." Com default 60s o texto é exatamente "Resposta enviada em menos de 1 minuto após geração do link."
    - Regra 3 (combinação): quando Regra 1 E Regra 2 disparam juntas, acrescentar "Link gerado e respondido rapidamente a partir da rede interna." e severity 'alta'
    - Regra 4 (sessão autenticada): `$isAuthenticatedSession === true` → "Resposta realizada em sessão autenticada de usuário interno." — APENAS marca; nenhum throw/abort (bloqueio é Fase 96)
    - severity: 'alta' se Regra 3; 'media' se qualquer outra regra disparou; 'nenhuma' se reasons vazio. is_suspicious = !empty(reasons).
    Docblock pt-BR referenciando Phase 94 AB-94-4 e citando que bloqueio é Fase 96. Config lida via `config('nps.anti_burlamento.*')` em helpers privados `isInternalIp(string $ip): bool` e `windowSeconds(): int` — nada de injeção.

    TDD: escrever `tests/Feature/Phase94/NpsSuspicionServiceTest.php` primeiro (RED) cobrindo os 9 comportamentos do behavior acima, usando `config(['nps.anti_burlamento.internal_ips' => [...]])` etc. para fixar a config por teste e `app(NpsSuspicionService::class)` para resolver o service. Depois implementar até GREEN. Commits RED/GREEN separados em pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsSuspicionServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=NpsSuspicionServiceTest` verde com ≥9 testes (4 regras + severidades + janela custom + config vazia + ip null + caso limpo)
    - `grep -c "IpUtils::checkIp" app/Services/Nps/NpsSuspicionService.php` ≥ 1 e `grep -c "ip2long" app/Services/Nps/NpsSuspicionService.php` = 0 (sem hand-roll de CIDR)
    - `grep -c "config('nps.anti_burlamento" app/Services/Nps/NpsSuspicionService.php` ≥ 2 (IPs e janela vêm de config, nunca hardcoded)
    - Os 4 textos de motivo aparecem literalmente no service: "rede interna da ECF", "após geração do link", "respondido rapidamente a partir da rede interna", "sessão autenticada de usuário interno"
    - `.env.example` contém as 3 chaves ECF_INTERNAL_IPS, ECF_INTERNAL_CIDRS, NPS_SUSPICION_WINDOW_SECONDS
    - O service não contém `abort(`, `throw` de bloqueio, nem referência a Auth:: (recebe bool por parâmetro — quem consulta a sessão é o controller no plano 94-02)
  </acceptance_criteria>
  <done>Service avalia as 4 regras com textos pt-BR travados, severidade alta/media/nenhuma e config 100% via .env.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retrocompatibilidade AB-94-5 — legado intacto + regressão Nps completa</name>
  <files>tests/Feature/Phase94/NpsAntiBurlamentoBackwardCompatTest.php</files>
  <read_first>
    - tests/Feature/Phase31NpsSubmitTest.php (baseline de regressão do submit legado — padrão de setup survey/response sem template)
    - tests/Feature/Phase94/NpsAntiBurlamentoBackwardCompatTest.php (estado atual criado na Task 1 — estender, não recriar)
    - app/Http/Controllers/NpsController.php linhas 519-545 apenas (fluxo respond atual — para entender o que o teste de tela exercita; NÃO modificar o controller neste plano)
  </read_first>
  <behavior>
    - Survey legada (template_id null, sem nenhum campo de rastro populado) responde GET /nps/{token} com 200 e componente Nps/Respond
    - Resposta legada criada sem campos novos: is_suspicious false, suspicion_reasons null, response_ip_address null — e as agregações existentes que leem NpsResponse não quebram (POST legado completo retorna Nps/ThankYou)
    - Suite NPS inteira permanece verde: nenhum dos 207 testes pré-existentes falha após as migrations deste plano
  </behavior>
  <action>
    Estender `tests/Feature/Phase94/NpsAntiBurlamentoBackwardCompatTest.php` com os cenários de comportamento legado (behavior acima): criar survey via `NpsSurvey::factory()` sem template e sem rastro, exercitar GET `route('nps.respond', $token)` e POST legado completo (score_estrategista/score_empresa obrigatórios — copiar shape do payload de Phase31NpsSubmitTest), assertar respostas 200/ThankYou e que os campos novos permanecem em default (null/false/0). Nenhum código de produção muda nesta task — se algum teste pré-existente da suite Nps quebrar, o defeito está nas migrations/models da Task 1 e deve ser corrigido lá (não no teste que quebrou).

    Rodar a regressão completa `php artisan test --filter=Nps` e confirmar ≥207 testes passando (baseline) + os novos deste plano. Registrar a contagem no commit final.
  </action>
  <verify>
    <automated>php artisan test --filter=Nps</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=NpsAntiBurlamentoBackwardCompatTest` verde incluindo os cenários de GET/POST legado
    - `php artisan test --filter=Nps` termina com 0 falhas e total de testes ≥ 207 (baseline preservado + novos)
    - Nenhum arquivo de `app/Http/Controllers/` nem `app/Console/Commands/` foi modificado neste plano (`git diff --name-only` do plano não contém esses paths)
  </acceptance_criteria>
  <done>AB-94-5 comprovado: legado 100% funcional, suite Nps sem regressão, fundação pronta para os planos 94-02/94-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` → config → service | Valores de ECF_INTERNAL_IPS/CIDRS entram no matching de IP (input de operador, não de usuário final nesta fase) |
| Banco de dados | Campos novos armazenam IP/user-agent (dados pessoais com finalidade legítima de auditoria) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | config/nps.php parsing de ECF_INTERNAL_* | mitigate | array_filter/trim remove entradas vazias antes de IpUtils::checkIp; lista vazia → short-circuit false (nunca exceção) |
| T-94-02 | Information Disclosure | Campos novos em nps_responses/nps_survey_events | mitigate | Nenhum campo novo entra em payload público neste plano (nenhum controller tocado); exposição admin-only é Fase 95 com blindagem própria |
| T-94-03 | Repudiation | nps_survey_events | mitigate | Tabela append-only com user_id/ip/created_at — é a própria implementação do controle ASVS V7 |
| T-94-SC | Tampering | Supply chain composer/npm | accept | Fase não instala NENHUM pacote novo (RESEARCH §Package Legitimacy Audit: não aplicável) |
</threat_model>

<verification>
- `php artisan test --filter=Phase94` — suite nova do plano verde
- `php artisan test --filter=Nps` — regressão: ≥207 testes, 0 falhas
- `grep -rn "status" database/migrations/2026_07_16_1000*.php` não mostra nenhum ALTER/change no enum `nps_surveys.status`
</verification>

<success_criteria>
- Schema de rastro + eventos migrado e idempotente nos 2 drivers (SQLite testes / MariaDB prod)
- NpsSuspicionService avalia as 4 regras com textos pt-BR exatos e severidade alta/media
- Config 100% via .env (`config/nps.php`), zero hardcode
- Suite Nps ≥207/207 verde (AB-94-5)
</success_criteria>

<output>
Criar `.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-01-SUMMARY.md` ao concluir (pt-BR).
</output>
