---
phase: 94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - app/Http/Controllers/NpsController.php
  - tests/Feature/Phase94/NpsOpenTrailTest.php
  - tests/Feature/Phase94/NpsResponseTrailAndSuspicionTest.php
  - tests/Feature/Phase94/NpsSurveyEventsTest.php
autonomous: true
requirements: [AB-94-1, AB-94-2, AB-94-3, AB-94-4]

must_haves:
  truths:
    - "Todo GET em /nps/{token} registra first_opened_at (só na primeira), last_opened_at (sempre), open_count incrementado, IP e user-agent — mesmo em survey completed/expired"
    - "Todo submit registra response_ip_address, response_user_agent e response_duration_seconds e persiste is_suspicious + suspicion_reasons calculados pelo NpsSuspicionService — nos DOIS paths (v15 e legacy)"
    - "Eventos opened/expired/submitted/generated(manual) são emitidos em nps_survey_events; o submitted reverte junto quando a transação de submit reverte (guard 23000)"
    - "Nada muda para o respondente: payload Inertia público idêntico, sem nenhum campo técnico ou de suspeita"
  artifacts:
    - path: "app/Http/Controllers/NpsController.php"
      provides: "respond()/submitResponse* instrumentados + helper compartilhado de rastro/suspeita + generate() emitindo evento"
      contains: "capturarRastroEAvaliarSuspeita"
    - path: "tests/Feature/Phase94/NpsOpenTrailTest.php"
      provides: "Cobertura AB-94-1 (rastro de abertura, reaberturas)"
      min_lines: 50
    - path: "tests/Feature/Phase94/NpsResponseTrailAndSuspicionTest.php"
      provides: "Cobertura AB-94-2 + AB-94-4 nos dois paths de submit"
      min_lines: 60
    - path: "tests/Feature/Phase94/NpsSurveyEventsTest.php"
      provides: "Cobertura de eventos opened/expired/submitted/generated-manual (AB-94-3 parcial)"
      min_lines: 50
  key_links:
    - from: "app/Http/Controllers/NpsController.php"
      to: "app/Services/Nps/NpsSuspicionService.php"
      via: "app() dentro do helper compartilhado"
      pattern: "NpsSuspicionService"
    - from: "app/Http/Controllers/NpsController.php"
      to: "nps_survey_events"
      via: "NpsSurveyEvent::create"
      pattern: "NpsSurveyEvent::create"
---

<objective>
Instrumentar o `NpsController` (único arquivo de produção deste plano): rastro de abertura no `respond()` (AB-94-1), rastro de resposta + veredito de suspeita nos dois paths de submit via helper compartilhado (AB-94-2 + AB-94-4), e emissão dos eventos `opened`/`expired`/`submitted`/`generated` (manual) na trilha `nps_survey_events` (AB-94-3 parcial — os emissores do disparo mensal são o plano 94-03).

Purpose: é aqui que os dados de auditoria passam a NASCER em produção — coleta silenciosa, UX pública intocada.
Output: `NpsController.php` instrumentado + 3 arquivos de teste novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-CONTEXT.md
@.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-RESEARCH.md
@.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-01-SUMMARY.md

<interfaces>
<!-- Contratos criados pelo plano 94-01 — usar diretamente. -->

De `app/Services/Nps/NpsSuspicionService.php` (plano 94-01):
```php
// evaluate(?string $ip, int $durationSeconds, bool $isAuthenticatedSession)
//   : array{is_suspicious: bool, reasons: array<int,string>, severity: string}
app(\App\Services\Nps\NpsSuspicionService::class)->evaluate(...);
```

De `app/Models/NpsSurveyEvent.php` (plano 94-01):
```php
NpsSurveyEvent::TYPE_GENERATED | TYPE_OPENED | TYPE_SUBMITTED | TYPE_EXPIRED | TYPE_SENT_EMAIL | TYPE_SENT_DIGISAC
// fillable: survey_id, event_type, ip_address, user_agent, user_id, metadata (cast array)
```

Pontos de inserção no `NpsController.php` (estado atual, verificado 2026-07-16):
- `public function respond(string $token)` — linha 519; NÃO recebe Request hoje (adicionar type-hint `Request $request`); `firstOrFail()` na linha 533; early-return `completed` linha 535-537; branch `isExpired()` linhas 539-542 (único lugar do codebase que persiste status='expired')
- `public function submitResponse(Request $request, string $token)` — linha 669; discrimina por template_id nas linhas 679-684
- `private function submitResponseV15(Request $request, NpsSurvey $survey)` — linha 696; `NpsResponse::create([...])` na linha 751 DENTRO de `DB::transaction` (748-821); guard `QueryException '23000'` linhas 822-831; `NpsSnapshotService` chamado na linha 813 (padrão a seguir: service dentro da transação, sem transação própria)
- `private function submitResponseLegacy(Request $request, NpsSurvey $survey)` — linha 848; `NpsResponse::create([...])` na linha 893 DENTRO de `DB::transaction` (892-927)
- `public function generate(Request $request, NpsTemplateService $templateService)` — linha 403; `NpsSurvey::create([...])` na linha 488
- Payload Inertia público atual do respond (linhas 632-644): chave `survey` com EXATAMENTE token, company_name, estrategista_name, analista_name, tem_analista, textos — mais `perguntas_extras` e `template`. Nenhuma chave nova pode entrar.
- `use Illuminate\Http\Request` já importado no arquivo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rastro de abertura no respond() + eventos opened/expired (AB-94-1)</name>
  <files>app/Http/Controllers/NpsController.php, tests/Feature/Phase94/NpsOpenTrailTest.php, tests/Feature/Phase94/NpsSurveyEventsTest.php</files>
  <read_first>
    - app/Http/Controllers/NpsController.php linhas 509-645 (método respond completo — estado atual a instrumentar)
    - .planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-RESEARCH.md §Ponto de inserção 1 (snippet de referência da captura)
    - tests/Feature/Phase31NpsSubmitTest.php (padrão de teste Feature do fluxo público NPS)
    - app/Models/NpsSurveyEvent.php (constantes TYPE_* criadas no 94-01)
  </read_first>
  <behavior>
    - GET /nps/{token} pela 1ª vez: first_opened_at e last_opened_at preenchidos, open_count 1, open_ip_address = IP do request, open_user_agent preenchido; evento 'opened' criado com metadata first_open=true
    - GET pela 2ª vez (com Carbon::setTestNow avançado): first_opened_at INALTERADO (nunca sobrescrever — decisão travada do CONTEXT), last_opened_at atualizado, open_count 2; 2º evento 'opened' com metadata first_open=false
    - GET em survey completed: rastro registrado normalmente (open_count incrementa) E a tela AlreadyCompleted continua sendo renderizada
    - GET em survey expirada (expires_at no passado): rastro registrado, status vira 'expired' (comportamento pré-existente) E evento 'expired' emitido na mesma passada — total: 1 evento opened + 1 evento expired
    - GET autenticado (actingAs): evento 'opened' carrega user_id do usuário logado; GET anônimo: user_id null
    - Payload Inertia da página Nps/Respond NÃO contém nenhuma chave nova (survey.keys exatamente: token, company_name, estrategista_name, analista_name, tem_analista, textos)
  </behavior>
  <action>
    Em `NpsController::respond()`: adicionar `Request $request` como primeiro parâmetro (Laravel resolve por type-hint; a rota `{token}` continua casando por nome). Logo APÓS o `firstOrFail()` (linha 533) e ANTES dos early-returns de completed/expired, inserir bloco comentado "Phase 94 AB-94-1 — rastro de abertura (roda em TODO GET, mesmo completed/expired)":
    1. `$survey->update([...])` com `first_opened_at => $survey->first_opened_at ?? now()`, `last_opened_at => now()`, `open_count => $survey->open_count + 1`, `open_ip_address => $request->ip()`, `open_user_agent => $request->userAgent()`
    2. `NpsSurveyEvent::create([...])` com event_type `NpsSurveyEvent::TYPE_OPENED`, ip_address `$request->ip()`, user_agent `$request->userAgent()`, user_id `auth()->id()` (nullable — registra sessão interna coexistente, insumo da Fase 95/96), metadata `['first_open' => $survey->open_count === 1]` (avaliado APÓS o update — open_count já incrementado)

    No branch `if ($survey->isExpired())` (linhas 539-542), junto do `$survey->update(['status' => 'expired'])`, emitir `NpsSurveyEvent::create` com TYPE_EXPIRED, ip/user_agent do request, user_id `auth()->id()`, metadata null — comentário citando que este é o ÚNICO ponto do codebase que transiciona para expired (expiração é lazy; confirmado no RESEARCH §Emissores). NÃO emitir expired no submitResponse (o 422 de expiração no POST não persiste status — fora do escopo, Open Question 2 do RESEARCH decidida como "não emitir").

    Nunca ler header X-Forwarded-For manualmente — sempre `$request->ip()` (T-94-01; trustProxies é assunto de infra fora desta fase).

    Adicionar `use App\Models\NpsSurveyEvent;` ao bloco de imports (ordem alfabética entre os Models).

    TDD: escrever `tests/Feature/Phase94/NpsOpenTrailTest.php` (cenários 1-3 e 6 do behavior; usar `$this->withServerVariables(['REMOTE_ADDR' => '203.0.113.55'])` para IP determinístico e `Carbon::setTestNow` para separar 1ª/2ª abertura) e os cenários opened/expired em `tests/Feature/Phase94/NpsSurveyEventsTest.php` — RED primeiro, GREEN depois, commits pt-BR separados.
  </action>
  <verify>
    <automated>php artisan test --filter="NpsOpenTrailTest|NpsSurveyEventsTest"</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=NpsOpenTrailTest` verde (≥4 testes: primeira abertura, reabertura preserva first_opened_at, abertura em completed, payload sem chaves novas)
    - Teste de reabertura prova assertEquals entre o first_opened_at da 1ª e da 2ª abertura E assertNotEquals no last_opened_at
    - Evento expired só existe após GET em survey vencida — count(TYPE_EXPIRED) = 1 e count(TYPE_OPENED) = 1 na mesma passada
    - Assinatura do método é exatamente `public function respond(Request $request, string $token)`
    - `grep -c "X-Forwarded" app/Http/Controllers/NpsController.php` = 0
    - Teste asserta que as chaves de `survey` no payload Inertia de Nps/Respond são exatamente ['token','company_name','estrategista_name','analista_name','tem_analista','textos']
  </acceptance_criteria>
  <done>Todo GET deixa rastro completo + eventos opened/expired, reaberturas preservam o primeiro registro, UX pública byte-idêntica.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rastro de resposta + suspeita nos dois paths de submit + evento submitted (AB-94-2/AB-94-4)</name>
  <files>app/Http/Controllers/NpsController.php, tests/Feature/Phase94/NpsResponseTrailAndSuspicionTest.php, tests/Feature/Phase94/NpsSurveyEventsTest.php</files>
  <read_first>
    - app/Http/Controllers/NpsController.php linhas 647-930 (submitResponse + submitResponseV15 + submitResponseLegacy completos)
    - .planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-RESEARCH.md §Ponto de inserção 2 e §Common Pitfalls 2-3 (helper compartilhado + evento dentro da transação)
    - app/Services/Nps/NpsSuspicionService.php (assinatura de evaluate criada no 94-01)
    - tests/Feature/Phase31NpsSubmitTest.php (shape do payload POST legado)
    - tests/Feature/Phase73/NpsV15E2ETest.php (shape do payload POST v15 com answers.<qid>)
  </read_first>
  <behavior>
    - POST v15 (survey com template): NpsResponse persistida com response_ip_address, response_user_agent, response_duration_seconds calculada e veredito do service — na MESMA linha do create (sem UPDATE extra)
    - POST legado (survey sem template): exatamente o mesmo tratamento — comprovando que o helper é compartilhado
    - Resposta rápida (survey criada agora, submit imediato → duração ~0 ≤ 60s): is_suspicious true, suspicion_reasons JSON objeto {"reasons": [...], "severity": "media"} com motivo da Regra 2
    - IP interno (withServerVariables REMOTE_ADDR + config internal_ips): motivo da Regra 1; IP interno + rápida: 3 motivos e severity "alta"
    - Sessão autenticada (actingAs + POST): motivo da Regra 4, resposta ACEITA normalmente (marca, não bloqueia — Fase 96 fará o bloqueio)
    - Resposta limpa (survey com created_at 10 min no passado, IP externo, anônimo): is_suspicious false, suspicion_reasons NULL (não objeto vazio)
    - response_duration_seconds é positivo e coerente: survey criada 10 min atrás → duração ~600 (tolerância ±5s)
    - Evento 'submitted' criado em POST bem-sucedido nos DOIS paths, com response_id no metadata
    - Dedup 23000 (2ª completação no mesmo mês/template): transação reverte E NENHUM evento 'submitted' órfão fica na tabela (Pitfall 3)
    - POST com falha de validação (422): nenhum evento submitted, nenhuma NpsResponse
  </behavior>
  <action>
    Criar no `NpsController` um helper privado `capturarRastroEAvaliarSuspeita(Request $request, NpsSurvey $survey): array` (docblock pt-BR "Phase 94 AB-94-2 + AB-94-4 — chamado por submitResponseV15() e submitResponseLegacy(); NÃO duplicar"):
    - Duração: `(int) $survey->created_at->diffInSeconds(now())` — ATENÇÃO Carbon 3 (Laravel 12): diffInSeconds é SIGNED; a ordem `created_at->diffInSeconds(now())` garante valor positivo (passado → agora). NÃO usar `now()->diffInSeconds($survey->created_at)`, que retornaria negativo. Decisão travada do CONTEXT: o "generated_at" é o `created_at` do survey (nenhuma coluna nova).
    - Veredito: `app(\App\Services\Nps\NpsSuspicionService::class)->evaluate(ip: $request->ip(), durationSeconds: $duracao, isAuthenticatedSession: auth()->check())`
    - Retorna array para merge no create: `response_ip_address => $request->ip()`, `response_user_agent => $request->userAgent()`, `response_duration_seconds => $duracao`, `is_suspicious => $veredito['is_suspicious']`, `suspicion_reasons =>` quando suspeita: `['reasons' => $veredito['reasons'], 'severity' => $veredito['severity']]` (shape objeto travado — decisão de discretion, pronto para a Fase 95 consumir); quando limpa: `null`.

    Criar segundo helper privado `registrarEventoSubmitted(Request $request, NpsSurvey $survey, NpsResponse $response): void` que faz `NpsSurveyEvent::create` com TYPE_SUBMITTED, ip/user_agent do request, user_id `auth()->id()`, metadata `['response_id' => $response->id]`.

    Em `submitResponseV15()`: no `NpsResponse::create([...])` da linha 751, fazer spread do helper: `...existentes, ...$this->capturarRastroEAvaliarSuspeita($request, $survey)` (o closure da transação precisa receber `$request` via `use`). Chamar `registrarEventoSubmitted()` DENTRO da `DB::transaction`, logo antes do `$survey->update(['status' => 'completed', ...])` (linha 817) — mesmo posicionamento do NpsSnapshotService e pelo mesmo motivo: se o guard 23000 estourar, o evento reverte junto (Pitfall 3 do RESEARCH).

    Em `submitResponseLegacy()`: mesmo spread no `NpsResponse::create` da linha 893 e mesma chamada `registrarEventoSubmitted()` dentro da transação (892-927), antes do update completed da linha 926.

    PROIBIDO duplicar o corpo da lógica de captura/avaliação nos dois métodos (Pitfall 2 — bug real de produção já aconteceu por divergência entre paths; comentário na linha 437 do controller).

    TDD: escrever `tests/Feature/Phase94/NpsResponseTrailAndSuspicionTest.php` (behaviors 1-7 e 10) e os cenários submitted/dedup-rollback em `NpsSurveyEventsTest.php` (behaviors 8-9). Para o dedup: montar 2 surveys do mesmo (company, month_reference, template) e completar a 2ª — assertar render AlreadyCompleted e zero evento submitted da 2ª tentativa. RED → GREEN, commits pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter="NpsResponseTrailAndSuspicionTest|NpsSurveyEventsTest"</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=NpsResponseTrailAndSuspicionTest` verde (≥7 testes cobrindo os 2 paths + 4 regras via HTTP + duração + limpa)
    - `grep -c "capturarRastroEAvaliarSuspeita" app/Http/Controllers/NpsController.php` = 3 (1 definição + 2 call-sites) — prova do helper compartilhado
    - `grep -c "registrarEventoSubmitted" app/Http/Controllers/NpsController.php` = 3 (1 definição + 2 call-sites dentro das transações)
    - Teste do dedup 23000 asserta `NpsSurveyEvent::where('event_type','submitted')->count()` inalterado após a 2ª tentativa
    - Resposta limpa persiste suspicion_reasons NULL (assertNull, não assertEquals([]))
    - suspicion_reasons suspeita decodifica como objeto com chaves 'reasons' (array de strings pt-BR) e 'severity' ('media'|'alta')
    - `grep -c "diffInSeconds" app/Http/Controllers/NpsController.php` = 1 e a expressão parte de `$survey->created_at->diffInSeconds(` (ordem Carbon 3 correta)
  </acceptance_criteria>
  <done>Ambos os paths de submit gravam rastro + veredito via helper único; evento submitted transacional; marca sem bloquear.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Evento generated no generate() manual + blindagem de payload + regressão Nps</name>
  <files>app/Http/Controllers/NpsController.php, tests/Feature/Phase94/NpsSurveyEventsTest.php</files>
  <read_first>
    - app/Http/Controllers/NpsController.php linhas 403-507 (método generate completo — NpsSurvey::create na linha 488)
    - tests/Feature/Phase94/NpsSurveyEventsTest.php (estado atual das Tasks 1-2 — estender)
  </read_first>
  <behavior>
    - POST autenticado na rota de geração manual de link: evento 'generated' criado com survey_id do survey novo, user_id do admin logado, ip do request e metadata origem=manual
    - Geração que falha antes do create (empresa sem contrato ativo no modelo) NÃO emite evento
    - Suite Nps completa permanece verde (≥207 + novos)
  </behavior>
  <action>
    Em `NpsController::generate()`: logo após o `NpsSurvey::create([...])` da linha 488 (antes do `return back()`), emitir `NpsSurveyEvent::create` com event_type `NpsSurveyEvent::TYPE_GENERATED`, survey_id `$survey->id`, ip_address `$request->ip()`, user_agent `$request->userAgent()`, user_id `$user->id` (variável já existente no método), metadata `['origem' => 'manual']`. Comentário pt-BR "Phase 94 AB-94-3 — trilha de auditoria: link manual gerado". O valor 'manual' em metadata.origem é o discriminador contra 'disparo_mensal' (plano 94-03) — manter esses dois literais exatos.

    Estender `NpsSurveyEventsTest.php` com os cenários generated-manual (behavior 1-2), usando a rota nomeada de geração existente com usuário admin autenticado (copiar setup de teste existente que já exercita generate, se houver na suite Nps; senão montar via factories Company + template).

    Fechar o plano com a regressão `php artisan test --filter=Nps` — 0 falhas obrigatório. Se qualquer teste pré-existente quebrar, corrigir a instrumentação (nunca o teste antigo).
  </action>
  <verify>
    <automated>php artisan test --filter=Nps</automated>
  </verify>
  <acceptance_criteria>
    - Teste generated-manual verde: evento com metadata `['origem' => 'manual']` e user_id preenchido
    - `php artisan test --filter=Nps` termina com 0 falhas e total ≥ 207 + testes novos da fase
    - `git diff --name-only` do plano contém apenas app/Http/Controllers/NpsController.php e arquivos em tests/Feature/Phase94/
  </acceptance_criteria>
  <done>Trilha do fluxo manual completa (generated → opened → submitted/expired) e suite Nps sem regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente anônimo → GET/POST /nps/{token} | Rota pública sem auth; todo input (IP, UA, payload) é não-confiável |
| Sessão admin coexistente → mesma rota pública | auth()->check() na aba pública é sinal de suspeita (Regra 4), nunca privilégio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04 | Spoofing/Tampering | Captura de IP em respond()/submit | mitigate | Usar exclusivamente `$request->ip()`; zero parse manual de X-Forwarded-For (grep gate = 0 no acceptance da Task 1); confiabilidade atrás de proxy verificada manualmente pós-deploy (plano 94-03 §verification) |
| T-94-05 | Information Disclosure | Payload Inertia público (Respond/ThankYou/AlreadyCompleted/Expired) | mitigate | Nenhuma chave nova nos props; teste automatizado asserta o conjunto exato de chaves de `survey`; `suspicion_reasons` jamais renderizado ao respondente (exposição admin-only é Fase 95 com blindagem própria AB-95-4) |
| T-94-06 | Information Disclosure | Mensagens de erro do submit (422) | mitigate | Nenhuma mudança nos textos de erro existentes; veredito de suspeita nunca aparece em resposta HTTP nesta fase |
| T-94-07 | Repudiation | Usuário interno nega ter respondido | mitigate | Eventos opened/submitted com user_id + ip + created_at imutáveis |
| T-94-08 | DoS | UPDATE + INSERT por GET público (bot abrindo link em loop) | accept | Volume esperado baixo (links com token UUID não-enumerável, expiram em 7-30 dias); tabela indexada; rate limiting seria Fase 96+ se necessário |
| T-94-09 | Tampering | Cliente manipula duração enviando submit "lento" artificialmente | accept | Regra 2 é heurística de detecção, não controle de segurança; evasão apenas reduz o sinal, não cria privilégio |
</threat_model>

<verification>
- `php artisan test --filter=Phase94` — suites novas verdes
- `php artisan test --filter=Nps` — regressão ≥207 + novos, 0 falhas
- Inspecionar diff: nenhum arquivo de `resources/js/` tocado (fase backend-only, sem `npm run build`)
</verification>

<success_criteria>
- Success Criteria 1 e 2 da Phase 94 (ROADMAP) comprovados por teste: abertura registra e preserva primeiro registro; resposta ganha rastro + veredito
- Success Criteria 3 parcial: motivos pt-BR persistidos para IP interno/resposta rápida/sessão autenticada, com severidade alta na combinação
- UX pública byte-idêntica (payload assertado por teste)
- Suite Nps sem regressão
</success_criteria>

<output>
Criar `.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-02-SUMMARY.md` ao concluir (pt-BR).
</output>
