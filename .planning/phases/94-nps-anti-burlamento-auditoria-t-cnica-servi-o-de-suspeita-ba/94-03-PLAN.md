---
phase: 94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba
plan: 03
type: execute
wave: 3
depends_on: ["94-01", "94-02"]
files_modified:
  - app/Console/Commands/NpsDispararMensal.php
  - tests/Feature/Phase94/NpsSurveyEventsTest.php
autonomous: true
requirements: [AB-94-3, AB-94-5]

must_haves:
  truths:
    - "O disparo mensal emite 1 evento generated por survey criada (por empresa × modelo), sent_email só no sucesso do Mail::send e sent_digisac só quando o envio Digisac retorna status enviado"
    - "nps_survey_events acumula a linha do tempo completa de um survey: generated → sent_email/sent_digisac → opened → submitted (Success Criteria 4 da fase)"
    - "Dry-run do comando não emite nenhum evento; falha de email não gera sent_email"
    - "Suite Nps completa (≥207 baseline + novos da fase) e suites V16 permanecem verdes — gate final da fase"
  artifacts:
    - path: "app/Console/Commands/NpsDispararMensal.php"
      provides: "Emissão de generated/sent_email/sent_digisac no disparo mensal"
      contains: "NpsSurveyEvent"
    - path: "tests/Feature/Phase94/NpsSurveyEventsTest.php"
      provides: "Cobertura dos emissores do disparo mensal + teste de linha do tempo completa"
      min_lines: 100
  key_links:
    - from: "app/Console/Commands/NpsDispararMensal.php"
      to: "nps_survey_events"
      via: "NpsSurveyEvent::create nos 3 pontos (create/email/digisac)"
      pattern: "NpsSurveyEvent::create"
---

<objective>
Completar a trilha de eventos (AB-94-3): instrumentar o `NpsDispararMensal` — único emissor automático de surveys — com os eventos `generated` (por survey criada no loop de modelos), `sent_email` (branch de sucesso do Mail) e `sent_digisac` (envio Digisac confirmado). Fechar a fase com o teste de linha do tempo completa (gerado → enviado → aberto → respondido) e o gate de regressão total.

Purpose: sem estes emissores, surveys automáticas teriam `sent_email` órfão de `generated` e a auditoria viva (Success Criteria 4) ficaria incompleta.
Output: `NpsDispararMensal.php` instrumentado + `NpsSurveyEventsTest.php` completo + gate de regressão da fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-CONTEXT.md
@.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-RESEARCH.md
@.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-02-SUMMARY.md

<interfaces>
<!-- Pontos exatos de inserção no NpsDispararMensal.php (estado atual, verificado 2026-07-16). -->

- `NpsSurvey::create([...])` na linha 250, DENTRO do `foreach` de modelos aplicáveis (1 survey por empresa × modelo — empresa com 2 modelos = 2 surveys = 2 eventos generated). O branch dry-run faz `continue` na linha 243, ANTES do create — logo dry-run naturalmente não emite eventos se a emissão ficar após o create.
- Branch de SUCESSO do email: linhas 308-324 — `Mail::to(...)->send(new NpsMonthlyMail(...))` seguido de `NpsEmailEnvio::create([... 'status' => 'enviado' ...])`. Emitir `sent_email` AQUI (dentro do try, após o Mail::send). O branch de falha (325-336) grava `NpsEmailEnvio status=falha` — NÃO emitir evento ali (não existe event_type de falha no enum travado pelo CONTEXT).
- Digisac: linhas 350-358 — `$envio = $this->digisacDispatch->send($survey, $empresa, $modelo, $varsDigisac)` seguido de `match ($envio->status)`. Emitir `sent_digisac` SOMENTE quando `$envio->status === 'enviado'` (não em 'falha'/'skipped').

De `app/Models/NpsSurveyEvent.php` (plano 94-01):
```php
NpsSurveyEvent::TYPE_GENERATED | TYPE_SENT_EMAIL | TYPE_SENT_DIGISAC
// fillable: survey_id, event_type, ip_address, user_agent, user_id, metadata (cast array)
// Contexto de console: ip_address/user_agent/user_id = null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NpsDispararMensal emite generated + sent_email + sent_digisac</name>
  <files>app/Console/Commands/NpsDispararMensal.php, tests/Feature/Phase94/NpsSurveyEventsTest.php</files>
  <read_first>
    - app/Console/Commands/NpsDispararMensal.php linhas 200-400 (loop de empresas × modelos, branches email/digisac — pontos exatos no bloco interfaces acima)
    - tests/Feature/Phase31NpsDispararMensalTest.php (padrão de teste do comando: Mail::fake, setup de empresa elegível)
    - tests/Feature/NpsDigisac/NpsDispararMensalDigisacTest.php (padrão de fake/assert do canal Digisac)
    - tests/Concerns/ContrataServicoNpsCoberto.php (concern de setup de empresa com serviço coberto por modelo — reusar)
    - tests/Feature/Phase94/NpsSurveyEventsTest.php (estado atual dos planos 94-01/94-02 — estender, não recriar)
  </read_first>
  <behavior>
    - Rodar nps:disparar-mensal para 1 empresa elegível com 1 modelo aplicável e canal email ativo: 1 evento 'generated' (metadata origem=disparo_mensal, user_id null, ip null) + 1 evento 'sent_email' com survey_id correto
    - Empresa com 2 modelos aplicáveis: 2 eventos 'generated' (um por survey criada)
    - Mail::fake com falha forçada (Mailable lançando): NpsEmailEnvio status=falha registrado (comportamento pré-existente) e ZERO evento 'sent_email'
    - Canal Digisac com envio confirmado (status 'enviado'): 1 evento 'sent_digisac' com metadata contendo nps_digisac_envio_id; envio 'skipped' (empresa sem grupo): ZERO evento 'sent_digisac'
    - Comando com --dry-run: ZERO eventos de qualquer tipo
    - Idempotência: 2ª rodada no mesmo mês pula as empresas (comportamento pré-existente) e não cria eventos duplicados
  </behavior>
  <action>
    Em `NpsDispararMensal::handle()`, três inserções (comentários pt-BR "Phase 94 AB-94-3"):

    1. Logo após o `NpsSurvey::create([...])` da linha 250 (e do `$criados++`): `NpsSurveyEvent::create` com event_type `NpsSurveyEvent::TYPE_GENERATED`, survey_id `$survey->id`, ip_address/user_agent/user_id null (contexto de console — sem request), metadata `['origem' => 'disparo_mensal']` (literal exato — o discriminador contra 'manual' do generate() instrumentado no 94-02).

    2. No branch de sucesso do email (dentro do try, após `NpsEmailEnvio::create` de status 'enviado', ~linha 322): `NpsSurveyEvent::create` com TYPE_SENT_EMAIL, survey_id `$survey->id`, metadata `['destinatario' => $empresa->email_cliente]`. NÃO tocar no branch catch de falha.

    3. No bloco Digisac, após o `$envio = $this->digisacDispatch->send(...)` (linha 352): emitir TYPE_SENT_DIGISAC somente se `$envio->status === 'enviado'` (mesmo literal usado no match existente da linha 353), metadata `['nps_digisac_envio_id' => $envio->id]`. Implementar sem quebrar o `match` existente de contadores (adicionar um `if` antes ou depois do match — não reescrever o match).

    Adicionar `use App\Models\NpsSurveyEvent;` aos imports. Nenhuma outra linha do comando muda — dry-run, idempotência, contadores e logs `[NPS Mensal]` permanecem intocados.

    TDD: estender `NpsSurveyEventsTest.php` com os 6 behaviors acima (RED), usando os padrões de setup dos testes existentes do comando (Mail::fake, concern ContrataServicoNpsCoberto, fake do Digisac conforme NpsDispararMensalDigisacTest). GREEN em seguida. Commits pt-BR.
  </action>
  <verify>
    <automated>php artisan test --filter=NpsSurveyEventsTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=NpsSurveyEventsTest` verde incluindo os ≥6 cenários novos do disparo mensal
    - `grep -c "NpsSurveyEvent::create" app/Console/Commands/NpsDispararMensal.php` = 3 (generated, sent_email, sent_digisac)
    - Evento generated do disparo mensal tem metadata origem='disparo_mensal' e user_id null (assertado em teste)
    - Teste de falha de email asserta `NpsSurveyEvent::where('event_type','sent_email')->count()` = 0
    - Teste de dry-run asserta `NpsSurveyEvent::count()` = 0 após o comando
    - Nenhuma linha existente de log/contador do comando alterada (diff só adiciona blocos, não modifica os existentes)
  </acceptance_criteria>
  <done>Disparo mensal 100% auditado: generated por survey, sent_email/sent_digisac só em sucesso confirmado, dry-run limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Linha do tempo completa + gate de regressão da fase</name>
  <files>tests/Feature/Phase94/NpsSurveyEventsTest.php</files>
  <read_first>
    - tests/Feature/Phase94/NpsSurveyEventsTest.php (todos os cenários já cobertos — o teste de timeline integra o que existe)
    - tests/Feature/Phase73/NpsV15E2ETest.php (padrão de teste E2E do fluxo v15: disparo → abertura → submit)
    - .planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-VALIDATION.md (§Sampling Rate e §Manual-Only Verifications)
  </read_first>
  <behavior>
    - Fluxo E2E automático: rodar nps:disparar-mensal (empresa elegível, canal email) → GET no link do survey criado → POST v15 completo. Ao final, `$survey->events` ordenados por id são exatamente: generated, sent_email, opened, submitted (Success Criteria 4 da fase)
    - Fluxo E2E manual: generate() via HTTP admin → GET → GET de novo → POST. Timeline: generated, opened, opened, submitted — com o generated carregando user_id do admin e origem=manual
    - Gate: suite Nps completa 0 falhas (baseline ≥207 + todos os novos da fase) e suite V16 0 falhas
  </behavior>
  <action>
    Adicionar a `NpsSurveyEventsTest.php` dois testes de linha do tempo (behaviors 1-2), assertando a SEQUÊNCIA exata de event_types via `$survey->events()->orderBy('id')->pluck('event_type')->all()` (usar a relação `events()` criada no 94-01). No fluxo automático, extrair o token do survey criado pelo comando (query por company_id + month_reference) e seguir com GET/POST HTTP reais (sem mock do controller).

    Rodar o gate final da fase, nesta ordem:
    1. `php artisan test --filter=Phase94` (suite completa da fase)
    2. `php artisan test --filter=Nps` (baseline ≥207 + novos, 0 falhas)
    3. `php artisan test --filter=V16` (suites v16.0 — bônus/snapshot intocados, 0 falhas)
    Registrar as 3 contagens no SUMMARY. Qualquer falha pré-existente e não-relacionada (padrão do projeto: documentar em deferred-items) deve ser listada no SUMMARY com evidência de que já falhava antes da fase (git stash / checkout do teste).
  </action>
  <verify>
    <automated>php artisan test --filter=Nps</automated>
  </verify>
  <acceptance_criteria>
    - Teste de timeline automática asserta a sequência exata ['generated','sent_email','opened','submitted']
    - Teste de timeline manual asserta ['generated','opened','opened','submitted'] com generated.user_id não-nulo
    - `php artisan test --filter=Phase94` 0 falhas; `php artisan test --filter=Nps` 0 falhas com total ≥ 207 + novos; `php artisan test --filter=V16` 0 falhas
    - SUMMARY registra as 3 contagens de teste e a pendência de verificação manual do proxy (abaixo)
  </acceptance_criteria>
  <done>Success Criteria 4 comprovado por E2E; fase inteira sem regressão em Nps e V16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduler/console → banco | NpsDispararMensal roda sem request HTTP; eventos de console têm ip/user_id null por design |
| Infra VPS (proxy?) → $request->ip() | Topologia não confirmada — trustProxies ausente em bootstrap/app.php |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-10 | Spoofing | Confiabilidade do IP capturado em produção (Pitfall 1 do RESEARCH) | mitigate | Verificação manual pós-deploy documentada abaixo; até lá a Regra 1 pode gerar falso negativo/positivo — aceitável porque a fase é backend-only e nada é exibido nem bloqueado (Fases 95/96 dependem desta confirmação) |
| T-94-11 | Repudiation | Envio email/Digisac negado posteriormente | mitigate | Eventos sent_email/sent_digisac com survey_id + timestamps, correlacionados a NpsEmailEnvio/NpsDigisacEnvio via metadata |
| T-94-12 | Information Disclosure | metadata de eventos com email do cliente (destinatario) | accept | Tabela interna sem exposição em UI nesta fase; Fase 95 exibirá somente para role admin (AB-95-4 blindará payload) |
</threat_model>

<verification>
- `php artisan test --filter=Phase94` / `--filter=Nps` / `--filter=V16` — todos 0 falhas (phase gate do 94-VALIDATION.md)
- **Verificação manual pós-deploy (fora do gate desta fase — registrar como pendência no SUMMARY e no STATE):** confirmar a topologia de rede do VPS (`https://admin.ecfconsultoria.com.br`) — abrir um link NPS de FORA da rede ECF e conferir no banco se `open_ip_address` bate com o IP público real do dispositivo (não `127.0.0.1`/IP de proxy). Se não bater, criar follow-up para configurar `$middleware->trustProxies(...)` em `bootstrap/app.php` ANTES de confiar na Regra 1 em produção (Pitfall 1 / Open Question 1 do RESEARCH). Deploy desta fase requer autorização explícita do usuário (convenção do projeto).
</verification>

<success_criteria>
- Success Criteria 4 da Phase 94: `nps_survey_events` acumula a linha do tempo completa gerado → enviado → aberto → respondido (comprovado por E2E)
- Disparo mensal audita os 3 tipos de evento sem alterar nenhum comportamento existente (dry-run, idempotência, contadores, logs)
- Gate da fase: Nps ≥207 baseline + novos e V16 verdes (AB-94-5)
</success_criteria>

<output>
Criar `.planning/phases/94-nps-anti-burlamento-auditoria-t-cnica-servi-o-de-suspeita-ba/94-03-SUMMARY.md` ao concluir (pt-BR), incluindo as contagens de teste e a pendência de verificação manual do proxy.
</output>
