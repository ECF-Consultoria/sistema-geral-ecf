---
phase: 85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-
plan: 05
type: execute
wave: 3
depends_on: [85-01, 85-03, 85-04]
files_modified: []
autonomous: false
requirements: [COL-85-1, COL-85-2, COL-85-3, COL-85-4]

must_haves:
  truths:
    - "Os 4 gates da fase passam: npm run build, npm run test:js, --filter=Phase85, --filter=Phase82"
    - "Phase75 continua no baseline conhecido (3 falhas pré-existentes de SSL, não regressão)"
    - "O publicador confirma em produção que o Premium publica com foto e que a planilha avisa antes"
  artifacts:
    - path: ".planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-05-SUMMARY.md"
      provides: "fechamento da fase + roteiro do checkpoint"
  key_links:
    - from: "gates da fase"
      to: "COL-85-1..4"
      via: "suíte JS de comportamento + gates estruturais + Feature PHP"
      pattern: "test:js"
---

<objective>
Fecha a fase: varredura de restos, os 4 gates obrigatórios rodados juntos (não plano a plano) e o
checkpoint humano em produção.

Purpose: cada plano rodou os gates no próprio escopo. A regressão que interessa é a **combinação** —
o filtro novo do backend (85-01) alimentando `aba.obrigatorios`, que alimenta a régua nova (85-02),
que acende o realce da grade (85-04) e o painel (85-03). Nenhum plano sozinho exercita essa cadeia.

Output: fase verde, checkpoint executado, SUMMARY com o alcance da mudança registrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-01-SUMMARY.md
@.planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-02-SUMMARY.md
@.planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-03-SUMMARY.md
@.planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: varredura de restos + os 4 gates juntos</name>
  <files>(nenhum — leitura e execução)</files>
  <action>
**Varredura** (reportar, não consertar fora do escopo):

1. `grep -rn "pictures: \[\]" resources/js/Pages/Mlb/` → esperado: **nada**. Se aparecer em
   `AnunciarML.jsx:1568`, é o **ternário do wizard** (`imagemUrl ? [...] : []`) — correto, deixar.
2. `grep -rn "ATTR_MARCA\|ATTR_MODELO" resources/js/ tests/js/` → esperado: **nada** (o laço do
   85-02 os tornou código morto).
3. `grep -rn "allow_variations" app/Http/Controllers/MlbAnuncioController.php` → só o método
   `atributos()`/comentários do wizard, **nunca** o `filter` de `colunasCategoria`.
4. Confirme que **nenhum pacote novo** entrou: `git diff --stat package.json package-lock.json
   composer.json composer.lock` → vazio. A fase inteira é código próprio; se houver diff, pare e
   reporte (a decisão travada não previa dependência nova, e o gate de legitimidade de pacote não
   foi executado nesta fase justamente porque não há instalação).

**Os 4 gates, na sequência, comparados com o baseline de 2026-07-15 (pré-fase):**

| Gate | Baseline | Esperado agora |
|------|----------|----------------|
| `npm run test:js` | 57/57 | **> 57**, zero falha (a fase só soma testes) |
| `npm run build` | verde | verde |
| `C:/php/php.exe artisan test --filter=Phase82` | 9 passed / 24 assertions | 9 passed (ou expectativa atualizada com justificativa registrada no 85-01) |
| `C:/php/php.exe artisan test --filter=Phase85` | (não existia) | verde |
| `C:/php/php.exe artisan test --filter=Phase75` | **3 failed / 40 passed** | **delta zero** — as 3 falhas são de ambiente (`cURL error 60` / SSL contra `api.mercadolibre.com`), **não** regressão |

Registre os números reais no SUMMARY. Qualquer delta em Phase75 **além** das 3 falhas de SSL é
regressão do 85-01 e bloqueia a fase.
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin &amp;&amp; npm run test:js 2>&amp;1 | tail -6 &amp;&amp; npm run build 2>&amp;1 | tail -3 &amp;&amp; C:/php/php.exe artisan test --filter=Phase85 2>&amp;1 | tail -3 &amp;&amp; C:/php/php.exe artisan test --filter=Phase82 2>&amp;1 | tail -3 &amp;&amp; C:/php/php.exe artisan test --filter=Phase75 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Varredura limpa; 4 gates verdes; Phase75 sem delta vs. 3 failed / 40 passed; números no SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: SUMMARY com o alcance da mudança em destaque</name>
  <files>.planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-05-SUMMARY.md</files>
  <action>
Escreva o SUMMARY da fase em pt-BR, seguindo o formato do `83-SUMMARY.md` (leia-o: tabela de
requisitos, decisões respeitadas, gates, checkpoint, fora do escopo).

**Seção obrigatória e em destaque — "Linhas que eram verdes vão ficar vermelhas":**

Depois desta fase, `errosLocaisLinha` cobre **todos** os obrigatórios da categoria (não só
BRAND/MODEL) e exige foto no Premium. Consequência direta e **pretendida**: planilhas existentes
vão acender vermelho em linhas que ontem pareciam OK, e a `PublishBar` vai mostrar **menos**
publicáveis do que antes.

Diga com todas as letras: **isto não é bug, é o 400 aparecendo mais cedo.** Aquelas linhas nunca
foram publicáveis — o ML as rejeitava com `missing_required` **depois** de gastar a chamada. A fase
inteira existe para mover a reprovação para antes da chamada. Sem este parágrafo, a primeira reação
ao ver a planilha vermelha vai ser abrir bug de regressão contra a Fase 85.

Registre também:
- Que **o risco crítico previsto não existia**: `colunasCategoria` (grade) e `atributos()` (wizard)
  já eram métodos separados desde a Fase 82 — o wizard nunca consumiu o endpoint da grade
  (`grep`: 1 consumidor). Nada de `?contexto=massa`; a parametrização teria sido complexidade sem
  causa. O que se compartilha é `MlCatalogoMetaService::atributos` (leitura crua), intocado.
- Que **o backend não precisou mudar para a foto**: `ItemBuilderBase::montarComum` já repassava
  `pictures` do payload. Faltava só a grade preencher — `pictures: []` era hardcode do frontend.
- A decisão discricionária **1 principal + 5 adicionais**, com a justificativa (canvas visível vs.
  `.xlsm` de 477 colunas; limite de 10 do ML; grupos colapsáveis são Phase 86) e o custo de mudar
  de ideia (acrescentar ids em `CAMPOS_FOTO`).
- Que a foto **não vem do cliente**: `montarProdutosDoCliente` não devolve campo de imagem, então
  "puxar produtos" nunca preenche foto — é sempre digitada/colada pelo publicador.
- Que **variações de verdade continuam fora** (linha-pai/linha-filha, `variations[]`): esta fase
  resolve anúncio **simples** com os obrigatórios preenchidos, que é o que o erro real pedia.
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin &amp;&amp; test -f ".planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-05-SUMMARY.md" &amp;&amp; grep -c "vermelh" ".planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-05-SUMMARY.md"</automated>
  </verify>
  <done>SUMMARY escrito, com a seção do alcance da mudança em destaque.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
A planilha em massa (`/mlb/anuncios` → aba "Em massa") ganhou:
  - **6 colunas de foto** (1 principal + 5 adicionais), grupo "Fotos", entre os campos base e a
    ficha técnica — URL, sem upload;
  - **as colunas de atributo que sumiam** (Cor, Tamanho): `colunasCategoria` parou de escondê-las
    quando são `required` **e** `allow_variations`;
  - **validação local prévia**: a linha reprova **antes** de chamar a API quando falta qualquer
    obrigatório da categoria ou quando é Premium sem foto;
  - **painel de pendências** abaixo da grade, dizendo por linha o que falta.
  </what-built>
  <how-to-verify>
**Este checkpoint NÃO é verificável em localhost** — 0 empresas com `ml_token` no banco local e sem
OAuth ML (mesma limitação das Fases 82/83). O roteiro roda **em produção**, depois do deploy, em
`https://admin.ecfconsultoria.com.br/mlb/anuncios` → aba "Em massa".

Use a **mesma categoria do erro real** (`MLB108791`, a que exige `[COLOR, SIZE]`).

1. **COL-85-2 — as colunas apareceram:** abra a aba da categoria. A ficha técnica agora mostra
   **Cor** e **Tamanho** como colunas. *(Antes da fase: não existiam — era a causa raiz do
   `missing_required`.)*
2. **COL-85-1 — as colunas de foto existem:** entre os campos base e a ficha técnica há o grupo
   **Fotos**, com "Foto principal *" + "Foto 2".."Foto 6".
3. **COL-85-1 — colar do Excel funciona:** copie 6 URLs numa linha do Excel/Sheets e cole na
   "Foto principal". As 6 colunas preenchem. Teste também o **fill handle** (arrastar a alça de uma
   URL para baixo) e **Delete** numa célula de foto.
4. **COL-85-3 — avisa ANTES:** monte uma linha **Premium** completa (título, preço, estoque, marca,
   Cor, Tamanho) mas **sem foto**. A linha fica **vermelha** com o glifo `!`, a `PublishBar` **não**
   a conta como publicável, e o painel abaixo da grade diz `Linha N · … — falta: foto`. **O botão
   de publicar não a envia** — nenhuma chamada gasta na API.
5. **COL-85-3 — o nome do campo é útil:** apague a Cor de uma linha. O painel diz `falta: Cor` (o
   nome em pt-BR que o ML devolve), não `COLOR`.
6. **COL-85-1 — o teste que motivou a fase:** preencha a foto principal com uma URL pública válida,
   complete Cor e Tamanho, e **publique** um **Premium**. **Deve publicar** — a linha fica verde
   com `✓`. *(Antes da fase: 400 garantido, 100% das vezes.)*
7. **COL-85-4 — aviso não bloqueia:** rode "Validar tudo". Se o ML devolver
   `shipping.lost_me1_by_user` ou `mandatory_free_shipping`, a linha fica **âmbar** com `◐` e
   **continua publicável** (a `PublishBar` a conta). Aviso ≠ erro.
8. **Zero regressão (Fases 82/83/84):** confira rapidamente — abas por categoria, seleção de range,
   fill, teclado (Tab/Enter/setas), dropdown do Tipo e do Gênero, badge de origem, autosave,
   **Ctrl+Z / Ctrl+Y**, polling do lote, painel de erro completo do ML com "Copiar erro".
9. **Alcance esperado (NÃO é bug):** planilhas antigas vão mostrar **mais linhas vermelhas** e
   **menos publicáveis** que antes. É o efeito pretendido: são as linhas que o ML rejeitaria com
   `missing_required`. Confirme que as linhas vermelhas realmente têm campo obrigatório vazio.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que divergiu (com a linha e o campo).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma nova nesta fase: o plano não escreve código de produção (varredura, gates e SUMMARY).
As fronteiras da fase estão registradas em `85-01` (rota `massa.colunas`), `85-02` (régua local),
`85-03` (payload → ML) e `85-04` (célula de foto no canvas).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-13 | Tampering | fase inteira | mitigate | Task 1 confirma `git diff --stat` vazio em `package*.json` / `composer*` — **nenhuma dependência entrou**; se houver diff, o plano para |
| T-85-14 | Denial of Service | API do ML | mitigate | É o objetivo da fase: a régua local (COL-85-3) corta a chamada que voltaria 400, reduzindo tráfego rejeitado contra o rate limit da conta do cliente |
| T-85-SC | Tampering | npm/composer | n/a | **Nenhum pacote instalado na fase** — auditoria de legitimidade não se aplica (verificado na Task 1) |
</threat_model>

<verification>
- `npm run test:js` > 57, zero falha
- `npm run build` verde
- `--filter=Phase85` verde; `--filter=Phase82` 9/9
- `--filter=Phase75` em 3 failed / 40 passed (delta zero vs. baseline)
- Checkpoint humano aprovado **em produção**
</verification>

<success_criteria>
- COL-85-1..4 confirmados pelo publicador na tela real
- O Premium com foto publica (o erro que motivou a fase não se reproduz)
- Zero regressão do que as Fases 82/83/84 entregaram
</success_criteria>

<output>
Create `.planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-05-SUMMARY.md` when done
</output>
</content>
</invoke>
