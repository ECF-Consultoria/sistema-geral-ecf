---
phase: 85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-
plan: 04
type: execute
wave: 2
depends_on: [85-02]
files_modified:
  - resources/js/Pages/Mlb/GradeAnuncioGlide.jsx
  - tests/js/estrutura-grade-glide.test.js
autonomous: true
requirements: [COL-85-1, COL-85-4]

must_haves:
  truths:
    - "A planilha tem colunas de foto: 1 URL principal + 5 adicionais, no grupo 'Fotos'"
    - "Colar 6 URLs do Excel de uma vez preenche as 6 colunas"
    - "Linha Premium sem foto acende vermelha e ganha o glifo '!' — sem chamar a API"
    - "Aviso do ML continua âmbar e continua não bloqueando (COL-85-4)"
  artifacts:
    - path: "resources/js/Pages/Mlb/GradeAnuncioGlide.jsx"
      provides: "COLS_FOTO no grupo Fotos, alimentadas por CAMPOS_FOTO"
      contains: "CAMPOS_FOTO"
  key_links:
    - from: "GradeAnuncioGlide.jsx"
      to: "gradeMassaUtils.CAMPOS_FOTO"
      via: "import — fonte única da lista de campos de foto"
      pattern: "CAMPOS_FOTO"
    - from: "colunas useMemo"
      to: "COLS_FOTO"
      via: "[status, ...base, ...fotos, ...dinamicas]"
      pattern: "fotos"
---

<objective>
As colunas de foto aparecem na planilha. É a metade visível de COL-85-1: o 85-03 faz a foto chegar
ao ML, este plano faz o publicador conseguir digitá-la (e colá-la do Excel).

Purpose: a coluna "Foto" foi **removida** na Fase 82 com a justificativa registrada em comentário
(`GradeAnuncioGlide.jsx:175`): *"'Foto' saiu — era placeholder decorativo ('+' que não fazia nada;
o payload sempre mandou pictures: []). Não é capacidade perdida."* Estava certo na época — era um
botão falso. Agora o payload leva foto de verdade (85-03), então a coluna volta **fazendo alguma
coisa**.

Output: 6 colunas no grupo "Fotos", entre os campos base e a ficha técnica.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-02-PLAN.md
@resources/js/Pages/Mlb/gradeMassaUtils.js
@resources/js/Pages/Mlb/GradeAnuncioGlide.jsx
@tests/js/estrutura-grade-glide.test.js
@CLAUDE.md
</context>

<contratos_do_85_02>
Consumir de `./gradeMassaUtils` (não redefinir):

```js
export const CAMPOS_FOTO;  // ['imagemUrl', 'imagemUrl2', ..., 'imagemUrl6'] — [0] é a principal
```

**Por que campo plano importa aqui:** `onEditarCelula(uid, campo, valor)` escreve
`{ ...l, [campo]: valor }`. Como os ids das colunas de foto **são** os nomes dos campos, a escrita
funciona pelo caminho genérico que já existe — e com ela, de graça: paste, fill handle, Delete e o
undo/redo da Fase 84. **Nenhum ramo novo em `onCellsEdited`.**
</contratos_do_85_02>

<tasks>

<task type="auto">
  <name>Task 1: COLS_FOTO no grupo "Fotos"</name>
  <files>resources/js/Pages/Mlb/GradeAnuncioGlide.jsx</files>
  <action>
Em `resources/js/Pages/Mlb/GradeAnuncioGlide.jsx`:

1. Importe `CAMPOS_FOTO` de `./gradeMassaUtils` (some à lista de imports já existente do módulo).
2. Logo após `COLS_BASE`, defina `COLS_FOTO` derivando de `CAMPOS_FOTO` — **não** escreva os 6 ids
   à mão (a lista tem uma fonte só; duplicá-la aqui faz as duas divergirem no dia em que virar 1+9):

   ```
   const COLS_FOTO = CAMPOS_FOTO.map((id, i) => ({
       id,
       title: i === 0 ? 'Foto principal *' : `Foto ${i + 1}`,
       width: 190,
   }));
   ```
   O `*` só na principal: é a única cobrada pela régua (e só no Premium).
3. No `useMemo` das `colunas`, monte as colunas de foto com `group: 'Fotos'` e insira-as
   **entre `base` e `dinamicas`**: `return [status, ...base, ...fotos, ...dinamicas];`
4. **Não** acrescente as colunas de foto a `COLS_COM_ORIGEM.** Elas caem no ramo final do
   `getCellContent` (`GridCellKind.Text`, `allowOverlay: true`), que é o que se quer: texto
   editável, `Delete` nativo, paste e fill sem nenhum código novo.
5. Atualize o comentário do bloco `COLS_BASE` (~linha 170-176): a justificativa *"'Foto' saiu — era
   placeholder decorativo"* ficou **obsoleta** e vai confundir quem ler depois. Substitua por: a
   Foto voltou na Fase 85 como **coluna de URL** de verdade (`pictures: [{source}]`, o mesmo shape
   que o wizard publica em produção desde sempre — `AnunciarML.jsx:1568`); o `+` decorativo é que
   não voltou.

Comentários pt-BR registrando, no `useMemo` das colunas:
  - **Por que 'Fotos' é um grupo próprio** e não parte de "Campos base": são 6 colunas de um
    assunto só, e o grupo é a costura para a Phase 86 (grupos colapsáveis + cor por grupo) — lá o
    collapse vira propriedade do grupo, sem tocar nestas colunas.
  - **Por que as fotos entram ANTES da ficha técnica:** posição fixa em toda categoria. Se fossem
    depois, a coluna de foto mudaria de lugar a cada aba (a ficha técnica varia por categoria) e o
    paste de bloco do Excel mudaria de alvo entre abas.
  - **Aviso de deslocamento (regressão real e assumida):** a ficha técnica anda **6 colunas para a
    direita**. `COLS_BASE` tem o comentário *"Esta ordem tambem e a ordem de mapeamento do paste —
    nao reordenar sem conferir"*: quem tinha um bloco do Excel mirando a ficha técnica precisa
    reposicionar. Inevitável — a fase existe para acrescentar coluna — e nenhum campo base mudou de
    posição, então o paste dos 10 primeiros campos continua caindo no mesmo lugar.
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin &amp;&amp; npm run build 2>&amp;1 | tail -4</automated>
  </verify>
  <done>6 colunas de foto no grupo "Fotos" entre base e ficha técnica, derivadas de `CAMPOS_FOTO`; comentário obsoleto do `COLS_BASE` corrigido; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: gates estruturais — colunas, fonte única e a distinção erro × aviso preservada</name>
  <files>tests/js/estrutura-grade-glide.test.js</files>
  <action>
Acrescente ao arquivo existente (que já tem os gates das Fases 82/83), via
`lerSemComentarios('resources/js/Pages/Mlb/GradeAnuncioGlide.jsx')`:

**COL-85-1 — as colunas existem e vêm da fonte única:**
1. A fonte sem comentários contém `CAMPOS_FOTO` e importa de `./gradeMassaUtils` — prova que a
   grade não reimplementou a lista.
2. A fonte **não** contém `'imagemUrl2'` literal (nenhum id de foto escrito à mão: se aparecer,
   alguém duplicou a lista e ela vai divergir do `CAMPOS_FOTO` no dia em que a contagem mudar).
3. A ordem `[status, ...base, ...fotos, ...dinamicas]` está no `useMemo` das colunas.
4. `COLS_COM_ORIGEM` **não** contém `imagemUrl` (o produto do cliente não tem foto — origem ali
   seria ruído; e mudaria o tipo da célula sem motivo).

**COL-85-4 — zero regressão da distinção erro-local × aviso-do-ML** (a fase mexe na régua, então
este é o gate que impede o dano colateral):
5. O ramo âmbar do `getRowThemeOverride` continua lá (`rgba(251,191,36`) — aviso do ML **não** virou
   erro.
6. O glifo `◐` (aviso, orientativo) e o glifo `!` (erro local, bloqueante) continuam sendo símbolos
   **diferentes** na cascata de status.
7. A precedência da cascata continua: `statusServidor === 'publicado'` e `=== 'erro'` **antes** do
   ramo de erro local — estado terminal é a verdade final sobre a linha; um erro local que a régua
   nova passou a detectar **não pode** repintar uma linha que já publicou com sucesso. *(Este é o
   risco concreto que COL-85-3 introduz: uma linha publicada semanas atrás, de categoria que agora
   exige COLOR, tem erro local hoje. Se a cascata invertesse, ela voltaria a ficar vermelha.)*

Cada `assert` com mensagem pt-BR dizendo **por que** o gate existe.
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin &amp;&amp; npm run test:js 2>&amp;1 | tail -8</automated>
  </verify>
  <done>7 gates novos verdes; nenhum gate anterior das Fases 82/83 quebrado.</done>
</task>

<task type="auto">
  <name>Task 3: varredura de regressão da grade</name>
  <files>resources/js/Pages/Mlb/GradeAnuncioGlide.jsx</files>
  <action>
Confira na fonte (leitura, não refatoração) que a grade continua inteira. Pontos que este plano
poderia ter quebrado:

1. **`getCellContent`** — as colunas de foto caem no ramo final (`GridCellKind.Text`). Confirme que
   elas **não** entram por engano no ramo `ehLista` (é `coluna._attr`, e foto não tem `_attr`) nem
   no `coluna._st`.
2. **`onCellsEdited`** — **nenhum ramo novo**. A foto passa pelo caminho genérico do fim
   (`onEditarCelula(linha.uid, coluna.id, valorNormalizado, ...)`). Confirme que o `if` do
   `coluna.id === 'price'` (`normalizarPreco`) e o do `alturaCm` (`parseDimensoes`) não pegam
   coluna de foto — os ids são diferentes, mas confirme.
3. **`coercePasteValue`** — foto é `GridCellKind.Text` → cai no ramo `{ ...celula, data: texto }`
   (trim). É o comportamento certo: URL colada do Excel entra crua, sem coerção de domínio.
4. **Índices por posição** — busque no arquivo qualquer índice de coluna **hardcoded** (algo como
   `colunas[3]` ou `col === 1`). Se existir, as 6 colunas novas o quebram. O código lido no
   planejamento usa `colunas[col]` (lookup dinâmico), mas confirme antes de fechar.
5. **Toolbar de lote** (`gerarEansEmLote`, `removerEmLote`) e `uidsSelecionados` operam por `uid`,
   não por índice de coluna — inalterados.

Registre no SUMMARY. Não conserte nada fora do escopo — reporte.
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin &amp;&amp; npm run test:js 2>&amp;1 | tail -8 &amp;&amp; npm run build 2>&amp;1 | tail -4</automated>
  </verify>
  <done>Os 5 pontos conferidos; achados no SUMMARY; suíte JS e build verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Publicador → célula de foto (canvas) | URL digitada ou colada do Excel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-10 | Injection / XSS | células de foto | accept | glide-data-grid desenha string em `<canvas>` via `fillText` — não há parse de HTML e não há `<img src>` nesta fase. Sem sink |
| T-85-11 | Denial of Service | 6 colunas novas × N linhas | mitigate | Células `GridCellKind.Text` (o ramo mais barato do `getCellContent`, sem custom renderer). `COLS_FOTO` é constante de módulo, criada uma vez — não recriada por render, mantendo a referência estável de que o canvas depende para não redesenhar |
| T-85-12 | Tampering | ordem das colunas | mitigate | Gate 3 da Task 2 trava a ordem `[status, base, fotos, dinamicas]`: reordenar em silêncio mudaria o alvo do paste de bloco (SHEET2-03) |
| T-85-SC | Tampering | npm | n/a | **Nenhum pacote instalado** — zero superfície de supply chain |
</threat_model>

<verification>
- `npm run test:js` verde, incluindo todos os gates das Fases 82/83
- `npm run build` verde
- Nenhum arquivo fora de `GradeAnuncioGlide.jsx` + `tests/js/estrutura-grade-glide.test.js` tocado
</verification>

<success_criteria>
- COL-85-1: 6 colunas de foto na planilha, editáveis, coláveis do Excel, derivadas de `CAMPOS_FOTO`
- COL-85-4: gates provando que o aviso do ML continua âmbar, com símbolo próprio, e não bloqueia
- Zero ramo novo em `onCellsEdited` (paste/fill/Delete/undo funcionam por herança do caminho genérico)
</success_criteria>

<output>
Create `.planning/phases/85-planilha-colunas-que-faltam-para-publicar-foto-atributos-de-/85-04-SUMMARY.md` when done
</output>
</content>
</invoke>
