---
id: 260627-09j
slug: dashboard-label-e-widget-desempenho-link
status: ready
files_modified:
  - resources/js/Pages/Dashboard/Admin.jsx
---

<objective>
Corrigir dois problemas visuais na dashboard admin em `Admin.jsx`: (1) substituir o label hardcoded "Carteira ativa Adman" por "Carteira ativa Performance", pois a query já conta todas as empresas do setor Performance sem filtrar por `adman_account_id`; (2) tornar o cabeçalho do widget "Desempenho da equipe" clicável, navegando para a rota `performance.index` via `Link` do Inertia.
</objective>

<context>
## Investigação realizada

### FIX 1 — Label "Carteira ativa Adman"

**Ocorrências encontradas** em `resources/js/Pages/Dashboard/Admin.jsx`:
- Linha 208: dentro de array de KPIs (layout full-screen / mobile) — `sub: 'Carteira ativa Adman'`
- Linha 352: KpiCard no layout principal — `sub="Carteira ativa Adman"`

**Query no controller** (`app/Http/Controllers/DashboardController.php`, linha 116–136):
```php
$companiesQuery = Company::with(['latestMetrics', 'consultor', 'estrategista'])
    ->where('active', true)
    ->whereDoesntHave('mlbEmpresa')
    ->whereHas('contratosServico', fn ($q) =>
        $q->where('contratos_servico.ativo', true)
          ->whereHas('servico', fn ($qs) =>
              $qs->where('setor', Servico::SETOR_PERFORMANCE)
          )
    );
```
Conclusão: **não filtra `adman_account_id`** — conta todas as empresas com contrato ativo no setor Performance (inclui empresas ML-only com `ml_store_id`). O label "Adman" está errado. Controller NÃO precisa de alteração.

### FIX 2 — Link no widget "Desempenho da equipe"

**Widget** em `Admin.jsx` linha 392–468:
```jsx
<div className="card-ecf rounded-2xl p-6">
    <p className="text-white/50 text-[11px] font-semibold tracking-widest uppercase mb-1 flex items-center gap-1.5">
        <Award size={12} /> Desempenho da equipe
    </p>
    <div className="flex items-baseline gap-2 mb-5">
        <p className="text-white font-display font-extrabold text-lg tracking-tight">Score por membro</p>
        ...
    </div>
    ...
</div>
```

**Rota alvo**: `performance.index` — confirmada em `routes/web.php` linha 242–244:
```php
Route::get('/performance', [PerformanceController::class, 'index'])
    ->middleware('permission:core.performance')
    ->name('performance.index');
```

**Import atual**: `Admin.jsx` linha 2 importa `{ router }` de `@inertiajs/react`, mas `Link` não está importado.

**Padrão do projeto**: outras páginas usam `router.visit(route('performance.index'))` (ver `Performance/Show.jsx` linha 56). Para título de widget, `Link` do Inertia é mais semântico e acessível por teclado nativamente.
</context>

<tasks>

<task id="T1">
  <name>Tarefa 1: Corrigir label "Carteira ativa Adman" → "Carteira ativa Performance"</name>
  <file>resources/js/Pages/Dashboard/Admin.jsx</file>
  <changes>
    <change>
      Linha 208 — dentro do array de KPIs (layout mobile/full-screen):
      ```jsx
      // ANTES:
      { title: 'Empresas', value: s.total_companies, icon: Users, color: 'blue', sub: 'Carteira ativa Adman' },
      // DEPOIS:
      { title: 'Empresas', value: s.total_companies, icon: Users, color: 'blue', sub: 'Carteira ativa Performance' },
      ```
    </change>
    <change>
      Linha 352 — KpiCard no layout principal:
      ```jsx
      // ANTES:
      &lt;KpiCard title="Empresas" value={s.total_companies} sub="Carteira ativa Adman" icon={Users} color="blue" empty={noData} /&gt;
      // DEPOIS:
      &lt;KpiCard title="Empresas" value={s.total_companies} sub="Carteira ativa Performance" icon={Users} color="blue" empty={noData} /&gt;
      ```
    </change>
  </changes>
  <commit>fix(dashboard): corrige label "Carteira ativa Adman" → "Carteira ativa Performance"</commit>
  <rationale>
    A query em DashboardController conta todas as empresas com contrato ativo no setor Performance,
    sem filtrar por adman_account_id. Com v11.0 adicionando empresas ML-only, o label "Adman" é enganoso.
    Controller não precisa de alteração — a query já é correta.
  </rationale>
</task>

<task id="T2">
  <name>Tarefa 2: Tornar cabeçalho do widget "Desempenho da equipe" clicável → /performance</name>
  <file>resources/js/Pages/Dashboard/Admin.jsx</file>
  <changes>
    <change>
      Linha 2 — adicionar `Link` ao import do Inertia:
      ```jsx
      // ANTES:
      import { router } from '@inertiajs/react';
      // DEPOIS:
      import { router, Link } from '@inertiajs/react';
      ```
    </change>
    <change>
      Linhas 392–401 — envolver o cabeçalho do widget (label + título) em `Link`:
      ```jsx
      // ANTES:
      &lt;div className="card-ecf rounded-2xl p-6"&gt;
          &lt;p className="text-white/50 text-[11px] font-semibold tracking-widest uppercase mb-1 flex items-center gap-1.5"&gt;
              &lt;Award size={12} /&gt; Desempenho da equipe
          &lt;/p&gt;
          &lt;div className="flex items-baseline gap-2 mb-5"&gt;
              &lt;p className="text-white font-display font-extrabold text-lg tracking-tight"&gt;Score por membro&lt;/p&gt;
              &lt;span className="text-white/40 text-[11px]"&gt;
                  · {performance_equipe.length} {performance_equipe.length === 1 ? 'pessoa' : 'pessoas'}
              &lt;/span&gt;
          &lt;/div&gt;

      // DEPOIS:
      &lt;div className="card-ecf rounded-2xl p-6"&gt;
          &lt;Link
              href={route('performance.index')}
              className="group block mb-5"
          &gt;
              &lt;p className="text-white/50 text-[11px] font-semibold tracking-widest uppercase mb-1 flex items-center gap-1.5 group-hover:text-white/70 transition-colors"&gt;
                  &lt;Award size={12} /&gt; Desempenho da equipe
              &lt;/p&gt;
              &lt;div className="flex items-baseline gap-2"&gt;
                  &lt;p className="text-white font-display font-extrabold text-lg tracking-tight group-hover:text-ecf-yellow transition-colors"&gt;Score por membro&lt;/p&gt;
                  &lt;span className="text-white/40 text-[11px]"&gt;
                      · {performance_equipe.length} {performance_equipe.length === 1 ? 'pessoa' : 'pessoas'}
                  &lt;/span&gt;
                  &lt;ChevronRight size={14} className="text-white/20 group-hover:text-ecf-yellow/60 transition-colors ml-auto self-center" /&gt;
              &lt;/div&gt;
          &lt;/Link&gt;
      ```
      Nota: `ChevronRight` já está importado na linha 10. O `mb-5` é movido para o `Link` wrapper. O restante do widget (BarChart + empty state) permanece inalterado abaixo do bloco `Link`.
    </change>
  </changes>
  <commit>feat(dashboard): widget Desempenho da equipe com link clicável para /performance</commit>
  <rationale>
    Torna o cabeçalho do widget navegável via Inertia Link (semântico, acessível por teclado/Tab).
    Hover feedback via group-hover: label sobe para white/70, título fica ecf-yellow, chevron aparece.
    Não altera a fonte de dados do widget nem unifica métricas — escopo restrito ao link (Phase 45 cobre o resto).
  </rationale>
</task>

</tasks>

<build>
Após ambas as tarefas:
```bash
npm run build
```
Verificar que build termina sem erros. Não executar deploy.sh sem autorização explícita.
</build>

<verification>
- [ ] `npm run build` termina verde (zero erros TypeScript/JSX)
- [ ] Visual FIX 1: card "Empresas" exibe sublabel "Carteira ativa Performance" (não "Adman")
- [ ] Visual FIX 2: cabeçalho do widget "Desempenho da equipe" tem cursor pointer; hover destaca label + título + chevron; click navega para `/performance`
- [ ] Acessibilidade: Tab no widget foca o Link; Enter navega para /performance
- [ ] Sem regressão: restante do widget (BarChart, barras coloridas por classificação, click por barra → carteira individual) funciona normalmente
</verification>
