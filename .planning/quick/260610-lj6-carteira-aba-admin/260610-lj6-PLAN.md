---
quick_id: 260610-lj6
slug: carteira-aba-admin
type: plan
mode: quick
created: 2026-06-10
description: Move widget "Carteira Portfólio por Profissional" do Dashboard para a aba Carteira; aba Carteira passa a mostrar visão consolidada de todos profissionais para admin, e segue mostrando carteira pessoal para os demais
files:
  - app/Http/Controllers/DashboardController.php
  - app/Http/Controllers/PortfolioController.php
  - resources/js/Pages/Dashboard/Admin.jsx
  - resources/js/Pages/Portfolio/Carteiras.jsx (NOVO)
must_haves:
  - Widget removido do Dashboard/Admin.jsx (JSX + prop user_portfolios)
  - DashboardController não calcula nem envia user_portfolios mais
  - PortfolioController::own() detecta admin via $user->isAdmin()
  - Admin acessando /portfolio → renderiza Portfolio/Carteiras (visão consolidada)
  - Não-admin acessando /portfolio → continua renderizando Portfolio/Show (carteira pessoal, como hoje)
  - Lógica de userPortfolios (analistas + estrategistas + TACOS por SUM/SUM) migrada para PortfolioController, sem regressão
---

# Quick Task 260610-lj6 — Carteira como aba admin (consolidada) ou pessoal

## Contexto

### Estado atual
- Dashboard Admin tem widget "Carteira Portfólio por Profissional" com a visão de
  TODOS analistas + estrategistas (impl. no quick 260610-f69).
- Sidebar tem item "Carteira" apontando para `portfolio.own` → renderiza
  `Portfolio/Show.jsx` (carteira pessoal do user logado).
- Para um admin, esse `portfolio.own` é inútil (admin não atende empresas).
- Para profissionais (analistas/estrategistas), `portfolio.own` é exatamente o que
  faz sentido (vê a SUA carteira).

### Mudança pedida
- Tirar o widget consolidado do Dashboard.
- Aba "Carteira" passa a ter comportamento bifurcado:
  - **Admin** → visão consolidada (cards de TODOS analistas/estrategistas + métricas
    de cada — o widget de hoje).
  - **Não-admin** → continua mostrando carteira pessoal (Portfolio/Show.jsx), sem
    mudança alguma.

## Tarefa única

### T-01 — Mover lógica + nova page + bifurcação no controller

#### A. `app/Http/Controllers/DashboardController.php` (limpeza)

**A.1 — Remover bloco de cálculo do `$userPortfolios`** (linhas ~418-446 +
queries dos analistas/estrategistas que são apenas pra ele). Atenção: as queries
`$analistas` e `$estrategistas` continuam sendo usadas pelos filtros (linhas
adicionadas no quick 260610-f69). NÃO removê-las. Só remover o bloco específico
do `$userPortfolios` (o `$todosProfissionais = $analistas->map(...)->concat(...)`
e o `$userPortfolios = ...->map(...)`).

**A.2 — Remover a key `'user_portfolios' => $userPortfolios,`** do array do
`Inertia::render('Dashboard/Admin', ...)`.

#### B. `app/Http/Controllers/PortfolioController.php` (bifurcação)

**B.1 — Modificar `own()`** para detectar admin e renderizar a página apropriada:

```php
public function own(Request $request)
{
    $user = $request->user();
    if ($user->isAdmin()) {
        return $this->renderCarteirasConsolidadas($request);
    }
    return $this->renderPortfolio($request, $user);
}
```

**B.2 — Adicionar método privado `renderCarteirasConsolidadas`** com a lógica que
estava no DashboardController. Esqueleto:

```php
/**
 * Visão consolidada de carteiras: cards de TODOS analistas e estrategistas
 * com métricas agregadas (TACOS, faturamento, margem, ad spend) da carteira
 * de cada um. Usado pela aba Carteira quando o user logado é admin.
 *
 * Fonte da verdade pra Analista vs Estrategista: cargo (slug) no pivot
 * user_setores → cargos, NÃO users.role (legacy).
 */
private function renderCarteirasConsolidadas(Request $request): \Inertia\Response
{
    $period = $request->get('period', '30');
    $days = match ($period) { '1' => 1, '7' => 7, '180' => 180, default => 30 };
    $since = now()->subDays($days);

    $analistas = User::where('active', 1)
        ->whereExists(function ($q) {
            $q->from('user_setores as us')
              ->join('cargos as c', 'c.id', '=', 'us.cargo_id')
              ->whereColumn('us.user_id', 'users.id')
              ->where('c.slug', 'analista');
        })
        ->orderBy('name')
        ->get(['id', 'name', 'role']);

    $estrategistas = User::where('active', 1)
        ->whereExists(function ($q) {
            $q->from('user_setores as us')
              ->join('cargos as c', 'c.id', '=', 'us.cargo_id')
              ->whereColumn('us.user_id', 'users.id')
              ->where('c.slug', 'estrategista');
        })
        ->orderBy('name')
        ->get(['id', 'name', 'role']);

    $todos = $analistas->map(fn($u) => ['user' => $u, 'tipo' => 'analista'])
        ->concat($estrategistas->map(fn($u) => ['user' => $u, 'tipo' => 'estrategista']));

    $portfolios = $todos->map(function ($item) use ($since) {
        $u = $item['user'];
        $tipo = $item['tipo'];

        $companyIds = ($tipo === 'estrategista')
            ? $u->estrategistaCompanies()->where('active', true)->pluck('companies.id')
            : $u->consultorCompanies()->where('active', true)->pluck('companies.id');

        if ($companyIds->isEmpty()) return null;

        $uMetrics = AdmanMetric::whereIn('company_id', $companyIds)
            ->where('reference_date', '>=', $since->toDateString())
            ->get();

        $totalRevenue = (float) $uMetrics->sum('revenue');
        $totalAdSpend = (float) $uMetrics->sum('ad_spend');
        $tacosCarteira = $totalRevenue > 0
            ? round(($totalAdSpend / $totalRevenue) * 100, 2)
            : null;

        return [
            'id'              => $u->id,
            'name'            => $u->name,
            'tipo'            => $tipo,
            'role'            => $u->role,
            'companies_count' => $companyIds->count(),
            'avg_tacos'       => $tacosCarteira,
            'total_revenue'   => $totalRevenue,
            'avg_margin'      => $uMetrics->count() > 0 ? round($uMetrics->avg('contribution_margin_pct'), 2) : null,
            'total_ad_spend'  => $totalAdSpend,
        ];
    })->filter()->sortBy('name')->values();

    return Inertia::render('Portfolio/Carteiras', [
        'user_portfolios' => $portfolios,
        'period'          => $period,
    ]);
}
```

#### C. `resources/js/Pages/Portfolio/Carteiras.jsx` (NOVO)

Criar nova page com AppLayout, cabeçalho "Carteiras" + "Portfólio por Profissional",
e o grid de cards. Pode reusar visualmente o mesmo card do widget removido. Deve:
- Receber prop `user_portfolios` (mesmo shape de antes).
- Render layout dentro de `AppLayout title="Carteiras"`.
- Cada card: nome, tipo (Analista/Estrategista), companies_count, 4 métricas
  (TACOS médio, Faturamento, Margem Méd., Gasto Ads), botão "Ver" linkando para
  `route('portfolio.show', u.id)`.
- Empty state se `user_portfolios.length === 0` (improvável em prod, mas defensivo).
- Reaproveitar imports/utilities (`formatCurrency`, `formatPercent`, `Briefcase`,
  `ChevronRight`, `route()`, `router`, `cn`) — espelhar o que o widget atual usa.

Texto do empty state: "Nenhum profissional com carteira."

#### D. `resources/js/Pages/Dashboard/Admin.jsx` (remoção)

**D.1 — Remover a prop `user_portfolios = []`** da assinatura do componente.
**D.2 — Remover o bloco JSX inteiro do widget** (`{user_portfolios.length > 0 && (...)}`,
linhas ~453-512 do estado atual).
**D.3 — Manter o restante (filtros analistas/estrategistas com cross-filter, KPIs,
ranking, etc) intacto.**

### Verifies (executor roda antes do commit)

```bash
# 1) Backend — DashboardController nao envia mais user_portfolios
grep -n "user_portfolios\|userPortfolios\|todosProfissionais" app/Http/Controllers/DashboardController.php
# Esperado: 0 linhas

# 2) Backend — PortfolioController tem o novo metodo + bifurcacao em own()
grep -nE "isAdmin\(\)|renderCarteirasConsolidadas" app/Http/Controllers/PortfolioController.php
# Esperado: pelo menos 2 linhas (1 em own(), 1 na definicao do metodo)

# 3) Frontend — Carteiras.jsx existe e usa AppLayout + user_portfolios
test -f resources/js/Pages/Portfolio/Carteiras.jsx && echo "OK Carteiras.jsx existe"
grep -nE "AppLayout|user_portfolios|portfolio\.show" resources/js/Pages/Portfolio/Carteiras.jsx

# 4) Frontend — Admin.jsx nao tem mais user_portfolios
grep -n "user_portfolios" resources/js/Pages/Dashboard/Admin.jsx
# Esperado: 0 linhas

# 5) Build
npm run build 2>&1 | tail -5
```

**Done**: commit atomico em pt-BR. Mensagem sugerida:
`fix(carteira): aba Carteira mostra visao consolidada para admin; widget removido do Dashboard`

## SUMMARY.md esperado

`.planning/quick/260610-lj6-carteira-aba-admin/260610-lj6-SUMMARY.md` em pt-BR com:
- Frontmatter status: complete, commits, files_modified, files_created (Carteiras.jsx)
- Resumo da bifurcação admin vs não-admin
- Saída dos 5 verifies
- Nota: orquestrador fará deploy + smoke pós-merge
