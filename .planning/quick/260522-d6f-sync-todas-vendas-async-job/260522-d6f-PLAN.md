---
phase: 260522-d6f-sync-todas-vendas-async-job
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Jobs/SyncTodasVendasAdmanJob.php
  - app/Http/Controllers/MlbController.php
autonomous: true
requirements:
  - QUICK-260522-D6F
must_haves:
  truths:
    - "POST /mlb/sync-vendas (mlb.sync-vendas) retorna imediatamente com flash de sucesso (sem 504)"
    - "O loop sobre MlbEmpresa com cust_id roda dentro de SyncTodasVendasAdmanJob no queue worker"
    - "syncVendasAdman por empresa individual continua sincrono, intocado"
    - "Falhas no job sao registradas com tag [MLB SyncTodasVendas]"
  artifacts:
    - path: "app/Jobs/SyncTodasVendasAdmanJob.php"
      provides: "Job assincrono que processa o sync de vendas de todas as empresas com cust_id"
      contains: "class SyncTodasVendasAdmanJob implements ShouldQueue"
    - path: "app/Http/Controllers/MlbController.php"
      provides: "syncTodasVendasAdman agora apenas valida + despacha o Job"
      contains: "SyncTodasVendasAdmanJob::dispatch"
  key_links:
    - from: "app/Http/Controllers/MlbController.php::syncTodasVendasAdman"
      to: "app/Jobs/SyncTodasVendasAdmanJob"
      via: "SyncTodasVendasAdmanJob::dispatch(date_from, date_to, user_id)"
      pattern: "SyncTodasVendasAdmanJob::dispatch"
    - from: "app/Jobs/SyncTodasVendasAdmanJob::handle"
      to: "app/Services/AdmanService::fetchPerformance"
      via: "DI do AdmanService no handle()"
      pattern: "fetchPerformance\\("
---

<objective>
Mover o loop sincrono de `MlbController::syncTodasVendasAdman` (que sincroniza vendas via Adman para ~17 empresas em uma unica requisicao) para um Job assincrono `SyncTodasVendasAdmanJob`. O controller passa a apenas validar + despachar + retornar flash imediatamente, eliminando o 504 do nginx.

Purpose: Eliminar o timeout 504 causado por loop sincrono de ~17 chamadas * ate 120s + delays. Manter rota, controller e front-end externamente identicos.
Output: Novo Job em `app/Jobs/`, controller refatorado para dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/Http/Controllers/MlbController.php
@app/Jobs/SyncAdmanCompanyJob.php
@app/Services/AdmanService.php

<interfaces>
<!-- Contratos chave extraidos do codigo existente. Use direto, sem precisar reexplorar. -->

From app/Jobs/SyncAdmanCompanyJob.php (padrao de Job a seguir):
```php
namespace App\Jobs;

use Illuminate\Bus\Queueable;
use Illuminate\Contracts\Queue\ShouldQueue;
use Illuminate\Foundation\Bus\Dispatchable;
use Illuminate\Queue\InteractsWithQueue;
use Illuminate\Queue\SerializesModels;
use Illuminate\Support\Facades\Log;

class SyncAdmanCompanyJob implements ShouldQueue
{
    use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;

    public int $tries = 3;
    public int $timeout = 120;

    public function __construct(public readonly Company $company, public readonly ?string $date = null) {}

    public function backoff(): array { return [60, 300, 900]; }

    public function handle(AdmanService $adman): void { /* ... */ }

    public function failed(\Throwable $e): void {
        Log::error("[SyncAdmanCompanyJob] Falha definitiva ...: {$e->getMessage()}");
    }
}
```

From app/Services/AdmanService.php:
```php
public function fetchPerformance(string $custId, string $dateFrom, string $dateTo, int $maxRetries = 3): array
```

From app/Http/Controllers/MlbController.php (loop atual a ser movido, linhas 1715-1774):
- Valida `date_from` (date|before_or_equal:today) e `date_to` (after_or_equal:date_from)
- `MlbEmpresa::whereNotNull('cust_id')->where('cust_id', '!=', '')->get()`
- Para cada empresa: `fetchPerformance()` -> `extrairMlbsVendidos()` -> update em `Publicacao` (`vendido=true`, `vendas_qty` via GREATEST, `preco_unitario`, `net_billing`)
- `usleep(600_000)` entre chamadas
- Acumula totais: itens, com_venda, encontradas, erros
- Log de erro por empresa: `Log::error("[MLB SyncTodasVendas] {$empresa->nome}: ...")`

From app/Http/Controllers/MlbController.php::extrairMlbsVendidos (linha 2006, privado):
- Recebe `array $items` da Adman
- Normaliza ID (MLB + digits), extrai qty/preco/netBilling
- Retorna `[mlb_code => ['qty' => N, 'preco' => P, 'net_billing' => B]]`
- Eh privado e usado em 3 lugares no controller (linhas 742, 1688, 1738) — NAO remover

From app/Models/Publicacao (uso atual no loop):
```php
Publicacao::where('mlb_code', $mlbCode)->update([
    'vendido'        => true,
    'vendas_qty'     => DB::raw("GREATEST(COALESCE(vendas_qty, 0), {$intQty})"),
    'preco_unitario' => $data['preco'],
    'net_billing'    => $data['net_billing'] > 0 ? $data['net_billing'] : null,
]);
```

From routes/web.php (linha 215, NAO alterar):
```php
Route::post('/sync-vendas', [MlbController::class, 'syncTodasVendasAdman'])->name('sync-vendas');
```

From resources/js/Pages/Mlb/Empresas.jsx::SyncVendasModal (linha 68, NAO alterar):
```jsx
post(route('mlb.sync-vendas'), { onSuccess: onClose });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar SyncTodasVendasAdmanJob com loop migrado e helper de extracao</name>
  <files>app/Jobs/SyncTodasVendasAdmanJob.php</files>
  <action>
Criar `app/Jobs/SyncTodasVendasAdmanJob.php` (namespace `App\Jobs`) seguindo o padrao de `SyncAdmanCompanyJob`. Especificacoes obrigatorias:

- `class SyncTodasVendasAdmanJob implements ShouldQueue` com traits `Dispatchable, InteractsWithQueue, Queueable, SerializesModels`.
- Propriedades publicas:
  - `public int $tries = 1` — sync de vendas e idempotente por publicacao, mas re-rodar o loop inteiro em caso de falha global desperdica chamadas API; preferir falha unica com log.
  - `public int $timeout = 0` — operacao longa (~17 empresas * ate 120s + delays); 0 = sem timeout no nivel do job.
- Construtor com promoted properties (readonly): `public readonly string $dateFrom, public readonly string $dateTo, public readonly ?int $userId = null`. NAO serializar o `Request` nem o `User` inteiro — apenas o `userId` para contexto de log.
- Metodo `handle(AdmanService $adman): void`:
  1. Carregar `MlbEmpresa::whereNotNull('cust_id')->where('cust_id', '!=', '')->get()` — usar import `App\Models\MlbEmpresa`.
  2. Inicializar `$totais = ['itens' => 0, 'com_venda' => 0, 'encontradas' => 0, 'erros' => 0]`.
  3. Log de inicio: `Log::info("[MLB SyncTodasVendas] Iniciando sync para {count} empresa(s) | de={$this->dateFrom} ate={$this->dateTo} | user_id={$this->userId}")`.
  4. Replicar o loop atual do controller (linhas 1734-1763) tal qual: `try { fetchPerformance -> $this->extrairMlbsVendidos($items) -> update Publicacao com GREATEST/COALESCE -> usleep(600_000) } catch (\Throwable $e) { Log::error("[MLB SyncTodasVendas] {$empresa->nome}: ".$e->getMessage()); $totais['erros']++; }`.
  5. Log final de resumo: `Log::info(sprintf('[MLB SyncTodasVendas] Concluido: %d itens, %d com venda, %d publicacoes sincronizadas, %d erro(s)', $totais['itens'], $totais['com_venda'], $totais['encontradas'], $totais['erros']))`.
- Metodo privado `extrairMlbsVendidos(array $items): array` dentro do proprio Job — COPIAR (nao mover) o corpo completo do metodo homonimo em `MlbController` (linhas 2006-2060+) para evitar tornar o helper publico. Adicionar comentario no topo do metodo: `// pt-BR: Copia local do helper privado em MlbController para encapsular o Job sem dependencia inversa.`.
- Metodo `failed(\Throwable $e): void` registrando `Log::error("[MLB SyncTodasVendas] Falha definitiva do job: {$e->getMessage()}")`.
- Imports necessarios: `App\Models\MlbEmpresa`, `App\Models\Publicacao`, `App\Services\AdmanService`, `Illuminate\Bus\Queueable`, `Illuminate\Contracts\Queue\ShouldQueue`, `Illuminate\Foundation\Bus\Dispatchable`, `Illuminate\Queue\InteractsWithQueue`, `Illuminate\Queue\SerializesModels`, `Illuminate\Support\Facades\DB`, `Illuminate\Support\Facades\Log`.
- Comentarios em pt-BR; docblock de classe explicando o porque (evitar 504 do nginx).
- NAO usar `Log::warning` para items sem ID dentro do `extrairMlbsVendidos` copiado — manter exatamente o comportamento atual com tag `[MLB SyncVendas]`.
  </action>
  <verify>
    <automated>php -l app/Jobs/SyncTodasVendasAdmanJob.php</automated>
  </verify>
  <done>Arquivo existe, passa no lint do PHP, class declara `implements ShouldQueue`, tem handle() + failed() + extrairMlbsVendidos() privado, e usa tag de log `[MLB SyncTodasVendas]`.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar syncTodasVendasAdman para despachar o Job</name>
  <files>app/Http/Controllers/MlbController.php</files>
  <action>
Editar `app/Http/Controllers/MlbController.php` no metodo `syncTodasVendasAdman` (linhas 1715-1774):

1. Adicionar import no topo do arquivo (apos os imports existentes de Jobs, se houver, ou apos imports de Models): `use App\Jobs\SyncTodasVendasAdmanJob;`.
2. Substituir o corpo do metodo `syncTodasVendasAdman` (entre `{` e `}` finais) por:
   - Manter `$this->checkPubAccess('vendas');` e `$this->checkPubRole(['gestor']);`.
   - REMOVER `set_time_limit(0)` e `ini_set('memory_limit', '256M')` — nao sao mais necessarios; o controller retorna em ms.
   - Manter o `$request->validate([...])` identico (mesmas regras: date_from/date_to required|date|before_or_equal:today, after_or_equal).
   - Adicionar uma checagem leve antes do dispatch: `$totalEmpresas = MlbEmpresa::whereNotNull('cust_id')->where('cust_id', '!=', '')->count();` para enriquecer a flash message.
   - Despachar: `SyncTodasVendasAdmanJob::dispatch($request->date_from, $request->date_to, $request->user()?->id);`.
   - Retornar imediatamente: `return back()->with('success', sprintf('Sync de vendas iniciado em background para %d empresa(s). Acompanhe pelos logs.', $totalEmpresas));`.
3. NAO tocar em `syncVendasAdman` (por empresa, linhas 1663-1710) — esse continua sincrono.
4. NAO tocar no metodo privado `extrairMlbsVendidos` (linha 2006) — outros pontos do controller (linhas 742 e 1688) ainda o usam.
5. NAO alterar a assinatura publica do metodo (`public function syncTodasVendasAdman(Request $request)`).
6. Comentarios em pt-BR: adicionar comentario antes do dispatch: `// pt-BR: Despacha o loop sincrono para queue worker — evita 504 do nginx em ~17 empresas * 120s.`.
  </action>
  <verify>
    <automated>php -l app/Http/Controllers/MlbController.php</automated>
  </verify>
  <done>php -l passa, `syncTodasVendasAdman` despacha o Job e retorna `back()->with('success', ...)` sem rodar o loop, `syncVendasAdman` individual permanece intocado, `extrairMlbsVendidos` privado permanece no controller, e a rota `mlb.sync-vendas` continua respondendo (sem mudancas em routes/web.php nem no front-end).</done>
</task>

</tasks>

<verification>
Verificacao end-to-end manual (apos `php artisan queue:work` ou worker rodando localmente):

1. Login como gestor MLB no painel local.
2. Abrir modal "Sync Vendas" sem selecionar empresa (sync de todas).
3. Submeter date_from/date_to validos.
4. Confirmar:
   - Resposta retorna em <1s com flash "Sync de vendas iniciado em background para N empresa(s)..."
   - Linha em `jobs` table aparece (ou e processada): `select * from jobs where payload like '%SyncTodasVendasAdmanJob%' limit 1;`
   - Logs em `storage/logs/laravel.log` mostram `[MLB SyncTodasVendas] Iniciando sync ...` seguido (apos alguns minutos) de `[MLB SyncTodasVendas] Concluido: ...`
5. Verificar que `POST /mlb/empresas/{id}/sync-vendas` (sync individual) ainda funciona sincrono e nao foi afetado.

Lint estatico:
- `php -l app/Jobs/SyncTodasVendasAdmanJob.php`
- `php -l app/Http/Controllers/MlbController.php`
</verification>

<success_criteria>
- `POST /mlb/sync-vendas` retorna em <1s (vs. >60s atualmente que causa 504).
- O loop original e fielmente reproduzido dentro do Job (mesmos campos atualizados em Publicacao, mesmo delay de 600ms, mesma logica de extracao).
- Nenhuma mudanca em: routes/web.php, resources/js/Pages/Mlb/Empresas.jsx, metodo syncVendasAdman individual, metodo privado extrairMlbsVendidos no controller.
- Tag de log `[MLB SyncTodasVendas]` mantida.
- Sem deploy (rodar apenas local).
</success_criteria>

<output>
Criar `.planning/quick/260522-d6f-sync-todas-vendas-async-job/260522-d6f-SUMMARY.md` ao concluir, registrando:
- Arquivo do Job criado
- Linhas alteradas no controller (faixa de linhas antes/depois)
- Confirmacao de que a rota e o front-end nao foram tocados
- Comando usado para testar localmente (queue worker)
</output>
