---
phase: quick-260602-d8i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/MercadoLivreOAuthController.php
  - routes/web.php
  - resources/js/Pages/MlOAuth/Index.jsx
autonomous: true
requirements: [QUICK-260602-d8i]
must_haves:
  truths:
    - "Admin vê um botão 'Sincronizar todas as conectadas' no cabeçalho do painel ML OAuth, ao lado do contador de conectadas"
    - "Ao clicar, todas as empresas com token ML ativo são enfileiradas para sync D-1 (ontem) — mesmo fan-out do comando ml:sync"
    - "O botão fica desabilitado quando não há empresas conectadas (connected.length === 0)"
    - "Durante o disparo o botão mostra spinner e fica desabilitado; após sucesso exibe a contagem de empresas enfileiradas"
  artifacts:
    - path: "app/Http/Controllers/MercadoLivreOAuthController.php"
      provides: "Método syncAll() que faz fan-out assíncrono via SyncMlCompanyJob"
      contains: "public function syncAll"
    - path: "routes/web.php"
      provides: "Rota POST ml.oauth.sync-all no grupo admin/role:admin"
      contains: "ml.oauth.sync-all"
    - path: "resources/js/Pages/MlOAuth/Index.jsx"
      provides: "Botão de sync global no cabeçalho com estados de loading/feedback"
  key_links:
    - from: "resources/js/Pages/MlOAuth/Index.jsx"
      to: "ml.oauth.sync-all"
      via: "fetch POST"
      pattern: "ml.oauth.sync-all"
    - from: "app/Http/Controllers/MercadoLivreOAuthController.php"
      to: "App\\Jobs\\SyncMlCompanyJob"
      via: "dispatch com delay escalonado"
      pattern: "SyncMlCompanyJob::dispatch"
---

<objective>
Adicionar um botão "Sincronizar todas as conectadas" no cabeçalho do painel Mercado Livre OAuth. Ao clicar, dispara o sync D-1 (ontem) de TODAS as empresas com token OAuth ativo — replicando o fan-out assíncrono que o comando `ml:sync` já executa, sem travar o request.

Purpose: Permitir que o admin dispare o sync global manualmente pelo painel, sem acesso ao servidor / linha de comando.
Output: Novo método `syncAll()` no controller, rota `ml.oauth.sync-all`, e botão no header do painel React. Build de frontend validado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Contratos já existentes no codebase. O executor deve usá-los diretamente. -->

Padrão de fan-out (app/Console/Commands/SyncMlData.php, linhas 66-90) a replicar no controller:
- `Company::query()->where('active', true)->whereHas('mlToken', fn($q) => $q->where('status', 'active'))->with('mlToken')->get()`
- Loop com `SyncMlCompanyJob::dispatch($company, $date)->delay(now()->addSeconds($i * 2))` — 2s entre jobs (rate limit ML 1500 req/min)
- Data alvo: `now()->subDay()->toDateString()` (ML é D-1)
- Caso 0 empresas: tratar e retornar contagem zero

Controller MercadoLivreOAuthController (app/Http/Controllers/MercadoLivreOAuthController.php):
- Já importa: App\Models\Company, Illuminate\Http\Request, Illuminate\Support\Facades\Log, Illuminate\Http\JsonResponse
- FALTA importar: App\Jobs\SyncMlCompanyJob
- Métodos existentes: adminIndex(), initiate(), callback(), syncNow() (sync de UMA empresa, síncrono via $this->ml->syncCompany), disconnect()
- syncNow() retorna response()->json([...]) — seguir esse padrão de resposta JSON

Rota (routes/web.php, grupo admin/role:admin, linhas 222-225) — adicionar logo após ml.sync.now:
- `Route::post('/ml-oauth/sync-all', [MercadoLivreOAuthController::class, 'syncAll'])->name('ml.oauth.sync-all');`
- SEM {company} (é global)

Frontend (resources/js/Pages/MlOAuth/Index.jsx):
- Imports já presentes: `useState` (linha 2), `RefreshCw` de lucide-react (linha 4)
- Componente default: `MlOAuthIndex({ companies: initial })`; possui `connected` e `pending` (arrays derivados)
- Header nas linhas 233-247: `<div>` com `<h1>Mercado Livre OAuth</h1>`, `<p>` mostrando `{connected.length} conectada(s) · {pending.length} aguardando autorização`, e um `<input>` de busca
- Padrão de fetch (gerarLink linhas 73-91, desconectar 100-115):
  - `fetch(route('nome.rota'), { method: 'POST', headers: { 'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]')?.content ?? '', 'Accept': 'application/json' } })`
- Estilo do botão amarelo (linha 171): `bg-[#ffe116]/10 border border-[#ffe116]/20 text-[#ffe116] hover:bg-[#ffe116]/15`
- Spinner em loading (linha 174): `<RefreshCw size={11} className="animate-spin" />`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Método syncAll() no controller + rota ml.oauth.sync-all</name>
  <files>app/Http/Controllers/MercadoLivreOAuthController.php, routes/web.php</files>
  <action>
    No controller, adicionar `use App\Jobs\SyncMlCompanyJob;` junto aos imports existentes. Criar o método público `syncAll(Request $request): JsonResponse` (colocar após syncNow(), antes de disconnect()), com docblock pt-BR explicando que dispara o fan-out D-1 de todas as empresas com token ML ativo (mesmo comportamento do comando ml:sync).

    Lógica do método:
    1. `$date = $request->input('date', now()->subDay()->toDateString());` (D-1 por padrão, mas aceita override).
    2. Buscar empresas: `Company::query()->where('active', true)->whereHas('mlToken', fn($q) => $q->where('status', 'active'))->with('mlToken')->get();`
    3. `$total = $companies->count();`
    4. Se `$total === 0`: logar com tag [MercadoLivre] que não há empresas e retornar `response()->json(['enfileiradas' => 0, 'date' => $date]);`
    5. Loop com delay escalonado: `foreach ($companies as $i => $company) { SyncMlCompanyJob::dispatch($company, $date)->delay(now()->addSeconds($i * 2)); }` — comentário pt-BR sobre os 2s e rate limit ML 1500 req/min.
    6. Logar com tag [MercadoLivre] incluindo total, data e o usuário que disparou: usar `auth()->user()?->name` (padrão do disconnect na linha 194).
    7. Retornar `response()->json(['enfileiradas' => $total, 'date' => $date]);`

    Usar o Job ASSÍNCRONO (dispatch), NÃO síncrono — para não travar o request, diferente do syncNow() que é síncrono para uma empresa só.

    Em routes/web.php, dentro do MESMO grupo admin/role:admin onde estão as rotas ml.oauth.*, adicionar logo após a linha do ml.sync.now (linha 225):
    `Route::post('/ml-oauth/sync-all', [MercadoLivreOAuthController::class, 'syncAll'])->name('ml.oauth.sync-all');`
    Manter o alinhamento de colunas das rotas vizinhas (padrão do arquivo) e adicionar comentário pt-BR curto.
  </action>
  <verify>
    <automated>php artisan route:list --name=ml.oauth.sync-all</automated>
  </verify>
  <done>A rota `ml.oauth.sync-all` aparece no route:list apontando para MercadoLivreOAuthController@syncAll com método POST. O método existe, importa SyncMlCompanyJob, faz fan-out assíncrono D-1, trata o caso de 0 empresas, loga com tag [MercadoLivre] + usuário, e retorna JSON com `enfileiradas` e `date`.</done>
</task>

<task type="auto">
  <name>Task 2: Botão "Sincronizar todas as conectadas" no header + npm run build</name>
  <files>resources/js/Pages/MlOAuth/Index.jsx</files>
  <action>
    No componente `MlOAuthIndex`, adicionar dois estados com useState: `syncing` (boolean, default false) e `syncMsg` (string|null, default null) para feedback.

    Criar a função `sincronizarTodas` (async), seguindo o padrão de fetch do arquivo (gerarLink/desconectar):
    1. Setar `syncing = true` e limpar `syncMsg`.
    2. `fetch(route('ml.oauth.sync-all'), { method: 'POST', headers: { 'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]')?.content ?? '', 'Accept': 'application/json' } })` e `const data = await res.json();`
    3. Em sucesso: `setSyncMsg(\`${data.enfileiradas} empresa(s) enfileirada(s) para sync (${data.date})\`)`.
    4. Em erro/catch: setar mensagem de erro pt-BR curta.
    5. No finally: `syncing = false`. Limpar `syncMsg` após ~5s com setTimeout (padrão do `copied` na linha 97).

    No header (div das linhas 233-247), adicionar um botão ANTES do `<input>` de busca (ou agrupado próximo ao contador `connected.length`). Ajustar o container para acomodar o novo botão sem quebrar o layout `flex items-center justify-between`.

    Botão:
    - `onClick={sincronizarTodas}`
    - `disabled={syncing || connected.length === 0}` — desabilitado quando não há conectadas ou durante o disparo.
    - Estilo amarelo do arquivo: `bg-[#ffe116]/10 border border-[#ffe116]/20 text-[#ffe116] hover:bg-[#ffe116]/15` com `disabled:opacity-40`, e classes de tamanho/padding consistentes com os botões existentes (ex: `inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-[11px] font-medium transition-colors`).
    - Ícone: `<RefreshCw size={11} className={syncing ? 'animate-spin' : ''} />` (RefreshCw já importado).
    - Texto: "Sincronizar todas as conectadas" (ou "Sincronizando…" enquanto `syncing`).

    Exibir `syncMsg` como feedback discreto perto do header quando não for null (ex: pequeno `<p>` em text-emerald-400/text-[11px] para sucesso). Manter pt-BR em todos os textos e comentários.

    Ao final, rodar `npm run build` e confirmar que passou sem erros (regra obrigatória do projeto — CLAUDE.md).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`npm run build` completa sem erros. O header do painel ML OAuth exibe o botão "Sincronizar todas as conectadas" próximo ao contador de conectadas; o botão dispara POST para ml.oauth.sync-all, mostra spinner + texto durante o disparo, fica desabilitado quando connected.length === 0, e exibe a contagem retornada após sucesso.</done>
</task>

</tasks>

<verification>
- `php artisan route:list --name=ml.oauth.sync-all` mostra a rota POST registrada no grupo admin.
- `npm run build` passa sem erros de compilação.
- Inspeção manual: botão aparece no header, desabilitado quando 0 conectadas; ao clicar com empresas conectadas, retorna JSON `{ enfileiradas, date }` e os SyncMlCompanyJob aparecem na tabela `jobs`.
</verification>

<success_criteria>
- Método `syncAll()` faz fan-out assíncrono D-1 idêntico ao comando `ml:sync` (empresas active + token ML active, delay 2s entre jobs).
- Rota `ml.oauth.sync-all` registrada no grupo admin/role:admin.
- Botão no header dispara o sync, trata loading/feedback/erro, e respeita o estado desabilitado quando não há conectadas.
- Comentários em pt-BR; `npm run build` validado; nenhum deploy executado.
</success_criteria>

<output>
Criar `.planning/quick/260602-d8i-btn-sync-todas-ml-oauth/260602-d8i-SUMMARY.md` ao concluir.
</output>
