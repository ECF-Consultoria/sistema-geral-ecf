---
quick_id: 260814-d9s
phase: quick-260814-d9s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Clicksign/ClicksignClient.php
  - tests/Feature/Phase126/ClicksignClientEnvelopeTest.php
  - tests/Fixtures/ClicksignSandboxFixtures.php
  - .planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md
autonomous: true
requirements: [CLICK-01]

must_haves:
  truths:
    - "O corpo aceito por POST /envelopes/{id}/signers/{id}/notifications é MEDIDO contra o sandbox real, nunca deduzido"
    - "reenviarNotificacao() envia o corpo JSON:API medido e deixa de receber \"data deve ser informado(a)\""
    - "Um teste automatizado ASSEVERA o corpo enviado (não só o status da resposta) — a lacuna que deixou o bug passar"
    - "O 429 anti-spam continua não sendo retentado e continua virando ClicksignException com httpStatus 429"
    - "O achado fica registrado no CLICKSIGN-SANDBOX-EMPIRICO.md, fonte de verdade empírica do projeto"
  artifacts:
    - path: "app/Services/Clicksign/ClicksignClient.php"
      provides: "reenviarNotificacao() com corpo JSON:API"
      contains: "notifications"
    - path: "tests/Feature/Phase126/ClicksignClientEnvelopeTest.php"
      provides: "teste que assevera o CORPO do POST de notificação"
      contains: "reenviar_notificacao"
    - path: ".planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md"
      provides: "registro da sessão de medição do endpoint de notificação"
      contains: "notifications"
  key_links:
    - from: "tests/Feature/Phase126/ClicksignClientEnvelopeTest.php"
      to: "ClicksignClient::reenviarNotificacao()"
      via: "Http::assertSent inspecionando o corpo"
      pattern: "assertSent"
---

<objective>
Corrigir `ClicksignClient::reenviarNotificacao()`, que faz `POST` **sem corpo** e recebe
`data deve ser informado(a)` da API v3 (JSON:API exige o membro `data`). O reenvio de notificação
está quebrado **também em produção** — nunca foi exercitado de verdade.

Propósito: o único teste que cobria o método faz `Http::fake()` de 429 e **não afirma nada sobre o
corpo enviado**. Um POST vazio passa nesse teste e falha na API real. Corrigir sem fechar essa
lacuna deixaria o bug voltar na próxima refatoração.

Saída: corpo medido empiricamente contra a sandbox, método corrigido, teste que assevera o corpo, e
o achado registrado no `CLICKSIGN-SANDBOX-EMPIRICO.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@app/Services/Clicksign/ClicksignClient.php
@tests/Feature/Phase126/ClicksignClientEnvelopeTest.php
@tests/Fixtures/ClicksignSandboxFixtures.php
@.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md

<interfaces>
<!-- Contratos que o executor já tem — não sair explorando o codebase. -->

Assinatura atual (app/Services/Clicksign/ClicksignClient.php:474):
  public function reenviarNotificacao(string $envelopeId, string $signerId): array

Este método NÃO passa pelo `enviar()` comum, de propósito (126-02-SUMMARY.md): usa
`baseRequest()->post($url)` direto, porque o retry disciplinado do `enviar()` (D-11) retentaria o
429 anti-spam 3 vezes — comportamento errado para um limite de anti-spam. **Manter esse desenho.**

`baseRequest(): PendingRequest` já monta `Authorization` (sem "Bearer"), `Accept` e
`Content-Type: application/vnd.api+json` + timeout 30s.

FORMA do corpo JSON:API que os outros POST do mesmo arquivo já usam e que funcionam:
  criarEnvelope()        -> ['data' => ['type' => 'envelopes',   'attributes' => [...]]]
  adicionarSignatario()  -> ['data' => ['type' => 'signers',     'attributes' => ['name','email','group']]]
  criarRequisito()       -> ['data' => ['type' => 'requirements','attributes' => [...], 'relationships' => [...]]]
  ativarEnvelope()       -> ['data' => ['id','type' => 'envelopes','attributes' => [...]]]   (PATCH)
  criarModelo()          -> ['data' => ['type' => 'templates',   'attributes' => [...]]]

Fixture do 429 (tests/Fixtures/ClicksignSandboxFixtures.php:289):
  ClicksignSandboxFixtures::erro429NotificacaoTexto(): ['body' => 'Too many requests']

Constantes do teste (tests/Feature/Phase126/ClicksignClientEnvelopeTest.php:27-37):
  BASE = 'https://sandbox.clicksign.com/api/v3'
  ENVELOPE_ID / DOCUMENT_ID / SIGNER_ID = UUIDs fictícios
  client(): new ClicksignClient(token: self::TOKEN, baseUrl: self::BASE)

Guarda de ambiente a copiar (app/Console/Commands/ClicksignSondarModelo.php:118):
  garantirAmbienteSeguro() — só segue quando config('services.clicksign.env') === 'sandbox'
</interfaces>
</context>

<alvo_vivo>
Recursos confirmados na sandbox (do `130-GATE.md`) para a medição da Task 1:

- Envelope ATIVO: `f010d235-ff75-400a-84b7-01cb89c3ef59` (status `running`)
- Documento: `36fac7dc-e6c7-4a51-84b7-ce168cf583a3`
- Os 4 `signerId` **devem ser obtidos** por `GET /envelopes/{id}/signers` — a v3 devolve
  `name`/`email` no recurso (achado 2 do `129-GATE.md`). **Não reusar id de log sem reconfirmar.**

⚠️ **RATE LIMIT ANTI-SPAM PRÓPRIO** (§7 do empírico, MEDIDO): este endpoint devolve **429 em texto
puro** (não JSON:API) enquanto a janela geral seguia com 19/20 disponíveis. Poucas tentativas,
espaçadas. Se vier 429, **isso já é informação útil** — prova que o corpo foi aceito pelo parser e
que o limite é outro. Registrar e PARAR. Não martelar.
</alvo_vivo>

<tasks>

<task type="auto">
  <name>Task 1: Medir empiricamente o corpo aceito pelo endpoint de notificação</name>
  <read_first>
    - `app/Services/Clicksign/ClicksignClient.php` (métodos POST + `baseRequest()`)
    - `app/Console/Commands/ClicksignSondarModelo.php:118-139` (`garantirAmbienteSeguro()`)
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` §7 (rate limit anti-spam) e §9 (formato de
      registro das sessões de medição anteriores)
  </read_first>
  <files>
    (script temporário no scratchpad da SESSÃO — fora do repositório; nenhum arquivo do repo é
    alterado nesta task)
  </files>
  <action>
    ⚠️ **A forma exata do corpo é DESCONHECIDA. NÃO INVENTAR.** Provavelmente é
    `{"data":{"type":"...","attributes":{...}}}`, mas `type` e quais `attributes` são
    obrigatórios/opcionais precisam ser MEDIDOS. Este projeto tem um histórico de campo deduzido que
    quebrou 100% dos envelopes (`communicate_by`, §9.1 do empírico) — a dedução já custou caro aqui.

    Escrever um script de sondagem **no diretório de scratchpad da própria sessão** (o temp dir da
    sessão, NUNCA dentro do repositório — o `deploy.sh` exige árvore limpa e outras sessões editam a
    mesma árvore). O script boota o framework manualmente:
    `require vendor/autoload.php` -> `bootstrap/app.php` -> `Kernel::bootstrap()`, e usa
    `Http::withHeaders([...])->contentType('application/vnd.api+json')` com o MESMO formato de header
    do `baseRequest()` (Authorization SEM prefixo "Bearer" — medido, §1 do empírico).

    **Guardas obrigatórias antes de qualquer requisição sair** (abortar com mensagem se falhar):
      1. `config('services.clicksign.env') === 'sandbox'`;
      2. `str_contains(config('services.clicksign.base_url'), 'sandbox.clicksign.com')`;
      3. o token usado é `services.clicksign.access_token` e NÃO qualquer variável de produção
         (`CLICKSIGN_PROD_TOKEN` e afins) — se `env` não for sandbox, o script simplesmente não roda.
    Nunca imprimir nem gravar o token, nem cabeçalho `Authorization`, nem e-mail de signatário real
    (anonimizar como `s1@…` ao registrar).

    **Orçamento duro: no máximo 4 requisições nesta task**, com pelo menos ~20s entre as tentativas
    de POST:
      - 1x `GET /envelopes/f010d235-ff75-400a-84b7-01cb89c3ef59/signers` — pegar um `signerId` real;
      - até 3x `POST /envelopes/{id}/signers/{signerId}/notifications`, uma tentativa de cada vez,
        **lendo a resposta antes de decidir a próxima** — a própria API nomeia o membro que falta em
        `errors[0].source.pointer` + `errors[0].detail`; seguir o que ELA diz, não um palpite.

    Ordem sugerida das tentativas (hipóteses a TESTAR, não verdades):
      1. `{"data":{"type":"notifications","attributes":{}}}` — mínimo JSON:API;
      2. o que o erro da tentativa 1 apontar (ex.: `type` inválido -> usar o `type` que o detalhe
         nomear; atributo faltando -> acrescentar SÓ ele);
      3. no máximo mais uma correção guiada pelo erro da tentativa 2.

    **Parar imediatamente** e considerar a medição encerrada se: (a) uma tentativa devolver 2xx, ou
    (b) qualquer tentativa devolver 429 (o limite anti-spam provou que o corpo passou do parser —
    registrar isso como achado, não insistir).

    Gravar cada tentativa (corpo enviado, status HTTP, corpo bruto da resposta, `code`/`pointer`/
    `detail` quando JSON:API) num arquivo `medicao-notificacao.md` **no scratchpad da sessão**. Esse
    arquivo é o insumo das Tasks 2 e 3.

    Apagar o script ao final; manter só o registro da medição.
  </action>
  <acceptance_criteria>
    - O `signerId` usado veio de um `GET /envelopes/{id}/signers` desta rodada, não de log antigo.
    - No máximo 4 requisições foram feitas contra a sandbox, nenhuma contra produção.
    - Para CADA tentativa existe registro de: corpo enviado, status HTTP e resposta bruta.
    - O resultado é conclusivo em um destes três estados, todos aceitáveis:
      (a) corpo que devolveu 2xx, com `type`/`attributes` exatos;
      (b) 429 anti-spam (corpo aceito pelo parser, limite é outro) — registrar qual corpo chegou até
          o 429;
      (c) erro JSON:API que nomeia explicitamente o que falta, com o corpo derivado dele registrado
          como "não confirmado por 2xx".
    - Nenhum token, header `Authorization` ou e-mail real aparece no arquivo de medição.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — medição contra API externa não é automatizável; a asserção automatizada
    nasce na Task 2. Checagem mecânica possível: o arquivo `medicao-notificacao.md` do scratchpad
    existe e contém uma linha de `status` HTTP para cada tentativa registrada, e
    `grep -i "authorization\|access_token"` nele não retorna nada.</automated>
  </verify>
  <done>Existe evidência bruta (status + corpo) de cada tentativa, e o corpo a implementar está
  determinado por resposta da API — não por dedução.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir reenviarNotificacao() e travar o CORPO enviado com teste</name>
  <read_first>
    - o `medicao-notificacao.md` produzido na Task 1 (fonte da forma do corpo)
    - `app/Services/Clicksign/ClicksignClient.php:460-512` (método atual)
    - `tests/Feature/Phase126/ClicksignClientEnvelopeTest.php:213-231` (teste do 429)
    - `tests/Fixtures/ClicksignSandboxFixtures.php:281-294` (`erro429NotificacaoTexto()`)
  </read_first>
  <files>
    app/Services/Clicksign/ClicksignClient.php,
    tests/Feature/Phase126/ClicksignClientEnvelopeTest.php,
    tests/Fixtures/ClicksignSandboxFixtures.php
  </files>
  <behavior>
    - Teste 1 (**o teste que faltava**): `reenviarNotificacao()` envia um corpo JSON:API com o
      membro `data` — `Http::assertSent()` inspecionando `$request->data()` / `$request['data']`,
      afirmando que `data` existe, que `data.type` é exatamente o valor MEDIDO na Task 1, e que os
      `attributes` medidos estão presentes. Um POST sem corpo tem de REPROVAR neste teste.
    - Teste 2 (regressão preservada): 429 continua não sendo retentado —
      `Http::assertSentCount(1)` e `ClicksignException` com `httpStatus === 429`, mesmo com resposta
      em TEXTO PURO (não JSON:API).
    - Teste 3: resposta 2xx devolve o bloco `data` desembrulhado (ou `[]` quando o corpo não tem
      `data` — o endpoint pode responder sem payload).
  </behavior>
  <action>
    Alterar `reenviarNotificacao()` para passar o corpo medido como segundo argumento do
    `->post($url, $corpo)`. **Manter tudo o que já está certo:**
      - continua FORA do `enviar()` comum (sem o `retry(3, ...)` — o 429 anti-spam não se retenta);
      - continua com o tratamento dedicado de 429 (mensagem pt-BR "aguarde alguns instantes");
      - continua com a decodificação defensiva (`$corpo = is_array($res->json()) ? ... : []`), porque
        a resposta de erro pode vir em texto puro;
      - continua logando só `contexto` + `status` no canal `ecf-webhooks` — NUNCA o corpo, que pode
        ecoar nome/e-mail do signatário (achado WR-11 da Fase 125).

    Se a Task 1 terminou no estado (b) 429 ou (c) erro que nomeia o campo — ou seja, sem 2xx —, o
    docblock DEVE dizer isso com todas as letras ("corpo derivado da mensagem da API, ainda não
    confirmado por 2xx"), no mesmo estilo dos avisos `⚠️ NÃO MEDIDO` que já existem no arquivo.
    Não escrever "MEDIDO" sobre o que não devolveu 2xx.

    Se a medição exigir fixture nova (ex.: resposta 2xx do endpoint), acrescentar em
    `ClicksignSandboxFixtures` seguindo o padrão do arquivo: dado anonimizado (`@example.com`,
    UUIDs fictícios) e docblock citando a seção do empírico de onde saiu.

    Comentários em pt-BR. Escopo fechado: **nenhum outro método do client é tocado.**
  </action>
  <acceptance_criteria>
    - `reenviarNotificacao()` envia corpo com membro `data` e `type` igual ao medido na Task 1.
    - Existe teste que falha se o corpo for removido — provar rodando a suíte com o segundo argumento
      do `post()` temporariamente retirado e conferindo que o teste novo REPROVA (e depois restaurar).
    - O teste de 429 existente continua verde e continua com `Http::assertSentCount(1)`.
    - `C:\xampp\php\php.exe artisan test --filter=Phase126` verde, sem regressão em nenhum teste.
    - Nenhum outro método de `ClicksignClient` foi alterado (`git diff` do arquivo restrito ao
      `reenviarNotificacao()` e seu docblock).
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase126</automated>
  </verify>
  <done>O método envia o corpo medido, o teste assevera o corpo (não só o status) e a suíte Phase126
  está verde.</done>
</task>

<task type="auto">
  <name>Task 3: Registrar o achado no CLICKSIGN-SANDBOX-EMPIRICO.md</name>
  <read_first>
    - `.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md` §7 (linha ~179, "Reenviar notificação tem
      rate limit próprio") e §13 (formato da última sessão de medição, linha ~620)
    - o `medicao-notificacao.md` da Task 1
  </read_first>
  <files>.planning/research/CLICKSIGN-SANDBOX-EMPIRICO.md</files>
  <action>
    Acrescentar uma seção `## 14. Sétima sessão — corpo do POST de notificação (quick 260814-d9s),
    2026-08-14` no mesmo formato das sessões anteriores: método, requisições feitas, e o bloco
    request/response verbatim de cada tentativa (com e-mails anonimizados).

    Registrar explicitamente:
      - que `POST /envelopes/{id}/signers/{id}/notifications` **exige o membro `data`** — um POST sem
        corpo devolve `data deve ser informado(a)`;
      - o `type` e os `attributes` medidos (ou, se não houve 2xx, o corpo derivado marcado como
        **NÃO CONFIRMADO POR 2xx**);
      - o comportamento observado do rate limit anti-spam nesta rodada (confirma ou refina §7).

    Acrescentar um ponteiro de uma linha na §7 apontando para a §14 nova, para quem ler o achado
    antigo primeiro não implementar POST vazio de novo.

    Não reescrever seções antigas — o arquivo é histórico de medição, é append.
  </action>
  <acceptance_criteria>
    - Existe seção nova numerada em sequência (§14) datada de 2026-08-14 citando o quick `260814-d9s`.
    - Cada afirmação da seção tem a resposta HTTP correspondente colada junto — nenhuma afirmação sem
      evidência.
    - O que não devolveu 2xx está rotulado como não confirmado.
    - §7 ganhou ponteiro para §14 e nenhuma seção anterior foi reescrita
      (`git diff` só mostra adição, fora a linha de ponteiro na §7).
    - Nenhum token, header `Authorization` ou e-mail real no texto.
  </acceptance_criteria>
  <verify>
    <automated>C:\xampp\php\php.exe artisan test --filter=Phase126</automated>
  </verify>
  <done>O empírico registra o corpo do endpoint de notificação; a próxima sessão não precisa medir de
  novo nem deduzir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| código local -> API Clicksign | credencial real atravessa aqui; sandbox e produção são o MESMO código com `.env` diferente |
| resposta da Clicksign -> log `ecf-webhooks` | a resposta de erro pode ecoar nome/e-mail de signatário (WR-11 da Fase 125) |

## Registro STRIDE

| Threat ID | Categoria | Componente | Disposição | Mitigação |
|-----------|-----------|------------|-----------|-----------|
| T-D9S-01 | Information Disclosure | script de sondagem da Task 1 | mitigate | nunca imprimir/gravar token nem header `Authorization`; e-mails anonimizados no `medicao-notificacao.md` e no empírico |
| T-D9S-02 | Tampering | chamada contra ambiente errado | mitigate | guardas duplas antes de qualquer requisição: `services.clicksign.env === 'sandbox'` E `base_url` contendo `sandbox.clicksign.com`; sem flag de produção neste quick |
| T-D9S-03 | Denial of Service | rate limit anti-spam do endpoint | mitigate | orçamento duro de 4 requisições, ~20s entre POSTs, parada obrigatória no primeiro 429 |
| T-D9S-04 | Information Disclosure | log de falha em `reenviarNotificacao()` | accept (já mitigado) | log segue restrito a `contexto` + `status`; nenhuma mudança que amplie o que é logado |
</threat_model>

<verification>
- `C:\xampp\php\php.exe artisan test --filter=Phase126` verde (PHP não está no PATH — usar o caminho
  completo).
- **NÃO rodar a suíte completa sem filtro** — há um ponto pré-existente de timeout no
  `MercadoLivreAdsService`, não relacionado a este quick.
- `git diff app/Services/Clicksign/ClicksignClient.php` restrito a `reenviarNotificacao()`.
</verification>

<success_criteria>
- `reenviarNotificacao()` envia corpo JSON:API com o membro `data` e para de receber
  `data deve ser informado(a)`.
- Existe teste que REPROVA se o corpo voltar a ser vazio — a lacuna que deixou o bug passar está
  fechada.
- O tratamento do 429 anti-spam (sem retry, mensagem pt-BR, resposta em texto puro) segue intacto.
- O corpo está registrado no `CLICKSIGN-SANDBOX-EMPIRICO.md` com a evidência HTTP colada.
- Suíte `Phase126` sem regressão.
</success_criteria>

<fora_de_escopo>
- Nenhum outro método do `ClicksignClient`.
- Fase 130 (gates SC1/SC2/SC3) — não tocar.
- O e-mail que não chega na sandbox — outro assunto, já documentado pela Fase 126/130.
- Tela de reenvio (CLICK-07, Fase 131).
</fora_de_escopo>

<output>
Criar `.planning/quick/260814-d9s-corrigir-reenviarnotificacao-do-clicksig/260814-d9s-SUMMARY.md` ao
concluir.
</output>
