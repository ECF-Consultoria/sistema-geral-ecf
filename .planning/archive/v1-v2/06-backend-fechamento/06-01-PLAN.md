---
phase: 06-backend-fechamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/AdminController.php
  - tests/Feature/AdminFechamentoControllerTest.php
  - tests/Unit/CalcularFaixaTest.php
autonomous: true
requirements:
  - FCH-04
  - FCH-05

must_haves:
  truths:
    - "O método privado calcularFaixa(float $faturamento) existe em AdminController e retorna array ['faixa' => string, 'valor' => float]"
    - "A constante FAIXAS define as 7 bandas com labels ate_499k, 500k_999k, 1m_1999k, 2m_2999k, 3m_3999k, 4m_4999k e a banda maxima"
    - "calcularFaixa(300000) retorna ['faixa' => 'ate_499k', 'valor' => 3000.0]"
    - "calcularFaixa(5500000) retorna ['faixa' => 'maxima', 'valor' => 12000.0]"
    - "8 novos métodos de teste existem em AdminFechamentoControllerTest sem modificar os 8 testes existentes"
    - "Os 8 novos testes estão RED antes da expansão de fechamento() no Plan 02"
  artifacts:
    - path: "app/Http/Controllers/AdminController.php"
      provides: "calcularFaixa() + constante FAIXAS adicionados; fechamento() permanece inalterado neste plano"
      contains: "calcularFaixa"
    - path: "tests/Unit/CalcularFaixaTest.php"
      provides: "Testes unitários GREEN para calcularFaixa() — todas as 7 faixas + limites exatos"
      contains: "CalcularFaixaTest"
    - path: "tests/Feature/AdminFechamentoControllerTest.php"
      provides: "8 novos métodos de teste para FCH-04 e FCH-05 (RED até Plan 02)"
      contains: "test_empresa_ok_recebe_faturamento_somado"
  key_links:
    - from: "app/Http/Controllers/AdminController.php"
      to: "tests/Unit/CalcularFaixaTest.php"
      via: "calcularFaixa() chamado via reflexão ou método público temporário nos testes unitários"
      pattern: "calcularFaixa"
    - from: "tests/Feature/AdminFechamentoControllerTest.php"
      to: "app/Http/Controllers/AdminController.php"
      via: "GET /administrativo/financeiro via actingAs + assertInertia sobre prop companies"
      pattern: "companies\\.0\\.faturamento"
---

<objective>
Adicionar o método privado calcularFaixa() e a constante FAIXAS ao AdminController, validar com testes unitários GREEN, e criar os 8 novos testes de feature (RED) que cobrem FCH-04 e FCH-05 — prontos para ficar GREEN no Plan 02.

Purpose: Isolar a lógica pura de faixa (sem banco de dados) para que seja testável e validada antes de qualquer mudança na query de fechamento(). Os testes de feature RED servem como contrato do que o Plan 02 precisa entregar.
Output: AdminController com calcularFaixa() + FAIXAS; CalcularFaixaTest GREEN; 8 novos testes de feature RED em AdminFechamentoControllerTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-backend-fechamento/06-CONTEXT.md
@.planning/phases/06-backend-fechamento/06-RESEARCH.md

<interfaces>
<!-- Estado atual de AdminController.php — lido na sessão de planejamento. -->

Namespace e imports atuais (AdminController.php):
```php
namespace App\Http\Controllers;

use App\Models\Company;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Validator;
use Inertia\Inertia;
```

Estado atual de fechamento() (linhas 22-38) — NÃO ALTERAR neste plano:
```php
public function fechamento()
{
    $companies = Company::where('active', true)
        ->orderBy('name')
        ->get()
        ->map(fn (Company $c) => [
            'id'                 => $c->id,
            'name'               => $c->name,
            'service_type'       => $c->service_type,
            'contract_start'     => $c->contract_start?->toDateString(),
            'contract_end'       => $c->contract_end?->toDateString(),
            'additional_service' => $c->additional_service,
            'has_adman'          => (bool) $c->adman_account_id,
        ]);

    return Inertia::render('Admin/Financeiro', compact('companies'));
}
```

Estado atual de updateFechamento() — NÃO ALTERAR em nenhum plano da Phase 6:
```php
public function updateFechamento(Request $request, Company $company)
{
    $validator = Validator::make($request->all(), [
        'service_type'   => 'nullable|in:polo,assessoria,incubadora',
        'contract_start' => 'nullable|date',
        'contract_end'   => 'nullable|date|after_or_equal:contract_start',
    ]);

    if ($validator->fails()) {
        return response()->json(['errors' => $validator->errors()], 422);
    }

    $company->update($validator->validated());

    return back()->with('success', 'Fechamento atualizado.');
}
```

AdmanMetric.$fillable inclui: company_id, reference_date, revenue (decimal:2)
AdmanMetric.$casts: reference_date => 'date', revenue => 'decimal:2'

Padrão de seeding nos testes (DevControllerTest.php):
```php
AdmanMetric::create([
    'company_id'     => $company->id,
    'reference_date' => now()->toDateString(),
    'synced_at'      => now(),
    'raw_data'       => ['x' => 1],
]);
```
Nota: synced_at e raw_data são required pelo $fillable do AdmanMetric — incluir no create().

Padrão criarAdmin() do AdminFechamentoControllerTest.php (existente):
```php
private function criarAdmin(): User
{
    return User::factory()->create(['role' => 'admin']);
}
```

Padrão match (true) do projeto (DashboardController.php:52):
```php
return match ($period) {
    '1'   => now()->subDay(),
    default => now()->subDays(30),
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calcularFaixa() + constante FAIXAS no AdminController</name>
  <files>app/Http/Controllers/AdminController.php</files>
  <behavior>
    - calcularFaixa(0.0) retorna ['faixa' => 'ate_499k', 'valor' => 3000.0]
    - calcularFaixa(300000.0) retorna ['faixa' => 'ate_499k', 'valor' => 3000.0]
    - calcularFaixa(499999.99) retorna ['faixa' => 'ate_499k', 'valor' => 3000.0]
    - calcularFaixa(500000.0) retorna ['faixa' => '500k_999k', 'valor' => 4500.0]
    - calcularFaixa(700000.0) retorna ['faixa' => '500k_999k', 'valor' => 4500.0]
    - calcularFaixa(999999.99) retorna ['faixa' => '500k_999k', 'valor' => 4500.0]
    - calcularFaixa(1000000.0) retorna ['faixa' => '1m_1999k', 'valor' => 6000.0]
    - calcularFaixa(1500000.0) retorna ['faixa' => '1m_1999k', 'valor' => 6000.0]
    - calcularFaixa(1999999.99) retorna ['faixa' => '1m_1999k', 'valor' => 6000.0]
    - calcularFaixa(2000000.0) retorna ['faixa' => '2m_2999k', 'valor' => 7500.0]
    - calcularFaixa(2400000.0) retorna ['faixa' => '2m_2999k', 'valor' => 7500.0]
    - calcularFaixa(2999999.99) retorna ['faixa' => '2m_2999k', 'valor' => 7500.0]
    - calcularFaixa(3000000.0) retorna ['faixa' => '3m_3999k', 'valor' => 9000.0]
    - calcularFaixa(3800000.0) retorna ['faixa' => '3m_3999k', 'valor' => 9000.0]
    - calcularFaixa(3999999.99) retorna ['faixa' => '3m_3999k', 'valor' => 9000.0]
    - calcularFaixa(4000000.0) retorna ['faixa' => '4m_4999k', 'valor' => 10500.0]
    - calcularFaixa(4300000.0) retorna ['faixa' => '4m_4999k', 'valor' => 10500.0]
    - calcularFaixa(4999999.99) retorna ['faixa' => '4m_4999k', 'valor' => 10500.0]
    - calcularFaixa(5000000.0) retorna ['faixa' => 'maxima', 'valor' => 12000.0]
    - calcularFaixa(6000000.0) retorna ['faixa' => 'maxima', 'valor' => 12000.0]
  </behavior>
  <action>
    Editar app/Http/Controllers/AdminController.php adicionando dois elementos à classe — uma constante e um método privado — sem modificar nenhum método existente.

    1. Adicionar a constante FAIXAS como private const logo após a declaração da classe (antes do método empresas()), com as 6 bandas indexadas (a sétima é o fallback maxima):

    ```
    private const FAIXAS = [
        ['limite' => 499_999.99,   'label' => 'ate_499k',   'valor' => 3_000.00],
        ['limite' => 999_999.99,   'label' => '500k_999k',  'valor' => 4_500.00],
        ['limite' => 1_999_999.99, 'label' => '1m_1999k',   'valor' => 6_000.00],
        ['limite' => 2_999_999.99, 'label' => '2m_2999k',   'valor' => 7_500.00],
        ['limite' => 3_999_999.99, 'label' => '3m_3999k',   'valor' => 9_000.00],
        ['limite' => 4_999_999.99, 'label' => '4m_4999k',   'valor' => 10_500.00],
    ];
    ```

    2. Adicionar o método privado calcularFaixa() APÓS o método inventario() (último método da classe), com PHPDoc docblock conforme convenção do projeto para métodos com retorno não-óbvio:

    ```
    /**
     * Retorna a faixa de investimento para um faturamento mensal.
     * Tabela de progressão definida em faturamento_adm.md.
     *
     * @return array{faixa: string, valor: float}
     */
    private function calcularFaixa(float $faturamento): array
    {
        foreach (self::FAIXAS as $faixa) {
            if ($faturamento <= $faixa['limite']) {
                return ['faixa' => $faixa['label'], 'valor' => (float) $faixa['valor']];
            }
        }
        // Faixa máxima — D-09
        return ['faixa' => 'maxima', 'valor' => 12_000.00];
    }
    ```

    NÃO alterar os métodos empresas(), relatorio(), fechamento(), updateFechamento(), inventario().
    NÃO adicionar novos use imports — calcularFaixa() não depende de classes externas.
  </action>
  <verify>
    <automated>php artisan test --filter=CalcularFaixaTest 2>&1 | tail -8</automated>
  </verify>
  <done>AdminController.php contém private const FAIXAS e private function calcularFaixa(); php artisan test --filter=CalcularFaixaTest passa em GREEN (os testes são criados na Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CalcularFaixaTest — testes unitários GREEN + stubs de feature RED</name>
  <files>
    tests/Unit/CalcularFaixaTest.php
    tests/Feature/AdminFechamentoControllerTest.php
  </files>
  <behavior>
    CalcularFaixaTest (Unit — deve ficar GREEN imediatamente após Task 1):
    - test_faturamento_zero_retorna_ate_499k: calcularFaixaPublic(0.0) == ['faixa' => 'ate_499k', 'valor' => 3000.0]
    - test_limite_exato_499k_retorna_ate_499k: calcularFaixaPublic(499999.99) == ['faixa' => 'ate_499k', 'valor' => 3000.0]
    - test_500k_exato_retorna_500k_999k: calcularFaixaPublic(500000.0) == ['faixa' => '500k_999k', 'valor' => 4500.0]
    - test_1m_exato_retorna_1m_1999k: calcularFaixaPublic(1000000.0) == ['faixa' => '1m_1999k', 'valor' => 6000.0]
    - test_2m_exato_retorna_2m_2999k: calcularFaixaPublic(2000000.0) == ['faixa' => '2m_2999k', 'valor' => 7500.0]
    - test_3m_exato_retorna_3m_3999k: calcularFaixaPublic(3000000.0) == ['faixa' => '3m_3999k', 'valor' => 9000.0]
    - test_4m_exato_retorna_4m_4999k: calcularFaixaPublic(4000000.0) == ['faixa' => '4m_4999k', 'valor' => 10500.0]
    - test_5m_exato_retorna_maxima: calcularFaixaPublic(5000000.0) == ['faixa' => 'maxima', 'valor' => 12000.0]
    - test_acima_5m_retorna_maxima: calcularFaixaPublic(6000000.0) == ['faixa' => 'maxima', 'valor' => 12000.0]

    AdminFechamentoControllerTest adições (Feature — devem estar RED após este plano, GREEN após Plan 02):
    - test_empresa_ok_recebe_faturamento_somado: empresa com adman_account_id + 2 AdmanMetric no mês corrente (500k + 500k) → companies.0.faturamento == 1000000.0, companies.0.estado == 'ok'
    - test_empresa_ok_recebe_periodo_coberto: empresa com 2 métricas (1° e 15° do mês corrente) → periodo_inicio == '01/05', periodo_fim == '15/05' (ou mês corrente equivalente)
    - test_empresa_sem_dados_recebe_estado_sem_dados: empresa com adman_account_id mas sem AdmanMetric no mês → estado == 'sem_dados', faturamento == null, faixa == null
    - test_empresa_sem_adman_recebe_estado_sem_integracao: empresa sem adman_account_id → estado == 'sem_integracao', faturamento == null
    - test_fatura_ate_499k_retorna_faixa_correta: empresa com SUM revenue = 300000 → faixa == 'ate_499k', valor_mensal == 3000.0
    - test_fatura_500k_999k_retorna_faixa_correta: empresa com SUM revenue = 700000 → faixa == '500k_999k', valor_mensal == 4500.0
    - test_fatura_acima_5m_retorna_maxima: empresa com SUM revenue = 5500000 → faixa == 'maxima', valor_mensal == 12000.0
    - test_metrica_fora_do_mes_nao_conta: empresa com AdmanMetric no mês ANTERIOR (sem nenhuma no mês corrente) → estado == 'sem_dados'
  </behavior>
  <action>
    Criar tests/Unit/CalcularFaixaTest.php:

    Namespace Tests\Unit. Use Tests\TestCase. Classe CalcularFaixaTest extends TestCase.
    Como calcularFaixa() é private, expô-la via ReflectionMethod para os testes unitários:

    ```php
    private function calcularFaixaPublic(float $faturamento): array
    {
        $controller = new \App\Http\Controllers\AdminController();
        $method     = new \ReflectionMethod($controller, 'calcularFaixa');
        $method->setAccessible(true);
        return $method->invoke($controller, $faturamento);
    }
    ```

    Implementar os 9 métodos de teste listados na behavior. Cada método:
    - Chama $this->calcularFaixaPublic($valor)
    - Usa $this->assertSame(['faixa' => '...', 'valor' => X.0], $resultado) — assertSame verifica tipo e valor
    - Sem RefreshDatabase — testes unitários puros, sem banco

    Adicionar os 8 novos métodos ao AdminFechamentoControllerTest.php APÓS o último método existente (test_nao_admin_recebe_403 na linha 128), sem alterar nenhum dos 8 métodos existentes.

    Novos use imports necessários no AdminFechamentoControllerTest.php:
    - use App\Models\AdmanMetric; (adicionar ao bloco use existente)
    - use Carbon\Carbon; (adicionar ao bloco use existente)

    Padrão de seeding de AdmanMetric nos novos testes — incluir synced_at e raw_data (campos sem default no schema):

    Para empresa ok com faturamento 1M:
    ```php
    $company = Company::create(['name' => 'Empresa OK', 'cnpj' => '10000000000001', 'active' => true, 'adman_account_id' => 'ACC001']);
    AdmanMetric::create(['company_id' => $company->id, 'reference_date' => Carbon::now()->startOfMonth()->toDateString(), 'revenue' => 500000.00, 'synced_at' => now(), 'raw_data' => []]);
    AdmanMetric::create(['company_id' => $company->id, 'reference_date' => Carbon::now()->toDateString(),                  'revenue' => 500000.00, 'synced_at' => now(), 'raw_data' => []]);
    ```

    Para test_empresa_ok_recebe_periodo_coberto — criar 2 métricas com datas distintas no mês corrente e comparar usando Carbon::parse() para evitar hardcode de mês:
    ```php
    $primeiroDoMes = Carbon::now()->startOfMonth();
    $dia15         = Carbon::now()->startOfMonth()->addDays(14);
    // Criar métricas com essas datas
    // Assert: periodo_inicio == $primeiroDoMes->format('d/m')
    //         periodo_fim    == $dia15->format('d/m')
    ```
    Atenção: se hoje for dia 1, o dia 15 ainda não ocorreu no mês corrente — use Carbon::now()->startOfMonth() e Carbon::now() como as duas datas de referência se Carbon::now()->day < 15. Simplificação: usar apenas startOfMonth() e subDay() para garantir que ambas estejam no mês corrente independentemente do dia atual:
    ```php
    $inicio = Carbon::now()->startOfMonth();
    $fim    = Carbon::now()->day > 1 ? Carbon::now() : Carbon::now()->startOfMonth();
    ```
    Alternativa mais robusta: criar as duas métricas com startOfMonth() e hoje, e asserir usando Carbon::now()->startOfMonth()->format('d/m') e Carbon::now()->format('d/m').

    Para test_metrica_fora_do_mes_nao_conta — criar métrica com reference_date no mês anterior:
    ```php
    AdmanMetric::create(['company_id' => $company->id, 'reference_date' => Carbon::now()->subMonth()->toDateString(), 'revenue' => 1000000.00, 'synced_at' => now(), 'raw_data' => []]);
    ```

    Os 8 novos testes de feature estarão RED (falham com "key faturamento not found" ou similar) até o Plan 02 expandir fechamento() — este é o estado esperado.
  </action>
  <verify>
    <automated>php artisan test --filter=CalcularFaixaTest 2>&1 | tail -6</automated>
  </verify>
  <done>CalcularFaixaTest: 9 testes GREEN. AdminFechamentoControllerTest: os 8 testes originais continuam GREEN; os 8 novos testes estão presentes (podem estar RED — esperado). php artisan test --filter=AdminFechamentoControllerTest não quebra os 8 originais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lógica de faixa | calcularFaixa() recebe float de origem do banco — cast explícito em Plan 02 garante tipo correto |
| Rota admin | EnsureUserHasRole já aplicado no grupo /administrativo/* — sem mudança neste plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | AdminController.FAIXAS | accept | Constante PHP privada; não exposta via UI nem editável em runtime; mudança requer deploy |
| T-06-02 | Elevation of Privilege | GET /administrativo/financeiro | accept | EnsureUserHasRole middleware já ativo no grupo de rotas — sem alteração necessária |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — sem risco de supply chain |
</threat_model>

<verification>
Plano completo quando:
- php artisan test --filter=CalcularFaixaTest → 9 testes GREEN
- php artisan test --filter=AdminFechamentoControllerTest → 8 testes originais GREEN (novos podem estar RED)
- AdminController.php contém 'calcularFaixa' e 'FAIXAS'
- tests/Unit/CalcularFaixaTest.php existe
- Nenhum dos 8 testes existentes em AdminFechamentoControllerTest foi modificado
</verification>

<success_criteria>
- private const FAIXAS com 6 entradas existe em AdminController
- private function calcularFaixa(float $faturamento): array existe em AdminController
- CalcularFaixaTest: 9 testes GREEN cobrindo todos os limites de faixa
- AdminFechamentoControllerTest: 8 testes originais GREEN + 8 novos métodos adicionados (RED até Plan 02)
- php artisan test não introduz regressões nos 40 testes existentes
</success_criteria>

<output>
Criar `.planning/phases/06-backend-fechamento/06-01-SUMMARY.md` ao concluir.
</output>
