---
phase: 06-backend-fechamento
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/Http/Controllers/AdminController.php
autonomous: true
requirements:
  - FCH-04
  - FCH-05

must_haves:
  truths:
    - "fechamento() executa SUM(revenue) GROUP BY company_id para o mês corrente sem chamada HTTP à API Adman"
    - "Período coberto calculado de MIN/MAX(reference_date) dos registros do mês corrente — ex: '01/05' a '18/05'"
    - "Empresa com adman_account_id mas sem registros no mês corrente recebe estado 'sem_dados' e faturamento null"
    - "Empresa sem adman_account_id recebe estado 'sem_integracao' e faturamento null"
    - "Empresa com dados recebe estado 'ok' + faturamento float + faixa string + valor_mensal float + periodo_inicio string + periodo_fim string"
    - "Todos os 16 testes em AdminFechamentoControllerTest estão GREEN (8 originais + 8 novos)"
  artifacts:
    - path: "app/Http/Controllers/AdminController.php"
      provides: "fechamento() expandido com aggregation query + mapeamento de estado + campos FCH-04/FCH-05"
      contains: "selectRaw.*SUM.*revenue.*faturamento"
  key_links:
    - from: "app/Http/Controllers/AdminController.php"
      to: "adman_metrics table"
      via: "AdmanMetric::whereBetween(reference_date)->selectRaw(SUM/MIN/MAX)->groupBy(company_id)->get()->keyBy(company_id)"
      pattern: "AdmanMetric::whereBetween"
    - from: "app/Http/Controllers/AdminController.php"
      to: "resources/js/Pages/Admin/Financeiro.jsx"
      via: "Inertia::render('Admin/Financeiro', compact('companies')) com campos novos nas props"
      pattern: "Inertia::render.*Financeiro"
---

<objective>
Expandir AdminController::fechamento() para incluir a aggregation query sobre adman_metrics e entregar os campos FCH-04 (faturamento + período) e FCH-05 (faixa + valor_mensal + estado) nas props Inertia. Todos os 8 novos testes criados no Plan 01 devem ficar GREEN.

Purpose: Tornar os dados de faturamento mensal disponíveis no frontend sem nenhuma chamada HTTP adicional — apenas leitura do banco já sincronizado pelo job Adman.
Output: AdminController.php com fechamento() expandido; 16/16 testes AdminFechamentoControllerTest GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-backend-fechamento/06-CONTEXT.md
@.planning/phases/06-backend-fechamento/06-RESEARCH.md
@.planning/phases/06-backend-fechamento/06-01-SUMMARY.md

<interfaces>
<!-- Contratos que este plano deve implementar. Extraídos do Plan 01 e do estado atual do codebase. -->

Imports a adicionar em AdminController.php (apenas os ausentes):
```php
use App\Models\AdmanMetric;
use Carbon\Carbon;
```

Assinatura de calcularFaixa() — implementada no Plan 01, apenas referenciada aqui:
```php
private function calcularFaixa(float $faturamento): array
// Retorna: ['faixa' => string, 'valor' => float]
// Faixas: ate_499k (3000), 500k_999k (4500), 1m_1999k (6000),
//         2m_2999k (7500), 3m_3999k (9000), 4m_4999k (10500), maxima (12000)
```

Pattern canônico de aggregation (verificado em MlbController.php:371-372 e CONTEXT.md):
```php
$metricas = AdmanMetric::whereBetween('reference_date', [$inicio, $fim])
    ->whereNotNull('revenue')
    ->selectRaw('company_id, SUM(revenue) as faturamento, MIN(reference_date) as periodo_inicio, MAX(reference_date) as periodo_fim')
    ->groupBy('company_id')
    ->get()
    ->keyBy('company_id');
```

Carbon date formatting — resultado de selectRaw é string, não Carbon (Pitfall #3 do RESEARCH.md):
```php
Carbon::parse($metrica->periodo_inicio)->format('d/m')
// NÃO usar: $metrica->periodo_inicio->format('d/m') — selectRaw não passa pelo cast do Model
```

Cast explícito do faturamento — MySQL/MariaDB retorna SUM() como string (Pitfall #4):
```php
(float) $metrica->faturamento
// NÃO usar: $metrica->faturamento diretamente como float
```

Props shape alvo por empresa (D-12):
```
'id'                 => int
'name'               => string
'service_type'       => string|null
'contract_start'     => string|null  (ISO date via ?->toDateString())
'contract_end'       => string|null
'additional_service' => string|null
'has_adman'          => bool
'estado'             => 'sem_integracao' | 'sem_dados' | 'ok'
'faturamento'        => float|null   (null se estado != 'ok')
'periodo_inicio'     => string|null  (formato 'd/m', ex: '01/05')
'periodo_fim'        => string|null  (formato 'd/m', ex: '18/05')
'faixa'              => string|null  (null se estado != 'ok')
'valor_mensal'       => float|null   (null se estado != 'ok')
```

Testes que devem ficar GREEN neste plano (adicionados no Plan 01):
- test_empresa_ok_recebe_faturamento_somado
- test_empresa_ok_recebe_periodo_coberto
- test_empresa_sem_dados_recebe_estado_sem_dados
- test_empresa_sem_adman_recebe_estado_sem_integracao
- test_fatura_ate_499k_retorna_faixa_correta
- test_fatura_500k_999k_retorna_faixa_correta
- test_fatura_acima_5m_retorna_maxima
- test_metrica_fora_do_mes_nao_conta
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expandir fechamento() com aggregation query + estado + campos FCH-04/FCH-05</name>
  <files>app/Http/Controllers/AdminController.php</files>
  <behavior>
    - GET /administrativo/financeiro com empresa ok (adman_account_id + 2 métricas no mês) → companies.0.faturamento == SUM dos revenues, companies.0.estado == 'ok'
    - GET /administrativo/financeiro com empresa ok → companies.0.periodo_inicio e periodo_fim no formato 'd/m'
    - GET /administrativo/financeiro com empresa sem adman_account_id → companies.0.estado == 'sem_integracao', companies.0.faturamento == null
    - GET /administrativo/financeiro com empresa integrada sem métricas no mês → companies.0.estado == 'sem_dados', companies.0.faturamento == null
    - GET /administrativo/financeiro com empresa faturando 300k → companies.0.faixa == 'ate_499k', companies.0.valor_mensal == 3000.0
    - GET /administrativo/financeiro com empresa faturando 700k → companies.0.faixa == '500k_999k', companies.0.valor_mensal == 4500.0
    - GET /administrativo/financeiro com empresa faturando 5.5M → companies.0.faixa == 'maxima', companies.0.valor_mensal == 12000.0
    - Métrica com reference_date no mês anterior não é incluída no SUM → empresa aparece com estado 'sem_dados'
  </behavior>
  <action>
    Editar app/Http/Controllers/AdminController.php aplicando as seguintes mudanças:

    1. Adicionar imports ao bloco use (após os existentes, em ordem alfabética de classe):
       - use App\Models\AdmanMetric;
       - use Carbon\Carbon;
       Não duplicar use App\Models\Company que já existe.

    2. Substituir o método fechamento() pelo corpo expandido abaixo. Os outros métodos (empresas, relatorio, updateFechamento, inventario) NÃO devem ser tocados.

    Novo corpo de fechamento():
    ```php
    public function fechamento()
    {
        $inicio   = Carbon::now()->startOfMonth();
        $fim      = Carbon::now();

        // Agrega faturamento e período coberto por empresa no mês corrente — D-01, D-02, D-03
        $metricas = AdmanMetric::whereBetween('reference_date', [$inicio, $fim])
            ->whereNotNull('revenue')
            ->selectRaw('company_id, SUM(revenue) as faturamento, MIN(reference_date) as periodo_inicio, MAX(reference_date) as periodo_fim')
            ->groupBy('company_id')
            ->get()
            ->keyBy('company_id');

        $companies = Company::where('active', true)
            ->orderBy('name')
            ->get()
            ->map(function (Company $c) use ($metricas) {
                $hasAdman = (bool) $c->adman_account_id;
                $metrica  = $metricas->get($c->id);

                // Determina estado da empresa — D-05
                $estado = match (true) {
                    !$hasAdman => 'sem_integracao',
                    !$metrica  => 'sem_dados',
                    default    => 'ok',
                };

                // Calcula faixa apenas para empresas com dados — D-06, D-10
                $faixaData = ($estado === 'ok')
                    ? $this->calcularFaixa((float) $metrica->faturamento)
                    : null;

                return [
                    'id'                 => $c->id,
                    'name'               => $c->name,
                    'service_type'       => $c->service_type,
                    'contract_start'     => $c->contract_start?->toDateString(),
                    'contract_end'       => $c->contract_end?->toDateString(),
                    'additional_service' => $c->additional_service,
                    'has_adman'          => $hasAdman,
                    'estado'             => $estado,
                    'faturamento'        => $metrica ? (float) $metrica->faturamento : null,
                    'periodo_inicio'     => $metrica ? Carbon::parse($metrica->periodo_inicio)->format('d/m') : null,
                    'periodo_fim'        => $metrica ? Carbon::parse($metrica->periodo_fim)->format('d/m') : null,
                    'faixa'              => $faixaData['faixa']  ?? null,
                    'valor_mensal'       => $faixaData['valor']  ?? null,
                ];
            });

        return Inertia::render('Admin/Financeiro', compact('companies'));
    }
    ```

    Pitfalls a evitar (per RESEARCH.md):
    - Carbon mutation: $inicio e $fim são variáveis separadas — não derivar $fim de $inicio
    - selectRaw retorna string: sempre usar (float) antes de passar faturamento para calcularFaixa()
    - selectRaw retorna string para datas: sempre usar Carbon::parse() antes de ->format('d/m')
    - NÃO usar $metrica->periodo_inicio->format() diretamente — selectRaw não passa pelo $casts do Model
    - revenue null: o whereNotNull('revenue') já exclui registros sem valor de billing real
    - Manter o padrão de alinhamento de colunas com espaços (estilo do projeto)
  </action>
  <verify>
    <automated>php artisan test --filter=AdminFechamentoControllerTest 2>&1 | tail -8</automated>
  </verify>
  <done>AdminFechamentoControllerTest: 16/16 testes GREEN (8 originais + 8 novos). php artisan test suite completa: zero regressões além do 1 ExampleTest pré-existente.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação da suite completa</name>
  <files></files>
  <action>
    Executar a suite completa de testes para confirmar que nenhuma regressão foi introduzida pelas mudanças da Phase 6.

    Sequência de verificação:
    1. php artisan test --filter=AdminFechamentoControllerTest — deve mostrar 16 testes GREEN
    2. php artisan test --filter=CalcularFaixaTest — deve mostrar 9 testes GREEN
    3. php artisan test — suite completa; resultado esperado: todos os testes GREEN exceto ExampleTest (1 falha pré-existente de 302 vs 200 em /)

    Se algum teste além do ExampleTest falhar:
    - Ler a mensagem de erro completa
    - Identificar se é regressão introduzida nesta fase (falha em teste de phase anterior) ou falha esperada
    - Corrigir antes de prosseguir — não criar SUMMARY enquanto houver regressão

    Não há mudanças de arquivo nesta task — é apenas verificação. Se tudo passar, prosseguir para criar o SUMMARY.
  </action>
  <verify>
    <automated>php artisan test 2>&1 | tail -10</automated>
  </verify>
  <done>Suite completa executa sem regressões: todos os testes GREEN exceto o ExampleTest pré-existente. AdminFechamentoControllerTest: 16/16. CalcularFaixaTest: 9/9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Query selectRaw | String estática sem interpolação de variáveis — sem risco de SQL injection |
| Props Inertia | Dados vindos do banco (adman_metrics) entregues ao frontend — nenhum dado de usuário externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | AdmanMetric::selectRaw | accept | selectRaw usa string literal sem interpolação de variáveis; whereBetween usa parâmetros bindados pelo Eloquent — sem SQL injection |
| T-06-04 | Information Disclosure | Props Inertia companies[] | accept | Rota protegida por EnsureUserHasRole (admin); dados financeiros expostos apenas para role admin — sem exposição indevida |
| T-06-05 | Elevation of Privilege | GET /administrativo/financeiro | accept | EnsureUserHasRole middleware ativo no grupo de rotas — sem alteração necessária em Phase 6 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — sem risco de supply chain |
</threat_model>

<verification>
Plano completo quando:
- php artisan test --filter=AdminFechamentoControllerTest → 16/16 GREEN
- php artisan test --filter=CalcularFaixaTest → 9/9 GREEN
- php artisan test → suite completa sem regressões (exceto ExampleTest pré-existente)
- AdminController.php contém: use App\Models\AdmanMetric; use Carbon\Carbon; AdmanMetric::whereBetween; selectRaw; keyBy; calcularFaixa
- Nenhum dos métodos updateFechamento(), empresas(), relatorio(), inventario() foi alterado
</verification>

<success_criteria>
- fechamento() entrega estado, faturamento, periodo_inicio, periodo_fim, faixa, valor_mensal para cada empresa
- Empresa sem adman_account_id: estado = 'sem_integracao', campos financeiros = null
- Empresa integrada sem métricas no mês: estado = 'sem_dados', campos financeiros = null
- Empresa com dados: estado = 'ok', faturamento = SUM(revenue), faixa e valor_mensal calculados por calcularFaixa()
- 16 testes AdminFechamentoControllerTest GREEN; 9 testes CalcularFaixaTest GREEN
- Suite completa sem regressões além do ExampleTest pré-existente
</success_criteria>

<output>
Criar `.planning/phases/06-backend-fechamento/06-02-SUMMARY.md` ao concluir.
</output>
