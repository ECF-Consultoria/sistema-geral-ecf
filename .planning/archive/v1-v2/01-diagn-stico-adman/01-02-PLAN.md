---
phase: 01-diagn-stico-adman
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - app/Http/Controllers/DevController.php
  - app/Services/AdmanService.php
  - routes/web.php
autonomous: true
requirements: [DEV-01, DEV-02, DEV-03, DEV-04]
tags: [laravel, inertia, controller, adman, diagnostico, job-dispatch]

must_haves:
  truths:
    - "GET /dev/desenvolvimento por um admin retorna 200 e a prop empresas[] vem com synced_at, raw_data, criados, atualizados, ignorados, error, status para cada Company com adman_account_id preenchido"
    - "POST /dev/adman/{company}/sync por um admin enfileira SyncAdmanCompanyJob e redireciona com flash success contendo o nome da empresa"
    - "POST /dev/adman/{company}/sync por um consultor retorna HTTP 403"
    - "AdmanService::syncCompany() grava uma linha em adman_sync_logs após cada updateOrCreate (sucesso ou erro), com created_count/updated_count/skipped_count derivados de wasRecentlyCreated/wasChanged"
    - "Os 5 testes de DevControllerTest viram verdes ao final desta plan"
  artifacts:
    - path: "app/Http/Controllers/DevController.php"
      provides: "Controller com index() e dispatchSync()"
      exports: ["index", "dispatchSync"]
      contains: "class DevController extends Controller"
    - path: "app/Services/AdmanService.php"
      provides: "syncCompany() modificado para registrar AdmanSyncLog (sucesso e erro)"
      contains: "AdmanSyncLog::create"
    - path: "routes/web.php"
      provides: "GET /dev/desenvolvimento via DevController + POST /dev/adman/{company}/sync"
      contains: "dev.adman.sync"
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Controllers/DevController.php"
      via: "Route::get + Route::post"
      pattern: "DevController::class"
    - from: "app/Http/Controllers/DevController.php"
      to: "app/Jobs/SyncAdmanCompanyJob.php"
      via: "SyncAdmanCompanyJob::dispatch(\\$company)"
      pattern: "SyncAdmanCompanyJob::dispatch"
    - from: "app/Services/AdmanService.php"
      to: "app/Models/AdmanSyncLog.php"
      via: "AdmanSyncLog::create([...])"
      pattern: "AdmanSyncLog::create"
---

## Phase Goal

**As a** admin do ECF Admin, **I want to** inspecionar e disparar o sync Adman de cada empresa direto no painel, **so that** eu identifique falhas e force atualizações sem precisar de acesso ao servidor.

<objective>
Segunda fatia vertical MVP — entrega o backend end-to-end completo da Phase 1.

Esta plan cria o DevController, migra a rota /dev/desenvolvimento de closure para controller,
adiciona a rota POST de dispatch e instrumenta AdmanService::syncCompany() para gravar logs
de diff em adman_sync_logs. Ao final desta plan, todos os 5 testes feature da Plan 01 viram
verdes (GREEN state) — todos os requisitos DEV-01/02/03/04 estão entregues no backend, e a
seção UI da Plan 03 só precisa consumir a prop `empresas` já formatada.

Purpose: Fechar a fatia backend completa antes de qualquer mudança visual. Garante que o
diff (DEV-03) é populado pelo próprio service em produção (não só nos testes), e que o
dispatch (DEV-04) é seguro contra empresas sem conta Adman configurada.

Output:
- DevController com index() (props para Inertia) + dispatchSync() (job + flash)
- AdmanService::syncCompany() grava AdmanSyncLog em sucesso e erro
- Rotas dev.desenvolvimento (GET, agora apontando para DevController) e dev.adman.sync (POST)
- Suite DevControllerTest GREEN (5/5)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-diagn-stico-adman/01-CONTEXT.md
@.planning/phases/01-diagn-stico-adman/01-RESEARCH.md
@.planning/phases/01-diagn-stico-adman/01-PATTERNS.md
@.planning/phases/01-diagn-stico-adman/01-VALIDATION.md
@.planning/phases/01-diagn-stico-adman/01-01-SUMMARY.md
@CLAUDE.md
@app/Services/AdmanService.php
@app/Models/AdmanMetric.php
@app/Models/AdmanSyncLog.php
@app/Models/Company.php
@app/Jobs/SyncAdmanCompanyJob.php
@app/Http/Controllers/DashboardController.php
@app/Http/Controllers/SugadorController.php
@routes/web.php

<interfaces>
<!-- Contratos extraídos do codebase + criados na Plan 01 -->

AdmanService::syncCompany (assinatura existente, a ser modificada):
- public function syncCompany(Company $company, ?string $date = null): AdmanMetric
- Linha 86 do arquivo atual: $metric = AdmanMetric::updateOrCreate([...], [...]);
- Linha 110 do arquivo atual: try { $this->syncCampaigns(...); } catch... (NÃO mexer)
- Ponto de inserção: ENTRE linha 108 (final do updateOrCreate) e linha 110 (try syncCampaigns)
- Eloquent API: $metric->wasRecentlyCreated (bool), $metric->wasChanged() (bool)

Company::latestMetrics() (já existente):
- return $this->hasOne(AdmanMetric::class)->latestOfMany('reference_date');
- USAR ESTE método (não criar latestAdmanMetric duplicado — confirmado em PATTERNS.md item 1)

Company::latestAdmanSyncLog() (criado na Plan 01):
- return $this->hasOne(AdmanSyncLog::class)->latestOfMany('created_at');

AdmanSyncLog (criado na Plan 01):
- fillable: company_id, created_count, updated_count, skipped_count, error_message, synced_at
- belongsTo(Company::class)

SyncAdmanCompanyJob (existente — não modificar):
- ::dispatch($company) — enfileira na queue 'database'
- Construtor: (public readonly Company $company, public readonly ?string $date = null)
- backoff(): [60, 300, 900]
- failed(\Throwable $e): Log::error("[SyncAdmanCompanyJob] ...")

DashboardController analog (app/Http/Controllers/DashboardController.php):
- Padrão de namespace App\Http\Controllers + use Inertia\Inertia
- Padrão Inertia::render('Component/Path', ['prop' => $data])

SugadorController analog (app/Http/Controllers/SugadorController.php):
- Padrão return back()->with('success', '...') após escrita

Rotas existentes em routes/web.php (linha 134-173 — grupo role:admin):
- Linha 139-140: Route::get('/dev/desenvolvimento', fn () => \Inertia\Inertia::render('Dev/Desenvolvimento'))->name('dev.desenvolvimento');
- Este closure será SUBSTITUÍDO por DevController::index
- Nova rota POST será ADICIONADA imediatamente após

Shape de cada item em $empresas (consumido pelo Desenvolvimento.jsx na Plan 03):
[
  'id'          => int,
  'name'        => string,
  'synced_at'   => string|null (ISO 8601),
  'raw_data'    => array|null,
  'criados'     => int|null,
  'atualizados' => int|null,
  'ignorados'   => int|null,
  'error'       => string|null,
  'status'      => 'ok' | 'erro' (derivado de error != null)
]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modificar AdmanService::syncCompany para registrar AdmanSyncLog</name>
  <files>
    app/Services/AdmanService.php
  </files>
  <read_first>
    - app/Services/AdmanService.php (ler 1-220 — entender estado atual do método)
    - app/Models/AdmanSyncLog.php (criado na Plan 01)
    - .planning/phases/01-diagn-stico-adman/01-PATTERNS.md (seção AdmanService — Padrão wasRecentlyCreated + error handling)
    - .planning/phases/01-diagn-stico-adman/01-RESEARCH.md (Padrão 2 — captura de diff)
  </read_first>
  <behavior>
    - Após o updateOrCreate (atualmente linha 86-108), capturar wasRecentlyCreated e wasChanged() ANTES de qualquer outra operação no $metric
    - Criar uma linha AdmanSyncLog com:
      * company_id = $company->id
      * created_count = $metric->wasRecentlyCreated ? 1 : 0
      * updated_count = (!$metric->wasRecentlyCreated && $metric->wasChanged()) ? 1 : 0
      * skipped_count = (!$metric->wasRecentlyCreated && !$metric->wasChanged()) ? 1 : 0
      * error_message = null
      * synced_at = now()
    - Em caso de \Throwable durante o método syncCompany (HTTP error, etc.), criar AdmanSyncLog com:
      * created_count = 0, updated_count = 0, skipped_count = 0
      * error_message = $e->getMessage() (truncar se > 65535 chars não é necessário — coluna é text)
      * synced_at = now()
      * E re-throw a exceção para preservar comportamento existente (job entra em failed/retry)
    - syncCampaigns NÃO conta como erro do syncCompany se falhar (mantém try/catch atual com Log::warning)
    - syncCompany continua retornando AdmanMetric (assinatura inalterada)
  </behavior>
  <action>
    Editar app/Services/AdmanService.php. Adicionar `use App\Models\AdmanSyncLog;` no bloco de
    imports do topo (após `use App\Models\AdmanCampaignMetric;` na linha 4, mantendo ordem
    alfabética: AdmanCampaignMetric → AdmanMetric → AdmanSyncLog → Company).

    Envolver o corpo INTEIRO do método syncCompany (atualmente linhas 55-117) com try/catch
    \Throwable. Estrutura:

    1. Manter o cabeçalho do método e a obtenção de $date/$custId/$performance/etc inalterados
    2. ENVOLVER toda a lógica de fetch + updateOrCreate + syncCampaigns em try {}
    3. Imediatamente APÓS o updateOrCreate retornar $metric (atualmente linha 108) e ANTES do
       try interno do syncCampaigns (atualmente linha 110), INSERIR o registro de sucesso:

       - Calcular $foiCriado     = $metric->wasRecentlyCreated;
       - Calcular $foiAtualizado = !$foiCriado && $metric->wasChanged();
       - Calcular $foiIgnorado   = !$foiCriado && !$metric->wasChanged();
       - AdmanSyncLog::create([
           'company_id'    => $company->id,
           'created_count' => $foiCriado     ? 1 : 0,
           'updated_count' => $foiAtualizado ? 1 : 0,
           'skipped_count' => $foiIgnorado   ? 1 : 0,
           'error_message' => null,
           'synced_at'     => now(),
         ]);

    4. No catch (\Throwable $e) externo:
       - AdmanSyncLog::create com created_count=0/updated_count=0/skipped_count=0 + error_message=$e->getMessage() + synced_at=now()
       - throw $e; (re-throw para preservar SyncAdmanCompanyJob retry/failed behavior)

    Comentários em pt-BR ANTES dos blocos novos, explicando o propósito:
    - "Registro de diff para o painel de diagnóstico (DEV-03)"
    - "Registro de erro mesmo em falha — admin precisa enxergar (DEV-02)"

    NÃO modificar nenhum outro método do AdmanService (syncAll, syncCampaigns, fetchPerformance,
    fetchCampaigns, syncHistorical, fetchAccountMetrics, listAccounts, fetchAllCampaigns,
    fetchCampaignsRange, fetchAdsMetrics permanecem intactos).

    Após editar, rodar `php artisan test --filter DevControllerTest::test_index_retorna_diff_do_ultimo_log`
    isoladamente para sanity check da lógica de diff (deve continuar vermelho por causa do
    controller ausente, mas não pode mostrar erro de classe AdmanSyncLog não encontrada).
  </action>
  <verify>
    <automated>grep -c "AdmanSyncLog::create" app/Services/AdmanService.php</automated>
    <automated>grep -c "wasRecentlyCreated" app/Services/AdmanService.php</automated>
    <automated>grep "use App\\\\Models\\\\AdmanSyncLog" app/Services/AdmanService.php</automated>
    <automated>php -l app/Services/AdmanService.php</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "AdmanSyncLog::create" app/Services/AdmanService.php retorna exatamente 2 (sucesso + erro)
    - grep -c "wasRecentlyCreated" app/Services/AdmanService.php retorna 1
    - grep -c "wasChanged" app/Services/AdmanService.php retorna 2 (uma para updated, outra para skipped)
    - grep "use App\\\\Models\\\\AdmanSyncLog" app/Services/AdmanService.php retorna 1 match
    - php -l app/Services/AdmanService.php → "No syntax errors detected"
    - Assinatura de syncCompany() inalterada (continua retornando AdmanMetric): grep "public function syncCompany(Company \$company, ?string \$date = null): AdmanMetric" retorna 1 match
    - O método syncAll permanece com 8 linhas de corpo (inalterado) — grep -A3 "public function syncAll" mostra a estrutura original
    - Existe `throw $e;` no bloco catch após o AdmanSyncLog::create de erro
  </acceptance_criteria>
  <done>
    AdmanService::syncCompany() registra AdmanSyncLog em sucesso (com diff correto via
    wasRecentlyCreated/wasChanged) e em erro (com mensagem preservada), re-throw da exceção
    mantido para integração com SyncAdmanCompanyJob.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar DevController com index() e dispatchSync()</name>
  <files>
    app/Http/Controllers/DevController.php
  </files>
  <read_first>
    - app/Http/Controllers/DashboardController.php (analog: query + map + Inertia::render)
    - app/Http/Controllers/SugadorController.php (analog: dispatch + back()->with('success'))
    - app/Models/Company.php (latestMetrics + latestAdmanSyncLog relationships)
    - app/Models/AdmanMetric.php (campos disponíveis: synced_at, raw_data)
    - app/Models/AdmanSyncLog.php (campos: created_count, updated_count, skipped_count, error_message)
    - app/Jobs/SyncAdmanCompanyJob.php (assinatura dispatch)
    - .planning/phases/01-diagn-stico-adman/01-PATTERNS.md (seção DevController.php — code completo)
    - .planning/phases/01-diagn-stico-adman/01-RESEARCH.md (Padrão 1 — Controller passando props)
  </read_first>
  <behavior>
    index():
    - Query: Company::where('active', true)->whereNotNull('adman_account_id')->where('adman_account_id', '!=', '')->with(['latestMetrics', 'latestAdmanSyncLog'])->get()
    - Map para array escalar (não passar Eloquent diretamente ao Inertia) com chaves: id, name, synced_at (ISO 8601 ou null), raw_data, criados, atualizados, ignorados, error, status ('erro' se error != null else 'ok')
    - Return Inertia::render('Dev/Desenvolvimento', ['empresas' => $empresas])

    dispatchSync(Company $company):
    - abort_unless($company->adman_account_id, 422, 'Empresa sem conta Adman configurada.')
    - SyncAdmanCompanyJob::dispatch($company)
    - return back()->with('success', "Sync enfileirado para {$company->name}.")

    Não precisa de check de autorização no controller — middleware role:admin do grupo de
    rotas (Task 3) garante 403 para não-admins.
  </behavior>
  <action>
    Criar app/Http/Controllers/DevController.php em namespace App\Http\Controllers, estendendo
    Controller (a classe base já é importada implicitamente em Laravel 12 — confirmar olhando
    DashboardController; não precisa importar Controller manualmente se o analog não importa).

    Imports obrigatórios no topo (ordem alfabética conforme convenção CONVENTIONS.md):
    - use App\Jobs\SyncAdmanCompanyJob;
    - use App\Models\Company;
    - use Inertia\Inertia;

    Método 1 — public function index(): \Inertia\Response
    - Comentário pt-BR explicando: "Lista empresas com conta Adman para diagnóstico de sync"
    - Query Company conforme behavior acima usando ->with(['latestMetrics', 'latestAdmanSyncLog']) (eager loading)
    - ->get()->map(fn(Company $c) => [...]) montando o array escalar com TODAS as chaves:
      * 'id' => $c->id
      * 'name' => $c->name
      * 'synced_at' => $c->latestMetrics?->synced_at?->toIso8601String()
      * 'raw_data' => $c->latestMetrics?->raw_data
      * 'criados' => $c->latestAdmanSyncLog?->created_count
      * 'atualizados' => $c->latestAdmanSyncLog?->updated_count
      * 'ignorados' => $c->latestAdmanSyncLog?->skipped_count
      * 'error' => $c->latestAdmanSyncLog?->error_message
      * 'status' => $c->latestAdmanSyncLog?->error_message ? 'erro' : 'ok'
    - return Inertia::render('Dev/Desenvolvimento', ['empresas' => $empresas]);

    Método 2 — public function dispatchSync(Company $company): \Illuminate\Http\RedirectResponse
    - Comentário pt-BR explicando: "Enfileira sync Adman manual para uma empresa específica (DEV-04)"
    - abort_unless($company->adman_account_id, 422, 'Empresa sem conta Adman configurada.');
    - SyncAdmanCompanyJob::dispatch($company);
    - return back()->with('success', "Sync enfileirado para {$company->name}.");

    Usar latestMetrics (já existe na Company) — NÃO criar latestAdmanMetric duplicado.

    NÃO usar response()->json() em dispatchSync — é rota Inertia; back()->with é o padrão correto
    para acionar o toast via AppLayout (anti-pattern documentado em RESEARCH.md).

    NÃO adicionar try/catch no controller — exceções do job são problema do worker; do
    controller apenas precisamos dispatchar.
  </action>
  <verify>
    <automated>test -f app/Http/Controllers/DevController.php && echo "FILE_OK"</automated>
    <automated>php -l app/Http/Controllers/DevController.php</automated>
    <automated>grep -c "public function index" app/Http/Controllers/DevController.php</automated>
    <automated>grep -c "public function dispatchSync" app/Http/Controllers/DevController.php</automated>
    <automated>grep "SyncAdmanCompanyJob::dispatch" app/Http/Controllers/DevController.php</automated>
    <automated>grep "abort_unless" app/Http/Controllers/DevController.php</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo app/Http/Controllers/DevController.php existe
    - php -l → "No syntax errors detected"
    - grep "class DevController extends Controller" app/Http/Controllers/DevController.php retorna 1 match
    - grep "namespace App\\\\Http\\\\Controllers" app/Http/Controllers/DevController.php retorna 1 match
    - grep "public function index" app/Http/Controllers/DevController.php retorna 1 match
    - grep "public function dispatchSync" app/Http/Controllers/DevController.php retorna 1 match
    - grep "use App\\\\Jobs\\\\SyncAdmanCompanyJob" app/Http/Controllers/DevController.php retorna 1 match
    - grep "use App\\\\Models\\\\Company" app/Http/Controllers/DevController.php retorna 1 match
    - grep "use Inertia\\\\Inertia" app/Http/Controllers/DevController.php retorna 1 match
    - grep "Inertia::render('Dev/Desenvolvimento'" app/Http/Controllers/DevController.php retorna 1 match
    - grep "abort_unless(\$company->adman_account_id" app/Http/Controllers/DevController.php retorna 1 match
    - grep "back()->with('success'" app/Http/Controllers/DevController.php retorna 1 match
    - grep "latestMetrics" app/Http/Controllers/DevController.php retorna pelo menos 2 matches (with + leitura)
    - grep "latestAdmanSyncLog" app/Http/Controllers/DevController.php retorna pelo menos 4 matches (with + 4 leituras: criados/atualizados/ignorados/error)
  </acceptance_criteria>
  <done>
    DevController criado com index() retornando array escalar pronto para Inertia e
    dispatchSync() seguro (abort 422 sem adman_account_id) enfileirando o job correto.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Substituir closure em routes/web.php por DevController e adicionar rota POST de sync</name>
  <files>
    routes/web.php
  </files>
  <read_first>
    - routes/web.php (ler 1-240 — entender grupo role:admin e closure atual na linha 139-140)
    - app/Http/Controllers/DevController.php (criado na Task 2 — verificar nomes dos métodos)
    - .planning/phases/01-diagn-stico-adman/01-PATTERNS.md (seção routes/web.php — modificação exata)
  </read_first>
  <behavior>
    - O import use App\Http\Controllers\DevController; é adicionado no bloco de imports do topo
      em ordem alfabética (entre DashboardController e GoalController)
    - A linha 139-140 atual (closure de /dev/desenvolvimento) é SUBSTITUÍDA por
      Route::get('/dev/desenvolvimento', [DevController::class, 'index'])->name('dev.desenvolvimento');
    - IMEDIATAMENTE APÓS essa linha, dentro do mesmo grupo middleware('role:admin'), é ADICIONADA
      a rota POST: Route::post('/dev/adman/{company}/sync', [DevController::class, 'dispatchSync'])->name('dev.adman.sync');
    - Route model binding {company} resolve Company por id automaticamente (Laravel 404 se não existir)
    - Nenhuma outra rota é modificada
    - Após essa mudança, todos os 5 testes DevControllerTest viram verdes
  </behavior>
  <action>
    Editar routes/web.php aplicando duas mudanças:

    Mudança 1 — bloco de imports do topo (atualmente linhas 1-23):
    Adicionar `use App\Http\Controllers\DevController;` em ordem alfabética. A ordem atual segue
    PascalCase do nome do Controller — inserir entre `use App\Http\Controllers\DashboardController;`
    (linha 7) e `use App\Http\Controllers\GoalController;` (linha 8). Resultado: D-Dashboard → D-Dev → G-Goal.

    Mudança 2 — dentro do grupo role:admin (atualmente linhas 134-173):
    Localizar o bloco atual (linhas 138-140):
    ```
    // Dev — area interna de projetos em desenvolvimento
    Route::get('/dev/desenvolvimento', fn () => \Inertia\Inertia::render('Dev/Desenvolvimento'))
        ->name('dev.desenvolvimento');
    ```

    SUBSTITUIR por:
    ```
    // Dev — area interna de projetos em desenvolvimento
    Route::get('/dev/desenvolvimento', [DevController::class, 'index'])
        ->name('dev.desenvolvimento');
    Route::post('/dev/adman/{company}/sync', [DevController::class, 'dispatchSync'])
        ->name('dev.adman.sync');
    ```

    Preservar o comentário pt-BR existente. Garantir que as novas rotas ficam DENTRO do
    `Route::middleware('role:admin')->group(function () { ... })` — verificar que a chave de
    fechamento `});` na linha 173 vem DEPOIS das duas novas rotas.

    NÃO mexer em nenhuma outra rota do arquivo (administrativo, mlb, sugadores, grants, users,
    companies, profile, dashboard, etc.).

    Após editar, rodar `php artisan route:list --columns=method,uri,name | grep dev.adman` para
    confirmar que as duas rotas aparecem. Depois rodar a suite completa:
    `php artisan test --filter DevControllerTest` — esperado: 5 testes VERDES.
  </action>
  <verify>
    <automated>php -l routes/web.php</automated>
    <automated>grep "use App\\\\Http\\\\Controllers\\\\DevController" routes/web.php</automated>
    <automated>grep "\\[DevController::class, 'index'\\]" routes/web.php</automated>
    <automated>grep "\\[DevController::class, 'dispatchSync'\\]" routes/web.php</automated>
    <automated>grep "dev.adman.sync" routes/web.php</automated>
    <automated>php artisan route:list --columns=method,uri,name 2>&1 | grep -E "dev\\.(desenvolvimento|adman\\.sync)"</automated>
    <automated>php artisan test --filter DevControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - php -l routes/web.php → "No syntax errors detected"
    - grep "use App\\\\Http\\\\Controllers\\\\DevController" routes/web.php retorna 1 match
    - grep "fn () => \\\\Inertia\\\\Inertia::render('Dev/Desenvolvimento')" routes/web.php retorna 0 matches (closure removida)
    - grep "\\[DevController::class, 'index'\\]" routes/web.php retorna 1 match
    - grep "\\[DevController::class, 'dispatchSync'\\]" routes/web.php retorna 1 match
    - grep "'dev.adman.sync'" routes/web.php retorna 1 match
    - grep "'dev.desenvolvimento'" routes/web.php retorna 1 match
    - php artisan route:list mostra as rotas `dev.desenvolvimento` (GET) e `dev.adman.sync` (POST), ambas com middleware role:admin
    - `php artisan test --filter DevControllerTest` retorna PASS para os 5 testes (5/5 green)
    - Suite completa não regrediu: `php artisan test --filter ProfileTest` continua verde
  </acceptance_criteria>
  <done>
    Rotas migradas para DevController, suite DevControllerTest 5/5 verde, backend completo de
    DEV-01/02/03/04 entregue. Pronto para a fatia UI (Plan 03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→app (GET /dev/desenvolvimento) | Admin autenticado; middleware role:admin já no grupo |
| browser→app (POST /dev/adman/{company}/sync) | CSRF via Inertia X-XSRF-TOKEN automático; role:admin no grupo |
| controller→queue | Dispatch enfileira job — sem execução síncrona (evita timeout nginx) |
| service→external API | AdmanService → Adman HTTP API (já existia); risco capturado em error_message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | GET/POST /dev/* | mitigate | Middleware role:admin via EnsureUserHasRole já aplicado ao grupo (routes/web.php linha 134); confirmado em RESEARCH Security Domain |
| T-02-02 | Tampering | POST /dev/adman/{company}/sync (CSRF) | mitigate | Inertia envia X-XSRF-TOKEN automaticamente; CSRF middleware no grupo web — sem opt-out |
| T-02-03 | Tampering | dispatchSync com Company sem adman_account_id | mitigate | abort_unless($company->adman_account_id, 422, 'Empresa sem conta Adman configurada.') no controller — evita job inútil/quebrado |
| T-02-04 | Information Disclosure | raw_data exposto na prop empresas | accept | Endpoint protegido por role:admin; payload Adman não contém PII (apenas métricas agregadas — grossBilling, netBilling, TACOS, soldQty); CONTEXT.md confirma escopo admin-only |
| T-02-05 | Repudiation | dispatch sem audit | accept | Job já loga falhas via SyncAdmanCompanyJob::failed; log de sucesso vem do AdmanSyncLog (Plan 01 + Task 1 desta plan); ALERT-01 (notificação de falha) deferido para v2 conforme STATE.md |
| T-02-06 | Denial of Service | Admin disparando syncs em loop | accept | role:admin reduz superfície a usuários confiáveis; job tem timeout=120s e tries=3; volume típico < 50 empresas. Rate limit pode ser adicionado em fase posterior se necessário |
| T-02-SC | Tampering | Package installs | accept | Zero pacotes instalados nesta plan — somente edição de arquivos PHP existentes |
</threat_model>

<verification>
**Confirmar antes de avançar para Plan 03:**

1. `php artisan test --filter DevControllerTest` → 5/5 VERDE
2. `php artisan route:list --columns=method,uri,name | grep dev` mostra:
   - GET /dev/desenvolvimento dev.desenvolvimento (middleware role:admin)
   - POST /dev/adman/{company}/sync dev.adman.sync (middleware role:admin)
3. `php artisan test` (suite completa) sem regressão
4. Smoke manual opcional: `php artisan tinker` → `App\Models\Company::with(['latestMetrics','latestAdmanSyncLog'])->whereNotNull('adman_account_id')->get()` retorna coleção sem erro de relacionamento
5. Acessar GET /dev/desenvolvimento como admin no navegador retorna a página atual (sem UI nova ainda) — a prop empresas chega no React mas ainda não é renderizada (Plan 03)
</verification>

<success_criteria>
- [x] app/Services/AdmanService.php registra AdmanSyncLog em sucesso e erro (2 ocorrências de AdmanSyncLog::create)
- [x] app/Http/Controllers/DevController.php existe com index() + dispatchSync()
- [x] routes/web.php tem GET dev.desenvolvimento (DevController) + POST dev.adman.sync, ambas em grupo role:admin
- [x] Suite DevControllerTest GREEN 5/5
- [x] Zero regressão em outras suites
- [x] Comentários pt-BR mantidos em todos os arquivos editados
- [x] Assinatura pública de AdmanService::syncCompany inalterada (retorna AdmanMetric)
</success_criteria>

<output>
Criar `.planning/phases/01-diagn-stico-adman/01-02-SUMMARY.md` ao final, registrando:
- Output de `php artisan test --filter DevControllerTest` (deve mostrar 5 PASS)
- Output de `php artisan route:list | grep dev` (confirmação das duas rotas)
- Confirmação de zero regressão (suite completa)
- Quaisquer ajustes de signature/imports tomados (e o porquê)
</output>
