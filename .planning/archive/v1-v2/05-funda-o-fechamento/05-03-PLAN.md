---
phase: 05-funda-o-fechamento
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - resources/js/Pages/Admin/Financeiro.jsx
  - resources/js/Layouts/AppLayout.jsx
autonomous: false
requirements:
  - FCH-01
  - CFG-01

must_haves:
  truths:
    - "O label no sidebar para a rota admin.financeiro exibe 'Fechamento' (não 'Financeiro')"
    - "A tela /administrativo/financeiro exibe todas as empresas ativas, ordenadas alfabeticamente"
    - "Empresas sem adman_account_id (has_adman=false) exibem badge 'Sem integração' na linha da lista"
    - "Clicar no nome de uma empresa expande o accordion inline; clicar novamente fecha; abrir outra empresa fecha a anterior"
    - "O accordion exibe select de tipo de serviço (POLO/Assessoria/Incubadora) e dois inputs de data (início/término de contrato)"
    - "Submeter o formulário via 'Salvar dados' dispara PATCH para admin.financeiro.update; flash de sucesso aparece e accordion fecha"
    - "Dados salvos persistem após recarregar a página (input de data exibe YYYY-MM-DD corretamente pré-preenchido)"
  artifacts:
    - path: "resources/js/Pages/Admin/Financeiro.jsx"
      provides: "Tela Fechamento completa com lista, accordion e formulário inline"
      min_lines: 150
    - path: "resources/js/Layouts/AppLayout.jsx"
      provides: "Label 'Fechamento' na linha 51 do array NAV_ITEMS"
      contains: "Fechamento"
  key_links:
    - from: "resources/js/Pages/Admin/Financeiro.jsx"
      to: "admin.financeiro.update"
      via: "patch(route('admin.financeiro.update', empresa.id), { preserveScroll: true })"
      pattern: "admin.financeiro.update"
    - from: "FechamentoAccordion ServiceForm"
      to: "empresa props"
      via: "useForm({ service_type: empresa.service_type ?? '', contract_start: empresa.contract_start ?? '', contract_end: empresa.contract_end ?? '' })"
      pattern: "useForm.*service_type"
    - from: "AppLayout NAV_ITEMS[51]"
      to: "admin.financeiro route"
      via: "label: 'Fechamento', routeName: 'admin.financeiro'"
      pattern: "Fechamento.*admin.financeiro"
---

<objective>
UI final da Phase 5: reescrever Financeiro.jsx como tela Fechamento funcional + renomear label sidebar de "Financeiro" para "Fechamento" + npm run build + checkpoint humano de verificação visual.

Purpose: Conectar o backend (Plan 02) à interface — o admin pode ver todas as empresas ativas, identificar as sem integração Adman, e editar tipo de serviço e datas de contrato via accordion inline.
Output: Financeiro.jsx reescrito com FechamentoList, FechamentoRow, FechamentoAccordion e ServiceForm; AppLayout.jsx com label corrigido; build de produção gerado; verificação humana aprovada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-funda-o-fechamento/05-CONTEXT.md
@.planning/phases/05-funda-o-fechamento/05-UI-SPEC.md
@.planning/phases/05-funda-o-fechamento/05-PATTERNS.md
@.planning/phases/05-funda-o-fechamento/05-02-SUMMARY.md

<interfaces>
<!-- Prop shape entregue pelo AdminController::fechamento() (Plan 02) -->

companies: Array<{
  id: number,
  name: string,
  service_type: 'polo' | 'assessoria' | 'incubadora' | null,
  contract_start: string | null,   // formato YYYY-MM-DD (já toDateString do Carbon)
  contract_end: string | null,     // formato YYYY-MM-DD (já toDateString do Carbon)
  additional_service: string | null,
  has_adman: boolean,
}>

<!-- Rota disponível após Plan 02 -->
route('admin.financeiro.update', empresa.id) → PATCH /administrativo/financeiro/{id}

<!-- Padrão accordion (Desenvolvimento.jsx SyncAdmanSection, linhas 180-211) -->
const [aberta, setAberta] = useState(null);
function toggleEmpresa(id) { setAberta(prev => prev === id ? null : id); }
{aberta === empresa.id && <FechamentoAccordion empresa={empresa} onClose={() => setAberta(null)} />}

<!-- Padrão useForm patch (Sugadores/Config.jsx adaptado) -->
const { data, setData, patch, processing, errors } = useForm({
    service_type: empresa.service_type ?? '',
    contract_start: empresa.contract_start ?? '',
    contract_end: empresa.contract_end ?? '',
});
function submit(e) {
    e.preventDefault();
    patch(route('admin.financeiro.update', empresa.id), {
        preserveScroll: true,
        onSuccess: () => onClose(),
    });
}

<!-- AppLayout linha 51 atual -->
{ label: 'Financeiro', routeName: 'admin.financeiro', page: 'Admin/Financeiro', icon: Banknote, roles: ['admin'] },

<!-- formatDate em @/lib/utils -->
export function formatDate(date) {
    if (!date) return '-';
    return new Date(date).toLocaleDateString('pt-BR', { timeZone: 'America/Sao_Paulo' });
}

<!-- Tokens de design system relevantes -->
ecf-yellow: #ffe600 — chevron ativo, botão Salvar, bordas de destaque
bg-black/30 — fundo do accordion expandido
divide-white/[0.04] — divisores de lista
border-white/[0.08] — bordas sutis
bg-white/[0.03] — fundo de hover suave / inputs
text-white/40 — texto secundário
bg-white/[0.05] — fundo de linha expandida
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever Financeiro.jsx e corrigir AppLayout.jsx</name>
  <files>
    resources/js/Pages/Admin/Financeiro.jsx
    resources/js/Layouts/AppLayout.jsx
  </files>
  <read_first>
    - resources/js/Pages/Admin/Financeiro.jsx — ler o arquivo atual (placeholder); entender o que existe antes de reescrever
    - resources/js/Pages/Dev/Desenvolvimento.jsx — padrão de accordion: EmpresaRow (linhas 126-177), EmpresaAccordion (linhas 102-123), SyncAdmanSection (linhas 180-211); extrair estrutura de cn(), ChevronDown rotate, useState(null), toggleEmpresa
    - resources/js/Pages/Sugadores/Config.jsx — padrão useForm() com put (adaptar para patch); extrair inicialização com dados do prop, processing, errors, preserveScroll
    - resources/js/Layouts/AppLayout.jsx — localizar linha 51 (array NAV_ITEMS entrada do Administrativo 'Financeiro') para edição pontual
    - resources/js/lib/utils.js — confirmar assinatura e comportamento de formatDate() e cn()
  </read_first>
  <action>
    PARTE A — AppLayout.jsx (edição cirúrgica, 1 linha):
    Na linha 51 do array NAV_ITEMS, mudar o valor da propriedade label de 'Financeiro' para 'Fechamento'. Todas as outras propriedades (routeName: 'admin.financeiro', page: 'Admin/Financeiro', icon: Banknote, roles: ['admin']) permanecem inalteradas — per D-01.

    PARTE B — Financeiro.jsx (reescrita completa):
    Substituir o conteúdo completo do arquivo pelo novo componente. O arquivo deve seguir a estrutura:

    Imports (nenhum pacote novo):
    - import AppLayout from '@/Layouts/AppLayout'
    - import { useForm } from '@inertiajs/react'
    - import { useState } from 'react'
    - import { Banknote, ChevronDown, Building2, WifiOff } from 'lucide-react'
    - import { cn, formatDate } from '@/lib/utils'

    Constantes locais:
    - SERVICE_LABELS = { polo: 'POLO', assessoria: 'Assessoria', incubadora: 'Incubadora' }
    - SERVICE_COLORS = { polo: 'bg-blue-500/10 text-blue-300 border-blue-500/20', assessoria: 'bg-purple-500/10 text-purple-300 border-purple-500/20', incubadora: 'bg-emerald-500/10 text-emerald-300 border-emerald-500/20' }

    Sub-componentes locais (definidos no mesmo arquivo, não exportados):

    ServiceBadge({ tipo }):
    - Se tipo é null: span "Sem tipo" com classes 'bg-white/[0.05] text-white/40 border-white/[0.08] text-[11px] font-semibold px-2 py-0.5 rounded-full border'
    - Se tipo existe: span com SERVICE_LABELS[tipo] e SERVICE_COLORS[tipo] + mesmas classes de tamanho/padding/border

    IntegrationBadge():
    - span "Sem integração" com ícone WifiOff size=10 shrink-0
    - Classes: 'inline-flex items-center gap-1 px-2 py-0.5 rounded-full border text-[11px] font-semibold bg-amber-500/10 text-amber-300 border-amber-500/20'

    FechamentoRow({ empresa, expandida, onToggle }):
    - div clicável com onClick={onToggle} e className={cn('flex items-center gap-4 px-4 py-3 cursor-pointer transition-colors', expandida ? 'bg-white/[0.05]' : 'hover:bg-white/[0.03]')}
    - ChevronDown size=14 com className={cn('transition-transform duration-200 shrink-0', expandida ? 'rotate-180 text-ecf-yellow' : 'text-white/40')}
    - span flex-1 com nome da empresa: 'flex-1 text-white font-semibold text-[13px] truncate'
    - ServiceBadge com empresa.service_type (shrink-0)
    - IntegrationBadge quando !empresa.has_adman (shrink-0)
    - Coluna de datas: se empresa.contract_start e empresa.contract_end: formatDate(contract_start) + ' – ' + formatDate(contract_end); se apenas contract_start: formatDate(contract_start) + ' –'; se nenhuma: '—'. Classes: 'text-white/40 text-[13px] font-mono shrink-0'

    FechamentoAccordion({ empresa, onClose }):
    - Contém ServiceForm internamente
    - Container: 'px-4 py-4 bg-black/30 border-t border-white/[0.04]'

    ServiceForm({ empresa, onClose }) — componente interno ou parte de FechamentoAccordion:
    - useForm({ service_type: empresa.service_type ?? '', contract_start: empresa.contract_start ?? '', contract_end: empresa.contract_end ?? '' })
    - submit() chama patch(route('admin.financeiro.update', empresa.id), { preserveScroll: true, onSuccess: () => onClose() })
    - Layout interno: grid grid-cols-1 sm:grid-cols-3 gap-4
    - Campo "Tipo de serviço": label uppercase + select nativo com opções '' → 'Selecionar tipo...', polo → 'POLO', assessoria → 'Assessoria', incubadora → 'Incubadora'. value={data.service_type} onChange={e => setData('service_type', e.target.value)}. Classe do select: 'w-full h-9 pl-3 pr-8 rounded-lg border border-white/[0.08] bg-white/[0.03] text-[13px] text-white/80 focus:outline-none focus:border-ecf-yellow/40'
    - Campo "Início do contrato": label uppercase + input type="date" value={data.contract_start} onChange={e => setData('contract_start', e.target.value)}. Mesma classe do select + appearance-none
    - Campo "Término do contrato": idem com data.contract_end
    - Erros inline: se errors.service_type, errors.contract_start ou errors.contract_end existem, span 'text-[11px] text-red-400 mt-1' abaixo do campo
    - Labels dos campos: 'block text-[11px] uppercase tracking-wider text-white/40 mb-1'
    - Rodapé: flex items-center gap-2 mt-4
      - Botão Salvar: type="submit" disabled={processing} texto 'Salvar dados' (ou 'Salvando dados...' se processing). Classes: 'bg-ecf-yellow/10 hover:bg-ecf-yellow/20 text-ecf-yellow text-[13px] h-9 px-4 rounded-lg transition-colors font-semibold'
      - Botão Descartar: type="button" onClick={onClose}. Classes: 'text-white/40 hover:text-white/70 text-[13px] h-9 px-3 rounded-lg transition-colors'

    FechamentoList({ empresas }):
    - useState(null) para aberta
    - toggleEmpresa(id): setAberta(prev => prev === id ? null : id)
    - Se empresas.length === 0: empty state com Building2 size=24 'text-white/20', heading 'Nenhuma empresa ativa encontrada.', body 'Cadastre uma empresa com status ativo para que ela apareça aqui.'
    - Mapear empresas: div com divide-y divide-white/[0.04]; para cada empresa: FechamentoRow + {aberta === empresa.id && <FechamentoAccordion empresa={empresa} onClose={() => setAberta(null)} />}

    Componente default exportado Financeiro({ companies }):
    - AppLayout title="Fechamento"
    - main p-6 > max-w-4xl mx-auto space-y-6
    - Header block: flex items-center gap-2 mb-2 > Banknote size=20 text-ecf-yellow + h1 'Fechamento' text-xl font-semibold font-display text-white
    - Subtítulo: p 'Tipo de serviço e datas de contrato por empresa ativa.' text-[13px] text-white/40
    - Card container: div rounded-xl border border-white/[0.08] bg-white/[0.02] > FechamentoList empresas={companies}

    PROIBIDO: fetch adicional dentro do accordion; múltiplos accordions abertos; uso de modal em vez de accordion; renomear o routeName ou o arquivo JSX.
  </action>
  <verify>
    <automated>cd /c/xampp/htdocs/ecf_admin && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build completa sem erros; AppLayout.jsx linha 51 contém label: 'Fechamento'; Financeiro.jsx contém FechamentoList, FechamentoRow, FechamentoAccordion, ServiceBadge, IntegrationBadge e useForm com patch para admin.financeiro.update.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Tela Fechamento completa implementada e compilada:
    - AppLayout sidebar mostra "Fechamento" no lugar de "Financeiro" para usuário admin
    - /administrativo/financeiro lista todas as empresas ativas com badges de tipo de serviço e integração Adman
    - Accordion inline por empresa para editar tipo de serviço e datas de contrato
    - Formulário submete via PATCH com flash de sucesso e fecha o accordion após save
  </what-built>
  <how-to-verify>
    1. Abrir http://localhost/ecf_admin/public (ou o URL local da aplicação) como usuário admin
    2. Verificar que o item "Fechamento" aparece na seção Administrativo do sidebar (não mais "Financeiro")
    3. Clicar em "Fechamento" — verificar que a rota /administrativo/financeiro abre sem erro 500
    4. Verificar que a lista de empresas aparece ordenada alfabeticamente
    5. Se existir empresa sem adman_account_id no banco: verificar badge "Sem integração" (fundo amber/amarelo claro) na linha dela
    6. Clicar no nome de uma empresa — verificar que o accordion abre mostrando select de tipo de serviço e dois inputs de data
    7. Selecionar um tipo de serviço (ex: POLO) e clicar "Salvar dados" — verificar flash de sucesso e accordion fecha
    8. Recarregar a página — verificar que o badge de tipo de serviço mostra "POLO" na linha da empresa
    9. Reabrir o accordion da empresa — verificar que o select mostra "POLO" pré-selecionado
    10. Clicar em "Descartar alterações" — verificar que o accordion fecha sem salvar
    11. Clicar em empresa A para abrir accordion, depois clicar em empresa B — verificar que A fecha e B abre (apenas um aberto por vez)
  </how-to-verify>
  <resume-signal>Digite "aprovado" se todos os 11 itens passaram, ou descreva o problema encontrado</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Inertia PATCH | Formulário inline envia dados não-sanitizados do usuário (service_type via select, datas via input) |
| Props Inertia → React | Props company já são filtradas pelo controller (apenas campos explicitamente mapeados); sem dados sensíveis expostos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | ServiceForm — useForm patch | mitigate | Validação server-side no updateFechamento() (Plan 02) rejeita service_type inválido com 422; frontend mostra errors.service_type inline; select com opções fixas no frontend reduz superfície de ataque mas não é garantia de segurança — a validação backend é a barreira real |
| T-05-08 | Information Disclosure | Props Inertia — companies array | accept | Props contêm apenas campos de fechamento + has_adman; sem dados financeiros, CPF/CNPJ completos ou credenciais; acesso restrito a admin via middleware |
| T-05-09 | Tampering | AppLayout NAV_ITEMS — label string | accept | Mudança de string; sem impacto em segurança; label apenas cosmético, rota e permissão RBAC permanecem via routeName e roles |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado; npm run build usa lockfile existente — sem risco de supply chain nesta etapa |
</threat_model>

<verification>
Verificação completa do Plan 03:
- npm run build termina sem erros de compilação Vite/JSX
- AppLayout.jsx linha 51: label valor é 'Fechamento' (não 'Financeiro')
- Financeiro.jsx: contém exports default Financeiro, contém useForm, contém route('admin.financeiro.update'), contém FechamentoList, FechamentoRow
- Checkpoint humano: admin verifica sidebar, lista, accordion, save, persistência, close e exclusividade de accordion
- php artisan test: suite completa verde após npm run build (nenhuma regressão PHP)
</verification>

<success_criteria>
- npm run build sem erros
- Sidebar exibe "Fechamento" para usuário admin (verificação visual humana)
- Rota /administrativo/financeiro abre sem erro e exibe lista de empresas ativas (verificação visual humana)
- Badge "Sem integração" aparece para empresa sem adman_account_id (verificação visual humana)
- Accordion abre/fecha ao clicar; apenas um aberto por vez (verificação visual humana)
- PATCH salva e flash de sucesso aparece; accordion fecha após save (verificação visual humana)
- Dados persistem após reload da página; input date pré-preenchido corretamente (verificação visual humana)
- Checkpoint humano retorna "aprovado"
</success_criteria>

<output>
Criar `.planning/phases/05-funda-o-fechamento/05-03-SUMMARY.md` ao concluir.
</output>
